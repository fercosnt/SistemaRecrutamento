/** ⚠ ESQUELETO DE RED (Phase 47 / Plano 47-04 Task 2). O commit GREEN o substitui. */
import type { Subprocessador } from '../constants/subprocessadores'

export interface SubprocessadorFichaProps {
  readonly entrada: Subprocessador
}

export function SubprocessadorFicha(_props: SubprocessadorFichaProps) {
  return null
}
