/**
 * Wave 0 stub — populated during Waves 1-3.
 * Behaviors covered: B12.
 * See .planning/phases/03-login-recuperacao-senha/03-VALIDATION.md
 */
import { describe, it } from 'vitest'

describe('useRecoverySession — Wave 0 stubs (03-login-recuperacao-senha)', () => {
  it.todo(
    'B12: hook surfaces InvalidLinkState when onAuthStateChange does NOT fire PASSWORD_RECOVERY within timeout window (expired/single-use recovery link, T-03-05)'
  )
})
