---
phase: 25-corre-o-do-funil-lado-rh-enums-colunas-contratos
plan: 07
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - database.types.ts
autonomous: false
requirements: [FUNIL-02, FUNIL-09, FUNIL-11]
user_setup:
  - service: supabase-prod
    why: "Applying 5 migrations to the PROD database + regenerating types requires the operator's authorization to touch PROD (precedent: Phases 6–24 apply via Supabase MCP with Fernando's go-ahead)."
    dashboard_config:
      - task: "Authorize the Supabase MCP apply_migration + execute_sql calls against PROD when prompted"
        location: "Supabase MCP (apply_migration / execute_sql / generate_typescript_types)"

must_haves:
  truths:
    - "The 5 Phase-25 migrations are LIVE on PROD via Supabase MCP apply_migration (each writes its own version row; bypasses 42601)"
    - "SMOKE A: a status-only reject (etapa unchanged, no flag) RAISEs (FUNIL-02 hole closed)"
    - "SMOKE B: registrar_decisao still rejects + writes the audit row + folds status/etapa/justificativa (FUNIL-02/09)"
    - "SMOKE C: the comparativo etapa-transition reject AND the knockout auto-reject still succeed (availability — guard did not break sanctioned paths)"
    - "SMOKE D: a second registrar_decisao (amend) leaves 1 decisao_final_historico OLD.* row (FUNIL-09)"
    - "SMOKE E: upsert_pergunta_opcoes_metadata on an ATIVA vaga RAISEs P0001 and a non-owner RH RAISEs 42501; on rascunho by owner succeeds (FUNIL-11)"
    - "database.types.ts regenerated to include decisao_final_historico + the amended RPCs"
  artifacts:
    - path: "database.types.ts"
      provides: "regenerated types after the PROD apply (decisao_final_historico Row present)"
      contains: "decisao_final_historico"
  key_links:
    - from: "Supabase MCP apply_migration"
      to: "PROD supabase_migrations ledger"
      via: "5 version rows (25-01 files)"
      pattern: "20260709000010|20260709000014"
    - from: "MCP execute_sql SMOKE A–E"
      to: "guard trigger / registrar_decisao / upsert guard / decisao_final_historico"
      via: "disposable-fixture + set_config jwt.claims behavioral smokes"
      pattern: "set_config\\('request.jwt.claims'"
---

<objective>
Apply the 5 Phase-25 migrations authored in 25-01 to PROD via Supabase MCP `apply_migration`, regenerate `database.types.ts` via MCP `generate_typescript_types`, and run the live SQL behavioral smokes A–E (`execute_sql`) that are the REAL gate for FUNIL-02/09/11 — build/tsc pass without the apply (a false-positive state), so the applied objects must be proven live.

Purpose: `[BLOCKING]` — migrations author DB objects (guard trigger, history table+trigger, RPC amendments) whose behavior can only be verified against PROD. `db push` is forbidden on these PL/pgSQL `$$` bodies (42601, D-22); MCP `apply_migration` writes the version row itself and bypasses 42601. Version-row reconcile stays deferred to Phase 27.
Output: 5 live migrations + regenerated types + 5 PASSing smokes.
Non-autonomous: touching PROD requires the operator's authorization (precedent Phases 6–24).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-RESEARCH.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-VALIDATION.md

<interfaces>
<!-- Apply order (numeric): guard trigger → decisao_final_historico → registrar_decisao amend → upsert guard → submit flag -->
<!-- NOTE: 25-01 renumbered 000001–000005 → 000010–000014 (000001/000002 were already taken by committed Phase-24 SEC migrations). Same 1:1 mapping + apply order. -->
supabase/migrations/20260709000010_guard_rejeicao_auditada.sql
supabase/migrations/20260709000011_decisao_final_historico.sql
supabase/migrations/20260709000012_registrar_decisao_amend.sql
supabase/migrations/20260709000013_upsert_opcoes_status_guard.sql
supabase/migrations/20260709000014_submit_candidatura_flag.sql

Smoke idiom (precedent Phases 8/11/24): disposable fixture vaga + candidatura, set_config('request.jwt.claims', json, true) + SET ROLE authenticated to simulate an RH, ROLLBACK-free cleanup (hard-delete the fixture). RNF-07a: no smoke asserts an auto-reject by score.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Apply the 5 migrations to PROD via Supabase MCP apply_migration</name>
  <files>supabase/migrations/20260709000010_guard_rejeicao_auditada.sql, supabase/migrations/20260709000011_decisao_final_historico.sql, supabase/migrations/20260709000012_registrar_decisao_amend.sql, supabase/migrations/20260709000013_upsert_opcoes_status_guard.sql, supabase/migrations/20260709000014_submit_candidatura_flag.sql</files>
  <read_first>
    - all 5 migration files from 25-01 (must exist + pass their structural greps)
    - CLAUDE.md §Migrations + db push workaround; MEMORY: apply via Supabase MCP apply_migration (NOT db push — 42601)
  </read_first>
  <what-built>
    5 authored migration files (25-01), applied to PROD IN ORDER via Supabase MCP apply_migration (each call writes its own supabase_migrations version row). After apply, existence is confirmed via execute_sql: `to_regclass('public.decisao_final_historico')` IS NOT NULL; pg_trigger has trg_candidaturas_guard_rejeicao (BEFORE UPDATE OF status on candidaturas) + trg_decisao_final_snapshot (AFTER UPDATE on decisao_final); pg_proc has the amended registrar_decisao / upsert_pergunta_opcoes_metadata / submit_candidatura_atomic.
  </what-built>
  <how-to-verify>
    1. Confirm the 5 migration files exist and pass their 25-01 greps.
    2. Authorize the executor's Supabase MCP apply_migration calls (5, in numeric order).
    3. After apply, the executor runs execute_sql existence checks: `SELECT to_regclass('public.decisao_final_historico');` (not null); `SELECT tgname FROM pg_trigger WHERE tgname IN ('trg_candidaturas_guard_rejeicao','trg_decisao_final_snapshot');` (both present); confirm the 3 amended functions exist.
    4. Expected: all objects present; 5 version rows in supabase_migrations.
  </how-to-verify>
  <acceptance_criteria>
    - `to_regclass('public.decisao_final_historico')` returns non-null (execute_sql)
    - pg_trigger contains trg_candidaturas_guard_rejeicao AND trg_decisao_final_snapshot
    - The n8n AFTER trigger (trg_n8n_status_candidatura) + candidaturas_avancar_etapa_trg still exist (not dropped) — coexistence sanity
    - 5 new version rows present in the ledger (version-row reconcile itself → Phase 27)
  </acceptance_criteria>
  <resume-signal>Type "applied" once the 5 apply_migration calls succeed and the existence checks pass, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Regenerate database.types.ts via MCP generate_typescript_types</name>
  <files>database.types.ts</files>
  <read_first>
    - database.types.ts (repo ROOT — CLI/MCP-generated, NEVER hand-edited)
    - CLAUDE.md §db:types + MEMORY (database.types.ts at the RAIZ, regen via MCP after migrations)
  </read_first>
  <action>
    Run Supabase MCP generate_typescript_types and write the result to database.types.ts (repo root). Do NOT hand-edit. Confirm decisao_final_historico appears and the amended RPC signatures (unchanged params) are present. This does not change any frontend-referenced type (no frontend consumes decisao_final_historico this phase) — so the tsc count is unaffected (the 25-08 re-pin measures the frontend-driven count).
  </action>
  <acceptance_criteria>
    - `grep -q "decisao_final_historico" database.types.ts` succeeds
    - `npm run build` succeeds against the regenerated types
    - No manual edit markers; file is the MCP-generated output
  </acceptance_criteria>
  <verify>
    <automated>bash -c "grep -q decisao_final_historico database.types.ts && npm run build >/dev/null 2>&1 && echo TYPES_REGEN_OK"</automated>
  </verify>
  <done>database.types.ts regenerated with the new table + amended RPCs; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [CHECKPOINT] Live SQL smokes A–E via MCP execute_sql — human-confirm the FUNIL-02/09/11 proof</name>
  <files>database.types.ts</files>
  <read_first>
    - 25-RESEARCH.md §Code Examples (SQL smoke skeleton A–E + set_config example) + §1a (4-path effect matrix); 25-VALIDATION.md (invariants A–E)
    - Phase 24 behavioral-smoke precedent (set_config jwt.claims + SET ROLE authenticated + disposable fixture + ROLLBACK-free cleanup)
  </read_first>
  <action>
    Run the 5 behavioral smokes via MCP execute_sql against a disposable fixture (a rascunho vaga + a candidatura in a non-rejeitado state; simulate an RH via set_config('request.jwt.claims', …, true) + SET ROLE authenticated). Hard-delete the fixture at the end (ROLLBACK-free cleanup).
    - SMOKE A (invariant A / FUNIL-02): raw UPDATE candidaturas SET status='rejeitado' with etapa_atual unchanged and NO flag → EXPECT exception (check_violation, "Rejeição sem trilha…").
    - SMOKE B (invariant B / FUNIL-02/09): SELECT registrar_decisao(fixture, 'rejeitado', <≥50 chars>) → EXPECT ok; candidaturas.status='rejeitado' AND etapa_atual='rejeitado' AND etapa_justificativa set; +1 historico_candidatura row.
    - SMOKE C (availability / FUNIL-02): (i) UPDATE candidaturas SET status='rejeitado', etapa_atual='rejeitado' (comparativo path) → EXPECT ok (etapa-transition branch); (ii) confirm submit_candidatura_atomic knockout still auto-rejects (status→rejeitado, etapa 'inscricao', flag branch) → EXPECT ok. No auto-reject by score (RNF-07a).
    - SMOKE D (invariant C / FUNIL-09): call registrar_decisao twice for the same candidatura (amend) → EXPECT exactly 1 decisao_final_historico row carrying the OLD decisao/justificativa/por_usuario.
    - SMOKE E (invariant D / FUNIL-11): upsert_pergunta_opcoes_metadata on a pergunta of an 'ativa' vaga → EXPECT RAISE P0001; as a non-owner RH → EXPECT 42501; on a rascunho vaga owned by the caller → EXPECT ok.
    Record each PASS/FAIL in the SUMMARY. Any FAIL blocks the phase (fix the migration, re-apply, re-smoke).
  </action>
  <acceptance_criteria>
    - SMOKE A RAISEs (hole closed); SMOKE B ok + audit row + folded columns; SMOKE C both sanctioned paths ok; SMOKE D exactly 1 history row on amend; SMOKE E P0001 + 42501 + owner-rascunho ok
    - Fixture hard-deleted (no test rows left in PROD beyond intentional seeds)
    - VALIDATION invariants A/B/C/D all green
  </acceptance_criteria>
  <how-to-verify>
    The smokes run via Supabase MCP execute_sql (not a CI command) — this is the authoritative proof of the phase's core LGPD/RNF-07a guarantee, so a human explicitly confirms the results before the phase is considered proven (precedent: Phase 24 wrapped apply+smoke+review in checkpoint:human-verify with the resume-signal conditioned on recorded smoke results).
    1. Execute smokes A–E against the disposable fixture; record each PASS/FAIL in 25-07-SUMMARY.md.
    2. Present the 5 results to the operator. Any FAIL blocks the phase (fix the migration → re-apply via 25-07 Task 1 → re-smoke) — do NOT proceed on a FAIL.
    3. Confirm the fixture was hard-deleted (no test rows left in PROD).
  </how-to-verify>
  <resume-signal>Type "smokes-pass" once all 5 live smokes (A–E) are confirmed PASS and the fixture is cleaned up, or describe the FAIL so the migration can be fixed and re-applied.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → PROD DB | apply_migration + execute_sql write directly to PROD; gated by operator authorization |
| authenticated RH (simulated in smoke) → candidaturas/perguntas | the smokes exercise the exact tamper vectors the migrations close |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07-01 | Repudiation/Tampering | reject-without-trail (A9) live on PROD | mitigate | apply the guard + SMOKE A proves a raw status-only reject RAISEs |
| T-25-07-02 | Denial of Service (self) | guard breaks the sanctioned knockout/comparativo paths on PROD | mitigate | SMOKE C proves both sanctioned paths still succeed (availability) before phase completion |
| T-25-07-03 | Tampering (IDOR) | non-owner / active-vaga option edit | mitigate | apply the upsert guard + SMOKE E proves P0001 + 42501 |
| T-25-07-04 | Tampering | wrong apply order / partial apply leaves an inconsistent DB | mitigate | apply in numeric order; existence checks (Task 1) before smokes; fixture hard-cleanup |
| T-25-07-05 | Information Disclosure | smoke fixture leaks PII / stays in PROD | mitigate | disposable fixture, ROLLBACK-free hard-delete cleanup; no real candidate data used |
| T-25-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | zero package installs this plan; migrations author zero external deps |
</threat_model>

<verification>
- 5 migrations live (existence checks + 5 version rows) and database.types.ts regenerated.
- Smokes A–E all PASS (recorded in the SUMMARY) — the authoritative gate for FUNIL-02/09/11.
- Coexistence: n8n AFTER trigger + avancar_etapa trigger intact.
- Version-row reconcile is explicitly deferred to Phase 27 (DBMIG).
</verification>

<success_criteria>
- FUNIL-02/09/11 proven live on PROD via smokes A–E; types regenerated; sanctioned paths preserved (availability).
</success_criteria>

<output>
Create `.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-07-SUMMARY.md` when done (record each smoke A–E PASS/FAIL + the 5 version rows).
</output>
