---
phase: 25-corre-o-do-funil-lado-rh-enums-colunas-contratos
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/config-vaga/services/configVagaService.ts
  - src/features/config-vaga/services/__tests__/updateVagaBase.test.ts
  - src/components/pages/CriarEditarVagaPage.tsx
autonomous: true
requirements: [FUNIL-04, UX-06]

must_haves:
  truths:
    - "Editing a vaga and reloading keeps the base fields (salary/jornada/requisitos/diferenciais) AND the config (pesos/testes) — hydration reads only columns that exist and the edit-save persists base fields + status (FUNIL-04)"
    - "The 8 phantom column reads are gone — CriarEditarVagaPage type-checks against the real vagas Row (FUNIL-04)"
    - "The no-op 'Usar da Biblioteca' and template 'Preview' buttons are removed with no dangling gap (UX-06)"
  artifacts:
    - path: "src/features/config-vaga/services/configVagaService.ts"
      provides: "updateVagaBase writer — real vagas columns + status, anon-client RLS-governed"
      contains: "faixa_salarial_min"
    - path: "src/components/pages/CriarEditarVagaPage.tsx"
      provides: "hydration on real columns + 'Salvar alterações' CTA persisting base+config"
      contains: "updateVagaBase"
    - path: "src/features/config-vaga/services/__tests__/updateVagaBase.test.ts"
      provides: "unit proof the writer sends real columns and maps 42501→FORBIDDEN"
      exports: []
  key_links:
    - from: "CriarEditarVagaPage 'Salvar alterações'"
      to: "configVagaService.updateVagaBase + existing config save"
      via: "edit-path submit handler"
      pattern: "updateVagaBase"
    - from: "updateVagaBase"
      to: "supabase.from('vagas').update"
      via: "anon client (RLS vaga-scoping from Phase 24)"
      pattern: "from\\('vagas'\\)\\.update"
---

<objective>
Make Editar Vaga round-trip: hydrate from the real `vagas` columns (kill the 8 phantom reads), add a real base-field write-path (`updateVagaBase`) wired to a "Salvar alterações" CTA that persists base fields + status radio alongside the already-working config save, and remove the two no-op buttons on the page ("Usar da Biblioteca", template "Preview").

Purpose: Today editing base fields + saving persists NOTHING (only pesos/testes save, via configVagaService which works). Hydration reads 8 columns that do not exist (each a tsc error). This closes FUNIL-04 and the CriarEditarVagaPage slice of UX-06.
Output: A `updateVagaBase` writer + unit test, a rewired hydration + edit-save CTA, and the dead buttons removed. Clears ≈ −8 tsc (phantom columns) — re-pinned in 25-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-RESEARCH.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-PATTERNS.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-UI-SPEC.md

<interfaces>
<!-- Phantom→real column map (VERIFIED against database.types.ts vagas Row, all present at the cited lines) -->
faixa_salarial        → faixa_salarial_min (number|null) + faixa_salarial_max (number|null)   [L3629-3630]
carga_horaria         → jornada_trabalho (string|null)                                          [L3632]
descricao_completa    → responsabilidades and/or sobre_cargo (string|null)                      [L3643 / L3645]
requisito_formacao    → requisitos_formacao        requisito_experiencia → requisitos_experiencia [L3640 / L3639]
requisito_tecnico     → requisitos_tecnicos         requisito_comportamental → requisitos_habilidades [L3642 / L3641]
requisito_diferencial → diferenciais and/or perfil_ideal (string|null)                          [L3624 / L3635]
other real base cols: titulo, slug, departamento, cidade, estado, tipo_contrato, modelo_trabalho, nivel_senioridade, descricao_curta, subtitulo, total_vagas, exibir_salario, status

Analog writer to mirror EXACTLY (error-mapping + isForbidden): configVagaService.updateVagaConfig L59-85; ConfigVagaServiceError + isForbidden L29-53 (42501→'FORBIDDEN'). Anon client only — NEVER supabaseAdmin (CLAUDE.md Security Rules).
Config save (untouched — already works): handleSalvarRascunho L302-318 / handlePublicar L322-374 via updateVagaConfigMut; publish_vaga still called with perguntas:[] (F7 deferred — do NOT expand).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: configVagaService.updateVagaBase writer + unit test</name>
  <files>src/features/config-vaga/services/configVagaService.ts, src/features/config-vaga/services/__tests__/updateVagaBase.test.ts</files>
  <read_first>
    - src/features/config-vaga/services/configVagaService.ts — updateVagaConfig L59-85 (the working real `.from('vagas').update`), ConfigVagaServiceError + isForbidden L29-53, error-mapping L71-84
    - database.types.ts vagas Row L3612-3731 — confirm every column name in the phantom→real map above
    - existing configVagaService test(s) under src/features/config-vaga/services/__tests__/ — the supabase mock idiom (from().update().eq() chain) to reuse
    - 25-RESEARCH.md §2a/§2b; 25-PATTERNS.md "configVagaService.ts"; Shared Patterns "Service error-class + isForbidden mapping"
  </read_first>
  <behavior>
    - updateVagaBase(vagaId, base) calls supabase.from('vagas').update({...real columns..., status}).eq('id', vagaId)
    - a Supabase 42501 error is mapped to ConfigVagaServiceError code 'FORBIDDEN'
    - any other error maps to 'DATABASE_ERROR'
    - the object passed to .update contains real column names only (faixa_salarial_min/max, jornada_trabalho, responsabilidades, requisitos_*, diferenciais, status) — no phantom key
  </behavior>
  <action>
    Add `export async function updateVagaBase(vagaId: string, base: VagaBaseInput): Promise<void>` mirroring updateVagaConfig: single `.from('vagas').update({ titulo, departamento, cidade, estado, faixa_salarial_min, faixa_salarial_max, jornada_trabalho, responsabilidades, requisitos_formacao, requisitos_experiencia, requisitos_tecnicos, requisitos_habilidades, diferenciais, status })` keyed on vagaId; reuse isForbidden → ConfigVagaServiceError('…','FORBIDDEN') else ('…','DATABASE_ERROR'). Define `VagaBaseInput` (map the form field names to the real columns; the planner-verified phantom→real map is authoritative). Anon client only. Write updateVagaBase.test.ts (mock the supabase chain) asserting the behaviors above; RED first.
  </action>
  <acceptance_criteria>
    - `npm run test:run -- updateVagaBase` passes
    - `grep -q "from('vagas')" src/features/config-vaga/services/configVagaService.ts` AND `grep -q "faixa_salarial_min" src/features/config-vaga/services/configVagaService.ts` succeed
    - `grep -c "supabaseAdmin" src/features/config-vaga/services/configVagaService.ts` == 0 (anon client only)
    - No phantom column name in the writer: `grep -nE "faixa_salarial[^_]|carga_horaria|descricao_completa|requisito_" src/features/config-vaga/services/configVagaService.ts` returns nothing
  </acceptance_criteria>
  <verify>
    <automated>bash -c "npm run test:run -- updateVagaBase >/dev/null 2>&1 && grep -q faixa_salarial_min src/features/config-vaga/services/configVagaService.ts && [ \$(grep -c supabaseAdmin src/features/config-vaga/services/configVagaService.ts) -eq 0 ] && echo WRITER_OK"</automated>
  </verify>
  <done>updateVagaBase persists real columns + status, maps 42501→FORBIDDEN, anon-client only; unit test green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewire hydration to real columns + wire 'Salvar alterações' CTA</name>
  <files>src/components/pages/CriarEditarVagaPage.tsx</files>
  <read_first>
    - src/components/pages/CriarEditarVagaPage.tsx — hydration L150-173 (8 phantom reads L158-168), the `.finally` on the hydration chain L187, save handlers handleSalvarRascunho L302-318 / handlePublicar L322-374, footer L1088-1118, form field wiring for salary/jornada/requisitos/diferenciais/status radio
    - src/features/config-vaga/services/configVagaService.ts updateVagaBase (Task 1) + updateVagaConfig
    - 25-UI-SPEC.md §5 (Editar Vaga wiring; 'Salvar alterações' accent CTA on the edit path; fidelity = edit→reload keeps values); 25-RESEARCH.md §2a/§2b/§2c; 25-PATTERNS.md "CriarEditarVagaPage.tsx"
  </read_first>
  <action>
    Rewire the hydration reads (L150-173) to the real columns per the verified phantom→real map (split faixa_salarial into min/max form fields; jornada_trabalho; responsabilidades/sobre_cargo; requisitos_*; diferenciais/perfil_ideal). Disentangle the diferenciais/perfil_ideal/beneficios/pessoaCerta field semantics against the real Row (beneficios at L169 is a real column — keep it). Add a "Salvar alterações" (accent) primary CTA on the EDIT path that calls updateVagaBase (base fields + status radio) AND the existing config save (updateVagaConfigMut) — config controls unchanged. Do NOT expand publish_vaga's perguntas:[] (F7 deferred). The incidental `.finally` on the hydration `.then()` chain likely clears with the rewrite.
  </action>
  <acceptance_criteria>
    - `grep -q "updateVagaBase" src/components/pages/CriarEditarVagaPage.tsx` succeeds (edit-save wired)
    - No phantom column read remains: `grep -nE "\.faixa_salarial[^_]|\.carga_horaria|\.descricao_completa|\.requisito_" src/components/pages/CriarEditarVagaPage.tsx` returns nothing
    - `grep -q "Salvar alterações" src/components/pages/CriarEditarVagaPage.tsx` succeeds
    - `npx tsc --noEmit | grep -c "error TS"` is at least 8 lower than the pre-task count (phantom reads cleared); `npm run build` succeeds
    - VALIDATION: FUNIL-04 (edit save persists base fields; round-trip is the deferred HUMAN-UAT)
  </acceptance_criteria>
  <verify>
    <automated>bash -c "grep -q updateVagaBase src/components/pages/CriarEditarVagaPage.tsx && ! grep -nE '\\.faixa_salarial[^_]|\\.carga_horaria|\\.descricao_completa|\\.requisito_' src/components/pages/CriarEditarVagaPage.tsx && grep -q 'Salvar alterações' src/components/pages/CriarEditarVagaPage.tsx && npm run build >/dev/null 2>&1 && echo HYDRATE_OK"</automated>
  </verify>
  <done>Hydration reads real columns; 'Salvar alterações' persists base+config; phantom tsc errors cleared; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove no-op 'Usar da Biblioteca' + template 'Preview' buttons</name>
  <files>src/components/pages/CriarEditarVagaPage.tsx</files>
  <read_first>
    - src/components/pages/CriarEditarVagaPage.tsx — "📚 Usar da Biblioteca" L887-893 / L999-1005 (onClick preventDefault no-op), template "Preview" line L506
    - 25-UI-SPEC.md §4 dead-affordance table + the No-visual-regression rule (no empty container / dangling border / orphaned label after removal)
    - 25-PATTERNS.md "Dead-affordance removals"
  </read_first>
  <action>
    Remove the two "📚 Usar da Biblioteca" no-op buttons (L887-893, L999-1005) and the template "Preview" no-op (L506). Reflow adjacent real controls so no empty container, dangling border, or orphaned label remains. (If "Preview" can be trivially wired to the real slug preview instead of removed, that is acceptable per UI-SPEC §4 — but do NOT add new surface; removal is the default.)
  </action>
  <acceptance_criteria>
    - `grep -c "Usar da Biblioteca" src/components/pages/CriarEditarVagaPage.tsx` == 0
    - No `onClick={(e) => e.preventDefault()}` no-op button remains for these affordances: `grep -c "e.preventDefault()" src/components/pages/CriarEditarVagaPage.tsx` does not exceed the count for legitimate form-submit handlers (verify no orphaned no-op button)
    - `npm run build` succeeds
    - VALIDATION: UX-06 (dead-affordance sweep — CriarEditarVagaPage slice)
  </acceptance_criteria>
  <verify>
    <automated>bash -c "[ \$(grep -c 'Usar da Biblioteca' src/components/pages/CriarEditarVagaPage.tsx) -eq 0 ] && npm run build >/dev/null 2>&1 && echo AFFORDANCE_OK"</automated>
  </verify>
  <done>No-op library/preview buttons removed; layout reflows cleanly; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH browser → vagas UPDATE | base-field write is an anon-client RLS-governed table UPDATE; Phase-24 vaga-scoping governs who can UPDATE which vaga |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-01 | Tampering / Elevation | non-owner RH edits another vaga's base fields | mitigate | updateVagaBase uses the anon client under RLS (Phase 24 vaga-scoping) — NEVER supabaseAdmin/service_role client-side |
| T-25-03-02 | Tampering | writing to a phantom/wrong column silently no-ops persistence | mitigate | real-column-only writer + unit test asserting the exact keys; phantom reads removed (tsc gate proves) |
| T-25-03-03 | Denial (data loss) | config save regression while adding base save | mitigate | config write-path (updateVagaConfig) untouched; base save is additive; both invoked from 'Salvar alterações' |
| T-25-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | zero package installs this plan |
</threat_model>

<verification>
- `npm run test:run` green (updateVagaBase); `npm run build` green.
- tsc count dropped by ≥8 (phantom columns) — re-measured/re-pinned in 25-08.
- No phantom column name anywhere in the two touched files.
- Full edit→reload round-trip fidelity is the deferred HUMAN-UAT (browser).
</verification>

<success_criteria>
- Editar Vaga hydrates real columns AND persists base fields + status on edit-save (FUNIL-04).
- No phantom column read remains (tsc cleared).
- No-op library/preview buttons removed (UX-06).
</success_criteria>

<output>
Create `.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-03-SUMMARY.md` when done.
</output>
