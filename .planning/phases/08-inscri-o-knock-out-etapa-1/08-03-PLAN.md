---
phase: 08-inscri-o-knock-out-etapa-1
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/features/config-vaga/templates/cargoTemplates.ts
  - src/features/config-vaga/schemas/qualificacaoSchema.ts
  - src/features/config-vaga/publishGate.ts
  - src/features/config-vaga/types/configVagaTypes.ts
autonomous: true
requirements: [INSCR-02, INSCR-03]

must_haves:
  truths:
    - "Every cargo template seeds a presencial-SP knockout question (Não → tag knockout)"
    - "Only the dentista template seeds the harmonização-orofacial knockout"
    - "The client publish gate rejects >10 perguntas or >1 open-ended pergunta in the Etapa-1 qualification block"
  artifacts:
    - path: "src/features/config-vaga/templates/cargoTemplates.ts"
      provides: "qualificacao field on CargoTemplate + default knockouts seeded; getCargoTemplateDefaults deep-copies it"
      contains: "qualificacao"
    - path: "src/features/config-vaga/schemas/qualificacaoSchema.ts"
      provides: "QualificacaoPergunta type + Zod schema (texto, tipo_resposta, obrigatoria, ordem, opcoes[{id,texto,tag,peso}])"
    - path: "src/features/config-vaga/publishGate.ts"
      provides: "≤10 perguntas / ≤1 aberta gate (D-09) mirroring publish_vaga RPC"
  key_links:
    - from: "src/features/config-vaga/templates/cargoTemplates.ts"
      to: "src/features/config-vaga/schemas/qualificacaoSchema.ts"
      via: "import QualificacaoPergunta type"
      pattern: "QualificacaoPergunta"
---

<objective>
Seed the per-cargo Etapa-1 qualification + default knockouts in `cargoTemplates.ts` (D-14, INSCR-03) and extend the client publish gate with the ≤10-perguntas / ≤1-aberta rule (D-09, INSCR-02). Single-tenant, single-clinic → presencial-SP uses a FIXED clinic text (not derived from vaga.cidade). Harmonização orofacial is dentista-only.

Purpose: the cargo template is the git source of truth for default knockouts copied into the vaga; the publish gate enforces the Etapa-1 structural limits client-side (server gate lands in Plan 04).
Output: 4 edited/created files; Plan 01 cargoTemplates + publishGate RED tests flip GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-inscri-o-knock-out-etapa-1/08-RESEARCH.md
@.planning/phases/08-inscri-o-knock-out-etapa-1/08-PATTERNS.md
@docs/conhecimento/perguntas-vagas.md

<interfaces>
From src/features/config-vaga/templates/cargoTemplates.ts:
- `interface CargoTemplate { slug; label; pesos_avaliacao; testes_aplicaveis }` (L33) — extend with `qualificacao`
- `const baseTestes = (overrides) => [...]` (L47) — factory idiom to mirror for `baseQualificacao()`
- `cargoTemplates: Record<CargoSlug, CargoTemplate>` (L67)
- `getCargoTemplateDefaults(slug): VagaConfig` (L163) — deep-copy; must clone the new qualificacao array

From src/features/config-vaga/types/configVagaTypes.ts:
- `VagaConfig` (returned by getCargoTemplateDefaults) — extend with qualificacao

Canonical knockout texts (docs/conhecimento/perguntas-vagas.md):
- presencial-SP (ALL cargos): "Você tem disponibilidade para trabalhar presencialmente em São Paulo, perto dos metros Brigadeiro e Paraíso?" → option "Não" tag='knockout'
- harmonização orofacial (dentista ONLY, Etapa 2 - Dentista section)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QualificacaoPergunta type + cargoTemplates default-knockout seed</name>
  <behavior>
    - Every cargoTemplates[slug].qualificacao contains a presencial-SP pergunta with a "Não" opcao tagged 'knockout'
    - cargoTemplates.dentista.qualificacao additionally contains a harmonização-orofacial knockout pergunta
    - No non-dentista template contains the harmonização pergunta
    - getCargoTemplateDefaults(slug) returns a deep copy: mutating result.qualificacao does not touch the source
  </behavior>
  <read_first>
    - src/features/config-vaga/templates/cargoTemplates.ts (CargoTemplate L33, baseTestes L47, cargoTemplates L67, getCargoTemplateDefaults L163)
    - src/features/config-vaga/types/configVagaTypes.ts (VagaConfig)
    - src/features/config-vaga/templates/__tests__/cargoTemplates.test.ts (Plan 01 RED contract — exact assertion shape)
    - docs/conhecimento/perguntas-vagas.md (canonical question texts)
    - src/lib/opcoes/opcoesNormalize.ts (the [{id,texto}] option shape — stay consistent)
  </read_first>
  <action>
    Create src/features/config-vaga/schemas/qualificacaoSchema.ts exporting a `QualificacaoPergunta` type + matching Zod schema with fields: `id` (string), `texto` (string), `tipo_resposta` ('single_choice' | 'multiple_choice' | 'texto' | 'numerica'), `obrigatoria` (boolean), `ordem` (number), `opcoes` (array of `{ id: string; texto: string; tag?: EnumTagOpcao; peso?: number }`). Reuse the existing `enum_tag_opcao` value union ('knockout'|'atencao'|'neutro'|'pontua'|'fortemente_pontua') — import or mirror the Phase 7 tag type; do NOT redefine the enum.

    In cargoTemplates.ts: extend `CargoTemplate` (L33) with `qualificacao: QualificacaoPergunta[]`. Add a `baseQualificacao(overrides)` factory mirroring `baseTestes` (L47) that returns the presencial-SP knockout pergunta seeded for ALL cargos: texto exactly "Você tem disponibilidade para trabalhar presencialmente em São Paulo, perto dos metros Brigadeiro e Paraíso?", tipo_resposta 'single_choice', obrigatoria true, opcoes [{texto:'Sim', tag:'neutro'}, {texto:'Não', tag:'knockout'}] with stable ids (use a deterministic id scheme, e.g. crypto.randomUUID() at copy time is wrong — use fixed template-local ids so tests are stable; generate fresh ids only in getCargoTemplateDefaults deep-copy). Assign `qualificacao: baseQualificacao()` to all 8 templates; for `dentista` ONLY, append a harmonização-orofacial knockout pergunta (texto from perguntas-vagas.md dentista section, "Não" → tag='knockout').

    Update `getCargoTemplateDefaults` (L163) to deep-copy `qualificacao` (clone each pergunta + its opcoes array, mint fresh opcao ids per copy if the copy-into-vaga write requires unique ids — mirror how testes_aplicaveis.perguntas is cloned). Update `VagaConfig` in configVagaTypes.ts to include `qualificacao`.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/config-vaga/templates/__tests__/cargoTemplates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "qualificacao" src/features/config-vaga/templates/cargoTemplates.ts` returns ≥ 3 (interface + factory + getCargoTemplateDefaults)
    - src/features/config-vaga/schemas/qualificacaoSchema.ts exports QualificacaoPergunta and a Zod schema
    - Plan 01 cargoTemplates "INSCR-03 / D-14 default knockouts" describe PASSES (presencial-SP for all 8, harmonização dentista-only)
    - getCargoTemplateDefaults deep-copy test (mutate result, source unchanged) PASSES
    - The presencial-SP texto matches the canonical string exactly
  </acceptance_criteria>
  <done>cargoTemplates seeds presencial-SP for all cargos + harmonização for dentista only; deep-copy preserved; Plan 01 template RED test flips GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: publish gate ≤10 perguntas / ≤1 aberta (client)</name>
  <read_first>
    - src/features/config-vaga/publishGate.ts (existing D-12 client gate)
    - src/features/config-vaga/__tests__/publishGate.test.ts (Plan 01 RED contract)
    - supabase/migrations/20260607010004_publish_vaga_rpc.sql (D-12 conditions — the server mirror Plan 04 extends)
    - src/features/config-vaga/schemas/qualificacaoSchema.ts (QualificacaoPergunta — created in Task 1)
  </read_first>
  <action>
    Extend the client publish gate in publishGate.ts with a D-09 check over the vaga's Etapa-1 qualification block: reject (return a gate error) when the block has MORE THAN 10 perguntas OR MORE THAN 1 open-ended pergunta (tipo_resposta 'texto'). Use pt-BR error messages consistent with the existing gate copy (e.g. "A qualificação da Etapa 1 permite no máximo 10 perguntas." / "A qualificação da Etapa 1 permite no máximo 1 pergunta aberta."). This is the client mirror; the authoritative server gate is added to publish_vaga in Plan 04. Keep the existing pesos=100 / ≥1-obrigatorio / knockout-must-be-obrigatoria checks intact.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/config-vaga/__tests__/publishGate.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - publishGate.ts rejects a block with 11 perguntas and a block with 2 open-ended perguntas
    - Plan 01 publishGate "INSCR-02 / D-09 qualification gate" describe PASSES
    - Existing publishGate D-12 cases remain GREEN (no regression)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Client publish gate enforces ≤10/≤1-aberta; Plan 01 gate RED test flips GREEN; no D-12 regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git template → vaga copy | cargoTemplates is the source of truth; the copy-into-vaga write (Phase 7 sync RPC) owns the per-vaga mutable copy. No untrusted input here (RH-authored). |
| client publish gate | UX-only; the authoritative gate is the server publish_vaga RPC (Plan 04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | Client bypasses ≤10/≤1-aberta gate | mitigate | Client gate is defense-in-depth; Plan 04 adds the server publish_vaga gate (authoritative). Flagged as the binding control. |
| T-08-05 | Process integrity | Knockout option seeded on a non-obrigatoria pergunta (silently never fires) | mitigate | baseQualificacao seeds knockout perguntas with obrigatoria=true; publish_vaga (live) already rejects knockout-on-non-obrigatoria (D-12 cond 3). |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase. |
</threat_model>

<verification>
`npm run test:run -- src/features/config-vaga` green; `npm run build` exit 0. The default knockouts are git-sourced and verified by unit assertion against the canonical texts.
</verification>

<success_criteria>
- presencial-SP knockout seeded for all 8 cargos; harmonização for dentista only
- QualificacaoPergunta type + Zod schema exist
- Client publish gate enforces ≤10 perguntas / ≤1 aberta
- Plan 01 cargoTemplates + publishGate RED tests GREEN
- Build exits 0
</success_criteria>

<output>
Create `.planning/phases/08-inscri-o-knock-out-etapa-1/08-03-SUMMARY.md` when done.
</output>
