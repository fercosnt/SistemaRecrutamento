---
phase: 18-resili-ncia-das-efs-de-ia-bugs-do-funil
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/gerar-devolutiva-bigfive/index.ts
  - supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts
autonomous: true
requirements: [RESIL-02]
must_haves:
  truths:
    - "gerar-devolutiva-bigfive fans out the 5 OCEAN dims with Promise.allSettled instead of a sequential await loop (D-Área1: parallelize 5 dims)"
    - "Each dim makes exactly 1 AI attempt (down from 2) under parallel (D-Área1: 1 attempt per dim)"
    - "On any per-dim failure the dim degrades to the deterministic BAND_TEMPLATES text — the EF never hard-fails on a single dim (D-Área1: graceful-degrade per-dim)"
    - "Output paginas preserve O-C-E-A-N order (allSettled preserves input order)"
    - "The handler completes within the EF execution window (no 5×2 sequential AI calls)"
    - "RNF-07a preserved — the degrade path writes no score/decision to candidaturas"
  artifacts:
    - path: "supabase/functions/gerar-devolutiva-bigfive/index.ts"
      provides: "Parallelized 5-dim devolutiva generation with per-dim degrade"
      contains: "allSettled"
    - path: "supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts"
      provides: "Deno regression test for parallel + 1-attempt + degrade + OCEAN order"
      contains: "allSettled"
  key_links:
    - from: "supabase/functions/gerar-devolutiva-bigfive/index.ts"
      to: "personalizeDim"
      via: "Promise.allSettled over DIMS, 1 attempt each, degrade to BAND_TEMPLATES"
      pattern: "Promise\\.allSettled"
---

<objective>
Parallelize the `gerar-devolutiva-bigfive` Edge Function so its 5 OCEAN-dimension AI calls run concurrently with one attempt each and a per-dim deterministic fallback, eliminating the timeout (achado #2) where 5 sequential dims × up to 2 attempts blow the EF execution window.

Purpose: RESIL-02 — `gerar-devolutiva-bigfive` completes dentro do limite de execução. Today the handler awaits 5 dims one-by-one, each with up to 2 attempts; at the observed 38–102s/call this is the documented timeout cause. With Plan 01's 25s per-call cap plus 5-way parallel + 1 attempt, worst-case wall time ≈ max(single call) ≈ 25s + persist, comfortably inside the 150s idle / 400s wall-clock ceiling (5 in-flight calls are well under the 256MB isolate limit — RESEARCH execution-budget proof).
Output: parallelized handler using `Promise.allSettled`, `personalizeDim` at 1 attempt, per-dim degrade preserved; new Deno test file asserting the shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-RESEARCH.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md

<interfaces>
<!-- Extracted from supabase/functions/gerar-devolutiva-bigfive/index.ts — use directly. -->

Sequential handler loop (CURRENT, ~L390-407) — the timeout cause, to replace with allSettled:
```
const paginas: PaginaOut[] = [];
const dashboard: { dim; percentil; banda }[] = [];
for (const dim of DIMS) {
  const meta = byDim.get(dim);
  const percentil = typeof meta?.percentil === "number" ? meta.percentil : 50;
  const banda = bandOf(percentil);
  const rawTemplate = BAND_TEMPLATES[dim][banda];
  const { texto, palavras } = await personalizeDim(dim, percentil, banda, rawTemplate, callAi, {candidato_id, vaga_id});
  paginas.push({ dim, banda, percentil, texto_interpretativo: texto, palavras });
  dashboard.push({ dim, percentil, banda });
}
```

personalizeDim attempt loop (CURRENT, ~L312-324) — change bound 2→1; KEEP the inRange gate + degrade return:
```
for (let attempt = 0; attempt < 2; attempt++) {   // ← change to 1 (or attempts arg)
  const res = await callAi({ ...baseArgs, attempt });
  ... if (texto.length > 0 && inRange(palavras)) return { texto, palavras };  // KEEP
}
return { texto: rawTemplate, palavras: wordCount(rawTemplate) };  // KEEP degrade (never throws)
```

Serve-wrapper status (CURRENT, ~L588-590) — KEEP; add 503 only if an AI_UNAVAILABLE path is introduced:
```
const httpStatus = out.status === "refused" ? 422 : out.status === "falhou" ? 500 : 200;
```

Preservation guards — DO NOT touch: RF-19b `tipo !== "big_five"` refuse (~L354-360), candidato_id/vaga_id attribution (~L362-379, LGPD-02), service_role precondition read (~L338-349), max_tokens 1200.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Deno test file for parallel + 1-attempt + degrade + OCEAN order</name>
  <files>supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts</files>
  <read_first>
    - supabase/functions/gerar-devolutiva-bigfive/index.ts (handler structure, DIMS order, BAND_TEMPLATES, personalizeDim, the HandlerDeps/DI shape for mocking callAi)
    - supabase/functions/_shared/__tests__/ai-client.test.ts (DI-mock idiom — `std@0.224.0/assert`, deferred-promise concurrency assertion pattern)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md (`gerar-devolutiva-bigfive/__tests__/index.test.ts` section — the exact case list)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-RESEARCH.md (Pattern 2 + Validation Architecture Test Map row RESIL-02)
  </read_first>
  <behavior>
    - Test: all 5 dims' `callAi` are invoked before any resolves (concurrency — use deferred promises that only resolve after all 5 have been called) → proves `Promise.allSettled` parallel, not sequential.
    - Test: each dim invokes `callAi` exactly once (1 attempt), not twice.
    - Test: when one dim's `callAi` rejects (or returns out-of-range words), that dim's output equals the deterministic `BAND_TEMPLATES` text for its band and the EF still returns all 5 paginas (no hard-fail).
    - Test: `paginas` are in O-C-E-A-N order matching DIMS input order.
  </behavior>
  <action>
    Create the `__tests__/` directory and `index.test.ts` for `gerar-devolutiva-bigfive`. Mock `callAi` via the EF's `HandlerDeps`/DI seam (mirror the `ai-client.test.ts` DI idiom). Write the four cases in the behavior block above. Because the handler is being refactored in Task 2, this test is written FIRST (TDD red) against the target parallel shape and must initially fail on the not-yet-parallel code, then pass after Task 2. Use `std@0.224.0/assert`. Write the test against exported handler internals — if the handler/`personalizeDim` is not currently exported for test, note the minimal export needed (Task 2 adds it). Do NOT call real network; all AI is DI-mocked.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && deno test --allow-read --allow-env supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts` exists with the four cases (concurrency, 1-attempt, per-dim degrade, OCEAN order).
    - Tests use DI-mocked `callAi` (no real network).
    - After Task 2, `deno test ... gerar-devolutiva-bigfive/__tests__/index.test.ts` exits 0.
  </acceptance_criteria>
  <done>Regression test file present asserting the parallel + 1-attempt + degrade + OCEAN-order contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parallelize the 5-dim handler with Promise.allSettled + 1 attempt/dim</name>
  <files>supabase/functions/gerar-devolutiva-bigfive/index.ts</files>
  <read_first>
    - supabase/functions/gerar-devolutiva-bigfive/index.ts (sequential loop ~L390-407; personalizeDim ~L291-325; serve-wrapper ~L588-590; preservation guards ~L338-379)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md (`gerar-devolutiva-bigfive/index.ts` section — replace loop, 2→1 attempt, preservation guards)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-RESEARCH.md (Pattern 2 example + "Important preservation notes")
  </read_first>
  <action>
    Replace the sequential `for (const dim of DIMS) { await personalizeDim(...) }` (~L390-407) with `const results = await Promise.allSettled(DIMS.map(async (dim) => { ... }))` per RESEARCH Pattern 2: each mapped function resolves `{ dim, banda, percentil, texto_interpretativo, palavras }`; then map `results` back to `paginas` by index (allSettled preserves input order → O-C-E-A-N safe), and for any `rejected` result degrade inline to `BAND_TEMPLATES[dim][banda]` with `wordCount`. Build the `dashboard[]` array in the same index-mapped pass. Change `personalizeDim`'s attempt loop bound from `< 2` to `< 1` (or add an attempts arg defaulting to 1) per D-Área1 (1 attempt/dim under parallel) — KEEP the `inRange(palavras)` word-count gate and the `return { texto: rawTemplate, ... }` degrade (personalizeDim must still never throw). Use `Promise.allSettled` (NOT `Promise.all` — Anti-Pattern: one rejected dim must not lose the other 4). DO NOT touch: the RF-19b `tipo !== "big_five"` refuse, the candidato_id/vaga_id LGPD attribution, the service_role precondition read, `max_tokens: 1200`, or the serve-wrapper status mapping. Keep all `npm:` imports STATIC (Pitfall 7). If you add the minimal export the Task-1 test needs, keep it to the handler/`personalizeDim` only.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && deno test --allow-read --allow-env supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Promise.allSettled" supabase/functions/gerar-devolutiva-bigfive/index.ts` returns the fan-out over DIMS.
    - `personalizeDim` makes 1 attempt (loop bound `< 1` or attempts=1 default); the `inRange` gate and `return rawTemplate` degrade are intact.
    - `Promise.all` is NOT used for the dim fan-out.
    - The RF-19b refuse, LGPD attribution, service_role read, max_tokens 1200, and serve-wrapper status mapping are unchanged.
    - All four Task-1 cases pass: `deno test ... gerar-devolutiva-bigfive/__tests__/index.test.ts` exits 0.
  </acceptance_criteria>
  <done>Handler fans out 5 dims in parallel at 1 attempt each with per-dim degrade and preserved OCEAN order; preservation guards intact; Deno test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bigfive EF → Anthropic (×5 concurrent) | 5 concurrent provider calls per devolutiva; must not exceed EF resource limits or cause RNF-07a violation on failure |
| bigfive EF → caller | success/error body shape stays the existing convention; degrade returns templates, not decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-DoS | Denial of Service | 5-way parallel provider fan-out | accept | 5 small concurrent calls per single candidate's devolutiva; within 256MB isolate + 150s/400s limits (RESEARCH budget proof). Per-call timeout (Plan 01) bounds each. Concurrency cap of 2 is the documented fallback lever if PROD shows 429 clustering — not needed now. |
| T-18-02-T | Tampering | per-dim degrade path | mitigate | RNF-07a: the degrade returns deterministic `BAND_TEMPLATES` text and writes NO score/decision to `candidaturas`. The error/degrade path is non-decisional by construction (personalizeDim never throws, returns template). |
| T-18-02-ID | Information Disclosure | EF logs / error body | mitigate | EF logs already redacted to ids/counts (~L448-455); preserve. No raw provider error or PII echoed in the response body. |
| T-18-02-EoP | Elevation of Privilege | serve-wrapper authz | mitigate | The two-client authenticate-THEN-authorize and service_role precondition read are NOT touched by the handler-internals refactor; ASVS V4 preserved. |
| T-18-SC | Tampering | Deno imports | mitigate | No package added; `npm:` imports stay STATIC (Pitfall 7). No install → no slopcheck gate. |
</threat_model>

<verification>
- `deno test --allow-read --allow-env supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts` green.
- `grep -n "Promise.allSettled" supabase/functions/gerar-devolutiva-bigfive/index.ts` present; `grep -n "Promise.all(" ...` absent for the dim fan-out.
- Preservation guards (RF-19b refuse, LGPD attribution, service_role read, max_tokens 1200) verified unchanged by diff review in SUMMARY.
</verification>

<success_criteria>
- 5 OCEAN dims generated concurrently via `Promise.allSettled`, 1 attempt each, per-dim deterministic degrade, OCEAN order preserved.
- No hard-fail on a single dim; RNF-07a preserved (no decisional write in degrade path).
- Handler completes within the EF window (no 5×2 sequential AI calls).
- (Live PROD pickup requires the [BLOCKING] redeploy in Plan 07 — bundle freeze.)
</success_criteria>

<output>
Create `.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-02-SUMMARY.md` when done.
</output>
