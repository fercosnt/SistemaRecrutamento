---
phase: 18-resili-ncia-das-efs-de-ia-bugs-do-funil
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/AsyncState.tsx
  - src/components/ui/__tests__/AsyncState.test.tsx
  - src/features/hub-candidato/components/HubSection.tsx
autonomous: true
requirements: [RESIL-03]
must_haves:
  truths:
    - "A shared <AsyncState> wrapper renders exactly one of loading / slow / error / empty / success in the binding priority order (D-Área2: generalize HubSection async-state pattern)"
    - "The slow state escalates from loading after slowAfterMs (default 8000) and never replaces a resolved success/error (18-UI-SPEC States Contract)"
    - "errorCode==='AI_UNAVAILABLE' selects the sobrecarga copy; any other error selects the generic copy (18-UI-SPEC Copywriting Contract, verbatim PT-BR)"
    - "The retry button appears in the error state ONLY, calls onRetry, and shows 'Tentando…' + disabled while retrying (D-Área2: standardized retry)"
    - "All copy is single-sourced in one COPY const so the verbatim PT-BR strings never drift (18-UI-SPEC)"
    - "HubSection delegates to / shares <AsyncState>'s source so the two never drift, keeping HubSection's futuro/sem_dados overrides"
  artifacts:
    - path: "src/components/ui/AsyncState.tsx"
      provides: "Shared loading/slow/error/empty/success wrapper with standardized retry"
      min_lines: 60
      contains: "AI_UNAVAILABLE"
    - path: "src/components/ui/__tests__/AsyncState.test.tsx"
      provides: "Component contract test (5 states + errorCode copy branch + retry disabled-while-retrying + slow@8s)"
      contains: "AI_UNAVAILABLE"
    - path: "src/features/hub-candidato/components/HubSection.tsx"
      provides: "Refactored to delegate to <AsyncState> (no drift)"
  key_links:
    - from: "src/features/hub-candidato/components/HubSection.tsx"
      to: "src/components/ui/AsyncState.tsx"
      via: "delegation / shared COPY source"
      pattern: "AsyncState"
---

<objective>
Extract one shared `<AsyncState>` presentational wrapper that generalizes the `HubSection` async-state pattern and the `ConsolidacaoDashboard` retry exemplar into the single graceful-degradation contract from 18-UI-SPEC — so candidate and RH screens always show a clear loading / slow / error / empty / success state with a standardized "Tentar novamente", never a blank screen.

Purpose: RESIL-03 (the component half). The wrapper is the reusable surface; Plan 05 adds the service-layer `error_code` plumbing it reads, and Plan 06 adopts it on the 5 AI-backed screens. This plan builds the component, its contract test, and refactors `HubSection` to delegate so the two never drift.
Output: `src/components/ui/AsyncState.tsx`, its Vitest+RTL contract test, and a `HubSection` that delegates to it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-UI-SPEC.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md

<interfaces>
<!-- 18-UI-SPEC §Component API — recommended shape (name/API Claude's discretion). -->
```
interface AsyncStateProps {
  isLoading?: boolean        // takes precedence (TanStack isLoading|isPending)
  isError?: boolean          // takes precedence over isEmpty
  isEmpty?: boolean          // caller predicate (e.g. !data?.items.length)
  errorCode?: string         // 'AI_UNAVAILABLE' → sobrecarga copy; else generic
  slowAfterMs?: number       // default 8000
  onRetry?: () => void       // shown in error state only
  retrying?: boolean         // drives "Tentando…" + disabled
  glass?: boolean            // wrap in <Glass variant="dark">; default true
  copy?: Partial<AsyncStateCopy>
  children: ReactNode        // success content
}
```

State priority (binding, 18-UI-SPEC §States Contract): isLoading||isPending → slow@8s → isError+retry → isEmpty → children.

Imports to reuse (from src/components/ui/glass.tsx + skeleton.tsx + lucide):
```
import { Glass, GlassButton } from '@/components/ui/glass'
import { Skeleton } from '@/components/ui/skeleton'
import { AlertTriangle, Loader2, RotateCcw } from 'lucide-react'
```

Glass surface idiom (HubSection.tsx ~L76): <Glass variant="dark" blur="lg" className="rounded-xl p-6">…</Glass>
Centered empty/error block (HubSection.EstadoVazio ~L64-72): text-base font-semibold text-white heading + text-sm text-white/70 body.
Error+retry idiom (ConsolidacaoDashboard.tsx ~L102-111): AlertTriangle text-red-300 + GlassButton min-h-[44px].
</interfaces>

<copy_contract>
<!-- 18-UI-SPEC §Copywriting Contract — VERBATIM PT-BR, single-sourced in one COPY const. -->
- slow heading: "Estamos processando com IA…"  · slow body: "Isso pode levar até ~30 segundos. Não feche esta tela."
- error heading: "Não foi possível concluir agora."
- error body (AI_UNAVAILABLE): "O serviço de IA está sobrecarregado. Tente novamente em instantes."
- error body (generic): "Verifique a conexão e tente novamente."
- empty heading: "Nada para mostrar ainda" · empty body: "Os dados desta etapa aparecerão aqui quando estiverem disponíveis."
- retry label: "Tentar novamente" · retry in-flight: "Tentando…"
</copy_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the shared <AsyncState> wrapper</name>
  <files>src/components/ui/AsyncState.tsx</files>
  <read_first>
    - src/features/hub-candidato/components/HubSection.tsx (base pattern: COPY const ~L48-62, EstadoVazio ~L64-72, Glass wrapper ~L76, Skeleton ~L80, state precedence ~L79-89)
    - src/features/decisao/components/ConsolidacaoDashboard.tsx (retry exemplar ~L90-127 — AlertTriangle + GlassButton)
    - src/components/ui/glass.tsx (Glass / GlassButton props — variant, blur, min-h)
    - src/components/ui/skeleton.tsx
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-UI-SPEC.md (States Contract + Component API + Copywriting Contract — binding)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md (`AsyncState.tsx (NEW)` section — verbatim idioms to copy)
  </read_first>
  <behavior>
    - Renders skeleton when isLoading and < slowAfterMs.
    - After slowAfterMs while still loading: shows skeleton + Loader2 + slow heading/body (slow never replaces a resolved state).
    - isError: shows AlertTriangle (text-red-300) + error heading + body chosen by errorCode (AI_UNAVAILABLE → overload copy; else generic) + retry button.
    - Retry button calls onRetry; while retrying shows "Tentando…" + disabled; retry present in error state ONLY.
    - isEmpty (and not loading/error): shows empty heading/body.
    - Otherwise: renders children (success).
    - Precedence: isLoading||isPending → slow → isError → isEmpty → children.
  </behavior>
  <action>
    Create `src/components/ui/AsyncState.tsx` exporting a NAMED `AsyncState` component matching the 18-UI-SPEC §Component API shape (props in the interfaces block; named export per CLAUDE.md convention). Single-source ALL copy in one `const COPY = {...} as const` filled VERBATIM from the copy_contract block (PT-BR — no paraphrase; the no-drift guarantee). Compose only already-vendored primitives: `Glass`/`GlassButton` (`@/components/ui/glass`), `Skeleton` (`@/components/ui/skeleton`), lucide `AlertTriangle`/`Loader2`/`RotateCcw` — NO registry pull (18-UI-SPEC §Registry Safety: N/A). Glass surface: `<Glass variant="dark" blur="lg" className="rounded-xl p-6">` when `glass !== false`; bare otherwise. Error icon `AlertTriangle className="h-8 w-8 text-red-300"` as a signal only (never a fill/red background — 18-UI-SPEC §Color). Retry: `GlassButton variant="white"` `min-h-[44px]`, label "Tentar novamente", in-flight `Loader2 animate-spin` + "Tentando…" + `disabled={retrying}`, optional `RotateCcw` 16px. Slow-state timer: `useEffect` starting `setTimeout(slowAfterMs ?? 8000)` while loading, set a local `isSlow` flag, clear on resolve/unmount — must NEVER replace a resolved success/error. Enforce the binding precedence (isLoading||isPending → slow → isError → isEmpty → children). Empty/error centered block uses the `HubSection.EstadoVazio` typography (text-base font-semibold text-white heading; text-sm text-white/70 body). Accept a `copy?: Partial<AsyncStateCopy>` override merged over defaults. Do NOT add any new dependency.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/AsyncState.tsx` exists with a NAMED `AsyncState` export (no default export).
    - One `COPY` const holds the verbatim PT-BR strings from the copy_contract (grep finds "Tentar novamente", "O serviço de IA está sobrecarregado", "Nada para mostrar ainda").
    - `grep -n "AI_UNAVAILABLE" src/components/ui/AsyncState.tsx` matches (errorCode copy branch).
    - Only vendored primitives + lucide are imported (no registry/new package).
    - `npm run lint` does not regress the tsc baseline.
  </acceptance_criteria>
  <done>Shared <AsyncState> renders the 5-state contract with single-sourced verbatim copy and the standardized retry; tsc baseline clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contract test for <AsyncState> (5 states + errorCode + retry + slow@8s)</name>
  <files>src/components/ui/__tests__/AsyncState.test.tsx</files>
  <read_first>
    - src/components/ui/AsyncState.tsx (the component under test — from Task 1)
    - src/features/triagem/components/__tests__/ComparativoScreen.test.tsx (RTL render idiom)
    - tests/setup.ts (fake-timer jest-shim ~L23-32 — already bridged for the slow@8s timer)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md (`AsyncState.test.tsx (NEW)` section — case list)
  </read_first>
  <behavior>
    - loading: renders skeleton, no error/retry text.
    - slow: after `vi.advanceTimersByTime(8000)` while loading, the slow heading "Estamos processando com IA…" appears.
    - error generic: isError without AI_UNAVAILABLE → "Verifique a conexão e tente novamente." + retry button.
    - error overload: isError + errorCode='AI_UNAVAILABLE' → "O serviço de IA está sobrecarregado. Tente novamente em instantes."
    - retry: clicking retry calls onRetry; with retrying=true the button shows "Tentando…" and is disabled.
    - empty: isEmpty → "Nada para mostrar ainda".
    - success: none set → children render.
  </behavior>
  <action>
    Create `src/components/ui/__tests__/AsyncState.test.tsx` with Vitest `^4.0.7` + `@testing-library/react` `^16.3.2` (env happy-dom). Cover every case in the behavior block. Use `vi.useFakeTimers()` + `vi.advanceTimersByTime(8000)` for the slow@8s escalation (the `tests/setup.ts` jest-shim already bridges fake timers). Assert the errorCode copy branch (overload vs generic) by querying the rendered verbatim PT-BR text. Assert retry calls `onRetry` and is disabled + shows "Tentando…" when `retrying`. Assert the precedence (e.g. isLoading wins over isEmpty). No network; pure component render.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && npm run test:run -- src/components/ui/__tests__/AsyncState.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exists covering all 7 behavior cases (loading, slow@8s, error generic, error overload, retry+disabled, empty, success).
    - Slow case uses fake timers (`advanceTimersByTime(8000)`).
    - errorCode branch asserted via the two verbatim PT-BR error bodies.
    - `npm run test:run` for the file exits 0.
  </acceptance_criteria>
  <done>The 5-state contract, errorCode copy split, retry-disabled-while-retrying, and slow@8s escalation are all locked by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor HubSection to delegate to <AsyncState></name>
  <files>src/features/hub-candidato/components/HubSection.tsx</files>
  <read_first>
    - src/features/hub-candidato/components/HubSection.tsx (full file — COPY const, EstadoVazio, state precedence, futuro/sem_dados semantics)
    - src/components/ui/AsyncState.tsx (the wrapper to delegate to)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-UI-SPEC.md (Adoption Surface — HubSection row: "keep its futuro/sem_dados semantics")
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md (`AsyncState.tsx` section — "HubSection refactor" note)
  </read_first>
  <action>
    Refactor `HubSection` so it delegates its loading/error/empty rendering to `<AsyncState>` (or shares `<AsyncState>`'s COPY source) so the verbatim strings never drift between the two. Preserve `HubSection`'s funnel-specific overrides — its `futuro` ("Etapa ainda não iniciada") and `sem_dados` ("Sem dados nesta etapa") copy must stay as HubSection-specific overrides passed via the `copy` prop (those strings do NOT change, per 18-UI-SPEC §Copywriting Contract). Keep `HubSection`'s public props/usage unchanged (its existing callers must not break). Verify the existing HubSection tests (if any) still pass; if `HubSection` has no test, the regression guard is the HubSection consumers' tests + tsc.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && npm run lint 2>&1 | tail -3 && npm run test:run -- src/features/hub-candidato 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AsyncState" src/features/hub-candidato/components/HubSection.tsx` matches (delegation or shared source).
    - HubSection's `futuro`/`sem_dados` copy is preserved verbatim (as overrides) — grep still finds "Etapa ainda não iniciada" / "Sem dados nesta etapa" reachable from HubSection.
    - HubSection's public props are unchanged (no caller breakage).
    - `npm run lint` baseline not regressed; `npm run test:run` for hub-candidato green.
  </acceptance_criteria>
  <done>HubSection delegates to <AsyncState> with its funnel-specific overrides preserved; no drift, no caller breakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF error body → <AsyncState> copy selector | the component must select copy from a code string only, never echo raw error/PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-ID | Information Disclosure | <AsyncState> error rendering | mitigate | The component renders ONLY static verbatim PT-BR copy keyed off `errorCode` (a string) — it never parses or echoes the raw Supabase/transport error, stack, or PII (18-UI-SPEC §Color/§Copywriting; Anti-Pattern: component must not parse Supabase errors). |
| T-18-04-ID2 | Information Disclosure | errorCode prop | accept | `errorCode` carries only a code like 'AI_UNAVAILABLE' (set by the service layer in Plan 05, which extracts only the body's `error_code`). Low-sensitivity; no PII by contract. |
| T-18-04-T | Tampering | retry action | mitigate | Retry re-runs a read/invoke (non-destructive — 18-UI-SPEC §Copywriting "Destructive actions: none"); disabled-while-retrying prevents double-submit. No write to candidaturas. |
| T-18-SC | Tampering | imports | mitigate | No registry pull, no new package — composes only vendored Glass/Skeleton + lucide (18-UI-SPEC §Registry Safety: N/A). No slopcheck gate. |
</threat_model>

<verification>
- `npm run test:run -- src/components/ui/__tests__/AsyncState.test.tsx` green (all 7 cases).
- `npm run test:run -- src/features/hub-candidato` green.
- `npm run lint` baseline not regressed.
- `grep -n "AI_UNAVAILABLE" src/components/ui/AsyncState.tsx` and the verbatim PT-BR copy strings present.
</verification>

<success_criteria>
- One shared <AsyncState> renders the binding 5-state contract with single-sourced verbatim PT-BR copy.
- errorCode==='AI_UNAVAILABLE' → sobrecarga copy; else generic; retry in error state only, disabled-while-retrying.
- HubSection delegates without drift, keeping its funnel-specific overrides.
- Adoption on the 5 AI-backed screens = Plan 06 (depends on this component + Plan 05 plumbing).
</success_criteria>

<output>
Create `.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-04-SUMMARY.md` when done.
</output>
