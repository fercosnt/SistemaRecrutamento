---
phase: 09-ai-prompt-library-cost-infra
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - supabase/migrations/20260609000001_prompt_library_schema.sql
  - supabase/migrations/20260609000002_prompt_library_rpcs.sql
  - supabase/migrations/20260609000003_prompt_library_cron.sql
  - supabase/migrations/20260609000004_prompt_library_seed.sql
autonomous: true
requirements: [IA-01, IA-02, IA-03, IA-04, LGPD-04]
must_haves:
  truths:
    - "The 6 AI tables + 3 enums exist with FKs retargeted to live pt-BR tables (candidatos, vagas) per RESEARCH Pitfall 2 (IA-02)"
    - "recruiter_alerts is CREATED with the orchestrator-decision #4 column set + administrador-read RLS (IA-04 / Pitfall 3)"
    - "The 3 promote/rollback RPCs + immutability trigger + cost-anomaly pg_net trigger are authored SECURITY DEFINER with in-body 'administrador' check (IA-01 / IA-04 / Pattern 4)"
    - "pg_cron jobs cover ONLY cost-aggregation (01:30) + retention-purge (02:00); HITL-SLA + Art.18 are OUT OF SCOPE per orchestrator-decision #5"
  artifacts:
    - path: "supabase/migrations/20260609000001_prompt_library_schema.sql"
      provides: "6 tables + 3 enums + indexes + RLS + extensions (pg_cron, pg_net, pgcrypto)"
      contains: "recruiter_alerts"
    - path: "supabase/migrations/20260609000002_prompt_library_rpcs.sql"
      provides: "immutability trigger + promote_to_canary/promote_canary_to_active/rollback_to_version + notify_cost_anomaly trigger"
      contains: "SECURITY DEFINER"
    - path: "supabase/migrations/20260609000003_prompt_library_cron.sql"
      provides: "pg_cron aggregation + purge jobs"
      contains: "cron.schedule"
    - path: "supabase/migrations/20260609000004_prompt_library_seed.sql"
      provides: "seed v1.0.0 x 7 prompts is_active=false"
      contains: "prompt_versions"
  key_links:
    - from: "ai_call_logs.candidato_id"
      to: "candidatos(id)"
      via: "FK retargeted to live pt-BR table (NOT candidates)"
      pattern: "REFERENCES public.candidatos"
    - from: "notify_cost_anomaly()"
      to: "cost-alerter EF"
      via: "net.http_post with Vault Bearer"
      pattern: "net.http_post"
---

<objective>
Author the 4 Phase-9 migrations (schema, RPCs+triggers, cron, seed) grounded in the LIVE pt-BR table names — authored but NOT applied (apply is the [BLOCKING] Plan 07 task). All FKs retarget candidatos/vagas; recruiter_alerts is created; PL/pgSQL written WITHOUT a BEGIN/COMMIT wrapper (42601 avoidance).

Purpose: IA-01/02/03/04 + LGPD-04 all depend on this schema (prompt_versions for versioning, ai_call_logs for cost/token audit, ai_cost_daily for aggregation, recruiter_alerts for the cost-alerter, the 3 RPCs for promote/rollback, the cost-anomaly trigger for IA-04). The PRD/AUDITORIA specs use English table names that do not exist live — every FK must be retargeted (RESEARCH Pitfall 2/3).
Output: 4 migration files authored to the publish_vaga_rpc.sql convention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-PATTERNS.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-SQL-SMOKE-RUNBOOK.md

<interfaces>
<!-- Authoritative schema source + the exact in-repo analogs -->
Schema spec: docs/conhecimento/prompts/AUDITORIA-LGPD-LOGGING-VERSIONING.md §2 (full DDL — but FKs say candidates/jobs/recruiters; RETARGET).
RPC bodies: docs/prds/m2-funil-rh/PRD-ai-prompt-library-m2.md §8.1 (promote_to_canary, promote_canary_to_active, rollback_to_version, immutability trigger). PRD says GRANT TO admin — WRONG; use in-body check role = 'administrador' + GRANT TO authenticated (RESEARCH Pattern 4).

Live pt-BR table names (database.types.ts at repo ROOT): candidatos, vagas, usuarios_rh, candidaturas. There is NO candidates/jobs/recruiters/applications.

RLS table analog: supabase/migrations/20260607000004_bias_audit_log.sql
  - gen_random_uuid() PK, timestamptz DEFAULT now(), ENABLE ROW LEVEL SECURITY
  - CREATE POLICY ... FOR SELECT USING ((select auth.jwt() #>> '{app_metadata,role}') = 'administrador')
  - NO INSERT policy (EF service_role bypasses RLS); header note "No BEGIN/COMMIT wrapper"

RPC + DEFINER analog: supabase/migrations/20260607010004_publish_vaga_rpc.sql
  - LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''
  - in-body: v_role := (auth.jwt() #>> '{app_metadata,role}'); IF v_role NOT IN (...) RAISE ... errcode '42501'
  - domain errors errcode 'P0001'; escape literal % as %% in RAISE format
  - footer: REVOKE ALL ... FROM PUBLIC; GRANT EXECUTE ... TO authenticated; COMMENT ON FUNCTION

recruiter_alerts column set (orchestrator-decision #4 / RESEARCH Open Q2):
  id uuid pk, call_type text/llm_call_type, threshold_violated text, vaga_id uuid? -> vagas(id),
  candidato_id uuid? -> candidatos(id), value numeric, threshold numeric, channel text,
  is_read boolean default false, created_at timestamptz default now(), resolved_at timestamptz?
  RLS: administrador (and rh) read-only; service_role writes.

cost-anomaly trigger: RESEARCH Pattern 3 — notify_cost_anomaly() SECURITY DEFINER → net.http_post to
  /functions/v1/cost-alerter with Authorization: Bearer (vault.decrypted_secrets edge_invoke_key) +
  project_url from vault; AFTER INSERT OR UPDATE ON ai_cost_daily; dedup per Pitfall 7.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration — 6 tables + 3 enums + indexes + RLS + extensions</name>
  <read_first>
    - docs/conhecimento/prompts/AUDITORIA-LGPD-LOGGING-VERSIONING.md §2 (full DDL for prompt_versions, ai_call_logs, candidate_ai_decisions, ai_cost_daily; data_deletion_log shape referenced in §7.4)
    - supabase/migrations/20260607000004_bias_audit_log.sql (RLS table + no-wrapper analog)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md §Common Pitfalls (2 pt-BR FKs, 3 recruiter_alerts) + §Environment Availability (extensions)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-PATTERNS.md §schema migration
  </read_first>
  <files>supabase/migrations/20260609000001_prompt_library_schema.sql</files>
  <action>Author the schema migration (NO BEGIN/COMMIT wrapper; header note copied from publish_vaga_rpc.sql L26-33 explaining 42601). CREATE EXTENSION IF NOT EXISTS pg_cron; pg_net; pgcrypto (do NOT add pgmq — deferred to Phase 11). Create enums llm_call_type (the 7 values cv_summary, cv_job_match, comparative_ranking, interview_guide, transcript_analysis, culture_fit_essay, work_sample_sjt), llm_provider (anthropic, openai, google), candidate_status (the 10 values from AUDITORIA §2.1). Create the 6 tables from AUDITORIA §2.2-2.5 + data_deletion_log + recruiter_alerts, with EVERY FK retargeted: ai_call_logs.candidato_id REFERENCES public.candidatos(id) ON DELETE SET NULL, ai_call_logs.vaga_id REFERENCES public.vagas(id) ON DELETE SET NULL; candidate_ai_decisions.candidato_id/vaga_id likewise (reviewer_id REFERENCES public.usuarios_rh(id), NOT recruiters); ai_cost_daily.vaga_id REFERENCES public.vagas(id). prompt_versions includes the schema_version_required column (PRD §8.1) + the unique_active_per_type EXCLUDE constraint + all indexes from AUDITORIA §2.2-2.3. recruiter_alerts per the interfaces column set. Add the AUDITORIA indexes. ENABLE ROW LEVEL SECURITY on all 6 tables; add a FOR SELECT administrador policy on each (and additionally rh on recruiter_alerts per decision #4); NO INSERT/UPDATE/DELETE policy (writes only via service_role EF or SECURITY DEFINER RPC — RF-PL-29). COMMENT ON each table. Do NOT create the known_schema_versions table (out of v1 scope; schema_version_required is a plain TEXT default '1.0.0').</action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && f=supabase/migrations/20260609000001_prompt_library_schema.sql; grep -c "REFERENCES public.candidatos\|REFERENCES public.vagas" $f; echo "no English FKs:"; grep -E "REFERENCES (public\.)?(candidates|jobs|recruiters|applications)\b" $f && echo "BAD" || echo "OK"; echo "tables:"; grep -cE "CREATE TABLE public\.(ai_call_logs|prompt_versions|candidate_ai_decisions|ai_cost_daily|data_deletion_log|recruiter_alerts)" $f; echo "RLS:"; grep -c "ENABLE ROW LEVEL SECURITY" $f; echo "no wrapper:"; grep -iE "^\s*(BEGIN|COMMIT);" $f && echo "HAS WRAPPER" || echo "NO WRAPPER"</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: zero REFERENCES to candidates/jobs/recruiters/applications (English FK scan prints OK).
    - All 6 tables created (grep count 6); ENABLE ROW LEVEL SECURITY count is 6.
    - prompt_versions carries the unique_active_per_type EXCLUDE constraint + schema_version_required column.
    - recruiter_alerts has vaga_id, candidato_id, threshold_violated, value, threshold, created_at, resolved_at, channel.
    - No BEGIN/COMMIT wrapper; pg_cron + pg_net + pgcrypto extensions present; NO pgmq.
  </acceptance_criteria>
  <done>The schema migration creates the 6 AI tables (pt-BR FKs), 3 enums, indexes, admin-read RLS, and the 3 extensions — no English FKs, no wrapper, no pgmq.</done>
</task>

<task type="auto">
  <name>Task 2: RPCs + triggers migration — immutability + promote/rollback + cost-anomaly</name>
  <read_first>
    - docs/prds/m2-funil-rh/PRD-ai-prompt-library-m2.md §8.1 (the 3 RPC bodies + immutability trigger)
    - supabase/migrations/20260607010004_publish_vaga_rpc.sql (exact DEFINER + in-body role check + RAISE %% + footer analog)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md §Architecture Patterns (Pattern 3 pg_net trigger, Pattern 4 RPC role correction) + §Common Pitfalls (5 EXCLUDE race, 7 dedup)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-PATTERNS.md §rpcs migration
  </read_first>
  <files>supabase/migrations/20260609000002_prompt_library_rpcs.sql</files>
  <action>Author the RPC+trigger migration (NO wrapper; header note). (1) prevent_published_prompt_edit() BEFORE UPDATE trigger on prompt_versions: block changes to system_template/user_template/content_hash/semver when deployed_at IS NOT NULL (RF-PL-04), but ALLOW state-column changes (is_active/is_canary/deprecated_at) so rollback works (Pitfall 5). (2) promote_to_canary(p_call_type llm_call_type, p_semver text, p_canary_pct int default 10) SECURITY DEFINER SET search_path='': in-body role check NOT IN ('administrador') RAISE errcode 42501; validate canary_pct between 1 and 50 (RAISE P0001, escape % as %%); set is_canary=true, canary_pct, deployed_at=COALESCE(deployed_at,NOW()) where call_type+semver and deprecated_at IS NULL; RAISE P0001 if not found. (3) promote_canary_to_active(p_call_type, p_semver): in-body admin check; deactivate current active (is_active=false, deprecated_at=NOW()) THEN activate target (is_active=true, is_canary=false, canary_pct=0) — order matters for the EXCLUDE constraint (Pitfall 5); single transaction (migration body is one txn). (4) rollback_to_version(p_call_type, p_semver): in-body admin check; validate target exists and deprecated_at > NOW()-INTERVAL '1 year' (RAISE P0001 otherwise); deactivate current active, reactivate target (deprecated_at=NULL), INSERT a data_deletion_log audit row ('prompt_rollback:'||call_type||':'||semver). (5) notify_cost_anomaly() SECURITY DEFINER AFTER INSERT OR UPDATE ON ai_cost_daily: evaluate the PRD thresholds against NEW (vaga >R$200/mo, candidate cumulative >R$1, error_rate, spam) returning an alert_type or NULL; if non-null, dedup-check recruiter_alerts for a same-(threshold_violated, vaga_id, date) row in the bucket (Pitfall 7) and if absent PERFORM net.http_post(url := vault project_url || '/functions/v1/cost-alerter', headers with Authorization Bearer vault edge_invoke_key, body jsonb_build_object(alert_type, vaga_id, date, value, threshold)). Each RPC footer: REVOKE ALL FROM PUBLIC; GRANT EXECUTE TO authenticated; COMMENT ON FUNCTION (do NOT grant to admin — RESEARCH Pattern 4). Add a header note that Vault secrets project_url + edge_invoke_key are set manually (human-gated, Plan 07).</action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && f=supabase/migrations/20260609000002_prompt_library_rpcs.sql; echo "DEFINER fns:"; grep -c "SECURITY DEFINER" $f; echo "admin check:"; grep -c "'administrador'" $f; echo "no grant-to-admin:"; grep -iE "GRANT .* TO admin\b" $f && echo "BAD" || echo "OK"; echo "pg_net:"; grep -c "net.http_post" $f; echo "RPCs:"; grep -cE "FUNCTION public\.(promote_to_canary|promote_canary_to_active|rollback_to_version|prevent_published_prompt_edit|notify_cost_anomaly)" $f</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: >=5 SECURITY DEFINER functions; >=3 in-body 'administrador' checks (the 3 promote/rollback RPCs).
    - English/wrong grant scan (GRANT ... TO admin) prints OK (none present); footers GRANT TO authenticated.
    - net.http_post present in notify_cost_anomaly; dedup check against recruiter_alerts present.
    - immutability trigger blocks template/hash/semver edits but not is_active/is_canary/deprecated_at.
    - No BEGIN/COMMIT wrapper.
  </acceptance_criteria>
  <done>The RPC migration authors the immutability trigger, the 3 admin-only promote/rollback RPCs, and the cost-anomaly pg_net trigger — all DEFINER with in-body admin check, granted to authenticated, no wrapper.</done>
</task>

<task type="auto">
  <name>Task 3: Cron migration + seed migration</name>
  <read_first>
    - docs/conhecimento/prompts/AUDITORIA-LGPD-LOGGING-VERSIONING.md §7.2 (retention purge) + §7.3 (cost aggregation cron)
    - docs/prds/m2-funil-rh/PRD-ai-prompt-library-m2.md §6.4 RF-PL-22/23 + §3.1 (cron jobs)
    - supabase/migrations/20260607010001_pergunta_opcao_metadata.sql (seed-insert analog)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md §user_constraints (Area 1 — only cost-aggregation + retention-purge crons; pgmq deferred) + §Open Questions (3 — HITL-SLA + Art.18 descope)
  </read_first>
  <files>supabase/migrations/20260609000003_prompt_library_cron.sql, supabase/migrations/20260609000004_prompt_library_seed.sql</files>
  <action>(A) Cron migration (NO wrapper; header note): register EXACTLY two pg_cron jobs via cron.schedule — 'ai-cost-aggregation' at '30 1 * * *' (INSERT INTO ai_cost_daily aggregating yesterday's ai_call_logs grouped by date/vaga_id/call_type/provider with ON CONFLICT DO UPDATE, AUDITORIA §7.3) and 'ai-logs-retention-cleanup' at '0 2 * * *' (DELETE ai_call_logs WHERE retain_until < NOW() AND id NOT IN review-pending decisions, AUDITORIA §7.2). Do NOT register human-review-sla-check (orchestrator-decision #5 — HITL-SLA OUT OF SCOPE) and do NOT author delete_candidate_data / Art.18 (deferred to Phase 15). Header note documents the descope. (B) Seed migration (NO wrapper): INSERT 7 prompt_versions rows (one per call_type) at semver '1.0.0', is_active=false, is_canary=false, with content_hash computed via encode(digest(system_template||user_template, 'sha256'),'hex') (pgcrypto — AUDITORIA §3.3), schema_version_required '1.0.0', model_id per PRD decision #5 (haiku for cv_summary, sonnet for others), temperature 0, max_tokens per template, change_summary 'seed v1.0.0', changed_by. Use placeholder system_template/user_template bodies that reference the git template path (the real bodies are hydrated by sync-prompts.ts on first merge — RF-PL-07; the seed exists so the tables/admin pages are non-empty and the first manual activation has a row to flip). Header note: first activation per call_type is one-time manual SQL (is_active=true), NOT auto-activated (orchestrator-decision #2 / RESEARCH A8).</action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && c=supabase/migrations/20260609000003_prompt_library_cron.sql; s=supabase/migrations/20260609000004_prompt_library_seed.sql; echo "cron jobs:"; grep -c "cron.schedule" $c; echo "no SLA cron:"; grep -i "human-review-sla\|delete_candidate_data" $c && echo "BAD" || echo "OK"; echo "seed rows:"; grep -c "INSERT INTO public.prompt_versions\|INSERT INTO prompt_versions" $s; echo "is_active false in seed:"; grep -c "is_active" $s; grep -c "BEGIN;\|COMMIT;" $c $s</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: cron migration registers exactly 2 cron.schedule jobs; the HITL-SLA / Art.18 scan prints OK (none present).
    - Seed inserts 7 prompt_versions rows, all is_active=false / is_canary=false; content_hash via pgcrypto digest.
    - cv_summary seed row model_id is claude-haiku-4-5; the other 6 are claude-sonnet-4-6.
    - Neither migration has a BEGIN/COMMIT wrapper.
  </acceptance_criteria>
  <done>The cron migration registers only the cost-aggregation + retention-purge jobs; the seed inserts the 7 v1.0.0 prompts as is_active=false — both wrapper-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client -> promote/rollback RPC | state-changing privileged ops; RLS does not apply in DEFINER body |
| ai_cost_daily INSERT -> cost-alerter EF | trigger fires net.http_post to an internet-reachable EF endpoint |
| candidate input -> ai_call_logs | PII must be masked before any row is written (Plan 05 audit-logger) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Elevation of Privilege | promote_to_canary/promote_canary_to_active/rollback_to_version | mitigate | SECURITY DEFINER + SET search_path='' + in-body role check 'administrador' (42501); REVOKE PUBLIC; GRANT authenticated only |
| T-09-07 | Information Disclosure | ai_call_logs / candidate_ai_decisions raw rows to candidate | mitigate | RLS administrador-read only; NO candidate/anon SELECT policy; writes via service_role/DEFINER only |
| T-09-08 | Spoofing | cost-alerter EF reachable by public POST | mitigate | notify_cost_anomaly sends Authorization Bearer from Vault (edge_invoke_key); EF validates the shared secret (Plan 07) |
| T-09-09 | Tampering | silent edit of a deployed prompt | mitigate | prevent_published_prompt_edit() BEFORE-UPDATE trigger blocks template/hash/semver changes post-deploy |
| T-09-10 | Tampering | EXCLUDE-constraint race during rollback | mitigate | deactivate-then-activate ordering in one txn (Pitfall 5); SQL-SMOKE-3/4 verify post-apply |
| T-09-SC | Tampering | pg_cron/pg_net extension enablement | mitigate | CREATE EXTENSION IF NOT EXISTS only (Supabase-managed); no third-party package install |
</threat_model>

<verification>
- All 4 migrations authored; English-FK scan, no-wrapper scan, and admin-grant scan all pass.
- Cron registers only the 2 in-scope jobs; seed inserts 7 is_active=false rows.
- Apply + live SQL smokes are the [BLOCKING] Plan 07 task (not this plan) — these files are authored, not applied; build/types are a false-positive until applied.
</verification>

<success_criteria>
- IA-01/02/03/04 schema foundation authored against live pt-BR tables.
- recruiter_alerts created (decision #4 shape); 3 admin RPCs + immutability + cost-anomaly trigger authored.
- pgmq, HITL-SLA cron, and Art.18 deletion correctly OMITTED (deferred).
</success_criteria>

<output>
Create `.planning/phases/09-ai-prompt-library-cost-infra/09-03-SUMMARY.md` when done.
</output>
