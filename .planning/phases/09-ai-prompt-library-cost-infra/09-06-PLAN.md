---
phase: 09-ai-prompt-library-cost-infra
plan: 06
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - scripts/sync-prompts.ts
  - .github/workflows/prompts-sync.yml
autonomous: true
requirements: [IA-01]
must_haves:
  truths:
    - "sync-prompts.ts Zod-validates each template frontmatter, computes SHA-256 content_hash, and UPSERTs prompt_versions with is_active=false + is_canary=false, idempotent on content_hash (IA-01 / RF-PL-07)"
    - "prompts-sync.yml runs on merge to main path-filtered to docs/conhecimento/prompts/templates/** and fails on invalid frontmatter (RF-PL-07)"
    - "sync-prompts.test.ts (Plan 01) flips GREEN"
  artifacts:
    - path: "scripts/sync-prompts.ts"
      provides: "Deno git->DB sync (hash + frontmatter Zod + UPSERT)"
      contains: "content_hash"
    - path: ".github/workflows/prompts-sync.yml"
      provides: "path-filtered CI sync on merge to main"
      contains: "templates"
  key_links:
    - from: ".github/workflows/prompts-sync.yml"
      to: "scripts/sync-prompts.ts"
      via: "deno run on path-filter match"
      pattern: "sync-prompts"
    - from: "scripts/sync-prompts.ts"
      to: "prompt_versions"
      via: "UPSERT ON CONFLICT(content_hash) DO NOTHING"
      pattern: "content_hash"
---

<objective>
Implement the git->DB sync: a Deno script that hydrates prompt_versions from the 7 git templates (Zod-validated frontmatter + SHA-256 content_hash + idempotent UPSERT at is_active=false), and a path-filtered GitHub Action that runs it on merge to main. Runtime never reads the filesystem (Plan 05); this script is the only writer of new versions from git.

Purpose: IA-01 hybrid git->DB versioning — git is the authoring source, DB is runtime. Flips the Plan 01 sync-prompts RED test GREEN.
Output: scripts/sync-prompts.ts + .github/workflows/prompts-sync.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-PATTERNS.md

<interfaces>
<!-- content_hash + UPSERT + CI analog -->
contentHash (RESEARCH §Code Examples): SHA-256 of (system + user + JSON.stringify(frontmatter without content_hash))
  via Web Crypto crypto.subtle.digest('SHA-256', ...) -> hex.
Frontmatter Zod shape (RF-PL-01): id, call_type (one of the 7), semver (^\d+\.\d+\.\d+$), content_hash,
  schema_version_required, model_id, fallback_model_id, temperature, max_tokens, change_summary, changed_by, created_at.
UPSERT: prompt_versions (call_type, semver, content_hash, system_template, user_template, model_id, temperature,
  max_tokens, schema_version_required, change_summary, changed_by) with is_active=false, is_canary=false,
  ON CONFLICT (content_hash) DO NOTHING (RF-PL-07 idempotent). On semver collision with a DIFFERENT hash -> fail
  with the RF-PL-11 message.
CI analog: .github/workflows/ci.yml (existing). New workflow is path-filtered on docs/conhecimento/prompts/templates/**.
RED test (Plan 01): scripts/__tests__/sync-prompts.test.ts.
Supabase write: service_role key + project URL injected as GH secrets (the workflow runs server-side).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/sync-prompts.ts (Deno) — flips sync-prompts.test.ts GREEN</name>
  <read_first>
    - scripts/__tests__/sync-prompts.test.ts (Plan 01 RED contract — the exported functions this must provide)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md §Code Examples (content_hash) + §Common Pitfalls (5 unique_active EXCLUDE)
    - docs/conhecimento/prompts/templates/01-cv-summary.md (frontmatter to parse) + 00-shared-zod-schemas.ts (call_type set)
    - docs/prds/m2-funil-rh/PRD-ai-prompt-library-m2.md §6.2 RF-PL-07/11
  </read_first>
  <files>scripts/sync-prompts.ts</files>
  <action>Create scripts/ (does not exist yet — no analog). Implement sync-prompts.ts exporting (so the test imports them): parseTemplate(md) returning { frontmatter, system, user } (split the frontmatter block + extract the SYSTEM PROMPT and USER message sections), frontmatterSchema (Zod per RF-PL-01 — reject a missing field, reject a non-SemVer string), contentHash(system, user, fmWithoutHash) returning a deterministic SHA-256 hex via Web Crypto over canonical JSON.stringify, and syncAll(supabaseAdmin, templatesDir) that reads all *.md, validates frontmatter (throw on invalid), computes content_hash, and UPSERTs each into prompt_versions at is_active=false + is_canary=false with ON CONFLICT(content_hash) DO NOTHING; if a row with the same (call_type, semver) but a DIFFERENT content_hash exists, throw the RF-PL-11 error ("Cannot publish semver X — existing row has different content_hash. Either bump SemVer or revert change."). Never auto-activate (orchestrator-decision #2). Read service_role key + URL from Deno.env. No forbidden product copy.</action>
  <verify>
    <automated>command -v deno >/dev/null 2>&1 && deno test --no-check scripts/__tests__/sync-prompts.test.ts 2>&1 | tail -15 || echo "deno not on PATH — confirm exports"; cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && grep -c "contentHash\|frontmatterSchema\|is_active\|ON CONFLICT\|content_hash" scripts/sync-prompts.ts</automated>
  </verify>
  <acceptance_criteria>
    - test command: scripts/__tests__/sync-prompts.test.ts passes (GREEN).
    - source: exports parseTemplate, frontmatterSchema, contentHash, syncAll.
    - UPSERT writes is_active=false + is_canary=false with ON CONFLICT(content_hash) DO NOTHING.
    - RF-PL-11 semver-collision error present; no auto-activate.
  </acceptance_criteria>
  <done>sync-prompts.ts validates frontmatter, hashes deterministically, and UPSERTs the 7 prompts at is_active=false — sync-prompts.test.ts GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: .github/workflows/prompts-sync.yml (path-filtered)</name>
  <read_first>
    - .github/workflows/ci.yml (existing workflow analog — runner setup, secrets idiom)
    - scripts/sync-prompts.ts (the script the workflow runs)
    - docs/prds/m2-funil-rh/PRD-ai-prompt-library-m2.md §6.2 RF-PL-07 + §3.1 (GitHub Action build/CI)
  </read_first>
  <files>.github/workflows/prompts-sync.yml</files>
  <action>Author prompts-sync.yml: trigger on push to main with paths: ['docs/conhecimento/prompts/templates/**'] (path filter so it only runs when a template changes). Job: checkout, setup Deno (denoland/setup-deno), run `deno run --allow-read --allow-env --allow-net scripts/sync-prompts.ts` with env SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY from GH secrets. The step fails the build if any frontmatter is invalid (the script throws). Add a header comment documenting that this hydrates prompt_versions at is_active=false (activation is one-time manual SQL — orchestrator-decision #2) and that the SERVICE_ROLE secret must be configured by Fernando (do NOT VITE_-prefix it — CLAUDE.md hard rule). Do NOT modify ci.yml (the LGPD-04 grep guard already rides its unit job — RESEARCH/PATTERNS).</action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && grep -c "docs/conhecimento/prompts/templates\|sync-prompts\|SUPABASE_SERVICE_ROLE_KEY\|setup-deno" .github/workflows/prompts-sync.yml; echo "ci.yml unchanged:"; git diff --quiet .github/workflows/ci.yml && echo "OK" || echo "MODIFIED"</automated>
  </verify>
  <acceptance_criteria>
    - prompts-sync.yml exists, path-filtered to docs/conhecimento/prompts/templates/**, runs sync-prompts.ts via Deno.
    - Uses SUPABASE_SERVICE_ROLE_KEY from secrets (not VITE_-prefixed).
    - ci.yml is unchanged (git diff OK).
  </acceptance_criteria>
  <done>prompts-sync.yml runs the sync script on template merges to main with the service_role secret, failing on invalid frontmatter; ci.yml untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git template -> DB | the only path that writes new prompt_versions from authoring source |
| CI runner -> Supabase | service_role write; secret must never be VITE_-prefixed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-19 | Tampering | invalid/forged frontmatter reaching DB | mitigate | frontmatterSchema Zod validation throws -> build fails before UPSERT (RF-PL-07) |
| T-09-20 | Elevation of Privilege | service_role key leak in CI | mitigate | injected as GH secret only, never VITE_-prefixed, never logged (CLAUDE.md) |
| T-09-21 | Tampering | silent prompt activation via sync | mitigate | sync writes is_active=false only; activation is one-time manual SQL (decision #2) |
| T-09-SC | Tampering | Deno setup-action supply chain | accept | pinned official denoland/setup-deno; low-value CI target; same trust as existing ci.yml actions |
</threat_model>

<verification>
- sync-prompts.test.ts GREEN; workflow path-filtered + uses service_role secret; ci.yml untouched.
</verification>

<success_criteria>
- IA-01 git->DB sync complete: idempotent UPSERT at is_active=false + path-filtered CI.
- No auto-activation; no VITE_-prefixed service key; product language clean.
</success_criteria>

<output>
Create `.planning/phases/09-ai-prompt-library-cost-infra/09-06-SUMMARY.md` when done.
</output>
