---
phase: 17-navegacao-arquitetura-informacao
plan: 04
type: execute
wave: 2
depends_on: ["17-02"]
files_modified:
  - src/components/pages/DashboardCandidatoPage.tsx
  - src/components/pages/MeuPerfilCandidatoPage.tsx
  - src/components/RoleGuard.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-09: the candidate Dashboard is the funnel hub — it lists my candidaturas AND surfaces a step-CTA guided by etapa_atual that routes (by CLICK, not URL) to the pending screen via funilNavMap (e.g. /candidato/avaliacao/:id during avaliacao_assincrona); the candidate landing is repointed so the Dashboard is actually reachable as the primary post-login surface (ROLE_HOME.candidato → /candidato/dashboard, Pitfall 3 / A5)."
    - "D-10: the candidate Perfil is reduced to dados pessoais + edição — the duplicated 'VAGAS PARTICIPANDO' + 'PROGRESSO' candidatura lists are removed (resolving CAND-DASH-DUP-01); the funnel/candidatura list now lives only on the Dashboard."
    - "D-11: when a final decision exists (etapa_atual in {decisao_final, aprovado, rejeitado} and a decisão is present) the Dashboard shows an in-app LGPD card ('Entenda a decisão sobre sua candidatura') whose CTA links to /candidato/explicacao/:id — the only in-app path that exists today (no notification/email infra)."
    - "D-09 (drift guard): the Dashboard CTA narrows/casts candidatura.etapa_atual (typed as M1 legacy EtapaProcesso but runtime-valued as the M2 enum) to EtapaFunilM2 before the funilNavMap lookup, and falls back to a neutral 'Acompanhar candidatura' CTA for any unknown/stale value (never crash, never invent)."
    - "D-16 (journey #1 reachability): because the landing is repointed to Dashboard, the D-16 smoke journey #1 (candidato pós-candidatura → avaliação via Dashboard) starts where real users actually land — a click path, not a direct URL."
  artifacts:
    - path: "src/components/pages/DashboardCandidatoPage.tsx"
      provides: "Funnel step-CTA (funilNavMap) + LGPD card replacing the hardcoded 'Testes disponíveis' mock block"
      contains: "funilNavMap"
    - path: "src/components/pages/MeuPerfilCandidatoPage.tsx"
      provides: "Perfil reduced to dados pessoais + edição (candidatura lists removed)"
    - path: "src/components/RoleGuard.tsx"
      provides: "ROLE_HOME.candidato repointed to /candidato/dashboard"
      contains: "/candidato/dashboard"
  key_links:
    - from: "src/components/pages/DashboardCandidatoPage.tsx"
      to: "src/lib/navegacao/funilNavMap.ts"
      via: "step-CTA route lookup keyed by etapa_atual"
      pattern: "funilNavMap"
    - from: "src/components/pages/DashboardCandidatoPage.tsx"
      to: "/candidato/explicacao/:id"
      via: "LGPD card CTA when a final decision exists"
      pattern: "explicacao"
    - from: "src/components/RoleGuard.tsx"
      to: "/candidato/dashboard"
      via: "ROLE_HOME.candidato landing repoint"
      pattern: "/candidato/dashboard"
---

<objective>
Wire the candidate side of the funnel and resolve the Dashboard×Perfil overlap. Make the
Dashboard the funnel hub (D-09): replace its hardcoded "Testes disponíveis" mock block with a
funilNavMap-driven step-CTA routing to the pending screen, add the in-app LGPD card (D-11), and
repoint the candidate landing so the Dashboard is actually reached by real users (Pitfall 3 / A5).
Strip the duplicated candidatura lists from Perfil so it is dados pessoais + edição only (D-10).

Purpose: This closes the audit's ❌-QUEBRA candidate journey — "candidato pós-candidatura →
avaliação assíncrona" (today neither Dashboard nor Perfil links to /candidato/avaliacao/:id) —
and the CAND-DASH-DUP-01 functional overlap. The repoint guarantees the D-16 smoke journey #1
starts where users land.
Output: 3 edited files (edit-in-place per D-15). No new feature dir, no service/EF/migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-RESEARCH.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-PATTERNS.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-UI-SPEC.md

<interfaces>
funilNavMap (from 17-02, src/lib/navegacao/funilNavMap.ts):
  funilNavMap[etapa].rotaCandidato(candidaturaId) -> string | null   (avaliacao_assincrona -> /candidato/avaliacao/{id}; rejeitado -> /candidato/explicacao/{id}; else null)
  funilNavMap[etapa].label / .ctaCandidato ("Continuar para {label}")

DashboardCandidatoPage.tsx (already wired — REUSE, L1-31):
  import { useNavigate } from 'react-router-dom'
  import { useCandidaturas, useCandidaturasCount } from '@/features/vagas/hooks'
  const { data: candidaturasData } = useCandidaturas(filters, 'mais_recentes', { page:1, limit:50 })
  each candidatura carries: id (candidaturaId), etapa_atual, status, vaga
  Mock block to REPLACE: L311-402 ("Testes disponíveis" — Big Five "Concluído", DISC "45%", "Vagas Compatíveis 3")

DRIFT LANDMINE (verified, vagasTypes.ts L200-210/L351): Candidatura.etapa_atual is typed EtapaProcesso (M1: bigfive/disc/raven/cultura) but the runtime DB value is the M2 enum (avaliacao_assincrona, ...). funilNavMap is keyed on EtapaFunilM2. → cast/narrow at the read site; guard an undefined map lookup with the neutral "Acompanhar candidatura" CTA. Do NOT use ETAPA_PROCESSO_LABELS (M1) for the CTA label — use ETAPA_M2_LABELS via the map.

UI-SPEC candidate copy (verbatim):
  CTA (stage pending): "Continuar para {label}" + eyebrow "Próximo passo"
  CTA (no pending action): "Acompanhar candidatura"
  zero candidaturas empty: "Você ainda não se candidatou" / "Explore as vagas abertas e dê o primeiro passo." + "Ver vagas" -> /vagas
  LGPD card: title "Entenda a decisão sobre sua candidatura" / body "Você tem direito a uma explicação sobre como sua avaliação foi conduzida (LGPD Art. 20)." / CTA "Ver explicação" -> /candidato/explicacao/:id

MeuPerfilCandidatoPage.tsx (D-10 strip): remove "VAGAS PARTICIPANDO" + "PROGRESSO" blocks (L656-782); remove now-unused imports useCandidaturas (L15), ETAPA_PROCESSO_LABELS/STATUS_CANDIDATURA_LABELS/Candidatura (L16) if unused after the strip. KEEP handleSalvarDados (L55-104) supabase update + setCandidato sync (the dados+edit responsibility).

RoleGuard.tsx (L48-52): ROLE_HOME.candidato = '/candidato/perfil' -> repoint to '/candidato/dashboard' (Pitfall 3 / A5 / Open Q2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dashboard funnel step-CTA + LGPD card + landing repoint (D-09/D-11)</name>
  <files>src/components/pages/DashboardCandidatoPage.tsx, src/components/RoleGuard.tsx</files>
  <read_first>
    - src/components/pages/DashboardCandidatoPage.tsx (FULL — confirm L1-31 already-wired useCandidaturas + useNavigate; L270-290 the per-candidatura row; L311-402 the hardcoded "Testes disponíveis" mock block to REPLACE)
    - src/lib/navegacao/funilNavMap.ts (from 17-02 — funilNavMap[etapa].rotaCandidato + ctaCandidato + label)
    - src/features/vagas/types/vagasTypes.ts (L200-210, L351 — Candidatura.etapa_atual typed EtapaProcesso; the drift to cast/narrow)
    - src/features/triagem/services/triagemService.ts (L293-325 — EtapaFunilM2 the cast targets)
    - src/components/RoleGuard.tsx (L48-52 ROLE_HOME — the landing repoint; verify what else reads ROLE_HOME before changing)
    - src/components/pages/LoginCandidatoPage.tsx + CadastroMultiStepForm + FormularioCandidaturaPage (grep their post-flow navigate targets — confirm whether they navigate('/candidato/perfil') literally and need repointing alongside ROLE_HOME)
    - 17-PATTERNS.md (§DashboardCandidatoPage — already-present wiring + the DRIFT LANDMINE note + bg-primary caveat)
    - 17-UI-SPEC.md (§Step-guided CTAs + §LGPD card + §Color accent reserved list)
  </read_first>
  <behavior>
    - For a candidatura with etapa_atual 'avaliacao_assincrona', the step-CTA reads "Continuar para Avaliação Assíncrona" and navigates to /candidato/avaliacao/{candidatura.id} on click.
    - For an etapa with no candidate-facing screen (rotaCandidato null) the CTA falls back to neutral "Acompanhar candidatura".
    - For an unknown/stale (non-M2) etapa_atual value, the lookup is guarded → neutral "Acompanhar candidatura" (never crash).
    - When etapa_atual in {decisao_final, aprovado, rejeitado} and a decisão exists, the LGPD card renders with CTA → /candidato/explicacao/{candidatura.id}.
    - Navigating to /candidato/dashboard is the post-login landing for a candidato (ROLE_HOME repoint).
  </behavior>
  <action>
    Edit src/components/pages/DashboardCandidatoPage.tsx. Replace the hardcoded "Testes disponíveis" block (Big Five "Concluído", DISC "45%", Inteligência "0%", "Vagas Compatíveis 3") with the funnel step-CTA: import funilNavMap from '@/lib/navegacao/funilNavMap'; for the candidatura(s), cast/narrow candidatura.etapa_atual to EtapaFunilM2 and look it up — `const entry = funilNavMap[etapa as EtapaFunilM2]`; if entry is undefined (stale/unknown value) OR entry.rotaCandidato(candidatura.id) is null, render the neutral "Acompanhar candidatura" CTA; else render the single dominant accent CTA with eyebrow "Próximo passo" + text "Continuar para {entry.label}" → onClick navigate(entry.rotaCandidato(candidatura.id)). Use the existing useNavigate (already imported) — SPA, never <a href>. Add the LGPD card (D-11): when etapa_atual in {decisao_final, aprovado, rejeitado} and a final decisão exists for the candidatura, render the UI-SPEC card (title "Entenda a decisão sobre sua candidatura" / body / CTA "Ver explicação" → navigate(`/candidato/explicacao/${candidatura.id}`)). Keep the existing "minhas candidaturas" list (D-09 — Dashboard owns this). NEVER bg-primary (use bg-[#00109E] / accent bg-[#35BFAD]); NEVER "teste psicológico" (use "avaliação comportamental/cognitiva"). Do NOT hardcode any score/percentage.

    Edit src/components/RoleGuard.tsx: repoint ROLE_HOME.candidato from '/candidato/perfil' to '/candidato/dashboard' (Pitfall 3 / A5). If the read_first grep shows LoginCandidatoPage / CadastroMultiStepForm / FormularioCandidaturaPage navigate literally to '/candidato/perfil' as their post-flow landing, repoint those to '/candidato/dashboard' as well so real users land on the funnel hub (the smoke journey #1 starts here). Keep Perfil reachable from the CandidatoNavbar "Área"/profile affordance (do not orphan Perfil — D-10 keeps it as dados+edição).
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -3 ; grep -c "funilNavMap\|Acompanhar candidatura\|Continuar para\|explicacao" src/components/pages/DashboardCandidatoPage.tsx ; grep -c "/candidato/dashboard" src/components/RoleGuard.tsx ; grep -c "bg-primary\|teste psicológico" src/components/pages/DashboardCandidatoPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DashboardCandidatoPage references funilNavMap + the "Continuar para"/"Acompanhar candidatura" CTAs + the explicacao LGPD path.
    - The hardcoded "45%"/"Concluído"/"Vagas Compatíveis 3" mock block is gone (`grep -Ec "45%|Vagas Compatíveis" src/components/pages/DashboardCandidatoPage.tsx` === 0).
    - etapa_atual is cast/narrowed to EtapaFunilM2 with an undefined-lookup guard → neutral CTA (no unguarded `funilNavMap[etapa].rotaCandidato` deref).
    - ROLE_HOME.candidato === '/candidato/dashboard' (`grep -c "/candidato/dashboard" src/components/RoleGuard.tsx` ≥ 1).
    - `grep -c "bg-primary\|teste psicológico" src/components/pages/DashboardCandidatoPage.tsx` === 0.
    - tsc baseline (~301) not grown; build green.
  </acceptance_criteria>
  <done>Dashboard is the funnel hub (step-CTA via funilNavMap + LGPD card); landing repointed to Dashboard; drift-guarded; mock block removed.</done>
</task>

<task type="auto">
  <name>Task 2: Strip candidatura lists from Perfil — dados pessoais + edição only (D-10)</name>
  <files>src/components/pages/MeuPerfilCandidatoPage.tsx</files>
  <read_first>
    - src/components/pages/MeuPerfilCandidatoPage.tsx (FULL — L15-17 imports, L25 useCandidaturas call, L55-104 handleSalvarDados to KEEP, L656-782 the "VAGAS PARTICIPANDO" + "PROGRESSO" blocks to REMOVE, the dados-pessoais/senha forms to KEEP)
    - src/components/pages/DashboardCandidatoPage.tsx (confirm the candidatura list lives there now — D-09 — so removing it from Perfil does not lose the capability)
    - 17-PATTERNS.md (§MeuPerfilCandidatoPage — what to remove vs keep)
    - 17-RESEARCH.md (§State of the Art — Perfil uses M1 ETAPA_PROCESSO_LABELS; the candidate funnel surfaces should not; the strip removes that consumer)
  </read_first>
  <action>
    Edit src/components/pages/MeuPerfilCandidatoPage.tsx. Remove the "VAGAS PARTICIPANDO" and "PROGRESSO NO PROCESSO SELETIVO" candidatura-list blocks (the duplicated funnel/status lists — the CAND-DASH-DUP-01 overlap). Remove the now-unused imports: useCandidaturas, and ETAPA_PROCESSO_LABELS / STATUS_CANDIDATURA_LABELS / Candidatura type IF they become unused after the strip (run tsc to confirm no dangling reference). KEEP everything that is dados pessoais + edição: the personal-data form, the password form, handleSalvarDados (supabase update + Zustand setCandidato sync), the CandidatoNavbar shell. Perfil becomes purely dados+edição; the candidatura list + funnel CTA live only on the Dashboard. Do not introduce bg-primary. No fenced code blocks.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 ; grep -Ec "VAGAS PARTICIPANDO|PROGRESSO NO PROCESSO|ETAPA_PROCESSO_LABELS|useCandidaturas" src/components/pages/MeuPerfilCandidatoPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "VAGAS PARTICIPANDO|PROGRESSO NO PROCESSO|ETAPA_PROCESSO_LABELS|useCandidaturas" src/components/pages/MeuPerfilCandidatoPage.tsx` === 0 (lists + their M1-label consumer + the candidatura fetch all removed).
    - handleSalvarDados and the dados-pessoais/senha forms are preserved (grep `handleSalvarDados` ≥ 1).
    - `npm run build` exits 0 (no dangling import after the strip); tsc baseline (~301) not grown.
  </acceptance_criteria>
  <done>Perfil reduced to dados pessoais + edição; duplicated candidatura lists removed; build green; CAND-DASH-DUP-01 resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidato → own-row reads | Dashboard/Perfil read the candidate's own candidaturas (candidate-facing own-row reads behind RLS). |
| Dashboard CTA → route | The step-CTA + LGPD CTA navigate to internal routes parameterized by the candidatura's own id. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-PII | Information Disclosure | Dashboard/Perfil own-row reads | mitigate | Reuse the existing useCandidaturas hook (already RLS-gated + allowlist-projected) — this plan adds NO new candidate-facing select. The Perfil strip REMOVES reads; the Dashboard reuses what is already there. No `select('*')`. |
| T-17-04-ID | Tampering | Dashboard CTA route param | mitigate | The step-CTA + LGPD CTA interpolate the candidatura's OWN id (candidatura.id from the user's own list) into a fixed internal template; no user-supplied target, no open-redirect; the etapa cast is guarded so a stale value yields a neutral CTA, never a crash or a wrong-id route. |
| T-17-04-LAND | Elevation of Privilege | ROLE_HOME landing repoint | accept | Repointing ROLE_HOME.candidato to /candidato/dashboard changes only WHERE a candidato lands; both /candidato/perfil and /candidato/dashboard are already candidato-RoleGuard-protected — no cross-role exposure introduced. |
| T-17-04-SC | Tampering | npm installs | accept | Zero packages installed (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npm run build` → exits 0; `npm run lint` → tsc baseline (~301) not grown.
- `grep -c "funilNavMap" src/components/pages/DashboardCandidatoPage.tsx` → ≥1 (step-CTA wired).
- `grep -c "/candidato/dashboard" src/components/RoleGuard.tsx` → ≥1 (landing repointed).
- `grep -Ec "VAGAS PARTICIPANDO|PROGRESSO NO PROCESSO" src/components/pages/MeuPerfilCandidatoPage.tsx` → 0 (overlap removed).
- `grep -Ec "bg-primary|teste psicológico" src/components/pages/DashboardCandidatoPage.tsx` → 0.
</verification>

<success_criteria>
The candidate Dashboard is the funnel hub — step-CTA (funilNavMap, drift-guarded) + LGPD card — and is the post-login landing; Perfil is dados pessoais + edição only (CAND-DASH-DUP-01 resolved); build + tsc clean; no bg-primary, no banned product language, no new PII-leak select.
</success_criteria>

<output>
Create `.planning/phases/17-navegacao-arquitetura-informacao/17-04-SUMMARY.md` when done.
</output>
