---
phase: 17-navegacao-arquitetura-informacao
plan: 05
type: execute
wave: 3
depends_on: ["17-02", "17-03", "17-04"]
files_modified:
  - src/router/routes.tsx
  - src/components/pages/VagaLPPage.tsx
  - src/components/pages/TesteBigFivePage.tsx
  - src/components/pages/TesteDISCPage.tsx
  - src/components/pages/TesteRavenPage.tsx
  - src/components/pages/InstrucoesBigFivePage.tsx
  - src/components/pages/InstrucoesDISCPage.tsx
  - src/components/pages/InstrucoesRavenPage.tsx
  - src/components/pages/ConclusaoTestesPage.tsx
  - src/components/pages/QuestionarioPage.tsx
  - src/components/pages/QuestionarioCulturaPage.tsx
  - src/components/pages/InscricaoPage.tsx
  - src/components/GlassShowcase.tsx
  - e2e/navegacao.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-12: conservative legacy deletion — ONLY the proven-dead set is hard-deleted (file + routes.tsx import + route object + devNavigationPages entry, atomically): VagaLPPage (unrouted, zero-import) plus the verified zero-inbound-nav set (/testes/* tree = TesteBigFive/DISC/Raven + their Instrucoes* + ConclusaoTestes, QuestionarioPage, QuestionarioCulturaPage, InscricaoPage, GlassShowcase). MeuPerfilPage (/rh/perfil) is KEPT — it has a live RHTopBar.tsx:38 entry; each deletion re-greps zero-use before removing."
    - "D-12 (gate): the legacy-routes grep guard (17-01) flips GREEN — the 12 deleted components have zero routes.tsx import/route/devNav reference and MeuPerfilPage stays referenced; tsc/build stay green (no dangling import)."
    - "D-16: the Playwright navegability smoke (e2e/navegacao.spec.ts) promotes from RED to GREEN — the 4 audit-broken journeys resolve to the right route/heading: (1) candidato pós-candidatura → avaliação via Dashboard; (2) RH TriagemTable → hub → each of 3 workspaces; (3) admin → sidebar → /admin/*; (4) URL inválida → 404; RH/admin gated behind E2E_AUTH_TEST_USERS."
    - "D-01: this closes the 7 nav-audit recommendations as a standalone Phase 17 — funnel wired (candidato + RH), mock retired, admin entry, 404, legacy removed, all proven by the navegable-journey E2E gate (DoD = D-03)."
  artifacts:
    - path: "e2e/navegacao.spec.ts"
      provides: "GREEN navegability smoke — 4 journeys resolve to route/heading"
    - path: "src/router/routes.tsx"
      provides: "Legacy imports + route objects + devNav entries removed for the confirmed-dead set"
  key_links:
    - from: "src/__tests__/guards/legacy-routes.grep.test.ts"
      to: "src/router/routes.tsx"
      via: "grep guard asserting zero references to deleted components"
      pattern: "routes.tsx"
    - from: "e2e/navegacao.spec.ts"
      to: "/rota/invalida"
      via: "journey 4 → NotFound heading"
      pattern: "Página não encontrada"
---

<objective>
Close the phase: hard-delete ONLY the proven-dead legacy (D-12, conservative — each re-grepped
zero-use before removal, MeuPerfilPage KEPT), and promote the Playwright navegability smoke
(D-16) from RED to GREEN now that the funnel is wired (17-02/03/04). This is the last wave because
the deletion edits routes.tsx (which 17-02 also edited — different wave avoids the collision) and
the E2E gate needs all wiring in place.

Purpose: D-03 makes the navegable-journey E2E the Definition of Done; D-01 frames Phase 17 as the
standalone closure of the 7 nav-audit recommendations. The legacy purge removes ~3-4k lines of
dead code without touching anything with a live entry.
Output: 12 deleted files + routes.tsx scrub + e2e/navegacao.spec.ts promoted GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-RESEARCH.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-PATTERNS.md

<interfaces>
Confirmed-dead set (RESEARCH §Legacy Cleanup Verdicts — each has zero external importers + zero in-app nav to its route):
  VagaLPPage (src/components/pages/VagaLPPage.tsx — UNROUTED, zero-import, 1213 LoC)
  /testes/* tree: TesteBigFivePage, TesteDISCPage, TesteRavenPage, InstrucoesBigFivePage, InstrucoesDISCPage, InstrucoesRavenPage, ConclusaoTestesPage
  QuestionarioPage (/candidato/questionario)
  QuestionarioCulturaPage (/candidato/questionario-cultura)
  InscricaoPage (/auth/inscricao — superseded by /cadastro)
  GlassShowcase (src/components/GlassShowcase.tsx — /showcase)

KEEP (do NOT delete): MeuPerfilPage (/rh/perfil) — LIVE entry at RHTopBar.tsx:38 navigate('/rh/perfil').

Per-file deletion = 4 atomic edits (Pitfall 6 / Runtime State Inventory):
  (1) delete the page file
  (2) delete its `import { X } from ...` in routes.tsx (imports L11-90)
  (3) delete its RouteObject in routes.tsx (the path entry)
  (4) delete its entry in the devNavigationPages array (routes.tsx ~L533-567)
  → run `npm run build` (tsc) after each file to catch a stale import early.

routes.tsx confirmed-dead route paths (verified): GlassShowcase L122 (/showcase), InscricaoPage L130 (/auth/inscricao), QuestionarioCulturaPage L201, QuestionarioPage L209, InstrucoesBigFive L301, TesteBigFive L309, InstrucoesDISC L317, TesteDISC L325, InstrucoesRaven L333, TesteRaven L341, ConclusaoTestes L349.

legacy-routes grep guard (17-01, src/__tests__/guards/legacy-routes.grep.test.ts) — the gate to flip GREEN (12 dead names = 0 refs; MeuPerfilPage > 0 refs).

e2e/navegacao.spec.ts (17-01) — journeys to promote:
  J4 (unconditional): goto('/rota/invalida/xyz') → heading /Página não encontrada/ + back-link /Voltar/
  J1 (gated): candidato login → /candidato/dashboard → step CTA → /candidato/avaliacao/:id heading
  J2 (gated, administrador cred): login-rh → TriagemTable → click name → hub heading → "Abrir {label}" → workspace headings ("Entrevista" / "Decisão final" / Redação panel)
  J3 (gated): administrador → sidebar "Admin" → /admin/* heading
  Real-auth gate: E2E_AUTH_TEST_USERS==='true' (login-flow.spec.ts precedent). Known gap (A3): only administrador creds exist (0 recrutador rows) — admin cred covers J2+J3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conservative legacy hard-delete + routes.tsx scrub (D-12)</name>
  <files>src/router/routes.tsx, src/components/pages/VagaLPPage.tsx, src/components/pages/TesteBigFivePage.tsx, src/components/pages/TesteDISCPage.tsx, src/components/pages/TesteRavenPage.tsx, src/components/pages/InstrucoesBigFivePage.tsx, src/components/pages/InstrucoesDISCPage.tsx, src/components/pages/InstrucoesRavenPage.tsx, src/components/pages/ConclusaoTestesPage.tsx, src/components/pages/QuestionarioPage.tsx, src/components/pages/QuestionarioCulturaPage.tsx, src/components/pages/InscricaoPage.tsx, src/components/GlassShowcase.tsx</files>
  <read_first>
    - src/router/routes.tsx (FULL — the import region L11-90, the legacy route objects at the verified line numbers in <interfaces>, the devNavigationPages array ~L533-567)
    - 17-RESEARCH.md (§Legacy Cleanup Verdicts table + §Runtime State Inventory — the 4-edit deletion + KEEP MeuPerfilPage)
    - 17-PATTERNS.md (§routes.tsx — legacy deletion scrub three references + run build after each)
    - src/__tests__/guards/legacy-routes.grep.test.ts (17-01 — the gate this task flips GREEN)
  </read_first>
  <action>
    Per-file re-grep-then-delete for the confirmed-dead set ONLY (D-12 conservative). For EACH component in the <interfaces> dead set: FIRST re-confirm zero use — run `grep -rn "<ComponentName>" src/ e2e/ tests/ | grep -v "routes.tsx" | grep -v "<the file itself>"` and confirm ZERO matches (if a live importer surfaces unexpectedly, STOP and defer that file per D-12, do not delete). THEN perform the 4-edit atomic removal: delete the page/component file; delete its `import { X }` line in routes.tsx; delete its RouteObject in routes.tsx; delete its devNavigationPages entry. Run `npm run build` after each file (or each small batch) to catch a stale import immediately (Pitfall 6). Do this for: VagaLPPage (file-only — it is UNROUTED, so only step 1 + confirm it has no import/route/devNav entry), TesteBigFivePage, TesteDISCPage, TesteRavenPage, InstrucoesBigFivePage, InstrucoesDISCPage, InstrucoesRavenPage, ConclusaoTestesPage, QuestionarioPage, QuestionarioCulturaPage, InscricaoPage, GlassShowcase. Do NOT touch MeuPerfilPage / /rh/perfil (live entry RHTopBar.tsx:38 — KEEP). Do NOT touch DevNavigationMenu/App.tsx (D-02). Use `git -c core.hooksPath=/dev/null` for commits (tsc baseline bypass convention) and do NOT grow the tsc baseline.
  </action>
  <verify>
    <automated>npm run test:run -- legacy-routes 2>&1 | tail -15 ; npm run test:run -- devnav-gate 2>&1 | tail -5 ; npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 17-01 legacy-routes grep guard flips GREEN: the 12 dead component names have zero routes.tsx references; MeuPerfilPage stays referenced (>0).
    - The 12 page files no longer exist (`ls` of each path errors); MeuPerfilPage.tsx still exists.
    - `npm run build` exits 0 (no dangling import); devnav-gate grep guard stays GREEN (App.tsx untouched — D-02).
    - tsc baseline (~301) not grown (deletions should hold it flat or reduce it).
    - Each deletion was preceded by a zero-use re-grep (documented in the SUMMARY); any file that surfaced a live importer was deferred, not force-deleted.
  </acceptance_criteria>
  <done>Confirmed-dead legacy purged (file + 3 routes.tsx refs each); MeuPerfilPage kept; legacy grep guard green; build green; DevNav intact.</done>
</task>

<task type="auto">
  <name>Task 2: Promote the navegability smoke RED → GREEN (D-16 / D-03 DoD)</name>
  <files>e2e/navegacao.spec.ts</files>
  <read_first>
    - e2e/navegacao.spec.ts (the 17-01 RED scaffold — the 4 journeys to finalize/promote)
    - e2e/login-flow.spec.ts (L17-73 — REAL_AUTH gate + TEST_USER; reuse for J1/J2/J3 login)
    - e2e/explicacao-flow.spec.ts (L39-67 — login(page) helper + heading/CTA assertion idiom)
    - src/lib/navegacao/funilNavMap.ts (confirm the candidate/RH route targets the smoke asserts resolve to)
    - src/features/entrevista/components/EntrevistaWorkspace.tsx (L114 heading "Entrevista") + src/features/decisao/components/DecisaoFinalPage.tsx (L131 heading "Decisão final") + src/features/triagem/components/RedacaoReviewPanel.tsx (the Redação panel heading) — the workspace headings J2 asserts
    - src/components/pages/NotFoundPage.tsx (the "Página não encontrada" heading J4 asserts)
    - 17-PATTERNS.md (§e2e/navegacao.spec.ts — three combined analogs + heading list)
  </read_first>
  <action>
    Finalize e2e/navegacao.spec.ts so the 4 journeys assert real, now-wired targets. J4 (unconditional): goto('/rota/invalida/xyz') → expect heading /Página não encontrada/i visible + back-link /Voltar/i visible (now GREEN — NotFoundPage + catch-all landed in 17-02). J1 (gated describeRealAuth): candidato login → land on /candidato/dashboard (landing repointed in 17-04) → click the step CTA (getByRole name /Continuar para|Acompanhar/i) → expect URL matches /\/candidato\/avaliacao\// OR the avaliação heading visible (route, not data). J2 (gated, administrador cred): login-rh → open a vaga TriagemTable (/rh/vagas/:id/candidatos) → click a candidate name (now SPA → hub) → expect the hub heading visible → click "Abrir {label}" (getByRole name /Abrir /i) → expect the matching workspace heading among "Entrevista" / "Decisão final" / the Redação panel heading. J3 (gated): administrador → expect sidebar getByRole name /Admin/i visible → click → expect URL matches /\/admin\// + an admin heading. Keep the file-top comment documenting the rh-role seed gap (A3 — admin cred covers J2+J3). Assert ONLY route/heading/CTA resolution (D-16) — never data flow. Run the unconditional J4 to confirm GREEN; the gated journeys run when E2E_AUTH_TEST_USERS=true + creds.
  </action>
  <verify>
    <automated>npx playwright test navegacao --list 2>&1 | tail -20 ; npx playwright test navegacao --grep "404|não encontrada|invalida" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - e2e/navegacao.spec.ts lists under all 3 Playwright projects; the 4 journeys present.
    - J4 (404, unconditional) runs and PASSES against the wired build (heading /Página não encontrada/ now resolves — was RED in 17-01).
    - J1/J2/J3 are inside the E2E_AUTH_TEST_USERS gate (grep confirms `E2E_AUTH_TEST_USERS`); their assertions target the now-wired routes/headings (avaliação, hub, "Entrevista"/"Decisão final"/Redação, /admin/*).
    - Assertions are route/heading/CTA only (getByRole heading/link/button) — no response-body/data assertions.
    - File-top comment records the rh-role seed gap (A3).
  </acceptance_criteria>
  <done>Navegability smoke promoted: J4 GREEN unconditionally; J1-J3 gated assert the wired routes/headings; DoD (D-03) satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time deletion | Legacy file removal is code-level; no runtime/user surface; the only references are in routes.tsx (verified). |
| test harness → live auth (gated) | J1-J3 log in against real auth only when E2E_AUTH_TEST_USERS=true; creds from env. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05-DEL | Denial of Service (self-inflicted build break) | routes.tsx legacy scrub | mitigate | Each deletion is the full 4-edit atomic removal + `npm run build` after each file (Pitfall 6); a stale import is caught immediately. A per-file zero-use re-grep precedes deletion (D-12 conservative) so nothing with a live entry is removed (MeuPerfilPage explicitly KEPT). |
| T-17-05-DEV | Information Disclosure | DevNav coverage | accept | DevNavigationMenu stays DEV-gated and untouched (D-02); the devnav-gate grep guard re-runs GREEN. Production nav now covers the funnel, but DevNav removal is explicitly deferred until 100% coverage is confirmed. |
| T-17-05-CRED | Information Disclosure | e2e/navegacao.spec.ts real-auth | mitigate | Creds from process.env; J1-J3 gated behind describeRealAuth so CI without creds skips them (login-flow.spec.ts precedent). |
| T-17-05-SC | Tampering | npm installs | accept | Zero packages installed (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npm run test:run -- legacy-routes devnav-gate` → both GREEN (legacy purged, DevNav gate intact).
- `npm run build` → exits 0 (no dangling import after deletions); `npm run lint` → tsc baseline (~301) not grown.
- `npx playwright test navegacao --grep "404|não encontrada"` → J4 GREEN.
- `npx playwright test navegacao --list` → 4 journeys list under all 3 projects.
- Full `npm run test:run` → green (no regression from deletions).
</verification>

<success_criteria>
Confirmed-dead legacy removed conservatively (12 files + their routes.tsx refs, MeuPerfilPage kept), legacy grep guard GREEN; the navegability smoke is promoted — J4 (404) GREEN unconditionally and J1-J3 assert the wired routes/headings behind the real-auth gate; build + full Vitest + tsc clean; DevNav untouched. Phase 17 closes the 7 nav-audit recommendations (D-01) with the DoD E2E gate satisfied (D-03).
</success_criteria>

<output>
Create `.planning/phases/17-navegacao-arquitetura-informacao/17-05-SUMMARY.md` when done.
</output>
