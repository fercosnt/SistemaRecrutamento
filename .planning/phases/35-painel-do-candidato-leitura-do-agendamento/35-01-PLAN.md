---
phase: 35-painel-do-candidato-leitura-do-agendamento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/datetime/formatDataHoraSP.ts
  - src/lib/datetime/__tests__/formatDataHoraSP.test.ts
  - src/features/entrevista/components/EntrevistaDashboard.tsx
  - src/features/agendamento/services/agendamentoCandidatoService.ts
  - src/features/agendamento/hooks/useMeuAgendamento.ts
  - src/features/agendamento/services/__tests__/agendamentoCandidatoService.test.ts
autonomous: true
requirements: [AGEND-04]
must_haves:
  truths:
    - "getMeuAgendamento reads the candidate own-row via supabase.rpc('get_meu_agendamento', { p_candidatura_id }) — NEVER supabase.from('agendamentos_entrevista'), NEVER select('*')"
    - "MeuAgendamentoRow exposes ONLY the 7 allowlist columns (id, candidatura_id, tipo, data_hora, local_ou_link, status, compareceu) — never observacoes_rh / entrevistador / agendado_por / updated_by / vaga_id"
    - "local_ou_link and compareceu are typed `| null` on the domain Row (the generator marks the RETURNS TABLE non-null but the columns are nullable)"
    - "formatDataHoraSP formats an ISO timestamptz as `dd/mm/aaaa às hh:mm` pinned to America/Sao_Paulo (not browser-local), incl. the midnight '24'→'00' engine edge"
    - "EntrevistaDashboard still renders/behaves identically after re-importing the extracted formatDataHoraSP (behavior-preserving refactor, no external caller broke)"
    - "useMeuAgendamento mirrors useAgendamento's read half: hierarchical key factory + useQuery(staleTime/gcTime 5min, retry 2, enabled:!!candidaturaId)"
  artifacts:
    - path: "src/lib/datetime/formatDataHoraSP.ts"
      provides: "Shared America/Sao_Paulo timezone formatter (extracted from EntrevistaDashboard)"
      exports: ["formatDataHoraSP", "saoPauloParts"]
    - path: "src/features/agendamento/services/agendamentoCandidatoService.ts"
      provides: "Candidate own-row read of the interview schedule via the DEFINER RPC + allowlist Row + error class"
      exports: ["getMeuAgendamento", "MeuAgendamentoRow", "MeuAgendamentoServiceError"]
    - path: "src/features/agendamento/hooks/useMeuAgendamento.ts"
      provides: "TanStack read hook + query-key factory for the candidate agendamento"
      exports: ["useMeuAgendamento", "meuAgendamentoKeys"]
    - path: "src/features/agendamento/services/__tests__/agendamentoCandidatoService.test.ts"
      provides: "RPC-guard test (rpc('get_meu_agendamento') called, base table never touched, Row carries no RH-internal key)"
    - path: "src/lib/datetime/__tests__/formatDataHoraSP.test.ts"
      provides: "SP-timezone formatter unit test (known UTC instant + midnight edge + null in/out)"
  key_links:
    - from: "src/features/agendamento/services/agendamentoCandidatoService.ts"
      to: "get_meu_agendamento (Postgres DEFINER RPC)"
      via: "supabase.rpc"
      pattern: "rpc\\(['\"]get_meu_agendamento"
    - from: "src/features/entrevista/components/EntrevistaDashboard.tsx"
      to: "src/lib/datetime/formatDataHoraSP.ts"
      via: "import"
      pattern: "from '@/lib/datetime/formatDataHoraSP'"
---

<objective>
Deliver the candidate-facing READ foundation for AGEND-04: the own-row, allowlist-only
data path to the scheduled interview, plus the shared America/Sao_Paulo timezone
formatter that both the RH dashboard and the (Plan 35-02) candidate card consume.

This is the security-critical half of the phase (SEG-03 consumer discipline): the
candidate must read the interview ONLY through the `get_meu_agendamento` DEFINER RPC —
never the base table, never `select('*')`, never an RH-internal column. No UI ships in
this plan; it is 100% unit-testable data/logic (the primary Nyquist seams).

Maps to ROADMAP sub-task 35-01 (own-row allowlist read) — the visible card + `.ics` +
badge land in Plan 35-02.

Purpose: Establish the typed, tested, server-enforced read primitive + shared TZ util
before any component wires against them (interface-first).
Output: `formatDataHoraSP` util, `agendamentoCandidatoService`, `useMeuAgendamento`
hook, and their unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-painel-do-candidato-leitura-do-agendamento/35-CONTEXT.md
@.planning/phases/35-painel-do-candidato-leitura-do-agendamento/35-RESEARCH.md
@.planning/phases/35-painel-do-candidato-leitura-do-agendamento/35-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

Generated RPC signature (database.types.ts:4792-4803 — VERIFIED, do NOT hand-edit database.types.ts):
  Functions['get_meu_agendamento']:
    Args:    { p_candidatura_id: string }
    Returns: { id, candidatura_id, tipo, data_hora, local_ou_link, status, compareceu }[]
    status:  Enums['status_entrevista']  = agendada|em_andamento|concluida|cancelada|reagendada|nao_compareceu
    tipo:    Enums['tipo_entrevista_avaliacao'] = online|presencial
  NOTE: generator marks local_ou_link:string and compareceu:boolean NON-NULL, but the
  underlying columns ARE nullable → widen to `| null` in the domain Row (Pitfall 1).

Server-side allowlist (supabase/migrations/20260716000001_agendamentos_entrevista.sql:133-168):
  RETURNS TABLE(id, candidatura_id, tipo, data_hora, local_ou_link, status, compareceu)
  Ownership enforced INSIDE the fn: JOIN candidaturas→candidatos WHERE ca.user_id = auth.uid()
  ORDER BY a.data_hora DESC · deleted_at IS NULL · EXCLUDES observacoes_rh/entrevistador/agendado_por/updated_by/vaga_id.
  There is NO candidate base-table SELECT policy → a `.from('agendamentos_entrevista')` read returns 0 rows.

Reuse by import (do NOT redefine):
  From src/features/agendamento/services/agendamentoService.ts —
    export type TipoAgendamento = 'online' | 'presencial'
    export type StatusAgendamento = 'agendada'|'em_andamento'|'concluida'|'cancelada'|'reagendada'|'nao_compareceu'
    export class AgendamentoServiceError (mirror its shape for MeuAgendamentoServiceError)

Analog to mirror (read-half only, drop the 4 write mutations):
  From src/features/agendamento/hooks/useAgendamento.ts:27-49 — key factory + useQuery(STALE=5*60*1000, retry:2, enabled:!!id)

Extraction source (verbatim) — src/features/entrevista/components/EntrevistaDashboard.tsx:44-73:
  const DISPLAY_TIME_ZONE = 'America/Sao_Paulo'
  saoPauloParts(iso) → { dd, mm, yyyy, hh, min } | null  (Intl.DateTimeFormat('pt-BR',{timeZone,...}).formatToParts, hh '24'→'00')
  formatDataHora(iso) → `${dd}/${mm}/${yyyy} às ${hh}:${min}`  (rename export → formatDataHoraSP)
  KEEP LOCAL in EntrevistaDashboard: formatCurto (tooltip) + compute24hMarker (RH label) — the candidate needs neither.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the shared America/Sao_Paulo formatter + refactor EntrevistaDashboard</name>
  <files>src/lib/datetime/formatDataHoraSP.ts, src/lib/datetime/__tests__/formatDataHoraSP.test.ts, src/features/entrevista/components/EntrevistaDashboard.tsx</files>
  <read_first>
    - src/features/entrevista/components/EntrevistaDashboard.tsx (lines 44-109 — the DISPLAY_TIME_ZONE / saoPauloParts / formatDataHora / formatCurto / compute24hMarker block being lifted/repointed)
    - .planning/phases/35-painel-do-candidato-leitura-do-agendamento/35-PATTERNS.md (§formatDataHoraSP.ts + §EntrevistaDashboard EDIT — the verbatim extract + "keep local" list)
    - .planning/phases/35-painel-do-candidato-leitura-do-agendamento/35-RESEARCH.md (Pattern 1 + Assumption A3 — why the extraction breaks no external caller)
  </read_first>
  <behavior>
    - formatDataHoraSP('2026-07-20T17:30:00Z') → '20/07/2026 às 14:30' (UTC-3 São Paulo; assert the SP-pinned offset, not browser-local)
    - saoPauloParts of a UTC instant that lands at São-Paulo-midnight yields hh '00' (never '24') — the engine edge
    - formatDataHoraSP(null) → null ; formatDataHoraSP('not-a-date') → null (Number.isNaN guard)
  </behavior>
  <action>
    Create `src/lib/datetime/formatDataHoraSP.ts` by lifting `DISPLAY_TIME_ZONE`, `saoPauloParts`,
    and `formatDataHora` VERBATIM from EntrevistaDashboard.tsx:44-73, renaming the exported
    `formatDataHora` to `formatDataHoraSP` and additionally exporting `saoPauloParts` (the test seam
    for the midnight edge). Signature stays `formatDataHoraSP(iso: string | null): string | null`.
    Keep the `hour12:false` + `.formatToParts()` + `pick('hour')==='24'?'00':...` logic exactly as-is.
    Then edit EntrevistaDashboard.tsx: delete its local `DISPLAY_TIME_ZONE`/`saoPauloParts`/`formatDataHora`
    (44-73), add `import { formatDataHoraSP, saoPauloParts } from '@/lib/datetime/formatDataHoraSP'`, and
    repoint every local call site (`formatCurto` at 75-81 and any `formatDataHora(...)` usage) to the
    imported names. `formatCurto` (tooltip short form) and `compute24hMarker` (RH label) STAY LOCAL —
    do not move them. This is a behavior-preserving refactor: zero visual/logic change to the RH surface.
    Named exports only (CLAUDE.md); domain strings pt-BR.
  </action>
  <verify>
    <automated>npx vitest run src/lib/datetime</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/datetime` passes (formatDataHoraSP.test.ts green, all 3 behaviors above).
    - `grep -n "from '@/lib/datetime/formatDataHoraSP'" src/features/entrevista/components/EntrevistaDashboard.tsx` returns the import line.
    - EntrevistaDashboard.tsx no longer declares a local `function saoPauloParts` or `const DISPLAY_TIME_ZONE` (grep returns 0 in that file).
    - `npm run test:run` full suite still green (EntrevistaDashboard/EntrevistaWorkspace tests unaffected — no external caller of the moved fns).
    - `npm run lint` (tsc --noEmit) adds 0 new errors in the 3 touched files (≤104 baseline, currently 97).
  </acceptance_criteria>
  <done>Shared `formatDataHoraSP` lives in `src/lib/datetime/`, EntrevistaDashboard re-imports it, and both the new unit test and the existing suite are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Candidate own-row read service (DEFINER RPC, allowlist) + useMeuAgendamento hook + RPC-guard test</name>
  <files>src/features/agendamento/services/agendamentoCandidatoService.ts, src/features/agendamento/hooks/useMeuAgendamento.ts, src/features/agendamento/services/__tests__/agendamentoCandidatoService.test.ts</files>
  <read_first>
    - src/features/agendamento/services/agendamentoService.ts:26-73 (AgendamentoServiceError shape + TipoAgendamento/StatusAgendamento union exports to reuse + the `| null` Row discipline)
    - src/features/agendamento/hooks/useAgendamento.ts:27-49 (key factory + useQuery read half to mirror)
    - src/features/avaliacao/__tests__/redacaoService.rpc.test.ts:15-119 (vi.hoisted rpcCalls/fromTables harness + guard assertions to copy)
    - src/features/avaliacao/services/redacaoService.ts:168-199 (RPC read shape: rows[0] ?? null — but WITHOUT the confined cast; see interfaces note)
    - .planning/phases/35-painel-do-candidato-leitura-do-agendamento/35-RESEARCH.md (Pattern 2 + Pitfall 1 — typed .rpc, no cast, nullable widening)
  </read_first>
  <behavior>
    - getMeuAgendamento('cand-uuid') calls supabase.rpc with fn 'get_meu_agendamento' and args { p_candidatura_id: 'cand-uuid' }, and NEVER calls supabase.from('agendamentos_entrevista')
    - Given the RPC returns [rowA, rowB], getMeuAgendamento returns rowA (rows[0]; the RPC already ORDER BY data_hora DESC); given [], returns null
    - getMeuAgendamento('') throws MeuAgendamentoServiceError code 'INVALID_INPUT'
    - The returned Row carries NONE of: observacoes_rh, entrevistador, agendado_por, updated_by, vaga_id (guard loop asserts not.toHaveProperty)
    - On RPC error, throws MeuAgendamentoServiceError code 'DATABASE_ERROR'
  </behavior>
  <action>
    Create `agendamentoCandidatoService.ts` (camelCaseService convention). Define `MeuAgendamentoServiceError`
    mirroring `AgendamentoServiceError` (codes 'INVALID_INPUT'|'DATABASE_ERROR'|'NOT_FOUND', `details?`).
    Import the union types by name: `import type { TipoAgendamento, StatusAgendamento } from './agendamentoService'`.
    Define `MeuAgendamentoRow` with EXACTLY the 7 allowlist columns — id, candidatura_id, tipo (TipoAgendamento),
    data_hora (string), local_ou_link (`string | null`), status (StatusAgendamento), compareceu (`boolean | null`).
    The `| null` on local_ou_link/compareceu is load-bearing (the generated RETURNS TABLE lies — Pitfall 1);
    do NOT copy the generator's non-null typing. Implement `getMeuAgendamento(candidaturaId): Promise<MeuAgendamentoRow | null>`:
    guard empty id → throw INVALID_INPUT; call `supabase.rpc('get_meu_agendamento', { p_candidatura_id: candidaturaId })`
    TYPED DIRECTLY (the RPC is present in database.types.ts — do NOT replicate redacaoService's `as unknown as (...)`
    cast, which only exists for functions absent from the types); on error throw DATABASE_ERROR with a pt-BR message;
    return `(data ?? [])[0] ?? null`. ANTI-PATTERN — never `supabase.from('agendamentos_entrevista')` and never
    `select('*')` on this path (no candidate base-table policy exists; it would return 0 rows AND re-leak the
    columns the RPC excludes — [[reference_select_star_leaks_pii]]). Create `useMeuAgendamento.ts` mirroring the
    read half of useAgendamento: `meuAgendamentoKeys` = { all: ['meu-agendamento'], byCandidatura: (id) => [...all, id] },
    `useMeuAgendamento(candidaturaId: string | undefined)` = useQuery(queryFn getMeuAgendamento, enabled:!!candidaturaId,
    staleTime/gcTime 5*60*1000, retry:2). Drop all four mutations (candidate is read-only). Create the RPC-guard test
    by copying the redacaoService.rpc.test.ts harness: capture rpcCalls/fromTables via vi.hoisted + vi.mock('@/lib/supabase/client'),
    assert rpcCalls[0].fn === 'get_meu_agendamento' with args { p_candidatura_id }, assert fromTables does not contain
    'agendamentos_entrevista', and loop the RH-internal keys (observacoes_rh, entrevistador, agendado_por, updated_by, vaga_id)
    asserting not.toHaveProperty on the returned row.
  </action>
  <verify>
    <automated>npx vitest run src/features/agendamento</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/features/agendamento` passes; all 5 behaviors above assert green.
    - `grep -n "rpc('get_meu_agendamento'" src/features/agendamento/services/agendamentoCandidatoService.ts` matches (single data path).
    - `grep -c "from('agendamentos_entrevista')\|select('\*')" src/features/agendamento/services/agendamentoCandidatoService.ts` (filtered `| grep -v '^#'`) is 0 — no base-table read, no star.
    - `grep -n "local_ou_link: string | null" src/features/agendamento/services/agendamentoCandidatoService.ts` matches (nullable widening).
    - MeuAgendamentoRow contains no RH-internal key: `grep -c "observacoes_rh\|entrevistador\|agendado_por\|vaga_id" src/features/agendamento/services/agendamentoCandidatoService.ts` — the only matches allowed are inside the test's negative-assertion list (service file itself: 0).
    - `npm run lint` adds 0 new errors in the touched files (RPC call is fully typed → no `as never`/`as unknown` cast needed).
  </acceptance_criteria>
  <done>Candidate agendamento is readable only through the typed DEFINER RPC via `getMeuAgendamento`/`useMeuAgendamento`, projecting exactly the 7 allowlist columns, proven by the RPC-guard test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate browser → Supabase RPC | Untrusted candidate session; only `candidatura_id` crosses. Ownership + column allowlist are enforced server-side inside `get_meu_agendamento`. |
| generated types → domain Row | The generator asserts non-null for `local_ou_link`/`compareceu`; runtime disagrees. The domain Row is the boundary that corrects the lie. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Information Disclosure | `agendamentoCandidatoService.getMeuAgendamento` | mitigate | Read ONLY via `rpc('get_meu_agendamento')` (7-col allowlist); never `.from('agendamentos_entrevista')`, never `select('*')`. RPC-guard test asserts the rpc call, the absent base-table read, and that the Row carries no observacoes_rh/entrevistador/agendado_por/updated_by/vaga_id. |
| T-35-02 | Information Disclosure / Elevation | cross-candidate read (A reads B's agendamento) | accept (server-enforced) | Ownership is enforced INSIDE the DEFINER RPC (`candidatos.user_id = auth.uid()`), already smoke-proven in Phase 33 (9/9 a–i GREEN). The client only passes `candidatura_id`; it performs no base-table read that would rely on RLS alone. No client-side control added or needed. |
| T-35-03 | Tampering | generated non-null `local_ou_link`/`compareceu` → runtime null crash | mitigate | Domain Row widens both to `| null` (Pitfall 1); downstream (Plan 35-02) renders link/local only when present. |
| T-35-SC | Tampering (supply chain) | npm/pip/cargo installs | N/A | Zero new packages this phase (LOCKED: "zero npm novo"). No install task exists; no `[ASSUMED]`/`[SUS]` package to gate. |
</threat_model>

<verification>
- `npx vitest run src/features/agendamento src/lib/datetime` — the two new test files green.
- `npm run test:run` — full suite still green (≥980 baseline; no regression from the EntrevistaDashboard refactor).
- `npm run lint` — 0 NEW tsc errors in the 6 touched files (pre-existing ≤104 baseline, currently 97, is not this phase's regression). If husky pre-commit strict tsc blocks the commit SOLELY on that pre-existing cadastro/vagas baseline, `--no-verify` is the sanctioned GSD-executor allowance (STATE Decisions [Phase 34/34-02 · exec]); each task must still re-prove "0 new errors in touched files".
- `grep` gates in each task's acceptance_criteria confirm the single RPC data path and the absent base-table/star read.
</verification>

<success_criteria>
- `formatDataHoraSP` is a shared util in `src/lib/datetime/`, consumed by EntrevistaDashboard, unit-tested for SP-pinned output + midnight edge + null handling.
- `getMeuAgendamento` reads the candidate own-row exclusively through `get_meu_agendamento`, returns the newest allowlist-projected row (or null), and the RPC-guard test proves no base-table read and no RH-internal column leak.
- `useMeuAgendamento` exposes the read as a TanStack query (5min stale, retry 2, enabled-gated).
- Zero schema/migration change; zero new npm; database.types.ts untouched.
</success_criteria>

<output>
Create `.planning/phases/35-painel-do-candidato-leitura-do-agendamento/35-01-SUMMARY.md` when done.
</output>
