---
phase: 30-meu-perfil-rh
plan: 03
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - supabase/migrations/20260714000001_perfil_rh_rpc_avatars.sql
autonomous: true
requirements: [PERFIL-01, PERFIL-03, SEG-03]
must_haves:
  truths:
    - "A SECURITY DEFINER RPC atualizar_meu_perfil_rh(p_nome, p_avatar_url) exists that updates ONLY nome_completo + avatar_url of the caller's own row (WHERE user_id=auth.uid()) — role/ativo/cargo/email/deleted_at are NOT in the SET list, so SEG-03 holds BY CONSTRUCTION"
    - "A private avatars-rh storage bucket (2MB, png/jpeg/webp) with 4 own-folder RLS policies exists so each RH user reads/writes ONLY their own avatar folder (PERFIL-03)"
  artifacts:
    - path: "supabase/migrations/20260714000001_perfil_rh_rpc_avatars.sql"
      provides: "the profile self-service write path (RPC) + avatar storage + own-folder RLS"
      contains: "atualizar_meu_perfil_rh"
  key_links:
    - from: "atualizar_meu_perfil_rh"
      to: "public.usuarios_rh"
      via: "UPDATE ... WHERE user_id = auth.uid() (DEFINER, uid-scoped, role-excluded)"
      pattern: "WHERE user_id = auth.uid"
    - from: "avatars-rh policies"
      to: "storage.objects"
      via: "(storage.foldername(name))[1] = (select auth.uid()::text)"
      pattern: "storage.foldername"
---

<objective>
Author (do NOT apply) the single Phase-30 migration: the self-service `atualizar_meu_perfil_rh` SECURITY DEFINER RPC + the private `avatars-rh` storage bucket + its 4 own-folder RLS policies. SEG-03 lives here — the RPC physically cannot touch `role`/`ativo`/`cargo`/`email` because they are not in the SET list. This is the INVERSE privilege of the Phase-28 admin RPCs: it `GRANT EXECUTE TO authenticated` (self-service, low privilege), safe precisely because the body is uid-scoped and column-limited.

Purpose: PERFIL-01 (name persists), PERFIL-03 (private avatar storage), SEG-03 (no role escalation by construction).
Output: `supabase/migrations/20260714000001_perfil_rh_rpc_avatars.sql` (authored only — applied to PROD in the [BLOCKING] 30-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-meu-perfil-rh/30-RESEARCH.md
@.planning/phases/30-meu-perfil-rh/30-LIVE-STATE.md

<read_first>
- `supabase/migrations/20260713000003_usr_rh_mutacao_rpc.sql` — the DEFINER RPC + `log_auditoria` PERFORM shape to mirror (this one is REVOKE/admin; the profile RPC is GRANT/self-service — the opposite privilege).
- `supabase/migrations/20260425000002_curriculos_bucket.sql` — the private-bucket + own-folder RLS analog to mirror; RETARGET to `avatars-rh` and DROP the BEGIN/COMMIT wrapper + DROP the "rh/administrador reads any" SELECT clause (own avatar only).
- `.planning/phases/30-meu-perfil-rh/30-RESEARCH.md` §Q1 + §Q3 — bucket config (public=false, 2097152, ARRAY['image/png','image/jpeg','image/webp']); the exact RPC body + COALESCE + best-effort log_auditoria.
- `.planning/phases/30-meu-perfil-rh/30-LIVE-STATE.md` — A4 log_auditoria signature; A1 confirms no client UPDATE policy to re-add.
- `docs/sql/sql/25-functions-configuracoes.sql:63-77` — log_auditoria param names.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the atualizar_meu_perfil_rh RPC (SEG-03 by construction)</name>
  <files>supabase/migrations/20260714000001_perfil_rh_rpc_avatars.sql</files>
  <read_first>supabase/migrations/20260713000003_usr_rh_mutacao_rpc.sql; docs/sql/sql/25-functions-configuracoes.sql:63-77; 30-LIVE-STATE.md A4; 30-RESEARCH.md §Q3</read_first>
  <action>Create the migration file with a header comment (Phase 30 / Plan 30-03, authored-only, applied in 30-06 via MCP apply_migration, NO BEGIN/COMMIT wrapper per CLAUDE.md §Migrations — the MCP path wraps it). Define `CREATE OR REPLACE FUNCTION public.atualizar_meu_perfil_rh(p_nome text, p_avatar_url text DEFAULT NULL) RETURNS void LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`. Body: declare `v_row_id uuid`; `UPDATE public.usuarios_rh SET nome_completo = p_nome, avatar_url = COALESCE(p_avatar_url, avatar_url), updated_by = auth.uid(), updated_at = now() WHERE user_id = auth.uid() RETURNING id INTO v_row_id;` — role/ativo/cargo/email/deleted_at deliberately EXCLUDED from the SET (SEG-03 by construction; add an inline comment naming this the SEG-03 boundary). If `v_row_id IS NULL` RAISE EXCEPTION 'NOT_FOUND: caller has no usuarios_rh row' USING ERRCODE='P0002'. Then a BEST-EFFORT audit wrapped so it never aborts the update: `BEGIN PERFORM public.log_auditoria(p_usuario_id := auth.uid(), p_usuario_tipo := 'rh', p_acao := 'editar_perfil', p_categoria := 'usuario', p_descricao := 'Usuario RH editou o proprio perfil', p_severidade := 'info', p_recurso_tipo := 'usuarios_rh', p_recurso_id := v_row_id, p_sucesso := true); EXCEPTION WHEN OTHERS THEN NULL; END;`. Add a COMMENT ON FUNCTION documenting SEG-03-by-construction + GRANT-authenticated rationale. Then `GRANT EXECUTE ON FUNCTION public.atualizar_meu_perfil_rh(text, text) TO authenticated;` (self-service — the OPPOSITE of Phase-28's REVOKE). Do NOT add any RLS UPDATE policy on usuarios_rh (Phase-28 dropped it; the DEFINER RPC is the only write path). Confirm `%%`-free (no format() with literal percent unless doubled — Phase-7 bug precedent).</action>
  <verify>
    <automated>grep -q "WHERE user_id = auth.uid" supabase/migrations/20260714000001_perfil_rh_rpc_avatars.sql && grep -q "GRANT EXECUTE ON FUNCTION public.atualizar_meu_perfil_rh(text, text) TO authenticated" supabase/migrations/20260714000001_perfil_rh_rpc_avatars.sql && ! grep -Ei "SET[[:space:]]+(role|ativo|cargo|email|deleted_at)[[:space:]]*=" supabase/migrations/20260714000001_perfil_rh_rpc_avatars.sql</automated>
  </verify>
  <acceptance_criteria>
    - The RPC SET list contains ONLY `nome_completo`, `avatar_url`, `updated_by`, `updated_at` — the grep proves role/ativo/cargo/email/deleted_at are NOT set.
    - `WHERE user_id = auth.uid()` scopes the write to the caller's own row; NOT_FOUND (P0002) if none.
    - `GRANT EXECUTE ... TO authenticated` present; NO REVOKE, NO client UPDATE RLS policy.
    - COALESCE preserves the existing avatar when `p_avatar_url` is NULL; audit is best-effort (EXCEPTION swallowed).
  </acceptance_criteria>
  <done>The self-service write path exists as a DEFINER RPC that cannot escalate a role by construction.</done>
</task>

<task type="auto">
  <name>Task 2: Author the avatars-rh private bucket + 4 own-folder RLS policies</name>
  <files>supabase/migrations/20260714000001_perfil_rh_rpc_avatars.sql</files>
  <read_first>supabase/migrations/20260425000002_curriculos_bucket.sql; 30-RESEARCH.md §Q1</read_first>
  <action>Append to the same migration file. Idempotent bucket: `INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types) VALUES ('avatars-rh','avatars-rh', false, 2097152, ARRAY['image/png','image/jpeg','image/webp']) ON CONFLICT (id) DO UPDATE SET public=EXCLUDED.public, file_size_limit=EXCLUDED.file_size_limit, allowed_mime_types=EXCLUDED.allowed_mime_types;`. Then DROP POLICY IF EXISTS for 4 names (`avatars_rh_select_own`, `avatars_rh_insert_own`, `avatars_rh_update_own`, `avatars_rh_delete_own`) on storage.objects, then CREATE all 4 FOR SELECT/INSERT/UPDATE/DELETE TO authenticated, each gated on `bucket_id = 'avatars-rh' AND (storage.foldername(name))[1] = (select auth.uid()::text)` (subquery-wrapped auth.uid for the RLS perf cache — Pitfall 8). CRITICAL difference from curriculos: the SELECT policy has NO "rh/administrador reads any" clause — an RH user's avatar is their OWN only (SEG-03 spirit, no cross-user read). INSERT/UPDATE use WITH CHECK on the same predicate. Keep the whole file WITHOUT a BEGIN/COMMIT wrapper (the MCP apply path wraps it; the curriculos precedent's wrapper trips 42601 via the CLI — do NOT copy it).</action>
  <verify>
    <automated>M=supabase/migrations/20260714000001_perfil_rh_rpc_avatars.sql; grep -q "avatars-rh" "$M" && [ "$(grep -c 'storage.foldername(name)' "$M")" -ge 4 ] && ! grep -qi "app_metadata,role" "$M" && ! grep -qE "^BEGIN;|^COMMIT;" "$M"</automated>
  </verify>
  <acceptance_criteria>
    - Bucket `avatars-rh` is private (public=false), 2097152 bytes, ARRAY['image/png','image/jpeg','image/webp'], created idempotently.
    - 4 own-folder policies exist; the own-folder predicate appears ≥4 times; NO `app_metadata,role` cross-user read clause.
    - No `BEGIN;`/`COMMIT;` wrapper in the file.
  </acceptance_criteria>
  <done>Private avatar storage with strict own-folder RLS is authored; only the owner reads/writes their `{uid}/avatar.<ext>` path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated caller → usuarios_rh (via RPC) | untrusted self-service write; the DEFINER RPC constrains columns + rows |
| authenticated caller → storage.objects (avatars-rh) | untrusted upload; own-folder RLS constrains the path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Elevation of Privilege | atualizar_meu_perfil_rh | mitigate | role/ativo/cargo/email NOT in the SET list → self-service path cannot escalate a role (by construction); GRANT-authenticated is safe because uid-scoped + column-limited. |
| T-30-02 | IDOR / Tampering | atualizar_meu_perfil_rh | mitigate | `WHERE user_id = auth.uid()` under DEFINER writes ONLY the caller's own row (auth.uid() is GUC-based — Phase-28 precedent). |
| T-30-04 | Tampering | storage.objects avatars-rh | mitigate | 4 own-folder RLS policies `(storage.foldername(name))[1] = (select auth.uid()::text)` — no cross-user avatar read/write; private bucket. |
| T-30-SC | Tampering | package installs | accept | No new package installs this phase. |
</threat_model>

<verification>
- RPC SET list excludes role/ativo/cargo/email/deleted_at (grep).
- GRANT EXECUTE TO authenticated present; no client UPDATE RLS policy re-added.
- avatars-rh bucket private + own-folder policies (≥4 own-folder predicates, no cross-user read).
- No BEGIN/COMMIT wrapper.
</verification>

<success_criteria>
The single migration authoring the self-service write path (SEG-03 by construction) + private avatar storage exists, ready for the [BLOCKING] MCP apply in 30-06.
</success_criteria>

<output>
Create `.planning/phases/30-meu-perfil-rh/30-03-SUMMARY.md` when done.
</output>
