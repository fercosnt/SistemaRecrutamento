---
phase: 30-meu-perfil-rh
plan: 05
type: execute
wave: 3
depends_on: ["30-04"]
files_modified:
  - src/features/perfil-rh/components/AvatarUpload.tsx
  - src/features/perfil-rh/components/PerfilSection.tsx
  - src/features/perfil-rh/components/SenhaSection.tsx
  - src/features/perfil-rh/components/__tests__/PerfilSection.test.tsx
  - src/features/perfil-rh/components/__tests__/SenhaSection.test.tsx
  - src/components/pages/MeuPerfilPage.tsx
autonomous: true
requirements: [PERFIL-01, PERFIL-02, PERFIL-03, SEG-03]
must_haves:
  truths:
    - "MeuPerfilPage renders the real profile (own-row loaded, AsyncState-gated) replacing the 'em breve' stub, keeping the single RHLayout owner + route + RoleGuard (PERFIL-01)"
    - "The Perfil section edits nome_completo + uploads an avatar and shows email/cargo/role as READ-ONLY context with NO input/select/button to change role — SEG-03 verified by a component test asserting the ABSENCE of any role/cargo/email control"
    - "The Senha section changes the password via re-auth; wrong current password sets a field error on 'Senha atual' and the session stays valid (PERFIL-02)"
  artifacts:
    - path: "src/components/pages/MeuPerfilPage.tsx"
      provides: "the real /rh/perfil profile page (stub replaced)"
      contains: "PerfilSection"
    - path: "src/features/perfil-rh/components/PerfilSection.tsx"
      provides: "name edit + avatar + read-only SEG-03 context"
      min_lines: 40
    - path: "src/features/perfil-rh/components/SenhaSection.tsx"
      provides: "password change with re-auth"
      min_lines: 40
  key_links:
    - from: "MeuPerfilPage"
      to: "usePerfilRh"
      via: "own-row query gated by AsyncState, feeds PerfilSection/SenhaSection"
      pattern: "usePerfilRh"
    - from: "PerfilSection"
      to: "SEG-03 read-only context"
      via: "email/cargo/role rendered as read-only rows, no editable control"
      pattern: "readonly|read-only|aria-readonly|as context"
---

<objective>
Replace the `MeuPerfilPage` stub with the real self-service profile: a Perfil section (name edit + avatar upload + read-only SEG-03 context) and a Senha section (password change with re-auth), composed under the existing RHLayout/route/RoleGuard. SEG-03's UX truth is load-bearing: role/cargo/email are shown READ-ONLY as context — there is NO affordance (no input, select, or button) to change one's own role, verified by a component test asserting the absence of such a control.

Purpose: PERFIL-01/02/03 reachable end-to-end through the UI; SEG-03 has no UI escalation path.
Output: PerfilSection + SenhaSection + AvatarUpload components + component tests + wired MeuPerfilPage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-meu-perfil-rh/30-UI-SPEC.md

<interfaces>
<!-- From 30-04 (data layer) — use directly. -->
usePerfilRh() → { data: PerfilRhRow, isLoading, isError, refetch }
useAtualizarPerfil() → mutation(atualizarPerfil(nome, avatarPath))
useAlterarSenha() → mutation(alterarSenha(email, current, nova)); WRONG_CURRENT surfaces as a typed error
useUploadAvatar() → mutation(uploadAvatar(file, uid)) → returns storage path
getAvatarSignedUrl(path) → signed URL for <img src>
Schemas: perfilNomeSchema, alterarSenhaSchema (perfilRhSchemas.ts)
PerfilRhRow: { id, user_id, nome_completo, email, cargo, role, avatar_url }
</interfaces>

<read_first>
- `src/components/pages/MeuPerfilPage.tsx` — the stub to replace; PRESERVE the named export `MeuPerfilPage`, the RHLayout wrapper (single owner), and the header. The route (`routes.tsx:400`) + RoleGuard are UNCHANGED — do NOT edit routes.tsx.
- `src/features/admin/components/NovoUsuarioDialog.tsx` — the bare-RHF (`register`/`Controller`) + `zodResolver` idiom (NOT the shadcn FormField primitive — grep-forbidden), AA input styling (`border-white/30 bg-white/20 text-white placeholder:text-white/60`), inline `role="alert"` errors, `Loader2` pending, turquoise CTA `bg-[#35BFAD] text-[#04121F]`, `min-h-[44px]`.
- `src/features/admin/components/GestaoUsuariosPage.tsx` — the body-only single-RHLayout page frame (`space-y-6`, h1 `text-3xl md:text-4xl`), AsyncState usage (`isLoading`/`isError`/`onRetry`/`copy` override).
- `src/features/vagas/services/cvUploadService.ts` — avatar validation error codes to map to inline field/toast.
- `.planning/phases/30-meu-perfil-rh/30-UI-SPEC.md` — the FULL design contract (tokens, sections, read-only context, states, copy honesty, a11y; Phase-29 AA-safe values `text-white/70` muted, `/60` placeholders, NO `text-white/50`; honest GoTrue copy — no logout/invented email).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AvatarUpload + PerfilSection (name + avatar + read-only SEG-03 context)</name>
  <files>src/features/perfil-rh/components/AvatarUpload.tsx, src/features/perfil-rh/components/PerfilSection.tsx, src/features/perfil-rh/components/__tests__/PerfilSection.test.tsx</files>
  <read_first>src/features/admin/components/NovoUsuarioDialog.tsx; src/features/vagas/services/cvUploadService.ts; 30-UI-SPEC.md §Section 1</read_first>
  <action>Create `AvatarUpload.tsx`: circular preview (signed-URL render via getAvatarSignedUrl; fallback = initials disc), a real focusable `<button>` "Alterar foto" triggering a visually-hidden-but-labeled `<input type=file accept="image/png,image/jpeg,image/webp">`, uploading spinner + `aria-live="polite"` "Enviando…", honest helper "PNG, JPG ou WebP, até 2 MB.", `<img alt="Foto de perfil de {nome}">`. On file pick → validateAvatar (inline field/toast on FILE_TOO_LARGE/INVALID_MIME) → useUploadAvatar → on success pass the path to the parent (auto-save via useAtualizarPerfil OR bubble the path). Create `PerfilSection.tsx` (bare RHF + zodResolver(perfilNomeSchema)): the "Nome de exibição" text input (AA styling, inline role=alert), the AvatarUpload block, and a READ-ONLY context definition-list rendering Email / Cargo / Papel as labelled rows (`text-xs text-white/60` label, `text-sm text-white/70` value) with the note "Papel e cargo são geridos por um administrador." and a NON-interactive Papel Badge (white on `#35BFAD/20`) — NO input/select/button for role/cargo/email. Single turquoise "Salvar" CTA (`min-h-[44px]`, Loader2 "Salvando…") calling useAtualizarPerfil with {nome, avatarPath}; success toast handled by the hook. Author `PerfilSection.test.tsx` (RTL): (a) editing + submitting the name calls atualizarPerfil; (b) **SEG-03 assertion** — `queryByRole('textbox', {name:/papel|cargo|e-?mail/i})` and `queryByRole('combobox')` and `queryByRole('button', {name:/papel|cargo/i})` all return null (no affordance to change role/cargo/email); (c) avatar validation error renders inline.</action>
  <verify>
    <automated>npm run test:run -- src/features/perfil-rh/components/__tests__/PerfilSection.test.tsx && npm run lint 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - PerfilSection.test.tsx GREEN including the SEG-03 no-affordance assertion (no role/cargo/email editable control exists).
    - Email/Cargo/Papel are read-only rows; the honest "geridos por um administrador" note is present.
    - Avatar upload uses a real `<button>` + labeled hidden file input; `alt` present; AA-safe values; no `text-white/50`.
  </acceptance_criteria>
  <done>The Perfil section edits name + avatar and exposes NO role-escalation affordance (SEG-03 UX).</done>
</task>

<task type="auto">
  <name>Task 2: SenhaSection (password re-auth) + wire MeuPerfilPage</name>
  <files>src/features/perfil-rh/components/SenhaSection.tsx, src/features/perfil-rh/components/__tests__/SenhaSection.test.tsx, src/components/pages/MeuPerfilPage.tsx</files>
  <read_first>src/features/admin/components/NovoUsuarioDialog.tsx; src/features/admin/components/GestaoUsuariosPage.tsx; src/components/pages/MeuPerfilPage.tsx; 30-UI-SPEC.md §Section 2 + §Layout</read_first>
  <action>Create `SenhaSection.tsx` (bare RHF + zodResolver(alterarSenhaSchema)): three `type="password"` fields (Senha atual / Nova senha / Confirmar nova senha), AA styling, inline role=alert, turquoise "Alterar senha" CTA (`min-h-[44px]`, Loader2 "Alterando…"). Reads the caller's own email from usePerfilRh data (never a form field). On submit → useAlterarSenha(email, atual, nova). WRONG_CURRENT → `setError('senha_atual', { message: 'Senha atual incorreta.' })`, form stays open, other fields preserved. Success → hook toasts 'Senha alterada com sucesso.'; clear the fields; session stays valid (NO logout, NO "email de confirmação" copy). Never render/log the password values (Pitfall-7). Rewrite `MeuPerfilPage.tsx` KEEPING the named export + RHLayout (single owner) + header (h1 "Meu Perfil" `text-3xl md:text-4xl` + subtitle): body is `space-y-6 max-w-3xl`, gate the own-row load with `<AsyncState>` (usePerfilRh isLoading/isError/onRetry + copy override for the profile error/retry strings), then render two stacked GlassCard sections `<PerfilSection>` + `<SenhaSection>`. Do NOT nest RHLayout; do NOT edit routes.tsx. Author `SenhaSection.test.tsx` (RTL): (a) valid change calls alterarSenha(email, atual, nova); (b) WRONG_CURRENT sets the 'Senha atual' field error and keeps the form open; (c) no signOut is called (session preserved); (d) Zod blocks nova<8 / mismatch / nova===atual.</action>
  <verify>
    <automated>npm run test:run -- src/features/perfil-rh/components/__tests__/SenhaSection.test.tsx && npm run test:run -- src/features/perfil-rh && npm run lint 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - SenhaSection.test.tsx GREEN: re-auth dispatch, WRONG_CURRENT→field, no logout, Zod rules enforced.
    - MeuPerfilPage keeps the named export, single RHLayout owner, AsyncState-gated body; the old "Edição de perfil em breve" string is ABSENT.
    - routes.tsx UNCHANGED (no diff); `npm run lint` tsc ≤104.
    - Full `npm run test:run -- src/features/perfil-rh` GREEN.
  </acceptance_criteria>
  <done>The profile page is real and reachable at /rh/perfil; password change re-auths without dropping the session.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → profile UI | the UI must expose no control that writes role/cargo/email (SEG-03) |
| UI → GoTrue (password fields) | password values never rendered/logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Elevation of Privilege | PerfilSection | mitigate | Role/cargo/email are read-only context — NO input/select/button; component test asserts the absence of any such control (no UI escalation path). |
| T-30-03 | Broken Authentication | SenhaSection | mitigate | Re-auth-before-rotate via useAlterarSenha; wrong current → field error, no rotate, session preserved. |
| T-30-05 | Information Disclosure | SenhaSection / AvatarUpload | mitigate | Password values never rendered/logged; signed URLs used only as `<img src>`, never logged (Pitfall-7 guard covers the sources). |
| T-30-SC | Tampering | package installs | accept | No new package installs this phase. |
</threat_model>

<verification>
- `npm run test:run -- src/features/perfil-rh` GREEN (incl. SEG-03 no-affordance + WRONG_CURRENT + no-logout).
- MeuPerfilPage: named export kept, single RHLayout, AsyncState-gated, old stub string absent.
- routes.tsx unchanged; `npm run lint` tsc ≤104.
</verification>

<success_criteria>
The real /rh/perfil page is live in code: name + avatar edit, password re-auth, read-only SEG-03 context with no role affordance — pending the [BLOCKING] PROD apply (30-06) and the SEG-03 smoke (30-07).
</success_criteria>

<output>
Create `.planning/phases/30-meu-perfil-rh/30-05-SUMMARY.md` when done.
</output>
