---
phase: 30-meu-perfil-rh
plan: 06
type: execute
wave: 3
depends_on: ["30-03"]
files_modified:
  - database.types.ts
  - src/features/perfil-rh/services/perfilRhService.ts
autonomous: false
requirements: [PERFIL-01, PERFIL-03, SEG-03]
must_haves:
  truths:
    - "The Phase-30 migration (atualizar_meu_perfil_rh RPC + avatars-rh bucket + own-folder policies) is LIVE in PROD via Supabase MCP apply_migration, with a reconciled version row"
    - "database.types.ts at the repo ROOT is regenerated to include the new RPC, and the `as never` cast in perfilRhService is dropped so the RPC call is fully typed — tsc stays ≤104"
  artifacts:
    - path: "database.types.ts"
      provides: "regenerated types including atualizar_meu_perfil_rh"
      contains: "atualizar_meu_perfil_rh"
  key_links:
    - from: "supabase/migrations/20260714000001_perfil_rh_rpc_avatars.sql"
      to: "PROD (MCP apply_migration)"
      via: "operator applies the migration; version row reconciled"
      pattern: "apply_migration"
---

<objective>
Apply the Phase-30 migration to PROD via Supabase MCP `apply_migration` (NOT `supabase db push` — the 42601 workaround and version-row convention are established for this project), regenerate `database.types.ts` at the repo ROOT, and drop the documented `as never` cast in `perfilRhService` now that the RPC is typed. This is [BLOCKING] — the operator drives the MCP apply. Scheduled AFTER authoring (30-03) and BEFORE the SEG-03 smoke runs GREEN (30-07).

Purpose: The self-service write path + avatar storage become LIVE; the client types match reality.
Output: migration LIVE in PROD, `database.types.ts` regenerated, cast dropped, tsc ≤104.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-meu-perfil-rh/30-LIVE-STATE.md

<read_first>
- `supabase/migrations/20260714000001_perfil_rh_rpc_avatars.sql` — the migration to apply (authored in 30-03).
- `CLAUDE.md` §Migrations — MCP apply_migration bypasses 42601; `database.types.ts` lives at the REPO ROOT (not src/types/); regen after the RPC migration.
- `.planning/phases/30-meu-perfil-rh/30-LIVE-STATE.md` — A5 (no existing RPC → fresh CREATE) + A6 (bucket state).
- `src/features/perfil-rh/services/perfilRhService.ts` — the `as never` cast + its `TODO(30-06)` marker to remove.
</read_first>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Apply the migration to PROD via Supabase MCP apply_migration + reconcile</name>
  <files>supabase/migrations/20260714000001_perfil_rh_rpc_avatars.sql</files>
  <read_first>supabase/migrations/20260714000001_perfil_rh_rpc_avatars.sql; CLAUDE.md §Migrations; 30-LIVE-STATE.md A5/A6</read_first>
  <action>The operator applies `20260714000001_perfil_rh_rpc_avatars.sql` to PROD via `mcp__supabase__apply_migration` (name `perfil_rh_rpc_avatars`, the file's SQL body — NO BEGIN/COMMIT wrapper; MCP wraps it; this bypasses the 42601 that `db push` hits on PL/pgSQL `$$` bodies and writes its own version row). Then verify live (read-only execute_sql):
    1. RPC live + GRANTed: `SELECT p.proname, pg_get_function_identity_arguments(p.oid) AS args, has_function_privilege('authenticated', p.oid, 'EXECUTE') AS auth_can_exec FROM pg_proc p WHERE p.proname='atualizar_meu_perfil_rh' AND p.pronamespace='public'::regnamespace;` — 1 row, args `p_nome text, p_avatar_url text`, auth_can_exec=true.
    2. Bucket: `SELECT id, public, file_size_limit, allowed_mime_types FROM storage.buckets WHERE id='avatars-rh';` — public=false, 2097152, {image/png,image/jpeg,image/webp}.
    3. Policies: `SELECT policyname, cmd FROM pg_policies WHERE schemaname='storage' AND tablename='objects' AND policyname LIKE 'avatars_rh_%' ORDER BY cmd;` — 4 rows (SELECT/INSERT/UPDATE/DELETE).
    4. SEG-03 preserved: `SELECT count(*) FROM pg_policies WHERE schemaname='public' AND tablename='usuarios_rh' AND cmd='UPDATE';` — 0.
    5. Reconcile the ledger (record the version row; note any drift for the DBMIG backlog, consistent with prior phases).</action>
  <verify>
    <automated>test -f supabase/migrations/20260714000001_perfil_rh_rpc_avatars.sql</automated>
  </verify>
  <done>The RPC + avatars-rh bucket + 4 own-folder policies are live on PROD; usuarios_rh still has 0 client UPDATE policies (SEG-03 preserved).</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate database.types.ts + drop the as-never cast</name>
  <files>database.types.ts, src/features/perfil-rh/services/perfilRhService.ts</files>
  <read_first>src/features/perfil-rh/services/perfilRhService.ts (the cast + TODO(30-06) marker); CLAUDE.md §db:types</read_first>
  <action>Regenerate `database.types.ts` at the repo ROOT (`npm run db:types`, or via MCP `generate_typescript_types` if the CLI is unavailable) so it includes `atualizar_meu_perfil_rh` under Functions. Then remove the `as never` cast on the `supabase.rpc('atualizar_meu_perfil_rh', {...})` call in `perfilRhService.ts` (and its `TODO(30-06)` comment) — the RPC is now typed, so the args typecheck natively. Run `npm run lint` and confirm tsc ≤104 (the regen + cast-drop must NOT raise the baseline); resolve any new errors to hold ≤104.</action>
  <verify>
    <automated>grep -q "atualizar_meu_perfil_rh" database.types.ts && ! grep -q "as never" src/features/perfil-rh/services/perfilRhService.ts && npm run lint 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `database.types.ts` contains `atualizar_meu_perfil_rh`.
    - No `as never` cast remains in perfilRhService.ts.
    - `npm run lint` tsc ≤104.
  </acceptance_criteria>
  <done>Types match the live RPC and the profile service is fully typed with no cast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → PROD (MCP apply_migration) | privileged schema change; verified post-apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Elevation of Privilege | usuarios_rh policies | mitigate | Post-apply assertion: 0 client UPDATE policies on usuarios_rh — the apply did not open a self-promotion path. |
| T-30-04 | Tampering | avatars-rh policies | mitigate | Post-apply assertion: 4 own-folder policies exist; bucket private. |
| T-30-SC | Tampering | package installs | accept | No new package installs this phase. |
</threat_model>

<verification>
- RPC live + EXECUTE granted to authenticated; avatars-rh bucket + 4 policies live; 0 client UPDATE policies on usuarios_rh.
- `database.types.ts` includes the RPC; no `as never` cast; tsc ≤104.
</verification>

<success_criteria>
The migration is LIVE in PROD, types are regenerated, and the client RPC call is fully typed — the SEG-03 smoke (30-07) can now run GREEN.
</success_criteria>

<output>
Create `.planning/phases/30-meu-perfil-rh/30-06-SUMMARY.md` when done.
</output>
