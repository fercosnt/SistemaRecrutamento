---
phase: 02-cadastro-candidato
plan: 06
type: execute
wave: 3
depends_on: [03, 04, 05]
files_modified:
  - src/features/cadastro/components/CadastroMultiStepForm.tsx
  - src/features/cadastro/components/steps/AutorizacoesStep.tsx
  - src/features/cadastro/components/steps/DadosPessoaisStep.tsx
  - src/features/cadastro/components/steps/DisponibilidadeStep.tsx
  - src/features/cadastro/components/steps/EnderecoStep.tsx
  - src/features/cadastro/components/steps/DadosProfissionaisStep.tsx
  - e2e/cadastro-flow.spec.ts
autonomous: false
requirements: [CAD-01, CAD-02, CAD-04, CAD-05, CAD-06]

must_haves:
  truths:
    - "On Step 4, clicking 'Criar conta' with valid data triggers (a) invoke('cadastrar-candidato'), (b) supabase.auth.signInWithPassword on success, (c) navigate('/candidato/perfil', { replace: true }) with Sonner toast containing `Bem-vindo(a), <first-name>`"
    - "When Edge Function returns error_code=EMAIL_EXISTS, the form auto-navigates to Step 1 and sets an inline error on `dadosPessoais.email`"
    - "When Edge Function returns error_code=CPF_EXISTS, the form auto-navigates to Step 1 and sets an inline error on `dadosPessoais.cpf`"
    - "Submitting with `autorizacao_uso_dados: false` blocks submit and shows an inline error + Sonner toast 'Para criar sua conta, você precisa autorizar o uso dos dados.'"
    - "After filling Step 1 and refreshing the page, the Step 1 fields (except senha/confirmar_senha) are pre-populated from sessionStorage; a Sonner toast 'Retomamos seu cadastro de onde você parou.' is shown"
    - "Inside `src/features/cadastro/components/`, grep `font-medium` and `font-bold` returns 0 matches (UI-SPEC Dimension 4 compliance)"
    - "The Step 4 primary CTA label reads `Criar conta` (not `Finalizar Cadastro`); during submit reads `Criando...` with `Loader2`"
    - "Running `npx playwright test cadastro-flow --project=chromium` exits 0"
  artifacts:
    - path: "src/features/cadastro/components/CadastroMultiStepForm.tsx"
      provides: "Wired form with draft persistence, leave guard, auto-login, structured error routing, updated CTA label"
      contains: "useCadastroDraft"
      min_lines: 500
    - path: "src/features/cadastro/components/steps/AutorizacoesStep.tsx"
      provides: "LGPD step with POLICY_VERSION captions and D-15 microcopy"
      contains: "POLICY_VERSION"
    - path: "e2e/cadastro-flow.spec.ts"
      provides: "6 new test cases covering happy path (auto-login), EMAIL_EXISTS, CPF_EXISTS, draft restore, LGPD-mandatory block, rate_limited"
      contains: "Criar conta"
  key_links:
    - from: "CadastroMultiStepForm onSubmit"
      to: "cadastrarCandidato + tryAutoLogin"
      via: "import from services/cadastroService"
      pattern: "cadastrarCandidato|tryAutoLogin"
    - from: "CadastroMultiStepForm"
      to: "useCadastroDraft + useLeaveGuard"
      via: "import from hooks/"
      pattern: "useCadastroDraft|useLeaveGuard"
    - from: "CadastroMultiStepForm error handler"
      to: "FIELD_TO_STEP_INDEX + FIELD_TO_STEP_PATH"
      via: "import from services/cadastroService"
      pattern: "FIELD_TO_STEP_INDEX|FIELD_TO_STEP_PATH"
    - from: "AutorizacoesStep caption"
      to: "POLICY_VERSION constant"
      via: "import from features/cadastro/constants"
      pattern: "POLICY_VERSION"
---

<objective>
Final integration wave: wire the three new hooks (Plan 02-04) + evolved services (Plan 02-05) into `CadastroMultiStepForm.tsx`. Replace the current `onSubmit` handler with the Phase 2 spec: structured error routing via `error_code` → step index + inline error; `tryAutoLogin` with 500ms-backoff retry; draft save/load/clear; `beforeunload` leave guard with submit-time removal; CTA rename "Finalizar Cadastro" → "Criar conta" / "Criando..." + Loader2; LoadingProgress dialog NOT opened during cadastro submit.

Update `AutorizacoesStep.tsx` per UI-SPEC § LGPD Checkboxes: stacked cards with the mandatory one emphasized (blue tint + Shield icon + "Obrigatório" badge + red asterisk), policy_version caption.

Sweep `src/features/cadastro/components/` — replace all `font-medium` / `font-bold` occurrences with `font-semibold` (or `font-normal` where body text) per UI-SPEC Dimension 4 (only weights 400 and 600 allowed).

Extend `e2e/cadastro-flow.spec.ts` with the 6 Wave 0 cases from 02-VALIDATION.md.

Purpose: closes D-01, D-03, D-04, D-06, D-07, D-13, D-14, D-15, D-16 at the UI layer; delivers CAD-01, CAD-05, CAD-06 end-to-end.

Output: Working cadastro form from Step 1 empty to `/candidato/perfil` logged-in, validated by E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-cadastro-candidato/02-CONTEXT.md
@.planning/phases/02-cadastro-candidato/02-RESEARCH.md
@.planning/phases/02-cadastro-candidato/02-PATTERNS.md
@.planning/phases/02-cadastro-candidato/02-UI-SPEC.md
@.planning/phases/02-cadastro-candidato/02-VALIDATION.md
@.planning/phases/02-cadastro-candidato/02-03-SUMMARY.md
@.planning/phases/02-cadastro-candidato/02-04-SUMMARY.md
@.planning/phases/02-cadastro-candidato/02-05-SUMMARY.md
@src/features/cadastro/components/CadastroMultiStepForm.tsx
@src/features/cadastro/components/steps/AutorizacoesStep.tsx
@e2e/cadastro-flow.spec.ts

<interfaces>
Hooks ready for consumption (from Plan 02-04):
  import { useCadastroDraft } from '@/features/cadastro/hooks/useCadastroDraft'
    // → { save(data), load(), clear() }
  import { useLeaveGuard } from '@/features/cadastro/hooks/useLeaveGuard'
    // → void (useLeaveGuard(isDirty: boolean))

Services ready for consumption (from Plan 02-05):
  import {
    cadastrarCandidato,
    tryAutoLogin,
    CadastroError,
    FIELD_TO_STEP_INDEX,
    FIELD_TO_STEP_PATH,
  } from '@/features/cadastro/services/cadastroService'
    // cadastrarCandidato(data) → Promise<CadastroCompleteResult>, throws CadastroError
    // tryAutoLogin(email, password) → Promise<boolean>

Constants:
  import { POLICY_VERSION } from '@/features/cadastro/constants'
    // → 'v1.0-2026-04'

React Router + Sonner (already installed):
  import { useNavigate } from 'react-router-dom'
  import { toast } from 'sonner'
  import { Loader2 } from 'lucide-react'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire CadastroMultiStepForm onSubmit handler + add draft/leave-guard/auto-login</name>
  <files>src/features/cadastro/components/CadastroMultiStepForm.tsx</files>
  <read_first>
    - src/features/cadastro/components/CadastroMultiStepForm.tsx (ALL 537 lines — imports block L17-43; existing onSubmit / handleFormSubmit handler; the literal `"Finalizar Cadastro"` string to rename; every `LoadingProgress` open-site to disable)
    - src/features/cadastro/services/cadastroService.ts (post-Plan 02-05 — confirm exports tryAutoLogin, FIELD_TO_STEP_INDEX, FIELD_TO_STEP_PATH, CadastroError with new codes)
    - src/features/cadastro/hooks/useCadastroDraft.ts (post-Plan 02-04)
    - src/features/cadastro/hooks/useLeaveGuard.ts (post-Plan 02-04)
    - .planning/phases/02-cadastro-candidato/02-CONTEXT.md D-01 through D-08, D-13, D-14
    - .planning/phases/02-cadastro-candidato/02-RESEARCH.md Operation 2 (L1106-1190 — target handleFormSubmit + routeCadastroError sketch)
    - .planning/phases/02-cadastro-candidato/02-UI-SPEC.md § Submit Flow (Loading / Success / Error states) + § LoadingProgress Dialog deprecation + § Microcopy Catalog (button labels) + § Error Code → UI Mapping
    - .planning/phases/02-cadastro-candidato/02-PATTERNS.md § `CadastroMultiStepForm.tsx` PATCH
  </read_first>
  <behavior>
    - Form mounts → `draft.load()`; if non-null, `methods.reset(loaded)` + Sonner `toast.info('Retomamos seu cadastro de onde você parou.', { duration: 4000, action: { label: 'Começar do zero', onClick: () => { draft.clear(); methods.reset() } } })`.
    - `methods.watch()` + `useEffect` (500ms debounce) → `draft.save(watchedData)` on every change.
    - `useLeaveGuard(methods.formState.isDirty && !isSubmitting && !submitSuccess)`.
    - `handleFormSubmit` (Step 4 submit):
        1. If `data.autorizacoes.autorizacao_uso_dados === false` → `methods.setError('autorizacoes.autorizacao_uso_dados', { type: 'required', message: 'Para criar sua conta, você precisa autorizar o uso dos dados.' })` + `toast.error('Para criar sua conta, você precisa autorizar o uso dos dados.')` + `return` (do not proceed).
        2. `setIsSubmitting(true)`; schedule `setTimeout(() => toast.loading('Criando sua conta...', { id: 'cadastro-submit' }), 2000)`.
        3. `await cadastrarCandidato(result.data)` — may throw `CadastroError`.
        4. `const loggedIn = await tryAutoLogin(email, senha)`.
        5. `draft.clear()`; `setSubmitSuccess(true)`.
        6. If `loggedIn` → `toast.success('Cadastro concluído! Bem-vindo(a), ${primeiroNome}.', { duration: 5000 })` + `navigate('/candidato/perfil', { replace: true })`. Else → `toast.success('Cadastro concluído. Faça login para continuar.', { duration: 5000 })` + `navigate('/auth/login?email=' + encodeURIComponent(email))`.
        7. On `catch (err instanceof CadastroError)` → call `routeCadastroError(err, methods, setCurrentStepIndex)` (helper defined in same file; implementation below).
        8. On `catch (any other error)` → `toast.error('Erro inesperado. Tente novamente.')`.
        9. `finally`: `clearTimeout(submitLoadingTimer)`; `toast.dismiss('cadastro-submit')`; `setIsSubmitting(false)`.
    - `routeCadastroError`:
        - `case 'EMAIL_EXISTS'`: `setCurrentStepIndex(0)`; `methods.setError('dadosPessoais.email', { type: 'duplicate', message: err.message })`; `toast.error('Este email já está cadastrado. Tente fazer login ou use outro email.', { duration: 6000 })`.
        - `case 'CPF_EXISTS'`: analogous for `dadosPessoais.cpf`.
        - `case 'VALIDATION'`: if `err.field && FIELD_TO_STEP_INDEX[err.field] !== undefined` → `setCurrentStepIndex(FIELD_TO_STEP_INDEX[err.field])`; `methods.setError(FIELD_TO_STEP_PATH[err.field] as any, { message: err.message })`. Always toast `'Há um problema com os dados enviados. Revise o formulário.'` with 6000ms.
        - `case 'SERVER_ERROR'` / `case 'NETWORK_ERROR'` / default: toast with appropriate copy per UI-SPEC Error Code table (6000ms).
    - `LoadingProgress` Dialog: do NOT open during submit (remove/ignore any `setOpen(true)` call on the submit path). Keep the import and component in the file (reused in Phase 4 per UI-SPEC directive).
    - CTA: Step 4 primary button label is `Criar conta` (idle) / `Criando...` (submitting) with `Loader2 className="animate-spin"` during submit. Use the existing button component; just change the label + add Loader2.
    - Step 4 `Enter` key does NOT trigger submit (UI-SPEC a11y directive); only clicking the button does. Implement by `onKeyDown` on the form's step 4 section (if an `<Input>` receives Enter on step 4, prevent default; otherwise let RHF handle submit).
    - At submit start, the `useLeaveGuard` passes `false` because `isSubmitting === true` — listener is removed automatically.
  </behavior>
  <action>
Add imports at the top of `src/features/cadastro/components/CadastroMultiStepForm.tsx` (KEEP every existing import; add if not present):

  import { useEffect, useState } from 'react'                // useState already present; verify
  import { useNavigate } from 'react-router-dom'
  import { toast } from 'sonner'
  import { Loader2 } from 'lucide-react'
  import { useCadastroDraft } from '../hooks/useCadastroDraft'
  import { useLeaveGuard } from '../hooks/useLeaveGuard'
  import {
    cadastrarCandidato,
    tryAutoLogin,
    CadastroError,
    FIELD_TO_STEP_INDEX,
    FIELD_TO_STEP_PATH,
  } from '../services/cadastroService'

Inside the component function body, add AFTER the existing `useForm(...)` call:

  const navigate = useNavigate()
  const draft = useCadastroDraft()
  const [isSubmitting, setIsSubmitting] = useState(false)
  const [submitSuccess, setSubmitSuccess] = useState(false)
  useLeaveGuard(methods.formState.isDirty && !isSubmitting && !submitSuccess)

  // Load draft on mount (once)
  useEffect(() => {
    const saved = draft.load()
    if (saved) {
      methods.reset(saved as Parameters<typeof methods.reset>[0])
      toast.info('Retomamos seu cadastro de onde você parou.', {
        duration: 4000,
        action: {
          label: 'Começar do zero',
          onClick: () => {
            draft.clear()
            methods.reset()
          },
        },
      })
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [])

  // Debounced draft save on every form change
  const watchedData = methods.watch()
  useEffect(() => {
    const timer = setTimeout(() => draft.save(watchedData as never), 500)
    return () => clearTimeout(timer)
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [JSON.stringify(watchedData)])

Define (near the bottom of the same file, before `export`) the `routeCadastroError` helper:

  function routeCadastroError(
    err: CadastroError,
    methods: ReturnType<typeof useForm<CandidatoFormData>>,
    setCurrentStepIndex: (i: number) => void,
  ) {
    switch (err.code) {
      case 'EMAIL_EXISTS':
        setCurrentStepIndex(0)
        methods.setError('dadosPessoais.email', { type: 'duplicate', message: err.message })
        toast.error('Este email já está cadastrado. Tente fazer login ou use outro email.', { duration: 6000 })
        return
      case 'CPF_EXISTS':
        setCurrentStepIndex(0)
        methods.setError('dadosPessoais.cpf', { type: 'duplicate', message: err.message })
        toast.error('Este CPF já está cadastrado. Tente fazer login ou verifique se é o correto.', { duration: 6000 })
        return
      case 'VALIDATION':
        if (err.field && FIELD_TO_STEP_INDEX[err.field] !== undefined) {
          setCurrentStepIndex(FIELD_TO_STEP_INDEX[err.field])
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          methods.setError(FIELD_TO_STEP_PATH[err.field] as any, { message: err.message })
        }
        toast.error('Há um problema com os dados enviados. Revise o formulário e tente novamente.', { duration: 6000 })
        return
      case 'NETWORK_ERROR':
        toast.error('Sem conexão com o servidor. Verifique sua internet e tente novamente.', { duration: 6000 })
        return
      case 'SERVER_ERROR':
      case 'EDGE_FUNCTION_ERROR':
      case 'UNKNOWN_ERROR':
      default:
        toast.error('Algo deu errado do nosso lado. Tente novamente em alguns instantes.', { duration: 6000 })
        return
    }
  }

REPLACE the existing `handleFormSubmit` (or whatever the current submit handler on Step 4 is called) with this implementation (keep the function name the same; if the current name differs, rename all call sites too):

  const handleFormSubmit = async () => {
    const formData = methods.getValues()
    // Mandatory LGPD check (D-15) — first-line guard before any async work
    if (!formData.autorizacoes?.autorizacao_uso_dados) {
      methods.setError('autorizacoes.autorizacao_uso_dados', {
        type: 'required',
        message: 'Para criar sua conta, você precisa autorizar o uso dos dados.',
      })
      toast.error('Para criar sua conta, você precisa autorizar o uso dos dados.', { duration: 6000 })
      return
    }

    setIsSubmitting(true)
    const submitLoadingTimer = setTimeout(() => {
      toast.loading('Criando sua conta...', { id: 'cadastro-submit' })
    }, 2000)

    try {
      const result = candidatoFormSchema.safeParse(formData)
      if (!result.success) {
        toast.error('Há erros no formulário. Por favor, revise todos os campos.', { duration: 6000 })
        return
      }

      await cadastrarCandidato(result.data)

      const loggedIn = await tryAutoLogin(
        result.data.dadosPessoais.email,
        result.data.dadosPessoais.senha,
      )

      draft.clear()
      setSubmitSuccess(true)
      const primeiroNome = result.data.dadosPessoais.nome_completo.split(' ')[0]

      if (loggedIn) {
        toast.success(`Cadastro concluído! Bem-vindo(a), ${primeiroNome}.`, { duration: 5000 })
        navigate('/candidato/perfil', { replace: true })
      } else {
        toast.success('Cadastro concluído. Faça login para continuar.', { duration: 5000 })
        navigate('/auth/login?email=' + encodeURIComponent(result.data.dadosPessoais.email))
      }
    } catch (err) {
      if (err instanceof CadastroError) {
        routeCadastroError(err, methods, setCurrentStepIndex)
      } else {
        toast.error('Erro inesperado. Tente novamente.', { duration: 6000 })
      }
    } finally {
      clearTimeout(submitLoadingTimer)
      toast.dismiss('cadastro-submit')
      setIsSubmitting(false)
    }
  }

REWRITE the Step 4 primary button JSX (around L506 — the button currently labeled "Finalizar Cadastro"):
  - Label: when `isSubmitting === true`, render `<><Loader2 className="mr-2 h-4 w-4 animate-spin" aria-hidden="true" />Criando...</>`. Otherwise render `<>Criar conta<Check className="ml-2 h-4 w-4" aria-hidden="true" /></>` (Check from lucide-react, already imported).
  - Add `disabled={isSubmitting}` — never disabled because of LGPD state (UI-SPEC directive: mandatory checkbox is guarded by click handler, not button-disabled).
  - onClick / form submit binding: if the form currently auto-submits via RHF `handleSubmit`, wrap with `methods.handleSubmit(handleFormSubmit)`. If it uses an explicit click handler, call `handleFormSubmit` directly.

REMOVE or NEUTER any `setLoadingDialogOpen(true)` or `setLoadingStage(...)` calls on the submit path (UI-SPEC LoadingProgress Dialog deprecation). KEEP the import + state hook (may be used by Phase 4 CV upload); just do not trigger the dialog open. If the component renders a `<LoadingProgress>` bound to a state that only flips during cadastro submit, convert the binding to always `open={false}`.

Enter-key guard on Step 4 (UI-SPEC a11y directive): add an `onKeyDown` handler to the Step 4 wrapper `<div>`:
  const handleStep4KeyDown = (e: React.KeyboardEvent<HTMLDivElement>) => {
    if (e.key === 'Enter' && e.target instanceof HTMLInputElement) {
      e.preventDefault()
    }
  }
  // and on <div className="..."> around step 4 children: onKeyDown={currentStepIndex === 3 ? handleStep4KeyDown : undefined}

Do NOT remove:
  - Existing step navigation (Next/Back) logic
  - Existing Zod validation on Next
  - Existing `useDuplicateCheck` bindings in DadosPessoaisStep
  - Existing progress bar / stepper
  </action>
  <verify>
    <automated>grep -q "import.*useCadastroDraft" src/features/cadastro/components/CadastroMultiStepForm.tsx &amp;&amp; grep -q "import.*useLeaveGuard" src/features/cadastro/components/CadastroMultiStepForm.tsx &amp;&amp; grep -q "cadastrarCandidato\|tryAutoLogin" src/features/cadastro/components/CadastroMultiStepForm.tsx &amp;&amp; grep -q "FIELD_TO_STEP_INDEX" src/features/cadastro/components/CadastroMultiStepForm.tsx &amp;&amp; grep -q "Criar conta" src/features/cadastro/components/CadastroMultiStepForm.tsx &amp;&amp; grep -q "Criando\.\.\." src/features/cadastro/components/CadastroMultiStepForm.tsx &amp;&amp; ! grep -q "Finalizar Cadastro" src/features/cadastro/components/CadastroMultiStepForm.tsx &amp;&amp; grep -q "Retomamos seu cadastro" src/features/cadastro/components/CadastroMultiStepForm.tsx &amp;&amp; grep -q "Para criar sua conta, você precisa autorizar o uso dos dados" src/features/cadastro/components/CadastroMultiStepForm.tsx &amp;&amp; test "$(npm run lint 2>&amp;1 | grep -v 'src/features/vagas' | grep -E 'error TS' | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - File imports `useCadastroDraft`, `useLeaveGuard`, `cadastrarCandidato`, `tryAutoLogin`, `CadastroError`, `FIELD_TO_STEP_INDEX`, `FIELD_TO_STEP_PATH` (grep each)
    - File contains literal `'Criar conta'` and `'Criando...'`
    - File does NOT contain literal `'Finalizar Cadastro'` anywhere (grep negative)
    - File contains literal `'Retomamos seu cadastro de onde você parou.'`
    - File contains literal `'Para criar sua conta, você precisa autorizar o uso dos dados.'`
    - File contains `navigate('/candidato/perfil', { replace: true })` (exact call)
    - File contains `'Cadastro concluído! Bem-vindo(a),'` (toast success)
    - `npm run lint` has 0 new TS errors outside `src/features/vagas/`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    Form orchestration fully wired to Wave 2 primitives. Ready for AutorizacoesStep LGPD polish and E2E validation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch AutorizacoesStep.tsx — LGPD stacked-cards layout + POLICY_VERSION caption + D-15 microcopy</name>
  <files>src/features/cadastro/components/steps/AutorizacoesStep.tsx</files>
  <read_first>
    - src/features/cadastro/components/steps/AutorizacoesStep.tsx (ALL lines — especially L154-176 where the "Seus direitos" list and contact paragraph live)
    - .planning/phases/02-cadastro-candidato/02-UI-SPEC.md § LGPD Checkboxes Layout (Stacked Cards) + § LGPD Banner + § Microcopy Catalog § LGPD Checkbox Labels
    - .planning/phases/02-cadastro-candidato/02-CONTEXT.md D-15, D-16
    - .planning/phases/02-cadastro-candidato/02-PATTERNS.md § `AutorizacoesStep.tsx` PATCH
    - src/features/cadastro/constants.ts (post-Plan 02-04 — import POLICY_VERSION)
  </read_first>
  <action>
In `src/features/cadastro/components/steps/AutorizacoesStep.tsx`:

Add import at the top (alongside existing imports — Lucide already provides Shield/Info):
  import { POLICY_VERSION } from '../../constants'
  import { Shield } from 'lucide-react'   // if not already imported

Lay out the 4 checkboxes per UI-SPEC § LGPD Checkboxes Layout. Use the EXACT microcopy from UI-SPEC Microcopy Catalog § LGPD Checkbox Labels &amp; Descriptions:

  MANDATORY ROW (first):
    Container classes: `bg-blue-500/10 border-2 border-blue-400/30 rounded-lg p-5 space-y-2`
    Icon: <Shield className="h-4 w-4 text-blue-400" aria-hidden="true" /> before label text
    Label: `Autorizo o uso dos meus dados <span className="text-red-400 ml-1">*</span>` (font-semibold)
    Badge BELOW description: `<span className="inline-flex items-center gap-1 px-2 py-1 bg-blue-500/20 rounded text-xs text-blue-300 font-semibold"><Shield className="h-3 w-3" aria-hidden="true" />Obrigatório</span>`
    Description: `Concordo que a Beauty Smile armazene e utilize meus dados pessoais para participação no processo seletivo. Sem esta autorização não é possível criar a conta.`
    Checkbox bound to `autorizacoes.autorizacao_uso_dados` (RHF field)

  OPTIONAL ROW 1 (`autorizacao_comunicacao`):
    Container: `bg-white/5 border border-white/10 rounded-lg p-5 space-y-2`
    Icon: none
    Label: `Autorizo receber comunicações` (font-semibold, no red asterisk)
    Description: `Concordo em receber emails e notificações sobre o andamento do processo seletivo e novas oportunidades de vagas da Beauty Smile.`

  OPTIONAL ROW 2 (`autorizacao_retencao_curriculo`):
    Container: same as Optional 1
    Label: `Autorizo manter meu currículo`
    Description: `Concordo que a Beauty Smile mantenha meu currículo em banco de dados por até 2 anos para futuras oportunidades, mesmo que eu não seja selecionado(a) no processo atual.`

  OPTIONAL ROW 3 (`autorizacao_analise_video`):
    Container: same as Optional 1
    Label: `Autorizo análise de vídeo-entrevistas`
    Description: `Concordo que minhas entrevistas em vídeo sejam gravadas e analisadas para avaliação comportamental e de comunicação.`
    NOTE: the word "IA" must NOT appear in this description — per CLAUDE.md linguistic rule.

Wrap the 4 rows in a `<fieldset><legend className="sr-only">Autorizações LGPD</legend>{...rows...}</fieldset>` (UI-SPEC a11y § Screen Reader Specific).

Add the policy version caption. BELOW the "Política de Privacidade" link (existing in the component), add:
  <p className="text-white/70 text-xs">Versão {POLICY_VERSION}</p>

Add BELOW the existing contact paragraph (UI-SPEC § LGPD Banner directive):
  <p className="text-white/70 text-sm mt-2">
    Esta política está na versão <strong>{POLICY_VERSION}</strong>.
  </p>

The font-weight sweep for this file runs in Task 4. Do not preempt it here — just use `font-semibold` for Label and badge per UI-SPEC.

Do NOT remove:
  - The "Política de Privacidade" link (keep the existing handler)
  - The "Seus direitos" list
  - The LGPD contact email paragraph (`lgpd@beautysmile.com.br`)
  - Existing RHF Controller / Checkbox wiring (extend with the new layout; do not replace with raw HTML `<input>`)
  </action>
  <verify>
    <automated>grep -q "POLICY_VERSION" src/features/cadastro/components/steps/AutorizacoesStep.tsx &amp;&amp; grep -q "Autorizo o uso dos meus dados" src/features/cadastro/components/steps/AutorizacoesStep.tsx &amp;&amp; grep -q "Obrigatório" src/features/cadastro/components/steps/AutorizacoesStep.tsx &amp;&amp; grep -q "Autorizo receber comunicações" src/features/cadastro/components/steps/AutorizacoesStep.tsx &amp;&amp; grep -q "Autorizo manter meu currículo" src/features/cadastro/components/steps/AutorizacoesStep.tsx &amp;&amp; grep -q "Autorizo análise de vídeo-entrevistas" src/features/cadastro/components/steps/AutorizacoesStep.tsx &amp;&amp; grep -q "avaliação comportamental e de comunicação" src/features/cadastro/components/steps/AutorizacoesStep.tsx &amp;&amp; ! grep -qE "\\bIA\\b" src/features/cadastro/components/steps/AutorizacoesStep.tsx &amp;&amp; grep -q "fieldset" src/features/cadastro/components/steps/AutorizacoesStep.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File imports `POLICY_VERSION` from `../../constants`
    - File contains all 4 label strings verbatim: `Autorizo o uso dos meus dados`, `Autorizo receber comunicações`, `Autorizo manter meu currículo`, `Autorizo análise de vídeo-entrevistas`
    - File contains literal `Obrigatório` badge text
    - File contains the analise_video description with literal `avaliação comportamental e de comunicação`
    - File does NOT contain the standalone word `IA` (word boundary grep — avoid false matches like `via`, `primaria`, etc.; use `\bIA\b`)
    - File contains a `<fieldset>` wrapping the checkbox rows
    - File contains `{POLICY_VERSION}` rendered in JSX
    - `npm run lint` has 0 new TS errors outside `src/features/vagas/`
  </acceptance_criteria>
  <done>
    LGPD step is UI-SPEC compliant; policy_version is visible to the user.
  </done>
</task>

<task type="auto">
  <name>Task 3: Font-weight sweep across src/features/cadastro/components/</name>
  <files>src/features/cadastro/components/CadastroMultiStepForm.tsx, src/features/cadastro/components/steps/AutorizacoesStep.tsx, src/features/cadastro/components/steps/DadosPessoaisStep.tsx, src/features/cadastro/components/steps/DisponibilidadeStep.tsx, src/features/cadastro/components/steps/EnderecoStep.tsx, src/features/cadastro/components/steps/DadosProfissionaisStep.tsx</files>
  <read_first>
    - .planning/phases/02-cadastro-candidato/02-UI-SPEC.md § Typography ("Exactly 4 sizes and 2 weights (400 regular, 600 semibold)") + § Forbidden in Phase 2 ("No font-medium (500) and NO font-bold (700) anywhere in Phase 2 surfaces")
    - .planning/phases/02-cadastro-candidato/02-UI-SPEC.md Open Question 5 (planner-level sweep instruction)
    - Each of the 6 files listed above — inspect current `font-medium` / `font-bold` usage
  </read_first>
  <action>
Run grep across the listed files for `font-medium` and `font-bold`:
  grep -rEn "font-medium|font-bold" src/features/cadastro/components/

For EACH match:
  - If the element is a TITLE (h1/h2/h3), LABEL, BUTTON, BADGE, or otherwise emphasized: replace with `font-semibold` (600).
  - If the element is body text, helper text, or a paragraph: replace with `font-normal` (400) — or remove the class entirely if the default is already 400.

Do NOT:
  - Change any other class (no color/spacing/size edits in this task)
  - Modify files outside `src/features/cadastro/components/`
  - Touch `src/components/ui/` primitives (they may have internal `font-medium`; out of this phase's scope)

After the sweep, re-run grep to confirm 0 matches inside `src/features/cadastro/components/`:
  grep -rEn "font-medium|font-bold" src/features/cadastro/components/   # expect: no output
  </action>
  <verify>
    <automated>test "$(grep -rE "font-medium|font-bold" src/features/cadastro/components/ 2>/dev/null | wc -l | tr -d ' ')" = "0" &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "font-medium|font-bold" src/features/cadastro/components/` returns 0 lines
    - `npm run build` exits 0
    - Every file listed in `<files>` is either (a) unchanged because it had no matches, or (b) modified — in both cases it compiles
  </acceptance_criteria>
  <done>
    UI-SPEC Dimension 4 compliance achieved for cadastro surface.
  </done>
</task>

<task type="auto">
  <name>Task 4: Extend e2e/cadastro-flow.spec.ts — 6 new test cases from 02-VALIDATION.md</name>
  <files>e2e/cadastro-flow.spec.ts</files>
  <read_first>
    - e2e/cadastro-flow.spec.ts (ALL 342 lines — Playwright describe block + existing happy path)
    - .planning/phases/02-cadastro-candidato/02-VALIDATION.md § Wave 0 Requirements > Test scaffolding > `e2e/cadastro-flow.spec.ts` (lists the 6 cases)
    - .planning/phases/02-cadastro-candidato/02-UI-SPEC.md § Success State + § Error States + § Draft Auto-Restore Notice
    - .planning/phases/02-cadastro-candidato/02-PATTERNS.md § `e2e/cadastro-flow.spec.ts` PATCH
    - .planning/phases/02-cadastro-candidato/02-RESEARCH.md Pitfall 5 (rate-limit test fragility) + Pitfall 6 (signInWithPassword rate-limit)
  </read_first>
  <action>
Edit `e2e/cadastro-flow.spec.ts`:

GLOBAL RENAME: change every `button:has-text("Finalizar Cadastro")` selector to `button:has-text("Criar conta")` (the label rename from Task 1).

ADD the following test cases inside the existing `test.describe('Cadastro de Candidato - Fluxo Completo', ...)` block. Use `test+${Date.now()}@beautysmile.com.br` / `test+${Date.now()}_n@...` for unique emails (Pitfall 6). If a test requires a known-duplicate, use a seed email from environment (e.g., `process.env.E2E_DUPLICATE_EMAIL`) with a `.skip(!process.env.E2E_DUPLICATE_EMAIL, 'requires seed')` guard. Each test fills Steps 1-4 with a reusable helper defined at the top of the file (if one does not exist, add one).

  // ─── Case 1: Happy path with auto-login to /candidato/perfil ───
  test('happy path: auto-login lands on /candidato/perfil with welcome toast', async ({ page }) => {
    await page.goto('/cadastro')
    // fill all 4 steps (reuse existing helper or inline the field fills from the existing happy-path test)
    // ... existing Step 1-4 fills ...
    await page.click('button:has-text("Criar conta")')
    // Wait for navigation + toast
    await page.waitForURL(/\/candidato\/perfil/, { timeout: 15000 })
    await expect(page.locator('text=/Cadastro concluído! Bem-vindo\\(a\\),/')).toBeVisible({ timeout: 8000 })
  })

  // ─── Case 2: EMAIL_EXISTS race → auto-navigate Step 1 + inline error ───
  test('EMAIL_EXISTS at submit: auto-navigates to Step 1 and shows inline error', async ({ page }) => {
    test.skip(!process.env.E2E_DUPLICATE_EMAIL, 'Set E2E_DUPLICATE_EMAIL in env to a known duplicate account to run this case')
    await page.goto('/cadastro')
    // Fill Step 1 with the seeded duplicate email; fill remaining steps normally
    // ... existing fills, but email = process.env.E2E_DUPLICATE_EMAIL! ...
    await page.click('button:has-text("Criar conta")')
    // Expect: form bounced back to Step 1
    await expect(page.locator('text=Dados Pessoais')).toBeVisible()
    // Inline error on email field
    await expect(page.locator('text=/[Ee]ste email já está cadastrado/')).toBeVisible()
  })

  // ─── Case 3: CPF_EXISTS path at blur (debounce) ───
  test('CPF_EXISTS at blur: inline indicator shows "já cadastrado" and blocks Next', async ({ page }) => {
    test.skip(!process.env.E2E_DUPLICATE_CPF, 'Set E2E_DUPLICATE_CPF in env to a known duplicate CPF to run this case')
    await page.goto('/cadastro')
    await page.fill('input[name="dadosPessoais.cpf"]', process.env.E2E_DUPLICATE_CPF!)
    await page.locator('input[name="dadosPessoais.cpf"]').blur()
    await expect(page.locator('text=/CPF já cadastrado|Este CPF já está cadastrado/')).toBeVisible({ timeout: 3000 })
  })

  // ─── Case 4: LGPD mandatory unchecked blocks submit ───
  test('LGPD mandatory: submit blocked when autorizacao_uso_dados is false', async ({ page }) => {
    await page.goto('/cadastro')
    // fill all 4 steps normally BUT leave autorizacao_uso_dados unchecked on Step 4
    // ... fills ...
    // On Step 4: do NOT check the mandatory checkbox
    await page.click('button:has-text("Criar conta")')
    await expect(page.locator('text=/Para criar sua conta, você precisa autorizar o uso dos dados/')).toBeVisible({ timeout: 3000 })
    // URL must NOT have navigated to /candidato/perfil
    expect(page.url()).toContain('/cadastro')
  })

  // ─── Case 5: Draft auto-restore on refresh ───
  test('draft restore: Step 1-2 fields preserved after refresh (senha NOT preserved)', async ({ page }) => {
    await page.goto('/cadastro')
    const uniqueName = `Maria Rascunho ${Date.now()}`
    await page.fill('input[name="dadosPessoais.nome_completo"]', uniqueName)
    // Wait a beat for the 500ms debounced save
    await page.waitForTimeout(700)
    // Reload
    await page.reload()
    await expect(page.locator('text=/Retomamos seu cadastro de onde você parou/')).toBeVisible({ timeout: 5000 })
    await expect(page.locator('input[name="dadosPessoais.nome_completo"]')).toHaveValue(uniqueName)
    // Senha field must be EMPTY (D-13)
    await expect(page.locator('input[name="dadosPessoais.senha"]')).toHaveValue('')
  })

  // ─── Case 6: rate_limited toast (optional, often flaky; skip by default) ───
  test.skip('rate_limited toast: 31 rapid blurs trigger "Muitas tentativas"', async ({ page }) => {
    // Skipped by default per Pitfall 5 (hard to trigger deterministically in E2E;
    // integration-level test lives in src/features/cadastro/services/__tests__).
  })

Update the `test.describe` happy-path existing test if it references `/candidato/perfil` yet — ensure it asserts the navigation + toast like Case 1 above. If the existing happy-path test does NOT yet wait for /candidato/perfil navigation (Phase 1 version may only fill form), either delete it or rename it to `legacy` and supersede with Case 1.
  </action>
  <verify>
    <automated>grep -q "happy path: auto-login lands on /candidato/perfil" e2e/cadastro-flow.spec.ts &amp;&amp; grep -q "LGPD mandatory: submit blocked" e2e/cadastro-flow.spec.ts &amp;&amp; grep -q "draft restore: Step 1-2 fields preserved" e2e/cadastro-flow.spec.ts &amp;&amp; grep -q "EMAIL_EXISTS at submit" e2e/cadastro-flow.spec.ts &amp;&amp; grep -q "CPF_EXISTS at blur" e2e/cadastro-flow.spec.ts &amp;&amp; grep -q "Criar conta" e2e/cadastro-flow.spec.ts &amp;&amp; ! grep -q "Finalizar Cadastro" e2e/cadastro-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File contains all 6 new test titles (grep the exact strings above)
    - File contains literal `Criar conta`
    - File does NOT contain literal `Finalizar Cadastro`
    - File references `process.env.E2E_DUPLICATE_EMAIL` and `process.env.E2E_DUPLICATE_CPF` (skip guards)
    - File contains assertion `Retomamos seu cadastro de onde você parou`
    - File contains assertion `Para criar sua conta, você precisa autorizar o uso dos dados`
  </acceptance_criteria>
  <done>
    E2E spec extended. Skipped cases (2, 3, 6) are explicitly gated so the CI run is deterministic.
  </done>
</task>

<task type="auto">
  <name>Task 5: Full-suite validation gate</name>
  <files>(verification only)</files>
  <read_first>
    - .planning/phases/02-cadastro-candidato/02-VALIDATION.md (sampling rate — full suite before verify-work)
  </read_first>
  <action>
Run sequentially:
  1. npm run lint 2>&1 | grep -v 'src/features/vagas' | grep -E 'error TS' | wc -l   # expect: 0
  2. npm run test:run                                                                   # all Vitest — expect: 0 fails
  3. npm run build                                                                       # expect: 0 errors
  4. npx playwright install chromium                                                     # ensure browser is installed (cheap if cached)
  5. npx playwright test cadastro-flow --project=chromium --reporter=line                # E2E — expect: pass. Cases 2, 3, 6 will be skipped by env gate.

If step 5 fails because the dev server is not running, start it in a separate terminal (`npm run dev`) first. Playwright config will target http://localhost:3003 per project convention.

If the happy-path E2E fails with a specific assertion (e.g., toast text not found, navigation timeout), DO NOT modify the test to be lenient — fix the root cause in Task 1 or Task 2.

Report results in the SUMMARY.
  </action>
  <verify>
    <automated>test "$(npm run lint 2>&amp;1 | grep -v 'src/features/vagas' | grep -E 'error TS' | wc -l | tr -d ' ')" = "0" &amp;&amp; npm run test:run &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` has 0 new TS errors outside `src/features/vagas/`
    - `npm run test:run` exits 0 with all Vitest tests passing
    - `npm run build` exits 0
    - Playwright `cadastro-flow` exits 0 (happy path passes; seed-dependent cases skipped if env not set)
  </acceptance_criteria>
  <done>
    Phase 2 feature code is green across unit + build + E2E.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Manual UAT preflight — iPhone 12 Pro viewport + Sonner toast audit + draft UX</name>
  <what-built>
    Tasks 1-4 wired the entire cadastro flow per Phase 2 CONTEXT + UI-SPEC. Task 5 proved it compiles and the automated tests pass. Now the human operator must eyeball the UX surfaces that automated tests cannot fully capture — these are the manual-only behaviors declared in 02-VALIDATION.md.
  </what-built>
  <how-to-verify>
    Run `npm run dev`. In Chrome, open DevTools → Device Toolbar → select "iPhone 12 Pro" (390×844). Navigate to http://localhost:3003/cadastro.

    Verify the following six behaviors. Report PASS/FAIL + any screenshots/notes for each.

    1. **Mobile fit**: No horizontal scroll on any step. Stepper labels are hidden (mobile) but circles are 32×32. Buttons are full-width on each step.

    2. **LGPD visual hierarchy (UI-SPEC)**: On Step 4, the mandatory checkbox row has a blue tint (`bg-blue-500/10`), Shield icon, red asterisk, and "Obrigatório" badge. The 3 optional rows are lighter (`bg-white/5`). The "Versão v1.0-2026-04" caption is visible below the "Política de Privacidade" link.

    3. **"avaliação comportamental" language**: The `autorizacao_analise_video` description contains the phrase "avaliação comportamental e de comunicação" and does NOT contain the word "IA" or "teste psicológico".

    4. **Draft restore UX**: Fill Step 1 partially (name + email + CPF). Refresh the page (Cmd-R). A Sonner toast "Retomamos seu cadastro de onde você parou." appears with a "Começar do zero" action. The name, email, CPF fields are pre-populated. The senha and confirmar_senha fields are EMPTY. Click the "Começar do zero" action — all fields clear.

    5. **CTA label**: On Step 4 idle, the primary button reads "Criar conta" with a check icon on the right. During submit (click), it reads "Criando..." with a spinning Loader2 on the left; button is disabled. After success toast, navigates to `/candidato/perfil` immediately.

    6. **Mandatory LGPD block**: On Step 4, without checking `autorizacao_uso_dados`, click "Criar conta". A red Sonner toast appears with copy "Para criar sua conta, você precisa autorizar o uso dos dados." The mandatory row shows an inline error. The URL does NOT change (still `/cadastro`).

    If any behavior fails, paste the observed behavior + screenshot as the resume-signal. If all pass, resume with "approved — UAT preflight green".
  </how-to-verify>
  <resume-signal>Type "approved — UAT preflight green" OR describe the failing behavior with enough detail to reproduce.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RHF form state → session storage | `senha`/`confirmar_senha` must never cross (asserted by Plan 02-04 Test 1; re-verified by Task 6 step 4) |
| Client → Edge Function | After Task 1, goes through `cadastrarCandidato` only; no `supabaseAdmin` import reachable |
| Client → auth server | Only via `tryAutoLogin` (from Plan 02-05); senha stays in RHF state then GC'd on unmount |
| Client → router navigation | `navigate('/candidato/perfil')` only after successful auto-login; fallback to `/auth/login` is also a success outcome |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Information Disclosure | Welcome toast shows `primeiroNome` (PII) | accept | First name is the user's own first name; toast is same-origin and same-session. Not a leak. |
| T-02-09 | DoS | `beforeunload` dialog during submit | mitigate | `useLeaveGuard(...)` passes `!isSubmitting` — listener removed at submit start. UI-SPEC directive satisfied (Pitfall 4). |
| T-02-11 | Spoofing | Server-provided `field` triggers client navigation | mitigate | `FIELD_TO_STEP_INDEX` whitelist in `routeCadastroError` rejects unknown fields (only schema leaves navigate). Unknown `field` → generic toast, no step change. |
| T-02-14 | Tampering | LGPD mandatory bypass via form-dev-tools | mitigate | Two layers: (a) `handleFormSubmit` first-line guard on `autorizacao_uso_dados === false`; (b) server Zod schema asserts `z.literal(true)` on the same field (Edge Function Plan 02-03). Client bypass still fails at server. |
| T-02-15 | Information Disclosure | E2E test uses real emails in production DB | mitigate | Happy-path E2E uses `test+${Date.now()}@beautysmile.com.br` — unique per run, filterable in DB by prefix. Seed-required cases gated by env var so CI does not spam prod. |
</threat_model>

<verification>
- `npm run lint` 0 new TS errors; `npm run build` clean; `npm run test:run` all green
- `npx playwright test cadastro-flow --project=chromium` exits 0
- Manual UAT preflight (Task 6) passes all 6 steps
- `grep -rE "font-medium|font-bold" src/features/cadastro/components/` returns 0 lines
- `grep "Finalizar Cadastro" src/features/cadastro e2e/ 2>/dev/null` returns 0 lines
</verification>

<success_criteria>
- CAD-01 (4-step submit), CAD-05 (LGPD block), CAD-06 (auto-login redirect) verified end-to-end
- D-03, D-04, D-13, D-14, D-15, D-16 implemented at UI layer
- UI-SPEC Dimension 4 compliant (weights 400/600 only in cadastro surface)
- No regression to features/vagas or store/authStore (Phase 3/4 deferred bugs untouched)
- Phase 2 goal fulfilled: candidate completes the form and lands on /candidato/perfil logged in
</success_criteria>

<output>
After completion, create `.planning/phases/02-cadastro-candidato/02-06-SUMMARY.md` and ALSO:
  1. Flip `nyquist_compliant: false` → `nyquist_compliant: true` in the frontmatter of `.planning/phases/02-cadastro-candidato/02-VALIDATION.md` (only if Task 5 proved green).
  2. Note in the SUMMARY: list of font-weight replacements made (file:line, before/after) for the sweep in Task 3.
  3. Capture the Playwright output of the cadastro-flow spec run (pass/skip/fail breakdown).
  4. Note any Task 6 UAT adjustments (screenshots / copy tweaks) applied post-human-review.
</output>
