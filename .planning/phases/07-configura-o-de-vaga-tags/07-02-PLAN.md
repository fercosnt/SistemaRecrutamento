---
phase: 07-configura-o-de-vaga-tags
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - supabase/migrations/20260607010001_pergunta_opcao_metadata.sql
  - supabase/migrations/20260607010002_vagas_config_columns.sql
  - supabase/migrations/20260607010003_upsert_pergunta_opcoes_metadata_rpc.sql
  - supabase/migrations/20260607010004_publish_vaga_rpc.sql
  - database.types.ts
autonomous: false
requirements: [VAGACFG-01, VAGACFG-02, VAGACFG-03]

must_haves:
  truths:
    - "The enum enum_tag_opcao (5 tags) and table pergunta_opcao_metadata (with opcao_id AND opcao_texto, peso CHECK, RLS) exist in the live DB"
    - "vagas has testes_aplicaveis jsonb and pesos_avaliacao jsonb columns with sane defaults"
    - "The only new vagas columns added this phase are testes_aplicaveis jsonb + pesos_avaliacao jsonb — qualificacao_etapa1 is NOT added (it is Phase 8) and testes_aplicaveis follows the RF-11 shape {teste,obrigatorio,customizado,perguntas?} (D-06)"
    - "Option tags are stored in the relational table pergunta_opcao_metadata keyed by a stable opcao_id (uuid generated inside opcoes_resposta), and the jsonb↔table state is synced atomically by the single SECURITY DEFINER RPC — not by non-atomic client orchestration (D-10)"
    - "The tag taxonomy is the 5-value enum_tag_opcao plus peso int CHECK(-999..100) default 0 plus nota_ia text nullable, so an unmarked option resolves to neutro/0/null (D-11)"
    - "pergunta_opcao_metadata persists BOTH opcao_id AND the denormalized opcao_texto, satisfying the F8/F10 join contract (match by opcao_id, opcao_texto as fallback/audit) (D-14)"
    - "upsert_pergunta_opcoes_metadata RPC atomically syncs opcoes_resposta jsonb (with stable opcao_id) and pergunta_opcao_metadata, RH/admin only (in-body 42501 check)"
    - "publish_vaga RPC re-checks the 3 D-12 conditions server-side before flipping status to ativa"
    - "database.types.ts is regenerated and contains the new enum, table, and columns"
  artifacts:
    - path: "supabase/migrations/20260607010001_pergunta_opcao_metadata.sql"
      provides: "enum_tag_opcao + pergunta_opcao_metadata table + indexes + RLS"
      contains: "enum_tag_opcao"
    - path: "supabase/migrations/20260607010002_vagas_config_columns.sql"
      provides: "vagas.testes_aplicaveis + vagas.pesos_avaliacao jsonb columns"
      contains: "testes_aplicaveis"
    - path: "supabase/migrations/20260607010003_upsert_pergunta_opcoes_metadata_rpc.sql"
      provides: "atomic jsonb↔table sync RPC (SECURITY DEFINER + in-body role check)"
      contains: "upsert_pergunta_opcoes_metadata"
    - path: "supabase/migrations/20260607010004_publish_vaga_rpc.sql"
      provides: "server-side D-12 publish gate RPC"
      contains: "publish_vaga"
  key_links:
    - from: "pergunta_opcao_metadata"
      to: "perguntas_formulario"
      via: "FK on opcao_id source (pergunta_id REFERENCES perguntas_formulario(id))"
      pattern: "REFERENCES public.perguntas_formulario"
    - from: "upsert_pergunta_opcoes_metadata"
      to: "perguntas_formulario.opcoes_resposta"
      via: "jsonb writeback with gen_random_uuid opcao_id"
      pattern: "gen_random_uuid"
---

<objective>
Build the entire Phase-7 schema + write-path in four migration files (enum + table + indexes + RLS;
2 vagas jsonb columns; the atomic sync RPC; the publish-gate RPC), then [BLOCKING, NON-AUTONOMOUS]
apply them to the live DB via the D-22 workaround and regenerate `database.types.ts`. This is the
load-bearing foundation: nothing in Plans 03/04 can persist real data without it, and verification
cannot pass without the apply (types come from the generated file, so build/type checks give a
false-positive without the live schema).

Purpose: server-authoritative schema for VAGACFG-01/02/03 + the F8/F10/F15 downstream contract.
Output: 4 migration files + regenerated database.types.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-configura-o-de-vaga-tags/07-CONTEXT.md
@.planning/phases/07-configura-o-de-vaga-tags/07-RESEARCH.md
@.planning/phases/07-configura-o-de-vaga-tags/07-PATTERNS.md
@.planning/phases/07-configura-o-de-vaga-tags/07-SQL-SMOKE-RUNBOOK.md

<interfaces>
<!-- Live idioms to mirror EXACTLY. Sourced from shipped migrations (verified). -->

RLS role idiom (live-verified, 20260607000006) — use EXACTLY this, NOT the stale PRD `'admin'`:
  (select auth.jwt() #>> '{app_metadata,role}') IN ('rh', 'administrador')

SECURITY DEFINER atomic-write idiom (20260425000003_submit_candidatura_rpc.sql):
  RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path = '' AS $$ ... $$;
  + jsonb_array_elements loop + REVOKE ALL ON FUNCTION ... FROM PUBLIC; + GRANT EXECUTE ...

DEVIATION from analog: this RPC is called from the authenticated CLIENT (not an EF), so:
  GRANT EXECUTE ... TO authenticated;  (NOT service_role)
  + in-body role check (RLS does not apply to DEFINER bodies):
    v_role := (auth.jwt() #>> '{app_metadata,role}');
    IF v_role IS NULL OR v_role NOT IN ('rh','administrador') THEN RAISE EXCEPTION 'forbidden' USING errcode='42501'; END IF;

status_vaga enum has 4 values (live): rascunho | ativa | inativa | arquivada (Pitfall 5).
publish_vaga only transitions rascunho → ativa.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: enum + pergunta_opcao_metadata table + indexes + RLS + vagas columns (migration files)</name>
  <files>supabase/migrations/20260607010001_pergunta_opcao_metadata.sql, supabase/migrations/20260607010002_vagas_config_columns.sql</files>
  <read_first>
    - .planning/phases/07-configura-o-de-vaga-tags/07-RESEARCH.md (§Code Examples: full table DDL + enum + indexes + RLS; vagas columns DDL; testes_aplicaveis + pesos_avaliacao jsonb shapes)
    - supabase/migrations/20260607000006_rls_policies_m2_backbone.sql (RLS role idiom + 42601 header note to copy verbatim)
    - supabase/migrations/20260607000003_decisao_final.sql (M2 table DDL pattern)
    - supabase/migrations/20260607000002_etapa_processo_v2_cutover.sql (ALTER TABLE column-add pattern)
    - .planning/phases/07-configura-o-de-vaga-tags/07-CONTEXT.md (D-06, D-07, D-10, D-11, D-14)
  </read_first>
  <action>
    Write two migration files (NO `BEGIN;/COMMIT;` wrappers — the CLI driver wraps each migration
    in an implicit transaction; outer wrapper triggers SQLSTATE 42601 per CLAUDE.md §Commands; copy
    the 42601 explanatory header comment from 20260607000006). File 010001: `CREATE TYPE public.enum_tag_opcao
    AS ENUM ('knockout','atencao','neutro','pontua','fortemente_pontua')` (D-11); `CREATE TABLE public.pergunta_opcao_metadata`
    with columns id (uuid pk default gen_random_uuid), pergunta_id uuid NOT NULL REFERENCES public.perguntas_formulario(id)
    ON DELETE CASCADE, opcao_id uuid NOT NULL, opcao_texto text NOT NULL (D-14 — store BOTH, denormalized for F8/F10
    join-by-text), tag public.enum_tag_opcao NOT NULL DEFAULT 'neutro', peso int NOT NULL DEFAULT 0 CHECK (peso BETWEEN -999 AND 100),
    nota_ia text, ordem int NOT NULL DEFAULT 0, created_at/updated_at timestamptz, UNIQUE (pergunta_id, opcao_id);
    index `idx_pom_pergunta` on (pergunta_id) + partial `idx_pom_knockout` on (pergunta_id) WHERE tag='knockout';
    ENABLE ROW LEVEL SECURITY; a `FOR ALL` policy `rh_gerencia_opcao_metadata` with USING + WITH CHECK both using the
    live role idiom `(select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador')` (Pitfall 3 — NOT 'admin').
    File 010002: `ALTER TABLE public.vagas ADD COLUMN IF NOT EXISTS testes_aplicaveis jsonb NOT NULL DEFAULT '[]'::jsonb,
    ADD COLUMN IF NOT EXISTS pesos_avaliacao jsonb NOT NULL DEFAULT '{"triagem":0,"work_sample_sjt":0,"redacao_cultural":0,"entrevista":0}'::jsonb`.
    Do NOT add a CHECK forcing pesos sum=100 — drafts are allowed to be invalid (D-12; sum is gated at publish only).
    Add a COMMENT documenting the testes_aplicaveis shape per RF-11 ({teste,obrigatorio,customizado,perguntas?}).
  </action>
  <verify>
    <automated>grep -q 'enum_tag_opcao' supabase/migrations/20260607010001_pergunta_opcao_metadata.sql && grep -q "app_metadata,role" supabase/migrations/20260607010001_pergunta_opcao_metadata.sql && ! grep -qiE "in \('admin'\)|'admin'\)" supabase/migrations/20260607010001_pergunta_opcao_metadata.sql && grep -q 'opcao_texto' supabase/migrations/20260607010001_pergunta_opcao_metadata.sql && grep -q 'testes_aplicaveis' supabase/migrations/20260607010002_vagas_config_columns.sql && ! grep -qiE '^begin;|^commit;' supabase/migrations/20260607010001_pergunta_opcao_metadata.sql && echo MIGRATIONS_DDL_OK</automated>
  </verify>
  <acceptance_criteria>
    - 010001 contains enum_tag_opcao with all 5 tag values, the table with opcao_id + opcao_texto + peso CHECK (-999..100), both indexes, RLS enabled, and the FOR ALL policy with the live role idiom.
    - 010001 uses `'administrador'` NOT `'admin'` (source assertion: grep finds no `'admin'` role literal).
    - 010002 adds exactly the 2 jsonb columns with the specified defaults and NO sum=100 CHECK.
    - Neither file has BEGIN;/COMMIT; wrappers.
  </acceptance_criteria>
  <done>Two DDL migration files committed, no transaction wrappers, live RLS idiom used.</done>
</task>

<task type="auto">
  <name>Task 2: sync RPC + publish-gate RPC (migration files)</name>
  <files>supabase/migrations/20260607010003_upsert_pergunta_opcoes_metadata_rpc.sql, supabase/migrations/20260607010004_publish_vaga_rpc.sql</files>
  <read_first>
    - .planning/phases/07-configura-o-de-vaga-tags/07-RESEARCH.md (§Code Examples: FULL upsert_pergunta_opcoes_metadata RPC body — copy verbatim; §Architecture Pattern 3 publish_vaga guard)
    - supabase/migrations/20260425000003_submit_candidatura_rpc.sql (EXACT idiom to mirror: DEFINER header, search_path='', jsonb_array_elements loop, REVOKE/GRANT footer, 42601 header note)
    - .planning/phases/07-configura-o-de-vaga-tags/07-CONTEXT.md (D-10, D-12, D-13, D-14)
    - .planning/phases/07-configura-o-de-vaga-tags/07-RESEARCH.md (Pitfall 5 — status_vaga 4 values; publish only rascunho→ativa)
  </read_first>
  <action>
    Write two PL/pgSQL RPC migration files (NO BEGIN/COMMIT wrappers; copy the 42601 header comment).
    File 010003 — `upsert_pergunta_opcoes_metadata(p_pergunta_id uuid, p_opcoes jsonb)` RETURNS jsonb,
    LANGUAGE plpgsql, SECURITY DEFINER, SET search_path='': copy the RESEARCH §Code Examples body verbatim —
    in-body role check raising 42501 if role NOT IN ('rh','administrador'); DELETE existing metadata rows for
    p_pergunta_id (idempotent); loop jsonb_array_elements, COALESCE the opcao_id or gen_random_uuid() (backfill),
    accumulate the new `[{id,texto}]` jsonb, INSERT into pergunta_opcao_metadata (pergunta_id, opcao_id, opcao_texto,
    tag, peso, nota_ia, ordem) with COALESCE defaults (tag→'neutro', peso→0, ordem→0); UPDATE perguntas_formulario
    SET opcoes_resposta = the id-bearing jsonb + updated_at=now(); RETURN a summary jsonb. COMMENT ON FUNCTION;
    REVOKE ALL ... FROM PUBLIC; GRANT EXECUTE ... TO authenticated (the client-called deviation, NOT service_role).
    File 010004 — `publish_vaga(p_vaga_id uuid)` RETURNS jsonb, SECURITY DEFINER, search_path='': in-body role check
    (42501); re-check the 3 D-12 conditions server-side — (1) the vaga's pesos_avaliacao jsonb sums to 100 across the
    4 keys triagem+work_sample_sjt+redacao_cultural+entrevista, (2) at least one testes_aplicaveis element has obrigatorio=true,
    (3) NO pergunta of this vaga has a knockout-tagged option while being obrigatoria=false (join perguntas_formulario ↔
    pergunta_opcao_metadata WHERE tag='knockout'); if any condition fails, RAISE EXCEPTION with a descriptive message
    USING errcode='P0001' (or a check-violation code) so the client can map it; only when all pass, UPDATE public.vagas
    SET status='ativa' WHERE id=p_vaga_id AND status='rascunho' (Pitfall 5 — only rascunho→ativa); RETURN summary.
    REVOKE ALL FROM PUBLIC + GRANT EXECUTE TO authenticated.
  </action>
  <verify>
    <automated>grep -q 'upsert_pergunta_opcoes_metadata' supabase/migrations/20260607010003_upsert_pergunta_opcoes_metadata_rpc.sql && grep -q 'gen_random_uuid' supabase/migrations/20260607010003_upsert_pergunta_opcoes_metadata_rpc.sql && grep -q "search_path = ''" supabase/migrations/20260607010003_upsert_pergunta_opcoes_metadata_rpc.sql && grep -q '42501' supabase/migrations/20260607010003_upsert_pergunta_opcoes_metadata_rpc.sql && grep -q 'GRANT EXECUTE' supabase/migrations/20260607010003_upsert_pergunta_opcoes_metadata_rpc.sql && grep -q 'publish_vaga' supabase/migrations/20260607010004_publish_vaga_rpc.sql && grep -q "status = 'ativa'\|status='ativa'" supabase/migrations/20260607010004_publish_vaga_rpc.sql && echo RPCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - 010003 is SECURITY DEFINER + search_path='' + in-body 42501 role check + gen_random_uuid opcao_id backfill + jsonb writeback to perguntas_formulario + GRANT EXECUTE TO authenticated (NOT service_role).
    - 010004 re-checks all 3 D-12 conditions server-side and only transitions rascunho→ativa.
    - Both files have REVOKE ALL FROM PUBLIC + GRANT EXECUTE TO authenticated and NO BEGIN/COMMIT wrappers.
  </acceptance_criteria>
  <done>Two RPC migration files committed mirroring the shipped DEFINER idiom with the client-called GRANT deviation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Apply migrations to live DB (NON-AUTONOMOUS — D-22 / SQLSTATE 42601)</name>
  <what-built>
    Four Phase-7 migration files (enum + table + RLS + indexes; 2 vagas jsonb columns; 2 PL/pgSQL
    SECURITY DEFINER RPCs). These CANNOT be applied via autonomous `supabase db push --linked` — the
    `$$`-body RPCs + adjacent COMMENT/GRANT/REVOKE trigger SQLSTATE 42601 in the transaction pooler
    (CLAUDE.md §Commands; recurred in Phase 4 + Phase 6). RH/admin authorization is enforced by RLS +
    in-body 42501 role checks. The publish_vaga RPC is the security control that prevents a buggy/malicious
    client from activating an invalid vaga (defense-in-depth for D-12).
  </what-built>
  <how-to-verify>
    1. Apply each of the 4 migrations to the live project (isljnozzlvckrgjjbjwp) via EITHER Supabase
       SQL Editor (paste each file, run) OR Supabase MCP `execute_sql` (the Phase-6 path that bypasses
       42601 and reconciles by writing supabase_migrations version rows).
    2. After applying, run `supabase migration repair --status applied 20260607010001 20260607010002 20260607010003 20260607010004`.
    3. Confirm `supabase db push --linked` reports "Remote database is up to date".
    4. Run the smoke sections in 07-SQL-SMOKE-RUNBOOK.md: RPC idempotency (call twice, same opcao_ids),
       opcao_id generation (null→uuid + jsonb rewritten to [{id,texto}]), RLS deny (candidato/anon cannot
       write metadata; RPC raises 42501), publish_vaga guard (rejects status='ativa' when a D-12 condition fails).
    5. Confirm `enum_tag_opcao`, `pergunta_opcao_metadata`, and `vagas.testes_aplicaveis`/`pesos_avaliacao`
       now exist in the live schema.
  </how-to-verify>
  <resume-signal>Type "applied" once db push reports up-to-date and the 4 runbook smokes pass, or describe failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Regenerate database.types.ts</name>
  <files>database.types.ts</files>
  <read_first>
    - CLAUDE.md (§Commands — `npm run db:types`; database.types.ts is GENERATED, never edit by hand; lives at repo ROOT)
    - .planning/phases/07-configura-o-de-vaga-tags/07-RESEARCH.md (Runtime State Inventory — build artifacts)
  </read_first>
  <action>
    Run `npm run db:types` to regenerate `./database.types.ts` (repo ROOT, NOT src/types/) from the now-applied
    live schema. Do NOT hand-edit the file. After regeneration, verify the file contains the new enum
    `enum_tag_opcao`, the new table `pergunta_opcao_metadata`, and the new `vagas` columns `testes_aplicaveis`
    + `pesos_avaliacao`. This step depends on Task 3 having applied the schema — running it before apply
    produces stale types (the false-positive trap noted in the phase brief).
  </action>
  <verify>
    <automated>grep -q 'enum_tag_opcao' database.types.ts && grep -q 'pergunta_opcao_metadata' database.types.ts && grep -q 'testes_aplicaveis' database.types.ts && grep -q 'pesos_avaliacao' database.types.ts && echo TYPES_REGEN_OK</automated>
  </verify>
  <acceptance_criteria>
    - database.types.ts contains enum_tag_opcao, pergunta_opcao_metadata, testes_aplicaveis, pesos_avaliacao (source assertion via grep).
    - File was generated by `npm run db:types`, not hand-edited.
    - `npm run build` exits 0 with the regenerated types.
  </acceptance_criteria>
  <done>database.types.ts regenerated and contains the new enum/table/columns; build exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → RPC | Client calls `upsert_pergunta_opcoes_metadata` / `publish_vaga` directly; RLS does NOT apply to DEFINER bodies → in-body role check is the control. |
| candidato/anon → pergunta_opcao_metadata | Untrusted roles must not write config; RLS policy + RPC GRANT scope the boundary. |
| client → vagas.status transition | A client could try to UPDATE status='ativa' directly, skipping D-12; publish_vaga RPC is the server guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Elevation of Privilege | upsert_pergunta_opcoes_metadata RPC | mitigate | In-body `v_role NOT IN ('rh','administrador') → RAISE 42501`; REVOKE ALL FROM PUBLIC; GRANT EXECUTE TO authenticated only. |
| T-07-02-02 | Elevation of Privilege | pergunta_opcao_metadata table | mitigate | RLS FOR ALL policy `(select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador')` USING+WITH CHECK; candidato/anon denied. |
| T-07-02-03 | Tampering | vagas.status publish path | mitigate | publish_vaga RPC re-checks 3 D-12 conditions server-side before flipping status='ativa'; only rascunho→ativa (Pitfall 5). |
| T-07-02-04 | Tampering (data integrity) | jsonb↔table sync | mitigate | Single transactional DEFINER RPC (DELETE+re-INSERT + jsonb writeback) — no non-atomic client orchestration. |
| T-07-02-05 | Tampering | peso overflow/negative abuse | mitigate | DB CHECK `peso BETWEEN -999 AND 100` + (client Zod in Plan 03). |
| T-07-02-06 | Information Disclosure | search_path injection | mitigate | `SET search_path = ''` on both DEFINER fns; fully schema-qualified identifiers. |
| T-07-02-SC | Tampering | npm/migration apply | mitigate | No new packages; migration apply is a blocking-human checkpoint (Task 3), not autonomous. |
</threat_model>

<verification>
- DDL + RPC grep gates pass (Tasks 1-2).
- Task 3 human checkpoint: db push up-to-date + 4 runbook smokes pass.
- database.types.ts regen grep gates pass (Task 4).
- `npm run build` exit 0; lint baseline ≤ 296 (zero growth — migrations + generated types add no src/ tsc errors).
</verification>

<success_criteria>
- enum_tag_opcao + pergunta_opcao_metadata (opcao_id AND opcao_texto, peso CHECK, RLS) live in DB.
- vagas.testes_aplicaveis + vagas.pesos_avaliacao columns live.
- Both RPCs live; in-body role checks + publish gate verified via runbook.
- database.types.ts regenerated with the new schema.
- D-22 workaround honored (manual/MCP apply + migration repair + db push up-to-date).
</success_criteria>

<output>
Create `.planning/phases/07-configura-o-de-vaga-tags/07-02-SUMMARY.md` when done. Document the
F8/F10 join contract (D-14: match by opcao_id, opcao_texto as fallback/audit) so Phase 8 does not re-litigate.
</output>
</content>
</invoke>
