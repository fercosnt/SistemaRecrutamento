---
phase: 10-triagem-rh-com-ia-comparativo-etapa-2
plan: 05
type: execute
wave: 5
depends_on: ["10-04"]
files_modified:
  - src/features/triagem/services/triagemService.ts
  - src/features/triagem/hooks/useTriagemPanel.ts
  - src/features/triagem/components/SugestaoIABadge.tsx
  - src/features/triagem/components/TriagemTable.tsx
  - src/components/pages/VagaCandidatosRHPage.tsx
autonomous: true
requirements: [TRIAGEM-02]
user_setup: []

must_haves:
  truths:
    - "The panel read at triagemService uses an EXPLICIT column allowlist joining analise_candidato_vaga (score_match, pontos_fortes, gaps, flags, status) — NO select('*'), NO cpf/data_nascimento/email/celular ([[reference_select_star_leaks_pii]]) — TRIAGEM-02"
    - "The panel is server-side paginated 20/page, default score_match DESC nulls-last (pendente/falhou sort to end), filterable by etapa+status, name-searchable — TRIAGEM-02"
    - "/rh/vagas/:id/candidatos renders a dense shadcn table inside the RHLayout glass shell with score-band chips, multi-select gated 2-10, and a sticky Comparar(N) bar — replacing the glass cards"
    - "falhou rows show a visible 'Reprocessar análise' label that re-fires the analise dispatch; pendente rows show 'Analisando…'"
    - "The shared SugestaoIABadge ('Sugestão da IA — decisão é sempre humana') is rendered near the Score IA column (RNF-07a) and is reusable by Plans 11-15"
  artifacts:
    - path: "src/features/triagem/services/triagemService.ts"
      provides: "allowlist paginated read + reprocess"
      min_lines: 40
    - path: "src/features/triagem/components/TriagemTable.tsx"
      provides: "dense panel table with bands + compare gating"
      min_lines: 80
    - path: "src/features/triagem/components/SugestaoIABadge.tsx"
      provides: "shared RNF-07a guardrail badge"
  key_links:
    - from: "src/components/pages/VagaCandidatosRHPage.tsx"
      to: "useTriagemPanel"
      via: "data hook swap (cards → TriagemTable)"
      pattern: "useTriagemPanel"
    - from: "triagemService.ts"
      to: "analise_candidato_vaga"
      via: "allowlist select join"
      pattern: "analise:analise_candidato_vaga"
---

<objective>
The RH triage panel: a `triagemService` allowlist read (the [[reference_select_star_leaks_pii]] fix), `useTriagemPanel` query hook, the shared `SugestaoIABadge`, the dense `TriagemTable` (score bands + 2-10 compare gating + reprocess), and the `VagaCandidatosRHPage` rework that swaps glass cards for the table while keeping the RHLayout shell.

Purpose: TRIAGEM-02 — RH scans 30+ candidatos pre-ranked by score_match. The allowlist projection is the named anti-PII-leak control.
Output: triagem feature data layer + panel UI; the comparativo screen + PDF land in Plan 10-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-UI-SPEC.md
@.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-PATTERNS.md

<interfaces>
<!-- Exact identifiers — do not re-explore. -->

candidaturasService.ts:1145-1230 listCandidaturasByVaga — the select('*') hazard to REPLACE:
  current: .select(`*, candidato:candidatos(id,nome_completo,email,celular,data_nascimento,cpf,cidade,estado,created_at)`)
  REPLACE with allowlist: .select(`id, status, etapa_atual, created_at, curriculo_nome_original,
    candidato:candidatos ( id, nome_completo ),
    analise:analise_candidato_vaga ( score_match, pontos_fortes, gaps, flags, status )`, {count:'exact'})
    .eq('vaga_id', vagaId).is('deleted_at', null)
  pagination: from=(page-1)*limit, to=from+limit-1, .range(from,to); 20/page
  order: score_match DESC nulls-last on the joined analise; pendente/falhou (null score) sort to end
  filters: .eq('status',...) / .eq('etapa_atual',...); name search retained
  error class precedent: CandidaturasServiceError (:1219-1225)

useCandidaturas.ts:40-117 candidaturasKeys hierarchy — mirror as triagemKeys:
  triagemKeys = { all:['triagem'], panel:(vagaId,filters?,orderBy?,pagination?)=>[...all,'panel',vagaId,{filters,orderBy,pagination}] }
  useQuery shape: queryKey, queryFn, enabled:!!vagaId, staleTime 5min, retry 2
PaginationParams { page, limit } typed in vagasTypes

VagaCandidatosRHPage.tsx (REWORK — keep shell):
  KEEP lines 13-45 (RHLayout, Glass/GlassButton, lucide imports — ADD Sparkles/RefreshCw/Download/AlertTriangle), useParams/useNavigate, format/ptBR
  KEEP lines 50-69 STATUS_COLORS/STATUS_LABELS (reuse for Status column badge)
  SWAP the glass-cards list for <TriagemTable> + swap data hook to useTriagemPanel

UI-SPEC §A score bands (thresholds): 70-100 verde, 40-69 amarelo, 0-39 vermelho, null sem-análise "—"
  band chip = number + color, never bare dot; flags = neutral badges (no gating color)
UI-SPEC §A compare bar: "N de 10 selecionados" + "Comparar (N)" accent CTA; disabled <2 (tooltip "Selecione ao menos 2 candidatos para comparar."), checkboxes disabled at 10 (tooltip "Máximo de 10 candidatos por comparativo.")
UI-SPEC §C SugestaoIABadge: Badge + Sparkles, border-[#35BFAD]/40 bg-[#35BFAD]/10 text-white, "Sugestão da IA — decisão é sempre humana", 12px semibold; compact + full variants
UI-SPEC §A falhou: visible "Reprocessar análise" text label (not tooltip-only) + RefreshCw; pendente: skeleton + "Analisando…"

primitives (all in src/components/ui/): table, badge, button, checkbox, pagination, tooltip, skeleton, select, input + glass

Reprocess routing note: the analise EF is Bearer-self-auth (--no-verify-jwt), so a client functions.invoke with the user JWT is rejected. Implement reprocessarAnalise via a SECURITY DEFINER RPC `reprocessar_analise(p_candidatura_id uuid)` that re-fires the SAME pg_net dispatch block as trg_candidatura_analise (reuse the trigger body) with a fresh idempotency. Author it as a tiny no-wrapper migration (20260610000003_reprocessar_rpc.sql) in Task 1; it is applied in this plan's verify via db push (or noted for the next PROD-apply window if db push is human-gated — call supabase.rpc('reprocessar_analise',{p_candidatura_id}) from the service).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: triagemService allowlist read + useTriagemPanel + reprocess RPC</name>
  <files>src/features/triagem/services/triagemService.ts, src/features/triagem/hooks/useTriagemPanel.ts, supabase/migrations/20260610000003_reprocessar_rpc.sql</files>
  <read_first>
    - src/features/vagas/services/candidaturasService.ts:1145-1230 (listByVaga select('*') hazard + CandidaturasServiceError + .range() math)
    - src/features/vagas/hooks/useCandidaturas.ts:40-117 (candidaturasKeys + useQuery shape)
    - src/features/triagem/services/__tests__/triagemService.test.ts (RED contract from 10-01 — allowlist + sort/paginate assertions)
    - supabase/migrations/20260610000002_analise_trigger.sql (the pg_net dispatch block to reuse in the reprocess RPC)
    - database.types.ts (regenerated in 10-04 — analise_candidato_vaga shape)
  </read_first>
  <behavior>
    - listTriagemByVaga select() contains NO `*`, NO cpf/data_nascimento/email/celular; joins analise:analise_candidato_vaga ( score_match, pontos_fortes, gaps, flags, status )
    - default order score_match DESC nulls-last; pendente/falhou rows (null score) sort to end
    - 20/page server-side .range((page-1)*20, ...); filters etapa+status; name search
    - reprocessarAnalise(candidaturaId) calls supabase.rpc('reprocessar_analise', { p_candidatura_id }) which re-fires the analise dispatch with a fresh idempotency
  </behavior>
  <action>
    Create `src/features/triagem/services/triagemService.ts` with `listTriagemByVaga(vagaId, filters?, orderBy?, pagination={page:1,limit:20})` returning the allowlist projection above (REPLACE the select('*') hazard — explicit columns, analise join, NO cpf/data_nascimento/email/celular). Apply `.eq('vaga_id').is('deleted_at',null)`, status/etapa filters, name search, score_match DESC nulls-last ordering on the joined analise, `.range(from,to)`. Throw a `TriagemServiceError` class on db error (mirror CandidaturasServiceError). Add `reprocessarAnalise(candidaturaId)` calling `supabase.rpc('reprocessar_analise', { p_candidatura_id: candidaturaId })`.

    Author `supabase/migrations/20260610000003_reprocessar_rpc.sql` (NO BEGIN/COMMIT wrapper — D-22): a SECURITY DEFINER function `public.reprocessar_analise(p_candidatura_id uuid)` that reads the candidatura's vaga_id and re-fires the SAME `net.http_post(.../analise-candidato-individual, Bearer edge_invoke_key, body {candidatura_id, vaga_id})` block as trg_candidatura_analise (graceful Vault skip). GRANT EXECUTE to authenticated (RH/admin reach it through RLS-gated panel; the EF itself re-validates). This migration applies in the next PROD-apply window (note in summary if db push is gated).

    Create `src/features/triagem/hooks/useTriagemPanel.ts` exporting `triagemKeys` (mirror candidaturasKeys) + useTriagemPanel (useQuery staleTime 5min, retry 2, enabled:!!vagaId). Flip the 10-01 triagemService RED test GREEN. Commit via `git -c core.hooksPath=/dev/null`.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && npm run test:run -- triagemService 2>&1 | grep -qiE "pass|✓" && ! grep -qE "select\('\*'\)|data_nascimento|cpf|celular" src/features/triagem/services/triagemService.ts && grep -q "analise:analise_candidato_vaga" src/features/triagem/services/triagemService.ts && grep -q "reprocessar_analise" supabase/migrations/20260610000003_reprocessar_rpc.sql</automated>
  </verify>
  <acceptance_criteria>
    - 10-01 triagemService test GREEN
    - `grep -E "select\('\*'\)|cpf|data_nascimento|email|celular" src/features/triagem/services/triagemService.ts` returns nothing (allowlist enforced)
    - select() joins analise:analise_candidato_vaga; order score_match DESC nulls-last; .range() 20/page; reprocessarAnalise via supabase.rpc
    - reprocessar_analise RPC authored no-wrapper, reusing the trigger pg_net block; triagemKeys + useTriagemPanel present
  </acceptance_criteria>
  <done>Allowlist paginated read + reprocess RPC + query hook; no PII columns; service test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SugestaoIABadge + TriagemTable + VagaCandidatosRHPage rework</name>
  <files>src/features/triagem/components/SugestaoIABadge.tsx, src/features/triagem/components/TriagemTable.tsx, src/components/pages/VagaCandidatosRHPage.tsx</files>
  <read_first>
    - src/components/pages/VagaCandidatosRHPage.tsx (full — shell lines 13-45, STATUS_COLORS/STATUS_LABELS 50-69, the cards list to replace)
    - src/components/ui/table.tsx + badge.tsx + checkbox.tsx + pagination.tsx + tooltip.tsx + skeleton.tsx (primitives — first RH use of table.tsx)
    - .planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-UI-SPEC.md (§A columns/bands/compare-bar/states, §C badge contract, Copywriting Contract)
    - src/features/triagem/components/__tests__/TriagemTable.test.tsx (RED contract from 10-01 — bands + gating + badge + reprocess)
  </read_first>
  <behavior>
    - score 85→verde chip, 55→amarelo, 20→vermelho, null→"—" sem-análise band (UI-SPEC thresholds 70/40)
    - Comparar disabled <2 selected; enabled 2-10; checkboxes disabled past 10 (with the two tooltips)
    - SugestaoIABadge full-text rendered near Score IA header (RNF-07a)
    - falhou row → visible "Reprocessar análise" label wired to reprocessarAnalise; pendente → skeleton + "Analisando…"
    - flags render as neutral badges (no red/green gating color)
  </behavior>
  <action>
    Create `SugestaoIABadge.tsx` (named export per project convention) — Badge + Sparkles, accent border/bg tint, full + compact variants, the exact RNF-07a copy. Create `TriagemTable.tsx` consuming the UI-SPEC §A column set (Checkbox, Candidato avatar+nome, Score IA band chip + Sparkles micro-icon, Top fortes ≤2, Top gaps ≤2, Etapa badge, Status badge reusing STATUS_LABELS/COLORS, Aplicou em, Ações Ver Perfil): score-band helper (70/40 thresholds → green/yellow/red/sem-análise tokens from UI-SPEC §Color), the sticky compare bar ("N de 10 selecionados" + "Comparar (N)" accent CTA + "Limpar seleção"), 2-10 gating with the two tooltips, pendente skeleton + falhou "Reprocessar análise" wired to `reprocessarAnalise`, and `pagination.tsx` 20/page. Emit the selected ids upward via a prop callback `onComparar(ids)` (the comparativo navigation lands in 10-06). Render SugestaoIABadge (full) near the Score column header.

    Rework `VagaCandidatosRHPage.tsx`: KEEP the RHLayout/glass shell + header + filter bar (etapa Select + status Select + name Input), ADD the lucide imports (Sparkles/RefreshCw/Download/AlertTriangle), SWAP the data hook to `useTriagemPanel` and the glass-cards list for `<TriagemTable>`. Keep the empty/loading/error states using the UI-SPEC Copywriting strings. Flip the 10-01 TriagemTable RED test GREEN. Keep tsc baseline 293 flat (annotate any vendored-primitive callback params explicitly to avoid implicit-any growth). Commit via `git -c core.hooksPath=/dev/null`.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && npm run test:run -- TriagemTable 2>&1 | grep -qiE "pass|✓" && grep -q "Sugestão da IA — decisão é sempre humana" src/features/triagem/components/SugestaoIABadge.tsx && grep -q "useTriagemPanel" src/components/pages/VagaCandidatosRHPage.tsx && npm run build 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - 10-01 TriagemTable test GREEN (bands by score + 2-10 gating + SugestaoIABadge + Reprocessar label asserted)
    - SugestaoIABadge renders the exact RNF-07a copy; flags are neutral (no gating color)
    - VagaCandidatosRHPage uses useTriagemPanel + <TriagemTable> (cards removed); RHLayout shell preserved
    - `npm run build` exits 0; tsc baseline not grown beyond 293
  </acceptance_criteria>
  <done>Shared badge + dense panel table + page rework; cards swapped; build green; component test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel read → candidato PII | RH grid must not pull cpf/data_nascimento/email/celular |
| score display → auto-action | RNF-07a: score/flags must never gate a status change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-16 | Information Disclosure | triagemService select | mitigate | explicit allowlist (no `*`/cpf/data_nascimento/email/celular); test-asserted ([[reference_select_star_leaks_pii]]) |
| T-10-17 | — (compliance RNF-07a) | TriagemTable score band | mitigate | bands are display-only; flags neutral badges; no reject-on-score path in the panel |
| T-10-18 | Elevation of Privilege | reprocessar_analise RPC | mitigate | SECURITY DEFINER re-fires the same Vault-Bearer dispatch; EF re-validates Bearer; panel is RLS-gated to RH/admin |
| T-10-SC | Tampering | no new npm installs | accept | jspdf already installed in 10-01; this plan adds no package surface |
</threat_model>

<verification>
- triagemService + TriagemTable tests GREEN
- No PII columns in the panel select; SugestaoIABadge copy present; build exit 0; tsc ≤293
</verification>

<success_criteria>
RH opens /rh/vagas/:id/candidatos and sees a dense, server-paginated, score-ranked table with the AI-suggestion guardrail, multi-select gated 2-10, and reprocess on failure — all without leaking candidate PII.
</success_criteria>

<output>
Create `.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-05-SUMMARY.md` when done
</output>
