---
phase: 10-triagem-rh-com-ia-comparativo-etapa-2
plan: 04
type: execute
wave: 4
depends_on: ["10-02", "10-03"]
files_modified:
  - database.types.ts
autonomous: false
requirements: [TRIAGEM-01, TRIAGEM-03]
user_setup: []

must_haves:
  truths:
    - "Both Phase-10 migrations are applied to live PROD (analise_candidato_vaga + comparativo_solicitado + RLS + trg_candidatura_analise) and supabase_migrations version rows reconciled — db push reports up to date"
    - "The cv_job_match AND comparative_ranking prompts are is_active=true in PROD (Pitfall 2 closed) so loadPrompt resolves an active row"
    - "analise-candidato-individual is deployed WITH --no-verify-jwt; comparativo-candidatos is deployed WITHOUT it (JWT-ON)"
    - "database.types.ts at repo ROOT is regenerated and exposes analise_candidato_vaga + comparativo_solicitado"
    - "The 5 SQL smokes (SMOKE-1..5) from 10-SQL-SMOKES.md PASS against live PROD — traceability for TRIAGEM-01 RLS + TRIAGEM-03 audit"
  artifacts:
    - path: "database.types.ts"
      provides: "regenerated types exposing the 2 new tables"
      contains: "analise_candidato_vaga"
  key_links:
    - from: "PROD prompt_versions"
      to: "loadPrompt resolution"
      via: "is_active=true flip"
      pattern: "cv_job_match"
---

<objective>
[BLOCKING — non-autonomous] Apply the Phase-10 schema + dispatch to live PROD: push both migrations (no-wrapper db push, MCP execute_sql fallback on 42601), flip cv_job_match + comparative_ranking to is_active=true, deploy both EFs with the correct --no-verify-jwt flags, regenerate database.types.ts, and run the 5 SQL smokes. The build/type checks pass WITHOUT this push (types come from config) — so this task is mandatory or the phase verifies false-positive.

Purpose: Make TRIAGEM-01 + TRIAGEM-03 real in PROD. Without the is_active flip both EFs return prompt_not_configured; without the migration apply the panel reads a non-existent table.
Output: live PROD schema + active prompts + deployed EFs + regenerated types + 5 smokes PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-SQL-SMOKES.md
@.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-RESEARCH.md
@.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-PATTERNS.md

<interfaces>
<!-- Apply identifiers — do not re-derive. -->

Migrations to apply (authored in 10-02): 20260610000001_analise_tables.sql, 20260610000002_analise_trigger.sql
Apply path (D-22): `supabase db push --linked` if no-wrapper authoring holds (Phase 8 precedent pushed clean). On SQLSTATE 42601 → Supabase MCP execute_sql (bypasses 42601) + reconcile by writing supabase_migrations version rows + `supabase migration repair --status applied <version>`; confirm `db push --linked` says up to date.

Prompt activation (Pitfall 2): in PROD run
  UPDATE prompt_versions SET is_active=true WHERE call_type IN ('cv_job_match','comparative_ranking') AND is_canary=false;
  (seeded is_active=false in 20260609000004_prompt_library_seed.sql). Apply via Supabase MCP execute_sql.

EF deploy:
  supabase functions deploy analise-candidato-individual --no-verify-jwt
  supabase functions deploy comparativo-candidatos          (NO --no-verify-jwt → JWT verify ON)

Vault secrets to confirm exist (Phase 9 P07, REUSED — none to create):
  select name from vault.decrypted_secrets;   -- expect project_url, edge_invoke_key
EF secrets to confirm:
  supabase secrets list   -- expect ANTHROPIC_API_KEY, OPENAI_API_KEY

Types regen: `npm run db:types` (or MCP generate_typescript_types) → database.types.ts at repo ROOT.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] confirm Vault + EF secrets present in PROD</name>
  <what-built>The 10-02 trigger relies on Vault secrets project_url + edge_invoke_key (Phase 9 P07); both EFs need ANTHROPIC_API_KEY + OPENAI_API_KEY. This task confirms they exist before relying on the async dispatch.</what-built>
  <read_first>
    - .planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-RESEARCH.md (Runtime State Inventory — A2/A6 assumptions)
  </read_first>
  <how-to-verify>
    1. Via Supabase MCP execute_sql: `select name from vault.decrypted_secrets;` → confirm `project_url` and `edge_invoke_key` are present.
    2. `supabase secrets list` → confirm `ANTHROPIC_API_KEY` and `OPENAI_API_KEY` are set (deployed in Phase 9). If absent, set them before deploying the EFs (`supabase secrets set ...`).
  </how-to-verify>
  <acceptance_criteria>
    - vault.decrypted_secrets lists both project_url + edge_invoke_key
    - supabase secrets list shows ANTHROPIC_API_KEY + OPENAI_API_KEY
  </acceptance_criteria>
  <resume-signal>Type "secrets confirmed" or list which are missing.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] apply migrations + flip prompts active + deploy EFs + db:types + 5 smokes</name>
  <files>database.types.ts</files>
  <read_first>
    - supabase/migrations/20260610000001_analise_tables.sql, supabase/migrations/20260610000002_analise_trigger.sql (the files to apply)
    - .planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-SQL-SMOKES.md (SMOKE-1..5 to run)
    - CLAUDE.md §Migrations (42601 workaround) + MEMORY: MCP execute_sql bypasses 42601 + reconciles version rows
  </read_first>
  <what-built>Live PROD apply of the Phase-10 schema + dispatch. Touches live Supabase production infra — non-autonomous by policy.</what-built>
  <how-to-verify>
    1. Apply migrations: `supabase db push --linked`. If SQLSTATE 42601 fires on the trigger migration → apply via Supabase MCP execute_sql (paste each file's SQL), then reconcile supabase_migrations version rows + `supabase migration repair --status applied 20260610000001 20260610000002`; confirm `supabase db push --linked` reports "Remote database is up to date".
    2. Flip prompts active via MCP execute_sql: `UPDATE prompt_versions SET is_active=true WHERE call_type IN ('cv_job_match','comparative_ranking') AND is_canary=false;` — confirm 2 rows affected.
    3. Deploy EFs: `supabase functions deploy analise-candidato-individual --no-verify-jwt` and `supabase functions deploy comparativo-candidatos` (NO flag).
    4. Regenerate types: `npm run db:types` → confirm database.types.ts (repo ROOT) now contains `analise_candidato_vaga` + `comparativo_solicitado`.
    5. Run SMOKE-1..5 from 10-SQL-SMOKES.md via MCP execute_sql (fixture + set_config('request.jwt.claims',...) for the RLS 3-role matrix). All 5 PASS. Capture results in the summary.
    Commit database.types.ts via `git -c core.hooksPath=/dev/null`.
  </what-built>
  <acceptance_criteria>
    - `supabase db push --linked` reports up to date (both migrations applied + reconciled)
    - is_active flip affected exactly 2 rows (cv_job_match + comparative_ranking)
    - both EFs deployed (analise with --no-verify-jwt, comparativo without); a `curl` to comparativo without auth returns 401 (JWT-ON evidence)
    - `grep -c analise_candidato_vaga database.types.ts` ≥ 1
    - SMOKE-1 (survivor-only dispatch), SMOKE-2 (candidato DENY), SMOKE-3 (RH/admin read), SMOKE-4 (upsert idempotency one row), SMOKE-5 (comparativo audit row) all PASS
  </acceptance_criteria>
  <resume-signal>Type "applied — 5 smokes PASS" or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migration → live PROD | high-blast-radius apply; non-autonomous |
| analise EF deploy flag | --no-verify-jwt MUST pair with Bearer self-auth or it's an open write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Spoofing | analise EF --no-verify-jwt | mitigate | the EF (10-03) self-validates the Vault Bearer; deploying --no-verify-jwt without that would be an open write path — 10-03 acceptance gates this |
| T-10-14 | Tampering | prompt is_active flip | mitigate | scoped UPDATE WHERE call_type IN (...) AND is_canary=false; verified 2 rows affected |
| T-10-15 | Information Disclosure | RLS in PROD | mitigate | SMOKE-2/3 prove candidato DENY + RH/admin read live before sign-off |
| T-10-SC | Tampering | live SQL apply | accept | human-gated blocking checkpoint; MCP execute_sql is the sanctioned path (never auto-approvable) |
</threat_model>

<verification>
- db push up to date; 2 prompts active; both EFs deployed with correct flags; types regenerated; SMOKE-1..5 PASS
</verification>

<success_criteria>
PROD carries the analysis tables + survivor-only trigger + active prompts + both deployed EFs; types regenerated; all 5 SQL smokes green. TRIAGEM-01/03 are live-capable.
</success_criteria>

<output>
Create `.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-04-SUMMARY.md` when done
</output>
