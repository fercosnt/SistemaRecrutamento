---
phase: 14-entrevistas-com-ia-companion-etapas-4-5
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - supabase/migrations/20260624000001_entrevista_cognitivo_tables.sql
  - supabase/migrations/20260624000002_salvar_avaliacao_entrevista_rpc.sql
  - supabase/migrations/20260624000003_pontuar_cognitivo_rpc.sql
  - supabase/migrations/20260624000004_avancar_etapa_flag_guard.sql
  - supabase/functions/gerar-guia-entrevista/index.ts
  - supabase/functions/avaliar-transcricao-entrevista/index.ts
autonomous: true
requirements: [ENTREV-01, ENTREV-03, ENTREV-04, ENTREV-05, ENTREV-02]
must_haves:
  truths:
    - "Both interview EFs are RH-authorize (role from usuarios_rh, NOT JWT claims) + two-client + STATIC npm: imports + injected zod helpers — the .join bug is avoided (per reference_ef_npm_join_import_bug)"
    - "The language/accent flag block is enforced INSIDE avancar_etapa() (server-authoritative funil invariant), not only in the UI (ENTREV-03, resolved Q2)"
    - "Cognitive scoring is a SECURITY DEFINER RPC pontuar_cognitivo (no LLM, clones pontuar_sjt), tipo='cognitivo' + subtipo='raciocinio_logico', NO ALTER TYPE — never auto-rejects (ENTREV-05, resolved Q1+Q3)"
    - "Every new candidate-adjacent table is candidate-DENY RLS; no scoring path writes candidaturas (RNF-07a)"
  artifacts:
    - path: "supabase/migrations/20260624000001_entrevista_cognitivo_tables.sql"
      provides: "entrevista_guias + entrevista_analises + cognitivo_itens + cognitivo_respostas + candidate-DENY/etapa-gated RLS"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "supabase/migrations/20260624000003_pontuar_cognitivo_rpc.sql"
      provides: "Deterministic server-authoritative cognitive scoring RPC (never auto-reject)"
      contains: "SECURITY DEFINER"
    - path: "supabase/migrations/20260624000004_avancar_etapa_flag_guard.sql"
      provides: "avancar_etapa() flag-block guard (blocks advance until revisao_confirmada)"
    - path: "supabase/functions/gerar-guia-entrevista/index.ts"
      provides: "STAR/PEI guide EF (online + presencial branch, weak-dim post-validation)"
      exports: ["handler"]
    - path: "supabase/functions/avaliar-transcricao-entrevista/index.ts"
      provides: "Transcript BARS EF (untrusted text → callAi, derived flag, never-absent persist)"
      exports: ["handler"]
  key_links:
    - from: "supabase/functions/avaliar-transcricao-entrevista/index.ts"
      to: "supabase/functions/avaliar-transcricao-entrevista/_local/derive-flags.ts"
      via: "server-side flag derivation"
      pattern: "deriveLanguageAccentFlag"
    - from: "supabase/migrations/20260624000004_avancar_etapa_flag_guard.sql"
      to: "entrevista_analises.revisao_confirmada_em"
      via: "guard reads the human-confirmed state"
      pattern: "revisao_confirmada"
---

<objective>
Author (NOT yet apply) the Phase-14 schema layer + the 2 interview Edge Functions: the new interview/cognitive tables with candidate-DENY/etapa-gated RLS, the two SECURITY DEFINER RPCs (`salvar_avaliacao_entrevista` human-override write, `pontuar_cognitivo` deterministic scoring), the `avancar_etapa()` flag-block guard, and the two RH-invoked EFs (`gerar-guia-entrevista`, `avaliar-transcricao-entrevista`) cloned verbatim from the PROD-green `avaliar-redacao-cultural` / `comparativo-candidatos` skeletons.

Purpose: This is the server core of the phase. Everything is a clone of a PROD-green Phase 10/11/13 pattern — the value is correct wiring (the `.join` static-import fix, role-from-`usuarios_rh`, the server-derived language/accent flag, the funil-state flag block, the no-LLM cognitive RPC). Migrations are authored no-wrapper (D-22) and applied LIVE in the [BLOCKING] wave (14-04) — they are NOT applied here.

Output: 4 migrations (tables+RLS, override RPC, cognitive RPC, flag guard) + 2 EF index.ts files wired to `callAi` + the `_local/` modules from 14-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-RESEARCH.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-PATTERNS.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-CONTEXT.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-01-SUMMARY.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-02-SUMMARY.md

<interfaces>
<!-- Authored-not-applied migrations: no-wrapper (D-22), apply via MCP apply_migration in 14-04. -->
From 14-01: GerarGuiaBodySchema, AvaliarTranscricaoBodySchema (.strict, no score), SubmitCognitivoBodySchema, BandaCognitivaEnum; scoreRaciocinio(rawResponses, gabarito); deriveLanguageAccentFlag(parsed); checkWeakDimCoverage(guide, weakDims).
From 14-02: ItemRaciocinio, SEED_ITENS_RACIOCINIO, buildGabarito.

Live enum tipo_score has 'entrevista' + 'cognitivo' ALREADY (NO ALTER TYPE — adding inside a txn fails anyway).
avancar_etapa() is a BEFORE-UPDATE trigger function (RETURNS trigger, SECURITY DEFINER, search_path='') registered as candidaturas_avancar_etapa_trg; it is NOT a callable RPC — the guard is a CREATE OR REPLACE of that trigger function.
EF auth analog: comparativo-candidatos/index.ts:114-190 — getUser (anon+Authorization) → role from usuarios_rh (select role .eq('user_id',user.id).eq('ativo',true).is('deleted_at',null)) → map recrutador→rh → vagas.created_by ownership for role='rh'.
Output schemas (consumed by the SDK helper) import zod as npm:zod@3.25.76/v4 (Pitfall 3); body schemas use plain v3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations — interview/cognitive tables + RLS + the 2 RPCs + the avancar_etapa flag guard</name>
  <files>supabase/migrations/20260624000001_entrevista_cognitivo_tables.sql, supabase/migrations/20260624000002_salvar_avaliacao_entrevista_rpc.sql, supabase/migrations/20260624000003_pontuar_cognitivo_rpc.sql, supabase/migrations/20260624000004_avancar_etapa_flag_guard.sql</files>
  <read_first>
    - supabase/migrations/20260611000001_scores_candidato.sql (candidate-DENY RLS idiom + forward-declared enum + no-BEGIN/COMMIT note; lines 35-49 enum, 80-90 RLS)
    - supabase/migrations/20260611000003_respostas_avaliacao.sql (etapa-gated USING + WITH CHECK back-lock RLS for the candidate-writable cognitivo_respostas)
    - supabase/migrations/20260611000004_pontuar_sjt_rpc.sql (the deterministic-scoring DEFINER RPC to clone for pontuar_cognitivo — authorize inside DEFINER, threshold-never-rejects, REVOKE/GRANT)
    - supabase/migrations/20260623100004_salvar_revisao_redacao_rpc.sql (the human-override DEFINER RPC to clone for salvar_avaliacao_entrevista — role + own-vaga guard, never touches candidaturas)
    - supabase/migrations/20260607000005_avancar_etapa_trigger.sql (the BEFORE-UPDATE trigger function to CREATE OR REPLACE with the flag guard; note SECURITY DEFINER + search_path='' + the auth.uid() GUC proof)
  </read_first>
  <action>Author 4 no-wrapper migrations (NO BEGIN;...COMMIT; — D-22, apply via MCP in 14-04). (1) `20260624000001_entrevista_cognitivo_tables.sql`: create `entrevista_guias` (id, candidatura_id FK, tipo text in ('online','presencial'), guia jsonb, prompt_version, created_at), `entrevista_analises` (id, candidatura_id FK, competencias jsonb, citacoes jsonb, bias_flags jsonb, bloqueio_avanco boolean default false, revisao_confirmada_em timestamptz null, revisada_por uuid null, status_analise text default 'pendente_humano', prompt_version, created_at), `cognitivo_itens` (id, secao text in ('matriz','letra_numero'), enunciado text, alternativas jsonb, ordem int — NO gabarito column exposed to a candidate-readable policy; store gabarito_idx in a server-only column), `cognitivo_respostas` (id, candidatura_id FK, raw_responses jsonb, shuffle_seed text, completion_time_seconds int, proctoring jsonb, created_at). Add `aplica_cognitivo boolean NOT NULL DEFAULT false` to `vagas` and the manual interview datetime column `entrevista_agendada_em timestamptz null` to support ENTREV-02. ENABLE RLS on all 4 new tables; candidate-DENY SELECT (RH/admin only via jwt app_metadata.role IN ('rh','administrador')) on guias/analises; `cognitivo_itens` readable by authenticated for the prova but NO gabarito column in that projection — keep gabarito_idx in a separate server-only table or a non-selectable column accessed only by the DEFINER RPC; `cognitivo_respostas` etapa-gated USING + WITH CHECK back-lock (clone respostas_avaliacao). Namespace indexes `idx_entrevista_guias_candidatura`, `idx_entrevista_analises_candidatura`, `idx_cognitivo_itens_secao`, `idx_cognitivo_respostas_candidatura` (grep existing idx_* first — Pitfall 7; legacy entrevistas_*/raven idx + Phase-13 idx_perguntas_cargo). (2) `20260624000002_salvar_avaliacao_entrevista_rpc.sql`: clone `salvar_revisao_redacao` — `salvar_avaliacao_entrevista(p_candidatura_id uuid, p_scores_humanos jsonb, p_notas text) RETURNS jsonb` SECURITY DEFINER search_path='', resolve the candidatura's vaga owner, guard role IN ('rh','administrador') + role='rh' must own vaga (vagas.created_by=auth.uid()); write notas_humanas/scores to entrevista_analises; NEVER writes candidaturas; REVOKE FROM PUBLIC + GRANT EXECUTE TO authenticated. (3) `20260624000003_pontuar_cognitivo_rpc.sql`: clone `pontuar_sjt` — `pontuar_cognitivo(p_candidatura_id uuid, p_respostas jsonb) RETURNS jsonb` SECURITY DEFINER, authorize caller owns candidatura via candidatos.user_id=auth.uid() AND correct etapa, recompute server-side from p_respostas using the gabarito (Σ 0/1 per item + section subtotals + 5-faixa banding — the SAME cutoffs as scoring.ts §8.2; recompute in SQL or read from a items table), UPSERT scores_candidato (tipo='cognitivo', subtipo='raciocinio_logico', score, score_max, status='sucesso' informational-only, metadata { banda, secoes, raw_responses, flags }) ON CONFLICT (candidatura_id,tipo,subtipo,pergunta_id), RETURN NEUTRAL jsonb('ok',true) — candidate never sees score/band; NO ALTER TYPE; REVOKE/GRANT. (4) `20260624000004_avancar_etapa_flag_guard.sql`: CREATE OR REPLACE `avancar_etapa()` (keep ALL existing transition+audit logic verbatim from 20260607000005) and ADD a guard: when the NEW etapa advances PAST entrevista_online and the candidatura has an entrevista_analises row with bloqueio_avanco=true AND revisao_confirmada_em IS NULL, RAISE EXCEPTION 'bloqueio: revise a bandeira de linguagem/sotaque antes de avancar' USING ERRCODE='check_violation'. Re-attach the trigger if needed.</action>
  <verify>
    <automated>for f in 20260624000001_entrevista_cognitivo_tables 20260624000002_salvar_avaliacao_entrevista_rpc 20260624000003_pontuar_cognitivo_rpc 20260624000004_avancar_etapa_flag_guard; do test -f "supabase/migrations/$f.sql" || exit 1; done; ! grep -lE "^\s*BEGIN;|^\s*COMMIT;" supabase/migrations/2026062400000*.sql; grep -lE "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260624000001_entrevista_cognitivo_tables.sql && grep -lE "SECURITY DEFINER" supabase/migrations/20260624000003_pontuar_cognitivo_rpc.sql</automated>
  </verify>
  <acceptance_criteria>
    - All 4 migration files exist; NONE contains a top-level `BEGIN;`/`COMMIT;` wrapper (D-22)
    - `grep -c "ALTER TYPE" supabase/migrations/2026062400000*.sql` returns 0 (no ALTER TYPE — 'entrevista'/'cognitivo' already exist)
    - `entrevista_cognitivo_tables.sql` ENABLEs RLS on all 4 new tables and has a candidate-DENY SELECT policy on entrevista_guias + entrevista_analises
    - `pontuar_cognitivo_rpc.sql` is SECURITY DEFINER, uses tipo='cognitivo' + subtipo='raciocinio_logico', returns a neutral payload, and has REVOKE FROM PUBLIC + GRANT EXECUTE TO authenticated
    - `avancar_etapa_flag_guard.sql` references `revisao_confirmada` and `bloqueio_avanco` and RAISEs when unresolved
    - `salvar_avaliacao_entrevista_rpc.sql` guards role + vagas.created_by ownership and never writes candidaturas
    - New index names do not collide with any existing `idx_*` (grep cross-check)
  </acceptance_criteria>
  <done>4 no-wrapper migrations authored: tables+RLS, the human-override RPC, the cognitive scoring RPC, and the avancar_etapa flag guard — all clones of PROD-green analogs.</done>
</task>

<task type="auto">
  <name>Task 2: EF gerar-guia-entrevista (online + presencial branch + weak-dim post-validation)</name>
  <files>supabase/functions/gerar-guia-entrevista/index.ts</files>
  <read_first>
    - supabase/functions/comparativo-candidatos/index.ts (RH-authorize EF — getUser, role from usuarios_rh, vagas.created_by ownership, OPTIONS-before-auth, two-client Deno.serve wiring; lines 36-50 imports, 114-190 authz, 308-352 wiring)
    - supabase/functions/avaliar-redacao-cultural/index.ts (callAi + loadPrompt + resolvedPromptFromLoaded shape; static npm: imports + injected zod helpers; lines 54-60, 225-273, 422-430)
    - supabase/functions/gerar-guia-entrevista/_local/weak-dim-coverage.ts (the checkWeakDimCoverage module from 14-01 — Pitfall 4 post-validation)
    - docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts (InterviewGuideSchema — the OUTPUT contract; import the OUTPUT-schema zod as npm:zod@3.25.76/v4 per Pitfall 3)
  </read_first>
  <action>Create `gerar-guia-entrevista/index.ts` cloning the comparativo-candidatos RH-authorize skeleton + the avaliar-redacao-cultural callAi shape. STATIC `npm:` imports at module top (Anthropic + zodOutputFormat/helpers/zod, OpenAI + zodResponseFormat/helpers/zod) — the `.join` fix (Pitfall 1/2); inject both builders in the Deno.serve wiring. Validate the body with `GerarGuiaBodySchema` (from 14-01). Auth: getUser (supabaseUser) → role from usuarios_rh (NOT JWT claims) → map recrutador→rh → role='rh' must own vagas.created_by. Resolve `loadPrompt('interview_guide', supabaseAdmin)`; read the prior scorecard from scores_candidato (tipo='entrevista' for the `presencial` gap branch — compute weak dims score<4; for `online` use the prior etapa scorecards, weak dims score<3). Branch by `body.tipo`: build the input block with the weak dims + per-vaga BARS competencies (the {{BARS_RUBRIC_PER_COMPETENCY}} block — resolved Q4: derive from the vaga's critical competencies). callAi with the `InterviewGuideSchema` OUTPUT schema (imported as npm:zod@3.25.76/v4). After parse, run `checkWeakDimCoverage(guide, weakDims)` — if uncovered, one bounded re-prompt emphasizing the missing dims; if STILL uncovered, persist the guide but flag for human (never silently pass an incomplete guide — Pitfall 4). Persist the guia to `entrevista_guias` (tipo, guia jsonb, prompt_version). NEVER writes candidaturas. OPTIONS returns before the authHeader 401 guard. Redacted logs only (ids/counts/status — never the guide content/nome).</action>
  <verify>
    <automated>deno check supabase/functions/gerar-guia-entrevista/index.ts && grep -cE "from \"npm:@anthropic-ai/sdk@0.102.0\"" supabase/functions/gerar-guia-entrevista/index.ts && grep -cE "checkWeakDimCoverage" supabase/functions/gerar-guia-entrevista/index.ts && ! grep -E "\.join\(\"npm:" supabase/functions/gerar-guia-entrevista/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `deno check` passes; the EF uses STATIC `npm:` imports (no `["npm:",pkg].join`) — Pitfall 1
    - Both zod helpers are injected in the Deno.serve wiring (Pitfall 2)
    - Role is read from `usuarios_rh` (grep `usuarios_rh`), NOT from JWT claims; ownership via `vagas.created_by`
    - `checkWeakDimCoverage` is called post-parse (Pitfall 4); the EF branches on `body.tipo` online/presencial
    - The EF never calls `.from('candidaturas').update` (RNF-07a) — grep confirms absence
    - `npm run test:run -- forbidden-strings.grep` GREEN (no forbidden terms in the EF)
  </acceptance_criteria>
  <done>gerar-guia-entrevista EF is authored — RH-authorize, static imports, online/presencial branch, weak-dim post-validation, persists to entrevista_guias, never writes candidaturas.</done>
</task>

<task type="auto">
  <name>Task 3: EF avaliar-transcricao-entrevista (untrusted text → callAi → derived flag → never-absent persist)</name>
  <files>supabase/functions/avaliar-transcricao-entrevista/index.ts</files>
  <read_first>
    - supabase/functions/avaliar-redacao-cultural/index.ts (closest analog — untrusted text → callAi → never-absent persist → neutral payload; module doc-comment invariant lines 19-23, length guard 214-217, persist 275-311, bloqueio_avanco 361-362)
    - supabase/functions/avaliar-transcricao-entrevista/_local/derive-flags.ts (the deriveLanguageAccentFlag module from 14-01)
    - docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts (TranscriptAnalysisSchema — OUTPUT contract; per-competency score + bias_flags; import OUTPUT zod as npm:zod@3.25.76/v4)
    - supabase/functions/comparativo-candidatos/index.ts (RH-authorize + two-client wiring — same as Task 2)
  </read_first>
  <action>Create `avaliar-transcricao-entrevista/index.ts` cloning avaliar-redacao-cultural verbatim. STATIC `npm:` imports + injected zod helpers (Pitfall 1/2). Validate the body with `AvaliarTranscricaoBodySchema`; add a server-side transcript length guard (`len(transcricao) >= 200` → else VALIDATION "muito curta", mirroring the word-count guard). RH-authorize (role from usuarios_rh, vagas.created_by ownership — resolve the vaga via the candidatura). The pasted transcript is UNTRUSTED — pass it as `rawInput` through `callAi('transcript_analysis', ...)` which does injection-detect + maskPII internally (NEVER bypass). Use `TranscriptAnalysisSchema` as the OUTPUT schema (npm:zod@3.25.76/v4). After parse, call `deriveLanguageAccentFlag(parsed)` (server-authoritative — resolved Q2, NOT the LLM); set `bloqueio_avanco = derived.flag`. Persist to `entrevista_analises` (competencias/citacoes/bias_flags into jsonb, bloqueio_avanco, status_analise='pendente_humano' ALWAYS) AND a `scores_candidato` row (tipo='entrevista', the BARS competency scores into metadata). On parse-failure/injection, persist a pendente_humano row with bloqueio_avanco:false (never a fabricated success — the never-absent invariant). Return neutral `{ ok: true }`. NEVER writes candidaturas (RNF-07a). OPTIONS before auth. Redacted logs (never the transcript/scores/nome — LGPD-02).</action>
  <verify>
    <automated>deno check supabase/functions/avaliar-transcricao-entrevista/index.ts && grep -cE "deriveLanguageAccentFlag" supabase/functions/avaliar-transcricao-entrevista/index.ts && grep -cE "callAi" supabase/functions/avaliar-transcricao-entrevista/index.ts && ! grep -E "from\\('candidaturas'\\)\\.update|\\.join\\(\"npm:" supabase/functions/avaliar-transcricao-entrevista/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `deno check` passes; STATIC imports (Pitfall 1) + injected helpers (Pitfall 2)
    - The transcript passes through `callAi` (untrusted text → injection-detect/maskPII); a length≥200 guard exists
    - `deriveLanguageAccentFlag` is called server-side and sets `bloqueio_avanco` (resolved Q2)
    - status_analise='pendente_humano' is the ONLY status written; the EF never writes candidaturas (RNF-07a)
    - persists both an `entrevista_analises` row and a `scores_candidato` tipo='entrevista' row
    - `npm run test:run -- forbidden-strings.grep` GREEN
  </acceptance_criteria>
  <done>avaliar-transcricao-entrevista EF is authored — RH-authorize, untrusted text through callAi, server-derived language/accent flag, never-absent pendente_humano persist, never writes candidaturas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → RH EF | RH-posted body crosses here; JWT-on + authenticate-THEN-authorize (role from usuarios_rh) |
| pasted transcript → LLM | untrusted text crosses here; must pass through callAi (injection-detect + maskPII) |
| candidate → pontuar_cognitivo | raw picks cross here; server recomputes, ignores any client score |
| funil-state advance | avancar_etapa is the unbypassable transition guard; the flag block lives here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Elevation (IDOR) | both EFs + both RPCs | mitigate | role from usuarios_rh (NOT JWT claims — silent-403 landmine); vagas.created_by ownership (admin bypass); candidate ownership via candidatos.user_id=auth.uid(), NEVER candidato_id===user.id |
| T-14-03-02 | Tampering | pontuar_cognitivo | mitigate | server recomputes score from raw_responses; SubmitCognitivoBodySchema .strict() rejects any client score; RPC returns neutral payload |
| T-14-03-03 | Tampering | pasted transcript | mitigate | transcript passed as rawInput through callAi (detectPromptInjection + maskPII); length≥200 guard; never bypassed |
| T-14-03-04 | Repudiation/legal | scoring paths | mitigate | RNF-07a — no EF/RPC writes candidaturas; cognitive status informational-only; the flag SEGURA the advance, the human confirms |
| T-14-03-05 | Information disclosure | entrevista_analises / cognitivo_itens | mitigate | candidate-DENY RLS on guias/analises; cognitivo_itens projection excludes the gabarito column (server-only access via the DEFINER RPC) |
| T-14-03-06 | Tampering (fairness) | avancar_etapa | mitigate | the language/accent flag block is enforced inside avancar_etapa() (server-authoritative), not only in the UI — blocks advance until revisao_confirmada_em is set (RF-24, anti-regional-bias) |
| T-14-03-07 | Spoofing | .join import bug | mitigate | STATIC npm: imports avoid the silent-500 ERR_MODULE_NOT_FOUND; deno check + grep guard |
| T-14-03-SC | Tampering | deno/npm installs | mitigate | zero new packages; all imports reuse PROD-pinned specifiers (RESEARCH audit) |
</threat_model>

<verification>
- `deno check` passes on both EFs; both use STATIC `npm:` imports + injected helpers
- All 4 migrations are no-wrapper (D-22), no ALTER TYPE, candidate-DENY/etapa-gated RLS
- `pontuar_cognitivo` recomputes server-side and never auto-rejects; `avancar_etapa` blocks on unresolved flag
- `npm run test:run -- forbidden-strings.grep` GREEN
- `npm run test:run` GREEN (the 14-01 schema/scorer/flag tests still pass); `deno test supabase/functions/` GREEN for the EF `_local` modules
- tsc baseline ≤305; commit via `git -c core.hooksPath=/dev/null`
- NOTE: migrations are AUTHORED-NOT-APPLIED — the live PROD apply is the [BLOCKING] 14-04 task
</verification>

<success_criteria>
- 4 no-wrapper migrations (tables+RLS, override RPC, cognitive RPC, flag guard) authored
- 2 RH-invoke EFs authored, both static-import + injected-helper + RH-authorize + never-writes-candidaturas
- The language/accent flag is server-derived and the block is in avancar_etapa (resolved Q2)
- Cognitive scoring is a no-LLM DEFINER RPC (resolved Q3), tipo='cognitivo'+subtipo (resolved Q1), never auto-rejects
- All 5 ENTREV reqs have a server artifact; ENTREV-02's interview-datetime + override RPC are wired
</success_criteria>

<output>
Create `.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-03-SUMMARY.md` when done
</output>
