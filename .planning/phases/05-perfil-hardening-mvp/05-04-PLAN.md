---
phase: 05-perfil-hardening-mvp
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - e2e/a11y.spec.ts
  - src/components/ui/input.tsx
  - src/components/ui/select.tsx
  - src/components/pages/LoginCandidatoPage.tsx
  - src/components/pages/EsqueciSenhaPage.tsx
  - src/components/pages/VagasPublicasPage.tsx
  - src/components/pages/FormularioCandidaturaPage.tsx
  - lighthouserc.js
autonomous: false
requirements: [HARD-02, HARD-04, HARD-05]

must_haves:
  truths:
    - "e2e/a11y.spec.ts passes with zero WCAG A/AA violations on the 4 public candidate routes"
    - "Every form input on candidate flows has an associated label (htmlFor or aria-label)"
    - "Focus indicators are visible on keyboard focus across candidate flows"
    - "Lighthouse mobile Accessibility >= 0.8 on the public candidate routes"
    - "Lighthouse mobile Performance is measured; the D-17 N+1 fix is applied ONLY if Performance < 0.8 (measure-first)"
    - "Logout button is reachable on iPhone 12 Pro viewport (manual UAT)"
  artifacts:
    - path: "e2e/a11y.spec.ts"
      provides: "Passing axe-core zero-violation gate on public routes (HARD-04)"
      contains: "violations"
    - path: "lighthouserc.js"
      provides: "LHCI budgets validated against the production build (HARD-02)"
  key_links:
    - from: "e2e/a11y.spec.ts"
      to: "candidate route DOM (labels/focus/contrast)"
      via: "AxeBuilder analyze"
      pattern: "AxeBuilder"
    - from: "lighthouserc.js"
      to: "categories:accessibility / categories:performance"
      via: "minScore 0.8 assertion"
      pattern: "minScore"
---

<objective>
Wave 3 — accessibility audit + Lighthouse gate. Make e2e/a11y.spec.ts (scaffolded in 05-01) pass with zero WCAG A/AA violations by fixing labels/tab-order/focus-visible across candidate flows (D-08/HARD-04), run LHCI against the production build and confirm mobile Accessibility + Performance >80 (D-05/HARD-02), apply the D-17 N+1 perf fix ONLY if measured Performance < 0.8 (D-06 measure-first), sweep the remaining candidate-facing hex literals (login/recovery/vagas/candidatura) to semantic tokens, and run the iPhone 12 Pro manual UAT for logout reachability (HARD-05/D-11).

Purpose: Close the accessibility + performance quality gates. Runs AFTER the design-system batch (05-02) and perfil/navbar work (05-03) so a11y/Lighthouse measure the FIXED, polished UI. Most a11y fixes land at the shared primitive (Input/Select), cascading to all consumers.

Output: green a11y spec, validated LHCI budgets, conditional perf fix, remaining hex sweep, iPhone UAT evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-perfil-hardening-mvp/05-RESEARCH.md
@.planning/phases/05-perfil-hardening-mvp/05-PATTERNS.md
@.planning/phases/05-perfil-hardening-mvp/05-VALIDATION.md

<interfaces>
<!-- Verified codebase facts -->
- a11y spec scaffold (05-01): e2e/a11y.spec.ts scans ['/auth/login','/auth/esqueci-senha','/cadastro','/vagas'] with AxeBuilder.withTags(['wcag2a','wcag2aa','wcag21a','wcag21aa']).analyze() asserting violations == []. Wave 0 RED expected; this plan makes it GREEN.
- a11y fix sites concentrate in shared primitives (RESEARCH Q4): the shared Input (src/components/ui/input.tsx) + shadcn Select (src/components/ui/select.tsx) — already touched in 05-02 for tokens; here fix label association + focus-visible. Plus custom glass inputs that bypass Radix.
- Manual a11y checklist (RESEARCH Q4): labels (every input has label htmlFor OR aria-label), tab order (follows visual order, no traps), focus-visible (visible ring — pair w/ the contrast work from 05-02), logout reachability (HARD-05 overlap).
- LHCI config (05-01): lighthouserc.js audits npm run preview (port 4173) public routes, mobile preset, minScore 0.8 Perf+A11y, aggregationMethod optimistic. RESEARCH recommends starting at 'warn' for one run to establish baseline, then 'error'.
- D-06 measure-first: D-17 N+1 is vagasService.enriquecerVaga (3 queries/vaga on /vagas listing). Run LHCI FIRST; only if /vagas Performance < 0.8 add a task to denormalize/RPC-batch enriquecerVaga. If it passes, D-17 stays DEFERRED.
- Remaining candidate-facing hex literals (RESEARCH Q5 sweep): login/recovery/vagas/candidatura pages still hold bg-[#...] workarounds (MeuPerfil was swept in 05-02). Sweep them here, file-by-file, smoke-runtime each.
- iPhone 12 Pro viewport = 390x844 (manual UAT; playwright mobile-chrome is Pixel 5, NOT iPhone — HARD-05 is a manual viewport check).
- CI Tier-2 secrets (RESEARCH Q1 — documentation only this phase): TEST_SUPABASE_URL/ANON_KEY (Tier 1+2), TEST_SUPABASE_SERVICE_ROLE_KEY + E2E_TEST_USER_EMAIL/PASSWORD (Tier 2 only, seed-step only, NEVER VITE_-prefixed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix a11y violations on public routes (labels / focus / tab order) — D-08/HARD-04</name>
  <files>e2e/a11y.spec.ts, src/components/ui/input.tsx, src/components/ui/select.tsx, src/components/pages/LoginCandidatoPage.tsx, src/components/pages/EsqueciSenhaPage.tsx, src/components/pages/VagasPublicasPage.tsx, src/components/pages/FormularioCandidaturaPage.tsx</files>
  <read_first>
    - e2e/a11y.spec.ts (the 05-01 scaffold — run it first to see the actual violation list per route).
    - .planning/phases/05-perfil-hardening-mvp/05-RESEARCH.md Q4 (axe approach + manual checklist + .exclude/.disableRules guidance).
    - src/components/ui/input.tsx + src/components/ui/select.tsx (post-05-02 state — fix label/focus here so it cascades).
    - src/components/pages/LoginCandidatoPage.tsx, EsqueciSenhaPage.tsx, VagasPublicasPage.tsx (the public-route pages axe scans).
  </read_first>
  <action>
    Run e2e/a11y.spec.ts to enumerate the real violations per public route, then FIX them (D-08 is a fix mandate, not just an audit). Concentrate fixes at the shared primitives where possible: ensure the Input primitive supports a properly associated label (htmlFor/id wiring or aria-label passthrough) and a visible focus ring (focus-visible:ring already present post-token-fix — confirm it is now visible against the glass); ensure the Select trigger has an accessible name. Fix page-level label gaps on LoginCandidato/EsqueciSenha/Vagas. For any element genuinely unfixable this phase, use AxeBuilder.exclude(selector) WITH a tracking comment (prefer fixing). Keep the candidatura form (FormularioCandidaturaPage) labels correct (auth-gated Tier-2, but fix label issues now since the file is in scope). Re-run until violations == [] on the 4 public routes.
  </action>
  <verify>
    <automated>npx playwright test e2e/a11y.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - playwright a11y spec passes — zero WCAG A/AA violations on all 4 public routes.
    - Every input on the 4 public routes has an associated label (htmlFor or aria-label) — confirmed by axe (no label violations).
    - Any .exclude/.disableRules usage carries a tracking comment naming the deferred element.
    - SMOKE-RUNTIME: Tab through /auth/login and /cadastro — focus ring visible on every interactive element; tab order follows visual order. Record evidence.
    - Lint baseline 296 preserved.
  </acceptance_criteria>
  <done>a11y spec green (zero violations) on public routes; labels + visible focus fixed at primitive + page level; lint 296.</done>
</task>

<task type="auto">
  <name>Task 2: Run LHCI; measure-first perf gate (D-05/D-06/HARD-02) + remaining hex sweep</name>
  <files>lighthouserc.js, src/components/pages/LoginCandidatoPage.tsx, src/components/pages/EsqueciSenhaPage.tsx, src/components/pages/VagasPublicasPage.tsx, src/components/pages/FormularioCandidaturaPage.tsx</files>
  <read_first>
    - lighthouserc.js (the 05-01 config — confirm preview server + 4173 URLs + minScore 0.8).
    - .planning/phases/05-perfil-hardening-mvp/05-RESEARCH.md Q2 (LHCI) + the D-06 measure-first gate.
    - src/features/vagas/services/vagasService.ts enriquecerVaga (the D-17 N+1 — read ONLY if Performance < 0.8).
    - The candidate-facing pages still holding bg-[#...] literals (grep each for hex literals).
  </read_first>
  <action>
    Run `npm run build && npx lhci autorun` and READ the mobile Accessibility + Performance scores for the public routes. Accessibility MUST be >= 0.8 (the a11y fixes from Task 1 should satisfy this). For Performance this is MEASURE-FIRST (D-06): only if /vagas Performance comes in < 0.8 do you add a follow-up to denormalize/RPC-batch vagasService.enriquecerVaga (the N+1); if Performance passes, D-17 stays DEFERRED and you note "measured >= 0.8, no perf fix needed" in the SUMMARY. Optionally start the LHCI assertion at 'warn' for the first run to establish a baseline, then flip to 'error'. Separately, sweep the REMAINING candidate-facing hex literals (login/recovery/vagas/candidatura — MeuPerfil was done in 05-02) from bg-[#00109E] etc. back to semantic tokens, file-by-file, smoke-runtime each swept page (Pitfall 4/6 — confirm no element goes transparent).
  </action>
  <verify>
    <automated>npm run build && npx lhci autorun</automated>
  </verify>
  <acceptance_criteria>
    - `npx lhci autorun` runs against the preview build and reports mobile Accessibility >= 0.8 on the public routes (assertion passes or is documented at 'warn' baseline with the actual score recorded).
    - Performance score recorded in SUMMARY; if < 0.8 the enriquecerVaga N+1 fix is applied and re-measured; if >= 0.8, documented as "D-17 deferred, measured >= 0.8".
    - Remaining candidate-facing hex literals swept to semantic tokens; before/after grep counts recorded.
    - SMOKE-RUNTIME: load each swept page in preview — no element regressed to transparent (F-04-08-D check). Record evidence.
    - npm run build exits 0; lint baseline 296 preserved.
  </acceptance_criteria>
  <done>LHCI A11y >= 0.8 confirmed; Performance measured and branched per D-06; remaining candidate hex swept; no visual regression; lint 296.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: iPhone 12 Pro mobile UAT + a11y/Lighthouse sign-off (HARD-05/D-11)</name>
  <what-built>
    Zero-violation a11y on public routes, validated LHCI mobile budgets, remaining hex sweep. HARD-05 (iPhone 12 Pro logout reachability) is a manual viewport check — playwright mobile-chrome is Pixel 5, not iPhone, so this requires a human viewport pass.
  </what-built>
  <how-to-verify>
    1. Run `npm run build && npm run preview`. Open the preview URL in a browser with device emulation set to iPhone 12 Pro (390x844).
    2. Walk each candidate flow at this viewport: /auth/login, /cadastro (4 steps), /auth/esqueci-senha, /vagas, /vagas/:slug, the candidatura form, /candidato/perfil.
    3. Confirm every flow completes successfully at 390x844 (no off-screen / scroll-trapped controls).
    4. Confirm the logout ("Sair") button is reachable on /candidato/perfil at this viewport — visible and tappable without a scroll trap (HARD-05).
    5. Confirm focus indicators are visible when navigating by keyboard (HARD-04 manual overlap).
    6. Review the LHCI report (from Task 2): confirm mobile Accessibility >= 80 and note the Performance score.
  </how-to-verify>
  <resume-signal>Type "approved" if all flows complete on iPhone 12 Pro viewport with logout reachable and LHCI A11y >= 80 — or describe any unreachable control / failing flow.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → LHCI preview server | LHCI audits the production build served locally; no external attack surface (CI-only) |
| keyboard/AT user → candidate forms | missing labels / focus indicators degrade accessibility (WCAG compliance, not a classic security threat) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | LHCI env (VITE_SUPABASE_*) | mitigate | LHCI uses only anon-safe VITE_ vars (TEST project); no service_role; Pitfall 2 invariant preserved. |
| T-05-04-02 | Denial of Service | LHCI auditing dev server instead of build | accept | Mitigated by design: lighthouserc.js startServerCommand is `npm run preview` (build/), port 4173 (Pitfall 1). CI-only. |
| T-05-04-03 | Tampering | hex sweep visual regression | mitigate | Smoke-runtime each swept page (Pitfall 4/6); token fix from 05-02 confirmed before sweep. |
| T-05-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (Wave 0 installed LHCI/axe). N/A. |
</threat_model>

<verification>
- `npx playwright test e2e/a11y.spec.ts --project=chromium` passes (zero violations, 4 public routes).
- `npx lhci autorun` reports mobile Accessibility >= 0.8; Performance recorded + branched per D-06.
- Remaining candidate hex literals swept; npm run build exits 0; lint baseline 296.
- Human iPhone 12 Pro UAT (Task 3) approved: flows complete, logout reachable, A11y >= 80.
</verification>

<success_criteria>
- a11y spec green: zero WCAG A/AA violations on public candidate routes (HARD-04).
- LHCI mobile Accessibility >= 80; Performance measured with D-06 measure-first branch (HARD-02).
- iPhone 12 Pro flows complete + logout reachable (HARD-05).
- Remaining candidate-facing hex literals swept to semantic tokens (no regression).
- Lint baseline 296 preserved.
</success_criteria>

<output>
Create `.planning/phases/05-perfil-hardening-mvp/05-04-SUMMARY.md` when done.
</output>
