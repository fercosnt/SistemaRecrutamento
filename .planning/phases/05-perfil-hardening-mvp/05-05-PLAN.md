---
phase: 05-perfil-hardening-mvp
plan: 05
type: execute
wave: 5
depends_on: ["05-01"]
files_modified:
  - supabase/migrations/20260606000001_vaga_status_sync.sql
  - supabase/migrations/20260606000002_bloco_valido_reconcile.sql
  - database.types.ts
autonomous: false
requirements: [PERF-01]

must_haves:
  truths:
    - "A vaga that is soft-deleted (deleted_at IS NOT NULL) can no longer have status='ativa' — enforced at the DB level (CHECK or trigger)"
    - "Any existing drift (soft-deleted vagas still marked ativa) is backfilled to a non-ativa status"
    - "The bloco_valido_check constraint is captured in supabase/migrations/ (migrations are the source of truth — FOUND-09 invariant)"
    - "supabase db push --linked reports the remote database is up to date after applying both migrations"
  artifacts:
    - path: "supabase/migrations/20260606000001_vaga_status_sync.sql"
      provides: "F-04-08-B: CHECK/trigger forcing status != ativa when deleted_at IS NOT NULL + backfill"
      contains: "deleted_at"
    - path: "supabase/migrations/20260606000002_bloco_valido_reconcile.sql"
      provides: "F-04-08-C: reconciliation migration re-declaring bloco_valido_check"
      contains: "bloco_valido"
  key_links:
    - from: "supabase/migrations/20260606000001_vaga_status_sync.sql"
      to: "public.vagas (status, deleted_at)"
      via: "CHECK constraint or BEFORE-UPDATE trigger"
      pattern: "vagas"
---

<objective>
Wave 4 — DB data-hygiene migrations (D-13). Two reconciliation migrations: F-04-08-B (a soft-deleted vaga must not stay status='ativa' — CHECK constraint or BEFORE-UPDATE trigger + a one-time backfill UPDATE) and F-04-08-C (the bloco_valido_check constraint exists in the live DB but is missing from supabase/migrations/ — schema drift; a reconciliation migration re-declares it so migrations remain the source of truth, FOUND-09 invariant). Includes the mandatory [BLOCKING] human-action db-push task per the project's PL/pgSQL workaround (D-22).

Purpose: Close the two deferred DB-hygiene findings carried from Phase 4. The listing-correctness invariant (no soft-deleted vaga surfaces as ativa) protects PERF-01/the candidate's view of valid data. The schema-drift reconciliation restores FOUND-09 (migrations = source of truth).

Output: two idempotent migration files + applied-to-live confirmation + regenerated database.types.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-perfil-hardening-mvp/05-RESEARCH.md
@.planning/phases/05-perfil-hardening-mvp/05-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Verified codebase facts -->
- Idempotent-DDL analog (PATTERNS): supabase/migrations/20260425000004_candidaturas_unique_constraint.sql — header doc block + DO $$ BEGIN IF NOT EXISTS (...) THEN ... END IF; END $$; NO outer BEGIN/COMMIT (the Supabase CLI driver wraps each migration in its own implicit transaction; an outer wrapper + DO $$...$$ breaks the prepared-statement boundary parser → SQLSTATE 42601).
- F-04-08-B (RESEARCH Q-DB): vaga soft-deleted (deleted_at set) but status='ativa' → listing could surface a deleted vaga. Fix: CHECK constraint OR BEFORE-UPDATE trigger forcing status != 'ativa' when deleted_at IS NOT NULL + a one-time backfill UPDATE for existing drift. Include a precondition check (find existing drift first) like the analog does.
- F-04-08-C (RESEARCH Q-DB): bloco_valido_check constraint exists in live DB but not in supabase/migrations/ → schema drift. Fix: reconciliation migration re-declaring the constraint (idempotent IF NOT EXISTS) so migrations are source of truth.
- PL/pgSQL db push workaround (CLAUDE.md / D-22 — HUMAN CHECKPOINT): migrations with CREATE FUNCTION / DO $$...$$ + adjacent COMMENT/GRANT/REVOKE FAIL via `supabase db push --linked` (SQLSTATE 42601). Workaround: (1) paste SQL into Supabase SQL Editor → execute manually; (2) `supabase migration repair --status applied <version>`; (3) confirm `supabase db push --linked` reports up-to-date; (4) remove outer BEGIN;/COMMIT; wrappers + inline note. Non-TTY: set SUPABASE_ACCESS_TOKEN.
- database.types.ts: NEVER hand-edit (CLAUDE.md). Regenerate via `npm run db:types` after migrations apply.
- Build/type checks PASS without the push (types come from config, not the live DB) — so the push task is MANDATORY; skipping it creates a false-positive verification state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author both D-13 migrations (idempotent DO-block pattern)</name>
  <files>supabase/migrations/20260606000001_vaga_status_sync.sql, supabase/migrations/20260606000002_bloco_valido_reconcile.sql</files>
  <read_first>
    - supabase/migrations/20260425000004_candidaturas_unique_constraint.sql (the canonical header + DO $$ IF NOT EXISTS END $$ pattern + the no-BEGIN/COMMIT note).
    - .planning/phases/05-perfil-hardening-mvp/05-PATTERNS.md "NEW migrations" section (the header template + per-finding DDL).
    - .planning/phases/05-perfil-hardening-mvp/05-RESEARCH.md Q-DB (F-04-08-B + F-04-08-C specifics).
    - CLAUDE.md "Migrations + db push — workaround conhecido (PL/pgSQL)".
    - The live vagas table schema (use the supabase MCP list_tables or read database.types.ts for the vagas row shape: status enum + deleted_at column).
  </read_first>
  <action>
    Author supabase/migrations/20260606000001_vaga_status_sync.sql (F-04-08-B): the canonical header doc block (Phase 05 / Requirement D-13 / F-04-08-B + PURPOSE + PRECONDITION + the no-BEGIN/COMMIT note). First, a one-time backfill UPDATE setting status to a non-ativa value (e.g. 'inativa' or the project's canonical closed status — confirm the status_vaga enum members from database.types.ts) WHERE deleted_at IS NOT NULL AND status='ativa'. Then enforce the invariant going forward via a BEFORE-UPDATE/INSERT trigger (preferred over a CHECK if the project uses soft-delete patterns that a CHECK would over-constrain) OR a CHECK constraint `(deleted_at IS NULL OR status <> 'ativa')` — choose the trigger if a plain CHECK would reject legitimate intermediate states; document the choice. Wrap DDL in DO $$ IF NOT EXISTS ... END $$ for idempotency. Author supabase/migrations/20260606000002_bloco_valido_reconcile.sql (F-04-08-C): re-declare the bloco_valido_check constraint idempotently (IF NOT EXISTS guard) so migrations capture it — read the live constraint definition first (supabase MCP or SQL Editor) to reproduce it exactly. NO outer BEGIN/COMMIT in either file.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260606000001_vaga_status_sync.sql && test -f supabase/migrations/20260606000002_bloco_valido_reconcile.sql && grep -q "deleted_at" supabase/migrations/20260606000001_vaga_status_sync.sql && grep -qi "bloco_valido" supabase/migrations/20260606000002_bloco_valido_reconcile.sql && ! grep -qiE "^\s*(BEGIN|COMMIT)\s*;" supabase/migrations/20260606000001_vaga_status_sync.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both migration files exist with the canonical Phase-05 header doc block.
    - 20260606000001 contains the backfill UPDATE (WHERE deleted_at IS NOT NULL AND status='ativa') + the enforcing trigger/CHECK, wrapped idempotently in DO $$...$$.
    - 20260606000002 re-declares bloco_valido_check idempotently.
    - Neither file contains an outer BEGIN;/COMMIT; wrapper (grep returns zero matches at line start).
    - The chosen status target for the backfill is a valid status_vaga enum member (confirmed against database.types.ts).
    - Lint baseline 296 preserved (SQL files do not affect tsc).
  </acceptance_criteria>
  <done>Both idempotent migrations authored following the canonical pattern; no BEGIN/COMMIT wrappers; backfill + enforcement + reconciliation present.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply both migrations to the live DB (PL/pgSQL db-push workaround) + regen types</name>
  <what-built>
    Two idempotent D-13 migration files authored (vaga status sync + bloco_valido reconcile). They are NOT yet applied to the live database. Build/type checks pass WITHOUT the push (types come from config, not the live DB) — so this push is MANDATORY; the phase cannot pass verification without it, and skipping it creates a false-positive verification state.
  </what-built>
  <how-to-verify>
    Apply via the CLAUDE.md / D-22 PL/pgSQL workaround (these migrations use DO $$...$$ blocks, which trigger SQLSTATE 42601 via `supabase db push --linked` in the transaction pooler):
    1. Open the Supabase SQL Editor for project isljnozzlvckrgjjbjwp.
    2. Paste the SQL from supabase/migrations/20260606000001_vaga_status_sync.sql → execute manually. Confirm: the backfill UPDATE affected the expected number of drift rows (or 0 if no drift), and the trigger/CHECK now exists.
    3. Paste the SQL from supabase/migrations/20260606000002_bloco_valido_reconcile.sql → execute manually. Confirm: bloco_valido_check is present.
    4. Sync local migration state: `supabase migration repair --status applied 20260606000001` then `... 20260606000002` (set SUPABASE_ACCESS_TOKEN if non-TTY).
    5. Confirm: `supabase db push --linked` reports "Remote database is up to date".
    6. SMOKE (live SQL): `SELECT count(*) FROM vagas WHERE deleted_at IS NOT NULL AND status='ativa'` returns 0 (invariant holds). Attempt an UPDATE setting a soft-deleted vaga to status='ativa' and confirm it is rejected by the trigger/CHECK.
    7. Regenerate types: `npm run db:types` → confirm `tsc --noEmit` still passes at baseline 296.
  </how-to-verify>
  <resume-signal>Type "applied" once both migrations are live, `supabase db push --linked` reports up-to-date, the SELECT returns 0 drift rows, the invariant rejects a bad UPDATE, and db:types regenerated cleanly — or describe any error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live Postgres (RLS-governed vagas) | DDL touches a table that feeds the public listing; an incorrect constraint could block legitimate vaga lifecycle transitions or surface deleted vagas |
| local migration state → remote schema | the repair/push reconciliation must keep migrations as the single source of truth (FOUND-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | vaga_status_sync trigger/CHECK | mitigate | Idempotent DO $$ IF NOT EXISTS guard prevents double-apply; backfill is scoped to deleted_at IS NOT NULL AND status='ativa' (narrow); choice of trigger vs CHECK documented to avoid over-constraining legitimate states. |
| T-05-05-02 | Information Disclosure | soft-deleted vaga surfacing in public listing | mitigate | The invariant guarantees a soft-deleted vaga cannot be status='ativa', so the VAGA-01 status='ativa' filter cannot leak a deleted vaga. |
| T-05-05-03 | Repudiation | manual SQL-Editor apply (human checkpoint) | mitigate | migration repair --status applied keeps the migrations ledger authoritative; db push --linked up-to-date confirmation is the audit gate; SMOKE SELECT verifies the live invariant. |
| T-05-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. N/A. |
</threat_model>

<verification>
- Both migration files exist, idempotent, no BEGIN/COMMIT wrappers.
- [BLOCKING] Task 2 applied: `supabase db push --linked` reports up-to-date; live SELECT returns 0 drift rows; bad UPDATE rejected; bloco_valido_check present.
- `npm run db:types` regenerated; tsc baseline 296 preserved.
</verification>

<success_criteria>
- F-04-08-B: soft-deleted vagas cannot be status='ativa' (enforced + backfilled).
- F-04-08-C: bloco_valido_check captured in migrations (FOUND-09 source-of-truth restored).
- Migrations applied to live DB via the D-22 workaround; push reports up-to-date.
- database.types.ts regenerated; lint baseline 296 preserved.
</success_criteria>

<output>
Create `.planning/phases/05-perfil-hardening-mvp/05-05-SUMMARY.md` when done.
</output>
