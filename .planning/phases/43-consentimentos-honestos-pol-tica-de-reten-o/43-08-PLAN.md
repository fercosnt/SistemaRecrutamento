---
phase: 43-consentimentos-honestos-pol-tica-de-reten-o
plan: 08
type: execute
wave: 5
depends_on: [43-07]
files_modified:
  - src/features/privacidade/services/privacidadeService.ts
  - src/features/privacidade/hooks/usePrivacidade.ts
  - src/features/privacidade/hooks/useRevogarMarketing.ts
  - src/features/privacidade/components/ConsentimentoSwitchRow.tsx
  - src/features/privacidade/components/AutorizacoesLista.tsx
  - src/features/privacidade/components/GuardaCurriculoBloco.tsx
  - src/features/privacidade/components/PrivacidadeCandidatoPage.tsx
  - src/features/privacidade/components/__tests__/ConsentimentoSwitchRow.test.tsx
  - src/features/privacidade/components/__tests__/PrivacidadeCandidatoPage.test.tsx
  - src/features/privacidade/components/__tests__/GuardaCurriculoBloco.test.tsx
  - src/router/routes.tsx
  - src/components/pages/MeuPerfilCandidatoPage.tsx
autonomous: true
requirements: [CONSENT-04, RETEN-03]

must_haves:
  truths:
    - "Um candidato abre `/candidato/privacidade` pelo próprio painel e vê o que autorizou (CONSENT-04)."
    - "Ele desliga o consentimento de divulgação de vagas sozinho, sem diálogo de confirmação, sem pedido de motivo e sem contra-oferta — zero fricção para revogar (CONSENT-04)."
    - "O controle reflete APENAS estado confirmado pelo servidor: em voo fica desabilitado, e em falha volta ao estado do servidor com erro visível (CONSENT-04)."
    - "O canal transacional aparece como informação com a base legal nomeada, sem nenhum controle — a assimetria é legível sem virar dark pattern."
    - "`autorizacao_retencao_curriculo` ganha seu primeiro consumidor real: o candidato lê, por escrito, sob qual base o currículo dele é guardado e por quanto tempo (RETEN-03)."
    - "Nenhuma leitura desta tela usa `select('*')`, e `ip_aceite` e `user_agent_aceite` nunca chegam ao navegador do candidato."
  artifacts:
    - src/features/privacidade/services/privacidadeService.ts
    - src/features/privacidade/components/PrivacidadeCandidatoPage.tsx
  key_links:
    - "A tela lê a MESMA linha que `pode_receber_marketing()` lê (a mais recente do candidato) — se as duas semânticas divergirem, a tela mostra uma coisa e o guard aplica outra."
    - "Rota `/candidato/privacidade` com `RoleGuard role=\"candidato\"` + card de navegação em `MeuPerfilCandidatoPage` — sem os dois, a página só existe por URL digitada."
    - "Escrita own-row direta por PostgREST sob a policy de UPDATE viva (medida em `pg_policies`, com `qual` e `with_check`) — nenhuma RPC `SECURITY DEFINER` nova."
---

<objective>
`/candidato/privacidade` — **Seus dados e autorizações**: a primeira superfície em que um
consentimento coletado por este sistema pode ser desfeito pela própria pessoa, e a primeira em que
`autorizacao_retencao_curriculo` é lido por alguém.

Purpose: os consentimentos opcionais são órfãos desde sempre — coletados e nunca consultados. Esta
tela é metade da correção (a outra metade é o guard do 43-05, que faz a revogação morder). Ela
também é a CASA que a Phase 44 (pedir cópia dos dados) e a Phase 45 (pedir exclusão) vão ocupar;
criá-la aqui evita que cada fase invente a sua.

Output: feature `src/features/privacidade/` completa, rota com guard, card de navegação no perfil
do candidato, e a suíte que prova ausência de UI otimista e ausência de fricção.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/43-consentimentos-honestos-pol-tica-de-reten-o/43-UI-SPEC.md
@.planning/phases/43-consentimentos-honestos-pol-tica-de-reten-o/43-07-SUMMARY.md
@CLAUDE.md

# Molde do serviço own-row por allowlist + classe de erro
@src/features/explicacao/services/explicacaoService.ts

# Shell mobile-first a clonar verbatim + skeleton de 3 blocos
@src/features/explicacao/components/ExplicacaoCandidatoPage.tsx

# Camada glass e o defeito de props do GlassButton (corrigido no 42-11)
@src/components/ui/glass.tsx
@src/components/ui/AsyncState.tsx

@src/router/routes.tsx
@src/components/pages/MeuPerfilCandidatoPage.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A revogação ponta a ponta — ler own-row, alternar, confirmar pelo servidor (CONSENT-04)</name>
  <files>src/features/privacidade/services/privacidadeService.ts, src/features/privacidade/hooks/usePrivacidade.ts, src/features/privacidade/hooks/useRevogarMarketing.ts, src/features/privacidade/components/ConsentimentoSwitchRow.tsx, src/features/privacidade/components/__tests__/ConsentimentoSwitchRow.test.tsx</files>
  <behavior>
    - Com o servidor devolvendo marketing autorizado, o switch aparece ligado com a data de quando foi ativado.
    - Com o servidor devolvendo `false`, aparece desligado com a data da desativação; com NULL, aparece desligado sem data.
    - Enquanto a escrita está em voo, o switch fica DESABILITADO e mostra o rótulo de pendência — nunca o estado desejado.
    - Concluída a escrita, o estado exibido é o que voltou do servidor.
    - Em falha, o switch volta ao estado do servidor E aparece alerta inline com `role="alert"` abaixo da linha.
    - Revogar não abre diálogo nenhum, não pede motivo e não mostra texto de dissuasão.
  </behavior>
  <action>
Crie `privacidadeService.ts` no idioma de `explicacaoService.ts`: classe de erro `PrivacidadeError`
com `code`, e **allowlist explícita e nomeada** de colunas em toda leitura.

`obterAutorizacoes()` lê `public.autorizacoes` sob a policy own-row viva
(`candidatos.user_id = auth.uid()`), selecionando **apenas** `id`, `autorizacao_uso_dados`,
`autorizacao_marketing_vagas`, `autorizacao_retencao_curriculo`, `consent_text_version`,
`created_at` e `updated_at`. **Nunca `select('*')`** — RLS é row-level e não filtra coluna, e
`ip_aceite` e `user_agent_aceite` moram nessa tabela e não têm por que chegar ao navegador
([[reference_select_star_leaks_pii]]).

⚠ A tabela é estruturalmente 1:N por candidato. Leia a linha **MAIS RECENTE**
(`order('created_at', { ascending: false }).limit(1)`) — exatamente a mesma semântica de
`pode_receber_marketing()` no banco. Se as duas divergirem, a tela mostra uma coisa e o guard
aplica outra, que é a classe de defeito que este milestone inteiro combate. Consulte a medição 0.4
do `43-07-SUMMARY.md` para saber se hoje há candidato com mais de uma linha, e registre no docblock
o que foi medido.

`revogarMarketing(idAutorizacao, novoValor)` faz **`UPDATE` own-row direto por PostgREST** sobre a
linha lida — a policy de `UPDATE` own-row com `qual` E `with_check` foi medida viva em
`pg_policies` em 2026-08-01, então **nenhuma RPC `SECURITY DEFINER` nova é necessária**, e o
`with_check` impede que a linha seja reapontada para outro candidato. A escrita devolve a linha
atualizada pela mesma allowlist, e é esse retorno — não o valor desejado — que alimenta a tela.
Um `42501`/`403` (negação own-row) vira `PrivacidadeError` com código próprio, nunca um erro cru
de transporte ecoado na interface.

`usePrivacidade` é TanStack Query com chave hierárquica `privacidadeKeys.autorizacoes(candidatoId)`.
`useRevogarMarketing` é `useMutation` nas duas direções que **invalida** a query no sucesso e
**nunca** usa `onMutate` otimista — a UI-SPEC Invariante 5 proíbe: uma interface dizendo "revogado"
enquanto o servidor diz "concedido" é o defeito central do milestone em miniatura.

`ConsentimentoSwitchRow` é uma linha com `Switch` + `Label` associado por `htmlFor`, o texto do
consentimento **sempre visível ao lado do controle** (é o texto que torna a concessão informada;
nunca colapsado atrás de "ler mais"), o rótulo textual de estado adjacente (regra colorblind-safe:
nunca só a posição do controle), `min-h-[44px]`, e a região de erro inline. Em voo: `disabled` +
"Salvando…" + `Loader2`. Copy verbatim da §`/candidato/privacidade` da 43-UI-SPEC (linhas 478-495),
incluindo os dois toasts de sucesso e o alerta de erro.

**Proibido nesta região** (Invariante 4): diálogo de confirmação ao revogar, pedido de motivo,
"tem certeza?", aviso de perda de oportunidades, contra-oferta ou atraso artificial. Fricção sobre
o exercício de um direito é dark pattern. Conceder é que exige o texto integral visível.

⚠ `GlassButton` descartava props não-estilo até a correção do 42-11 — confira o estado vivo antes
de contar com `aria-label` passado por prop.

Escreva a suíte de `ConsentimentoSwitchRow` cobrindo os seis comportamentos, com o serviço
mockado. A ausência de UI otimista é asserida pelo estado do controle DURANTE a promessa pendente,
não por leitura de código.
  </action>
  <verify>
    <automated>npx vitest run src/features/privacidade && npm run -s lint 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>A leitura é por allowlist nomeada da linha mais recente; a escrita é own-row direta e devolve a linha atualizada; o switch nunca antecipa o estado desejado; a falha reverte com erro visível; não existe diálogo, motivo ou dissuasão no caminho de revogação; contagem `tsc` na baseline 97.</done>
</task>

<task type="auto">
  <name>Task 2: A página, a lista de três linhas, o bloco do currículo e os dois caminhos de chegada (RETEN-03)</name>
  <files>src/features/privacidade/components/AutorizacoesLista.tsx, src/features/privacidade/components/GuardaCurriculoBloco.tsx, src/features/privacidade/components/PrivacidadeCandidatoPage.tsx, src/router/routes.tsx, src/components/pages/MeuPerfilCandidatoPage.tsx</files>
  <action>
`PrivacidadeCandidatoPage` clona **verbatim** o `ScreenShell` de `ExplicacaoCandidatoPage`
(`BackgroundImage background="gradient"` + overlay 15% + `container mx-auto px-4 max-w-2xl` +
`GlassPanel variant="white" blur="xl"`). H1, subtítulo, as duas seções e "Voltar ao painel"
(`min-h-[44px]`) com a copy verbatim da 43-UI-SPEC (linhas 462-468). Estados de carregamento pelo
skeleton de 3 blocos glass pulsantes já vivo em `ExplicacaoCandidatoPage`, preservando a altura
para não haver salto de layout; erro com a copy própria da spec + "Tentar novamente"; nunca ecoar
erro cru de transporte.

`AutorizacoesLista` renderiza **três linhas fixas, nesta ordem contratual**:
1. **Transacional — sem controle nenhum.** Nem `Checkbox`, nem `Switch`, nem `role="checkbox"`,
   nem `aria-checked`, nem `aria-disabled`, nem `<input disabled>`. Um leitor de tela tem de ouvir
   informação, não opção desabilitada. Copy verbatim (linhas 470-476), com a base legal do
   Art. 7º V nomeada e a fronteira explícita de que estes avisos não incluem divulgação de vagas.
   A ordem é contratual: o item sem escolha vem PRIMEIRO, para que a assimetria seja lida antes
   da escolha.
2. **Marketing** — o `ConsentimentoSwitchRow` da Task 1.
3. **Guarda do currículo** — leitura, sem controle.
A lista é fixa em 3 linhas conhecidas em tempo de compilação; não existe caminho de dado que a
esvazie, e ausência de valor no servidor cai nos estados POR LINHA, nunca num vazio de lista.

`GuardaCurriculoBloco` renderiza um de três casos, com a copy verbatim da spec (linhas 499-507):
autorizado com currículo · **não** autorizado com currículo · sem currículo. Mais a nota de
revogação por canal humano nomeado (o Encarregado), sempre visível nos dois primeiros casos.
Nenhum dos três promete exclusão automática.

**Decisão registrada no docblock — de onde sai o "Prazo previsto".** Ele é a data da autorização
mais **24 meses**, o teto que a pessoa leu e aceitou no cadastro. **NÃO** vem da matriz
`config_retencao_etapa`: aquela é configuração interna do operador, mostrá-la ao candidato
exporia política interna, e ela pode ser encurtada por um administrador sem que a copy consentida
mude. Registre como dependência da Phase 46: quando a purga for ligada, esta data e a matriz têm
de ser reconciliadas, ou a copy revista. Hoje nada apaga, então a frase continua verdadeira.

**Decisão registrada — por que a guarda do currículo NÃO ganha switch nesta fase.** A LGPD
(Art. 8º, §5) exige que o consentimento seja revogável, e a exigência é atendida por canal humano
nomeado e existente: o Encarregado. O que não existe é motor de exclusão — ele é a Phase 45. Um
switch aqui desligaria uma flag e nada mais aconteceria: a promessa órfã, de novo, agora sobre o
dado mais sensível da fase. **Proibido**, na copy do candidato, prometer que a autogestão chegará
— isso é roadmap, não compromisso com o titular.

Registre a rota `/candidato/privacidade` em `src/router/routes.tsx` com
`RoleGuard role="candidato"`, no idioma das rotas de candidato vizinhas, e acrescente em
`MeuPerfilCandidatoPage` **um card de navegação** para ela (`min-h-[44px]`). **Não** um item novo
na `CandidatoNavbar` — ela hoje tem exatamente um link e engordá-la é mudança de shell fora de
escopo. E **não** mova nada de perfil para a página nova: a D-10 da Phase 17 estabeleceu que
perfil é dados pessoais + edição APENAS.

Escopo negativo: zero token novo, zero primitivo novo, zero dependência npm nova; nenhum
`npx shadcn add`/`init`; nenhum `text-xs` autorado (toda string nova desta fase renderiza a 14px).
  </action>
  <verify>
    <automated>npx vitest run src/features/privacidade && npm run -s lint 2>&1 | grep -c "error TS" && grep -q "candidato/privacidade" src/router/routes.tsx</automated>
  </verify>
  <done>A página existe no shell clonado, com três linhas na ordem contratual e o bloco do currículo em três casos; a rota está registrada com guard de candidato e há card de navegação a partir do perfil; as duas decisões (origem do prazo e ausência de switch no currículo) estão escritas no código.</done>
</task>

<task type="auto">
  <name>Task 3: A suíte que prova o que a tela NÃO faz</name>
  <files>src/features/privacidade/components/__tests__/PrivacidadeCandidatoPage.test.tsx, src/features/privacidade/components/__tests__/GuardaCurriculoBloco.test.tsx</files>
  <action>
Casos obrigatórios, todos com o serviço mockado:

(a) **A linha do transacional não é controle.** Dentro da região dela, zero elementos com
`role="checkbox"`, `role="switch"`, `aria-checked`, `aria-disabled` ou `<input>`. Asserção
estrutural, não textual: uma asserção que olhasse só o texto visível não pegaria um controle
acrescentado ali depois.

(b) **Zero fricção para revogar.** Ao alternar o switch para desligado, nenhum `dialog` /
`alertdialog` é aberto e o documento não contém texto de dissuasão. As strings proibidas são
montadas em runtime dentro do teste, no idioma do 42-11.

(c) **Sem UI otimista.** Com a mutação pendente, o switch está desabilitado e o estado exibido
continua sendo o do servidor.

(d) **Os três casos do bloco do currículo** renderizam a copy correta e nenhum deles contém
promessa de exclusão automática — asserção negativa contra as expressões de futuro-de-máquina
listadas na §Copywriting Contract da UI-SPEC, montadas em runtime.

(e) **Nenhuma coluna de PII de aceite chega à tela:** o objeto que o serviço devolve, no mock e no
contrato de tipo, não tem `ip_aceite` nem `user_agent_aceite`; e a chamada ao Supabase é feita com
lista de colunas nomeada, provada por asserção sobre o argumento de `select`.

(f) **Estados de carregamento e erro** renderizam a copy própria da spec, com botão de nova
tentativa, e nunca o texto cru do erro.

Conteúdo em portal (diálogos, toasts) é lido de `document.body`, não do container do render —
`container.textContent` fica vazio para portal e toda asserção negativa passaria sem olhar nada
(3 falsos verdes medidos no 42-10).
  </action>
  <verify>
    <automated>npx vitest run src/features/privacidade && npm run test:run</automated>
  </verify>
  <done>Os seis casos passam; as asserções negativas são estruturais e usam literais montados em runtime; conteúdo em portal é lido de `document.body`; a suíte inteira do repositório fica verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do candidato → `public.autorizacoes` | leitura e escrita own-row sob RLS; a tabela carrega colunas de PII de aceite que não devem chegar à tela |
| tela → guard do banco | se a semântica de "qual linha vale" divergir entre os dois, a interface afirma um estado que o servidor não aplica |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-38 | Information disclosure | leitura de `autorizacoes` na tela | high | mitigate | Allowlist nomeada de 7 colunas, nunca `select('*')`; caso (e) da suíte assere o argumento de `select`; `ip_aceite` e `user_agent_aceite` ficam fora |
| T-43-39 | Tampering | revogação por outro titular | high | mitigate | Escrita own-row sob a policy viva com `qual` E `with_check` medidos; o `with_check` impede reapontar a linha para outro candidato |
| T-43-40 | Spoofing | UI afirmando estado não confirmado | high | mitigate | Proibição de `onMutate` otimista; o estado exibido vem do retorno do servidor; caso (c) da suíte |
| T-43-41 | Repudiation | divergência tela × guard sobre qual linha vale | medium | mitigate | A leitura usa a MESMA semântica "linha mais recente" de `pode_receber_marketing()`, com a medição 0.4 do checkpoint registrada no docblock |
| T-43-42 | Spoofing | dark pattern na revogação | medium | mitigate | Caso (b) da suíte: nenhum diálogo, nenhum texto de dissuasão; a proibição é testada, não confiada à revisão |
| T-43-SC | Tampering | instalação de pacotes | low | accept | Zero dependência nova; todos os primitivos já vendorizados desde o M1 |
</threat_model>

<verification>
- `npm run test:run` verde; `npm run -s lint 2>&1 | grep -c "error TS"` na baseline **97**.
- O portão de grep do 43-02 continua verde com `src/features/privacidade/` já dentro da allowlist.
- A rota responde sob `RoleGuard role="candidato"` e há caminho de chegada a partir do perfil.
- Nenhuma leitura da feature usa `select('*')`.
</verification>

<success_criteria>
- O candidato chega à página pelo painel, vê o que autorizou e desliga o marketing sem fricção.
- O controle nunca mente sobre o estado do servidor.
- `autorizacao_retencao_curriculo` é lido e citado como base legal da guarda do currículo.
- Nenhuma coluna de PII de aceite atravessa para o navegador.
</success_criteria>

<output>
Create `.planning/phases/43-consentimentos-honestos-pol-tica-de-reten-o/43-08-SUMMARY.md` when done.
</output>
