---
phase: 43-consentimentos-honestos-pol-tica-de-reten-o
plan: 07
type: execute
wave: 4
depends_on: [43-01, 43-03, 43-04, 43-05, 43-06]
files_modified:
  - database.types.ts
  - supabase/migrations/20260801000001_p43_consent_prova_e_marketing.sql
  - supabase/migrations/20260801000002_p43_config_retencao.sql
  - supabase/migrations/20260801000003_p43_guard_marketing.sql
  - supabase/migrations/20260801000004_p43_previa_retencao.sql
  - supabase/tests/p43_previa_smoke.sql
autonomous: false
requirements: [CONSENT-01, CONSENT-02, CONSENT-03, CONSENT-04, CONSENT-05, CONSENT-06, RETEN-01, RETEN-02, RETEN-04]
user_setup:
  - service: resend
    why: "CONSENT-06 exige confirmação POSITIVA de que o tracking de clique e de abertura está desligado no domínio remetente — é setting de provedor, não de código"
    env_vars:
      - name: RESEND_API_KEY
        source: "Vault do Supabase (PROD) ou Resend Dashboard → API Keys; a chave não existe no ambiente local"
    dashboard_config:
      - task: "Conferir (e desligar, se estiver ligado) open tracking e click tracking do domínio rh.beautysmile.com.br"
        location: "Resend Dashboard → Domains → rh.beautysmile.com.br → Configuration/Tracking"

must_haves:
  truths:
    - "As quatro migrations desta fase estão vivas em PROD, na ordem, cada uma com a `version` do ledger reparada e a fidelidade byte-a-byte provada por md5 — não presumida."
    - "As colunas de prova existem ANTES de a Edge Function que grava o hash ser deployada: não existe janela em que um cadastro grave consentimento sem prova."
    - "Os quatro smokes passaram integralmente em PROD, cada um numa única chamada, incluindo todas as asserções negativas."
    - "Nenhuma linha de candidato foi criada, alterada ou apagada pelo checkpoint além da fixture explicitamente criada e removida."
    - "`database.types.ts` foi regenerado depois do último apply e reflete as estruturas novas."
    - "O estado do tracking do Resend é um fato DATADO e verificado no provedor, não uma presunção sobre o padrão dele (CONSENT-06)."
  artifacts:
    - database.types.ts
  key_links:
    - "apply de `20260801000001` → deploy da EF `cadastrar-candidato`: a ordem é o controle, e inverter destrói o SC#1 para toda a janela."
    - "`apply_migration` carimba a própria `version` e guarda o SQL literal em `schema_migrations.statements[]` — as duas propriedades tornam o reparo obrigatório e o drift mensurável."
    - "`npm run db:types` desbloqueia os planos 43-08 e 43-09, que não compilam contra tipos antigos."
---

<objective>
O checkpoint de PROD desta fase: aplicar as quatro migrations na ordem, provar cada apply por
md5, rodar os quatro smokes, deployar a Edge Function DEPOIS das colunas, regenerar os tipos e
fechar o CONSENT-06 no provedor.

Purpose: subagentes GSD não recebem os tools MCP do Supabase (anthropics/claude-code#13898),
`supabase db push` é proibido neste projeto e o CLI não está autenticado. Todo apply, todo deploy
e toda inspeção viva são trabalho do orquestrador. Isto não é descoberta de meio de fase — é
premissa de desenho de wave, e este plano existe para que ela seja executada como runbook em vez
de improvisada.

Output: quatro migrations vivas com fidelidade provada, quatro smokes verdes, a EF na versão nova,
`database.types.ts` regenerado e commitado, e o veredito datado do tracking do Resend.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/43-consentimentos-honestos-pol-tica-de-reten-o/43-RESEARCH.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-07-SUMMARY.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-08-SUMMARY.md
@.planning/todos/pending/processo-origem-do-drift-desconhecida.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: CHECKPOINT — medir, aplicar as 4 migrations na ordem, provar por md5 e rodar os 4 smokes</name>
  <action>
Runbook do orquestrador. **A ORDEM É O CONTROLE.** Executar de cima para baixo; qualquer passo que
não bata o esperado interrompe a sequência.

### 0. Medir ANTES de aplicar qualquer coisa

Estas medições não são curiosidade: três delas viram as linhas `>>> antes:` dos cabeçalhos das
migrations, e duas respondem perguntas abertas da pesquisa. Aplicar primeiro destruiria
irrecuperavelmente a evidência.

| # | Consulta | Esperado | Onde vai |
|---|----------|----------|----------|
| 0.1 | `SELECT count(*) FROM public.autorizacoes;` · `… FROM public.candidatos;` · `… FROM public.candidaturas;` | 17 · 21 · (medir) | `>>> antes:` de `…0001` |
| 0.2 | `SELECT polname, cmd, pg_get_expr(polqual, polrelid) AS qual, pg_get_expr(polwithcheck, polrelid) AS wc FROM pg_policy WHERE polrelid = 'public.autorizacoes'::regclass;` | **3** policies: SELECT own-row do candidato, SELECT do RH ativo, UPDATE own-row com `qual` **e** `with_check` | `>>> antes:` de `…0001` |
| 0.3 | `SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'notificacoes_enviadas_evento_check';` | **6** valores, terminando em `revisao_respondida` | `>>> antes:` de `…0003` |
| 0.4 | `SELECT candidato_id, count(*) FROM public.autorizacoes GROUP BY 1 HAVING count(*) > 1;` | (medir) — responde a questão aberta 1 da pesquisa: `autorizacoes` é 1:1 ou 1:N na prática | SUMMARY |
| 0.5 | `SELECT count(*) FROM public.candidatos c WHERE NOT EXISTS (SELECT 1 FROM public.autorizacoes a WHERE a.candidato_id = c.id);` | **4** — a linha de base de BD-4 | SUMMARY |
| 0.6 | `SELECT count(*) FROM cron.job;` | **3** | asserção negativa do passo 5 |

**Se 0.2 devolver número diferente de 3, ou se o `qual` divergir do medido em 2026-08-01: PARE.**
As 3 policies vivem em PROD e em nenhum arquivo de migration — é a 4ª instância do drift em
aberto. Nenhuma migration desta fase toca policy; a divergência seria sinal de que outra coisa
mexeu, e a lição do 42-07 é que o arquivo não é o objeto vivo.

**Se 0.3 trouxer qualquer cláusula além da lista de eventos: PARE.** O `ADD CONSTRAINT` do
`…0003` tem de preservá-la.

**0.5 é medição, não gatilho de correção. NÃO faça backfill dos 4.** Consentimento retroativo é
fabricar prova, que é o oposto exato do que esta fase entrega. A ausência é ela própria o registro
honesto (BD-4).

Transcreva 0.1, 0.2 e 0.3 nas linhas `>>> antes:` dos cabeçalhos **antes** do apply.

### 1–4. Aplicar, na ordem, uma de cada vez

Para **cada** uma das quatro, na sequência `…0001` → `…0002` → `…0003` → `…0004`:

1. `apply_migration` com o nome sem o prefixo de timestamp (ex.: `p43_consent_prova_e_marketing`).
2. **Reparar a `version` do ledger** para o prefixo do arquivo:
   `UPDATE supabase_migrations.schema_migrations SET version = '<prefixo>' WHERE version = '<carimbo do apply>';`
   `apply_migration` grava um timestamp do MOMENTO do apply, não o prefixo — sem o reparo, um
   `db push` leria a migration como não-aplicada. Medido três vezes na Phase 42.
3. **Provar a fidelidade por md5, não presumir:**
   `SELECT version, md5(statements[1]) FROM supabase_migrations.schema_migrations ORDER BY version DESC LIMIT 5;`
   comparado com `md5 -q supabase/migrations/<arquivo>.sql`. **Têm de bater.** `apply_migration`
   retransmite a migration como STRING e já perdeu blocos de comentário duas vezes neste
   milestone, sem erro e sem warning.
4. Rodar o smoke correspondente numa **ÚNICA** chamada `execute_sql` — `set_config(..., false)` é
   escopado à sessão, e statements espalhados zeram o contador e reprovam por run parcial.

| Migration | Smoke | Esperado |
|-----------|-------|----------|
| `…0001_p43_consent_prova_e_marketing` | `supabase/tests/p43_consent_prova_smoke.sql` | 6/6 PASS. Crítico: contagem de linhas com `consent_text_version IS NOT NULL` = **0**, e `pg_policies` de `autorizacoes` ainda com **3** linhas |
| `…0002_p43_config_retencao` | `supabase/tests/p43_matriz_retencao_smoke.sql` | 10/10 PASS. Crítico: 8 linhas semeadas em 24 meses, zero policy de escrita, guard recusa papel `rh` **e** chamador sem claim, uma linha de auditoria por alteração |
| `…0003_p43_guard_marketing` | `supabase/tests/p43_guard_marketing_smoke.sql` | 9/9 PASS. O apply tem de emitir o `NOTICE` do bloco `DO`; se vier `EXCEPTION`, **leia a mensagem antes de qualquer outra coisa** — se disser que um dos 6 valores vivos sumiu, o REVISAO-01 ou o REVISAO-04 está em risco |
| `…0004_p43_previa_retencao` | `supabase/tests/p43_previa_smoke.sql` | 9/9 PASS. Se o pin de `md5(prosrc)` não bater **e** o md5 do passo 3 tiver batido, a divergência é da EXTRAÇÃO do corpo: atualize o pin UMA vez com o valor medido e registre a discrepância. Nunca afrouxe a asserção |

### 5. Asserções negativas pós-apply — o que NÃO aconteceu

| # | Consulta | Esperado |
|---|----------|----------|
| 5.1 | `SELECT count(*) FROM cron.job;` | **3**, idêntico a 0.6 — esta fase não cria cron nenhum |
| 5.2 | `SELECT count(*) FROM pg_policy WHERE polrelid = 'public.autorizacoes'::regclass;` | **3**, idêntico a 0.2 |
| 5.3 | `SELECT count(*) FROM public.candidatos;` · `… FROM public.candidaturas;` | idênticos a 0.1 — zero ação destrutiva por desenho |
| 5.4 | `SELECT count(*) FROM public.autorizacoes WHERE consent_text_version IS NOT NULL OR consent_text_hash IS NOT NULL OR autorizacao_marketing_vagas IS NOT NULL;` | **0** — nenhum back-fill |
| 5.5 | `SELECT count(*) FROM net._http_response WHERE created > now() - interval '15 minutes';` | **0** novas — nenhum smoke tocou o provedor de e-mail |

Transcreva o resultado de 0.2 e 0.3 também nas linhas `>>> depois:` e confira contra o
`pg_get_constraintdef` / `pg_policy` vivos.
  </action>
  <what-built>
Quatro migrations aditivas escritas e não aplicadas (colunas de prova de consentimento, matriz de
retenção com RPC auditada, guard de marketing no ledger, predicado único + prévia agregada) e
quatro smokes SQL gate-GUC que as provam por execução.
  </what-built>
  <how-to-verify>
Siga os passos 0 a 5 do runbook acima, na ordem. Cole no SUMMARY: as seis medições do passo 0, os
quatro pares `md5(statements[1])` × md5 do arquivo, a saída completa dos quatro smokes, e as cinco
asserções negativas do passo 5.

**Tabela de falhas:**

| Sintoma | Significado | Ação |
|---------|-------------|------|
| 0.2 devolve ≠ 3 policies, ou `qual` divergente | Algo fora do ledger mexeu em `autorizacoes` | **PARE.** Não aplique. Registre em `processo-origem-do-drift-desconhecida` e escale |
| 0.3 traz cláusula além da lista de eventos | O `ADD CONSTRAINT` do `…0003` a perderia | **PARE.** Reescreva o BLOCO C preservando a cláusula |
| md5 do ledger ≠ md5 do arquivo | O apply perdeu conteúdo na retransmissão | **PARE.** Não avance para a próxima migration. Diffe `statements[1]` contra o arquivo e registre no todo do drift |
| `EXCEPTION` no bloco `DO` do `…0003` | O CHECK vivo divergia do esperado | Leia a mensagem; se um dos 6 valores sumiu, um requirement da Phase 42 está em risco |
| Smoke reprova em qualquer asserção negativa | O apply teve efeito colateral | **PARE.** A asserção que reprovou nomeia o quê |
| 5.3 mudou | Uma linha de candidato foi tocada | **INCIDENTE.** Esta fase é zero-destrutiva por desenho |
| 5.4 ≠ 0 | Back-fill silencioso aconteceu | **INCIDENTE.** O SC#1 está destruído; investigue o `DEFAULT` |
  </how-to-verify>
  <verify>
    <automated>for f in supabase/migrations/2026080100000*.sql; do echo "$f $(md5 -q "$f")"; done</automated>
    <human-check>Os quatro pares de md5 batem, os quatro smokes fecham no total exato, e as cinco asserções negativas do passo 5 confirmam que nada além do previsto mudou.</human-check>
  </verify>
  <resume-signal>Cole as seis medições do passo 0, os quatro pares de md5, a saída dos quatro smokes e as cinco asserções negativas. Depois digite "aplicado" ou descreva o que falhou.</resume-signal>
  <done>As quatro migrations estão vivas, com `version` reparada e md5 batendo; os quatro smokes fecharam no total exato; as cinco asserções negativas confirmam cron, policies e contagens de candidato inalterados e zero back-fill.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — deploy da EF `cadastrar-candidato` (DEPOIS das colunas) e regeneração dos tipos</name>
  <action>
**Pré-condição dura: a Task 1 fechou e `20260801000001` está viva.** Deployar a EF que grava
`consent_text_hash` antes de as colunas existirem faria toda uma janela de cadastros gravar
consentimento SEM PROVA — e essas linhas ficariam indistinguíveis das históricas, destruindo o
SC#1 para o período inteiro. Colunas primeiro, sempre.

### 1. Fechar o conjunto de dependências pelos IMPORTS, não pelo `files_modified`

Leia os `import` de `supabase/functions/cadastrar-candidato/index.ts` e, recursivamente, os de
cada módulo local que ele puxa. O fechamento mínimo esperado é: `index.ts`,
`_shared/constants.ts`, `_shared/schemas.ts`, `_shared/consent-hash.ts`,
`_shared/autorizacoes-registro.ts` e `_shared/consent-text.json`. **Confira a lista real** — no
checkpoint da 42-08 a lista do plano tinha quatro arquivos e o fechamento real tinha cinco, e
deployar a lista curta teria quebrado um evento vivo em produção. O fechamento se lê nos
`import` do entrypoint, nunca no `files_modified` de um plano.

⚠ **Se o bundle de deploy não aceitar o arquivo `.json`:** não converta o arquivo à mão aqui. Isso
invalida a suposição A3 do lado do servidor, e a resposta é voltar ao ramo de fallback já
documentado no plano 43-01 (texto espelhado + sonda de igualdade byte-a-byte) e re-executar
aquele plano. Um texto de consentimento improvisado no meio de um deploy é exatamente a
divergência que a fase existe para impedir.

Após o deploy, **confira de volta com `get_edge_function`** que os blocos longos de comentário
voltaram íntegros — é a checagem que descartou a classe de perda do drift P41 nas EFs da Phase 42.

### 2. Smoke que discrimina a versão SEM escrever nada

`POST` para a EF com um corpo que carrega as três chaves NOVAS de autorização e um `email`
deliberadamente malformado:

- **Código novo:** `400 VALIDATION` com `field` apontando para `email` — o schema aceitou
  `autorizacao_marketing_vagas` e reprovou o e-mail.
- **Código antigo:** `400 VALIDATION` apontando para chave não reconhecida — o `.strict()` antigo
  não conhece `autorizacao_marketing_vagas`.

Os dois caminhos são `400` e **nenhum dos dois escreve linha alguma**. É o campo do erro que
discrimina, e é por isso que este smoke é seguro contra um banco de produção.

### 3. Smoke do caminho feliz — decisão do OPERADOR, não do executor

Provar que o hash realmente aterrissa exige um cadastro real. Um cadastro isolado **não** dispara
e-mail (a confirmação é disparada por `INSERT` em `candidaturas`, não pela criação do candidato),
então não há risco de envio. As saídas:

| Opção | O que fazer | Custo |
|-------|-------------|-------|
| **A (recomendada)** | Cadastrar com um endereço interno real do operador, conferir a linha de `autorizacoes` e remover a fixture ao fim | Cria e remove 1 usuário de Auth + 1 candidato + 1 autorização |
| B | Pular o caminho feliz e fechar só com o passo 2 | O hash fica provado por teste unitário e por deploy, não por escrita real; registre a lacuna no SUMMARY |

Se A: confira na linha criada que `consent_text_version` = `v2-2026-08`, que `consent_text_hash`
é igual ao hex pinado em `consent-hash.test.ts`, que `consent_registrado_em` está preenchida, que
`autorizacao_marketing_vagas` é **`false`** (mandado `false` no corpo — a prova de que o servidor
parou de repor `true`) e que **não existe** valor novo em `autorizacao_analise_video`. Teardown:
remova `autorizacoes` → `candidatos` → usuário do Auth, nessa ordem.

Teste também, na mesma janela, o caminho FAIL-CLOSED se for barato fazê-lo sem risco; se não for,
registre que ele está provado só por teste unitário.

### 4. Regenerar `database.types.ts`

`npm run db:types`. ⚠ O script usa `>` que **TRUNCA antes de executar**: rodar "pra ver se
funciona" é destruir o arquivo para descobrir. Gere primeiro contra um arquivo TEMPORÁRIO, confira
que a saída tem as estruturas novas (`autorizacoes` com as 4 colunas, `config_retencao_etapa`,
`classe_evento_notificacao`, e as funções `listar_matriz_retencao`, `salvar_janela_retencao`,
`previa_retencao`, `previa_retencao_total`), e só então aponte para o arquivo trackeado. Se o
`gen types --linked` falhar por estado de link ausente, `supabase link` resolve sem prompt e sem
contato de escrita com PROD (lição do 37-05) — não escale antes de tentar.

Diffe o resultado: espere adições e **zero deleções**. Deleção é sinal de drift lateral.

Commite `database.types.ts` com o hook ativo, zero `--no-verify`, e confirme que
`npm run -s lint 2>&1 | grep -c "error TS"` continua na baseline **97**.
  </action>
  <what-built>
EF `cadastrar-candidato` com hash de consentimento, defaults corrigidos e gravação fail-closed
(plano 43-01), pronta para deploy mas nunca deployada por subagente.
  </what-built>
  <how-to-verify>
Passos 1 a 4 acima, na ordem. Cole no SUMMARY: a lista real de dependências deployadas, a versão
resultante da EF, a resposta do smoke do passo 2 com o campo do erro, o resultado do passo 3 com a
opção escolhida, e o diff resumido de `database.types.ts` (adições/deleções).

**Tabela de falhas:**

| Sintoma | Significado | Ação |
|---------|-------------|------|
| Passo 2 devolve erro em chave não reconhecida | O deploy não pegou o código novo | Redeploy; **não** prossiga para o passo 3 |
| Passo 2 devolve `200` | O corpo malformado passou — o schema não está `.strict()` | **PARE.** É regressão de segurança (D-04/LGPD-01) |
| `get_edge_function` mostra comentários truncados | A retransmissão perdeu conteúdo | Redeploy e confira de novo |
| Bundle recusa o `.json` | Suposição A3 falsa do lado do servidor | Volte ao ramo de fallback do 43-01; não improvise o texto |
| `db:types` traz deleções | Drift lateral | **PARE.** Investigue antes de commitar |
| Contagem `tsc` > 97 | Regressão de tipo | Corrija antes do commit; zero `--no-verify` |
  </how-to-verify>
  <verify>
    <automated>npm run -s lint 2>&1 | grep -c "error TS"</automated>
    <human-check>A EF está na versão nova com o fechamento correto de dependências, o smoke discriminante devolveu erro no campo `email`, e `database.types.ts` foi regenerado com adições e zero deleções.</human-check>
  </verify>
  <resume-signal>Cole a lista de dependências deployadas, a versão da EF, a resposta do smoke do passo 2, o resultado do passo 3 e o diff de tipos. Depois digite "deployado" ou descreva o que falhou.</resume-signal>
  <done>A EF viva grava versão, hash e timestamp do consentimento e não repõe `true`; `database.types.ts` reflete as estruturas novas e está commitado; a contagem `tsc` continua em 97.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT — CONSENT-06: tracking do Resend verificado NO PROVEDOR, com data</name>
  <action>
CONSENT-06 **não é mudança de código.** Tracking de abertura e de clique no Resend é configuração
de DOMÍNIO, não parâmetro de envio: não existe campo de tracking no corpo de `POST /emails`, e
`grep -rni "tracking" supabase/functions/` devolve zero. Alterar o corpo do envio não mudaria nada.
É verificação de provedor, e a ferramenta já existe neste repositório desde a Phase 36.

1. Rode `RESEND_API_KEY=<chave> npm run check:resend-dominio` e **capture o output com a data**.
2. Interprete:

| Saída | Significado | Ação |
|-------|-------------|------|
| `✓ click_tracking: false` **e** `✓ open_tracking: false` | SC#3 fechado por API, com evidência | Cole o output datado no SUMMARY |
| `… not reported by this API version` | **NÃO é passe.** "Não reportado" não é "verificado no provedor", e o SC#3 exige confirmação positiva | Feche por leitura direta do dashboard (registre a data e o que foi visto) ou por `GET /domains/{id}` bruto via `curl` |
| `✗ click_tracking: true` ou `✗ open_tracking: true` | Está ligado | O operador desliga no dashboard e re-roda o reporter. **Não** ligue essa ação ao script: ele é read-only por decisão de segurança, e o único caminho state-changing é `--verify`; erodir essa postura para poupar um clique é o tipo de atalho que esta fase existe para não tomar |

3. Registre o veredito datado no SUMMARY, com o domínio verificado nomeado.

**Ganho colateral a registrar:** isto fecha a metade "tracking desligado" do UAT-36-1, que segue
`partial` desde o encerramento do v7.0.

Nota de escopo: `cost-alerter` também envia por Resend com a chave em env secret da EF
(divergência já rastreada em `36-resend-chave-divergencia`). É e-mail interno de custo, fora do
CONSENT-06 — mas o tracking é por domínio, então se o remetente for o mesmo domínio a configuração
o cobre de graça. Registre qual é o caso.
  </action>
  <what-built>
Nada de código. O reporter `scripts/check-resend-dominio.mjs` já existe desde a Phase 36 e reporta
os dois flags; ele é opt-in, read-only e no-op sem chave.
  </what-built>
  <how-to-verify>
Rode o reporter com a chave e siga a tabela de interpretação. O critério só fecha com confirmação
POSITIVA — flag ausente não conta.
  </how-to-verify>
  <verify>
    <automated>grep -n "click_tracking\|open_tracking" scripts/check-resend-dominio.mjs</automated>
    <human-check>Existe evidência datada de que `click_tracking` e `open_tracking` do domínio remetente estão `false`, vinda da API do Resend ou de leitura direta do dashboard.</human-check>
  </verify>
  <resume-signal>Cole o output datado do reporter (ou a leitura do dashboard com data e o que foi visto). Depois digite "verificado" ou descreva o que estava ligado.</resume-signal>
  <done>O estado dos dois flags de tracking é um fato datado e positivo, registrado no SUMMARY, e a metade correspondente do UAT-36-1 pode ser fechada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arquivo de migration → objeto vivo em PROD | `apply_migration` retransmite SQL como string e já perdeu conteúdo três vezes neste milestone |
| repositório → Edge Function deployada | o fechamento de dependências do deploy não é o `files_modified` do plano; omitir um import quebra função viva em runtime |
| sistema → provedor externo (Resend) | configuração fora do git, que muda sem passar por revisão |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-32 | Tampering | drift no apply | high | mitigate | `md5(statements[1])` × md5 do arquivo após CADA apply, com PARADA obrigatória se divergirem |
| T-43-33 | Repudiation | EF deployada antes das colunas | critical | mitigate | Ordem imposta por pré-condição dura da Task 2 e escrita no cabeçalho da migration `…0001` |
| T-43-34 | Denial of service | deploy com fechamento incompleto | high | mitigate | Fechamento lido dos `import` do entrypoint + conferência de volta por `get_edge_function` (lição 42-08) |
| T-43-35 | Repudiation | back-fill silencioso de consentimento | critical | mitigate | Asserção 5.4 e asserção (b) do smoke `…0001`; medição 0.5 é registro, explicitamente NÃO gatilho de correção |
| T-43-36 | Information disclosure | tracking de clique ligado | medium | mitigate | Verificação positiva no provedor; flag ausente não conta como passe |
| T-43-37 | Denial of service | `db:types` truncando o arquivo | medium | mitigate | Gerar contra arquivo temporário primeiro; o script usa `>` que trunca antes de executar (lição 37-05) |
| T-43-SC | Tampering | instalação de pacotes | low | accept | Nenhum `npm install` neste checkpoint |
</threat_model>

<verification>
- Quatro pares de md5 batendo, quatro smokes no total exato, cinco asserções negativas confirmadas.
- EF na versão nova, com o fechamento de dependências conferido de volta.
- `database.types.ts` regenerado com adições e zero deleções, commitado com o hook ativo.
- Veredito datado do tracking do Resend.
- Zero `--no-verify` em qualquer commit deste checkpoint.
</verification>

<success_criteria>
- As quatro estruturas estão vivas em PROD com fidelidade provada, e nada de candidato foi tocado.
- A ordem colunas → Edge Function foi respeitada e é demonstrável pela sequência do runbook.
- Os planos 43-08 e 43-09 estão desbloqueados: os tipos existem.
- CONSENT-06 tem confirmação positiva e datada no provedor.
</success_criteria>

<output>
Create `.planning/phases/43-consentimentos-honestos-pol-tica-de-reten-o/43-07-SUMMARY.md` when done.
Registre também a resposta da medição 0.4 (`autorizacoes` é 1:1 ou 1:N na prática) — ela é questão
aberta da pesquisa e informa a leitura own-row do plano 43-08.
</output>
