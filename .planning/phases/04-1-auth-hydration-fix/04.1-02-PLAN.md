---
phase: 4.1
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/store/authStore.ts
  - src/App.tsx
  - src/components/RoleGuard.tsx
  - src/features/auth/utils/waitForCandidatoHydrated.ts
  - src/features/auth/utils/index.ts
autonomous: true
requirements:
  - CAD-06
  - AUTH-01
  - VAGA-03
  - CAND-01
  - CAND-02
  - CAND-03
gap_closure_targets:
  - INT-BLOCKER-1
  - INT-BLOCKER-2
  - INT-WARNING-3
must_haves:
  truths:
    - "After SIGNED_IN/TOKEN_REFRESHED/USER_UPDATED/PASSWORD_RECOVERY events, useAuthStore.getState().candidato becomes populated within ~3s without a page reload"
    - "The App.tsx onAuthStateChange callback wraps async work in setTimeout(..., 0) (Pitfall 2 deadlock prevention)"
    - "When isAuthenticated && role === null, RoleGuard triggers initialize() ONCE and renders a spinner (no infinite redirect loop)"
    - "Vitest authStore tests transition from RED to GREEN; RoleGuard one-shot test transitions from RED to GREEN"
    - "waitForCandidatoHydrated() utility exists at src/features/auth/utils/waitForCandidatoHydrated.ts and returns Promise<boolean>"
  artifacts:
    - path: "src/store/authStore.ts"
      provides: "hydrateFromSession action that populates profile + candidato + adminUser based on session + JWT role + DB fallback"
      contains: "hydrateFromSession"
    - path: "src/App.tsx"
      provides: "onAuthStateChange listener with setTimeout(0) wrapper + hydrateFromSession dispatch + explicit PASSWORD_RECOVERY case"
      contains: "setTimeout"
    - path: "src/components/RoleGuard.tsx"
      provides: "fallbackTriedRef one-shot DB fallback when isAuthenticated && role === null"
      contains: "fallbackTriedRef"
    - path: "src/features/auth/utils/waitForCandidatoHydrated.ts"
      provides: "Async polling utility that resolves true when candidato hydrated, false on hard timeout (3000ms default)"
      contains: "export async function waitForCandidatoHydrated"
    - path: "src/features/auth/utils/index.ts"
      provides: "Re-export of waitForCandidatoHydrated alongside existing extractRole + rememberMeStorage"
      contains: "waitForCandidatoHydrated"
  key_links:
    - from: "src/App.tsx"
      to: "useAuthStore.getState().hydrateFromSession"
      via: "onAuthStateChange callback inside setTimeout(0)"
      pattern: "hydrateFromSession"
    - from: "src/store/authStore.ts hydrateFromSession"
      to: "fetchProfile (private) for DB fallback"
      via: "shared helper called by both initialize() and hydrateFromSession()"
      pattern: "fetchProfile"
    - from: "src/components/RoleGuard.tsx fallback path"
      to: "useAuthStore.getState().initialize"
      via: "one-shot ref guard"
      pattern: "fallbackTriedRef"
---

<objective>
Implementa o core fix de Phase 4.1 (3 patterns): hydrating listener + JWT role-null fallback + new utility. Adiciona uma nova action `hydrateFromSession` (async + fetchProfile) ao authStore, reescreve o listener em App.tsx para usar o pattern oficial Supabase `setTimeout(0)`, e adiciona um one-shot DB fallback ao RoleGuard que previne loop de redirect quando JWT não emite role.

Purpose: Fecha INT-BLOCKER-1, INT-BLOCKER-2 (a fix raiz) e INT-WARNING-3 (defesa). Esta é a única plan que toca a auth surface central — Plan 03 adiciona defense-in-depth nos submit handlers.

Output: 1 nova action no store, 1 listener reescrito, 1 guard atualizado, 1 utility nova, 1 export adicionado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-1-auth-hydration-fix/04.1-RESEARCH.md
@.planning/phases/04-1-auth-hydration-fix/04.1-PATTERNS.md
@.planning/phases/04-1-auth-hydration-fix/04.1-VALIDATION.md
@.planning/phases/04-1-auth-hydration-fix/04.1-01-PLAN.md
@./CLAUDE.md

# Source files to modify
@src/store/authStore.ts
@src/App.tsx
@src/components/RoleGuard.tsx

# Reference utility (analog for new file structure)
@src/features/auth/utils/extractRole.ts
@src/features/auth/utils/index.ts

<interfaces>
<!-- Key types/exports the executor will need (extracted from codebase). -->

From src/store/authStore.ts (PRE-Phase-4.1 shape):
```
// Existing private helper (lines 136-203 — REUSE, do NOT duplicate):
async function fetchProfile(userId: string, jwtRole: Role | null): Promise<{
  profile: Record<string, unknown> | null
  resolvedRole: Role
}>

// Existing private helper (lines 117-130 — preserve):
function permissionsFromRole(role: Role | null): Record<string, boolean> | null

// Existing actions (preserve unchanged):
initialize: () => Promise<void>           // lines 296-340 — full hydration on mount
setSession: (session: Session | null) => void   // lines 284-294 — sync, JWT-decode only
clearAuth: () => void                     // lines 270-282
logout: () => Promise<void>               // lines 342-349
```

From src/App.tsx (PRE-Phase-4.1 shape, lines 152-186):
```
useEffect(() => {
  const { initialize, setSession, clearAuth } = useAuthStore.getState()
  let cancelled = false
  ;(async () => {
    await initialize()
    if (cancelled) return
    const { data: { subscription } } = supabase.auth.onAuthStateChange(
      (event, session) => {
        if (event === 'SIGNED_IN' || event === 'TOKEN_REFRESHED' || event === 'USER_UPDATED') {
          setSession(session)
        } else if (event === 'SIGNED_OUT') {
          clearAuth()
        }
      }
    )
    subscriptionRef.current = subscription
  })()
  return () => { cancelled = true; subscriptionRef.current?.unsubscribe() }
}, [])
```

From src/components/RoleGuard.tsx (PRE-Phase-4.1, lines 117-126):
```
// 2. Nao autenticado ou sem role -> redireciona para login preservando destino
if (!isAuthenticated || currentRole === null) {
  const redirectTo = encodeURIComponent(location.pathname + location.search)
  return <Navigate to={`/auth/login?redirect=${redirectTo}`} replace />
}
```

From src/features/auth/utils/index.ts (extend):
```
// Existing exports:
//   extractRole, type Role, rememberMeStorage
// Phase 4.1 ADD:
export { waitForCandidatoHydrated } from './waitForCandidatoHydrated'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hydrateFromSession action + create waitForCandidatoHydrated utility</name>
  <files>
    src/store/authStore.ts,
    src/features/auth/utils/waitForCandidatoHydrated.ts,
    src/features/auth/utils/index.ts
  </files>
  <read_first>
    src/store/authStore.ts,
    src/features/auth/utils/index.ts,
    src/features/auth/utils/extractRole.ts,
    .planning/phases/04-1-auth-hydration-fix/04.1-PATTERNS.md,
    .planning/phases/04-1-auth-hydration-fix/04.1-RESEARCH.md
  </read_first>
  <behavior>
    - Test: hydrateFromSession(null) resets all state to logged-out shape (skips fetchProfile)
    - Test: hydrateFromSession(session_with_candidato_role) populates state.candidato + state.role === 'candidato' + state.isAuthenticated === true
    - Test: hydrateFromSession(session_without_role_claim) runs fetchProfile fallback chain (queries usuarios_rh THEN candidatos)
    - Test: hydrateFromSession does NOT toggle isLoading=true mid-execution (Pitfall 6)
    - Test: hydrateFromSession does NOT emit any console.* output (Pitfall 7)
    - Test: waitForCandidatoHydrated returns true within ~50ms when candidato is already populated
    - Test: waitForCandidatoHydrated returns false within timeoutMs when role stays null indefinitely
    - Test: waitForCandidatoHydrated returns false immediately when role === 'rh' (RH/admin path)
  </behavior>
  <action>
**Step 1 — Add hydrateFromSession to AuthState interface** in `src/store/authStore.ts`

Localize a interface `AuthState` (linhas ~75-114). Adicione UMA nova entrada de método logo após `setSession` (linha ~88):

```
  hydrateFromSession: (session: Session | null) => Promise<void>
```

Inclua docstring completa em pt-BR explicando: (1) diferença vs setSession (sync vs async + fetchProfile), (2) fecha INT-BLOCKER-1+2, (3) referência a RESEARCH §Pattern 1.

**Step 2 — Implement hydrateFromSession action** em `src/store/authStore.ts`

Localize a implementação de `setSession` (linhas 284-294) e a de `initialize` (linhas 296-340). Adicione `hydrateFromSession` IMEDIATAMENTE APÓS `initialize`. NÃO modifique `setSession` nem `initialize`. NÃO duplique a função privada `fetchProfile`. NÃO adicione `set({ isLoading: true })` dentro de `hydrateFromSession` (Pitfall 6 — re-trigger spinner).

Implementação completa:

```
      hydrateFromSession: async (session) => {
        try {
          if (!session?.user) {
            get().clearAuth()
            return
          }

          const roleFromJwt = extractRole(session)
          const { profile, resolvedRole } = await fetchProfile(session.user.id, roleFromJwt)

          set({
            user: session.user,
            session,
            role: resolvedRole,
            profile,
            candidato:
              resolvedRole === 'candidato'
                ? (profile as CandidatoRow | null)
                : null,
            adminUser:
              resolvedRole === 'rh' || resolvedRole === 'administrador'
                ? (profile as UsuarioRHRow | null)
                : null,
            permissions: permissionsFromRole(resolvedRole),
            isAdmin: resolvedRole === 'administrador',
            isAuthenticated: true,
          })
        } catch {
          // Pitfall 7: zero console.*. Hydration failure → keep last known good state;
          // RoleGuard will eventually bounce if needed.
        }
      },
```

**IMPORTANTE — preservação:**
- `setSession` (linhas 284-294) permanece intocado — legacy alias `setAdminSession` (linha 360-363) continua funcionando.
- `initialize` (linhas 296-340) permanece intocado — chamado UMA vez no App mount.
- `partialize` (linhas 439-444) permanece intocado — `profile`/`candidato` continuam EXCLUÍDOS de localStorage (security: state leak across tabs).
- Zero `console.*` adicionados (Pitfall 7).

**Step 3 — Create utility** em `src/features/auth/utils/waitForCandidatoHydrated.ts`

Crie arquivo novo com este conteúdo EXATO:

```
/**
 * waitForCandidatoHydrated — Phase 4.1 Pattern 2 (defense in depth)
 *
 * Aguarda candidato (legacy alias for profile when role==='candidato')
 * ser populado no store. Usado por submit handlers que precisam navegar
 * para uma rota dependente de candidato.id.
 *
 * Defense in depth: complementa Pattern 1 (listener centralized hydration).
 * O listener é assíncrono via setTimeout(0); submit handlers podem chamar
 * navigate() ANTES de fetchProfile resolver. Este helper bloqueia até o
 * store reportar candidato !== null OU hard timeout disparar.
 *
 * @param timeoutMs hard cap to avoid hanging forever (default 3000ms)
 * @returns
 *   - true se candidato hidratou na janela
 *   - false em timeout OU se role autenticou como rh/administrador
 *
 * @see RESEARCH §Pattern 2 + §Pitfall 5
 * @see PATTERNS.md §Pattern C
 *
 * Pitfall 7: zero console.* — utility lives under src/features/auth which
 * is recursively scanned by pitfall7.grep.test.ts PHASE_3_AUTH_PATHS.
 *
 * @module features/auth/utils/waitForCandidatoHydrated
 */
import { useAuthStore } from '@/store/authStore'

export async function waitForCandidatoHydrated(
  { timeoutMs = 3000 }: { timeoutMs?: number } = {}
): Promise<boolean> {
  const start = Date.now()
  while (Date.now() - start < timeoutMs) {
    const { candidato, role } = useAuthStore.getState()
    if (candidato && role === 'candidato') return true
    if (role && role !== 'candidato') return false
    await new Promise((r) => setTimeout(r, 50))
  }
  return false
}
```

**Step 4 — Re-export** em `src/features/auth/utils/index.ts`

Adicione UMA linha de re-export, preservando os exports existentes:

```
export { waitForCandidatoHydrated } from './waitForCandidatoHydrated'
```

**Step 5 — Run authStore Vitest scaffold (created in Plan 01)**

Após salvar os arquivos, rode `npm run test:run -- src/store/__tests__/authStore.test.ts`. Os testes do `describe('hydrateFromSession')` devem passar GREEN. O teste `describe('Pitfall 7')` também deve passar GREEN.

Pitfall 7 enforcement: zero `console.*` em qualquer dos 3 arquivos editados/criados.
  </action>
  <verify>
    <automated>npm run test:run -- src/store/__tests__/authStore.test.ts src/features/auth/utils/__tests__/pitfall7.grep.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hydrateFromSession" src/store/authStore.ts` returns >= 2 (interface + implementation)
    - File `src/features/auth/utils/waitForCandidatoHydrated.ts` exists; `grep -q "export async function waitForCandidatoHydrated" src/features/auth/utils/waitForCandidatoHydrated.ts` exits 0
    - `grep -c "waitForCandidatoHydrated" src/features/auth/utils/index.ts` returns 1
    - `npm run test:run -- src/store/__tests__/authStore.test.ts` exits 0 (all hydrateFromSession + Pitfall 7 tests GREEN)
    - `npm run test:run -- src/features/auth/utils/__tests__/pitfall7.grep.test.ts` exits 0
    - `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' src/features/auth/utils/waitForCandidatoHydrated.ts | grep -c "console\." returns 0
    - `npm run lint 2>&1 | grep -c "error TS"` outputs `<= 320` (baseline preserved)
    - The line `setSession: (session) => {` still exists in `src/store/authStore.ts` (preserve legacy action)
  </acceptance_criteria>
  <done>
    hydrateFromSession action exists on useAuthStore, populates candidato/profile via fetchProfile; waitForCandidatoHydrated utility exists and re-exported; authStore Vitest scaffold all GREEN; Pitfall 7 grep still GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite App.tsx onAuthStateChange listener with setTimeout(0) + hydrateFromSession dispatch</name>
  <files>
    src/App.tsx
  </files>
  <read_first>
    src/App.tsx,
    src/store/authStore.ts,
    .planning/phases/04-1-auth-hydration-fix/04.1-PATTERNS.md,
    .planning/phases/04-1-auth-hydration-fix/04.1-RESEARCH.md
  </read_first>
  <behavior>
    - Grep: listener callback contains `setTimeout(`
    - Grep: listener calls `useAuthStore.getState().hydrateFromSession(session)`
    - Grep: listener handles `PASSWORD_RECOVERY` event explicitly (per RESEARCH Q4)
    - Grep: SIGNED_OUT path runs synchronously (NOT inside setTimeout) to avoid flash-of-unauth-state
    - Behavioral: app boots; initial initialize() still runs on mount; subsequent SIGNED_IN populates candidato within ~3s without page reload
  </behavior>
  <action>
Reescreva o useEffect em `src/App.tsx`. Mantenha a estrutura geral (mount-once + cleanup); altere APENAS o callback de `onAuthStateChange`.

**Step 1 — Reescrever o useEffect** (substituir linhas 152-186)

ANTES:
```
useEffect(() => {
  const { initialize, setSession, clearAuth } = useAuthStore.getState()
  let cancelled = false
  ;(async () => {
    await initialize()
    if (cancelled) return
    const { data: { subscription } } = supabase.auth.onAuthStateChange(
      (event, session) => {
        if (event === 'SIGNED_IN' || event === 'TOKEN_REFRESHED' || event === 'USER_UPDATED') {
          setSession(session)
        } else if (event === 'SIGNED_OUT') {
          clearAuth()
        }
      }
    )
    subscriptionRef.current = subscription
  })()
  return () => { cancelled = true; if (subscriptionRef.current) { subscriptionRef.current.unsubscribe(); subscriptionRef.current = null } }
}, [])
```

DEPOIS:
```
  useEffect(() => {
    const { initialize, clearAuth } = useAuthStore.getState()

    let cancelled = false

    ;(async () => {
      // Inicializa o store unificado checando sessão existente.
      await initialize()
      if (cancelled) return

      // Phase 4.1 (RESEARCH §Pattern 1):
      //   Async work inside onAuthStateChange MUST be deferred via setTimeout(0)
      //   to prevent supabase-js Web Lock deadlock.
      //   Citation: https://supabase.com/docs/reference/javascript/initializing
      //             https://github.com/supabase/auth-js/issues/762
      //   Sync work (clearAuth) runs immediately to avoid flash-of-unauth-state.
      const { data: { subscription } } = supabase.auth.onAuthStateChange(
        (event, session) => {
          // Sync path — NEVER inside setTimeout (would create flash of unauth state).
          if (event === 'SIGNED_OUT') {
            clearAuth()
            return
          }

          // Async path — defer to next tick so Web Lock releases.
          setTimeout(() => {
            if (
              event === 'SIGNED_IN' ||
              event === 'TOKEN_REFRESHED' ||
              event === 'USER_UPDATED' ||
              event === 'PASSWORD_RECOVERY'
            ) {
              void useAuthStore.getState().hydrateFromSession(session)
            }
          }, 0)
        }
      )

      subscriptionRef.current = subscription
    })()

    // Cleanup: remover listener quando componente desmontar
    return () => {
      cancelled = true
      if (subscriptionRef.current) {
        subscriptionRef.current.unsubscribe()
        subscriptionRef.current = null
      }
    }
  }, [])
```

**IMPORTANTE — preservação:**
- `subscriptionRef` declarada na linha 147 — preservar.
- `useSessionTimeout(isAdminAuthenticated)` na linha 150 — preservar.
- Cleanup function inalterado.
- `initialize` ainda chamado UMA vez no mount.
- Variável `setSession` removida da desestruturação — não é mais chamada aqui (`hydrateFromSession` substitui).
- `import { useIsAdminAuthenticated } from './store/adminAuthStore'` (linha 26) permanece — Plan 04 cuidará dessa migração.

**Step 2 — Atualizar a doc-string do RootLayout** (linhas 135-144)

Substitua o bloco de docstring atual por:

```
 * Layout raiz que envolve todas as páginas
 *
 * Responsabilidades:
 * - Inicializar o store de auth unificado UMA única vez.
 * - Registrar UM único listener supabase.auth.onAuthStateChange que despacha
 *   para hydrateFromSession (assíncrono, via setTimeout(0) per RESEARCH §Pattern 1)
 *   ou clearAuth (síncrono) no store unificado. Phase 4.1 fix de
 *   INT-BLOCKER-1+2: hydrateFromSession chama fetchProfile para popular
 *   candidato/profile após qualquer SIGNED_IN/TOKEN_REFRESHED/USER_UPDATED/
 *   PASSWORD_RECOVERY.
 * - Montar o DevNavigationMenu apenas em ambiente de desenvolvimento.
 * - Renderizar o Toaster de notificações.
 */
```

Pitfall 7: App.tsx não está em `PHASE_3_AUTH_PATHS`; não adicionar novos `console.*` mesmo assim.
  </action>
  <verify>
    <automated>grep -c "setTimeout" src/App.tsx; grep -c "hydrateFromSession" src/App.tsx; grep -c "PASSWORD_RECOVERY" src/App.tsx; grep -c "supabase.auth.onAuthStateChange" src/App.tsx; npm run lint 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setTimeout" src/App.tsx` returns >= 1
    - `grep -c "hydrateFromSession" src/App.tsx` returns >= 1
    - `grep -c "PASSWORD_RECOVERY" src/App.tsx` returns >= 1
    - `grep -c "useAuthStore.getState().hydrateFromSession" src/App.tsx` returns >= 1
    - `grep -c "supabase.auth.onAuthStateChange" src/App.tsx` returns 1 (still single listener — no second subscription regression)
    - SIGNED_OUT branch is OUTSIDE the setTimeout block — verified by `awk '/onAuthStateChange/,/}, 0\)/' src/App.tsx | grep -B 5 "SIGNED_OUT" | grep -c "setTimeout"` returns 0 OR by manual code review
    - `npm run lint 2>&1 | grep -c "error TS"` outputs `<= 320` (baseline preserved)
    - `npm run build` exits 0 (production build still passes)
    - `npm run test:run -- src/components/__tests__/RoleGuard.test.tsx` runs (RoleGuard test untouched in this task)
  </acceptance_criteria>
  <done>
    App.tsx listener uses setTimeout(0) wrapper, dispatches hydrateFromSession for all 4 auth events including PASSWORD_RECOVERY; SIGNED_OUT runs sync; build and lint baseline preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add RoleGuard one-shot DB fallback for role===null (INT-WARNING-3 defense)</name>
  <files>
    src/components/RoleGuard.tsx
  </files>
  <read_first>
    src/components/RoleGuard.tsx,
    src/store/authStore.ts,
    src/components/__tests__/RoleGuard.test.tsx,
    .planning/phases/04-1-auth-hydration-fix/04.1-PATTERNS.md
  </read_first>
  <behavior>
    - Test: when isAuthenticated && role === null, RoleGuard calls useAuthStore.getState().initialize() exactly ONCE per component instance
    - Test: when isAuthenticated && role === null, RoleGuard renders Loader2 spinner (NOT Navigate to login) on first render
    - Test: when not authenticated (existing behavior), RoleGuard still redirects to /auth/login (no regression)
    - Test: when role matches, RoleGuard renders children (no regression)
  </behavior>
  <action>
Edite `src/components/RoleGuard.tsx`. Adicione um `useRef` para o one-shot guard e divida a condição "não autenticado || role===null" em 2 paths separados.

**Step 1 — Adicione novo ref junto a `toastFiredRef`** (logo após linha 91)

ANTES (linha 91):
```
  const toastFiredRef = useRef(false)
```

DEPOIS:
```
  const toastFiredRef = useRef(false)
  // Phase 4.1 — Pattern 3 (RESEARCH §Pattern 3 / INT-WARNING-3 defense).
  // Quando JWT app_metadata.role não é emitido, role==null. Em vez de
  // redirect direto a /auth/login (que cria loop, login é bem-sucedido com
  // role ainda null), tentamos UM one-shot initialize() que tem fallback DB
  // via fetchProfile (queries usuarios_rh + candidatos com RLS).
  const fallbackTriedRef = useRef(false)
```

**Step 2 — Substituir o bloco "2. Não autenticado ou sem role"** (linhas 117-121)

ANTES (linhas 117-121):
```
  // 2. Nao autenticado ou sem role -> redireciona para login preservando destino
  if (!isAuthenticated || currentRole === null) {
    const redirectTo = encodeURIComponent(location.pathname + location.search)
    return <Navigate to={`/auth/login?redirect=${redirectTo}`} replace />
  }
```

DEPOIS — substitua por DOIS blocos sequenciais (2a auth-only + 2b role-null fallback):

```
  // 2a. Nao autenticado -> redireciona para login preservando destino
  if (!isAuthenticated) {
    const redirectTo = encodeURIComponent(location.pathname + location.search)
    return <Navigate to={`/auth/login?redirect=${redirectTo}`} replace />
  }

  // 2b. Authenticated mas role===null -> tentar one-shot DB fallback antes de bouncar.
  //     Previne INT-WARNING-3 redirect loop quando JWT custom hook nao emite
  //     app_metadata.role. fetchProfile (via initialize) consulta usuarios_rh
  //     + candidatos com RLS.
  if (currentRole === null) {
    if (!fallbackTriedRef.current) {
      fallbackTriedRef.current = true
      void useAuthStore.getState().initialize()
      return (
        <LoadingDelay delay={200}>
          <div className="min-h-screen flex items-center justify-center">
            <Loader2 className="h-8 w-8 animate-spin text-primary" />
          </div>
        </LoadingDelay>
      )
    }
    // Fallback ja tentado e ainda role===null -> bounce to login (last resort)
    const redirectTo = encodeURIComponent(location.pathname + location.search)
    return <Navigate to={`/auth/login?redirect=${redirectTo}`} replace />
  }
```

**IMPORTANTE — preservação:**
- Bloco "1. Loading" (linhas 107-115) inalterado — Pitfall 6 não regredido.
- Bloco "3. Role incorreto" (linhas 124-126) inalterado.
- Bloco "4. Role correto" (linhas 128-130) inalterado.
- `toastFiredRef` e seu `useEffect` (linhas 99-104) preservados.
- Imports (linhas 18-23) já incluem `useRef`, `useAuthStore`, `LoadingDelay`, `Loader2` — não adicionar nada.
- Pitfall 7: zero novos `console.*` (RoleGuard agora está em `PHASE_3_AUTH_PATHS` por Plan 01 Task 2).

**Step 3 — Run RoleGuard Vitest scaffold (created in Plan 01)**

Após salvar, rode `npm run test:run -- src/components/__tests__/RoleGuard.test.tsx`. Os 3 testes devem passar GREEN: (a) one-shot fallback chamado quando role===null, (b) redirect quando não autenticado, (c) renders children quando role matches.
  </action>
  <verify>
    <automated>npm run test:run -- src/components/__tests__/RoleGuard.test.tsx src/features/auth/utils/__tests__/pitfall7.grep.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fallbackTriedRef" src/components/RoleGuard.tsx` returns >= 2 (declaration + check + assignment)
    - `grep -c "useAuthStore.getState().initialize" src/components/RoleGuard.tsx` returns >= 1
    - `grep -c "if (currentRole === null)" src/components/RoleGuard.tsx` returns >= 1
    - The original combined check `if (!isAuthenticated || currentRole === null)` is REMOVED — `grep -c "!isAuthenticated || currentRole === null" src/components/RoleGuard.tsx` returns 0
    - `npm run test:run -- src/components/__tests__/RoleGuard.test.tsx` exits 0 (all 3 tests GREEN)
    - `npm run test:run -- src/features/auth/utils/__tests__/pitfall7.grep.test.ts` exits 0 (zero new console.* in RoleGuard.tsx)
    - `npm run lint 2>&1 | grep -c "error TS"` outputs `<= 320` (baseline preserved)
    - The `if (isLoading)` block at the start of the component (lines 107-115) is unchanged (Pitfall 6 — preserve LoadingDelay 200ms gate)
  </acceptance_criteria>
  <done>
    RoleGuard has fallbackTriedRef one-shot guard; when role===null, calls initialize() once and renders spinner; second-render fallback bounces to login; existing tests still pass; Pitfall 7 grep still GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Web Lock ↔ supabase-js auth callbacks | Async work inside onAuthStateChange enters the same Web Lock holding the current operation; setTimeout(0) defers to next tick |
| Client (extractRole) ↔ Supabase server (RLS) | extractRole decodes JWT claim only (no signature verification); RLS validates real signatures server-side |
| RoleGuard fallback ↔ DB tables (usuarios_rh, candidatos) | Fallback re-runs initialize() which queries via supabase-js anon client; RLS gates ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.1-01 | Tampering | hydrateFromSession action | mitigate | Reuses existing `fetchProfile` (lines 136-203) which queries `usuarios_rh` + `candidatos` via supabase-js anon client → RLS validates ownership server-side; client cannot inject role |
| T-4.1-02 | Denial of Service | App.tsx onAuthStateChange | mitigate | `setTimeout(..., 0)` defer pattern (Supabase official + Pitfall 2) prevents Web Lock deadlock; `void useAuthStore.getState().hydrateFromSession(session)` discards promise so listener doesn't accumulate pending awaits |
| T-4.1-03 | Elevation of Privilege | RoleGuard fallback | mitigate | `fallbackTriedRef` ONE-SHOT prevents loop; fallback calls `initialize()` which uses `fetchProfile` (RLS-gated); if user not in usuarios_rh nor candidatos, fetchProfile returns role='candidato' default per existing logic — RLS still gates subsequent selects (no privilege widening) |
| T-4.1-04 | Information Disclosure | hydrateFromSession + utility | mitigate | Pitfall 7: zero `console.*` in authStore + waitForCandidatoHydrated; both files in `PHASE_3_AUTH_PATHS` recursive scan |
| T-4.1-09 | Information Disclosure | partialize for localStorage | accept | `partialize` (authStore.ts:439-444) NEVER persists `profile`/`candidato` — preserve unchanged in this plan; cross-tab leak prevented |
| T-4.1-10 | Spoofing | extractRole (no signature verification) | accept | Client-side decode is UX-only; RLS server-side validates JWT signature on every query (out of Phase 4.1 scope) |
</threat_model>

<verification>
After Plan 02 completes:
1. `npm run test:run` exits 0 (all hydrateFromSession + RoleGuard + Pitfall 7 tests GREEN)
2. `npm run build` exits 0 (production build green)
3. `npm run lint 2>&1 | grep -c "error TS"` outputs `<= 320` (baseline preserved)
4. `grep -c "hydrateFromSession\|setTimeout\|fallbackTriedRef" src/store/authStore.ts src/App.tsx src/components/RoleGuard.tsx` returns >= 4 across the 3 files
5. Manual smoke: `npm run dev` boots without runtime errors; SIGNED_IN flow via DevTools shows candidato hydrated within 3s
</verification>

<success_criteria>
- [ ] `hydrateFromSession` exists in AuthState interface + implementation
- [ ] `waitForCandidatoHydrated` utility exists at `src/features/auth/utils/waitForCandidatoHydrated.ts` and re-exported from index.ts
- [ ] App.tsx listener uses setTimeout(0) wrapper + handles 4 events (SIGNED_IN, TOKEN_REFRESHED, USER_UPDATED, PASSWORD_RECOVERY) + SIGNED_OUT runs sync
- [ ] RoleGuard has fallbackTriedRef one-shot guard for role===null
- [ ] All Vitest tests created in Plan 01 transition from RED to GREEN
- [ ] tsc baseline preserved at <= 320 errors
- [ ] Production build (npm run build) passes
- [ ] Pitfall 7 grep still GREEN (zero new console.* in touched paths)
</success_criteria>

<output>
After completion, create `.planning/phases/04-1-auth-hydration-fix/04.1-02-SUMMARY.md` summarizing:
- 3 files modified + 1 created + 1 extended
- RED → GREEN transitions (which tests flipped)
- Manual smoke result (`npm run dev` + DevTools observation)
- Any deviations from PATTERNS.md analogs
</output>
