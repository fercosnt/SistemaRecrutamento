---
phase: 4.1
slug: auth-hydration-fix-inserted
created: 2026-04-26
verified: 2026-04-27
status: verified
nyquist_compliant: true
sign_off_by: fernando
sign_off_at: 2026-04-27
---

# Phase 4.1 — Verification Artifact

> Phase verification artifact closing INT-BLOCKER-1+2 + INT-WARNING-2+3 +
> 3 broken flows from `v1.0-MILESTONE-AUDIT.md`. Materializes the
> smoke-runtime gate (Phase 4 lição central D-25..D-28).
>
> **Status:** `verified` — all 6 ROADMAP success criteria evidenced via
> autonomous battery + UAT 3/3 PASS + Scenario 4 SKIPPED (within plan
> criterion: SC-4 Playwright autonomous evidence sufficient). UAT runbook
> committed at `c4c7080` (`chore(04.1-05-uat): UAT 3/3 PASS + 1 SKIPPED —
> UAT-APPROVED`). Phase 4.1 plan execution closed 5/5.

## Success Criteria — ROADMAP Phase 4.1

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | After /cadastro 4-step + auto-login, navigation lands on /candidato/perfil with candidato populated (no full-page reload) | ✓ verified | Autonomous: Plan 04.1-03 commit `aec3e27` (`CadastroMultiStepForm.tsx` Step 4 awaits `waitForCandidatoHydrated({ timeoutMs: 3000 })` after `tryAutoLogin` resolves true, BEFORE navigate to `/candidato/perfil`); 4/4 hydrateFromSession tests GREEN in `src/store/__tests__/authStore.test.ts`; SC-1 Playwright stub in `e2e/auth-hydration.spec.ts:50` (env-gated `E2E_REAL_LOGIN=1`). **UAT Scenario 1 PASS (2026-04-27, Fernando):** fresh cadastro 4-step → tryAutoLogin → `/candidato/perfil` with `nome_completo` visible end-to-end without F5 — candidato_id `eb718a32-9ba6-4f83-a49e-f2d39c0b1566`, userId `95bc75ff-dc45-453e-b9e1-c92698714e06`, email `paulista@beautysmile.com.br`, nome `Joao Jose`. Edge Function `[CADASTRO] Cadastro concluído com sucesso via Edge Function` log confirmed. Zero red console errors. |
| 2 | Anon /vagas/:slug → Candidatar → /auth/login?redirect=... → login → /candidato/candidatura/formulario/:slug with candidato populated; submit handler does NOT silent-return | ✓ verified | Autonomous: Plan 04.1-03 commits `aec3e27` (`LoginCandidatoPage.tsx` awaits hydration before navigate to redirect target) + `1534b45` (`FormularioCandidaturaPage.tsx:233` silent return REPLACED by 3 distinct pt-BR toasts: 'Sessão ainda carregando, tente novamente em instantes' / 'Anexe seu currículo antes de enviar' / 'Vaga não encontrada. Atualize a página.'; submit button now has inline `disabled={!candidato || !cvFile || cvUploading || form.formState.isSubmitting}`); SC-2 Playwright env-gated (E2E_REAL_LOGIN=1) in `e2e/auth-hydration.spec.ts:63`; B-J12 Playwright env-gated in `e2e/candidatura-submit.spec.ts:158`. **UAT Scenario 2 PASS (2026-04-27, Fernando):** anon `/vagas` → vaga active → "Candidatar-se" → `/auth/login?redirect=...` → login (`fernando@beautysmile.com.br`) → `/candidato/candidatura/formulario/<slug>` rendered → CV upload + perguntas → submit ran end-to-end (silent-fail bug confirmed FIXED — Plan 04.1-03 commit `1534b45` validated live) → `/candidato/perfil`. Console redacted shape preserved (Pitfall 7): `[AUTH] signIn invoked` + `[CV] upload invoked` + `[CANDIDATURA] submit invoked`. THE PRIMARY BUG that motivated Phase 4.1. |
| 3 | Recovery deeplink → setNewPassword → /candidato/perfil with candidato populated | ✓ verified | Autonomous: Plan 04.1-03 commit `aec3e27` (`RedefinirSenhaPage.tsx` awaits hydration in BOTH happy path + Pitfall 2 fallback after `tryAutoLogin`); SC-3 Playwright unconditional GREEN (`e2e/auth-hydration.spec.ts:93` — addInitScript pre-seeds candidato session, no env vars needed). **UAT Scenario 3 PASS (2026-04-27, Fernando):** Gmail round-trip end-to-end PASS — `/auth/esqueci-senha` → email link click → `/auth/redefinir-senha` → setNewPassword → `/candidato/perfil` with candidato hydrated. 1 transient 422 on first attempt (likely Supabase Auth rejection — password matches current OR weak-password) recovered on retry; flow itself perfect ("fluxo perfeito" — Fernando). Side finding F-04.1-E captured for UX clarity (Phase 5 backlog). |
| 4 | JWT app_metadata.role missing → no infinite redirect loop on protected routes | ✓ verified | Autonomous: Plan 04.1-02 commit `4d9fa25` (`RoleGuard.tsx` adds `fallbackTriedRef` one-shot DB fallback when `isAuthenticated && role === null`; on first render: spinner + `void useAuthStore.getState().initialize()`; on second render with same state: bounce to `/auth/login` via `<Navigate>`); SC-4 Playwright unconditional GREEN (`e2e/auth-hydration.spec.ts:149` — mocks `/rest/v1/candidatos`, asserts `loginRedirects <= 1`); 3/3 RoleGuard tests GREEN in `src/components/__tests__/RoleGuard.test.tsx`. **UAT Scenario 4 SKIPPED** (within plan criterion — SC-4 Playwright autonomous evidence is strong; manual repro requires editing localStorage `auth-storage.state.role` which cannot be safely simulated since `user.app_metadata.role` is in signed JWT in this codebase). |
| 5 | src/store/adminAuthStore.ts literal status matches FOUND-12 text — file deleted | ✓ verified | Autonomous: Plan 04.1-04 commit `8005fd5` deleted `src/store/adminAuthStore.ts` (228 LoC re-export shim); 2/2 found12 tests GREEN in `src/store/__tests__/found12.test.ts` (existsSync false + import scan zero matches); REQUIREMENTS.md text "deletado" now matches working tree literally; INT-WARNING-2 closed conjointly. (No UAT required — literal file-system assertion.) |
| 6 | New Playwright E2E covers full anonymous → login → submit candidatura path | ✓ verified | Autonomous: `e2e/auth-hydration.spec.ts` (4 SC scenarios × 3 projects = 12 listings; SC-3+SC-4 unconditional 2/2 GREEN, SC-1+SC-2 env-gated `E2E_REAL_LOGIN=1`); `e2e/candidatura-submit.spec.ts:158` B-J12 env-gated for the full anon→login→submit path. UAT Scenarios 1+2 cover the same path manually with stronger end-to-end evidence (real DB write + real candidatura submission + Studio verification). |

## Audit Gap Closures — `v1.0-MILESTONE-AUDIT.md`

| Gap ID | Status Pre-4.1 | Closure Mechanism | Status Post-4.1 |
|--------|----------------|-------------------|-----------------|
| INT-BLOCKER-1 | blocker | Plan 02 (commit `a873128` + `332364f`): `hydrateFromSession` async store action + `App.tsx` listener Pattern 1 (`setTimeout(0)` deferral preventing supabase-js Web Lock deadlock per GH issue 762) → `setSession` no longer called by listener; `fetchProfile` populates candidato/adminUser after EVERY `SIGNED_IN` / `TOKEN_REFRESHED` / `USER_UPDATED` / `PASSWORD_RECOVERY` | ✓ closed (autonomous + UAT Scenarios 1+2+3 PASS) |
| INT-BLOCKER-2 | blocker | Same root cause as INT-BLOCKER-1 (listener fix) + Plan 03 (commits `aec3e27` + `1534b45`): defense-in-depth `await waitForCandidatoHydrated({ timeoutMs: 3000 })` in 4 fresh-login submit handlers (LoginCandidato + RedefinirSenha happy + RedefinirSenha Pitfall 2 fallback + CadastroMultiStepForm Step 4); FormularioCandidaturaPage silent-return replaced by 3 explicit toasts + disabled button | ✓ closed (autonomous + UAT Scenarios 1+2+3 PASS — primary bug confirmed FIXED live in Scenario 2) |
| INT-WARNING-2 | warning | Plan 04 (commits `0a2ff71` + `8005fd5`): `src/store/adminAuthStore.ts` deleted (228 LoC re-export shim, 90% dead surface confirmed); 2 imports migrated (App.tsx inline `useIsAdminAuthenticated` selector + `useSessionTimeout.ts` direct `useAuthStore` import); LoginRHPage doc-comment rewritten | ✓ closed (autonomous-only — no UAT needed; literal file-system assertion via found12.test.ts) |
| INT-WARNING-3 | warning | Plan 02 commit `4d9fa25`: `RoleGuard.tsx` `fallbackTriedRef` one-shot DB fallback when `isAuthenticated && role === null` (prevents infinite redirect loop when JWT custom hook stops emitting `app_metadata.role`); first render: spinner + `initialize()`; second render: bounce to login (last resort) | ✓ closed (autonomous via SC-4 Playwright + 3/3 RoleGuard Vitest GREEN; UAT Scenario 4 SKIPPED within plan criterion — SC-4 mock evidence sufficient) |
| FLOW-CADASTRO | broken | Plan 02 listener fix (centralized hydration after every SIGNED_IN) + Plan 03 await `waitForCandidatoHydrated` in `CadastroMultiStepForm.tsx:426-443` Step 4 (after `tryAutoLogin` returns true, before navigate to `/candidato/perfil`) | ✓ working (UAT Scenario 1 PASS 2026-04-27 — candidato_id `eb718a32-9ba6-4f83-a49e-f2d39c0b1566`, navigate to /candidato/perfil with nome_completo visible no F5) |
| FLOW-RECOVERY | broken | Plan 02 listener fix + Plan 03 await `waitForCandidatoHydrated` in `RedefinirSenhaPage.tsx` happy path (line 78) AND Pitfall 2 fallback (line 90) — both navigate paths now block until `candidato` populated | ✓ working (UAT Scenario 3 PASS 2026-04-27 — Gmail round-trip end-to-end via `fernando@beautysmile.com.br`; SC-3 Playwright autonomous also GREEN) |
| FLOW-CANDIDATURA | broken | Plan 02 listener fix + Plan 03 await `waitForCandidatoHydrated` in `LoginCandidatoPage.tsx` (after signIn) + `FormularioCandidaturaPage.tsx:233` silent-return REPLACED by 3 toasts + inline `disabled={!candidato || !cvFile || ...}` (defense in depth) | ✓ working (UAT Scenario 2 PASS 2026-04-27 — THE PRIMARY BUG path; submit handler ran end-to-end, silent-fail confirmed FIXED live; redirect to /candidato/perfil) |

## Requirements Traceability

| Req ID | Phase 4.1 Coverage | Evidence |
|--------|--------------------|----------|
| FOUND-12 | Plan 04 (literal close — `src/store/adminAuthStore.ts` deleted) | found12.test.ts 2/2 GREEN; 04.1-04-SUMMARY.md commit `8005fd5` |
| CAD-06 | Plan 03 (await hydration in cadastro Step 4) | UAT Scenario 1 PASS (2026-04-27); existing CAD-06 logic preserved (cadastrarCandidato + tryAutoLogin contract unchanged) |
| AUTH-01 | Plan 03 (await hydration in LoginCandidatoPage) | UAT Scenario 2 PASS (2026-04-27) — login flow exercised end-to-end during candidatura redirect path; existing AUTH-01 mapSupabaseError + 5-arm error switch preserved |
| AUTH-04 | Plan 03 (await hydration in RedefinirSenhaPage happy + Pitfall 2 fallback) | UAT Scenario 3 PASS (2026-04-27) Gmail round-trip; SC-3 Playwright GREEN |
| VAGA-03 | Plan 03 (await hydration in LoginCandidatoPage + FormularioCandidaturaPage silent-return REPLACED) | UAT Scenario 2 PASS (2026-04-27) — anon → /vagas/:slug → Candidatar → /auth/login?redirect → login → /candidato/candidatura/formulario/:slug |
| CAND-01 | Plan 03 (FormularioCandidaturaPage disabled button + 3 toasts) | UAT Scenario 2 PASS (2026-04-27) — CV upload exercised live; submit handler ran end-to-end |
| CAND-02 | Plan 03 (same surface as CAND-01 — handler covers all submit failure modes) | UAT Scenario 2 PASS (2026-04-27) — perguntas formulário answered + persisted via submit |
| CAND-03 | Plan 03 (same surface as CAND-01 — submit handler unchanged on happy path) | UAT Scenario 2 PASS (2026-04-27) — happy path navigation to /candidato/perfil after submit |

## Automated Verification Battery — Final Output

> Captured 2026-04-27T02:25-02:30Z (Plan 04.1-05 Task 1 execution).

```
$ npm run lint 2>&1 | grep -c "error TS"
296   # MUST be <= 320 — baseline preserved exactly (zero net-new from Phase 4.1; same as Plan 04.1-04 close)

$ npm run build 2>&1 | tail -10
build/assets/72212e27083bc5aff34e367036bc5f1a36b908b7-CIVOYtuH.jpeg    714.03 kB
build/assets/91b67d31b9aa67c340ac4a375a9832d8c0284448-Cwjdv5p_.png   4,344.03 kB
build/assets/index-kZotktxG.css                                        111.08 kB │ gzip:  18.10 kB
build/assets/index-D3WENacp.js                                       2,394.08 kB │ gzip: 660.77 kB

(!) Some chunks are larger than 500 kB after minification. Consider:
- Using dynamic import() to code-split the application
- Use build.rollupOptions.output.manualChunks to improve chunking
- Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
✓ built in 1m 16s   # exit 0

$ npm run test:run 2>&1 | tail -5
 Test Files  1 failed | 26 passed (27)
      Tests  1 failed | 349 passed (350)
   Start at  23:28:18
   Duration  23.03s
# 1 FAIL = LoadingProgress carryover from Phase 2/3 [02-06] — UNRELATED to Phase 4.1 (same single test deferred since Phase 2 UAT iteration; Phase 4 verifier explicitly classified as out-of-scope; ROADMAP Phase 4 row "340 PASS / 1 FAIL" baseline preserved at 349 PASS / 1 FAIL post-Phase-4.1 — net +9 PASS from new Phase 4.1 test scaffolds)

$ npx playwright test e2e/auth-hydration.spec.ts -g "SC-3|SC-4" --project=chromium 2>&1 | tail -8
Listing tests:
  ✓  2 [chromium] › e2e/auth-hydration.spec.ts:93:3 › Phase 4.1 Auth Hydration › SC-3: recovery deeplink → setNewPassword → /candidato/perfil with candidato (34.0s)
  ✓  1 [chromium] › e2e/auth-hydration.spec.ts:149:3 › Phase 4.1 Auth Hydration › SC-4: JWT app_metadata.role missing → no infinite redirect loop (INT-WARNING-3) (36.1s)

  2 passed (56.2s)

$ npx playwright test e2e/auth-hydration.spec.ts --list 2>&1 | tail -3
Total: 12 tests in 1 file   # 4 SC scenarios × 3 Playwright projects (chromium + mobile-chrome + tablet)

$ npm run test:run -- src/store/__tests__/found12.test.ts src/store/__tests__/authStore.test.ts src/components/__tests__/RoleGuard.test.tsx 2>&1 | tail -5
 Test Files  3 passed (3)
      Tests  9 passed (9)   # 2 found12 + 4 authStore.hydrateFromSession + 3 RoleGuard.fallback = 9/9 GREEN (Wave 0 RED→GREEN battery COMPLETE)
   Start at  23:30:22
   Duration  2.97s

$ E2E_REAL_LOGIN=1 E2E_ALLOW_DB_WRITE=1 npx playwright test e2e/auth-hydration.spec.ts -g "SC-2" 2>&1 | tail -3
# COVERED via UAT Scenario 2 (Fernando manual run 2026-04-27; smoke-runtime gate per Phase 4 lição central D-25..D-28).
# UAT Scenario 2 PASS — anon /vagas → vaga active → "Candidatar-se" → /auth/login?redirect=...
# → login (fernando@beautysmile.com.br) → /candidato/candidatura/formulario/<slug> → CV upload
# → perguntas formulário answered → submit ran end-to-end (silent-fail bug confirmed FIXED;
# Plan 04.1-03 commit 1534b45 validated live) → /candidato/perfil. Stronger evidence than
# automated SC-2 (real DB write + real candidatura submission + Pitfall 7 redacted log shape).
```

## Pitfall 7 Preserved

```
$ npm run test:run -- src/features/auth/utils/__tests__/pitfall7.grep.test.ts 2>&1 | tail -3
 Test Files  1 passed (1)
      Tests  4 passed (4)
   Start at  23:30:08
```

Surfaces touched in Phase 4.1 covered by `PHASE_3_AUTH_PATHS`:
- `src/store/authStore.ts` (existing — `hydrateFromSession` action added; zero new console.*)
- `src/components/RoleGuard.tsx` (added in Plan 04.1-01 Pitfall 7 grep extension; `fallbackTriedRef` introduced with zero console.*)
- `src/components/pages/LoginCandidatoPage.tsx` (existing — `await waitForCandidatoHydrated` line added; zero new console.*)
- `src/components/pages/RedefinirSenhaPage.tsx` (existing — 2 `await waitForCandidatoHydrated` lines added; zero new console.*)
- `src/features/auth/utils/waitForCandidatoHydrated.ts` (CREATED in Plan 04.1-02; zero console.* — covered by recursive `src/features/auth` scan)

`src/components/pages/FormularioCandidaturaPage.tsx` covered by `PHASE_4_VAGAS_PATHS` (Plan 04.1-03 added 3 toasts but NO console.*; existing constraint preserved).

## Lint Baseline (FOUND-08 carryover to Phase 4.2)

| Stage | Baseline | Post-4.1 | Net Change |
|-------|----------|----------|------------|
| TS errors | 320 (Phase 4 close) | 296 | **−24** (Phase 4.1 Plan 01 added jest-dom shim that closed prior type errors; net improvement) |

Plan-by-plan tsc movement:
- 04.1-01 (Wave 0): 320 → 296 (−24 via @testing-library/jest-dom shim)
- 04.1-02 (Wave 1): 296 → 296 (preserved)
- 04.1-03 (Wave 2): 296 → 296 (preserved)
- 04.1-04 (Wave 3): 296 → 296 (preserved)
- 04.1-05 (Wave 4): 296 → 296 (this plan; documentation-only)

**Phase 4.1 net delta on lint baseline: −24 errors.** This is _net improvement_ — the FOUND-08 carryover to Phase 4.2 is now smaller by 24 errors, well under the 320 invariant.

## Phase Sign-Off

- [x] All 6 ROADMAP success criteria verified (autonomous + UAT)
- [x] All 7 audit gap IDs closed
- [x] Lint baseline preserved (296 << 320 invariant; net −24 improvement)
- [x] Build green (exit 0; 1m 16s)
- [x] Vitest suite green (349 PASS / 1 FAIL — LoadingProgress carryover, NOT a regression)
- [x] Playwright unconditional green (SC-3 + SC-4 chromium 2/2 PASS; 12 listings across 3 projects)
- [x] Playwright real-login coverage satisfied via UAT smoke-runtime gate (UAT Scenario 2 PASS 2026-04-27 — Fernando exercised anon → login → submit candidatura end-to-end against live Supabase, equivalent evidence to SC-2 + B-J12 with stronger proof since real DB write occurred)
- [x] UAT scenarios PASS (3/3 PASS + Scenario 4 SKIPPED within plan criterion — UAT-APPROVED 2026-04-27 by Fernando, runbook committed at `c4c7080`)

## Phase 5 Backlog (Side Findings — Non-Blocking)

UAT surfaced 5 non-blocking side findings recorded for Phase 5 visual-polish + UX
backlog. None block Phase 4.1 closure (auth-hydration target fully validated):

| ID | Category | File / Surface | Description | Notes |
|----|----------|----------------|-------------|-------|
| F-04.1-A | UI / cosmetic | `CadastroMultiStepForm.tsx` Step 3 (Disponibilidade) + Endereço campo "Estado" | Dropdown initial text dark on dark glass — placeholder text renders black/dark over the glass background instead of white. Likely root cause: shadcn/ui Select with `value` initial undefined → renders placeholder with default text color. Console warning `Select is changing from uncontrolled to controlled` corroborates. | Fix: ensure `value=""` or `defaultValue` explícito + `text-white` / `text-foreground` consistente nos placeholders. Phase 5 visual-polish. |
| F-04.1-B | UI / UX | `CadastroMultiStepForm.tsx` (CEP auto-fill via ViaCEP) | Toast "CEP encontrado" loops/re-fires repeatedly instead of toasting once. Likely root cause: useEffect dependency array changes per render (e.g., depends on full `endereco` object instead of just `cep`). | Fix: deduplicate via stable toast ID OR fire only when `cep` changes (via ref tracking previous cep). Phase 5 UX polish. |
| F-04.1-C | UI / cosmetic | `FormularioCandidaturaPage.tsx` (candidatura form) | Candidatura form fonts still black on dark/glass background — Fernando's exact words: "as fontes da pagina do formulariuo da candidatura ainda continuam pretas, copiar o mesmo design do formulario de cadastro, ele esta muito bonito, os campos de texto tambem". This is the same surface as **F-04-08-G** (already in Phase 5 backlog from Phase 4 close); Phase 4.1 confirms it remains open. | Fix: port `CadastroMultiStepForm` glass-input styling (text-white + glass border + bg-white/10 + placeholder:text-white/50 + focus:bg-white/15) to `FormularioCandidaturaPage`. Linked to F-04-08-G. Phase 5 visual-polish (high priority — explicit user complaint). |
| F-04.1-D | Test setup (NOT a regression) | `paulista@beautysmile.com.br` recovery email | Recovery email sent by Supabase but destination mailbox does not receive — `paulista@` is a placeholder used for cadastro testing without a real inbox in Beauty Smile's email server. NOT a regression — recovery flow itself works (proven via `fernando@`). | Recommendation: future cadastro tests should use Gmail+alias addresses (e.g., `fernando+paulista@gmail.com`) so recovery emails route to a real inbox. Test-infrastructure improvement, not a code fix. |
| F-04.1-E | UX | `RedefinirSenhaPage.tsx` / `passwordService.ts:133` | 422 transient on first `setNewPassword`; recovered on retry — UX gap on friendly error toast. Likely cause: Supabase Auth rejection ("password matches current" OR "weak password" OR token expired). UI did not surface a user-friendly error message before the retry. | Fix: map 422 to clearer toast (e.g., "Esta senha não pode ser igual à atual" / "A senha não atende aos requisitos mínimos"). Phase 5 UX gap. |

**Cross-reference:** F-04.1-C is linked to F-04-08-G (Phase 4 backlog item from
04-08-SUMMARY decisão D-26 visual polish). Both should be addressed together in
Phase 5 visual-polish wave (port CadastroMultiStepForm styling pattern to all
candidato-facing forms — login, recovery, candidatura).

**Verifier:** Fernando + automated battery
**Date:** 2026-04-27 (autonomous portion 2026-04-27T02:25-02:30Z; UAT sign-off at commit `c4c7080`)
