---
phase: 16-compliance-a11y-hardening
plan: 02
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - src/features/decisao/components/DecisaoFinalPage.tsx
  - src/features/decisao/components/ConsolidacaoDashboard.tsx
  - src/features/decisao/components/RegistrarDecisaoForm.tsx
  - src/features/admin/bias-audit/components/BiasAuditPage.tsx
autonomous: true
requirements: [LGPD-05]
user_setup: []

must_haves:
  truths:
    - "R7 DecisaoFinalPage tabs use the Radix Tabs tablist/tab/tabpanel pattern (not custom aria-pressed buttons) — AB-3"
    - "R7 RegistrarDecisaoForm decision selector uses the Radix RadioGroup primitive with keyboard arrow-key roving focus (not a hand-rolled aria-checked group) — AB-2/AB-3/AB-5"
    - "The #35BFAD accent leak on the Comparativo spinner is gone and the bias H1 + breakdown label use role tokens (FX-01/02/03)"
    - "Amber em_espera / bias-banner text and the cursor-help N/A tooltips clear AA contrast + are keyboard-reachable at the composited background (FX-07/FX-10)"
    - "axe serious|critical count for R7 DecisaoFinalPage and R8 BiasAuditPage (mockable subset) = 0 — LGPD-05"
  artifacts:
    - path: "src/features/decisao/components/DecisaoFinalPage.tsx"
      provides: "Radix Tabs migration + FX-01 accent fix"
      contains: "Tabs"
    - path: "src/features/decisao/components/RegistrarDecisaoForm.tsx"
      provides: "Radix RadioGroup decision selector"
      contains: "RadioGroup"
    - path: "src/features/admin/bias-audit/components/BiasAuditPage.tsx"
      provides: "FX-03 H1 token + FX-07 amber + FX-10 tooltip a11y"
      contains: "text-3xl"
  key_links:
    - from: "src/features/decisao/components/DecisaoFinalPage.tsx"
      to: "@/components/ui/tabs"
      via: "import Tabs/TabsList/TabsTrigger/TabsContent"
      pattern: "from '@/components/ui/tabs'"
    - from: "src/features/decisao/components/RegistrarDecisaoForm.tsx"
      to: "@/components/ui/radio-group"
      via: "import RadioGroup/RadioGroupItem"
      pattern: "from '@/components/ui/radio-group'"
---

<objective>
Fix the highest-defect-density RH cluster — R7 DecisaoFinalPage / RegistrarDecisaoForm /
ConsolidacaoDashboard and R8 BiasAuditPage — by swapping the hand-rolled tab + radiogroup
defects for the already-vendored Radix primitives, removing the accent leak, fixing the
amber-on-translucent contrast and the cursor-help tooltip reachability, and applying the
three Phase-15 polish one-liners. Flip the Tier-A axe loop GREEN for the mockable screens
in this cluster.

Purpose: per the UI-SPEC §"Likely axe-core risk flags," R7 stacks four issues (custom tabs
FX-04, custom radiogroup FX-05, amber em_espera FX-07, accent leak FX-01) and R8 stacks
three (amber banner FX-07, cursor-help tooltip FX-10, arbitrary H1 FX-03). This is the
single biggest score mover toward axe-core ≥90 (LGPD-05).

Output: four edited components under src/features/decisao + src/features/admin/bias-audit;
the Wave-0 Tier-A assertions for these screens flip GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-compliance-a11y-hardening/16-UI-SPEC.md
@.planning/phases/16-compliance-a11y-hardening/16-RESEARCH.md

<interfaces>
<!-- Vendored Radix primitives the executor uses directly — already in src/components/ui/. -->

From src/components/ui/tabs.tsx (Radix — gives tablist/tab/tabpanel + roving focus for free):
  export { Tabs, TabsList, TabsTrigger, TabsContent }

From src/components/ui/radio-group.tsx (Radix — arrow-key roving focus + name/role/state for free):
  export { RadioGroup, RadioGroupItem }

From src/components/ui/tooltip.tsx (Radix — focus-triggerable + aria-describedby):
  export { Tooltip, TooltipTrigger, TooltipContent, TooltipProvider }

Accent discipline (UI-SPEC §Design System): #35BFAD is reserved to SugestaoIABadge + the
candidate autosave Check ONLY. Any other #35BFAD usage is a leak (FX-01).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | presentation-tier component edits only |

No new attack surface; a11y/cleanup only; no migration; RLS unchanged. RoleGuard remains UX defense-in-depth; server RLS (Phase 15 WR-03 vaga-scoped) is the real gate, untouched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-LGPD | Information Disclosure | DecisaoFinalPage / BiasAuditPage candidate-visibility | accept | These are RH/admin-only routes (RoleGuard + RLS); a11y fixes add/relabel ARIA + tokens only, surface no new score/band/PII. No candidate-facing change. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DecisaoFinalPage Radix Tabs (FX-04) + accent leak (FX-01)</name>
  <read_first>
    - src/features/decisao/components/DecisaoFinalPage.tsx (lines ~117-150 the custom aria-pressed tab rows; line ~178 the `text-[#35BFAD]` Comparativo loading spinner)
    - src/components/ui/tabs.tsx (the vendored Radix Tabs API)
    - .planning/phases/16-compliance-a11y-hardening/16-RESEARCH.md §"FX-04" code example + §"FX-01"
    - .planning/phases/16-compliance-a11y-hardening/16-UI-SPEC.md §3a FX-01 + §3b FX-04
  </read_first>
  <behavior>
    - The two tab triggers (Consolidação / Decisão) render as Radix TabsTrigger inside a TabsList; switching is keyboard-operable (arrow keys) and exposes role=tab/tabpanel with aria-selected
    - The Comparativo loading spinner no longer uses `text-[#35BFAD]`; it uses `text-white/70` (neutral, matching the error-state two lines below)
    - The dashboard content for each tab renders inside TabsContent panels
  </behavior>
  <action>
    Migrate the custom `<button aria-pressed>` tab rows at DecisaoFinalPage.tsx:117-150 to the
    vendored Radix `Tabs`/`TabsList`/`TabsTrigger`/`TabsContent` (import from `@/components/ui/tabs`),
    driving the existing active-tab state via `value`/`onValueChange`. Wrap the existing
    ConsolidacaoDashboard panel and the Decisão panel in `TabsContent value=...`. Separately, change
    the `text-[#35BFAD]` accent on the Comparativo loading spinner at line ~178 to `text-white/70`
    (FX-01 — accent reserved to SugestaoIABadge per UI-SPEC; matches the neutral error state below).
    Preserve all existing behavior, copy, and the glass shell; this is a semantics+token swap, not a redesign.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && npm run test:run -- DecisaoFinal 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/decisao/components/DecisaoFinalPage.tsx` imports from `@/components/ui/tabs` (grep matches)
    - No `text-[#35BFAD]` remains in DecisaoFinalPage.tsx (grep returns 0 outside comments)
    - `npm run build` exits 0
    - `npm run -s lint` tsc count does not rise above 291
    - The Tier-A axe assertion for R7 DecisaoFinalPage (mockable) reports no `serious`/`critical` `aria-required-children` / `color-contrast` violation attributable to the tabs or the accent spinner
  </acceptance_criteria>
  <done>DecisaoFinalPage uses Radix Tabs, the accent leak is gone, build green, tsc no-growth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RegistrarDecisaoForm Radix RadioGroup (FX-05) + ConsolidacaoDashboard weight (FX-02)</name>
  <read_first>
    - src/features/decisao/components/RegistrarDecisaoForm.tsx (lines ~97-118 the custom role="radiogroup" + role="radio" buttons that set only aria-checked, no keyboard nav; line ~60 the `em_espera` amber)
    - src/features/decisao/components/ConsolidacaoDashboard.tsx (line ~58 the `font-medium` breakdown label)
    - src/components/ui/radio-group.tsx + src/components/ui/label.tsx (vendored Radix RadioGroup API)
    - .planning/phases/16-compliance-a11y-hardening/16-RESEARCH.md §"FX-05" code example + §Pitfall 4 (amber composite measurement)
    - .planning/phases/16-compliance-a11y-hardening/16-UI-SPEC.md §3a FX-02 + §3b FX-05/FX-07
  </read_first>
  <behavior>
    - The decision selector renders as Radix RadioGroup + RadioGroupItem with Label htmlFor pairing; arrow keys move selection (roving focus), each item exposes role=radio + checked state + accessible name, min-h-[44px] preserved
    - The em_espera amber text clears ≥4.5:1 at the composited glass background (semantic stays amber/warning)
    - The ConsolidacaoDashboard breakdown label uses `font-semibold` (the 600 label role), not `font-medium`
  </behavior>
  <action>
    Replace the hand-rolled radiogroup at RegistrarDecisaoForm.tsx:97-118 with the vendored Radix
    `RadioGroup`/`RadioGroupItem` (import from `@/components/ui/radio-group`, pairing each with a `Label
    htmlFor`), driving the existing decisao state via `value`/`onValueChange`, keeping `min-h-[44px]`
    and the existing option set/copy. This gives ArrowUp/Down/Left/Right roving focus + name/role/state
    for free (the exact FX-05 remedy). Fix the FX-07 `em_espera` amber at line ~60: let the axe
    color-contrast report drive the exact node and bump the amber token (darker amber text token OR
    raise tint opacity) until ≥4.5:1 at the composited background — do NOT change the amber semantic.
    Separately, in ConsolidacaoDashboard.tsx line ~58, change the breakdown label `font-medium`→`font-semibold`
    (FX-02, typography contract).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && npm run test:run -- RegistrarDecisao 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/decisao/components/RegistrarDecisaoForm.tsx` imports from `@/components/ui/radio-group` (grep matches); no `role="radiogroup"` hand-roll remains (grep returns 0)
    - `src/features/decisao/components/ConsolidacaoDashboard.tsx` line ~58 uses `font-semibold` (grep confirms; the prior `font-medium` on that label is gone)
    - `npm run build` exits 0; `npm run -s lint` tsc ≤291
    - The Tier-A axe assertion for R7 reports no `serious`/`critical` `color-contrast` (amber) or `aria-required-children`/`name-role-value` (radiogroup) violation
  </acceptance_criteria>
  <done>RegistrarDecisaoForm uses Radix RadioGroup (keyboard nav), em_espera amber passes AA, breakdown label is font-semibold, build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BiasAuditPage H1 token (FX-03) + amber banner (FX-07) + cursor-help tooltip (FX-10)</name>
  <read_first>
    - src/features/admin/bias-audit/components/BiasAuditPage.tsx (line ~87 the `text-[28px]` arbitrary H1; lines ~107,152 the amber-on-translucent banner; the N/A pill + 4/5-flag `cursor-help` tooltip triggers)
    - src/components/ui/tooltip.tsx (vendored Radix Tooltip — focus-triggerable by default; verify the trigger element actually receives focus)
    - .planning/phases/16-compliance-a11y-hardening/16-RESEARCH.md §Pitfall 4 (amber composite) + §"FX-15 keep types" note (this task does NOT touch biasMath)
    - .planning/phases/16-compliance-a11y-hardening/16-UI-SPEC.md §3a FX-03 + §3b FX-07/FX-10
  </read_first>
  <behavior>
    - The page H1 uses `text-3xl md:text-4xl` (role token with responsive cap), not `text-[28px]`
    - The amber banner text clears ≥4.5:1 at the composited background (semantic stays amber)
    - The cursor-help tooltip triggers (N/A pill, 4/5 flag) are keyboard-focusable (a real `<button>` or `tabIndex={0}`) and the tooltip content is referenced via aria-describedby — info is not exclusively pointer-gated
  </behavior>
  <action>
    At BiasAuditPage.tsx line ~87 change the H1 `text-[28px]`→`text-3xl md:text-4xl` (FX-03 — role token,
    restores responsive cap, matches the other two H1s). Fix the FX-07 amber banner at lines ~107,152:
    measure at the real composited background and bump the amber token until axe color-contrast goes green
    (do not change the warning semantic). For FX-10: ensure every `cursor-help` tooltip trigger (the N/A
    pill + the 4/5-flag) is a keyboard-focusable element — wrap in a real `<button type="button">` or add
    `tabIndex={0}` — and that the Radix TooltipContent is wired so the trigger carries aria-describedby, so
    the explanation is reachable by keyboard/SR, not pointer-only. Do NOT touch biasMath.ts in this task
    (FX-15 dead-fn removal is a separate cleanup plan; the BandResult/AdverseImpactResult type imports stay live).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && npm run test:run -- BiasAudit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/admin/bias-audit/components/BiasAuditPage.tsx` H1 uses `text-3xl md:text-4xl` (grep matches); no `text-[28px]` remains (grep returns 0)
    - Every `cursor-help` trigger is keyboard-focusable (grep: each `cursor-help` site is inside a `<button` or carries `tabIndex={0}`)
    - `npm run build` exits 0; `npm run -s lint` tsc ≤291
    - The Tier-A axe assertion for R8 BiasAuditPage reports no `serious`/`critical` `color-contrast` (amber/H1) or interactive-name violation
  </acceptance_criteria>
  <done>BiasAuditPage H1 is a role token, the amber banner passes AA, cursor-help tooltips are keyboard-reachable, build green, biasMath untouched.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 after all three tasks.
- `npm run -s lint` tsc count ≤291 (no growth).
- `npx playwright test e2e/a11y.spec.ts --project=chromium` — the Tier-A assertions for the mockable R7 + R8 screens are GREEN (zero serious/critical).
- `npm run test:run` regression: no decisao/bias-audit unit test regressions.
</verification>

<success_criteria>
- R7 DecisaoFinalPage uses Radix Tabs + Radix RadioGroup; accent leak gone; amber + tooltips AA-clean.
- R8 BiasAuditPage H1 token + amber + cursor-help all pass.
- FX-01/02/03 polish one-liners applied.
- Tier-A axe GREEN for the mockable screens in this cluster; build green; tsc no-growth.
</success_criteria>

<output>
Create `.planning/phases/16-compliance-a11y-hardening/16-02-SUMMARY.md` when done
</output>
