---
phase: 46-purga-autom-tica-dry-run-live
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/fixtures/p46_medicoes_pre_fixture.sql
  - supabase/tests/fixtures/p46_teardown_fixture.sql
  - supabase/tests/fixtures/p46_fixture_elegivel.sql
  - .planning/phases/46-purga-autom-tica-dry-run-live/46-01-MEDICOES.md
autonomous: false
requirements: [PURGA-03, PURGA-07]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`public.candidaturas_alem_da_janela()` devolve pelo menos 3 linhas de titular sintético depois da fixture aplicada — medido, não presumido (hoje devolve 0 por aritmética)"
    - "Cada uma das 4 fixtures negativas (hold, vaga ativa, revisão Art. 20 aberta, etapa fora da allowlist) está ALÉM da janela: se a exceção correspondente for removida do predicado, ela passa a ser elegível"
    - "O teardown existe em disco e está commitado ANTES de qualquer linha de fixture ser escrita em PROD (D-46-21)"
    - "Nenhuma linha da fixture aparece em `v_triagem_panel`, na fila do RH, no snapshot de viés k=5 ou em `notificacoes_enviadas` — medido por consulta, não por raciocínio"
    - "Todas as linhas da fixture são recuperáveis por grep do namespace `fixture-p46+%@invalido.local` a partir de `auth.users`"
    - "O comportamento de `updated_at` em `public.candidaturas` sob retrodate foi MEDIDO (existe ou não trigger que sobrescreva) antes de a fixture ser escrita"
  artifacts:
    - supabase/tests/fixtures/p46_medicoes_pre_fixture.sql
    - supabase/tests/fixtures/p46_teardown_fixture.sql
    - supabase/tests/fixtures/p46_fixture_elegivel.sql
    - .planning/phases/46-purga-autom-tica-dry-run-live/46-01-MEDICOES.md
  key_links:
    - "`p46_fixture_elegivel.sql` → `auth.users` → `public.candidatos.user_id` → `public.candidaturas.candidato_id` → `public.vagas.id` (vaga arquivada) — a cadeia inteira tem de existir ou o titular não é selecionável"
    - "`candidaturas.updated_at` retrodatado → degrau (3) do `COALESCE` de `candidaturas_alem_da_janela()` — o elo que, se falhar, faz a fixture render ZERO e se autoderrotar"
    - "namespace do e-mail → `p46_teardown_fixture.sql` — o único caminho de remoção; sem o namespace a fixture vira lixo permanente em PROD"
  prohibitions:
    - "A fixture sintética NUNCA participa de decisão real sobre pessoa — não entra em fila de RH, snapshot de viés, estatística de funil nem dispara envio de e-mail"
    - "NUNCA retrodatar uma candidatura de pessoa real para tornar o conjunto elegível não-vazio — o erro é irreversível (PITR desligado, Storage fora do backup)"
---

<objective>
Construir o conjunto elegível **não-vazio** sobre o qual toda a Phase 46 será medida, e escrever o
caminho de remoção dele **antes** de criá-lo.

Purpose: medido em PROD em 2026-08-22, `candidaturas_alem_da_janela()` devolve **0 linhas** — a
candidatura mais antiga tem ~9,5 meses e a matriz está em 24. O conjunto elegível é vazio **por
aritmética** e continuará vazio até ~2027. Sem fixture, 18 das 21 linhas do contrato de validação
passam por **vacuidade**, e o dry-run de 14 dias (PURGA-03) prova exatamente zero. Esta é a lição da
Phase 45 repetida verbatim: as 3 tabelas de IA estavam VAZIAS em PROD e as asserções sobre as 5
tabelas `SET NULL` teriam passado sobre nada.

Output: três scripts SQL versionados (medições · teardown · fixture) e um artefato de medições com
os valores lidos do banco vivo.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-purga-autom-tica-dry-run-live/46-CONTEXT.md
@.planning/phases/46-purga-autom-tica-dry-run-live/46-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Medições read-only que a fixture não pode presumir</name>
  <files>supabase/tests/fixtures/p46_medicoes_pre_fixture.sql, .planning/phases/46-purga-autom-tica-dry-run-live/46-01-MEDICOES.md</files>
  <read_first>
    - `.planning/phases/46-purga-autom-tica-dry-run-live/46-VALIDATION.md` §"A fixture — sem ela, 18 das 21 linhas passam por vacuidade" (itens 1-7 e os dois avisos ⚠⚠)
    - `.planning/phases/46-purga-autom-tica-dry-run-live/46-RESEARCH.md` §Assumptions Log (A3, A5, A6) e §Runtime State Inventory
    - `supabase/migrations/20260801000004_p43_previa_retencao.sql:174-201` — o `COALESCE` de 4 degraus, para saber exatamente qual coluna cada degrau lê
    - `supabase/migrations/20260801000002_p43_config_retencao.sql:167-179` — o `COMMENT` da coluna que declara a pré-condição de D-46-22 dentro do banco
  </read_first>
  <action>
Escrever `supabase/tests/fixtures/p46_medicoes_pre_fixture.sql`: um script **estritamente
read-only** (zero verbo de escrita) que o orquestrador roda por MCP `execute_sql` numa única
chamada e cujo resultado é transcrito para `46-01-MEDICOES.md`. Ele mede sete coisas que a fixture
não pode presumir:

(M1) Existe trigger em `public.candidaturas` que escreva `updated_at`? Consultar `pg_trigger` +
`pg_proc` por `tgrelid = 'public.candidaturas'::regclass` e `tgenabled`, listando `tgname` e o
`proname` de cada função disparada. Este é o degrau (3) da data-âncora e nasce `pg_catalog.now()`;
se um trigger sobrescrever o retrodate, a fixture rende ZERO e se autoderrota. É o modo de falha
mais provável desta fase (D-46-21).

(M2) O conjunto NOT NULL de `auth.users` — `information_schema.columns` com `is_nullable = 'NO'` e
`column_default IS NULL`, para saber quais colunas o INSERT da fixture tem obrigatoriamente de
preencher. Medir, nunca copiar de um tutorial.

(M3) `auth.uid()`, `auth.jwt() #>> '{app_metadata,role}'` e
`current_setting('request.jwt.claims', true)` avaliados no papel corrente sem claims — re-afere por
execução o `[ASSUMED A3]` que fundamenta D-46-18 (Saída B).

(M4) `public.plano_exclusao_titular` é chamável sem sessão? Ler `proacl`, `prosecdef` e `proconfig`
de `pg_proc` para essa função (A6). Não invocá-la — apenas ler o catálogo.

(M5) `SELECT * FROM public.listar_matriz_retencao()` — as 8 linhas com `etapa`, `janela_meses`,
`origem`, `alterado_por`. É a medição que D-46-22 transforma em pré-condição do flip, e é ela que
diz quantas linhas ainda estão em seed.

(M6) `SELECT jobname, schedule, active FROM cron.job ORDER BY jobname` mais `count(*)` — o
instantâneo que a asserção (a) de `p42_invent05_cron_smoke.sql` fixa hoje em 3, e que D-46-23 manda
converter em invariante no mesmo commit do 4º job.

(M7) `SHOW statement_timeout` e `current_user`, mais a candidatura mais antiga viva
(`min(data_candidatura)` sobre `public.candidaturas` com `deleted_at IS NULL`) e
`SELECT count(*) FROM public.candidaturas_alem_da_janela()` — a linha de base de zero que a fixture
tem de mover.

Escrever `46-01-MEDICOES.md` com uma seção por medição, cada uma com o valor **medido** e a data. O
artefato existe porque diagnóstico registrado em prosa não é evidência: se uma medição contradisser
uma premissa da RESEARCH, o texto do artefato é a premissa nova, e a contradição fica escrita.
  </action>
  <acceptance_criteria>
    - `supabase/tests/fixtures/p46_medicoes_pre_fixture.sql` existe e contém sete blocos rotulados `M1` a `M7`
    - Nenhum verbo de escrita no script: `grep -viE '^\s*--' supabase/tests/fixtures/p46_medicoes_pre_fixture.sql | grep -ciE '\b(insert|update|delete|alter|drop|truncate|create)\b'` retorna `0`
    - O script não abre transação explícita: `grep -c 'BEGIN;' supabase/tests/fixtures/p46_medicoes_pre_fixture.sql` retorna `0`
    - `46-01-MEDICOES.md` tem sete seções `## M1` … `## M7`, cada uma com um valor medido e a data da medição — nenhuma diz "presumido", "provavelmente" ou "igual à Phase 45"
    - M1 declara explicitamente `trigger de updated_at: presente` ou `ausente`, com o `tgname` quando presente
    - M5 lista as 8 etapas com sua `origem`, e o texto nomeia quantas ainda estão em `seed`
    - M7 registra `count(*) FROM candidaturas_alem_da_janela()` — o valor de base contra o qual a Task 3 prova que a fixture funcionou
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/tests/fixtures/p46_medicoes_pre_fixture.sql && grep -viE '^\s*--' supabase/tests/fixtures/p46_medicoes_pre_fixture.sql | grep -ciE '\b(insert|update|delete|alter|drop|truncate|create)\b' | grep -qx 0 && grep -c '^## M' .planning/phases/46-purga-autom-tica-dry-run-live/46-01-MEDICOES.md | grep -qx 7 && echo OK</automated>
  </verify>
  <done>O script de medições existe, é provadamente read-only, e `46-01-MEDICOES.md` carrega os sete valores medidos com data.</done>
</task>

<task type="auto">
  <name>Task 2: O teardown escrito ANTES da fixture, e a fixture com as 8 variantes</name>
  <files>supabase/tests/fixtures/p46_teardown_fixture.sql, supabase/tests/fixtures/p46_fixture_elegivel.sql</files>
  <read_first>
    - `.planning/phases/46-purga-autom-tica-dry-run-live/46-01-MEDICOES.md` (produzido na Task 1 — M1 e M2 governam a forma do INSERT)
    - `.planning/phases/46-purga-autom-tica-dry-run-live/46-VALIDATION.md` §fixture, itens 1-7 e §Teardown
    - `supabase/migrations/20260801000004_p43_previa_retencao.sql:174-201` — os 4 degraus do `COALESCE` e a exceção de revisão do Art. 20 já viva no predicado
    - `supabase/migrations/20260805000006_p45_anonimizar_candidato.sql:247-249` — a razão medida para derivar identificador de um UUID em vez de sortear sobre coluna `UNIQUE`
    - `supabase/migrations/20260805000006_p45_anonimizar_candidato.sql:839-849` — o envelope de subtransação encerrada por `RAISE EXCEPTION`, o método de teardown já exercitado em PROD
    - `.planning/phases/46-purga-autom-tica-dry-run-live/46-RESEARCH.md` §Code Examples (os 8 valores de `etapa_processo` e os 4 de `status_vaga`, verbatim do catálogo)
  </read_first>
  <action>
Escrever **primeiro** `supabase/tests/fixtures/p46_teardown_fixture.sql` e só depois
`p46_fixture_elegivel.sql` — a ordem é obrigação de D-46-21, e o motivo é que uma fixture durável
em PROD sem caminho de remoção escrito é lixo permanente num banco que guarda PII.

**`p46_teardown_fixture.sql`** — remove tudo por um único ponto de entrada: o namespace do e-mail
`fixture-p46+%@invalido.local` em `auth.users`. Ordem de remoção invertida em relação à criação:
`public.retencao_hold` → `public.decisao_final` → `public.historico_candidatura` →
`public.candidaturas` → `public.candidatos` → `public.vagas` (só as criadas pela fixture, marcadas
por um `titulo` com o mesmo prefixo `fixture-p46`) → `auth.users`. Cada `DELETE` correlacionado por
`EXISTS`, jamais por negação de pertencimento a conjunto de valores. Ao fim, um bloco de
verificação que conta o resíduo em cada uma das sete tabelas e levanta exceção se qualquer contagem
for diferente de zero — porque "não lançou" não é "completou". O script é idempotente: rodá-lo duas
vezes remove zero na segunda e não falha.

**`p46_fixture_elegivel.sql`** — cria **um titular sintético por variante**, oito no total, todos
sob o mesmo namespace. Cada variante tem um sufixo determinístico no e-mail
(`fixture-p46+pos1@invalido.local`, `+pos2`, `+pos3`, `+neg-hold`, `+neg-vaga`, `+neg-art20`,
`+neg-etapa`, `+cap2`) e cada UUID é **literal e fixo no arquivo**, não sorteado: o CPF de
`public.candidatos` é derivado do UUID da fixture por transformação determinística, nunca de
`random()` sobre coluna `UNIQUE` — com sorteio, uma colisão faz tudo falhar sem que a próxima
pessoa entenda por quê.

Estrutura comum a todas as variantes: uma `public.vagas` com `status = 'arquivada'` (se a vaga
estiver aberta, a exceção de D-46-03 protege e a fixture rende zero), um `public.candidatos`
ligado a `auth.users`, e uma `public.candidaturas` com **`data_candidatura` E `updated_at`
retrodatados** 30 meses. O retrodate de `updated_at` é feito por `UPDATE` explícito **depois** do
`INSERT`, e — se M1 tiver medido trigger presente — o script desabilita a sessão de trigger pelo
caminho que M1 registrou e reabilita ao fim, nunca removendo o trigger.

As três variantes positivas exercitam três degraus diferentes da data-âncora, para que
`ancora_origem` seja observada em mais de um valor e a coluna prove alguma coisa:
`pos1` com linha em `public.historico_candidatura` cuja `etapa_para` é a etapa atual (degrau 1);
`pos2` sem histórico e com `data_decisao_final` preenchida e retrodatada (degrau 2); `pos3` sem
histórico e sem decisão, dependendo só de `updated_at` retrodatado (degrau 3). Todas as três em
etapa da allowlist de D-46-19 (`aprovado`, `rejeitado` ou `decisao_final`).

As quatro variantes negativas ficam **cada uma além da janela** — se estiverem dentro, a asserção
passa porque a data protegeu e não porque a exceção funcionou, e isso é um falso verde:
`neg-hold` idêntica a `pos1` mais uma linha em `public.retencao_hold` com `liberado_em IS NULL`;
`neg-vaga` idêntica a `pos1` mas com a vaga em `status = 'ativa'`; `neg-art20` idêntica a `pos1`
mais uma linha em `public.decisao_final` com `revisao_solicitada_em` preenchida e
`revisao_respondida_em` nula; `neg-etapa` idêntica a `pos1` mas em `entrevista_online`, que não
está na allowlist. A variante `cap2` é um segundo titular elegível idêntico a `pos1`, que existe
só para que a prova do cap (D-46-08) possa ser feita reduzindo o cap para 1 por RPC em vez de
criar 51 titulares.

⚠ `public.retencao_hold` só nasce no plano 46-03. O bloco que cria a variante `neg-hold` é escrito
agora mas fica guardado por um `IF to_regclass('public.retencao_hold') IS NOT NULL` — presente e
inerte enquanto a tabela não existe, ativo assim que existir, sem uma segunda edição do arquivo.

O arquivo termina imprimindo, por `RAISE NOTICE`, o `count(*)` de
`public.candidaturas_alem_da_janela()` — o número que a Task 3 compara contra a linha de base de M7.

⚠ `NOTIFICACOES_MODO` está em `producao`: nenhum caminho da fixture pode escrever em
`public.notificacoes_enviadas` nem disparar trigger de notificação. O domínio `@invalido.local` é
reservado e não roteável, mas a defesa real é não escrever a linha.
  </action>
  <acceptance_criteria>
    - Os dois arquivos existem e o `git log --follow` mostra o teardown num commit igual ou anterior ao da fixture
    - A fixture não usa sorteio — aferido **fora de linhas de comentário**, para que a prosa que explica a regra não invalide a regra: `grep -vE '^\s*--' supabase/tests/fixtures/p46_fixture_elegivel.sql | grep -c 'random()'` retorna `0`
    - Nenhuma negação por conjunto de valores no teardown: `grep -viE '^\s*--' supabase/tests/fixtures/p46_teardown_fixture.sql | grep -cE '\mNOT[[:space:]]+IN\M'` retorna `0`
    - O namespace é único e greppável: `grep -c 'fixture-p46+' supabase/tests/fixtures/p46_teardown_fixture.sql` retorna valor `>= 1` e o mesmo literal aparece em `p46_fixture_elegivel.sql`
    - As 8 variantes existem: `grep -cE 'fixture-p46\+(pos1|pos2|pos3|neg-hold|neg-vaga|neg-art20|neg-etapa|cap2)@' supabase/tests/fixtures/p46_fixture_elegivel.sql` retorna `>= 8`
    - `updated_at` é retrodatado explicitamente: o arquivo contém um `UPDATE public.candidaturas` que atribui `updated_at` a um valor derivado de um intervalo em meses, e `grep -c 'updated_at' supabase/tests/fixtures/p46_fixture_elegivel.sql` retorna `>= 3`
    - Nenhuma escrita em `notificacoes_enviadas`, aferido fora de comentários: `grep -vE '^\s*--' supabase/tests/fixtures/p46_fixture_elegivel.sql | grep -c 'notificacoes_enviadas'` retorna `0`
    - O bloco de `neg-hold` está guardado por `to_regclass('public.retencao_hold')`
    - O teardown termina com um bloco que conta resíduo nas sete tabelas e levanta exceção se alguma contagem for diferente de zero
    - ⊖ Não-vacuidade: se a fixture fosse aplicada e o predicado devolvesse 0, a Task 3 REPROVA — o critério de aceite da Task 3 é `count(*) >= 3`, não `count(*) >= 0`
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/tests/fixtures/p46_teardown_fixture.sql && test -f supabase/tests/fixtures/p46_fixture_elegivel.sql && grep -vE '^\s*--' supabase/tests/fixtures/p46_fixture_elegivel.sql | grep -c 'random()' | grep -qx 0 && grep -cE 'fixture-p46\+(pos1|pos2|pos3|neg-hold|neg-vaga|neg-art20|neg-etapa|cap2)@' supabase/tests/fixtures/p46_fixture_elegivel.sql | awk '$1>=8{exit 0}{exit 1}' && grep -vE '^\s*--' supabase/tests/fixtures/p46_fixture_elegivel.sql | grep -c 'notificacoes_enviadas' | grep -qx 0 && echo OK</automated>
  </verify>
  <done>Teardown e fixture existem em disco, o teardown foi escrito primeiro, as 8 variantes são identificáveis pelo namespace, e nenhum identificador é sorteado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [CHECKPOINT] Aplicar a fixture em PROD e provar que o conjunto deixou de ser vazio</name>
  <what-built>
    Três scripts SQL: medições read-only, teardown (escrito primeiro) e a fixture de 8 variantes
    sintéticas sob o namespace `fixture-p46+%@invalido.local`. Nada foi escrito em PROD ainda.
  </what-built>
  <precondition>`46-01-MEDICOES.md` existe com os sete valores medidos, e M1 declara presente/ausente o trigger de `updated_at` em `public.candidaturas`.</precondition>
  <how-to-verify>
    O apply é do ORQUESTRADOR — subagentes GSD não recebem os tools MCP do Supabase
    (`anthropics/claude-code#13898`). Sequência exata:

    1. MCP `execute_sql` com o conteúdo de `supabase/tests/fixtures/p46_medicoes_pre_fixture.sql`
       (uma única chamada). Transcrever os sete valores para `46-01-MEDICOES.md`. Se M1 acusar
       trigger de `updated_at`, a fixture precisa do ajuste que a Task 2 previu ANTES do passo 2.
    2. MCP `execute_sql` com o conteúdo de `supabase/tests/fixtures/p46_fixture_elegivel.sql`.
       ⚠ Isto ESCREVE em PROD, inclusive em `auth.users`. É aditivo e sintético — nenhuma linha de
       pessoa real é tocada, e nenhuma data de pessoa real é alterada.
    3. **A prova de não-vacuidade**, na mesma sessão:
       `SELECT count(*) FROM public.candidaturas_alem_da_janela();` → tem de ser `>= 3`.
       Se vier `0`, o degrau (3) da data-âncora venceu o retrodate: **parar aqui** e consertar a
       fixture antes de qualquer outro plano. Todo o resto da fase mede sobre este número.
    4. **Contaminação — quatro consultas, cada uma tem de devolver zero linhas de fixture:**
       `SELECT count(*) FROM public.v_triagem_panel WHERE candidato_id IN (<os 8 ids>)` ·
       a fila do RH pelo mesmo filtro · o snapshot de viés k=5 (`gerar_bias_snapshot()` em modo de
       leitura, conferindo que nenhuma célula mudou de tamanho) ·
       `SELECT count(*) FROM public.notificacoes_enviadas WHERE candidatura_id IN (<as 9 ids>)`.
       Qualquer contaminação ⇒ rodar `p46_teardown_fixture.sql` e replanejar o isolamento.
    5. Registrar os números dos passos 3 e 4 em `46-01-MEDICOES.md` §Pós-fixture, com o `now()` do
       servidor.

    ⚠ Se qualquer passo falhar, o caminho de saída é `p46_teardown_fixture.sql` — que já existe,
    de propósito, desde antes de a fixture ter sido criada.
  </how-to-verify>
  <resume-signal>Responda "aprovado" com os números medidos nos passos 3 e 4, ou descreva o que contaminou.</resume-signal>
  <acceptance_criteria>
    - `SELECT count(*) FROM public.candidaturas_alem_da_janela()` retorna `>= 3` — comparado explicitamente contra o valor de base de M7 (`0`)
    - Pelo menos 2 valores distintos de degrau de âncora são exercitados pelas variantes `pos1`/`pos2`/`pos3` (verificável pelas colunas que cada uma preenche)
    - As 4 consultas de contaminação do passo 4 retornam `0` para as linhas da fixture
    - `46-01-MEDICOES.md` ganha a seção `## Pós-fixture` com os números e o `now()` do servidor
    - Zero linha de pessoa real foi alterada: `SELECT count(*) FROM public.candidaturas WHERE deleted_at IS NULL` bate o valor de M7 mais exatamente 9 (as 9 candidaturas da fixture)
  </acceptance_criteria>
  <verify>
    <automated>grep -c '^## Pós-fixture' .planning/phases/46-purga-autom-tica-dry-run-live/46-01-MEDICOES.md | grep -qx 1 && echo OK  # o número em si vem do orquestrador por MCP execute_sql: SELECT count(*) FROM public.candidaturas_alem_da_janela() >= 3</automated>
  </verify>
  <done>A fixture está viva em PROD, `candidaturas_alem_da_janela()` devolve `>= 3`, as quatro consultas de contaminação devolvem zero, e os números estão registrados com a hora do servidor.</done>
</task>

</tasks>

## Artifacts this phase produces (inventário completo da Phase 46)

> Este inventário é o mesmo nos sete planos. Os itens **em negrito** nascem NESTE plano.

**Tabelas novas:** `public.config_purga` · `public.retencao_hold` · `public.purga_execucoes` ·
`public.purga_execucao_itens`
**Coluna nova:** `public.config_retencao_etapa.elegivel_purga`
**Funções novas:** `public.titulares_alem_da_janela()` · `public.varrer_purga_retencao()` ·
`public.salvar_config_purga(text, integer, integer, boolean)` ·
`public.reivindicar_item_purga(uuid, uuid)` · `public.concluir_item_purga(uuid, jsonb)`
**Funções editadas:** `public.candidaturas_alem_da_janela()` (assinatura estendida + 3 exceções) ·
`public.anonimizar_candidato(uuid, boolean)` (4º ramo do guard — D-46-18)
**Cron job novo:** `purga-retencao-sweep` (`0 3 * * *`)
**Edge Function nova:** `purgar-retencao`
**ERRCODEs novos:** `P46NT` (dry-run não terminou no SQLSTATE do motor) · `P46B0` (teardown da
fixture do smoke) · `P46FB` (item forjado / não confere) · `P46RN` (reversão do DELETE de RETEN-05
fora de `live`)
**Arquivos novos:** 11 migrations `20260823000001`–`20260823000011` ·
`supabase/tests/p46_purga_smoke.sql` · `supabase/functions/purgar-retencao/{index.ts,index.test.ts}` ·
**`supabase/tests/fixtures/p46_medicoes_pre_fixture.sql`** ·
**`supabase/tests/fixtures/p46_teardown_fixture.sql`** ·
**`supabase/tests/fixtures/p46_fixture_elegivel.sql`**
**Arquivos emendados:** `supabase/tests/p42_invent05_cron_smoke.sql` (a) ·
`supabase/tests/p43_previa_smoke.sql` (e)(f)(g) · `supabase/tests/p45_motor_exclusao_smoke.sql` (C3) ·
`supabase/config.toml` · `vite.config.ts` · `docs/compliance/cron-inventory.md`
**Letras de asserção do smoke novo:** (a) cron · (b) dry-run termina no SQLSTATE do motor ·
(c) ⊖ dry-run não apagou · (d) flip recusado · (e) auditoria do flip · (f) ⊖ kill switch ·
(g) ⊖ cap excedido aborta · (h) ⊖ ledger sem PII · (i) item completo · (j.1-3) ⊖ as três exceções ·
(k) degrau correto sem decisão · (l) ⊖ etapa fora da allowlist · (m) RETEN-05 · (n) ⊖ COMMENT
reescrito · (o) ⊖ guard recusa fora de `live`

## Multi-Source Coverage Audit (Phase 46)

| Fonte | Item | Plano que cobre | Estado |
|---|---|---|---|
| GOAL | "O dado expira sozinho, dentro de um cerco" | 46-02..46-06 | COVERED |
| GOAL | "a primeira coisa que a purga faz em produção é não apagar nada" | 46-02 (tracer), 46-07 (flip recusável) | COVERED |
| REQ | PURGA-01 cron espelhando `notif-retry-sweep` | 46-06 | COVERED |
| REQ | PURGA-02 dry-run pela MESMA query | 46-02, 46-03, 46-04, 46-05 | COVERED |
| REQ | PURGA-03 primeira ativação em dry-run por período documentado | 46-01, 46-02, 46-07 | COVERED |
| REQ | PURGA-04 flip como checkpoint evidenciado | 46-07 | COVERED |
| REQ | PURGA-05 cap + kill switch | 46-02, 46-04, 46-06, 46-07 | COVERED |
| REQ | PURGA-06 ledger do "o quê, quando, sob qual política" | 46-02, 46-05 | COVERED |
| REQ | PURGA-07 `COALESCE` explícito + allowlist de terminais | 46-01, 46-03 | COVERED |
| REQ | RETEN-05 retenção de `notificacoes_enviadas` | 46-06 | COVERED |
| RESEARCH | Blocker B-01 (Saída B) | 46-04 | COVERED |
| RESEARCH | Pitfalls 1-12 | 1→46-06 · 2→46-02/03/04 · 3→46-01 · 4→46-07 · 5→46-02/06 · 6→46-02 · 7→46-05 · 8→46-02 · 9→46-02 · 10→46-02 · 11→46-05 · 12→46-05 | COVERED |
| RESEARCH | Patterns 1-6 | 1→46-06 · 2→46-04 · 3→46-07 · 4→46-02 · 5→46-02 · 6→46-06 | COVERED |
| CONTEXT | D-46-01, D-46-02 (satisfeitas por ausência, declaradas) | 46-03 | COVERED |
| CONTEXT | D-46-03, D-46-04 | 46-03 | COVERED |
| CONTEXT | D-46-05, D-46-06, D-46-07 | 46-02 (tabela), 46-07 (RPC) | COVERED |
| CONTEXT | D-46-08, D-46-09 | 46-02 (implementação), 46-06 (prova g) | COVERED |
| CONTEXT | D-46-10, D-46-13 | 46-06 | COVERED |
| CONTEXT | D-46-11 | 46-02 | COVERED |
| CONTEXT | D-46-12 | 46-05 | COVERED |
| CONTEXT | D-46-14, D-46-22 | 46-07 | COVERED |
| CONTEXT | D-46-15, D-46-16 | 46-02 | COVERED |
| CONTEXT | D-46-17, D-46-20 | 46-02 (coluna), 46-06 (regra) | COVERED |
| CONTEXT | D-46-18 | 46-04 | COVERED |
| CONTEXT | D-46-19 | 46-02 (coluna+seed), 46-03 (consumo) | COVERED |
| CONTEXT | D-46-21 | 46-01 | COVERED |
| CONTEXT | D-46-23 | 46-06 | COVERED |
| CONTEXT | Deferred (janela de rascunho · `autorizacao_retencao_curriculo` encurtador · tela do ledger · alerta ativo · baixar a matriz) | — | EXCLUÍDO por decisão (Deferred Ideas) |

Zero itens MISSING.

## Suposições sinalizadas (edges não resolvidas)

O relatório de cobertura de edges classificou como `unclassified` — e portanto **não resolvidas** —
os probes de PURGA-03, PURGA-04, PURGA-06, PURGA-07 e RETEN-05. Elas não foram descartadas nem
convertidas em backstop: ficam registradas aqui como suposição sinalizada. Os requirements em si
têm asserção explícita no contrato de `46-VALIDATION.md`; o que segue sem probe é a **categoria de
borda** de cada um. Revisar manualmente antes do fecho da fase.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orquestrador → PROD (`execute_sql`) | Escrita direta em `auth.users` e nas tabelas de domínio, sem passar por RLS |
| fixture sintética → superfícies de produto | `v_triagem_panel`, fila do RH, snapshot de viés k=5, `notificacoes_enviadas` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-01-01 | Tampering | `public.candidaturas` de pessoa real | high | mitigate | A fixture NUNCA retrodata linha existente; só cria linhas novas sob o namespace. Critério de aceite compara a contagem viva antes/depois (M7 + 9) |
| T-46-01-02 | Information Disclosure | `v_triagem_panel` / fila do RH / snapshot k=5 | medium | mitigate | Quatro consultas de contaminação no checkpoint da Task 3; contaminação ⇒ teardown imediato |
| T-46-01-03 | Denial of Service | envio de e-mail real (`NOTIFICACOES_MODO=producao`) | medium | mitigate | Domínio `@invalido.local` não roteável **e** zero escrita em `notificacoes_enviadas` (grep == 0) |
| T-46-01-04 | Repudiation | fixture órfã em PROD sem caminho de remoção | high | mitigate | Teardown escrito e commitado ANTES da criação (D-46-21); namespace único e greppável; bloco de verificação de resíduo |
| T-46-01-05 | Spoofing | conta sintética em `auth.users` usável para login | medium | mitigate | Sem senha utilizável e e-mail em domínio reservado não confirmável; a conta existe para ser destruída, não para autenticar |
| T-46-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | Nenhum pacote novo nesta fase (RESEARCH §Package Legitimacy Audit: tabela vazia, zero `[ASSUMED]`/`[SUS]`) |
</threat_model>

<verification>
- `46-01-MEDICOES.md` tem sete seções de medição mais `## Pós-fixture`
- Os três scripts SQL existem e o teardown precede a fixture no histórico do git
- `SELECT count(*) FROM public.candidaturas_alem_da_janela()` saiu de `0` para `>= 3`
- As quatro consultas de contaminação devolvem zero
- `npm run lint && npm run test:run` continua verde (nenhum arquivo TypeScript foi tocado)
</verification>

<success_criteria>
O conjunto elegível deixou de ser vazio **por construção deliberada**, o caminho de remoção existe
desde antes, e o número medido está escrito com a hora do servidor. A partir daqui, nenhuma asserção
da Phase 46 pode passar por vacuidade.
</success_criteria>

<output>
Create `.planning/phases/46-purga-autom-tica-dry-run-live/46-01-SUMMARY.md` when done
</output>
