---
phase: 46-purga-autom-tica-dry-run-live
plan: 04
type: execute
wave: 4
depends_on: ["46-03"]
files_modified:
  - supabase/migrations/20260823000006_p46_guard_purga.sql
  - supabase/migrations/20260823000007_p46_sweep_dry_run.sql
  - supabase/tests/p45_motor_exclusao_smoke.sql
  - supabase/tests/p46_purga_smoke.sql
  - database.types.ts
autonomous: false
requirements: [PURGA-02, PURGA-05]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "O laço de dry-run de `varrer_purga_retencao()` chama `public.anonimizar_candidato(<id>, true)` e cada chamada termina no SQLSTATE `P45DR` — o corpo COMPLETO do motor executou e foi revertido (PURGA-02 / SC#1)"
    - "⊖ Depois de uma varredura completa em `dry_run` sobre o conjunto não-vazio, ZERO coluna mutou nas cinco tabelas de domínio e nenhum item tem desfecho de Storage/Postgres/Auth carimbado"
    - "Uma chamada de dry-run que NÃO levantar `P45DR` é tratada como DEFEITO e derruba a execução com o ERRCODE próprio `P46NT` — chegar ao fim do bloco significa que o terminador sumiu e a transação teria commitado"
    - "⊖ O 4º ramo do guard RECUSA com `42501` quando `config_purga.modo` não é `live`, mesmo com item aberto em `purga_execucao_itens` — um modo que não seja `live` não autoriza destruição (D-46-18)"
    - "⊖ O 4º ramo RECUSA quando não existe item vivo para aquele `candidato_id`, ou quando a execução não está em `situacao = 'executando'`, ou quando o item já está concluído — as três metades falham FECHADAS"
    - "A metade (a) do guard (chamador sem sessão) NÃO foi tocada: a mensagem original continua no arquivo verbatim, e aceitar sessão nula sob `service_role` continua sendo a saída recusada (DI-45-07-01)"
    - "O apply da migration do guard ABORTA se `authenticated` puder de fato escrever em `purga_execucoes` ou `purga_execucao_itens` — o pressuposto de segurança vira asserção executada, não confiança"
    - "O md5 de `anonimizar_candidato` foi re-carimbado com os DOIS lados medidos (objeto vivo × arquivo commitado), e a asserção (C3) do smoke da Phase 45 continua exigindo igualdade byte a byte"
  artifacts:
    - supabase/migrations/20260823000006_p46_guard_purga.sql
    - supabase/migrations/20260823000007_p46_sweep_dry_run.sql
  key_links:
    - "`purga_execucao_itens` (item aberto) + `purga_execucoes.situacao='executando'` + `config_purga.modo='live'` → 4º ramo do guard — a segurança do motor destrutivo PASSA A SER a segurança destas três leituras"
    - "`anonimizar_candidato(id, true)` → SQLSTATE `P45DR` → `SQLERRM` capturado → `purga_execucao_itens.relato_dry_run` — a variável local sobrevive ao rollback da subtransação, e é assim que o relatório existe sem nada persistir"
    - "bloco de auto-verificação da migration → `has_table_privilege`/`pg_policies` sobre as duas tabelas do ledger — o apply para ANTES de criar a porta"
  prohibitions:
    - "O 4º ramo NUNCA é escrito relaxando a metade (a) do guard — aceitar chamador sem sessão sob `service_role` é a saída explicitamente recusada pelo operador em 2026-08-05, e continua recusada"
    - "A purga NUNCA é instrumento de exclusão dirigida — o 4º ramo exige o estado que SÓ o motor da purga produz, jamais uma credencial que uma pessoa possa portar"
---

<objective>
⛔ **O plano de maior risco da fase: ele edita uma função destrutiva que rodou em produção em
2026-08-22.**

Purpose: resolver o Blocker B-01. Medido em PROD, como `postgres` e sem claims, `auth.uid()` é
NULL, `auth.jwt()` é NULL e `request.jwt.claims` é NULL — as três metades do guard de
`anonimizar_candidato` recusam com `42501`. Um cron não tem sessão, não tem papel e não tem pedido
em `solicitacoes_dados`. Sem um caminho autorizado, a purga **não consegue nem fazer o dry-run**, e
o requisito de PURGA-02 (o dry-run pela MESMA expressão do delete real) fica impossível de cumprir.
D-46-18 resolve pela **Saída B**: um quarto ramo que aceita o chamador exclusivamente sob o estado
que só o motor da purga produz.

Output: a migration do guard com bloco de auto-verificação que **aborta o apply**, o laço de dry-run
que passa a chamar o motor de verdade, o re-pin cruzado do md5, e duas asserções que provam que o
ramo novo recusa fora das condições.

⚠ Este plano tem `checkpoint:decision` antes da edição e **code review bloqueante antes do apply** —
os dois são condição de fechamento do portão de fase destrutiva, não conselho.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-purga-autom-tica-dry-run-live/46-CONTEXT.md
@.planning/phases/46-purga-autom-tica-dry-run-live/46-RESEARCH.md
@.planning/phases/46-purga-autom-tica-dry-run-live/46-PATTERNS.md
@.planning/phases/46-purga-autom-tica-dry-run-live/46-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: [CHECKPOINT] D-46-18 — confirmar a Saída B e o escopo exato do 4º ramo</name>
  <decision>
    Editar `public.anonimizar_candidato(uuid, boolean)` — função destrutiva exercitada em produção
    em 2026-08-22 — acrescentando um quarto ramo autorizado ao guard, e definir se esse ramo
    autoriza também o **caminho de dry-run** ou só o destrutivo.
  </decision>
  <context>
    D-46-18 já escolheu a Saída B, e este checkpoint não a reabre: a Saída A (credencial de operador
    permanente) foi recusada por criar credencial standing capaz de destruir a PII de qualquer
    pessoa e por poluir a fila do RH com pedidos que ninguém fez; a Saída C (segundo motor
    destrutivo) foi recusada por contradizer D-46-12. O que este checkpoint decide é o **escopo**,
    porque o texto de D-46-18 e o contrato de validação puxam em direções diferentes e a diferença
    é de segurança:

    - D-46-18 diz, na letra: *"Um `modo` que não seja `live` não autoriza nada."*
    - `46-VALIDATION.md` exige a asserção (b): *"O loop de dry-run termina em `P45DR` e zero coluna
      mutou"* — e essa asserção roda sob `modo = 'dry_run'`, porque é durante o dry-run de 14 dias
      que ela precisa valer.

    Sob a leitura literal, o laço de dry-run recusaria em `dry_run` e a Phase 46 passaria 14 dias
    provando **nada** sobre o caminho do delete — exatamente o "dry-run que é decoração" que o SC#1
    nomeia e a mesma classe de falha do P39/CR-02.

    **Recomendação técnica:** o 4º ramo tem duas metades, espelhando a metade (b) que **já** tem
    duas formas por `v_dry_run` desde o 45-13:
    · caminho de **dry-run** (`p_dry_run = true`): autoriza sob `modo IN ('dry_run','live')`;
    · caminho **destrutivo** (`p_dry_run = false`): autoriza **só** sob `modo = 'live'`.
    Nas duas metades o resto das condições é idêntico e cumulativo: item vivo em
    `purga_execucao_itens` para aquele `candidato_id`, execução em `situacao = 'executando'`, item
    com `concluido_em IS NULL`. A frase de D-46-18 continua verdadeira no que ela protege — nenhum
    modo além de `live` autoriza **destruição**.
  </context>
  <options>
    <option id="escopo-duplo">
      <name>Recomendada — ramo com duas metades (dry-run sob `dry_run` ou `live`; destrutivo só sob `live`)</name>
      <pros>Torna a asserção (b) possível e o dry-run de 14 dias significativo; espelha a forma que a metade (b) já tem; nenhum modo além de `live` autoriza destruição</pros>
      <cons>Duas condições de modo em vez de uma; o revisor tem de conferir que a metade destrutiva não herdou a permissividade da metade de leitura</cons>
    </option>
    <option id="escopo-live-apenas">
      <name>Literal — o ramo exige `modo = 'live'` nas duas metades</name>
      <pros>Lê exatamente como D-46-18 está escrito; uma condição só</pros>
      <cons>⛔ O laço de dry-run recusa durante todo o período de `dry_run`; a asserção (b) fica impossível; os 14 dias provam zero sobre o caminho do delete — o falso verde que a fase inteira existe para evitar</cons>
    </option>
    <option id="adiar">
      <name>Adiar e usar `plano_exclusao_titular()` como dry-run</name>
      <pros>Não toca a função destrutiva agora</pros>
      <cons>⛔ O dry-run deixa de ser a MESMA expressão do delete real e vira "uma query equivalente" — recusado por PURGA-02 e pelo SC#1, e é a definição de dry-run como decoração</cons>
    </option>
  </options>
  <resume-signal>Selecione: escopo-duplo, escopo-live-apenas, ou adiar.</resume-signal>
  <acceptance_criteria>
    - A escolha está registrada no SUMMARY com a data e o texto integral do ramo aprovado
    - Se `escopo-duplo`: o SUMMARY registra explicitamente que a frase literal de D-46-18 foi lida como escopada à destruição, e por quê
    - Se `escopo-live-apenas` ou `adiar`: o SUMMARY registra qual asserção do contrato de validação deixa de ser satisfeita e quem aceitou essa perda
  </acceptance_criteria>
  <verify>
    <automated>grep -ciE 'escopo-(duplo|live-apenas)|adiar' .planning/phases/46-purga-autom-tica-dry-run-live/46-04-SUMMARY.md | awk '$1>=1{exit 0}{exit 1}'</automated>
  </verify>
  <done>O operador escolheu o escopo do 4º ramo e a escolha está escrita, com a razão, antes de qualquer linha da função destrutiva ser editada.</done>
  <reversibility rating="one-way">Reverter exige uma migration nova sobre uma função destrutiva viva, mais um terceiro re-pin de md5. D-46-18 é `one-way` na prática, e é por isso que este checkpoint existe.</reversibility>
</task>

<task type="auto">
  <name>Task 2: O 4º ramo do guard e o bloco de auto-verificação que aborta o apply</name>
  <files>supabase/migrations/20260823000006_p46_guard_purga.sql</files>
  <read_first>
    - `supabase/migrations/20260805000006_p45_anonimizar_candidato.sql:307-313` — a normalização da intenção, UM lugar só, e as três variáveis (`v_dry_run`, `v_uid`, `v_role`) que o corpo inteiro consome
    - `supabase/migrations/20260805000006_p45_anonimizar_candidato.sql:341-349` — a metade (a), sessão, que **NÃO é tocada**
    - `supabase/migrations/20260805000006_p45_anonimizar_candidato.sql:362-403` — a metade (b), papel, com as DUAS formas por `v_dry_run` desde o 45-13, e a declaração em `:372-375` de que aceitar sessão nula sob `service_role` é a saída RECUSADA
    - `supabase/migrations/20260805000006_p45_anonimizar_candidato.sql:405-449` — a metade (c), intenção: o modelo EXATO do ramo novo, incluindo a propriedade NULL-safe escrita em `:418-423` e a dependência de segurança nomeada em `:425-429`
    - `supabase/migrations/20260805000006_p45_anonimizar_candidato.sql:1000-1028` — o bloco de auto-verificação, **as duas metades**: a que aborta e a que apenas avisa e declara a RLS como parte do guard
    - `supabase/migrations/20260805000006_p45_anonimizar_candidato.sql:826-836` — o ACL: `REVOKE` de `PUBLIC, anon, authenticated` e `GRANT` só a `service_role`
    - `supabase/migrations/20260823000002_p46_ledger.sql` (do 46-02) — os nomes e vocabulários exatos de `purga_execucoes.situacao` e `purga_execucao_itens.concluido_em`
    - `46-04-SUMMARY.md` §decisão da Task 1 — o escopo aprovado
  </read_first>
  <action>
Uma migration cirúrgica, com escopo negativo em **uma linha** no topo dizendo que ela edita uma
função destrutiva viva e não faz mais nada — é isso que permite ao code review saber onde olhar.
Cabeçalho de seis partes do INV-1, sem transação explícita, delimitador de cifrão nomeado.

`CREATE OR REPLACE FUNCTION public.anonimizar_candidato(uuid, boolean)` — o arquivo carrega o corpo
**inteiro** da função viva, com **uma** mudança: o quarto ramo. Copiar o corpo do arquivo
`20260805000006`, não do catálogo, e conferir por md5 que a cópia é fiel **antes** de editar.

**O que NÃO muda, e é asserção:**
· A metade (a) permanece verbatim, mensagem inclusive. Aceitar sessão nula sob `service_role` é a
  saída recusada (DI-45-07-01 + decisão do operador de 2026-08-05) e continua recusada.
· A normalização `v_dry_run := coalesce(p_dry_run, true)` continua sendo o **único** lugar em que o
  parâmetro cru é lido. Uma leitura nova do parâmetro cru é a regressão a procurar em qualquer diff
  futuro deste arquivo.
· As metades (b) e (c) continuam valendo para os chamadores humanos. O ramo novo é uma **alternativa
  cumulativa**, escrita de modo que passar por ele exija todas as suas próprias condições.

**O quarto ramo**, no escopo aprovado na Task 1, escrito no idioma do arquivo:
uma variável `v_por_purga boolean` calculada por `EXISTS` correlacionado sobre
`public.purga_execucao_itens i` juntado a `public.purga_execucoes e` e a `public.config_purga cp`,
exigindo `i.candidato_id = p_candidato_id` **e** `i.concluido_em IS NULL` **e**
`e.situacao = 'executando'` **e** `e.modo_vigente` no conjunto autorizado pelo escopo da Task 1.
As comparações de papel continuam por `IS DISTINCT FROM`, e a verificação de estado por `EXISTS`
correlacionado — jamais negação por pertencimento a conjunto de valores, que avalia NULL e falha
ABERTO. ⚠ **Falha FECHADA por construção:** com qualquer lado nulo o predicado avalia NULL, a linha
não é selecionada, `v_por_purga` é falso e a função recusa — a mesma propriedade que `:418-423`
descreve para `executar_em`, e é por isso que não há cláusula `IS NOT NULL` extra.

As mensagens de recusa das metades (b) e (c) são estendidas para **nomear a quarta condição**: hoje
elas enumeram as condições exigidas, e uma mensagem que não menciona o caminho da purga faria a
próxima pessoa procurar o defeito no lugar errado às três da manhã.

**`COMMENT ON FUNCTION` reescrito** — o texto vivo declara a obrigação do chamador de passar as
claims e declara a saída recusada. O texto novo mantém as duas frases **e** acrescenta a dependência
de segurança que agora passa a valer: *de que este guard depende* deixa de ser só a segurança de
`solicitacoes_dados` e passa a incluir a de `purga_execucoes` e `purga_execucao_itens` — quem
escrever `situacao` e `concluido_em` naquelas tabelas autoriza a destruição. Dizer isso nominalmente
é o que obriga a próxima emenda a se justificar sozinha.

**O bloco de auto-verificação que ABORTA o apply**, espelho verbatim de `:1000-1028` com as tabelas
trocadas. Ele tem **as duas metades**, e as duas são obrigatórias:
· **Aborta** quando o papel `authenticated` PODE DE FATO escrever: tem o privilégio (por
  `has_table_privilege` ou `has_column_privilege` sobre `situacao` e `concluido_em`) **E** (a RLS
  está desligada **OU** existe policy de `UPDATE`/`ALL` que o alcance, consultada em `pg_policies`).
  A mensagem nomeia os três valores medidos e as saídas honestas, e o apply para **AQUI, antes de
  criar essa porta**.
· **Avisa** — `RAISE NOTICE`, sem abortar — quando o privilégio existe mas a RLS está ligada e não
  há policy que alcance, declarando que **a RLS daquela tabela virou parte do guard de intenção
  desta função**. Um alarme parcial que aborta treina quem executa a desligá-lo; um que avisa e
  registra a dependência é o que o precedente faz.

ACL ao fim: `REVOKE ALL ON FUNCTION public.anonimizar_candidato(uuid, boolean) FROM PUBLIC, anon,
authenticated;` seguido de `GRANT EXECUTE … TO service_role;` — `CREATE OR REPLACE` não repõe
grants, mas reemitir é barato e o `pg_default_acl` deste schema é agressivo o bastante para
justificar a redundância.
  </action>
  <acceptance_criteria>
    - `supabase/migrations/20260823000006_p46_guard_purga.sql` existe e o escopo negativo do cabeçalho declara em uma linha que ela edita função destrutiva viva
    - Sem transação explícita: `grep -c 'BEGIN;' <arquivo>` retorna `0` · sem cifrão anônimo: `grep -cE '[$][$]' <arquivo>` retorna `0`
    - A metade (a) está intacta: `grep -c 'chamador sem sessao nao anonimiza ninguem' <arquivo>` retorna `1`
    - A intenção é normalizada uma vez só: `grep -c 'coalesce(p_dry_run, true)' <arquivo>` retorna `1`, e fora de linhas de comentário o parâmetro cru aparece exatamente duas vezes — `grep -vE '^\s*--' <arquivo> | grep -c 'p_dry_run'` retorna `2` (a assinatura e a normalização). Uma terceira leitura do parâmetro cru é a regressão que o próprio arquivo manda procurar
    - O ramo novo é correlacionado e NULL-safe: `grep -viE '^\s*--' <arquivo> | grep -cE '\mNOT[[:space:]]+IN\M'` retorna `0`
    - O ramo novo lê as três fontes de estado: `grep -c 'purga_execucao_itens' <arquivo>` retorna `>= 2`, `grep -c "situacao = 'executando'" <arquivo>` retorna `>= 1`, `grep -c 'config_purga' <arquivo>` retorna `>= 2`
    - O ramo exige item aberto: `grep -c 'concluido_em IS NULL' <arquivo>` retorna `>= 1`
    - O bloco de auto-verificação tem as DUAS metades: `grep -c 'RAISE EXCEPTION' <arquivo>` inclui a do bloco, e `grep -c 'RAISE NOTICE' <arquivo>` retorna `>= 1`; o bloco consulta `has_table_privilege`, `has_column_privilege`, `pg_class.relrowsecurity` e `pg_policies` — os quatro presentes
    - O ACL nomeia `anon`: `grep -c 'FROM PUBLIC, anon, authenticated' <arquivo>` retorna `>= 1` e `grep -c 'TO service_role' <arquivo>` retorna `>= 1`
    - O `COMMENT ON FUNCTION` nomeia as duas tabelas do ledger como dependência de segurança: `grep -c 'purga_execucoes' <arquivo>` retorna `>= 3`
    - ⊖ Não-vacuidade: o bloco de auto-verificação é executado no apply e sua condição é aferida sobre o catálogo vivo — se as tabelas do 46-02 tivessem sido criadas com policy de escrita, o apply REPROVA em vez de passar em silêncio
  </acceptance_criteria>
  <verify>
    <automated>F=supabase/migrations/20260823000006_p46_guard_purga.sql; test -f "$F" && grep -c 'BEGIN;' "$F" | grep -qx 0 && grep -cE '[$][$]' "$F" | grep -qx 0 && grep -c 'chamador sem sessao nao anonimiza ninguem' "$F" | grep -qx 1 && grep -c 'coalesce(p_dry_run, true)' "$F" | grep -qx 1 && grep -viE '^\s*--' "$F" | grep -cE '\mNOT[[:space:]]+IN\M' | grep -qx 0 && grep -c 'has_table_privilege' "$F" | awk '$1>=1{exit 0}{exit 1}' && grep -c 'pg_policies' "$F" | awk '$1>=1{exit 0}{exit 1}' && echo OK</automated>
  </verify>
  <done>A migration do guard existe, a metade (a) está verbatim intacta, o ramo novo exige o estado que só o motor da purga produz e falha fechada, e o bloco de auto-verificação com as duas metades está no arquivo.</done>
  <reversibility rating="one-way">Edita função destrutiva viva; reverter exige migration nova + terceiro re-pin. Precedido por `checkpoint:decision` (Task 1).</reversibility>
</task>

<task type="auto">
  <name>Task 3: O laço de dry-run passa a chamar o motor, e as duas asserções que provam o ramo</name>
  <files>supabase/migrations/20260823000007_p46_sweep_dry_run.sql, supabase/tests/p45_motor_exclusao_smoke.sql, supabase/tests/p46_purga_smoke.sql</files>
  <read_first>
    - `supabase/migrations/20260823000004_p46_sweep_tracer.sql` (do 46-02) — o corpo que esta migration substitui, com o bloco por titular já vazio à espera da chamada
    - `supabase/migrations/20260805000006_p45_anonimizar_candidato.sql:758-777` — o terminador de dry-run e por que a forma de dois corpos é o parente direto do CR-02 da P39
    - `.planning/phases/46-purga-autom-tica-dry-run-live/46-RESEARCH.md` §Pattern 2 — a forma recomendada do laço, incluindo a razão pela qual a variável local sobrevive ao rollback da subtransação
    - `supabase/functions/executar-direito-titular/index.ts:975-999` — o tratamento do lado do chamador: o SQLSTATE de dry-run chegando no caminho real vira erro atribuído a passo, jamais sucesso
    - `supabase/tests/p45_motor_exclusao_smoke.sql:217-265` — a proveniência dos md5 e a única divergência autorizada
    - `supabase/tests/p45_motor_exclusao_smoke.sql:1584-1614` — a asserção (C3) e o pin `8c86e0f040219e7eade47eb587dbf5de` na linha 1591
    - `supabase/tests/p46_purga_smoke.sql` — o envelope e o contador já existentes
  </read_first>
  <action>
**A migration `20260823000007_p46_sweep_dry_run.sql`** — `CREATE OR REPLACE FUNCTION
public.varrer_purga_retencao()`, corpo idêntico ao do 46-02 exceto pelo interior do bloco por
titular, que passa a ser o laço completo:

Para cada titular da temporária: `PERFORM public.anonimizar_candidato(r.candidato_id, true)` dentro
de um `BEGIN … EXCEPTION … END`. **Chegar à linha seguinte à chamada é DEFEITO**: significa que o
terminador do dry-run sumiu e a transação teria commitado. Essa linha é um `RAISE EXCEPTION` com o
ERRCODE próprio **`P46NT`**, cuja mensagem diz exatamente isso. A cláusula de exceção captura
`WHEN SQLSTATE 'P45DR'` e **só** ele, gravando `SQLERRM` numa variável local — que sobrevive ao
rollback da subtransação, e é por isso que o relatório existe sem nada ter persistido.

⚠ **A captura genérica de exceção é proibida neste laço**, e a razão está escrita no motor
(`20260805000006:765-768`): um erro real disfarçado de "dry-run concluído" seria o pior falso verde
desta fase, e no sentido inverso o SQLSTATE de dry-run chegando no caminho real passaria por sucesso
quando nada foi apagado. Bônus mecânico: a captura tipada deixa `QUERY_CANCELED` derrubar a
execução em vez de virar item de ledger falso.

O item de `purga_execucao_itens` é inserido **depois** do bloco, com `relato_dry_run` recebendo a
variável local. Ao fim, `veredito = 'dry_run'`, `processados = 0` (dry-run não processa ninguém),
`situacao = 'concluida'`, `concluida_em = pg_catalog.now()`.

⚠ **Ainda sem dispatch e sem leitura de Vault** — o ramo `live` chega no 46-06. Dito em uma linha no
escopo negativo do arquivo.

**A asserção (b) em `p46_purga_smoke.sql` (PURGA-02).** Dentro do envelope, com `modo = 'dry_run'`:
rodar `varrer_purga_retencao()` sobre o conjunto não-vazio e provar as três coisas juntas —
(i) toda linha de `purga_execucao_itens` da execução tem `relato_dry_run` **não nulo** e contendo o
identificador do terminador do motor; (ii) as cinco contagens de domínio idênticas antes e depois;
(iii) `elegiveis >= 3`. Uma asserção que só medisse (ii) passaria por vacuidade sobre conjunto
vazio; é (i) + (iii) que a tornam prova.

**A asserção (o) em `p46_purga_smoke.sql` (B-01).** ⊖ O ramo novo recusa fora das condições, provado
por **quatro** chamadas diretas, cada uma esperando `42501`:
· com `config_purga.modo = 'off'` e item aberto → recusa;
· com `modo = 'dry_run'` e `p_dry_run := false` → recusa (a metade destrutiva exige `live`);
· com `modo = 'live'` mas sem item aberto para aquele `candidato_id` → recusa;
· com `modo = 'live'` e item aberto porém `purga_execucoes.situacao = 'concluida'` → recusa.
Cada uma capturada por `WHEN SQLSTATE '42501'` e contabilizada; reprovar se qualquer uma **não**
levantar. ⚠ Uma quinta chamada, positiva, com as três condições satisfeitas e `p_dry_run := true`,
tem de **passar** e terminar em `P45DR` — sem ela, a asserção (o) provaria apenas que a função
recusa tudo, que é o modo de falha nº 3 dos sete portões da Phase 45.

**O re-pin cruzado de (C3) em `p45_motor_exclusao_smoke.sql`.** O pin
`8c86e0f040219e7eade47eb587dbf5de` da linha 1591 deixa de valer. Extrair o md5 **do arquivo**
`20260823000006` entre os dois delimitadores nomeados, pelo comando registrado em
`p45_motor_exclusao_smoke.sql:852-858`, e escrever o valor novo. Atualizar o bloco de PROVENIÊNCIA
com o valor, os octetos, a data, e a frase de que a conferência foi **cruzada** — o lado vivo fecha
na Task 4.
⚠ **O re-pin não pode virar desculpa para afrouxar a asserção** (Pitfall 2 / D-46-18 obrigação 4).
A rede estrutural embaixo do md5 de (C3) **ganha** checagens: além das que já existem, passa a
exigir que o corpo vivo contenha `purga_execucao_itens` e `concluido_em IS NULL`, e que **não**
contenha a forma banida de negação por conjunto. Um md5 que casa com a forma errada significa que
alguém re-pinou sem a migration ter mudado.
  </action>
  <acceptance_criteria>
    - `supabase/migrations/20260823000007_p46_sweep_dry_run.sql` existe; sem transação explícita e sem cifrão anônimo (`grep -c 'BEGIN;'` = `0`, `grep -cE '[$][$]'` = `0`)
    - O laço chama o motor: `grep -c 'anonimizar_candidato' supabase/migrations/20260823000007_p46_sweep_dry_run.sql` retorna `>= 1`
    - A captura é tipada e única, aferido fora de comentários (o corpo **precisa** poder explicar por que a captura genérica é proibida sem invalidar o portão): `grep -c "WHEN SQLSTATE 'P45DR'" <arquivo>` retorna `1` e `grep -vE '^\s*--' <arquivo> | grep -c 'WHEN OTHERS'` retorna `0`
    - O caminho de "não levantou" é defeito: `grep -c "ERRCODE = 'P46NT'" <arquivo>` retorna `1`
    - Ainda sem dispatch, aferido fora de comentários: `grep -vE '^\s*--' <arquivo> | grep -c 'net.http_post'` retorna `0` e `grep -vE '^\s*--' <arquivo> | grep -ci 'vault'` retorna `0`
    - (o) tem cinco casos, quatro negativos e um positivo: `grep -cE "^-- \(o\)" supabase/tests/p46_purga_smoke.sql` retorna `1` e o bloco contém `>= 4` capturas de `42501` mais uma expectativa de `P45DR`
    - (b) prova as três coisas juntas: o bloco assere `relato_dry_run IS NOT NULL`, as cinco contagens e `elegiveis >= 3`
    - O pin de (C3) mudou: a **atribuição** deixou de carregar o valor antigo — `grep -c "v_pin_anon  text := '8c86e0f040219e7eade47eb587dbf5de'" supabase/tests/p45_motor_exclusao_smoke.sql` retorna `0` —, o valor novo tem 32 caracteres hexadecimais, e o antigo **continua no bloco de PROVENIÊNCIA** como histórico
    - A rede estrutural de (C3) cresceu: o bloco (C3) contém checagem de `purga_execucao_itens` e de `concluido_em`, mais a negativa da forma banida por fronteira de palavra
    - ⊖ Não-vacuidade: (b) e (o) reprovam se `elegiveis = 0`; e (o) reprova se a chamada positiva **não** passar — provar só recusa é o modo de falha nº 3 dos sete portões
  </acceptance_criteria>
  <verify>
    <automated>F=supabase/migrations/20260823000007_p46_sweep_dry_run.sql; test -f "$F" && grep -c "WHEN SQLSTATE 'P45DR'" "$F" | grep -qx 1 && grep -vE '^\s*--' "$F" | grep -c 'WHEN OTHERS' | grep -qx 0 && grep -c "ERRCODE = 'P46NT'" "$F" | grep -qx 1 && grep -vE '^\s*--' "$F" | grep -c 'net.http_post' | grep -qx 0 && grep -c "v_pin_anon  text := '8c86e0f040219e7eade47eb587dbf5de'" supabase/tests/p45_motor_exclusao_smoke.sql | grep -qx 0 && echo OK</automated>
  </verify>
  <done>O laço de dry-run chama o motor com captura tipada, o caminho de "não levantou" é tratado como defeito com ERRCODE próprio, e as duas asserções mais o re-pin de (C3) estão escritos com proveniência.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] Code review bloqueante ANTES do apply, depois apply pelo ORQUESTRADOR</name>
  <what-built>
    Duas migrations: o 4º ramo do guard de uma função destrutiva viva (`20260823000006`) e o laço de
    dry-run que passa a chamar o motor (`20260823000007`). Mais o re-pin de (C3) e as asserções (b)
    e (o). **Nada foi aplicado.**
  </what-built>
  <precondition>A migration `20260823000005` do 46-03 está aplicada, e `public.config_purga.modo` está em `'off'`.</precondition>
  <how-to-verify>
    ⚠ **O code review vem ANTES do apply.** É item do portão de fase destrutiva do M8, e este é o
    plano em que ele mais importa: a Phase 45 fechou 9 blockers em 3 rodadas de review, e **zero**
    chegou a produção.

    **1. Code review bloqueante** (`/gsd-code-review` ou revisor humano), escopado a
    `20260823000006` e `20260823000007`, com estas perguntas nomeadas:
    · A metade (a) está verbatim intacta? (diff contra `20260805000006:341-349`)
    · O ramo novo é cumulativo, ou algum `OR` mal colocado deixa uma condição sozinha autorizar?
    · A metade destrutiva do ramo exige `modo = 'live'`, ou herdou a permissividade da metade de
      leitura?
    · `p_dry_run` é lido em exatamente dois lugares (assinatura e normalização)?
    · O bloco de auto-verificação tem as **duas** metades, e a que aborta exige privilégio **E**
      (RLS desligada **OU** policy que alcance)?
    · O laço tem captura tipada e nenhuma captura genérica?
    · Alguma mensagem de erro carrega PII ou valor de segredo?
    **Blocker aberto ⇒ o apply não acontece.** Registrar o veredito em `46-REVIEW.md`.

    **2. Apply, na ordem `000006 → 000008 → 000009 → 000007`**, **pelo orquestrador**:
    · ⚠⚠ **RD4-02 — ESTE BLOCO ESTAVA ERRADO EM TRÊS PONTOS, e os três eram perigosos.**
      Ele dizia *"na ordem 000006 → 000007, por MCP `apply_migration`"* + *"Reparo da `version`"*.
      (1) **Omitia a `008` e a `009`**, que nasceram depois deste plano ser escrito. Seguir a
      letra ressuscitaria o **HI-01**: sem a `008`, o guard próprio de `plano_exclusao_titular`
      recusa o chamador e todo titular sai com `desfecho_postgres='falha'` — com o diagnóstico
      apontando para a `006`, que estaria correta; sem a `009`, a primeira reconciliação aborta
      com `23514`, porque é ela que alarga o `CHECK` para aceitar `desconhecido`.
      (2) A **via MCP é justamente a que o `CLAUDE.md` substituiu**, porque ela *transcreve* o
      SQL — foi assim que duas das cinco migrations do M8 chegaram a PROD com os comentários
      descartados.
      (3) O **reparo de `version` está declarado OBSOLETO** no `CLAUDE.md`: pela via atual a
      `version` nasce correta, do nome do arquivo. Rodar aquele `UPDATE` hoje é ruído no melhor
      caso e corrupção do ledger no pior.
    · **Via obrigatória:** a corrente do `CLAUDE.md` §"Via de apply ATUAL" — Management API, com
      o SQL **lido do arquivo** (`p46apply.cjs migrate <arquivo>`), migration e linha do ledger
      na **mesma requisição** (o endpoint é atômico; medido por sonda).
    · ⚠ O apply de `20260823000006` **pode abortar de propósito** — o bloco de auto-verificação
      levanta exceção se `authenticated` puder escrever no ledger. Se abortar, isso **não é um bug
      do apply**: é o pressuposto de segurança recusando. Investigar a RLS das duas tabelas do
      46-02 antes de qualquer outra coisa, e nunca contornar removendo o bloco.
    · Conferência cruzada `md5(statements[1])` × md5 do arquivo, **os dois lados registrados**.
    3. `npm run db:types && npm run lint`.
    4. **Fechar o re-pin cruzado de (C3):**
       `SELECT md5(prosrc), octet_length(prosrc) FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace WHERE n.nspname='public' AND p.proname='anonimizar_candidato';`
       tem de bater o valor escrito na Task 3. Divergência com md5 do apply batendo ⇒ é de
       **extração**; corrigir a extração, **nunca** afrouxar a asserção.
    5. Rodar, cada um numa única chamada de MCP `execute_sql`: `p45_motor_exclusao_smoke.sql`
       (24/24 + C3 re-pinado) · `p46_purga_smoke.sql` · `p43_previa_smoke.sql` ·
       `p43_matriz_retencao_smoke.sql` · `p42_invent05_cron_smoke.sql`.
    6. **A prova ao vivo, sobre a fixture:** `UPDATE public.config_purga SET modo = 'dry_run';` ·
       `SELECT public.varrer_purga_retencao();` · conferir que todo item da execução tem
       `relato_dry_run` não nulo, `processados = 0`, `veredito = 'dry_run'`, e ⊖ as cinco contagens
       de domínio idênticas. **Deixar `modo` em `'off'` ao final.**

    ⚠ Zero `--no-verify` em qualquer commit deste plano.
  </how-to-verify>
  <resume-signal>Responda "aprovado" com o veredito do review, os 2 pares de md5 do apply, o par do re-pin de (C3), o contador de cada smoke, e o resultado do passo 6 — ou descreva o blocker.</resume-signal>
  <acceptance_criteria>
    - `46-REVIEW.md` existe com veredito e zero blocker aberto **antes** do timestamp do apply
    - As duas linhas existem em `supabase_migrations.schema_migrations` com a `version` do arquivo, sem buracos desde `20260823000001`
    - Os dois pares `md5(statements[1])` × md5 do arquivo batem, os dois lados registrados
    - O bloco de auto-verificação **executou** — o apply de `20260823000006` produziu o `RAISE NOTICE` ou passou silencioso; um apply que não exercitou o bloco é indistinguível de um bloco ausente e reprova
    - O re-pin de (C3) fecha com os dois lados medidos (vivo × arquivo)
    - `p45_motor_exclusao_smoke.sql` verde com o contador cheio, incluindo (C3)
    - `p46_purga_smoke.sql` verde, com (b) e (o) entre as asserções contabilizadas
    - A varredura em `dry_run` produz `relato_dry_run` não nulo em **todos** os itens, `processados = 0`, `elegiveis >= 3`
    - ⊖ As cinco contagens de domínio idênticas antes e depois · ⊖ nenhum item com desfecho de Storage/Postgres/Auth carimbado · ⊖ `net.http_request_queue` sem linha nova
    - `config_purga.modo` fica em `'off'` ao final
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/46-purga-autom-tica-dry-run-live/46-REVIEW.md && npm run db:types && npm run lint && echo OK</automated>
  </verify>
  <done>O review bloqueante passou antes do apply, as duas migrations estão aplicadas com `version` reparada e md5 conferido dos dois lados, o bloco de auto-verificação executou, o re-pin de (C3) fechou medido, e a varredura em `dry_run` produziu relatório por titular sem apagar nada.</done>
</task>

</tasks>

## Artifacts this phase produces (inventário completo da Phase 46)

> Inventário idêntico nos sete planos. Os itens **em negrito** nascem NESTE plano.

**Tabelas novas:** `public.config_purga` · `public.retencao_hold` · `public.purga_execucoes` ·
`public.purga_execucao_itens`
**Coluna nova:** `public.config_retencao_etapa.elegivel_purga`
**Funções novas:** `public.titulares_alem_da_janela()` · `public.varrer_purga_retencao()` ·
`public.salvar_config_purga(...)` · `public.reivindicar_item_purga(uuid, uuid)` ·
`public.concluir_item_purga(uuid, jsonb)`
**Funções editadas:** `public.candidaturas_alem_da_janela()` ·
**`public.anonimizar_candidato(uuid, boolean)` — 4º ramo autorizado (D-46-18 / Blocker B-01)** ·
**`public.varrer_purga_retencao()` — v2, o laço passa a chamar o motor**
**Cron job novo:** `purga-retencao-sweep` — 46-06 · **Edge Function nova:** `purgar-retencao` — 46-05
**ERRCODEs:** `P46B0` · **`P46NT`** (dry-run não terminou no SQLSTATE do motor) · `P46FB` · `P46RN`
**Migrations deste plano:** **`20260823000006_p46_guard_purga.sql`** ·
**`20260823000007_p46_sweep_dry_run.sql`**
**Letras de asserção deste plano:** **(b)(o)**
**Emendas deste plano:** **`supabase/tests/p45_motor_exclusao_smoke.sql` (C3) — re-pin cruzado**
**Artefato de review:** **`46-REVIEW.md`** (bloqueante, anterior ao apply)

## Suposições sinalizadas (edges não resolvidas)

O probe de PURGA-04 veio `unclassified` e permanece **não resolvido**. Além dele, este plano
carrega uma **suposição de projeto agora fechada por medição**: `[ASSUMED A3]` da RESEARCH
(`auth.uid()` NULL sob `pg_cron`) foi confirmado por execução em PROD em 2026-08-22 e re-aferido no
46-01 (M3) — deixou de ser suposição.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chamador sem sessão (`postgres`/cron) → `anonimizar_candidato` | O caminho que este plano **abre**; é a superfície de autorização nova da fase |
| `authenticated` → `purga_execucoes` / `purga_execucao_itens` | A segurança do guard destrutivo PASSA A SER a segurança destas duas tabelas |
| `config_purga.modo` → autorização de destruição | Um `modo` mal setado é a diferença entre relatório e destruição irreversível |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-04-01 | Elevation of Privilege | 4º ramo autorizando mais do que pretende (um `OR` mal colocado) | critical | mitigate | Condições cumulativas num único `EXISTS` correlacionado; asserção (o) com **quatro** casos negativos e um positivo; code review bloqueante com a pergunta nomeada |
| T-46-04-02 | Elevation of Privilege | `authenticated` escrevendo no ledger e assim autorizando a própria destruição | critical | mitigate | Bloco de auto-verificação que **ABORTA o apply** (espelho de `20260805000006:1022-1027`) mais a metade que avisa e declara a RLS como parte do guard |
| T-46-04-03 | Elevation of Privilege | relaxamento da metade (a) para "aceitar sessão nula sob service_role" | critical | mitigate | Saída explicitamente recusada (DI-45-07-01 + operador 2026-08-05); asserção estática de que a mensagem da metade (a) continua verbatim no arquivo |
| T-46-04-04 | Tampering | md5 re-pinado sem a migration ter mudado, esvaziando (C3) | high | mitigate | Conferência cruzada vivo × arquivo, bloco de proveniência com data e octetos, e rede estrutural que só cresce; a única divergência autorizada está escrita em `p45_motor_exclusao_smoke.sql:257-264` |
| T-46-04-05 | Repudiation | erro real disfarçado de "dry-run concluído" | high | mitigate | Captura tipada pelo SQLSTATE do terminador, nunca genérica; e o caminho "não levantou" vira `P46NT`, porque não lançou nunca foi o mesmo que completou |
| T-46-04-06 | Denial of Service | dry-run que commita porque o terminador sumiu do motor | critical | mitigate | `P46NT` levantado na linha seguinte à chamada; a asserção (b) exige `relato_dry_run` não nulo em todos os itens e as cinco contagens idênticas |
| T-46-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | Nenhum pacote novo (RESEARCH §Package Legitimacy Audit — tabela vazia) |
</threat_model>

<verification>
- `46-REVIEW.md` com veredito e zero blocker aberto, com timestamp anterior ao apply
- Portões estáticos verdes nas duas migrations (metade (a) verbatim, `p_dry_run` lido 2×, captura tipada, zero captura genérica, zero dispatch, sem negação por conjunto)
- `npm run lint && npm run test:run` verde · `npm run db:types` regenerado
- Os cinco smokes verdes por execução; `p45_motor_exclusao_smoke.sql` com (C3) re-pinado e o contador cheio
- A varredura em `dry_run` produziu relatório por titular, com as cinco contagens de domínio idênticas
</verification>

<success_criteria>
O cron passa a poder pedir ao motor provado o **relatório** de uma exclusão sem obter a exclusão, e
o caminho destrutivo continua fechado para todo chamador que não esteja dentro de uma execução de
purga em `live`. O pressuposto de que `authenticated` não escreve no ledger deixou de ser confiança
e virou uma asserção que aborta o apply.
</success_criteria>

<output>
Create `.planning/phases/46-purga-autom-tica-dry-run-live/46-04-SUMMARY.md` when done
</output>
