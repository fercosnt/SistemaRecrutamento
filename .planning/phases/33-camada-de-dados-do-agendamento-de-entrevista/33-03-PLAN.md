---
phase: 33-camada-de-dados-do-agendamento-de-entrevista
plan: 03
type: execute
wave: 3
depends_on: ["33-01", "33-02"]
files_modified:
  - database.types.ts
autonomous: false
requirements: [AGEND-01, SEG-03]
must_haves:
  truths:
    - "Migration 20260716000001_agendamentos_entrevista is applied LIVE to PROD via Supabase MCP apply_migration (bypasses 42601)"
    - "supabase_migrations.schema_migrations has exactly ONE version row for 20260716000001 (ledger reconciled — filename == version, Pitfall 6)"
    - "database.types.ts (repo ROOT) is regenerated and includes agendamentos_entrevista + get_meu_agendamento; tsc baseline stays <= 104"
    - "All 8 seg33 smoke assertions emit PASS in PROD (SEG-03 acceptance gate — count them; zero SKIP) and supabase db push --linked reports Remote database is up to date"
  artifacts:
    - path: "database.types.ts"
      provides: "regenerated Postgres types including the new table + RPC"
      contains: "agendamentos_entrevista"
  key_links:
    - from: "MCP apply_migration(name='20260716000001_agendamentos_entrevista')"
      to: "supabase_migrations.schema_migrations.version"
      via: "version row == filename timestamp"
      pattern: "20260716000001"
    - from: "seg33_agendamento_smokes.sql"
      to: "PROD agendamentos_entrevista + get_meu_agendamento"
      via: "MCP execute_sql → 8 PASS notices"
      pattern: "PASS \\("
---

<objective>
[BLOCKING] Land the Phase 33 data layer to PROD and prove SEG-03. Apply `supabase/migrations/20260716000001_agendamentos_entrevista.sql` via Supabase MCP `apply_migration` (NEVER `supabase db push` — 42601 on `$$` bodies via the pooler, CLAUDE.md §Migrations), reconcile the `schema_migrations` ledger (Pitfall 6), regenerate `database.types.ts` at repo ROOT, then run `seg33_agendamento_smokes.sql` via MCP `execute_sql` and confirm all 8 assertions PASS. This is the SEG-03 acceptance gate — the behavioral smoke is authoritative above any structural check.

Purpose: Turn the RED smoke GREEN in PROD; make the table + RLS + RPC real for P34 (RH form) and P35 (candidate card) to build on, with isolation proven before any UI exists.

Output: applied migration (live), reconciled ledger row, regenerated `database.types.ts`, and 8 GREEN smoke assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33-camada-de-dados-do-agendamento-de-entrevista/33-RESEARCH.md
@.planning/phases/33-camada-de-dados-do-agendamento-de-entrevista/33-VALIDATION.md
@CLAUDE.md   # §Migrations — the 42601 apply_migration workaround + repair/ledger; database.types.ts at ROOT

# Artifacts produced by 33-01/33-02 (apply these):
@supabase/migrations/20260716000001_agendamentos_entrevista.sql
@supabase/tests/seg33_agendamento_smokes.sql

<interfaces>
<!-- The apply + gate sequence (RESEARCH "Apply + gate sequence (P33-03, BLOCKING)"). -->
1. MCP apply_migration(name='20260716000001_agendamentos_entrevista', query=<full file text>)
2. MCP execute_sql: SELECT version FROM supabase_migrations.schema_migrations WHERE version='20260716000001';  -- expect exactly 1 row
   (if the ledger drifted — MCP recorded a timestamp-version ≠ filename, Phase 11 landmine — reconcile with a single UPDATE, P27 idiom)
3. npm run db:types     -- regen database.types.ts at repo ROOT; git-diff shows agendamentos_entrevista + get_meu_agendamento
4. npm run lint         -- tsc --noEmit; error count must stay <= 104 baseline
5. MCP execute_sql(<supabase/tests/seg33_agendamento_smokes.sql>)  -- confirm 8x "PASS (…)" notices, ZERO "SEG-33 SKIP"
6. supabase db push --linked   -- must report "Remote database is up to date"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply migration to PROD via MCP + reconcile schema_migrations ledger</name>
  <files>supabase/migrations/20260716000001_agendamentos_entrevista.sql</files>
  <read_first>
    - `supabase/migrations/20260716000001_agendamentos_entrevista.sql` (the full file to apply — table + trigger + RLS + RPC)
    - `.planning/phases/33-.../33-RESEARCH.md` Pitfall 6 (MCP records version from `name` → pass the EXACT filename version `20260716000001`; confirm exactly one ledger row; reconcile drift with one UPDATE — P27 converged 42 drift rows this way)
    - `CLAUDE.md` §Migrations (apply_migration bypasses 42601; `migration repair --status applied <version>` is the CLI fallback if MCP drifts)
    - `.planning/STATE.md` decision `[M2/Phases 6–15 · M4]` (MCP apply_migration path)
  </read_first>
  <action>
    Apply the migration LIVE using Supabase MCP `apply_migration` with `name='20260716000001_agendamentos_entrevista'` and `query` = the full text of the file. Then via MCP `execute_sql` run `SELECT version FROM supabase_migrations.schema_migrations WHERE version = '20260716000001'` and confirm EXACTLY one row. If the recorded version differs from the filename prefix (Pitfall 6 drift), reconcile with a single `UPDATE supabase_migrations.schema_migrations SET version='20260716000001' WHERE version='<drifted>'` (P27 idiom) so the filename and the ledger agree. Do NOT use `supabase db push` to apply (42601). Confirm the objects exist: `agendamentos_entrevista` table, `rh_gerencia_agendamento` policy, `get_meu_agendamento` function, `trg_agendamento_normaliza_vaga` trigger.
  </action>
  <verify>
    <automated>MISSING — verified live via MCP: SELECT version FROM supabase_migrations.schema_migrations WHERE version='20260716000001' returns exactly 1 row; SELECT to_regclass('public.agendamentos_entrevista') IS NOT NULL; SELECT count(*) FROM pg_policies WHERE tablename='agendamentos_entrevista' = 1; SELECT to_regprocedure('public.get_meu_agendamento(uuid)') IS NOT NULL. Record results in SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - Migration applied via MCP `apply_migration` (not `db push`).
    - `schema_migrations` has exactly one row for version `20260716000001` (filename == ledger; drift reconciled if any).
    - Live objects confirmed: `public.agendamentos_entrevista` (table), `rh_gerencia_agendamento` (exactly 1 policy on the table), `public.get_meu_agendamento(uuid)` (function), `trg_agendamento_normaliza_vaga` (trigger).
  </acceptance_criteria>
  <done>Ledger row present + reconciled; all four objects live in PROD.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate database.types.ts at repo ROOT + tsc baseline</name>
  <files>database.types.ts</files>
  <read_first>
    - `CLAUDE.md` §Commands (`npm run db:types` regen; `database.types.ts` is at repo ROOT, NEVER hand-edited) + `.planning/STATE.md` tsc baseline note (104)
    - `.planning/phases/33-.../33-RESEARCH.md` "Apply + gate sequence" steps 3-4
    - `database.types.ts` (repo ROOT — the file that regenerates)
  </read_first>
  <action>
    Run `npm run db:types` to regenerate `database.types.ts` at the repo ROOT against live PROD (now that Task 1 applied the migration). Confirm the git-diff shows the new `agendamentos_entrevista` Row/Insert/Update types and the `get_meu_agendamento` function type. Do NOT hand-edit the file. Run `npm run lint` (`tsc --noEmit`) and confirm the error count stays at or below the 104 baseline (P33 adds no TS logic, so tsc must not increase). If `npm run db:types` is blocked by CLI auth, fall back to MCP `generate_typescript_types` and write its output to `database.types.ts` (Environment Availability fallback).
  </action>
  <verify>
    <automated>grep -Eq "agendamentos_entrevista" database.types.ts && grep -Eq "get_meu_agendamento" database.types.ts && npm run lint 2>&1 | tail -5; echo "TYPES_REGEN (confirm tsc error count <= 104)"</automated>
  </verify>
  <acceptance_criteria>
    - `database.types.ts` (repo ROOT) contains `agendamentos_entrevista` and `get_meu_agendamento`.
    - File was regenerated (not hand-edited).
    - `npm run lint` error count <= 104 baseline (no increase).
  </acceptance_criteria>
  <done>Regenerated types include the new table + RPC; tsc baseline held.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SEG-03 acceptance gate — run seg33 smoke, confirm all 8 PASS (zero SKIP)</name>
  <files>supabase/tests/seg33_agendamento_smokes.sql</files>
  <action>Run supabase/tests/seg33_agendamento_smokes.sql via Supabase MCP execute_sql; confirm all 8 PASS (a..h) notices are present with ZERO SEG-33 SKIP lines (Pitfall 2 — count the PASS notices, "no EXCEPTION" is insufficient); then run `supabase db push --linked` and confirm "Remote database is up to date". Blocking human-verify gate: do NOT close the phase on any FAIL (a real leak) or SKIP (fixture failure) — return the failing assertion for gap-closure.</action>
  <read_first>
    - `supabase/tests/seg33_agendamento_smokes.sql` (the 8-assertion gate to run)
    - `.planning/phases/33-.../33-VALIDATION.md` "GREEN gate rule (Pitfall 2)" — count the PASS notices; all 8 must be present, zero SKIP
    - `.planning/phases/33-.../33-RESEARCH.md` Pitfall 2 (SKIP masquerading as success) + Pitfall 6 (ledger/`db push` up-to-date)
  </read_first>
  <acceptance_criteria>
    - All 8 notices `PASS (a)`…`PASS (h)` present in the MCP execute_sql output.
    - ZERO `SEG-33 SKIP` lines (a SKIP = fixture did not build = gate NOT proven).
    - Discriminating `PASS (c)` (spoofed-`vaga_id` INSERT denied) + `PASS (e)`/`PASS (f)` (candidate direct read = 0; RPC allowlist excludes `observacoes_rh`) confirmed.
    - `supabase db push --linked` reports "Remote database is up to date" (ledger reconciled).
    - On any FAIL or SKIP: phase does NOT close; failing assertion returned for gap-closure.
  </acceptance_criteria>
  <what-built>
    The full Phase 33 data layer is now live in PROD: `agendamentos_entrevista` (table + vaga_id-normalization trigger), the single WR-04 join-through RH policy `rh_gerencia_agendamento`, and the candidate DEFINER allowlist RPC `get_meu_agendamento`. `database.types.ts` is regenerated; tsc baseline held. This checkpoint confirms the SEG-03 acceptance gate — the 8-assertion behavioral smoke — is GREEN before the phase is declared secure (Pitfall 2: count the PASS notices; "no EXCEPTION" is insufficient because an all-SKIP fixture failure masquerades as success).
  </what-built>
  <how-to-verify>
    1. Run the smoke via MCP `execute_sql` with the full text of `supabase/tests/seg33_agendamento_smokes.sql`.
    2. In the NOTICE output, confirm ALL 8 of `PASS (a)` `PASS (b)` `PASS (c)` `PASS (d)` `PASS (e)` `PASS (f)` `PASS (g)` `PASS (h)` are present — and that there are ZERO `SEG-33 SKIP` lines (a SKIP means the fixture did not build → the gate is NOT proven). Specifically confirm the discriminating `PASS (c)` (spoofed-`vaga_id` INSERT denied — proves the join-through predicate was chosen, not the Pitfall-1 direct form) and `PASS (e)`/`PASS (f)` (candidate direct base-table read = 0 rows; RPC allowlist excludes `observacoes_rh`).
    3. Run `supabase db push --linked` and confirm it reports "Remote database is up to date" (ledger reconciled — Pitfall 6).
    4. If any assertion FAILs (a real leak) or SKIPs (fixture failure), do NOT approve — return the failing assertion(s) for a gap-closure fix before the phase closes.
  </how-to-verify>
  <resume-signal>Type "approved" once all 8 PASS notices are present (zero SKIP) and `db push --linked` says up to date; otherwise describe the failing/SKIPped assertion(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration apply → PROD | The privileged apply path; must not silently half-apply (table without RLS) — the single-file migration lands table + RLS + RPC atomically. |
| Smoke (JWT impersonation) → PROD RLS/RPC | The load-bearing SEG-03 proof runs above `pg_policies`, in PROD, via MCP `execute_sql`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01..07 | Info Disclosure / Elevation / Tampering | the RLS + DEFINER RPC authored in 33-02 | mitigate (proven) | This plan proves all seven access-control threats live: the 8-assertion behavioral smoke GREEN in PROD is the authoritative gate (cross-recruiter read/write deny, spoofed-vaga_id deny, observacoes_rh exclusion, cross-candidate RPC deny, admin bypass). |
| T-33-08 | Tampering | `schema_migrations` ledger drift (Pitfall 6) | mitigate | Version row reconciled to == filename `20260716000001`; `db push --linked` confirms "up to date". |
| T-33-09 | Denial of Service / false-green | fixture SKIP masquerading as PASS (Pitfall 2) | mitigate | GREEN gate = count 8 PASS notices AND zero SKIP; blocking human-verify checkpoint enforces the count. |
| T-33-SC | Tampering | package installs | accept | Zero installs this phase (M6 invariant). |

Block-on: HIGH. The phase does not close until the human-verify checkpoint confirms all 8 PASS (zero SKIP) — a real leak or a masked SKIP blocks completion.
</threat_model>

<verification>
- MCP `apply_migration` succeeded; `schema_migrations` has exactly one row for `20260716000001` (reconciled).
- Live objects confirmed (table, single policy, RPC, trigger).
- `database.types.ts` regenerated with the table + RPC; `npm run lint` <= 104.
- `seg33_agendamento_smokes.sql` via MCP `execute_sql` → 8 `PASS (a..h)`, zero SKIP.
- `supabase db push --linked` → "Remote database is up to date".
</verification>

<success_criteria>
- Phase 33 data layer is LIVE and PROVEN: AGEND-01 (RH vaga-owner writes an agendamento with `agendado_por`) and SEG-03 (bidirectional isolation, cross-recruiter + cross-candidate, `observacoes_rh` excluded) hold in PROD via the 8-assertion behavioral gate.
- Ledger reconciled; types regenerated; tsc baseline held.
- P34 (RH agendamento form) and P35 (candidate card) can build on a proven-secure primitive.
</success_criteria>

<output>
Create `.planning/phases/33-camada-de-dados-do-agendamento-de-entrevista/33-03-SUMMARY.md` when done (record the 8 PASS notices verbatim + the ledger version row + the tsc count).
</output>
