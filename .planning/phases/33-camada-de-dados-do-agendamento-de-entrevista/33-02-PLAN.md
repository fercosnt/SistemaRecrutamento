---
phase: 33-camada-de-dados-do-agendamento-de-entrevista
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - supabase/migrations/20260716000001_agendamentos_entrevista.sql
  - supabase/tests/seg33_agendamento_smokes.sql
autonomous: true
requirements: [AGEND-01, SEG-03]
must_haves:
  truths:
    - "RH vaga-owner can read + write agendamentos of their own vaga; a non-owner recruiter is DENIED both read and write (WR-04 join-through-candidaturas predicate — NOT direct vaga_id)"
    - "administrador bypasses the vaga scope (reads/writes any agendamento)"
    - "The candidate has NO base-table SELECT policy → a direct read returns 0 rows and observacoes_rh is physically unreachable"
    - "The candidate reads own agendamento ONLY via get_meu_agendamento, whose RETURNS TABLE allowlist excludes observacoes_rh/entrevistador/agendado_por/updated_by"
    - "seg33_agendamento_smokes.sql encodes the 8 JWT-impersonated assertions (a-h), RED pre-apply (all SKIP because objects absent)"
  artifacts:
    - path: "supabase/migrations/20260716000001_agendamentos_entrevista.sql"
      provides: "ENABLE RLS + single RH policy rh_gerencia_agendamento (FOR ALL, WR-04 join-through USING+WITH CHECK) + get_meu_agendamento DEFINER allowlist RPC + REVOKE/GRANT"
      contains: "CREATE POLICY rh_gerencia_agendamento"
    - path: "supabase/tests/seg33_agendamento_smokes.sql"
      provides: "8 behavioral assertions (a-h); disposable fixed-UUID fixture with 2 real 0-vaga usuarios_rh + real candidato"
      contains: "PASS (c)"
  key_links:
    - from: "rh_gerencia_agendamento USING/WITH CHECK"
      to: "public.vagas.created_by"
      via: "candidatura_id IN (candidaturas JOIN vagas WHERE created_by = (select auth.uid()))"
      pattern: "JOIN public.vagas v ON v.id = c.vaga_id"
    - from: "get_meu_agendamento ownership"
      to: "public.candidatos.user_id"
      via: "candidaturas JOIN candidatos WHERE user_id = auth.uid()"
      pattern: "ca.user_id = auth.uid\\(\\)"
---

<objective>
Add the bidirectional RLS layer + the candidate-read DEFINER RPC to the SAME migration file 33-01 created, and author the load-bearing SEG-03 behavioral smoke (RED pre-apply). This is where the phase's security lives: exactly ONE RH policy using the WR-04 **join-through-candidaturas** predicate (immune to `vaga_id` spoofing), NO candidate base-table SELECT policy, and a `get_meu_agendamento` allowlist RPC that physically cannot return `observacoes_rh`.

Purpose: Prove SEG-03 isolation (cross-recruiter + cross-candidate + `observacoes_rh` exclusion) by construction, and encode it as an 8-assertion JWT-impersonated smoke — the acceptance gate that runs above `pg_policies` (P24/P32 precedent) once 33-03 applies.

Output: the RLS + RPC section of `supabase/migrations/20260716000001_agendamentos_entrevista.sql`; and `supabase/tests/seg33_agendamento_smokes.sql` (RED — all assertions SKIP until 33-03 applies).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-camada-de-dados-do-agendamento-de-entrevista/33-CONTEXT.md
@.planning/phases/33-camada-de-dados-do-agendamento-de-entrevista/33-RESEARCH.md
@.planning/phases/33-camada-de-dados-do-agendamento-de-entrevista/33-VALIDATION.md

# The file this plan extends (33-01 output):
@supabase/migrations/20260716000001_agendamentos_entrevista.sql

# Source-of-truth analogs (copy predicates VERBATIM — the failure mode is inventing a new predicate):
@supabase/migrations/20260715000002_funil_kpis_and_rh_le_historico.sql   # rh_le_historico WR-04 join-through (:137-147)
@supabase/migrations/20260706110004_sec05_08_vaga_scope.sql              # USING + WITH CHECK vaga-scope shape (:94-124)
@supabase/migrations/20260706110003_sec02_redacao_verdict.sql            # get_minha_redacao DEFINER allowlist template (:52-88)
@supabase/tests/seg32_smokes.sql                                         # JWT-impersonated smoke structure + real 0-vaga usuarios_rh fixture (:45-113)

<interfaces>
<!-- The EXACT predicate + RPC signature to copy. Do NOT explore; use these directly. -->

WR-04 join-through-candidaturas predicate (rh_le_historico, 20260715000002:140-147) — use IDENTICALLY in USING and WITH CHECK:
```sql
    (select auth.jwt() #>> '{app_metadata,role}') = 'administrador'
    OR ((select auth.jwt() #>> '{app_metadata,role}') = 'rh'
        AND candidatura_id IN (
          SELECT c.id FROM public.candidaturas c
            JOIN public.vagas v ON v.id = c.vaga_id
           WHERE v.created_by = (select auth.uid())))
```
NOTE: the denormalized `vaga_id` column is authorization-IRRELEVANT under this predicate (scope keyed on the candidatura's REAL vaga) — a spoofed vaga_id cannot cross scope. This is Pitfall 1: the direct `vaga_id IN (SELECT id FROM vagas WHERE created_by=…)` form is FORBIDDEN.

Candidate DEFINER allowlist RPC (clone get_minha_redacao, 20260706110003:52-88). Signature/allowlist for get_meu_agendamento:
```sql
CREATE OR REPLACE FUNCTION public.get_meu_agendamento(p_candidatura_id uuid)
RETURNS TABLE (id uuid, candidatura_id uuid, tipo public.tipo_entrevista_avaliacao,
               data_hora timestamptz, local_ou_link text,
               status public.status_entrevista, compareceu boolean)
LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''
-- body: SELECT the 7 allowlist cols FROM public.agendamentos_entrevista a
--       JOIN public.candidaturas c ON c.id = a.candidatura_id
--       JOIN public.candidatos ca ON ca.id = c.candidato_id
--       WHERE a.candidatura_id = p_candidatura_id
--         AND ca.user_id = auth.uid()      -- own-row guard inside DEFINER
--         AND a.deleted_at IS NULL          -- hide hard-removed; cancelada STAYS visible
--       ORDER BY a.data_hora DESC;
```
EXCLUDED from the signature (unreachable by construction): observacoes_rh, entrevistador, agendado_por, updated_by, vaga_id, created_at, updated_at, deleted_at.
Then: REVOKE ALL ON FUNCTION public.get_meu_agendamento(uuid) FROM PUBLIC; GRANT EXECUTE ... TO authenticated;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RH RLS — ENABLE + single WR-04 join-through policy (USING + WITH CHECK)</name>
  <files>supabase/migrations/20260716000001_agendamentos_entrevista.sql</files>
  <read_first>
    - `supabase/migrations/20260715000002_funil_kpis_and_rh_le_historico.sql:132-147` — the `rh_le_historico` join-through predicate to copy VERBATIM (keep `(select auth.jwt() …)` / `(select auth.uid())` subquery-wrapping — planner-cache idiom)
    - `supabase/migrations/20260706110004_sec05_08_vaga_scope.sql:94-124` — the USING + WITH CHECK bidirectional shape (redacao_rh_select/update)
    - `supabase/migrations/20260624000001_entrevista_cognitivo_tables.sql:189-206` — the `FOR ALL` single-policy idiom (cand_escreve_cognitivo_respostas)
    - `<interfaces>` — the exact predicate + the Pitfall-1 FORBIDDEN direct-vaga_id form
    - 33-01 output file (append below the trigger)
  </read_first>
  <action>
    In `supabase/migrations/20260716000001_agendamentos_entrevista.sql`, append `ALTER TABLE public.agendamentos_entrevista ENABLE ROW LEVEL SECURITY;` then `DROP POLICY IF EXISTS rh_gerencia_agendamento ON public.agendamentos_entrevista;` and a SINGLE `CREATE POLICY rh_gerencia_agendamento ... FOR ALL TO authenticated` whose USING and WITH CHECK are BOTH the WR-04 join-through predicate from `<interfaces>` (admin bypass OR `rh` AND `candidatura_id IN (candidaturas JOIN vagas WHERE created_by = (select auth.uid()))`). Use the direct-`vaga_id IN (…)` form NOWHERE — it is the Pitfall-1 spoofable predicate. Create EXACTLY this one RH policy and NO candidate SELECT/INSERT/UPDATE/DELETE policy (candidate column isolation is by construction — the RPC in Task 2 is the only candidate path; a second permissive policy would OR-defeat the scope, P24 SEC-08 / P32's caught 2nd live policy). Add a short inline note that the denormalized `vaga_id` is authorization-irrelevant here.
  </action>
  <verify>
    <automated>grep -Eq "ALTER TABLE public\.agendamentos_entrevista ENABLE ROW LEVEL SECURITY" "supabase/migrations/20260716000001_agendamentos_entrevista.sql" && grep -Eq "CREATE POLICY rh_gerencia_agendamento ON public\.agendamentos_entrevista" "supabase/migrations/20260716000001_agendamentos_entrevista.sql" && grep -Eq "JOIN public\.vagas v ON v\.id = c\.vaga_id" "supabase/migrations/20260716000001_agendamentos_entrevista.sql" && grep -Eq "WITH CHECK" "supabase/migrations/20260716000001_agendamentos_entrevista.sql" && test "$(grep -cE 'CREATE POLICY' "supabase/migrations/20260716000001_agendamentos_entrevista.sql")" = "1" && ! grep -Eq "vaga_id IN \(SELECT id FROM public\.vagas" "supabase/migrations/20260716000001_agendamentos_entrevista.sql" && echo RLS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `ENABLE ROW LEVEL SECURITY` on `agendamentos_entrevista` present.
    - EXACTLY ONE `CREATE POLICY` in the file: `rh_gerencia_agendamento`, `FOR ALL TO authenticated`, preceded by `DROP POLICY IF EXISTS`.
    - Both USING and WITH CHECK contain the join-through predicate (`candidaturas JOIN vagas WHERE created_by = (select auth.uid())`); admin-bypass branch present.
    - Zero occurrences of the Pitfall-1 direct form `vaga_id IN (SELECT id FROM public.vagas …)`.
    - NO candidate SELECT/write policy anywhere.
    - `(select auth.jwt() …)` / `(select auth.uid())` subquery-wrapping preserved verbatim.
  </acceptance_criteria>
  <done>grep gate `RLS_OK` prints (single policy, join-through, no spoofable form).</done>
</task>

<task type="auto">
  <name>Task 2: Candidate read — get_meu_agendamento DEFINER allowlist RPC</name>
  <files>supabase/migrations/20260716000001_agendamentos_entrevista.sql</files>
  <read_first>
    - `supabase/migrations/20260706110003_sec02_redacao_verdict.sql:46-91` — clone the whole `get_minha_redacao` shape: no candidate base-table SELECT policy + DEFINER RPC + ownership join `→candidaturas→candidatos WHERE user_id=auth.uid()` + `SET search_path=''` + REVOKE PUBLIC / GRANT authenticated + COMMENT
    - `<interfaces>` — the exact `get_meu_agendamento` signature + allowlist + the EXCLUDED columns
    - `.planning/phases/33-.../33-RESEARCH.md` Pattern 2 + Pitfall 3 (schema-qualify EVERYTHING under search_path='')
  </read_first>
  <action>
    Append `CREATE OR REPLACE FUNCTION public.get_meu_agendamento(p_candidatura_id uuid)` returning the 7-column allowlist TABLE from `<interfaces>` (id, candidatura_id, tipo, data_hora, local_ou_link, status, compareceu — and NOTHING else, so observacoes_rh/entrevistador/agendado_por/updated_by are physically absent from the return type). `LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''`. Body: `RETURN QUERY SELECT` those 7 columns `FROM public.agendamentos_entrevista a JOIN public.candidaturas c ON c.id = a.candidatura_id JOIN public.candidatos ca ON ca.id = c.candidato_id WHERE a.candidatura_id = p_candidatura_id AND ca.user_id = auth.uid() AND a.deleted_at IS NULL ORDER BY a.data_hora DESC`. Schema-qualify every object (Pitfall 3); keep `auth.uid()` (GUC-based, survives DEFINER). Then `REVOKE ALL ON FUNCTION public.get_meu_agendamento(uuid) FROM PUBLIC; GRANT EXECUTE ON FUNCTION public.get_meu_agendamento(uuid) TO authenticated;`. Add a `COMMENT ON FUNCTION` documenting: SEG-03 own-row candidate reader; ownership enforced INSIDE via `candidatos.user_id = auth.uid()`; allowlist excludes observacoes_rh/entrevistador/agendado_por/updated_by; `deleted_at IS NULL` hides only hard-removed rows while `status='cancelada'` stays visible (candidate must see a cancellation — CONTEXT Area 3). Do NOT add a candidate base-table SELECT policy (that would defeat the column isolation).
  </action>
  <verify>
    <automated>grep -Eq "CREATE OR REPLACE FUNCTION public\.get_meu_agendamento\(p_candidatura_id uuid\)" "supabase/migrations/20260716000001_agendamentos_entrevista.sql" && grep -Eq "SECURITY DEFINER" "supabase/migrations/20260716000001_agendamentos_entrevista.sql" && grep -Eq "ca\.user_id = auth\.uid\(\)" "supabase/migrations/20260716000001_agendamentos_entrevista.sql" && grep -Eq "REVOKE ALL ON FUNCTION public\.get_meu_agendamento\(uuid\) FROM PUBLIC" "supabase/migrations/20260716000001_agendamentos_entrevista.sql" && grep -Eq "GRANT EXECUTE ON FUNCTION public\.get_meu_agendamento\(uuid\) TO authenticated" "supabase/migrations/20260716000001_agendamentos_entrevista.sql" && ! grep -nE "RETURNS TABLE" "supabase/migrations/20260716000001_agendamentos_entrevista.sql" | grep -Eqi "observacoes_rh|entrevistador|agendado_por|updated_by" && echo RPC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `get_meu_agendamento(uuid)` present, `SECURITY DEFINER`, `SET search_path = ''`.
    - RETURNS TABLE lists EXACTLY the 7 allowlist columns; `observacoes_rh`, `entrevistador`, `agendado_por`, `updated_by`, `vaga_id` are ABSENT from the signature.
    - Body joins `agendamentos_entrevista → candidaturas → candidatos` and guards `ca.user_id = auth.uid()`; filters `deleted_at IS NULL`; all objects schema-qualified.
    - `REVOKE ALL … FROM PUBLIC` + `GRANT EXECUTE … TO authenticated` present; `COMMENT ON FUNCTION` documents the boundary.
    - Still exactly one `CREATE POLICY` in the file (no candidate SELECT policy added).
  </acceptance_criteria>
  <done>grep gate `RPC_OK` prints; observacoes_rh unreachable by construction (row-deny + allowlist).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author seg33_agendamento_smokes.sql — 8 JWT-impersonated assertions (RED)</name>
  <files>supabase/tests/seg33_agendamento_smokes.sql</files>
  <behavior>
    - (a) cross-recruiter READ deny: recruiter A (rh, owns only empty vagaA) counts agendamentos on vagaB's candidatura → 0.
    - (b) owner READ+WRITE allow (AGEND-01): recruiter B (owns vagaB) INSERTs an agendamento for its candidatura with `agendado_por` set, reads it back → 1.
    - (c) DISCRIMINATING spoofed-vaga_id INSERT: recruiter A INSERTs candidatura_id=vagaB's + vaga_id=own vagaA → DENIED (42501 / 0 rows). Fails iff the direct-vaga_id predicate was chosen (Pitfall 1 discriminator).
    - (d) admin bypass: administrador reads any agendamento → rows returned.
    - (e) candidate DIRECT base-table deny: owning candidate SELECTs from the base table → 0 rows (no candidate SELECT policy; observacoes_rh unreachable).
    - (f) candidate RPC allow + allowlist: owning candidate calls get_meu_agendamento(own) → 1 row; assert the result carries NO observacoes_rh/entrevistador/agendado_por/updated_by.
    - (g) cross-candidate RPC deny: a DIFFERENT candidate calls get_meu_agendamento(other's candidatura) → 0 rows.
    - (h) candidate write deny: owning candidate INSERT/UPDATE/DELETE on base table → denied (42501 / 0 rows).
    - GREEN gate = 8 `PASS (…)` notices present (Pitfall 2 — "no EXCEPTION" is insufficient; all-SKIP masks a fixture failure).
  </behavior>
  <read_first>
    - `supabase/tests/seg32_smokes.sql` (whole file) — clone the structure: `RESET ROLE` → fixture `DO` block with fixed disposable UUIDs discovering 2 distinct REAL 0-vaga `usuarios_rh` (recruiter A/B) + a 3rd usuarios_rh (admin) + a REAL FK-bound candidato via `candidatos.user_id`; `EXCEPTION WHEN OTHERS` sets `smoke.ready='n'`; per-assertion `SET ROLE authenticated` + `set_config('request.jwt.claims', jsonb_build_object('sub',…, 'role','authenticated','app_metadata', jsonb_build_object('role', 'rh'|'administrador'|'candidato'))::text, false)`; `RAISE NOTICE 'PASS (x) …'` / `RAISE EXCEPTION` on leak; ROLLBACK-free cleanup deleting only the disposable fixed-UUID rows
    - `supabase/tests/seg32_smokes.sql:26-29,67-77` — the ⚠ `vagas.created_by` HAS FK correction → recruiters MUST be real 0-vaga users, NOT synthetic UUIDs (Pitfall 4)
    - `.planning/phases/33-.../33-VALIDATION.md` (the 8-assertion a-h map + Wave 0 fixture spec) + `33-RESEARCH.md` "Validation Architecture" (the discriminating assertion (c))
  </read_first>
  <action>
    Create `supabase/tests/seg33_agendamento_smokes.sql` cloning `seg32_smokes.sql`'s harness. Header: it is the SEG-03 load-bearing gate; run AFTER 33-03 applies. Use a fresh disposable fixed-UUID namespace (e.g. `33010033-*`) distinct from seg32's `32010032-*`. Fixture `DO` block: pre-delete the disposable rows; discover a REAL candidato (`candidatos.user_id IS NOT NULL`) as the owning candidate + a SECOND real candidato for assertion (g); discover TWO distinct REAL `usuarios_rh` owning 0 vagas as recruiter A / recruiter B (the seg32 `NOT EXISTS (SELECT 1 FROM public.vagas WHERE created_by=u.user_id)` pattern) + a 3rd `usuarios_rh` as admin; if any is NULL set `smoke.ready='n'` and SKIP. Build: disposable vagaA(created_by=A, empty) + vagaB(created_by=B) + one candidatura on vagaB tied to the owning candidato + (for assertion g) one candidatura on vagaB tied to the second candidato. Encode assertions (a)-(h) exactly as in `<behavior>`, each emitting `PASS (<letter>) …` on success and `RAISE EXCEPTION` on a real leak; wrap RLS-deny reads so `insufficient_privilege` (42501) counts as PASS (seg32 (c) idiom). For (f), after calling `public.get_meu_agendamento(<own candidatura>)`, assert exactly 1 row AND that the returned columns are the 7 allowlist fields (the RETURNS TABLE signature guarantees exclusion — assert row present + safe columns populated). ROLLBACK-free cleanup deleting only the `33010033-*` rows. Do NOT delete any discovered real user/candidato row. This file is RED pre-apply (objects absent → fixture raises `relation does not exist` → all assertions SKIP) — that is the expected RED state.
  </action>
  <verify>
    <automated>test -f "supabase/tests/seg33_agendamento_smokes.sql" && for L in a b c d e f g h; do grep -Eq "PASS \($L" "supabase/tests/seg33_agendamento_smokes.sql" || { echo "MISSING assertion $L"; exit 1; }; done && grep -Eq "get_meu_agendamento" "supabase/tests/seg33_agendamento_smokes.sql" && grep -Eq "NOT EXISTS \(SELECT 1 FROM public\.vagas" "supabase/tests/seg33_agendamento_smokes.sql" && grep -Eq "request\.jwt\.claims" "supabase/tests/seg33_agendamento_smokes.sql" && echo SMOKE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/tests/seg33_agendamento_smokes.sql` exists with all 8 `PASS (a)`…`PASS (h)` notices.
    - Assertion (c) is the discriminating spoofed-`vaga_id` INSERT-deny; assertion (e) proves candidate direct base-table read = 0; assertion (f) proves the RPC allowlist (no observacoes_rh); assertion (g) proves cross-candidate RPC deny.
    - Fixture discovers 2 distinct REAL 0-vaga `usuarios_rh` (NOT synthetic UUIDs — Pitfall 4) + real candidato(s); `smoke.ready='n'` SKIP path present (Pitfall 2).
    - JWT impersonation via `set_config('request.jwt.claims', …, false)` + `SET ROLE authenticated`; disposable UUID namespace distinct from seg32.
    - ROLLBACK-free cleanup touches only the disposable fixed-UUID rows.
    - RED pre-apply is EXPECTED (all SKIP because table/RPC absent) — 33-03 turns it GREEN.
  </acceptance_criteria>
  <done>grep gate `SMOKE_OK` prints (8 assertions authored); the file is RED until 33-03 applies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH JWT → PostgREST → `agendamentos_entrevista` | Recruiter reads/writes across vagas; horizontal (IDOR) isolation both ways is the phase core. |
| Candidate JWT → PostgREST → base table / RPC | Candidate must reach ONLY their own row and NEVER a column beyond the allowlist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | recruiter A reads recruiter B's agendamento | mitigate | WR-04 join-through USING on `rh_gerencia_agendamento`; smoke (a). |
| T-33-02 | Elevation / Tampering | spoofed denormalized `vaga_id` at INSERT crosses scope | mitigate | Join-through-candidaturas predicate (auth keyed on the candidatura's REAL vaga) + 33-01 consistency trigger; **smoke (c)** is the discriminator. Direct-`vaga_id` predicate FORBIDDEN. |
| T-33-03 | Information Disclosure | `observacoes_rh` (+ every column) leaks to candidate | mitigate | Base-table row-deny (NO candidate SELECT policy) + `get_meu_agendamento` allowlist projection (RETURNS TABLE excludes it); never `select('*')`, never column REVOKE; smoke (e)/(f). |
| T-33-04 | Information Disclosure | OR-defeat: a second permissive/role-only policy re-opens the row | mitigate | EXACTLY one RH policy, zero candidate SELECT policy; grep gate asserts `CREATE POLICY` count == 1; behavioral smoke tests NET effect (P24 SEC-08; P32 caught a 2nd live policy). |
| T-33-05 | Information Disclosure | column-REVOKE no-op mistaken for a column hide | mitigate | Row-deny mechanism (not column REVOKE); smoke is authoritative (P24 SEC-07). |
| T-33-06 | Elevation | cross-candidate read via the RPC (ownership join wrong) | mitigate | `ca.user_id = auth.uid()` INSIDE the DEFINER, schema-qualified under `search_path=''`; smoke (g). |
| T-33-07 | Elevation | search_path hijack of the DEFINER RPC | mitigate | `SET search_path = ''` + fully schema-qualified objects (Pitfall 3). |
| T-33-SC | Tampering | package installs | accept | Zero npm/PyPI/crates installs this phase (M6 invariant) — no supply-chain surface. |

Block-on: HIGH. Every HIGH access-control threat maps to a mitigation AND a smoke assertion; the smoke (33-03) is the load-bearing proof (structural greps here are pre-checks, not the gate).
</threat_model>

<verification>
- grep gates `RLS_OK`, `RPC_OK`, `SMOKE_OK` all print.
- Migration file has EXACTLY one `CREATE POLICY` (RH), one DEFINER RPC, zero candidate policies, zero direct-`vaga_id` predicate.
- `seg33_agendamento_smokes.sql` has all 8 `PASS (a..h)` assertions and the real-user fixture; it is RED (SKIP) pre-apply — the expected state until 33-03.
- No `database.types.ts` change and no PROD apply in this plan.
</verification>

<success_criteria>
- The migration file now contains the full data layer: table + trigger (33-01) + ENABLE RLS + single WR-04 join-through RH policy + `get_meu_agendamento` DEFINER allowlist RPC.
- The 8-assertion SEG-03 smoke exists and is authored RED-first.
- AGEND-01 (owner write, vaga-scoped) and SEG-03 (bidirectional isolation + `observacoes_rh` exclusion) are encoded and ready for the 33-03 GREEN gate.
</success_criteria>

<output>
Create `.planning/phases/33-camada-de-dados-do-agendamento-de-entrevista/33-02-SUMMARY.md` when done.
</output>
