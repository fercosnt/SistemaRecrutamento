---
phase: 11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - supabase/migrations/20260611000001_scores_candidato.sql
  - supabase/migrations/20260611000002_perguntas_sjt.sql
  - supabase/migrations/20260611000003_respostas_avaliacao.sql
  - supabase/migrations/20260611000004_pontuar_sjt_rpc.sql
  - supabase/functions/_shared/avaliacao-schemas.ts
  - supabase/functions/avaliar-redacao/index.ts
autonomous: true

requirements: [AVAL-02, AVAL-03, AVAL-09]

must_haves:
  truths:
    - "scores_candidato is a GENERIC score sink: tipo_score enum forward-declares sjt/big_five/redacao/entrevista/cognitivo/decisao (P12-15 need no ALTER TYPE), status_score = sucesso/pendente_humano/falhou; candidato has NO read policy (denied), RH/admin SELECT allowlist (D-RNF-07a / [[reference_select_star_leaks_pii]])"
    - "respostas_avaliacao enforces the back-lock: candidato write policy joins candidaturas.etapa_atual='avaliacao_assincrona' in BOTH USING and WITH CHECK (AVAL-09)"
    - "pontuar_sjt is SECURITY DEFINER, authorizes auth.uid() owns candidatura AND etapa='avaliacao_assincrona' (42501 otherwise — C1), computes Σ peso from pergunta_opcao_metadata server-side, applies <mc_min_pct OR ≥1 atencao → pendente_humano, NEVER writes candidaturas (RNF-07a), returns a NEUTRAL payload (no score to candidate)"
    - "avaliar-redacao EF authenticates via supabaseUser.auth.getUser() then AUTHORIZES ownership + etapa (403 — C1 lesson), loads the work_sample_sjt prompt (NOT sjt_evaluation), maps 1-5 dims to weighted composite 0-25, routes <13/25 OR red_flag to pendente_humano, persists scores_candidato tipo='sjt' subtipo='caso_aberto', never touches candidaturas.etapa_atual (AVAL-03)"
  artifacts:
    - path: "supabase/migrations/20260611000001_scores_candidato.sql"
      provides: "generic score sink + tipo_score/status_score enums + candidato-DENY RLS"
      contains: "tipo_score"
    - path: "supabase/migrations/20260611000003_respostas_avaliacao.sql"
      provides: "autosave/progress table + etapa-gated back-lock RLS"
      contains: "avaliacao_assincrona"
    - path: "supabase/migrations/20260611000004_pontuar_sjt_rpc.sql"
      provides: "deterministic MC scoring SECURITY DEFINER RPC"
      contains: "SECURITY DEFINER"
    - path: "supabase/functions/avaliar-redacao/index.ts"
      provides: "candidate-invoked AI open-case scoring EF (two-client C1)"
      contains: "work_sample_sjt"
    - path: "supabase/functions/_shared/avaliacao-schemas.ts"
      provides: "WorkSampleScoringSchema verbatim copy + EF body schema (no score field)"
      contains: "WorkSampleScoringSchema"
  key_links:
    - from: "supabase/functions/avaliar-redacao/index.ts"
      to: "_shared/ai-client.ts callAi + loadPrompt"
      via: "loadPrompt('work_sample_sjt', supabaseAdmin) + callAi"
      pattern: "work_sample_sjt"
    - from: "supabase/migrations/20260611000004_pontuar_sjt_rpc.sql"
      to: "pergunta_opcao_metadata"
      via: "JOIN by opcao_id for Σ peso"
      pattern: "pergunta_opcao_metadata"
---

<objective>
Author the four Phase-11 migrations (no-wrapper, D-22), the shared Zod schema, and the `avaliar-redacao` Edge Function — turning the RED tests from Plan 11-01 GREEN at the unit/deno level. **PROD apply, prompt activation, EF deploy and db:types regen are explicitly deferred to the [BLOCKING] Plan 11-04** — this plan only authors files and runs deno tests against the mocked handler.

Purpose: Build the deterministic + AI scoring backbone that never auto-rejects (RNF-07a), with the C1 authenticate-then-authorize fix baked in from line one.
Output: 4 migrations + 1 shared schema + 1 EF (with its deno test from 11-01 now GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-RESEARCH.md
@.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-PATTERNS.md

<interfaces>
<!-- Exact contracts the executor needs — extracted from live analogs. No exploration needed. -->

From supabase/migrations/20260610000001_analise_tables.sql:74-92 (candidato-DENY RLS idiom to copy):
- `CREATE POLICY rh_le_<t> ON public.<t> FOR SELECT USING ((select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador'));`
- NO candidato/anon policy → denied; NO INSERT/UPDATE policy → only service_role/DEFINER writes.

From supabase/migrations/20260607000006_rls_policies_m2_backbone.sql:60-70 (ownership-join idiom):
- `candidatura_id IN (SELECT c.id FROM public.candidaturas c JOIN public.candidatos ca ON ca.id=c.candidato_id WHERE ca.user_id=(select auth.uid()))` — the back-lock adds `AND c.etapa_atual='avaliacao_assincrona'`.

From supabase/migrations/20260610000003_reprocessar_rpc.sql:86-87 (DEFINER tail):
- `REVOKE ALL ON FUNCTION public.<fn>(...) FROM PUBLIC; GRANT EXECUTE ON FUNCTION public.<fn>(...) TO authenticated;`

From supabase/migrations/20260607010001_pergunta_opcao_metadata.sql (weight source):
- `pergunta_opcao_metadata (pergunta_id uuid, opcao_id uuid, opcao_texto text, tag enum_tag_opcao, peso int, UNIQUE(pergunta_id, opcao_id))`. Scale: fortemente_pontua=4, pontua=2, neutro=1, atencao=0. NOTE: its FK targets `perguntas_formulario` — for SJT, this phase writes rows whose `pergunta_id` points at the NEW `perguntas` table; author the SJT metadata rows in the same seed migration (see Task 2 note on FK).

From supabase/functions/comparativo-candidatos/index.ts:64-119 (EF skeleton to clone):
- `function errorResponse(code, message, status=400): Response`
- `export interface ComparativoDeps { anthropic; openai; supabaseAdmin; supabaseUser; }`
- `export async function handler(req: Request, deps): Promise<Response>` — testable; real clients built in `Deno.serve`.
- authn: `supabaseUser.auth.getUser()` → 401; authz: role check → 403 (candidate clone = ownership + etapa → 403).

From supabase/functions/_shared/ai-client.ts + prompt-loader.ts:
- `loadPrompt('work_sample_sjt', supabaseAdmin)` (the seeded call_type — NOT 'sjt_evaluation', which is an orphan key); `resolvedPromptFromLoaded(loaded, 'work_sample_sjt', 'gpt-4o-mini')`; `callAi({ prompt, rawInput, schema, candidato_id, vaga_id }, { anthropic, openai, supabase })` owns injection/maskPII/retry/fallback/cost/log.

From docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts:284-316 (copy verbatim into _shared):
- `WorkSampleScoringSchema` = { scenario_understanding, dimension_scores[1..8] (each: dimension, inclusion/exclusion arrays, cited_evidence[0..3], reasoning, score Score1to5, level), overall_score int 0-100, recommendation, confidence, red_flags[0..5], bias_audit }. `Score1to5` = union(int 1-5, literal 'insufficient_evidence').

From docs/conhecimento/sjt/banco-sjt-dentista.md (the seed source for the 0-25 weighted mapping):
- Open-case rubric weights: Raciocínio clínico-estético 25%, Planejamento 20%, Comunicação 25%, Ética 15%, Consentimento 15%. Band: ≥18 avança · 13-17 entrevista · <13 revisão.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scores_candidato + respostas_avaliacao migrations (table+enums+RLS, no-wrapper)</name>
  <files>supabase/migrations/20260611000001_scores_candidato.sql, supabase/migrations/20260611000003_respostas_avaliacao.sql</files>
  <read_first>
    - supabase/migrations/20260610000001_analise_tables.sql (candidato-DENY RLS + idempotent UNIQUE + no-wrapper header)
    - supabase/migrations/20260607000006_rls_policies_m2_backbone.sql:39-70 (ownership-join idiom)
    - .planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-RESEARCH.md §Pattern 1 (lines 232-281) + §Pattern 2 (lines 288-329) + Pitfall 7
  </read_first>
  <action>
    Author `20260611000001_scores_candidato.sql` (NO outer BEGIN/COMMIT — D-22). Create `public.tipo_score AS ENUM ('sjt','big_five','redacao','entrevista','cognitivo','decisao')` (forward-declared for P12-15 per A1) and `public.status_score AS ENUM ('sucesso','pendente_humano','falhou')`. Create `public.scores_candidato` with: id uuid PK, candidatura_id uuid NOT NULL REFERENCES candidaturas ON DELETE CASCADE, tipo tipo_score NOT NULL, subtipo text, pergunta_id uuid, score numeric, score_max numeric, status status_score NOT NULL DEFAULT 'sucesso', metadata jsonb NOT NULL DEFAULT '{}'::jsonb, citacoes jsonb, red_flags jsonb, created_at/updated_at timestamptz. Idempotency key: use `UNIQUE NULLS NOT DISTINCT (candidatura_id, tipo, subtipo, pergunta_id)` (verify Supabase is PG15 — it is, per A2; if version probe shows <15, fall back to a unique expression index over COALESCE(subtipo,''), COALESCE(pergunta_id,'00000000-0000-0000-0000-000000000000'::uuid)). RLS: ENABLE; `rh_le_scores` SELECT for role IN ('rh','administrador'); NO candidato policy (denied); NO INSERT/UPDATE policy (only DEFINER RPC/service_role EF writes). Add a COMMENT documenting the generic-sink purpose.
    Author `20260611000003_respostas_avaliacao.sql` (no-wrapper). Create `public.respostas_avaliacao` with: id uuid PK, candidatura_id uuid NOT NULL REFERENCES candidaturas ON DELETE CASCADE, teste text NOT NULL, respostas jsonb NOT NULL DEFAULT '{}'::jsonb, updated_at timestamptz NOT NULL DEFAULT now(), UNIQUE (candidatura_id, teste). RLS: ENABLE; `cand_le_respostas_aval` SELECT via the ownership-join idiom; `cand_escreve_respostas_aval` FOR ALL with the ownership join PLUS `AND c.etapa_atual = 'avaliacao_assincrona'` in BOTH USING and WITH CHECK (the back-lock — AVAL-09). Index on candidatura_id.
    These two are table-only DDL (no $$ bodies) — they push clean (Pitfall 8).
  </action>
  <verify>
    <automated>grep -q "tipo_score" supabase/migrations/20260611000001_scores_candidato.sql && grep -q "avaliacao_assincrona" supabase/migrations/20260611000003_respostas_avaliacao.sql && ! grep -qiE "^\s*BEGIN;|^\s*COMMIT;" supabase/migrations/20260611000001_scores_candidato.sql</automated>
  </verify>
  <acceptance_criteria>
    - `tipo_score` enum lists exactly sjt/big_five/redacao/entrevista/cognitivo/decisao; `status_score` lists sucesso/pendente_humano/falhou.
    - scores_candidato has NO candidato SELECT/INSERT/UPDATE policy; has `rh_le_scores` role-gated SELECT.
    - respostas_avaliacao write policy contains `etapa_atual = 'avaliacao_assincrona'` in both USING and WITH CHECK.
    - Neither file has an outer `BEGIN;`/`COMMIT;` wrapper (D-22).
  </acceptance_criteria>
  <done>Generic score sink + etapa-gated autosave table authored, RLS-hardened, no-wrapper.</done>
</task>

<task type="auto">
  <name>Task 2: perguntas SJT bank migration (table + RLS + seed) + pontuar_sjt RPC</name>
  <files>supabase/migrations/20260611000002_perguntas_sjt.sql, supabase/migrations/20260611000004_pontuar_sjt_rpc.sql</files>
  <read_first>
    - supabase/migrations/20260607010001_pergunta_opcao_metadata.sql (option taxonomy + RH-manage RLS + seed shape)
    - supabase/migrations/20260607000005_avancar_etapa_trigger.sql:18-27 (auth.uid() GUC survives DEFINER)
    - docs/conhecimento/sjt/banco-sjt-dentista.md (the seed source: 3 MC items with tag/peso + 1 open case)
    - .planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-RESEARCH.md §Pattern 3 (lines 336-411) + lines 589-599 (seed) + Pitfall 4/5
  </read_first>
  <action>
    Author `20260611000002_perguntas_sjt.sql` (no-wrapper). Create `public.perguntas` (NEW table, distinct from perguntas_formulario): id uuid PK, tipo text NOT NULL DEFAULT 'sjt', cargo text NOT NULL, dimensao_primaria text, cenario text NOT NULL, formato text NOT NULL CHECK (formato IN ('mc','caso_aberto')), tempo_est_min int, rubric jsonb (open-case dimension weights live here per post-research), content_hash text, status text NOT NULL DEFAULT 'active', created_at timestamptz. RLS: ENABLE; `rh_gerencia_perguntas` FOR ALL role IN ('rh','administrador'); `cand_le_perguntas_ativas` SELECT USING (status='active') so the candidate can read the battery. Add a `pergunta_opcao_metadata` companion: this phase's SJT metadata rows reference the NEW `perguntas.id` — since the live `pergunta_opcao_metadata` FK targets `perguntas_formulario`, author a parallel insert path: EITHER (a) add a nullable `sjt_pergunta_id uuid REFERENCES public.perguntas(id)` column to `pergunta_opcao_metadata` and seed SJT option weights against it, OR (b) create a dedicated `perguntas_opcao_sjt` weights table mirroring (pergunta_id→perguntas, opcao_id, opcao_texto, tag enum_tag_opcao, peso int, UNIQUE(pergunta_id,opcao_id)). Choose (b) — a dedicated SJT weights table avoids mutating the Phase-7 FK and keeps `pontuar_sjt`'s JOIN unambiguous; document the choice inline. SEED dentista (3 MC items + their option weights from the markdown table: fortemente_pontua=4/pontua=2/neutro=1/atencao=0, with stable gen_random_uuid() opcao_ids) + the open-case pergunta row with `rubric` jsonb = the 5 weighted dimensions (25/20/25/15/15). content_hash via `encode(extensions.digest('sjt:dentista:Dn:v1.0','sha256'),'hex')`. Seed the other 7 cargos at minimum 1 MC item each (recepcao, consultor-vendas, sdr-social-seller, assistente-financeiro, asb-tsb shared, vaga-generica) from their banco-sjt-*.md files — V1 is 1-2/cargo per D-05. ALL seeded scenario/option text must pass the LGPD-04 grep guard (no forbidden term).
    Author `20260611000004_pontuar_sjt_rpc.sql` (PL/pgSQL $$ — at 42601 risk; D-22). Implement `public.pontuar_sjt(p_candidatura_id uuid, p_respostas jsonb) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''` exactly per RESEARCH §Pattern 3: authorize (EXISTS owns candidatura AND etapa='avaliacao_assincrona' via auth.uid() → RAISE 42501 if not), Σ peso from the SJT weights table joined by opcao_id, v_max = Σ max(peso) per pergunta, v_has_atencao = bool_or(tag='atencao'), threshold = read `mc_min_pct` from the candidatura's vaga.testes_aplicaveis (default 0.60) → `(v_has_atencao OR v_score/v_max < mc_min_pct)` ⇒ status 'pendente_humano' else 'sucesso', UPSERT scores_candidato (tipo='sjt', subtipo='mc', pergunta_id=NULL) ON CONFLICT DO UPDATE, return NEUTRAL `jsonb_build_object('ok',true,'registrado',true)`. NEVER write candidaturas. Tail: REVOKE ALL FROM PUBLIC; GRANT EXECUTE TO authenticated. NO outer BEGIN/COMMIT.
  </action>
  <verify>
    <automated>grep -q "SECURITY DEFINER" supabase/migrations/20260611000004_pontuar_sjt_rpc.sql && grep -q "42501" supabase/migrations/20260611000004_pontuar_sjt_rpc.sql && ! grep -qi "UPDATE public.candidaturas" supabase/migrations/20260611000004_pontuar_sjt_rpc.sql && npm run test:run -- forbidden-strings 2>&1 | grep -qi "pass"</automated>
  </verify>
  <acceptance_criteria>
    - `perguntas` table exists with formato CHECK ('mc','caso_aberto') + RH-manage + candidato active-read RLS; a dedicated SJT weights table (FK→perguntas) carries tag/peso; dentista is seeded 3 MC + 1 open-case with the rubric weights, plus ≥1 MC for each of the other 7 cargos.
    - `pontuar_sjt` is SECURITY DEFINER, RAISEs 42501 on non-owner/wrong-etapa, computes Σ peso server-side, applies the per-vaga `mc_min_pct` threshold (default 0.60), writes `status='pendente_humano'` on fail, and contains NO `UPDATE public.candidaturas` (RNF-07a).
    - The LGPD-04 grep guard PASSES over the seeded scenario/option text (SCAN_ROOTS now includes migrations).
    - Both files are no-wrapper (D-22).
  </acceptance_criteria>
  <done>SJT item bank seeded (8 cargos), deterministic scoring RPC authored with C1 authz + RNF-07a invariant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: avaliar-redacao EF + _shared/avaliacao-schemas.ts (turn the 11-01 deno test GREEN)</name>
  <files>supabase/functions/_shared/avaliacao-schemas.ts, supabase/functions/avaliar-redacao/index.ts</files>
  <behavior>
    - 401 when supabaseUser.auth.getUser() returns no user.
    - 403 when the authenticated user does NOT own the candidatura.
    - 403 when the user owns it but etapa_atual !== 'avaliacao_assincrona'.
    - Given mocked callAi returning fixed dimension_scores (1-5), composite maps to 0-25 (weighted by the pergunta rubric) and <13 OR red_flags.length>0 → status 'pendente_humano'; any insufficient_evidence dim → pendente_humano (no fabricated score).
    - Handler NEVER calls supabaseAdmin.from('candidaturas').update (RNF-07a).
    - loadPrompt is called with 'work_sample_sjt' (not 'sjt_evaluation').
  </behavior>
  <read_first>
    - supabase/functions/comparativo-candidatos/index.ts (full: two-client, errorResponse, handler, Deno.serve tail)
    - supabase/functions/analise-candidato-individual/index.ts:219,247-278,355-386 (callAi/loadPrompt composition + npm: runtime import + allowlist read)
    - supabase/functions/_shared/analise-schemas.ts:9-13 (verbatim-copy rule for docs/ schemas)
    - docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts:284-316 (WorkSampleScoringSchema to copy)
    - supabase/functions/avaliar-redacao/__tests__/index.test.ts (the RED test from 11-01 that must go GREEN)
  </read_first>
  <action>
    Create `_shared/avaliacao-schemas.ts`: copy `WorkSampleScoringSchema` (+ its `Citation`/`Score1to5`/`BarsLevel`/`RecommendationEnum`/`ConfidenceEnum` deps) VERBATIM from the docs template (docs/ is not in the EF bundle — Pitfall/anti-pattern). Add the EF body schema `AvaliarRedacaoBodySchema = z.object({ candidatura_id: z.string().uuid(), pergunta_id: z.string().uuid(), texto: z.string().min(1) })` — NO score field (Pitfall 5: client posts only the answer text).
    Create `avaliar-redacao/index.ts` cloning the comparativo skeleton: `errorResponse`, `interface AvaliarRedacaoDeps { anthropic; openai; supabaseAdmin; supabaseUser; }`, `export async function handler(req, deps)`. Flow: (1) authn `supabaseUser.auth.getUser()` → 401; (2) parse+validate body via AvaliarRedacaoBodySchema → VALIDATION 400; (3) AUTHORIZE (C1) — supabaseAdmin reads candidaturas with an EXPLICIT allowlist (`id, candidato_id, vaga_id, etapa_atual` — never select('*')), verify the candidatura's candidato maps to user.id AND etapa_atual='avaliacao_assincrona' → else 403; (4) read the pergunta `cenario` + `rubric` (allowlist) from `perguntas`; (5) `loadPrompt('work_sample_sjt', supabaseAdmin)` + `resolvedPromptFromLoaded(...,'work_sample_sjt','gpt-4o-mini')`; (6) `callAi({ prompt, rawInput: cenario + texto, schema: WorkSampleScoringSchema, candidato_id, vaga_id }, { anthropic, openai, supabase: supabaseAdmin })`; (7) never-absent: `result.parsed == null` → throw; injection/flagged → throw (persist falhou); (8) map `dimension_scores` (1-5, weighted by `rubric`) → composite 0-25, documenting the derivation in a code comment; any `insufficient_evidence` → pendente_humano; (9) threshold `composite < 13 OR red_flags.length > 0` → status 'pendente_humano' else 'sucesso'; (10) UPSERT scores_candidato { tipo:'sjt', subtipo:'caso_aberto', pergunta_id, score:composite, score_max:25, status, metadata, citacoes, red_flags }; (11) NEVER touch candidaturas.etapa_atual; return neutral `{ ok:true }`. Real clients + npm: SDK runtime import in the `Deno.serve` tail (never in tests). Redacted logs only (ids/counts — Pitfall 7).
  </action>
  <verify>
    <automated>deno test supabase/functions/avaliar-redacao 2>&1 | grep -qiE "ok|passed" && grep -q "work_sample_sjt" supabase/functions/avaliar-redacao/index.ts && ! grep -q "sjt_evaluation" supabase/functions/avaliar-redacao/index.ts && grep -q "WorkSampleScoringSchema" supabase/functions/_shared/avaliacao-schemas.ts</automated>
  </verify>
  <acceptance_criteria>
    - `deno test supabase/functions/avaliar-redacao` PASSES (the 11-01 RED cases — 401/403-non-owner/403-wrong-etapa/mapping/never-etapa — now GREEN against mocked deps).
    - The EF calls `loadPrompt('work_sample_sjt', ...)` and the string `sjt_evaluation` appears NOWHERE in the file.
    - The candidatura read uses an explicit column allowlist (no `select('*')`).
    - `_shared/avaliacao-schemas.ts` exports `WorkSampleScoringSchema` (verbatim) + `AvaliarRedacaoBodySchema` with NO score field.
    - The handler contains no `candidaturas` update (RNF-07a).
  </acceptance_criteria>
  <done>Open-case AI scoring EF authored with C1 authz + work_sample_sjt prompt + weighted 0-25 mapping; deno test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate JWT → avaliar-redacao EF | untrusted answer text + candidatura_id cross here; service_role bypasses RLS so the EF must authorize |
| candidate JWT → pontuar_sjt RPC | DEFINER runs as owner; ownership/etapa must be checked inside |
| client → scores_candidato | candidate must NEVER read scores (PII) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Elevation/Info | avaliar-redacao EF (IDOR) | mitigate | authenticate (getUser→401) THEN authorize ownership + etapa (→403) before any read/write — C1 lesson [[reference_ef_authenticate_vs_authorize]]; deno test asserts 403 non-owner + 403 wrong-etapa |
| T-11-02-02 | Elevation | pontuar_sjt RPC (DEFINER) | mitigate | EXISTS guard on auth.uid() ownership + etapa='avaliacao_assincrona' → RAISE 42501; REVOKE PUBLIC + GRANT authenticated |
| T-11-02-03 | Info disclosure | scores_candidato read | mitigate | candidato has NO read policy (denied); RH/admin role-gated SELECT; reads use allowlist not select('*') ([[reference_select_star_leaks_pii]]) |
| T-11-02-04 | Tampering | client-submitted score | mitigate | RPC accepts only {pergunta_id, opcao_id}; EF body schema has no score field; weights read server-side from the SJT weights table |
| T-11-02-05 | Tampering | back-lock bypass | mitigate | etapa-gate in respostas_avaliacao RLS WITH CHECK + re-asserted in pontuar_sjt + EF authz |
| T-11-02-06 | (compliance) | auto-rejection by score | mitigate | RPC + EF write scores_candidato.status only; never candidaturas.etapa_atual; verify gate `! grep UPDATE public.candidaturas`; SMOKE-5 (11-04) asserts no etapa/historico change (RNF-07a) |
| T-11-02-07 | Tampering | prompt injection via answer text | mitigate | callAi runs detectPromptInjection + maskPII before the API; injection → throw → pendente_humano, never a fabricated success |
| T-11-02-SC | Tampering | npm/deno SDK | accept | No new installs — @anthropic-ai/sdk@0.102.0 / openai@6.42.0 / zod@3.25.76 are existing audited pins (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `deno test supabase/functions/avaliar-redacao` GREEN.
- The 4 migrations are no-wrapper; pontuar_sjt has the 42501 authz + no candidaturas write.
- `npm run test:run -- forbidden-strings` GREEN over the seeded text.
- PROD apply / activation / deploy / db:types are NOT done here — they are Plan 11-04.
</verification>

<success_criteria>
The scoring backbone is authored and unit/deno-verified: generic score sink, etapa-gated autosave table, deterministic MC RPC, and the candidate-invoked open-case EF — all with C1 authz and the never-auto-reject invariant.
</success_criteria>

<output>
Create `.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-02-SUMMARY.md` when done.
</output>
