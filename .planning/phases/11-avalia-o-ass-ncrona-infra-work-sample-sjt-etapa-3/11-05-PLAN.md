---
phase: 11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3
plan: 05
type: execute
wave: 4
depends_on: ["11-03", "11-04"]
files_modified:
  - src/features/avaliacao/components/AvaliacaoContainer.tsx
  - src/features/avaliacao/components/SjtMultiplaEscolhaScreen.tsx
  - src/features/avaliacao/components/SjtCasoAbertoScreen.tsx
  - src/features/avaliacao/components/index.ts
  - src/router/routes.tsx
autonomous: true

requirements: [AVAL-01, AVAL-02, AVAL-03, AVAL-09]

must_haves:
  truths:
    - "The route /candidato/avaliacao/:candidaturaId is RoleGuard role='candidato' guarded and renders AvaliacaoContainer (AVAL-01)"
    - "AvaliacaoContainer copies the DashboardCandidatoPage glass shell (D-27), renders one card per pending teste from testes_aplicaveis with neutral status (Pendente/Concluído/Indisponível) + tempo estimado, ordem livre — the candidate NEVER sees a score/threshold/percent (RNF-07a)"
    - "SjtMultiplaEscolhaScreen renders radio-group items, randomizes option order per session (anti-cheat), shows a soft timer (no hard limit), and submits via avaliacaoService.pontuarSjt behind an alert-dialog confirm"
    - "SjtCasoAbertoScreen renders a textarea with 200-500 word count helper + autosave affordance, submits via avaliacaoService.avaliarRedacao behind the alert-dialog confirm, and surfaces the neutral back-lock state when etapa advanced (locked)"
  artifacts:
    - path: "src/features/avaliacao/components/AvaliacaoContainer.tsx"
      provides: "candidate assessment container (glass shell, cards per teste)"
      contains: "Pendente"
    - path: "src/features/avaliacao/components/SjtMultiplaEscolhaScreen.tsx"
      provides: "MC radio-group screen + soft timer + option shuffle"
    - path: "src/features/avaliacao/components/SjtCasoAbertoScreen.tsx"
      provides: "open-case textarea + word count + autosave"
    - path: "src/router/routes.tsx"
      provides: "guarded /candidato/avaliacao/:candidaturaId route"
      contains: "avaliacao"
  key_links:
    - from: "src/router/routes.tsx"
      to: "AvaliacaoContainer"
      via: "RoleGuard role='candidato'"
      pattern: "avaliacao/:candidaturaId"
    - from: "src/features/avaliacao/components/SjtMultiplaEscolhaScreen.tsx"
      to: "avaliacaoService.pontuarSjt"
      via: "alert-dialog confirm → RPC"
      pattern: "pontuarSjt"
---

<objective>
Build the candidate-facing Avaliação Assíncrona UI: the container (cards per pending teste in the glass shell), the SJT multiple-choice screen (radio-group + soft timer + option shuffle), and the SJT open-case screen (textarea + word count + autosave). Wire the guarded route. All copy + states follow 11-UI-SPEC.md exactly; the candidate never sees a score, threshold, or pass/fail (RNF-07a).

Purpose: Deliver the working candidate assessment flow on top of the live backend (Plan 11-04) and the service/hooks layer (Plan 11-03), turning the 11-01 container RED test GREEN.
Output: 3 components + a barrel + the route. Mobile-first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-UI-SPEC.md
@.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-PATTERNS.md

<interfaces>
<!-- Exact contracts. The shell + service + hooks already exist. -->

From src/components/pages/DashboardCandidatoPage.tsx (the shell to copy — D-27):
- `BackgroundImage background="gradient" overlayColor="bg-black" overlayOpacity={15}` + sticky glass navbar (avatar/nome/email/Sair) + `BeautySmileLogo` + `Glass`/`GlassPanel`/`GlassCard`/`GlassButton` from `../ui/glass` (note: from a component path; in features/ use `@/components/ui/glass`) + `useAuthStore`/`useCandidato` + `sonner` toast. Downscale H1 to `text-3xl` for mobile (UI-SPEC:68).

From src/features/avaliacao/services/avaliacaoService.ts (Plan 11-03):
- `getAvaliacaoContext(candidaturaId)`, `loadResposta(candidaturaId, teste)`, `pontuarSjt(candidaturaId, respostas)`, `avaliarRedacao({ candidatura_id, pergunta_id, texto })`.

From src/features/avaliacao/hooks/useAutosaveAvaliacao.ts (Plan 11-03):
- returns `{ status: 'idle'|'saving'|'saved'|'error'|'locked', flushNow }` — render the autosave affordance + the neutral back-lock state when `locked`.

From src/router/routes.tsx (RoleGuard candidato pattern, analog at the existing /candidato/* entries):
- `{ path: '/candidato/avaliacao/:candidaturaId', element: (<RoleGuard role="candidato"><AvaliacaoContainer /></RoleGuard>) }` — add after the existing candidato routes.

From src/components/ui/ (vendored shadcn — all present since M1):
- `radio-group`, `textarea`, `alert-dialog`, `progress`, `badge`, `skeleton`, `card`, `label`, `button`. lucide-react icons: Circle, CheckCircle2, Lock, Loader2, Check, AlertCircle.

From 11-UI-SPEC.md §Copywriting Contract — use these EXACT pt-BR strings (no score/percent/pass-fail copy):
- Heading "Avaliação"; subheading "Conclua as avaliações abaixo no seu ritmo. Você pode fazê-las em qualquer ordem."; status labels "Pendente"/"Concluído"/"Indisponível"; CTAs "Começar avaliação"/"Continuar avaliação"; empty "Nenhuma avaliação pendente"; all-done "Tudo concluído!".
- MC screen heading "Situação {n} de {total}"; "Cenário"; CTA "Avançar"/"Concluir avaliação"; soft timer "Tempo sugerido: {mm:ss} (sem limite rígido)".
- Open-case heading "Caso prático"; instruction "...Escreva entre 200 e 500 palavras."; word-count "{N} palavras"/"{N} palavras — mínimo 200"/"{N} palavras — máximo 500"; CTA "Enviar resposta".
- Autosave "Salvando…"/"Salvo automaticamente" (Check icon #35BFAD)/"Não foi possível salvar agora — tentando novamente…".
- alert-dialog confirm: title "Enviar avaliação?", body "Após enviar, você não poderá editar suas respostas.", confirm "Enviar", cancel "Revisar".
- back-lock: "Sua etapa avançou. Esta avaliação foi encerrada e suas respostas já estão salvas." + "Voltar ao painel"; wrong-etapa: "Esta avaliação não está disponível. Ela aparecerá aqui quando for a sua etapa." + "Voltar ao painel".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AvaliacaoContainer (glass shell, cards per teste) + barrel + guarded route</name>
  <files>src/features/avaliacao/components/AvaliacaoContainer.tsx, src/features/avaliacao/components/index.ts, src/router/routes.tsx</files>
  <behavior>
    - Renders one card per pending teste from testes_aplicaveis with status pill + "Tempo estimado: ~{N} min".
    - Status labels are exactly "Pendente"/"Concluído"/"Indisponível"; zero score/percent/pass-fail strings (RNF-07a).
    - Empty state renders "Nenhuma avaliação pendente"; all-done renders "Tudo concluído!".
    - Wrong-etapa (not 'avaliacao_assincrona') renders the neutral "Esta avaliação não está disponível." state + "Voltar ao painel".
    - The route /candidato/avaliacao/:candidaturaId is wrapped in RoleGuard role='candidato'.
  </behavior>
  <read_first>
    - src/components/pages/DashboardCandidatoPage.tsx (copy the shell — D-27)
    - src/features/avaliacao/components/__tests__/AvaliacaoContainer.test.tsx (the RED test from 11-01)
    - src/router/routes.tsx (the candidato RoleGuard entries to extend)
    - 11-UI-SPEC.md §Copywriting Contract + §Color (neutral status colors)
  </read_first>
  <action>
    Create `AvaliacaoContainer.tsx`: replicate the DashboardCandidatoPage glass shell (BackgroundImage gradient + sticky navbar + BeautySmileLogo + GlassPanel/GlassCard). Read context via TanStack Query + `avaliacaoService.getAvaliacaoContext(candidaturaId)` (candidaturaId from `useParams`). If `etapa_atual !== 'avaliacao_assincrona'` → render the neutral wrong-etapa state (Lock icon, "Esta avaliação não está disponível.", "Voltar ao painel" → /candidato/dashboard). Else map `testes_aplicaveis` to a list of GlassCards: each shows the test name, "Tempo estimado: ~{N} min", a neutral status pill (Pendente=Circle/text-white/70, Concluído=CheckCircle2/#35BFAD, Indisponível=Lock/text-white/60), and a CTA ("Começar avaliação"/"Continuar avaliação"; none when Concluído) that navigates to the MC or open-case screen by `formato`. Loading = skeleton glass cards; load-error = the "Não foi possível carregar a avaliação." retry state. Empty = "Nenhuma avaliação pendente"; all done = "Tudo concluído!". Heading "Avaliação" at `text-3xl md:text-4xl`. NEVER render any score/threshold/percent. Add `components/index.ts` barrel exporting the 3 components. Add the guarded route in `routes.tsx` after the existing /candidato/* entries.
  </action>
  <verify>
    <automated>npm run test:run -- AvaliacaoContainer 2>&1 | grep -qi "pass" && grep -q "avaliacao/:candidaturaId" src/router/routes.tsx && grep -q "Pendente" src/features/avaliacao/components/AvaliacaoContainer.tsx && npm run test:run -- forbidden-strings 2>&1 | grep -qi "pass"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- AvaliacaoContainer` PASSES (the 11-01 RED test is GREEN); cards render with neutral status + tempo, empty/all-done states present, zero score/percent strings.
    - The route entry `/candidato/avaliacao/:candidaturaId` is RoleGuard role='candidato'.
    - The LGPD-04 grep guard PASSES over the new component copy.
    - The wrong-etapa neutral state renders "Esta avaliação não está disponível." (not an error/alarm).
  </acceptance_criteria>
  <done>Container + guarded route live; the candidate sees only neutral progress.</done>
</task>

<task type="auto">
  <name>Task 2: SjtMultiplaEscolhaScreen + SjtCasoAbertoScreen (submit via service + autosave + confirm)</name>
  <files>src/features/avaliacao/components/SjtMultiplaEscolhaScreen.tsx, src/features/avaliacao/components/SjtCasoAbertoScreen.tsx</files>
  <read_first>
    - src/features/avaliacao/components/AvaliacaoContainer.tsx (shell + nav patterns from Task 1)
    - src/features/avaliacao/hooks/useAutosaveAvaliacao.ts + useAvaliacaoDraft.ts (Plan 11-03)
    - src/features/avaliacao/services/avaliacaoService.ts (pontuarSjt + avaliarRedacao)
    - src/components/ui/radio-group.tsx + textarea.tsx + alert-dialog.tsx (vendored primitives)
    - 11-UI-SPEC.md §SJT screens + §Autosave + §irreversible confirmation
  </read_first>
  <action>
    Create `SjtMultiplaEscolhaScreen.tsx`: render the `perguntas` (formato='mc') for the cargo one situation at a time ("Situação {n} de {total}"), each with the "Cenário" label + the scenario body (16px/1.5) + a shadcn `radio-group` of options. Randomize option display order per session (anti-cheat — shuffle once on mount, keep the opcao_id→label mapping stable). Show the soft timer "Tempo sugerido: {mm:ss} (sem limite rígido)" — count up/down with NO hard cutoff. "Avançar" between items, "Concluir avaliação" on the last → open the alert-dialog confirm ("Enviar avaliação?" / "Após enviar, você não poderá editar suas respostas." / Enviar / Revisar) → on confirm, `avaliacaoService.pontuarSjt(candidaturaId, respostas[{pergunta_id,opcao_id}])` → on success toast "Avaliação enviada com sucesso" + return to container (card now Concluído). Submit button disabled + "Enviando…" while in-flight. The candidate NEVER sees a computed score.
    Create `SjtCasoAbertoScreen.tsx`: render the open-case `pergunta` (formato='caso_aberto'): "Caso prático" heading + "Cenário" + scenario body + a `textarea` ("Comece a escrever sua resposta aqui…"). Live word count with the 3 helper variants (in-range "{N} palavras", below-min "{N} palavras — mínimo 200", above-max "{N} palavras — máximo 500"); disable submit until ≥200 words. Wire `useAutosaveAvaliacao` (30s + on textarea blur) and render the autosave affordance (Salvando…/Salvo automaticamente/tentando novamente). If the hook returns `locked` (etapa advanced) → render "Sua etapa avançou. Esta avaliação foi encerrada e suas respostas já estão salvas." + "Voltar ao painel". "Enviar resposta" → alert-dialog confirm → `avaliacaoService.avaliarRedacao({ candidatura_id, pergunta_id, texto })` → success toast + return. Submit disabled + "Enviando…" while in-flight.
  </action>
  <verify>
    <automated>grep -q "pontuarSjt" src/features/avaliacao/components/SjtMultiplaEscolhaScreen.tsx && grep -q "avaliarRedacao" src/features/avaliacao/components/SjtCasoAbertoScreen.tsx && grep -q "useAutosaveAvaliacao" src/features/avaliacao/components/SjtCasoAbertoScreen.tsx && npm run test:run -- forbidden-strings 2>&1 | grep -qi "pass" && npm run build 2>&1 | tail -3 | grep -qiE "built|done" </automated>
  </verify>
  <acceptance_criteria>
    - MC screen submits via `avaliacaoService.pontuarSjt`, shuffles options per session, shows a non-hard soft timer, gates submit behind the alert-dialog confirm; no score shown.
    - Open-case screen enforces 200-500 word count, wires `useAutosaveAvaliacao` (30s + blur), surfaces the neutral `locked` back-lock state, submits via `avaliacaoService.avaliarRedacao` behind the confirm.
    - LGPD-04 grep guard PASSES; `npm run build` exits 0; tsc baseline unchanged.
  </acceptance_criteria>
  <done>Both SJT screens functional with anti-cheat shuffle, autosave, back-lock UX, and server-authoritative submit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate UI → service/RPC/EF | the candidate submits answers; the UI must never display or post a score |
| candidate UI → autosave | must degrade neutrally on the RLS back-lock (no alarming error) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Info disclosure | candidate-facing copy | mitigate | The UI renders zero score/threshold/percent (RNF-07a); LGPD-04 grep guard covers the new component copy; status is neutral (Pendente/Concluído/Indisponível) |
| T-11-05-02 | Tampering | client score submission | mitigate | The screens submit only opcao_ids / answer text via the service; no score is computed or posted client-side |
| T-11-05-03 | Tampering | back-lock UX | mitigate | useAutosaveAvaliacao `locked` state surfaces the neutral "Sua etapa avançou…" message; the real enforcement is the DB RLS (Plan 11-02/04) |
| T-11-05-SC | Tampering | shadcn primitives | accept | All UI primitives vendored since M1 (UI-SPEC §Registry Safety) — no install/vetting gate |
</threat_model>

<verification>
- `npm run test:run -- AvaliacaoContainer` GREEN; route guarded.
- LGPD-04 grep GREEN over new copy; `npm run build` exits 0.
- MC submits via pontuarSjt, open-case via avaliarRedacao; autosave + back-lock + confirm wired.
</verification>

<success_criteria>
The candidate can open /candidato/avaliacao/:id, see neutral pending-test cards, complete the SJT MC + open case in any order with autosave, and submit — never seeing a score, with the back-lock handled gracefully.
</success_criteria>

<output>
Create `.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-05-SUMMARY.md` when done.
</output>
