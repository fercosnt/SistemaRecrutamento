---
phase: 41-reconcilia-o-de-entrega-retry-testing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260727000001_p41_recon_retry.sql
  - supabase/tests/p41_recon_retry_smoke.sql
autonomous: true
requirements: [RECON-01, RECON-02, RECON-03]

must_haves:
  truths:
    - "A migration ADITIVA adiciona `bounce_em` e `reclamado_em` (timestamptz NULL, sem NOT NULL — a tabela pode ter linhas) + COMMENT ON COLUMN; NÃO cria índice (idx_notif_retry e idx_notif_provider_msg JÁ vivem em PROD); sem wrapper BEGIN/COMMIT externo (42601)"
    - "ler_resend_webhook_secret() é mirror EXATO de ler_resend_api_key: sem argumento, escopado ao literal 'resend_webhook_secret', SECURITY DEFINER SET search_path='', retorna NULL graceful, REVOKE de PUBLIC/anon/authenticated + GRANT EXECUTE TO service_role"
    - "varrer_retry_notificacoes() é SECURITY DEFINER SET search_path='' que lê project_url + edge_invoke_key do Vault (graceful-skip se NULL), seleciona status IN ('pendente','falhou') AND tentativas < 5 AND (proxima_tentativa_em IS NULL OR proxima_tentativa_em <= pg_catalog.now()) ORDER BY proxima_tentativa_em NULLS FIRST LIMIT 20, e para cada linha faz net.http_post à EF notificar-candidato com body {retry_id, evento, candidatura_id, agendamento_id?} e Bearer edge_invoke_key (NUNCA service_role) — cada dispatch em EXCEPTION WHEN OTHERS → RAISE WARNING (a varredura NÃO incrementa tentativas)"
    - "cron.schedule('notif-retry-sweep', '*/15 * * * *', 'SELECT public.varrer_retry_notificacoes();') é registrado com cron.unschedule guard antes (idempotente); o agendamento_id de convite sai de split_part(dedupe_key, ':', 1)"
    - "O smoke p41 é gate-GUC adaptativo (mirror P37/P39): as asserções estruturais/catálogo SEMPRE rodam pós-apply (colunas novas, cron job registrado, def da função contém o predicado + edge_invoke_key + split_part, RPC revogada de PUBLIC); um RESUMO levanta exceção se a contagem não bater — run parcial NÃO é verde"
  artifacts:
    - path: "supabase/migrations/20260727000001_p41_recon_retry.sql"
      provides: "Migration aditiva: bounce_em/reclamado_em + ler_resend_webhook_secret + varrer_retry_notificacoes + cron notif-retry-sweep"
      contains: "varrer_retry_notificacoes"
      min_lines: 120
    - path: "supabase/tests/p41_recon_retry_smoke.sql"
      provides: "smoke gate-GUC de fidelidade (colunas + cron + def da função + RPC)"
      contains: "smoke41.pass"
      min_lines: 80
  key_links:
    - from: "supabase/migrations/20260727000001_p41_recon_retry.sql (varrer_retry_notificacoes)"
      to: "vault (project_url, edge_invoke_key)"
      via: "SELECT decrypted_secret INTO ... WHERE name IN ('project_url','edge_invoke_key')"
      pattern: "edge_invoke_key"
    - from: "supabase/migrations/20260727000001_p41_recon_retry.sql (varrer_retry_notificacoes)"
      to: "EF notificar-candidato (branch retry)"
      via: "net.http_post body jsonb_build_object('retry_id', r.id, ...)"
      pattern: "retry_id"
    - from: "supabase/migrations/20260727000001_p41_recon_retry.sql"
      to: "cron.job (notif-retry-sweep)"
      via: "cron.schedule('notif-retry-sweep','*/15 * * * *', ...) com cron.unschedule guard"
      pattern: "notif-retry-sweep"
---

<objective>
Escrever a ÚNICA migration aditiva da P41 + o smoke SQL de fidelidade — sem aplicar nada. A migration: (1) adiciona `bounce_em` e `reclamado_em` a `notificacoes_enviadas` (colunas finais da state machine, RECON-01); (2) cria `ler_resend_webhook_secret()` (RPC leitora do Vault, mirror de `ler_resend_api_key`, RECON-02); (3) cria `varrer_retry_notificacoes()` (função SECURITY DEFINER que re-invoca a EF `notificar-candidato` em modo retry) + agenda `cron.schedule('notif-retry-sweep', '*/15 * * * *', …)` (RECON-03).

Purpose: cada peça tem gêmeo vivo — as colunas aditivas copiam a forma de `20260722000002` (P37), o hop `net.http_post`+Vault copia `20260726000001` (P39) verbatim, o `cron.schedule` copia `20260609000003`, e a RPC do Vault espelha `20260722000001` (P36). É adaptação cirúrgica de padrões provados, não construção nova.
Output: 1 arquivo de migration + 1 arquivo de smoke SQL. **Zero contato com PROD** — este plano só ESCREVE os arquivos; o apply via MCP `apply_migration` + reconcile + a execução do smoke são o plano GATED 41-05.

⚠ **SAFE-NOW.** Nenhuma tarefa toca o banco. O executor NÃO roda `apply_migration`, `db push`, `psql`, nem `cron.schedule` contra PROD. A prova não vem do `npm run build` (tipos vêm do config, não do banco vivo) — vem do smoke SQL executado no 41-05.

⚠ **NÃO recriar índice.** `idx_notif_retry` (btree proxima_tentativa_em WHERE status IN ('pendente','falhou')) e `idx_notif_provider_msg` JÁ vivem em PROD (provado no smoke da P37). Nenhum `CREATE INDEX` neste arquivo — nem com `IF NOT EXISTS` (mascararia divergência). O smoke os VERIFICA estruturalmente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-CONTEXT.md
@.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-RESEARCH.md
@.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-PATTERNS.md

<interfaces>
<!-- Contratos que o executor consome. Extraídos das migrations vivas — sem exploração. -->

Colunas aditivas + COMMENT (forma) — supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql:99-128 + nota de header sobre apply via MCP sem BEGIN/COMMIT (85-90). Diferença: bounce_em/reclamado_em entram timestamptz NULL (SEM NOT NULL — a tabela pode ter ganho linhas; ≠ destinatario_original NOT NULL da P37 que exigia tabela vazia).

Hop net.http_post + Vault (copiar verbatim a estrutura) — supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql:62-111:
- `SECURITY DEFINER SET search_path = ''`; `SELECT decrypted_secret INTO v_project_url/v_invoke_key FROM vault.decrypted_secrets WHERE name = 'project_url'/'edge_invoke_key'`; `IF ... IS NULL THEN RETURN; END IF;` (graceful-skip);
- `PERFORM net.http_post(url := v_project_url || '/functions/v1/notificar-candidato', headers := jsonb_build_object('Content-Type','application/json','Authorization','Bearer '||v_invoke_key), body := jsonb_build_object(...))`;
- `EXCEPTION WHEN OTHERS THEN RAISE WARNING ...`; `REVOKE ALL ON FUNCTION ... FROM PUBLIC;`.

cron.schedule (molde) — supabase/migrations/20260609000003_prompt_library_cron.sql:36-64 (corpo `$$ ... $$` como job) + nota 42601 (24-29).

RPC leitora do Vault escopada (mirror EXATO) — supabase/migrations/20260722000001_p36_vault_resend_reader.sql:54-83 (ler_resend_api_key): sem argumento, literal, SECURITY DEFINER SET search_path='', NULL graceful, REVOKE FROM PUBLIC/anon/authenticated + GRANT EXECUTE TO service_role, COMMENT explicando o blast-radius de 1 segredo.

Seleção da varredura (CONTEXT Área 1 / RESEARCH Pattern 2, linhas 282-342):
- `FOR r IN SELECT id, evento, candidatura_id, dedupe_key FROM public.notificacoes_enviadas WHERE status IN ('pendente','falhou') AND tentativas < 5 AND (proxima_tentativa_em IS NULL OR proxima_tentativa_em <= pg_catalog.now()) ORDER BY proxima_tentativa_em NULLS FIRST LIMIT 20 LOOP ... END LOOP`.
- body: `'retry_id', r.id, 'evento', r.evento, 'candidatura_id', r.candidatura_id, 'agendamento_id', CASE WHEN r.evento = 'convite' THEN split_part(r.dedupe_key, ':', 1) END`.
- Bearer = `edge_invoke_key` (== NOTIFICAR_SECRET da EF). NUNCA `SUPABASE_SERVICE_ROLE_KEY` (invariante quebrada por rotação — Pitfall 5). A varredura NÃO incrementa `tentativas` (só a EF, ao tentar — at-most-once).

Padrão gate-GUC do smoke — supabase/tests/p39_rewire_triggers_smoke.sql:44-70, 340-358 (set_config('smoke39.pass',...), incremento por asserção via pg_get_functiondef estrutural SEM INSERT, RESUMO (z) que RAISE EXCEPTION se a contagem ≠ esperado). E supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql (asserção de índice/coluna via catálogo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a migration aditiva (colunas + RPC + varredura + cron)</name>
  <files>supabase/migrations/20260727000001_p41_recon_retry.sql</files>
  <read_first>
    - supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql:85-128 (ALTER ADD COLUMN + COMMENT + nota de header MCP/no-BEGIN)
    - supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql:30-33, 62-111 (nota 42601 + hop net.http_post+Vault verbatim)
    - supabase/migrations/20260722000001_p36_vault_resend_reader.sql:54-83 (mirror da RPC leitora do Vault)
    - supabase/migrations/20260609000003_prompt_library_cron.sql:24-64 (cron.schedule + nota 42601)
    - .planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-RESEARCH.md Pattern 2 (282-342) + Code Examples migração (441-458) + Pitfall 7 (434-437)
    - .planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-PATTERNS.md § "202607XXXXXXXX_p41_recon.sql" (241-265)
  </read_first>
  <action>
    Criar `supabase/migrations/20260727000001_p41_recon_retry.sql` com header (PURPOSE/Requirements RECON-01/02/03 + a nota "sem wrapper BEGIN/COMMIT externo, apply via MCP apply_migration + reconcile do ledger, NÃO db push" verbatim do estilo P37/P39). Blocos, nesta ordem:
    (A) `ALTER TABLE public.notificacoes_enviadas ADD COLUMN bounce_em timestamptz;` + `ADD COLUMN reclamado_em timestamptz;` (SEM NOT NULL, SEM default) + `COMMENT ON COLUMN` para cada (citando M7/P41 RECON-02, webhook Resend, NULL até o evento). NÃO criar índice — comentário explicando que idx_notif_retry/idx_notif_provider_msg já vivem.
    (B) `CREATE OR REPLACE FUNCTION public.ler_resend_webhook_secret() RETURNS text LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''` mirror EXATO de `ler_resend_api_key` — escopado ao literal `'resend_webhook_secret'`, NULL graceful; depois `REVOKE ALL ... FROM PUBLIC/anon/authenticated` + `GRANT EXECUTE ... TO service_role` + `COMMENT ON FUNCTION` (blast-radius de 1 segredo).
    (C) `CREATE OR REPLACE FUNCTION public.varrer_retry_notificacoes() RETURNS void LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''` — lê `project_url`+`edge_invoke_key` do Vault, graceful-skip se NULL; `FOR r IN SELECT ... WHERE status IN ('pendente','falhou') AND tentativas < 5 AND (proxima_tentativa_em IS NULL OR proxima_tentativa_em <= pg_catalog.now()) ORDER BY proxima_tentativa_em NULLS FIRST LIMIT 20 LOOP`; dentro do loop `BEGIN PERFORM net.http_post(url := v_project_url || '/functions/v1/notificar-candidato', headers := ..Bearer v_invoke_key.., body := jsonb_build_object('retry_id', r.id, 'evento', r.evento, 'candidatura_id', r.candidatura_id, 'agendamento_id', CASE WHEN r.evento = 'convite' THEN split_part(r.dedupe_key, ':', 1) END)); EXCEPTION WHEN OTHERS THEN RAISE WARNING 'varrer_retry: dispatch falhou id=% (%: %)', r.id, SQLSTATE, SQLERRM; END;` ; após o corpo `REVOKE ALL ON FUNCTION public.varrer_retry_notificacoes() FROM PUBLIC;` + `COMMENT ON FUNCTION` (nota: a varredura NÃO incrementa tentativas — quem incrementa é a EF, at-most-once; Bearer usa edge_invoke_key, nunca service_role).
    (D) agendamento idempotente: `SELECT cron.unschedule('notif-retry-sweep') WHERE EXISTS (SELECT 1 FROM cron.job WHERE jobname = 'notif-retry-sweep');` seguido de `SELECT cron.schedule('notif-retry-sweep', '*/15 * * * *', $sweep$ SELECT public.varrer_retry_notificacoes(); $sweep$);`.
    Domínio em pt-BR nos COMMENTs; nomes técnicos em en. Nenhum segredo hardcoded. NÃO envolver em BEGIN/COMMIT.
  </action>
  <verify>
    <automated>grep -Eic "add column (bounce_em|reclamado_em)" supabase/migrations/20260727000001_p41_recon_retry.sql | grep -qx 2 && grep -qc "varrer_retry_notificacoes" supabase/migrations/20260727000001_p41_recon_retry.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eic "add column bounce_em" ...` e `... reclamado_em ...` cada um ≥1; `grep -ci "create index" supabase/migrations/20260727000001_p41_recon_retry.sql` retorna 0.
    - `grep -c "ler_resend_webhook_secret" ...` ≥1; `grep -c "'resend_webhook_secret'" ...` ≥1; `grep -c "GRANT EXECUTE" ...` ≥1.
    - `grep -c "tentativas < 5" ...` ≥1; `grep -c "edge_invoke_key" ...` ≥1; `grep -ci "service_role_key" ...` retorna 0 no header da varredura (Bearer NÃO usa service_role); `grep -c "split_part(r.dedupe_key" ...` ≥1; `grep -c "retry_id" ...` ≥1.
    - `grep -c "notif-retry-sweep" ...` ≥2 (unschedule guard + schedule); `grep -c "\*/15 \* \* \* \*" ...` ≥1.
    - `grep -Eic "^begin;|^commit;" ...` retorna 0 (sem wrapper transacional externo).
  </acceptance_criteria>
  <done>Migration aditiva escrita: colunas finais da state machine, RPC do Vault, varredura re-invocando a EF em modo retry, cron 15 min idempotente — sem índice, sem BEGIN/COMMIT.</done>
</task>

<task type="auto">
  <name>Task 2: Escrever o smoke SQL de fidelidade (gate-GUC adaptativo)</name>
  <files>supabase/tests/p41_recon_retry_smoke.sql</files>
  <read_first>
    - supabase/tests/p39_rewire_triggers_smoke.sql:44-90, 300-358 (padrão gate-GUC: set_config, asserção estrutural via pg_get_functiondef SEM INSERT, RESUMO com RAISE EXCEPTION na contagem)
    - supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql (asserção de coluna/índice via catálogo information_schema/pg_indexes)
    - supabase/migrations/20260727000001_p41_recon_retry.sql (o arquivo escrito na Task 1 — o alvo a verificar)
  </read_first>
  <action>
    Criar `supabase/tests/p41_recon_retry_smoke.sql` no padrão gate-GUC de `p39_rewire_triggers_smoke.sql`. Cabeçalho explicando: rodar via MCP `execute_sql` DEPOIS do apply do 41-05; RED até lá; estrutural/catálogo = SEM INSERT, seguro em PROD vivo. Inicializar `smoke41.pass`='0'. Asserções (cada uma incrementa o GUC + RAISE NOTICE 'PASS (x)'):
    (a) colunas `bounce_em` e `reclamado_em` existem em `notificacoes_enviadas` (information_schema.columns) e são timestamptz nullable.
    (b) `idx_notif_retry` e `idx_notif_provider_msg` continuam vivos com a forma esperada (pg_indexes.indexdef contém o predicado parcial) — prova de que a migration NÃO os recriou.
    (c) `ler_resend_webhook_secret()` existe, é SECURITY DEFINER, e NÃO tem EXECUTE para PUBLIC/anon/authenticated (checar via has_function_privilege / pg_proc.proacl).
    (d) `pg_get_functiondef('public.varrer_retry_notificacoes()')` contém: `status IN ('pendente','falhou')`, `tentativas < 5`, `edge_invoke_key`, `/functions/v1/notificar-candidato`, `retry_id`, `split_part`, e NÃO contém `service_role` (Bearer não usa service_role).
    (e) o job `notif-retry-sweep` está registrado em `cron.job` com schedule `*/15 * * * *`.
    RESUMO (z): `v_esperado := 5;` `IF v_n <> v_esperado THEN RAISE EXCEPTION 'P41 FAIL (z): RESUMO % PASS de % — run parcial; NÃO tratar como verde', v_n, v_esperado; END IF;` + NOTICE do gate verde. Se alguma asserção depender de comportamento (INSERT), gatear atrás de `smoke41.behavioral` no molde adaptativo do P39 e ajustar o esperado — mas as 5 acima são todas estruturais/catálogo (sem INSERT), então o esperado fixo 5 é honesto e seguro em PROD vivo. `RESET ROLE` nas trocas; NOTICEs sem PII.
  </action>
  <verify>
    <automated>grep -c "smoke41.pass" supabase/tests/p41_recon_retry_smoke.sql && grep -c "RAISE EXCEPTION" supabase/tests/p41_recon_retry_smoke.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "smoke41.pass" ...` ≥6 (init + 5 incrementos); `grep -c "notif-retry-sweep" ...` ≥1; `grep -c "varrer_retry_notificacoes" ...` ≥1.
    - O RESUMO (z) faz `RAISE EXCEPTION` quando a contagem ≠ 5 (run parcial/todo-SKIP falha aqui, não em silêncio).
    - As asserções são estruturais/catálogo (SEM `INSERT INTO public.notificacoes_enviadas`) — seguro em PROD vivo: `grep -ci "insert into public.notificacoes_enviadas" ...` retorna 0.
    - Cobre: colunas novas (a), índices preservados (b), RPC revogada de PUBLIC (c), def da varredura com predicado+edge_invoke_key+retry_id+split_part e sem service_role (d), cron job registrado (e).
  </acceptance_criteria>
  <done>Smoke gate-GUC escrito, provando estruturalmente as colunas, os índices preservados, a RPC escopada, o corpo da varredura e o job cron — com RESUMO que reprova run parcial.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron/pg_net → EF notificar-candidato | Server-to-server; a varredura re-invoca a EF com Bearer do Vault |
| função SECURITY DEFINER → Vault | A varredura e a RPC leem segredos com privilégio elevado; search_path='' + refs qualificadas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-09 | Elevation of Privilege / DoS | Varredura re-invoca a EF com Bearer errado (rotação) | mitigate | Bearer = `edge_invoke_key` do Vault (== NOTIFICAR_SECRET da EF), NUNCA service_role (Pitfall 5); o smoke asserta que o def NÃO contém service_role |
| T-41-10 | Denial of Service | Loop/rajada de e-mail (retry sem freio) | mitigate | `tentativas < 5` na seleção + `LIMIT 20` por sweep + backoff exponencial (proxima_tentativa_em) — a varredura não re-tenta linhas capadas |
| T-41-11 | Information Disclosure | Segredo do Vault vazado por RPC genérica | mitigate | `ler_resend_webhook_secret` sem argumento, escopado ao literal, REVOKE de PUBLIC/anon/authenticated, GRANT só service_role (mirror ler_resend_api_key) |
| T-41-12 | Tampering | 42601 no apply de corpo `$$` adjacente a COMMENT/REVOKE | mitigate | Sem wrapper BEGIN/COMMIT externo; apply via MCP `apply_migration` (não db push) — checkpoint do orquestrador no 41-05 |
| T-41-13 | Denial of Service | Free-tier Resend estoura no flush do backlog pós-DELIV-01 | accept | `LIMIT 20` por sweep como ponto de partida; decisão de subir p/ Pro fica com Fernando antes do flush (RESEARCH Pitfall 6) |
</threat_model>

<verification>
- Migration escrita sem `CREATE INDEX`, sem BEGIN/COMMIT externo; varredura usa `edge_invoke_key` (nunca service_role); cron `notif-retry-sweep` `*/15 * * * *` idempotente.
- Smoke gate-GUC com RESUMO que reprova run parcial; asserções estruturais seguras em PROD vivo (sem INSERT no ledger).
- Nenhum apply/execução — arquivos apenas. O apply + smoke rodam no plano GATED 41-05.
</verification>

<success_criteria>
- Migration aditiva completa (bounce_em/reclamado_em + ler_resend_webhook_secret + varrer_retry_notificacoes + cron) escrita, pronta para apply via MCP (RECON-01/02/03).
- Smoke de fidelidade escrito no padrão gate-GUC.
- Zero contato com PROD.
</success_criteria>

<output>
Create `.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-03-SUMMARY.md` when done
</output>
