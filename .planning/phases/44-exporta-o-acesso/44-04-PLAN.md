---
phase: 44-exporta-o-acesso
plan: 04
type: execute
wave: 2
depends_on: [44-02]
files_modified:
  - database.types.ts
  - supabase/migrations/20260804000001_p44_config_sla_dados.sql
  - supabase/migrations/20260804000002_p44_solicitacoes_dados.sql
autonomous: false
requirements: [EXPORT-05]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "As duas tabelas e as duas RPCs existem EM PRODUÇÃO, e a existência foi verificada no catálogo vivo — não no arquivo de migration (o arquivo não é o objeto vivo; este projeto pagou por isso duas vezes, na 42-07 e na 43/A1)."
    - "As policies vivas de `solicitacoes_dados` e `config_sla_dados` foram LIDAS de `pg_policies` depois do apply e ANTES de qualquer afirmação de RLS ser declarada satisfeita (M3)."
    - "A ordem `config_sla_dados` → `solicitacoes_dados` foi respeitada: a config não tem dependência nem corpo `$$`, então seu apply é o teste barato do procedimento 42601 antes da tabela que importa."
    - "O ledger de migrations foi reconciliado após CADA apply — `apply_migration` carimba um timestamp próprio, que não é o prefixo do arquivo, e sem o reparo um `db push` leria a migration como não-aplicada."
    - "A fidelidade do que chegou ao banco foi provada por md5, não por leitura: `apply_migration` retransmite a migration como STRING e já perdeu blocos de comentário duas vezes neste milestone, sem erro e sem warning."
    - "`database.types.ts` foi REGENERADO pelo CLI, nunca editado à mão, e o diff foi lido hunk a hunk antes do commit."
    - "Nenhuma linha de candidato foi lida, escrita ou apagada por este apply — as únicas escritas são as duas tabelas novas e o seed de uma linha de configuração."
  artifacts:
    - database.types.ts
  key_links:
    - "`pg_policies` (M3) → toda asserção de RLS dos planos 44-05/44-08/44-09: nenhuma delas pode ser declarada satisfeita antes desta leitura."
    - "`solicitacoes_dados` viva → EF `exportar-meus-dados` (44-05): sem a tabela, o registro-antes-de-montar do Pitfall 4 não tem onde acontecer."
    - "`database.types.ts` regenerado → `pedidosDadosService` (44-08): o tipo da linha da fila é escrito à MÃO mesmo assim, porque o gerador declara toda coluna de `RETURNS TABLE` como não-nula."
---

<objective>
As duas migrations da fase chegam a produção na ordem certa, com o ledger reconciliado, a fidelidade
provada por md5 e o smoke executado — e as policies vivas das duas tabelas novas são LIDAS do
catálogo antes de qualquer afirmação de RLS ser feita por qualquer plano posterior.

Purpose: M3 é a única das cinco medições da pesquisa que não pôde ser feita antes do planejamento,
porque as tabelas não existiam. Ela é tarefa do plano por construção. E o idioma que a torna
obrigatória — "o arquivo não é o objeto vivo" — já custou duas correções neste milestone: a 42-07
descobriu um CHECK que o plano não enumerava, e a 43/A1 descobriu três policies vivas que não
existem em arquivo nenhum.

Output: as duas tabelas e as duas RPCs vivas, o ledger limpo, o smoke 14/14, o retrato de
`pg_policies` colado no SUMMARY, e `database.types.ts` regenerado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-exporta-o-acesso/44-CONTEXT.md
@.planning/phases/44-exporta-o-acesso/44-MEASUREMENTS.md
@.planning/phases/44-exporta-o-acesso/44-RESEARCH.md
@.planning/phases/44-exporta-o-acesso/44-02-SUMMARY.md
@CLAUDE.md
@supabase/migrations/20260801000002_p43_config_retencao.sql
</context>

<artifacts_this_phase_produces>
Roster completo da Phase 44. Os itens **em negrito** ganham existência VIVA neste plano.

**Banco (arquivos escritos no 44-02, vivos aqui)**
- **`public.config_sla_dados`** · **`public.solicitacoes_dados`**
- **`public.listar_pedidos_dados(boolean)`** · **`public.contar_pedidos_dados_pendentes()`**
- **`idx_solicitacoes_dados_cooldown`**
- **`database.types.ts` regenerado**

**Compliance** — `export-scope-rules.yaml`, `gen-export-allowlist.cjs`, `catalogo-vivo-44.json`,
`export-allowlist.json`, `_shared/exportAllowlist.ts`, `exportAllowlist.test.ts`,
`05-export-allowlist-drift.sql` (44-01/44-03)

**Edge Function** — `exportar-meus-dados` (44-05)

**Candidato** — `PedirCopiaBloco`, `CurriculosBloco`, `useExportarMeusDados`, `useUltimoPedidoDados`,
`exportacaoService.ts` (`invocarExportMeusDados`, `gerarJsonExport`, `gerarHtmlExport`,
`dispararDownloads`, `lerUltimoPedidoDados`, `mintarUrlCurriculoProprio`, `ExportacaoError`),
entradas novas em `privacidadeKeys` (44-05/44-06/44-07)

**RH** — `PedidosDadosRHPage`, `FilaPedidosDadosTable`, `SituacaoPedidoBadge`, `useFilaPedidosDados`,
`useConfigSlaDados`, `usePedidosDadosPendentesCount`, `pedidosDadosService.ts`,
`constants/slaDados.ts`, `pedidosDadosKeys`, rota `/rh/pedidos-dados`, item `pedidos-dados-rh`
(44-08/44-09)

**Editados** — `package.json`, `vite.config.ts`, `PrivacidadeCandidatoPage.tsx`,
`RevisaoSlaBadge.tsx`, `RHSidebar.tsx`, `routes.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: CHECKPOINT — aplicar as duas migrations na ordem, reparar o ledger, provar por md5 e rodar o smoke</name>
  <read_first>
- `supabase/migrations/20260804000001_p44_config_sla_dados.sql` e `…0002_p44_solicitacoes_dados.sql` — os dois arquivos escritos no 44-02
- `supabase/tests/p44_pedidos_dados_smoke.sql` — o smoke gate-GUC de 14 asserções
- `CLAUDE.md` §"Migrations + db push — workaround conhecido (PL/pgSQL)" — o procedimento 42601
- `44-RESEARCH.md` §G — Migration Mechanics, os 7 passos prescritos
- `.planning/STATE.md` §Blockers/Concerns — o estado do acesso de escrita ao MCP
  </read_first>
  <what-built>
Os dois arquivos `.sql` e o smoke, escritos e verificados por grep no 44-02. **Zero contato com
PROD até aqui.** Subagentes GSD não recebem os tools MCP do Supabase (anthropics/claude-code#13898),
então todo o apply é do orquestrador.
  </what-built>
  <how-to-verify>
Runbook do orquestrador. **A ORDEM É O CONTROLE.** Qualquer passo que não bata o esperado
interrompe a sequência.

### 0 · Medir ANTES de aplicar (a evidência é destruída pelo apply)

| # | Consulta | Esperado | Onde vai |
|---|----------|----------|----------|
| 0.1 | `SELECT to_regclass('public.solicitacoes_dados'), to_regclass('public.config_sla_dados');` | **NULL, NULL** | SUMMARY |
| 0.2 | `SELECT count(*) FROM public.candidatos;` · `… FROM public.candidaturas;` | (medir) | linha de base da asserção negativa do passo 4 |
| 0.3 | `SELECT version FROM supabase_migrations.schema_migrations ORDER BY version DESC LIMIT 3;` | topo = `20260803000001` | confirma que o prefixo escolhido no 44-02 não colidiu |

**Se 0.1 não devolver NULL nos dois: PARE.** Alguma coisa já criou objeto com esse nome, e aplicar
por cima é o caminho para um estado que ninguém consegue descrever depois.

### 1–2 · Aplicar, **uma de cada vez**, na ordem `…0001` → `…0002`

Para **cada** uma:

1. `apply_migration` com o nome sem o prefixo de timestamp (`p44_config_sla_dados`, depois
   `p44_solicitacoes_dados`). **`supabase db push` é PROIBIDO** — o `…0002` tem corpo `$$` adjacente
   a `REVOKE`/`GRANT`/`COMMENT`, que é exatamente a combinação recusada pelo pooler com 42601.
2. **Reparar a `version` do ledger** para o prefixo do arquivo:
   `UPDATE supabase_migrations.schema_migrations SET version = '<prefixo>' WHERE version = '<carimbo do apply>';`
   `apply_migration` grava o timestamp do MOMENTO do apply, não o prefixo. Sem o reparo, um
   `db push` leria a migration como não-aplicada. Medido três vezes na Phase 42.
3. **Provar a fidelidade por md5, não presumir:**
   `SELECT version, md5(statements[1]) FROM supabase_migrations.schema_migrations ORDER BY version DESC LIMIT 3;`
   comparado com `md5 -q supabase/migrations/<arquivo>.sql`. **Têm de bater.** `apply_migration`
   retransmite a migration como STRING e já perdeu blocos de comentário duas vezes neste milestone,
   sem erro e sem warning.
4. Só então passe para a próxima.

**Por que a config vem primeiro:** ela não tem dependência e não tem corpo `$$`. Se o procedimento
42601 tiver mudado de comportamento, isso aparece no apply barato, não na tabela que importa.

### 3 · Rodar o smoke

`supabase/tests/p44_pedidos_dados_smoke.sql` numa **ÚNICA** chamada `execute_sql` —
`set_config(..., false)` é escopado à sessão, e statements espalhados por chamadas separadas zerariam
o contador e reprovariam por run parcial (medido no 41-05).

**Esperado: 14/14 PASS.** Se reprovar, leia a mensagem antes de qualquer outra coisa: a asserção
`(f)` (privilégio residual de `anon`) e a `(i)` (guard sem claim) são as duas que reprovam por
defeito real, não por fixture.

### 4 · Asserções negativas — o que NÃO aconteceu

- `SELECT count(*) FROM public.candidatos;` e `… candidaturas;` **idênticos** a 0.2
- `SELECT count(*) FROM public.solicitacoes_dados;` = **0** (nenhuma linha de fixture sobreviveu ao
  smoke)
- `SELECT count(*) FROM public.config_sla_dados;` = **1** (só o seed)
- `SELECT count(*) FROM cron.job;` inalterado — esta fase não cria cron nenhum
- Nenhuma policy de `autorizacoes`, `candidatos` ou `candidaturas` foi tocada: a contagem de
  `pg_policies` dessas três tabelas é a mesma de antes

### 5 · Confirmar o ledger limpo

`supabase db push --linked` deve responder *"Remote database is up to date"*. Se responder outra
coisa, o reparo do passo 2 ficou incompleto.
  </how-to-verify>
  <acceptance_criteria>
- `to_regclass` das duas tabelas deixou de ser NULL
- Os dois md5 (banco × arquivo) batem, e os dois valores estão no SUMMARY
- `supabase_migrations.schema_migrations` contém `20260804000001` e `20260804000002` como `version`
- O smoke devolveu **14/14 PASS** numa única chamada
- As contagens de `candidatos` e `candidaturas` são idênticas às de 0.2
- `solicitacoes_dados` tem **0** linhas e `config_sla_dados` tem **1**
- `supabase db push --linked` responde "Remote database is up to date"
- Zero `--no-verify` em qualquer commit desta tarefa
  </acceptance_criteria>
  <resume-signal>Responda "aplicado" com os dois md5, o resultado do smoke (n/14) e as contagens do passo 4 — ou descreva o passo que interrompeu a sequência.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — M3: ler as policies VIVAS das duas tabelas novas, antes de qualquer asserção de RLS</name>
  <read_first>
- `44-MEASUREMENTS.md` §M3 — a consulta prescrita e a razão de ela ser pós-apply por construção
- `supabase/migrations/20260804000001_p44_config_sla_dados.sql` e `…0002…` — o que os arquivos DIZEM ter criado (o objeto vivo é o que este checkpoint mede)
- `44-CONTEXT.md` §"M3 continua pendente, por construção"
  </read_first>
  <what-built>
As duas tabelas e as duas RPCs aplicadas na Task 1, com o smoke verde. O smoke prova comportamento;
este checkpoint produz o **retrato do catálogo**, que é o artefato que os planos 44-05, 44-08 e
44-09 citam quando afirmam qualquer coisa sobre RLS.
  </what-built>
  <how-to-verify>
### 1 · A consulta do M3, verbatim

```sql
SELECT schemaname, tablename, policyname, cmd, roles, qual, with_check
  FROM pg_policies
 WHERE tablename IN ('solicitacoes_dados','config_sla_dados')
 ORDER BY tablename, policyname;
```

### 2 · O que o resultado tem de mostrar — e o que reprova

| Tabela | Esperado | Reprova se |
|---|---|---|
| `config_sla_dados` | **exatamente 1** linha: SELECT, `{authenticated}`, `qual` comparando o claim de papel com `rh`/`administrador` | 0 linhas (RLS ligada sem policy = tabela inacessível) · >1 linha · `roles = {public}` · qualquer `cmd` de escrita |
| `solicitacoes_dados` | **exatamente 1** linha: SELECT, `{authenticated}`, `qual` own-row via `candidatos.user_id = auth.uid()` | qualquer policy de INSERT/UPDATE/DELETE (o candidato ganharia caminho de escrita) · `qual` sem o vínculo own-row |

⚠ **O `qual` é asserido por igualdade catálogo-contra-catálogo**, comparando com o `qual` da policy
`candidato_le_propria_candidatura` já auditada — **nunca** contra uma string transcrita à mão. A
37-02 estabeleceu esse idioma: transcrição introduz divergência de espaçamento que reprova uma
policy correta ou, pior, aprova uma errada.

### 3 · Confirmar que RLS está de fato LIGADA (a policy não basta)

```sql
SELECT relname, relrowsecurity, relforcerowsecurity
  FROM pg_class
 WHERE relname IN ('solicitacoes_dados','config_sla_dados');
```
`relrowsecurity` = **true** nas duas. Uma tabela com policy e sem RLS ligada é uma tabela aberta com
documentação bonita.

### 4 · Asserção negativa de privilégio das RPCs (o defeito sistêmico da 42-06)

```sql
SELECT p.proname,
       has_function_privilege('anon',        p.oid, 'EXECUTE') AS anon,
       has_function_privilege('authenticated', p.oid, 'EXECUTE') AS auth
  FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
 WHERE n.nspname = 'public'
   AND p.proname IN ('listar_pedidos_dados','contar_pedidos_dados_pendentes');
```
`anon` = **false** nas duas, `auth` = **true** nas duas. O `pg_default_acl` do schema `public`
concede EXECUTE a `anon` em todo `CREATE FUNCTION` como grant direto e nomeado; ler o `REVOKE` do
arquivo não responde a pergunta — só o catálogo responde.

### 5 · Colar o retrato no SUMMARY

O resultado bruto das quatro consultas vai para o `44-04-SUMMARY.md`, com o timestamp da coleta.
É esse bloco que os planos 44-05/44-08/44-09 citam. **Nenhum plano posterior pode declarar uma
afirmação de RLS satisfeita sem apontar para ele.**
  </how-to-verify>
  <acceptance_criteria>
- `pg_policies` devolveu exatamente 1 linha por tabela, ambas `cmd = SELECT`, ambas `roles = {authenticated}`
- Zero policy de INSERT/UPDATE/DELETE em `solicitacoes_dados`
- `relrowsecurity = true` nas duas tabelas
- `has_function_privilege('anon', …, 'EXECUTE')` = false nas duas RPCs
- O retrato bruto das quatro consultas, com timestamp, está colado no `44-04-SUMMARY.md`
  </acceptance_criteria>
  <resume-signal>Responda "M3 medido" colando o retrato, ou descreva a divergência entre o arquivo e o objeto vivo.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Regenerar `database.types.ts` e ler o diff hunk a hunk</name>
  <files>database.types.ts</files>
  <precondition>As duas migrations do 44-02 estão aplicadas em PROD e o M3 foi medido (Tasks 1 e 2 deste plano concluídas); `supabase` CLI disponível e o projeto linkado (`supabase/.temp/` é gitignored — se o link estiver ausente, `supabase link` resolve sem prompt e sem contato de escrita, como medido na 37-05).</precondition>
  <read_first>
- `package.json` — o script `db:types` e o redirecionamento que ele usa
- `database.types.ts` — o cabeçalho gerado e a forma das entradas de `Functions`
- `src/features/revisao/services/revisaoService.ts` linhas 193–210 — o comentário que explica por que o tipo da linha da fila é escrito à mão apesar deste arquivo existir
- `.planning/STATE.md` §Decisions — as duas lições da 37-05 sobre este script (o `>` que trunca; o link ausente diagnosticado antes de escalar)
  </read_first>
  <action>
⚠ **O script usa `>` , que TRUNCA o arquivo ANTES de executar.** "Rodar pra ver se funciona" é
destruir o arquivo para descobrir. Lição literal da 37-05: **prove o gerador contra um arquivo
TEMPORÁRIO antes de apontá-lo ao arquivo git-trackeado.** Backup sozinho protege contra perda, não
contra o repositório quebrado no intervalo.

Sequência:

1. Rode o gerador para um caminho temporário (`supabase gen types typescript --project-id <id> >
   /tmp/db.types.check.ts` ou equivalente ao que o `db:types` faz), confirme que o arquivo saiu
   não-vazio e contém `solicitacoes_dados`.
2. Só então `npm run db:types`.
3. `git diff --stat database.types.ts` e depois o diff completo.

**Leia o diff hunk a hunk.** O que se espera:
- `Tables` ganha `config_sla_dados` e `solicitacoes_dados` (Row/Insert/Update/Relationships)
- `Functions` ganha `listar_pedidos_dados` e `contar_pedidos_dados_pendentes`
- **Zero deleções.** Deleção é o sinal mais forte de drift lateral: significa que algo que existia
  no tipo sumiu do banco. Se aparecer uma, **pare e registre** antes de commitar.

**Não corrija o tipo à mão, em hipótese alguma** (regra C4 do CLAUDE.md). Se algo parecer errado, o
erro está no banco ou na assinatura da RPC, e o conserto é lá.

**Comportamento conhecido que vai morder aqui, e que NÃO é bug:** o gerador declara toda coluna de
`RETURNS TABLE` como **não-nula**, porque a assinatura SQL não carrega nulidade. Portanto
`listar_pedidos_dados` vai aparecer com `candidato_nome: string`. Isso é falso — o `LEFT JOIN`
devolve NULL quando o nome não é resolvível. A P42 resolveu escrevendo o tipo da linha **à mão** no
serviço, com nulidade honesta, e o 44-08 fará o mesmo com `FilaPedidoDadosRow`. Registre no SUMMARY
que o tipo gerado é conhecidamente otimista neste ponto, para que o 44-08 não o adote por engano.
  </action>
  <acceptance_criteria>
- `grep -c "solicitacoes_dados" database.types.ts` >= 1 e `grep -c "config_sla_dados" database.types.ts` >= 1
- `grep -c "listar_pedidos_dados" database.types.ts` >= 1 e `grep -c "contar_pedidos_dados_pendentes" database.types.ts` >= 1
- `git diff --numstat database.types.ts` mostra **0 deleções** (segunda coluna = 0)
- `npm run lint` sem regressão da baseline 97
- O SUMMARY registra a nota sobre `candidato_nome` chegar não-nulo ao tipo gerado
  </acceptance_criteria>
  <verify>
    <automated>grep -q "solicitacoes_dados" database.types.ts && grep -q "config_sla_dados" database.types.ts && grep -q "listar_pedidos_dados" database.types.ts && grep -q "contar_pedidos_dados_pendentes" database.types.ts && npm run lint</automated>
  </verify>
  <done>`database.types.ts` regenerado pelo CLI (nunca editado à mão), com as duas tabelas e as duas funções presentes, zero deleções no diff, e a nota sobre a nulidade otimista de `candidato_nome` registrada para o 44-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arquivo `.sql` do repositório → objeto vivo em PROD | `apply_migration` retransmite como STRING; o md5 é a única prova de que chegou íntegro |
| ledger `schema_migrations` → CLI | Uma `version` não reconciliada faz o CLI acreditar que a migration não foi aplicada |
| `pg_default_acl` → funções novas | Concede EXECUTE a `anon` por padrão; só o catálogo revela o resíduo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-16 | Elevation of Privilege | privilégio residual de `anon` nas RPCs | high | mitigate | M3 passo 4 pergunta ao catálogo com `has_function_privilege`; false obrigatório nas duas |
| T-44-21 | Repudiation | afirmação de RLS baseada no arquivo, não no objeto vivo | high | mitigate | M3 é checkpoint bloqueante ANTES de qualquer plano posterior declarar RLS satisfeita; o retrato bruto vai para o SUMMARY e é citado nominalmente |
| T-44-22 | Tampering | migration truncada/alterada no transporte | medium | mitigate | md5 banco × arquivo após cada apply; divergência interrompe a sequência |
| T-44-17 | Tampering | policy de escrita inesperada em `solicitacoes_dados` | high | mitigate | M3 passo 2 reprova qualquer `cmd` diferente de SELECT |
| T-44-10 | Information Disclosure | `config_sla_dados` legível por `public` | medium | mitigate | M3 passo 2 reprova `roles = {public}` |
| T-44-23 | Repudiation | `database.types.ts` editado à mão | medium | mitigate | Regeneração pelo CLI, diff com 0 deleções, e o gerador provado contra arquivo temporário antes do trackeado |
</threat_model>

<verification>
- As duas tabelas e as duas RPCs existem no catálogo vivo, com RLS ligada e policies conforme o M3
- Smoke `p44_pedidos_dados_smoke.sql` 14/14 numa única chamada
- Contagens de `candidatos` e `candidaturas` idênticas antes e depois — **asserção negativa obrigatória**
- `supabase db push --linked` responde "Remote database is up to date"
- `npm run lint` sem regressão · **zero `--no-verify`**
</verification>

<success_criteria>
- Objetos vivos em PROD, ledger reconciliado, fidelidade provada por md5
- Retrato de `pg_policies` das duas tabelas colado no SUMMARY, com timestamp
- `database.types.ts` regenerado com 0 deleções
</success_criteria>

<output>
Create `.planning/phases/44-exporta-o-acesso/44-04-SUMMARY.md` when done
</output>
