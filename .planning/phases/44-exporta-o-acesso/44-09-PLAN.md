---
phase: 44-exporta-o-acesso
plan: 09
type: execute
wave: 4
depends_on: [44-08]
files_modified:
  - src/features/pedidos-dados/components/SituacaoPedidoBadge.tsx
  - src/features/pedidos-dados/components/__tests__/SituacaoPedidoBadge.test.tsx
  - src/features/revisao/components/RevisaoSlaBadge.tsx
  - src/features/pedidos-dados/components/FilaPedidosDadosTable.tsx
  - src/features/pedidos-dados/components/__tests__/FilaPedidosDadosTable.test.tsx
  - src/features/pedidos-dados/components/PedidosDadosRHPage.tsx
  - src/features/pedidos-dados/components/__tests__/PedidosDadosRHPage.test.tsx
  - src/router/routes.tsx
  - src/components/RHSidebar.tsx
  - src/components/__tests__/RHSidebarPedidosDados.test.tsx
autonomous: true
requirements: [EXPORT-05]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O SC#4 ganha superfície: o prazo do Art. 19, II é medido a partir de `solicitado_em` e está VISÍVEL ao RH — um pedido perto do teto é distinguível de um recém-chegado pela faixa do badge, e um pedido que FALHOU é distinguível de um atendido pela palavra na coluna Situação (EXPORT-05)."
    - "Âmbar e vermelho codificam EIXOS DIFERENTES e a regra não tem exceção: Situação (o que aconteceu) é âmbar; Acompanhamento (quanto do prazo passou) é vermelho. Dois alarmes vermelhos numa linha destruiriam a distinção que o SC#4 existe para criar. Um ícone por linha, no máximo — e ele pertence à faixa vermelha."
    - "O badge do menu conta o que ESTA tela mostra: o contador e a fila consomem o mesmo par de RPCs, com o predicado do BD-8 escrito uma vez no servidor. Um badge que contasse o que a tela não mostra mandaria o operador procurar trabalho invisível, com 15 dias correndo."
    - "O tooltip desta coluna NÃO é o tooltip de `/rh/revisoes`. Lá a frase nega a existência de prazo, e ali isso é verdade; aqui seria afirmação falsa — o Art. 19, II fixa 15 dias corridos. As duas filas parecem gêmeas e este é o único ponto onde divergem de forma juridicamente relevante."
    - "A fila não oferece ação nenhuma: zero botão, zero caminho de download, zero elemento accent na linha. O atendimento é self-service e um botão seria promessa órfã; um caminho de download seria a segunda superfície de exfiltração que a autorização own-row da EF existe para fechar (Invariante 5)."
    - "O toggle abre DESLIGADO na visão completa — o inverso semântico do análogo. Aqui a linha nasce atendida, e abrir filtrado mostraria tela vazia em quase todo acesso; uma fila que quase sempre aparece vazia deixa de ser consultada, e com ela morre a supervisão."
    - "Os TRÊS sítios da `RHSidebar` entram no mesmo commit — o item existe, o item se acende, o item navega. Cada omissão tem um modo de falha silencioso já documentado no arquivo vivo."
    - "`RevisaoSlaBadge` é reusado VERBATIM, sem renomear e sem uma linha de código alterada; só o docblock muda, porque ele afirma hoje ter um único consumidor e passa a ter dois. Um comentário que mente sobre o componente é o mesmo defeito que a 43 mandou corrigir junto com a copy."
    - "A faixa de controles (banner, nota de ordenação e `Switch`) é estática e renderiza junto com a página; o esqueleto pertence à tabela abaixo, via `AsyncState`. (UI-SPEC E5/loading)"
    - "A faixa de controles não tem fonte de dado própria, logo não tem caminho de falha: o erro pertence à tabela e é tratado lá. (UI-SPEC E5/error)"
    - "`flex flex-wrap items-center justify-between gap-2` — a nota e o toggle quebram para duas linhas em viewport estreito, idioma verbatim da `RevisoesRHPage`. (UI-SPEC E5/overflow)"
    - "Strings fixas na faixa de controles; o banner de escopo é a mais longa e é parágrafo de altura livre, jamais colapsável — precedente de banner sempre visível travado na Phase 43. (UI-SPEC E5/long-text)"
    - "DOIS vazios distintos conforme o toggle, nunca o vazio genérico do `AsyncState`: 'nenhum pedido registrado' e 'nenhum ficou sem atendimento' são fatos diferentes, e o segundo é uma boa notícia que precisa ser lida como tal. (UI-SPEC E6/empty)"
    - "Esqueleto do `AsyncState` no carregamento; a config de SLA NUNCA participa dele — `null` já é apresentação completa (faixa degenerada), então nem `isLoading` nem `isError` da config entram na tabela. (UI-SPEC E6/loading)"
    - "Erro com copy própria + retry via `AsyncState`, jamais o eco da mensagem crua do transporte. (UI-SPEC E6/error)"
    - "Não atendidos primeiro (mais antigo → mais recente), depois atendidos (mais recente → mais antigo); 5 colunas, todas com cabeçalho visível; realce âmbar redundante nas linhas não atendidas. (UI-SPEC E6/populated)"
    - "Sem paginação: o volume vivo é de dezenas e o corte real é do servidor (200), anunciado FORA da tabela quando atingido. `max-h-[70vh]` e o cabeçalho fixo nas CÉLULAS são herdados verbatim e mantidos mesmo com volume baixo — o custo é zero e a alternativa é descobrir a falta deles no dia em que a fila crescer. (UI-SPEC E6/overflow)"
    - "Zero → os dois vazios; um → linha única sem tratamento especial; muitos → cap de 200 com aviso honesto. A copy de dias atendidos tem TRÊS formas (mesmo dia / 1 dia / {n} dias), porque `0d` e `1 dias` seriam ambos lidos como defeito — e com atendimento síncrono o zero é o caso mais comum. (UI-SPEC E6/zero-one-many)"
    - "O nome do candidato usa `CELULA_TRUNCADA` (`max-w-[220px] truncate`) com `title` carregando o valor íntegro — o par obrigatório; as causas vêm de vocabulário fechado, de comprimento conhecido. (UI-SPEC E6/long-text)"
    - statement: "Uma linha NÃO ATENDIDA é distinguível de uma atendida pelo canal TEXTUAL — a asserção procura a palavra 'Não atendido' DENTRO da linha e nunca a classe de cor do realce, porque uma asserção por classe passaria numa UI que quebrou a regra colorblind-safe, que é exatamente o falso verde que a Invariante 6 existe para impedir. O mesmo teste cobre o parcial de dado: causa nula ⇒ 'Motivo não registrado.' e candidato sem nome resolvível ⇒ 'Não identificado', nunca UUID e nunca célula vazia. (UI-SPEC E6/partial)"
      verification: backstop
    - "Duas palavras no máximo no badge de Situação ('Não atendido'); largura natural, sem contêiner fixo. (UI-SPEC E7/overflow)"
    - "Vocabulário fechado com normalização defensiva no cliente: valor desconhecido cai no tratamento neutro exibindo o token cru — nunca célula vazia, nunca superfície fechada. O CHECK do banco já fecha o vocabulário, mas um invariante REMOTO é a coisa errada para uma decisão de RENDERIZAÇÃO se apoiar (precedente 42-11). (UI-SPEC E7/long-text)"
    - "Rótulo e contagem do badge de acompanhamento num único elemento inline; a faixa degenerada é ainda mais curta; sem contêiner de altura fixa. (UI-SPEC E8/overflow)"
    - "Rótulos fixos de uma palavra mais `{n}d`; a faixa degenerada é a resolução do caso extremo — config ausente ou ilegível de `config_sla_dados`, estado alcançável em produção porque a tabela é alterável sem deploy — e ela renderiza a contagem sem badge, nunca uma tela de erro (Invariante 7). (UI-SPEC E8/long-text)"
    - "O item de menu renderiza junto com o menu; o contador chega depois e fica AUSENTE enquanto indefinido — `formatarBadgePendentes` devolve `undefined`, nunca um `0` provisório, e o retorno jamais é comparado com string vazia. (UI-SPEC E9/loading)"
    - "Falha da contagem ⇒ sem badge, item plenamente funcional. Um contador errado no menu é pior que contador nenhum: manda o operador procurar trabalho que não existe. (UI-SPEC E9/error)"
    - "Rótulo de duas palavras; no menu recolhido vira só ícone com `title`, comportamento vivo do componente. (UI-SPEC E9/overflow)"
    - "'Pedidos de dados' — 16 caracteres, mais curto que 'Configurações' somado ao badge, combinação que o menu já acomoda hoje em 'Revisões'. (UI-SPEC E9/long-text)"
  artifacts:
    - src/features/pedidos-dados/components/PedidosDadosRHPage.tsx
    - src/features/pedidos-dados/components/FilaPedidosDadosTable.tsx
    - src/features/pedidos-dados/components/SituacaoPedidoBadge.tsx
    - src/features/pedidos-dados/components/__tests__/FilaPedidosDadosTable.test.tsx
    - src/components/__tests__/RHSidebarPedidosDados.test.tsx
  key_links:
    - "`usePedidosDadosPendentesCount` → badge do item de menu **e** `useFilaPedidosDados` → a tabela: os dois lados do invariante do BD-8. O predicado é do servidor (44-02, smoke (m) sob impersonação real); o que este plano garante é que o cliente lê os dois pelo mesmo par de RPCs e não acrescenta filtro nenhum."
    - "`traduzirCausa` (44-08) → coluna 'O que aconteceu': o vocabulário fechado com fallback total é o que impede que um identificador de caminho de falha apareça cru na tela do RH."
    - "`classifySlaDados` + `lerConfigSlaDados` (44-08) → `RevisaoSlaBadge` reusado: a config alimenta a função viva, e a faixa degenerada absorve os estados que um `UPDATE` mal feito produz."
    - "Rota `/rh/pedidos-dados` + os três sítios da `RHSidebar` → alcançabilidade: sem eles o EXPORT-05 existe em disco e ninguém o alcança, que é exatamente o estado em que a fase estava antes destes dois planos."
    - "Ordenação composta do servidor (44-02) → copy do aviso de corte: 'todos os não atendidos aparecem' só é honesto porque o `ORDER BY` põe os não atendidos primeiro. Mudar um sem o outro faz a fila mentir por omissão."
---

<objective>
A tela. `/rh/pedidos-dados` passa a existir, alcançável pelo menu com contador, com a fila de
supervisão de 5 colunas onde um pedido que falhou é visualmente distinguível de um atendido e um
pedido perto do teto de 15 dias é distinguível de um recém-chegado.

Purpose: o EXPORT-05 tinha tabelas, RPCs e — desde o 44-08 — camada de dados. **Nenhuma tela.** O
SC#4 do ROADMAP não diz "o prazo é medido", diz que ele **está visível ao RH**; uma verificação que
só olhasse o id do requirement no frontmatter dos planos passaria com a fase inteira sem superfície,
e foi exatamente isso que o plan-checker encontrou. Este plano fecha o buraco.

Output: os dois badges (um novo, um reusado com o docblock corrigido), a tabela, a página, a rota e
os três sítios do menu — mais a prova ao vivo de que o contador e a fila contam a mesma coisa sob
dois papéis diferentes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-exporta-o-acesso/44-CONTEXT.md
@.planning/phases/44-exporta-o-acesso/44-UI-SPEC.md
@.planning/phases/44-exporta-o-acesso/44-PATTERNS.md
@.planning/phases/44-exporta-o-acesso/44-VALIDATION.md
@.planning/phases/44-exporta-o-acesso/44-08-SUMMARY.md
@CLAUDE.md

# Os gêmeos estruturais — copiar a estrutura, nunca o conteúdo
@src/features/revisao/components/RevisoesRHPage.tsx
@src/features/revisao/components/FilaRevisoesTable.tsx
@src/features/revisao/components/RevisaoSlaBadge.tsx
@src/features/revisao/components/VereditoBadge.tsx
</context>

<artifacts_this_phase_produces>
Roster completo da Phase 44. Os itens **em negrito** nascem NESTE plano.

**RH — apresentação (`src/features/pedidos-dados/components/`)**
- **`PedidosDadosRHPage.tsx`** (+ **`PAGINA_COPY`**), **`FilaPedidosDadosTable.tsx`** (+
  **`FILA_COPY`**, **`CAP_LEITURA`**, **`TH_CLASSES`**, **`CELULA_TRUNCADA`**, **`formatarData`**,
  **`formatarDataHora`**, **`rotularAtendimento`**), **`SituacaoPedidoBadge.tsx`**
- **rota `/rh/pedidos-dados`** em `src/router/routes.tsx` (via `lazyNamed`, `RoleGuard role={['rh','administrador']}`)
- **item `pedidos-dados-rh`** na `RHSidebar` — **os 3 sítios** — com contador por
  `formatarBadgePendentes` **importada**
- **docblock de `RevisaoSlaBadge.tsx` emendado** (segundo consumidor nomeado; zero mudança de código)

**RH — dados (44-08, prontos e consumidos aqui)** — `pedidosDadosService.ts`, `constants/slaDados.ts`,
`useFilaPedidosDados` (+ `pedidosDadosKeys`), `useConfigSlaDados`, `usePedidosDadosPendentesCount`

**⚠ Três arquivos de teste que o roster compartilhado não enumera, declarados aqui em vez de
contrabandeados** (mesmo procedimento com que o 44-07 declarou `listarMeusCurriculos`):

| Arquivo | Por que ele existe |
|---|---|
| **`components/__tests__/SituacaoPedidoBadge.test.tsx`** | O vocabulário fechado com normalização defensiva (E7/long-text) é uma decisão de renderização sobre um invariante REMOTO; sem suíte própria, o caso "valor desconhecido" só existiria dentro de um teste de tabela, onde o badge não é o sujeito |
| **`components/__tests__/PedidosDadosRHPage.test.tsx`** | O default do toggle é o inverso semântico do análogo e é onde uma cópia cega erra. Ele não é asserível na suíte da tabela, que recebe o valor por prop |
| **`components/__tests__/RHSidebarPedidosDados.test.tsx`** (em `src/components/__tests__/`) | Os três sítios só são prendíveis por comportamento, e o análogo vivo é `RHSidebarRevisoes.test.tsx`. ⚠ A `44-VALIDATION.md` aponta a linha do contador para `src/features/pedidos-dados/components/__tests__/`; o componente vive em `src/components/`, então a asserção mora aqui — divergência declarada, não silenciosa |

**Banco** — `config_sla_dados`, `solicitacoes_dados`, `listar_pedidos_dados`,
`contar_pedidos_dados_pendentes`, `idx_solicitacoes_dados_cooldown`, `p44_pedidos_dados_smoke.sql`
(44-02/44-04)

**Compliance** — `export-scope-rules.yaml`, `gen-export-allowlist.cjs`, `catalogo-vivo-44.json`,
`export-allowlist.json`, `_shared/exportAllowlist.ts`, `exportAllowlist.test.ts`,
`05-export-allowlist-drift.sql` (44-01/44-03)

**Edge Function** — `exportar-meus-dados` (44-05)

**Candidato** — `PedirCopiaBloco`, `CurriculosBloco`, `useExportarMeusDados`, `useUltimoPedidoDados`,
`useMeusCurriculos`, `exportacaoService.ts`, entradas novas em `privacidadeKeys` (44-05/44-06/44-07)

**Editados** — `package.json`, `vite.config.ts`, `PrivacidadeCandidatoPage.tsx`, `database.types.ts`,
e — **neste plano** — **`RevisaoSlaBadge.tsx`**, **`RHSidebar.tsx`**, **`routes.tsx`**
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Os dois badges da linha — o novo, âmbar; e o reusado, cujo docblock passa a dizer a verdade</name>
  <files>src/features/pedidos-dados/components/SituacaoPedidoBadge.tsx, src/features/pedidos-dados/components/__tests__/SituacaoPedidoBadge.test.tsx, src/features/revisao/components/RevisaoSlaBadge.tsx</files>
  <reversibility rating="reversible">Redeclarar a constante tipográfica no badge novo (em vez de exportá-la do módulo vizinho) custa uma linha duplicada e zero comportamento; exportá-la depois é uma edição de duas linhas. O que a duplicação evita é tocar a feature 42 e sua suíte por ganho nulo.</reversibility>
  <read_first>
- `src/features/revisao/components/VereditoBadge.tsx` (49 linhas, INTEIRO) — o molde: vocabulário fechado em `Record<string,string>`, tratamento glass neutro idêntico para os valores do caminho feliz, `TIPOGRAFIA_BADGE` local, e o docblock `:11-14` que explica por que fechar o vocabulário é o que dá largura previsível
- `src/features/revisao/components/RevisaoSlaBadge.tsx` (95 linhas, INTEIRO) — as props públicas `:43-48` (zero acoplamento a revisão), `TIPOGRAFIA_BADGE` **privado ao módulo** em `:60`, e a frase de `:27-28` que afirma consumidor único e passa a ser falsa
- `src/features/revisao/components/__tests__/RevisaoSlaBadge.test.tsx` — a suíte viva que continua verde e é a prova de que o docblock não mudou comportamento
- `src/features/revisao/components/__tests__/VereditoBadge.test.tsx` — o idioma de teste de badge desta família
- `44-UI-SPEC.md` §Badge de Situação (a tabela de três linhas, verbatim), §"A regra de cor mais substantiva desta fase", §Typography → `text-xs`, §UI Considerations E7 e E8
- `44-PATTERNS.md` §0.2 e §`SituacaoPedidoBadge`
  </read_first>
  <behavior>
Testes em `src/features/pedidos-dados/components/__tests__/SituacaoPedidoBadge.test.tsx`. ⚠ O
diretório `__tests__/` é obrigatório — o `include` do Vitest é `**/__tests__/**`, e um arquivo fora
dele não roda e não falha.

- **(bm)** `atendido` → o rótulo **Atendido** com o tratamento glass **neutro** — um pedido atendido
  não é alarme, mesma lógica do `VereditoBadge`
- **(bn)** `pendente` → o rótulo **Não atendido** com o tratamento âmbar. A asserção primária é sobre
  o **texto**, e só depois sobre a classe: é o texto que satisfaz a Invariante 6
- **(bo)** **valor desconhecido** → o token cru visível, com o tratamento neutro. A asserção prova que
  a célula **não** fica vazia e que o componente **não** devolve nulo — divergência deliberada do
  `VereditoBadge`, que devolve nulo fora do vocabulário
- **(bp)** `null` e `undefined` → também renderizam, com o tratamento neutro, e sem lançar
- **(bq)** **asserção negativa de tipografia**: o elemento renderizado não carrega a classe do 5º
  tamanho (12px) que o primitivo traz por padrão — ela é sobrescrita pela constante local
- **(br)** **asserção negativa de ícone**: nenhum `svg` é renderizado por este badge em nenhum dos
  quatro casos. Um ícone por linha, no máximo, e ele pertence à faixa vermelha do outro badge
  </behavior>
  <action>
**`SituacaoPedidoBadge.tsx`** — molde do `VereditoBadge`, com **duas divergências deliberadas**, e as
duas precisam estar escritas no docblock ou a próxima leitura "uniformiza" com o análogo.

| Aspecto | `VereditoBadge` (análogo) | Aqui |
|---|---|---|
| Valor fora do vocabulário | devolve nulo — nada renderiza | renderiza o **token cru** no tratamento neutro |
| Tratamento por valor | idêntico para os dois valores | **neutro** para atendido, **âmbar** para não atendido |

A primeira divergência é a normalização defensiva do precedente 42-11: o CHECK do banco já fecha o
vocabulário, mas um invariante **remoto** é a coisa errada para uma decisão de **renderização** se
apoiar. Um valor novo tem de aparecer, não sumir — sumir fecharia a superfície em silêncio.
A segunda é a regra de eixos: Situação codifica **o que aconteceu**, e o não atendido é o que pede
alguém. O `Record` de vocabulário traz os dois rótulos da UI-SPEC §Badge de Situação, verbatim.

`TIPOGRAFIA_BADGE` é **redeclarado localmente** (`'text-sm font-semibold'`) — a constante do
`RevisaoSlaBadge` é privada ao módulo e o `VereditoBadge` já a redeclara pelo mesmo motivo. Um
`export` novo naquele arquivo seria mudança de código num arquivo que a UI-SPEC trava em "zero
mudança de código".

⚠ **Zero autoria do 5º tamanho** (o de 12px da §Typography): o primitivo `Badge` traz um tamanho
menor por padrão e ele é sobrescrito, exatamente como os dois análogos fazem. **Zero ícone** neste
componente, **zero accent**.

**`RevisaoSlaBadge.tsx` — SOMENTE o docblock.** A frase das linhas 27–28 que hoje afirma um
consumidor único sai e entra a verdade: dois consumidores, a fila de revisão de decisão e a fila de
pedidos de dados, **e a invariante que a frase protege continua valendo integralmente** — faixa, cor,
contagem de dias e rótulo de atraso nunca aparecem em superfície de candidato, em nenhuma das duas
filas (Invariante 8 da 44-UI-SPEC). Acrescente também, na lista de `@see`, o consumidor novo.

⚠ **Nem uma linha de código.** Sem renomear, sem props novas, sem generalizar nome. Renomear tocaria
a feature 42 e seus testes por zero ganho comportamental, e a suíte viva daquele componente é o gate
que prova que nada mudou. A razão de a edição existir é a mesma que a 43 nomeou: **um comentário que
mente sobre o componente é um defeito**, e a próxima pessoa que ler "consumidor único" vai concluir
que pode acoplá-lo a revisão.
  </action>
  <acceptance_criteria>
- `npx vitest run src/features/pedidos-dados/components/__tests__/SituacaoPedidoBadge.test.tsx` — verde, cobrindo (bm)–(br)
- O teste vive em `__tests__/`: `ls src/features/pedidos-dados/components/__tests__/SituacaoPedidoBadge.test.tsx` existe
- A tipografia de label é sobrescrita por constante local: `grep -c 'TIPOGRAFIA_BADGE' src/features/pedidos-dados/components/SituacaoPedidoBadge.tsx` >= 2
- Zero ícone e zero accent no badge de Situação: `grep -cE 'lucide-react|text-accent|35BFAD' src/features/pedidos-dados/components/SituacaoPedidoBadge.tsx` devolve 0
- Zero 5º tamanho autorado: `grep -c 'text-xs' src/features/pedidos-dados/components/SituacaoPedidoBadge.tsx` devolve 0
- O docblock do componente reusado nomeia o consumidor novo: `grep -c 'pedidos-dados' src/features/revisao/components/RevisaoSlaBadge.tsx` >= 1
- A afirmação de consumidor único saiu: `grep -c 'exclusivamente pela fila do RH' src/features/revisao/components/RevisaoSlaBadge.tsx` devolve 0
- **Zero mudança de código no componente reusado**: `npx vitest run src/features/revisao/components/__tests__/RevisaoSlaBadge.test.tsx` verde **e** `git diff -U0 src/features/revisao/components/RevisaoSlaBadge.tsx | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -cvE '^[+-]\s*(\*|/\*\*|\*/|//)' ` devolve 0
- `npm run lint` sem regressão da baseline 97
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/features/pedidos-dados/components/__tests__/SituacaoPedidoBadge.test.tsx src/features/revisao/components/__tests__/RevisaoSlaBadge.test.tsx && [ "$(grep -cE 'lucide-react|text-accent|35BFAD|text-xs' src/features/pedidos-dados/components/SituacaoPedidoBadge.tsx)" = "0" ] && [ "$(grep -c 'exclusivamente pela fila do RH' src/features/revisao/components/RevisaoSlaBadge.tsx)" = "0" ] && npm run lint</automated>
  </verify>
  <done>O badge de Situação existe com vocabulário fechado, normalização defensiva que mostra o token cru em vez de fechar a superfície, âmbar só no não atendido, zero ícone e zero quinto tamanho; e o docblock do badge reusado nomeia os dois consumidores sem que uma linha de código dele tenha mudado, provado pela suíte viva e pelo diff filtrado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `FilaPedidosDadosTable` — cinco colunas, nenhuma ação, e a linha que falhou dizendo isso em palavra</name>
  <files>src/features/pedidos-dados/components/FilaPedidosDadosTable.tsx, src/features/pedidos-dados/components/__tests__/FilaPedidosDadosTable.test.tsx</files>
  <read_first>
- `src/features/revisao/components/FilaRevisoesTable.tsx` (348 linhas, INTEIRO) — o molde: `CAP_LEITURA` (`:59`), `FILA_COPY` como fonte única (`:62-105`), `TH_CLASSES` com o comentário que explica o sticky nas CÉLULAS (`:107-111`), `CELULA_TRUNCADA` (`:114`), `formatarData` com travessão (`:117-125`), o `AsyncState` com copy sobrescrita (`:157-167`), a config fora do carregamento (`:149-151`), o wrapper de scrollport (`:174`) e o aviso de corte (`:334-336`). **E tudo o que SAI**: a coluna de ação (`:208-210`, `:293-326`), o diálogo (`:338-344`) e o `linhaEmFoco`
- `src/features/revisao/components/__tests__/FilaRevisoesTable.test.tsx` linhas 1–60 — o idioma de mock desta família: os hooks mockados por caminho `@/features/…`, **com a fábrica de chaves re-declarada dentro do mock** (senão o import da chave quebra), e as fábricas `linha(over)` / `filaState(over)` para os cenários parciais
- `src/features/pedidos-dados/services/pedidosDadosService.ts` (44-08) — `FilaPedidoDadosRow`, `traduzirCausa`, `COPY_CAUSA`, e a fronteira declarada: a copy da linha atendida é desta tabela, não do serviço
- `src/features/pedidos-dados/constants/slaDados.ts` (44-08) — `diasEmEspera` e os tipos
- `44-UI-SPEC.md` §`/rh/pedidos-dados` (a tabela de copy inteira), §Tooltip do cabeçalho "Acompanhamento", §Coluna "Acompanhamento" (as quatro situações), §Coluna "O que aconteceu", §O que a fila do RH NUNCA mostra, §Color (a consequência declarada: primeira tabela do projeto sem accent na linha), §UI Considerations E6
  </read_first>
  <behavior>
Testes em `src/features/pedidos-dados/components/__tests__/FilaPedidosDadosTable.test.tsx`, no idioma
de mock do análogo (os três hooks do 44-08 mockados, com `pedidosDadosKeys` re-declarada dentro do
mock do hook primário):

- **(bs)** **empty × 2**: com o toggle desligado, o vazio da visão completa; com o toggle ligado, o
  vazio das falhas. A asserção compara os **dois headings entre si** e exige que sejam diferentes —
  o mesmo vazio para os dois estados mentiria
- **(bt)** **loading**: esqueleto do `AsyncState`; e com a config de SLA em erro **a tabela renderiza
  normalmente** — a asserção load-bearing é que `isError` da config **não** aparece na tela
- **(bu)** **error**: copy própria + botão de nova tentativa; e a mensagem crua do transporte **não**
  está no documento (asserção negativa sobre o texto do erro simulado)
- **(bv)** **BACKSTOP E6/partial — a asserção que carrega o SC#4 inteiro**: com uma linha não
  atendida e uma atendida, a asserção localiza a **linha** (`<tr>`) da não atendida e exige nela a
  presença do texto **Não atendido**; e exige que esse texto **não** esteja na linha atendida. A
  asserção é **textual**; ⚠ ela **não** pode ser sobre a classe do realce — uma asserção por classe
  passaria numa UI que quebrou a regra colorblind-safe, que é o falso verde que a Invariante 6 existe
  para impedir
- **(bw)** **partial de dado, no mesmo teste**: causa nula → **Motivo não registrado.**; candidato sem
  nome resolvível → **Não identificado** (nunca travessão, nunca UUID). A asserção **conta as linhas**
  além de procurar o texto: uma asserção puramente textual passaria numa implementação que omitisse a
  linha e escrevesse o rótulo em outro lugar. Asserção negativa explícita: nenhum UUID no documento
- **(bx)** **os dois eixos, na mesma linha**: uma linha não atendida e atrasada carrega **Não
  atendido** (âmbar) **e** **Atrasado** (vermelho) — dois textos distintos, e o teste assere que os
  dois estão presentes e são diferentes. É a asserção que morde se alguém unificar as cores
- **(by)** **a coluna de acompanhamento sob as quatro situações**: pendente com config → a faixa;
  pendente com config `null` → **a contagem sem badge, e a tabela não quebra**; atendido no mesmo dia
  → **Atendido no mesmo dia**; atendido em 1 dia → **Atendido em 1 dia**; atendido em 3 dias →
  **Atendido em 3 dias**. ⚠ O caso do mesmo dia é o mais comum em produção e um `0d` na tela leria
  como bug em vez de sucesso
- **(bz)** **zero ação**: nenhum elemento acionável existe dentro da tabela — a asserção percorre o
  documento renderizado e exige zero controles de ação na linha, além de zero link. É a Invariante 5
  em forma executável
- **(ca)** **aviso de corte**: com 199 linhas o aviso **não** aparece; com 200 ele aparece. As duas
  asserções juntas, porque só a segunda passaria numa implementação que sempre mostra o aviso
- **(cb)** **overflow**: o scrollport carrega `max-h-[70vh]` e o cabeçalho é fixo nas células — a
  asserção é sobre as classes do container e das células de cabeçalho, que são herdadas verbatim
- **(cc)** **long-text**: nome de candidato longo trunca **e** carrega o atributo de tooltip com o
  valor íntegro — o par obrigatório; `truncate` sozinho apaga informação sem oferecer como recuperá-la
- **(cd)** **sonda de texto-fonte da armadilha jurídica**: o módulo da tabela **não** contém a frase
  do tooltip vivo de `/rh/revisoes` que nega a existência de prazo, e **contém** a menção aos 15 dias
  corridos. Os literais são montados em runtime (idioma 42-11)
  </behavior>
  <action>
Clone estrutural do `FilaRevisoesTable`, com **seis desvios**, todos já decididos a montante.

**O que se copia verbatim:** `CAP_LEITURA = 200` (espelho do `LIMIT` do servidor — se um lado mudar
sem o outro a tela para de avisar o corte, ou avisa um corte que não houve), `TH_CLASSES` **com o
comentário que explica por que o sticky vai nas células** (com `border-collapse` do preflight,
sticky em `<thead>` não gruda), `CELULA_TRUNCADA`, `formatarData` com travessão para data inválida,
o wrapper de scrollport com `max-h-[70vh]`, o `AsyncState` com copy sobrescrita, a leitura da config
**fora** do carregamento (com o comentário `:149-151` junto: `null` já é apresentação completa) e o
aviso de corte renderizado **fora** da tabela, só no cap.

**Os seis desvios:**

| # | Desvio | Por quê |
|---|---|---|
| 1 | **Zero coluna de ação.** Sai o cabeçalho `sr-only` de Ações, sai a célula de ação, sai o diálogo e sai todo o estado de linha em foco | Invariante 5: o atendimento é self-service e não há código de RH que o refaça — um controle seria promessa órfã; e um caminho de download seria a segunda superfície de exfiltração |
| 2 | **Zero accent na linha** | Consequência declarada da §Color: esta é a primeira tabela do projeto sem elemento accent na linha. Quem sentir a linha "vazia" e acrescentar um link accent está reintroduzindo a ação que o desvio 1 remove |
| 3 | **5 colunas, todas com cabeçalho VISÍVEL** — Candidato · Pedido feito em · Situação · Acompanhamento · O que aconteceu | Não há coluna de ações, logo não há cabeçalho oculto |
| 4 | **Realce âmbar na linha não atendida** (`bg-amber-500/5 hover:bg-amber-500/10`), canal **redundante** | Invariante 6: a palavra no badge é o canal exigido; a cor nunca é o único |
| 5 | **A copy do tooltip é OUTRA, e a diferença é jurídica** | ⚠ O tooltip vivo do análogo nega a existência de prazo para a revisão de decisão. Copiá-lo aqui produziria afirmação falsa: o Art. 19, II fixa 15 dias corridos. Use a string da 44-UI-SPEC §Tooltip, com o gêmeo `sr-only` ao lado — o tooltip do Radix só monta o conteúdo ao abrir |
| 6 | **`key` da linha** é o identificador do pedido, não o da candidatura | Um pedido de acesso não tem candidatura |

**`FILA_COPY`** `as const`, com o comentário `/** Copy verbatim da 44-UI-SPEC (§/rh/pedidos-dados) —
fonte ÚNICA, sem deriva. */`, carregando: os 5 rótulos de coluna, o tooltip, os **dois** vazios, o
erro, `naoIdentificado`, `semValor`, o aviso de corte interpolando `CAP_LEITURA`, e as três formas da
copy de atendimento.

**A coluna Acompanhamento**, quatro situações:

| Situação da linha | Célula |
|---|---|
| não atendida | `RevisaoSlaBadge` **importado da feature de revisão**, recebendo `diasEmEspera(solicitado_em)` e os limiares — a faixa completa, inclusive a degenerada |
| atendida, mesmo dia | **Atendido no mesmo dia**, `text-sm text-white/50`, neutro, **sem faixa colorida** |
| atendida, 1 dia | **Atendido em 1 dia** |
| atendida, n > 1 | **Atendido em {n} dias** |

Faixa colorida em item fechado é ruído (regra herdada da 42). E **"Atendido em 0d" é inaceitável**:
com atendimento síncrono esse é o caso esmagadoramente mais comum, e um `0d` na tela lê como bug em
vez de sucesso. Por isso o zero tem copy própria e o singular tem a sua — escreva `rotularAtendimento`
como função local pura, para que o teste (by) morda nas três formas.

**A coluna "O que aconteceu"**, duas linhas de texto:

- Atendida → **Cópia entregue em {dd/mm/aaaa} às {HH:mm}.** — `formatarDataHora` local, no mesmo
  idioma de `formatarData`, com travessão para data inválida.
- Não atendida → `traduzirCausa(causa)` do 44-08 (vocabulário fechado, fallback **Motivo não
  registrado.**) **mais uma segunda linha, em TODA linha não atendida**: *Atender pelo Encarregado de
  Dados e responder ao titular.* (`text-sm text-white/60`, mesmo idioma da nota de guard do análogo).
  A segunda linha é o que a Invariante 5 oferece no lugar de um botão: **texto**, a causa e o próximo
  passo humano.

⚠ **Nunca aparecem nesta coluna:** mensagem crua do transporte, código HTTP, stack, nome de tabela,
caminho de Storage ou identificador interno. E a tabela inteira **nunca** mostra e-mail, CPF,
documento, nenhuma parte do conteúdo exportado e nenhum caminho de download — a fila é sobre o
**pedido**, não sobre o **dado**.

**Candidato sem nome resolvível → `Não identificado`**, nunca o travessão genérico e nunca o UUID: as
duas coisas são fatos diferentes, e o docblock `:18-22` do análogo já explica qual é qual. O
travessão fica para "não há valor" (uma data inválida, por exemplo).

**Tipografia e cor sem exceção nova:** nome do candidato em `text-base`, todo o resto em
`text-sm font-semibold` ou `text-sm text-white/…`, **zero autoria do 5º tamanho (12px)**, **zero
accent**, destructive somente no estado de erro do `AsyncState`.
  </action>
  <acceptance_criteria>
- `npx vitest run src/features/pedidos-dados/components/__tests__/FilaPedidosDadosTable.test.tsx` — verde, cobrindo (bs)–(cd)
- O backstop E6/partial é **textual**: o teste (bv) procura a palavra na linha e **não** consulta classe de cor. Porta mecânica secundária (barata, complementa a leitura no code review): `grep -c 'toHaveClass' src/features/pedidos-dados/components/__tests__/FilaPedidosDadosTable.test.tsx` devolve **0** — se um dia alguém trocar a asserção textual por uma de classe, esta linha morde antes do review. A asserção falha se o texto sumir
- O cap espelha o servidor e é interpolado na copy: `grep -c 'CAP_LEITURA' src/features/pedidos-dados/components/FilaPedidosDadosTable.tsx` >= 3
- Zero controle acionável na linha: `grep -cE '<button|onClick=' src/features/pedidos-dados/components/FilaPedidosDadosTable.tsx` devolve 0
- Zero accent e zero 5º tamanho: `grep -cE 'text-accent|35BFAD|text-xs' src/features/pedidos-dados/components/FilaPedidosDadosTable.tsx` devolve 0
- A armadilha jurídica não foi copiada: `grep -c 'não fixa prazo' src/features/pedidos-dados/components/FilaPedidosDadosTable.tsx` devolve 0 · e o teto correto está escrito: `grep -c '15 dias corridos' src/features/pedidos-dados/components/FilaPedidosDadosTable.tsx` >= 1
  > ⚠ **Ao explicar a divergência num comentário de código, PARAFRASEIE — nunca reproduza a frase
  > proibida literalmente.** O grep acima não distingue copy renderizada de comentário explicativo:
  > um comentário do tipo *"diferente de /rh/revisoes, que diz «não fixa prazo»"* faria a asserção
  > falhar pelo motivo errado (o comportamento estaria certo e o teste vermelho). Escreva, por
  > exemplo, *"o tooltip do Art. 20 em /rh/revisoes nega a existência de prazo; aqui o Art. 19, II
  > fixa 15 dias corridos"*.
- O cabeçalho fixo está nas células, nas cinco colunas: `grep -c 'TH_CLASSES' src/features/pedidos-dados/components/FilaPedidosDadosTable.tsx` >= 6
- O scrollport é o herdado: `grep -c 'max-h-\[70vh\]' src/features/pedidos-dados/components/FilaPedidosDadosTable.tsx` devolve 1
- O badge de faixa é o reusado, não uma cópia: `grep -c "from '@/features/revisao/components/RevisaoSlaBadge'" src/features/pedidos-dados/components/FilaPedidosDadosTable.tsx` devolve 1
- `npm run test:run` verde · `npm run lint` sem regressão da baseline 97
  </acceptance_criteria>
  <verify>
    <automated>F=src/features/pedidos-dados/components/FilaPedidosDadosTable.tsx; npx vitest run src/features/pedidos-dados/components/__tests__/FilaPedidosDadosTable.test.tsx && [ "$(grep -cE '<button|onClick=' "$F")" = "0" ] && [ "$(grep -cE 'text-accent|35BFAD|text-xs' "$F")" = "0" ] && [ "$(grep -c 'não fixa prazo' "$F")" = "0" ] && [ "$(grep -c '15 dias corridos' "$F")" -ge 1 ] && [ "$(grep -c 'max-h-\[70vh\]' "$F")" = "1" ] && npm run lint</automated>
  </verify>
  <done>A tabela existe com 5 colunas de cabeçalho visível, sem nenhuma ação e sem accent, com os dois vazios distintos, o realce âmbar redundante, o badge de faixa reusado, as três formas da copy de atendimento, a causa traduzida com fallback total e a segunda linha de próximo passo humano; o tooltip diz os 15 dias corridos e não repete a frase jurídica errada; e o teste prova a distinção não atendido × atendido pelo canal TEXTUAL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: A página, a rota e os três sítios do menu — o EXPORT-05 deixa de ser inalcançável</name>
  <files>src/features/pedidos-dados/components/PedidosDadosRHPage.tsx, src/features/pedidos-dados/components/__tests__/PedidosDadosRHPage.test.tsx, src/router/routes.tsx, src/components/RHSidebar.tsx, src/components/__tests__/RHSidebarPedidosDados.test.tsx</files>
  <precondition>Existem contas de TESTE com login funcional para os dois papéis do BD-8 — um recrutador (precedente vivo: `recrutador.rh@teste.com`) e um administrador — e o recrutador é `created_by` de pelo menos uma vaga com candidatura. Sem essa vaga, a fila dele aparece vazia por **escopo**, não por ausência de pedidos, e as duas coisas são indistinguíveis na tela: o passo ao vivo mediria a coisa errada e concluiria o oposto.</precondition>
  <reversibility rating="reversible">A posição do item no menu e o caminho da rota são uma linha cada; nada migra e nenhum contrato de banco muda. A posição travada (entre Revisões e Vagas) é escolha de vizinhança semântica — as duas filas de direito do titular ficam adjacentes.</reversibility>
  <read_first>
- `src/features/revisao/components/RevisoesRHPage.tsx` (95 linhas, INTEIRO) — o esqueleto `RHLayout` + `GlassCard variant="dark" blur="lg"` + H1/subtítulo + `<h2>` + faixa de controles compacta + tabela, e o docblock `:9-17` que declara a âncora visual e a restrição da dobra
- `src/components/RHSidebar.tsx` linhas 80–140 (contador + `getActivePageFromPath` + `menuItems`), 165–185 (o mapa `routes`) e 290–312 (o render do badge por TERNÁRIO, com o comentário que explica por que `&&` colocava um `0` no menu)
- `src/components/__tests__/RHSidebarRevisoes.test.tsx` (205 linhas, INTEIRO) — o análogo direto desta suíte: os três sítios prendidos por comportamento e os três estados que ocultam o badge
- `src/router/routes.tsx` linha 66 (o `lazyNamed`) e 455–467 (a rota de `/rh/revisoes` com `RoleGuard role={['rh','administrador']}` e o comentário que explica que o gate real de dados é o escopo dentro do `SECURITY DEFINER`)
- `src/features/pedidos-dados/hooks/usePedidosDadosPendentesCount.ts` (44-08) — o contador
- `44-UI-SPEC.md` §Rota nova (os três sítios, a posição travada e o ícone), §Contador do menu **incluindo a correção factual sobre o valor de retorno**, §Âncora visual primária, §UI Considerations E5 e E9
  </read_first>
  <behavior>
**Página** — `src/features/pedidos-dados/components/__tests__/PedidosDadosRHPage.test.tsx`:

- **(ce)** **o toggle abre DESLIGADO e a tabela recebe a visão COMPLETA**: a asserção é sobre a prop
  entregue à tabela mockada, não sobre o estado visual do controle. ⚠ É aqui que uma cópia cega do
  análogo produz a tela errada com o mesmo `false`: lá o `false` significa "não incluir respondidos"
  (abre filtrado), aqui significa "não filtrar" (abre completo)
- **(cf)** **alternar o toggle** troca a prop entregue à tabela — a asserção olha o valor capturado
  antes e depois
- **(cg)** **o banner de escopo é sempre visível e não é colapsável**: presente no render inicial, sem
  nenhum controle que o esconda
- **(ch)** **ordem no DOM**: H1 → subtítulo → banner → título de seção → faixa de controles → tabela.
  A asserção é de **ordem**, não de presença: presença sozinha passaria com o banner montado no rodapé
- **(ci)** o H1 e o subtítulo são a copy verbatim da UI-SPEC, e o subtítulo cita o artigo do acesso

**Sidebar** — `src/components/__tests__/RHSidebarPedidosDados.test.tsx`, no molde de
`RHSidebarRevisoes.test.tsx` (com `usePedidosDadosPendentesCount` mockado):

- **(cj)** **sítio 1**: o item existe e fica **entre Revisões e Vagas** — a asserção compara índices,
  não presença
- **(ck)** **sítio 2**: clicar navega para `/rh/pedidos-dados`
- **(cl)** **sítio 3**: estar em `/rh/pedidos-dados` **realça** o item; e estar em `/rh/revisoes`
  **não** o realça (o realce é exclusivo — a asserção negativa é a que morde numa linha de match
  genérica demais)
- **(cm)** **os três estados que ocultam o badge**: zero, carregando e erro → **nenhum `0`** no DOM da
  sidebar, e o item continua clicável e navegável no caso de erro
- **(cn)** **preenchido e transbordo**: 1 → "1"; 99 → "99"; 100 e 1234 → "99+", com no máximo 3
  caracteres
- **(co)** **o badge acompanha o item certo**: com contagem 7, o item de Pedidos de dados contém "7" e
  o item de Revisões **não**
  </behavior>
  <action>
**`PedidosDadosRHPage.tsx`** — esqueleto verbatim da `RevisoesRHPage`, com **dois desvios**:

1. **O default do toggle é o inverso semântico.** O estado local é `soNaoAtendidos`, inicializado em
   `false` — a tela abre na **visão completa**. A UI-SPEC dá o motivo duro: aqui a linha nasce
   atendida, então abrir filtrado mostraria tela vazia em praticamente todo acesso, e uma fila que
   quase sempre aparece vazia deixa de ser consultada — com ela morre a supervisão que a tela existe
   para dar. Escreva esse motivo como comentário na linha do estado, do jeito que o análogo escreve o
   dele (`:57`), porque os dois `false` parecem iguais e significam o oposto.
2. **+1 banner de escopo** entre o subtítulo e o `<h2>`, tratamento neutro
   `rounded-lg border border-white/15 bg-white/5 p-4`, **nunca colapsável**, com a copy verbatim da
   §Copywriting. É o único elemento estrutural novo em relação ao análogo, e ele diz a coisa que o
   operador precisa saber antes de olhar a tabela: **esta fila é de supervisão**, o pedido é atendido
   pelo próprio candidato no clique, e o que precisa de alguém aqui é o pedido que **não** foi
   atendido.

⚠ **A âncora visual é a TABELA.** O H1, o subtítulo, o banner, a nota de ordenação e o toggle vivem
numa faixa de controles compacta, **não ganham card próprio** e **não podem empurrar a primeira linha
da tabela para fora da dobra em 1366×768**. Copie essa restrição para o docblock: é a que uma edição
futura mais provavelmente viola sem perceber, ao acrescentar um card de resumo acima da tabela.

`PAGINA_COPY` `as const` com a copy verbatim: H1, subtítulo, banner, título de seção, nota de
ordenação e rótulo do toggle. A nota de ordenação descreve a ordem **composta** do servidor — não
atendidos primeiro, do mais antigo para o mais recente; depois os atendidos, do mais recente para o
mais antigo — e é ela que torna verdadeiro o aviso de corte da tabela.

**Rota**, em `src/router/routes.tsx`, no idioma vivo das vizinhas: `lazyNamed` para
`PedidosDadosRHPage` junto dos outros imports preguiçosos, e a entrada `/rh/pedidos-dados` com
`RoleGuard role={['rh','administrador']}` — **o mesmo wrapper das rotas RH vizinhas**, não um gate
admin-only: um gate admin-only excluiria todo recrutador, que é a persona primária desta tela. O
comentário registra que o gate REAL de dados não é o wrapper: é o predicado do BD-8 dentro do
`SECURITY DEFINER` das duas RPCs (44-02), e o administrador ver os órfãos é decisão de escopo do
servidor, não de rota.

**`RHSidebar.tsx` — os TRÊS sítios, no mesmo commit**, cada um com o comentário no idioma vivo do
arquivo (o análogo tem os três comentados nominalmente):

| Sítio | Onde | O que é |
|---|---|---|
| 1 de 3 | `menuItems` | `{ id: 'pedidos-dados-rh', label: 'Pedidos de dados', icon: <FileDown size={24} />, badge: badgePedidosDados }` — posição travada **logo após `revisoes-rh`, antes de `vagas-rh`**: as duas filas de direito do titular ficam adjacentes. **Não** é role-gated: supervisão é trabalho de RH, igual a Revisões |
| 2 de 3 | `getActivePageFromPath` | `if (pathname.startsWith('/rh/pedidos-dados')) return 'pedidos-dados-rh';` junto das irmãs de `/rh/*`. Sem esta linha o item navega e nunca se acende |
| 3 de 3 | mapa `routes` de `handleMenuClick` | `'pedidos-dados-rh': '/rh/pedidos-dados',`. Sem esta entrada o item existe, se acende, e não navega |

O ícone é `FileDown`, 24px, do mesmo pacote de ícones que a sidebar já importa — ele está livre entre
os ícones vivos. Não há armadilha de precedência de prefixo aqui: não existe rota `/rh` genérica.

**O contador:** `const { data: pedidosDadosPendentes } = usePedidosDadosPendentesCount()` e
`const badgePedidosDados = formatarBadgePendentes(pedidosDadosPendentes)`, com
`formatarBadgePendentes` **importada** de `revisaoService` — a mesma que a entrada de Revisões já usa,
nunca reescrita. Reescrevê-la reintroduziria o `0` solto no menu.

⚠ **A função devolve `undefined` para zero/ausente, não string vazia.** Uma versão anterior da
44-UI-SPEC citou string vazia e foi corrigida contra o código vivo; o teste vivo prende o contrato com
`toBeUndefined()`. **Não escreva nenhuma comparação do retorno com string vazia** — ela seria sempre
falsa e faria o badge reaparecer como pílula em branco. O render do slot já é **ternário** e não
precisa de mudança.

⚠ **Contexto que o executor vai encontrar e não deve interpretar como quebra:** três suítes vivas de
sidebar renderizam o componente e não mockam este hook novo. O precedente é `RHSidebar.admin.test.tsx`,
que já hoje renderiza o contador de revisões sem mocká-lo e passa. Rode-as; **se alguma ficar
vermelha, o conserto é acrescentar o mock do hook novo NAQUELA suíte**, e essa edição faz parte desta
tarefa — declare-a no SUMMARY em vez de silenciar o teste.
  </action>
  <acceptance_criteria>
- `npx vitest run src/features/pedidos-dados/ src/components/__tests__/` — verde, cobrindo (ce)–(co) **e** as três suítes vivas de sidebar
- Os três sítios estão presentes: `grep -c 'pedidos-dados-rh' src/components/RHSidebar.tsx` devolve 3
- O contador é o formatador importado, não reescrito: `grep -c 'formatarBadgePendentes' src/components/RHSidebar.tsx` >= 3 · e nenhuma comparação com string vazia entrou: `grep -c "=== ''" src/components/RHSidebar.tsx` devolve 0
- A rota existe com o guard das vizinhas: `grep -c '/rh/pedidos-dados' src/router/routes.tsx` >= 1 · `grep -c 'PedidosDadosRHPage' src/router/routes.tsx` devolve 2 · `grep -A4 "path: '/rh/pedidos-dados'" src/router/routes.tsx | grep -c 'RoleGuard'` >= 1
- O default do toggle abre na visão completa — provado pelo teste (ce) sobre a prop entregue, não por leitura
- A página não ganha card acima da tabela: `grep -c 'GlassCard' src/features/pedidos-dados/components/PedidosDadosRHPage.tsx` devolve 1
- Zero accent e zero 5º tamanho autorados na página: `grep -cE 'text-accent|35BFAD|text-xs' src/features/pedidos-dados/components/PedidosDadosRHPage.tsx` devolve 0
- `npm run test:run` verde · `npm run lint` sem regressão da baseline 97 · **zero `--no-verify`**
- A prova ao vivo do `<human-check>` está registrada no SUMMARY, com o resultado de cada passo
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/features/pedidos-dados/ src/components/__tests__/ && [ "$(grep -c 'pedidos-dados-rh' src/components/RHSidebar.tsx)" = "3" ] && [ "$(grep -c 'PedidosDadosRHPage' src/router/routes.tsx)" = "2" ] && [ "$(grep -c 'GlassCard' src/features/pedidos-dados/components/PedidosDadosRHPage.tsx)" = "1" ] && npm run test:run && npm run lint</automated>
    <human-check>
UAT ao vivo do SC#4 — **é o único passo que prova o requisito na forma em que o ROADMAP o escreveu**
("está visível ao RH"). Não é mockável: o predicado do BD-8 depende de papel real, de JWT real e de
quem é dono de qual vaga.

1. Login com a conta de **recrutador** de teste. O item **Pedidos de dados** aparece no menu, entre
   Revisões e Vagas. Registrar se ele traz badge e **qual número**.
2. Clicar. A tela abre em `/rh/pedidos-dados`, o item do menu **se acende**, e a fila abre na **visão
   completa** (o toggle desligado). Registrar quantas linhas apareceram.
3. **A igualdade do BD-8, medida na tela:** contar as linhas com Situação **Não atendido** e comparar
   com o número do badge do menu. **Têm de ser iguais.** Um badge que conta o que a tela não mostra é
   o defeito que o BD-8 nomeia — e aqui esse trabalho invisível tem prazo de 15 dias.
4. Ligar o toggle **Mostrar só os não atendidos**. Se não houver nenhum, a tela mostra o vazio
   **"Nenhum pedido ficou sem atendimento"** — o vazio da boa notícia, **não** o da fila nunca usada.
   Confirmar que é esse, e não o outro.
5. **Repetir os passos 1–3 com a conta de administrador.** Registrar os dois pares (linhas não
   atendidas × badge) lado a lado. Se o admin vir mais linhas que o recrutador, isso é o BD-8
   funcionando — o admin vê a fila inteira, inclusive os órfãos.
6. ⚠ **Se não existir nenhum pedido não atendido em PROD, registre o fato e pare aí.** **Não fabrique
   um** quebrando a EF nem inserindo linha de fixture: seria poluir um registro de exercício de
   direito, com marco temporal legal, para satisfazer um teste. A distinção não atendido × atendido
   está provada pelo teste (bv); o que este passo mede é a igualdade fila ≡ contador, e ela é
   observável mesmo em zero.
7. Confirmar a 1366×768 que **a primeira linha da tabela está dentro da dobra** — o banner de escopo é
   o elemento novo que pode empurrá-la para fora, e a restrição da âncora é explícita.
8. Registrar no SUMMARY: os dois pares do passo 5, o vazio observado no passo 4 e o resultado do
   passo 7.
    </human-check>
  </verify>
  <done>A página existe com banner de escopo sempre visível e o toggle abrindo na visão completa; a rota `/rh/pedidos-dados` está registrada com o guard das rotas RH vizinhas; os três sítios da sidebar entraram no mesmo commit com o contador pelo formatador importado; as suítes vivas de sidebar seguem verdes; e um RH real e um administrador real abriram a fila ao vivo, com a igualdade fila ≡ contador registrada para os dois papéis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JWT do recrutador → linhas de candidatos de outro recrutador | O predicado do BD-8 dentro do `SECURITY DEFINER` é o controle; a tela não pode alargá-lo, mas pode **contradizê-lo** exibindo um contador de outra fonte |
| contador do menu → fila da tela | Duas leituras, um predicado. A divergência manda o operador procurar trabalho invisível com prazo legal correndo |
| linha da fila → tela do RH | A fila é sobre o **pedido**; qualquer parte do **dado** exportado ou qualquer caminho de download aqui seria uma segunda superfície de exfiltração |
| copy jurídica da fila gêmea → esta fila | As duas telas parecem gêmeas e divergem num fato legal: uma nega a existência de prazo, a outra tem teto de 15 dias corridos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-42 | Information Disclosure | acesso horizontal (recrutador A × candidatos de B) | high | mitigate | O controle é o predicado do BD-8 dentro das duas RPCs, provado por impersonação real no smoke (k)/(l) do 44-02. A contribuição desta camada é **não abrir um segundo caminho**: a tela consome exclusivamente os hooks do 44-08, sem leitura própria, sem parâmetro de escopo e sem id vindo de rota |
| T-44-09 | Information Disclosure | divergência contador × fila (BD-8) | high | mitigate | O badge do menu e a tabela consomem o mesmo par de RPCs; o passo 3 do UAT ao vivo mede a igualdade **na tela**, em dois papéis, e é a única medição que atravessa cliente e servidor de ponta a ponta |
| T-44-12 | Information Disclosure | superfície da fila do RH | high | mitigate | 5 colunas derivadas da allowlist de sete do 44-08; zero coluna de ação, zero caminho de download, zero e-mail/CPF/documento, zero parte do conteúdo exportado. Gate executável: nenhum controle acionável na linha (teste (bz) + grep) |
| T-44-37 | Information Disclosure | identificador interno de falha na tela | medium | mitigate | A coluna traduz por `traduzirCausa` (44-08), vocabulário fechado com fallback **Motivo não registrado.**; a mensagem crua do transporte também não aparece no estado de erro (teste (bu)) |
| T-44-39 | Information Disclosure | valor de acompanhamento vazando para o candidato | medium | mitigate | `RevisaoSlaBadge` continua importado apenas por telas de RH, e o docblock emendado **nomeia os dois consumidores** mantendo a invariante escrita — um comentário que mente é como a próxima pessoa conclui que pode acoplá-lo a uma superfície de candidato |
| T-44-40 | Repudiation | copy que afirma fato jurídico falso | high | mitigate | O tooltip é a string da 44-UI-SPEC, com gêmeo `sr-only`; sonda de texto-fonte (cd) exige a ausência da frase que nega prazo e a presença do teto de 15 dias corridos. Uma cópia da fila gêmea produziria, na tela que o RH usa para decidir sobre prazo legal, a afirmação oposta à lei |
| T-44-41 | Repudiation | fila que mente por omissão no corte | medium | mitigate | `CAP_LEITURA` espelha o `LIMIT` do servidor e o aviso só renderiza **no** cap (teste (ca), com o caso 199 junto do 200); a nota de ordenação descreve a ordem composta que torna verdadeira a frase "todos os não atendidos aparecem" |
</threat_model>

<verification>
- `npx vitest run src/features/pedidos-dados/ src/components/__tests__/` verde
- `npm run test:run` verde · `npm run lint` sem regressão da baseline 97 · **zero `--no-verify`**
- `git diff --exit-code package.json` — **zero dependência npm nova** nesta fase
- `git diff` de `RevisaoSlaBadge.tsx` contém **apenas** linhas de comentário
- Revisão visual a 1366×768 registrada no SUMMARY: a primeira linha da tabela dentro da dobra
- UAT ao vivo com os **dois** papéis do BD-8, com a igualdade fila ≡ contador registrada para cada um
</verification>

<success_criteria>
- `/rh/pedidos-dados` existe, é alcançável pelo menu com contador, e é gateada como as rotas RH vizinhas
- Uma linha não atendida é distinguível de uma atendida **pela palavra**, não pela cor — provado por teste
- Um pedido perto do teto de 15 dias é distinguível de um recém-chegado pela faixa do badge, e a faixa
  degenerada absorve config ausente/ilegível sem erro de tela
- A fila não oferece ação nenhuma e não expõe nenhuma parte do dado exportado
- O contador do menu e a fila mostram o mesmo trabalho, medido ao vivo nos dois papéis do BD-8
- O tooltip desta fila diz a verdade jurídica desta fila
</success_criteria>

<output>
Create `.planning/phases/44-exporta-o-acesso/44-09-SUMMARY.md` when done
</output>