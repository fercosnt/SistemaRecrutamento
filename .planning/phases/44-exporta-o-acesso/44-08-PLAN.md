---
phase: 44-exporta-o-acesso
plan: 08
type: execute
wave: 3
depends_on: [44-04]
files_modified:
  - src/features/pedidos-dados/constants/slaDados.ts
  - src/features/pedidos-dados/constants/__tests__/slaDados.test.ts
  - src/features/pedidos-dados/services/pedidosDadosService.ts
  - src/features/pedidos-dados/services/__tests__/pedidosDadosService.test.ts
  - src/features/pedidos-dados/hooks/useFilaPedidosDados.ts
  - src/features/pedidos-dados/hooks/useConfigSlaDados.ts
  - src/features/pedidos-dados/hooks/usePedidosDadosPendentesCount.ts
  - src/features/pedidos-dados/hooks/__tests__/useFilaPedidosDados.test.ts
autonomous: true
requirements: [EXPORT-05]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A fila e o contador do menu leem as MESMAS duas RPCs e nada mais: o cliente não filtra, não reordena e não repagina. O predicado do BD-8 é do servidor, e um segundo predicado no cliente seria exatamente a divergência que o BD-8 nomeia — um badge que conta o que a tela não mostra, com prazo de 15 dias correndo (EXPORT-05)."
    - "O toggle da tela ('mostrar só os não atendidos') é o INVERSO do parâmetro da RPC (`p_incluir_atendidos`), e a inversão vive em UM lugar só, prendida por teste. Invertê-la por engano abre a tela filtrada — e uma fila que quase sempre aparece vazia deixa de ser consultada, o que mata a supervisão inteira."
    - "A linha da fila é projetada por allowlist nomeada na fronteira do cliente: uma coluna acrescentada no servidor sem revisão de privacidade não alcança a tela nem o cache do TanStack Query. `select('*')` é a classe de vulnerabilidade nº 1 deste projeto, com dois incidentes anteriores."
    - "O tipo da linha é escrito à MÃO, com nulidade honesta — o nome do candidato É nulo quando não resolvível. O gerador declara toda coluna de `RETURNS TABLE` como não-nula, e o 44-04 registrou isso nominalmente para que este plano não adote o tipo gerado por engano."
    - "`classifySlaDados` é a MESMA referência de função de `classifyRevisaoSla`, não uma cópia adaptada — `expect(classifySlaDados).toBe(classifyRevisaoSla)` é a asserção executável que uma cópia-e-cola futura reprova. `LimiaresSlaDados` é alias de tipo re-exportado, nunca uma segunda declaração do formato do limiar."
    - "O classificador continua TOTAL sob o dado desta fase: config ausente, limiar `0` ou ordem invertida — todos alcançáveis em produção, porque `config_sla_dados` é alterável sem deploy — resolvem para a faixa degenerada, jamais para erro de tela (Invariante 7 da UI-SPEC)."
    - "`lerConfigSlaDados` NUNCA lança: erro de transporte, linha ausente ou limiar não numérico resolvem para `null`, que é resultado válido. Uma célula de configuração ilegível não pode derrubar a fila do RH — e por isso a config não participa nem do carregamento nem do erro da tabela."
    - "`traduzirCausa` é total sobre vocabulário fechado e resolve causa nula ou desconhecida para **Motivo não registrado** — nunca célula em branco, nunca o token cru. Aqui o fallback DIVERGE do análogo (`rotularDecisao` mostra o token cru): a causa é um identificador interno do caminho de falha, e mostrá-lo cru seria detalhe de infraestrutura na tela do RH."
    - "Nenhuma leitura desta feature toca a tabela de registro de pedidos por PostgREST: o nome do candidato vive em outra tabela e o escopo do BD-8 é predicado explícito dentro do `SECURITY DEFINER`. As duas únicas portas são as duas RPCs."
    - "A fábrica de chaves é UMA só na feature e o filtro participa da chave da lista — as duas variantes do toggle são duas listas distintas no cache; colidi-las mostraria a lista errada ao alternar o switch, sem refetch e sem erro visível (precedente 42)."
    - "O erro cru do transporte não atravessa esta camada: um `PedidosDadosError` de vocabulário pequeno sai no lugar, e a fila nunca ecoa mensagem de banco, SQLSTATE, nome de tabela ou caminho."
  artifacts:
    - src/features/pedidos-dados/constants/slaDados.ts
    - src/features/pedidos-dados/services/pedidosDadosService.ts
    - src/features/pedidos-dados/hooks/useFilaPedidosDados.ts
    - src/features/pedidos-dados/hooks/useConfigSlaDados.ts
    - src/features/pedidos-dados/hooks/usePedidosDadosPendentesCount.ts
    - src/features/pedidos-dados/constants/__tests__/slaDados.test.ts
    - src/features/pedidos-dados/services/__tests__/pedidosDadosService.test.ts
  key_links:
    - "`listar_pedidos_dados(boolean)` e `contar_pedidos_dados_pendentes()` (escritas no 44-02, vivas desde o 44-04) → `listarFilaPedidosDados` e `contarPedidosDadosPendentes`: dois consumidores, um predicado. A igualdade é provada no SERVIDOR (smoke (m) do 44-02, sob impersonação real) e preservada aqui **por não existir filtro de cliente** — é a única contribuição que o cliente pode dar a esse invariante."
    - "`classifyRevisaoSla` (`slaRevisao.ts:59`) → `classifySlaDados`: mesma referência de função, zero lógica nova. A Área 4 separou as duas TABELAS de config porque são dois prazos legais distintos; ela não separa a FUNÇÃO, que é agnóstica ao prazo."
    - "`config_sla_dados` (44-02) → `lerConfigSlaDados` → faixa do `RevisaoSlaBadge` reusado (44-09): a tabela alimenta a função viva, e a faixa `degenerado` existe porque um `UPDATE` mal feito ainda produz estado que as faixas nominais não cobrem."
    - "`pedidosDadosKeys.list(filtros)` → `FilaPedidosDadosTable` (44-09): o filtro participa da chave, e é isso que faz o toggle trocar de lista em vez de mostrar a lista errada em cache."
---

<objective>
A camada de dados inteira de `/rh/pedidos-dados`: o re-export do classificador de faixa (aliases, zero
lógica nova), o serviço que lê as duas RPCs por allowlist e traduz causa por vocabulário fechado, e os
três hooks com a fábrica de chaves única da feature.

Purpose: o EXPORT-05 tem backend (44-02/44-04) e **nenhuma superfície**. Este plano é a metade de
dados dessa superfície — e é onde o invariante do BD-8 pode ser quebrado do lado do cliente sem que
ninguém perceba: basta um filtro local, uma reordenação "para melhorar", ou uma segunda leitura
PostgREST sobre a tabela. Nenhuma das três acontece aqui, e o teste é quem prova.

Output: `constants/slaDados.ts`, `services/pedidosDadosService.ts` e os três hooks, com o teste de
identidade de referência que a `44-VALIDATION.md` já cita pelo nome. **Zero componente, zero rota,
zero item de menu** — isso é o 44-09.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-exporta-o-acesso/44-CONTEXT.md
@.planning/phases/44-exporta-o-acesso/44-UI-SPEC.md
@.planning/phases/44-exporta-o-acesso/44-PATTERNS.md
@.planning/phases/44-exporta-o-acesso/44-VALIDATION.md
@.planning/phases/44-exporta-o-acesso/44-02-SUMMARY.md
@.planning/phases/44-exporta-o-acesso/44-04-SUMMARY.md
@CLAUDE.md

# Os análogos vivos — este plano copia estrutura destes quatro, e reusa a função de um deles
@src/features/revisao/services/revisaoService.ts
@src/features/revisao/hooks/useFilaRevisoes.ts
@src/features/revisao/hooks/useConfigSlaRevisao.ts
@src/features/revisao/constants/slaRevisao.ts
</context>

<artifacts_this_phase_produces>
Roster completo da Phase 44. Os itens **em negrito** nascem NESTE plano.

**RH (`src/features/pedidos-dados/`)**
- **`constants/slaDados.ts`** — re-export de `classifySlaDados`, `diasEmEspera`,
  `ROTULOS_FAIXA_SLA_DADOS` e dos tipos `LimiaresSlaDados` / `FaixaSlaRevisao`
- **`services/pedidosDadosService.ts`** — `PedidosDadosError`, `classificarErroPedidosDados`,
  `FILA_PEDIDOS_DADOS_COLUNAS`, `FilaPedidoDadosRow`, `FiltrosFilaPedidosDados`,
  `listarFilaPedidosDados`, `contarPedidosDadosPendentes`, `CHAVE_SLA_DADOS`, `lerConfigSlaDados`,
  `traduzirCausa`, `COPY_CAUSA`
- **`hooks/useFilaPedidosDados.ts`** (+ **`pedidosDadosKeys`**), **`hooks/useConfigSlaDados.ts`**,
  **`hooks/usePedidosDadosPendentesCount.ts`**
- **`constants/__tests__/slaDados.test.ts`**
- `PedidosDadosRHPage`, `FilaPedidosDadosTable`, `SituacaoPedidoBadge`, rota `/rh/pedidos-dados`,
  item de menu `pedidos-dados-rh` (44-09)

**⚠ Dois arquivos que o roster compartilhado dos sete planos anteriores NÃO enumera, declarados aqui
em vez de contrabandeados** (mesmo procedimento com que o 44-07 declarou `listarMeusCurriculos`):

| Arquivo | Por que ele existe |
|---|---|
| **`services/__tests__/pedidosDadosService.test.ts`** | A `44-VALIDATION.md` §Wave 0 lista `slaDados.test.ts` e `FilaPedidosDadosTable.test.tsx`, e nenhum teste de serviço. Mas a asserção de allowlist deste projeto **só morde no serviço**: é lá que o mock captura a string de `select` e os argumentos de `rpc`. Sem este arquivo, a projeção explícita — o SC#1 da fase — não tem onde ser provada, e a inversão do toggle não tem onde ser prendida |
| **`hooks/__tests__/useFilaPedidosDados.test.ts`** | Prova que o filtro participa da chave e que existe **uma** fábrica na feature. São asserções sobre valor puro (a chave), sem render e sem rede |

**Banco** — `config_sla_dados`, `solicitacoes_dados`, `listar_pedidos_dados`,
`contar_pedidos_dados_pendentes`, `idx_solicitacoes_dados_cooldown`, `p44_pedidos_dados_smoke.sql`
(44-02/44-04)

**Compliance** — `export-scope-rules.yaml`, `gen-export-allowlist.cjs`, `catalogo-vivo-44.json`,
`export-allowlist.json`, `_shared/exportAllowlist.ts`, `exportAllowlist.test.ts`,
`05-export-allowlist-drift.sql` (44-01/44-03)

**Edge Function** — `exportar-meus-dados` (44-05)

**Candidato** — `PedirCopiaBloco`, `CurriculosBloco`, `useExportarMeusDados`, `useUltimoPedidoDados`,
`useMeusCurriculos`, `exportacaoService.ts`, entradas novas em `privacidadeKeys` (44-05/44-06/44-07)

**Editados** — `package.json`, `vite.config.ts`, `PrivacidadeCandidatoPage.tsx`, `RevisaoSlaBadge.tsx`
(docblock — 44-09), `RHSidebar.tsx` (44-09), `routes.tsx` (44-09), `database.types.ts` (44-04)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `constants/slaDados.ts` — o reuso por alias, provado por identidade de referência</name>
  <files>src/features/pedidos-dados/constants/slaDados.ts, src/features/pedidos-dados/constants/__tests__/slaDados.test.ts</files>
  <reversibility rating="reversible">Trocar o alias por um classificador próprio depois custa um arquivo e nenhuma migração; nenhum contrato de banco muda. O que a decisão compra é a ausência de um segundo lugar onde a faixa degenerada pode apodrecer.</reversibility>
  <read_first>
- `src/features/revisao/constants/slaRevisao.ts` (113 linhas, INTEIRO) — os cinco exports vivos (`FaixaSlaRevisao`, `LimiaresSlaRevisao`, `classifyRevisaoSla`, `diasEmEspera`, `ROTULOS_FAIXA_SLA_REVISAO`) e o docblock `:12-33` que declara a invariante de totalidade e a colorblind-safe
- `src/features/revisao/constants/__tests__/slaRevisao.test.ts` — o idioma de teste do classificador já vivo neste repositório (as faixas nominais e os quatro caminhos degenerados já estão cobertos LÁ; este arquivo não os re-testa)
- `44-PATTERNS.md` §0.1 — o bloco de re-export já escrito, verbatim, e o aviso de que `LimiaresSlaDados` é alias de TIPO
- `44-UI-SPEC.md` §Component Inventory → "As três reutilizações que o plano NÃO pode transformar em cópia" (item 1) e §Faixas do badge de acompanhamento
- `44-VALIDATION.md` §Per-Task Verification Map — a linha `classifySlaDados === classifyRevisaoSla`
  </read_first>
  <behavior>
Testes em `src/features/pedidos-dados/constants/__tests__/slaDados.test.ts`. ⚠ O diretório
`__tests__/` é obrigatório: o `include` do Vitest é `**/__tests__/**`, e um arquivo fora dele **não
roda e não falha** — a forma mais barata de fabricar um falso verde, num plano cuja fase inteira
existe para impedir falsos verdes.

- **(au)** **identidade de referência**: `expect(classifySlaDados).toBe(classifyRevisaoSla)`. Não é
  igualdade de comportamento — é igualdade de **referência**, e é o que uma cópia-e-cola futura
  reprova mesmo que a cópia esteja correta no dia em que for feita
- **(av)** `diasEmEspera` e `ROTULOS_FAIXA_SLA_DADOS` também são a mesma referência dos originais
- **(aw)** **smoke de faixa sob os limiares DESTA fase** (seed 7/12 do 44-02): 3 dias → `em_dia`,
  8 dias → `atencao`, 15 dias → `atrasado`. Não re-testa o classificador (ele já tem suíte própria);
  testa que os limiares de acesso a dados caem nas faixas que a UI-SPEC promete
- **(ax)** **totalidade sob a config desta fase**: `null`, `{ diasAtencao: 0, diasAtraso: 12 }` e
  `{ diasAtencao: 12, diasAtraso: 7 }` (ordem invertida) resolvem para `degenerado` **sem lançar**.
  Os três são estados alcançáveis em PROD porque `config_sla_dados` é alterável sem deploy — o CHECK
  de ordem do banco cobre o `INSERT`, não todo `UPDATE` possível
  </behavior>
  <action>
Crie a feature `src/features/pedidos-dados/` com o arquivo de constantes primeiro — ele é o único que
não depende de nada e é o que o 44-09 vai importar.

**`constants/slaDados.ts` é um arquivo de RE-EXPORT.** Ele re-exporta, de
`@/features/revisao/constants/slaRevisao`: `classifyRevisaoSla` sob o nome `classifySlaDados`,
`diasEmEspera` com o nome original, `ROTULOS_FAIXA_SLA_REVISAO` sob o nome `ROTULOS_FAIXA_SLA_DADOS`,
e — como re-export **de tipo** — `LimiaresSlaRevisao` sob o nome `LimiaresSlaDados` mais
`FaixaSlaRevisao` com o nome original. O bloco exato está na `44-PATTERNS.md` §0.1 e é para ser
copiado de lá.

⚠ **`LimiaresSlaDados` é alias de tipo, nunca uma declaração estrutural nova.** A asserção (au) é
sobre a função e continuaria verde se alguém redeclarasse o formato do limiar aqui — e aí voltaria a
existir um segundo lugar onde `{ diasAtencao, diasAtraso }` pode divergir do que a tabela devolve.
O gate de tamanho da `<acceptance_criteria>` é o que impede isso mecanicamente.

**Zero lógica neste arquivo. Zero import de biblioteca de datas. Zero constante numérica.** Os
limiares vêm da tabela (44-02), nunca compilados — é a diferença entre esta fila e a tabela por
etapa da Phase 34, e o docblock do análogo já a explica.

**O docblock** carrega as duas coisas que um leitor futuro precisa e não deduz: (i) por que a função
é compartilhada mesmo com as tabelas de config separadas — a Área 4 separou o **dado** (dois prazos
legais distintos não cabem numa linha de configuração), não a **função**, que é um classificador
total de três faixas agnóstico ao prazo; e (ii) que o teto de 15 dias corridos do Art. 19, II é
**teto legal**, não limiar de atenção, e por isso não aparece em constante nenhuma deste arquivo.
  </action>
  <acceptance_criteria>
- `npx vitest run src/features/pedidos-dados/constants/__tests__/slaDados.test.ts` — verde, cobrindo (au)–(ax)
- O teste vive em `__tests__/` (senão não é coletado): `ls src/features/pedidos-dados/constants/__tests__/slaDados.test.ts` existe
- O arquivo é re-export e nada mais — corpo de no máximo 12 linhas não-comentário: `[ "$(grep -vcE '^\s*(//|\*|/\*|$)' src/features/pedidos-dados/constants/slaDados.ts)" -le 12 ]`
- As duas re-exportações apontam para o módulo vivo: `grep -c "@/features/revisao/constants/slaRevisao" src/features/pedidos-dados/constants/slaDados.ts` devolve 2
- Zero cópia de lógica de data: `grep -c 'date-fns' src/features/pedidos-dados/constants/slaDados.ts` devolve 0
- `npm run lint` sem regressão da baseline 97
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/features/pedidos-dados/constants/__tests__/slaDados.test.ts && [ "$(grep -c '@/features/revisao/constants/slaRevisao' src/features/pedidos-dados/constants/slaDados.ts)" = "2" ] && [ "$(grep -vcE '^\s*(//|\*|/\*|$)' src/features/pedidos-dados/constants/slaDados.ts)" -le 12 ] && npm run lint</automated>
  </verify>
  <done>`constants/slaDados.ts` existe como re-export puro, com no máximo 12 linhas de corpo, sem lógica e sem import de biblioteca de datas; o teste prova identidade de REFERÊNCIA com `classifyRevisaoSla`, as faixas sob o seed 7/12 e os três caminhos degenerados alcançáveis em produção.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `pedidosDadosService` — duas RPCs, uma allowlist, e a inversão do toggle escrita em UM lugar</name>
  <files>src/features/pedidos-dados/services/pedidosDadosService.ts, src/features/pedidos-dados/services/__tests__/pedidosDadosService.test.ts</files>
  <precondition>As duas RPCs (`listar_pedidos_dados`, `contar_pedidos_dados_pendentes`) e as duas tabelas estão VIVAS em PROD e `database.types.ts` foi regenerado — isto é, o 44-04 fechou com o M3 medido. Sem os tipos regenerados a chamada de RPC não tipa e `npm run lint` reprova; sem as RPCs vivas o serviço compila e mente. Confirme lendo o `44-04-SUMMARY.md` (retrato de `pg_policies` + a nota sobre a nulidade otimista de `candidato_nome`) antes de escrever a primeira linha.</precondition>
  <read_first>
- `src/features/revisao/services/revisaoService.ts` (338 linhas, INTEIRO) — as cinco formas a espelhar: classe de erro com `code` de vocabulário pequeno (`:66-75`), classificador total que nunca lança (`:92-128`), allowlist nomeada + `projetarLinhaFila` na fronteira (`:152-164`, `:231-237`), o tipo da linha escrito à mão com o docblock `:193-199` que explica por quê, e o leitor de config que resolve erro para `null` (`:284-297`)
- `src/features/revisao/services/__tests__/revisaoService.test.ts` linhas 34–105 — o mock de `@/lib/supabase/client` com `vi.hoisted`, que **captura a string de `select()`**, os `eq()` e as chamadas de `rpc()`. O mock vem ANTES do import do serviço, porque o client valida `VITE_SUPABASE_*` no topo do módulo
- `.planning/phases/44-exporta-o-acesso/44-02-SUMMARY.md` — a assinatura real das duas RPCs, as sete colunas do `RETURNS TABLE`, o parâmetro `p_incluir_atendidos` e a ordenação composta que o servidor impõe
- `.planning/phases/44-exporta-o-acesso/44-04-SUMMARY.md` — o retrato do M3 (a autorização desta leitura) e a nota de que o tipo GERADO declara `candidato_nome` como não-nulo, o que é falso
- `44-UI-SPEC.md` §Coluna "O que aconteceu" (o vocabulário fechado de causa, verbatim), §O que a fila do RH NUNCA mostra, §Toggle e §Nota de ordenação
- `44-CONTEXT.md` §BD-8 — o invariante fila ≡ contador e por que ele é de servidor
  </read_first>
  <behavior>
Testes em `src/features/pedidos-dados/services/__tests__/pedidosDadosService.test.ts`, no idioma de
mock de `revisaoService.test.ts` (o mesmo `vi.hoisted` capturando `selects`, `eqs`, `froms` e as
chamadas de `rpc`):

- **(ay)** **a fila chama a RPC certa com o argumento certo**: `listarFilaPedidosDados({ soNaoAtendidos: false })`
  chama `listar_pedidos_dados` com `p_incluir_atendidos: true`
- **(az)** **a INVERSÃO do toggle, asserida nos dois sentidos**: `{ soNaoAtendidos: true }` produz
  `p_incluir_atendidos: false`. É a asserção que morde numa cópia cega do análogo — lá o nome do
  filtro e o nome do parâmetro têm a mesma polaridade; aqui têm polaridades opostas
- **(ba)** **allowlist na fronteira**: uma linha crua da RPC que traga uma coluna extra (um caminho
  de Storage, um e-mail) sai da projeção **sem ela**; as chaves do objeto devolvido são exatamente as
  da constante. A asserção compara o conjunto de chaves, não campo a campo
- **(bb)** **coluna faltando na resposta** resolve para `null` na linha projetada, nunca `undefined`
  e nunca chave ausente — a tela não pode distinguir "não veio" de "veio nulo" por acidente
- **(bc)** **o contador usa a outra RPC e nada mais**: `contarPedidosDadosPendentes()` chama
  `contar_pedidos_dados_pendentes` **sem argumento**, e resposta nula resolve para `0`
- **(bd)** **asserção negativa de superfície**: exercitadas fila e contador, a lista de tabelas que
  chegou a `from(...)` está **vazia** — as duas leituras são RPC. A asserção é sobre o mock, não sobre
  o texto do arquivo: uma leitura PostgREST acrescentada depois traria o nome do candidato por join
  do cliente e furaria o escopo do BD-8, que vive dentro do `SECURITY DEFINER`
- **(be)** `lerConfigSlaDados` projeta **três colunas nomeadas** e filtra pela chave da fase; a
  asserção captura a string passada a `select(...)` e prova a igualdade com a constante
- **(bf)** **asserção negativa de projeção**: a string de `select` não contém o caractere de projeção
  total, com o literal montado em runtime (idioma 42-11 — um literal escrito no teste seria ele mesmo
  a ocorrência que o grep encontra)
- **(bg)** `lerConfigSlaDados` **não lança** em nenhum dos três caminhos: erro de transporte, linha
  ausente, limiar não numérico → `null` nos três
- **(bh)** **erro da fila sai classificado, nunca cru**: um erro `42501` e um erro genérico produzem
  `PedidosDadosError` com `code` do vocabulário pequeno, e a mensagem crua do transporte **não**
  aparece na mensagem do erro devolvido (asserção negativa sobre o texto do transporte)
- **(bi)** **`traduzirCausa` é total**: os três tokens do vocabulário viram a copy verbatim da
  UI-SPEC; `null`, string vazia e um token desconhecido viram **Motivo não registrado.** — e a
  asserção do token desconhecido prova que ele **não** aparece no retorno
  </behavior>
  <action>
Crie `src/features/pedidos-dados/services/pedidosDadosService.ts` espelhando as cinco formas vivas de
`revisaoService.ts`. Mesmo diretório-irmão, mesma persona, mesmo contrato de erro.

**`PedidosDadosError`** — `extends Error`, com `code: 'PERMISSAO' | 'DESCONHECIDO'` e
`details?: unknown`, `this.name` preenchido. Dois valores, e o vocabulário é pequeno de propósito:
a UI desta fila distingue **dois** tratamentos, não mais. **`classificarErroPedidosDados`** é total —
qualquer entrada, inclusive `null`, resolve para um `PedidosDadosError`, nunca lança. SQLSTATE
`42501` → `PERMISSAO`; qualquer outra coisa → `DESCONHECIDO`. ⚠ **Não copie a discriminação por
MENSAGEM do análogo** (`:82-91`): lá ela existe porque o servidor usa o mesmo `42501` para duas
recusas semanticamente distintas; aqui o `42501` das duas RPCs tem uma causa só — o guard de papel.
Copiar a heurística de mensagem criaria um ramo que nunca é alcançado, que é a classe "guarda que era
dead code" (P39/CR-02) que este projeto já embarcou uma vez.

**`FILA_PEDIDOS_DADOS_COLUNAS`** — as **sete** colunas do `RETURNS TABLE` do 44-02, `as const`:
identificador do pedido, identificador do candidato, nome do candidato, situação, causa, momento do
pedido e momento do atendimento. Nada além disso: a fila é sobre o **pedido**, não sobre o **dado**
(Invariante 5 da UI-SPEC), e não existe e-mail, CPF, documento nem caminho de Storage nesta camada.
O docblock diz o que a constante é — o espelho CLIENTE do contrato do servidor — e o que ela FAZ:
travar a expectativa, para que uma coluna acrescentada no servidor sem revisão de privacidade quebre
um teste antes de chegar à tela.

**`FilaPedidoDadosRow`** — o tipo escrito **à mão**, com nulidade honesta. ⚠ **Não derive de
`database.types.ts`**: o 44-04 registrou nominalmente que o gerador declara toda coluna de
`RETURNS TABLE` como não-nula, o que é otimista e errado — o `LEFT JOIN` da RPC devolve nome nulo
quando não há correspondência, e é exatamente esse o caso que a tela resolve para "Não identificado"
(44-09). Confiar no tipo gerado aqui produziria uma chamada de método sobre nulo em produção com o
compilador calado — o defeito literal que o docblock `:193-199` do análogo descreve.

**`projetarLinhaPedido`** — a projeção defensiva na fronteira, no molde de `projetarLinhaFila`
(`:231-237`): percorre a allowlist e devolve exatamente essas chaves, com `null` para as ausentes.
É defesa em profundidade, não redundância: o `RETURNS TABLE` é o contrato primário, mas um
`CREATE OR REPLACE` futuro que acrescente uma coluna passaria a trafegar sem que nada reclamasse.

**`FiltrosFilaPedidosDados`** — `{ soNaoAtendidos: boolean }`, e o nome é do domínio da TELA.

**`listarFilaPedidosDados(filtros)`** — `supabase.rpc('listar_pedidos_dados', { p_incluir_atendidos:
!filtros.soNaoAtendidos })`, erro classificado e **lançado**, linhas projetadas.

⚠ **A negação é o ponto mais escorregadio deste plano, e ela existe UMA vez, aqui.** O filtro da tela
é "mostrar só os não atendidos"; o parâmetro do servidor é "incluir os atendidos". As polaridades são
opostas **de propósito** — a UI-SPEC exige que a tela abra na visão completa, porque nesta fila a
linha nasce atendida e abrir filtrado mostraria tela vazia em quase todo acesso, e uma fila que
aparece vazia deixa de ser consultada. O `COMMENT` do 44-02 e o teste (az) são os dois lados dessa
trava; escreva no docblock que qualquer outro lugar do cliente que precise da polaridade tem de
importar daqui, nunca renegar por conta própria.

⚠ **O cliente não reordena e não repagina.** A ordenação composta (não atendidos do mais antigo ao
mais recente, depois atendidos do mais recente ao mais antigo) e o `LIMIT 200` são contrato do
servidor. Reordenar aqui daria uma segunda fonte de verdade para a promessa da tela, e as duas
divergiriam no primeiro corte do `LIMIT` — as 200 linhas escolhidas pelo servidor não são as 200
primeiras de uma ordenação nova. É a mesma razão escrita em `useFilaRevisoes.ts:8-13`, e aqui ela
tem uma consequência a mais: é a ordenação que torna verdadeira a copy do aviso de corte.

**`contarPedidosDadosPendentes()`** — `supabase.rpc('contar_pedidos_dados_pendentes')`, sem
argumento, erro classificado e lançado, retorno nulo resolvido para `0`. O docblock declara o
invariante do BD-8 do lado do cliente: **esta função e a de listagem não podem ganhar filtro,
predicado ou pós-processamento algum** — a igualdade fila ≡ contador é provada no servidor (smoke (m)
do 44-02, sob impersonação real de dois recrutadores e um administrador) e a única forma de o cliente
quebrá-la é acrescentar um segundo predicado aqui.

**`CHAVE_SLA_DADOS`** = a chave da única linha semeada no 44-02 (`acesso_dados`).
**`lerConfigSlaDados()`** — `from('config_sla_dados')` com allowlist de três colunas nomeadas
(`chave, dias_atencao, dias_atraso`) passada como constante a `.select(...)`, `.eq` pela chave,
`maybeSingle()`, e `if (error || !data) return null` mais a checagem de que os dois limiares são
numéricos. **Nunca lança.** O docblock cita o retrato do M3 no `44-04-SUMMARY.md` como fonte da
autorização — não o arquivo de migration, porque o arquivo não é o objeto vivo — e explica a
proporção: uma célula de configuração ilegível não pode derrubar a fila inteira do RH; `null` já é
uma apresentação completa (a faixa degenerada).

**`COPY_CAUSA`** `as const` exportada, com o comentário `/** Copy verbatim da 44-UI-SPEC (§Coluna "O
que aconteceu") — fonte única desta coluna. */`, e **`traduzirCausa(causa)`** total sobre ela: os
três tokens fechados pelo CHECK do banco viram as três frases da UI-SPEC; causa nula, vazia ou
desconhecida vira **Motivo não registrado.**

⚠ **Aqui o fallback DIVERGE do análogo, e a divergência é deliberada.** `rotularDecisao`
(`FilaRevisoesTable.tsx:128-131`) resolve token desconhecido para o **valor cru**, porque lá o token
é vocabulário de produto que o RH reconhece. A causa desta fila é um identificador do **caminho de
falha interno**; mostrá-lo cru seria detalhe de infraestrutura na tela, que a UI-SPEC §O que a fila
NUNCA mostra proíbe nominalmente. Escreva a razão no docblock, senão a próxima leitura "uniformiza"
com o análogo e reintroduz o vazamento.

⚠ **`traduzirCausa` não cobre a linha atendida.** A copy do caminho feliz interpola data e hora
("Cópia entregue em …") e é formatação de apresentação — ela mora na tabela (44-09), junto do
formatador de data que já resolve data inválida para travessão. Declare essa fronteira no docblock
para que o 44-09 não a duplique aqui nem a invente lá.

Fecha com o **export namespaced** `pedidosDadosService`, convenção `camelCaseService` do projeto.
  </action>
  <acceptance_criteria>
- `npx vitest run src/features/pedidos-dados/services/__tests__/pedidosDadosService.test.ts` — verde, cobrindo (ay)–(bi)
- As duas leituras são RPC, e são exatamente duas: `grep -c 'supabase.rpc(' src/features/pedidos-dados/services/pedidosDadosService.ts` devolve 2
- A allowlist é constante nomeada E é o que a projeção percorre: `grep -c 'FILA_PEDIDOS_DADOS_COLUNAS' src/features/pedidos-dados/services/pedidosDadosService.ts` >= 2
- A inversão do toggle existe uma vez só: `grep -c 'p_incluir_atendidos' src/features/pedidos-dados/services/pedidosDadosService.ts` devolve 1
- A projeção total está ausente — provado pelo teste (bf), com o literal montado em runtime, não por leitura de arquivo
- A superfície de tabela está ausente — provado pelo teste (bd) sobre o mock, não por grep
- O tipo da linha é escrito à mão e existe: `grep -c 'export interface FilaPedidoDadosRow' src/features/pedidos-dados/services/pedidosDadosService.ts` devolve 1
- E não é derivado do gerador — **forma filtrada**, porque o docblock cita o arquivo gerado nominalmente (é a razão de o tipo ser manual) e um grep cru sobre o arquivo inteiro se autoinvalidaria: `grep -vE '^\s*(//|\*|/\*)' src/features/pedidos-dados/services/pedidosDadosService.ts | grep -c 'database.types'` devolve 0
<!-- planner-discipline-allow: database.types -->
- Zero dependência nova: `git diff --exit-code package.json` sem alteração neste plano
- `npm run test:run` verde · `npm run lint` sem regressão da baseline 97
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/features/pedidos-dados/services/__tests__/pedidosDadosService.test.ts && [ "$(grep -c 'supabase.rpc(' src/features/pedidos-dados/services/pedidosDadosService.ts)" = "2" ] && [ "$(grep -c 'p_incluir_atendidos' src/features/pedidos-dados/services/pedidosDadosService.ts)" = "1" ] && [ "$(grep -c 'export interface FilaPedidoDadosRow' src/features/pedidos-dados/services/pedidosDadosService.ts)" = "1" ] && npm run lint</automated>
  </verify>
  <done>O serviço lê as duas RPCs e nada mais, projeta por allowlist nomeada de sete colunas na fronteira, escreve o tipo da linha à mão com nulidade honesta, inverte o toggle num único ponto documentado, resolve config ilegível para `null` sem lançar, classifica erro sem ecoar a mensagem crua, e traduz causa por vocabulário fechado com fallback total para "Motivo não registrado."</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Os três hooks e a fábrica de chaves única — o filtro dentro da chave, o contador fora do carregamento</name>
  <files>src/features/pedidos-dados/hooks/useFilaPedidosDados.ts, src/features/pedidos-dados/hooks/useConfigSlaDados.ts, src/features/pedidos-dados/hooks/usePedidosDadosPendentesCount.ts, src/features/pedidos-dados/hooks/__tests__/useFilaPedidosDados.test.ts</files>
  <read_first>
- `src/features/revisao/hooks/useFilaRevisoes.ts` (59 linhas, INTEIRO) — a fábrica `revisoesKeys` (`:36-42`) morando no hook primário do domínio, o `list(filtros)` que carrega o filtro na chave, e o docblock `:8-13` sobre a ordem vir do servidor
- `src/features/revisao/hooks/useConfigSlaRevisao.ts` (41 linhas, INTEIRO) — `retry: false` com a justificativa literal nas linhas 1–15
- `src/features/revisao/hooks/useRevisoesPendentesCount.ts` (35 linhas, INTEIRO) — `retry: 2` e o docblock `:9-11` que proíbe renderizar o número cru
- `src/features/pedidos-dados/services/pedidosDadosService.ts` — os três leitores criados na Task 2 e os tipos que os hooks devolvem
- `44-PATTERNS.md` §`src/features/pedidos-dados/hooks/*` — a tabela hook-a-hook e o aviso de que o invariante do BD-8 é de servidor, não dos hooks
- `44-UI-SPEC.md` §Component Inventory (linhas dos três hooks) e §UI Considerations E6/loading, E9/loading e E9/error
  </read_first>
  <behavior>
Testes em `src/features/pedidos-dados/hooks/__tests__/useFilaPedidosDados.test.ts` — asserções sobre
**valor puro** (a chave), sem render e sem rede:

- **(bj)** a fábrica é hierárquica e o filtro **participa** da chave: `pedidosDadosKeys.list({ soNaoAtendidos: true })`
  e `pedidosDadosKeys.list({ soNaoAtendidos: false })` produzem chaves **diferentes**, e as duas
  começam pelo prefixo de `lists()`
- **(bk)** as chaves do contador e da config são distintas entre si e distintas das duas listas — a
  asserção compara os quatro valores serializados, porque duas chaves que coincidem fazem uma
  invalidação derrubar a outra query sem erro visível
- **(bl)** a raiz da fábrica não colide com a fábrica da revisão: o primeiro segmento é o do domínio
  novo. Duas features com a mesma raiz compartilhariam invalidação silenciosamente
  </behavior>
  <action>
Três arquivos, cada um no molde verbatim do seu irmão da feature de revisão. **Nada estrutural
diverge** — o que diverge são as três coisas abaixo, e todas são de docblock.

**`useFilaPedidosDados.ts`** hospeda a fábrica **`pedidosDadosKeys`** (raiz `'pedidos-dados'`, mais
`lists()`, `list(filtros)`, `pendentesCount()` e `configSla()`), pela mesma razão que `revisoesKeys`
mora em `useFilaRevisoes.ts`: é a convenção viva — a chave mora junto do hook primário do domínio e
os irmãos a importam daqui. **Uma fábrica por feature**; duas seriam o começo de duas convenções de
invalidação. O hook usa `staleTime` = `gcTime` = 5 min e `retry: 2`, e o filtro entra na chave via
`list(filtros)`, porque as duas variantes do toggle são **duas listas distintas no cache** — colidi-las
mostraria a lista errada ao alternar o switch, sem refetch e sem erro visível.

**`useConfigSlaDados.ts`** — molde verbatim de `useConfigSlaRevisao`: `retry: false`, `staleTime` =
`gcTime`, chave `configSla()`. O docblock carrega a justificativa, que não é estilística: repetir a
leitura de uma configuração que falhou não muda o desenlace, porque o consumidor **já tem** uma
apresentação correta para "sem configuração" — a faixa degenerada. Cada retry só adia essa
apresentação enquanto a fila inteira já está legível ao lado. E como o serviço resolve erro para
`null`, este hook **nunca fica em `isError`** — é o que permite ao 44-09 manter a config fora do
carregamento e do erro da tabela (E6/loading).

**`usePedidosDadosPendentesCount.ts`** — molde verbatim de `useRevisoesPendentesCount`: `retry: 2`,
chave `pendentesCount()`. ⚠ O docblock repete a proibição do análogo, que é mecânica: **o consumidor
nunca renderiza o número cru**. Em JS `0 && …` curto-circuita para `0` e o React renderiza `0` como
TEXTO — um "0" solto no menu. Quem traduz é `formatarBadgePendentes`, **importada** de
`revisaoService` no 44-09, e ela devolve `undefined` (não string vazia) para zero/ausente. Escreva o
valor de retorno correto no docblock: uma versão anterior da UI-SPEC citou string vazia, e uma
comparação com string vazia seria sempre falsa e faria o badge reaparecer como pílula em branco.

⚠ **Nenhum dos três hooks impõe escopo, filtro ou ordem.** O invariante do BD-8 é de **servidor**: as
duas RPCs compartilham o predicado. Um hook que "melhorasse" a fila com um filtro local reintroduziria
exatamente a divergência que o BD-8 nomeia — e o contador continuaria contando o que a tela deixou de
mostrar. Registre isso no docblock do hook primário.
  </action>
  <acceptance_criteria>
- `npx vitest run src/features/pedidos-dados/` — verde, cobrindo (bj)–(bl) além das Tasks 1 e 2
- Existe **uma** fábrica de chaves na feature: `grep -crE 'Keys = \{' src/features/pedidos-dados/hooks/` devolve 1
- As políticas de retry são as do molde: `grep -c 'retry: false' src/features/pedidos-dados/hooks/useConfigSlaDados.ts` devolve 1 · `grep -c 'retry: 2' src/features/pedidos-dados/hooks/useFilaPedidosDados.ts` devolve 1 · `grep -c 'retry: 2' src/features/pedidos-dados/hooks/usePedidosDadosPendentesCount.ts` devolve 1
- Os hooks não impõem escopo: `grep -crE '\.filter\(|\.sort\(|\.slice\(' src/features/pedidos-dados/hooks/` devolve 0
- O filtro participa da chave — provado pelo teste (bj), não por leitura
- `npm run test:run` verde · `npm run lint` sem regressão da baseline 97 · **zero `--no-verify`**
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/features/pedidos-dados/ && [ "$(grep -crE 'Keys = \{' src/features/pedidos-dados/hooks/)" = "1" ] && [ "$(grep -crE '\.filter\(|\.sort\(|\.slice\(' src/features/pedidos-dados/hooks/)" = "0" ] && npm run test:run && npm run lint</automated>
  </verify>
  <done>Os três hooks existem no molde dos irmãos, com uma única fábrica de chaves hospedada no hook primário, o filtro dentro da chave da lista, `retry: false` na config com a razão escrita, `retry: 2` nos outros dois, e nenhum filtro, ordenação ou corte do lado do cliente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC `SECURITY DEFINER` → linha no cache do cliente | O escopo do BD-8 é do servidor; o cliente só pode **estreitar errado** (filtro local) ou **alargar** (leitura PostgREST paralela) |
| `RETURNS TABLE` do servidor → tela e cache | Uma coluna acrescentada no servidor trafega sem que nada reclame; a allowlist do cliente é a fronteira que a descarta |
| erro cru do transporte → tela do RH | Mensagem de banco carrega SQLSTATE, nome de tabela e detalhe de infraestrutura |
| token de `causa` → coluna "O que aconteceu" | O token nomeia o caminho de falha interno; cru na tela é detalhe de infraestrutura |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-09 | Information Disclosure | divergência fila × contador (BD-8) | high | mitigate | As duas leituras são RPC e **nenhuma** carrega filtro, ordenação ou corte de cliente; teste (bd) assere que nenhuma tabela foi tocada por PostgREST, e o gate de hooks reprova `filter`/`sort`/`slice`. A igualdade é provada no servidor pelo smoke (m) do 44-02 sob impersonação real |
| T-44-12 | Information Disclosure | superfície da linha da fila | high | mitigate | `FILA_PEDIDOS_DADOS_COLUNAS` de sete colunas + `projetarLinhaPedido` na fronteira; teste (ba) prova que coluna extra da resposta não atravessa, (bf) prova a ausência da projeção total com literal montado em runtime |
| T-44-35 | Information Disclosure | limiares de acompanhamento vazando para o candidato | medium | mitigate | `lerConfigSlaDados` nasce na feature do RH e não é importado por `src/features/privacidade/**`; o controle real é a policy RH-only lida no M3 (44-04), não a ausência de import (Invariante 8 da UI-SPEC) |
| T-44-36 | Information Disclosure | erro cru do banco na tela do RH | medium | mitigate | `classificarErroPedidosDados` total, vocabulário de dois valores; teste (bh) assere que o texto do transporte não aparece no erro devolvido |
| T-44-37 | Information Disclosure | token de `causa` cru na coluna | medium | mitigate | `traduzirCausa` total sobre vocabulário fechado, fallback **Motivo não registrado.**; teste (bi) prova que o token desconhecido não aparece no retorno — divergência deliberada de `rotularDecisao`, escrita no docblock |
| T-44-38 | Repudiation | fila que mente por omissão | medium | mitigate | O cliente não reordena e não repagina: a ordenação composta e o `LIMIT 200` são contrato do servidor, e é a ordenação que torna verdadeira a copy do aviso de corte (44-09) |
</threat_model>

<verification>
- `npx vitest run src/features/pedidos-dados/` verde (constantes + serviço + chaves)
- `npm run test:run` verde · `npm run lint` sem regressão da baseline 97 · **zero `--no-verify`**
- `git diff --exit-code package.json` — **zero dependência npm nova** nesta fase
- **Nenhum arquivo fora de `src/features/pedidos-dados/` é tocado neste plano** — rota, menu e
  componentes são o 44-09. `git diff --name-only` confirma
</verification>

<success_criteria>
- A camada de dados de `/rh/pedidos-dados` existe e lê exclusivamente as duas RPCs do BD-8
- A identidade de referência `classifySlaDados === classifyRevisaoSla` é asserida e verde
- A projeção é allowlist nomeada de sete colunas, provada por teste, com o tipo da linha escrito à mão
- A inversão do toggle vive num único ponto e está prendida por teste nos dois sentidos
- Config ilegível resolve para `null` e causa desconhecida resolve para "Motivo não registrado." —
  nenhum dos dois vira erro de tela nem célula em branco
</success_criteria>

<output>
Create `.planning/phases/44-exporta-o-acesso/44-08-SUMMARY.md` when done
</output>
</content>
</invoke>
