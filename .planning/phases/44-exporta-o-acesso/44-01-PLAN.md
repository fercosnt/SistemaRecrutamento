---
phase: 44-exporta-o-acesso
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - docs/compliance/export-scope-rules.yaml
  - docs/compliance/sql/gen-export-allowlist.cjs
  - docs/compliance/__tests__/genExportAllowlist.test.ts
autonomous: true
requirements: [EXPORT-02, EXPORT-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Existe um arquivo versionado que responde, tabela a tabela, a pergunta que o `pii-inventory.yaml` NÃO responde: esta tabela é dado do titular sob o Art. 18, II? (EXPORT-02, Achado #3 da pesquisa)"
    - "O gerador da allowlist lê TRÊS fontes — catálogo vivo (existência), `pii-inventory.yaml` (classificação) e `export-scope-rules.yaml` (escopo do titular) — e uma coluna viva que nenhuma das três resolve FALHA a geração com o nome da coluna no stderr, nunca sai em silêncio (BD-6)."
    - "O gerador tem modo `--check` que sai 1 quando qualquer um dos artefatos gerados diverge das fontes, no idioma vivo de `gen-pii-md.cjs` (EXPORT-06)."
    - "`js-yaml` deixou de ser dependência-fantasma: está declarado em `devDependencies` e o gerador novo usa `safeLoad`, não `load`."
    - "A linha literal de `exclude` da EF nova já existe em `vite.config.ts` ANTES de o teste Deno da EF ser criado — a árvore nunca fica vermelha entre dois commits."
  artifacts:
    - docs/compliance/export-scope-rules.yaml
    - docs/compliance/sql/gen-export-allowlist.cjs
    - docs/compliance/__tests__/genExportAllowlist.test.ts
  key_links:
    - "`gen-export-allowlist.cjs` → `docs/compliance/pii-inventory.yaml` (classificação) — a decisão da Área 2 do CONTEXT continua valendo; o que muda é que o YAML deixa de ser fonte ÚNICA (BD-6)."
    - "`gen-export-allowlist.cjs` → `docs/compliance/catalogo-vivo-44.json` (existência de coluna) — o arquivo é escrito pelo orquestrador no plano 44-03; sem ele o gerador falha alto, e é isso que impede uma allowlist derivada de documento datado."
    - "`vite.config.ts#exclude` → `supabase/functions/exportar-meus-dados/**/*.test.ts` — a linha nasce aqui e é RE-ASSERIDA no 44-05 antes de o teste existir."
---

<objective>
Os dois insumos de decisão da allowlist nascem antes de qualquer linha de export: o arquivo que
declara o escopo do titular (a pergunta que o inventário da Phase 42 não responde) e o gerador que
o funde com o catálogo vivo e com a classificação do YAML — falhando alto quando os três não
fecham.

Purpose: BD-6 mediu que o `pii-inventory.yaml` está 5 dias atrasado e que as QUATRO colunas de
consentimento versionado que o ROADMAP nomeia como a dependência da Phase 44 sobre a Phase 43
aparecem **zero** vezes nele. Derivar a allowlist do YAML sozinho produziria um export que omite em
silêncio a própria dependência declarada da fase. Este plano constrói o mecanismo que torna essa
omissão **impossível**: um erro de fechamento que para a geração.

Output: `export-scope-rules.yaml`, `gen-export-allowlist.cjs` com `--check`/`--sql-values`/`--emit-ts`,
seu teste de unidade, `js-yaml` promovido a devDependency e a linha de `exclude` da EF nova.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-exporta-o-acesso/44-CONTEXT.md
@.planning/phases/44-exporta-o-acesso/44-MEASUREMENTS.md
@.planning/phases/44-exporta-o-acesso/44-RESEARCH.md
@.planning/phases/44-exporta-o-acesso/44-PATTERNS.md
@.planning/phases/44-exporta-o-acesso/44-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
Roster completo da Phase 44 (todo símbolo criado). Os itens **em negrito** nascem NESTE plano.

**Compliance / artefatos**
- **`docs/compliance/export-scope-rules.yaml`** — decisão "é dado do titular?"
- **`docs/compliance/sql/gen-export-allowlist.cjs`** — gerador (`--check`, `--sql-values`, `--emit-ts`)
- **`docs/compliance/__tests__/genExportAllowlist.test.ts`** — teste do fecho BD-6
- `docs/compliance/catalogo-vivo-44.json` — medição M2 (44-03, orquestrador)
- `docs/compliance/export-allowlist.json` — o artefato do SC#5 (44-03)
- `supabase/functions/_shared/exportAllowlist.ts` — espelho TS do artefato (44-03)
- `docs/compliance/__tests__/exportAllowlist.test.ts` — snapshot inline + negativas (44-03)
- `docs/compliance/sql/05-export-allowlist-drift.sql` — smoke SQL do SC#3 (44-03)

**Banco**
- `public.config_sla_dados` (tabela) · `public.solicitacoes_dados` (tabela) — 44-02
- `public.listar_pedidos_dados()` · `public.contar_pedidos_dados_pendentes()` (RPC) — 44-02
- `supabase/tests/p44_pedidos_dados_smoke.sql` — 44-02
- migrations `20260804000001_p44_config_sla_dados.sql`, `20260804000002_p44_solicitacoes_dados.sql`

**Edge Function**
- `exportar-meus-dados` (`supabase/functions/exportar-meus-dados/index.ts` + `__tests__/index.test.ts`) — 44-05

**Candidato (`src/features/privacidade/`)**
- `PedirCopiaBloco` · `CurriculosBloco` (components) — 44-05/44-06/44-07
- `useExportarMeusDados` · `useUltimoPedidoDados` (hooks) — 44-05/44-06
- `exportacaoService.ts`: `invocarExportMeusDados`, `gerarJsonExport`, `gerarHtmlExport`,
  `dispararDownloads`, `lerUltimoPedidoDados`, `mintarUrlCurriculoProprio`, `ExportacaoError`
- chaves de query: entradas novas em `privacidadeKeys` (`ultimoPedido`, `curriculos`)

**RH (`src/features/pedidos-dados/`)**
- `PedidosDadosRHPage` · `FilaPedidosDadosTable` · `SituacaoPedidoBadge` — 44-09
- `useFilaPedidosDados` · `useConfigSlaDados` · `usePedidosDadosPendentesCount` — 44-08
- `pedidosDadosService.ts`: `PedidosDadosError`, `FILA_PEDIDOS_DADOS_COLUNAS`, `listarPedidosDados`,
  `contarPedidosDadosPendentes`, `lerConfigSlaDados`, `traduzirCausa` — 44-08
- `constants/slaDados.ts`: re-exports `classifySlaDados`, `diasEmEspera`, `ROTULOS_FAIXA_SLA_DADOS`
- `pedidosDadosKeys` (fábrica de chaves, dentro de `useFilaPedidosDados.ts`)
- rota `/rh/pedidos-dados` · item de menu `pedidos-dados-rh`

**Arquivos editados**
- **`package.json` / `package-lock.json`** (js-yaml) · **`vite.config.ts`** (exclude)
- `src/features/privacidade/components/PrivacidadeCandidatoPage.tsx` (seção 3)
- `src/features/revisao/components/RevisaoSlaBadge.tsx` (docblock: 2º consumidor)
- `src/components/RHSidebar.tsx` (3 sítios) · `src/router/routes.tsx` (1 rota)
- `database.types.ts` (regenerado, 44-04)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Os dois portões de configuração — a dependência-fantasma e a linha de exclude que precede o teste</name>
  <files>package.json, package-lock.json, vite.config.ts</files>
  <read_first>
- `package.json` — o bloco `devDependencies` a partir da linha 67 (idioma de versionamento vivo)
- `vite.config.ts` linhas 1–90 — o bloco `test:`, o `include` e as 15+ entradas LITERAIS de `exclude`, cada uma com seu comentário de fase
- `docs/compliance/sql/gen-pii-md.cjs` linhas 19–25 — o `require('js-yaml')` que hoje resolve por hoisting
- `44-RESEARCH.md` §Achado #2 — a medição de que `grep -n "js-yaml" package.json` devolve 0 linhas
- `44-VALIDATION.md` §Duas armadilhas de configuração
  </read_first>
  <action>
**(a) `js-yaml` promovido a devDependency explícita.** Hoje ele existe em `node_modules` na
versão 3.14.2 apenas por hoisting de `@testing-library/dom`; um bump futuro daquele pacote mata o
`gen-pii-md.cjs` da Phase 42 e o gerador desta fase **juntos** — e mata o `--check` no mesmo golpe,
que é o modo de falha em que o guarda morre com aquilo que ele guardava.

Antes de pinar, execute `npm view js-yaml version` e registre a saída no SUMMARY. Não há dúvida
sobre a legitimidade do pacote (a `44-RESEARCH.md` §Package Legitimacy Audit já o classificou
`[OK]`, >10 anos, dezenas de milhões de downloads/semana, `github.com/nodeca/js-yaml`); o comando
existe porque o ritual é o que mantém o portão vivo. Instale com
`npm install --save-dev js-yaml@^3.14.2` — a MESMA major já presente na árvore. Não suba para 4.x:
a API muda (`safeLoad` some) e o `gen-pii-md.cjs` vivo quebraria.

**(b) A linha de `exclude` da EF nova, ANTES do teste que a exige.** Acrescente ao array `exclude`
do bloco `test:` do `vite.config.ts`, no idioma literal das entradas vizinhas (nunca glob de
diretório), a entrada `supabase/functions/exportar-meus-dados/**/*.test.ts`, precedida de um
comentário no estilo das outras: Phase 44, a EF importa de `esm.sh` e roda sob `deno test`, e o
teste dela mora em `__tests__/` — que é justamente o padrão que o `include` do Vitest coleta.

A ordem importa e é o oposto da intuição. A restrição da `44-VALIDATION.md` é "a linha entra no
MESMO commit que o teste"; o que ela protege é a árvore vermelha entre dois commits. Uma entrada de
`exclude` apontando para um caminho que ainda não existe é **no-op** — inofensiva. Uma entrada que
chega DEPOIS do teste é `npm run test:run` vermelho. Logo a única ordem segura é esta: a linha
primeiro, o teste depois (o 44-05 re-assere a presença da linha antes de criar o arquivo de teste).

Não toque em nenhuma outra entrada do array, nem no `include`, nem no `resolve.alias`.
  </action>
  <acceptance_criteria>
- `node -e "const p=require('./package.json'); if(!p.devDependencies['js-yaml']) process.exit(1)"` sai 0
- A major instalada continua sendo 3: `node -e "const v=require('js-yaml/package.json').version; process.exit(v.startsWith('3.')?0:1)"` sai 0
- `grep -c "supabase/functions/exportar-meus-dados/\*\*/\*.test.ts" vite.config.ts` devolve 1
- A entrada é literal, não glob de diretório: `grep -c "supabase/functions/\*\*/\*.test.ts" vite.config.ts` devolve 0
- `npm run test:run` continua verde (a entrada nova é no-op enquanto o diretório não existe)
- `npm run lint` não regride a baseline de 97
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json');if(!p.devDependencies||!p.devDependencies['js-yaml'])process.exit(1)" && grep -c "exportar-meus-dados/\*\*/\*.test.ts" vite.config.ts && npm run test:run</automated>
  </verify>
  <done>`js-yaml` está em `devDependencies` na major 3, a versão consultada no registry está no SUMMARY, a linha literal de `exclude` da EF nova existe em `vite.config.ts` com comentário de fase, e a suíte continua verde.</done>
</task>

<task type="auto">
  <name>Task 2: `export-scope-rules.yaml` — a pergunta que o inventário da Phase 42 não responde</name>
  <files>docs/compliance/export-scope-rules.yaml</files>
  <read_first>
- `docs/compliance/pii-inventory.yaml` linhas 1–90 — bloco `meta`, `classificacoes` e `regras_padrao` R1–R5 (o idioma de cobertura auditável a espelhar)
- `docs/compliance/pii-inventory.yaml` linhas 87–160 — as entradas de `candidatos`, `candidaturas`, `autorizacoes` (forma de entrada por coluna)
- `docs/compliance/pii-inventory.yaml` linhas 400–460 — `usuarios_rh`, `preferencias_notificacoes`, `configuracoes_empresa`, `webhooks_config` e o achado A-05
- `44-RESEARCH.md` §Achado #3 e §Pattern 2 — por que classificação ≠ escopo do titular, e a forma proposta
- `44-CONTEXT.md` §Área 2 — o escopo travado pelo operador e a exclusão nomeada de `ai_call_logs`
- `44-MEASUREMENTS.md` §M2 — as contagens vivas por tabela candidata
  </read_first>
  <action>
Crie o arquivo com cabeçalho em comentário declarando: `Requirement: EXPORT-02` · fase 44 ·
`Decisão de origem: 44-CONTEXT §Área 2 + BD-6` · e a frase que justifica o arquivo existir —
o `pii-inventory.yaml` responde **o que a exclusão faz com esta coluna** (vocabulário
`apagar`/`anonimizar`/`preservar`/`preservar_com_ressalva`), e a Phase 44 precisa de outra
resposta: **isto é dado do titular sob o Art. 18, II?**. Os eixos não coincidem, e três famílias
provam: `autorizacoes.autorizacao_uso_dados` é `preservar` **e é** do titular;
`configuracoes_empresa.smtp_senha_encrypted` é `preservar` **e é segredo**; `usuarios_rh.*` é PII
de funcionário, que o próprio YAML declara fora do escopo do direito do titular candidato.

Blocos obrigatórios:

1. **`meta`** — `requirement`, `fase`, `plano`, `fonte` (prosa acima), `consumidores` declarando
   Phase 44 (projeção da EF) e Phase 45 (plano de exclusão), no mesmo padrão do `pii-inventory.yaml`.

2. **`escopo_titular`** — mapa `tabela → { chave_titular, ligacao, razao }`. `chave_titular` é a
   coluna que amarra a linha ao titular, e `ligacao` é `direta` (a coluna é `candidato_id`/`id`) ou
   `via: <tabela>` quando o vínculo é indireto (`historico_candidatura` liga por `candidatura_id`).
   **Este mapa é dado, nunca inferência do código** — a Task 3 obriga o gerador a validá-lo contra
   o catálogo vivo. Semente a partir da Área 2 do CONTEXT (cadastro, candidaturas, autorizações,
   entrevistas agendadas, histórico de cada candidatura, e o resultado e a explicação das
   avaliações) e das tabelas com entrada explícita no `pii-inventory.yaml`. Toda entrada carrega
   `razao` em uma linha — a razão é o que a §Copywriting da UI-SPEC promete ao titular na frase "O
   que está na cópia", e as duas têm de continuar batendo.

3. **`fora_do_escopo`** — mapa `tabela → razao_nomeada`, com o vocabulário fechado
   `telemetria_interna` · `pii_de_terceiro` · `segredo` · `configuracao_do_produto` ·
   `vocabulario_do_sistema`. `ai_call_logs: telemetria_interna` é decisão travada da Área 2 e da
   §Deferred. `usuarios_rh` e `preferencias_notificacoes` são `pii_de_terceiro` — o M2 mediu que a
   segunda chaveia por `usuario_rh_id`, não por candidato, e é o exemplo vivo de um `preservar` que
   é PII de outra pessoa. `configuracoes_empresa` e `webhooks_config` são `segredo` (achado A-05 do
   próprio inventário).

4. **`fora_do_escopo_por_regra`** — lista de `{ id, padrao, razao }` para as famílias que não
   merecem uma linha cada: tabelas de configuração (`config_*`), de vocabulário
   (`classe_evento_notificacao`), de log de infraestrutura. Cada regra carrega `razao`; uma regra
   sem razão é a mesma omissão que este arquivo existe para impedir. O M1 mediu 3 tabelas novas
   ausentes do YAML (`config_sla_revisao`, `config_retencao_etapa`, `classe_evento_notificacao`) —
   nenhuma é PII do titular, e são exatamente o caso que este bloco resolve sem inflar o arquivo.

5. **`colunas_nunca`** — veto que atravessa QUALQUER tabela, mesmo em escopo:
   `smtp_senha_encrypted`, `webhook_secret`, `secret`, `session_token`, `hash_cpf_email`. É o veto
   que sobrevive a alguém pôr uma tabela nova em `escopo_titular` sem ler o que há dentro dela.

6. **`decisoes_por_coluna`** — mapa `tabela.coluna → { export: true|false, razao }` para as colunas
   que nem o `pii-inventory.yaml` nem as regras resolvem. As QUATRO colunas de `autorizacoes` que o
   BD-6 mediu com zero ocorrências no YAML (`consent_text_version`, `consent_text_hash`,
   `consent_registrado_em`, `autorizacao_marketing_vagas`) entram aqui com `export: true` e razão
   citando o ROADMAP §Phase 44 *Depends on* — são a dependência declarada da fase sobre a Phase 43,
   e é literalmente por elas que este bloco existe. `candidaturas.opcao_knockout_id`, a 5ª coluna
   nova medida, também precisa de veredito explícito.

7. **`regra_de_fecho`** — em prosa, dentro do arquivo: **toda tabela viva do schema `public` tem de
   cair em `escopo_titular`, em `fora_do_escopo` ou em uma regra de `fora_do_escopo_por_regra`; e
   toda coluna viva de tabela em escopo tem de ter veredito por `decisoes_por_coluna`, por entrada
   explícita no `pii-inventory.yaml` ou por regra R1–R5. Qualquer sobra é ERRO DE FECHAMENTO que
   falha a geração.** Este parágrafo é o contrato que a Task 3 implementa.
  </action>
  <acceptance_criteria>
- `node -e "const y=require('js-yaml');const fs=require('fs');const d=y.safeLoad(fs.readFileSync('docs/compliance/export-scope-rules.yaml','utf8'));for(const k of ['meta','escopo_titular','fora_do_escopo','fora_do_escopo_por_regra','colunas_nunca','decisoes_por_coluna'])if(!d[k])throw new Error('bloco ausente: '+k)"` sai 0
- Toda entrada de `escopo_titular` tem `chave_titular` e `razao` não-vazios (asserção no teste da Task 3)
- Toda entrada de `fora_do_escopo` tem razão do vocabulário fechado de 5 valores
- As 4 colunas do BD-6 aparecem em `decisoes_por_coluna` com `export: true`: `grep -c "consent_text_version\|consent_text_hash\|consent_registrado_em\|autorizacao_marketing_vagas" docs/compliance/export-scope-rules.yaml` devolve 4 ou mais
- `ai_call_logs` aparece em `fora_do_escopo` com a razão `telemetria_interna`
- `meta.consumidores` cita a Phase 45
  </acceptance_criteria>
  <verify>
    <automated>node -e "const y=require('js-yaml'),fs=require('fs');const d=y.safeLoad(fs.readFileSync('docs/compliance/export-scope-rules.yaml','utf8'));['meta','escopo_titular','fora_do_escopo','fora_do_escopo_por_regra','colunas_nunca','decisoes_por_coluna'].forEach(k=>{if(!d[k])throw new Error('bloco ausente: '+k)});if(d.fora_do_escopo.ai_call_logs!=='telemetria_interna')throw new Error('ai_call_logs');if(!JSON.stringify(d.meta.consumidores).includes('Phase 45'))throw new Error('consumidores')"</automated>
  </verify>
  <done>O arquivo existe com os sete blocos; as quatro colunas medidas pelo BD-6 têm veredito explícito de inclusão citando o ROADMAP; `ai_call_logs`, `usuarios_rh`, `preferencias_notificacoes`, `configuracoes_empresa` e `webhooks_config` têm razão nomeada de exclusão; a regra de fecho está escrita como contrato do gerador.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `gen-export-allowlist.cjs` — três entradas, fecho obrigatório, `--check` que morde</name>
  <files>docs/compliance/sql/gen-export-allowlist.cjs, docs/compliance/__tests__/genExportAllowlist.test.ts</files>
  <read_first>
- `docs/compliance/sql/gen-pii-md.cjs` (125 linhas, INTEIRO) — a casca reusável: `require`s do topo, o bloco `--check` das linhas 113–125 com `process.exit(1)` na divergência, e o `fs.writeFileSync(OUT, out)` final
- `docs/compliance/export-scope-rules.yaml` — o arquivo escrito na Task 2 (as três entradas do gerador)
- `docs/compliance/pii-inventory.yaml` §`regras_padrao` (linhas 48–80) — as cinco regras R1–R5 que o gerador tem de aplicar programaticamente
- `44-RESEARCH.md` §Pattern 2 e §E2 — a forma do JSON de saída e por que o espelho `.ts` é mais seguro que import de JSON na EF (A1)
- `44-MEASUREMENTS.md` §Regra de honestidade de número — todo número carrega `medido_em`
  </read_first>
  <behavior>
Testes a escrever ANTES da implementação, em `docs/compliance/__tests__/genExportAllowlist.test.ts`.
Cada teste monta um diretório temporário com fixtures mínimas (catálogo, inventário, scope-rules) e
executa o gerador por `child_process.execFileSync`, asserindo código de saída e stderr:

- **(a) Caminho feliz:** fixtures que fecham → exit 0, o `.json` sai com `meta.medido_em` copiado do
  catálogo e `tabelas` só com as colunas em escopo.
- **(b) Coluna viva sem veredito → ERRO DE FECHAMENTO (BD-6):** o catálogo traz
  `autorizacoes.consent_text_version`, o inventário não a menciona, o scope-rules não a decide, e
  nenhuma regra R1–R5 casa → exit 1 e o stderr **nomeia a coluna**. Este é o teste que impede
  literalmente o defeito que o BD-6 mediu.
- **(c) Tabela viva sem disposição → ERRO DE FECHAMENTO:** o catálogo traz uma tabela ausente de
  `escopo_titular`, de `fora_do_escopo` e de toda regra → exit 1 nomeando a tabela.
- **(d) `chave_titular` inexistente no catálogo → exit 1**: o mapa de chaves é dado, e um dado
  errado tem de parar a geração em vez de produzir uma leitura vazia em runtime.
- **(e) Veto de `colunas_nunca` atravessa o escopo:** uma coluna `session_token` numa tabela EM
  escopo não entra no `.json`, mesmo que o inventário a classifique como `preservar`.
- **(f) `--check` sai 1 quando o `.json` de disco diverge da geração, e sai 0 quando bate.**
- **(g) `--check` cobre TAMBÉM o espelho `.ts`:** apagar/alterar `_shared/exportAllowlist.ts` sem
  regenerar faz o `--check` sair 1. Um `--check` que olha só um dos dois artefatos deixa o outro
  apodrecer.
- **(h) Catálogo ausente → exit 1 com mensagem citando o caminho esperado.** É o que impede uma
  allowlist derivada de documento datado quando o 44-03 ainda não rodou.
- **(i) `--sql-values` imprime pares `('tabela','coluna')` ordenados e nada mais** — a saída é
  colável no `VALUES` do smoke SQL sem edição manual.
  </behavior>
  <action>
Escreva o gerador em CommonJS, no idioma de `gen-pii-md.cjs` — mesmo estilo de `require`, mesmas
constantes de caminho no topo, mesmo bloco final de `--check`.

**Três entradas, nomeadas em constantes no topo:**
`CATALOGO = docs/compliance/catalogo-vivo-44.json` (existência de coluna — escrito pelo orquestrador
no 44-03) · `INVENTARIO = docs/compliance/pii-inventory.yaml` (classificação) ·
`ESCOPO = docs/compliance/export-scope-rules.yaml` (escopo do titular).

**Use `yaml.safeLoad`, nunca `yaml.load`.** Em js-yaml 3.x o `load` usa `DEFAULT_FULL_SCHEMA`, que
aceita tipos JavaScript; o `safeLoad` usa o schema seguro. O `gen-pii-md.cjs:110` usa a forma
insegura — **essa linha especificamente não é para ser copiada**. O risco prático é nulo (a entrada
é arquivo do próprio repositório), mas um script de compliance que carrega YAML com schema full é
exatamente o detalhe que uma auditoria futura marca.

**Algoritmo, e o fecho é o coração dele:**

1. Ler as três entradas. Catálogo ausente ou sem `meta.medido_em` → `process.exit(1)` com mensagem
   citando o caminho e o plano que o produz.
2. **Fecho de tabelas:** para cada tabela viva do catálogo, resolver a disposição por
   `escopo_titular` → `fora_do_escopo` → `fora_do_escopo_por_regra`. Sobra → erro de fechamento.
3. **Fecho de colunas:** para cada coluna viva de tabela EM escopo, resolver o veredito nesta
   precedência: `colunas_nunca` (veto absoluto) → `decisoes_por_coluna` → entrada explícita de
   coluna no `pii-inventory.yaml` → regras R1–R5 aplicadas por padrão. Sobra → erro de fechamento
   nomeando `tabela.coluna`. Implemente R1–R5 como funções pequenas e testáveis, com o `id` da
   regra registrado na saída para que a proveniência de cada coluna seja auditável.
4. **Validar `chave_titular`:** cada valor declarado tem de existir como coluna viva daquela
   tabela. Não existe → erro de fechamento.
5. **Emitir `docs/compliance/export-allowlist.json`** com `meta` (`requirement`, `fase`, `versao`
   semântica, `gerado_em`, `medido_em` **copiado do catálogo**, `fonte_catalogo`,
   `fonte_classificacao`, `fonte_escopo`, `consumidores` incluindo a string "Phase 45"), `tabelas`
   (`chave_titular`, `ligacao`, `colunas` ordenadas) e `excluidas` (tabela → razão nomeada).
   Serialize com chaves ordenadas e newline final — determinismo é o que torna o `--check` possível.
6. **Emitir `supabase/functions/_shared/exportAllowlist.ts`** com o mesmo objeto sob
   `export const EXPORT_ALLOWLIST = { … } as const`, mais um docblock declarando que o arquivo é
   **gerado** e que editá-lo à mão é reprovado pelo `--check`. Esta é a saída que a EF importa: o
   import estático de JSON saindo do diretório da função é a assunção A1 da pesquisa e **pode não
   sobreviver ao bundler** do deploy; um módulo TypeScript comum não depende de resolução de JSON.
   Uma fonte só (o gerador), dois artefatos, ambos sob `--check`.
7. **`--check`**: regenera em memória e compara com o conteúdo em disco dos **dois** artefatos; sai
   1 na primeira divergência imprimindo qual arquivo divergiu, sai 0 quando ambos batem — idioma
   literal de `gen-pii-md.cjs:113-125`.
8. **`--sql-values`**: imprime em stdout apenas os pares para o `VALUES` do smoke SQL, ordenados por
   tabela e coluna. Nenhum outro texto — a saída é para ser colada.

O gerador **não** consulta o banco e **não** aceita credencial. A medição do catálogo é trabalho do
orquestrador (subagentes GSD não recebem os tools MCP do Supabase — anthropics/claude-code#13898), e
o arquivo medido é a fronteira entre os dois mundos.
  </action>
  <acceptance_criteria>
- `npx vitest run docs/compliance/__tests__/genExportAllowlist.test.ts` — 9 testes verdes, cobrindo (a)–(i)
- O gerador usa o carregador seguro: `grep -c "safeLoad" docs/compliance/sql/gen-export-allowlist.cjs` >= 1 e `grep -cE "yaml\.load\(" docs/compliance/sql/gen-export-allowlist.cjs` devolve 0
- Sem o catálogo presente, `node docs/compliance/sql/gen-export-allowlist.cjs` sai 1 (não 0, não crash sem mensagem)
- O gerador não abre conexão de banco: `grep -cE "\b(pg|postgres|DATABASE_URL|SUPABASE_SERVICE)" docs/compliance/sql/gen-export-allowlist.cjs` devolve 0
- `npm run test:run` verde; `npm run lint` sem regressão da baseline 97
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run docs/compliance/__tests__/genExportAllowlist.test.ts</automated>
  </verify>
  <done>O gerador existe com as três entradas e o fecho obrigatório; uma coluna viva sem veredito e uma tabela viva sem disposição falham a geração nomeando o objeto; `--check` cobre os dois artefatos; `--sql-values` imprime pares coláveis; os 9 testes passam e a suíte completa segue verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catálogo vivo (PROD) → repositório | O arquivo medido pelo orquestrador é a única ponte; nenhum script desta fase conecta ao banco |
| `pii-inventory.yaml` → allowlist | Documento datado atravessando para um artefato que autoriza leitura de PII |
| `node_modules` (hoisting) → pipeline de compliance | Uma dependência não declarada é uma dependência que some sem aviso |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-07 | Information Disclosure | `gen-export-allowlist.cjs` | high | mitigate | `colunas_nunca` como veto absoluto que precede toda outra resolução; teste (e) prova que o veto atravessa uma tabela em escopo |
| T-44-08 | Information Disclosure | fecho do gerador (BD-6) | high | mitigate | Coluna viva sem veredito = erro de fechamento com a coluna nomeada no stderr; teste (b) reproduz exatamente a medição do BD-6 |
| T-44-13 | Repudiation | proveniência do artefato | medium | mitigate | `meta.medido_em` copiado do catálogo, nunca gerado pelo script; catálogo ausente ⇒ exit 1 |
| T-44-SC | Tampering | `npm install --save-dev js-yaml` | high | mitigate | Pacote `[OK]` na `44-RESEARCH` §Package Legitimacy Audit (>10 anos, github.com/nodeca/js-yaml); `npm view js-yaml version` executado e registrado; major fixada em 3 (4.x remove `safeLoad` e quebraria o `gen-pii-md.cjs` vivo); zero pacote novo |
| T-44-14 | Tampering | `_shared/exportAllowlist.ts` editado à mão | medium | mitigate | `--check` cobre os DOIS artefatos; docblock declara o arquivo como gerado |
</threat_model>

<verification>
- `npm run test:run` verde
- `npm run lint` sem regressão da baseline congelada de 97 (o hook `.husky/pre-commit` morde antes do CI; **zero `--no-verify`**)
- `node docs/compliance/sql/gen-export-allowlist.cjs` sai 1 enquanto `catalogo-vivo-44.json` não existir — e essa é a saída correta neste ponto da fase
- `node docs/compliance/sql/gen-pii-md.cjs --check` continua saindo 0 (a promoção de `js-yaml` não pode quebrar o gerador da Phase 42)
</verification>

<success_criteria>
- `export-scope-rules.yaml` existe com os sete blocos e as 4 colunas do BD-6 com veredito explícito
- `gen-export-allowlist.cjs` falha alto em coluna sem veredito, tabela sem disposição, `chave_titular` inválida e catálogo ausente
- `--check` cobre `export-allowlist.json` **e** `_shared/exportAllowlist.ts`
- `js-yaml` declarado em `devDependencies` na major 3
- A linha literal de `exclude` da EF nova existe em `vite.config.ts`
</success_criteria>

<output>
Create `.planning/phases/44-exporta-o-acesso/44-01-SUMMARY.md` when done
</output>
