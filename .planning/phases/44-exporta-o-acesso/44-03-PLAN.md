---
phase: 44-exporta-o-acesso
plan: 03
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - docs/compliance/catalogo-vivo-44.json
  - docs/compliance/export-allowlist.json
  - supabase/functions/_shared/exportAllowlist.ts
  - docs/compliance/__tests__/exportAllowlist.test.ts
  - docs/compliance/sql/05-export-allowlist-drift.sql
autonomous: false
requirements: [EXPORT-02, EXPORT-04, EXPORT-06]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Existe um artefato nomeado e versionado, `docs/compliance/export-allowlist.json`, que declara coluna a coluna o que entra na cópia do titular — e ele declara a Phase 45 como consumidora, para que a fase irreversível consuma um levantamento já exercitado em vez de refazê-lo (EXPORT-06, SC#5)."
    - "Todo número do artefato carrega `medido_em`, copiado da medição do catálogo vivo — nenhum número vem de documento datado (regra de honestidade herdada de `04-invent05-blast-radius.sql:30-40`)."
    - "As quatro colunas de consentimento versionado da Phase 43 (`consent_text_version`, `consent_text_hash`, `consent_registrado_em`, `autorizacao_marketing_vagas`) ESTÃO na allowlist — a dependência que o ROADMAP declara entre a Phase 44 e a Phase 43 é verificável no artefato, não prometida em prosa (BD-6)."
    - "Adicionar uma coluna nova ao banco faz o smoke SQL devolver ≥1 linha; remover uma coluna que a allowlist declara faz o mesmo — as duas direções, por razões diferentes (EXPORT-04, SC#3 asserção 2)."
    - "Alterar as chaves do export faz o CI falhar pelo snapshot inline; e mesmo um `-u` distraído não desarma a rede, porque a asserção negativa de segredos e telemetria não é um snapshot (EXPORT-04, SC#3 asserção 1)."
    - "O gate do smoke SQL foi visto FALHANDO pelo motivo declarado, com o par antes/depois colado no VERIFICATION — um gate que nunca foi visto falhando não é um gate."
    - "Nenhum segredo do schema `public` atravessou a allowlist, e `ai_call_logs` está fora com razão nomeada — asserção negativa nomeada, não confiança no gerador."
  artifacts:
    - docs/compliance/catalogo-vivo-44.json
    - docs/compliance/export-allowlist.json
    - supabase/functions/_shared/exportAllowlist.ts
    - docs/compliance/__tests__/exportAllowlist.test.ts
    - docs/compliance/sql/05-export-allowlist-drift.sql
  key_links:
    - "`catalogo-vivo-44.json` → `gen-export-allowlist.cjs` → `export-allowlist.json` + `_shared/exportAllowlist.ts`: uma fonte medida, um gerador, dois artefatos, ambos sob `--check`."
    - "`_shared/exportAllowlist.ts` → import estático da EF `exportar-meus-dados` (44-05) — é o que resolve a assunção A1 sem depender de resolução de JSON pelo bundler do deploy."
    - "`05-export-allowlist-drift.sql` ⋈ `information_schema.columns`: o Vitest lê o ARTEFATO, este lê o CATÁLOGO. São universos disjuntos, e só o segundo enxerga a coluna nova."
---

<objective>
O inventário do export nasce como artefato versionado — gerado, nunca digitado — e ganha os DOIS
guardas que o SC#3 exige: o snapshot que pega alteração silenciosa do artefato e o smoke SQL que
pega a coluna nova no banco. E o segundo é provado **mordendo**.

Purpose: a asserção Vitest sozinha passa para sempre, porque uma coluna nova no banco não move um
byte do JSON commitado — e é literalmente esse o modo de falha que o SC#3 nomeia. Este projeto já
embarcou uma vez a classe "guarda que era dead code" (P39/CR-02); uma asserção que não pode falhar
pelo motivo declarado é a mesma classe de defeito.

Output: o catálogo medido, o `export-allowlist.json`, o espelho `_shared/exportAllowlist.ts`, o
teste Vitest com snapshot inline lido por um humano, e o smoke SQL com a prova de mordida.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-exporta-o-acesso/44-CONTEXT.md
@.planning/phases/44-exporta-o-acesso/44-MEASUREMENTS.md
@.planning/phases/44-exporta-o-acesso/44-RESEARCH.md
@.planning/phases/44-exporta-o-acesso/44-PATTERNS.md
@.planning/phases/44-exporta-o-acesso/44-VALIDATION.md
@.planning/phases/44-exporta-o-acesso/44-01-SUMMARY.md
@docs/compliance/sql/01-pii-catalog.sql
@docs/compliance/sql/04-invent05-blast-radius.sql
</context>

<artifacts_this_phase_produces>
Roster completo da Phase 44. Os itens **em negrito** nascem NESTE plano.

**Compliance / artefatos**
- `docs/compliance/export-scope-rules.yaml` · `docs/compliance/sql/gen-export-allowlist.cjs` (44-01)
- **`docs/compliance/catalogo-vivo-44.json`** — medição M2, escrita pelo orquestrador
- **`docs/compliance/export-allowlist.json`** — o artefato do SC#5
- **`supabase/functions/_shared/exportAllowlist.ts`** — `export const EXPORT_ALLOWLIST`
- **`docs/compliance/__tests__/exportAllowlist.test.ts`**
- **`docs/compliance/sql/05-export-allowlist-drift.sql`**

**Banco** — `config_sla_dados`, `solicitacoes_dados`, `listar_pedidos_dados`,
`contar_pedidos_dados_pendentes`, `idx_solicitacoes_dados_cooldown`,
`supabase/tests/p44_pedidos_dados_smoke.sql` (44-02/44-04)

**Edge Function** — `exportar-meus-dados` (44-05)

**Candidato** — `PedirCopiaBloco`, `CurriculosBloco`, `useExportarMeusDados`, `useUltimoPedidoDados`,
`exportacaoService.ts` (`invocarExportMeusDados`, `gerarJsonExport`, `gerarHtmlExport`,
`dispararDownloads`, `lerUltimoPedidoDados`, `mintarUrlCurriculoProprio`, `ExportacaoError`),
entradas novas em `privacidadeKeys` (44-05/44-06/44-07)

**RH** — `PedidosDadosRHPage`, `FilaPedidosDadosTable`, `SituacaoPedidoBadge`, `useFilaPedidosDados`,
`useConfigSlaDados`, `usePedidosDadosPendentesCount`, `pedidosDadosService.ts`,
`constants/slaDados.ts`, `pedidosDadosKeys`, rota `/rh/pedidos-dados`, item `pedidos-dados-rh`
(44-08/44-09)

**Editados** — `package.json`, `vite.config.ts`, `PrivacidadeCandidatoPage.tsx`,
`RevisaoSlaBadge.tsx`, `RHSidebar.tsx`, `routes.tsx`, `database.types.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: CHECKPOINT — medir o catálogo vivo (M2) e materializá-lo como arquivo datado</name>
  <read_first>
- `docs/compliance/sql/01-pii-catalog.sql` — as consultas (a) e (d), a serem reusadas **sem alterar uma vírgula**
- `docs/compliance/export-scope-rules.yaml` — a lista de tabelas cujo catálogo precisa ser colhido
- `44-MEASUREMENTS.md` §M1/§M2 — os números de 2026-08-03 (67/1013/104) contra os quais o resultado de hoje será comparado
- `44-RESEARCH.md` §Live-Catalogue Measurement Protocol
  </read_first>
  <what-built>
Nada ainda — este é o insumo. O `gen-export-allowlist.cjs` (44-01) **falha alto** enquanto
`docs/compliance/catalogo-vivo-44.json` não existir, exatamente para impedir que a allowlist seja
derivada de um documento datado.
  </what-built>
  <how-to-verify>
Runbook do orquestrador. Subagentes GSD não recebem os tools MCP do Supabase
(anthropics/claude-code#13898), então esta tarefa é do main thread. **Tudo READ-ONLY.**

### 1. Totais de escopo (consulta **(d)** de `01-pii-catalog.sql`, sem alteração)

Registre `tabelas_base_public`, `colunas_public`, `fks_public` e o timestamp UTC do momento.
Compare com o medido em 2026-08-03 (**67 / 1013 / 104**). **Qualquer divergência é dado, não erro** —
anote o delta no SUMMARY. Uma divergência grande significa que outra coisa mexeu no schema desde a
medição, e a allowlist tem de nascer do número de hoje.

### 2. Catálogo coluna-a-coluna das tabelas do escopo (consulta **(a)**, filtrada)

Filtre por `c.table_name = ANY(...)` com a união de `escopo_titular` + `fora_do_escopo` do
`export-scope-rules.yaml`. Colha `table_name`, `column_name`, `data_type`, `is_nullable`,
`ordinal_position`.

### 3. Catálogo de TABELAS de `public` (para o fecho de tabelas)

`SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND
table_type='BASE TABLE' ORDER BY 1;` — o gerador precisa da lista completa para provar que nenhuma
tabela viva ficou sem disposição.

### 4. Escrever `docs/compliance/catalogo-vivo-44.json`

```
{
  "meta": {
    "medido_em": "<timestamp UTC do passo 1, do banco, não do relógio local>",
    "projeto": "isljnozzlvckrgjjbjwp",
    "fonte": "information_schema via execute_sql do MCP, pelo orquestrador",
    "query_reprodutora": "docs/compliance/sql/01-pii-catalog.sql (consultas (a) e (d))",
    "totais": { "tabelas_base_public": N, "colunas_public": N, "fks_public": N }
  },
  "tabelas": ["…"],                                  // passo 3
  "colunas": [{ "tabela": "…", "coluna": "…", "tipo": "…", "nullable": true }]   // passo 2
}
```

`medido_em` sai do **banco** (`now()` na própria consulta), não do relógio da máquina. Um carimbo
que depende de alguém lembrar de anotá-lo é promessa sem código que a execute.

### 5. Conferência obrigatória antes de liberar

- As quatro colunas do BD-6 aparecem no arquivo?
  `consent_text_version`, `consent_text_hash`, `consent_registrado_em`,
  `autorizacao_marketing_vagas` — todas em `autorizacoes`.
  **Se alguma não aparecer, PARE**: ou a Phase 43 não está aplicada em PROD, ou o filtro do passo 2
  está errado. Gerar a allowlist sem elas reproduziria exatamente o defeito que o BD-6 mediu.
- `candidaturas.opcao_knockout_id` aparece? (5ª coluna nova medida no M1.)
- O total de colunas colhidas no passo 2 bate com a soma por tabela do §M2 (`candidatos` 32,
  `candidaturas` 39, `autorizacoes` 16, `agendamentos_entrevista` 15, `historico_candidatura` 8) —
  **ou o delta está explicado por escrito**.

### 6. Rodar o gerador e observar o fecho

`node docs/compliance/sql/gen-export-allowlist.cjs`

**Este comando PODE falhar, e falhar aqui é o comportamento correto.** Cada erro de fechamento
nomeia uma tabela ou uma coluna viva sem veredito. Para cada uma, a decisão é escrita no
`export-scope-rules.yaml` (44-01) — **nunca** afrouxando o gerador. Itere até exit 0. Cole no
SUMMARY a lista de colunas/tabelas que precisaram de decisão nova: ela é a medida exata do drift
que o BD-6 previu.
  </how-to-verify>
  <acceptance_criteria>
- `docs/compliance/catalogo-vivo-44.json` existe com `meta.medido_em` em ISO-8601 UTC e `meta.totais` preenchido
- `node -e "const c=require('./docs/compliance/catalogo-vivo-44.json'); const q=c.colunas.filter(x=>x.tabela==='autorizacoes').map(x=>x.coluna); ['consent_text_version','consent_text_hash','consent_registrado_em','autorizacao_marketing_vagas'].forEach(k=>{if(!q.includes(k))throw new Error('BD-6 ausente: '+k)})"` sai 0
- `node docs/compliance/sql/gen-export-allowlist.cjs` sai 0
- O SUMMARY carrega: os três totais medidos hoje, o delta contra 67/1013/104, e a lista de decisões novas que o fecho exigiu
  </acceptance_criteria>
  <resume-signal>Responda "medido" com os três totais e o `medido_em`, ou descreva o que divergiu.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: O artefato gerado e a asserção (1) do SC#3 — snapshot inline mais a rede que sobrevive a um `-u`</name>
  <files>docs/compliance/export-allowlist.json, supabase/functions/_shared/exportAllowlist.ts, docs/compliance/__tests__/exportAllowlist.test.ts</files>
  <read_first>
- `docs/compliance/sql/gen-export-allowlist.cjs` — o gerador escrito no 44-01 (as flags `--check`, `--emit-ts`, `--sql-values`)
- `docs/compliance/catalogo-vivo-44.json` — a medição da Task 1
- `44-RESEARCH.md` §E4 — o esboço das quatro asserções e por que inline em vez de `.snap`
- `src/features/revisao/services/__tests__/revisaoService.test.ts` linhas 1–40 — o docblock e o idioma de allowlist congelada com asserções negativas nomeadas, uma por chave proibida
- `supabase/functions/_shared/__tests__/strict-schema.test.ts` linhas 30–45 — o idioma de sonda por texto-fonte (`readFileSync` + asserção), o parente mais próximo desta técnica no repo
- `44-VALIDATION.md` §Nota sobre a estreia do snapshot
  </read_first>
  <behavior>
Testes em `docs/compliance/__tests__/exportAllowlist.test.ts`, lendo o artefato por `readFileSync`
(nunca por `import`, para que o teste continue valendo se o formato mudar):

- **(a)** o conjunto de TABELAS do export, ordenado, sob `toMatchInlineSnapshot()`
- **(b)** o conjunto achatado `tabela.coluna`, ordenado, sob `toMatchInlineSnapshot()`
- **(c)** **asserção negativa nomeada, uma por token proibido** — nenhum dos tokens de segredo/
  telemetria aparece em nenhum lugar de `tabelas`. **Os tokens são montados em runtime**
  (`['ai','call','logs'].join('_')`, `['smtp','senha','encrypted'].join('_')`, etc.), no idioma que
  o 42-11 estabeleceu: a asserção fica real e o literal proibido não passa a existir dentro do
  próprio arquivo que o proíbe. Esta é a única asserção do arquivo que **não** é snapshot, e é de
  propósito: ela sobrevive a um `-u` distraído.
- **(d)** as quatro colunas do BD-6 estão presentes em `tabelas.autorizacoes.colunas` — asserção
  **positiva** nomeada, uma por coluna. É a dependência declarada entre a Phase 44 e a Phase 43, e
  ela merece falhar com o nome da coluna no output, não sumir dentro de um diff de snapshot.
- **(e)** `meta.consumidores` contém a string "Phase 45" e `meta.versao` casa `/^\d+\.\d+\.\d+$/`
  (SC#5 em forma executável)
- **(f)** `meta.medido_em` existe, é ISO-8601 parseável, e é **idêntico** ao `meta.medido_em` do
  `catalogo-vivo-44.json` — prova de que o artefato não foi gerado de um catálogo diferente do
  commitado
- **(g)** toda tabela de `tabelas` tem `chave_titular` não-vazio (o mapa é dado; uma chave vazia
  produziria leitura vazia silenciosa em runtime)
- **(h)** o espelho `_shared/exportAllowlist.ts` está em sincronia: o objeto exportado, serializado,
  é igual ao `.json`. Um dos dois apodrecendo em silêncio é o modo de falha de ter dois artefatos
  </behavior>
  <action>
**1. Gerar.** `node docs/compliance/sql/gen-export-allowlist.cjs` produz
`docs/compliance/export-allowlist.json` e `supabase/functions/_shared/exportAllowlist.ts`. Os dois
são **saída de máquina**: nenhuma edição manual, em nenhuma circunstância. Se algo estiver errado,
o conserto é no `export-scope-rules.yaml` ou no gerador, e regera-se.

**2. Escrever o teste** com as oito asserções do bloco `<behavior>`, com um docblock no topo no
idioma de `revisaoService.test.ts:23-27`: por que a lista é congelada, por que as negativas são
nomeadas uma a uma, e — a frase que este arquivo precisa carregar — **por que este teste NÃO
substitui o smoke SQL**: ele lê o artefato commitado, não o catálogo do banco; uma coluna nova em
PROD não move um byte deste JSON.

**3. Preencher o snapshot UMA vez**, com `npx vitest run docs/compliance/__tests__/exportAllowlist.test.ts -u`.
A técnica **estreia neste repositório** — zero ocorrências de `toMatchSnapshot`/`toMatchInlineSnapshot`
hoje. Depois de preencher, **não commite ainda**: a leitura humana do conteúdo gerado é a Task 3.
Um snapshot aceito sem leitura é um contrato que ninguém assinou.

**4. Conferir o `--check`.** `node docs/compliance/sql/gen-export-allowlist.cjs --check` tem de sair
0 com os artefatos recém-gerados. Depois altere uma linha do `.json` à mão, rode de novo, confirme
exit 1 e a mensagem nomeando o arquivo — e **desfaça a alteração**. É a mesma disciplina de prova de
mordida do smoke SQL, aplicada ao guarda barato.

⚠ **`docs/` não está no `include` do `tsconfig.json`** (`["src","e2e","scripts","playwright.config.ts"]`),
então este teste **não** entra na contagem de `tsc --noEmit` e não pode regredir a baseline de 97.
Ele **entra** no `include` do Vitest (`**/__tests__/**`) e roda no CI. Confirme os dois fatos com um
run antes de fechar a tarefa, em vez de assumi-los.
  </action>
  <acceptance_criteria>
- `npx vitest run docs/compliance/__tests__/exportAllowlist.test.ts` — 8 testes verdes
- O snapshot foi materializado, não deixado vazio: `grep -c "toMatchInlineSnapshot(\`" docs/compliance/__tests__/exportAllowlist.test.ts` devolve 2
- Nenhum token proibido aparece literalmente no arquivo de teste (eles são montados em runtime): `grep -cE "'(ai_call_logs|smtp_senha_encrypted|webhook_secret|session_token|hash_cpf_email)'" docs/compliance/__tests__/exportAllowlist.test.ts` devolve 0
- `node docs/compliance/sql/gen-export-allowlist.cjs --check` sai 0
- `node -e "const a=require('./docs/compliance/export-allowlist.json'),c=require('./docs/compliance/catalogo-vivo-44.json'); if(a.meta.medido_em!==c.meta.medido_em) process.exit(1)"` sai 0
- `npm run test:run` verde; `npm run lint` sem regressão da baseline 97
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run docs/compliance/__tests__/exportAllowlist.test.ts && node docs/compliance/sql/gen-export-allowlist.cjs --check</automated>
  </verify>
  <done>Os dois artefatos existem como saída do gerador; o teste tem oito asserções verdes com os dois snapshots inline materializados, as negativas montadas em runtime e as quatro colunas do BD-6 asseridas nominalmente; `--check` sai 0 com os artefatos e 1 com o `.json` adulterado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT — a leitura humana do snapshot, o smoke SQL contra PROD, e a PROVA de que ele morde</name>
  <files>docs/compliance/sql/05-export-allowlist-drift.sql</files>
  <read_first>
- `docs/compliance/sql/04-invent05-blast-radius.sql` linhas 1–45 — o cabeçalho obrigatório do idioma vivo: `Requirement coberto`, `Decisão de origem`, `Milestone`, `Autoria`, `Natureza: READ-ONLY — seguro em PROD`, `COMO EXECUTAR`, e a regra de honestidade de número das linhas 30–40
- `docs/compliance/sql/01-pii-catalog.sql` linhas 1–33 — o segundo exemplar do mesmo cabeçalho
- `scripts/assert-no-secrets.mjs` linhas 33–45 — o bloco `META-TEST — proves this gate is real and not a no-op`, o idioma de prova de mordida deste repositório
- `44-RESEARCH.md` §E5 — o `FULL OUTER JOIN` que falha nos dois sentidos e a razão de cada sentido
- `docs/compliance/export-allowlist.json` — a fonte do `VALUES`
  </read_first>
  <what-built>
O `export-allowlist.json`, o espelho `_shared/exportAllowlist.ts` e o teste Vitest com dois
snapshots inline **preenchidos mas ainda não aprovados**. Falta a leitura humana do conteúdo e o
guarda que enxerga o banco.
  </what-built>
  <how-to-verify>
### A · Leitura humana do snapshot (obrigatória, antes do commit)

Abra `docs/compliance/__tests__/exportAllowlist.test.ts` e leia os DOIS blocos de snapshot linha a
linha. Não é revisão de diff — é a primeira e única vez que um humano assina este contrato.
Perguntas a responder em voz alta:

1. Alguma coluna aqui é **segredo, credencial ou identificador interno de infraestrutura**?
2. Alguma coluna aqui é PII de **outra pessoa** (RH, terceiro citado em campo livre)?
3. Falta alguma coisa que o RH **enxerga** sobre o titular? (A honestidade do export é critério: se
   o sistema guarda e o RH vê, aparece na cópia — o que fica de fora precisa de razão nomeada.)
4. A lista bate com a frase "O que está na cópia" da §Copywriting da 44-UI-SPEC? Se a allowlist
   projeta algo que a tela não menciona, ou a tela promete algo que a allowlist não traz, **uma das
   duas está errada** — e a divergência tem de ser resolvida agora, não descoberta pelo titular.

### B · Escrever `docs/compliance/sql/05-export-allowlist-drift.sql`

Cabeçalho no idioma vivo (blocos obrigatórios do `04-…:1-45`), com estas duas linhas próprias:
`Natureza: READ-ONLY — seguro em PROD. Zero statement de escrita.` e `COMO EXECUTAR: pelo
orquestrador via execute_sql do MCP (subagentes GSD não recebem esses tools —
anthropics/claude-code#13898)`. Mais o parágrafo que declara **por que este arquivo existe e por que
a asserção Vitest não o substitui**: o Vitest lê o artefato, este lê o catálogo; são universos
disjuntos.

Corpo: CTE `allowlist(tabela, coluna)` cujo `VALUES` é **gerado, nunca digitado** —
`node docs/compliance/sql/gen-export-allowlist.cjs --sql-values` e cole a saída. Depois um
`FULL OUTER JOIN` contra `information_schema.columns` restrito a `table_schema='public'`,
`table_type='BASE TABLE'` e às tabelas da própria allowlist, devolvendo apenas as linhas onde um dos
lados é nulo, com a coluna `veredito` distinguindo os dois sentidos:
`COLUNA NOVA NO BANCO — fora da allowlist` × `COLUNA DA ALLOWLIST SUMIU DO BANCO`.

**As duas direções importam por razões diferentes**, e o comentário tem de dizer isso: coluna nova é
o vazamento em potencial (alguém precisa decidir se ela é dado do titular); coluna sumida é o export
entregando menos do que declara — a mentira por omissão que o EXPORT-06 combate.

Rodapé: `APROVADO = 0 linhas` · `REPROVADO = qualquer linha` · e o bloco de prova de mordida no
idioma do `assert-no-secrets.mjs:33-45`.

### C · Executar contra PROD (orquestrador, READ-ONLY)

`execute_sql` com o conteúdo do arquivo. **Esperado: 0 linhas.**

Se vier ≥1 linha, **NÃO afrouxe o smoke.** A linha é informação: ou o `catalogo-vivo-44.json`
envelheceu entre a Task 1 e agora (regenere), ou uma coluna nova apareceu e precisa de decisão no
`export-scope-rules.yaml`. Registre qual foi.

### D · A PROVA DE MORDIDA — o item que fecha o SC#3

Um gate que nunca foi visto falhando não é um gate.

1. Copie o arquivo para um scratch. **Remova UMA linha do `VALUES`** — escolha uma coluna real e
   nomeie qual no VERIFICATION.
2. Execute o arquivo alterado contra PROD.
3. **Esperado: exatamente 1 linha**, com `veredito = 'COLUNA NOVA NO BANCO — fora da allowlist'`
   (do ponto de vista do smoke, uma coluna viva que a allowlist não declara).
4. Cole no `44-VERIFICATION.md` o **par antes/depois**: a saída de 0 linhas do passo C e a saída de
   1 linha deste passo, com o nome da coluna removida e o timestamp de cada execução.
5. Descarte o arquivo alterado. O versionado permanece intacto.

⚠ O arquivo alterado **nunca** é commitado, e o experimento **nunca** toca `information_schema` de
forma que escreva. Todo o experimento é uma consulta.
  </how-to-verify>
  <acceptance_criteria>
- O snapshot foi lido por um humano e as quatro perguntas de A estão respondidas por escrito no SUMMARY
- `docs/compliance/sql/05-export-allowlist-drift.sql` existe com o cabeçalho de 7 blocos do idioma vivo e o `VALUES` gerado por `--sql-values`
- O arquivo é READ-ONLY: `grep -v '^--' docs/compliance/sql/05-export-allowlist-drift.sql | grep -ciE '\b(INSERT|UPDATE|DELETE|CREATE|ALTER|DROP|GRANT|REVOKE)\b'` devolve 0
- A execução contra PROD devolveu **0 linhas**, com timestamp registrado
- A prova de mordida devolveu **≥1 linha**, e o par antes/depois está colado no `44-VERIFICATION.md` com o nome da coluna removida
- O `VALUES` não foi digitado à mão: a contagem de pares bate com `node docs/compliance/sql/gen-export-allowlist.cjs --sql-values | wc -l`
  </acceptance_criteria>
  <resume-signal>Responda "aprovado" com o número de linhas do passo C, o nome da coluna usada na prova de mordida, e as respostas às quatro perguntas da leitura do snapshot — ou descreva o que divergiu.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catálogo vivo (PROD) → artefato commitado | Fronteira medida uma vez, com carimbo; o smoke é quem a re-verifica depois |
| `export-allowlist.json` → projeção da EF | Uma coluna a mais aqui é uma coluna vazada em toda cópia entregue |
| snapshot inline → CI | Um `-u` distraído aprova qualquer coisa; a rede que sobrevive é a negativa nomeada |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-08 | Information Disclosure | drift PROD→artefato | high | mitigate | `05-export-allowlist-drift.sql` com `FULL OUTER JOIN` nos dois sentidos, executado contra PROD com 0 linhas E provado mordendo (par antes/depois no VERIFICATION) |
| T-44-07 | Information Disclosure | segredo atravessando a allowlist | high | mitigate | Asserção (c) negativa nomeada, uma por token, montada em runtime; sobrevive a `-u` porque não é snapshot |
| T-44-19 | Repudiation | snapshot aprovado sem leitura | medium | mitigate | Checkpoint humano bloqueante com quatro perguntas respondidas por escrito antes do commit |
| T-44-13 | Repudiation | artefato gerado de catálogo diferente do commitado | medium | mitigate | Asserção (f): `meta.medido_em` do artefato idêntico ao do `catalogo-vivo-44.json` |
| T-44-20 | Information Disclosure | omissão silenciosa da dependência da Phase 43 | high | mitigate | Asserção (d) positiva nomeada, uma por coluna do BD-6; e a conferência do passo 5 do checkpoint PARA se alguma faltar no catálogo |
| T-44-14 | Tampering | `_shared/exportAllowlist.ts` fora de sincronia | medium | mitigate | Asserção (h) compara os dois artefatos; `--check` cobre ambos |
</threat_model>

<verification>
- `npx vitest run docs/compliance/__tests__/exportAllowlist.test.ts` — 8 verdes
- `node docs/compliance/sql/gen-export-allowlist.cjs --check` — exit 0
- `npm run test:run` verde · `npm run lint` sem regressão da baseline 97 · **zero `--no-verify`**
- Smoke `05-export-allowlist-drift.sql` contra PROD: **0 linhas**, com timestamp
- Prova de mordida: **≥1 linha** com o par antes/depois no `44-VERIFICATION.md`
</verification>

<success_criteria>
- `export-allowlist.json` existe, versionado, com `meta.versao`, `meta.medido_em` e `meta.consumidores` citando a Phase 45
- As quatro colunas do BD-6 estão na allowlist e há asserção nominal para cada uma
- Os dois guardas do SC#3 existem, e o segundo foi visto falhando pelo motivo declarado
</success_criteria>

<output>
Create `.planning/phases/44-exporta-o-acesso/44-03-SUMMARY.md` when done
</output>
