---
phase: 34-superf-cies-do-rh-cv-ia-agendamento-fila-de-trabalho-kpis
plan: 04
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src/features/funil/constants/slaThresholds.ts
  - src/features/funil/services/filaTrabalhoService.ts
  - src/features/funil/hooks/useFilaTrabalho.ts
  - src/features/funil/components/SlaBadge.tsx
  - src/features/funil/components/FilaTrabalhoTab.tsx
  - src/components/pages/CandidatosRHPage.tsx
  - src/features/funil/constants/__tests__/slaThresholds.test.ts
  - src/features/funil/components/__tests__/FilaTrabalhoTab.test.tsx
autonomous: true
requirements: [KPI-01, KPI-03]
must_haves:
  truths:
    - "RH sees a cross-vaga work queue in a new 'Fila' tab on CandidatosRHPage, sorted by time-in-stage (oldest-waiting first), reading v_fila_trabalho (vaga-scoped by inherited RLS)"
    - "The new 'Fila' tab COEXISTS with the existing Kanban tab — both preserved (todos | por-vaga | kanban | fila)"
    - "An SLA badge shows aging (amber) when dias-na-etapa >= threshold and breach (red) when > 1.5x threshold, from a hardcoded SLA_POR_ETAPA constant; the badge always carries a text label + day count (never color-only)"
    - "Each queue row links to /rh/candidatos/:candidaturaId; the read is allowlist-projected (never select('*'))"
  artifacts:
    - path: "src/features/funil/constants/slaThresholds.ts"
      provides: "SLA_POR_ETAPA per-etapa day thresholds + a pure classifySla(etapa, dias) → within/aging/breach function"
      contains: "SLA_POR_ETAPA"
    - path: "src/features/funil/services/filaTrabalhoService.ts"
      provides: "Allowlist read of v_fila_trabalho sorted by entrou_etapa_em ASC (oldest-waiting first)"
      contains: "v_fila_trabalho"
    - path: "src/components/pages/CandidatosRHPage.tsx"
      provides: "A 4th 'fila' tab (grid-cols-4) rendering FilaTrabalhoTab, coexisting with Kanban"
      contains: "value=\"fila\""
  key_links:
    - from: "filaTrabalhoService"
      to: "public.v_fila_trabalho (security_invoker → rh_le_candidaturas)"
      via: "allowlist select order entrou_etapa_em asc"
      pattern: "order\\('entrou_etapa_em'"
    - from: "SlaBadge"
      to: "SLA_POR_ETAPA × differenceInCalendarDays(now, entrou_etapa_em)"
      via: "classifySla → aging/breach classes"
      pattern: "classifySla"
---

<objective>
Add the cross-vaga work queue: a new "Fila" tab on `CandidatosRHPage` that reads the `v_fila_trabalho` view (created in 34-01), sorts candidaturas by time-in-current-stage (oldest waiting first — "o que precisa da minha ação agora"), and flags aging/SLA-breach via a hardcoded per-etapa threshold constant. It coexists with the existing per-vaga Kanban tab (both preserved).

Purpose: KPI-01 (cross-vaga queue by time-in-stage) + KPI-03 (aging/SLA-breach indicator). Depends on 34-01 for the `v_fila_trabalho` view + its regenerated type. The scope is FREE — `v_fila_trabalho` is `security_invoker`, inheriting the vaga-scoped `rh_le_candidaturas` RLS — so this plan only projects an allowlist and computes SLA client-side (thresholds hardcoded; per-vaga config is deferred to backlog).

Output: `slaThresholds` constant + pure classifier, `filaTrabalhoService` + `useFilaTrabalho`, `SlaBadge` + `FilaTrabalhoTab`, and the 4th-tab wiring on `CandidatosRHPage`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-superf-cies-do-rh-cv-ia-agendamento-fila-de-trabalho-kpis/34-CONTEXT.md
@.planning/phases/34-superf-cies-do-rh-cv-ia-agendamento-fila-de-trabalho-kpis/34-UI-SPEC.md
@.planning/phases/34-superf-cies-do-rh-cv-ia-agendamento-fila-de-trabalho-kpis/34-RESEARCH.md

# The view this plan reads (created in 34-01) + the page it edits + patterns:
@src/components/pages/CandidatosRHPage.tsx
@src/features/triagem/components/TriagemTable.tsx
@src/features/triagem/services/triagemService.ts
@src/features/entrevista/hooks/useEntrevistaScorecard.ts

<interfaces>
<!-- The EXACT view contract + tab wiring. Do NOT explore; use these directly. -->

View `public.v_fila_trabalho` (created in 34-01, security_invoker; type regenerated in database.types.ts):
  columns candidatura_id, vaga_id, vaga_titulo, candidato_id, candidato_nome, etapa_atual (public.etapa_processo),
  status, entrou_etapa_em (timestamptz = latest historico transition into the current stage). Terminals aprovado/
  rejeitado are excluded by the view. RH sees only their own vagas' rows (inherited rh_le_candidaturas scope).
  Allowlist read = name those columns explicitly; sort `entrou_etapa_em` ASC (oldest-waiting first). Never select('*').

SLA thresholds (KPI-03, hardcoded — CONTEXT Area 3; per-vaga config deferred). Recommended per-etapa days (UI-SPEC
  §Surface 3): triagem 3 · avaliacao_assincrona 5 · entrevista_online 4 · entrevista_presencial 4 · decisao_final 3.
  Badge levels: aging (amber) when dias >= threshold; breach (red) when dias > 1.5 × threshold; else no badge / subtle.
  dias = differenceInCalendarDays(now, entrou_etapa_em) (date-fns, already in use, e.g. EntrevistaDashboard.tsx).

Tab wiring (`CandidatosRHPage.tsx`): activeTab state is `'todos'|'por-vaga'|'kanban'` at :143; the <Tabs> is at :561; the
  TabsList is `grid w-full grid-cols-3` at :563 with three TabsTrigger (:564-592) each carrying className
  `data-[state=active]:bg-white/30 … px-4 py-2.5 rounded-md font-medium text-sm border-0 h-auto`. Add a 4th value 'fila':
  widen the union + grid-cols-3→grid-cols-4, add a 4th TabsTrigger reusing that EXACT className, add a TabsContent
  value="fila". The Kanban TabsContent (:927-948) is UNTOUCHED (coexists — KPI-01 explicit).

Table shell + states: glass Table shell like TriagemTable (`overflow-x-auto rounded-xl border border-white/10`, header
  `bg-white/10`); AsyncState/HubSection for loading/empty/error. Row link to `/rh/candidatos/${candidatura_id}` (:id IS
  candidaturaId). Query-key/useQuery config: clone entrevistaKeys + staleTime/gcTime 5min/retry 2.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SLA constant + classifier + filaTrabalhoService + useFilaTrabalho</name>
  <files>src/features/funil/constants/slaThresholds.ts, src/features/funil/services/filaTrabalhoService.ts, src/features/funil/hooks/useFilaTrabalho.ts, src/features/funil/constants/__tests__/slaThresholds.test.ts</files>
  <behavior>
    - SLA_POR_ETAPA maps each working etapa to a day threshold (triagem 3, avaliacao_assincrona 5, entrevista_online 4, entrevista_presencial 4, decisao_final 3).
    - classifySla(etapa, dias) returns 'within' when dias < threshold, 'aging' when threshold <= dias <= 1.5×threshold, 'breach' when dias > 1.5×threshold — a table of unit cases proves each boundary.
    - An etapa with no threshold (e.g. inscricao) returns 'within' (never crashes).
    - filaTrabalhoService.listFila() reads v_fila_trabalho with the explicit allowlist (test asserts no `*`) ordered entrou_etapa_em ASC.
  </behavior>
  <read_first>
    - `<interfaces>` — the view columns + the SLA threshold days + aging/breach math
    - `src/features/triagem/services/triagemService.ts:100-200` — allowlist read + `.from(view).select(...).order(...)` shape to clone
    - `src/features/triagem/services/triagemService.ts:302-348` — EtapaFunilM2 type + ETAPA_M2_LABELS (reuse for labels)
    - `src/features/entrevista/hooks/useEntrevistaScorecard.ts:39-63` — keys factory + useQuery config
    - `34-UI-SPEC.md` §Surface 3 (SLA badge thresholds) + §Copywriting §Fila
  </read_first>
  <action>
    Create `slaThresholds.ts`: `export const SLA_POR_ETAPA: Partial<Record<EtapaFunilM2, number>>` with the 5 day thresholds from `<interfaces>`; a pure `export function classifySla(etapa: string, dias: number): 'within' | 'aging' | 'breach'` (undefined threshold → 'within'; dias >= threshold → 'aging'; dias > 1.5×threshold → 'breach'); optionally a `diasNaEtapa(entrou_etapa_em)` helper using `differenceInCalendarDays` from date-fns. Import `EtapaFunilM2` from triagemService.
    Create `filaTrabalhoService.ts`: `FilaTrabalhoServiceError` + a `FILA_ALLOWLIST` const (candidatura_id, vaga_id, vaga_titulo, candidato_id, candidato_nome, etapa_atual, status, entrou_etapa_em) + a `FilaRow` interface + `listFila()` = `.from('v_fila_trabalho').select(FILA_ALLOWLIST).order('entrou_etapa_em', { ascending: true })` → `FilaRow[]`. If the regenerated type is not yet present at author time, a transient `as never`/`as FilaRow[]` cast at the client boundary is acceptable (Phase 8/11/25 precedent) — drop it once 34-01's types land. Never `select('*')`.
    Create `useFilaTrabalho.ts`: a `filaKeys` factory + `useFilaTrabalho()` mirroring the entrevista useQuery config.
    Test: slaThresholds.test.ts covers within/aging/breach at each boundary + the undefined-threshold path.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/funil/constants/__tests__/slaThresholds.test.ts && grep -Eq "SLA_POR_ETAPA" src/features/funil/constants/slaThresholds.ts && grep -Eq "classifySla" src/features/funil/constants/slaThresholds.ts && grep -Eq "v_fila_trabalho" src/features/funil/services/filaTrabalhoService.ts && grep -Eq "order\('entrou_etapa_em'" src/features/funil/services/filaTrabalhoService.ts && ! grep -Eq "select\('\*'\)" src/features/funil/services/filaTrabalhoService.ts && echo FILA_DATA_OK</automated>
  </verify>
  <acceptance_criteria>
    - SLA_POR_ETAPA has the 5 thresholds; classifySla returns within/aging/breach at the correct boundaries (unit table).
    - filaTrabalhoService reads v_fila_trabalho via the allowlist, sorted entrou_etapa_em ASC; zero `select('*')`.
    - useFilaTrabalho mirrors the house useQuery config.
  </acceptance_criteria>
  <done>`FILA_DATA_OK` prints; SLA classifier + allowlist queue read in place.</done>
</task>

<task type="auto">
  <name>Task 2: SlaBadge + FilaTrabalhoTab + 4th tab on CandidatosRHPage</name>
  <files>src/features/funil/components/SlaBadge.tsx, src/features/funil/components/FilaTrabalhoTab.tsx, src/components/pages/CandidatosRHPage.tsx, src/features/funil/components/__tests__/FilaTrabalhoTab.test.tsx</files>
  <read_first>
    - `<interfaces>` — the tab wiring anchors (:143 union, :561 Tabs, :563 grid-cols-3, :564-592 TabsTrigger className, :927 Kanban content untouched) + the Task-1 hook/classifier
    - `src/components/pages/CandidatosRHPage.tsx:143,560-594,927-948` — the activeTab union + TabsList + the Kanban TabsContent to preserve
    - `src/features/triagem/components/TriagemTable.tsx:1-70` — the glass Table shell + header styling to match
    - `34-UI-SPEC.md` §Surface 3 (columns, SLA badge, states, row link) + §Copywriting §Fila (empty/error copy verbatim) + §Accessibility (badge carries text+number, not color-only)
  </read_first>
  <action>
    Create `SlaBadge.tsx`: `{ etapa, entrouEtapaEm }` → compute dias (differenceInCalendarDays) + `classifySla`; render a `Badge` — aging = `Atenção · {n}d` (amber classes `bg-yellow-500/20 text-yellow-200 border-yellow-500/30`), breach = `Atrasado · {n}d` (red `bg-red-500/20 text-red-300 border-red-500/30`), within = subtle `text-white/50` `{n}d` or none. Always render the day count as text (never color-only; colorblind-safe).
    Create `FilaTrabalhoTab.tsx`: uses `useFilaTrabalho()`; renders the glass Table shell (`overflow-x-auto rounded-xl border border-white/10`, header row `bg-white/10`) with columns Candidato · Vaga · Etapa · Tempo na etapa · SLA · Ações (per UI-SPEC), sorted as the service returns (oldest-waiting first). Each row: candidato_nome, vaga_titulo, `ETAPA_M2_LABELS[etapa_atual]`, a "Tempo na etapa" cell (n dias), a `SlaBadge`, and an "Ações" link to `/rh/candidatos/${candidatura_id}`. Wrap in AsyncState/HubSection for loading (skeleton rows) / empty (`Nenhum candidato aguardando ação` + the body copy) / error (`Não foi possível carregar a fila.` + `Tentar novamente`). Use the verbatim UI-SPEC copy.
    Edit `CandidatosRHPage.tsx`: widen the activeTab union to include `'fila'` (:143); change the TabsList `grid-cols-3` → `grid-cols-4` (:563); add a 4th `TabsTrigger value="fila"` reusing the EXACT sibling className with a `Fila` label + an icon/emoji sibling; add a `<TabsContent value="fila">` rendering `<FilaTrabalhoTab />`. Leave the Kanban tab/content (:584-592, :927-948) UNTOUCHED — the two coexist.
    Test: FilaTrabalhoTab.test.tsx renders with a mocked queue of 2 rows (one aging, one breach) and asserts both SlaBadge labels + the row links; renders empty → asserts the empty copy.
  </action>
  <verify>
    <automated>grep -Eq "value=\"fila\"" src/components/pages/CandidatosRHPage.tsx && grep -Eq "grid-cols-4" src/components/pages/CandidatosRHPage.tsx && grep -Eq "value=\"kanban\"" src/components/pages/CandidatosRHPage.tsx && grep -Eq "FilaTrabalhoTab" src/components/pages/CandidatosRHPage.tsx && npm run test:run -- src/features/funil && npm run lint && echo FILA_UI_OK</automated>
  </verify>
  <acceptance_criteria>
    - CandidatosRHPage has 4 tabs (todos|por-vaga|kanban|fila) grid-cols-4; the Kanban tab is preserved (coexists).
    - FilaTrabalhoTab renders the cross-vaga table sorted oldest-first with SLA badges (text+number) + row links to :candidaturaId.
    - Empty/error states use the verbatim UI-SPEC copy; funil tests pass; tsc ≤104.
  </acceptance_criteria>
  <done>`FILA_UI_OK` prints; the Fila tab is live and coexists with Kanban.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH client → v_fila_trabalho | security_invoker view → base-table RLS (rh_le_candidaturas) is the vaga-scope; the client must not re-scope nor select('*') |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-01 | Information Disclosure / Elevation | Cross-recruiter queue read | mitigate | v_fila_trabalho is security_invoker (34-01) → inherits rh_le_candidaturas vaga-scope; proven by 34-01 smoke (h). This plan adds no bypass |
| T-34-04-02 | Information Disclosure | select('*') on the queue read | mitigate | Explicit FILA_ALLOWLIST; grep gate forbids `select('*')` |
| T-34-04-03 | Information Disclosure | SLA badge encodes state by color only (a11y/data-integrity) | mitigate | Badge always carries a text label + day count (never color-only) — the ScoreCell invariant |
| T-34-04-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new deps (date-fns + shadcn Table/Badge vendored); no install task |
</threat_model>

<verification>
- Fila tab reads v_fila_trabalho (allowlist, oldest-first) and coexists with Kanban.
- SLA badge aging/breach classification correct (unit) + text+number (not color-only).
- Zero `select('*')`; tsc ≤104; funil tests pass.
</verification>

<success_criteria>
- `npm run test:run -- src/features/funil` green; `npm run lint` ≤104; `npm run build` green.
- RH sees a cross-vaga, SLA-aware work queue in the new Fila tab, alongside the preserved Kanban.
</success_criteria>

<output>
Create `.planning/phases/34-superf-cies-do-rh-cv-ia-agendamento-fila-de-trabalho-kpis/34-04-SUMMARY.md` when done.
</output>
