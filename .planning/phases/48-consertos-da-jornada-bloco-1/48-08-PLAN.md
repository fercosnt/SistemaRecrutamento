---
phase: 48-consertos-da-jornada-bloco-1
plan: 08
type: execute
wave: 2
depends_on: ["48-01", "48-07"]
files_modified:
  - supabase/functions/notificar-candidato/helpers.ts
  - supabase/functions/notificar-candidato/index.ts
  - supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts
  - supabase/functions/notificar-rh/helpers.ts
  - supabase/functions/notificar-rh/index.ts
  - supabase/functions/notificar-rh/__tests__/notificar-rh.test.ts
  - supabase/migrations/20260921000006_p48_dedupe_decisao_por_historico.sql
  - supabase/migrations/20260921000007_p48_dedupe_revisao_por_ciclo.sql
  - supabase/tests/p48_dedupe_smoke.sql
autonomous: true
requirements: [JORN-18, JORN-20]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A chave de dedupe do evento `decisao` distingue decisões: `{candidatura_id}:decisao:{historico_id}`, com o `historico_id` passado pelo trigger `trg_notif_transicao` (AFTER INSERT em `historico_candidatura` — `NEW.id` É a transição). Uma segunda decisão sobre a mesma candidatura gera linha nova e e-mail (JORN-18)."
    - "A rejeição na triagem reusa o evento `decisao` — nenhum evento novo (correção de fato: o despacho já existia e era ENGOLIDO pelo dedupe). Com a chave por transição, a candidatura que já tinha uma aprovação e é rejeitada na triagem recebe o aviso de rejeição (JORN-20)."
    - "`avanco` recebe o mesmo discriminador por simetria: um re-avanço legítimo a `avaliacao_assincrona` depois de retroceder volta a avisar."
    - "O ciclo de revisão também distingue ciclos: `revisao_respondida` → `{candidatura}:revisao_respondida:{ciclo}` e `revisao_solicitada` (notificar-rh) → `{candidatura}:revisao_solicitada:{ciclo}:{user_id}`, com `ciclo = extract(epoch from revisao_solicitada_em)::bigint` passado pelos dois triggers de `decisao_final` — a premissa «no máximo UMA revisão por candidatura» cai com D-01 e a 2ª revisão depois de uma reabertura seria engolida (pré-requisito do JORN-19, plano 48-11)."
    - "BORDA adjacency (explicit): o MESMO `historico_id` entregue duas vezes colapsa num e-mail (`skipped:'duplicate'`); duas decisões são duas linhas de histórico, logo duas chaves, logo dois e-mails."
    - "BORDA empty (explicit): corpo SEM `historico_id`/`ciclo` → chave legada `{candidatura}:{evento}` (tolerância, para a EF poder ir antes da migration); `historico_id` que não é uuid, ou `ciclo` que não é inteiro, → `400 VALIDATION` sem claim e sem envio."
    - "BORDA ordering (explicit): o retry de uma decisão ANTIGA feito depois de uma decisão NOVA sai com a cópia da decisão ANTIGA — o desfecho vem de `historico_candidatura.etapa_para` da linha da chave, não de `candidaturas.etapa_atual` na hora do envio (L1 da varredura). No branch retry, o `historico_id` é derivado da `dedupe_key` da linha."
    - "`historico_id` que não pertence à `candidatura_id` do corpo → `200 {skipped:'historico_inconsistente'}`, sem claim e sem envio (nunca mandar cópia errada)."
    - "O retry continua deduplicado: o branch retry usa `retry_id` (a linha existente) e o `Idempotency-Key` do Resend é `retry_id ?? dedupe_key`."
    - "Ordem de entrega respeitada (RESEARCH §L): EF tolerante deployada ANTES da migration que passa o campo novo. E a trava terminal de `rejeitar_candidatura` (48-01, D3) está VIVA antes deste plano publicar a chave nova — conferido por catálogo — senão re-rejeitar um knockout mandaria um 2º e-mail."
    - "As migrations partiram das definições VIVAS com pré-portão de md5 e foram aplicadas por `p46apply.cjs` (D-16); varredura de portões rodada antes (D-17)."
  artifacts:
    - path: supabase/functions/notificar-candidato/helpers.ts
      provides: "montarDedupeKey com discriminador para decisao/avanco/revisao_respondida; extrairVersaoDaChave"
      contains: "historico"
    - path: supabase/migrations/20260921000006_p48_dedupe_decisao_por_historico.sql
      provides: "trg_notif_transicao passa historico_id"
      contains: "'historico_id', NEW.id"
    - path: supabase/migrations/20260921000007_p48_dedupe_revisao_por_ciclo.sql
      provides: "triggers de revisão passam ciclo"
      contains: "'ciclo'"
    - path: supabase/tests/p48_dedupe_smoke.sql
      provides: "prova no banco de que o corpo do net.http_post carrega o discriminador"
  key_links:
    - from: "trg_notif_transicao (body do net.http_post)"
      to: "notificar-candidato montarDedupeKey"
      via: "campo historico_id"
      pattern: "historico_id"
    - from: "trg_notif_revisao_solicitada / trg_notif_revisao_respondida"
      to: "notificar-rh montarDedupeKeyRh / notificar-candidato montarDedupeKey"
      via: "campo ciclo"
      pattern: "ciclo"
  prohibitions:
    - requirement_id: JORN-18
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT deixar uma decisão comunicada ao candidato ser descartada em silêncio — skipped:duplicate sobre uma decisão NOVA é o Defeito 18"
    - requirement_id: JORN-20
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT mandar ao candidato a cópia de um desfecho diferente do que o e-mail anuncia (retry com cópia de aprovação para uma rejeição)"
---

<objective>
Fazer a chave de dedupe distinguir decisões (e ciclos de revisão), para que redecidir avise e para que
a rejeição na triagem — cujo despacho já existia — deixe de ser engolida.

Purpose: JORN-18 é a espinha da fase. O log da EF provou que a rejeição da Etapa 9 foi despachada e
descartada como `skipped:"duplicate"` na chave `…:decisao`, ocupada pela aprovação — JORN-20 é o mesmo
mecanismo. E a reabertura (JORN-19) só funciona se o ciclo de revisão também ganhar discriminador.

Output: `notificar-candidato` e `notificar-rh` tolerantes e deployados; duas migrations nos triggers,
aplicadas; smoke que prova o corpo do despacho no banco.

Orçamento: a estimativa (115k, confiança baixa — sem amostra calibrada) passa do smart-zone e é aceita
em vez de fatiar: as tasks não são separáveis — as Tasks 1 e 2 editam o mesmo `montarDedupeKey` e o
mesmo `CorpoRequisicao` de `notificar-candidato`, as Tasks 1 e 3 estendem o mesmo `p48_dedupe_smoke.sql`,
e a ordem EF-tolerante-antes-da-migration atravessa as duas migrations.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

`48-RESEARCH.md`, `48-PATTERNS.md` e `48-VARREDURA-ETAPA-ATUAL.md` NÃO entram inteiros: cada task lê só as seções do seu `<read_first>`.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer) [BLOCKING]: a decisão por transição de ponta a ponta — EF tolerante deployada, trigger passando `historico_id`, corpo provado no banco</name>
  <precondition>`node p46apply.cjs sql "SET TRANSACTION READ ONLY; select position('candidatura_encerrada(' in pg_get_functiondef('public.rejeitar_candidatura(uuid, public.motivo_rejeicao_rh, text)'::regprocedure)) > 0 as trava"` devolve `true` (a trava D3 do plano 48-01 está viva). Se `false`, PARAR: publicar a chave nova antes da trava faz a re-rejeição de um knockout mandar um 2º e-mail.</precondition>
  <files>supabase/functions/notificar-candidato/helpers.ts, supabase/functions/notificar-candidato/index.ts, supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts, supabase/migrations/20260921000006_p48_dedupe_decisao_por_historico.sql, supabase/tests/p48_dedupe_smoke.sql</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §D.1, §D.2, §D.3
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §1 (cabeçalho, transporte e ACL de toda migration), §4, §6 (redefinição de função viva) e §9
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-ETAPA-ATUAL.md` linha L1 (o desfecho lido por `etapa_atual` na hora do envio)
    - `supabase/functions/notificar-candidato/helpers.ts` (`:64-94` `montarDedupeKey` e o docblock que fica falso; `:142-160` `logSeguro`)
    - `supabase/functions/notificar-candidato/index.ts` (`:66-81` `CorpoRequisicao`; `:118-148` parse; `:165-183` branch retry; `:188-211` leitura + survivor-guard; `:256-275` chave; `:366-369` desfecho por `etapa_atual` — L1)
    - `supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts` («T-42-V4 — montarDedupeKey…», «as dedupe keys de uma mesma candidatura NÃO colidem», `:148-191` stubs e `makeRequest`)
    - `supabase/migrations/20260906000006_notifica_rejeicao_automatica.sql` (`:40-123` — corpo vivo de `trg_notif_transicao` e o portão final)
    - `supabase/migrations/20260906000004_reagendamento_notifica_e_reseta_comparecimento.sql` (`:55-61` — discriminador no body, precedente vivo)
    - `supabase/functions/executar-direito-titular/index.ts` (`:269` — a regex de uuid a reusar)
  </read_first>
  <behavior>
    - `montarDedupeKey('decisao', C, undefined, H)` → `C:decisao:H`; sem H → `C:decisao`; `('avanco', C, undefined, H)` → `C:avanco:H`; `('convite', C, A, D)` inalterado
    - duas decisões (H1 ≠ H2) → chaves distintas; mesmo H duas vezes → mesma chave
    - corpo com `historico_id: 'nao-uuid'` → 400 `VALIDATION`, nenhum upsert
    - corpo com `historico_id` de outra candidatura → 200 `skipped:'historico_inconsistente'`, nenhum upsert, nenhum fetch
    - `historico.etapa_para='rejeitado'` com `candidaturas.etapa_atual='aprovado'` (retry antigo depois de nova aprovação) → cópia de REJEIÇÃO
    - branch retry com linha de `dedupe_key = C:decisao:H` → o `historico_id` H é derivado da chave e o desfecho vem do histórico
    - corpo sem `historico_id` → chave legada e desfecho por `etapa_atual` (comportamento atual)
  </behavior>
  <action>
`helpers.ts`: `montarDedupeKey` passa a aplicar o 4º parâmetro (`versao`) também a `decisao` e `avanco` (e, na Task 2, a `revisao_respondida`), produzindo `{candidatura}:{evento}:{versao}`; sem `versao`, a chave legada. Reescrever o docblock de `:64-76` (a premissa «no máximo UMA revisão / uma decisão por candidatura» caiu com o Defeito 18 e com D-01). Nova função pura exportada `extrairVersaoDaChave(dedupeKey: string, evento: EventoLedger): string | undefined` que devolve o 3º segmento quando o evento é `decisao`/`avanco` e o segmento tem forma de uuid. `logSeguro` aceita a chave `historico_id` na allowlist.

`index.ts`: `CorpoRequisicao.historico_id?: string` (docblock); no parse, se presente, exigir string com forma de uuid (mesma regex de `executar-direito-titular/index.ts:269`) — senão `400 VALIDATION`. No branch retry, derivar `historico_id` com `extrairVersaoDaChave(row.dedupe_key, evento)`. Depois de ler a candidatura, quando houver `historico_id` e o evento for `decisao`/`avanco`, ler `historico_candidatura` pela allowlist `etapa_para, candidatura_id` com `maybeSingle`; linha ausente ou de outra candidatura → log redigido + `200 {ok:true, skipped:'historico_inconsistente'}` ANTES do claim. O `desfecho` passa a vir de `etapa_para` quando houver histórico (L1); sem histórico, a regra atual. A chave passa `historico_id` como `versao` para `decisao`/`avanco` (o convite continua com `data_hora` no reagendamento). Testes com os casos de `<behavior>`; atualizar os testes que pinam o formato antigo só onde o formato legítimo mudou.

Rodar `deno test --allow-all supabase/functions/notificar-candidato/` e deployar ANTES da migration: `node efdeploy.cjs notificar-candidato` (a EF tolera a ausência do campo; o inverso — trigger mandando campo que a EF rejeita — é o Pitfall 7).

Migration `20260921000006_p48_dedupe_decisao_por_historico.sql`: ler o vivo (`pg_get_functiondef('public.trg_notif_transicao()'::regprocedure)` + `md5(prosrc)`), pré-portão de md5, `CREATE OR REPLACE` a partir do corpo vivo com UMA mudança — o `jsonb_build_object` do body ganha `'historico_id', NEW.id` (vale para `avanco` e `decisao`, que compartilham o builder). Mantidos intactos: o CASE de eventos, Vault + graceful-skip, `EXCEPTION WHEN OTHERS` fail-open, `REVOKE`. Cabeçalho registrando por que (log de 2026-09-20 16:03:25 UTC, `skipped:"duplicate"`). Portão final: estender o `DO` de `20260906000006:103-123` — a definição instalada contém `'historico_id', NEW.id` E ainda contém `ELSIF NEW.auto_rejeitado THEN` e `auto_rejeitado = false`. Rodar a varredura de portões antes. Aplicar: `node p46apply.cjs migrate supabase/migrations/20260921000006_p48_dedupe_decisao_por_historico.sql`.

Smoke novo `supabase/tests/p48_dedupe_smoke.sql` (contador, gate, SELECT final): (a) catálogo — `trg_notif_transicao` contém `historico_id`; (b) numa subtransação que reverte, com claim de `administrador`, `rejeitar_candidatura` sobre uma candidatura real de conta de teste em andamento → existe em `net.http_request_queue` uma linha nova para `/functions/v1/notificar-candidato` cujo body (`convert_from(body,'UTF8')::jsonb`) tem `evento='decisao'` e `historico_id` igual ao `id` da linha de `historico_candidatura` recém-criada; reverter (a fila do `pg_net` é transacional — nada sai); (c) NEGATIVA — contagens de `historico_candidatura` e `net.http_request_queue` iguais antes e depois. Rodar: `node p46apply.cjs run supabase/tests/p48_dedupe_smoke.sql`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/notificar-candidato/</automated>
    <fails_when>non-zero exit, "FAILED" or "0 passed" in the output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs notificar-candidato && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/notificar-candidato/body" | grep -a -c "historico_inconsistente"</automated>
    <fails_when>non-zero exit from efdeploy, or the grep count printed is 0 (the tolerant EF is not live — the migration must NOT be applied)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260921000006_p48_dedupe_decisao_por_historico.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');for(const t of [\"'historico_id', NEW.id\",'ELSIF NEW.auto_rejeitado THEN','auto_rejeitado = false','edge_invoke_key','EXCEPTION WHEN OTHERS','md5'])if(!c.includes(t))throw new Error('falta: '+t);console.log('OK')" && node p46apply.cjs migrate supabase/migrations/20260921000006_p48_dedupe_decisao_por_historico.sql && node p46apply.cjs run supabase/tests/p48_dedupe_smoke.sql</automated>
    <fails_when>non-zero exit; "falta:" in stderr; "md5 do ledger BATE" absent; or "FAIL" in the smoke output</fails_when>
  </verify>
  <acceptance_criteria>
    - Testes novos de `<behavior>` verdes; a suíte inteira de `notificar-candidato` verde.
    - A EF tolerante foi deployada ANTES da migration (ordem registrada no SUMMARY com os horários).
    - Em PROD, `pg_get_functiondef` de `trg_notif_transicao` contém `'historico_id', NEW.id`, o ramo do knockout e o `auto_rejeitado = false`.
    - `p48_dedupe_smoke.sql` (a)–(c) verde: o corpo do despacho carrega o id da transição.
  </acceptance_criteria>
  <done>Cada decisão agora tem chave própria — a próxima rejeição depois de uma aprovação chega ao candidato.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: o ciclo de revisão nas duas EFs — `ciclo` tolerado, chave por ciclo, deploy</name>
  <files>supabase/functions/notificar-candidato/helpers.ts, supabase/functions/notificar-candidato/index.ts, supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts, supabase/functions/notificar-rh/helpers.ts, supabase/functions/notificar-rh/index.ts, supabase/functions/notificar-rh/__tests__/notificar-rh.test.ts</files>
  <read_first>
    - `supabase/functions/notificar-rh/helpers.ts` (`:55-62` `montarDedupeKeyRh`; `:84-142` o bloco P45-09 — chave por destinatário)
    - `supabase/functions/notificar-rh/index.ts` (`:159-178` parse — hoje IGNORA campos extras, o que torna o `ciclo` inofensivo antes do deploy)
    - `supabase/functions/notificar-rh/__tests__/notificar-rh.test.ts`
    - `supabase/functions/notificar-candidato/index.ts` (o estado deixado pela Task 1)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §D.2 (último parágrafo — o mesmo defeito no ciclo de revisão)
  </read_first>
  <behavior>
    - `notificar-candidato`: `montarDedupeKey('revisao_respondida', C, undefined, '1758400000')` → `C:revisao_respondida:1758400000`; corpo com `ciclo: 'abc'` → 400; sem `ciclo` → chave legada
    - `notificar-rh`: `montarDedupeKeyRh(C, U, '1758400000')` → `C:revisao_solicitada:1758400000:U`; sem ciclo → `C:revisao_solicitada:U` (legado); corpo com `ciclo` inválido → 400; o evento `candidatura_encerrada_a_pedido` não muda
  </behavior>
  <action>
`notificar-candidato`: `CorpoRequisicao.ciclo?: string` (só dígitos, até 12 — `^\d{1,12}$`; inválido → `400 VALIDATION`); `revisao_respondida` usa `ciclo` como `versao` em `montarDedupeKey`. Docblock do campo: é o instante do pedido de revisão, a identidade do ciclo; e é genérico — o plano 48-10 o reusa para a liberação cognitiva.

`notificar-rh`: o parse passa a aceitar `ciclo` opcional com a mesma validação; `montarDedupeKeyRh(candidaturaId, userId, ciclo?)` produz `{candidatura}:revisao_solicitada:{ciclo}:{user_id}` com ciclo e a chave legada sem ele; docblock atualizado (a premissa de uma revisão por candidatura caiu). Não mexer no evento `candidatura_encerrada_a_pedido` nem no laço claim-before-send.

Testes das duas EFs com os casos de `<behavior>`. Deployar as duas ANTES da migration da Task 3: `node efdeploy.cjs notificar-candidato` e `node efdeploy.cjs notificar-rh` (o bundle do `notificar-rh` passa a levar também os exports aditivos de `_shared/email-config.ts` do plano 48-07 — sem mudança de comportamento).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/notificar-candidato/ supabase/functions/notificar-rh/</automated>
    <fails_when>non-zero exit, "FAILED" or "0 passed" in the output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs notificar-candidato && node efdeploy.cjs notificar-rh && T=$(security find-generic-password -s 'Supabase CLI' -a supabase -w) && curl -s -H "Authorization: Bearer $T" https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/notificar-rh/body | grep -a -c "ciclo"</automated>
    <fails_when>non-zero exit from either efdeploy, or the grep count printed is 0</fails_when>
  </verify>
  <acceptance_criteria>
    - As duas EFs aceitam `ciclo` válido, recusam inválido, e mantêm a chave legada sem ele (testes).
    - Suítes completas das duas EFs verdes.
    - As duas deployadas antes da Task 3 (horários no SUMMARY).
  </acceptance_criteria>
  <done>As duas EFs sabem distinguir o 2º ciclo de revisão — e nada quebra enquanto o banco ainda não manda o campo.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: os triggers de revisão passam o ciclo — aplicado, provado no banco, sem regressão</name>
  <files>supabase/migrations/20260921000007_p48_dedupe_revisao_por_ciclo.sql, supabase/tests/p48_dedupe_smoke.sql</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §6 (redefinição de função viva com pré-portão de md5)
    - `supabase/migrations/20260730000003_p42_trg_revisao_solicitada.sql` (`:142-215` — `trg_notif_revisao_solicitada`)
    - `supabase/migrations/20260730000004_p42_evento_revisao_respondida.sql` (`:175-257` — `trg_notif_revisao_respondida`; `:196-200` o comentário que fica falso)
    - `supabase/tests/p42_notif_revisao_smoke.sql` ((d) `:280-290` pina a forma do trigger)
    - `supabase/tests/p48_dedupe_smoke.sql` (Task 1)
  </read_first>
  <action>
Ler o vivo das duas funções de trigger (`pg_get_functiondef` + `md5(prosrc)`). Rodar a varredura de portões (o `p42_notif_revisao_smoke` (d) pina `AFTER UPDATE OF revisao_solicitada_em` — a definição do TRIGGER não muda, só a função).

Migration `20260921000007_p48_dedupe_revisao_por_ciclo.sql`: pré-portão de md5 por função; `CREATE OR REPLACE` de `trg_notif_revisao_solicitada()` e `trg_notif_revisao_respondida()` a partir dos corpos vivos, cada um com UMA mudança — o body ganha `'ciclo', extract(epoch from NEW.revisao_solicitada_em)::bigint::text`. Guards de transição, Vault, fail-open, `REVOKE` intactos. Cabeçalho: o comentário de `20260730000004:196-200` («por isso `montarDedupeKey` NÃO ganhou ramo») ficou falso com D-01 — registrar. `DO` final: as duas definições contêm `'ciclo'`. Aplicar: `node p46apply.cjs migrate supabase/migrations/20260921000007_p48_dedupe_revisao_por_ciclo.sql`.

Estender `supabase/tests/p48_dedupe_smoke.sql`: (d) catálogo — as duas funções contêm `'ciclo'`; (e) numa subtransação que reverte: numa candidatura real de conta de teste em andamento, `registrar_decisao` rejeitado com claim de administrador A; `solicitar_revisao_decisao` com claim do candidato titular → linha nova em `net.http_request_queue` para `/functions/v1/notificar-rh` com `ciclo` igual ao epoch de `revisao_solicitada_em`; `responder_revisao_decisao` com veredito `mantida` e claim de administrador B (≠ A) → linha nova para `/functions/v1/notificar-candidato` com `evento='revisao_respondida'` e o MESMO `ciclo`; reverter tudo; (f) NEGATIVA — contagens de `decisao_final`, `decisao_final_historico`, `historico_candidatura` e da fila iguais antes e depois. Usar ids reais de usuários RH/admin ativos lidos na execução (FK de `por_usuario`).

Rodar: `node p46apply.cjs run supabase/tests/p48_dedupe_smoke.sql`, `node p46apply.cjs run supabase/tests/p42_notif_revisao_smoke.sql` e `node p46apply.cjs run supabase/tests/p42_revisao_art20_smoke.sql` (regressão — o comportamento da revisão ainda é o antigo até o plano 48-11).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260921000007_p48_dedupe_revisao_por_ciclo.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');for(const t of ['trg_notif_revisao_solicitada','trg_notif_revisao_respondida',\"'ciclo'\",'revisao_solicitada_em','md5'])if(!c.includes(t))throw new Error('falta: '+t);console.log('OK')" && node p46apply.cjs migrate supabase/migrations/20260921000007_p48_dedupe_revisao_por_ciclo.sql && node p46apply.cjs run supabase/tests/p48_dedupe_smoke.sql && node p46apply.cjs run supabase/tests/p42_notif_revisao_smoke.sql && node p46apply.cjs run supabase/tests/p42_revisao_art20_smoke.sql</automated>
    <fails_when>non-zero exit; "falta:" in stderr; "md5 do ledger BATE" absent; or "FAIL" in any smoke output</fails_when>
  </verify>
  <acceptance_criteria>
    - Em PROD, as duas funções de trigger contêm `'ciclo'`; as definições dos TRIGGERS (`AFTER UPDATE OF …`) são as mesmas de antes.
    - `p48_dedupe_smoke.sql` (a)–(f) verde; `p42_notif_revisao_smoke` e `p42_revisao_art20_smoke` verdes.
    - A prova com conta real (2 decisões → 2 linhas `decisao` com chaves distintas; rejeição na triagem de candidatura que nunca teve `decisao` → 1 linha nova `enviado`/`entregue`, log sem `skipped`) é do plano 48-18.
  </acceptance_criteria>
  <done>O banco passou a dizer à EF qual decisão e qual ciclo cada e-mail anuncia — e nenhum smoke existente regrediu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| banco (trigger, Bearer do Vault) → EFs de notificação | corpo ids-only; a EF resolve o resto por allowlist |
| EF → Resend | e-mail ao candidato/RH, at-most-once no transporte, idempotente por chave |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-08-01 | Repudiation | decisão descartada em silêncio (skipped:duplicate) | high | mitigate | chave por `historico_id`/`ciclo`; smoke prova o corpo; prova em PROD no 48-18 |
| T-48-08-02 | Tampering | corpo com `historico_id` forjado/malformado | medium | mitigate | validação de forma (uuid / inteiro) → 400; pertinência à candidatura conferida → skip |
| T-48-08-03 | Information Disclosure | cópia de desfecho errado num retry tardio | medium | mitigate | desfecho pela linha de histórico da chave (L1), não pelo estado atual |
| T-48-08-04 | Denial of Service | trigger mandando campo que a EF rejeita (e-mail some, at-most-once) | high | mitigate | EF tolerante deployada antes da migration; verify reprova o apply se o bundle não tiver o marcador |
| T-48-08-05 | Tampering | 2º e-mail de rejeição a knockout | high | mitigate | precondição de catálogo: trava D3 viva antes de publicar a chave |
| T-48-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `deno test` de `notificar-candidato` e `notificar-rh` verde; as duas deployadas antes das migrations.
- Duas migrations aplicadas (md5 BATE); `p48_dedupe_smoke`, `p42_notif_revisao_smoke`, `p42_revisao_art20_smoke` verdes.
</verification>

<success_criteria>
Redecidir avisa, a rejeição na triagem avisa, e o 2º ciclo de revisão depois de uma reabertura terá chave própria.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `montarDedupeKey` (versão para `decisao`/`avanco`/`revisao_respondida`) | helper EF | editado |
| `extrairVersaoDaChave` | helper EF | **novo** |
| `CorpoRequisicao.historico_id`, `CorpoRequisicao.ciclo` (notificar-candidato) | contrato de payload | **novos** |
| skip `historico_inconsistente` | ramo da EF | **novo** |
| `montarDedupeKeyRh(candidaturaId, userId, ciclo?)` + parse de `ciclo` (notificar-rh) | helper/contrato | editado |
| `public.trg_notif_transicao()` (body com `historico_id`) | função de trigger | editada |
| `public.trg_notif_revisao_solicitada()`, `public.trg_notif_revisao_respondida()` (body com `ciclo`) | funções de trigger | editadas |
| `supabase/migrations/20260921000006_p48_dedupe_decisao_por_historico.sql`, `…000007_p48_dedupe_revisao_por_ciclo.sql` | migrations | **novas** |
| `supabase/tests/p48_dedupe_smoke.sql` | smoke | **novo** |

## Premissas sinalizadas (probe de borda)

O probe classificou JORN-18 em três categorias (adjacency, empty, ordering) — as três estão resolvidas
como bordas `explicit` nos must_haves acima. JORN-20 foi `unclassified`; sua borda (candidatura que já
teve `decisao` volta a ser avisada na rejeição) está nos truths e é provada no 48-18.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-08-SUMMARY.md` when done
</output>
