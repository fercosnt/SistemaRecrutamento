---
phase: 48-consertos-da-jornada-bloco-1
plan: 12
type: execute
wave: 3
depends_on: ["48-01", "48-09"]
files_modified:
  - supabase/tests/p48_retroativos_ensaio.sql
  - supabase/migrations/20260921000013_p48_retro_desfaz_encerrada_knockout.sql
  - supabase/migrations/20260921000014_p48_retro_feedback_rejeicao_triagem.sql
autonomous: false
requirements: [JORN-26, JORN-22]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "As duas escritas retroativas sobre linhas existentes são decisão do OPERADOR, tomada na execução por `checkpoint:decision` com a contagem medida — nunca silenciosas (D-18, CONTEXT §Specifics)."
    - "(A) Desfazer as 2 marcas erradas de `encerrada_a_pedido_em` deixadas pelo Defeito 26 em candidaturas de knockout (`92522073-484c-46a3-9e9d-8e80afa3c062`, `25a4231c-612b-4f86-9c5a-904ca09f18f4`, ambas `+claude`) — só se aprovado; com contagem antes (2) e depois (0), escopo travado nesses ids (JORN-26)."
    - "(B) Gravar o `feedback_rejeicao` neutro na 1 rejeição humana na triagem já feita sem ele (`bf26ee3c-0ae3-4e92-a99b-6e05efc2a662`, conta de teste da jornada) — só se aprovado; contagem antes (1) e depois (0) sem feedback; devolve o Art. 20 a quem foi rejeitado antes do conserto (JORN-22)."
    - "Antes da decisão, um ENSAIO roda os MESMOS UPDATEs da escrita real dentro de uma transação que sempre reverte e reporta as contagens — o relatório do ensaio é gerado pela mesma query do write (portão destrutivo, item 5, aplicado por analogia)."
    - "Nenhuma das escritas dispara e-mail: desfazer `encerrada_a_pedido_em` (NOT NULL→NULL) não aciona o trigger de aviso ao RH (que só dispara em NULL→NOT NULL), e `feedback_rejeicao` não tem despacho; a fila do `pg_net` é conferida antes e depois (nenhum e-mail retroativo a candidato — D-18)."
    - "Cada escrita aprovada vai numa migration própria com portão de contagem (aborta se o conjunto medido na hora não for exatamente o esperado) aplicada por `p46apply.cjs` (D-16) — auditável no ledger."
    - "Uma escrita RECUSADA pelo operador não gera arquivo nem apply; a recusa fica registrada no SUMMARY."
    - "Os resets/escritas são escopados aos `candidatura_id` listados; nada fora deles é tocado (D-19)."
  artifacts:
    - path: supabase/tests/p48_retroativos_ensaio.sql
      provides: "ensaio revertido com as contagens das duas escritas"
    - path: supabase/migrations/20260921000013_p48_retro_desfaz_encerrada_knockout.sql
      provides: "desfaz as 2 marcas (só se aprovado)"
    - path: supabase/migrations/20260921000014_p48_retro_feedback_rejeicao_triagem.sql
      provides: "backfill de 1 feedback (só se aprovado)"
  key_links:
    - from: "supabase/tests/p48_retroativos_ensaio.sql"
      to: "as migrations 000013/000014"
      via: "o mesmo predicado de UPDATE, copiado"
      pattern: "encerrada_a_pedido_em|feedback_rejeicao"
  prohibitions:
    - requirement_id: JORN-26
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT executar escrita retroativa sem a aprovação explícita do operador naquele momento, nem fora dos ids medidos"
    - requirement_id: JORN-22
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT disparar e-mail retroativo a candidato por causa destas correções"
---

<objective>
Levar ao operador, com contagem medida e ensaio revertido, as duas correções retroativas de dados que a
fase deixou pendentes — e executar só as que ele aprovar.

Purpose: D-18 separa aditivo (roda sem pausa) de UPDATE retroativo sobre linha existente (checkpoint com
contagem antes/depois). A marcação do D-02 já foi autorizada e está no 48-04. Estas duas não: desfazer
as marcas erradas do Defeito 26 e devolver o Art. 20 à rejeição na triagem feita antes do conserto.
Ambas sobre contas de teste; nenhuma apaga linha.

Output: script de ensaio; decisão do operador; migrations com portão para as aprovadas.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-CONTEXT.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-ETAPA-ATUAL.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): o ensaio — os mesmos UPDATEs, numa transação que sempre reverte, com as contagens</name>
  <files>supabase/tests/p48_retroativos_ensaio.sql</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-ETAPA-ATUAL.md` §5 D1 e §7 item 4 (as 2 marcas)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §B «Retroatividade» e §C «Retroativo»
    - `supabase/migrations/20260921000009_p48_rejeicao_triagem_feedback.sql` (o texto neutro exato gravado pela RPC — o backfill usa o MESMO)
    - a função de trigger viva do aviso de encerramento (`trg_notif_candidatura_encerrada`, por `pg_get_functiondef`) — confirmar que só age em NULL→NOT NULL
    - os triggers vivos de `candidaturas` (`select tgname, pg_get_triggerdef(oid) from pg_trigger where tgrelid='public.candidaturas'::regclass and not tgisinternal`) — confirmar quais disparam num UPDATE de `feedback_rejeicao` ou `encerrada_a_pedido_em`
  </read_first>
  <action>
Criar `supabase/tests/p48_retroativos_ensaio.sql` — um único bloco `DO` que: mede as contagens de base (`net.http_request_queue`, `historico_candidatura`); (A) executa `UPDATE public.candidaturas SET encerrada_a_pedido_em = NULL WHERE encerrada_a_pedido_em IS NOT NULL AND etapa_atual = 'inscricao' AND status = 'rejeitado' AND opcao_knockout_id IS NOT NULL` e captura `ROW_COUNT` e os ids afetados; (B) executa `UPDATE public.candidaturas SET feedback_rejeicao = '<texto neutro idêntico ao da RPC>' WHERE status = 'rejeitado' AND etapa_atual = 'rejeitado' AND feedback_rejeicao IS NULL AND data_decisao_final IS NULL AND motivo_rejeicao IS DISTINCT FROM 'knockout_automatico' AND NOT EXISTS (SELECT 1 FROM public.decisao_final d WHERE d.candidatura_id = candidaturas.id)` e captura `ROW_COUNT` e ids; mede de novo a fila e o histórico; e termina SEMPRE com `RAISE EXCEPTION 'ENSAIO OK: desfazer=% (%) backfill=% (%) fila_delta=% historico_delta=%', …` — a exceção reverte tudo e a mensagem é o relatório. Docblock: é ensaio, nunca comita; os predicados são os que as migrations copiarão.

Rodar: `node p46apply.cjs run supabase/tests/p48_retroativos_ensaio.sql` — a saída ESPERADA é erro com `ENSAIO OK: desfazer=2 (…92522073…, …25a4231c…) backfill=1 (…bf26ee3c…) fila_delta=0 historico_delta=0`. Qualquer outro número ou id, ou delta diferente de 0, é achado: registrar e levar ao checkpoint como está (não ajustar o predicado para caber no esperado).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && OUT=$(node p46apply.cjs run supabase/tests/p48_retroativos_ensaio.sql 2>&1); echo "$OUT" | tail -2; echo "$OUT" | grep -q "ENSAIO OK: desfazer=2" && echo "$OUT" | grep -q "backfill=1" && echo "$OUT" | grep -q "fila_delta=0 historico_delta=0"</automated>
    <fails_when>the output lacks "ENSAIO OK: desfazer=2", "backfill=1" or "fila_delta=0 historico_delta=0" (counts or side effects diverge from the measured scope — report at the checkpoint, do not tune the predicate)</fails_when>
  </verify>
  <acceptance_criteria>
    - O script existe, termina sempre em exceção (nunca comita) e imprime as contagens e os ids.
    - Contagens registradas no SUMMARY; `fila_delta=0` e `historico_delta=0` provam que as escritas não disparam e-mail nem trilha.
    - Depois do ensaio, as contagens reais de PROD são as de antes (conferir `encerrada_a_pedido_em` não nulo em 2 knockouts e `feedback_rejeicao` nulo em `bf26ee3c…`).
  </acceptance_criteria>
  <done>O operador vai decidir sobre números medidos pela mesma query que escreveria, não sobre uma estimativa.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: o operador decide as duas escritas retroativas</name>
  <decision>Aplicar (A) desfazer as 2 marcas de «encerrada a pedido» em knockout e/ou (B) gravar o `feedback_rejeicao` neutro na rejeição de triagem já feita?</decision>
  <context>
Medido pelo ensaio da Task 1 (colar aqui o relatório `ENSAIO OK: …`). Ambas são UPDATE sobre linha
existente de conta de teste; nenhuma apaga linha; nenhuma dispara e-mail (deltas de fila e histórico = 0).
(A) As 2 marcas nasceram do Defeito 26 e os 3 e-mails ao RH já saíram — desfazer corrige o dado, não o
aviso. (B) Com o backfill, a candidata da jornada volta a ver o cartão «Entenda a decisão» e a página
de explicação `humana_triagem` (plano 48-09) — é o que o conserto faria se ela fosse rejeitada hoje.
  </context>
  <options>
    <option id="ambos">
      <name>Aplicar (A) e (B)</name>
      <pros>Os dados de teste passam a refletir o sistema consertado; o cartão da candidata vira material de prova no 48-18.</pros>
      <cons>Reescreve o estado deixado pelos defeitos (que também é evidência dos defeitos — já registrada na JORNADA).</cons>
    </option>
    <option id="so-a">
      <name>Só (A)</name>
      <pros>Corrige a marca errada sem mexer na rejeição histórica.</pros>
      <cons>A rejeição de triagem anterior ao conserto segue sem Art. 20 alcançável.</cons>
    </option>
    <option id="so-b">
      <name>Só (B)</name>
      <pros>Devolve o Art. 20 à rejeição anterior ao conserto.</pros>
      <cons>As 2 marcas erradas de «encerrada a pedido» permanecem.</cons>
    </option>
    <option id="nenhum">
      <name>Nenhuma</name>
      <pros>Preserva o estado exatamente como os defeitos o deixaram.</pros>
      <cons>Dados de teste inconsistentes com o sistema consertado.</cons>
    </option>
  </options>
  <read_first>
    - a saída do ensaio da Task 1
  </read_first>
  <action>Apresentar ao operador o relatório do ensaio e as quatro opções; registrar a escolha literal no SUMMARY. Nenhuma escrita acontece nesta task.</action>
  <acceptance_criteria>
    - A opção escolhida (`ambos` | `so-a` | `so-b` | `nenhum`) está registrada no SUMMARY com o horário.
  </acceptance_criteria>
  <resume-signal>Responda: ambos, so-a, so-b ou nenhum</resume-signal>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: executar SÓ o que foi aprovado — migration com portão de contagem, apply e contagem depois</name>
  <precondition>A Task 2 registrou a escolha do operador. Com `nenhum`, esta task só registra a recusa e termina sem arquivo e sem apply.</precondition>
  <files>supabase/migrations/20260921000013_p48_retro_desfaz_encerrada_knockout.sql, supabase/migrations/20260921000014_p48_retro_feedback_rejeicao_triagem.sql</files>
  <read_first>
    - `supabase/tests/p48_retroativos_ensaio.sql` (os predicados)
    - `supabase/migrations/20260921000004_p48_analise_descartada_knockout.sql` (48-04 — molde de escrita retroativa com portão)
  </read_first>
  <action>
Para (A), se aprovado: `supabase/migrations/20260921000013_p48_retro_desfaz_encerrada_knockout.sql` (cabeçalho do M8 com a decisão do operador datada e o relatório do ensaio, transporte `p46apply.cjs migrate`, sem `BEGIN;`) com um `DO` que: conta o conjunto do predicado de (A); aborta se o total não for 2 OU se algum id não for `92522073-484c-46a3-9e9d-8e80afa3c062`/`25a4231c-612b-4f86-9c5a-904ca09f18f4`; mede a fila do `pg_net`; executa o UPDATE restrito a esses ids; exige `ROW_COUNT = 2`; aborta se a fila mudou.

Para (B), se aprovado: `supabase/migrations/20260921000014_p48_retro_feedback_rejeicao_triagem.sql` no mesmo molde — total 1, id `bf26ee3c-0ae3-4e92-a99b-6e05efc2a662`, o texto neutro IDÊNTICO ao da RPC (`Após análise da sua candidatura pela nossa equipe, não seguiremos com ela neste momento.`), `ROW_COUNT = 1`, fila inalterada.

Aplicar cada arquivo criado com `node p46apply.cjs migrate <arquivo>`. Medir depois e registrar no SUMMARY: (A) 0 knockouts com `encerrada_a_pedido_em`; (B) `bf26ee3c…` com `feedback_rejeicao` = texto neutro. Um arquivo NÃO criado (opção recusada) é registrado como recusado no SUMMARY, e o verify correspondente é pulado por isso.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && for F in supabase/migrations/20260921000013_p48_retro_desfaz_encerrada_knockout.sql supabase/migrations/20260921000014_p48_retro_feedback_rejeicao_triagem.sql; do if [ -f "$F" ]; then node -e "const s=require('fs').readFileSync(process.argv[1],'utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');if(/^\s*DELETE\s+FROM/im.test(c))throw new Error('DELETE proibido');if(!/ROW_COUNT/.test(c))throw new Error('sem portao ROW_COUNT');if(!/http_request_queue/.test(c))throw new Error('sem conferencia da fila (nenhum e-mail retroativo)');console.log('OK '+process.argv[1])" "$F" && node p46apply.cjs migrate "$F" || exit 1; else echo "recusado pelo operador: $F"; fi; done</automated>
    <fails_when>non-zero exit; "wrapper BEGIN proibido", "DELETE proibido", "sem portao ROW_COUNT" or "sem conferencia da fila" in stderr; or a migrate output without "md5 do ledger BATE"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs sql "SET TRANSACTION READ ONLY; select (select count(*) from public.candidaturas where encerrada_a_pedido_em is not null and etapa_atual='inscricao' and status='rejeitado' and opcao_knockout_id is not null)::int as marcas_knockout, (select (feedback_rejeicao is not null) from public.candidaturas where id='bf26ee3c-0ae3-4e92-a99b-6e05efc2a662') as feedback_marina"</automated>
    <fails_when>non-zero exit; or, for an APPROVED option, marcas_knockout is not 0 (A) / feedback_marina is not true (B) — compare against the choice recorded in the SUMMARY</fails_when>
  </verify>
  <acceptance_criteria>
    - Só as opções aprovadas têm arquivo e apply; cada uma com portão de contagem, `ROW_COUNT` exato e fila do `pg_net` inalterada.
    - Contagens depois registradas e coerentes com a escolha.
    - Nenhum `DELETE`, nenhum e-mail disparado.
  </acceptance_criteria>
  <done>O estado de teste deixado pelos defeitos foi corrigido exatamente na medida que o operador decidiu — e o ledger guarda a prova.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → PROD (UPDATE retroativo) | escrita sobre linha existente; autorização do operador na hora |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-12-01 | Tampering | escrita retroativa fora do escopo | high | mitigate | ensaio pela mesma query; portão por contagem e por id na migration; `ROW_COUNT` exato |
| T-48-12-02 | Repudiation | escrita sem autorização | high | mitigate | `checkpoint:decision` com `gate="blocking-human"`; escolha registrada; recusa não gera arquivo |
| T-48-12-03 | Spoofing | e-mail retroativo a candidato | medium | mitigate | conferência da fila do `pg_net` antes/depois no ensaio e na migration |
| T-48-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Ensaio com `ENSAIO OK` e deltas zero.
- Escolha do operador registrada.
- Migrations só das opções aprovadas, aplicadas com md5; contagens depois coerentes.
</verification>

<success_criteria>
Nenhuma linha existente foi reescrita sem a decisão do operador, e as que foram estão no ledger com a contagem que as justificou.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `supabase/tests/p48_retroativos_ensaio.sql` | ensaio revertido | **novo** |
| `supabase/migrations/20260921000013_p48_retro_desfaz_encerrada_knockout.sql` | escrita retroativa com portão | **nova** (só se aprovada) |
| `supabase/migrations/20260921000014_p48_retro_feedback_rejeicao_triagem.sql` | escrita retroativa com portão | **nova** (só se aprovada) |

## Premissas sinalizadas (probe de borda)

Nenhuma borda do relatório do probe é resolvida aqui. Premissa registrada: a cancelação do pedido de
exclusão da candidata não desfez `encerrada_a_pedido_em` por desenho da Phase 45 (a retirada continua
visível ao RH — Invariante 9); a opção (A) corrige só as marcas que nunca deveriam ter existido (knockout).

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-12-SUMMARY.md` when done
</output>
