---
phase: 48-consertos-da-jornada-bloco-1
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - src/lib/candidatura/candidaturaEncerrada.ts
  - src/lib/candidatura/__tests__/candidaturaEncerrada.test.ts
  - src/features/entrevista/services/entrevistaService.ts
  - src/features/hub-candidato/components/HubCandidatoRH.tsx
  - src/features/hub-candidato/components/__tests__/hubAcoesEncerrada.test.tsx
  - src/components/pages/CandidatosRHPage.tsx
  - src/components/pages/DashboardCandidatoPage.tsx
autonomous: true
requirements: [JORN-26]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Existe UM helper TS `candidaturaEncerrada(etapa, status)` em `src/lib/candidatura/candidaturaEncerrada.ts`, exportado nomeado, que espelha a função SQL `public.candidatura_encerrada` do plano 48-01: etapa em {aprovado, rejeitado} OU status em {rejeitado, finalizado}, null-safe (D-11, D-21)."
    - "O hub do RH (`HubCandidatoRH`) NÃO oferece «Avançar» nem «Rejeitar» para candidatura encerrada — knockout (`inscricao`/`rejeitado`) e `finalizado` legado inclusos — e diz por quê, com a linha `data-testid=\"hub-acoes-encerrada\"` (C1/C1b da varredura)."
    - "O hub continua oferecendo «Avançar» e «Rejeitar» para candidatura em andamento, exatamente como antes (o teste `hubAcoesEmTriagem.test.tsx` segue verde)."
    - "O contexto do hub (`getEntrevistaContexto`) passa a ler `status` pela allowlist de colunas — sem ele o hub não teria como saber que o knockout acabou."
    - "A contagem do funil por etapa da aba «Por Vaga» (`CandidatosRHPage`) não conta knockout nem `finalizado` legado nas etapas de trabalho; `aprovado` e `rejeitado` seguem contando por etapa (C2 · D6)."
    - "O painel do candidato passa a usar o helper canônico nos lugares onde já decidia «encerrada» (`STATUS_TERMINAIS`/`emAndamento`), SEM mudar comportamento — e `hasDecisaoFinal` (`DashboardCandidatoPage.tsx:148-159`) fica intocado (D-12)."
    - "BORDA (explicit): `candidaturaEncerrada('triagem','finalizado')` = true; `candidaturaEncerrada('inscricao','rejeitado')` = true; `candidaturaEncerrada('triagem', null)` = false; `candidaturaEncerrada(null, 'rejeitado')` = true."
    - "Nenhuma mudança introduz portão de evidência no avanço (D-08): Avançar em candidatura em andamento continua 1 clique."
    - "`tsc` não passa de 90 erros (D-15); o front foi publicado e `git log --oneline origin/main..HEAD` sai vazio (D-16)."
  artifacts:
    - path: src/lib/candidatura/candidaturaEncerrada.ts
      provides: "predicado canônico TS"
      exports: ["candidaturaEncerrada", "STATUS_TERMINAIS", "ETAPAS_TERMINAIS"]
    - path: src/features/hub-candidato/components/__tests__/hubAcoesEncerrada.test.tsx
      provides: "prova de que o hub esconde as ações para knockout e finalizado"
  key_links:
    - from: "src/features/hub-candidato/components/HubCandidatoRH.tsx"
      to: "src/lib/candidatura/candidaturaEncerrada.ts"
      via: "import nomeado; gating de Avançar/Rejeitar"
      pattern: "candidaturaEncerrada\\("
    - from: "src/features/entrevista/services/entrevistaService.ts"
      to: "candidaturas.status"
      via: "allowlist do select do contexto"
      pattern: "etapa_atual, status"
  prohibitions:
    - requirement_id: JORN-26
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT oferecer ao RH uma ação de funil que devolveria ao funil uma pessoa já eliminada — Avançar sobre knockout recolocava a candidata em triagem com status rejeitado"
    - requirement_id: JORN-26
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT esconder as ações sem dizer por quê — botão que some sem explicação faz o operador procurar um defeito que não existe"
---

<objective>
O espelho TS do predicado canônico e as três instâncias de front da varredura (C1, C1b, C2): o hub do
RH para de oferecer «Avançar»/«Rejeitar» para candidatura encerrada e o funil da aba «Por Vaga» para
de contar knockout como inscrição pendente.

Purpose: D-21 manda aplicar UM predicado em todas as instâncias não-destrutivas — «os botões
Avançar/Rejeitar do `HubCandidatoRH`» e «o KPI do funil (`CandidatosRHPage.tsx:258`)» estão na
lista. O servidor já recusa (plano 48-01, trava D3); este plano impede que a tela ofereça a ação.

Output: helper + teste, hub + teste novo, allowlist do contexto, contagem do funil, painel do
candidato usando o helper; publicado.

Depende do 48-01 (wave 2): o helper TS espelha a definição de `public.candidatura_encerrada` que o
48-01 escreve e aplica, e a regra do funil espelha a CTE `volume` de `funil_kpis` do mesmo plano —
espelhar a versão instalada, não a prevista, evita que os dois lados divirjam se o 48-01 ajustar o
predicado na execução.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-ETAPA-ATUAL.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): helper canônico → contexto com `status` → hub sem ações para encerrada</name>
  <files>src/lib/candidatura/candidaturaEncerrada.ts, src/lib/candidatura/__tests__/candidaturaEncerrada.test.ts, src/features/entrevista/services/entrevistaService.ts, src/features/hub-candidato/components/HubCandidatoRH.tsx, src/features/hub-candidato/components/__tests__/hubAcoesEncerrada.test.tsx</files>
  <read_first>
    - `src/components/pages/DashboardCandidatoPage.tsx` (`:57-67` `STATUS_TERMINAIS`/`candidaturaEncerrada`; `:459-464` `emAndamento`) — a regra a espelhar
    - `src/lib/datetime/formatDataHoraSP.ts` e `src/lib/datetime/__tests__/formatDataHoraSP.test.ts` — forma de util compartilhado (docblock `@module`/`@see`)
    - `src/features/hub-candidato/components/HubCandidatoRH.tsx` (`:120-150` `proximaEtapa`/`podeRetroceder`; `:225-285` a linha de ações)
    - `src/features/hub-candidato/components/__tests__/hubAcoesEmTriagem.test.tsx` — molde do teste novo (mocks do contexto)
    - `src/features/entrevista/services/entrevistaService.ts` (`:181` o tipo `EntrevistaContextoRow`; `:225-251` o select do contexto)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §15, §18
    - `supabase/migrations/20260921000001_p48_candidatura_encerrada.sql` se já existir (a função SQL que este helper espelha)
  </read_first>
  <behavior>
    - `candidaturaEncerrada('aprovado','finalizado')` → true; `('rejeitado','rejeitado')` → true; `('inscricao','rejeitado')` → true; `('triagem','finalizado')` → true
    - `('triagem','em_analise')` → false; `('decisao_final','aguardando_resposta')` → false; `('triagem', null)` → false; `(null, null)` → false; `(null,'rejeitado')` → true; `(undefined,'finalizado')` → true
    - hub com contexto `etapa_atual='inscricao', status='rejeitado'` → sem botão «Avançar», sem gatilho «Rejeitar», com `data-testid="hub-acoes-encerrada"`
    - hub com `etapa_atual='triagem', status='finalizado'` → idem
    - hub com `etapa_atual='triagem', status='em_analise'` → «Avançar» e «Rejeitar» presentes, sem a linha de encerrada
  </behavior>
  <action>
Criar `src/lib/candidatura/candidaturaEncerrada.ts`: docblock `@module` explicando que é o espelho TS de `public.candidatura_encerrada` (migration `20260921000001`), que a regra veio de `STATUS_TERMINAIS` do painel do candidato (`@see`), que é ALLOWLIST de terminais e que o servidor é quem decide — o front só esconde ação. Exportar nomeados `ETAPAS_TERMINAIS` (ReadonlySet com `aprovado`, `rejeitado`), `STATUS_TERMINAIS` (ReadonlySet com `rejeitado`, `finalizado`) e `candidaturaEncerrada(etapa: string | null | undefined, status: string | null | undefined): boolean`. Teste em `src/lib/candidatura/__tests__/candidaturaEncerrada.test.ts` cobrindo os casos de `<behavior>`.

Em `entrevistaService.ts`, acrescentar `status` à allowlist do select de `getEntrevistaContexto` (a string passa a começar por `id, vaga_id, etapa_atual, status,`) e ao tipo `EntrevistaContextoRow` / ao objeto mapeado que o hub recebe (o nome do campo no contexto do hub pode ser `status`). Conferir os outros consumidores do contexto (`grep -rn "useEntrevistaContexto" src`) e não quebrar nenhum.

Em `HubCandidatoRH.tsx`: importar `candidaturaEncerrada` de `@/lib/candidatura/candidaturaEncerrada`; calcular `const encerrada = candidaturaEncerrada(etapaAtual, status)`; «Avançar» só renderiza se `proximaEtapa && !encerrada`; o `RejeitarCandidaturaDialog` só renderiza se `!encerrada`; quando `encerrada`, renderizar no lugar uma linha curta com `data-testid="hub-acoes-encerrada"` e o texto «Candidatura encerrada — não há ação de funil a tomar.» (mesmo tom do bloco). Retroceder e o CTA do workspace ficam como estão — são posicionais (varredura §4 ii). Comentário junto do gating citando C1/C1b e o plano 48-01 (o servidor também recusa).

Criar `src/features/hub-candidato/components/__tests__/hubAcoesEncerrada.test.tsx` no molde de `hubAcoesEmTriagem.test.tsx`, com os três cenários de hub de `<behavior>`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/lib/candidatura src/features/hub-candidato src/features/entrevista</automated>
    <fails_when>non-zero exit, "failed" in the summary, or the summary lists zero tests for src/lib/candidatura</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }</automated>
    <fails_when>non-zero exit — lint did not run (exit code other than 0 or 2), exit code and error count disagree, or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/lib/candidatura/candidaturaEncerrada.ts` exporta `candidaturaEncerrada`, `STATUS_TERMINAIS`, `ETAPAS_TERMINAIS` (export nomeado, nenhum default).
    - `grep -n "etapa_atual, status" src/features/entrevista/services/entrevistaService.ts` encontra a allowlist nova.
    - `HubCandidatoRH.tsx` contém `candidaturaEncerrada(` e `data-testid="hub-acoes-encerrada"`.
    - `hubAcoesEncerrada.test.tsx` e `hubAcoesEmTriagem.test.tsx` verdes; teste do helper verde com os 10 casos.
    - `tsc errors` ≤ 90.
  </acceptance_criteria>
  <done>O RH abre o hub de uma candidata eliminada por knockout e vê que ela está encerrada, sem botão que a devolveria ao funil.</done>
</task>

<task type="auto">
  <name>Task 2: o funil da aba «Por Vaga» e o painel do candidato no mesmo predicado</name>
  <files>src/components/pages/CandidatosRHPage.tsx, src/components/pages/DashboardCandidatoPage.tsx</files>
  <read_first>
    - `src/components/pages/CandidatosRHPage.tsx` (`:248-262` `funilEtapas`)
    - `src/components/pages/DashboardCandidatoPage.tsx` (`:57-67`, `:113`, `:148-159` — `hasDecisaoFinal`, INTOCÁVEL por D-12; `:313`, `:363-375`, `:459-464`)
    - `src/components/pages/__tests__/DashboardCandidatoPage.encerrada.test.tsx`, `DashboardCandidatoPage.funnel.test.tsx`
    - `src/lib/candidatura/candidaturaEncerrada.ts` (Task 1)
  </read_first>
  <action>
`CandidatosRHPage.tsx` (`funilEtapas`): uma candidatura só soma na sua etapa se a etapa for terminal (`aprovado`/`rejeitado`) OU se `!candidaturaEncerrada(c.etapa_atual, c.status)` — a mesma regra da CTE `volume` de `funil_kpis` no plano 48-01. Se `vagaCandidaturas` não carregar `status`, conferir a origem da lista e acrescentar `status` à allowlist correspondente (é leitura de coluna já exposta ao RH). Comentário curto citando C2/D6.

`DashboardCandidatoPage.tsx`: substituir a definição local de `STATUS_TERMINAIS`/`candidaturaEncerrada(status)` e a expressão de `emAndamento` pelo helper canônico (`candidaturaEncerrada(candidatura.etapa_atual, candidatura.status)` e `!candidaturaEncerrada(…)`), SEM mudar o comportamento de nenhum ramo — onde hoje só o status era passado, a etapa terminal implica status terminal em todas as linhas reais (varredura §2), então o resultado é o mesmo. `hasDecisaoFinal` (`:148-159`) não é tocado (D-12): a linha `return Boolean(candidatura.data_decisao_final || candidatura.feedback_rejeicao);` tem de continuar no arquivo.

Rodar a suíte inteira do front e o build; conferir em qual chunk o marcador `hub-acoes-encerrada` mora (`grep -rl "hub-acoes-encerrada" build/assets/` — `/rh/*` é chunk lazy).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && grep -q "return Boolean(candidatura.data_decisao_final || candidatura.feedback_rejeicao);" src/components/pages/DashboardCandidatoPage.tsx && npx vitest run src/components/pages src/lib/candidatura && npm run test:run</automated>
    <fails_when>non-zero exit; grep finds no hasDecisaoFinal return line (D-12 broken); or "failed" in any vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npm run build >/dev/null && grep -rl "hub-acoes-encerrada" build/assets/</automated>
    <fails_when>non-zero exit, or grep prints no file (the marker did not reach the bundle)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }</automated>
    <fails_when>non-zero exit — lint did not run (exit code other than 0 or 2), exit code and error count disagree, or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `CandidatosRHPage.tsx` importa `candidaturaEncerrada` e o usa em `funilEtapas`.
    - `DashboardCandidatoPage.tsx` importa `candidaturaEncerrada` de `@/lib/candidatura/candidaturaEncerrada`; não há mais `new Set(['rejeitado', 'finalizado'])` local no arquivo.
    - A linha de retorno de `hasDecisaoFinal` está idêntica.
    - `npm run test:run` verde; build contém o marcador em algum arquivo de `build/assets/`.
    - `tsc errors` ≤ 90.
  </acceptance_criteria>
  <done>O funil do RH e o painel do candidato decidem «encerrada» pela mesma função, e o painel não mudou de comportamento.</done>
</task>

<task type="auto">
  <name>Task 3: publicar — push do main e prova de que o marcador chegou ao ar</name>
  <files>src/features/hub-candidato/components/HubCandidatoRH.tsx</files>
  <read_first>
    - `CLAUDE.md` §«Esta via NÃO passa pelo git — e o código do front SAI POR OUTRO CANAL»
  </read_first>
  <action>
Ação para fora do repositório, autorizada pela divisão do operador (D-18: deploy corretivo roda sem pausa, reportando). Com as Tasks 1–2 commitadas, rodar `git push origin main`. A Vercel publica a partir do `main`; aguardar a publicação (reconsultar a cada ~30 s, até ~10 min) e conferir que o marcador `hub-acoes-encerrada` está servido por `https://rh.beautysmile.com.br` — o comando abaixo percorre o `index.html` e os chunks JS referenciados (inclusive os lazy de `/rh/*`). Nenhuma mudança de código nesta task (o arquivo listado é o dono do marcador).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && node -e "const M=process.argv[1],B='https://rh.beautysmile.com.br';(async()=>{const vis=new Set(),fila=['/'];let ok=false;while(fila.length&&vis.size<300){const p=fila.shift();if(vis.has(p))continue;vis.add(p);let t='';try{t=await (await fetch(B+p)).text()}catch{continue}if(t.includes(M)){ok=true;break}for(const m of t.matchAll(/(?:\/assets\/|\.\/)([\w.-]+\.js)/g))fila.push('/assets/'+m[1])}if(!ok){console.error('AUSENTE em PROD: '+M);process.exit(1)}console.log('PRESENTE em PROD: '+M)})()" "hub-acoes-encerrada"</automated>
    <fails_when>non-zero exit; git log origin/main..HEAD printed a commit; or "AUSENTE em PROD" in stderr</fails_when>
  </verify>
  <acceptance_criteria>
    - `git log --oneline origin/main..HEAD` vazio.
    - O crawler imprime `PRESENTE em PROD: hub-acoes-encerrada`.
  </acceptance_criteria>
  <done>O hub corrigido está no ar, não só no disco.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do RH → PostgREST (`candidaturas`) | o front lê `status` pela allowlist; escrita segue por RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-02-01 | Tampering | ação de funil sobre candidatura encerrada | medium | mitigate | gating no hub + guard no servidor (48-01, D3); o front é conveniência, o servidor é a autoridade |
| T-48-02-02 | Information Disclosure | ampliar a allowlist do contexto | low | accept | `status` já é legível pelo RH na lista (`candidaturasService`); nenhuma coluna sensível nova |
| T-48-02-03 | Repudiation | divergência entre helper TS e função SQL | low | mitigate | ambos citam um ao outro no docblock; tabela-verdade do SQL no smoke do 48-01 e casos iguais no teste TS |
| T-48-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `npx vitest run src/lib/candidatura src/features/hub-candidato src/features/entrevista src/components/pages` verde; `npm run test:run` verde.
- `tsc errors` ≤ 90.
- Marcador `hub-acoes-encerrada` presente em PROD; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
O RH não vê mais ação de funil para quem já saiu do funil, e o funil por vaga deixa de mostrar knockout como inscrição pendente.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `src/lib/candidatura/candidaturaEncerrada.ts` → `candidaturaEncerrada`, `STATUS_TERMINAIS`, `ETAPAS_TERMINAIS` | util TS | **novo** |
| `src/lib/candidatura/__tests__/candidaturaEncerrada.test.ts` | teste vitest | **novo** |
| `src/features/hub-candidato/components/__tests__/hubAcoesEncerrada.test.tsx` | teste vitest | **novo** |
| `EntrevistaContextoRow.status` | campo de tipo | **novo** |
| `HubCandidatoRH` (`data-testid="hub-acoes-encerrada"`) | componente | editado |
| `CandidatosRHPage.funilEtapas` | memo | editado |
| `DashboardCandidatoPage` (usa o helper) | página | editada (sem mudança de comportamento) |

## Premissas sinalizadas (probe de borda)

JORN-26 foi classificado `unclassified` pelo probe; as bordas `explicit` deste plano (os 10 casos do
helper) vêm da varredura §2–§3, não do probe. Retroceder não é gated por encerrada: a varredura
classificou-o como posicional (ii) e D-21 lista só Avançar/Rejeitar.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-02-SUMMARY.md` when done
</output>
