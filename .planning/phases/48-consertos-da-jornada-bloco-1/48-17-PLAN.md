---
phase: 48-consertos-da-jornada-bloco-1
plan: 17
type: execute
wave: 6
depends_on: ["48-04", "48-07", "48-11", "48-13", "48-16"]
files_modified:
  - docs/compliance/catalogo-vivo-44.json
  - docs/compliance/export-scope-rules.yaml
  - docs/compliance/pii-inventory.yaml
  - docs/compliance/export-allowlist.json
  - supabase/functions/_shared/exportAllowlist.ts
  - docs/compliance/pii-inventory.md
  - docs/compliance/__tests__/exportAllowlist.test.ts
  - docs/compliance/sql/05-export-allowlist-drift.sql
  - docs/compliance/recibo-exclusao.json
  - supabase/functions/_shared/reciboExclusao.ts
  - database.types.ts
autonomous: true
requirements: [JORN-24, JORN-27, JORN-19]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Toda coluna que esta fase criou em tabela inventariada tem veredito ESCRITO — export (`decisoes_por_coluna` de `export-scope-rules.yaml`) e classificação (`pii-inventory.yaml`) — com razão nomeada; esquecimento nunca é razão (fecho obrigatório do EXPORT-06): `analise_candidato_vaga.descartada_em`/`descartada_motivo`, `solicitacoes_dados.aviso_pedido_enviado_em`/`aviso_cancelamento_enviado_em`, `decisao_final.reaberta_em`/`prazo_nova_decisao_em`/`alerta_prazo_enviado_em`, e as 9 colunas novas de `decisao_final_historico`."
    - "Vereditos de export (decisão do planejador, critério do próprio arquivo — «se o RH enxerga sobre a pessoa, aparece na cópia»): a marca de descarte do D-02 ENTRA na cópia do titular (é o que torna auditável, para ele, que a análise que já recebeu foi descartada — premissa A6 do RESEARCH); `reaberta_em` e `prazo_nova_decisao_em` ENTRAM (estado do próprio caso, já visível ao candidato); `alerta_prazo_enviado_em` e os `aviso_*_enviado_em` NÃO entram (telemetria de entrega a funcionário/controle de envio); cada coluna de `decisao_final_historico` herda o veredito da coluna homônima de `decisao_final` (regra já escrita no arquivo: o histórico não pode entregar o que a corrente esconde)."
    - "`catalogo-vivo-44.json` recebe SÓ as colunas desta fase, MEDIDAS em PROD (`information_schema.columns`) na execução, com a data e a query registradas no `meta` — as 9 colunas de drift pré-existentes (`candidatos.faixa_etaria_materializada`, `candidaturas.encerrada_a_pedido_em`, 7 de `solicitacoes_dados`, inclusive `recibo_enviado_em`) ficam FORA, registradas como contato não consertado (não são desta fase)."
    - "Os artefatos gerados foram regenerados pelos geradores (nunca à mão) e os portões passam: `check:export-allowlist`, `check:pii-inventory-md`, `check:recibo-exclusao`, `check:matriz-retencao`, e o vitest de `docs/compliance`; a `versao` do `export-allowlist` sobe de `1.1.0` para `1.2.0` porque o conjunto de colunas exportadas mudou."
    - "A consulta `docs/compliance/sql/05-export-allowlist-drift.sql` rodada contra PROD (só leitura) NÃO lista nenhuma coluna desta fase como sem veredito."
    - "As EFs que embarcam os artefatos gerados foram redeployadas quando o artefato mudou (`exportar-meus-dados` por `exportAllowlist.ts`; `executar-direito-titular` por `reciboExclusao.ts`, se mudou) — senão a decisão escrita não chega à cópia real."
    - "`database.types.ts` regenerado pelo comando com `< /dev/null` (final da fase) e não vazio; `tsc` ≤ 90 (D-15)."
  artifacts:
    - path: docs/compliance/export-scope-rules.yaml
      provides: "vereditos de export das colunas novas"
      contains: "descartada_motivo"
    - path: docs/compliance/pii-inventory.yaml
      provides: "classificação das colunas novas"
      contains: "prazo_nova_decisao_em"
    - path: docs/compliance/export-allowlist.json
      provides: "artefato regenerado, versao 1.2.0"
      contains: "1.2.0"
  key_links:
    - from: "docs/compliance/export-scope-rules.yaml + pii-inventory.yaml + catalogo-vivo-44.json"
      to: "supabase/functions/_shared/exportAllowlist.ts → exportar-meus-dados"
      via: "node docs/compliance/sql/gen-export-allowlist.cjs + redeploy"
      pattern: "descartada_em"
  prohibitions:
    - requirement_id: JORN-24
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT deixar coluna nova de tabela com dado do titular sem veredito escrito — a omissão silenciosa é exatamente a falha que o fecho do EXPORT-06 existe para impedir"
    - requirement_id: JORN-27
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT editar à mão artefato gerado nem inventar coluna no catálogo — o catálogo é medido, os artefatos são regenerados"
---

<objective>
Fechar o inventário das colunas que a fase criou: cada uma com veredito de export e classificação
escritos, catálogo medido, artefatos regenerados, portões verdes e a cópia real do titular atualizada.

Purpose: o M8 é o milestone de LGPD. O `check:export-allowlist` compara com um catálogo versionado e fica
verde sozinho; quem acusa coluna sem veredito é a consulta de drift contra PROD, que hoje já lista 9
colunas pré-existentes. Esta fase não pode somar as suas a essa lista. A premissa A6 do RESEARCH (a
marca do D-02 entra na cópia) é decidida aqui.

Output: YAMLs com os vereditos, catálogo com as colunas medidas, artefatos regenerados, EFs
redeployadas, tipos finais.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): uma tabela de ponta a ponta — a marca do D-02 medida, decidida, regenerada e na cópia</name>
  <files>docs/compliance/catalogo-vivo-44.json, docs/compliance/export-scope-rules.yaml, docs/compliance/pii-inventory.yaml, docs/compliance/export-allowlist.json, supabase/functions/_shared/exportAllowlist.ts</files>
  <read_first>
    - `docs/compliance/sql/gen-export-allowlist.cjs` (`:1-60` as três fontes e o fecho; `:270-330` fecho de tabela/coluna; `:436-470` VEREDITO ÓRFÃO — decisão para coluna fora do catálogo quebra a geração)
    - `docs/compliance/catalogo-vivo-44.json` (`meta` e o formato das colunas por tabela)
    - `docs/compliance/export-scope-rules.yaml` (`:25-40` `meta.versao`; `:487-505` vereditos de `analise_candidato_vaga`; `:536-545` vereditos de `decisao_final`; `:640-650` a regra do histórico)
    - `docs/compliance/pii-inventory.yaml` (`:120-170` formato `{ classificacao, tipo, nota }`; entradas de `analise_candidato_vaga`, `decisao_final`, `decisao_final_historico`, `solicitacoes_dados`)
    - `docs/compliance/sql/01-pii-catalog.sql` (a query reprodutora do catálogo)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §F «Portões que uma coluna nova aciona»
  </read_first>
  <action>
Medir, só leitura, as colunas vivas de `analise_candidato_vaga` (`information_schema.columns`, com `data_type`/`udt_name` no formato que o catálogo usa — conferir a consulta (a)/(d) de `01-pii-catalog.sql`). Acrescentar a `catalogo-vivo-44.json` APENAS `descartada_em` e `descartada_motivo` nessa tabela, e registrar no `meta` uma nota de acréscimo (data da medição, a query usada, «colunas criadas pela Phase 48 — drift pré-existente não incluído»), sem alterar `medido_em` original nem os totais históricos de forma enganosa (se o `meta` tiver totais, acrescentar um campo de acréscimo em vez de reescrever os totais).

`export-scope-rules.yaml`: em `decisoes_por_coluna`, `analise_candidato_vaga.descartada_em` e `analise_candidato_vaga.descartada_motivo` com `export: true` e razão numerada no estilo do arquivo — marca de tratamento sem finalidade corrigido (D-02); o titular já recebeu a análise numa cópia anterior e a marca é o que lhe mostra que ela foi descartada; é estado de processo da análise, como `status`. `pii-inventory.yaml`: classificação das duas no mesmo padrão das vizinhas da tabela (a marca segue o destino da linha — conferir a disposição de `analise_candidato_vaga.status`).

Regenerar: `node docs/compliance/sql/gen-export-allowlist.cjs` (atualiza `export-allowlist.json` e `supabase/functions/_shared/exportAllowlist.ts`); subir `meta.versao` para `1.2.0` no YAML e regenerar de novo se o gerador propaga a versão. Conferir que as duas colunas aparecem na lista de `analise_candidato_vaga` do artefato. Rodar `npm run -s check:export-allowlist`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node docs/compliance/sql/gen-export-allowlist.cjs && npm run -s check:export-allowlist && node -e "const a=JSON.parse(require('fs').readFileSync('docs/compliance/export-allowlist.json','utf8'));const s=JSON.stringify(a);if(!s.includes('descartada_em')||!s.includes('descartada_motivo'))throw new Error('as colunas do D-02 nao entraram na allowlist');if(!s.includes('1.2.0'))throw new Error('versao nao subiu para 1.2.0');console.log('OK')"</automated>
    <fails_when>non-zero exit; a generator "ERRO DE FECHAMENTO"/"VEREDITO ÓRFÃO" in stderr; check failure; "nao entraram" or "versao nao subiu" in stderr</fails_when>
  </verify>
  <acceptance_criteria>
    - O catálogo tem as 2 colunas medidas, com a nota de acréscimo datada; nenhuma outra coluna foi acrescentada nesta task.
    - Os dois vereditos existem com razão; o artefato regenerado as exporta; `versao` = `1.2.0`.
    - `check:export-allowlist` verde.
  </acceptance_criteria>
  <done>A marca do D-02 passa a sair na cópia do titular — decidida por escrito, não por omissão.</done>
</task>

<task type="auto">
  <name>Task 2: as demais colunas da fase — vereditos, classificação, artefatos e portões</name>
  <files>docs/compliance/catalogo-vivo-44.json, docs/compliance/export-scope-rules.yaml, docs/compliance/pii-inventory.yaml, docs/compliance/pii-inventory.md, docs/compliance/__tests__/exportAllowlist.test.ts, docs/compliance/sql/05-export-allowlist-drift.sql</files>
  <read_first>
    - o estado deixado pela Task 1
    - `docs/compliance/export-scope-rules.yaml` (`:640-650` — a regra «mesma coluna, versões arquivadas»; `:695-725` vereditos de `solicitacoes_dados`)
    - `docs/compliance/__tests__/exportAllowlist.test.ts` (snapshots inline das chaves)
    - `docs/compliance/sql/gen-pii-md.cjs`, `docs/compliance/sql/gen-recibo-exclusao.cjs` (o que cada um lê)
  </read_first>
  <action>
Medir as colunas novas de `solicitacoes_dados`, `decisao_final` e `decisao_final_historico` e acrescentá-las ao catálogo (mesma nota de acréscimo). Vereditos em `export-scope-rules.yaml`, cada um com razão: `solicitacoes_dados.aviso_pedido_enviado_em`/`aviso_cancelamento_enviado_em` → `export: false` (telemetria do envio do aviso — controle de conta; não é dado sobre a pessoa; mesma natureza de `recibo_enviado_em`, cujo veredito segue pendente FORA desta fase); `decisao_final.reaberta_em`/`prazo_nova_decisao_em` → `export: true` (estado do próprio caso, visível ao candidato na página e no painel); `decisao_final.alerta_prazo_enviado_em` → `export: false` (entrega de alerta a funcionário); cada coluna nova de `decisao_final_historico` → o MESMO veredito da coluna homônima de `decisao_final` (onde a homônima não tiver veredito explícito, seguir a proveniência que o gerador dá a ela e escrever a decisão explicitamente). Classificação em `pii-inventory.yaml` para todas, no padrão das vizinhas de cada tabela.

Regenerar tudo: `node docs/compliance/sql/gen-export-allowlist.cjs`, `node docs/compliance/sql/gen-pii-md.cjs`, `node docs/compliance/sql/gen-recibo-exclusao.cjs`. Regenerar os DOIS blocos `VALUES` de `docs/compliance/sql/05-export-allowlist-drift.sql` com `node docs/compliance/sql/gen-export-allowlist.cjs --sql-values` e `--sql-values-excluidas` (GERADOS, nunca digitados — a asserção (k) de `exportAllowlist.test.ts` compara os blocos com o artefato e reprova se envelhecerem) e atualizar a contagem de pares no cabeçalho do arquivo com os números que os comandos imprimirem. Atualizar DE PROPÓSITO os snapshots inline de `exportAllowlist.test.ts` que mudaram (o diff dos snapshots tem de ser exatamente as colunas com `export: true` desta fase). Rodar os quatro `check:*` e `npx vitest run docs/compliance`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node docs/compliance/sql/gen-export-allowlist.cjs && node docs/compliance/sql/gen-pii-md.cjs && node docs/compliance/sql/gen-recibo-exclusao.cjs && npm run -s check:matriz-retencao && npm run -s check:pii-inventory-md && npm run -s check:recibo-exclusao && npm run -s check:export-allowlist && npx vitest run docs/compliance</automated>
    <fails_when>non-zero exit; "ERRO DE FECHAMENTO" or "VEREDITO ÓRFÃO" in stderr; any check failure; or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const y=require('fs').readFileSync('docs/compliance/export-scope-rules.yaml','utf8');for(const c of ['solicitacoes_dados.aviso_pedido_enviado_em','solicitacoes_dados.aviso_cancelamento_enviado_em','decisao_final.reaberta_em','decisao_final.prazo_nova_decisao_em','decisao_final.alerta_prazo_enviado_em','decisao_final_historico.reaberta_em','decisao_final_historico.prazo_nova_decisao_em','decisao_final_historico.revisao_veredito','analise_candidato_vaga.descartada_em','analise_candidato_vaga.descartada_motivo'])if(!y.includes(c+':'))throw new Error('sem veredito: '+c);console.log('vereditos escritos')"</automated>
    <fails_when>non-zero exit with "sem veredito:" in stderr</fails_when>
  </verify>
  <acceptance_criteria>
    - Todas as colunas novas da fase têm veredito e classificação; o catálogo tem só elas como acréscimo.
    - Os quatro `check:*` e o vitest de `docs/compliance` verdes (inclusive a asserção (k), que cruza os `VALUES` do `05-export-allowlist-drift.sql` com o artefato); snapshots atualizados só pelas colunas exportadas desta fase.
  </acceptance_criteria>
  <done>Nenhuma coluna criada pela fase ficou sem decisão escrita sobre ir ou não para a cópia do titular.</done>
</task>

<task type="auto">
  <name>Task 3: a decisão chega à cópia real — drift contra PROD, redeploys e tipos finais</name>
  <files>database.types.ts, docs/compliance/recibo-exclusao.json, supabase/functions/_shared/reciboExclusao.ts</files>
  <read_first>
    - `docs/compliance/sql/05-export-allowlist-drift.sql` (só leitura — seguro em PROD)
    - `efdeploy.cjs` (tabela `VERIFY_JWT`: `exportar-meus-dados: true`, `executar-direito-titular: true`)
  </read_first>
  <action>
Rodar a consulta de drift contra PROD, só leitura: `node p46apply.cjs run docs/compliance/sql/05-export-allowlist-drift.sql`. O resultado NÃO pode listar nenhuma coluna desta fase; as 9 pré-existentes podem aparecer — registrar a lista no SUMMARY como contato não consertado (fora da fase).

Redeployar quem embarca artefato alterado: `node efdeploy.cjs exportar-meus-dados` (sempre — `exportAllowlist.ts` mudou); `node efdeploy.cjs executar-direito-titular` SE `supabase/functions/_shared/reciboExclusao.ts` mudou na Task 2 (conferir com `git diff --stat`). Conferir no bundle de `exportar-meus-dados` a string `descartada_em`.

Regenerar os tipos finais da fase: `SUPABASE_ACCESS_TOKEN=$(security find-generic-password -s "Supabase CLI" -a supabase -w) npx supabase gen types typescript --project-id isljnozzlvckrgjjbjwp < /dev/null > database.types.ts` (0 octeto → `git checkout -- database.types.ts` e repetir). `tsc` ≤ 90. Commitar e fazer push (`origin/main..HEAD` vazio).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs run docs/compliance/sql/05-export-allowlist-drift.sql | tee /dev/stderr | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const novas=['descartada_em','descartada_motivo','aviso_pedido_enviado_em','aviso_cancelamento_enviado_em','reaberta_em','prazo_nova_decisao_em','alerta_prazo_enviado_em'];const achadas=novas.filter(c=>s.includes(c));if(achadas.length){console.error('drift ainda lista colunas da fase: '+achadas.join(', '));process.exit(1)}console.log('nenhuma coluna da fase no drift')})"</automated>
    <fails_when>non-zero exit with "drift ainda lista colunas da fase" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs exportar-meus-dados && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/exportar-meus-dados/body" | grep -a -c "descartada_em"</automated>
    <fails_when>non-zero exit from efdeploy, or the grep count printed is 0 (the decision did not reach the real export)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -s database.types.ts && grep -q "varrer_prazos_reabertura\|descartada_motivo" database.types.ts && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; } && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>non-zero exit; database.types.ts empty or without the phase's objects; lint did not run (exit code other than 0 or 2), exit code and error count disagree, or more than 90 "error TS" lines; or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - O drift contra PROD não lista coluna desta fase; a lista pré-existente está no SUMMARY.
    - `exportar-meus-dados` redeployada com a allowlist nova; `executar-direito-titular` redeployada se o recibo mudou.
    - `database.types.ts` final regenerado e não vazio; `tsc` ≤ 90; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>A cópia LGPD que o titular baixa hoje reflete as decisões desta fase — e o inventário não ganhou uma única coluna órfã.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| titular → EF `exportar-meus-dados` | a allowlist gerada decide o que sai na cópia |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-17-01 | Information Disclosure | coluna nova saindo na cópia sem decisão (ou dado de funcionário exportado) | high | mitigate | veredito explícito por coluna; `alerta_prazo_enviado_em`/`aviso_*` com `export: false`; `decisor` não é coluna nova (D-23 lê o histórico) |
| T-48-17-02 | Repudiation | coluna sem veredito somando ao drift | medium | mitigate | drift contra PROD conferido no verify |
| T-48-17-03 | Tampering | artefato gerado editado à mão | medium | mitigate | só os geradores escrevem; os `check:*` reprovam divergência |
| T-48-17-04 | Denial of Service | decisão escrita que não chega à cópia real | medium | mitigate | redeploy de `exportar-meus-dados` com marcador no bundle |
| T-48-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Vereditos e classificação escritos para as 16 colunas novas; catálogo medido; artefatos regenerados; `check:*` e vitest de compliance verdes.
- Drift contra PROD sem coluna da fase; `exportar-meus-dados` redeployada; tipos finais; `tsc` ≤ 90.
</verification>

<success_criteria>
O inventário de dados do titular termina a fase exatamente tão fechado quanto começou — com as colunas novas decididas por escrito.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| vereditos `decisoes_por_coluna` das 16 colunas novas | `export-scope-rules.yaml` | editado |
| classificação das 16 colunas novas | `pii-inventory.yaml` | editado |
| acréscimo medido ao catálogo | `catalogo-vivo-44.json` | editado |
| `export-allowlist.json` / `exportAllowlist.ts` (versao 1.2.0) | artefatos gerados | regenerados |
| `pii-inventory.md`, `recibo-exclusao.json`, `reciboExclusao.ts` | artefatos gerados | regenerados (se mudaram) |
| snapshots de `exportAllowlist.test.ts` | teste | atualizados |
| blocos `VALUES` de `05-export-allowlist-drift.sql` | consulta de drift | regenerados pelo gerador |
| `database.types.ts` | tipos gerados | regenerado (final) |

## Premissas sinalizadas (probe de borda)

A premissa A6 do RESEARCH (a marca do D-02 entra na cópia) é DECIDIDA aqui como `export: true`, com a
razão escrita no YAML — revisável pelo operador. As 9 colunas de drift pré-existentes ficam fora (não
são desta fase) e são registradas.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-17-SUMMARY.md` when done
</output>
