---
phase: 48-consertos-da-jornada-bloco-1
plan: 19
type: execute
wave: 7
depends_on: ["48-05"]
files_modified:
  - supabase/functions/gerar-devolutiva-bigfive/index.ts
  - supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts
  - src/features/avaliacao/components/DevolutivaBigFiveView.tsx
  - src/features/avaliacao/components/__tests__/DevolutivaBigFiveView.test.tsx
  - supabase/tests/p48_prova_prod.sql
  - .planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md
  - .planning/phases/48-consertos-da-jornada-bloco-1/48-18-PLAN.md
autonomous: false
requirements: [JORN-06]
origem: "sessão 1 do 48-18 (2026-09-21) — Defeitos 30 e 31, decisão do operador registrada no 48-PROVA-PROD.md §4"

must_haves:
  truths:
    - "Defeito 30: nenhuma devolutiva nova passa pela IA. Cada página serve o texto OFICIAL da faixa (`BAND_TEMPLATES[dim][banda]`), byte a byte; `callAi` não é chamado; a linha persiste `modelo_ia = null` e `prompt_version = 'template_oficial'` — o banco diz a verdade sobre como o texto nasceu."
    - "A personalização continua no código, desligada por uma constante exportada (`PERSONALIZACAO_IA_ATIVA = false`) e por um teste que falha se ela for ligada sem revisão — religar exige prompt corrigido e testado (decisão do operador), não uma edição de uma linha."
    - "O fechamento de imports da EF não muda (o `efdeploy.cjs` recusa subir se mudar): o caminho da IA segue importado, só não é exercido."
    - "Defeito 31: as cinco abas da devolutiva são clicáveis e legíveis em qualquer largura — a lista de abas cresce com a quebra de linha em vez de transbordar sob o cartão — sem alterar `src/components/ui/tabs.tsx`."
    - "JORN-06 só fecha com os dois consertos no ar e provados em PROD: uma devolutiva NOVA de conta de teste, gerada depois do deploy, com `prompt_version = 'template_oficial'`, páginas que abrem por «Pessoas com », e zero chamada `bigfive_devolutiva` em `ai_call_logs` para ela."
  prohibitions:
    - statement: "MUST NOT alterar o componente base de abas (`src/components/ui/tabs.tsx`)"
    - statement: "MUST NOT reescrever ou apagar as 2 devolutivas já gravadas em PROD sem confirmação do operador — a da +claude5 é a evidência do Defeito 30"
    - statement: "MUST NOT fazer deploy de EF pelo `deploy_edge_function` do MCP (transcrição) — a via é o `efdeploy.cjs`, rodado pelo operador"
---

<objective>
Servir o texto oficial da faixa no lugar da reescrita da IA, que inventava nome, percentil e cargo
(Defeito 30), e tornar as cinco abas da devolutiva clicáveis e legíveis (Defeito 31). Os dois são
consequência do JORN-06: a página só passou a existir para candidato real depois do conserto do 401
(48-05), e foi a primeira submissão pelo caminho real que os mostrou.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: EF — texto oficial da faixa, IA desligada por constante</name>
  <files>supabase/functions/gerar-devolutiva-bigfive/index.ts, supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts</files>
  <action>
`PERSONALIZACAO_IA_ATIVA = false` exportada. `handler` recebe `deps.personalizar?` (padrão: a
constante). Desligada: cada página = `BAND_TEMPLATES[dim][banda]`, `palavras = wordCount`, `callAi`
nunca chamado, upsert com `modelo_ia: null` e `prompt_version: 'template_oficial'`. Ligada: o caminho
atual, sem mudança. No `Deno.serve`, desligada, não resolve o prompt (uma falha de prompt não pode
derrubar uma devolutiva que não usa IA) e passa um `callAi` que lança se for chamado.
Testes: os que exercem a IA passam `personalizar: true`; novos — padrão serve o template exato,
zero `callAi`, marcadores de persistência; a constante é `false` (mordida deliberada).
  </action>
  <verify><automated>cd supabase/functions && deno test --allow-env --allow-read --no-check gerar-devolutiva-bigfive/__tests__/index.test.ts</automated></verify>
</task>

<task type="auto">
  <name>Task 2: front — as cinco abas clicáveis e legíveis</name>
  <files>src/features/avaliacao/components/DevolutivaBigFiveView.tsx, src/features/avaliacao/components/__tests__/DevolutivaBigFiveView.test.tsx</files>
  <action>
Causa: o `TabsList` base tem `h-9 w-fit` fixos; com `flex-wrap`, a 2ª linha transborda para fora da
caixa e fica sob o `TabsContent`, que a cobre. Conserto só no `className` da view (tailwind-merge
sobrescreve): lista `h-auto w-full flex-wrap`, gatilhos `flex-none h-auto min-h-[44px]` com cor
legível sobre o vidro e estado ativo contrastado. Teste: as cinco abas existem, a quinta ativa a
página N, e a lista não carrega mais altura fixa. Conferência visual real num harness temporário
(Vite + Playwright), 375 px e 1280 px, clicando a quinta aba.
  </action>
  <verify><automated>npx vitest run src/features/avaliacao/components/__tests__/DevolutivaBigFiveView.test.tsx</automated></verify>
</task>

<task type="auto">
  <name>Task 3: a prova — p1_devolutiva_gerada corrigida e a prova do template</name>
  <files>supabase/tests/p48_prova_prod.sql, .planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md, .planning/phases/48-consertos-da-jornada-bloco-1/48-18-PLAN.md</files>
  <action>
`p1_devolutiva_gerada` comparava `devolutivas_candidato.candidato_id` (uid do Auth) com
`candidatos.id` — nunca casaria; passa a filtrar por `candidatura_id IN cand`. Nova
`p2_devolutiva_template_oficial` (depende do deploy e de uma submissão nova). Sessão 2 do 48-18
ganha o passo (m).
  </action>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: deploy — EF pelo operador, front pelo push</name>
  <action>O operador roda `node efdeploy.cjs gerar-devolutiva-bigfive` no Terminal dele; Claude confere
  o bundle vivo pelo MCP (`PERSONALIZACAO_IA_ATIVA = false`, `template_oficial`) e o front publicado
  pelo marcador no chunk certo; `origin/main..HEAD` vazio.</action>
  <resume-signal>«deploy feito»</resume-signal>
</task>

</tasks>

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-19-SUMMARY.md` when done
</output>
