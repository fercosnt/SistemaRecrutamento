---
phase: 48-consertos-da-jornada-bloco-1
plan: 15
type: execute
wave: 4
depends_on: ["48-11"]
files_modified:
  - src/features/decisao/services/decisaoService.ts
  - src/features/decisao/services/__tests__/decisaoService.test.ts
  - src/features/decisao/components/RegistrarDecisaoForm.tsx
  - src/features/decisao/components/__tests__/RegistrarDecisaoForm.test.tsx
  - src/features/revisao/schemas/responderRevisaoSchema.ts
  - src/features/revisao/schemas/__tests__/responderRevisaoSchema.test.ts
  - src/features/revisao/components/ResponderRevisaoDialog.tsx
  - src/features/revisao/components/__tests__/ResponderRevisaoDialog.test.tsx
autonomous: true
requirements: [JORN-19]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Quando `registrar_decisao` recusa o decisor revertido (42501 com a mensagem D-23 do 48-11), o formulário `RegistrarDecisaoForm` mostra a recusa com clareza — `data-testid=\"decisao-recusa-decisor-revertido\"` e o texto «Você registrou a decisão que foi revertida na revisão. A nova decisão deste caso precisa ser registrada por outra pessoa do RH.» — em vez de um erro genérico de banco (regra 11 do operador; D-23)."
    - "`decisaoService.registrarDecisao` distingue a recusa D-23 (SQLSTATE 42501 E mensagem com `D-23`) de uma recusa de permissão comum (42501 sem D-23) e de falha de banco — três códigos de erro distintos na classe `DecisaoServiceError`."
    - "O RH é informado corretamente do que `revertida` faz agora (D-01): o diálogo de resposta e o schema deixam de dizer «A decisão original deixará de valer» e passam a dizer que a candidatura volta para «Decisão final», precisa de nova decisão em até 10 dias corridos, e que quem registrou a decisão original não poderá registrar a nova."
    - "Nenhuma mudança de comportamento no servidor neste plano — o bloqueio D-23 é server-side (48-11); a tela só o comunica. A opção `mantida` e seu texto não mudam."
    - "BORDA (explicit): 42501 sem a marca D-23 (ex.: papel não autorizado) continua caindo na mensagem de permissão genérica, não na de decisor revertido."
    - "`tsc` ≤ 90 (D-15); front publicado com `origin/main..HEAD` vazio e o marcador servido em PROD (D-16)."
  artifacts:
    - path: src/features/decisao/services/decisaoService.ts
      provides: "mapa SQLSTATE+mensagem → código de erro (D-23)"
      contains: "FORBIDDEN_DECISOR_REVERTIDO"
    - path: src/features/decisao/components/RegistrarDecisaoForm.tsx
      provides: "mensagem clara da recusa D-23"
      contains: "decisao-recusa-decisor-revertido"
    - path: src/features/revisao/components/ResponderRevisaoDialog.tsx
      provides: "cópia de revertida = reabrir"
  key_links:
    - from: "src/features/decisao/components/RegistrarDecisaoForm.tsx"
      to: "public.registrar_decisao (guard D-23, 42501)"
      via: "decisaoService.registrarDecisao → DecisaoServiceError('FORBIDDEN_DECISOR_REVERTIDO')"
      pattern: "FORBIDDEN_DECISOR_REVERTIDO"
  prohibitions:
    - requirement_id: JORN-19
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT dizer ao RH que reverter faz a decisão original «deixar de valer» ou aprova o candidato — reverter reabre para nova decisão (D-01)"
---

<objective>
O que o RH vê do JORN-19: o diálogo de resposta à revisão diz o que `revertida` faz agora (reabre, com
prazo, e o decisor original não decide de novo), e o formulário de decisão explica a recusa D-23 em vez
de mostrar um erro de banco.

Purpose: regra 11 do operador — D-23 é imposto no servidor e a tela surfaça a recusa com clareza. E a
cópia atual promete ao RH um efeito que o sistema nunca teve («a decisão original deixará de valer»).

Output: serviço e formulário com o mapeamento D-23; diálogo e schema com a cópia de D-01; publicado.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): a recusa D-23 de ponta a ponta na tela — serviço distingue, formulário explica</name>
  <files>src/features/decisao/services/decisaoService.ts, src/features/decisao/services/__tests__/decisaoService.test.ts, src/features/decisao/components/RegistrarDecisaoForm.tsx, src/features/decisao/components/__tests__/RegistrarDecisaoForm.test.tsx</files>
  <read_first>
    - `src/features/decisao/services/decisaoService.ts` (`:1-60` `DecisaoServiceError`; `:136-165` `registrarDecisao` que hoje embrulha tudo como `DATABASE_ERROR`)
    - `src/features/explicacao/services/explicacaoService.ts` (`:403-434` — molde de mapa SQLSTATE → desfecho)
    - `src/features/revisao/services/revisaoRedacaoService.ts` (`:257-271` — `42501 → FORBIDDEN`)
    - `src/features/decisao/components/RegistrarDecisaoForm.tsx` (`:90-100` o aviso «Já existe uma decisão registrada…»; onde o erro da mutação é exibido)
    - `supabase/migrations/20260921000012_p48_registrar_decisao_reabertura.sql` (a mensagem exata do guard D-23)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §19
  </read_first>
  <behavior>
    - erro `{ code: '42501', message: 'quem teve a decisao revertida nao registra a nova decisao deste caso (D-23)' }` → `DecisaoServiceError` com código `FORBIDDEN_DECISOR_REVERTIDO`
    - erro `{ code: '42501', message: 'forbidden' }` → código `FORBIDDEN`
    - outro erro → `DATABASE_ERROR` (inalterado)
    - formulário com a mutação falhando por `FORBIDDEN_DECISOR_REVERTIDO` → renderiza `data-testid="decisao-recusa-decisor-revertido"` com o texto do must_have; com `FORBIDDEN` → mensagem de permissão; com `DATABASE_ERROR` → a mensagem atual
  </behavior>
  <action>
`decisaoService.ts`: acrescentar `FORBIDDEN_DECISOR_REVERTIDO` e `FORBIDDEN` aos códigos de `DecisaoServiceError`; em `registrarDecisao`, mapear `error.code === '42501'` com mensagem contendo `D-23` → `FORBIDDEN_DECISOR_REVERTIDO`; `42501` sem ela → `FORBIDDEN`; o resto → `DATABASE_ERROR` como hoje. Comentário citando D-23 e o 48-11 (a decisão é do servidor; aqui só se traduz).

`RegistrarDecisaoForm.tsx`: quando o erro da mutação tiver código `FORBIDDEN_DECISOR_REVERTIDO`, mostrar um bloco com `data-testid="decisao-recusa-decisor-revertido"` e o texto «Você registrou a decisão que foi revertida na revisão. A nova decisão deste caso precisa ser registrada por outra pessoa do RH.» (tom e estilo dos avisos existentes do formulário); `FORBIDDEN` → «Você não tem permissão para registrar esta decisão.»; o resto como hoje.

Testes com os casos de `<behavior>`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/features/decisao</automated>
    <fails_when>non-zero exit, or "failed" in the summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }</automated>
    <fails_when>non-zero exit — lint did not run (exit code other than 0 or 2), exit code and error count disagree, or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `decisaoService.ts` contém `FORBIDDEN_DECISOR_REVERTIDO` e o mapeamento por SQLSTATE + marca D-23.
    - `RegistrarDecisaoForm.tsx` contém `data-testid="decisao-recusa-decisor-revertido"`.
    - Testes do serviço e do formulário verdes com os seis casos; `tsc errors` ≤ 90.
  </acceptance_criteria>
  <done>O RH que teve a decisão revertida entende, na tela, por que não pode registrar a nova — e quem pode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: o diálogo de resposta à revisão diz o que «reverter» faz agora</name>
  <files>src/features/revisao/schemas/responderRevisaoSchema.ts, src/features/revisao/schemas/__tests__/responderRevisaoSchema.test.ts, src/features/revisao/components/ResponderRevisaoDialog.tsx, src/features/revisao/components/__tests__/ResponderRevisaoDialog.test.tsx</files>
  <read_first>
    - `src/features/revisao/schemas/responderRevisaoSchema.ts` (`:60-72` — opções e ajudas)
    - `src/features/revisao/components/ResponderRevisaoDialog.tsx` (`:128-150` — `CONFIRMACAO_COPY`)
    - os dois testes correspondentes (pinam a cópia antiga)
  </read_first>
  <behavior>
    - opção `revertida`: rótulo «Reverter a decisão (reabrir a candidatura)»; ajuda «A candidatura volta para «Decisão final» e precisa de uma nova decisão em até 10 dias corridos, registrada por outra pessoa do RH.»
    - confirmação `revertida`: título «Reabrir a candidatura?»; forte «A candidatura volta para «Decisão final» e precisa de uma nova decisão em até 10 dias corridos.»; corpo menciona que quem registrou a decisão original não poderá registrar a nova e que o candidato é avisado por e-mail com a data; botão «Registrar e reabrir»
    - `mantida`: inalterada
    - nenhuma ocorrência de «deixa de valer» / «deixará de valer» nos dois arquivos
  </behavior>
  <action>
Trocar as constantes de cópia do ramo `revertida` no schema e no diálogo pelos textos de `<behavior>` (constantes, sem lógica). A promessa antiga não pode sobreviver nem em comentário sob `src/features/revisao/` (o verify reprova qualquer ocorrência). Atualizar os testes que pinam o texto antigo. Comentário curto citando D-01/D-10/D-23 e o 48-11.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/features/revisao && ! grep -rn "deixa de valer\|deixará de valer" src/features/revisao/</automated>
    <fails_when>non-zero exit; "failed" in the summary; or grep still finds the old promise</fails_when>
  </verify>
  <acceptance_criteria>
    - O schema e o diálogo têm os textos de `<behavior>`; a cópia antiga sumiu de `src/features/revisao/`.
    - Testes da pasta verdes.
  </acceptance_criteria>
  <done>O revisor sabe, antes de confirmar, que está reabrindo o caso com prazo — não aprovando ninguém.</done>
</task>

<task type="auto">
  <name>Task 3: publicar — suíte, build, push e marcador no ar</name>
  <files>src/features/decisao/components/RegistrarDecisaoForm.tsx</files>
  <read_first>
    - `CLAUDE.md` §«Esta via NÃO passa pelo git»
  </read_first>
  <action>
`npm run test:run`, `npm run build`, conferir o chunk (`grep -rl "decisao-recusa-decisor-revertido" build/assets/` — tela do RH, chunk lazy). Commitar, `git push origin main` (deploy corretivo — D-18), aguardar a Vercel e provar o marcador em PROD. Nenhuma mudança de código nesta task.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npm run test:run >/dev/null && npm run build >/dev/null && grep -rl "decisao-recusa-decisor-revertido" build/assets/</automated>
    <fails_when>non-zero exit; "failed" in vitest; or grep prints no file</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && node -e "const M=process.argv[1],B='https://rh.beautysmile.com.br';(async()=>{const vis=new Set(),fila=['/'];let ok=false;while(fila.length&&vis.size<300){const p=fila.shift();if(vis.has(p))continue;vis.add(p);let t='';try{t=await (await fetch(B+p)).text()}catch{continue}if(t.includes(M)){ok=true;break}for(const m of t.matchAll(/(?:\/assets\/|\.\/)([\w.-]+\.js)/g))fila.push('/assets/'+m[1])}if(!ok){console.error('AUSENTE em PROD: '+M);process.exit(1)}console.log('PRESENTE em PROD: '+M)})()" "decisao-recusa-decisor-revertido"</automated>
    <fails_when>non-zero exit; git log origin/main..HEAD printed a commit; or "AUSENTE em PROD" in stderr</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run test:run` verde; build com o marcador.
    - `git log --oneline origin/main..HEAD` vazio; `PRESENTE em PROD: decisao-recusa-decisor-revertido`.
  </acceptance_criteria>
  <done>As telas do RH sobre revisão e decisão estão no ar dizendo o que o sistema faz.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do RH → RPC `registrar_decisao` / `responder_revisao_decisao` | a autorização é do servidor; a tela só traduz |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-15-01 | Tampering | burlar D-23 pela UI | high | mitigate | bloqueio server-side (48-11); a tela não decide nada — só mapeia 42501+D-23 |
| T-48-15-02 | Repudiation | RH confirmando uma ação cujo efeito a tela descreve errado | medium | mitigate | cópia de D-01 no schema e no diálogo; teste pina; grep reprova a cópia antiga |
| T-48-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Vitest de `decisao` e `revisao` verdes; suíte inteira verde; `tsc` ≤ 90.
- Marcador em PROD; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
O RH vê o efeito real de reverter e entende a recusa D-23 sem precisar ler um erro de banco.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `DecisaoServiceError` códigos `FORBIDDEN_DECISOR_REVERTIDO`, `FORBIDDEN` | serviço | **novos** |
| bloco `data-testid="decisao-recusa-decisor-revertido"` | formulário | **novo** |
| cópia `revertida` em `responderRevisaoSchema` e `ResponderRevisaoDialog` (`CONFIRMACAO_COPY`) | constantes | editadas |

## Premissas sinalizadas (probe de borda)

JORN-19 foi classificado `unclassified` pelo probe; a borda `explicit` (42501 sem D-23 cai na mensagem
de permissão) é do planejador, para não confundir falta de papel com D-23.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-15-SUMMARY.md` when done
</output>
