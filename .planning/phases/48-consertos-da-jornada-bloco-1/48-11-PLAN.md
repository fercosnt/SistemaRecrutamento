---
phase: 48-consertos-da-jornada-bloco-1
plan: 11
type: execute
wave: 3
depends_on: ["48-01", "48-08"]
files_modified:
  - supabase/migrations/20260921000011_p48_reabertura_colunas_e_resposta.sql
  - supabase/migrations/20260921000012_p48_registrar_decisao_reabertura.sql
  - supabase/tests/p48_reabertura_smoke.sql
  - supabase/tests/p42_revisao_art20_smoke.sql
autonomous: true
requirements: [JORN-19]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O veredito `revertida` REABRE a candidatura na mesma transação de `responder_revisao_decisao`: `candidaturas.etapa_atual='decisao_final'`, `status='em_analise'`, aguardando nova decisão — nunca `aprovado` (D-01: reabrir, não reverter; nenhuma decisão automática, RNF-07a)."
    - "A reabertura grava prazo de 10 DIAS CORRIDOS: `decisao_final.reaberta_em = now()` e `decisao_final.prazo_nova_decisao_em` = início do dia (data da reabertura em `America/Sao_Paulo` + 11) no fuso de SP — ou seja, vence no FIM do 10º dia; a data exata dita ao candidato é (data da reabertura em SP + 10) (D-10; premissa A3 do RESEARCH decidida aqui e registrada no COMMENT)."
    - "A regressão `rejeitado → decisao_final` carrega justificativa PRÓPRIA no mesmo UPDATE — «Candidatura reaberta após revisão (Art. 20) — aguardando nova decisão até DD/MM/AAAA.» — e NÃO a `etapa_justificativa` residual (Defeito 17, fora do escopo, contornado) nem a justificativa do revisor. O histórico ganha 1 linha `rejeitado→decisao_final` com esse `criterio_texto` e `ator` = revisor; `trg_notif_transicao` não despacha para `decisao_final` (o aviso é o `revisao_respondida`)."
    - "`candidaturas.data_decisao_final` vira NULL na reabertura: o cartão «Entenda a decisão» some até existir decisão nova; a data antiga fica em `decisao_final.em`, no snapshot e no histórico (premissa A4 decidida aqui)."
    - "O veredito, `reaberta_em` e `prazo_nova_decisao_em` vão no MESMO UPDATE de `decisao_final` — um snapshot só na resposta (o 3b, fora do escopo, não é agravado)."
    - "Quem teve a decisão revertida NÃO registra a nova decisão daquele caso (D-23): `registrar_decisao` recusa com 42501 quando `auth.uid()` é o `por_usuario` de uma decisão cujo veredito foi `revertida` — na linha vigente (janela entre reabertura e nova decisão) OU em qualquer linha de `decisao_final_historico` daquela candidatura (depois da nova decisão, impedindo que o decisor sobrescreva a decisão de outro). Qualquer outro RH/admin decide."
    - "Na nova decisão (aprovado/rejeitado) de uma linha REABERTA, `registrar_decisao` zera o ciclo — `explicacao_solicitada_em`, `revisao_solicitada_em`, `revisao_veredito`, `revisao_resultado`, `revisao_por_usuario`, `revisao_respondida_em`, `reaberta_em`, `prazo_nova_decisao_em`, `alerta_prazo_enviado_em` — no MESMO upsert, depois de o snapshot AFTER UPDATE (que lê OLD) tê-lo arquivado nas colunas novas de `decisao_final_historico`. Sem isso, a nova rejeição apareceria como «revertida» e o Art. 20 ficaria inalcançável nela."
    - "BORDA (explicit): `em_espera` registrado durante a reabertura NÃO conta como nova decisão — o ciclo e o prazo continuam, e o alerta do vencimento dispara (premissa A5 decidida aqui; `em_espera` não é decisão comunicada, D-22). Redecisão fora de reabertura mantém o comportamento de hoje (não zera nada)."
    - "BORDA (explicit): `mantida` não reabre nada; `revertida` sobre decisão que não é `rejeitado` ou candidatura que não está `rejeitado/rejeitado` → 22023 sem nenhuma escrita."
    - "`registrar_decisao` deixa de falhar ABERTO sem JWT: guard `coalesce(v_role,'') NOT IN ('rh','administrador')` → 42501."
    - "A definição viva de `registrar_decisao` não existe em arquivo (patch dinâmico `20260826000004`): a migration parte do corpo lido por `pg_get_functiondef` e ABORTA se o `md5(prosrc)` vivo divergir do transcrito; o arquivo passa a conter o texto vivo completo (regra 10 do operador)."
    - "O revisor ≠ decisor no ato de responder continua valendo; candidato não chama RPC do RH (guards por `app_metadata.role`)."
    - "Migrations aplicadas por `p46apply.cjs` (D-16); `p42_revisao_art20_smoke.sql` h.2 corrigido NA MESMA ENTREGA (a asserção passa a mutar `candidaturas` — agora dentro de subtransação revertida, sem resíduo) (D-17)."
  artifacts:
    - path: supabase/migrations/20260921000011_p48_reabertura_colunas_e_resposta.sql
      provides: "colunas do ciclo, snapshot que as arquiva, responder_revisao_decisao que reabre"
      contains: "prazo_nova_decisao_em"
    - path: supabase/migrations/20260921000012_p48_registrar_decisao_reabertura.sql
      provides: "registrar_decisao com D-23, fail-closed e zeragem do ciclo"
      contains: "coalesce(v_role"
    - path: supabase/tests/p48_reabertura_smoke.sql
      provides: "reabrir, prazo, D-23 nas duas janelas, zeragem, em_espera, fail-closed"
  key_links:
    - from: "public.responder_revisao_decisao (revertida)"
      to: "public.candidaturas + avancar_etapa() + historico_candidatura"
      via: "UPDATE com etapa_justificativa própria na mesma transação"
      pattern: "Candidatura reaberta"
    - from: "public.registrar_decisao (upsert)"
      to: "public.snapshot_decisao_final → decisao_final_historico"
      via: "AFTER UPDATE lê OLD e arquiva o ciclo antes da zeragem"
      pattern: "revisao_veredito"
  prohibitions:
    - requirement_id: JORN-19
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT aprovar ou rejeitar automaticamente em nenhum ramo — nem na reabertura, nem no vencimento do prazo (D-01, D-10, RNF-07a)"
    - requirement_id: JORN-19
      category: fairness
      status: resolved
      verification: judgment
      statement: "MUST NOT permitir que quem teve a decisão revertida registre a nova decisão daquele caso, nem sobrescreva a de outro depois (D-23)"
    - requirement_id: JORN-19
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT escrever na trilha visível ao candidato a justificativa do revisor, nem reaproveitar a justificativa residual de outra transição"
---

<objective>
O núcleo do JORN-19 no banco: o veredito `revertida` reabre a candidatura em `decisao_final` com prazo
de 10 dias corridos, a nova decisão é registrada pelo upsert que já existe — por outra pessoa (D-23) — e
o ciclo de revisão é arquivado e zerado para a decisão nova.

Purpose: Defeito 19 — hoje o veredito é gravado e `decisao`, `etapa_atual` e `status` seguem
`rejeitado`, enquanto o e-mail diz que «a decisão foi revista». Desenho A do RESEARCH §E.3 (reabrir
dentro de `responder_revisao_decisao`), escolhido por menor superfície: `UNIQUE(candidatura_id)`
intacto e nenhum leitor de `decisao_final` quebra. Depende do 48-08 (chaves do ciclo): sem elas, a 2ª
revisão seria engolida.

Output: duas migrations aplicadas, smoke novo, `p42_revisao_art20_smoke` corrigido. Os avisos (e-mail
com a data, alerta ao RH) são do 48-13; as telas, do 48-14 e 48-15.

Orçamento: a estimativa (120k, confiança baixa — sem amostra calibrada) passa do smart-zone e é aceita
em vez de fatiar: as três tasks são uma cadeia de `CREATE OR REPLACE` sobre corpo vivo + smoke que tem de
viajar junta — a Task 2 lê as colunas que a Task 1 cria, a zeragem depende do snapshot que a Task 1
redefine, e as duas estendem o mesmo `p48_reabertura_smoke.sql`. Dois planos aqui seriam dois executores
em sequência sobre o mesmo smoke, sem reduzir o que cada um precisa ler.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-CONTEXT.md

`48-RESEARCH.md` e `48-PATTERNS.md` NÃO entram inteiros: cada task lê só as seções do seu `<read_first>`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer) [BLOCKING]: reabrir de ponta a ponta — colunas do ciclo, snapshot que as arquiva, `responder_revisao_decisao` que reabre; aplicado e provado</name>
  <reversibility rating="costly">`decisao_final` ganha semântica de ciclo reaberto lida por EF, página e painel (planos 48-13..48-15); desfazer exige migração coordenada desses leitores — o desenho já foi decidido pelo operador (D-01), então não há checkpoint.</reversibility>
  <files>supabase/migrations/20260921000011_p48_reabertura_colunas_e_resposta.sql, supabase/tests/p48_reabertura_smoke.sql</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §E INTEIRA (E.1 definições vivas; E.3 Desenho A passos 1–5)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §1 (cabeçalho, transporte e ACL de toda migration), §5a, §5c, §5d, §6
    - `supabase/migrations/20260730000002_p42_revisao_art20_authz_fail_closed.sql` (`:88-171` — `responder_revisao_decisao`: guards `:103-155`, UPDATE `:157-163`)
    - `supabase/migrations/20260709000011_decisao_final_historico.sql` (`:90-103` — `snapshot_decisao_final`)
    - `supabase/migrations/20260805000001_p45_pedido_exclusao.sql` (`:204-259` — ADD COLUMN sem IF NOT EXISTS + COMMENT)
    - a função `avancar_etapa()` viva (regressão exige `etapa_justificativa` no MESMO UPDATE) — ler por `pg_get_functiondef`
    - `supabase/tests/p42_revisao_art20_smoke.sql` (fixture e impersonação de revisor/decisor)
  </read_first>
  <action>
Ler o vivo, só leitura, com md5: `responder_revisao_decisao`, `snapshot_decisao_final`, `avancar_etapa`, e as colunas de `decisao_final`/`decisao_final_historico` (`information_schema.columns`). Registrar no SUMMARY. Rodar a varredura de portões (achados que citem `decisao_final`, `responder_revisao_decisao`, `snapshot_decisao_final`).

Migration `20260921000011_p48_reabertura_colunas_e_resposta.sql` (cabeçalho do M8 com O QUE ESTAVA ERRADO — Defeito 19 medido —, transporte `p46apply.cjs migrate`, sem `BEGIN;`), pré-portão de md5 para as duas funções redefinidas, e:
(a) `ALTER TABLE public.decisao_final ADD COLUMN reaberta_em timestamptz, ADD COLUMN prazo_nova_decisao_em timestamptz, ADD COLUMN alerta_prazo_enviado_em timestamptz` + CHECK nomeado `(reaberta_em IS NULL) = (prazo_nova_decisao_em IS NULL)` + `COMMENT ON COLUMN` em cada (D-01/D-10; A3 — a fórmula do prazo; o alerta é idempotência da varredura do 48-13);
(b) `ALTER TABLE public.decisao_final_historico ADD COLUMN` (todas nullable, sem CHECK copiado): `explicacao_solicitada_em`, `revisao_solicitada_em`, `revisao_veredito`, `revisao_resultado`, `revisao_por_usuario`, `revisao_respondida_em`, `reaberta_em`, `prazo_nova_decisao_em`, `alerta_prazo_enviado_em`, com os mesmos tipos das colunas de `decisao_final` + COMMENT (arquivo do ciclo; o 3b segue fora de escopo — a tabela continua ganhando linha em todo UPDATE);
(c) `CREATE OR REPLACE FUNCTION public.snapshot_decisao_final()` a partir do vivo, só acrescentando as colunas novas ao INSERT (valores de `OLD`);
(d) `CREATE OR REPLACE FUNCTION public.responder_revisao_decisao(...)` a partir do vivo: guards todos mantidos (inclusive `v_uid = por_usuario` → 42501 e o de idempotência); ANTES do UPDATE do veredito, quando `p_veredito = 'revertida'`, exigir `decisao_final.decisao = 'rejeitado'` e `candidaturas` com `etapa_atual='rejeitado' AND status='rejeitado'`, senão `RAISE … USING ERRCODE = '22023'` («nada a reabrir»); calcular `v_data_limite := (now() AT TIME ZONE 'America/Sao_Paulo')::date + 10` e `v_prazo := ((v_data_limite + 1)::timestamp AT TIME ZONE 'America/Sao_Paulo')`; o UPDATE do veredito ganha, no MESMO statement, `reaberta_em` e `prazo_nova_decisao_em` (preenchidos só no ramo `revertida`); depois, no ramo `revertida`, `UPDATE public.candidaturas SET etapa_atual='decisao_final', status='em_analise', etapa_justificativa = format('Candidatura reaberta após revisão (Art. 20) — aguardando nova decisão até %s.', to_char(v_data_limite,'DD/MM/YYYY')), data_decisao_final = NULL WHERE id = p_candidatura_id`. Grants vivos reafirmados (`anon` = false);
(e) `COMMENT ON FUNCTION` de `responder_revisao_decisao` descrevendo o ramo novo (D-01, D-10, A3, A4) e que o aviso ao candidato é o `revisao_respondida` (48-13 corrige a cópia);
(f) `DO` final: colunas presentes; definição de `responder_revisao_decisao` contém `America/Sao_Paulo`, `em_analise` e `Candidatura reaberta`; a de `snapshot_decisao_final` contém `revisao_veredito`.

Aplicar: `node p46apply.cjs migrate supabase/migrations/20260921000011_p48_reabertura_colunas_e_resposta.sql`.

Smoke `supabase/tests/p48_reabertura_smoke.sql` (contador, gate, SELECT final), parte 1 — tudo numa subtransação que reverte (o `pg_net` só entrega pós-COMMIT: nenhum e-mail), sobre uma candidatura real de conta de teste em andamento, com ids REAIS de três usuários RH/admin ativos lidos na execução (A decide, B revisa, C redecide — FK de `por_usuario`): (a) A `registrar_decisao` rejeitado; titular `solicitar_revisao_decisao`; B `responder_revisao_decisao` `revertida` (justificativa ≥ 50) → `etapa_atual='decisao_final'`, `status='em_analise'`, `data_decisao_final IS NULL`, 1 linha nova em `historico_candidatura` `rejeitado→decisao_final` com `criterio_texto` começando por «Candidatura reaberta» e `ator` = B; `reaberta_em` preenchido; `prazo_nova_decisao_em` igual à fórmula de SP; o snapshot arquivou a linha anterior; (b) caminho `mantida` numa segunda fixture → nada muda em `candidaturas`; (c) `revertida` sobre decisão `aprovado` → 22023 e zero escrita; (d) A tenta responder a própria revisão → 42501 (invariante preservada). Rodar: `node p46apply.cjs run supabase/tests/p48_reabertura_smoke.sql`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260921000011_p48_reabertura_colunas_e_resposta.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');for(const t of ['ADD COLUMN reaberta_em','ADD COLUMN prazo_nova_decisao_em','ADD COLUMN alerta_prazo_enviado_em','public.decisao_final_historico','public.snapshot_decisao_final','public.responder_revisao_decisao','America/Sao_Paulo','em_analise','Candidatura reaberta','data_decisao_final = NULL','22023','md5'])if(!c.includes(t))throw new Error('falta: '+t);if(/etapa_atual\s*=\s*'aprovado'|status\s*=\s*'aprovado_proxima'/.test(c))throw new Error('reabrir NAO aprova (D-01)');console.log('OK')" && node p46apply.cjs migrate supabase/migrations/20260921000011_p48_reabertura_colunas_e_resposta.sql && node p46apply.cjs run supabase/tests/p48_reabertura_smoke.sql</automated>
    <fails_when>non-zero exit; "falta:" or "reabrir NAO aprova" in stderr; "md5 do ledger BATE" absent; or "FAIL" in the smoke output</fails_when>
  </verify>
  <acceptance_criteria>
    - Em PROD: `decisao_final` tem as 3 colunas e o CHECK de coerência; `decisao_final_historico` tem as 9 colunas novas; as duas funções contêm os marcadores do `DO`.
    - `p48_reabertura_smoke` parte 1 (a)–(d) verde, sem resíduo.
    - A fórmula do prazo e as decisões A3/A4 estão no COMMENT e no SUMMARY.
  </acceptance_criteria>
  <done>«Revertida» passa a reabrir de fato: a candidatura volta a «Decisão final», aguardando decisão nova, com prazo — e ninguém foi aprovado por um sistema.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: `registrar_decisao` a partir do corpo vivo — D-23, fail-closed e a zeragem do ciclo na nova decisão</name>
  <reversibility rating="costly">`registrar_decisao` é o write-path único da decisão final e sua definição viva só existia em memória do banco; o arquivo passa a ser a fonte — desfazer exige outro CREATE OR REPLACE conferido por md5.</reversibility>
  <files>supabase/migrations/20260921000012_p48_registrar_decisao_reabertura.sql, supabase/tests/p48_reabertura_smoke.sql</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §5b (como capturar e o pré-portão) e §1 (guard fail-closed)
    - `supabase/migrations/20260826000004_registrar_decisao_grava_a_data_e_o_status.sql` (`:46-84` — o patch dinâmico: por que não há texto vivo em arquivo)
    - `supabase/migrations/20260709000012_registrar_decisao_amend.sql` (a base estática)
    - `supabase/migrations/20260805000007_p45_retirada_e_evento.sql` (`:648-698` — BLOCO G: formato do md5/length no cabeçalho e o `DO` que aborta)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §E.3 passo 3 e §J (fail-open de `registrar_decisao`)
    - `supabase/migrations/20260921000011_p48_reabertura_colunas_e_resposta.sql` (Task 1)
  </read_first>
  <action>
Passo 1 — ler o vivo, só leitura: `node p46apply.cjs sql "SET TRANSACTION READ ONLY; select md5(p.prosrc), length(p.prosrc), pg_get_functiondef(p.oid) from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public' and p.proname='registrar_decisao'"`. Anotar md5 e length no cabeçalho (formato do BLOCO G).

Passo 2 — migration `20260921000012_p48_registrar_decisao_reabertura.sql`: `DO` de pré-portão que aborta se o `md5(prosrc)` vivo divergir do anotado; depois `CREATE OR REPLACE` COMPLETO transcrito do vivo (NÃO outro `replace()` cego — o patch dinâmico é a razão de não haver texto vivo em arquivo), com exatamente estas mudanças:
(i) guard de papel `coalesce(v_role, '') NOT IN ('rh','administrador')` → 42501 (fecha o fail-open);
(ii) guard D-23 antes do upsert: se existe linha vigente em `decisao_final` da candidatura com `revisao_veredito = 'revertida'` e `por_usuario = auth.uid()`, OU existe linha em `decisao_final_historico` da candidatura com `revisao_veredito = 'revertida'` e `por_usuario = auth.uid()` → `RAISE EXCEPTION 'quem teve a decisao revertida nao registra a nova decisao deste caso (D-23)' USING errcode = '42501'`. Vale para qualquer valor de `p_decisao` (bloqueio duro);
(iii) no `ON CONFLICT (candidatura_id) DO UPDATE SET`, cada coluna do ciclo (`explicacao_solicitada_em`, `revisao_solicitada_em`, `revisao_veredito`, `revisao_resultado`, `revisao_por_usuario`, `revisao_respondida_em`, `reaberta_em`, `prazo_nova_decisao_em`, `alerta_prazo_enviado_em`) recebe `CASE WHEN public.decisao_final.reaberta_em IS NOT NULL AND EXCLUDED.decisao IN ('aprovado','rejeitado') THEN NULL ELSE public.decisao_final.<coluna> END` — zera só na nova decisão de uma linha reaberta; `em_espera` e redecisões fora de reabertura mantêm tudo como hoje. O CHECK «resposta completa» fica satisfeito (tudo nulo junto).
Tudo o mais idêntico ao vivo (movimento de etapa/status, `data_decisao_final = now()`, `etapa_justificativa`). Grants reafirmados como estão hoje. `COMMENT ON FUNCTION` atualizado (D-23, A5, fail-closed, a zeragem e por que ela depende do snapshot lendo OLD). `DO` final: definição contém `coalesce(v_role`, `D-23`, `reaberta_em IS NOT NULL`, e o `md5(prosrc)` novo é registrado por NOTICE para o SUMMARY.

Passo 3 — aplicar: `node p46apply.cjs migrate supabase/migrations/20260921000012_p48_registrar_decisao_reabertura.sql`.

Passo 4 — estender `p48_reabertura_smoke.sql`, parte 2 (subtransação que reverte, mesma fixture da parte 1 já reaberta): (e) A (decisor revertido) `registrar_decisao` → 42501; (f) C `registrar_decisao` `em_espera` → aceito, e `reaberta_em`/`prazo_nova_decisao_em`/`revisao_veredito` continuam preenchidos; (g) A de novo → 42501 (agora a trava vem do histórico, porque o snapshot do `em_espera` arquivou a linha revertida); (h) C `registrar_decisao` `aprovado` → aceito; candidatura `aprovado/finalizado`; todas as colunas do ciclo NULL na linha vigente; existe em `decisao_final_historico` uma linha com `revisao_veredito='revertida'` e `por_usuario` = A; (i) A tenta `registrar_decisao` `rejeitado` → 42501 (não sobrescreve a decisão de C); (j) sem claims → 42501 (fail-closed); (k) redecisão FORA de reabertura numa segunda fixture com revisão pendente não zera `revisao_solicitada_em` (comportamento de hoje preservado); (l) NEGATIVA — contagens de `decisao_final`, `decisao_final_historico`, `historico_candidatura`, `candidaturas` e da fila do `pg_net` iguais antes e depois do smoke. Rodar o smoke.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260921000012_p48_registrar_decisao_reabertura.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');if(/replace\s*\(\s*pg_get_functiondef/i.test(c))throw new Error('patch dinamico proibido: transcrever o corpo vivo completo');for(const t of ['CREATE OR REPLACE FUNCTION public.registrar_decisao','coalesce(v_role','D-23','42501','decisao_final_historico','reaberta_em IS NOT NULL',\"EXCLUDED.decisao IN ('aprovado','rejeitado')\",'ON CONFLICT (candidatura_id)','md5'])if(!c.includes(t))throw new Error('falta: '+t);if(!/length/i.test(s)||!/[0-9a-f]{32}/.test(s))throw new Error('cabecalho sem md5/length do vivo');console.log('OK')" && node p46apply.cjs migrate supabase/migrations/20260921000012_p48_registrar_decisao_reabertura.sql && node p46apply.cjs run supabase/tests/p48_reabertura_smoke.sql</automated>
    <fails_when>non-zero exit; "falta:", "patch dinamico proibido" or "cabecalho sem md5" in stderr; "md5 do ledger BATE" absent; or "FAIL" in the smoke output</fails_when>
  </verify>
  <acceptance_criteria>
    - O cabeçalho da migration registra `md5(prosrc)` e `length` do vivo; o pré-portão aborta em divergência.
    - Em PROD, `registrar_decisao` contém `coalesce(v_role`, o guard D-23 e a zeragem condicionada à reabertura.
    - `p48_reabertura_smoke` partes 1 e 2, (a)–(l), verde e sem resíduo.
  </acceptance_criteria>
  <done>A nova decisão de um caso reaberto é de outra pessoa, nasce limpa do ciclo anterior, e o ciclo anterior fica arquivado.</done>
</task>

<task type="auto">
  <name>Task 3: o smoke do Art. 20 que muda de efeito, a regressão, e a publicação</name>
  <files>supabase/tests/p42_revisao_art20_smoke.sql</files>
  <read_first>
    - `supabase/tests/p42_revisao_art20_smoke.sql` (`:138-153` teardown; `:598` h.2 — chama `responder_revisao_decisao(…,'revertida',…)` sobre candidatura real de `candidato.funil@teste.com` sem decisão)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §I (linha do `p42_revisao_art20_smoke`)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-PORTOES.md`
  </read_first>
  <action>
`p42_revisao_art20_smoke.sql` (h.2): com o 48-11 o veredito `revertida` passa a exigir candidatura `rejeitado/rejeitado` (senão 22023) e a MUTAR `candidaturas` + gravar histórico — o teardown atual (só `decisao_final`/histórico da decisão) deixaria resíduo. Reescrever a asserção dentro de uma subtransação que reverte (SQLSTATE próprio capturado): preparar a fixture no estado exigido (decisão `rejeitado` registrada na própria subtransação, candidatura `rejeitado/rejeitado`), responder `revertida` com um revisor ≠ decisor, asserir o efeito NOVO (etapa `decisao_final`, `status='em_analise'`, `reaberta_em` preenchido) e reverter. Acrescentar asserção NEGATIVA fora da subtransação: as contagens de `candidaturas` (por etapa/status da fixture), `historico_candidatura`, `decisao_final` e `decisao_final_historico` são as de antes. Comentar no bloco por que a asserção mudou de efeito (D-01, plano 48-11) e registrar a mudança no SUMMARY (a linha correspondente de `48-VARREDURA-PORTOES.md` já aponta este plano como o dono).

Regressão: rodar `p42_revisao_art20_smoke.sql`, `p42_notif_revisao_smoke.sql`, `p48_dedupe_smoke.sql` (48-08 — usa `registrar_decisao` e `responder_revisao_decisao` `mantida`) e `oper31_rejeitar_candidatura_smokes.sql`.

Publicar: a reabertura é visível na tela (etapa/status do candidato e do RH) e o front que a descreve sai nos planos 48-14/48-15 desta fase (D-18 — o passo seguinte pertence a um plano desta fase). Fazer o push dos commits pendentes: `git push origin main`, e conferir `git log --oneline origin/main..HEAD` vazio.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs run supabase/tests/p42_revisao_art20_smoke.sql && node p46apply.cjs run supabase/tests/p42_notif_revisao_smoke.sql && node p46apply.cjs run supabase/tests/p48_dedupe_smoke.sql && node p46apply.cjs run supabase/tests/oper31_rejeitar_candidatura_smokes.sql</automated>
    <fails_when>non-zero exit, or "FAIL" in any output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && echo "origin/main em dia"</automated>
    <fails_when>non-zero exit (git log origin/main..HEAD printed at least one commit)</fails_when>
  </verify>
  <acceptance_criteria>
    - `p42_revisao_art20_smoke.sql` h.2 roda dentro de subtransação revertida e assere o efeito novo; a asserção negativa prova zero resíduo.
    - Os quatro smokes de regressão verdes.
    - `git log --oneline origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>A reabertura está no ar, provada, e os portões do Art. 20 continuam mordendo sem deixar sujeira em PROD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH/admin (JWT) → `responder_revisao_decisao` / `registrar_decisao` | write-paths SECURITY DEFINER da decisão final |
| candidato (JWT) → `solicitar_revisao_decisao` | abre o ciclo; não pode responder nem decidir |
| trilha (`historico_candidatura`, `decisao_final_historico`) → cópia LGPD e explicação | o que é escrito aqui chega ao titular |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-11-01 | Elevation of Privilege | `registrar_decisao` falhando aberto sem JWT | high | mitigate | `coalesce(v_role,'')`; smoke (j) |
| T-48-11-02 | Tampering | decisor revertido redecidindo ou sobrescrevendo | high | mitigate | guard D-23 na linha vigente e no histórico; smoke (e), (g), (i) |
| T-48-11-03 | Tampering | revisor = decisor | high | mitigate | guard existente preservado; smoke (d) |
| T-48-11-04 | Repudiation | ciclo anterior perdido na redecisão | medium | mitigate | snapshot AFTER UPDATE arquiva OLD nas colunas novas antes da zeragem; smoke (h) |
| T-48-11-05 | Information Disclosure | justificativa do revisor ou residual na trilha visível | medium | mitigate | `etapa_justificativa` com texto próprio; smoke (a) confere o `criterio_texto` |
| T-48-11-06 | Tampering | CREATE OR REPLACE apagando divergência viva de `registrar_decisao` | high | mitigate | pré-portão de md5 do `prosrc` vivo; corpo completo transcrito |
| T-48-11-07 | Denial of Service | smoke deixando candidatura real reaberta | medium | mitigate | subtransações revertidas + asserções negativas de contagem |
| T-48-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Duas migrations aplicadas (md5 BATE), a segunda com pré-portão sobre o corpo vivo.
- `p48_reabertura_smoke`, `p42_revisao_art20_smoke`, `p42_notif_revisao_smoke`, `p48_dedupe_smoke`, `oper31` verdes.
- `origin/main..HEAD` vazio.
</verification>

<success_criteria>
O veredito `revertida` produz uma candidatura reaberta com prazo, uma nova decisão só pode vir de outra pessoa, e a trilha guarda o ciclo inteiro.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `decisao_final.reaberta_em`, `.prazo_nova_decisao_em`, `.alerta_prazo_enviado_em` + CHECK de coerência | colunas/constraint | **novas** |
| `decisao_final_historico.{explicacao_solicitada_em, revisao_solicitada_em, revisao_veredito, revisao_resultado, revisao_por_usuario, revisao_respondida_em, reaberta_em, prazo_nova_decisao_em, alerta_prazo_enviado_em}` | colunas de arquivo | **novas** |
| `public.snapshot_decisao_final()` | função de trigger | editada (arquiva o ciclo) |
| `public.responder_revisao_decisao(uuid, text, text)` | RPC | editada (ramo `revertida` reabre) |
| `public.registrar_decisao(uuid, decisao_final_resultado, text)` | RPC | editada (D-23, fail-closed, zeragem) — texto vivo passa a existir em arquivo |
| `supabase/migrations/20260921000011_…`, `…000012_…` | migrations | **novas** |
| `supabase/tests/p48_reabertura_smoke.sql` | smoke | **novo** |
| `supabase/tests/p42_revisao_art20_smoke.sql` | smoke | editado (h.2) |

## Premissas sinalizadas (probe de borda)

JORN-19 foi classificado `unclassified` pelo probe. A pergunta «o decisor original pode redecidir depois
da reabertura?» (RESEARCH, Open Question 5) NÃO está em aberto: foi fechada pelo operador em **D-23** —
quem teve a decisão revertida não registra a nova decisão daquele caso; qualquer outro RH/admin decide.
Este plano a implementa como bloqueio duro server-side na Task 2 (guard 42501 em `registrar_decisao`,
na linha vigente e no histórico; smoke (e), (g), (i)). As únicas premissas decididas aqui como
discrição do planejador são A3 (fórmula do prazo em SP), A4 (zerar `data_decisao_final`) e A5
(`em_espera` não é nova decisão) — sem reabrir D-01/D-10/D-23 — e ficam no COMMENT das funções para o
operador revisar no 48-18. Contato registrado, não consertado: 3b (snapshot em todo UPDATE) e 17 (justificativa
que gruda) — contornados, não consertados.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-11-SUMMARY.md` when done
</output>
