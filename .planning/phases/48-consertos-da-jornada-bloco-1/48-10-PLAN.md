---
phase: 48-consertos-da-jornada-bloco-1
plan: 10
type: execute
wave: 3
depends_on: ["48-06", "48-07", "48-08"]
files_modified:
  - supabase/functions/notificar-candidato/helpers.ts
  - supabase/functions/notificar-candidato/index.ts
  - supabase/functions/_shared/email-config.ts
  - supabase/functions/_shared/email-templates.ts
  - supabase/migrations/20260921000010_p48_cognitivo_liberado_notifica.sql
  - supabase/functions/_shared/__tests__/email-templates.test.ts
  - supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts
  - supabase/functions/notificar-candidato/__tests__/vocabulario-eventos.test.ts
  - supabase/tests/p48_cognitivo_notifica_smoke.sql
autonomous: true
requirements: [JORN-15]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Liberar a avaliação cognitiva passa a avisar o candidato por e-mail (D-22): `liberar_cognitivo` grava em `cognitivo_liberacao`, e o trigger novo `trg_notif_cognitivo_liberado` despacha o evento `cognitivo_liberado` para `notificar-candidato` — para que a promessa nova de D-09 («avisaremos quando houver algo para você fazer») seja verdade no dia em que for publicada (plano 48-16)."
    - "O evento novo carrega as CINCO obrigações de RESEARCH §D.1 na mesma entrega: (1) valor no CHECK `notificacoes_enviadas_evento_check`, preservando os 8 vivos; (2) linha em `classe_evento_notificacao` com classe `transacional` (o guard é fail-closed P0003); (3) vocabulário da EF (`EventoLedger`, `EVENTO_MAP` → template `avaliacao_cognitiva_liberada`, `EventoNotificacao`, e as três entradas `SUBJECTS`/`CORPOS`/`PREHEADERS`) com a paridade pinada; (4) é evento de CANDIDATO — continua elegível à varredura de retry (sem exclusão); (5) os smokes de vocabulário convertidos no 48-06 seguem verdes com ele."
    - "Ordem obrigatória respeitada: EF com o evento no vocabulário DEPLOYADA → CHECK + classe → trigger (o trigger no ar antes da EF faria o e-mail sumir num 400 sobre `net.http_post`, que é at-most-once)."
    - "A chave distingue liberações: `{candidatura}:cognitivo_liberado:{ciclo}` com `ciclo = extract(epoch from liberado_em)::bigint`; o trigger só despacha no INSERT e na RE-liberação depois de uma revogação (`OLD.revogado_em IS NOT NULL AND NEW.revogado_em IS NULL`) — clicar «Liberar» de novo numa liberação vigente não manda segundo e-mail."
    - "`em_espera` continua SEM aviso — não é decisão comunicada ao candidato (D-22)."
    - "Linguagem de produto: o e-mail diz «avaliação cognitiva», nunca «teste psicológico»; não traz nota, score nem instrumento; passa no grep-guard de palavras."
    - "`p39_rewire_triggers_smoke.sql:189` (`trg_notif_*` <> 3) já está vermelho (6) e passa a 7 com este trigger — registrado, não tocado (instrução do operador)."
    - "EFs importadoras de `_shared/email-*.ts` (`notificar-candidato`, `notificar-rh`, `executar-direito-titular`) redeployadas por `efdeploy.cjs`; migration por `p46apply.cjs` com md5 (D-16); varredura de portões rodada antes (D-17)."
  artifacts:
    - path: supabase/migrations/20260921000010_p48_cognitivo_liberado_notifica.sql
      provides: "CHECK + classe + função de trigger + trigger em cognitivo_liberacao"
      contains: "cognitivo_liberado"
    - path: supabase/functions/_shared/email-templates.ts
      provides: "template avaliacao_cognitiva_liberada"
      contains: "avaliacao_cognitiva_liberada"
    - path: supabase/tests/p48_cognitivo_notifica_smoke.sql
      provides: "prova do despacho no banco e da não-duplicação"
  key_links:
    - from: "public.liberar_cognitivo → cognitivo_liberacao (INSERT/UPDATE)"
      to: "trg_notif_cognitivo_liberado → notificar-candidato"
      via: "net.http_post com evento cognitivo_liberado e ciclo"
      pattern: "cognitivo_liberado"
    - from: "supabase/functions/notificar-candidato/helpers.ts EVENTO_MAP"
      to: "supabase/functions/_shared/email-templates.ts SUBJECTS/CORPOS/PREHEADERS"
      via: "EventoNotificacao avaliacao_cognitiva_liberada"
      pattern: "avaliacao_cognitiva_liberada"
  prohibitions:
    - requirement_id: JORN-15
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT chamar a avaliação cognitiva de «teste psicológico» nem expor instrumento, nota ou critério no e-mail ao candidato"
    - requirement_id: JORN-15
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT colocar o trigger no ar antes de a EF conhecer o evento — o e-mail sumiria sem rastro"
---

<objective>
A liberação da avaliação cognitiva passa a avisar o candidato (D-22) — o último pedaço de «toda ação
pedida ao candidato avisa» que faltava para a promessa de D-09 ser verdade.

Purpose: medido na pesquisa — `liberar_cognitivo` não notifica (`prosrc` sem `net.http`, nenhum
trigger nas tabelas do Raven). Com o texto novo do e-mail de confirmação («avisaremos quando houver
algo para você fazer»), essa liberação seria uma promessa descumprida.

Output: vocabulário e template na EF (deployada), migration com CHECK + classe + trigger (aplicada),
testes e smoke.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-PORTOES.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer) [BLOCKING]: o aviso da liberação cognitiva de ponta a ponta — vocabulário e template na EF, deploy, CHECK + classe + trigger</name>
  <files>supabase/functions/notificar-candidato/helpers.ts, supabase/functions/notificar-candidato/index.ts, supabase/functions/_shared/email-config.ts, supabase/functions/_shared/email-templates.ts, supabase/migrations/20260921000010_p48_cognitivo_liberado_notifica.sql</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §D.1 (as cinco obrigações) e §D.5 (medição de `liberar_cognitivo`)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §8 e §«Shared Patterns» A e B
    - `supabase/migrations/20260730000004_p42_evento_revisao_respondida.sql` INTEIRO (evento de candidato ponta a ponta: ordem `:12-33`, CHECK + DO `:113-153`, trigger `:175-257`)
    - `supabase/migrations/20260805000007_p45_retirada_e_evento.sql` (`:269-319` CHECK preservando os vivos; `:357-394` classe com `ON CONFLICT DO NOTHING`)
    - `supabase/migrations/20260826000007_*.sql` e `20260826000008_rpcs_liberar_revogar_cognitivo.sql` (a tabela e o upsert que recarimba `liberado_em`)
    - `supabase/functions/notificar-candidato/helpers.ts` e `index.ts` (o estado deixado pelo 48-08 — `ciclo` genérico)
    - `supabase/functions/_shared/email-config.ts` (`:42-58` a união e o aviso de que cada valor obriga 3 entradas)
    - `supabase/functions/_shared/email-templates.ts` (`:84-112` `DadosEmail`; `:142-300` corpos e os três `Record`)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-PORTOES.md` (os portões já convertidos)
  </read_first>
  <action>
EF (vocabulário primeiro): em `helpers.ts`, `EventoLedger` ganha `'cognitivo_liberado'`, `EVENTO_MAP` ganha `cognitivo_liberado: 'avaliacao_cognitiva_liberada'` (atualizar o docblock com o 6º evento de candidato e a contagem do CHECK vivo), e `montarDedupeKey` aplica `versao` a `cognitivo_liberado`. Em `index.ts`, o `ciclo` validado pelo 48-08 vira `versao` também para `cognitivo_liberado`. Em `email-config.ts`, `EventoNotificacao` ganha `'avaliacao_cognitiva_liberada'`. Em `email-templates.ts`, as três entradas: assunto «Uma avaliação cognitiva foi liberada para você»; prévia curta equivalente; corpo — saudação existente + «A equipe da Beauty Smile liberou uma avaliação cognitiva para a sua candidatura à vaga <vaga>. Acesse o seu painel para ver as instruções.» (o botão de acesso ao painel é acrescentado a TODOS os corpos de candidato pelo plano 48-16 — não duplicar aqui). Sem instrumento, sem nota.

Rodar `deno test --allow-all supabase/functions/notificar-candidato/ supabase/functions/_shared/` (o compilador força as três entradas; a paridade do vocabulário é derivada). Deployar as importadoras de `_shared`: `node efdeploy.cjs notificar-candidato`, `node efdeploy.cjs notificar-rh`, `node efdeploy.cjs executar-direito-titular`. Conferir `cognitivo_liberado` no bundle de `notificar-candidato`.

Só então a migration `20260921000010_p48_cognitivo_liberado_notifica.sql` (cabeçalho do M8 com o `pg_get_constraintdef` vivo transcrito, transporte `p46apply.cjs migrate`, sem `BEGIN;`), nesta ordem: (1) `DROP CONSTRAINT IF EXISTS notificacoes_enviadas_evento_check` + `ADD CONSTRAINT` com os 8 vivos + `cognitivo_liberado`, e `DO` que aborta se algum valor vivo sumiu; (2) `INSERT INTO public.classe_evento_notificacao (evento, classe, descricao) VALUES ('cognitivo_liberado', 'transacional', …) ON CONFLICT (evento) DO NOTHING` (nunca upsert); (3) `CREATE OR REPLACE FUNCTION public.trg_notif_cognitivo_liberado()` no molde de `trg_notif_revisao_respondida` (Vault + graceful-skip, `EXCEPTION WHEN OTHERS` fail-open com WARNING sem PII, body ids-only com `evento`, `candidatura_id` e `ciclo = extract(epoch from NEW.liberado_em)::bigint::text`), com guard no corpo: INSERT despacha; UPDATE só despacha quando `OLD.revogado_em IS NOT NULL AND NEW.revogado_em IS NULL`; `REVOKE ALL … FROM PUBLIC; REVOKE ALL … FROM anon;` (4) `DROP TRIGGER IF EXISTS` + `CREATE TRIGGER trg_notif_cognitivo_liberado AFTER INSERT OR UPDATE OF liberado_em, revogado_em ON public.cognitivo_liberacao FOR EACH ROW EXECUTE FUNCTION public.trg_notif_cognitivo_liberado()`; (5) `COMMENT`s (D-22; `em_espera` fica sem aviso; por que a re-liberação vigente não reenvia); (6) `DO` final: CHECK contém os 9, classe existe e é `transacional`, trigger existe. NÃO acrescentar este evento à exclusão de `varrer_retry_notificacoes` (é de candidato — deve ser re-tentado).

Rodar a varredura de portões e conferir contra `48-VARREDURA-PORTOES.md`. Aplicar: `node p46apply.cjs migrate supabase/migrations/20260921000010_p48_cognitivo_liberado_notifica.sql`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/notificar-candidato/ supabase/functions/_shared/</automated>
    <fails_when>non-zero exit, "FAILED" or "0 passed" in the output (includes a type error for a missing Record entry)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs notificar-candidato && node efdeploy.cjs notificar-rh && node efdeploy.cjs executar-direito-titular && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/notificar-candidato/body" | grep -a -c "cognitivo_liberado"</automated>
    <fails_when>non-zero exit from any efdeploy, or the grep count printed is 0 (the migration must NOT be applied)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260921000010_p48_cognitivo_liberado_notifica.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');for(const ev of ['confirmacao','avanco','convite','decisao','revisao_solicitada','revisao_respondida','divulgacao_vagas','candidatura_encerrada_a_pedido','cognitivo_liberado'])if(!new RegExp(\"'\"+ev+\"'\").test(c))throw new Error('CHECK sem o evento: '+ev);for(const t of ['notificacoes_enviadas_evento_check','classe_evento_notificacao','transacional','ON CONFLICT (evento) DO NOTHING','trg_notif_cognitivo_liberado','AFTER INSERT OR UPDATE OF liberado_em, revogado_em','revogado_em IS NOT NULL','edge_invoke_key','EXCEPTION WHEN OTHERS',\"'ciclo'\"])if(!c.includes(t))throw new Error('falta: '+t);if(/CREATE\s+OR\s+REPLACE\s+FUNCTION\s+public\.varrer_retry_notificacoes/i.test(c))throw new Error('evento de candidato NAO entra na exclusao do retry');console.log('OK')" && node p46apply.cjs migrate supabase/migrations/20260921000010_p48_cognitivo_liberado_notifica.sql</automated>
    <fails_when>non-zero exit; "CHECK sem o evento", "falta:" or "NAO entra na exclusao" in stderr; or "md5 do ledger BATE" absent</fails_when>
  </verify>
  <acceptance_criteria>
    - As três EFs deployadas ANTES da migration (horários no SUMMARY); bundle de `notificar-candidato` com o evento.
    - Em PROD: o CHECK contém os 9 valores; `classe_evento_notificacao` tem `cognitivo_liberado` = `transacional`; `pg_trigger` mostra `trg_notif_cognitivo_liberado` em `cognitivo_liberacao`.
    - `varrer_retry_notificacoes` NÃO foi redefinida por este plano.
  </acceptance_criteria>
  <done>Quando o RH libera a avaliação cognitiva, o candidato é avisado — e o evento nasce registrado em todos os lugares em que um esquecimento o faria sumir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: os testes do evento, o smoke do despacho, e os portões de vocabulário com o evento novo</name>
  <files>supabase/functions/_shared/__tests__/email-templates.test.ts, supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts, supabase/functions/notificar-candidato/__tests__/vocabulario-eventos.test.ts, supabase/tests/p48_cognitivo_notifica_smoke.sql</files>
  <read_first>
    - `supabase/functions/_shared/__tests__/email-templates.test.ts` (`:72-79` grep-guard; os testes por evento)
    - `supabase/functions/notificar-candidato/__tests__/vocabulario-eventos.test.ts` (paridade derivada)
    - `supabase/tests/p48_dedupe_smoke.sql` (48-08 — o idioma de ler `net.http_request_queue` numa subtransação que reverte)
    - `supabase/tests/p43_guard_marketing_smoke.sql`, `p42_notif_revisao_smoke.sql`, `p37_fidelidade_schema_smoke.sql` (convertidos no 48-06)
  </read_first>
  <behavior>
    - `renderarEmail('avaliacao_cognitiva_liberada', dados)` → assunto com «avaliação cognitiva»; corpo com o título da vaga e «Acesse o seu painel»; prévia não vazia; nenhum match de `/score|percentil|trait|motivo|nota|ranking|pontuaç|crit[ée]rio|teste psicol/i`
    - `montarDedupeKey('cognitivo_liberado', C, undefined, '1758400000')` → `C:cognitivo_liberado:1758400000`
    - handler com `evento:'cognitivo_liberado'` e `ciclo` válido → claim com a chave versionada e template `avaliacao_cognitiva_liberada`; com `ciclo:'x'` → 400
    - paridade: `cognitivo_liberado` ∈ `EVENTOS_VALIDOS` e mapeia para um template com assunto, corpo e prévia
  </behavior>
  <action>
Acrescentar os testes de `<behavior>` nos três arquivos de teste (mantendo o estilo e os nomes `T-48-…`). Rodar a suíte.

Smoke `supabase/tests/p48_cognitivo_notifica_smoke.sql` (contador, gate, SELECT final): numa subtransação que reverte, com claim de administrador, sobre uma candidatura real de conta de teste EM ANDAMENTO (`liberar_cognitivo` recusa encerrada): (a) `liberar_cognitivo` → exatamente 1 linha nova em `net.http_request_queue` para `/functions/v1/notificar-candidato` com `evento='cognitivo_liberado'` e `ciclo` presente; (b) `liberar_cognitivo` de novo, sem revogar → 0 linha nova; (c) `revogar_cognitivo` → 0 linha nova; `liberar_cognitivo` de novo → 1 linha nova (dentro de uma transação `now()` é constante, então o `ciclo` repete — a asserção é sobre o despacho, não sobre o valor; comentar isso); (d) catálogo — CHECK com o evento, classe `transacional`, trigger presente; (e) NEGATIVA — contagens de `cognitivo_liberacao` e da fila iguais antes e depois. Rodar o smoke e, em seguida, os três portões convertidos no 48-06 (`p43_guard_marketing_smoke`, `p42_notif_revisao_smoke`, `p37_fidelidade_schema_smoke`) — eles têm de continuar verdes COM o evento novo (o p43 (c) passa a iterar sobre ele).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/notificar-candidato/ supabase/functions/_shared/</automated>
    <fails_when>non-zero exit, "FAILED" or "0 passed" in the output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs run supabase/tests/p48_cognitivo_notifica_smoke.sql && node p46apply.cjs run supabase/tests/p43_guard_marketing_smoke.sql && node p46apply.cjs run supabase/tests/p42_notif_revisao_smoke.sql && node p46apply.cjs run supabase/tests/p37_fidelidade_schema_smoke.sql</automated>
    <fails_when>non-zero exit, or "FAIL" in any output</fails_when>
  </verify>
  <acceptance_criteria>
    - Testes novos verdes; suíte completa de `notificar-candidato` e `_shared` verde.
    - `p48_cognitivo_notifica_smoke` (a)–(e) verde.
    - Os três portões de vocabulário verdes com o 9º evento (o p43 (c) o exercitou por inserção real revertida).
    - A prova com conta real (RH libera → linha `cognitivo_liberado` `enviado`/`entregue`) é do plano 48-18.
  </acceptance_criteria>
  <done>O evento novo está coberto por teste, por smoke e pelos portões que antes nem o veriam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH (JWT) → `liberar_cognitivo` → trigger → `net.http_post` → EF | escrita do RH vira e-mail ao candidato |
| EF → Resend | e-mail transacional ao candidato |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-10-01 | Denial of Service | evento sem classe (guard P0003) ou fora do CHECK (23514) — e-mail some | high | mitigate | CHECK + classe na mesma migration; `DO` assere os 9 valores e a classe |
| T-48-10-02 | Denial of Service | trigger antes da EF (400 sobre at-most-once) | high | mitigate | deploy e marcador no bundle conferidos antes do apply |
| T-48-10-03 | Information Disclosure | instrumento/nota no e-mail | medium | mitigate | corpo sem avaliação; grep-guard em teste |
| T-48-10-04 | Repudiation | e-mail duplicado a cada clique em «Liberar» | low | mitigate | guard de transição no corpo do trigger; smoke (b) |
| T-48-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- EFs deployadas antes da migration; migration aplicada (md5 BATE).
- `deno test` verde; `p48_cognitivo_notifica_smoke` e os três portões de vocabulário verdes.
</verification>

<success_criteria>
Toda ação pedida ao candidato — avaliação assíncrona, convite de entrevista e agora a avaliação cognitiva — gera aviso.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| evento `cognitivo_liberado` (CHECK + `classe_evento_notificacao` transacional) | vocabulário do ledger | **novo** |
| `public.trg_notif_cognitivo_liberado()` + trigger `trg_notif_cognitivo_liberado` em `cognitivo_liberacao` | função de trigger + trigger | **novos** |
| `EventoLedger 'cognitivo_liberado'`, `EVENTO_MAP.cognitivo_liberado` | vocabulário da EF | **novo** |
| `EventoNotificacao 'avaliacao_cognitiva_liberada'` + `SUBJECTS`/`CORPOS`/`PREHEADERS` | template | **novo** |
| `supabase/migrations/20260921000010_p48_cognitivo_liberado_notifica.sql` | migration | **nova** |
| `supabase/tests/p48_cognitivo_notifica_smoke.sql` | smoke | **novo** |

## Premissas sinalizadas (probe de borda)

JORN-15 foi classificado `unclassified` pelo probe. A borda `explicit` deste plano (re-liberação
vigente não reenvia; re-liberação após revogação reenvia) é decisão do planejador sobre o upsert de
`liberar_cognitivo` (`20260826000008:72-79`), registrada no COMMENT do trigger.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-10-SUMMARY.md` when done
</output>
