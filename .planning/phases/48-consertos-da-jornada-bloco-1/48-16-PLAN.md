---
phase: 48-consertos-da-jornada-bloco-1
plan: 16
type: execute
wave: 5
depends_on: ["48-07", "48-08", "48-10", "48-13", "48-14"]
files_modified:
  - supabase/functions/_shared/email-templates.ts
  - supabase/functions/_shared/__tests__/email-templates.test.ts
  - supabase/functions/notificar-candidato/index.ts
  - supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts
  - src/components/modals/UpdateStatusModal.tsx
  - src/components/modals/__tests__/UpdateStatusModal.test.tsx
  - src/features/vagas/services/candidaturasService.ts
  - supabase/functions/notificar-rh/helpers.ts
  - supabase/functions/notificar-rh/__tests__/notificar-rh.test.ts
  - supabase/functions/executar-direito-titular/index.test.ts
autonomous: true
requirements: [JORN-15, JORN-U2]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Todo e-mail transacional ao CANDIDATO — `candidatura_recebida`, `avaliacao_liberada`, `convite_entrevista`, `decisao_final` (aprovado, rejeitado e knockout), `revisao_respondida`, `avaliacao_cognitiva_liberada` — traz o botão «Acessar meu painel» e o link por extenso para `https://rh.beautysmile.com.br/auth/login` (base por `APP_BASE_URL` com fail-safe) (JORN-U2, D-09); os avisos ao titular do 48-07 já levam o link com retorno à privacidade."
    - "O link vai por PARÂMETRO nos corpos de candidato (`DadosEmail.urlLogin`), NUNCA no rodapé de `layoutBase` — que também monta os e-mails do RH e o recibo pós-exclusão, enviado depois de a conta deixar de existir."
    - "EXCEÇÃO registrada ao JORN-U2: o recibo de exclusão NÃO leva link de login (a conta foi apagada) — pinado em teste."
    - "O e-mail de confirmação deixa de prometer «Avisaremos por e-mail a cada etapa» e passa a apontar para o painel: «Acompanhe o andamento pelo seu painel a qualquer momento. Avisaremos por e-mail quando houver algo para você fazer ou uma decisão sobre a sua candidatura.» (JORN-15, D-09)."
    - "A promessa nova só é PUBLICADA quando é verdade (D-09 — publicar por último): antes do deploy, uma checagem automatizada confirma no catálogo e nos bundles deployados que todo desfecho avisa (chave por transição do 48-08 viva; knockout já avisava) e que toda ação pedida ao candidato avisa (`avanco`, `convite` e o `cognitivo_liberado` do 48-10 vivos). Se faltar um, a cópia NÃO sobe."
    - "A promessa sem código do modal de status do RH sai: `UpdateStatusModal` deixa de oferecer «Notificar candidato por email» (a opção não tem efeito — nenhum código a honra desde a aposentadoria do n8n) e passa a dizer a verdade: mudar o status ali não envia e-mail; o candidato é avisado quando uma decisão é registrada ou quando há algo para ele fazer."
    - "`em_espera` e o avanço entre etapas de trabalho continuam SEM e-mail — o operador rejeitou «avisar em cada avanço» (D-09); a cópia nova não promete isso."
    - "E-mail e página dizem a MESMA frase de reabertura (conferido cruzando `email-templates.ts` com `ExplicacaoCandidatoPage.tsx` — 42-UI-SPEC)."
    - "`notificar-rh` passa a usar a base do app de `_shared/email-config.ts` (sem a constante duplicada)."
    - "Nenhum e-mail novo cita o endereço do canal de privacidade por literal (D-07); linguagem de produto preservada."
    - "As três EFs importadoras de `_shared/email-*.ts` deployadas por `efdeploy.cjs`; front publicado com `origin/main..HEAD` vazio (D-16); `tsc` ≤ 90 (D-15)."
  artifacts:
    - path: supabase/functions/_shared/email-templates.ts
      provides: "urlLogin nos corpos de candidato + cópia D-09"
      contains: "Acompanhe o andamento pelo seu painel"
    - path: supabase/functions/notificar-candidato/index.ts
      provides: "monta urlLogin a partir de APP_BASE_URL"
      contains: "montarUrlLogin"
    - path: src/components/modals/UpdateStatusModal.tsx
      provides: "sem a promessa de e-mail sem código"
  key_links:
    - from: "supabase/functions/notificar-candidato/index.ts"
      to: "supabase/functions/_shared/email-config.ts montarUrlLogin"
      via: "DadosEmail.urlLogin → blocoAcessoPainel em cada corpo de candidato"
      pattern: "urlLogin"
    - from: "supabase/functions/notificar-rh/helpers.ts"
      to: "supabase/functions/_shared/email-config.ts APP_BASE_URL_PADRAO / normalizarBaseApp"
      via: "import (fim da duplicação)"
      pattern: "email-config"
  prohibitions:
    - requirement_id: JORN-15
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT publicar a promessa nova antes de o sistema cumpri-la — nem prometer aviso «a cada etapa»"
    - requirement_id: JORN-U2
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT pôr o link de login do candidato no layout comum — ele vazaria para os e-mails do RH e para o recibo de uma conta que já não existe"
---

<objective>
Fechar a promessa: todo e-mail ao candidato leva ao login dele (JORN-U2), e o e-mail de confirmação
deixa de prometer «a cada etapa» e aponta para o painel (JORN-15) — publicado por último, só depois de
confirmado que a promessa nova é verdade.

Purpose: D-09 — o operador escolheu mudar a promessa em vez de avisar em cada avanço, com uma condição
obrigatória: o link para o login sobe para este bloco, senão a promessa aponta para um lugar
inalcançável. E a promessa só pode ser publicada quando todo desfecho e toda ação pedida ao candidato
avisam (48-08, 48-10 — e o knockout, que já avisava).

Output: link de login em todos os corpos de candidato; cópia D-09; modal de status sem promessa falsa;
`notificar-rh` sobre a base compartilhada; EFs deployadas e front publicado.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-CONTEXT.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): o link de login em todo e-mail ao candidato — parâmetro, bloco, EF, deploy</name>
  <files>supabase/functions/_shared/email-templates.ts, supabase/functions/_shared/__tests__/email-templates.test.ts, supabase/functions/notificar-candidato/index.ts, supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §D.6 (rota, destinos por evento, por que não no `layoutBase`)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §10
    - `supabase/functions/_shared/email-templates.ts` (`:74-81` `escapeHtml`; `:84-112` `DadosEmail`; `:113-140` `layoutBase` — NÃO tocar o rodapé; `:142-300` os corpos de candidato)
    - `supabase/functions/_shared/email-config.ts` (`montarUrlLogin` do 48-07)
    - `supabase/functions/notificar-rh/helpers.ts` (`:244-245` — HTML do botão a copiar)
    - `supabase/functions/notificar-candidato/index.ts` (a chamada a `renderarEmail`)
  </read_first>
  <behavior>
    - para CADA `EventoNotificacao` de candidato, `renderarEmail(evento, { …, urlLogin: 'https://rh.beautysmile.com.br/auth/login' })` → HTML contém «Acessar meu painel» e o link por extenso
    - `urlLogin` ausente → o corpo sai sem o bloco (nunca link quebrado)
    - `layoutBase` chamado sem `urlLogin` (como fazem `notificar-rh` e o recibo) → não contém `/auth/login`
    - handler de `notificar-candidato` com `APP_BASE_URL` ausente → `urlLogin` = default; com `APP_BASE_URL='lixo'` → default
  </behavior>
  <action>
`email-templates.ts`: `DadosEmail.urlLogin?: string` (docblock: JORN-U2; opcional com caminho honesto para ausência; por que NÃO no `layoutBase`); função `blocoAcessoPainel(url?: string): string` — vazia sem url; com url, o botão «Acessar meu painel» (HTML do botão de `notificar-rh/helpers.ts:244-245`, cores do e-mail do candidato) + «Se o botão não funcionar, acesse: <url>», sempre com `escapeHtml`. Acrescentar `blocoAcessoPainel(d.urlLogin)` ao fim de CADA corpo de candidato em `CORPOS` (inclusive `avaliacao_cognitiva_liberada` do 48-10). `layoutBase` NÃO muda.

`notificar-candidato/index.ts`: calcular `urlLogin = montarUrlLogin(Deno.env.get('APP_BASE_URL') || undefined)` e passar ao `renderarEmail` para todos os eventos.

Testes com os casos de `<behavior>` (iterar sobre as chaves de `SUBJECTS`/`CORPOS` — baseline do próprio código, não lista literal). Rodar as suítes e deployar `node efdeploy.cjs notificar-candidato`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/_shared/ supabase/functions/notificar-candidato/</automated>
    <fails_when>non-zero exit, "FAILED" or "0 passed" in the output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const s=require('fs').readFileSync('supabase/functions/_shared/email-templates.ts','utf8');const i=s.indexOf('export function layoutBase');const j=s.indexOf('\n}\n',i);if(i<0||j<0)throw new Error('layoutBase nao encontrado');if(/auth\/login|urlLogin|blocoAcessoPainel/.test(s.slice(i,j)))throw new Error('o link de login NAO pode ir no layoutBase');console.log('layoutBase sem link de login')" && node efdeploy.cjs notificar-candidato && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/notificar-candidato/body" | grep -a -c "Acessar meu painel"</automated>
    <fails_when>non-zero exit; "NAO pode ir no layoutBase" in stderr; efdeploy failure; or the grep count printed is 0</fails_when>
  </verify>
  <acceptance_criteria>
    - Cada corpo de candidato renderiza o link quando `urlLogin` vem, e nenhum link quando não vem (testes iterando o próprio `Record`).
    - `layoutBase` não contém link de login.
    - `notificar-candidato` deployada com o bloco no bundle.
  </acceptance_criteria>
  <done>Todo e-mail que o candidato recebe leva ao painel dele com um clique.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: a promessa nova — só depois de provado que é verdade; e a promessa falsa do modal sai</name>
  <precondition>A checagem de prontidão abaixo (primeiro verify) passa: `trg_notif_transicao` passa `historico_id`, `trg_notif_cognitivo_liberado` existe, o CHECK contém `cognitivo_liberado`, e o bundle deployado de `notificar-candidato` contém `historico_inconsistente` e `cognitivo_liberado`. Se falhar, a cópia D-09 NÃO sobe — parar e reportar qual promessa ainda não é verdade.</precondition>
  <files>supabase/functions/_shared/email-templates.ts, supabase/functions/_shared/__tests__/email-templates.test.ts, src/components/modals/UpdateStatusModal.tsx, src/components/modals/__tests__/UpdateStatusModal.test.tsx, src/features/vagas/services/candidaturasService.ts</files>
  <read_first>
    - `supabase/functions/_shared/email-templates.ts` (`:150` — a linha da promessa)
    - `src/__tests__/guards/wait-state-copy.grep.test.ts` (`:11` — a frase canônica «Acompanhe o andamento pelo seu painel»)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §D.5 (as outras promessas achadas)
    - `src/components/modals/UpdateStatusModal.tsx` (`:85-90` estado `notificarCandidato`; `:160-170` o payload; `:265-290` o checkbox e a promessa)
    - `src/components/modals/__tests__/UpdateStatusModal.test.tsx`
    - `src/features/vagas/services/candidaturasService.ts` (`:500-506` — o comentário «honored server-side/M5», falso desde a aposentadoria do n8n; o tipo do payload de status)
    - `src/features/explicacao/components/ExplicacaoCandidatoPage.tsx` (a frase de reabertura do 48-14)
  </read_first>
  <behavior>
    - o corpo de `candidatura_recebida` NÃO contém «a cada etapa» e contém «Acompanhe o andamento pelo seu painel» e «quando houver algo para você fazer ou uma decisão»
    - o modal de status não renderiza mais o checkbox «Notificar candidato por email» nem a frase «O candidato receberá um email informando sobre a mudança de status»; renderiza a nota verdadeira
    - o payload de status não carrega mais `notificar_candidato`
  </behavior>
  <action>
Rodar a checagem de prontidão (primeiro verify). Só com ela verde:

`email-templates.ts`: trocar a linha `:150` pela cópia D-09 — «Acompanhe o andamento pelo seu painel a qualquer momento. Avisaremos por e-mail quando houver algo para você fazer ou uma decisão sobre a sua candidatura.» Teste de ausência de «a cada etapa» em TODOS os corpos e de presença da frase nova na confirmação. A frase antiga não pode sobreviver nem em comentário de `email-templates.ts` (o verify reprova qualquer ocorrência) — nos testes, montar a string proibida por concatenação.

Cruzar página × e-mail (42-UI-SPEC): conferir que `email-templates.ts` e `ExplicacaoCandidatoPage.tsx` contêm a MESMA frase de reabertura («sua candidatura foi reaberta e será decidida novamente»); se divergirem, alinhar o e-mail à página do 48-14 ou vice-versa, sem mudar o sentido de D-01.

`UpdateStatusModal.tsx`: remover o estado `notificarCandidato`, o checkbox e a frase de promessa; no lugar, uma nota estática: «Mudar o status aqui não envia e-mail ao candidato. Ele é avisado por e-mail quando uma decisão é registrada ou quando há algo para ele fazer, e acompanha o resto pelo painel.» Parar de enviar `notificar_candidato` no payload. Em `candidaturasService.ts`, remover o campo do tipo do payload se nada mais o usa e corrigir o comentário «honored server-side/M5» (é falso: nenhum código honra a opção). Atualizar o teste do modal.

Rodar as suítes do front e das EFs tocadas; `tsc` ≤ 90.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs sql "SET TRANSACTION READ ONLY; select position('historico_id' in pg_get_functiondef('public.trg_notif_transicao()'::regprocedure))>0 as decisao_por_transicao, exists(select 1 from pg_trigger where tgname='trg_notif_cognitivo_liberado') as cognitivo_avisa, position('cognitivo_liberado' in (select pg_get_constraintdef(oid) from pg_constraint where conname='notificacoes_enviadas_evento_check'))>0 as cognitivo_no_check" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s)[0];const faltas=Object.entries(r).filter(([k,v])=>v!==true).map(([k])=>k);if(faltas.length){console.error('PROMESSA AINDA NAO E VERDADE: '+faltas.join(', '));process.exit(1)}console.log('prontidao de catalogo OK')})" && B=$(curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/notificar-candidato/body") && echo "$B" | grep -a -q "historico_inconsistente" && echo "$B" | grep -a -q "cognitivo_liberado" && echo "prontidao de bundle OK"</automated>
    <fails_when>non-zero exit; "PROMESSA AINDA NAO E VERDADE" in stderr; or the deployed bundle lacks historico_inconsistente/cognitivo_liberado (the D-09 copy must not be published)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/_shared/ && npx vitest run src/components/modals src/features/vagas && ! grep -rn "a cada etapa" supabase/functions/_shared/email-templates.ts && node -e "const fs=require('fs');const f='sua candidatura foi reaberta e será decidida novamente';if(!fs.readFileSync('supabase/functions/_shared/email-templates.ts','utf8').includes(f)||!fs.readFileSync('src/features/explicacao/components/ExplicacaoCandidatoPage.tsx','utf8').includes(f))throw new Error('pagina e e-mail divergem na frase de reabertura (42-UI-SPEC)');console.log('pagina e e-mail com a mesma frase')"</automated>
    <fails_when>non-zero exit; "FAILED"/"failed" in a test summary; grep still finds «a cada etapa»; or "pagina e e-mail divergem" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }</automated>
    <fails_when>non-zero exit — lint did not run (exit code other than 0 or 2), exit code and error count disagree, or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - A prontidão foi conferida e registrada no SUMMARY ANTES de a cópia mudar.
    - Nenhum corpo de e-mail contém «a cada etapa»; a confirmação contém a cópia D-09.
    - O modal não oferece mais a opção sem efeito nem promete e-mail; o payload não carrega `notificar_candidato`; o comentário falso foi corrigido.
    - Página e e-mail com a mesma frase de reabertura.
    - `tsc errors` ≤ 90.
  </acceptance_criteria>
  <done>O sistema promete ao candidato só o que cumpre — e o RH deixa de ver uma opção de aviso que nunca fez nada.</done>
</task>

<task type="auto">
  <name>Task 3: `notificar-rh` sobre a base compartilhada, a exceção do recibo pinada, deploys e publicação</name>
  <files>supabase/functions/notificar-rh/helpers.ts, supabase/functions/notificar-rh/__tests__/notificar-rh.test.ts, supabase/functions/executar-direito-titular/index.test.ts</files>
  <read_first>
    - `supabase/functions/notificar-rh/helpers.ts` (`:28-37` `APP_BASE_URL_PADRAO`; `:180-217` `montarUrlFila`/`montarUrlListaVaga`)
    - `supabase/functions/_shared/email-config.ts` (`APP_BASE_URL_PADRAO`, `normalizarBaseApp` do 48-07)
    - `supabase/functions/executar-direito-titular/helpers.ts` (`corpoReciboExclusao` — o recibo pós-exclusão)
    - `supabase/functions/executar-direito-titular/index.test.ts`
  </read_first>
  <action>
`notificar-rh/helpers.ts`: importar `APP_BASE_URL_PADRAO` e `normalizarBaseApp` de `../_shared/email-config.ts`, remover a constante duplicada (re-exportar o nome se os testes o importam daqui) e fazer `montarUrlFila`/`montarUrlListaVaga` usarem `normalizarBaseApp`. Comportamento idêntico (os testes existentes provam). Teste novo: nenhum corpo de e-mail do RH contém `/auth/login`.

`executar-direito-titular/index.test.ts`: teste que pina a exceção do JORN-U2 — o HTML do recibo pós-exclusão NÃO contém `/auth/login` (a conta foi apagada); e os avisos do 48-07 contêm `/auth/login?redirect=`.

Rodar as suítes das três EFs. Deployar as importadoras de `_shared/email-*.ts` com a cópia final: `node efdeploy.cjs notificar-candidato`, `node efdeploy.cjs notificar-rh`, `node efdeploy.cjs executar-direito-titular`. Conferir no bundle de `notificar-candidato` a frase «Acompanhe o andamento pelo seu painel».

Publicar o front (modal): `npm run test:run`, `npm run build`, commit, `git push origin main`, `origin/main..HEAD` vazio. Conferir em PROD, pelo crawler, que a frase antiga do modal não é mais servida e que o marcador ASCII `Mudar o status aqui n` (início da nota nova) está presente.

Zero literal do canal de privacidade nos arquivos de e-mail (D-07).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/notificar-rh/ supabase/functions/executar-direito-titular/ supabase/functions/notificar-candidato/ supabase/functions/_shared/ && ! grep -n "export const APP_BASE_URL_PADRAO = " supabase/functions/notificar-rh/helpers.ts && ! grep -rn "lgpd@" supabase/functions/_shared/email-templates.ts supabase/functions/_shared/email-config.ts supabase/functions/notificar-candidato/ supabase/functions/notificar-rh/ supabase/functions/executar-direito-titular/</automated>
    <fails_when>non-zero exit; "FAILED"/"0 passed" in deno output; notificar-rh still declares its own APP_BASE_URL_PADRAO; or a literal privacy-channel address is found (D-07)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs notificar-candidato && node efdeploy.cjs notificar-rh && node efdeploy.cjs executar-direito-titular && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/notificar-candidato/body" | grep -a -c "Acompanhe o andamento pelo seu painel"</automated>
    <fails_when>non-zero exit from any efdeploy, or the grep count printed is 0</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npm run test:run >/dev/null && npm run build >/dev/null && test -z "$(git log --oneline origin/main..HEAD)" && node -e "const M=process.argv[1],B='https://rh.beautysmile.com.br';(async()=>{const vis=new Set(),fila=['/'];let ok=false;while(fila.length&&vis.size<300){const p=fila.shift();if(vis.has(p))continue;vis.add(p);let t='';try{t=await (await fetch(B+p)).text()}catch{continue}if(t.includes(M)){ok=true;break}for(const m of t.matchAll(/(?:\/assets\/|\.\/)([\w.-]+\.js)/g))fila.push('/assets/'+m[1])}if(!ok){console.error('AUSENTE em PROD: '+M);process.exit(1)}console.log('PRESENTE em PROD: '+M)})()" "Mudar o status aqui n"</automated>
    <fails_when>non-zero exit; "failed" in vitest; git log origin/main..HEAD printed a commit; or "AUSENTE em PROD" in stderr</fails_when>
  </verify>
  <acceptance_criteria>
    - `notificar-rh` sem a constante duplicada e com comportamento idêntico (testes antigos verdes); nenhum e-mail do RH com link de login.
    - Recibo pós-exclusão sem link (exceção pinada); avisos do titular com link.
    - Três EFs deployadas com a cópia final; front publicado; `origin/main..HEAD` vazio; nota do modal em PROD.
    - Zero literal do canal de privacidade nos arquivos de e-mail.
    - A conferência humana dos e-mails na caixa (confirmação com a cópia nova e o botão) é do plano 48-18.
  </acceptance_criteria>
  <done>A promessa escrita ao candidato passou a ser verdade, e cada e-mail o leva ao painel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EFs → Resend → caixa do candidato | link embutido no e-mail |
| navegador do RH → modal de status | cópia que descreve efeito de uma ação |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-16-01 | Information Disclosure | link de login do candidato no e-mail do RH / recibo pós-exclusão | medium | mitigate | link por parâmetro só nos corpos de candidato; verify reprova link no `layoutBase`; testes nos e-mails do RH e no recibo |
| T-48-16-02 | Tampering | base URL hostil/malformada via env | low | mitigate | `montarUrlLogin`/`normalizarBaseApp` só aceitam `https:`; default em caso de erro; `escapeHtml` |
| T-48-16-03 | Repudiation | promessa publicada antes de ser verdade | medium | mitigate | checagem de prontidão (catálogo + bundle) como precondição da cópia D-09 |
| T-48-16-04 | Repudiation | opção de UI sem efeito prometendo e-mail | low | mitigate | checkbox removido; nota verdadeira; comentário falso corrigido |
| T-48-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Suítes deno das quatro EFs/_shared verdes; vitest do front verde; `tsc` ≤ 90.
- Prontidão conferida antes da cópia; três EFs deployadas; front publicado e marcador em PROD.
</verification>

<success_criteria>
Todo e-mail leva ao painel, a confirmação promete só o que o sistema cumpre, e nenhuma tela do RH promete um aviso que não existe.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `DadosEmail.urlLogin`, `blocoAcessoPainel` | template | **novos** |
| cópia D-09 em `candidatura_recebida` | template | editada |
| `urlLogin` em `notificar-candidato` | EF | editada |
| `notificar-rh/helpers.ts` sobre `_shared/email-config.ts` | EF | editada (fim da duplicação) |
| nota verdadeira em `UpdateStatusModal`; payload sem `notificar_candidato` | front | editado |
| teste da exceção do recibo (JORN-U2) | teste | **novo** |

## Premissas sinalizadas (probe de borda)

JORN-15 e JORN-U2 foram classificados `unclassified` pelo probe. As bordas `explicit` (sem `urlLogin`
não há link quebrado; env malformada cai no default; recibo sem link) vêm do RESEARCH §D.6. A promessa
de `explicacaoService.ts:206` («Avisaremos você sobre os próximos passos», razão de `em_espera`) segue
inalcançável — a página só existe para `rejeitado` — e não é tocada.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-16-SUMMARY.md` when done
</output>
