---
phase: 48-consertos-da-jornada-bloco-1
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/p43_guard_marketing_smoke.sql
  - supabase/tests/p42_notif_revisao_smoke.sql
  - supabase/tests/p37_fidelidade_schema_smoke.sql
  - .planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-PORTOES.md
autonomous: true
requirements: [JORN-15, JORN-19]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A varredura de portões do CLAUDE.md foi rodada ANTES de qualquer evento novo entrar no CHECK de `notificacoes_enviadas` (planos 48-10 e 48-13), e cada achado que esta fase toca está classificado em `48-VARREDURA-PORTOES.md` como escopo deliberado × fotografia, com o plano que o trata (D-17)."
    - "`p43_guard_marketing_smoke.sql` (y2, `:738`) deixa de comparar a contagem de `classe_evento_notificacao` com a constante 7 (já vermelha: PROD tem 8) e passa a comparar a impressão digital da tabela (md5 do `to_jsonb` de todas as linhas) capturada no início da PRÓPRIA execução (D-17)."
    - "`p43_guard_marketing_smoke.sql` (c, `:344`) e `p42_notif_revisao_smoke.sql` (a, `:181`) deixam de iterar uma lista literal de 6 eventos: passam a iterar os eventos EXTRAÍDOS do `pg_get_constraintdef` vivo (idioma de `p43` (e), `:404-441`), excluindo os de classe `marketing`, e exigem aceite de todos; e continuam exigindo que os 6 eventos históricos estejam no CHECK (o contrato do P42 que a lista protegia)."
    - "`p37_fidelidade_schema_smoke.sql` deixa de exigir o CHECK com EXATAMENTE 4 valores e a contagem exata de colunas (fotografias do P37, vermelhas contra PROD): passa a exigir que o CHECK vivo CONTENHA os 4 eventos do P37 e que `notificacoes_enviadas` CONTENHA as colunas do P37 por nome — o invariante, não o instantâneo."
    - "Cada portão convertido foi provado por EXECUÇÃO que ainda MORDE: um gancho de sonda por GUC (`smoke<id>.sonda`) perturba o estado dentro de uma subtransação, roda a MESMA comparação e termina SEMPRE em exceção — `SONDA OK: <asserção> mordeu` quando o portão reprovou, `SONDA FALHOU` quando não — de modo que nenhum caminho da sonda comita a perturbação."
    - "Os três smokes convertidos rodam VERDES em PROD depois da conversão (antes de qualquer evento novo)."
    - "`p39_rewire_triggers_smoke.sql:189` (`trg_notif_*` <> 3; PROD tem 6, e o 48-10 leva a 7) é registrado como fotografia vermelha NÃO tocada, por instrução do operador."
  artifacts:
    - path: .planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-PORTOES.md
      provides: "padrão, contagem, classificação dos achados tocados, resultado das sondas"
    - path: supabase/tests/p43_guard_marketing_smoke.sql
      provides: "y2 por impressão digital; (c) por vocabulário do catálogo; ganchos de sonda"
    - path: supabase/tests/p42_notif_revisao_smoke.sql
      provides: "(a) por vocabulário do catálogo; gancho de sonda"
    - path: supabase/tests/p37_fidelidade_schema_smoke.sql
      provides: "CHECK e colunas por pertinência; gancho de sonda"
  key_links:
    - from: "supabase/tests/p43_guard_marketing_smoke.sql (c)"
      to: "pg_get_constraintdef(notificacoes_enviadas_evento_check)"
      via: "regexp_matches sobre a definição viva"
      pattern: "regexp_matches"
  prohibitions:
    - requirement_id: JORN-15
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT trocar uma constante envelhecida por outra constante nova — conserto de fotografia é baseline capturada na execução"
    - requirement_id: JORN-19
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT deixar um portão convertido incapaz de falhar — sem sonda que prove a mordida, o conserto é pior que o portão quebrado"
---

<objective>
Converter, ANTES de a fase acrescentar eventos novos ao ledger de notificações, os portões que vigiam
esse vocabulário por fotografia — e provar por execução que cada um ainda morde.

Purpose: esta fase acrescenta dois eventos ao CHECK de `notificacoes_enviadas` (`cognitivo_liberado`,
D-22, plano 48-10; `prazo_reabertura_vencido`, JORN-19, plano 48-13) e uma classe nova a cada um. Os
smokes que vigiam o vocabulário congelam instantâneos: dois iteram lista literal (não reprovam nada — o
evento novo fica fora da vigilância e o portão segue verde) e dois comparam com constante (um já reprova
trabalho correto hoje, com diagnóstico falso). É exatamente a assimetria da tabela do CLAUDE.md
§«Portões: varra pela FORMA». D-17 manda converter antes, para baseline capturada na execução.

Output: três smokes convertidos e verdes em PROD, sondas de mordida executadas, e o artefato
`48-VARREDURA-PORTOES.md`.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): p43 convertido de ponta a ponta — impressão digital no teardown, vocabulário do catálogo em (c), sondas que mordem</name>
  <files>supabase/tests/p43_guard_marketing_smoke.sql</files>
  <read_first>
    - `CLAUDE.md` §«Portões: varra pela FORMA, não pelo sintoma» (a tabela dos dois modos de falha e o padrão de varredura)
    - `supabase/tests/p43_guard_marketing_smoke.sql` INTEIRO (cabeçalho `:1-100` com o gate de 9 aprovações; (c) `:300-350`; (e) `:394-441` — o idioma de extrair o vocabulário do CHECK; teardown y2 `:700-745`)
    - `supabase/tests/p43_matriz_retencao_smoke.sql` (`:95-163` e `:668-683` — impressão digital por `to_jsonb`, guardada em GUC)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §I
  </read_first>
  <action>
Rodar o p43 como está (`node p46apply.cjs run supabase/tests/p43_guard_marketing_smoke.sql`) e registrar a falha atual (esperado: y2 com «tem 8, esperado 7»). Se reprovar em outra asserção por motivo alheio a esta fase, PARAR e reportar em vez de ampliar o escopo.

(y2) No início do smoke (junto do `set_config('smoke43g.pass','0')`), capturar em GUC a impressão digital de `classe_evento_notificacao`: md5 do `string_agg` ordenado do `to_jsonb` de cada linha. No teardown, trocar `v_classe <> 7` pela comparação com essa impressão digital; a mensagem de FAIL passa a dizer que a tabela MUDOU durante o smoke (o DELETE de (b3) não foi revertido) — sem número mágico.

(c) Trocar a iteração da lista literal de 6 eventos pela lista extraída do `pg_get_constraintdef` vivo de `notificacoes_enviadas_evento_check` (o mesmo `regexp_matches` de (e)), excluindo os eventos cuja classe em `classe_evento_notificacao` é `marketing` (o guard os recusa sem consentimento — é outra asserção). Exigir aceite de TODOS os extraídos e `v_aceitos` igual ao total extraído (baseline da execução). Acrescentar a exigência de que os 6 históricos (`confirmacao`, `avanco`, `convite`, `decisao`, `revisao_solicitada`, `revisao_respondida`) estão no CHECK — é o contrato que a lista protegia, agora como pertinência.

Ganchos de sonda, lidos de `current_setting('smoke43g.sonda', true)`: `'y2'` → dentro de um bloco `BEGIN … EXCEPTION`, apagar uma linha de `classe_evento_notificacao`, rodar a comparação de y2 e, se ela NÃO levantar, `RAISE EXCEPTION 'SONDA FALHOU: y2 não mordeu'`; no handler, se a mensagem for o FAIL de y2, `RAISE EXCEPTION 'SONDA OK: y2 mordeu'`, senão re-levantar. `'c'` → incluir um evento inexistente (`evento_sonda_inexistente`) no conjunto histórico exigido e esperar o FAIL de pertinência, com o mesmo fecho. Todo caminho da sonda termina em exceção — a transação da Management API é revertida inteira e a perturbação nunca comita. Documentar os ganchos no cabeçalho do arquivo. O gate final (contagem de aprovações do RESUMO z) só muda se o número de asserções mudar — e, se mudar, registrar por quê.

Os comentários novos descrevem a conversão sem reescrever as comparações antigas (`<> 7`, `<> 6`) — os critérios de aceite procuram essas formas por grep. Rodar verde. Rodar as duas sondas criando cópias TEMPORÁRIAS no diretório de scratch (fora do repositório), cada uma com uma primeira linha `SELECT set_config('smoke43g.sonda', '<y2|c>', false);` seguida do conteúdo do arquivo, via `node p46apply.cjs run <cópia>`; cada uma TEM de sair com erro contendo `SONDA OK`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs run supabase/tests/p43_guard_marketing_smoke.sql</automated>
    <fails_when>non-zero exit, or "P43G FAIL" in the output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && D=$(mktemp -d) && for S in y2 c; do { echo "SELECT set_config('smoke43g.sonda', '$S', false);"; cat supabase/tests/p43_guard_marketing_smoke.sql; } > "$D/sonda_$S.sql"; OUT=$(node p46apply.cjs run "$D/sonda_$S.sql" 2>&1); echo "$OUT" | grep -q "SONDA OK: $S mordeu" || { echo "SONDA $S NAO mordeu: $OUT" | tail -3; exit 1; }; echo "sonda $S mordeu"; done</automated>
    <fails_when>non-zero exit with "SONDA y2 NAO mordeu" or "SONDA c NAO mordeu" (the converted gate cannot fail)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "<> 7" supabase/tests/p43_guard_marketing_smoke.sql` não encontra mais a comparação de classes com constante; `grep -n "<> 6"` não encontra a comparação de eventos aceitos com constante.
    - O arquivo usa `regexp_matches` sobre o `pg_get_constraintdef` também em (c), e `to_jsonb` para a impressão digital de y2.
    - O p43 roda verde em PROD; as sondas `y2` e `c` saem com `SONDA OK`.
    - Nenhuma perturbação de sonda comitou (a contagem de `classe_evento_notificacao` depois das sondas é a mesma de antes — registrada no SUMMARY).
  </acceptance_criteria>
  <done>O smoke que reprovava trabalho correto com diagnóstico falso passa a vigiar o vocabulário vivo — e foi visto mordendo.</done>
</task>

<task type="auto">
  <name>Task 2: p42 e p37 convertidos, e o artefato da varredura de portões</name>
  <files>supabase/tests/p42_notif_revisao_smoke.sql, supabase/tests/p37_fidelidade_schema_smoke.sql, .planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-PORTOES.md</files>
  <read_first>
    - `supabase/tests/p42_notif_revisao_smoke.sql` (`:130-190` — (a) com a lista literal e `v_aceitos <> 6`; `:280-290` (d))
    - `supabase/tests/p37_fidelidade_schema_smoke.sql` (`:140-160` constraints transcritas VERBATIM; `:215-235` contagem de colunas)
    - `supabase/tests/p43_guard_marketing_smoke.sql` (a conversão da Task 1 — mesmo idioma)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §I (a tabela dos smokes tocados)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-ETAPA-ATUAL.md` §1 (formato de artefato de varredura com padrão re-rodável)
  </read_first>
  <action>
`p42_notif_revisao_smoke.sql` (a): mesma conversão do p43 (c) — vocabulário extraído do CHECK vivo, sem os de classe `marketing`; aceite de todos; os 6 históricos exigidos por pertinência; gancho `smoke42n.sonda='a'` com o mesmo fecho (sempre termina em exceção, `SONDA OK: a mordeu`). A (d) não muda.

`p37_fidelidade_schema_smoke.sql`: a asserção de constraints de `notificacoes_enviadas` deixa de comparar o texto do CHECK de evento com a transcrição de 4 valores: passa a exigir que o CHECK vivo contenha `confirmacao`, `avanco`, `convite` e `decisao` (as demais constraints transcritas — FKs, PK, `uq_notif_dedupe` — continuam comparadas como estão, se ainda batem com PROD; se alguma outra também envelheceu, registrar e tratar com a mesma regra, sem constante nova). A contagem exata de colunas vira pertinência por NOME das colunas do P37 (e das 2 aditivas do modo teste). Gancho `smoke37f.sonda='check'` (exige um evento inexistente) e `'colunas'` (exige uma coluna inexistente), com o mesmo fecho. Comentar no cabeçalho que o smoke passou de «fidelidade ao instantâneo do P37» para «o P37 continua garantido» — e por quê (D-17).

Rodar os dois verdes; rodar as três sondas por cópia temporária no scratch (como na Task 1), cada uma saindo com `SONDA OK`.

Escrever `.planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-PORTOES.md` com: data; o padrão EXATO da varredura do CLAUDE.md e a contagem de linhas medida agora; a tabela dos achados que esta fase toca (partindo do RESEARCH §I) com classe (escopo deliberado × fotografia), o plano que trata cada um e o estado (convertido aqui / atualizado na mesma entrega do objeto novo / registrado sem tocar) — incluindo `p42_invent05_cron_smoke.sql` a.iii (escopo deliberado, atualizado pelo 48-13 junto do cron novo), `p45_motor_exclusao_smoke.sql` C6 (48-01), `p42_revisao_art20_smoke.sql` h.2 (48-11), `p41_recon_retry_smoke.sql:234` (48-13 confere), `p39_rewire_triggers_smoke.sql:189` (fotografia vermelha NÃO tocada — 6 hoje, 7 depois do trigger do 48-10), `p43_guard_marketing_smoke.sql:616` (escopo deliberado, nenhum trigger novo em `notificacoes_enviadas`); o resultado de cada sonda; e a instrução de re-rodar a varredura antes de cada apply que acrescente objeto vigiado.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs run supabase/tests/p42_notif_revisao_smoke.sql && node p46apply.cjs run supabase/tests/p37_fidelidade_schema_smoke.sql</automated>
    <fails_when>non-zero exit, or "FAIL" in either output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && D=$(mktemp -d) && run(){ { echo "SELECT set_config('$1', '$2', false);"; cat "$3"; } > "$D/s.sql"; OUT=$(node p46apply.cjs run "$D/s.sql" 2>&1); echo "$OUT" | grep -q "SONDA OK: $2 mordeu" || { echo "SONDA $2 NAO mordeu"; exit 1; }; echo "sonda $2 mordeu"; }; run smoke42n.sonda a supabase/tests/p42_notif_revisao_smoke.sql && run smoke37f.sonda check supabase/tests/p37_fidelidade_schema_smoke.sql && run smoke37f.sonda colunas supabase/tests/p37_fidelidade_schema_smoke.sql</automated>
    <fails_when>non-zero exit with "SONDA <x> NAO mordeu"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const s=require('fs').readFileSync('.planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-PORTOES.md','utf8');for(const t of ['IS DISTINCT FROM','p42_invent05_cron_smoke','p45_motor_exclusao_smoke','p42_revisao_art20_smoke','p41_recon_retry_smoke','p39_rewire_triggers_smoke','p43_guard_marketing_smoke','p42_notif_revisao_smoke','p37_fidelidade_schema_smoke','fotografia','escopo deliberado','SONDA OK'])if(!s.includes(t))throw new Error('artefato sem: '+t);if(!/\d{4}-\d{2}-\d{2}/.test(s))throw new Error('sem data');console.log('OK')"</automated>
    <fails_when>non-zero exit with "artefato sem:" or "sem data"</fails_when>
  </verify>
  <acceptance_criteria>
    - `p42_notif_revisao_smoke.sql` não compara mais `v_aceitos` com constante; `p37_fidelidade_schema_smoke.sql` não exige mais o CHECK com exatamente 4 valores nem contagem exata de colunas.
    - Os dois smokes verdes em PROD; as três sondas com `SONDA OK`.
    - `48-VARREDURA-PORTOES.md` existe, datado, com o padrão re-rodável, a contagem medida, a tabela classificada e o resultado das sondas.
  </acceptance_criteria>
  <done>Os portões do vocabulário vigiam o banco vivo em vez de um instantâneo — antes de o primeiro evento novo chegar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke → PROD | rodar um smoke é escrita em PROD (fixture + reversão) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-06-01 | Tampering | sonda comitando a perturbação (ex.: apagar uma classe de evento em PROD → P0003 em todo e-mail daquele evento) | high | mitigate | todo caminho da sonda termina em `RAISE EXCEPTION` dentro de subtransação; a Management API reverte a transação inteira; contagem conferida depois |
| T-48-06-02 | Repudiation | portão convertido incapaz de falhar | high | mitigate | sonda de mordida por execução para cada asserção convertida |
| T-48-06-03 | Denial of Service | smoke vermelho por fotografia bloqueando a fase | medium | mitigate | conversão para baseline da execução / pertinência |
| T-48-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- p43, p42_notif_revisao e p37 verdes em PROD; cinco sondas com `SONDA OK`.
- `48-VARREDURA-PORTOES.md` escrito.
</verification>

<success_criteria>
Quando o 48-10 e o 48-13 acrescentarem eventos, os portões os verão — e nenhum deles reprova trabalho correto por um número que envelheceu.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| ganchos `smoke43g.sonda` (`y2`, `c`), `smoke42n.sonda` (`a`), `smoke37f.sonda` (`check`, `colunas`) | mecanismo de sonda em smoke | **novos** |
| `supabase/tests/p43_guard_marketing_smoke.sql` | smoke | editado (y2 por impressão digital; (c) por catálogo) |
| `supabase/tests/p42_notif_revisao_smoke.sql` | smoke | editado ((a) por catálogo) |
| `supabase/tests/p37_fidelidade_schema_smoke.sql` | smoke | editado (pertinência) |
| `.planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-PORTOES.md` | artefato de varredura | **novo** |

## Premissas sinalizadas (probe de borda)

Este plano não tem requisito próprio; serve JORN-15 (D-22 cria evento) e JORN-19 (evento do prazo).
Nenhuma linha do relatório de borda é resolvida aqui.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-06-SUMMARY.md` when done
</output>
