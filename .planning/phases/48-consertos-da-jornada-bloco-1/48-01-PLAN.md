---
phase: 48-consertos-da-jornada-bloco-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260921000001_p48_candidatura_encerrada.sql
  - supabase/migrations/20260921000002_p48_encerrada_demais_instancias.sql
  - supabase/tests/p45_motor_exclusao_smoke.sql
  - supabase/tests/p48_candidatura_encerrada_smoke.sql
  - supabase/tests/funil34_kpis_smokes.sql
autonomous: true
requirements: [JORN-26]

estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Existe UMA função SQL `public.candidatura_encerrada(p_etapa public.etapa_processo, p_status public.status_candidatura)`, IMMUTABLE, `SET search_path = ''`, por ALLOWLIST de terminais — etapa em {aprovado, rejeitado} OU status em {rejeitado, finalizado} — NULL-safe por COALESCE. É o critério de `STATUS_TERMINAIS` que o painel do candidato já usa (`DashboardCandidatoPage.tsx:65`), promovido a canônico (D-11, D-21)."
    - "`registrar_pedido_exclusao` deixa de marcar `encerrada_a_pedido_em` em candidatura de knockout (`etapa_atual='inscricao'`, `status='rejeitado'`) e em `finalizado` legado, e continua marcando a candidatura em andamento — Defeito 26 (JORN-26, D-21)."
    - "`retirar_candidatura` recusa com 22023 candidatura encerrada pelo predicado canônico, no guard E no UPDATE — o caminho não-UI (EF acao=`retirar_candidatura`) deixa de retirar knockout e de avisar o RH (D2 da varredura)."
    - "`rejeitar_candidatura` recusa com `check_violation` candidatura encerrada pelo predicado canônico. Isto é pré-requisito do JORN-18 (plano 48-08): sem esta trava, a chave nova por transição transformaria a re-rejeição de um knockout num 2º e-mail de rejeição (D3 da varredura)."
    - "`v_fila_trabalho` não lista candidatura encerrada — em PROD, `select count(*) from public.v_fila_trabalho f join public.candidaturas c on c.id = f.candidatura_id where public.candidatura_encerrada(c.etapa_atual, c.status)` devolve 0 (hoje: 3 knockouts + 3 finalizado) (D4)."
    - "`funil_kpis` (CTE `volume`) não conta knockout nem `finalizado` legado como volume de etapa de trabalho; os baldes `aprovado` e `rejeitado` continuam contando o que contavam (D6)."
    - "BORDA (explicit): as 4 linhas legadas com `status='finalizado'` em etapa de trabalho (`triagem`, `entrevista_online`, `decisao_final`) e a linha `rejeitado/finalizado` contam como ENCERRADAS para a fila, a retirada, a rejeição e o pedido de exclusão."
    - "BORDA (explicit): etapa de trabalho com status NULO não é encerrada (COALESCE para false — nulo não é terminal); etapa NULA com status `rejeitado` É encerrada."
    - "A purga NÃO foi tocada: `md5(prosrc)` de `candidaturas_alem_da_janela()` é o mesmo antes e depois dos dois applies (D-21 — retenção não declarada do knockout fica registrada em Deferred, não consertada)."
    - "O smoke `p45_motor_exclusao_smoke.sql` passa com a fixture refeita (INSERT com `status='rejeitado'` que desarma o dispatch + UPDATE para `em_analise`), e ganhou a asserção negativa: fixture com forma de knockout NÃO é marcada."
    - "Toda migration deste plano foi aplicada com `node p46apply.cjs migrate`, md5 do ledger conferido, e partiu da definição VIVA lida por `pg_get_functiondef`/`pg_get_viewdef` com pré-portão de md5 (D-16, D-11a)."
    - "A varredura de portões do CLAUDE.md foi rodada ANTES do apply e nenhum achado que cite objeto deste plano ficou sem classificação (D-17)."
  artifacts:
    - path: supabase/migrations/20260921000001_p48_candidatura_encerrada.sql
      provides: "função canônica + D1 (registrar_pedido_exclusao)"
      contains: "candidatura_encerrada"
    - path: supabase/migrations/20260921000002_p48_encerrada_demais_instancias.sql
      provides: "D2 retirar_candidatura, D3 rejeitar_candidatura, D4 v_fila_trabalho, D6 funil_kpis"
      contains: "candidatura_encerrada"
    - path: supabase/tests/p48_candidatura_encerrada_smoke.sql
      provides: "tabela-verdade do predicado lida do pg_enum + comportamento de D1–D6"
    - path: supabase/tests/p45_motor_exclusao_smoke.sql
      provides: "fixture C6 refeita + C6-neg"
  key_links:
    - from: "public.candidatura_encerrada"
      to: "registrar_pedido_exclusao / retirar_candidatura / rejeitar_candidatura / v_fila_trabalho / funil_kpis"
      via: "chamada no predicado de cada objeto vivo"
      pattern: "candidatura_encerrada\\("
    - from: "public.candidatura_encerrada"
      to: "src/lib/candidatura/candidaturaEncerrada.ts (plano 48-02)"
      via: "mesma allowlist de terminais, escrita duas vezes — SQL decide, TS esconde ação"
      pattern: "rejeitado.*finalizado"
  prohibitions:
    - requirement_id: JORN-26
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT disparar `candidatura_encerrada_a_pedido` aos RH para candidatura que o knockout já encerrou — o aviso revela o exercício de um direito do titular sobre um caso que já tinha acabado"
    - requirement_id: JORN-26
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT tocar o predicado da purga (`candidaturas_alem_da_janela`, `config_retencao_etapa.elegivel_purga`) — mecanismo destrutivo com flip dry_run→live pendente, fora por D-21"
    - requirement_id: JORN-26
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT editar migration antiga para «corrigir» o predicado — migrations são histórico e o `statements[1]` do ledger é conferido por md5"
---

<objective>
Um predicado canônico de «candidatura encerrada» no banco, e as cinco instâncias vivas da mesma
forma (D1, D2, D3, D4, D6 da varredura) consertadas por ele — antes de qualquer outro plano desta
fase mexer em `rejeitar_candidatura` ou na chave de dedupe.

Purpose: os Defeitos 22 e 26 são o MESMO erro — código que usa `etapa_atual` para saber se a
candidatura acabou, ignorando `status` (D-11). O knockout mantém `etapa_atual='inscricao'` com
`status='rejeitado'` **por desenho**, e todo predicado que olha só a etapa erra exatamente ali. A
varredura (`48-VARREDURA-ETAPA-ATUAL.md`) já está feita e classificada; este plano é o conserto que
ela alimenta (D-11d). A purga (D5) fica de fora por D-21.

Output: duas migrations aplicadas em PROD, o smoke novo do predicado, e a fixture do
`p45_motor_exclusao_smoke.sql` refeita (ela codificava o Defeito 26 e reprovaria o conserto correto).
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-CONTEXT.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-ETAPA-ATUAL.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer) [BLOCKING]: a função canônica + o pedido de exclusão (D1) + a fixture do p45 — aplicados e provados em PROD</name>
  <precondition>`node p46apply.cjs sql "SET TRANSACTION READ ONLY; select 1"` responde (token no Keychain, serviço "Supabase CLI"). Sem isso não há apply nem leitura viva — parar e reportar.</precondition>
  <files>supabase/migrations/20260921000001_p48_candidatura_encerrada.sql, supabase/tests/p45_motor_exclusao_smoke.sql</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-ETAPA-ATUAL.md` §3 (forma recomendada da função), §5 D1, §6 (a receita da fixture do p45)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §1 (cabeçalho, transporte, ACL com `anon` NOMEADO, portão de auto-verificação), §2, §6
    - `supabase/migrations/20260805000002_p45_rpc_pedido_exclusao.sql` (`:216-220` o UPDATE com o predicado errado — base estática de D1)
    - `supabase/migrations/20260805000009_p45_claims_do_titular.sql` (também toca a função — conferir o que mudou)
    - `supabase/migrations/20260906000007_explicacao_knockout.sql` (`:1-52` — cabeçalho e nota de transporte atuais)
    - `supabase/tests/p45_motor_exclusao_smoke.sql` (`:853-864` e `:2238-2245` — as duas fixtures; e a asserção C6 que as consome)
    - `CLAUDE.md` §«Via de apply ATUAL» e §«Portões: varra pela FORMA»
  </read_first>
  <action>
Passo 1 — ler o VIVO, só leitura: `node p46apply.cjs sql "SET TRANSACTION READ ONLY; select md5(p.prosrc), length(p.prosrc), pg_get_functiondef(p.oid) from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public' and p.proname in ('registrar_pedido_exclusao','candidaturas_alem_da_janela')"`. Anotar os dois md5 no SUMMARY (o da purga é a linha de base da prova de não-toque). Diffar o corpo vivo de `registrar_pedido_exclusao` contra `20260805000002` + `20260805000009` e registrar a diferença, se houver.

Passo 2 — rodar a varredura de portões (D-17), o padrão exato do CLAUDE.md sobre `supabase/tests/*.sql`, e conferir se algum achado cita `candidatura_encerrada` ou `registrar_pedido_exclusao`. Registrar a contagem de linhas e a conclusão no SUMMARY. Se algum achado citar um desses objetos, classificá-lo (escopo deliberado × fotografia) e tratar antes do apply.

Passo 3 — escrever `supabase/migrations/20260921000001_p48_candidatura_encerrada.sql` com o cabeçalho do M8 (O QUE ESTAVA ERRADO medido, POR QUÊ, AUTHZ, IDEMPOTÊNCIA, nota de transporte `APLICAR COM: node p46apply.cjs migrate supabase/migrations/20260921000001_p48_candidatura_encerrada.sql`), SEM wrapper de transação externo e SEM o bloco obsoleto de «reparo do ledger». Conteúdo, nesta ordem:
(a) bloco `DO` de pré-portão que lê `md5(prosrc)` de `registrar_pedido_exclusao` e aborta se divergir do md5 medido no Passo 1 (o CREATE OR REPLACE apagaria uma divergência em silêncio);
(b) `CREATE OR REPLACE FUNCTION public.candidatura_encerrada(p_etapa public.etapa_processo, p_status public.status_candidatura) RETURNS boolean LANGUAGE sql IMMUTABLE SET search_path = ''` com a allowlist de terminais NULL-safe da varredura §3 (etapa em aprovado/rejeitado, status em rejeitado/finalizado, cada comparação envolvida em COALESCE para false);
(c) `REVOKE ALL … FROM PUBLIC, anon` e `GRANT EXECUTE … TO authenticated, service_role` (anon NOMEADO — o `pg_default_acl` concede direto);
(d) `COMMENT ON FUNCTION` registrando: a origem (`STATUS_TERMINAIS` do painel, D-11), que é allowlist de terminais e não denylist de ativos, que `encerrada_a_pedido_em` fica FORA de propósito (Invariante 9 da 45-UI-SPEC — a retirada continua visível ao RH), e que a purga é uma instância conhecida da mesma forma NÃO consertada aqui (D-21);
(e) `CREATE OR REPLACE` de `registrar_pedido_exclusao` a partir do corpo VIVO do Passo 1 com UMA única troca: o predicado do UPDATE de `candidaturas` que hoje é a negação da etapa em aprovado/rejeitado passa a ser `AND NOT public.candidatura_encerrada(c.etapa_atual, c.status)`. Nada mais muda (assinatura, retorno, guards, escopo por `candidato_id`); reafirmar os grants vivos lidos no Passo 1 depois do CREATE OR REPLACE;
(f) bloco `DO` de auto-verificação por leitura de catálogo: a definição instalada de `registrar_pedido_exclusao` contém `candidatura_encerrada(`; `candidatura_encerrada` tem `provolatile = 'i'`; `has_function_privilege('anon', 'public.candidatura_encerrada(public.etapa_processo, public.status_candidatura)', 'EXECUTE')` é false; o md5 de `candidaturas_alem_da_janela` é o do Passo 1.

Passo 4 — refazer as duas fixtures de `supabase/tests/p45_motor_exclusao_smoke.sql` (`:853-864`, `:2238-2245`) conforme a varredura §6: INSERT com `status='rejeitado'` (desarma o dispatch do AFTER INSERT) e em seguida UPDATE para `status='em_analise'` — a candidatura passa a estar em andamento DE VERDADE, e C6 continua esperando o encerramento. Acrescentar ao lado de C6 a asserção C6-neg: uma fixture do mesmo titular com a forma do knockout (`etapa_atual='inscricao'`, `status='rejeitado'`) NÃO recebe `encerrada_a_pedido_em`. Comentar no arquivo por que a fixture antiga codificava o Defeito 26.

Passo 5 — aplicar: `node p46apply.cjs migrate supabase/migrations/20260921000001_p48_candidatura_encerrada.sql`.

Passo 6 — rodar `node p46apply.cjs run supabase/tests/p45_motor_exclusao_smoke.sql` (é escrita em PROD com fixture e teardown — autorizada por D-18). Ele tem de passar inteiro, incluindo C6 e C6-neg.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const f='supabase/migrations/20260921000001_p48_candidatura_encerrada.sql';const s=fs.readFileSync(f,'utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');if(!/APLICAR COM: node p46apply\.cjs migrate/.test(s))throw new Error('nota de transporte ausente');for(const t of ['CREATE OR REPLACE FUNCTION public.candidatura_encerrada','IMMUTABLE',\"SET search_path = ''\",'COALESCE','REVOKE ALL ON FUNCTION public.candidatura_encerrada','anon','GRANT EXECUTE','COMMENT ON FUNCTION public.candidatura_encerrada','registrar_pedido_exclusao','NOT public.candidatura_encerrada(c.etapa_atual, c.status)','md5'])if(!c.includes(t))throw new Error('falta: '+t);if(/etapa_atual\s+NOT\s+IN\s*\(\s*'aprovado'\s*,\s*'rejeitado'\s*\)/i.test(c.split('CREATE OR REPLACE FUNCTION public.registrar_pedido_exclusao')[1]||''))throw new Error('o predicado antigo continua no corpo novo');if(/CREATE\s+OR\s+REPLACE\s+FUNCTION\s+public\.candidaturas_alem_da_janela/i.test(c))throw new Error('a purga NAO pode ser redefinida (D-21)');console.log('OK migration')"</automated>
    <fails_when>non-zero exit with "falta:", "wrapper BEGIN proibido", "o predicado antigo continua" or "a purga NAO pode ser redefinida" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs migrate supabase/migrations/20260921000001_p48_candidatura_encerrada.sql</automated>
    <fails_when>non-zero exit, or the output lacks the line "md5 do ledger BATE"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs run supabase/tests/p45_motor_exclusao_smoke.sql</automated>
    <fails_when>non-zero exit, or the output contains "FAIL"</fails_when>
  </verify>
  <acceptance_criteria>
    - O arquivo da migration existe, sem `BEGIN;` externo, com a nota de transporte `p46apply.cjs migrate`, e sem o bloco obsoleto de reparo de `version` do ledger.
    - `pg_get_functiondef('public.candidatura_encerrada(public.etapa_processo, public.status_candidatura)'::regprocedure)` existe em PROD e o catálogo mostra `provolatile='i'` e `prosecdef=false`.
    - `pg_get_functiondef` de `registrar_pedido_exclusao` em PROD contém `candidatura_encerrada(` e não contém mais a negação da etapa em aprovado/rejeitado.
    - `has_function_privilege('anon', …candidatura_encerrada…, 'EXECUTE')` = false; `has_function_privilege('authenticated', …, 'EXECUTE')` = true.
    - O md5 de `candidaturas_alem_da_janela` lido depois do apply é igual ao do Passo 1 (registrado no SUMMARY).
    - `p45_motor_exclusao_smoke.sql` roda sem `FAIL`, com C6 e C6-neg aprovados.
    - A contagem de linhas da varredura de portões e a classificação dos achados tocados estão no SUMMARY.
  </acceptance_criteria>
  <done>O pedido de exclusão para de marcar candidatura que o knockout encerrou — medido por um smoke em PROD que também prova que a candidatura em andamento continua sendo marcada.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: as demais instâncias vivas — retirar (D2), rejeitar (D3), fila (D4) e KPI do funil (D6)</name>
  <files>supabase/migrations/20260921000002_p48_encerrada_demais_instancias.sql</files>
  <read_first>
    - `supabase/migrations/20260921000001_p48_candidatura_encerrada.sql` (a função escrita na Task 1)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-VARREDURA-ETAPA-ATUAL.md` §5 (D2, D3, D4, D6)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §2 (os pontos exatos a trocar em D2, D3, D4, D6)
    - `supabase/migrations/20260805000007_p45_retirada_e_evento.sql` (BLOCO A `:156-235` — `retirar_candidatura`)
    - `supabase/migrations/20260714100001_rejeitar_candidatura_rpc.sql` (`:114-138` — o SELECT e o guard terminal; `:140-151` — o UPDATE único que NÃO muda aqui)
    - `supabase/migrations/20260716000003_funil_kpis_v2_and_v_fila_trabalho.sql` (`:24-43` a view; `:92-99` a CTE `volume`)
    - `src/features/funil/services/funilKpisService.ts` (`:68` — como o front consome o volume por etapa; confirmar que os baldes terminais continuam necessários)
  </read_first>
  <action>
Ler o VIVO, só leitura, com md5: `pg_get_functiondef` + `md5(prosrc)` de `retirar_candidatura`, `rejeitar_candidatura`, `funil_kpis`, e `pg_get_viewdef('public.v_fila_trabalho'::regclass, true)` + o md5 desse texto. Diffar contra os arquivos-base citados e registrar divergências no SUMMARY.

Escrever `supabase/migrations/20260921000002_p48_encerrada_demais_instancias.sql` (mesmo cabeçalho e transporte da Task 1), com um `DO` de pré-portão por objeto (aborta se o md5 vivo divergir do medido) e depois:

(D2) `retirar_candidatura`: o SELECT do guard passa a ler também `c.status`; o guard vira «deletada OU `public.candidatura_encerrada(etapa, status)`» mantendo o `22023` e a mensagem `CANDIDATURA_NAO_RETIRAVEL`; o predicado do UPDATE troca a negação da etapa por `AND NOT public.candidatura_encerrada(c.etapa_atual, c.status)`. Reafirmar os grants vivos (hoje: sem `anon`, sem `authenticated`, EXECUTE para `service_role`).

(D3) `rejeitar_candidatura`: o SELECT passa a ler `c.status`; o guard terminal passa a ser `IF public.candidatura_encerrada(<etapa>, <status>) THEN RAISE … USING ERRCODE = 'check_violation'` com mensagem que diga «candidatura já encerrada (etapa %, status %) — não pode ser rejeitada novamente». Use os tipos enum (cast explícito se a variável local for `text`). O UPDATE único (`:146-151`) NÃO muda neste plano — a gravação do `feedback_rejeicao` neutro é do plano 48-09, que parte do corpo instalado aqui.

(D4) `v_fila_trabalho`: `CREATE OR REPLACE VIEW … WITH (security_invoker = true)` com AS MESMAS colunas NA MESMA ORDEM do texto vivo; trocar só o filtro de etapa terminal por `AND NOT public.candidatura_encerrada(c.etapa_atual, c.status)`. Reafirmar `GRANT SELECT ON public.v_fila_trabalho TO authenticated`.

(D6) `funil_kpis`: na CTE `volume`, acrescentar o filtro que exclui do volume das etapas de TRABALHO as linhas com status terminal, preservando os baldes terminais: `AND (c.etapa_atual IN ('aprovado','rejeitado') OR NOT public.candidatura_encerrada(c.etapa_atual, c.status))`. Nenhuma coluna de retorno muda (a assinatura muda nada — senão seria DROP).

Terminar com um `DO` de auto-verificação por catálogo: as três funções e a view contêm `candidatura_encerrada(`; `retirar_candidatura` e `rejeitar_candidatura` não contêm mais o teste da etapa em aprovado/rejeitado como guard terminal; a lista de colunas de `v_fila_trabalho` (de `information_schema.columns`, na ordem de `ordinal_position`) é idêntica à lida antes.

Rodar a varredura de portões de novo e conferir se algum achado cita `retirar_candidatura`, `rejeitar_candidatura`, `v_fila_trabalho` ou `funil_kpis`; o `funil34_kpis_smokes.sql` é o suspeito natural (volume por etapa). Se ele codificar o volume antigo (knockout contado em `inscricao`), ajustá-lo NESTE plano para comparar com a regra nova e registrar a razão no arquivo.

Aplicar: `node p46apply.cjs migrate supabase/migrations/20260921000002_p48_encerrada_demais_instancias.sql`. Depois rodar `node p46apply.cjs run supabase/tests/oper31_rejeitar_candidatura_smokes.sql` e `node p46apply.cjs run supabase/tests/funil34_kpis_smokes.sql` (regressão — escrita em PROD autorizada por D-18).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260921000002_p48_encerrada_demais_instancias.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');for(const t of ['public.retirar_candidatura','public.rejeitar_candidatura','public.v_fila_trabalho','public.funil_kpis','security_invoker','GRANT SELECT ON public.v_fila_trabalho TO authenticated','check_violation','22023','md5'])if(!c.includes(t))throw new Error('falta: '+t);const n=(c.match(/candidatura_encerrada\(/g)||[]).length;if(n<5)throw new Error('predicado canonico usado so '+n+' vezes (esperado >=5: D2 guard, D2 update, D3, D4, D6)');if(/CREATE\s+OR\s+REPLACE\s+FUNCTION\s+public\.candidaturas_alem_da_janela/i.test(c))throw new Error('a purga NAO pode ser redefinida (D-21)');console.log('OK')"</automated>
    <fails_when>non-zero exit with "falta:", "predicado canonico usado so" or "a purga NAO pode ser redefinida" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs migrate supabase/migrations/20260921000002_p48_encerrada_demais_instancias.sql && node p46apply.cjs run supabase/tests/oper31_rejeitar_candidatura_smokes.sql && node p46apply.cjs run supabase/tests/funil34_kpis_smokes.sql</automated>
    <fails_when>non-zero exit; "md5 do ledger BATE" absent from the migrate output; or "FAIL" in either smoke output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs sql "SET TRANSACTION READ ONLY; select count(*)::int as n from public.v_fila_trabalho f join public.candidaturas c on c.id = f.candidatura_id where public.candidatura_encerrada(c.etapa_atual, c.status)" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);if(r[0].n!==0){console.error('fila ainda tem '+r[0].n+' encerrada(s)');process.exit(1)}console.log('fila sem encerradas')})"</automated>
    <fails_when>non-zero exit with "fila ainda tem N encerrada(s)"</fails_when>
  </verify>
  <acceptance_criteria>
    - Em PROD, `pg_get_functiondef` de `retirar_candidatura` e de `rejeitar_candidatura` contém `candidatura_encerrada(`; o de `funil_kpis` também; `pg_get_viewdef` de `v_fila_trabalho` também.
    - As colunas de `v_fila_trabalho` (nome e ordem) são idênticas às de antes do apply — o `DO` da migration prova.
    - `v_fila_trabalho` sem nenhuma linha encerrada (consulta acima devolve 0).
    - `oper31_rejeitar_candidatura_smokes.sql` e `funil34_kpis_smokes.sql` passam; se o `funil34` foi ajustado, o motivo está comentado no arquivo e no SUMMARY.
    - O `UPDATE` único de `rejeitar_candidatura` é byte a byte o que era (nenhum `feedback_rejeicao` ainda — isso é o 48-09).
  </acceptance_criteria>
  <done>Retirada, re-rejeição, fila de trabalho e volume do funil passam a respeitar o mesmo critério de «encerrada» — e a trava de re-rejeição está no ar antes da chave de dedupe nova.</done>
</task>

<task type="auto">
  <name>Task 3: o smoke do predicado — tabela-verdade lida do catálogo, comportamento de D1–D6, e a publicação</name>
  <files>supabase/tests/p48_candidatura_encerrada_smoke.sql</files>
  <read_first>
    - `supabase/tests/p43_matriz_retencao_smoke.sql` (`:95-163` fixture + impressão digital; `:668-683` comparação; o contador `smoke43m.pass`)
    - `supabase/migrations/20260805000007_p45_retirada_e_evento.sql` (`:443-530` — subtransação que reverte com SQLSTATE próprio)
    - `supabase/tests/p45_motor_exclusao_smoke.sql` (a fixture refeita na Task 1 — o idioma INSERT `rejeitado` → UPDATE `em_analise`)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §«Shared Patterns» E
  </read_first>
  <action>
Criar `supabase/tests/p48_candidatura_encerrada_smoke.sql` com docblock que diga o que ele vigia e por que a tabela-verdade é lida do catálogo (D-17: baseline capturada na execução, nunca constante). Contador `smoke48e.pass` por `set_config`, gate final que levanta exceção se o total não bater, e um `SELECT` final que devolve `{smoke:'p48_candidatura_encerrada', pass:<n>}` (é o que o `p46apply run` imprime). Asserções, nesta ordem:

(a) Tabela-verdade: iterar TODOS os pares de `pg_enum` de `etapa_processo` × `status_candidatura` (mais os casos com NULL de cada lado) e comparar `public.candidatura_encerrada` com uma expressão independente escrita no próprio smoke; a lista de valores vem do catálogo na execução.
(b) Bordas explícitas: `(triagem, finalizado)` → true; `(inscricao, rejeitado)` → true; `(triagem, NULL)` → false; `(NULL, rejeitado)` → true.
(c) D1 — numa subtransação que reverte (SQLSTATE próprio, capturado): fixture de um titular com uma candidatura em andamento e uma com forma de knockout (idioma da fixture refeita do p45); chamar `registrar_pedido_exclusao` com o JWT do titular; exigir a em andamento marcada e a de knockout não.
(d) D2 — `retirar_candidatura` sobre fixture de knockout e de `finalizado` em etapa de trabalho → `22023`; sobre a em andamento → aceita. Tudo revertido.
(e) D3 — `rejeitar_candidatura` com claim de `administrador` e justificativa de 50+ caracteres sobre knockout e sobre `finalizado` → `check_violation`; sobre a em andamento → aceita. Tudo revertido (o `net.http_post` do trigger só sai depois do COMMIT, então a reversão não manda e-mail).
(f) D4 — sem fixture: nenhuma linha de `v_fila_trabalho` é encerrada pelo predicado.
(g) D6 — `funil_kpis(NULL)` com claim de administrador: o volume de cada etapa de trabalho é igual à contagem direta das candidaturas daquela etapa que NÃO são encerradas; os baldes `aprovado`/`rejeitado` são iguais à contagem direta por etapa.
(h) NEGATIVA — contagens de `candidaturas`, `historico_candidatura` e `solicitacoes_dados` antes e depois do smoke são idênticas (o smoke não deixa resíduo).

Rodar: `node p46apply.cjs run supabase/tests/p48_candidatura_encerrada_smoke.sql`.

Publicar: a mudança de `v_fila_trabalho` e do KPI é VISÍVEL na tela do RH, e o front sai pela Vercel a partir do `main` (CLAUDE.md §«Esta via NÃO passa pelo git»). Commitar e rodar `git push origin main`; depois conferir que `git log --oneline origin/main..HEAD` sai vazio.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs run supabase/tests/p48_candidatura_encerrada_smoke.sql</automated>
    <fails_when>non-zero exit, "FAIL" in output, or the final row is absent / its pass count is lower than the number of assertions (a)–(h)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && echo "origin/main em dia"</automated>
    <fails_when>non-zero exit (git log origin/main..HEAD printed at least one commit)</fails_when>
  </verify>
  <acceptance_criteria>
    - O smoke existe, lê os valores dos dois enums de `pg_enum` na execução (nenhuma lista literal de etapas/status como fonte da tabela-verdade) e tem as asserções (a)–(h) nomeadas.
    - O smoke roda em PROD sem `FAIL` e imprime o total de aprovações.
    - A asserção (h) prova zero resíduo.
    - `git log --oneline origin/main..HEAD` vazio depois do push.
  </acceptance_criteria>
  <done>O critério de «encerrada» está provado por execução em PROD, em todos os pares do vocabulário vivo, e cada instância consertada tem uma asserção que morde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| titular (JWT) → `registrar_pedido_exclusao` / `retirar_candidatura` (via EF) | o titular escolhe o que pede; a RPC decide o que encerra |
| RH (JWT) → `rejeitar_candidatura` | escrita privilegiada SECURITY DEFINER |
| `authenticated` → `v_fila_trabalho` / `funil_kpis` | leitura com `security_invoker` e escopo por vaga |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01-01 | Information Disclosure | aviso ao RH de «encerrada a pedido» sobre knockout | medium | mitigate | predicado canônico no UPDATE de D1/D2 — o trigger de aviso só dispara em NULL→NOT NULL, e a linha de knockout deixa de ser tocada; smoke (c)/(d) |
| T-48-01-02 | Tampering | re-rejeição de knockout reescrevendo `motivo_rejeicao` | high | mitigate | guard D3 por `candidatura_encerrada`; smoke (e) prova `check_violation` |
| T-48-01-03 | Elevation of Privilege | função nova chamável por `anon` | low | mitigate | `REVOKE … FROM PUBLIC, anon` nomeado; `DO` da migration assere `has_function_privilege('anon',…)=false` |
| T-48-01-04 | Tampering | CREATE OR REPLACE apagando divergência viva | high | mitigate | pré-portão de md5 do `prosrc`/viewdef vivo em cada objeto redefinido |
| T-48-01-05 | Denial of Service | view com colunas reordenadas quebrando o front da fila | medium | mitigate | `DO` compara nome+ordem das colunas antes/depois |
| T-48-01-06 | Tampering | purga alterada por engano | high | mitigate | md5 de `candidaturas_alem_da_janela` conferido antes e depois; verify reprova a redefinição |
| T-48-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação — o plano só escreve SQL |
</threat_model>

<verification>
- Duas migrations aplicadas, md5 do ledger conferido em cada uma.
- `p45_motor_exclusao_smoke`, `oper31_rejeitar_candidatura_smokes`, `funil34_kpis_smokes` e `p48_candidatura_encerrada_smoke` verdes em PROD.
- `v_fila_trabalho` sem candidatura encerrada; purga intocada (md5).
- `git log --oneline origin/main..HEAD` vazio.
</verification>

<success_criteria>
Nenhum objeto vivo do banco da lista D-21 decide «acabou / está em andamento» só por `etapa_atual`: todos chamam a mesma função, e a trava de re-rejeição está no ar antes do plano 48-08.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `public.candidatura_encerrada(etapa_processo, status_candidatura)` | função SQL IMMUTABLE | **nova** |
| `public.registrar_pedido_exclusao(uuid)` | RPC SECURITY DEFINER | editada (D1) |
| `public.retirar_candidatura(uuid)` | RPC SECURITY DEFINER | editada (D2) |
| `public.rejeitar_candidatura(uuid, motivo_rejeicao_rh, text)` | RPC SECURITY DEFINER | editada (D3 — guard) |
| `public.v_fila_trabalho` | view `security_invoker` | editada (D4) |
| `public.funil_kpis(uuid)` | RPC | editada (D6 — CTE `volume`) |
| `supabase/migrations/20260921000001_p48_candidatura_encerrada.sql` | migration | **nova** |
| `supabase/migrations/20260921000002_p48_encerrada_demais_instancias.sql` | migration | **nova** |
| `supabase/tests/p48_candidatura_encerrada_smoke.sql` | smoke SQL | **novo** |
| `supabase/tests/p45_motor_exclusao_smoke.sql` | smoke SQL | editado (fixture C6 + C6-neg) |
| `supabase/tests/funil34_kpis_smokes.sql` | smoke SQL | conferido; editado só se codificar o volume antigo |

## Premissas sinalizadas (probe de borda)

O probe determinístico classificou **JORN-26** como `unclassified` (texto em português). A linha fica
`unresolved` no relatório do probe; as duas bordas `explicit` acima (os 4 `finalizado` legados contam
como encerrados; status nulo não é terminal) foram escritas a partir de `48-VARREDURA-ETAPA-ATUAL.md`
§2–§4 e do RESEARCH §A, não inferidas pelo probe.

**Contato registrado, não consertado:** D5 (`candidaturas_alem_da_janela()` — o knockout nunca é
purgável) fica em Deferred por D-21. `registrar_decisao` também não tem guard de estado (varredura
§5 iii) — não está na lista de D-21 e fica fora deste plano.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-01-SUMMARY.md` when done
</output>
