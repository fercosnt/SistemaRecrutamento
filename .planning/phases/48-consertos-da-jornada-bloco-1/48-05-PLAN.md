---
phase: 48-consertos-da-jornada-bloco-1
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/gerar-devolutiva-bigfive/index.ts
  - supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts
  - supabase/functions/submit-bigfive-final/index.ts
  - supabase/functions/submit-bigfive-final/index.test.ts
  - .planning/phases/48-consertos-da-jornada-bloco-1/48-EVIDENCIA-JORN06.md
autonomous: false
requirements: [JORN-06]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A CAUSA do 401 é MEDIDA antes do conserto (D-13): uma submissão real de Big Five, feita pelo operador com conta de teste em `avaliacao_assincrona`, produz no log de `gerar-devolutiva-bigfive` a linha `diag-auth` e a linha de recusa da guarda (`Rejected request: invalid/absent Bearer`), lidas NO MESMO DIA (retenção ≈ 1 dia) e transcritas em `48-EVIDENCIA-JORN06.md`."
    - "A instrumentação loga SÓ presença, esquema, classe de formato (`vazio|sb_secret|sb_publishable|jwt|outro`), comprimento e o NOME da env comparada — nunca o valor do Bearer, do apikey ou da chave esperada."
    - "A investigação distingue gateway de código (D-13): o conserto só é escrito se a linha de recusa da GUARDA estiver no log (o código rodou — não foi o `verify_jwt` do gateway) E o `diag-auth` casar com a previsão de H1: `auth_presente:false`, `apikey_formato:\"sb_secret\"`, `esperado_formato:\"sb_secret\"`."
    - "Se a medição NÃO casar com H1, o plano PARA e reporta ao operador — nenhum conserto por palpite, nenhuma troca às cegas de `functions.invoke` por `fetch` (D-13, D-14)."
    - "O conserto derivado de H1: `submit-bigfive-final` passa `Authorization: Bearer <chave de serviço>` explicitamente nas `headers` de `supabaseAdmin.functions.invoke('gerar-devolutiva-bigfive', …)`; a chave entra nas deps do handler (`SubmitBigfiveFinalDeps.serviceKey`) a partir de `SUPABASE_SERVICE_ROLE_KEY` no wiring do `Deno.serve`. `verify_jwt=false` de `gerar-devolutiva-bigfive` NÃO muda."
    - "A falha da devolutiva passa a ser OBSERVÁVEL e continua best-effort: o `catch` mudo vira log por código (`error?.name`, `error?.context?.status`, `timeout`), e o candidato continua recebendo `{ ok: true }` (contrato inalterado)."
    - "DECISÃO sobre o log de diagnóstico: o bloco `diag-auth` é REMOVIDO no deploy do conserto — cumpriu a função de medir, e mesmo formato+comprimento de uma credencial em todo request é impressão digital desnecessária nos logs da plataforma. O log por código do `submit-bigfive-final` FICA (observabilidade permanente)."
    - "O backlog de devolutivas faltantes é MEDIDO, só leitura, depois do conserto (`scores_candidato` `tipo='big_five'` sem devolutiva — esperado 2: `bf26ee3c-0ae3-4e92-a99b-6e05efc2a662` e a da submissão de prova), com o par `{candidatura_id, score_id}` de cada um e a confirmação MEDIDA de que todos são de conta de teste `+claude`; com candidato real no backlog, o plano para antes da geração (D-18)."
    - "A geração retroativa é do OPERADOR (checkpoint:human-action): ele invoca `gerar-devolutiva-bigfive` pelo painel da Supabase — a plataforma fornece a credencial do lado do servidor — com os pares medidos; depois, cada par tem exatamente uma linha em `devolutivas_candidato` e o edge log mostra 200. O operador pode RECUSAR: o backlog é só de contas de teste, e a prova do JORN-06 é a submissão NOVA pelo navegador depois do conserto (48-18, sessão 1, passo f), que passa pelo `Deno.serve` + `functions.invoke` reais."
    - "Nenhum processo local obtém a credencial de serviço: nenhuma task chama endpoint da Management API que devolva segredo (a pesquisa proibiu; o CLAUDE.md mantém a credencial de serviço dentro das Edge Functions). Se o painel devolver 401, isso é registrado e o plano para — a credencial não é procurada por outra via."
    - "BORDA (explicit): antes do conserto, a submissão de prova devolve `{ok:true}` ao candidato mesmo com o 401 interno (o best-effort não mudou) — o que muda é que a falha aparece no log com código."
  artifacts:
    - path: supabase/functions/gerar-devolutiva-bigfive/index.ts
      provides: "instrumentação diag-auth (Task 1) e sua remoção (Task 3)"
    - path: supabase/functions/submit-bigfive-final/index.ts
      provides: "log por código + Authorization explícito no invoke"
      contains: "serviceKey"
    - path: .planning/phases/48-consertos-da-jornada-bloco-1/48-EVIDENCIA-JORN06.md
      provides: "a medição datada que autoriza (ou barra) o conserto"
  key_links:
    - from: "supabase/functions/submit-bigfive-final/index.ts (functions.invoke)"
      to: "supabase/functions/gerar-devolutiva-bigfive/index.ts (guardDevolutivaBearer)"
      via: "header Authorization: Bearer <SUPABASE_SERVICE_ROLE_KEY>"
      pattern: "Authorization.*Bearer"
  prohibitions:
    - requirement_id: JORN-06
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT logar o valor (nem prefixo que o reconstrua) do Bearer, do apikey ou da chave de serviço — a EF de destino guarda o segredo e um log com ele vazaria a chave de serviço nos logs da plataforma"
    - requirement_id: JORN-06
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT escrever o conserto antes de a medição casar com a causa prevista — aplicado às cegas é o chute que D-13 proíbe"
    - requirement_id: JORN-06
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT trazer a credencial de serviço para nenhum processo local (script, shell, memória de processo), com ou sem checkpoint — nenhum endpoint da Management API que devolva segredo é chamado; a geração retroativa é do operador, no painel"
---

<objective>
Fazer a devolutiva do Big Five voltar a ser gerada — com a causa do 401 PROVADA por medição antes do
conserto (D-13).

Purpose: é o único item do bloco que precisa de investigação antes de virar conserto. O que está
provado: o 401 vem da guarda no código de `gerar-devolutiva-bigfive` (log da própria guarda, 9 ms
antes), com `verify_jwt=false` no deploy; a chave do ambiente das EFs é `sb_secret_`; o supabase-js
2.110.9 do bundle não põe `Authorization` para chave `sb_secret_` sem sessão (`omitApiKeyAsBearer`).
O que falta é ver, num log que o nomeie, que o header chegou AUSENTE (H1). Uma submissão instrumentada
fecha isso — e a retenção de log é de ≈ 1 dia, então a leitura é no mesmo dia.

Output: instrumentação deployada → checkpoint da submissão de prova → evidência datada → (se H1)
conserto deployado, diagnóstico removido, backlog medido → checkpoint do operador para a geração
retroativa (ou recusa).

Quatro tasks, duas delas checkpoints (o executor pausa — não consomem contexto de execução): a ordem
medir → consertar é D-13 e não se funde, e a geração retroativa exige a credencial de serviço, que nenhum
processo local pode obter — por isso é do operador, no painel, e vem depois do conserto.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-CONTEXT.md

`48-RESEARCH.md` e `48-PATTERNS.md` NÃO entram inteiros: cada task lê só as seções do seu `<read_first>`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): instrumentar sem vazar — diag-auth na devolutiva, log por código no submit, deploy dos dois</name>
  <files>supabase/functions/gerar-devolutiva-bigfive/index.ts, supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts, supabase/functions/submit-bigfive-final/index.ts, supabase/functions/submit-bigfive-final/index.test.ts</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §G INTEIRA (tabela de evidência, H1/H2/H3, o bloco de instrumentação mínima, janela de logs)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §14
    - `supabase/functions/gerar-devolutiva-bigfive/index.ts` (`:590-628` `guardDevolutivaBearer`; `:630-662` o `Deno.serve` e onde a guarda é chamada)
    - `supabase/functions/submit-bigfive-final/index.ts` (`:74-79` deps; `:236-268` o invoke best-effort e o `catch` mudo; `:286-326` o wiring e o comentário do Defeito 5)
    - `supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts`, `supabase/functions/submit-bigfive-final/index.test.ts`
    - `efdeploy.cjs` (tabela `VERIFY_JWT`: `submit-bigfive-final: true`, `gerar-devolutiva-bigfive: false` — não sobrescrever)
  </read_first>
  <action>
Conferir antes que o deployado bate com o repositório (CONTEXT §code_context): baixar `GET https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/submit-bigfive-final/body` e `…/gerar-devolutiva-bigfive/body` (token do Keychain) e confirmar marcadores do fonte (`SEM early-return em`, `timingSafeEqualStr`). Registrar no SUMMARY.

`gerar-devolutiva-bigfive/index.ts`: extrair e exportar uma função pura `classificarFormatoCredencial(v: string): 'vazio' | 'sb_secret' | 'sb_publishable' | 'jwt' | 'outro'` (a classificação do RESEARCH §G) e, no `Deno.serve`, ANTES de `guardDevolutivaBearer(req, SERVICE_KEY)`, um único `console.log('[gerar-devolutiva-bigfive] diag-auth', {...})` com: `auth_presente`, `esquema` (`ausente|Bearer|outro`), `recebido_formato`, `recebido_len`, `esperado_env: 'SUPABASE_SERVICE_ROLE_KEY'`, `esperado_formato`, `esperado_len`, `apikey_formato`. Nenhum valor, nenhum prefixo além da classe. Comentário marcando o bloco como diagnóstico TEMPORÁRIO do JORN-06, a ser removido no deploy do conserto (Task 3). A guarda em si NÃO muda. Teste: `classificarFormatoCredencial` para as 5 classes, e uma asserção de que o objeto de log montado a partir de um Bearer conhecido não contém a string do Bearer em nenhum valor (serializar e procurar).

`submit-bigfive-final/index.ts`: desestruturar também `error` do resultado do invoke; no ramo de erro e no `catch`, logar por código na linha redigida já existente — `devolutiva_erro: error?.name ?? 'desconhecido'`, `devolutiva_status: error?.context?.status ?? null` (ou `'timeout'`) — sem corpo, sem header, sem segredo. O retorno ao candidato continua `{ ok: true, devolutiva_id }`. Testes: invoke que devolve `{ error: { name: 'FunctionsHttpError', context: { status: 401 } } }` → resposta `ok:true` e log com `devolutiva_status: 401`; invoke que lança → `ok:true` e log com código.

Rodar os testes das duas EFs e deployar: `node efdeploy.cjs gerar-devolutiva-bigfive` e `node efdeploy.cjs submit-bigfive-final`. Conferir que os bundles deployados contêm `diag-auth` e `devolutiva_status`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/gerar-devolutiva-bigfive/ supabase/functions/submit-bigfive-final/</automated>
    <fails_when>non-zero exit, "FAILED" in the output, or "0 passed"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs gerar-devolutiva-bigfive && node efdeploy.cjs submit-bigfive-final && T=$(security find-generic-password -s 'Supabase CLI' -a supabase -w) && curl -s -H "Authorization: Bearer $T" https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/gerar-devolutiva-bigfive/body | grep -a -c "diag-auth" && curl -s -H "Authorization: Bearer $T" https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/submit-bigfive-final/body | grep -a -c "devolutiva_status"</automated>
    <fails_when>non-zero exit from efdeploy, or either grep prints 0 (the instrumentation is not in the deployed bundle)</fails_when>
  </verify>
  <acceptance_criteria>
    - `classificarFormatoCredencial` exportada e testada nas 5 classes; o teste prova que o log não contém o valor do Bearer.
    - `submit-bigfive-final` loga `devolutiva_erro`/`devolutiva_status` e continua devolvendo `ok:true` nos dois caminhos de falha (testes).
    - Os dois deploys concluídos; bundles com os marcadores; `verify_jwt` de cada EF igual ao da tabela do `efdeploy.cjs`.
  </acceptance_criteria>
  <done>A próxima submissão de Big Five vai deixar no log a prova do que a devolutiva recebeu — sem vazar nada.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: a submissão de prova — o operador conclui um Big Five, e o log é lido no MESMO DIA</name>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §G (previsão de H1 e a janela de logs de ≈ 1 dia)
    - `.planning/JORNADA-GUIADA.md` §«Consultas de conferência» (SÓ LEITURA — nunca escrever nesse arquivo)
  </read_first>
  <action>Concluir um Big Five em produção com uma conta de teste (alias `+claude`) cuja candidatura esteja em `avaliacao_assincrona` — é o gate de `submit-bigfive-final:178`. Se nenhuma conta de teste estiver nessa etapa, o RH avança uma candidatura de teste até «Avaliação assíncrona» antes (isso manda o e-mail `avanco` para o alias de teste — esperado).</action>
  <instructions>
Claude já deployou a instrumentação (Task 1). Falta a única coisa que exige pessoa: uma submissão real,
pelo navegador, com a sessão do candidato — é o wiring do `Deno.serve` + `functions.invoke` que está
sob prova, e nenhum teste o exercita.

1. Entrar em `https://rh.beautysmile.com.br/auth/login` com a conta de teste, responder e enviar o Big Five.
2. Avisar aqui o horário aproximado do envio.

Logo em seguida, Claude lê, pela Management API (o mesmo endpoint de logs que a pesquisa usou —
`/v1/projects/isljnozzlvckrgjjbjwp/analytics/endpoints/logs.all`, `function_logs` e
`function_edge_logs` na janela do envio): (a) a linha `diag-auth`; (b) a linha
`Rejected request: invalid/absent Bearer` da guarda; (c) o status 401 do edge log de
`gerar-devolutiva-bigfive`; (d) a linha `devolutiva_status` do `submit-bigfive-final`. Transcreve as
quatro em `48-EVIDENCIA-JORN06.md` (só os campos de formato — nenhum valor de credencial existe no log
para transcrever) e aplica o PORTÃO:

- casa com H1 (`auth_presente:false`, `apikey_formato:"sb_secret"`, `esperado_formato:"sb_secret"`) E a
  linha da guarda está presente → a Task 3 (conserto) está autorizada;
- qualquer outra combinação → PARAR: o plano termina aqui, a evidência vai ao operador, e nenhum
  conserto é escrito (D-13, D-14).
  </instructions>
  <files>.planning/phases/48-consertos-da-jornada-bloco-1/48-EVIDENCIA-JORN06.md</files>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const s=require('fs').readFileSync('.planning/phases/48-consertos-da-jornada-bloco-1/48-EVIDENCIA-JORN06.md','utf8');for(const t of ['diag-auth','auth_presente','apikey_formato','esperado_formato','Rejected request','401','devolutiva_status'])if(!s.includes(t))throw new Error('evidencia sem: '+t);if(!/\d{4}-\d{2}-\d{2}/.test(s))throw new Error('evidencia sem data');if(/sb_secret_[A-Za-z0-9]{6,}|eyJ[A-Za-z0-9_-]{10,}\./.test(s))throw new Error('a evidencia contem o que parece ser um VALOR de credencial');const h1=/auth_presente\W+false/.test(s)&&/apikey_formato\W+sb_secret/.test(s)&&/esperado_formato\W+sb_secret/.test(s);console.log(h1?'H1 CONFIRMADA — Task 3 autorizada':'H1 NAO CONFIRMADA — PARAR');if(!h1)process.exit(2)"</automated>
    <fails_when>exit 1 with "evidencia sem:" or "contem o que parece ser um VALOR"; exit 2 with "H1 NAO CONFIRMADA — PARAR" (the plan stops here and reports)</fails_when>
  </verify>
  <acceptance_criteria>
    - `48-EVIDENCIA-JORN06.md` existe, datado, com as quatro linhas de log (campos de formato) e o horário do envio.
    - A evidência não contém nenhum valor de credencial.
    - O veredito do portão (H1 confirmada ou não) está escrito no arquivo e no SUMMARY.
  </acceptance_criteria>
  <verification>Claude lê os logs da janela do envio pela Management API (`function_logs` e `function_edge_logs` de `gerar-devolutiva-bigfive` e `submit-bigfive-final`), transcreve as quatro linhas em `48-EVIDENCIA-JORN06.md` e roda o verify abaixo: exit 0 = H1 confirmada (Task 3 autorizada); exit 2 = PARAR e reportar.</verification>
  <resume-signal>Digite «enviado HH:MM» depois de concluir o Big Five (ou descreva o que impediu o envio).</resume-signal>
  <done>A causa do 401 está medida e registrada — e a decisão de consertar ou parar saiu da medição, não de palpite.</done>
</task>

<!-- planner-region-allow: diag-auth -->
<task type="auto">
  <name>Task 3: o conserto derivado da causa medida — Bearer explícito, diagnóstico removido, backlog medido</name>
  <precondition>`48-EVIDENCIA-JORN06.md` registra H1 CONFIRMADA (o verify da Task 2 saiu com exit 0). Sem isso, esta task NÃO roda — parar e reportar.</precondition>
  <files>supabase/functions/submit-bigfive-final/index.ts, supabase/functions/submit-bigfive-final/index.test.ts, supabase/functions/gerar-devolutiva-bigfive/index.ts, supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts, .planning/phases/48-consertos-da-jornada-bloco-1/48-EVIDENCIA-JORN06.md</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-EVIDENCIA-JORN06.md` (a medição que autoriza esta task)
    - `supabase/functions/notificar-candidato/index.ts` (`:87-94` — `NotificarDeps.serviceKey`, molde de chave nas deps)
    - `supabase/functions/submit-bigfive-final/index.ts` (o estado deixado pela Task 1)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §G «Consertos derivados»
  </read_first>
  <action>
`submit-bigfive-final`: acrescentar `serviceKey: string` a `SubmitBigfiveFinalDeps`; no wiring do `Deno.serve`, passar `serviceKey: SERVICE_KEY`; no invoke, acrescentar `headers: { Authorization: 'Bearer ' + serviceKey }` às opções — o `fetchWithAuth` do supabase-js respeita um `Authorization` já presente. Comentário junto do invoke explicando a causa MEDIDA (citar `48-EVIDENCIA-JORN06.md`): com chave `sb_secret_` e sem sessão, o supabase-js ≥ versão com `omitApiKeyAsBearer` não põe `Authorization`; a guarda SEC-04 compara com `SUPABASE_SERVICE_ROLE_KEY`, então o header explícito é o que o único chamador legítimo precisa mandar. Atualizar o comentário SEC-04 de `gerar-devolutiva-bigfive` («o valor que o único chamador já envia como Bearer») para dizer que o chamador passou a mandá-lo EXPLICITAMENTE desde este conserto. Teste: o mock de `functions.invoke` registra as opções; exigir `options.headers.Authorization === 'Bearer ' + serviceKey` e que nenhum log capturado contenha `serviceKey`.

`gerar-devolutiva-bigfive`: REMOVER o bloco `diag-auth` (decisão registrada no must_have) — sem deixar o marcador em comentário do arquivo (o verify reprova qualquer ocorrência; a história fica no SUMMARY e em `48-EVIDENCIA-JORN06.md`). `classificarFormatoCredencial` pode ficar exportada com seu teste (útil e sem efeito colateral) ou sair — escolher e registrar.

Rodar os testes e deployar os dois (`node efdeploy.cjs submit-bigfive-final`, `node efdeploy.cjs gerar-devolutiva-bigfive`). Conferir: bundle do submit contém `serviceKey`; bundle da devolutiva NÃO contém `diag-auth`.

Backlog — SÓ LEITURA, nenhuma geração nesta task: medir as candidaturas com linha em `scores_candidato` `tipo='big_five'` e sem linha em `devolutivas_candidato`, com o par e a classe da conta — `node p46apply.cjs sql "SET TRANSACTION READ ONLY; select s.candidatura_id, s.id as score_id, (ct.email ilike '%+claude%') as conta_teste from public.scores_candidato s join public.candidaturas c on c.id = s.candidatura_id join public.candidatos ct on ct.id = c.candidato_id where s.tipo='big_five' and not exists (select 1 from public.devolutivas_candidato d where d.candidatura_id = s.candidatura_id)"` — e o total de `devolutivas_candidato`. Esperado 2 (`bf26ee3c-0ae3-4e92-a99b-6e05efc2a662` e a da submissão de prova), todas de conta de teste. Acrescentar a `48-EVIDENCIA-JORN06.md` a seção `## Backlog` com: `BACKLOG: <N>`, `DEVOLUTIVAS_ANTES: <total>`, uma linha `PAR: <candidatura_id> <score_id>` por candidatura, e `CONTAS: todas de teste` — ou `CONTAS: HA CANDIDATO REAL` seguido dos ids que não são de teste. Com candidato real no backlog, PARAR antes da Task 4: gerar a devolutiva de candidato real fora do fluxo normal é decisão do operador (D-18), não desta task. A geração em si é da Task 4, pelo operador; esta task não busca a credencial de serviço por via nenhuma.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/submit-bigfive-final/ supabase/functions/gerar-devolutiva-bigfive/ && ! grep -n "diag-auth" supabase/functions/gerar-devolutiva-bigfive/index.ts && grep -q "serviceKey" supabase/functions/submit-bigfive-final/index.ts && echo OK</automated>
    <fails_when>non-zero exit; "FAILED" or "0 passed" in deno output; grep still finds diag-auth in the devolutiva source; or serviceKey absent from the submit source</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const s=require('fs').readFileSync('.planning/phases/48-consertos-da-jornada-bloco-1/48-EVIDENCIA-JORN06.md','utf8');const b=s.split('## Backlog')[1];if(!b)throw new Error('evidencia sem a secao Backlog');const n=+((b.match(/^BACKLOG: (\d+)/m)||[])[1]);if(!Number.isInteger(n))throw new Error('Backlog sem BACKLOG: N');if(!/^DEVOLUTIVAS_ANTES: \d+/m.test(b))throw new Error('Backlog sem DEVOLUTIVAS_ANTES');const pares=[...b.matchAll(/^PAR: [0-9a-f-]{36} [0-9a-f-]{36}/gm)].length;if(pares!==n)throw new Error('BACKLOG '+n+' mas '+pares+' linha(s) PAR');if(!/^CONTAS: todas de teste/m.test(b)){console.error('backlog com candidato real — PARAR antes da Task 4');process.exit(2)}console.log('backlog medido: '+n+' par(es), todos de conta de teste')"</automated>
    <fails_when>exit 1 with "evidencia sem a secao Backlog", "Backlog sem BACKLOG", "Backlog sem DEVOLUTIVAS_ANTES" or "linha(s) PAR"; exit 2 with "backlog com candidato real — PARAR antes da Task 4" (the plan stops and reports)</fails_when>
  </verify>
  <acceptance_criteria>
    - `SubmitBigfiveFinalDeps` tem `serviceKey`; o invoke manda `Authorization` explícito (teste prova); nenhum log contém a chave.
    - O bloco `diag-auth` não existe mais no fonte nem no bundle deployado de `gerar-devolutiva-bigfive`.
    - `48-EVIDENCIA-JORN06.md` §Backlog registra N, o total ANTES, um `PAR:` por candidatura e a classe das contas, MEDIDA.
    - A prova em PROD do caminho completo (submissão NOVA pelo navegador → devolutiva criada pelo wiring real) fica para o plano 48-18.
  </acceptance_criteria>
  <done>A devolutiva volta a ser gerada pelo único chamador legítimo, pela causa medida — e quem ficou sem ela está contado, com os pares prontos para o operador.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: geração retroativa do backlog de contas de teste — o operador invoca pelo painel, ou recusa</name>
  <precondition>`48-EVIDENCIA-JORN06.md` §Backlog registra `CONTAS: todas de teste` (o segundo verify da Task 3 saiu com exit 0). Com candidato real no backlog, esta task NÃO é apresentada — parar e reportar.</precondition>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-EVIDENCIA-JORN06.md` §Backlog (os pares medidos na Task 3)
  </read_first>
  <action>O operador invoca `gerar-devolutiva-bigfive` pelo painel da Supabase, uma vez por linha `PAR:` de §Backlog, com o corpo `{"candidatura_id": "<1º id>", "score_id": "<2º id>"}` — a plataforma fornece a credencial do lado do servidor, e nenhum processo local a obtém. O operador também pode RECUSAR: o backlog é só de contas de teste, e recusar não afeta a prova do JORN-06.</action>
  <instructions>
Claude já deployou o conserto (Task 3) e mediu o backlog: N candidatura(s), todas de conta de teste
(`+claude`), com os pares `{candidatura_id, score_id}` em `48-EVIDENCIA-JORN06.md` §Backlog.

Por que isto é seu, e não do Claude: gerar essas devolutivas exige a credencial de serviço, que neste
projeto só existe dentro das Edge Functions e do painel. Nenhuma task a busca por outra via — nem pela
Management API.

Por que você pode recusar: são contas de teste. A prova de que o conserto funciona NÃO é esta geração —
é a submissão NOVA de Big Five pelo navegador no plano 48-18 (sessão 1, passo f), que passa pelo
`Deno.serve` + `functions.invoke` reais. Esta geração acontece antes do T0 do 48-18, então não contamina
aquela prova.

Se for gerar:
1. Painel da Supabase → Edge Functions → `gerar-devolutiva-bigfive` → a ferramenta de teste/invocação da função, com o papel de serviço.
2. Para cada linha `PAR:` de §Backlog, enviar `POST` com `{"candidatura_id": "<1º id>", "score_id": "<2º id>"}`.
3. Avisar aqui o horário e o status que o painel mostrou.

Logo em seguida, Claude lê o `function_edge_logs` de `gerar-devolutiva-bigfive` na janela (o mesmo
endpoint de logs da Task 2), mede `devolutivas_candidato` e acrescenta a `48-EVIDENCIA-JORN06.md` a seção
`## Retroativo` com UMA linha de veredito:
- gerou → `RETROATIVO: GERADAS <n>`, uma linha `edge_status: 200` por chamada, e `DEVOLUTIVAS_DEPOIS: <total>` (= `DEVOLUTIVAS_ANTES` + N; se uma submissão real entrou no intervalo, registrar);
- recusou → `RETROATIVO: RECUSADO` (o backlog fica registrado; nada mais a fazer);
- o painel devolveu 401 ou outro erro → `RETROATIVO: FALHOU <status>` e PARAR: a guarda SEC-04 compara com a env da própria EF, e o painel pode mandar uma credencial de outro formato. Registrar e não procurar a credencial por outra via.
  </instructions>
  <files>.planning/phases/48-consertos-da-jornada-bloco-1/48-EVIDENCIA-JORN06.md</files>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs'),cp=require('child_process');const s=fs.readFileSync('.planning/phases/48-consertos-da-jornada-bloco-1/48-EVIDENCIA-JORN06.md','utf8');const v=(s.match(/^RETROATIVO: (GERADAS|RECUSADO|FALHOU)/m)||[])[1];if(!v)throw new Error('evidencia sem veredito RETROATIVO');if(v!=='GERADAS'){console.log('RETROATIVO '+v+' — backlog de conta de teste registrado e nao gerado; a prova do JORN-06 segue no 48-18');process.exit(0)}if(!/edge_status: 200/.test(s))throw new Error('GERADAS sem edge_status 200');const ids=[...(s.split('## Backlog')[1]||'').matchAll(/^PAR: ([0-9a-f-]{36}) /gm)].map(m=>m[1]);if(!ids.length)throw new Error('GERADAS sem nenhum PAR');const q='SET TRANSACTION READ ONLY; select count(*)::int as n from public.devolutivas_candidato where candidatura_id in ('+ids.map(i=>'\x27'+i+'\x27').join(',')+')';const r=JSON.parse(cp.execFileSync('node',['p46apply.cjs','sql',q]).toString());if(r[0].n!==ids.length)throw new Error('devolutivas dos pares: '+r[0].n+' (esperado '+ids.length+')');console.log('RETROATIVO GERADAS: uma devolutiva por par ('+ids.length+')')"</automated>
    <fails_when>exit 1 with "evidencia sem veredito RETROATIVO", "GERADAS sem edge_status 200", "GERADAS sem nenhum PAR" or "devolutivas dos pares: n (esperado N)"; or an SQL error from p46apply. Exit 0 with "RETROATIVO RECUSADO"/"RETROATIVO FALHOU" is a recorded outcome, not a failure</fails_when>
  </verify>
  <acceptance_criteria>
    - `48-EVIDENCIA-JORN06.md` §Retroativo tem exatamente um veredito (`GERADAS`, `RECUSADO` ou `FALHOU`); se `GERADAS`, `edge_status: 200` por chamada e cada par com exatamente uma devolutiva (verify).
    - Nenhum comando registrado no SUMMARY chama endpoint que devolva segredo; a credencial de serviço não passou por processo local.
  </acceptance_criteria>
  <verification>Claude lê o edge log da janela informada pelo operador, escreve §Retroativo em `48-EVIDENCIA-JORN06.md` e roda o verify: `GERADAS` exige uma devolutiva por par e o 200 registrado; `RECUSADO`/`FALHOU` saem com exit 0 e ficam no SUMMARY.</verification>
  <resume-signal>Digite «geradas HH:MM» com o status visto no painel, «recuso», ou descreva o erro.</resume-signal>
  <done>O backlog de contas de teste está gerado pelo operador — ou recusado com registro — sem que a credencial de serviço tenha saído do servidor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `submit-bigfive-final` → `gerar-devolutiva-bigfive` (servidor→servidor, `verify_jwt=false`) | a guarda SEC-04 é a única autenticação da chamada |
| logs da plataforma | legíveis por quem tem acesso ao painel; nunca podem conter segredo |
| operador → painel da Supabase → `gerar-devolutiva-bigfive` | geração retroativa; a credencial de serviço é fornecida pela plataforma do lado do servidor e não atravessa nenhum processo local |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-05-01 | Information Disclosure | log de diagnóstico da credencial | high | mitigate | só classe de formato e comprimento; teste prova que o valor não aparece; bloco removido no conserto |
| T-48-05-02 | Elevation of Privilege | enfraquecer a guarda SEC-04 para «resolver» o 401 | high | mitigate | a guarda não muda; o conserto é o chamador mandar o Bearer certo; `verify_jwt` não muda |
| T-48-05-03 | Information Disclosure | credencial de serviço fora da EF na geração retroativa | high | mitigate | eliminada por desenho: nenhuma task chama endpoint da Management API que devolva segredo; a geração é do operador no painel (Task 4, checkpoint), com a credencial fornecida do lado do servidor; 401 do painel é registrado e o plano para, sem rota alternativa |
| T-48-05-06 | Tampering | geração retroativa atingindo candidato real fora do fluxo | medium | mitigate | classe da conta MEDIDA na Task 3 (`ilike '%+claude%'`); exit 2 e parada antes da Task 4 se houver candidato real (D-18); a EF faz upsert idempotente por `candidatura_id` |
| T-48-05-04 | Repudiation | falha da devolutiva silenciosa | medium | mitigate | `catch` mudo vira log por código; contrato com o candidato inalterado |
| T-48-05-05 | Tampering | conserto por palpite | medium | mitigate | portão da Task 2 com exit 2 quando H1 não casa; Task 3 com precondição |
| T-48-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Instrumentação deployada e testada (Task 1).
- Evidência datada com o veredito do portão (Task 2).
- Se H1: conserto deployado, diagnóstico removido, backlog medido com a classe das contas (Task 3). Se não H1: plano parado com a evidência entregue ao operador.
- Geração retroativa pelo operador no painel, ou recusa registrada (Task 4) — em nenhum caso a credencial de serviço passou por processo local.
</verification>

<success_criteria>
A devolutiva volta a sair, e existe no repositório a medição que prova POR QUE ela parou — escrita antes do conserto. O backlog de contas de teste está gerado pelo operador ou recusado com registro; a prova do conserto é a submissão nova do 48-18.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `classificarFormatoCredencial` (gerar-devolutiva-bigfive) | função pura exportada | **nova** (fica ou sai na Task 3 — registrado) |
| bloco `diag-auth` | log temporário | criado na Task 1, **removido** na Task 3 |
| `SubmitBigfiveFinalDeps.serviceKey` | campo de deps | **novo** |
| log `devolutiva_erro` / `devolutiva_status` | observabilidade | **novo** |
| `.planning/phases/48-consertos-da-jornada-bloco-1/48-EVIDENCIA-JORN06.md` | evidência datada (medição, §Backlog, §Retroativo) | **nova** |

## Premissas sinalizadas (probe de borda)

JORN-06 foi classificado `unclassified` pelo probe. A borda `explicit` (o candidato recebe `ok:true`
mesmo com o 401 interno) vem do contrato best-effort do RESEARCH §G. **Premissa A1** (o header chegou
AUSENTE, não com valor diferente) é exatamente o que a Task 2 mede — não é assumida.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-05-SUMMARY.md` when done
</output>
