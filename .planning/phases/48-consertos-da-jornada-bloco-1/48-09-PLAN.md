---
phase: 48-consertos-da-jornada-bloco-1
plan: 09
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - supabase/migrations/20260921000008_p48_explicacao_rejeicao_origem.sql
  - supabase/migrations/20260921000009_p48_rejeicao_triagem_feedback.sql
  - supabase/tests/p48_rejeicao_triagem_smoke.sql
  - database.types.ts
  - src/features/explicacao/services/explicacaoService.ts
  - src/features/explicacao/services/__tests__/explicacaoService.test.ts
  - src/features/explicacao/components/ExplicacaoCandidatoPage.tsx
  - src/features/explicacao/components/__tests__/ExplicacaoCandidatoPage.test.tsx
  - src/features/vagas/services/candidaturasService.ts
  - src/features/vagas/services/__tests__/candidaturasService.test.ts
autonomous: true
requirements: [JORN-22]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`rejeitar_candidatura` passa a gravar `feedback_rejeicao` com texto NEUTRO — «Após análise da sua candidatura pela nossa equipe, não seguiremos com ela neste momento.» — no MESMO UPDATE único que já move a etapa (o que o knockout já faz em `20260709000014:143`). O texto NÃO é a `etapa_justificativa` do RH e não revela motivo (D-12)."
    - "A condição do cartão no painel (`DashboardCandidatoPage.tsx:148-159`, `data_decisao_final OR feedback_rejeicao`) NÃO muda (D-12); o cartão aparece porque o dado passou a existir."
    - "Existe a RPC `public.explicacao_rejeicao_origem(p_candidatura_id uuid) RETURNS text` (STABLE, SECURITY DEFINER, `search_path=''`), que devolve `'automatica'` (knockout), `'humana_triagem'` (rejeição humana sem linha em `decisao_final`) ou NULL — lendo `motivo_rejeicao` sem devolvê-lo; NULL cobre «não é sua» E «não se aplica» (anti-oráculo). A função booleana `explicacao_rejeicao_automatica` fica (sem DROP)."
    - "A página de explicação SERVE a rejeição humana na triagem: razão neutra própria («analisada por uma pessoa da nossa equipe», sem motivo nem critério) e o canal de contato, SEM oferecer pedido de revisão — espelhando o knockout (D-20). Ela não reusa `resultLineAutomatica` nem `semRevisaoBody`, que afirmam «sem avaliação de uma pessoa»."
    - "O canal citado na página vem de `CANAL_PRIVACIDADE_EMAIL` importado (a mesma fonte de hoje) — nenhuma quinta ocorrência literal do endereço do canal (D-07)."
    - "O front é publicado ANTES de `rejeitar_candidatura` começar a gravar o `feedback_rejeicao` — o cartão nunca aparece levando a «Esta página não está disponível»."
    - "O ramo morto de `candidaturasService.ts:458` que escrevia `feedback_rejeicao: motivo_rejeicao` (texto livre) é removido — reativado, violaria D-12."
    - "BORDA (explicit): candidatura com linha em `decisao_final` (mesmo rejeitada) → a RPC devolve NULL para `humana_triagem` e a página segue o caminho humano atual; candidatura em andamento → NULL; candidatura de outro titular → NULL; sem JWT → NULL."
    - "Linguagem de produto preservada: nenhum texto novo contém «teste psicológico», nem as palavras que o grep-guard dos e-mails veta (`score|percentil|trait|motivo|nota|ranking|pontuaç|crit[ée]rio`)."
    - "Migrations aplicadas por `p46apply.cjs` a partir do corpo VIVO (o de `rejeitar_candidatura` foi instalado pelo 48-01) com pré-portão de md5 (D-16); `database.types.ts` regenerado pelo comando com `< /dev/null`; `tsc` ≤ 90 (D-15); `origin/main..HEAD` vazio."
  artifacts:
    - path: supabase/migrations/20260921000008_p48_explicacao_rejeicao_origem.sql
      provides: "RPC tri-estado"
      contains: "explicacao_rejeicao_origem"
    - path: supabase/migrations/20260921000009_p48_rejeicao_triagem_feedback.sql
      provides: "feedback_rejeicao neutro no UPDATE único"
      contains: "feedback_rejeicao"
    - path: src/features/explicacao/components/ExplicacaoCandidatoPage.tsx
      provides: "ramo humana_triagem (data-testid explicacao-humana-triagem)"
      contains: "humana_triagem"
  key_links:
    - from: "src/features/explicacao/services/explicacaoService.ts"
      to: "public.explicacao_rejeicao_origem"
      via: "supabase.rpc quando não há linha em decisao_final"
      pattern: "explicacao_rejeicao_origem"
    - from: "public.rejeitar_candidatura"
      to: "src/components/pages/DashboardCandidatoPage.tsx (hasDecisaoFinal)"
      via: "candidaturas.feedback_rejeicao passa a existir na rejeição humana"
      pattern: "feedback_rejeicao"
  prohibitions:
    - requirement_id: JORN-22
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT revelar ao candidato o motivo, o critério, a justificativa escrita pelo RH ou qualquer nota — nem no `feedback_rejeicao`, nem na página"
    - requirement_id: JORN-22
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT oferecer pedido de revisão para a rejeição na triagem (D-20) — o servidor não o aceita, e um botão que sempre falha é pior que o canal honesto"
    - requirement_id: JORN-22
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT dizer ao rejeitado pela equipe que a decisão foi automática ou sem avaliação de uma pessoa"
---

<objective>
Tornar o Art. 20 alcançável para quem é rejeitado na triagem: o cartão «Entenda a decisão» aparece e
leva a uma explicação que EXISTE.

Purpose: D-12 — `rejeitar_candidatura` grava `feedback_rejeicao` neutro, como o knockout, e a condição
do front não muda. Mas a pesquisa provou que isso é necessário e NÃO suficiente: com o cartão visível,
a página responderia «Esta página não está disponível», porque a RPC de fallback serve só o knockout.
D-20 manda a página servir o caso com razão neutra própria e canal, sem revisão.

Output: RPC tri-estado aplicada, tipos regenerados, serviço e página com o ramo `humana_triagem`,
publicado — e SÓ ENTÃO a gravação do `feedback_rejeicao` na RPC de rejeição, com smoke.

Orçamento: a estimativa (110k, confiança baixa — sem amostra calibrada) passa do smart-zone e é aceita
em vez de fatiar: as duas tasks são UM entregável em ordem obrigatória, não dois — a Task 2 sozinha
causaria o defeito que a fase conserta (cartão levando a «Esta página não está disponível»), seu verify
exige o marcador da Task 1 no ar antes do apply, e o smoke dela exercita a RPC da Task 1. Fatiar trocaria
um verify local por uma dependência entre planos com deploy no meio.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-CONTEXT.md

`48-RESEARCH.md` e `48-PATTERNS.md` NÃO entram inteiros: cada task lê só as seções do seu `<read_first>`.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer) [BLOCKING]: a explicação da rejeição na triagem de ponta a ponta — RPC tri-estado → serviço → página</name>
  <files>supabase/migrations/20260921000008_p48_explicacao_rejeicao_origem.sql, database.types.ts, src/features/explicacao/services/explicacaoService.ts, src/features/explicacao/services/__tests__/explicacaoService.test.ts, src/features/explicacao/components/ExplicacaoCandidatoPage.tsx, src/features/explicacao/components/__tests__/ExplicacaoCandidatoPage.test.tsx</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §B INTEIRA (Achado Contraditório #2)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §1 (cabeçalho, transporte e ACL de toda migration), §3 e §17
    - `supabase/migrations/20260906000007_explicacao_knockout.sql` INTEIRO (a RPC booleana: guard own-row, anti-oráculo, COMMENT `:84-94`, ACL)
    - `src/features/explicacao/services/explicacaoService.ts` (`:140-150` `origem`; `:220-232` `REASON_KNOCKOUT`; `:245-344` `getExplicacao` e `getExplicacaoAutomatica`)
    - `src/features/explicacao/components/ExplicacaoCandidatoPage.tsx` (`:36-100` COPY e COPY_REVISAO; `:181-262` ramos por origem; `:35` import de `CANAL_PRIVACIDADE_EMAIL`)
    - `src/features/explicacao/services/__tests__/explicacaoService.test.ts`, `src/features/explicacao/components/__tests__/ExplicacaoCandidatoPage.test.tsx`
    - `supabase/functions/_shared/__tests__/email-templates.test.ts` (`:72-79` — o grep-guard de palavras)
  </read_first>
  <behavior>
    - serviço: sem linha em `decisao_final` e RPC devolvendo `'humana_triagem'` → `{ origem: 'humana_triagem', decisao: 'rejeitado', reason: REASON_HUMANA_TRIAGEM, revisao_*: null }`
    - serviço: RPC `'automatica'` → o objeto automático atual (inalterado)
    - serviço: RPC `null`, erro, ou qualquer outro valor (comparação ESTRITA) → `null`
    - página `humana_triagem`: renderiza `data-testid="explicacao-humana-triagem"`, a linha de resultado própria, o bloco sem-revisão próprio com o canal importado; NÃO renderiza o CTA de revisão; NÃO contém «sem avaliação de uma pessoa»
    - `REASON_HUMANA_TRIAGEM` e as linhas novas não casam `/score|percentil|trait|motivo|nota|ranking|pontuaç|crit[ée]rio|teste psicol/i`
  </behavior>
  <action>
Migration `20260921000008_p48_explicacao_rejeicao_origem.sql` (cabeçalho do M8, transporte `p46apply.cjs migrate`, sem `BEGIN;`): FUNÇÃO NOVA `public.explicacao_rejeicao_origem(p_candidatura_id uuid) RETURNS text LANGUAGE plpgsql STABLE SECURITY DEFINER SET search_path = ''` copiando a forma de `20260906000007` (own-row por `candidatos.user_id = auth.uid()`), com um CASE: knockout (`status='rejeitado' AND motivo_rejeicao='knockout_automatico' AND opcao_knockout_id IS NOT NULL`) → `'automatica'`; `status='rejeitado'` sem knockout e SEM linha em `decisao_final` → `'humana_triagem'`; senão NULL. Nunca devolve `motivo_rejeicao`. `REVOKE ALL … FROM PUBLIC, anon` e `GRANT EXECUTE … TO authenticated` (anon NOMEADO — diferente da booleana, que ficou com anon por default; registrar). `COMMENT ON FUNCTION` com as invariantes (discriminador só; anti-oráculo; por que função nova e não redefinição — trocar o tipo de retorno exigiria DROP e quebraria o front tipado até o regen). `DO` final: a função existe, `prosecdef`, `provolatile='s'`, e `has_function_privilege('anon', …)` false. Rodar a varredura de portões (D-17; conferir listas `proname IN`). Aplicar: `node p46apply.cjs migrate supabase/migrations/20260921000008_p48_explicacao_rejeicao_origem.sql`.

Regenerar tipos: `SUPABASE_ACCESS_TOKEN=$(security find-generic-password -s "Supabase CLI" -a supabase -w) npx supabase gen types typescript --project-id isljnozzlvckrgjjbjwp < /dev/null > database.types.ts` (o `< /dev/null` é load-bearing; se o arquivo sair com 0 octeto, `git checkout -- database.types.ts` e repetir).

`explicacaoService.ts`: `origem` passa a `'humana' | 'automatica' | 'humana_triagem'` (docblock atualizado); constante `REASON_HUMANA_TRIAGEM` no molde de `REASON_KNOCKOUT` — «A sua candidatura foi analisada por uma pessoa da nossa equipe logo no início do processo seletivo, e ela decidiu não seguir com a candidatura neste momento. Esta decisão vale para esta vaga nesta seleção e não impede que você se candidate a outras.»; o fallback (sem linha em `decisao_final`) passa a chamar `explicacao_rejeicao_origem` e ramificar por comparação ESTRITA (`=== 'automatica'` → objeto atual; `=== 'humana_triagem'` → objeto novo; senão `null`). A chamada à RPC booleana antiga sai do serviço.

`ExplicacaoCandidatoPage.tsx`: com três origens, a linha de resultado e o bloco sem-revisão ramificam por `origem`. Para `'humana_triagem'`: `resultLineHumanaTriagem` = «Após a análise da sua candidatura por uma pessoa da nossa equipe, decidimos não seguir com ela nesta vaga.»; bloco sem revisão com o eyebrow existente e `semRevisaoBodyHumanaTriagem` = «Esta decisão foi tomada por uma pessoa da nossa equipe. Se quiser falar sobre ela, entre em contato pelo canal abaixo.» seguido do canal por `CANAL_PRIVACIDADE_EMAIL` importado; sem CTA de revisão; contêiner com `data-testid="explicacao-humana-triagem"`. Os ramos `humana` e `automatica` não mudam.

Testes (serviço e página) com os casos de `<behavior>`, mockando a RPC nova.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260921000008_p48_explicacao_rejeicao_origem.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');if(/^\s*DROP\s+FUNCTION/im.test(c))throw new Error('sem DROP: funcao nova');for(const t of ['public.explicacao_rejeicao_origem','RETURNS text','SECURITY DEFINER','STABLE',\"SET search_path = ''\",'auth.uid()',\"'humana_triagem'\",\"'automatica'\",'decisao_final','REVOKE ALL','anon','GRANT EXECUTE','COMMENT ON FUNCTION'])if(!c.includes(t))throw new Error('falta: '+t);console.log('OK')" && node p46apply.cjs migrate supabase/migrations/20260921000008_p48_explicacao_rejeicao_origem.sql && test -s database.types.ts && grep -q "explicacao_rejeicao_origem" database.types.ts</automated>
    <fails_when>non-zero exit; "falta:" or "sem DROP" in stderr; "md5 do ledger BATE" absent; database.types.ts empty or without the new function</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/features/explicacao</automated>
    <fails_when>non-zero exit, or "failed" in the summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }</automated>
    <fails_when>non-zero exit — lint did not run (exit code other than 0 or 2), exit code and error count disagree, or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - A RPC existe em PROD com `prosecdef=true`, `provolatile='s'`, sem EXECUTE para `anon`; a booleana antiga continua existindo.
    - `database.types.ts` não vazio e com `explicacao_rejeicao_origem`.
    - Testes do serviço e da página verdes com todos os casos de `<behavior>`, incluindo o grep-guard de palavras.
    - `grep -c "lgpd@" src/features/explicacao/components/ExplicacaoCandidatoPage.tsx` = 0 (o canal vem da constante importada).
    - `tsc errors` ≤ 90.
  </acceptance_criteria>
  <done>Existe uma explicação honesta para quem foi rejeitado por uma pessoa na triagem — pronta antes de o cartão passar a levar até ela.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: publicar a página, e SÓ ENTÃO gravar o `feedback_rejeicao` neutro — com smoke</name>
  <files>src/features/vagas/services/candidaturasService.ts, src/features/vagas/services/__tests__/candidaturasService.test.ts, supabase/migrations/20260921000009_p48_rejeicao_triagem_feedback.sql, supabase/tests/p48_rejeicao_triagem_smoke.sql</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §6 (redefinição de função viva com pré-portão de md5) e Shared Pattern §H (deploy e prova de publicação)
    - `src/features/vagas/services/candidaturasService.ts` (`:440-470` o ramo morto que escreve `feedback_rejeicao: motivo_rejeicao`; `:500-510`)
    - `src/components/modals/UpdateStatusModal.tsx` (confirmar que ele nunca passa `motivo_rejeicao` ao caminho de status — o ramo é morto)
    - `supabase/migrations/20260714100001_rejeitar_candidatura_rpc.sql` (`:140-151` — o UPDATE único e por que tem de ser um só)
    - `supabase/migrations/20260921000002_p48_encerrada_demais_instancias.sql` (o corpo de `rejeitar_candidatura` instalado pelo 48-01)
    - `supabase/migrations/20260709000014_submit_candidatura_flag.sql` (`:138-150` — o texto neutro do knockout)
    - `supabase/tests/oper31_rejeitar_candidatura_smokes.sql`
  </read_first>
  <action>
(1) `candidaturasService.ts`: remover o ramo morto que grava `feedback_rejeicao: motivo_rejeicao` (texto livre) e o parâmetro correspondente, se só ele o usa; comentário de uma linha dizendo que `feedback_rejeicao` só é escrito no servidor com texto neutro (knockout e `rejeitar_candidatura`, D-12). Ajustar/limpar o teste do serviço que exercitava o ramo. Rodar `npm run test:run`.

(2) Publicar a página ANTES da gravação: `npm run build`, conferir o marcador no chunk (`grep -rl "explicacao-humana-triagem" build/assets/`), commitar, `git push origin main` (deploy corretivo — D-18), aguardar a Vercel e provar o marcador em PROD (crawler do verify).

(3) Migration `20260921000009_p48_rejeicao_triagem_feedback.sql`: ler o vivo de `rejeitar_candidatura` (`pg_get_functiondef` + `md5(prosrc)` — o corpo é o do 48-01), pré-portão de md5, `CREATE OR REPLACE` com UMA mudança — o UPDATE único ganha `feedback_rejeicao = 'Após análise da sua candidatura pela nossa equipe, não seguiremos com ela neste momento.'`. Nada de segundo UPDATE (o único UPDATE é o que dispara `avancar_etapa` e satisfaz `guard_rejeicao_auditada`). Cabeçalho: D-12, o experimento da Etapa 10, por que o texto é neutro e diferente do do knockout (o do knockout fala de «requisitos da vaga»; este é decisão de pessoa). `DO` final: a definição contém `feedback_rejeicao` e ainda contém `candidatura_encerrada(` (a trava do 48-01 não pode ter sumido). Aplicar: `node p46apply.cjs migrate supabase/migrations/20260921000009_p48_rejeicao_triagem_feedback.sql`.

(4) Smoke `supabase/tests/p48_rejeicao_triagem_smoke.sql` (contador, gate, SELECT final). Numa subtransação que reverte (o despacho só sai pós-COMMIT), sobre uma candidatura real de conta de teste em andamento, com claim de administrador e justificativa de 50+ caracteres: (a) `rejeitar_candidatura` grava exatamente o texto neutro em `feedback_rejeicao`, diferente de `etapa_justificativa`, e deixa `data_decisao_final` nulo; (b) o texto não casa o grep-guard de palavras; (c) `explicacao_rejeicao_origem` com o JWT do titular (sub = `candidatos.user_id`, `app_metadata.role='candidato'`) → `'humana_triagem'`; com o JWT de outro candidato → NULL; sem claims → NULL; (d) numa candidatura de knockout real, com o JWT do titular → `'automatica'`; (e) numa candidatura com linha em `decisao_final` rejeitada, com o JWT do titular → NULL; (f) NEGATIVA — contagens de `candidaturas`, `historico_candidatura` e `net.http_request_queue` iguais antes e depois. Rodar esse smoke e o `oper31_rejeitar_candidatura_smokes.sql`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && ! grep -n "feedback_rejeicao: motivo_rejeicao" src/features/vagas/services/candidaturasService.ts && npm run test:run >/dev/null && npm run build >/dev/null && grep -rl "explicacao-humana-triagem" build/assets/</automated>
    <fails_when>non-zero exit; the dead branch still exists; "failed" in vitest; or grep prints no file</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && node -e "const M=process.argv[1],B='https://rh.beautysmile.com.br';(async()=>{const vis=new Set(),fila=['/'];let ok=false;while(fila.length&&vis.size<300){const p=fila.shift();if(vis.has(p))continue;vis.add(p);let t='';try{t=await (await fetch(B+p)).text()}catch{continue}if(t.includes(M)){ok=true;break}for(const m of t.matchAll(/(?:\/assets\/|\.\/)([\w.-]+\.js)/g))fila.push('/assets/'+m[1])}if(!ok){console.error('AUSENTE em PROD: '+M);process.exit(1)}console.log('PRESENTE em PROD: '+M)})()" "explicacao-humana-triagem"</automated>
    <fails_when>non-zero exit; git log origin/main..HEAD printed a commit; or "AUSENTE em PROD" (the migration below must NOT be applied until this passes)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260921000009_p48_rejeicao_triagem_feedback.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');for(const t of ['feedback_rejeicao','Após análise da sua candidatura pela nossa equipe, não seguiremos com ela neste momento.','candidatura_encerrada(','md5'])if(!c.includes(t))throw new Error('falta: '+t);if(/feedback_rejeicao\s*=\s*(v_just|p_justificativa|p_motivo)/i.test(c))throw new Error('texto nao neutro: feedback_rejeicao recebendo justificativa/motivo');console.log('OK')" && node p46apply.cjs migrate supabase/migrations/20260921000009_p48_rejeicao_triagem_feedback.sql && node p46apply.cjs run supabase/tests/p48_rejeicao_triagem_smoke.sql && node p46apply.cjs run supabase/tests/oper31_rejeitar_candidatura_smokes.sql</automated>
    <fails_when>non-zero exit; "falta:" or "texto nao neutro" in stderr; "md5 do ledger BATE" absent; or "FAIL" in a smoke output</fails_when>
  </verify>
  <acceptance_criteria>
    - O ramo morto não existe mais; `npm run test:run` verde.
    - O marcador `explicacao-humana-triagem` estava em PROD ANTES do apply da migration 000009 (horários no SUMMARY).
    - Em PROD, `rejeitar_candidatura` contém o texto neutro e a trava `candidatura_encerrada(`.
    - `p48_rejeicao_triagem_smoke` (a)–(f) e `oper31_rejeitar_candidatura_smokes` verdes.
    - A prova com conta real (rejeitar na triagem → `feedback_rejeicao` preenchido, cartão, página renderizada) e o backfill da rejeição já feita (1, decisão do operador) são dos planos 48-18 e 48-12.
  </acceptance_criteria>
  <done>Quem é rejeitado na triagem passa a ter o cartão, e o cartão leva a uma explicação que existe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidato (JWT) → `explicacao_rejeicao_origem` | RPC SECURITY DEFINER lê coluna que o cliente não pode ler |
| RH (JWT) → `rejeitar_candidatura` | escrita privilegiada; o texto gravado chega ao candidato |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-09-01 | Information Disclosure | oráculo de existência/motivo pela RPC | high | mitigate | own-row por `auth.uid()`; devolve só o discriminador; NULL para «não é sua» e «não se aplica»; smoke (c)–(e) |
| T-48-09-02 | Information Disclosure | motivo/justificativa do RH chegando ao candidato | high | mitigate | texto neutro constante no UPDATE; grep-guard no smoke e nos testes; ramo morto que escrevia texto livre removido |
| T-48-09-03 | Elevation of Privilege | RPC nova chamável por `anon` | low | mitigate | `REVOKE … FROM PUBLIC, anon` nomeado; `DO` assere |
| T-48-09-04 | Repudiation | cartão levando a página indisponível | medium | mitigate | página publicada e provada em PROD antes do apply da gravação |
| T-48-09-05 | Tampering | trava D3 perdida ao redefinir `rejeitar_candidatura` | high | mitigate | parte do corpo vivo com md5; `DO` exige `candidatura_encerrada(` na definição instalada |
| T-48-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Duas migrations aplicadas (md5 BATE); tipos regenerados.
- Vitest da explicação e suíte inteira verdes; `tsc` ≤ 90.
- Marcador em PROD antes da gravação; smokes verdes.
</verification>

<success_criteria>
A rejeição humana na triagem deixa de tirar do candidato o direito de entender a decisão — e a explicação diz a verdade sobre quem decidiu.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `public.explicacao_rejeicao_origem(uuid) → text` | RPC STABLE SECURITY DEFINER | **nova** |
| `public.rejeitar_candidatura(…)` (grava `feedback_rejeicao` neutro) | RPC | editada |
| `supabase/migrations/20260921000008_p48_explicacao_rejeicao_origem.sql`, `…000009_p48_rejeicao_triagem_feedback.sql` | migrations | **novas** |
| `supabase/tests/p48_rejeicao_triagem_smoke.sql` | smoke | **novo** |
| `Explicacao.origem = 'humana_triagem'`, `REASON_HUMANA_TRIAGEM` | serviço | editado / **nova constante** |
| `resultLineHumanaTriagem`, `semRevisaoBodyHumanaTriagem`, `data-testid="explicacao-humana-triagem"` | página | **novos** |
| ramo morto `feedback_rejeicao: motivo_rejeicao` (`candidaturasService.ts`) | serviço | **removido** |
| `database.types.ts` | tipos gerados | regenerado |

## Premissas sinalizadas (probe de borda)

JORN-22 foi classificado `unclassified` pelo probe; as bordas `explicit` (anti-oráculo; decisão final
não é `humana_triagem`) vêm do RESEARCH §B e da invariante da `20260906000007`. **Premissa A8** (o texto
neutro) é discrição do Claude, registrada aqui e revisável pelo operador no 48-18.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-09-SUMMARY.md` when done
</output>
