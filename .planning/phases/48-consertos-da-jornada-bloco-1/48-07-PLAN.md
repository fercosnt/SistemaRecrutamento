---
phase: 48-consertos-da-jornada-bloco-1
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260921000005_p48_aviso_titular.sql
  - supabase/functions/_shared/email-config.ts
  - supabase/functions/_shared/__tests__/email-config.test.ts
  - supabase/functions/executar-direito-titular/helpers.ts
  - supabase/functions/executar-direito-titular/index.ts
  - supabase/functions/executar-direito-titular/index.test.ts
autonomous: true
requirements: [JORN-27, JORN-U2]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Quando o titular PEDE a exclusão, a EF `executar-direito-titular` manda-lhe um e-mail com a data de execução e como cancelar, e grava `solicitacoes_dados.aviso_pedido_enviado_em` (JORN-27)."
    - "Quando o titular CANCELA o pedido, a EF manda-lhe um e-mail de cancelamento e grava `solicitacoes_dados.aviso_cancelamento_enviado_em` (JORN-27)."
    - "O registro de envio é PRÓPRIO: `recibo_enviado_em` NUNCA é escrito por `pedir` nem por `cancelar` — ele é o cinto do recibo pós-exclusão (`index.ts:1075-1103`) e, escrito no pedido, suprimiria o recibo final (correção de fato da pesquisa)."
    - "O aviso ao titular NÃO passa por `notificacoes_enviadas` (o ledger exige `candidatura_id`; 16 de 42 titulares não têm candidatura) — segue a mecânica do recibo: `resolverModo`, `resolverDestinatarioComLabel`, `exigirSinkTeste`, `ler_resend_api_key`, `Idempotency-Key` própria nunca logada."
    - "Falha de envio NÃO desfaz o pedido nem muda a resposta ao titular: grava a causa por código (`logErro(acao, 'aviso_<causa>', …)`) e deixa a coluna nula — observável por `aviso_pedido_enviado_em IS NULL` em pedido agendado. O aviso é controle de conta invadida: silêncio é o pior modo de falha."
    - "Idempotência por estado: se a coluna já está preenchida, o aviso não é reenviado."
    - "O conteúdo NÃO traz `solicitacao_id` (Invariante 12), NÃO cita o endereço do canal de privacidade por literal (D-07 — o rodapé já diz «responda a este e-mail» e o `REPLY_TO` é o RH), e traz o link para o login do candidato com retorno à área de privacidade: `https://rh.beautysmile.com.br/auth/login?redirect=%2Fcandidato%2Fprivacidade` (JORN-U2)."
    - "`_shared/email-config.ts` ganha `APP_BASE_URL_PADRAO`, `normalizarBaseApp` e `montarUrlLogin` sem quebrar o contrato ZERO IMPORTS; env malformada cai no default; `redirect` só é aceito se for caminho interno (começa com `/`, não com `//`)."
    - "BORDA (explicit): titular SEM candidatura pede exclusão → recebe o aviso (o aviso não depende do ledger); um segundo `pedir` com o pedido já agendado não gera segundo aviso."
    - "A migration foi aplicada por `p46apply.cjs` (md5 conferido); a EF deployada por `node efdeploy.cjs executar-direito-titular` (verify_jwt=true pela tabela)."
  artifacts:
    - path: supabase/migrations/20260921000005_p48_aviso_titular.sql
      provides: "aviso_pedido_enviado_em, aviso_cancelamento_enviado_em"
      contains: "aviso_pedido_enviado_em"
    - path: supabase/functions/_shared/email-config.ts
      provides: "base do app e link de login com fail-safe"
      exports: ["APP_BASE_URL_PADRAO", "normalizarBaseApp", "montarUrlLogin"]
    - path: supabase/functions/executar-direito-titular/helpers.ts
      provides: "assunto/corpo/rótulo de sink/chave de idempotência dos dois avisos"
  key_links:
    - from: "supabase/functions/executar-direito-titular/index.ts (acao pedir/cancelar)"
      to: "solicitacoes_dados.aviso_*_enviado_em"
      via: "carimbo depois do 2xx do Resend"
      pattern: "aviso_(pedido|cancelamento)_enviado_em"
    - from: "supabase/functions/executar-direito-titular/helpers.ts"
      to: "supabase/functions/_shared/email-config.ts"
      via: "montarUrlLogin(base, '/candidato/privacidade')"
      pattern: "montarUrlLogin"
  prohibitions:
    - requirement_id: JORN-27
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT escrever `recibo_enviado_em` ao pedir ou cancelar — isso faria o titular ter a conta apagada sem o único e-mail que prova a exclusão"
    - requirement_id: JORN-27
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT incluir `solicitacao_id` nem o endereço literal do canal de privacidade no aviso (Invariante 12; D-07 — o PP-16 precisa continuar sendo troca em lugares contados)"
---

<objective>
Avisar o titular, no e-mail dele, quando um pedido de exclusão dos seus dados é feito e quando é
cancelado — com registro próprio de envio.

Purpose: Defeito 27 — hoje o pedido e o cancelamento geram três e-mails, todos para o RH, zero para a
titular. Com conta invadida, a exclusão é pedida e a pessoa não sabe. É controle de segurança, não
cortesia. A letra antiga do requisito mandava escrever `recibo_enviado_em`; a pesquisa provou que isso
suprimiria o recibo final — o operador confirmou colunas próprias (CONTEXT, correções de fato).

Output: migration com as duas colunas aplicada; `email-config` com o link de login; EF com os dois
avisos, testada e deployada.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-CONTEXT.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer) [BLOCKING]: o aviso do PEDIDO de ponta a ponta — coluna, link de login, e-mail, carimbo, deploy</name>
  <files>supabase/migrations/20260921000005_p48_aviso_titular.sql, supabase/functions/_shared/email-config.ts, supabase/functions/executar-direito-titular/helpers.ts, supabase/functions/executar-direito-titular/index.ts, supabase/functions/executar-direito-titular/index.test.ts</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §D.4 (desenho) e §D.6 (rota `/auth/login`, `?redirect=`, `resolveRedirect`)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §10 (base URL a mover, ZERO IMPORTS) e §13 (diferenças obrigatórias para o aviso)
    - `supabase/functions/_shared/email-config.ts` INTEIRO (contrato ZERO IMPORTS `:12-17`)
    - `supabase/functions/notificar-rh/helpers.ts` (`:28-37` `APP_BASE_URL_PADRAO`; `:180-217` `montarUrlFila` — o fail-safe a replicar; NÃO editar este arquivo aqui)
    - `supabase/functions/executar-direito-titular/index.ts` (`:230-249` `Deps`; `:547-573` ramo `pedir`; `:630-666` ramo `cancelar` e a busca do pedido agendado; `:1075-1103` o cinto do recibo; `:1343-1420` `enviarRecibo`)
    - `supabase/functions/executar-direito-titular/helpers.ts` (`:285-299` `dataBR`; `:301-418` assunto/corpo/chave do recibo; `:35-84` `logSeguroExclusao`/`causaDaFalha`)
    - `supabase/functions/executar-direito-titular/index.test.ts` (`:102` `makeMockSupabaseAdmin`, `:185` `makeMockTitular`, `:883` `makeFetchMock`; casos `(e)` `:318`, `(jj)` `:1570`, `(kk)` `:1585`, `(pp)` `:1829`)
    - `src/features/auth/utils/resolveRedirect.ts` (o login já sanitiza `?redirect=`)
    - `supabase/migrations/20260805000001_p45_pedido_exclusao.sql` (`:204-259` — ADD COLUMN + COMMENT)
  </read_first>
  <behavior>
    - `montarUrlLogin()` → `https://rh.beautysmile.com.br/auth/login`; `montarUrlLogin(undefined, '/candidato/privacidade')` → `…/auth/login?redirect=%2Fcandidato%2Fprivacidade`; `montarUrlLogin('lixo')` → default; `montarUrlLogin('http://x.com')` → default (só https); `montarUrlLogin(undefined, '//evil.com')` → sem `redirect`
    - pedir com sucesso → exatamente 1 POST a `https://api.resend.com/emails`, destinatário = e-mail do titular (ou sink em modo teste), `Idempotency-Key` presente; `aviso_pedido_enviado_em` carimbado; resposta ao titular IDÊNTICA à de antes (`ok, acao, executar_em, candidaturas_encerradas`)
    - pedir com `aviso_pedido_enviado_em` já preenchido → 0 POST
    - pedir com Resend respondendo 500 → resposta idêntica (`ok:true`), coluna NÃO carimbada, log com `aviso_resend_nao_2xx`
    - o corpo do e-mail contém a data de execução e o link de login com `redirect`; NÃO contém o id do pedido nem `lgpd@`
    - nenhum INSERT em `notificacoes_enviadas`; nenhum UPDATE com `recibo_enviado_em`
  </behavior>
  <action>
Rodar a varredura de portões (D-17) e conferir achados que citem `solicitacoes_dados` (o `p44_pedidos_dados_smoke.sql` conta policies e linhas — conferir que coluna nova não o afeta).

Migration `supabase/migrations/20260921000005_p48_aviso_titular.sql` (cabeçalho do M8, transporte `p46apply.cjs migrate`, sem `BEGIN;`): `ALTER TABLE public.solicitacoes_dados ADD COLUMN aviso_pedido_enviado_em timestamptz, ADD COLUMN aviso_cancelamento_enviado_em timestamptz` (sem `IF NOT EXISTS`) e `COMMENT ON COLUMN` em cada uma dizendo: carimbo do aviso ao titular (controle de conta invadida); NULL em pedido agendado = aviso não entregue (observável); e por que NÃO é `recibo_enviado_em`. `DO` de auto-verificação: as duas colunas existem. Aplicar: `node p46apply.cjs migrate supabase/migrations/20260921000005_p48_aviso_titular.sql`.

`_shared/email-config.ts` (ZERO IMPORTS preservado): exportar `APP_BASE_URL_PADRAO = 'https://rh.beautysmile.com.br'`, `normalizarBaseApp(baseBruta?: string): string` (fail-safe idêntico ao de `montarUrlFila`: só `https:` vira origem; malformada → default; nunca lança) e `montarUrlLogin(baseBruta?: string, redirect?: string): string` (`<base>/auth/login`; `redirect` só se começar com `/` e não com `//`, codificado com `encodeURIComponent`). Docblock: é a mesma base que `notificar-rh` usa (a consolidação de `notificar-rh` sobre este módulo é do plano 48-16) e é o link do JORN-U2.

`executar-direito-titular/helpers.ts`: `LABEL_SINK_AVISO_PEDIDO = 'aviso_exclusao_pedido'`, `LABEL_SINK_AVISO_CANCELAMENTO = 'aviso_exclusao_cancelado'` (só `[a-z_]`), `assuntoAvisoPedidoExclusao()` («Recebemos seu pedido de exclusão de dados»), `corpoAvisoPedidoExclusao({ dataExecucao, urlPrivacidade })` — via `layoutBase`, com: que um pedido de exclusão dos dados foi recebido; a data em que será executado (`dataBR`); que até lá pode ser cancelado na área de privacidade (botão «Acessar minha área de privacidade» + «Se o botão não funcionar, acesse: <url>», sempre `escapeHtml`); e «Se não foi você quem pediu, entre na sua conta e cancele o pedido — e responda a este e-mail.» Nada de id, nada de endereço literal do canal. `chaveIdempotenciaAviso(tipo: 'pedido' | 'cancelamento', pedidoId: string)` no molde de `chaveIdempotenciaRecibo` (nunca logada).

`executar-direito-titular/index.ts`: uma função `enviarAvisoTitular(deps, { tipo, pedidoId, para, assunto, html, label })` com o NÚCLEO de `enviarRecibo` (modo, sink, `exigirSinkTeste`, `ler_resend_api_key`, fetch com `Idempotency-Key`) que DEVOLVE `{ ok: true } | { ok: false, causa }` em vez de lançar. No ramo `pedir`, depois do fail-closed «sem linha»: localizar o pedido agendado do titular com a MESMA consulta que o ramo `cancelar` usa (escopo `candidato_id` + `TIPO_EXCLUSAO` + `SITUACAO_AGENDADO`), lendo também `aviso_pedido_enviado_em`; se nulo, enviar ao `emailTitular` já resolvido, com `urlPrivacidade = montarUrlLogin(Deno.env.get('APP_BASE_URL') || undefined, '/candidato/privacidade')`; se `ok`, carimbar `aviso_pedido_enviado_em = now()` pelo `supabaseAdmin`; se falhar, `logErro('pedir', 'aviso_' + causa, candidatoId)`. A resposta ao titular não muda em nenhum caso. Testes novos com os casos de `<behavior>` (inclusive titular sem candidatura).

Rodar os testes e deployar: `node efdeploy.cjs executar-direito-titular`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260921000005_p48_aviso_titular.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');for(const t of ['ADD COLUMN aviso_pedido_enviado_em','ADD COLUMN aviso_cancelamento_enviado_em','COMMENT ON COLUMN'])if(!c.includes(t))throw new Error('falta: '+t);if(/recibo_enviado_em\s*=/.test(c))throw new Error('a migration nao pode escrever recibo_enviado_em');const e=fs.readFileSync('supabase/functions/_shared/email-config.ts','utf8');if(/^\s*import\s/m.test(e))throw new Error('email-config.ts perdeu o contrato ZERO IMPORTS');for(const t of ['export const APP_BASE_URL_PADRAO','export function normalizarBaseApp','export function montarUrlLogin'])if(!e.includes(t))throw new Error('email-config sem: '+t);console.log('OK')"</automated>
    <fails_when>non-zero exit with "falta:", "wrapper BEGIN proibido", "recibo_enviado_em", "ZERO IMPORTS" or "email-config sem:" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs migrate supabase/migrations/20260921000005_p48_aviso_titular.sql && deno test --allow-all supabase/functions/executar-direito-titular/ supabase/functions/_shared/</automated>
    <fails_when>non-zero exit; "md5 do ledger BATE" absent; "FAILED" or "0 passed" in deno output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs executar-direito-titular && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/executar-direito-titular/body" | grep -a -c "aviso_pedido_enviado_em"</automated>
    <fails_when>non-zero exit from efdeploy, or the grep count printed is 0</fails_when>
  </verify>
  <acceptance_criteria>
    - As duas colunas existem em PROD (`information_schema.columns`).
    - `email-config.ts` exporta os três símbolos e continua sem `import`.
    - Os testes novos do ramo `pedir` passam (1 POST, carimbo, idempotência, falha não carimba e não muda a resposta, corpo sem id e sem `lgpd@`, com link de login), e nenhum teste antigo quebrou — em especial `(kk)` (nada em `notificacoes_enviadas`) e o recibo pós-exclusão.
    - `grep -n "recibo_enviado_em" supabase/functions/executar-direito-titular/index.ts` só aparece no passo 4 do motor e na leitura de órfãos (nenhuma escrita nova).
    - Deploy concluído; bundle com o carimbo novo.
  </acceptance_criteria>
  <done>Quem pede a exclusão dos próprios dados recebe um e-mail dizendo quando ela acontece e como cancelar — e, se não foi a pessoa, ela fica sabendo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: o aviso do CANCELAMENTO, e os testes do link de login</name>
  <files>supabase/functions/executar-direito-titular/helpers.ts, supabase/functions/executar-direito-titular/index.ts, supabase/functions/executar-direito-titular/index.test.ts, supabase/functions/_shared/__tests__/email-config.test.ts</files>
  <read_first>
    - `supabase/functions/executar-direito-titular/index.ts` (o estado deixado pela Task 1; ramo `cancelar` `:630-666`)
    - `supabase/functions/executar-direito-titular/index.test.ts` (caso `(g)` cancelar `:368`)
    - `supabase/functions/_shared/__tests__/email-config.test.ts` (estilo dos testes do módulo)
  </read_first>
  <behavior>
    - cancelar com sucesso → exatamente 1 POST ao Resend; `aviso_cancelamento_enviado_em` carimbado; resposta idêntica (`ok, acao:'cancelar', cancelado_em`)
    - cancelar com `aviso_cancelamento_enviado_em` já preenchido → 0 POST
    - cancelar com falha de envio → resposta idêntica, coluna nula, log `aviso_<causa>`
    - corpo do cancelamento: data do cancelamento, «seus dados continuam guardados conforme a política de retenção», «Se não foi você…», link de login com `redirect`; sem id, sem `lgpd@`
    - `email-config.test.ts`: os 5 casos de `montarUrlLogin`/`normalizarBaseApp` da Task 1
  </behavior>
  <action>
`helpers.ts`: `assuntoAvisoCancelamentoExclusao()` («Seu pedido de exclusão de dados foi cancelado») e `corpoAvisoCancelamentoExclusao({ dataCancelamento, urlPrivacidade })` no mesmo molde do aviso de pedido (data do cancelamento em `dataBR`; que os dados continuam guardados conforme a política de retenção; «Se não foi você quem cancelou, entre na sua conta — e responda a este e-mail.»; botão para a área de privacidade). Sem id, sem endereço literal do canal.

`index.ts`, ramo `cancelar`: a busca do pedido (`:636`) passa a ler também `aviso_cancelamento_enviado_em`; depois do fail-closed «sem linha» e do `cancelado_em` válido, se a coluna for nula, `enviarAvisoTitular` com `tipo='cancelamento'`, `label=LABEL_SINK_AVISO_CANCELAMENTO`, `pedidoId=pedido.id`; carimbo em `ok`; `logErro('cancelar', 'aviso_' + causa, candidatoId, pedido.id)` em falha. Resposta inalterada.

Testes: os casos de `<behavior>` em `index.test.ts`; os casos de URL em `email-config.test.ts`.

Rodar a suíte das EFs tocadas e redeployar `executar-direito-titular`. Registrar no SUMMARY que `notificar-candidato` e `notificar-rh` também importam `_shared/email-config.ts`: os exports novos são aditivos e chegam ao bundle deles no próximo deploy de cada uma (planos 48-08 e 48-16) — sem mudança de comportamento até lá.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/executar-direito-titular/ supabase/functions/_shared/ supabase/functions/notificar-candidato/ supabase/functions/notificar-rh/</automated>
    <fails_when>non-zero exit, "FAILED" or "0 passed" in the output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs executar-direito-titular && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/executar-direito-titular/body" | grep -a -c "aviso_cancelamento_enviado_em"</automated>
    <fails_when>non-zero exit from efdeploy, or the grep count printed is 0</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && ! grep -rn "lgpd@" supabase/functions/executar-direito-titular/ supabase/functions/_shared/email-config.ts && echo "zero ocorrencia literal do canal (D-07)"</automated>
    <fails_when>non-zero exit (grep found a literal privacy-channel address in the EF or in email-config)</fails_when>
  </verify>
  <acceptance_criteria>
    - Testes do cancelamento e da URL verdes; suítes de `notificar-candidato` e `notificar-rh` intactas (importam o `email-config` alterado).
    - Deploy com o carimbo de cancelamento no bundle.
    - Zero ocorrência literal do canal de privacidade nas EFs tocadas (D-07).
  </acceptance_criteria>
  <done>Pedido e cancelamento de exclusão chegam ao e-mail do titular, com registro próprio, e o recibo final continua saindo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| titular (JWT) → EF `executar-direito-titular` | a EF resolve o titular e o pedido no servidor (Invariante 12) |
| EF → Resend | e-mail ao endereço do titular; modo teste com sink obrigatório |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-07-01 | Spoofing | conta invadida pedindo exclusão sem o dono saber | high | mitigate | aviso no pedido e no cancelamento ao e-mail do titular, com instrução de cancelar |
| T-48-07-02 | Denial of Service | recibo final suprimido | high | mitigate | colunas próprias; teste prova que `pedir`/`cancelar` não escrevem `recibo_enviado_em`; testes do recibo seguem verdes |
| T-48-07-03 | Information Disclosure | `solicitacao_id` ou canal literal no e-mail | medium | mitigate | teste de corpo; grep D-07 no verify |
| T-48-07-04 | Repudiation | falha de envio silenciosa | medium | mitigate | log por código + coluna nula observável; resposta ao titular inalterada |
| T-48-07-05 | Tampering | open redirect pelo link de login | low | mitigate | `montarUrlLogin` só aceita caminho interno; o login sanitiza de novo com `resolveRedirect` |
| T-48-07-06 | Information Disclosure | e-mail real num run de teste | medium | mitigate | `exigirSinkTeste` antes de qualquer envio (DELIV-03) |
| T-48-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Migration aplicada (md5 BATE); colunas em PROD.
- `deno test` das EFs `executar-direito-titular`, `_shared`, `notificar-candidato`, `notificar-rh` verde.
- EF deployada com os dois carimbos; zero literal do canal.
- A prova com conta real (pedido + cancelamento, e-mails na caixa, colunas preenchidas, `recibo_enviado_em` nulo) é do plano 48-18.
</verification>

<success_criteria>
O titular fica sabendo do que acontece com os próprios dados — e o recibo que prova a exclusão continua existindo.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `solicitacoes_dados.aviso_pedido_enviado_em`, `solicitacoes_dados.aviso_cancelamento_enviado_em` | colunas | **novas** |
| `supabase/migrations/20260921000005_p48_aviso_titular.sql` | migration | **nova** |
| `APP_BASE_URL_PADRAO`, `normalizarBaseApp`, `montarUrlLogin` (`_shared/email-config.ts`) | config/util | **novos** |
| `LABEL_SINK_AVISO_PEDIDO`, `LABEL_SINK_AVISO_CANCELAMENTO`, `assuntoAvisoPedidoExclusao`, `corpoAvisoPedidoExclusao`, `assuntoAvisoCancelamentoExclusao`, `corpoAvisoCancelamentoExclusao`, `chaveIdempotenciaAviso` | helpers da EF | **novos** |
| `enviarAvisoTitular` (`executar-direito-titular/index.ts`) | função interna | **nova** |

## Premissas sinalizadas (probe de borda)

JORN-27 e JORN-U2 foram classificados `unclassified` pelo probe. As bordas `explicit` (titular sem
candidatura recebe; segundo `pedir` não reenvia) vêm do RESEARCH §D.4 e da contagem 16/42. Não existe
varredura de reenvio automático do aviso (fora de escopo): um aviso que falhou fica visível pela
coluna nula e pelo log por código.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-07-SUMMARY.md` when done
</output>
