---
phase: 48-consertos-da-jornada-bloco-1
plan: 14
type: execute
wave: 4
depends_on: ["48-02", "48-09", "48-11"]
files_modified:
  - database.types.ts
  - src/features/explicacao/services/explicacaoService.ts
  - src/features/explicacao/services/__tests__/explicacaoService.test.ts
  - src/features/explicacao/components/ExplicacaoCandidatoPage.tsx
  - src/features/explicacao/components/__tests__/ExplicacaoCandidatoPage.test.tsx
  - src/features/vagas/services/candidaturasService.ts
  - src/features/vagas/types/vagasTypes.ts
  - src/components/pages/DashboardCandidatoPage.tsx
  - src/components/pages/__tests__/DashboardCandidatoPage.reaberta.test.tsx
autonomous: true
requirements: [JORN-19]
coupling_justified: ["48-13: os dois escrevem, na mesma wave, a frase de reabertura de D-01 (aqui na página, no 48-13 no template de e-mail) como literal pinado letra por letra em cada plano, e o 48-16 cruza os dois arquivos antes de publicar — qualquer ordem produz o mesmo texto; a regeneração de database.types.ts deste plano pode ou não ver a função do 48-13, e o 48-17 faz a regeneração final"]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A página de explicação e o e-mail dizem a MESMA coisa sobre a reabertura (regra da 42-UI-SPEC): `COPY_REVISAO.veredito.revertida` passa a «sua candidatura foi reaberta e será decidida novamente até DD/MM/AAAA.», com a mesma data do e-mail (data-limite em SP) e a mesma forma sem data quando o prazo não é legível (D-01)."
    - "Numa candidatura REABERTA (`decisao_final.reaberta_em` não nulo), a página NÃO diz «decidimos não seguir com a sua candidatura» nem oferece o CTA de revisão — mostra o estado de reabertura com o prazo. A decisão antiga não é apresentada como vigente."
    - "O painel do candidato, numa candidatura reaberta, troca a linha de SLA da etapa («Em decisão final — resposta em até 3 dias úteis», que contradiria os 10 dias) pela linha `data-testid=\"prazo-reabertura\"` «Candidatura reaberta — nova decisão até DD/MM/AAAA.» (RESEARCH §E.3 passo 8)."
    - "O painel lê o prazo pela allowlist de leitura do candidato, embutindo `decisao_final ( reaberta_em, prazo_nova_decisao_em )` no select de `candidaturas` (RLS de SELECT do candidato sobre a própria `decisao_final`); nenhuma coluna de revisão/justificativa é lida."
    - "`hasDecisaoFinal` (`DashboardCandidatoPage.tsx:148-159`) não muda (D-12): com `data_decisao_final` zerado pela reabertura (48-11), o cartão «Entenda a decisão» some sozinho até a decisão nova."
    - "BORDA (explicit): candidatura sem linha em `decisao_final`, ou com `reaberta_em` nulo, mantém exatamente a linha de SLA de hoje; candidatura encerrada continua sem linha de prazo."
    - "`database.types.ts` regenerado pelo comando com `< /dev/null` e não vazio; `tsc` ≤ 90 (D-15); front publicado com `origin/main..HEAD` vazio e o marcador `prazo-reabertura` servido em PROD (D-16)."
  artifacts:
    - path: src/features/explicacao/components/ExplicacaoCandidatoPage.tsx
      provides: "estado de reabertura e cópia igual à do e-mail"
      contains: "reaberta e será decidida novamente"
    - path: src/components/pages/DashboardCandidatoPage.tsx
      provides: "linha de prazo da reabertura"
      contains: "prazo-reabertura"
    - path: src/components/pages/__tests__/DashboardCandidatoPage.reaberta.test.tsx
      provides: "prova do ramo de reabertura do painel"
  key_links:
    - from: "src/features/vagas/services/candidaturasService.ts (select do candidato)"
      to: "decisao_final.reaberta_em / prazo_nova_decisao_em"
      via: "embed PostgREST pela FK candidatura_id"
      pattern: "decisao_final \\( reaberta_em, prazo_nova_decisao_em \\)"
    - from: "src/features/explicacao/components/ExplicacaoCandidatoPage.tsx"
      to: "supabase/functions/_shared/email-templates.ts (COPY_REVISAO_REVERTIDA)"
      via: "mesma frase, mesma data"
      pattern: "decidida novamente"
  prohibitions:
    - requirement_id: JORN-19
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT mostrar ao candidato de uma candidatura reaberta a rejeição revertida como decisão vigente, nem um prazo que contradiga o do e-mail"
---

<objective>
O que o candidato vê numa candidatura reaberta: a página de explicação e o painel dizem que ela foi
reaberta e até quando será decidida — a mesma frase e a mesma data do e-mail (48-13).

Purpose: sem este plano, a reabertura (48-11) deixaria a página dizendo «decidimos não seguir» ao lado
de «a decisão anterior foi revista», e o painel prometendo «resposta em até 3 dias úteis» contra os 10
dias do e-mail — exatamente a classe de promessa sem código que a fase existe para eliminar.

Output: tipos regenerados; serviço e página com o estado de reabertura; painel com a linha de prazo;
publicado.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): a reabertura na página de explicação — tipos, serviço, página, a mesma frase do e-mail</name>
  <files>database.types.ts, src/features/explicacao/services/explicacaoService.ts, src/features/explicacao/services/__tests__/explicacaoService.test.ts, src/features/explicacao/components/ExplicacaoCandidatoPage.tsx, src/features/explicacao/components/__tests__/ExplicacaoCandidatoPage.test.tsx</files>
  <read_first>
    - `src/features/explicacao/services/explicacaoService.ts` (o estado deixado pelo 48-09; o select de `decisao_final` por allowlist e o objeto devolvido no ramo `humana`)
    - `src/features/explicacao/components/ExplicacaoCandidatoPage.tsx` (`:40-100` COPY e COPY_REVISAO; o bloco de resultado da revisão e o CTA)
    - `src/features/explicacao/components/__tests__/ExplicacaoCandidatoPage.test.tsx` (`:60`, `:165` — pinam «a decisão anterior foi revista»)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-13-PLAN.md` Task 3 (a frase de D-01 que o 48-13 escreve no template de e-mail, em paralelo nesta wave — a página usa a MESMA, letra por letra)
    - `supabase/migrations/20260921000011_p48_reabertura_colunas_e_resposta.sql` (a fórmula do prazo — a data-limite é o instante gravado menos 1 segundo, em SP)
    - `src/lib/datetime/formatDataHoraSP.ts` (formatação SP compartilhada)
  </read_first>
  <behavior>
    - serviço: linha de `decisao_final` com `reaberta_em` e `prazo_nova_decisao_em` → o objeto devolvido inclui `reaberta_em` e `prazo_nova_decisao_em`
    - página: `revisao_veredito='revertida'` + `reaberta_em` + prazo `2026-10-04T03:00:00Z` → texto «Após a revisão, sua candidatura foi reaberta e será decidida novamente até 03/10/2026.»; NÃO contém «decidimos não seguir»; sem CTA de revisão
    - página: `revertida` sem prazo legível → a frase sem data
    - página: `mantida` → inalterada; caminho `humana` sem revisão → inalterado; `humana_triagem` e `automatica` → inalterados
    - nenhuma ocorrência de «a decisão anterior foi revista» no arquivo da página
  </behavior>
  <action>
Regenerar os tipos (colunas novas do 48-11; o que o 48-13 criar em paralelo entra aqui se já estiver aplicado, e o 48-17 faz a regeneração final): `SUPABASE_ACCESS_TOKEN=$(security find-generic-password -s "Supabase CLI" -a supabase -w) npx supabase gen types typescript --project-id isljnozzlvckrgjjbjwp < /dev/null > database.types.ts` (se sair com 0 octeto: `git checkout -- database.types.ts` e repetir).

`explicacaoService.ts`: a allowlist do select de `decisao_final` ganha `reaberta_em, prazo_nova_decisao_em`; o objeto do ramo `humana` expõe os dois (docblock: estado de reabertura, D-01/D-10; nenhuma coluna nova de conteúdo). Os ramos `automatica` e `humana_triagem` devolvem `null` para eles.

`ExplicacaoCandidatoPage.tsx`: `COPY_REVISAO.veredito.revertida` passa a ser montado com a data (`sua candidatura foi reaberta e será decidida novamente até ${data}.`) e sem ela (`sua candidatura foi reaberta e será decidida novamente.`), mantendo o prefixo tipográfico «Após a revisão, » — a concatenação TEM de ser idêntica à frase do e-mail do 48-13. A data = `prazo_nova_decisao_em − 1 s` formatada `dd/mm/aaaa` em `America/Sao_Paulo`. Quando `reaberta_em` não é nulo: não renderizar a `resultLine` de rejeição («decidimos não seguir…») nem o CTA de revisão; o cabeçalho continua o mesmo, e o bloco de resultado da revisão (com a frase nova e «Respondida em …») passa a ser a informação principal. Comentário citando D-01 e a regra da 42-UI-SPEC (e-mail e página dizem a mesma coisa).

Atualizar os testes que pinam a cópia antiga e acrescentar os casos de `<behavior>`. A frase antiga não pode sobreviver nem em comentário no arquivo da página (o verify reprova qualquer ocorrência).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -s database.types.ts && grep -q "prazo_nova_decisao_em" database.types.ts && npx vitest run src/features/explicacao && ! grep -n "a decisão anterior foi revista" src/features/explicacao/components/ExplicacaoCandidatoPage.tsx</automated>
    <fails_when>non-zero exit; database.types.ts empty or without the new column; "failed" in the vitest summary; or grep still finds the old copy in the page</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const p=require('fs').readFileSync('src/features/explicacao/components/ExplicacaoCandidatoPage.tsx','utf8');for(const t of ['sua candidatura foi reaberta e será decidida novamente até','sua candidatura foi reaberta e será decidida novamente.'])if(!p.includes(t))throw new Error('pagina sem a frase de D-01: '+t);console.log('frase de D-01 na pagina (o cruzamento com o template de e-mail e conferido no 48-16)')"</automated>
    <fails_when>non-zero exit with "pagina sem a frase de D-01"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }</automated>
    <fails_when>non-zero exit — lint did not run (exit code other than 0 or 2), exit code and error count disagree, or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `database.types.ts` regenerado, não vazio, com as colunas novas.
    - Testes da explicação verdes com todos os casos de `<behavior>`; cópia antiga removida da página.
    - A frase de D-01 (com e sem data) está na página exatamente como o 48-13 a escreve no template de e-mail — o cruzamento automático dos dois arquivos é feito no 48-16, quando ambos já estão no disco.
    - `tsc errors` ≤ 90.
  </acceptance_criteria>
  <done>Quem abre a explicação de uma candidatura reaberta lê que ela foi reaberta e até quando — não uma rejeição que já não vale.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: o prazo da reabertura no painel do candidato</name>
  <files>src/features/vagas/services/candidaturasService.ts, src/features/vagas/types/vagasTypes.ts, src/components/pages/DashboardCandidatoPage.tsx, src/components/pages/__tests__/DashboardCandidatoPage.reaberta.test.tsx</files>
  <read_first>
    - `src/features/vagas/services/candidaturasService.ts` (`:270-300` — o select do candidato por allowlist)
    - `src/features/vagas/types/vagasTypes.ts` (`:212` — `Candidatura`)
    - `src/components/pages/DashboardCandidatoPage.tsx` (`:148-159` `hasDecisaoFinal` INTOCÁVEL; `:360-375` `PrazoEstimadoLinha` e `rotuloDeEspera`)
    - `src/features/timeline/components/PrazoEstimadoLinha.tsx` (`:16-22` — só recebe `rotulo`)
    - `src/components/pages/__tests__/DashboardCandidatoPage.encerrada.test.tsx` (molde de teste do painel)
  </read_first>
  <behavior>
    - candidatura `decisao_final`/`em_analise` com `decisao_final: { reaberta_em: '…', prazo_nova_decisao_em: '2026-10-04T03:00:00Z' }` → renderiza `data-testid="prazo-reabertura"` com «Candidatura reaberta — nova decisão até 03/10/2026.» e NÃO renderiza «resposta em até 3 dias úteis»
    - candidatura em `decisao_final` sem reabertura (`decisao_final` nulo ou `reaberta_em` nulo) → a linha de SLA de hoje
    - candidatura encerrada → sem linha de prazo (como hoje)
  </behavior>
  <action>
`candidaturasService.ts`: acrescentar ao select do candidato o embed `decisao_final ( reaberta_em, prazo_nova_decisao_em )` (docblock: só o estado de reabertura; nenhuma coluna de revisão, justificativa ou decisor). `vagasTypes.ts`: `Candidatura` ganha `decisao_final?: { reaberta_em: string | null; prazo_nova_decisao_em: string | null } | null` (o PostgREST devolve objeto — relação um-para-um pela UNIQUE — ou `null`; tratar array defensivamente se o tipo gerado vier como array).

`DashboardCandidatoPage.tsx`: no `rotulo` de `PrazoEstimadoLinha`, antes de `rotuloDeEspera`, um ramo: candidatura não encerrada E `decisao_final?.reaberta_em` E `decisao_final?.prazo_nova_decisao_em` → «Candidatura reaberta — nova decisão até DD/MM/AAAA.» (mesma regra de data do Task 1: prazo − 1 s, em SP), renderizado com `data-testid="prazo-reabertura"` (se `PrazoEstimadoLinha` não aceitar o atributo, envolver a linha num contêiner com ele). `hasDecisaoFinal` não muda.

Teste novo `DashboardCandidatoPage.reaberta.test.tsx` com os três cenários de `<behavior>`. Rodar as suítes do painel e da vaga.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && grep -q "return Boolean(candidatura.data_decisao_final || candidatura.feedback_rejeicao);" src/components/pages/DashboardCandidatoPage.tsx && npx vitest run src/components/pages src/features/vagas</automated>
    <fails_when>non-zero exit; the hasDecisaoFinal return line changed (D-12); or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }</automated>
    <fails_when>non-zero exit — lint did not run (exit code other than 0 or 2), exit code and error count disagree, or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - O select do candidato embute só `reaberta_em, prazo_nova_decisao_em` de `decisao_final`.
    - `DashboardCandidatoPage.reaberta.test.tsx` verde com os três cenários; testes antigos do painel verdes.
    - `hasDecisaoFinal` intocado; `tsc errors` ≤ 90.
  </acceptance_criteria>
  <done>O painel de uma candidatura reaberta mostra o prazo verdadeiro em vez de prometer uma resposta em 3 dias.</done>
</task>

<task type="auto">
  <name>Task 3: publicar — suíte, build, push e marcador no ar</name>
  <files>src/components/pages/DashboardCandidatoPage.tsx</files>
  <read_first>
    - `CLAUDE.md` §«Esta via NÃO passa pelo git»
  </read_first>
  <action>
`npm run test:run`, `npm run build`, conferir o chunk (`grep -rl "prazo-reabertura" build/assets/`). Commitar, `git push origin main` (deploy corretivo — D-18; a reabertura já está no ar desde o 48-11 e este é o passo seguinte da cadeia `migration → EF → cliente`), aguardar a Vercel e provar o marcador em PROD. Nenhuma mudança de código nesta task.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npm run test:run >/dev/null && npm run build >/dev/null && grep -rl "prazo-reabertura" build/assets/</automated>
    <fails_when>non-zero exit; "failed" in vitest; or grep prints no file</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && node -e "const M=process.argv[1],B='https://rh.beautysmile.com.br';(async()=>{const vis=new Set(),fila=['/'];let ok=false;while(fila.length&&vis.size<300){const p=fila.shift();if(vis.has(p))continue;vis.add(p);let t='';try{t=await (await fetch(B+p)).text()}catch{continue}if(t.includes(M)){ok=true;break}for(const m of t.matchAll(/(?:\/assets\/|\.\/)([\w.-]+\.js)/g))fila.push('/assets/'+m[1])}if(!ok){console.error('AUSENTE em PROD: '+M);process.exit(1)}console.log('PRESENTE em PROD: '+M)})()" "prazo-reabertura"</automated>
    <fails_when>non-zero exit; git log origin/main..HEAD printed a commit; or "AUSENTE em PROD" in stderr</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run test:run` verde; build com o marcador.
    - `git log --oneline origin/main..HEAD` vazio; `PRESENTE em PROD: prazo-reabertura`.
  </acceptance_criteria>
  <done>O que o candidato de uma candidatura reaberta vê na tela está no ar e bate com o e-mail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do candidato → PostgREST (`candidaturas` + embed `decisao_final`) | leitura própria sob RLS de SELECT |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-14-01 | Information Disclosure | embed de `decisao_final` expondo justificativa/decisor ao candidato | medium | mitigate | allowlist de 2 colunas (`reaberta_em`, `prazo_nova_decisao_em`); RLS de SELECT própria |
| T-48-14-02 | Repudiation | página/painel contradizendo o e-mail | medium | mitigate | mesma frase e mesma regra de data; verify pina a frase na página e o 48-16 cruza página × template |
| T-48-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Tipos regenerados; vitest da explicação, do painel e da vaga verdes; suíte inteira verde; `tsc` ≤ 90.
- Marcador `prazo-reabertura` em PROD; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
O candidato de uma candidatura reaberta vê, na página e no painel, a mesma verdade que recebeu por e-mail.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `Explicacao.reaberta_em`, `Explicacao.prazo_nova_decisao_em` | campos do serviço | **novos** |
| estado de reabertura em `ExplicacaoCandidatoPage` (`COPY_REVISAO.veredito.revertida` com data) | página | editada |
| `Candidatura.decisao_final` (embed) | tipo + select | **novo** |
| linha `data-testid="prazo-reabertura"` | painel | **nova** |
| `src/components/pages/__tests__/DashboardCandidatoPage.reaberta.test.tsx` | teste | **novo** |
| `database.types.ts` | tipos gerados | regenerado |

## Premissas sinalizadas (probe de borda)

JORN-19 foi classificado `unclassified` pelo probe; as bordas `explicit` (sem reabertura mantém o SLA de
hoje; encerrada sem linha) vêm do RESEARCH §E.3 passo 8.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-14-SUMMARY.md` when done
</output>
