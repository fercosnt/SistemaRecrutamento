---
phase: 48-consertos-da-jornada-bloco-1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/analise-candidato-individual/index.ts
  - supabase/functions/analise-candidato-individual/__tests__/index.test.ts
  - supabase/migrations/20260921000004_p48_analise_descartada_knockout.sql
autonomous: true
requirements: [JORN-24]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A EF `analise-candidato-individual` lê a candidatura ANTES da marca `pendente` e de qualquer `callAi`; se `status='rejeitado'` e `opcao_knockout_id` não nulo, devolve `200 {ok:true, skipped:'knockout'}` sem criar linha em `analise_candidato_vaga` e sem chamar a IA (JORN-24 parte a — o conserto principal de D-02)."
    - "A guarda mora na EF, não no trigger: `trg_candidaturas_analise` é AFTER INSERT e a candidatura nasce `aguardando_resposta`; o knockout é um UPDATE posterior da mesma transação, e o `pg_net` só entrega depois do COMMIT — só a EF vê o estado final."
    - "`analise_candidato_vaga` ganha `descartada_em timestamptz` e `descartada_motivo text` com CHECK (`descartada_motivo IN ('knockout_automatico')`) e CHECK de coerência (os dois nulos ou os dois preenchidos) — marca, não apaga (D-02)."
    - "As análises já geradas depois de knockout automático são MARCADAS em PROD: exatamente 3 linhas (`92522073-484c-46a3-9e9d-8e80afa3c062`, `0f7b217c-dcb0-44cd-939b-72b7f2c36856`, `25a4231c-612b-4f86-9c5a-904ca09f18f4`, todas de contas `+claude`). Contagem medida antes (3 sem marca) e depois (3 com `descartada_motivo='knockout_automatico'`); a migration aborta se o predicado não devolver exatamente essas 3 (D-02, D-18 — escrita retroativa já autorizada)."
    - "Nenhuma análise é apagada; a contagem total de `analise_candidato_vaga` é a mesma antes e depois (D-02)."
    - "BORDA (explicit): uma candidatura em andamento (não knockout) continua sendo analisada exatamente como antes — a guarda só dispara com as DUAS condições."
    - "Parar a análise reduz o Defeito 25 daqui para frente mas NÃO o conserta — o comparativo segue sem filtrar por `status`; o Defeito 25 não é declarado resolvido."
    - "A EF foi deployada por `node efdeploy.cjs analise-candidato-individual` (verify_jwt=false pela tabela do script); a migration por `p46apply.cjs` com md5 conferido (D-16)."
  artifacts:
    - path: supabase/functions/analise-candidato-individual/index.ts
      provides: "guarda de knockout antes da marca pendente"
      contains: "skipped: \"knockout\""
    - path: supabase/migrations/20260921000004_p48_analise_descartada_knockout.sql
      provides: "colunas de descarte + marcação retroativa com portão de contagem"
      contains: "descartada_motivo"
  key_links:
    - from: "trg_candidaturas_analise (AFTER INSERT em candidaturas)"
      to: "supabase/functions/analise-candidato-individual/index.ts"
      via: "net.http_post pós-COMMIT; a EF lê o estado final e pula o knockout"
      pattern: "opcao_knockout_id"
  prohibitions:
    - requirement_id: JORN-24
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT enviar a um provedor de IA os dados de quem o knockout já eliminou — é tratamento sem finalidade (LGPD Art. 6º III) e contradiz a explicação que diz que nenhuma análise foi usada"
    - requirement_id: JORN-24
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT apagar as análises já geradas — o titular já recebeu essa análise numa cópia LGPD e a marca é o que deixa auditável que houve tratamento sem finalidade e que foi corrigido (D-02)"
---

<objective>
Parar de analisar por IA quem o knockout já eliminou, e marcar (não apagar) as análises que já
existem nessa condição.

Purpose: o Defeito 24 custa US$ 0,045 por candidato eliminado e é tratamento sem finalidade. A parte
(a) — parar — é o conserto principal; a parte (b) — marcar o passivo — é D-02, com escrita
retroativa já autorizada pelo operador sobre 3 linhas de conta de teste.

Output: EF com a guarda, deployada; migration com as colunas de descarte e a marcação retroativa
com portão de contagem, aplicada. O veredito de export das colunas novas é do plano 48-17.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): a guarda de knockout na EF — testada e deployada</name>
  <files>supabase/functions/analise-candidato-individual/index.ts, supabase/functions/analise-candidato-individual/__tests__/index.test.ts</files>
  <read_first>
    - `supabase/functions/analise-candidato-individual/index.ts` (`:240-275` — a marca `pendente` hoje ANTES da leitura da candidatura)
    - `supabase/functions/analise-candidato-individual/__tests__/index.test.ts` (`:35-49` `makeMockAnthropic` com `calls`; `:74-126` `makeMockSupabase` com `candidaturaRow`)
    - `supabase/functions/notificar-candidato/index.ts` (`:196-211` — o survivor-guard do knockout, molde)
    - `supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts` (`:476-493` — molde de asserção «nada foi tocado»)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §F
    - `efdeploy.cjs` (tabela `VERIFY_JWT`: `analise-candidato-individual: false` — não sobrescrever)
  </read_first>
  <behavior>
    - candidatura `status='rejeitado'` + `opcao_knockout_id='opt-1'` → resposta 200 com `skipped: "knockout"`; `upserts.length === 0`; `anthropic.calls.length === 0`
    - candidatura `status='aguardando_resposta'` + `opcao_knockout_id=null` → caminho atual (marca `pendente`, chama a IA) inalterado
    - candidatura `status='rejeitado'` + `opcao_knockout_id=null` (rejeição humana posterior ao INSERT) → caminho atual inalterado (a guarda exige as duas condições)
  </behavior>
  <action>
Em `index.ts`, mover a leitura da candidatura para ANTES da marca `pendente` (hoje `:257-267`) e acrescentar `opcao_knockout_id` à allowlist de colunas do select (nunca `*`). Logo depois da leitura, se `status === 'rejeitado' && opcao_knockout_id !== null`, logar pela via redigida existente (só `candidatura_id` e `skipped:'knockout'`, sem PII) e devolver `200 {ok:true, skipped:'knockout'}` — antes de qualquer escrita e de qualquer chamada de IA. Comentário curto dizendo por que a guarda não pode morar no trigger AFTER INSERT (o INSERT nasce `aguardando_resposta`; o knockout é UPDATE posterior; `pg_net` entrega pós-COMMIT — o mesmo motivo do survivor-guard de `notificar-candidato`). O resto do fluxo usa a leitura já feita (sem ler a candidatura duas vezes).

Testes novos no `__tests__/index.test.ts` com os três cenários de `<behavior>`, usando os mocks existentes; o caso de knockout usa um stub que explode se o provedor de IA for chamado.

Rodar `deno test --allow-all supabase/functions/analise-candidato-individual/` e deployar: `node efdeploy.cjs analise-candidato-individual`. Conferir no bundle deployado que o marcador `skipped: "knockout"` (ou a forma exata que o código usar) está presente.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/analise-candidato-individual/</automated>
    <fails_when>non-zero exit, "FAILED" in the output, or "0 passed"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs analise-candidato-individual && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/analise-candidato-individual/body" | grep -a -c "knockout"</automated>
    <fails_when>non-zero exit from efdeploy, or the grep count printed is 0 (the deployed bundle lacks the guard)</fails_when>
  </verify>
  <acceptance_criteria>
    - A leitura da candidatura acontece antes do `upsert` de `pendente` no código; a allowlist inclui `opcao_knockout_id`.
    - Os três testes novos passam; nenhum teste antigo da EF quebrou.
    - Deploy concluído; o bundle deployado contém a guarda.
  </acceptance_criteria>
  <done>A próxima inscrição eliminada por knockout não gera análise de IA nem custo — provado no handler; a prova em PROD com inscrição real é do plano 48-18.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: marcar, não apagar — as colunas de descarte e as 3 análises pós-knockout</name>
  <files>supabase/migrations/20260921000004_p48_analise_descartada_knockout.sql</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §F (predicado de medição e a tabela das 3 linhas)
    - `supabase/migrations/20260805000001_p45_pedido_exclusao.sql` (`:204-259` — ADD COLUMN sem `IF NOT EXISTS` de propósito + `COMMENT ON COLUMN`)
    - `supabase/migrations/20260826000004_registrar_decisao_grava_a_data_e_o_status.sql` (portão antes do write)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §1
  </read_first>
  <action>
Medir ANTES, só leitura, e registrar no SUMMARY: (i) `select count(*) from public.analise_candidato_vaga` (total); (ii) o conjunto do predicado D-02 do RESEARCH §F — candidaturas com `motivo_rejeicao='knockout_automatico' AND opcao_knockout_id IS NOT NULL` que têm linha em `analise_candidato_vaga` com `created_at >=` o `criado_em` da linha de `historico_candidatura` com `auto_rejeitado = true` — esperado exatamente os 3 ids do must_have. Se não forem esses 3, PARAR e reportar (D-14): a autorização do operador é para esse escopo medido.

Rodar a varredura de portões (D-17) e conferir achados que citem `analise_candidato_vaga`.

Escrever `supabase/migrations/20260921000004_p48_analise_descartada_knockout.sql` (cabeçalho do M8 com a medição acima, transporte `p46apply.cjs migrate`, sem `BEGIN;` externo):
- `ALTER TABLE public.analise_candidato_vaga ADD COLUMN descartada_em timestamptz, ADD COLUMN descartada_motivo text` (sem `IF NOT EXISTS`, de propósito) + `CHECK (descartada_motivo IS NULL OR descartada_motivo IN ('knockout_automatico'))` + `CHECK ((descartada_em IS NULL) = (descartada_motivo IS NULL))`, com nomes de constraint explícitos;
- `COMMENT ON COLUMN` nas duas: marca de tratamento sem finalidade corrigido (D-02), por que não um valor novo em `status` (o `status` é lido por `v_triagem_panel.analise_status`, pela fila e pelo hub — mudar o vocabulário arrastaria leitores), e que a linha NÃO é apagada;
- bloco `DO` de escrita retroativa com portão: conta o conjunto do predicado; aborta se o total for diferente de 3 OU se algum id não estiver na lista dos 3 esperados; faz o UPDATE `SET descartada_em = now(), descartada_motivo = 'knockout_automatico'` restrito a esse conjunto e a linhas ainda sem marca; confere `ROW_COUNT = 3`; aborta se a contagem total da tabela mudou.

Aplicar: `node p46apply.cjs migrate supabase/migrations/20260921000004_p48_analise_descartada_knockout.sql`. Medir DEPOIS e registrar no SUMMARY: total igual ao de antes; 3 linhas com `descartada_motivo='knockout_automatico'`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260921000004_p48_analise_descartada_knockout.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');if(/^\s*DELETE\s+FROM/im.test(c))throw new Error('DELETE proibido (D-02: marcar, nao apagar)');for(const t of ['ADD COLUMN descartada_em','ADD COLUMN descartada_motivo','knockout_automatico','COMMENT ON COLUMN','ROW_COUNT','92522073-484c-46a3-9e9d-8e80afa3c062','0f7b217c-dcb0-44cd-939b-72b7f2c36856','25a4231c-612b-4f86-9c5a-904ca09f18f4'])if(!c.includes(t))throw new Error('falta: '+t);console.log('OK')"</automated>
    <fails_when>non-zero exit with "falta:", "wrapper BEGIN proibido" or "DELETE proibido" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs migrate supabase/migrations/20260921000004_p48_analise_descartada_knockout.sql && node p46apply.cjs sql "SET TRANSACTION READ ONLY; select count(*) filter (where descartada_motivo='knockout_automatico')::int as marcadas from public.analise_candidato_vaga" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);if(r[0].marcadas!==3){console.error('marcadas='+r[0].marcadas+' (esperado 3)');process.exit(1)}console.log('marcadas=3')})"</automated>
    <fails_when>non-zero exit; "md5 do ledger BATE" absent from the migrate output; or "marcadas=N (esperado 3)" in stderr</fails_when>
  </verify>
  <acceptance_criteria>
    - Contagem ANTES registrada (total e os 3 ids sem marca) e contagem DEPOIS registrada (total igual; 3 marcadas).
    - O `DO` de escrita aborta por construção se o escopo divergir de 3 — nenhum UPDATE fora dos 3 ids.
    - Nenhum `DELETE` na migration; total de linhas inalterado.
    - Em PROD, `information_schema.columns` mostra as duas colunas novas e os dois CHECKs existem em `pg_constraint`.
  </acceptance_criteria>
  <done>O passivo está marcado e auditável, nenhuma análise foi apagada, e a escrita retroativa não podia tocar nada além do escopo autorizado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| banco (pg_net, Bearer do Vault) → EF `analise-candidato-individual` | chamada servidor→servidor pós-COMMIT |
| EF → provedor de IA (Anthropic/OpenAI) | dados do candidato saem do sistema |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-04-01 | Information Disclosure | envio de dados de eliminado ao provedor de IA | high | mitigate | guarda na EF antes de qualquer `callAi`; teste com stub que explode se a IA for chamada |
| T-48-04-02 | Tampering | escrita retroativa fora do escopo autorizado | high | mitigate | `DO` com portão: exatamente 3 ids esperados, `ROW_COUNT=3`, total inalterado; senão aborta a transação inteira |
| T-48-04-03 | Repudiation | apagar a prova de que houve tratamento sem finalidade | medium | mitigate | marca por coluna, nenhum `DELETE` (verify reprova) |
| T-48-04-04 | Information Disclosure | log da guarda vazando dado pessoal | low | mitigate | log só com `candidatura_id` e `skipped`, pela via redigida existente |
| T-48-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `deno test` da EF verde; deploy com a guarda no bundle.
- Migration aplicada (md5 BATE); 3 marcadas; total inalterado.
</verification>

<success_criteria>
Knockout não custa mais análise de IA, e o que já tinha sido analisado está marcado — sem uma única linha apagada.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| guarda `skipped:'knockout'` em `analise-candidato-individual` | EF | editada |
| `analise_candidato_vaga.descartada_em`, `analise_candidato_vaga.descartada_motivo` | colunas | **novas** |
| CHECKs de vocabulário e de coerência das colunas de descarte | constraints | **novas** |
| `supabase/migrations/20260921000004_p48_analise_descartada_knockout.sql` | migration (DDL + escrita retroativa com portão) | **nova** |

## Premissas sinalizadas (probe de borda)

JORN-24 foi classificado `unclassified` pelo probe; as bordas `explicit` (não-knockout segue analisado;
rejeição humana sem `opcao_knockout_id` segue analisada; portão de 3) vêm do RESEARCH §F.
**Premissa A6 do RESEARCH** (a marca deve entrar na cópia LGPD) é decidida no plano 48-17, onde
nascem os vereditos de export de todas as colunas novas da fase.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-04-SUMMARY.md` when done
</output>
