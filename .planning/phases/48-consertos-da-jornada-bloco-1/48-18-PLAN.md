---
phase: 48-consertos-da-jornada-bloco-1
plan: 18
type: execute
wave: 7
depends_on: ["48-01", "48-02", "48-03", "48-04", "48-05", "48-06", "48-07", "48-08", "48-09", "48-10", "48-11", "48-12", "48-13", "48-14", "48-15", "48-16", "48-17"]
files_modified:
  - supabase/tests/p48_prontidao_prod.sql
  - supabase/tests/p48_prova_prod.sql
  - .planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md
autonomous: false
requirements: [JORN-22, JORN-26, JORN-20, JORN-27, JORN-18, JORN-15, JORN-U2, JORN-24, JORN-06, JORN-19, JORN-D5]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Cada critério de sucesso do ROADMAP da Phase 48 tem uma prova em PROD que é uma CONSULTA com resultado esperado, não «apareceu na tela» (D-19): `p48_prova_prod.sql` (só leitura) devolve uma linha de booleanos, um por critério, e todos saem `true` depois da jornada."
    - "Antes de chamar o operador, uma sonda automatizada e só leitura (`p48_prontidao_prod.sql` + bundles + front) confirma que todo objeto da fase está no ar — funções, triggers, CHECK com 10 eventos, classes, cron, colunas, EFs com os marcadores, front com os marcadores e `origin/main..HEAD` vazio — para que a sessão humana não prove um deploy que faltou."
    - "JORN-20/JORN-22 em PROD: uma candidatura de teste que NUNCA teve `decisao`, rejeitada na triagem pelo RH, tem `feedback_rejeicao` neutro, `data_decisao_final` nulo, 1 linha nova `decisao` com chave `…:decisao:<uuid>` `enviado`/`entregue`, e a candidata vê o cartão e a página «analisada por uma pessoa da nossa equipe» (conferência humana)."
    - "JORN-18/JORN-19/D-23 em PROD: decisão `rejeitado` por RH-A → pedido de revisão → RH2 responde `revertida` → candidatura reaberta (`decisao_final`/`em_analise`), prazo pela fórmula de SP, histórico com a justificativa própria, e-mail «…reaberta e será decidida novamente até DD/MM/AAAA» (caixa) → RH-A é recusado ao redecidir (mensagem D-23 na tela) → RH3 decide → 2ª linha `decisao` com chave distinta, ciclo zerado na linha vigente e arquivado no histórico."
    - "JORN-27 em PROD: pedido e cancelamento de exclusão por titular de teste geram os dois avisos (caixa) e preenchem `aviso_pedido_enviado_em` e `aviso_cancelamento_enviado_em`, com `recibo_enviado_em` NULO."
    - "JORN-24 em PROD: uma inscrição de teste eliminada por knockout não cria linha em `analise_candidato_vaga` nem chamada em `ai_call_logs`."
    - "JORN-06 em PROD: uma submissão NOVA de Big Five pelo navegador, depois do conserto, cria linha em `devolutivas_candidato` (é a única prova do wiring do `Deno.serve` + `functions.invoke`)."
    - "D-22 e JORN-15/U2 em PROD: liberar a avaliação cognitiva gera linha `cognitivo_liberado` `enviado`/`entregue`; a confirmação de uma inscrição nova chega com a cópia D-09 e o botão para `/auth/login` (caixa)."
    - "JORN-D5 em PROD: tentar agendar online com `dddd` é recusado na tela e nenhuma linha inválida entra."
    - "JORN-26 em PROD: `v_fila_trabalho` sem candidatura encerrada; nenhum `encerrada_a_pedido_em` novo em candidatura encerrada desde o início da prova."
    - "Nenhum reset de conta de teste é feito se inscrições novas bastarem; qualquer reset que se mostre necessário é escrita destrutiva — contagem antes/depois, escopo ao `candidatura_id`, confirmação do operador (D-19). `p47_teardown_dados_de_teste.sql` e o bloco `salvar_config_purga(... p_confirmo_live := true)` do `46-07-RUNBOOK-FLIP` NÃO são rodados."
  artifacts:
    - path: supabase/tests/p48_prontidao_prod.sql
      provides: "sonda de prontidão só leitura (objetos da fase no ar)"
    - path: supabase/tests/p48_prova_prod.sql
      provides: "uma linha de booleanos por critério, a partir do instante T0"
    - path: .planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md
      provides: "T0, baseline, resultado de cada consulta e as conferências humanas"
  key_links:
    - from: "supabase/tests/p48_prova_prod.sql"
      to: "ROADMAP Phase 48 Success Criteria 1–7"
      via: "um booleano por critério, filtrado por alias +claude e criado depois de T0"
      pattern: "p48.t0"
  prohibitions:
    - requirement_id: JORN-19
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT rodar `p47_teardown_dados_de_teste.sql` nem o flip `p_confirmo_live := true` da purga durante a prova"
    - requirement_id: JORN-22
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT dar um critério por provado só porque a tela renderizou — toda prova é a consulta com o resultado esperado (D-19)"
---

<objective>
Provar em produção, com as contas de teste da jornada e medindo NO BANCO, que cada critério de sucesso da
Phase 48 é verdade — a mesma disciplina que achou os defeitos.

Purpose: D-19 — verificação no banco, nunca só na tela; foi assim que 29 defeitos apareceram, todos
renderizando bem. Os planos anteriores provaram cada peça por smoke e por deploy; faltam as provas que
exigem pessoa: sessão de candidato, RH distintos (revisor ≠ decisor, D-23), caixa de e-mail e tela.

Output: sonda de prontidão; consulta de prova; duas sessões humanas guiadas; `48-PROVA-PROD.md` com T0,
resultados e conferências.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-CONTEXT.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): a sonda de prontidão e a consulta de prova — tudo no ar antes de chamar alguém, e T0 registrado</name>
  <files>supabase/tests/p48_prontidao_prod.sql, supabase/tests/p48_prova_prod.sql, .planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 48 (os 7 critérios de sucesso)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-VALIDATION.md` (tabela requisito → verificação em PROD)
    - `.planning/JORNADA-GUIADA.md` §«Consultas de conferência» e §«Regras de operação desta jornada» (SÓ LEITURA — nunca escrever nesse arquivo; format-on-save já o corrompeu duas vezes)
    - os SUMMARYs dos planos 48-01..48-17 (marcadores de bundle e de front, nomes de objetos)
  </read_first>
  <action>
`supabase/tests/p48_prontidao_prod.sql` — SÓ LEITURA (começa com `SET TRANSACTION READ ONLY`), devolve uma linha de booleanos: `candidatura_encerrada` existe; `registrar_pedido_exclusao`, `retirar_candidatura`, `rejeitar_candidatura`, `funil_kpis` e `v_fila_trabalho` usam o predicado; `rejeitar_candidatura` grava o `feedback_rejeicao` neutro; `explicacao_rejeicao_origem` existe; `trg_notif_transicao` passa `historico_id`; as duas funções de revisão passam `ciclo`; `trg_notif_cognitivo_liberado` existe; CHECK de evento com os 10 valores; classes de `cognitivo_liberado` (transacional) e `prazo_reabertura_vencido` (interno); `varrer_retry_notificacoes` exclui o evento do prazo; `varrer_prazos_reabertura` existe sem EXECUTE para `authenticated`; job `prazo-reabertura-sweep` em `cron.job`; triggers de validação de `local_ou_link`; colunas novas de `decisao_final`, `decisao_final_historico`, `solicitacoes_dados`, `analise_candidato_vaga`; `registrar_decisao` com `coalesce(v_role` e D-23.

Prontidão dos bundles (Management API `/functions/<slug>/body`): `notificar-candidato` (`historico_inconsistente`, `cognitivo_liberado`, `Acessar meu painel`, `Acompanhe o andamento pelo seu painel`, `prazo_nova_decisao_em`), `notificar-rh` (`prazo_reabertura_vencido`, `ciclo`), `executar-direito-titular` (`aviso_pedido_enviado_em`, `aviso_cancelamento_enviado_em`), `analise-candidato-individual` (`knockout`), `submit-bigfive-final` (`serviceKey`), `gerar-devolutiva-bigfive` (SEM `diag-auth`), `exportar-meus-dados` (`descartada_em`). Prontidão do front (crawler em `https://rh.beautysmile.com.br`): `hub-acoes-encerrada`, `http:// ou https://`, `explicacao-humana-triagem`, `prazo-reabertura`, `decisao-recusa-decisor-revertido`, `Mudar o status aqui n`; e `git log --oneline origin/main..HEAD` vazio.

`supabase/tests/p48_prova_prod.sql` — SÓ LEITURA por construção (só SELECT; NÃO começa com `SET TRANSACTION`, porque quem o roda prefixa `SET TRANSACTION READ ONLY; SELECT set_config('p48.t0', …)` — o `SET TRANSACTION` tem de vir antes de qualquer consulta), lê `T0` de `current_setting('p48.t0')` e devolve UMA linha com um booleano por prova, filtrando contas de teste por `candidatos.email ilike '%+claude%'` e eventos com instante `> T0`. Parte 1: `p1_confirmacao_d09_enviada` (linha `confirmacao` nova `enviado`/`entregue`); `p1_knockout_sem_analise` (candidaturas de knockout criadas após T0 sem linha em `analise_candidato_vaga` nem em `ai_call_logs`); `p1_triagem_feedback_neutro` (candidatura rejeitada na triagem após T0 com o texto neutro exato e `data_decisao_final` nulo); `p1_triagem_decisao_avisada` (linha `decisao` nova para ela, chave `…:decisao:<uuid>`, `enviado`/`entregue`); `p1_cognitivo_avisado` (linha `cognitivo_liberado` nova `enviado`/`entregue`); `p1_agendamento_invalido_fora` (zero agendamento `online` criado após T0 sem `^https?://`); `p1_devolutiva_gerada` (linha nova em `devolutivas_candidato` após T0); `p1_fila_sem_encerrada` (`v_fila_trabalho` sem encerrada); `p1_sem_encerrada_a_pedido_errada` (nenhum `encerrada_a_pedido_em > T0` em candidatura encerrada). Parte 2: `p2_reabertura_registrada` (linha em `historico_candidatura` `rejeitado→decisao_final` após T0 com `criterio_texto` iniciando por «Candidatura reaberta»); `p2_revisao_respondida_avisada` (linha `revisao_respondida` nova com chave `…:revisao_respondida:<ciclo>`, `enviado`/`entregue`); `p2_revisao_solicitada_com_ciclo` (linhas de RH com chave contendo o ciclo); `p2_redecisao_avisada` (a candidatura reaberta tem ≥ 2 linhas `decisao` de chaves distintas, a última após T0 `enviado`/`entregue`); `p2_ciclo_zerado_e_arquivado` (linha vigente de `decisao_final` sem `revisao_veredito`/`reaberta_em`; linha arquivada com `revisao_veredito='revertida'`); `p2_d23` (o `por_usuario` da linha arquivada revertida ≠ o da decisão vigente); `p2_titular_avisado` (pedido de exclusão de conta de teste com `aviso_pedido_enviado_em > T0`, `aviso_cancelamento_enviado_em > T0` e `recibo_enviado_em IS NULL`).

Criar `48-PROVA-PROD.md` com: a linha `T0: <timestamp ISO UTC>` (agora), o resultado da sonda de prontidão e da consulta de prova rodada AGORA (baseline — as `p1_*`/`p2_*` que dependem de ação humana saem `false`), e as seções vazias das duas sessões. Se a prontidão falhar em qualquer item, PARAR e reportar qual plano não chegou ao ar.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs sql "$(cat supabase/tests/p48_prontidao_prod.sql)" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s)[0];const f=Object.entries(r).filter(([k,v])=>v!==true).map(([k])=>k);if(f.length){console.error('PRONTIDAO FALHOU: '+f.join(', '));process.exit(1)}console.log('prontidao do banco: '+Object.keys(r).length+' itens OK')})" && test -z "$(git log --oneline origin/main..HEAD)" && grep -q "^T0: " .planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md</automated>
    <fails_when>non-zero exit with "PRONTIDAO FALHOU: <itens>"; unpushed commits; or 48-PROVA-PROD.md without the T0 line</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && T=$(security find-generic-password -s 'Supabase CLI' -a supabase -w) && for par in "notificar-candidato:historico_inconsistente" "notificar-candidato:cognitivo_liberado" "notificar-candidato:Acompanhe o andamento pelo seu painel" "notificar-rh:prazo_reabertura_vencido" "executar-direito-titular:aviso_cancelamento_enviado_em" "analise-candidato-individual:knockout" "submit-bigfive-final:serviceKey" "exportar-meus-dados:descartada_em"; do slug=${par%%:*}; m=${par#*:}; curl -s -H "Authorization: Bearer $T" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/$slug/body" | grep -a -q "$m" || { echo "BUNDLE SEM MARCADOR: $slug -> $m"; exit 1; }; done; curl -s -H "Authorization: Bearer $T" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/gerar-devolutiva-bigfive/body" | grep -a -q "diag-auth" && { echo "diag-auth AINDA NO AR"; exit 1; }; echo "bundles OK"</automated>
    <fails_when>non-zero exit with "BUNDLE SEM MARCADOR" or "diag-auth AINDA NO AR"</fails_when>
  </verify>
  <acceptance_criteria>
    - `p48_prontidao_prod.sql` começa com `SET TRANSACTION READ ONLY`; `p48_prova_prod.sql` só tem SELECT e é sempre rodado com o prefixo `SET TRANSACTION READ ONLY` (grep: nenhum INSERT/UPDATE/DELETE nos dois arquivos).
    - Prontidão do banco, dos bundles e do front verde; `origin/main..HEAD` vazio.
    - `48-PROVA-PROD.md` com T0 e o baseline da consulta de prova.
  </acceptance_criteria>
  <done>Tudo o que a fase construiu está comprovadamente no ar — e existe uma consulta pronta para provar cada critério assim que a pessoa agir.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: sessão 1 — a jornada do candidato e do RH (inscrição, knockout, triagem, cognitivo, agendamento, Big Five)</name>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md` (T0 e baseline)
    - `.planning/JORNADA-GUIADA.md` §«IDs fixados» (vagas, opção que elimina — SÓ LEITURA)
  </read_first>
  <action>O operador executa, com as contas de teste (alias `+claude`) e uma conta RH, os seis passos abaixo, na ordem, e confere na caixa e na tela o que está marcado como conferência humana.</action>
  <instructions>
Claude já provou que tudo está no ar (Task 1). Faltam as ações que só uma pessoa com sessão de
candidato e de RH faz — e a leitura da caixa de e-mail e da tela.

(a) Inscrição NORMAL com conta de teste nova numa vaga (sem marcar a opção que elimina). Conferir na
caixa: o e-mail de confirmação diz «Acompanhe o andamento pelo seu painel a qualquer momento. Avisaremos
por e-mail quando houver algo para você fazer ou uma decisão…», NÃO diz «a cada etapa», e o botão
«Acessar meu painel» abre a tela de login do candidato.
(b) Inscrição com KNOCKOUT (marcar «Tenho disponibilidade apenas para trabalho remoto») com outra conta
de teste. Nada a conferir na tela — o banco confere.
(c) Como RH, rejeitar NA TRIAGEM a candidatura do passo (a) (ela nunca teve decisão). Como a
candidata, conferir: o e-mail de rejeição chegou (com o botão do painel); o cartão «Entenda a decisão»
aparece no painel; a página diz que a candidatura foi analisada por uma pessoa da equipe, NÃO oferece
pedido de revisão e mostra o canal de contato.
(d) Como RH, liberar a avaliação cognitiva para uma candidatura de teste em andamento. Conferir na caixa
do candidato o e-mail «Uma avaliação cognitiva foi liberada para você».
(e) Como RH, tentar agendar uma entrevista ONLINE com o link `dddd`. Conferir: o formulário recusa com a
mensagem de link inválido e não salva.
(f) Com uma conta de teste em «Avaliação assíncrona», concluir e enviar o Big Five. Conferir no painel
que a devolutiva aparece (ou avisar se não aparecer).

Avisar aqui «sessão 1 feita» com as observações das conferências. Claude então roda
`p48_prova_prod.sql` com T0 e confere as `p1_*`.
  </instructions>
  <files>.planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md</files>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && T0=$(grep -m1 "^T0: " .planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md | cut -d' ' -f2) && D=$(mktemp -d) && { echo "SET TRANSACTION READ ONLY; SELECT set_config('p48.t0', '$T0', false);"; cat supabase/tests/p48_prova_prod.sql; } > "$D/prova.sql" && node p46apply.cjs run "$D/prova.sql" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=s.slice(s.indexOf('['));const r=JSON.parse(j)[0];const f=Object.entries(r).filter(([k,v])=>k.startsWith('p1_')&&v!==true).map(([k])=>k);if(f.length){console.error('SESSAO 1 NAO PROVADA: '+f.join(', '));process.exit(1)}console.log('sessao 1: todas as p1_* true')})"</automated>
    <fails_when>non-zero exit with "SESSAO 1 NAO PROVADA: <provas>" — each listed boolean names the requirement that is not true in PROD</fails_when>
    <human-check>
      <test>Caixa do alias +claude e tela do candidato, conforme (a), (c), (d), (f)</test>
      <expected>Confirmação com a cópia D-09 e o botão do painel; rejeição com botão; página «analisada por uma pessoa» sem revisão e com canal; e-mail da avaliação cognitiva; devolutiva no painel</expected>
      <why_human>cópia de e-mail na caixa real e renderização da página — o banco prova o envio, não o texto lido</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Todas as `p1_*` de `p48_prova_prod.sql` saem `true` com o T0 registrado.
    - As conferências humanas de (a), (c), (d), (e), (f) estão registradas em `48-PROVA-PROD.md`.
  </acceptance_criteria>
  <verification>Claude roda `p48_prova_prod.sql` com o T0 de `48-PROVA-PROD.md` (verify abaixo) e exige todas as `p1_*` = true; registra o resultado e as observações do operador em `48-PROVA-PROD.md`.</verification>
  <resume-signal>Digite «sessão 1 feita» com as observações (ou descreva o que não bateu)</resume-signal>
  <done>JORN-15, U2, 20, 22, 24, 06, D5, D-22 e a fila do JORN-26 estão provados no banco de produção, com as contas da jornada.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: sessão 2 — revisão, reabertura, D-23, redecisão e os avisos do titular</name>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md`
  </read_first>
  <action>O operador executa, com uma candidatura de teste em «Decisão final», três contas RH distintas (RH-A decide, RH2 revisa, RH3 redecide) e uma conta de titular de teste, os passos abaixo, na ordem.</action>
  <instructions>
(g) RH-A registra a decisão final «rejeitado» numa candidatura de teste em «Decisão final» (se nenhuma
estiver nessa etapa, avançar uma pelo fluxo normal antes — os e-mails de avanço vão para o alias de teste).
(h) Como a candidata, pedir a revisão (Art. 20) na página de explicação.
(i) RH2 responde «Reverter a decisão (reabrir a candidatura)». Conferir: o diálogo fala em reabrir com
prazo de 10 dias; na caixa da candidata, o e-mail diz «…sua candidatura foi reaberta e será decidida
novamente até DD/MM/AAAA» (data = hoje + 10); no painel dela, a linha «Candidatura reaberta — nova
decisão até DD/MM/AAAA» no lugar do «3 dias úteis»; o cartão «Entenda a decisão» sumiu.
(j) RH-A tenta registrar a nova decisão. Conferir: o formulário mostra que a nova decisão precisa ser
registrada por outra pessoa do RH, e nada muda.
(k) RH3 registra «aprovado». Conferir: a candidata recebe o e-mail de aprovação.
(l) Com uma conta de titular de teste, pedir a exclusão dos dados em «Privacidade» e, em seguida,
cancelar o pedido. Conferir na caixa: dois e-mails — o do pedido, com a data de execução e como
cancelar, e o do cancelamento; nenhum cita número de pedido; ambos têm o botão para a área de
privacidade.

(m) *(acrescentado pelo 48-19 — Defeitos 30/31, JORN-06; só depois do deploy da EF e do front do
48-19)* Com uma conta de teste levada pelo fluxo normal até «Avaliação assíncrona» (a `+claude1`
não serve: já enviou o Big Five e está na sessão 2), concluir e enviar o Big Five. Conferir no
painel: a devolutiva aparece; as **cinco** abas abrem, inclusive «Sensibilidade Emocional»; cada
página começa por «Pessoas com …» e não cita nome, número nem cargo. Na devolutiva já existente da
`+claude5`, as cinco abas também devem abrir (o texto dela continua o inventado — a linha é
evidência e não foi tocada).

Avisar aqui «sessão 2 feita» com as observações. Claude roda `p48_prova_prod.sql` e confere as `p2_*`
(e de novo as `p1_*`, que não podem ter regredido) — inclusive a `p2_devolutiva_template_oficial`
do passo (m).
  </instructions>
  <files>.planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md</files>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && T0=$(grep -m1 "^T0: " .planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md | cut -d' ' -f2) && D=$(mktemp -d) && { echo "SET TRANSACTION READ ONLY; SELECT set_config('p48.t0', '$T0', false);"; cat supabase/tests/p48_prova_prod.sql; } > "$D/prova.sql" && node p46apply.cjs run "$D/prova.sql" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=s.slice(s.indexOf('['));const r=JSON.parse(j)[0];const f=Object.entries(r).filter(([k,v])=>v!==true).map(([k])=>k);if(f.length){console.error('FASE NAO PROVADA EM PROD: '+f.join(', '));process.exit(1)}console.log('todas as provas true: '+Object.keys(r).length)})"</automated>
    <fails_when>non-zero exit with "FASE NAO PROVADA EM PROD: <provas>" — any false boolean (p1_ or p2_) names what is not true in production</fails_when>
    <human-check>
      <test>Diálogo do RH2, formulário do RH-A, caixa e painel da candidata, caixa do titular, conforme (i)–(l)</test>
      <expected>Cópia de reabertura com a data certa no e-mail e no painel; recusa D-23 clara na tela; dois avisos ao titular sem número de pedido e com o botão</expected>
      <why_human>três contas RH distintas e leitura de caixa/tela — o banco prova o estado e o envio, não o texto lido</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Todas as provas (`p1_*` e `p2_*`) saem `true`.
    - As conferências humanas de (i)–(l) estão registradas em `48-PROVA-PROD.md`, com a data exibida no e-mail e no painel.
    - Nenhum teardown nem flip de purga foi rodado; nenhum reset foi necessário (ou, se foi, está registrado com contagem antes/depois e confirmação do operador).
  </acceptance_criteria>
  <verification>Claude roda `p48_prova_prod.sql` com o T0 de `48-PROVA-PROD.md` (verify abaixo) e exige TODAS as provas (`p1_*` e `p2_*`) = true; registra o resultado e as observações em `48-PROVA-PROD.md`.</verification>
  <resume-signal>Digite «sessão 2 feita» com as observações (ou descreva o que não bateu)</resume-signal>
  <done>JORN-18, 19 (com D-01, D-10, D-23) e 27 estão provados em produção — e a fase inteira fecha com uma consulta que diz `true` para cada critério.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador/contas de teste → PROD | ações reais em produção, só em contas de teste |
| consultas de prova → PROD | só leitura |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-18-01 | Tampering | teardown ou flip da purga rodado por engano | high | mitigate | proibido no plano; nenhuma task referencia os arquivos; prova é só leitura |
| T-48-18-02 | Information Disclosure | ação de prova atingindo candidato real | medium | mitigate | contas `+claude` apenas; consultas filtradas pelo alias; inscrições novas no lugar de resets |
| T-48-18-03 | Repudiation | critério «provado» só pela tela | medium | mitigate | cada critério é booleano de consulta; `<human-check>` complementa, não substitui |
| T-48-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Prontidão (banco, bundles, front) verde antes das sessões.
- `p48_prova_prod.sql` com todas as provas `true` depois da sessão 2.
- `48-PROVA-PROD.md` com T0, resultados e conferências humanas.
</verification>

<success_criteria>
Os sete critérios de sucesso do ROADMAP da Phase 48 são `true` numa consulta contra produção, com as contas da jornada.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `supabase/tests/p48_prontidao_prod.sql` | sonda só leitura | **nova** |
| `supabase/tests/p48_prova_prod.sql` (GUC `p48.t0`) | consulta de prova só leitura | **nova** |
| `.planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md` | evidência | **nova** |

## Premissas sinalizadas (probe de borda)

As 13 linhas do relatório do probe (JORN-18 ×3 e as 10 `unclassified`) têm cada uma uma prova em
PROD aqui ou um smoke nos planos de origem. O vencimento dos 10 dias não é esperado em tempo real: o
alerta está provado pelo `p48_prazo_reabertura_smoke` (48-13) e fica como verificação diferida
natural — o primeiro alerta real sai 10 dias depois da reabertura da sessão 2 e pode ser conferido
por `select alerta_prazo_enviado_em from decisao_final …` se a nova decisão não tivesse sido
registrada (na prova, RH3 decide antes, e o alerta corretamente não sai).

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-18-SUMMARY.md` when done
</output>
