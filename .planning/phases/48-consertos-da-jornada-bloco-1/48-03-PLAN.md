---
phase: 48-consertos-da-jornada-bloco-1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260921000003_p48_local_ou_link_valida.sql
  - supabase/tests/p48_local_ou_link_smoke.sql
  - supabase/tests/seg33_agendamento_smokes.sql
  - src/lib/url/isSafeHttpUrl.ts
  - src/lib/url/__tests__/isSafeHttpUrl.test.ts
  - src/features/agendamento/components/AgendamentoCandidatoCard.tsx
  - src/features/agendamento/schemas/agendamentoSchema.ts
  - src/features/agendamento/schemas/__tests__/agendamentoSchema.test.ts
  - src/features/agendamento/components/__tests__/AgendamentoBlock.test.tsx
autonomous: true
requirements: [JORN-D5]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`local_ou_link` é obrigatório (texto não vazio depois de `btrim`) nas duas modalidades, e exigido como URL http(s) com host SÓ em `tipo='online'` — validado na escrita pelo banco (trigger) e pelo formulário (Zod) (D-05)."
    - "Um INSERT de agendamento `online` com `local_ou_link='dddd'` é recusado com `check_violation` em PROD (o link inválido não entra)."
    - "BORDA (explicit): a linha legada `online`/`dddd` (conta de teste, `reagendada`) continua cancelável e atualizável em outras colunas (`status`, `compareceu`, `data_hora`) — o trigger só age no INSERT ou quando `local_ou_link`/`tipo` MUDAM. Nenhum UPDATE retroativo nela."
    - "BORDA (explicit): um endereço de entrevista presencial (texto com dígitos e vírgula, sem esquema) é aceito; vazio, só espaços e NULL são recusados nas duas modalidades; `javascript:` e `data:` são recusados no online."
    - "`isSafeHttpUrl` passa a morar em `src/lib/url/isSafeHttpUrl.ts` (export nomeado) e é a MESMA função usada pelo card do candidato (renderização) e pelo schema Zod (escrita)."
    - "O `.ics` continua pondo não-URL em LOCATION (não foi tocado)."
    - "NÃO há `CHECK … NOT VALID` — um CHECK é reavaliado em qualquer UPDATE da linha e quebraria cancelar a linha `dddd`."
    - "Migration aplicada por `p46apply.cjs` com md5 conferido (D-16); varredura de portões rodada antes (D-17); `tsc` ≤ 90 (D-15); front publicado com `origin/main..HEAD` vazio."
  artifacts:
    - path: supabase/migrations/20260921000003_p48_local_ou_link_valida.sql
      provides: "função de trigger + 2 triggers BEFORE em agendamentos_entrevista"
      contains: "validar_local_ou_link_agendamento"
    - path: supabase/tests/p48_local_ou_link_smoke.sql
      provides: "recusa do inválido, aceite do endereço, linha legada cancelável"
    - path: src/lib/url/isSafeHttpUrl.ts
      provides: "validação única de URL http(s)"
      exports: ["isSafeHttpUrl"]
  key_links:
    - from: "src/features/agendamento/schemas/agendamentoSchema.ts"
      to: "src/lib/url/isSafeHttpUrl.ts"
      via: "superRefine no online"
      pattern: "isSafeHttpUrl"
    - from: "src/features/agendamento/components/AgendamentoCandidatoCard.tsx"
      to: "src/lib/url/isSafeHttpUrl.ts"
      via: "import (a função local sai do card)"
      pattern: "from '@/lib/url/isSafeHttpUrl'"
  prohibitions:
    - requirement_id: JORN-D5
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT exigir URL de entrevista presencial — o mesmo campo guarda o ENDEREÇO, e exigir link quebraria o agendamento presencial"
    - requirement_id: JORN-D5
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT reescrever retroativamente a linha legada `dddd` nem torná-la impossível de cancelar"
---

<objective>
Tornar `local_ou_link` obrigatório e validado na escrita — a única parte antecipada do D5 (D-05). O
redesenho do agendamento (janelas, escolha, confirmação — PP-8) é fase própria, depois do Bloco 2.

Purpose: `isSafeHttpUrl` já impediu o `dddd` de virar link clicável na renderização
(`AgendamentoCandidatoCard.tsx:210`), mas nada impede o link inválido de ENTRAR. A escrita é PostgREST
direto sob RLS (`agendamentoService.ts:163,191`), sem RPC no meio — então a camada servidor é um
trigger, e a camada cliente é o Zod.

Output: migration com trigger aplicada, smoke novo, fixture do `seg33` corrigida (ela insere `online`
sem link), util compartilhado, schema Zod; publicado.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer) [BLOCKING]: o trigger de validação no banco — aplicado e provado em PROD, com a linha legada intacta</name>
  <files>supabase/migrations/20260921000003_p48_local_ou_link_valida.sql, supabase/tests/p48_local_ou_link_smoke.sql, supabase/tests/seg33_agendamento_smokes.sql</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-RESEARCH.md` §H (o campo guarda endereço; por que NÃO `CHECK NOT VALID`)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §1 e §7
    - `supabase/migrations/20260906000004_reagendamento_notifica_e_reseta_comparecimento.sql` (`:77-108` — BEFORE UPDATE OF + WHEN por tupla; `:113-117` portão final)
    - `supabase/migrations/20260709000010_guard_rejeicao_auditada.sql` (`:40-43` ordem alfabética dos BEFORE triggers; `:53-99` RAISE com `check_violation`)
    - `supabase/tests/seg33_agendamento_smokes.sql` (`:105-125`, `:140-155`, `:210-220` — INSERTs `online` SEM `local_ou_link`)
    - `supabase/tests/p43_matriz_retencao_smoke.sql` (contador `pass`, subtransação que reverte)
  </read_first>
  <action>
Medir antes, só leitura: `select id, tipo, status, length(local_ou_link), local_ou_link ~* '^https?://' from public.agendamentos_entrevista` (esperado: 2 linhas — `presencial` com endereço de 57 caracteres; `online` com `dddd`) e os triggers vivos da tabela (`select tgname from pg_trigger where tgrelid='public.agendamentos_entrevista'::regclass and not tgisinternal order by tgname`). Registrar no SUMMARY. Rodar a varredura de portões (D-17) e conferir achados que citem `agendamentos_entrevista` ou `tgname`.

Escrever `supabase/migrations/20260921000003_p48_local_ou_link_valida.sql` (cabeçalho do M8, transporte `p46apply.cjs migrate`, sem `BEGIN;` externo):
- `CREATE OR REPLACE FUNCTION public.validar_local_ou_link_agendamento() RETURNS trigger LANGUAGE plpgsql SET search_path = ''`: recusa com `RAISE EXCEPTION … USING ERRCODE = 'check_violation'` quando `btrim(coalesce(NEW.local_ou_link,''))` é vazio (mensagem: local ou link obrigatório); e, quando `NEW.tipo = 'online'`, recusa se o valor aparado não casar com `^https?://[^[:space:]/?#]+` (esquema http/https + host — espelha `isSafeHttpUrl`: parseável e protocolo http/https). Presencial aceita qualquer texto não vazio. Retorna NEW.
- `REVOKE ALL ON FUNCTION … FROM PUBLIC; REVOKE ALL … FROM anon;`
- dois triggers: `trg_agendamento_valida_local_ins` BEFORE INSERT FOR EACH ROW; e `trg_agendamento_valida_local_upd` BEFORE UPDATE OF `local_ou_link`, `tipo` FOR EACH ROW WHEN `((OLD.local_ou_link, OLD.tipo) IS DISTINCT FROM (NEW.local_ou_link, NEW.tipo))`. `DROP TRIGGER IF EXISTS` antes de cada CREATE. Comentar a ordem alfabética: `trg_agendamento_valida_*` roda depois de `trg_agendamento_normaliza_vaga` e `trg_agendamento_reagendado_reset`.
- `COMMENT ON FUNCTION` explicando: obrigatório nos dois, URL só no online (o campo guarda endereço no presencial — medido), por que trigger e não `CHECK NOT VALID` (a linha legada `dddd` tem de continuar cancelável sem UPDATE retroativo).
- `DO` final de auto-verificação por catálogo: os dois triggers existem e apontam para a função; a definição do trigger de UPDATE contém `IS DISTINCT FROM`.

Corrigir `supabase/tests/seg33_agendamento_smokes.sql`: todo INSERT `online` passa a levar um `local_ou_link` http(s) de fixture (ex.: `https://meet.example.com/smoke33`), com comentário citando esta migration. E fechar o ponto cego que a recusa nova exporia: hoje, quando a fixture não monta, o arquivo marca `smoke.ready='n'` e cada asserção só emite um NOTICE de `SKIP` — e NOTICE não volta pela Management API, então um smoke inteiro pulado sai «verde». Acrescentar ao FIM do arquivo um bloco que levanta `RAISE EXCEPTION 'SEG-33 FAIL: a fixture não montou — as asserções foram PULADAS'` quando `current_setting('smoke.ready', true) IS DISTINCT FROM 'y'`, seguido de um `SELECT` final que devolve `{smoke:'seg33', ready:<valor>}`. Nada mais muda no seg33.

Criar `supabase/tests/p48_local_ou_link_smoke.sql` (contador, gate final, SELECT final com o total). Numa subtransação que reverte (o `trg_notif_convite` do INSERT/UPDATE só sai depois do COMMIT — a reversão não manda convite), com claim de `administrador`, sobre uma candidatura real de conta de teste: (a) INSERT `online` com `dddd` → `check_violation`; (b) `online` com `https://meet.google.com/abc-defg-hij` → aceito; (c) `presencial` com endereço → aceito; (d) `presencial` com `''`, `'   '` e NULL → `check_violation`; (e) `online` NULL → `check_violation`; (f) `online` com `javascript:alert(1)` → `check_violation`; (g) na linha legada `dddd` (localizada por `tipo='online' and local_ou_link='dddd'`): UPDATE `status='cancelada'` aceito, UPDATE `compareceu` aceito, UPDATE `data_hora` aceito; (h) UPDATE da mesma linha para `local_ou_link='eeee'` → `check_violation`; (i) NEGATIVA — contagem e impressão digital (`md5` do `to_jsonb` das linhas) de `agendamentos_entrevista` idênticas antes e depois do smoke.

Aplicar e rodar: `node p46apply.cjs migrate supabase/migrations/20260921000003_p48_local_ou_link_valida.sql`, depois `node p46apply.cjs run supabase/tests/p48_local_ou_link_smoke.sql` e `node p46apply.cjs run supabase/tests/seg33_agendamento_smokes.sql` (com o bloco final novo, um seg33 pulado passa a REPROVAR em vez de sair verde).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260921000003_p48_local_ou_link_valida.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');if(/ADD\s+CONSTRAINT[^;]*NOT\s+VALID/i.test(c))throw new Error('CHECK NOT VALID proibido (quebra cancelar a linha dddd)');for(const t of ['validar_local_ou_link_agendamento','trg_agendamento_valida_local_ins','trg_agendamento_valida_local_upd','BEFORE INSERT','BEFORE UPDATE OF local_ou_link, tipo','IS DISTINCT FROM','check_violation',\"'online'\",'https?://','REVOKE ALL'])if(!c.includes(t))throw new Error('falta: '+t);const g=fs.readFileSync('supabase/tests/seg33_agendamento_smokes.sql','utf8');if(!/SEG-33 FAIL: a fixture n/.test(g))throw new Error('seg33 sem o bloco final que reprova o SKIP silencioso');console.log('OK')"</automated>
    <fails_when>non-zero exit with "falta:", "wrapper BEGIN proibido", "CHECK NOT VALID proibido" or "seg33 sem o bloco final" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs migrate supabase/migrations/20260921000003_p48_local_ou_link_valida.sql && node p46apply.cjs run supabase/tests/p48_local_ou_link_smoke.sql && node p46apply.cjs run supabase/tests/seg33_agendamento_smokes.sql</automated>
    <fails_when>non-zero exit; "md5 do ledger BATE" absent from the migrate output; "FAIL" in any output; or the seg33 final row does not read ready "y"</fails_when>
  </verify>
  <acceptance_criteria>
    - Em PROD, `pg_trigger` mostra `trg_agendamento_valida_local_ins` e `trg_agendamento_valida_local_upd` em `agendamentos_entrevista`.
    - `p48_local_ou_link_smoke.sql` passa com (a)–(i); a (g) prova a linha legada cancelável; a (i) prova zero resíduo.
    - `seg33_agendamento_smokes.sql` passa e o SELECT final devolve `ready = y` (o bloco novo reprova o SKIP silencioso).
    - A linha legada `dddd` continua com `local_ou_link='dddd'` depois de tudo (nenhum UPDATE retroativo).
  </acceptance_criteria>
  <done>Link inválido não entra mais pelo banco, o endereço presencial entra, e a linha legada segue cancelável.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: a mesma regra no formulário — util compartilhado e Zod</name>
  <files>src/lib/url/isSafeHttpUrl.ts, src/lib/url/__tests__/isSafeHttpUrl.test.ts, src/features/agendamento/components/AgendamentoCandidatoCard.tsx, src/features/agendamento/schemas/agendamentoSchema.ts, src/features/agendamento/schemas/__tests__/agendamentoSchema.test.ts</files>
  <read_first>
    - `src/features/agendamento/components/AgendamentoCandidatoCard.tsx` (`:62-76` a função a mover; `:210` o uso)
    - `src/features/agendamento/schemas/agendamentoSchema.ts` (`:18-44` `MSG` e o `z.object`)
    - `src/features/agendamento/components/AgendamentoBlock.tsx` (`:190-240` normalização vazio→null e `:290-300` o campo e o rótulo por modalidade)
    - `src/features/agendamento/components/__tests__/AgendamentoBlock.test.tsx` (submissões `online` sem link quebrariam com o schema novo)
    - `src/features/cadastro/schemas/candidatoSchema.ts` (precedente de `superRefine`)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-PATTERNS.md` §15, §16
  </read_first>
  <behavior>
    - `isSafeHttpUrl('https://meet.google.com/x')` → true; `('http://a.b')` → true; `('dddd')` → false; `('javascript:alert(1)')` → false; `('')` → false; `('  https://a.b  ')` → true
    - schema: `online` + `https://meet.google.com/abc` → válido; `online` + `dddd` → erro em `local_ou_link`; `online` + vazio → erro; `presencial` + `Av. Paulista, 1000` → válido; `presencial` + `'   '` → erro
  </behavior>
  <action>
Criar `src/lib/url/isSafeHttpUrl.ts` movendo a função VERBATIM do card, exportada nomeada, com docblock `@module` e `@see` para o card (renderização, WR-01) e para o schema (escrita). Teste em `src/lib/url/__tests__/isSafeHttpUrl.test.ts` com os casos de `<behavior>`. `AgendamentoCandidatoCard.tsx` passa a importar de `@/lib/url/isSafeHttpUrl` e perde a cópia local (o uso de `:210` não muda).

`agendamentoSchema.ts`: acrescentar a `MSG` três mensagens pt-BR — `localObrigatorio: 'Informe o local da entrevista.'`, `linkObrigatorio: 'Informe o link da videochamada.'`, `linkInvalido: 'Informe um link válido, começando com http:// ou https://.'` — e um `superRefine` no `z.object` que: com `tipo='presencial'`, exige `local_ou_link` aparado não vazio (issue em `['local_ou_link']` com `localObrigatorio`); com `tipo='online'`, exige não vazio (`linkObrigatorio`) e `isSafeHttpUrl` (`linkInvalido`). Atualizar o docblock do schema (os «Validation gates» agora incluem o local/link, e citar o trigger `validar_local_ou_link_agendamento` como a camada servidor). Teste novo em `src/features/agendamento/schemas/__tests__/agendamentoSchema.test.ts` com os casos de `<behavior>`.

Se `AgendamentoBlock.test.tsx` submeter `online` sem link ou com link inválido, ajustar a fixture do teste para um link válido (o comportamento do componente não muda). Rodar a suíte da pasta.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/lib/url src/features/agendamento</automated>
    <fails_when>non-zero exit, "failed" in the summary, or zero tests reported for src/lib/url</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && ! grep -n "^function isSafeHttpUrl" src/features/agendamento/components/AgendamentoCandidatoCard.tsx && grep -q "from '@/lib/url/isSafeHttpUrl'" src/features/agendamento/components/AgendamentoCandidatoCard.tsx && grep -q "isSafeHttpUrl" src/features/agendamento/schemas/agendamentoSchema.ts && echo OK</automated>
    <fails_when>non-zero exit (the local copy still exists in the card, or the card/schema do not import the shared util)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }</automated>
    <fails_when>non-zero exit — lint did not run (exit code other than 0 or 2), exit code and error count disagree, or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/lib/url/isSafeHttpUrl.ts` exporta `isSafeHttpUrl` (nomeado); o card não tem mais cópia local.
    - `agendamentoSchema.ts` contém as três mensagens novas e um `superRefine` que usa `isSafeHttpUrl`.
    - Testes do util e do schema verdes com todos os casos de `<behavior>`; `AgendamentoBlock.test.tsx` e `AgendamentoCandidatoCard.test.tsx` verdes.
    - `tsc errors` ≤ 90.
  </acceptance_criteria>
  <done>O RH não consegue salvar um agendamento online sem link válido nem um presencial sem local — e a tela diz o que falta.</done>
</task>

<task type="auto">
  <name>Task 3: publicar — build, push e marcador no ar</name>
  <files>src/features/agendamento/schemas/agendamentoSchema.ts</files>
  <read_first>
    - `CLAUDE.md` §«Esta via NÃO passa pelo git»
  </read_first>
  <action>
Rodar `npm run test:run` e `npm run build`; conferir em qual chunk a mensagem mora (`grep -rl "http:// ou https://" build/assets/` — o agendamento é tela do RH, chunk lazy). Commitar, `git push origin main` (deploy corretivo — D-18), aguardar a Vercel (reconsultar a cada ~30 s até ~10 min) e provar que o marcador está servido em PROD. Nenhuma mudança de código nesta task.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npm run test:run >/dev/null && npm run build >/dev/null && grep -rl "http:// ou https://" build/assets/</automated>
    <fails_when>non-zero exit; "failed" in the vitest run; or grep prints no file</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && node -e "const M=process.argv[1],B='https://rh.beautysmile.com.br';(async()=>{const vis=new Set(),fila=['/'];let ok=false;while(fila.length&&vis.size<300){const p=fila.shift();if(vis.has(p))continue;vis.add(p);let t='';try{t=await (await fetch(B+p)).text()}catch{continue}if(t.includes(M)){ok=true;break}for(const m of t.matchAll(/(?:\/assets\/|\.\/)([\w.-]+\.js)/g))fila.push('/assets/'+m[1])}if(!ok){console.error('AUSENTE em PROD: '+M);process.exit(1)}console.log('PRESENTE em PROD: '+M)})()" "http:// ou https://"</automated>
    <fails_when>non-zero exit; git log origin/main..HEAD printed a commit; or "AUSENTE em PROD" in stderr</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run test:run` verde; o build contém o marcador.
    - `git log --oneline origin/main..HEAD` vazio; o crawler imprime `PRESENTE em PROD`.
  </acceptance_criteria>
  <done>A validação do formulário está no ar, não só no disco.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do RH → PostgREST `agendamentos_entrevista` (RLS `rh_gerencia_agendamento`) | escrita direta, sem RPC — o trigger é a única camada servidor |
| `local_ou_link` → navegador do candidato (link clicável) e `.ics` | dado escrito pelo RH chega ao candidato |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-03-01 | Tampering | link `javascript:`/`data:` no agendamento online | high | mitigate | trigger BEFORE INSERT/UPDATE exige `^https?://host`; Zod com `isSafeHttpUrl`; renderização mantém `isSafeHttpUrl` (defesa em profundidade) |
| T-48-03-02 | Denial of Service | CHECK reavaliado bloqueando cancelar a linha legada | medium | mitigate | trigger com WHEN só em mudança de link/tipo; smoke (g) prova cancelar/compareceu/data_hora |
| T-48-03-03 | Tampering | burlar o Zod chamando PostgREST direto | medium | mitigate | o trigger vale para qualquer cliente |
| T-48-03-04 | Repudiation | smoke do seg33 pulando em silêncio | medium | mitigate | bloco final do seg33 levanta exceção quando a fixture não montou (NOTICE não volta pela Management API) |
| T-48-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Migration aplicada (md5 BATE); `p48_local_ou_link_smoke` e `seg33_agendamento_smokes` verdes (seg33 com `ready = y`).
- Vitest da pasta + suíte inteira verdes; `tsc` ≤ 90.
- Marcador em PROD; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
Link inválido não entra por nenhum caminho, o endereço presencial continua entrando, e nenhuma linha existente foi reescrita.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `public.validar_local_ou_link_agendamento()` | função de trigger | **nova** |
| `trg_agendamento_valida_local_ins`, `trg_agendamento_valida_local_upd` | triggers BEFORE em `agendamentos_entrevista` | **novos** |
| `supabase/migrations/20260921000003_p48_local_ou_link_valida.sql` | migration | **nova** |
| `supabase/tests/p48_local_ou_link_smoke.sql` | smoke SQL | **novo** |
| `supabase/tests/seg33_agendamento_smokes.sql` | smoke SQL | editado (fixtures `online` com link) |
| `src/lib/url/isSafeHttpUrl.ts` → `isSafeHttpUrl` | util TS | **novo** (movido do card) |
| `agendamentoSchema` (`superRefine`, 3 mensagens) | schema Zod | editado |
| `src/features/agendamento/schemas/__tests__/agendamentoSchema.test.ts` | teste | **novo** |

## Premissas sinalizadas (probe de borda)

JORN-D5 foi classificado `unclassified` pelo probe; as bordas `explicit` (linha legada cancelável,
endereço aceito, vazio recusado) vêm do RESEARCH §H e da medição das 2 linhas vivas.

<output>
Create `.planning/phases/48-consertos-da-jornada-bloco-1/48-03-SUMMARY.md` when done
</output>
