---
phase: 45-motor-de-exclus-o-anonimiza-o
plan: 11
type: execute
wave: 6
depends_on: ["45-04", "45-05", "45-06", "45-07", "45-08", "45-09", "45-10", "45-12"]
files_modified:
  - .planning/phases/45-motor-de-exclus-o-anonimiza-o/45-11-EVIDENCIA-PORTAO.md
  - supabase/tests/p45_motor_exclusao_smoke.sql
autonomous: false
requirements: [ERASE-01, ERASE-02, ERASE-03, ERASE-04, ERASE-05, ERASE-06, ERASE-07, ERASE-08, ERASE-09, ERASE-10]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O code review bloqueante rodou e fechou **ANTES** do primeiro `apply_migration` destrutivo — não depois. A origem desta regra é a P39: fechou sem VERIFICATION.md e sem code review, e 2 defeitos CRÍTICOS chegaram a produção."
    - "O dry-run foi exercitado pela **MESMA query** do delete real: `anonimizar_candidato(..., p_dry_run := true)` executa o mesmo corpo e termina em `RAISE EXCEPTION`, e o smoke pina `md5(prosrc)` das duas funções e assere que o chamador CONTÉM a chamada. Um segundo corpo reprova o gate."
    - "Zero commit desta fase usou `--no-verify`. O `.husky/pre-commit` é gate de NÃO-REGRESSÃO contra a baseline congelada de 97 desde a Phase 42, então a exigência é satisfazível honestamente."
    - "ASSERÇÃO NEGATIVA medida em PROD antes e depois: as 3 FKs `NO ACTION` continuam `confdeltype='a'`, e as contagens de `historico_candidatura`, `decisao_final` e `decisao_final_historico` são idênticas às de antes da execução real."
    - "ASSERÇÃO NEGATIVA medida em PROD: zero linha de `candidatos` com `user_id` que exista em `auth.users`, e zero `historico_candidatura.ator` apontando ao titular executado."
    - "ASSERÇÃO NEGATIVA medida em PROD: nenhuma linha nova em `notificacoes_enviadas` com `evento='decisao'` para as candidaturas encerradas, e nenhuma com `auto_rejeitado = true` em `historico_candidatura`."
    - "ASSERÇÃO NEGATIVA medida em PROD: nenhuma linha em `notificacoes_enviadas` para o recibo ao titular (D-45-12 / R1)."
    - "O apply seguiu a ordem obrigatória: `p45_bias_k5` ANTES de `p45_anonimizar_candidato` (a faixa etária precisa existir para ser materializada — restrição de ordenação nº 1 do ROADMAP); `p45_sever_user_id` ANTES de qualquer execução que chame `deleteUser`; e a EF `notificar-rh` deployada ANTES do trigger de `p45_retirada_e_evento` (`net.http_post` é at-most-once)."
    - "O ledger `supabase_migrations.schema_migrations` foi reconciliado após CADA apply, e `md5(statements[1])` de cada version bate o md5 computado do arquivo do repositório."
    - "`supabase/tests/p45_motor_exclusao_smoke.sql` roda verde em PROD numa ÚNICA chamada, com o contador batendo o número FIXO do cabeçalho, e os marcadores `PENDENTE-45-07` da proveniência do md5 foram substituídos por valores reais com a data e o comando de recomputação."
    - "A execução real foi feita sobre uma conta descartável criada para isso, nunca sobre conta de pessoa real, e o estado final foi medido em PROD nos três sistemas: zero objeto sob o prefixo, tombstone completo, usuário do Auth inexistente."
    - "`45-VERIFICATION.md` existe com **veredito** — nunca ausente, nunca `status: draft`."
  artifacts:
    - .planning/phases/45-motor-de-exclus-o-anonimiza-o/45-11-EVIDENCIA-PORTAO.md
  key_links:
    - "os 5 itens do portão destrutivo do M8 → condição de fechamento da fase, não conselho: `VERIFICATION.md` com veredito · code review bloqueante ANTES do apply · asserções negativas · zero `--no-verify` · dry-run pela MESMA query"
    - "`45-SONDAS-PROD.md` (45-01) → as contagens ANTES contra as quais as asserções negativas deste plano são medidas"
    - "G1 e G2 da Phase 44 → pré-condição declarada deste portão (`STATE.md`: «G1 e G2 devem fechar antes desse portão»)"
  prohibitions:
    - "MUST NOT executar qualquer apply destrutivo, dry-run destrutivo ou execução real sobre a conta, o currículo ou as candidaturas de uma pessoa real. A conta da execução real é criada pela própria execução, para ela, e o estado final dela é o que se mede."
    - "MUST NOT relaxar nenhuma das 3 FKs `NO ACTION` para CASCADE diante de um 23503. O 23503 é agora um desfecho ESPERADO, não uma surpresa — a saída é anonimizar a linha filha e severar o ponteiro."
---

<objective>
**O PORTÃO DE FASE DESTRUTIVA.** Os cinco itens que o ROADMAP declara como *exit criterion*, na
ordem em que só eles fazem sentido: review antes do apply, dry-run pela mesma query antes da
execução, asserções negativas antes e depois, zero bypass do hook, e um `VERIFICATION.md` com
veredito.

Purpose: com o PITR desligado por decisão datada (D-45-10) e o backup do Supabase cobrindo 7 dias
**excluindo Storage inteiramente**, um CV apagado por engano é **irrecuperável por qualquer meio**.
Não existe segunda rede. É por isso que o dry-run deixou de ser processo e virou **o mecanismo de
segurança**, e é por isso que este plano existe como plano — e não como uma etapa dentro de outro.

A origem da regra não é teórica: a **P39, a fase de maior risco do M7, fechou sem `VERIFICATION.md`
e sem code review, e 2 defeitos CRÍTICOS chegaram a produção.** Cada camada de verificação aplicada
depois achou algo que a anterior não pegou.

⚠ **Plano 100% checkpoint do orquestrador.** Nenhum código é escrito. Todo apply, todo deploy e toda
inspeção passam pelos tools MCP do Supabase, que subagentes GSD não recebem.

## Pré-condição declarada, herdada da Phase 44

O `STATE.md` escreve que **G1 e G2 devem fechar antes deste portão**. Se `45-SONDAS-PROD.md` § "Pré-
condições do portão destrutivo" registrar qualquer uma como bloqueada, **este plano não abre** — a
Phase 45 consumiria como plano de exclusão irreversível um inventário que nunca foi exercido ponta a
ponta, e a EF `exportar-meus-dados` continuaria em PROD na v1 com um cooldown que falha ABERTO.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-SONDAS-PROD.md
@.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Code review BLOQUEANTE + dry-run pela MESMA query — os dois antes de qualquer apply</name>
  <precondition>`45-SONDAS-PROD.md` § "Pré-condições do portão destrutivo (G1/G2)" registra as duas como FECHADAS. Se qualquer uma estiver bloqueada, este plano não prossegue — é a pré-condição que o `STATE.md` declara para o portão destrutivo da Phase 45.</precondition>
  <files>.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-11-EVIDENCIA-PORTAO.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § "Portão de fase destrutiva (exit criterion de ROADMAP, não conselho)" (os 5 itens e o precedente da P39)
    - `.planning/STATE.md` § "⚠ Phase 44 — `gaps_found` diferido" (G1/G2 como pré-condição deste portão)
    - `supabase/migrations/20260805000004_p45_sever_user_id.sql`, `20260805000005_p45_plano_e_dry_run.sql`, `20260805000006_p45_anonimizar_candidato.sql` (os três arquivos INTEIROS — são o objeto do review)
    - `supabase/functions/executar-direito-titular/index.ts` e `helpers.ts` (o objeto do review do lado da EF)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-07-SUMMARY.md` (os md5 computados dos arquivos e a varredura de consumidores non-null de `user_id`)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-SONDAS-PROD.md` (as contagens ANTES e o corpo de erro real do `deleteUser`)
  </read_first>
  <what-built>
Nada de código. Os dois primeiros dos cinco itens do portão de fase destrutiva do M8: o code review
bloqueante e o dry-run pela MESMA query — ambos **antes** de qualquer `apply_migration` destrutivo.

⚠ **Isto é trabalho do orquestrador, não de executor autônomo.** Subagentes GSD não recebem os
tools MCP do Supabase (bug upstream anthropics/claude-code#13898), e um executor sem eles não falha
alto: ele pode **reportar como executado um dry-run que nunca rodou**. Com o PITR desligado por
decisão datada (D-45-10) e o backup de 7 dias **excluindo Storage inteiramente**, o dry-run é a
**única rede desta fase** — um CV apagado por engano é irrecuperável por qualquer meio. Um dry-run
fabricado não é um relatório impreciso: é a remoção silenciosa da única proteção que existe antes
de uma mutação irreversível sobre PII viva.
  </what-built>
  <how-to-verify>
Orquestrador. Dois portões, nesta ordem, e **nenhum apply destrutivo antes dos dois**.

**1 · `/gsd-code-review 45`, bloqueante.** O escopo mínimo, e cada item tem um defeito nomeado que
ele existe para pegar:
- `p45_sever_user_id.sql` — **candidato nº 1**: DDL destrutiva sobre tabela viva, sem precedente
  interno. Conferir que o `conname` dropado é o medido na SONDA 4, que a FK volta como `SET NULL`, e
  que o `DO` block prova que o UNIQUE aceita múltiplos NULLs.
- `p45_anonimizar_candidato.sql` — conferir que **cada** sentinela corresponde a uma restrição
  registrada na SONDA 1 (Pitfall 1: um marcador em prosa numa coluna com CHECK de formato aborta a
  transação inteira, e o Storage já foi apagado); que a faixa etária é materializada ANTES da
  sentinela de `data_nascimento`; que as cinco severações são statements explícitos; que os dois
  `inet NOT NULL` são mascarados e não anulados; e que o `p_dry_run` termina o MESMO corpo.
- `executar-direito-titular/index.ts` — conferir que o erro de `deleteUser` **não** é engolido, que
  o passo 3 recusa sem `postgres_concluido_em`, que o retorno de `remove()` é verificado, e que o
  `list()` é paginado.
- Repo-wide — conferir que nenhuma das 3 FKs `NO ACTION` foi tocada e que não existe instrução SQL
  de remoção direta sobre a tabela de objetos do Storage.
- `git log` da fase — conferir **zero** `--no-verify`.

Registrar o veredito do review em `45-11-EVIDENCIA-PORTAO.md`. **Blocker aberto = portão fechado.**

**2 · O dry-run, pela MESMA query.** Numa subtransação com `ROLLBACK`, contra uma linha real de
`candidatos`:
- Chamar `anonimizar_candidato(<id>, p_dry_run := true)` e registrar o SQLSTATE devolvido — tem de
  ser o `ERRCODE` combinado de dry-run, **nunca** outro. Um erro real disfarçado de sucesso de
  dry-run é o pior falso verde desta fase.
- Medir, imediatamente depois: **zero coluna mudou** naquela linha. Delta esperado ≡ 0.
- Chamar `plano_exclusao_titular(<id>)` e transcrever o `jsonb` — é o relatório do dry-run, e ele
  sai da mesma expressão que a execução real consome.
- Conferir `md5(prosrc)` das duas funções contra os md5 computados registrados no
  `45-07-SUMMARY.md`, e conferir por `pg_get_functiondef` que `anonimizar_candidato` **CONTÉM** a
  chamada a `plano_exclusao_titular`.

⚠ **Ordem lógica que não pode inverter:** o dry-run precisa das funções aplicadas, e o apply
destrutivo precisa do dry-run. A saída é aplicar `p45_plano_e_dry_run` e `p45_anonimizar_candidato`
— que são **criação de função, não mutação de dado** — na Task 2, e rodar o dry-run **antes de
qualquer chamada com `p_dry_run := false`**. O `p45_sever_user_id`, que é o único apply
verdadeiramente destrutivo de schema, só entra **depois** do review desta task.

Transcrever tudo em `45-11-EVIDENCIA-PORTAO.md`.

**Conferência automática, depois de escrever o documento** (o mesmo gate que existia como
gate automatizado antes desta task virar checkpoint — inclui o item 4 do portão, zero
`--no-verify` nos commits da fase; rodar e transcrever a saída):

`test -f .planning/phases/45-motor-de-exclus-o-anonimiza-o/45-11-EVIDENCIA-PORTAO.md &amp;&amp; for t in "code review" "dry_run" "md5" "delta"; do grep -qi "$t" .planning/phases/45-motor-de-exclus-o-anonimiza-o/45-11-EVIDENCIA-PORTAO.md || { echo "FALTA: $t"; exit 1; }; done; N=$(git log --format=%B $(git merge-base HEAD main)..HEAD 2>/dev/null | grep -ci "no-verify" || true); if [ "$N" != "0" ]; then echo "commits da fase mencionam bypass do hook: $N"; exit 1; fi; echo OK`
  </how-to-verify>
  <acceptance_criteria>
    - `45-11-EVIDENCIA-PORTAO.md` registra o veredito do `/gsd-code-review 45` com data, e **zero blocker aberto**.
    - O documento registra o SQLSTATE devolvido pelo dry-run e afirma que é o `ERRCODE` combinado, não outro.
    - O documento registra o delta medido do dry-run: **zero coluna alterada** na linha exercitada.
    - O documento transcreve o `jsonb` devolvido por `plano_exclusao_titular`.
    - O documento registra o `md5(prosrc)` vivo das duas funções e a comparação contra os md5 computados no `45-07-SUMMARY.md`.
    - O documento afirma que `pg_get_functiondef(anonimizar_candidato)` contém a chamada a `plano_exclusao_titular`.
    - `git log` da fase tem **zero** menção a bypass do hook de pre-commit.
    - Nenhum `apply_migration` de `p45_sever_user_id` aconteceu antes desta task fechar.
    - A conferência automática saiu `OK` — os quatro tokens presentes e zero menção a bypass do hook — com a saída transcrita.
  </acceptance_criteria>
  <resume-signal>Digite "review e dry-run fechados" com o veredito do `/gsd-code-review 45` (zero blocker aberto), o SQLSTATE devolvido pelo dry-run e o delta medido — ou descreva o que reprovou. O código destrutivo tem de ter sido lido por alguém antes de rodar, e o dry-run tem de ter provado — pela mesma expressão que o delete real usa — que ele não muta nada quando não deve. Blocker aberto ou delta diferente de zero = portão fechado, e a Task 2 não começa.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Apply na ordem obrigatória + deploy das EFs + smoke verde em PROD</name>
  <files>.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-11-EVIDENCIA-PORTAO.md, supabase/tests/p45_motor_exclusao_smoke.sql</files>
  <read_first>
    - `supabase/tests/p45_motor_exclusao_smoke.sql` e `supabase/tests/p45_bias_k5_smoke.sql` (os dois arquivos INTEIROS — o cabeçalho diz como rodar e qual é o contador FIXO)
    - Os cabeçalhos das cinco migrations desta fase (cada um carrega o comando de reparo do ledger e a conferência `md5(statements[1])`)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-09-SUMMARY.md` § ordem obrigatória (EF `notificar-rh` ANTES do trigger)
    - `.planning/STATE.md` § Blockers/Concerns (o checkpoint 42-07: `NOTIFICACOES_MODO` é `producao` em PROD e um smoke de e-mail manda e-mail real aos RH)
    - `CLAUDE.md` § "Migrations + db push — workaround conhecido"
  </read_first>
  <what-built>
Cinco migrations e duas Edge Functions, todas revisadas e nenhuma aplicada. A ordem de apply não é
preferência: três dependências são estruturais e uma violação delas produz dano irreversível ou
silêncio.
  </what-built>
  <how-to-verify>
**Ordem obrigatória de apply/deploy — as quatro dependências e o porquê de cada uma:**

1. `20260805000003_p45_bias_k5.sql` — **ANTES** de `p45_anonimizar_candidato`. A coluna
   `faixa_etaria_materializada` precisa existir para o tombstone poder preenchê-la; é a restrição de
   ordenação nº 1 do ROADMAP, e a violação corrompe a série EEOC 4/5 **permanentemente**.
2. `20260805000004_p45_sever_user_id.sql` — **ANTES** de qualquer execução que chame `deleteUser`.
   Com o `CASCADE` vivo, `deleteUser` cascateia e falha com 23503 **depois** de o currículo já ter
   sido apagado.
3. `20260805000005_p45_plano_e_dry_run.sql`, depois `20260805000006_p45_anonimizar_candidato.sql`
   (a segunda chama a primeira).
4. **Deploy da EF `notificar-rh`** (com o 6º evento) **ANTES** do apply de
   `20260805000007_p45_retirada_e_evento.sql`. `net.http_post` é **at-most-once**: um trigger
   apontando para uma EF que ainda não conhece o evento dispara num 404 **silenciosamente
   droppado**. Foi assim que a P42 ordenou a mesma coisa.
5. Deploy da EF `executar-direito-titular` (com os quatro passos).

**Após CADA apply:** reconciliar `supabase_migrations.schema_migrations` para o prefixo do arquivo e
conferir `md5(statements[1])` contra o md5 computado do arquivo do repositório. Divergência = o que
foi aplicado não é o que foi revisado → **parada imediata**. `supabase db push` é proibido (42601 em
corpos `$$`).

**Antes de qualquer smoke que possa enviar e-mail:** ler `NOTIFICACOES_MODO`. Em PROD ele é
`producao`, e um smoke descuidado manda e-mail real. A `SONDA 5` já mediu; re-confirmar.

**Rodar os dois smokes**, cada um numa **ÚNICA chamada** de `execute_sql` — `set_config(..., false)`
é escopado à sessão, e statements em chamadas separadas zeram o contador:
- `p45_bias_k5_smoke.sql` → contador tem de bater **9**.
- `p45_motor_exclusao_smoke.sql` → contador tem de bater o número FIXO do cabeçalho.

⚠ **O smoke do motor ESCREVE** (o tombstone é `UPDATE`): confirmar, ao fim, que as contagens de
`candidatos` e `candidaturas` voltaram ao valor de antes — é a prova de que o `ROLLBACK` aconteceu.

**Fechar a proveniência do md5.** Substituir os marcadores `PENDENTE-45-07` em
`p45_motor_exclusao_smoke.sql` pelos `md5(prosrc)` **vivos** de `plano_exclusao_titular` e
`anonimizar_candidato`, com a data e o comando `node -e` de recomputação. Re-pinar é ato consciente
e revisável, e é isso que faz a assertiva `C3` ter dentes.

Transcrever tudo em `45-11-EVIDENCIA-PORTAO.md`.
  </how-to-verify>
  <acceptance_criteria>
    - As cinco versions (`20260805000003` a `20260805000007`) aparecem em `supabase_migrations.schema_migrations` com o prefixo do arquivo, na ordem declarada.
    - `md5(statements[1])` de cada version bate o md5 computado do arquivo correspondente, com os dez valores transcritos no documento.
    - `candidatos.user_id` tem `attnotnull = false` e `candidatos_user_id_fkey` tem `confdeltype = 'n'`, medidos após o apply.
    - `candidatos.faixa_etaria_materializada` existe antes de `anonimizar_candidato` ser aplicada — verificável pela ordem das versions no ledger.
    - A EF `notificar-rh` foi deployada **antes** do apply de `20260805000007`, com as duas versions/timestamps transcritos para provar a ordem.
    - `notificacoes_enviadas_evento_check` tem 6 valores, medido em `pg_constraint`.
    - `NOTIFICACOES_MODO` foi lido e registrado **antes** de qualquer smoke.
    - `p45_bias_k5_smoke.sql` sai verde com contador 9, numa única chamada.
    - `p45_motor_exclusao_smoke.sql` sai verde com o contador FIXO do cabeçalho, numa única chamada, e as contagens de `candidatos`/`candidaturas` voltaram ao valor de antes.
    - Os marcadores `PENDENTE-45-07` foram substituídos por `md5(prosrc)` vivos, com data e comando de recomputação.
    - As duas EFs estão `ACTIVE` com `verify_jwt: true`.
  </acceptance_criteria>
  <resume-signal>Digite "apply e smokes verdes" com as cinco versions, os dois contadores e as versions das duas EFs — ou pare e descreva a divergência. Uma divergência de md5 é parada, não nota de rodapé.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: A execução real, vigiada — e as asserções negativas medidas nos três sistemas</name>
  <files>.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-11-EVIDENCIA-PORTAO.md</files>
  <read_first>
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-SONDAS-PROD.md` (as contagens ANTES de TUDO, e a SONDA 6 — o corpo real do erro do `deleteUser`, que agora não deve mais acontecer)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-11-EVIDENCIA-PORTAO.md` (o que as Tasks 1 e 2 registraram)
    - `.planning/ROADMAP.md` § Phase 45 § Success Criteria (os cinco critérios, que são o que este passo mede)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-VALIDATION.md` § Manual-Only Verifications
  </read_first>
  <what-built>
Toda a fase. Este é o único momento em que a mutação de três sistemas roda de verdade, e ela é
**irreversível**: sem PITR, com backup de 7 dias que exclui Storage inteiramente, um CV apagado é
irrecuperável por qualquer meio.
  </what-built>
  <how-to-verify>
**A conta é descartável, criada para isto.** Nunca uma conta de pessoa real, nunca uma conta de
teste que outra fase ainda vá usar.

**Montar o cenário completo**, para que a execução exercite todos os caminhos:
1. Criar a conta descartável; registrar `user_id` e `candidato.id`.
2. Criar DUAS candidaturas, e forçar uma transição de etapa com o JWT dela (gera
   `historico_candidatura` com `ator`).
3. Registrar uma `decisao_final` com `justificativa` de ≥50 caracteres, e a linha correspondente em
   `decisao_final_historico`.
4. Subir DOIS currículos (um deles substituindo o outro **sem** remover o antigo) — é o cenário do
   Pitfall 4: o segundo upload gera UUID novo e o primeiro fica no bucket **sem linha que o aponte**.
5. Criar fixture em `logs_acesso`, `autorizacoes`, `recruiter_alerts`, `ai_call_logs`,
   `candidate_ai_decisions` e `notificacoes_enviadas` para aquele titular.

**Medir ANTES** e transcrever: contagem das 3 tabelas da trilha, `confdeltype` das 3 FKs, número de
objetos sob o prefixo, contagem de `auth.users`, e o `jsonb` de `plano_exclusao_titular` para aquele
candidato.

**Executar pelo caminho real:** pedir a exclusão pela tela, e então antecipar `executar_em` para o
passado (apenas naquela linha) e invocar `acao='executar'`. Não pular a EF — o que se está medindo é
o motor inteiro, não a RPC isolada.

**Medir DEPOIS, nos três sistemas:**
- **Storage:** zero objeto sob o prefixo do titular — os **dois**, inclusive o órfão sem ponteiro.
- **Postgres:** tombstone completo (nome, e-mail, celular, CPF, data de nascimento, gênero, cidade
  conforme a tabela de tratamento); `user_id` NULL; `faixa_etaria_materializada` preenchida;
  `decisao_final.justificativa` e `decisao_final_historico.justificativa` **não-nulas e
  desidentificadas**; as 5 tabelas `SET NULL` severadas; `ip_address` e `ip_aceite` mascarados;
  ledger de e-mail com sentinela e `dedupe_key` re-namespaceada.
- **Auth:** o `user_id` **não existe mais** em `auth.users`, e a contagem caiu em exatamente 1.

**As sete asserções negativas** — e são elas que provam que a coisa certa sumiu:
1. `confdeltype = 'a'` nas 3 FKs `NO ACTION`, **inalterado**.
2. Contagens de `historico_candidatura`, `decisao_final`, `decisao_final_historico` **idênticas** às
   de antes.
3. Zero `candidatos` com `user_id` existente em `auth.users`.
4. Zero `historico_candidatura.ator` apontando ao titular.
5. Zero linha nova em `notificacoes_enviadas` com `evento='decisao'` para as candidaturas encerradas.
6. Zero linha em `historico_candidatura` com `auto_rejeitado = true` para aquelas candidaturas.
7. Zero linha em `notificacoes_enviadas` para o **recibo** ao titular (D-45-12 / R1).

**Re-identificação como gate:** procurar o titular por (faixa etária + UF + vaga + timestamp).
**Achou 1 linha → a anonimização falhou** e a fase não fecha.

**O recibo:** confirmar que o e-mail chegou, que está em tempo passado, que tem as duas colunas
empilhadas, e que **não contém** nome, CPF, telefone, `candidato_id`, `solicitacao_id`, nome de vaga
nem link autenticado.

**O `plano`:** confirmar que foi esvaziado, restando só as contagens — nenhum caminho de Storage.

**Idempotência:** re-invocar `acao='executar'` no mesmo pedido e confirmar que nada muda e que
nenhuma das duas APIs privilegiadas é chamada de novo.

**Bias:** rodar `gerar_bias_snapshot` e confirmar que o titular executado continua na mesma faixa
etária de antes e **fora** de `excluidos_sem_data` (SC#5), e que células com menos de 5 saem
suprimidas com a supressão complementar.

⚠ **Se qualquer asserção negativa falhar, PARAR.** Não "consertar e continuar": registrar, e tratar
como incidente. Em particular, **um 23503 não autoriza relaxar FK nenhuma** — ele é agora um
desfecho esperado, e a saída é anonimizar a linha filha e severar o ponteiro.

**Fechar o portão:** produzir `45-VERIFICATION.md` com **veredito** (nunca ausente, nunca `draft`),
e transcrever tudo em `45-11-EVIDENCIA-PORTAO.md`.
  </how-to-verify>
  <acceptance_criteria>
    - O documento nomeia o e-mail da conta descartável e registra que nenhuma conta de pessoa real foi tocada.
    - As medições ANTES e DEPOIS estão transcritas para os três sistemas.
    - Zero objeto sob o prefixo do titular após a execução — incluindo o currículo órfão sem ponteiro.
    - O tombstone está completo coluna a coluna, conforme a tabela de tratamento, e as duas colunas de `justificativa` estão não-nulas e desidentificadas.
    - `auth.users` caiu em exatamente 1 e o `user_id` do titular não existe mais.
    - As sete asserções negativas estão medidas, cada uma com o SQL e o resultado transcritos.
    - A busca de re-identificação por (faixa etária + UF + vaga + timestamp) devolveu **zero** linhas.
    - O recibo chegou, em tempo passado, sem nenhum dos identificadores proibidos — com o HTML recebido transcrito ou anexado.
    - O `plano` está esvaziado, sem caminho de Storage.
    - A re-invocação de `acao='executar'` não mudou nada e não chamou Storage Admin API nem `deleteUser`.
    - O snapshot de bias mostra o titular na mesma faixa de antes e fora de `excluidos_sem_data`, e as células com menos de 5 suprimidas com complemento.
    - `45-VERIFICATION.md` existe com veredito, e o documento registra os 5 itens do portão destrutivo como satisfeitos, cada um com a evidência apontada.
  </acceptance_criteria>
  <resume-signal>Digite "portão destrutivo fechado 5/5" com o link para `45-VERIFICATION.md`, ou descreva qual asserção negativa falhou. Uma asserção negativa vermelha fecha a fase, não abre exceção.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orquestrador → PROD (apply destrutivo, deploy, execução real) | Mutação irreversível sobre três sistemas, sem PITR e sem backup de Storage |
| conta descartável → dados de PROD | A execução real acontece no mesmo projeto onde vivem contas reais |
| smoke que escreve → PROD | O tombstone é `UPDATE`, dentro de subtransação com `ROLLBACK` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-11-01 | Tampering | execução real | critical | mitigate | Conta descartável criada pela própria execução; medições ANTES e DEPOIS nos três sistemas; sete asserções negativas com parada imediata em qualquer vermelho |
| T-45-11-02 | Tampering | apply fora de ordem | critical | mitigate | Quatro dependências de ordem declaradas com o dano de cada violação; a ordem é provada pelas versions no ledger e pelos timestamps de deploy |
| T-45-11-03 | Repudiation | apply sem review | critical | mitigate | Task 1 é bloqueante e precede qualquer `apply_migration` destrutivo; blocker aberto fecha o portão; origem da regra registrada (P39) |
| T-45-11-04 | Repudiation | dry-run decorativo | critical | mitigate | `md5(prosrc)` conferido contra o computado do arquivo + `pg_get_functiondef` contendo a chamada; delta ≡ 0 medido; SQLSTATE do dry-run conferido para não mascarar erro real |
| T-45-11-05 | Tampering | migration divergente | high | mitigate | `md5(statements[1])` conferido após cada apply; divergência é parada imediata, não nota de rodapé |
| T-45-11-06 | Denial of Service | trigger apontando para EF sem o evento | high | mitigate | Deploy de `notificar-rh` ANTES do apply do trigger, com os dois timestamps transcritos — `net.http_post` é at-most-once e o 404 é silenciosamente droppado |
| T-45-11-07 | Information Disclosure | smoke de e-mail em `producao` | high | mitigate | `NOTIFICACOES_MODO` lido e registrado antes de qualquer smoke; nenhum smoke desta fase envia e-mail a endereço real de RH |
| T-45-11-08 | Repudiation | relaxar FK diante de 23503 | critical | mitigate | Proibição explícita no plano; asserção negativa de `confdeltype` medida antes e depois; o 23503 é desfecho esperado e a saída é severar o ponteiro |
| T-45-11-09 | Information Disclosure | re-identificação pós-execução | high | mitigate | Busca por quase-identificadores é gate: 1 linha encontrada reprova a fase |
| T-45-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | **Zero instalação.** `45-RESEARCH.md § Package Legitimacy Audit` registra zero pacotes; nenhum `[ASSUMED]`/`[SUS]`/`[SLOP]` |
</threat_model>

<verification>
Os cinco itens do portão de fase destrutiva do M8, cada um com evidência apontada em
`45-11-EVIDENCIA-PORTAO.md`:

1. `45-VERIFICATION.md` presente e com **veredito** — nunca ausente, nunca `status: draft`.
2. Code review **bloqueante ANTES** do apply em PROD, com zero blocker aberto.
3. **Asserções negativas** — as sete, medidas antes e depois, com SQL e resultado transcritos.
4. **Zero `--no-verify`** em todos os commits da fase.
5. **Dry-run pela MESMA query** do delete real, exercitado, com delta ≡ 0 e `md5(prosrc)` conferido.

Mais: os dois smokes verdes com contador fixo, as cinco versions reconciliadas com md5 conferido, as
duas EFs `ACTIVE` com `verify_jwt: true`, e `npm run test:run` + `npm run lint` (≤97) +
`npm run build` + `npm run check:export-allowlist` + `npm run check:recibo-exclusao` verdes.
</verification>

<success_criteria>
Um candidato pediu que seus dados fossem apagados e isso aconteceu de verdade — o currículo sumiu do
Storage, o registro virou tombstone anônimo e o usuário do Auth deixou de existir, **nessa ordem** —
sem levar junto a trilha de decisão humana que a RNF-07a existe para proteger. E cada uma dessas
afirmações está medida em PROD, com o que **não** aconteceu medido junto.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (contribuição deste plano)

| Artefato | Tipo | Novo/Editado |
|---|---|---|
| `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-11-EVIDENCIA-PORTAO.md` | evidência datada do portão destrutivo | **novo** |
| `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-VERIFICATION.md` | veredito da fase | **novo** |
| `supabase/tests/p45_motor_exclusao_smoke.sql` — proveniência do md5 | pin de `md5(prosrc)` vivo | **editado** |
| versions `20260805000003`–`20260805000007` no ledger | estado do ledger | **novo** |
| EFs `executar-direito-titular` e `notificar-rh` em PROD | deploy | **novo/atualizado** |
</artifacts_produced>

<output>
Create `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-11-SUMMARY.md` when done
</output>
