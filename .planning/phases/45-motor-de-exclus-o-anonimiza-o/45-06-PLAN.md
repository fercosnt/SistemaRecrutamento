---
phase: 45-motor-de-exclus-o-anonimiza-o
plan: 06
type: execute
wave: 3
depends_on: ["45-03"]
files_modified:
  - .planning/phases/45-motor-de-exclus-o-anonimiza-o/45-06-EVIDENCIA-APPLY.md
  - database.types.ts
autonomous: false
requirements: [ERASE-05, ERASE-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "As duas migrations do tracer estão aplicadas em PROD por MCP `apply_migration`, o ledger `supabase_migrations.schema_migrations` foi reconciliado para as versions `20260805000001` e `20260805000002`, e `md5(statements[1])` de cada uma bate o valor computado do arquivo do repositório."
    - "A EF `executar-direito-titular` está viva em PROD com `verify_jwt: true` — nunca `--no-verify-jwt` — e a version resultante está registrada."
    - "PROVA PONTA A PONTA DO TRACER: um titular de teste, pelo navegador, clica «Apagar meus dados», e o resultado observado é UMA linha `solicitacoes_dados` com `tipo='exclusao'`, `situacao='agendado'`, `executar_em` exatamente 15 dias após `solicitado_em`, e a tela renderiza «Exclusão agendada» com a data por extenso."
    - "ASSERÇÃO NEGATIVA medida ao vivo: após o clique, `notificacoes_enviadas` não ganhou linha com `evento='decisao'` para candidatura alguma daquele titular, e `historico_candidatura` não ganhou linha com `auto_rejeitado = true`."
    - "ASSERÇÃO NEGATIVA medida ao vivo: as candidaturas encerradas do titular têm `encerrada_a_pedido_em` preenchida E `deleted_at` ainda NULL — elas continuam visíveis para as 5 leituras de RH que filtram `.is('deleted_at', null)`."
    - "ASSERÇÃO NEGATIVA medida ao vivo: uma linha `tipo='exclusao'` NÃO aparece em `listar_pedidos_dados()` nem é contada por `contar_pedidos_dados_pendentes()` — a fila de acesso do RH continua byte-idêntica."
    - "Segundo clique em «Apagar meus dados» pelo mesmo titular devolve a MESMA data e não cria linha nova — idempotência por estado, medida por contagem antes/depois, não por inspeção de código."
    - "O estado do tracer em PROD está registrado num artefato datado no repositório, com as contagens antes/depois de cada asserção negativa."
  artifacts:
    - .planning/phases/45-motor-de-exclus-o-anonimiza-o/45-06-EVIDENCIA-APPLY.md
  key_links:
    - "apply do tracer → 45-07 e 45-10 (o esqueleto destrutivo só é escrito sobre uma arquitetura já provada viva; um beco arquitetural descoberto aqui custa um commit, descoberto depois custa dez)"
    - "`45-06-EVIDENCIA-APPLY.md` → 45-11 (o portão destrutivo confere o estado do ledger e a version da EF a partir daqui)"
  prohibitions:
    - "MUST NOT declarar o pedido concluído, em tela ou em resposta de API, enquanto os três sistemas não confirmarem — nesta wave nenhum dos três rodou ainda, e a única palavra permitida sobre um pedido agendado é «agendada», nunca «concluído», «apagado» ou «pronto»."
---

<objective>
Aplicar o tracer em PROD e **provar a fatia vertical ponta a ponta** — o único momento desta fase
em que a arquitetura inteira é exercitada por uma pessoa real sem que nada seja destruído.

Purpose: o valor do tracer não está em ter sido escrito, está em ter **rodado**. Este plano é o
gate que separa "o desenho parece certo" de "o desenho funciona", e ele acontece **antes** de uma
única linha destrutiva ser escrita. Um beco arquitetural encontrado aqui custa um commit; encontrado
depois de Storage, tombstone e `deleteUser` estarem prontos, custa dez — e nesta fase alguns desses
dez são irreversíveis.

⚠ **Plano 100% checkpoint do orquestrador.** Todo apply, toda inspeção em PROD e todo deploy de EF
passam pelos tools MCP do Supabase, que subagentes GSD não recebem (bug upstream
anthropics/claude-code#13898). `supabase db push` é proibido: bate SQLSTATE 42601 em corpos PL/pgSQL
`$$`. Nenhum código é escrito neste plano.

Output: `45-06-EVIDENCIA-APPLY.md` + (condicionalmente) `database.types.ts` regenerado.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-03-SUMMARY.md
@.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Apply das duas migrations do tracer + reconcile do ledger + conferência do md5</name>
  <precondition>`supabase/migrations/20260805000001_p45_pedido_exclusao.sql` e `20260805000002_p45_rpc_pedido_exclusao.sql` existem no repositório e o plano 45-03 fechou verde — este plano não escreve SQL, só aplica o que já foi revisado.</precondition>
  <files>.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-06-EVIDENCIA-APPLY.md</files>
  <read_first>
    - `supabase/migrations/20260805000001_p45_pedido_exclusao.sql` e `supabase/migrations/20260805000002_p45_rpc_pedido_exclusao.sql` (os dois arquivos INTEIROS, inclusive o cabeçalho com o protocolo de apply e o comando de reparo do ledger)
    - `CLAUDE.md` § "Migrations + db push — workaround conhecido (PL/pgSQL)"
    - `.planning/STATE.md` § Blockers/Concerns (o drift de ledger pré-existente, as 7 versions órfãs de 07-13/07-14, e por que reconciliar após CADA apply não é opcional)
    - `.planning/phases/44-exporta-o-acesso/44-04-SUMMARY.md` (o precedente mais recente de apply + reconcile desta cadeia)
  </read_first>
  <action>
Orquestrador (main thread, tools MCP do Supabase).

1. **Medir ANTES.** `SELECT count(*) FROM public.solicitacoes_dados`, `SELECT count(*) FROM
   public.candidaturas WHERE encerrada_a_pedido_em IS NOT NULL` (deve ser 0 — a coluna não existe
   ainda, então a consulta falha, e registrar isso é o baseline), `SELECT count(*) FROM
   public.historico_candidatura`, `SELECT count(*) FROM public.notificacoes_enviadas`, e o
   `confdeltype` das 3 FKs `NO ACTION`. Transcrever em `45-06-EVIDENCIA-APPLY.md`.
2. **Apply na ordem**: `20260805000001_p45_pedido_exclusao.sql` primeiro (schema), depois
   `20260805000002_p45_rpc_pedido_exclusao.sql` (funções, que dependem das colunas). Via MCP
   `apply_migration`. **Nunca** `db push`.
3. **Reconciliar o ledger após CADA apply** — `UPDATE supabase_migrations.schema_migrations SET
   version = '20260805000001'` para o registro recém-criado com timestamp, e idem para o segundo.
   O comando exato está no cabeçalho de cada migration. Não reconciliar é como o drift de causa
   desconhecida deste banco nasce.
4. **Conferir `md5(statements[1])`** de cada version aplicada contra o md5 computado do arquivo do
   repositório. Divergência = o que foi aplicado não é o que foi revisado, e isso é parada
   imediata, não nota de rodapé.
5. **Verificar o pós-estado**: `config_janela_exclusao` existe com 1 linha (`dias = 15`) e
   exatamente 1 policy (SELECT, `authenticated`); `solicitacoes_dados` tem as 7 colunas novas;
   `ck_solicitacoes_dados_situacao` tem 6 valores; `candidaturas.encerrada_a_pedido_em` existe;
   as 2 funções existem com `prosecdef = true` e `proacl` **sem** `anon`/`authenticated`/PUBLIC.
6. **Re-medir as 3 FKs `NO ACTION`** e exigir `confdeltype = 'a'` nas três — asserção negativa do
   ERASE-08 executada já neste apply, não só no final da fase.
7. Se `supabase migration list` estiver disponível, registrar Local/Remote alinhados. Se não,
   registrar o estado do ledger por SQL direto (foi o que a 44-04 fez).

Registrar tudo em `45-06-EVIDENCIA-APPLY.md`, com o SQL e a saída transcritos.
  </action>
  <verify>
    <automated>test -f .planning/phases/45-motor-de-exclus-o-anonimiza-o/45-06-EVIDENCIA-APPLY.md &amp;&amp; for t in "20260805000001" "20260805000002" "md5" "confdeltype" "config_janela_exclusao" "proacl"; do grep -q "$t" .planning/phases/45-motor-de-exclus-o-anonimiza-o/45-06-EVIDENCIA-APPLY.md || { echo "FALTA: $t"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - As duas versions `20260805000001` e `20260805000002` aparecem em `supabase_migrations.schema_migrations` com o prefixo do arquivo, não com o timestamp de apply.
    - O `md5(statements[1])` de cada version bate o md5 computado do arquivo correspondente do repositório, e os dois valores estão transcritos no documento.
    - `config_janela_exclusao` tem 1 linha com `dias = 15` e exatamente 1 policy, de SELECT, para `authenticated`.
    - `solicitacoes_dados` tem as sete colunas novas; `ck_solicitacoes_dados_situacao` aceita seis valores.
    - `candidaturas.encerrada_a_pedido_em` existe e é nullable.
    - `registrar_pedido_exclusao` e `cancelar_pedido_exclusao` existem com `prosecdef = true`, e `proacl` de nenhuma delas concede EXECUTE a `anon`, `authenticated` ou PUBLIC.
    - `confdeltype = 'a'` nas 3 FKs `NO ACTION`, medido DEPOIS do apply.
    - `45-06-EVIDENCIA-APPLY.md` transcreve o SQL e a saída de cada passo, incluindo as contagens ANTES.
  </acceptance_criteria>
  <done>O schema do pedido de exclusão está vivo em PROD, com ledger reconciliado, md5 conferido e a trilha de auditoria re-medida intacta.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Deploy da EF + a PROVA ponta a ponta do tracer, no navegador</name>
  <files>.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-06-EVIDENCIA-APPLY.md</files>
  <read_first>
    - `supabase/functions/executar-direito-titular/index.ts` (o docblock, o wiring two-client e a nota de que `--no-verify-jwt` é NUNCA)
    - `.planning/phases/44-exporta-o-acesso/44-05-EVIDENCIA-DEPLOY.md` (o precedente de prova de deploy desta cadeia — e a lição de que o discriminador correto é a DIFERENÇA entre os dois corpos de resposta, não o 401 sozinho)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-UI-SPEC.md` § Seção 4 Estado A e Estado B (a copy exata que a tela tem de renderizar)
    - `.planning/STATE.md` § Blockers/Concerns (as contas de teste de PROD nomeadas)
  </read_first>
  <what-built>
A fatia vertical inteira do "pedir exclusão": migration → RPC `SECURITY DEFINER` → Edge Function
privilegiada → service → hook → componente → página. Nada destrutivo: nenhum arquivo de Storage é
tocado, nenhuma coluna é anonimizada, nenhum usuário do Auth é apagado. Esta é a última
oportunidade de descobrir um beco arquitetural barato.
  </what-built>
  <how-to-verify>
**Deploy.** `npx supabase functions deploy executar-direito-titular` (o CLI lê os bytes do disco;
o caminho MCP também serve). **`--no-verify-jwt` nunca.** Registrar a version resultante e
confirmar `verify_jwt: true`.

**Sonda de fronteira, antes do navegador.** `POST` sem `Authorization` → o **gateway** responde
(`UNAUTHORIZED_NO_AUTH_HEADER`) e a requisição nunca chega à função. `POST` com publishable key →
o **handler** responde com a string do próprio `index.ts`. O discriminador é a **diferença entre os
dois corpos**, não o 401 sozinho — foi assim que a assunção A1 da Phase 44 foi fechada. Confirma
que o módulo sobreviveu ao bundler.

**Contagens ANTES**, por SQL: `solicitacoes_dados` (total e `tipo='exclusao'`),
`historico_candidatura`, `notificacoes_enviadas` com `evento='decisao'`, e as candidaturas do
titular de teste com `etapa_atual` e `deleted_at`.

**No navegador, com a conta de teste de candidato** (`.planning/STATE.md` § Blockers nomeia as
contas de PROD):

1. Abrir `/candidato/privacidade`. A **seção 4** aparece **abaixo** da seção 3, separada pelo mesmo
   `border-t border-white/15 pt-6`. As seções 1–3 renderizam exatamente como antes.
2. O CTA é **glass-branco**, não vermelho, não accent, não full-bleed, e tem altura de toque ≥44px.
3. A prosa da consequência está inteira e legível — **não truncada, não em accordion**. O parágrafo
   «o que o cancelamento NÃO desfaz» é um parágrafo próprio, não uma cláusula embutida.
4. O ponteiro para «Retirar minha candidatura» aparece em **texto**, sem link e sem botão.
5. Clicar. O botão desabilita, mostra «Registrando seu pedido…» com o spinner, e o motivo aparece
   ao lado — não em `title`.
6. A tela passa a «**Exclusão agendada**» com a **data por extenso** e a nota de que cancelar não
   reabre as candidaturas.
7. Recarregar a página: o Estado B persiste (não é estado local).
8. Clicar de novo não existe — o CTA foi substituído. Invocar a EF de novo pela sonda com o mesmo
   JWT devolve a MESMA data.
9. A 320px (DevTools, modo dispositivo): nada estoura, nada rola horizontalmente, o botão cresce em
   altura.

**Contagens DEPOIS e as asserções negativas**, por SQL:
- `solicitacoes_dados`: **exatamente +1** linha, `tipo='exclusao'`, `situacao='agendado'`,
  `executar_em - solicitado_em = 15 days`.
- `candidaturas` do titular: `encerrada_a_pedido_em` preenchida **e** `deleted_at` ainda **NULL**.
- `historico_candidatura`: contagem **inalterada**, e **zero** linha nova com `auto_rejeitado = true`.
- `notificacoes_enviadas`: **zero** linha nova com `evento='decisao'` para aquelas candidaturas.
- `listar_pedidos_dados()` e `contar_pedidos_dados_pendentes()`: **não** incluem a linha nova.
- Segunda invocação: `solicitacoes_dados` continua com o mesmo total.

Registrar tudo em `45-06-EVIDENCIA-APPLY.md`, seção "Prova ponta a ponta do tracer".

⚠ **Se a etapa 6 mostrar qualquer palavra de desfecho** («concluído», «apagado», «pronto»), isso é
violação da Invariante 5 e é bloqueio, não cosmético: nenhum dos três sistemas rodou.
  </how-to-verify>
  <acceptance_criteria>
    - A EF está `ACTIVE` em PROD com `verify_jwt: true`, e a version está registrada no documento.
    - A sonda de fronteira mostra DOIS corpos de resposta distintos (gateway × handler), transcritos.
    - `solicitacoes_dados` ganhou exatamente 1 linha, com `tipo='exclusao'`, `situacao='agendado'` e `executar_em - solicitado_em = 15 days`.
    - As candidaturas encerradas têm `encerrada_a_pedido_em` NÃO-NULA e `deleted_at` NULL.
    - `historico_candidatura` tem contagem inalterada e zero linha nova com `auto_rejeitado = true`.
    - `notificacoes_enviadas` tem zero linha nova com `evento='decisao'` para aquelas candidaturas.
    - `listar_pedidos_dados()` e `contar_pedidos_dados_pendentes()` não retornam nem contam a linha nova.
    - A segunda invocação não cria linha e devolve a mesma `executar_em`.
    - A tela renderiza «Exclusão agendada» com a data por extenso e persiste após recarregar; nenhuma palavra de desfecho aparece.
    - A 320px não há scroll horizontal nem truncamento da prosa de consequência.
    - Todas as contagens ANTES e DEPOIS estão transcritas no documento.
  </acceptance_criteria>
  <resume-signal>Digite "tracer provado" com a version da EF e o id da linha criada — ou descreva o que divergiu. Se qualquer asserção negativa falhar, **não** prosseguir para 45-07/45-10.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Regenerar `database.types.ts` — com o cuidado do `>` que trunca antes de executar</name>
  <files>database.types.ts, .planning/phases/45-motor-de-exclus-o-anonimiza-o/45-06-EVIDENCIA-APPLY.md</files>
  <read_first>
    - `package.json` (a entrada `db:types`, que usa `>` — o redirecionamento **trunca o arquivo antes de o comando rodar**)
    - `.planning/STATE.md` § Blockers/Concerns (o item 44-04: tipos não regenerados por auth gate do CLI; e o desbloqueio nomeado)
    - `src/features/privacidade/services/exportacaoService.ts` (`:766-792` — a ponte de tipos que o 45-03 usa e que continua correta mesmo se este passo falhar)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-RESEARCH.md` § Pitfall 10 e § Environment Availability
  </read_first>
  <what-built>
`database.types.ts` está desatualizado desde 2026-08-02 e **não conhece** `solicitacoes_dados`,
`config_sla_dados` nem `listar_pedidos_dados` — a Phase 44 embarcou sem regenerar por auth gate do
CLI. Depois deste apply ele também não conhece `config_janela_exclusao`,
`candidaturas.encerrada_a_pedido_em` nem as duas RPCs novas.

Este passo é **oportunista, não bloqueante**: o 45-03 usa deliberadamente a ponte de tipos estreita
de `exportacaoService.ts:766-792`, que mantém o nome da tabela LITERAL no tipo (um erro de digitação
nele ainda não compila) e converte o **objeto** cliente, nunca a extração do método. Se a
regeneração não passar, o código continua correto e a baseline `tsc` continua em 97.
  </what-built>
  <how-to-verify>
1. `npx supabase login` (ou `SUPABASE_ACCESS_TOKEN` no ambiente). Se não passar, **parar aqui**,
   registrar o bloqueio e seguir — não é regressão.
2. ⚠ **Nunca rodar `npm run db:types` direto.** O script usa `>`, e o shell **trunca
   `database.types.ts` antes de o comando executar**: se o comando falhar, o arquivo fica vazio e o
   `tsc` explode muito acima da baseline 97. Lição da P37/37-05. Gerar para **temporário** primeiro:
   `npx supabase gen types typescript --project-id <id> > /tmp/database.types.next.ts`, conferir
   que o temporário é não-vazio e contém `solicitacoes_dados` e `config_janela_exclusao`, e **só
   então** movê-lo sobre o arquivo trackeado.
3. `npm run lint` e comparar a contagem `tsc` contra 97. Se subir, **reverter o arquivo** e
   registrar — a baseline é gate de não-regressão e "zero `--no-verify`" é exigência do portão
   destrutivo desta fase.
4. `npx vitest run` e `npm run build` para confirmar que nada quebrou.
5. Registrar o resultado em `45-06-EVIDENCIA-APPLY.md`, seção "Tipos".
  </how-to-verify>
  <acceptance_criteria>
    - `database.types.ts` está não-vazio e contém `solicitacoes_dados` e `config_janela_exclusao` — OU o documento registra o bloqueio de credencial e o arquivo permanece byte-idêntico ao commit anterior.
    - Em nenhum momento o arquivo trackeado ficou vazio: a geração passou por temporário.
    - `npm run lint` reporta contagem `tsc` **≤ 97**.
    - `npm run test:run` e `npm run build` verdes.
    - A seção "Tipos" do documento registra o desfecho (regenerado com a version, ou bloqueado com o motivo medido).
  </acceptance_criteria>
  <resume-signal>Digite "tipos regenerados" ou "tipos bloqueados: {motivo}" — as duas saídas são aceitáveis; inventar um resultado não é.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orquestrador → PROD (`apply_migration`, `execute_sql`, deploy) | DDL e deploy privilegiados sobre um banco com PII viva |
| navegador do titular de teste → EF em PROD | Primeiro tráfego real contra a superfície nova |
| shell local → `database.types.ts` trackeado | Redirecionamento `>` trunca antes de executar |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-06-01 | Tampering | apply das migrations | high | mitigate | Ordem fixa (schema antes de funções); reconcile do ledger após CADA apply; `md5(statements[1])` conferido contra o arquivo do repositório, com divergência tratada como parada imediata |
| T-45-06-02 | Elevation | EF recém-deployada | high | mitigate | `verify_jwt: true` confirmado após o deploy; `--no-verify-jwt` nunca; sonda de fronteira prova que o gateway barra o request sem `Authorization` antes de a função rodar |
| T-45-06-03 | Repudiation | asserções negativas ao vivo | high | mitigate | Contagens ANTES e DEPOIS transcritas para `historico_candidatura`, `notificacoes_enviadas` e as 3 FKs; a ausência de linha é medida, não presumida |
| T-45-06-04 | Information Disclosure | fila do RH | medium | mitigate | Asserção ao vivo de que `listar_pedidos_dados()` não devolve a linha `tipo='exclusao'` — o RH não vê uma lista de pessoas com data de desaparecimento |
| T-45-06-05 | Denial of Service | `database.types.ts` truncado | high | mitigate | Geração para temporário, conferência de não-vazio e de conteúdo, e só então movimento sobre o trackeado; `npm run lint` contra a baseline 97 com reversão em caso de regressão |
| T-45-06-06 | Tampering | linha de teste em PROD | medium | accept | A linha criada é de conta de teste nomeada em `STATE.md`, é `tipo='exclusao'` `situacao='agendado'`, e nada destrutivo roda nesta wave; ela é insumo legítimo do 45-11 |
| T-45-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | **Zero instalação.** `45-RESEARCH.md § Package Legitimacy Audit` registra zero pacotes; `npx supabase` executa o CLI oficial do fornecedor, já usado nas Phases 37/44 |
</threat_model>

<verification>
- `45-06-EVIDENCIA-APPLY.md` existe, é datado e transcreve SQL + saída de cada passo.
- Ledger reconciliado para as duas versions, com md5 conferido.
- EF `ACTIVE` com `verify_jwt: true`.
- As seis asserções negativas ao vivo estão medidas com contagem antes/depois.
- `npm run lint` ≤ 97, `npm run test:run` e `npm run build` verdes.
- Nenhum commit desta wave usou `--no-verify`.
</verification>

<success_criteria>
A arquitetura da fase está provada viva por uma pessoa real, sem que nada tenha sido destruído — e
as três asserções negativas que o Pitfall 3 previu (sem e-mail de rejeição, sem `auto_rejeitado`,
sem sumiço das superfícies de RH) deixaram de ser previsão e passaram a ser medição.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (contribuição deste plano)

| Artefato | Tipo | Novo/Editado |
|---|---|---|
| `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-06-EVIDENCIA-APPLY.md` | evidência datada de apply + prova ponta a ponta | **novo** |
| `database.types.ts` | tipos gerados pelo Supabase CLI | **editado (condicional)** — só se `supabase login` passar; nunca por escrita à mão |
| versions `20260805000001` e `20260805000002` em `supabase_migrations.schema_migrations` | estado do ledger | **novo** |
| EF `executar-direito-titular` em PROD (`verify_jwt: true`) | deploy | **novo** |
</artifacts_produced>

<output>
Create `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-06-SUMMARY.md` when done
</output>
