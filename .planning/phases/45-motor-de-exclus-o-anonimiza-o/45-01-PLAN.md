---
phase: 45-motor-de-exclus-o-anonimiza-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/45-motor-de-exclus-o-anonimiza-o/45-SONDAS-PROD.md
autonomous: false
requirements: [ERASE-02, ERASE-03, ERASE-04, ERASE-08, ERASE-10]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Existe `45-SONDAS-PROD.md` datado, no repositório, que responde com SQL executado ao vivo: quais CHECK constraints e qual nullability `public.candidatos` tem HOJE, quais FKs `storage.objects` tem HOJE, sob qual prefixo os objetos do bucket `curriculos` estão HOJE, e quais `cron.job` estão ativos HOJE."
    - "As 3 FKs `NO ACTION` (`historico_candidatura.candidatura_id`, `decisao_final.candidatura_id`, `decisao_final_historico.candidatura_id`) foram re-lidas de `pg_constraint` e o documento registra `confdeltype='a'` para as três — a linha de base contra a qual a asserção negativa do ERASE-08 será medida no fim da fase."
    - "`candidatos.user_id → auth.users` foi re-lido de `pg_constraint` e o documento registra o `confdeltype` medido, confirmando ou refutando o `CASCADE` vivo que a `FK-AUDIT-LIVE.md` mediu em 2026-07-29."
    - "A sonda de escrita mediu, em conta descartável, o que `auth.admin.deleteUser` faz sobre um usuário COM `historico_candidatura.ator` e COM objeto no bucket: o corpo exato do erro (ou do sucesso) está transcrito no documento, e a conta descartável foi apagada ao fim."
    - "Nenhuma sonda tocou conta real: o documento nomeia o e-mail da conta descartável criada e a evidência de que ela foi removida."
    - "`NOTIFICACOES_MODO` de PROD foi lido e registrado ANTES de qualquer smoke desta fase que possa enviar e-mail."
  artifacts:
    - .planning/phases/45-motor-de-exclus-o-anonimiza-o/45-SONDAS-PROD.md
  key_links:
    - "45-SONDAS-PROD.md → 45-07 (as sentinelas do tombstone são escolhidas contra as constraints medidas aqui, nunca contra `docs/sql/sql/02-tabela-candidatos.sql`, que é de 2025 e tem drift documentado)"
    - "45-SONDAS-PROD.md → 45-10 (a enumeração de Storage é escrita contra o prefixo medido aqui, não contra a convenção assumida `{authUid}/`)"
    - "45-SONDAS-PROD.md → 45-11 (a contagem das 3 FKs `NO ACTION` medida aqui é o ANTES da asserção negativa do portão destrutivo)"
  prohibitions:
    - "MUST NOT executar qualquer sonda de escrita contra uma conta, candidatura, currículo ou linha que pertença a uma pessoa real — a sonda de hard delete só é legítima sobre uma conta criada por ela mesma, para ela mesma, e apagada por ela mesma."
---

<objective>
Medir o estado vivo de PROD **antes** de a primeira migration desta fase ser escrita.

Purpose: cinco afirmações centrais da `45-RESEARCH.md` descrevem estado vivo de um banco com
drift repo→PROD **documentado e de causa parcialmente desconhecida**. Três delas, se estiverem
erradas, quebram a transação de anonimização **no primeiro pedido real** — e essa transação é a
única atomicidade que a fase tem. Uma quarta (a "imposição de plataforma" do Storage) está
escrita em `REQUIREMENTS.md:25` como se fosse controle, e a doc oficial do Supabase a contradiz.

Output: `45-SONDAS-PROD.md` — artefato datado, com o SQL executado e o resultado transcrito,
que passa a ter **precedência sobre qualquer arquivo de migration** para as perguntas que responde.

⚠ **Este plano é 100% checkpoint do orquestrador.** Subagentes GSD não recebem os tools MCP do
Supabase (bug upstream anthropics/claude-code#13898). Nenhuma tarefa aqui é trabalho autônomo de
executor. Nenhuma migration é escrita neste plano, e nenhuma migration de fase alguma pode ser
escrita antes de ele fechar.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/research/FK-AUDIT-LIVE.md
@.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: As 5 sondas read-only de PROD — o estado vivo do catálogo, numa única passagem</name>
  <files>.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-SONDAS-PROD.md</files>
  <read_first>
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-RESEARCH.md` § Environment Availability (as 5 consultas estão escritas lá, verbatim — não reescrever)
    - `.planning/research/FK-AUDIT-LIVE.md` (§"Drift repo → PROD confirmado", §"As 3 FKs que bloqueiam", §"Onde PII sobrevive a um CASCADE", §"Contagens ao vivo")
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-RESEARCH.md` § Pitfall 1 e § Pitfall 9 (por que o arquivo de DDL de 2025 não serve como fonte de nullability)
  </read_first>
  <action>
Orquestrador (main thread, tools MCP do Supabase). Rodar as cinco consultas read-only da
§ Environment Availability da `45-RESEARCH.md` por `execute_sql`, e transcrever o resultado —
não o resumo do resultado — em `45-SONDAS-PROD.md`.

Sonda 1 — constraints e nullability vivas de `public.candidatos`: `information_schema.columns`
(`column_name, is_nullable, data_type`) e `pg_constraint` (`conname, pg_get_constraintdef(oid)`
sobre `'public.candidatos'::regclass`). Registrar explicitamente, coluna a coluna, o veredito
para as dez colunas que o tombstone toca: `nome_completo`, `email`, `celular`, `cpf`,
`data_nascimento`, `genero`, `cidade`, `estado`, `user_id`, `linkedin`/`instagram`. Confirmar ou
refutar as seis CHECKs nomeadas no Pitfall 1 (`candidatos_email_check`, `candidatos_cpf_check`,
`candidatos_celular_check`, `candidatos_data_nascimento_check`, `candidatos_genero_check`,
`candidatos_estado_check`) e registrar QUALQUER constraint não prevista.

Sonda 2 — FKs de `storage.objects` (`contype='f'`, `confdeltype` decodificado). Fecha o §C4: se
não existir FK de `storage.objects.owner` para `auth.users`, a frase de `REQUIREMENTS.md:25`
("o Supabase recusa deletar usuário que possua objetos no Storage") **não é um controle**, e o
documento tem de dizer isso com todas as letras.

Sonda 3 — `SELECT bucket_id, count(*), min(name), max(name) FROM storage.objects GROUP BY
bucket_id`. Fecha a assunção A4: os objetos do bucket `curriculos` estão TODOS sob o prefixo
`{authUid}/`? Se aparecer qualquer `name` que não case com `^[0-9a-f-]{36}/`, isso é achado
bloqueante para o ERASE-04 e vai para o topo do documento.

Sonda 4 — re-confirmar o grafo de FK do `FK-AUDIT-LIVE.md` (coletado em 2026-07-29) pela mesma
consulta de reprodução daquele arquivo, restrita a: `candidatos.user_id → auth.users`
(o `CASCADE` que redesenha a fase), as 3 FKs `NO ACTION` do ERASE-08, e as 5 FKs `SET NULL` do
ERASE-09. Registrar também as contagens vivas de `ai_call_logs`, `candidate_ai_decisions`,
`logs_acesso`, `recruiter_alerts`, `autorizacoes` e `notificacoes_enviadas`.

Sonda 5 — `SELECT jobname, schedule, active, left(command,120) FROM cron.job ORDER BY jobname` e
o valor vivo de `NOTIFICACOES_MODO` no projeto. Fecha A6 e A7. ⚠ O valor de `NOTIFICACOES_MODO`
tem de estar registrado ANTES de qualquer smoke desta fase que envie e-mail.

Escrever `45-SONDAS-PROD.md` com: cabeçalho datado, uma seção por sonda contendo o SQL executado
e a saída transcrita, e uma seção final "O que isto muda no plano" listando cada divergência
entre o medido e o que os documentos de entrada assumem. Se ZERO divergências, dizer isso
explicitamente — a ausência de divergência é um resultado, não um silêncio.
  </action>
  <verify>
    <automated>test -f .planning/phases/45-motor-de-exclus-o-anonimiza-o/45-SONDAS-PROD.md &amp;&amp; for t in "SONDA 1" "SONDA 2" "SONDA 3" "SONDA 4" "SONDA 5" "candidatos_estado_check" "confdeltype" "NOTIFICACOES_MODO"; do grep -q "$t" .planning/phases/45-motor-de-exclus-o-anonimiza-o/45-SONDAS-PROD.md || { echo "FALTA: $t"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `45-SONDAS-PROD.md` existe e contém as cinco seções de sonda, cada uma com o SQL executado E a saída transcrita.
    - Para `public.candidatos`, o documento lista `is_nullable` para as dez colunas do tombstone e o `pg_get_constraintdef` de toda constraint viva — incluindo qualquer uma não prevista pelo Pitfall 1.
    - O documento responde SIM ou NÃO, com a linha de `pg_constraint`, à pergunta "existe FK de `storage.objects` para `auth.users`?".
    - O documento registra `bucket_id`, contagem, `min(name)` e `max(name)` do bucket `curriculos`, e afirma explicitamente se todos os `name` casam com o prefixo `{authUid}/`.
    - O documento registra o `confdeltype` medido para `candidatos.user_id → auth.users`, para as 3 FKs `NO ACTION` do ERASE-08 e para as 5 FKs `SET NULL` do ERASE-09, com as contagens de linhas das cinco.
    - O documento registra o valor vivo de `NOTIFICACOES_MODO`.
    - A seção "O que isto muda no plano" existe e é não-vazia (ou afirma explicitamente "zero divergências medidas").
  </acceptance_criteria>
  <done>Existe um artefato datado no repositório que responde, com SQL executado ao vivo, todas as perguntas de estado de schema de que as migrations desta fase dependem — e ele tem precedência sobre qualquer arquivo de migration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Sonda de ESCRITA — hard delete de conta descartável COM histórico de funil</name>
  <files>.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-SONDAS-PROD.md</files>
  <read_first>
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-RESEARCH.md` § Open Questions, item 1 (o protocolo de 6 passos está escrito lá)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-RESEARCH.md` § Pitfall 2 (o 23503 previsto e por que a sequência de falha é pior que "não funciona")
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-PATTERNS.md` § No Analog Found (o precedente de `deleteUser` no repo engole o erro com `.catch()` — correto lá, catastrófico aqui)
    - `supabase/functions/cadastrar-candidato/index.ts` (linhas ~260-280 e ~385-395 — o `deleteUser` como rollback compensatório)
  </read_first>
  <what-built>
Nada de código. Esta é a única medição desta fase que **escreve**, e ela existe porque o desfecho
mais importante do ERASE-03/ERASE-10 — o `23503` do Pitfall 2 — é hoje uma previsão derivada de
`pg_constraint`, não um fato observado. `auth.users` tem 0 linhas soft-deletadas e nenhum código
deste projeto jamais chamou `deleteUser` sobre usuário com histórico: o caminho **nunca foi
exercitado**.
  </what-built>
  <how-to-verify>
Orquestrador, em ambiente controlado, **nunca sobre conta real**. Protocolo de seis passos:

1. Criar conta descartável via Auth Admin (e-mail com prefixo inequívoco, ex.
   `p45.sonda.descartavel+{timestamp}@teste.local`). Registrar o `user_id` e o `candidato.id`.
2. Criar uma candidatura para essa conta e forçar UMA transição de etapa com o JWT dela, de modo
   a gravar uma linha em `historico_candidatura` com `ator = {user_id}`. Confirmar por `SELECT`
   que a linha existe.
3. Subir um arquivo qualquer no bucket `curriculos` sob o prefixo daquela conta. Confirmar por
   `storage.objects`.
4. Chamar `auth.admin.deleteUser({user_id})` **sem severar nada** e **sem `.catch()` que engula**.
   Transcrever o corpo EXATO da resposta — status HTTP, mensagem, e o SQLSTATE se houver. Se a
   chamada tiver SUCESSO em vez de falhar, isso é a descoberta mais importante da fase e vai para
   o topo do documento: significa que a premissa do Pitfall 2 mudou.
5. Severar `historico_candidatura.ator = NULL` e `candidatos.user_id` (por `UPDATE` direto, já
   que a migration S1 ainda não existe — se `NOT NULL` impedir, **registrar isso como a prova
   executada da D-45-11**) e repetir o `deleteUser`. Transcrever o resultado.
6. Apagar tudo o que a sonda criou: o objeto do Storage pela Storage Admin API, as linhas de
   `candidaturas`/`candidatos`, e o usuário do Auth. Confirmar por contagem que `auth.users`
   voltou ao número medido na Task 1.

Acrescentar a `45-SONDAS-PROD.md` uma seção **"SONDA 6 (escrita) — hard delete com histórico"**
com o protocolo, os corpos de erro transcritos e a evidência de limpeza.
  </how-to-verify>
  <acceptance_criteria>
    - `45-SONDAS-PROD.md` contém a seção `SONDA 6` com o e-mail da conta descartável nomeado.
    - O corpo EXATO da resposta do `deleteUser` do passo 4 está transcrito — não parafraseado — incluindo status e SQLSTATE quando presentes.
    - O resultado do passo 5 (após severação) está transcrito.
    - A seção afirma se o `23503` previsto pelo Pitfall 2 ocorreu, não ocorreu, ou ocorreu com forma diferente.
    - A evidência de limpeza está registrada: contagem de `auth.users` de volta ao valor da Task 1, zero objeto residual no bucket sob o prefixo da conta descartável, zero linha residual em `candidatos`/`candidaturas` para aquele id.
    - Nenhum id, e-mail ou currículo de pessoa real aparece em passo algum da sonda.
  </acceptance_criteria>
  <resume-signal>Digite "sonda-6 registrada" quando `SONDA 6` estiver no documento com os corpos transcritos e a limpeza confirmada — ou descreva o que impediu.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Fechar G1 e G2 da Phase 44 — pré-condição declarada do portão destrutivo</name>
  <files>.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-SONDAS-PROD.md</files>
  <read_first>
    - `.planning/STATE.md` § "⚠ Phase 44 — `gaps_found` diferido por decisão do operador (2026-08-04)" (a tabela G1/G2/G3 e a consequência direta declarada para a Phase 45)
    - `.planning/phases/44-exporta-o-acesso/44-VERIFICATION.md`
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-RESEARCH.md` § Environment Availability (o fallback `npx supabase` + `supabase login`)
  </read_first>
  <what-built>
Nada de código nesta fase. O `STATE.md` escreve, com todas as letras, que **G1 e G2 devem fechar
antes do portão destrutivo** da Phase 45: a cadeia `44 → 45` é declarada estrita porque *"o
inventário do export **é** o plano de exclusão"*, e a cláusula do goal da 44 — *"exercitado em
produção"* — é exatamente a que falhou. A Phase 45 consumiria como plano de exclusão
**irreversível** um inventário que nunca foi exercido ponta a ponta.

**Isto não bloqueia planejar nem escrever código.** Bloqueia o apply destrutivo (45-11). Está
neste plano, e não no 45-11, porque descobrir no portão que o `supabase login` não passa é
descobrir tarde.
  </what-built>
  <how-to-verify>
1. **G2 (redeploy da EF `exportar-meus-dados`)** — PROD roda a **v1 pré-correção**, cujo cooldown
   falha ABERTO em timestamp ilegível; os 8 commits de fix estão no `main`. Executar
   `npx supabase login` e `npx supabase functions deploy exportar-meus-dados`. O caminho MCP não
   serve aqui (exigiria retranscrever ~45 KB de artefato gerado). Registrar a version resultante.
2. **G1 (exercitar o export ponta a ponta)** — sessão de navegador com conta de teste: clicar
   "Pedir uma cópia dos meus dados", confirmar que chegam DOIS arquivos e que
   `solicitacoes_dados` ganha 1 linha `tipo='acesso'`, `situacao='atendido'`, `causa` NULA.
3. Se o `supabase login` **não** passar (auth gate já registrado no 44-04), **parar e registrar**
   — isto é bloqueio de credencial do operador, não trabalho de agente, e é melhor conhecê-lo
   agora do que no portão.

Acrescentar a `45-SONDAS-PROD.md` uma seção **"Pré-condições do portão destrutivo (G1/G2)"** com
o estado de cada uma: fechada, ou bloqueada com o motivo medido.
  </how-to-verify>
  <acceptance_criteria>
    - `45-SONDAS-PROD.md` contém a seção "Pré-condições do portão destrutivo (G1/G2)".
    - G2: a version viva da EF `exportar-meus-dados` está registrada, e o documento afirma se ela é ou não a versão pós-correção.
    - G1: o documento registra a contagem de linhas de `solicitacoes_dados` antes e depois do exercício, ou o motivo medido pelo qual o exercício não pôde ser feito.
    - Se qualquer uma ficar aberta, o documento diz explicitamente que o portão destrutivo do 45-11 **não pode abrir** até que feche.
  </acceptance_criteria>
  <resume-signal>Digite "g1/g2 registrados" (fechados ou bloqueados-com-motivo) — não presuma o resultado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orquestrador → PROD via MCP `execute_sql` | Escrita e leitura privilegiadas sobre PII viva, sem RLS |
| sonda de escrita → `auth.users` / `storage.objects` | Criação e destruição de conta e de objeto reais no projeto de produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-01-01 | Tampering | sonda de escrita (Task 2) | critical | mitigate | Conta descartável com prefixo inequívoco criada pela própria sonda; nenhum id de pessoa real entra em passo algum; passo 6 é limpeza obrigatória com contagem de `auth.users` conferida contra o valor da Task 1 |
| T-45-01-02 | Information Disclosure | `45-SONDAS-PROD.md` | high | mitigate | O documento transcreve estrutura (constraints, `confdeltype`, contagens, prefixos), **nunca** valor de coluna de PII: sem e-mail real, sem CPF, sem `name` completo de objeto que embuta `authUid` de pessoa real — usar `min(name)`/`max(name)` mascarados quando o UUID for de conta real |
| T-45-01-03 | Repudiation | Task 3 (G1/G2) | high | mitigate | O estado de G1/G2 é registrado como fato datado; se bloqueado, o documento declara que o portão do 45-11 não abre — a ausência de registro nunca conta como "fechado" |
| T-45-01-04 | Denial of Service | `deleteUser` do passo 4 | medium | accept | A conta descartável não tem consumidor; um `deleteUser` que falhe deixa uma conta órfã que o passo 6 remove |
| T-45-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | **Nenhum pacote é instalado nesta fase.** `45-RESEARCH.md § Package Legitimacy Audit` registra zero pacotes, zero `[ASSUMED]`, zero `[SUS]`. `npx supabase` da Task 3 executa o CLI oficial do fornecedor já usado nas Phases 37/44 |
</threat_model>

<verification>
- `45-SONDAS-PROD.md` existe, é datado e está commitado.
- As seis sondas (5 read-only + 1 de escrita) têm SQL/protocolo E saída transcritos.
- A seção "O que isto muda no plano" é não-vazia ou afirma explicitamente zero divergências.
- Nenhuma migration foi escrita neste plano.
- `auth.users` voltou à contagem medida na Task 1 após a sonda de escrita.
</verification>

<success_criteria>
Nenhuma migration da Phase 45 pode ser escrita antes deste plano fechar. Ao fim, toda pergunta de
estado de schema de que o tombstone (45-07), a enumeração de Storage (45-10) e a asserção negativa
do ERASE-08 (45-11) dependem está respondida por SQL executado ao vivo e datado — não por arquivo
de migration, não por `docs/sql/sql/02-tabela-candidatos.sql` (de 2025, com drift documentado),
não por assunção.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (contribuição deste plano)

| Artefato | Tipo | Novo/Editado |
|---|---|---|
| `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-SONDAS-PROD.md` | documento de medição datado | **novo** |

Nenhum símbolo de código, nenhuma migration, nenhuma RPC, nenhuma EF, nenhum script npm.
</artifacts_produced>

<output>
Create `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-01-SUMMARY.md` when done
</output>
