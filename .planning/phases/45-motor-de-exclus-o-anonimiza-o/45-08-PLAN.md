---
phase: 45-motor-de-exclus-o-anonimiza-o
plan: 08
type: execute
wave: 3
depends_on: ["45-02", "45-03"]
files_modified:
  - src/features/privacidade/components/ConfirmarExclusaoDialog.tsx
  - src/features/privacidade/components/ReciboExclusao.tsx
  - src/features/privacidade/hooks/useCancelarExclusao.ts
  - src/features/privacidade/components/ExcluirDadosBloco.tsx
  - src/features/privacidade/services/exclusaoService.ts
  - src/features/privacidade/components/GuardaCurriculoBloco.tsx
autonomous: true
requirements: [ERASE-05, ERASE-06, ERASE-07]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "O diálogo de confirmação tem QUATRO rótulos de saída, todos distintos entre si, e a string genérica «Cancelar» não é rótulo de controle em nenhum dos dois diálogos da fase — nesta fase «Cancelar» significaria três coisas (fechar o diálogo / recuar um passo / cancelar a exclusão agendada)."
    - "A caixa de consequência do diálogo renderiza literalmente «não podem ser recuperados» e «Não existe cópia de reserva do seu currículo» — a tradução honesta do D-45-10, dita em linguagem de pessoa, sem «PITR», sem «backup», sem «Storage»."
    - "O parágrafo que aponta para «Pedir uma cópia dos seus dados» está presente e manda FECHAR o diálogo — sem `<a>` e sem `<button>` de navegação dentro dele."
    - "E3·loading: o diálogo NÃO faz leitura própria — recebe a data já resolvida do bloco. Não existe estado de carregamento dentro dele, porque um diálogo destrutivo que carrega dados enquanto está aberto pode mudar a frase debaixo do dedo de quem já leu."
    - "E3·overflow: `sm:max-w-lg` herdado, conteúdo de altura livre; a 320px com fonte ampliada o `DialogContent` rola internamente e o botão de confirmar fica no FIM do fluxo — nunca fixo acima do texto de consequência."
    - "E4·empty: coluna vazia é impossível por construção — as três linhas obrigatórias da coluna «mantém» e o cadastro na coluna «sai» existem em qualquer titular. Derivação vazia é FALHA DE DERIVAÇÃO, não estado vazio: o bloco não renderiza e o CTA é desabilitado com motivo."
    - "E4·loading: o recibo herda o pulsante do bloco — nunca aparece antes do estado que o contextualiza (futuro × passado)."
    - "E4·populated: duas colunas em `sm:grid-cols-2 gap-4`; as três linhas obrigatórias sempre presentes; cada linha da coluna «mantém» com a citação do artigo a 14px/600 ao lado, nunca em nota de rodapé nem em tooltip."
    - "E4·overflow: abaixo de `sm:`, dois grupos EMPILHADOS com os mesmos dois cabeçalhos, na ordem «sai» → «mantém». Nunca `<table>`, nunca scroll horizontal, nunca truncamento."
    - "E4·zero-one-many: uma linha, várias linhas e linha ausente têm o mesmo tratamento (lista de itens rotulados); o cabeçalho é neutro quanto ao número, então não há singular/plural a errar."
    - "E4·long-text: item e citação legal renderizam ÍNTEGROS — sem `truncate`, sem `line-clamp`, sem `title` como substituto. Truncar a base legal de uma retenção é apagar a justificativa que a torna legítima."
    - "As duas colunas do recibo têm tratamento visual IDÊNTICO (`border-white/15 bg-white/5`): pintar «sai» de vermelho e «mantém» de verde transformaria um relato factual num julgamento."
    - "E5·loading: no cancelamento em voo o botão mostra «Cancelando…», desabilitado, `aria-busy`, com motivo irmão — e a data-alvo PERMANECE visível durante o voo, porque sumi-la faria parecer que o cancelamento já valeu."
    - "E5·error: a copy de erro do cancelamento diz que o pedido CONTINUA agendado para a data e nomeia o canal humano — nunca a mensagem crua de transporte, nunca «tente novamente» genérico."
    - "E5·overflow: altura livre em `max-w-2xl`; o recibo abaixo empilha a 320px."
    - "Estado C (execução em andamento) existe, não oferece ação nenhuma — sem cancelar, sem tentar de novo, sem barra de progresso — e nenhuma palavra de desfecho aparece nele."
    - "`GuardaCurriculoBloco.notaRevogacao` foi reescrita (Emenda B): aponta para «Apagar meus dados» no fim da página E mantém nomeado o canal do Encarregado de Dados para a revogação isolada. É a ÚNICA string de outra fase alterada; `AutorizacoesLista`, `PedirCopiaBloco` e `CurriculosBloco` ficam byte-idênticos."
    - { statement: "E3·error — os quatro rótulos de saída dos dois diálogos são distintos entre si, e a string «Cancelar» sozinha não aparece como rótulo de controle em nenhum dos dois. A asserção percorre os rótulos renderizados, não o código-fonte.", verification: backstop }
    - { statement: "E3·long-text — a caixa de consequência não pode ser encurtada por uma edição futura «de tom»: a asserção exige a presença literal das duas frases que traduzem o D-45-10 no corpo renderizado do diálogo.", verification: backstop }
    - { statement: "E4·error — o recibo não afirma mais do que o motor faz: o teste confronta as linhas da coluna «sai» com o inventário derivado e falha quando existe linha sem caminho de código correspondente. Uma asserção de snapshot do texto NÃO serve — ela passaria numa lista honesta hoje e continuaria passando depois de o motor deixar de apagar algo.", verification: backstop }
    - { statement: "E4·partial — nos dois recortes perigosos (titular sem currículo, titular sem decisão registrada) a linha inaplicável é OMITIDA: nunca renderizada vazia e nunca renderizada mesmo assim. Prometer apagar um arquivo que não existe é superestimar na direção oposta, e é igualmente proibido.", verification: backstop }
    - { statement: "E5·long-text — a nota do que não volta não pode desaparecer numa edição de concisão: em qualquer render do Estado B, a menção a cancelar e a frase sobre as candidaturas não reabrirem aparecem juntas.", verification: backstop }
  artifacts:
    - src/features/privacidade/components/ConfirmarExclusaoDialog.tsx
    - src/features/privacidade/components/ReciboExclusao.tsx
    - src/features/privacidade/hooks/useCancelarExclusao.ts
  key_links:
    - "`reciboExclusao.generated.ts` (45-02) → `ReciboExclusao.tsx`: as duas colunas são DERIVADAS, nunca digitadas (Invariante 4 da UI-SPEC)"
    - "`cancelar_pedido_exclusao` (45-03) → `useCancelarExclusao` → o painel do Estado B: cancelar interrompe a exclusão e NÃO reabre candidatura alguma"
    - "Emenda B → `GuardaCurriculoBloco`: a copy da Phase 43 fica FALSA no dia em que esta fase embarcar, e a página passaria a se contradizer dentro de um scroll"
  prohibitions:
    - "MUST NOT usar dark pattern, contra-oferta, atraso artificial, digitação-refém ou omissão do fato de que não existe cópia de reserva para dissuadir ou obstruir o exercício do direito de exclusão — a rede de segurança desta fase é a janela cancelável, e fricção sobre um direito do Art. 18 não é rede, é obstáculo."
    - "MUST NOT declarar, em tela alguma, que a exclusão foi concluída antes de os três sistemas confirmarem; e MUST NOT usar a palavra de totalidade sobre os dados do titular, porque a justificativa do recrutador sobrevive sem ligação com ele e a trilha de decisão sobrevive inteira."
---

<objective>
A superfície onde a decisão irreversível realmente acontece: o diálogo de confirmação, o recibo em
duas colunas, o painel da janela com o cancelamento, o estado de execução, e a emenda à string da
Phase 43 que ficaria falsa.

Purpose: nas Phases 42–44 a copy podia mentir e o dano era retratável por escrito. Aqui não. Com o
PITR desligado por decisão datada (D-45-10) e o Storage fora de qualquer caminho de backup, **um
currículo apagado por engano é irrecuperável por qualquer meio**. As strings deste plano são,
literalmente, a última coisa que uma pessoa lê antes de um efeito que ninguém pode desfazer — e um
executor que "melhore" a redação de qualquer linha da §Copywriting Contract sem passar pela UI-SPEC
não está editando texto, está alterando o consentimento sob o qual um dado é destruído.

Output: `ConfirmarExclusaoDialog`, `ReciboExclusao` (um componente, dois tempos verbais),
`useCancelarExclusao`, os Estados B e C completos, e a Emenda B.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-UI-SPEC.md
@.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-PATTERNS.md
@.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-03-SUMMARY.md
@.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: `ConfirmarExclusaoDialog` — a confirmação aninhada, sem digitação-refém</name>
  <files>src/features/privacidade/components/ConfirmarExclusaoDialog.tsx, src/features/privacidade/components/ExcluirDadosBloco.tsx</files>
  <read_first>
    - `src/features/admin/retencao/components/EditarJanelaDialog.tsx` (306 linhas INTEIRAS — as quatro regras do docblock `:12-39` com a de rótulos distintos, a copy como constante `as const` `:75-104`, a estrutura `Dialog` → `AlertDialogTrigger asChild` → `AlertDialogContent` `:161-301`, a armadilha WR-09 de `:264-272` — `AlertDialogContent` renderizado com condição MAIS ESTRITA que a do CTA produziu um diálogo vazio: sem confirmação, sem erro, sem salvar — e a regra de foco delegado ao Radix)
    - `src/features/privacidade/components/ExcluirDadosBloco.tsx` (o arquivo criado em 45-03, e `COPY_EXCLUIR_DADOS`)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-UI-SPEC.md` § "O `AlertDialog` de confirmação" (a tabela de copy INTEIRA — título, três parágrafos, caixa de consequência, os dois rótulos), Invariantes 3/6/7/12, § Color (a lista reservada de três itens do destructive), § Acessibilidade
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-PATTERNS.md` §11
  </read_first>
  <behavior>
    - O diálogo abre a partir do CTA glass-branco do bloco; o gatilho NÃO é vermelho.
    - Dentro dele, e só dentro dele, o `AlertDialogAction` é destructive e a caixa de consequência tem tratamento destructive.
    - Quatro rótulos de saída, todos distintos; nenhum é a string genérica de recuo.
    - O foco entra no recuo, nunca no botão de confirmar — um `Enter` reflexo não confirma uma exclusão.
    - A caixa de consequência é ligada ao diálogo por `aria-describedby`: ela É a descrição, não um enfeite ao lado dela.
    - O diálogo não faz leitura própria; recebe a data já resolvida.
    - Não existe campo de texto: nada de transcrever palavra para confirmar.
    - O `AlertDialogContent` é montado sob condição **não mais estrita** que a do gatilho (WR-09).
  </behavior>
  <action>
Criar `ConfirmarExclusaoDialog.tsx` copiando a **estrutura** do `EditarJanelaDialog` — e **nunca** a
copy dele. `Dialog` → `DialogContent className="border-white/25 bg-[#00109E]/95 text-white
backdrop-blur-xl sm:max-w-lg"` → `DialogHeader` com `DialogTitle text-xl font-semibold` →
corpo → `DialogFooter` com o recuo largo e o `AlertDialogTrigger asChild` envolvendo o
`GlassButton variant="white"`.

**Copy como constante exportada `as const`** (`COPY_CONFIRMAR_EXCLUSAO`), verbatim da UI-SPEC
§ "O `AlertDialog` de confirmação", com um comentário apontando a seção de origem — nunca literal
no JSX. Os quatro rótulos de saída são distintos entre si e nenhum é o verbo genérico de recuo. A
UI-SPEC explica por que a colisão aqui é **pior** que no molde: aquele verbo é também o nome da
ação de **cancelar a exclusão** no Estado B, e a mesma palavra para três saídas diferentes numa
superfície irreversível é a fábrica de erro que esta fase existe para não construir.

**A caixa de consequência** usa o único tratamento destructive permitido fora dos alertas de erro:
`rounded-lg border border-destructive/40 bg-destructive/10 p-4`. Ela carrega as duas frases que
traduzem o D-45-10 para linguagem de pessoa — sem nome de tecnologia — e é ligada ao diálogo por
`aria-describedby`.

**O parágrafo que aponta para o export é obrigatório**, e é a razão de isto ser um diálogo em vez
de um tooltip: o único momento em que o titular ainda pode salvar os próprios dados é **antes** de
confirmar, e é neste instante que ele está pensando no assunto — oferecer o export depois seria
oferecer o resgate ao lado do naufrágio. O ponteiro é **textual, sem botão e sem link**: ele manda
**fechar** o diálogo, não navegar dentro dele.

**Sem digitação-refém** (Invariante 7). Exigir transcrição de uma palavra para exercer um direito
do Art. 18 é fricção sobre um direito, numa superfície mobile-first, para um público que a Phase 43
já obrigou a escrever em linguagem simples. A rede é a janela cancelável, que protege sem obstruir.

**Foco:** o Radix cuida — **não sobrescrever** —, exceto por garantir que o primeiro elemento
focável do `AlertDialogContent` seja o recuo, não a confirmação.

⚠ **WR-09:** montar o `AlertDialogContent` sob condição **não mais estrita** que a do gatilho. No
molde, uma condição mais estrita produziu um diálogo vazio — *"sem confirmação, sem erro, sem
salvar: uma mudança válida sem caminho até o servidor"*. Aqui o efeito seria um titular que confirma
e não acontece nada, numa tela cuja premissa inteira é honestidade.

Editar `ExcluirDadosBloco.tsx` apenas para montar o diálogo a partir do CTA e passar a data já
resolvida — nenhuma outra mudança estrutural.
  </action>
  <verify>
    <automated>npx vitest run src/features/privacidade/components/__tests__/ConfirmarExclusaoDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/features/privacidade` verde, com arquivo de teste novo para o diálogo.
    - Teste que coleta os rótulos RENDERIZADOS dos controles de saída dos dois níveis e assere que os quatro são distintos entre si e que nenhum é a string genérica de recuo — a asserção é sobre o rótulo renderizado, não sobre o código-fonte.
    - Teste que assere a presença literal, no corpo renderizado, das duas frases da caixa de consequência que traduzem o D-45-10.
    - Teste que assere **ausência** de qualquer `<a href>` ou `<button>` de navegação para a rota de privacidade dentro do `DialogContent`.
    - Teste que assere ausência de qualquer `<input>`/`<textarea>` dentro dos dois níveis do diálogo.
    - Teste que assere que o primeiro elemento focável do `AlertDialogContent` é o controle de recuo.
    - O `AlertDialogContent` é montado sob a MESMA condição do gatilho (ou mais frouxa) — verificável por teste que abre o diálogo no estado mínimo e encontra o conteúdo.
    - Todo controle acionável tem `min-h-[44px]`; a caixa de consequência é referenciada por `aria-describedby`.
    - O gatilho que abre o diálogo NÃO tem classe destructive; o `AlertDialogAction` interno tem.
  </acceptance_criteria>
  <done>A pessoa lê, antes de confirmar, que não existe cópia de reserva — e o único caminho para o botão vermelho passa por esse texto, sem fricção artificial e sem rótulo ambíguo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `ReciboExclusao` — um componente, dois tempos, duas colunas derivadas</name>
  <files>src/features/privacidade/components/ReciboExclusao.tsx</files>
  <read_first>
    - `src/features/privacidade/constants/reciboExclusao.generated.ts` (o artefato gerado em 45-02 — `RECIBO_EXCLUSAO` com `colunas_sai[]`, `colunas_mantem[]`, `passos_motor[]`, `texto_futuro`, `texto_passado`, `aplicavel_quando`, `base_legal`)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-UI-SPEC.md` § "O recibo em duas colunas" (as cinco regras, os cabeçalhos fixos, as três linhas obrigatórias, o vocabulário travado «sem ligação com você» e os cinco termos proibidos, o layout `sm:grid-cols-2` × empilhado), § Color § "As duas colunas do recibo têm tratamento visual IDÊNTICO", § Typography, § UI Considerations E4 (as oito linhas), § Acessibilidade (cabeçalho REAL `<h3>`/`<dt>`, não `<div>` em negrito)
    - `src/features/privacidade/components/PedirCopiaBloco.tsx` (o container neutro e o idioma de tipografia)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-PATTERNS.md` §10
  </read_first>
  <behavior>
    - `tempo="futuro"` renderiza `texto_futuro` e os cabeçalhos de futuro; `tempo="passado"` renderiza `texto_passado` e os de passado. Um componente, dois tempos.
    - Em `sm:` e acima: duas colunas `sm:grid-cols-2 gap-4`. Abaixo de `sm:`: dois grupos empilhados, cada um com o SEU cabeçalho, na ordem «sai» → «mantém».
    - As três linhas obrigatórias da coluna «mantém» sempre aparecem.
    - Cada linha de «mantém» renderiza a citação do artigo a 14px/600 ao lado do item — nunca em rodapé, nunca em tooltip.
    - Item e citação renderizam íntegros: sem `truncate`, sem `line-clamp`, sem `title` como substituto.
    - `temCurriculo=false` omite a linha do currículo; `temDecisaoRegistrada=false` omite a linha da justificativa. Omite — não renderiza vazia, não renderiza mesmo assim.
    - Derivação vazia (`colunas_sai` ou `colunas_mantem` sem itens aplicáveis) → o componente devolve `null` e sinaliza falha de derivação ao pai, que desabilita o CTA com motivo. Nunca renderiza um recibo vazio ao lado de um botão que apaga.
    - As duas colunas têm as MESMAS classes de container.
  </behavior>
  <action>
Criar `ReciboExclusao.tsx` consumindo `RECIBO_EXCLUSAO` do espelho gerado. **Zero linha digitada
no componente** — a Invariante 4 é o mecanismo, e o precedente é o EXPORT-04 desta mesma cadeia: o
inventário é gerado, nunca digitado.

Props: `tempo: 'futuro' | 'passado'`, `temCurriculo: boolean`, `temDecisaoRegistrada: boolean`,
`onFalhaDerivacao?: () => void`. **Um componente, dois tempos** — dois componentes divergiriam na
primeira edição, e a divergência apareceria justamente entre o que foi prometido e o que foi
relatado.

Filtrar por `aplicavel_quando` antes de renderizar. Se o filtro esvaziar qualquer das duas colunas,
chamar `onFalhaDerivacao` e devolver `null`: **um recibo vazio ao lado de um botão que apaga seria
a pior tela desta fase**, e a UI-SPEC classifica isso como falha de derivação, não como estado vazio.

**Layout — e "duas colunas" é uma relação semântica, não uma `<table>`.** `sm:grid-cols-2 gap-4`
acima de `sm:`; abaixo, dois grupos **empilhados** com os **mesmos dois cabeçalhos**, na ordem
«sai» → «mantém». Nunca scroll horizontal, nunca truncamento, nunca uma `<table>` de 320px. A
leitura em coluna única preserva o pareamento **porque cada grupo carrega o próprio cabeçalho** —
é por isso que o cabeçalho é obrigatório nos dois breakpoints. Cabeçalho **real** (`<h3>` ou `<dt>`),
não um `<div>` em negrito: em coluna única ele é a única coisa que preserva o pareamento para o
leitor de tela.

**Tratamento visual IDÊNTICO nas duas colunas** (`rounded-lg border border-white/15 bg-white/5 p-4`).
Pintar «sai» de vermelho e «mantém» de verde transformaria um relato factual num julgamento — como
se preservar a prova de não-discriminação exigida pelo Art. 7º, VI fosse uma concessão ou uma falha.
A distinção é feita por **cabeçalho e conteúdo**, canais que sobrevivem ao daltonismo, ao leitor de
tela e à impressão em preto e branco.

**Tipografia:** item em 16px/400 (`text-base leading-relaxed`), citação do artigo em 14px/600
(`text-sm font-semibold`). **Zero `text-xs`** — o 5º tamanho não é criado por esta fase, inclusive
na citação legal.

**Vocabulário travado:** a coluna «mantém» usa «sem ligação com você». Os cinco termos de engenharia
e de jurista que a UI-SPEC proíbe nesta superfície já são barrados na origem pelo gerador de 45-02;
o componente não os reintroduz porque não digita texto nenhum.

⚠ **O recibo não é um arquivo.** Não é baixável, não vira `.pdf` nem `.json`, e **não** reusa o
mecanismo de download da Phase 44. Quem quer o próprio dado usa «Pedir uma cópia dos seus dados» —
que existe e é o direito do Art. 18, II. Um segundo caminho de download aqui seria uma segunda
superfície de exfiltração pelo preço de zero função nova.
  </action>
  <verify>
    <automated>npx vitest run src/features/privacidade/components/__tests__/ReciboExclusao.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Teste que renderiza com `tempo="futuro"` e com `tempo="passado"` e assere textos diferentes vindos do mesmo item.
    - Teste que confronta as linhas renderizadas da coluna «sai» com `RECIBO_EXCLUSAO.passos_motor` e falha quando existe linha sem `passo_motor` correspondente. Nenhum `toMatchSnapshot()` sobre o texto do recibo.
    - Teste dos dois recortes (`temCurriculo=false`, `temDecisaoRegistrada=false`) que assere OMISSÃO da linha inaplicável — e que nenhuma linha renderizada tem texto vazio.
    - Teste que assere que as três linhas obrigatórias da coluna «mantém» estão presentes em todos os recortes.
    - Teste que assere que cada linha de «mantém» renderiza a citação legal no MESMO nó de lista do item (não em rodapé, não em `title`).
    - Teste que assere que o `className` do container da coluna «sai» é idêntico ao da coluna «mantém».
    - Com as linhas de comentário removidas, o componente tem **zero** ocorrências de `<table`, `truncate`, `line-clamp`, `overflow-x` e do token de fonte de 12px.
    - Teste que, com derivação vazia, assere que o componente devolve `null` e chama `onFalhaDerivacao`.
    - Os cabeçalhos são elementos de cabeçalho reais (`<h3>` ou `<dt>`), verificável por `getByRole('heading')` / `getAllByRole('term')`.
  </acceptance_criteria>
  <done>O titular vê, antes e depois, exatamente o que sai e o que fica — e o texto vem do inventário, não da mão de quem escreveu o componente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Estados B e C, o cancelamento, e a Emenda B da Phase 43</name>
  <files>src/features/privacidade/hooks/useCancelarExclusao.ts, src/features/privacidade/services/exclusaoService.ts, src/features/privacidade/components/ExcluirDadosBloco.tsx, src/features/privacidade/components/GuardaCurriculoBloco.tsx</files>
  <read_first>
    - `src/features/privacidade/components/ExcluirDadosBloco.tsx` e `src/features/privacidade/services/exclusaoService.ts` (os arquivos de 45-03)
    - `src/features/privacidade/hooks/useExportarMeusDados.ts` (mutation sem `onMutate`, sem toast, sem `onError`)
    - `src/features/privacidade/components/GuardaCurriculoBloco.tsx` (`:57` — a string `notaRevogacao` viva, aprovada na Phase 43, e o `COPY_GUARDA_CURRICULO`)
    - `src/features/privacidade/components/PedirCopiaBloco.tsx` (`:175-182` o sucesso PERSISTENTE no mesmo container neutro; `:187-195` o alerta inline destructive `role="alert"`)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-UI-SPEC.md` § "Estado B", § "Estado C", § "Emenda B", Invariantes 3/5/11, § UI Considerations E5, § Formatação, § Acessibilidade
  </read_first>
  <behavior>
    - Estado B renderiza «Exclusão agendada», a data por extenso, a nota do que não volta, o recibo em tempo futuro visível SEM clique, e o botão «Cancelar a exclusão» glass-branco — nunca destructive, porque cancelar é a ação construtiva deste fluxo.
    - Cancelamento em voo: rótulo de voo, `disabled`, `aria-busy`, motivo irmão visível — e a data-alvo permanece visível.
    - Sucesso do cancelamento: mensagem PERSISTENTE no container neutro, nunca toast, afirmando que os dados continuam e que as candidaturas encerradas não foram reabertas. `aria-live="polite"`.
    - Erro do cancelamento: copy própria dizendo que o pedido CONTINUA agendado para a data, com o canal humano nomeado. `role="alert"`.
    - Estado C: título e corpo de execução em andamento, **zero** ação — sem cancelar, sem tentar de novo, sem barra de progresso, sem porcentagem, sem detalhe de qual sistema respondeu.
    - `GuardaCurriculoBloco.notaRevogacao` reescrita; nenhuma outra string daquele arquivo muda.
    - Data inválida ou ausente em qualquer sítio: a frase que a conteria é OMITIDA — nunca travessão, nunca `Invalid Date`, nunca `NaN`.
  </behavior>
  <action>
`exclusaoService.ts` — acrescentar `invocarCancelarExclusao(solicitacaoId)` e os códigos de erro do
cancelamento ao vocabulário fechado de `ExclusaoError`, com fallback total. Nenhuma mensagem crua do
transporte cruza para a tela.

`useCancelarExclusao.ts` — `useMutation` **sem `onMutate`**, **sem toast**, **sem `onError`**;
invalida a query de `usePedidoExclusao`. A proibição de UI otimista é ainda mais dura aqui: a tela
nunca declara conclusão antes dos três sistemas confirmarem.

`ExcluirDadosBloco.tsx` — acrescentar os Estados B e C ao switch de estado já existente.

**Estado B.** Substitui o CTA. O bloco **não** deixa de existir e **não** vira uma linha de status
discreta: é o estado mais importante que esta página pode carregar. Título, linha de estado com a
data **por extenso** (`dd/mm/aaaa` via `toLocaleDateString('pt-BR', …)`), corpo, a **nota do que não
volta**, o `ReciboExclusao tempo="futuro"` visível **sem clique** (uma janela de arrependimento só
vale se a pessoa souber do que se arrepender — sem a prévia, os dias são espera, não escolha), e o
botão de cancelar **glass-branco**.

A copy de erro do cancelamento é o **inverso exato** da copy de erro do pedido, e a assimetria é
deliberada: falhar ao *pedir* deixa a pessoa segura ("nada foi apagado"); falhar ao *cancelar* deixa
a pessoa **em risco**, e a tela tem de dizer isso de frente, com a data e com uma saída humana. Uma
mensagem genérica de "tente novamente" aqui seria a tela escondendo da pessoa que o relógio continua
correndo.

⚠ A frase "Nada foi apagado" é **permitida apenas no erro de registro do pedido**, que acontece
antes de qualquer mutação. Ela é **proibida** em qualquer erro posterior ao início da execução, onde
seria uma afirmação que ninguém pode garantir.

**Estado C.** Alcançável em produção — a mutação `Storage → Postgres → Auth` não é atômica, e o
estado "Storage apagado, Postgres ainda não" é real, não hipotético. Título, corpo dizendo que o
titular não precisa fazer nada, e **ação nenhuma**: sem cancelar (a janela acabou), sem tentar de
novo (a retomabilidade é do motor — um retry na mão do titular seria convidá-lo a re-disparar uma
mutação destrutiva não-atômica), sem barra de progresso. Proibido neste estado: qualquer palavra de
desfecho, qualquer porcentagem, qualquer detalhe de qual sistema já respondeu.

**Emenda B.** Editar **somente** `COPY_GUARDA_CURRICULO.notaRevogacao`, com a copy nova da UI-SPEC
§ Emenda B. Motivo: depois desta fase a mesma página passa a oferecer a eliminação **duas seções
abaixo**, em autoatendimento; mandar a pessoa escrever um e-mail para fazer o que o botão logo
abaixo faz é a página se contradizendo dentro de um scroll. Duas propriedades preservadas de
propósito: o canal humano **continua nomeado** (Art. 8º §5, e é a única saída de quem não consegue
entrar na conta), e a frase **não promete** que a revogação isolada da autorização virará
autoatendimento — prometer roadmap ao titular é proibido desde a Phase 43. **Zero mudança
estrutural** naquele arquivo, e `AutorizacoesLista`, `PedirCopiaBloco` e `CurriculosBloco` ficam
byte-idênticos.
  </action>
  <verify>
    <automated>npx vitest run src/features/privacidade &amp;&amp; npm run lint 2>&amp;1 | grep -c "error TS" | awk '{ if ($1 > 97) { print "REGRESSAO tsc: " $1 " > 97"; exit 1 } else print "tsc " $1 " <= 97" }'</automated>
  </verify>
  <acceptance_criteria>
    - Teste de coocorrência do Estado B: em qualquer render dele, a menção a cancelar e a frase sobre as candidaturas não reabrirem aparecem juntas. Um grep de ausência não satisfaz este critério.
    - Teste que assere que a data-alvo continua visível durante o cancelamento em voo.
    - Teste que assere que o botão de cancelar **não** carrega classe destructive.
    - Teste do erro de cancelamento que assere a presença da data e do endereço do Encarregado de Dados, e a **ausência** de qualquer mensagem genérica de nova tentativa.
    - Teste do Estado C que assere **zero** elemento `role="button"` dentro do bloco, e zero ocorrência de palavra de desfecho no texto renderizado.
    - Teste que assere que a frase «Nada foi apagado» aparece **somente** no erro de registro do pedido, e não nos erros de cancelamento nem no Estado C.
    - Teste que assere que o recibo em tempo futuro está visível no Estado B sem nenhuma interação.
    - Teste que, com data inválida/ausente, assere que a frase que a conteria foi OMITIDA — sem travessão, sem `Invalid Date`, sem `NaN`.
    - `git diff src/features/privacidade/components/GuardaCurriculoBloco.tsx` mostra alteração em exatamente uma entrada de copy; nenhuma mudança estrutural.
    - `git status` não lista `AutorizacoesLista.tsx`, `PedirCopiaBloco.tsx` nem `CurriculosBloco.tsx` como modificados.
    - `npm run lint` reporta contagem `tsc` ≤ 97.
    - `npm run check:recibo-exclusao` continua saindo 0 — o espelho gerado do frontend não foi editado à mão.
  </acceptance_criteria>
  <done>A janela de arrependimento é utilizável de verdade: o titular vê a data, vê o que sai e o que fica, cancela quando quer, e a tela nunca finge que a exclusão acabou.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy renderizada → decisão irreversível do titular | O texto é o consentimento; alterá-lo altera o que a pessoa aceitou |
| artefato gerado → componente | Uma linha de recibo divergente afirma um apagamento que o motor não faz |
| navegador → EF (cancelamento) | Mutação de estado legal disparada do cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-08-01 | Repudiation | copy do diálogo de confirmação | critical | mitigate | Copy como constante `as const` verbatim da UI-SPEC; testes de presença literal das duas frases que traduzem o D-45-10; testes de rótulos distintos sobre o texto RENDERIZADO |
| T-45-08-02 | Repudiation | `ReciboExclusao` | high | mitigate | Componente não digita texto: consome o espelho gerado sob `--check`; teste confronta linha «sai» × `passo_motor`, sem snapshot |
| T-45-08-03 | Information Disclosure | Estado C | high | mitigate | Zero detalhe de qual sistema respondeu, zero identificador interno (Invariante 12); teste assere ausência de palavra de desfecho e de qualquer controle |
| T-45-08-04 | Tampering | Emenda B | medium | mitigate | Diff restrito a uma entrada de copy; verificação assere que os três blocos irmãos não aparecem como modificados |
| T-45-08-05 | Elevation | `invocarCancelarExclusao` | medium | mitigate | O cancelamento passa pela EF com `verify_jwt: true`; o `solicitacao_id` é validado no servidor contra `auth.uid()`, e a UI nunca é a autoridade |
| T-45-08-06 | Repudiation | WR-09 (diálogo vazio) | high | mitigate | `AlertDialogContent` montado sob condição não mais estrita que a do gatilho; teste abre o diálogo no estado mínimo e exige o conteúdo |
| T-45-08-07 | Denial of Service | recibo com derivação vazia | high | mitigate | Derivação vazia devolve `null` e desabilita o CTA com motivo — nunca um recibo vazio ao lado de um botão que apaga |
| T-45-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | **Zero instalação.** `45-RESEARCH.md § Package Legitimacy Audit` registra zero pacotes; `lucide-react` e `@tanstack/react-query` já são dependências; explicitamente rejeitadas: qualquer biblioteca de PDF, de tabela ou de toast |
</threat_model>

<verification>
- `npx vitest run src/features/privacidade` verde, com os três arquivos de teste novos.
- `npm run lint` ≤ 97 erros `tsc`.
- `npm run check:recibo-exclusao` sai 0 (espelhos gerados intactos).
- `npm run build` verde com `postbuild`.
- `git status` confirma que `AutorizacoesLista.tsx`, `PedirCopiaBloco.tsx` e `CurriculosBloco.tsx` não foram tocados.
- Zero commit com `--no-verify`.
</verification>

<success_criteria>
A janela de arrependimento existe de verdade na tela: a pessoa sabe o que vai sair, o que vai ficar
e por qual artigo, sabe que não há cópia de reserva, e pode cancelar — sem que nenhuma tela minta
sobre o que já aconteceu.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (contribuição deste plano)

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `ConfirmarExclusaoDialog`, `COPY_CONFIRMAR_EXCLUSAO` | componente + copy `as const` | **novos** |
| `ReciboExclusao` (props `tempo`, `temCurriculo`, `temDecisaoRegistrada`, `onFalhaDerivacao`) | componente | **novo** |
| `useCancelarExclusao` | hook | **novo** |
| `invocarCancelarExclusao` + códigos de erro de cancelamento em `ExclusaoError` | service | **editado** |
| `ExcluirDadosBloco` — Estados B e C | componente | **editado** |
| `COPY_GUARDA_CURRICULO.notaRevogacao` | string (Emenda B) | **editada** |
</artifacts_produced>

<output>
Create `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-08-SUMMARY.md` when done
</output>
