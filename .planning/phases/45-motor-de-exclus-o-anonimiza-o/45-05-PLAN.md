---
phase: 45-motor-de-exclus-o-anonimiza-o
plan: 05
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - supabase/migrations/20260805000003_p45_bias_k5.sql
  - supabase/tests/p45_bias_k5_smoke.sql
autonomous: true
requirements: [ERASE-01]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`public.candidatos.faixa_etaria_materializada text` existe, e `gerar_bias_snapshot()` a lê com precedência SOBRE a derivação de `data_nascimento` (`COALESCE(faixa_etaria_materializada, <faixa derivada>)`) — a sentinela escrita em `data_nascimento` pelo tombstone nunca é lida como faixa real."
    - "Rodar o snapshot ANTES e DEPOIS de anonimizar um candidato sintético produz a MESMA composição de coorte para aquele candidato: ele continua na mesma faixa etária, porque a faixa foi materializada antes de `data_nascimento` morrer (SC#5)."
    - "A linha com tombstone NÃO entra em `excluidos_sem_data` — hoje esse contador conta `idade IS NULL OR idade < 18`, e um titular anonimizado cairia lá dentro, mudando o denominador da série retroativamente."
    - "Toda célula com menos de 5 candidatos tem a CONTAGEM oculta e a PRESENÇA declarada — some o número, não o fato de a faixa existir (D-45-04, k=5)."
    - "Quando existe supressão primária, existe supressão COMPLEMENTAR: `n_total` ou a segunda menor faixa também é suprimida, de modo que o leitor não recupere a célula por subtração dos totais marginais."
    - "Nenhum campo derivado de uma célula suprimida é publicado: `selection_rate`, `razao_4_5`, `flag` e `faixa_referencia` da faixa suprimida saem do payload — publicar a razão 4/5 de uma célula suprimida devolve a contagem por outro caminho quando `selected` é pequeno."
    - "`small_sample_warning` (< 30) permanece como sinal SEPARADO e com o limiar inalterado (D-45-05): o `< 30` é sinal estatístico (a razão 4/5 é instável), o `< 5` é controle de re-identificação — os dois não colapsam num só número."
    - "As linhas já gravadas em `bias_audit_log` permanecem BYTE-IDÊNTICAS após esta migration: não há FK de `bias_audit_log` para `candidatos`, e a migration não faz `UPDATE` nem `DELETE` naquela tabela."
    - "`gerar_bias_snapshot` recriada não é executável por `anon`, `authenticated` nem PUBLIC, e o guard recusa com 42501 tanto o papel errado quanto o chamador sem claim nenhuma."
  artifacts:
    - supabase/migrations/20260805000003_p45_bias_k5.sql
    - supabase/tests/p45_bias_k5_smoke.sql
  key_links:
    - "`candidatos.faixa_etaria_materializada` → 45-07 (o tombstone TEM de preencher esta coluna ANTES de escrever a sentinela em `data_nascimento`; a ordem é o ERASE-01 inteiro)"
    - "`gerar_bias_snapshot` recriada → o recibo (45-02, linha obrigatória «números agregados usados no relatório de não-discriminação»): a promessa de que ninguém chega ao titular a partir deles é o k=5 com supressão complementar"
    - "restrição de ordenação #1 do ROADMAP → este plano é a primeira wave de execução do ERASE-01, e tem de estar PROVADO antes de o ERASE-02 tocar qualquer linha real"
  prohibitions:
    - "MUST NOT apresentar como anonimizado um relatório do qual uma pessoa possa ser recuperada: uma faixa com 1–2 candidatos somada ao desfecho de seleção re-identifica alguém dentro do relatório que existe justamente para provar não-discriminação, e suprimir a célula sem suprimir o complemento não suprime nada."
---

<objective>
Fechar o ERASE-01: materializar a faixa etária **antes** de qualquer anonimização e recriar
`gerar_bias_snapshot()` com supressão k=5 **e complementar**.

Purpose: a restrição de ordenação nº 1 do ROADMAP é explícita — *"anonimizar antes de
materializar a faixa etária corrompe a série EEOC 4/5 **permanentemente**"*. `gerar_bias_snapshot()`
deriva a idade por JOIN vivo em `candidatos.data_nascimento` **no momento do snapshot**, e
`data_nascimento` é `NOT NULL` com `CHECK (< CURRENT_DATE)`: a sentinela do tombstone **cairia numa
faixa real** se a idade continuasse sendo derivada. Materializar antes e ler com precedência é o
que impede a série histórica de mudar retroativamente.

E o segundo pedaço é o D-45-04: hoje a função só **sinaliza** (`small_sample_warning < 30`) e
**nunca suprime**. Depois da anonimização a coorte encolhe, e uma faixa com 1–2 candidatos mais o
desfecho de seleção re-identifica a pessoa dentro do relatório que existe para provar
não-discriminação.

Este plano roda na wave 2, junto com o tracer, porque ele é **pré-requisito de ordenação** do
tombstone (45-07) — a coluna que o tombstone preenche nasce aqui.

⚠ Escreve migration, **não aplica**. O apply é 45-11 (portão destrutivo), com esta migration na
frente da `p45_anonimizar_candidato`.

## O que "não pode ser tocado" significa aqui — e o executor precisa ler isto antes de travar

O `45-CONTEXT.md § O que NÃO pode ser tocado` nomeia o **arquivo de migration**
`20260625100001_decisao_final_phase15.sql`, **não a função**. Migration aplicada é imutável por
convenção do repositório; o idioma vivo para corrigir uma função é `CREATE OR REPLACE` numa
migration NOVA, exatamente como `20260803000001_p43_fix_listar_matriz_cast.sql` fez. A função
**vai** ser substituída de qualquer modo — o D-45-04 exige. Não travar.

## A tensão SC#5 × D-45-04, resolvida por escrito (Pitfall 6 · assunção A8)

`p_periodo` de `gerar_bias_snapshot()` é um **rótulo, não um filtro**: a coorte não tem cláusula de
período nenhuma — `p_periodo` só é gravado em `bias_audit_log.periodo`, e o `CREATE TEMP TABLE
_bias_cohort` agrega **toda** `decisao_final`. Logo "a série continua produzindo os mesmos números
para os períodos anteriores" **nunca** significou "reexecutar o snapshot de um período passado
devolve o mesmo resultado", porque isso nunca foi verdade.

**A leitura coerente, e ela é decisão de plano:** SC#5 fala das **linhas já gravadas** em
`bias_audit_log` — que permanecem intactas, porque não há FK daquela tabela para `candidatos` — e
da **composição da coorte**, que não muda por causa da anonimização, que é exatamente o que a
materialização da faixa garante. A **apresentação futura** suprime células pequenas. Sob qualquer
outra leitura, D-45-04 e SC#5 seriam incompatíveis por interpretação, não por código — e a
verificação vai perguntar. Esta leitura vai para o `COMMENT ON FUNCTION`, dentro do banco.

## Premissa sinalizada (aresta ERASE-01 deixada `unresolved` pelo probe)

O probe não conseguiu classificar a forma "snapshot-antes-da-anonimização" e registrou a questão
aberta: **k=5 sozinho é derrotado por subtração dos totais publicados** — hoje `n_total` e
`excluidos_sem_data` fecham a aritmética. **Assunção declarada do planner:** o limiar k=5 é o de
D-45-04 e não é reaberto; a regra complementar é escolha discricionária dentro dele, e este plano
a fixa como: quando existe supressão primária, suprime-se **`n_total` E a segunda menor faixa**, e
todo campo derivado da célula suprimida sai do payload. A forma exata é revisável; a propriedade
— o leitor não recupera a célula por aritmética — não é.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-RESEARCH.md
@.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-PATTERNS.md
@.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-SONDAS-PROD.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: `faixa_etaria_materializada` + a precedência que faz a sentinela nunca ser lida como faixa</name>
  <precondition>`45-SONDAS-PROD.md` existe e a SONDA 1 registra que `candidatos.data_nascimento` continua `NOT NULL` com a CHECK `< CURRENT_DATE` — se a nullability medida divergir disso, a forma da sentinela do tombstone muda e este plano tem de ser relido antes de escrito.</precondition>
  <files>supabase/migrations/20260805000003_p45_bias_k5.sql</files>
  <read_first>
    - `supabase/migrations/20260625100001_decisao_final_phase15.sql` (`:283-441` — `gerar_bias_snapshot()` INTEIRA: o `CREATE TEMP TABLE _bias_cohort` de `:322-331` com o JOIN vivo em `data_nascimento`, o `excluidos_sem_data` de `:406-417`, o `small_sample_warning < 30` de `:374`, o payload `dados jsonb` e a gravação em `bias_audit_log` de `:417-419`)
    - `supabase/migrations/20260803000001_p43_fix_listar_matriz_cast.sql` (INTEIRO — o precedente de PROCESSO: `CREATE OR REPLACE` corretiva numa migration nova, `:5-7` o docblock "CORRETIVA · ZERO-DESTRUTIVA", `:110-168` a ordem REVOKE/GRANT → `DO` → `COMMENT`, e `:153-168` o COMMENT que registra o defeito corrigido e por que o gate anterior não o pegou)
    - `supabase/migrations/20260804000001_p44_config_sla_dados.sql` (`:1-105` — o cabeçalho de migration do M8)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-RESEARCH.md` § Pattern 6 e § Pitfall 6
    - `.planning/research/FK-AUDIT-LIVE.md` (`:14` — por que `ADD COLUMN IF NOT EXISTS` é o idioma proibido)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-SONDAS-PROD.md` (SONDA 1)
  </read_first>
  <action>
Criar `supabase/migrations/20260805000003_p45_bias_k5.sql` com o cabeçalho de migration do M8
(cinco blocos: escopo negativo em uma linha · apply por MCP `apply_migration` pelo orquestrador ·
**sem** wrapper `BEGIN;/COMMIT;` · reparo do ledger · conferência `md5(statements[1])` ·
proveniência) e o docblock **CORRETIVA · ZERO-DESTRUTIVA** no molde de `20260803000001:5-7`.

1. `ALTER TABLE public.candidatos ADD COLUMN faixa_etaria_materializada text` — **sem
   `IF NOT EXISTS`**. O idioma condicional é a causa medida do drift de `candidatos.user_id`
   (`FK-AUDIT-LIVE.md:14`): um `ADD COLUMN IF NOT EXISTS` virou no-op contra o schema legado e a
   cláusula FK nunca foi aplicada, e o repositório passou a descrever uma semântica que o banco
   nunca teve. Aqui um no-op silencioso significaria snapshot lendo `data_nascimento`
   tombstoneada. Nullable e sem default: NULL significa "ainda não materializada", e o `COALESCE`
   trata isso.

2. `COMMENT ON COLUMN public.candidatos.faixa_etaria_materializada` — quem escreve (o tombstone de
   45-07, ANTES de tocar `data_nascimento`), quem lê (`gerar_bias_snapshot`, com precedência), e
   por que a ordem é a restrição de ordenação nº 1 do ROADMAP.

3. `CREATE OR REPLACE FUNCTION public.gerar_bias_snapshot(...)` preservando **assinatura, tipo de
   retorno e semântica** vigentes, com **uma** mudança nesta tarefa: no `CREATE TEMP TABLE
   _bias_cohort`, a faixa passa a sair de
   `COALESCE(ca.faixa_etaria_materializada, <faixa derivada de ca.data_nascimento>)`. A
   **precedência importa e é o ponto**: com a coluna na frente, a sentinela que o tombstone escreve
   em `data_nascimento` (que é `NOT NULL` e tem CHECK `< CURRENT_DATE`, logo cairia numa faixa
   real) **nunca é lida**.

4. Ajustar `excluidos_sem_data` para **não** contar a linha tombstoneada: hoje o contador conta
   `idade IS NULL OR idade < 18`; uma linha com faixa materializada tem faixa conhecida e não
   pertence ali. Mover a linha para a faixa materializada muda o denominador na direção certa —
   ela **permanece** na coorte, com a mesma faixa de antes.

⚠ **Não tocar** `20260625100001_decisao_final_phase15.sql`. Migration aplicada é imutável; a
correção é `CREATE OR REPLACE` aqui.

⚠ **Nenhum `UPDATE` e nenhum `DELETE` em `bias_audit_log`** nesta migration — as linhas históricas
são intocadas por desenho (SC#5), e não há FK daquela tabela para `candidatos`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f='supabase/migrations/20260805000003_p45_bias_k5.sql';const s=fs.readFileSync(f,'utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN; proibido');if(/ADD COLUMN IF NOT EXISTS/i.test(c))throw new Error('ADD COLUMN IF NOT EXISTS proibido');if(/(UPDATE|DELETE)\s+(FROM\s+)?public\.bias_audit_log/i.test(c))throw new Error('bias_audit_log historico nao pode ser mutado');for(const t of ['faixa_etaria_materializada','COALESCE','CREATE OR REPLACE FUNCTION public.gerar_bias_snapshot','excluidos_sem_data'])if(!c.includes(t))throw new Error('falta: '+t);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe com o cabeçalho de migration do M8 (cinco blocos) e o docblock CORRETIVA · ZERO-DESTRUTIVA.
    - `ALTER TABLE public.candidatos ADD COLUMN faixa_etaria_materializada text` está presente, e o arquivo (ignoradas as linhas de comentário) tem **zero** ocorrências do idioma condicional de `ADD COLUMN`.
    - Existe `COMMENT ON COLUMN public.candidatos.faixa_etaria_materializada` nomeando o escritor (tombstone de 45-07), o leitor (`gerar_bias_snapshot`) e a ordem obrigatória.
    - `gerar_bias_snapshot` é recriada por `CREATE OR REPLACE`, com assinatura e tipo de retorno idênticos aos de `20260625100001`.
    - A faixa da coorte sai de `COALESCE(faixa_etaria_materializada, <derivada>)`, com a coluna **na frente**.
    - `excluidos_sem_data` não conta linha que tenha `faixa_etaria_materializada` não-nula.
    - O arquivo (ignoradas as linhas de comentário) tem **zero** `UPDATE` e **zero** `DELETE` sobre `bias_audit_log`.
    - `supabase/migrations/20260625100001_decisao_final_phase15.sql` não aparece em `git diff`.
  </acceptance_criteria>
  <done>A faixa etária tem onde ser materializada, e o snapshot a lê antes de derivar — a série histórica deixa de depender de uma coluna que o tombstone vai destruir.</done>
</task>

<task type="auto">
  <name>Task 2: k=5 com supressão COMPLEMENTAR — porque suprimir só a célula não suprime nada</name>
  <files>supabase/migrations/20260805000003_p45_bias_k5.sql</files>
  <read_first>
    - `supabase/migrations/20260625100001_decisao_final_phase15.sql` (`:374` o `small_sample_warning < 30`; `:406-417` o payload `dados jsonb` com `bands[]` carregando `applicants`/`selected` por faixa, mais `n_total` e `excluidos_sem_data` — exatamente os campos que fecham a conta por subtração)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-RESEARCH.md` § Pitfall 7 (supressão primária × complementar, com as fontes)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-CONTEXT.md` § "k-anonymity no `gerar_bias_snapshot()`" (D-45-04 e D-45-05)
    - `supabase/migrations/20260803000001_p43_fix_listar_matriz_cast.sql` (`:153-168` — o molde do `COMMENT ON FUNCTION` que registra o defeito corrigido e por que o gate anterior não o pegou)
  </read_first>
  <action>
Editar a mesma migration, dentro do corpo de `gerar_bias_snapshot` recriado na Task 1.

**Supressão primária (D-45-04).** Toda faixa com `applicants < 5` tem a **contagem oculta** e a
**presença declarada**: a entrada continua em `bands[]` com o rótulo da faixa e um marcador
explícito de supressão, e **sem** `applicants` e **sem** `selected`. Some o número, não o fato de a
faixa existir.

**Supressão complementar — a metade sem a qual a primária não faz nada.** O payload atual publica
`bands[]` com `applicants`/`selected` por faixa, mais `n_total` e `excluidos_sem_data`: o leitor
subtrai e recupera a célula. Regra fixada por este plano: **quando existe pelo menos uma faixa
primária-suprimida**, também são suprimidos (i) `n_total` e (ii) a faixa de MENOR contagem entre
as remanescentes. Duas supressões, porque uma só continua fechando a conta quando resta uma única
faixa não-suprimida.

**Campos derivados de célula suprimida saem do payload.** `selection_rate`, `razao_4_5`, `flag` e
`faixa_referencia` de uma faixa suprimida **não** são publicados — publicar a razão 4/5 de uma
célula suprimida devolve a contagem por outro caminho quando `selected` é pequeno. Se a
`faixa_referencia` (a de maior taxa) for a suprimida, a razão 4/5 do relatório inteiro é suprimida:
não existe razão 4/5 honesta sobre um denominador que não pode ser publicado.

**`small_sample_warning` permanece SEPARADO e inalterado em `< 30`** (D-45-05). Os dois sinais não
colapsam: o `< 30` é estatístico (a razão 4/5 é instável), o `< 5` é controle de re-identificação.
O payload carrega os dois campos, com nomes distintos e significados distintos, e o `COMMENT`
explica a diferença — porque a próxima pessoa que ler o código vai querer unificá-los.

**`COMMENT ON FUNCTION`** ao final (ordem herdada: REVOKE/GRANT antes, `DO` de auto-verificação,
`COMMENT` depois), registrando: (a) a leitura resolvida da tensão SC#5 × D-45-04 — *as linhas
históricas não mudam; a apresentação futura suprime células pequenas*; (b) que `p_periodo` é
**rótulo, não filtro**, e portanto "o mesmo período" nunca significou "o mesmo resultado"; (c) que
suprimir a célula sem suprimir o complemento não suprime nada, com o motivo.

**REVOKE/GRANT.** `CREATE OR REPLACE` preserva o ACL, e o `REVOKE ALL ON FUNCTION
public.gerar_bias_snapshot(...) FROM PUBLIC, anon, authenticated` ao final apenas **reafirma** o
estado — mas reafirmar é obrigatório, porque `pg_default_acl` de `public` concede a `anon` e
`authenticated` como grants diretos e nomeados. Conceder de volta só ao papel que hoje a chama.

**`DO` block de auto-verificação** no molde de `20260803000001:116-150`: numa subtransação com
`ROLLBACK`, montar uma coorte sintética com uma faixa de 3 candidatos e outra de 12, chamar a
função, e exigir (i) que ela **completou**, (ii) que a faixa de 3 aparece com presença e sem
contagem, (iii) que `n_total` está suprimido, e (iv) que a soma dos `applicants` publicados **não**
permite recuperar o 3. Se não houver dado suficiente para montar a coorte, `RAISE EXCEPTION` — e a
mensagem diz que verificar só a recusa foi exatamente o defeito que a `20260803000001` corrigiu.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/20260805000003_p45_bias_k5.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');for(const t of ['n_total','small_sample_warning','razao_4_5','faixa_referencia','REVOKE ALL ON FUNCTION','FROM PUBLIC, anon, authenticated','DO \$'])if(!c.includes(t))throw new Error('falta: '+t);if(!/<\s*5|&lt;\s*5|applicants\s*<\s*5/.test(c))throw new Error('falta o limiar k=5');if(!/30/.test(c))throw new Error('o limiar < 30 do small_sample_warning nao pode desaparecer');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Toda faixa com `applicants < 5` é publicada com rótulo e marcador de supressão, **sem** `applicants` e **sem** `selected`.
    - Existindo supressão primária, `n_total` é suprimido E a faixa de menor contagem entre as remanescentes também.
    - `selection_rate`, `razao_4_5`, `flag` e `faixa_referencia` de faixa suprimida não aparecem no payload; se a `faixa_referencia` for suprimida, a razão 4/5 do relatório é suprimida.
    - `small_sample_warning` continua existindo com o limiar 30, como campo distinto do marcador de supressão.
    - Existe `REVOKE ALL ON FUNCTION public.gerar_bias_snapshot(...) FROM PUBLIC, anon, authenticated` nominal, seguido do grant mínimo ao papel que hoje a chama.
    - Existe `DO` block de auto-verificação que monta coorte sintética (uma faixa de 3, outra de 12), assere que a função COMPLETOU, que a faixa de 3 tem presença sem contagem, que `n_total` está suprimido, e faz `ROLLBACK`.
    - `COMMENT ON FUNCTION` contém os três registros (a leitura SC#5 × D-45-04, `p_periodo` como rótulo, e a razão da supressão complementar) e vem DEPOIS do `DO` block.
  </acceptance_criteria>
  <done>Uma faixa com menos de cinco pessoas deixa de ser recuperável — nem pela célula, nem por subtração dos totais, nem pela razão 4/5.</done>
</task>

<task type="auto">
  <name>Task 3: `p45_bias_k5_smoke.sql` — a prova de que a série não mudou e a célula não volta</name>
  <files>supabase/tests/p45_bias_k5_smoke.sql</files>
  <read_first>
    - `supabase/tests/p43_previa_smoke.sql` (`:1-120` — o cabeçalho gate-GUC completo, o contador FIXO e a regra de rodar numa ÚNICA chamada)
    - `supabase/migrations/20260805000003_p45_bias_k5.sql` (o arquivo das Tasks 1 e 2)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-VALIDATION.md` (as duas linhas de ERASE-01 do mapa e a nota da lição W-1)
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-PATTERNS.md` §6
  </read_first>
  <action>
Criar `supabase/tests/p45_bias_k5_smoke.sql` no idioma gate-GUC de `p43_previa_smoke.sql`:
cabeçalho de cinco blocos, contador FIXO, execução por MCP `execute_sql` numa **ÚNICA chamada**
pelo orquestrador, e o bloco "ESTE ARQUIVO É A ESPECIFICAÇÃO, NÃO UM RELATÓRIO".

Asserções, dentro de subtransação com `ROLLBACK`:

- `K1` · a coluna `candidatos.faixa_etaria_materializada` existe, é `text` e é nullable.
- `K2` · `pg_get_functiondef(gerar_bias_snapshot)` contém `faixa_etaria_materializada` **antes**
  da derivação por `data_nascimento` no `COALESCE` — a precedência é o mecanismo, e um `COALESCE`
  com os argumentos invertidos passaria por um teste que só procurasse o nome da coluna.
- `K3` · **ERASE-01, a asserção central.** Com um candidato sintético: (i) rodar
  `gerar_bias_snapshot` e guardar a faixa dele; (ii) preencher `faixa_etaria_materializada` e
  escrever em `data_nascimento` a MESMA sentinela que o tombstone de 45-07 vai escrever; (iii)
  rodar de novo e exigir que ele esteja **na mesma faixa** e **fora de `excluidos_sem_data`**. É
  esta asserção que prova o SC#5, e ela roda cedo no arquivo.
- `K4` · supressão primária: coorte com faixa de 3 → a faixa aparece com presença e sem contagem.
- `K5` · supressão complementar: no mesmo payload, `n_total` suprimido E a segunda menor faixa
  suprimida; e uma asserção **aritmética** de que a soma dos `applicants` publicados mais os
  campos publicados não reconstrói o 3.
- `K6` · campos derivados de célula suprimida ausentes (`selection_rate`, `razao_4_5`, `flag`,
  `faixa_referencia`).
- `K7` · `small_sample_warning` continua presente e com limiar 30 (D-45-05, sinal separado).
- `K8` · `⊖ proacl` de `gerar_bias_snapshot` não concede EXECUTE a `anon`, `authenticated` nem
  PUBLIC; e `⊖ guard` recusa com 42501 tanto o papel errado quanto o chamador **sem claim nenhuma**.
- `K9` · asserção negativa: a contagem de `bias_audit_log` é idêntica antes e depois do smoke
  (o `ROLLBACK` cobriu tudo), e nenhuma linha histórica daquela tabela foi alterada.

Ordem: `K1`–`K3` antes de qualquer coisa que possa abortar por outro motivo — a lição W-1 da P43
vale aqui exatamente como no smoke do motor: num batch de chamada única, tudo depois do primeiro
`RAISE` é inalcançável e conta como verde.

Fechar com o contador `IF v_asserts <> 9 THEN RAISE EXCEPTION`, com o 9 também escrito no cabeçalho.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/tests/p45_bias_k5_smoke.sql','utf8');for(const t of ['faixa_etaria_materializada','excluidos_sem_data','n_total','small_sample_warning','proacl','42501','bias_audit_log','ROLLBACK'])if(!s.includes(t))throw new Error('falta: '+t);if(!/v_asserts\s*<>\s*9/.test(s))throw new Error('falta o contador fixo 9');const iK3=s.indexOf('excluidos_sem_data');const iK4=s.indexOf('n_total');if(iK3>-1&&iK4>-1&&iK4<iK3)throw new Error('K3 (SC#5) tem de vir antes das assercoes de supressao');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe com o cabeçalho gate-GUC de cinco blocos e o contador FIXO `9` no cabeçalho e no rodapé.
    - `K2` assere a POSIÇÃO do argumento no `COALESCE`, não apenas a presença do nome da coluna.
    - `K3` executa o snapshot duas vezes — antes e depois de materializar a faixa e escrever a sentinela — e assere mesma faixa **e** ausência de `excluidos_sem_data`.
    - `K5` contém uma asserção aritmética explícita de não-recuperação por subtração.
    - `K8` tem as duas metades do guard (papel errado E chamador sem claim), cada uma exigindo 42501.
    - `K9` assere contagem idêntica de `bias_audit_log` antes/depois.
    - `K1`–`K3` precedem `K4`–`K9` no arquivo.
    - Todo o bloco de escrita está em subtransação com `ROLLBACK`.
  </acceptance_criteria>
  <done>Existe prova executável de que anonimizar um candidato não move a coorte de faixa, e de que uma faixa de três pessoas não volta nem pela célula nem pela conta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `gerar_bias_snapshot` → `bias_audit_log` | Um relatório de não-discriminação publicado pode carregar contagens re-identificantes |
| chamador → função `SECURITY DEFINER` recriada | `CREATE OR REPLACE` preserva o ACL, então uma concessão herdada a `anon` sobrevive silenciosamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-05-01 | Information Disclosure | payload `dados jsonb` do snapshot | high | mitigate | Supressão primária k=5 **com** complementar (`n_total` + segunda menor faixa) e remoção dos campos derivados da célula suprimida; asserção aritmética de não-recuperação no smoke (`K5`) |
| T-45-05-02 | Information Disclosure | `razao_4_5` de célula suprimida | high | mitigate | Campo derivado de célula suprimida sai do payload; se a `faixa_referencia` for a suprimida, a razão 4/5 do relatório inteiro é suprimida |
| T-45-05-03 | Elevation | `gerar_bias_snapshot` recriada | high | mitigate | `REVOKE ALL ... FROM PUBLIC, anon, authenticated` nominal reafirmado após o `CREATE OR REPLACE`; `K8` assere `proacl` e as duas metades do guard |
| T-45-05-04 | Tampering | série histórica em `bias_audit_log` | high | mitigate | Guard automático rejeita `UPDATE`/`DELETE` sobre `bias_audit_log` na migration; `K9` assere contagem idêntica |
| T-45-05-05 | Repudiation | ordem materialização × tombstone | critical | mitigate | `COMMENT ON COLUMN` fixa o contrato dentro do banco; `K3` prova a propriedade; a restrição de ordenação nº 1 do ROADMAP é must-have de plano, e o apply desta migration precede o da `p45_anonimizar_candidato` em 45-11 |
| T-45-05-06 | Tampering | `ADD COLUMN` condicional | high | mitigate | Guard automático rejeita o idioma condicional — é a causa medida do drift de `candidatos.user_id`, e aqui um no-op silencioso faria o snapshot ler `data_nascimento` tombstoneada |
| T-45-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | **Zero instalação.** `45-RESEARCH.md § Package Legitimacy Audit` registra zero pacotes; este plano escreve apenas SQL |
</threat_model>

<verification>
- Os dois arquivos existem e passam os guards automáticos de forma.
- `20260625100001_decisao_final_phase15.sql` não aparece em `git diff` — a migration aplicada continua imutável.
- Nenhum apply foi feito (o apply é 45-11, com esta migration NA FRENTE da `p45_anonimizar_candidato`).
- `npm run lint` inalterado (o plano não toca TypeScript).
</verification>

<success_criteria>
A faixa etária tem onde viver antes de `data_nascimento` morrer, o snapshot a lê com precedência, e
o relatório que existe para provar não-discriminação deixa de ser um caminho de re-identificação —
com a tensão SC#5 × D-45-04 resolvida por escrito dentro do próprio banco, e não deixada para a
verificação descobrir.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (contribuição deste plano)

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `supabase/migrations/20260805000003_p45_bias_k5.sql` | migration corretiva zero-destrutiva | **nova** |
| `public.candidatos.faixa_etaria_materializada` (`text`, nullable) | coluna | **nova** |
| `public.gerar_bias_snapshot(...)` | função | **recriada** (`CREATE OR REPLACE`; assinatura preservada) |
| marcador de supressão k=5 + supressão complementar no payload `dados jsonb` | contrato de saída | **novo** |
| `supabase/tests/p45_bias_k5_smoke.sql` (asserções `K1`–`K9`, contador fixo 9) | smoke SQL gate-GUC | **novo** |
</artifacts_produced>

<output>
Create `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-05-SUMMARY.md` when done
</output>
