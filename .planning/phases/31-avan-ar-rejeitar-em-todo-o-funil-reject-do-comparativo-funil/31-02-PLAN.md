---
phase: 31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/triagem/services/triagemService.ts
  - src/features/triagem/services/__tests__/triagemService.test.ts
  - src/features/vagas/hooks/useCandidaturas.ts
  - src/features/triagem/hooks/useRejeitarCandidatura.ts
  - src/features/triagem/hooks/__tests__/useRejeitarCandidatura.test.ts
autonomous: true
requirements: [OPER-01, OPER-02, OPER-03]

must_haves:
  truths:
    - "OPER-02: rejeitarCandidatura service calls supabase.rpc('rejeitar_candidatura',{p_candidatura_id,p_motivo,p_justificativa}) and maps RPC error → TriagemServiceError"
    - "OPER-01/03: updateCandidaturaEtapa ALWAYS includes etapa_justificativa in the UPDATE SET (justificativa ?? null) so the trigger never reads a stale OLD value — fixes the regression stale-justificativa hazard"
    - "OPER-01/02/03: useRejeitarCandidatura invalidates all three key trees candidaturasKeys.all + vagasKeys.all + triagemKeys.all so the RH list/panel refresh after a reject"
  artifacts:
    - path: "src/features/triagem/services/triagemService.ts"
      provides: "rejeitarCandidatura() + MotivoRejeicaoRh type + extended updateCandidaturaEtapa(justificativa?)"
      exports: ["rejeitarCandidatura", "updateCandidaturaEtapa"]
    - path: "src/features/triagem/hooks/useRejeitarCandidatura.ts"
      provides: "TanStack mutation → toast + 3-tree invalidation"
      exports: ["useRejeitarCandidatura"]
    - path: "src/features/vagas/hooks/useCandidaturas.ts"
      provides: "useUpdateCandidaturaEtapa vars extended with optional justificativa forwarded to the service"
      contains: "justificativa"
  key_links:
    - from: "rejeitarCandidatura service"
      to: "rejeitar_candidatura RPC"
      via: "supabase.rpc call with p_-prefixed params"
      pattern: "supabase.rpc\\('rejeitar_candidatura'"
    - from: "useRejeitarCandidatura"
      to: "candidaturasKeys.all + vagasKeys.all + triagemKeys.all"
      via: "queryClient.invalidateQueries x3"
      pattern: "invalidateQueries"
---

<objective>
Build the TypeScript service + hook layer that calls the reject RPC and extends the advance/regress path — the contract the dialogs (31-03) and surfaces (31-04/05) consume. Two changes: (1) extend `updateCandidaturaEtapa` to carry an optional `justificativa` and ALWAYS write `etapa_justificativa` (fixing the stale-OLD hazard behind OPER-03), and (2) add the `rejeitarCandidatura` service + `MotivoRejeicaoRh` type + `useRejeitarCandidatura` mutation hook, copying the `registrarDecisao` / `useRegistrarDecisao` triad verbatim.

Purpose: The RPC (31-01) is the server authority; this plan is the typed client path to it. The advance/regress extension is independent of the RPC and is where OPER-01/03 live in the client tier.
Output: extended `triagemService.ts` + `useCandidaturas.ts`, new `useRejeitarCandidatura.ts`, plus the extended service test and the new hook test (Wave 0 gaps).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-PATTERNS.md

<interfaces>
<!-- Verified against live source. -->

triagemService.ts (existing, to extend):
  export class TriagemServiceError extends Error   // (msg, code: 'INVALID_INPUT'|'DATABASE_ERROR', cause?) — :28-43
  export type EtapaFunilM2 = 'inscricao'|'triagem'|'avaliacao_assincrona'|'entrevista_online'|'entrevista_presencial'|'decisao_final'|'aprovado'|'rejeitado'  // :302-311
  export const ETAPA_M2_LABELS: Record<EtapaFunilM2,string>          // :325
  export const ETAPA_M2_OPTIONS: {value:EtapaFunilM2;label:string}[] // :337 (funnel order)
  export async function updateCandidaturaEtapa(candidaturaId, novaEtapa)  // :350-378 — CURRENT body sets only etapa_atual (+ status for rejeitado); NEVER touches etapa_justificativa. Uses `.update(update as never)` cast (:368).
  StatusCandidatura imported from '@/features/vagas/...'  // :20

Copy-template service: src/features/decisao/services/decisaoService.ts
  export async function registrarDecisao({...}) — :137-164 — `supabase.rpc('registrar_decisao',{p_candidatura_id,p_decisao,p_justificativa})`; error → DecisaoServiceError('...', 'DATABASE_ERROR', error)

Copy-template hook: src/features/decisao/hooks/useRegistrarDecisao.ts
  useMutation → onSuccess: toast.success + invalidateQueries(decisaoKeys.all, candidaturasKeys.all, vagasKeys.all); onError: toast.error

useCandidaturas.ts (existing, to extend):
  export interface UpdateCandidaturaEtapaVars { candidaturaId: string; novaEtapa: EtapaFunilM2 }  // :397-400
  export function useUpdateCandidaturaEtapa(options?)  // :414-450 — mutationFn calls updateCandidaturaEtapa(candidaturaId, novaEtapa); already invalidates candidaturasKeys.all (+refetch) + vagasKeys.all
  export const candidaturasKeys = { all: [...] }  // :55

Key factories (invalidation targets for the new hook):
  candidaturasKeys.all — src/features/vagas/hooks/useCandidaturas.ts:55
  vagasKeys.all        — src/features/vagas/hooks/useVagas.ts:38
  triagemKeys.all      — src/features/triagem/hooks/useTriagemPanel.ts:24

Enum → MotivoRejeicaoRh union (mirror 31-01 enum): 'perfil_desalinhado'|'reprovado_avaliacao'|'reprovado_entrevista'|'nao_compareceu'|'desistencia'|'outro'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend updateCandidaturaEtapa + add rejeitarCandidatura service (+ tests)</name>
  <read_first>
    - src/features/triagem/services/triagemService.ts (self — updateCandidaturaEtapa :350-378, TriagemServiceError :28-43, EtapaFunilM2/ETAPA_M2_* :302-348)
    - src/features/decisao/services/decisaoService.ts (registrarDecisao :137-164 — the RPC-call shape to copy)
    - src/features/triagem/services/__tests__/triagemService.test.ts (existing — extend it)
  </read_first>
  <behavior>
    - updateCandidaturaEtapa(id,'triagem') → UPDATE object includes etapa_justificativa: null (always in SET list, never omitted).
    - updateCandidaturaEtapa(id,'inscricao','voltou por X') → UPDATE object includes etapa_justificativa:'voltou por X'.
    - updateCandidaturaEtapa(id,'rejeitado') → UPDATE includes status:'rejeitado' AND etapa_justificativa:null.
    - updateCandidaturaEtapa('') → throws TriagemServiceError('candidaturaId é obrigatório','INVALID_INPUT').
    - rejeitarCandidatura(id,'perfil_desalinhado','...') → calls supabase.rpc('rejeitar_candidatura',{p_candidatura_id,p_motivo,p_justificativa}); on RPC error → throws TriagemServiceError('...','DATABASE_ERROR',error).
    - rejeitarCandidatura('', ...) → throws TriagemServiceError('candidaturaId é obrigatório','INVALID_INPUT').
  </behavior>
  <action>
    In `triagemService.ts`: (1) export `type MotivoRejeicaoRh` as the 6-value union mirroring the enum. (2) Extend `updateCandidaturaEtapa` signature to `(candidaturaId, novaEtapa, justificativa?: string)`; widen the local `update` object type to include `etapa_justificativa: string | null` and ALWAYS set `etapa_justificativa: justificativa ?? null` (Pitfall 3 — a bare regression must carry the fresh text into NEW; a forward advance writes null; reject writes the ≥50 text). Keep the existing `status:'rejeitado'` branch and the `.update(update as never)` cast (local workaround until types regen in 31-06). (3) Add `export async function rejeitarCandidatura(candidaturaId: string, motivo: MotivoRejeicaoRh, justificativa: string): Promise<void>` mirroring registrarDecisao: guard empty candidaturaId → TriagemServiceError INVALID_INPUT; `const { error } = await supabase.rpc('rejeitar_candidatura', { p_candidatura_id: candidaturaId, p_motivo: motivo, p_justificativa: justificativa })`; on error throw `TriagemServiceError('Não foi possível rejeitar o candidato: ' + error.message, 'DATABASE_ERROR', error)`. Note the `.rpc(...)` may need a cast until 31-06 regen; mirror the file's existing cast convention if tsc complains.

    Write the tests FIRST in `triagemService.test.ts` (extend the existing suite — mock `supabase` as the file already does): cover the 6 behaviors above. Assert the UPDATE payload includes `etapa_justificativa` in all three updateCandidaturaEtapa cases; assert rejeitarCandidatura passes the exact `p_`-prefixed params and maps RPC error to TriagemServiceError.
  </action>
  <verify>
    <automated>npx vitest run src/features/triagem/services/__tests__/triagemService.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run triagemService` is GREEN.
    - `updateCandidaturaEtapa` sets `etapa_justificativa` in the UPDATE object on EVERY call (verified by test asserting the payload for advance, regress-with-text, and reject).
    - `rejeitarCandidatura` exists, calls `supabase.rpc('rejeitar_candidatura', …)` with `p_candidatura_id`/`p_motivo`/`p_justificativa`, and throws `TriagemServiceError` with code `DATABASE_ERROR` on RPC error.
    - `MotivoRejeicaoRh` union type exported with the 6 enum values.
  </acceptance_criteria>
  <done>triagemService extended + tested; the stale-justificativa hazard is closed and the reject RPC caller exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useRejeitarCandidatura hook + extend useUpdateCandidaturaEtapa vars (+ hook test)</name>
  <read_first>
    - src/features/decisao/hooks/useRegistrarDecisao.ts (whole file — the mutation+toast+invalidation shape to copy)
    - src/features/vagas/hooks/useCandidaturas.ts (self — UpdateCandidaturaEtapaVars :397-400, useUpdateCandidaturaEtapa :414-450)
    - src/features/triagem/hooks/useTriagemPanel.ts (triagemKeys.all :24)
  </read_first>
  <behavior>
    - useRejeitarCandidatura().mutate({candidaturaId,motivo,justificativa}) on success → toast.success('Candidato movido para "Rejeitado".') AND invalidateQueries called for candidaturasKeys.all, vagasKeys.all, triagemKeys.all (all three).
    - on error → toast.error('Não foi possível rejeitar o candidato. Tente novamente.').
    - useUpdateCandidaturaEtapa mutationFn forwards an optional justificativa: mutate({candidaturaId,novaEtapa,justificativa}) → updateCandidaturaEtapa(candidaturaId, novaEtapa, justificativa).
  </behavior>
  <action>
    Create `src/features/triagem/hooks/useRejeitarCandidatura.ts` copying `useRegistrarDecisao` shape: `useMutation<void, Error, { candidaturaId: string; motivo: MotivoRejeicaoRh; justificativa: string }>` with `mutationFn` → `rejeitarCandidatura(...)`; `onSuccess` → `toast.success('Candidato movido para "Rejeitado".')` + `queryClient.invalidateQueries` for `candidaturasKeys.all`, `vagasKeys.all`, and `triagemKeys.all` (import the three factories from their verified paths); `onError` → `toast.error('Não foi possível rejeitar o candidato. Tente novamente.')`. Import `MotivoRejeicaoRh` + `rejeitarCandidatura` from `triagemService`.

    In `useCandidaturas.ts`: add optional `justificativa?: string` to `UpdateCandidaturaEtapaVars` and forward it in the mutationFn: `updateCandidaturaEtapa(candidaturaId, novaEtapa, justificativa)`. Leave the existing onSuccess invalidation block (candidaturasKeys.all invalidate+refetch + vagasKeys.all) unchanged.

    Write the hook test FIRST at `src/features/triagem/hooks/__tests__/useRejeitarCandidatura.test.ts` (mirror any existing hook test that wraps a QueryClientProvider; mock the service + assert the 3 invalidation keys fire on success and the error toast on failure).
  </action>
  <verify>
    <automated>npx vitest run src/features/triagem/hooks/__tests__/useRejeitarCandidatura.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run useRejeitarCandidatura` is GREEN.
    - Hook invalidates all THREE trees (candidaturasKeys.all, vagasKeys.all, triagemKeys.all) on success (asserted by test).
    - `UpdateCandidaturaEtapaVars` has optional `justificativa` and the mutationFn forwards it to `updateCandidaturaEtapa`.
    - Success/error toast copy matches the 31-UI-SPEC strings.
  </acceptance_criteria>
  <done>Reject mutation hook exists + invalidates the 3 trees; the advance/regress hook can now carry justificativa for the Retroceder dialog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → service | Free-text justificativa + motivo enter the service here; the service is a thin typed pass-through — the DB RPC (31-01) is the authority, not this tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02 | Tampering | client ≥50 gate | mitigate (defense-in-depth) | The service does NOT re-implement the ≥50 check — it forwards to the RPC whose `check_violation` RAISE is authoritative; the service maps that error to TriagemServiceError for the toast |
| T-31-05 | Integrity | stale/duplicate audit rows | mitigate | Extended updateCandidaturaEtapa ALWAYS SETs etapa_justificativa (no stale OLD read); no client tier ever INSERTs historico_candidatura |
| T-31-SC | Tampering (supply-chain) | npm installs | accept | Zero new packages; all imports (@supabase/supabase-js, @tanstack/react-query, sonner) already in production use |
</threat_model>

<verification>
- `npx vitest run src/features/triagem` GREEN (service + hook tests).
- `npm run lint` (tsc) must not regress the ~104 baseline.
</verification>

<success_criteria>
- Service + hook layer typed and tested; reject RPC caller + extended advance/regress path both exist.
- The three invalidation trees fire on reject success.
- tsc baseline held.
</success_criteria>

<output>
Create `.planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-02-SUMMARY.md` when done.
</output>
