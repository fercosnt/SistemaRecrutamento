---
phase: 13-reda-o-cultural-revis-o-humana
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - supabase/migrations/20260623100001_perguntas_redacao.sql
  - supabase/migrations/20260623100002_redacoes_candidato_em_progresso.sql
  - supabase/migrations/20260623100003_redacoes_candidato.sql
  - supabase/migrations/20260623100004_salvar_revisao_redacao_rpc.sql
  - supabase/functions/avaliar-redacao-cultural/index.ts
  - supabase/functions/avaliar-redacao-cultural/index.test.ts
autonomous: true
requirements: [AVAL-05, AVAL-06, AVAL-07]

must_haves:
  truths:
    - "D-13 traceability: the binding PRD v1.1 §8.1/§8.2/§8.3 is transcribed verbatim — 3 tables (perguntas_redacao + redacoes_candidato_em_progresso + redacoes_candidato), the RLS set + the BEFORE UPDATE review-fields-only trigger, and the new dedicated culture-fit essay EF cloning the PROD-green static-import skeleton."
    - "The new EF avaliar-redacao-cultural ALWAYS sets status_analise='pendente_humano' regardless of color (human review mandatory), sets bloqueio_avanco only when vermelho, and NEVER writes candidaturas (RNF-07a — never auto-rejects)."
    - "The EF authenticates via anon getUser() THEN authorizes ownership+etapa='avaliacao_assincrona' via service_role before any privileged read/write (IDOR defense, D-23 two-client)."
  artifacts:
    - path: "supabase/migrations/20260623100001_perguntas_redacao.sql"
      provides: "perguntas_redacao table + seed 11 rows (PADRAO_BS + D1/D2/D3 + R1/R2/R3 + C1/C2/C3 + F1) + RLS"
      contains: "CREATE TABLE"
    - path: "supabase/migrations/20260623100003_redacoes_candidato.sql"
      provides: "redacoes_candidato final table + candidate-DENY scoring RLS + client-INSERT-deny + RH SELECT/UPDATE + review-fields trigger"
      contains: "trg_redacao_rh_only_review_fields"
    - path: "supabase/migrations/20260623100004_salvar_revisao_redacao_rpc.sql"
      provides: "salvar_revisao_redacao SECURITY DEFINER RPC (RH role+own-vaga guard, notas≥50, decisao enum, duvida→escalate)"
      contains: "salvar_revisao_redacao"
    - path: "supabase/functions/avaliar-redacao-cultural/index.ts"
      provides: "new dedicated essay scoring EF (static npm imports, two-client, callAi, computeScoreAndCors, always pendente_humano)"
      exports: ["handler"]
  key_links:
    - from: "supabase/functions/avaliar-redacao-cultural/index.ts"
      to: "supabase/functions/_shared/essay-schemas.ts"
      via: "import EssayScoringV1Schema"
      pattern: "EssayScoringV1Schema"
    - from: "supabase/functions/avaliar-redacao-cultural/index.ts"
      to: "npm:@anthropic-ai/sdk@0.102.0"
      via: "STATIC top-level npm import (NOT .join)"
      pattern: "import Anthropic from \"npm:@anthropic-ai/sdk@0\\.102\\.0\""
---

<objective>
Author (NOT apply — PROD apply is the [BLOCKING] Plan 04) the 4 migrations + the new dedicated culture-fit essay scoring Edge Function. This is faithful transcription of PRD v1.1 §8.1 (schema), §8.2 (RLS + review-fields trigger), and §8.3 (EF pseudocode), wired through the Phase 9 AI infra and the PROD-green static-import EF skeleton.

Purpose: Cover AVAL-06 (EF + scoring persistence) and the DB foundation of AVAL-05/AVAL-07. The single highest-risk surface in the phase is this EF — the project was bitten 4× by the `.join("npm:")` import bug. The fix is fully understood: clone `analise-candidato-individual`/`gerar-devolutiva-bigfive` exactly (static imports + injected zodOutputFormat/zodResponseFormat + EssayScoringV1 on /v4).

Output: 4 authored migrations (no-BEGIN/COMMIT wrapper, D-22) + 1 new EF directory (index.ts + index.test.ts) consuming the Plan 01 contracts (essay-schemas.ts + compute-score.ts). Migrations are NOT applied here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-reda-o-cultural-revis-o-humana/13-CONTEXT.md
@.planning/phases/13-reda-o-cultural-revis-o-humana/13-PATTERNS.md

<interfaces>
<!-- Contracts authored in Plan 01 (this plan imports them — no re-derivation): -->
From supabase/functions/_shared/essay-schemas.ts:
  export const EssayScoringV1Schema  (zod/v4, .length(4) dims, red_flag_etico boolean)
  export type EssayScoringV1
From supabase/functions/avaliar-redacao-cultural/_local/compute-score.ts:
  export function computeScoreAndCors(parsed, threshold, wordCount, tempoSegundos)
    → { scoreGeral, classificacaoCor, flags, redFlagEtico }
  export function normalizeForHash(texto): string

<!-- callAi pipeline already owns injection/maskPII/retry/fallback/cost/audit-log — never re-implement: -->
From supabase/functions/_shared/ai-client.ts:
  export function callAi(args, deps)  — args: {prompt, rawInput, schema, candidato_id, vaga_id, idempotency_key, ...}
  export function loadPrompt(call_type, supabaseAdmin)
  export function resolvedPromptFromLoaded(...)
</interfaces>

<!-- Binding spec — transcribe these line ranges verbatim: -->
<!-- PRD §8.1 schema (lines 249-369), §8.2 RLS+trigger (lines 371-455), §8.3 EF pseudocode (lines 459-601) -->
@docs/prds/m2-funil-rh/PRD-redacao-fit-cultural.md

<!-- Seed content source (11 rows, authoritative) — NOT the deprecated banco-itens SJT file: -->
@docs/conhecimento/fit-cultural/pergunta-padrao-redacao.md

<!-- PROD-green EF skeleton to clone (static imports + deps wiring + two-client + auth-then-authz): -->
@supabase/functions/analise-candidato-individual/index.ts
@supabase/functions/submit-bigfive-final/index.ts
@supabase/functions/avaliar-redacao/index.ts

<!-- Migration analogs (seed shape, candidate-DENY RLS, SECURITY DEFINER RH RPC, no-wrapper header): -->
@supabase/migrations/20260612000001_bigfive_itens.sql
@supabase/migrations/20260611000001_scores_candidato.sql
@supabase/migrations/20260611000004_pontuar_sjt_rpc.sql
@supabase/migrations/20260610000003_reprocessar_rpc.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations — 3 tables + seed 11 perguntas + RLS + review-fields trigger (no-wrapper)</name>
  <files>supabase/migrations/20260623100001_perguntas_redacao.sql, supabase/migrations/20260623100002_redacoes_candidato_em_progresso.sql, supabase/migrations/20260623100003_redacoes_candidato.sql</files>
  <read_first>
    - docs/prds/m2-funil-rh/PRD-redacao-fit-cultural.md §8.1 (lines 249-369) — the 3 CREATE TABLE blocks + indexes verbatim; §8.2 (lines 371-455) — the RLS policy set + trg_redacao_rh_only_review_fields verbatim
    - docs/conhecimento/fit-cultural/pergunta-padrao-redacao.md — the 11 seed rows (Q1 PADRAO_BS + D1/D2/D3 + R1/R2/R3 + C1/C2/C3 + F1 with codes, texts, valor_primario/secundario, template_cargo, default_on)
    - supabase/migrations/20260612000001_bigfive_itens.sql (lines 1-40 header no-wrapper note + LGPD-04 note; lines 72-192 multi-row INSERT seed form; the rh_gerencia + candidate-read RLS idiom)
    - supabase/migrations/20260611000001_scores_candidato.sql (the candidate-DENY scoring-read RLS idiom — no candidato SELECT policy on verdict columns + the live role values used in M2 policies)
  </read_first>
  <action>Author 3 migrations transcribing PRD §8.1 + §8.2 VERBATIM (AVAL-05/AVAL-06/AVAL-07 / per D-13 binding-PRD decision), each with the no-BEGIN/COMMIT-wrapper header note (D-22) copied from bigfive_itens.sql:1-40. (1) `..._perguntas_redacao.sql`: CREATE TABLE perguntas_redacao (PRD lines 254-274 — codigo UNIQUE, texto CHECK len≥30, template_cargo/valor_primario/valor_secundario CHECKs, is_padrao/default_on/ativa, the partial unique index idx_perguntas_padrao_unica, idx_perguntas_cargo) + ENABLE RLS + perguntas_select_all (FOR SELECT USING true — candidate reads the prompt text; the prompt is NOT an answer key, so a SELECT-all policy is correct, UNLIKE bigfive) + perguntas_admin_write (FOR ALL, admin per PRD §8.2 line 452 — use the live `auth.jwt() -> 'app_metadata' ->> 'role'` idiom matching the codebase) + the SEED INSERT of the 11 rows from pergunta-padrao-redacao.md (PADRAO_BS is_padrao=true valor_primario='multi' template_cargo=NULL; D1/D2/D3 template_cargo='dentista_padrao'; R1/R2/R3 'recepcao_padrao'; C1/C2/C3 'coord_admin_padrao'; F1 'freela_simples'; default_on per the source's ON/OFF column). NOTE the count (PRD-GAP — RESOLVED): the PRD §8.1 line 276 ships NO seed INSERT block — it is prose only, pointing to pergunta-padrao-redacao.md as the authoritative seed source. That source defines exactly 11 codes (PADRAO_BS, D1, D2, D3, R1, R2, R3, C1, C2, C3, F1); the PRD prose's "13 rows" overcounts the freela template (it assumed 3 freela codes, but freela_simples ships only F1, default OFF). The seed file wins (the PRD references it, not the reverse). Seed exactly the 11 rows that exist in the source AND add an inline SQL comment in the migration recording this reconciliation ("-- 11 rows per pergunta-padrao-redacao.md v1.1; PRD prose '13' overcounted freela (F1 only)"). (2) `..._redacoes_candidato_em_progresso.sql`: CREATE TABLE per PRD lines 281-296 (candidatura_id FK ON DELETE CASCADE, pergunta_id FK, texto_em_progresso, word_count, timestamps, user_agent, UNIQUE(candidatura_id,pergunta_id), idx on ultima_atividade_em) + ENABLE RLS + em_progresso_candidato_own (FOR ALL, candidate own via candidatos.auth_user_id=auth.uid() AND etapa_atual='avaliacao_assincrona' in USING, own in WITH CHECK — PRD lines 380-393) + em_progresso_rh_select (RH/admin SELECT — PRD lines 396-397). (3) `..._redacoes_candidato.sql`: CREATE TABLE per PRD lines 303-353 (all columns incl. ordem CHECK 1-3, eh_pergunta_padrao, word_count CHECK 200-500, texto_hash, the IA fields, the revisão fields notas_revisor CHECK len≥50, decisao_revisor CHECK enum, status_analise CHECK enum incl. 'pendente_humano', bloqueio_avanco, the 5 indexes, the table COMMENT) + ENABLE RLS + the 4 policies (redacao_candidato_select own-row; redacao_no_client_insert WITH CHECK false; redacao_rh_select; redacao_rh_update) + trg_redacao_rh_only_review_fields + the CREATE TRIGGER (PRD lines 421-448 verbatim — the BEFORE UPDATE that RAISEs if RH changes any non-review field). Use the live `auth.jwt() -> 'app_metadata' ->> 'role'` projection. IMPORTANT — role-value reconciliation: the PRD writes 'rh','admin' but the live M2 policies (scores_candidato.sql, bigfive_itens.sql) use 'rh','administrador' — use the LIVE codebase value 'administrador' so the JWT app_metadata.role matches the custom_access_token_hook output; mismatched role strings silently deny every RH read. All seed texts and column COMMENTs must pass the LGPD-04 forbidden-strings grep guard.</action>
  <verify>
    <automated>grep -l "trg_redacao_rh_only_review_fields" supabase/migrations/20260623100003_redacoes_candidato.sql && grep -c "PADRAO_BS\|'D1'\|'R1'\|'C1'\|'F1'" supabase/migrations/20260623100001_perguntas_redacao.sql && grep -vE "^\s*--|^\s*#" supabase/migrations/20260623100001_perguntas_redacao.sql | grep -ci "teste psicol"</automated>
  </verify>
  <done>3 migrations authored verbatim from PRD §8.1/§8.2; no BEGIN/COMMIT wrapper; perguntas seed has 11 rows (codes PADRAO_BS,D1,D2,D3,R1,R2,R3,C1,C2,C3,F1); review-fields trigger present; role value 'administrador' (live); LGPD-04 grep = 0 matches; NOT applied (apply is Plan 04).</done>
</task>

<task type="auto">
  <name>Task 2: salvar_revisao_redacao SECURITY DEFINER RPC (RH role + own-vaga guard)</name>
  <files>supabase/migrations/20260623100004_salvar_revisao_redacao_rpc.sql</files>
  <read_first>
    - docs/prds/m2-funil-rh/PRD-redacao-fit-cultural.md §6.3 (RF-R-21/24/25/27/28 — the review write semantics: scores_humanos jsonb + notas_revisor≥50 + decisao_revisor + revisada_por/em; aprovado/reprovado → status_analise='concluida'; duvida → escalate, does NOT finalize)
    - supabase/migrations/20260610000003_reprocessar_rpc.sql (lines ~50-59 — the canonical RH role IN ('rh','administrador') + role='rh' must own vaga guard)
    - supabase/migrations/20260611000004_pontuar_sjt_rpc.sql (the $$ body structure + RAISE EXCEPTION USING ERRCODE validation + REVOKE ALL FROM PUBLIC; GRANT EXECUTE TO authenticated tail + no-wrapper header)
  </read_first>
  <action>Author `salvar_revisao_redacao(p_redacao_id uuid, p_decisao text, p_notas text, p_scores_humanos jsonb)` as `SECURITY DEFINER SET search_path = ''` (AVAL-07 / per D-13). Copy the no-wrapper header note + REVOKE/GRANT tail from pontuar_sjt_rpc.sql. Guard order (load-bearing): resolve the redação's vaga owner via redacoes_candidato → candidaturas → vagas.created_by; then `v_role := (select auth.jwt() #>> '{app_metadata,role}')`; RAISE insufficient_privilege if role NOT IN ('rh','administrador'); RAISE if role='rh' AND vaga owner IS DISTINCT FROM (select auth.uid()) (copy reprocessar_rpc.sql:50-59). Body validation (RAISE ... USING errcode): notas length ≥ 50 (mirror the DB CHECK, defense in depth); p_decisao IN ('aprovado','reprovado','duvida'). UPDATE redacoes_candidato SET scores_humanos=p_scores_humanos, notas_revisor=p_notas, decisao_revisor=p_decisao, revisada_por=(select auth.uid()), revisada_em=now(), status_analise = CASE WHEN p_decisao='duvida' THEN status_analise (stays pendente_humano — escalated to gestor, NOT finalized) ELSE 'concluida' END WHERE id=p_redacao_id. The RPC NEVER advances the funil and NEVER writes candidaturas (RNF-07a — advancing is a separate human action; aprovado/reprovado only RECORD the decision + flip status to concluida). The 'duvida' path leaves status as pendente_humano so the gestor list (Plan 05) reads `decisao_revisor='duvida'`. Note the UPDATE passes through the trg_redacao_rh_only_review_fields trigger — confirm all written columns (scores_humanos/notas_revisor/decisao_revisor/revisada_*/status_analise) are NOT in the trigger's forbidden set.</action>
  <verify>
    <automated>grep -q "SECURITY DEFINER" supabase/migrations/20260623100004_salvar_revisao_redacao_rpc.sql && grep -q "insufficient_privilege" supabase/migrations/20260623100004_salvar_revisao_redacao_rpc.sql && grep -q "GRANT EXECUTE" supabase/migrations/20260623100004_salvar_revisao_redacao_rpc.sql && echo "RPC structure OK"</automated>
  </verify>
  <done>salvar_revisao_redacao authored: SECURITY DEFINER + search_path='' + role+own-vaga guard + notas≥50 + decisao enum + duvida-does-not-finalize + REVOKE/GRANT tail + no-wrapper; never writes candidaturas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: New essay scoring EF (static imports + two-client + callAi + always pendente_humano)</name>
  <files>supabase/functions/avaliar-redacao-cultural/index.ts, supabase/functions/avaliar-redacao-cultural/index.test.ts</files>
  <read_first>
    - supabase/functions/analise-candidato-individual/index.ts (lines 38-54 STATIC npm imports; lines ~392-404 the Deno.serve deps wiring with zodOutputFormat/zodResponseFormat forwarded into callAi) — clone this VERBATIM, NOT avaliar-redacao's .join import (line 356-357)
    - supabase/functions/submit-bigfive-final/index.ts (two-client from env + Authorization; CORS + jsonResponse/errorResponse helpers; the `.select("id").single()` CR-03 requirement)
    - supabase/functions/avaliar-redacao/index.ts (lines 154-191 the auth-then-authorize essay-EF skeleton; lines 212-302 prompt resolve + callAi + persist shape + redacted logging — but its IMPORTS at 356-357 are the BUG, do not copy them)
    - docs/prds/m2-funil-rh/PRD-redacao-fit-cultural.md §8.3 (lines 459-601 — the EF pseudocode: load redação+pergunta+vaga threshold, callAi, computeScoreAndCors, anti-plágio hash query, persist with status_analise='pendente_humano' + bloqueio=cor==='vermelho', neutral response)
  </read_first>
  <behavior>
    - Unauthenticated (no/invalid JWT) → getUser() fails → 401 UNAUTHORIZED, EF body never processes.
    - Authenticated but candidatura.candidato_id !== resolved candidate of auth.uid() → 403 FORBIDDEN (IDOR defense — Pitfall 4).
    - Authenticated owner but candidatura.etapa_atual !== 'avaliacao_assincrona' → 403 FORBIDDEN.
    - Body missing candidatura_id/pergunta_id/texto → 400 VALIDATION.
    - Happy path (deps mocked): callAi returns valid EssayScoringV1 → computeScoreAndCors derives score/cor → UPSERT redacoes_candidato sets status_analise='pendente_humano' ALWAYS + bloqueio_avanco === (cor==='vermelho') + NEVER calls .from('candidaturas').update → returns neutral { ok: true }.
    - cor='vermelho' case → bloqueio_avanco=true in the persisted payload.
    - cor='verde' case → status_analise STILL 'pendente_humano', bloqueio_avanco=false.
  </behavior>
  <action>Create `avaliar-redacao-cultural/index.ts` (AVAL-06 / per D-13). Clone the PROD-green skeleton: STATIC top-level `import Anthropic from "npm:@anthropic-ai/sdk@0.102.0"`, `import { zodOutputFormat } from "npm:@anthropic-ai/sdk@0.102.0/helpers/zod"`, `import OpenAI from "npm:openai@6.42.0"`, `import { zodResponseFormat } from "npm:openai@6.42.0/helpers/zod"`, plus createClient from esm.sh, callAi/loadPrompt from ../_shared/ai-client.ts, EssayScoringV1Schema from ../_shared/essay-schemas.ts, computeScoreAndCors/normalizeForHash from ./_local/compute-score.ts (Plan 01 + 02 Task 1). In Deno.serve build supabaseUser (anon + Authorization → getUser) + supabaseAdmin (service_role) two-client (D-23), construct anthropic/openai clients, and forward `zodOutputFormat: (s,_n)=>zodOutputFormat(s as never)` + `zodResponseFormat: (s,n)=>zodResponseFormat(s as never,n)` into the handler deps (WITHOUT these forwarded, callAi falls to no-op and both providers break — analise-candidato-individual precedent). In handler: (1) getUser() → 401 if invalid; (2) parse body {candidatura_id, pergunta_id, texto}; (3) AUTHORIZE — supabaseAdmin reads candidaturas (allowlist 'id, candidato_id, vaga_id, etapa_atual', NEVER *) → resolve candidate via candidatos.auth_user_id=user.id → 403 if not owner OR etapa_atual !== 'avaliacao_assincrona' (Pitfall 4); (4) resolve threshold_cor from vaga.testes_aplicaveis (default {vermelho_max:40, amarelo_max:64}); (5) server-side word_count revalidation (200-500) + texto_hash = sha256(normalizeForHash(texto)) via Web Crypto crypto.subtle (anti-tamper, server-authoritative — collapse the PRD's separate submit-redacao EF into this one per Open Question 2 / RESEARCH alternative: one EF, fewer moving parts; the client INSERT stays denied by RLS WITH CHECK false); (6) loadPrompt('culture_fit_essay', supabaseAdmin) + callAi({prompt, rawInput: texto, schema: EssayScoringV1Schema, candidato_id, vaga_id, idempotency_key: candidatura_id+pergunta_id}, deps) — callAi already does injection/maskPII/retry/fallback/cost/audit-log (RNF-R-12/LGPD-02 — never re-implement); essay text is UNTRUSTED → flows through callAi's detectPromptInjection+maskPII; (7) computeScoreAndCors(parsed, threshold, word_count, tempo_gasto_segundos); (8) anti-plágio: query redacoes_candidato WHERE texto_hash matches AND candidatura_id != current → push 'possivel_plagio_intercandidato' flag + referencia_match (no block); (9) UPSERT redacoes_candidato (texto, word_count, texto_hash, ordem, eh_pergunta_padrao, tempo_gasto_segundos, analise_ia=parsed, scores_dimensao, score_ponderado_0_100=scoreGeral, classificacao_cor, red_flag_etico, flags, referencia_match, prompt_version, model_version, input_hash, cost_tokens_*, ia_processada_em, status_analise='pendente_humano' ALWAYS, bloqueio_avanco = classificacao_cor==='vermelho') with `.select("id").single()` (CR-03 mandatory — without it supabase-js v2 does not flush and the id never returns); (10) NEVER touch candidaturas (RNF-07a); (11) parsed==null / injection-detected / output-invalid → status 'falhou'/'pendente_humano' row with flag, never a fabricated success (copy avaliar-redacao:241-273); (12) return neutral jsonResponse({ ok: true }) — candidate NEVER sees score/color (avaliar-redacao:314-315). Redacted logging: console.log only ids/counts/status, NEVER essay text/score/name. Author `index.test.ts` (deno test, deps INJECTED — no network) covering all behaviors above; assert the UPSERT payload sets pendente_humano always + bloqueio only on vermelho + asserts `.from('candidaturas')` is never called with update.</action>
  <verify>
    <automated>grep -q 'import Anthropic from "npm:@anthropic-ai/sdk@0.102.0"' supabase/functions/avaliar-redacao-cultural/index.ts && ! grep -q '\.join(' supabase/functions/avaliar-redacao-cultural/index.ts && deno test supabase/functions/avaliar-redacao-cultural/index.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>EF clones the static-import skeleton (no .join); two-client auth-then-authorize; calls callAi (never re-implements injection/mask/retry); persists status_analise='pendente_humano' ALWAYS + bloqueio only on vermelho; never writes candidaturas; deno test (deps injected) GREEN for 401/403/403-etapa/400/happy/vermelho/verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate (JWT) → essay EF | Untrusted candidate invokes the EF; ownership must be authorized after authentication. |
| candidate essay text → LLM | The essay is user-controlled free text fed to the model — prompt-injection surface. |
| RH (JWT) → salvar_revisao_redacao RPC | Privileged review write; role + vaga-ownership boundary. |
| service_role EF/RPC → redacoes_candidato | service_role bypasses RLS — in-handler authorization is the real control. |
| EF logs → audit store | LGPD: logs must carry ids/counts only, never essay text/score/name. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Elevation/Info | candidate IDOR — scores/reads another's essay | mitigate | getUser() then service_role ownership check (candidatos.auth_user_id=user.id) + etapa='avaliacao_assincrona' before any read/write; 403 otherwise (Pitfall 4). |
| T-13-02-02 | Info disclosure | answer-key/rubric or verdict exposed to candidate | mitigate | redacoes_candidato has NO candidato policy on verdict columns (RLS row-deny); EF returns neutral { ok:true }; candidate reads via Plan 03 allowlist excluding all verdict columns. |
| T-13-02-03 | Repudiation/Integrity | AI auto-rejects a candidate (RNF-07a) | mitigate | EF NEVER writes candidaturas; status_analise always 'pendente_humano'; bloqueio_avanco only HOLDS auto-advance; salvar_revisao_redacao never advances the funil. |
| T-13-02-04 | Tampering | prompt injection in essay text | mitigate | Essay flows through callAi which runs detectPromptInjection (short-circuit) + maskPII; injection-detected → human-review row with flag, never a fabricated success. |
| T-13-02-05 | Info disclosure | PII (essay text/score/name) in AI logs (LGPD-02) | mitigate | auditLog inside callAi masks-then-INSERT; EF console.log limited to ids/counts/status (redacted-logging precedent). |
| T-13-02-06 | Tampering | client forges score / bypasses word_count | mitigate | client INSERT denied (RLS WITH CHECK false); EF revalidates word_count 200-500 + derives score server-side; texto_hash computed server-side via crypto.subtle. |
| T-13-02-07 | Elevation | RH writes outside their vaga / non-review fields | mitigate | salvar_revisao_redacao role+own-vaga guard (reprocessar_rpc idiom); BEFORE UPDATE trigger RAISEs on any non-review-field change. |
| T-13-02-08 | Tampering | runtime-constructed `.join("npm:")` import (4× recurrence) | mitigate | STATIC top-level npm imports only; verify grep asserts the static import present and `.join(` absent. |
| T-13-02-SC | Tampering | npm/deno EF installs (anthropic/openai/zod-v4) | accept | Zero net-new packages — identical pins deployed green in 3 EFs (RESEARCH Package Legitimacy Audit, all Approved). No [ASSUMED]/[SUS]. |
</threat_model>

<verification>
- `deno test supabase/functions/avaliar-redacao-cultural/index.test.ts` GREEN (401/403/403-etapa/400/happy/vermelho/verde)
- `deno test supabase/functions/_shared/__tests__/essay-schemas.test.ts` + compute-score test still GREEN (consumed unchanged from Plan 01)
- grep confirms static `npm:@anthropic-ai/sdk@0.102.0` import present and NO `.join(` in the EF
- All 4 migrations have the no-BEGIN/COMMIT-wrapper header note (D-22); none applied (apply is Plan 04)
- perguntas_redacao seed = 11 rows; review-fields trigger + salvar_revisao_redacao present; role value 'administrador'
- LGPD-04 grep clean across migrations + EF
- `npm run build` exits 0; tsc baseline does not grow (EF/migration files are outside tsc include scope)
</verification>

<success_criteria>
- 3 table migrations + 1 RPC migration authored verbatim from PRD §8.1/§8.2 (no-wrapper), NOT applied
- New essay EF clones the PROD-green static-import + two-client + callAi chain; never the .join bug
- EF invariants test-asserted: always pendente_humano, bloqueio only on vermelho, never writes candidaturas, IDOR 403
- salvar_revisao_redacao RPC enforces role+own-vaga + notas≥50 + decisao enum + duvida-does-not-finalize
- EF + RPC consume Plan 01 contracts (essay-schemas.ts + compute-score.ts) directly
</success_criteria>

<output>
Create `.planning/phases/13-reda-o-cultural-revis-o-humana/13-02-SUMMARY.md` when done
</output>
