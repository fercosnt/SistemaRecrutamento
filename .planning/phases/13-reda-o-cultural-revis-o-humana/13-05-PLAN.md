---
phase: 13-reda-o-cultural-revis-o-humana
plan: 05
type: execute
wave: 4
depends_on: [13-04]
files_modified:
  - src/features/triagem/services/revisaoRedacaoService.ts
  - src/features/triagem/hooks/useRedacaoRevisao.ts
  - src/features/triagem/components/RedacaoCorBadge.tsx
  - src/features/triagem/components/RedacaoSidebar.tsx
  - src/features/triagem/components/RedacaoOverrideForm.tsx
  - src/features/triagem/components/RedacaoReviewPanel.tsx
  - src/router/routes.tsx
autonomous: true
requirements: [AVAL-07]

must_haves:
  truths:
    - "D-13 traceability: the RH human-review queue is 1-redação-por-vez with a color sidebar sorted by severity (vermelho→amarelo→verde, default filter vermelho+amarelo), per-dimension BARS slider override that live-recomputes composite/color, mandatory notas_revisor ≥50 chars on EVERY decision, and a decisao_revisor radio (aprovado/reprovado/duvida) where duvida escalates to the gestor — every essay enters pendente_humano and the AI never decides alone (RNF-07a/AVAL-07)."
    - "The RH read uses an explicit allowlist (never select('*')); the review write goes through the salvar_revisao_redacao SECURITY DEFINER RPC (role+own-vaga guarded), never a direct UPDATE."
    - "Every AI-derived block carries the SugestaoIABadge ('Sugestão da IA — decisão é sempre humana'); the candidate never reaches this RH surface."
  artifacts:
    - path: "src/features/triagem/components/RedacaoReviewPanel.tsx"
      provides: "1-at-a-time two-column review panel (35% AI analysis + 65% essay text 16-18px/1.6)"
      min_lines: 120
    - path: "src/features/triagem/components/RedacaoOverrideForm.tsx"
      provides: "4 BARS sliders (live recompute) + notas≥50 + decisao radio + A/R confirm dialogs + J/K/A/R/D shortcuts"
      min_lines: 100
    - path: "src/features/triagem/services/revisaoRedacaoService.ts"
      provides: "allowlist read of the review queue + salvarRevisao via salvar_revisao_redacao RPC"
      contains: "salvar_revisao_redacao"
  key_links:
    - from: "src/features/triagem/services/revisaoRedacaoService.ts"
      to: "salvar_revisao_redacao"
      via: "supabase.rpc"
      pattern: "rpc\\('salvar_revisao_redacao'"
    - from: "src/features/triagem/components/RedacaoReviewPanel.tsx"
      to: "src/features/triagem/components/SugestaoIABadge.tsx"
      via: "SugestaoIABadge on every AI block"
      pattern: "SugestaoIABadge"
    - from: "src/router/routes.tsx"
      to: "/rh/candidato/:id/redacao"
      via: "RoleGuard role={['rh','administrador']}"
      pattern: "/rh/candidato/:id/redacao"
---

<objective>
Build the RH human-review queue: the allowlist read service + RPC write, the TanStack Query hook, the 3-color badge, the severity-sorted sidebar, the BARS-slider override form, the 1-at-a-time two-column review panel, and the role-gated route (+ the in-app gestor `duvida` list). Covers AVAL-07 — mandatory human review post-AI, where the AI is always a suggestion and the human always decides.

Purpose: This is the one genuinely new surface in the phase (the rest is Phase 11/12 reuse). It reads the live verdict data the EF persisted (Plan 02/04) and writes the reviewer decision through the salvar_revisao_redacao RPC. The 3-color triage lives ONLY here (never candidate-facing). Every decision requires ≥50-char notas; "duvida" escalates without finalizing. Runs after Plan 04 so the RPC + regenerated types exist.

Output: 1 service + 1 hook + 4 components + 1 route (+ gestor duvida list), flipping the Plan 01 RedacaoOverrideForm + RedacaoSidebar RED scaffolds GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-reda-o-cultural-revis-o-humana/13-PATTERNS.md
@.planning/phases/13-reda-o-cultural-revis-o-humana/13-UI-SPEC.md

<interfaces>
<!-- Live after Plan 04 — read/write against these: -->
RPC salvar_revisao_redacao(p_redacao_id uuid, p_decisao text, p_notas text, p_scores_humanos jsonb)
  → role+own-vaga guarded; notas≥50; decisao ∈ {aprovado,reprovado,duvida}; duvida does NOT finalize
redacoes_candidato review columns: scores_dimensao, score_ponderado_0_100, classificacao_cor (cor),
  red_flag_etico, flags, analise_ia, scores_humanos, notas_revisor, decisao_revisor, revisada_*, status_analise, bloqueio_avanco
<!-- Reuse verbatim: -->
From src/features/triagem/components/SugestaoIABadge.tsx:
  SugestaoIABadge (SUGESTAO_IA_COPY='Sugestão da IA — decisão é sempre humana', accent #35BFAD, Sparkles)
From src/features/triagem/hooks/useTriagemPanel.ts:
  the hierarchical query-keys + useQuery (staleTime 5min, retry 2) shape to mirror
<!-- The compute-score math the slider recompute mirrors client-side (Plan 01): -->
computeScoreAndCors logic: composite = mean(valid dims) × 20; color from per-vaga threshold (default 40/64)
</interfaces>

<!-- RH desktop panel + neutral-score + SugestaoIABadge precedent: -->
@src/features/avaliacao/components/ScorecardAvaliacao.tsx
<!-- Queue list + filter/sort + query-keys precedent: -->
@src/features/triagem/components/TriagemTable.tsx
@src/features/triagem/hooks/useTriagemPanel.ts
<!-- Allowlist RH read + RPC write precedent: -->
@src/features/avaliacao/services/scoresRhService.ts
@src/features/triagem/services/triagemService.ts
<!-- radio-group + AlertDialog + slider-controlled patterns: -->
@src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx
<!-- RH route block to mirror + the RHLayout shell: -->
@src/router/routes.tsx
@src/components/pages/PerfilCandidatoRHPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: revisaoRedacaoService (allowlist read + RPC write) + useRedacaoRevisao hook</name>
  <files>src/features/triagem/services/revisaoRedacaoService.ts, src/features/triagem/hooks/useRedacaoRevisao.ts</files>
  <read_first>
    - src/features/avaliacao/services/scoresRhService.ts (lines ~122-154 — the explicit allowlist constant + RH/admin-only read, never select('*'))
    - src/features/triagem/services/triagemService.ts (lines ~220-236 — reprocessarAnalise RPC call + error mapping; the updateCandidaturaEtapa shape)
    - src/features/triagem/hooks/useTriagemPanel.ts (lines ~24-56 — hierarchical query-keys + useQuery staleTime 5min/retry 2/enabled)
    - .planning/phases/13-reda-o-cultural-revis-o-humana/13-RESEARCH.md §Code Examples (the REDACAO_ALLOWLIST string for the RH read)
  </read_first>
  <behavior>
    - listRedacoesRevisao(vagaId) reads redacoes_candidato via REDACAO_ALLOWLIST (explicit columns incl. scoring + review fields) + RH-only; never select('*').
    - the returned rows are sortable by severity (vermelho→amarelo→verde) and filterable by cor.
    - salvarRevisao(redacaoId, {decisao, notas, scores_humanos}) calls supabase.rpc('salvar_revisao_redacao', {...}) — never a direct UPDATE; maps insufficient_privilege → a FORBIDDEN error, a notas<50 → INVALID_INPUT.
    - getDuvidasGestor(vagaId?) reads rows WHERE decisao_revisor='duvida' (the gestor escalation list).
    - useRedacaoRevisao(vagaId) returns the queue list + a salvarRevisao mutation that invalidates the list on success.
  </behavior>
  <action>Create `revisaoRedacaoService.ts` (AVAL-07 / per D-13): the constant `REDACAO_ALLOWLIST = 'id, candidatura_id, pergunta_id, texto, scores_dimensao, score_ponderado_0_100, classificacao_cor, red_flag_etico, flags, analise_ia, scores_humanos, notas_revisor, decisao_revisor, revisada_por, revisada_em, status_analise, bloqueio_avanco'` (RH read — explicit, never select('*'), copy scoresRhService idiom); `listRedacoesRevisao(vagaId)` joining to candidaturas for the vaga filter; `getDuvidasGestor(vagaId?)` filtering decisao_revisor='duvida'; `salvarRevisao(redacaoId, payload)` calling `supabase.rpc('salvar_revisao_redacao', { p_redacao_id, p_decisao, p_notas, p_scores_humanos })` (never a direct UPDATE — the RPC is the only authorized write path; the trg_redacao_rh_only_review_fields trigger backstops it) with the error map (insufficient_privilege→FORBIDDEN, notas<50→INVALID_INPUT, transport→NETWORK_ERROR). Create `useRedacaoRevisao.ts` mirroring useTriagemPanel's query-key + useQuery shape (staleTime 5min, retry 2, enabled:!!vagaId) + a salvarRevisao mutation invalidating the queue key on success.</action>
  <verify>
    <automated>npm run test:run -- revisaoRedacaoService 2>&1 | tail -6; grep -q "rpc('salvar_revisao_redacao'" src/features/triagem/services/revisaoRedacaoService.ts && grep -q "REDACAO_ALLOWLIST" src/features/triagem/services/revisaoRedacaoService.ts && ! grep -q "select('\*')\|select(\"\*\")" src/features/triagem/services/revisaoRedacaoService.ts</automated>
  </verify>
  <done>revisaoRedacaoService reads via REDACAO_ALLOWLIST (no select('*')) + writes via salvar_revisao_redacao RPC (never direct UPDATE) + getDuvidasGestor; useRedacaoRevisao hook wraps list+mutation with invalidation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RedacaoCorBadge + RedacaoSidebar + RedacaoOverrideForm (flip Plan 01 GREEN)</name>
  <files>src/features/triagem/components/RedacaoCorBadge.tsx, src/features/triagem/components/RedacaoSidebar.tsx, src/features/triagem/components/RedacaoOverrideForm.tsx</files>
  <read_first>
    - src/features/triagem/components/SugestaoIABadge.tsx (lines ~30-42 — the Badge + cn tint convention to mirror for the 3 colors)
    - src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx (RadioGroup/RadioGroupItem/Label glass-selected pattern ~lines 117-144; the AlertDialog submit-confirm ~lines 326-355; the disabled-until-valid + counter idiom ~lines 288-293)
    - src/features/triagem/components/TriagemTable.tsx (the queue-list + filter + sort structure)
    - .planning/phases/13-reda-o-cultural-revis-o-humana/13-UI-SPEC.md §Color (RH 3-color triage tints + sidebar default filter/severity sort) + §Copywriting Contract (the override form + sidebar + decision-confirm copy)
    - src/features/triagem/components/__tests__/RedacaoOverrideForm.test.tsx + RedacaoSidebar.test.tsx (Plan 01 RED scaffolds to flip GREEN)
  </read_first>
  <behavior>
    - RedacaoCorBadge: verde→emerald tint, amarelo→amber tint, vermelho→red tint + a top badge whose tooltip names the firing rule ("score ≤40" / "red flag ético" / "D1≤2").
    - RedacaoSidebar: lists pending essays with color chips; default filter = vermelho+amarelo (verde on demand); sort = severity DESC (vermelho first); empty → "Nenhuma redação pendente de revisão."
    - RedacaoOverrideForm: 4 BARS sliders (D1-D4, 1-5, default = IA score) that recompute composite=mean(valid)×20 + color (per-vaga threshold) live on every onValueChange; notas textarea with "{N}/50 — mínimo 50 caracteres" counter (Salvar disabled <50); decisao radio aprovado/reprovado/duvida; A/R require AlertDialog confirm, D escalates inline (no destructive confirm); J/K/A/R/D keyboard shortcuts; Salvar calls salvarRevisao.
  </behavior>
  <action>Create the 3 RH components (AVAL-07 / per D-13). `RedacaoCorBadge.tsx`: copy the SugestaoIABadge Badge+cn tint convention; map the 3 triage colors (UI-SPEC §Color: verde emerald-500/15, amarelo amber-500/15, vermelho red-500/15) + the vermelho top badge with the rule tooltip. RH-facing ONLY. `RedacaoSidebar.tsx`: the queue list (mirror TriagemTable) with color chips, the "Filtrar por cor" filter defaulting to vermelho+amarelo, severity-DESC sort, the empty-state copy — all copy verbatim from UI-SPEC. `RedacaoOverrideForm.tsx`: the 4 BARS `slider`s (D1-D4 labeled Experiência UAU · Inovação · Atitude de Dono · Sede de Crescimento, with "Ética como princípio fundante acima das 4" note) defaulting to the IA scores and recomputing composite/color LIVE on change (pure derived state mirroring computeScoreAndCors — mean of valid dims ×20, color from the per-vaga threshold); the notas_revisor textarea with the ≥50 counter gating Salvar; the decisao radio (RadioGroup glass-selected, options Aprovado/Reprovado/"Dúvida — escalar ao gestor" + the duvida helper); the A/R AlertDialog confirms (Reprovar uses #EF4444 destructive affordance) + D inline escalate; the J/K/A/R/D + ? keyboard shortcuts; Salvar → salvarRevisao(redacaoId, {decisao, notas, scores_humanos: the slider values}). All copy verbatim from UI-SPEC §Copywriting Contract. This flips the Plan 01 RedacaoOverrideForm + RedacaoSidebar RED tests GREEN.</action>
  <verify>
    <automated>npm run test:run -- RedacaoOverrideForm RedacaoSidebar 2>&1 | tail -8</automated>
  </verify>
  <done>3 RH components built with UI-SPEC copy/color; sidebar severity-sort + default filter; override form 4 BARS sliders live-recompute + notas≥50 gate + decisao radio + A/R confirm + D escalate + shortcuts; CorBadge 3 tints + rule tooltip; Plan 01 RedacaoOverrideForm + RedacaoSidebar tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: RedacaoReviewPanel (1-at-a-time two-column) + gestor duvida list + RH route</name>
  <files>src/features/triagem/components/RedacaoReviewPanel.tsx, src/router/routes.tsx</files>
  <read_first>
    - src/features/avaliacao/components/ScorecardAvaliacao.tsx (the RH desktop panel contract: per-dimension BARS + citations + reasoning, SugestaoIABadge per AI block ~lines 128-220; loading/error/empty states ~lines 314-340; the candidate-never-reaches note ~lines 9-12)
    - src/components/pages/PerfilCandidatoRHPage.tsx (the RHLayout + Glass/GlassButton wrapper ~lines 602-609)
    - src/router/routes.tsx (the /rh/candidato/:id RH route block to mirror, role-gated)
    - .planning/phases/13-reda-o-cultural-revis-o-humana/13-UI-SPEC.md §Component Inventory (the 35%/65% layout) + §Typography (essay text 16-18px/line-height 1.6 — the one place line-height goes 1.6+) + §Copywriting Contract (RH queue copy)
  </read_first>
  <action>Create `RedacaoReviewPanel.tsx` (AVAL-07): the RHLayout + Glass desktop shell (NOT the candidate glass-over-gradient — copy PerfilCandidatoRHPage wrapper); 1-redação-por-vez two-column layout — LEFT 35%: the RedacaoSidebar + "Análise da IA" heading + the AI scores/citations/reasoning per the 4 Beauty Smile dimensions each carrying `<SugestaoIABadge variant="full"/>` (every AI-derived block, never on a candidate surface) + the RedacaoOverrideForm (sliders + notas + decisao + Salvar) anchored here; RIGHT 65%: the full essay text rendered at `text-base leading-relaxed` (≈16px/1.625 — the load-bearing reading panel per PRD RF-R-22, never compress below 16px) with the "Redação do candidato" heading + the vermelho top badge (RedacaoCorBadge) when classificacao_cor='vermelho'. Wire `useRedacaoRevisao(vagaId)` for the list + the salvarRevisao mutation; toast.success "Revisão salva." / toast.error on save. Include the loading/error/empty states (copy ScorecardAvaliacao). Add a `duvida`-filtered list view (gestor escalation — reads getDuvidasGestor; in-app only, notification plumbing OUT OF SCOPE per Open Question 4). Then EDIT `routes.tsx`: add `{ path: '/rh/candidato/:id/redacao', element: <RoleGuard role={['rh','administrador']}><RedacaoReviewPanel /></RoleGuard> }` mirroring the RH namespace block. Confirm no file-overlap collision with Plan 03's route edit (Plan 03 added the candidate route; this adds the RH route — different lines, sequential waves).</action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -q "/rh/candidato/:id/redacao" src/router/routes.tsx && grep -q "SugestaoIABadge" src/features/triagem/components/RedacaoReviewPanel.tsx && grep -q "leading-relaxed" src/features/triagem/components/RedacaoReviewPanel.tsx</automated>
  </verify>
  <done>RedacaoReviewPanel is 1-at-a-time two-column (35% AI+override / 65% essay 16-18px/1.6) with SugestaoIABadge on every AI block + gestor duvida list; RH route registered role-gated; build exits 0; candidate never reaches this surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH browser → redacoes_candidato read | RH read must use an allowlist; the route is role-gated; the candidate must never reach this surface. |
| RH browser → salvar_revisao_redacao RPC | The review write is the privileged mutation — role + vaga-ownership enforced in the RPC. |
| reviewer decision → funil progression | RNF-07a: recording a decision must NOT auto-advance/auto-reject; duvida escalates without finalizing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Elevation | RH writes a decision outside their vaga | mitigate | salvarRevisao only calls salvar_revisao_redacao (role+own-vaga guard); no direct UPDATE path in the service. |
| T-13-05-02 | Tampering | review-write modifies texto/score/IA fields | mitigate | The RPC writes only review fields; the BEFORE UPDATE trigger RAISEs on any non-review-field change (DB-enforced, Plan 02). |
| T-13-05-03 | Repudiation | AI decision recorded without a human justification | mitigate | notas_revisor ≥50 chars gated client-side (Salvar disabled) AND server-side (RPC + CHECK); decisao required on every save. |
| T-13-05-04 | Info disclosure | candidate reaches the RH review surface | mitigate | Route role-gated RoleGuard role={['rh','administrador']}; RLS denies the candidate the verdict rows; the panel is desktop RH-only. |
| T-13-05-05 | Integrity | "duvida" silently finalizes the evaluation | mitigate | RPC leaves status_analise=pendente_humano on duvida; the gestor list reads decisao_revisor='duvida'; only aprovado/reprovado set concluida. |
| T-13-05-SC | Tampering | npm web installs | accept | Zero net-new web packages — slider/radio-group/alert-dialog vendored since Phase 7 (UI-SPEC Registry Safety). No [ASSUMED]/[SUS]. |
</threat_model>

<verification>
- `npm run test:run -- revisaoRedacaoService RedacaoOverrideForm RedacaoSidebar` GREEN (the last 2 Plan 01 RED scaffolds flipped)
- `npm run build` exits 0
- grep: review write uses rpc('salvar_revisao_redacao'); RH read uses REDACAO_ALLOWLIST; no select('*'); SugestaoIABadge present; essay panel leading-relaxed
- route `/rh/candidato/:id/redacao` registered role-gated; no collision with Plan 03's candidate route
- the candidate never reaches the RH surface (route gate + RLS deny)
- tsc baseline does not grow beyond documented carryover
</verification>

<success_criteria>
- RH review queue: 1-at-a-time two-column, severity-sorted color sidebar (default vermelho+amarelo), BARS slider override with live recompute, notas≥50 on every decision, decisao radio with duvida→gestor escalation
- Review write goes through salvar_revisao_redacao RPC only; RH read via allowlist (no select('*'))
- SugestaoIABadge on every AI block; 3-color triage RH-only; candidate never reaches this surface
- Plan 01 RED scaffolds RedacaoOverrideForm + RedacaoSidebar flipped GREEN
- All 3 phase requirements (AVAL-05/06/07) now have a live surface end-to-end
</success_criteria>

<output>
Create `.planning/phases/13-reda-o-cultural-revis-o-humana/13-05-SUMMARY.md` when done
</output>
