---
phase: 36-deliverability-sender-identity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/assert-no-secrets.mjs
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [DELIV-02]

must_haves:
  truths:
    - "Um build público que carregasse uma chave ou endpoint do Resend falha o `npm run build` localmente e o job `e2e` no CI"
    - "O guard é comprovadamente real: com um secret sintético plantado em build/, ele sai com exit 1"
    - "O guard nunca imprime o valor casado — só arquivo, offset, nome do padrão e um excerto mascarado"
    - "Um build/ ausente ou vazio faz o guard FALHAR, nunca passar silenciosamente"
    - "O guard passa contra o build atual do repo (zero falso-positivo com o domínio recruta.beautysmile.com.br já legitimamente no bundle)"
  artifacts:
    - path: "scripts/assert-no-secrets.mjs"
      provides: "Gate de saída de build para DELIV-02 — leitura pura, sem rede, sem eval"
      min_lines: 90
      contains: "process.exit(1)"
    - path: "package.json"
      provides: "Encadeamento do guard no postbuild + aliases standalone"
      contains: "assert-no-secrets"
    - path: ".github/workflows/ci.yml"
      provides: "Step dedicado no job e2e, logo após Bundle gate (PERF-03)"
      contains: "assert-no-secrets.mjs"
  key_links:
    - from: "package.json postbuild"
      to: "scripts/assert-no-secrets.mjs"
      via: "node scripts/assert-no-secrets.mjs && node scripts/assert-chunks.mjs"
      pattern: "postbuild.*assert-no-secrets\\.mjs.*assert-chunks\\.mjs"
    - from: ".github/workflows/ci.yml job e2e"
      to: "scripts/assert-no-secrets.mjs"
      via: "step run"
      pattern: "node scripts/assert-no-secrets\\.mjs"
    - from: "scripts/assert-no-secrets.mjs"
      to: "build/"
      via: "walk recursivo a partir de join(__dirname, '..', 'build')"
      pattern: "'\\.\\.', 'build'"
---

<objective>
Provar mecanicamente que nenhuma credencial ou endpoint do provedor de e-mail entra no bundle público — com um gate de build que falha duro local e no CI, e cuja realidade é demonstrada por um meta-teste.

Purpose: DELIV-02 é uma asserção de **ausência**. Asserções negativas só valem se o mecanismo que as verifica falhar ruidosamente quando não consegue verificar. Este plano entrega o mecanismo e a prova de que ele não é no-op.
Output: `scripts/assert-no-secrets.mjs` + encadeamento no `postbuild` + step no job `e2e` do CI + transcrição do meta-teste no SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-deliverability-sender-identity/36-CONTEXT.md
@.planning/phases/36-deliverability-sender-identity/36-RESEARCH.md
@.planning/phases/36-deliverability-sender-identity/36-VALIDATION.md
@scripts/assert-chunks.mjs
@package.json
@.github/workflows/ci.yml

<interfaces>
<!-- Fatos de repo que decidem o desenho. Verificados na pesquisa; não re-derivar. -->

Diretório de saída ....... build/   (vite.config.ts:123 `outDir: 'build'`) — NUNCA dist/, que produziria um guard que sempre passa
Modelo de estilo ......... scripts/assert-chunks.mjs (150 linhas): shebang, docblock com postura de segurança + lista numerada de asserções,
                           imports só de node:fs/node:path/node:url, acumulador `failures[]` (não aborta no 1º), diretório ausente ⇒ exit 1,
                           inventário legível antes das asserções, `✗ `/`✓ ` nas mensagens, ID do requisito entre parênteses, idioma inglês
Invocação atual .......... package.json:98 `"postbuild": "node scripts/assert-chunks.mjs"` · package.json:99 `"assert:chunks"`
CI ....................... NÃO existe job `build`. Jobs reais: unit, deno-test, e2e, lighthouse.
                           O step `Bundle gate (PERF-03)` está no job `e2e` (ci.yml:111), logo após o step `Build` (ci.yml:105-106).
                           O job `lighthouse` também roda `npm run build` (ci.yml:135) → herda o postbuild de graça.

Padrões do guard — EXATAMENTE três famílias (4 regexes: a família "chave" tem 2), nenhuma a mais:
  resend-api-key ........... /\bre_[A-Za-z0-9]{6,12}_[A-Za-z0-9]{16,}\b/g
  resend-key-formatdrift ... /\bre_[A-Za-z0-9_]{28,}\b/g
  resend-endpoint .......... /api\.resend\.com/g
  resend-key-identifier .... /RESEND_API_KEY/g

O `\b` inicial é load-bearing: sem ele, `measure_something` casa `re_something` e o guard nasce vermelho (Pitfall 4).
PROIBIDO como padrão: `recruta.beautysmile.com.br`, `beautysmile.com.br`, `resend.dev` — o domínio de envio JÁ está
legitimamente no bundle (src/components/pages/CriarEditarVagaPage.tsx:565; ErrorBoundary.tsx:202; AutorizacoesStep.tsx:188). Pitfall 5.

Extensões binárias a PULAR (o resto é tudo varrido, inclusive index.html, css, svg, json, map, webmanifest):
  png jpg jpeg gif webp avif ico svgz woff woff2 ttf otf eot mp4 webm pdf zip

Chave sintética para o meta-teste (exemplo público da doc do Resend, não é uma credencial viva):
  re_c1tpEyD8_REDACTED_P36_SYNTHETIC
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar scripts/assert-no-secrets.mjs (walk recursivo de build/, regex ancorado, match mascarado)</name>
  <files>scripts/assert-no-secrets.mjs</files>
  <read_first>
    - scripts/assert-chunks.mjs (arquivo inteiro, 150 linhas) — o contrato de estilo a replicar: docblock, imports, `failures[]`, tratamento de diretório ausente/vazio, formato das mensagens `✗ `/`✓ `, report final e `process.exit`
    - .planning/phases/36-deliverability-sender-identity/36-RESEARCH.md § Q5 (tabela do contrato, linha a linha), § Code Examples "assert-no-secrets.mjs — esqueleto", § Pattern 1, e Pitfalls 4, 5, 6 e 7
    - vite.config.ts (linha ~123, `outDir: 'build'`) — confirmar que o alvo é `build/`, nunca `dist/`
    - src/components/pages/CriarEditarVagaPage.tsx (linha ~565) — a ocorrência legítima de `recruta.beautysmile.com.br` que explica por que o domínio não pode ser padrão do guard
  </read_first>
  <action>
    Criar `scripts/assert-no-secrets.mjs` no estilo exato de `scripts/assert-chunks.mjs` (idioma inglês — código técnico, conforme CLAUDE.md), com:

    (1) Shebang `#!/usr/bin/env node` e docblock JSDoc contendo: título `assert-no-secrets.mjs — the DELIV-02 build-output secret gate`, declaração explícita de postura de segurança ("reads ONLY the local `build/` output — no untrusted input, no network, no eval"), a nota de que roda sob o BUILD gate e não sob o Vitest (`scripts/**` está no `test.exclude`), a lista numerada das asserções, e — obrigatório — um bloco `META-TEST` com o comando exato para reprovar que o gate não é no-op (plantar a chave sintética em `build/`, rodar, exigir exit 1, remover). Fechar com `@see` para `36-RESEARCH.md` (Q5, Pitfalls 4-7) e para `scripts/assert-chunks.mjs`.

    (2) Imports apenas de `node:fs`, `node:path` e `node:url`. `__dirname` via `dirname(fileURLToPath(import.meta.url))`; `BUILD_DIR = join(__dirname, '..', 'build')` com comentário citando `vite.config.ts:123`.

    (3) Constante `BINARY_EXT` com a regex de extensões a pular (lista no bloco `<interfaces>`) e constante `PATTERNS` como array de `{ name, re }` com exatamente as quatro entradas do bloco `<interfaces>`, cada regex com flag `g`. Logo abaixo, um comentário `NOTE:` explicando que `beautysmile.com.br` / `recruta.beautysmile.com.br` estão DELIBERADAMENTE ausentes por já serem enviados legitimamente (citar `CriarEditarVagaPage.tsx:565`).

    (4) Walk recursivo de `BUILD_DIR` com `readdirSync`/`statSync`, acumulando os arquivos de texto (todo arquivo cuja extensão não casa `BINARY_EXT`). Contar arquivos varridos e bytes lidos.

    (5) Falha dura em três condições, cada uma com mensagem instrutiva e `process.exit(1)`: `build/` não existe ou não é legível (mensagem manda rodar `npm run build` primeiro — cláusula que converte "apontei para o lugar errado" de falso-verde em erro visível); zero arquivos de texto varridos; qualquer match de padrão.

    (6) Para cada match: registrar em `failures[]` o caminho RELATIVO ao repo, o byte offset do match, o nome do padrão, o comprimento do match e um excerto MASCARADO com no máximo os 4 primeiros caracteres seguidos de reticências. NUNCA imprimir o valor casado — um log do GitHub Actions é retido e não é cofre (Pitfall 7). Coletar todos os hits antes de sair (não abortar no primeiro).

    (7) Report final no padrão do irmão: imprimir as notas/inventário (incluindo a linha com a contagem `N files scanned` — um `0 files scanned` visível é o antídoto para a falha silenciosa), e em caso de falha `console.error('\nassert-no-secrets FAILED:')` + lista + `process.exit(1)`; em caso de sucesso `✓ assert-no-secrets PASSED — no Resend key/endpoint in the public bundle (DELIV-02).` + `process.exit(0)`.

    NÃO estender `scripts/assert-chunks.mjs` — os dois gates têm ciclos de vida diferentes (um afrouxa quando o bundle cresce legitimamente; este nunca afrouxa). NÃO adicionar rede, `eval`, nem dependência npm.

    Ao terminar, rodar `npm run build` e o guard standalone contra o `build/` atual do repo: ele DEVE passar. Se falhar, é falso-positivo de regex — corrigir a âncora, nunca afrouxar o padrão removendo uma das quatro famílias.
  </action>
  <verify>
    <automated>npm run build && node scripts/assert-no-secrets.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/assert-no-secrets.mjs` (após `npm run build`) termina com exit 0 e a saída contém a contagem de arquivos varridos com valor > 0. Registrar o número no SUMMARY.
    - A saída de sucesso contém a string `assert-no-secrets PASSED` e cita `DELIV-02`.
    - `mv build /tmp/build-bkp && node scripts/assert-no-secrets.mjs; echo "exit=$?"; mv /tmp/build-bkp build` → `exit=1` (build/ ausente FALHA, não passa).
    - `grep -cE "^import .* from 'node:(fs|path|url)'" scripts/assert-no-secrets.mjs` retorna 3 e `grep -cE "fetch\(|require\(|eval\(|child_process" scripts/assert-no-secrets.mjs` retorna 0.
    - `grep -c "beautysmile" scripts/assert-no-secrets.mjs | head -1` — qualquer ocorrência de `beautysmile` no arquivo está em linha de comentário; `grep -n "beautysmile" scripts/assert-no-secrets.mjs | grep -v "//" | grep -c .` retorna 0 (o domínio nunca é padrão do guard).
    - `grep -c "\\\\b" scripts/assert-no-secrets.mjs` ≥ 2 — os padrões de chave usam limite de palavra.
    - `grep -n "'build'" scripts/assert-no-secrets.mjs` retorna ≥ 1 linha e `grep -c "'dist'" scripts/assert-no-secrets.mjs` retorna 0.
  </acceptance_criteria>
  <done>Guard existe, é leitura pura sem rede/eval, varre todo o `build/`, passa contra o bundle atual, falha com `build/` ausente, e nunca imprime o valor casado.</done>
</task>

<task type="auto">
  <name>Task 2: Meta-teste — plantar secret sintético em build/ e exigir exit 1</name>
  <files>scripts/assert-no-secrets.mjs (docblock META-TEST), build/ (arquivo temporário, criado e removido)</files>
  <read_first>
    - scripts/assert-no-secrets.mjs (criado na Task 1) — o bloco `META-TEST` do docblock, os nomes dos padrões e o formato de mensagem mascarada
    - .planning/phases/36-deliverability-sender-identity/36-VALIDATION.md § Per-Task Verification Map, linha "DELIV-02 | O gate é real, não no-op"
    - .planning/phases/36-deliverability-sender-identity/36-RESEARCH.md § Validation Architecture → SC2 (o argumento de por que sem este passo o SC2 é indistinguível de um script que sempre passa)
    - scripts/assert-chunks.mjs (docblock, linhas 12-13) — a lição literal: "Today (pre-split) it MUST exit non-zero against the monolith, proving it is a real gate, not a no-op"
  </read_first>
  <action>
    Executar o meta-teste que prova que o gate não é no-op, em quatro provas separadas, uma por família de padrão relevante, e capturar a transcrição (comando + exit code + saída mascarada) para o SUMMARY:

    (a) Com um `build/` recém-gerado, escrever um arquivo temporário `build/__meta_test_secret.js` contendo a chave sintética `re_c1tpEyD8_REDACTED_P36_SYNTHETIC` (exemplo público da doc do Resend — NÃO é credencial viva; nunca usar uma chave real aqui). Rodar `node scripts/assert-no-secrets.mjs` e exigir exit 1 com o padrão `resend-api-key` citado. Remover o arquivo.

    (b) Repetir com o conteúdo `https://api.resend.com/emails` e exigir exit 1 citando `resend-endpoint`. Remover.

    (c) Repetir com o conteúdo `RESEND_API_KEY` e exigir exit 1 citando `resend-key-identifier`. Remover.

    (d) Repetir plantando a chave sintética dentro de `build/index.html` (num comentário HTML), para provar que o walk cobre o `index.html` e não apenas `build/assets/*` — exigir exit 1. Restaurar o `index.html` original (regenerar com `npm run build` é a forma mais segura de restaurar).

    Em TODAS as provas, verificar que a saída de erro NÃO contém a chave sintética completa: a mensagem deve trazer arquivo, offset, nome do padrão, comprimento e no máximo os 4 primeiros caracteres. Se a chave completa aparecer, corrigir o mascaramento na Task 1 antes de prosseguir (Pitfall 7).

    Após as quatro provas, confirmar que `build/` voltou ao estado limpo e que `node scripts/assert-no-secrets.mjs` passa novamente (exit 0). Nenhum arquivo de meta-teste pode ficar no repo nem no `build/` ao final. Transcrever no docblock `META-TEST` do guard o comando canônico de reprodução (prova a), para que qualquer pessoa possa repetir o experimento sem reler este plano.
  </action>
  <verify>
    <automated>npm run build >/dev/null 2>&1; printf 're_c1tpEyD8_REDACTED_P36_SYNTHETIC' > build/__meta_test_secret.js; node scripts/assert-no-secrets.mjs; ec=$?; rm -f build/__meta_test_secret.js; test "$ec" -eq 1 && node scripts/assert-no-secrets.mjs && echo "META-TEST OK: guard exits 1 with planted secret, 0 when clean"</automated>
  </verify>
  <acceptance_criteria>
    - Prova (a): exit code 1, saída cita o padrão `resend-api-key` e o caminho `build/__meta_test_secret.js`.
    - Prova (b): exit code 1, saída cita `resend-endpoint`.
    - Prova (c): exit code 1, saída cita `resend-key-identifier`.
    - Prova (d): exit code 1 com o secret plantado dentro de `build/index.html` — prova que o escopo do walk não é só `build/assets/*`.
    - Em nenhuma das provas a saída contém a string completa `re_c1tpEyD8_REDACTED_P36_SYNTHETIC`: `node scripts/assert-no-secrets.mjs 2>&1 | grep -c "REDACTED_P36_SYNTHETIC"` retorna 0 durante a prova (a).
    - Estado final: `ls build/__meta_test_secret.js` retorna "No such file", e `node scripts/assert-no-secrets.mjs` termina com exit 0.
    - `grep -n "META-TEST" scripts/assert-no-secrets.mjs` retorna ≥ 1 linha com o comando de reprodução.
    - A transcrição das quatro provas (comando + exit code) está no SUMMARY.
  </acceptance_criteria>
  <done>As quatro provas plantadas produzem exit 1, o secret nunca aparece inteiro na saída, o `build/` fica limpo e o comando de reprodução está gravado no docblock do guard.</done>
</task>

<task type="auto">
  <name>Task 3: Encadear no postbuild, adicionar aliases npm e o step no job e2e do CI</name>
  <files>package.json, .github/workflows/ci.yml</files>
  <read_first>
    - package.json (linhas 95-113, bloco `scripts`) — o `postbuild` atual e o alias `assert:chunks`
    - .github/workflows/ci.yml (linhas 95-138) — o job `e2e` inteiro (step `Build` em ~105, step `Bundle gate (PERF-03)` em 111 com o comentário CI-10 que justifica o step dedicado) e o job `lighthouse` (que também roda `npm run build`)
    - .planning/phases/36-deliverability-sender-identity/36-RESEARCH.md § "Encadeamento no `package.json` e no CI" e § Q5 (a correção: NÃO existe job `build`)
    - .planning/phases/36-deliverability-sender-identity/36-CONTEXT.md § Correções e Decisões Pós-Research — a correção do job de CI
  </read_first>
  <action>
    (1) `package.json`: alterar o valor de `postbuild` para encadear `node scripts/assert-no-secrets.mjs && node scripts/assert-chunks.mjs` — segurança ANTES de performance (se algo vazou, o tamanho do chunk é irrelevante). Adicionar o alias standalone `"assert:no-secrets": "node scripts/assert-no-secrets.mjs"` ao lado de `assert:chunks`.

    (2) `package.json`: adicionar também `"check:resend-dominio": "node scripts/check-resend-dominio.mjs"`. Este `package.json` é propriedade EXCLUSIVA deste plano na Wave 1; o arquivo `scripts/check-resend-dominio.mjs` é criado pelo Plano 36-03 na mesma wave. Uma entrada de `scripts` é apenas uma string — a ordem de execução dos dois planos é irrelevante. NÃO adicionar este comando a nenhum hook (`prebuild`/`postbuild`/`pretest`) e NÃO adicioná-lo ao CI: ele faz rede e é opt-in por decisão travada do CONTEXT.

    (3) `.github/workflows/ci.yml`: no job `e2e`, imediatamente APÓS o step `Bundle gate (PERF-03)` (ci.yml:111), adicionar um step `Secret gate (DELIV-02)` que roda `node scripts/assert-no-secrets.mjs`, precedido de um comentário no estilo do CI-10 existente explicando que o guard já se auto-gateia via `postbuild` e que este step re-roda para emitir um sinal de falha distinto. NÃO criar job novo, NÃO tocar nos jobs `unit`, `deno-test` ou `lighthouse` — o `lighthouse` já herda o gate pelo `postbuild`.

    (4) Restrição dura sobre o `ci.yml`: nenhuma variável de ambiente, `secrets.*` ou string em CAIXA ALTA `RESEND` pode ser introduzida — o CI roda sem chave viva (DELIV-03) e a asserção `grep -n "RESEND" .github/workflows/ci.yml` deve continuar retornando zero linhas. Se precisar mencionar o provedor num comentário, escrever `Resend` (capitalizado), nunca `RESEND`.
  </action>
  <verify>
    <automated>npm run build && node -e "const s=require('./package.json').scripts; if(!/assert-no-secrets\.mjs.*&&.*assert-chunks\.mjs/.test(s.postbuild)) throw new Error('postbuild not chained'); if(!s['assert:no-secrets']||!s['check:resend-dominio']) throw new Error('alias missing'); console.log('package.json wiring OK')" && grep -q "node scripts/assert-no-secrets.mjs" .github/workflows/ci.yml && test "$(grep -c 'RESEND' .github/workflows/ci.yml)" -eq 0 && echo "CI wiring OK; zero RESEND refs in ci.yml"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` executa os dois gates em sequência e termina com exit 0; a saída contém tanto `assert-no-secrets PASSED` quanto `assert-chunks PASSED`, nessa ordem.
    - `node -e "console.log(require('./package.json').scripts.postbuild)"` imprime `node scripts/assert-no-secrets.mjs && node scripts/assert-chunks.mjs`.
    - `npm run assert:no-secrets` termina com exit 0 após um build.
    - `node -e "console.log(require('./package.json').scripts['check:resend-dominio'])"` imprime `node scripts/check-resend-dominio.mjs`.
    - `grep -n "Secret gate (DELIV-02)" .github/workflows/ci.yml` retorna exatamente 1 linha, e ela está dentro do job `e2e`, depois da linha do `Bundle gate (PERF-03)`.
    - `grep -c "RESEND" .github/workflows/ci.yml` retorna 0.
    - `grep -c "check-resend-dominio" .github/workflows/ci.yml` retorna 0 — o script de rede nunca entra no CI.
    - `npm run test:run` continua verde (nenhum efeito colateral em `scripts/**`, que já está no `test.exclude`).
  </acceptance_criteria>
  <done>`npm run build` falha se um segredo Resend vazar; o job `e2e` tem um step dedicado com sinal de falha distinto; o script de rede está disponível como alias npm e ausente do CI; `ci.yml` continua sem nenhuma referência `RESEND`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `build/` → navegador do candidato/RH (internet pública) | Tudo que entra em `build/` é publicamente legível; é a fronteira que o DELIV-02 defende |
| Guard → log do GitHub Actions | A saída do gate é retida e potencialmente visível; é uma superfície de vazamento própria |
| `package.json` scripts → shell do desenvolvedor e do runner de CI | Encadeamento errado (`;` em vez de `&&`) transforma o gate em decoração |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02-01 | Information Disclosure | Chave Resend inlinada no bundle via `VITE_` ou hardcode | mitigate | `scripts/assert-no-secrets.mjs` varre todo o `build/` com 4 padrões ancorados e falha o build; enforcement em `postbuild` (local + jobs `e2e` e `lighthouse`) + step dedicado no `e2e` |
| T-36-02-02 | Information Disclosure | O próprio guard vaza o segredo no log público do CI ao imprimir o match | mitigate | Match sempre mascarado (arquivo + offset + nome do padrão + comprimento + ≤4 chars); acceptance criteria da Task 2 grep-a a ausência da chave completa na saída |
| T-36-02-03 | Repudiation | Guard no-op (aponta para `dist/`, ou `build/` vazio) dá falso senso de segurança permanente | mitigate | Alvo `join(__dirname,'..','build')` conforme `vite.config.ts:123`; falha dura em diretório ausente/zero arquivos; meta-teste de 4 provas na Task 2 comprova exit 1 |
| T-36-02-04 | Denial of Service | Falso-positivo do regex (âncora ausente) faz o time afrouxar o gate até matá-lo | mitigate | `\b` obrigatório nos padrões de chave; domínio Beauty Smile proibido como padrão; guard validado contra o `build/` atual antes de ser encadeado |
| T-36-02-05 | Elevation of Privilege | Script opt-in de rede (`check-resend-dominio`) promovido a step de CI exigiria chave viva nos GitHub Secrets | mitigate | Alias npm sem hook; `grep -c "check-resend-dominio" .github/workflows/ci.yml` = 0 e `grep -c "RESEND" ci.yml` = 0 como acceptance criteria |
| T-36-02-SC | Tampering | Cadeia de suprimentos (npm/pip/cargo installs) | accept | Zero pacotes novos nesta fase (36-RESEARCH.md § Package Legitimacy Audit: tabela vazia, delta de dependências = 0). O guard é `node:` stdlib puro. Nenhum checkpoint de legitimidade é aplicável |
</threat_model>

<verification>
- `npm run build` verde com os dois gates na ordem segurança→performance
- `node scripts/assert-no-secrets.mjs` exit 0 com `N files scanned`, N > 0
- Meta-teste: 4 provas plantadas ⇒ exit 1 em todas; `build/` limpo ao final
- `grep -rn "RESEND\|api\.resend\.com" build/ ; echo "exit=$?"` → `exit=1` (cross-check independente do guard)
- `grep -c "RESEND" .github/workflows/ci.yml` → 0
</verification>

<success_criteria>
1. Um segredo ou endpoint Resend em qualquer arquivo de texto de `build/` (inclusive `index.html`) faz `npm run build` sair não-zero, local e no CI.
2. O meta-teste está executado e transcrito: o gate é comprovadamente real, não no-op.
3. A saída do guard nunca contém o valor casado; um `build/` ausente falha em vez de passar.
</success_criteria>

<output>
Create `.planning/phases/36-deliverability-sender-identity/36-02-SUMMARY.md` when done
</output>
