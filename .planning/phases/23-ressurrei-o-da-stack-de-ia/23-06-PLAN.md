---
phase: 23-ressurrei-o-da-stack-de-ia
plan: 06
type: execute
wave: 4
depends_on: ["23-01", "23-02", "23-03", "23-04", "23-05"]
files_modified: []
autonomous: false
requirements: [AI-01, AI-04, AI-06, UX-07, UX-09]
must_haves:
  truths:
    - "As 9 EFs tocadas (7 IA + cost-alerter + consolidar) rodam o bundle NOVO em PROD — a edição de _shared alcança cada EF redeployada (bundle-freeze) (AI-01/04/06/UX-07/UX-09)"
    - "Os 5 call_types antes quebrados logam a semver REAL em ai_call_logs numa invocação live (não 0.0.0/stub/500) (AI-01)"
    - "bigfive_devolutiva loga em ai_call_logs (enum aceito) numa invocação live (AI-01)"
    - "Nenhum verify_jwt/autz foi alterado (Phase 24) e todos os imports permanecem npm: ESTÁTICOS (sem ERR_MODULE_NOT_FOUND)"
  artifacts: []
  key_links:
    - from: "supabase functions deploy"
      to: "9 EFs com bundle _shared novo"
      via: "redeploy pós-código+migration"
      pattern: "functions deploy"
---

<objective>
[BLOCKING · non-autonomous · deploy PROD] Redeployar as 9 Edge Functions tocadas para que as edições de `_shared/*` (Planos 23-01/02/03) e dos index.ts (23-02/04) alcancem o runtime — bundle-freeze: uma mudança em `_shared` só vale para a EF redeployada. Smoke live de que os call_types antes quebrados agora logam a semver real.

Purpose: cada EF carrega sua própria cópia de `_shared` no bundle de deploy; sem redeploy, as EFs continuam rodando o código velho (stubs/breaker morto/timeout não-retriável). Este é o último passo do phase, depois de todo o código mergeado (Waves 1-2) e das migrations aplicadas (Wave 3 — o enum precisa existir antes do redeploy da devolutiva ser útil).
Output: 9 EFs redeployadas + smoke live registrado; 4 UATs live diferidos ao HUMAN-UAT. NÃO tocar verify_jwt/autz (Phase 24).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-ressurrei-o-da-stack-de-ia/23-RESEARCH.md

<interfaces>
Conjunto de redeploy (9 EFs): analise-candidato-individual, comparativo-candidatos, avaliar-redacao, avaliar-redacao-cultural, avaliar-transcricao-entrevista, gerar-guia-entrevista, gerar-devolutiva-bigfive (as 7 de IA — bundlam _shared/ai-client|circuit-breaker|prompt-loader|audit-logger|ai-cost) + consolidar-decisao-final (UX-09, index.ts editado) + cost-alerter (messages.ts extraído no 23-03).
Flags de deploy a PRESERVAR: cost-alerter `--no-verify-jwt` (server-internal, Bearer Vault self-auth); analise-candidato-individual `--no-verify-jwt` (Bearer self-auth). As demais mantêm o verify_jwt atual — NÃO alterar (Phase 24).
Seed de UAT (memory): candidatura live do funil E2E em PROD (candidato.funil@teste.com) — usar p/ o smoke das EFs.
Regra load-bearing: imports `npm:` ESTÁTICOS (nunca `await import([...].join(""))`) — senão 500 ERR_MODULE_NOT_FOUND.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 [BLOCKING]: redeploy das 9 EFs + confirmação de imports estáticos</name>
  <files>(nenhum arquivo — deploy PROD)</files>
  <read_first>
    - 23-RESEARCH.md §Runtime State Inventory bundle-freeze (328), Anti-Patterns (168-169)
    - reference_ef_shared_bundle_freeze + reference_ef_npm_join_import_bug (memory)
  </read_first>
  <what-built>
    Redeploy de 9 EFs para propagar o bundle _shared novo + os index.ts editados. Pré-requisito: Waves 1-3 completas (código mergeado + migrations aplicadas).
  </what-built>
  <action>
    Runbook (rodar da raiz do repo, com o CLI linkado ao projeto isljnozzlvckrgjjbjwp):
    1. Confirmar imports estáticos ANTES do deploy: `grep -rn 'import(\[' supabase/functions` deve retornar 0 (nenhum `await import([...].join(""))`); os SDKs continuam `npm:@anthropic-ai/sdk@0.102.0` / `npm:openai@6.42.0` estáticos.
    2. Redeployar CADA EF (preservando flags): `supabase functions deploy analise-candidato-individual --no-verify-jwt`; `supabase functions deploy comparativo-candidatos`; `supabase functions deploy avaliar-redacao`; `supabase functions deploy avaliar-redacao-cultural`; `supabase functions deploy avaliar-transcricao-entrevista`; `supabase functions deploy gerar-guia-entrevista`; `supabase functions deploy gerar-devolutiva-bigfive`; `supabase functions deploy consolidar-decisao-final`; `supabase functions deploy cost-alerter --no-verify-jwt`. (Alternativa: Supabase MCP `deploy_edge_function` por EF.) NÃO passar nenhuma flag que mude verify_jwt das EFs que hoje o exigem (Phase 24 é quem aperta authz).
    3. Confirmar: `supabase functions list` mostra os timestamps atualizados das 9 EFs.
    Não fazer as invocações live aqui — elas vão para a Task 2 (checkpoint humano com o seed).
  </action>
  <how-to-verify>
    `grep -rn 'import(\[' supabase/functions` == 0; `supabase functions list` mostra as 9 EFs com deploy recente; nenhuma flag verify_jwt alterada.
  </how-to-verify>
  <resume-signal>Cole a saída de `supabase functions list` (timestamps) e digite "redeployado".</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 [BLOCKING]: smoke live — call_types logam a semver real (não 0.0.0/500)</name>
  <files>(nenhum arquivo — invocação live PROD)</files>
  <read_first>
    - 23-VALIDATION.md §Manual-Only Verifications (linhas 68-72)
    - reference_candidatura_test_data_reset + project_funil_e2e_seed_achados (memory — seed live)
  </read_first>
  <what-built>
    Prova live de que a stack ressuscitou: cada call_type antes quebrado (work_sample_sjt, culture_fit_essay, transcript_analysis, interview_guide, bigfive_devolutiva) roda o prompt real e loga a semver da library, não o stub 0.0.0 nem um 500.
  </what-built>
  <action>
    Runbook (usar a candidatura seed do funil E2E em PROD):
    1. Disparar cada EF de IA contra o seed (via a UI RH/candidato ou curl autenticado às EFs — precedente memory: gotrue /token grant_type=password → Bearer → curl EF).
    2. Após cada invocação: `SELECT call_type, prompt_version, success, error_code, created_at FROM public.ai_call_logs WHERE created_at > now() - interval '10 min' ORDER BY created_at DESC;` — confirmar que cada call_type mostra a semver REAL (ex.: 1.0.0) e success=true, NÃO "0.0.0" nem uma row de 500/stub. Para bigfive_devolutiva: confirmar que a row EXISTE (o enum aceita agora).
    3. Registrar quaisquer 500 remanescentes (esperado: nenhum se as migrations do 23-05 aplicaram; um 500 persistente em bigfive_devolutiva significa que o enum/seed não aterrissou → voltar ao 23-05).
    Diferir ao 23-HUMAN-UAT.md as verificações visuais (devolutiva sem percentil, dashboard de consolidação com supressão ≥2 etapas) e qualquer E2E de tela que exija olho humano.
  </action>
  <how-to-verify>
    A query de ai_call_logs mostra semver real (não 0.0.0) + success=true para os 5 call_types antes quebrados; bigfive_devolutiva loga sem 22P02. Colar as saídas no SUMMARY + abrir 23-HUMAN-UAT.md com os itens visuais.
  </how-to-verify>
  <resume-signal>Cole a query de ai_call_logs pós-invocação e digite "vivo" (ou aponte call_types ainda em 0.0.0/500).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy → runtime PROD | um redeploy que altere verify_jwt/autz mudaria a superfície de segurança (fora de escopo — Phase 24) |
| bundle _shared → EF | uma EF não-redeployada mantém o bundle congelado (drift silencioso) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06-01 | Tampering (drift de bundle) | editar _shared sem redeployar cada EF → EF continua rodando código morto | mitigate | redeploy explícito das 9 EFs tocadas + `functions list` confirmando timestamps (Task 1) |
| T-23-06-02 | Elevation of Privilege | um redeploy alterando verify_jwt exporia autz cedo (Phase 24) | mitigate | preservar as flags exatas; grep/checklist de que nenhum verify_jwt mudou (Task 1) — NÃO tocar autz |
| T-23-06-03 | Availability | import `await import([...].join(""))` esconde o pacote → 500 ERR_MODULE_NOT_FOUND | mitigate | grep pré-deploy `import(\[` == 0; SDKs `npm:` estáticos (Task 1) |
| T-23-06-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote instalado; deploy usa os pins existentes |
</threat_model>

<verification>
- `supabase functions list`: 9 EFs com deploy recente; verify_jwt inalterado
- ai_call_logs live: semver real (não 0.0.0) + success=true nos 5 call_types antes quebrados + bigfive_devolutiva loga
- `grep -rn 'import(\[' supabase/functions` == 0
</verification>

<success_criteria>
- 9 EFs redeployadas (bundle _shared novo) sem alterar verify_jwt/autz
- smoke live prova a semver real; 23-HUMAN-UAT.md aberto com os itens visuais diferidos
</success_criteria>

<output>
Create `.planning/phases/23-ressurrei-o-da-stack-de-ia/23-06-SUMMARY.md` when done.
Também criar `.planning/phases/23-ressurrei-o-da-stack-de-ia/23-HUMAN-UAT.md` com os checks visuais live (devolutiva sem percentil, consolidação ≥2 etapas, guardrail de custo).
</output>
