---
phase: 23-ressurrei-o-da-stack-de-ia
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/avaliacao/components/DevolutivaBigFiveView.tsx
  - src/features/avaliacao/components/ScorecardAvaliacao.tsx
  - src/components/ScoreCard.tsx
  - src/components/pages/RelatoriosRHPage.tsx
  - src/components/pages/CandidatosRHPage.tsx
  - supabase/functions/consolidar-decisao-final/index.ts
  - src/features/decisao/components/ConsolidacaoDashboard.tsx
autonomous: true
requirements: [UX-07, UX-09]
must_haves:
  truths:
    - "A devolutiva Big Five do candidato mostra APENAS bandas NEUTRAS (Muito baixo…Muito alto) — nenhum 'Percentil {n}' cru, barra proporcional numérica nem analogia com dígito (UX-07)"
    - "As telas RH (ScorecardAvaliacao Big Five, ScoreCard Raven) não exibem percentil numérico cru — Big Five = banda NEUTRA, cognitivo job-fit = banda avaliativa 3-níveis provisória (UX-07)"
    - "triagem sai de WEIGHTED_KEYS da consolidação (vira contexto visível, sem peso) (UX-09)"
    - "O número consolidado só aparece com ≥2 etapas concluídas; com <2 o dashboard mostra mensagem distinta ('Agregado suprimido até ≥2 etapas'), não o empty-state genérico (UX-09)"
    - "UX-07/UX-09 traceability: o agregado é advisory — IA recomenda, humano decide (RNF-07a)"
  artifacts:
    - path: "src/features/avaliacao/components/DevolutivaBigFiveView.tsx"
      provides: "devolutiva sem percentil cru; banda neutra (BANDA_LABEL)"
    - path: "src/components/ScoreCard.tsx"
      provides: "Raven → banda avaliativa 3-níveis (sem P{n})"
    - path: "supabase/functions/consolidar-decisao-final/index.ts"
      provides: "WEIGHTED_KEYS sem triagem + gate ≥2 etapas"
      contains: "work_sample_sjt"
    - path: "src/features/decisao/components/ConsolidacaoDashboard.tsx"
      provides: "mensagem distinta p/ consolidated=null com 1 etapa present"
  key_links:
    - from: "consolidar-decisao-final WEIGHTED_KEYS"
      to: "['work_sample_sjt','redacao_cultural','entrevista']"
      via: "remoção de triagem"
      pattern: "WEIGHTED_KEYS"
    - from: "ConsolidacaoDashboard"
      to: "consolidated=null com 1 etapa"
      via: "mensagem de supressão"
      pattern: "≥2 etapas|>= 2"
---

<objective>
Honestidade psicométrica (UX-07 + UX-09): remover o percentil numérico cru da devolutiva do candidato e das telas RH, substituindo por descritores qualitativos — bandas NEUTRAS de 5 níveis no Big Five (não-avaliativo) e uma banda avaliativa de 3 níveis no cognitivo job-fit; e tirar o peso de `triagem` da consolidação da decisão final, exigindo ≥2 etapas concluídas antes de exibir um número consolidado.

Purpose: o percentil bruto engana (a norma Johnson do Big Five já está wired, mas o número ainda sugere ranking); triagem é pré-triagem de CV, não etapa de avaliação — ponderá-la distorce o agregado, e 1 etapa presente já gerava um número consolidado enganoso. Big Five NÃO é avaliativo (traço alto de Neuroticismo não é "pior") — usar bandas neutras, nunca "abaixo/dentro/acima do esperado" (Pitfall 5).
Output: 5 componentes frontend + 1 EF (consolidar) corrigidos; Vitest + Deno verdes. A EF consolidar é redeployada no Plan 23-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-ressurrei-o-da-stack-de-ia/23-RESEARCH.md

<interfaces>
DevolutivaBigFiveView.tsx: BANDA_LABEL (:48, 5 bandas neutras) já existe; analogia() (:85-91, deriva de percentil); render :168 `Percentil {row.percentil} · {BANDA_LABEL[row.banda]}`, :171 `<Progress value={row.percentil}>`, :198 `Percentil {dash.percentil}`, :200 analogia(dim,dash.percentil).
ScorecardAvaliacao.tsx: BIGFIVE_BANDA_LABEL (:231) já existe; :276 `Percentil {d.percentil}`, :278 já renderiza `{BIGFIVE_BANDA_LABEL[d.banda]}`.
ScoreCard.tsx: :105-107 `P${inteligencia}` (Raven percentile 0-100); getScoreColor (:35-41).
RelatoriosRHPage.tsx: :519-521 percentil já BUCKETADO em faixas 0-25/26-50/51-75/76-100 (histograma agregado — distribuição, não número por-candidato).
CandidatosRHPage.tsx: :331 `inteligencia = candidatura.scores_raven?.percentil` → alimenta <ScoreCard> (corrigido acima).
consolidar-decisao-final/index.ts: WEIGHTED_KEYS (:99 = ["triagem","work_sample_sjt","redacao_cultural","entrevista"]), CONTEXT_KEYS (:100 = ["big_five","cognitivo"]), normalizeWeighted triagem branch (:139-143), gate (:360 `presentRows.length > 0 && sumPresentWeight > 0`), buildRecommendation (:184-209).
ConsolidacaoDashboard.tsx: hasAnyPresent (:100), empty-state "Ainda não há scorecards" (:113-119), ConsolidacaoBody (:127+).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: devolutiva do candidato sem percentil cru — bandas NEUTRAS (UX-07)</name>
  <files>src/features/avaliacao/components/DevolutivaBigFiveView.tsx</files>
  <read_first>
    - src/features/avaliacao/components/DevolutivaBigFiveView.tsx:47-54 (BANDA_LABEL neutras), :85-91 (analogia com dígito), :160-208 (render :168/171/198/200)
    - 23-RESEARCH.md §UX-07 tabela (362-379) + Pitfall 5 (351-354 — Big Five é NÃO-avaliativo → bandas neutras)
  </read_first>
  <action>
    Remover `Percentil {row.percentil}` (:168) — exibir só `{BANDA_LABEL[row.banda]}`. Substituir a `<Progress value={row.percentil}>` (:171) por um indicador NÃO-quantitativo: ou remover a barra, ou trocar por um indicador de 5 segmentos keyed em row.banda (posição da banda, sem value numérico de percentil). Remover `Percentil {dash.percentil}` (:198). Reformular analogia() (:85-91) para NÃO emitir dígito: remover a analogia OU reescrevê-la keyed na banda (frase qualitativa, sem `acima`/`abaixo` numéricos). Manter as 5 bandas NEUTRAS (muito_baixo…muito_alto) — NÃO introduzir "abaixo/dentro/acima do esperado" aqui (Big Five é não-avaliativo). Preservar o disclaimer LGPD/CRP existente (:63-68) intacto.
    Adicionar/estender src/features/avaliacao/components/__tests__/DevolutivaBigFiveView.test.tsx (Vitest RTL) — render com um payload de devolutiva mockado; assere `screen.queryByText(/Percentil\s*\d/)` === null e que uma label de banda ("Muito alto"/"Médio"/…) está presente.
  </action>
  <acceptance_criteria>
    - `grep -c "Percentil {" src/features/avaliacao/components/DevolutivaBigFiveView.tsx` == 0
    - `grep -c "value={row.percentil}\|value={dash.percentil}" src/features/avaliacao/components/DevolutivaBigFiveView.tsx` == 0
    - `npm run test:run` VERDE p/ DevolutivaBigFiveView: `queryByText(/Percentil \d/)` null + banda presente
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/features/avaliacao/components/__tests__/DevolutivaBigFiveView.test.tsx</automated>
  </verify>
  <done>devolutiva candidato = banda neutra, zero percentil cru, zero analogia numérica.</done>
</task>

<task type="auto">
  <name>Task 2: telas RH sem percentil cru — Big Five banda neutra + Raven banda avaliativa (UX-07)</name>
  <files>src/features/avaliacao/components/ScorecardAvaliacao.tsx, src/components/ScoreCard.tsx, src/components/pages/RelatoriosRHPage.tsx, src/components/pages/CandidatosRHPage.tsx</files>
  <read_first>
    - src/features/avaliacao/components/ScorecardAvaliacao.tsx:231 (BIGFIVE_BANDA_LABEL), :271-278 (percentil :276 + banda :278)
    - src/components/ScoreCard.tsx:34-41 (getScoreColor), :94-110 (P{inteligencia})
    - src/components/pages/CandidatosRHPage.tsx:325-335, src/components/pages/RelatoriosRHPage.tsx:485-525 (faixas agregadas)
    - 23-RESEARCH.md §UX-07 (362-379, recomendação de 2 famílias de banda) + Open Q4 (519-520)
  </read_first>
  <action>
    ScorecardAvaliacao :276 — remover `Percentil {d.percentil}`; manter `{BIGFIVE_BANDA_LABEL[d.banda]}` (:278 já renderiza) — as rows Big Five do RH usam banda NEUTRA, sem número.
    ScoreCard.tsx :105-107 — substituir `P${inteligencia}` por uma banda AVALIATIVA de 3 níveis para o cognitivo job-fit via um helper de cutoff (ex.: <40 "Abaixo do esperado" / 40-69 "Dentro do esperado" / ≥70 "Acima do esperado" — provisório, norma real diferida ao M5); N/A quando null; manter getScoreColor. (O cognitivo é score de job-fit → a moldura avaliativa cabe aqui, ao contrário do Big Five.)
    RelatoriosRHPage (:485-525): o percentil já é bucketado em faixas p/ um histograma AGREGADO de distribuição (não um número por-candidato). Confirmar que NENHUM número de percentil cru por-candidato é exibido; o histograma de faixas (ranges) pode permanecer como distribuição agregada — documentar a decisão no SUMMARY. Se algum número cru por-candidato aparecer, bandá-lo.
    CandidatosRHPage: alimenta <ScoreCard> (corrigido) — nenhuma exibição crua adicional; confirmar.
    Adicionar Vitest: ScorecardAvaliacao (`queryByText(/Percentil \d/)` null + banda presente) e ScoreCard (`queryByText(/^P\d/)` null + label de banda presente).
  </action>
  <acceptance_criteria>
    - `grep -c "Percentil {d.percentil}" src/features/avaliacao/components/ScorecardAvaliacao.tsx` == 0
    - `grep -c "P\${inteligencia}\|\`P\${" src/components/ScoreCard.tsx` == 0
    - `npm run test:run` VERDE p/ ScorecardAvaliacao + ScoreCard (sem dígito de percentil; bandas presentes)
    - SUMMARY documenta a decisão sobre RelatoriosRHPage (histograma agregado permanece / ou bandado)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/features/avaliacao/components src/components/ScoreCard</automated>
  </verify>
  <done>RH não vê percentil cru; Big Five banda neutra, Raven banda avaliativa provisória.</done>
</task>

<task type="auto">
  <name>Task 3: triagem fora da consolidação + ≥2 etapas (UX-09)</name>
  <files>supabase/functions/consolidar-decisao-final/index.ts, src/features/decisao/components/ConsolidacaoDashboard.tsx</files>
  <read_first>
    - supabase/functions/consolidar-decisao-final/index.ts:98-108 (WEIGHTED_KEYS/CONTEXT_KEYS/TIPO_BY_KEY), :133-157 (normalizeWeighted triagem branch), :184-209 (buildRecommendation), :319-368 (montagem + gate :360)
    - src/features/decisao/components/ConsolidacaoDashboard.tsx:98-145 (hasAnyPresent, empty-state, ConsolidacaoBody)
    - 23-RESEARCH.md §UX-09 (381-389)
  </read_first>
  <action>
    Remover "triagem" de WEIGHTED_KEYS (:99) → `["work_sample_sjt", "redacao_cultural", "entrevista"]`. Preservar a VISIBILIDADE do score de CV: adicionar triagem como uma row de CONTEXTO (status 'context', sem peso) usando analise_candidato_vaga.score_match — o RH continua vendo o score de CV marcado "Contextual · não pondera", sem distorcer o agregado. Ajustar normalizeWeighted/TIPO_BY_KEY para remover o ramo triagem do caminho ponderado (a leitura de analiseTriagem passa a alimentar só a row de contexto). `pesos['triagem']` na config jsonb passa a ser ignorado com segurança (o rebalanceamento da soma de pesos é config de vaga — Phase 25).
    Mudar o gate de consolidação (:360) de `presentRows.length > 0 && sumPresentWeight > 0` para `presentRows.length >= 2 && sumPresentWeight > 0` → com <2 etapas present, consolidated=null (server-authoritative). Ajustar buildRecommendation (:192) p/ a cláusula <2 ("Agregado suprimido até ≥2 etapas concluídas.").
    Em ConsolidacaoDashboard: quando hasAnyPresent é true MAS data.consolidated é null (1 etapa present), renderizar uma mensagem DISTINTA ("Agregado suprimido até ≥2 etapas concluídas" + as rows de breakdown por etapa continuam visíveis) — NÃO o empty-state genérico "Ainda não há scorecards" (:113-119). As rows individuais de score seguem legítimas; só o AGREGADO é gated.
    Estender a suíte Deno de consolidar (novo/existente): 1 etapa present → consolidated===null; 2 etapas present → número; triagem-only present → consolidated null (triagem não pondera). Estender/adicionar Vitest do ConsolidacaoDashboard p/ a mensagem de supressão com 1 etapa present.
  </action>
  <acceptance_criteria>
    - `grep -c '"triagem"' supabase/functions/consolidar-decisao-final/index.ts` — triagem NÃO aparece mais dentro de WEIGHTED_KEYS (verificar a linha :99)
    - `grep -c ">= 2" supabase/functions/consolidar-decisao-final/index.ts` >= 1 (gate ≥2)
    - `deno test … consolidar` VERDE: 1 etapa → null; 2 → número; triagem não pondera
    - `npm run test:run` VERDE p/ ConsolidacaoDashboard: mensagem de supressão com 1 etapa present
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/consolidar-decisao-final && npm run test:run -- src/features/decisao</automated>
  </verify>
  <done>triagem = contexto sem peso; agregado exige ≥2 etapas; dashboard mostra supressão distinta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF consolidar → client dashboard | o agregado é advisory (server-authoritative gate); o client só reflete consolidated=null |
| dados psicométricos → candidato/RH | percentil bruto é informação sensível que engana (LGPD-adjacente / honestidade) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-01 | Information Disclosure (honestidade psicométrica) | percentil cru na devolutiva/telas RH sugere ranking/avaliação num self-assessment não-avaliativo | mitigate | bandas qualitativas — 5 neutras (Big Five) / 3 avaliativas (cognitivo job-fit); zero dígito de percentil (Task 1/2) |
| T-23-04-02 | Tampering (integridade da decisão) | número consolidado com 1 etapa (ou triagem ponderada) engana o RH a decidir cedo | mitigate | triagem fora de WEIGHTED_KEYS + gate ≥2 etapas server-authoritative (Task 3); agregado permanece advisory (RNF-07a) |
| T-23-04-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote instalado |
</threat_model>

<verification>
- Vitest verde: `npm run test:run`; Deno verde p/ consolidar; `npm run lint` no baseline pinado (133)
- RNF-07a preservado: o consolidado é advisory; nenhuma auto-rejeição por score
</verification>

<success_criteria>
- DevolutivaBigFiveView + ScorecardAvaliacao + ScoreCard sem percentil cru (bandas corretas por família)
- consolidar-decisao-final: triagem fora dos WEIGHTED_KEYS (vira contexto) + gate ≥2 etapas; ConsolidacaoDashboard com mensagem de supressão distinta
- Vitest + Deno verdes
</success_criteria>

<output>
Create `.planning/phases/23-ressurrei-o-da-stack-de-ia/23-04-SUMMARY.md` when done
</output>
