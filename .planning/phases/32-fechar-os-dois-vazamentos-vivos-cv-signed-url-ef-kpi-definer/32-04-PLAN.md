---
phase: 32-fechar-os-dois-vazamentos-vivos-cv-signed-url-ef-kpi-definer
plan: 04
type: execute
wave: 3
depends_on: ["32-02", "32-03"]
files_modified:
  - database.types.ts
autonomous: false
requirements: [SEG-01, SEG-02]

must_haves:
  truths:
    - "SEG-01: the EF get-curriculo-url is deployed JWT-ON and the curriculos RH read branch is dropped IN THAT ORDER (deploy EF → drop policy) — a live curl proves no-auth→401, candidato→403, administrador→200 with a signed URL"
    - "SEG-02: funil_kpis + the rh_le_historico WR-04 hardening are live on PROD; seg32_smokes.sql assertions (a)-(e) all NOTICE PASS via MCP execute_sql — recruiter A cannot read B's CV / KPIs / historico, funil_kpis returns zero PII, admin sees all + p_vaga_id narrows"
    - "database.types.ts (repo ROOT) is regenerated with funil_kpis; supabase_migrations.schema_migrations version rows are reconciled to the two filenames; npm run lint + build stay green"
  artifacts:
    - path: "database.types.ts"
      provides: "regenerated types including public.funil_kpis (repo ROOT, never hand-edited)"
      contains: "funil_kpis"
  key_links:
    - from: "supabase functions deploy get-curriculo-url"
      to: "PROD (JWT-ON)"
      via: "must be live BEFORE the curriculos policy drop"
      pattern: "get-curriculo-url"
    - from: "MCP apply_migration"
      to: "supabase_migrations.schema_migrations"
      via: "timestamp version row ≠ filename → reconcile (P31/P11 precedent)"
      pattern: "schema_migrations"
---

<objective>
[BLOCKING] Take Phase 32 live and prove it with the load-bearing behavioral smoke. Deploy the `get-curriculo-url` Edge Function, apply the two migrations via Supabase MCP `apply_migration` (NOT `db push` — the pooler trips SQLSTATE 42601 on `$$` bodies + adjacent REVOKE/GRANT), regenerate `database.types.ts`, reconcile the migration ledger, then run `seg32_smokes.sql` via MCP `execute_sql` so every assertion `NOTICE PASS`.

This plan is `autonomous: false` — it writes to PROD (EF deploy + MCP migrations) and requires the operator's authorization for the privileged MCP `apply_migration`/`execute_sql` calls (auth gates surface at execution time). The behavioral JWT-impersonated smoke is authoritative above any structural check (P24: smokes caught a REVOKE no-op + an OR-defeat that `pg_policies` passed).

Purpose: Phase 32 is the BLOCKING gate for Phase 34 — the RH screens there must not render against an un-proven primitive.
Output: EF live (JWT-ON); 2 migrations live; `database.types.ts` regenerated; smokes GREEN; version ledger reconciled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-fechar-os-dois-vazamentos-vivos-cv-signed-url-ef-kpi-definer/32-PATTERNS.md
@.planning/phases/32-fechar-os-dois-vazamentos-vivos-cv-signed-url-ef-kpi-definer/32-VALIDATION.md

# Artifacts to apply/deploy/run (authored in 32-01/32-02/32-03):
@supabase/functions/get-curriculo-url/index.ts
@supabase/migrations/20260715000001_curriculos_drop_rh_read.sql
@supabase/migrations/20260715000002_funil_kpis_and_rh_le_historico.sql
@supabase/tests/seg32_smokes.sql

<interfaces>
<!-- The exact live-ops order + tools. -->

Ordered live sequence (Pitfall 1 — dropping the policy before the EF is live leaves RH with no CV path):
  1. supabase functions deploy get-curriculo-url          (JWT-ON = verify_jwt default; do NOT pass --no-verify-jwt)
  2. MCP apply_migration  20260715000002_funil_kpis_and_rh_le_historico   (RPC + policy — order vs (3) is free)
  3. MCP apply_migration  20260715000001_curriculos_drop_rh_read          (drop RH Storage branch — AFTER step 1)
  4. reconcile supabase_migrations.schema_migrations (MCP apply_migration writes a timestamp version ≠ filename — P31/P11)
  5. npm run db:types  → regenerate database.types.ts (repo ROOT)
  6. MCP execute_sql  supabase/tests/seg32_smokes.sql  → all (a)-(e) NOTICE PASS
  7. live curl (gotrue /token grant_type=password from .env.test): no-auth→401, candidato→403, administrador→200

Test accounts (.env.test / MEMORY): e2e.admin@beautysmile.com.br (administrador), candidato.funil@teste.com (candidato).
NOTE (Pitfall 6): there is NO role='recrutador' PROD account → the cross-recruiter 403 is proven by the deno unit test (32-02) + seg32_smokes.sql (a), NOT a live recruiter curl. Do NOT block the phase on a recruiter-vs-recruiter live curl.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ordered live apply — deploy EF, apply both migrations, reconcile ledger, regen types</name>
  <files>supabase/functions/get-curriculo-url/index.ts, supabase/migrations/20260715000001_curriculos_drop_rh_read.sql, supabase/migrations/20260715000002_funil_kpis_and_rh_le_historico.sql, database.types.ts, supabase_migrations.schema_migrations (via MCP execute_sql)</files>
  <read_first>
    - 32-PATTERNS.md § Sequencing Landmine — the exact 4-step order + the schema_migrations reconcile note
    - CLAUDE.md § Migrations + db push workaround — MCP apply_migration bypasses 42601; MEMORY: apply_migration writes a version row, reconcile to filename
    - supabase/functions/get-curriculo-url/index.ts + the two migration files (from 32-02/32-03)
  </read_first>
  <action>
    Execute the live sequence in this exact order (the order is correctness-load-bearing — Pitfall 1). (1) Deploy the EF: `supabase functions deploy get-curriculo-url` — JWT-ON (verify_jwt default; do NOT pass `--no-verify-jwt`). Confirm it appears in `supabase functions list`. (2) Apply migration B via Supabase MCP `apply_migration` with the SQL from `supabase/migrations/20260715000002_funil_kpis_and_rh_le_historico.sql` (funil_kpis RPC + rh_le_historico WR-04). (3) Apply migration A via MCP `apply_migration` with the SQL from `supabase/migrations/20260715000001_curriculos_drop_rh_read.sql` — this MUST run AFTER the EF deploy in step 1 (dropping the Storage RH branch before the EF is live leaves RH with no CV path). (4) Reconcile `supabase_migrations.schema_migrations`: MCP `apply_migration` writes a timestamp version row that ≠ the filename version, so via MCP `execute_sql` upsert/align the two version rows to `20260715000001` and `20260715000002` (P31/P11 precedent) so `supabase db push --linked` reports "up to date". (5) Regenerate types: `npm run db:types` → `database.types.ts` at the repo ROOT (never hand-edit); confirm it now contains a `funil_kpis` entry. The privileged MCP calls require operator authorization — request it at execution time (auth gate).
  </action>
  <verify>
    <automated>npm run lint && npm run build && grep -q "funil_kpis" database.types.ts && echo "types regenerated with funil_kpis; tsc + build green"</automated>
  </verify>
  <acceptance_criteria>
    - `get-curriculo-url` is deployed JWT-ON and listed by `supabase functions list`.
    - Both migrations are live on PROD (MCP apply_migration), applied in the order: EF deploy → migration A drop.
    - `supabase_migrations.schema_migrations` contains rows `20260715000001` and `20260715000002`; `supabase db push --linked` reports the remote is up to date (no drift for these two).
    - `database.types.ts` (repo ROOT) is regenerated and contains `funil_kpis`; `npm run lint` (tsc) + `npm run build` are green.
  </acceptance_criteria>
  <done>EF live JWT-ON; both migrations applied via MCP in the ordered sequence; ledger reconciled; database.types.ts regenerated with funil_kpis; tsc + build green.</done>
</task>

<task type="auto">
  <name>Task 2: Run seg32_smokes.sql GREEN + deno test + live curl</name>
  <files>supabase/tests/seg32_smokes.sql (run via MCP execute_sql), supabase/functions/get-curriculo-url/index.test.ts (deno test)</files>
  <read_first>
    - supabase/tests/seg32_smokes.sql (from 32-01) — the 5 assertions (a)-(e); NOTICE PASS = pass, EXCEPTION = a real leak
    - 32-VALIDATION.md § Manual-Only Verifications — the live-curl matrix + the recruiter-account caveat (Pitfall 6)
    - MEMORY / .env.test — gotrue `/token?grant_type=password` to mint a Bearer, then curl the EF
  </read_first>
  <action>
    (1) Run `supabase/tests/seg32_smokes.sql` via Supabase MCP `execute_sql` (privileged — request operator authorization). Read the NOTICE stream: every assertion (a)-(e) must emit `PASS`; ANY `RAISE EXCEPTION` (or an unexpected `SKIP` because the fixture could not build) is a FAIL — stop and fix the underlying migration/EF before proceeding (do NOT edit the smoke to pass). (2) Re-run the EF deno unit test `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/get-curriculo-url` — this is the authoritative cross-recruiter 403 gate (no recruiter PROD account for a live curl — Pitfall 6). (3) Live curl against the deployed EF: mint Bearers via gotrue `/token?grant_type=password` for `e2e.admin@beautysmile.com.br` and `candidato.funil@teste.com` from `.env.test`, then invoke `get-curriculo-url` with `{ candidatura_id }`: no Authorization header → 401; `candidato` Bearer → 403; `administrador` Bearer with a real candidatura → 200 with a `signedUrl`. Do NOT block on a recruiter-vs-recruiter live curl (no such account) — the deno test + smoke (a) are the cross-recruiter deny gates. Record the smoke NOTICE output + the three curl status codes in the SUMMARY.
  </action>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/get-curriculo-url && echo "deno EF gate green (SQL smokes + live curl evidence captured in SUMMARY)"</automated>
  </verify>
  <acceptance_criteria>
    - seg32_smokes.sql: all of (a)-(e) emit `NOTICE PASS` via MCP execute_sql; no `RAISE EXCEPTION`, no unexpected `SKIP`.
    - deno EF unit test green (401 / 403-role / 403-owner / 404-NULL / 200).
    - Live curl: no-auth→401, `candidato`→403, `administrador`→200 signed URL — status codes recorded in the SUMMARY.
    - The smoke was NOT edited to make it pass; any failure was fixed at the migration/EF source.
  </acceptance_criteria>
  <done>All 5 behavioral smokes NOTICE PASS; deno test green; live curl 401/403/200 captured in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Human-verify PROD apply + behavioral smokes + no live regression</name>
  <files>none (verification checkpoint)</files>
  <action>Pause for the operator to confirm the live evidence recorded by Tasks 1-2 before Phase 32 is closed — the seg32_smokes.sql PASS stream, the live curl status matrix, the reconciled migration ledger, and a green tsc/build. This is the leak-closure + supply-chain/legitimacy gate; it is never auto-approved.</action>
  <what-built>
    The two live leaks are closed on PROD: (SEG-01) the `get-curriculo-url` EF is deployed JWT-ON, the `curriculos` RH role-only Storage read branch is dropped, and `cvUploadService` invokes the EF; (SEG-02) `funil_kpis` DEFINER RPC + the `rh_le_historico` WR-04 hardening are live. `database.types.ts` regenerated; migration ledger reconciled. seg32_smokes.sql (a)-(e) all PASS; deno test green; live curl 401/403/200.
  </what-built>
  <how-to-verify>
    1. Confirm the seg32_smokes.sql NOTICE output in the SUMMARY shows PASS for all five assertions (a) CV cross-recruiter deny, (b) KPI cross-recruiter deny, (c) historico RLS deny, (d) zero-PII jsonb, (e) admin-sees-all + p_vaga_id narrows — with NO EXCEPTION/SKIP.
    2. Confirm the live curl matrix: no-auth→401, candidato→403, administrador→200 with a signedUrl.
    3. Confirm `supabase db push --linked` reports the remote is up to date (ledger reconciled) and `npm run lint` + `npm run build` are green.
    4. Sanity: no `role='recrutador'` account exists, so the cross-recruiter 403 was proven by the deno test + smoke (a) — confirm you accept that as the authoritative gate (research Pitfall 6).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 32 (BLOCKING gate for Phase 34 lifted), or describe the failing assertion / status code to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Supabase MCP (apply_migration / execute_sql) | privileged PROD writes; require explicit authorization |
| RH browser → live EF `get-curriculo-url` | JWT-ON; only authenticated callers reach the handler |
| Live Storage/RLS surfaces | the policy drop changes a live authorization surface — ordering matters |

## STRIDE Threat Register (the live proof of the phase register)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure / Elevation | recruiter A reads vaga-B CV | mitigate | proven live by deno test + smoke (a); EF ownership guard live |
| T-32-02 | Elevation of Privilege | candidato/anon reaches EF | mitigate | live curl: no-auth→401, candidato→403 |
| T-32-04 | Information Disclosure | funil_kpis PII leak | mitigate | smoke (d) walks the live jsonb — zero PII keys |
| T-32-05 | Information Disclosure | recruiter A sees vaga-B KPIs | mitigate | smoke (b) live deny |
| T-32-06 | Information Disclosure | recruiter A SELECTs vaga-B historico | mitigate | smoke (c) live RLS deny |
| T-32-10 | Denial of Service / Availability | policy drop before EF live | mitigate | Task 1 enforces deploy-EF-first ordering |
| T-32-SC | Tampering (supply-chain) | EF deploy / MCP calls | mitigate | zero new packages; EF uses only auto-injected env; MCP calls operator-authorized; legitimacy is the blocking human-verify gate (Task 3, never auto-approved) |
</threat_model>

<verification>
- seg32_smokes.sql (a)-(e) all NOTICE PASS via MCP execute_sql — the load-bearing gate.
- deno EF test green; live curl 401/403/200 captured.
- `npm run lint` + `npm run build` green after `npm run db:types`; ledger reconciled (`db push` up to date).
- Blocking human-verify checkpoint approved.
</verification>

<success_criteria>
Phase 32 is live and proven:
- EF deployed JWT-ON; curriculos RH read branch dropped (in that order); client invokes the EF.
- funil_kpis + rh_le_historico WR-04 live; all 5 behavioral smokes PASS; zero PII; cross-recruiter/candidate/historico denies hold.
- database.types.ts regenerated; migration ledger reconciled; tsc + build green.
The BLOCKING gate for Phase 34 is lifted — the RH read-primitives are comprovadamente seguros.
</success_criteria>

<output>
Create `.planning/phases/32-fechar-os-dois-vazamentos-vivos-cv-signed-url-ef-kpi-definer/32-04-SUMMARY.md` when done
</output>
