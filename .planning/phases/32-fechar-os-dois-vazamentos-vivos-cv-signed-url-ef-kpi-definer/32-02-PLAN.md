---
phase: 32-fechar-os-dois-vazamentos-vivos-cv-signed-url-ef-kpi-definer
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - supabase/functions/get-curriculo-url/index.ts
  - supabase/migrations/20260715000001_curriculos_drop_rh_read.sql
  - src/features/vagas/services/cvUploadService.ts
autonomous: true
requirements: [SEG-01]

must_haves:
  truths:
    - "SEG-01: the EF get-curriculo-url authenticates the JWT (anon client getUser) THEN authorizes role (from usuarios_rh, recrutador→rh) + vaga ownership (vagas.created_by, admin bypasses) before minting a 60s signed URL — the deno test passes all of 401 / 403-role / 403-owner / 404-NULL / 200"
    - "SEG-01: cvUploadService.getSignedUrl(candidatura_id) invokes the EF via supabase.functions.invoke('get-curriculo-url') — no createSignedUrl over curriculos remains anywhere in src/ (extended guard GREEN)"
    - "SEG-01: Migration A recreates curriculos_select_own_or_rh with the candidate own-folder branch ONLY (the RH role-only OR branch removed); the three curriculos upload policies are untouched (authored, applied in 32-04)"
  artifacts:
    - path: "supabase/functions/get-curriculo-url/index.ts"
      provides: "authorize-THEN-authenticate two-client EF; input candidatura_id only (never a raw path); createSignedUrl(path, 60)"
      exports: ["handler"]
      min_lines: 90
    - path: "supabase/migrations/20260715000001_curriculos_drop_rh_read.sql"
      provides: "DROP+CREATE curriculos_select_own_or_rh with candidate own-folder branch only; no BEGIN/COMMIT wrapper"
      contains: "storage.foldername"
    - path: "src/features/vagas/services/cvUploadService.ts"
      provides: "getSignedUrl(candidaturaId) → functions.invoke('get-curriculo-url'); no client-side createSignedUrl"
      contains: "get-curriculo-url"
  key_links:
    - from: "src/features/vagas/services/cvUploadService.ts"
      to: "get-curriculo-url EF"
      via: "supabase.functions.invoke('get-curriculo-url', { body: { candidatura_id } })"
      pattern: "functions\\.invoke\\('get-curriculo-url'"
    - from: "supabase/functions/get-curriculo-url/index.ts"
      to: "curriculos bucket"
      via: "supabaseAdmin.storage.from('curriculos').createSignedUrl(path, 60)"
      pattern: "createSignedUrl\\([^,]+,\\s*60"
    - from: "supabase/functions/get-curriculo-url/index.ts"
      to: "usuarios_rh + vagas.created_by"
      via: "role read from usuarios_rh (not app_metadata), ownership check on vagas.created_by"
      pattern: "usuarios_rh"
---

<objective>
Close leak 1 (SEG-01 — CV). Build the `get-curriculo-url` Edge Function as the single privileged RH path to a candidate CV, rewire the client service to invoke it, and author the migration that removes the role-only RH read branch from the `curriculos` Storage policy. This greens the deno EF test, the extended bundle guard, and the cvUploadService `getSignedUrl` block authored in 32-01.

Every security-load-bearing line is a verbatim copy of the shipped, PROD-live `comparativo-candidatos` two-client authorize-THEN-authenticate skeleton (D-23). The failure mode this phase must avoid (P10/P11 landmine) is an authenticate-ONLY EF: calling `getUser()` then reading via service_role WITHOUT the role + ownership check → any authenticated candidate reads any CV. authenticate ≠ authorize.

Purpose: the EF + policy drop are the SEG-01 primitive that Phase 34's RH screens consume.
Output: 1 new EF (`index.ts`), 1 new migration (authored, applied in 32-04), 1 modified service. The migration and the EF go LIVE in 32-04 in a strict order (see landmine).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-fechar-os-dois-vazamentos-vivos-cv-signed-url-ef-kpi-definer/32-PATTERNS.md

# Analogs to clone (exact line ranges in 32-PATTERNS.md):
@supabase/functions/comparativo-candidatos/index.ts
@supabase/migrations/20260425000002_curriculos_bucket.sql
@src/features/admin/services/usuariosRhService.ts
@src/features/vagas/services/cvUploadService.ts

<interfaces>
<!-- What the EF reads (allowlist projections, never select('*')) and what the client sends. -->

EF get-curriculo-url — envelope (mirror comparativo-candidatos verbatim):
  Request  body: { candidatura_id: string }              // zod .strict() recommended; NEVER a raw path
  Response 200:  { ok: true, signedUrl: string }
  Response err:  { ok: false, error_code, message }
    ErrorCode = "UNAUTHORIZED"(401) | "FORBIDDEN"(403) | "VALIDATION"(400) | "NOT_FOUND"(404) | "SERVER_ERROR"(500)
  handler(req, { supabaseAdmin, supabaseUser }): Promise<Response>   // export for the deno test

Server-side reads inside the EF (via supabaseAdmin, allowlist projections):
  usuarios_rh(role) WHERE user_id=user.id AND ativo=true AND deleted_at IS NULL  → map recrutador→rh, administrador→administrador
  candidaturas(curriculo_url, vaga_id) WHERE id = candidatura_id                 → NULL curriculo_url ⇒ 404
  vagas(created_by) WHERE id = cand.vaga_id                                       → role='rh' must match user.id; administrador bypasses
  storage.from('curriculos').createSignedUrl(cand.curriculo_url, 60)             → { ok:true, signedUrl }

Client rewire (cvUploadService.ts) — CVUploadServiceError already carries 'UPLOAD_FAILED' + details:
  getSignedUrl(candidaturaId: string): Promise<string>   // was getSignedUrl(path)
  supabase.functions.invoke('get-curriculo-url', { body: { candidatura_id: candidaturaId } })
  Optional: normalize the EF error via extractEfErrorCode from @/lib/efErrors into .details (usuariosRhService precedent)

Env the EF needs (auto-injected by Supabase — NO Vault secret):
  SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Edge Function get-curriculo-url (authorize-THEN-authenticate)</name>
  <files>supabase/functions/get-curriculo-url/index.ts</files>
  <behavior>
    - No/invalid Authorization → 401 UNAUTHORIZED (anon client getUser fails)
    - Authed but role ∉ {rh, administrador} (candidato) → 403 FORBIDDEN
    - candidatura missing OR curriculo_url NULL → 404 NOT_FOUND
    - role='rh' not owning cand.vaga_id → 403 FORBIDDEN; administrador bypasses ownership
    - owner rh / administrador → 200 { ok:true, signedUrl } with a 60s TTL
  </behavior>
  <read_first>
    - supabase/functions/comparativo-candidatos/index.ts — clone char-for-char on the security lines: imports+CORS+helpers (:36,60-77), injectable Deps + handler entry (:83-114), AUTHENTICATE (:118-123), AUTHORIZE role from usuarios_rh map recrutador→rh (:131-151 incl. the "why not app_metadata" comment), AUTHORIZE ownership vagas.created_by (:182-194), Deno.serve two-client wiring (:310-338)
    - 32-RESEARCH.md § Code Examples #1 — the full copyable handler (parse candidatura_id, resolve path, 404-on-NULL, mint 60s URL, Pitfall-7 redaction)
    - 32-PATTERNS.md § get-curriculo-url/index.ts — the phase-specific body + "ErrorCode swaps MIXED_VAGA for NOT_FOUND"
    - supabase/functions/get-curriculo-url/index.test.ts (from 32-01) — the 5 branches this must satisfy
  </read_first>
  <action>
    Create supabase/functions/get-curriculo-url/index.ts cloning comparativo-candidatos/index.ts. Use a STATIC import `import { createClient } from "https://esm.sh/@supabase/supabase-js@2"` — NEVER the `.join("npm:")` dynamic form (that hid the package from the deploy bundler in P10-13). Copy the CORS headers, `jsonResponse`/`errorResponse`, and the `ErrorCode` union (swap comparativo's `"MIXED_VAGA"` for `"NOT_FOUND"`). Export `interface Deps { supabaseAdmin; supabaseUser }` and `async function handler(req, deps)`. In handler: OPTIONS→200 CORS short-circuit; non-POST→405. Step 1 AUTHENTICATE: `supabaseUser.auth.getUser()`; null/err → 401 UNAUTHORIZED. Step 2 AUTHORIZE role: read `usuarios_rh(role)` via `supabaseAdmin` with `.eq('user_id',user.id).eq('ativo',true).is('deleted_at',null).maybeSingle()`, map `recrutador→rh` / `administrador→administrador`; role ∉ {rh, administrador} → 403 FORBIDDEN. Do NOT read role from `getUser().app_metadata` (the hook injects role only into signed JWT claims; DB raw_app_meta_data is null). Step 3 parse body: accept `candidatura_id: string` ONLY (recommend zod `.strict()`); reject a raw `path` — never trust a client path. Step 4 resolve server-side with an allowlist projection `candidaturas.select('curriculo_url, vaga_id').eq('id', candidatura_id).maybeSingle()` (never select('*')); missing row or NULL `curriculo_url` → 404 NOT_FOUND. Step 5 AUTHORIZE ownership: if role='rh', read `vagas.select('created_by').eq('id', cand.vaga_id).maybeSingle()` and require `created_by === user.id` else 403; administrador skips this. Step 6 mint `supabaseAdmin.storage.from('curriculos').createSignedUrl(cand.curriculo_url, 60)` → `{ ok:true, signedUrl }` 200. NEVER `console.*` the signedUrl (Pitfall 7 redaction) — log only `{ candidatura_id, role }`. Add the `if (import.meta.main) Deno.serve(...)` production wiring that builds `supabaseUser` (anon + Authorization header) and `supabaseAdmin` (service_role, autoRefresh off) from `Deno.env`, with the OPTIONS preflight BEFORE the auth-header guard and a missing-env → 500. Do NOT deploy — deploy is 32-04.
  </action>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/get-curriculo-url</automated>
  </verify>
  <acceptance_criteria>
    - deno test passes all 5 branches from 32-01 (401 / 403-role / 404-NULL / 403-owner / 200).
    - Role is read from `usuarios_rh` via service_role (grep: file contains `usuarios_rh`, does NOT read role from `app_metadata`); `recrutador→rh` map present.
    - Body accepts `candidatura_id` only; there is NO code path that signs a client-supplied `path`.
    - `createSignedUrl(..., 60)` present; the signed URL is never passed to `console.*`.
    - Import is the static `https://esm.sh/@supabase/supabase-js@2` form (no `.join("npm:")`).
    - handler is exported and the server is guarded by `import.meta.main`.
  </acceptance_criteria>
  <done>get-curriculo-url/index.ts authored (authorize-THEN-authenticate, candidatura_id-only, 60s signed URL, static npm import); deno test green on all 5 branches.</done>
</task>

<task type="auto">
  <name>Task 2: Migration A — drop the RH read branch of curriculos_select_own_or_rh (authored, not applied)</name>
  <files>supabase/migrations/20260715000001_curriculos_drop_rh_read.sql</files>
  <read_first>
    - supabase/migrations/20260425000002_curriculos_bucket.sql:54-102 — current policy (RH branch L64-67 to remove; candidate own-folder branch + the 3 upload policies to keep)
    - 32-PATTERNS.md § …_curriculos_drop_rh_read.sql — DROP whole policy, re-CREATE candidate-branch-only (no per-branch DROP)
    - CLAUDE.md § Migrations workaround — NO outer BEGIN;…COMMIT; wrapper (D-22)
  </read_first>
  <action>
    Create supabase/migrations/20260715000001_curriculos_drop_rh_read.sql. `DROP POLICY IF EXISTS "curriculos_select_own_or_rh" ON storage.objects;` then `CREATE POLICY "curriculos_select_own_or_rh" ON storage.objects FOR SELECT TO authenticated USING (bucket_id = 'curriculos' AND (storage.foldername(name))[1] = (select auth.uid()::text));` — the candidate own-folder branch ONLY. The RH/admin role-only OR clause (`(select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador')`) is REMOVED; the EF (service_role) is the only RH CV path after this. Do NOT touch `curriculos_insert_own` / `curriculos_update_own` / `curriculos_delete_own`. Keep any explanatory note as a full-line `--` SQL comment (so a comment cannot re-introduce the role-only token in the executable body). NO `BEGIN;…COMMIT;` wrapper (the MCP/CLI wraps each migration itself — D-22). Keep the policy name as-is (a rename to `curriculos_select_own` is cosmetic — either is fine; the behavioral smoke is the gate). This file is authored now and APPLIED in 32-04 (AFTER the EF is deployed — see landmine).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260715000001_curriculos_drop_rh_read.sql && grep -q "DROP POLICY IF EXISTS \"curriculos_select_own_or_rh\"" supabase/migrations/20260715000001_curriculos_drop_rh_read.sql && grep -q "storage.foldername" supabase/migrations/20260715000001_curriculos_drop_rh_read.sql && grep -v '^--' supabase/migrations/20260715000001_curriculos_drop_rh_read.sql | grep -c "app_metadata" | (read n; [ "$n" -eq 0 ] && echo "role-only branch removed from executable body")</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists; DROPs then re-CREATEs `curriculos_select_own_or_rh` with ONLY the `storage.foldername(name)[1] = (select auth.uid()::text)` branch.
    - The executable body (comments filtered) contains NO `app_metadata`/role-only OR clause.
    - `curriculos_insert_own` / `update_own` / `delete_own` are NOT referenced (untouched).
    - No `BEGIN;`/`COMMIT;` wrapper.
    - File is authored only — not applied in this plan.
  </acceptance_criteria>
  <done>Migration A authored: DROP+CREATE curriculos_select_own_or_rh with candidate own-folder branch only, upload policies untouched, no BEGIN/COMMIT; applied in 32-04.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire cvUploadService.getSignedUrl → functions.invoke (greens guard + service test)</name>
  <files>src/features/vagas/services/cvUploadService.ts</files>
  <read_first>
    - src/features/vagas/services/cvUploadService.ts:34-47 (CVUploadServiceError), :193-206 (current getSignedUrl with createSignedUrl), :25 (supabase import)
    - src/features/admin/services/usuariosRhService.ts:130-152 — the functions.invoke + extractEfErrorCode idiom
    - 32-PATTERNS.md § cvUploadService.ts — the exact new shape (signature path→candidaturaId)
    - src/features/vagas/services/__tests__/cvUploadService.test.ts (from 32-01) + src/__tests__/guards/no-service-role-src.grep.test.ts (extended in 32-01) — the two tests this greens
  </read_first>
  <action>
    Modify src/features/vagas/services/cvUploadService.ts. Change `getSignedUrl(path: string)` to `getSignedUrl(candidaturaId: string): Promise<string>` and replace the body: `const { data, error } = await supabase.functions.invoke('get-curriculo-url', { body: { candidatura_id: candidaturaId } })`; on `error || !data?.signedUrl` throw `new CVUploadServiceError('Não foi possível gerar URL de download', 'UPLOAD_FAILED', error ?? data)`; return `data.signedUrl as string`. Do NOT log `data.signedUrl` (Pitfall 7). Remove the `createSignedUrl` call and the `EXPIRES_IN_SECONDS = 3600` constant entirely (the EF owns the 60s TTL now). Update the two doc-comment references (:61, :172) that mention `getSignedUrl(path)` to reflect the new `candidaturaId` contract. Optionally normalize the EF error via `extractEfErrorCode` from `@/lib/efErrors` into `.details` (usuariosRhService precedent). There is NO live component consumer of `getSignedUrl` today (only the test) — so the whole surface is the service + its already-updated test; keep the type-check green.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/vagas/services/__tests__/cvUploadService.test.ts src/__tests__/guards/no-service-role-src.grep.test.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - cvUploadService.ts `getSignedUrl(candidaturaId)` calls `supabase.functions.invoke('get-curriculo-url', { body: { candidatura_id: candidaturaId } })`; no `createSignedUrl` token remains in the file.
    - cvUploadService.test.ts `getSignedUrl` T3.1/T3.2 + Pitfall-7 path PASS (invoke mock).
    - The extended `no-service-role-src.grep.test.ts` scan PASSES (no client `createSignedUrl` over curriculos anywhere in src/).
    - `npm run lint` (tsc --noEmit) is green (baseline unchanged).
  </acceptance_criteria>
  <done>cvUploadService.getSignedUrl(candidaturaId) invokes the EF; no client createSignedUrl remains; cvUploadService.test + extended guard GREEN; tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH browser → EF `get-curriculo-url` | untrusted `candidatura_id`; Authorization header is the only identity claim |
| EF → Storage `curriculos` | service_role signs; the browser must never hold this power |
| Build output → shipped `src/` bundle | no service_role, no client `createSignedUrl` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure / Elevation | recruiter A reads vaga-B CV | mitigate | EF ownership check `vagas.created_by === user.id` after role check (Task 1, step 5); Storage RH branch removed (Task 2) |
| T-32-02 | Elevation of Privilege | candidato/anon reaches EF; authenticate-only EF | mitigate | authorize-THEN-authenticate: getUser (401) → role from usuarios_rh (403) BEFORE any privileged read (Task 1, steps 1-2) |
| T-32-03 | Tampering | forged storage path in the request | mitigate | EF ignores any client path; resolves `curriculo_url` from `candidatura_id` server-side (Task 1, step 3-4) |
| T-32-08 | Information Disclosure | service_role / client createSignedUrl in bundle | mitigate | client rewired to functions.invoke; extended guard green (Task 3) |
| T-32-09 | Information Disclosure | signed URL leaked via logs | mitigate | never `console.*` the URL; 60s TTL bounds exposure (Task 1) |
| T-32-10 | Denial of Service / Availability | policy drop before EF live breaks CV reads | mitigate | apply ordering enforced in 32-04 (deploy EF → drop policy); blast radius today is nil (no live consumer) |
| T-32-SC | Tampering (supply-chain) | esm.sh import in the EF | accept | zero new packages; STATIC `esm.sh/@supabase/supabase-js@2` import (no `.join("npm:")` — do not reintroduce the deploy-bundler bug) |
</threat_model>

<verification>
- `deno test … get-curriculo-url` green (all 5 branches) — the authoritative cross-recruiter 403 gate.
- `npm run test:run` on cvUploadService.test.ts + the extended guard green.
- `npm run lint` (tsc) green; `npm run build` succeeds.
- Migration A authored (candidate-branch-only), NOT applied here (applied AFTER EF deploy in 32-04).
</verification>

<success_criteria>
SEG-01 primitive built and greened at the code layer:
- EF authorize-THEN-authenticate with vaga-ownership guard + 60s signed URL — deno test green.
- Client invokes the EF; no client-side `createSignedUrl` survives in `src/` — guard green.
- Migration A (candidate own-folder branch only) authored, ready for the ordered 32-04 apply.
The live behavioral proof (cross-recruiter deny + Storage RH-read gone) runs in 32-04 via seg32_smokes.sql + live curl.
</success_criteria>

<output>
Create `.planning/phases/32-fechar-os-dois-vazamentos-vivos-cv-signed-url-ef-kpi-definer/32-02-SUMMARY.md` when done
</output>
