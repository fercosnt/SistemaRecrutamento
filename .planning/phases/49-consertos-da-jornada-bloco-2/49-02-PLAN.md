---
phase: 49-consertos-da-jornada-bloco-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/ai-client.ts
  - supabase/functions/_shared/audit-logger.ts
  - supabase/functions/_shared/ai-error-codes.ts
  - supabase/functions/_shared/__tests__/ai-client.test.ts
  - supabase/functions/_shared/__tests__/ai-client-budget.test.ts
  - supabase/functions/_shared/__tests__/ai-error-codes.test.ts
autonomous: true
requirements: [JORN-28, JORN-39]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "«Não coube», «demorou» e «fora do schema» deixam de virar o mesmo código: `callAi` embrulha o `parse` do `output_config.format` para que ele nunca lance (formato `FALHA_PARSE`, RESEARCH §E.2/§Code Examples, verificado contra `@anthropic-ai/sdk@0.102.0`), e classifica pela resposta inteira: `stop_reason === 'max_tokens'` ⇒ `anthropic_max_tokens`; parse falho com `end_turn` ⇒ `anthropic_schema_invalid`; `refusal` ⇒ `anthropic_refusal`; `APIConnectionTimeoutError`/regex de timeout ⇒ `anthropic_timeout`; 429/503/529 esgotado ⇒ `anthropic_overloaded`; demais ⇒ `anthropic_api_error`; disjuntor aberto ⇒ `anthropic_circuit_open` (D-27c)."
    - "Todo fallback grava DUAS linhas em `ai_call_logs`: a tentativa Anthropic (`provider='anthropic'`, `success=false`, `error_code` da causa, custo e tokens do `usage` quando houver, `idempotency_key` NULL — senão o upsert por chave a apagaria, Pitfall 1) e o resultado (`provider='openai'`, `model_snapshot = response.model`, `success=true`, `error_code = 'fallback_' || causa`). Com o disjuntor aberto não há tentativa, logo só a linha do resultado (`fallback_anthropic_circuit_open`)."
    - "O fallback respeita o prompt: `max_completion_tokens = prompt.max_tokens` e `temperature = prompt.temperature` na chamada OpenAI (hoje ignora — `ai-client.ts:742-749`). `LengthFinishReasonError`/erro da OpenAI grava a linha `provider='openai'`, `success=false`, `error_code` `openai_max_tokens`/`openai_schema_invalid`/`openai_error` ANTES de relançar (Discretion obrigatória do JORN-28)."
    - "O replay de idempotência não esconde fallback: uma linha com `error_code` que começa com `fallback_` NÃO é replayada (o clique seguinte tenta o Sonnet de novo); o replay de sucesso Anthropic passa a devolver `model` (de `model_snapshot`), `log_id` (o `id` da linha) e `replayed: true` — flag própria, distinta de `cache_hit`, que continua significando prompt-cache (RESEARCH Correção 8)."
    - "`CallAiResult` ganha `model` (modelo REAL: `response.model` no caminho Anthropic e no OpenAI, `model_snapshot` no replay), `log_id` (id da linha do resultado em `ai_call_logs`), `replayed` e `fallback_cause` — é daqui que as EFs gravam `provedor_ia`/`modelo_ia` (D-28) e `ai_call_log_id` (D-38)."
    - "O disjuntor só conta falha de saúde do provedor (`anthropic_timeout`, `anthropic_overloaded`, `anthropic_api_error`); truncamento, schema e recusa são determinísticos pelo input e não abrem o disjuntor."
    - "O teto de custo diário AI-06 soma o `cost_usd` de TODAS as linhas do dia da vaga, com ou sem sucesso — a tentativa truncada foi cobrada (≈ US$ 0,058 em 20/09) e é gasto real."
    - "`logAiCall` devolve `{ id, error }` (lendo `id` com `.select('id').single()` quando o cliente oferece, e degradando para `id: null` quando o mock não oferece) e NUNCA lança; quando a linha que falhou é de `provider='none'` (teto de custo, injeção), o erro é registrado em `recruiter_alerts` no molde de `emitPromptStubAlert` — o erro deixa de ser engolido (JORN-39)."
    - "BORDA explícita (probe JORN-39/idempotency): as linhas `provider='none'` são gravadas com `idempotency_key` NULL — N chamadas bloqueadas geram N linhas de auditoria; um sucesso posterior da mesma chave nunca sobrescreve um bloqueio, e um bloqueio nunca sobrescreve um sucesso (teste Deno com o mock que impõe a UNIQUE)."
    - { statement: "BORDA (probe JORN-39/concurrency): chamadas bloqueadas simultâneas gravam cada uma a sua linha (INSERT simples, sem disputa de upsert por chave NULL); nenhuma se perde por conflito.", verification: backstop }
    - "`inputHashDe(raw)` exportada de `audit-logger.ts` devolve exatamente o `input_hash` que `logAiCall` grava para a mesma entrada passada ao `callAi` — é o hash do D-38, conferível por consulta (`entrevista_analises.texto_hash = ai_call_logs.input_hash`); um teste compara os dois."
    - "Os códigos e os rótulos pt-BR da causa (não coube / demorou / fora do schema / recusa / sobrecarga / erro da API / disjuntor aberto) moram numa constante única, sem imports, em `_shared/ai-error-codes.ts`, que o `ai-client` usa e a tela do admin (49-15) importa."
    - "As asserções de `ai-client.test.ts` que codificavam o comportamento antigo (`:289-298`, `:306-338`, `:437-453`, `:460-479`, `:482-510`, `:646`) mudam DE PROPÓSITO neste plano, cada uma com um comentário de proveniência, e a suíte Deno inteira das EFs segue verde (baseline 411/0 nas EFs tocadas); a mordida é provada por mutação (D-56)."
    - "Nenhuma EF é deployada por este plano: o deploy de cada consumidora é do plano que a toca (49-08, 49-09, 49-10, 49-11, 49-23, 49-24), depois do schema do 49-01 (Pitfall 8, D-55)."
  artifacts:
    - path: supabase/functions/_shared/ai-client.ts
      provides: "taxonomia de causa, duas linhas por fallback, replay honesto, proveniência no CallAiResult"
      contains: "fallback_"
    - path: supabase/functions/_shared/audit-logger.ts
      provides: "logAiCall → {id, error}; alerta para linha none; inputHashDe"
      contains: "inputHashDe"
    - path: supabase/functions/_shared/ai-error-codes.ts
      provides: "constante única de códigos e rótulos de causa"
      contains: "anthropic_max_tokens"
    - path: supabase/functions/_shared/__tests__/ai-client.test.ts
      provides: "asserções do comportamento novo + mudadas de propósito"
  key_links:
    - from: "CallAiResult.model / .provider / .log_id"
      to: "INSERT/UPSERT das EFs (49-08..49-11): provedor_ia, modelo_ia, ai_call_log_id"
      via: "campo do retorno do callAi"
      pattern: "result\\.model"
    - from: "_shared/ai-error-codes.ts"
      to: "src/features/admin/ai-logs/components/AiLogsPage.tsx (49-15)"
      via: "import relativo de constante sem imports (precedente exportacaoService.ts:61)"
      pattern: "ai-error-codes"
    - from: "audit-logger.inputHashDe"
      to: "supabase/functions/avaliar-transcricao-entrevista/index.ts (49-10) — D-40 antes da chamada"
      via: "mesmo hash que ai_call_logs.input_hash"
      pattern: "inputHashDe\\("
  prohibitions:
    - requirement_id: JORN-28
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT apresentar um resultado de fallback como se fosse do modelo configurado — nem no log (estado próprio), nem no replay (não replaya fallback), nem no retorno (`model` real)"
    - requirement_id: JORN-39
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT lançar do `audit-logger` no caminho da EF — falha de auditoria vira alerta, nunca derruba a avaliação do candidato"
    - requirement_id: JORN-28
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT transformar o teto de custo ou a injeção em rejeição do candidato — o resultado continua `hold` + revisão humana (RNF-07a)"
---

<objective>
O cliente de IA compartilhado (`_shared/ai-client.ts`) e o logger de auditoria passam a dizer a verdade
sobre o que aconteceu em cada chamada: qual foi a causa quando o Sonnet falhou, qual modelo produziu o
resultado, quanto a tentativa falha custou — e o bloqueio por teto de custo ou injeção deixa de sumir.

Purpose: JORN-28 (D-27c) e JORN-39. É o dono único destes dois arquivos na fase (RESEARCH «dono único»);
as 7 EFs consumidoras só LEEM o novo contrato e são deployadas pelos planos que as tocam.

Output: `ai-client.ts`, `audit-logger.ts`, a constante `ai-error-codes.ts` e os testes Deno — sem deploy.

Orçamento (revisão do checker, W1): NÃO dividido — é o dono único de `ai-client.ts`/`audit-logger.ts` e todo consumidor
embarca esses dois arquivos no próprio deploy (49-08/09/10/11/23/24); uma segunda fatia teria de ocupar uma onda
sozinha antes de todos eles (+1 onda no caminho crítico inteiro) ou correr em paralelo com deploys que empacotariam um
`ai-client` pela metade. A Task 3 é a menor das três; o excesso (1,20×) é sobre estimativa sem calibração
(sample_count 0).
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-VARREDURA-KICKOFF.md
</context>

<interfaces>
Contrato novo do retorno (as EFs dos planos 49-08..49-11 dependem destes nomes):

- `CallAiResult` = `{ provider, parsed, cost_usd, latency_ms, cache_hit, prompt_version, error_code?, flagged_for_human_review?, model: string | null, log_id: string | null, replayed: boolean, fallback_cause: string | null }`
- `logAiCall(supabaseAdmin, row): Promise<{ id: string | null; error: string | null }>`
- `inputHashDe(rawInput: string): Promise<string>` (em `audit-logger.ts`)
- `_shared/ai-error-codes.ts`: `AI_ERROR_CODE` (objeto `as const` com os códigos), `CAUSA_FALLBACK_ROTULO` (código → rótulo pt-BR), `ehFallback(errorCode)`, `causaDoFallback(errorCode)` — zero imports.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): «não coube» de ponta a ponta — truncamento detectado, duas linhas no log, fallback com o teto do prompt, modelo real no retorno</name>
  <files>supabase/functions/_shared/ai-client.ts, supabase/functions/_shared/audit-logger.ts, supabase/functions/_shared/__tests__/ai-client.test.ts</files>
  <read_first>
    - `supabase/functions/_shared/ai-client.ts` (inteiro — 806 linhas; em especial `:119-153` ResolvedPrompt, `:266-275` CallAiResult, `:288-302` isRetryable, `:376-415` replay, `:626-706` loop Anthropic, `:731-806` runOpenAIFallback)
    - `supabase/functions/_shared/audit-logger.ts` (inteiro — `:112-118` computeInputHash, `:127-186` logAiCall, `:209-235` emitPromptStubAlert)
    - `supabase/functions/_shared/__tests__/ai-client.test.ts` (os mocks `makeMockAnthropic`/`makeMockOpenAI`/`makeMockSupabase`, o mock que impõe a UNIQUE de `:621-680`)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md` §E.1, §E.2, §«Code Examples» (formato embrulhado), Pitfall 1
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md` §14
    - O fonte do SDK no cache do Deno, para conferir o comportamento antes de confiar nele: `~/Library/Caches/deno/npm/registry.npmjs.org/@anthropic-ai/sdk/0.102.0/resources/messages/messages.js` (`parse` = `create().then(parseMessage)`) e `lib/parser.js` (`:51-64`); `openai/6.42.0/lib/parser.js` (`:96-97`, `LengthFinishReasonError`)
  </read_first>
  <behavior>
    - Teste A: Anthropic mock devolve `stop_reason: 'max_tokens'`, `usage` com tokens e `parsed_output` do formato embrulhado com a falha → callAi grava 2 linhas: (1) `provider='anthropic'`, `success=false`, `error_code='anthropic_max_tokens'`, `idempotency_key` null, `cost_usd > 0`; (2) `provider='openai'`, `success=true`, `error_code='fallback_anthropic_max_tokens'`, `model_snapshot` = o `model` que o mock da OpenAI devolveu.
    - Teste B: a chamada OpenAI do fallback recebe `max_completion_tokens` = `prompt.max_tokens` e `temperature` = `prompt.temperature`.
    - Teste C: o retorno tem `provider='openai'`, `model` = o `model` do mock OpenAI, `fallback_cause='anthropic_max_tokens'`, `replayed=false`, `log_id` = o id que o mock de insert devolveu para a linha do resultado.
    - Teste D: caminho feliz Anthropic → `model` = `response.model` do mock, `log_id` preenchido, `fallback_cause` null.
    - Teste E: com o mock de supabase SEM `.select` encadeável, `logAiCall` devolve `{ id: null, error: null }` e nada lança (os mocks das 7 EFs continuam válidos).
  </behavior>
  <action>
Passo 1 — D-50: re-rodar os padrões C6 e C9 do `49-VARREDURA-KICKOFF.md` (o `grep` de escrita sem erro destruturado e a consulta `max_tokens` × saída real por `call_type`, só leitura) e registrar o delta no SUMMARY. A C9 é a varredura obrigatória do JORN-28: registrar os 7 `call_type` com `max_tokens`, maior saída, maior latência e o teto por tempo (~4950 tok); o `interview_guide` (98,4 s de 110 s) fica registrado como risco P1, NÃO consertado (§Deferred).

Passo 2 — `audit-logger.ts`: `logAiCall` passa a devolver `Promise<{ id: string | null; error: string | null }>`. Depois do upsert/insert, quando o objeto devolvido pelo cliente tiver `select` função, encadear `.select('id').single()` e ler `data.id`; senão, aguardar o resultado como hoje e devolver `id: null` (é o que mantém válidos os mocks das EFs, que devolvem só `{ error }`). O `console.error` com só código+resumo continua; o erro vira `error` no retorno, nunca exceção.

Passo 3 — `ai-client.ts`, caminho Anthropic (`:626-706`): montar o formato com `zodOutputFormat(schema, prompt.call_type)` e, se ele tiver `parse` função, embrulhá-lo num objeto de mesma forma cujo `parse` captura a exceção e devolve `{ [FALHA_PARSE]: erro }` (`FALHA_PARSE = Symbol.for("callAi.falhaParse")`); o no-op dos testes (sem `parse`) passa intacto. Depois de `messages.parse`, ler `stop_reason` e o marcador: `max_tokens` ⇒ causa `anthropic_max_tokens`; `refusal` ⇒ `anthropic_refusal`; marcador presente ⇒ `anthropic_schema_invalid`; senão é sucesso. Com causa: gravar a linha da tentativa (`provider: "anthropic"`, `model_id: prompt.model_id`, `model_snapshot: response.model`, `success: false`, `error_code: <causa>`, tokens e `cost_usd` do `usage` via `calculateCost`, `raw_response: { stop_reason, usage }` — nunca o texto truncado —, `idempotency_key: null`) e ir ao fallback com a causa. O `JSON.stringify` do fingerprint ignora a função do embrulho, então a chave de idempotência não muda por causa dele (conferir num teste).

Passo 4 — `runOpenAIFallback`: receber `causa: string` em vez de `circuitWasOpen`/`triggerError` genéricos; passar `max_completion_tokens: prompt.max_tokens` e `temperature: prompt.temperature` ao `chat.completions.parse`; em sucesso gravar a linha `provider: "openai"`, `model_id: OPENAI_FALLBACK_MODEL` (P1 fora: o modelo segue hardcoded), `model_snapshot: response.model`, `success: parsed !== null`, `error_code: "fallback_" + causa`, `idempotency_key` = a chave efetiva (é a linha do resultado); devolver `model: response.model ?? OPENAI_FALLBACK_MODEL`, `log_id` do `logAiCall`, `fallback_cause: causa`, `replayed: false`, e `error_code: "fallback_" + causa`. Caminho Anthropic de sucesso devolve `model: response.model ?? prompt.model_id`, `log_id`, `fallback_cause: null`, `replayed: false`.

Passo 5 — testes A–E do `<behavior>` em `ai-client.test.ts` (mock Anthropic com `stop_reason` e `usage`; mock OpenAI que registra os parâmetros e devolve `model`; mock de supabase cujo insert/upsert devolve algo com `select().single()` → `{ data: { id } }`). Rodar o arquivo.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/_shared/__tests__/ai-client.test.ts</automated>
    <fails_when>non-zero exit, or "FAILED" in the output, or the summary line reports 0 passed</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const s=require('fs').readFileSync('supabase/functions/_shared/ai-client.ts','utf8');for(const t of ['FALHA_PARSE','stop_reason','anthropic_max_tokens','max_completion_tokens','temperature','fallback_','log_id','replayed','fallback_cause'])if(!s.includes(t))throw new Error('falta no ai-client: '+t);console.log('OK')"</automated>
    <fails_when>non-zero exit with "falta no ai-client:" in stderr</fails_when>
  </verify>
  <acceptance_criteria>
    - Os testes A–E existem com esses comportamentos nomeados e passam.
    - Um fallback por truncamento produz exatamente 2 linhas no mock de `ai_call_logs` (a tentativa com `idempotency_key` null e o resultado com a chave).
    - A chamada OpenAI do fallback recebe `max_completion_tokens` e `temperature` do prompt.
    - `logAiCall` não lança com nenhum dos mocks existentes (teste E).
  </acceptance_criteria>
  <done>Um comparativo que não coube passa a deixar rastro verdadeiro: a tentativa cobrada, a causa, o modelo que de fato respondeu — e o retorno leva esse modelo para a tabela de resultado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: o resto da taxonomia, disjuntor, replay honesto, AI-06 — e as asserções antigas mudadas de propósito</name>
  <files>supabase/functions/_shared/ai-client.ts, supabase/functions/_shared/ai-error-codes.ts, supabase/functions/_shared/__tests__/ai-client.test.ts, supabase/functions/_shared/__tests__/ai-client-budget.test.ts, supabase/functions/_shared/__tests__/ai-error-codes.test.ts</files>
  <read_first>
    - o estado deixado pela Task 1
    - `supabase/functions/_shared/__tests__/ai-client.test.ts` (`:289-338`, `:437-510`, `:512-760`, `:646`) e `supabase/functions/_shared/__tests__/ai-client-budget.test.ts`
    - `supabase/functions/_shared/email-config.ts` (`:12-30` — o contrato ZERO IMPORTS e as constantes `as const`)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md` §E.2 itens 2, 4, 5, 7
    - `CLAUDE.md` §«Portões: varra pela FORMA» (prove que o portão ainda morde)
  </read_first>
  <behavior>
    - timeout (erro com nome `APIConnectionTimeoutError`) esgotado ⇒ tentativa com `anthropic_timeout` e resultado `fallback_anthropic_timeout`; 529 esgotado ⇒ `anthropic_overloaded`; 400 genérico ⇒ `anthropic_api_error`; `end_turn` com marcador ⇒ `anthropic_schema_invalid`; `refusal` ⇒ `anthropic_refusal`; disjuntor aberto ⇒ só a linha `fallback_anthropic_circuit_open`.
    - `breaker.recordFailure()` chamado para timeout/overloaded/api_error e NÃO chamado para max_tokens/schema/refusal.
    - replay: linha `success=true` com `error_code` `fallback_…` ⇒ não replaya (provedor chamado de novo); linha Anthropic de sucesso ⇒ replaya com `replayed=true`, `model` = `model_snapshot`, `log_id` = id; `cache_hit` continua refletindo só prompt-cache.
    - AI-06: a soma do dia inclui linhas `success=false` com custo (a tentativa truncada conta para o teto).
    - `ai-error-codes.ts`: todo código emitido pelo `ai-client` tem rótulo em `CAUSA_FALLBACK_ROTULO`; `ehFallback('fallback_anthropic_timeout')` = true; `causaDoFallback('fallback_anthropic_timeout')` = 'anthropic_timeout'.
  </behavior>
  <action>
Criar `supabase/functions/_shared/ai-error-codes.ts` SEM IMPORTS (docblock no molde de `email-config.ts:12-17`, dizendo que o front o importa por caminho relativo): `AI_ERROR_CODE` com `anthropic_max_tokens`, `anthropic_timeout`, `anthropic_schema_invalid`, `anthropic_refusal`, `anthropic_overloaded`, `anthropic_api_error`, `anthropic_circuit_open`, `openai_max_tokens`, `openai_schema_invalid`, `openai_error`, `cost_cap_exceeded`, `prompt_injection_detected`; o legado `anthropic_retries_exhausted` como código conhecido só para ler as 17 linhas antigas (sem escrita retroativa); `CAUSA_FALLBACK_ROTULO` pt-BR (`anthropic_max_tokens` → «não coube», `anthropic_timeout` → «demorou», `anthropic_schema_invalid` → «fora do schema», `anthropic_refusal` → «recusa do modelo», `anthropic_overloaded` → «provedor sobrecarregado», `anthropic_api_error` → «erro do provedor», `anthropic_circuit_open` → «disjuntor aberto», `anthropic_retries_exhausted` → «causa não registrada (antes da Phase 49)»); `PREFIXO_FALLBACK = 'fallback_'`; `ehFallback(code)` e `causaDoFallback(code)`.

`ai-client.ts` passa a importar os códigos dessa constante. No `catch` do loop Anthropic: classificar o erro — nome `APIConnectionTimeoutError` ou a regex de timeout já usada por `isRetryable` ⇒ `anthropic_timeout`; status em `RETRYABLE_STATUS` ⇒ `anthropic_overloaded`; demais ⇒ `anthropic_api_error`; `breaker.recordFailure()` SÓ nesses três. Quando o loop esgota com uma dessas causas, gravar a linha da tentativa (mesma forma da Task 1, `idempotency_key: null`, sem `usage` ⇒ custo 0) antes do fallback; `triggerError.message` vai no `error_message` da tentativa, não do resultado. Disjuntor aberto (`:614-621`) ⇒ fallback direto com causa `anthropic_circuit_open`, sem linha de tentativa. Erro da OpenAI no fallback ⇒ gravar linha `provider: "openai"`, `success: false`, `error_code` `openai_max_tokens` (nome `LengthFinishReasonError`) / `openai_error`, e relançar a mensagem combinada de hoje.

`tryIdempotencyReplay`: selecionar também `id` e `model_snapshot`; se `ehFallback(existing.error_code)` ⇒ `null`; senão devolver `replayed: true`, `model: existing.model_snapshot`, `log_id: existing.id`, `fallback_cause: null`. `isDailyCostCapExceeded`: tirar o filtro `success=true` da soma (comentário com a razão: tentativa falha é gasto cobrado).

Mudar DE PROPÓSITO as asserções que codificavam o comportamento antigo — `ai-client.test.ts:289-298` (circuit open: `error_code` passa a ser `fallback_anthropic_circuit_open`, `fallback_cause='anthropic_circuit_open'`), `:306-338` (o disjuntor acumula só falhas de saúde), `:437-453` (retry-budget: a causa agora é `anthropic_timeout` e há a linha da tentativa), `:460-479` e `:482-510` (replay: fallback não é replayado; sucesso Anthropic replaya com `replayed`), `:646` (upsert da linha de resultado; a tentativa é insert com chave nula) — cada uma com um comentário «Phase 49 / JORN-28: antes assertia X; mudou porque Y». Ajustar `ai-client-budget.test.ts` onde ele assere a soma com filtro de sucesso ou o código genérico. Escrever `__tests__/ai-error-codes.test.ts` (Deno): todo código literal que aparece no `ai-client.ts` tem rótulo; os helpers.

Provar que os portões mordem (D-56): numa cópia de rascunho em `$TMPDIR`, reverter o `error_code` do fallback para o genérico e rodar o arquivo de teste contra a cópia (import map igual) — esperado: FAIL nos testes de taxonomia; registrar a saída no SUMMARY e descartar a cópia. Rodar a suíte Deno inteira das EFs (sem `strict-schema.test.ts`, deferred 48-07).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/_shared/__tests__/ai-client.test.ts supabase/functions/_shared/__tests__/ai-client-budget.test.ts supabase/functions/_shared/__tests__/ai-error-codes.test.ts supabase/functions/_shared/__tests__/structured-output-compat.test.ts supabase/functions/_shared/__tests__/prompt-catch.test.ts</automated>
    <fails_when>non-zero exit, or "FAILED" in the output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && find supabase/functions -name "*.test.ts" | grep -v strict-schema | xargs deno test --allow-all 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, or "FAILED" / a "failed" count above 0 in the final summary lines (a consumer EF test broken by the new contract)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const s=require('fs').readFileSync('supabase/functions/_shared/ai-error-codes.ts','utf8');if(/^\s*import\s/m.test(s))throw new Error('ai-error-codes.ts tem import (o front nao pode importa-lo)');for(const t of ['anthropic_max_tokens','anthropic_timeout','anthropic_schema_invalid','anthropic_retries_exhausted','fallback_'])if(!s.includes(t))throw new Error('falta: '+t);console.log('OK')"</automated>
    <fails_when>non-zero exit with "tem import" or "falta:" in stderr</fails_when>
  </verify>
  <acceptance_criteria>
    - Cada causa do `<behavior>` tem teste e passa; o disjuntor só conta falha de saúde.
    - Replay de linha de fallback não acontece; replay de sucesso Anthropic devolve `replayed`, `model`, `log_id`.
    - AI-06 soma também as linhas sem sucesso.
    - As 6 asserções antigas mudaram com comentário de proveniência; a mutação de rascunho reprova (saída registrada no SUMMARY).
    - A suíte Deno das EFs segue sem falha (contagem no SUMMARY, comparada à baseline 411/0 das tocadas).
  </acceptance_criteria>
  <done>O log do admin terá dado para mostrar «não coube / demorou / fora do schema» separados, e nenhum clique de RH recebe de novo, calado, um resultado de fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: JORN-39 — o bloqueio por custo e por injeção fica registrado, e o hash do D-38 sai do mesmo lugar que o log</name>
  <files>supabase/functions/_shared/ai-client.ts, supabase/functions/_shared/audit-logger.ts, supabase/functions/_shared/__tests__/ai-client.test.ts</files>
  <read_first>
    - o estado deixado pelas Tasks 1–2
    - `supabase/functions/_shared/ai-client.ts` (`:512-608` — replay, teto de custo e injeção, as duas linhas `provider: "none"`)
    - `supabase/functions/_shared/audit-logger.ts` (`:209-235` — `emitPromptStubAlert`: o molde de «nunca lança, registra em `recruiter_alerts`, loga só código»)
    - `supabase/migrations/20260922000001_p49_llm_provider_none.sql` (o valor `none` que o 49-01 aplicou)
  </read_first>
  <behavior>
    - As linhas de teto de custo e de injeção são gravadas com `idempotency_key` null: duas chamadas bloqueadas com a mesma chave efetiva ⇒ 2 linhas `none` (mock com UNIQUE não reprova); um sucesso posterior da mesma chave não apaga nenhuma delas.
    - Quando o insert da linha `none` devolve erro, `recruiter_alerts` recebe um alerta (tipo próprio, só código e `call_type`, nunca o input) e `callAi` ainda devolve o `hold` + revisão humana (nada lança).
    - `inputHashDe(raw)` = o `input_hash` que o mock capturou na linha gravada pelo `callAi` com o mesmo `raw` (entrada com CPF e e-mail, para exercitar a máscara).
  </behavior>
  <action>
Nas duas linhas `provider: "none"` (`:536-570` teto de custo, `:573-608` injeção): `idempotency_key: null` (comentário: é evento de auditoria, cada bloqueio é uma linha; com a chave efetiva o upsert apagaria o bloqueio no primeiro sucesso). Capturar o `{ error }` devolvido por `logAiCall`; se houver erro, chamar uma função nova `emitAuditLossAlert(supabase, call_type, error_code)` em `audit-logger.ts`, no molde exato de `emitPromptStubAlert` (insert em `recruiter_alerts` dentro de `try`, `catch` que só loga o código, NUNCA lança; conferir no `pg_constraint`/`information_schema` de `recruiter_alerts` — só leitura — que o `tipo`/colunas usados existem; se exigirem valor novo de enum/CHECK, PARAR e reportar em vez de inventar, D-14). O retorno `hold` + `flagged_for_human_review` não muda (RNF-07a).

`audit-logger.ts`: exportar `inputHashDe(rawInput: string): Promise<string>`, que reproduz exatamente o que o `callAi` + `logAiCall` gravam (a máscara do `callAi` e a do `logAiCall` em sequência, depois `computeInputHash`) — com docblock dizendo que é o hash do D-38 e que `entrevista_analises.texto_hash` tem de bater com `ai_call_logs.input_hash`. Testes do `<behavior>`. Rodar a suíte Deno inteira das EFs de novo e commitar.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/_shared/__tests__/ai-client.test.ts supabase/functions/_shared/__tests__/ai-client-budget.test.ts supabase/functions/_shared/__tests__/pii-masker.test.ts</automated>
    <fails_when>non-zero exit, or "FAILED" in the output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && find supabase/functions -name "*.test.ts" | grep -v strict-schema | xargs deno test --allow-all 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, or "FAILED" / a "failed" count above 0 in the final summary lines</fails_when>
  </verify>
  <acceptance_criteria>
    - As linhas `none` saem com `idempotency_key` null; N bloqueios ⇒ N linhas no mock.
    - Falha de insert da linha `none` gera alerta em `recruiter_alerts` e não lança.
    - `inputHashDe` bate com o `input_hash` gravado (teste).
    - Suíte Deno das EFs sem falha.
  </acceptance_criteria>
  <done>Todo bloqueio por custo ou injeção vira uma linha de auditoria — e, se nem ela puder ser gravada, alguém fica sabendo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF → provedores de IA (Anthropic/OpenAI) | resposta externa decide o conteúdo do resultado; o log é a prova de quem respondeu |
| EF → `ai_call_logs` (service_role) | trilha de auditoria de IA, só administrador lê |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-02-01 | Repudiation | resultado de fallback apresentado como do modelo configurado | high | mitigate | duas linhas por fallback; `error_code` `fallback_<causa>`; `model` real no retorno; replay não serve fallback |
| T-49-02-02 | Repudiation | bloqueio por custo/injeção sem registro (22P02 engolido) | high | mitigate | `idempotency_key` null nas linhas `none`; erro de insert vira alerta em `recruiter_alerts` |
| T-49-02-03 | Information Disclosure | texto truncado ou input bruto no log de erro | medium | mitigate | tentativa grava só `{stop_reason, usage}`; alerta leva só código e `call_type`; máscara PII inalterada |
| T-49-02-04 | Denial of Service | disjuntor aberto por falha determinística de tamanho/schema | medium | mitigate | `recordFailure` só para timeout/overloaded/api_error |
| T-49-02-05 | Tampering | gasto real fora do teto AI-06 | medium | mitigate | soma do dia sem filtro de sucesso |
| T-49-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação — SDKs já pinados |
</threat_model>

<verification>
- `ai-client.test.ts`, `ai-client-budget.test.ts`, `ai-error-codes.test.ts`, `structured-output-compat.test.ts` verdes; suíte Deno das EFs sem falha.
- Mutação de rascunho reprova os testes de taxonomia (saída no SUMMARY).
- Varreduras C6/C9 re-rodadas, delta no SUMMARY.
</verification>

<success_criteria>
O contrato do `callAi` carrega a verdade que as EFs precisam gravar (modelo real, id da linha, causa) e o log de IA passa a distinguir sucesso, fallback e falha — sem nenhum deploy ainda.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `CallAiResult.model`, `.log_id`, `.replayed`, `.fallback_cause` | campos de retorno | **novos** |
| códigos `anthropic_max_tokens`, `anthropic_timeout`, `anthropic_schema_invalid`, `anthropic_refusal`, `anthropic_overloaded`, `anthropic_api_error`, `openai_max_tokens`, `openai_schema_invalid`, `openai_error`, `fallback_<causa>` | valores de `ai_call_logs.error_code` | **novos** |
| `FALHA_PARSE` (formato embrulhado) | símbolo interno | **novo** |
| `logAiCall` → `{ id, error }` | assinatura | editada |
| `emitAuditLossAlert(supabase, call_type, error_code)` | função | **nova** |
| `inputHashDe(rawInput)` | função | **nova** |
| `supabase/functions/_shared/ai-error-codes.ts` (`AI_ERROR_CODE`, `CAUSA_FALLBACK_ROTULO`, `PREFIXO_FALLBACK`, `ehFallback`, `causaDoFallback`) | constante sem imports | **nova** |
| `supabase/functions/_shared/__tests__/ai-error-codes.test.ts` | teste Deno | **novo** |
| `ai-client.test.ts`, `ai-client-budget.test.ts` | testes Deno | editados (de propósito) |

## Premissas sinalizadas (probe de borda)

JORN-28 saiu `unclassified` no probe (texto pt-BR) e fica `unresolved` no relatório; as bordas deste plano
foram escritas a partir do RESEARCH §E. As sondas de JORN-39 foram resolvidas: `idempotency` como verdade
explícita (N bloqueios ⇒ N linhas, testado); `concurrency` como marcador `backstop` (a garantia é do INSERT
com chave nula; não há teste de concorrência real).

A premissa A1 do RESEARCH (a OpenAI usa `temperature=1` quando omitida) só muda a justificativa: o conserto
passa a enviar o valor do prompt de qualquer jeito.

Fora deste plano, registrado: o modelo de fallback continua hardcoded (`OPENAI_FALLBACK_MODEL`) e o
parâmetro `fallback_model_id` das EFs continua ignorado — é P1 (§Deferred). O `interview_guide` a 89 % do
timeout fica no SUMMARY como risco, sem mudança de `max_tokens`.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-02-SUMMARY.md` when done
</output>
