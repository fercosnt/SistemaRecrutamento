---
phase: 49-consertos-da-jornada-bloco-2
plan: 08
type: execute
wave: 2
depends_on: ["49-01", "49-02", "49-03"]
files_modified:
  - supabase/functions/_shared/comparativo-config.ts
  - supabase/functions/_shared/analise-schemas.ts
  - supabase/functions/comparativo-candidatos/index.ts
  - supabase/functions/comparativo-candidatos/__tests__/index.test.ts
  - supabase/migrations/20260922000005_p49_comparativo_max_tokens.sql
autonomous: true
requirements: [JORN-32, JORN-25, JORN-28]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-59: uma constante única `COMPARATIVO_MAX_CANDIDATOS = 4` (e `COMPARATIVO_MIN_CANDIDATOS = 2`) em `supabase/functions/_shared/comparativo-config.ts`, sem imports, alimenta a validação da EF, o `ComparativeRankingSchema.ranked_candidates.max(...)` de `analise-schemas.ts` e (no plano 49-22) a seleção do front. Pedido com mais de 4 é recusado ANTES da chamada, com mensagem verdadeira montada da constante."
    - "D-59: `prompt_versions.max_tokens` do `comparative_ranking` ativo passa de 3000 para 3600 por UPDATE na própria linha, com guarda de valor esperado e pós-condição, sem versão nova de prompt (`prevent_published_prompt_edit` não protege `max_tokens` — RESEARCH Correção 6). Conta registrada no cabeçalho: 45 tok/s × 80 s = 3600."
    - "JORN-32 (IDOR): a EF lê `candidaturas(id, vaga_id, etapa_atual, status)` dos ids pedidos e recusa com 403 genérico se QUALQUER uma não pertence a `body.vaga_id` (a vaga cuja posse foi checada) ou não existe — hoje ela só confere que as análises são da MESMA vaga entre si."
    - "D-34: candidatura encerrada pelo predicado canônico (`candidaturaEncerrada` de `_shared/candidaturaEncerrada.ts`, a MESMA do front) ⇒ 400 `ENCERRADA` «Candidatura encerrada não entra no comparativo.»; análise ausente ⇒ 400 `SEM_ANALISE` com os ids sem análise — a mensagem falsa «pertencem a vagas diferentes» deixa de aparecer para knockout sem análise. Retirada a pedido NÃO é encerrada e segue comparável."
    - "Rótulo pela chave (Discretion obrigatória do 25): a ordenação é `score_match DESC, candidatura_id ASC` (desempate estável) e a resposta devolve `posicoes: { C1: <candidatura_id>, … }` na mesma ordem do prompt — o front rotula por ela, nunca pela posição da seleção."
    - "D-28: o INSERT em `comparativo_solicitado` grava `provedor_ia` (= `result.provider`) e `modelo_ia` (= `result.model`, o modelo REAL do 49-02) e CHECA o erro — escrita falha devolve 500, nunca `{ ok: true }` (varredura C6 #4). A resposta também leva `provedor_ia`, `modelo_ia` e `fallback_cause` para o selo da tela e do PDF (D-27b, plano 49-13)."
    - "O bloco de cada candidato no prompt continua com o formato `Candidato C<n> (id=<candidatura_id>)` — é por esse `id=` que o motor de exclusão acha as linhas `comparative_ranking` do titular no `ai_call_logs` (D-63, plano 49-14)."
    - "As asserções de `comparativo-candidatos/__tests__/index.test.ts:191-259` (MIXED_VAGA, teto 2–10) mudam DE PROPÓSITO, com comentário de proveniência (D-56)."
    - "Deploy por `node efdeploy.cjs comparativo-candidatos` com `--dry-run` antes (fechamento conferido à mão: `_shared/comparativo-config.ts`, `_shared/candidaturaEncerrada.ts`, `_shared/ai-error-codes.ts` presentes) e `verify_jwt=true` preservado; migration do 49-01 conferida no catálogo ANTES do deploy (Pitfall 8)."
  artifacts:
    - path: supabase/functions/_shared/comparativo-config.ts
      provides: "o teto do comparativo numa constante (EF + schema + front)"
      contains: "COMPARATIVO_MAX_CANDIDATOS"
    - path: supabase/migrations/20260922000005_p49_comparativo_max_tokens.sql
      provides: "max_tokens 3000 → 3600 com guarda"
      contains: "comparative_ranking"
    - path: supabase/functions/comparativo-candidatos/index.ts
      provides: "IDOR, encerrada, sem análise, teto, desempate estável, posicoes, proveniência com erro checado"
      contains: "posicoes"
  key_links:
    - from: "_shared/comparativo-config.ts"
      to: "comparativo-candidatos/index.ts, analise-schemas.ts, TriagemTable/ComparativoCandidatosPage/DecisaoFinalPage (49-22)"
      via: "import da constante"
      pattern: "COMPARATIVO_MAX_CANDIDATOS"
    - from: "resposta { posicoes, provedor_ia, modelo_ia }"
      to: "triagemService.invokeComparativo → ComparativoCandidatosPage / exportComparativo (49-13) e DecisaoFinalPage (49-22)"
      via: "contrato JSON da EF"
      pattern: "posicoes"
    - from: "prompt `Candidato C<n> (id=<candidatura_id>)`"
      to: "anonimizar_candidato — redação das linhas comparative_ranking (49-14, D-63)"
      via: "busca pelo token id= no user_prompt_template"
      pattern: "\\(id=\\$\\{"
  prohibitions:
    - requirement_id: JORN-32
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT revelar a um RH, nem pela mensagem de erro, se uma candidatura de vaga alheia existe — a recusa de IDOR é 403 genérico"
    - requirement_id: JORN-25
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT inventar critério de encerrada — o predicado canônico decide; retirada a pedido segue comparável (D-34)"
    - requirement_id: JORN-28
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT devolver `ok: true` quando a auditoria do comparativo não foi gravada"
---

<objective>
A Edge Function do comparativo passa a conferir a posse de cada candidatura, recusar encerrada com mensagem
verdadeira, caber no tempo (teto de 4 e `max_tokens` 3600), gravar e devolver o modelo real, e devolver a
chave de cada posição do ranking.

Purpose: JORN-32 (IDOR medido em `index.ts:182-220`), D-34 (knockout sem análise recebe «vagas diferentes»),
D-29/D-59 (o de 20/09, com 6 candidatos, truncou em 3000 tokens e saiu do `gpt-4o-mini` como sucesso), D-28
(nenhuma proveniência gravada) e o rótulo por posição (Discretion obrigatória do 25). Uma EF, uma onda de
deploy (CONTEXT §Integração).

Output: a constante do teto, o schema ligado a ela, a migration de `max_tokens`, a EF consertada e deployada.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-02-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): o teto numa constante até a chamada — `max_tokens` 3600 aplicado, EF recusa &gt; 4, grava e devolve o modelo real com `posicoes`</name>
  <precondition>O 49-01 está em PROD (`comparativo_solicitado.provedor_ia` existe: `node p46apply.cjs sql "SET TRANSACTION READ ONLY; select count(*) from information_schema.columns where table_name='comparativo_solicitado' and column_name in ('provedor_ia','modelo_ia')"` = 2) e o 49-02 está no disco (`CallAiResult.model` existe em `_shared/ai-client.ts`).</precondition>
  <files>supabase/functions/_shared/comparativo-config.ts, supabase/functions/_shared/analise-schemas.ts, supabase/migrations/20260922000005_p49_comparativo_max_tokens.sql, supabase/functions/comparativo-candidatos/index.ts, supabase/functions/comparativo-candidatos/__tests__/index.test.ts</files>
  <read_first>
    - `supabase/functions/comparativo-candidatos/index.ts` (inteiro; `:83-95` deps, `:150-310` handler)
    - `supabase/functions/comparativo-candidatos/__tests__/index.test.ts` (fixtures `:26-48`, `makeMockSupabaseAdmin` `:69+`, casos `:191-259`)
    - `supabase/functions/_shared/analise-schemas.ts` (`:130-160` — `ComparativeRankingSchema`, `ranked_candidates.max(10)` em `:156`)
    - `supabase/migrations/20260906000003_interview_prompts_max_tokens.sql` (o analog inteiro da guarda de valor esperado)
    - `supabase/functions/_shared/email-config.ts` (`:12-17` — contrato ZERO IMPORTS)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md` §D-29 (a conta) e §H.2
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md` §6, §15, §16a
  </read_first>
  <action>
Passo 1 — D-50: re-rodar o padrão P3 da C1 do `49-VARREDURA-KICKOFF.md` (os literais do teto: `COMPARE_MAX`, «2 a 10», `.max(10)`, «Máximo de 10») e registrar no SUMMARY os 8 lugares, marcando quais este plano conserta (EF, schema, teste Deno) e quais são do 49-13/49-22 (front). Medir, só leitura: `select max_tokens, is_active, is_canary from public.prompt_versions where call_type='comparative_ranking'` (esperado: a ativa com 3000). Varredura de portões do `CLAUDE.md` (D-56): classificar no SUMMARY todo achado que cite `prompt_versions`, `max_tokens` ou `comparativo_solicitado` (um smoke que pine o `max_tokens` vivo reprovaria o conserto certo).

Passo 2 — criar `supabase/functions/_shared/comparativo-config.ts` SEM IMPORTS: `COMPARATIVO_MIN_CANDIDATOS = 2 as const` e `COMPARATIVO_MAX_CANDIDATOS = 4 as const`, com docblock (D-59: a conta 45 tok/s × 80 s = 3600 tok; saída conservadora de 4 ≈ 3140 tok = 87 %; o front importa por caminho relativo; mudar o teto = mudar aqui e o `max_tokens` junto). `analise-schemas.ts` importa a constante e usa `ranked_candidates.max(COMPARATIVO_MAX_CANDIDATOS)`.

Passo 3 — escrever `supabase/migrations/20260922000005_p49_comparativo_max_tokens.sql` no molde exato de `20260906000003`: `DO` que lê `max_tokens` da linha ativa de `comparative_ranking` e aborta se ≠ 3000, `UPDATE … SET max_tokens = 3600 WHERE call_type='comparative_ranking' AND is_active`, pós-condição; cabeçalho com a conta do D-59 e a observação de que o `interview_guide` NÃO é tocado (P1, §Deferred). Aplicar com `node p46apply.cjs migrate`.

Passo 4 — EF: validar `ids.length` contra as duas constantes, com a mensagem «Selecione de 2 a 4 candidatos para comparar.» montada DELAS; depois do `callAi`, o INSERT em `comparativo_solicitado` passa a gravar `provedor_ia` (só quando `result.provider` é `anthropic` ou `openai`; senão NULL) e `modelo_ia: result.model`, destruturando `{ error }` e lançando se houver (o `catch` externo devolve 500); ordenar por `score_match DESC` com desempate por `candidatura_id` e montar `posicoes` (`C<n>` → `candidatura_id`) a partir da MESMA lista `ordered` que gera o prompt (sem mudar o formato `Candidato C<n> (id=<id>)`); responder `{ ok: true, ranking, posicoes, provedor_ia, modelo_ia, fallback_cause: result.fallback_cause ?? null, latencia_ms }`. Testes: > 4 ⇒ 400 sem chamada de IA; resposta com `posicoes` na ordem do prompt e com `modelo_ia` = o `model` do mock; empate de `score_match` ⇒ ordem por `candidatura_id`; INSERT com erro ⇒ 500; o prompt enviado contém `(id=`. Rodar `deno test --allow-all supabase/functions/comparativo-candidatos/`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260922000005_p49_comparativo_max_tokens.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');for(const t of ['comparative_ranking','3000','3600','RAISE EXCEPTION','is_active'])if(!c.includes(t))throw new Error('falta: '+t);if(/interview_guide/.test(c))throw new Error('interview_guide nao e tocado nesta fase (P1)');const k=fs.readFileSync('supabase/functions/_shared/comparativo-config.ts','utf8');if(/^\s*import\s/m.test(k))throw new Error('comparativo-config.ts tem import');if(!/COMPARATIVO_MAX_CANDIDATOS\s*=\s*4/.test(k))throw new Error('teto nao e 4');if(!/COMPARATIVO_MAX_CANDIDATOS/.test(fs.readFileSync('supabase/functions/_shared/analise-schemas.ts','utf8')))throw new Error('schema nao usa a constante');console.log('OK')" && node p46apply.cjs migrate supabase/migrations/20260922000005_p49_comparativo_max_tokens.sql</automated>
    <fails_when>non-zero exit with "falta:", "tem import", "teto nao e 4", "schema nao usa a constante" or "interview_guide nao e tocado"; or the migrate output lacks "md5 do ledger BATE"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs sql "SET TRANSACTION READ ONLY; select max_tokens from public.prompt_versions where call_type='comparative_ranking' and is_active and not is_canary" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);if(r.length!==1||r[0].max_tokens!==3600){console.error('comparative_ranking ativo: '+JSON.stringify(r));process.exit(1)}console.log('max_tokens 3600')})"</automated>
    <fails_when>non-zero exit with "comparative_ranking ativo:" (not exactly one active row, or max_tokens different from 3600)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/comparativo-candidatos/</automated>
    <fails_when>non-zero exit, or "FAILED" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `comparativo-config.ts` existe sem imports, com o teto 4; o schema e a EF usam a constante.
    - Em PROD, `max_tokens` do `comparative_ranking` ativo = 3600 (migration com guarda aplicada).
    - Os testes novos (teto, `posicoes`, desempate, `modelo_ia`, erro do INSERT, `(id=` no prompt) passam.
  </acceptance_criteria>
  <done>O comparativo pede só o que cabe no tempo do Sonnet, e quando responde diz qual modelo respondeu e quem é cada posição.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 [BLOCKING]: IDOR, encerrada e sem análise com mensagens verdadeiras — e o deploy</name>
  <files>supabase/functions/comparativo-candidatos/index.ts, supabase/functions/comparativo-candidatos/__tests__/index.test.ts</files>
  <read_first>
    - o estado deixado pela Task 1
    - `supabase/functions/_shared/candidaturaEncerrada.ts` (plano 49-03)
    - `supabase/functions/comparativo-candidatos/__tests__/index.test.ts` (`:191-259` — C1 403, TRIAGEM-03 teto, MIXED_VAGA)
    - `src/features/triagem/services/triagemService.ts` (`:299-345` — `invokeComparativo` e o mapeamento de `MIXED_VAGA`, que o 49-13 atualiza)
    - `efdeploy.cjs` (`VERIFY_JWT['comparativo-candidatos'] = true`)
  </read_first>
  <behavior>
    - RH dono da vaga A pede ids em que uma candidatura é da vaga B ⇒ 403 `FORBIDDEN` «Acesso negado.», 0 leituras de análise, 0 chamadas de IA.
    - id inexistente ⇒ 403 genérico (sem oráculo de existência).
    - uma candidatura `inscricao/rejeitado` (knockout) ⇒ 400 `ENCERRADA`, 0 chamadas de IA.
    - uma candidatura `triagem/em_analise` com `encerrada_a_pedido_em` preenchida (retirada) ⇒ NÃO é recusada por encerrada.
    - todas elegíveis mas uma sem linha em `analise_candidato_vaga` ⇒ 400 `SEM_ANALISE` com `candidaturas_sem_analise` = [o id], 0 chamadas de IA.
    - ids repetidos ⇒ 400 `VALIDATION`.
  </behavior>
  <action>
Na EF, logo depois da posse da vaga (`:180-194`) e ANTES de ler as análises: ler `candidaturas` com a allowlist `id, vaga_id, etapa_atual, status` para os ids (erro destruturado, 500 em falha, no idioma de `:198-205`); ids repetidos ⇒ `VALIDATION`; alguma ausente ou com `vaga_id ≠ body.vaga_id` ⇒ `errorResponse("FORBIDDEN", "Acesso negado.", 403)` (comentário JORN-32: a posse foi checada em `body.vaga_id`, então TODA candidatura tem de ser dela; genérico de propósito); alguma com `candidaturaEncerrada(etapa_atual, status)` ⇒ `errorResponse("ENCERRADA", "Candidatura encerrada não entra no comparativo.", 400)`. Depois das análises: alguma ausente ⇒ 400 `SEM_ANALISE` «Ainda não há análise para N candidato(s) selecionado(s).» com os ids faltantes no corpo; o antigo `MIXED_VAGA` só sobra se uma análise tiver `vaga_id` diferente (defesa em profundidade) — conferir com a Task 1. Mudar DE PROPÓSITO os testes `:191-259` com comentário de proveniência e acrescentar os do `<behavior>` (o mock ganha a tabela `candidaturas`).

Deploy (D-52, Pitfall 8): conferir no catálogo, só leitura, que as colunas de proveniência de `comparativo_solicitado` existem; `node efdeploy.cjs comparativo-candidatos --dry-run` e conferir À MÃO a lista (tem de conter `functions/_shared/comparativo-config.ts`, `functions/_shared/candidaturaEncerrada.ts`, `functions/_shared/ai-error-codes.ts`, `functions/_shared/ai-client.ts`, `functions/_shared/audit-logger.ts`, e nenhum arquivo a menos que a versão viva — registrar no SUMMARY); `node efdeploy.cjs comparativo-candidatos` com `verify_jwt=true`; marcador `ENCERRADA` no bundle vivo. Commitar e `git push origin main` (o front ainda é o antigo: o campo `posicoes` é aditivo e não quebra o bundle publicado; a troca do rótulo e da seleção é do 49-13, D-55).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/comparativo-candidatos/ supabase/functions/_shared/__tests__/structured-output-compat.test.ts</automated>
    <fails_when>non-zero exit, or "FAILED" in the output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs comparativo-candidatos --dry-run | tee /dev/stderr | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const need=['functions/_shared/comparativo-config.ts','functions/_shared/candidaturaEncerrada.ts','functions/_shared/ai-error-codes.ts','functions/_shared/ai-client.ts','functions/_shared/audit-logger.ts'];const f=need.filter(n=>!s.includes(n));if(f.length){console.error('fechamento sem: '+f.join(', '));process.exit(1)}console.log('fechamento ok')})"</automated>
    <fails_when>non-zero exit with "fechamento sem:" (the deploy closure misses a _shared file the EF now imports)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs comparativo-candidatos | tee /dev/stderr | grep -q "efdeploy: OK .*verify_jwt=true" && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/comparativo-candidatos/body" | grep -a -c "SEM_ANALISE" && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>non-zero exit; efdeploy output lacks "efdeploy: OK" with "verify_jwt=true"; the grep count printed is 0 (new code not in the live bundle); or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - Os 6 casos do `<behavior>` têm teste e passam; os testes `:191-259` antigos mudaram com comentário.
    - O dry-run lista os 5 arquivos `_shared` exigidos (registrado no SUMMARY).
    - A EF está deployada com `verify_jwt=true` e o bundle vivo contém `SEM_ANALISE`; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>Nenhum RH compara candidatos de vaga alheia, nenhuma encerrada entra no ranking, e a mensagem de recusa diz o motivo verdadeiro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do RH (JWT) → EF `comparativo-candidatos` | o corpo (`vaga_id`, `candidatura_ids`) é não confiável |
| EF (service_role) → `analise_candidato_vaga`, `candidaturas` | a EF lê ignorando RLS; a posse é dela |
| EF → Anthropic/OpenAI | o input carrega `resumo_cv` de vários titulares |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-08-01 | Information Disclosure | IDOR: análises de outra vaga via `candidatura_ids` | high | mitigate | toda candidatura tem de ser de `body.vaga_id`; 403 genérico; teste |
| T-49-08-02 | Information Disclosure | oráculo de existência pela mensagem de erro | medium | mitigate | inexistente e alheia recebem o MESMO 403 |
| T-49-08-03 | Repudiation | ranking de fallback sem proveniência / auditoria perdida | high | mitigate | `provedor_ia`/`modelo_ia` gravados com erro checado; 500 se a auditoria falhar |
| T-49-08-04 | Denial of Service | saída acima do tempo (truncamento → fallback) | medium | mitigate | teto 4 + `max_tokens` 3600 (D-59); prova n=4 no 49-18 |
| T-49-08-05 | Tampering | rótulo C{n} trocado por posição | medium | mitigate | desempate estável + `posicoes` na resposta |
| T-49-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `max_tokens` 3600 em PROD; `deno test` da EF e do `structured-output-compat` verdes.
- EF deployada com `verify_jwt=true`; fechamento conferido; marcador no bundle; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
O comparativo só ranqueia candidaturas da vaga do RH, em andamento, com análise — dentro do tempo do modelo configurado, e dizendo qual modelo respondeu.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `supabase/functions/_shared/comparativo-config.ts` (`COMPARATIVO_MIN_CANDIDATOS`, `COMPARATIVO_MAX_CANDIDATOS`) | constante sem imports | **nova** |
| `ComparativeRankingSchema.ranked_candidates.max(COMPARATIVO_MAX_CANDIDATOS)` | schema | editado |
| `prompt_versions.max_tokens` (`comparative_ranking` ativo) = 3600 | configuração viva | editada |
| `supabase/migrations/20260922000005_p49_comparativo_max_tokens.sql` | migration | **nova** |
| códigos de erro `ENCERRADA`, `SEM_ANALISE` (+ `candidaturas_sem_analise`) | contrato da EF | **novos** |
| resposta `posicoes`, `provedor_ia`, `modelo_ia`, `fallback_cause` | contrato da EF | **novos** |
| EF `comparativo-candidatos` | deploy | nova versão |

## Premissas sinalizadas (probe de borda)

JORN-32, JORN-25 e JORN-28 saíram `unclassified` no probe e ficam `unresolved` no relatório. A premissa A3 do
RESEARCH (P ≈ 280–410 tok/candidato, F ≈ 1260–1500) sustenta o teto de 4 e é MEDIDA pela prova n=4 do
plano 49-18: se a saída passar de 3140 tok, o teto volta ao operador antes de fechar a fase (D-59).

Contato registrado, não consertado: o bloco de contexto `Vaga: <uuid>` do prompt (varredura C7 #3) é escopo
deliberado — o comparativo não tem rubrica; anotado.

**Ordem de apply (revisão do checker, B1):** esta migration é a `20260922000005` e vai a PROD na onda 2, antes da
`…000006` do 49-07 (onda 3) — entre ondas, a ordem de apply é a ordem de versão. Na mesma onda, as `…000003`/
`…000004` do 49-06 tocam objetos disjuntos (as funções de etapa e decisão × a linha `comparative_ranking` de
`prompt_versions`; dono único), então a ordem relativa dentro da onda não muda o estado final.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-08-SUMMARY.md` when done
</output>
