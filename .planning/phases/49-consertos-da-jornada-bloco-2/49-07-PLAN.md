---
phase: 49-consertos-da-jornada-bloco-2
plan: 07
type: execute
wave: 3
depends_on: ["49-06"]
files_modified:
  - supabase/migrations/20260922000006_p49_snapshot_so_com_mudanca.sql
  - supabase/tests/p49_snapshot_smoke.sql
autonomous: true
requirements: [JORN-3b]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-44: `trg_decisao_final_snapshot` passa a ser `AFTER UPDATE … FOR EACH ROW WHEN ((to_jsonb(OLD) - 'explicacao_solicitada_em' - 'alerta_prazo_enviado_em') IS DISTINCT FROM (to_jsonb(NEW) - 'explicacao_solicitada_em' - 'alerta_prazo_enviado_em'))` — arquiva quando muda qualquer coluna, EXCETO o carimbo de leitura e a telemetria do alerta; um UPDATE que não muda nada nunca arquiva. A comparação por `to_jsonb` cobre coluna nova por construção."
    - "BORDA explícita (probe JORN-3b/idempotency): recarregar a explicação N vezes (N chamadas de `stamp_explicacao_acessada`) gera ZERO linha nova em `decisao_final_historico`, e `explicacao_solicitada_em` guarda o PRIMEIRO carimbo."
    - { statement: "BORDA (probe JORN-3b/concurrency): duas leituras simultâneas da explicação gravam o carimbo no máximo uma vez (UPDATE `… AND explicacao_solicitada_em IS NULL`; a segunda espera o lock da linha e não casa) e nenhuma arquiva (a coluna está fora do WHEN).", verification: backstop }
    - "`stamp_explicacao_acessada` fica idempotente: `UPDATE … WHERE candidatura_id = p AND explicacao_solicitada_em IS NULL` seguido de re-SELECT da linha (idioma de `solicitar_revisao_decisao`) — na 2ª chamada o retorno continua sendo a linha, nunca NULL (RESEARCH Correção 30). O guard own-row e o ACL ficam; `anon` entra no REVOKE."
    - "Continuam arquivando (e o smoke prova cada um): a decisão (`registrar_decisao`, que muda `em`), o ciclo de revisão (`solicitar_revisao_decisao`, `responder_revisao_decisao`), a reabertura e o tombstone do motor de exclusão (muda `justificativa`) — a ordem snapshot → raspagem do `anonimizar_candidato` continua valendo (Pitfall 5)."
    - "Um teste reprova quando `decisao_final` e `decisao_final_historico` divergirem de colunas: `colunas(decisao_final) − {id, em} ∪ {decidido_em}` = `colunas(decisao_final_historico) − {id, arquivado_em}` = a lista do INSERT de `snapshot_decisao_final()`, lidas do catálogo e do corpo vivo NA EXECUÇÃO — coluna nova em `decisao_final` sem entrar no arquivo passa a reprovar em vez de sumir em silêncio."
    - "D-45: os 5 snapshots sem mudança que já existem (todos de teste) FICAM — nenhuma escrita retroativa no arquivo (ERASE-08)."
    - "`p48_reabertura_smoke.sql` (os casos (a)(g)(h) dependem do snapshot no ciclo) e `p45_motor_exclusao_smoke.sql` (tombstone) seguem verdes depois do apply; a prova antiga `p48_prova_prod.sql` rodada só leitura com o T0 da 48 mantém `p2_ciclo_zerado_e_arquivado` e `p2_d23` verdadeiros."
    - "`snapshot_decisao_final()` NÃO muda nesta migration (lista explícita de 14 colunas)."
  artifacts:
    - path: supabase/migrations/20260922000006_p49_snapshot_so_com_mudanca.sql
      provides: "trigger com WHEN por to_jsonb + stamp idempotente"
      contains: "to_jsonb(OLD)"
    - path: supabase/tests/p49_snapshot_smoke.sql
      provides: "leitura/no-op/telemetria não arquivam; decisão/ciclo/reabertura/tombstone arquivam; paridade de colunas"
      contains: "smoke49s.pass"
  key_links:
    - from: "trg_decisao_final_snapshot (WHEN)"
      to: "snapshot_decisao_final() → decisao_final_historico"
      via: "AFTER UPDATE condicionado à mudança real"
      pattern: "IS DISTINCT FROM"
    - from: "anonimizar_candidato (tombstone_decisao_final)"
      to: "trg_decisao_final_snapshot"
      via: "o UPDATE de justificativa continua arquivando OLD, e o scrub do arquivo vem depois"
      pattern: "tombstone_decisao_final"
  prohibitions:
    - requirement_id: JORN-3b
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT suprimir o snapshot de nenhuma mudança real — decisão, ciclo de revisão, reabertura e tombstone continuam arquivando"
    - requirement_id: JORN-3b
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT apagar os snapshots sem mudança já existentes — o arquivo só aceita acréscimo (D-45, ERASE-08)"
---

<objective>
Ler a explicação — ou qualquer UPDATE que não muda nada — deixa de versionar a decisão, sem que nenhuma
mudança real deixe de ser arquivada.

Purpose: JORN-3b/D-44. Medido: 11 snapshots em 3 candidaturas, 5 sem mudança e 1 só de carimbo de leitura;
o gatilho é `stamp_explicacao_acessada` fazer UPDATE sem filtro `IS NULL`, somado a um trigger AFTER UPDATE
sem `WHEN` (RESEARCH Correção 12, varredura C3). Depende do 49-06 porque o smoke da reabertura, que este plano
re-roda, foi ajustado lá.

Output: uma migration (trigger com `WHEN` + stamp idempotente), o smoke novo, e a regressão dos smokes do ciclo.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer) [BLOCKING]: o trigger com WHEN e o stamp idempotente — ensaiados, aplicados, e N leituras não arquivam nada</name>
  <files>supabase/migrations/20260922000006_p49_snapshot_so_com_mudanca.sql, supabase/tests/p49_snapshot_smoke.sql</files>
  <read_first>
    - O VIVO, só leitura: `pg_get_triggerdef` de `trg_decisao_final_snapshot`; `pg_get_functiondef` + `md5(prosrc)` de `snapshot_decisao_final` e `stamp_explicacao_acessada`; a lista de TODOS os triggers de `public.decisao_final` (`select tgname, pg_get_triggerdef(oid) from pg_trigger where tgrelid='public.decisao_final'::regclass and not tgisinternal`) e as colunas de `decisao_final` e `decisao_final_historico` (`information_schema.columns`)
    - `supabase/migrations/20260921000003_p48_local_ou_link_valida.sql` (`:101-142` — DROP/CREATE TRIGGER com WHEN + portão de catálogo)
    - `supabase/migrations/20260625100001*` (a definição de `stamp_explicacao_acessada` `:238-270` e o idioma `UPDATE … IS NULL` + re-SELECT de `solicitar_revisao_decisao`)
    - `supabase/migrations/20260921000011_p48_reabertura_colunas_e_resposta.sql` (`:167-185` — `snapshot_decisao_final` vivo, 14 colunas)
    - `supabase/tests/p48_reabertura_smoke.sql` (o molde de smoke que escreve e reverte; ajustado no 49-06)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md` §5, §10
  </read_first>
  <action>
Passo 1 — medir antes (D-49, D-51): contagem de `decisao_final_historico` (11 na pesquisa) e o número de snapshots «sem mudança» (5) — registrar no SUMMARY; e conferir na lista de triggers de `decisao_final` que NENHUM trigger BEFORE UPDATE carimba uma coluna de timestamp em todo UPDATE. Se existir um (ex.: `updated_at`), um UPDATE sem mudança ainda arquivaria — o D-44 nomeia só duas exceções: PARAR e reportar ao operador (D-14), não acrescentar a coluna à exclusão por conta própria. Rodar a varredura C3 do `49-VARREDURA-KICKOFF.md` e a de portões do `CLAUDE.md`; classificar no SUMMARY os achados que citem `decisao_final_historico` ou os dois objetos.

Passo 2 — escrever `supabase/migrations/20260922000006_p49_snapshot_so_com_mudanca.sql` (cabeçalho do M8, com a medição e o Pitfall 5 — excluir SÓ as duas colunas do D-44 —, nota de transporte, sem `BEGIN;`): pré-portão do `md5(prosrc)` de `stamp_explicacao_acessada` e de `snapshot_decisao_final` (esta última só conferida, não redefinida); `DROP TRIGGER IF EXISTS trg_decisao_final_snapshot ON public.decisao_final;` + `CREATE TRIGGER trg_decisao_final_snapshot AFTER UPDATE ON public.decisao_final FOR EACH ROW WHEN (<a comparação por to_jsonb sem as duas colunas>) EXECUTE FUNCTION public.snapshot_decisao_final();`; `CREATE OR REPLACE` de `stamp_explicacao_acessada` com o guard own-row intacto, o UPDATE com `AND explicacao_solicitada_em IS NULL` e o re-SELECT da linha para o retorno; ACL `REVOKE ALL … FROM PUBLIC, anon` + `GRANT EXECUTE … TO authenticated`; pós-portão: `pg_get_triggerdef` contém `to_jsonb(old` (comparação em minúsculas — o catálogo normaliza), contém as duas colunas excluídas e NÃO contém `justificativa`/`decisao` na exclusão; `tgenabled = 'O'`; o corpo de `stamp_explicacao_acessada` contém `explicacao_solicitada_em IS NULL`; `md5` de `snapshot_decisao_final` igual ao do pré-portão; `RAISE NOTICE` com os md5.

Passo 3 — criar `supabase/tests/p49_snapshot_smoke.sql` (molde do `p48_reabertura_smoke.sql`: contador `smoke49s.pass`, SQLSTATE `P49S1`, fixture sintética `@invalido.local`, atores vivos lidos na execução, julgamento fora da subtransação). Nesta task: (a) com o JWT do titular sintético, 5 chamadas de `stamp_explicacao_acessada` ⇒ 0 linhas novas no arquivo e `explicacao_solicitada_em` = o valor da 1ª chamada; (a2) a 2ª chamada devolve a linha (não NULL); (b) `UPDATE decisao_final SET justificativa = justificativa` ⇒ 0 linhas novas; (c) UPDATE só de `alerta_prazo_enviado_em` ⇒ 0; (d) mudança real de `justificativa` (a forma do tombstone) ⇒ 1 linha nova com o valor ANTIGO; (z) negativa de resíduo zero. Gate com o número exato.

Passo 4 — ensaio (migration + smoke + `DO` que levanta `ENSAIO_OK`, numa requisição que aborta), depois `node p46apply.cjs migrate …` e `node p46apply.cjs run supabase/tests/p49_snapshot_smoke.sql`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260922000006_p49_snapshot_so_com_mudanca.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');for(const t of ['DROP TRIGGER IF EXISTS trg_decisao_final_snapshot','CREATE TRIGGER trg_decisao_final_snapshot','AFTER UPDATE','to_jsonb(OLD)','to_jsonb(NEW)',\"'explicacao_solicitada_em'\",\"'alerta_prazo_enviado_em'\",'IS DISTINCT FROM','CREATE OR REPLACE FUNCTION public.stamp_explicacao_acessada','explicacao_solicitada_em IS NULL','md5'])if(!c.includes(t))throw new Error('falta: '+t);if(/CREATE\s+OR\s+REPLACE\s+FUNCTION\s+public\.snapshot_decisao_final/i.test(c))throw new Error('snapshot_decisao_final nao pode ser redefinida aqui');const w=(c.match(/WHEN\s*\(([\s\S]*?)EXECUTE FUNCTION/)||[])[1]||'';if(/'justificativa'|'decisao'|'em'/.test(w))throw new Error('o WHEN exclui coluna de mudanca real (Pitfall 5)');console.log('OK')"</automated>
    <fails_when>non-zero exit with "falta:", "wrapper BEGIN proibido", "nao pode ser redefinida" or "o WHEN exclui coluna de mudanca real" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && D=$(mktemp -d) && { cat supabase/migrations/20260922000006_p49_snapshot_so_com_mudanca.sql supabase/tests/p49_snapshot_smoke.sql; printf '\nDO $e$ BEGIN RAISE EXCEPTION %s; END $e$;\n' "'ENSAIO_OK'"; } > "$D/ensaio.sql" && node p46apply.cjs run "$D/ensaio.sql" 2>&1 | tee "$D/out.txt" | tail -3; grep -q "ENSAIO_OK" "$D/out.txt" && ! grep -q "FAIL" "$D/out.txt"</automated>
    <fails_when>the output lacks "ENSAIO_OK" or contains "FAIL"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs migrate supabase/migrations/20260922000006_p49_snapshot_so_com_mudanca.sql && node p46apply.cjs run supabase/tests/p49_snapshot_smoke.sql</automated>
    <fails_when>non-zero exit; "md5 do ledger BATE" absent from the migrate output; or "FAIL" in the smoke output</fails_when>
  </verify>
  <acceptance_criteria>
    - Em PROD, `pg_get_triggerdef` de `trg_decisao_final_snapshot` contém a comparação por `to_jsonb` sem as duas colunas; `stamp_explicacao_acessada` filtra `IS NULL` e re-seleciona.
    - O smoke prova 0 snapshot em 5 leituras, em no-op e em telemetria, e 1 snapshot numa mudança real.
    - `select count(*) from public.decisao_final_historico` depois = o medido no Passo 1 (nenhuma linha apagada; D-45).
  </acceptance_criteria>
  <done>Abrir a página da explicação deixa de criar versão da decisão.</done>
</task>

<task type="auto">
  <name>Task 2: o que tem de continuar arquivando, a paridade de colunas — e a regressão do ciclo e do motor</name>
  <files>supabase/tests/p49_snapshot_smoke.sql</files>
  <read_first>
    - o estado deixado pela Task 1
    - o corpo vivo de `snapshot_decisao_final()` (a lista de 14 colunas do INSERT e o mapeamento `OLD.em → decidido_em`)
    - `supabase/tests/p45_motor_exclusao_smoke.sql` (as asserções do tombstone sobre `decisao_final_historico`)
    - `supabase/tests/p48_prova_prod.sql` (`:85-100`, `:195-212` — `p2_ciclo_zerado_e_arquivado`, `p2_d23`) e `.planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md` (o T0 da 48, `grep -m1 "^T0: "`)
  </read_first>
  <action>
Acrescentar ao `p49_snapshot_smoke.sql`: (e) `registrar_decisao` (JWT de RH/admin vivo) sobre fixture em `decisao_final/em_analise` com linha em `decisao_final` ⇒ 1 snapshot; (f) `solicitar_revisao_decisao` (JWT do titular) ⇒ 1 snapshot; `responder_revisao_decisao` `mantida` (revisor ≠ decisor) ⇒ 1 snapshot; (g) PARIDADE, lida na execução: o conjunto de colunas de `decisao_final` menos {`id`, `em`} mais {`decidido_em`} é igual ao de `decisao_final_historico` menos {`id`, `arquivado_em`}, e igual à lista de colunas do INSERT extraída do `prosrc` vivo de `snapshot_decisao_final` — reprova nomeando a coluna que falta de qual lado (é a forma que não envelhece: nenhuma lista literal de colunas no smoke). Subir o esperado do gate e registrar no cabeçalho.

Provar que a (g) morde (D-56): numa requisição que aborta, `ALTER TABLE public.decisao_final ADD COLUMN p49_sonda text` + o smoke + `DO` final `MUTACAO_TERMINOU` ⇒ esperado `FAIL (g)` citando `p49_sonda` antes do fim. Registrar a saída no SUMMARY (e conferir, só leitura, que a coluna não existe em PROD depois).

Regressão em PROD: `node p46apply.cjs run supabase/tests/p48_reabertura_smoke.sql` (13/13, os casos (a)(g)(h) do ciclo) e `node p46apply.cjs run supabase/tests/p45_motor_exclusao_smoke.sql` (tombstone ainda arquiva e a raspagem vem depois). Prova antiga, só leitura: rodar `p48_prova_prod.sql` prefixado com `SET TRANSACTION READ ONLY; SELECT set_config('p48.t0', '<T0 da 48>', false);` e conferir `p2_ciclo_zerado_e_arquivado` e `p2_d23` = true (os snapshots que as sustentam foram gravados antes e ficam — D-45).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs run supabase/tests/p49_snapshot_smoke.sql && node p46apply.cjs run supabase/tests/p48_reabertura_smoke.sql && node p46apply.cjs run supabase/tests/p45_motor_exclusao_smoke.sql</automated>
    <fails_when>non-zero exit, or "FAIL" in any of the three outputs</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && D=$(mktemp -d) && { echo "ALTER TABLE public.decisao_final ADD COLUMN p49_sonda text;"; cat supabase/tests/p49_snapshot_smoke.sql; printf '\nDO $e$ BEGIN RAISE EXCEPTION %s; END $e$;\n' "'MUTACAO_TERMINOU'"; } > "$D/mut.sql" && node p46apply.cjs run "$D/mut.sql" 2>&1 | tee "$D/out.txt" | tail -3; grep -q "FAIL (g)" "$D/out.txt" && ! grep -q "MUTACAO_TERMINOU" "$D/out.txt"</automated>
    <fails_when>the output lacks "FAIL (g)" or reaches "MUTACAO_TERMINOU" (the parity assertion does not bite when a column is added)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && T0=$(grep -m1 "^T0: " .planning/phases/48-consertos-da-jornada-bloco-1/48-PROVA-PROD.md | cut -d' ' -f2) && D=$(mktemp -d) && { echo "SET TRANSACTION READ ONLY; SELECT set_config('p48.t0', '$T0', false);"; cat supabase/tests/p48_prova_prod.sql; } > "$D/prova.sql" && node p46apply.cjs run "$D/prova.sql" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s.slice(s.indexOf('[')))[0];const f=['p2_ciclo_zerado_e_arquivado','p2_d23'].filter(k=>r[k]!==true);if(f.length){console.error('prova da 48 regrediu: '+f.join(', '));process.exit(1)}console.log('prova da 48 intacta')})"</automated>
    <fails_when>non-zero exit with "prova da 48 regrediu" in stderr</fails_when>
  </verify>
  <acceptance_criteria>
    - O smoke prova 1 snapshot por decisão, pedido de revisão e resposta; a paridade (g) passa e MORDE na mutação (saída no SUMMARY).
    - `p48_reabertura_smoke` e `p45_motor_exclusao_smoke` verdes depois do apply.
    - `p2_ciclo_zerado_e_arquivado` e `p2_d23` seguem `true`.
  </acceptance_criteria>
  <done>O arquivo da decisão volta a ter uma versão por mudança real — e deixa de perder, em silêncio, a coluna que alguém acrescentar depois.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| titular (JWT) → `stamp_explicacao_acessada` | a leitura da explicação escreve um carimbo |
| qualquer UPDATE em `decisao_final` → `decisao_final_historico` | o arquivo vai na cópia do titular |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-07-01 | Repudiation | mudança real sem snapshot (WHEN largo demais) | high | mitigate | exclusão restrita às 2 colunas do D-44; smoke (d)(e)(f); verify reprova `justificativa`/`decisao`/`em` no WHEN |
| T-49-07-02 | Tampering | coluna nova em `decisao_final` sumindo do arquivo | medium | mitigate | asserção (g) de paridade lida do catálogo, provada por mutação |
| T-49-07-03 | Information Disclosure | tombstone deixando de arquivar e quebrando a ordem snapshot → raspagem | high | mitigate | `p45_motor_exclusao_smoke` re-rodado |
| T-49-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Migration aplicada (ensaio antes), md5 do ledger conferido.
- `p49_snapshot_smoke`, `p48_reabertura_smoke`, `p45_motor_exclusao_smoke` verdes; mutação de coluna reprova (g); prova da 48 intacta.
</verification>

<success_criteria>
O histórico da decisão que o titular recebe tem uma linha por mudança real — nem mais (leitura), nem menos (coluna nova).
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `trg_decisao_final_snapshot` (com `WHEN` por `to_jsonb`) | trigger | recriado |
| `public.stamp_explicacao_acessada(uuid)` | RPC | editada (idempotente + re-SELECT; `anon` no REVOKE) |
| `supabase/migrations/20260922000006_p49_snapshot_so_com_mudanca.sql` | migration | **nova** |
| `supabase/tests/p49_snapshot_smoke.sql` (`smoke49s.pass`, SQLSTATE `P49S1`) | smoke | **novo** |

## Premissas sinalizadas (probe de borda)

As duas sondas do probe para JORN-3b foram resolvidas: `idempotency` como verdade explícita (N leituras ⇒ 0
snapshot, no smoke); `concurrency` como marcador `backstop` (a garantia vem do `UPDATE … IS NULL` sob lock de
linha e do WHEN; não há teste de concorrência real). O front (`useExplicacao.ts:57-77`, guarda por `useRef`
que zera a cada recarga) não muda: com o banco idempotente, a recarga deixa de ter efeito.

**Ordem de apply (revisão do checker, B1):** esta migration é a `20260922000006` e vai a PROD na onda 3, depois
da `…000005` do 49-08 (onda 2) — entre ondas, a ordem de apply é a ordem de versão. Na mesma onda, as `…000007`/
`…000008` do 49-10 tocam objetos disjuntos (as RPCs de `entrevista_analises` × `trg_decisao_final_snapshot` e
`stamp_explicacao_acessada`; dono único), então a ordem relativa dentro da onda não muda o estado final.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-07-SUMMARY.md` when done
</output>
