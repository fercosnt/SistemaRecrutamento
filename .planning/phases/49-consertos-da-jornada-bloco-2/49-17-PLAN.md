---
phase: 49-consertos-da-jornada-bloco-2
plan: 17
type: execute
wave: 7
depends_on: ["49-01", "49-14", "49-20", "49-21"]
files_modified:
  - docs/compliance/catalogo-vivo-44.json
  - docs/compliance/export-scope-rules.yaml
  - docs/compliance/pii-inventory.yaml
  - docs/compliance/pii-inventory.md
  - docs/compliance/sql/gen-recibo-exclusao.cjs
  - docs/compliance/recibo-exclusao.json
  - supabase/functions/_shared/reciboExclusao.ts
  - src/features/privacidade/constants/reciboExclusao.generated.ts
  - docs/compliance/export-allowlist.json
  - supabase/functions/_shared/exportAllowlist.ts
  - docs/compliance/sql/05-export-allowlist-drift.sql
  - docs/compliance/__tests__/exportAllowlist.test.ts
  - database.types.ts
autonomous: false
requirements: [JORN-28, JORN-12, JORN-07]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-57: TODA coluna que esta fase criou em tabela inventariada tem o checklist completo — medida no catálogo, veredito de export ESCRITO com razão, classificação no inventário, linha ou razão no recibo, artefatos regenerados pelos geradores, portões verdes e as duas EFs que embarcam os artefatos redeployadas: as 16 do plano 49-01 (`provedor_ia`/`modelo_ia` nas 5 tabelas; `redacoes_candidato.rubrica_versao`; `entrevista_analises.tipo`, `solicitado_por`, `texto_hash`, `ai_call_log_id`, `superada_em`). Esquecimento nunca é razão."
    - "Vereditos de export (decisão do planejador, revisável pelo operador, seguindo os precedentes do próprio arquivo): `provedor_ia`, `modelo_ia` e `rubrica_versao` ⇒ `export: false` — a família de `devolutivas_candidato.modelo_ia` e `redacoes_candidato.model_version`/`prompt_version` (telemetria interna: «o RESULTADO entregue à pessoa entra; a ficha técnica de como ele foi produzido, não», decisão travada na 44-CONTEXT §Área 2); `entrevista_analises.tipo` e `superada_em` ⇒ `export: true` (estado/contexto da análise sobre a pessoa, como `descartada_em` e `status_analise`); `texto_hash` e `ai_call_log_id` ⇒ `export: false` (chave técnica/ponteiro para tabela fora do escopo, como `input_hash`); `solicitado_por` ⇒ fora da cópia por construção (nome já em `ponteiros.de_terceiro` e na R2), «dado_de_funcionario» no recibo. As colunas de `comparativo_solicitado` (tabela `pii_de_terceiro`) e de `entrevista_guias` (`configuracao_do_produto`) seguem o destino da tabela."
    - "O UUID de funcionário (`entrevista_analises.solicitado_por`) NÃO aparece no artefato `export-allowlist.json` para `entrevista_analises` — conferido por asserção."
    - "D-66: `analise_candidato_vaga` e `entrevista_guias` saem de `tabelas_sem_pii_titular` no inventário (medido: 13/24 análises e 2/5 guias contêm o primeiro nome do titular) — e o texto do recibo que passar a cobri-las é decidido pelo operador num checkpoint, porque o item candidato (`avaliacoes_e_analises`) promete «sem ligação com você» e a desidentificação de `resumo_cv`/`guia` é §Deferred (D-14: a classificação foi decidida, a promessa ao titular não)."
    - "`catalogo-vivo-44.json` recebe SÓ as colunas desta fase, MEDIDAS em PROD na execução, num item novo de `meta.acrescimos` (`fase: 49`, data, query) — o drift pré-existente (9 colunas, deferred da 48) fica fora e é registrado."
    - "A `versao` do `export-allowlist` sobe de `1.2.0` para `1.3.0` (o conjunto de colunas exportadas muda: `tipo` e `superada_em`); os dois blocos `VALUES` do `05-export-allowlist-drift.sql` são REGENERADOS pelo gerador (`--sql-values`, `--sql-values-excluidas`) e os snapshots inline de `exportAllowlist.test.ts` mudam só pelas colunas exportadas desta fase."
    - "Os quatro `check:*` e o vitest de `docs/compliance` passam; a consulta de drift contra PROD (só leitura) não lista nenhuma coluna desta fase."
    - "`exportar-meus-dados` e `executar-direito-titular` redeployadas com os artefatos novos (`verify_jwt=true`), e o marcador novo está no bundle vivo de cada uma — senão a decisão escrita não chega à cópia real nem ao recibo real."
    - "`database.types.ts` final regenerado com `< /dev/null`, não vazio; `tsc` ≤ 90; `origin/main..HEAD` vazio."
  artifacts:
    - path: docs/compliance/export-scope-rules.yaml
      provides: "vereditos das colunas da fase; versão 1.3.0"
      contains: "entrevista_analises.superada_em"
    - path: docs/compliance/pii-inventory.yaml
      provides: "classificação das colunas da fase; D-66"
      contains: "rubrica_versao"
    - path: docs/compliance/export-allowlist.json
      provides: "artefato regenerado, 1.3.0"
      contains: "1.3.0"
  key_links:
    - from: "export-scope-rules.yaml + pii-inventory.yaml + catalogo-vivo-44.json"
      to: "supabase/functions/_shared/exportAllowlist.ts → exportar-meus-dados"
      via: "node docs/compliance/sql/gen-export-allowlist.cjs + redeploy"
      pattern: "superada_em"
    - from: "gen-recibo-exclusao.cjs"
      to: "supabase/functions/_shared/reciboExclusao.ts → executar-direito-titular"
      via: "gerador + redeploy"
      pattern: "dado_de_funcionario"
  prohibitions:
    - requirement_id: JORN-12
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT deixar o UUID de funcionário chegar à cópia do titular"
    - requirement_id: JORN-28
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT editar à mão artefato gerado nem inventar coluna no catálogo — o catálogo é medido, os artefatos são regenerados"
    - requirement_id: JORN-07
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT escrever no recibo uma promessa ao titular que o motor não cumpre (D-66 × «sem ligação»)"
    - requirement_id: JORN-28
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT introduzir ocorrência literal nova do endereço do canal de privacidade nem a expressão «teste psicológico» em texto novo (D-07, D-58 — a única exceção é o disclaimer negado da devolutiva, já decidido)"
---

<objective>
Fechar o inventário LGPD de todas as colunas que a fase criou — veredito de export, classificação, recibo, catálogo
medido, artefatos regenerados, portões verdes e as cópias/recibos reais atualizados —, e corrigir a classificação das
duas tabelas que se diziam sem PII do titular (D-66).

Purpose: D-57 (precedente 48-17). Vem DEPOIS do motor (49-14, 49-20) e do alinhamento do recibo ao motor (49-21)
porque este plano e o 49-21 escrevem o mesmo gerador do recibo, o inventário e os dois espelhos TS (RESEARCH «dono
único»). Entre o 49-01 e aqui, a cópia exporta por allowlist e as colunas novas NÃO
saíram (fail-safe) — este plano as decide por escrito.

Output: YAMLs com os vereditos, catálogo medido, artefatos regenerados, drift conferido, EFs redeployadas, tipos finais.

Orçamento (revisão do checker, W1): NÃO dividido — o `checkpoint:decision` da Task 2 já reparte a execução em dois
agentes (o executor para no checkpoint e um agente de continuação, de contexto novo, faz a Task 3 — `execute-phase`
«spawn continuation agent, NOT resume»), então nenhum agente carrega as três tasks; e as Tasks 1 e 3 escrevem os
mesmos artefatos gerados, que só podem ter um dono por onda. Excesso de 1,10× sobre estimativa sem calibração.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/48-consertos-da-jornada-bloco-1/48-17-PLAN.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-21-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): uma tabela de ponta a ponta — `entrevista_analises` medida, decidida, regenerada, e o UUID de funcionário fora da cópia</name>
  <files>docs/compliance/catalogo-vivo-44.json, docs/compliance/export-scope-rules.yaml, docs/compliance/pii-inventory.yaml, docs/compliance/pii-inventory.md, docs/compliance/sql/gen-recibo-exclusao.cjs, docs/compliance/recibo-exclusao.json, supabase/functions/_shared/reciboExclusao.ts, src/features/privacidade/constants/reciboExclusao.generated.ts, docs/compliance/export-allowlist.json, supabase/functions/_shared/exportAllowlist.ts</files>
  <read_first>
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-17-PLAN.md` (Tasks 1–3 — a sequência e os `<verify>` que este plano copia)
    - `docs/compliance/sql/gen-export-allowlist.cjs` (`:1-60` as três fontes e o fecho; `:270-330`; `:436-470` VEREDITO ÓRFÃO)
    - `docs/compliance/catalogo-vivo-44.json` (`meta.acrescimos` — o item da fase 48 como molde)
    - `docs/compliance/export-scope-rules.yaml` (`:35` `meta.versao`; `:165-215` tabelas; `:385-400` `ponteiros.de_terceiro` com `solicitado_por`; `:515-600` vereditos vizinhos: `descartada_em`, `status_analise`, `prompt_version`, `model_version`, `input_hash`, `devolutivas_candidato.modelo_ia`)
    - `docs/compliance/pii-inventory.yaml` (`:58-66` R2; `:285-303` `entrevista_analises`/`redacoes_candidato`; `:492-520` `tabelas_sem_pii_titular`)
    - `docs/compliance/sql/gen-recibo-exclusao.cjs` (`:620-661` o mapa de razões — `dado_de_funcionario`, `chave_tecnica`, telemetria)
    - `docs/compliance/sql/01-pii-catalog.sql` (a query reprodutora do catálogo)
  </read_first>
  <action>
Medir, só leitura, as colunas vivas de `entrevista_analises` (`information_schema.columns`, no formato de `01-pii-catalog.sql`) e acrescentar ao catálogo, num item novo de `meta.acrescimos` (`fase: 49`, `plano: "49-17"`, data, a query, «colunas criadas pela Phase 49 — drift pré-existente não incluído»), SÓ as 7 colunas novas dessa tabela; não reescrever `medido_em` nem os totais. Vereditos em `decisoes_por_coluna` (razão numerada no estilo do arquivo): `entrevista_analises.tipo` `export: true` ((ii) de qual entrevista é a análise — contexto do evento de que o titular participou); `entrevista_analises.superada_em` `export: true` ((ii) marca de tratamento — mostra ao titular que a análise que ele pode ter recebido foi substituída; mesmo argumento de `descartada_em`); `entrevista_analises.texto_hash` `export: false` ((i) chave técnica — idem `input_hash`); `entrevista_analises.ai_call_log_id` `export: false` ((i) ponteiro para `ai_call_logs`, tabela fora do escopo por decisão travada); `entrevista_analises.provedor_ia`/`modelo_ia` `export: false` ((i) telemetria interna — idem `devolutivas_candidato.modelo_ia`); `solicitado_por` sem veredito próprio (sai pela R2/`ponteiros.de_terceiro` — conferir que o gerador o trata assim). Inventário: as 7 no padrão das vizinhas da tabela (`solicitado_por` como ponteiro de terceiro). Recibo: cada coluna com linha/razão no mapa (`texto_hash`/`ai_call_log_id` → `chave_tecnica`; `solicitado_por` → `dado_de_funcionario`; `provedor_ia`/`modelo_ia` → a mesma razão de `model_version`; `tipo`/`superada_em` → a razão de estado de processo usada por `descartada_em`).

Regenerar: `node docs/compliance/sql/gen-export-allowlist.cjs`, `node docs/compliance/sql/gen-pii-md.cjs`, `node docs/compliance/sql/gen-recibo-exclusao.cjs`; `check:export-allowlist`, `check:pii-inventory-md`, `check:recibo-exclusao`; e conferir no artefato que `entrevista_analises` exporta `tipo` e `superada_em` e NÃO exporta `solicitado_por`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node docs/compliance/sql/gen-export-allowlist.cjs && node docs/compliance/sql/gen-pii-md.cjs && node docs/compliance/sql/gen-recibo-exclusao.cjs && npm run -s check:export-allowlist && npm run -s check:pii-inventory-md && npm run -s check:recibo-exclusao && node -e "const a=JSON.parse(require('fs').readFileSync('docs/compliance/export-allowlist.json','utf8'));const t=(a.tabelas||{}).entrevista_analises;if(!t)throw new Error('entrevista_analises ausente de tabelas');const cols=t.colunas||[];for(const c of ['tipo','superada_em'])if(!cols.includes(c))throw new Error(c+' nao entrou na copia de entrevista_analises');for(const c of ['solicitado_por','texto_hash','ai_call_log_id','provedor_ia','modelo_ia'])if(cols.includes(c))throw new Error(c+' na copia do titular (esperado fora)');if(!/R2/.test(String((t.colunas_excluidas||{}).solicitado_por||'')))throw new Error('solicitado_por nao saiu pela R2 (UUID de funcionario)');console.log('OK')"</automated>
    <fails_when>non-zero exit; "ERRO DE FECHAMENTO" or "VEREDITO ÓRFÃO" in stderr; a check failure; or stderr naming a column that did not enter the copy, a column that entered but should not, or "solicitado_por nao saiu pela R2"</fails_when>
  </verify>
  <acceptance_criteria>
    - O catálogo tem as 7 colunas medidas num item `fase: 49` datado; nenhuma outra coluna nesta task.
    - Vereditos, classificação e linha de recibo das 7 existem com razão; artefatos regenerados; os três checks verdes.
    - O artefato exporta `tipo`/`superada_em` de `entrevista_analises` e não exporta `solicitado_por`.
  </acceptance_criteria>
  <done>A tabela mais nova da fase entra na cópia do titular exatamente como decidido — com a marca de superada e sem o UUID de quem pediu.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: D-66 — que promessa o recibo faz sobre as análises e os guias que contêm o nome do titular</name>
  <files>.planning/phases/49-consertos-da-jornada-bloco-2/49-17-SUMMARY.md</files>
  <read_first>
    - `docs/compliance/sql/gen-recibo-exclusao.cjs` (`:552-600` — item `avaliacoes_e_analises`: «Ficaram guardadas sem ligação com você…»)
    - `docs/compliance/pii-inventory.yaml` (`:492-512` — as ressalvas R5 já anotadas em `analise_candidato_vaga` e `entrevista_guias`)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md` D-66 e §Deferred («desidentificar `resumo_cv`/`guia` no motor»)
  </read_first>
  <action>Claude apresenta ao operador o fato medido (13/24 análises e 2/5 guias contêm o primeiro nome do titular; o motor NÃO os desidentifica — §Deferred) e o conflito: tirar as duas tabelas de «sem PII» (D-66, decidido) obriga o recibo a cobri-las, e o item natural promete «sem ligação com você». Nada é escrito até a resposta.</action>
  <decision>Como o recibo descreve, ao titular, as análises de triagem e os guias de entrevista que ficam depois da exclusão e podem conter o primeiro nome dele</decision>
  <context>D-66 corrigiu só a classificação; a desidentificação no motor foi para §Deferred. Escrever «sem ligação» sobre texto que contém o nome seria o mesmo defeito do JORN-36 (recibo que afirma o que o motor não faz). A escolha é reversível (texto do recibo), mas é promessa ao titular — decisão do operador (D-14).</context>
  <options>
    <option id="a">
      <name>Ressalva no item existente</name>
      <pros>Sem mudança de motor; o recibo fica verdadeiro; um item só para «avaliações e análises».</pros>
      <cons>O texto do item `avaliacoes_e_analises` muda para todos (ex.: «Ficaram guardadas, desligadas do seu cadastro; o texto das análises automáticas e dos roteiros de entrevista pode conter o seu primeiro nome»).</cons>
    </option>
    <option id="b">
      <name>Item próprio para as duas tabelas</name>
      <pros>O item atual fica como está para as demais origens; a ressalva vale só onde é verdade.</pros>
      <cons>Um item a mais no recibo e nos dois espelhos TS; mais uma frase para o titular ler.</cons>
    </option>
    <option id="c">
      <name>Trazer a desidentificação para esta fase</name>
      <pros>O recibo continua dizendo «sem ligação» e passa a ser verdade.</pros>
      <cons>Escopo novo no motor (mecanismo destrutivo, D-48 ampliado): nova migration, smoke, re-pin — hoje em §Deferred por decisão do portão.</cons>
    </option>
  </options>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && grep -n "item_id: 'avaliacoes_e_analises'" docs/compliance/sql/gen-recibo-exclusao.cjs</automated>
    <fails_when>non-zero exit (the recibo item the decision is about is not where the plan says — re-read before presenting)</fails_when>
  </verify>
  <acceptance_criteria>
    - A opção escolhida (a, b ou c), com a frase aprovada quando for (a) ou (b), está registrada no SUMMARY. Opção (c) ⇒ este plano PARA e reporta ao orquestrador (é escopo novo de motor, não cabe aqui).
  </acceptance_criteria>
  <resume-signal>Responda «a», «b» (com a frase, se quiser ajustá-la) ou «c»</resume-signal>
  <done>A promessa do recibo sobre as análises e os guias é decidida pelo operador, com o fato medido na mesa.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: as demais tabelas, D-66 conforme a decisão, versão 1.3.0, drift contra PROD, redeploys e tipos finais</name>
  <files>docs/compliance/catalogo-vivo-44.json, docs/compliance/export-scope-rules.yaml, docs/compliance/pii-inventory.yaml, docs/compliance/pii-inventory.md, docs/compliance/sql/gen-recibo-exclusao.cjs, docs/compliance/recibo-exclusao.json, supabase/functions/_shared/reciboExclusao.ts, src/features/privacidade/constants/reciboExclusao.generated.ts, docs/compliance/export-allowlist.json, supabase/functions/_shared/exportAllowlist.ts, docs/compliance/sql/05-export-allowlist-drift.sql, docs/compliance/__tests__/exportAllowlist.test.ts, database.types.ts</files>
  <read_first>
    - o estado deixado pela Task 1 e a decisão da Task 2
    - `docs/compliance/sql/05-export-allowlist-drift.sql` (cabeçalho `:75-78` com a contagem de pares; os dois `VALUES` GERADOS)
    - `docs/compliance/__tests__/exportAllowlist.test.ts` (snapshots inline `:133`, `:170`, `:644`; a asserção `(k)` `:712`)
    - `efdeploy.cjs` (`exportar-meus-dados: true`, `executar-direito-titular: true`)
  </read_first>
  <action>
Medir e acrescentar ao item `fase: 49` do catálogo as 9 colunas restantes (`provedor_ia`/`modelo_ia` de `analise_candidato_vaga`, `comparativo_solicitado`, `entrevista_guias`, `redacoes_candidato`; `redacoes_candidato.rubrica_versao`). Vereditos: as de `analise_candidato_vaga` e `redacoes_candidato` com `export: false` e a razão da família `model_version`/`prompt_version`/`devolutivas_candidato.modelo_ia` (a de `rubrica_versao` é a mesma de `prompt_version`: descreve como o sistema avaliou, não a pessoa); as de `comparativo_solicitado`/`entrevista_guias` seguem a exclusão da tabela (conferir se o gerador exige veredito explícito mesmo assim e escrevê-lo se exigir). Inventário e recibo das 9 no padrão da Task 1. D-66: tirar `analise_candidato_vaga` e `entrevista_guias` de `tabelas_sem_pii_titular` (mantendo a nota de proveniência da 48-17 como histórico) e cobri-las no recibo conforme a opção aprovada na Task 2 — sem texto com o endereço literal do canal de privacidade e sem «teste psicológico». `meta.versao` → `1.3.0`.

Regenerar tudo pelos geradores; os dois `VALUES` do drift por `node docs/compliance/sql/gen-export-allowlist.cjs --sql-values` e `--sql-values-excluidas`, com a contagem de pares do cabeçalho atualizada pelo que os comandos imprimirem; atualizar DE PROPÓSITO os snapshots de `exportAllowlist.test.ts` (o diff = só `entrevista_analises.tipo`/`superada_em` e a versão). Rodar os quatro `check:*` e `npx vitest run docs/compliance src/features/privacidade src/__tests__/guards`.

Drift contra PROD, só leitura: `node p46apply.cjs run docs/compliance/sql/05-export-allowlist-drift.sql` — nenhuma coluna desta fase; a lista pré-existente vai para o SUMMARY. Redeploy: `node efdeploy.cjs exportar-meus-dados` e `node efdeploy.cjs executar-direito-titular` (`--dry-run` antes de cada, fechamento registrado), com o marcador novo no bundle vivo de cada (`superada_em` no de `exportar-meus-dados`; o texto novo do recibo, ou `dado_de_funcionario` na linha de `solicitado_por`, no de `executar-direito-titular`). Tipos finais da fase: `SUPABASE_ACCESS_TOKEN=$(security find-generic-password -s "Supabase CLI" -a supabase -w) npx supabase gen types typescript --project-id isljnozzlvckrgjjbjwp < /dev/null > database.types.ts` (0 octeto ⇒ `git checkout -- database.types.ts` e repetir); `tsc` ≤ 90; commitar e `git push origin main`; `origin/main..HEAD` vazio.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node docs/compliance/sql/gen-export-allowlist.cjs && node docs/compliance/sql/gen-pii-md.cjs && node docs/compliance/sql/gen-recibo-exclusao.cjs && npm run -s check:matriz-retencao && npm run -s check:pii-inventory-md && npm run -s check:recibo-exclusao && npm run -s check:export-allowlist && npx vitest run docs/compliance src/features/privacidade src/__tests__/guards && node -e "const fs=require('fs');const a=fs.readFileSync('docs/compliance/export-allowlist.json','utf8');if(!a.includes('1.3.0'))throw new Error('versao nao subiu para 1.3.0');const y=fs.readFileSync('docs/compliance/pii-inventory.yaml','utf8');const bloco=(y.split('tabelas_sem_pii_titular:')[1]||'').split(/\n[a-z_]+:\s*\n/)[0];if(/^\s*-\s*analise_candidato_vaga\b/m.test(bloco)||/^\s*-\s*entrevista_guias\b/m.test(bloco))throw new Error('D-66: as duas tabelas ainda estao em tabelas_sem_pii_titular');console.log('OK')"</automated>
    <fails_when>non-zero exit; "ERRO DE FECHAMENTO"/"VEREDITO ÓRFÃO" in stderr; any check or vitest failure; "versao nao subiu" or "D-66: as duas tabelas ainda estao" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs run docs/compliance/sql/05-export-allowlist-drift.sql | tee /dev/stderr | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const novas=['provedor_ia','modelo_ia','rubrica_versao','superada_em','texto_hash','ai_call_log_id','solicitado_por'];const achadas=novas.filter(c=>s.includes(c));if(achadas.length){console.error('drift ainda lista colunas da fase: '+achadas.join(', '));process.exit(1)}console.log('nenhuma coluna da fase no drift')})"</automated>
    <fails_when>non-zero exit with "drift ainda lista colunas da fase" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs exportar-meus-dados | tee /dev/stderr | grep -q "efdeploy: OK .*verify_jwt=true" && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/exportar-meus-dados/body" | grep -a -c "superada_em" && node efdeploy.cjs executar-direito-titular | tee /dev/stderr | grep -q "efdeploy: OK .*verify_jwt=true"</automated>
    <fails_when>non-zero exit; either efdeploy output lacks "efdeploy: OK" with "verify_jwt=true"; or the grep count printed is 0 (the export decision did not reach the live EF)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -s database.types.ts && grep -q "superada_em" database.types.ts && grep -q "registrar_analise_entrevista" database.types.ts && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; } && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>non-zero exit; database.types.ts empty or without the phase objects; lint exit code other than 0/2 or more than 90 "error TS" lines; or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - As 16 colunas da fase têm veredito, classificação, linha de recibo e medição no catálogo; a versão é `1.3.0`; D-66 aplicado conforme a decisão da Task 2.
    - Os quatro `check:*` e o vitest de compliance/privacidade/guards verdes; snapshots mudaram só pelas colunas exportadas da fase.
    - O drift contra PROD não lista coluna da fase (pré-existentes no SUMMARY).
    - As duas EFs redeployadas com `verify_jwt=true` e o marcador no bundle de `exportar-meus-dados`; tipos finais; `tsc` ≤ 90; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>O inventário do titular termina a fase tão fechado quanto começou, com as colunas novas decididas por escrito e as duas tabelas «sem PII» corrigidas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| titular → EF `exportar-meus-dados` | a allowlist gerada decide o que sai na cópia |
| titular ← recibo de exclusão (EF `executar-direito-titular`) | a promessa escrita |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-17-01 | Information Disclosure | UUID de funcionário na cópia do titular | high | mitigate | nome coberto pela R2/`ponteiros.de_terceiro`; asserção no artefato |
| T-49-17-02 | Repudiation | recibo prometendo «sem ligação» sobre texto com o nome | high | mitigate | `checkpoint:decision` do D-66 antes de escrever o recibo |
| T-49-17-03 | Tampering | artefato gerado editado à mão | medium | mitigate | só os geradores escrevem; `check:*` reprovam |
| T-49-17-04 | Denial of Service | decisão escrita que não chega à cópia/recibo reais | medium | mitigate | redeploy das duas EFs com marcador no bundle |
| T-49-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Vereditos/classificação/recibo/catálogo das 16 colunas; `check:*` e vitest verdes; drift sem coluna da fase.
- Duas EFs redeployadas; tipos finais; `tsc` ≤ 90; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
Nenhuma coluna criada pela fase ficou sem decisão escrita sobre ir ou não para a cópia do titular, e o recibo não promete sobre as análises o que o motor não faz.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| vereditos `decisoes_por_coluna` das 16 colunas; `meta.versao` 1.3.0 | `export-scope-rules.yaml` | editado |
| classificação das 16 colunas; D-66 em `tabelas_sem_pii_titular` | `pii-inventory.yaml` (+ `.md`) | editado |
| item `meta.acrescimos` `fase: 49` | `catalogo-vivo-44.json` | editado |
| linhas/razões das 16 colunas + cobertura D-66 | `gen-recibo-exclusao.cjs` (+ `recibo-exclusao.json`, 2 espelhos TS) | editado/regenerado |
| `export-allowlist.json` / `_shared/exportAllowlist.ts` 1.3.0 | artefatos gerados | regenerados |
| `VALUES` do drift, snapshots de `exportAllowlist.test.ts` | consulta/teste | regenerados/atualizados |
| `database.types.ts` | tipos gerados | regenerado (final) |

## Premissas sinalizadas (probe de borda)

JORN-28, JORN-12 e JORN-07 saíram `unclassified` no probe e ficam `unresolved` no relatório. Os vereditos de export
são DECISÃO DO PLANEJADOR, revisável pelo operador: a tensão registrada é que o D-27 tornou a troca de modelo
visível ao RH, e o critério do arquivo («se o RH enxerga sobre a pessoa, aparece na cópia») puxaria `provedor_ia`/
`modelo_ia` para `true`; o precedente específico e mais recente (`devolutivas_candidato.modelo_ia: false`, decisão da
44-CONTEXT §Área 2) puxa para `false`, e foi o seguido. As 9 colunas de drift pré-existentes seguem fora (deferred da 48).

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-17-SUMMARY.md` when done
</output>
