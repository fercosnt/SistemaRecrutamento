---
phase: 49-consertos-da-jornada-bloco-2
plan: 14
type: execute
wave: 4
depends_on: ["49-01", "49-07", "49-08"]
files_modified:
  - supabase/migrations/20260922000012_p49_motor_logs_e_revisao.sql
  - supabase/tests/p45_motor_exclusao_smoke.sql
autonomous: true
requirements: [JORN-36]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "D-61: `ai_call_logs.user_prompt_template` (o input mascarado, com nomes e fala literais) recebe sentinela no MESMO UPDATE do passo `severar_fks_set_null`, antes de `candidato_id := NULL` (é o `candidato_id` que acha a linha)."
    - "D-63: as linhas `call_type='comparative_ranking'` (com `candidato_id` NULL por desenho) cujo `user_prompt_template` cita `id=<candidatura_id>` de uma candidatura do titular têm `user_prompt_template`, `raw_response` e `parsed_reasoning` substituídos INTEIROS por sentinela — o resultado continua em `comparativo_solicitado`; linha de comparativo que não cita o titular não é tocada (asserção negativa no smoke)."
    - "D-60: `decisao_final.revisao_resultado` e a cópia em `decisao_final_historico.revisao_resultado` recebem sentinela (onde não nulos) nos MESMOS dois UPDATEs do passo `tombstone_decisao_final`, na ordem snapshot → raspagem (o UPDATE da corrente dispara o snapshot com o valor antigo; o do arquivo vem depois e o raspa) — nenhuma cópia identificável sobrevive."
    - "`plano_exclusao_titular` (o dry-run) conta exatamente o que o motor fará, pela MESMA expressão (regra (ii) do C3 do smoke): `revisao_resultado` no `tombstone_decisao_final` e as linhas `comparative_ranking` no `severar_fks_set_null`; o retorno `'passos'` do motor ganha as mesmas chaves. O passo novo `apagar_respostas_e_producoes` (D-48, D-62) e a sua contagem são do plano 49-20, que parte do corpo que este plano deixa."
    - "O `p45_motor_exclusao_smoke.sql` é ampliado para as três origens deste plano: fixture de `ai_call_logs` com `user_prompt_template`, uma linha `comparative_ranking` que cita a fixture e outra que não cita, `revisao_resultado` na decisão e no arquivo; uma asserção de pós-estado por coluna nova; a rede estrutural (C3) cresce com a forma (antes de re-pinar os md5 — PATTERNS §Shared C); o contador 25 sobe pelo número exato de asserções novas; e a mordida é provada por mutação (D-56): o corpo vivo ANTERIOR + o smoke ⇒ FAIL nas asserções novas."
    - "Esta migration não apaga linha (nenhum `DELETE` — D-54; a exceção D-62 vale só para o passo novo do 49-20) e nada executa o caminho feliz dentro dela (proibição do 46-04); a prova é do smoke, dentro de subtransação que reverte. A PRIMEIRA execução real do motor alterado é o checkpoint do plano 49-19, em conta de teste (D-54) — e este plano confere, só leitura, que a purga automática está em ensaio (`dry_run`), para que nenhum titular real passe pelo motor novo antes do checkpoint."
    - "O recibo e o inventário ainda descrevem `user_prompt_template` como conteúdo preservado até o plano 49-21, que os alinha ao motor antes de qualquer execução real (49-19): entre um e outro o recibo diz MENOS do que o motor apaga, nunca mais."
    - "Nenhum texto novo (sentinelas, comentários) contém o endereço literal do canal de privacidade nem «teste psicológico» (D-07, D-58)."
  artifacts:
    - path: supabase/migrations/20260922000012_p49_motor_logs_e_revisao.sql
      provides: "D-60, D-61, D-63 no motor e no plano"
      contains: "revisao_resultado"
    - path: supabase/tests/p45_motor_exclusao_smoke.sql
      provides: "uma asserção por origem nova (input da IA, comparativos, revisão) + pins re-pinados com proveniência"
      contains: "comparative_ranking"
  key_links:
    - from: "anonimizar_candidato (tombstone_decisao_final / severar_fks_set_null)"
      to: "plano_exclusao_titular"
      via: "mesma expressão de seleção por passo (dry-run = espelho)"
      pattern: "revisao_resultado"
    - from: "comparativo-candidatos prompt `(id=<candidatura_id>)` (49-08)"
      to: "redação D-63 das linhas comparative_ranking"
      via: "position('id=' || candidatura_id in user_prompt_template)"
      pattern: "comparative_ranking"
  prohibitions:
    - requirement_id: JORN-36
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT apagar linha nesta migration — a única exceção decidida (D-62) é o passo novo do plano 49-20, nas 4 tabelas de resposta"
    - requirement_id: JORN-36
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT executar o motor alterado sobre titular real antes do checkpoint do 49-19 — nem pela migration, nem pelo smoke fora de subtransação revertida"
    - requirement_id: JORN-36
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT re-pinar o md5 do motor sem fazer crescer a rede estrutural do smoke no mesmo commit (D-56)"
    - requirement_id: JORN-36
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT introduzir ocorrência literal nova do endereço do canal de privacidade nem a expressão «teste psicológico» em texto novo (D-07, D-58 — a única exceção é o disclaimer negado da devolutiva, já decidido)"
---

<objective>
O motor de exclusão passa a apagar o conteúdo que o titular mandou para as análises automáticas — inclusive nas linhas
de comparativo que o citam — e a resposta do revisor à revisão da decisão; o dry-run conta o mesmo.

Purpose: JORN-36/D-48 e as decisões do portão D-60, D-61 e D-63. Medido: o conteúdo enviado (`user_prompt_template`)
está mapeado como «conteúdo do produto»; as linhas `comparative_ranking` têm `candidato_id` NULL e o motor nunca as
alcança; `revisao_resultado` fica literal (RESEARCH Correções 13, 16, 18, Portão itens 3 e 4). É a primeira de três
fatias sequenciais do mesmo motor, cada uma com dono único do trecho que escreve: este plano (logs e revisão) →
49-20 (o passo novo `apagar_respostas_e_producoes`, D-48/D-62) → 49-21 (recibo e inventário alinhados). Mecanismo
destrutivo: a prova é o smoke revertido aqui e a primeira execução real é checkpoint do 49-19.

Output: a migration `20260922000012`, o smoke ampliado e re-pinado, a mutação que prova a mordida.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-VARREDURA-KICKOFF.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer) [BLOCKING]: o conteúdo enviado à IA e a resposta do revisor saem com o titular — motor, plano e smoke de ponta a ponta</name>
  <precondition>A purga automática está em ensaio (o modo vivo lido da tabela de configuração que `salvar_config_purga` escreve é `dry_run`) — se estiver `live`, PARAR e reportar: o motor alterado rodaria sobre titular real antes do checkpoint do 49-19.</precondition>
  <reversibility rating="one-way">Cada exclusão executada pelo motor novo é irreversível por desenho (D-48); o CÓDIGO é reaplicável, a execução não — por isso a primeira execução real é checkpoint (49-19), e aqui só há prova revertida.</reversibility>
  <files>supabase/migrations/20260922000012_p49_motor_logs_e_revisao.sql, supabase/tests/p45_motor_exclusao_smoke.sql</files>
  <read_first>
    - o VIVO, só leitura: `md5(prosrc)`, `length(prosrc)` e `pg_get_functiondef` de `anonimizar_candidato(uuid,boolean)` e `plano_exclusao_titular` — conferir contra os pins do smoke (`p45_motor_exclusao_smoke.sql:234-235`: `5209239f…`/`42f916d8…`) e guardar a saída no scratchpad (base da mutação do Passo 5)
    - `supabase/migrations/20260823000006_p46_guard_purga.sql` (o analog inteiro: escopo negativo `:6-18`, PROVENIÊNCIA do corpo `:56-110`, a função `:201-930`, `tombstone_decisao_final :724-746`, `severar_fks_set_null :748-763`, `'passos' :912-930`)
    - `supabase/migrations/20260823000008_p46_guard_plano.sql` (`:224-760` — `plano_exclusao_titular`; `:556-569` tombstone; `:615-630` severar)
    - `supabase/tests/p45_motor_exclusao_smoke.sql` (cabeçalho `:1-360` — protocolo de re-pin, contador `:25-33`, «a rede embaixo do md5 só cresce» `:1680-1688`; fixture de `ai_call_logs` `:945-965`; bloco (C3) `:1660-1760`; RESUMO `:2503`)
    - `supabase/tests/p46_purga_smoke.sql` (chama `anonimizar_candidato` em (b) e (o) — regressão)
    - `supabase/functions/comparativo-candidatos/index.ts` (o formato `Candidato C<n> (id=<candidatura_id>)` do prompt)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md` §K, §«Portão» itens 3 e 4, Correções 13, 16, 18
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md` §9, §11, §Shared A e C
  </read_first>
  <action>
Passo 1 — D-50: re-rodar a C4 do `49-VARREDURA-KICKOFF.md` (origens do recibo × tabelas que o motor toca) e montar no SUMMARY a tabela «origem prometida → ação no motor → asserção» para os itens `respostas_e_producoes`, `dados_enviados_a_analise_automatica` e `anotacoes_da_equipe` (revisao_resultado) — as linhas de `respostas_e_producoes` ficam com a ação «plano 49-20» (é a base dele). Varredura de portões do `CLAUDE.md` e classificação dos achados que citem `anonimizar_candidato`/`plano_exclusao_titular` (os pins de md5 e o contador 25 são os conhecidos).

Passo 2 — escrever `supabase/migrations/20260922000012_p49_motor_logs_e_revisao.sql` no molde de `20260823000006` (escopo negativo no topo; seção PROVENIÊNCIA: o corpo é copiado do ARQUIVO entre os delimitadores nomeados `$anonimizar_candidato$`/`$plano_exclusao_titular$` e conferido por md5 contra o vivo antes de editar; pré-portão com os dois md5; nota de transporte; sem `BEGIN;`; nenhum bloco que execute o caminho feliz). Mudanças no `anonimizar_candidato`:
(a) `tombstone_decisao_final`: nos dois UPDATEs existentes, acrescentar `revisao_resultado = CASE WHEN revisao_resultado IS NULL THEN NULL ELSE '<sentinela da revisão>' END` — o da corrente e o do arquivo, na ordem de hoje (D-60), com o comentário do mecanismo estendido à coluna nova; contar `v_n_df_rev`/`v_n_dfh_rev`;
(b) `severar_fks_set_null` (1/5): o UPDATE de `ai_call_logs` ganha `user_prompt_template = '<sentinela do input>'` (D-61);
(c) antes do (1/5), um UPDATE novo em `ai_call_logs` para `call_type = 'comparative_ranking'` e `EXISTS (candidatura do titular c WHERE position('id=' || c.id::text IN l.user_prompt_template) > 0)`: `user_prompt_template`, `raw_response` e `parsed_reasoning` inteiros por sentinela (D-63; comentário: perde a telemetria dos outros titulares daquela chamada, o resultado segue em `comparativo_solicitado`; o corte só do bloco foi rejeitado por depender do formato); contar `v_n_aicall_cmp`;
(d) `'passos'`: as contagens novas nas chaves existentes (`tombstone_decisao_final.revisao_resultado_*`, `severar_fks_set_null.ai_call_logs_comparativo`).
`plano_exclusao_titular`: as MESMAS três contagens pela mesma expressão. Pós-portão: presença das formas novas + ausência de regressão das asserções que o 46-04 exige; `RAISE NOTICE` com os md5 novos (o 49-20 pina o seu pré-portão neles).

Passo 3 — `p45_motor_exclusao_smoke.sql`: fixture de `ai_call_logs` com `user_prompt_template` de texto e uma linha `comparative_ranking` (`candidato_id` NULL) cujo `user_prompt_template` contém `id=<candidatura da fixture>` e OUTRA que não a contém; `revisao_resultado` preenchido na `decisao_final` da fixture e numa versão do arquivo. Asserções novas: `user_prompt_template` do titular = sentinela; a linha de comparativo que cita = redigida inteira; a que não cita = intocada (negativa); `revisao_resultado` = sentinela na corrente E em TODAS as versões do arquivo, inclusive a recém-criada pelo snapshot; o plano (dry-run) prevê exatamente essas contagens. Rede estrutural (C3/iii-iv) cresce com a forma (o corpo vivo contém `revisao_resultado` nos dois UPDATEs, a ordem corrente→arquivo, o predicado `position('id=' ||`); SÓ DEPOIS re-pinar os dois md5, escrevendo a PROVENIÊNCIA do re-pin no cabeçalho (conferência vivo × arquivo, data, plano); subir o contador 25 pelo número exato e registrar o bump no cabeçalho (como o 48-01 fez) — o valor novo vai para o SUMMARY (é a base do 49-20).

Passo 4 — ensaio (a migration + o smoke + `DO` `ENSAIO_OK`, numa requisição que aborta); depois `node p46apply.cjs migrate …`, `node p46apply.cjs run supabase/tests/p45_motor_exclusao_smoke.sql` e a regressão `node p46apply.cjs run supabase/tests/p46_purga_smoke.sql`.

Passo 5 — mutação (D-56), em requisição que aborta: o `CREATE OR REPLACE` de `anonimizar_candidato` e `plano_exclusao_titular` com os corpos ANTERIORES (guardados no scratchpad no read_first) + o smoke ampliado ⇒ esperado `FAIL` na primeira asserção nova deste plano; registrar a saída no SUMMARY e conferir, só leitura, que o md5 vivo é o do pós-portão (a mutação não vazou). Commitar (migration + smoke) e `git push origin main`; `origin/main..HEAD` vazio.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260922000012_p49_motor_logs_e_revisao.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');for(const t of ['CREATE OR REPLACE FUNCTION public.anonimizar_candidato','CREATE OR REPLACE FUNCTION public.plano_exclusao_titular','revisao_resultado','user_prompt_template','comparative_ranking',\"position('id=' ||\",'md5'])if(!c.includes(t))throw new Error('falta: '+t);if(/DELETE\s+FROM/i.test(c))throw new Error('esta migration nao apaga linha');if(/PERFORM\s+public\.anonimizar_candidato\s*\(/i.test(c))throw new Error('a migration nao pode executar o motor (46-04)');console.log('OK')"</automated>
    <fails_when>non-zero exit with "falta:", "wrapper BEGIN proibido", "nao apaga linha" or "nao pode executar o motor" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && D=$(mktemp -d) && { cat supabase/migrations/20260922000012_p49_motor_logs_e_revisao.sql supabase/tests/p45_motor_exclusao_smoke.sql; printf '\nDO $e$ BEGIN RAISE EXCEPTION %s; END $e$;\n' "'ENSAIO_OK'"; } > "$D/ensaio.sql" && node p46apply.cjs run "$D/ensaio.sql" 2>&1 | tee "$D/out.txt" | tail -3; grep -q "ENSAIO_OK" "$D/out.txt" && ! grep -q "FAIL" "$D/out.txt"</automated>
    <fails_when>the output lacks "ENSAIO_OK" or contains "FAIL"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs migrate supabase/migrations/20260922000012_p49_motor_logs_e_revisao.sql && node p46apply.cjs run supabase/tests/p45_motor_exclusao_smoke.sql && node p46apply.cjs run supabase/tests/p46_purga_smoke.sql && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>non-zero exit; "md5 do ledger BATE" absent from the migrate output; "FAIL" in either smoke output; or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - A tabela «origem → ação → asserção» está no SUMMARY para os três itens do recibo.
    - Em PROD, `anonimizar_candidato` e `plano_exclusao_titular` contêm `revisao_resultado` no tombstone, `user_prompt_template` no severar e a redação `comparative_ranking` por `position('id=' ||`.
    - O smoke passa com as asserções novas, a rede estrutural cresceu antes do re-pin, o contador subiu e o cabeçalho registra a proveniência do re-pin; `p46_purga_smoke` segue verde.
    - A mutação com os corpos anteriores reprova (saída no SUMMARY) e o md5 vivo é o do pós-portão.
    - A precondição da purga em `dry_run` está registrada (valor lido); os md5 novos e o contador novo do smoke estão no SUMMARY (base do 49-20).
  </acceptance_criteria>
  <done>Excluir um titular passa a apagar o que ele mandou para as análises automáticas — inclusive nos comparativos que o citaram — e a resposta do revisor ao seu pedido de revisão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| titular/administrador (JWT) → EF `executar-direito-titular` → `anonimizar_candidato` | mecanismo destrutivo, transação única depois do passo de Storage |
| purga automática (cron, service) → `anonimizar_candidato` | em `dry_run` hoje; conferido antes do apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-14-01 | Information Disclosure | input mascarado do titular (`user_prompt_template`) sobrevivendo à exclusão | high | mitigate | sentinela no UPDATE do `severar_fks_set_null`; asserção no smoke |
| T-49-14-03 | Tampering | apagar linha fora do escopo decidido | high | mitigate | verify reprova qualquer `DELETE` nesta migration; pós-portão |
| T-49-14-05 | Information Disclosure | input de vários titulares na linha de comparativo | medium | mitigate | redação inteira da linha que cita o titular (D-63); negativa para a que não cita |
| T-49-14-06 | Tampering | re-pin do md5 sem rede estrutural | medium | mitigate | rede cresce antes do re-pin; proveniência no cabeçalho; mutação reprova |
| T-49-14-08 | Information Disclosure | resposta do revisor (`revisao_resultado`) sobrevivendo na corrente ou no arquivo | high | mitigate | sentinela nos dois UPDATEs do tombstone, na ordem snapshot → raspagem; asserção em todas as versões do arquivo |
| T-49-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Migration `20260922000012` aplicada (ensaio antes), md5 do ledger conferido; purga em `dry_run` registrada.
- `p45_motor_exclusao_smoke` ampliado e re-pinado verde; `p46_purga_smoke` verde; mutação reprova.
- `origin/main..HEAD` vazio.
</verification>

<success_criteria>
O que o titular mandou para as análises automáticas, as linhas de comparativo que o citam e a resposta do revisor saem
com ele na exclusão — provado coluna a coluna por um smoke que reprova quando o conserto é desfeito.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| contagens novas em `plano_exclusao_titular` e em `'passos'` (`revisao_resultado_*`, `ai_call_logs_comparativo`) | dry-run / retorno do motor | **novas** |
| sentinelas da revisão e do input | valores gravados | **novos** |
| `supabase/migrations/20260922000012_p49_motor_logs_e_revisao.sql` | migration | **nova** |
| `supabase/tests/p45_motor_exclusao_smoke.sql` | smoke | ampliado, re-pinado, contador > 25 |

## Premissas sinalizadas (probe de borda)

JORN-36 saiu `unclassified` no probe e fica `unresolved` no relatório. As bordas escritas aqui vêm das Correções
13, 16, 18 e das decisões D-60, D-61, D-63.

**Por que o 49-14 original virou três planos (revisão do checker, W1):** o plano original somava duas reescritas
completas dos mesmos dois corpos (`anonimizar_candidato` ~730 linhas, `plano_exclusao_titular` ~540) e a regeneração
dos artefatos de compliance, estimado em 150k. As fatias são SEQUENCIAIS sobre os mesmos objetos (dono único
preservado: 49-14 → 49-20 → 49-21 → 49-17), cada uma com o seu pré-portão pinado no pós-portão da anterior. Nada foi
perdido entre elas: a mutação do passo novo, o checkpoint da primeira execução real (49-19) e a precondição da purga
em `dry_run` seguem, e a precondição agora é conferida de novo no 49-20.

**Portas de mão única sem `checkpoint:decision`, de propósito:** a Task 1 leva `rating="one-way"` para o registro
(cada execução do motor é irreversível), mas a porta já foi atravessada pelo operador no portão (D-48, D-60..D-63) —
re-perguntar seria o anti-padrão «gating a decision already made». O que ainda é gate é a PRIMEIRA execução real, e
ela é `checkpoint:human-verify` com contagem antes/depois no plano 49-19 (D-54). O apply do código é
`CREATE OR REPLACE` (D-54: sem pausa), provado antes por ensaio em requisição que aborta. `parsed_reasoning` das
linhas de comparativo entra na redação junto com `raw_response` (é saída derivada dela; o passo `severar_fks_set_null`
já a anula junto nas linhas com `candidato_id`).

**Ordem de apply:** a `…000012` divide a onda 4 com as `…000009`..`…000011` do 49-12, escritas retroativas de
linhas que não tocam o motor — objetos disjuntos (dono único), então a ordem relativa dentro da onda não muda o
estado final; entre ondas, a ordem de apply é a ordem de versão.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-14-SUMMARY.md` when done
</output>
