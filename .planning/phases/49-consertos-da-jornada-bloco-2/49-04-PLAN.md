---
phase: 49-consertos-da-jornada-bloco-2
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/vagas/services/candidaturasService.ts
  - src/features/vagas/services/__tests__/candidaturasService.test.ts
  - src/features/vagas/types/vagasTypes.ts
  - src/components/pages/CandidatosRHPage.tsx
  - src/components/ScoreCard.tsx
  - src/components/__tests__/ScoreCard.test.tsx
  - src/lib/cognitivo/cognitivoBanda.ts
  - src/lib/cognitivo/__tests__/cognitivoBanda.test.ts
  - src/features/avaliacao-cognitiva/components/LiberacaoCognitivoBlock.tsx
  - src/features/avaliacao-cognitiva/components/__tests__/LiberacaoCognitivoBlock.test.tsx
autonomous: true
requirements: [JORN-13, JORN-38, JORN-40]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Invariante do 13: no card da lista do RH, AUSÊNCIA NUNCA VIRA 0 — nenhuma célula mostra `0` nem cor de nota quando a fonte não tem dado. Os helpers de `vagasTypes.ts` que devolviam `0` (`calculateBigFiveAverage`, `getCultureScore`) são substituídos por funções que devolvem ESTADO TIPADO."
    - "Célula Big Five = «concluído» / «não fez», sem número e sem cor de nota, pela existência de `scores_candidato` com `tipo='big_five'` (D-31, UX-07/RNF-07a: Big Five é não avaliativo)."
    - "Célula Cultura = SÓ a nota revisada por humano: `scores_candidato` `tipo='redacao'` com `status='sucesso'` (gravada por `sincronizar_score_redacao`), 0–100 com cor; redação existente sem essa linha ⇒ «aguardando revisão»; sem redação ⇒ «não fez»; NUNCA a sugestão da IA de `redacoes_candidato` (D-32)."
    - "DISC sai do card; Inteligência = faixa do vocabulário `cognitivoBanda` (Acima/Dentro/Abaixo do esperado), SEM percentil e SEM cor de nota, ou «não fez» (D-33, D-64). `cognitivoBanda` mora em `src/lib/cognitivo/cognitivoBanda.ts`, extraída de `ScoreCard.tsx:23-27`, e é a MESMA função que o card e o hub usam."
    - "A aba «Por Vaga» lê a mesma fonte das notas que a lista geral (JORN-13); os embeds mortos (`scores_bigfive`, `scores_disc`) e a leitura de `analise_ia_cultura` saem."
    - "JORN-38: nenhum dos três selects da forma (`updateCandidaturaStatus`, `listAllCandidaturas`, `listCandidaturasByVaga`) traz ao navegador colunas do candidato que a tela não exibe — sem embed curinga de `candidatos`, sem `cpf`, sem `data_nascimento`; o card usa `id, nome_completo, email, celular` (RESEARCH Correção 21). Um teste reprova se a string do select voltar a ter a forma."
    - "JORN-40: o `LiberacaoCognitivoBlock` do hub deixa de mostrar o percentil cru E o «Acertos X de 60» (D-64); mostra a faixa de `cognitivoBanda`. Um teste reprova se algum dígito de percentil ou «de 60» aparecer no DOM."
    - "Nenhum texto novo contém «teste psicológico» nem o endereço literal do canal de privacidade: o guard `forbidden-strings.grep.test.ts` passa (D-58, D-07)."
    - "`tsc` ≤ 90 (D-53); build com o marcador no chunk certo; push; o marcador está no ar em `https://rh.beautysmile.com.br` (D-52)."
  artifacts:
    - path: src/lib/cognitivo/cognitivoBanda.ts
      provides: "vocabulário único da faixa cognitiva nas telas do RH"
      contains: "export function cognitivoBanda"
    - path: src/features/vagas/types/vagasTypes.ts
      provides: "estados tipados das células (sem 0 por ausência)"
      contains: "aguardando_revisao"
    - path: src/components/ScoreCard.tsx
      provides: "células Big Five / Cultura / Inteligência sem DISC"
      contains: "scorecard-cultura-estado"
    - path: src/features/avaliacao-cognitiva/components/LiberacaoCognitivoBlock.tsx
      provides: "hub com faixa, sem percentil nem acertos"
      contains: "cognitivoBanda"
  key_links:
    - from: "src/features/vagas/services/candidaturasService.ts (embed scores_candidato + redacoes_candidato + scores_raven)"
      to: "src/components/pages/CandidatosRHPage.tsx → ScoreCard"
      via: "helpers de estado em vagasTypes.ts"
      pattern: "scores_candidato"
    - from: "src/lib/cognitivo/cognitivoBanda.ts"
      to: "ScoreCard.tsx e LiberacaoCognitivoBlock.tsx"
      via: "import da mesma função"
      pattern: "cognitivoBanda\\("
  prohibitions:
    - requirement_id: JORN-13
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT mostrar número ou cor de nota para Big Five — é avaliação comportamental não avaliativa (UX-07/RNF-07a)"
    - requirement_id: JORN-13
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT mostrar como nota de Cultura a sugestão da IA — só a nota revisada por humano pesa na tela (D-32, RNF-07a)"
    - requirement_id: JORN-38
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT trazer ao navegador CPF, data de nascimento ou qualquer coluna do candidato que a tela não exibe"
    - requirement_id: JORN-40
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT exibir ao RH percentil numérico nem contagem bruta de acertos da avaliação cognitiva — só faixas (UX-07)"
    - requirement_id: JORN-13
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT introduzir ocorrência literal nova do endereço do canal de privacidade nem a expressão «teste psicológico» em texto novo (D-07, D-58 — a única exceção é o disclaimer negado da devolutiva, já decidido)"
---

<objective>
O card da lista do RH e o bloco cognitivo do hub mostram o que existe — e dizem «não fez» ou «aguardando
revisão» quando não existe — em vez de transformar ausência em 0; e a lista para de puxar o cadastro
inteiro do candidato para o navegador.

Purpose: medido no kickoff, OS 38 CARDS mostravam 0 onde não há nota: `CandidatosRHPage.tsx:352-355` lê
`scores_bigfive` (0 linhas), `scores_disc` (0 linhas, DISC não existe no produto) e `analise_ia_cultura`
(0 de 38); os helpers devolvem `0`, e o `?? 'N/A'` nunca dispara (D-31..D-33). O mesmo select traz
`candidatos(*)`, CPF inclusive (JORN-38), e o hub mostra percentil e acertos crus (JORN-40, D-64).

Output: service com allowlist e a fonte canônica, helpers de estado, card e hub consertados, `cognitivoBanda`
extraída, publicado.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): a célula Cultura de ponta a ponta — fonte canônica no select, estado tipado, card sem 0</name>
  <files>src/features/vagas/services/candidaturasService.ts, src/features/vagas/services/__tests__/candidaturasService.test.ts, src/features/vagas/types/vagasTypes.ts, src/components/pages/CandidatosRHPage.tsx, src/components/ScoreCard.tsx, src/components/__tests__/ScoreCard.test.tsx</files>
  <read_first>
    - `src/features/vagas/services/candidaturasService.ts` (`:551-600` `listAllCandidaturas`; `:676-706` `listCandidaturasByVaga`; `:417-470` `updateCandidaturaStatus`)
    - `src/features/vagas/types/vagasTypes.ts` (`:660-730` — `CandidaturaComScores`, `calculateBigFiveAverage`, `formatDiscProfile`, `getCultureScore`)
    - `src/components/pages/CandidatosRHPage.tsx` (`:340-430` — `CandidatoCard` e o uso do `ScoreCard`)
    - `src/components/ScoreCard.tsx` (inteiro) e `src/components/__tests__/ScoreCard.test.tsx`
    - `src/features/avaliacao/services/scoresRhService.ts` (`:120-150` — `SCORES_ALLOWLIST`, o molde de allowlist auditável)
    - o vivo de `sincronizar_score_redacao` (só leitura, `pg_get_functiondef`) — grava `tipo='redacao'`, `status='sucesso'`, `score_max=100`
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md` §18
  </read_first>
  <behavior>
    - `estadoCultura(scoresCandidato, redacoes)`: linha `redacao`/`sucesso` com score 72 ⇒ `{ estado: 'nota', valor: 72 }`; redação existente sem linha de sucesso ⇒ `{ estado: 'aguardando_revisao' }`; nada ⇒ `{ estado: 'nao_fez' }`; linha `redacao` com `status='pendente_humano'` ⇒ `aguardando_revisao` (nunca a nota da IA).
    - ScoreCard com `cultura = { estado: 'nao_fez' }` não renderiza nenhum `0` nem classe de cor de nota na célula; com `aguardando_revisao` mostra «aguardando revisão»; com nota 72 mostra 72 com cor.
    - A string do select de `listAllCandidaturas` não contém o embed curinga de candidatos nem `cpf`, e contém `scores_candidato`.
  </behavior>
  <action>
Passo 1 — D-50: re-rodar o padrão da C8 do `49-VARREDURA-KICKOFF.md` (o `grep` de `return 0` / `?? 0` / `|| 0` em `vagasTypes.ts`, `src/components`, `triagem`, `hub-candidato`, `decisao`) e registrar o delta no SUMMARY, classificando cada ocorrência (a `CandidatosRHPage.tsx:900` `funilEtapas[...] || 0` é escopo deliberado: contagem). Medir, só leitura, as fontes: `select tipo, status, count(*), count(score) from public.scores_candidato group by 1,2` e `select count(*) from public.scores_raven` — registrar no SUMMARY (a pesquisa mediu: `big_five` 5 linhas com `score` NULL; `redacao` 2; `scores_raven` 1).

Passo 2 — `listAllCandidaturas`: reescrever o select mantendo `*` da própria `candidaturas` (o RH vê a linha dele) e trocando os embeds: `candidato:candidatos(id, nome_completo, email, celular)`, `vaga:vagas(id, titulo)` (conferir antes quem lê outras colunas de `vaga` na `CandidatosRHPage` e acrescentar só essas), `scores_candidato!left(tipo, status, score, score_max)`, `redacoes_candidato!left(id)` (o sinal de «fez a redação», D-32) e `scores_raven!left(percentil)`; tirar `scores_bigfive` e `scores_disc`. Declarar as projeções como constantes nomeadas no topo do arquivo, no molde de `SCORES_ALLOWLIST` (docblock: nunca curinga, JORN-38).

Passo 3 — `vagasTypes.ts`: `CandidaturaComScores` passa a declarar `scores_candidato`, `redacoes_candidato`, `scores_raven` (sem `scores_bigfive`/`scores_disc`); criar `EstadoCultura` (`{ estado: 'nota'; valor: number } | { estado: 'aguardando_revisao' } | { estado: 'nao_fez' }`) e `estadoCultura(...)`; remover `getCultureScore` e `calculateBigFiveAverage` (conferir por `grep` que o único chamador é a `CandidatosRHPage`); `formatDiscProfile` sai se não tiver outro chamador.

Passo 4 — `CandidatosRHPage.tsx` `CandidatoCard`: calcular os estados pelos helpers novos e passá-los ao `ScoreCard`. `ScoreCard.tsx`: a célula Cultura recebe `EstadoCultura`; só `estado === 'nota'` usa `getScoreColor` e mostra número; os outros estados mostram o rótulo em cor neutra (`text-white/50`); `data-testid="scorecard-cultura-estado"` na célula. Testes do `<behavior>` em `ScoreCard.test.tsx` e em `candidaturasService.test.ts` (asserção sobre a string passada a `.select`, no idioma do Wave-0 de `scoresRhService`). Rodar os testes e o portão `tsc`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/components/__tests__/ScoreCard.test.tsx src/features/vagas/services/__tests__/candidaturasService.test.ts</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }</automated>
    <fails_when>lint exit code other than 0 or 2, exit code and error count disagree, or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - Os testes do `<behavior>` existem e passam; nenhuma célula de Cultura mostra `0` por ausência.
    - `getCultureScore` e `calculateBigFiveAverage` não existem mais em `vagasTypes.ts`.
    - O select de `listAllCandidaturas` usa as constantes de projeção, sem curinga em `candidatos`, com `scores_candidato` e `redacoes_candidato`.
    - `tsc` ≤ 90.
  </acceptance_criteria>
  <done>O RH deixa de ler «0» de cultura para quem nunca teve a redação revisada — e passa a ler «aguardando revisão» ou «não fez».</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Big Five, Inteligência pela faixa, DISC fora, aba «Por Vaga» na mesma fonte, e os outros dois selects sem PII a mais</name>
  <files>src/lib/cognitivo/cognitivoBanda.ts, src/lib/cognitivo/__tests__/cognitivoBanda.test.ts, src/components/ScoreCard.tsx, src/components/__tests__/ScoreCard.test.tsx, src/features/vagas/services/candidaturasService.ts, src/features/vagas/services/__tests__/candidaturasService.test.ts, src/features/vagas/types/vagasTypes.ts, src/components/pages/CandidatosRHPage.tsx</files>
  <read_first>
    - o estado deixado pela Task 1
    - `src/lib/candidatura/candidaturaEncerrada.ts` e `src/lib/candidatura/__tests__/candidaturaEncerrada.test.ts` (a forma de util com docblock + tabela-verdade `it.each`)
    - `src/features/avaliacao/components/ScorecardAvaliacao.tsx` (`:230-275` — faixa NEUTRA: `Badge className="border-white/15 bg-white/5 text-white/70 text-xs"`, regra UX-07)
    - quem consome `listCandidaturasByVaga` e `updateCandidaturaStatus` (`grep -rn` em `src/`): conferir quais colunas de `candidato` e `vaga` cada tela lê ANTES de cortar (o `KanbanBoard` lê `email` e `celular`)
  </read_first>
  <behavior>
    - `cognitivoBanda(85)` = «Acima do esperado»; `(55)` = «Dentro do esperado»; `(20)` = «Abaixo do esperado»; limites 70 e 40 como no `ScoreCard.tsx:23-27` de hoje.
    - Card com Big Five concluído mostra «concluído» sem número nem cor; sem linha `big_five` mostra «não fez».
    - Card com percentil 85 mostra «Acima do esperado» e NÃO mostra `85` nem `P85`; sem Raven mostra «não fez».
    - O card não tem mais célula DISC.
    - A string do select de `listCandidaturasByVaga` não contém `cpf` nem `data_nascimento`; a de `updateCandidaturaStatus` é uma projeção explícita de `candidaturas` sem embed de `candidatos`.
  </behavior>
  <action>
Criar `src/lib/cognitivo/cognitivoBanda.ts` (export nomeado, docblock: regra UX-07 «telas do RH sem percentil numérico», limites PROVISÓRIOS herdados do `ScoreCard`, quem usa — card e hub, D-64) movendo a função privada de `ScoreCard.tsx:23-27` sem mudar limites; teste de tabela-verdade `it.each` em `src/lib/cognitivo/__tests__/cognitivoBanda.test.ts`.

`vagasTypes.ts`: `estadoBigFive(scoresCandidato)` → `'concluido' | 'nao_fez'` pela existência de linha `tipo='big_five'` (o `score` é NULL por desenho; os dados vivem em `metadata.dimensoes`); `estadoInteligencia(scoresRaven)` → `{ estado: 'faixa'; faixa: string } | { estado: 'nao_fez' }` usando `cognitivoBanda`. `ScoreCard.tsx`: props passam a ser os três estados (Big Five, Cultura, Inteligência) + `scoreGeral` como hoje; a célula DISC e o ícone dela saem; Big Five e Inteligência em faixa neutra no idioma de `ScorecardAvaliacao.tsx:246-275`, sem `getScoreColor`; atualizar o docblock do topo (4 → 3 células, e por quê). Reescrever `ScoreCard.test.tsx` para os estados (os testes atuais passam `inteligencia={85}` e esperam a faixa — manter a intenção, trocar a forma). `CandidatosRHPage.tsx`: usar os helpers novos; a aba «Por Vaga» lê as mesmas notas.

Selects (JORN-38): `listCandidaturasByVaga` recebe os MESMOS embeds de notas da Task 1 e um `candidato:candidatos(...)` só com as colunas que as telas consumidoras exibem (medido no read_first; `cpf` e `data_nascimento` saem); `updateCandidaturaStatus` passa a `select('id, etapa_atual, status')` (o corpo só lê `etapa_atual`, `:446`). Testes das duas strings em `candidaturasService.test.ts`. Rodar os testes tocados, `npx vitest run src/components src/features/vagas src/lib` e o portão `tsc`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/lib/cognitivo src/components src/features/vagas src/lib/candidatura</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const s=require('fs').readFileSync('src/features/vagas/services/candidaturasService.ts','utf8');const code=s.split('\n').filter(l=>!/^\s*(\/\/|\*)/.test(l)).join('\n');if(/candidatos\s*\(\s*\*\s*\)/.test(code))throw new Error('embed curinga de candidatos ainda presente');if(/\bcpf\b/.test(code))throw new Error('cpf ainda no select');if(/data_nascimento/.test(code))throw new Error('data_nascimento ainda no select');if(/scores_bigfive|scores_disc/.test(code))throw new Error('embed morto ainda presente');console.log('OK selects')" && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; }</automated>
    <fails_when>non-zero exit with "embed curinga", "cpf ainda", "data_nascimento ainda" or "embed morto" in stderr; lint exit code other than 0/2 or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `cognitivoBanda` existe em `src/lib/cognitivo/` com teste; `ScoreCard.tsx` não define mais a função localmente.
    - O card não tem célula DISC; Big Five e Inteligência sem número nem cor de nota.
    - Os três selects do `candidaturasService` sem curinga de `candidatos`, sem `cpf`, sem `data_nascimento`, sem embeds mortos.
    - `vitest` das pastas tocadas verde; `tsc` ≤ 90.
  </acceptance_criteria>
  <done>O card do RH passa a ter três células verdadeiras e nenhuma inventada; a lista deixa de levar CPF ao navegador.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: o hub sem percentil nem acertos crus (JORN-40) — e a publicação com o marcador no ar</name>
  <files>src/features/avaliacao-cognitiva/components/LiberacaoCognitivoBlock.tsx, src/features/avaliacao-cognitiva/components/__tests__/LiberacaoCognitivoBlock.test.tsx</files>
  <read_first>
    - `src/features/avaliacao-cognitiva/components/LiberacaoCognitivoBlock.tsx` (inteiro; `:80-115` — o `<dl>` com Percentil, Acertos e Tempo; o comentário `:87-91` que fica falso)
    - `src/features/avaliacao-cognitiva/hooks/useLiberacaoCognitivo.ts` (o formato de `data.resultado`)
    - `src/lib/cognitivo/cognitivoBanda.ts` (Task 2)
    - `src/__tests__/guards/forbidden-strings.grep.test.ts`
  </read_first>
  <behavior>
    - Com `resultado = { percentil: 12, classificacao: 'Inferior', total_acertos: 8, tempo_total_segundos: 900 }`: o DOM mostra «Abaixo do esperado», o tempo, e NÃO contém `12`, `8`, «de 60» nem «Percentil».
    - Sem `resultado`: o bloco não quebra e não mostra faixa.
  </behavior>
  <action>
No `<dl>` de `LiberacaoCognitivoBlock.tsx`, trocar os itens «Percentil» e «Acertos … de 60» por um item «Faixa» com `cognitivoBanda(data.resultado.percentil)` (quando houver percentil) em Badge neutro, com `data-testid="cognitivo-banda-rh"`; o item «Tempo» fica. Reescrever o comentário `:87-91` (ele justificava mostrar o percentil) com a regra nova: UX-07/D-33/D-64 — o RH vê faixa, nunca número; a classificação por extenso do instrumento (`scores_raven.classificacao`) NÃO é o vocabulário das telas do RH. Criar `__tests__/LiberacaoCognitivoBlock.test.tsx` com o `<behavior>` (mock do hook). Rodar os testes, o guard de strings e o portão `tsc`.

Publicar (D-52): `npm run build` e conferir os dois marcadores (`scorecard-cultura-estado`, `cognitivo-banda-rh`) em `build/assets/` — as rotas `/rh/*` são lazy, então anotar em QUAL chunk cada um mora (`grep -rl`); commitar; `git push origin main`; `origin/main..HEAD` vazio; conferir o marcador no ar com o crawler.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/features/avaliacao-cognitiva src/__tests__/guards</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npm run build >/dev/null && grep -rl "scorecard-cultura-estado" build/assets/ && grep -rl "cognitivo-banda-rh" build/assets/</automated>
    <fails_when>non-zero exit (build failed, or either marker is absent from every file in build/assets/)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && node -e "const M=process.argv[1],B='https://rh.beautysmile.com.br';(async()=>{const vis=new Set(),fila=['/'];let ok=false;while(fila.length&&vis.size<300){const p=fila.shift();if(vis.has(p))continue;vis.add(p);let t='';try{t=await (await fetch(B+p)).text()}catch{continue}if(t.includes(M)){ok=true;break}for(const m of t.matchAll(/(?:\/assets\/|\.\/)([\w.-]+\.js)/g))fila.push('/assets/'+m[1])}if(!ok){console.error('AUSENTE em PROD: '+M);process.exit(1)}console.log('PRESENTE em PROD: '+M)})()" "scorecard-cultura-estado"</automated>
    <fails_when>non-zero exit: unpushed commits remain, or "AUSENTE em PROD: scorecard-cultura-estado" (Vercel has not published the build)</fails_when>
  </verify>
  <acceptance_criteria>
    - O bloco do hub mostra a faixa e não mostra percentil nem «de 60» (teste).
    - `forbidden-strings` verde.
    - Os dois marcadores estão no build (chunk anotado no SUMMARY) e `scorecard-cultura-estado` está no ar; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>No hub, a avaliação cognitiva aparece ao RH como faixa — a mesma do card — e nunca como um número que convida a ser lido como nota.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do RH ← PostgREST (RLS) | o select decide o que chega ao navegador; a RLS filtra linhas, não colunas |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-04-01 | Information Disclosure | CPF/nascimento do candidato no payload da lista | high | mitigate | projeções explícitas; teste reprova curinga, `cpf`, `data_nascimento` |
| T-49-04-02 | Repudiation | nota inventada (0) orientando decisão do RH | medium | mitigate | estados tipados; teste «ausência nunca vira 0» |
| T-49-04-03 | Information Disclosure | percentil cru exposto (UX-07) | medium | mitigate | faixa `cognitivoBanda`; teste sem dígito nem «de 60» |
| T-49-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `vitest` de `src/components`, `src/features/vagas`, `src/lib`, `src/features/avaliacao-cognitiva`, `src/__tests__/guards` verde.
- `tsc` ≤ 90; build com os marcadores; `origin/main..HEAD` vazio; marcador no ar.
</verification>

<success_criteria>
O que o RH lê na lista e no hub sobre cada avaliação é o que existe no banco — ou a palavra que diz que não existe.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `src/lib/cognitivo/cognitivoBanda.ts` (`cognitivoBanda`) | util | **novo** (extraído do ScoreCard) |
| `src/lib/cognitivo/__tests__/cognitivoBanda.test.ts` | teste | **novo** |
| `EstadoCultura`, `estadoCultura`, `estadoBigFive`, `estadoInteligencia` | tipos/helpers em `vagasTypes.ts` | **novos** |
| `calculateBigFiveAverage`, `getCultureScore` (e `formatDiscProfile` se sem chamador) | helpers | **removidos** |
| constantes de projeção do `candidaturasService` | allowlists de select | **novas** |
| `data-testid="scorecard-cultura-estado"`, `data-testid="cognitivo-banda-rh"` | marcadores | **novos** |
| `src/features/avaliacao-cognitiva/components/__tests__/LiberacaoCognitivoBlock.test.tsx` | teste | **novo** |

## Premissas sinalizadas (probe de borda)

JORN-13, JORN-38 e JORN-40 saíram `unclassified` no probe (texto pt-BR) e ficam `unresolved` no relatório.
As bordas escritas aqui vêm do CONTEXT D-31..D-33/D-64 e da C8 da varredura: Cultura `pendente_humano` é
«aguardando revisão», nunca a nota da IA; Big Five tem `score` NULL por desenho e o estado sai da existência
da linha.

Fora deste plano, registrado: `score_geral` (0 de 38) segue escondido quando nulo (§Deferred); a forma
inversa da C8 (`classificacao_cor ?? 'verde'` na fila de revisão, `revisaoRedacaoService.ts:107-108`) fica
registrada, fora dos IDs.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-04-SUMMARY.md` when done
</output>
