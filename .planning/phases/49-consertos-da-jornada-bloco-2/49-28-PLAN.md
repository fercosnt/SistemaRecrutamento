---
phase: 49-consertos-da-jornada-bloco-2
plan: 28
type: execute
wave: 5
depends_on: ["49-12", "49-14"]
files_modified:
  - supabase/tests/p46_purga_smoke.sql
autonomous: true
requirements: [JORN-3b]

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A asserção (j.2) do `p46_purga_smoke` passa a ESTABELECER o estado inicial da vaga-semente (`ativa`) dentro do próprio envelope, em vez de LER o estado vivo de PROD como linha de base — e volta a reprovar por não-vacuidade só quando o predicado realmente não distingue vaga aberta de arquivada."
    - "PROD não muda: a vaga `4601d000-0000-4000-8000-000000000003` permanece `arquivada` depois da execução, e `anon` continua vendo 2 vagas. O operador recusou explicitamente o UPDATE retroativo porque ele publicaria `fixture-p46 vaga ativa (sintetica)` no site."
    - "As asserções (o), (o.6), (o.7) e (p) — o 4º ramo do guard do motor de exclusão — voltam a ser exercitadas: hoje o fail-fast de (j.2) as impede de rodar, e é por isso que elas entram NÃO exercitadas no 49-19."
    - "O contador final do smoke volta ao total esperado. O 49-14 mediu 27/27 com a vaga devolvida a `ativa` dentro de requisição que aborta — esse é o alvo, mas o número tem de ser LIDO da execução, não transcrito daqui."
    - "A mesma forma é varrida no arquivo inteiro: toda asserção que LÊ o estado vivo como linha de base (em vez de estabelecê-lo) é listada no SUMMARY, com veredito de se é ESCOPO deliberado ou FOTOGRAFIA que vai envelhecer. Consertar só a (j.2) e declarar o arquivo saudável repetiria o defeito uma asserção adiante."
    - "Nenhuma migration, nenhuma EF, nenhum `src/`. Este plano toca UM arquivo de teste."
  artifacts:
    - path: supabase/tests/p46_purga_smoke.sql
      provides: "(j.2) independente do estado vivo de PROD"
      contains: "j.2"
  key_links:
    - from: "(j.2) estabelece `ativa` no envelope"
      to: "(o), (o.6), (o.7), (p) voltam a ser alcançadas"
      via: "o fail-fast deixa de disparar"
      pattern: "j\\.2"
  prohibitions:
    - requirement_id: JORN-3b
      category: integrity
      status: resolved
      verification: automated
      statement: "MUST NOT deixar uma asserção de não-vacuidade depender de um estado de PROD que ninguém garante"
---

<objective>
A asserção que prova «vaga aberta protege» para de depender de a vaga estar aberta em PROD.

Purpose: o 49-14 mediu que `p46_purga_smoke (j.2)` reprova porque a vaga-semente está `arquivada` desde 2026-08-23,
e que isso bloqueia (o), (o.6), (o.7) e (p) — justamente as que exercitam o 4º ramo do guard do motor de exclusão,
na véspera do 49-19, que é quem roda o motor de verdade. O 49-12 levou o conserto ao operador, que **recusou o UPDATE
retroativo**: ele publicaria `fixture-p46 vaga ativa (sintetica)` no site (medido com `SET LOCAL ROLE ROLE anon`:
o público passaria de 2 para 3 vagas). Este plano é a alternativa que o operador escolheu.

A causa, lida no arquivo: o bloco (j.2) faz `SELECT v.status INTO v_j2_status` — **lê** o estado vivo como linha de
base —, mede, arquiva, mede de novo e repõe o que leu. É auto-reversível e roda dentro do envelope que aborta; o
defeito não é escrita vazada, é que ele **não estabelece** o estado de partida. Com a vaga já `arquivada`, as duas
medições ocorrem no mesmo estado, o delta é zero e a metade de não-vacuidade reprova — corretamente, sobre uma
premissa que ninguém garante.

É a forma que o CLAUDE.md §«Portões: varra pela FORMA» descreve: um instantâneo do estado de PROD apresentado como
invariante. O conserto é o que aquele texto prescreve — baseline capturada na própria execução, nunca lida do ambiente.

Output: `(j.2)` estabelecendo `ativa` no envelope, o contador de volta ao total, e a varredura de forma do arquivo.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-14-SUMMARY.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-12-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: (j.2) estabelece o estado que ela mede</name>
  <precondition>A vaga `4601d000-0000-4000-8000-000000000003` está `arquivada` em PROD e o `p46_purga_smoke` reprova em (j.2) — MEDIR os dois antes de editar, para não consertar um sintoma que mudou.</precondition>
  <files>supabase/tests/p46_purga_smoke.sql</files>
  <read_first>
    - `supabase/tests/p46_purga_smoke.sql` (`:793-808` — o bloco (j.2); `:790` — como (j.1) ESTABELECE o estado com `UPDATE retencao_hold SET liberado_em = NULL` em vez de o ler, que é o molde certo já presente no arquivo; `:1303-1312` — o TEARDOWN por `RAISE EXCEPTION … P46B0`, que é o envelope; `:180` — a asserção (z) do resumo, que exige o total exato)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-14-SUMMARY.md` (o 49-14 já reproduziu a falha e já mediu o portão cheio com a vaga devolvida a `ativa` dentro de requisição que aborta — reaproveite o método, não repita a investigação)
  </read_first>
  <action>
**Medir primeiro:** rodar o smoke como está e registrar a falha literal; ler o status vivo da vaga; contar as vagas
visíveis a `anon`. Esses três números são a linha de base do SUMMARY.

**Consertar (j.2)** seguindo o molde que (j.1) já usa no mesmo arquivo: **estabelecer** o estado de partida dentro do
envelope — `UPDATE public.vagas SET status = 'ativa' …` antes da primeira medição — em vez de `SELECT … INTO
v_j2_status`. Manter as duas metades da asserção (protege aberta; deixa de proteger arquivada), que são o que dá valor
a ela, e manter a reposição ao estado ORIGINAL de PROD no fim do bloco, lido antes de qualquer escrita, para que o
envelope e a reposição concordem.

Depois do conserto, **provar por execução** que PROD não mudou: a vaga segue `arquivada` e `anon` segue vendo 2 vagas.
Isto não é formalidade — é a condição que o operador impôs ao recusar o UPDATE.

**Provar que (j.2) ainda MORDE**, e aqui há duas mutações distintas, porque a asserção tem duas metades:
(1) fazer o predicado ignorar `status` da vaga ⇒ a metade «deixa de proteger» reprova;
(2) remover o `UPDATE … 'ativa'` recém-acrescentado ⇒ a asserção volta a reprovar por não-vacuidade, o que prova que
o conserto é o que a mantém significativa. Um portão que você tornou incapaz de falhar é pior que o quebrado.
⚠ Descontar códigos ANSI ao extrair contagem e conferir o exit code do runner: PATTERNS §L — duas rodadas de mutação
desta fase saíram «0 reprovados» por bug do harness, não do portão.

**Ler o contador final da execução** e conferir contra a asserção (z); se o total tiver mudado por causa deste plano,
atualizar (z) e dizer por quê. Não transcrever «27» deste plano: o número vem da execução.

**Varrer o arquivo inteiro pela FORMA** — toda asserção que lê o estado vivo como linha de base em vez de estabelecê-lo.
Para cada achado, dizer no SUMMARY se é escopo deliberado ou fotografia que vai envelhecer. Consertar só as que são
fotografia E estão no caminho das asserções bloqueadas; as demais ficam registradas, não consertadas (Scope Boundary).

Commitar e `git push origin main`; `origin/main..HEAD` vazio.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs run supabase/tests/p46_purga_smoke.sql 2>&1 | tee /dev/stderr | grep -qE "P46P_REG_CONTADOR=[0-9]+" && ! node p46apply.cjs run supabase/tests/p46_purga_smoke.sql 2>&1 | grep -qE "P46P FAIL"</automated>
    <fails_when>the smoke still reports any "P46P FAIL", or no final counter line is emitted</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs sql "select status::text as s from public.vagas where id='4601d000-0000-4000-8000-000000000003'" 2>&1 | grep -q arquivada && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>the seed vaga is no longer `arquivada` in PROD (the operator's condition), or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - (j.2) estabelece `ativa` no envelope; nenhuma asserção depende do status vivo como linha de base.
    - Smoke sem nenhum `P46P FAIL`; contador lido da execução e conciliado com (z).
    - (o), (o.6), (o.7) e (p) executadas — nomeadas no SUMMARY como alcançadas.
    - PROD intocado: vaga `arquivada`, `anon` vendo 2 vagas, ambos medidos depois.
    - As duas mutações provam que (j.2) morde; varredura de forma do arquivo no SUMMARY.
  </acceptance_criteria>
  <done>A asserção prova o predicado, não o estado em que PROD calhou de estar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke → `vagas` (dentro do envelope) | a escrita de estabelecimento vive e morre na subtransação que aborta |
| PROD → público (`anon`) | nenhuma vaga sintética passa a ser visível |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-28-01 | Tampering | vaga sintética vazar para o público | **high** | mitigate | escrita só no envelope; `anon` contado depois da execução |
| T-49-28-02 | Repudiation | asserção incapaz de falhar depois do conserto | medium | mitigate | mutação (2) remove o estabelecimento e a asserção reprova |
| T-49-28-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Smoke sem `P46P FAIL`, contador lido; vaga `arquivada` e `anon` = 2 vagas medidos DEPOIS; as duas mutações mordem; varredura de forma registrada; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
O 4º ramo do guard do motor de exclusão entra no 49-19 exercitado, e nenhuma vaga sintética foi publicada para consegui-lo.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `(j.2)` de `p46_purga_smoke.sql` | teste (asserção) | editado |

## Premissas sinalizadas

Por que a vaga foi para `arquivada` em 2026-08-23 **não** é investigado aqui: o bloco (j.2) é auto-reversível e roda
dentro de envelope que aborta, então não foi ele. Se a varredura de forma achar o escritor, registrar; caçá-lo não é
condição para o conserto, que torna a asserção imune à causa de qualquer modo.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-28-SUMMARY.md` when done
</output>
