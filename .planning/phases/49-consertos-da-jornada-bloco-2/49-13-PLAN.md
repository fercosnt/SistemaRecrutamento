---
phase: 49-consertos-da-jornada-bloco-2
plan: 13
type: execute
wave: 3
depends_on: ["49-08", "49-02"]
files_modified:
  - src/features/triagem/services/triagemService.ts
  - src/features/triagem/services/__tests__/triagemService.test.ts
  - src/features/triagem/components/ProvenienciaIABadge.tsx
  - src/features/triagem/components/__tests__/ProvenienciaIABadge.test.tsx
  - src/features/triagem/components/ComparativoScreen.tsx
  - src/features/triagem/components/__tests__/ComparativoScreen.test.tsx
  - src/components/pages/ComparativoCandidatosPage.tsx
  - src/components/pages/__tests__/ComparativoCandidatosPage.test.tsx
  - src/features/triagem/pdf/exportComparativo.ts
  - src/features/triagem/pdf/__tests__/exportComparativo.test.ts
autonomous: true
requirements: [JORN-25, JORN-28]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "O ranking do comparativo da vaga rotula cada posição pela chave que a EF devolve (`posicoes: { C1: candidatura_id, … }`, plano 49-08), nunca pela posição na seleção; posição sem entrada em `posicoes` mostra o rótulo cru, nunca o nome do vizinho (Discretion obrigatória do 25). A mesma regra na tela da decisão final é do plano 49-22."
    - "D-27b: existe UM componente `ProvenienciaIABadge` (molde `SugestaoIABadge`, copy canônica exportada) que, dado `provedor_ia`/`modelo_ia` (e a causa, quando houver), mostra «gerado pelo modelo de contingência» com o modelo real quando `provedor_ia = 'openai'` (o primário do `callAi` é sempre Anthropic — P1 fora) e «modelo não registrado» quando `modelo_ia` é NULL (D-30); nunca silêncio. É o mesmo componente que as telas da decisão final (49-22), redação, guia, análise da triagem e análise de entrevista usam (49-15/49-16)."
    - "D-27b no PDF: o PDF exportado do comparativo imprime, abaixo do título, o modelo que gerou o ranking e, se foi fallback, o aviso de contingência com a causa legível (`CAUSA_FALLBACK_ROTULO` de `_shared/ai-error-codes.ts`)."
    - "`invokeComparativo` devolve `posicoes`, `provedor_ia`, `modelo_ia`, `fallback_cause` e mapeia as recusas novas da EF para cópia verdadeira: `ENCERRADA`, `SEM_ANALISE`, `VALIDATION` (teto), `FORBIDDEN`. Os campos são só ACRESCENTADOS ao tipo: a `DecisaoFinalPage` continua compilando e funcionando como hoje até o 49-22."
    - "A invariante da Phase 45 de `ComparativoScreen.test.tsx:121` (retirada segue visível) continua verde (D-56)."
    - "Texto novo passa pelo guard `forbidden-strings.grep.test.ts` (D-58, D-07); `tsc` ≤ 90; build com o marcador no chunk certo; push; marcador no ar."
  artifacts:
    - path: src/features/triagem/components/ProvenienciaIABadge.tsx
      provides: "selo de proveniência único (comparativo, decisão final, redação, guia, análises)"
      contains: "proveniencia-ia-badge"
    - path: src/components/pages/ComparativoCandidatosPage.tsx
      provides: "rótulo por posicoes"
      contains: "posicoes"
    - path: src/features/triagem/pdf/exportComparativo.ts
      provides: "linha de proveniência abaixo do título"
      contains: "proveniencia"
  key_links:
    - from: "EF comparativo-candidatos (resposta)"
      to: "triagemService.invokeComparativo → ComparativoCandidatosPage → ComparativoScreen → exportComparativo"
      via: "posicoes + provedor_ia + modelo_ia + fallback_cause"
      pattern: "posicoes\\["
    - from: "supabase/functions/_shared/ai-error-codes.ts (49-02)"
      to: "ProvenienciaIABadge / exportComparativo"
      via: "import relativo de CAUSA_FALLBACK_ROTULO (precedente exportacaoService.ts:61)"
      pattern: "CAUSA_FALLBACK_ROTULO"
  prohibitions:
    - requirement_id: JORN-28
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT exibir resultado de fallback, na tela ou no PDF, sem dizer que veio do modelo de contingência (D-27b)"
    - requirement_id: JORN-25
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT introduzir ocorrência literal nova do endereço do canal de privacidade nem a expressão «teste psicológico» em texto novo (D-07, D-58 — a única exceção é o disclaimer negado da devolutiva, já decidido)"
---

<objective>
O comparativo da vaga passa a rotular cada posição pelo id que a IA ranqueou e a mostrar — na tela e no PDF — quando o
ranking veio do modelo de contingência; o selo de proveniência nasce aqui para as outras telas usarem.

Purpose: JORN-25 (rótulo por posição — `ComparativoCandidatosPage.tsx:66-80`) e JORN-28 (D-27b, selo na tela e no
PDF). É o passo «cliente» da sequência cujo passo «EF» é o 49-08 (D-55). Primeira de duas fatias do 49-13 original
(revisão do checker, W1): a seleção sem encerrada, o teto da constante no front, o «Avançar» real e a decisão final
(D-34, D-36, D-36b, D-59) são do 49-22, que parte do que este plano deixa em `ComparativoCandidatosPage` e
`ComparativoScreen`.

Output: o selo de proveniência, o service com o contrato novo, a tela e o PDF do comparativo — publicados.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-08-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): a resposta nova da EF até a tela — rótulo por `posicoes` e o selo de proveniência</name>
  <precondition>A EF `comparativo-candidatos` do 49-08 está no ar (o bundle vivo contém `SEM_ANALISE` — conferido no SUMMARY do 49-08).</precondition>
  <files>src/features/triagem/services/triagemService.ts, src/features/triagem/services/__tests__/triagemService.test.ts, src/features/triagem/components/ProvenienciaIABadge.tsx, src/features/triagem/components/__tests__/ProvenienciaIABadge.test.tsx, src/features/triagem/components/ComparativoScreen.tsx, src/features/triagem/components/__tests__/ComparativoScreen.test.tsx, src/components/pages/ComparativoCandidatosPage.tsx, src/components/pages/__tests__/ComparativoCandidatosPage.test.tsx</files>
  <read_first>
    - `src/features/triagem/services/triagemService.ts` (`:299-345` `invokeComparativo`; o helper `extractEfErrorCode`)
    - `src/features/triagem/hooks/useComparativo.ts` (o tipo de `data`)
    - `src/features/triagem/components/SugestaoIABadge.tsx` (o molde inteiro: docblock, copy exportada, variantes)
    - `src/features/triagem/components/ComparativoScreen.tsx` e `__tests__/ComparativoScreen.test.tsx` (`:121` — a invariante da Phase 45)
    - `src/components/pages/ComparativoCandidatosPage.tsx` (inteiro — `resolveCandidates :63-80`, efeito `:97-104`, `handleAvancar :118-126`)
    - `src/features/decisao/components/DecisaoFinalPage.tsx` (só para conferir que o tipo novo não a quebra — o conserto dela é do 49-22)
    - `supabase/functions/_shared/ai-error-codes.ts` (plano 49-02 — `CAUSA_FALLBACK_ROTULO`) e `supabase/functions/_shared/comparativo-config.ts` (49-08)
    - `src/features/privacidade/services/exportacaoService.ts` (`:57-61` — import relativo de `_shared` pelo front, com o comentário do porquê)
  </read_first>
  <behavior>
    - `resolveCandidates(ranked, posicoes, selection)`: com `posicoes = { C1: 'b', C2: 'a' }` e seleção `[a, b]`, C1 resolve para o nome de `b` e C2 para o de `a`; `candidate_id` sem entrada em `posicoes` mostra o próprio `candidate_id`.
    - `ProvenienciaIABadge`: `provedor_ia='openai'`, `modelo_ia='gpt-4o-mini-2024'`, causa `anthropic_max_tokens` ⇒ texto de contingência com o modelo e «não coube»; `provedor_ia='anthropic'` ⇒ não renderiza aviso de contingência; `modelo_ia=null` ⇒ «modelo não registrado».
    - `invokeComparativo`: 400 `ENCERRADA` ⇒ `TriagemServiceError` com a cópia de encerrada; `SEM_ANALISE` ⇒ cópia de análise pendente; resposta 200 ⇒ devolve `posicoes`, `provedor_ia`, `modelo_ia`, `fallback_cause`.
    - `ComparativoScreen` com `provedor_ia='openai'` ⇒ o selo aparece acima do ranking; a invariante `:121` da Phase 45 continua.
  </behavior>
  <action>
Passo 1 — D-50: re-rodar os padrões P3 da C1 (literais de teto) e C7 #7 (`replace(/\D/g` no front) do `49-VARREDURA-KICKOFF.md` e registrar o delta; os literais de teto e o `PROXIMA_ETAPA_APOS_TRIAGEM` ficam anotados como escopo do 49-22.

Passo 2 — `triagemService.invokeComparativo`: tipo de retorno `{ ranking, posicoes: Record<string,string>, provedor_ia: string | null, modelo_ia: string | null, fallback_cause: string | null, latencia_ms?: number }` (só acréscimos — a `DecisaoFinalPage` segue compilando); mapear `ENCERRADA` («Uma das candidaturas selecionadas está encerrada e não entra no comparativo.»), `SEM_ANALISE` («Ainda não há análise de IA para todos os selecionados. Aguarde a análise ou reprocesse.»), `VALIDATION` (mensagem com o teto vindo da constante `COMPARATIVO_MAX_CANDIDATOS`), `FORBIDDEN` (genérica), mantendo `MIXED_VAGA` e `AI_UNAVAILABLE`. Atualizar o tipo do hook se necessário.

Passo 3 — criar `ProvenienciaIABadge.tsx` (export nomeado; docblock com D-27b/D-30, a regra «fallback ⇔ `provedor_ia='openai'` enquanto o primário do `callAi` for Anthropic — se a P1 trocar o primário, esta regra muda aqui», e que é o selo único das telas; `PROVENIENCIA_IA_COPY` exportada; `data-testid="proveniencia-ia-badge"`; variante `compact`/`full`; a causa legível importada de `supabase/functions/_shared/ai-error-codes.ts` por caminho relativo, com o comentário do precedente). Teste do `<behavior>`.

Passo 4 — `ComparativoCandidatosPage.tsx`: `resolveCandidates` passa a receber `posicoes` e resolver `C<n>` → `candidatura_id` → item da seleção POR ID (nunca por índice); exportá-la (ou testá-la pela página) em `src/components/pages/__tests__/ComparativoCandidatosPage.test.tsx` (novo), com seleção em ordem diferente do ranking; passar `provedor_ia`/`modelo_ia`/`fallback_cause` ao `ComparativoScreen`, que mostra o selo acima do ranking. Testes: `ComparativoScreen.test.tsx` (selo presente em fallback; a invariante `:121` da Phase 45 continua) e o de `triagemService.test.ts`. Rodar `npx vitest run src/features/triagem src/components/pages` e o portão `tsc`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/features/triagem src/components/pages</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const p=fs.readFileSync('src/components/pages/ComparativoCandidatosPage.tsx','utf8');const code=p.split('\n').filter(l=>!/^\s*(\/\/|\*)/.test(l)).join('\n');if(/replace\(\/\\\\D\/g/.test(code))throw new Error('ComparativoCandidatosPage ainda resolve C{n} por posicao');if(!/posicoes/.test(code))throw new Error('pagina nao usa posicoes');console.log('OK')" && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; }</automated>
    <fails_when>non-zero exit with "ainda resolve C{n} por posicao" or "nao usa posicoes"; lint exit code other than 0/2 or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `ProvenienciaIABadge` existe com teste; o comparativo da vaga mostra o selo em fallback.
    - `resolveCandidates` resolve por `posicoes` → id (teste com seleção em ordem diferente do ranking).
    - `invokeComparativo` devolve os campos novos e mapeia as 4 recusas (teste).
    - `vitest` das pastas tocadas verde (invariante `:121` inclusive); `tsc` ≤ 90.
  </acceptance_criteria>
  <done>O RH lê no comparativo o nome certo em cada posição e sabe, na tela, quando o ranking não veio do modelo configurado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: o PDF leva a proveniência — e a publicação</name>
  <files>src/features/triagem/pdf/exportComparativo.ts, src/features/triagem/pdf/__tests__/exportComparativo.test.ts, src/features/triagem/components/ComparativoScreen.tsx</files>
  <read_first>
    - `src/features/triagem/pdf/exportComparativo.ts` (inteiro — título em `:68`)
    - `src/features/triagem/components/ComparativoScreen.tsx` (`:136-150` — o import dinâmico e a chamada `exportComparativo(candidates)`)
    - `src/features/triagem/components/ProvenienciaIABadge.tsx` (Task 1 — `PROVENIENCIA_IA_COPY`, para o PDF usar a mesma cópia)
  </read_first>
  <behavior>
    - `exportComparativo(candidates, proveniencia)`: o `doc.text` logo abaixo do título contém o modelo; em fallback, o aviso e a causa legível; com `modelo_ia` nulo, «modelo não registrado».
  </behavior>
  <action>
`exportComparativo(candidates, proveniencia)` imprime a linha de proveniência abaixo do título (`doc.text(…, 14, 20)` ou a posição que o layout aceitar — ajustar o `startY` da tabela), com a MESMA cópia de `PROVENIENCIA_IA_COPY` e a causa de `CAUSA_FALLBACK_ROTULO`. `ComparativoScreen` repassa ao PDF a proveniência que já recebe (Task 1). Teste novo `__tests__/exportComparativo.test.ts` com `jspdf` mockado (texto da proveniência capturado). Rodar `npx vitest run src/features/triagem src/__tests__/guards` e o portão `tsc`.

Publicar (D-52): `npm run build`; conferir `proveniencia-ia-badge` em `build/assets/` e anotar o chunk (as rotas `/rh/*` são lazy); commitar; `git push origin main`; `origin/main..HEAD` vazio; marcador no ar pelo crawler.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/features/triagem src/__tests__/guards && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; }</automated>
    <fails_when>non-zero exit, "failed" in the vitest summary, lint exit code other than 0/2 or more than 90 "error TS" lines</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npm run build >/dev/null && grep -rl "proveniencia-ia-badge" build/assets/</automated>
    <fails_when>non-zero exit (build failed, or no file in build/assets/ contains the marker)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && node -e "const M=process.argv[1],B='https://rh.beautysmile.com.br';(async()=>{const vis=new Set(),fila=['/'];let ok=false;while(fila.length&&vis.size<300){const p=fila.shift();if(vis.has(p))continue;vis.add(p);let t='';try{t=await (await fetch(B+p)).text()}catch{continue}if(t.includes(M)){ok=true;break}for(const m of t.matchAll(/(?:\/assets\/|\.\/)([\w.-]+\.js)/g))fila.push('/assets/'+m[1])}if(!ok){console.error('AUSENTE em PROD: '+M);process.exit(1)}console.log('PRESENTE em PROD: '+M)})()" "proveniencia-ia-badge"</automated>
    <fails_when>non-zero exit: unpushed commits remain, or "AUSENTE em PROD: proveniencia-ia-badge"</fails_when>
  </verify>
  <acceptance_criteria>
    - O PDF imprime a proveniência (teste com `jspdf` mockado), com a mesma cópia da tela.
    - `vitest` da triagem e o guard verdes; `tsc` ≤ 90; `proveniencia-ia-badge` no build e no ar; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>O PDF do comparativo que circula fora do sistema diz qual modelo ranqueou — e o selo está no ar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do RH → EF `comparativo-candidatos` | a tela só deixa de oferecer; a EF recusa (49-08) |
| resposta da EF → PDF baixado pelo RH | o PDF circula fora do sistema; a proveniência tem de ir junto |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-13-01 | Repudiation | nome trocado na posição do ranking (empate de score) | high | mitigate | rótulo por `posicoes` → id; teste com seleção em outra ordem |
| T-49-13-02 | Repudiation | PDF de fallback sem aviso circulando como decisão do modelo configurado | medium | mitigate | linha de proveniência no PDF; teste com `jspdf` mockado |
| T-49-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `vitest` de `src/features/triagem`, `src/components/pages`, `src/__tests__/guards` verde; `tsc` ≤ 90.
- `proveniencia-ia-badge` no build e no ar; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
O comparativo que o RH lê — na tela e no PDF — diz quem é cada posição e qual modelo ranqueou.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `src/features/triagem/components/ProvenienciaIABadge.tsx` (`ProvenienciaIABadge`, `PROVENIENCIA_IA_COPY`, `data-testid="proveniencia-ia-badge"`) | componente | **novo** |
| `src/features/triagem/components/__tests__/ProvenienciaIABadge.test.tsx` | teste | **novo** |
| `src/components/pages/__tests__/ComparativoCandidatosPage.test.tsx` | teste | **novo** (o 49-22 estende) |
| `src/features/triagem/pdf/__tests__/exportComparativo.test.ts` | teste | **novo** |
| `exportComparativo(candidates, proveniencia)` | função | editada (assinatura) |
| `invokeComparativo` → `{ ranking, posicoes, provedor_ia, modelo_ia, fallback_cause }` | service | editado |
| `resolveCandidates` por `posicoes` | helper de página | editado |

## Premissas sinalizadas (probe de borda)

JORN-25 e JORN-28 saíram `unclassified` no probe e ficam `unresolved` no relatório. A regra «fallback ⇔
`provedor_ia='openai'`» depende de o primário do `callAi` ser Anthropic; se a P1 trocar o primário, o selo muda
junto (registrado no docblock do componente). Entre este plano e o 49-22 a seleção ainda mostra o teto antigo e o
«Avançar» ainda grava a etapa fixa: a EF já recusa acima de 4 e encerrada (49-08, com a cópia mapeada aqui) e a
trava do banco recusa mover encerrada (49-06) — nenhum dos dois estados intermediários grava algo errado.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-13-SUMMARY.md` when done
</output>
