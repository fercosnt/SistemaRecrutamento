---
phase: 49-consertos-da-jornada-bloco-2
plan: 16
type: execute
wave: 4
depends_on: ["49-10", "49-11", "49-13", "49-24"]
files_modified:
  - src/features/entrevista/services/entrevistaService.ts
  - src/features/entrevista/__tests__/entrevista-allowlist.test.ts
  - src/features/entrevista/__tests__/entrevista-contract.test.ts
  - src/features/entrevista/components/EntrevistaWorkspace.tsx
  - src/features/entrevista/components/TranscricaoReviewPanel.tsx
  - src/features/entrevista/components/__tests__/TranscricaoReviewPanel.test.tsx
  - src/features/entrevista/components/GuiaEntrevistaPanel.tsx
  - src/features/entrevista/components/__tests__/GuiaEntrevistaPanel.test.tsx
  - src/features/hub-candidato/services/analiseCandidatoService.ts
  - src/features/hub-candidato/services/__tests__/analiseCandidatoService.test.ts
  - src/features/hub-candidato/components/AnaliseIABlock.tsx
  - src/features/hub-candidato/components/__tests__/AnaliseIABlock.test.tsx
autonomous: true
requirements: [JORN-12, JORN-28]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-41: a aba da transcrição tem o seletor online/presencial — o mesmo par de opções da aba do guia (`GuiaEntrevistaPanel.tsx:544-575`) —, com a etapa atual como padrão; `analisarTranscricao` manda `tipo` à EF (que aceita o campo desde o 49-10, Pitfall 4)."
    - "D-39/D4: a aba mostra a análise VIGENTE de cada tipo e, acessíveis, as anteriores marcadas como superadas (com a data em que foram superadas), e as que falharam como falha — nunca a mais nova de qualquer estado como se fosse a vigente."
    - "D-42: a análise superada mostra a revisão humana que teve (quem revisou — pelo nome do RH quando a tela já o resolve, senão «revisada por um membro da equipe» — e quando), e a vigente nova aparece como aguardando revisão."
    - "A revisão humana é oferecida SÓ sobre a análise que `salvar_avaliacao_entrevista` grava (a vigente mais recente, 49-10) — a tela não oferece revisar uma superada nem uma falha; o scorecard inline lê as competências dessa mesma vigente."
    - "D-40: quando a EF devolve `reaproveitada: true`, a tela diz que aquele texto já tinha sido analisado (e se aquela análise é a vigente) em vez de simular uma análise nova; quando devolve `falhou: true`, diz que a análise não pôde ser concluída e que a vigente anterior continua valendo."
    - "D-27b: o `ProvenienciaIABadge` (49-13) aparece na análise de entrevista, no guia de entrevista e na análise da triagem do hub (`AnaliseIABlock`), a partir de `provedor_ia`/`modelo_ia` lidos por allowlist (`ENTREVISTA_ANALISE_ALLOWLIST`, `ENTREVISTA_ALLOWLIST`, `ANALISE_HUB_ALLOWLIST` ganham só essas colunas e as de vigência) — nunca `select('*')`."
    - "O `getGuia` segue sem filtro de tipo (§Deferred, achado não perguntado ao operador)."
    - "Os testes que pinam as allowlists (`entrevista-allowlist.test.ts`) e o shape do body (`entrevista-contract.test.ts`) mudam DE PROPÓSITO com comentário de proveniência (D-56)."
    - "Texto novo passa pelo guard `forbidden-strings.grep.test.ts` (D-58, D-07); `tsc` ≤ 90; build; push; marcador no ar."
  artifacts:
    - path: src/features/entrevista/components/TranscricaoReviewPanel.tsx
      provides: "seletor de tipo, vigente por tipo, superadas com a revisão, falhas, selo"
      contains: "transcricao-tipo-seletor"
    - path: src/features/entrevista/services/entrevistaService.ts
      provides: "leitura por vigência; tipo no body"
      contains: "superada_em"
    - path: src/features/hub-candidato/components/AnaliseIABlock.tsx
      provides: "selo de proveniência na análise da triagem"
      contains: "ProvenienciaIABadge"
  key_links:
    - from: "TranscricaoReviewPanel (seletor de tipo)"
      to: "entrevistaService.analisarTranscricao → EF avaliar-transcricao-entrevista (49-10)"
      via: "body { candidatura_id, transcricao, tipo }"
      pattern: "tipo"
    - from: "entrevistaService.getAnalises"
      to: "entrevista_analises (vigência pelas colunas do 49-01)"
      via: "allowlist com superada_em, status_analise, tipo"
      pattern: "superada_em"
  prohibitions:
    - requirement_id: JORN-12
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT esconder a análise superada nem a revisão que ela teve — as anteriores ficam acessíveis (D4, D-42)"
    - requirement_id: JORN-12
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT apresentar como vigente uma análise que falhou (D-39)"
    - requirement_id: JORN-12
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT introduzir ocorrência literal nova do endereço do canal de privacidade nem a expressão «teste psicológico» em texto novo (D-07, D-58 — a única exceção é o disclaimer negado da devolutiva, já decidido)"
---

<objective>
A tela da entrevista passa a mostrar ao RH de qual entrevista é cada análise, qual é a vigente, quais foram superadas
(com a revisão que tiveram) e qual falhou — e as telas de resultado de IA mostram quando o texto veio do modelo de
contingência.

Purpose: D-41 (a aba da transcrição não tem seletor de tipo), D-39/D4/D-42 (a tela mostra a mais nova de qualquer
estado; a revisão anterior some), D-40 (a tela precisa dizer quando o texto já tinha sido analisado), D-27b (selo no
guia, na análise da triagem e na análise de entrevista). Passo «cliente» das sequências cujos passos «EF» são os
planos 49-10, 49-11 e 49-24 (proveniência do guia) (D-55).

Output: service com leitura por vigência e `tipo` no body, a aba da transcrição, os selos, publicados.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-10-SUMMARY.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-13-SUMMARY.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-24-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): o RH escolhe a entrevista, analisa, e vê a vigente e as superadas — da aba até a EF e de volta</name>
  <precondition>A EF `avaliar-transcricao-entrevista` do 49-10 está no ar (o bundle vivo contém `registrar_analise_entrevista` — conferido no SUMMARY do 49-10).</precondition>
  <files>src/features/entrevista/services/entrevistaService.ts, src/features/entrevista/__tests__/entrevista-allowlist.test.ts, src/features/entrevista/__tests__/entrevista-contract.test.ts, src/features/entrevista/components/EntrevistaWorkspace.tsx, src/features/entrevista/components/TranscricaoReviewPanel.tsx, src/features/entrevista/components/__tests__/TranscricaoReviewPanel.test.tsx</files>
  <read_first>
    - `src/features/entrevista/services/entrevistaService.ts` (allowlists `ENTREVISTA_ANALISE_ALLOWLIST`/`ENTREVISTA_ALLOWLIST`; `getAnalise :457-488`; `analisarTranscricao :663-700`; as chamadas de `salvar_avaliacao_entrevista`/`confirmar_revisao_entrevista`)
    - `src/features/entrevista/components/EntrevistaWorkspace.tsx` (`:195-220` — a aba `transcricao` e o `EntrevistaScorecardInline` que lê `analise.competencias`)
    - `src/features/entrevista/components/TranscricaoReviewPanel.tsx` (inteiro)
    - `src/features/entrevista/components/GuiaEntrevistaPanel.tsx` (`:544-575` — os dois CTAs online/presencial, o molde do seletor)
    - `src/features/entrevista/__tests__/entrevista-allowlist.test.ts` e `entrevista-contract.test.ts`
    - `supabase/functions/_shared/entrevista-schemas.ts` (o `tipo` opcional do 49-10) e a resposta da EF (`{ analise_id, tipo, reaproveitada, vigente, falhou }`)
    - `src/features/triagem/components/ProvenienciaIABadge.tsx` (49-13)
  </read_first>
  <behavior>
    - `getAnalises(candidatura)` devolve, a partir de linhas com `superada_em`/`status_analise`/`competencias`/`tipo`, a vigente de cada tipo (inclusive o grupo sem tipo das análises antigas), as superadas (ordenadas por data) e as falhas; `getAnalise(candidatura)` continua devolvendo UMA análise — a vigente mais recente —, que é a que o scorecard e a revisão usam.
    - `analisarTranscricao(id, texto, 'presencial')` manda `{ candidatura_id, transcricao, tipo: 'presencial' }` (o contrato passa no `AvaliarTranscricaoBodySchema` `.strict()`).
    - Painel com candidatura em `entrevista_online` ⇒ seletor com «Entrevista online» pré-selecionado (`data-testid="transcricao-tipo-seletor"`).
    - Painel com uma vigente e duas superadas (uma revisada) ⇒ mostra a vigente como «aguardando revisão», e as superadas acessíveis com a revisão (quem/quando) da revisada.
    - Resposta `reaproveitada: true, vigente: false` ⇒ mensagem de texto já analisado e de que a vigente é outra; `falhou: true` ⇒ mensagem de falha e a vigente anterior continua na tela.
    - O botão de revisar/confirmar só aparece na vigente mais recente.
    - `provedor_ia='openai'` na vigente ⇒ selo de contingência.
  </behavior>
  <action>
`entrevistaService.ts`: `ENTREVISTA_ANALISE_ALLOWLIST` ganha `tipo, superada_em, status_analise, texto_hash, provedor_ia, modelo_ia, revisada_por, revisao_confirmada_em, scores_humanos, notas_humanas` (conferir quais já estão; NÃO incluir `ai_call_log_id` nem `solicitado_por` — não são lidos pela tela); `getAnalises(candidaturaId)` novo, uma consulta só (sem N+1), que classifica as linhas pela MESMA regra do banco (vigente = não superada, não falhou, com competências — comentário apontando `public.entrevista_analise_vigente`); `getAnalise` passa a devolver a vigente mais recente (não a mais nova de qualquer estado); `analisarTranscricao(candidaturaId, transcricao, tipo)` inclui `tipo` no body e devolve a resposta nova da EF (`reaproveitada`, `vigente`, `falhou`). Atualizar DE PROPÓSITO `entrevista-allowlist.test.ts` e `entrevista-contract.test.ts` (body com `tipo`, e `.strict()` ainda recusando campo estranho), com comentário de proveniência.

`EntrevistaWorkspace.tsx` + `TranscricaoReviewPanel.tsx`: seletor de tipo no molde dos dois CTAs do guia (padrão = etapa atual); lista «vigente por tipo» + «anteriores (superadas)» + «falhas», cada uma com data; revisão da superada visível (D-42); ações de revisão só na vigente mais recente; mensagens de `reaproveitada`/`falhou`; selo de proveniência na vigente e nas superadas. Criar `__tests__/TranscricaoReviewPanel.test.tsx` com o `<behavior>` de tela. Rodar `npx vitest run src/features/entrevista src/__tests__/guards` e o portão `tsc`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/features/entrevista src/__tests__/guards</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('src/features/entrevista/services/entrevistaService.ts','utf8');for(const t of ['superada_em','getAnalises','tipo'])if(!s.includes(t))throw new Error('service sem: '+t);const p=fs.readFileSync('src/features/entrevista/components/TranscricaoReviewPanel.tsx','utf8');if(!p.includes('transcricao-tipo-seletor'))throw new Error('painel sem o seletor de tipo');console.log('OK')" && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; }</automated>
    <fails_when>non-zero exit with "service sem:" or "painel sem o seletor de tipo"; lint exit code other than 0/2 or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - O service lê por vigência numa consulta e manda `tipo`; os testes de allowlist e contrato mudaram com comentário.
    - A aba tem o seletor, a vigente por tipo, as superadas com a revisão e as falhas; revisão só na vigente mais recente; mensagens de `reaproveitada`/`falhou` (testes do `<behavior>`).
    - `vitest` de `src/features/entrevista` e o guard verdes; `tsc` ≤ 90.
  </acceptance_criteria>
  <done>O RH sabe de qual entrevista é a análise que está lendo, que ela é a vigente, e o que veio antes dela.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: o selo de proveniência no guia e na análise da triagem — e a publicação</name>
  <files>src/features/entrevista/services/entrevistaService.ts, src/features/entrevista/components/GuiaEntrevistaPanel.tsx, src/features/entrevista/components/__tests__/GuiaEntrevistaPanel.test.tsx, src/features/hub-candidato/services/analiseCandidatoService.ts, src/features/hub-candidato/services/__tests__/analiseCandidatoService.test.ts, src/features/hub-candidato/components/AnaliseIABlock.tsx, src/features/hub-candidato/components/__tests__/AnaliseIABlock.test.tsx</files>
  <read_first>
    - `src/features/entrevista/components/GuiaEntrevistaPanel.tsx` e seu teste
    - `src/features/entrevista/services/entrevistaService.ts` (`ENTREVISTA_ALLOWLIST` do guia — ganha `provedor_ia, modelo_ia`)
    - `src/features/hub-candidato/services/analiseCandidatoService.ts` (`:43` `ANALISE_HUB_ALLOWLIST`) e seu teste
    - `src/features/hub-candidato/components/AnaliseIABlock.tsx` e seu teste
    - `src/features/triagem/components/ProvenienciaIABadge.tsx` (49-13)
  </read_first>
  <behavior>
    - Guia com `provedor_ia='openai'` ⇒ selo de contingência; `modelo_ia=null` ⇒ «modelo não registrado».
    - `ANALISE_HUB_ALLOWLIST` inclui `provedor_ia, modelo_ia` e continua sem curinga; `AnaliseIABlock` com `provedor_ia='openai'` ⇒ selo.
  </behavior>
  <action>
`ENTREVISTA_ALLOWLIST` (guia) e `ANALISE_HUB_ALLOWLIST` ganham `provedor_ia, modelo_ia` (tipos atualizados); o `GuiaEntrevistaPanel` e o `AnaliseIABlock` renderizam o `ProvenienciaIABadge` perto do título do resultado de IA. Atualizar DE PROPÓSITO os testes que pinam as allowlists (comentário de proveniência) e acrescentar os do `<behavior>`. Rodar `npx vitest run src/features/entrevista src/features/hub-candidato src/features/triagem src/__tests__/guards` e o portão `tsc`.

Publicar (D-52): `npm run build`; conferir `transcricao-tipo-seletor` em `build/assets/` e anotar o chunk (rota `/rh/*`, lazy); commitar; `git push origin main`; `origin/main..HEAD` vazio; crawler com `transcricao-tipo-seletor`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/features/entrevista src/features/hub-candidato src/features/triagem src/__tests__/guards</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npm run build >/dev/null && grep -rl "transcricao-tipo-seletor" build/assets/ && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; }</automated>
    <fails_when>non-zero exit (build failed, marker absent from build/assets/, lint exit code other than 0/2, or more than 90 "error TS" lines)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && node -e "const M=process.argv[1],B='https://rh.beautysmile.com.br';(async()=>{const vis=new Set(),fila=['/'];let ok=false;while(fila.length&&vis.size<300){const p=fila.shift();if(vis.has(p))continue;vis.add(p);let t='';try{t=await (await fetch(B+p)).text()}catch{continue}if(t.includes(M)){ok=true;break}for(const m of t.matchAll(/(?:\/assets\/|\.\/)([\w.-]+\.js)/g))fila.push('/assets/'+m[1])}if(!ok){console.error('AUSENTE em PROD: '+M);process.exit(1)}console.log('PRESENTE em PROD: '+M)})()" "transcricao-tipo-seletor"</automated>
    <fails_when>non-zero exit: unpushed commits remain, or "AUSENTE em PROD: transcricao-tipo-seletor"</fails_when>
  </verify>
  <acceptance_criteria>
    - Guia e análise da triagem mostram o selo (testes); allowlists sem curinga.
    - `vitest` das pastas tocadas e o guard verdes; `tsc` ≤ 90; marcador no build e no ar; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>Todo resultado de IA que o RH lê — análise, guia, redação, análise de entrevista, comparativo — diz quando veio do modelo de contingência.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do RH → EF de transcrição | o texto é UNTRUSTED; `tipo` é enum |
| navegador do RH ← `entrevista_analises`/`entrevista_guias`/`analise_candidato_vaga` (RLS RH/admin) | leitura por allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-16-01 | Repudiation | RH lendo análise superada/falha como vigente | medium | mitigate | leitura por vigência com a mesma regra do banco; testes |
| T-49-16-02 | Information Disclosure | colunas técnicas/de funcionário no navegador | low | mitigate | allowlist sem `ai_call_log_id`/`solicitado_por`; sem curinga |
| T-49-16-03 | Tampering | revisão oferecida sobre análise que a RPC não grava | medium | mitigate | ações só na vigente mais recente; a RPC recusa superada (49-10) |
| T-49-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `vitest` de `src/features/entrevista`, `src/features/hub-candidato`, `src/features/triagem` e o guard verdes; `tsc` ≤ 90.
- `transcricao-tipo-seletor` no build e no ar; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
A tela da entrevista mostra, para cada entrevista, qual análise vale e o histórico das que vieram antes; e nenhum resultado de IA esconde que veio do modelo de contingência.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `getAnalises(candidaturaId)` | service | **novo** |
| `getAnalise` (vigente mais recente), `analisarTranscricao(…, tipo)` | service | editados |
| `ENTREVISTA_ANALISE_ALLOWLIST`, `ENTREVISTA_ALLOWLIST`, `ANALISE_HUB_ALLOWLIST` (+ proveniência/vigência) | allowlists | editadas |
| seletor de tipo (`data-testid="transcricao-tipo-seletor"`), lista vigente/superadas/falhas | componente | editado |
| `src/features/entrevista/components/__tests__/TranscricaoReviewPanel.test.tsx` | teste | **novo** |

## Premissas sinalizadas (probe de borda)

JORN-12 e JORN-28 saíram `unclassified` no probe e ficam `unresolved` no relatório. O grupo «sem tipo» das análises
antigas aparece como tal na tela (RESEARCH Correção 10). O nome de quem revisou só aparece se a tela já resolve RH por
id; senão, a frase genérica — nenhuma leitura nova de `usuarios_rh` entra por causa disto.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-16-SUMMARY.md` when done
</output>
