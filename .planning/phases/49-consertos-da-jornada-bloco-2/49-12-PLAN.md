---
phase: 49-consertos-da-jornada-bloco-2
plan: 12
type: execute
wave: 4
depends_on: ["49-06", "49-10"]
files_modified:
  - supabase/tests/p49_retroativos_ensaio.sql
  - supabase/migrations/20260922000009_p49_retro_justificativa_grudada.sql
  - supabase/migrations/20260922000010_p49_retro_trilha_bd9.sql
  - supabase/migrations/20260922000011_p49_retro_marca_analises.sql
autonomous: false
requirements: [JORN-17, JORN-37, JORN-12]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "As três escritas retroativas sobre linhas existentes são aprovadas pelo OPERADOR na execução, num checkpoint com a contagem e os ids medidos NA HORA — nunca silenciosas (D-54). A D-47 exige a aprovação no momento, explicitamente."
    - "Antes do checkpoint, um ENSAIO roda os MESMOS predicados e UPDATEs das migrations dentro de uma transação que sempre reverte, e reporta os conjuntos, as contagens e os efeitos colaterais (fila do `pg_net`, `historico_candidatura`, `notificacoes_enviadas`): o relatório do ensaio sai da mesma consulta do write. Conjunto diferente do medido na pesquisa é ACHADO levado ao checkpoint como está — o predicado não é ajustado para caber."
    - "D-46 / JORN-17: as candidaturas com `etapa_justificativa` não nula (9 na pesquisa: 5 `+claude`, 1 fixture, 2 do titular já anonimizado, 1 real `d31c78bb` com 5 caracteres) passam a NULL — só se aprovado, só se cada valor bater byte a byte (md5) com o `criterio_texto` mais recente do histórico da mesma candidatura (o texto não se perde); depois, `select count(*) from public.candidaturas where etapa_justificativa is not null` = 0. O UPDATE não muda `etapa_atual`, então o `avancar_etapa` nem dispara e nenhuma linha de histórico nasce."
    - "D-47 / JORN-37: as linhas de `historico_candidatura` cujo `criterio_texto` é igual à justificativa da decisão final da mesma candidatura — na linha corrente de `decisao_final` OU em qualquer versão de `decisao_final_historico` (RESEARCH Correção 11: 5 linhas em 4 candidaturas, não 4) — passam a ter `criterio_texto = 'Decisão final registrada.'` (a mesma constante que o `registrar_decisao` grava desde o 49-06); o texto continua em `decisao_final.justificativa`, que é a fonte (BD-9). Depois, a consulta de seleção devolve 0."
    - "D-43 / JORN-12: as 6 análises de entrevista existentes (3 candidaturas, todas de teste) são MARCADAS: em cada candidatura, a mais recente bem-sucedida fica vigente e as anteriores recebem `superada_em`; `texto_hash`/`ai_call_log_id` são preenchidos onde a correspondência com o `ai_call_logs` é reconstruível (5 de 6 na pesquisa, pela igualdade de competências/citações) e ficam NULL onde não é; `tipo`, `solicitado_por`, `provedor_ia`, `modelo_ia` ficam NULL (desconhecidos — D-30). Depois, nenhuma `(candidatura, tipo)` tem mais de uma vigente."
    - "Nenhuma das escritas apaga linha, nenhuma dispara e-mail (a fila do `pg_net`, o `historico_candidatura` fora da D-47 e o `notificacoes_enviadas` são conferidos antes e depois, na mesma requisição), e cada uma é uma migration própria com o conjunto AUTORIZADO literal (`v_autorizados`, escopo deliberado da autorização — não fotografia), que aborta se o conjunto medido na hora divergir (D-54)."
    - "D-45: os 5 snapshots sem mudança de `decisao_final_historico` NÃO são tocados. D-26/D-30: redações e resultados antigos também não."
    - "Escrita recusada pelo operador não gera arquivo nem apply; a recusa fica no SUMMARY."
  artifacts:
    - path: supabase/tests/p49_retroativos_ensaio.sql
      provides: "ensaio revertido com os três conjuntos, as contagens e os efeitos colaterais"
      contains: "ENSAIO OK"
    - path: supabase/migrations/20260922000009_p49_retro_justificativa_grudada.sql
      provides: "D-46 (só se aprovado)"
      contains: "v_autorizados"
    - path: supabase/migrations/20260922000010_p49_retro_trilha_bd9.sql
      provides: "D-47 (só se aprovado)"
      contains: "Decisão final registrada."
    - path: supabase/migrations/20260922000011_p49_retro_marca_analises.sql
      provides: "D-43 (só se aprovado)"
      contains: "superada_em"
  key_links:
    - from: "supabase/tests/p49_retroativos_ensaio.sql"
      to: "as três migrations retroativas"
      via: "o MESMO predicado de seleção, copiado"
      pattern: "etapa_justificativa|criterio_texto|superada_em"
  prohibitions:
    - requirement_id: JORN-17
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT executar escrita retroativa sem a aprovação explícita do operador naquele momento, nem fora dos ids medidos e aprovados (D-54)"
    - requirement_id: JORN-37
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT apagar linha de histórico nem de análise — a trilha é editada só no texto da decisão (D-47) e as análises só são marcadas (D-43)"
    - requirement_id: JORN-12
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT disparar e-mail a candidato por causa destas correções"
---

<objective>
Levar ao operador, com ensaio revertido e contagem medida na hora, as três correções retroativas que a fase
decidiu — limpar as justificativas grudadas (D-46), tirar da trilha as cópias da justificativa da decisão final
(D-47) e marcar vigente/superada nas análises de entrevista existentes (D-43) — e executar só as aprovadas.

Purpose: D-54 separa aditivo (sem pausa) de UPDATE retroativo sobre linha existente (checkpoint com contagem
antes/depois). Os consertos de código que impedem novas ocorrências já estão no ar (49-06: trigger limpa e
`registrar_decisao` não copia; 49-10: RPC marca). Aqui só se corrige o resíduo.

Output: script de ensaio; aprovação do operador; até três migrations com portão de conjunto, aplicadas.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): o ensaio — os três conjuntos, medidos pela mesma consulta do write, numa transação que sempre reverte</name>
  <precondition>Os planos 49-06 e 49-10 estão em PROD: `pg_get_functiondef('public.avancar_etapa()'::regprocedure)` contém `NEW.etapa_justificativa := NULL` e `to_regproc('public.registrar_analise_entrevista')` não é nulo.</precondition>
  <files>supabase/tests/p49_retroativos_ensaio.sql</files>
  <read_first>
    - `supabase/tests/p48_retroativos_ensaio.sql` (o molde inteiro: mesmos UPDATEs da escrita real, contagens de efeito colateral, o «controle que prova que a medida da fila morde» `:32`, a saída `ENSAIO OK: …` no texto do erro)
    - `supabase/migrations/20260921000014_p48_retro_feedback_rejeicao_triagem.sql` (o DO com `v_autorizados`, `IS DISTINCT FROM`, `ROW_COUNT`, aborto por efeito colateral)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md` §I.4 (a correspondência das 6 análises com os logs), Correções 9, 10, 11, 35
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md` §8 e §Shared I
    - os triggers vivos das três tabelas, só leitura: `select tgrelid::regclass, tgname, pg_get_triggerdef(oid) from pg_trigger where tgrelid in ('public.candidaturas'::regclass,'public.historico_candidatura'::regclass,'public.entrevista_analises'::regclass) and not tgisinternal`
  </read_first>
  <action>
Criar `supabase/tests/p49_retroativos_ensaio.sql` no molde do `p48_retroativos_ensaio.sql`: um único `DO` que (1) mede as contagens de controle (`net.http_request_queue`, `historico_candidatura`, `notificacoes_enviadas`, `decisao_final_historico`); (2) D-46 — seleciona `candidaturas` com `etapa_justificativa IS NOT NULL`, e para cada uma confere que `md5(etapa_justificativa)` = `md5` do `criterio_texto` da linha MAIS RECENTE de `historico_candidatura` da mesma candidatura (as que não batem vão para uma lista à parte e NÃO entram no UPDATE), marca quais são de titular anonimizado (e-mail em `@invalido.local`, sem imprimir o endereço) e roda o `UPDATE … SET etapa_justificativa = NULL` sobre o conjunto seguro; (3) D-47 — seleciona as linhas de `historico_candidatura` com `criterio_texto` igual a `decisao_final.justificativa` da mesma candidatura OU a qualquer `decisao_final_historico.justificativa` da mesma candidatura, e roda o `UPDATE … SET criterio_texto = 'Decisão final registrada.'`; (4) D-43 — para as candidaturas com análise, calcula por `(candidatura, tipo)` a mais recente bem-sucedida e roda o `UPDATE … SET superada_em = <criado_em da seguinte>` nas demais, e o preenchimento de `texto_hash`/`ai_call_log_id` pela correspondência do RESEARCH §I.4, conferida pela consulta (igualdade de `competencias` com o `raw_response` do log de `call_type='transcript_analysis'` da mesma candidatura; `texto_hash` = o `input_hash` desse log) — sem correspondência, fica NULL; (5) mede as contagens de controle de novo; (6) termina com `RAISE EXCEPTION` levando `ENSAIO OK: d46=<n> (<ids>) d46_nao_batem=<n> d46_anonimizados=<n> d47=<n> linhas em <m> candidaturas (<ids>) d43=<n> marcadas, <k> com vinculo (<ids>) fila_delta=<x> historico_delta=<y> notif_delta=<z>` — que desfaz tudo por construção. O `historico_delta` esperado é 0 (a D-47 é UPDATE, não INSERT).

Rodar: `node p46apply.cjs run supabase/tests/p49_retroativos_ensaio.sql`. Esperado (pesquisa): `d46=9`, `d46_nao_batem=0`, `d46_anonimizados=2`, `d47=5 linhas em 4`, `d43` com 3 superadas e 5 com vínculo, e os três deltas 0. Qualquer outro número ou id, ou delta ≠ 0, é achado: registrar e levar ao checkpoint como está (D-49).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs run supabase/tests/p49_retroativos_ensaio.sql 2>&1 | tee /dev/stderr | grep -E "ENSAIO OK: d46=[0-9]+" | grep -q "fila_delta=0 historico_delta=0 notif_delta=0"</automated>
    <fails_when>the output lacks "ENSAIO OK: d46=" or lacks "fila_delta=0 historico_delta=0 notif_delta=0" (a side effect was measured, or the rehearsal aborted before reporting — report at the checkpoint, do not tune the predicate)</fails_when>
  </verify>
  <acceptance_criteria>
    - O ensaio existe, usa os mesmos predicados/UPDATEs das migrations, e reverte por construção.
    - A saída `ENSAIO OK: …` com os três conjuntos e os deltas está registrada no SUMMARY; as linhas de D-46 que não batem md5 (se houver) estão listadas à parte.
    - Os triggers das três tabelas foram listados e nenhum dispara efeito colateral nos UPDATEs (ou o achado está registrado).
  </acceptance_criteria>
  <done>O operador tem, antes de decidir, exatamente o que cada escrita faria — e a prova de que nada além disso aconteceria.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: o operador aprova (ou recusa) cada uma das três escritas, com os ids e as contagens do ensaio</name>
  <files>.planning/phases/49-consertos-da-jornada-bloco-2/49-12-SUMMARY.md</files>
  <read_first>
    - a saída do ensaio da Task 1
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md` D-43, D-46, D-47, D-54
  </read_first>
  <action>Claude apresenta ao operador, por escrita: o conjunto (ids completos), a contagem antes e a esperada depois, as linhas especiais (D-46: as 2 do titular anonimizado e a real `d31c78bb`; D-47: a da conta real `d31c78bb` e a que casa uma justificativa ARQUIVADA em `2ce20fbf`; D-43: a `a1dd4c42` sem vínculo reconstruível) e os deltas de efeito colateral = 0. O operador aprova ou recusa CADA escrita, separadamente. Nada é escrito nesta task.</action>
  <what-built>Ensaio revertido das três escritas retroativas, com os conjuntos medidos agora.</what-built>
  <how-to-verify>
    1. Ler, no relatório do ensaio, os ids de cada conjunto e conferir que batem com o que foi decidido (D-46: 9; D-47: 5 linhas em 4 candidaturas; D-43: as 6 análises das 3 candidaturas).
    2. Conferir que os três deltas de efeito colateral são 0 (nenhum e-mail, nenhum histórico novo).
    3. Responder, para cada escrita, «aprovo D-46», «aprovo D-47», «aprovo D-43» — ou recusar a que não quiser.
  </how-to-verify>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs run supabase/tests/p49_retroativos_ensaio.sql 2>&1 | grep -q "ENSAIO OK: d46="</automated>
    <fails_when>the output lacks "ENSAIO OK: d46=" (the rehearsal cannot be reproduced at decision time)</fails_when>
  </verify>
  <acceptance_criteria>
    - A resposta do operador para cada uma das três escritas (aprovada/recusada) está registrada no SUMMARY, com o conjunto aprovado.
  </acceptance_criteria>
  <resume-signal>Digite «aprovo D-46», «aprovo D-47», «aprovo D-43» (as que aprovar), ou descreva o que não bateu</resume-signal>
  <done>Cada escrita retroativa tem a autorização explícita do operador, sobre o conjunto medido.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: as escritas aprovadas — uma migration por decisão, com portão de conjunto, aplicadas, e a contagem depois</name>
  <files>supabase/migrations/20260922000009_p49_retro_justificativa_grudada.sql, supabase/migrations/20260922000010_p49_retro_trilha_bd9.sql, supabase/migrations/20260922000011_p49_retro_marca_analises.sql</files>
  <read_first>
    - a resposta do operador (Task 2) e os ids aprovados
    - `supabase/migrations/20260921000014_p48_retro_feedback_rejeicao_triagem.sql` (o DO inteiro: `v_autorizados`, `IS DISTINCT FROM` do conjunto medido, `ROW_COUNT`, aborto por efeito colateral, `RAISE NOTICE` final)
    - `supabase/tests/p49_retroativos_ensaio.sql` (os predicados — copiados, não reescritos)
  </read_first>
  <action>
Para CADA escrita aprovada (e só para elas), escrever a migration correspondente no molde de `20260921000014` (cabeçalho do M8 com a decisão, a data e a frase da aprovação do operador; nota de transporte; sem `BEGIN;`): um `DO` com `v_autorizados uuid[]` = os ids APROVADOS (literal — escopo deliberado da autorização, comentado como tal); o conjunto medido na hora pelo MESMO predicado do ensaio; `IF v_conjunto IS DISTINCT FROM v_autorizados THEN RAISE EXCEPTION '… escopo divergente, nada foi escrito'`; contagens de controle antes; o UPDATE restrito a `id = ANY (v_autorizados)` E ao predicado; `GET DIAGNOSTICS` com aborto se ≠ `cardinality`; contagens de controle depois com aborto se qualquer delta ≠ 0 (para a D-47, o controle de `historico_candidatura` é `count(*)`, que não muda num UPDATE); `RAISE NOTICE` com o número e os ids. `000009` = D-46 (`etapa_justificativa = NULL` nas candidaturas); `000010` = D-47 (`criterio_texto = 'Decisão final registrada.'` nas linhas de histórico — o `v_autorizados` é de `historico_candidatura.id`); `000011` = D-43 (`superada_em` e, onde houver, `texto_hash`/`ai_call_log_id` — o `v_autorizados` é de `entrevista_analises.id`, com os valores por linha numa tabela `VALUES` literal conferida pelo ensaio).

Aplicar cada uma com `node p46apply.cjs migrate` e, depois de cada, a contagem de pós-estado só leitura (verify). Escrita recusada ⇒ nenhum arquivo, registro no SUMMARY. Commitar e `git push origin main`; `origin/main..HEAD` vazio.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && for f in supabase/migrations/20260922000009_p49_retro_justificativa_grudada.sql supabase/migrations/20260922000010_p49_retro_trilha_bd9.sql supabase/migrations/20260922000011_p49_retro_marca_analises.sql; do if [ -f "$f" ]; then node -e "const s=require('fs').readFileSync(process.argv[1],'utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');for(const t of ['v_autorizados','IS DISTINCT FROM','ROW_COUNT','RAISE EXCEPTION'])if(!c.includes(t))throw new Error(process.argv[1]+' sem: '+t);if(/DELETE\s+FROM/i.test(c))throw new Error(process.argv[1]+' apaga linha (proibido)');console.log('ok '+process.argv[1])" "$f" && node p46apply.cjs migrate "$f" || exit 1; else echo "recusada (sem arquivo): $f"; fi; done</automated>
    <fails_when>non-zero exit with "sem: <token>" or "apaga linha" in stderr; or a migrate output lacks "md5 do ledger BATE"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs sql "SET TRANSACTION READ ONLY; select (select count(*) from public.candidaturas where etapa_justificativa is not null)::int as d46, (select count(*) from public.historico_candidatura h where exists (select 1 from public.decisao_final d where d.candidatura_id=h.candidatura_id and d.justificativa=h.criterio_texto) or exists (select 1 from public.decisao_final_historico a where a.candidatura_id=h.candidatura_id and a.justificativa=h.criterio_texto))::int as d47, (select count(*) from (select candidatura_id, coalesce(tipo,'-') t, count(*) n from public.entrevista_analises where public.entrevista_analise_vigente(superada_em, status_analise, competencias) group by 1,2 having count(*)>1) x)::int as d43" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s)[0];console.log(JSON.stringify(r));const f=Object.entries(r).filter(([k,v])=>v!==0).map(([k,v])=>k+'='+v);if(f.length){console.error('pos-estado nao zerou: '+f.join(', ')+' (se a escrita foi RECUSADA, registre no SUMMARY — nao e falha dela)');process.exit(1)}})"</automated>
    <fails_when>non-zero exit with "pos-estado nao zerou" for a write the operator APPROVED (a refused write legitimately stays non-zero and is recorded in the SUMMARY)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && echo "origin/main em dia"</automated>
    <fails_when>non-zero exit (unpushed commits remain)</fails_when>
  </verify>
  <acceptance_criteria>
    - Uma migration por escrita APROVADA, com portão de conjunto e aborto por efeito colateral, aplicada com md5 do ledger conferido; nenhuma escrita recusada tem arquivo.
    - Pós-estado das aprovadas: `d46 = 0`, `d47 = 0`, nenhuma `(candidatura, tipo)` com mais de uma vigente (consulta acima).
    - `select count(*) from public.decisao_final_historico` igual ao de antes (D-45).
    - `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>O resíduo que os consertos de código não alcançam foi corrigido — só onde o operador aprovou, e sem efeito colateral.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orquestrador → PROD (escrita retroativa) | UPDATE sobre linha existente, inclusive de conta real e de titular anonimizado |
| trilha de auditoria (`historico_candidatura`) → cópia do titular | o que fica escrito chega ao titular |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-12-01 | Tampering | escrita fora do conjunto aprovado | high | mitigate | `v_autorizados` literal + conjunto medido na hora `IS DISTINCT FROM` ⇒ aborta |
| T-49-12-02 | Repudiation | escrita sem autorização | high | mitigate | `checkpoint:human-verify` `gate="blocking-human"`; aprovação por escrita no SUMMARY; recusa não gera arquivo |
| T-49-12-03 | Information Disclosure | justificativa da decisão final ficando na trilha exportada | high | mitigate | D-47 aprovado ⇒ constante; pós-estado = 0 |
| T-49-12-04 | Tampering | perder o texto ao limpar a justificativa grudada | medium | mitigate | só limpa o que bate md5 com o histórico |
| T-49-12-05 | Denial of Service | e-mail disparado por UPDATE retroativo | medium | mitigate | deltas da fila/histórico/notificações = 0, na mesma requisição |
| T-49-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Ensaio verde e reproduzível; aprovação registrada por escrita.
- Migrations das aprovadas aplicadas; pós-estado zerado; `decisao_final_historico` intocado; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
Nenhuma candidatura carrega justificativa de outra transição, nenhuma trilha leva ao titular a justificativa da decisão final, e toda análise de entrevista antiga tem estado de vigente — por decisão explícita do operador, sobre o conjunto medido.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `supabase/tests/p49_retroativos_ensaio.sql` | ensaio revertido | **novo** |
| `supabase/migrations/20260922000009_p49_retro_justificativa_grudada.sql` | migration retroativa (D-46) | **nova** (se aprovada) |
| `supabase/migrations/20260922000010_p49_retro_trilha_bd9.sql` | migration retroativa (D-47) | **nova** (se aprovada) |
| `supabase/migrations/20260922000011_p49_retro_marca_analises.sql` | migration retroativa (D-43) | **nova** (se aprovada) |

## Premissas sinalizadas (probe de borda)

JORN-17, JORN-37 e JORN-12 saíram `unclassified` no probe e ficam `unresolved` no relatório.

- **A5 (RESEARCH):** as 3 candidaturas com análise são as únicas — recontado pelo ensaio.
- Correspondência análise × log do D-43 é por igualdade de `competencias`/`citacoes` (RESEARCH §I.4); onde não
  houver igualdade exata, o vínculo fica NULL — nunca aproximado.
- As 2 justificativas grudadas do titular já anonimizado (22/08) são limpas junto: o texto está no histórico e
  a linha é tombstone; o fato vai registrado no checkpoint (RESEARCH Correção 35).
- **Ordem de apply:** as `…000009`..`…000011` dividem a onda 4 com a `…000012` do 49-14, que redefine
  `anonimizar_candidato`/`plano_exclusao_titular` — objetos disjuntos das linhas que este plano corrige (dono
  único), então a ordem relativa dentro da onda não muda o estado final; entre ondas, a ordem de apply é a ordem
  de versão.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-12-SUMMARY.md` when done
</output>
