---
phase: 49-consertos-da-jornada-bloco-2
plan: 27
type: execute
wave: 5
depends_on: ["49-02", "49-08", "49-26"]
files_modified:
  - supabase/functions/comparativo-candidatos/index.ts
  - supabase/functions/comparativo-candidatos/__tests__/index.test.ts
autonomous: true
requirements: [JORN-28, JORN-39]

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "WINDOWS 68: um comparativo bloqueado ANTES de tocar provedor (teto de custo AI-06, injeção RF-PL-18) deixa de voltar ao RH como `{ok:true, ranking:<stub>}`. O RH recebe uma recusa que diz o motivo, nunca um ranking que nenhum modelo produziu."
    - "A pergunta é estrutural e vem da fonte única: `algumProvedorRespondeu()` de `_shared/resultado-de-provedor.ts` (49-26). Nenhuma lista literal de `error_code`, nenhuma sétima cópia do predicado (WINDOWS 70 já registra seis)."
    - "A linha de auditoria CONTINUA sendo gravada (JORN-28: um comparativo mostrado e não auditado é indistinguível de um que nunca aconteceu — e um bloqueio não auditado também). `ranking` é `jsonb NOT NULL` em PROD, então o bloqueio NÃO pode ser gravado como NULL: a forma escolhida tem de ser medida contra o schema vivo e declarada no SUMMARY, não improvisada."
    - "`provedor_ia` segue mapeando `'none'` para NULL (o CHECK `comparativo_solicitado_provedor_ia_check` só aceita NULL|anthropic|openai — forçar a string dá 23514). O que muda é a RESPOSTA ao RH e a legibilidade do bloqueio na auditoria, não o mapeamento."
    - "Nenhuma migration se o schema vivo já permitir registrar o bloqueio; se NÃO permitir, o plano PARA e relata em vez de alterar a tabela por conta — acrescentar coluna a `comparativo_solicitado` é decisão do operador e o 49-17 é o inventário LGPD dela."
    - "Nada do 49-08 é desfeito: posse conferida por candidatura, recusa de encerrada com mensagem própria, `posicoes`, o teto da constante e a proveniência real seguem valendo, asseridos."
  artifacts:
    - path: supabase/functions/comparativo-candidatos/index.ts
      provides: "bloqueio pré-provedor não vira ranking entregue ao RH"
      contains: "algumProvedorRespondeu"
  key_links:
    - from: "algumProvedorRespondeu (_shared/resultado-de-provedor.ts, 49-26)"
      to: "resposta da EF ao RH + linha de comparativo_solicitado"
      via: "guarda antes do jsonResponse de sucesso"
      pattern: "algumProvedorRespondeu"
  prohibitions:
    - requirement_id: JORN-39
      category: transparency
      status: resolved
      verification: automated
      statement: "MUST NOT devolver ao RH como ranking uma resposta que nenhum provedor produziu"
    - requirement_id: JORN-28
      category: transparency
      status: resolved
      verification: automated
      statement: "MUST NOT deixar de auditar um comparativo bloqueado"
---

<objective>
O comparativo bloqueado por custo ou por injeção para de chegar ao RH como ranking.

Purpose: sexto e último sítio da família medida nesta fase — e o único com consequência VISÍVEL. O 49-26 varreu as
sete EFs e mediu: `comparativo-candidatos:434` faz `const ranking = result.parsed ?? null`, e no bloqueio pré-provedor
`result.parsed` é `{recommendation:"hold", flagged_for_human_review:true}` — não nulo. Esse objeto entra em
`comparativo_solicitado.ranking` **e volta no payload** (`jsonResponse({ok:true, ranking, …})`). Os outros cinco
sítios erravam no banco; este entrega a um recrutador, que decide sobre pessoas, um ranking que nenhum modelo fez.

Estado medido: **zero** linhas `provider='none'` em `ai_call_logs` desde sempre; maior gasto diário por vaga
US$ 0,3096 contra teto US$ 50 (161× de folga). **Latente** — nunca manifestou. É conserto de corretude antes do
fecho do M8, não incidente.

Output: a EF `comparativo-candidatos` com a guarda, deployada.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-26-SUMMARY.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-08-SUMMARY.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: o comparativo bloqueado recusa em vez de entregar (WINDOWS 68)</name>
  <precondition>`_shared/resultado-de-provedor.ts` existe com `algumProvedorRespondeu()` e testes verdes (49-26), e `comparativo-candidatos` está em v28 (49-08).</precondition>
  <files>supabase/functions/comparativo-candidatos/index.ts, supabase/functions/comparativo-candidatos/__tests__/index.test.ts</files>
  <read_first>
    - `supabase/functions/comparativo-candidatos/index.ts` (`:433` — `const ranking = result.parsed ?? null`; `:441-450` — o mapeamento de `provedor_ia`, já correto; `:457-471` — o INSERT de auditoria com `ranking`; `:487-496` — o `jsonResponse` de sucesso que devolve `ranking` ao RH)
    - `supabase/functions/_shared/resultado-de-provedor.ts` (a API: `algumProvedorRespondeu(resultado)`, `PROVEDOR_NENHUM`)
    - o schema vivo de `comparativo_solicitado` (`ranking` é `jsonb NOT NULL` — MEDIR, não presumir, e é isto que decide a forma do registro de bloqueio)
    - `supabase/functions/comparativo-candidatos/__tests__/index.test.ts` (os 18 testes do 49-08 — nenhuma asserção deles pode ser editada para este conserto passar)
  </read_first>
  <action>
**MEDIR primeiro e relatar** (o plano afirma, não verificou): (a) o tipo e a nulidade de `comparativo_solicitado.ranking`
em PROD; (b) se existe alguma coluna onde o bloqueio caiba sem alterar a tabela; (c) quantas linhas de
`comparativo_solicitado` em PROD já têm um `ranking` com a chave `recommendation` (o stub) — só leitura.

**RED.** Acrescentar ao teste da EF: teto de custo estourado ⇒ a resposta ao RH **não** é `{ok:true}` com ranking, e a
linha de auditoria **existe** registrando o bloqueio. Medir o RED real (reprova por asserção; um RED que trava no
carregamento é `INVALID_RED`, #3770; o `check tdd-red-evidence` não lê o TAP do Deno — registre o medido e não
sintetize contadores).

**GREEN.** Guarda com `algumProvedorRespondeu(result)` antes do caminho de sucesso. Duas exigências que se cruzam e
cuja conciliação é o trabalho deste plano:
- o RH recebe **recusa com motivo** (a EF já tem `errorResponse`; use o vocabulário das recusas que o 49-08
  estabeleceu, para a tela não precisar de caso novo), nunca `ok:true` com stub;
- a auditoria **continua existindo** — `ranking` é `NOT NULL`, então decida a forma pelo que o schema vivo permite e
  **escreva no SUMMARY o que escolheu e por quê**. Se nenhuma forma couber sem alterar a tabela, **PARE e relate**:
  acrescentar coluna é decisão do operador, e o inventário LGPD dessa coluna é o 49-17.

Não repetir o predicado (WINDOWS 70 já conta seis cópias de `provedorDeResultado`); consumir a fonte única. Não tocar
o mapeamento de `provedor_ia`, que já está correto. Não editar nenhuma asserção dos 18 testes do 49-08.

Rodar `deno test --allow-all supabase/functions/comparativo-candidatos/`. Provar por mutação (commitar antes;
`git diff --quiet` como guarda; **descontar códigos ANSI** ao extrair contagem — o harness do 49-26 mentiu «0
reprovados» três vezes por isso). Deploy: `--dry-run`, fechamento conferido **à mão** contra o bundle vivo, deployar,
reler versão/status/`verify_jwt` da Management API, e conferir o marcador no bundle **publicado** (foi o bundle, não o
disco, que pegou a violação do §K no 49-25).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/comparativo-candidatos/ && test "$(grep -c 'algumProvedorRespondeu' supabase/functions/comparativo-candidatos/index.ts)" -ge 1</automated>
    <fails_when>non-zero exit, "FAILED" in the Deno output, or the EF does not consume the shared predicate</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npm run -s lint 2>&1 | grep -cE "error TS" | awk '{if ($1+0 > 90) {print "tsc ceiling exceeded: " $1; exit 1} else {print "tsc " $1 " <= 90"}}' && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>tsc error count exceeds the D-53 ceiling of 90, or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - Bloqueio pré-provedor ⇒ recusa com motivo ao RH; nunca `ok:true` com ranking-stub.
    - Linha de auditoria gravada registrando o bloqueio, na forma que o schema vivo permite, com a escolha justificada no SUMMARY.
    - Predicado consumido da fonte única; zero cópia nova.
    - Os 18 testes do 49-08 verdes, nenhuma asserção editada.
    - EF deployada, versão relida de PROD; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>Nenhum ranking que a IA não produziu chega a quem decide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF → RH (payload HTTP) | o recrutador deixa de poder receber um ranking que nenhum modelo produziu |
| EF → `comparativo_solicitado` (service_role) | o bloqueio passa a ser legível na auditoria |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-27-01 | Repudiation | ranking-stub entregue ao RH como resultado de IA | **high** | mitigate | guarda por `algumProvedorRespondeu` antes do sucesso |
| T-49-27-02 | Repudiation | bloqueio não auditado | medium | mitigate | linha de auditoria preservada, forma medida contra o schema vivo |
| T-49-27-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `deno test` da EF verde com os 18 testes do 49-08 intactos; mutação prova que a guarda morde (contagem sem ANSI); `tsc` ≤ 90 com conjunto de mensagens diffado; EF deployada com versão relida de PROD; fechamento conferido à mão; marcador no bundle publicado; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
Um comparativo que nenhum modelo produziu se apresenta ao RH como recusa, com motivo, e fica auditado como bloqueio.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| guarda por `algumProvedorRespondeu` em `comparativo-candidatos` | EF (lógica) | editado |
| EF `comparativo-candidatos` | deploy | nova versão |

## Premissas sinalizadas

Linhas já gravadas com o stub: a medir nesta sessão (item c). O 49-25 e o 49-26 mediram **zero** nos seus sítios; se
aqui a contagem for não-zero, é matéria do **49-12** (correções retroativas, checkpoint, D-30) — medir e registrar,
não consertar aqui.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-27-SUMMARY.md` when done
</output>
