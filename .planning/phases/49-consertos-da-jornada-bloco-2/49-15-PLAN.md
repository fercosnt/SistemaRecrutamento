---
phase: 49-consertos-da-jornada-bloco-2
plan: 15
type: execute
wave: 4
depends_on: ["49-09", "49-02", "49-13"]
files_modified:
  - src/features/triagem/components/RedacaoReviewPanel.tsx
  - src/features/triagem/components/__tests__/RedacaoReviewPanel.test.tsx
  - src/features/triagem/components/RedacaoOverrideForm.tsx
  - src/features/triagem/components/__tests__/RedacaoOverrideForm.test.tsx
  - src/features/triagem/services/revisaoRedacaoService.ts
  - src/features/triagem/services/__tests__/revisaoRedacaoService.test.ts
  - src/features/admin/ai-logs/components/AiLogsPage.tsx
  - src/features/admin/ai-logs/components/__tests__/AiLogsPage.test.tsx
  - src/features/admin/ai-logs/services/aiLogsService.ts
autonomous: true
requirements: [JORN-07, JORN-28]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-25: a tela de revisão da redação (`RedacaoReviewPanel`) e o formulário de override (`RedacaoOverrideForm`) rotulam as 4 dimensões com os rótulos da constante `DIMENSOES_REDACAO` de `supabase/functions/_shared/bars-redacao.ts` — a MESMA que a EF envia ao modelo (plano 49-09) —, pela CHAVE (D1..D4), nunca por posição com um mapa próprio da tela. Os mapas locais `DIM_LABEL` e `DIMENSOES` (os 4 valores) saem."
    - "D-25: a tela mostra o raciocínio (`analise_ia.dimension_scores[k].reasoning`) e as citações (`analise_ia.dimension_scores[k].cited_evidence[]`) POR DIMENSÃO, e o `qualitative_summary`, que a IA já grava — a leitura de chaves inexistentes (`analise_ia.reasoning`, `analise_ia.citacoes`) sai."
    - "D-26: redação com `rubrica_versao` NULL (as 2 antigas, de conta de teste) mostra o aviso de versão antiga («avaliada antes de a rubrica ser enviada ao modelo — os números podem não corresponder a estes rótulos»); nenhuma escrita retroativa."
    - "D-27b: o painel da redação mostra o `ProvenienciaIABadge` (plano 49-13) com `provedor_ia`/`modelo_ia` da linha."
    - "Teste obrigatório (Discretion do JORN-07): um teste reprova se os rótulos renderizados divergirem da constante enviada ao modelo — ele importa `DIMENSOES_REDACAO` e itera sobre ela, sem lista literal de rótulos (a forma que não envelhece); o `RedacaoOverrideForm.test.tsx:33-37`, que travava os 4 valores, muda DE PROPÓSITO (D-56)."
    - "`EntrevistaScorecardInline.tsx` (que usa os 4 valores como competências DE ENTREVISTA) NÃO muda — é outro assunto."
    - "D-27c: o log de IA do admin mostra três estados: «Sucesso», «Falha» e «Fallback» (âmbar) — este quando `error_code` começa com `fallback_` —, com a causa legível do sufixo (não coube / demorou / fora do schema / …) vinda de `CAUSA_FALLBACK_ROTULO` de `supabase/functions/_shared/ai-error-codes.ts`; a coluna Modelo mostra o modelo REAL (`model_snapshot`, com `model_id` como reserva). As 17 linhas antigas `anthropic_retries_exhausted` mostram «causa não registrada (antes da Phase 49)». Sem aviso ativo ao admin (D-27)."
    - "O `listAiLogs` segue com allowlist explícita (sem `select('*')`), acrescida só de `error_code` e `model_snapshot` se faltarem."
    - "Texto novo passa pelo guard `forbidden-strings.grep.test.ts` (D-58, D-07); `tsc` ≤ 90; build com os marcadores; push; marcador no ar (a rota `/admin/*` é lazy)."
  artifacts:
    - path: src/features/triagem/components/RedacaoReviewPanel.tsx
      provides: "rótulos da constante, raciocínio e citações por dimensão, aviso de versão antiga, selo"
      contains: "DIMENSOES_REDACAO"
    - path: src/features/admin/ai-logs/components/AiLogsPage.tsx
      provides: "estado Fallback com a causa"
      contains: "ai-log-fallback"
  key_links:
    - from: "supabase/functions/_shared/bars-redacao.ts (DIMENSOES_REDACAO)"
      to: "RedacaoReviewPanel.tsx e RedacaoOverrideForm.tsx"
      via: "import relativo da mesma constante que a EF envia ao modelo"
      pattern: "bars-redacao"
    - from: "supabase/functions/_shared/ai-error-codes.ts"
      to: "AiLogsPage.tsx"
      via: "ehFallback / causaDoFallback / CAUSA_FALLBACK_ROTULO"
      pattern: "ai-error-codes"
  prohibitions:
    - requirement_id: JORN-07
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT rotular uma dimensão por posição ou por mapa próprio da tela — o rótulo é o da constante que o modelo recebeu"
    - requirement_id: JORN-28
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT mostrar um fallback como «Sucesso» no log do admin (D-27c)"
    - requirement_id: JORN-07
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT introduzir ocorrência literal nova do endereço do canal de privacidade nem a expressão «teste psicológico» em texto novo (D-07, D-58 — a única exceção é o disclaimer negado da devolutiva, já decidido)"
---

<objective>
O RH passa a ler, na revisão da redação, o nome das dimensões que a IA de fato avaliou — com o raciocínio e as
citações que a IA escreveu — e o admin passa a ver, no log de IA, quando uma chamada caiu no modelo de contingência e
por quê.

Purpose: D-25/D-26 (a tela lê chaves inexistentes e rotula D1–D4 com os 4 valores; o RH «lê UAU 5/5» sobre um
raciocínio de outra coisa) e D-27c (o fallback aparece como «Sucesso» verde). Passo «cliente» das sequências cujos
passos «EF» são o 49-09 (redação) e o 49-02 (códigos) — D-55.

Output: as duas telas da redação com a constante única, o log do admin com o estado de fallback, publicados.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-09-SUMMARY.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-13-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): a rubrica que o modelo recebeu é a que o RH lê — constante única até a tela, com raciocínio, citações e versão</name>
  <files>src/features/triagem/components/RedacaoReviewPanel.tsx, src/features/triagem/components/__tests__/RedacaoReviewPanel.test.tsx, src/features/triagem/components/RedacaoOverrideForm.tsx, src/features/triagem/components/__tests__/RedacaoOverrideForm.test.tsx, src/features/triagem/services/revisaoRedacaoService.ts, src/features/triagem/services/__tests__/revisaoRedacaoService.test.ts</files>
  <read_first>
    - `supabase/functions/_shared/bars-redacao.ts` (plano 49-09 — `DIMENSOES_REDACAO`, `RUBRICA_REDACAO_VERSAO`)
    - `src/features/triagem/components/RedacaoReviewPanel.tsx` (inteiro; `:46-52` `DIM_LABEL`; `:60-115` `AnaliseIA`)
    - `src/features/triagem/components/RedacaoOverrideForm.tsx` (`:40-55` `DIMENSOES`) e `__tests__/RedacaoOverrideForm.test.tsx` (`:28-40`)
    - `src/features/triagem/services/revisaoRedacaoService.ts` (`REDACAO_ALLOWLIST`/`REDACAO_EMBED` `:80-92`, os tipos `RedacaoReviewRow`/`ScoresDimensao`) e o teste dele
    - `supabase/functions/_shared/essay-schemas.ts` (a forma de `analise_ia`: `dimension_scores[].{dimension, dimension_name, cited_evidence[{text,location}], reasoning, score, level}`, `qualitative_summary`)
    - `src/features/triagem/components/ProvenienciaIABadge.tsx` (plano 49-13)
    - `src/features/privacidade/services/exportacaoService.ts` (`:57-61` — o import relativo de `_shared` com o comentário do porquê)
  </read_first>
  <behavior>
    - `RedacaoReviewPanel` com `analise_ia` de 4 dimensões (reasoning e 2 citações cada) mostra, para cada chave de `DIMENSOES_REDACAO`, o `rotulo` da constante, a nota, o raciocínio e as citações daquela chave; mostra o `qualitative_summary`.
    - Os rótulos renderizados são exatamente os `rotulo` da constante importada no teste (iterando sobre ela).
    - `dimension_scores` em ordem trocada (D3, D1, D4, D2) ⇒ cada raciocínio aparece sob o rótulo da SUA chave.
    - `rubrica_versao = null` ⇒ aviso de versão antiga (`data-testid="redacao-rubrica-versao-antiga"`); `rubrica_versao = 'bars-prd-1.1'` ⇒ sem aviso.
    - `provedor_ia='openai'` ⇒ selo de contingência no painel.
    - `RedacaoOverrideForm` renderiza os 4 rótulos da constante (iterando sobre ela) e o radio de decisão como hoje.
  </behavior>
  <action>
D-50: re-rodar o `grep` da C7 no front (`DIM_LABEL|DIMENSOES = \[`) e registrar o delta (o `EntrevistaScorecardInline.tsx:29-34` é escopo deliberado — competências de entrevista, anotar e não tocar).

`revisaoRedacaoService.ts`: `REDACAO_ALLOWLIST` ganha `rubrica_versao, provedor_ia, modelo_ia` (e `analise_ia` se ainda não estiver); tipos atualizados; teste da string do select. `RedacaoReviewPanel.tsx`: importar `DIMENSOES_REDACAO` de `../../../../supabase/functions/_shared/bars-redacao` (caminho relativo, comentário com o precedente do `exportacaoService`); `DIM_LABEL` sai; `AnaliseIA` percorre `DIMENSOES_REDACAO` e, para cada chave, acha o `dimension_scores` de mesma `dimension` (nunca por índice) e mostra rótulo, nota, raciocínio e citações (`text` e `location`); `qualitative_summary` abaixo; aviso de versão antiga com o `data-testid`; selo de proveniência. `RedacaoOverrideForm.tsx`: `DIMENSOES` sai; os sliders percorrem `DIMENSOES_REDACAO` pela `chave`. Reescrever `RedacaoOverrideForm.test.tsx:28-40` para iterar sobre a constante importada (comentário: Phase 49 / D-25 — o teste travava os 4 valores; agora trava a constante que o modelo recebe). Criar `__tests__/RedacaoReviewPanel.test.tsx` com o `<behavior>`. Rodar `npx vitest run src/features/triagem src/__tests__/guards` e o portão `tsc`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/features/triagem src/__tests__/guards</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');for(const f of ['src/features/triagem/components/RedacaoReviewPanel.tsx','src/features/triagem/components/RedacaoOverrideForm.tsx']){const code=fs.readFileSync(f,'utf8').split('\n').filter(l=>!/^\s*(\/\/|\*)/.test(l)).join('\n');if(!/DIMENSOES_REDACAO/.test(code))throw new Error(f+' nao usa a constante');if(/Experiência UAU|Sede de Crescimento|Atitude de Dono/.test(code))throw new Error(f+' ainda rotula pelos 4 valores')}const t=fs.readFileSync('src/features/triagem/components/__tests__/RedacaoOverrideForm.test.tsx','utf8');if(!/DIMENSOES_REDACAO/.test(t))throw new Error('o teste do override nao itera sobre a constante');console.log('OK')" && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; }</automated>
    <fails_when>non-zero exit with "nao usa a constante", "ainda rotula pelos 4 valores" or "nao itera sobre a constante"; lint exit code other than 0/2 or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - As duas telas rotulam pela constante (sem os 4 valores no código); o painel mostra raciocínio e citações por chave, o resumo, o aviso de versão antiga e o selo (testes do `<behavior>`).
    - O teste do override itera sobre a constante; `vitest` de `src/features/triagem` e o guard verdes; `tsc` ≤ 90.
  </acceptance_criteria>
  <done>O RH que revisa uma redação lê o nome da dimensão que a IA avaliou, e o raciocínio dela sobre aquela dimensão.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: o log de IA do admin com estado de fallback e causa (D-27c) — e a publicação</name>
  <files>src/features/admin/ai-logs/components/AiLogsPage.tsx, src/features/admin/ai-logs/components/__tests__/AiLogsPage.test.tsx, src/features/admin/ai-logs/services/aiLogsService.ts</files>
  <read_first>
    - `src/features/admin/ai-logs/components/AiLogsPage.tsx` (`:215-245` — tabela, badge `Sucesso/Falha` `:236-240`, colunas Provider/Modelo `:231-232`)
    - `src/features/admin/ai-logs/services/aiLogsService.ts` (a allowlist da listagem `:4-20` e `listAiLogs :104+`)
    - `supabase/functions/_shared/ai-error-codes.ts` (plano 49-02 — `ehFallback`, `causaDoFallback`, `CAUSA_FALLBACK_ROTULO`)
  </read_first>
  <behavior>
    - Linha `success=true`, `error_code='fallback_anthropic_max_tokens'` ⇒ badge «Fallback» âmbar (`data-testid="ai-log-fallback"`) com «não coube»; NUNCA «Sucesso».
    - Linha `success=false`, `provider='anthropic'`, `error_code='anthropic_timeout'` ⇒ «Falha» com «demorou».
    - Linha `success=true`, `error_code=null` ⇒ «Sucesso».
    - Linha antiga `error_code='anthropic_retries_exhausted'` (fallback de antes) ⇒ «causa não registrada (antes da Phase 49)».
    - A coluna Modelo mostra `model_snapshot` quando presente.
  </behavior>
  <action>
`aiLogsService.ts`: garantir `error_code` e `model_snapshot` na allowlist da listagem (sem abrir as colunas sensíveis, que seguem só no detalhe). `AiLogsPage.tsx`: o badge de status passa a ter três estados, com `ehFallback(row.error_code)` decidindo «Fallback» (classe âmbar, `data-testid="ai-log-fallback"`) e a causa legível ao lado (`CAUSA_FALLBACK_ROTULO[causaDoFallback(code)]`, ou do próprio `error_code` em «Falha»); importar de `../../../../../supabase/functions/_shared/ai-error-codes` (relativo, com o comentário do precedente — conferir a profundidade do caminho); coluna Modelo = `row.model_snapshot ?? row.model_id`. Criar `__tests__/AiLogsPage.test.tsx` com o `<behavior>` (mock do hook/serviço). Rodar `npx vitest run src/features/admin src/__tests__/guards` e o portão `tsc`.

Publicar (D-52): `npm run build`; conferir `redacao-rubrica-versao-antiga` e `ai-log-fallback` em `build/assets/` e anotar os chunks (`/rh/*` e `/admin/*` são lazy); commitar; `git push origin main`; `origin/main..HEAD` vazio; crawler com `ai-log-fallback`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/features/admin src/features/triagem src/__tests__/guards</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npm run build >/dev/null && grep -rl "ai-log-fallback" build/assets/ && grep -rl "redacao-rubrica-versao-antiga" build/assets/</automated>
    <fails_when>non-zero exit (build failed, or either marker is absent from every file in build/assets/)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && node -e "const M=process.argv[1],B='https://rh.beautysmile.com.br';(async()=>{const vis=new Set(),fila=['/'];let ok=false;while(fila.length&&vis.size<300){const p=fila.shift();if(vis.has(p))continue;vis.add(p);let t='';try{t=await (await fetch(B+p)).text()}catch{continue}if(t.includes(M)){ok=true;break}for(const m of t.matchAll(/(?:\/assets\/|\.\/)([\w.-]+\.js)/g))fila.push('/assets/'+m[1])}if(!ok){console.error('AUSENTE em PROD: '+M);process.exit(1)}console.log('PRESENTE em PROD: '+M)})()" "ai-log-fallback"</automated>
    <fails_when>non-zero exit: unpushed commits remain, or "AUSENTE em PROD: ai-log-fallback"</fails_when>
  </verify>
  <acceptance_criteria>
    - Os 5 casos do `<behavior>` têm teste e passam; a listagem continua sem colunas sensíveis.
    - `vitest` de `src/features/admin`, `src/features/triagem` e o guard verdes; `tsc` ≤ 90.
    - Os dois marcadores no build (chunks anotados) e `ai-log-fallback` no ar; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>O admin vê, linha a linha, quando o modelo configurado não respondeu e o motivo — «não coube», «demorou» ou «fora do schema».</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do RH ← `redacoes_candidato` (RLS) | o RH lê o que a IA escreveu sobre a redação |
| navegador do admin ← `ai_call_logs` (RLS só administrador) | listagem com allowlist; conteúdo só no detalhe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-15-01 | Repudiation | rótulo da tela diferente da rubrica que o modelo recebeu | medium | mitigate | constante única importada pela EF e pelas telas; teste itera sobre ela |
| T-49-15-02 | Repudiation | fallback exibido como sucesso ao admin | medium | mitigate | terceiro estado por `ehFallback`; teste |
| T-49-15-03 | Information Disclosure | colunas sensíveis do log na listagem | medium | mitigate | allowlist acrescida só de `error_code`/`model_snapshot`; conteúdo só no detalhe |
| T-49-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `vitest` de `src/features/triagem`, `src/features/admin` e o guard verdes; `tsc` ≤ 90.
- Marcadores no build e no ar; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
A rubrica que o RH lê é a que a IA avaliou, e o fallback deixa de passar por sucesso no log.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `RedacaoReviewPanel` por `DIMENSOES_REDACAO` (+ `data-testid="redacao-rubrica-versao-antiga"`) | componente | editado |
| `RedacaoOverrideForm` por `DIMENSOES_REDACAO` | componente | editado |
| `REDACAO_ALLOWLIST` + `rubrica_versao, provedor_ia, modelo_ia` | allowlist de select | editada |
| `AiLogsPage` estado «Fallback» (`data-testid="ai-log-fallback"`) + causa | componente | editado |
| `src/features/triagem/components/__tests__/RedacaoReviewPanel.test.tsx`, `src/features/admin/ai-logs/components/__tests__/AiLogsPage.test.tsx` | testes | **novos** |

## Premissas sinalizadas (probe de borda)

JORN-07 e JORN-28 saíram `unclassified` no probe e ficam `unresolved` no relatório. A tela de custos do admin
(sem coluna de modelo) é o Defeito 29, Bloco 3 — fora (§Deferred).

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-15-SUMMARY.md` when done
</output>
