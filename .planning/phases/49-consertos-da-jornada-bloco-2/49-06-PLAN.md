---
phase: 49-consertos-da-jornada-bloco-2
plan: 06
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - supabase/migrations/20260922000003_p49_trava_encerrada.sql
  - supabase/migrations/20260922000004_p49_trilha_justificativa_e_vigente.sql
  - supabase/tests/p49_trilha_smoke.sql
  - supabase/tests/p48_reabertura_smoke.sql
  - supabase/tests/p48_rejeicao_triagem_smoke.sql
  # editados SÓ se a conferência A4 da Task 2 achar fixture que a trava ou a guarda recusem:
  - supabase/tests/p42_revisao_art20_smoke.sql
  - supabase/tests/p45_motor_exclusao_smoke.sql
  - supabase/tests/p48_candidatura_encerrada_smoke.sql
  - supabase/tests/p48_dedupe_smoke.sql
  - supabase/tests/p48_cognitivo_notifica_smoke.sql
  - supabase/tests/p48_prazo_reabertura_smoke.sql
autonomous: true
requirements: [JORN-25, JORN-17, JORN-37, JORN-34, JORN-12]

estimate:
  tokens: 135000
  raw_tokens: 135000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-35: o trigger `avancar_etapa()` recusa (`check_violation`, «candidatura encerrada (etapa %, status %) — não pode mudar de etapa») mover de etapa uma candidatura cujo ESTADO ANTERIOR é encerrado pelo predicado canônico `candidatura_encerrada(OLD.etapa_atual, OLD.status)`, exceto quando a GUC `app.transicao_sancionada` está em `reabertura` (D-01) ou `decisao` (`registrar_decisao`). Medido: knockout `inscricao/rejeitado` → `avaliacao_assincrona` deixa de ser aceito."
    - "A sanção é por GUC e nunca por destino terminal: um PATCH direto `etapa_atual='aprovado'` num knockout, pela policy `rh_avanca_etapa`, também é recusado (RESEARCH §H.1)."
    - "`responder_revisao_decisao` liga `app.transicao_sancionada = 'reabertura'` imediatamente antes do UPDATE de reabertura e a zera DEPOIS do `GET DIAGNOSTICS v_n = ROW_COUNT` (um `PERFORM` entre os dois zeraria o ROW_COUNT); `registrar_decisao` liga `decisao` antes de cada um dos seus dois UPDATEs e zera logo depois (RESEARCH Correção 31: `set_config(…, true)` vale até o fim da TRANSAÇÃO e vazaria para o UPDATE seguinte num smoke)."
    - "D-01 intacta: a reabertura `rejeitado/rejeitado → decisao_final/em_analise` continua passando — `p48_reabertura_smoke.sql` verde, com a fixture F4 entrando pela GUC sancionada e uma asserção NOVA provando que, sem a GUC, o mesmo UPDATE é recusado (o smoke continua mordendo; esperado 12 → 13)."
    - "JORN-17: `avancar_etapa()` faz `NEW.etapa_justificativa := NULL` depois do INSERT no histórico, na mesma função BEFORE — o texto vai para `historico_candidatura.criterio_texto` e não sobrevive na linha; a próxima regressão volta a exigir motivo NOVO (a forma do `oper31 (c)`)."
    - "D-47/JORN-37: `registrar_decisao` deixa de copiar `p_justificativa` para `etapa_justificativa` nos dois ramos (aprovado, rejeitado) e grava a constante sem PII `'Decisão final registrada.'` — o histórico registra que houve a decisão, não o texto; o texto continua em `decisao_final.justificativa` (fonte, BD-9)."
    - "D-39: a bandeira de linguagem do portão de avanço olha SÓ as análises vigentes, pela função única `public.entrevista_analise_vigente(ea.superada_em, ea.status_analise, ea.competencias)` do 49-01 — uma bandeira antiga, já superada, deixa de bloquear o avanço."
    - "JORN-34 (defesa no banco, escopada ao cliente): `guard_rejeicao_auditada` ganha um ramo irmão que recusa (`check_violation`) sair de candidatura encerrada SÓ PELO STATUS (`candidatura_encerrada(OLD…)` e não `candidatura_encerrada(NEW…)`) quando `auth.uid() IS NOT NULL` e a GUC sancionada não é `reabertura`. O escopo por `auth.uid()` preserva o idioma de fixture de 8 smokes (INSERT `rejeitado` + UPDATE `em_analise` como `postgres` sem JWT) — conferido smoke a smoke (premissa A4)."
    - "D8 intacto: nenhuma das mudanças exige evidência da etapa para avançar; a trava recusa mover ENCERRADA, não exige nada de quem está em andamento."
    - "`rejeitar_candidatura` NÃO muda (já recusa encerrada desde a 48-01; RESEARCH Correção 28: justificativa de etapa chega ao titular por decisão tomada e fica fora do D-47)."
    - "Cada função reescrita partiu do corpo VIVO (`pg_get_functiondef` + `md5(prosrc)`), com pré-portão de md5 medido na véspera e pós-portão que assere o que entrou e o que não podia sumir (todas as asserções do pós-portão de `20260921000012` seguem valendo), e `RAISE NOTICE` com o md5 novo (PATTERNS §Shared A)."
    - "BORDA explícita: a GUC não vaza — no `p49_trilha_smoke`, depois de uma reabertura/decisão sancionada na mesma transação, `current_setting('app.transicao_sancionada', true)` é vazio e um UPDATE cru de etapa sobre encerrada é recusado."
    - "O portão morde (D-56): em requisição atômica que aborta, a mutação com o corpo ANTERIOR de `avancar_etapa` faz o `p49_trilha_smoke` reprovar nas asserções da trava, da limpeza e da vigente — saída registrada no SUMMARY."
    - "Nenhum e-mail a candidato real sai deste plano: toda escrita de fixture é revertida por subtransação e o `oper31` roda em envelope que aborta (D-54)."
  artifacts:
    - path: supabase/migrations/20260922000003_p49_trava_encerrada.sql
      provides: "D-35: trava em avancar_etapa + GUC sancionada nas duas RPCs"
      contains: "app.transicao_sancionada"
    - path: supabase/migrations/20260922000004_p49_trilha_justificativa_e_vigente.sql
      provides: "JORN-17, D-47, vigente no portão, guarda JORN-34"
      contains: "entrevista_analise_vigente("
    - path: supabase/tests/p49_trilha_smoke.sql
      provides: "prova da trilha: trava, reabertura, decisão, limpeza, regressão, GUC sem vazamento, JORN-34, vigente, resíduo zero"
      contains: "smoke49t.pass"
  key_links:
    - from: "avancar_etapa() (BEFORE UPDATE OF etapa_atual)"
      to: "public.candidatura_encerrada / app.transicao_sancionada"
      via: "trava no topo da função, logo depois do early-return"
      pattern: "candidatura_encerrada\\(OLD\\.etapa_atual, OLD\\.status\\)"
    - from: "responder_revisao_decisao / registrar_decisao"
      to: "avancar_etapa"
      via: "set_config('app.transicao_sancionada', …, true) antes do UPDATE e reset depois"
      pattern: "set_config\\('app\\.transicao_sancionada'"
    - from: "avancar_etapa (bandeira de entrevista)"
      to: "public.entrevista_analise_vigente (49-01)"
      via: "EXISTS só sobre vigentes"
      pattern: "entrevista_analise_vigente\\("
  prohibitions:
    - requirement_id: JORN-25
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT introduzir portão de evidência no avanço — a trava recusa candidatura encerrada, não exige prova da etapa (D8)"
    - requirement_id: JORN-25
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT deixar a GUC de sanção ligada depois do UPDATE que ela sanciona"
    - requirement_id: JORN-37
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT copiar a justificativa da decisão final para qualquer coluna que chega ao titular (BD-9)"
    - requirement_id: JORN-25
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT disparar e-mail a candidato real ao provar a trava — fixtures revertidas, envelope que aborta (D-54)"
---

<objective>
A trilha da candidatura passa a registrar só o que aconteceu: o banco recusa mover candidatura encerrada
(exceto as transições sancionadas), a justificativa não gruda de uma transição para a outra, a justificativa
da decisão final não vaza para a trilha que o titular recebe, o portão de avanço olha só a análise vigente,
e nenhuma tela reabre por status.

Purpose: D-35 (medido: knockout → «Avançar» → `avaliacao_assincrona` é aceito, grava histórico e dispara
`avanco`), JORN-17 (portão e cópia na mesma função BEFORE, nunca limpa), D-47/JORN-37 (4 de 7 decisões
chegam ao titular pela trilha), D-39 (o portão olha todas as análises), JORN-34. As quatro funções
compartilham a GUC nova e o mesmo trigger central — por isso um plano só é dono delas (RESEARCH «dono
único»; D-55).

Output: duas migrations (a trava primeiro, porque é a mudança de maior peso — costly, D-35), o smoke novo da
trilha, as duas fixturas antigas consertadas de propósito e a regressão dos 8 smokes do idioma de fixture.

Orçamento (revisão do checker, W1): NÃO dividido — as quatro funções têm um dono só (RESEARCH «dono único», CONTEXT
§Integração) e a mutação da Task 3 usa o corpo capturado na Task 1; a única divisão permitida seria sequencial sobre os
mesmos objetos, e ela empurraria 49-07/49-10/49-12/49-14 (e a cadeia do motor atrás deles) uma onda adiante sem
reduzir o que cada migration precisa ler. A Task 3 é execução de smokes já escritos; o excesso (1,35×) é sobre
estimativa sem calibração (sample_count 0).
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-VARREDURA-KICKOFF.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer) [BLOCKING]: a trava D-35 com as duas sanções — ensaiada em requisição que aborta, aplicada, e o knockout deixa de avançar</name>
  <precondition>O plano 49-01 foi aplicado: `node p46apply.cjs sql "SET TRANSACTION READ ONLY; select to_regprocedure('public.entrevista_analise_vigente(timestamptz,text,jsonb)') is not null as ok"` devolve `true`.</precondition>
  <reversibility rating="costly">Trigger central do funil: toda escrita de etapa passa por ele; desfazer é re-aplicar o corpo anterior (guardado no SUMMARY), mas enquanto errado ele bloqueia o funil inteiro (D-35).</reversibility>
  <files>supabase/migrations/20260922000003_p49_trava_encerrada.sql, supabase/tests/p49_trilha_smoke.sql, supabase/tests/p48_reabertura_smoke.sql</files>
  <read_first>
    - O VIVO das três funções, só leitura: `node p46apply.cjs sql "SET TRANSACTION READ ONLY; select p.proname, md5(p.prosrc), length(p.prosrc), pg_get_functiondef(p.oid) from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public' and p.proname in ('avancar_etapa','registrar_decisao','responder_revisao_decisao','guard_rejeicao_auditada')"` — guardar a saída inteira no scratchpad (é a base da mutação da Task 3 e do rollback)
    - `supabase/migrations/20260712110001_avancar_etapa_auto_rejeitado_fix.sql` (`:60-139` — corpo base; ⚠ NÃO copiar a nota de transporte `:55-57`, é a via MCP obsoleta)
    - `supabase/migrations/20260921000012_p48_registrar_decisao_reabertura.sql` (`:73-90` pré-portão; `:96-236` corpo vivo; `:265-294` pós-portão — todas as asserções dele continuam)
    - `supabase/migrations/20260921000011_p48_reabertura_colunas_e_resposta.sql` (`:72-92` pré-portão de duas funções num bloco; `:200-330` `responder_revisao_decisao`)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md` §1, §4 (4a–4d), §10, §12, §Shared A/B/C/D
    - `supabase/tests/p48_reabertura_smoke.sql` (inteiro — preâmbulo `:68-71`, baseline `:77-127`, fixture `:180-230` com a F4 em `:224-227`, subtransação `P48R1`, gate `:731-755` esperado 12)
  </read_first>
  <action>
Passo 1 — varreduras (D-50, D-56), antes de escrever: (a) os padrões C1-P4 (escritores vivos de `etapa_atual`) e C2 (`prosrc ~ 'etapa_justificativa'` e as GUCs `set_config('app.…`) do `49-VARREDURA-KICKOFF.md`; para CADA escritor vivo de `etapa_atual`, registrar no SUMMARY se ele pode mover uma candidatura encerrada e, se pode, qual sanção o cobre (esperado: só `registrar_decisao` e `responder_revisao_decisao`; `rejeitar_candidatura` já recusa encerrada; o knockout não muda a etapa). Escritor não previsto que mova encerrada = parar e reportar (D-14). (b) a varredura de portões do `CLAUDE.md` (282 na abertura), classificando os achados que citem estas funções ou `candidaturas`. (c) `grep -rn "UPDATE public.candidaturas" supabase/tests/*.sql` e, para cada UPDATE que mude `etapa_atual` de uma linha com `status` ou etapa terminal, anotar se a trava o recusa — os conhecidos: `p48_reabertura_smoke.sql:224-227` (F4).

Passo 2 — escrever `supabase/migrations/20260922000003_p49_trava_encerrada.sql` (cabeçalho do M8: O QUE ESTAVA ERRADO medido; POR QUE GUC E NÃO DESTINO; a ordem dos triggers BEFORE em `candidaturas` — `candidaturas_avancar_etapa_trg` → `trg_candidaturas_guard_rejeicao` → `update_candidaturas_updated_at`; ERRO `check_violation`; IDEMPOTÊNCIA; nota de transporte `APLICAR COM: node p46apply.cjs migrate …`; sem `BEGIN;`). Conteúdo:
(a) `DO` de pré-portão com UM `IF` por função (`avancar_etapa`, `registrar_decisao`, `responder_revisao_decisao`) contra o `md5(prosrc)` medido no read_first;
(b) `CREATE OR REPLACE FUNCTION public.avancar_etapa()` = corpo vivo + a trava logo DEPOIS do early-return de etapa igual: se `public.candidatura_encerrada(OLD.etapa_atual, OLD.status)` e `coalesce(current_setting('app.transicao_sancionada', true), '')` não está em (`reabertura`, `decisao`) ⇒ `RAISE EXCEPTION 'candidatura encerrada (etapa %, status %) — não pode mudar de etapa', OLD.etapa_atual, OLD.status USING ERRCODE = 'check_violation'`. Nada mais muda nesta migration (a limpeza e a vigente são da Task 2). `REVOKE ALL … FROM PUBLIC; REVOKE ALL … FROM anon` (hoje só PUBLIC);
(c) `CREATE OR REPLACE` de `responder_revisao_decisao` = corpo vivo + `PERFORM set_config('app.transicao_sancionada', 'reabertura', true)` imediatamente antes do UPDATE de reabertura de `candidaturas` e `PERFORM set_config('app.transicao_sancionada', '', true)` DEPOIS do `GET DIAGNOSTICS v_n = ROW_COUNT` (e antes do `IF v_n <> 1`); grants vivos reafirmados + `REVOKE … FROM anon`;
(d) `CREATE OR REPLACE` de `registrar_decisao` = corpo vivo + `set_config('app.transicao_sancionada', 'decisao', true)` antes de cada UPDATE de `candidaturas` e reset logo depois de cada um (a D-47 é da Task 2); grants vivos + `REVOKE … FROM anon` (fecha o item «EXECUTE para anon» do deferred da 48 para esta RPC; o guard fail-closed já roda antes de tudo);
(e) `DO` de pós-portão: `avancar_etapa` contém `candidatura_encerrada(OLD.etapa_atual, OLD.status)` e `app.transicao_sancionada`, e ainda contém `Regressão de etapa exige justificativa`, `app.rejeicao_sancionada` e `bloqueio: revise a bandeira`; `responder_revisao_decisao` contém o set e o reset, e o reset aparece DEPOIS de `GET DIAGNOSTICS`; `registrar_decisao` mantém TODAS as asserções do pós-portão de `20260921000012:265-294` e ganha as duas GUCs; `has_function_privilege('anon', …, 'EXECUTE')` = false nas três; `RAISE NOTICE` com os md5 novos.

Passo 3 — `p48_reabertura_smoke.sql`: a fixture F4 (`:224-227`) passa a entrar pela GUC sancionada (`set_config('app.transicao_sancionada','reabertura', true)` antes, reset depois) com comentário de proveniência (Phase 49 / D-35: a trava recusaria o UPDATE cru); ACRESCENTAR uma asserção nova, numa subtransação que reverte, provando que o MESMO UPDATE sem a GUC é recusado com `candidatura encerrada`; subir o esperado 12 → 13 no gate e no JSON final, registrando o bump no cabeçalho como o 48-01 fez.

Passo 4 — criar `supabase/tests/p49_trilha_smoke.sql` no molde do `p48_reabertura_smoke.sql` (cabeçalho O QUE VIGIA / A FIXTURE NÃO É UMA CANDIDATURA REAL / ESTE SMOKE ESCREVE / COMO RODAR / GATE; preâmbulo `RESET ROLE` + claims vazias; contador `smoke49t.pass`; atores RH/admin ATIVOS lidos na execução; titular sintético `@invalido.local`; fixtures no idioma INSERT `rejeitado` + UPDATE só de `status`; tudo dentro de subtransação que reverte com SQLSTATE próprio `P49T1`; julgamento FORA). Nesta task, as asserções da trava: (a) knockout `inscricao/rejeitado` → UPDATE `etapa_atual='avaliacao_assincrona'` com claims de RH ⇒ `check_violation` «candidatura encerrada»; (a2) o mesmo sem claims ⇒ também recusado (a trava não é escopada por JWT); (b) reabertura por `responder_revisao_decisao` (veredito `revertida`, revisor ≠ decisor) sobre `rejeitado/rejeitado` ⇒ passa e deixa `decisao_final/em_analise`; (c) `registrar_decisao` sobre fixture em `decisao_final/em_analise` passa; (g) GUC sem vazamento: depois de (b) e (c), na mesma transação, `current_setting('app.transicao_sancionada', true)` é vazio e um UPDATE cru de etapa sobre encerrada é recusado; (j) negativa: contagens de `candidaturas`, `historico_candidatura`, `decisao_final`, `notificacoes_enviadas` e `net.http_request_queue` idênticas antes/depois. Gate com o número exato de asserções desta task (documentado no cabeçalho; a Task 2 acrescenta as suas e sobe o esperado).

Passo 5 — ENSAIO antes do apply (idioma 48-01/48-03): montar num arquivo temporário a migration + `p49_trilha_smoke.sql` + `p48_reabertura_smoke.sql` + um `DO` final que levanta `ENSAIO_OK`, e rodar com `node p46apply.cjs run` — a requisição aborta por construção e PROD não muda; esperado: erro contendo `ENSAIO_OK` (qualquer `FAIL` antes dele é achado). Só então aplicar: `node p46apply.cjs migrate supabase/migrations/20260922000003_p49_trava_encerrada.sql` e rodar os dois smokes de verdade.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260922000003_p49_trava_encerrada.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');if(!/APLICAR COM: node p46apply\.cjs migrate/.test(s))throw new Error('nota de transporte ausente');for(const t of ['CREATE OR REPLACE FUNCTION public.avancar_etapa','CREATE OR REPLACE FUNCTION public.responder_revisao_decisao','CREATE OR REPLACE FUNCTION public.registrar_decisao','candidatura_encerrada(OLD.etapa_atual, OLD.status)',\"set_config('app.transicao_sancionada', 'reabertura', true)\",\"set_config('app.transicao_sancionada', 'decisao', true)\",\"set_config('app.transicao_sancionada', '', true)\",'check_violation','md5','anon'])if(!c.includes(t))throw new Error('falta: '+t);const r=c.split('CREATE OR REPLACE FUNCTION public.responder_revisao_decisao')[1]||'';const g=r.indexOf('GET DIAGNOSTICS');const z=r.indexOf(\"set_config('app.transicao_sancionada', '', true)\");if(g<0||z<0||z<g)throw new Error('o reset da GUC tem de vir DEPOIS do GET DIAGNOSTICS');console.log('OK')"</automated>
    <fails_when>non-zero exit with "falta:", "wrapper BEGIN proibido", "nota de transporte ausente" or "o reset da GUC tem de vir DEPOIS" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && D=$(mktemp -d) && { cat supabase/migrations/20260922000003_p49_trava_encerrada.sql supabase/tests/p49_trilha_smoke.sql supabase/tests/p48_reabertura_smoke.sql; printf '\nDO $e$ BEGIN RAISE EXCEPTION %s; END $e$;\n' "'ENSAIO_OK'"; } > "$D/ensaio.sql" && node p46apply.cjs run "$D/ensaio.sql" 2>&1 | tee "$D/out.txt" | tail -3; grep -q "ENSAIO_OK" "$D/out.txt" && ! grep -q "FAIL" "$D/out.txt"</automated>
    <fails_when>the output lacks "ENSAIO_OK" (the migration or a smoke aborted first) or contains "FAIL"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs migrate supabase/migrations/20260922000003_p49_trava_encerrada.sql && node p46apply.cjs run supabase/tests/p49_trilha_smoke.sql && node p46apply.cjs run supabase/tests/p48_reabertura_smoke.sql</automated>
    <fails_when>non-zero exit; "md5 do ledger BATE" absent from the migrate output; "FAIL" in either smoke output; or the p48_reabertura result shows "esperado" other than 13</fails_when>
  </verify>
  <acceptance_criteria>
    - A migration existe com pré-portão das 3 funções, a trava, as duas GUCs com reset (o de `responder_revisao_decisao` depois do `GET DIAGNOSTICS`) e o pós-portão.
    - O ensaio abortou com `ENSAIO_OK` e sem `FAIL` ANTES do apply (saída no SUMMARY).
    - Em PROD, `pg_get_functiondef` de `avancar_etapa` contém `candidatura_encerrada(OLD.etapa_atual, OLD.status)`; `responder_revisao_decisao` e `registrar_decisao` contêm `app.transicao_sancionada`.
    - `p49_trilha_smoke.sql` e `p48_reabertura_smoke.sql` (13/13) passam em PROD.
    - O escritor vivo de `etapa_atual` de cada linha da varredura está classificado no SUMMARY.
  </acceptance_criteria>
  <done>Um knockout não pode mais ser empurrado para a avaliação — e a reabertura do Art. 20 continua funcionando.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: justificativa que não gruda (JORN-17), decisão fora da trilha (D-47), portão pela vigente (D-39) e a guarda de reabrir por status (JORN-34)</name>
  <files>supabase/migrations/20260922000004_p49_trilha_justificativa_e_vigente.sql, supabase/tests/p49_trilha_smoke.sql, supabase/tests/p48_rejeicao_triagem_smoke.sql</files>
  <read_first>
    - o VIVO das quatro funções DEPOIS da Task 1 (mesma consulta do read_first dela) — o pré-portão desta migration pina os md5 NOVOS impressos pelo `RAISE NOTICE` da Task 1
    - `supabase/migrations/20260709000010_guard_rejeicao_auditada.sql` (`:53-73` — corpo e o bind `BEFORE UPDATE OF status`)
    - `supabase/tests/p48_rejeicao_triagem_smoke.sql` (`:150-250` — a fixture, `a_just` lido de `candidaturas.etapa_justificativa`, as asserções `:239` e `:245`)
    - `supabase/tests/oper31_rejeitar_candidatura_smokes.sql` (`:155-185` — (c), a regressão com justificativa vazia)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md` §4b (tabela de trechos) e §4e
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md` §H.5, §J.1, §J.3, Correções 11, 20, 28
  </read_first>
  <action>
Escrever `supabase/migrations/20260922000004_p49_trilha_justificativa_e_vigente.sql` (mesmo cabeçalho e transporte), com pré-portão dos md5 vivos pós-Task-1 das quatro funções e:
(a) `avancar_etapa()`: a bandeira de entrevista (o `EXISTS` sobre `entrevista_analises`) ganha `AND public.entrevista_analise_vigente(ea.superada_em, ea.status_analise, ea.competencias)` — o predicado único, nunca o texto dele copiado (D-39); depois do INSERT no histórico e antes do `RETURN NEW`, `NEW.etapa_justificativa := NULL;` com comentário (JORN-17: o valor foi consumido pelo histórico; nenhum trigger posterior lê a coluna — RESEARCH §J.1 mediu: `trg_candidaturas_guard_rejeicao` lê `status`, `update_candidaturas_updated_at`, e o AFTER `trg_candidatura_encerrada_a_pedido` lê `encerrada_a_pedido_em`); reescrever o `COMMENT ON FUNCTION` inteiro com os três acréscimos da fase (trava D-35, limpeza JORN-17, vigente D-39);
(b) `registrar_decisao`: nos dois UPDATEs de `candidaturas`, `etapa_justificativa` passa de `p_justificativa` para a constante `'Decisão final registrada.'` (D-47; comentário: BD-9, o texto fica em `decisao_final.justificativa`, e é o que evita a cópia em `historico_candidatura.criterio_texto`); o pós-portão desta migration exige a constante e a AUSÊNCIA da atribuição de `p_justificativa` a `etapa_justificativa`, além de todas as asserções herdadas;
(c) `guard_rejeicao_auditada`: manter o ramo existente e acrescentar o irmão — se `public.candidatura_encerrada(OLD.etapa_atual, OLD.status)` e NÃO `public.candidatura_encerrada(NEW.etapa_atual, NEW.status)` e `auth.uid() IS NOT NULL` e `coalesce(current_setting('app.transicao_sancionada', true), '')` não é `reabertura` ⇒ `RAISE EXCEPTION 'candidatura encerrada não pode ser reaberta pelo status — use o pedido de revisão (Art. 20)' USING ERRCODE = 'check_violation'`. Comentário com a razão do escopo por `auth.uid()` (idioma de fixture de 8 smokes, RESEARCH Correção 20) e que o PATCH pela policy `rh_avanca_etapa` sempre tem JWT. `REVOKE ALL … FROM PUBLIC; REVOKE ALL … FROM anon`;
(d) pós-portão por catálogo (asserções de presença E de ausência das formas antigas) + `RAISE NOTICE` com os md5 novos.

ANTES do apply (premissa A4 do RESEARCH): para cada um dos 8 smokes do idioma (`p42_revisao_art20_smoke.sql:508`, `p45_motor_exclusao_smoke.sql:880,2284`, `p48_candidatura_encerrada_smoke.sql:264`, `p48_dedupe_smoke.sql:187,320`, `p48_cognitivo_notifica_smoke.sql:137`, `p48_reabertura_smoke.sql:188,482`, `p48_rejeicao_triagem_smoke.sql:161`, `p48_prazo_reabertura_smoke.sql:183`), ler as linhas anteriores ao UPDATE de status e registrar no SUMMARY se `request.jwt.claims` está vazio ali (a guarda só não morde a fixture se estiver); e se algum desses UPDATEs também muda `etapa_atual` de uma linha encerrada (a trava da Task 1 o recusaria). Qualquer caso que a guarda ou a trava recuse = ajustar a fixture NESTE plano (INSERT já com a etapa final + UPDATE só de status) com comentário de proveniência, nunca afrouxar a guarda.

`p48_rejeicao_triagem_smoke.sql`: `a_just` passa a vir de `historico_candidatura.criterio_texto` da transição `→ rejeitado` da fixture (o texto sobrevive lá byte a byte); `:239` e `:245` mudam DE PROPÓSITO com comentário (JORN-17), e acrescentar a asserção de que `candidaturas.etapa_justificativa` ficou NULL depois da rejeição.

`p49_trilha_smoke.sql`: acrescentar (d) o histórico da decisão de (c) tem `criterio_texto = 'Decisão final registrada.'` e `decisao_final.justificativa` segue com o texto (D-47); (e) `etapa_justificativa` NULL depois de cada transição de (b), (c) e de um avanço comum com justificativa; (f) regressão: avançar com justificativa X e em seguida regredir SEM mandar `etapa_justificativa` ⇒ recusado com «Regressão de etapa exige justificativa» (antes, X grudado desarmava o portão); (h) JORN-34: com claims de RH, UPDATE só de `status` `rejeitado → em_analise` numa fixture encerrada ⇒ `check_violation`; sem claims ⇒ aceito (o idioma de fixture segue funcionando); (i) vigente: fixture em `entrevista_online` com uma análise SUPERADA (`superada_em` preenchida) com `bloqueio_avanco = true` e sem revisão ⇒ o avanço para `entrevista_presencial` passa; com uma VIGENTE bloqueada sem revisão ⇒ recusado com a mensagem da bandeira. Subir o esperado do gate para o total final e registrar no cabeçalho.

Aplicar (ensaio com a migration + os três smokes em requisição que aborta, como na Task 1; depois `migrate`) e rodar `p49_trilha_smoke.sql`, `p48_rejeicao_triagem_smoke.sql`, `p48_reabertura_smoke.sql`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260922000004_p49_trilha_justificativa_e_vigente.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');for(const t of ['CREATE OR REPLACE FUNCTION public.avancar_etapa','CREATE OR REPLACE FUNCTION public.registrar_decisao','CREATE OR REPLACE FUNCTION public.guard_rejeicao_auditada','entrevista_analise_vigente(ea.superada_em, ea.status_analise, ea.competencias)','NEW.etapa_justificativa := NULL',\"'Decisão final registrada.'\",'auth.uid() IS NOT NULL','candidatura_encerrada(NEW.etapa_atual, NEW.status)','md5'])if(!c.includes(t))throw new Error('falta: '+t);if(/etapa_justificativa\s*=\s*p_justificativa/.test((c.split('CREATE OR REPLACE FUNCTION public.registrar_decisao')[1]||'').split('CREATE OR REPLACE FUNCTION')[0]))throw new Error('registrar_decisao ainda copia a justificativa para a trilha (D-47)');console.log('OK')"</automated>
    <fails_when>non-zero exit with "falta:", "wrapper BEGIN proibido" or "ainda copia a justificativa" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && D=$(mktemp -d) && { cat supabase/migrations/20260922000004_p49_trilha_justificativa_e_vigente.sql supabase/tests/p49_trilha_smoke.sql supabase/tests/p48_rejeicao_triagem_smoke.sql supabase/tests/p48_reabertura_smoke.sql; printf '\nDO $e$ BEGIN RAISE EXCEPTION %s; END $e$;\n' "'ENSAIO_OK'"; } > "$D/ensaio.sql" && node p46apply.cjs run "$D/ensaio.sql" 2>&1 | tee "$D/out.txt" | tail -3; grep -q "ENSAIO_OK" "$D/out.txt" && ! grep -q "FAIL" "$D/out.txt"</automated>
    <fails_when>the output lacks "ENSAIO_OK" or contains "FAIL"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs migrate supabase/migrations/20260922000004_p49_trilha_justificativa_e_vigente.sql && node p46apply.cjs run supabase/tests/p49_trilha_smoke.sql && node p46apply.cjs run supabase/tests/p48_rejeicao_triagem_smoke.sql && node p46apply.cjs run supabase/tests/p48_reabertura_smoke.sql</automated>
    <fails_when>non-zero exit; "md5 do ledger BATE" absent from the migrate output; or "FAIL" in any smoke output</fails_when>
  </verify>
  <acceptance_criteria>
    - Em PROD, `avancar_etapa` contém `entrevista_analise_vigente(` e `NEW.etapa_justificativa := NULL`; `registrar_decisao` contém `'Decisão final registrada.'` e não contém a atribuição de `p_justificativa` a `etapa_justificativa`; `guard_rejeicao_auditada` contém o ramo com `auth.uid() IS NOT NULL`.
    - A classificação dos 8 smokes (claims vazias? etapa mudando?) está no SUMMARY, com as fixturas ajustadas se houve caso.
    - `p49_trilha_smoke`, `p48_rejeicao_triagem_smoke` e `p48_reabertura_smoke` passam em PROD.
  </acceptance_criteria>
  <done>A trilha deixa de herdar texto de outra transição, deixa de levar ao titular a justificativa da decisão final, e o portão de avanço deixa de ser travado por uma análise que já foi superada.</done>
</task>

<task type="auto">
  <name>Task 3: regressão dos smokes que usam a mesma função — e a prova de que o portão morde</name>
  <files>supabase/tests/p49_trilha_smoke.sql</files>
  <read_first>
    - o estado deixado pelas Tasks 1–2 e o corpo ANTERIOR de `avancar_etapa` guardado no scratchpad (read_first da Task 1)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/deferred-items.md` (item «Smokes de regressão que COMMITAM fixture…»: o `oper31` roda em envelope que aborta)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/48-01-SUMMARY.md` (seção «O portão morde (medido nesta sessão)» — o formato do registro da mutação)
  </read_first>
  <action>
Regressão em PROD, cada uma registrada no SUMMARY com o resultado: `p45_motor_exclusao_smoke.sql`, `p42_revisao_art20_smoke.sql`, `p48_candidatura_encerrada_smoke.sql`, `p48_dedupe_smoke.sql`, `p48_cognitivo_notifica_smoke.sql`, `p48_prazo_reabertura_smoke.sql` por `node p46apply.cjs run` puro (revertem por subtransação), e `oper31_rejeitar_candidatura_smokes.sql` em ENVELOPE que aborta (ele COMMITA fixture ligada a candidato real — deferred da 48): arquivo temporário com o smoke + `DO` final que levanta `ENVELOPE ready=%` com `current_setting('smoke.ready', true)`; esperado `ENVELOPE ready=y` e a (c) passando. Se algum reprovar pela trava ou pela guarda nova, é fixture: ajustar no idioma da Task 2 (no arquivo do smoke, com comentário de proveniência) — nunca afrouxar a função.

Mutação (D-56): em arquivo temporário, `CREATE OR REPLACE` de `avancar_etapa` com o corpo ANTERIOR à Task 1 + `p49_trilha_smoke.sql` + `DO` final que levanta `MUTACAO_TERMINOU`, numa requisição só (`node p46apply.cjs run`), que aborta por construção. Esperado: a saída contém `FAIL` nas asserções da trava (a/a2), da limpeza (e/f) e da vigente (i) ANTES de `MUTACAO_TERMINOU`. Repetir com o `registrar_decisao` anterior (esperado `FAIL (d)`) e com o `guard_rejeicao_auditada` anterior (esperado `FAIL (h)`). Registrar as três saídas no SUMMARY, e conferir em seguida, só leitura, que o `md5(prosrc)` vivo das quatro funções é o impresso pelo pós-portão da Task 2 (a mutação não vazou).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && D=$(mktemp -d) && for s in p45_motor_exclusao_smoke p42_revisao_art20_smoke p48_candidatura_encerrada_smoke p48_dedupe_smoke p48_cognitivo_notifica_smoke p48_prazo_reabertura_smoke; do node p46apply.cjs run supabase/tests/$s.sql > "$D/$s.txt" 2>&1 || { echo "REPROVOU: $s"; tail -5 "$D/$s.txt"; exit 1; }; grep -q "FAIL" "$D/$s.txt" && { echo "FAIL em $s"; exit 1; }; echo "ok $s"; done</automated>
    <fails_when>non-zero exit with "REPROVOU: <smoke>" or "FAIL em <smoke>" printed</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && D=$(mktemp -d) && { cat supabase/tests/oper31_rejeitar_candidatura_smokes.sql; printf '\nDO $e$ BEGIN RAISE EXCEPTION %s, current_setting(%s, true); END $e$;\n' "'ENVELOPE ready=%'" "'smoke.ready'"; } > "$D/env.sql" && node p46apply.cjs run "$D/env.sql" 2>&1 | tee "$D/out.txt" | tail -3; grep -q "ENVELOPE ready=y" "$D/out.txt"</automated>
    <fails_when>the output lacks "ENVELOPE ready=y" (the fixture was not built, or an OPER-31 assertion raised FAIL before the envelope)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs sql "SET TRANSACTION READ ONLY; select p.proname as f, p.prosrc as s from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public' and p.proname in ('avancar_etapa','registrar_decisao','responder_revisao_decisao','guard_rejeicao_auditada')" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=Object.fromEntries(JSON.parse(s).map(x=>[x.f,x.s]));const exige={avancar_etapa:['candidatura_encerrada(OLD.etapa_atual, OLD.status)','entrevista_analise_vigente(','NEW.etapa_justificativa := NULL'],registrar_decisao:['app.transicao_sancionada','Decisão final registrada.'],responder_revisao_decisao:['app.transicao_sancionada'],guard_rejeicao_auditada:['auth.uid() IS NOT NULL']};const faltas=[];for(const [f,ts] of Object.entries(exige))for(const t of ts)if(!(r[f]||'').includes(t))faltas.push(f+': '+t);if(faltas.length){console.error('corpo vivo sem o conserto (a mutacao vazou?): '+faltas.join(' | '));process.exit(1)}console.log('os quatro corpos vivos carregam o conserto')})"</automated>
    <fails_when>non-zero exit with "corpo vivo sem o conserto" (a mutation leaked into PROD, or an apply did not land)</fails_when>
  </verify>
  <acceptance_criteria>
    - Os 6 smokes de regressão passam por `run` puro e o `oper31` passa em envelope (`ready=y`); qualquer fixture ajustada tem comentário de proveniência.
    - As três mutações reprovaram nas asserções esperadas (saídas no SUMMARY).
    - Os md5 vivos das quatro funções são os do pós-portão (registrados no SUMMARY).
  </acceptance_criteria>
  <done>O conserto da trilha está provado por execução, e o smoke novo reprova quando o conserto é desfeito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH (JWT) → PostgREST `candidaturas` (policy `rh_avanca_etapa`) | PATCH direto de etapa/status passa pelos triggers BEFORE |
| RH/titular (JWT) → RPCs SECURITY DEFINER (`registrar_decisao`, `responder_revisao_decisao`) | as únicas transições sancionadas de encerrada |
| banco → `trg_notif_transicao` → EF `notificar-candidato` | o histórico gravado vira e-mail |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-06-01 | Tampering | knockout/encerrada movida de etapa por PATCH ou tela | high | mitigate | trava D-35 em `avancar_etapa` por predicado canônico; smoke (a)/(a2) |
| T-49-06-02 | Elevation of Privilege | GUC de sanção vazando para UPDATE posterior da mesma transação | high | mitigate | reset logo após o UPDATE (depois do `GET DIAGNOSTICS`); smoke (g) |
| T-49-06-03 | Information Disclosure | justificativa da decisão final chegando ao titular pela trilha (BD-9) | high | mitigate | constante sem PII em `etapa_justificativa`; smoke (d) |
| T-49-06-04 | Tampering | reabrir encerrada só por status sem trilha | high | mitigate | ramo novo em `guard_rejeicao_auditada` escopado a `auth.uid() IS NOT NULL`; smoke (h) |
| T-49-06-05 | Denial of Service | trava recusando a própria reabertura (D-01) | high | mitigate | GUC `reabertura`; `p48_reabertura_smoke` 13/13 com asserção nova da recusa sem GUC |
| T-49-06-06 | Tampering | CREATE OR REPLACE apagando divergência viva | high | mitigate | pré-portão md5 por função; pós-portão com presença e ausência |
| T-49-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação — só SQL |
</threat_model>

<verification>
- Duas migrations aplicadas (ensaio antes de cada uma), md5 do ledger conferido.
- `p49_trilha_smoke`, `p48_reabertura_smoke` (13), `p48_rejeicao_triagem_smoke`, 6 regressões e `oper31` em envelope verdes.
- Três mutações reprovam; md5 vivos = pós-portão.
</verification>

<success_criteria>
O banco recusa mover quem já saiu do funil, a trilha registra cada transição com o seu próprio motivo, e o titular não recebe pela trilha o que o BD-9 guarda dele.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| GUC `app.transicao_sancionada` (`reabertura`, `decisao`) | configuração de transação | **nova** |
| `public.avancar_etapa()` | função de trigger | editada (trava D-35, limpeza JORN-17, vigente D-39) |
| `public.registrar_decisao(uuid, decisao_final_resultado, text)` | RPC | editada (GUC + D-47 `'Decisão final registrada.'`) |
| `public.responder_revisao_decisao(...)` | RPC | editada (GUC reabertura) |
| `public.guard_rejeicao_auditada()` | função de trigger | editada (ramo JORN-34) |
| `supabase/migrations/20260922000003_p49_trava_encerrada.sql` | migration | **nova** |
| `supabase/migrations/20260922000004_p49_trilha_justificativa_e_vigente.sql` | migration | **nova** |
| `supabase/tests/p49_trilha_smoke.sql` (`smoke49t.pass`, SQLSTATE `P49T1`) | smoke | **novo** |
| `supabase/tests/p48_reabertura_smoke.sql` | smoke | editado (F4 pela GUC + asserção nova; 12 → 13) |
| `supabase/tests/p48_rejeicao_triagem_smoke.sql` | smoke | editado (`:239`/`:245` pelo histórico) |

## Premissas sinalizadas (probe de borda)

JORN-25, JORN-17, JORN-37, JORN-34 e JORN-12 saíram `unclassified` no probe (texto pt-BR) e ficam
`unresolved` no relatório; as bordas foram escritas a partir do RESEARCH §H/§J.

- **A4 (RESEARCH)** — os 8 smokes do idioma rodam o UPDATE de status sem JWT: conferido smoke a smoke na
  Task 2 antes do apply, não presumido.
- Uma escrita de `etapa_justificativa` SEM mudança de etapa (o trigger `BEFORE UPDATE OF etapa_atual` nem
  dispara) ainda seria consumida pela próxima transição. A varredura C2 da Task 1 registra os escritores; o
  kickoff mediu que a UI sempre manda a justificativa junto com a etapa (`triagemService.ts:446-458`), e o
  D-46 (plano 49-12) limpa o resíduo. Registrado como premissa, não como conserto.
- A constante do D-47 é `'Decisão final registrada.'` (o RESEARCH sugeria um parêntese técnico que citava
  o nome da tabela; o histórico vai na cópia do titular, então o texto precisa ser legível para ele).
- **Ordem de apply:** as `…000003`/`…000004` dividem a onda 2 com a `…000005` do 49-08, que toca só a linha
  `comparative_ranking` de `prompt_versions` — objetos disjuntos (dono único), então a ordem relativa dentro da
  onda não muda o estado final; entre ondas, a ordem de apply é a ordem de versão.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-06-SUMMARY.md` when done
</output>
