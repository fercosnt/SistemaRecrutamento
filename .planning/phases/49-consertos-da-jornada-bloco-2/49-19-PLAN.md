---
phase: 49-consertos-da-jornada-bloco-2
plan: 19
type: execute
wave: 9
depends_on: ["49-14", "49-20", "49-21", "49-17", "49-18"]
files_modified:
  - supabase/tests/p49_motor_antes_depois.sql
  - .planning/phases/49-consertos-da-jornada-bloco-2/49-PROVA-PROD.md
autonomous: false
requirements: [JORN-36]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-54: a PRIMEIRA execução real do passo novo do motor (D-48, D-60..D-63) acontece sobre a conta DESCARTÁVEL de teste criada no 49-18, com autorização explícita do operador na sessão, e com contagem antes/depois por tabela e por coluna — nunca sobre pessoa real."
    - "A execução passa pelo caminho real: o pedido de exclusão feito pela própria conta na tela, a antecipação de `executar_em` por UPDATE de `WHERE` estreito (candidato + tipo + situação — escrita retroativa autorizada no mesmo checkpoint) e a chamada da EF `executar-direito-titular` (`acao: 'executar'`) com o JWT do titular — o precedente do 45-11, que mediu o motor inteiro (EF, claims, ordem dos três sistemas) e não a RPC isolada. Credencial da conta só na sessão, nunca gravada em arquivo."
    - "Depois da execução, para a conta descartável: 0 linhas em `respostas_raven`/`respostas_bigfive`/`respostas_disc`/`respostas_formulario` (D-62); sentinela nas colunas de texto/jsonb do recibo (`redacoes_candidato.texto`, `respostas_cultura.resposta_texto`, `respostas_avaliacao.respostas`, `cognitivo_respostas.*`…); nenhum `cited_evidence` na análise da redação nem na `metadata` da SJT; `entrevista_analises.citacoes`/`scores_candidato.citacoes` nulas; nenhuma linha de `ai_call_logs` com o `candidato_id` dela, e as que tinham (ids capturados ANTES) com `user_prompt_template` em sentinela (D-61); as linhas `comparative_ranking` que citavam as candidaturas dela redigidas inteiras (D-63); `revisao_resultado` em sentinela onde havia (D-60); `scores_raven`/`scores_candidato` presentes; Storage sob o prefixo = 0; o usuário de Auth não existe; o pedido concluído com recibo enviado."
    - "Nenhum outro titular foi tocado: as contagens globais das tabelas do passo, descontada a conta descartável, são iguais antes e depois; a linha `comparative_ranking` que NÃO citava a conta segue intacta."
    - "O recibo recebido pela caixa da conta descartável diz, sobre respostas, textos, transcrições e conteúdo enviado às análises automáticas, o que o banco mostra que foi feito (conferência humana contra a consulta)."
    - "A consulta `p49_motor_antes_depois.sql` é só leitura (só SELECT, rodada sempre com o prefixo `SET TRANSACTION READ ONLY`), parametrizada pelo `candidato_id` da conta (`p49.titular`), e registra no `49-PROVA-PROD.md` as colunas `p36_*` todas `true` depois."
  artifacts:
    - path: supabase/tests/p49_motor_antes_depois.sql
      provides: "contagem por tabela/coluna da conta descartável e dos demais, antes e depois; colunas p36_*"
      contains: "p49.titular"
    - path: .planning/phases/49-consertos-da-jornada-bloco-2/49-PROVA-PROD.md
      provides: "evidência do portão: autorização, antes, depois, recibo"
      contains: "p36_"
  key_links:
    - from: "EF executar-direito-titular (JWT do titular)"
      to: "anonimizar_candidato (passo apagar_respostas_e_producoes)"
      via: "o caminho real do Art. 18 — pedido, prazo antecipado, execução"
      pattern: "apagar_respostas_e_producoes"
  prohibitions:
    - requirement_id: JORN-36
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT executar a exclusão sem a autorização explícita do operador na sessão, nem sobre conta que não seja a descartável criada para isto"
    - requirement_id: JORN-36
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT gravar a senha ou o JWT da conta descartável em arquivo do repositório ou do planejamento"
---

<objective>
Executar, uma vez e sob o portão do operador, o motor de exclusão com o passo novo sobre a conta descartável da
jornada — medindo antes e depois, tabela a tabela e coluna a coluna — e conferir que o recibo que ela recebe diz o
que o banco mostra.

Purpose: D-48 é destrutivo e irreversível a cada execução; D-54 manda que a primeira execução real seja checkpoint
com contagem antes/depois. Os smokes do 49-14 e do 49-20 provaram o motor novo dentro de transações revertidas; aqui ele roda de verdade,
pelo caminho do titular (EF + JWT + ordem dos três sistemas), como o 45-11 fez com o motor original.

Output: a consulta antes/depois, a evidência do portão em `49-PROVA-PROD.md`.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-14-SUMMARY.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-20-SUMMARY.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-21-SUMMARY.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PROVA-PROD.md
@.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-11-EVIDENCIA-PORTAO.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): a consulta antes/depois e o ANTES medido — com o plano do motor (dry-run) para a conta</name>
  <precondition>O `49-PROVA-PROD.md` tem o `candidato_id` da conta descartável (49-18, Task 2) e a EF `executar-direito-titular` viva contém `apagar_respostas_e_producoes` (prontidão do 49-18).</precondition>
  <files>supabase/tests/p49_motor_antes_depois.sql, .planning/phases/49-consertos-da-jornada-bloco-2/49-PROVA-PROD.md</files>
  <read_first>
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-11-EVIDENCIA-PORTAO.md` (como a execução real foi disparada e medida: tabela ANTES × DEPOIS, negativas, re-identificação)
    - `supabase/tests/p45_motor_exclusao_smoke.sql` (as asserções do passo novo — a mesma lista de colunas, agora sobre a conta real de teste)
    - `docs/compliance/sql/gen-recibo-exclusao.cjs` (as origens dos itens `respostas_e_producoes` e `dados_enviados_a_analise_automatica`)
    - o vivo de `plano_exclusao_titular` (assinatura e o que exige de claims)
  </read_first>
  <action>
Criar `supabase/tests/p49_motor_antes_depois.sql`: só SELECT e SEM `SET TRANSACTION` no arquivo (quem roda prefixa `SET TRANSACTION READ ONLY;` e os dois `set_config`, como no `p48_prova_prod.sql` — o `SET TRANSACTION` tem de ser a primeira instrução), lê `current_setting('p49.titular')::uuid`, e devolve UMA linha com: por tabela do passo novo, a contagem de linhas da conta e a contagem GLOBAL menos a da conta; por coluna de texto/jsonb do recibo, quantas linhas da conta estão em sentinela e quantas não; se há `cited_evidence` na análise da redação e na `metadata` da SJT da conta; `ai_call_logs` com o `candidato_id` da conta (os ids); linhas `comparative_ranking` que citam `id=` de candidatura da conta (os ids) e quantas delas estão redigidas; `revisao_resultado` na corrente e no arquivo da conta; `scores_raven`/`scores_candidato` da conta; e as colunas `p36_*` (booleanas do DEPOIS esperado: `p36_respostas_apagadas`, `p36_textos_em_sentinela`, `p36_sem_citacao_literal`, `p36_logs_redigidos`, `p36_comparativos_redigidos`, `p36_revisao_redigida_ou_ausente`, `p36_scores_preservados`, `p36_outros_intactos` — comparando com o ANTES passado por `current_setting('p49.antes')::jsonb`). Nenhuma escrita (verify confere).

Medir o ANTES: rodar a consulta com `SET TRANSACTION READ ONLY; SELECT set_config('p49.titular', '<id>', false); SELECT set_config('p49.antes', '{}', false);` prefixados (o id vem da linha `DESCARTAVEL_CANDIDATO_ID: <uuid>` que o 49-18 gravou) e gravar a saída inteira no `49-PROVA-PROD.md` (seção «Motor — ANTES»), com o JSON do ANTES entre as linhas `ANTES_JSON_BEGIN` e `ANTES_JSON_END` (é o que a Task 3 passa de volta como `p49.antes`), incluindo a lista de tabelas do passo que a conta NÃO populou pelo fluxo real (essas ficam cobertas só pelos smokes do 49-14/49-20 — registrar como tal). Rodar também o dry-run do motor para a conta (`plano_exclusao_titular`, com as claims que ele exige, dentro de uma transação READ ONLY) e gravar o plano — as contagens do plano têm de bater com o ANTES.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const c=require('fs').readFileSync('supabase/tests/p49_motor_antes_depois.sql','utf8');if(/SET\s+TRANSACTION/i.test(c.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n')))throw new Error('o arquivo nao pode ter SET TRANSACTION (quem roda prefixa)');const code=c.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/\b(INSERT|UPDATE|DELETE)\s/i.test(code))throw new Error('a consulta escreve');for(const k of ['p49.titular','p36_respostas_apagadas','p36_logs_redigidos','p36_comparativos_redigidos','p36_outros_intactos'])if(!c.includes(k))throw new Error('falta: '+k);console.log('OK')" && grep -q "Motor — ANTES" .planning/phases/49-consertos-da-jornada-bloco-2/49-PROVA-PROD.md</automated>
    <fails_when>non-zero exit with "nao pode ter SET TRANSACTION", "a consulta escreve" or "falta:" in stderr; or 49-PROVA-PROD.md without the «Motor — ANTES» section</fails_when>
  </verify>
  <acceptance_criteria>
    - A consulta existe, só SELECT (rodada com o prefixo READ ONLY), parametrizada, com as colunas `p36_*`.
    - O ANTES e o plano (dry-run) da conta estão no `49-PROVA-PROD.md`, com as contagens batendo; as tabelas não populadas pelo fluxo real estão listadas.
  </acceptance_criteria>
  <done>O operador tem, antes de autorizar, o que exatamente vai ser apagado da conta descartável — e o que não vai.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: a execução real, com autorização explícita — pedido pela conta, prazo antecipado, EF com o JWT do titular</name>
  <files>.planning/phases/49-consertos-da-jornada-bloco-2/49-PROVA-PROD.md</files>
  <read_first>
    - a seção «Motor — ANTES» do `49-PROVA-PROD.md`
    - `.planning/phases/45-motor-de-exclus-o-anonimiza-o/45-11-EVIDENCIA-PORTAO.md` («Como foi disparado»)
  </read_first>
  <action>Claude para e apresenta o ANTES e o plano ao operador. Só com a autorização explícita («autorizo a execução do motor na conta descartável <e-mail>»): o operador faz o pedido de exclusão pela tela da conta (`/candidato/privacidade`); Claude antecipa `executar_em` por um UPDATE com `WHERE` estreito (o `candidato_id` da conta + tipo de pedido + situação pendente, com `ROW_COUNT` = 1 exigido e contagem antes/depois); e a EF `executar-direito-titular` é chamada com `{ "acao": "executar" }` e o JWT da conta — pelo operador, com o comando que Claude entrega, ou por Claude com a credencial que o operador passar NA SESSÃO (nunca gravada). A resposta da EF vai para o `49-PROVA-PROD.md`.</action>
  <instructions>
Claude apresenta o ANTES e o plano. Se estiver de acordo:
1. Digite «autorizo a execução do motor na conta descartável <e-mail da conta>».
2. Entre na conta descartável e peça a exclusão em «Seus dados e autorizações».
3. Avise «pedido feito» — Claude antecipa o prazo e prepara a chamada.
4. Rode (ou autorize Claude a rodar) a chamada da EF com a sessão da conta.
5. Confira a caixa: o aviso do pedido e, depois da execução, o recibo.
  </instructions>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && grep -q "autoriz" .planning/phases/49-consertos-da-jornada-bloco-2/49-PROVA-PROD.md && grep -q '"acao":"executar"' .planning/phases/49-consertos-da-jornada-bloco-2/49-PROVA-PROD.md</automated>
    <fails_when>non-zero exit (the explicit authorization or the EF response for acao executar is not recorded in 49-PROVA-PROD.md)</fails_when>
  </verify>
  <acceptance_criteria>
    - A frase de autorização do operador, o UPDATE de `executar_em` (1 linha, antes/depois) e a resposta da EF estão no `49-PROVA-PROD.md`; nenhuma credencial gravada.
  </acceptance_criteria>
  <verification>Claude confere que a frase de autorização, a antecipação (1 linha) e a resposta `{"ok":true,"acao":"executar",…}` estão registradas, e que nenhum arquivo do repositório contém a senha ou o JWT da conta (`git grep` pelo e-mail da conta devolve só o `49-PROVA-PROD.md`).</verification>
  <resume-signal>Digite a frase de autorização e, depois, «executado» (ou descreva o que não bateu)</resume-signal>
  <done>O motor novo rodou de verdade, uma vez, sobre a conta criada para isso, com a autorização do operador.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: o DEPOIS — contagem por coluna, os outros intactos e o recibo conferido contra o banco</name>
  <files>.planning/phases/49-consertos-da-jornada-bloco-2/49-PROVA-PROD.md</files>
  <read_first>
    - as seções «Motor — ANTES» e a resposta da EF no `49-PROVA-PROD.md`
    - o recibo recebido na caixa da conta descartável
  </read_first>
  <action>Claude roda `p49_motor_antes_depois.sql` com `p49.titular` e `p49.antes` (o JSON do ANTES), grava o DEPOIS, e confere também Storage (0 objetos sob o prefixo) e Auth (o usuário não existe) pela Management API. O operador confere que o recibo recebido diz, item a item, o que o DEPOIS mostra.</action>
  <what-built>A primeira exclusão real com o passo `apagar_respostas_e_producoes`.</what-built>
  <how-to-verify>
    1. Ler o DEPOIS no `49-PROVA-PROD.md`: todas as `p36_*` = true.
    2. Abrir o recibo na caixa da conta descartável e conferir os itens «O que você escreveu, respondeu e falou» e «conteúdo enviado às análises automáticas» contra o DEPOIS.
    3. Responder «recibo confere» (ou o que divergiu).
  </how-to-verify>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && TIT=$(grep -m1 "^DESCARTAVEL_CANDIDATO_ID: " .planning/phases/49-consertos-da-jornada-bloco-2/49-PROVA-PROD.md | cut -d' ' -f2) && ANTES=$(sed -n '/^ANTES_JSON_BEGIN$/,/^ANTES_JSON_END$/p' .planning/phases/49-consertos-da-jornada-bloco-2/49-PROVA-PROD.md | sed '1d;$d' | tr -d '\n') && D=$(mktemp -d) && { echo "SET TRANSACTION READ ONLY; SELECT set_config('p49.titular', '$TIT', false); SELECT set_config('p49.antes', '$ANTES', false);"; cat supabase/tests/p49_motor_antes_depois.sql; } > "$D/d.sql" && node p46apply.cjs run "$D/d.sql" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s.slice(s.indexOf('[')))[0];const f=Object.entries(r).filter(([k,v])=>k.startsWith('p36_')&&v!==true).map(([k])=>k);if(f.length){console.error('MOTOR NAO PROVADO: '+f.join(', '));process.exit(1)}console.log('p36: todas true')})"</automated>
    <fails_when>non-zero exit with "MOTOR NAO PROVADO: <colunas>"; or the DESCARTAVEL_CANDIDATO_ID / ANTES_JSON block is missing from 49-PROVA-PROD.md</fails_when>
    <human-check>
      <test>O recibo na caixa da conta descartável, item a item, contra o DEPOIS</test>
      <expected>O que o recibo diz ter sido apagado está apagado no banco; o que diz ficar, ficou</expected>
      <why_human>o texto do e-mail recebido — o banco prova o apagamento, não a frase lida</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - DEPOIS gravado; todas as `p36_*` = true; Storage 0 e Auth ausente registrados; `p36_outros_intactos` = true.
    - A conferência do recibo pelo operador está no `49-PROVA-PROD.md`.
  </acceptance_criteria>
  <resume-signal>Digite «recibo confere» (ou descreva o que divergiu)</resume-signal>
  <done>O recibo de exclusão deixou de prometer o que o motor não fazia — provado numa exclusão real, coluna a coluna.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| titular descartável (JWT) → EF `executar-direito-titular` → motor | mecanismo destrutivo, uma execução real |
| orquestrador → `solicitacoes_dados.executar_em` | escrita retroativa estreita, autorizada no checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-19-01 | Tampering | exclusão de conta errada | high | mitigate | `candidato_id` da descartável registrado no 49-18; UPDATE com `WHERE` estreito e `ROW_COUNT` = 1; autorização com o e-mail da conta |
| T-49-19-02 | Information Disclosure | senha/JWT da conta gravados | medium | mitigate | credencial só na sessão; `git grep` no verification |
| T-49-19-03 | Repudiation | execução sem registro de autorização | high | mitigate | frase do operador + ANTES/DEPOIS no `49-PROVA-PROD.md` |
| T-49-19-04 | Tampering | outro titular afetado | high | mitigate | `p36_outros_intactos` (contagens globais menos a conta, antes = depois) |
| T-49-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- ANTES e plano registrados; autorização explícita; execução pela EF com o JWT do titular; DEPOIS com todas as `p36_*` = true; recibo conferido.
</verification>

<success_criteria>
Uma exclusão real, sobre a conta criada para isso, apagou exatamente o que o recibo diz — e nada de outro titular.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `supabase/tests/p49_motor_antes_depois.sql` (colunas `p36_*`, parâmetros `p49.titular`/`p49.antes`) | consulta só leitura | **nova** |
| seções «Motor — ANTES», `DESCARTAVEL_CANDIDATO_ID:`, `ANTES_JSON_BEGIN/END`, «Motor — DEPOIS» | `49-PROVA-PROD.md` | editado |

## Premissas sinalizadas (probe de borda)

JORN-36 saiu `unclassified` no probe e fica `unresolved` no relatório; a prova aqui é a execução real medida. Tabelas
do passo que a conta descartável não alcança pelo fluxo real (ex.: `entrevistas_online`/`entrevistas_presenciais`,
sem escritor desde 2025) ficam provadas só pelo smoke revertido do 49-20 — registrado no ANTES, não omitido.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-19-SUMMARY.md` when done
</output>
