---
phase: 49-consertos-da-jornada-bloco-2
plan: 22
type: execute
wave: 5
# 49-15 e 49-16 não produzem nada que este plano consome: são arestas de ACOPLAMENTO. Os dois
# rodam `vitest run src/features/triagem` no verify (onda 4) e este plano edita testes dessa pasta
# em TDD (vermelho primeiro) — em paralelo, o vermelho deste reprovaria o verify deles.
depends_on: ["49-13", "49-05", "49-03", "49-08", "49-15", "49-16"]
files_modified:
  - src/features/triagem/components/TriagemTable.tsx
  - src/features/triagem/components/__tests__/TriagemTable.test.tsx
  - src/components/pages/VagaCandidatosRHPage.tsx
  - src/components/pages/ComparativoCandidatosPage.tsx
  - src/components/pages/__tests__/ComparativoCandidatosPage.test.tsx
  - src/features/triagem/components/ComparativoScreen.tsx
  - src/features/triagem/components/__tests__/ComparativoScreen.test.tsx
  - src/features/decisao/services/decisaoService.ts
  - src/features/decisao/services/__tests__/decisaoService.test.ts
  - src/features/decisao/components/DecisaoFinalPage.tsx
  - src/features/decisao/components/__tests__/DecisaoFinalPage.test.tsx
autonomous: true
requirements: [JORN-25, JORN-28]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-34: na seleção do comparativo da vaga, candidatura encerrada pelo predicado canônico aparece com selo (`data-testid=\"triagem-selo-encerrada\"`) e checkbox desabilitado com tooltip próprio; retirada a pedido CONTINUA selecionável (invariante da Phase 45, `TriagemTable.test.tsx` ~188 — D-56)."
    - "D-59: o teto da seleção (e as mensagens «Máximo de …») vem de `COMPARATIVO_MAX_CANDIDATOS`/`COMPARATIVO_MIN_CANDIDATOS` de `supabase/functions/_shared/comparativo-config.ts` — nenhum literal de teto sobra no front (padrão P3 da varredura C1 zerado)."
    - "D-36: o «Avançar» do comparativo leva cada candidato à SUA próxima etapa de trabalho real, por `proximaEtapaDeTrabalho(etapa_atual)` (o mesmo util do hub e do Kanban, plano 49-05); candidato sem próxima etapa de trabalho não recebe o botão. O `PROXIMA_ETAPA_APOS_TRIAGEM` deixa de ser usado pelo comparativo."
    - "D-36b: o comparativo da tela de decisão final compara quem está em `etapa_atual='decisao_final'` e não é encerrado (`listFinalistas` sobre `candidaturas`, filtrado pelo predicado); com menos de 2 a tela diz a verdade (estado vazio — Correção 23: hoje 1 em PROD); com mais que o teto, diz que o comparativo aceita até 4 e aponta o comparativo da vaga — sem cortar em silêncio."
    - "A decisão final rotula cada posição pela chave `posicoes` (a mesma regra do 49-13) e mostra o `ProvenienciaIABadge` (49-13) no ranking (D-27b)."
    - "Texto novo passa pelo guard `forbidden-strings.grep.test.ts` (D-58, D-07); `tsc` ≤ 90; build com o marcador no chunk certo; push; marcador no ar."
  artifacts:
    - path: src/features/triagem/components/TriagemTable.tsx
      provides: "encerrada com selo e sem seleção; teto pela constante"
      contains: "COMPARATIVO_MAX_CANDIDATOS"
    - path: src/components/pages/ComparativoCandidatosPage.tsx
      provides: "Avançar para a próxima etapa real"
      contains: "proximaEtapaDeTrabalho"
    - path: src/features/decisao/services/decisaoService.ts
      provides: "finalistas = decisao_final e não encerrada"
      contains: "candidaturaEncerrada"
  key_links:
    - from: "supabase/functions/_shared/comparativo-config.ts"
      to: "TriagemTable, ComparativoCandidatosPage, DecisaoFinalPage"
      via: "import relativo da constante"
      pattern: "COMPARATIVO_MAX_CANDIDATOS"
    - from: "src/lib/candidatura/proximaEtapa.ts (49-05)"
      to: "ComparativoCandidatosPage.handleAvancar"
      via: "proximaEtapaDeTrabalho(etapa_atual)"
      pattern: "proximaEtapaDeTrabalho\\("
    - from: "triagemService.invokeComparativo (49-13)"
      to: "DecisaoFinalPage → ComparativoScreen"
      via: "posicoes + provedor_ia + modelo_ia + fallback_cause"
      pattern: "posicoes"
  prohibitions:
    - requirement_id: JORN-25
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT oferecer seleção nem «Avançar» para candidatura encerrada, e MUST NOT tratar retirada a pedido como encerrada (D-34)"
    - requirement_id: JORN-25
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT cortar em silêncio a lista de finalistas acima do teto — a tela diz o limite"
    - requirement_id: JORN-25
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT introduzir ocorrência literal nova do endereço do canal de privacidade nem a expressão «teste psicológico» em texto novo (D-07, D-58 — a única exceção é o disclaimer negado da devolutiva, já decidido)"
---

<objective>
O comparativo passa a não oferecer candidatura encerrada, a usar o teto da constante, a levar o «Avançar» à próxima
etapa real — e, na decisão final, a comparar quem de fato está em decisão final, rotulado pela chave e com o selo.

Purpose: JORN-25 (D-34, D-36, D-36b, D-59 no front — «Avançar» grava sempre `avaliacao_assincrona`; a decisão final
compara quem tem linha em `decisao_final`, quase todos encerrados; `DecisaoFinalPage.tsx:66-78` nem resolve nomes) e
JORN-28 (D-27b na decisão final). Segunda de duas fatias do 49-13 original (revisão do checker, W1); parte do contrato
do service, do selo e da página que o 49-13 deixou publicados.

Output: a tabela de seleção, o «Avançar» da página do comparativo, o service e a página da decisão final — publicados.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-13-SUMMARY.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-05-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): a seleção sem encerrada e com o teto da constante, e o «Avançar» para a próxima etapa real — da tabela ao UPDATE de etapa</name>
  <precondition>O 49-13 está no ar (`proveniencia-ia-badge` presente em PROD — conferido no SUMMARY do 49-13) e `src/lib/candidatura/proximaEtapa.ts` exporta `proximaEtapaDeTrabalho` (49-05).</precondition>
  <files>src/features/triagem/components/TriagemTable.tsx, src/features/triagem/components/__tests__/TriagemTable.test.tsx, src/components/pages/VagaCandidatosRHPage.tsx, src/components/pages/ComparativoCandidatosPage.tsx, src/components/pages/__tests__/ComparativoCandidatosPage.test.tsx, src/features/triagem/components/ComparativoScreen.tsx, src/features/triagem/components/__tests__/ComparativoScreen.test.tsx</files>
  <read_first>
    - `src/features/triagem/components/TriagemTable.tsx` (`:45-60` `COMPARE_MAX`/`COMPARE_MIN`; `:205-275` a seleção com `checkboxDisabled` + Tooltip; `:425` a mensagem do teto)
    - `src/features/triagem/components/__tests__/TriagemTable.test.tsx` (o teste ~188 da retirada selecionável)
    - `src/components/pages/VagaCandidatosRHPage.tsx` (`:121` — o `state` da rota para o comparativo)
    - `src/components/pages/ComparativoCandidatosPage.tsx` e o teste criado pelo 49-13 (o estado deixado por ele)
    - `src/features/triagem/components/ComparativoScreen.tsx` (`:52-66` as props `onAvancar`/`onRejeitar`; `:312-350` a linha de ação)
    - `src/lib/candidatura/proximaEtapa.ts` (49-05) e `src/lib/candidatura/candidaturaEncerrada.ts`
    - `src/features/triagem/services/triagemService.ts` (`:374` `PROXIMA_ETAPA_APOS_TRIAGEM`; `updateCandidaturaEtapa`)
    - `supabase/functions/_shared/comparativo-config.ts` (49-08)
  </read_first>
  <behavior>
    - Linha `inscricao/rejeitado` (knockout): selo «Encerrada» (`data-testid="triagem-selo-encerrada"`), checkbox desabilitado, tooltip «Candidatura encerrada não entra no comparativo.».
    - Linha com `encerrada_a_pedido_em` e status em andamento (retirada): selecionável (regressão da Phase 45).
    - Com 4 selecionados, a 5ª linha fica desabilitada com o tooltip do teto montado da constante.
    - Comparativo: candidato em `triagem` ⇒ «Avançar» chama `updateCandidaturaEtapa(id, 'avaliacao_assincrona')`; em `entrevista_online` ⇒ `'entrevista_presencial'`; em `decisao_final` ⇒ sem botão «Avançar».
  </behavior>
  <action>
Passo 1 — D-50: re-rodar o padrão P3 da C1 do `49-VARREDURA-KICKOFF.md` (literais de teto no front) e registrar o delta em relação ao 49-13-SUMMARY.

Passo 2 — `TriagemTable.tsx`: `COMPARE_MAX`/`COMPARE_MIN` passam a ser as constantes importadas de `supabase/functions/_shared/comparativo-config.ts` (import relativo com comentário do precedente); as mensagens do teto são montadas delas; a linha encerrada (`candidaturaEncerrada(row.etapa_atual, row.status)`) ganha selo neutro «Encerrada» com `data-testid="triagem-selo-encerrada"` e checkbox desabilitado com tooltip próprio — o MESMO idioma de `checkboxDisabled` + Tooltip que já existe; retirada a pedido não muda. `VagaCandidatosRHPage.tsx`: o `state` da rota leva `etapa_atual` e `status` de cada selecionado (hoje só `id`/`nome`). `ComparativoCandidatosPage.tsx`: `SelectionItem` ganha `etapa_atual`/`status`; o efeito que dispara a EF usa as duas constantes; `handleAvancar(candidaturaId)` procura o item e chama `updateCandidaturaEtapa(id, proximaEtapaDeTrabalho(item.etapa_atual))`; o `ComparativoScreen` ganha uma prop opcional (ex.: `podeAvancar(candidaturaId)`) e só mostra «Avançar» para quem tem próxima etapa de trabalho e não é encerrado (a EF já recusa encerrada; aqui é a segunda camada) — sem a prop, o comportamento de hoje. O import de `PROXIMA_ETAPA_APOS_TRIAGEM` sai da página (a constante fica em `triagemService` se outro chamador a usar — conferir por `grep`). Testes do `<behavior>` em `TriagemTable.test.tsx`, `ComparativoCandidatosPage.test.tsx` e `ComparativoScreen.test.tsx` (atualizar DE PROPÓSITO os que asseriam o teto 10, com comentário; o ~188 e o `:121` da Phase 45 seguem verdes). Rodar `npx vitest run src/features/triagem src/components/pages src/lib` e o portão `tsc`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/features/triagem src/components/pages src/lib</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const files=['src/features/triagem/components/TriagemTable.tsx','src/components/pages/ComparativoCandidatosPage.tsx'];for(const f of files){const code=fs.readFileSync(f,'utf8').split('\n').filter(l=>!/^\s*(\/\/|\*)/.test(l)).join('\n');if(/Máximo de 10|2 a 10|<=\s*10\b|>\s*10\b/.test(code))throw new Error(f+': literal de teto antigo');if(!/COMPARATIVO_MAX_CANDIDATOS/.test(code))throw new Error(f+': sem a constante do teto')}const p=fs.readFileSync('src/components/pages/ComparativoCandidatosPage.tsx','utf8');if(!/proximaEtapaDeTrabalho\(/.test(p))throw new Error('Avancar sem a proxima etapa real');if(!/triagem-selo-encerrada/.test(fs.readFileSync('src/features/triagem/components/TriagemTable.tsx','utf8')))throw new Error('TriagemTable sem o selo de encerrada');console.log('OK')" && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; }</automated>
    <fails_when>non-zero exit with "literal de teto antigo", "sem a constante do teto", "Avancar sem a proxima etapa real" or "sem o selo de encerrada"; lint exit code other than 0/2 or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - Encerrada com selo e sem seleção; retirada selecionável (teste da Phase 45 verde).
    - Nenhum literal de teto antigo nos dois arquivos; ambos usam a constante.
    - «Avançar» leva à próxima etapa real de cada um e some para quem não tem (teste); `tsc` ≤ 90.
  </acceptance_criteria>
  <done>Só entra no comparativo quem ainda está no funil, e avançar leva cada um para a etapa que vem depois da sua.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: a decisão final compara quem está em decisão final (D-36b), rotula pela chave — e a publicação</name>
  <files>src/features/decisao/services/decisaoService.ts, src/features/decisao/services/__tests__/decisaoService.test.ts, src/features/decisao/components/DecisaoFinalPage.tsx, src/features/decisao/components/__tests__/DecisaoFinalPage.test.tsx</files>
  <read_first>
    - `src/features/decisao/services/decisaoService.ts` (`:206-240` `listFinalistas`, `DecisaoServiceError`, o tipo `Finalista`)
    - `src/features/decisao/services/__tests__/decisaoService.test.ts`
    - `src/features/decisao/components/DecisaoFinalPage.tsx` (inteiro — `resolveFinalistCandidates :66-80`, `:115-130`, `:195-205` os literais de teto)
    - `src/features/decisao/components/__tests__/RegistrarDecisaoForm.test.tsx` (o molde de teste de componente da feature)
    - `src/features/triagem/components/ProvenienciaIABadge.tsx` e `resolveCandidates` de `ComparativoCandidatosPage.tsx` (49-13 — a regra do rótulo por `posicoes`)
  </read_first>
  <behavior>
    - `listFinalistas(vaga)`: devolve só candidaturas da vaga com `etapa_atual='decisao_final'` e não encerradas (fixture com `decisao_final/em_analise`, `decisao_final/finalizado` e `rejeitado/rejeitado` ⇒ só a primeira); a projeção é `id, etapa_atual, status` (sem curinga).
    - Página com 1 finalista ⇒ estado vazio verdadeiro («é preciso ao menos 2 candidaturas em decisão final para comparar»), sem chamada da EF.
    - Página com 5 finalistas ⇒ mensagem do teto (até 4, use o comparativo da vaga), sem chamada da EF.
    - Página com 3 finalistas ⇒ chama a EF e rotula pelo `posicoes`; em fallback, o selo aparece.
  </behavior>
  <action>
`listFinalistas`: `from('candidaturas').select('id, etapa_atual, status').eq('vaga_id', vagaId).eq('etapa_atual', 'decisao_final')` e filtrar com `candidaturaEncerrada`; manter `DecisaoServiceError`/`DATABASE_ERROR`; ajustar o tipo `Finalista` e os chamadores (conferir por `grep` quem lê `decisao` do finalista). `DecisaoFinalPage.tsx`: `resolveFinalistCandidates` por `posicoes` (mesma regra do 49-13); teto e mínimo pela constante; estados «menos de 2» e «acima do teto» com a cópia do `<behavior>`; o selo de proveniência no ranking; o texto «em decisão final» passa a ser verdade (D-36b). Testes do `<behavior>` em `decisaoService.test.ts` e no `DecisaoFinalPage.test.tsx` novo (molde `RegistrarDecisaoForm.test.tsx`, service e hook mockados). Rodar `npx vitest run src/features/decisao src/features/triagem src/components/pages src/__tests__/guards` e o portão `tsc`.

Publicar (D-52): `npm run build`; conferir `triagem-selo-encerrada` em `build/assets/` e anotar o chunk (as rotas `/rh/*` são lazy); commitar; `git push origin main`; `origin/main..HEAD` vazio; marcador no ar pelo crawler.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/features/decisao src/features/triagem src/components/pages src/__tests__/guards && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; }</automated>
    <fails_when>non-zero exit, "failed" in the vitest summary, lint exit code other than 0/2 or more than 90 "error TS" lines</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const code=fs.readFileSync('src/features/decisao/components/DecisaoFinalPage.tsx','utf8').split('\n').filter(l=>!/^\s*(\/\/|\*)/.test(l)).join('\n');if(!/COMPARATIVO_MAX_CANDIDATOS/.test(code))throw new Error('DecisaoFinalPage sem a constante do teto');if(!/posicoes/.test(code))throw new Error('DecisaoFinalPage nao rotula por posicoes');const s=fs.readFileSync('src/features/decisao/services/decisaoService.ts','utf8');if(!/candidaturaEncerrada\(/.test(s))throw new Error('listFinalistas sem o predicado canonico');console.log('OK')" && npm run build >/dev/null && grep -rl "triagem-selo-encerrada" build/assets/</automated>
    <fails_when>non-zero exit with "sem a constante do teto", "nao rotula por posicoes" or "sem o predicado canonico"; build failed; or no file in build/assets/ contains the marker</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && node -e "const M=process.argv[1],B='https://rh.beautysmile.com.br';(async()=>{const vis=new Set(),fila=['/'];let ok=false;while(fila.length&&vis.size<300){const p=fila.shift();if(vis.has(p))continue;vis.add(p);let t='';try{t=await (await fetch(B+p)).text()}catch{continue}if(t.includes(M)){ok=true;break}for(const m of t.matchAll(/(?:\/assets\/|\.\/)([\w.-]+\.js)/g))fila.push('/assets/'+m[1])}if(!ok){console.error('AUSENTE em PROD: '+M);process.exit(1)}console.log('PRESENTE em PROD: '+M)})()" "triagem-selo-encerrada"</automated>
    <fails_when>non-zero exit: unpushed commits remain, or "AUSENTE em PROD: triagem-selo-encerrada"</fails_when>
  </verify>
  <acceptance_criteria>
    - `listFinalistas` lê `candidaturas` em `decisao_final` e filtra encerradas (teste); a página tem os estados «menos de 2» e «acima do teto» (teste), rotula por `posicoes` e mostra o selo.
    - `vitest` das pastas tocadas e o guard verdes; `tsc` ≤ 90; `triagem-selo-encerrada` no build e no ar; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>A tela de decisão final passa a comparar as candidaturas que de fato aguardam decisão — e diz a verdade quando não há o que comparar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do RH → EF `comparativo-candidatos` | a tela só deixa de oferecer; a EF recusa (49-08) |
| navegador do RH → PostgREST `candidaturas` (UPDATE de etapa pelo «Avançar») | a trava do banco (49-06) é a última camada |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-22-01 | Tampering | «Avançar» de encerrada pelo comparativo | medium | mitigate | seleção bloqueada + botão só com próxima etapa; trava do banco (49-06) |
| T-49-22-02 | Tampering | «Avançar» gravando etapa fixa (regressão ou salto) | medium | mitigate | `proximaEtapaDeTrabalho(etapa_atual)` por candidato; teste por etapa |
| T-49-22-03 | Repudiation | decisão final comparando encerrados ou cortando finalistas em silêncio | medium | mitigate | `listFinalistas` pelo predicado; estados «menos de 2» e «acima do teto» |
| T-49-22-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `vitest` de `src/features/triagem`, `src/features/decisao`, `src/components/pages`, `src/lib`, `src/__tests__/guards` verde; `tsc` ≤ 90.
- `triagem-selo-encerrada` no build e no ar; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
O comparativo só inclui quem ainda pode avançar, avança cada um para a etapa certa, e a decisão final compara quem de
fato aguarda decisão.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| selo «Encerrada» na seleção (`data-testid="triagem-selo-encerrada"`) | UI | **novo** |
| `ComparativoScreen` prop opcional de elegibilidade do «Avançar» | componente | editado |
| `listFinalistas` sobre `candidaturas` (`decisao_final` e não encerrada) | service | editado |
| `resolveFinalistCandidates` por `posicoes` | helper de página | editado |
| `src/features/decisao/components/__tests__/DecisaoFinalPage.test.tsx` | teste | **novo** |

## Premissas sinalizadas (probe de borda)

JORN-25 e JORN-28 saíram `unclassified` no probe e ficam `unresolved` no relatório. Fatia do 49-13 original (revisão
do checker, W1). Onda 5 em vez de 4 por acoplamento de pasta de teste com 49-15/49-16 (comentário no `depends_on`),
não por dado consumido. O teste de página da decisão final não existia (`src/features/decisao/components/__tests__`
tinha só `ConsolidacaoDashboard` e `RegistrarDecisaoForm`) — o `<behavior>` da página o exige, por isso ele entra aqui.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-22-SUMMARY.md` when done
</output>
