---
phase: 49-consertos-da-jornada-bloco-2
plan: 10
type: execute
wave: 3
depends_on: ["49-01", "49-02", "49-06"]
files_modified:
  - supabase/migrations/20260922000007_p49_analise_entrevista_vigente.sql
  - supabase/migrations/20260922000008_p49_revisao_entrevista_vigente.sql
  - supabase/tests/p49_analise_vigente_smoke.sql
  - supabase/functions/_shared/entrevista-schemas.ts
  - supabase/functions/avaliar-transcricao-entrevista/index.ts
  - supabase/functions/avaliar-transcricao-entrevista/__tests__/index.test.ts
autonomous: true
requirements: [JORN-12, JORN-28]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Cada análise de entrevista nova sabe de qual entrevista é (`tipo` online/presencial — o que o RH escolheu, com a etapa atual como padrão, D-41), quem a pediu (`solicitado_por` = o RH do JWT), qual texto a gerou (`texto_hash` = `ai_call_logs.input_hash` via `inputHashDe` do 49-02, e `ai_call_log_id` = a linha do log que contém o texto mascarado, D-38) e qual modelo a produziu (`provedor_ia`/`modelo_ia` reais, D-28). NENHUMA coluna de conteúdo nova (D-38)."
    - "D-39: gravar é UMA transação numa RPC nova `public.registrar_analise_entrevista(...)` SECURITY DEFINER, só `service_role`: marca `superada_em = now()` nas vigentes do mesmo `(candidatura_id, tipo)`, insere a nova e faz o upsert de `scores_candidato` `tipo='entrevista'` com `status='pendente_humano'` e `score` NULL (D-42, D-65: uma linha só para online e presencial). Um `pg_advisory_xact_lock` por `(candidatura, tipo)` impede duas vigentes por corrida."
    - "Análise que falhou (parse nulo, injeção) entra com `status_analise='falhou'`, NUNCA é vigente (pelo predicado único `entrevista_analise_vigente` do 49-01) e não supera ninguém — a análise boa anterior continua vigente."
    - "D-40 (com o caso do CONTEXT §specifics): reenviar um texto já analisado com sucesso na mesma `(candidatura, tipo)` NÃO cria linha nem chama a IA — devolve a análise existente (`reaproveitada: true`, e se ela é a vigente); A, B, A ⇒ 2 análises, a vigente é a B. A EF confere o hash ANTES da chamada; a RPC confere de novo (corrida). `cache_hit` do `CallAiResult` não é usado para isso (RESEARCH Correção 8)."
    - "D-42: a análise superada conserva `scores_humanos`, `notas_humanas`, `revisada_por` e `revisao_confirmada_em` — a revisão anterior continua visível (a tela é do 49-16); a nota consolidada volta a aguardar revisão."
    - "D-39 na revisão humana: `salvar_avaliacao_entrevista` passa a gravar na análise VIGENTE mais recente (não mais na mais recente de qualquer estado) e `confirmar_revisao_entrevista(p_analise_id)` recusa análise superada ou falha com mensagem própria; as duas trocam o guard de papel por fail-closed (`coalesce(v_role,'') NOT IN …`, SQLSTATE `insufficient_privilege` mantido). As assinaturas NÃO mudam."
    - "Todo INSERT/RPC da EF de transcrição checa erro — escrita falha devolve 500, nunca `{ ok: true }` (hoje `:266,299,309`, varredura C6 #5)."
    - "A rubrica BARS da análise vem só do guia DO TIPO da análise (hoje lê todos os guias da candidatura, `index.ts:198-203`)."
    - "O body `.strict()` da EF aceita `tipo` OPCIONAL (`z.enum(['online','presencial'])`) — a EF sai ANTES do front que manda o campo (Pitfall 4, D-55; o front é do 49-16); sem `tipo` e fora de etapa de entrevista ⇒ 400 com mensagem que pede o tipo."
    - "D-37: só a análise da ENTREVISTA ganha o append por etapa nesta fase; `analise_candidato_vaga` segue sobrescrita ao reprocessar (§Deferred)."
    - "O `p49_analise_vigente_smoke.sql` prova em PROD (fixtures revertidas): A/B/A, falha nunca vigente, revisão preservada na superada, nota de volta a pendente, tipos independentes, revisão só na vigente — e o portão morde por mutação (D-56)."
    - "Deploy da EF de transcrição por `efdeploy.cjs` com `--dry-run` e `verify_jwt=true`, depois da migration do 49-01 e da deste plano conferidas no catálogo (Pitfall 8). A proveniência do guia (`gerar-guia-entrevista`, D-28) é do plano 49-24."
  artifacts:
    - path: supabase/migrations/20260922000007_p49_analise_entrevista_vigente.sql
      provides: "RPC de gravação com superação + revisão só na vigente"
      contains: "registrar_analise_entrevista"
    - path: supabase/tests/p49_analise_vigente_smoke.sql
      provides: "prova da vigente, do D-40, do D-42 e da revisão"
      contains: "smoke49v.pass"
    - path: supabase/functions/avaliar-transcricao-entrevista/index.ts
      provides: "tipo, hash, vínculo, proveniência, D-40, erro checado"
      contains: "registrar_analise_entrevista"
  key_links:
    - from: "avaliar-transcricao-entrevista (EF, service_role)"
      to: "public.registrar_analise_entrevista"
      via: "supabaseAdmin.rpc com erro checado"
      pattern: "rpc\\(\"registrar_analise_entrevista\""
    - from: "registrar_analise_entrevista / salvar_avaliacao_entrevista / confirmar_revisao_entrevista"
      to: "public.entrevista_analise_vigente (49-01)"
      via: "o mesmo predicado de vigente"
      pattern: "entrevista_analise_vigente\\("
    - from: "entrevista_analises.texto_hash"
      to: "ai_call_logs.input_hash"
      via: "inputHashDe(transcricao) — o mesmo cálculo do audit-logger"
      pattern: "inputHashDe\\("
  prohibitions:
    - requirement_id: JORN-12
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT criar coluna com o texto da transcrição — o texto mascarado já mora em `ai_call_logs` (só admin, 180 d); a análise guarda hash e vínculo (D-38)"
    - requirement_id: JORN-12
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT apagar análise nem revisão anterior — superar é marcar (D-02, D4)"
    - requirement_id: JORN-12
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT fazer uma análise nova de IA pesar antes da revisão humana (D-42, RNF-07a)"
---

<objective>
A análise de entrevista passa a ter dono: tipo, autor, texto (por hash e vínculo), modelo e estado de vigente
— e o portão de avanço e a revisão humana passam a olhar a mesma análise.

Purpose: JORN-12/D-37..D-42. Medido: 6 análises em 3 candidaturas, sem tipo, sem autor, sem vínculo com o
texto (que já está no `ai_call_logs`); os leitores discordam (tela e revisão pegam a mais nova, o portão olhava
todas); uma falha grava linha que vira «a mais nova»; reanálise após revisão zera a nota e a revisão some da
tela; 2 das 4 análises de `bf26ee3c` são replays do mesmo texto (RESEARCH Correção 9). O portão já olha só a
vigente desde o 49-06.

Output: a RPC de gravação com superação, as duas RPCs de revisão pela vigente, a EF de transcrição consertada, o
smoke — tudo deployado. A marcação retroativa das 6 é do 49-12 (checkpoint); a tela é do 49-16; a proveniência do guia
saiu para o 49-24 (onda 2).

Orçamento (revisão do checker, W1): aparado de 130k para ~115k tirando a Task 3 (guia → 49-24) e NÃO dividido além
disso — as Tasks 1–2 são a família de RPCs que o `p49_analise_vigente_smoke` prova junta (A/B/A exige
`registrar_analise_entrevista` e `salvar_avaliacao_entrevista`/`confirmar_revisao_entrevista` na mesma fixture), e
separá-las empurraria 49-12 e 49-16 uma onda adiante e poria o 49-16 em paralelo com o 49-22 na pasta de testes da
triagem; o excesso restante (1,15×) é sobre estimativa sem calibração (sample_count 0).
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-02-SUMMARY.md
</context>

<interfaces>
- `public.registrar_analise_entrevista(p_candidatura_id uuid, p_tipo text, p_solicitado_por uuid, p_texto_hash text, p_ai_call_log_id uuid, p_provedor_ia text, p_modelo_ia text, p_prompt_version text, p_status_analise text, p_competencias jsonb, p_citacoes jsonb, p_bias_flags jsonb, p_bloqueio_avanco boolean, p_score_metadata jsonb) RETURNS jsonb` → `{ ok, analise_id, reaproveitada, vigente, superadas }`. `p_status_analise` ∈ {`pendente_humano`, `falhou`}. `REVOKE … FROM PUBLIC, anon, authenticated; GRANT EXECUTE … TO service_role`.
- Resposta da EF: `{ ok: true, analise_id, tipo, reaproveitada, vigente, falhou }` (payload neutro, sem texto nem nota).
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer) [BLOCKING]: uma análise nova de ponta a ponta — RPC com superação aplicada, EF com tipo/hash/vínculo/modelo, erro checado, deploy</name>
  <precondition>O 49-01 está em PROD (as 7 colunas novas de `entrevista_analises` e `entrevista_analise_vigente` existem) e o 49-02 está no disco (`inputHashDe` e `CallAiResult.log_id`/`model` em `_shared`).</precondition>
  <files>supabase/migrations/20260922000007_p49_analise_entrevista_vigente.sql, supabase/functions/_shared/entrevista-schemas.ts, supabase/functions/avaliar-transcricao-entrevista/index.ts, supabase/functions/avaliar-transcricao-entrevista/__tests__/index.test.ts</files>
  <read_first>
    - `supabase/functions/avaliar-transcricao-entrevista/index.ts` (inteiro; `:170-330`)
    - `supabase/functions/_shared/entrevista-schemas.ts` (`:52-66` — `AvaliarTranscricaoBodySchema` `.strict()`) e `src/features/entrevista/__tests__/entrevista-contract.test.ts` (importa o mesmo schema)
    - `supabase/migrations/20260906000002_avaliacao_entrevista_grava_score.sql` (`:21-125` — forma de RPC DEFINER com readback jsonb, o upsert `ON CONFLICT (candidatura_id, tipo, subtipo, pergunta_id)` e o portão de auto-verificação `:120-125`)
    - `supabase/migrations/20260921000004_p48_analise_descartada_knockout.sql` (o «marcar, não apagar» do D-02)
    - `supabase/functions/analise-candidato-individual/index.ts` (`:565-585` — escrita de EF com erro checado)
    - `supabase/functions/avaliar-transcricao-entrevista/_local/bars-rubric.ts` (o bloco a partir dos guias)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md` §I (I.1–I.3) e Correções 8, 9, 10
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md` §1 (ACL de RPC só da EF), §7, §16d
  </read_first>
  <behavior>
    - EF com `tipo='online'` e texto novo ⇒ `callAi` chamado uma vez; RPC chamada com `p_tipo='online'`, `p_solicitado_por` = o `user.id`, `p_texto_hash` = `inputHashDe(transcricao)`, `p_ai_call_log_id` = `result.log_id`, `p_provedor_ia`/`p_modelo_ia` = os do resultado, `p_status_analise='pendente_humano'`.
    - EF sem `tipo`, candidatura em `entrevista_presencial` ⇒ `p_tipo='presencial'`; sem `tipo`, candidatura em `triagem` ⇒ 400 pedindo o tipo, sem chamada de IA.
    - EF com texto cujo hash já tem análise de sucesso na mesma `(candidatura, tipo)` ⇒ 0 chamadas de IA, 0 RPCs de gravação, resposta `reaproveitada: true` com o id existente.
    - parse nulo ⇒ RPC com `p_status_analise='falhou'`; resposta `falhou: true`.
    - RPC devolvendo erro ⇒ 500 (não `{ ok: true }`).
    - Os guias lidos são filtrados pelo `tipo`.
    - Body com campo extra continua recusado (`.strict()`).
  </behavior>
  <action>
Passo 1 — D-50/D-56 e medição, só leitura: varredura de portões do `CLAUDE.md` (classificar os achados que citem `entrevista_analises`, `salvar_avaliacao_entrevista`, `confirmar_revisao_entrevista` ou listas de funções por `proname IN (…)` que a RPC nova deveria integrar); re-rodar a C6 (`grep` de escrita sem erro) e a C7 #5 (guias sem filtro de tipo) do `49-VARREDURA-KICKOFF.md`; contar `entrevista_analises` por candidatura e registrar (6 em 3 na pesquisa — premissa A5); `pg_get_functiondef` + `md5` de `salvar_avaliacao_entrevista` e `confirmar_revisao_entrevista` (base da Task 2) guardados no scratchpad.

Passo 2 — migration `supabase/migrations/20260922000007_p49_analise_entrevista_vigente.sql` (cabeçalho do M8, transporte, sem `BEGIN;`), SÓ com a RPC nova `public.registrar_analise_entrevista` do `<interfaces>` — esta task a escreve e aplica; as duas RPCs de revisão vão numa migration própria na Task 2 (`20260922000008`). Corpo da RPC: guard `p_status_analise IN ('pendente_humano','falhou')` e `p_tipo IS NULL OR IN ('online','presencial')`; `PERFORM pg_advisory_xact_lock(hashtext('p49_analise:' || p_candidatura_id::text || ':' || coalesce(p_tipo,'')))`; se `p_status_analise <> 'falhou'` e já existe análise do mesmo `(candidatura_id, tipo)` com o mesmo `texto_hash`, `status_analise IS DISTINCT FROM 'falhou'` e `competencias IS NOT NULL` ⇒ devolver `{ ok, analise_id: <existente>, reaproveitada: true, vigente: <entrevista_analise_vigente(...) da existente>, superadas: 0 }` sem escrever; senão, se não é falha, `UPDATE … SET superada_em = now() WHERE candidatura_id = p AND tipo IS NOT DISTINCT FROM p_tipo AND public.entrevista_analise_vigente(superada_em, status_analise, competencias)` (`GET DIAGNOSTICS` para `superadas`); `INSERT` da nova com todos os campos (`status_analise` = o parâmetro); se não é falha, upsert de `scores_candidato` (`tipo='entrevista'`, `subtipo` NULL, `score` NULL, `score_max` NULL, `status='pendente_humano'`, `metadata = p_score_metadata`) `ON CONFLICT (candidatura_id, tipo, subtipo, pergunta_id) DO UPDATE` (D-42/D-65); readback `{ ok, analise_id, reaproveitada: false, vigente, superadas }`. `SECURITY DEFINER`, `SET search_path = ''`; ACL `REVOKE ALL … FROM PUBLIC, anon, authenticated` + `GRANT EXECUTE … TO service_role`; `COMMENT ON FUNCTION`; `DO` de auto-verificação (ACL: `has_function_privilege('authenticated', …)` = false; o corpo contém `entrevista_analise_vigente(`, `pg_advisory_xact_lock`, `superada_em = now()`). Aplicar com `node p46apply.cjs migrate`.

Passo 3 — `_shared/entrevista-schemas.ts`: `AvaliarTranscricaoBodySchema` ganha `tipo: z.enum(["online","presencial"]).optional()` (continua `.strict()`; comentário D-41 e a ordem EF → front).

Passo 4 — EF: resolver `tipo` (`body.tipo` ?? da etapa `entrevista_online`/`entrevista_presencial`; fora delas ⇒ 400 `VALIDATION` «Informe se a transcrição é da entrevista online ou presencial.»); ler `etapa_atual` na mesma allowlist de `candidaturas` que a EF já lê; `texto_hash = await inputHashDe(body.transcricao)`; D-40: ler (`supabaseAdmin`, allowlist `id, superada_em, status_analise, competencias`) a análise de sucesso da mesma `(candidatura, tipo, texto_hash)` e, se houver, responder `reaproveitada: true` sem `callAi`; guias com `.eq("tipo", tipo)`; `callAi` como hoje; montar os campos (competências/citações/bias como hoje) e chamar `supabaseAdmin.rpc("registrar_analise_entrevista", …)` com `p_status_analise = parse nulo ou injeção ? 'falhou' : 'pendente_humano'`, destruturando `{ data, error }` e lançando se houver (o `catch` externo devolve 500). Os dois INSERTs diretos e o upsert direto de hoje SAEM (a RPC é o único escritor). Log redigido só com ids/contagens/`provider`. Criar `__tests__/index.test.ts` (não existe teste do handler hoje; molde `comparativo-candidatos/__tests__/index.test.ts`: `handler(req, deps)` com mocks sem rede) com o `<behavior>`. Rodar `deno test --allow-all supabase/functions/avaliar-transcricao-entrevista/` e o contrato do front `npx vitest run src/features/entrevista/__tests__/entrevista-contract.test.ts`.

Passo 5 — deploy: conferir a RPC no catálogo; `node efdeploy.cjs avaliar-transcricao-entrevista --dry-run` (fechamento com `_shared/entrevista-schemas.ts`, `ai-client.ts`, `audit-logger.ts`, `ai-error-codes.ts`; registrar), `node efdeploy.cjs avaliar-transcricao-entrevista` (`verify_jwt=true`), marcador `registrar_analise_entrevista` no bundle.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260922000007_p49_analise_entrevista_vigente.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');for(const t of ['CREATE OR REPLACE FUNCTION public.registrar_analise_entrevista','SECURITY DEFINER',\"SET search_path = ''\",'pg_advisory_xact_lock','entrevista_analise_vigente(','superada_em = now()','ON CONFLICT (candidatura_id, tipo, subtipo, pergunta_id)','GRANT EXECUTE','service_role','authenticated'])if(!c.includes(t))throw new Error('falta: '+t);if(/DELETE\s+FROM\s+public\.entrevista_analises/i.test(c))throw new Error('superar e marcar, nao apagar (D-02)');console.log('OK')" && node p46apply.cjs migrate supabase/migrations/20260922000007_p49_analise_entrevista_vigente.sql</automated>
    <fails_when>non-zero exit with "falta:", "wrapper BEGIN proibido" or "superar e marcar" in stderr; or the migrate output lacks "md5 do ledger BATE"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs sql "SET TRANSACTION READ ONLY; select has_function_privilege('authenticated', p.oid, 'EXECUTE') as auth, has_function_privilege('service_role', p.oid, 'EXECUTE') as svc from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public' and p.proname='registrar_analise_entrevista'" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);if(r.length!==1||r[0].auth!==false||r[0].svc!==true){console.error('ACL da RPC: '+JSON.stringify(r));process.exit(1)}console.log('RPC so para service_role')})"</automated>
    <fails_when>non-zero exit with "ACL da RPC:" (function missing, executable by authenticated, or not by service_role)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/avaliar-transcricao-entrevista/ && npx vitest run src/features/entrevista/__tests__/entrevista-contract.test.ts</automated>
    <fails_when>non-zero exit, "FAILED" in the Deno output, or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs avaliar-transcricao-entrevista --dry-run | tee /dev/stderr | grep -q "functions/_shared/entrevista-schemas.ts" && node efdeploy.cjs avaliar-transcricao-entrevista | tee /dev/stderr | grep -q "efdeploy: OK .*verify_jwt=true" && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/avaliar-transcricao-entrevista/body" | grep -a -c "registrar_analise_entrevista"</automated>
    <fails_when>non-zero exit; closure lacks functions/_shared/entrevista-schemas.ts; efdeploy output lacks "efdeploy: OK" with "verify_jwt=true"; or the grep count printed is 0</fails_when>
  </verify>
  <acceptance_criteria>
    - A RPC existe em PROD, executável só por `service_role`, com lock, superação por marca e upsert pendente.
    - A EF grava só pela RPC, com tipo/hash/vínculo/autor/modelo, checa erro, faz o D-40 antes da IA e filtra o guia pelo tipo (testes do `<behavior>`).
    - O schema do body aceita `tipo` opcional e continua `.strict()`; o contrato do front segue verde.
    - EF deployada com `verify_jwt=true`; marcador no bundle.
  </acceptance_criteria>
  <done>A próxima transcrição analisada entra como a vigente da sua entrevista, com o autor e o elo para o texto — e colar o mesmo texto de novo não duplica nada.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: a revisão humana só na vigente — e o smoke que prova A/B/A, falha, revisão preservada e tipos independentes</name>
  <files>supabase/migrations/20260922000008_p49_revisao_entrevista_vigente.sql, supabase/tests/p49_analise_vigente_smoke.sql</files>
  <read_first>
    - os corpos vivos de `salvar_avaliacao_entrevista` e `confirmar_revisao_entrevista` guardados na Task 1 (e o md5 de cada)
    - `supabase/migrations/20260906000002_avaliacao_entrevista_grava_score.sql` (`:21-125`, o portão `:120-125`)
    - `supabase/migrations/20260625000001_phase14_gap_closure.sql` (`:208-270` — `confirmar_revisao_entrevista`)
    - `src/features/entrevista/services/entrevistaService.ts` (como o front chama as duas RPCs e mapeia `insufficient_privilege`/`no_data_found`)
    - `supabase/tests/p48_reabertura_smoke.sql` (molde de smoke que escreve e reverte)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md` §1 (guard fail-closed), §7, §10
  </read_first>
  <action>
Escrever `supabase/migrations/20260922000008_p49_revisao_entrevista_vigente.sql` (cabeçalho, transporte, sem `BEGIN;`): pré-portão dos dois md5; `CREATE OR REPLACE` de `salvar_avaliacao_entrevista(uuid, jsonb, text)` = corpo vivo com DUAS trocas — a escolha da análise passa a `… WHERE ea.candidatura_id = p AND public.entrevista_analise_vigente(ea.superada_em, ea.status_analise, ea.competencias) ORDER BY ea.created_at DESC LIMIT 1` (sem vigente ⇒ `no_data_found` «nenhuma análise vigente para revisar»), e o guard de papel vira `IF coalesce(v_role, '') NOT IN ('rh','administrador') THEN RAISE EXCEPTION 'forbidden' USING ERRCODE = 'insufficient_privilege'` —; a média, o UPDATE e o upsert em `scores_candidato` ficam byte a byte (o portão `:120-125` é preservado); `CREATE OR REPLACE` de `confirmar_revisao_entrevista(uuid)` = corpo vivo + ler `superada_em, status_analise, competencias` da análise e recusar com `check_violation` «análise superada ou sem resultado: confirme a revisão da análise vigente» quando não for vigente + o mesmo guard fail-closed; ACL das duas: grants vivos reafirmados + `REVOKE … FROM anon`; pós-portão (presença do predicado e do guard fail-closed, ausência de `IF v_role NOT IN`, `ON CONFLICT` preservado); `RAISE NOTICE` com os md5 novos. As assinaturas NÃO mudam (o front não precisa mudar para chamar). Ensaio (migration + smoke + `DO` `ENSAIO_OK`, requisição que aborta) e depois `migrate`.

Criar `supabase/tests/p49_analise_vigente_smoke.sql` (molde do `p48_reabertura_smoke.sql`: contador `smoke49v.pass`, SQLSTATE `P49V1`, fixture sintética `@invalido.local` em `entrevista_online`, atores RH/admin vivos, julgamento fora): (a) RPC com texto A (tipo `online`) ⇒ 1 vigente; (b) texto B ⇒ A superada (`superada_em` preenchida), B vigente, `scores_candidato` entrevista `pendente_humano` com `score` NULL; (c) texto A de novo ⇒ `reaproveitada: true`, `count(*)` da candidatura continua 2, vigente continua B; (d) falha (`p_status_analise='falhou'`) ⇒ linha nova, não vigente, vigente continua B; (e) `salvar_avaliacao_entrevista` com claims de RH/admin ⇒ grava em B; `confirmar_revisao_entrevista(A)` ⇒ `check_violation`; `confirmar_revisao_entrevista(B)` ⇒ ok; (f) texto D ⇒ B superada CONSERVA `scores_humanos`, `revisada_por`, `revisao_confirmada_em`; nota de volta a `pendente_humano` com `score` NULL; (g) tipo `presencial` ⇒ não supera a vigente online (uma vigente por tipo); (h) sem claims ⇒ `salvar_avaliacao_entrevista` recusa com `insufficient_privilege` (fail-closed); (z) resíduo zero. Gate com o total. Rodar em PROD.

Mutação (D-56), em requisição que aborta: redefinir `registrar_analise_entrevista` SEM o UPDATE de superação + o smoke ⇒ esperado `FAIL (b)`; redefinir `salvar_avaliacao_entrevista` com o corpo ANTERIOR ⇒ esperado `FAIL (e)` (grava na falha/na mais nova). Registrar as saídas no SUMMARY. Commitar e `git push origin main`; `origin/main..HEAD` vazio (o front ainda não manda `tipo` — o campo é opcional, D-55; a tela é do 49-16).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260922000008_p49_revisao_entrevista_vigente.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');for(const t of ['CREATE OR REPLACE FUNCTION public.salvar_avaliacao_entrevista','CREATE OR REPLACE FUNCTION public.confirmar_revisao_entrevista','entrevista_analise_vigente(',\"coalesce(v_role, '') NOT IN\",'ON CONFLICT (candidatura_id, tipo, subtipo, pergunta_id)','md5'])if(!c.includes(t))throw new Error('falta: '+t);if(/IF\s+v_role\s+NOT\s+IN/i.test(c))throw new Error('guard fail-open continua');console.log('OK')"</automated>
    <fails_when>non-zero exit with "falta:" or "guard fail-open continua" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && D=$(mktemp -d) && { cat supabase/migrations/20260922000008_p49_revisao_entrevista_vigente.sql supabase/tests/p49_analise_vigente_smoke.sql; printf '\nDO $e$ BEGIN RAISE EXCEPTION %s; END $e$;\n' "'ENSAIO_OK'"; } > "$D/ensaio.sql" && node p46apply.cjs run "$D/ensaio.sql" 2>&1 | tee "$D/out.txt" | tail -3; grep -q "ENSAIO_OK" "$D/out.txt" && ! grep -q "FAIL" "$D/out.txt"</automated>
    <fails_when>the output lacks "ENSAIO_OK" or contains "FAIL"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs migrate supabase/migrations/20260922000008_p49_revisao_entrevista_vigente.sql && node p46apply.cjs run supabase/tests/p49_analise_vigente_smoke.sql && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>non-zero exit; "md5 do ledger BATE" absent from the migrate output; "FAIL" in the smoke output; or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - Em PROD, as duas RPCs de revisão usam `entrevista_analise_vigente(` e o guard fail-closed; assinaturas iguais às de antes.
    - `p49_analise_vigente_smoke` passa (a)–(h) e (z); as duas mutações reprovam (saídas no SUMMARY).
    - O ensaio abortou com `ENSAIO_OK` antes do apply; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>Revisar e avançar passam a olhar a mesma análise — a vigente — e a revisão feita antes de uma análise nova continua lá para quem quiser ler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do RH (JWT) → EF de transcrição | o texto é UNTRUSTED; `tipo` é validado por enum |
| EF (service_role) → `registrar_analise_entrevista` | único escritor da análise; `authenticated` não executa |
| RH (JWT) → `salvar_avaliacao_entrevista` / `confirmar_revisao_entrevista` | revisão humana, guard de papel e posse |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-10-01 | Tampering | RH gravando análise direto pela RPC nova | high | mitigate | `REVOKE … FROM authenticated`; ACL conferida no verify |
| T-49-10-02 | Repudiation | análise sem autor/texto/modelo | medium | mitigate | `solicitado_por` do JWT, `texto_hash`, `ai_call_log_id`, `provedor_ia`/`modelo_ia` |
| T-49-10-03 | Tampering | duas vigentes por corrida | medium | mitigate | `pg_advisory_xact_lock` por `(candidatura, tipo)` na RPC |
| T-49-10-04 | Information Disclosure | coluna nova com a transcrição | high | mitigate | nenhuma coluna de conteúdo; hash + vínculo (D-38) |
| T-49-10-05 | Elevation of Privilege | guard de papel fail-open com `v_role` nulo | medium | mitigate | `coalesce(v_role,'') NOT IN`; smoke (h) |
| T-49-10-06 | Repudiation | escrita falha devolvendo `ok: true` | medium | mitigate | RPC com erro destruturado ⇒ 500 |
| T-49-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Duas migrations aplicadas (ensaio antes da segunda), md5 do ledger conferido; ACL da RPC nova conferida.
- `p49_analise_vigente_smoke` verde; mutações reprovam; `deno test` da EF e contrato do front verdes.
- EF de transcrição deployada com `verify_jwt=true`; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
A análise de entrevista que o RH lê, a que o portão de avanço consulta e a que a revisão humana confirma são a mesma — e a trilha diz de qual entrevista, de qual texto e de qual modelo ela veio.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `public.registrar_analise_entrevista(...)` | RPC SECURITY DEFINER (só `service_role`) | **nova** |
| `public.salvar_avaliacao_entrevista(uuid, jsonb, text)` | RPC | editada (vigente + fail-closed) |
| `public.confirmar_revisao_entrevista(uuid)` | RPC | editada (recusa superada + fail-closed) |
| `supabase/migrations/20260922000007_p49_analise_entrevista_vigente.sql` | migration | **nova** |
| `supabase/migrations/20260922000008_p49_revisao_entrevista_vigente.sql` | migration | **nova** |
| `supabase/tests/p49_analise_vigente_smoke.sql` (`smoke49v.pass`, SQLSTATE `P49V1`) | smoke | **novo** |
| `AvaliarTranscricaoBodySchema.tipo` (opcional) | schema de body | editado |
| resposta `{ analise_id, tipo, reaproveitada, vigente, falhou }` | contrato da EF | **novo** |
| `supabase/functions/avaliar-transcricao-entrevista/__tests__/index.test.ts` | teste Deno | **novo** |
| EF `avaliar-transcricao-entrevista` | deploy | nova versão |

## Premissas sinalizadas (probe de borda)

JORN-12 e JORN-28 saíram `unclassified` no probe e ficam `unresolved` no relatório.

- **D-40 × RESEARCH §I.3:** o RESEARCH reaproveitava só a VIGENTE com o mesmo hash; o CONTEXT §specifics
  (operador) diz «A, B, A ⇒ 2 análises, a vigente é a B, e A não cria linha». Este plano segue o CONTEXT:
  reaproveita qualquer análise de SUCESSO com o mesmo hash, sem mudar a vigente. Consequência registrada:
  reenviar A não torna A vigente de novo.
- **Tipo nulo (RESEARCH Correção 10 / Open Q):** as 6 análises antigas não têm tipo; `tipo` NULL é um grupo
  próprio para a regra de vigente (uma análise nova `online` não supera uma antiga de tipo desconhecido).
- **`salvar_avaliacao_entrevista` sem `p_analise_id`:** com uma linha só de nota para os dois tipos (D-65), a
  revisão vai para a vigente MAIS RECENTE de qualquer tipo; a tela (49-16) só oferece revisar essa. Mudar a
  assinatura foi considerado e descartado (acoplaria front e RPC sem necessidade).
- Uma unicidade no banco («uma vigente por tipo» como índice parcial) seria defesa extra; não entra aqui porque
  as 6 análises antigas a violam até a marcação do 49-12. A garantia fica no lock da RPC e na prova
  `p12_uma_vigente_por_tipo` (49-18).
- **A5 (RESEARCH):** as 3 candidaturas com análise são as únicas também na execução — contado no Passo 1.
- **Ordem de apply:** as `…000007`/`…000008` dividem a onda 3 com a `…000006` do 49-07
  (`trg_decisao_final_snapshot`, `stamp_explicacao_acessada`) — objetos disjuntos das RPCs deste plano (dono
  único), então a ordem relativa dentro da onda não muda o estado final; entre ondas, a ordem de apply é a ordem
  de versão.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-10-SUMMARY.md` when done
</output>
