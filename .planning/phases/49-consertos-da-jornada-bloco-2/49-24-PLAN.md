---
phase: 49-consertos-da-jornada-bloco-2
plan: 24
type: execute
wave: 2
depends_on: ["49-01", "49-02"]
files_modified:
  - supabase/functions/gerar-guia-entrevista/index.ts
autonomous: true
requirements: [JORN-28]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "D-28: `gerar-guia-entrevista` grava `provedor_ia` (`result.provider` quando `anthropic`/`openai`, senão NULL) e `modelo_ia` REAL (`result.model`, do `CallAiResult` do 49-02) no upsert de `entrevista_guias`."
    - "O `max_tokens` do `interview_guide` NÃO é tocado (P1, §Deferred do portão); o risco de 98,4 s / 110 s fica registrado no SUMMARY."
    - "Deploy por `efdeploy.cjs` com `--dry-run` (fechamento com os `_shared` do 49-02) e `verify_jwt=true` preservado, depois das colunas do 49-01 conferidas no catálogo (Pitfall 8); marcador `modelo_ia` no bundle vivo."
  artifacts:
    - path: supabase/functions/gerar-guia-entrevista/index.ts
      provides: "proveniência real do guia de entrevista"
      contains: "modelo_ia"
  key_links:
    - from: "CallAiResult.model / .provider (49-02)"
      to: "entrevista_guias.provedor_ia / modelo_ia → selo no GuiaEntrevistaPanel (49-16)"
      via: "upsert da EF"
      pattern: "modelo_ia"
  prohibitions:
    - requirement_id: JORN-28
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT gravar no guia o modelo CONFIGURADO no lugar do modelo que de fato respondeu (D-28)"
---

<objective>
O guia de entrevista passa a dizer qual modelo o escreveu.

Purpose: D-28 lista `entrevista_guias` entre as tabelas de resultado sem proveniência. Era a Task 3 do 49-10 (revisão
do checker, W1): não depende da trilha (49-06) nem da RPC da análise, só das colunas do 49-01 e do contrato do 49-02 —
por isso sobe para a onda 2 e sai do caminho do 49-10. É o passo «EF» cujo passo «cliente» (o selo no guia) é o 49-16
(D-55).

Output: a EF `gerar-guia-entrevista` com proveniência, deployada.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-02-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): proveniência do guia de entrevista (D-28) — upsert → deploy → bundle vivo</name>
  <precondition>O 49-01 está em PROD: `entrevista_guias.provedor_ia` e `.modelo_ia` existem (`information_schema.columns`), e o 49-02 está no disco (`CallAiResult.model`).</precondition>
  <files>supabase/functions/gerar-guia-entrevista/index.ts</files>
  <read_first>
    - `supabase/functions/gerar-guia-entrevista/index.ts` (`:300-375` — callAi e o upsert com `upsertErr` já checado)
    - `efdeploy.cjs` (`VERIFY_JWT['gerar-guia-entrevista'] = true`)
  </read_first>
  <action>
No upsert de `entrevista_guias` (`:362-370`), acrescentar `provedor_ia` (`result.provider` quando `anthropic`/`openai`, senão NULL) e `modelo_ia: result.model` (comentário D-28). Nada mais muda (o `max_tokens` do `interview_guide` NÃO é tocado — P1, §Deferred; o risco de 98,4 s/110 s fica registrado no SUMMARY). Se não houver teste do handler, acrescentar em `_local/` ou no molde do comparativo um teste mínimo de que o objeto do upsert leva `modelo_ia` = o do `CallAiResult` — ou registrar no SUMMARY por que a asserção fica na prova de PROD (`p28_resultados_com_modelo`, 49-18). Rodar `deno test --allow-all supabase/functions/gerar-guia-entrevista/`.

Deploy: conferir as duas colunas no catálogo; `node efdeploy.cjs gerar-guia-entrevista --dry-run` (fechamento com os `_shared` do 49-02; registrar), `node efdeploy.cjs gerar-guia-entrevista` (`verify_jwt=true`), marcador `modelo_ia` no bundle. Commitar e `git push origin main`; `origin/main..HEAD` vazio.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/gerar-guia-entrevista/ && grep -q "modelo_ia" supabase/functions/gerar-guia-entrevista/index.ts</automated>
    <fails_when>non-zero exit, "FAILED" in the Deno output, or the EF source lacks modelo_ia</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs gerar-guia-entrevista --dry-run | tee /dev/stderr | grep -q "functions/_shared/ai-client.ts" && node efdeploy.cjs gerar-guia-entrevista | tee /dev/stderr | grep -q "efdeploy: OK .*verify_jwt=true" && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/gerar-guia-entrevista/body" | grep -a -c "modelo_ia" && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>non-zero exit; efdeploy output lacks "efdeploy: OK" with "verify_jwt=true"; grep count 0; or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - O upsert de `entrevista_guias` grava `provedor_ia`/`modelo_ia` reais.
    - EF deployada com `verify_jwt=true`, marcador no bundle; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>O guia de entrevista passa a dizer qual modelo o escreveu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF → `entrevista_guias` (service_role) | a linha do guia passa a carregar quem o produziu |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-24-01 | Repudiation | guia sem registro do modelo que o escreveu (ou com o configurado) | medium | mitigate | `modelo_ia = result.model` (modelo real), `provedor_ia` do resultado |
| T-49-24-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `deno test` da EF verde (ou a razão registrada); EF deployada com `verify_jwt=true`; marcador no bundle; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
Todo guia de entrevista gerado depois do deploy diz qual provedor e qual modelo o escreveram.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `entrevista_guias.provedor_ia`, `.modelo_ia` (escritas) | colunas (do 49-01) | escrita nova |
| EF `gerar-guia-entrevista` | deploy | nova versão |

## Premissas sinalizadas (probe de borda)

JORN-28 saiu `unclassified` no probe e fica `unresolved` no relatório. Os guias antigos ficam com `modelo_ia` NULL =
«modelo não registrado» no selo (D-30), sem escrita retroativa.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-24-SUMMARY.md` when done
</output>
