---
phase: 49-consertos-da-jornada-bloco-2
plan: 09
type: execute
wave: 2
depends_on: ["49-01", "49-02"]
files_modified:
  - supabase/functions/_shared/bars-redacao.ts
  - supabase/functions/_shared/__tests__/bars-redacao.test.ts
  - supabase/functions/_shared/essay-schemas.ts
  - supabase/functions/avaliar-redacao-cultural/index.ts
  - supabase/functions/avaliar-redacao-cultural/index.test.ts
autonomous: true
requirements: [JORN-07, JORN-28]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "D-24: a rubrica enviada ao modelo na redação cultural é a BARS do PRD v1.1 — D1 Especificidade da situação · D2 Ação demonstrada · D3 Aprendizado/Reflexão · D4 Alinhamento com os valores Beauty Smile (os 4 valores DENTRO da D4) —, transcrita com âncoras 5→1 e caps de `docs/conhecimento/fit-cultural/bars-redacao-4-dimensoes.md` v1.1 para UMA constante versionada sem imports, `supabase/functions/_shared/bars-redacao.ts` (`RUBRICA_REDACAO_VERSAO = 'bars-prd-1.1'`, `DIMENSOES_REDACAO`, `montarBlocoRubricaRedacao`). É a mesma constante que a tela do RH importa no plano 49-15 (D-25)."
    - "O bloco entra por `vagaRubricBlock` (o prompt vivo já manda «use as âncoras BARS fornecidas no input»), sem versão nova de prompt e sem schema novo; ele entra no fingerprint de idempotência, então nenhuma avaliação antiga é replayada sob a rubrica nova. A pergunta vai como `Pergunta: <texto>` SEM o código (`perguntas_redacao.codigo` inclui D1/D2/D3 — colisão com as dimensões, varredura C7 #8). Few-shot NÃO entra nesta fase (RESEARCH §F.1; reversível)."
    - "O cap `D1 ≤ 2 → no máximo 50, 'situacao_generica_ou_inventada'` segue valendo com o significado de sempre (D-24); `_local/compute-score.ts` não muda."
    - "Pós-parse obrigatório: se o conjunto de `dimension` devolvido não for exatamente {D1, D2, D3, D4}, sem repetição, a redação NÃO é gravada como concluída — vai para o caminho de revisão humana com a flag `dimensoes_invalidas`, sem score. Quando é válido, o `dimension_name` gravado em `analise_ia` é o NOME DA CONSTANTE, não o que o modelo devolveu."
    - "D-26 / D-28: cada redação avaliada grava `rubrica_versao = 'bars-prd-1.1'`, `provedor_ia` e `modelo_ia` REAIS (do `CallAiResult` do 49-02); `model_version` passa a receber o modelo REAL (o mesmo valor de `modelo_ia`) — hoje grava o configurado (`index.ts:304,364`, RESEARCH Correção 28). As 2 redações antigas ficam como estão (sem escrita retroativa)."
    - "O comentário falso de `_shared/essay-schemas.ts:19-21` (D1–D4 = os 4 valores) sai e é substituído pela definição do D-24; o `:43` (que já lista especificidade/ação/aprendizado/alinhamento) fica."
    - "As escritas da EF destruturam o erro e nunca devolvem `{ ok: true }` depois de escrita falha (varredura C6 #6: `avaliar-redacao-cultural:291,346`)."
    - "Testes obrigatórios (Discretion do JORN-07): (1) Deno — o bloco enviado contém os 4 rótulos e as âncoras da constante; (2) Deno — `dimension` fora de {D1..D4} ou repetido ⇒ não grava concluída; `structured-output-compat.test.ts` segue verde (nenhum schema novo). O teste do SJT por chave é do plano 49-23; o que reprova se a rubrica enviada e os rótulos da TELA divergirem é do plano 49-15 (importa a mesma constante)."
    - "RNF-07a intacto: nenhuma das mudanças rejeita candidato por score; toda redação segue para revisão humana."
    - "Deploy da EF por `efdeploy.cjs` com `--dry-run` (fechamento com `_shared/bars-redacao.ts`, `ai-client.ts`, `audit-logger.ts`, `ai-error-codes.ts`) e `verify_jwt=true` preservado, depois do 49-01 conferido no catálogo (Pitfall 8)."
  artifacts:
    - path: supabase/functions/_shared/bars-redacao.ts
      provides: "rubrica BARS D1–D4 versionada — fonte única do modelo e da tela"
      contains: "RUBRICA_REDACAO_VERSAO"
    - path: supabase/functions/avaliar-redacao-cultural/index.ts
      provides: "rubrica no input, checagem pós-parse, rubrica_versao e proveniência real"
      contains: "montarBlocoRubricaRedacao"
  key_links:
    - from: "supabase/functions/_shared/bars-redacao.ts"
      to: "avaliar-redacao-cultural (bloco) e RedacaoReviewPanel/RedacaoOverrideForm (49-15, rótulos)"
      via: "import da mesma constante"
      pattern: "DIMENSOES_REDACAO"
  prohibitions:
    - requirement_id: JORN-07
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT gravar como concluída uma redação cujas dimensões devolvidas não batem com a rubrica enviada"
    - requirement_id: JORN-07
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT rejeitar candidato por score nem inventar âncoras que não estão nas fontes (RNF-07a, D-24)"
---

<objective>
A IA passa a avaliar a redação pela rubrica que o RH vai ler — em vez de o modelo inventar os nomes das dimensões.

Purpose: JORN-07/D-24..D-26 — o prompt manda «usar as âncoras BARS fornecidas no input», mas o input só leva a
pergunta; nomes inventados e instáveis (2 redações, 2 conjuntos). D-28 — proveniência real. A mesma forma de conserto
(a rubrica vai no input, o rótulo vem da chave — CONTEXT §Integração) é aplicada ao SJT no plano 49-23, que era a
Task 2 deste plano (revisão do checker, W1: duas EFs sem arquivo em comum, agora em paralelo na mesma onda).

Output: a constante sem imports, a EF da redação cultural consertada e deployada.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-02-SUMMARY.md
</context>

<interfaces>
- `_shared/bars-redacao.ts`: `RUBRICA_REDACAO_VERSAO: 'bars-prd-1.1'`; `DIMENSOES_REDACAO: ReadonlyArray<{ chave: 'D1'|'D2'|'D3'|'D4'; nome: 'especificidade'|'acao'|'aprendizado'|'alinhamento_valores'; rotulo: string; ancoras: Record<1|2|3|4|5, string> }>`; `montarBlocoRubricaRedacao(p: { perguntaTexto: string; valorPrimario?: string | null; valorSecundario?: string | null }): string`; `validarDimensoesRedacao(dims: Array<{ dimension?: string }>): { ok: boolean; motivo?: string }`. Zero imports.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer) [BLOCKING]: a redação cultural pela rubrica do PRD — constante → bloco → checagem pós-parse → linha com versão e modelo real → deploy</name>
  <precondition>O 49-01 está em PROD: `redacoes_candidato.rubrica_versao`, `.provedor_ia`, `.modelo_ia` existem (`information_schema.columns`).</precondition>
  <reversibility rating="costly">D-24: a rubrica define o que a nota consolidada significa; voltar atrás exige nova versão da constante e reavaliação — a versão gravada na linha (D-26) é o que mantém as antigas distinguíveis.</reversibility>
  <files>supabase/functions/_shared/bars-redacao.ts, supabase/functions/_shared/__tests__/bars-redacao.test.ts, supabase/functions/_shared/essay-schemas.ts, supabase/functions/avaliar-redacao-cultural/index.ts, supabase/functions/avaliar-redacao-cultural/index.test.ts</files>
  <read_first>
    - `docs/prds/m2-funil-rh/PRD-redacao-fit-cultural.md` (fonte BINDING da rubrica e do schema, §8.4)
    - `docs/conhecimento/fit-cultural/bars-redacao-4-dimensoes.md` v1.1 (inteiro — âncoras 1–5 por dimensão, os 3 caps, marcadores por valor)
    - `docs/conhecimento/prompts/templates/06-culture-fit-essay.md` (o prompt ativo — o que ele manda fazer com «as âncoras fornecidas no input»)
    - `supabase/functions/avaliar-transcricao-entrevista/_local/bars-rubric.ts` + `bars-rubric.test.ts` (o molde do builder puro e do teste)
    - `supabase/functions/_shared/email-config.ts` (`:12-17` — contrato ZERO IMPORTS)
    - `supabase/functions/avaliar-redacao-cultural/index.ts` (inteiro; `:244-376` — bloco, callAi, upsert de falha `:291-313`, cálculo `:318-323`, upsert de sucesso `:346-368`)
    - `supabase/functions/avaliar-redacao-cultural/_local/compute-score.ts` (`:56-61` — o cap D1 ≤ 2)
    - `supabase/functions/_shared/essay-schemas.ts` (`:1-60`)
  </read_first>
  <behavior>
    - `montarBlocoRubricaRedacao` devolve um texto que contém os 4 `rotulo` e, para cada dimensão, as âncoras 5, 4, 3, 2, 1 da constante; contém `Pergunta: <texto>` e NÃO contém `Pergunta (D1)`/qualquer código de pergunta; contém a instrução de devolver `dimension` com as chaves D1–D4.
    - `validarDimensoesRedacao`: {D1,D2,D3,D4} ⇒ ok; {D1,D2,D3} ⇒ não; {D1,D1,D3,D4} ⇒ não; {D1,D2,D3,D5} ⇒ não.
    - EF: resposta com dimensões válidas ⇒ upsert com `rubrica_versao='bars-prd-1.1'`, `provedor_ia`/`modelo_ia` = os do `CallAiResult`, `model_version` = o modelo real, e `analise_ia.dimension_scores[*].dimension_name` = o `nome` da constante; o `vagaRubricBlock` passado ao `callAi` = o bloco da constante.
    - EF: resposta com dimensões inválidas ⇒ upsert no caminho de revisão humana com a flag `dimensoes_invalidas`, sem `score_ponderado_0_100`.
    - EF: upsert que devolve erro ⇒ 500 (não `{ ok: true }`).
  </behavior>
  <action>
Passo 1 — D-50: re-rodar os padrões da C7 do `49-VARREDURA-KICKOFF.md` (`vagaRubricBlock:` nas EFs, `DIM_LABEL`/`DIMENSOES = [` no front, a consulta de `{{BARS_RUBRIC_DIMENSIONS}}` em `prompt_versions`) e registrar o delta no SUMMARY. Medir só leitura: as 2 redações avaliadas (`select id, model_version, analise_ia->'dimension_scores' from public.redacoes_candidato where analise_ia is not null` — só chaves/nomes, sem o texto) e `perguntas_redacao.codigo` distintos.

Passo 2 — criar `_shared/bars-redacao.ts` sem imports (docblock no molde de `bars-rubric.ts`: o defeito medido, D-24, a fonte binding, que é a MESMA constante da tela, e o contrato zero imports) com os nomes do `<interfaces>`; as âncoras são TRANSCRITAS do `bars-redacao-4-dimensoes.md` v1.1, sem paráfrase e sem inventar (se alguma âncora não existir na fonte, parar e reportar — D-14); o bloco inclui os caps em linguagem de instrução (D1 ≤ 2; red flag ético ⇒ D2 = 1 e D4 = 1) e os valores primário/secundário da pergunta como contexto da D4. Teste Deno `__tests__/bars-redacao.test.ts` com o `<behavior>` dos dois helpers.

Passo 3 — `essay-schemas.ts`: trocar o parágrafo falso (`:19-21`) pela definição do D-24 com a referência à constante; nada mais muda no schema (`dimension: z.enum([...])` e `.length(4)` ficam — sem bump de `SCHEMA_VERSIONS`).

Passo 4 — EF `avaliar-redacao-cultural`: `vagaRubricBlock: montarBlocoRubricaRedacao({ perguntaTexto: pergRow.texto, valorPrimario: pergRow.valor_primario, valorSecundario: pergRow.valor_secundario })`; depois do parse e ANTES de `computeScoreAndCors`, `validarDimensoesRedacao` — inválido ⇒ o upsert de revisão humana com a flag `dimensoes_invalidas` (o mesmo caminho do never-absent), sem score; válido ⇒ normalizar `dimension_name` pelo `nome` da constante antes de gravar `analise_ia`; os dois upserts gravam `rubrica_versao: RUBRICA_REDACAO_VERSAO` (só no caminho em que o modelo avaliou com a rubrica — o de falha grava `rubrica_versao` também, porque a rubrica foi enviada), `provedor_ia` (`anthropic`|`openai`, senão NULL), `modelo_ia: result.model`, `model_version: result.model` (comentário: D-28 — era o configurado), e destruturam `{ error }` lançando se houver. Testes do `<behavior>` em `index.test.ts`; rodar `deno test --allow-all supabase/functions/avaliar-redacao-cultural/ supabase/functions/_shared/__tests__/bars-redacao.test.ts supabase/functions/_shared/__tests__/essay-schemas.test.ts supabase/functions/_shared/__tests__/structured-output-compat.test.ts`.

Passo 5 — deploy: conferir as 3 colunas no catálogo; `node efdeploy.cjs avaliar-redacao-cultural --dry-run` (conferir À MÃO que a lista tem `functions/_shared/bars-redacao.ts` e os `_shared` do 49-02; registrar no SUMMARY); `node efdeploy.cjs avaliar-redacao-cultural` (`verify_jwt=true`); marcador `bars-prd-1.1` no bundle vivo. Commitar e `git push origin main`; `origin/main..HEAD` vazio.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/avaliar-redacao-cultural/ supabase/functions/_shared/__tests__/bars-redacao.test.ts supabase/functions/_shared/__tests__/essay-schemas.test.ts supabase/functions/_shared/__tests__/structured-output-compat.test.ts</automated>
    <fails_when>non-zero exit, or "FAILED" in the output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const b=fs.readFileSync('supabase/functions/_shared/bars-redacao.ts','utf8');if(/^\s*import\s/m.test(b))throw new Error('bars-redacao.ts tem import');for(const t of ['bars-prd-1.1','Especificidade da situação','Ação demonstrada','Aprendizado','Alinhamento com os valores'])if(!b.includes(t))throw new Error('constante sem: '+t);const e=fs.readFileSync('supabase/functions/_shared/essay-schemas.ts','utf8');if(/D1-D4 mapeiam os 4 valores/.test(e))throw new Error('o comentario falso continua em essay-schemas.ts');const f=fs.readFileSync('supabase/functions/avaliar-redacao-cultural/index.ts','utf8');if(/model_version:\s*resolved\.model_id/.test(f))throw new Error('model_version ainda grava o modelo configurado');if(!/montarBlocoRubricaRedacao\(/.test(f)||!/rubrica_versao/.test(f))throw new Error('EF sem o bloco ou sem rubrica_versao');console.log('OK')"</automated>
    <fails_when>non-zero exit with "tem import", "constante sem:", "comentario falso continua", "ainda grava o modelo configurado" or "EF sem o bloco" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs avaliar-redacao-cultural --dry-run | tee /dev/stderr | grep -q "functions/_shared/bars-redacao.ts" && node efdeploy.cjs avaliar-redacao-cultural | tee /dev/stderr | grep -q "efdeploy: OK .*verify_jwt=true" && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/avaliar-redacao-cultural/body" | grep -a -c "bars-prd-1.1" && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>non-zero exit; the dry-run closure lacks functions/_shared/bars-redacao.ts; efdeploy output lacks "efdeploy: OK" with "verify_jwt=true"; the grep count printed is 0; or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - `bars-redacao.ts` existe, sem imports, com as 4 dimensões do PRD v1.1, âncoras transcritas e `bars-prd-1.1`.
    - O comentário falso saiu de `essay-schemas.ts`; o schema não mudou.
    - Os testes do `<behavior>` passam; `structured-output-compat` verde.
    - A EF está deployada com `verify_jwt=true` e o bundle vivo contém `bars-prd-1.1`; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>A próxima redação avaliada o será pela rubrica que o RH vai ler, com a versão e o modelo gravados na linha.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidato → texto da redação (UNTRUSTED) → modelo | o texto pode tentar mudar as instruções; `callAi` detecta injeção |
| modelo → `dimension`/`dimension_name` devolvidos | saída não confiável; a chave é validada contra a rubrica enviada |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-09-01 | Tampering | conjunto de dimensões inválido gravado como redação concluída | high | mitigate | validação pós-parse por chave; inválido ⇒ revisão humana sem score |
| T-49-09-02 | Repudiation | redação sem registro de qual rubrica/modelo a avaliou | medium | mitigate | `rubrica_versao` + `provedor_ia`/`modelo_ia` reais |
| T-49-09-03 | Repudiation | escrita falha devolvendo `ok: true` | medium | mitigate | erro destruturado nos 2 upserts; 500 |
| T-49-09-04 | Tampering | rótulo da tela divergindo da rubrica enviada | medium | mitigate | constante única importada pela EF e (49-15) pela tela, com teste |
| T-49-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `deno test` da EF e do teste da constante verdes; `structured-output-compat` verde.
- EF deployada com `verify_jwt=true`, fechamento conferido, marcador no bundle; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
A nota de cultura passa a medir o que a rubrica escrita diz que mede, e cada avaliação carrega a versão da rubrica e o
modelo que a produziu.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `supabase/functions/_shared/bars-redacao.ts` (`RUBRICA_REDACAO_VERSAO`, `DIMENSOES_REDACAO`, `montarBlocoRubricaRedacao`, `validarDimensoesRedacao`) | constante sem imports | **nova** |
| `supabase/functions/_shared/__tests__/bars-redacao.test.ts` | teste Deno | **novo** |
| flag `dimensoes_invalidas` (redação) | valor gravado | **novo** |
| `redacoes_candidato.rubrica_versao`, `.provedor_ia`, `.modelo_ia` (escritas); `model_version` = modelo real | colunas | escrita nova |
| EF `avaliar-redacao-cultural` | deploy | nova versão |

## Premissas sinalizadas (probe de borda)

JORN-07 e JORN-28 saíram `unclassified` no probe e ficam `unresolved` no relatório. A premissa A2 do RESEARCH (o
strict mode da OpenAI não aceita tupla por posição) só justifica a checagem ser pós-parse; ela vale de qualquer jeito.
O `culture_fit_essay` tem `max_tokens` 2500 com 50 % de folga (C9); a rubrica no input pode alongar a saída — a saída
real é medida na prova (49-18), sem mexer no teto aqui.

Contato registrado: as redações de 26/06 e 20/09 avaliadas com a rubrica fantasma ficam como estão (D-26).

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-09-SUMMARY.md` when done
</output>
