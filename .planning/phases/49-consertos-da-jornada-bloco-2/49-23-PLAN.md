---
phase: 49-consertos-da-jornada-bloco-2
plan: 23
type: execute
wave: 2
depends_on: ["49-01", "49-02"]
files_modified:
  - supabase/functions/_shared/sjt-rubrica.ts
  - supabase/functions/_shared/__tests__/sjt-rubrica.test.ts
  - supabase/functions/avaliar-redacao/index.ts
  - supabase/functions/avaliar-redacao/__tests__/index.test.ts
autonomous: true
requirements: [JORN-35, JORN-28]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "JORN-35: o SJT (`avaliar-redacao`, caso aberto) recebe as dimensões da rubrica da pergunta POR CHAVE — as chaves e os pesos vivos de `perguntas.rubric.dimensoes` (`raciocinio_clinico_estetico`, `planejamento_decisao`, `comunicacao_expectativa`, `etica_minimamente_invasivo`, `consentimento_continuidade`) com rótulo e âncoras de `supabase/functions/_shared/sjt-rubrica.ts` —, e a instrução de devolver `dimension` = a chave. Dimensão devolvida que não é chave da rubrica ⇒ `has_insufficient_evidence`/`pendente_humano` com a flag `dimensao_desconhecida`, NUNCA peso 1 em silêncio."
    - "D-68: a SJT grava proveniência (`provedor_ia`, `modelo_ia` reais, do `CallAiResult` do 49-02) na `metadata` de `scores_candidato` — sem coluna nova e sem checklist D-57."
    - "As três escritas da EF destruturam o erro e nunca devolvem `{ ok: true }` depois de escrita falha (varredura C6 #6: `avaliar-redacao:281,297,322`)."
    - "Teste obrigatório (Discretion do JORN-07/35): Deno — SJT com nome inventado ⇒ pendente humano, não peso 1; e pesos da rubrica aplicados pela chave."
    - "RNF-07a intacto: o limiar `composite < 13 || red flag || insufficient ⇒ pendente_humano` não muda; todo caso aberto segue para revisão humana; nenhuma rejeição por score."
    - "Deploy da EF por `efdeploy.cjs` com `--dry-run` (fechamento com `_shared/sjt-rubrica.ts` e os `_shared` do 49-02) e `verify_jwt=true` preservado."
  artifacts:
    - path: supabase/functions/_shared/sjt-rubrica.ts
      provides: "rótulos/âncoras do SJT por chave de dimensão"
      contains: "raciocinio_clinico_estetico"
    - path: supabase/functions/avaliar-redacao/index.ts
      provides: "SJT por chave, sem peso uniforme silencioso, proveniência na metadata"
      contains: "dimensao_desconhecida"
  key_links:
    - from: "perguntas.rubric.dimensoes[].dimension (vivo)"
      to: "bloco do SJT + pesos de mapDimensionsToComposite"
      via: "chave da dimensão, nunca o nome devolvido"
      pattern: "rubricWeights"
  prohibitions:
    - requirement_id: JORN-35
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT pesar em silêncio uma dimensão que a IA inventou — vai para revisão humana"
    - requirement_id: JORN-35
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT rejeitar candidato por score nem inventar âncoras que não estão nas fontes (RNF-07a)"
---

<objective>
O SJT passa a receber a rubrica da pergunta e a pesar pela chave — em vez de o modelo inventar os nomes das dimensões e
o código pesar tudo igual — e grava qual modelo o avaliou.

Purpose: JORN-35 — `perguntas.rubric` nunca vai ao modelo, e o peso-padrão 1 para dimensão de nome desconhecido
(`avaliar-redacao/index.ts:131`) torna os pesos uniformes em silêncio (a única SJT avaliada devolveu 5 nomes, 0 casam).
D-68 — proveniência na `metadata`. Mesma forma de conserto da redação (49-09): a rubrica vai no input, o rótulo vem da
chave (CONTEXT §Integração). Era a Task 2 do 49-09 (revisão do checker, W1): as duas EFs não têm arquivo em comum e
rodam em paralelo na mesma onda.

Output: a constante sem imports, a EF `avaliar-redacao` consertada e deployada.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-02-SUMMARY.md
</context>

<interfaces>
- `_shared/sjt-rubrica.ts`: `DIMENSOES_SJT: Record<string, { rotulo: string; ancoras: Record<1|2|3|4|5, string> | null }>`; `montarBlocoRubricaSjt(dimensoes: Array<{ dimension: string; peso: number }>): string`. Zero imports.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer) [BLOCKING]: o SJT pela chave da dimensão (JORN-35), com proveniência na metadata (D-68) — constante → bloco → composto → linha → deploy</name>
  <precondition>O 49-02 está no disco: `supabase/functions/_shared/ai-client.ts` devolve `model` no `CallAiResult` (grep `model:` no retorno) — é daqui que a `metadata` lê o modelo real.</precondition>
  <files>supabase/functions/_shared/sjt-rubrica.ts, supabase/functions/_shared/__tests__/sjt-rubrica.test.ts, supabase/functions/avaliar-redacao/index.ts, supabase/functions/avaliar-redacao/__tests__/index.test.ts</files>
  <read_first>
    - `supabase/functions/avaliar-redacao/index.ts` (inteiro; `:108-160` `mapDimensionsToComposite`/`rubricWeightsFrom`; `:215-335` leitura da pergunta, callAi com `Vaga: <uuid>` em `:256`, os três inserts `:281`, `:297`, `:322`)
    - `supabase/functions/avaliar-redacao/__tests__/index.test.ts`
    - `supabase/migrations/20260611000002_perguntas_sjt.sql` (`:176-195` — o caso aberto da dentista com `rubric.dimensoes` e pesos 25/20/25/15/15) e o vivo: `select id, rubric from public.perguntas where formato = 'caso_aberto'` (só leitura)
    - `docs/conhecimento/sjt/banco-sjt-dentista.md` (o caso «Mariana» e a rubrica dele) e `docs/conhecimento/sjt/bars-rubrics-por-dimensao.md` (Parte B — templates B1–B5 com âncoras)
    - `docs/prds/m2-funil-rh/PRD-sjt-work-sample-odontologia.md` (§8.3, RF-SJT-05 — `< 13/25 OU red flag` → revisão; nunca rejeição automática)
    - `supabase/functions/_shared/email-config.ts` (`:12-17` — contrato ZERO IMPORTS)
  </read_first>
  <behavior>
    - `montarBlocoRubricaSjt([{dimension:'raciocinio_clinico_estetico',peso:25}, …])` contém cada chave, o peso, o rótulo e as âncoras da constante; para uma chave sem entrada na constante, contém a chave, o peso e o aviso de que não há âncoras oficiais (nunca âncora inventada).
    - EF: resposta com as 5 chaves da rubrica ⇒ composto pelos pesos da rubrica (não uniforme); `metadata.provedor_ia`/`metadata.modelo_ia` = os do `CallAiResult`.
    - EF: resposta com uma dimensão fora das chaves ⇒ `status='pendente_humano'`, `metadata.has_insufficient_evidence=true` e flag `dimensao_desconhecida` com o nome devolvido; a dimensão desconhecida não entra na soma.
    - EF: insert que devolve erro ⇒ 500.
  </behavior>
  <action>
Passo 1 — D-50: re-rodar o padrão da C7 do `49-VARREDURA-KICKOFF.md` para `vagaRubricBlock:` em `avaliar-redacao` e o `grep` da C6 #6 (escritas sem erro destruturado nesta EF); registrar o delta no SUMMARY.

Passo 2 — criar `_shared/sjt-rubrica.ts` sem imports: `DIMENSOES_SJT` indexado pelas 5 chaves vivas, com rótulo pt-BR e âncoras 1–5 TRANSCRITAS dos documentos do read_first (a correspondência chave → template/âncoras vai no docblock, com a fonte de cada uma); chave sem âncoras nas fontes ⇒ `ancoras: null` e registro no SUMMARY (nunca inventar); `montarBlocoRubricaSjt`. Teste Deno com o `<behavior>` do builder.

Passo 3 — EF `avaliar-redacao`: `vagaRubricBlock` passa a ser `montarBlocoRubricaSjt(rubric.dimensoes)` (com a instrução de devolver `dimension` = a chave); `mapDimensionsToComposite` recebe o conjunto de chaves da rubrica — dimensão devolvida que não é chave ⇒ marca `hasInsufficient` e acumula o nome em `dimensoes_desconhecidas`, SEM peso (comentário JORN-35: antes caía em peso 1 e o composto virava média uniforme); com `rubric` nula (pergunta sem rubrica) o comportamento é o de hoje, mas registrado na `metadata` como `rubrica_ausente: true`. Os três inserts em `scores_candidato` levam na `metadata` `provedor_ia`, `modelo_ia` (D-68) e, quando houver, `dimensoes_desconhecidas`, e destruturam `{ error }` lançando se houver. O limiar `composite < 13 || red flag || insufficient ⇒ pendente_humano` não muda (RNF-07a: nunca rejeição). Testes do `<behavior>`; rodar `deno test --allow-all supabase/functions/avaliar-redacao/ supabase/functions/_shared/__tests__/sjt-rubrica.test.ts`.

Passo 4 — deploy: `node efdeploy.cjs avaliar-redacao --dry-run` (lista com `functions/_shared/sjt-rubrica.ts` e os `_shared` do 49-02; registrar), `node efdeploy.cjs avaliar-redacao` (`verify_jwt=true`), marcador `dimensao_desconhecida` no bundle. Commitar e `git push origin main`; `origin/main..HEAD` vazio.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/avaliar-redacao/ supabase/functions/_shared/__tests__/sjt-rubrica.test.ts</automated>
    <fails_when>non-zero exit, or "FAILED" in the output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/functions/_shared/sjt-rubrica.ts','utf8');if(/^\s*import\s/m.test(s))throw new Error('sjt-rubrica.ts tem import');for(const k of ['raciocinio_clinico_estetico','planejamento_decisao','comunicacao_expectativa','etica_minimamente_invasivo','consentimento_continuidade'])if(!s.includes(k))throw new Error('constante sem a chave '+k);const f=fs.readFileSync('supabase/functions/avaliar-redacao/index.ts','utf8');if(/rubricWeights\?\.\[d\.dimension\]\s*\?\?\s*1/.test(f))throw new Error('peso 1 silencioso continua');if(!/montarBlocoRubricaSjt\(/.test(f)||!/modelo_ia/.test(f))throw new Error('EF sem o bloco ou sem proveniencia');console.log('OK')"</automated>
    <fails_when>non-zero exit with "tem import", "constante sem a chave", "peso 1 silencioso continua" or "EF sem o bloco" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs avaliar-redacao --dry-run | tee /dev/stderr | grep -q "functions/_shared/sjt-rubrica.ts" && node efdeploy.cjs avaliar-redacao | tee /dev/stderr | grep -q "efdeploy: OK .*verify_jwt=true" && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/avaliar-redacao/body" | grep -a -c "dimensao_desconhecida" && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>non-zero exit; closure lacks functions/_shared/sjt-rubrica.ts; efdeploy output lacks "efdeploy: OK" with "verify_jwt=true"; grep count 0; or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - `sjt-rubrica.ts` existe sem imports, com as 5 chaves vivas e a fonte de cada âncora no docblock (chave sem âncora registrada no SUMMARY).
    - Nome inventado vai para revisão humana e não pesa (teste); pesos da rubrica aplicados pela chave (teste).
    - `metadata` com `provedor_ia`/`modelo_ia`; escritas com erro checado.
    - EF deployada com `verify_jwt=true`; marcador no bundle; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>O caso aberto passa a ser avaliado pelas dimensões e pesos que a vaga definiu — e o que a IA inventar fica na mesa do RH, não na nota.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidato → resposta do caso (UNTRUSTED) → modelo | o texto pode tentar mudar as instruções; `callAi` detecta injeção |
| modelo → `dimension` devolvida | saída não confiável; a chave é validada contra a rubrica enviada |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-23-01 | Tampering | dimensão inventada pelo modelo virando nota (peso 1) | high | mitigate | validação por chave; desconhecida ⇒ revisão humana, sem peso |
| T-49-23-02 | Repudiation | SJT sem registro de qual modelo o avaliou | medium | mitigate | `provedor_ia`/`modelo_ia` reais na `metadata` (D-68) |
| T-49-23-03 | Repudiation | escrita falha devolvendo `ok: true` | medium | mitigate | erro destruturado nos 3 inserts; 500 |
| T-49-23-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `deno test` da EF e do teste da constante verdes.
- EF deployada com `verify_jwt=true`, fechamento conferido, marcador no bundle; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
A nota do caso aberto passa a medir o que a rubrica da vaga diz que mede, e cada avaliação carrega o modelo que a
produziu.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `supabase/functions/_shared/sjt-rubrica.ts` (`DIMENSOES_SJT`, `montarBlocoRubricaSjt`) | constante sem imports | **nova** |
| `supabase/functions/_shared/__tests__/sjt-rubrica.test.ts` | teste Deno | **novo** |
| flags `dimensao_desconhecida` / `dimensoes_desconhecidas` / `rubrica_ausente` (SJT) | valores gravados | **novos** |
| `scores_candidato.metadata.provedor_ia` / `.modelo_ia` (tipo `sjt`) | jsonb | **novo** (D-68) |
| EF `avaliar-redacao` | deploy | nova versão |

## Premissas sinalizadas (probe de borda)

JORN-35 e JORN-28 saíram `unclassified` no probe e ficam `unresolved` no relatório. A única SJT avaliada (`a1dd4c42`,
fixture) fica como está (mesma lógica do D-30). O `work_sample_sjt` tem `max_tokens` 3000 (C9); o bloco da rubrica no
input pode alongar a saída — medido na prova (49-18), sem mexer no teto aqui.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-23-SUMMARY.md` when done
</output>
