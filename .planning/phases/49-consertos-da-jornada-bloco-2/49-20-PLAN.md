---
phase: 49-consertos-da-jornada-bloco-2
plan: 20
type: execute
wave: 5
depends_on: ["49-14"]
files_modified:
  - supabase/migrations/20260922000013_p49_motor_respostas_e_producoes.sql
  - supabase/tests/p45_motor_exclusao_smoke.sql
autonomous: true
requirements: [JORN-36]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "D-48: o motor de exclusão (`anonimizar_candidato`) passa a apagar TODA origem que o recibo promete ter apagado no item `respostas_e_producoes` — cada origem listada no gerador do recibo tem uma ação no motor (sentinela em `text`/`jsonb` NOT NULL, NULL onde a coluna aceita, DELETE só nas 4 tabelas do D-62) e uma asserção de pós-estado no `p45_motor_exclusao_smoke.sql`. Origem sem ação possível dentro das decisões (ex.: inteiro/enum NOT NULL fora das 4 tabelas) ⇒ o executor PARA e reporta (D-14)."
    - "D-62: o passo novo APAGA as linhas do titular em `respostas_raven`, `respostas_bigfive`, `respostas_disc` e `respostas_formulario` — a exceção explícita do operador à regra «nenhuma escrita apaga linha», válida só neste passo e só nestas quatro tabelas; `scores_raven` e `scores_candidato` ficam."
    - "Colunas `text`/`jsonb` do titular recebem sentinela e a linha fica: `redacoes_candidato.texto`, `redacoes_candidato_em_progresso.texto_em_progresso`, `respostas_cultura.resposta_texto`, `respostas_avaliacao.respostas`, `cognitivo_respostas.raw_responses`/`proctoring`, as colunas de `entrevistas_online`/`entrevistas_presenciais` listadas no recibo; `entrevista_analises.citacoes` e `scores_candidato.citacoes` ⇒ NULL onde nulável (conferido no catálogo)."
    - "Consequência do D-48 (não é escolha nova): os trechos literais em `redacoes_candidato.analise_ia.dimension_scores[].cited_evidence` e em `scores_candidato.metadata.dimension_scores[].cited_evidence` (SJT) são removidos pelo passo novo, preservando o resto da análise."
    - "O passo novo não depende do papel de quem executa: `trg_redacao_rh_only_review_fields` ganha uma sanção por GUC (`app.motor_exclusao`), ligada pelo motor imediatamente antes do seu UPDATE em `redacoes_candidato` e zerada logo depois — uma execução por administrador não aborta DEPOIS do passo de Storage (RESEARCH Correção 18, Pitfall 6)."
    - "`plano_exclusao_titular` (o dry-run) conta o passo novo `apagar_respostas_e_producoes` com a contagem por tabela, pela MESMA expressão do motor (regra (ii) do C3 do smoke); o retorno `'passos'` do motor ganha a mesma chave."
    - "O `p45_motor_exclusao_smoke.sql` é ampliado a partir do estado deixado pelo 49-14: fixtures em cada tabela do passo novo (respeitando os CHECKs de cada uma); uma asserção de pós-estado por coluna/tabela nova; a rede estrutural (C3) cresce com a forma do passo (antes de re-pinar os md5 — PATTERNS §Shared C); o contador sobe pelo número exato de asserções novas; e a mordida é provada por mutação (passo desligado ⇒ FAIL, D-56)."
    - "Nada executa o caminho feliz dentro da migration (proibição do 46-04); a prova é do smoke, dentro de subtransação que reverte. A PRIMEIRA execução real do passo novo é o checkpoint do plano 49-19, em conta de teste (D-54) — e este plano confere de novo, só leitura, que a purga automática está em ensaio (`dry_run`), para que nenhum titular real passe pelo passo antes do checkpoint."
    - "O recibo passa a apontar o passo novo no plano 49-21, que vem logo depois e antes de qualquer execução real (49-19)."
    - "Nenhum texto novo (sentinelas, comentários) contém o endereço literal do canal de privacidade nem «teste psicológico» (D-07, D-58)."
  artifacts:
    - path: supabase/migrations/20260922000013_p49_motor_respostas_e_producoes.sql
      provides: "passo novo apagar_respostas_e_producoes (D-48, D-62) + sanção no trigger da redação"
      contains: "apagar_respostas_e_producoes"
    - path: supabase/tests/p45_motor_exclusao_smoke.sql
      provides: "uma asserção por origem do passo novo + pins re-pinados com proveniência"
      contains: "apagar_respostas_e_producoes"
  key_links:
    - from: "p45_motor_exclusao_smoke.sql (uma asserção por origem de respostas_e_producoes do gerador do recibo)"
      to: "anonimizar_candidato (passo apagar_respostas_e_producoes)"
      via: "cada origem prometida tem ação no motor e asserção no smoke (o gerador passa a apontar o passo no 49-21)"
      pattern: "apagar_respostas_e_producoes"
    - from: "anonimizar_candidato"
      to: "plano_exclusao_titular"
      via: "mesma expressão de seleção por passo (dry-run = espelho)"
      pattern: "apagar_respostas_e_producoes"
  prohibitions:
    - requirement_id: JORN-36
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT apagar linha fora de respostas_raven, respostas_bigfive, respostas_disc e respostas_formulario (D-62 é a única exceção)"
    - requirement_id: JORN-36
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT executar o passo novo sobre titular real antes do checkpoint do 49-19 — nem pela migration, nem pelo smoke fora de subtransação revertida"
    - requirement_id: JORN-36
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT re-pinar o md5 do motor sem fazer crescer a rede estrutural do smoke no mesmo commit (D-56)"
    - requirement_id: JORN-36
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT introduzir ocorrência literal nova do endereço do canal de privacidade nem a expressão «teste psicológico» em texto novo (D-07, D-58 — a única exceção é o disclaimer negado da devolutiva, já decidido)"
---

<objective>
O motor de exclusão ganha o passo que apaga as respostas, os textos, as transcrições e as citações do titular — o que
o recibo sempre disse que apagava —, com a exceção D-62 e sem depender do papel de quem executa.

Purpose: JORN-36/D-48 e D-62. Medido: o motor não toca nenhuma das 14 origens do item `respostas_e_producoes`
(RESEARCH Correções 14, 15, 18). A única exclusão concluída (22/08) era de titular sem esses dados — o recibo ainda não
mentiu, mas mentiria. Segunda de três fatias sequenciais do mesmo motor (49-14 → 49-20 → 49-21): parte do corpo que o
49-14 deixou em PROD e o entrega ao 49-21, que alinha o recibo. Mecanismo destrutivo: a prova é o smoke revertido
aqui, a mordida é provada por mutação e a primeira execução real é checkpoint do 49-19.

Output: a migration `20260922000013`, o smoke ampliado e re-pinado, a mutação registrada.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-14-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer) [BLOCKING]: o passo novo `apagar_respostas_e_producoes` — respostas, textos, transcrições e citações, com a exceção D-62 e sem depender do papel de quem executa</name>
  <precondition>A purga automática está em ensaio (o modo vivo lido da tabela de configuração que `salvar_config_purga` escreve é `dry_run`) e o motor vivo é o do 49-14 (o `md5(prosrc)` de `anonimizar_candidato` = o impresso pelo pós-portão registrado no 49-14-SUMMARY) — purga `live` ou md5 diferente ⇒ PARAR e reportar.</precondition>
  <reversibility rating="one-way">D-62: as linhas das 4 tabelas de resposta deixam de existir a cada execução real; decidido pelo operador no portão (não re-perguntar), primeira execução real no checkpoint do 49-19.</reversibility>
  <files>supabase/migrations/20260922000013_p49_motor_respostas_e_producoes.sql, supabase/tests/p45_motor_exclusao_smoke.sql</files>
  <read_first>
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-14-SUMMARY.md` (md5 pós-portão das duas funções, o contador do smoke depois do bump, a tabela «origem → ação → asserção» — as linhas de `respostas_e_producoes` são o escopo deste plano)
    - `supabase/migrations/20260922000012_p49_motor_logs_e_revisao.sql` (o corpo base desta migration E da mutação — é o motor vivo sem o passo novo)
    - o vivo das duas funções (md5 impresso pelo pós-portão do 49-14) e o de `trg_redacao_rh_only_review_fields` (`supabase/migrations/20260623100003_redacoes_candidato.sql:139-170` — recusa mudar `texto`/`analise_ia`/… com JWT `rh`/`administrador`)
    - o catálogo de CADA tabela de origem, só leitura: `information_schema.columns` (tipo, `is_nullable`) e `pg_constraint` (CHECKs) de `redacoes_candidato`, `redacoes_candidato_em_progresso`, `respostas_cultura`, `respostas_formulario`, `respostas_bigfive`, `respostas_disc`, `respostas_raven`, `respostas_avaliacao`, `cognitivo_respostas`, `entrevistas_online`, `entrevistas_presenciais`, `entrevista_analises`, `scores_candidato`; e os triggers BEFORE/AFTER UPDATE/DELETE de cada uma (`pg_trigger`)
    - `docs/compliance/sql/gen-recibo-exclusao.cjs` (`:293-317` — as 14 origens de `respostas_e_producoes`)
    - `supabase/tests/p45_motor_exclusao_smoke.sql` (o cabeçalho com o re-pin e o bump do 49-14; o bloco (C3))
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md` Correções 14, 15, 18 e Pitfall 6
  </read_first>
  <action>
Passo 1 — D-50: conferir a tabela «origem → ação → asserção» do 49-14-SUMMARY para as 14 origens de `respostas_e_producoes` contra o catálogo lido no read_first (o catálogo pode ter mudado entre as ondas; delta no SUMMARY), e a varredura de portões do `CLAUDE.md` para os achados que citem as 13 tabelas de origem ou `trg_redacao_rh_only_review_fields`.

Passo 2 — escrever `supabase/migrations/20260922000013_p49_motor_respostas_e_producoes.sql` (mesmo molde e proveniência do `20260922000012`; pré-portão com os md5 pós-49-14 e o de `trg_redacao_rh_only_review_fields`):
(a) `CREATE OR REPLACE FUNCTION public.trg_redacao_rh_only_review_fields()` = corpo vivo + no topo: se `coalesce(current_setting('app.motor_exclusao', true), '') = 'on'` ⇒ `RETURN NEW` (comentário: sanção do motor de exclusão, ligada e zerada pelo próprio motor em volta de UM UPDATE; nenhum cliente seta `app.*`);
(b) no `anonimizar_candidato`, um passo novo `-- ══ passo_motor: apagar_respostas_e_producoes ══` ANTES de `severar_fks_set_null`, escopado por `candidatura_id IN (SELECT id FROM public.candidaturas WHERE candidato_id = p_candidato_id)`, com UM statement por tabela e `GET DIAGNOSTICS v_n_<tabela>` em cada: `DELETE FROM` `respostas_raven`, `respostas_bigfive`, `respostas_disc`, `respostas_formulario` (D-62, comentário com a exceção e o porquê — valores inteiros/enum com CHECK não aceitam sentinela); sentinela de texto (`'[removido a pedido do titular — LGPD Art. 18]'`) nas colunas `text` NOT NULL e de jsonb (`'{"redigido":"anonimizacao_p49"}'::jsonb`) nas `jsonb` NOT NULL, NULL nas nuláveis — a escolha POR COLUNA vem do catálogo lido no read_first e é CONFERIDA por um `DO` de auto-verificação da migration (se alguma coluna mudar de nulidade/tipo, a migration aborta antes de instalar — Pitfall 6); em `redacoes_candidato`: `PERFORM set_config('app.motor_exclusao','on',true)`, o UPDATE (`texto` = sentinela e `analise_ia` com `cited_evidence` removido de cada elemento de `dimension_scores` quando for array), `PERFORM set_config('app.motor_exclusao','',true)`; em `scores_candidato`: `citacoes` = NULL (se nulável) e, para `tipo='sjt'`, `metadata` com `cited_evidence` removido de `dimension_scores[]`; `entrevista_analises.citacoes` = NULL; as demais origens do recibo pelo mesmo critério. `devolutivas_candidato` NÃO entra: a linha já morre por FK CASCADE no passo 3 (`auth_delete_user`, Correção 15) — comentário. Qualquer origem inteira/enum NOT NULL fora das 4 tabelas ⇒ PARAR e reportar (D-14);
(c) `'passos'` ganha `apagar_respostas_e_producoes` com as contagens por tabela; `plano_exclusao_titular` conta o mesmo pela mesma expressão;
(d) pós-portão (presença do passo, dos 4 `DELETE`, do set/reset da GUC em volta do UPDATE da redação, do passo antes de `severar_fks_set_null`; ausência de `DELETE` em qualquer outra tabela; as formas do 49-14 — `revisao_resultado`, `comparative_ranking` — continuam) + `RAISE NOTICE` com os md5.

Passo 3 — `p45_motor_exclusao_smoke.sql`: fixtures (dentro da subtransação que reverte) em cada tabela do passo, com valores que respeitam os CHECKs vivos; asserções novas — 0 linhas nas 4 tabelas depois; sentinela/NULL em cada coluna; `cited_evidence` ausente e o resto da análise presente em `redacoes_candidato.analise_ia` e na `metadata` da SJT; `scores_raven`/`scores_candidato` presentes (ficam); o plano previu cada contagem; uma execução com claims de `administrador` NÃO aborta no trigger da redação (Correção 18); `current_setting('app.motor_exclusao', true)` vazio depois. Rede estrutural cresce ANTES do re-pin; re-pin com proveniência; contador sobe a partir do valor deixado pelo 49-14 (bump registrado no cabeçalho). Ensaio em requisição que aborta, `migrate`, smoke, `p46_purga_smoke`.

Passo 4 — mutação (D-56), em requisição que aborta: o corpo de `anonimizar_candidato` do arquivo `20260922000012` (sem o passo novo) + o smoke ⇒ esperado `FAIL` na primeira asserção do passo novo; registrar a saída no SUMMARY e conferir, só leitura, que o md5 vivo é o do pós-portão. Commitar (migration + smoke) e `git push origin main`; `origin/main..HEAD` vazio.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260922000013_p49_motor_respostas_e_producoes.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');for(const t of ['apagar_respostas_e_producoes','CREATE OR REPLACE FUNCTION public.trg_redacao_rh_only_review_fields',\"set_config('app.motor_exclusao','on',true)\",\"set_config('app.motor_exclusao','',true)\",'cited_evidence','CREATE OR REPLACE FUNCTION public.plano_exclusao_titular'])if(!c.replace(/\s+/g,' ').includes(t.replace(/\s+/g,' ')))throw new Error('falta: '+t);const dels=[...c.matchAll(/DELETE\s+FROM\s+public\.([a-z_]+)/gi)].map(m=>m[1]);const ok=new Set(['respostas_raven','respostas_bigfive','respostas_disc','respostas_formulario']);const fora=dels.filter(t=>!ok.has(t));if(fora.length)throw new Error('DELETE fora do D-62: '+fora.join(', '));for(const t of ok)if(!dels.includes(t))throw new Error('falta o DELETE de '+t);if(/PERFORM\s+public\.anonimizar_candidato\s*\(/i.test(c))throw new Error('a migration nao pode executar o motor');console.log('OK')"</automated>
    <fails_when>non-zero exit with "falta:", "DELETE fora do D-62", "falta o DELETE de", "nao pode executar o motor" or "wrapper BEGIN proibido" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && D=$(mktemp -d) && { cat supabase/migrations/20260922000013_p49_motor_respostas_e_producoes.sql supabase/tests/p45_motor_exclusao_smoke.sql; printf '\nDO $e$ BEGIN RAISE EXCEPTION %s; END $e$;\n' "'ENSAIO_OK'"; } > "$D/ensaio.sql" && node p46apply.cjs run "$D/ensaio.sql" 2>&1 | tee "$D/out.txt" | tail -3; grep -q "ENSAIO_OK" "$D/out.txt" && ! grep -q "FAIL" "$D/out.txt"</automated>
    <fails_when>the output lacks "ENSAIO_OK" or contains "FAIL"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs migrate supabase/migrations/20260922000013_p49_motor_respostas_e_producoes.sql && node p46apply.cjs run supabase/tests/p45_motor_exclusao_smoke.sql && node p46apply.cjs run supabase/tests/p46_purga_smoke.sql && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>non-zero exit; "md5 do ledger BATE" absent; "FAIL" in either smoke output; or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - Em PROD, `anonimizar_candidato` tem o passo `apagar_respostas_e_producoes` antes de `severar_fks_set_null`, com DELETE só nas 4 tabelas e a GUC em volta do UPDATE da redação; `plano_exclusao_titular` conta o mesmo; as formas do 49-14 continuam.
    - O smoke passa com uma asserção por origem nova, inclusive a execução com claims de administrador; a mutação reprova (saída no SUMMARY).
    - `p46_purga_smoke` verde; a precondição (purga em `dry_run`, md5 de partida) está registrada com os valores lidos.
  </acceptance_criteria>
  <done>Excluir um titular passa a apagar as respostas, os textos, as transcrições e as citações dele — o que o recibo sempre disse que apagava.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| titular/administrador (JWT) → EF `executar-direito-titular` → `anonimizar_candidato` | mecanismo destrutivo, transação única depois do passo de Storage |
| purga automática (cron, service) → `anonimizar_candidato` | em `dry_run` hoje; conferido antes do apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-20-01 | Information Disclosure | texto/transcrição/citação do titular sobrevivendo à exclusão | high | mitigate | passo novo por origem do recibo; asserção por coluna no smoke |
| T-49-20-02 | Tampering | DELETE além do escopo decidido | high | mitigate | verify reprova DELETE fora das 4 tabelas; pós-portão da migration |
| T-49-20-03 | Denial of Service | passo abortando depois do Storage (NOT NULL/CHECK/trigger de papel) | high | mitigate | ação por coluna conferida no catálogo pela migration; sanção `app.motor_exclusao`; smoke com claims de administrador |
| T-49-20-04 | Tampering | re-pin do md5 sem rede estrutural | medium | mitigate | rede cresce antes do re-pin; proveniência no cabeçalho; mutação reprova |
| T-49-20-05 | Elevation of Privilege | cliente ligando a GUC `app.motor_exclusao` | low | accept | `app.*` não é setável por PostgREST/RPC (mesmo pressuposto de `app.rejeicao_sancionada`); a GUC é zerada logo depois do UPDATE |
| T-49-20-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Migration `20260922000013` aplicada (ensaio antes), md5 do ledger conferido; purga em `dry_run` registrada de novo.
- `p45_motor_exclusao_smoke` ampliado e re-pinado verde; `p46_purga_smoke` verde; mutação reprova.
- `origin/main..HEAD` vazio.
</verification>

<success_criteria>
O que o recibo de exclusão diz ao titular sobre respostas, textos, transcrições e citações é o que o motor apaga —
provado coluna a coluna por um smoke que reprova quando o passo é desligado.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| passo `apagar_respostas_e_producoes` em `anonimizar_candidato` (+ chave em `'passos'`) | passo do motor | **novo** |
| contagem `apagar_respostas_e_producoes` em `plano_exclusao_titular` | dry-run | **nova** |
| GUC `app.motor_exclusao` + sanção em `trg_redacao_rh_only_review_fields()` | trigger | editado |
| sentinelas `'[removido a pedido do titular — LGPD Art. 18]'`, `'{"redigido":"anonimizacao_p49"}'` | valores gravados | **novos** |
| `supabase/migrations/20260922000013_p49_motor_respostas_e_producoes.sql` | migration | **nova** |
| `supabase/tests/p45_motor_exclusao_smoke.sql` | smoke | ampliado, re-pinado |

## Premissas sinalizadas (probe de borda)

JORN-36 saiu `unclassified` no probe e fica `unresolved` no relatório. As bordas escritas aqui vêm das Correções
14, 15, 18 e das decisões D-48 e D-62. Fatia do 49-14 original (revisão do checker, W1): a migration mantém a versão
`20260922000013` e segue aplicada DEPOIS da `…000012` (onda 5 × onda 4), então a ordem de apply continua igual à de
versão.

**Porta de mão única sem `checkpoint:decision`, de propósito:** o passo leva `rating="one-way"` para o registro, mas
a porta já foi atravessada pelo operador no portão (D-48, D-62 como exceção explícita) — re-perguntar seria o
anti-padrão «gating a decision already made». O que ainda é gate é a PRIMEIRA execução real (49-19, D-54). O apply do
código é `CREATE OR REPLACE` (D-54: sem pausa), provado antes por ensaio em requisição que aborta.
`devolutivas_candidato` é apagada por FK CASCADE no passo 3 — a frase do recibo sobre a devolutiva já era verdade
(Correção 15). Fora deste plano, §Deferred: desidentificar `analise_candidato_vaga.resumo_cv` e
`entrevista_guias.guia` (D-66 corrige só a classificação, no 49-17).

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-20-SUMMARY.md` when done
</output>
