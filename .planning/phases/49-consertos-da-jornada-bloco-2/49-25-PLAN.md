---
phase: 49-consertos-da-jornada-bloco-2
plan: 25
type: execute
wave: 3
depends_on: ["49-02", "49-24"]
files_modified:
  - supabase/functions/gerar-guia-entrevista/index.ts
  - supabase/functions/gerar-guia-entrevista/__tests__/index.test.ts
autonomous: true
requirements: [JORN-28, JORN-39]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Um bloqueio ANTERIOR ao provedor (`result.provider === 'none'` — teto de custo AI-06, injeção RF-PL-18, e qualquer bloqueio futuro da mesma classe) NUNCA é persistido como guia bem-sucedido: a linha sai com `incompleto: true` e com o `error_code` do bloqueio em `flags`."
    - "O caso MEDIDO pelo 49-24 deixa de existir: teto de custo estourado + nenhuma dimensão fraca ⇒ hoje grava guia com 0 perguntas, `flags: []` e devolve `{ok:true}`; depois, grava `incompleto: true` com `flags: ['cost_cap_exceeded']`."
    - "A condição é pelo PROVEDOR (`provider === 'none'`), não por uma lista literal de `error_code`: enumerar códigos é a forma «iteração sobre lista literal» que o CLAUDE.md §Portões descreve — o bloqueio novo nasceria fora da vigilância e o portão seguiria verde."
    - "As perguntas `origem:'manual'` continuam PRESERVADAS pelo merge (ENTREV-08): um bloqueio por custo não pode apagar edição humana."
    - "Nenhum `max_tokens` é tocado; nenhuma migration; nenhuma coluna nova. O `interview_guide` a 98,4 s / 110 s segue registrado como P1 e NÃO é agravado (este plano não acrescenta bloco ao pedido)."
    - "Deploy por `efdeploy.cjs` com `--dry-run` antes, `verify_jwt=true` preservado, versão e status RELIDOS da Management API."
  artifacts:
    - path: supabase/functions/gerar-guia-entrevista/index.ts
      provides: "bloqueio pré-provedor nunca vira guia vazio bem-sucedido"
      contains: "cost_cap_exceeded"
  key_links:
    - from: "CallAiResult.provider === 'none' (49-02, enum do 49-01)"
      to: "entrevista_guias.guia.flags / .incompleto → selo no GuiaEntrevistaPanel (49-16)"
      via: "persistFlags da EF"
      pattern: "provider\\s*===\\s*[\"']none[\"']"
  prohibitions:
    - requirement_id: JORN-39
      category: transparency
      status: resolved
      verification: automated
      statement: "MUST NOT persistir como sucesso um guia que nenhum modelo produziu (bloqueio por custo ou injeção)"
    - requirement_id: JORN-28
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT deixar um roteiro barrado por gasto indistinguível de um roteiro vazio bem-sucedido"
---

<objective>
Um guia de entrevista que nenhum modelo escreveu para de se apresentar como guia pronto.

Purpose: MEDIDO pelo teste novo do 49-24 e registrado no `WINDOWS.md` 60. Com o teto diário de custo (AI-06)
estourado, `callAi` devolve `parsed = {recommendation:'hold'}` — que **não é null**. Em `index.ts:328-332` o
`persistFlags` só considera `guide == null` ou `prompt_injection_detected`, então ele fica **vazio**; e se a vaga não
tiver dimensão fraca descoberta, `needsHumanFlag` também é `false`. A EF persiste um guia com **0 perguntas, nenhuma
flag**, e responde `{ok:true}`. Um roteiro barrado por gasto é indistinguível de um roteiro vazio bem-sucedido.

É a mesma família do JORN-28/JORN-39 que a fase inteira combate — ausência que se disfarça de valor real — e é
pré-requisito honesto do **49-16**: sem este conserto, o selo do guia encontra NULL por **duas** causas diferentes
(guia legado × guia barrado por custo) com a mesma aparência, e não há como distingui-las na tela.

Output: a EF `gerar-guia-entrevista` com o bloqueio pré-provedor sinalizado, deployada.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-02-SUMMARY.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-24-SUMMARY.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: bloqueio pré-provedor não vira guia pronto (WINDOWS 60)</name>
  <precondition>O 49-24 está em PROD (`gerar-guia-entrevista` v18, `modelo_ia` no bundle) e `_shared/ai-error-codes.ts` do 49-02 está no disco com `AI_ERROR_CODE.cost_cap_exceeded`.</precondition>
  <files>supabase/functions/gerar-guia-entrevista/index.ts, supabase/functions/gerar-guia-entrevista/__tests__/index.test.ts</files>
  <read_first>
    - `supabase/functions/gerar-guia-entrevista/index.ts` (`:300-340` — passo 7 do re-prompt, passo 8 do `persistFlags`; `:340-410` — o merge ENTREV-08 e o fallback `guide ?? {incompleto}`)
    - `supabase/functions/_shared/ai-client.ts` (`:680-770` — os quatro retornos `provider: "none"`; confirmar quais carregam `error_code` e o que vai em `parsed`)
    - `supabase/functions/_shared/ai-error-codes.ts` (`AI_ERROR_CODE.cost_cap_exceeded`, `prompt_injection_detected`)
    - `supabase/functions/gerar-guia-entrevista/__tests__/index.test.ts` (o teste do teto de custo que o 49-24 acrescentou — é ele que MEDIU o defeito; NÃO reescrever a sua asserção para passar)
  </read_first>
  <action>
**RED primeiro.** Acrescentar ao teste da EF o caso que hoje reprova: teto de custo estourado **sem** dimensão fraca
descoberta ⇒ a linha persistida deve ter `incompleto: true` e `flags` contendo `cost_cap_exceeded`, e a resposta NÃO
deve ser um sucesso silencioso. Medir e registrar a saída real do RED (reprova por asserção, não por carregamento —
um RED que trava no carregamento é `INVALID_RED`, #3770; e o `check tdd-red-evidence` não lê o TAP do Deno, então
registre o RED medido em vez de sintetizar linhas de contador, o que forjaria o artefato que o checker existe para ler).

**GREEN.** Em `:328-332`, trocar a condição por uma que pergunte pelo **provedor**, não por uma lista de códigos:
quando `result.provider === 'none'`, nenhum modelo respondeu, logo o que estiver em `parsed` não é um guia gerado —
`guide` deve ser tratado como ausente e `result.error_code` entra em `persistFlags`. Isso cobre `cost_cap_exceeded`,
`prompt_injection_detected` e qualquer bloqueio futuro da mesma classe **por construção**. Enumerar códigos é
proibido pelo `must_haves` (forma de lista literal — CLAUDE.md §Portões).

Preservar intactos: o merge ENTREV-08 (as perguntas `origem:'manual'` sobrevivem a um bloqueio por custo — conferir
por asserção, não por leitura), a proveniência do 49-24 (`provedor_ia`/`modelo_ia` NULL quando `provider === 'none'`,
que é o comportamento correto e já testado), e o `verify_jwt=true`.

Rodar `deno test --allow-all supabase/functions/gerar-guia-entrevista/`. Provar por **mutação** que o portão morde:
desfazer o ramo novo deve reprovar o teste do RED; se alguma mutação reprovar 0, dizer POR QUE (ramo inalcançável) e
dar vigilância ao que está alcançável e não tem — nunca declarar o portão aprovado. Commitar antes de montar qualquer
harness de mutação e usar `git diff --quiet` como guard: um `git checkout --` sobre trabalho não commitado já destruiu
a EF sob teste uma vez nesta fase (49-09).

**Deploy.** `node efdeploy.cjs gerar-guia-entrevista --dry-run` (registrar o fechamento), depois sem `--dry-run`;
reler versão/status/`verify_jwt` da Management API e conferir o marcador novo no bundle vivo. Commitar e
`git push origin main`; `origin/main..HEAD` vazio.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/gerar-guia-entrevista/ && grep -qE "provider\s*===\s*[\"']none[\"']" supabase/functions/gerar-guia-entrevista/index.ts</automated>
    <fails_when>non-zero exit, "FAILED" in the Deno output, or the EF source still keys the block on a literal error_code list instead of the provider</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npm run -s lint 2>&1 | grep -cE "error TS" | awk '{if ($1+0 > 90) {print "tsc ceiling exceeded: " $1; exit 1} else {print "tsc " $1 " <= 90"}}' && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>tsc error count exceeds the D-53 ceiling of 90, or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - Teto de custo estourado ⇒ linha `incompleto: true` com `cost_cap_exceeded` em `flags`; nunca `{ok:true}` silencioso.
    - A condição é pelo provedor, não por lista literal de códigos.
    - Perguntas `origem:'manual'` preservadas sob bloqueio, por asserção.
    - EF deployada com `verify_jwt=true`; versão relida de PROD; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>Um guia que nenhum modelo escreveu diz que não foi escrito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF → `entrevista_guias` (service_role) | a linha passa a declarar quando nenhum modelo a produziu |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-25-01 | Repudiation | roteiro barrado por custo persistido como sucesso vazio | medium | mitigate | `provider === 'none'` ⇒ `incompleto` + `error_code` em `flags` |
| T-49-25-02 | Tampering | bloqueio apagar pergunta manual | medium | mitigate | merge ENTREV-08 preservado, conferido por asserção |
| T-49-25-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `deno test` da EF verde; mutação prova que o ramo novo morde (ou a razão medida de não morder está escrita); `tsc` ≤ 90; EF deployada com `verify_jwt=true`; versão relida de PROD; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
Nenhum guia de entrevista que a IA não escreveu se apresenta ao RH como guia pronto — e o 49-16 encontra uma causa só por trás de um selo vazio.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `persistFlags` por provedor em `gerar-guia-entrevista` | EF (lógica) | editado |
| EF `gerar-guia-entrevista` | deploy | nova versão |

## Premissas sinalizadas

Guias já persistidos vazios por teto de custo **não** são reescritos retroativamente (D-30, mesma regra do 49-24).
Eles permanecem indistinguíveis dos legados; a distinção vale de agora em diante. Se a contagem deles em PROD for
não-trivial, é matéria do 49-12 (correções retroativas, com checkpoint) — medir e registrar, não consertar aqui.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-25-SUMMARY.md` when done
</output>
