---
phase: 49-consertos-da-jornada-bloco-2
plan: 11
type: execute
wave: 3
depends_on: ["49-01", "49-02", "49-08"]
files_modified:
  - supabase/functions/analise-candidato-individual/index.ts
  - supabase/functions/analise-candidato-individual/__tests__/index.test.ts
autonomous: true
requirements: [JORN-28, JORN-39]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-28: a análise da triagem (`analise_candidato_vaga`) grava `provedor_ia` e `modelo_ia` REAIS (do `CallAiResult` do 49-02) no upsert de sucesso; o upsert de `pendente` (reprocessamento) NÃO toca a proveniência — ela sempre descreve o conteúdo que está na linha."
    - "As duas escritas da EF que hoje descartam o `error` (a marca `pendente` e a linha `falhou` do catch — varredura C6 #6, `:301`, `:602`) passam a destruturá-lo e registrá-lo no log redigido (só código); a marca `pendente` continua não interrompendo a análise (decisão escrita no próprio arquivo), mas deixa de ser silenciosa."
    - "As 7 EFs que embarcam `_shared/ai-client.ts` ficam com o bundle do contrato novo (JORN-28/JORN-39) em PROD: este plano deploya `analise-candidato-individual` (`verify_jwt=false`) e `gerar-devolutiva-bigfive` (`verify_jwt=false`, IA desligada — só sincroniza o `_shared`); as outras 5 são deployadas pelos planos 49-03 (não embarca), 49-08, 49-09 (`avaliar-redacao-cultural`), 49-23 (`avaliar-redacao`), 49-10 (`avaliar-transcricao-entrevista`) e 49-24 (`gerar-guia-entrevista`)."
    - "D-37: a análise da triagem continua sobrescrita ao reprocessar — o append por etapa dela é §Deferred."
    - "Nenhum `max_tokens` é alterado aqui (o `cv_job_match` a 79 % do teto fica registrado como risco moderado, varredura C9)."
  artifacts:
    - path: supabase/functions/analise-candidato-individual/index.ts
      provides: "proveniência real na análise da triagem; escritas sem erro descartado"
      contains: "modelo_ia"
  key_links:
    - from: "CallAiResult.model / .provider (49-02)"
      to: "analise_candidato_vaga.provedor_ia / modelo_ia"
      via: "upsert final de sucesso"
      pattern: "modelo_ia:\\s*result\\.model"
  prohibitions:
    - requirement_id: JORN-28
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT deixar a análise da triagem sem dizer qual modelo a produziu quando houve fallback"
---

<objective>
A análise da triagem passa a dizer qual modelo a escreveu, e as 7 EFs de IA passam a rodar o contrato novo do
`callAi` em PROD.

Purpose: D-28 lista `analise_candidato_vaga` entre as tabelas de resultado sem proveniência; o `cv_job_match`
foi o `call_type` com mais fallbacks em 05–06/09 (timeout ×6, truncamento ×2, Zod ×5 — RESEARCH Correção 2).
Os planos 49-08/09/10/23/24 deployam as EFs que tocam; estas duas sobram (RESEARCH «Plano / ondas», 49-09 original).

Output: a EF da análise com proveniência e escritas conferidas, e os dois deploys.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-02-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer) [BLOCKING]: a análise da triagem grava o modelo real — testada e deployada</name>
  <precondition>O 49-01 está em PROD (`analise_candidato_vaga.provedor_ia`/`modelo_ia` existem) e o 49-02 está no disco (`CallAiResult.model`).</precondition>
  <files>supabase/functions/analise-candidato-individual/index.ts, supabase/functions/analise-candidato-individual/__tests__/index.test.ts</files>
  <read_first>
    - `supabase/functions/analise-candidato-individual/index.ts` (`:280-310` a marca `pendente`; `:560-612` o upsert final com `upsertErr` checado e o catch que grava `falhou`)
    - `supabase/functions/analise-candidato-individual/__tests__/index.test.ts` (os mocks do handler)
    - `efdeploy.cjs` (`VERIFY_JWT['analise-candidato-individual'] = false` — servidor → servidor)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-VARREDURA-KICKOFF.md` C5 #1 e C6 #6
  </read_first>
  <behavior>
    - Sucesso ⇒ o objeto do upsert final contém `provedor_ia` = `result.provider` (quando `anthropic`/`openai`) e `modelo_ia` = `result.model`.
    - A marca `pendente` não contém `provedor_ia`/`modelo_ia`.
    - A marca `pendente` e o upsert `falhou` que devolvem `error` ⇒ o código do erro aparece no log redigido; a resposta segue o comportamento de hoje (a análise não é interrompida pela marca).
  </behavior>
  <action>
D-50: re-rodar o `grep` da C5 (`model_version|modelo_ia|provider:` nas EFs) e registrar o delta. No upsert final de sucesso, acrescentar `provedor_ia` (NULL se o provider não for `anthropic`/`openai`) e `modelo_ia: result.model`, com comentário D-28. A marca `pendente` e o upsert `falhou` passam a destruturar `{ error }` e logar `error.code` pelo mesmo `console.error` redigido (sem texto, sem nome) — a marca continua sem interromper. Testes do `<behavior>`; rodar `deno test --allow-all supabase/functions/analise-candidato-individual/`.

Deploy: conferir as duas colunas no catálogo; `node efdeploy.cjs analise-candidato-individual --dry-run` (fechamento com `_shared/ai-client.ts`, `audit-logger.ts`, `ai-error-codes.ts` e `analise-schemas.ts` — que agora importa `comparativo-config.ts` se o 49-08 já estiver no disco; registrar a lista no SUMMARY); `node efdeploy.cjs analise-candidato-individual` (`verify_jwt=false`); marcador `modelo_ia` no bundle.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/analise-candidato-individual/</automated>
    <fails_when>non-zero exit, or "FAILED" in the output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs analise-candidato-individual --dry-run | tee /dev/stderr | grep -q "functions/_shared/ai-error-codes.ts" && node efdeploy.cjs analise-candidato-individual | tee /dev/stderr | grep -q "efdeploy: OK .*verify_jwt=false" && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/analise-candidato-individual/body" | grep -a -c "modelo_ia"</automated>
    <fails_when>non-zero exit; closure lacks functions/_shared/ai-error-codes.ts (the 49-02 contract is not in the bundle); efdeploy output lacks "efdeploy: OK" with "verify_jwt=false"; or the grep count printed is 0</fails_when>
  </verify>
  <acceptance_criteria>
    - O upsert final grava `provedor_ia`/`modelo_ia`; a marca `pendente` não os toca; as duas escritas antes silenciosas logam o erro (testes).
    - EF deployada com `verify_jwt=false`; o fechamento inclui os `_shared` do 49-02; marcador no bundle.
  </acceptance_criteria>
  <done>A próxima análise de triagem diz, na própria linha, qual modelo a escreveu.</done>
</task>

<task type="auto">
  <name>Task 2: `gerar-devolutiva-bigfive` com o `_shared` do contrato novo — redeploy sem mudança de comportamento</name>
  <files>supabase/functions/gerar-devolutiva-bigfive/index.ts (sem edição — só o deploy do fechamento atual)</files>
  <read_first>
    - `supabase/functions/gerar-devolutiva-bigfive/index.ts` (`:40-60` imports de `_shared/ai-client.ts`; o `PERSONALIZACAO_IA_ATIVA` que mantém a IA desligada)
    - `.planning/phases/48-consertos-da-jornada-bloco-1/deferred-items.md` (itens do 48-19: fiação do `Deno.serve` com IA desligada; `modelo_ia` literal ao religar)
    - `efdeploy.cjs` (`VERIFY_JWT['gerar-devolutiva-bigfive'] = false`)
  </read_first>
  <action>
Nenhuma edição de código na devolutiva (a IA está desligada e o `modelo_ia` literal é item aberto da 48-19, fora desta fase). Rodar `deno test --allow-all supabase/functions/gerar-devolutiva-bigfive/` para provar que o contrato novo do `callAi` não quebra o handler; `node efdeploy.cjs gerar-devolutiva-bigfive --dry-run` e conferir À MÃO que o fechamento é o da versão viva (v27) mais os arquivos `_shared` novos que o `ai-client.ts` passou a importar (`ai-error-codes.ts`) — registrar no SUMMARY; `node efdeploy.cjs gerar-devolutiva-bigfive` (`verify_jwt=false`). Conferir, só leitura, que a versão da EF subiu. Commitar e `git push origin main`; `origin/main..HEAD` vazio. Registrar no SUMMARY a tabela das 7 EFs que embarcam `ai-client.ts` com a versão viva de cada uma depois dos planos 49-08..49-11 (as que ainda não tiverem subido nesta onda ficam anotadas com o plano dono).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/gerar-devolutiva-bigfive/</automated>
    <fails_when>non-zero exit, or "FAILED" in the output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs gerar-devolutiva-bigfive --dry-run | tee /dev/stderr | grep -q "functions/_shared/ai-error-codes.ts" && node efdeploy.cjs gerar-devolutiva-bigfive | tee /dev/stderr | grep -q "efdeploy: OK .*verify_jwt=false" && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>non-zero exit; closure lacks functions/_shared/ai-error-codes.ts; efdeploy output lacks "efdeploy: OK" with "verify_jwt=false"; or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - Testes da devolutiva verdes com o contrato novo; EF redeployada com `verify_jwt=false` e o `_shared` novo no fechamento.
    - A tabela das 7 EFs com a versão viva está no SUMMARY; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>Nenhuma EF de IA fica rodando, em PROD, o cliente antigo que escondia o fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| banco (`net.http_post` com Bearer) → EF `analise-candidato-individual` | servidor → servidor; `verify_jwt=false` |
| EF → `analise_candidato_vaga` (service_role) | resultado de IA sobre o titular |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-11-01 | Repudiation | análise de fallback sem proveniência | medium | mitigate | `provedor_ia`/`modelo_ia` reais no upsert final |
| T-49-11-02 | Repudiation | escrita falha silenciosa na marca/na falha | low | mitigate | erro destruturado e logado (só código) |
| T-49-11-03 | Denial of Service | redeploy com `verify_jwt` trocado | medium | mitigate | tabela `VERIFY_JWT` do `efdeploy.cjs`; saída conferida |
| T-49-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `deno test` das duas EFs verdes; duas EFs deployadas com `verify_jwt=false`; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
As 7 EFs de IA de PROD rodam o `callAi` que registra a causa e o modelo real, e a análise da triagem guarda o modelo na linha.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `analise_candidato_vaga.provedor_ia` / `.modelo_ia` (escrita) | colunas | escrita nova |
| EF `analise-candidato-individual` | deploy | nova versão |
| EF `gerar-devolutiva-bigfive` | deploy | nova versão (só `_shared`) |

## Premissas sinalizadas (probe de borda)

JORN-28 e JORN-39 saíram, no probe, como `unclassified` e com as sondas de idempotência/concorrência (JORN-39);
as sondas são resolvidas no 49-02 (dono do comportamento). Aqui só se registra que a devolutiva, com a IA
desligada, não produz linha `none` nem de fallback — o redeploy é de sincronia de bundle.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-11-SUMMARY.md` when done
</output>
