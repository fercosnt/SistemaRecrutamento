---
phase: 49-consertos-da-jornada-bloco-2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/candidaturaEncerrada.ts
  - supabase/functions/_shared/__tests__/candidaturaEncerrada.test.ts
  - src/lib/candidatura/candidaturaEncerrada.ts
  - supabase/functions/notificar-candidato/index.ts
  - supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts
autonomous: true
requirements: [JORN-25]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Existe UMA implementação TS do predicado canônico, em `supabase/functions/_shared/candidaturaEncerrada.ts` (zero imports), com a MESMA allowlist da função SQL `public.candidatura_encerrada` (etapa em aprovado/rejeitado OU status em rejeitado/finalizado, null-safe); `src/lib/candidatura/candidaturaEncerrada.ts` passa a REEXPORTAR dela. Motivo, registrado no arquivo: D-21 manda reusar e não recriar; uma cópia Deno separada seria a segunda cópia TS que envelhece sozinha, e a direção de import `src/` → `supabase/functions/_shared/` já é a usada em produção (`exportacaoService.ts:61`)."
    - "A tabela-verdade do predicado é testada nos dois runtimes sobre a MESMA implementação: o vitest existente (`src/lib/candidatura/__tests__/candidaturaEncerrada.test.ts`, via reexport) e um teste Deno com os mesmos casos — e a SQL segue provada pelo `p48_candidatura_encerrada_smoke.sql`."
    - "A EF `notificar-candidato` recusa o evento `avanco` para candidatura encerrada pelo predicado canônico, ANTES do claim do ledger: devolve `{ ok: true, skipped: 'encerrada' }`, não chama o Resend e não grava/atualiza `notificacoes_enviadas` (D-35, segunda camada da trava do banco do plano 49-06)."
    - "BORDA explícita: `avanco` para `inscricao/rejeitado` (knockout) e para `triagem/finalizado` (legado) é pulado; `avanco` para candidatura em andamento continua sendo enviado (a guarda não morde demais); a guarda do knockout em `confirmacao` continua igual."
    - "A EF foi deployada por `node efdeploy.cjs notificar-candidato` com `--dry-run` antes (o fechamento de imports passa a incluir `_shared/candidaturaEncerrada.ts` e foi conferido à mão, porque o cabeçalho do script afirma uma checagem que não existe — D-52), com `verify_jwt=false` preservado, e o marcador está no bundle vivo."
    - "Nenhum e-mail a candidato é disparado por este plano: os testes usam mocks e o deploy não reenvia nada (D-54)."
  artifacts:
    - path: supabase/functions/_shared/candidaturaEncerrada.ts
      provides: "a única implementação TS do predicado canônico (Deno + front)"
      contains: "export function candidaturaEncerrada"
    - path: src/lib/candidatura/candidaturaEncerrada.ts
      provides: "reexport — os imports `@/lib/candidatura/candidaturaEncerrada` seguem válidos"
      contains: "supabase/functions/_shared/candidaturaEncerrada"
    - path: supabase/functions/notificar-candidato/index.ts
      provides: "guarda de `avanco` para encerrada"
      contains: "encerrada"
  key_links:
    - from: "supabase/functions/_shared/candidaturaEncerrada.ts"
      to: "notificar-candidato (este plano), comparativo-candidatos (49-08), telas do RH (TriagemTable, KanbanBoard, HubCandidatoRH, CandidatosRHPage)"
      via: "import direto no Deno; reexport de `@/lib/candidatura/candidaturaEncerrada` no front"
      pattern: "candidaturaEncerrada\\("
    - from: "public.candidatura_encerrada (SQL, vivo)"
      to: "supabase/functions/_shared/candidaturaEncerrada.ts"
      via: "mesma allowlist; SQL decide, TS esconde ação / recusa e-mail"
      pattern: "rejeitado.*finalizado"
  prohibitions:
    - requirement_id: JORN-25
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT criar critério novo de «encerrada» — retirada a pedido não é encerrada por este predicado, por desenho (D-34, D-21)"
    - requirement_id: JORN-25
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT enviar, reenviar ou reprocessar e-mail a candidato real para testar a guarda (D-54)"
---

<objective>
Fechar o e-mail de avanço para candidatura encerrada (segunda camada do D-35) e dar ao lado Deno o
predicado canônico sem criar uma segunda cópia dele.

Purpose: medido no kickoff — knockout (`inscricao`/`rejeitado`) → «Avançar» → `avaliacao_assincrona` é
aceito pelo banco, grava histórico e o `trg_notif_transicao` despacha `avanco`; a guarda do knockout da EF
só cobre `confirmacao` (`notificar-candidato/index.ts:288-298`). A trava do banco é do plano 49-06; esta é a
defesa na EF, que é a única a ver o estado depois do COMMIT. E a EF do comparativo (49-08) precisa do mesmo
predicado no Deno — não existe nenhum hoje em `supabase/functions/` (PATTERNS «No Analog Found»).

Output: `_shared/candidaturaEncerrada.ts` (fonte única), o reexport no front, a guarda na EF e o deploy.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-VARREDURA-KICKOFF.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): o predicado numa fonte só — `_shared` sem imports, reexport no front, tabela-verdade nos dois runtimes</name>
  <files>supabase/functions/_shared/candidaturaEncerrada.ts, supabase/functions/_shared/__tests__/candidaturaEncerrada.test.ts, src/lib/candidatura/candidaturaEncerrada.ts</files>
  <read_first>
    - `src/lib/candidatura/candidaturaEncerrada.ts` (inteiro — docblock, `ETAPAS_TERMINAIS`, `STATUS_TERMINAIS`, `candidaturaEncerrada`)
    - `src/lib/candidatura/__tests__/candidaturaEncerrada.test.ts` (a tabela-verdade `it.each`)
    - `supabase/migrations/20260921000001_p48_candidatura_encerrada.sql` (a função SQL espelhada) e o vivo: `node p46apply.cjs sql "SET TRANSACTION READ ONLY; select pg_get_functiondef('public.candidatura_encerrada(public.etapa_processo, public.status_candidatura)'::regprocedure)"`
    - `supabase/functions/_shared/email-config.ts` (`:12-17` — contrato ZERO IMPORTS)
    - `src/features/privacidade/services/exportacaoService.ts` (`:50-61` — import relativo de `_shared` pelo front, com o comentário do porquê)
  </read_first>
  <action>
Passo 1 — D-50: re-rodar os padrões P1/P2 da C1 do `49-VARREDURA-KICKOFF.md` (`grep` de escritores de etapa/status e de quem usa `candidaturaEncerrada(` no front) e `grep -rn "candidatura_encerrada\|candidaturaEncerrada" supabase/functions --include='*.ts'` (o kickoff mediu: nenhum espelho TS no Deno; os `notificar-*` citam o nome do evento). Registrar o delta no SUMMARY. Conferir o corpo VIVO da função SQL: se a allowlist mudou, parar e reportar (D-49).

Passo 2 — criar `supabase/functions/_shared/candidaturaEncerrada.ts` SEM IMPORTS: mover para cá, idênticos, `ETAPAS_TERMINAIS`, `STATUS_TERMINAIS` e `candidaturaEncerrada(etapa, status)`, com o docblock do arquivo do front (origem D-11/D-21, «o servidor é quem decide», retirada a pedido fora por desenho) acrescido de: por que mora em `_shared` (D-21: uma cópia TS só; o Deno e o front importam a mesma), e o contrato ZERO IMPORTS (o front importa por caminho relativo).

Passo 3 — `src/lib/candidatura/candidaturaEncerrada.ts` vira reexport dos três nomes a partir de `../../../supabase/functions/_shared/candidaturaEncerrada`, mantendo um docblock curto que aponte a fonte e o porquê. Nenhum chamador do front muda de import.

Passo 4 — `supabase/functions/_shared/__tests__/candidaturaEncerrada.test.ts` (Deno, `https://deno.land/std@0.224.0/assert/mod.ts`): os mesmos casos da tabela-verdade do vitest (encerradas, em andamento, `aprovado_proxima`, etapa terminal sem status, valor desconhecido). Rodar os dois lados e o portão `tsc`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/_shared/__tests__/candidaturaEncerrada.test.ts && npx vitest run src/lib/candidatura</automated>
    <fails_when>non-zero exit, "FAILED" in the Deno output, or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/functions/_shared/candidaturaEncerrada.ts','utf8');if(/^\s*import\s/m.test(s))throw new Error('_shared/candidaturaEncerrada.ts tem import');if(!/export function candidaturaEncerrada/.test(s))throw new Error('funcao ausente no _shared');const f=fs.readFileSync('src/lib/candidatura/candidaturaEncerrada.ts','utf8');if(!/supabase\/functions\/_shared\/candidaturaEncerrada/.test(f))throw new Error('o front nao reexporta do _shared');if(/new Set\(\[/.test(f))throw new Error('o front ainda tem uma copia propria da allowlist');console.log('OK: uma fonte so')" && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; }</automated>
    <fails_when>non-zero exit with "tem import", "funcao ausente", "nao reexporta" or "copia propria" in stderr; lint exit code other than 0/2 or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `_shared/candidaturaEncerrada.ts` existe, sem imports, com a allowlist idêntica à da SQL viva.
    - O arquivo do front só reexporta (nenhuma `new Set([` própria).
    - Os testes Deno e vitest do predicado passam; `tsc` ≤ 90.
  </acceptance_criteria>
  <done>Front e Edge Functions passam a perguntar «acabou?» à mesma função TS — que diz o mesmo que a SQL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 [BLOCKING]: a guarda de `avanco` na EF — testada, deployada e no bundle vivo</name>
  <files>supabase/functions/notificar-candidato/index.ts, supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts</files>
  <read_first>
    - `supabase/functions/notificar-candidato/index.ts` (`:240-300` — o claim, a leitura por allowlist `:274-277`, o survivor-guard `:283-298`)
    - `supabase/functions/notificar-candidato/helpers.ts` (`:142-160`, `:249` — `logSeguro` e a allowlist de chaves; conferir se `skipped` tem allowlist de VALORES)
    - `supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts` (`:539-556` — o teste CR-02 do knockout: `makeRetryMockSupabase`, `makeFetchMock`, asserções `fetchMock.calls.length === 0` e `supa.upserts.length === 0`)
    - `efdeploy.cjs` (tabela `VERIFY_JWT`: `notificar-candidato: false`; a função `closure`)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md` §17
  </read_first>
  <behavior>
    - `avanco` + `{ etapa_atual: 'inscricao', status: 'rejeitado' }` ⇒ 200 `skipped: 'encerrada'`, 0 chamadas ao Resend, 0 upserts no ledger.
    - `avanco` + `{ etapa_atual: 'triagem', status: 'finalizado' }` ⇒ 200 `skipped: 'encerrada'`, 0 chamadas, 0 upserts.
    - `avanco` + `{ etapa_atual: 'avaliacao_assincrona', status: 'em_analise' }` ⇒ envia (1 chamada ao Resend) — a guarda não morde demais.
    - `confirmacao` + knockout ⇒ continua `skipped: 'knockout'` (regressão).
  </behavior>
  <action>
Em `notificar-candidato/index.ts`, importar `candidaturaEncerrada` de `../_shared/candidaturaEncerrada.ts` e acrescentar, logo depois do bloco 3a do knockout e ANTES de qualquer claim/escrita no ledger, o bloco irmão: se `evento === "avanco"` e `candidaturaEncerrada(candidatura.etapa_atual, candidatura.status)`, logar com `logSeguro({ evento, candidatura_id, skipped: "encerrada" })` e devolver `jsonResponse({ ok: true, skipped: "encerrada" }, 200)`. Comentário: D-35, segunda camada da trava do `avancar_etapa` (plano 49-06); a EF vê o estado pós-COMMIT; «encerrada» é o predicado canônico, sem critério novo; retirada a pedido não é encerrada. Se o claim do ledger acontecer ANTES do bloco 3a no código vivo, mover a guarda nova para antes dele e registrar no SUMMARY. Se `logSeguro` tiver allowlist de valores para `skipped`, acrescentar `"encerrada"`.

Testes do `<behavior>` no molde de `:539-556`. Rodar `deno test --allow-all supabase/functions/notificar-candidato/`.

Deploy (D-52): `node efdeploy.cjs notificar-candidato --dry-run` — conferir À MÃO que a lista de arquivos inclui `functions/_shared/candidaturaEncerrada.ts` e nenhum arquivo a menos que a versão viva (anotar a lista no SUMMARY); depois `node efdeploy.cjs notificar-candidato` e conferir `verify_jwt=false` e a versão nova na saída; conferir o marcador no bundle vivo. Commitar e publicar (`git push origin main`; `origin/main..HEAD` vazio).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/notificar-candidato/</automated>
    <fails_when>non-zero exit, or "FAILED" in the output</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs notificar-candidato --dry-run | tee /dev/stderr | grep -q "functions/_shared/candidaturaEncerrada.ts"</automated>
    <fails_when>non-zero exit (the closure list printed by the dry-run does not contain functions/_shared/candidaturaEncerrada.ts)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs notificar-candidato | tee /dev/stderr | grep -q "efdeploy: OK .*verify_jwt=false" && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/notificar-candidato/body" | grep -a -c "candidaturaEncerrada"</automated>
    <fails_when>non-zero exit; efdeploy output lacks "efdeploy: OK" with "verify_jwt=false"; or the grep count printed is 0 (the guard did not reach the live bundle)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && echo "origin/main em dia"</automated>
    <fails_when>non-zero exit (git log origin/main..HEAD printed at least one commit)</fails_when>
  </verify>
  <acceptance_criteria>
    - Os 4 casos do `<behavior>` têm teste e passam; a guarda está antes de qualquer escrita no ledger.
    - O dry-run lista `_shared/candidaturaEncerrada.ts`; a lista está no SUMMARY.
    - A EF está deployada com `verify_jwt=false` e o bundle vivo contém `candidaturaEncerrada`.
    - `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>Mesmo que um avanço de candidatura encerrada passasse pelo banco, o e-mail de avanço não sairia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| banco (`net.http_post` com Bearer do Vault) → EF `notificar-candidato` | a EF decide se envia; `verify_jwt=false`, autentica pelo segredo |
| EF → Resend (e-mail ao candidato) | efeito externo irreversível |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-03-01 | Information Disclosure | e-mail de «avanço» a quem já foi eliminado (sinal falso ao titular) | high | mitigate | guarda `avanco` + predicado canônico antes do claim; testes com 0 chamadas ao Resend |
| T-49-03-02 | Tampering | segunda cópia TS do predicado divergindo da SQL | medium | mitigate | fonte única em `_shared` + reexport; tabela-verdade nos dois runtimes |
| T-49-03-03 | Denial of Service | deploy com arquivo `_shared` a menos (falha só na primeira invocação) | medium | mitigate | `--dry-run` com a lista conferida à mão e registrada |
| T-49-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Predicado numa fonte só; testes Deno e vitest verdes; `tsc` ≤ 90.
- `deno test supabase/functions/notificar-candidato/` verde; EF deployada com `verify_jwt=false`; marcador no bundle; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
O e-mail de avanço respeita o mesmo critério de «encerrada» que o banco e as telas, e o Deno ganhou esse critério sem uma segunda cópia para envelhecer.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `supabase/functions/_shared/candidaturaEncerrada.ts` (`ETAPAS_TERMINAIS`, `STATUS_TERMINAIS`, `candidaturaEncerrada`) | módulo TS sem imports | **novo** (movido do front) |
| `supabase/functions/_shared/__tests__/candidaturaEncerrada.test.ts` | teste Deno | **novo** |
| `src/lib/candidatura/candidaturaEncerrada.ts` | reexport | editado |
| `skipped: "encerrada"` (evento `avanco`) | resposta da EF `notificar-candidato` | **novo** |
| EF `notificar-candidato` | deploy | nova versão |

## Premissas sinalizadas (probe de borda)

JORN-25 saiu `unclassified` no probe (texto pt-BR) e fica `unresolved` no relatório; as bordas deste plano
(knockout, `finalizado` legado, em andamento) foram escritas a partir do CONTEXT D-35 e da varredura C1.

Decisão de desenho registrada (pedida na orientação do plano): o lado Deno não pergunta ao banco
(`rpc('candidatura_encerrada')`) — seria uma ida ao banco por e-mail e, no comparativo, por candidatura —
e também não ganha cópia própria: a implementação TS passa a ser única e compartilhada, e a paridade com a
SQL continua provada pelo smoke `p48_candidatura_encerrada_smoke.sql` (tabela-verdade lida do `pg_enum`).

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-03-SUMMARY.md` when done
</output>
