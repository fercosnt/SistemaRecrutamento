---
phase: 49-consertos-da-jornada-bloco-2
plan: 21
type: execute
wave: 6
depends_on: ["49-14", "49-20"]
files_modified:
  - docs/compliance/sql/gen-recibo-exclusao.cjs
  - docs/compliance/recibo-exclusao.json
  - supabase/functions/_shared/reciboExclusao.ts
  - src/features/privacidade/constants/reciboExclusao.generated.ts
  - docs/compliance/pii-inventory.yaml
  - docs/compliance/pii-inventory.md
  - docs/compliance/__tests__/genReciboExclusao.test.ts
autonomous: true
requirements: [JORN-36]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "O recibo e o inventário passam a descrever o motor como ele é depois dos planos 49-14 e 49-20: no gerador, `ai_call_logs.user_prompt_template` sai de `conteudo_do_produto` e entra como origem de `dados_enviados_a_analise_automatica` (D-61), cobrindo no texto também o conteúdo das análises comparativas que citavam o titular (D-63); `revisao_resultado` recebe o tratamento da justificativa (D-60); o item `respostas_e_producoes` aponta o passo novo `apagar_respostas_e_producoes` (D-48)."
    - "No `pii-inventory.yaml`, `user_prompt_template` passa de «preservar» para «apagar» (R5) com a nota medida (D-61), a nota de `raw_response` é corrigida (ela guarda a SAÍDA), o «⚠ não a toca» de `revisao_resultado` sai, e cada coluna/tabela que o passo novo apaga tem classificação coerente com a ação (nada que o motor apaga fica como «preservar»)."
    - "Artefatos regenerados pelos geradores (nunca à mão) e os quatro `check:*` verdes; os testes de `genReciboExclusao.test.ts` que pinavam o mapeamento antigo mudam DE PROPÓSITO, com comentário de proveniência."
    - "`executar-direito-titular` redeployada com `verify_jwt=true` e o bundle vivo contém o `passo_motor` novo — senão o recibo novo não chega ao titular real; o espelho do front (`reciboExclusao.generated.ts`) publicado (`origin/main..HEAD` vazio) e dentro do teto de `tsc` (D-53, ≤ 90)."
    - "Isto acontece ANTES de qualquer execução real do motor novo (49-19): a partir deste plano o recibo não afirma apagamento que o motor não faz, nem esconde apagamento que ele faz."
    - "Nenhum texto novo do recibo contém o endereço literal do canal de privacidade nem «teste psicológico» (D-07, D-58)."
  artifacts:
    - path: docs/compliance/sql/gen-recibo-exclusao.cjs
      provides: "recibo alinhado ao motor"
      contains: "user_prompt_template"
    - path: docs/compliance/pii-inventory.yaml
      provides: "user_prompt_template reclassificado para apagar (R5)"
      contains: "user_prompt_template"
  key_links:
    - from: "gen-recibo-exclusao.cjs (origens de respostas_e_producoes e dados_enviados_a_analise_automatica)"
      to: "anonimizar_candidato (passo apagar_respostas_e_producoes / severar_fks_set_null)"
      via: "cada origem prometida tem ação no motor (49-14, 49-20) e asserção no smoke"
      pattern: "apagar_respostas_e_producoes"
    - from: "gen-recibo-exclusao.cjs"
      to: "supabase/functions/_shared/reciboExclusao.ts → executar-direito-titular"
      via: "gerador + redeploy"
      pattern: "apagar_respostas_e_producoes"
  prohibitions:
    - requirement_id: JORN-36
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT deixar o recibo afirmar ao titular um apagamento que o motor não faz"
    - requirement_id: JORN-36
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT editar à mão artefato gerado — o recibo JSON e os dois espelhos TS saem só do gerador"
    - requirement_id: JORN-36
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT introduzir ocorrência literal nova do endereço do canal de privacidade nem a expressão «teste psicológico» em texto novo (D-07, D-58 — a única exceção é o disclaimer negado da devolutiva, já decidido)"
---

<objective>
O recibo de exclusão e o inventário de PII passam a descrever o motor como ele é — e o recibo novo chega ao titular
real pela EF que o embarca.

Purpose: JORN-36/D-48, D-60, D-61, D-63 — o lado «promessa» das duas fatias de motor (49-14, 49-20). Terceira de três
fatias sequenciais do 49-14 original; vem ANTES do 49-17 porque os dois escrevem o mesmo gerador do recibo, o
inventário e os dois espelhos TS (RESEARCH «dono único») e antes da primeira execução real (49-19).

Output: gerador, recibo JSON, os dois espelhos TS e o inventário alinhados e regenerados; `executar-direito-titular`
redeployada; o espelho do front publicado.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-14-SUMMARY.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-20-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): o recibo e o inventário descrevem o motor como ele é — gerador → artefatos → EF no ar → espelho do front publicado</name>
  <precondition>Os planos 49-14 e 49-20 estão em PROD: `pg_get_functiondef('public.anonimizar_candidato(uuid,boolean)'::regprocedure)` contém `apagar_respostas_e_producoes`, `revisao_resultado` e `comparative_ranking` — se faltar qualquer um, PARAR: o recibo prometeria o que o motor ainda não faz.</precondition>
  <files>docs/compliance/sql/gen-recibo-exclusao.cjs, docs/compliance/recibo-exclusao.json, supabase/functions/_shared/reciboExclusao.ts, src/features/privacidade/constants/reciboExclusao.generated.ts, docs/compliance/pii-inventory.yaml, docs/compliance/pii-inventory.md, docs/compliance/__tests__/genReciboExclusao.test.ts</files>
  <read_first>
    - a tabela «origem → ação → asserção» do 49-14-SUMMARY e o delta do 49-20-SUMMARY (o que o motor faz, coluna a coluna)
    - `docs/compliance/sql/gen-recibo-exclusao.cjs` (`:293-317` `respostas_e_producoes` e o `passo_motor`; `:344-356` `dados_enviados_a_analise_automatica`; `:528-550` `anotacoes_da_equipe`; `:620-661` o mapa de razões, `:650` a linha de `conteudo_do_produto`)
    - `docs/compliance/pii-inventory.yaml` (`:163`, `:183` `revisao_resultado`; `:325-335` `ai_call_logs`)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md` §«Portão» Item 4 (o texto proposto para `user_prompt_template` e `raw_response`)
    - `docs/compliance/__tests__/genReciboExclusao.test.ts` e `docs/compliance/__tests__/portoesInvocados.test.ts`
    - `efdeploy.cjs` (`executar-direito-titular: true`)
  </read_first>
  <action>
Gerador do recibo: o item `respostas_e_producoes` passa a apontar `passo_motor: 'apagar_respostas_e_producoes'` e mantém as 14 origens (agora verdadeiras); `dados_enviados_a_analise_automatica` ganha a origem `ai_call_logs.user_prompt_template` e cobre, no texto, também o conteúdo das análises comparativas que citavam o titular (D-63), sem citar nome de tabela; a linha de `conteudo_do_produto` do mapa perde `user_prompt_template` (D-61); `revisao_resultado` (corrente e arquivo) sai de `anotacoes_da_equipe` e recebe o tratamento da justificativa da decisão (preservada desidentificada, D-60). Inventário: `ai_call_logs.user_prompt_template` → `{ classificacao: apagar, regra: R5, tipo: text, nota: <o texto medido do RESEARCH §Item 4> }`; a nota de `raw_response` corrigida (saída do modelo; o input é `user_prompt_template`); o «⚠ anonimizar_candidato não a toca» de `revisao_resultado` removido nas duas tabelas; e cada coluna/tabela que o passo novo apaga com classificação coerente com a ação (sem classificar como «preservar» o que o motor apaga). Nenhum texto novo com o endereço literal do canal de privacidade nem «teste psicológico».

Regenerar pelos geradores (nunca à mão): `node docs/compliance/sql/gen-recibo-exclusao.cjs` (recibo JSON + os dois espelhos TS) e `node docs/compliance/sql/gen-pii-md.cjs`; atualizar DE PROPÓSITO os testes de `genReciboExclusao.test.ts` que pinavam o mapeamento antigo (comentário de proveniência). Rodar os quatro `check:*`, `npx vitest run docs/compliance src/features/privacidade src/__tests__/guards` e o portão `tsc` (D-53: o espelho `reciboExclusao.generated.ts` está no escopo do `tsconfig`).

Redeploy (o recibo chega ao titular por esta EF): `node efdeploy.cjs executar-direito-titular --dry-run` (fechamento com `_shared/reciboExclusao.ts`; registrar), `node efdeploy.cjs executar-direito-titular` (`verify_jwt=true`), conferir no bundle vivo o `passo_motor` novo. Commitar e `git push origin main`; `origin/main..HEAD` vazio (o espelho do front muda; a tela do recibo é pública ao titular).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node docs/compliance/sql/gen-recibo-exclusao.cjs && node docs/compliance/sql/gen-pii-md.cjs && npm run -s check:matriz-retencao && npm run -s check:pii-inventory-md && npm run -s check:recibo-exclusao && npm run -s check:export-allowlist && npx vitest run docs/compliance src/features/privacidade src/__tests__/guards && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; }</automated>
    <fails_when>non-zero exit; a generator "ERRO DE FECHAMENTO" in stderr; any check failure; "failed" in the vitest summary; or lint exit code other than 0/2 or more than 90 "error TS" lines</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const r=fs.readFileSync('docs/compliance/recibo-exclusao.json','utf8');if(!r.includes('apagar_respostas_e_producoes'))throw new Error('recibo nao aponta o passo novo');const y=fs.readFileSync('docs/compliance/pii-inventory.yaml','utf8');const m=y.match(/user_prompt_template:\s*\{[^}]*\}/);if(!m||!/classificacao:\s*apagar/.test(m[0]))throw new Error('user_prompt_template nao reclassificado para apagar (D-61)');console.log('OK')"</automated>
    <fails_when>non-zero exit with "recibo nao aponta o passo novo" or "nao reclassificado para apagar" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node efdeploy.cjs executar-direito-titular | tee /dev/stderr | grep -q "efdeploy: OK .*verify_jwt=true" && curl -s -H "Authorization: Bearer $(security find-generic-password -s 'Supabase CLI' -a supabase -w)" "https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/functions/executar-direito-titular/body" | grep -a -c "apagar_respostas_e_producoes" && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>non-zero exit; efdeploy output lacks "efdeploy: OK" with "verify_jwt=true"; the grep count printed is 0 (the new recibo did not reach the live EF); or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - Recibo e inventário alinhados ao motor (D-48, D-60, D-61, D-63), regenerados pelos geradores; os quatro `check:*` e os testes de compliance/privacidade/guards verdes; `tsc` ≤ 90.
    - `executar-direito-titular` deployada com `verify_jwt=true` e o bundle vivo contém o passo novo; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>O recibo que o titular recebe passa a descrever exatamente o que o motor apagou.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gerador do recibo → texto entregue ao titular (EF `executar-direito-titular`, tela pública do recibo) | a promessa escrita ao titular |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-21-01 | Repudiation | recibo afirmando apagamento não feito | high | mitigate | precondição: o motor vivo já faz o que o recibo novo promete; recibo regenerado do gerador; `check:recibo-exclusao` |
| T-49-21-02 | Tampering | artefato gerado editado à mão | medium | mitigate | só os geradores escrevem; `check:*` reprovam divergência |
| T-49-21-03 | Denial of Service | recibo novo que não chega ao titular real | medium | mitigate | redeploy com o marcador no bundle vivo; espelho do front publicado |
| T-49-21-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Recibo/inventário regenerados; `check:*`, vitest de compliance e `tsc` ≤ 90 verdes.
- `executar-direito-titular` no ar com o recibo novo; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
O recibo de exclusão diz ao titular exatamente o que o motor apaga — nem mais, nem menos — e é esse recibo que a EF
viva entrega.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `gen-recibo-exclusao.cjs` (`passo_motor` novo, origem `user_prompt_template`, `revisao_resultado` como justificativa) | gerador | editado |
| `recibo-exclusao.json`, `_shared/reciboExclusao.ts`, `reciboExclusao.generated.ts` | artefatos gerados | regenerados |
| `pii-inventory.yaml` (+ `.md`) | inventário | editado/regenerado |
| `genReciboExclusao.test.ts` | teste | atualizado de propósito |
| EF `executar-direito-titular` | deploy | nova versão |

## Premissas sinalizadas (probe de borda)

JORN-36 saiu `unclassified` no probe e fica `unresolved` no relatório. Fatia do 49-14 original (revisão do checker,
W1/W2): separada porque misturava o mecanismo destrutivo com a regeneração de artefatos; o portão `tsc` (D-53) entrou
no verify da regeneração porque `reciboExclusao.generated.ts` está no escopo do `tsconfig` e o vitest transpila sem
checar tipos. O 49-17 regenera os mesmos artefatos depois (checklist D-57) a partir do estado deixado aqui.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-21-SUMMARY.md` when done
</output>
