---
phase: 49-consertos-da-jornada-bloco-2
plan: 29
type: execute
wave: 8
depends_on: ["49-14", "49-20", "49-21", "49-17"]
files_modified:
  - supabase/migrations/PENDING_p49_motor_desidentifica_analises.sql
  - supabase/tests/p45_motor_exclusao_smoke.sql
autonomous: true
requirements: [JORN-36]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "O motor de exclusão passa a desidentificar os textos livres do titular em `analise_candidato_vaga` (e no que a medição mostrar equivalente em `entrevista_guias`), tabelas que ele HOJE NÃO CITA — medido no corpo vivo: `position('analise_candidato_vaga' in prosrc) = 0`."
    - "O escopo é MEDIDO, não herdado: a varredura do 49-17 estava errada nos dois sentidos (`entrevista_guias` é 0 de 5, não 2 de 5 — os «achados» eram a palavra portuguesa comum `candidato` no `rationale`, casando por acidente com a conta «Candidato Funil Teste»), e a contagem de `analise_candidato_vaga` é LIMITE INFERIOR porque casar contra o nome ATUAL é cego ao titular que já exerceu a exclusão."
    - "A promessa que o recibo JÁ FAZ volta a ser verdadeira: o item `avaliacoes_e_analises` diz que as análises ficam guardadas «sem ligação com você». Por isso o texto do recibo NÃO muda — é o motor que passa a cumpri-lo (decisão (c) do operador no checkpoint do 49-17, 2026-09-23)."
    - "O passo trata RETROATIVAMENTE os titulares JÁ anonimizados: medido pelo orquestrador, 8 linhas de `analise_candidato_vaga` pertencentes a titulares com e-mail `@invalido.local` ainda têm `resumo_cv` preenchido, de 57 a 1918 caracteres. Uma desidentificação que só vale para exclusões futuras deixa de pé exatamente o caso que motivou a decisão."
    - "Desidentificar NÃO é apagar a linha: a análise continua existindo (é registro de tratamento que o JORN-28 exige); o que sai é o texto livre do titular. A escolha por coluna é lida do catálogo, como o 49-20 fez — sentinela nas `NOT NULL`, NULL nas nuláveis."
    - "As colunas de telemetria (`provedor_ia`, `modelo_ia`, `prompt_version`, `status`) NÃO são tocadas: não são dado do titular, e o 49-17 acabou de lhes dar veredito de export."
    - "D-70 continua valendo: os hashes (`texto_hash`, `input_hash`) NÃO são apagados e não são descritos como apagados."
  artifacts:
    - path: supabase/tests/p45_motor_exclusao_smoke.sql
      provides: "asserção de que o texto livre do titular não sobrevive à exclusão nessas tabelas"
      contains: "resumo_cv"
  key_links:
    - from: "passo novo de desidentificação"
      to: "item `avaliacoes_e_analises` do recibo, que promete «sem ligação com você»"
      via: "o motor passa a cumprir a promessa existente"
      pattern: "resumo_cv"
  prohibitions:
    - requirement_id: JORN-36
      category: privacy
      status: resolved
      verification: automated
      statement: "MUST NOT deixar texto livre do titular numa tabela de análise depois de a exclusão concluir"
---

<objective>
O nome do titular para de sobreviver à exclusão dentro das análises.

Purpose: medido no checkpoint do 49-17 e reconferido pelo orquestrador — `anonimizar_candidato` **não cita**
`analise_candidato_vaga` nem `resumo_cv` (`position(...) = 0` no corpo vivo), e há **8 linhas** de titulares com
e-mail já anonimizado cujo `resumo_cv` segue preenchido, de 57 a 1918 caracteres. `resumo_cv` é o texto do currículo
do próprio titular. E o recibo diz a essas pessoas que as análises ficaram guardadas «**sem ligação com você**».

Não é desalinhamento de registro como os outros desta fase: é a promessa do recibo sendo **falsa** para quem já
exerceu o direito. O operador escolheu a opção (c) do 49-17 — desidentificar, em vez de enfraquecer o recibo — o que
faz a frase existente voltar a ser verdadeira e trata os casos já concluídos.

Output: o passo de desidentificação no motor, aplicado, com a asserção no smoke e as 8 linhas tratadas.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-17-SUMMARY.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-20-SUMMARY.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-21-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: o motor desidentifica os textos livres das análises</name>
  <precondition>`config_purga.modo = dry_run`; md5 vivos = `anonimizar_candidato` `1d8f96c8f21a755ded0505a0b652113a` (78 301) e `plano_exclusao_titular` `6f2ef83664944b9a39c7d495a49ab8f1` (33 716). Medir os três ANTES de editar, e usá-los como pré-portão do apply.</precondition>
  <files>supabase/migrations/PENDING_p49_motor_desidentifica_analises.sql, supabase/tests/p45_motor_exclusao_smoke.sql</files>
  <read_first>
    - o corpo vivo de `anonimizar_candidato` (o passo `apagar_respostas_e_producoes` do 49-20/49-21 é o molde: escolha por coluna lida do catálogo, `GET DIAGNOSTICS`, registro em `'passos'`, chave própria por origem)
    - `plano_exclusao_titular` (o dry-run tem de contar as mesmas coisas pela MESMA expressão — foi assim no 49-14 e no 49-20)
    - `docs/compliance/` — o item `avaliacoes_e_analises` do recibo e o `pii-inventory.yaml` que o 49-17 acabou de regenerar
    - `supabase/tests/p45_motor_exclusao_smoke.sql` (contador `P45=36` depois do 49-20; leia-o da execução, não daqui — o 49-20 achou o gate num número que vivia em TRÊS lugares e só dois foram bumpados)
  </read_first>
  <action>
**MEDIR O ESCOPO PRIMEIRO — e não confiar em nenhum número herdado.** O 49-17 mediu que a varredura anterior errava
nos dois sentidos. Para cada coluna de texto livre de `analise_candidato_vaga` (`resumo_cv`, `resumo_respostas`,
`descartada_motivo`, `erro`) e de `entrevista_guias` (`guia` jsonb, e o que mais houver), medir em PROD (só leitura):
quantas linhas têm conteúdo, quantas pertencem a titular já anonimizado, e **se o conteúdo é do titular ou do
sistema**. `erro` e `status` provavelmente são do sistema; `resumo_cv` é o currículo do titular. Declarar a
classificação de cada uma no SUMMARY, com a evidência — é ela que decide o que entra no passo.

⚠ **Não medir por igualdade com o nome atual.** É o instrumento cego que produziu o «13 de 24»: o titular
anonimizado teve o nome substituído em `candidatos`, então casar contra o nome atual não o encontra. Medir por
**presença de conteúdo** nas linhas cujo titular está anonimizado — que é como o orquestrador achou as 8.

**Escrever o passo**, no molde do 49-20/49-21: escolha por coluna lida do catálogo (sentinela nas `NOT NULL`, NULL
nas nuláveis), escopado por `candidato_id`/`candidatura_id`, contado por `GET DIAGNOSTICS` e declarado em `'passos'`
com chave por origem. A linha **fica** — é registro de tratamento que o JORN-28 exige; sai o texto livre.

**Tratar as 8 linhas retroativas.** Elas pertencem a titulares cuja exclusão JÁ concluiu, então o passo novo nunca
rodará sobre elas por si. Duas formas, e a escolha tem de estar no SUMMARY com a razão: um `UPDATE` retroativo no
mesmo apply, escopado ao conjunto medido e literal (padrão §I, com `IS DISTINCT FROM` do conjunto autorizado e aborto
se divergir), ou re-executar o motor sobre eles. ⚠ Se a segunda, **PARE e reporte** — reexecutar o motor é 49-19,
sob portão do operador.

**NÃO tocar** a telemetria (`provedor_ia`, `modelo_ia`, `prompt_version`, `status`) — o 49-17 acabou de lhes dar
veredito de export; nem os hashes (D-70); nem o texto do recibo, que já promete o certo.

**Asserção no smoke:** depois da exclusão, nenhuma das colunas classificadas como do titular tem conteúdo nessas
tabelas. Provar mordida por inversão **uma por cláusula** (a fase fez 5, 7, 8, 11, 12, 18). ⚠ Descontar ANSI e ler o
exit code do RUNNER, não de um `sed` no pipe (§L, medido quatro vezes).

**Apply:** `p46apply.cjs migrate`, SQL lido do arquivo, migration + linha do ledger na MESMA requisição, md5 conferido
por leitura de volta. Renomear o arquivo `PENDING_` para a versão real (`20260923000002_…` ou a seguinte livre) antes
do apply — **e não reutilizar o slot `20260922000010`**, que está deliberadamente vazio, registrando a recusa da D-47.

Commitar e `git push origin main`; `origin/main..HEAD` vazio.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs run supabase/tests/p45_motor_exclusao_smoke.sql 2>&1 | tee /dev/stderr | grep -q "P45=" && ! node p46apply.cjs run supabase/tests/p45_motor_exclusao_smoke.sql 2>&1 | grep -qE "P45 FAIL"</automated>
    <fails_when>the smoke reports any "P45 FAIL", or no final counter line is emitted</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs sql "select count(*) as n from public.analise_candidato_vaga a join public.candidaturas c on c.id=a.candidatura_id join public.candidatos k on k.id=c.candidato_id where k.email like '%@invalido.local' and a.resumo_cv is not null" 2>&1 | tee /dev/stderr | grep -qE '(^| )0( |$)' && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>any row of an anonymised titular still carries `resumo_cv`, or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - O escopo medido nesta sessão, coluna por coluna, com a classificação titular/sistema justificada.
    - Passo no motor, contado e registrado em `'passos'`; o dry-run conta pela mesma expressão.
    - As linhas retroativas tratadas, ou a decisão de não tratá-las escrita com a razão.
    - Telemetria, hashes e texto do recibo intocados.
    - Smoke com a asserção nova, mordida provada por inversão.
    - Migration no ledger com md5 conferido; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>Concluída a exclusão, nenhuma análise guarda o texto que o titular escreveu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| motor → `analise_candidato_vaga` / `entrevista_guias` | duas tabelas que o motor nunca tocou passam a ser alcançadas |
| recibo → titular | a promessa «sem ligação com você» passa a ser cumprida em vez de reescrita |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-29-01 | Information Disclosure | nome/currículo do titular sobrevivendo à exclusão | **high** | mitigate | passo de desidentificação + tratamento retroativo do conjunto medido |
| T-49-29-02 | Tampering | apagar telemetria ou hash por engano e desfazer proveniência/idempotência | medium | mitigate | classificação por coluna lida do catálogo; telemetria e hashes explicitamente fora |
| T-49-29-03 | Repudiation | apagar a LINHA e perder o registro de tratamento | medium | mitigate | a linha fica; sai o texto livre |
| T-49-29-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- Escopo medido e classificado; smoke verde com a asserção nova e mordida provada; as 8 linhas (ou o conjunto que a medição achar) sem texto do titular; telemetria e hashes intactos; migration no ledger com md5 conferido; `origin/main..HEAD` vazio; `config_purga.modo` ainda `dry_run`.
</verification>

<success_criteria>
Um titular que exerceu o direito de exclusão não tem mais o próprio nome nem o próprio currículo guardados dentro de uma análise — e o recibo que ele recebeu volta a dizer a verdade.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| passo de desidentificação em `anonimizar_candidato` | migration (função) | editado |
| asserção no `p45_motor_exclusao_smoke.sql` | teste | editado |

## Premissas sinalizadas

As 8 linhas foram medidas pelo orquestrador por **presença de conteúdo** em titular com e-mail `@invalido.local`.
Parte delas pode ser fixture de teste da semente p46 — a classificação (real × fixture) é do executor, e muda o
tratamento retroativo, não a necessidade do passo. O fato estrutural não depende dessa distinção: o motor não cita
a tabela.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-29-SUMMARY.md` when done
</output>
