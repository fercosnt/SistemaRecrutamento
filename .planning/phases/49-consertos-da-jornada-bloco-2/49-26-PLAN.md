---
phase: 49-consertos-da-jornada-bloco-2
plan: 26
type: execute
wave: 4
depends_on: ["49-02", "49-10", "49-24", "49-25"]
files_modified:
  - supabase/functions/_shared/resultado-de-provedor.ts
  - supabase/functions/_shared/__tests__/resultado-de-provedor.test.ts
  - supabase/functions/gerar-guia-entrevista/index.ts
  - supabase/functions/gerar-guia-entrevista/_local/merge-preserve.test.ts
  - supabase/functions/avaliar-transcricao-entrevista/index.ts
  - supabase/functions/avaliar-transcricao-entrevista/__tests__/index.test.ts
autonomous: true
requirements: [JORN-28, JORN-39]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WINDOWS 64: `persistFlags` deixa de ser descartado no caminho de sucesso — um roteiro que ainda deixa dimensão fraca descoberta é gravado COM `weak_dim_uncovered` em `flags`, e o docblock da EF passa a descrever o que o código faz."
    - "WINDOWS 65: `avaliar-transcricao-entrevista` deixa de tratar bloqueio pré-provedor como sucesso — a pergunta passa a ser estrutural (`provider === 'none'` ⇒ nenhum modelo respondeu), não uma lista literal de `error_code`."
    - "O predicado vive em UM lugar (`_shared/resultado-de-provedor.ts`), consumido pelas duas EFs — D-21 «reusar, não recriar». O `guiaDeResultado` do 49-25 passa a se construir sobre ele em vez de repetir a pergunta."
    - "Acrescentar arquivo NOVO a `_shared` não dessincroniza as outras cinco EFs (elas não o importam); mas as duas EFs deste plano precisam de deploy, e o fechamento de imports de cada uma é conferido À MÃO contra o bundle vivo — o `efdeploy.cjs` NÃO confere fechamento contra lista esperada, apesar do que o cabeçalho afirma (item aberto do 48-19, reconfirmado pelo 49-11)."
    - "O selo do 49-16 passa a poder ver `weak_dim_uncovered` — sem este plano ele entregaria interface para um estado que não pode ocorrer."
    - "Nenhuma migration, nenhuma coluna, nenhum `max_tokens` tocado. O disclaimer NEGADO do rodapé da devolutiva não é tocado (exceção decidida no CLAUDE.md) — nenhuma das duas EFs deste plano o contém, e isso é conferido."
  artifacts:
    - path: supabase/functions/_shared/resultado-de-provedor.ts
      provides: "fonte única de «algum modelo respondeu?»"
      contains: "none"
    - path: supabase/functions/avaliar-transcricao-entrevista/index.ts
      provides: "bloqueio pré-provedor não vira análise pendente_humano vazia"
      contains: "resultado-de-provedor"
  key_links:
    - from: "CallAiResult.provider (49-02)"
      to: "entrevista_guias.guia.flags  ·  entrevista_analises.status_analise"
      via: "_shared/resultado-de-provedor.ts"
      pattern: "resultado-de-provedor"
  prohibitions:
    - requirement_id: JORN-39
      category: transparency
      status: resolved
      verification: automated
      statement: "MUST NOT tratar como resultado de IA uma resposta que nenhum provedor produziu"
    - requirement_id: JORN-28
      category: transparency
      status: resolved
      verification: automated
      statement: "MUST NOT computar uma flag de revisão humana e descartá-la antes de gravar"
---

<objective>
As duas EFs que ainda confundem «nenhum modelo respondeu» com «o modelo respondeu isto» param de confundir — e a
flag de revisão humana que já era computada passa a ser gravada.

Purpose: os dois defeitos foram MEDIDOS pelo 49-25 (`WINDOWS.md` 64 e 65) e nenhum plano da fila era dono deles.
São a mesma família do JORN-28/JORN-39 — ausência que se apresenta como valor real — e um deles é pré-requisito
honesto do **49-16**: sem o 64, o selo do guia nunca verá `weak_dim_uncovered`, e o 49-16 entregaria interface
para um estado que o código torna impossível.

Estado medido que enquadra o risco: **zero** linhas `provider='none'` em `ai_call_logs` desde sempre, e o maior
gasto diário por vaga da história é US$ 0,3096 contra teto de US$ 50 (161× de folga). Os dois defeitos são
**latentes** — nunca manifestaram. Isto é conserto de corretude e de registro, não incidente.

Output: o predicado em `_shared`, as duas EFs consertadas e deployadas.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-25-SUMMARY.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-10-SUMMARY.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: o predicado em `_shared` e a flag que para de ser descartada (WINDOWS 64)</name>
  <precondition>`gerar-guia-entrevista` está em v20 com `guiaDeResultado` no bundle vivo (49-25), e `_shared/ai-error-codes.ts` do 49-02 está no disco.</precondition>
  <files>supabase/functions/_shared/resultado-de-provedor.ts, supabase/functions/_shared/__tests__/resultado-de-provedor.test.ts, supabase/functions/gerar-guia-entrevista/index.ts, supabase/functions/gerar-guia-entrevista/_local/merge-preserve.test.ts</files>
  <read_first>
    - `supabase/functions/gerar-guia-entrevista/index.ts` (o `guiaDeResultado` do 49-25; o `persistFlags` no passo 8; e o upsert onde `flags` só aparece no ramo `{incompleto:true, …}` — é ali que a flag morre)
    - `supabase/functions/_shared/ai-client.ts` (os DOIS retornos reais com `provider: "none"` — o 49-25 mediu que dos 4 acertos de grep só 2 são retornos; os outros são payloads de `logAiCall`)
    - `supabase/functions/_shared/ai-error-codes.ts`
  </read_first>
  <action>
**RED primeiro.** Acrescentar ao harness que captura o upsert (`_local/merge-preserve.test.ts`, onde o 49-25 pôs os
seus) o caso que hoje reprova: existe roteiro **e** uma dimensão fraca segue descoberta ⇒ a linha gravada deve ter
`weak_dim_uncovered` em `flags`. Medir e registrar o RED real (reprova por asserção; um RED que trava no
carregamento é `INVALID_RED`, #3770 — e o `check tdd-red-evidence` não lê o TAP do Deno, então registre o medido e
**não sintetize linhas de contador**).

**GREEN.** Extrair para `_shared/resultado-de-provedor.ts` o predicado «algum provedor respondeu?» — a pergunta
estrutural que o 49-25 estabeleceu (`provider === 'none'` ⇒ não) — com testes próprios, **zero imports**, no molde de
`_shared/candidaturaEncerrada.ts` (49-03) e `_shared/sjt-rubrica.ts` (49-23). O `guiaDeResultado` passa a se construir
sobre ele em vez de repetir a pergunta.

Fazer `flags` chegar ao upsert **também no caminho de sucesso**, preservando tudo que o 49-25 e o 49-24 deixaram: o
merge ENTREV-08 (perguntas `origem:'manual'` sobrevivem — asserir, não ler), a proveniência real, e `flags` **ausente**
(não `[]`) quando não há nada a sinalizar — um array vazio e «não havia flag» têm de ser distinguíveis, e um `[]`
explícito sob `onConflict` apagaria a flag da execução anterior no instante do reprocessamento (precedente medido no
49-11).

**Corrigir o docblock** que hoje afirma o contrário do que o código faz — descrevendo a afirmação retirada **sem
reproduzir a forma proibida** (PATTERNS §K; o 49-25 violou isto e o que o pegou foi o **bundle vivo**, não o disco:
depois de consertar, confira o grep no BUNDLE publicado, não só no arquivo).

Rodar `deno test --allow-all supabase/functions/gerar-guia-entrevista/ supabase/functions/_shared/`. Provar por
mutação (commitar antes; `git diff --quiet` como guarda). Deploy: `--dry-run`, conferir o fechamento **à mão** contra
o bundle vivo, deployar, reler versão/status/`verify_jwt` da Management API.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/_shared/__tests__/resultado-de-provedor.test.ts supabase/functions/gerar-guia-entrevista/ && test "$(grep -c 'import.*resultado-de-provedor' supabase/functions/gerar-guia-entrevista/index.ts)" -ge 1</automated>
    <fails_when>non-zero exit, "FAILED" in the Deno output, or the EF does not consume the shared predicate</fails_when>
  </verify>
  <acceptance_criteria>
    - Roteiro com dimensão fraca descoberta é gravado com `weak_dim_uncovered`; sem flag, a chave fica AUSENTE.
    - O predicado vive em `_shared` e as duas pontas o consomem.
    - Docblock descreve o comportamento real, sem reproduzir a forma proibida — conferido no bundle vivo.
  </acceptance_criteria>
  <done>A flag que já era computada chega ao banco.</done>
</task>

<task type="tdd">
  <name>Task 2: a análise da transcrição deixa de chamar bloqueio de «pendente» (WINDOWS 65)</name>
  <precondition>Task 1 concluída: `_shared/resultado-de-provedor.ts` existe com testes verdes.</precondition>
  <files>supabase/functions/avaliar-transcricao-entrevista/index.ts, supabase/functions/avaliar-transcricao-entrevista/__tests__/index.test.ts</files>
  <read_first>
    - `supabase/functions/avaliar-transcricao-entrevista/index.ts` (`:377` — a guarda por lista literal; e o ramo de sucesso que grava `status_analise` via `registrar_analise_entrevista`)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-10-SUMMARY.md` (esta EF é a dele: v17, RPC `registrar_analise_entrevista`, e o `provedorDeResultado` já em uso aqui)
  </read_first>
  <action>
**RED primeiro:** teto de custo estourado ⇒ a linha NÃO deve entrar como análise aguardando revisão com competências
vazias; deve entrar como `falhou` (o ramo que já existe e que o 49-10 desenhou para não superar a análise boa
anterior), com o `error_code` registrado. Medir o RED real.

**GREEN:** trocar a lista literal de `:377` pela pergunta estrutural do `_shared/resultado-de-provedor.ts`. Preservar
o que o 49-10 provou: a linha de falha **não** supera ninguém e **não** é vigente, e a análise boa anterior segue
sendo a que a tela, a revisão e o portão de avanço leem. Não tocar a RPC nem o predicado SQL.

Rodar `deno test` da EF. Provar por mutação. Deploy com `--dry-run`, fechamento conferido à mão, versão relida.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && deno test --allow-all supabase/functions/avaliar-transcricao-entrevista/ && test "$(grep -c 'resultado-de-provedor' supabase/functions/avaliar-transcricao-entrevista/index.ts)" -ge 1</automated>
    <fails_when>non-zero exit, "FAILED" in the Deno output, or the EF does not consume the shared predicate</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npm run -s lint 2>&1 | grep -cE "error TS" | awk '{if ($1+0 > 90) {print "tsc ceiling exceeded: " $1; exit 1} else {print "tsc " $1 " <= 90"}}' && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>tsc error count exceeds the D-53 ceiling of 90, or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - Teto de custo ⇒ `status_analise='falhou'`, nunca `pendente_humano` com competências vazias.
    - A análise vigente anterior permanece vigente (asserido, não lido).
    - EF deployada, versão relida de PROD; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>Uma análise que nenhum modelo produziu não espera revisão humana.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF → `entrevista_guias` / `entrevista_analises` (service_role) | as duas linhas passam a declarar quando nenhum modelo as produziu |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-26-01 | Repudiation | flag de revisão humana computada e descartada | medium | mitigate | `flags` no upsert de sucesso; chave ausente quando não há flag |
| T-49-26-02 | Repudiation | bloqueio por custo apresentado como análise aguardando revisão | medium | mitigate | predicado estrutural por provedor em `_shared` |
| T-49-26-03 | Tampering | `[]` explícito apagar flag anterior sob `onConflict` | low | mitigate | chave AUSENTE, asserida por teste |
| T-49-26-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `deno test` das duas EFs e do `_shared` verdes; mutação prova que cada ramo novo morde (ou a razão medida está escrita); `tsc` ≤ 90 com conjunto de mensagens diffado; as duas EFs deployadas com versão relida de PROD; fechamento de imports conferido à mão contra o bundle vivo; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
Nenhuma das duas EFs apresenta como resultado de IA algo que nenhum provedor produziu, e a flag de revisão humana que o código computa chega ao banco — de modo que o selo do 49-16 tenha um estado real para mostrar.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `_shared/resultado-de-provedor.ts` | utility (zero imports) | novo |
| `gerar-guia-entrevista` · `avaliar-transcricao-entrevista` | deploy | novas versões |

## Premissas sinalizadas

Linhas já gravadas nos dois estados defeituosos: **zero**, medido pelo 49-25 (e zero `provider='none'` em
`ai_call_logs` desde sempre). Não há nada retroativo — se uma medição desta sessão contradisser isso, é matéria do
**49-12** (checkpoint, D-30), a registrar e não consertar aqui.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-26-SUMMARY.md` when done
</output>
