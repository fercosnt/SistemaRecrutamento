---
phase: 49-consertos-da-jornada-bloco-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260922000001_p49_llm_provider_none.sql
  - supabase/migrations/20260922000002_p49_colunas_proveniencia_e_analise.sql
  - database.types.ts
autonomous: true
requirements: [JORN-39, JORN-28, JORN-12, JORN-07]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "O enum `public.llm_provider` passa a ter o valor `none` em PROD (`enum_range(null::public.llm_provider)` contém `anthropic`, `openai`, `google`, `none`), por migration PRÓPRIA que não usa o valor na mesma transação — é o que deixa de derrubar em 22P02 as linhas de teto de custo e de injeção (JORN-39; NULL descartado porque `ai_cost_daily.provider` é NOT NULL e o cron `ai-cost-aggregation` agrupa por ele — RESEARCH Correção 32)."
    - "As 5 tabelas de resultado de IA têm `provedor_ia text` e `modelo_ia text`, ambas NULÁVEIS, com CHECK de vocabulário `provedor_ia IS NULL OR provedor_ia IN ('anthropic','openai')`: `analise_candidato_vaga`, `comparativo_solicitado`, `entrevista_guias`, `entrevista_analises`, `redacoes_candidato` (D-28). NULL = «proveniência desconhecida» — é o valor das linhas antigas, sem escrita retroativa (D-30)."
    - "`redacoes_candidato.rubrica_versao text` NULÁVEL existe; NULL = redação avaliada antes de a rubrica ser enviada ao modelo (as 2 de teste ficam assim, D-26)."
    - "`entrevista_analises` ganha `tipo` (CHECK `tipo IS NULL OR tipo IN ('online','presencial')`), `solicitado_por uuid` (sem FK — UUID de funcionário), `texto_hash text`, `ai_call_log_id uuid` (sem FK — o log é purgado em 180 d e o motor o redige) e `superada_em timestamptz` (D-02: marcar, não apagar), todas NULÁVEIS (D-38, D-39, D-41)."
    - "Existe UMA função `public.entrevista_analise_vigente(p_superada_em timestamptz, p_status_analise text, p_competencias jsonb)`, IMMUTABLE, `SET search_path = ''`, que é a única definição de «vigente» (D-39): não superada, não falhou, competências presentes. `avancar_etapa` (49-06), a RPC de gravação e as duas RPCs de revisão (49-10) chamam ESTA função — o predicado não é reescrito em cada lugar."
    - "Nenhuma coluna nova é NOT NULL (D-55 / Pitfall 3): a EF velha continua gravando entre esta migration e o deploy da EF nova. O bloco de auto-verificação da migration prova `is_nullable = 'YES'` nas 16 colunas."
    - "`database.types.ts` regenerado pelo comando com `< /dev/null`, não vazio, contém `none` no enum e as colunas novas; `tsc` ≤ 90 (D-53)."
    - "Toda migration deste plano foi aplicada com `node p46apply.cjs migrate`, md5 do ledger conferido (D-52); `git log --oneline origin/main..HEAD` sai vazio no fim (os commits de docs da fase, hoje não enviados — RESEARCH Correção 1 — vão junto)."
    - "Se a medição viva divergir do que este plano assume (enum com outro conjunto, coluna já existente com outro tipo), o executor PARA e reporta (D-49, D-14) — não ajusta o plano para caber."
    - "A varredura de portões do CLAUDE.md foi rodada antes do apply (282 linhas na abertura) e todo achado que cite `llm_provider` ou as 5 tabelas foi classificado no SUMMARY (D-50, D-56)."
  artifacts:
    - path: supabase/migrations/20260922000001_p49_llm_provider_none.sql
      provides: "valor `none` no enum llm_provider (JORN-39)"
      contains: "ADD VALUE IF NOT EXISTS 'none'"
    - path: supabase/migrations/20260922000002_p49_colunas_proveniencia_e_analise.sql
      provides: "16 colunas nulas (D-28, D-26, D-38..D-41) + predicado único de vigente"
      contains: "entrevista_analise_vigente"
    - path: database.types.ts
      provides: "tipos regenerados com as colunas e o enum novos"
      contains: "rubrica_versao"
  key_links:
    - from: "public.entrevista_analise_vigente"
      to: "avancar_etapa (49-06), registrar_analise_entrevista / salvar_avaliacao_entrevista / confirmar_revisao_entrevista (49-10)"
      via: "chamada à mesma função IMMUTABLE — um predicado, não quatro cópias"
      pattern: "entrevista_analise_vigente\\("
    - from: "colunas provedor_ia / modelo_ia"
      to: "EFs de IA (49-08..49-11, 49-23, 49-24) e telas (49-13, 49-22, 49-15, 49-16)"
      via: "INSERT/UPSERT pela EF com o modelo REAL do CallAiResult (49-02); leitura pelo selo de proveniência"
      pattern: "modelo_ia"
    - from: "entrevista_analises.solicitado_por"
      to: "docs/compliance/export-scope-rules.yaml ponteiros.de_terceiro (49-17)"
      via: "o nome `solicitado_por` JÁ está na R2 do inventário e em `ponteiros.de_terceiro` — o UUID de funcionário não entra na cópia do titular por construção"
      pattern: "solicitado_por"
  prohibitions:
    - requirement_id: JORN-28
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT criar coluna NOT NULL nesta onda — a EF velha grava entre a migration e o deploy da nova, e uma NOT NULL a derrubaria em silêncio (D-55)"
    - requirement_id: JORN-28
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT preencher retroativamente provedor/modelo das linhas antigas — a proveniência delas é desconhecida e NULL é a verdade (D-30)"
    - requirement_id: JORN-39
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT usar o valor novo do enum na mesma migration que o cria — ADD VALUE só é utilizável depois do COMMIT"
---

<objective>
O esquema aditivo que toda a fase consome: o valor `none` no enum de provedor (JORN-39), as colunas de
proveniência real nas 5 tabelas de resultado (D-28), a versão da rubrica na redação (D-26), as colunas
de dono da análise de entrevista (D-38, D-39, D-41) e o predicado único de «vigente» (D-39).

Purpose: D-55 manda migration → EF → cliente. Nenhuma EF desta fase pode gravar `provedor_ia`, `tipo`
ou `provider='none'` antes de a coluna e o valor existirem em PROD (Pitfall 8 do RESEARCH). Tudo aqui é
NULÁVEL para a EF velha continuar funcionando até o deploy da nova.

Output: duas migrations aplicadas; `database.types.ts` regenerado; `main` enviado.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-VARREDURA-KICKOFF.md
</context>

<assumption_delta_decision>
**Termo pluralizado:** «fallback» — um resultado deixava de ser implicitamente «produzido pelo modelo
configurado» (singular) e passa a poder vir de outro provedor/modelo.

**Substantivo que passa a ser primário:** o provedor/modelo REAL de cada resultado, por linha
(`provedor_ia`/`modelo_ia`), vindo da resposta do SDK (`response.model`, que o `audit-logger` já grava
em `ai_call_logs.model_snapshot`) — nunca o `model_id` configurado em `prompt_versions`.

**Decisão (D-28): PROMOVER.** `modelo_ia`/`provedor_ia` são a proveniência canônica nas 5 tabelas.
`redacoes_candidato.model_version` hoje grava o modelo CONFIGURADO (`avaliar-redacao-cultural/index.ts:304,364`,
RESEARCH Correção 28), o que o D-28 declara defeito. Ela **não é removida** (o `trg_redacao_rh_only_review_fields`
e o veredito `export: false` a citam) e **passa a receber o modelo REAL**, o mesmo valor de `modelo_ia`, a
partir do deploy do plano 49-09. As linhas antigas ficam com o valor configurado (D-26/D-30, nenhuma escrita
retroativa) e com `modelo_ia` NULL = «desconhecida» — por isso todo leitor novo lê `modelo_ia`, nunca
`model_version`.

**Leitores que mudam:** nenhum leitor de `model_version` existe em `src/` (grep: só o veredito de export em
`_shared/exportAllowlist.ts:879`). Leitores novos de `modelo_ia`/`provedor_ia`: o selo de proveniência
(49-13) e as telas de comparativo, decisão final, redação, guia, análise da triagem e análise de entrevista (49-13,
49-22, 49-15, 49-16), e o PDF do comparativo (49-13).

**Invariante companheira (sugerida pela capacidade, entra na prova):** toda linha de resultado gravada
depois de T0 carrega o provedor/modelo que o SDK reportou, no caminho Anthropic E no caminho de fallback —
coluna `p28_resultados_com_modelo` (+ `p28_modelo_real_bate_log`) do `p49_prova_prod.sql` (plano 49-18) e o
teste Deno do 49-02 que assere `model` = `response.model` nos dois caminhos.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer) [BLOCKING]: o valor `none` no enum — migration própria, aplicada, conferida no catálogo e nos tipos</name>
  <precondition>`node p46apply.cjs sql "SET TRANSACTION READ ONLY; select 1"` responde (token no Keychain, serviço "Supabase CLI"). Sem isso não há apply nem leitura viva — parar e reportar.</precondition>
  <reversibility rating="costly">Valor de enum não se remove sem recriar o tipo; mas é aditivo e nenhum leitor quebra com um valor a mais.</reversibility>
  <files>supabase/migrations/20260922000001_p49_llm_provider_none.sql, database.types.ts</files>
  <read_first>
    - `supabase/migrations/20260706010519_bigfive_devolutiva_enum.sql` (o analog inteiro: ADD VALUE em arquivo próprio, idempotente)
    - `supabase/migrations/20260921000003_p48_local_ou_link_valida.sql` (`:1-62` — a forma do cabeçalho e a nota de transporte atual)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md` §1 (cabeçalho/transporte/ACL) e §2
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-RESEARCH.md` §E.3 e Correção 32
    - `supabase/functions/_shared/ai-client.ts` (`:536-608` — as duas linhas `provider: "none"` que hoje falham)
    - `CLAUDE.md` §«Via de apply ATUAL» e §«Portões: varra pela FORMA»
  </read_first>
  <action>
Passo 1 — medir o vivo, só leitura: `node p46apply.cjs sql "SET TRANSACTION READ ONLY; select enum_range(null::public.llm_provider)::text as v, (select count(*) from public.ai_call_logs where provider::text = 'none') as n_none"`. Esperado: `{anthropic,openai,google}` e 0. Qualquer outro resultado é correção de fato: parar e reportar (D-49).

Passo 2 — varredura de portões (D-56): rodar o padrão exato do `CLAUDE.md` §«Portões» sobre `supabase/tests/*.sql`, registrar a contagem (282 na abertura) e classificar no SUMMARY todo achado que cite `llm_provider`, `typname` ou as tabelas `analise_candidato_vaga`, `comparativo_solicitado`, `entrevista_guias`, `entrevista_analises`, `redacoes_candidato` (escopo deliberado × fotografia). Conferir também `grep -ln` dessas tabelas em `supabase/tests/` (hoje: `p48_prova_prod.sql`, `p48_prontidao_prod.sql`, `sec02_smokes.sql`, `sec05_08_smokes.sql`) e registrar se algum assere lista de colunas.

Passo 3 — escrever `supabase/migrations/20260922000001_p49_llm_provider_none.sql`: cabeçalho no molde do M8 (O QUE ESTAVA ERRADO medido em 2026-09-22 — as linhas de teto de custo AI-06 e de injeção gravam `provider='none'`, o enum não tem o valor, o INSERT falha em 22P02 e o `audit-logger` engole: 0 linhas `none` em PROD, nenhum bloqueio jamais registrado; POR QUE `none` e não NULL — Correção 32; IDEMPOTÊNCIA — `IF NOT EXISTS`; nota de transporte `APLICAR COM: node p46apply.cjs migrate supabase/migrations/20260922000001_p49_llm_provider_none.sql`). Corpo: SÓ `ALTER TYPE public.llm_provider ADD VALUE IF NOT EXISTS 'none';` e, opcionalmente, um `DO` que LÊ `pg_enum` e aborta se o valor não estiver lá. Nenhum INSERT, nenhum uso do valor, nenhum `BEGIN;` externo.

Passo 4 — aplicar: `node p46apply.cjs migrate supabase/migrations/20260922000001_p49_llm_provider_none.sql`.

Passo 5 — tipos: `SUPABASE_ACCESS_TOKEN=$(security find-generic-password -s "Supabase CLI" -a supabase -w) npx supabase gen types typescript --project-id isljnozzlvckrgjjbjwp < /dev/null > database.types.ts` (sem o `< /dev/null` pendura e trunca — memória `db-types-pendura-e-trunca`). Arquivo com 0 octeto ⇒ `git checkout -- database.types.ts` e repetir. Conferir que o union de `llm_provider` contém `"none"`. Rodar o portão `tsc` (≤ 90). Commitar.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260922000001_p49_llm_provider_none.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');if(!/APLICAR COM: node p46apply\.cjs migrate/.test(s))throw new Error('nota de transporte ausente');if(!/ALTER TYPE public\.llm_provider ADD VALUE IF NOT EXISTS 'none'/.test(c))throw new Error('falta o ADD VALUE');if(/INSERT\s+INTO/i.test(c))throw new Error('o valor novo nao pode ser usado na mesma transacao');console.log('OK migration')"</automated>
    <fails_when>non-zero exit with "wrapper BEGIN proibido", "nota de transporte ausente", "falta o ADD VALUE" or "o valor novo nao pode ser usado" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs migrate supabase/migrations/20260922000001_p49_llm_provider_none.sql</automated>
    <fails_when>non-zero exit, or the output lacks the line "md5 do ledger BATE"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs sql "SET TRANSACTION READ ONLY; select enum_range(null::public.llm_provider)::text as v" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);if(!/none/.test(r[0].v)){console.error('enum sem none: '+r[0].v);process.exit(1)}console.log('enum: '+r[0].v)})"</automated>
    <fails_when>non-zero exit with "enum sem none" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -s database.types.ts && grep -Eq 'llm_provider: .*"none"' database.types.ts && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; }</automated>
    <fails_when>non-zero exit; database.types.ts empty or its llm_provider union without "none"; lint exit code other than 0/2, exit code and error count disagree, or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - A migration existe, sem `BEGIN;` externo, com a nota de transporte `p46apply.cjs migrate`, e nenhum statement usa o valor novo.
    - Em PROD, `enum_range(null::public.llm_provider)` contém `none` (consulta acima).
    - `database.types.ts` não vazio e contém `"none"`; `tsc` ≤ 90.
    - A contagem da varredura de portões e a classificação dos achados tocados estão no SUMMARY.
  </acceptance_criteria>
  <done>O teto de custo e a injeção passam a ter um valor de provedor aceito pelo banco — o caminho de gravação que o 49-02 conserta tem onde cair.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: as 16 colunas nulas e o predicado único de vigente — aplicados, provados no catálogo, tipos e push</name>
  <files>supabase/migrations/20260922000002_p49_colunas_proveniencia_e_analise.sql, database.types.ts</files>
  <read_first>
    - `supabase/migrations/20260921000004_p48_analise_descartada_knockout.sql` (o analog: ADD COLUMN + CHECK nomeado + COMMENT por coluna + auto-verificação)
    - `supabase/migrations/20260921000001_p48_candidatura_encerrada.sql` (função SQL IMMUTABLE pequena, `SET search_path = ''`, ACL com `anon` NOMEADO, COMMENT)
    - `supabase/migrations/20260624000001_entrevista_cognitivo_tables.sql` (`:75-97` — `entrevista_analises` como nasceu: `status_analise text NOT NULL DEFAULT 'pendente_humano'` sem CHECK, `revisada_por uuid` sem FK)
    - `supabase/migrations/20260623100003_redacoes_candidato.sql` (`:87` — CHECK de `status_analise` da redação, que inclui `falhou`)
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md` §3 (e o ⚠ do nome `solicitada_por` × R2)
    - `docs/compliance/pii-inventory.yaml` (`:58-66` — lista de nomes da R2) e `docs/compliance/export-scope-rules.yaml` (`:385-400` — `ponteiros.de_terceiro`, que já contém `solicitado_por`)
  </read_first>
  <action>
Passo 1 — medir o vivo, só leitura: `information_schema.columns` das 5 tabelas (nome, tipo, `is_nullable`) e conferir que NENHUMA das colunas novas já existe; `pg_constraint` de `entrevista_analises` (confirmar que `status_analise` não tem CHECK e que `falhou` é gravável); e `select count(*) from public.entrevista_analises` (6 na pesquisa). Divergência = parar e reportar (D-49).

Passo 2 — escrever `supabase/migrations/20260922000002_p49_colunas_proveniencia_e_analise.sql` (cabeçalho do M8, com «O QUE ESTAVA ERRADO» medido, «POR QUE NULÁVEL» — D-55/Pitfall 3 —, «POR QUE text E NÃO O ENUM llm_provider» — o enum ganhou `none`, que não é provedor de resultado —, «POR QUE `solicitado_por`» — é o nome já coberto pela R2 do inventário e por `ponteiros.de_terceiro`, e um nome novo exigiria editar os dois ou vazaria o UUID de funcionário na cópia do titular —, nota de transporte). Conteúdo, nesta ordem:
(a) `ADD COLUMN provedor_ia text, ADD COLUMN modelo_ia text` em `public.analise_candidato_vaga`, `public.comparativo_solicitado`, `public.entrevista_guias`, `public.entrevista_analises`, `public.redacoes_candidato`; em cada uma, `ADD CONSTRAINT <tabela>_provedor_ia_check CHECK (provedor_ia IS NULL OR provedor_ia IN ('anthropic','openai'))`;
(b) `ALTER TABLE public.redacoes_candidato ADD COLUMN rubrica_versao text`;
(c) `ALTER TABLE public.entrevista_analises ADD COLUMN tipo text, ADD COLUMN solicitado_por uuid, ADD COLUMN texto_hash text, ADD COLUMN ai_call_log_id uuid, ADD COLUMN superada_em timestamptz` e `ADD CONSTRAINT entrevista_analises_tipo_check CHECK (tipo IS NULL OR tipo IN ('online','presencial'))`; SEM FK em `ai_call_log_id` (precedente `candidate_ai_decisions.ai_call_log_ids uuid[]`: o log é purgado em 180 d e redigido pelo motor) e SEM FK em `solicitado_por` (precedente `revisada_por`);
(d) `CREATE OR REPLACE FUNCTION public.entrevista_analise_vigente(p_superada_em timestamptz, p_status_analise text, p_competencias jsonb) RETURNS boolean LANGUAGE sql IMMUTABLE SET search_path = ''` com o corpo `p_superada_em IS NULL AND p_status_analise IS DISTINCT FROM 'falhou' AND p_competencias IS NOT NULL`; `REVOKE ALL … FROM PUBLIC, anon`; `GRANT EXECUTE … TO authenticated, service_role`; `COMMENT ON FUNCTION` dizendo que é a ÚNICA definição de vigente (D-39) e quem a chama;
(e) um `COMMENT ON COLUMN` por coluna nova no estilo do analog: fase/ID/decisão, o que NULL significa (proveniência: «desconhecida», D-30; `rubrica_versao`: «avaliada antes de a rubrica ser enviada», D-26; `tipo`: «análise anterior à Phase 49, tipo não registrado», RESEARCH Correção 10; `superada_em`: «vigente ou nunca vigente»; `texto_hash`: «= `ai_call_logs.input_hash` da chamada, sha256 do texto mascarado» D-38; `ai_call_log_id`: «linha do log que contém o texto mascarado; aponta para o vazio depois da purga de 180 d» D-38; `solicitado_por`: «funcionário que pediu a análise; UUID de funcionário, fora da cópia do titular»);
(f) `DO` de auto-verificação por catálogo: as 16 colunas existem com o tipo certo e `is_nullable = 'YES'`; as 6 CHECKs existem (`pg_constraint`); `entrevista_analise_vigente` tem `provolatile = 'i'` e `has_function_privilege('anon', 'public.entrevista_analise_vigente(timestamptz,text,jsonb)', 'EXECUTE')` = false; `select count(*) from public.entrevista_analises` não mudou (nenhuma linha tocada).

Passo 3 — aplicar: `node p46apply.cjs migrate supabase/migrations/20260922000002_p49_colunas_proveniencia_e_analise.sql`.

Passo 4 — tipos (mesmo comando da Task 1, com `< /dev/null`), conferir `provedor_ia`, `rubrica_versao`, `superada_em` e `entrevista_analise_vigente` no arquivo; portão `tsc` ≤ 90.

Passo 5 — publicar: commitar e `git push origin main`; conferir `git log --oneline origin/main..HEAD` vazio (leva junto os commits de docs da fase — Correção 1).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260922000002_p49_colunas_proveniencia_e_analise.sql','utf8');const c=s.split('\n').filter(l=>!/^\s*--/.test(l)).join('\n');if(/^\s*BEGIN\s*;/mi.test(c))throw new Error('wrapper BEGIN proibido');if(/ADD\s+COLUMN[^,;]*\bNOT\s+NULL/i.test(c))throw new Error('coluna NOT NULL proibida nesta onda (D-55)');for(const t of ['public.analise_candidato_vaga','public.comparativo_solicitado','public.entrevista_guias','public.entrevista_analises','public.redacoes_candidato','provedor_ia','modelo_ia','rubrica_versao','solicitado_por','texto_hash','ai_call_log_id','superada_em','entrevista_analises_tipo_check','CREATE OR REPLACE FUNCTION public.entrevista_analise_vigente','IMMUTABLE',\"SET search_path = ''\",'anon','GRANT EXECUTE','COMMENT ON COLUMN'])if(!c.includes(t))throw new Error('falta: '+t);const n=(c.match(/_provedor_ia_check/g)||[]).length;if(n<5)throw new Error('CHECK de provedor em '+n+' tabelas (esperado 5)');if(/ADD\s+COLUMN[^,;]*\bREFERENCES/i.test(c))throw new Error('FK proibida nas colunas novas (log purgado; UUID de funcionario)');console.log('OK')"</automated>
    <fails_when>non-zero exit with "falta:", "NOT NULL proibida", "CHECK de provedor em", "FK proibida" or "wrapper BEGIN proibido" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs migrate supabase/migrations/20260922000002_p49_colunas_proveniencia_e_analise.sql</automated>
    <fails_when>non-zero exit, or the output lacks the line "md5 do ledger BATE"</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node p46apply.cjs sql "SET TRANSACTION READ ONLY; select count(*)::int as n, count(*) filter (where is_nullable='YES')::int as nulas from information_schema.columns where table_schema='public' and ((column_name in ('provedor_ia','modelo_ia') and table_name in ('analise_candidato_vaga','comparativo_solicitado','entrevista_guias','entrevista_analises','redacoes_candidato')) or (table_name='redacoes_candidato' and column_name='rubrica_versao') or (table_name='entrevista_analises' and column_name in ('tipo','solicitado_por','texto_hash','ai_call_log_id','superada_em')))" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s)[0];if(r.n!==16||r.nulas!==16){console.error('colunas em PROD: '+r.n+' (nulas '+r.nulas+'), esperado 16/16');process.exit(1)}console.log('16 colunas nulas em PROD')})"</automated>
    <fails_when>non-zero exit with "colunas em PROD: N (nulas M), esperado 16/16" in stderr</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -s database.types.ts && grep -q "rubrica_versao" database.types.ts && grep -q "superada_em" database.types.ts && grep -q "entrevista_analise_vigente" database.types.ts && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; } && test -z "$(git log --oneline origin/main..HEAD)"</automated>
    <fails_when>non-zero exit; database.types.ts empty or missing a new column/function; lint exit code other than 0/2 or more than 90 "error TS" lines; or unpushed commits remain</fails_when>
  </verify>
  <acceptance_criteria>
    - Em PROD, as 16 colunas existem e todas são nuláveis (consulta acima = 16/16); as 6 CHECKs existem; `entrevista_analise_vigente` é IMMUTABLE e `anon` não a executa.
    - `select count(*) from public.entrevista_analises` depois do apply = o medido no Passo 1 (nenhuma linha tocada; a marcação das 6 é do plano 49-12, com checkpoint).
    - `database.types.ts` regenerado contém as colunas e a função; `tsc` ≤ 90.
    - `git log --oneline origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>Toda EF e toda tela desta fase têm onde gravar e ler a proveniência real, a versão da rubrica e o dono da análise — sem que nada do que roda hoje quebre.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF (service_role) → tabelas de resultado | só a EF escreve as colunas novas; nenhuma policy de escrita nova |
| `authenticated` → `entrevista_analise_vigente` | função pura, sem leitura de tabela |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-01-01 | Denial of Service | coluna NOT NULL derrubando a EF velha entre migration e deploy | high | mitigate | todas nulas; verify reprova `NOT NULL` no arquivo e a consulta exige 16/16 nulas em PROD |
| T-49-01-02 | Information Disclosure | UUID de funcionário (`solicitado_por`) saindo na cópia do titular | high | mitigate | nome já coberto pela R2 e por `ponteiros.de_terceiro`; veredito explícito no 49-17 |
| T-49-01-03 | Elevation of Privilege | função nova executável por `anon` | low | mitigate | `REVOKE … FROM PUBLIC, anon` nomeado; auto-verificação `has_function_privilege` |
| T-49-01-04 | Repudiation | linha antiga ganhando proveniência inventada | medium | mitigate | nenhuma escrita retroativa (D-30); NULL = desconhecida, documentado no COMMENT |
| T-49-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação — o plano só escreve SQL e regenera tipos |
</threat_model>

<verification>
- Duas migrations aplicadas, md5 do ledger conferido em cada uma.
- `enum_range(null::public.llm_provider)` com `none`; 16 colunas nulas; predicado de vigente IMMUTABLE e fechado para `anon`.
- `database.types.ts` regenerado; `tsc` ≤ 90; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
As ondas seguintes (ai-client, EFs, telas, motor, checklist LGPD) partem de um esquema que já existe em PROD, sem nenhuma coluna obrigatória que a EF de hoje não saiba preencher.
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `llm_provider` += `'none'` | valor de enum | **novo** |
| `analise_candidato_vaga.provedor_ia`, `.modelo_ia` (+ `analise_candidato_vaga_provedor_ia_check`) | colunas + CHECK | **novas** |
| `comparativo_solicitado.provedor_ia`, `.modelo_ia` (+ CHECK) | colunas + CHECK | **novas** |
| `entrevista_guias.provedor_ia`, `.modelo_ia` (+ CHECK) | colunas + CHECK | **novas** |
| `entrevista_analises.provedor_ia`, `.modelo_ia` (+ CHECK), `.tipo` (+ `entrevista_analises_tipo_check`), `.solicitado_por`, `.texto_hash`, `.ai_call_log_id`, `.superada_em` | colunas + CHECKs | **novas** |
| `redacoes_candidato.provedor_ia`, `.modelo_ia` (+ CHECK), `.rubrica_versao` | colunas + CHECK | **novas** |
| `public.entrevista_analise_vigente(timestamptz, text, jsonb)` | função SQL IMMUTABLE | **nova** |
| `supabase/migrations/20260922000001_p49_llm_provider_none.sql` | migration | **nova** |
| `supabase/migrations/20260922000002_p49_colunas_proveniencia_e_analise.sql` | migration | **nova** |
| `database.types.ts` | tipos gerados | regenerado |

## Premissas sinalizadas (probe de borda)

O probe determinístico classificou JORN-28, JORN-12 e JORN-07 como `unclassified` (texto em português) e
deu a JORN-39 as sondas `idempotency`/`concurrency`. As linhas `unclassified` ficam `unresolved` no
relatório do probe e são assumidas aqui só no que este plano toca: o esquema é aditivo e nulável. As
sondas de JORN-39 viram verdades do plano 49-02 (dono do comportamento de gravação), não deste.

Desvio registrado, com razão: o RESEARCH/PATTERNS chama a coluna do autor de `solicitada_por`; este plano
usa `solicitado_por` porque é o nome que a R2 do inventário e `ponteiros.de_terceiro` já cobrem — o risco
R2 do PATTERNS (UUID de funcionário na cópia) desaparece por construção, em vez de depender de duas
edições no plano 49-17.

Contato registrado, não consertado: entre este apply e o checklist do 49-17, a consulta de drift
`05-export-allowlist-drift.sql` acusa as 16 colunas como sem veredito. A cópia exporta por allowlist, então
elas NÃO saem (fail-safe) — o mesmo estado que a Phase 48 atravessou entre 48-11 e 48-17.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-01-SUMMARY.md` when done
</output>
