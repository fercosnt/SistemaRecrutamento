---
phase: 49-consertos-da-jornada-bloco-2
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/candidatura/proximaEtapa.ts
  - src/lib/candidatura/__tests__/proximaEtapa.test.ts
  - src/components/KanbanBoard.tsx
  - src/components/__tests__/KanbanBoard.test.tsx
  - src/features/hub-candidato/components/HubCandidatoRH.tsx
  - src/components/modals/UpdateStatusModal.tsx
  - src/components/modals/__tests__/UpdateStatusModal.test.tsx
autonomous: true
requirements: [JORN-33, JORN-34, JORN-25]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Existe UM util `proximaEtapaDeTrabalho(etapa)` em `src/lib/candidatura/proximaEtapa.ts`, com a lista das 6 etapas de trabalho na ordem do funil, que devolve a próxima etapa de trabalho (ou `undefined` em `decisao_final` e em etapa terminal/desconhecida). As duas cópias locais de `WORKING_STAGES` (`KanbanBoard.tsx:58`, `HubCandidatoRH.tsx:76`) passam a importá-lo — é o mesmo caminho que o «Avançar» do comparativo usará no plano 49-13 (D-36)."
    - "O Kanban trata encerrada pelo predicado canônico `candidaturaEncerrada(etapa, status)` (D-21): o card de `status='finalizado'` em etapa de trabalho (3 linhas em PROD — `triagem/finalizado`, `entrevista_online/finalizado`, `decisao_final/finalizado`) ganha o selo «Encerrada», não é arrastável e não mostra o menu Avançar/Retroceder/Rejeitar (JORN-33). Os selos Aprovado/Rejeitado de hoje continuam."
    - "Retirada a pedido NÃO é encerrada por esse predicado, por desenho: o card continua arrastável e com menu (invariante da Phase 45)."
    - "O `UpdateStatusModal` não oferece mais `rejeitado → em_analise` (reabrir só por status, sem histórico nem justificativa — JORN-34) nem `aprovado_proxima → finalizado` (encerrar só por status — D-67): `VALID_TRANSITIONS.rejeitado` vira lista vazia e `finalizado` sai de `aprovado_proxima`. Reabrir tem caminho próprio e auditado (`responder_revisao_decisao`, D-01). A defesa no banco é do plano 49-06."
    - "Nada aqui introduz portão de evidência no avanço (D8): o util só diz qual é a próxima etapa; quem decide aceitar é o trigger."
    - "`tsc` ≤ 90; build com o marcador; push; marcador no ar (D-52, D-53)."
  artifacts:
    - path: src/lib/candidatura/proximaEtapa.ts
      provides: "próxima etapa de trabalho — fonte única (Kanban, hub, comparativo)"
      contains: "export function proximaEtapaDeTrabalho"
    - path: src/components/KanbanBoard.tsx
      provides: "selo «Encerrada» pelo predicado canônico"
      contains: "kanban-selo-encerrada"
    - path: src/components/modals/UpdateStatusModal.tsx
      provides: "sem reabrir nem encerrar só por status"
      contains: "VALID_TRANSITIONS"
  key_links:
    - from: "src/lib/candidatura/proximaEtapa.ts"
      to: "KanbanBoard.tsx, HubCandidatoRH.tsx, ComparativoCandidatosPage.tsx (49-22)"
      via: "import do util"
      pattern: "proximaEtapaDeTrabalho\\("
    - from: "src/lib/candidatura/candidaturaEncerrada.ts"
      to: "KanbanBoard.getTerminalBadge"
      via: "predicado canônico"
      pattern: "candidaturaEncerrada\\("
  prohibitions:
    - requirement_id: JORN-33
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT introduzir portão de evidência no avanço — trava de encerrada não é exigência de evidência da etapa (D8)"
    - requirement_id: JORN-34
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT deixar tela alguma reabrir ou encerrar candidatura só mudando o status, sem trilha"
---

<objective>
O Kanban e o modal de status deixam de oferecer ação sobre candidatura encerrada, e a regra de «próxima
etapa de trabalho» passa a morar num lugar só.

Purpose: JORN-33 — o `getTerminalBadge` ignora `status='finalizado'` e o mesmo `!terminalBadge` libera o
arraste e o menu inteiro (RESEARCH Correção 22). JORN-34/D-67 — o modal reabre `rejeitado → em_analise` e
encerra `aprovado_proxima → finalizado` só por status, sem histórico (a etapa não muda, então o
`avancar_etapa` sai no early-return). D-36 — o «Avançar» do comparativo precisa do mesmo cálculo de próxima
etapa que o hub e o Kanban usam; hoje ele vive duplicado nos dois.

Output: util `proximaEtapa`, Kanban e hub usando o util, selo pelo predicado, modal sem as duas transições.
</objective>

<execution_context>
@/Users/fernando/.claude/gsd-core/workflows/execute-plan.md
@/Users/fernando/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-CONTEXT.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-PATTERNS.md
@.planning/phases/49-consertos-da-jornada-bloco-2/49-VARREDURA-KICKOFF.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): próxima etapa numa fonte só → Kanban com selo «Encerrada» pelo predicado → teste</name>
  <files>src/lib/candidatura/proximaEtapa.ts, src/lib/candidatura/__tests__/proximaEtapa.test.ts, src/components/KanbanBoard.tsx, src/components/__tests__/KanbanBoard.test.tsx, src/features/hub-candidato/components/HubCandidatoRH.tsx</files>
  <read_first>
    - `src/components/KanbanBoard.tsx` (`:50-120` `WORKING_STAGES`, `getTerminalBadge`, a função de coluna; `:170-200` menu e `canDrag`; `:298-316` o menu gateado por `!terminalBadge`)
    - `src/features/hub-candidato/components/HubCandidatoRH.tsx` (`:60-80` `TIMELINE`/`WORKING_STAGES`; `:132-160` `proximaEtapa` e `encerrada`)
    - `src/lib/candidatura/candidaturaEncerrada.ts` e seu teste (a forma de util + tabela-verdade)
    - `src/components/__tests__/KanbanBoard.test.tsx`
    - `src/features/triagem/services/triagemService.ts` (`:374` — `PROXIMA_ETAPA_APOS_TRIAGEM`, que o 49-22 substitui; e onde mora o tipo `EtapaFunilM2`)
  </read_first>
  <behavior>
    - `proximaEtapaDeTrabalho('inscricao')` = `'triagem'`; `('triagem')` = `'avaliacao_assincrona'`; `('entrevista_presencial')` = `'decisao_final'`; `('decisao_final')` = `undefined`; `('aprovado')`, `('rejeitado')`, `(null)`, `('desconhecida')` = `undefined`.
    - Kanban: card `{ etapa_atual: 'triagem', status: 'finalizado' }` mostra o selo «Encerrada» (`data-testid="kanban-selo-encerrada"`), não expõe o menu de ações e não é arrastável.
    - Kanban: card `{ etapa_atual: 'triagem', status: 'em_analise', encerrada_a_pedido_em: <data> }` (retirada a pedido) NÃO tem selo e mantém o menu.
    - Kanban: os selos «Aprovado» e «Rejeitado» de hoje continuam como estão.
  </behavior>
  <action>
Passo 1 — D-50: re-rodar o padrão P1 da C1 do `49-VARREDURA-KICKOFF.md` (escritores de etapa/status no cliente) e `grep -rn "WORKING_STAGES" src` — registrar no SUMMARY cada cópia local da lista e se ela decide «próxima etapa» (defeito de forma) ou só colunas (escopo).

Passo 2 — criar `src/lib/candidatura/proximaEtapa.ts`: `ETAPAS_DE_TRABALHO` (as 6, na ordem do funil: inscricao, triagem, avaliacao_assincrona, entrevista_online, entrevista_presencial, decisao_final; tipo `EtapaFunilM2` importado só como tipo) e `proximaEtapaDeTrabalho(etapa)`. Docblock: D-36 (o «Avançar» leva à próxima etapa REAL), quem usa, e que o servidor (`avancar_etapa`) é quem aceita ou recusa — o util não autoriza nada. Teste `it.each` do `<behavior>`.

Passo 3 — `KanbanBoard.tsx`: `WORKING_STAGES` passa a vir de `ETAPAS_DE_TRABALHO` (as colunas continuam as mesmas) e a `proximaEtapa` do menu passa a ser `proximaEtapaDeTrabalho(currentEtapa)`. `getTerminalBadge`: manter os ramos Aprovado e Rejeitado; acrescentar, depois deles, o ramo «Encerrada» quando `candidaturaEncerrada(etapa, status)` for verdadeiro e nenhum dos dois anteriores casou (o caso `finalizado` em etapa de trabalho), com classe neutra e `data-testid="kanban-selo-encerrada"` no pill; o tipo de `key` ganha `'encerrada'`. `canDrag` e o menu já dependem do selo — conferir que nada mais precisa mudar. Comentário com JORN-33 e a medição (3 linhas em PROD).

Passo 4 — `HubCandidatoRH.tsx`: trocar o cálculo local de `proximaEtapa` por `proximaEtapaDeTrabalho(etapaAtual)` (a lista `TIMELINE` da tela fica — ela desenha a linha do tempo, não decide o avanço). Testes do Kanban do `<behavior>`; rodar os testes do hub existentes (`hubAcoesEncerrada`, `hubAcoesEmTriagem`) como regressão.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/lib/candidatura src/components/__tests__/KanbanBoard.test.tsx src/features/hub-candidato</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && node -e "const fs=require('fs');for(const f of ['src/components/KanbanBoard.tsx','src/features/hub-candidato/components/HubCandidatoRH.tsx']){const s=fs.readFileSync(f,'utf8');if(!/proximaEtapaDeTrabalho\(/.test(s))throw new Error(f+' nao usa o util')}const k=fs.readFileSync('src/components/KanbanBoard.tsx','utf8');if(!/candidaturaEncerrada\(/.test(k))throw new Error('Kanban sem o predicado canonico');console.log('OK')" && { T=$(mktemp); npm run -s lint >"$T" 2>&1; RC=$?; C=$(grep -c "error TS" "$T"); rm -f "$T"; echo "lint exit=$RC tsc errors=$C"; { [ "$RC" -eq 0 ] && [ "$C" -eq 0 ]; } || { [ "$RC" -eq 2 ] && [ "$C" -ge 1 ] && [ "$C" -le 90 ]; }; }</automated>
    <fails_when>non-zero exit with "nao usa o util" or "sem o predicado canonico" in stderr; lint exit code other than 0/2 or more than 90 "error TS" lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `proximaEtapa.ts` existe com teste; Kanban e hub o importam.
    - O Kanban usa `candidaturaEncerrada(` e o card `finalizado` em etapa de trabalho tem selo, sem arraste e sem menu (teste); retirada a pedido segue com menu (teste).
    - Testes do hub existentes verdes; `tsc` ≤ 90.
  </acceptance_criteria>
  <done>No Kanban, uma candidatura que acabou aparece como encerrada e não pode ser empurrada para a frente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: o modal sem reabrir nem encerrar só por status (JORN-34, D-67) — e a publicação</name>
  <files>src/components/modals/UpdateStatusModal.tsx, src/components/modals/__tests__/UpdateStatusModal.test.tsx</files>
  <read_first>
    - `src/components/modals/UpdateStatusModal.tsx` (`:55-70` `VALID_TRANSITIONS`; `:150-165` o caminho de rejeitar pela RPC; o texto de ajuda consertado no 48-16)
    - `src/components/modals/__tests__/UpdateStatusModal.test.tsx`
    - `.planning/phases/49-consertos-da-jornada-bloco-2/49-VARREDURA-KICKOFF.md` C1 #10 e #11
  </read_first>
  <behavior>
    - Modal aberto com status atual `rejeitado`: nenhuma opção de novo status é oferecida (e a tela diz por quê: reabrir é pelo pedido de revisão).
    - Modal com status atual `aprovado_proxima`: oferece `em_analise` e `rejeitado`, NÃO `finalizado`.
    - `aguardando_resposta` e `em_analise` continuam oferecendo o que oferecem hoje.
  </behavior>
  <action>
`VALID_TRANSITIONS`: `rejeitado` passa a `[]` e `aprovado_proxima` perde `finalizado`, cada linha com comentário (JORN-34: status só não reabre, o caminho auditado é `responder_revisao_decisao`, D-01; D-67: encerrar só por status grava zero histórico — origem plausível das 3 linhas `finalizado` em etapa de trabalho). Quando a lista de transições do status atual for vazia, o modal mostra uma frase neutra explicando que o status não pode ser mudado por aqui (sem prometer reabertura ao candidato, sem «teste psicológico», sem endereço do canal de privacidade) em vez de um select vazio. Atualizar DE PROPÓSITO os testes que asseriam as transições antigas (comentário de proveniência) e acrescentar os do `<behavior>`.

Publicar (D-52): rodar `npx vitest run src/components src/lib src/features/hub-candidato src/__tests__/guards`, o portão `tsc`, `npm run build` e conferir `kanban-selo-encerrada` em `build/assets/` (anotar o chunk: a rota do Kanban é `/rh/*`, lazy); commitar; `git push origin main`; `origin/main..HEAD` vazio; marcador no ar pelo crawler.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/components src/lib src/features/hub-candidato src/__tests__/guards</automated>
    <fails_when>non-zero exit, or "failed" in the vitest summary</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npm run build >/dev/null && grep -rl "kanban-selo-encerrada" build/assets/</automated>
    <fails_when>non-zero exit (build failed, or no file in build/assets/ contains the marker)</fails_when>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && test -z "$(git log --oneline origin/main..HEAD)" && node -e "const M=process.argv[1],B='https://rh.beautysmile.com.br';(async()=>{const vis=new Set(),fila=['/'];let ok=false;while(fila.length&&vis.size<300){const p=fila.shift();if(vis.has(p))continue;vis.add(p);let t='';try{t=await (await fetch(B+p)).text()}catch{continue}if(t.includes(M)){ok=true;break}for(const m of t.matchAll(/(?:\/assets\/|\.\/)([\w.-]+\.js)/g))fila.push('/assets/'+m[1])}if(!ok){console.error('AUSENTE em PROD: '+M);process.exit(1)}console.log('PRESENTE em PROD: '+M)})()" "kanban-selo-encerrada"</automated>
    <fails_when>non-zero exit: unpushed commits remain, or "AUSENTE em PROD: kanban-selo-encerrada"</fails_when>
  </verify>
  <acceptance_criteria>
    - `VALID_TRANSITIONS.rejeitado` é vazio e `aprovado_proxima` não contém `finalizado` (teste).
    - O modal com lista vazia mostra a frase neutra, não um select vazio.
    - `vitest` das pastas tocadas e o guard de strings verdes; `tsc` ≤ 90; marcador no ar; `origin/main..HEAD` vazio.
  </acceptance_criteria>
  <done>Nenhuma tela do RH reabre ou encerra uma candidatura por um atalho que não deixa rastro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do RH → PostgREST `candidaturas` (policy `rh_avanca_etapa`) | a tela só deixa de OFERECER; o banco decide (plano 49-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-05-01 | Tampering | reabrir/encerrar por status sem trilha | high | mitigate | transições removidas do modal; defesa no banco escopada ao cliente no 49-06 |
| T-49-05-02 | Tampering | arrastar encerrada para a frente no Kanban | medium | mitigate | selo pelo predicado canônico gateia arraste e menu; trava no trigger (49-06) é a segunda camada |
| T-49-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | zero instalação |
</threat_model>

<verification>
- `vitest` de `src/components`, `src/lib`, `src/features/hub-candidato`, `src/__tests__/guards` verde; `tsc` ≤ 90.
- `kanban-selo-encerrada` no build e no ar; `origin/main..HEAD` vazio.
</verification>

<success_criteria>
Kanban, hub e (depois, no 49-22) comparativo dizem a mesma coisa sobre «qual é a próxima etapa» e «esta candidatura acabou».
</success_criteria>

## Artifacts this phase produces

| Símbolo / arquivo | Tipo | Novo/Editado |
|---|---|---|
| `src/lib/candidatura/proximaEtapa.ts` (`ETAPAS_DE_TRABALHO`, `proximaEtapaDeTrabalho`) | util | **novo** |
| `src/lib/candidatura/__tests__/proximaEtapa.test.ts` | teste | **novo** |
| `getTerminalBadge` ramo `'encerrada'` + `data-testid="kanban-selo-encerrada"` | componente | editado |
| `VALID_TRANSITIONS` (`rejeitado: []`, `aprovado_proxima` sem `finalizado`) | constante | editada |

## Premissas sinalizadas (probe de borda)

JORN-33, JORN-34 e JORN-25 saíram `unclassified` no probe (texto pt-BR) e ficam `unresolved` no relatório.
As bordas escritas aqui (finalizado em etapa de trabalho = encerrada; retirada a pedido não é) vêm do
predicado canônico da Phase 48 e da varredura C1 #9–#11.

<output>
Create `.planning/phases/49-consertos-da-jornada-bloco-2/49-05-SUMMARY.md` when done
</output>
