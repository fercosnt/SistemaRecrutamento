---
phase: 20-refino-rh-editar-guia-de-entrevista-seed-001
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - database.types.ts
autonomous: false
requirements: [ENTREV-08]
must_haves:
  truths:
    - "[BLOCKING] The 20-01 migration is applied to PROD via Supabase MCP apply_migration (bypasses 42601; no BEGIN/COMMIT wrapper) — Área 1 / RESEARCH Pitfall 4"
    - "After apply, the 6+1 SQL smokes PASS in a ROLLBACK fixture: candidato/RH-no-posse → 42501, RH-own → ok, admin bypass, upsert collapses to 1 row, dedup leaves 1, claim-says-rh-no-table-row → DENY (ENTREV-08)"
    - "database.types.ts (repo ROOT) is regenerated so save_entrevista_guia_edits + updated_at are known to TypeScript (RESEARCH Pitfall 5)"
    - "Live PROD round-trip (real RH edits) is NOT done here — deferred to Phase 21 (planning_context schema_push_requirement)"
  artifacts:
    - path: "database.types.ts"
      provides: "regenerated types including save_entrevista_guia_edits RPC + entrevista_guias.updated_at"
      contains: "save_entrevista_guia_edits"
  key_links:
    - from: "Supabase MCP apply_migration"
      to: "PROD entrevista_guias + save_entrevista_guia_edits"
      via: "apply_migration (no BEGIN/COMMIT, writes version row)"
      pattern: "apply_migration"
    - from: "npm run db:types"
      to: "database.types.ts"
      via: "Supabase CLI type generation"
      pattern: "save_entrevista_guia_edits"
---

<objective>
[BLOCKING] Apply the 20-01 migration to PROD (Supabase MCP `apply_migration`), run the SQL smokes against the live RPC in a ROLLBACK fixture, and regenerate `database.types.ts`. This is a PROD action — gate it.

Purpose: The RPC + UNIQUE arbiter + updated_at must exist in PROD before the service/hook (20-03) and the EF upsert (20-04) can compile against fresh types and target the conflict arbiter. The dedup also collapses today's duplicate `entrevista_guias` rows so the new UNIQUE constraint can be added without 23505.
Output: Live RPC + schema in PROD, smokes green, regenerated types. Live RH round-trip is Phase 21.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-RESEARCH.md
@.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-01-SUMMARY.md
@supabase/migrations/20260629000001_entrevista_guia_edits.sql
@.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/_smoke/save_entrevista_guia_edits.smoke.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply the migration to PROD via Supabase MCP apply_migration</name>
  <files>supabase/migrations/20260629000001_entrevista_guia_edits.sql (applied to PROD, not edited)</files>
  <action>[BLOCKING — PROD action, human-gated] Apply `supabase/migrations/20260629000001_entrevista_guia_edits.sql` (authored in 20-01) to PROD via Supabase MCP `apply_migration`, passing the migration name `20260629000001_entrevista_guia_edits` and the file SQL verbatim. Do NOT use `supabase db push --linked` (it 42601s on the `$$` body adjacent to REVOKE/GRANT) and do NOT add a BEGIN/COMMIT wrapper. Then run the four post-apply MCP execute_sql confirmations described in how-to-verify. Confirm with the user before running (apply_migration is gated).</action>
  <what-built>The migration `supabase/migrations/20260629000001_entrevista_guia_edits.sql` (authored in 20-01): dedup → updated_at → UNIQUE(candidatura_id,tipo) → `save_entrevista_guia_edits` SECURITY DEFINER RPC.</what-built>
  <how-to-verify>
    Apply via Supabase MCP `apply_migration` (the established M2 path — bypasses the 42601 multi-statement error, writes the version row itself). Pass the migration name `20260629000001_entrevista_guia_edits` and the file SQL verbatim. Do NOT use `supabase db push --linked`. Do NOT add a BEGIN/COMMIT wrapper.
    After apply, confirm in PROD via MCP `execute_sql`:
    1. `SELECT proname FROM pg_proc WHERE proname = 'save_entrevista_guia_edits'` returns the function.
    2. `SELECT conname FROM pg_constraint WHERE conname = 'entrevista_guias_candidatura_tipo_key'` returns the UNIQUE.
    3. `SELECT count(*) AS dupes FROM (SELECT candidatura_id, tipo, count(*) c FROM public.entrevista_guias GROUP BY 1,2 HAVING count(*) > 1) d` returns 0 (dedup worked).
    4. `SELECT column_name FROM information_schema.columns WHERE table_name='entrevista_guias' AND column_name='updated_at'` returns the column.
    This is a PROD action requiring your authorization (apply_migration is gated — confirm before running).
  </how-to-verify>
  <verify>
    <human-check>apply_migration succeeded (no 42601); the four MCP checks pass — pg_proc has the function, pg_constraint has the UNIQUE, dupes = 0, updated_at column present.</human-check>
  </verify>
  <acceptance_criteria>
    - migration applied via MCP apply_migration (NOT db push); version row written
    - pg_proc has save_entrevista_guia_edits; pg_constraint has the UNIQUE; dupes = 0; updated_at column present
    - no 42601 error (MCP path bypasses it)
  </acceptance_criteria>
  <done>Migration live in PROD via apply_migration; function + UNIQUE + updated_at present; zero duplicate rows; no 42601.</done>
  <resume-signal>Type "applied" once apply_migration succeeded and the four MCP checks pass, or describe the error.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run the SQL smokes against the live RPC (ROLLBACK fixture)</name>
  <read_first>.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/_smoke/save_entrevista_guia_edits.smoke.sql, .planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-RESEARCH.md §Code Examples (DENY smoke skeleton)</read_first>
  <files>(no repo files modified — smokes run via MCP execute_sql; results recorded in the SUMMARY)</files>
  <action>Fill the smoke script's placeholder UUIDs from real PROD seed data (a candidato user, an RH recrutador who owns a vaga with a candidatura, an RH recrutador who does NOT own it, an administrador) — query PROD via MCP `execute_sql` to find suitable rows (the funil E2E seed candidatura `a1dd4c42` + its vaga.created_by is a known anchor; MEMORY project-funil-e2e-seed-achados). Run each smoke case via MCP `execute_sql` inside a `BEGIN; ... ROLLBACK;` fixture so nothing persists. Assert: candidato → 42501; RH-without-ownership → 42501; RH-with-ownership → ok=true; administrador → ok=true (bypass); two saves of the same (candidatura_id, tipo) → exactly 1 row; the claim-says-rh-but-no-usuarios_rh-row identity → DENY (proves role-from-table). The dedup-leaves-1 case is already proven live by Task 1's check #3. Capture each result for the SUMMARY. If ANY DENY case unexpectedly authorizes (or any OK case denies), STOP — that is an ENTREV-08 security failure; report it.</action>
  <verify>
    <automated>echo "SQL smokes run via MCP execute_sql — results recorded in 20-02-SUMMARY.md; gate is all-PASS (see acceptance_criteria)"</automated>
    <human-check>All six smoke cases produced the expected outcome (3 DENY/forbidden, 3 OK, upsert→1 row), recorded in the SUMMARY with the actual PROD UUIDs used.</human-check>
  </verify>
  <acceptance_criteria>
    - candidato → ERROR insufficient_privilege (42501)
    - RH-without-ownership → ERROR insufficient_privilege (42501)
    - RH-with-ownership → ok=true; administrador → ok=true
    - two saves same (cand,tipo) → 1 row (upsert collapse)
    - claim-says-rh-no-usuarios_rh-row → DENY (role-from-table, not JWT)
    - every smoke ran inside BEGIN/ROLLBACK (zero persisted writes)
  </acceptance_criteria>
  <done>All six+1 SQL smoke cases pass against the live RPC; results recorded; no PROD data mutated.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate database.types.ts</name>
  <read_first>CLAUDE.md §Commands (npm run db:types), .planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-RESEARCH.md §Pitfall 5 (stale types)</read_first>
  <files>database.types.ts</files>
  <action>Run `npm run db:types` to regenerate `database.types.ts` at the repo ROOT (NOT src/types/ — MEMORY) so `save_entrevista_guia_edits` and `entrevista_guias.updated_at` are known to TypeScript. This unblocks 20-03 (service calls `supabase.rpc('save_entrevista_guia_edits', ...)`) and 20-04 (EF upsert) without `as never` casts. If the CLI cannot reach the project (auth), surface a [BLOCKING] auth gate; otherwise confirm the regenerated file contains the new RPC name. Then `npm run lint` to confirm the regen did not regress the tsc baseline beyond ≤258.</action>
  <verify>
    <automated>grep -q "save_entrevista_guia_edits" database.types.ts && echo TYPES_REGEN_OK</automated>
  </verify>
  <acceptance_criteria>
    - database.types.ts (repo root) contains `save_entrevista_guia_edits`
    - database.types.ts knows `entrevista_guias.updated_at`
    - `npm run lint` tsc baseline ≤ 258 (no new structural errors from the regen)
  </acceptance_criteria>
  <done>Types regenerated; new RPC + updated_at present; tsc baseline held at ≤258.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migration → PROD schema | DDL + a privileged write-path function are introduced to the live DB |
| smoke fixture → PROD data | smokes run against PROD; must be ROLLBACK-wrapped to avoid mutating live rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | Tampering (PROD data) | SQL smoke run | mitigate | every smoke wrapped in BEGIN/ROLLBACK; dedup DELETE is the migration's own (idempotent keep-latest) |
| T-20-07 | Elevation (the live RPC) | save_entrevista_guia_edits in PROD | mitigate | smokes prove DENY for candidato + RH-no-posse + claim-spoof BEFORE the UI ships (20-03/05) |
| T-20-08 | Info disclosure (apply errors) | apply_migration | mitigate | MCP apply path (no raw pooler error echo); version-row drift accepted as cosmetic (MEMORY Phase 11) |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | zero external packages this phase; `npm run db:types`/`lint` use already-installed CLI/tsc |
</threat_model>

<verification>
- Task 1 MCP checks confirm the function/constraint/updated_at exist and dupes = 0.
- Task 2 smokes are all-PASS (3 DENY, 3 OK, upsert→1 row) in ROLLBACK fixtures.
- Task 3 grep confirms the regenerated types contain the new RPC; tsc ≤258.
</verification>

<success_criteria>
- Migration live in PROD via MCP apply_migration; no 42601.
- All ENTREV-08 authz smokes pass against the live RPC.
- database.types.ts regenerated; downstream waves can compile.
- Live RH round-trip explicitly deferred to Phase 21.
</success_criteria>

<output>
Create `.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-02-SUMMARY.md` when done
</output>
