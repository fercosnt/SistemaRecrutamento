---
phase: 39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql
autonomous: true
requirements: [DISPATCH-01, DISPATCH-02, DISPATCH-03, DISPATCH-04]

must_haves:
  truths:
    - "Uma ÚNICA migration atômica contém o DROP dos 4 triggers n8n + 4 funções ANTES do CREATE dos 3 triggers novos — sem janela interna de double-send (D-10)"
    - "trg_notif_transicao dispara evento 'avanco' SÓ quando NEW.etapa_para='avaliacao_assincrona' e 'decisao' SÓ quando NEW.etapa_para IN ('aprovado','rejeitado') AND NEW.auto_rejeitado=false; qualquer outra etapa faz RETURN NEW sem dispatch (D-01/D-02)"
    - "trg_notif_confirmacao suprime o dispatch quando NEW.status='rejeitado' OR NEW.opcao_knockout_id IS NOT NULL (survivor-guard, D-03/D-05); trg_notif_convite sempre dispara e o body carrega NEW.id como agendamento_id (D-04)"
    - "cada uma das 3 funções é SECURITY DEFINER SET search_path='', lê project_url + edge_invoke_key do Vault, faz graceful-skip se NULL, envolve net.http_post em EXCEPTION WHEN OTHERS → RAISE WARNING → RETURN NEW (fail-open), header Bearer do Vault, body SÓ ids (D-11/DISPATCH-04)"
  artifacts:
    - path: "supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql"
      provides: "Migration atômica: DROP dos 4 triggers/funções n8n + CREATE de trg_notif_transicao/confirmacao/convite"
      contains: "trg_notif_transicao"
      min_lines: 130
  key_links:
    - from: "supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql"
      to: "vault.decrypted_secrets (project_url, edge_invoke_key)"
      via: "SELECT decrypted_secret INTO ... WHERE name IN ('project_url','edge_invoke_key')"
      pattern: "decrypted_secret"
    - from: "supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql"
      to: "EF notificar-candidato"
      via: "PERFORM net.http_post(url := v_project_url || '/functions/v1/notificar-candidato', ...)"
      pattern: "/functions/v1/notificar-candidato"
---

<objective>
Escrever a ÚNICA migration atômica que aposenta o n8n e cabeia os 3 triggers novos: DROP dos 4 triggers/funções n8n (SEC-03 x3 + o 4º de `candidatos`) + CREATE de 3 funções/triggers `AFTER INSERT` que auto-disparam a EF `notificar-candidato` via `net.http_post` com Bearer self-auth do Vault, corpo ids-only, graceful-skip + fail-open.

Purpose: trocar a topologia de disparo do funil numa transação atômica — sem janela interna de double-send — respaldada pelo `UNIQUE(dedupe_key)` já vivo (P37). Resolve SEC-03 por substituição (DISPATCH-01/02/03/04). Fase de MAIOR RISCO do milestone.
Output: 1 arquivo de migration. **Zero contato com PROD** — este plano só ESCREVE o arquivo; o apply em PROD é o plano GATED 39-04. O `npm run build` NÃO prova o rewire (tipos vêm do config, não do banco vivo): a prova é o smoke SQL (39-03) + a verificação pós-apply (39-04).

⚠ **SAFE-NOW.** Nenhuma tarefa aqui toca o banco. O executor NÃO roda `apply_migration`, `db push`, nem `psql` contra PROD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03/39-CONTEXT.md
@.planning/phases/39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03/39-RESEARCH.md
@supabase/migrations/20260610000002_analise_trigger.sql
@supabase/migrations/20260706110005_sec03_n8n_serverside.sql
@supabase/migrations/20260712100004_n8n_novo_candidato.sql

<interfaces>
<!-- CONTRATO TRAVADO deste plano. Os nomes de trigger/função são identificadores concretos
     NÃO-negociáveis (o smoke 39-03 e a verificação 39-04 asserem exatamente estes). O corpo é
     cópia verbatim do esqueleto do analise-trigger; muda só o predicado do guard e o body. -->

Projeto Supabase (PROD, só referência — NÃO tocar neste plano): isljnozzlvckrgjjbjwp
Arquivo a criar: supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql
Esqueleto verbatim do hop (copiar): 20260610000002_analise_trigger.sql:25-70 (Vault → graceful-skip → net.http_post Bearer).
Wrapper fail-open verbatim (copiar): 20260712100004_n8n_novo_candidato.sql:60-74 (BEGIN … EXCEPTION WHEN OTHERS → RAISE WARNING → END).
SEM wrapper `BEGIN; … COMMIT;` no topo do arquivo (D-13 / CLAUDE.md — o driver do CLI já envolve cada migration; o outer BEGIN/COMMIT é o gatilho do 42601).

BLOCO A — DROP (aposentadoria n8n, D-08). Ordem OBRIGATÓRIA trigger-antes-de-função (Pitfall 4;
`DROP FUNCTION` falha enquanto o trigger referencia). Nunca CASCADE.
  DROP TRIGGER IF EXISTS trg_n8n_nova_candidatura   ON public.candidaturas;
  DROP TRIGGER IF EXISTS trg_n8n_status_candidatura ON public.candidaturas;
  DROP TRIGGER IF EXISTS trg_n8n_revisao_decisao    ON public.decisao_final;
  DROP TRIGGER IF EXISTS trg_n8n_novo_candidato     ON public.candidatos;
  DROP FUNCTION IF EXISTS public.trg_n8n_nova_candidatura();
  DROP FUNCTION IF EXISTS public.trg_n8n_status_candidatura();
  DROP FUNCTION IF EXISTS public.trg_n8n_revisao_decisao();
  DROP FUNCTION IF EXISTS public.trg_n8n_novo_candidato();

BLOCO B — CREATE (nova topologia, Patterns 2/3/4 do RESEARCH). Vocabulário do evento IMUTÁVEL
(EventoLedger da EF, helpers.ts:11 / index.ts:58): "confirmacao" | "avanco" | "convite" | "decisao".

  function public.trg_notif_transicao()  → trigger trg_notif_transicao AFTER INSERT ON public.historico_candidatura
    CASE sobre NEW.etapa_para:
      = 'avaliacao_assincrona'                               → dispatch body {evento:'avanco',  candidatura_id: NEW.candidatura_id}
      IN ('aprovado','rejeitado') AND NEW.auto_rejeitado=false → dispatch body {evento:'decisao', candidatura_id: NEW.candidatura_id}
      ELSE                                                    → RETURN NEW (triagem/entrevistas/decisao_final: sem e-mail)

  function public.trg_notif_confirmacao() → trigger trg_notif_confirmacao AFTER INSERT ON public.candidaturas
    survivor-guard (verbatim do trg_candidaturas_analise, 20260610000002:37):
      IF NEW.status = 'rejeitado' OR NEW.opcao_knockout_id IS NOT NULL THEN RETURN NEW; END IF;
    dispatch body {evento:'confirmacao', candidatura_id: NEW.id}

  function public.trg_notif_convite()     → trigger trg_notif_convite AFTER INSERT ON public.agendamentos_entrevista
    sem predicado (sempre dispara)
    dispatch body {evento:'convite', candidatura_id: NEW.candidatura_id, agendamento_id: NEW.id}

Corpo comum de cada função (após o guard/CASE), IDS-ONLY (DISPATCH-04):
  SELECT decrypted_secret INTO v_project_url FROM vault.decrypted_secrets WHERE name='project_url';
  SELECT decrypted_secret INTO v_invoke_key  FROM vault.decrypted_secrets WHERE name='edge_invoke_key';
  IF v_project_url IS NULL OR v_invoke_key IS NULL THEN RETURN NEW; END IF;  -- graceful-skip
  BEGIN
    PERFORM net.http_post(
      url     := v_project_url || '/functions/v1/notificar-candidato',
      headers := jsonb_build_object('Content-Type','application/json','Authorization','Bearer '||v_invoke_key),
      body    := jsonb_build_object('evento', <str>, 'candidatura_id', <id> [, 'agendamento_id', NEW.id])
    );
  EXCEPTION WHEN OTHERS THEN
    RAISE WARNING 'trg_notif_<evento>: dispatch falhou (%: %) — funil intacto', SQLSTATE, SQLERRM;
  END;
  RETURN NEW;
Cada função termina com: REVOKE ALL ON FUNCTION public.trg_notif_<x>() FROM PUBLIC;

Enum etapa_processo (contexto): inscricao, triagem, avaliacao_assincrona, entrevista_online,
  entrevista_presencial, decisao_final, aprovado, rejeitado. Comparação com literal text coage
  ao enum pela coluna (funciona sob search_path='').

NÃO TOCAR (D-12): avancar_etapa(), trg_candidaturas_analise, guard_rejeicao_auditada,
  notify_cost_anomaly (cost-alerter, NÃO n8n — fora de escopo, não dropar).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bloco A (DROP dos 4 triggers/funções n8n) + trg_notif_transicao (CASE avanço+decisão)</name>
  <files>supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql</files>
  <read_first>
    - supabase/migrations/20260610000002_analise_trigger.sql — o esqueleto verbatim do hop (Vault → graceful-skip → net.http_post Bearer); linhas 25-70 são a base da função nova
    - supabase/migrations/20260706110005_sec03_n8n_serverside.sql — os 3 triggers/funções SEC-03 a dropar (nomes exatos + tabelas: candidaturas x2, decisao_final)
    - supabase/migrations/20260712100004_n8n_novo_candidato.sql — o 4º trigger a dropar (candidatos) + o wrapper fail-open `BEGIN … EXCEPTION WHEN OTHERS` (linhas 60-74) a replicar
    - .planning/phases/39-.../39-RESEARCH.md § "Pattern 4" + "Recommended migration structure" + "Pitfall 4" (ordem DROP TRIGGER→FUNCTION)
    - .planning/phases/39-.../39-CONTEXT.md § D-01, D-02, D-08 (predicados + escopo do DROP, PROD-verified)
  </read_first>
  <behavior>
    - Bloco A dropa EXATAMENTE 4 triggers + 4 funções (`trg_n8n_*`), na ordem trigger-antes-de-função, com `IF EXISTS`, sem CASCADE.
    - trg_notif_transicao dispara `avanco` só para `etapa_para='avaliacao_assincrona'`.
    - trg_notif_transicao dispara `decisao` só para `etapa_para IN ('aprovado','rejeitado') AND auto_rejeitado=false`.
    - Qualquer outra `etapa_para` (triagem, entrevistas, decisao_final, inscricao) → RETURN NEW, sem net.http_post.
    - Body do dispatch carrega SÓ `evento` + `candidatura_id` (= NEW.candidatura_id) — zero PII.
    - A função é SECURITY DEFINER SET search_path='', graceful-skip se Vault NULL, fail-open via EXCEPTION WHEN OTHERS.
  </behavior>
  <action>
    Criar o arquivo `supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql` com um cabeçalho-comentário (data 2026-07-26, phase 39, requirements DISPATCH-01/03, nota "SEM BEGIN/COMMIT wrapper — D-13/CLAUDE.md", nota "apply é o plano GATED 39-04 via MCP apply_migration; NÃO db push").

    Escrever o BLOCO A conforme o contrato `<interfaces>`: os 8 statements DROP na ordem trigger-antes-de-função (4 `DROP TRIGGER IF EXISTS … ON public.<tabela>` seguidos de 4 `DROP FUNCTION IF EXISTS public.…()`). Comentar cada par com a origem (SEC-03 `20260706110005` para os 3 primeiros; `20260712100004` para `trg_n8n_novo_candidato`).

    Escrever o BLOCO B — função `public.trg_notif_transicao()` — copiando o esqueleto de `20260610000002:25-70` e substituindo o guard por um `CASE`/`IF-ELSIF-ELSE` sobre `NEW.etapa_para` conforme D-01/D-02: ramo `= 'avaliacao_assincrona'` despacha `evento='avanco'`; ramo `IN ('aprovado','rejeitado') AND NEW.auto_rejeitado = false` despacha `evento='decisao'`; `ELSE RETURN NEW`. Em ambos os ramos que despacham, ler `project_url`+`edge_invoke_key` do Vault (graceful-skip se NULL), envolver o `PERFORM net.http_post` (url `… || '/functions/v1/notificar-candidato'`, header Bearer, body `jsonb_build_object('evento', <str>, 'candidatura_id', NEW.candidatura_id)`) no wrapper `BEGIN … EXCEPTION WHEN OTHERS THEN RAISE WARNING … END` (verbatim de `20260712100004:60-74`). Terminar com `RETURN NEW`, depois `REVOKE ALL ON FUNCTION public.trg_notif_transicao() FROM PUBLIC;` e um `COMMENT ON FUNCTION` em pt-BR descrevendo o CASE. Criar o trigger: `CREATE TRIGGER trg_notif_transicao AFTER INSERT ON public.historico_candidatura FOR EACH ROW EXECUTE FUNCTION public.trg_notif_transicao();` (precedido de `DROP TRIGGER IF EXISTS`).

    Fatorar a leitura do Vault + dispatch de forma que os dois ramos não dupliquem 30 linhas — pode centralizar o `SELECT decrypted_secret` no topo da função (após o `IF … ELSIF …` decidir o `v_evento`), desde que o `ELSE` faça `RETURN NEW` ANTES de qualquer leitura de Vault. Não inserir código fora do contrato.

    NÃO editar `avancar_etapa()` nem nenhum trigger existente além dos 4 `trg_n8n_*` no DROP.
  </action>
  <verify>
    <automated>f=supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql; test -f "$f" && [ "$(grep -c 'DROP TRIGGER IF EXISTS trg_n8n_' "$f")" = "4" ] && [ "$(grep -c 'DROP FUNCTION IF EXISTS public.trg_n8n_' "$f")" = "4" ] && grep -q "CREATE TRIGGER trg_notif_transicao" "$f" && grep -q "avaliacao_assincrona" "$f" && grep -q "auto_rejeitado = false" "$f" && grep -q "SECURITY DEFINER" "$f" && ! grep -qiE '^\s*(BEGIN|COMMIT)\s*;' "$f" && echo "T1 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DROP TRIGGER IF EXISTS trg_n8n_'` = 4 e `grep -c 'DROP FUNCTION IF EXISTS public.trg_n8n_'` = 4; nenhum `CASCADE`; cada `DROP TRIGGER` precede seu `DROP FUNCTION` no arquivo.
    - A função `trg_notif_transicao` contém os literais `avaliacao_assincrona`, `'aprovado'`, `'rejeitado'` e `auto_rejeitado = false`, e um ramo `ELSE`/fallback que faz `RETURN NEW`.
    - O body do dispatch contém `jsonb_build_object` com as chaves `'evento'` e `'candidatura_id'` (NEW.candidatura_id) e NENHUMA chave de PII (grep por `nome`, `email`, `cpf`, `telefone` no corpo da função = 0).
    - A função é `SECURITY DEFINER` + `SET search_path = ''`, lê `vault.decrypted_secrets` para `project_url` e `edge_invoke_key`, tem graceful-skip (`RETURN NEW` quando NULL) e o wrapper `EXCEPTION WHEN OTHERS THEN RAISE WARNING`.
    - O arquivo NÃO contém `BEGIN;`/`COMMIT;` de topo e NÃO usa `supabase db push`.
    - `avancar_etapa`, `trg_candidaturas_analise`, `notify_cost_anomaly` NÃO aparecem em nenhum DROP/CREATE/ALTER.
  </acceptance_criteria>
  <done>O Bloco A (aposentadoria dos 4 n8n) e a fonte canônica de transições (`trg_notif_transicao`, CASE avanço+decisão, ids-only, graceful-skip, fail-open) estão escritos no arquivo de migration — sem nenhum contato com PROD.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Satélites trg_notif_confirmacao (survivor-guard) + trg_notif_convite (agendamento_id)</name>
  <files>supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql</files>
  <read_first>
    - supabase/migrations/20260610000002_analise_trigger.sql:37 — o survivor-guard EXATO (`NEW.status='rejeitado' OR NEW.opcao_knockout_id IS NOT NULL`) a copiar para trg_notif_confirmacao
    - supabase/functions/notificar-candidato/index.ts:107-113 — a EF EXIGE agendamento_id quando evento='convite' (o body do convite tem de carregá-lo)
    - .planning/phases/39-.../39-RESEARCH.md § "Pattern 2" + "Pattern 3" (predicados + bodies verificados) + "Pitfall 2" (fail-open)
    - .planning/phases/39-.../39-CONTEXT.md § D-03, D-04, D-05 (survivor-guard, convite carrega agendamento_id, knockout não recebe e-mail)
    - o próprio arquivo criado na Task 1 (append no MESMO arquivo, mantendo o esqueleto/estilo)
  </read_first>
  <behavior>
    - trg_notif_confirmacao NÃO despacha quando NEW.status='rejeitado' OR NEW.opcao_knockout_id IS NOT NULL (knockout suprimido); despacha `confirmacao` com candidatura_id=NEW.id para survivors.
    - trg_notif_convite despacha SEMPRE `convite` com candidatura_id=NEW.candidatura_id e agendamento_id=NEW.id.
    - Ambas as funções: SECURITY DEFINER SET search_path='', graceful-skip, fail-open, body ids-only.
    - Os triggers são AFTER INSERT nas tabelas certas (candidaturas / agendamentos_entrevista) e coexistem (ortogonais) com os triggers já vivos.
  </behavior>
  <action>
    Fazer append no MESMO arquivo `20260726000001_p39_rewire_triggers_aposenta_n8n.sql` (após o Bloco B da Task 1) das duas funções/triggers satélites, cada uma copiando o esqueleto comum (Vault → graceful-skip → BEGIN/EXCEPTION/END fail-open → RETURN NEW → REVOKE ALL → COMMENT pt-BR):

    (1) `public.trg_notif_confirmacao()`: primeiro statement do corpo é o survivor-guard verbatim de `20260610000002:37` — `IF NEW.status = 'rejeitado' OR NEW.opcao_knockout_id IS NOT NULL THEN RETURN NEW; END IF;` (comentar "knockout: confirmação suprimida — D-03/D-05"). Depois o dispatch com body `jsonb_build_object('evento','confirmacao','candidatura_id', NEW.id)`. Trigger `CREATE TRIGGER trg_notif_confirmacao AFTER INSERT ON public.candidaturas FOR EACH ROW EXECUTE FUNCTION public.trg_notif_confirmacao();` (precedido de DROP TRIGGER IF EXISTS). Comentar no COMMENT que coexiste ortogonalmente com `trg_candidaturas_analise`.

    (2) `public.trg_notif_convite()`: sem predicado (dispara sempre). Dispatch body `jsonb_build_object('evento','convite','candidatura_id', NEW.candidatura_id, 'agendamento_id', NEW.id)`. Trigger `CREATE TRIGGER trg_notif_convite AFTER INSERT ON public.agendamentos_entrevista FOR EACH ROW EXECUTE FUNCTION public.trg_notif_convite();` (precedido de DROP TRIGGER IF EXISTS).

    Não redeclarar o Bloco A. Manter os nomes de trigger/função EXATOS do contrato (`trg_notif_confirmacao`, `trg_notif_convite`).
  </action>
  <verify>
    <automated>f=supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql; grep -q "CREATE TRIGGER trg_notif_confirmacao" "$f" && grep -q "CREATE TRIGGER trg_notif_convite" "$f" && grep -q "opcao_knockout_id IS NOT NULL" "$f" && grep -q "'agendamento_id', NEW.id" "$f" && [ "$(grep -c 'CREATE OR REPLACE FUNCTION public.trg_notif_' "$f")" = "3" ] && [ "$(grep -c 'REVOKE ALL ON FUNCTION public.trg_notif_' "$f")" = "3" ] && echo "T2 OK"</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo agora define EXATAMENTE 3 funções `trg_notif_*` (`grep -c 'CREATE OR REPLACE FUNCTION public.trg_notif_'` = 3) e 3 triggers `CREATE TRIGGER trg_notif_*`.
    - trg_notif_confirmacao contém o survivor-guard `NEW.status = 'rejeitado' OR NEW.opcao_knockout_id IS NOT NULL` com `RETURN NEW`, e o body `('evento','confirmacao','candidatura_id', NEW.id)`.
    - trg_notif_convite NÃO tem predicado de guard e o body contém `'agendamento_id', NEW.id` além de `'candidatura_id', NEW.candidatura_id`.
    - As 3 funções têm `REVOKE ALL … FROM PUBLIC` e `COMMENT ON FUNCTION` em pt-BR.
    - Nenhum body de função contém chave de PII (grep por nome/email/cpf/telefone nos corpos = 0).
    - Os triggers são `AFTER INSERT` em `public.candidaturas` e `public.agendamentos_entrevista` respectivamente; nenhum trigger já-vivo foi redefinido.
  </acceptance_criteria>
  <done>A migration está completa: DROP dos 4 n8n + os 3 triggers novos (transição CASE + confirmação survivor-guarded + convite com agendamento_id), todos ids-only/graceful-skip/fail-open, numa única migration atômica pronta para o apply GATED do 39-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trigger (SECURITY DEFINER, lê Vault) → EF `notificar-candidato` | O hop self-auth; nem o trigger nem a EF confiam no cliente |
| write do funil (INSERT candidatura/histórico/agendamento) → dispatch async | O dispatch NUNCA pode abortar/reverter o write do funil |
| body do net.http_post → rede | Só ids podem cruzar (zero PII) — o payload n8n antigo carregava PII (motivo do SEC-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01-01 | Spoofing | EF de envio invocável sem auth (spoof de e-mail a candidato) | mitigate | Header `Authorization: Bearer <edge_invoke_key do Vault>` em cada `net.http_post`; a EF valida `Bearer==service_role` (`--no-verify-jwt` + validação própria) |
| T-39-01-02 | Information Disclosure | PII no payload do dispatch | mitigate | Body = `jsonb_build_object` só com `evento` + ids; acceptance grep-guarda contra nome/email/cpf/telefone nos corpos das 3 funções |
| T-39-01-03 | Denial of Service (do funil) | Dispatch falha e reverte o INSERT/UPDATE do funil | mitigate | graceful-skip (`RETURN NEW` se Vault NULL) + `BEGIN … EXCEPTION WHEN OTHERS THEN RAISE WARNING … END` (fail-open) em cada função (Pitfall 2) |
| T-39-01-04 | Tampering/Repudiation | Double-send (n8n vivo + trigger novo coexistindo) | mitigate | DROP dos 4 `trg_n8n_*` + CREATE dos 3 na MESMA migration atômica (D-10); `UNIQUE(dedupe_key)` durável da P37 é o cinto |
| T-39-01-05 | Information Disclosure | Segredo/URL hardcoded (o bug que aposentamos) | mitigate | `project_url` + `edge_invoke_key` SÓ do Vault (`vault.decrypted_secrets`), nunca hardcoded, nunca logado; `SET search_path=''` + refs qualificadas (Pitfall 6) |
| T-39-01-06 | Elevation of Privilege | Objeto homônimo em schema de atacante resolvido pela função DEFINER | mitigate | `SECURITY DEFINER SET search_path=''` + tudo qualificado (`vault.`, `net.`, `public.`) + `REVOKE ALL … FROM PUBLIC` |
</threat_model>

<verification>
- `grep -c 'DROP TRIGGER IF EXISTS trg_n8n_'` = 4 e `grep -c 'DROP FUNCTION IF EXISTS public.trg_n8n_'` = 4 (ordem trigger→função)
- `grep -c 'CREATE OR REPLACE FUNCTION public.trg_notif_'` = 3 e 3 `CREATE TRIGGER trg_notif_*`
- Predicados presentes: `avaliacao_assincrona`, `auto_rejeitado = false`, survivor-guard `opcao_knockout_id IS NOT NULL`, convite `'agendamento_id', NEW.id`
- Cada função: `SECURITY DEFINER`, `SET search_path = ''`, graceful-skip, EXCEPTION WHEN OTHERS, REVOKE ALL, body ids-only
- Sem `BEGIN;/COMMIT;` de topo; sem `db push`; `avancar_etapa`/`notify_cost_anomaly` intocados
- NÃO há apply em PROD neste plano
</verification>

<success_criteria>
1. Uma única migration atômica aposenta os 4 triggers/funções n8n e cria os 3 triggers canônicos.
2. Os 4 predicados (D-01..D-04) estão codificados exatamente: avanço só em `avaliacao_assincrona`, decisão só humana em aprovado/rejeitado, confirmação survivor-guarded, convite com agendamento_id.
3. O hop é self-auth Bearer do Vault, ids-only, graceful-skip + fail-open — o funil nunca bloqueia e nenhuma PII trafega.
4. Zero contato com PROD; o arquivo está pronto para o smoke (39-03) e o apply GATED (39-04).
</success_criteria>

<output>
Create `.planning/phases/39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03/39-01-SUMMARY.md` when done
</output>
