---
phase: 24-blindagem-de-seguran-a-pii-lgpd
plan: 07
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - supabase/migrations/20260706110008_ux08_o6_deactivate.sql
  - supabase/functions/_shared/bigfive-scoring.ts
  - supabase/functions/_shared/bigfive-scoring.test.ts
  - supabase/functions/submit-bigfive-final/index.ts
  - src/features/avaliacao/schemas/bigfiveSchema.ts
  - src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx
autonomous: true             # code-local authoring; DB migration applies 24-08, submit EF redeploys 24-09
requirements: [UX-08]

must_haves:
  truths:
    - "UX-08: the 4 political O6 Big Five items {28,58,88,118} (all dimensao='O', faceta 28 'Liberalism/Values') are deactivated from the item bank (ativo=false flag, reversible for the M5 authoral replacement) and get_bigfive_itens filters WHERE ativo — per CONTEXT UX-08 decision (remove exposure without breaking the calculation; authoral replacement deferred to M5)"
    - "The scorer (bigfive-scoring.ts) does NOT throw on 116 items: count guard 120→116 (or derive from response length), REVERSED drops 88 and 118 (O reversed 12→10, total 55→53), and the O domain is prorated ×6/5 so the wired Johnson norm stays valid — only O is prorated, N/E/A/C untouched (CONTEXT discretion: prorate over 5 facets, preserve the Johnson norm)"
    - "The 120-count invariant is distributed across 6 sites and ALL move in lockstep (RESEARCH Pitfall 3): scorer, submit-bigfive-final validateBody (active-id-set, non-contiguous ids), bigfiveSchema (BIGFIVE_TOTAL_ITENS + isAllAnswered/countAnswered loops), BigFiveQuestionnaireScreen copy, and the golden test"
    - "submit-bigfive-final validates coverage against the ACTIVE id set (non-contiguous — gaps at 28/58/88/118), not a 1..120 range; it bundles the scorer so it redeploys in 24-09"
    - "Scoring stays non-eliminatory — nothing auto-rejects on a trait/percentile (RNF-07a); percentile stays contextual (bands, not raw — UX-07 already shipped)"
  artifacts:
    - path: "supabase/migrations/20260706110008_ux08_o6_deactivate.sql"
      provides: "ativo flag + deactivate {28,58,88,118} + get_bigfive_itens filters ativo"
      contains: "28,58,88,118"
    - path: "supabase/functions/_shared/bigfive-scoring.ts"
      provides: "116-item scorer with O ×6/5 prorate"
      contains: "116"
    - path: "supabase/functions/_shared/bigfive-scoring.test.ts"
      provides: "golden test at 116 items / 53 reversed + prorate assertion"
      contains: "116"
  key_links:
    - from: "get_bigfive_itens"
      to: "ativo filter"
      via: "WHERE b.ativo (returns 116)"
      pattern: "WHERE b.ativo"
    - from: "submit-bigfive-final validateBody"
      to: "active-id-set coverage"
      via: "non-contiguous id check, not 1..120"
      pattern: "116|active"
    - from: "scorer domain O"
      to: "prorated ×6/5"
      via: "domainRaw.O = round(O * 6 / 5)"
      pattern: "6 / 5|6/5"
---

<objective>
Remove the LGPD special-category exposure of the 4 political-opinion O6 Big Five items {28,58,88,118} WITHOUT breaking the psychometric calculation. This is the highest-risk plan of the phase: the "120 items" constant is duplicated across 6 files and the scorer throws if it does not see exactly 120 contiguous ids. Removing the 4 items makes the set 116 AND non-contiguous — every count guard and every `for id=1..120` loop must move in lockstep, and the O domain (now 5 facets) must be prorated ×6/5 so the wired Johnson norm stays valid. This plan is isolated (RESEARCH §waves E) and lands its 6 changes together.

Purpose: A candidate never sees the political items; the Big Five submit + scorer accept 116 items and produce a valid O percentile via prorate; the golden test proves 116/53/prorate. The authoral O6 replacement is M5.
Output: 1 DB migration, scorer + golden test, submit EF validateBody, schema, component copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

All 6 changes are authored here in ONE plan (Pitfall 3 — land in lockstep). The DB migration applies in 24-08; `submit-bigfive-final` (which bundles `_shared/bigfive-scoring.ts`) REDEPLOYS in 24-09 (bundle-freeze). Use a reversible `ativo` flag, NOT a hard DELETE (M5 flips it back). NO outer BEGIN/COMMIT in the migration.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-RESEARCH.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-PATTERNS.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-LIVE-STATE.md

<interfaces>
<!-- The 6 lockstep sites (RESEARCH §8 lines 369-401 · PATTERNS §Wave E lines 283-326) -->
DB + RPC:        20260612000001_bigfive_itens.sql:200-212 (get_bigfive_itens) — items {28,58,88,118} at seed L100,130,160,190; add `ativo` (24-01 noted if it exists)
Scorer:          _shared/bigfive-scoring.ts — REVERSED set L34-45 (O row L40 has 88 & 118), count guard L237 (120), domain sum ~L257-260, facetas output ~L272-275
Submit EF:       submit-bigfive-final/index.ts:82-107 validateBody (Object.keys !== 120 at :97; for id=1..120 at :99) — REDEPLOY in 24-09
Client schema:   bigfiveSchema.ts:30 (BIGFIVE_TOTAL_ITENS=120), :62-72 (isAllAnswered/countAnswered loop 1..BIGFIVE_TOTAL_ITENS)
Client copy:     BigFiveQuestionnaireScreen.tsx:133 ("{n}/120"), :267 ("120 afirmações"), :280 ("São 120 afirmações...")
Golden test:     _shared/bigfive-scoring.test.ts (55 reversed / 120 asserts + NEUTRAL_VECTOR loop)
Prorate math:    domainRaw.O = Math.round(domainRaw.O * 6 / 5)  // only O; N/E/A/C untouched (Johnson norm preserved)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration — ativo flag, deactivate {28,58,88,118}, get_bigfive_itens filters ativo</name>
  <files>supabase/migrations/20260706110008_ux08_o6_deactivate.sql</files>
  <read_first>
    - RESEARCH §8 (lines 369-381) — the reversible ativo approach + get_bigfive_itens filter
    - `supabase/migrations/20260612000001_bigfive_itens.sql:200-212` (get_bigfive_itens) + seed rows for 28/58/88/118 (L100,130,160,190)
    - 24-LIVE-STATE.md (24-01 Task 2 — bigfive_itens count=120; whether `ativo` already exists; {28,58,88,118} all dimensao='O')
  </read_first>
  <action>
    Author `20260706110008_ux08_o6_deactivate.sql`: `ALTER TABLE public.bigfive_itens ADD COLUMN IF NOT EXISTS ativo boolean NOT NULL DEFAULT true`; `UPDATE public.bigfive_itens SET ativo = false WHERE item_id IN (28,58,88,118)`; recreate `public.get_bigfive_itens()` (same signature RETURNS TABLE(item_id int, texto text, ordem int), LANGUAGE sql SECURITY DEFINER SET search_path = '') adding `WHERE b.ativo` and keeping ORDER BY b.ordem + the REVOKE ALL FROM PUBLIC / GRANT EXECUTE TO authenticated. Reversible by design (M5 flips ativo back for the authoral replacement). No outer BEGIN/COMMIT. Do NOT hard-DELETE the rows.
  </action>
  <acceptance_criteria>
    - Migration adds `ativo` (IF NOT EXISTS) and sets ativo=false for exactly item_id IN (28,58,88,118)
    - get_bigfive_itens recreated with `WHERE b.ativo`, same projection (item_id, texto, ordem), SECURITY DEFINER, SET search_path='', REVOKE/GRANT intact
    - No hard DELETE; no outer BEGIN/COMMIT; writes nothing to candidaturas (RNF-07a)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "28,58,88,118\|28, 58, 88, 118" supabase/migrations/20260706110008_ux08_o6_deactivate.sql ⇒ ≥1; grep -c "WHERE b.ativo\|ADD COLUMN IF NOT EXISTS ativo" ⇒ ≥2; grep -c "DELETE FROM public.bigfive_itens" ⇒ 0. get_bigfive_itens=116 smoke runs in 24-08.</automated>
  </verify>
  <done>4 political items deactivated (reversible); get_bigfive_itens returns 116; answer bank hardened.</done>
</task>

<task type="auto">
  <name>Task 2: Scorer 116-item + O prorate + golden test</name>
  <files>supabase/functions/_shared/bigfive-scoring.ts, supabase/functions/_shared/bigfive-scoring.test.ts</files>
  <read_first>
    - RESEARCH §8 (lines 383-401) scorer changes + Pitfall 3
    - PATTERNS §Wave E scorer block (lines 301-326)
    - `_shared/bigfive-scoring.ts:34-45` (REVERSED, O row L40), `:235-243` (count guard), `:257-260` (domain sum), `:272-275` (facetas output)
    - `_shared/bigfive-scoring.test.ts` (current 55-reversed / 120 golden asserts + NEUTRAL_VECTOR)
  </read_first>
  <action>
    Edit `bigfive-scoring.ts`: (1) change the count guard from 120 to 116 (or derive from response length so it accepts exactly the active set); keep the per-id 1..120 integer validity range (ids stay within 1..120, just non-contiguous) OR validate ids against the active set. (2) Remove 88 and 118 from the REVERSED O row (O reversed 12→10, total 55→53). (3) After summing facetRaw into domainRaw, add `domainRaw.O = Math.round(domainRaw.O * 6 / 5)` — ONLY O; leave N/E/A/C untouched — so the wired 24-item Johnson O norm stays valid (documented V1 prorate stopgap; M5 restores a real O6 facet). (4) facet-28 output may stay raw 0 (devolutiva uses domain-level paginas — cosmetic). Preserve every other constant verbatim. Then retarget `bigfive-scoring.test.ts`: golden counts 116 items / 53 reversed; NEUTRAL_VECTOR loop skips {28,58,88,118}; add an explicit assertion that O is prorated ×6/5 (e.g. a neutral all-3s vector yields the expected prorated O domain raw). Keep the norm/percentile assertions honoring the Johnson norm.
  </action>
  <acceptance_criteria>
    - Scorer count guard is 116 (or response-length-derived); accepts a 116-item non-contiguous vector without throwing
    - REVERSED no longer contains 88 or 118; total reversed count is 53
    - domainRaw.O is prorated ×6/5; N, E, A, C are NOT prorated
    - Golden test asserts 116 items, 53 reversed, and the O prorate; passes under deno test
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/_shared/bigfive-scoring.test.ts ⇒ green; grep -c "6 / 5\|6/5" supabase/functions/_shared/bigfive-scoring.ts ⇒ ≥1; grep -c "116" supabase/functions/_shared/bigfive-scoring.ts ⇒ ≥1; grep -c "\b88\b\|\b118\b" the REVERSED O row ⇒ 0.</automated>
  </verify>
  <done>Scorer computes on 116 items with O prorated; Johnson norm preserved; golden test green.</done>
</task>

<task type="auto">
  <name>Task 3: submit-bigfive-final active-set validate + client schema/copy lockstep</name>
  <files>supabase/functions/submit-bigfive-final/index.ts, src/features/avaliacao/schemas/bigfiveSchema.ts, src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx</files>
  <read_first>
    - RESEARCH §8 "Downstream lockstep" (lines 396-401) + Pitfall 3 (non-contiguous ids)
    - `supabase/functions/submit-bigfive-final/index.ts:82-107` (validateBody: Object.keys !== 120 at :97, for id=1..120 at :99)
    - `src/features/avaliacao/schemas/bigfiveSchema.ts:30,62-72` (BIGFIVE_TOTAL_ITENS, isAllAnswered/countAnswered loops)
    - `src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx:133,267,280` (count copy)
  </read_first>
  <action>
    Edit `submit-bigfive-final/index.ts` validateBody: replace the `Object.keys(rec).length !== 120` check and the `for (let id=1; id<=120; id++)` contiguity loop with an ACTIVE-id-set coverage check (the ids are non-contiguous — gaps at 28/58/88/118). RESEARCH recommends deriving the active set from the bank server-side (a select of active item_ids) so the EF never needs re-touching when M5 re-adds items; if a DB round-trip is undesirable, use a shared `EXCLUDED_ITEM_IDS = {28,58,88,118}` + count 116. Keep the `.strict()`/1-5 range checks. This EF redeploys in 24-09 (bundles the scorer). Edit `bigfiveSchema.ts`: drive `isAllAnswered`/`countAnswered` off the loaded items (which come from get_bigfive_itens = 116) rather than iterating `1..BIGFIVE_TOTAL_ITENS`; update BIGFIVE_TOTAL_ITENS to 116 or make the gate "all loaded items answered". Edit `BigFiveQuestionnaireScreen.tsx:133,267,280`: replace the hardcoded "120" copy ("120 afirmações", "São 120 afirmações...", "{n}/120") with a dynamic count derived from the loaded items.
  </action>
  <acceptance_criteria>
    - submit-bigfive-final validateBody accepts exactly the 116 active ids (non-contiguous) and rejects a 120-item or wrong-coverage body; no `1..120` contiguity loop remains
    - bigfiveSchema isAllAnswered/countAnswered are driven off loaded/active items, not a literal 1..120 range; BIGFIVE_TOTAL_ITENS reflects 116 (or the gate is loaded-item-based)
    - BigFiveQuestionnaireScreen shows a dynamic count (no hardcoded "120 afirmações")
    - tsc ≤ 133; client Vitest for the schema passes
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/submit-bigfive-final ⇒ green; grep -c "for (let id = 1; id <= 120" supabase/functions/submit-bigfive-final/index.ts ⇒ 0; grep -c "120 afirma" src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx ⇒ 0; npm run test:run -- bigfiveSchema ⇒ green; npm run lint ⇒ ≤133. Live 116-item submit smoke runs in 24-09.</automated>
  </verify>
  <done>Submit EF + client schema + copy all count 116 in lockstep; no contiguous-range assumption survives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate → Big Five instrument | political-opinion (LGPD special-category) O6 items presented to the subject |
| 120-count invariant | distributed across 6 sites — a partial edit silently corrupts scores or 400s every submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07-01 | Information Disclosure / Compliance | 4 political O6 items exposed to the candidate | mitigate | deactivate {28,58,88,118} (ativo=false); get_bigfive_itens filters ativo → 116 |
| T-24-07-02 | Tampering (integrity) | scorer throws / O percentile skews on 116 items | mitigate | count 116 + REVERSED 53 + O prorate ×6/5 (Johnson norm preserved); golden test green |
| T-24-07-03 | Denial of Service | every Big Five submit 400s (partial count edit) | mitigate | all 6 lockstep sites edited in one plan; active-id-set validation (non-contiguous); submit redeploy 24-09 |
| T-24-07-04 | (RNF-07a) | scoring becomes eliminatory | accept/mitigate | non-eliminatory preserved; no auto-reject on trait/percentile; bands (UX-07) unchanged |
| T-24-07-SC | Tampering | package installs | accept | zero new packages (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- get_bigfive_itens filters ativo (116); scorer + golden test green at 116/53/prorate
- submit-bigfive-final + schema + copy all count-lockstep; no 1..120 loop; tsc ≤ 133
- Non-eliminatory scoring preserved (RNF-07a)
</verification>

<success_criteria>
- UX-08: 4 political items removed from administration (reversible)
- Scorer/instrument work on 116 items with O prorated (Johnson norm intact)
- All 6 count sites move together; golden test proves it
</success_criteria>

<output>
Create `.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-07-SUMMARY.md` when done.
</output>
