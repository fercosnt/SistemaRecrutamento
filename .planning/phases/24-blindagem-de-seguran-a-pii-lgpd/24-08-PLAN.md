---
phase: 24-blindagem-de-seguran-a-pii-lgpd
plan: 08
type: execute
wave: 3
depends_on: [24-02, 24-03, 24-04, 24-05, 24-06, 24-07]
files_modified:
  - .planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-PROD-LANDING.md
autonomous: false            # [BLOCKING] applies all Phase-24 migrations to PROD via Supabase MCP + creates Vault secret + runs live SQL smokes
requirements: [SEC-01, SEC-02, SEC-03, SEC-05, SEC-06, SEC-07, SEC-08, SEC-09, SEC-10, UX-08]
user_setup:
  - service: n8n (personal webhook)
    why: "SEC-03 moves the webhook dispatch server-side; the URL must live in a Vault secret, never in the bundle"
    env_vars:
      - name: n8n_webhook_base (Vault secret)
        source: "Existing n8n workflow URLs (fernandocosta.app.n8n.cloud/webhook/{nova-candidatura,status-candidatura,revisao-decisao}) — Fernando confirms/rotates the value before it is stored in Vault"

must_haves:
  truths:
    - "All 8 Phase-24 migration FILES are applied to PROD via Supabase MCP apply_migration (bypasses the 42601 that db push throws on $$ bodies; writes the version row itself) — the established M2-M3-P23 path; build/type checks pass WITHOUT this, so the migration files alone are a false-positive verification state (schema_push_requirement)"
    - "Every SEC SQL smoke runs LIVE against PROD and proves the attacker is denied: candidato SELECT gabarito_idx → denied; candidato reads no redação verdict + RH still reads it; recrutador-não-dono → 0 rows on analise/comparativo/candidaturas/redacoes + reprocessar 42501; to_regclass('backup_m2.candidaturas_pre_funil') IS NULL; get_bigfive_itens returns 116; auth_admin policy present"
    - "SEC-03: the n8n URL Vault secret is created in PROD (vault) so the server-side pg_net dispatch has its URL; graceful-skip proves no error when absent"
    - "SEC-09 is applied ONLY if 24-01 confirmed the mirror equals the live predicate byte-for-behavior (idempotent DROP/CREATE); version-row reconcile is deferred to Phase 27"
    - "No migration writes candidaturas or auto-rejects (RNF-07a) — every applied statement only tightens SELECT/UPDATE, adds a DEFINER reader, deactivates items, or drops a backup"
  artifacts:
    - path: ".planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-PROD-LANDING.md"
      provides: "apply log (version rows) + live SQL-smoke results per SEC req"
      contains: "apply_migration"
  key_links:
    - from: "authored migration files (24-02..07)"
      to: "PROD schema"
      via: "Supabase MCP apply_migration"
      pattern: "apply_migration"
    - from: "committed SQL smokes"
      to: "PROD proof"
      via: "execute_sql set_config jwt.claims → denied/0-rows"
      pattern: "set_config"
---

<objective>
Land every Phase-24 migration on PROD and prove each attacker is actually denied. Build and type checks pass WITHOUT applying (types come from config, not the live DB), so the authored migration files create a false-positive "done" state — this [BLOCKING] plan is what makes the phase real. Apply via Supabase MCP `apply_migration` (bypasses the transaction-pooler 42601 on `$$` bodies and writes the version row), create the n8n URL Vault secret for SEC-03, then run the committed SQL smokes live for every SEC requirement.

Purpose: Turn the authored security boundary into an enforced one, verified by candidato/recrutador-não-dono JWT simulations against the real database.
Output: `24-PROD-LANDING.md` apply log + per-requirement live smoke results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**[BLOCKING · non-autonomous]** This plan writes to PROD. Apply migrations with Supabase MCP `apply_migration` / `execute_sql` — NOT `supabase db push` (42601 on the $$ bodies). Author-side files have NO outer BEGIN/COMMIT (the MCP path wraps each apply). Before applying SEC-09, re-confirm the 24-01 live-predicate match; if 24-01 FLAGGED a divergence, HALT and escalate (a behavior change is out of scope). EF redeploys are 24-09 (next wave). Coordinate SEC-01/02 apply with the frontend deploy of the 24-02/24-03 client rewires (cognitivo_itens is empty so SEC-01 is latent; SEC-02 has live candidate data → the redacaoService rewire must ship in the same release or "minha redação" blanks).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-RESEARCH.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-LIVE-STATE.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Applied the four candidate-secrecy + horizontal-scope migrations to PROD via Supabase MCP and ran their live SQL smokes.</what-built>
  <how-to-verify>
    Apply via `apply_migration` in order: `20260706110001_sec01_cognitivo_gabarito.sql`, `20260706110002_sec07_rubric.sql`, `20260706110003_sec02_redacao_verdict.sql`, `20260706110004_sec05_08_vaga_scope.sql`. Confirm each version row is written (`SELECT version FROM supabase_migrations.schema_migrations ORDER BY version DESC LIMIT 4`). Then run the committed smokes via `execute_sql` (fixture + set_config request.jwt.claims + ROLLBACK-free cleanup): `supabase/tests/sec01_07_smokes.sql`, `sec02_smokes.sql`, `sec05_08_smokes.sql`. Record in 24-PROD-LANDING.md the PASS/FAIL for: candidato SELECT gabarito_idx → permission denied; candidato get_cognitivo_itens → no key column; candidato SELECT perguntas.rubric → denied; candidato SELECT own redação verdict col → 0/denied AND RH → verdict present AND get_minha_redacao → safe cols only; recrutador-não-dono → 0 rows on analise/comparativo/candidaturas/redacoes; non-owner reprocessar_analise → 42501; RNF-07a: pontuar_* did not write candidaturas.
    Expected: every attacker assertion is DENIED/0-rows; every legitimate reader (RH verdict, owner rows, admin) still works.
  </how-to-verify>
  <resume-signal>Type "approved" once all Wave-A/B smokes PASS in 24-PROD-LANDING.md, or describe the failing assertion.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Applied the SEC-03 server-side dispatch migration + created the n8n URL Vault secret, and applied the SEC-09/SEC-10 declaration + erasure migrations, with live smokes.</what-built>
  <how-to-verify>
    Create the n8n URL Vault secret (via `vault.create_secret(...)` through execute_sql, or the dashboard) using the value Fernando confirms — name matching what `20260706110005_sec03_n8n_serverside.sql` reads. Apply `20260706110005_sec03_n8n_serverside.sql`, then `20260706110006_sec09_auth_admin_policy.sql` (only after re-confirming the 24-01 live-predicate match), then `20260706110007_sec10_drop_backup.sql`. Confirm version rows written. Live smokes into 24-PROD-LANDING.md: (SEC-03) trigger a candidatura status change and confirm net.http_post fired server-side (or graceful-skip if the secret is intentionally absent) AND `grep -r 'n8n.cloud\|fernandocosta' build/ ⇒ 0` after a fresh `npm run build`; (SEC-09) `SELECT policyname FROM pg_policies WHERE tablename='usuarios_rh' AND policyname='auth_admin_le_usuarios_rh'` → present; (SEC-10) `SELECT to_regclass('backup_m2.candidaturas_pre_funil')` → NULL.
    Expected: SEC-03 dispatch server-side + bundle clean; auth_admin policy present; backup gone.
  </how-to-verify>
  <resume-signal>Type "approved" once SEC-03/09/10 smokes PASS, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Applied the UX-08 item-deactivation migration and confirmed the item bank returns 116 active items.</what-built>
  <how-to-verify>
    Apply `20260706110008_ux08_o6_deactivate.sql` via `apply_migration`. Confirm the version row. Live smokes into 24-PROD-LANDING.md: `SELECT count(*) FROM public.bigfive_itens WHERE ativo` → 116; `SELECT count(*) FROM public.get_bigfive_itens()` → 116; `SELECT item_id, ativo FROM public.bigfive_itens WHERE item_id IN (28,58,88,118)` → all ativo=false; confirm N/E/A/C item counts unchanged (24 each) and O active = 20 (before prorate). Note: `submit-bigfive-final` still runs the OLD bundle until 24-09 redeploys it — a live 116-item submit is validated in 24-09, not here.
    Expected: 116 active items; the 4 O6 items inactive; other domains intact.
  </how-to-verify>
  <resume-signal>Type "approved" once the UX-08 bank smoke PASSES, or describe the mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored migration files → live PROD schema | until applied, every SEC boundary is inert; build/tsc give a false "done" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08-01 | Information Disclosure | migrations authored but never applied (false-positive verification) | mitigate | [BLOCKING] apply_migration for all 8 files + live smokes prove enforcement, not just presence |
| T-24-08-02 | Tampering | 42601 on $$ bodies via db push | mitigate | apply via Supabase MCP apply_migration (writes version row, bypasses 42601); no outer BEGIN/COMMIT |
| T-24-08-03 | Denial of Service | SEC-09 applied as a behavior change (wrong predicate) demotes RH | mitigate | apply only after 24-01 byte-for-behavior confirmation; idempotent DROP/CREATE |
| T-24-08-04 | (RNF-07a) | an applied statement auto-rejects a candidate | mitigate | every migration only tightens SELECT/UPDATE / adds a reader / deactivates / drops; smoke asserts no candidaturas write |
| T-24-08-SC | Tampering | package installs / Vault secret handling | accept/mitigate | zero new packages (RESEARCH §Package Legitimacy Audit); the n8n URL Vault secret is set/rotated by Fernando, never committed |
</threat_model>

<verification>
- All 8 migrations applied via MCP; version rows written; recorded in 24-PROD-LANDING.md
- Every SEC live smoke PASS (candidato-DENY / non-owner-0-rows / 42501 / to_regclass NULL / 116 active / auth_admin present)
- n8n URL Vault secret created; bundle grep clean; SEC-09 applied only on confirmed match
</verification>

<success_criteria>
- Every Phase-24 DB boundary is LIVE and proven by a live attacker-role smoke
- RNF-07a preserved (no candidaturas write / auto-reject introduced)
- Ready for 24-09 EF redeploys
</success_criteria>

<output>
Create `.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-08-SUMMARY.md` when done, and the apply log `24-PROD-LANDING.md`.
</output>
