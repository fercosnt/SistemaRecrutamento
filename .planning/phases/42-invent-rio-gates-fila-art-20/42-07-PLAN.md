---
phase: 42-invent-rio-gates-fila-art-20
plan: 07
type: execute
wave: 3
depends_on: ["42-06"]
files_modified:
  - supabase/functions/_shared/email-config.ts
  - supabase/functions/_shared/__tests__/email-config.test.ts
  - supabase/functions/notificar-rh/helpers.ts
  - supabase/functions/notificar-rh/index.ts
  - supabase/functions/notificar-rh/__tests__/notificar-rh.test.ts
  - supabase/migrations/20260730000002_p42_trg_revisao_solicitada.sql
autonomous: false
requirements: [REVISAO-01]

must_haves:
  truths:
    - "Quando um candidato solicita revisão de uma decisão, o RH recebe e-mail — o pedido deixa de gravar um timestamp que ninguém lê"
    - "Os destinatários são resolvidos por consulta no momento do envio, e o predicado usa o vocabulário da **coluna** `usuarios_rh.role` (`administrador`, `recrutador`), não o vocabulário do JWT — usar `'rh'` ali descartaria em silêncio todo recrutador"
    - "O e-mail ao RH entra no mesmo ledger `notificacoes_enviadas`, com chave de deduplicação por destinatário, e respeita `NOTIFICACOES_MODO` — não existe caminho de envio sem trilha nem sem modo de teste"
    - "Uma falha de envio ao RH não pode ser re-despachada contra a Edge Function errada: a varredura de retry viva seleciona apenas eventos de candidato, e isso está asserido negativamente"
    - "O corpo do e-mail ao RH não carrega nome de candidato nem identificador de candidatura — em modo de teste o corpo viaja para um domínio de terceiro"
  artifacts:
    - supabase/functions/notificar-rh/index.ts
    - supabase/functions/notificar-rh/helpers.ts
    - supabase/functions/notificar-rh/__tests__/notificar-rh.test.ts
    - supabase/migrations/20260730000002_p42_trg_revisao_solicitada.sql
  key_links:
    - "`trg_notif_revisao_solicitada` → `net.http_post` → EF `notificar-rh` — o hop é **at-most-once**: a EF tem de estar deployada e aceitando o evento **antes** de o trigger existir"
    - "`notificacoes_enviadas` ⟷ `varrer_retry_notificacoes` — o sweep vivo re-despacha TODA linha não-terminal para `notificar-candidato` por URL fixa; sem um predicado que exclua os eventos de RH, uma linha `falhou` do RH entraria em laço infinito de `400 VALIDATION` consumindo o orçamento de 20 linhas por varredura"
---

<objective>
Fechar a metade que falta do round-trip: o pedido de revisão do candidato passa a **chegar** ao RH.

Purpose: REVISAO-01. Hoje `solicitar_revisao_decisao` grava `revisao_solicitada_em` e o processo
termina ali. Esta é a re-criação de um trigger que já existiu — `trg_n8n_revisao_decisao`
(`20260706110005:163-211`), com exatamente este guard de transição, DROPado pela P39 ao aposentar o
n8n — agora apontado para uma Edge Function em vez de um webhook externo. Não é caminho novo; é o
caminho antigo no idioma vivo.

Output: a EF `notificar-rh` (irmã e separada de `notificar-candidato`, D-P42-11), o trigger de
disparo (D-P42-13), e a correção da colisão entre o ledger compartilhado e a varredura de retry viva.

Escopo negativo: nada em `notificar-candidato` é tocado neste plano (o 5º evento é do plano 42-08,
numa wave posterior). Nenhuma UI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-CONTEXT.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-RESEARCH.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-PATTERNS.md
@docs/compliance/achados-inventario.md
</context>

> IDs `D-P42-NN` são índices atribuídos pelo planner sobre os bullets de `42-CONTEXT.md`
> `<decisions>`, em ordem de documento. A tabela completa está em `42-01-PLAN.md`. O prefixo
> `P42` existe porque o projeto já usa `D-NN` para decisões de milestones anteriores (por
> exemplo `D-13` em `RHSidebar.tsx` e `D-15` no guardrail do template de rejeição) — os dois
> namespaces são distintos e não devem ser confundidos.

## Colisão encontrada no planejamento e resolvida aqui (não estava na pesquisa)

D-P42-15 manda o e-mail ao RH entrar no **mesmo** ledger `notificacoes_enviadas`. Mas
`varrer_retry_notificacoes` (P41, `20260727000001:150-200`) seleciona **toda** linha com
`status IN ('pendente','falhou') AND tentativas < 5` e a re-despacha por **URL fixa** para
`/functions/v1/notificar-candidato`. Uma linha de evento de RH que falhasse ao enviar seria
re-postada contra a EF errada, que a rejeitaria com `400 VALIDATION` **antes** do branch de retry —
logo `tentativas` nunca incrementa e a linha volta a ser selecionada a cada 15 minutos, para sempre,
consumindo o orçamento `LIMIT 20` que existe para os retries legítimos de candidato.

**Resolução adotada:** a varredura passa a excluir explicitamente os eventos de RH do seu `SELECT`,
com o motivo escrito no corpo e no `COMMENT`. O retry automático do e-mail ao RH fica **fora do
v1**, e isso é decisão registrada, não omissão: a **fila `/rh/revisoes` é a superfície durável** —
um nudge perdido é recuperável abrindo a fila, ao passo que um e-mail de candidato perdido não é
recuperável por nenhum caminho, porque o candidato não tem fila. A assimetria é justificada, não
preguiça. Fica um todo rastreado para reavaliar em M8+.

**Rejeitada:** gravar `tentativas = 5` na primeira falha do RH para que o predicado `tentativas < 5`
a exclua. Funciona, não toca a função viva, e é exatamente o tipo de truque opaco que produz um
CR-01: um leitor futuro vê `tentativas = 5` e conclui que houve cinco tentativas.

## Artifacts this phase produces (escopo deste plano)

**Edge Function nova**
- `supabase/functions/notificar-rh/index.ts` · `helpers.ts` · `__tests__/notificar-rh.test.ts`

**Migration**
- `supabase/migrations/20260730000002_p42_trg_revisao_solicitada.sql`

**Objetos de banco**
- `public.trg_notif_revisao_solicitada()` (função) · `trg_notif_revisao_solicitada` (trigger em `public.decisao_final`)
- `public.varrer_retry_notificacoes()` — **substituída** com predicado de exclusão de eventos de RH

**Símbolos**
- `resolverDestinatarioComLabel` (novo em `_shared/email-config.ts`; `resolverDestinatario` passa a delegar)
- `montarDedupeKeyRh`, `assuntoRevisaoSolicitada`, `corpoRevisaoSolicitada`, `NotificarRhDeps`, `handler` (em `notificar-rh/`)

<!-- Literais abaixo aparecem em <action> por serem identificadores load-bearing;
     os mesmos literais sao negative-grepped em criterios de aceitacao sobre ARQUIVOS DE CODIGO,
     nunca sobre este plano. Allowlist explicita (#429): -->
<!-- planner-discipline-allow: \ -->
<!-- planner-discipline-allow: edge_invoke_key -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `notificar-rh/helpers.ts` (puro) + o rótulo de sink generalizado em `_shared/email-config.ts`</name>
  <read_first>
    - `supabase/functions/_shared/email-config.ts` (ler inteiro — `resolverModo` em `:61`, `DestinatarioResolvido` em `:70`, `resolverDestinatario` em `:93` com o `evento.replace(/[^a-z_]/g,'')`, `exigirChaveApi` em `:121`, `exigirSinkTeste` em `:143`, `FROM`/`REPLY_TO`)
    - `supabase/functions/_shared/__tests__/email-config.test.ts` (o arquivo de teste a estender)
    - `supabase/functions/_shared/email-templates.ts` linhas 55-110 (`escapeHtml` e `layoutBase`, ambos exportados e reusáveis)
    - `supabase/functions/notificar-candidato/helpers.ts` (ler inteiro — o cabeçalho JSDoc que explica **por que** helpers puros existem, `montarDedupeKey`, `construirCorpoResend`, `logSeguro`, `computeProximaTentativa`)
    - `supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts` linhas 1-46 (o molde de teste Deno sem rede)
    - `.planning/phases/42-invent-rio-gates-fila-art-20/42-RESEARCH.md` § "Pattern 5" (as duas taxonomias de papel)
  </read_first>
  <behavior>
    - `resolverDestinatarioComLabel('a@b.com', 'revisao_solicitada_rh', 'producao')` devolve `para === 'a@b.com'`, `redirecionado === false`.
    - `resolverDestinatarioComLabel('a@b.com', 'revisao_solicitada_rh', 'teste')` devolve `para === 'delivered+revisao_solicitada_rh@resend.dev'`, `destinatario_original === 'a@b.com'`, `redirecionado === true`.
    - `resolverDestinatarioComLabel('a@b.com', 'ROTULO-com 99 lixo!', 'teste')` sanitiza o rótulo para `[a-z_]` — o endereço resultante não contém dígito, espaço nem pontuação além do `+` e do `@`.
    - `resolverDestinatario` (a função existente) continua produzindo exatamente os mesmos endereços para os eventos vivos — os testes já existentes em `email-config.test.ts` continuam verdes sem alteração.
    - `montarDedupeKeyRh('cand-1', 'user-9')` === `'cand-1:revisao_solicitada:user-9'`; dois destinatários distintos da mesma candidatura produzem chaves distintas (asserção explícita de não-colisão).
    - `assuntoRevisaoSolicitada('Dentista Sênior')` devolve string não-vazia contendo o título da vaga e **não** contendo a palavra `candidato` seguida de nome próprio interpolado.
    - `corpoRevisaoSolicitada({ tituloVaga, urlFila })` devolve HTML contendo o título da vaga escapado e o link para a fila; **não** contém nome de candidato nem `candidatura_id` (asserção negativa: passar um objeto com um campo extra de nome não o faz aparecer no HTML, porque o corpo não tem esse parâmetro).
    - `corpoRevisaoSolicitada` escapa HTML: um título de vaga contendo `<script>` sai escapado.
  </behavior>
  <action>
    **(1) `_shared/email-config.ts`** — extrair a generalização mínima: criar
    `export function resolverDestinatarioComLabel(emailReal: string, label: string, modo: ModoNotificacao = resolverModo()): DestinatarioResolvido`
    com o corpo atual de `resolverDestinatario` (incluindo a sanitização `label.replace(/[^a-z_]/g, '')`),
    e reescrever `resolverDestinatario(emailReal, evento, modo)` para **delegar** a ela passando
    `evento` como rótulo. Zero mudança de comportamento para os chamadores existentes; o motivo vai
    no JSDoc: o rótulo do sink de teste é conceito de **destino**, não de vocabulário de evento de
    candidato — e a EF do RH precisa de um rótulo que não pertence à união `EventoNotificacao`,
    porque adicionar `revisao_solicitada_rh` àquela união exigiria entradas correspondentes nos três
    mapas `Record<>` de template de candidato, poluindo um vocabulário fechado por outra razão.
    Estender `_shared/__tests__/email-config.test.ts` com os 3 casos do bloco `<behavior>` que
    cobrem a função nova; **não** alterar nenhum teste existente.

    **(2) `supabase/functions/notificar-rh/helpers.ts`** — funções puras, zero rede, zero segredo,
    com o mesmo cabeçalho JSDoc explicativo de `notificar-candidato/helpers.ts:1-8` (a separação
    existe para que o teste importe este módulo sem disparar `Deno.serve`).
    - `export function montarDedupeKeyRh(candidaturaId: string, userId: string): string` →
      `` `${candidaturaId}:revisao_solicitada:${userId}` ``. Comentário obrigatório: a chave é **por
      destinatário**; uma chave só por candidatura faria o primeiro RH consumir o claim e os demais
      receberem `duplicate`, silenciosamente.
    - `export function assuntoRevisaoSolicitada(tituloVaga: string): string` — assunto interno em
      pt-BR nomeando o pedido de revisão e a vaga.
    - `export function corpoRevisaoSolicitada(args: { tituloVaga: string; urlFila: string }): string`
      — HTML montado com `layoutBase` e `escapeHtml` importados de `../_shared/email-templates.ts`.
      **Decisão registrada de privacidade:** o corpo **não** carrega nome de candidato nem
      `candidatura_id`. Em modo `teste` o corpo inteiro viaja para `resend.dev`, um domínio de
      terceiro; e o RH não precisa do nome no e-mail — ele o vê na fila, que é a superfície
      durável. O corpo diz que há um pedido de revisão na vaga X e leva para `/rh/revisoes`.
    - `export function construirCorpoResendRh(args: {...}): Record<string, unknown>` — o corpo JSON
      do Resend, no molde de `construirCorpoResend`, **sem** anexo e **sem** a chave da API.
    - `export function logSeguroRh(...)` — ou importar `logSeguro` de
      `../notificar-candidato/helpers.ts` se ele já for genérico o bastante; decidir por leitura e
      registrar a escolha no JSDoc. Allowlist de chaves; nenhum e-mail, nenhum segredo, nenhum
      `candidatura_id` completo em log.

    **(3) `supabase/functions/notificar-rh/__tests__/notificar-rh.test.ts`** — escrito **antes** da
    implementação (RED), cobrindo todo o bloco `<behavior>` relativo aos helpers do RH. Cabeçalho no
    idioma do arquivo vizinho, com o comando de execução. Sem `--allow-net`.

    ⚠ Registrar no SUMMARY o desvio em relação à `42-VALIDATION.md`, que lista este arquivo de teste
    como item de Wave 0: um teste Deno que importa um módulo ainda inexistente **reprova o job
    `deno-test` bloqueante do CI** para todos os planos paralelos. Ele é portanto autorado aqui,
    ainda assim RED-antes-de-GREEN dentro do plano.
  </action>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/_shared supabase/functions/notificar-rh</automated>
  </verify>
  <acceptance_criteria>
    - `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions` sai 0 (corpus inteiro).
    - `grep -c 'resolverDestinatarioComLabel' supabase/functions/_shared/email-config.ts` >= 2 (declaração + delegação de `resolverDestinatario`).
    - Os testes pré-existentes de `email-config.test.ts` **não** foram alterados: `git diff` mostra apenas adições nesse arquivo.
    - `grep -c 'revisao_solicitada' supabase/functions/notificar-rh/helpers.ts` >= 1 e a chave gerada por `montarDedupeKeyRh` inclui o `userId` (asserção de não-colisão presente no teste).
    - **Asserção negativa (privacidade):** o teste prova que `corpoRevisaoSolicitada` não aceita nem emite nome de candidato — a assinatura não tem esse parâmetro, e há um teste que falha se o HTML contiver um nome injetado por qualquer outro caminho.
    - **Asserção negativa (XSS):** um `tituloVaga` contendo `<script>` sai escapado no HTML.
    - O SUMMARY registra o output RED e o output GREEN, e o desvio de Wave 0 com a justificativa.
  </acceptance_criteria>
  <done>Os helpers puros da EF do RH existem, são testados sem rede, e o rótulo do sink de teste está generalizado sem tocar no vocabulário de evento do candidato.</done>
</task>

<task type="auto">
  <name>Task 2: `notificar-rh/index.ts` + migration do trigger e do predicado da varredura</name>
  <read_first>
    - `supabase/functions/notificar-candidato/index.ts` (ler inteiro, 446 linhas — o molde: `NotificarDeps` em `:84-100`, self-auth Bearer em `:110-120`, validação de payload em `:126-140`, leitura por allowlist em `:182-215`, claim-before-send em `:240-282`, e o caminho de falha fire-and-forget em `:285-300`)
    - `supabase/functions/notificar-rh/helpers.ts` (o módulo puro da Task 1)
    - `supabase/functions/_shared/email-config.ts` (`resolverModo`, `resolverDestinatarioComLabel`, `exigirChaveApi`, `exigirSinkTeste`, `FROM`, `REPLY_TO`)
    - `supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql` linhas 55-120 (`trg_notif_transicao` — o corpo do dispatch a copiar quase verbatim: leitura do Vault com graceful-skip, `net.http_post` dentro de `BEGIN … EXCEPTION WHEN OTHERS THEN RAISE WARNING`)
    - `supabase/migrations/20260706110005_sec03_n8n_serverside.sql` linhas 160-215 (`trg_n8n_revisao_decisao` — o **ancestral droppado**, com exatamente o mesmo guard `OLD IS NULL AND NEW IS NOT NULL` e o mesmo `AFTER UPDATE OF revisao_solicitada_em ON public.decisao_final`)
    - `supabase/migrations/20260727000001_p41_recon_retry.sql` linhas 140-215 (`varrer_retry_notificacoes` — o corpo vivo que esta migration substitui, e o `COMMENT` a preservar)
    - `supabase/migrations/20260721000001_*.sql` linhas 60-90 (a forma da tabela `notificacoes_enviadas` e o CHECK do vocabulário de evento; ⛔ arquivo marcado NÃO APLICAR, é espelho documental)
    - `docs/sql/sql/03-tabela-usuarios-rh.sql` linhas 45-60 (o CHECK de `role`: `administrador | gerente | recrutador | visualizador`)
    - `supabase/migrations/20260420000002_*.sql` linhas 40-60 (`custom_access_token_hook`: o filtro `ativo = true AND deleted_at IS NULL` e o mapeamento `recrutador → rh`)
  </read_first>
  <reversibility rating="costly">A EF é redeployável e o trigger é droppável, mas a substituição de `varrer_retry_notificacoes` altera uma função viva da P41 que roda a cada 15 minutos em PROD.</reversibility>
  <action>
    **(1) `supabase/functions/notificar-rh/index.ts`** — clonar a estrutura de
    `notificar-candidato/index.ts`, mantendo cinco propriedades copiadas verbatim:
    - `export interface NotificarRhDeps { supabaseAdmin; fetchImpl: typeof fetch; serviceKey: string }`
      e `export async function handler(req: Request, deps: NotificarRhDeps): Promise<Response>`;
      `Deno.serve` **somente** sob `import.meta.main` — é pré-requisito do teste de CI sem rede.
    - Self-auth por Bearer comparado a `deps.serviceKey`; `401` sem Bearer, com o log sem interpolar
      o valor. O `401` sem Bearer é asserção obrigatória (precedente P39).
    - Validação de payload: aceitar **apenas** `evento === 'revisao_solicitada'` e um
      `candidatura_id` string não-vazio; qualquer outra coisa → `400 VALIDATION`.
    - Leitura por **allowlist de colunas**, nunca projeção total: da `candidaturas`, apenas o
      `vaga_id`; da `vagas`, apenas o `titulo`. Nada do candidato é lido — o corpo do e-mail não o
      usa (decisão de privacidade da Task 1).
    - Caminho de falha fire-and-forget: qualquer erro grava `status='falhou'` no ledger e devolve
      **200** — `net.http_post` é at-most-once e um não-200 não volta para o banco.

    **A divergência real** (único ponto sem análogo vivo): resolução **multi-destinatário**,
    resolvida por consulta **no momento do envio** e nunca por lista fixa em env ou no Vault (D-P42-12).
    Consultar `usuarios_rh` com
    `.select("user_id, nome_completo, email").eq("ativo", true).is("deleted_at", null).in("role", ["administrador","recrutador"])`.
    ⚠ **Nunca** `'rh'` no `.in(...)`: a coluna `usuarios_rh.role` é CHECK-constrained a
    `administrador | gerente | recrutador | visualizador`, e `'rh'` é valor do **JWT**
    (`app_metadata.role`), produzido pelo hook que mapeia `recrutador → rh`. Uma consulta por `'rh'`
    devolveria só administradores e descartaria em silêncio todo recrutador — hoje, 1 de 5 pessoas.
    Comentar isso na linha, com o ponteiro para `docs/compliance/achados-inventario.md` (achado
    A-02, que também registra que `gerente`/`visualizador` ficam de fora e por quê).
    Se a consulta devolver zero destinatários, devolver `200 { ok: true, skipped: "sem_destinatarios" }`
    e gravar um `RAISE`-equivalente em log — é condição operacional, não erro de request.

    Para **cada** destinatário, em laço: `dedupe_key = montarDedupeKeyRh(candidatura_id, user_id)`,
    claim-before-send por `upsert(..., { onConflict: "dedupe_key", ignoreDuplicates: true }).select("id")`;
    claim vazio ⇒ pular aquele destinatário (duplicate), nunca abortar o laço. `modo = resolverModo()`,
    `dest = resolverDestinatarioComLabel(email, 'revisao_solicitada_rh', modo)`,
    `exigirSinkTeste(dest.para, modo)` **após** o claim (mesma ordem da P41-01), `destinatario_original`
    preenchido nos dois modos. Envio por `deps.fetchImpl` a `https://api.resend.com/emails` com
    header `Idempotency-Key` = `dedupe_key`, **nunca** logado. Uma falha de um destinatário não
    interrompe os demais.

    **(2) `supabase/migrations/20260730000002_p42_trg_revisao_solicitada.sql`** — sem wrapper
    `BEGIN;/COMMIT;`. Cabeçalho pt-BR: REVISAO-01, D-P42-13, e a nota de que este trigger **não é novo**
    — é a re-criação de `trg_n8n_revisao_decisao` (`20260706110005:163-211`), com o mesmo guard e a
    mesma cláusula `AFTER UPDATE OF`, apontado para uma Edge Function em vez do webhook aposentado.
    - `CREATE OR REPLACE FUNCTION public.trg_notif_revisao_solicitada() RETURNS trigger
      LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''`: guard de transição
      `IF NOT (OLD.revisao_solicitada_em IS NULL AND NEW.revisao_solicitada_em IS NOT NULL) THEN RETURN NEW; END IF;`;
      leitura de `project_url` e `edge_invoke_key` de `vault.decrypted_secrets` com **graceful-skip**
      (`RETURN NEW` se qualquer um for nulo — segredo ausente nunca quebra o pedido do candidato);
      `net.http_post` para `…/functions/v1/notificar-rh` com Bearer do `edge_invoke_key`
      (**nunca** service-role — invariante P41, quebrada por rotação) e corpo
      `jsonb_build_object('evento','revisao_solicitada','candidatura_id', NEW.candidatura_id)` —
      **ids-only, zero PII**; tudo dentro de `BEGIN … EXCEPTION WHEN OTHERS THEN RAISE WARNING`
      (fail-open: o funil nunca quebra por falha de dispatch), com a mensagem do `RAISE WARNING`
      sem interpolar PII.
    - `REVOKE ALL ON FUNCTION … FROM PUBLIC;`, `DROP TRIGGER IF EXISTS trg_notif_revisao_solicitada
      ON public.decisao_final;`, `CREATE TRIGGER trg_notif_revisao_solicitada AFTER UPDATE OF
      revisao_solicitada_em ON public.decisao_final FOR EACH ROW EXECUTE FUNCTION
      public.trg_notif_revisao_solicitada();`, `COMMENT ON FUNCTION`.
      Aqui o `DROP TRIGGER IF EXISTS` **é** correto (recria-se um trigger que já existiu) — ao
      contrário do trigger de `atualizado_em` do plano 42-06, que usa `CREATE TRIGGER` puro. As duas
      convenções coexistem por razões diferentes; comentar a assimetria.
    - `CREATE OR REPLACE FUNCTION public.varrer_retry_notificacoes()` — o corpo **vivo** transcrito
      verbatim (o checkpoint da Task 3 o lê de `pg_get_functiondef` antes, e o diff é a evidência),
      com **uma** alteração: acrescentar ao `WHERE` do `FOR r IN SELECT`
      `AND evento NOT LIKE 'revisao\_solicitada%'`, e um comentário de bloco explicando a colisão
      inteira (URL fixa para `notificar-candidato` · `400 VALIDATION` antes do branch de retry ·
      `tentativas` nunca incrementa · laço infinito consumindo o `LIMIT 20`) e a decisão de deixar o
      retry do e-mail ao RH fora do v1, porque a fila é a superfície durável. Atualizar o `COMMENT ON
      FUNCTION` acrescentando essa cláusula — sem apagar nenhuma frase do comentário existente.
  </action>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions && M=supabase/migrations/20260730000002_p42_trg_revisao_solicitada.sql; test $(grep -v '^--' $M | grep -ciE '^[[:space:]]*(BEGIN|COMMIT);') -eq 0 && test $(grep -v '^--' $M | grep -c 'service_role') -eq 0 && echo SHAPE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions` sai 0 (corpus inteiro, com type-check ligado — é o job bloqueante do CI).
    - **Vocabulário de papel (correção que a pesquisa isolou):** `grep -c "\"recrutador\"" supabase/functions/notificar-rh/index.ts` >= 1 **e** `grep -cE '\.in\(\s*"role"\s*,\s*\[[^]]*"rh"' supabase/functions/notificar-rh/index.ts` == 0 — o filtro usa o vocabulário da coluna, nunca o do JWT.
    - `grep -c 'ativo' supabase/functions/notificar-rh/index.ts` >= 1 e `grep -c 'deleted_at' supabase/functions/notificar-rh/index.ts` >= 1 — o par literal do hook está presente.
    - **Asserção negativa (projeção total):** `grep -c "select(\"\*\")" supabase/functions/notificar-rh/index.ts` == 0 e toda chamada `.select(` do arquivo lista colunas nomeadas.
    - `grep -c 'import.meta.main' supabase/functions/notificar-rh/index.ts` == 1 — `Deno.serve` não dispara sob import de teste.
    - `grep -c 'montarDedupeKeyRh' supabase/functions/notificar-rh/index.ts` >= 1 — o claim é por destinatário.
    - **Migration — asserção negativa (Bearer):** `grep -v '^--' <migration> | grep -c 'service_role'` == 0 e `grep -c 'edge_invoke_key' <migration>` >= 1.
    - **Migration — asserção negativa (PII no dispatch):** o `jsonb_build_object` do `net.http_post` contém exatamente 2 chaves (`evento`, `candidatura_id`) e nenhuma referência a `nome`, `email` ou `cpf`.
    - **Migration — colisão da varredura:** `grep -c "evento NOT LIKE 'revisao" <migration>` == 1, e o bloco de comentário adjacente nomeia as quatro etapas da colisão (URL fixa · `400 VALIDATION` · `tentativas` não incrementa · laço consumindo o `LIMIT 20`).
    - **Migration — CLAUDE.md / 42601:** nenhuma linha executável `BEGIN;` ou `COMMIT;`.
    - `grep -c 'DROP TRIGGER IF EXISTS trg_notif_revisao_solicitada' <migration>` == 1, com o comentário que explica a assimetria em relação ao trigger de `atualizado_em` do plano 42-06.
  </acceptance_criteria>
  <done>A EF do RH compila, é testável sem rede, resolve destinatários pelo vocabulário correto, e a migration que a aciona existe — com a colisão do ledger compartilhado fechada explicitamente.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (CHECKPOINT): Diffar a função viva, deployar a Edge Function e só então aplicar o trigger</name>
  <what-built>
    A EF `notificar-rh` (código completo, `deno test` verde no corpus inteiro) e a migration
    `20260730000002_p42_trg_revisao_solicitada.sql` (trigger + substituição de
    `varrer_retry_notificacoes` com o predicado de exclusão). Nada foi deployado nem aplicado:
    subagentes GSD não recebem os tools MCP do Supabase (STATE.md).
  </what-built>
  <how-to-verify>
    O **orquestrador / main thread**, **nesta ordem exata** — a ordem é o controle, não uma
    preferência: `net.http_post` é **at-most-once**, então um trigger que dispara contra uma EF
    inexistente perde a notificação sem erro em lugar nenhum. Uma EF que aceita um evento que
    ninguém envia é inerte; o contrário não é.

    1. **Diff da função viva ANTES da substituição.** Executar
       `SELECT pg_get_functiondef('public.varrer_retry_notificacoes()'::regprocedure);` via
       `execute_sql` e comparar, linha a linha, com o corpo transcrito na migration. **A única
       diferença admissível é a cláusula `AND evento NOT LIKE 'revisao\_solicitada%'` e o
       comentário adjacente.** Qualquer outra divergência significa que a função viva não é a do
       arquivo `20260727000001` — PARE e reporte (é a mesma classe do achado A1 da pesquisa: ler um
       objeto vivo a partir de um único arquivo de migration em vez do estado acumulado).
    2. **Verificar os destinatários ao vivo** antes do deploy:
       `SELECT role, count(*) FROM public.usuarios_rh WHERE ativo = true AND deleted_at IS NULL GROUP BY 1;`
       — confirmar que o predicado `('administrador','recrutador')` alcança de fato mais de um
       administrador, e registrar quantos recrutadores entram. Se houver linhas `gerente`/`visualizador`,
       registrar que ficam de fora e por quê (achado A-02).
    3. **Deploy da EF** `notificar-rh` via MCP `deploy_edge_function`. Confirmar a env
       `NOTIFICACOES_MODO` **explícita** na função nova — ela é env **por Edge Function**; a
       ausência resolve para `teste` (fail-safe correto), mas a ausência silenciosa é o modo de
       falha que a P36 fechou. Registrar o valor observado.
    4. **Smoke da EF antes do trigger:** `POST` sem Bearer → esperar **401**. `POST` com Bearer do
       Vault e corpo `{"evento":"revisao_invalido","candidatura_id":"…"}` → esperar **400
       VALIDATION**. Isso prova que a EF está viva e que o grafo de módulos carregou (um import
       quebrado falharia no boot e nunca alcançaria o corpo do handler — discriminador da P41-05).
    5. **Só então** aplicar a migration via `apply_migration` (nome `p42_trg_revisao_solicitada`) e
       **reconciliar o ledger** (`schema_migrations.version` → prefixo `20260730000002`, em sequência
       após `20260730000001`).
    6. **Prova ao vivo do round-trip**, com a candidatura de teste registrada
       (`candidato.funil@teste.com`), em modo `teste`: chamar `solicitar_revisao_decisao` como o
       candidato, e verificar (a) que `notificacoes_enviadas` ganhou **uma linha por destinatário
       RH ativo**, cada uma com `dedupe_key` distinta terminando no `user_id`; (b) que `modo` gravado
       é `teste` e `destinatario_original` preserva o e-mail real; (c) que nenhuma dessas linhas
       ficou com `status='pendente'` órfã. Ao fim, **remover** as linhas de teste do ledger e
       reverter a fixture — precedente P41-05 T3 (ledger de volta ao estado anterior, zero e-mail
       real enviado).
    7. **Asserção negativa da colisão:** executar `public.varrer_retry_notificacoes()` e confirmar
       que ela **não** seleciona nenhuma linha de evento de RH — o modo mais direto é deixar uma
       linha de RH em `status='falhou'` propositalmente, rodar a varredura, e confirmar que
       `net._http_response` não ganhou requisição para `notificar-candidato` referente a ela.
       Remover a linha depois.
    8. Colar no VERIFICATION.md da fase: o diff da função viva (passo 1), o `401`/`400` da EF
       (passo 4), a contagem de linhas por destinatário (passo 6) e a asserção negativa (passo 7).
  </how-to-verify>
  <resume-signal>Cole o diff da função viva, o resultado dos passos 4, 6 e 7, e a confirmação de que ledger e fixture voltaram ao estado anterior; ou descreva o passo que falhou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trigger Postgres → EF `notificar-rh` | `net.http_post` at-most-once; o Bearer é `edge_invoke_key` do Vault, nunca service-role (invariante P41, quebrada por rotação) |
| EF (`service_role`) → `usuarios_rh` | A EF bypassa RLS para resolver o roster; um predicado errado aqui não é barrado por nada |
| EF → Resend (domínio de terceiro) | Em modo `teste` o corpo inteiro do e-mail viaja para `resend.dev` |
| ledger compartilhado → varredura de retry viva | A varredura re-despacha por URL fixa; um evento que ela não deveria tocar vira laço |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-04 | Information Disclosure | resolução de destinatários em `notificar-rh` | high | mitigate | Predicado sobre o vocabulário da **coluna** (`administrador`, `recrutador`) + `ativo = true AND deleted_at IS NULL`; gate de grep reprova o valor de JWT no filtro |
| T-42-23 | Denial of Service | `varrer_retry_notificacoes` × linhas de evento de RH | high | mitigate | Predicado explícito de exclusão + asserção negativa ao vivo (passo 7 do checkpoint) provando que a varredura não seleciona a linha |
| T-42-24 | Information Disclosure | corpo do e-mail ao RH em modo `teste` | medium | mitigate | O corpo não carrega nome de candidato nem `candidatura_id`; a assinatura de `corpoRevisaoSolicitada` não aceita esses campos, e há teste que falha se aparecerem |
| T-42-25 | Spoofing | invocação direta da EF | high | mitigate | Self-auth Bearer comparado a `serviceKey`; `401` sem Bearer é asserção obrigatória do checkpoint (precedente P39) |
| T-42-26 | Repudiation | envio ao RH sem trilha | medium | mitigate | Toda tentativa passa pelo ledger `notificacoes_enviadas` com claim-before-send e `dedupe_key` por destinatário; `destinatario_original` preenchido nos dois modos |
| T-42-27 | Tampering | substituição de função viva da P41 | high | mitigate | `pg_get_functiondef` lido e diffado **antes** da substituição; única diferença admissível é a cláusula nova + comentário |
| T-42-SC | Tampering | instalações npm/pip/cargo | high | accept | Zero pacote novo; a EF usa `fetch` plano ao Resend e o client `@supabase/supabase-js@2` já importado por todas as EFs |
</threat_model>

<verification>
- `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions` verde (corpus inteiro).
- Gates de grep do Task 2 passam sobre `notificar-rh/index.ts` e sobre a migration.
- Diff de `pg_get_functiondef('public.varrer_retry_notificacoes()')` contra a transcrição da migration difere **apenas** na cláusula de exclusão e no comentário.
- EF deployada responde `401` sem Bearer e `400 VALIDATION` com evento desconhecido.
- Um pedido real de revisão produz uma linha de ledger **por destinatário RH ativo**, com `modo='teste'`, e o ledger volta ao estado anterior após a prova.
- A varredura de retry não seleciona linhas de evento de RH (asserção negativa ao vivo).
- `npm run -s lint 2>&1 | grep -c "error TS"` ≤ 97 e `sh .husky/pre-commit` sai 0.
</verification>

<success_criteria>
- Metade do critério de sucesso #1 do ROADMAP está satisfeita: o RH é notificado por e-mail quando um candidato solicita revisão — o pedido deixou de gravar um timestamp que ninguém lê.
- Nenhum recrutador é descartado em silêncio da lista de destinatários pelo choque entre os dois vocabulários de papel.
- O ledger compartilhado não introduziu um laço de retry contra a Edge Function errada, e isso está provado por asserção negativa ao vivo.
</success_criteria>

<output>
Create `.planning/phases/42-invent-rio-gates-fila-art-20/42-07-SUMMARY.md` when done
</output>