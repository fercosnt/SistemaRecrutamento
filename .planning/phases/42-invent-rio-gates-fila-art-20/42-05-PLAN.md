---
phase: 42-invent-rio-gates-fila-art-20
plan: 05
type: execute
wave: 2
depends_on: ["42-02"]
files_modified:
  - docs/compliance/sql/02-cron-live.sql
  - docs/compliance/ddl-idiom-sweep.md
  - docs/compliance/cron-inventory.md
  - docs/compliance/backup-posture.md
  - docs/compliance/achados-inventario.md
autonomous: false
requirements: [INVENT-02, INVENT-03, INVENT-04]

must_haves:
  truths:
    - "Cada `cron.job` vivo em PROD tem origem rastreável a uma migration do repositório, e o corpo vivo foi diffado contra o arquivo — qualquer job sem origem é registrado como achado bloqueante"
    - "O status de PITR está registrado como fato datado, com a janela real utilizável, e não como o tier contratado"
    - "O artefato de backup afirma, verbatim da documentação oficial, que backups de banco **não** incluem objetos do Storage — verdade independente de o PITR estar ligado"
    - "A varredura do idioma condicional de `ADD COLUMN` está completa, com cada ocorrência classificada por risco real e não por aparência, e a única ocorrência com cláusula FK está verificada contra o catálogo vivo"
    - "As duas assimetrias de autorização pré-existentes (`rh_le_decisao_final` não é vaga-scoped enquanto `rh_le_candidaturas` é; `gerente`/`visualizador` emitem um papel de JWT que não casa com policy nenhuma) estão registradas como achado — não corrigidas de passagem"
  artifacts:
    - docs/compliance/sql/02-cron-live.sql
    - docs/compliance/ddl-idiom-sweep.md
    - docs/compliance/cron-inventory.md
    - docs/compliance/backup-posture.md
    - docs/compliance/achados-inventario.md
  key_links:
    - "`cron-inventory.md` → o predicado que o plano 42-12 corrige (`ai-logs-retention-cleanup`) tem de constar aqui com o corpo vivo transcrito **antes** de ser alterado"
    - "`backup-posture.md` → a Phase 45 depende deste fato: um CV apagado do Storage é irrecuperável por qualquer caminho, e essa é a razão da ordem `Storage → Postgres → Auth`"
---

<objective>
Fechar os três artefatos de inventário que faltam: o diff dos `cron.job` vivos contra o repositório
(INVENT-03), a postura de backup como fato datado (INVENT-02) e a varredura do idioma condicional
de `ADD COLUMN` (INVENT-04) — mais o registro dos achados de autorização que esta fase encontra e
deliberadamente **não** corrige.

Purpose: critério de sucesso #4 e #5 do ROADMAP. A Phase 45 é a de maior risco do milestone
justamente porque o backup do Supabase exclui o Storage inteiramente — esse fato precisa estar
datado e verbatim no repositório **antes** de qualquer código destrutivo, não descoberto no meio
dele. E o INVENT-03 é o que responde se existe um caminho de escrita fora do repositório apontado
para uma purga: qualquer 4º job é achado bloqueante.

Output: 4 artefatos em `docs/compliance/` + a query de cron versionada.

Escopo negativo: nenhum `cron.schedule`/`cron.unschedule` é executado neste plano — a correção do
`ai-logs-retention-cleanup` é integralmente do plano 42-12, atrás do portão destrutivo. Aqui o job
só é **lido e transcrito**. Nenhuma policy de RLS é alterada.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-CONTEXT.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-RESEARCH.md
@docs/compliance/README.md
</context>

> IDs `D-P42-NN` são índices atribuídos pelo planner sobre os bullets de `42-CONTEXT.md`
> `<decisions>`, em ordem de documento. A tabela completa está em `42-01-PLAN.md`. O prefixo
> `P42` existe porque o projeto já usa `D-NN` para decisões de milestones anteriores (por
> exemplo `D-13` em `RHSidebar.tsx` e `D-15` no guardrail do template de rejeição) — os dois
> namespaces são distintos e não devem ser confundidos.

## Artifacts this phase produces (escopo deste plano)

**Arquivos novos**
- `docs/compliance/sql/02-cron-live.sql` — a query do catálogo de `cron.job` (INVENT-03)
- `docs/compliance/ddl-idiom-sweep.md` — a varredura do idioma condicional de `ADD COLUMN` (INVENT-04)
- `docs/compliance/cron-inventory.md` — diff vivo × repositório (INVENT-03)
- `docs/compliance/backup-posture.md` — PITR + Storage sem backup (INVENT-02)
- `docs/compliance/achados-inventario.md` — achados que esta fase registra e não corrige

<tasks>

<task type="auto">
  <name>Task 1: Varredura do idioma condicional de `ADD COLUMN` + query de cron + achados do repositório</name>
  <read_first>
    - `.planning/phases/42-invent-rio-gates-fila-art-20/42-RESEARCH.md` § "INVENT-04 — varredura" (a varredura já executada: 16 ocorrências / 8 arquivos, classes A/B/C) e § "Code Examples E6"
    - `supabase/migrations/20260421000001_*.sql` linhas 185-200 (a única ocorrência de classe A — a que carrega cláusula FK, sobre `public.autorizacoes`)
    - `supabase/migrations/20260609000003_prompt_library_cron.sql` linhas 30-80 (os dois `cron.schedule` do repositório: `ai-cost-aggregation` em `:36` e `ai-logs-retention-cleanup` em `:70`)
    - `supabase/migrations/20260727000001_p41_recon_retry.sql` linhas 215-230 (o terceiro job, `notif-retry-sweep` em `:223`, e o idioma de guard do `unschedule` em `:220-221`)
    - `supabase/migrations/20260709000002_*.sql` linhas 35-50 (`rh_le_candidaturas`, vaga-scoped) e `supabase/migrations/20260607000003_decisao_final.sql` linhas 55-62 (`rh_le_decisao_final`, **não** vaga-scoped)
    - `supabase/migrations/20260420000002_*.sql` linhas 40-60 (`custom_access_token_hook`: o filtro `ativo = true AND deleted_at IS NULL` e o mapeamento `recrutador → rh`, com `gerente`/`visualizador` caindo no `ELSE`)
    - `docs/sql/sql/01-setup-inicial.sql` linhas 75-110 e `docs/sql/sql/06-tabela-sessoes.sql` linha 148 e `docs/sql/sql/07-tabela-logs.sql` linha 149 (os 4 `cron.schedule` **comentados**)
  </read_first>
  <action>
    **(1)** Re-executar a varredura no repositório inteiro e escrever `docs/compliance/ddl-idiom-sweep.md`:
    `grep -rn` do idioma condicional de `ADD COLUMN` em `supabase/migrations/` **e** em `docs/sql/`
    (a recomendação original de varrer só as migrations deixaria de fora a fonte de schema legada).
    O artefato traz: data de coleta, o comando exato da varredura, a contagem total, e uma tabela
    com uma linha por ocorrência (`arquivo:linha` · tabela · coluna · classe · risco · detalhe).

    Classificação obrigatória em 3 classes, com a mecânica explicada no artefato (o Postgres pula o
    **subcomando inteiro** quando a coluna já existe — constraints incluídas):
    - **A** — a sentença carrega cláusula FK. Esperado: **1** ocorrência,
      `autorizacoes.user_id … ON DELETE SET NULL`. Registrar que ela é risco **aparente e não
      materializado**: PROD tem `SET NULL` nessa FK, igual ao declarado — a cláusula não foi
      silenciada.
    - **B** — a sentença carrega `NOT NULL` e/ou `DEFAULT`. Esperado: **6** ocorrências. Risco real
      e médio: se a coluna já existia sem o default, nem o default nem o `NOT NULL` foram
      aplicados, deixando linhas legadas nulas numa coluna que o tipo declara não-nula.
    - **C** — coluna nullable simples. Esperado: **9**. Sem cláusula a silenciar.

    Escrever no artefato a **manchete correta**, com a evidência: o que esta varredura encontra são
    6 riscos de classe B, e **não** uma FK silenciada — a FK de `candidatos.user_id` que a semente
    `FK-AUDIT-LIVE.md` atribuía a este idioma não foi criada por ele (ver
    `docs/compliance/pii-inventory.md` § "Correções à semente"). Registrar também que esta fase
    **não reproduz** o idioma: a migration do plano 42-06 declara as colunas novas na forma
    incondicional (D-P42-07), por autoconsistência.

    Listar as 7 linhas das classes A+B como **pendentes de verificação viva**, com o que verificar
    em cada uma (`is_nullable`, `column_default`, `pg_get_constraintdef`) — a Task 2 as colhe.

    **(2)** Criar `docs/compliance/sql/02-cron-live.sql` com
    `SELECT jobid, jobname, schedule, active, nodename, database, username, command FROM cron.job ORDER BY jobname;`
    mais `SELECT status, count(*) FROM cron.job_run_details GROUP BY 1;` e uma terceira consulta
    que devolve as últimas execuções do `ai-logs-retention-cleanup` com `status` e `return_message`.
    Cabeçalho: INVENT-03 · read-only · data · executada via `execute_sql` do MCP.

    **(3)** Criar `docs/compliance/achados-inventario.md` com a parte apurável no repositório —
    achados que esta fase **registra e não corrige**:
    - **A-01 — assimetria de escopo de autorização.** `rh_le_candidaturas` é vaga-scoped para o
      papel `rh` (`vagas.created_by = auth.uid()`), enquanto `rh_le_decisao_final` não tem escopo
      por vaga: um recrutador lê **todas** as decisões finais mas só **algumas** candidaturas. As
      duas policies discordam em PROD hoje. Consequência para esta fase: um `.select()` com join
      devolveria linhas-fantasma com candidatura nula, e a mera existência/timestamp da revisão de
      uma vaga alheia já é vazamento lateral — é por isso que a fila é lida por RPC
      `SECURITY DEFINER` com o escopo re-implementado dentro dela (plano 42-06), espelhando a
      policy **mais restritiva**. Relaxar ou apertar `rh_le_decisao_final` está **fora de escopo**:
      é mudança de superfície de autorização e vira débito rastreado, não conserto de passagem.
    - **A-02 — dois papéis sem policy correspondente.** A coluna `usuarios_rh.role` admite
      `administrador | gerente | recrutador | visualizador`; o `custom_access_token_hook` mapeia
      `recrutador → rh` e deixa `gerente`/`visualizador` caírem no `ELSE`, emitindo um
      `app_metadata.role` que não casa com policy nenhuma. Quem tiver esses papéis tem sessão
      válida e zero acesso. A Task 2 mede se existem linhas assim.
    - **A-03 — quatro `cron.schedule` comentados** em `docs/sql/` (`limpar-sessoes` e `limpar-logs`,
      duas ocorrências cada): promessa de retenção sem código que a execute. Registrar; **não**
      ativar. Alimenta o checklist CONSOL-04 da Phase 47.
  </action>
  <verify>
    <automated>test -f docs/compliance/ddl-idiom-sweep.md && test -f docs/compliance/sql/02-cron-live.sql && test -f docs/compliance/achados-inventario.md && grep -c 'A-01' docs/compliance/achados-inventario.md</automated>
  </verify>
  <acceptance_criteria>
    - `ddl-idiom-sweep.md` traz o comando exato de varredura, a contagem total e uma tabela com uma linha por ocorrência, cada uma com `arquivo:linha`.
    - A soma das ocorrências por classe A+B+C na tabela é igual à contagem total declarada; A == 1, B == 6, C == 9. Se a re-execução da varredura devolver números diferentes, o artefato registra os números **medidos** e nota a divergência com a pesquisa como achado.
    - O artefato afirma explicitamente que a FK de `candidatos.user_id` **não** foi criada por este idioma, com o ponteiro para `docs/compliance/pii-inventory.md`.
    - `docs/compliance/sql/02-cron-live.sql` contém as 3 consultas e `grep -icE '\b(insert|update|delete|drop|alter|truncate|schedule|unschedule)\b' docs/compliance/sql/02-cron-live.sql` == 0.
    - `achados-inventario.md` contém os 3 achados rotulados `A-01`, `A-02`, `A-03`, cada um com a citação `arquivo:linha` da evidência e uma linha explícita de "não corrigido nesta fase, e por quê".
    - **Asserção negativa:** nenhum arquivo deste plano contém `cron.schedule(` ou `cron.unschedule(` como statement executável — `grep -rc 'cron\.\(un\)\?schedule(' docs/compliance/` == 0 fora de blocos citados como transcrição.
  </acceptance_criteria>
  <done>A varredura do INVENT-04 está completa, classificada por risco real, e os achados de autorização estão registrados como achado em vez de corrigidos de passagem.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (CHECKPOINT): Coletar cron.job vivo, as 7 colunas A+B, os papéis de usuarios_rh e o status do PITR</name>
  <what-built>
    `docs/compliance/sql/02-cron-live.sql` (read-only, verificado), a varredura do INVENT-04 com as
    7 linhas A+B marcadas como pendentes de verificação viva, e os 3 achados de repositório. Falta a
    metade viva: o catálogo de `cron.job`, o histórico de execuções, o estado das 7 colunas, os
    papéis de `usuarios_rh` e o status do PITR.
  </what-built>
  <how-to-verify>
    O **orquestrador / main thread** executa, via MCP `execute_sql` do Supabase (projeto
    `isljnozzlvckrgjjbjwp`) — tudo read-only:

    1. As 3 consultas de `docs/compliance/sql/02-cron-live.sql`. Devolver o `command` de cada job
       **na íntegra**, sem truncar — o diff byte a byte contra o arquivo de migration depende disso.
       **Esperado: exatamente 3 jobs, todos `active = true`** — `ai-cost-aggregation` (`30 1 * * *`),
       `ai-logs-retention-cleanup` (`0 2 * * *`), `notif-retry-sweep` (`*/15 * * * *`). **Qualquer
       4º job, ou qualquer job cujo corpo divirja do arquivo, é achado BLOQUEANTE**: significa que
       existe um caminho de escrita fora do repositório apontado para uma purga. Nesse caso, pare e
       escale antes de a Task 3 escrever o artefato.
    2. Para as 7 colunas das classes A+B listadas em `docs/compliance/ddl-idiom-sweep.md`:
       `is_nullable`, `column_default` de `information_schema.columns`, e o `pg_get_constraintdef`
       da FK quando houver. São 7 verificações, não sessenta.
    3. `SELECT role, ativo, (deleted_at IS NULL) AS vivo, count(*) FROM public.usuarios_rh GROUP BY 1,2,3 ORDER BY 1;`
       — confirma ou refuta a suposição A7 da pesquisa (existem linhas ativas com role `gerente` ou
       `visualizador`?). Este resultado também é o insumo do predicado de destinatários da EF
       `notificar-rh` (plano 42-07).
    4. **PITR (INVENT-02).** O MCP do Supabase **não** expõe backups. Caminho autoritativo:
       `curl -s -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" https://api.supabase.com/v1/projects/isljnozzlvckrgjjbjwp/database/backups`
       Anotar `pitr_enabled`, `walg_enabled`, `region` e
       `physical_backup_data.earliest_physical_backup_date_unix` (é a **janela real utilizável** —
       é isso que a Phase 45 precisa, não o tier contratado). **Sem o token:** abrir o dashboard em
       `Database → Backups → Point in Time` e transcrever o que estiver na tela, registrando no
       artefato que a fonte foi o dashboard e não a API.
    5. Anotar a data/hora UTC de todas as coletas.
  </how-to-verify>
  <resume-signal>Cole os 4 outputs + a data/hora UTC; ou, se aparecer um 4º `cron.job` ou divergência de corpo, PARE e reporte como achado bloqueante.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Escrever `cron-inventory.md` e `backup-posture.md`; fechar `achados-inventario.md`</name>
  <read_first>
    - O output colado no checkpoint da Task 2
    - `docs/compliance/sql/02-cron-live.sql` e `docs/compliance/ddl-idiom-sweep.md` (os artefatos que este fecha)
    - `supabase/migrations/20260609000003_prompt_library_cron.sql` linhas 60-80 (o corpo do `ai-logs-retention-cleanup` no repositório — o lado esquerdo do diff)
    - `.planning/phases/42-invent-rio-gates-fila-art-20/42-RESEARCH.md` § "Code Examples E8" (a forma da resposta da Management API e as **duas frases verbatim** da documentação oficial) e § "E5" (a mecânica exata do bug do predicado)
    - `docs/compliance/README.md` (as linhas que descrevem estes artefatos)
  </read_first>
  <precondition>O catálogo de `cron.job` vivo, o estado das 7 colunas, os agregados de `usuarios_rh.role` e o status do PITR foram entregues pelo checkpoint da Task 2, com data/hora UTC.</precondition>
  <action>
    **`docs/compliance/cron-inventory.md` (INVENT-03).** Cabeçalho com requirement, data de coleta e
    a query reprodutora. Uma tabela com uma linha por job vivo:
    `jobname · schedule · active · origem no repositório (arquivo:linha) · corpo bate byte a byte?`.
    Abaixo dela, para **cada** job, um bloco com o `command` vivo transcrito e o trecho
    correspondente do arquivo, para que o diff seja auditável por leitura e não por confiança.
    Registrar os 4 `cron.schedule` **comentados** de `docs/sql/` como seção separada, rotulada como
    promessa de retenção sem código que a execute (ponteiro para o achado A-03).

    Seção obrigatória **"Estado do `ai-logs-retention-cleanup` antes da correção"**: transcrever o
    predicado vivo e explicar a mecânica exata do defeito, sem simplificá-la —
    `candidate_ai_decisions.ai_call_log_ids` é `uuid[] NOT NULL`, o **array** não pode ser nulo mas
    seus **elementos** podem; `unnest` de um elemento nulo produz uma linha nula; e em SQL
    `x NOT IN (…, NULL)` avalia para `NULL`, nunca `TRUE`, para **toda** linha `x` — logo o
    `DELETE` das 02:00 apaga zero, em silêncio, todo dia. Registrar que o defeito é **latente**
    hoje (com `candidate_ai_decisions` vazia, `NOT IN ()` sobre conjunto vazio é `TRUE`) e que ele
    arma no instante em que a primeira linha for gravada com um nulo no array. Ponteiro explícito
    para o plano 42-12, que é quem corrige, atrás do portão destrutivo.

    **`docs/compliance/backup-posture.md` (INVENT-02 / D-P42-20).** Cabeçalho com requirement, data de
    coleta e a **fonte** (Management API com PAT, ou dashboard — dizer qual). Registrar como fato
    datado: `pitr_enabled`, `walg_enabled`, `region`, e a **janela real utilizável** derivada de
    `earliest_physical_backup_date_unix` (não o tier contratado). Se a coleta veio do dashboard,
    dizer isso e dizer o que não foi possível apurar.

    Seção obrigatória **"Storage não tem backup"**, com as duas frases **verbatim** da documentação
    oficial do Supabase, citadas como citação e com a URL:
    - *"Database backups do not include objects you store via the Storage API, as the database only includes metadata about these objects."*
    - *"Restoring an old backup does not restore objects you deleted after that backup."*
    Escrever imediatamente abaixo, em pt-BR e sem ambiguidade, a consequência operacional: isto é
    verdade **independentemente** de o PITR estar ligado; um currículo apagado do Storage é
    irrecuperável por qualquer caminho; e é essa a razão de a Phase 45 executar na ordem
    `Storage → Postgres → Auth` com os caminhos capturados **antes** da primeira mutação. Registrar
    também que PITR é add-on pago em Pro/Team/Enterprise, com janelas de 7 / 14 / 28 dias.

    **Fechar `docs/compliance/achados-inventario.md`** com os achados que só a coleta viva revela:
    - Resultado do A-02: existem (ou não) linhas ativas com role `gerente`/`visualizador`, com a
      contagem. Se existirem, escalar a severidade do achado e nomear a consequência (pessoas com
      sessão válida e zero acesso, **e** fora da lista de destinatários da EF `notificar-rh`).
    - Novo achado A-04, se aplicável: qualquer job de cron sem origem no repositório, ou qualquer
      corpo divergente. Se não houver, registrar explicitamente **"3/3 jobs rastreados, zero
      divergência de corpo"** — uma asserção negativa registrada vale mais que a ausência de menção.
    - Novo achado A-05: o resultado das 7 verificações de classe A+B. Para cada uma, se o `NOT NULL`
      e o `DEFAULT` declarados estão de fato aplicados no catálogo vivo. Divergência = risco
      materializado, e vira linha própria.
  </action>
  <verify>
    <automated>test -f docs/compliance/cron-inventory.md && test -f docs/compliance/backup-posture.md && grep -c 'do not include objects you store via the Storage API' docs/compliance/backup-posture.md && grep -c 'A-04' docs/compliance/achados-inventario.md</automated>
  </verify>
  <acceptance_criteria>
    - `cron-inventory.md` lista **cada** job vivo com `jobname`, `schedule`, `active`, a origem `arquivo:linha` no repositório e o veredito do diff de corpo.
    - `cron-inventory.md` contém a seção do estado do `ai-logs-retention-cleanup` antes da correção, com o predicado vivo transcrito e a explicação da mecânica do nulo no array.
    - `backup-posture.md` contém as **duas** frases em inglês verbatim da documentação oficial (`grep -c` == 1 para cada) e a URL da fonte.
    - `backup-posture.md` contém a afirmação explícita de que a ausência de backup do Storage vale independentemente de o PITR estar ligado.
    - `backup-posture.md` registra `pitr_enabled` com um valor concreto e a fonte da coleta (API ou dashboard).
    - Os 3 artefatos trazem a mesma data de coleta ISO do checkpoint.
    - `achados-inventario.md` contém `A-04` com veredito explícito (achado, ou "3/3 rastreados, zero divergência") e `A-05` com as 7 verificações.
    - **Asserção negativa (PII):** `grep -cE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' docs/compliance/cron-inventory.md docs/compliance/backup-posture.md docs/compliance/achados-inventario.md` == 0, exceto a URL de documentação.
  </acceptance_criteria>
  <done>INVENT-02, INVENT-03 e INVENT-04 estão satisfeitos como fatos datados no repositório, e a Phase 45 herda a postura de backup escrita antes de qualquer código destrutivo existir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caminho de escrita fora do repositório → `cron.job` | Um job vivo sem origem em migration significa que alguém consegue agendar `DELETE` em PROD sem passar pelo ledger |
| plataforma Supabase → expectativa de recuperabilidade | A crença de que "tem backup" é o que torna uma exclusão de Storage aceitável; a documentação diz o contrário |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-16 | Tampering | `cron.job` em PROD | high | mitigate | Diff byte a byte do `command` vivo contra a migration de origem; qualquer 4º job é achado bloqueante que interrompe o plano |
| T-42-17 | Destruction of data | expectativa de recuperabilidade do Storage | critical | mitigate | As duas frases verbatim da doc oficial exigidas por critério de aceitação, com a consequência operacional escrita em pt-BR ao lado |
| T-42-18 | Information Disclosure | `docs/compliance/*.md` | medium | mitigate | Asserção negativa de e-mail por regex nos 3 artefatos |
| T-42-19 | Elevation of Privilege | `rh_le_decisao_final` sem escopo por vaga | medium | accept | Registrado como achado A-01 e mitigado **nesta fase** pela leitura via RPC DEFINER com escopo re-implementado (42-06); alterar a policy viva é mudança de superfície de autorização fora de escopo |
| T-42-SC | Tampering | instalações npm/pip/cargo | high | accept | Zero pacote novo nesta fase |
</threat_model>

<verification>
- `docs/compliance/sql/02-cron-live.sql` é comprovadamente read-only e não contém verbo de agendamento executável.
- Os 3 jobs vivos estão rastreados a `arquivo:linha` com veredito de diff.
- `backup-posture.md` traz as duas citações verbatim e um `pitr_enabled` concreto.
- `ddl-idiom-sweep.md` fecha em A=1 / B=6 / C=9 (ou registra os números medidos e a divergência).
- `npm run -s lint 2>&1 | grep -c "error TS"` continua ≤ 97 (plano sem TypeScript).
</verification>

<success_criteria>
- Os 4 artefatos do critério de sucesso #4 do ROADMAP existem, datados, no repositório — e existem **antes** de qualquer linha destrutiva do milestone.
- O predicado do `ai-logs-retention-cleanup` está transcrito no seu estado pré-correção, de modo que o antes/depois do plano 42-12 seja auditável contra um registro e não contra memória.
</success_criteria>

<output>
Create `.planning/phases/42-invent-rio-gates-fila-art-20/42-05-SUMMARY.md` when done
</output>
