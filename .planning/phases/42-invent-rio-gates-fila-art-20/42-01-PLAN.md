---
phase: 42-invent-rio-gates-fila-art-20
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .husky/pre-commit
  - supabase/functions/notificar-candidato/helpers.ts
  - supabase/functions/notificar-candidato/index.ts
  - supabase/functions/notificar-candidato/__tests__/vocabulario-eventos.test.ts
  - supabase/functions/_shared/__tests__/email-templates.test.ts
autonomous: true
requirements: [REVISAO-04]

must_haves:
  truths:
    - "`git commit` sem bypass do hook funciona no repositório: o `.husky/pre-commit` falha somente quando a contagem `tsc` SOBE acima da baseline congelada de 97, e passa com a contagem atual"
    - "Um 5º evento adicionado a `EVENTO_MAP` sem os demais sítios reprova `deno test` antes de qualquer deploy — a paridade do vocabulário é asserida nos dois sentidos, não presumida"
    - "Os 4 eventos vivos (`confirmacao`, `avanco`, `convite`, `decisao`) continuam produzindo o mesmo `subject` E o mesmo `preheader` de hoje, incluindo os 3 desfechos de `decisao_final` — a classe de defeito W-01 fica pinada por string"
  artifacts:
    - .husky/pre-commit
    - supabase/functions/notificar-candidato/__tests__/vocabulario-eventos.test.ts
    - supabase/functions/_shared/__tests__/email-templates.test.ts
  key_links:
    - "`EVENTOS_VALIDOS` deixa de ser uma lista independente e passa a ser derivada das chaves de `EVENTO_MAP` — o sítio de registro nº1 (o único que produz `400 VALIDATION` silencioso sobre um `net.http_post` at-most-once) deixa de existir como ponto de drift"
    - "`.husky/pre-commit` ⟷ `.github/workflows/ci.yml:61-67` — o hook local passa a ser a versão mais estrita (97) do gate que o CI já roda (104)"
---

<objective>
Levantar os dois portões que todo o resto da Phase 42 usa como rede de segurança, **antes** de
qualquer edição na EF `notificar-candidato` viva.

Purpose: (1) O portão de fase destrutiva do milestone exige **zero `--no-verify`** no commit do
INVENT-05, e hoje isso é literalmente impossível — o `.husky/pre-commit` roda `npm run lint` cru
contra uma baseline pré-existente de 97 erros `tsc`. Este plano converte o hook no **mesmo gate de
não-regressão que o CI já roda desde a Phase 5**, tornando o portão satisfazível honestamente em
vez de por exceção. (2) O risco nomeado da fase é a edição cirúrgica em `notificar-candidato` — o
mesmo arquivo que embarcou CR-01, CR-02 e W-01 em produção. O vocabulário de evento tem **nove**
sítios de registro em código, dos quais **cinco não são forçados pelo compilador**. Este plano
fecha o pior deles por construção (derivação) e pina os demais por teste, **antes** de o 5º evento
ser adicionado no plano 42-08.

Output: hook convertido, `EVENTOS_VALIDOS` derivado + exportado, um teste de paridade de
vocabulário novo e o teste de templates estendido com a não-regressão W-01.

Escopo negativo: este plano **não** adiciona o 5º evento. Ele apenas torna a adição detectável.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-CONTEXT.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-RESEARCH.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-PATTERNS.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-VALIDATION.md
</context>

## Mapeamento de IDs de decisão (D-P42-NN)

`42-CONTEXT.md` não numera suas decisões. Os IDs abaixo são **índices atribuídos pelo planner**
sobre os bullets de `<decisions>` em ordem de documento, e são a referência usada por todos os
planos da Phase 42. O prefixo `P42` é deliberado: o projeto já usa `D-NN` para decisões de
milestones anteriores (`D-13` em `RHSidebar.tsx`, `D-15` no guardrail do template de rejeição,
`D-22` na proibição de wrapper transacional em migration). Os dois namespaces são distintos.

| ID | Decisão travada (42-CONTEXT.md `<decisions>`) |
|----|-----------------------------------------------|
| D-P42-01 | Rota nova `/rh/revisoes`, superfície RH net-new desktop-first, entrada no menu RH + contador de pendentes |
| D-P42-02 | Limiar do badge de SLA vive em tabela de configuração (padrão `config_sla_etapa`), BD-4 entra como **seed**, nunca constante compilada |
| D-P42-03 | Badge de 3 faixas (verde/âmbar/vermelho) com rótulo em dias; SLA **interno**, nunca exibido ao candidato; copy nunca usa a expressão vetada de prazo estatutário |
| D-P42-04 | Ordenação mais antigo primeiro; só pendentes por padrão, com toggle "Incluir respondidos" |
| D-P42-05 | Cada linha mostra: candidato · vaga · decisão original · quem decidiu · dias em espera · badge |
| D-P42-06 | RPC `SECURITY DEFINER` único `responder_revisao_decisao` como write-path |
| D-P42-07 | Colunas novas `revisao_por_usuario uuid REFERENCES auth.users(id)` e `revisao_respondida_em timestamptz`; a migration **não** usa a variante condicional de `ADD COLUMN` |
| D-P42-08 | Resultado estruturado: veredito (`mantida`/`revertida`) em coluna própria + justificativa ≥50 caracteres em `revisao_resultado` |
| D-P42-09 | Guard absoluto `auth.uid()` ≠ `decisao_final.por_usuario`, sem exceção, sem override de admin, sem fallback "só 1 RH" |
| D-P42-10 | Prova do bloqueio por tentativa real com JWT impersonado de dois usuários RH distintos, output no VERIFICATION.md |
| D-P42-11 | EF nova `notificar-rh`, irmã e separada de `notificar-candidato` |
| D-P42-12 | Destinatários = `usuarios_rh` ativos, resolvidos por query no momento do envio (nunca lista fixa em env/Vault) |
| D-P42-13 | Disparo por trigger na transição `revisao_solicitada_em` NULL→NOT NULL + `pg_net` |
| D-P42-14 | 5º evento `revisao_respondida` registrado nos dois lugares que fecham o vocabulário (união em código + CHECK vivo) na mesma entrega, com asserção de não-regressão dos 4 eventos **incluindo o preheader** |
| D-P42-15 | E-mail ao RH entra no mesmo ledger `notificacoes_enviadas`, com dedupe key própria e o mesmo respeito a `NOTIFICACOES_MODO` |
| D-P42-16 | Artefatos de inventário vivem em `docs/compliance/` (pasta nova) no repositório |
| D-P42-17 | Inventário PII em formato duplo: YAML machine-readable como fonte de verdade + Markdown gerado dele |
| D-P42-18 | Semente obrigatória `.planning/research/FK-AUDIT-LIVE.md` (grafo de FK vivo), nunca arquivos de migration |
| D-P42-19 | REVISAO-06 entregue **antes de qualquer tela**: SQL versionado em `docs/compliance/` + resultado datado colado no artefato |
| D-P42-20 | INVENT-02 (PITR) verificado e registrado como fato datado, com a frase explícita de que Storage não é coberto por nenhum caminho de backup |
| D-P42-21 | INVENT-05 sob o portão de fase destrutiva: contagem antes/depois pela mesma query, dry-run, review bloqueante, blast radius registrado como fato datado antes do apply |

## Artifacts this phase produces (escopo deste plano)

**Arquivos novos**
- `supabase/functions/notificar-candidato/__tests__/vocabulario-eventos.test.ts`

**Arquivos modificados**
- `.husky/pre-commit` (convertido em gate de não-regressão)
- `supabase/functions/notificar-candidato/helpers.ts` (+ `EVENTOS_VALIDOS` exportado e derivado)
- `supabase/functions/notificar-candidato/index.ts` (passa a importar `EVENTOS_VALIDOS`)
- `supabase/functions/_shared/__tests__/email-templates.test.ts` (+ não-regressão W-01 dos 4 eventos)

**Símbolos**
- `EVENTOS_VALIDOS` (movido de `index.ts` para `helpers.ts`, exportado, derivado de `EVENTO_MAP`)

<!-- Literais abaixo aparecem em <action> por serem identificadores load-bearing;
     os mesmos literais sao negative-grepped em criterios de aceitacao sobre ARQUIVOS DE CODIGO,
     nunca sobre este plano. Allowlist explicita (#429): -->
<!-- planner-discipline-allow: EVENTOS_VALIDOS -->
<!-- planner-discipline-allow: new Set( -->
<!-- planner-discipline-allow: revisao_respondida -->

<tasks>

<task type="auto">
  <name>Task 1: Converter `.husky/pre-commit` em gate de não-regressão `tsc` (baseline 97)</name>
  <read_first>
    - `.husky/pre-commit` (o arquivo sendo modificado — 15 linhas, ler inteiro)
    - `.github/workflows/ci.yml` linhas 55-75 (o step "Type-check (frozen tsc baseline 104)" — a fonte a copiar)
    - `package.json` linha 103 (`"lint": "tsc --noEmit"`)
    - `.planning/phases/42-invent-rio-gates-fila-art-20/42-RESEARCH.md` § "Known Conflict Resolution"
  </read_first>
  <precondition>`.husky/pre-commit` existe no repositório e tem bit de execução (`test -x .husky/pre-commit`).</precondition>
  <reversibility rating="reversible">O hook é um arquivo de 20 linhas versionado; reverter é um `git checkout` do arquivo.</reversibility>
  <action>
    Antes de editar, MEDIR: `npm run -s lint 2>&1 | grep -c "error TS"`. O valor esperado é
    **97** (medido pela pesquisa em 2026-07-29 e confirmado por `.planning/M7-HANDOFF.md:86`).
    Se o valor medido for diferente de 97, **PARE e reporte** — a baseline se moveu entre a
    pesquisa e a execução, e isso é achado, não motivo para re-pinar em silêncio.

    Substituir a linha final `npm run lint` do hook pelo mesmo mecanismo de não-regressão do
    `ci.yml`, com a baseline pinada em **97** (não 104): capturar a contagem em `COUNT` via
    `npm run -s lint 2>&1 | grep -c "error TS" || true`, ecoar a linha
    `tsc errors: $COUNT (frozen baseline: 97)`, e sair com código 1 somente quando
    `[ "$COUNT" -gt 97 ]`, imprimindo antes as primeiras 50 linhas de `error TS` para diagnóstico.
    Manter `#!/usr/bin/env sh` na primeira linha.

    Reescrever o docblock do topo para explicar (a) por que o gate é de contagem e não de exit
    code binário, (b) que a baseline local (97) é deliberadamente mais estrita que a do CI (104),
    que existe como folga herdada para PRs de terceiros, (c) que a baseline é re-medida e
    re-pinada quando um trabalho legítimo a mover, e (d) que a razão de existir desta conversão é
    o portão de fase destrutiva do M8 (STATE.md), que exige commit sem bypass do hook — hoje
    impossível. Remover a instrução de escape reflexivo que o docblock atual traz.

    Este commit em particular ainda roda contra o hook ANTIGO, então precisa de bypass —
    registrar a contagem 97→97 no corpo do commit, seguindo o precedente P36–P41.
  </action>
  <verify>
    <automated>sh .husky/pre-commit; test $? -eq 0 && grep -qc 'frozen baseline: 97' .husky/pre-commit && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run -s lint 2>&1 | grep -c "error TS"` imprime `97`; se imprimir outro número a task PARA e reporta.
    - `sh .husky/pre-commit` sai com código **0** e imprime a linha exata `tsc errors: 97 (frozen baseline: 97)`.
    - `grep -c 'frozen baseline: 97' .husky/pre-commit` == 1 e `grep -c '\-gt 97' .husky/pre-commit` == 1.
    - **Prova de comportamento (o gate morde):** criar `src/__gate_probe.ts` contendo `export const x: number = 'nao-e-numero'`; `sh .husky/pre-commit` sai com código **1** e imprime uma contagem `98`; remover o arquivo; `sh .husky/pre-commit` volta a sair **0**. O arquivo de prova NÃO fica no commit.
    - `head -1 .husky/pre-commit` == `#!/usr/bin/env sh` e `test -x .husky/pre-commit` continua verdadeiro.
    - O corpo do commit desta task registra a contagem `97 → 97`.
  </acceptance_criteria>
  <done>O hook falha somente quando a contagem `tsc` sobe acima de 97, e passa com o repositório como está — tornando o commit do INVENT-05 (plano 42-12) capaz de rodar o hook e passar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derivar `EVENTOS_VALIDOS` de `EVENTO_MAP` e pinar a paridade do vocabulário por teste</name>
  <read_first>
    - `supabase/functions/notificar-candidato/helpers.ts` (107 linhas — ler inteiro; é o arquivo sendo modificado)
    - `supabase/functions/notificar-candidato/index.ts` linhas 60-140 (a declaração atual em `:65-70` e o único ponto de consumo em `:128`)
    - `supabase/functions/_shared/email-config.ts` linhas 38-50 (`type EventoNotificacao`)
    - `supabase/functions/_shared/email-templates.ts` linhas 155-207 (`SUBJECTS`, `CORPOS`, `PREHEADERS`, `renderarEmail`)
    - `supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts` linhas 1-46 (cabeçalho, imports e o molde do teste de mapa)
    - `.planning/phases/42-invent-rio-gates-fila-art-20/42-RESEARCH.md` § "Pattern 1" (a tabela dos 10 sítios)
  </read_first>
  <behavior>
    - Teste 1 (T-42-V3a): `EVENTOS_VALIDOS` contém exatamente as chaves de `EVENTO_MAP` — comparação de conjunto nos DOIS sentidos (nenhuma chave sobrando de cada lado).
    - Teste 2 (T-42-V3b): os valores de `EVENTO_MAP` cobrem exatamente as chaves de `SUBJECTS`, e `SUBJECTS`/`CORPOS`/`PREHEADERS` têm conjuntos de chaves idênticos entre si — nos dois sentidos.
    - Teste 3 (T-42-V3c): para cada chave de `EVENTO_MAP`, `renderarEmail(mapearEvento(chave), dados)` devolve `subject` não-vazio e um `html` contendo um preheader não-vazio. Cobre o sítio nº9 (`DadosEmail`) indiretamente: um evento cujo corpo dependa de campo ausente ainda tem de renderizar.
    - Teste 4 (T-42-V4): `montarDedupeKey` para cada evento não-`convite` devolve `` `${candidaturaId}:${evento}` ``; `convite` devolve `` `${agendamentoId}:convite` ``; e o conjunto das chaves geradas para uma mesma `candidaturaId` não tem colisão (tamanho do Set == número de eventos).
  </behavior>
  <action>
    **RED primeiro.** Criar `supabase/functions/notificar-candidato/__tests__/vocabulario-eventos.test.ts`
    com os 4 testes do bloco `<behavior>`, importando de `../helpers.ts` (`EVENTOS_VALIDOS`,
    `EVENTO_MAP`, `mapearEvento`, `montarDedupeKey`, `type EventoLedger`) e de
    `../../_shared/email-templates.ts` (`SUBJECTS`, `renderarEmail`). Cabeçalho JSDoc no idioma de
    `notificar-candidato.test.ts:1-8`: explicar que este arquivo existe porque `EVENTOS_VALIDOS`
    era um `ReadonlySet<string>` que o compilador nunca conferiu, e que a omissão de um evento ali
    produz `400 VALIDATION` sobre um `net.http_post` **at-most-once** — falha totalmente
    silenciosa. Citar D-P42-14. Comando de execução no docblock, no idioma do arquivo vizinho.

    **GREEN depois.** Em `helpers.ts`:
    - exportar `EVENTO_MAP` (hoje é `const` privado em `:14`);
    - declarar, logo abaixo de `EVENTO_MAP`,
      `export const EVENTOS_VALIDOS: ReadonlySet<string> = new Set(Object.keys(EVENTO_MAP));`
      com comentário explicando que o tipo declarado permanece `ReadonlySet<string>` (o call site
      em `index.ts` testa uma string vinda do corpo JSON, então estreitar o tipo quebraria a
      compilação) mas o **valor** deixa de ser autoral: adicionar um evento a `EVENTO_MAP` passa a
      registrá-lo aqui automaticamente. Isso elimina o sítio de registro nº1 da tabela do
      §Pattern 1 — de 10 sítios restam 9.

    Em `index.ts`: apagar a declaração local de `EVENTOS_VALIDOS` (`:65-70`) e importá-la de
    `./helpers.ts`, mantendo o consumo em `:128` (`!EVENTOS_VALIDOS.has(raw.evento)`) byte a byte
    inalterado. Nenhuma outra linha de `index.ts` muda.

    Este é o único ponto da fase em que `index.ts` da `notificar-candidato` é tocado fora do plano
    42-08; o diff tem de ser exatamente: uma remoção de bloco e uma adição de import.
  </action>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/notificar-candidato supabase/functions/_shared</automated>
  </verify>
  <acceptance_criteria>
    - `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions` sai 0, com os 4 testes novos passando.
    - `grep -c 'EVENTOS_VALIDOS' supabase/functions/notificar-candidato/index.ts` == 2 (uma linha de import + o consumo em `:128`) e `grep -c 'new Set(' supabase/functions/notificar-candidato/index.ts` == 0.
    - `grep -c 'Object.keys(EVENTO_MAP)' supabase/functions/notificar-candidato/helpers.ts` == 1.
    - **Prova de que o teste morde:** adicionar temporariamente `revisao_respondida: "decisao_final"` a `EVENTO_MAP` sem tocar em mais nada faz `deno test` **falhar** no teste 2 (a chave não existe em `SUBJECTS`); reverter e o teste volta a passar. Registrar o output das duas execuções no SUMMARY.
    - `deno check` (implícito no `deno test`) não emite erro em `index.ts` — o call site `EVENTOS_VALIDOS.has(raw.evento)` continua compilando com `raw.evento` do tipo original.
    - O diff de `index.ts` tem no máximo 8 linhas removidas e 1 adicionada.
  </acceptance_criteria>
  <done>Um evento presente em `EVENTO_MAP` e ausente de `SUBJECTS`/`CORPOS`/`PREHEADERS` reprova o job `deno-test` bloqueante do CI antes de qualquer deploy, e `EVENTOS_VALIDOS` não pode mais divergir do mapa.</done>
</task>

<task type="auto">
  <name>Task 3: Estender `email-templates.test.ts` com a não-regressão W-01 dos 4 eventos vivos</name>
  <read_first>
    - `supabase/functions/_shared/__tests__/email-templates.test.ts` (arquivo sendo modificado — ler inteiro, em especial os 3 testes do W-01 em `:109-137`)
    - `supabase/functions/_shared/email-templates.ts` linhas 55-70 (`escapeHtml`, `interface DadosEmail`) e 155-207 (`SUBJECTS`, `CORPOS`, `PREHEADERS`, `renderarEmail`)
    - `.planning/STATE.md` § "Blockers/Concerns" (o registro do achado W-01 no UAT ao vivo da P39)
  </read_first>
  <action>
    Acrescentar ao arquivo existente (NÃO criar arquivo novo) um bloco de testes T-42-V1 que pina,
    **por string literal**, o `subject` e o `preheader` produzidos hoje pelos 4 eventos vivos:
    `candidatura_recebida`, `avaliacao_liberada`, `convite_entrevista` e `decisao_final` — este
    último nos **3** desfechos que `corpoDecisao`/`SUBJECTS`/`PREHEADERS` ramificam
    (`aprovado`, `rejeitado`, `em_espera`). As strings esperadas devem ser lidas do código-fonte
    vivo, não reescritas de memória.

    O preheader não é texto visível: extraí-lo do `html` devolvido por `renderarEmail` com a mesma
    técnica já usada pelos 3 testes W-01 em `:109-137` — reusar o helper que aquele bloco já tem;
    se ele for local ao bloco, promovê-lo ao topo do arquivo sem mudar o corpo.

    Cabeçalho de bloco em pt-BR explicando o porquê: o defeito W-01 (2026-07-28) era um preheader
    que ficou literal quando `subject` e `corpo` ramificaram, e ele foi invisível a toda asserção
    que olha só o texto visível. Este bloco é a rede que impede a Phase 42 de repetir a classe ao
    adicionar o 5º evento (D-P42-14).

    **Não** adicionar nada sobre `revisao_respondida` aqui — o 5º evento é do plano 42-08, que
    estende este mesmo arquivo numa wave posterior.
  </action>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/_shared</automated>
  </verify>
  <acceptance_criteria>
    - `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/_shared` sai 0, com **6** asserções novas de par (subject, preheader): 3 eventos simples + 3 desfechos de `decisao_final`.
    - Cada asserção compara contra uma **string literal completa** — nenhuma usa `.includes(`, `.startsWith(` ou regex parcial sobre subject/preheader.
    - **Prova de que o teste morde:** trocar temporariamente o preheader de `decisao_final` por um literal fixo (a forma exata do defeito W-01) faz o teste falhar em pelo menos 2 dos 3 desfechos; reverter e volta a passar. Registrar o output no SUMMARY.
    - `grep -c 'revisao_respondida' supabase/functions/_shared/__tests__/email-templates.test.ts` == 0.
    - Os 3 testes W-01 pré-existentes continuam presentes e passando (contagem de `Deno.test(` no arquivo aumenta, nunca diminui).
  </acceptance_criteria>
  <done>Qualquer mudança futura no assunto ou na prévia de caixa de entrada dos 4 eventos vivos reprova o CI, incluindo a metade invisível que produziu o W-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trigger Postgres → EF `notificar-candidato` | `net.http_post` **at-most-once**: uma rejeição na EF não volta para o banco e não gera erro em lugar nenhum |
| desenvolvedor → repositório | O hook de pre-commit é o último ponto onde um erro de tipo novo é barrado antes do CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Denial of Service (silencioso) | `EVENTOS_VALIDOS` em `notificar-candidato` | high | mitigate | Sítio eliminado por derivação de `EVENTO_MAP` (Task 2) + teste de paridade bidirecional no job `deno-test` bloqueante |
| T-42-06 | Tampering | `.husky/pre-commit` | medium | mitigate | Gate de contagem com baseline pinada em 97 e prova de que morde (arquivo-sonda com erro de tipo); baseline local mais estrita que a do CI |
| T-42-07 | Repudiation | copy de e-mail dos 4 eventos vivos | medium | mitigate | Subject e preheader pinados por string literal completa — um desalinhamento entre o que o candidato lê na prévia e no corpo passa a reprovar o CI |
| T-42-SC | Tampering | instalações npm/pip/cargo | high | accept | **Zero pacote novo nesta fase** (§Package Legitimacy Audit da 42-RESEARCH.md registra a tabela vazia). Nenhum `npm install`, `npx shadcn add` ou extensão Postgres nova. Se um plano propuser um pacote, isso por si só é desvio do CONTEXT e escala |
</threat_model>

<verification>
- `sh .husky/pre-commit` sai 0 hoje e sai 1 com um erro de tipo adicional.
- `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions` verde (corpus inteiro, não só os subdiretórios tocados).
- `npm run -s lint 2>&1 | grep -c "error TS"` continua **97** ao fim do plano (nenhuma das edições é TypeScript de `src/**`).
- `npm run test:run` verde (nenhuma mudança de front neste plano; a execução é regressão).
</verification>

<success_criteria>
- O commit do INVENT-05 (plano 42-12) pode ser criado com o hook executando e passando — o portão de fase destrutiva do M8 passa a ser satisfazível sem exceção.
- Os sítios de registro do vocabulário de evento caem de 10 para 9, e os 9 restantes ficam cobertos por compilador (4) ou por teste de paridade (os demais em código).
- A classe de defeito W-01 fica pinada: subject **e** preheader dos 4 eventos vivos, incluindo os 3 desfechos de `decisao_final`.
</success_criteria>

<output>
Create `.planning/phases/42-invent-rio-gates-fila-art-20/42-01-SUMMARY.md` when done
</output>
