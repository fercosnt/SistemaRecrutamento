---
phase: 42-invent-rio-gates-fila-art-20
plan: 06
type: execute
wave: 2
depends_on: ["42-02", "42-03"]
files_modified:
  - supabase/migrations/20260730000001_p42_revisao_art20.sql
  - database.types.ts
autonomous: false
requirements: [REVISAO-02, REVISAO-03, REVISAO-05]

must_haves:
  truths:
    - "Quem registrou a decisão original é barrado **pelo servidor** ao tentar responder à revisão dela — provado por tentativa real com JWT impersonado de dois usuários RH distintos, com o SQLSTATE e a mensagem asseridos"
    - "Não existe caminho de exceção para esse bloqueio: nenhum override de administrador, nenhum fallback para o caso de haver um único RH ativo"
    - "A resposta à revisão só entra por um write-path: `decisao_final` não tem policy de UPDATE, logo o RPC `SECURITY DEFINER` é o único caminho possível e é onde o guard vive"
    - "Uma justificativa de 49 caracteres é recusada e uma de 50 é aceita — a fronteira exata do guardrail de substância está asserida"
    - "Uma segunda resposta à mesma revisão é recusada com erro próprio: a decisão de fechar a reabertura em v1 está no código, não omitida"
    - "A fila é lida por um RPC que projeta colunas nomeadas e nunca a justificativa interna do recrutador"
    - "O limiar do badge de acompanhamento vive em tabela de configuração alterável sem deploy, e essa tabela não é legível pelo papel anônimo"
  artifacts:
    - supabase/migrations/20260730000001_p42_revisao_art20.sql
    - database.types.ts
  key_links:
    - "`responder_revisao_decisao` ⟷ `decisao_final.por_usuario` — o guard compara `auth.uid()` com o decisor gravado pela Phase 15; se aquela coluna deixar de ser preenchida, o guard vira no-op"
    - "`listar_revisoes_decisao` re-implementa **dentro** do DEFINER o escopo por vaga de `rh_le_candidaturas`, porque um `SECURITY DEFINER` bypassa RLS e as duas policies vivas discordam entre si (achado A-01)"
    - "`config_sla_revisao` ⟷ `classifyRevisaoSla` (42-03) — a tabela alimenta os limiares; config ausente resolve para a faixa degenerada, nunca para erro de tela"
---

<objective>
**Tracer da fase.** Fatiar o caminho de escrita da revisão Art. 20 de ponta a ponta pela camada
onde ele é de fato aplicável — do papel `authenticated` real, através do RPC `SECURITY DEFINER`,
até a linha gravada e lida de volta — e prová-lo com uma tentativa real de impersonação antes de
qualquer tela existir.

Purpose: este é o ponto da fase em que a arquitetura pode estar errada. Todo o resto (fila, EF,
e-mail, badge) assume que `auth.uid()` dentro de um DEFINER é a fronteira de autorização
enforceable e que a leitura da fila **não pode** ser uma query PostgREST. Se qualquer uma dessas
duas coisas não for verdade, é melhor descobrir agora, com uma migration e um smoke, do que depois
de dez camadas commitadas. O critério de sucesso #3 do ROADMAP exige exatamente esta prova, e exige
que ela **não** seja aviso de UI nem teste com mock (D-P42-10).

Output: a migration `p42_revisao_art20` aplicada em PROD, o smoke de 8 asserções verde, e
`database.types.ts` regenerado.

Escopo negativo: nenhum trigger de notificação (`trg_notif_*` são dos planos 42-07 e 42-08 —
a ordem obrigatória é **EF primeiro, trigger por último**, porque `net.http_post` é at-most-once e
um trigger que aponta para uma EF que ainda rejeita o evento perde a notificação sem erro em lugar
nenhum). Nenhum componente React.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-CONTEXT.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-RESEARCH.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-PATTERNS.md
@.planning/phases/42-invent-rio-gates-fila-art-20/42-VALIDATION.md
@supabase/tests/p42_revisao_art20_smoke.sql
</context>

> IDs `D-P42-NN` são índices atribuídos pelo planner sobre os bullets de `42-CONTEXT.md`
> `<decisions>`, em ordem de documento. A tabela completa está em `42-01-PLAN.md`. O prefixo
> `P42` existe porque o projeto já usa `D-NN` para decisões de milestones anteriores (por
> exemplo `D-13` em `RHSidebar.tsx` e `D-15` no guardrail do template de rejeição) — os dois
> namespaces são distintos e não devem ser confundidos.

## Questões abertas da pesquisa — resolvidas neste plano

| # | Questão | Resolução adotada |
|---|---------|-------------------|
| 1 | Uma segunda solicitação de revisão após veredito `revertida` é possível? | **Fechada em v1 pelo guard (4) do RPC**: uma revisão já respondida recusa nova resposta com `22023 'revisao ja respondida'`. É decisão registrada, não omissão. A alternativa — permitir reabertura — bloquearia o 2º e-mail **em silêncio** pelo `dedupe_key`, que é a classe exata do CR-02 |
| 2 | A fila do recrutador é vaga-scoped ou global? | **Vaga-scoped**, espelhando a policy mais restritiva (`rh_le_candidaturas`: `vagas.created_by = auth.uid()`). `administrador` vê tudo. A assimetria de `rh_le_decisao_final` fica registrada como achado A-01 (plano 42-05), **não** é corrigida de passagem |
| 4 | `revisao_veredito` como CHECK ou ENUM? | **CHECK.** Vocabulário fechado de 2 valores, auditável em `pg_constraint` ao lado do CHECK do mínimo de 50 que a mesma migration adiciona; não cria objeto de tipo novo numa fase cuja tese é inventariar o que existe; e `ALTER TYPE … ADD VALUE` carrega restrições transacionais que a P37 já pagou |

## Artifacts this phase produces (escopo deste plano)

**Migration**
- `supabase/migrations/20260730000001_p42_revisao_art20.sql`

**Colunas novas em `public.decisao_final`**
- `revisao_veredito text` · `revisao_por_usuario uuid` (FK → `auth.users(id)`) · `revisao_respondida_em timestamptz`

**Constraints**
- `decisao_final_revisao_veredito_check` · `decisao_final_revisao_resposta_completa_check` · `decisao_final_revisao_justificativa_min_check` · FK `decisao_final_revisao_por_usuario_fkey`

**Tabela nova**
- `public.config_sla_revisao` (+ policy `config_sla_revisao_rh_read`, + trigger `trg_config_sla_revisao_atualizado_em`)

**RPCs novas**
- `public.responder_revisao_decisao(uuid, text, text)` · `public.listar_revisoes_decisao(boolean)` · `public.contar_revisoes_pendentes()`

**Regenerado**
- `database.types.ts`

<!-- Literais abaixo aparecem em <action> por serem identificadores load-bearing;
     os mesmos literais sao negative-grepped em criterios de aceitacao sobre ARQUIVOS DE CODIGO,
     nunca sobre este plano. Allowlist explicita (#429): -->
<!-- planner-discipline-allow: RETURNS SETOF -->
<!-- planner-discipline-allow: FUNCTION public.tocar_atualizado_em -->
<!-- planner-discipline-allow: net.http_post -->
<!-- planner-discipline-allow: justificativa -->

<tasks>

<task type="tracer">
  <name>Task 1 (TRACER): Migration `p42_revisao_art20` — colunas, CHECKs, os 2 RPCs, a tabela de config e o trigger herdado</name>
  <read_first>
    - `supabase/migrations/20260607000003_decisao_final.sql` (ler inteiro — a tabela alvo: `por_usuario` NOT NULL, `candidatura_id` UNIQUE em `:39`, `revisao_solicitada_em`, `revisao_resultado`, e as policies em `:55-62`, incluindo o `WITH CHECK (false)` de INSERT e a **ausência** de policy de UPDATE)
    - `supabase/migrations/20260625100001_decisao_final_phase15.sql` linhas 174-231 (`solicitar_revisao_decisao` — o irmão literal desta RPC: `SET search_path = ''`, guards ordenados com `USING errcode`, readback, o par `REVOKE ALL FROM PUBLIC` + `GRANT EXECUTE TO authenticated`, e o `COMMENT` que descreve o contrato incluindo os SQLSTATEs)
    - `supabase/migrations/20260721000002_config_sla_etapa.sql` linhas 40-80 (o molde da tabela de config — ⛔ marcado NÃO APLICAR, é molde de leitura; e a policy `sla_public_read` que **não** deve ser copiada)
    - `supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql` linhas 140-175 (`public.tocar_atualizado_em()` já existe e é reutilizável; e o idioma deliberado de `CREATE TRIGGER` puro, sem `DROP TRIGGER` prévio)
    - `supabase/migrations/20260709000002_*.sql` linhas 35-50 (`rh_le_candidaturas` — o escopo por vaga a espelhar dentro do DEFINER)
    - `supabase/migrations/20260713000001_*.sql` linhas 65-115 (`is_active_rh_admin()` e `usuarios_rh_admin_select` — a razão de o nome do decisor ser ilegível por PostgREST para um recrutador)
    - `supabase/tests/p42_revisao_art20_smoke.sql` (a espec RED que esta migration tem de satisfazer — **é contrato, não sugestão**)
    - `.planning/phases/42-invent-rio-gates-fila-art-20/42-RESEARCH.md` § "Code Examples E3" (o corpo do RPC de escrita e a ordem dos guards), § "Pattern 4" (por que a leitura tem de ser RPC) e § "Pitfall 3" (a RLS que não pode ser copiada)
    - `CLAUDE.md` § Migrations (o workaround `42601` e a proibição de wrapper `BEGIN;/COMMIT;`)
  </read_first>
  <reversibility rating="costly">Colunas e RPCs são aditivos e uma migration de reversão é escrevível, mas o apply é em PROD via MCP e o ledger `schema_migrations` registra a version — desfazer exige uma segunda migration e um segundo reconcile, não um `git revert`.</reversibility>
  <action>
    Criar `supabase/migrations/20260730000001_p42_revisao_art20.sql`. **Sem wrapper `BEGIN;/COMMIT;`**
    (o driver envolve cada migration na própria transação; o wrapper externo é o gatilho do `42601`).
    Cabeçalho pt-BR no molde de `20260721000002:1-40`: requirements cobertos (REVISAO-02/03/05),
    decisões travadas (D-P42-02, D-P42-06, D-P42-07, D-P42-08, D-P42-09), proveniência dos análogos, e a nota de
    aplicação por MCP `apply_migration` + reconcile do ledger.

    **(1) Colunas em `public.decisao_final`** — três sentenças `ALTER TABLE public.decisao_final
    ADD COLUMN <nome> <tipo>;` na forma **incondicional**, uma por coluna: `revisao_veredito text`,
    `revisao_por_usuario uuid`, `revisao_respondida_em timestamptz`. D-P42-07 proíbe a variante
    condicional desta cláusula nesta fase, e o motivo vai escrito no cabeçalho: é exatamente o
    idioma que o INVENT-04 cataloga como capaz de silenciar cláusulas em silêncio, e a fase que
    documenta o defeito não pode reproduzi-lo. A FK vai em sentença própria e nomeada:
    `ALTER TABLE public.decisao_final ADD CONSTRAINT decisao_final_revisao_por_usuario_fkey
    FOREIGN KEY (revisao_por_usuario) REFERENCES auth.users(id);` — sem `ON DELETE` implícito por
    omissão de leitura: declarar `ON DELETE SET NULL` explicitamente e registrar a escolha no
    cabeçalho (a autoria da revisão é auditoria; apagar o usuário RH não pode apagar a linha de
    decisão, e as 3 FKs `NO ACTION` do FK-AUDIT-LIVE não são tocadas por esta migration).

    **(2) Três CHECKs nomeados**, todos `ADD CONSTRAINT` explícito:
    - `decisao_final_revisao_veredito_check`:
      `CHECK (revisao_veredito IS NULL OR revisao_veredito IN ('mantida','revertida'))`.
    - `decisao_final_revisao_resposta_completa_check`: coerência tudo-ou-nada entre
      `revisao_veredito`, `revisao_por_usuario` e `revisao_respondida_em` — as três nulas ou as três
      preenchidas. Impede um estado meio-respondido gravado por `service_role`.
    - `decisao_final_revisao_justificativa_min_check`:
      `CHECK (revisao_veredito IS NULL OR length(btrim(coalesce(revisao_resultado,''))) >= 50)`.
      Comentário: o guard do RPC protege o caminho da aplicação; o CHECK protege contra qualquer
      `service_role`. Os dois existem de propósito.

    **(3) `public.responder_revisao_decisao(p_candidatura_id uuid, p_veredito text, p_justificativa text)
    RETURNS public.decisao_final`**, `LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''`, com
    os guards **nesta ordem** (a ordem é load-bearing e vai comentada):
    1. papel: `(select auth.jwt() #>> '{app_metadata,role}')` tem de estar em `('rh','administrador')`,
       senão `RAISE EXCEPTION 'forbidden' USING errcode = '42501'`. O DEFINER bypassa RLS — a
       autorização tem de ser explícita aqui.
    2. alcançabilidade: `SELECT * INTO v_row FROM public.decisao_final WHERE candidatura_id = p_candidatura_id;`
       `IF NOT FOUND` → `errcode = 'no_data_found'`. Depois, `revisao_solicitada_em IS NULL` →
       `22023 'sem pedido de revisao para esta decisao'`.
    3. **GUARD REVISAO-05 (D-P42-09), absoluto:** `IF auth.uid() = v_row.por_usuario THEN RAISE EXCEPTION
       'quem registrou a decisao nao pode responder a revisao dela (decisor)' USING errcode = '42501';`
       Sem exceção de administrador, sem fallback de "só há 1 RH ativo". A mensagem tem de conter a
       palavra `decisor` porque `42501` também cobre "não é RH", e a UI precisa discriminar os dois
       sem adivinhar (o cliente casa `/decisor/i` — plano 42-03). Comentar que **(3) vem antes de
       (4)** de propósito: o decisor recebe a recusa do guard mesmo numa revisão já respondida, que
       é a mensagem honesta.
    4. idempotência: `revisao_respondida_em IS NOT NULL` → `22023 'revisao ja respondida'`. Esta é a
       resolução da questão aberta nº1 e vai comentada como decisão, não como efeito colateral.
    5. substância: `p_veredito NOT IN ('mantida','revertida')` → `22023 'veredito invalido'`;
       `length(btrim(coalesce(p_justificativa,''))) < 50` → `22023 'justificativa precisa de ao
       menos 50 caracteres'`.
    Depois, `UPDATE public.decisao_final SET revisao_veredito, revisao_resultado,
    revisao_por_usuario = auth.uid(), revisao_respondida_em = now() WHERE candidatura_id = …
    RETURNING * INTO v_row; RETURN v_row;` (readback, idioma de `solicitar_revisao_decisao`).
    **O RPC nunca chama `net.http_post`** — o despacho é do trigger, e um segundo despachante
    produziria e-mail duplo ou colisão de `dedupe_key`.
    Fechar com `REVOKE ALL ON FUNCTION … FROM PUBLIC;` + `GRANT EXECUTE ON FUNCTION … TO authenticated;`
    + `COMMENT ON FUNCTION` descrevendo o contrato **incluindo os SQLSTATEs por classe de recusa**.

    **(4) `public.listar_revisoes_decisao(p_incluir_respondidos boolean DEFAULT false)`**,
    `SECURITY DEFINER STABLE SET search_path = ''`, `RETURNS TABLE(...)` com **exatamente as 11
    colunas nomeadas** de `FILA_REVISAO_COLUNAS` (plano 42-03): `candidatura_id uuid`,
    `candidato_nome text`, `vaga_titulo text`, `decisao text`, `decidido_por_nome text`,
    `revisao_solicitada_em timestamptz`, `revisao_respondida_em timestamptz`,
    `revisao_veredito text`, `revisao_resultado text`, `respondida_por_nome text`,
    `pode_responder boolean`.
    - **Nunca** `RETURNS SETOF public.decisao_final` — isso é o mesmo que projetar todas as colunas
      por outro nome e arrastaria a `justificativa` interna do recrutador para a fila (BD-9 em
      aberto).
    - Guard de papel idêntico ao do RPC de escrita (`42501` se não for RH/administrador).
    - **Escopo re-implementado** (questão aberta nº2): `administrador` vê todas as linhas; o papel
      `rh` vê apenas as candidaturas cujas vagas tenham `created_by = auth.uid()`, espelhando
      `rh_le_candidaturas`. O DEFINER bypassa RLS, então este escopo tem de ser um predicado
      explícito no `WHERE`.
    - `decidido_por_nome` e `respondida_por_nome` resolvem por join em `public.usuarios_rh` pelo
      `user_id`, projetando `nome_completo`; sem correspondência → `NULL` (a UI mostra
      "Não identificado"; um UUID **nunca** chega à tela).
    - `pode_responder` = `revisao_respondida_em IS NULL AND por_usuario IS DISTINCT FROM auth.uid()`
      — é o espelho **cosmético** do guard, calculado no servidor para a UI não recalcular regra de
      autorização. Comentar que quem impede continua sendo o guard do RPC de escrita.
    - Filtro: `revisao_solicitada_em IS NOT NULL`; quando `p_incluir_respondidos` é falso, também
      `revisao_respondida_em IS NULL` (D-P42-04).
    - `ORDER BY revisao_solicitada_em ASC` (D-P42-04, mais antigo primeiro) e `LIMIT 200` server-side.
    - `REVOKE ALL … FROM PUBLIC` + `GRANT EXECUTE … TO authenticated` + `COMMENT`.

    **(5) `public.contar_revisoes_pendentes() RETURNS integer`**, `SECURITY DEFINER STABLE
    SET search_path = ''`, mesmo guard de papel e **mesmo escopo por vaga** de (4), contando as
    linhas com `revisao_solicitada_em IS NOT NULL AND revisao_respondida_em IS NULL`. É o que
    alimenta o contador da sidebar (D-P42-01). Mesmo par REVOKE/GRANT e `COMMENT`.

    **(6) `public.config_sla_revisao`** — reusar o **padrão** de `config_sla_etapa`, nunca a
    tabela (a PK dela é um enum de etapa e não há valor para "revisão Art. 20") e **nunca** a
    policy. Forma: `chave text PRIMARY KEY`, `dias_atencao integer NOT NULL CHECK (dias_atencao > 0)`,
    `dias_atraso integer NOT NULL CHECK (dias_atraso > 0)`, `descricao text`,
    `atualizado_em timestamptz NOT NULL DEFAULT now()`, mais
    `CONSTRAINT ck_config_sla_revisao_ordem CHECK (dias_atraso > dias_atencao)`.
    `ALTER TABLE … ENABLE ROW LEVEL SECURITY;` e **uma única** policy:
    `CREATE POLICY config_sla_revisao_rh_read ON public.config_sla_revisao FOR SELECT TO authenticated
    USING ((select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador'));`
    Nenhuma policy de escrita (default-deny; a alteração do limiar é operação de banco, não de
    aplicação). Comentário de invariante: esta tabela guarda o limiar **interno** de acompanhamento
    (D-P42-03); torná-la legível sem autenticação derrubaria por baixo da UI a invariante de que o SLA
    nunca é exibido ao candidato. Seed único com `ON CONFLICT DO NOTHING`:
    `('revisao_art20', 7, 15, 'Meta operacional interna da equipe para responder pedidos de revisão do Art. 20. BD-4 em aberto: o número é decisão do operador e altera-se por UPDATE, sem deploy.')`.
    Escrever no comentário que 7/15 é **seed**, não recomendação técnica, e que o Art. 20 não fixa
    prazo — a copy da UI e deste arquivo nunca chama isto de exigência estatutária.

    **(7) Trigger de `atualizado_em` — trabalho herdado, não novo.** Escrever **apenas**
    `CREATE TRIGGER trg_config_sla_revisao_atualizado_em BEFORE UPDATE ON public.config_sla_revisao
    FOR EACH ROW EXECUTE FUNCTION public.tocar_atualizado_em();`. A função **já existe**
    (`20260722000002:144`) e é reutilizável — **não** redefini-la. `CREATE TRIGGER` puro, **sem**
    `DROP TRIGGER IF EXISTS` prévio: é o idioma deliberado da P37, que prefere falhar alto contra um
    trigger inesperado a substituí-lo em silêncio. Comentar a assimetria com os triggers de
    `pg_net` dos planos 42-07/42-08, que **usam** `DROP TRIGGER IF EXISTS` porque recriam um trigger
    que já existiu — as duas convenções coexistem por razões diferentes e não devem ser
    uniformizadas.
  </action>
  <verify>
    <automated>M=supabase/migrations/20260730000001_p42_revisao_art20.sql; test -f $M && grep -c "SET search_path = ''" $M && test $(grep -v '^--' $M | grep -ciE 'add column[[:space:]]+if[[:space:]]+not[[:space:]]+exists') -eq 0 && test $(grep -v '^--' $M | grep -c 'TO anon') -eq 0 && test $(grep -v '^--' $M | grep -ciE '^[[:space:]]*(BEGIN|COMMIT);') -eq 0 && test $(grep -v '^--' $M | grep -c 'RETURNS SETOF') -eq 0 && echo MIGRATION_SHAPE_OK</automated>
  </verify>
  <acceptance_criteria>
    - **Asserção negativa (D-P42-07, autoconsistência com INVENT-04):** `grep -v '^--' <migration> | grep -ciE 'add column[[:space:]]+if[[:space:]]+not[[:space:]]+exists'` == 0.
    - **Asserção negativa (Pitfall 3):** `grep -v '^--' <migration> | grep -c 'TO anon'` == 0.
    - **Asserção negativa (Pitfall 8):** `grep -v '^--' <migration> | grep -c 'RETURNS SETOF'` == 0.
    - **Asserção negativa (CLAUDE.md / 42601):** nenhuma linha executável `BEGIN;` ou `COMMIT;` no topo ou no fim do arquivo.
    - **Asserção negativa (trigger herdado):** `grep -v '^--' <migration> | grep -c 'FUNCTION public.tocar_atualizado_em'` == 1 e `grep -v '^--' <migration> | grep -ciE 'create[[:space:]]+(or[[:space:]]+replace[[:space:]]+)?function[[:space:]]+public\.tocar_atualizado_em'` == 0 — a função é reusada, nunca redefinida.
    - **Asserção negativa (Pitfall 9):** `grep -v '^--' <migration> | grep -c 'net.http_post'` == 0 — o RPC não despacha nada.
    - `grep -c "SET search_path = ''" <migration>` == 3 (os três RPCs).
    - `grep -c 'REVOKE ALL ON FUNCTION' <migration>` == 3 e `grep -c 'GRANT EXECUTE ON FUNCTION' <migration>` == 3.
    - `grep -c 'COMMENT ON FUNCTION' <migration>` == 3, e cada `COMMENT` nomeia pelo menos um SQLSTATE.
    - O `RETURNS TABLE` de `listar_revisoes_decisao` declara **exatamente 11** colunas, e `grep -c 'justificativa' <migration>` == 0 fora de comentário.
    - `grep -c "(decisor)" <migration>` >= 1 — a mensagem do guard carrega o discriminador que o cliente casa.
    - O seed de `config_sla_revisao` usa `ON CONFLICT DO NOTHING` e a tabela tem `ENABLE ROW LEVEL SECURITY` com **exatamente uma** policy.
    - `grep -ciE 'prazo legal|prazo da lei|prazo LGPD' <migration>` == 0.
  </acceptance_criteria>
  <done>A migration existe, satisfaz por leitura estática todas as invariantes de forma que a fase declarou, e está pronta para o apply — cuja prova ao vivo é o checkpoint imediatamente seguinte, no mesmo plano.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (CHECKPOINT): Aplicar a migration, reconciliar o ledger e rodar o smoke de 8 asserções</name>
  <what-built>
    `supabase/migrations/20260730000001_p42_revisao_art20.sql` — colunas, 3 CHECKs, FK nomeada, os 3
    RPCs `SECURITY DEFINER`, a tabela `config_sla_revisao` com RLS RH-only e seed, e o trigger que
    reusa `public.tocar_atualizado_em()`. Verificada estaticamente contra 12 invariantes de forma.
    Nada foi aplicado: subagentes GSD não recebem os tools MCP do Supabase (STATE.md), e
    `db push --linked` está proibido neste projeto (`42601` garantido em corpos `$$`).
  </what-built>
  <how-to-verify>
    O **orquestrador / main thread**, nesta ordem:

    1. **Antes do apply**, ler o estado corrente que a migration presume:
       `SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='decisao_final';`
       — confirmar que `revisao_veredito`, `revisao_por_usuario` e `revisao_respondida_em`
       **não existem ainda**. Se alguma já existir, PARE: a migration usa a forma incondicional de
       `ADD COLUMN` de propósito e vai falhar alto — que é o comportamento desejado, mas indica
       drift a investigar antes.
    2. Aplicar via MCP `apply_migration` com o nome `p42_revisao_art20`. **Nunca** `db push`.
    3. **Reconciliar o ledger** (obrigatório após CADA `apply_migration`, precedente P37-04 /
       P39-04 / P41-05): o MCP grava a version com timestamp próprio; ajustar
       `supabase_migrations.schema_migrations.version` para casar com o prefixo do arquivo
       (`20260730000001`), preservando a ordem em relação à P41 (`20260727000001`). Confirmar zero
       drift novo.
    4. **Rodar `supabase/tests/p42_revisao_art20_smoke.sql` numa ÚNICA chamada `execute_sql`** —
       `set_config(..., false)` é escopado à sessão, e statements espalhados por chamadas separadas
       zerariam o contador e reprovariam no RESUMO por run parcial (lição P41-05).
       **Esperado: 8/8 PASS.** Colar o output inteiro (os NOTICEs + o RESUMO).
    5. Se o smoke reprovar: **corrigir a implementação, não o smoke.** O arquivo é a espec escrita
       antes da migration; alterá-lo para caber na implementação é escalar, não resolver. Divergência
       genuína de spec vira nova rodada deste checkpoint com a justificativa registrada.
    6. Registrar no VERIFICATION.md da fase o output das asserções **(f)** e **(g)** — a tentativa
       real do decisor barrada com `42501 …(decisor)` e a asserção negativa de que nada foi gravado.
       É a evidência exigida nominalmente pelo critério de sucesso #3 do ROADMAP (D-P42-10).
    7. Confirmar que a fixture do smoke foi removida: a linha de teste de `decisao_final` não pode
       ficar em PROD.
  </how-to-verify>
  <resume-signal>Cole o output do smoke (8/8) + a confirmação do reconcile do ledger + a confirmação de que a fixture foi removida; ou descreva a asserção que reprovou.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Regenerar `database.types.ts` com a cautela do redirecionamento truncante</name>
  <read_first>
    - `package.json` linha 104 (`"db:types": "npx supabase gen types typescript --linked > database.types.ts"` — o `>` **trunca antes** de o comando rodar)
    - `.planning/STATE.md` § Decisions, entradas `[Phase 37 · 37-05]` (o precedente completo: provar contra arquivo temporário; `supabase link` resolve o estado local ausente sem prompt e sem contato de escrita com PROD; esperar **zero deleções** no diff)
    - `database.types.ts` (o arquivo alvo — não editar à mão, CLAUDE.md)
  </read_first>
  <precondition>A migration `20260730000001_p42_revisao_art20` foi aplicada em PROD e o ledger reconciliado (checkpoint da Task 2).</precondition>
  <action>
    Gerar os tipos para um arquivo **temporário** primeiro
    (`npx supabase gen types typescript --linked > /tmp/database.types.next.ts`), nunca apontando o
    gerador ao arquivo trackeado numa execução exploratória — o `>` trunca antes de executar, então
    "rodar pra ver se funciona" destrói o arquivo. Se o comando falhar por estado de link ausente
    (`supabase/.temp/` é gitignored), rodar `supabase link` — o precedente P37-05 mostra que isso
    resolve sem prompt e sem contato de escrita com PROD; escalar antes de tentar isso reportaria um
    bloqueio inexistente.

    Diffar `/tmp/database.types.next.ts` contra `database.types.ts`. O esperado é **zero deleções**
    e adições restritas a: as 3 colunas novas de `decisao_final` (nos 3 blocos `Row`/`Insert`/`Update`),
    a tabela `config_sla_revisao`, e as 3 funções novas. Qualquer deleção, ou qualquer adição fora
    dessa lista, é **drift lateral** — parar e reportar como achado antes de sobrescrever.

    Só então copiar o arquivo temporário sobre `database.types.ts`. Registrar no SUMMARY a contagem
    de linhas adicionadas/removidas e a enumeração dos hunks, no formato do precedente P37-05.
  </action>
  <verify>
    <automated>npm run -s lint 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `database.types.ts` contém `revisao_veredito`, `revisao_por_usuario` e `revisao_respondida_em` dentro do bloco de `decisao_final`.
    - `database.types.ts` contém `config_sla_revisao`, `responder_revisao_decisao`, `listar_revisoes_decisao` e `contar_revisoes_pendentes`.
    - O diff contra a versão anterior tem **zero deleções**. Se houver qualquer deleção, a task PARA e reporta em vez de sobrescrever.
    - `npm run -s lint 2>&1 | grep -c "error TS"` continua **≤ 97** — a regeneração não pode introduzir erro de tipo novo.
    - `sh .husky/pre-commit` sai 0 (o gate convertido no plano 42-01 já vale aqui).
    - O SUMMARY registra a enumeração dos hunks e confirma que nenhum deles está fora da lista esperada.
  </acceptance_criteria>
  <done>Os hooks e serviços dos planos 42-09/42-10 podem chamar as 3 RPCs com tipagem real, e o repositório não carrega drift lateral de tipos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente RH (`authenticated`) → RPC `SECURITY DEFINER` | O DEFINER roda com privilégio do dono e **bypassa RLS**: toda autorização e todo escopo têm de ser predicado explícito dentro da função |
| `service_role` → `decisao_final` | Um caminho privilegiado que contorne o RPC ainda é barrado pelos CHECKs de coluna — por isso guard e CHECK coexistem |
| RPC de leitura → navegador do recrutador | Cada coluna projetada é uma coluna que sai do banco para uma máquina de RH |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-02 | Elevation of Privilege | guard reviewer ≠ decider em `responder_revisao_decisao` | critical | mitigate | Guard absoluto comparando `auth.uid()` com `por_usuario`, sem override nem fallback; provado por impersonação real de dois RHs distintos (asserção (f) do smoke), com asserção negativa (g) de que nada foi gravado na tentativa barrada |
| T-42-03 | Information Disclosure | `listar_revisoes_decisao` | high | mitigate | `RETURNS TABLE` com 11 colunas nomeadas; `RETURNS SETOF` proibido por gate de grep; `justificativa` ausente do arquivo inteiro fora de comentário |
| T-42-11 | Information Disclosure | `config_sla_revisao` | high | mitigate | Policy única `FOR SELECT TO authenticated` restrita a `rh`/`administrador`; gate de grep reprova qualquer concessão ao papel anônimo |
| T-42-20 | Information Disclosure | vazamento lateral entre recrutadores | high | mitigate | Escopo por vaga re-implementado dentro do DEFINER espelhando `rh_le_candidaturas`; sem ele, um recrutador veria a existência e o timestamp de revisões de vagas alheias |
| T-42-21 | Tampering | escrita direta em `decisao_final` pelo cliente | high | mitigate | `decisao_final` não tem policy de UPDATE (default-deny) e o INSERT é `WITH CHECK (false)`; o RPC DEFINER é o único caminho e é onde o guard vive. Os 3 CHECKs cobrem o caminho `service_role` |
| T-42-22 | Repudiation | resposta sem autoria | medium | mitigate | `revisao_por_usuario = auth.uid()` gravado pelo próprio RPC (nunca vindo do cliente) + CHECK de coerência tudo-ou-nada |
| T-42-SC | Tampering | instalações npm/pip/cargo | high | accept | Zero pacote novo; nenhuma extensão Postgres nova (`pg_cron`, `pg_net`, `pgcrypto`, `supabase_vault` já vivas e versionadas) |
</threat_model>

<verification>
- Os 6 gates de grep negativo do Task 1 passam sobre o arquivo de migration.
- `p42_revisao_art20_smoke.sql` fecha **8/8** numa única chamada `execute_sql`, com a fixture removida ao fim.
- O output das asserções (f) e (g) está registrado no VERIFICATION.md da fase.
- O ledger `schema_migrations` está reconciliado, com a version `20260730000001` em sequência após `20260727000001` e zero drift novo.
- `database.types.ts` regenerado com zero deleções.
- `npm run -s lint 2>&1 | grep -c "error TS"` ≤ 97 e `sh .husky/pre-commit` sai 0.
</verification>

<success_criteria>
- O critério de sucesso #3 do ROADMAP está satisfeito e evidenciado: quem registrou a decisão tenta responder à revisão dela e é barrado pelo servidor, provado por tentativa real com JWT impersonado — não por aviso de UI, não por mock.
- A arquitetura da fase está provada na camada em que ela pode estar errada: `auth.uid()` dentro do DEFINER é fronteira enforceable, e a fila tem um caminho de leitura que resolve o nome do decisor sem depender de uma policy que o recrutador não passa.
</success_criteria>

<output>
Create `.planning/phases/42-invent-rio-gates-fila-art-20/42-06-SUMMARY.md` when done
</output>
