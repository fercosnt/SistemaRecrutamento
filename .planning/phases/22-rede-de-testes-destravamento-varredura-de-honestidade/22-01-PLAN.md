---
phase: 22-rede-de-testes-destravamento-varredura-de-honestidade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/deno.json
  - supabase/functions/_shared/__tests__/ai-client.test.ts
autonomous: true
requirements: [CI-01, CI-02]
must_haves:
  truths:
    - "Running the Deno EF corpus with type-check ON exits 0 (was 148 pass / 1 fail / 2 type-errors) — CI-02"
    - "The Vitest-only Node probe strict-schema.test.ts is no longer collected by the Deno runner — CI-01/CI-02"
    - "The corpus command that Plan 22-06 wires into ci.yml is proven green locally first — CI-01"
    - "Zero product code changed — only test-side type + a runner-scope config"
  artifacts:
    - path: "supabase/functions/deno.json"
      provides: "Deno runner config that excludes the strict-schema Vitest probe from `deno test`"
      contains: "exclude"
    - path: "supabase/functions/_shared/__tests__/ai-client.test.ts"
      provides: "corpus test with the stale local timeoutMs type corrected to match CallAiArgs"
  key_links:
    - from: "supabase/functions/deno.json"
      to: "supabase/functions/_shared/__tests__/strict-schema.test.ts"
      via: "exclude glob"
      pattern: "strict-schema"
---

<objective>
Make the already-existing (but never-CI-run) Deno Edge-Function test corpus pass **green under type-check** so Plan 22-06 can wire it into `ci.yml` as a blocking gate. The corpus is *almost* green: a raw run yields 148 pass / 1 fail, and type-check surfaces exactly 2 errors. All three failures are TEST-SIDE and fixed with zero product change (per 22-RESEARCH §Deno-in-CI, Pitfall 3, Pitfall — ai-client).

Purpose: The Deno layer produced every live defect in M1–M3; this phase turns it into a regression net. Green corpus is the precondition for the blocking `deno-test` job.
Output: `supabase/functions/deno.json` (runner exclude) + a one-line type fix in `ai-client.test.ts`. Corpus runs green locally under type-check ON.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-RESEARCH.md
@.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-CONTEXT.md

<notes>
- 22-RESEARCH §Deno-in-CI: raw `deno test --allow-all --no-check supabase/functions` = 148 pass / 1 fail (strict-schema `__dirname`); `deno test --no-run` (type-check) = 2 errors (`ai-client.test.ts:242`, `strict-schema.test.ts:88`).
- 22-RESEARCH Pitfall 3: `strict-schema.test.ts` lives under `_shared/__tests__/` (Deno territory) but is a **Vitest** Node probe (`import { describe, it, expect } from 'vitest'`, `__dirname`, `node:fs`). Excluding it from the Deno run kills BOTH its runtime fail and its type-check fail. It KEEPS running under Vitest (vite.config.ts:16-18 keeps it in the Vitest include). Do NOT delete it.
- The `timeoutMs` error is a STALE LOCAL type in the test file — the real `CallAiArgs` already carries `timeoutMs` (added Phase 18/21, see the passing test at ai-client.test.ts:242 that already passes `timeoutMs: 60_000`). Fix the local declaration/cast, not the product `CallAiArgs`.
- Deno config discovery walks up from CWD, not from the target path — so pass `--config` explicitly (see task actions) rather than relying on auto-discovery from repo root.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exclude the strict-schema Vitest probe from the Deno runner</name>
  <files>supabase/functions/deno.json</files>
  <read_first>
    - 22-RESEARCH.md §Architecture Patterns → Pattern 2 (Deno test job scoped away from Vitest probes) + Pitfall 3
    - supabase/functions/_shared/__tests__/strict-schema.test.ts (confirm it is a Vitest probe: `from 'vitest'`, `__dirname`, `node:fs`)
    - vite.config.ts:16-18 (confirms strict-schema is intentionally KEPT in Vitest — do not touch that)
  </read_first>
  <action>
    Create `supabase/functions/deno.json` with a top-level `exclude` array whose single entry is the strict-schema probe path RELATIVE to the config file's directory: `_shared/__tests__/strict-schema.test.ts`. Top-level `exclude` (not `test.exclude`) so it is honored globally. Because Deno discovers config from CWD, the canonical corpus command (used here and re-used by 22-06) passes the config explicitly: `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions`. If `--config`+`exclude` does not collect-skip the file on this Deno version, fall back to the equivalent `--ignore=supabase/functions/_shared/__tests__/strict-schema.test.ts` flag on the command — the acceptance is empirical (file not collected, exit 0). Do not add any other exclude. Do not modify strict-schema.test.ts.
  </action>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions 2>&1 | grep -q "strict-schema" && echo "LEAKED" || echo "excluded-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/functions/deno.json` exists and its `exclude` names `_shared/__tests__/strict-schema.test.ts`.
    - The Deno corpus run does NOT collect/execute `strict-schema.test.ts` (no `ReferenceError: __dirname` and no `strict-schema` file in the run output).
    - `git diff --stat supabase/functions/_shared/__tests__/strict-schema.test.ts` is empty (probe untouched).
  </acceptance_criteria>
  <done>The Deno corpus no longer globs the Vitest probe; both its runtime (`__dirname`) and type-check (`TS7053` at :88) failures disappear from the Deno run.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the stale local timeoutMs type in ai-client.test.ts</name>
  <files>supabase/functions/_shared/__tests__/ai-client.test.ts</files>
  <read_first>
    - 22-RESEARCH.md §phase_requirements (CI-02 row: "fix stale local `timeoutMs` type at `ai-client.test.ts:242`, real `CallAiArgs` already has it")
    - supabase/functions/_shared/__tests__/ai-client.test.ts around :238-246 (the P21 `timeoutMs` override test) and wherever `CallAiArgs`/`baseArgs` local type is declared in the file
    - supabase/functions/_shared/ai-client.ts (the REAL `CallAiArgs` — confirm it already declares `timeoutMs?: number`; do NOT change it)
  </read_first>
  <action>
    The type-check error is a STALE LOCAL type in the test file — a local `CallAiArgs`-shaped type or cast in the test does not include `timeoutMs`, while the production `CallAiArgs` (in `_shared/ai-client.ts`) already does (added in Phase 18/21). Correct the test's local declaration/cast so `timeoutMs?: number` is present (or import the real `CallAiArgs` type instead of re-declaring). This is a test-side type correction only — zero product change. Confirm the fix by running the corpus with type-check ON and seeing 0 type errors.
  </action>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions` exits 0 with type-check ON (no `--no-check`).
    - No type error referencing `timeoutMs` remains in the Deno type-check output.
    - `git diff supabase/functions/_shared/ai-client.ts` is empty (only the TEST file changed).
  </acceptance_criteria>
  <done>The corpus type-checks and runs green (target ~148+ tests pass, 0 fail, 0 type errors) — ready for 22-06 to wire as a blocking CI job.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → Deno test process | Repo-authored test code executes under Deno; tests inject mocks, no network at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Tampering | Deno runner scope (glob picks up wrong-runtime file) | mitigate | `deno.json` exclude confines the run to Deno-native tests; strict-schema stays under Vitest only |
| T-22-01-SC | Tampering | package-manager installs | accept | Zero net-new packages this plan; no `npm`/Deno registry install. Per 22-RESEARCH §Package Legitimacy Audit — slopcheck N/A |
</threat_model>

<verification>
- `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions` exits 0 (type-check ON).
- `npm run test:run` still green (strict-schema.test.ts still runs under Vitest — not broken by the exclude).
- No product file (`_shared/ai-client.ts`, any EF handler) changed.
</verification>

<success_criteria>
The Deno EF corpus passes green under type-check ON locally, with the strict-schema Vitest probe excluded from the Deno runner and the stale `timeoutMs` type corrected — zero product change. This unblocks the blocking `deno-test` CI job in Plan 22-06 (CI-01).
</success_criteria>

<output>
Create `.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-01-SUMMARY.md` when done.
</output>
