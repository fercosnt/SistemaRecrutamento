---
phase: 22-rede-de-testes-destravamento-varredura-de-honestidade
plan: 06
type: execute
wave: 2
depends_on: ["22-01", "22-02", "22-03", "22-04", "22-05"]
files_modified:
  - tsconfig.json
  - e2e/login-flow.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [CI-01, CI-04, CI-05, CI-14]
must_haves:
  truths:
    - "The 65 phantom TS2307 (versioned imports) are resolved via tsconfig `paths` — `npm run lint | grep -c TS2307` == 0 (CI-05)"
    - "tsc coverage now includes e2e/, scripts/, and playwright.config.ts, with the Deno files in scripts/ excluded (no TS2304 'Deno') (CI-14)"
    - "The ci.yml tsc gate is pinned to the REAL measured green baseline (measure-first, ~133 — not the loose 290, not a guessed number) and goes red above it (CI-04)"
    - "A blocking `deno-test` job runs the EF corpus (type-check ON) in ci.yml (CI-01)"
  artifacts:
    - path: "tsconfig.json"
      provides: "versioned-import `paths` + expanded `include` + Deno-file `exclude`"
      contains: "node_modules/lucide-react"
    - path: ".github/workflows/ci.yml"
      provides: "deno-test blocking job + tsc gate pinned to measured baseline"
      contains: "denoland/setup-deno"
  key_links:
    - from: "tsconfig.json paths"
      to: "vite.config.ts resolve.alias"
      via: "mirror every versioned specifier to its node_modules bare package"
      pattern: "lucide-react@0.487.0"
    - from: ".github/workflows/ci.yml tsc gate"
      to: "npm run lint measured count"
      via: "pin the frozen baseline to the measured green number"
      pattern: "COUNT.*-gt"
---

<objective>
Land the typecheck-destravamento and CI wiring — the FINAL, measure-first plan of the phase. Add tsconfig `paths` to resolve the 65 phantom `TS2307` (which cascades the total from 257 to ~133), expand tsc coverage to `e2e/`/`scripts/`/`playwright.config.ts` while excluding the Deno files that would otherwise throw `TS2304 'Deno'`, wire the blocking `deno-test` job into `ci.yml`, and — as the LAST task — measure the real green `npm run lint` count and pin THAT number into the ci.yml tsc gate.

Purpose: Unresolved versioned imports were silently typing whole shadcn components as `any`, masking real drift. Resolving them via `paths` (tsc-only, preserves Vite `resolve.dedupe`) finally type-checks the UI layer. Measure-first is load-bearing: 257 minus 65 is NOT the answer; the real post-fix number must be measured after ALL Wave-1 edits land, then pinned (per 22-RESEARCH Pitfall 1).
Output: tsconfig with paths+coverage+excludes, e2e unused-import fixed, ci.yml with the Deno job + measured tsc gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-RESEARCH.md
@.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-CONTEXT.md

<interfaces>
Current tsconfig.json (verified): baseUrl "." · paths `{ "@/*": ["./src/*"] }` · include `["src"]` · strict + noUnusedLocals/noUnusedParameters · references to tsconfig.node.json.

Current ci.yml tsc gate (verified, lines 48-56): step named "Type-check (frozen tsc baseline 290 — CI red only on growth)" runs `COUNT=$(npm run -s lint 2>&1 | grep -c "error TS" || true)` and fails when `[ "$COUNT" -gt 290 ]`. Existing jobs use actions/checkout@v4 + actions/setup-node@v4 (node 20). Add the Deno job in the same idiom.

Versioned aliases live in vite.config.ts resolve.alias (~37 versioned specifiers such as `'lucide-react@0.487.0': 'lucide-react'`, plus 8 `figma:asset/*` — SKIP those).

Deno files inside scripts/: `scripts/sync-prompts.ts` + `scripts/sync-prompts.test.ts` (and anything under `scripts/__tests__/`) — Deno globals / npm: / https: imports → must be EXCLUDED from Node tsc. `scripts/assert-chunks.mjs` is `.mjs` and is NOT tsc-collected (no allowJs) → no exclude needed.

Canonical Deno corpus command (from Plan 22-01, verified green): `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions`.

e2e unused import: `login-flow.spec.ts:57` declares an unused `expectAuthenticated` helper → `TS6133` once e2e enters coverage (research: e2e contributes exactly this 1 error before the fix, 0 after).
</interfaces>

<notes>
- 22-RESEARCH Pitfall 1 (THE big one): resolving the 65 TS2307 cascades 257 to ~133 (implicit-any TS7006 drops 43 to 1, etc.). Do NOT hardcode 133, 192, or 257 — MEASURE and pin the real number. Validated by scratch tsconfig (zero new error codes).
- 22-RESEARCH Pitfall 2: adding `scripts/` to include pulls in the Deno `sync-prompts` files → 18 unfixable Node-tsc errors. Exclude them (same treatment the EF corpus gets by simply not being in include). After exclusion: scripts contributes 0, e2e contributes exactly 1 (the unused import — fix it → 0).
- Pattern 1: paths is mechanically derivable from vite.config.ts aliases — for each `'<spec>@<ver>': '<bare>'` emit `"<spec>@<ver>": ["./node_modules/<bare>"]`; skip `figma:asset/*`; keep `@/*` and `@`. Preserves `resolve.dedupe` (tsc-only, no runtime touch).
- Deno job: `denoland/setup-deno@v2` with `deno-version: v2.x`; BLOCKING (not allow-fail) per CONTEXT "a suíte para de apodrecer". Command = the canonical one above.
- This plan is Wave 2: it runs AFTER 22-01..05 merge. The measured baseline MUST be taken with all those edits present (isValid removal, extended guards, de-hardcoded specs, new guard files) or the pinned number will be wrong. Task 3 (measure + pin) is LAST.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tsconfig paths to resolve the 65 versioned-import TS2307 (CI-05)</name>
  <files>tsconfig.json</files>
  <read_first>
    - tsconfig.json (current compilerOptions.paths + baseUrl)
    - vite.config.ts:62-108 (the full resolve.alias list — source of truth for the versioned specifiers)
    - 22-RESEARCH.md §Architecture Patterns Pattern 1 + Pitfall 1
  </read_first>
  <action>
    In tsconfig.json compilerOptions.paths, keep `"@/*": ["./src/*"]` and add one entry per VERSIONED alias in vite.config.ts: for each `'<spec>@<ver>': '<bare>'` emit `"<spec>@<ver>": ["./node_modules/<bare>"]` (paths are relative to baseUrl "."). Cover all ~37 versioned specifiers (lucide-react@0.487.0, class-variance-authority@0.7.1, recharts@2.15.2, react-hook-form@7.55.0, every `@radix-ui/*@x`, vaul@1.1.2, react-resizable-panels@2.1.7, react-day-picker@8.10.1, next-themes@0.4.6, input-otp@1.4.2, embla-carousel-react@8.6.0, cmdk@1.1.1, etc.). SKIP the `figma:asset/*` aliases (they map to local png assets, not node_modules). Do NOT rewrite any source import. After the edit, TS2307 must be 0 and the total must drop well below 257 with zero NEW error codes.
  </action>
  <verify>
    <automated>npm run -s lint 2>&1 | grep -c "error TS2307"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run -s lint 2>&1 | grep -c "error TS2307"` == 0.
    - Total `npm run -s lint 2>&1 | grep -c "error TS"` is materially below 257 (cascade confirmed; exact number is measured/pinned in Task 3).
    - No source file under src/ was modified (only tsconfig.json).
  </acceptance_criteria>
  <done>Versioned imports resolve under tsc; the phantom TS2307 are gone and the masked drift is now visible.</done>
</task>

<task type="auto">
  <name>Task 2: Expand tsc coverage to e2e/scripts/playwright, exclude Deno files, fix e2e unused import (CI-14)</name>
  <files>tsconfig.json, e2e/login-flow.spec.ts</files>
  <read_first>
    - tsconfig.json (include array — currently `["src"]`)
    - 22-RESEARCH.md Pitfall 2 (scripts/sync-prompts Deno trap) + §tsc Coverage
    - e2e/login-flow.spec.ts around :55-58 (the unused expectAuthenticated helper)
    - scripts/ listing (sync-prompts.ts + sync-prompts.test.ts are the Deno files; assert-chunks.mjs is .mjs)
  </read_first>
  <action>
    In tsconfig.json expand include to `["src", "e2e", "scripts", "playwright.config.ts"]`. Add an exclude array covering the Deno files that would throw `TS2304 'Deno'` / npm:/https: `TS2307` under Node tsc: `"scripts/sync-prompts.ts"`, `"scripts/sync-prompts.test.ts"`, plus the glob `"scripts/**/sync-prompts*.ts"` to catch any under scripts/__tests__/. Do NOT add `supabase` to include (the EF corpus stays under `deno test`). Then fix the one genuine e2e error: delete the unused `expectAuthenticated` declaration at login-flow.spec.ts:57 (research confirms it is unused). After the edit, lint must show NO `TS2304: Cannot find name 'Deno'` and e2e must contribute 0 errors.
  </action>
  <verify>
    <automated>echo "Deno leaks:"; npm run -s lint 2>&1 | grep -c "TS2304.*Deno"; echo "login-flow errors:"; npm run -s lint 2>&1 | grep -c "login-flow.spec"</automated>
  </verify>
  <acceptance_criteria>
    - tsconfig.json include lists `e2e`, `scripts`, `playwright.config.ts`; exclude lists the Deno sync-prompts files.
    - `npm run -s lint 2>&1 | grep -c "TS2304.*Deno"` == 0 (no Deno file leaked into Node tsc).
    - `npm run -s lint 2>&1 | grep -c "login-flow.spec"` == 0 (unused import fixed).
    - `npm run test:run` still green (the e2e edit is dead-code removal only).
  </acceptance_criteria>
  <done>tsc covers e2e/scripts/playwright with the Deno files excluded and the one real e2e error fixed — coverage expanded, no phantom Deno errors.</done>
</task>

<task type="auto">
  <name>Task 3 (LAST): Wire the blocking Deno CI job, then measure and pin the real tsc baseline (CI-01, CI-04)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full — the unit job tsc gate at :48-56 + job idioms checkout@v4 / setup-node@v4)
    - 22-RESEARCH.md §Architecture Patterns Pattern 2 (ci.yml Deno job YAML) + Pitfall 1 (measure-first) + §State of the Art
    - Plan 22-01 canonical Deno command (the CI job command must match it)
  </read_first>
  <action>
    (a) Add a new BLOCKING `deno-test` job to ci.yml as a sibling of unit/e2e/lighthouse: `runs-on: ubuntu-latest`; steps are actions/checkout@v4, denoland/setup-deno@v2 with `deno-version: v2.x`, then run `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions`. It is a normal job (NO continue-on-error) so a red corpus fails the pipeline (CI-01). (b) THEN, as the LAST action of the phase, run `npm run -s lint 2>&1 | grep -c "error TS"` on the fully-merged tree (all Wave-1 edits present) to get the REAL green COUNT. Rewrite the unit job tsc-gate step (:48-56): replace every `290` with that measured COUNT, update the step name to `Type-check (frozen tsc baseline <COUNT> — CI red only on growth)`, and keep the `-gt <COUNT>` red-on-growth logic. Do NOT guess or hardcode 133/257 — pin the measured number and record it in the SUMMARY.
  </action>
  <verify>
    <automated>MEASURED=$(npm run -s lint 2>&1 | grep -c "error TS"); echo "measured=$MEASURED"; grep -nE "baseline [0-9]+|-gt [0-9]+|denoland/setup-deno" .github/workflows/ci.yml; node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); if(y.includes('-gt 290')){console.error('GATE STILL 290');process.exit(1)} if(!y.includes('denoland/setup-deno')){console.error('NO DENO JOB');process.exit(1)} console.log('ok: deno job present + gate off 290')"</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml contains a `deno-test` job using `denoland/setup-deno@v2` and the canonical corpus command; it is blocking (no continue-on-error).
    - The unit-job tsc gate no longer references `290`; it uses the freshly measured COUNT in both the step name and the `-gt` threshold.
    - `npm run -s lint 2>&1 | grep -c "error TS"` equals (or is below) the pinned baseline — the gate is green on the current tree and red only on growth.
    - The measured baseline number is recorded in the plan SUMMARY.
  </acceptance_criteria>
  <done>The Deno corpus runs as a blocking CI job and the tsc gate is pinned to the real measured green baseline — regressions in either surface now fail CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions marketplace → CI job | Third-party action (`denoland/setup-deno`) executes in the runner |
| tsc gate threshold → regression detection | A loose/guessed baseline lets new type errors pass silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06-01 | Tampering | silent green regression (loose 290 gate) | mitigate | Pin the gate to the measured real baseline (~133); red-on-growth catches new drift |
| T-22-06-02 | Tampering | `denoland/setup-deno` third-party action | accept | Official Deno-org action pinned at `@v2`; matches existing checkout@v4/setup-node@v4 supply-chain posture |
| T-22-06-03 | Tampering | Deno file leaking into Node tsc masks/false-fails the gate | mitigate | Exclude scripts Deno files; verify 0 `TS2304 'Deno'` before pinning |
| T-22-06-SC | Tampering | package-manager installs | accept | Zero net-new npm packages; per 22-RESEARCH §Package Legitimacy Audit, slopcheck N/A |
</threat_model>

<verification>
- `npm run -s lint 2>&1 | grep -c "error TS2307"` == 0 and no `TS2304 'Deno'`.
- ci.yml has the blocking `deno-test` job; the tsc gate uses the measured baseline (not 290).
- `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions` exits 0 (depends on 22-01).
- Full suite green: `npm run test:run` + Deno corpus + `npm run lint` at/under the pinned baseline.
</verification>

<success_criteria>
Versioned imports resolve under tsc (TS2307 = 0), coverage spans e2e/scripts/playwright with Deno files excluded, the ci.yml tsc gate is pinned to the real measured green baseline (red-on-growth), and the EF corpus runs as a blocking CI job — the phase's regression net is fully wired.
</success_criteria>

<output>
Create `.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-06-SUMMARY.md` when done (record the measured/pinned tsc baseline number).
</output>
