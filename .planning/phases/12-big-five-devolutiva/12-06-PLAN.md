---
phase: 12-big-five-devolutiva
plan: 06
type: execute
wave: 5
depends_on: ["12-02", "12-03", "12-04", "12-05"]
files_modified:
  - database.types.ts
autonomous: false
requirements: [AVAL-04, AVAL-08]
must_haves:
  truths:
    - "[BLOCKING] Both migrations (bigfive_itens + 120-item seed + get_bigfive_itens reader + RLS; devolutivas_candidato + own-row + RH allowlist) are applied to live PROD via MCP apply_migration, with version rows reconciled so `supabase db push --linked` reports up to date - D-12-AVAL-04/08"
    - "The bigfive_devolutiva prompt is synced git->DB and flipped is_active=true on PROD"
    - "submit-bigfive-final + gerar-devolutiva-bigfive are deployed JWT-ON to PROD; database.types.ts is regenerated at the repo ROOT"
    - "The 12-SMOKES runbook passes live: item reader projects only item_id/texto/ordem; seed=120 with reverse_keyed=55; candidate cannot read scores_candidato; candidate reads own devolutiva only; RH allowlist reads both"
  artifacts:
    - path: "database.types.ts"
      provides: "regenerated types reflecting bigfive_itens + devolutivas_candidato"
      contains: "devolutivas_candidato"
  key_links:
    - from: "PROD prompt_versions"
      to: "bigfive_devolutiva is_active=true"
      via: "sync-prompts + activation flip"
      pattern: "bigfive_devolutiva"
---

<objective>
[BLOCKING] Apply the data + AI + EF layer to live PROD: 2 migrations via MCP apply_migration, sync + activate the bigfive_devolutiva prompt, deploy both EFs JWT-ON, regenerate database.types.ts, and run the 12-SMOKES SQL battery. This is the one non-autonomous plan - the orchestrator applies to production per the Phase-10/11 path.

Purpose: Nothing in the candidate flow works until the tables/reader/EFs/prompt are live. The 42601 PL/pgSQL push risk (Pitfall 8) is mitigated via MCP apply_migration. The smokes prove the answer-key projection + the candidate-deny + the own-row devolutiva + RH allowlist hold on the real database.

Output: live PROD schema + active prompt + 2 deployed EFs + regenerated types + passing smokes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-big-five-devolutiva/12-RESEARCH.md
@.planning/phases/12-big-five-devolutiva/12-SMOKES.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] PROD apply — migrations + prompt activation + EF deploy + db:types + smokes</name>
  <action>The orchestrator (Fernando-authorized) applies to live PROD per the how-to-verify steps below: 2 migrations via MCP apply_migration + version reconciliation, sync + is_active flip of the bigfive_devolutiva prompt, deploy both EFs JWT-ON, regenerate database.types.ts, run the 12-SMOKES battery. Non-autonomous — touches production.</action>
  <files>database.types.ts</files>
  <verify><human-check>All 5 SQL smokes PASS + both EFs return 401 to anon curl + prompt is_active row count = 1 for the call_type.</human-check></verify>
  <done>Both migrations live + reconciled; prompt is_active=true; both EFs deployed JWT-ON; db:types regenerated; 5/5 smokes PASS.</done>
  <what-built>
    Authored and verified across 12-02/03/04/05 (all green in CI, awaiting PROD apply):
    - `supabase/migrations/20260612000001_bigfive_itens.sql` (table + 120 seed + get_bigfive_itens reader + RLS)
    - `supabase/migrations/20260612000002_devolutivas_candidato.sql` (table + own-row + RH allowlist RLS)
    - `docs/conhecimento/prompts/templates/08-bigfive-devolutiva.md` (new prompt, not yet is_active)
    - EFs `submit-bigfive-final` + `gerar-devolutiva-bigfive` (authored, JWT-ON, not yet deployed)
  </what-built>
  <how-to-verify>
    The orchestrator (Fernando-authorized) performs the live PROD apply, in this order:
    1. Apply `20260612000001_bigfive_itens.sql` via MCP `apply_migration` (table+seed is plain DDL+INSERT - pushes clean; the `$$`-body reader RPC uses apply_migration to bypass 42601 per CLAUDE.md / Pitfall 8). Then `20260612000002_devolutivas_candidato.sql`. Reconcile version rows so `supabase db push --linked` reports "Remote database is up to date".
    2. Sync the prompt git->DB (run `scripts/sync-prompts.ts` or the established sync path) so the `bigfive_devolutiva` row lands, then flip `is_active=true` for it on PROD (deactivate any prior version of the same call_type).
    3. Deploy both EFs JWT-ON: `supabase functions deploy submit-bigfive-final` + `supabase functions deploy gerar-devolutiva-bigfive` (verify_jwt ON). Confirm anon curl returns 401 (verify_jwt behavioral evidence).
    4. Regenerate `npm run db:types` (database.types.ts at the repo ROOT, per M2 convention).
    5. Run the `.planning/phases/12-big-five-devolutiva/12-SMOKES.md` battery against live PROD via MCP execute_sql / `supabase db query --linked` (set_config request.jwt.claims for the candidate/RH cases): item reader projects ONLY item_id/texto/ordem; `bigfive_itens` seed count = 120 and reverse_keyed = 55; candidate JWT SELECT on `scores_candidato` denied/0-rows; candidate own-row SELECT on `devolutivas_candidato` returns their row and 0 for another uuid; RH 'administrador' reads both.
    All 5 smokes must PASS. Capture the evidence (counts, row presence/absence, EF version + 401) in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "applied" once both migrations are live + reconciled, the prompt is is_active=true, both EFs are deployed JWT-ON, db:types is regenerated, and all 5 SQL smokes PASS - or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local migration -> PROD DB | high-blast-radius write to live production schema |
| anon caller -> deployed EF | JWT-ON gate must reject unauthenticated calls |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-22 | Tampering | 42601 partial migration apply | mitigate | MCP apply_migration for the `$$`-body reader; no-wrapper authoring (D-22); version-row reconciliation (Pitfall 8) |
| T-12-23 | Info Disclosure | answer-key exposed post-apply | mitigate | live smoke asserts get_bigfive_itens projects only item_id/texto/ordem |
| T-12-24 | Elevation | EF deployed verify_jwt OFF | mitigate | deploy JWT-ON; anon-curl-401 smoke is the behavioral proof |
| T-12-25 | Info Disclosure | candidate reads scores_candidato | mitigate | live smoke: candidate JWT SELECT returns 0/denied |
| T-12-26 | Repudiation/compliance | wrong prompt active | mitigate | activation flip deactivates prior versions of the same call_type; verify is_active row count = 1 |
| T-12-SC | Tampering | npm/deno installs at deploy | accept | no new packages; EFs reuse pinned Phase-9/11 SDKs |
</threat_model>

<verification>
- Both migrations live on PROD; `supabase db push --linked` reports up to date.
- `bigfive_devolutiva` prompt row is_active=true (exactly 1 active for the call_type).
- Both EFs deployed JWT-ON (anon curl -> 401).
- `database.types.ts` (ROOT) regenerated; contains `bigfive_itens` + `devolutivas_candidato`.
- All 5 12-SMOKES PASS with captured evidence.
</verification>

<success_criteria>
- Live PROD schema + active prompt + 2 deployed EFs + regenerated types.
- 12-SMOKES 5/5 PASS (answer-key projection, seed 120/55, candidate-deny, own-row devolutiva, RH allowlist).
- AVAL-04 + AVAL-08 are live end-to-end, pending /gsd:verify-work.
</success_criteria>

<output>
Create `.planning/phases/12-big-five-devolutiva/12-06-SUMMARY.md` when done, capturing the live apply evidence (migration versions, prompt is_active, EF versions + 401, db:types diff, 5 smoke results).
</output>
