---
phase: 37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql
  - supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql
autonomous: true
requirements: [LEDGER-01, LEDGER-02, LEDGER-03, TIMELINE-01]

must_haves:
  truths:
    - "Existe uma migration ADITIVA que fecha as 3 lacunas sem editar nenhum arquivo já presente no ledger"
    - "`notificacoes_enviadas` ganha `destinatario_original` e `modo` — o par que torna um envio de UAT em modo teste distinguível de um envio real no ledger"
    - "`modo` só aceita 'producao' ou 'teste', com default fail-safe 'teste' espelhando `resolverModo()` de `_shared/email-config.ts`"
    - "`atualizado_em` passa a refletir o último UPDATE nas duas tabelas, via `public.tocar_atualizado_em()` em pt-BR — nunca reusando a `update_updated_at_column()` em inglês"
    - "O índice parcial `(status, proxima_tentativa_em) WHERE status IN ('pendente','falhou')` existe — a varredura de retry da P41 não precisará de uma segunda migration"
    - "Existe um smoke que prova COMPORTAMENTALMENTE: candidato impersonado lê 0 linhas, RH não-dono lê 0 linhas, dedupe_key duplicada levanta 23505, e `atualizado_em` avança num UPDATE"
    - "O smoke limpa suas próprias linhas: `notificacoes_enviadas` termina com 0 linhas, como começou"
  artifacts:
    - path: "supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql"
      provides: "Colunas de auditoria do modo teste + função/triggers tocar_atualizado_em + índice parcial de retry"
      contains: "tocar_atualizado_em"
      min_lines: 45
    - path: "supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql"
      provides: "Prova comportamental de LEDGER-02 (UNIQUE), LEDGER-03 (candidato-DENY + RH vaga-scoped), CHECKs de modo e de SLA, e do trigger de atualizado_em"
      contains: "request.jwt.claims"
      min_lines: 120
  key_links:
    - from: "supabase/functions/_shared/email-config.ts (resolverDestinatario/resolverModo)"
      to: "notificacoes_enviadas.destinatario_original / .modo"
      via: "as colunas armazenam exatamente o par que a função da P36 produz; a EF da P38 as grava"
      pattern: "destinatario_original"
    - from: "public.tocar_atualizado_em()"
      to: "notificacoes_enviadas.atualizado_em / config_sla_etapa.atualizado_em"
      via: "trigger BEFORE UPDATE FOR EACH ROW nas duas tabelas"
      pattern: "BEFORE UPDATE"
    - from: "idx (status, proxima_tentativa_em) WHERE status IN ('pendente','falhou')"
      to: "varredura pg_cron de retry (P41 / RECON-02)"
      via: "índice parcial casando exatamente o predicado da query de retry"
      pattern: "proxima_tentativa_em"
---

<objective>
Escrever a migration aditiva que fecha as 3 lacunas do schema vivo, e o smoke comportamental que prova as garantias de segurança e idempotência do ledger — tudo em arquivos, sem tocar PROD.

Purpose: o schema órfão é bom mas incompleto em três pontos que quebram a P38 e a P41. (1) Sem `destinatario_original`/`modo`, um envio de UAT redirecionado para `delivered+<evento>@resend.dev` fica indistinguível de um envio real no ledger — a auditoria mente. (2) `atualizado_em` tem `DEFAULT now()` mas 0 triggers: ele congela no INSERT e engana justamente onde seria mais útil (reivindicação de idempotência, retry, reconciliação). (3) O índice parcial que o `pg_cron` da P41 vai usar não existe; criá-lo agora evita uma segunda migration lá.
Output: 1 migration aditiva (NÃO aplicada aqui — a 37-04 aplica) + 1 smoke comportamental. Zero contato com PROD.

⚠ **Nenhuma linha dos arquivos `20260721000001_*` / `20260721000002_*` pode ser editada por este plano.** Reescrever um arquivo que já consta no ledger é mentira histórica: o que rodou em PROD não foi aquele conteúdo. Decisão travada do CONTEXT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-CONTEXT.md
@.planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-SCHEMA-VIVO.md
@supabase/functions/_shared/email-config.ts
@supabase/migrations/20260722000001_p36_vault_resend_reader.sql

<interfaces>
<!-- Contratos já fixados. Não re-derivar. -->

Contrato que as colunas novas armazenam (supabase/functions/_shared/email-config.ts, P36):
  type ModoNotificacao = 'producao' | 'teste'
  resolverModo(bruto?): ModoNotificacao       // fail-safe: qualquer coisa != 'producao' ⇒ 'teste'
  resolverDestinatario(emailReal, evento, modo): {
      para: string                 // → notificacoes_enviadas.destinatario_email (já existe)
      destinatario_original: string// → NOVA coluna (o e-mail REAL, sempre preservado)
      modo: ModoNotificacao        // → NOVA coluna
      redirecionado: boolean       // derivável de (para <> destinatario_original); NÃO virar coluna
  }
  Em modo teste, `para` = `delivered+<evento>@resend.dev`.

Função de trigger a criar — pt-BR, NOVA:
  public.tocar_atualizado_em() RETURNS trigger LANGUAGE plpgsql SET search_path = ''
  corpo: NEW.atualizado_em := now(); RETURN NEW;
  ⚠ NÃO reusar public.update_updated_at_column() — ela seta NEW.updated_at (inglês),
  coluna que NÃO existe nestas duas tabelas. Decisão travada do CONTEXT.
  ⚠ `now()` resolve mesmo com search_path='' (pg_catalog é sempre implícito); qualificar
  como pg_catalog.now() é aceitável e preferível para explicitude.

Aplicação em PROD (CLAUDE.md § Migrations): Supabase MCP `apply_migration` + reconcile do
ledger para a version `20260722000002`. NUNCA `supabase db push --linked` (SQLSTATE 42601
em corpo `$$` adjacente a outros statements). Sem wrapper BEGIN;/COMMIT;.
Quem aplica é o plano 37-04 (checkpoint do orquestrador) — NÃO este plano.

Idioma de smoke do repo (supabase/tests/seg33_agendamento_smokes.sql):
  fixture descartável com namespace de UUID fixo · descobrir usuários REAIS (vagas.created_by
  tem FK — UUID sintético viola) · impersonação via
  set_config('request.jwt.claims', <jsonb com sub + app_metadata.role>, false) + SET ROLE authenticated
  · RESET ROLE entre asserções · RAISE NOTICE 'PASS (x) …' · gate = CONTAR os PASS.
  Conta de candidato de teste em PROD: candidato.funil@teste.com
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a migration aditiva 20260722000002 (3 lacunas)</name>
  <files>supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql</files>
  <read_first>
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-SCHEMA-VIVO.md — seções A (as 16 colunas atuais, para não colidir), E (0 triggers hoje), I (ausência de `tocar_atualizado_em`), J (0 linhas), C (nomes de índice já usados, para não colidir)
    - supabase/functions/_shared/email-config.ts (linhas 38-108) — o contrato exato que as duas colunas armazenam
    - supabase/migrations/20260722000001_p36_vault_resend_reader.sql — o precedente imediato de cabeçalho, `SET search_path = ''`, `COMMENT ON FUNCTION` e ausência de wrapper transacional
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-CONTEXT.md § "As Três Lacunas" — os valores travados (tipos, default, CHECK, predicado do índice)
    - CLAUDE.md § "Migrations + db push — workaround conhecido (PL/pgSQL)"
  </read_first>
  <action>
    Criar `supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql`.

    Cabeçalho em comentário no estilo do `20260722000001_p36_vault_resend_reader.sql`, declarando: M7 / Phase 37 / Plano 37-03, requisitos LEDGER-01 e LEDGER-02, e — obrigatoriamente — o parágrafo explicando por que esta migration é ADITIVA e não uma edição dos arquivos `20260721000001`/`20260721000002` (aqueles já constam no ledger; reescrevê-los seria mentira histórica sobre o que rodou em PROD). Incluir a nota padrão de ausência de wrapper `BEGIN;`/`COMMIT;` (D-22).

    Lacuna 1 — auditoria do modo teste. `ALTER TABLE public.notificacoes_enviadas` adicionando `destinatario_original text NOT NULL` (sem default: a tabela tem 0 linhas conforme a seção J do dump, então o `NOT NULL` entra sem backfill; documentar essa premissa inline) e `modo text NOT NULL DEFAULT 'teste'`. Em seguida `ADD CONSTRAINT ck_notif_modo CHECK (modo IN ('producao','teste'))`. Dois `COMMENT ON COLUMN`: o de `destinatario_original` explicando que guarda o e-mail REAL do candidato mesmo quando `destinatario_email` foi reescrito para a sandbox `delivered+<evento>@resend.dev`, e citando `resolverDestinatario()` de `supabase/functions/_shared/email-config.ts` como produtor; o de `modo` explicando o default fail-safe (espelha `resolverModo()`: qualquer valor que não seja exatamente `producao` cai em `teste`) e que ele existe para que um envio de UAT nunca seja confundido com um envio real na auditoria. NÃO criar coluna `redirecionado` — é derivável de `destinatario_email <> destinatario_original`.

    Lacuna 2 — `atualizado_em` congelado. `CREATE OR REPLACE FUNCTION public.tocar_atualizado_em()` retornando `trigger`, em `plpgsql`, com `SET search_path = ''`, cujo corpo atribui o timestamp corrente a `NEW.atualizado_em` e retorna `NEW`. Sem `SECURITY DEFINER` (stamping de coluna não precisa de privilégio elevado; DEFINER aqui só ampliaria superfície). Um `COMMENT ON FUNCTION` explicando que ela é a variante pt-BR deliberada e que `public.update_updated_at_column()` NÃO serve porque seta `NEW.updated_at`, coluna inexistente nestas tabelas. Depois, dois triggers `BEFORE UPDATE ... FOR EACH ROW EXECUTE FUNCTION public.tocar_atualizado_em()`: `trg_notificacoes_atualizado_em` em `public.notificacoes_enviadas` e `trg_config_sla_atualizado_em` em `public.config_sla_etapa`. Usar `CREATE TRIGGER` puro, sem `DROP TRIGGER IF EXISTS` — a seção E do dump prova que hoje há 0 triggers não-internos nas duas tabelas, e falhar alto é preferível a mascarar um trigger inesperado.

    Lacuna 3 — índice parcial de retry. `CREATE INDEX idx_notif_retry ON public.notificacoes_enviadas (status, proxima_tentativa_em) WHERE status IN ('pendente'::public.status_notificacao, 'falhou'::public.status_notificacao);` — com os casts explícitos para o enum, de modo que o predicado do índice case com o predicado da query do `pg_cron` da P41 e o planner consiga provar a implicação. Sem `IF NOT EXISTS` (a seção C do dump prova que o nome está livre). Um `COMMENT ON INDEX` citando RECON-02 / P41 como consumidor.

    Antes de escrever, conferir na seção C do dump que nenhum índice existente já se chama `idx_notif_retry` e na seção B que nenhuma constraint já se chama `ck_notif_modo`. Se houver colisão, PARAR e reportar em vez de renomear silenciosamente.

    PROIBIÇÕES: não editar nenhum arquivo `20260721000001_*` ou `20260721000002_*`; nenhum `BEGIN;`/`COMMIT;`; nenhum `IF NOT EXISTS`; nenhum re-seed de `config_sla_etapa`; nenhuma alteração da policy `rh_le_notificacoes`; NENHUMA policy nova de candidato (o candidato-DENY permanece implícito pelo default-deny da RLS — decisão travada; adicionar uma policy permissiva mal escrita ABRE acesso, a ausência de policy nunca abre).
  </action>
  <verify>
    <automated>F=supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql; test -f "$F" && S=$(grep -v '^\s*--' "$F"); echo "$S" | grep -q "destinatario_original text NOT NULL" && echo "$S" | grep -q "modo text NOT NULL DEFAULT 'teste'" && echo "$S" | grep -q "ck_notif_modo" && echo "$S" | grep -q "CREATE OR REPLACE FUNCTION public.tocar_atualizado_em" && echo "$S" | grep -q "SET search_path = ''" && [ "$(echo "$S" | grep -c 'BEFORE UPDATE')" -eq 2 ] && echo "$S" | grep -q "idx_notif_retry" && echo "$S" | grep -q "proxima_tentativa_em" && [ "$(echo "$S" | grep -ci 'IF NOT EXISTS')" -eq 0 ] && [ "$(echo "$S" | grep -ciE '^\s*(BEGIN|COMMIT)\s*;')" -eq 0 ] && [ "$(echo "$S" | grep -ci 'CREATE POLICY\|update_updated_at_column\|redirecionado')" -eq 0 ] && git diff --name-only HEAD -- supabase/migrations/20260721000001_notificacoes_enviadas.sql supabase/migrations/20260721000002_config_sla_etapa.sql | grep -c . | grep -q '^0$' && echo "migration aditiva OK; reconstruidos intocados"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql` existe.
    - Contém `ADD COLUMN destinatario_original text NOT NULL` sem `DEFAULT`, e `ADD COLUMN modo text NOT NULL DEFAULT 'teste'`.
    - Contém `ADD CONSTRAINT ck_notif_modo CHECK (modo IN ('producao','teste'))`.
    - Contém `COMMENT ON COLUMN` para as duas colunas novas, e o de `destinatario_original` cita `_shared/email-config.ts`.
    - Contém `CREATE OR REPLACE FUNCTION public.tocar_atualizado_em()` com `RETURNS trigger`, `LANGUAGE plpgsql` e `SET search_path = ''`, e NÃO contém `SECURITY DEFINER`.
    - `grep -v '^\s*--' <arquivo> | grep -c 'BEFORE UPDATE'` retorna exatamente 2, com os triggers nomeados `trg_notificacoes_atualizado_em` e `trg_config_sla_atualizado_em`.
    - Contém `CREATE INDEX idx_notif_retry ON public.notificacoes_enviadas (status, proxima_tentativa_em) WHERE status IN (...)` com casts explícitos para `public.status_notificacao`.
    - `grep -v '^\s*--' <arquivo> | grep -ci 'IF NOT EXISTS'` retorna 0; `... grep -ciE '^\s*(BEGIN|COMMIT)\s*;'` retorna 0.
    - `grep -v '^\s*--' <arquivo> | grep -ci 'CREATE POLICY'` retorna 0 — nenhuma policy nova, o candidato-DENY continua implícito.
    - `grep -v '^\s*--' <arquivo> | grep -ci 'update_updated_at_column'` retorna 0 (fora do comentário explicativo) e `... grep -ci 'redirecionado'` retorna 0.
    - `git status --porcelain supabase/migrations/20260721000001_notificacoes_enviadas.sql supabase/migrations/20260721000002_config_sla_etapa.sql` não mostra modificação (`M`) — os reconstruídos permanecem intocados.
    - O cabeçalho contém o parágrafo justificando "aditiva, não edição" e a nota de ausência de wrapper transacional.
  </acceptance_criteria>
  <done>As 3 lacunas estão declaradas numa migration aditiva pronta para apply, com nomes livres de colisão, sem policy nova e sem tocar nos arquivos já presentes no ledger.</done>
</task>

<task type="auto">
  <name>Task 2: Escrever o smoke comportamental de RLS, idempotência, CHECKs e trigger</name>
  <files>supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql</files>
  <read_first>
    - supabase/tests/seg33_agendamento_smokes.sql (arquivo inteiro) — o idioma canônico: cabeçalho enumerando cada asserção, namespace de UUID fixo, descoberta de usuários REAIS (`vagas.created_by` tem FK), impersonação por `set_config('request.jwt.claims', ...)` + `SET ROLE authenticated`, `RESET ROLE` entre asserções, `RAISE NOTICE 'PASS (x) …'`, e o gate de CONTAR os PASS (um run todo-SKIP mascara falha de fixture)
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-SCHEMA-VIVO.md — seção D: o literal de role da policy `rh_le_notificacoes` (usar EXATAMENTE esse literal, não `admin` por suposição); seção J: as 8 linhas do seed
    - supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql (escrito na Task 1) — os nomes reais de constraint/índice/trigger a asserir
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-CONTEXT.md § "Specific Ideas" — os requisitos explícitos de prova (impersonação real, violação empírica do UNIQUE, cleanup)
  </read_first>
  <action>
    Criar `supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql` — a prova COMPORTAMENTAL do LEDGER-02 e do LEDGER-03. Este arquivo é rodado em PROD pelo orquestrador (plano 37-04), depois do apply da aditiva.

    Cabeçalho `-- ===` enumerando cada asserção, declarando o gate verde (contar TODOS os `PASS` esperados), declarando o namespace de UUID fixo da fixture, e declarando explicitamente que o smoke ALTERA `config_sla_etapa.atualizado_em` de uma etapa (efeito correto e esperado do trigger; as colunas de negócio `prazo_valor`/`prazo_unidade`/`rotulo_candidato` são asseridas inalteradas antes e depois).

    Fixture, no idioma do seg33: namespace `37010037-*`. Limpar antes e depois. Descobrir entidades REAIS (não UUID sintético — `vagas.created_by` tem FK): o candidato de teste `candidato.funil@teste.com` (resolver `candidatos.id` + `candidatos.user_id` por e-mail; se não encontrar, cair para o primeiro candidato com `user_id` não nulo e REGISTRAR o fallback no NOTICE em vez de falhar em silêncio); dois `usuarios_rh` reais distintos com 0 vagas (recrutador A e recrutador B); um terceiro `usuarios_rh` real para impersonar administrador. Criar vagaA (`created_by` = A, vazia), vagaB (`created_by` = B) e uma candidatura na vagaB pertencente ao candidato de teste.

    Asserções obrigatórias, cada uma com `RAISE NOTICE 'PASS (x) …'` e `RAISE EXCEPTION` descritiva na falha:

    (a) LEDGER-02 — idempotência EMPÍRICA. Como `service_role` (bypassa RLS), inserir uma linha em `notificacoes_enviadas` com uma `dedupe_key` do namespace da fixture. Inserir uma SEGUNDA linha com a MESMA `dedupe_key` dentro de um bloco `EXCEPTION WHEN unique_violation` — o PASS exige que a exceção OCORRA (SQLSTATE 23505) e cite `uq_notif_dedupe`. Se a segunda inserção passar, `RAISE EXCEPTION` — a guarda de idempotência está morta. NÃO aceitar "não deu erro" como prova de nada.
    (b) LEDGER-02 — claim pattern. Provar que `INSERT ... ON CONFLICT (dedupe_key) DO NOTHING RETURNING id` devolve 0 linhas na segunda tentativa (é exatamente o claim que a EF da P38 fará) e 1 linha numa `dedupe_key` inédita.
    (c) Colunas novas — `destinatario_original` é `NOT NULL`: um INSERT omitindo-a levanta `not_null_violation` (23502). E `modo` default: um INSERT que a omite grava `teste`.
    (d) `ck_notif_modo` — um INSERT com `modo = 'producaozinha'` levanta `check_violation` (23514) citando `ck_notif_modo`.
    (e) Trigger em `notificacoes_enviadas` — capturar `atualizado_em` da linha da fixture, `pg_sleep(0.05)`, fazer um UPDATE qualquer (ex.: `tentativas = tentativas + 1`), reler e exigir `atualizado_em` ESTRITAMENTE maior que o valor anterior. Exigir também que `criado_em` NÃO tenha mudado.
    (f) Trigger em `config_sla_etapa` — escolher a etapa `triagem`, guardar `(prazo_valor, prazo_unidade, rotulo_candidato, atualizado_em)`, fazer um UPDATE no-op (`SET rotulo_candidato = rotulo_candidato`), reler e exigir `atualizado_em` estritamente maior E as três colunas de negócio idênticas às guardadas. Registrar no NOTICE que `atualizado_em` dessa linha mudou de propósito.
    (g) LEDGER-03 candidato-DENY — impersonar o candidato REAL: `SET ROLE authenticated` + `set_config('request.jwt.claims', jsonb com `sub` = `user_id` do candidato e `app_metadata.role` = `'candidato'`, false)`. Com a linha da fixture EXISTINDO (garantida por (a)), exigir `count(*) from public.notificacoes_enviadas` = 0. Exigir também que um INSERT como candidato levante `42501` (insufficient_privilege) ou afete 0 linhas. `RESET ROLE` ao final. Esta asserção é a mais importante do arquivo: ela prova a ausência de policy por COMPORTAMENTO, não por afirmação.
    (h) LEDGER-03 RH não-dono — impersonar o recrutador A (que só possui a vagaA, vazia) com o literal de role `rh`: exigir `count(*)` = 0 sobre a linha da fixture, que pertence à candidatura da vagaB.
    (i) LEDGER-03 RH dono — impersonar o recrutador B (dono da vagaB): exigir `count(*)` >= 1 sobre a mesma linha. Sem esta asserção, (h) passaria trivialmente por um bug que nega tudo.
    (j) LEDGER-03 admin — impersonar com o literal de role ADMIN registrado na seção D do dump (usar exatamente esse literal): exigir `count(*)` >= 1.
    (k) TIMELINE-01 seed e CHECKs — exigir `count(*) from public.config_sla_etapa` = 8; exigir que o conjunto de `etapa` cubra os 8 labels de `etapa_processo`; provar `ck_sla_prazo_consistente` empiricamente com um UPDATE que deixe `prazo_valor` não nulo e `prazo_unidade` nula (deve levantar 23514) dentro de bloco `EXCEPTION`; provar o CHECK de `prazo_unidade` com um valor inválido; provar o CHECK de `prazo_valor` com `0`. Todos dentro de blocos `EXCEPTION` que exigem a violação, e nenhum deles pode deixar a linha alterada.
    (l) TIMELINE-01 public-read — impersonar `SET ROLE anon` e exigir `count(*) from public.config_sla_etapa` = 8 (a policy `sla_public_read` é o que a P40 vai usar). `RESET ROLE` ao final.
    (m) Índice parcial — asserção ESTRUTURAL (a tabela é pequena demais para o planner escolhê-lo): exigir que `pg_indexes.indexdef` de `idx_notif_retry` contenha tanto `proxima_tentativa_em` quanto o predicado com `pendente` e `falhou`. Declarar no comentário que esta é a única asserção estrutural do arquivo e por quê.
    (n) CLEANUP — apagar TODAS as linhas do namespace da fixture (notificações, candidatura, vagas) e exigir `count(*) from public.notificacoes_enviadas` = 0 ao final. Emitir `RAISE NOTICE 'PASS (n) cleanup: notificacoes_enviadas voltou a 0 linhas'`. Se a contagem final não for 0, `RAISE EXCEPTION` — a tabela precisa terminar como começou.
    (o) RESUMO — `RAISE NOTICE 'RESUMO: N asserções PASS de N esperadas'`.

    Regras transversais: `RESET ROLE` antes de cada mudança de identidade e ao final do arquivo (um `SET ROLE` vazado envenena as asserções seguintes). Nenhum `motivo_rejeicao`, score, percentil ou trait em nenhum literal. Nenhum e-mail real de candidato impresso em `RAISE NOTICE` — usar contagens e predicados booleanos.
  </action>
  <verify>
    <automated>F=supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql; test -f "$F" && S=$(grep -v '^\s*--' "$F"); echo "$S" | grep -q "request.jwt.claims" && echo "$S" | grep -q "app_metadata" && echo "$S" | grep -q "candidato" && echo "$S" | grep -q "unique_violation" && echo "$S" | grep -q "uq_notif_dedupe" && echo "$S" | grep -q "ON CONFLICT (dedupe_key) DO NOTHING" && echo "$S" | grep -q "ck_notif_modo" && echo "$S" | grep -q "ck_sla_prazo_consistente" && echo "$S" | grep -q "idx_notif_retry" && echo "$S" | grep -q "SET ROLE anon" && [ "$(echo "$S" | grep -c 'RESET ROLE')" -ge 4 ] && [ "$(echo "$S" | grep -c 'PASS')" -ge 14 ] && [ "$(echo "$S" | grep -ci 'motivo_rejeicao\|percentil\|score')" -eq 0 ] && echo "smoke comportamental OK"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql` existe com cabeçalho enumerando as asserções (a)..(o) e declarando o gate de contagem de PASS.
    - A asserção (a) insere a MESMA `dedupe_key` duas vezes dentro de um bloco `EXCEPTION WHEN unique_violation` e o caminho de sucesso só é atingido quando a exceção OCORRE; o caminho sem exceção levanta `RAISE EXCEPTION`.
    - A asserção (b) usa `ON CONFLICT (dedupe_key) DO NOTHING RETURNING` e assere 0 linhas na segunda tentativa.
    - As asserções (c) e (d) provam `NOT NULL` de `destinatario_original`, o default `teste` de `modo` e a violação de `ck_notif_modo`, cada uma exigindo o SQLSTATE correspondente.
    - As asserções (e) e (f) comparam `atualizado_em` antes/depois de um UPDATE e exigem estritamente maior; (f) assere adicionalmente que `prazo_valor`, `prazo_unidade` e `rotulo_candidato` permanecem idênticos.
    - A asserção (g) contém `set_config('request.jwt.claims', ...)` com `app_metadata` e o valor `candidato`, além de `SET ROLE authenticated`, e exige `count(*) = 0` com a linha da fixture existindo — não é uma consulta a `pg_policies`.
    - As asserções (h), (i) e (j) cobrem RH não-dono (0), RH dono (>=1) e admin (>=1), usando os literais de role registrados na seção D do `37-SCHEMA-VIVO.md`.
    - A asserção (k) prova os 3 CHECKs de `config_sla_etapa` por violação exigida, e (l) prova `sla_public_read` com `SET ROLE anon` retornando 8.
    - A asserção (n) termina com `count(*) from public.notificacoes_enviadas` = 0 e falha alto se não for.
    - `grep -c 'RESET ROLE'` (fora de comentários) é >= 4 e a última linha executável do arquivo é um `RESET ROLE`.
    - `grep -c 'PASS'` (fora de comentários) é >= 14.
    - `grep -ci 'motivo_rejeicao\|percentil\|score'` retorna 0 e nenhum `RAISE NOTICE` imprime endereço de e-mail.
  </acceptance_criteria>
  <done>Existe um smoke que prova, por comportamento em PROD, que o candidato não lê o ledger, que o RH lê só a própria vaga, que a `dedupe_key` duplicada é rejeitada, que os CHECKs e o trigger funcionam — e que deixa a tabela com 0 linhas ao final.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Candidato (JWT `authenticated`, role `candidato`) → `notificacoes_enviadas` | Fronteira crítica: a tabela contém e-mail, evento e status de funil de TODOS os candidatos |
| Recrutador (JWT `rh`) → linhas de vagas alheias | Fronteira horizontal entre recrutadores; a policy join-through é a única barreira |
| Trigger/EF (`service_role`) → `notificacoes_enviadas` | Único escritor legítimo; bypassa RLS por construção |
| Modo teste vs. produção → destinatário real | Fronteira de segurança de PRODUTO: um envio real disfarçado de teste (ou vice-versa) é incidente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | Information Disclosure | Candidato lendo o ledger de notificações de outro candidato (PII de funil: e-mail, evento, status) | mitigate | Default-deny da RLS (nenhuma policy casa para role `candidato`) — e a migration é PROIBIDA de adicionar qualquer `CREATE POLICY`. Provado pela asserção (g) com JWT de candidato REAL, não por inspeção de `pg_policies` |
| T-37-03-02 | Information Disclosure | RH lendo notificações de candidatura de vaga que não é sua | mitigate | Policy `rh_le_notificacoes` join-through vaga-scoped já viva e intocada; provada pelo par de asserções (h) nega / (i) permite — o par impede que um bug "nega tudo" passe como sucesso |
| T-37-03-03 | Tampering | Double-send por falha de idempotência: retry do mesmo evento gera segundo e-mail ao candidato | mitigate | `uq_notif_dedupe UNIQUE (dedupe_key)` provado EMPIRICAMENTE pela asserção (a) (23505 exigido) e o claim pattern da P38 provado por (b) |
| T-37-03-04 | Repudiation | Envio de UAT em modo teste indistinguível de envio real no ledger — auditoria mente sobre quem deveria ter recebido | mitigate | Colunas `destinatario_original` (NOT NULL) e `modo` (CHECK, default fail-safe `teste`); asserções (c) e (d) |
| T-37-03-05 | Repudiation | `atualizado_em` congelado no INSERT faz a trilha de retry/reconciliação da P41 mentir sobre quando a linha mudou | mitigate | `public.tocar_atualizado_em()` + trigger `BEFORE UPDATE` nas duas tabelas; asserções (e) e (f) exigem avanço estrito do timestamp |
| T-37-03-06 | Elevation of Privilege | `SECURITY DEFINER` desnecessário na função de trigger ampliaria superfície sem ganho | mitigate | Função declarada sem `SECURITY DEFINER`; acceptance criteria da Task 1 exige `grep -ci 'SECURITY DEFINER'` = 0 |
| T-37-03-07 | Elevation of Privilege | `search_path` mutável em função `plpgsql` permite sequestro de resolução de nome | mitigate | `SET search_path = ''` obrigatório, com referências qualificadas — idioma já padrão no repo |
| T-37-03-08 | Tampering | O próprio smoke deixando linhas de teste no ledger de PROD, poluindo a auditoria e a P38 | mitigate | Asserção (n) exige `count(*) = 0` ao final e falha alto se não for; fixture em namespace de UUID fixo, limpa antes e depois |
| T-37-03-09 | Tampering | O smoke alterando o seed de `config_sla_etapa` (texto voltado ao candidato) | mitigate | (f) usa UPDATE no-op e assere as 3 colunas de negócio idênticas antes/depois; (k) prova os CHECKs só por violações que abortam o UPDATE |
| T-37-03-10 | Denial of Service | Varredura de retry da P41 fazendo seq scan em ledger crescente | mitigate | Índice parcial `idx_notif_retry` com predicado casando a query de retry, com casts explícitos para o enum; asserção estrutural (m) |
| T-37-03-11 | Tampering | Adicionar uma policy de candidato "para auto-documentar o DENY" — uma policy PERMISSIVE mal escrita ABRE acesso, enquanto a ausência nunca abre | mitigate | Proibição explícita na Task 1 + `grep -ci 'CREATE POLICY'` = 0 no acceptance criteria |
</threat_model>

<verification>
- A migration aditiva existe, tem as 2 colunas, o CHECK, a função, os 2 triggers e o índice parcial
- A migration aditiva não contém `CREATE POLICY`, `IF NOT EXISTS`, `BEGIN;`, `COMMIT;` nem `SECURITY DEFINER`
- Os dois arquivos reconstruídos da 37-02 permanecem sem modificação no `git status`
- O smoke comportamental existe com >= 14 PASS, impersonação real por `request.jwt.claims`, violação empírica do UNIQUE e cleanup para 0 linhas
- `npm run lint` mantém 97 erros `tsc` (baseline; teto CI 104) e nenhum erro cita arquivos deste plano
- `npm run build` e `npm run test:run` continuam verdes
</verification>

<success_criteria>
1. A EF da P38 tem onde gravar `destinatario_original` e `modo` — a auditoria distingue um envio de UAT de um envio real.
2. `atualizado_em` passa a significar o que o nome diz, nas duas tabelas.
3. O índice que o `pg_cron` da P41 vai usar existe, sem exigir uma segunda migration lá.
4. Existe um smoke que prova por comportamento — não por afirmação — que o candidato não lê o ledger, que o RH lê só a própria vaga e que uma `dedupe_key` repetida é rejeitada.
</success_criteria>

<output>
Create `.planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-03-SUMMARY.md` when done
</output>
