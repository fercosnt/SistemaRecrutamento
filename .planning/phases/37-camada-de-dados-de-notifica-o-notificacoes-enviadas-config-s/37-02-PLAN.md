---
phase: 37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - supabase/migrations/20260721000001_notificacoes_enviadas.sql
  - supabase/migrations/20260721000002_config_sla_etapa.sql
  - supabase/tests/p37_fidelidade_schema_smoke.sql
autonomous: true
requirements: [LEDGER-01, LEDGER-02, LEDGER-03, TIMELINE-01]

must_haves:
  truths:
    - "O repositório contém os arquivos de migration correspondentes às versions 20260721000001 e 20260721000002 que já estão no ledger de PROD — um rebuild de ambiente reproduz as duas tabelas em vez de perdê-las silenciosamente"
    - "Os nomes reais dos objetos (uq_notif_dedupe, ck_sla_prazo_consistente, rh_le_notificacoes, sla_public_read) estão preservados byte-a-byte — um rebuild não inventa nomes divergentes"
    - "Nenhum dos dois arquivos usa CREATE TABLE IF NOT EXISTS: um rebuild contra um banco que já tem a tabela falha ALTO em vez de mascarar divergência"
    - "Existe um smoke que compara o catálogo Postgres vivo campo-a-campo contra os literais dos arquivos reconstruídos e falha em qualquer divergência"
    - "O smoke de fidelidade é re-executável depois da migration aditiva da 37-03 (toggle de baseline), em vez de virar um arquivo permanentemente vermelho no repo"
  artifacts:
    - path: "supabase/migrations/20260721000001_notificacoes_enviadas.sql"
      provides: "DDL fiel de public.notificacoes_enviadas — tabela, 5 constraints, 5 índices, RLS + policy rh_le_notificacoes, enum status_notificacao, COMMENTs"
      contains: "uq_notif_dedupe"
      min_lines: 40
    - path: "supabase/migrations/20260721000002_config_sla_etapa.sql"
      provides: "DDL fiel de public.config_sla_etapa — tabela, 4 constraints, RLS + policy sla_public_read, seed das 8 etapas"
      contains: "ck_sla_prazo_consistente"
      min_lines: 30
    - path: "supabase/tests/p37_fidelidade_schema_smoke.sql"
      provides: "Gate de fidelidade: assere o catálogo vivo campo-a-campo contra os literais dos dois arquivos, com toggle v_pos_aditiva"
      contains: "v_pos_aditiva"
      min_lines: 80
  key_links:
    - from: ".planning/phases/37-.../37-SCHEMA-VIVO.md"
      to: "supabase/migrations/20260721000001_notificacoes_enviadas.sql"
      via: "transcrição verbatim das definições de constraint/policy/índice do dump"
      pattern: "rh_le_notificacoes"
    - from: "supabase/tests/p37_fidelidade_schema_smoke.sql"
      to: "pg_constraint / pg_policies / pg_indexes / information_schema.columns"
      via: "asserções de igualdade contra os literais transcritos"
      pattern: "pg_get_constraintdef"
---

<objective>
Devolver ao repositório a autoria das duas tabelas que hoje só existem em PROD, e construir o gate que prova que a devolução foi fiel.

Purpose: hoje um `supabase db reset` ou um rebuild de ambiente perde `notificacoes_enviadas` e `config_sla_etapa` silenciosamente — o repo não sabe que elas existem. Reconstruir os arquivos com as versions que JÁ constam no ledger restaura a correspondência 1:1 arquivo↔linha do ledger sem mentir sobre o histórico. E como foi exatamente "escrever e revisar por leitura" que deixou o drift nascer, a fidelidade é provada por um smoke que consulta o catálogo, não por revisão.
Output: 2 arquivos de migration reconstruídos (NÃO aplicados) + 1 smoke de fidelidade. Zero contato com PROD neste plano — todo o trabalho é de arquivo.

⚠ **Estes dois arquivos NÃO serão aplicados, nunca.** As versions `20260721000001` e `20260721000002` já constam em `supabase_migrations.schema_migrations` (confirmado pelo gate K da 37-01). O ledger é a proteção contra re-execução. Qualquer tentativa de `apply_migration` ou `db push` com eles é erro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-CONTEXT.md
@.planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-SCHEMA-VIVO.md
@supabase/migrations/20260716000001_agendamentos_entrevista.sql

<interfaces>
<!-- Idioma SQL do repo, já verificado. Não re-derivar. -->

Cabeçalho de migration (precedente: 20260716000001_agendamentos_entrevista.sql linhas 1-19):
  bloco `-- ===` com fase/plano, requisito, e a nota "NO explicit BEGIN;/COMMIT; wrapper
  (D-22, CLAUDE.md §Migrations — the CLI driver wraps each migration itself)".

Policy join-through vaga-scoped (precedente LITERAL: 20260716000001, linhas 103-121):
  CREATE POLICY <nome> ON public.<tabela> FOR <cmd> TO authenticated
    USING ( (select auth.jwt() #>> '{app_metadata,role}') = '<role_admin>'
            OR ((select auth.jwt() #>> '{app_metadata,role}') = 'rh'
                AND candidatura_id IN (SELECT c.id FROM public.candidaturas c
                                         JOIN public.vagas v ON v.id = c.vaga_id
                                        WHERE v.created_by = (select auth.uid()))) );
  ⚠ o literal <role_admin> NÃO é escolha: use exatamente o que a seção D do
  37-SCHEMA-VIVO.md registrou no `qual` de `rh_le_notificacoes`. O repo usa
  'administrador' em 28 lugares; o todo do drift parafraseou como 'admin'.

Smoke SQL do repo (precedente: supabase/tests/seg33_agendamento_smokes.sql):
  cabeçalho `-- ===` documentando cada asserção · `RESET ROLE;` · um `DO $$ ... $$;`
  com DECLARE + RAISE NOTICE 'PASS (x) …' por asserção · gate = CONTAR os NOTICEs PASS
  (um run todo-SKIP mascara falha de fixture) · rodado via MCP `execute_sql`.

Ledger: 20260721000001 e 20260721000002 JÁ aplicadas. Estes arquivos são declarativos.
A migration que EFETIVAMENTE roda em PROD nesta fase é a aditiva da 37-03.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconstruir 20260721000001_notificacoes_enviadas.sql a partir do dump</name>
  <files>supabase/migrations/20260721000001_notificacoes_enviadas.sql</files>
  <read_first>
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-SCHEMA-VIVO.md — seções A (colunas), B (constraints), C (índices), D (policies), E (RLS), F (enums), G (COMMENTs), H (GRANTs). É a ÚNICA fonte da verdade do DDL; não usar o todo do drift para nada além de contexto narrativo
    - .planning/todos/pending/37-drift-prod-tabelas-notificacao.md — o retrato narrativo (por que a tabela existe, o que cada coluna significa); NÃO copiar DDL daqui, é paráfrase
    - supabase/migrations/20260716000001_agendamentos_entrevista.sql (linhas 1-58 e 100-127) — cabeçalho, formatação de CREATE TABLE, COMMENT ON TABLE multi-linha, e o idioma exato da policy join-through vaga-scoped
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-CONTEXT.md § "Estratégia de Reconciliação do Drift" — proibições travadas
  </read_first>
  <action>
    Criar `supabase/migrations/20260721000001_notificacoes_enviadas.sql` reconstruindo o DDL vivo a partir das seções A..H do `37-SCHEMA-VIVO.md`.

    Cabeçalho em comentário, no estilo do `20260716000001_agendamentos_entrevista.sql`, declarando: milestone/fase/plano (M7 / Phase 37 / Plano 37-02), os requisitos (LEDGER-01 audit trail, LEDGER-02 idempotência via `uq_notif_dedupe`, LEDGER-03 RLS RH vaga-scoped + candidato-DENY), e — obrigatoriamente — um parágrafo de PROVENIÊNCIA explicando que este arquivo é uma RECONSTRUÇÃO por engenharia reversa do schema vivo em PROD (version `20260721000001` já presente em `supabase_migrations.schema_migrations`, origem desconhecida, drift descoberto em 2026-07-22), que ele NÃO deve ser aplicado, e que as lacunas posteriores estão na migration aditiva `20260722000002` e NÃO neste arquivo. Incluir a nota padrão de ausência de wrapper `BEGIN;`/`COMMIT;` (D-22, CLAUDE.md).

    Corpo, na ordem: (1) `CREATE TYPE public.status_notificacao AS ENUM (...)` com os labels da seção F na ordem de `enumsortorder` — e uma nota inline de que `public.etapa_processo` é pré-existente e apenas referenciada, nunca re-declarada. (2) `CREATE TABLE public.notificacoes_enviadas` com as 16 colunas da seção A na ordem de `ordinal_position`, cada uma com o tipo de `udt_name`, a nulidade de `is_nullable` e o default de `column_default`; as FKs para `public.candidaturas(id)` e `public.candidatos(id)` com `ON DELETE CASCADE` e o CHECK de `evento` declarados conforme as definições da seção B — preservando os nomes de constraint reais, incluindo `uq_notif_dedupe`. (3) Os índices da seção C que NÃO forem os implícitos de PK/UNIQUE, transcritos a partir do `indexdef`. (4) `ALTER TABLE ... ENABLE ROW LEVEL SECURITY;`. (5) `CREATE POLICY rh_le_notificacoes` transcrita do `qual` da seção D, usando o literal de role exato registrado ali. (6) `COMMENT ON TABLE` e os `COMMENT ON COLUMN` da seção G; se a seção G mostrar que não há comentários em PROD, escrever comentários novos e marcá-los inline como "adicionado na reconstrução (não existia em PROD)" — comentário não é schema estrutural, mas a divergência precisa ficar declarada. (7) Os GRANTs da seção H, se houver algum além dos defaults do Supabase.

    PROIBIÇÕES, todas travadas no CONTEXT: nenhum `IF NOT EXISTS` em `CREATE TABLE`; nenhum `CREATE OR REPLACE`/`DROP ... IF EXISTS` que mascare divergência; nenhum nome de objeto inventado; nenhuma coluna, constraint, índice ou policy que não esteja no dump; nenhuma das 3 lacunas (colunas `destinatario_original`/`modo`, função/trigger `tocar_atualizado_em`, índice parcial de retry) — essas pertencem exclusivamente à 37-03. Nenhum `BEGIN;`/`COMMIT;`.

    Se alguma seção do dump estiver ausente ou ambígua, PARAR e reportar em vez de adivinhar.
  </action>
  <verify>
    <automated>F=supabase/migrations/20260721000001_notificacoes_enviadas.sql; test -f "$F" && S=$(grep -v '^\s*--' "$F"); echo "$S" | grep -q "CREATE TABLE public.notificacoes_enviadas" && echo "$S" | grep -q "uq_notif_dedupe" && echo "$S" | grep -q "rh_le_notificacoes" && echo "$S" | grep -q "ENABLE ROW LEVEL SECURITY" && echo "$S" | grep -q "status_notificacao" && [ "$(echo "$S" | grep -ci 'IF NOT EXISTS')" -eq 0 ] && [ "$(echo "$S" | grep -ciE '^\s*(BEGIN|COMMIT)\s*;')" -eq 0 ] && [ "$(echo "$S" | grep -ci 'destinatario_original\|tocar_atualizado_em\|proxima_tentativa_em)\s*WHERE')" -eq 0 ] && echo "reconstrucao notificacoes_enviadas OK"</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe com o nome exato `supabase/migrations/20260721000001_notificacoes_enviadas.sql`.
    - Ignorando linhas de comentário, contém `CREATE TABLE public.notificacoes_enviadas` (sem `IF NOT EXISTS`) com exatamente as 16 colunas da seção A do dump, na mesma ordem, com os mesmos tipos, nulidades e defaults.
    - Contém os nomes de constraint literais do dump, incluindo `uq_notif_dedupe` sobre `dedupe_key` e o CHECK de `evento` com os 4 valores.
    - Contém `CREATE TYPE public.status_notificacao AS ENUM` com os labels da seção F na ordem de `enumsortorder`, e NÃO contém nenhum `CREATE TYPE ... etapa_processo`.
    - Contém `ALTER TABLE public.notificacoes_enviadas ENABLE ROW LEVEL SECURITY;` e exatamente um `CREATE POLICY rh_le_notificacoes`.
    - O literal de role dentro da policy é idêntico ao registrado na seção D do `37-SCHEMA-VIVO.md` (comparação literal, não "equivalente").
    - `grep -v '^\s*--' <arquivo> | grep -ci 'IF NOT EXISTS'` retorna 0.
    - `grep -v '^\s*--' <arquivo> | grep -ciE '^\s*(BEGIN|COMMIT)\s*;'` retorna 0.
    - `grep -v '^\s*--' <arquivo> | grep -ci 'destinatario_original'` retorna 0 e `... grep -ci 'tocar_atualizado_em'` retorna 0 — as lacunas NÃO entram aqui.
    - O cabeçalho contém um parágrafo de PROVENIÊNCIA citando `20260721000001`, "reconstrução", "não aplicar" e a migration aditiva `20260722000002`.
  </acceptance_criteria>
  <done>O DDL vivo de `notificacoes_enviadas` está declarado no repositório com nomes e definições fiéis ao catálogo, sem mascaramento e sem antecipar as lacunas.</done>
</task>

<task type="auto">
  <name>Task 2: Reconstruir 20260721000002_config_sla_etapa.sql, incluindo o seed das 8 etapas</name>
  <files>supabase/migrations/20260721000002_config_sla_etapa.sql</files>
  <read_first>
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-SCHEMA-VIVO.md — seções A, B, C, D, E, G, H (parte `config_sla_etapa`) e J (as 8 linhas do seed com `prazo_valor`, `prazo_unidade`, `rotulo_candidato` literais)
    - supabase/migrations/20260721000001_notificacoes_enviadas.sql (escrito na Task 1) — para manter cabeçalho e formatação consistentes entre os dois reconstruídos
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-CONTEXT.md § "Escopo e Prova" — a decisão travada de NÃO re-seedar em PROD
  </read_first>
  <action>
    Criar `supabase/migrations/20260721000002_config_sla_etapa.sql` reconstruindo o DDL vivo de `public.config_sla_etapa` a partir do dump.

    Cabeçalho no mesmo estilo da Task 1, declarando: M7 / Phase 37 / Plano 37-02, requisito TIMELINE-01, o parágrafo de PROVENIÊNCIA (reconstrução da version `20260721000002` já presente no ledger, NÃO aplicar) e a nota de ausência de `BEGIN;`/`COMMIT;`.

    Corpo, na ordem: (1) `CREATE TABLE public.config_sla_etapa` com as 5 colunas da seção A (`etapa` como PK do tipo `public.etapa_processo`, `prazo_valor`, `prazo_unidade`, `rotulo_candidato`, `atualizado_em`), com tipos/nulidades/defaults do dump, e as 4 constraints da seção B pelos nomes reais — incluindo `ck_sla_prazo_consistente` com a definição verbatim do `pg_get_constraintdef`, e os dois CHECKs de `prazo_unidade` e `prazo_valor`. (2) `ALTER TABLE ... ENABLE ROW LEVEL SECURITY;` e `CREATE POLICY sla_public_read` transcrita da seção D (roles `anon` e `authenticated`, `SELECT`, `USING (true)`). (3) `COMMENT ON TABLE` explicitando: config estática non-PII, public-read por design (TIMELINE-01/TIMELINE-02 — o painel do candidato lê esta tabela sem autenticação privilegiada), e que o `rotulo_candidato` é texto voltado ao candidato, enquadrado como ESTIMATIVA e nunca countdown. (4) O seed das 8 etapas, transcrito literalmente das 8 linhas da seção J do dump — mesmos `prazo_valor`, `prazo_unidade` e `rotulo_candidato`, com a acentuação pt-BR preservada byte-a-byte. Escrever o seed como `INSERT ... ON CONFLICT (etapa) DO NOTHING` para que um rebuild de ambiente não quebre num banco parcialmente seedado. NÃO incluir `atualizado_em` no INSERT — deixar o default `now()` agir.

    PROIBIÇÕES: nenhum `CREATE TABLE IF NOT EXISTS`; nenhum `ON CONFLICT ... DO UPDATE` (um upsert re-seedaria e sobrescreveria em PROD o texto que já está correto — decisão travada do CONTEXT); nenhuma coluna ou constraint fora do dump; nenhum `BEGIN;`/`COMMIT;`; nenhuma alteração dos rótulos pt-BR (nem correção de acentuação, nem melhoria de redação — o texto vivo é o texto).
  </action>
  <verify>
    <automated>F=supabase/migrations/20260721000002_config_sla_etapa.sql; test -f "$F" && S=$(grep -v '^\s*--' "$F"); echo "$S" | grep -q "CREATE TABLE public.config_sla_etapa" && echo "$S" | grep -q "ck_sla_prazo_consistente" && echo "$S" | grep -q "sla_public_read" && echo "$S" | grep -q "ENABLE ROW LEVEL SECURITY" && [ "$(echo "$S" | grep -ci 'IF NOT EXISTS')" -eq 0 ] && [ "$(echo "$S" | grep -ci 'DO UPDATE')" -eq 0 ] && [ "$(echo "$S" | grep -ciE '^\s*(BEGIN|COMMIT)\s*;')" -eq 0 ] && [ "$(echo "$S" | grep -c "ON CONFLICT (etapa) DO NOTHING")" -ge 1 ] && for e in inscricao triagem avaliacao_assincrona entrevista_online entrevista_presencial decisao_final aprovado rejeitado; do echo "$S" | grep -q "$e" || { echo "FALTA etapa $e"; exit 1; }; done && echo "reconstrucao config_sla_etapa OK (8 etapas)"</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe com o nome exato `supabase/migrations/20260721000002_config_sla_etapa.sql`.
    - Contém `CREATE TABLE public.config_sla_etapa` (sem `IF NOT EXISTS`) com as 5 colunas do dump, `etapa` como PK do tipo `public.etapa_processo`.
    - Contém os nomes das 4 constraints do dump, incluindo `ck_sla_prazo_consistente` com a definição verbatim de `pg_get_constraintdef`.
    - Contém `ENABLE ROW LEVEL SECURITY` e exatamente um `CREATE POLICY sla_public_read` com roles `anon` e `authenticated`, comando `SELECT`, `USING (true)`.
    - O seed cobre os 8 valores de `etapa_processo` (`inscricao`, `triagem`, `avaliacao_assincrona`, `entrevista_online`, `entrevista_presencial`, `decisao_final`, `aprovado`, `rejeitado`), cada um com `prazo_valor`/`prazo_unidade`/`rotulo_candidato` idênticos à seção J do dump — comparação literal, incluindo acentuação.
    - O seed usa `ON CONFLICT (etapa) DO NOTHING`; `grep -ci 'DO UPDATE'` (fora de comentários) retorna 0.
    - `grep -v '^\s*--' <arquivo> | grep -ci 'IF NOT EXISTS'` retorna 0 e `... grep -ciE '^\s*(BEGIN|COMMIT)\s*;'` retorna 0.
    - O `COMMENT ON TABLE` cita `TIMELINE-01`, "non-PII", "public-read" e "estimativa".
  </acceptance_criteria>
  <done>`config_sla_etapa` e seu seed de 8 etapas estão declarados no repositório de forma fiel e re-executável em rebuild, sem risco de sobrescrever o texto vivo em PROD.</done>
</task>

<task type="auto">
  <name>Task 3: Escrever o smoke de fidelidade campo-a-campo contra o catálogo</name>
  <files>supabase/tests/p37_fidelidade_schema_smoke.sql</files>
  <read_first>
    - supabase/tests/seg33_agendamento_smokes.sql (linhas 1-45 e o corpo do `DO $$`) — o idioma canônico de smoke deste repo: cabeçalho documentando cada asserção, `RESET ROLE;`, `DO $$ ... $$;` com `RAISE NOTICE 'PASS (x) …'` e o gate de CONTAR os PASS
    - supabase/migrations/20260721000001_notificacoes_enviadas.sql e supabase/migrations/20260721000002_config_sla_etapa.sql (escritos nas Tasks 1 e 2) — os literais a asserir vêm DELES, para que o smoke feche o triângulo dump→arquivo→banco
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-SCHEMA-VIVO.md — as contagens exatas (colunas, constraints, índices, policies, triggers)
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-CONTEXT.md § "Fidelidade provada por smoke de diff"
  </read_first>
  <action>
    Criar `supabase/tests/p37_fidelidade_schema_smoke.sql` — o gate que prova que os dois arquivos reconstruídos descrevem o banco real, e não o banco imaginado.

    Cabeçalho `-- ===` documentando: para que serve, que é rodado via MCP `execute_sql` pelo orquestrador (nunca pelo executor), quais asserções existem, e o gate verde = contar TODOS os `PASS` esperados (um run sem exceção mas com asserções puladas mascara falha — precedente Pitfall 2 do seg33).

    Primeira linha executável relevante: uma variável booleana `v_pos_aditiva` declarada com valor `false`, documentada como o toggle de baseline. Com `false`, o smoke assere o estado ANTERIOR à migration aditiva `20260722000002`; com `true`, assere o estado POSTERIOR (2 colunas a mais, 1 constraint a mais, 1 índice a mais, 1 trigger a mais em cada tabela). Isso torna o arquivo permanentemente re-executável em vez de virar um smoke morto após a 37-03. Documentar no cabeçalho como alternar.

    Asserções obrigatórias, cada uma com seu `RAISE NOTICE 'PASS (x) …'` e um `RAISE EXCEPTION` com a mensagem do que divergiu:
    (a) `notificacoes_enviadas` — contagem de colunas em `information_schema.columns` = 16 quando `v_pos_aditiva` é false, 18 quando true.
    (b) `notificacoes_enviadas` — para CADA uma das 16 colunas baseline, a tupla `(column_name, udt_name, is_nullable, coalesce(column_default,''))` bate com o literal transcrito do arquivo reconstruído. Comparar por igualdade de tupla, não por `LIKE`.
    (c) `notificacoes_enviadas` — contagem de constraints em `pg_constraint` = 5 (baseline) / 6 (pós-aditiva), e para cada constraint baseline, `pg_get_constraintdef(oid)` é IDÊNTICO ao literal do dump. Nomear explicitamente `uq_notif_dedupe` e o CHECK de `evento`.
    (d) `notificacoes_enviadas` — contagem de índices em `pg_indexes` = 5 (baseline) / 6 (pós-aditiva), e cada `indexdef` baseline bate com o literal.
    (e) `notificacoes_enviadas` — `relrowsecurity` é true; existe exatamente 1 policy; ela se chama `rh_le_notificacoes`; seu `cmd` é `SELECT`; seus `roles` são `{authenticated}`; e seu `qual` é idêntico ao literal do dump. A igualdade do `qual` é a asserção mais importante do arquivo — é ela que prova que o LEDGER-03 vivo é o join-through vaga-scoped e não outra coisa.
    (f) `notificacoes_enviadas` — contagem de triggers não-internos em `pg_trigger` = 0 (baseline) / 1 (pós-aditiva, `tocar_atualizado_em`).
    (g) `config_sla_etapa` — contagem de colunas = 5; cada coluna bate por tupla; contagem de constraints = 4 com `pg_get_constraintdef` idêntico (nomeando `ck_sla_prazo_consistente`); contagem de índices = 1; `relrowsecurity` true; exatamente 1 policy chamada `sla_public_read` com `roles` contendo `anon` e `authenticated`, `cmd` `SELECT` e `qual` `true`; triggers não-internos = 0 (baseline) / 1 (pós-aditiva).
    (h) enum `public.status_notificacao` — os labels, na ordem de `enumsortorder`, são idênticos à lista do arquivo reconstruído.
    (i) enum `public.etapa_processo` — 8 labels (referenciado, não criado por estes arquivos).
    (j) Seed — `count(*) from public.config_sla_etapa` = 8 e o conjunto de `etapa` é igual ao conjunto de labels de `etapa_processo` (nenhuma etapa sem rótulo, nenhum rótulo órfão).
    (k) Contagem final: emitir `RAISE NOTICE 'RESUMO: N asserções PASS de N esperadas'`.

    O smoke é 100% somente-leitura: nenhum INSERT, UPDATE, DELETE ou DDL. Ele não cria fixture nem precisa de cleanup — as provas comportamentais (RLS, idempotência, trigger) vivem no smoke da 37-03.

    NÃO usar `LIKE`/`similar to`/normalização de espaços nas comparações de definição: a comparação frouxa é exatamente o que deixaria uma divergência passar.
  </action>
  <verify>
    <automated>F=supabase/tests/p37_fidelidade_schema_smoke.sql; test -f "$F" && S=$(grep -v '^\s*--' "$F"); echo "$S" | grep -q "v_pos_aditiva" && echo "$S" | grep -q "pg_get_constraintdef" && echo "$S" | grep -q "pg_policies\|pg_policy" && echo "$S" | grep -q "pg_indexes" && echo "$S" | grep -q "information_schema.columns" && echo "$S" | grep -q "pg_trigger" && echo "$S" | grep -q "enumsortorder" && echo "$S" | grep -q "uq_notif_dedupe" && echo "$S" | grep -q "rh_le_notificacoes" && echo "$S" | grep -q "sla_public_read" && echo "$S" | grep -q "ck_sla_prazo_consistente" && [ "$(echo "$S" | grep -ciE '\b(insert|update|delete|drop|alter|create table)\b')" -eq 0 ] && [ "$(echo "$S" | grep -c "PASS")" -ge 10 ] && echo "smoke de fidelidade OK"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/tests/p37_fidelidade_schema_smoke.sql` existe.
    - Declara `v_pos_aditiva boolean := false` e o cabeçalho documenta como alterná-lo e o que muda em cada modo (contagens 16→18 colunas, 5→6 constraints, 5→6 índices, 0→1 trigger por tabela).
    - Contém asserções que leem os QUATRO catálogos: `information_schema.columns`, `pg_constraint` (via `pg_get_constraintdef`), `pg_indexes` e `pg_policies`, além de `pg_trigger` e `pg_enum`.
    - As comparações de definição usam igualdade (`=` / `IS DISTINCT FROM`), não `LIKE` nem normalização — `grep -ci "like '%"` fora de comentários retorna 0.
    - Cada asserção emite `RAISE NOTICE 'PASS (x) …'` e cada falha emite `RAISE EXCEPTION` nomeando o objeto divergente e mostrando esperado vs. encontrado; há pelo menos 10 ocorrências de `PASS`.
    - Assere nominalmente `uq_notif_dedupe`, `rh_le_notificacoes`, `sla_public_read` e `ck_sla_prazo_consistente`.
    - `grep -v '^\s*--' <arquivo> | grep -ciE '\b(insert|update|delete|drop|alter|create table)\b'` retorna 0 — o smoke é somente-leitura.
    - O arquivo termina emitindo um `RESUMO` com a contagem de asserções PASS.
  </acceptance_criteria>
  <done>Existe um gate executável que falha se qualquer coluna, constraint, índice, policy, trigger ou label de enum divergir entre os arquivos reconstruídos e o banco vivo — nos dois lados do apply da migration aditiva.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositório (declarado) → PROD (real) | Um arquivo infiel só se manifesta num rebuild/`db reset` futuro, longe da fase que o criou |
| Autor do arquivo → revisor | "Escrever e revisar por leitura" é o processo que produziu o drift original |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | Tampering | `CREATE TABLE IF NOT EXISTS` num rebuild contra banco divergente: silenciosamente mantém o schema errado e o repo "passa" | mitigate | Proibido pelo acceptance criteria das Tasks 1 e 2 (`grep -ci 'IF NOT EXISTS'` = 0) |
| T-37-02-02 | Tampering | Nome de constraint/policy inventado (`uq_dedupe_key` em vez de `uq_notif_dedupe`) faz um rebuild produzir schema que não bate com PROD — o drift volta pela porta dos fundos | mitigate | Nomes transcritos do dump; smoke da Task 3 assere nominalmente os 4 objetos-chave |
| T-37-02-03 | Elevation of Privilege | Policy reconstruída com predicado frouxo (ex.: perder o `AND candidatura_id IN (...)`) abriria o ledger de PII a qualquer recrutador num rebuild | mitigate | Asserção (e) da Task 3 exige igualdade EXATA do `qual` contra o literal do dump — não `LIKE`, não "equivalente" |
| T-37-02-04 | Elevation of Privilege | Literal de role errado (`admin` da paráfrase vs. `administrador` do repo) faz a policy nunca casar para admin — ou, pior, casar para um valor inesperado | mitigate | Literal vem da seção D do dump; acceptance criteria da Task 1 exige comparação literal contra o dump |
| T-37-02-05 | Information Disclosure | `sla_public_read` reconstruída sobre uma tabela que ganhasse coluna PII no futuro vazaria por `anon` | accept | `config_sla_etapa` é config estática non-PII por design (TIMELINE-01); o `COMMENT ON TABLE` declara isso como invariante para futuras alterações |
| T-37-02-06 | Repudiation | Re-aplicar os arquivos reconstruídos (via `apply_migration` ou `db push`) corromperia o histórico e poderia falhar no meio | mitigate | Plano é 100% de arquivo, `autonomous: true`, sem qualquer contato com PROD; o objetivo declara a proibição; o ledger já contém as duas versions |
| T-37-02-07 | Tampering | Re-seed com `ON CONFLICT DO UPDATE` sobrescreveria em PROD os rótulos pt-BR que já estão corretos | mitigate | `DO NOTHING` obrigatório; `grep -ci 'DO UPDATE'` = 0 no acceptance criteria da Task 2 |
</threat_model>

<verification>
- Os dois arquivos existem em `supabase/migrations/` com os nomes exatos das versions do ledger
- Nenhum dos dois contém `IF NOT EXISTS`, `BEGIN;`, `COMMIT;` ou `DO UPDATE` fora de comentários
- Nenhum dos dois contém as lacunas da 37-03 (`destinatario_original`, `modo`, `tocar_atualizado_em`, índice parcial)
- O smoke de fidelidade existe, é somente-leitura, tem toggle `v_pos_aditiva` e assere os 4 catálogos
- `npm run lint` mantém a contagem de erros `tsc` em 97 (baseline; teto CI 104) e nenhum erro cita arquivos deste plano
- `npm run build` e `npm run test:run` continuam verdes
</verification>

<success_criteria>
1. Um `supabase db reset` / rebuild de ambiente reproduz `notificacoes_enviadas` e `config_sla_etapa` com os mesmos nomes de objeto, os mesmos predicados de RLS e o mesmo seed que estão vivos em PROD hoje.
2. O repositório voltou a ser fonte da verdade para as duas tabelas, sem mentir sobre o histórico (nada foi re-aplicado, nada foi editado retroativamente).
3. A fidelidade é verificável por execução, não por revisão — o smoke falha alto em qualquer divergência de coluna, constraint, índice, policy, trigger ou enum.
</success_criteria>

<output>
Create `.planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-02-SUMMARY.md` when done
</output>
