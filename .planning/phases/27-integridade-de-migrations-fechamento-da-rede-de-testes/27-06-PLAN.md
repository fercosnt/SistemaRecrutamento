---
phase: 27-integridade-de-migrations-fechamento-da-rede-de-testes
plan: 06
type: execute
wave: 4
depends_on: [27-05]
files_modified: []
autonomous: false
requirements: [CI-07, CI-13]
user_setup:
  - service: supabase
    why: "CI-07 bare-`zod` import map only takes effect after the affected EFs are redeployed; CI-13 verify_jwt posture must be live-smoked"
    dashboard_config:
      - task: "Ensure the Supabase CLI is linked to the project and has deploy access (functions deploy)"
        location: "Supabase Dashboard → Project → Edge Functions"
must_haves:
  truths:
    - "CI-07: the 5 EFs whose bundle reaches the bare-`zod` shared module (submit-candidatura, avaliar-redacao-cultural, avaliar-transcricao-entrevista, gerar-guia-entrevista, consolidar-decisao-final) are redeployed so the deno.json/config.toml import map resolves bare `zod` at runtime (bundle-freeze — a _shared edit only takes effect on redeploy)"
    - "each redeployed EF is live-smoked and returns its expected response (no 500 from an unresolved `zod` import — RESEARCH Assumption A2)"
    - "CI-13: the live verify_jwt posture matches supabase/config.toml — the 3 Bearer-self-auth EFs stay reachable server-to-server; the 9 jwt-on EFs return 401 on a no-auth curl"
    - "submit-candidatura still enforces getUser 401 + `.strict()` after the handler refactor (27-03) is deployed"
  artifacts: []
  key_links:
    - from: "redeployed EF bundles"
      to: "supabase/functions/deno.json import map (bare zod)"
      via: "supabase functions deploy + config.toml import_map"
      pattern: "functions deploy"
    - from: "live verify_jwt posture"
      to: "supabase/config.toml (27-02)"
      via: "curl no-auth → 401 (jwt-on) / reachable (Bearer-self-auth)"
      pattern: "verify_jwt"
---

<objective>
[BLOCKING · non-autonomous · PROD deploy] The final wave: redeploy the EFs whose import graph now reaches the bare-`zod` shared module (CI-07) so the `deno.json`/`config.toml` import map takes effect at runtime, and live-smoke each. Then verify the live `verify_jwt` posture matches `supabase/config.toml` (CI-13) — the three Bearer-self-auth EFs must stay reachable server-to-server, the nine jwt-on EFs must 401 on a no-auth request. This mirrors the Phase 24-09 redeploy-BLOCKING precedent (separate from the 24-08/27-05 apply wave).

Purpose: `_shared` edits are bundle-frozen — they only land for the EFs actually redeployed. The bare-`zod` rewrite (27-02) + the submit-candidatura handler refactor (27-03) are inert until these deploys; and config.toml's declared posture must be confirmed against the live functions.
Output: 5 redeployed + live-smoked EFs; a verify_jwt live-posture confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md
@.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-PATTERNS.md

<interfaces>
<!-- Redeploy set + verify_jwt live-smoke expectations. RESEARCH §3c/§6. -->
EFs to redeploy (bundle reaches the bare-`zod` shared module — Pitfall 3):
  submit-candidatura, avaliar-redacao-cultural, avaliar-transcricao-entrevista, gerar-guia-entrevista, consolidar-decisao-final
Deploy: supabase functions deploy <name>   (CLI v2 reads config.toml import_map = "./functions/deno.json")

Live verify_jwt smoke (RESEARCH §6, issue #4059 caveat — verify AFTER deploy):
  jwt-on (curl no-auth → expect 401): avaliar-redacao-cultural, avaliar-transcricao-entrevista, gerar-guia-entrevista, consolidar-decisao-final, submit-candidatura
  Bearer-self-auth (stay reachable server-to-server, curl no-auth → NOT a broken 500): analise-candidato-individual, cost-alerter, gerar-devolutiva-bigfive
Do NOT flip any Bearer-self-auth EF to verify_jwt=true (breaks server-to-server / SEC-04); do NOT leave a jwt-on EF false (auth hole).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI-07 — redeploy the 5 shared-`zod` EFs so the import map takes effect</name>
  <files></files>
  <read_first>
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md §3c (redeploy set + bundle-freeze) + §Assumptions A2 (verify import_map propagation with ONE EF first)
    - supabase/config.toml (from 27-02 — import_map entries for the 5 EFs)
    - supabase/functions/deno.json (from 27-02 — the zod import map)
    - reference_ef_shared_bundle_freeze / reference_ef_npm_join_import_bug (editing _shared only lands on redeploy; watch for unresolved-import 500s)
  </read_first>
  <action>
    De-risk first (A2): redeploy ONE low-risk EF of the set — `avaliar-redacao-cultural` — via `supabase functions deploy avaliar-redacao-cultural`, then smoke it to confirm the bare-`zod` import resolves at runtime (no ERR_MODULE / 500). Once confirmed, redeploy the remaining four: `submit-candidatura`, `avaliar-transcricao-entrevista`, `gerar-guia-entrevista`, `consolidar-decisao-final`. Each deploy uses the CLI which reads `config.toml`'s `import_map = "./functions/deno.json"`. Do NOT redeploy any EF outside this set (they keep their frozen bundles — no gratuitous redeploys). If import_map propagation fails (issue #4059 class → bare `zod` 500s at runtime), fall back to passing `--import-map supabase/functions/deno.json` explicitly on the deploy command.
  </action>
  <acceptance_criteria>
    - all 5 EFs redeployed (deploy command returns success for each)
    - the canary (avaliar-redacao-cultural) live-smoke returns a handled response (NOT a 500 from an unresolved `zod` import)
    - submit-candidatura live-smoke exercises a valid path without an import-resolution 500
    - no EF outside the 5-set was redeployed
  </acceptance_criteria>
  <verify>
    <manual>Deploy each of the 5 EFs; curl/live-invoke the canary + submit-candidatura → handled responses, zero import-resolution 500s.</manual>
  </verify>
  <done>The 5 shared-`zod` EFs are redeployed and live-smoked; the import map resolves at runtime.</done>
</task>

<task type="auto">
  <name>Task 2: CI-13 — verify the live verify_jwt posture matches config.toml</name>
  <files></files>
  <read_first>
    - supabase/config.toml (from 27-02 — the declared 12-fn posture: 3 false / 9 true)
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md §6 (per-fn posture + issue #4059 caveat: verify live after deploy) + §Security Domain
    - reference_ef_authenticate_vs_authorize / SEC-04 (the 3 false EFs are Bearer-self-auth; a wrong flag opens/breaks)
  </read_first>
  <action>
    Live-smoke the verify_jwt posture with a no-auth curl to each function's invoke URL. Confirm the 5 jwt-on EFs touched this phase (submit-candidatura, avaliar-redacao-cultural, avaliar-transcricao-entrevista, gerar-guia-entrevista, consolidar-decisao-final) return 401 on a no-auth request (verify_jwt still ON — the redeploy did not silently flip it, #4059 guard). Confirm the 3 Bearer-self-auth EFs (analise-candidato-individual, cost-alerter, gerar-devolutiva-bigfive) remain reachable server-to-server (a no-auth curl is rejected by their own Bearer guard, not broken by a config flip). If any live flag diverges from config.toml, that is a real drift — redeploy that function with the correct `--no-verify-jwt`/default to make config.toml authoritative, and note it. Do NOT change any Bearer-self-auth EF to jwt-on.
  </action>
  <acceptance_criteria>
    - each of the 5 jwt-on redeployed EFs: no-auth curl → HTTP 401
    - each of the 3 Bearer-self-auth EFs: reachable server-to-server (their own Bearer guard governs; no config-induced break)
    - the live posture matches supabase/config.toml for all 12 declarations (any divergence reconciled + noted)
    - no Bearer-self-auth EF was flipped to verify_jwt=true
  </acceptance_criteria>
  <verify>
    <manual>curl no-auth each EF: 5 jwt-on → 401; 3 Bearer-self-auth → reachable; posture == config.toml.</manual>
  </verify>
  <done>Live verify_jwt posture confirmed to match config.toml; no auth hole opened, no server-to-server path broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy config → live EF auth + bundle | redeploy propagates the import map + verify_jwt; a wrong flag or unresolved import breaks/opens the EF |
| no-auth caller → EF invoke URL | the verify_jwt gate must reject unauthenticated callers for the 9 jwt-on EFs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06-01 | Elevation | a jwt-on EF silently deploys verify_jwt=false (auth hole, #4059) | mitigate | no-auth curl each redeployed EF → assert 401; reconcile any divergence |
| T-27-06-02 | DoS | flipping a Bearer-self-auth EF to jwt-on breaks server-to-server (SEC-04) | mitigate | config.toml keeps the 3 EFs false; live-smoke confirms reachability; never flip |
| T-27-06-03 | Availability | bare-`zod` import fails to resolve at runtime → 500 on every call | mitigate | canary redeploy + smoke ONE EF first (A2); `--import-map` fallback |
| T-27-06-04 | Tampering | submit-candidatura handler refactor changes behavior on deploy | mitigate | live-smoke the 401 + valid path; Deno test (27-03) already asserts byte-equivalence |
| T-27-06-SC | Tampering | package installs | accept | none this phase; deploys only |
</threat_model>

<verification>
- 5 EFs redeployed; canary + submit-candidatura live-smoked (no import-resolution 500).
- 5 jwt-on EFs → 401 on no-auth curl; 3 Bearer-self-auth EFs reachable server-to-server.
- Live posture matches config.toml across all 12 declarations.
</verification>

<success_criteria>
CI-07's shared-`zod` contract is live (redeployed EFs resolve bare `zod`); CI-13's declared verify_jwt posture is confirmed against the live functions — no auth hole, no broken server-to-server path.
</success_criteria>

<output>
Create `.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-06-SUMMARY.md` when done.
</output>
