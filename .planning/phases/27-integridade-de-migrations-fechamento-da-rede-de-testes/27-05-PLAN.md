---
phase: 27-integridade-de-migrations-fechamento-da-rede-de-testes
plan: 05
type: execute
wave: 3
depends_on: [27-03, 27-04]
files_modified:
  - supabase/migrations/20260419000000_baseline.sql
  - database.types.ts
autonomous: false
requirements: [DBMIG-01, DBMIG-02, CI-03]
user_setup:
  - service: supabase
    why: "DBMIG-01 rebuild-from-zero verification needs a Supabase preview branch (Pro plan) OR a local db reset fallback; PROD ledger reconcile + DBMIG-02 apply touch the live database"
    dashboard_config:
      - task: "Confirm the project is on Supabase Pro (branching is Pro+; a Micro branch bills ~$0.01344/hr — create→verify→drop promptly). If not on Pro, use the local `db reset` fallback with seed disabled (a genuinely empty target — the same anti-false-pass property the branch reset provides)."
        location: "Supabase Dashboard → Organization → Billing"
must_haves:
  truths:
    - "DBMIG-01: rebuilding the 71 migration files into a clean target (preview branch, or local db reset with seed disabled) yields a catalog-fingerprint diff vs live PROD that is EMPTY — and the emptiness is TRUSTWORTHY because the branch was RESET to an empty ledger and PRE-CHECKED (`schema_migrations` count==0) BEFORE the local files were pushed, so the diff cannot false-pass on create_branch's tracked-history clone (Pitfall 1)"
    - "DBMIG-01: the empty 20260419000000_baseline.sql is filled with the Figma-Make base schema (a schema-only dump minus what the 70 later migrations create) so a from-zero replay does not fail with `relation/type does not exist`"
    - "DBMIG-01: the PROD ledger is reconciled so `schema_migrations` version rows == the 71 filename timestamps; `supabase db push --linked` reports 'Remote database is up to date'"
    - "DBMIG-02: the trigger fix + backfill migrations from 27-04 are applied via MCP apply_migration; the submit_candidatura_atomic + DBMIG-02 smokes pass on the live DB (survivor auto_rejeitado=false, knockout still true)"
    - "the DBMIG-02 apply_migration version rows are factored INTO the ledger reconcile so they add no fresh drift (Pitfall 5); RNF-07a preserved (no new auto-reject path)"
  artifacts:
    - path: "supabase/migrations/20260419000000_baseline.sql"
      provides: "filled base-schema baseline (was 0 lines) enabling from-zero replay"
      min_lines: 30
    - path: "database.types.ts"
      provides: "regenerated types (root) reflecting the converged schema"
  key_links:
    - from: "71 migration files (baseline + 70)"
      to: "live PROD catalog fingerprint"
      via: "reset-to-empty → pre-check → push-local-files → diff (empty diff = acceptance)"
      pattern: "information_schema|pg_catalog"
    - from: "schema_migrations version rows"
      to: "the 71 filename timestamps"
      via: "migration repair / execute_sql reconcile"
      pattern: "supabase db push --linked"
---

<objective>
[BLOCKING · non-autonomous · PROD] The single database-integrity wave. Verify the 71 migrations reconstruct the schema from zero (DBMIG-01) by rebuilding into a clean preview branch (or local `db reset` fallback) — RESETTING the branch to an empty ledger and PRE-CHECKING it is empty BEFORE pushing the local files (so a create_branch history-clone cannot false-pass), fingerprinting live PROD via catalog queries, diffing, and iterating the baseline/catch-up files until the diff is EMPTY. Apply the 27-04 DBMIG-02 trigger fix + backfill via MCP `apply_migration` and run the `submit_candidatura_atomic` + DBMIG-02 smokes on the live DB. Then reconcile the PROD ledger so filenames == version rows and `supabase db push --linked` says "up to date", factoring in the just-applied version rows. Regenerate `database.types.ts`.

Purpose: Reproducibility (rebuild-from-zero) + ledger convergence unblock pgTAP/`db push` and prove no only-in-PROD objects; the DBMIG-02 apply makes the audit trail honest and turns the CI-03 smoke green.
Output: filled baseline (+ any catch-up files), applied DBMIG-02, green live smokes, converged ledger, regenerated types.

This wave mirrors the Phase 24-08 / 25-07 / 26-07 BLOCKING precedent. Apply is via Supabase MCP `apply_migration` (bypasses the 42601 pooler trap; self-inserts a version row) — NOT `supabase db push` for the PL/pgSQL migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md
@.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-PATTERNS.md

<interfaces>
<!-- Catalog fingerprint queries + ledger reconcile primitives. RESEARCH §1b/§1d. -->
Catalog fingerprint (run on PROD via execute_sql AND on the rebuild target; diff must be empty):
  columns:   SELECT table_name,column_name,data_type,is_nullable FROM information_schema.columns WHERE table_schema='public' ORDER BY 1,2;
  enums:     SELECT t.typname,e.enumlabel,e.enumsortorder FROM pg_type t JOIN pg_enum e ON e.enumtypid=t.oid JOIN pg_namespace n ON n.oid=t.typnamespace WHERE n.nspname='public' ORDER BY 1,3;
  functions: SELECT proname, pg_get_function_identity_arguments(oid) FROM pg_proc WHERE pronamespace='public'::regnamespace ORDER BY 1;
  triggers:  SELECT tgname, tgrelid::regclass FROM pg_trigger WHERE NOT tgisinternal ORDER BY 2,1;
  policies:  SELECT schemaname,tablename,policyname FROM pg_policies WHERE schemaname='public' ORDER BY 2,3;
  indexes:   SELECT indexname FROM pg_indexes WHERE schemaname='public' ORDER BY 1;

Anti-false-pass pre-check (RESEARCH §1a Pitfall 1 / Open Question 1 — run ON THE BRANCH, BEFORE the push):
  ledger empty:   SELECT count(*) FROM supabase_migrations.schema_migrations;   -- MUST be 0 after reset (proves NOT auto-populated from tracked history)
  catalog empty:  SELECT count(*) FROM information_schema.tables WHERE table_schema='public';  -- MUST be the pre-baseline expectation (no app tables yet)

Branch reset primitives (force an empty→local-files replay, not a history clone):
  create_branch                 → MCP: creates the branch (clones tracked schema_migrations history — do NOT diff yet)
  reset_branch <version=0>      → MCP: resets the branch DB to the pre-baseline / empty migration version (empties schema_migrations)
  supabase db push (→ branch)   → pushes the LOCAL 71 files onto the emptied branch (the true from-zero-FILES replay)

Ledger reconcile primitives (RESEARCH §1d):
  supabase migration list --linked                          → drift map (local files vs remote version rows)
  supabase migration repair --status applied   <version>    → INSERTS a version row without running SQL
  supabase migration repair --status reverted  <version>    → DELETES a spurious timestamp-only version row
  goal: SELECT version FROM supabase_migrations.schema_migrations ORDER BY version == the 71 filename timestamps
  acceptance: supabase db push --linked → "Remote database is up to date"

DBMIG-02 files to apply (from 27-04): 20260712110001_avancar_etapa_auto_rejeitado_fix.sql, then 20260712110002_backfill_auto_rejeitado.sql
Smoke to run: supabase/tests/submit_candidatura_atomic_smokes.sql (from 27-03)
NEVER run supabase/seed.sql in the rebuild loop (placeholder-UUID FK to auth.users breaks db reset — Pitfall 6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DBMIG-01 — rebuild-from-zero (reset branch + pre-check empty ledger + push local files) + fill baseline until catalog diff is empty</name>
  <files>supabase/migrations/20260419000000_baseline.sql</files>
  <read_first>
    - supabase/migrations/20260419000000_baseline.sql (0 lines today — the "empty baseline" DBMIG-01 forbids)
    - supabase/migrations/20260420000001_rls_anon_to_rpc.sql (the first real migration — names the base objects it ASSUMES pre-exist)
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md §1a (create_branch replays tracked history NOT files) + §1b step 2 (reset→push-local) + §1c + §Runtime State Inventory (empty baseline row) + §Pitfall 1/6 + §Open Questions 1 (RESOLVED)
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-PATTERNS.md §"20260419000000_baseline.sql"
  </read_first>
  <action>
    FIRST (path decision, human-gated): confirm the project is on Supabase Pro so `create_branch` (MCP) is available — the CONTEXT-locked primary path (clean ephemeral branch; bills ~$0.01344/hr so create→verify→drop promptly; do NOT leave running). If Pro/branching is unavailable, fall back to a local `db reset` with the seed DISABLED (Docker + local stack — a genuinely empty target that provides the same anti-false-pass property).

    Author the baseline: run `supabase db dump --schema public --schema-only` against PROD (or MCP catalog reads) to get all object definitions; subtract what the 70 non-baseline migrations create; write the remainder (the Figma-Make base schema: candidatos, vagas, usuarios_rh, candidaturas, respostas_formulario, perguntas_formulario, enums, historico_acoes, storage buckets) into `20260419000000_baseline.sql`.

    ANTI-FALSE-PASS MECHANISM (CRITICAL — RESEARCH §1a Pitfall 1 + Open Question 1): MCP `create_branch` CLONES the tracked `schema_migrations` history and replays it — it does NOT replay the raw local files. A plain `create_branch` → catalog-diff would return EMPTY regardless of whether `20260419000000_baseline.sql` is complete (a silent false-green on this phase's headline goal). So do NOT diff a freshly-created branch. Instead, per RESEARCH §1b step 2:
      (1) after `create_branch`, RESET the branch to an empty/pre-baseline state — `reset_branch` to version zero (or otherwise truncate `supabase_migrations.schema_migrations` on the branch so no tracked history remains);
      (2) PRE-CHECK the branch is genuinely empty BEFORE any push: `SELECT count(*) FROM supabase_migrations.schema_migrations` on the branch == 0 AND `SELECT count(*) FROM information_schema.tables WHERE table_schema='public'` == the pre-baseline expectation (app tables absent). This PROVES the branch was NOT auto-populated from tracked history. If the pre-check shows a populated ledger, the reset did not take — STOP; do NOT proceed to the diff (it would false-pass);
      (3) THEN push the LOCAL 71 files onto the emptied branch: `supabase db push` targeting the branch (baseline + 20260420…latest replays from empty). Each `relation X does not exist` / `type Y does not exist` names an object still missing from the baseline — add it to the baseline (or a catch-up file) and repeat;
      (4) ONLY THEN fingerprint the branch AND PROD with the six catalog queries and diff: any object in PROD but absent in the branch → author a catch-up migration file (or add to the baseline if base schema) with a timestamp that sorts correctly; any object in the branch but absent in PROD → reconcile in Task 3.

    Iterate create→reset→pre-check→push→diff→add→drop until the diff is EMPTY. NEVER run seed.sql in the loop. Drop the branch promptly after verification (cost).
  </action>
  <acceptance_criteria>
    - PRE-CHECK (anti-false-pass, Pitfall 1): BEFORE pushing the local files, the branch ledger is empty — `SELECT count(*) FROM supabase_migrations.schema_migrations` on the branch == 0 (proves the branch was reset, NOT auto-populated from create_branch's tracked-history clone); app tables absent from `public` at pre-check time
    - `wc -l < supabase/migrations/20260419000000_baseline.sql` > 0 (baseline no longer empty)
    - a from-zero replay of `baseline + the 70 migrations` on the EMPTIED target (push of the local files) completes with NO `does not exist` error
    - the six catalog-fingerprint queries diff (branch vs live PROD) returns EMPTY for tables/columns, enums, functions, triggers, policies, indexes
    - the preview branch (if used) is DROPPED after verification (not left running)
    - any authored catch-up files sort correctly relative to their dependencies
  </acceptance_criteria>
  <verify>
    <automated>wc -l < supabase/migrations/20260419000000_baseline.sql</automated>
    <manual>Branch ledger empty BEFORE push (schema_migrations count==0 — anti-false-pass pre-check per Pitfall 1); after pushing the 71 LOCAL files onto the emptied branch, the six-query catalog-fingerprint diff (branch vs PROD) returns empty across all object classes; branch dropped.</manual>
  </verify>
  <done>Baseline filled + catch-up files (if any) authored; the branch is reset-then-pre-checked-empty before the local files are pushed, and a from-zero replay reproduces PROD with an empty catalog diff (no false-pass).</done>
</task>

<task type="auto">
  <name>Task 2: DBMIG-02 — apply trigger fix + backfill via MCP, then run the live smokes</name>
  <files>database.types.ts</files>
  <read_first>
    - supabase/migrations/20260712110001_avancar_etapa_auto_rejeitado_fix.sql + 20260712110002_backfill_auto_rejeitado.sql (from 27-04 — the files to apply)
    - supabase/tests/submit_candidatura_atomic_smokes.sql (from 27-03 — the smoke to run; RED until the trigger applies)
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md §2d (apply via MCP apply_migration; factor version rows into the ledger reconcile) + §4 layer (c)
    - CLAUDE.md: apply PL/pgSQL via MCP apply_migration (bypasses 42601); database.types.ts at REPO ROOT via MCP generate_typescript_types
  </read_first>
  <action>
    Apply the two DBMIG-02 migrations to PROD via Supabase MCP `apply_migration` IN ORDER: first the trigger `CREATE OR REPLACE` (`20260712110001`), then the backfill `UPDATE` (`20260712110002`). Record the version rows apply_migration inserts (needed for Task 3's reconcile — Pitfall 5). Run `supabase/tests/submit_candidatura_atomic_smokes.sql` on the live DB (via MCP execute_sql / SQL Editor) and confirm all three assertions PASS: knockout sanctioned-reject (one history row `auto_rejeitado=true, ator NULL`), survivor advance (`etapa→triagem`, `auto_rejeitado=false`), dedup DUPLICATE. Regenerate `database.types.ts` at the repo root via MCP `generate_typescript_types`. After regen, run `npm run -s lint 2>&1 | grep -c "error TS"`; expected UNCHANGED (DBMIG-02 adds zero columns) — if it changed, re-pin the ci.yml gate to the new measured value (belt-and-suspenders vs 27-03's re-pin).
  </action>
  <acceptance_criteria>
    - both migrations applied (apply_migration returns success); their version rows recorded for Task 3
    - the smoke reports PASS for all three assertions (knockout true / survivor false / dedup DUPLICATE) on live PROD
    - a live query confirms survivor-advance history rows now have `auto_rejeitado=false` and terminal-reject rows still `true`
    - `database.types.ts` regenerated at repo root; `npm run -s lint 2>&1 | grep -c "error TS"` == the pinned baseline (or ci.yml re-pinned if it changed)
    - RNF-07a preserved: no new auto-reject path introduced (only the knockout writes a sanctioned reject)
  </acceptance_criteria>
  <verify>
    <automated>npm run -s lint 2>&1 | grep -c "error TS"</automated>
    <manual>submit_candidatura_atomic_smokes.sql on PROD → all three PASS; survivor auto_rejeitado=false verified by direct query.</manual>
  </verify>
  <done>DBMIG-02 trigger + backfill live on PROD; smokes green; types regenerated; RNF-07a intact.</done>
</task>

<task type="auto">
  <name>Task 3: DBMIG-01 — reconcile the PROD ledger to convergence</name>
  <files>supabase/migrations/20260419000000_baseline.sql</files>
  <read_first>
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md §1d + §Runtime State Inventory (ledger drift from Phases 24/25/26 apply_migration timestamp rows) + §Pitfall 5
    - CLAUDE.md D-22 step 3 (db push "Remote database is up to date" = convergence signal)
    - the version rows recorded in Task 2 (must be included in the reconciled set)
  </read_first>
  <action>
    Run `supabase migration list --linked` to produce the drift map (local filenames vs remote `schema_migrations` versions), including the timestamp-only rows written by MCP `apply_migration` across Phases 24/25/26 and this phase's Task 2. Reconcile per row using the NON-DESTRUCTIVE primitive first: `supabase migration repair --status applied <version>` to insert a missing filename version whose SQL is already live; `supabase migration repair --status reverted <version>` only to remove a spurious timestamp-only row so its real filename-versioned file can be recorded. Prefer aligning a version row to a filename over dropping/re-pushing live DDL (Tampering guard). Goal state: `SELECT version FROM supabase_migrations.schema_migrations ORDER BY version` equals the set of 71 filename timestamps exactly. Confirm convergence: `supabase db push --linked` must report "Remote database is up to date".
  </action>
  <acceptance_criteria>
    - `supabase migration list --linked` shows local and remote columns aligned (no orphan timestamp-only rows, no unrecorded filenames)
    - `SELECT count(*) FROM supabase_migrations.schema_migrations` equals the count of migration filenames (71)
    - `supabase db push --linked` outputs "Remote database is up to date"
    - reconcile used repair/version-row ops only — no live DDL re-run (verified by the absence of new object changes in a post-reconcile catalog fingerprint)
  </acceptance_criteria>
  <verify>
    <automated>ls supabase/migrations/*.sql | wc -l</automated>
    <manual>`supabase db push --linked` prints "Remote database is up to date"; schema_migrations version set == the 71 filename timestamps.</manual>
  </verify>
  <done>Ledger converged: filenames == version rows; db push clean; reconcile non-destructive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migration files → live PROD schema/ledger | reconcile + apply touch the production database; must be non-destructive |
| rebuild target (branch) → PROD comparison | the catalog diff is the acceptance oracle; a false pass hides only-in-PROD objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05-01 | Tampering | ledger reconcile re-runs unintended DDL on PROD | mitigate | prefer `migration repair` (version-row only) over drop/re-push; human-gated BLOCKING wave; post-reconcile catalog fingerprint unchanged |
| T-27-05-02 | Spoofing | `create_branch` replays tracked history not files → false "rebuild passed" | mitigate | RESET branch to an empty ledger + PRE-CHECK `schema_migrations` count==0 BEFORE pushing the local files, THEN mechanism-independent catalog-fingerprint diff (branch vs PROD) is the acceptance — not "the branch came up" (Pitfall 1 / Open Question 1) |
| T-27-05-03 | Elevation | DBMIG-02 apply introduces a 2nd auto-reject path | mitigate | terminal+GUC-gated predicate (27-04); smoke asserts survivor false / knockout-only true; RNF-07a |
| T-27-05-04 | DoS/cost | preview branch left running bills continuously | accept/mitigate | create→verify→drop promptly; checkpoint confirms Pro-plan + cost |
| T-27-05-05 | Tampering | apply_migration adds fresh ledger drift | mitigate | Task 2 records its version rows; Task 3 includes them in the reconcile (Pitfall 5) |
| T-27-05-SC | Tampering | package installs | accept | none this phase |
</threat_model>

<verification>
- Branch reset to empty ledger + pre-check (schema_migrations count==0) BEFORE the local-files push; from-zero replay clean; catalog diff (branch vs PROD) empty.
- DBMIG-02 applied via MCP; live smokes PASS (knockout true / survivor false / dedup DUPLICATE).
- `supabase db push --linked` = "Remote database is up to date"; schema_migrations == 71 filename timestamps.
- `database.types.ts` regenerated; tsc gate still green.
</verification>

<success_criteria>
The 71 migrations reconstruct the DB from zero (empty catalog diff, proven by the reset+pre-check anti-false-pass), the ledger converges (db push clean), and DBMIG-02 is live with green smokes — RNF-07a preserved throughout.
</success_criteria>

<output>
Create `.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-05-SUMMARY.md` when done.
</output>
