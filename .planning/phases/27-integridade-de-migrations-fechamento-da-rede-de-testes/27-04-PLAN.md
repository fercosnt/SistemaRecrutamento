---
phase: 27-integridade-de-migrations-fechamento-da-rede-de-testes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260712110001_avancar_etapa_auto_rejeitado_fix.sql
  - supabase/migrations/20260712110002_backfill_auto_rejeitado.sql
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [DBMIG-02, DBMIG-01]
must_haves:
  truths:
    - "DBMIG-02: the avancar_etapa trigger writes auto_rejeitado=true ONLY for a system write (v_ator IS NULL) that is ALSO a sanctioned auto-reject (GUC app.rejeicao_sancionada='on') landing on a terminal 'rejeitado' etapa — a survivor advance (inscricao→triagem) writes false"
    - "DBMIG-02: the trigger fix (CREATE OR REPLACE) and the one-time backfill UPDATE are TWO distinct migration files — a code change does not retro-correct existing rows (Pitfall 4)"
    - "DBMIG-02 preserves RNF-07a: the predicate change creates NO new auto-reject path; the knockout in submit_candidatura_atomic remains the only sanctioned auto-reject; guard_rejeicao_auditada backstop untouched"
    - "DBMIG-01: the stale '49 migrations' count is corrected to 71 in REQUIREMENTS.md, ROADMAP.md, and STATE.md"
  artifacts:
    - path: "supabase/migrations/20260712110001_avancar_etapa_auto_rejeitado_fix.sql"
      provides: "CREATE OR REPLACE avancar_etapa with GUC-gated auto_rejeitado predicate; no BEGIN/COMMIT wrapper"
      contains: "app.rejeicao_sancionada"
    - path: "supabase/migrations/20260712110002_backfill_auto_rejeitado.sql"
      provides: "one-time UPDATE correcting mismarked system-advance rows to false"
      contains: "SET auto_rejeitado = false"
  key_links:
    - from: "avancar_etapa trigger predicate"
      to: "app.rejeicao_sancionada GUC (set by submit_candidatura_atomic)"
      via: "current_setting('app.rejeicao_sancionada', true)"
      pattern: "current_setting\\('app.rejeicao_sancionada'"
---

<objective>
Author the two DBMIG-02 migration files (file-only; applied in the BLOCKING wave 27-05) and correct the stale migration count. The trigger fix replaces the defective `auto_rejeitado = (v_ator IS NULL)` literal at `20260607000005_avancar_etapa_trigger.sql:82` — which mismarks EVERY system write (including survivor advances) as an auto-rejection — with a predicate that also requires the sanctioned-reject GUC and a terminal etapa. A SEPARATE backfill migration corrects the historical rows already mismarked. A docs task fixes "49"→71.

Purpose: `historico_candidatura.auto_rejeitado` must honestly distinguish "system write" from "sanctioned auto-rejection" so the audit trail (and the CI-03 smoke) reflects reality — without adding a column and without opening a second auto-reject path (RNF-07a).
Output: 2 new migration files + corrected counts in 3 planning docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md
@.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-PATTERNS.md

<interfaces>
<!-- The exact defect + the CONTEXT-locked fix. Executor mirrors 20260709000014's footer. -->
Defect — supabase/migrations/20260607000005_avancar_etapa_trigger.sql:78-82:
  INSERT INTO public.historico_candidatura (candidatura_id, etapa_de, etapa_para, criterio_texto, ator, auto_rejeitado, criado_em)
  VALUES (NEW.id, OLD.etapa_atual, NEW.etapa_atual, NEW.etapa_justificativa,
          v_ator, (v_ator IS NULL), now());      -- ← DEFECT: marks EVERY system write

CONTEXT-locked replacement predicate (RESEARCH §2b — reuse ator IS NULL + GUC, zero new column):
  (v_ator IS NULL
   AND current_setting('app.rejeicao_sancionada', true) IS NOT DISTINCT FROM 'on'
   AND NEW.etapa_atual = 'rejeitado')

GUC precedent (already set by the knockout path) — 20260709000014_submit_candidatura_flag.sql:136:
  PERFORM set_config('app.rejeicao_sancionada', 'on', true);   -- SET LOCAL, pooler-safe

Backfill (RESEARCH §2c — DISTINCT file):
  UPDATE public.historico_candidatura SET auto_rejeitado = false
   WHERE auto_rejeitado = true AND etapa_para <> 'rejeitado';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DBMIG-02 — CREATE OR REPLACE avancar_etapa with the GUC-gated auto_rejeitado predicate</name>
  <files>supabase/migrations/20260712110001_avancar_etapa_auto_rejeitado_fix.sql</files>
  <read_first>
    - supabase/migrations/20260607000005_avancar_etapa_trigger.sql (full — the function to CREATE OR REPLACE; defect at :78-82; the `IS NOT DISTINCT FROM` etapa-change guard at :61 that makes the knockout path skip this trigger)
    - supabase/migrations/20260709000014_submit_candidatura_flag.sql:38-49,136,146-150,184-195 (GUC set + the knockout's OWN explicit history row + the SECURITY DEFINER footer to mirror)
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md §2b,§2d + §Open Questions 3 (leave the explicit knockout literal `true` as-is — already correct)
    - CLAUDE.md "Migrations + db push — 42601 workaround": NO outer BEGIN;…COMMIT; wrapper; apply via MCP apply_migration (BLOCKING wave 27-05)
  </read_first>
  <action>
    Author a new migration `CREATE OR REPLACE FUNCTION public.avancar_etapa()` that is byte-identical to `20260607000005` EXCEPT the `auto_rejeitado` value in the audit INSERT: replace the `(v_ator IS NULL)` literal (`:82`) with `(v_ator IS NULL AND current_setting('app.rejeicao_sancionada', true) IS NOT DISTINCT FROM 'on' AND NEW.etapa_atual = 'rejeitado')`. Preserve everything else: `SECURITY DEFINER`, `SET search_path = ''`, the etapa-change guard, the `$$ … $$` body, plus the trailing `COMMENT ON FUNCTION …` and `REVOKE ALL … FROM PUBLIC;`. NO outer `BEGIN;/COMMIT;` wrapper (D-22 42601 trap; the CLI wraps each migration itself) — add a one-line inline comment stating the no-wrapper reason and that apply is via MCP apply_migration in the BLOCKING wave. Leave the knockout path's OWN explicit `auto_rejeitado=true, ator NULL` INSERT in `submit_candidatura_atomic` untouched (it does not flow through this trigger — etapa unchanged → guard skips — and its literal is already a genuine sanctioned auto-reject).
  </action>
  <acceptance_criteria>
    - file exists; `grep -c "CREATE OR REPLACE FUNCTION public.avancar_etapa" supabase/migrations/20260712110001_avancar_etapa_auto_rejeitado_fix.sql` == 1
    - `grep -c "app.rejeicao_sancionada" supabase/migrations/20260712110001_avancar_etapa_auto_rejeitado_fix.sql` >= 1 AND `grep -c "NEW.etapa_atual = 'rejeitado'" supabase/migrations/20260712110001_avancar_etapa_auto_rejeitado_fix.sql` >= 1
    - the old bare `(v_ator IS NULL)` literal is NOT the auto_rejeitado value: `grep -v '^--' supabase/migrations/20260712110001_avancar_etapa_auto_rejeitado_fix.sql | grep -c "v_ator IS NULL AND current_setting" ` >= 1
    - `grep -ci "^begin;" supabase/migrations/20260712110001_avancar_etapa_auto_rejeitado_fix.sql` == 0 (no outer transaction wrapper)
    - `grep -c "SECURITY DEFINER" supabase/migrations/20260712110001_avancar_etapa_auto_rejeitado_fix.sql` >= 1 AND footer has COMMENT + REVOKE
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/migrations/20260712110001_avancar_etapa_auto_rejeitado_fix.sql && grep -c "app.rejeicao_sancionada" supabase/migrations/20260712110001_avancar_etapa_auto_rejeitado_fix.sql && grep -ci "^begin;" supabase/migrations/20260712110001_avancar_etapa_auto_rejeitado_fix.sql</automated>
  </verify>
  <done>Trigger migration authored with the GUC+terminal-gated predicate; SECURITY DEFINER footer mirrored; no BEGIN/COMMIT wrapper; knockout explicit literal left intact.</done>
</task>

<task type="auto">
  <name>Task 2: DBMIG-02 — one-time backfill migration (distinct file, Pitfall 4)</name>
  <files>supabase/migrations/20260712110002_backfill_auto_rejeitado.sql</files>
  <read_first>
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md §2c + §Pitfall 4 (code fix ≠ data fix; ship as two tasks/files)
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-PATTERNS.md §"DBMIG-02 backfill"
    - confirm zero client reads of auto_rejeitado: `grep -rn "auto_rejeitado" src/` should return 0 (audit-only column, safe to backfill)
  </read_first>
  <action>
    Author a SEPARATE migration containing the one-time data correction: `UPDATE public.historico_candidatura SET auto_rejeitado = false WHERE auto_rejeitado = true AND etapa_para <> 'rejeitado';`. This corrects historical survivor-advance rows the old trigger mismarked; genuine terminal-rejection rows (`etapa_para='rejeitado'`) keep `true`. Add a header comment explaining it is a one-time backfill paired with the trigger fix, that the column is audit-only with zero `src/` reads, and that apply is via MCP apply_migration in the BLOCKING wave 27-05 AFTER the trigger fix. NO BEGIN/COMMIT wrapper. Do NOT combine with Task 1's file.
  </action>
  <acceptance_criteria>
    - file exists and is distinct from Task 1's file
    - `grep -c "SET auto_rejeitado = false" supabase/migrations/20260712110002_backfill_auto_rejeitado.sql` == 1
    - `grep -c "etapa_para <> 'rejeitado'" supabase/migrations/20260712110002_backfill_auto_rejeitado.sql` == 1
    - `grep -c "CREATE OR REPLACE\|CREATE FUNCTION\|CREATE TRIGGER" supabase/migrations/20260712110002_backfill_auto_rejeitado.sql` == 0 (pure data migration, no DDL)
    - `grep -rc "auto_rejeitado" src/ | grep -vc ":0"` — confirms zero client reads (backfill is safe)
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/migrations/20260712110002_backfill_auto_rejeitado.sql && grep -c "SET auto_rejeitado = false" supabase/migrations/20260712110002_backfill_auto_rejeitado.sql && grep -rn "auto_rejeitado" src/ | wc -l</automated>
  </verify>
  <done>Backfill authored as a distinct data-only migration; corrects mismarked non-terminal rows to false; terminal rejections preserved.</done>
</task>

<task type="auto">
  <name>Task 3: DBMIG-01 (docs) — correct the stale "49 migrations" count to 71</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md:63 (DBMIG-01 text says "As 49 migrations…")
    - .planning/ROADMAP.md Phase 27 section (Goal + Success Criterion 1 say "49 migrations")
    - .planning/STATE.md Roadmap table Phase 27 row ("49 migrations reconstroem o banco")
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md §Pitfall 7 (real count = 71, verified `ls | wc -l`)
  </read_first>
  <action>
    Confirm the live count with `ls supabase/migrations/*.sql | wc -l` (currently 71). Update the "49" literal to "71" wherever it describes the migration corpus for DBMIG-01: REQUIREMENTS.md DBMIG-01 line, ROADMAP.md Phase-27 Goal + Success Criterion 1, and the STATE.md Phase-27 roadmap-table row. Add a short parenthetical noting the requirement text was written 2026-07-05 before P25/26 added migrations. Do NOT alter any other requirement text, and do NOT touch the ROADMAP plan-list section (the planner/orchestrator owns that during planning).
  </action>
  <acceptance_criteria>
    - `ls supabase/migrations/*.sql | wc -l` == 71 (confirms the correct number)
    - `grep -c "49 migrations" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` == 0 for the DBMIG-01 descriptions (stale count gone)
    - `grep -c "71 migrations" .planning/REQUIREMENTS.md` >= 1 (corrected)
    - no other requirement IDs' text changed: `git diff .planning/REQUIREMENTS.md` touches only the DBMIG-01 line
  </acceptance_criteria>
  <verify>
    <automated>ls supabase/migrations/*.sql | wc -l; grep -c "71 migrations" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>The "49"→71 count is corrected in REQUIREMENTS/ROADMAP/STATE; no other text changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| system write vs sanctioned auto-reject | the trigger must distinguish an audit "system wrote this" from "the system auto-rejected this candidate" (RNF-07a) |
| historical audit rows | backfill mutates the audit table; must not delete or over-correct terminal rejections |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04-01 | Elevation | DBMIG-02 predicate accidentally creates a 2nd auto-reject path (RNF-07a) | mitigate | predicate is terminal-only + GUC-gated + system-write-only; knockout stays the sole sanctioned path; guard_rejeicao_auditada backstop untouched |
| T-27-04-02 | Tampering | backfill over-corrects genuine terminal rejections | mitigate | `WHERE etapa_para <> 'rejeitado'` preserves terminal-reject rows; data-only, no DDL |
| T-27-04-03 | Repudiation | code fix without backfill leaves the audit trail lying | mitigate | Pitfall 4 — trigger fix and backfill are two distinct files, both applied in 27-05 |
| T-27-04-SC | Tampering | package installs | accept | none this phase |
</threat_model>

<verification>
- Both migration files exist, are distinct, and carry no BEGIN/COMMIT wrapper.
- Trigger migration contains the GUC+terminal predicate; backfill is data-only.
- `grep -rn "auto_rejeitado" src/` == 0 (backfill safety confirmed).
- Migration count corrected to 71 in the 3 planning docs.
- Apply/smoke of these files happens in 27-05 (BLOCKING) — this plan is file-only.
</verification>

<success_criteria>
DBMIG-02 trigger fix + backfill authored as two files honoring RNF-07a and the D-22 no-wrapper rule; the "49"→71 count corrected. Ready for MCP apply in 27-05.
</success_criteria>

<output>
Create `.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-04-SUMMARY.md` when done.
</output>
