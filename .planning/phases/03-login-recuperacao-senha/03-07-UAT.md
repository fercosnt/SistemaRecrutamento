---
plan: 03-07
phase: 03-login-recuperacao-senha
type: uat
status: pending
started: 2026-04-25
---

# Phase 3 Plan 03-07 — UAT Runbook

> **Manual-only behaviors that automated gates cannot reliably exercise.**
> Phase 2 Plan 02-06 surfaced 3 production-only bugs in UAT that zero automated
> coverage caught (Sonner split-instance, duplicateCheck `this`-detachment,
> digest schema-qualifier). Phase 3 runs the same playbook: this runbook
> captures B5, B6, B10 real-email, B13 real-network, B14 DevTools inspection,
> and T-03-09 Dashboard re-audit.
>
> **Each section requires:** a human to follow the steps, mark the checkbox,
> paste any anomalies, then commit the runbook. After all 6 boxes are checked,
> reply `approved` to the executor — or `issues: <list>` with any UAT-discovered
> bugs (Phase 2 02-06 precedent: discovered bugs become fix commits in this
> plan or are deferred to Phase 4 per scope).

---

## UAT-1 (B6) — Lembrar-me CHECKED → session survives tab close

**Behavior ID:** B6 (AUTH-02 / D-05 / T-03-04)
**Why manual:** Playwright `context.close()` is not equivalent to a real tab
close for sessionStorage / localStorage semantics. Real browser only.

**Steps:**

1. Run `npm run dev` (Vite dev server on port 3003).
2. Open Chrome incognito → navigate to `http://localhost:3003/auth/login`.
3. Confirm "Lembrar-me" checkbox is **CHECKED** (D-05 default).
4. Sign in with a real test-seed candidato account (e.g. one of the seeded
   `test+*@beautysmile.com.br` users from Phase 2).
5. Land on `/candidato/perfil`.
6. DevTools → Application → Local Storage → confirm `sb-auth-token` exists.
7. DevTools → Application → Session Storage → confirm `sb-auth-token` does
   **NOT** exist (rememberMeStorage routed to localStorage).
8. Close the **entire** Chrome browser.
9. Reopen Chrome → navigate to `http://localhost:3003/candidato/perfil`.

**Expected:** Still authenticated, lands on `/candidato/perfil` directly
(no redirect to `/auth/login`).

- [ ] **UAT-1 verified** by ____________ on YYYY-MM-DD

<!-- Anomalies / observations:
     paste here if anything diverged from the expected flow above
-->

---

## UAT-2 (B5) — Lembrar-me UNCHECKED → session dies on tab close

**Behavior ID:** B5 (AUTH-02 / T-03-04 / T-03-10)
**Why manual:** sessionStorage tab-lifetime semantics require a real browser
tab close.

**Steps:**

1. Log out (or clear browser storage from UAT-1).
2. Chrome incognito → `/auth/login`.
3. **UNCHECK** "Lembrar-me".
4. Sign in.
5. DevTools → Session Storage has `sb-auth-token`; Local Storage does **NOT**
   (rememberMeStorage routed to sessionStorage; D-19 ORDER-LOCK ensured the
   first SDK write landed in the chosen storage).
6. Close the **tab** (or the whole incognito window — both work because
   sessionStorage is per-tab).
7. Open a new tab → navigate to `/candidato/perfil`.

**Expected:** Redirected to `/auth/login` (session dead — sessionStorage
cleared on tab close).

- [ ] **UAT-2 verified** by ____________ on YYYY-MM-DD

<!-- Anomalies / observations: -->

---

## UAT-3 (B10 real email) — End-to-end deeplink → reset → auto-login

**Behavior ID:** B10 (AUTH-03 / AUTH-04)
**Why manual:** Supabase SMTP rate-limits + real inbox required. Mocked
B10-lite covers the `useRecoverySession.status === 'valid'` branch in CI;
this UAT covers actual email deliverability + deeplink hash-fragment parsing
under a real Supabase recovery token.

**Steps:**

1. Use a real email address you control (preferably a `test+*@beautysmile.com.br`
   seed account so the row gets cleaned by Phase 2 conventions).
2. Open `/auth/esqueci-senha` → submit the email.
3. Verify the inbox receives a Supabase password-reset email within 60s.
4. Click the deeplink in the email.
5. Confirm landing on `/auth/redefinir-senha#access_token=...&type=recovery`
   (hash fragment present; URL allow-listed in Step 2 of T-03-09 Dashboard
   audit).
6. Form renders with both password fields (Nova senha + Confirmar nova senha).
7. Fill new password (min 8 chars + upper + lower + number) → submit.
8. Land on `/candidato/perfil` with toast `Senha alterada com sucesso.`

**Expected:** Email arrives < 60s; deeplink resolves to working form;
new password is accepted; redirect to `/candidato/perfil` happens immediately
(D-12 — no countdown).

- [ ] **UAT-3 verified** by ____________ on YYYY-MM-DD

<!-- Anomalies / observations:
     paste email screenshot reference + deeplink URL (redact access_token if shared in PR)
-->

---

## UAT-4 (B13 real network failure) — NETWORK_ERROR toast + retry

**Behavior ID:** B13 (AUTH-01)
**Why manual:** DevTools "Offline" mode is not equivalent to real flaky
network. Validates the NETWORK_ERROR mapping path + the "Tentar novamente"
toast action button (LoginCandidatoPage.tsx onSubmit catch).

**Steps:**

1. Open `/auth/login` and fill valid credentials (do NOT submit yet).
2. macOS: turn WiFi off via menu bar. Windows: disable network adapter.
3. Click "Entrar".
4. Confirm toast text: `Sem conexão com o servidor. Verifique sua internet.`
   with a `Tentar novamente` action button (NOT a generic SERVER_ERROR or
   unhandled-promise toast).
5. Turn WiFi back on.
6. Click `Tentar novamente` in the toast → re-submits → lands on
   `/candidato/perfil`.

**Expected:** Toast is the specific NETWORK_ERROR copy (not generic). The
action button retries and succeeds once connectivity is restored.

- [ ] **UAT-4 verified** by ____________ on YYYY-MM-DD

<!-- Anomalies / observations: -->

---

## UAT-5 (B14 password NOT in network request body or console) — DevTools inspection

**Behavior ID:** B14 (AUTH-01..04 / T-03-03)
**Why manual:** Static grep + Vitest pitfall7.grep cover console-leak paths.
DevTools inspection covers the network surface + verifies the redaction
discipline holds in actual runtime call frames.

**Steps:**

1. DevTools → Network tab, **Preserve log** ON.
2. Sign in with valid credentials.
3. Find `POST /auth/v1/token?grant_type=password`.
4. Inspect Request **Body** (Payload tab):
   - Expected: `{"email":"...","password":"..."}` — password IS in the body
     over HTTPS (Supabase needs it; this is correct).
5. Inspect Request **URL** (Headers tab):
   - Expected: password does **NOT** appear in the query string.
6. Switch to DevTools Console:
   - Find `[AUTH] signIn invoked` log.
   - Expected payload shape: `{ email, rememberMe, hasPassword: true }`.
   - **Forbidden:** any `{ password: 'plaintext' }`, `{ senha: '...' }`,
     or `password` appearing as a value (only `hasPassword: Boolean(...)` is
     allowed — the redaction idiom from Plan 03-04).
7. Trigger an invalid-credentials submit too (same flow with wrong password).
   - Inspect Console — `[AUTH] signIn error:` log should emit only
     `{ code, status }`. **Forbidden:** the error message containing `senha`
     / `password` plaintext.

**Expected:** Password is in the HTTPS body (correct) but never in URL,
query string, or console output. Console payload uses `hasPassword: true`
sentinel (per Phase 3 Pitfall 7 redaction idiom).

- [ ] **UAT-5 verified** by ____________ on YYYY-MM-DD

<!-- Anomalies / observations:
     paste Console screenshot if anything off
-->

---

## UAT-6 (T-03-09) — Supabase Dashboard re-audit

**Behavior ID:** T-03-09 (Tampering — Dashboard drift)
**Why manual:** Dashboard state lives outside the repo. Re-audit confirms
Phase 1 + Plan 03-01 settings have not silently drifted (e.g. Supabase
auto-rotated a default, an admin manually changed something, a dependency
upgrade reset values). Plan 03-01 captured the baseline; this UAT
re-confirms.

**Steps:**

1. Open `.planning/phases/03-login-recuperacao-senha/03-01-DASHBOARD-AUDIT.md`
   for the recorded baseline.
2. Supabase Dashboard → Auth → Providers → Email → confirm
   **Email OTP Expiration = `3600`** (1 hour, AUTH-03 contract).
3. Supabase Dashboard → Auth → URL Configuration → Redirect URLs → confirm
   the allow-list still contains BOTH:
   - `http://localhost:3003/auth/redefinir-senha`
   - `http://localhost:3003/auth/redefinir-senha?tipo=rh`
4. (Optional sanity check) Decode any fresh access_token at <https://jwt.io>
   → confirm `app_metadata.role` is still emitted (Phase 1 Custom Access
   Token Hook still healthy).

**Expected:** All three values match the baseline in 03-01-DASHBOARD-AUDIT.md.
If anything drifted, document below + raise an `issues:` reply to the executor
so a follow-up Dashboard audit + commit can land.

- [ ] **UAT-6 verified** by ____________ on YYYY-MM-DD
- One-liner confirmation:
      `Re-audited YYYY-MM-DD — unchanged since Plan 03-01 (or: drift detected — see notes)`

<!-- Anomalies / observations:
     paste Dashboard screenshots if anything drifted
-->

---

## Sign-off

Once all 6 boxes are checked AND any UAT-discovered bugs documented,
commit this file:

```bash
git add .planning/phases/03-login-recuperacao-senha/03-07-UAT.md
git commit -m "test(03-07-uat): manual UAT runbook complete — 6 behaviors verified"
```

Then reply to the executor with:
- `approved` — all 6 sections passed; no production-only bugs surfaced
- `issues: <list>` — describe any UAT-discovered bugs (Phase 2 02-06 pattern:
  bug found in UAT becomes a fix commit in this plan, OR is deferred to
  Phase 4 per scope and tracked in deferred-items.md)

**Consolidated checklist (mirrors the per-section boxes above):**

- [ ] UAT-1 (B6) Lembrar-me CHECKED → survives browser close
- [ ] UAT-2 (B5) Lembrar-me UNCHECKED → dies on tab close
- [ ] UAT-3 (B10 real email) → deeplink → form → redirect /candidato/perfil
- [ ] UAT-4 (B13 real network) → NETWORK_ERROR toast + Tentar novamente retry works
- [ ] UAT-5 (B14 DevTools) → password in HTTPS body only, never in console / URL / query
- [ ] UAT-6 (T-03-09 Dashboard) → OTP=3600 + Redirect URLs unchanged

**Once all 6 boxes are checked, flip the frontmatter `status: pending` → `status: completed`.**

---

*Authored by Claude (executor) at Wave 6, plan 03-07. Tasks 1-3 (E2E
promotions + pitfall7 grep guard) already committed as f82d568 + 4257556 +
6bcda7c. This is the human-gated finale of Phase 3.*
