---
phase: 28-gest-o-de-usu-rios-rh-n-cleo-seguro
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/gerenciar-usuario-rh/__tests__/index.test.ts
  - src/__tests__/guards/no-service-role-src.grep.test.ts
autonomous: true
requirements: [SEG-01]
must_haves:
  truths:
    - "SEG-01 traceability: a Deno handler test asserts unauth->401, authenticated-non-admin->403, active-admin->dispatch, and criar orphan-rollback calls deleteUser — RED until 28-06 authors index.ts"
    - "SEG-01 client invariant: a Vitest grep-guard fails if any service_role key / privileged createClient appears under src/"
    - "The RED Deno test uses injected mock deps (no network), mirroring consolidar-decisao-final test precedent"
  artifacts:
    - path: "supabase/functions/gerenciar-usuario-rh/__tests__/index.test.ts"
      provides: "Deno handler test (401/403/dispatch/rollback/email) with injected mock two-client"
      min_lines: 120
    - path: "src/__tests__/guards/no-service-role-src.grep.test.ts"
      provides: "node:fs comment-aware guard: no service_role/privileged client under src/"
      min_lines: 60
  key_links:
    - from: "index.test.ts loadHandler()"
      to: "supabase/functions/gerenciar-usuario-rh/index.ts"
      via: "dynamic import('../index.ts') — RED (module-not-found) until 28-06"
      pattern: "import\\(\"\\.\\./index\\.ts\"\\)"
---

<objective>
Land the RED handler test + the SEG-01 client-bundle grep-guard BEFORE the EF exists (Nyquist test-first discipline). The Deno test encodes the SEG-01 authenticate-THEN-authorize contract (401/403/dispatch) plus the USR-02 orphan-rollback and USR-05 email-path expectations, using injected mock deps so it runs with no network. It is RED (dynamic import of a not-yet-authored index.ts throws) until 28-06 makes it GREEN. The grep-guard is GREEN immediately and stays green — it is the standing proof that no service_role/admin client ever leaks into the shipped client bundle (SEG-01 invariant).

Purpose: the EF's security behavior is defined by a failing test before a line of the EF is written; the client-side invariant gets a permanent regression tripwire.
Output: two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-PATTERNS.md
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-VALIDATION.md
@supabase/functions/consolidar-decisao-final/__tests__/index.test.ts
@src/__tests__/guards/rh-console.grep.test.ts

<interfaces>
<!-- Contracts the RED test asserts (index.ts + schema authored in 28-06). Executor writes assertions against these. -->
handler signature (mirror consolidar-decisao-final/index.ts:227):
  export async function handler(req: Request, deps: { supabaseAdmin, supabaseUser }): Promise<Response>
Response contract: { ok:false, error_code, message } with HTTP 401 UNAUTHORIZED / 403 FORBIDDEN / 400 VALIDATION | SERVER_ERROR ; { ok:true, ... } on success.
Authorize rule the test drives: role read from usuarios_rh (mock .from('usuarios_rh').select('role').eq('user_id').eq('ativo',true).is('deleted_at',null).maybeSingle()); ONLY role==='administrador' dispatches.
criar rollback: when the injected rpc('criar_usuario_rh_com_audit') mock errors, handler MUST call supabaseAdmin.auth.admin.deleteUser(userId) (no orphan GoTrue user).
email path: on criar success handler calls supabaseAdmin.auth.resetPasswordForEmail(email, { redirectTo: <origin>/auth/redefinir-senha?tipo=rh }); a thrown send error must NOT fail the request (surfaces EMAIL_SEND_FAILED warning).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED Deno handler test for gerenciar-usuario-rh</name>
  <read_first>
    - supabase/functions/consolidar-decisao-final/__tests__/index.test.ts (lines 29, 142-158 loadHandler/makeRequest; 70-140 makeMockSupabaseAdmin/User; 393-430 the four authorize cases — the exact idiom to clone)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-PATTERNS.md (section: gerenciar-usuario-rh/__tests__/index.test.ts)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-VALIDATION.md (Per-Requirement map rows SEG-01, USR-02, USR-05)
  </read_first>
  <action>
    Create supabase/functions/gerenciar-usuario-rh/__tests__/index.test.ts mirroring the consolidar test: std assert import, a loadHandler() doing a dynamic import('../index.ts'), a makeRequest(body) builder with Authorization: Bearer, a chainable makeMockSupabaseAdmin exposing from('usuarios_rh').select().eq().eq().is().maybeSingle() (for the role lookup) plus auth.admin.createUser / auth.admin.deleteUser / auth.resetPasswordForEmail / rpc(), and a makeMockSupabaseUser(user|null) exposing auth.getUser(). Encode these cases: (1) unauthenticated getUser->null => 401 UNAUTHORIZED; (2) authenticated but usuarios_rh role is null or 'recrutador' => 403 FORBIDDEN (note the deviation from the analog: do NOT accept a normalized 'rh' role — administrador ONLY); (3) active administrador + a valid action body => dispatch (2xx / ok:true, not 401/403); (4) criar orphan-rollback: force the rpc('criar_usuario_rh_com_audit') mock to return an error and assert auth.admin.deleteUser was invoked with the created userId; (5) email path: on criar success assert resetPasswordForEmail was called with redirectTo ending /auth/redefinir-senha?tipo=rh, and that a thrown send error yields a non-fatal response (request still succeeds / EMAIL_SEND_FAILED surfaced, not a 500). The suite is EXPECTED RED now (index.ts absent -> import throws) — that is the intended failing state per the consolidar precedent comment.
  </action>
  <verify>
    <automated>test -f supabase/functions/gerenciar-usuario-rh/__tests__/index.test.ts && grep -q "loadHandler" supabase/functions/gerenciar-usuario-rh/__tests__/index.test.ts && grep -q "deleteUser" supabase/functions/gerenciar-usuario-rh/__tests__/index.test.ts && grep -q "resetPasswordForEmail" supabase/functions/gerenciar-usuario-rh/__tests__/index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test file asserts all five cases (401, 403 for non-admin/recrutador, admin dispatch, criar deleteUser rollback, resetPasswordForEmail redirectTo + non-fatal send failure).
    - Role authorization is asserted from usuarios_rh (mock), NOT from getUser().app_metadata; the 'rh' normalization is absent (administrador-only).
    - `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/gerenciar-usuario-rh` FAILS with a module-not-found on '../index.ts' (RED confirmed).
  </acceptance_criteria>
  <done>The SEG-01 handler contract is captured as a failing Deno test, ready for 28-06 to satisfy.</done>
</task>

<task type="auto">
  <name>Task 2: SEG-01 grep-guard — no service_role/privileged client under src/</name>
  <read_first>
    - src/__tests__/guards/rh-console.grep.test.ts (lines 32-94 collectFiles + isCommentLine walk; 118-137 positive/negative regex + files.length floor — clone the idiom)
    - src/__tests__/guards/n8n-bundle.grep.test.ts (the optional build/ artifact leg, skipped when build/ absent)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-PATTERNS.md (section: no-service-role-src.grep.test.ts)
  </read_first>
  <action>
    Create src/__tests__/guards/no-service-role-src.grep.test.ts using the node:fs comment-aware walk from rh-console.grep.test.ts: import { existsSync, readFileSync, readdirSync, statSync } from 'node:fs'; ROOT = resolve(__dirname, '../../..'); recursive collectFiles() that skips __tests__/node_modules and only reads .ts/.tsx under src/; isCommentLine() strip so a doc-comment mentioning the token cannot self-trip; collect violations[] with file:line context and throw if non-empty. Forbidden patterns (SEG-01): SUPABASE_SERVICE_ROLE_KEY, the bare token service_role, and a createClient(...) construction passing a service-role/admin key. Add positive/negative regex unit assertions and a sanity files.length floor. This guard MUST be GREEN now (no service_role currently exists under src/) and stay green. Do NOT scan supabase/functions (server-side service_role is legitimate there).
  </action>
  <verify>
    <automated>npm run test:run -- src/__tests__/guards/no-service-role-src.grep.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The guard passes today (0 violations under src/).
    - A synthetic positive line (e.g. `const x = 'service_role'`) added in a scratch fixture would be flagged (proven by the in-file positive regex unit assertion).
    - The comment-aware strip prevents a doc-comment mention from tripping the guard.
  </acceptance_criteria>
  <done>A permanent SEG-01 client-bundle tripwire exists and is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client bundle -> shipped JS | a service_role key or admin client compiled into src/ would hand full DB power to any browser (SEG-01 violation) |
| unauthenticated/authenticated caller -> EF | the handler must reject 401/403 before any privileged read/write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Elevation of Privilege | gerenciar-usuario-rh handler | mitigate | RED Deno test pins 401/403/administrador-only dispatch before impl (28-06 satisfies it) |
| T-28-01b | Elevation of Privilege | src/ client bundle | mitigate | node:fs grep-guard fails CI if any service_role/privileged client lands under src/ |
| T-28-06 | Tampering/Integrity | criar orphan GoTrue user | mitigate | test asserts deleteUser rollback when the row+audit rpc errors |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | no new packages (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- Deno test file exists and is RED (module-not-found on ../index.ts) — the intended pre-impl state.
- Grep-guard is green (npm run test:run) with 0 service_role violations under src/.
</verification>

<success_criteria>
The SEG-01 handler contract is a failing test and the client-bundle invariant is a passing guard — both landed before the EF is authored.
</success_criteria>

<output>
Create `.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-02-SUMMARY.md` when done.
</output>
