---
phase: 28-gest-o-de-usu-rios-rh-n-cleo-seguro
plan: 06
type: execute
wave: 2
depends_on: ["28-02"]
files_modified:
  - supabase/functions/_shared/usuario-rh-schemas.ts
  - supabase/functions/gerenciar-usuario-rh/index.ts
autonomous: true
requirements: [SEG-01, USR-06]
must_haves:
  truths:
    - "SEG-01: the EF authenticates (anon client getUser()) THEN authorizes by reading role from usuarios_rh via service_role — 401 unauth, 403 non-admin, BEFORE any write; role is administrador-only (no recrutador->rh normalization)"
    - "SEG-01: a Zod .strict() discriminated-union validates action (criar/mudar_papel/ativar/desativar/resetar_senha); papel enum-constrained to {recrutador,administrador}; unknown key -> VALIDATION/400"
    - "USR-02/06: criar does createUser(temp random pw, email_confirm:true) then criar_usuario_rh_com_audit RPC; on rpc failure it calls deleteUser (no orphan GoTrue user)"
    - "USR-05: set-password/reset uses resetPasswordForEmail(redirectTo /auth/redefinir-senha?tipo=rh); send failure is non-fatal EMAIL_SEND_FAILED"
    - "The DB-only actions call gerir_usuario_rh_mutacao; SQLSTATE P0001 maps to error_code LAST_ADMIN, P0002 to NOT_FOUND"
  artifacts:
    - path: "supabase/functions/_shared/usuario-rh-schemas.ts"
      provides: "Zod discriminated-union schema + UsuarioRhErrorCode union + zodPathToFieldName"
      exports: ["gerenciarUsuarioRhSchema", "GerenciarUsuarioRhInput"]
    - path: "supabase/functions/gerenciar-usuario-rh/index.ts"
      provides: "handler(req, deps) two-client authenticate-THEN-authorize + action dispatch + Deno.serve wiring"
      exports: ["handler"]
      min_lines: 150
  key_links:
    - from: "gerenciar-usuario-rh/index.ts authorize"
      to: "usuarios_rh.role read via service_role"
      via: ".from('usuarios_rh').select('role').eq('user_id', user.id).eq('ativo', true).is('deleted_at', null).maybeSingle()"
      pattern: "role !== \"administrador\""
    - from: "criar dispatch"
      to: "criar_usuario_rh_com_audit RPC + deleteUser rollback"
      via: "rpc error -> auth.admin.deleteUser(userId) compensating rollback"
      pattern: "deleteUser"
---

<objective>
Author the single privileged Edge Function gerenciar-usuario-rh and its shared Zod schema — the ONE authenticate-THEN-authorize entrypoint for every RH-account write (SEG-01). It makes the RED Deno test from 28-02 GREEN. The handler clones the consolidar-decisao-final two-client skeleton (anon getUser() to authenticate, service_role read of usuarios_rh to authorize) but tightens the authorization to administrador-only (dropping the recrutador->rh normalization), validates the body with a .strict() discriminated union, and dispatches: criar (createUser + criar_usuario_rh_com_audit RPC + compensating deleteUser + resetPasswordForEmail) / mudar_papel|ativar|desativar (gerir_usuario_rh_mutacao RPC) / resetar_senha (resetPasswordForEmail).

Purpose: the only writer of usuarios_rh.role is this server-side, admin-gated EF — the fact that P30 verifies SEG-03 against.
Output: two files; the Deno test goes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-PATTERNS.md
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-RESEARCH.md
@supabase/functions/consolidar-decisao-final/index.ts
@supabase/functions/cadastrar-candidato/index.ts
@supabase/functions/_shared/schemas.ts

<interfaces>
<!-- RPC contracts authored in 28-05 (called by name via .rpc()). Deno test mocks these. -->
rpc('criar_usuario_rh_com_audit', { p_actor, p_user_id, p_email, p_nome, p_cargo, p_papel }) -> new usuarios_rh id
rpc('gerir_usuario_rh_mutacao', { p_actor, p_target, p_action, p_novo_papel }) -> void (fires anti-lockout trigger; P0001/P0002)
error-code union (from RESEARCH): "UNAUTHORIZED" | "FORBIDDEN" | "VALIDATION" | "NOT_FOUND" | "EMAIL_EXISTS" | "LAST_ADMIN" | "EMAIL_SEND_FAILED" | "SERVER_ERROR"
Zod bare import: `import { z } from "zod"` (deno.json import map -> npm:zod@3.25.76; resolves in Deno AND Vitest — CI-07).
handler signature (mirror consolidar-decisao-final/index.ts:227): export async function handler(req, { supabaseAdmin, supabaseUser }): Promise<Response>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: _shared/usuario-rh-schemas.ts (strict discriminated union + error codes)</name>
  <read_first>
    - supabase/functions/_shared/schemas.ts (lines 28 bare-zod import; 118-147 .strict() allowlist; 159-198 error-code union + zodPathToFieldName; 204-235 submitCandidaturaSchema .uuid()/.strict() style)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-PATTERNS.md (section: _shared/usuario-rh-schemas.ts)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-RESEARCH.md (Code Examples: Zod discriminated-union; Pitfall 8 NOT NULL + role CHECK)
  </read_first>
  <action>
    Create supabase/functions/_shared/usuario-rh-schemas.ts. Bare import { z } from "zod". const papel = z.enum(["recrutador","administrador"]) — NOT the legacy 4-value DB CHECK ('gerente'/'visualizador' excluded). export const gerenciarUsuarioRhSchema = z.discriminatedUnion("action", [ criar {action:literal, email: z.string().email().toLowerCase().trim(), nome_completo: z.string().min(3).max(255), cargo: z.string().min(1).max(100), papel}, mudar_papel {action, target_id: z.string().uuid(), novo_papel: papel}, ativar {action, target_id: uuid}, desativar {action, target_id: uuid}, resetar_senha {action, target_id: uuid} ]) — every object .strict() (fail-closed: unknown key -> Zod failure -> EF maps to VALIDATION/400 before any write). export type GerenciarUsuarioRhInput = z.infer<...>. export the UsuarioRhErrorCode union: "UNAUTHORIZED" | "FORBIDDEN" | "VALIDATION" | "NOT_FOUND" | "EMAIL_EXISTS" | "LAST_ADMIN" | "EMAIL_SEND_FAILED" | "SERVER_ERROR". Reuse the zodPathToFieldName helper pattern for the structured error field slot. nome_completo + cargo are required on criar because usuarios_rh has them NOT NULL (Pitfall 8).
  </action>
  <verify>
    <automated>deno check --config supabase/functions/deno.json supabase/functions/_shared/usuario-rh-schemas.ts</automated>
  </verify>
  <acceptance_criteria>
    - gerenciarUsuarioRhSchema is a discriminatedUnion on "action" with all five actions; every object is .strict().
    - papel/novo_papel enum = {recrutador, administrador} exactly (no legacy values).
    - criar requires email + nome_completo + cargo + papel; the error-code union is exported.
    - deno check passes on the module.
  </acceptance_criteria>
  <done>The request contract + error vocabulary exist for the handler to import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gerenciar-usuario-rh/index.ts (authenticate-THEN-authorize + dispatch)</name>
  <behavior>
    - Unauthenticated (getUser -> null) => 401 { ok:false, error_code:"UNAUTHORIZED" }.
    - Authenticated but usuarios_rh role is null / 'recrutador' => 403 FORBIDDEN (administrador-only; no 'rh' normalization).
    - Active administrador + valid body => dispatch (2xx / ok:true).
    - Unknown/invalid body key => 400 VALIDATION (before any write).
    - criar: createUser(temp pw, email_confirm:true) then criar_usuario_rh_com_audit; rpc error => auth.admin.deleteUser(userId) called (no orphan), returns SERVER_ERROR; success => resetPasswordForEmail(redirectTo .../auth/redefinir-senha?tipo=rh) best-effort, a thrown send error yields EMAIL_SEND_FAILED (non-fatal, request still ok).
    - mudar_papel/ativar/desativar: gerir_usuario_rh_mutacao rpc; SQLSTATE P0001 => LAST_ADMIN (409/400), P0002 => NOT_FOUND (404).
    - resetar_senha: resetPasswordForEmail; send failure => EMAIL_SEND_FAILED.
  </behavior>
  <read_first>
    - supabase/functions/consolidar-decisao-final/index.ts (lines 45-74 imports/CORS/error helpers; 216-231 testable handler(req,deps) + injectable deps; 233-261 authenticate-THEN-authorize skeleton; 263-274 safeParse->VALIDATION; 426-458 Deno.serve two-client wiring)
    - supabase/functions/cadastrar-candidato/index.ts (lines 149-235 createUser + compensating deleteUser rollback idiom, esp. 224-226 the .catch on deleteUser)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-PATTERNS.md (section: gerenciar-usuario-rh/index.ts — the verbatim skeleton + the deviation note: administrador-only)
    - supabase/functions/gerenciar-usuario-rh/__tests__/index.test.ts (the RED test this must satisfy)
  </read_first>
  <action>
    Create supabase/functions/gerenciar-usuario-rh/index.ts. Export async function handler(req, { supabaseAdmin, supabaseUser }) and a Deno.serve production wiring block (copy consolidar 426-458: OPTIONS short-circuit, env-var guard, anon client carrying the caller Authorization header, service_role client with autoRefreshToken:false/persistSession:false). Handler flow: (1) authenticate — supabaseUser.auth.getUser(); no user => errorResponse("UNAUTHORIZED", ..., 401). (2) authorize — supabaseAdmin.from('usuarios_rh').select('role').eq('user_id', user.id).eq('ativo', true).is('deleted_at', null).maybeSingle(); if role !== 'administrador' => errorResponse("FORBIDDEN", ..., 403). CRITICAL deviation from the analog: do NOT copy consolidar's dbRole==='recrutador'?'rh':... normalization — require administrador exactly, and read role from the TABLE, never getUser().app_metadata. (3) parse — gerenciarUsuarioRhSchema.safeParse(await req.json()); on failure => errorResponse("VALIDATION", ..., 400). (4) dispatch on body.action: criar => createUser({ email, password: crypto.randomUUID()+crypto.randomUUID(), email_confirm:true, user_metadata:{nome_completo} }); on 'already registered' => EMAIL_EXISTS(409); then rpc('criar_usuario_rh_com_audit', {...}); on rpc error => await supabaseAdmin.auth.admin.deleteUser(userId).catch(logRollbackErr) then SERVER_ERROR; on success => resetPasswordForEmail(email, { redirectTo: `${origin}/auth/redefinir-senha?tipo=rh` }).catch(() => surface EMAIL_SEND_FAILED warning, do NOT roll back). mudar_papel/ativar/desativar => optional non-authoritative admin pre-count for a friendly early LAST_ADMIN, then rpc('gerir_usuario_rh_mutacao', { p_actor:user.id, p_target:target_id, p_action:action, p_novo_papel:novo_papel ?? null }); map SQLSTATE/message P0001 => LAST_ADMIN, P0002 => NOT_FOUND, else SERVER_ERROR. resetar_senha => resetPasswordForEmail(target email looked up by target_id via service_role, redirectTo the same recovery page); send failure => EMAIL_SEND_FAILED. Never log passwords/OTP/tokens. All responses use the { ok, error_code, message, field? } contract. Then run the Deno test to GREEN.
  </action>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/gerenciar-usuario-rh</automated>
  </verify>
  <acceptance_criteria>
    - The 28-02 Deno test passes GREEN (401 / 403-non-admin / admin-dispatch / criar deleteUser rollback / resetPasswordForEmail redirectTo + non-fatal send failure).
    - Authorization reads role from usuarios_rh (service_role) and requires administrador exactly; no 'rh' normalization; role never read from getUser().app_metadata.
    - criar compensates with deleteUser on rpc failure; P0001->LAST_ADMIN and P0002->NOT_FOUND mapping present.
    - `npm run lint` (tsc) does not inflate the frozen baseline.
  </acceptance_criteria>
  <done>The SEG-01 write-path EF exists, is admin-gated, and the Deno test is GREEN; P29 can invoke it for USR-01..05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated/authenticated caller -> EF | must be rejected 401/403 before any privileged read/write |
| EF -> GoTrue admin API | createUser/deleteUser/resetPasswordForEmail run with service_role; temp password never returned to client |
| EF -> usuarios_rh write path | role written only here, only for an authorized active admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Elevation of Privilege | non-admin invokes EF | mitigate | authenticate (getUser) THEN authorize from usuarios_rh (active admin) before any action; administrador-only |
| T-28-06 | Tampering/Integrity | orphan GoTrue identity | mitigate | createUser -> row+audit rpc -> compensating deleteUser on rpc failure |
| T-28-07 | Information Disclosure | recovery-link hijack / email enumeration | mitigate | reuse existing OTP recovery flow; never log email/OTP/token; send failure is a non-fatal warning |
| T-28-08 | Tampering | SQL injection via role/free-text | mitigate | Zod .strict() discriminated union; papel enum-constrained; RPC args parameterized |
| T-28-04 | Denial of Service | last-admin lockout | mitigate | P0001 from the trigger mapped to a friendly LAST_ADMIN; optional EF pre-count for UX |
| T-28-SC | Tampering | esm.sh/npm imports | accept | reuses pinned @supabase/supabase-js@2 + npm:zod@3.25.76; no new installs (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- deno check on the schema; deno test on the handler GREEN.
- npm run lint not above the frozen tsc baseline.
- No service_role construction leaks into src/ (guarded by 28-02; this code lives under supabase/functions/, legitimate).
</verification>

<success_criteria>
The single admin-gated EF is the only server-side writer of RH accounts; the RED handler test is GREEN.
</success_criteria>

<output>
Create `.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-06-SUMMARY.md` when done.
</output>
