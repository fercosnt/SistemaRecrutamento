---
phase: 28-gest-o-de-usu-rios-rh-n-cleo-seguro
plan: 04
type: execute
wave: 2
depends_on: ["28-01", "28-03"]
files_modified:
  - supabase/migrations/20260713000001_usr_rh_rls_seg02.sql
  - supabase/migrations/20260713000004_logs_auditoria_append_only.sql
autonomous: true
requirements: [SEG-02, USR-06]
must_haves:
  truths:
    - "SEG-02: the two live qual=true SELECT policies (per 28-LIVE-STATE 'Policies to DROP') are dropped; a recrutador/candidato reads 0 roster rows"
    - "SEG-02: admin-only SELECT is enforced via a LANGUAGE plpgsql SECURITY DEFINER helper is_active_rh_admin() (never LANGUAGE sql -> no recursion trap)"
    - "SEG-02: a mandatory own-row SELECT policy (user_id = auth.uid()) preserves recrutador login (authStore.fetchProfile reads own row)"
    - "SEG-02: auth_admin_le_usuarios_rh (SEC-09) is left untouched; custom_access_token_hook is NOT re-created"
    - "USR-06: the logs_auditoria authenticated INSERT policy is dropped so only the DEFINER path writes; no UPDATE/DELETE policy exists (append-only)"
  artifacts:
    - path: "supabase/migrations/20260713000001_usr_rh_rls_seg02.sql"
      provides: "DROP leaks + is_active_rh_admin() DEFINER helper + admin-only SELECT + own-row SELECT"
      contains: "is_active_rh_admin"
    - path: "supabase/migrations/20260713000004_logs_auditoria_append_only.sql"
      provides: "DROP 'Sistema insere logs' INSERT policy + REVOKE INSERT/UPDATE/DELETE from authenticated/anon"
      contains: "logs_auditoria"
  key_links:
    - from: "usuarios_rh_admin_select policy"
      to: "public.is_active_rh_admin()"
      via: "USING (public.is_active_rh_admin()) — DEFINER helper bypasses RLS, no recursion"
      pattern: "USING \\(public.is_active_rh_admin\\(\\)\\)"
    - from: "authStore.fetchProfile own-row read"
      to: "usuarios_rh_own_select policy"
      via: "USING (user_id = (select auth.uid())) keeps recrutador login working"
      pattern: "user_id = \\(select auth.uid\\(\\)\\)"
---

<objective>
Author the two RLS-hardening migrations that deliver SEG-02 (roster is admin-only) and the append-only half of USR-06 (nobody but the DEFINER path writes logs_auditoria). This is leak REMOVAL, not guard addition: the live usuarios_rh has two permissive qual=true SELECT policies that let ANY authenticated candidato/recrutador read the entire RH roster. The migration drops the exact names captured in 28-LIVE-STATE.md, adds a recursion-safe admin-only SELECT (via a PL/pgSQL DEFINER helper) plus the mandatory own-row SELECT, and denies all client writes. The logs_auditoria migration drops the forgeable authenticated INSERT policy.

Purpose: close the standing PII leak and make the audit trail append-only, without breaking the auth hook (SEC-09) or recrutador login (own-row read).
Output: two migration files (authored only; applied in 28-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-LIVE-STATE.md
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-PATTERNS.md
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-RESEARCH.md
@supabase/migrations/20260706110006_sec09_auth_admin_policy.sql
@src/store/authStore.ts

<interfaces>
<!-- Load-bearing contracts. Author the SQL against 28-LIVE-STATE.md's REAL names, not assumptions. -->
is_active_rh_admin(): RETURNS boolean LANGUAGE plpgsql STABLE SECURITY DEFINER SET search_path=public;
  body: SELECT EXISTS(SELECT 1 FROM usuarios_rh WHERE user_id=auth.uid() AND role='administrador' AND ativo AND deleted_at IS NULL);
  REVOKE EXECUTE FROM public; GRANT EXECUTE TO authenticated.
DROP (per 28-LIVE-STATE 'Policies to DROP'): usuarios_rh_authenticated_read, usuarios_rh_simple_read, + any other permissive client SELECT/write policy captured.
CREATE usuarios_rh_admin_select FOR SELECT TO authenticated USING (public.is_active_rh_admin());
CREATE usuarios_rh_own_select   FOR SELECT TO authenticated USING (user_id = (select auth.uid()));  -- MANDATORY
PRESERVE untouched: auth_admin_le_usuarios_rh (SEC-09); do NOT CREATE OR REPLACE custom_access_token_hook.
logs_auditoria: DROP POLICY "Sistema insere logs"; REVOKE INSERT,UPDATE,DELETE ON logs_auditoria FROM authenticated, anon; keep "Admin vê logs" SELECT; NO FORCE ROW LEVEL SECURITY.
authStore.fetchProfile (src/store/authStore.ts:164-170): .from('usuarios_rh').select('*').eq('user_id', userId).eq('ativo', true).is('deleted_at', null).single()  -- breaks without own-row policy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: usr_rh_rls_seg02 migration (drop leaks + DEFINER helper + admin/own-row SELECT)</name>
  <read_first>
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-LIVE-STATE.md ('Policies to DROP' / 'Policies to PRESERVE' — the REAL names)
    - supabase/migrations/20260706110006_sec09_auth_admin_policy.sql (lines 40-56: NO BEGIN/COMMIT wrapper + idempotent DROP POLICY IF EXISTS / CREATE POLICY idiom)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-RESEARCH.md (Code Examples: recursion-safe admin RLS; Pitfall 1 recursion, Pitfall 2 leak-removal)
    - src/store/authStore.ts (lines 158-225: fetchProfile own-row read — why the own-row policy is mandatory)
  </read_first>
  <action>
    Create supabase/migrations/20260713000001_usr_rh_rls_seg02.sql. NO outer BEGIN/COMMIT wrapper (driver wraps each migration; a wrapper trips 42601). Steps: (1) CREATE OR REPLACE FUNCTION public.is_active_rh_admin() RETURNS boolean LANGUAGE plpgsql STABLE SECURITY DEFINER SET search_path = public — body does SELECT EXISTS(...) over usuarios_rh for auth.uid() with role='administrador' AND ativo AND deleted_at IS NULL; then REVOKE EXECUTE FROM public and GRANT EXECUTE TO authenticated. It MUST be LANGUAGE plpgsql (never sql — Postgres inlines simple sql functions and loses the DEFINER context, re-introducing the recursion trap). (2) DROP POLICY IF EXISTS for every permissive client SELECT policy listed in 28-LIVE-STATE 'Policies to DROP' (at minimum usuarios_rh_authenticated_read + usuarios_rh_simple_read; also drop any client INSERT/UPDATE/DELETE policy found). (3) CREATE POLICY usuarios_rh_admin_select FOR SELECT TO authenticated USING (public.is_active_rh_admin()). (4) CREATE POLICY usuarios_rh_own_select FOR SELECT TO authenticated USING (user_id = (select auth.uid())) — mandatory, keeps recrutador login working. Add NO INSERT/UPDATE/DELETE policy (all client writes denied; the service_role EF/RPC bypass RLS). Do NOT enable FORCE ROW LEVEL SECURITY. Do NOT touch auth_admin_le_usuarios_rh or custom_access_token_hook. Add a COMMENT ON POLICY documenting SEG-02. Use idempotent DROP POLICY IF EXISTS / CREATE POLICY per the SEC-09 analog.
  </action>
  <verify>
    <automated>f=supabase/migrations/20260713000001_usr_rh_rls_seg02.sql; test -f "$f" && grep -q "LANGUAGE plpgsql" "$f" && grep -q "is_active_rh_admin" "$f" && grep -q "usuarios_rh_own_select" "$f" && grep -q "DROP POLICY IF EXISTS usuarios_rh_authenticated_read" "$f" && ! grep -qi "^BEGIN;" "$f" && ! grep -qi "custom_access_token_hook" "$f" && ! grep -qi "LANGUAGE sql" "$f"</automated>
  </verify>
  <acceptance_criteria>
    - is_active_rh_admin() is LANGUAGE plpgsql SECURITY DEFINER with REVOKE public / GRANT authenticated; NOT LANGUAGE sql.
    - Every permissive client SELECT policy from 28-LIVE-STATE is dropped; usuarios_rh_admin_select + usuarios_rh_own_select created.
    - No INSERT/UPDATE/DELETE policy for authenticated/anon; no FORCE ROW LEVEL SECURITY.
    - auth_admin_le_usuarios_rh and custom_access_token_hook are not referenced/altered; no outer BEGIN/COMMIT wrapper.
  </acceptance_criteria>
  <done>The SEG-02 RLS rewrite is authored, reconciled to real live names, RED until applied in 28-07 and smoked in 28-08.</done>
</task>

<task type="auto">
  <name>Task 2: logs_auditoria_append_only migration (drop forgeable INSERT + REVOKE)</name>
  <read_first>
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-LIVE-STATE.md (logs_auditoria policy capture + owner/BYPASSRLS verdict A4)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-PATTERNS.md (section: logs_auditoria_append_only.sql — the exact live "Sistema insere logs" policy to DROP + "Admin vê logs" to KEEP)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-RESEARCH.md (Pitfall 5 append-only; do NOT enable FORCE RLS)
  </read_first>
  <action>
    Create supabase/migrations/20260713000004_logs_auditoria_append_only.sql. NO BEGIN/COMMIT wrapper. Steps: (1) DROP POLICY IF EXISTS "Sistema insere logs" ON public.logs_auditoria (the authenticated WITH CHECK(true) INSERT policy that lets any authenticated user forge audit rows). (2) REVOKE INSERT, UPDATE, DELETE ON public.logs_auditoria FROM authenticated, anon (defense in depth). Keep the "Admin vê logs" SELECT policy intact. Do NOT add any UPDATE/DELETE policy (append-only). Do NOT enable FORCE ROW LEVEL SECURITY (it would subject the DEFINER log_auditoria() INSERT — owner postgres — to RLS and break the audit write; 28-LIVE-STATE A4 confirms the owner is BYPASSRLS). Add a COMMENT documenting USR-06 append-only. If 28-LIVE-STATE flagged any live UPDATE/DELETE policy on logs_auditoria, drop it here too.
  </action>
  <verify>
    <automated>f=supabase/migrations/20260713000004_logs_auditoria_append_only.sql; test -f "$f" && grep -q "Sistema insere logs" "$f" && grep -qi "REVOKE INSERT" "$f" && ! grep -qi "FORCE ROW LEVEL SECURITY" "$f" && ! grep -qi "^BEGIN;" "$f"</automated>
  </verify>
  <acceptance_criteria>
    - The authenticated INSERT policy "Sistema insere logs" is dropped; INSERT/UPDATE/DELETE revoked from authenticated + anon.
    - No UPDATE/DELETE policy is added; FORCE ROW LEVEL SECURITY is not enabled.
    - The "Admin vê logs" SELECT policy is preserved.
  </acceptance_criteria>
  <done>logs_auditoria is append-only (DEFINER-write-only), RED until applied in 28-07 and smoked in 28-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated candidato/recrutador -> usuarios_rh roster | must read 0 rows except own row; the live qual=true policies violate this |
| authenticated user -> logs_auditoria INSERT | any authenticated user can currently forge audit rows via "Sistema insere logs" |
| policy evaluation -> usuarios_rh self-reference | a subquery-on-self admin policy causes infinite recursion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | Information Disclosure | usuarios_rh SELECT policies | mitigate | DROP the two qual=true leaks; admin-only SELECT via is_active_rh_admin() DEFINER helper + own-row only |
| T-28-03 | Elevation of Privilege | usuarios_rh client writes | mitigate | No client INSERT/UPDATE/DELETE policy -> EF service_role is the ONLY writer of role (SEG-03 verified in P30 against this fact) |
| T-28-05 | Repudiation/Tampering | logs_auditoria INSERT | mitigate | DROP "Sistema insere logs"; REVOKE INSERT/UPDATE/DELETE; DEFINER-only writes, no FORCE RLS |
| T-28-09 | Tampering | custom_access_token_hook / SEC-09 policy | mitigate | Migration does not reference the hook and does not touch auth_admin_le_usuarios_rh (verified by grep gate) |
| T-28-08 | Tampering | RLS recursion via LANGUAGE sql | mitigate | Helper is LANGUAGE plpgsql (grep gate forbids LANGUAGE sql) — not inlined, keeps DEFINER context |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | no new packages (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- Both migration files exist, have no BEGIN/COMMIT wrapper, and pass the grep gates above.
- The RLS migration reconciles the REAL policy names from 28-LIVE-STATE.md (not the assumed pair only, if extras were found).
- Behavioral proof (0-row roster read, denied audit INSERT) is deferred to 28-08 smokes on PROD.
</verification>

<success_criteria>
SEG-02 roster is admin-only + own-row (recursion-safe, SEC-09 preserved) and logs_auditoria is append-only — authored and ready to apply.
</success_criteria>

<output>
Create `.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-04-SUMMARY.md` when done.
</output>
