---
phase: 28-gest-o-de-usu-rios-rh-n-cleo-seguro
plan: 08
type: execute
wave: 4
depends_on: ["28-07"]
files_modified:
  - supabase/tests/usr_rh_seg02_smoke.sql
  - supabase/tests/usr_rh_anti_lockout_smoke.sql
  - supabase/tests/usr_rh_audit_append_only_smoke.sql
autonomous: false
requirements: [SEG-01, SEG-02, USR-06, USR-07]
must_haves:
  truths:
    - "SEG-02 GREEN: candidato + recrutador impersonated roster reads return 0 rows on PROD; admin reads full roster; recrutador own-row read returns 1; auth_admin_le_usuarios_rh present USING true"
    - "USR-07 GREEN: demote/deactivate/delete of the last active admin raises P0001 with 0 mutation; the 2-session concurrency case leaves >=1 admin"
    - "USR-06 GREEN: one categoria='usuario' audit row per DB-only mutation, rollback-together on forced failure; candidato/recrutador INSERT + admin UPDATE/DELETE on logs_auditoria all denied"
    - "SEG-01 GREEN: the full Vitest suite (incl. the no-service-role-src grep-guard) + the Deno handler test pass; tsc not above baseline"
  artifacts: []
  key_links:
    - from: "28-03 smoke files"
      to: "PROD objects applied in 28-07"
      via: "run each smoke via mcp execute_sql; assert every DO block RAISE NOTICE 'PASS' and 0 FAIL"
      pattern: "PASS"
---

<objective>
Run the three behavioral SQL smokes (authored RED in 28-03) against PROD now that 28-07 applied the objects, and run the full local suite — the load-bearing verification gate for the phase. The SQL smokes are what actually prove SEG-02/USR-06/USR-07 (a structural pg_policies grep passes while a real leak persists — M4/SEC-07/08). autonomous:false — the smokes run against PROD via MCP execute_sql and (for the anti-lockout single-admin case) must be run carefully so the real e2e.admin account is never left demoted/deactivated.

Purpose: convert every phase requirement from "authored" to "proven on PROD"; surface any migration defect for a targeted fix before /gsd:verify-work and /gsd-secure-phase.
Output: recorded PASS/FAIL for all smokes + green local suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-VALIDATION.md
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-PATTERNS.md
@supabase/tests/usr_rh_seg02_smoke.sql
@supabase/tests/usr_rh_anti_lockout_smoke.sql
@supabase/tests/usr_rh_audit_append_only_smoke.sql

<interfaces>
<!-- Gate = every smoke PASS + full suite green. -->
SQL smokes (run via mcp__supabase__execute_sql): usr_rh_seg02_smoke, usr_rh_anti_lockout_smoke, usr_rh_audit_append_only_smoke -> each DO block RAISE NOTICE 'PASS ...', 0 RAISE EXCEPTION '... FAIL'.
Concurrency proof (USR-07): two parallel MCP execute_sql sessions each attempting to demote a distinct admin of a 2-admin disposable fixture -> exactly one succeeds, >=1 admin remains (advisory-lock proof).
Local suite: npm run test:run (Vitest incl. no-service-role-src grep-guard) ; deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/gerenciar-usuario-rh ; npm run lint (tsc, not above baseline).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Run SEG-02 + USR-06 + USR-07 smokes on PROD</name>
  <read_first>
    - supabase/tests/usr_rh_seg02_smoke.sql, usr_rh_anti_lockout_smoke.sql, usr_rh_audit_append_only_smoke.sql (the assertions to execute)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-VALIDATION.md (Per-Requirement map — the exact behaviors each smoke must prove)
    - reference_candidatura_test_data_reset (disposable-fixture + cleanup discipline; never delete a real row)
  </read_first>
  <action>
    Execute each smoke file's SQL against PROD via mcp__supabase__execute_sql and record the RAISE NOTICE output. Confirm every DO block reports PASS and none RAISEs a FAIL. For usr_rh_anti_lockout_smoke, run the 2-session concurrency proof by issuing two parallel execute_sql demotions against a disposable 2-admin fixture and assert exactly one succeeds with >=1 admin remaining. CRITICAL: the smokes use disposable fixed-UUID fixtures — verify the real e2e.admin@beautysmile.com.br row is NEVER left demoted, deactivated, or deleted after the run (its role='administrador', ativo=true, deleted_at IS NULL must hold at the end). Clean up only the disposable fixture + disposable audit rows. If any smoke FAILs, capture the failure, do a targeted fix to the offending migration file, re-apply via 28-07's MCP path, and re-run — do not mask a FAIL.
  </action>
  <verify>
    <automated>MISSING — PROD smokes via MCP execute_sql; evidence is the PASS output recorded in the SUMMARY. Post-run integrity SQL: SELECT role, ativo, deleted_at FROM usuarios_rh WHERE email='e2e.admin@beautysmile.com.br' must show administrador/true/null.</automated>
  </verify>
  <acceptance_criteria>
    - SEG-02: candidato + recrutador roster reads = 0 rows; admin full read; recrutador own-row = 1; auth_admin_le_usuarios_rh present USING true — all PASS.
    - USR-07: demote/deactivate/delete of last active admin each raise P0001 with 0 mutation; concurrency leaves >=1 admin — all PASS.
    - USR-06: one categoria='usuario' audit row per DB-only mutation + rollback-together; non-admin INSERT and admin UPDATE/DELETE on logs_auditoria denied — all PASS.
    - The real e2e.admin row is intact (administrador/active/not-deleted) after the run; only disposable rows removed.
  </acceptance_criteria>
  <done>SEG-02, USR-06, and USR-07 are proven on PROD with the roster/last-admin/append-only invariants intact.</done>
</task>

<task type="auto">
  <name>Task 2: Full local suite + tsc gate</name>
  <read_first>
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-VALIDATION.md (Sampling Rate: phase gate = full suite green + lint not above baseline)
    - src/__tests__/guards/no-service-role-src.grep.test.ts (SEG-01 client-bundle guard must be green)
  </read_first>
  <action>
    Run npm run test:run (full Vitest suite including the no-service-role-src grep-guard for SEG-01), then the Deno handler test (deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/gerenciar-usuario-rh), then npm run lint (tsc --noEmit). All Vitest + Deno tests must pass; the grep-guard confirms no service_role/privileged client under src/; tsc must not exceed the frozen baseline (301). Record the counts.
  </action>
  <verify>
    <automated>npm run test:run && deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/gerenciar-usuario-rh</automated>
  </verify>
  <acceptance_criteria>
    - Full Vitest suite green (incl. the SEG-01 grep-guard); Deno handler test green.
    - npm run lint does not inflate the frozen tsc baseline.
  </acceptance_criteria>
  <done>The whole test net is green over the applied phase — ready for /gsd:verify-work and /gsd-secure-phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored assertion -> proven behavior | structural checks pass while a real leak persists; only the row-projection smoke proves SEG-02 |
| smoke fixture -> real admin account | a careless anti-lockout smoke could leave e2e.admin demoted/deactivated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | Information Disclosure | usuarios_rh roster | mitigate | SEG-02 smoke asserts 0-row candidato/recrutador reads on PROD (row projection, not pg_policies) |
| T-28-04 | Denial of Service | last-admin lockout | mitigate | USR-07 smoke asserts P0001 + concurrency; post-run integrity check keeps e2e.admin active |
| T-28-05 | Repudiation/Tampering | audit forge/mutate | mitigate | USR-06 smoke asserts append-only denial + atomic mutate+audit on PROD |
| T-28-01b | Elevation of Privilege | src/ client bundle | mitigate | full-suite run includes the no-service-role-src grep-guard (SEG-01) |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | no new packages (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- All three SQL smokes report PASS, 0 FAIL, on PROD; e2e.admin intact.
- Full Vitest + Deno suites green; tsc not above baseline.
</verification>

<success_criteria>
Every Phase-28 requirement (SEG-01, SEG-02, USR-06, USR-07) is proven by a behavioral check on the applied objects; the phase is ready for /gsd:verify-work and /gsd-secure-phase.
</success_criteria>

<output>
Create `.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-08-SUMMARY.md` when done.
</output>
