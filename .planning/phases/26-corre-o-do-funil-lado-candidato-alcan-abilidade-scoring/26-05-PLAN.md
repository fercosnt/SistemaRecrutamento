---
phase: 26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring
plan: 05
type: execute
wave: 2
depends_on: ['26-01', '26-02']
files_modified:
  - src/features/avaliacao/services/avaliacaoService.ts
  - src/features/avaliacao/__tests__/avaliacaoService.funil.test.ts
autonomous: true
requirements: [FUNIL-07, FUNIL-12, FUNIL-01, FUNIL-08]
must_haves:
  truths:
    - "FUNIL-07 (client, D-Question-Filter): getAvaliacaoContext filters the perguntas query by the SJT element's itens_ids when present, else by cargo — the candidate never sees another cargo's SJT questions in the UI (server-side membership check in pontuar_sjt is the security teeth, unchanged)."
    - "FUNIL-12 (client, D-Card-Status): a new getAvaliacaoStatus(candidaturaId) service reads the neutral get_avaliacao_status RPC via a NARROW confined cast (RPC not yet in database.types.ts — regen is Phase 27), returning per-test presence booleans only; never a raw score."
    - "FUNIL-08 (client half): getAvaliacaoContext adds aplica_cognitivo to the vaga join allowlist + AvaliacaoContext so the container (26-06) can gate the cognitivo card on vaga.aplica_cognitivo === true."
    - "FUNIL-01 (client half): pontuarSjt maps the new RAISEs (42501 foreign/re-submit, 22023 dup/incomplete) to NEUTRAL AvaliacaoServiceError codes (LOCKED / DATABASE_ERROR) — no score/threshold/gabarito ever surfaced to the candidate (RNF-07a)."
    - "The candidate read stays a strict allowlist projection — never select('*') (avaliacaoService invariant #1); the score/gabarito are hidden columns RLS does not protect."
  artifacts:
    - path: "src/features/avaliacao/services/avaliacaoService.ts"
      provides: "battery-filtered getAvaliacaoContext + aplica_cognitivo join + getAvaliacaoStatus RPC call + pontuarSjt error mapping"
      exports: ["getAvaliacaoContext", "getAvaliacaoStatus", "pontuarSjt"]
    - path: "src/features/avaliacao/__tests__/avaliacaoService.funil.test.ts"
      provides: "unit tests: battery filter (.in vs .eq) + pontuarSjt neutral error mapping"
      contains: "getAvaliacaoContext"
  key_links:
    - from: "src/features/avaliacao/services/avaliacaoService.ts"
      to: "supabase.from('perguntas')"
      via: ".in('id', itensIds) when present else .eq('cargo', cargo)"
      pattern: "\\.in\\('id'"
    - from: "src/features/avaliacao/services/avaliacaoService.ts"
      to: "get_avaliacao_status RPC"
      via: "narrow confined cast (supabase.rpc as unknown as ...)"
      pattern: "get_avaliacao_status"
---

<objective>
Wire the candidate SJT service to the corrected DB tier: filter the perguntas query by the vaga's SJT battery (FUNIL-07 presentation), add `aplica_cognitivo` to the avaliação context (FUNIL-08 client half consumed by the container), add a `getAvaliacaoStatus` reader over the neutral status RPC (FUNIL-12 client half), and map `pontuarSjt`'s new RAISEs to neutral errors (FUNIL-01 client half). Plus unit tests for the filter + error mapping.

Purpose: The service is the single candidate data boundary; these deltas make the container's card state + cognitivo card real while keeping every projection allowlist-only and every candidate-facing error neutral.
Output: avaliacaoService.ts edits + a new unit test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-CONTEXT.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md

<interfaces>
<!-- getAvaliacaoContext (avaliacaoService.ts:98-166): currently joins `vaga:vagas ( testes_aplicaveis )` and queries perguntas with only `.eq('status','active')`. -->
<!--   Delta A (FUNIL-07): after resolving testesAplicaveis, find the element with tipo==='sjt', read itens_ids (string[]) + cargo; build the perguntas query then `.in('id', itensIds)` when itensIds.length>0 else `.eq('cargo', cargo)`. Keep allowlist columns `id, cargo, cenario, formato, tempo_est_min, status`. (26-RESEARCH §Code Example 2.) -->
<!--   Delta B (FUNIL-08): extend the join to `vaga:vagas ( testes_aplicaveis, aplica_cognitivo )`, add aplica_cognitivo to the candRow type + AvaliacaoContext interface, and return it. -->
<!-- Delta C (FUNIL-12): NARROW confined cast idiom = cognitivoService.ts:162-169 (widen ONLY the RPC name, never a blanket UntypedClient; add a "// Drop the cast after the Phase-27 regen" note). RPC name 'get_avaliacao_status', arg { p_candidatura_id }. Return a narrowed interface of per-test booleans. -->
<!-- Delta D (FUNIL-01): pontuarSjt error block (avaliacaoService.ts:274-295) already maps 42501/403 → LOCKED. Add a neutral branch for 22023 (dup/incomplete) → DATABASE_ERROR (or LOCKED); keep messages NEUTRAL (no score). Preserve the AvaliacaoServiceError code union (:36-44). -->
<!-- Keep the two SJT paths separate: caso_aberto goes to avaliar-redacao (EF), never through pontuarSjt (Pitfall 2/Open Q4). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Battery filter + aplica_cognitivo join + getAvaliacaoStatus + pontuarSjt error mapping</name>
  <files>src/features/avaliacao/services/avaliacaoService.ts</files>
  <read_first>
    - src/features/avaliacao/services/avaliacaoService.ts (:98-166 getAvaliacaoContext, :180-201 getOpcoesSjt, :261-300 pontuarSjt, :36-44 error code union, :1-9 allowlist invariant)
    - src/features/avaliacao-cognitiva/services/cognitivoService.ts (:158-172 the NARROW confined cast idiom to mirror)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md §Code Example 2 + §Component Responsibilities (avaliacaoService row) + Pitfall 4
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md §avaliacaoService (Deltas A/B/C)
  </read_first>
  <behavior>
    - getAvaliacaoContext filters perguntas by itens_ids when the SJT element has them (asserts `.in('id', itensIds)` called), else by cargo (asserts `.eq('cargo', cargo)`), else unfiltered active only.
    - getAvaliacaoContext returns aplica_cognitivo from the vaga join (boolean, default false when absent).
    - getAvaliacaoStatus('id') calls the 'get_avaliacao_status' RPC and returns its per-test booleans; a RPC error throws a neutral AvaliacaoServiceError.
    - pontuarSjt maps a 42501 error → LOCKED and a 22023 error → a neutral code (DATABASE_ERROR/LOCKED) with NO score/threshold in the message.
  </behavior>
  <action>
    Apply Deltas A–D from the interfaces block to avaliacaoService.ts: (A) FUNIL-07 battery filter in getAvaliacaoContext — resolve the tipo==='sjt' element's itens_ids/cargo, build the perguntas query then `.in('id', itensIds)` when present else `.eq('cargo', cargo)`, keeping the exact allowlist columns (never select('*')); (B) FUNIL-08 add `aplica_cognitivo` to the `vaga:vagas ( ... )` join, the candRow type, the AvaliacaoContext interface, and the returned object (default false); (C) FUNIL-12 add an exported `getAvaliacaoStatus(candidaturaId)` calling the `get_avaliacao_status` RPC via the narrow confined cast (mirror cognitivoService.listItens) returning a narrowed booleans interface + a "// Drop the cast after the Phase-27 regen" note; (D) FUNIL-01 extend the pontuarSjt catch/error-mapping to map 22023 (dup/incomplete) and keep 42501 (foreign/re-submit) → neutral AvaliacaoServiceError codes with neutral pt-BR messages. Preserve RNF-07a: nothing here writes candidaturas; no score/gabarito surfaces. Do NOT edit database.types.ts.
  </action>
  <acceptance_criteria>
    - `grep -c "aplica_cognitivo" src/features/avaliacao/services/avaliacaoService.ts` >= 3 (join + type + return).
    - `grep -Ec "\\.in\\('id'|\\.eq\\('cargo'" avaliacaoService.ts` >= 2 (battery filter both branches).
    - `grep -c "get_avaliacao_status" avaliacaoService.ts` >= 1 AND the narrow cast note present (`grep -c "Drop the cast" avaliacaoService.ts` >= 1).
    - No `select('*')` introduced (`grep -c "select('\\*')" avaliacaoService.ts` == 0).
    - `npm run lint` (tsc) not above 107.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "cd '/Users/fernando/Cursor Repo/DB Sistema de recrutamento' && f=src/features/avaliacao/services/avaliacaoService.ts && test \$(grep -c 'aplica_cognitivo' \$f) -ge 3 && test \$(grep -Ec \"\\.in\\('id'|\\.eq\\('cargo'\" \$f) -ge 2 && grep -c 'get_avaliacao_status' \$f && test \$(grep -c \"select('\\*')\" \$f) -eq 0 && echo OK"</automated>
  </verify>
  <done>avaliacaoService filters the SJT battery, exposes aplica_cognitivo + getAvaliacaoStatus, and maps pontuarSjt's new RAISEs to neutral errors — all allowlist-only, tsc ≤ 107.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests — battery filter + neutral pontuarSjt error mapping</name>
  <files>src/features/avaliacao/__tests__/avaliacaoService.funil.test.ts</files>
  <read_first>
    - src/features/avaliacao/__tests__/avaliacaoService.rubric.test.ts (the supabase-mock idiom used for avaliacaoService unit tests)
    - src/features/avaliacao/services/avaliacaoService.ts (the just-edited functions)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md §Validation Test Map (FUNIL-07 client + FUNIL-01 client rows)
  </read_first>
  <behavior>
    - Given a testes_aplicaveis SJT element with itens_ids=[a,b], getAvaliacaoContext calls perguntas.in('id', [a,b]) and NOT .eq('cargo', …).
    - Given an SJT element with cargo but no itens_ids, getAvaliacaoContext calls .eq('cargo', <cargo>).
    - pontuarSjt: a mocked RPC error with code 42501 throws AvaliacaoServiceError code LOCKED; a 22023 error throws a neutral code; neither error message contains a number/score/threshold.
  </behavior>
  <action>
    Create `avaliacaoService.funil.test.ts` mirroring the supabase-mock idiom in `avaliacaoService.rubric.test.ts`. Cover the three behaviors above: assert the perguntas query builder receives `.in('id', itensIds)` when itens_ids present and `.eq('cargo', cargo)` otherwise; assert pontuarSjt maps 42501 → LOCKED and 22023 → a neutral code, and assert (regex) the thrown message contains no digit sequence resembling a score/percentage. Keep the tests deterministic (mock supabase; no network).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/features/avaliacao/__tests__/avaliacaoService.funil.test.ts` passes with >= 3 assertions covering filter-in / filter-cargo / error-mapping.
    - The error-mapping test asserts no numeric score leaks in the neutral message.
    - `npm run test:run` full suite green; `npm run lint` (tsc) not above 107.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "cd '/Users/fernando/Cursor Repo/DB Sistema de recrutamento' && npx vitest run src/features/avaliacao/__tests__/avaliacaoService.funil.test.ts && echo OK"</automated>
  </verify>
  <done>Unit tests prove the battery filter routing and the neutral error mapping; full suite + tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate browser → getAvaliacaoContext / getAvaliacaoStatus | Reads must project allowlist columns only and never surface a verdict |
| candidate browser → pontuarSjt error surface | Error messages must be neutral (no score/threshold echoed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05-01 | Information Disclosure | select('*') leaks hidden gabarito/score columns | mitigate | keep the strict allowlist projection; acceptance criterion greps for select('*')==0 |
| T-26-05-02 | Information Disclosure | pontuarSjt error message leaks score/threshold | mitigate | map RAISEs to NEUTRAL codes/messages; unit test asserts no numeric leak |
| T-26-05-03 | Tampering (defense-in-depth) | client-only battery filter bypassed | accept | client filter is UX only; the server membership check in pontuar_sjt (26-01) is the control (Pitfall 4) |
| T-26-05-04 | Tampering | blanket UntypedClient cast widens the surface | mitigate | narrow confined cast widens only the RPC name (mirror cognitivoService), dropped at Phase-27 regen |
| T-26-05-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase; N/A |
</threat_model>

<verification>
- Acceptance criteria pass; `npm run test:run` green; tsc ≤ 107.
- Runtime correctness of the RPCs is proven by the smokes in 26-07 (this plan mocks them).
</verification>

<success_criteria>
avaliacaoService battery-filters the SJT questions, exposes aplica_cognitivo + a neutral status reader, and maps pontuarSjt's new errors neutrally — with unit tests green and every candidate projection allowlist-only.
</success_criteria>

<output>
Create `.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-05-SUMMARY.md` when done.
</output>
