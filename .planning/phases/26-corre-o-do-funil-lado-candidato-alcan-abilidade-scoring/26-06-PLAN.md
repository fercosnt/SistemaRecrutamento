---
phase: 26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring
plan: 06
type: execute
wave: 3
depends_on: ['26-02', '26-04', '26-05']
files_modified:
  - src/features/avaliacao/components/AvaliacaoContainer.tsx
  - src/features/avaliacao/components/__tests__/AvaliacaoContainer.test.tsx
  - src/features/avaliacao/__tests__/cognitivo-contract.test.ts
autonomous: true
requirements: [FUNIL-08, FUNIL-12]
must_haves:
  truths:
    - "FUNIL-08 (client, D-Cognitive-Reachability Option A): AvaliacaoContainer renders a cognitivo card LAST, gated by vaga.aplica_cognitivo === true — deriveCards SKIPs the template-driven cognitivo entry (cargoTemplates.baseTestes always emits it) so the gated append is the ONLY cognitivo card (zero when false, exactly one when true; no duplicate/ungated card), routing to the REAL route /candidato/prova-cognitiva/:candidaturaId — replacing the stub /candidato/avaliacao/:id/cognitivo in CONTAINER_TESTE_CONFIG.cognitivo; testeContract.ts is NOT modified (Phase-25 FUNIL-05 guard)."
    - "FUNIL-12 (client, D-Card-Status): EVERY card's completion state (all five: sjt_mc, sjt_caso_aberto, redacao, big_five, cognitivo) is derived from the candidate's own rows via the neutral get_avaliacao_status booleans (registrado→Concluído, iniciado→Em andamento, else Pendente) — the phantom `entry.status` read at AvaliacaoContainer:312 is removed for ALL cards (no residual entry.status read for sjt_caso_aberto)."
    - "A route↔gate contract test asserts the etapa in which the cognitivo card renders (avaliacao_assincrona) equals an etapa the pontuar_cognitivo RPC accepts — importing the REAL container config, not a replica (integration-contract-gap lesson)."
    - "RNF-07a + neutral UI preserved: no card ever shows a pass/fail/red state; Concluído is the only accent touchpoint; a below-threshold completed test still reads Concluído (26-UI-SPEC Card State Contract). The all-done copy fixed in 26-04 (:209) is NOT reverted."
  artifacts:
    - path: "src/features/avaliacao/components/AvaliacaoContainer.tsx"
      provides: "cognitivo card gate + real route + card-state-from-status-RPC (phantom removed)"
      contains: "prova-cognitiva"
    - path: "src/features/avaliacao/__tests__/cognitivo-contract.test.ts"
      provides: "route↔gate contract test (card etapa == RPC-accepted etapa)"
      contains: "avaliacao_assincrona"
    - path: "src/features/avaliacao/components/__tests__/AvaliacaoContainer.test.tsx"
      provides: "cognitivo card gate/route + card-state-from-RPC component assertions"
      contains: "aplica_cognitivo"
  key_links:
    - from: "src/features/avaliacao/components/AvaliacaoContainer.tsx"
      to: "getAvaliacaoStatus (avaliacaoService)"
      via: "TanStack Query → per-test booleans → derived card state"
      pattern: "getAvaliacaoStatus"
    - from: "src/features/avaliacao/components/AvaliacaoContainer.tsx"
      to: "/candidato/prova-cognitiva/:candidaturaId"
      via: "CONTAINER_TESTE_CONFIG.cognitivo.route"
      pattern: "prova-cognitiva"
---

<objective>
Make the cognitive assessment reachable in the candidate assessment hub (FUNIL-08) and derive every card's completion state from the candidate's own rows via the neutral status RPC (FUNIL-12), removing the phantom `entry.status` read. Add a route↔gate contract test + extend the container component test.

Purpose: The cognitivo card must render (gated by `vaga.aplica_cognitivo`) and route to the real prova-cognitiva screen — a screen the `pontuar_cognitivo` gate now accepts (26-02) — and the four-state neutral card contract must reflect real progress, not a config field that does not exist.
Output: AvaliacaoContainer.tsx edits + extended component test + new contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-CONTEXT.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-UI-SPEC.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md

<interfaces>
<!-- CONTAINER_TESTE_CONFIG.cognitivo (AvaliacaoContainer.tsx:88-93): { label:'Avaliação cognitiva', route:(id)=>`/candidato/avaliacao/${id}/cognitivo` }. -->
<!--   Delta A: change route to (id)=>`/candidato/prova-cognitiva/${id}` (real route verified routes.tsx:269). Keep the label. -->
<!-- deriveCards (AvaliacaoContainer.tsx:303-326): drops non-CANDIDATE_FACING, reads phantom `const status = String(entry.status ?? 'pendente')` at :312. -->
<!--   Delta B (BLOCKER 1): cargoTemplates.baseTestes() UNCONDITIONALLY emits {teste:'cognitivo'} for all 8 cargos, and testeContract.ts CANDIDATE_FACING includes 'cognitivo' with TEMPLATE_TO_CONTAINER.cognitivo=['cognitivo'] — so the MAIN entry loop ALREADY emits a cognitivo card for EVERY vaga today, ungated. Therefore: (1) inside the main entry loop `continue` (SKIP) when templateTeste === 'cognitivo' so the template-driven emission is suppressed; (2) AFTER the loop, append a cognitivo card LAST ONLY when ctx.aplica_cognitivo === true (source of truth = the vaga column added to AvaliacaoContext in 26-05; NOT cargoTemplates.ts:71). Net: ZERO cognitivo cards when aplica_cognitivo=false, exactly ONE (the gated append) when true — no duplicate React key, no ungated card. Do NOT edit src/lib/testes/testeContract.ts — the Phase-25 FUNIL-05 guard src/lib/testes/__tests__/testeContract.test.ts asserts templateTesteToContainerCards('cognitivo')===['cognitivo']; the skip lives in deriveCards ONLY. -->
<!--   Delta C (BLOCKER 2): replace the phantom `entry.status` with derivation from get_avaliacao_status booleans (via avaliacaoService.getAvaliacaoStatus, added in 26-05). ALL FIVE cards derive from the status RPC — Map container card id → RPC key 1:1: sjt_mc→sjt_mc, sjt_caso_aberto→sjt_caso_aberto, redacao→redacao, big_five→big_five, cognitivo→cognitivo. NO residual entry.status read for any card (the "keep existing behavior for sjt_caso_aberto" idea is dropped — the RPC now returns an sjt_caso_aberto key). registrado→'concluido'; iniciado→'em_andamento'; else 'pendente'. Feed statusInfo() (AvaliacaoContainer.tsx:116-127) unchanged. -->
<!-- Query keys: container uses inline ['avaliacao','context',candidaturaId] (:348). Add a sibling status query ['avaliacao','status',candidaturaId] consistently (or follow candidaturasKeys factory useCandidaturas.ts:55-74). -->
<!-- Card State Contract + Cognitivo card table: 26-UI-SPEC §Card State Contract (four states, accent only on Concluído, cognitivo LAST, min-h-[44px] CTA). -->
<!-- Contract-test analog: src/lib/testes/testeContract.ts + AvaliacaoContainer.tsx:98-104 CONTAINER_RECOGNIZED (single-source contract). Import the REAL config. -->
<!-- The all-done copy at :209 was already corrected in 26-04 — do NOT revert it. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cognitivo card + real route + card-state-from-status-RPC (phantom removed)</name>
  <files>src/features/avaliacao/components/AvaliacaoContainer.tsx</files>
  <read_first>
    - src/features/avaliacao/components/AvaliacaoContainer.tsx (:66-94 CONTAINER_TESTE_CONFIG + cognitivo stub, :98-104 CONTAINER_RECOGNIZED, :116-127 statusInfo, :147-148 allDone, :303-326 deriveCards + phantom :312, :348 inline query key)
    - src/features/avaliacao/services/avaliacaoService.ts (getAvaliacaoContext now returns aplica_cognitivo; getAvaliacaoStatus reader — both from 26-05)
    - src/router/routes.tsx (:269-275 the real /candidato/prova-cognitiva/:candidaturaId route)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md §Code Example 4 + Pitfall 6/8
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-UI-SPEC.md §Card State Contract + §Cognitivo card
  </read_first>
  <behavior>
    - CONTAINER_TESTE_CONFIG.cognitivo.route('X') === '/candidato/prova-cognitiva/X'.
    - The main deriveCards loop SKIPS the template-driven cognitivo entry (cargoTemplates.baseTestes always emits it); the ONLY cognitivo card is the gated append. With a testes_aplicaveis that INCLUDES a cognitivo template entry (mirroring real cargoTemplates): ZERO cognitivo cards when aplica_cognitivo=false, exactly ONE (appended last) when aplica_cognitivo===true.
    - Card state comes from the status RPC booleans for ALL FIVE cards (sjt_mc, sjt_caso_aberto, redacao, big_five, cognitivo): registrado→Concluído, iniciado→Em andamento, neither→Pendente; no read of entry.status remains for any card.
    - allDone still counts a card done only when its derived state is Concluído.
  </behavior>
  <action>
    Apply Deltas A–C from the interfaces block to AvaliacaoContainer.tsx: (A) fix CONTAINER_TESTE_CONFIG.cognitivo.route to `(id) => \`/candidato/prova-cognitiva/${id}\`` (keep the 'Avaliação cognitiva' label); (B) in deriveCards, `continue` (SKIP) the main-loop entry when `templateTeste === 'cognitivo'` (cargoTemplates.baseTestes always emits it — BLOCKER 1), then in the connected container fetch the neutral status via a sibling TanStack Query calling `getAvaliacaoStatus(candidaturaId)` (key `['avaliacao','status',candidaturaId]`, enabled on candidaturaId), and append a cognitivo card LAST ONLY when `ctx.aplica_cognitivo === true` (26-UI-SPEC ordering) — so the cognitivo card is emitted exactly once (gated), never twice, never ungated; do NOT edit testeContract.ts (Phase-25 FUNIL-05 guard), the skip lives in deriveCards only; (C) replace the phantom `String(entry.status ?? 'pendente')` at :312 with a derivation that maps EACH of the five container card ids (sjt_mc, sjt_caso_aberto, redacao, big_five, cognitivo) → its status-RPC key 1:1 and computes 'concluido' | 'em_andamento' | 'pendente' from the booleans, feeding the existing neutral `statusInfo()` unchanged — NO entry.status read remains for any card (including sjt_caso_aberto). Keep the four-state neutral contract (accent only on Concluído; never a red pass/fail); a below-threshold completed test still reads Concluído. Do NOT touch the :209 all-done copy (fixed in 26-04). Keep tsc ≤ 107.
  </action>
  <acceptance_criteria>
    - Real route wired: `grep -c "prova-cognitiva" src/features/avaliacao/components/AvaliacaoContainer.tsx` >= 1 AND the stub is gone (`grep -c "avaliacao/\\${id}/cognitivo" file` == 0).
    - Phantom removed: `grep -c "entry.status" file` == 0.
    - Gate on the vaga column: `grep -c "aplica_cognitivo" file` >= 1; status reader wired: `grep -c "getAvaliacaoStatus" file` >= 1.
    - Template cognitivo skip present (BLOCKER 1 — prevents the ungated/duplicate template emission): deriveCards `continue`s on `templateTeste === 'cognitivo'`, proven by the component test (below) asserting ZERO cognitivo cards when aplica_cognitivo=false even though testes_aplicaveis includes a cognitivo entry; testeContract.ts is NOT modified (`git diff --quiet -- src/lib/testes/testeContract.ts`).
    - The :209 canonical copy is intact: `grep -c "Acompanhe o andamento pelo seu painel" file` >= 1.
    - `npm run lint` (tsc) not above 107.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "cd '/Users/fernando/Cursor Repo/DB Sistema de recrutamento' && f=src/features/avaliacao/components/AvaliacaoContainer.tsx && grep -c 'prova-cognitiva' \$f && test \$(grep -c 'entry.status' \$f) -eq 0 && grep -c 'getAvaliacaoStatus' \$f && grep -c 'Acompanhe o andamento pelo seu painel' \$f && echo OK"</automated>
  </verify>
  <done>The cognitivo card renders (gated by aplica_cognitivo) to the real route, card state derives from the neutral status booleans, the phantom read is gone, and the 26-04 copy is preserved; tsc ≤ 107.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route↔gate contract test + extend the container component test</name>
  <files>src/features/avaliacao/__tests__/cognitivo-contract.test.ts, src/features/avaliacao/components/__tests__/AvaliacaoContainer.test.tsx</files>
  <read_first>
    - src/lib/testes/__tests__/testeContract.test.ts (the FUNIL-05 single-source contract idiom — asserts against the REAL exported set, not a replica)
    - src/features/avaliacao/components/__tests__/AvaliacaoContainer.test.tsx (existing container tests — extend, do not rewrite)
    - src/features/avaliacao/components/AvaliacaoContainer.tsx (CONTAINER_TESTE_CONFIG + CONTAINER_RECOGNIZED exports)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md §Pattern 3 (single-source contract) + §cognitivo-contract analog
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md §cognitivo-contract.test.ts
  </read_first>
  <behavior>
    - Contract: the etapa the cognitivo card is shown in (avaliacao_assincrona) is a member of the pontuar_cognitivo accepted etapa set — encoded as a shared constant/assertion so a future edit to either side breaks the test.
    - Component (BLOCKER 1): using a mock `testes_aplicaveis` that INCLUDES a cognitivo entry (mirroring real cargoTemplates.baseTestes), assert EXACTLY ZERO 'Avaliação cognitiva' cards when aplica_cognitivo=false, and EXACTLY ONE when aplica_cognitivo===true (so the template-driven emission is proven suppressed and only the gated append renders); the one card's CTA routes to /candidato/prova-cognitiva/:id.
    - Component: a card with registrado=true renders Concluído; iniciado=true renders Em andamento; neither renders Pendente (from mocked status booleans).
  </behavior>
  <action>
    Create `cognitivo-contract.test.ts` mirroring the FUNIL-05 contract test: define/import the cognitivo card's render etapa (`avaliacao_assincrona`) and the `pontuar_cognitivo` accepted-etapa set (the same three literals used in 26-02) and assert membership — importing the REAL container config/constant, not a copy ([[feedback_integration_contract_gap]]). Extend `AvaliacaoContainer.test.tsx` (presentational + mocked getAvaliacaoStatus) to assert, using a mock `testes_aplicaveis` that INCLUDES a cognitivo template entry (mirroring real cargoTemplates.baseTestes so the test catches a template-driven duplicate/ungated card): EXACTLY ZERO cognitivo cards when aplica_cognitivo=false and EXACTLY ONE when true; the card's CTA routes to the real prova-cognitiva path; and the three derived states (Concluído/Em andamento/Pendente) render from mocked status booleans. Keep tests deterministic (mock the service/query; no network).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/features/avaliacao/__tests__/cognitivo-contract.test.ts` passes and asserts `avaliacao_assincrona` ∈ the pontuar_cognitivo accepted set against a shared/imported source.
    - `npx vitest run src/features/avaliacao/components/__tests__/AvaliacaoContainer.test.tsx` passes with new assertions: with a cognitivo-including testes_aplicaveis, ZERO cognitivo cards when aplica_cognitivo=false and exactly ONE when true (BLOCKER 1 — no template duplicate); real-route CTA; and the three derived card states.
    - `npm run test:run` full suite green; `npm run lint` (tsc) not above 107.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "cd '/Users/fernando/Cursor Repo/DB Sistema de recrutamento' && npx vitest run src/features/avaliacao/__tests__/cognitivo-contract.test.ts src/features/avaliacao/components/__tests__/AvaliacaoContainer.test.tsx && echo OK"</automated>
  </verify>
  <done>The contract test locks route↔gate consistency and the component test proves the cognitivo card gate/route + the three neutral card states; full suite + tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| status RPC data → card rendering | The container must render only presence-based state, never a raw score/verdict |
| card CTA → route | The cognitivo card must route only to an etapa the RPC accepts (no dead/42501 route) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06-01 | Information Disclosure | container renders a raw score/pass-fail to the candidate | mitigate | derive state only from neutral booleans; no card shows red/failed; a below-threshold test still reads Concluído (RNF-07a) |
| T-26-06-02 | Integrity/UX | cognitivo card routes to a screen the RPC will 42501 | mitigate | route↔gate contract test asserts card etapa ∈ RPC-accepted set against the real config |
| T-26-06-03 | Integrity | card shows Concluído from a phantom/config field | mitigate | derive from the candidate's own rows via get_avaliacao_status; phantom entry.status read removed |
| T-26-06-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase (no new shadcn primitive — 26-UI-SPEC Registry Safety); N/A |
</threat_model>

<verification>
- Acceptance criteria pass; `npm run test:run` green; tsc ≤ 107.
- The RPC behaviors the UI consumes are proven live by the smokes in 26-07 (this plan mocks them).
</verification>

<success_criteria>
The cognitivo card is reachable (gated by aplica_cognitivo → real route), card state derives from the candidate's own rows via the neutral status RPC (phantom removed), and both the contract and component tests are green — with the neutral RNF-07a card language preserved and the 26-04 copy intact.
</success_criteria>

<output>
Create `.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-06-SUMMARY.md` when done.
</output>
