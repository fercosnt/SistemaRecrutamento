---
phase: 26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260712100004_n8n_novo_candidato.sql
  - supabase/tests/n8n_novo_candidato_smoke.sql
  - src/features/cadastro/services/n8nService.ts
  - src/features/cadastro/services/index.ts
  - src/features/cadastro/services/n8nService.test.ts
  - src/__tests__/guards/n8n-bundle.grep.test.ts
autonomous: true
requirements: [SEC-03]
must_haves:
  truths:
    - "n8n 2nd-leak fix (D-n8n-ServerSide, routed pós-P24, SEC-03 pattern): candidate PII (nome/email/telefone/cpf) and the n8n host 'n8n.srv881294.hstgr.cloud' no longer ship in the public bundle — the whole n8nService.ts subtree + its barrel re-export + its test are deleted (zero runtime callers, verified)."
    - "The candidato-created dispatch is preserved server-side: a new AFTER INSERT ON candidatos SECURITY DEFINER trigger posts via pg_net a body carrying ONLY {candidato_id, event, timestamp} — no PII — reading the Vault n8n_webhook_base secret with a graceful RETURN NEW when it is NULL (secret is Fernando's human-action)."
    - "RNF-07a preserved: the trigger never writes candidatos; it is a fire-and-forget AFTER INSERT."
    - "The n8n-bundle grep guard is EXTENDED to ban 'n8n.srv881294.hstgr.cloud' + the PII payload field names (drops the old carve-out) across build/ and src/, keeping the no-false-positive sub-tests; and a behavioral smoke proves the trigger graceful-skips with no PII + candidatos unchanged when the secret is NULL."
  artifacts:
    - path: "supabase/migrations/20260712100004_n8n_novo_candidato.sql"
      provides: "trg_n8n_novo_candidato AFTER INSERT ON candidatos (pg_net + Vault, id-only body, graceful-skip)"
      contains: "AFTER INSERT ON public.candidatos"
    - path: "src/__tests__/guards/n8n-bundle.grep.test.ts"
      provides: "extended guard banning the hstgr host + PII literals in build/ and src/"
      contains: "n8n.srv881294.hstgr.cloud"
    - path: "supabase/tests/n8n_novo_candidato_smoke.sql"
      provides: "graceful-skip / no-PII / candidatos-unchanged smoke"
      contains: "trg_n8n_novo_candidato"
  key_links:
    - from: "supabase/migrations/20260712100004_n8n_novo_candidato.sql"
      to: "vault.decrypted_secrets (n8n_webhook_base) + net.http_post"
      via: "read secret → graceful RETURN NEW if NULL → id-only body"
      pattern: "n8n_webhook_base"
    - from: "src/features/cadastro/services/index.ts"
      to: "(deleted) n8nService"
      via: "removed barrel re-export line"
      pattern: "n8nService"
---

<objective>
Close the second n8n PII leak (routed in after Phase 24, SEC-03 pattern): delete the client-side `n8nService.ts` subtree that hardcodes 18 `n8n.srv881294.hstgr.cloud` URLs and ships `notifyCandidatoCriado` candidate PII, move the candidato-created dispatch server-side to an `AFTER INSERT ON candidatos` trigger (pg_net + Vault, id-only body), extend the bundle grep guard to ban the host + PII literals, and author the trigger smoke. Migration is files-only (applied Wave 4 / 26-07).

Purpose: `n8nService.ts` inlines the host + PII field names into the public bundle even though it has zero runtime callers. Mirror the SEC-03 server-side precedent (`20260706110005`) exactly, retargeted to `candidatos` with a PII-free body.
Output: 1 trigger migration + 1 smoke + client subtree deletion + guard extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-CONTEXT.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md

<interfaces>
<!-- SEC-03 precedent to mirror: 20260706110005_sec03_n8n_serverside.sql:52-99 (trg fn + Vault read + graceful RETURN NEW + net.http_post + trigger). -->
<!-- pg_net enabled at 20260609000001:47. Vault secret name = 'n8n_webhook_base' (same as SEC-03; NULL until Fernando sets it). -->
<!-- n8nService.ts: hstgr URLs :122-168 ; notifyCandidatoCriado PII (id, nome_completo, email, telefone, cpf) :388-417. -->
<!-- Barrel: src/features/cadastro/services/index.ts line `export * from './n8nService'`. Verified ZERO runtime callers (only barrel + test). -->
<!-- Guard current state: n8n-bundle.grep.test.ts uses FORBIDDEN_BUILD_TOKENS = ['n8n.cloud','fernandocosta'] and EXPLICITLY carves out 'n8n.srv881294.hstgr.cloud' in its SCOPE NOTE. Phase 26 removes that carve-out and ADDS the host as an exact-substring token (n8n.cloud does NOT match the hstgr host). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the AFTER INSERT ON candidatos server-side trigger migration + smoke</name>
  <files>supabase/migrations/20260712100004_n8n_novo_candidato.sql, supabase/tests/n8n_novo_candidato_smoke.sql</files>
  <read_first>
    - supabase/migrations/20260706110005_sec03_n8n_serverside.sql (:52-99 the SEC-03 trigger to mirror verbatim; Vault read + graceful-skip + net.http_post + trigger DDL)
    - supabase/tests/sec05_08_smokes.sql (impersonation/cleanup idiom)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md §Code Example 8 (:519-539) + Pitfall 9 + Runtime State Inventory (n8n row)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md §n8n migration
  </read_first>
  <action>
    Mirror `20260706110005:52-94`: `CREATE OR REPLACE FUNCTION public.trg_n8n_novo_candidato() RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER SET search_path=''` that `SELECT decrypted_secret INTO v_base FROM vault.decrypted_secrets WHERE name='n8n_webhook_base'`, `IF v_base IS NULL THEN RETURN NEW; END IF;` (graceful-skip), then `PERFORM net.http_post(url := v_base || '/novo-candidato', headers := jsonb_build_object('Content-Type','application/json'), body := jsonb_build_object('event','candidato.created','timestamp', now(), 'data', jsonb_build_object('candidato_id', NEW.id)))` — body carries ONLY `candidato_id`, NEVER nome/email/cpf/telefone (D-n8n-ServerSide, LGPD). `RETURN NEW` (RNF-07a — never writes candidatos). Tail: `REVOKE ALL ON FUNCTION public.trg_n8n_novo_candidato() FROM PUBLIC;`, `DROP TRIGGER IF EXISTS trg_n8n_novo_candidato ON public.candidatos;`, `CREATE TRIGGER trg_n8n_novo_candidato AFTER INSERT ON public.candidatos FOR EACH ROW EXECUTE FUNCTION public.trg_n8n_novo_candidato();`. NO `BEGIN;/COMMIT;` wrapper; header note apply via MCP (Wave 4).
    Then author `supabase/tests/n8n_novo_candidato_smoke.sql`: with the Vault secret absent (NULL), INSERT a disposable candidato → assert (NOTICE=PASS/EXCEPTION=FAIL): no error is raised (graceful-skip), the inserted candidatos row is unchanged/present, and (structurally) the trigger body carries no PII column reference. ROLLBACK-free cleanup deleting the disposable candidato. Header note: RED until Wave 4.
  </action>
  <acceptance_criteria>
    - `grep -c "AFTER INSERT ON public.candidatos" file` == 1; trigger fn is DEFINER (`grep -c "SECURITY DEFINER" file` >= 1).
    - Body is PII-free: `grep -Eic "nome_completo|nome|email|telefone|cpf" file` == 0 (only candidato_id crosses).
    - Graceful-skip present: `grep -c "IF v_base IS NULL THEN RETURN NEW" file` >= 1 (or equivalent NULL-check RETURN NEW).
    - RNF-07a: no write to candidatos in the trigger fn (`grep -Eic "UPDATE +public\\.candidatos|INSERT +INTO +public\\.candidatos" file` == 0).
    - No txn wrapper; smoke exists asserting graceful-skip (`grep -Ec "RAISE NOTICE 'PASS" smoke` >= 1).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "cd '/Users/fernando/Cursor Repo/DB Sistema de recrutamento' && m=supabase/migrations/20260712100004_n8n_novo_candidato.sql && s=supabase/tests/n8n_novo_candidato_smoke.sql && grep -c 'AFTER INSERT ON public.candidatos' \$m && test \$(grep -Eic 'nome_completo|telefone|cpf|email' \$m) -eq 0 && grep -c 'RETURN NEW' \$m && test -f \$s && echo OK"</automated>
  </verify>
  <done>The trigger migration mirrors SEC-03 with a PII-free id-only body, graceful-skips when the secret is NULL, never writes candidatos; the smoke proves graceful-skip. Not applied yet.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the n8nService client subtree + extend the bundle grep guard</name>
  <files>src/features/cadastro/services/n8nService.ts, src/features/cadastro/services/index.ts, src/features/cadastro/services/n8nService.test.ts, src/__tests__/guards/n8n-bundle.grep.test.ts</files>
  <read_first>
    - src/features/cadastro/services/n8nService.ts (the subtree to delete — hstgr URLs :122-168, notifyCandidatoCriado PII :388-417)
    - src/features/cadastro/services/index.ts (the barrel — remove the `export * from './n8nService'` line only; keep viaCep/duplicateCheck/authService/cadastroService)
    - src/__tests__/guards/n8n-bundle.grep.test.ts (:18-70 the SCOPE NOTE carve-out + FORBIDDEN_BUILD_TOKENS + firstForbiddenBuildToken + build/ and src/ legs + no-false-positive sub-test)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md §Code Example 9 + Pitfall 9
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md §n8n-bundle guard (all four deletion steps)
  </read_first>
  <action>
    Per Pitfall 9, do all four deletions cleanly: (1) delete `src/features/cadastro/services/n8nService.ts`; (2) remove ONLY the `export * from './n8nService'` line from `index.ts` (leave the other barrel exports intact); (3) delete `src/features/cadastro/services/n8nService.test.ts`; (4) confirm `tsc` has no dangling re-export.
    Then EXTEND `n8n-bundle.grep.test.ts`: add `'n8n.srv881294.hstgr.cloud'` as an exact-substring token to `FORBIDDEN_BUILD_TOKENS` and drop the SCOPE-NOTE carve-out that exempted it; also add the PII payload field literals co-located with an n8n host (e.g. `nome_completo`) to the banned set. Keep the two-plane structure (build/ leg + src/ leg), the comment-aware line filter, and the existing no-false-positive sub-test — and PRESERVE the guarantee that an unrelated `cpf`/`nome` schema field is NOT false-flagged (only flag when co-located with an n8n host string). Flip the previous hstgr `toBeNull()` assertion to a positive-ban assertion. The guard rides the existing `npm run test:run` CI leg (no new workflow); its build/ leg requires `npm run build` first.
  </action>
  <acceptance_criteria>
    - `src/features/cadastro/services/n8nService.ts` and `src/features/cadastro/services/n8nService.test.ts` no longer exist.
    - `grep -c "n8nService" src/features/cadastro/services/index.ts` == 0 (barrel line removed).
    - No runtime dangling reference: `grep -rEc "notifyCandidatoCriado|N8N_WORKFLOWS|sendToN8N" src/ --include=*.ts --include=*.tsx` == 0.
    - Guard bans the host: `grep -c "n8n.srv881294.hstgr.cloud" src/__tests__/guards/n8n-bundle.grep.test.ts` >= 1 (now a banned token, not a carve-out).
    - `npm run build` clean AND `npx vitest run src/__tests__/guards/n8n-bundle.grep.test.ts` passes (build/ + src/ clean of the host + PII literals) AND `npm run lint` (tsc) not above 107.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "cd '/Users/fernando/Cursor Repo/DB Sistema de recrutamento' && test ! -f src/features/cadastro/services/n8nService.ts && test ! -f src/features/cadastro/services/n8nService.test.ts && test \$(grep -c 'n8nService' src/features/cadastro/services/index.ts) -eq 0 && test \$(grep -rEc 'notifyCandidatoCriado|N8N_WORKFLOWS|sendToN8N' src/ --include=*.ts --include=*.tsx) -eq 0 && npm run build >/dev/null 2>&1 && npx vitest run src/__tests__/guards/n8n-bundle.grep.test.ts && echo OK"</automated>
  </verify>
  <done>The n8nService subtree (file + barrel line + test) is gone, no runtime references remain, tsc/build are clean, and the extended guard bans the hstgr host + PII literals in build/ and src/ while preserving no-false-positives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public bundle → end user | Anything in build/ ships to any visitor; the n8n host + PII field names must not be there |
| DB (candidatos INSERT) → n8n webhook | Server-side dispatch; the URL lives only in Vault, the body carries no PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | Information Disclosure | n8n host URL ships in the public bundle | mitigate | delete client subtree + barrel line; server-side pg_net dispatch; extended grep guard bans the host in build/ + src/ |
| T-26-03-02 | Information Disclosure (LGPD) | candidate PII (nome/email/telefone/cpf) in the webhook body / bundle | mitigate | trigger body carries only candidato_id; delete the PII payload; guard bans PII literals co-located with n8n host |
| T-26-03-03 | Spoofing/DoS | forged/enumerable n8n webhook | mitigate | URL never leaves the server (Vault); body carries no PII even if the endpoint is hit; graceful-skip when secret NULL |
| T-26-03-04 | Elevation/RNF-07a | trigger writes candidatos | mitigate | AFTER INSERT fire-and-forget; RETURN NEW; never writes candidatos |
| T-26-03-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase; a dependency is REMOVED (dead code), none added; N/A |
</threat_model>

<verification>
- Acceptance criteria pass; `npm run build` clean; `npx vitest run` of the guard green; tsc ≤ 107.
- The trigger smoke runs GREEN in plan 26-07 after MCP apply (secret still NULL → graceful-skip is the expected live behavior).
</verification>

<success_criteria>
The client n8n PII/URL leak is removed from the bundle, the candidato-created dispatch is server-side (pg_net + Vault, id-only body, graceful-skip, RNF-07a), and the extended grep guard is the durable regression net — all proven by build+guard green now and the trigger smoke live in 26-07.
</success_criteria>

<output>
Create `.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-03-SUMMARY.md` when done.
</output>
