---
phase: 26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260712100001_funil01_pontuar_sjt_v2.sql
  - supabase/tests/funil01_pontuar_sjt_smokes.sql
autonomous: true
requirements: [FUNIL-01, FUNIL-07]
must_haves:
  truths:
    - "FUNIL-01 (D-SJT-Integrity): pontuar_sjt is non-manipulable — a repeated pergunta_id RAISEs, the denominator spans the FULL active-MC battery of the vaga (not only answered), an incomplete submit RAISEs, an empty/unconfigured battery (v_expected=0) RAISEs 'bateria SJT nao configurada' (Open Q2 resolved — no silent 0/0 'sucesso'), and a second submit over an existing non-'falhou' MC row RAISEs (hard re-submit lock closing A41)."
    - "FUNIL-07 (server teeth, D-Question-Filter): pontuar_sjt RAISEs 42501 for any submitted pergunta_id not in the vaga battery — the client filter is presentation only, the server is the security control."
    - "RNF-07a preserved: pontuar_sjt writes ONLY scores_candidato and returns a NEUTRAL payload ({ok,registrado}); it NEVER writes candidaturas.status/etapa_atual and never auto-rejects."
    - "The caso_aberto item is excluded from the MC battery (formato='mc' AND status='active' only) so completeness/denominator math is correct (Pitfall 2)."
    - "A SQL behavioral smoke file exists that proves every above behavior over an impersonated candidate JWT — the load-bearing acceptance gate (structural greps are insufficient; runs GREEN in the Wave 4 apply plan 26-07)."
  artifacts:
    - path: "supabase/migrations/20260712100001_funil01_pontuar_sjt_v2.sql"
      provides: "CREATE OR REPLACE public.pontuar_sjt(uuid,jsonb) with dedup/full-battery-denominator/battery-membership/completeness/re-submit-lock + metadata{expected,answered,versao}"
      contains: "CREATE OR REPLACE FUNCTION public.pontuar_sjt"
    - path: "supabase/tests/funil01_pontuar_sjt_smokes.sql"
      provides: "Impersonated-JWT behavioral smoke for FUNIL-01 + FUNIL-07 server surface"
      contains: "set_config('request.jwt.claims'"
  key_links:
    - from: "supabase/migrations/20260712100001_funil01_pontuar_sjt_v2.sql"
      to: "public.scores_candidato"
      via: "INSERT one (tipo='sjt',subtipo='mc',pergunta_id=NULL) row; re-submit lock keys the SAME tuple"
      pattern: "scores_candidato"
    - from: "supabase/migrations/20260712100001_funil01_pontuar_sjt_v2.sql"
      to: "public.perguntas_opcao_sjt"
      via: "server-side Σ peso re-derivation over marked picks + MAX(peso) over full battery"
      pattern: "perguntas_opcao_sjt"
---

<objective>
Rewrite the `pontuar_sjt` SECURITY DEFINER RPC so SJT scoring is non-manipulable (FUNIL-01) and cross-battery question injection is rejected server-side (FUNIL-07 teeth), and author its behavioral smoke. Files-only: the migration applies to PROD in the Wave 4 BLOCKING plan (26-07) via Supabase MCP `apply_migration`.

Purpose: The current `pontuar_sjt` (`20260611000004`) inflates >100% (no DISTINCT), computes the denominator over answered questions only, and overwrites via `ON CONFLICT DO UPDATE` (A8 + A41). This plan closes all four holes while preserving RNF-07a.
Output: One migration file + one SQL behavioral smoke file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-CONTEXT.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md

<interfaces>
<!-- The prescriptive rewrite is 26-RESEARCH.md §Code Example 1 (lines 306-403). Preserve verbatim from the live file: -->
<!-- AUTHZ posture 20260611000004:55-66 (owner via candidatos.user_id=auth.uid() AND etapa_atual='avaliacao_assincrona' → RAISE 42501). -->
<!-- Envelope + tail 20260611000004:37-45,:138-139 (SECURITY DEFINER SET search_path='' ; REVOKE ALL FROM PUBLIC ; GRANT EXECUTE TO authenticated). -->
<!-- NEUTRAL return 20260611000004:133-134 (jsonb_build_object('ok',true,'registrado',true)). -->
<!-- scores_candidato MC row tuple: (candidatura_id, tipo='sjt', subtipo='mc', pergunta_id=NULL) — UNIQUE NULLS NOT DISTINCT key 20260611000001:66-71. -->
<!-- Battery source: vagas.testes_aplicaveis element where tipo='sjt' → itens_ids (uuid[]) when present else cargo; perguntas.formato IN ('mc','caso_aberto') 20260611000002:46. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the rewritten pontuar_sjt migration (FUNIL-01 + FUNIL-07 server)</name>
  <files>supabase/migrations/20260712100001_funil01_pontuar_sjt_v2.sql</files>
  <read_first>
    - supabase/migrations/20260611000004_pontuar_sjt_rpc.sql (the function being rewritten — authz :55-66, marked/scored/maxes CTEs :84-100, ON CONFLICT :121-131, envelope/tail :37-45/:138-139)
    - supabase/migrations/20260611000001_scores_candidato.sql (:66-71 UNIQUE NULLS NOT DISTINCT key; :80-90 candidate-DENY RLS)
    - supabase/migrations/20260611000002_perguntas_sjt.sql (:46 formato; perguntas_opcao_sjt peso/tag; get_opcoes_sjt)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md §Code Example 1 (lines 306-404) + Pitfalls 1/2/3
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md §funil01 (Fix 1/2/3 + Preserve blocks)
  </read_first>
  <action>
    `CREATE OR REPLACE FUNCTION public.pontuar_sjt(p_candidatura_id uuid, p_respostas jsonb)` following 26-RESEARCH §Code Example 1 exactly (blocks A–I). Implement per D-SJT-Integrity:
    (A) PRESERVE the authz posture verbatim — owner via `candidatos.user_id = auth.uid()` (NEVER candidato_id) AND `c.etapa_atual = 'avaliacao_assincrona'`, else `RAISE EXCEPTION ... USING errcode='42501'`.
    (B) Re-submit LOCK (FUNIL-12 overwrite / A41): `IF EXISTS (SELECT 1 FROM public.scores_candidato WHERE candidatura_id=p_candidatura_id AND tipo='sjt' AND subtipo='mc' AND pergunta_id IS NULL AND status <> 'falhou') THEN RAISE 42501 'avaliacao ja registrada'`. The predicate MUST match the exact MC row tuple (Pitfall 3).
    (C) Resolve the vaga SJT battery from `vagas.testes_aplicaveis` element `tipo='sjt'` → `itens_ids` (uuid[]) when non-empty ELSE `cargo`; scope `perguntas` to `formato='mc' AND status='active'` ONLY so the `caso_aberto` item is excluded (Pitfall 2). `v_expected = COALESCE(array_length(battery,1),0)`.
    (C2) EMPTY BATTERY (Open Q2 — RESOLVED): immediately after computing `v_expected`, if `v_expected = 0` → `RAISE EXCEPTION 'bateria SJT nao configurada' USING errcode='22023'`. This MUST run BEFORE the completeness check so an empty/unconfigured battery fails loudly instead of short-circuiting to `sucesso` at `v_max=0` (a real manipulability hole: an empty `p_respostas` submit would otherwise pass completeness `0<>0=false` and score 0/0 'sucesso').
    (D) DEDUP (FUNIL-01): if `count(*)` of respostas <> `count(DISTINCT pergunta_id)` → `RAISE errcode='22023' 'resposta duplicada'`.
    (E) BATTERY MEMBERSHIP (FUNIL-07 server teeth): if any submitted `pergunta_id <> ALL(battery)` → `RAISE errcode='42501' 'pergunta fora da bateria'`.
    (F) COMPLETENESS: if `count(DISTINCT pergunta_id) <> v_expected` → `RAISE errcode='22023' 'bateria incompleta'`.
    (G) mc_min_pct: read from the SJT element `threshold.mc_min_pct`, default 60 (unchanged read).
    (H) numerator = Σ peso over marked picks; DENOMINATOR = Σ MAX(peso) over `pergunta_id = ANY(battery)` — the FULL active-MC battery, replacing the `IN (SELECT pergunta_id FROM marked)` bug at :95-100 (Fix 1). status = pendente_humano when has_atencao OR pct < mc_min_pct, else sucesso.
    (I) INSERT exactly one `scores_candidato` MC row with `metadata` jsonb carrying `expected`, `answered`, `versao:2`, `respostas` breakdown, `has_atencao`, `mc_min_pct` (metadata shape is Claude's discretion, must be auditable). RETURN the NEUTRAL `jsonb_build_object('ok',true,'registrado',true)` — RNF-07a: never touch `candidaturas`.
    Tail: `REVOKE ALL ON FUNCTION public.pontuar_sjt(uuid, jsonb) FROM PUBLIC;` + `GRANT EXECUTE ... TO authenticated;`. Migration mechanics: NO `BEGIN;/COMMIT;` wrapper, `SET search_path=''` on the function, add a one-line header note that apply is via Supabase MCP `apply_migration` (bypasses 42601) — do NOT plan a db push. Do NOT edit `database.types.ts` (regen is Phase 27).
  </action>
  <acceptance_criteria>
    - `grep -c "CREATE OR REPLACE FUNCTION public.pontuar_sjt" supabase/migrations/20260712100001_funil01_pontuar_sjt_v2.sql` == 1.
    - File contains all six RAISE guards: `grep -Ec "RAISE EXCEPTION" file` >= 6 (authz + empty-battery + dedup + membership + completeness + re-submit-lock) and both SQLSTATEs appear (`42501` and `22023`).
    - Empty-battery guard present (Open Q2 resolved): `grep -c "bateria SJT nao configurada" file` >= 1 AND it precedes the completeness check.
    - Denominator over full battery: `grep -c "ANY(v_battery)" file` >= 1 AND no residual `IN (SELECT pergunta_id FROM marked)` in the maxes CTE (`grep -c "FROM marked" file` in the maxes WHERE == 0).
    - RNF-07a: no write to candidaturas — `grep -Eic "UPDATE +public\\.candidaturas|INSERT +INTO +public\\.candidaturas" file` == 0.
    - Envelope hygiene: file has `SET search_path` and `GRANT EXECUTE ON FUNCTION public.pontuar_sjt(uuid, jsonb) TO authenticated` and NO `BEGIN;`/`COMMIT;` wrapper (`grep -Eic "^\\s*(BEGIN|COMMIT);" file` == 0).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "cd '/Users/fernando/Cursor Repo/DB Sistema de recrutamento' && f=supabase/migrations/20260712100001_funil01_pontuar_sjt_v2.sql && grep -c 'CREATE OR REPLACE FUNCTION public.pontuar_sjt' \$f && grep -Ec 'RAISE EXCEPTION' \$f && grep -c 'ANY(v_battery)' \$f && test \$(grep -Eic '^[[:space:]]*(BEGIN|COMMIT);' \$f) -eq 0 && echo OK"</automated>
  </verify>
  <done>The migration file exists, defines the rewritten pontuar_sjt with all five guards, full-battery denominator, MC-only battery, neutral return, correct envelope/tail, and no transaction wrapper. Not yet applied (Wave 4).</done>
</task>

<task type="auto">
  <name>Task 2: Author the FUNIL-01/07 behavioral smoke</name>
  <files>supabase/tests/funil01_pontuar_sjt_smokes.sql</files>
  <read_first>
    - supabase/tests/sec05_08_smokes.sql (canonical impersonation idiom :37-57 privileged discovery + GUC stash, :59-88 SET ROLE authenticated + set_config request.jwt.claims + NOTICE=PASS/EXCEPTION=FAIL, :311-313 ROLLBACK-free cleanup)
    - supabase/migrations/20260712100001_funil01_pontuar_sjt_v2.sql (the surface under test — from Task 1)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md §Validation Test Map (FUNIL-01 + FUNIL-07 rows) + §Open Q1 (UAT reset note)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md §SQL behavioral smokes (per-smoke assertions)
  </read_first>
  <action>
    Author `supabase/tests/funil01_pontuar_sjt_smokes.sql` mirroring `sec05_08_smokes.sql`: `RESET ROLE` to discover a disposable/`[TESTE]`-vaga candidatura at `etapa_atual='avaliacao_assincrona'` with a known SJT battery (create a disposable fixture if none — insert vaga + perguntas MC + perguntas_opcao_sjt + candidato/candidatura, stash ids via `set_config('smoke.*',...,false)`; skip-with-NOTICE if the fixture cannot be built), then `SET ROLE authenticated` + `set_config('request.jwt.claims', ...owner sub..., false)` and assert (NOTICE=PASS / EXCEPTION=FAIL):
    (1) duplicate pergunta_id in p_respostas → EXCEPTION (dedup);
    (2) complete honest submit of the full battery → succeeds and the recorded score uses the FULL-battery denominator (assert score_max == Σ MAX(peso) over the battery, NOT the subset);
    (3) a subset submit (fewer than battery) → EXCEPTION `bateria incompleta`;
    (4) a valid-but-foreign pergunta_id (belongs to another cargo/battery) → EXCEPTION `42501` (FUNIL-07 server);
    (5) a SECOND complete submit → EXCEPTION `42501` (re-submit lock) AND assert exactly ONE `scores_candidato` MC row exists for the candidatura;
    (6) RNF-07a structural check: `candidaturas.status`/`etapa_atual` are byte-identical before/after scoring;
    (7) EMPTY BATTERY (Open Q2): build a SECOND disposable candidatura at `avaliacao_assincrona` on a vaga with NO SJT battery (no `tipo='sjt'` element, OR `itens_ids=[]` with a non-matching `cargo`) and submit any/empty `p_respostas` → EXCEPTION `bateria SJT nao configurada` (proves an unconfigured battery fails loudly, never scores 0/0 'sucesso'); skip-with-NOTICE if that fixture cannot be built.
    End with ROLLBACK-free cleanup: `SELECT set_config('request.jwt.claims','',false); RESET ROLE;` and delete any disposable fixture rows created. Add a header note: run via Supabase MCP `execute_sql` / SQL Editor AFTER the migration applies (Wave 4 / 26-07) — this file is RED until then.
  </action>
  <acceptance_criteria>
    - File exists and uses the impersonation idiom: `grep -c "set_config('request.jwt.claims'" file` >= 1 AND `grep -c "SET ROLE authenticated" file` >= 1.
    - All seven assertions are present as distinct DO blocks / NOTICE labels: `grep -Ec "RAISE NOTICE 'PASS" file` >= 7.
    - Cleanup present: `grep -c "RESET ROLE" file` >= 1 AND `grep -c "set_config('request.jwt.claims', ''" file` >= 1 (or equivalent empty-claims reset).
    - The smoke references the exact error strings/SQLSTATEs it expects (`bateria incompleta`, `bateria SJT nao configurada`, `42501`) so a silent regression cannot pass.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "cd '/Users/fernando/Cursor Repo/DB Sistema de recrutamento' && f=supabase/tests/funil01_pontuar_sjt_smokes.sql && test -f \$f && grep -c \"set_config('request.jwt.claims'\" \$f && grep -c 'SET ROLE authenticated' \$f && test \$(grep -Ec \"RAISE NOTICE 'PASS\" \$f) -ge 7 && grep -c 'RESET ROLE' \$f && echo OK"</automated>
  </verify>
  <done>The smoke file exists, impersonates a candidate JWT, asserts all six FUNIL-01/07 behaviors with PASS/FAIL semantics, and cleans up. It is the load-bearing acceptance gate, executed GREEN in plan 26-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate browser → `pontuar_sjt` RPC | Untrusted picks cross here; the candidate can forge/replay the call body |
| RPC → `perguntas_opcao_sjt` (gabarito) | The peso/tag answer-key must never cross back to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-01 | Tampering | client posts a forged `score` / inflated picks | mitigate | RPC ignores any client score; re-derives Σ peso server-side from perguntas_opcao_sjt; returns NEUTRAL {ok,registrado} |
| T-26-01-02 | Tampering | duplicate/subset answers inflate the % | mitigate | dedup RAISE (22023) + full-battery denominator + completeness RAISE (22023) |
| T-26-01-03 | Tampering/Repudiation | re-submit overwrites a recorded score without a trail | mitigate | hard re-submit lock: RAISE 42501 on an existing non-'falhou' MC row; exactly one MC row asserted by the smoke |
| T-26-01-04 | Tampering/IDOR | cross-cargo/battery pergunta_id injected into scoring | mitigate | server-side `pergunta_id ∈ battery` RAISE 42501 (FUNIL-07); client filter is UX only |
| T-26-01-05 | Information Disclosure | candidate reads own score/gabarito/threshold | mitigate | scores_candidato row-deny (no candidate SELECT policy); NEUTRAL return; no peso/tag echoed |
| T-26-01-06 | Elevation/RNF-07a | scoring auto-rejects the candidatura | mitigate | function never writes candidaturas; smoke asserts status/etapa_atual unchanged |
| T-26-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit — zero external packages); N/A |
</threat_model>

<verification>
- `grep`-based acceptance criteria above pass for both files.
- `npm run lint` (tsc) stays ≤ 107 (no TS files touched by this plan; no growth).
- The load-bearing proof (behavioral smokes) runs in plan 26-07 AFTER MCP apply — this plan authors them RED.
</verification>

<success_criteria>
The rewritten pontuar_sjt migration and its behavioral smoke exist, are structurally correct (all guards, full-battery denominator, MC-only battery, neutral return, no txn wrapper, correct grants), and encode FUNIL-01 + FUNIL-07-server + RNF-07a as executable assertions to be proven live in 26-07.
</success_criteria>

<output>
Create `.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-01-SUMMARY.md` when done.
</output>
