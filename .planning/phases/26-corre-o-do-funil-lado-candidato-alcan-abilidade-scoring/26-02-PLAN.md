---
phase: 26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260712100002_funil08_pontuar_cognitivo_gate.sql
  - supabase/migrations/20260712100003_funil12_get_avaliacao_status.sql
  - supabase/tests/funil08_pontuar_cognitivo_smokes.sql
  - supabase/tests/funil12_status_rpc_smoke.sql
autonomous: true
requirements: [FUNIL-08, FUNIL-12]
must_haves:
  truths:
    - "FUNIL-08 (D-Cognitive-Reachability, Option A): the LIVE 5-arg pontuar_cognitivo accepts etapa 'avaliacao_assincrona' ADDED to the interview stages ('entrevista_online','entrevista_presencial') — ADDED not replaced, so interview-stage cognitivo submits do not regress."
    - "FUNIL-12 (D-Card-Status): a new neutral get_avaliacao_status(uuid) DEFINER RPC returns per-test presence booleans ONLY (never status/score/score_max/metadata) — the candidate's own-row truth for card state, closing the phantom entry.status read."
    - "RNF-07a + answer-key protection preserved: get_avaliacao_status never leaks a verdict; scores_candidato keeps its candidate-DENY row policy (no candidate SELECT policy added); ownership mismatch RAISEs 42501 (IDOR)."
    - "Behavioral smokes exist proving (a) cognitivo submit during avaliacao_assincrona registers AND interview stage still registers, and (b) the status RPC returns booleans only + 42501 on a foreign candidatura — the load-bearing gate, run GREEN in plan 26-07."
  artifacts:
    - path: "supabase/migrations/20260712100002_funil08_pontuar_cognitivo_gate.sql"
      provides: "CREATE OR REPLACE the 5-arg public.pontuar_cognitivo adding 'avaliacao_assincrona' to the etapa IN"
      contains: "pontuar_cognitivo"
    - path: "supabase/migrations/20260712100003_funil12_get_avaliacao_status.sql"
      provides: "New public.get_avaliacao_status(uuid) neutral booleans-only DEFINER RPC"
      contains: "CREATE OR REPLACE FUNCTION public.get_avaliacao_status"
    - path: "supabase/tests/funil08_pontuar_cognitivo_smokes.sql"
      provides: "etapa-relax smoke (async + interview, no regression)"
      contains: "avaliacao_assincrona"
    - path: "supabase/tests/funil12_status_rpc_smoke.sql"
      provides: "neutral-booleans + ownership-42501 smoke"
      contains: "get_avaliacao_status"
  key_links:
    - from: "supabase/migrations/20260712100003_funil12_get_avaliacao_status.sql"
      to: "public.scores_candidato / public.respostas_avaliacao"
      via: "EXISTS(...) presence booleans per test — never a column value"
      pattern: "EXISTS"
    - from: "supabase/migrations/20260712100002_funil08_pontuar_cognitivo_gate.sql"
      to: "public.candidaturas.etapa_atual"
      via: "authz IN (...) etapa gate — ADD avaliacao_assincrona"
      pattern: "avaliacao_assincrona"
---

<objective>
Author two migration files that make the cognitive assessment reachable and that give the candidate cards a neutral own-row truth source: (1) relax the LIVE 5-arg `pontuar_cognitivo` etapa gate to accept `avaliacao_assincrona` (FUNIL-08 DB back-stop), and (2) create the neutral `get_avaliacao_status` DEFINER RPC returning per-test presence booleans only (FUNIL-12 truth source). Author both smokes. Files-only; PROD apply is Wave 4 (26-07).

Purpose: The cognitivo screen is unreachable because the gate rejects the async stage (A18); the cards read a phantom `entry.status` (A41). The DB half of both fixes lives here; the client half is 26-05/26-06.
Output: 2 migration files + 2 SQL behavioral smokes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-CONTEXT.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md

<interfaces>
<!-- pontuar_cognitivo: the LIVE overload is the 5-arg one in 20260625000001_phase14_gap_closure.sql:66-103. -->
<!--   Signature: public.pontuar_cognitivo(uuid, jsonb, text DEFAULT NULL, int DEFAULT NULL, jsonb DEFAULT '{}'). -->
<!--   Etapa gate at :99: IN ('entrevista_online','entrevista_presencial'). The 2-arg overload was DROPPED at :66 — do NOT edit it. -->
<!-- get_avaliacao_status: prescriptive body in 26-RESEARCH §Code Example 6 (:476-500). Envelope analog = get_opcoes_sjt (20260611000002:107-130). -->
<!-- CONFIRMED autosave keys (respostas_avaliacao.teste) for the `iniciado`/em_andamento boolean: -->
<!--   redacao → 'redacao' ; sjt_caso_aberto → 'sjt_caso_aberto' ; big_five → 'big_five'. -->
<!--   SJT MC screen (SjtMultiplaEscolhaScreen) has NO autosave (submits picks directly) → there is NO respostas_avaliacao row with teste='sjt'. -->
<!--   cognitivo screen: no autosave row confirmed. So per-test `iniciado` booleans MUST use the real keys above; do NOT invent teste='sjt'. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relax the 5-arg pontuar_cognitivo etapa gate + author the FUNIL-08 smoke</name>
  <files>supabase/migrations/20260712100002_funil08_pontuar_cognitivo_gate.sql, supabase/tests/funil08_pontuar_cognitivo_smokes.sql</files>
  <read_first>
    - supabase/migrations/20260625000001_phase14_gap_closure.sql (:66-103 the LIVE 5-arg pontuar_cognitivo; etapa gate :99; :66 the DROP of the old 2-arg)
    - supabase/migrations/20260624000003_pontuar_cognitivo_rpc.sql (the DROPed 2-arg overload — do NOT edit)
    - supabase/tests/sec05_08_smokes.sql (impersonation idiom to mirror)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md §Code Example 3 + Pitfall 5/6
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md §funil08
  </read_first>
  <action>
    Per D-Cognitive-Reachability (Option A): `CREATE OR REPLACE FUNCTION public.pontuar_cognitivo` with the EXACT 5-arg signature `(p_candidatura_id uuid, p_respostas jsonb, p_shuffle_seed text DEFAULT NULL, p_completion_time_seconds int DEFAULT NULL, p_proctoring jsonb DEFAULT '{}'::jsonb)`, body copied VERBATIM from `20260625000001:68-103`, changing ONLY the authz etapa `IN` list to `IN ('entrevista_online','entrevista_presencial','avaliacao_assincrona')` — ADD the async stage, do NOT drop the interview stages (Pitfall 5). Keep `SECURITY DEFINER SET search_path=''`, the empty-bank guard, CTT soma, banding, neutral insert/return, and the REVOKE/GRANT tail all unchanged. Do NOT touch the 2-arg overload (already dropped). NO `BEGIN;/COMMIT;` wrapper; header note: apply via MCP `apply_migration` (Wave 4).
    Then author `supabase/tests/funil08_pontuar_cognitivo_smokes.sql` mirroring `sec05_08_smokes.sql`: discover/create a disposable candidatura with `aplica_cognitivo=true` and a cognitivo item bank; `SET ROLE authenticated` + impersonate the owner; assert (NOTICE=PASS/EXCEPTION=FAIL): (1) submit during `etapa_atual='avaliacao_assincrona'` → registrado (row written, no error); (2) submit during an interview stage → still registrado (no regression). ROLLBACK-free cleanup at the tail. Header note: RED until the Wave 4 apply.
  </action>
  <acceptance_criteria>
    - `grep -c "CREATE OR REPLACE FUNCTION public.pontuar_cognitivo" file` == 1 AND the etapa IN list contains all three: `grep -c "avaliacao_assincrona" file` >= 1 AND `grep -c "entrevista_online" file` >= 1 AND `grep -c "entrevista_presencial" file` >= 1.
    - The 5-arg signature is present (`grep -Ec "p_shuffle_seed|p_completion_time_seconds|p_proctoring" file` >= 3) — proving the LIVE overload, not the 2-arg one.
    - No txn wrapper (`grep -Eic "^\\s*(BEGIN|COMMIT);" file` == 0); `SET search_path` present; `GRANT EXECUTE` present.
    - Smoke asserts both stages: `grep -Ec "RAISE NOTICE 'PASS" funil08 smoke` >= 2 and both `avaliacao_assincrona` and an interview stage literal appear.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "cd '/Users/fernando/Cursor Repo/DB Sistema de recrutamento' && m=supabase/migrations/20260712100002_funil08_pontuar_cognitivo_gate.sql && s=supabase/tests/funil08_pontuar_cognitivo_smokes.sql && grep -c 'CREATE OR REPLACE FUNCTION public.pontuar_cognitivo' \$m && grep -c 'avaliacao_assincrona' \$m && grep -c 'entrevista_online' \$m && test \$(grep -Ec 'p_shuffle_seed|p_completion_time_seconds|p_proctoring' \$m) -ge 3 && test -f \$s && echo OK"</automated>
  </verify>
  <done>The cognitivo gate migration CREATE-OR-REPLACEs the 5-arg overload adding avaliacao_assincrona (interview stages kept), and the FUNIL-08 smoke asserts both stages register. Not applied yet.</done>
</task>

<task type="auto">
  <name>Task 2: Author get_avaliacao_status neutral RPC + FUNIL-12 status smoke</name>
  <files>supabase/migrations/20260712100003_funil12_get_avaliacao_status.sql, supabase/tests/funil12_status_rpc_smoke.sql</files>
  <read_first>
    - supabase/migrations/20260611000002_perguntas_sjt.sql (:107-130 get_opcoes_sjt — the neutral DEFINER-reader envelope + REVOKE/GRANT tail to copy)
    - supabase/migrations/20260611000001_scores_candidato.sql (columns + candidate-DENY RLS — do NOT add a candidate SELECT policy)
    - supabase/migrations/20260611000003_respostas_avaliacao.sql (autosave table; teste keys)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md §Code Example 6 (:476-500) + Pitfall 8 + Assumptions A2/A3 + Open Q3
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md §funil12
  </read_first>
  <action>
    Per D-Card-Status: `CREATE OR REPLACE FUNCTION public.get_avaliacao_status(p_candidatura_id uuid) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path=''`. First an ownership gate mirroring `pontuar_sjt:55-66` but WITHOUT the etapa clause (status is readable across etapas): owner via `candidatos.user_id=auth.uid()` else `RAISE 42501 'forbidden'` (IDOR guard, Pitfall 8). Return `jsonb_build_object` of per-test objects carrying ONLY presence booleans — NEVER status/score/score_max/metadata:
    - `sjt_mc`: `registrado` = EXISTS a scores_candidato row (tipo='sjt', subtipo='mc'). Do NOT add an `iniciado` for sjt_mc keyed on respostas_avaliacao teste='sjt' — SjtMultiplaEscolhaScreen has NO autosave (confirmed), so that boolean would always be false; either omit `iniciado` for sjt_mc or key it correctly if a real autosave key exists.
    - `redacao`: `registrado` = EXISTS scores_candidato tipo='redacao'; `iniciado` = EXISTS respostas_avaliacao teste='redacao'.
    - `big_five`: `registrado` = EXISTS scores_candidato tipo='big_five'; `iniciado` = EXISTS respostas_avaliacao teste='big_five'.
    - `cognitivo`: `registrado` = EXISTS scores_candidato tipo='cognitivo'.
    Return shape/keys are Claude's discretion (must not leak a verdict). Tail: `REVOKE ALL ... FROM PUBLIC; GRANT EXECUTE ... TO authenticated;`. NO txn wrapper; header note apply via MCP (Wave 4). Do NOT touch database.types.ts (Phase 27 regen; the client uses a narrow confined cast in 26-05).
    Then author `supabase/tests/funil12_status_rpc_smoke.sql` mirroring `sec05_08_smokes.sql`: impersonate the owner and assert (1) the RPC returns only boolean-valued fields (no numeric/`status`/`score` reachable — assert the returned jsonb has no `score`/`status`/`score_max`/`metadata` keys); (2) an owner with a scored SJT MC row reads `sjt_mc.registrado = true`; (3) impersonating a DIFFERENT user for the same candidatura → EXCEPTION 42501 (IDOR). ROLLBACK-free cleanup. Header note: RED until Wave 4.
  </action>
  <acceptance_criteria>
    - `grep -c "CREATE OR REPLACE FUNCTION public.get_avaliacao_status" file` == 1; ownership RAISE 42501 present (`grep -Ec "42501" file` >= 1).
    - Return is booleans-only: the migration contains NO `score`/`score_max`/`status`/`metadata` column in the RETURN jsonb keys — `grep -Eic "'score'|'score_max'|'status'|'metadata'" file` == 0 (RNF-07a / Pitfall 8).
    - Per-test `registrado` uses EXISTS over scores_candidato: `grep -c "EXISTS" file` >= 4.
    - No txn wrapper; `SET search_path` + `GRANT EXECUTE ... TO authenticated` present.
    - Smoke asserts booleans-only + IDOR 42501: `grep -c "get_avaliacao_status" smoke` >= 1 AND `grep -c "42501" smoke` >= 1 AND `grep -Ec "RAISE NOTICE 'PASS" smoke` >= 3.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "cd '/Users/fernando/Cursor Repo/DB Sistema de recrutamento' && m=supabase/migrations/20260712100003_funil12_get_avaliacao_status.sql && s=supabase/tests/funil12_status_rpc_smoke.sql && grep -c 'CREATE OR REPLACE FUNCTION public.get_avaliacao_status' \$m && test \$(grep -Eic \"'score'|'score_max'|'metadata'\" \$m) -eq 0 && test \$(grep -c 'EXISTS' \$m) -ge 4 && test -f \$s && grep -c '42501' \$s && echo OK"</automated>
  </verify>
  <done>The neutral status RPC returns per-test presence booleans only, gated by ownership (42501 on foreign candidatura), never leaking a verdict; its smoke proves booleans-only + IDOR. Not applied yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate browser → `pontuar_cognitivo` RPC | Untrusted submit; the etapa gate is the submit-time authz |
| candidate browser → `get_avaliacao_status` RPC | Untrusted read; must not leak the verdict of any test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | Tampering | cognitivo submit bypasses the etapa gate | mitigate | RPC etapa gate ADD avaliacao_assincrona + keep interview stages; owner via candidatos.user_id=auth.uid() → 42501 |
| T-26-02-02 | Information Disclosure | get_avaliacao_status leaks score/status/threshold | mitigate | return per-test presence booleans ONLY; smoke asserts no numeric/status key reachable (Pitfall 8) |
| T-26-02-03 | IDOR | candidate reads another candidatura's status | mitigate | ownership gate → RAISE 42501 on foreign candidatura; smoke covers it |
| T-26-02-04 | Information Disclosure | candidate SELECTs scores_candidato directly | mitigate | keep scores_candidato candidate-DENY row policy; NO candidate SELECT policy added; read only via the neutral RPC |
| T-26-02-05 | Elevation/RNF-07a | status/cognitivo path writes candidaturas | mitigate | neither function writes candidaturas; pontuar_cognitivo insert is scores-only, status RPC is read-only |
| T-26-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH §Package Legitimacy Audit); N/A |
</threat_model>

<verification>
- `grep`-based acceptance criteria pass for all four files.
- `npm run lint` (tsc) unchanged ≤ 107 (no TS touched).
- Live proof (both smokes) runs GREEN in plan 26-07 after MCP apply.
</verification>

<success_criteria>
The cognitivo gate migration (async ADDED, interview kept), the neutral status RPC (booleans-only, ownership-gated), and their two behavioral smokes exist and are structurally correct — encoding FUNIL-08-DB, FUNIL-12-DB, RNF-07a, and IDOR protection as assertions proven live in 26-07.
</success_criteria>

<output>
Create `.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-02-SUMMARY.md` when done.
</output>
