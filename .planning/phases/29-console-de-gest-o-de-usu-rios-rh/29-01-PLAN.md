---
phase: 29-console-de-gest-o-de-usu-rios-rh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/admin/schemas/usuarioRhSchemas.ts
  - src/features/admin/services/usuariosRhService.ts
  - src/features/admin/hooks/useUsuariosRh.ts
  - src/features/admin/services/__tests__/usuariosRhService.test.ts
autonomous: true
requirements: [USR-01, USR-02, USR-03, USR-04, USR-05]

must_haves:
  truths:
    - "The console can load the full RH roster (allowlist projection, admin-only RLS) as typed rows (USR-01)"
    - "The client can dispatch all 5 account actions (criar/mudar_papel/ativar/desativar/resetar_senha) through the Phase-28 EF write-path — never writing usuarios_rh directly (USR-02..05)"
    - "Every EF error_code (LAST_ADMIN / EMAIL_EXISTS / VALIDATION / FORBIDDEN / NOT_FOUND / SERVER_ERROR / EMAIL_SEND_FAILED) is mapped to a distinct client outcome; EMAIL_SEND_FAILED is success-with-warning, not a failure"
    - "The client create body parses under the EF's own Zod schema shape — the client↔EF contract cannot drift silently"
  artifacts:
    - path: "src/features/admin/schemas/usuarioRhSchemas.ts"
      provides: "novoUsuarioSchema + editarPapelSchema + PAPEL_OPTIONS + inferred form types (mirrors the EF _shared/usuario-rh-schemas.ts criar/mudar_papel branches)"
      min_lines: 25
    - path: "src/features/admin/services/usuariosRhService.ts"
      provides: "listUsuariosRh (allowlist) + criarUsuario/mudarPapel/ativarDesativar/resetarSenha (invoke gerenciar-usuario-rh) + UsuarioRhRow type + UsuariosRhServiceError"
      min_lines: 90
      exports: ["usuariosRhService", "listUsuariosRh", "criarUsuario", "mudarPapel", "ativarDesativar", "resetarSenha", "UsuarioRhRow", "USUARIOS_RH_LIST_COLUMNS"]
    - path: "src/features/admin/hooks/useUsuariosRh.ts"
      provides: "useUsuariosRh query + criar/mudarPapel/ativarDesativar/resetarSenha mutations that invalidate the list on success; usuariosRhKeys"
      exports: ["useUsuariosRh", "usuariosRhKeys"]
    - path: "src/features/admin/services/__tests__/usuariosRhService.test.ts"
      provides: "allowlist-projection assertion + every-error_code mapping + EMAIL_SEND_FAILED-as-warning + client↔EF contract-drift guard"
  key_links:
    - from: "src/features/admin/services/usuariosRhService.ts"
      to: "supabase.functions.invoke('gerenciar-usuario-rh')"
      via: "structured error_code parse via extractEfErrorCode from @/lib/efErrors"
      pattern: "functions\\.invoke\\(['\"]gerenciar-usuario-rh"
    - from: "src/features/admin/services/usuariosRhService.ts"
      to: "usuarios_rh"
      via: "explicit allowlist .select (never select('*'))"
      pattern: "USUARIOS_RH_LIST_COLUMNS"
    - from: "src/features/admin/hooks/useUsuariosRh.ts"
      to: "usuariosRhKeys.list()"
      via: "invalidateQueries on every mutation success"
      pattern: "invalidateQueries"
---

<objective>
Build the data layer for the RH user-management console: the Zod form schemas (mirroring the Phase-28 EF contract), the `usuariosRhService` (allowlist roster read + 5 EF-backed write actions), and the `useUsuariosRh` TanStack hooks (query + 4 mutations). This is the contract layer every Wave-2/3 component consumes — no UI in this plan.

Purpose: give the console a single, typed, contract-safe path to read the roster and to dispatch account actions through the already-shipped secure EF, with every EF `error_code` normalized to a client outcome. NO backend work — the `gerenciar-usuario-rh` EF, RLS, RPCs and anti-lockout trigger are live on PROD (Phase 28).

Output: `usuarioRhSchemas.ts`, `usuariosRhService.ts`, `useUsuariosRh.ts`, and the service test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-console-de-gest-o-de-usu-rios-rh/29-CONTEXT.md
@.planning/phases/29-console-de-gest-o-de-usu-rios-rh/29-PATTERNS.md
@.planning/phases/29-console-de-gest-o-de-usu-rios-rh/29-VALIDATION.md

**Path decision (locked):** use the FLAT feature layout `src/features/admin/{schemas,services,hooks,components}/` per CONTEXT §Área 1 (the 4 existing admin features are nested sub-features, but CONTEXT/UI-SPEC lock flat — follow flat). Test files live in `__tests__/` dirs (repo convention: 74/74 tests use `__tests__`, 0 co-located).

<interfaces>
<!-- Executor uses these directly — no codebase exploration needed. -->

EF `gerenciar-usuario-rh` request contract (SOURCE OF TRUTH — supabase/functions/_shared/usuario-rh-schemas.ts:38,55-93):
  discriminated union on `action`, every branch `.strict()`:
  - { action:'criar', email, nome_completo, cargo, papel }        papel ∈ {'recrutador','administrador'}
  - { action:'mudar_papel', target_id:uuid, novo_papel }          novo_papel ∈ {'recrutador','administrador'}
  - { action:'ativar', target_id:uuid }
  - { action:'desativar', target_id:uuid }
  - { action:'resetar_senha', target_id:uuid }
  validation msgs (mirror verbatim): email "Email inválido"; nome_completo min(3) "Nome deve ter no mínimo 3 caracteres" max(255) "Nome muito longo"; cargo min(1) "Cargo é obrigatório" max(100) "Cargo muito longo".

EF response shapes (supabase/functions/gerenciar-usuario-rh/index.ts):
  - criar OK        → 201 { ok:true, data:{ userId, usuarioRhId } }
  - criar OK+warn   → 200 { ok:true, data:{ userId, usuarioRhId }, warning:'EMAIL_SEND_FAILED' }
  - mudar_papel/ativar/desativar OK → 200 { ok:true }
  - resetar_senha OK → 200 { ok:true }   (may also carry warning:'EMAIL_SEND_FAILED')
  - any failure     → { ok:false, error_code, message, field? } at 400/401/403/404
  error_code vocabulary (UsuarioRhErrorCode): UNAUTHORIZED | FORBIDDEN | VALIDATION | NOT_FOUND | EMAIL_EXISTS | LAST_ADMIN | EMAIL_SEND_FAILED | SERVER_ERROR

usuarios_rh Row (database.types.ts:3606-3624) — ALLOWLIST projects exactly:
  id, user_id, nome_completo, email, cargo, role, ativo, primeiro_acesso, data_ultimo_login
  (cargo:string NOT NULL, role:string, data_ultimo_login:string|null; NEVER project avatar_url/telefone/created_by/updated_by/deleted_at)

extractEfErrorCode (src/lib/efErrors.ts:38-68):
  export async function extractEfErrorCode(data: unknown, error: EfInvokeError | null | undefined): Promise<string | undefined>
  — reads error_code from BOTH the non-2xx FunctionsHttpError body (error.context.json()) and the 200 body (data.error_code); code-only, never throws, never leaks PII. Use verbatim.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define client Zod schemas mirroring the EF contract</name>
  <files>src/features/admin/schemas/usuarioRhSchemas.ts</files>
  <read_first>
    - supabase/functions/_shared/usuario-rh-schemas.ts:38,55-93 (the EF criar/mudar_papel branches — the shape to mirror EXACTLY so client↔EF cannot drift; [[feedback_integration_contract_gap]])
    - src/features/decisao/schemas/decisaoSchema.ts:24-38 (client zod convention: z.object + z.infer export + OPTIONS list)
  </read_first>
  <action>
    Create `usuarioRhSchemas.ts` exporting: `novoUsuarioSchema` = z.object({ email (trim+toLowerCase+email("Email inválido")), nome_completo (min(3,"Nome deve ter no mínimo 3 caracteres").max(255,"Nome muito longo")), cargo (min(1,"Cargo é obrigatório").max(100,"Cargo muito longo")), papel (z.enum(['recrutador','administrador'])) }). Do NOT include the `action` field on the form schema — the service adds `action:'criar'`. Export `editarPapelSchema` = z.object({ novo_papel: z.enum(['recrutador','administrador']) }). Export inferred types `NovoUsuarioForm = z.infer<typeof novoUsuarioSchema>` and `EditarPapelForm`. Export `PAPEL_OPTIONS = [{ value:'recrutador', label:'Recrutador' }, { value:'administrador', label:'Administrador' }]` typed to the papel union. Use the EXACT pt-BR validation messages from the EF so client and server agree.
  </action>
  <acceptance_criteria>
    - `novoUsuarioSchema` has fields email/nome_completo/cargo/papel and NO `action` field.
    - The four validation messages are byte-identical to the EF schema strings.
    - `PAPEL_OPTIONS` has exactly two entries (Recrutador, Administrador).
    - `npm run lint` does not raise new tsc errors (baseline stays ≤104).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "usuarioRhSchemas" | grep -qx 0 && echo "no tsc errors in schema"</automated>
  </verify>
  <done>usuarioRhSchemas.ts exports novoUsuarioSchema, editarPapelSchema, PAPEL_OPTIONS, and inferred types; validation messages mirror the EF verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: usuariosRhService — allowlist roster read + 5 EF-backed writes (test-first)</name>
  <files>src/features/admin/services/usuariosRhService.ts, src/features/admin/services/__tests__/usuariosRhService.test.ts</files>
  <read_first>
    - src/features/decisao/services/decisaoService.ts:40-54 (custom service error class + code union), :76-123 (safeParse-before-invoke → invoke → extractEfErrorCode → ok===false branch), :262-268 (namespaced export object)
    - src/features/triagem/services/triagemService.ts:245-284 (code-SPECIFIC branch — mirror for LAST_ADMIN / EMAIL_EXISTS / EMAIL_SEND_FAILED)
    - src/features/admin/bias-audit/services/biasAuditService.ts:24-25,59-67 (allowlist `const COLUMNS='…'` idiom + .order)
    - src/lib/efErrors.ts:38-68 (extractEfErrorCode — use verbatim, do not re-implement)
    - src/features/cadastro/__tests__/submitCandidaturaContract.test.ts (precedent for a client↔EF contract-drift test importing the shared schema)
  </read_first>
  <behavior>
    - listUsuariosRh(): calls supabase.from('usuarios_rh').select(USUARIOS_RH_LIST_COLUMNS).order('nome_completo') and returns typed UsuarioRhRow[]; on error throws UsuariosRhServiceError('DATABASE_ERROR').
    - The select argument is the exact allowlist string 'id, user_id, nome_completo, email, cargo, role, ativo, primeiro_acesso, data_ultimo_login' and NEVER '*'.
    - criarUsuario(input): safeParse under novoUsuarioSchema BEFORE invoke; invoke('gerenciar-usuario-rh',{ body:{ action:'criar', ...input } }); on { ok:true } resolves { ok:true, warning? } (warning from data.warning); on EMAIL_SEND_FAILED warning it RESOLVES (success-with-warning), does NOT throw.
    - criarUsuario on EF { ok:false } throws UsuariosRhServiceError carrying error_code (and field) on .details — EMAIL_EXISTS surfaces error_code='EMAIL_EXISTS' + field='email'.
    - mudarPapel(targetId,novoPapel): invoke action:'mudar_papel' with target_id+novo_papel.
    - ativarDesativar(targetId,ativar:boolean): invoke action:'ativar' when ativar=true else 'desativar' with target_id.
    - resetarSenha(targetId): invoke action:'resetar_senha' with target_id.
    - Every error_code (LAST_ADMIN/EMAIL_EXISTS/VALIDATION/FORBIDDEN/NOT_FOUND/SERVER_ERROR) round-trips to .details.error_code; transport `error` also yields error_code via extractEfErrorCode.
    - CONTRACT-DRIFT guard: a body built from a valid NovoUsuarioForm ({action:'criar',...}) passes gerenciarUsuarioRhSchema.safeParse().success === true (import the EF _shared schema).
  </behavior>
  <action>
    Write the test FIRST (RED) in `usuariosRhService.test.ts`, mocking `@/lib/supabase` (`supabase.from().select().order()` and `supabase.functions.invoke`) — enumerate every case in the behavior block, including the allowlist-string assertion (assert `.select` was called with USUARIOS_RH_LIST_COLUMNS, never '*') and the contract-drift `safeParse` guard importing `supabase/functions/_shared/usuario-rh-schemas.ts`. Then implement `usuariosRhService.ts` (GREEN): export `UsuariosRhServiceError` (code union INVALID_INPUT|NETWORK_ERROR|DATABASE_ERROR|NOT_FOUND|UNAUTHORIZED, error_code carried on `details`), `UsuarioRhRow` type (the 9 allowlisted columns, typed from database.types Row), `USUARIOS_RH_LIST_COLUMNS`, `listUsuariosRh`, and the 4 write functions per the interfaces contract. Writes call `extractEfErrorCode(data,error)` and branch: transport `error` → throw NETWORK_ERROR(details.error_code); `data.ok===false` → throw carrying error_code (+ field for EMAIL_EXISTS); `data.ok===true` → resolve (criar returns { ok:true, warning:data.warning, data:data.data }; others return { ok:true }). EMAIL_SEND_FAILED is a resolve-with-warning, NOT a throw. safeParse the create input under novoUsuarioSchema before invoke (INVALID_INPUT on failure). Export a namespaced `usuariosRhService` object. No fenced code — mirror decisaoService.ts:76-123 shape.
  </action>
  <acceptance_criteria>
    - `npm run test:run src/features/admin/services/__tests__/usuariosRhService.test.ts` passes with assertions covering: allowlist string (not '*'), each of the 5 actions' invoke body, each error_code → .details.error_code, EMAIL_SEND_FAILED resolves (no throw), and the safeParse contract-drift guard.
    - A grep for `select('*')` / `.select("*")` in usuariosRhService.ts returns 0.
    - No `as never` casts; `UsuarioRhRow` typed from database.types.ts Row.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/features/admin/services/__tests__/usuariosRhService.test.ts</automated>
  </verify>
  <done>Service test GREEN; usuariosRhService exposes list + 5 writes; allowlist projection enforced; every error_code mapped; EMAIL_SEND_FAILED is success-with-warning; contract-drift guard passes.</done>
</task>

<task type="auto">
  <name>Task 3: useUsuariosRh — query + 4 mutations that invalidate the list</name>
  <files>src/features/admin/hooks/useUsuariosRh.ts</files>
  <read_first>
    - src/features/admin/bias-audit/hooks/useBiasAudit.ts:21-24 (hierarchical keys), :27-35 (useQuery staleTime 5min/retry 2), :38-52 (mutation → onSuccess(toast+invalidate) → onError(toast))
    - src/features/decisao/hooks/useRegistrarDecisao.ts:32-52 (invalidate on success), src/features/decisao/hooks/useConsolidacao.ts:27-31 (v5 useQuery has no onError — component renders error)
    - src/features/admin/services/usuariosRhService.ts (this plan — the functions to wrap)
  </read_first>
  <action>
    Create `useUsuariosRh.ts`. Export `usuariosRhKeys = { all:['usuarios-rh'] as const, list: () => [...all,'list'] as const }`. Export `useUsuariosRh()` → useQuery({ queryKey: usuariosRhKeys.list(), queryFn: listUsuariosRh, staleTime: 5*60*1000, gcTime: 10*60*1000, retry: 2 }). Export four mutation hooks: `useCriarUsuario`, `useMudarPapel`, `useAtivarDesativar`, `useResetarSenha`, each getting queryClient. On success ALL call `invalidateQueries({ queryKey: usuariosRhKeys.list() })` (simple refetch — the anti-lockout count must reflect server truth, per UI-SPEC §States). For useCriarUsuario onSuccess: if result.warning==='EMAIL_SEND_FAILED' → toast.warning(the UI-SPEC EMAIL_SEND_FAILED copy) else toast.success(the create-success copy); leave EMAIL_EXISTS/field routing to the dialog (do NOT toast on criar onError — the dialog owns create errors via mutateAsync try/catch). For the row mutations, onSuccess → toast.success (Papel atualizado. / Usuário desativado. / Usuário reativado. / E-mail de redefinição enviado…) and onError → read err.details?.error_code and toast.error: LAST_ADMIN → "Não é possível remover o último administrador ativo do sistema."; else the generic SERVER_ERROR copy. Use the EXACT UI-SPEC §Copywriting strings. Note: mutation success toasts that need the target email/name are parameterized by the caller (pass through variables), so keep copy building in the component where the name is known where necessary — otherwise use the plain success verbs.
  </action>
  <acceptance_criteria>
    - `usuariosRhKeys` is hierarchical (all → list).
    - `useUsuariosRh` uses staleTime 5*60*1000 and retry 2.
    - Every mutation's onSuccess calls invalidateQueries(usuariosRhKeys.list()).
    - `npm run test:run` (existing suite) stays green; `npm run lint` tsc ≤104.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/features/admin/services/__tests__/usuariosRhService.test.ts && npx tsc --noEmit 2>&1 | grep -c "error TS" | awk '{ if ($1<=104) print "tsc ok: "$1; else { print "tsc regressed: "$1; exit 1 } }'</automated>
  </verify>
  <done>useUsuariosRh query + 4 mutations exported; all invalidate the list on success; LAST_ADMIN mapped on row mutations; criar errors deferred to the dialog; tsc ≤104.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → usuarios_rh (RLS) | The roster read crosses into internal PII; RLS is admin-only but is row-level, NOT column-level |
| client → gerenciar-usuario-rh EF | All privileged writes cross here; the EF authenticates-THEN-authorizes (Phase 28) — the client is untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Information Disclosure | listUsuariosRh projection | mitigate | Explicit allowlist `USUARIOS_RH_LIST_COLUMNS` (9 cols); NEVER `select('*')` — RLS does not hide columns ([[reference_select_star_leaks_pii]]); test asserts the string + a grep guard forbids '*' |
| T-29-02 | Elevation of Privilege | client write path | mitigate | Zero direct usuarios_rh writes; ALL 5 actions go through the EF (authenticate-THEN-authorize, Phase 28); no service_role in the client bundle (project invariant) |
| T-29-03 | Tampering | create/edit body | mitigate | Client safeParse under novoUsuarioSchema (papel enum-constrained to {recrutador,administrador}) BEFORE invoke; the EF re-validates with `.strict()` — client validation is UX, server is authoritative |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies introduced this plan (all primitives + zod + TanStack already vendored) — supply-chain surface unchanged |
</threat_model>

<verification>
- `npm run test:run -- src/features/admin/services/__tests__/usuariosRhService.test.ts` GREEN (allowlist + every error_code + EMAIL_SEND_FAILED warning + contract-drift guard).
- `grep -RnE "select\\(['\"]\\*['\"]\\)" src/features/admin/services/usuariosRhService.ts` → 0 matches.
- `npx tsc --noEmit` error count ≤ 104 (frozen baseline).
- Full `npm run test:run` stays green.
</verification>

<success_criteria>
- The console has a typed, contract-safe data layer: roster read (allowlist) + 5 EF-backed writes + TanStack hooks that invalidate on success.
- Every EF error_code is normalized to a client outcome; EMAIL_SEND_FAILED is success-with-warning.
- Client create body provably parses under the EF's own schema (no drift).
- tsc ≤104, full suite green.
</success_criteria>

<output>
Create `.planning/phases/29-console-de-gest-o-de-usu-rios-rh/29-01-SUMMARY.md` when done.
</output>
