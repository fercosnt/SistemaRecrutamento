---
phase: 29-console-de-gest-o-de-usu-rios-rh
plan: 04
type: execute
wave: 3
depends_on: ["29-01", "29-02", "29-03"]
files_modified:
  - src/features/admin/components/GestaoUsuariosPage.tsx
  - src/components/pages/ConfiguracoesPage.tsx
  - src/features/admin/components/__tests__/GestaoUsuariosPage.test.tsx
autonomous: true
requirements: [USR-01, USR-02]

must_haves:
  truths:
    - "Opening /rh/configuracoes shows the real console: header 'Gestão de usuários' + subtitle, a 'Novo usuário' CTA, and the roster (replacing the old empty-state) (USR-01)"
    - "Loading → skeleton, error → retry, empty → defensive block, success → roster — never a blank screen (AsyncState 5-state)"
    - "The 'Novo usuário' CTA opens the create dialog end-to-end (USR-02)"
    - "The route + RoleGuard role='administrador' are preserved (defense-in-depth over the EF server authz); exactly one RHLayout owner (no double-nest)"
  artifacts:
    - path: "src/features/admin/components/GestaoUsuariosPage.tsx"
      provides: "Console body: useUsuariosRh() → AsyncState → UsuariosRhTable; header + 'Novo usuário' CTA owning NovoUsuarioDialog open state"
      min_lines: 60
      exports: ["GestaoUsuariosPage"]
    - path: "src/components/pages/ConfiguracoesPage.tsx"
      provides: "Route host: keeps RHLayout + route + RoleGuard; renders <GestaoUsuariosPage/> in place of the empty-state (single RHLayout owner)"
    - path: "src/features/admin/components/__tests__/GestaoUsuariosPage.test.tsx"
      provides: "RTL: roster render (mocked service) + loading/error/empty AsyncState states + CTA opens the create dialog; RoleGuard-preserved source assertion"
  key_links:
    - from: "src/features/admin/components/GestaoUsuariosPage.tsx"
      to: "useUsuariosRh() + AsyncState + UsuariosRhTable"
      via: "query state gates the roster; never a blank surface"
      pattern: "AsyncState"
    - from: "src/components/pages/ConfiguracoesPage.tsx"
      to: "GestaoUsuariosPage"
      via: "renders the console in place of the empty-state, RHLayout kept single-owner"
      pattern: "GestaoUsuariosPage"
    - from: "src/router/routes.tsx (unchanged)"
      to: "ConfiguracoesPage"
      via: "RoleGuard role='administrador' OUTSIDE the lazy element (Phase 19) — preserved"
      pattern: "RoleGuard role=\"administrador\""
---

<objective>
Compose the console and wire it into the live route: `GestaoUsuariosPage` (the roster query gated by `<AsyncState>`, the header + "Novo usuário" CTA owning the create-dialog open state), and the `ConfiguracoesPage` swap that replaces today's empty-state with the real console while keeping the route, `RoleGuard`, and a single `RHLayout` owner intact.

Purpose: deliver the USR-01 landing (the admin opens `/rh/configuracoes` and sees the real roster) and the USR-02 entry point (the CTA opens the create dialog), assembling the 29-02 dialog + 29-03 table + 29-01 data layer.
Output: `GestaoUsuariosPage.tsx`, modified `ConfiguracoesPage.tsx`, and the integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-console-de-gest-o-de-usu-rios-rh/29-UI-SPEC.md
@.planning/phases/29-console-de-gest-o-de-usu-rios-rh/29-PATTERNS.md

**Depends on 29-01/02/03:** composes `useUsuariosRh()` (query), `NovoUsuarioDialog` (29-02), `UsuariosRhTable` (29-03), and `<AsyncState>`. Path: flat `src/features/admin/components/`. DO NOT touch `src/router/routes.tsx` — the route + guard stay as-is; wiring happens inside `ConfiguracoesPage`.

<interfaces>
useUsuariosRh() (29-01): useQuery<UsuarioRhRow[]> → { data, isLoading, isError, refetch }.
NovoUsuarioDialog (29-02): props { open, onOpenChange }.
UsuariosRhTable (29-03): props { rows: UsuarioRhRow[] }.
AsyncState (src/components/ui/AsyncState.tsx:78-101): props { isLoading, isError, isEmpty, onRetry, retrying, glass, copy, children }; precedence loading→slow→error→empty→success; `copy` overrides per-slot.
Current ConfiguracoesPage (src/components/pages/ConfiguracoesPage.tsx): <RHLayout> → header (<h1>Configurações</h1>) → GlassCard empty-state. Route: routes.tsx:60,407-414 (lazyNamed ConfiguracoesPage under RoleGuard role="administrador").

UI-SPEC copy (verbatim):
  page title "Gestão de usuários"; subtitle "Gerencie as contas da equipe de RH — papéis, acesso e redefinição de senha."
  CTA "Novo usuário" (leading UserPlus icon, min-h-[44px]).
  error heading "Não foi possível carregar os usuários." / body "Verifique a conexão e tente novamente." / retry "Tentar novamente".
  empty heading "Nenhum usuário encontrado." / body "Crie o primeiro usuário com o botão 'Novo usuário'."
  page frame: space-y-6; do NOT re-add the legacy p-8 wrapper (RHLayout already pads p-4 lg:p-6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GestaoUsuariosPage — query + AsyncState + header CTA + create dialog owner</name>
  <files>src/features/admin/components/GestaoUsuariosPage.tsx</files>
  <read_first>
    - src/features/admin/bias-audit/components/BiasAuditPage.tsx:86-104 (header flex row + h1 + right-side primary CTA min-h-[44px]) — structural twin
    - src/components/ui/AsyncState.tsx:78-101 (props) + PATTERNS §AsyncState example (copy overrides for the UI-SPEC error/empty strings)
    - src/features/admin/hooks/useUsuariosRh.ts (29-01), src/features/admin/components/UsuariosRhTable.tsx (29-03), src/features/admin/components/NovoUsuarioDialog.tsx (29-02)
  </read_first>
  <action>
    Create `GestaoUsuariosPage.tsx` rendering the console BODY only (NOT its own RHLayout — ConfiguracoesPage owns the shell; single-owner rule). Root `<div className="space-y-6">`. Header: `flex flex-wrap items-center justify-between gap-4` with left `<h1 className="text-3xl md:text-4xl font-semibold text-white">Gestão de usuários</h1>` + subtitle `<p className="text-white/80 text-sm">Gerencie as contas da equipe de RH — papéis, acesso e redefinição de senha.</p>`, and right a primary `Button` "Novo usuário" (leading UserPlus icon, min-h-[44px], aria-label if icon-only) that sets local `open` state true. Consume `const { data, isLoading, isError, refetch } = useUsuariosRh()`. Wrap the roster in `<AsyncState isLoading={isLoading} isError={isError} isEmpty={(data?.length ?? 0) === 0} onRetry={() => refetch()} copy={{ error:{ heading:'Não foi possível carregar os usuários.', generic:'Verifique a conexão e tente novamente.' }, empty:{ heading:'Nenhum usuário encontrado.', body:"Crie o primeiro usuário com o botão 'Novo usuário'." }, retry:{ label:'Tentar novamente' } }}>` around `<UsuariosRhTable rows={data ?? []} />`. Mount `<NovoUsuarioDialog open={open} onOpenChange={setOpen} />`. Do NOT re-add a p-8 wrapper. No fenced code in this action.
  </action>
  <acceptance_criteria>
    - GestaoUsuariosPage does NOT render <RHLayout> (single-owner: ConfiguracoesPage keeps it).
    - The roster is wrapped in <AsyncState> with the UI-SPEC error/empty copy overrides; never a bare blank on loading/error.
    - The "Novo usuário" CTA toggles NovoUsuarioDialog open.
    - `npm run lint` tsc ≤104.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS" | awk '{ if ($1<=104) print "tsc ok: "$1; else exit 1 }'</automated>
  </verify>
  <done>GestaoUsuariosPage composes the query + AsyncState + table + header CTA + create dialog; no RHLayout self-mount; verified by Task 3's integration test.</done>
</task>

<task type="auto">
  <name>Task 2: Wire GestaoUsuariosPage into ConfiguracoesPage (keep route/guard/RHLayout)</name>
  <files>src/components/pages/ConfiguracoesPage.tsx</files>
  <read_first>
    - src/components/pages/ConfiguracoesPage.tsx (current empty-state: RHLayout → header → GlassCard "Gestão de usuários ainda não disponível")
    - src/router/routes.tsx:60,407-414 (RoleGuard role="administrador" OUTSIDE the lazy element — DO NOT touch)
    - src/features/admin/components/GestaoUsuariosPage.tsx (Task 1)
  </read_first>
  <action>
    Modify `ConfiguracoesPage.tsx`: keep `<RHLayout>` as the single shell owner and keep the export name `ConfiguracoesPage` (the route imports it). Remove the legacy header (`<h1>Configurações</h1>`) and the empty-state `GlassCard` body (the `Users` "ainda não disponível" block) and the outer `min-h-screen p-8` wrapper; render `<GestaoUsuariosPage />` inside `<RHLayout>` (the page frame + header now live in GestaoUsuariosPage). Do NOT modify `src/router/routes.tsx`, `RoleGuard`, or the sidebar nav label ("Configurações" stays — out of scope). Drop the now-unused `GlassCard`/`Users` imports. No fenced code in this action.
  </action>
  <acceptance_criteria>
    - `src/router/routes.tsx` is NOT in this plan's diff; `grep -n 'RoleGuard role="administrador"' src/router/routes.tsx` still matches for the /rh/configuracoes block.
    - ConfiguracoesPage renders exactly one <RHLayout> and <GestaoUsuariosPage/>; the "ainda não disponível" empty-state string is gone.
    - No dangling unused imports (tsc clean).
    - `npm run lint` tsc ≤104.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'RoleGuard role="administrador"' src/router/routes.tsx && grep -q "GestaoUsuariosPage" src/components/pages/ConfiguracoesPage.tsx && ! grep -q "ainda não disponível" src/components/pages/ConfiguracoesPage.tsx && echo "wired + guard preserved"</automated>
  </verify>
  <done>ConfiguracoesPage renders the real console under one RHLayout; route + RoleGuard untouched; empty-state removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GestaoUsuariosPage integration test — states + CTA + guard assertion</name>
  <files>src/features/admin/components/__tests__/GestaoUsuariosPage.test.tsx</files>
  <read_first>
    - src/features/admin/services/__tests__/usuariosRhService.test.ts (29-01 — service mock pattern) + src/features/admin/components/__tests__/UsuariosRhTable.test.tsx (29-03 — render-harness / QueryClient wrapper idiom)
    - src/components/ui/AsyncState.tsx:127 (state precedence to assert)
  </read_first>
  <behavior>
    - With a mocked useUsuariosRh returning a roster → the table rows render (nome/email visible) under the "Gestão de usuários" header.
    - isLoading=true → an AsyncState loading surface renders (skeleton), NOT the table and NOT a blank node.
    - isError=true → the error heading "Não foi possível carregar os usuários." + a "Tentar novamente" control that calls refetch.
    - isEmpty (data=[]) → the defensive empty block "Nenhum usuário encontrado." (never a blank surface).
    - Clicking "Novo usuário" opens the NovoUsuarioDialog (dialog title "Novo usuário" appears).
    - Guard preserved: a source assertion that routes.tsx still wraps ConfiguracoesPage in RoleGuard role="administrador" (read the file, assert the substring) — the console is defense-in-depth over the EF server authz.
  </behavior>
  <action>
    Write `GestaoUsuariosPage.test.tsx` (RTL) mocking `useUsuariosRh` (toggle data/isLoading/isError/isEmpty across cases) and rendering `GestaoUsuariosPage` inside a QueryClientProvider (reuse the 29-03 test harness). Assert every behavior-block case. For the guard assertion, read `src/router/routes.tsx` via fs (or import a routes snapshot) and assert it contains `RoleGuard role="administrador"` around the ConfiguracoesPage route — this locks the defense-in-depth invariant without modifying the route. Keep the NovoUsuarioDialog interaction to opening the dialog (the create round-trip is covered by 29-02). No fenced code in this action.
  </action>
  <acceptance_criteria>
    - `npm run test:run -- src/features/admin/components/__tests__/GestaoUsuariosPage.test.tsx` passes: roster render, loading, error+retry, empty, CTA-opens-dialog, and the RoleGuard source assertion.
    - No blank-surface path (loading/error/empty each render visible copy).
    - `npm run lint` tsc ≤104; full `npm run test:run` green; build passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/features/admin/components/__tests__/GestaoUsuariosPage.test.tsx</automated>
  </verify>
  <done>Integration test GREEN across all 5 AsyncState/CTA cases + the RoleGuard-preserved assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route → ConfiguracoesPage | RoleGuard(administrador) gates the UI; but it is UX only — the EF re-authorizes every write server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-11 | Elevation of Privilege (client role trust) | RoleGuard + console | mitigate | RoleGuard role="administrador" is preserved as defense-in-depth ONLY; NO real privilege is gated on client role — every write goes through the Phase-28 EF which authenticates-THEN-authorizes; a spoofed client role still cannot write (server denies) |
| T-29-12 | Denial of Service (blank screen / silent failure) | roster load | mitigate | <AsyncState> renders loading/error/empty/success deterministically with retry — never a blank surface or a fabricated roster; the test asserts each state |
| T-29-13 | Tampering (route/guard regression) | ConfiguracoesPage wire | mitigate | routes.tsx is out of this plan's diff; the test asserts RoleGuard role="administrador" still wraps the route; single RHLayout owner prevents shell double-nest |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (AsyncState, TanStack, shadcn already vendored) |
</threat_model>

<verification>
- `npm run test:run -- src/features/admin/components/__tests__/GestaoUsuariosPage.test.tsx` GREEN (5 state/CTA cases + guard assertion).
- `grep -q 'RoleGuard role="administrador"' src/router/routes.tsx` → present (guard preserved; route untouched).
- `grep -q "ainda não disponível" src/components/pages/ConfiguracoesPage.tsx` → absent (empty-state replaced).
- Exactly one `<RHLayout>` in the render tree (ConfiguracoesPage owns it; GestaoUsuariosPage does not mount it).
- `npx tsc --noEmit` ≤104; full `npm run test:run` green; `npm run build` succeeds; axe Tier-A green.
</verification>

<success_criteria>
- `/rh/configuracoes` shows the real console (header + CTA + roster), replacing the empty-state (USR-01).
- The "Novo usuário" CTA opens the create dialog (USR-02 entry point).
- Loading/error/empty/success are all handled (no blank screen); route + RoleGuard preserved; single RHLayout owner.
</success_criteria>

<output>
Create `.planning/phases/29-console-de-gest-o-de-usu-rios-rh/29-04-SUMMARY.md` when done.
</output>
