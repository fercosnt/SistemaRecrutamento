---
phase: 08-inscri-o-knock-out-etapa-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/__tests__/strict-schema.test.ts
  - src/features/cadastro/services/__tests__/duplicateCheckService.test.ts
  - src/features/config-vaga/templates/__tests__/cargoTemplates.test.ts
  - src/features/config-vaga/__tests__/publishGate.test.ts
  - .planning/phases/08-inscri-o-knock-out-etapa-1/08-SQL-SMOKE-RUNBOOK.md
  - e2e/inscricao-knockout.spec.ts
autonomous: true
requirements: [INSCR-01, INSCR-02, INSCR-03, INSCR-04, LGPD-01]

must_haves:
  truths:
    - "RED unit tests exist that fail against pre-Phase-8 code and will flip green as Plans 02-05 land"
    - "SQL smoke runbook documents the knockout-fires / survivor-passes / single-history-row / seeded-defaults assertions"
    - "Playwright E2E stub exists describing the inscrição → knockout → neutral-message flow"
  artifacts:
    - path: "supabase/functions/_shared/__tests__/strict-schema.test.ts"
      provides: "D-04 forbidden-key rejection RED test (cpf/foto/estado_civil/saude → reject)"
      min_lines: 25
    - path: ".planning/phases/08-inscri-o-knock-out-etapa-1/08-SQL-SMOKE-RUNBOOK.md"
      provides: "Disposable-fixture SQL smoke procedure for the knockout sweep"
      min_lines: 40
    - path: "e2e/inscricao-knockout.spec.ts"
      provides: "E2E skeleton for knockout + feedback_rejeicao display"
      min_lines: 20
  key_links:
    - from: "src/features/config-vaga/templates/__tests__/cargoTemplates.test.ts"
      to: "src/features/config-vaga/templates/cargoTemplates.ts"
      via: "import + assert qualificacao seed"
      pattern: "qualificacao"
---

<objective>
Wave 0: author the RED test scaffolds + SQL smoke runbook that make every Phase 8 behavior detectable BEFORE implementation. This establishes the smoke-runtime gate (Phase 4.1 / Phase 5 precedent) for the inscrição + knockout flow.

Purpose: tests calibrated against pre-Phase-8 code FAIL now and flip GREEN as Plans 02–05 implement the behavior. The failure IS the assertion that the behavior is missing.
Output: 2 new test files, 2 extended test files, 1 SQL smoke runbook, 1 E2E spec stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-inscri-o-knock-out-etapa-1/08-RESEARCH.md
@.planning/phases/08-inscri-o-knock-out-etapa-1/08-VALIDATION.md
@.planning/phases/08-inscri-o-knock-out-etapa-1/08-PATTERNS.md

<interfaces>
From supabase/functions/_shared/schemas.ts:
- `submitCandidaturaSchema` (z.object, L199) — currently NO .strict()
- `cadastroCandidatoSchema` (z.object, L115) — currently has `cpf` (L126, required) + `genero` (L132, optional)
- `validateCPF(cpf: string): boolean` (L35)

From src/features/cadastro/services/duplicateCheckService.ts:
- `checkEmailDuplicate(email: string): Promise<DuplicateCheckResult>` (L311) — calls RPC with p_cpf=''
- `checkCPFDuplicate(cpf: string)` (L259) — to be deprecated from cadastro flow
- RPC args shape: `{ p_cpf: string; p_email: string }` (L187)

From src/features/config-vaga/templates/cargoTemplates.ts:
- `cargoTemplates: Record<CargoSlug, CargoTemplate>` (L67) — CargoTemplate has NO `qualificacao` field yet
- `getCargoTemplateDefaults(slug): VagaConfig` (L163) — deep-copy helper

From src/features/config-vaga/publishGate.ts:
- existing client-side publish gate (mirror of publish_vaga RPC D-12 conditions)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED unit scaffolds — .strict() allowlist + cadastro dedup/schema</name>
  <read_first>
    - supabase/functions/_shared/schemas.ts (submitCandidaturaSchema L199, cadastroCandidatoSchema L115, cpf L126, genero L132)
    - supabase/functions/_shared/__tests__/ (existing test layout to mirror naming/imports)
    - src/features/cadastro/services/__tests__/duplicateCheckService.test.ts (existing cases to extend)
    - src/features/cadastro/schemas/candidatoSchema.ts (cpfSchema L24, cpf L165, genero L169)
  </read_first>
  <action>
    Create supabase/functions/_shared/__tests__/strict-schema.test.ts. Import `cadastroCandidatoSchema` and `submitCandidaturaSchema` from ../schemas.ts. Write cases asserting D-04 forbidden-field rejection: parsing an object containing an unknown key `cpf` (and separately `foto`, `estado_civil`, `saude`) must FAIL with a Zod error (`.success === false`). These cases FAIL today (neither schema has `.strict()` yet — extra keys are silently stripped). Also assert the allowlist positive case: a payload with exactly the INSCR-01-valid keys parses successfully. Name the describe block "D-04 .strict() forbidden-field allowlist (LGPD-01)".

    Extend src/features/cadastro/services/__tests__/duplicateCheckService.test.ts with a describe "D-03 email-only dedup": assert that the cadastro flow's email check path calls the RPC with `p_cpf` empty string (the existing `checkEmailDuplicate` already passes p_cpf=''; the new RED assertion is that the cadastro flow no longer invokes `checkCPFDuplicate`). Since the consumer flip lands in Plan 02, write the assertion against the intended post-Plan-02 contract so it is RED now. Mark with a comment `// RED until Plan 08-02 drops the cpf invocation`.

    Extend src/features/cadastro/schemas tests (create src/features/cadastro/schemas/__tests__/candidatoSchema.test.ts if absent): assert that `candidatoSchema` parses a DadosPessoais object that OMITS `cpf` and `genero` successfully. This FAILS today because cpf (cpfSchema L165) and genero (z.enum L169) are required. Comment `// RED until Plan 08-02 makes cpf/genero non-collected`.

    Do NOT add `.strict()` or edit production schemas in this task — only the failing tests.
  </action>
  <verify>
    <automated>npm run test:run -- supabase/functions/_shared/__tests__/strict-schema.test.ts 2>&1 | grep -E "fail|✗|FAIL" ; echo "RED expected"</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/functions/_shared/__tests__/strict-schema.test.ts exists and imports from ../schemas.ts
    - Running the strict-schema test produces FAILING assertions for the cpf/foto/estado_civil/saude forbidden-key cases (RED — proves .strict() not yet applied)
    - src/features/cadastro/services/__tests__/duplicateCheckService.test.ts contains a "D-03 email-only dedup" describe block
    - A candidatoSchema test asserts parse-success when cpf and genero are omitted (RED today)
    - `grep -c "\.strict()" supabase/functions/_shared/schemas.ts` returns 0 (no production schema edited in this task)
  </acceptance_criteria>
  <done>Three RED unit scaffolds exist and fail against current code; no production schema was modified.</done>
</task>

<task type="auto">
  <name>Task 2: RED template/publish scaffolds + SQL smoke runbook + E2E stub</name>
  <read_first>
    - src/features/config-vaga/templates/cargoTemplates.ts (CargoTemplate L33, cargoTemplates L67, getCargoTemplateDefaults L163)
    - src/features/config-vaga/templates/__tests__/cargoTemplates.test.ts (existing cases)
    - src/features/config-vaga/__tests__/publishGate.test.ts (existing cases)
    - supabase/migrations/20260607010004_publish_vaga_rpc.sql (D-12 gate conditions to mirror)
    - .planning/phases/07-configura-o-de-vaga-tags/ for the Phase 7 SQL smoke runbook idiom (disposable fixture + set_config request.jwt.claims, ROLLBACK-free cleanup)
    - .planning/phases/08-inscri-o-knock-out-etapa-1/08-RESEARCH.md (Validation Architecture → Test Map; Pattern 2 knockout sweep SQL)
  </read_first>
  <action>
    Extend src/features/config-vaga/templates/__tests__/cargoTemplates.test.ts with a describe "INSCR-03 / D-14 default knockouts": assert that EVERY cargoTemplates entry carries a `qualificacao` array containing a presencial-SP knockout question (text "Você tem disponibilidade para trabalhar presencialmente em São Paulo, perto dos metros Brigadeiro e Paraíso?") whose "Não" option is tagged `knockout`; and that ONLY the `dentista` template additionally carries a harmonização-orofacial knockout question. These FAIL today (CargoTemplate has no `qualificacao` field). Comment `// RED until Plan 08-03 adds qualificacao seed`.

    Extend src/features/config-vaga/__tests__/publishGate.test.ts with a describe "INSCR-02 / D-09 qualification gate": assert the client publish gate rejects when a vaga's Etapa-1 qualification block has >10 perguntas OR >1 open-ended (resposta_texto) pergunta. RED today (gate has no such check). Comment `// RED until Plan 08-03 extends the gate`.

    Create .planning/phases/08-inscri-o-knock-out-etapa-1/08-SQL-SMOKE-RUNBOOK.md. Mirror the Phase 7 runbook structure: a disposable-fixture procedure (CREATE temp vaga + pergunta + pergunta_opcao_metadata with tag='knockout' + candidato) using `set_config('request.jwt.claims', ...)` to simulate candidato/RH, ROLLBACK-free cleanup. Document FOUR named smokes with exact expected outcomes: SMOKE-1 knockout-fires (answer "Não" → candidatura `status='rejeitado'`, `etapa_atual='inscricao'`, `motivo_rejeicao='knockout_automatico'`, `opcao_knockout_id` set); SMOKE-2 survivor-passes (answer "Sim" → `etapa_atual='triagem'`, status NOT rejeitado); SMOKE-3 single-history-row (exactly ONE historico_candidatura row with `auto_rejeitado=true`, `ator IS NULL` after a knockout — no double-write from avancar_etapa trigger); SMOKE-4 seeded-defaults (presencial-SP fires for any cargo; harmonização only for dentista). Include the exact texto-join predicate `r.resposta_opcoes @> to_jsonb(m.opcao_texto)` and an [VERIFY LIVE — A4] note to re-confirm `resposta_opcoes` shape via Supabase MCP execute_sql before the RPC is locked in Plan 04.

    Create e2e/inscricao-knockout.spec.ts as a Playwright skeleton (using test.fixme for the live-dependent steps, mirroring Phase 4 candidatura-submit.spec patterns): scenario "candidate answers a knockout option → sees neutral D-15 message inline, then sees feedback_rejeicao on /perfil". Assert the neutral message text "Após análise dos requisitos da vaga, não seguiremos com sua candidatura neste momento." appears and that NO knockout criterion text is rendered.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/config-vaga 2>&1 | grep -E "fail|✗|FAIL" ; test -f .planning/phases/08-inscri-o-knock-out-etapa-1/08-SQL-SMOKE-RUNBOOK.md && test -f e2e/inscricao-knockout.spec.ts && echo "files exist"</automated>
  </verify>
  <acceptance_criteria>
    - cargoTemplates.test.ts contains an "INSCR-03 / D-14 default knockouts" describe that FAILS today (no qualificacao field)
    - publishGate.test.ts contains an "INSCR-02 / D-09 qualification gate" describe that FAILS today
    - 08-SQL-SMOKE-RUNBOOK.md documents SMOKE-1..SMOKE-4 with exact expected `status`/`etapa_atual`/`motivo_rejeicao`/`auto_rejeitado` values and the `@> to_jsonb(m.opcao_texto)` predicate
    - 08-SQL-SMOKE-RUNBOOK.md carries the [VERIFY LIVE — A4] note for the resposta_opcoes shape
    - e2e/inscricao-knockout.spec.ts exists with the neutral-message assertion and no-criterion-leak assertion
    - `npm run test:e2e -- e2e/inscricao-knockout.spec.ts --list` parses the spec without error
  </acceptance_criteria>
  <done>Template + publish-gate RED scaffolds fail against current code; SQL runbook + E2E stub authored with concrete expected outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wave 0 produces test files + a markdown runbook only; no runtime code, no DB writes, no trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-00-FP | Tampering | Test scaffolds asserting wrong contract | accept | RED tests are calibrated against the documented Plan 02–05 contracts; any miscalibration surfaces as a non-flipping test in later waves (caught at wave merge). No production surface touched. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Phase 8 installs ZERO new packages (RESEARCH Package Legitimacy Audit). No supply-chain checkpoint needed. |
</threat_model>

<verification>
Run `npm run test:run -- supabase/functions/_shared/__tests__/strict-schema.test.ts src/features/config-vaga` — the new Phase 8 assertions are RED (expected). The pre-existing suite (395 cases) must remain otherwise unchanged. `npm run test:e2e -- e2e/inscricao-knockout.spec.ts --list` parses.
</verification>

<success_criteria>
- 2 new test files + 2 extended test files + 1 SQL runbook + 1 E2E spec exist
- All NEW Phase 8 assertions are RED (fail against current code)
- No production source file modified
- `wave_0_complete: true` can be set in 08-VALIDATION.md after this plan
</success_criteria>

<output>
Create `.planning/phases/08-inscri-o-knock-out-etapa-1/08-01-SUMMARY.md` when done.
</output>
