---
phase: 08-inscri-o-knock-out-etapa-1
plan: 05
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - src/components/pages/FormularioCandidaturaPage.tsx
  - src/components/pages/MeuPerfilCandidatoPage.tsx
  - src/components/pages/DashboardCandidatoPage.tsx
  - supabase/functions/submit-candidatura/index.ts
  - e2e/inscricao-knockout.spec.ts
autonomous: true
requirements: [INSCR-04, LGPD-01]

must_haves:
  truths:
    - "A knocked-out candidate sees the D-15 neutral message inline immediately post-submit (no criterion exposed, calm/muted tone)"
    - "A survivor sees the normal 'Candidatura enviada!' confirmation"
    - "feedback_rejeicao renders below the rejeitado status badge on /perfil and the dashboard"
    - "The knockout criterion (opcao_knockout_id) is never rendered"
    - "Implements CONTEXT decision D-16 (rejection message visible inline post-submit AND persisted in feedback_rejeicao, surfaced on dashboard /perfil)"
  artifacts:
    - path: "src/components/pages/FormularioCandidaturaPage.tsx"
      provides: "post-submit branch on status==='rejeitado' → inline neutral result; survivor → success"
      contains: "rejeitado"
    - path: "src/components/pages/MeuPerfilCandidatoPage.tsx"
      provides: "feedback_rejeicao body rendered below the rejeitado badge"
      contains: "feedback_rejeicao"
  key_links:
    - from: "src/components/pages/FormularioCandidaturaPage.tsx"
      to: "submit_candidatura_atomic RPC return"
      via: "branch on response.status"
      pattern: "status"
    - from: "src/components/pages/MeuPerfilCandidatoPage.tsx"
      to: "candidaturas.feedback_rejeicao"
      via: "conditional render below Badge"
      pattern: "feedback_rejeicao"
---

<objective>
Wire the candidate-facing result of the knockout (INSCR-04, D-15, D-16): branch the post-submit flow on the rejection state returned by the RPC, show the neutral D-15 message inline immediately for a knocked-out candidate (vs. the normal success confirmation for a survivor), and render the persisted `feedback_rejeicao` below the existing `rejeitado` badge on `/perfil` + the dashboard. The criterion is NEVER exposed.

Purpose: dual visibility (inline at submit + persisted on dashboard) per D-16; dignified/calm rejection tone per D-15 + UI-SPEC.
Output: 3 page edits + EF passthrough + the E2E spec promoted from stub to live; Plan 01 E2E assertions exercised.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-inscri-o-knock-out-etapa-1/08-UI-SPEC.md
@.planning/phases/08-inscri-o-knock-out-etapa-1/08-PATTERNS.md

<interfaces>
From src/components/pages/FormularioCandidaturaPage.tsx:
- `submitCandidaturaWithRespostas` (L61) → currently always toast.success + navigate('/candidato/perfil') (L331-332)
- `toast` from 'sonner' (L32 — NEVER versioned import, Pitfall 2)
- `navigate` (L96)

From src/components/pages/MeuPerfilCandidatoPage.tsx:
- `getStatusBadge(status)` (L323) — rejeitado branch L337-339
- Badge render L683-685 (and L734-736)
- `STATUS_CANDIDATURA_LABELS` from '@/features/vagas/types/vagasTypes' (L16)
- candidatura object carries `feedback_rejeicao` (string | null, per database.types.ts L455)

From src/components/pages/DashboardCandidatoPage.tsx:
- `getStatusInfo(status)` (L64), rejeitado case L72
- candidatura render loop L249 (`candidatura.status_candidatura`)

From supabase/functions/submit-candidatura/index.ts:
- two-client EF; passes RPC result through to the client; n8n webhook fires after commit (~L289)

UI-SPEC copy (08-UI-SPEC.md):
- knockout message (D-15 LOCKED): "Após análise dos requisitos da vaga, não seguiremos com sua candidatura neste momento."
- optional warm closer (dashboard): "Agradecemos seu interesse na Beauty Smile."
- success heading: "Candidatura enviada!" / body: "Recebemos sua candidatura. Em breve nossa equipe entrará em contato sobre os próximos passos."
- rejection surface: calm/muted glass, NOT red-on-red; never expose criterion; Display 28px/600; Body 16px/400
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EF passthrough + FormularioCandidaturaPage rejection/survivor branch</name>
  <read_first>
    - src/components/pages/FormularioCandidaturaPage.tsx (submit handler L320-340, toast.success/navigate L331-332)
    - supabase/functions/submit-candidatura/index.ts (RPC call + return passthrough; n8n webhook ~L289)
    - .planning/phases/08-inscri-o-knock-out-etapa-1/08-UI-SPEC.md (surfaces 3 & 5, color/typography rules)
    - src/components/ui/glass.tsx (GlassCard, GlassButton)
    - src/features/vagas/hooks (the submitCandidaturaWithRespostas wrapper return type — confirm it now surfaces status/etapa_atual from the RPC)
  </read_first>
  <action>
    In supabase/functions/submit-candidatura/index.ts: ensure the RPC return (now including `status` and `etapa_atual` from Plan 04) is passed through in the EF success response so the client can branch. Do NOT suppress the n8n `nova-candidatura` webhook for knocked-out rows in V1 — A5 resolution: fire it for all candidaturas (RH may want the record); add a one-line comment documenting that knocked-out candidaturas also fire the webhook. Keep the two-client pattern + IDOR cross-check + path check unchanged.

    In FormularioCandidaturaPage.tsx submit handler (L320-340): after a successful `submitCandidaturaWithRespostas`, branch on the returned `status`. If `status==='rejeitado'`: render the D-15 inline neutral result state — a calm/muted GlassCard (NOT red alarm) with Display heading + the exact locked message "Após análise dos requisitos da vaga, não seguiremos com sua candidatura neste momento." and a "Voltar" action; do NOT navigate to /perfil automatically and do NOT toast.success. NEVER render the criterion. If survivor (status not rejeitado, etapa advanced to triagem): keep the existing success path — show "Candidatura enviada!" confirmation then navigate('/candidato/perfil'). Introduce a local result state (e.g. `submitResult: 'rejeitado' | 'enviado' | null`) to drive which surface renders. Use only the UI-SPEC tokens (Body 16/400, Display 28/600; muted/neutral surface for rejection; accent/success treatment for survivor). Keep `toast` imported from 'sonner' (never versioned).
  </action>
  <verify>
    <automated>npm run build && grep -c "Após análise dos requisitos da vaga" src/components/pages/FormularioCandidaturaPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - FormularioCandidaturaPage branches on the RPC `status`; rejeitado → inline neutral result, survivor → success + navigate
    - The exact D-15 locked message string appears in FormularioCandidaturaPage.tsx
    - No criterion/opcao_knockout_id is rendered (grep shows opcao_knockout_id absent from the page)
    - The rejection surface uses muted/neutral tone (no red alarm banner) per UI-SPEC
    - EF passes status/etapa_atual through; n8n webhook still fires for knocked-out rows (documented)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Knockout vs survivor branch wired inline; D-15 message shown; criterion never exposed; build green.</done>
</task>

<task type="auto">
  <name>Task 2: feedback_rejeicao display on /perfil + dashboard + promote E2E spec</name>
  <read_first>
    - src/components/pages/MeuPerfilCandidatoPage.tsx (getStatusBadge L323, Badge render L683-685 + L734-736, STATUS_CANDIDATURA_LABELS L16)
    - src/components/pages/DashboardCandidatoPage.tsx (getStatusInfo L64, rejeitado L72, render loop L249)
    - e2e/inscricao-knockout.spec.ts (Plan 01 stub to promote)
    - .planning/phases/08-inscri-o-knock-out-etapa-1/08-UI-SPEC.md (surface 4: feedback below badge, muted tone)
  </read_first>
  <action>
    In MeuPerfilCandidatoPage.tsx: below the existing rejeitado Badge (L683-685, and the L734-736 surface if it shows the same candidatura), conditionally render `candidatura.feedback_rejeicao` when `candidatura.status === 'rejeitado' && candidatura.feedback_rejeicao` — a muted-foreground 16px paragraph (text-base text-muted-foreground), optionally followed by the warm closer "Agradecemos seu interesse na Beauty Smile." Never render the criterion. Confirm the candidatura query selects feedback_rejeicao (it exists on the type; ensure the select list includes it — if the service uses an explicit column list, add feedback_rejeicao).

    In DashboardCandidatoPage.tsx: same treatment — render feedback_rejeicao below the status display for rejeitado candidaturas in the render loop (L249). Reuse getStatusInfo for the badge; add the persisted neutral message beneath it.

    If STATUS_CANDIDATURA_LABELS.rejeitado is harsh in candidate-facing copy ("Rejeitada"), confirm the neutral wording per UI-SPEC (label "Não selecionada"); if the shared label is RH-facing, do NOT change it globally — render the candidate-facing neutral phrasing locally without altering the shared label used by RH screens.

    Promote e2e/inscricao-knockout.spec.ts from stub: implement the scenario asserting (a) a knockout answer yields the inline neutral message, (b) /perfil shows feedback_rejeicao, (c) NO criterion text leaks. Use test.fixme only for steps requiring a live seeded knockout vaga if the fixture is not yet available; keep the neutral-message + no-leak assertions runnable where possible.
  </action>
  <verify>
    <automated>npm run test:run -- src/components/pages 2>/dev/null; npm run build && npm run test:e2e -- e2e/inscricao-knockout.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "feedback_rejeicao" src/components/pages/MeuPerfilCandidatoPage.tsx` returns ≥ 1 (read+render, not just the type)
    - DashboardCandidatoPage renders feedback_rejeicao below the rejeitado status
    - Neither page renders opcao_knockout_id or any criterion text
    - The candidatura select includes feedback_rejeicao (renders non-empty for a rejeitado row)
    - e2e/inscricao-knockout.spec.ts parses and contains the neutral-message + no-leak assertions
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>feedback_rejeicao renders below the rejeitado badge on both surfaces with neutral tone; criterion never shown; E2E spec promoted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RPC/EF response → candidate browser render | The response carries status + feedback_rejeicao; the UI must render ONLY the neutral message, never the criterion |
| persisted candidaturas.feedback_rejeicao → /perfil + dashboard read | Candidate reads own row only (RLS); the rendered text must not expose opcao_knockout_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 (HIGH) | Info Disclosure (LGPD) | UI leaks the knockout criterion (which answer fired) | mitigate | Both surfaces render ONLY feedback_rejeicao (the single D-15 neutral message); opcao_knockout_id is never sent to / rendered by the client (Task 1 + Task 2 grep-asserted absent). E2E asserts no-leak. |
| T-08-14 | Spoofing / Access Control | Candidate reads another candidate's feedback_rejeicao | mitigate | RLS on candidaturas restricts reads to own rows (already live, Phase 6); no change to the query's row scope. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase. |
</threat_model>

<verification>
`npm run build` exit 0; `npm run test:run -- src/components/pages` green; `npm run test:e2e -- e2e/inscricao-knockout.spec.ts --list` parses. Manual (phase UAT): a real knockout answer shows the neutral message inline + on /perfil with no criterion leaked (UI-SPEC neutrality is a manual-only verification per 08-VALIDATION.md).
</verification>

<success_criteria>
- Inline neutral result on knockout; success confirmation on survivor
- feedback_rejeicao rendered on /perfil + dashboard; criterion never shown
- E2E spec promoted with no-leak assertion
- Build green
</success_criteria>

<output>
Create `.planning/phases/08-inscri-o-knock-out-etapa-1/08-05-SUMMARY.md` when done.
</output>
