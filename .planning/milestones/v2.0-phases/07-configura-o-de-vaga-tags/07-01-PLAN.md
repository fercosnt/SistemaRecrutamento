---
phase: 07-configura-o-de-vaga-tags
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/config-vaga/schemas/__tests__/pesosAvaliacaoSchema.test.ts
  - src/features/config-vaga/templates/__tests__/cargoTemplates.test.ts
  - src/features/config-vaga/components/__tests__/TemplateVagaSelector.test.tsx
  - src/features/config-vaga/components/__tests__/PesosSliders.test.tsx
  - src/features/config-vaga/components/__tests__/PerguntaWithTagsForm.test.tsx
  - src/features/config-vaga/components/__tests__/BulkMarkDialog.test.tsx
  - src/features/config-vaga/services/__tests__/configVagaService.test.ts
  - src/features/config-vaga/__tests__/publishGate.test.ts
  - src/features/vagas/schemas/__tests__/candidaturaFormSchema.test.ts
  - .planning/phases/07-configura-o-de-vaga-tags/07-SQL-SMOKE-RUNBOOK.md
autonomous: true
requirements: [VAGACFG-01, VAGACFG-02, VAGACFG-03]

must_haves:
  truths:
    - "Failing/Wave-0 unit tests exist for every Phase-7 module before its implementation lands (Nyquist scaffold)"
    - "The Phase-4 candidato form regression case (objects→z.enum) exists and is wired to assert D-13 does not break the shipped reader, importing the normalizer from the neutral @/lib/opcoes lib"
    - "A live SQL smoke runbook documents RPC idempotency, opcao_id generation, RLS deny-for-candidato, and the D-22 db push up-to-date check"
    - "07-VALIDATION.md is flipped to wave_0_complete: true once the Wave-0 scaffold lands (and nyquist_compliant: true once tests are green in later plans)"
  artifacts:
    - path: "src/features/config-vaga/schemas/__tests__/pesosAvaliacaoSchema.test.ts"
      provides: "VAGACFG-02 refine + integer-guard test (Pitfall 4)"
    - path: "src/features/config-vaga/templates/__tests__/cargoTemplates.test.ts"
      provides: "VAGACFG-01 all-8-sum-to-100 + copy-semantics test"
    - path: "src/features/config-vaga/components/__tests__/TemplateVagaSelector.test.tsx"
      provides: "VAGACFG-01 8-card render + deep-copy-on-select + Trocar-template confirm test"
    - path: "src/features/config-vaga/__tests__/publishGate.test.ts"
      provides: "D-12 three-condition publish-gate test"
    - path: ".planning/phases/07-configura-o-de-vaga-tags/07-SQL-SMOKE-RUNBOOK.md"
      provides: "Live DB smoke procedure for RPCs + RLS + db push"
      contains: "upsert_pergunta_opcoes_metadata"
  key_links:
    - from: "candidaturaFormSchema.test.ts"
      to: "candidaturaFormSchema.ts"
      via: "objects→enum regression case (normalizer from @/lib/opcoes)"
      pattern: "opcoes(ToStrings|_resposta)"
---

<objective>
Establish the Wave-0 test scaffold and the live SQL smoke runbook for Phase 7 BEFORE any module
is implemented. Per VALIDATION.md, each Phase-7 module gets a failing/Wave-0 test written ahead of
(or alongside) the module it covers; this plan front-loads all of them so later waves flip them
GREEN. It also adds the D-13 regression case to the shipped Phase-4 form test, and authors the SQL
smoke runbook that the schema apply (Plan 02) and `/gsd:verify-work` consume.

Purpose: Nyquist compliance — no module ships without an automated verify; the central Phase-4
lesson (autonomous gates green but page UNUSABLE) is closed by calibrating RED tests first.
Output: 9 test files + 1 SQL smoke runbook + VALIDATION.md wave_0_complete flag flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-configura-o-de-vaga-tags/07-CONTEXT.md
@.planning/phases/07-configura-o-de-vaga-tags/07-RESEARCH.md
@.planning/phases/07-configura-o-de-vaga-tags/07-VALIDATION.md
@.planning/phases/07-configura-o-de-vaga-tags/07-PATTERNS.md

<interfaces>
<!-- Module surfaces the Wave-0 tests assert against. Sourced from RESEARCH §Code Examples + CONTEXT decisions. -->
<!-- These modules DO NOT EXIST YET — tests import from the planned paths and are expected to be RED until later waves. -->

Planned exports (config-vaga):
- schemas/pesosAvaliacaoSchema.ts → `pesosAvaliacaoSchema` (Zod), `PesosAvaliacao` (type), `somaPesos(p): number`
- templates/cargoTemplates.ts → `cargoTemplates: Record<CargoSlug, CargoTemplate>`, `CargoSlug`, `CargoTemplate`
- __tests__ target → a `publishGate.ts` pure fn (D-12): returns the list of failing conditions given { pesos_avaliacao, testes_aplicaveis, perguntas-with-knockout-flags }
- services/configVagaService.ts → `ConfigVagaServiceError` (code union incl. 'FORBIDDEN'), `updateVagaConfig`, `upsertOpcoesMetadata`, `publishVaga`
- components/TemplateVagaSelector.tsx → renders 8 cargo cards; on-select deep-copies template defaults into form state; re-select opens "Trocar template" AlertDialog

NEUTRAL normalizer (NOT a feature) — src/lib/opcoes/opcoesNormalize.ts → `opcoesToStrings`, `opcoesToObjects`.
  The Phase-4 regression case imports the normalizer from `@/lib/opcoes/opcoesNormalize` (Plan 03 creates it there to
  avoid a vagas→config-vaga cross-feature edge per CLAUDE.md).

Tag taxonomy (D-11): 5 tags `knockout | atencao | neutro | pontua | fortemente_pontua`; default unmarked = `neutro` + peso 0 + nota_ia null.
Pesos keys (D-07): `triagem`, `work_sample_sjt`, `redacao_cultural`, `entrevista` (sum=100). `big_five` + `cognitivo` are context-only (not in sum).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 schema + template + publish-gate + TemplateVagaSelector tests</name>
  <files>src/features/config-vaga/schemas/__tests__/pesosAvaliacaoSchema.test.ts, src/features/config-vaga/templates/__tests__/cargoTemplates.test.ts, src/features/config-vaga/__tests__/publishGate.test.ts, src/features/config-vaga/components/__tests__/TemplateVagaSelector.test.tsx</files>
  <read_first>
    - .planning/phases/07-configura-o-de-vaga-tags/07-RESEARCH.md (§Code Examples: pesosAvaliacaoSchema, cargoTemplates.ts, somaPesos)
    - .planning/phases/07-configura-o-de-vaga-tags/07-CONTEXT.md (D-04, D-07, D-09, D-11, D-12)
    - .planning/phases/07-configura-o-de-vaga-tags/07-UI-SPEC.md (TemplateVagaSelector interaction notes: 8 cards, Trocar template AlertDialog copy)
    - src/features/vagas/utils/__tests__/isUuid.test.ts (pure-fn test idiom to mirror for publishGate)
    - src/features/vagas/schemas/__tests__/candidaturaFormSchema.test.ts (Zod test idiom)
  </read_first>
  <action>
    Write four Vitest/RTL test files importing from the planned (not-yet-existing) module paths.
    pesosAvaliacaoSchema.test.ts: assert `pesosAvaliacaoSchema.safeParse` REJECTS sum≠100 (e.g. {triagem:30,work_sample_sjt:30,redacao_cultural:15,entrevista:20}) and ACCEPTS sum=100; assert integer guard rejects floats (Pitfall 4); assert `somaPesos` returns the live sum for a Partial. cargoTemplates.test.ts: iterate all 8 `CargoSlug` keys (dentista, recepcionista, consultor_vendas_premium, sdr_social_seller, assistente_financeiro, asb, tsb, vaga_generica) and assert each template's pesos sum to exactly 100 (VAGACFG-01); assert selecting a template yields a deep-copy (mutating the copy does not mutate `cargoTemplates`). publishGate.test.ts: assert the D-12 fn flags ALL three conditions — (1) pesos sum≠100, (2) zero `testes_aplicaveis` with obrigatorio=true, (3) a pergunta carrying a knockout-tagged option that is obrigatoria=false — and returns no failures when all three pass. TemplateVagaSelector.test.tsx (VAGACFG-01): render the component and assert all 8 cargo cards render; assert selecting a card invokes the form-state callback with a DEEP COPY of that template's pesos+testes (mutating the received value does not mutate cargoTemplates); assert re-selecting a DIFFERENT template after one is already chosen surfaces the "Trocar template" AlertDialog (overwrite confirm) before applying. These are Wave-0 RED scaffolds: they MUST compile under TS strict but fail at runtime against absent modules until Plan 03/04 lands them. Do NOT use watch mode. Use `--reporter=verbose` so the run output names each test file (so a "module not found in a compiled test" RED is distinguishable from a "test file not found" misconfig).
  </action>
  <verify>
    <automated>for f in src/features/config-vaga/schemas/__tests__/pesosAvaliacaoSchema.test.ts src/features/config-vaga/templates/__tests__/cargoTemplates.test.ts src/features/config-vaga/__tests__/publishGate.test.ts src/features/config-vaga/components/__tests__/TemplateVagaSelector.test.tsx; do test -f "$f" || { echo "MISSING_TESTFILE:$f"; exit 1; }; done && npx vitest run --reporter=verbose src/features/config-vaga/schemas/__tests__/pesosAvaliacaoSchema.test.ts src/features/config-vaga/templates/__tests__/cargoTemplates.test.ts src/features/config-vaga/__tests__/publishGate.test.ts src/features/config-vaga/components/__tests__/TemplateVagaSelector.test.tsx 2>&1 | grep -Eq 'Cannot find module|failed|FAIL' && echo WAVE0_RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist (file-existence preflight passes BEFORE the vitest run — so RED means "module not found in a test that itself compiled", not "test file missing").
    - All four import from `@/features/config-vaga/...` planned paths.
    - Test run is RED (module-not-found or assertion-fail) — the RED state IS the assertion the modules are absent.
    - cargoTemplates.test.ts asserts all 8 slugs sum to 100 (source assertion: 8 distinct `.toBe(100)` or a loop covering all 8 keys).
    - publishGate.test.ts has at least one case per D-12 condition (3 distinct failing cases + 1 all-pass case).
    - TemplateVagaSelector.test.tsx asserts 8 cards render, deep-copy-on-select, and the Trocar-template AlertDialog on re-select (VAGACFG-01).
  </acceptance_criteria>
  <done>Four test files committed; file-existence preflight + verbose vitest run confirm RED scaffold (module-not-found acceptable, test files confirmed present).</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 component + service tests + Phase-4 D-13 regression case</name>
  <files>src/features/config-vaga/components/__tests__/PesosSliders.test.tsx, src/features/config-vaga/components/__tests__/PerguntaWithTagsForm.test.tsx, src/features/config-vaga/components/__tests__/BulkMarkDialog.test.tsx, src/features/config-vaga/services/__tests__/configVagaService.test.ts, src/features/vagas/schemas/__tests__/candidaturaFormSchema.test.ts</files>
  <read_first>
    - src/features/vagas/schemas/candidaturaFormSchema.ts (lines 65-90 — the two `as string[]` breakage sites at single_choice + multiple_choice)
    - src/features/vagas/schemas/__tests__/candidaturaFormSchema.test.ts (the file to EXTEND, not replace)
    - src/features/vagas/services/__tests__/vagasService.test.ts (mock-supabase service test idiom)
    - .planning/phases/07-configura-o-de-vaga-tags/07-UI-SPEC.md (D-08 live-sum color states; D-11 choice-only render + empty state; BulkMarkDialog copy "Marcar tudo como informativa")
    - .planning/phases/07-configura-o-de-vaga-tags/07-CONTEXT.md (D-08, D-10, D-11, D-13, D-14)
  </read_first>
  <action>
    Write three RTL component tests + one service test as Wave-0 RED scaffolds, plus EXTEND the
    shipped Phase-4 form test with the D-13 regression case. PesosSliders.test.tsx: assert live-sum
    indicator renders the error color/text when sum≠100 and the accent/valid copy when =100, and that
    dragging one slider does NOT auto-rebalance the others (D-08 no silent rebalance). PerguntaWithTagsForm.test.tsx:
    assert the wizard renders option rows for `single_choice`/`multiple_choice` and renders the empty-state
    copy ("Nenhuma pergunta de escolha nesta vaga") for `texto`/`numerico` (D-11). BulkMarkDialog.test.tsx:
    assert confirming sets every option to neutro + peso 0 + nota_ia null (D-11). configVagaService.test.ts:
    mock `@/lib/supabase/client` and assert `updateVagaConfig` issues a `.from('vagas').update(...)`, `upsertOpcoesMetadata`
    calls `.rpc('upsert_pergunta_opcoes_metadata', {p_pergunta_id, p_opcoes})`, `publishVaga` calls `.rpc('publish_vaga', {p_vaga_id})`,
    and a Supabase 42501 error maps to `ConfigVagaServiceError` code `'FORBIDDEN'`. EXTEND candidaturaFormSchema.test.ts:
    add a regression case proving the candidato form still builds the correct `z.enum` of option STRINGS
    when `opcoes_resposta` is the NEW `[{id,texto}]` object shape (D-13 / Pitfall 1) AND still works for legacy `string[]`
    (idempotent normalization). This extension targets the neutral helper `opcoesToStrings` (which Plan 03 will add at
    `@/lib/opcoes/opcoesNormalize` — NOT inside config-vaga) — the new case is RED until then; do NOT break the existing
    passing cases. Do NOT use watch mode. Use `--reporter=verbose`.
  </action>
  <verify>
    <automated>for f in src/features/config-vaga/components/__tests__/PesosSliders.test.tsx src/features/config-vaga/components/__tests__/PerguntaWithTagsForm.test.tsx src/features/config-vaga/components/__tests__/BulkMarkDialog.test.tsx src/features/config-vaga/services/__tests__/configVagaService.test.ts src/features/vagas/schemas/__tests__/candidaturaFormSchema.test.ts; do test -f "$f" || { echo "MISSING_TESTFILE:$f"; exit 1; }; done && npx vitest run --reporter=verbose src/features/config-vaga/components src/features/config-vaga/services src/features/vagas/schemas/__tests__/candidaturaFormSchema.test.ts 2>&1 | grep -Eq 'Cannot find module|opcoesToStrings|failed|FAIL' && echo WAVE0_RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - File-existence preflight passes for all 5 files BEFORE the vitest run (RED = "module not found in a compiled test", not "test file missing").
    - 3 component tests + 1 service test exist as RED scaffolds (module-not-found acceptable).
    - candidaturaFormSchema.test.ts gains a NEW case feeding the `[{id,texto}]` object shape and asserting a string-valued `z.enum` (behavior assertion: parsing a valid option string succeeds, an invalid one fails); the case targets `@/lib/opcoes/opcoesNormalize` (neutral lib).
    - The pre-existing candidaturaFormSchema.test.ts cases are unchanged and still present (no deletions).
    - configVagaService.test.ts asserts the exact RPC names `upsert_pergunta_opcoes_metadata` and `publish_vaga` (source assertion via mock call args).
  </acceptance_criteria>
  <done>Four config-vaga test files + extended Phase-4 form test committed; file-existence preflight + RED scaffold confirmed; existing Phase-4 cases preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Author the SQL smoke runbook + flip VALIDATION wave_0_complete</name>
  <files>.planning/phases/07-configura-o-de-vaga-tags/07-SQL-SMOKE-RUNBOOK.md, .planning/phases/07-configura-o-de-vaga-tags/07-VALIDATION.md</files>
  <read_first>
    - .planning/phases/07-configura-o-de-vaga-tags/07-RESEARCH.md (§Code Examples: upsert RPC; §Validation Architecture; §Security Domain V4)
    - .planning/phases/07-configura-o-de-vaga-tags/07-VALIDATION.md (Manual-Only Verifications table + frontmatter flags + Wave 0 Requirements list)
    - CLAUDE.md (§Commands — SQLSTATE 42601 workaround / migration repair)
    - supabase/migrations/20260607000006_rls_policies_m2_backbone.sql (live RLS role idiom to assert deny)
  </read_first>
  <action>
    Author a markdown runbook with numbered, copy-pasteable SQL smoke steps to run against the LIVE
    project (isljnozzlvckrgjjbjwp) after Plan 02 applies the schema. Sections: (1) RPC idempotency —
    call `upsert_pergunta_opcoes_metadata` twice with the same payload and assert the second call
    yields the same `pergunta_opcao_metadata` row count and the same `opcao_id`s (no duplicates,
    DELETE+re-INSERT semantics). (2) opcao_id generation/backfill — call with `opcao_id:null` options
    and assert each row in `pergunta_opcao_metadata` got a non-null uuid AND the source `perguntas_formulario.opcoes_resposta`
    jsonb was rewritten to `[{id,texto}]`. (3) RLS deny — assert a candidato/anon JWT cannot write
    `pergunta_opcao_metadata` (in-body 42501 from the RPC + table policy denies direct write). (4) publish_vaga
    guard — assert flipping `status='ativa'` is rejected when any D-12 condition fails. (5) db push up-to-date —
    `supabase db push --linked` reports "Remote database is up to date" after `migration repair`. Each step:
    expected result + pass/fail box. Mark the runbook as the artifact consumed by Plan 02's apply checkpoint and by `/gsd:verify-work`.
    THEN update 07-VALIDATION.md: set `wave_0_complete: true` in the frontmatter (the Wave-0 scaffold now exists);
    add the TemplateVagaSelector test to the Wave 0 Requirements list and the Per-Requirement Verification Map under
    VAGACFG-01; leave `nyquist_compliant: false` for now (it flips true in Plan 03/04 once the Wave-0 tests are GREEN,
    per those plans' success criteria).
  </action>
  <verify>
    <automated>grep -v '^#' .planning/phases/07-configura-o-de-vaga-tags/07-SQL-SMOKE-RUNBOOK.md | grep -Eqc 'upsert_pergunta_opcoes_metadata' && grep -v '^#' .planning/phases/07-configura-o-de-vaga-tags/07-SQL-SMOKE-RUNBOOK.md | grep -Eq 'publish_vaga' && grep -Eq '42501|candidato|anon' .planning/phases/07-configura-o-de-vaga-tags/07-SQL-SMOKE-RUNBOOK.md && grep -q 'wave_0_complete: true' .planning/phases/07-configura-o-de-vaga-tags/07-VALIDATION.md && grep -q 'TemplateVagaSelector' .planning/phases/07-configura-o-de-vaga-tags/07-VALIDATION.md && echo RUNBOOK_OK</automated>
  </verify>
  <acceptance_criteria>
    - Runbook references both RPC names, the RLS deny path, and the `db push` up-to-date check.
    - Five numbered smoke sections present (idempotency, opcao_id gen, RLS deny, publish guard, db push).
    - Each section has an explicit expected result.
    - 07-VALIDATION.md frontmatter has `wave_0_complete: true`; TemplateVagaSelector test added to Wave 0 Requirements + Per-Requirement Map (VAGACFG-01).
  </acceptance_criteria>
  <done>07-SQL-SMOKE-RUNBOOK.md committed with all five smoke sections; 07-VALIDATION.md wave_0_complete flipped + TemplateVagaSelector mapped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → planned modules | Tests assert contracts (RPC names, code unions) before code exists — no runtime trust boundary crossed; this is scaffold only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-RB | Information Disclosure | SQL smoke runbook | accept | Runbook contains procedure only, no secrets/JWTs; live JWTs are supplied at run-time by the operator, never committed. |
| T-07-01-SC | Tampering | npm installs | mitigate | No new packages installed this plan (RESEARCH Package Legitimacy Audit = N/A); Vitest/Playwright already in tree. |
</threat_model>

<verification>
- File-existence preflight + `npx vitest run --reporter=verbose src/features/config-vaga` confirms RED Wave-0 scaffold (module-not-found acceptable; test files confirmed present).
- Pre-existing Phase-4 candidaturaFormSchema.test.ts cases still present (no deletions).
- Runbook grep gates pass; 07-VALIDATION.md wave_0_complete: true.
- `npm run build` exit 0 (test files are excluded from the production build; no src/ runtime code added).
</verification>

<success_criteria>
- 9 test files exist at the VALIDATION.md Wave-0 paths (incl. TemplateVagaSelector.test.tsx).
- D-13 regression case wired into the shipped Phase-4 form test (targeting @/lib/opcoes/opcoesNormalize) without breaking existing cases.
- SQL smoke runbook authored with 5 smoke sections.
- 07-VALIDATION.md set to wave_0_complete: true with the TemplateVagaSelector test mapped to VAGACFG-01.
- nyquist scaffold in place for every Phase-7 module (later waves flip GREEN and set nyquist_compliant: true before /gsd:verify-work).
</success_criteria>

<output>
Create `.planning/phases/07-configura-o-de-vaga-tags/07-01-SUMMARY.md` when done.
</output>
</content>
