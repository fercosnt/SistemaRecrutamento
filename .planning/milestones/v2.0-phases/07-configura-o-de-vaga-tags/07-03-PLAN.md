---
phase: 07-configura-o-de-vaga-tags
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - src/features/config-vaga/schemas/pesosAvaliacaoSchema.ts
  - src/features/config-vaga/schemas/testesAplicaveisSchema.ts
  - src/features/config-vaga/schemas/tagOpcaoSchema.ts
  - src/features/config-vaga/templates/cargoTemplates.ts
  - src/features/config-vaga/types/configVagaTypes.ts
  - src/features/config-vaga/services/configVagaService.ts
  - src/features/config-vaga/hooks/useConfigVaga.ts
  - src/features/config-vaga/hooks/usePerguntaOpcaoMetadata.ts
  - src/features/config-vaga/hooks/index.ts
  - src/features/config-vaga/publishGate.ts
  - src/lib/opcoes/opcoesNormalize.ts
  - src/features/vagas/schemas/candidaturaFormSchema.ts
autonomous: true
requirements: [VAGACFG-01, VAGACFG-02, VAGACFG-03]

must_haves:
  truths:
    - "config-vaga schemas (pesos refine sum=100 integer-guard, testes, tag-row) exist and flip the Wave-0 schema tests GREEN"
    - "cargoTemplates.ts holds all 8 cargo templates whose pesos sum to 100, copied (not referenced) into the vaga on select"
    - "The 8 cargo templates live as a git TS config module (cargoTemplates.ts); selecting one COPIES its defaults into the vaga (override-on-the-vaga) and there is NO vaga_templates table in V1 (D-04)"
    - "Each template references only WHICH tests apply via testes_aplicaveis (perguntas left empty); the SJT question bank + sync pipeline is NOT built here — it is deferred to Phase 11 (D-05)"
    - "Exactly 4 pesos keys (triagem, work_sample_sjt, redacao_cultural, entrevista) sum to 100; big_five and cognitivo are context-only (no peso, excluded from the sum) (D-07)"
    - "Each template's default pesos are sensible STARTER values flagged for UAT calibration — the exact numbers do not block the phase (D-09)"
    - "configVagaService exposes updateVagaConfig (vagas UPDATE), upsertOpcoesMetadata (sync RPC), publishVaga (publish RPC) with ConfigVagaServiceError incl. FORBIDDEN"
    - "publishGate pure fn returns the failing D-12 conditions; Wave-0 publishGate test is GREEN"
    - "The shipped Phase-4 candidato form normalizes [{id,texto}] → string[] via the NEUTRAL shared lib (no vagas→config-vaga edge) and its regression test is GREEN (D-13 / Pitfall 1)"
  artifacts:
    - path: "src/features/config-vaga/schemas/pesosAvaliacaoSchema.ts"
      provides: "Zod 4-key integer pesos + .refine(sum===100) + somaPesos"
      contains: "somaPesos"
    - path: "src/features/config-vaga/templates/cargoTemplates.ts"
      provides: "8 cargo templates with starter pesos+testes"
      contains: "cargoTemplates"
    - path: "src/features/config-vaga/services/configVagaService.ts"
      provides: "ConfigVagaServiceError + updateVagaConfig + upsertOpcoesMetadata + publishVaga"
      exports: ["ConfigVagaServiceError", "updateVagaConfig", "upsertOpcoesMetadata", "publishVaga"]
    - path: "src/lib/opcoes/opcoesNormalize.ts"
      provides: "opcoesToStrings/opcoesToObjects normalization (legacy string[] ↔ [{id,texto}]) — NEUTRAL shared lib, no feature ownership"
      contains: "opcoesToStrings"
  key_links:
    - from: "configVagaService.ts"
      to: "upsert_pergunta_opcoes_metadata"
      via: "supabase.rpc"
      pattern: "rpc\\(['\"]upsert_pergunta_opcoes_metadata"
    - from: "candidaturaFormSchema.ts"
      to: "@/lib/opcoes/opcoesNormalize"
      via: "opcoesToStrings replacing `as string[]` (neutral import — no vagas→config-vaga edge)"
      pattern: "opcoesToStrings"
---

<objective>
Scaffold `src/features/config-vaga/` mirroring `src/features/vagas/` (schemas, templates, types,
service with custom error class, TanStack hooks + query keys, the D-12 publishGate pure fn), plus a
NEUTRAL shared `src/lib/opcoes/opcoesNormalize.ts` helper (consumed by BOTH config-vaga and the
Phase-4 vagas reader via `@/lib/...` — never a cross-feature import), wiring real persistence of the
new vaga fields against the now-applied schema. In a DEDICATED task (Task 3, its own commit boundary)
update the shipped Phase-4 reader (`candidaturaFormSchema.ts`) to normalize the new `[{id,texto}]`
jsonb shape — D-13 / Pitfall 1, inseparable from the shape migration. This wave flips the Plan-01
Wave-0 schema/template/service/publishGate tests and the Phase-4 regression case GREEN.

Purpose: the non-UI half of the feature — contracts, persistence, validation logic — that Plan 04's UI consumes.
Output: 10 new config-vaga files + 1 neutral shared lib helper + 1 edited shipped file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-configura-o-de-vaga-tags/07-CONTEXT.md
@.planning/phases/07-configura-o-de-vaga-tags/07-RESEARCH.md
@.planning/phases/07-configura-o-de-vaga-tags/07-PATTERNS.md
@database.types.ts

<interfaces>
<!-- Analogs to mirror. Read the real files; these are the load-bearing line anchors from PATTERNS.md. -->

Service error class — src/features/vagas/services/vagasService.ts:28-42 (VagasServiceError code union):
  'INVALID_INPUT' | 'NETWORK_ERROR' | 'DATABASE_ERROR' | 'NOT_FOUND' | 'UNAUTHORIZED'
  → ConfigVagaServiceError adds 'FORBIDDEN' (maps RPC 42501).

Query keys — src/features/vagas/hooks/useVagas.ts:38-60 (vagasKeys); defaults :105-109 (staleTime 5min/gcTime 10min/retry 2).
Read hook idiom — src/features/vagas/hooks/useVagaPerguntas.ts:54-74 (inline queryFn, enabled:!!id gate).
Zod idiom — src/features/vagas/schemas/candidaturaFormSchema.ts:24,154 (import z, pt-BR messages, z.infer export).

NEUTRAL shared lib (NOT a feature) — src/lib/opcoes/opcoesNormalize.ts:
  This helper lives under `src/lib/` precisely so BOTH the config-vaga feature AND the shipped vagas
  reader import it via `@/lib/opcoes/opcoesNormalize`. CLAUDE.md forbids cross-feature relative
  imports and a permanent vagas→config-vaga dependency; routing through the neutral lib keeps the
  dependency direction acyclic (feature → lib, never feature → feature). Look at any existing
  `src/lib/*` module for the bare-utility idiom (no React, no Supabase — pure functions only).

Phase-4 breakage sites (VERIFIED) — candidaturaFormSchema.ts:
  single_choice: `const opts = (p.opcoes_resposta as string[] | null) ?? []` then z.enum(opts)
  multiple_choice: same `as string[]` cast then z.enum(opts)
  → replace both with `opcoesToStrings(p.opcoes_resposta)` imported from `@/lib/opcoes/opcoesNormalize`
    (reads .texto, idempotent for legacy string[]).

RPC arg shapes (from Plan 02):
  upsert_pergunta_opcoes_metadata(p_pergunta_id uuid, p_opcoes jsonb)  // p_opcoes: [{opcao_id|null, texto, tag, peso, nota_ia, ordem}]
  publish_vaga(p_vaga_id uuid)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schemas + cargoTemplates + types + neutral opcoes normalization + publishGate</name>
  <files>src/features/config-vaga/schemas/pesosAvaliacaoSchema.ts, src/features/config-vaga/schemas/testesAplicaveisSchema.ts, src/features/config-vaga/schemas/tagOpcaoSchema.ts, src/features/config-vaga/templates/cargoTemplates.ts, src/features/config-vaga/types/configVagaTypes.ts, src/lib/opcoes/opcoesNormalize.ts, src/features/config-vaga/publishGate.ts</files>
  <read_first>
    - .planning/phases/07-configura-o-de-vaga-tags/07-RESEARCH.md (§Code Examples: pesosAvaliacaoSchema verbatim, cargoTemplates.ts interface + 8-cargo starter pesos table, testes_aplicaveis shape)
    - src/features/vagas/schemas/candidaturaFormSchema.ts (Zod pt-BR idiom + z.infer convention)
    - .planning/phases/07-configura-o-de-vaga-tags/07-CONTEXT.md (D-07, D-08, D-09, D-11, D-12, D-13)
    - src/features/config-vaga/schemas/__tests__/pesosAvaliacaoSchema.test.ts (Wave-0 contract to satisfy)
    - src/features/config-vaga/templates/__tests__/cargoTemplates.test.ts (Wave-0 contract)
    - src/features/config-vaga/__tests__/publishGate.test.ts (Wave-0 contract)
  </read_first>
  <behavior>
    - pesosAvaliacaoSchema rejects sum≠100 and floats; accepts integer 4-key sum=100; somaPesos returns live partial sum.
    - All 8 cargoTemplates pesos sum to exactly 100; selecting a template deep-copies defaults (no shared reference).
    - tagOpcaoSchema: tag ∈ 5-enum, peso int -999..100 (matches DB CHECK), nota_ia nullable/optional.
    - opcoesToStrings (in @/lib/opcoes/opcoesNormalize) accepts BOTH legacy string[] AND [{id,texto}] and returns string[] of texts (idempotent); opcoesToObjects yields [{id,texto}].
    - publishGate returns the list of failing D-12 conditions (empty when all pass).
  </behavior>
  <action>
    Implement the schemas verbatim from RESEARCH §Code Examples: pesosAvaliacaoSchema (z.number().int().min(0).max(100)
    per key + .refine sum===100 + exported somaPesos, Pitfall 4 integer-only); testesAplicaveisSchema (Zod array of
    {teste:string, obrigatorio:boolean, customizado:boolean, perguntas?:array} per RF-11, leaving perguntas empty — D-05);
    tagOpcaoSchema ({tag: z.enum of the 5 tags, peso: z.number().int().min(-999).max(100), nota_ia: z.string().nullable().optional()}).
    cargoTemplates.ts: the CargoSlug union (8 cargos), CargoTemplate interface, and the cargoTemplates record using the
    RESEARCH §Code Examples starter pesos table (D-09 — all 8 sum to 100). configVagaTypes.ts: derive types from
    Database['public'] (the regenerated database.types.ts) for the new table/columns/enum + the jsonb shapes — never
    hand-write DB types. Create the NEUTRAL shared helper at src/lib/opcoes/opcoesNormalize.ts (NOT inside any feature —
    so both config-vaga and the vagas reader import it via @/lib/opcoes/opcoesNormalize, keeping the dependency direction
    feature→lib and avoiding a vagas→config-vaga edge per CLAUDE.md import rules): `opcoesToStrings(jsonb): string[]`
    reading `.texto` (idempotent across both shapes) + `opcoesToObjects(jsonb): {id,texto}[]`; pure functions only, no
    React/Supabase imports. publishGate.ts: a pure fn taking { pesos_avaliacao, testes_aplicaveis,
    perguntasComKnockout: {texto,obrigatoria}[] } returning the failing-condition list per D-12 (sum≠100 / no obrigatorio
    test / knockout-pergunta-not-obrigatoria) using somaPesos. pt-BR messages per UI-SPEC Copywriting Contract.
  </action>
  <verify>
    <automated>npx vitest run src/features/config-vaga/schemas src/features/config-vaga/templates src/features/config-vaga/__tests__/publishGate.test.ts 2>&1 | grep -Eq 'passed|PASS' && test -f src/lib/opcoes/opcoesNormalize.ts && echo SCHEMAS_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - Wave-0 pesosAvaliacaoSchema, cargoTemplates, and publishGate tests are GREEN (test command passes).
    - cargoTemplates has all 8 slugs each summing to 100 (test-asserted).
    - opcoesNormalize.ts lives under src/lib/opcoes/ (NEUTRAL — no feature path) and exports opcoesToStrings/opcoesToObjects with no React/Supabase imports.
    - configVagaTypes derives from Database['public'] (no hand-written DB interfaces).
  </acceptance_criteria>
  <done>Schemas, templates, types, neutral normalization helper (src/lib/opcoes), and publishGate implemented; Wave-0 schema/template/publishGate tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: configVagaService + hooks</name>
  <files>src/features/config-vaga/services/configVagaService.ts, src/features/config-vaga/hooks/useConfigVaga.ts, src/features/config-vaga/hooks/usePerguntaOpcaoMetadata.ts, src/features/config-vaga/hooks/index.ts</files>
  <read_first>
    - src/features/vagas/services/vagasService.ts (lines 13, 28-42, 141-149, 270-318 — client import, error class, .from('vagas').update, try/catch error-mapping)
    - src/features/vagas/hooks/useVagas.ts (lines 38-60 keys, 105-109 defaults)
    - src/features/vagas/hooks/useVagaPerguntas.ts (lines 54-74 inline queryFn read idiom + enabled gate)
    - src/features/config-vaga/services/__tests__/configVagaService.test.ts (Wave-0 contract: RPC names + FORBIDDEN mapping)
  </read_first>
  <behavior>
    - configVagaService.updateVagaConfig → supabase.from('vagas').update({testes_aplicaveis, pesos_avaliacao}).eq('id', vagaId).
    - upsertOpcoesMetadata → supabase.rpc('upsert_pergunta_opcoes_metadata', {p_pergunta_id, p_opcoes}).
    - publishVaga → supabase.rpc('publish_vaga', {p_vaga_id}); a 42501 error maps to ConfigVagaServiceError 'FORBIDDEN'.
    - useConfigVaga exposes configVagaKeys + read query + mutations invalidating config(vagaId).
  </behavior>
  <action>
    Implement configVagaService.ts: import the anon `supabase` client only (never supabaseAdmin); ConfigVagaServiceError
    mirroring VagasServiceError's code union plus 'FORBIDDEN'; `updateVagaConfig(vagaId, {testes_aplicaveis, pesos_avaliacao})`
    as a plain `.from('vagas').update(...).eq('id', vagaId)` (the D-02 "ligar de verdade" greenfield persistence, used by
    Salvar rascunho — no validation per D-12); `upsertOpcoesMetadata(perguntaId, opcoes)` calling `.rpc('upsert_pergunta_opcoes_metadata',
    {p_pergunta_id: perguntaId, p_opcoes: opcoes})`; `publishVaga(vagaId)` calling `.rpc('publish_vaga', {p_vaga_id: vagaId})`,
    mapping a Supabase error whose code/message indicates 42501 → ConfigVagaServiceError('...', 'FORBIDDEN'), and other DB
    errors → 'DATABASE_ERROR' (throw-style for mutations per the analog). hooks/useConfigVaga.ts: configVagaKeys hierarchy
    (all/config(vagaId)/opcoesMetadata) + a read useQuery for the vaga config + useMutation wrappers for the three service
    methods, invalidating configVagaKeys.config(vagaId) onSuccess; staleTime 5min/retry 2 per CLAUDE.md.
    hooks/usePerguntaOpcaoMetadata.ts: read useQuery mirroring useVagaPerguntas' inline-queryFn over `pergunta_opcao_metadata`
    by pergunta_id, enabled:!!id. hooks/index.ts barrel. Mind the @hookform/resolvers v5 input/output cast caveat if any
    schema uses .optional().default() (STATE [03-05]).
  </action>
  <verify>
    <automated>npx vitest run src/features/config-vaga/services 2>&1 | grep -Eq 'passed|PASS' && echo SERVICE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - configVagaService Wave-0 test GREEN; asserts exact RPC names + FORBIDDEN mapping.
    - Service imports the anon client only (no supabaseAdmin — security rule).
    - useConfigVaga + usePerguntaOpcaoMetadata + barrel exist mirroring features/vagas hook idioms.
  </acceptance_criteria>
  <done>Service + hooks + barrel implemented; configVagaService Wave-0 test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Phase-4 reader edit via neutral lib (D-13 — dedicated commit boundary)</name>
  <files>src/features/vagas/schemas/candidaturaFormSchema.ts</files>
  <read_first>
    - src/features/vagas/schemas/candidaturaFormSchema.ts (lines 65-90 — the two `as string[]` sites to replace)
    - src/lib/opcoes/opcoesNormalize.ts (the neutral helper from Task 1 — opcoesToStrings)
    - src/features/vagas/schemas/__tests__/candidaturaFormSchema.test.ts (the extended D-13 regression case from Plan 01)
    - .planning/phases/07-configura-o-de-vaga-tags/07-CONTEXT.md (D-13 / Pitfall 1 — inseparable reader edit + regression test)
  </read_first>
  <behavior>
    - candidato form (D-13): single_choice + multiple_choice build z.enum of option STRINGS for BOTH legacy string[] and new [{id,texto}].
    - No vagas→config-vaga import edge: the normalization is imported from @/lib/opcoes/opcoesNormalize (neutral lib).
  </behavior>
  <action>
    Edit ONLY src/features/vagas/schemas/candidaturaFormSchema.ts: replace BOTH `const opts = (p.opcoes_resposta as string[] | null) ?? []`
    sites (single_choice + multiple_choice) with `const opts = opcoesToStrings(p.opcoes_resposta)`, importing opcoesToStrings
    from `@/lib/opcoes/opcoesNormalize` (the NEUTRAL shared lib — NOT from config-vaga; this preserves CLAUDE.md import
    boundaries and creates no vagas→config-vaga dependency). Do not change any other behavior of the candidato form.
    This is the D-13 / Pitfall 1 reader migration, isolated into its own task so the shipped-file edit has a dedicated
    commit boundary protecting it from a mid-execution context failure in Task 2. After the edit, run the Plan-01 D-13
    regression case (objects→enum) AND the pre-existing legacy string[] cases — both must be GREEN before commit.
  </action>
  <verify>
    <automated>npx vitest run src/features/vagas/schemas/__tests__/candidaturaFormSchema.test.ts 2>&1 | grep -Eq 'passed|PASS' && grep -q 'opcoesToStrings' src/features/vagas/schemas/candidaturaFormSchema.ts && grep -q "@/lib/opcoes/opcoesNormalize" src/features/vagas/schemas/candidaturaFormSchema.ts && ! grep -q 'as string\[\]' src/features/vagas/schemas/candidaturaFormSchema.ts && echo D13_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - candidaturaFormSchema.ts no longer contains `as string[]` casts for opcoes (source assertion); uses opcoesToStrings.
    - candidaturaFormSchema.ts imports opcoesToStrings from `@/lib/opcoes/opcoesNormalize` (NOT from src/features/config-vaga — no cross-feature edge).
    - The Phase-4 D-13 regression case (objects→enum) is GREEN AND the pre-existing legacy string[] cases still pass.
  </acceptance_criteria>
  <done>Phase-4 reader normalized via neutral lib; D-13 regression GREEN; legacy cases preserved; no as-string[] casts and no config-vaga import remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client service → Supabase | configVagaService uses the anon client; privileged writes go through the DEFINER RPCs (role-checked in-body, Plan 02). |
| Phase-4 reader ↔ new jsonb shape | The candidato form is a shipped consumer; the neutral lib normalization helper is the compatibility boundary (D-13). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Elevation of Privilege | configVagaService | mitigate | Uses anon client only; never supabaseAdmin/service_role on client (CLAUDE.md Security Rules). Authorization enforced server-side by the RPCs + RLS (Plan 02). |
| T-07-03-02 | Tampering (data integrity) | Phase-4 candidato form | mitigate | opcoesToStrings (neutral lib) idempotent across both shapes + Wave-0 regression test prevents the breaking-change from shipping silently (Pitfall 1). |
| T-07-03-03 | Spoofing | publishVaga error mapping | accept | Client maps 42501→FORBIDDEN for UX only; the authoritative gate is the server RPC, not the client mapping. |
| T-07-03-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH audit N/A); all deps in tree. |
</threat_model>

<verification>
- Wave-0 schema/template/service/publishGate tests GREEN.
- Phase-4 D-13 regression GREEN + legacy cases preserved; no `as string[]` casts remain; reader imports from @/lib/opcoes (no config-vaga edge).
- `npm run test:run` full Vitest green (minus the 1 pre-existing LoadingProgress carryover).
- `npm run build` exit 0; lint baseline ≤ 296.
</verification>

<success_criteria>
- config-vaga schemas/templates/types/service/hooks/publishGate implemented mirroring features/vagas; opcoes normalization in neutral src/lib/opcoes.
- All 8 cargo templates sum to 100; copied-into-vaga semantics (deep copy).
- configVagaService persists the new fields for real (D-02) + calls both RPCs by exact name.
- D-13 Phase-4 reader migrated (own commit boundary, neutral-lib import) with passing regression test (Pitfall 1 closed at the schema layer).
</success_criteria>

<output>
Create `.planning/phases/07-configura-o-de-vaga-tags/07-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
