---
phase: 10-triagem-rh-com-ia-comparativo-etapa-2
plan: 06
type: execute
wave: 6
depends_on: ["10-05"]
files_modified:
  - src/features/triagem/services/triagemService.ts
  - src/features/triagem/hooks/useComparativo.ts
  - src/features/triagem/pdf/exportComparativo.ts
  - src/features/triagem/components/ComparativoScreen.tsx
  - src/components/pages/ComparativoCandidatosPage.tsx
  - src/router/routes.tsx
  - src/features/triagem/components/__tests__/ComparativoScreen.test.tsx
autonomous: true
requirements: [TRIAGEM-03, TRIAGEM-04]
user_setup: []

must_haves:
  truths:
    - "From the panel Comparar(N) CTA, RH reaches a comparativo screen that invokes comparativo-candidatos via supabase.functions.invoke (user JWT) and renders candidates-as-columns (≤10) with ranking 1-N, stable score band, fortes, gaps, justificativa_ia — TRIAGEM-03/04"
    - "Selecting candidatos from different vagas surfaces the EF 400 as the pt-BR copy 'Os candidatos selecionados pertencem a vagas diferentes...' — TRIAGEM-03"
    - "Each candidate column has inline Avançar (accent, confirm dialog) + Rejeitar (destructive, confirm, no long justification) calling the existing avancar_etapa/status flow — IA is suggestion, never auto-action (RNF-07a)"
    - "Exportar PDF produces a selectable-text landscape PDF (jspdf + jspdf-autotable, attributes-as-rows / candidates-as-columns) with success/error toasts — TRIAGEM-04"
    - "The SugestaoIABadge is rendered prominently once at the top of the comparativo (RNF-07a) and the first attribute column is sticky-left on horizontal scroll"
  artifacts:
    - path: "src/features/triagem/components/ComparativoScreen.tsx"
      provides: "candidates-as-columns comparativo + inline actions + export"
      min_lines: 90
    - path: "src/features/triagem/pdf/exportComparativo.ts"
      provides: "jspdf-autotable export"
      min_lines: 20
    - path: "src/features/triagem/hooks/useComparativo.ts"
      provides: "invoke mutation hook"
  key_links:
    - from: "src/features/triagem/hooks/useComparativo.ts"
      to: "comparativo-candidatos EF"
      via: "supabase.functions.invoke"
      pattern: "comparativo-candidatos"
    - from: "src/router/routes.tsx"
      to: "ComparativoCandidatosPage"
      via: "RH-guarded route"
      pattern: "ComparativoCandidatosPage"
---

<objective>
The comparativo screen + PDF export: the `invokeComparativo` service method + `useComparativo` mutation, the `ComparativoScreen` (candidates-as-columns, sticky attribute column, inline Avançar/Rejeitar, SugestaoIABadge, export button), the `exportComparativo` jspdf util, the page wrapper, and the RH-guarded route. Reached from the panel's Comparar(N) CTA.

Purpose: TRIAGEM-03 (frontend invoke + same-vaga 400 surfacing) + TRIAGEM-04 (≤10-column screen + PDF). The candidates-as-columns table is reused by DECISAO-02 (Phase 15).
Output: comparativo screen + PDF + route; the phase's full RH triage loop is wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-UI-SPEC.md
@.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-PATTERNS.md
@.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-RESEARCH.md

<interfaces>
<!-- Exact identifiers — do not re-explore. -->

comparativo-candidatos EF (10-03) returns: { ok:true, ranking: ComparativeRanking, latencia_ms }
  ComparativeRanking.ranked_candidates[].{candidate_id, rank, composite_score, relative_strengths[], relative_weaknesses[], rationale}
  + recommendation.{top_choice, backup_choice, note}, ties_or_concerns[]
  EF 400 codes: <2/>10 → "Selecione 2 a 10 candidatos."; mixed-vaga → "Os candidatos pertencem a vagas diferentes."

invoke pattern (candidaturasService.ts:1328-1382 submitCandidaturaWithRespostas):
  const { data, error } = await supabase.functions.invoke('comparativo-candidatos', { body: { vaga_id, candidatura_ids } })
  if (error) throw NETWORK_ERROR; if (!data?.ok) map error_code/message → UI copy (mixed-vaga → the pt-BR string)

triagemKeys (from 10-05 useTriagemPanel): add comparativo:(vagaId, ids)=>[...all,'comparativo',vagaId,ids.slice().sort()]

routes.tsx:53,302 — VagaCandidatosRHPage import + /rh/vagas/:id/candidatos guard role={['rh','administrador']}; ADD a sibling route /rh/vagas/:id/comparativo (same guard) → ComparativoCandidatosPage.

avancar_etapa RPC (Phase 6) + the panel's existing status-update handler (updateCandidaturaStatus) — reuse for inline Avançar/Rejeitar; Rejeitar moves to 'rejeitado' (NO long justification here — deferred to Etapa 6). Wrap in alert-dialog.tsx confirm.

UI-SPEC §B comparativo: candidates=columns (≤10), attributes=rows (Header name+avatar+ranking medal, Ranking IA, Score IA band, Pontos fortes, Gaps, Justificativa IA, Flags neutral, Ação Avançar accent + Rejeitar destructive). First column sticky-left (sticky left-0 z-10), horizontal scroll on overflow, min column width 200px. Score band reuses the panel band helper (70/40 thresholds).
UI-SPEC §B export: "Exportar PDF" accent-outline + Download icon; Generating "Gerando PDF…"; success toast "PDF exportado."; error toast "Não foi possível gerar o PDF. Tente novamente."
UI-SPEC §B AI framing: SugestaoIABadge (full, from 10-05) once at top of comparativo (header band).
UI-SPEC Copywriting: Avançar confirm "Avançar {nome} para a próxima etapa?"; Rejeitar confirm "Rejeitar {nome}?" destructive, body notes justification deferred to Decisão Final.

RESEARCH Code Examples PDF export (candidates-as-columns):
  import { jsPDF } from 'jspdf'; import autoTable from 'jspdf-autotable'
  doc = new jsPDF({ orientation:'landscape' }); head=[['Atributo', ...names]]; body rows = Ranking/Score/Fortes/Gaps/Justificativa (join arrays with '; '); autoTable(doc,{head,body,startY:22,styles:{fontSize:8,cellWidth:'wrap'}}); doc.save('comparativo-candidatos.pdf')

primitives: table, badge, button, alert-dialog, tooltip, sonner (toast) — all in src/components/ui/. SugestaoIABadge from src/features/triagem/components/SugestaoIABadge.tsx (10-05).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: invokeComparativo service + useComparativo hook + exportComparativo PDF</name>
  <files>src/features/triagem/services/triagemService.ts, src/features/triagem/hooks/useComparativo.ts, src/features/triagem/pdf/exportComparativo.ts</files>
  <read_first>
    - src/features/vagas/services/candidaturasService.ts:1328-1382 (submitCandidaturaWithRespostas — functions.invoke + error_code mapping shape)
    - src/features/triagem/hooks/useTriagemPanel.ts (triagemKeys from 10-05 — extend with comparativo key)
    - .planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-RESEARCH.md (Code Examples — invoke hook + PDF export)
    - .planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-UI-SPEC.md (B export copy)
  </read_first>
  <behavior>
    - invokeComparativo(vagaId, ids) posts { vaga_id, candidatura_ids } to functions.invoke('comparativo-candidatos'); on transport error throws NETWORK_ERROR; on !data.ok maps the EF 400 mixed-vaga to the pt-BR UI copy
    - useComparativo = useMutation wrapping invokeComparativo; toast on error
    - exportComparativo(ranking) builds a landscape jspdf with attributes-as-rows / candidates-as-columns via autoTable and doc.save('comparativo-candidatos.pdf') — selectable text
  </behavior>
  <action>
    Add `invokeComparativo(vagaId, candidaturaIds)` to triagemService.ts following the submitCandidaturaWithRespostas invoke+error-map shape: invoke 'comparativo-candidatos' with body { vaga_id, candidatura_ids }; throw on transport error; map `!data.ok` by error message → surface the mixed-vaga pt-BR copy "Os candidatos selecionados pertencem a vagas diferentes. Compare candidatos de uma mesma vaga." Create `src/features/triagem/hooks/useComparativo.ts` = useMutation wrapping it, extending triagemKeys with the comparativo key, with a toast on error. Create `src/features/triagem/pdf/exportComparativo.ts` per RESEARCH Code Examples: import jsPDF from 'jspdf' + autoTable from 'jspdf-autotable'; landscape doc; head = ['Atributo', ...candidate names]; body rows = Ranking IA / Score / Pontos fortes / Gaps / Justificativa (arrays joined with '; '); autoTable with startY 22, fontSize 8, cellWidth 'wrap'; doc.save('comparativo-candidatos.pdf'). Commit via `git -c core.hooksPath=/dev/null`.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && npm run test:run -- triagemService 2>&1 | grep -qiE "pass|✓" && grep -q "comparativo-candidatos" src/features/triagem/services/triagemService.ts && grep -q "pertencem a vagas diferentes" src/features/triagem/services/triagemService.ts && grep -q "jspdf-autotable" src/features/triagem/pdf/exportComparativo.ts && grep -q "useMutation" src/features/triagem/hooks/useComparativo.ts</automated>
  </verify>
  <acceptance_criteria>
    - invokeComparativo posts {vaga_id, candidatura_ids} and maps the mixed-vaga 400 to the pt-BR copy
    - useComparativo is a useMutation with a toast on error; triagemKeys extended with comparativo key
    - exportComparativo imports jspdf + jspdf-autotable, builds candidates-as-columns head/body, calls doc.save
  </acceptance_criteria>
  <done>Invoke service + mutation hook + PDF util landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ComparativoScreen + ComparativoCandidatosPage + route + test</name>
  <files>src/features/triagem/components/ComparativoScreen.tsx, src/components/pages/ComparativoCandidatosPage.tsx, src/router/routes.tsx, src/features/triagem/components/__tests__/ComparativoScreen.test.tsx</files>
  <read_first>
    - src/components/pages/VagaCandidatosRHPage.tsx (RHLayout shell + updateCandidaturaStatus handler shape to reuse for inline actions)
    - src/components/ui/table.tsx + alert-dialog.tsx + sonner.tsx (primitives)
    - src/features/triagem/components/SugestaoIABadge.tsx (from 10-05)
    - .planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-UI-SPEC.md (B row set / sticky column / actions / export states, Copywriting confirms)
    - src/router/routes.tsx:53,302 (import + route precedent)
  </read_first>
  <behavior>
    - renders ≤10 candidate columns; rows = Ranking IA, Score IA band, Pontos fortes, Gaps, Justificativa IA, Flags (neutral), Ação
    - SugestaoIABadge (full) rendered once at the top (RNF-07a)
    - first attribute column sticky-left; horizontal scroll on overflow
    - Avançar → confirm dialog "Avançar {nome} para a próxima etapa?" (accent); Rejeitar → destructive confirm "Rejeitar {nome}?" (no long justification)
    - Exportar PDF calls exportComparativo → success toast "PDF exportado."; error toast on failure
    - mixed-vaga invoke error renders the pt-BR mixed-vaga copy
  </behavior>
  <action>
    Author `ComparativoScreen.tsx` consuming the useComparativo result (ranked_candidates from the EF) and rendering the UI-SPEC §B candidates-as-columns table inside a Glass panel: header row (name + avatar initial + ranking medal), then Ranking IA / Score IA band (reuse the 10-05 band helper) / Pontos fortes / Gaps / Justificativa IA / Flags (neutral badges) / Ação rows. First column sticky-left (sticky left-0 z-10), overflow-x-auto, 200px min column width. Render SugestaoIABadge (full) at the top. Inline Avançar (accent) + Rejeitar (destructive) per column via alert-dialog confirm calling the existing status-update / avancar_etapa flow (reuse VagaCandidatosRHPage's handler shape; Rejeitar → 'rejeitado', no long justification). Add the "Exportar PDF" accent-outline button (Download icon) with Generating/done/error states + sonner toasts, calling exportComparativo(ranking).

    Author `ComparativoCandidatosPage.tsx` (RHLayout shell wrapper) that reads the selected ids (router state / query param passed by the panel's onComparar) + vaga_id, runs useComparativo, and renders ComparativoScreen with loading/error states (mixed-vaga 400 → the pt-BR copy). Add the route `/rh/vagas/:id/comparativo` to routes.tsx with the same role={['rh','administrador']} guard (mirror line 302) and the import (mirror line 53). Wire VagaCandidatosRHPage's onComparar(ids) to navigate to this route carrying the ids.

    Author `ComparativoScreen.test.tsx` (RTL): assert ≤10 columns render, SugestaoIABadge present, Avançar/Rejeitar confirm dialogs, Exportar PDF calls a mocked exportComparativo, and the mixed-vaga error copy renders. Keep tsc baseline 293 flat. Commit via `git -c core.hooksPath=/dev/null`.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && npm run test:run -- ComparativoScreen 2>&1 | grep -qiE "pass|done" && grep -q "ComparativoCandidatosPage" src/router/routes.tsx && grep -q "Exportar PDF" src/features/triagem/components/ComparativoScreen.tsx && npm run build 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - ComparativoScreen test GREEN (≤10 columns, SugestaoIABadge, Avançar/Rejeitar confirms, mocked export, mixed-vaga copy)
    - /rh/vagas/:id/comparativo route added with role={['rh','administrador']} guard; ComparativoCandidatosPage imported + wired from the panel's onComparar
    - SugestaoIABadge rendered once at top; first column sticky-left; Exportar PDF wired to exportComparativo with toasts
    - inline Rejeitar requires NO long justification (deferred to Etapa 6); both actions go through alert-dialog confirms
    - `npm run build` exits 0; tsc baseline not grown beyond 293
  </acceptance_criteria>
  <done>Comparativo screen + page + route + PDF wired; RH triage loop complete; component test GREEN; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH client → comparativo EF | user JWT carried by functions.invoke; EF verifies + same-vaga |
| score/ranking display → action | RNF-07a: inline actions are human-confirmed, never auto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-19 | Elevation of Privilege (IDOR) | mixed-vaga selection | mitigate | EF (10-03) returns 400; the screen surfaces the pt-BR copy and does not render a partial comparativo |
| T-10-20 | — (compliance RNF-07a) | inline Avançar/Rejeitar | mitigate | both actions require an alert-dialog confirm; no score-gated auto-action; SugestaoIABadge framing prominent |
| T-10-21 | Information Disclosure | PDF export | accept | client-side jspdf renders only what is already on the authenticated RH screen; no PII leaves the session beyond on-screen data |
| T-10-SC | Tampering | jspdf/jspdf-autotable (installed 10-01) | accept | already audited [ASSUMED] + npm-view-confirmed in 10-01; no new package surface here |
</threat_model>

<verification>
- ComparativoScreen test GREEN; route added; build exit 0; tsc ≤293
- Full suite + UAT (panel + comparativo + PDF + reprocess) is the phase gate before /gsd:verify-work
</verification>

<success_criteria>
RH selects 2-10 candidatos, opens the comparativo (candidates-as-columns, ranked, AI-framed), takes human-confirmed inline actions, and exports a selectable-text PDF — the full Etapa-2 triage loop.
</success_criteria>

<output>
Create `.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-06-SUMMARY.md` when done
</output>
