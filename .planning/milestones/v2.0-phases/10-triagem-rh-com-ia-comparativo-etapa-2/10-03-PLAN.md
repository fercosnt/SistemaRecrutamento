---
phase: 10-triagem-rh-com-ia-comparativo-etapa-2
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - supabase/functions/_shared/analise-schemas.ts
  - supabase/functions/analise-candidato-individual/index.ts
  - supabase/functions/comparativo-candidatos/index.ts
autonomous: true
requirements: [TRIAGEM-01, TRIAGEM-03]
user_setup: []

must_haves:
  truths:
    - "analise-candidato-individual self-authenticates the Vault Bearer (== service_role), rejects 401 on mismatch, composes loadPrompt('cv_job_match')+callAi, maps CvJobMatch English keys → pt-BR columns, and UPSERTs analise_candidato_vaga — writing status='falhou'+erro on ANY throw (never-absent invariant) — TRIAGEM-01"
    - "The analise EF extracts CV PDF text via service_role storage download (unpdf) + truncates to a token budget before the prompt; on extraction failure it proceeds with respostas-only + flag 'cv_nao_extraido', never breaking the row (D resolved post-research)"
    - "comparativo-candidatos verifies the RH JWT (two-client D-23, JWT-ON), validates 2-10 ids all belonging to the same vaga (400 otherwise), composes loadPrompt('comparative_ranking')+callAi single-eval, and INSERTs comparativo_solicitado with latencia_ms — TRIAGEM-03"
    - "Both EFs import the AI runtime ONLY from _shared/ai-client.ts and re-implement NO retry/cache/logging; Zod schemas are copied into _shared/ (docs/ not reached at runtime)"
    - "Both EF deno suites (RED in 10-01) are now GREEN"
  artifacts:
    - path: "supabase/functions/analise-candidato-individual/index.ts"
      provides: "trigger-sink EF (--no-verify-jwt, Vault Bearer)"
      min_lines: 100
    - path: "supabase/functions/comparativo-candidatos/index.ts"
      provides: "RH-invoked EF (JWT-ON, two-client)"
      min_lines: 100
    - path: "supabase/functions/_shared/analise-schemas.ts"
      provides: "CvJobMatch + ComparativeRanking + EF body schemas in EF import scope"
  key_links:
    - from: "analise-candidato-individual/index.ts"
      to: "analise_candidato_vaga"
      via: "supabaseAdmin upsert ON CONFLICT (candidatura_id)"
      pattern: "analise_candidato_vaga"
    - from: "comparativo-candidatos/index.ts"
      to: "comparativo_solicitado"
      via: "supabaseAdmin insert"
      pattern: "comparativo_solicitado"
---

<objective>
Build the two thin Edge Functions that compose the Phase-9 AI runtime against the new tables: `analise-candidato-individual` (trigger sink, Vault Bearer self-auth, CV-PDF-text extraction + cv_job_match → pt-BR mapping → never-absent upsert) and `comparativo-candidatos` (RH-invoked two-client, 2-10 same-vaga validation, comparative_ranking single-eval → audit insert). Flip both 10-01 deno suites GREEN.

Purpose: TRIAGEM-01 analysis generation + TRIAGEM-03 comparativo. These are ~150 LoC each — input assembly + output mapping + persistence. NO AI plumbing re-implemented (callAi owns retry/cache/log/idempotency).
Output: 2 EFs + 1 shared schema copy; deno suites GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-RESEARCH.md
@.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-PATTERNS.md

<interfaces>
<!-- Exact identifiers — do not re-explore. -->

ai-client.ts re-exports (line 58): { CircuitBreaker, calculateCost, detectPromptInjection, loadPrompt, logAiCall, maskPII }
callAi(args, deps) (line 249):
  args = { prompt, rawInput, vagaRubricBlock, candidato_id, vaga_id, schema, idempotency_key }
  deps = { anthropic, openai, supabase, breaker?, zodOutputFormat?, zodResponseFormat? }
  → callAi ALREADY does: idempotency-replay → injection-detect → maskPII → breaker → Anthropic(parse+ephemeral-cache+3x retry) → OpenAI fallback → cost → logAiCall (ai_call_logs). DO NOT re-implement.
resolvedPromptFromLoaded(loaded, call_type, fallbackModelHint) (line 85)
loadPrompt(call_type, supabaseAdmin) — DB-only, active/canary

CvJobMatchSchema (copy into _shared/analise-schemas.ts) — English keys:
  reasoning(str), strengths[].{competency,evidence:Citation,impact}, gaps[].{requirement,severity,note}, competency_scores[], match_score(int 0-100), recommendation, confidence, bias_check
  MAP → row: score_match=match_score; pontos_fortes=strengths.map(s=>s.competency); gaps=gaps.map(g=>g.requirement); resumo_respostas=reasoning; flags=[] (+ 'cv_nao_extraido' on extract failure); resumo_cv=summarized-CV-text-or-fallback-note
ComparativeRankingSchema — English keys:
  reasoning, ranked_candidates[].{candidate_id,rank,composite_score,relative_strengths[],relative_weaknesses[],rationale}, recommendation.{top_choice,backup_choice,note}, ties_or_concerns[], bias_audit

cost-alerter/index.ts:113-170 — Vault Bearer self-auth verbatim:
  SERVICE_KEY=env SUPABASE_SERVICE_ROLE_KEY; bearer from Authorization 'Bearer '; expectedSecret = env override ?? SERVICE_KEY; 401 if mismatch.
  --no-verify-jwt deploy. corsHeaders + jsonResponse + errorResponse helpers at :45-70.
submit-candidatura/index.ts:85-178 — two-client D-23 (JWT-ON):
  authHeader required; supabaseUser=anon WITH Authorization → auth.getUser() (401 if fail); supabaseAdmin=service_role for reads/writes only.

candidaturas relevant cols: id, vaga_id, status, opcao_knockout_id, curriculo_url (PDF path in 'curriculos' bucket), curriculo_nome_original
respostas: respostas_formulario rows by candidatura_id (Etapa 1 answers)
analise_candidato_vaga target cols (from 10-02): candidatura_id, vaga_id, score_match, pontos_fortes[], gaps[], flags[], resumo_cv, resumo_respostas, status, erro
comparativo_solicitado target cols: vaga_id, candidatura_ids[], ranking jsonb, latencia_ms, solicitado_por

unpdf — Deno-compatible PDF text extractor (npm: specifier resolved at EF runtime). Storage download via supabaseAdmin.storage.from('curriculos').download(path).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _shared/analise-schemas.ts copy + analise-candidato-individual EF</name>
  <files>supabase/functions/_shared/analise-schemas.ts, supabase/functions/analise-candidato-individual/index.ts</files>
  <read_first>
    - supabase/functions/cost-alerter/index.ts:45-170 (CORS/response helpers + Vault Bearer self-auth + service_role client — copy verbatim)
    - supabase/functions/_shared/ai-client.ts:58,85,249 (re-exports, resolvedPromptFromLoaded, callAi signature)
    - docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts:106-133 (CvJobMatchSchema — copy into _shared/analise-schemas.ts)
    - supabase/functions/analise-candidato-individual/__tests__/index.test.ts (the RED contract from 10-01 — mapping + 401 + falhou-row assertions)
    - .planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-PATTERNS.md (Shared Pattern A/B/C/D)
  </read_first>
  <behavior>
    - Bearer absent/mismatch → 401 UNAUTHORIZED (Vault self-auth)
    - body {candidatura_id, vaga_id} → fetch candidatura (allowlist) + respostas + vaga rubric + download CV PDF
    - CV extract OK → resumo_cv from CV text; extract fails (corrupt/image) → respostas-only + flags includes 'cv_nao_extraido', NEVER throws
    - cv_job_match parse OK → upsert row: score_match=match_score, pontos_fortes=strengths.map(competency), gaps=gaps.map(requirement), resumo_respostas=reasoning, status='sucesso'
    - ANY throw (loadPrompt/storage/parse/db) → upsert {candidatura_id, vaga_id, status='falhou', erro} (never-absent invariant)
    - idempotency_key = candidatura_id (trigger); reprocess path uses `${candidatura_id}:retry:${ts}` (fresh, avoids stale-replay — Pitfall 8)
  </behavior>
  <action>
    Copy `CvJobMatchSchema` (+ its dependencies Citation/BarsDimension/enums as needed) verbatim from docs/.../00-shared-zod-schemas.ts into NEW `supabase/functions/_shared/analise-schemas.ts` (docs/ is NOT deployed — anti-pattern to reach into it at runtime). Also define the EF body Zod schema `{ candidatura_id: uuid, vaga_id: uuid }`.

    Author `analise-candidato-individual/index.ts` (~150 LoC). Copy CORS/jsonResponse/errorResponse + Vault Bearer self-auth verbatim from cost-alerter (expectedSecret = Deno.env.get('ANALISE_SECRET') ?? SERVICE_KEY). Build supabaseAdmin service_role client. Wrap the whole analysis in try/catch:
    - allowlist-select the candidatura (id, vaga_id, curriculo_url, curriculo_nome_original, status, opcao_knockout_id) + its respostas_formulario rows + the vaga rubric fields
    - download curriculo_url from the 'curriculos' bucket via supabaseAdmin.storage; extract text via unpdf (npm: specifier); truncate to a token budget (cap input tokens — DoS/cost mitigation). On extraction failure: continue with respostas-only and push 'cv_nao_extraido' into flags
    - loadPrompt('cv_job_match', supabaseAdmin) → resolvedPromptFromLoaded → callAi({ prompt, rawInput: cvText+respostas, vagaRubricBlock, candidato_id, vaga_id, schema: CvJobMatchSchema, idempotency_key: candidatura_id }, { anthropic, openai, supabase: supabaseAdmin, zodOutputFormat, zodResponseFormat })
    - MAP result.parsed (English) → pt-BR columns per the behavior block; UPSERT analise_candidato_vaga ON CONFLICT (candidatura_id) DO UPDATE (status='sucesso')
    On ANY catch: upsert {candidatura_id, vaga_id, status:'falhou', erro: e.message}. Log only ids/counts/error.code — NO CV text/respostas/score/nome (Pitfall 7 redaction). Flip the 10-01 deno suite GREEN. Commit via `git -c core.hooksPath=/dev/null`.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && deno test supabase/functions/analise-candidato-individual/ 2>&1 | grep -qiE "ok|passed" && grep -q "ON CONFLICT" supabase/functions/analise-candidato-individual/index.ts && grep -q "falhou" supabase/functions/analise-candidato-individual/index.ts && grep -q "cv_nao_extraido" supabase/functions/analise-candidato-individual/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `deno test supabase/functions/analise-candidato-individual/` is GREEN (10-01 RED → GREEN)
    - index.ts imports callAi/loadPrompt/resolvedPromptFromLoaded from ../_shared/ai-client.ts; does NOT re-implement retry/cache/logging (`grep -L "for.*retry\|setTimeout.*backoff"`)
    - English→pt-BR mapping present (score_match=match_score etc.); ON CONFLICT (candidatura_id) upsert; status='falhou' on catch; 'cv_nao_extraido' flag path
    - Zod schemas imported from _shared/analise-schemas.ts, NOT from docs/ (`! grep "docs/conhecimento"` in index.ts)
    - forbidden-strings.grep + Pitfall-7 redaction GREEN (no PII/score in logs)
  </acceptance_criteria>
  <done>analise EF composes the AI runtime, maps to pt-BR, never leaves an absent row; deno suite GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: comparativo-candidatos EF (two-client, 2-10 same-vaga, single-eval, audit)</name>
  <files>supabase/functions/comparativo-candidatos/index.ts, supabase/functions/_shared/analise-schemas.ts</files>
  <read_first>
    - supabase/functions/submit-candidatura/index.ts:85-178 (two-client D-23 JWT-ON + IDOR cross-check shape)
    - supabase/functions/_shared/ai-client.ts:249 (callAi) + prompt-loader.ts (loadPrompt('comparative_ranking') — now valid after 10-01)
    - docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts:139-166 (ComparativeRankingSchema — copy into _shared/analise-schemas.ts)
    - supabase/functions/comparativo-candidatos/__tests__/index.test.ts (RED contract from 10-01 — 2-10 + mixed-vaga 400 + audit row)
  </read_first>
  <behavior>
    - JWT absent / auth.getUser() fails → 401 UNAUTHORIZED
    - ids.length < 2 OR > 10 → 400 VALIDATION
    - fetched analise rows span >1 vaga_id OR rows.length !== ids.length → 400 VALIDATION ("vagas diferentes")
    - order the N analise rows by score_match DESC BEFORE building the prompt (position-bias anchor; single-eval V1 — double-eval deferred to V2, documented)
    - callAi comparative_ranking → returns { ranking, latencia_ms }; INSERT one comparativo_solicitado row (vaga_id, candidatura_ids, ranking jsonb, latencia_ms, solicitado_por=user.id)
  </behavior>
  <action>
    Copy `ComparativeRankingSchema` verbatim into the existing `_shared/analise-schemas.ts`. Define the EF body schema `{ vaga_id: uuid, candidatura_ids: uuid[] }`.

    Author `comparativo-candidatos/index.ts` (~150 LoC, two-client D-23, deploy WITHOUT --no-verify-jwt). Copy the authHeader → supabaseUser anon → auth.getUser() (401 on fail) → supabaseAdmin service_role pattern from submit-candidatura. Validate body 2-10. Allowlist-fetch the N analise_candidato_vaga rows (candidatura_id, vaga_id, score_match, pontos_fortes, gaps, flags, resumo_cv) by `.in('candidatura_id', ids)`. Compute `vagas = new Set(rows.map(r=>r.vaga_id))`; if `vagas.size !== 1 || rows.length !== ids.length` → 400 VALIDATION "Os candidatos pertencem a vagas diferentes." Order rows by score_match DESC, build the compact prompt input (pre-computed analyses, NOT raw CVs — token budget), single-eval. loadPrompt('comparative_ranking', supabaseAdmin) → callAi(... schema: ComparativeRankingSchema, idempotency_key fresh per request ...). Measure latencia_ms = Date.now()-start. INSERT comparativo_solicitado via supabaseAdmin. Return { ok:true, ranking: result.parsed, latencia_ms }. Add a code comment documenting the single-eval V1 simplification (double-eval → V2) per CONTEXT. Redacted logging only. Flip the 10-01 deno suite GREEN. Commit via `git -c core.hooksPath=/dev/null`.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && deno test supabase/functions/comparativo-candidatos/ 2>&1 | grep -qiE "ok|passed" && grep -q "vagas diferentes" supabase/functions/comparativo-candidatos/index.ts && grep -q "comparativo_solicitado" supabase/functions/comparativo-candidatos/index.ts && grep -q "auth.getUser" supabase/functions/comparativo-candidatos/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `deno test supabase/functions/comparativo-candidatos/` GREEN (10-01 RED → GREEN)
    - two-client D-23: auth.getUser() present (JWT-ON); NOT deployed --no-verify-jwt (deploy flag handled in 10-04)
    - 2-10 + mixed-vaga 400s present; single-eval comment documents the V1 simplification; comparativo_solicitado INSERT with latencia_ms
    - feeds compact pre-computed analyses (not raw CVs); schemas from _shared/ not docs/
  </acceptance_criteria>
  <done>comparativo EF validates 2-10 same-vaga, runs single-eval ranking, persists audit; deno suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_net → analise EF | no user JWT; Vault Bearer self-auth |
| RH client → comparativo EF | user JWT verified (two-client) |
| candidate CV/respostas → LLM | untrusted text; injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Spoofing | analise EF Bearer | mitigate | compare incoming Bearer == SERVICE_KEY, 401 on mismatch (cost-alerter precedent); --no-verify-jwt deploy in 10-04 |
| T-10-09 | Elevation of Privilege (IDOR) | comparativo same-vaga | mitigate | reject 400 when ids span >1 vaga OR count mismatch; two-client auth.getUser verifies RH |
| T-10-10 | Tampering | prompt injection via CV/respostas | mitigate | detectPromptInjection short-circuits INSIDE callAi (already wired) — EF passes raw text through callAi, never bypasses it |
| T-10-11 | Information Disclosure | EF logs | mitigate | log only ids/counts/error.code; never CV text/respostas/score/nome (Pitfall 7) |
| T-10-12 | DoS (cost) | 10-candidate token budget + CV extraction | mitigate | truncate CV text to token budget; feed compact analyses not raw CVs; ephemeral cache + idempotency replay inside callAi; ≤R$0,50/candidato ceiling |
| T-10-SC | Tampering | unpdf npm: specifier (Deno runtime) | mitigate | pin a specific unpdf version in the import specifier; resolved at EF runtime only, not in package.json; legitimacy confirmed at deploy |
</threat_model>

<verification>
- Both deno suites GREEN
- No retry/cache/log re-implementation in either EF
- Schemas copied to _shared/, not reached from docs/ at runtime
- Pitfall-7 redaction + LGPD-04 grep GREEN over the new EF dirs
</verification>

<success_criteria>
Two ~150-LoC EFs compose the Phase-9 AI runtime against the new tables; analise never leaves an absent row and maps English→pt-BR; comparativo enforces 2-10 same-vaga and persists the audit; both deno suites GREEN.
</success_criteria>

<output>
Create `.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-03-SUMMARY.md` when done
</output>
