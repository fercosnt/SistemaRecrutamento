---
phase: 10-triagem-rh-com-ia-comparativo-etapa-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/prompt-loader.ts
  - supabase/functions/analise-candidato-individual/__tests__/index.test.ts
  - supabase/functions/comparativo-candidatos/__tests__/index.test.ts
  - src/features/triagem/services/__tests__/triagemService.test.ts
  - src/features/triagem/components/__tests__/TriagemTable.test.tsx
  - src/__tests__/guards/forbidden-strings.grep.test.ts
  - .planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-SQL-SMOKES.md
  - package.json
  - package-lock.json
autonomous: true
requirements: [TRIAGEM-01, TRIAGEM-02, TRIAGEM-03, TRIAGEM-04]
user_setup: []

must_haves:
  truths:
    - "Every Phase-10 surface (both EFs, triagemService, TriagemTable) has a calibrated RED test that fails for module-not-found BEFORE its implementation lands (smoke-runtime gate, Phase-4 lesson) — traceability for TRIAGEM-01/02/03/04"
    - "loadPrompt('comparative_ranking', ...) no longer throws SchemaVersionMismatchError because comparative_ranking is in SCHEMA_VERSIONS (Pitfall 1 closed)"
    - "LGPD-04 forbidden-strings grep guard scans the new analise-candidato-individual + comparativo-candidatos + src/features/triagem paths and is GREEN"
    - "jspdf@4.2.1 + jspdf-autotable@5.0.8 are installed and the lockfile is committed"
    - "A SQL-smoke runbook exists covering trigger-post-knockout-only, analise_candidato_vaga + comparativo_solicitado RLS 3-role matrix, and upsert idempotency"
  artifacts:
    - path: "supabase/functions/analise-candidato-individual/__tests__/index.test.ts"
      provides: "RED deno test for the trigger-sink EF (mock deps)"
    - path: "supabase/functions/comparativo-candidatos/__tests__/index.test.ts"
      provides: "RED deno test for 2-10 + same-vaga validation"
    - path: "src/features/triagem/services/__tests__/triagemService.test.ts"
      provides: "RED vitest for allowlist + sort/paginate + invoke"
    - path: "src/features/triagem/components/__tests__/TriagemTable.test.tsx"
      provides: "RED vitest for score bands + 2-10 compare gating + SugestaoIABadge"
    - path: ".planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-SQL-SMOKES.md"
      provides: "SQL smoke runbook for migration/RLS/trigger verification"
  key_links:
    - from: "supabase/functions/_shared/prompt-loader.ts"
      to: "loadPrompt('comparative_ranking')"
      via: "SCHEMA_VERSIONS map"
      pattern: "comparative_ranking"
---

<objective>
Wave-0 RED scaffolds + the two code gaps that block every downstream EF: add `comparative_ranking` to `prompt-loader.ts` SCHEMA_VERSIONS (Pitfall 1), extend the LGPD-04 grep guard to the new paths, install the PDF libs, and author the SQL-smoke runbook. Every Phase-10 production surface gets a calibrated failing test BEFORE its implementation lands (smoke-runtime gate — the central Phase-4 lesson).

Purpose: Honor the project smoke-runtime gate — no new surface ships without a calibrated RED test first. Close the SCHEMA_VERSIONS gap now so the comparativo EF can resolve its prompt in Wave 3.
Output: 4 RED test files (deno + vitest), prompt-loader edit, grep-guard extension, jspdf install, SQL-smoke runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-RESEARCH.md
@.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-PATTERNS.md
@.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-VALIDATION.md

<interfaces>
<!-- Exact identifiers the executor needs — extracted from codebase. Do not re-explore. -->

prompt-loader.ts SCHEMA_VERSIONS (current — line ~33):
  { cv_summary, cv_job_match, sjt_evaluation, interview_questions, interview_summary, reference_check, final_recommendation } all "1.0.0"
  MISSING: comparative_ranking → add `comparative_ranking: "1.0.0"` (confirmed seed semver = 1.0.0 per 00-shared-zod-schemas.ts:357 COMPARATIVE_RANKING_SCHEMA_VERSION)

forbidden-strings.grep.test.ts:
  SCAN_ROOTS = ['src', 'supabase/functions']  (line 48) — already recursive over both roots, so new EFs + src/features/triagem are auto-covered. Add an explicit assertion that the new paths are non-empty when scanned (sanity that the guard actually reaches them), OR confirm SCAN_ROOTS already covers them and leave a comment. RNF_12_TERMS list at line 59.

CvJobMatchSchema (00-shared-zod-schemas.ts:106) — ENGLISH keys:
  reasoning, strengths[].{competency,evidence,impact}, gaps[].{requirement,severity,note}, competency_scores[], match_score (int 0-100), recommendation, confidence, bias_check
ComparativeRankingSchema (00-shared-zod-schemas.ts:139) — ENGLISH keys:
  reasoning, ranked_candidates[].{candidate_id,rank,composite_score,relative_strengths[],relative_weaknesses[],rationale}, recommendation.{top_choice,backup_choice,note}, ties_or_concerns[], bias_audit

callAi(args, deps) from ai-client.ts:249 — deps-injected mocks (orchestrator-decision #2, no network):
  args = { prompt, rawInput, vagaRubricBlock, candidato_id, vaga_id, schema, idempotency_key }
  deps = { anthropic, openai, supabase, breaker?, zodOutputFormat?, zodResponseFormat? }
  Re-exports at ai-client.ts:58 — { CircuitBreaker, calculateCost, detectPromptInjection, loadPrompt, logAiCall, maskPII }

Deno test precedent: supabase/functions/_shared/__tests__/ai-client.test.ts (mock Anthropic/OpenAI/supabase via deps; pin deno.land/std@0.224.0/assert)
Vitest precedent: src/features/vagas/services/__tests__ ; happy-dom; include **/__tests__/**/*.{test,spec}.{ts,tsx}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: prompt-loader comparative_ranking + LGPD-04 grep extension + jspdf install</name>
  <files>supabase/functions/_shared/prompt-loader.ts, src/__tests__/guards/forbidden-strings.grep.test.ts, package.json, package-lock.json</files>
  <read_first>
    - supabase/functions/_shared/prompt-loader.ts (the SCHEMA_VERSIONS map ~line 33)
    - src/__tests__/guards/forbidden-strings.grep.test.ts (SCAN_ROOTS line 48, RNF_12_TERMS line 59)
    - docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts:357 (confirm COMPARATIVE_RANKING_SCHEMA_VERSION = "1.0.0")
    - .planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-RESEARCH.md (Pitfall 1, Package Legitimacy Audit)
  </read_first>
  <action>
    Add `comparative_ranking: "1.0.0"` to the SCHEMA_VERSIONS record in prompt-loader.ts (Pitfall 1 — loadPrompt('comparative_ranking') throws SchemaVersionMismatchError without it). Keep the existing 7 keys and the PROMPT_COLUMNS allowlist comment intact; do NOT touch loadPrompt body or the error classes.

    In forbidden-strings.grep.test.ts: SCAN_ROOTS already = ['src','supabase/functions'] so the new analise-candidato-individual, comparativo-candidatos, and src/features/triagem paths are recursively covered automatically. Add one assertion that verifies the scan actually reaches at least one file under supabase/functions/ (guard-the-guard sanity, so a future glob regression cannot silently exclude the new EF dirs). Do not weaken RNF_12_TERMS.

    Run `npm view jspdf version` and `npm view jspdf-autotable version` to confirm 4.2.1+ / 5.0.8+ exist (Package Legitimacy Audit [ASSUMED] — these are decade-old, multi-M-download, zero-postinstall libs; the npm view confirm satisfies the gate). Then `npm install jspdf@4.2.1 jspdf-autotable@5.0.8`. Do NOT add @anthropic-ai/sdk / openai / zod to package.json — those are Deno npm: specifiers resolved at EF runtime only.

    Commit via `git -c core.hooksPath=/dev/null` (project tsc-pre-commit-hook convention).
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && grep -c "comparative_ranking" supabase/functions/_shared/prompt-loader.ts && node -e "const p=require('./package.json'); if(!p.dependencies.jspdf||!p.dependencies['jspdf-autotable']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c comparative_ranking supabase/functions/_shared/prompt-loader.ts` returns ≥1
    - `package.json` dependencies include `jspdf` and `jspdf-autotable`; `package-lock.json` updated
    - `npm run test:run -- forbidden-strings.grep` is GREEN (no forbidden product terms; guard reaches supabase/functions)
    - prompt-loader loadPrompt body + error classes unchanged (diff touches only the SCHEMA_VERSIONS map)
  </acceptance_criteria>
  <done>comparative_ranking registered; grep guard extended + green; jspdf libs installed + lockfile committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deno RED tests for both EFs (mock deps, calibrated module-not-found)</name>
  <files>supabase/functions/analise-candidato-individual/__tests__/index.test.ts, supabase/functions/comparativo-candidatos/__tests__/index.test.ts</files>
  <read_first>
    - supabase/functions/_shared/__tests__/ai-client.test.ts (deno deps-injection mock pattern, std@0.224.0/assert import)
    - supabase/functions/cost-alerter/index.ts:113-170 (Vault Bearer self-auth shape the analise EF must satisfy)
    - supabase/functions/submit-candidatura/index.ts:85-178 (two-client D-23 + IDOR cross-check shape the comparativo EF must satisfy)
    - docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts:106,139 (CvJobMatch / ComparativeRanking English keys)
  </read_first>
  <behavior>
    analise-candidato-individual RED tests (assert against the FUTURE index.ts, expected to fail module-not-found now):
    - Bearer mismatch / absent → 401 UNAUTHORIZED (Vault self-auth, cost-alerter precedent)
    - On a mocked successful CvJobMatch parse, the upserted row maps English→pt-BR: score_match=match_score, pontos_fortes=strengths.map(competency), gaps=gaps.map(requirement), status='sucesso'
    - On any thrown error (prompt-not-configured / storage / parse), a row with status='falhou' + erro is upserted (never-absent invariant)
    comparativo-candidatos RED tests:
    - ids.length < 2 OR > 10 → 400 VALIDATION
    - rows span >1 vaga_id OR rows.length !== ids.length → 400 VALIDATION ("vagas diferentes")
    - happy path returns { ranking, latencia_ms } and writes one comparativo_solicitado row
  </behavior>
  <action>
    Author both `__tests__/index.test.ts` as RED deno tests using the ai-client.test.ts dependency-injection mock style (mock anthropic/openai/supabase via the `deps` arg — NO real npm: SDK import, NO network; orchestrator-decision #2). Import assertions from deno.land/std@0.224.0/assert. Tests reference the not-yet-existing `../index.ts` so the suite fails with module-not-found (calibrated Wave-0 RED, not syntax errors). Encode the exact English→pt-BR mapping table from the behavior block as assertions. For comparativo, assert the 400 codes for <2/>10 and mixed-vaga. Use `idempotency_key` shape `${candidatura_id}` (analise) and a fresh-key note for reprocess. Commit via `git -c core.hooksPath=/dev/null`.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && deno test supabase/functions/analise-candidato-individual/ supabase/functions/comparativo-candidatos/ 2>&1 | grep -qiE "module not found|no such file|cannot find|error" && echo "RED-OK calibrated failure"</automated>
  </verify>
  <acceptance_criteria>
    - Both deno suites FAIL with module-not-found against the absent `../index.ts` (calibrated RED, not a syntax/parse error in the test file itself)
    - Tests use deps-injected mocks only — `grep -L "fetch(" ` shows no real network; no `npm:@anthropic` import in the test files
    - The English→pt-BR mapping assertions and the 2-10/mixed-vaga 400 assertions are present (`grep score_match`, `grep "vagas diferentes"`)
  </acceptance_criteria>
  <done>Both EF deno suites exist and fail RED for module-not-found; mapping + validation contracts encoded as assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: vitest RED stubs (triagemService allowlist + TriagemTable bands/gating) + SQL-smoke runbook</name>
  <files>src/features/triagem/services/__tests__/triagemService.test.ts, src/features/triagem/components/__tests__/TriagemTable.test.tsx, .planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-SQL-SMOKES.md</files>
  <read_first>
    - src/features/vagas/services/candidaturasService.ts:1145-1230 (listByVaga select('*') hazard — the anti-pattern these tests forbid)
    - src/features/vagas/hooks/useCandidaturas.ts:40-75 (candidaturasKeys hierarchy the new triagemKeys mirror)
    - .planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-UI-SPEC.md (§A score bands 70/40 thresholds, §A compare-bar 2-10 gating, §C SugestaoIABadge)
    - src/__tests__/guards/forbidden-strings.grep.test.ts (reference for fs+regex node test style)
  </read_first>
  <behavior>
    triagemService.test.ts (RED against future src/features/triagem/services/triagemService.ts):
    - the panel read's select() string contains NO `*`, NO `cpf`, NO `data_nascimento`, NO `email`, NO `celular` (allowlist projection — [[reference_select_star_leaks_pii]])
    - the select() joins analise:analise_candidato_vaga ( score_match, pontos_fortes, gaps, flags, status )
    - default order is score_match DESC nulls-last; .range() math = (page-1)*limit for 20/page
    - invokeComparativo posts { vaga_id, candidatura_ids } to functions.invoke('comparativo-candidatos')
    - invokeComparativo maps the EF mixed-vaga 400 to the exact pt-BR copy "Os candidatos selecionados pertencem a vagas diferentes. Compare candidatos de uma mesma vaga." (asserted against a mocked !data.ok response) — the contract 10-06/T1 implements
    TriagemTable.test.tsx (RED against future component):
    - score 85 → verde band chip; 55 → amarelo; 20 → vermelho; null → "—" sem-análise band (UI-SPEC §A thresholds 70/40)
    - "Comparar" disabled at <2 selected, enabled at 2-10, checkboxes disabled past 10
    - SugestaoIABadge text "Sugestão da IA — decisão é sempre humana" rendered (RNF-07a)
    - a falhou row shows visible "Reprocessar análise" text label (not tooltip-only)
  </behavior>
  <action>
    Author the two vitest RED stubs importing from the not-yet-existing `../triagemService` and `../TriagemTable` so they fail module-not-found (calibrated RED). For the service test, mock the supabase chain (`.from().select().eq().is().order().range()`) and assert the select() string via the captured argument — forbid `*`/`cpf`/`data_nascimento`/`email`/`celular`, require the analise join. ALSO add an `invokeComparativo` describe block: mock `supabase.functions.invoke` returning `{ data: { ok:false, error_code:'MIXED_VAGA' }, error:null }` and assert the thrown/returned message equals the exact pt-BR copy "Os candidatos selecionados pertencem a vagas diferentes. Compare candidatos de uma mesma vaga." (the Wave-0 contract 10-06/T1 must satisfy). For TriagemTable, render via RTL (happy-dom) and assert band classes by score, compare-bar gating, the SugestaoIABadge copy, and the Reprocessar label.

    Author 10-SQL-SMOKES.md: a runbook (executed later via Supabase MCP execute_sql with set_config('request.jwt.claims',...)) with named smokes: SMOKE-1 trigger fires net.http_post ONLY for survivors (insert knockout row → no dispatch; insert survivor → dispatch); SMOKE-2 candidato JWT SELECT on analise_candidato_vaga returns 0 rows (RLS DENY); SMOKE-3 role=rh + role=administrador SELECT assertions; SMOKE-4 UNIQUE(candidatura_id) upsert keeps exactly one row (ON CONFLICT DO UPDATE); SMOKE-5 comparativo_solicitado audit row written with candidatura_ids + ranking + latencia_ms. Each smoke = exact SQL + expected result.

    Commit via `git -c core.hooksPath=/dev/null`.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && npm run test:run -- triagemService TriagemTable 2>&1 | grep -qiE "fail|cannot find module|error" && test -f .planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-SQL-SMOKES.md && echo "RED-OK + runbook present"</automated>
  </verify>
  <acceptance_criteria>
    - Both vitest stubs FAIL (module-not-found against absent service/component) — calibrated RED
    - `grep -E "cpf|data_nascimento|select\('\\*'\)" src/features/triagem/services/__tests__/triagemService.test.ts` shows them only as FORBIDDEN-assertions, never as expected output
    - 10-SQL-SMOKES.md contains SMOKE-1..SMOKE-5 with concrete SQL + expected results
  </acceptance_criteria>
  <done>Two vitest RED stubs fail module-not-found; SQL-smoke runbook authored with 5 named smokes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → real secrets | tests must never embed ANTHROPIC/OPENAI keys or service_role; mocks only |
| grep guard → product language | new EF/frontend paths must be scanned for forbidden terms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | triagemService allowlist | mitigate | RED test asserts select() forbids cpf/data_nascimento/email/celular/`*` BEFORE the service exists ([[reference_select_star_leaks_pii]]) |
| T-10-02 | Spoofing | analise EF Bearer | mitigate | deno RED test asserts 401 on Bearer mismatch before impl (cost-alerter precedent) |
| T-10-03 | Tampering | LGPD-04 product language | mitigate | forbidden-strings.grep extended to new paths, GREEN |
| T-10-SC | Tampering | npm jspdf/jspdf-autotable install | mitigate | `npm view` confirm at install ([ASSUMED] in audit — decade-old, zero-postinstall); no [SUS]/[SLOP] pkgs |
</threat_model>

<verification>
- `npm run test:run -- forbidden-strings.grep` GREEN
- Both EF deno suites + both vitest stubs FAIL calibrated-RED (module-not-found)
- `comparative_ranking` present in prompt-loader SCHEMA_VERSIONS
- jspdf + jspdf-autotable in package.json + lockfile
</verification>

<success_criteria>
Every Phase-10 production surface has a calibrated failing test; the SCHEMA_VERSIONS gap is closed; PDF libs installed; SQL-smoke runbook authored. Wave-0 contract honored.
</success_criteria>

<output>
Create `.planning/phases/10-triagem-rh-com-ia-comparativo-etapa-2/10-01-SUMMARY.md` when done
</output>
