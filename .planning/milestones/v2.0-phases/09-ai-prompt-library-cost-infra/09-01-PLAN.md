---
phase: 09-ai-prompt-library-cost-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/guards/forbidden-strings.grep.test.ts
  - supabase/functions/_shared/__tests__/pii-masker.test.ts
  - supabase/functions/_shared/__tests__/circuit-breaker.test.ts
  - supabase/functions/_shared/__tests__/injection-detector.test.ts
  - supabase/functions/_shared/__tests__/ai-cost.test.ts
  - supabase/functions/_shared/__tests__/ai-client.test.ts
  - scripts/__tests__/sync-prompts.test.ts
  - .planning/phases/09-ai-prompt-library-cost-infra/09-SQL-SMOKE-RUNBOOK.md
autonomous: true
requirements: [IA-01, IA-02, IA-03, IA-04, LGPD-04]
must_haves:
  truths:
    - "Wave-0 RED scaffolds exist for every Phase-9 code surface per orchestrator-decision #2 (mocked Deno tests are the autonomous proof)"
    - "forbidden-strings.grep.test.ts is the LGPD-04 gate, GREEN only when src/ + supabase/functions/ are clean"
    - "SQL-smoke runbook documents the IA-04 cost-anomaly trigger + IA-01 promote/rollback RPC fixture checks (D-22 MCP apply path)"
  artifacts:
    - path: "src/__tests__/guards/forbidden-strings.grep.test.ts"
      provides: "LGPD-04 CI grep guard"
      min_lines: 30
    - path: "supabase/functions/_shared/__tests__/ai-client.test.ts"
      provides: "Deno RED test for ai-client (mocked Anthropic/OpenAI) — IA-02/03/04"
      min_lines: 20
    - path: "scripts/__tests__/sync-prompts.test.ts"
      provides: "Deno RED test for sync-prompts frontmatter Zod + content_hash determinism — IA-01"
      min_lines: 20
    - path: ".planning/phases/09-ai-prompt-library-cost-infra/09-SQL-SMOKE-RUNBOOK.md"
      provides: "SQL smoke checks for migration apply (IA-01 RPCs + IA-04 trigger)"
      min_lines: 20
  key_links:
    - from: "src/__tests__/guards/forbidden-strings.grep.test.ts"
      to: "src/ + supabase/functions/"
      via: "node:fs recursive scan with __tests__ self-exclusion"
      pattern: "SCAN_ROOTS|forbidden|psicol"
---

<objective>
Author the Wave-0 RED test scaffolds and the SQL-smoke runbook for Phase 9, so every downstream code surface has a calibrated failing test BEFORE its implementation lands (smoke-runtime gate established Phase 4.1; Nyquist rule).

Purpose: Backend/infra phase with NO real AI consumer; orchestrator-decision #2 makes mocked Deno unit tests the autonomous proof. The RED scaffolds make the RED-then-GREEN contract explicit and prevent "green gates but unusable" (the central Phase 4 lesson).
Output: 1 Vitest grep guard, 5 Deno test stubs (mocked SDK), 1 Deno sync-script test, 1 SQL-smoke runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-PATTERNS.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-VALIDATION.md

<interfaces>
<!-- Exact analogs the executor copies. Do not explore the codebase for these. -->

LGPD-04 grep guard analog: src/features/auth/utils/__tests__/pitfall7.grep.test.ts
  - imports { existsSync, readFileSync, readdirSync, statSync } from 'node:fs', { join, resolve } from 'node:path'
  - recursive collectFiles() that skips entry === '__tests__' and node_modules (self-exclusion is load-bearing)
  - accumulates violations[] then throws-with-list so CI prints offending file:line

Deno test analog: supabase/functions/_shared/__tests__/strict-schema.test.ts
  - Deno.test() blocks; uses node:fs source-text probe idiom because Vitest under Node cannot resolve esm.sh/npm: URL specifiers
  - run via: deno test supabase/functions/_shared/__tests__/

LGPD-04 FORBIDDEN regex (RESEARCH §Code Examples):
  /teste\s+psicol[oó]gico|teste\s+psicot[eé]cnico|psicot[eé]cnico|laudo\s+psicol[oó]gico|psic[oó]logo/i
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LGPD-04 forbidden-strings Vitest grep guard</name>
  <read_first>
    - src/features/auth/utils/__tests__/pitfall7.grep.test.ts (exact analog — copy whole shape)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md §Code Examples (LGPD-04 guard) + §Common Pitfalls
    - .planning/phases/09-ai-prompt-library-cost-infra/09-UI-SPEC.md §Copywriting Contract (forbidden-string note)
  </read_first>
  <files>src/__tests__/guards/forbidden-strings.grep.test.ts</files>
  <action>Create the LGPD-04 grep guard by copying the pitfall7.grep.test.ts shape. Set ROOT = resolve(__dirname, '../../..') (file is 3 levels deep at src/__tests__/guards/). Set SCAN_ROOTS = ['src', 'supabase/functions'] (exclude docs/ and .planning/ per locked decision — those legitimately cite the terms). FORBIDDEN regex per the interfaces block. Keep the __tests__ + node_modules self-exclusion so the guard's own regex literal does not trip it. Scan only .ts/.tsx files. Accumulate violations[] and assert toHaveLength(0) with the offending file:line list in the message. Add one sub-test that asserts the regex matches each of the 5 RNF-12 terms (proves the regex is correct independent of the scan). Header comment: this is the LGPD-04 / RNF-12 gate, lists the 5 forbidden terms, names the product-language replacement ("avaliacao comportamental/cognitiva"), and states it rides ci.yml's existing unit job via npm run test:run (no new workflow).</action>
  <verify>
    <automated>npx vitest run src/__tests__/guards/forbidden-strings.grep.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/__tests__/guards/forbidden-strings.grep.test.ts and is picked up by Vitest.
    - A sub-test asserts the FORBIDDEN regex matches all 5 RNF-12 terms.
    - source assertion: grep "SCAN_ROOTS" returns the array ['src', 'supabase/functions']; __tests__ self-exclusion present.
    - Test runs with no module-resolution error (node:fs only).
  </acceptance_criteria>
  <done>The LGPD-04 grep guard exists, its regex matches all 5 forbidden terms in a sub-assertion, and it scans src/ + supabase/functions/ with __tests__ self-exclusion.</done>
</task>

<task type="auto">
  <name>Task 2: Deno RED test stubs for the 6 _shared helpers (mocked SDK)</name>
  <read_first>
    - supabase/functions/_shared/__tests__/strict-schema.test.ts (Deno test layout + node:fs probe idiom analog)
    - docs/conhecimento/prompts/templates/08-edge-function-reference.ts (CircuitBreaker class L47, injection patterns L82, PII_RULES L106, COST_PER_TOKEN L141 — the logic the helpers extract)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md §Validation Architecture (Phase Requirements to Test Map) + §Code Examples (cost calc verified pricing)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-PATTERNS.md §Deno tests
  </read_first>
  <files>supabase/functions/_shared/__tests__/pii-masker.test.ts, supabase/functions/_shared/__tests__/circuit-breaker.test.ts, supabase/functions/_shared/__tests__/injection-detector.test.ts, supabase/functions/_shared/__tests__/ai-cost.test.ts, supabase/functions/_shared/__tests__/ai-client.test.ts</files>
  <action>Create 5 Deno test files asserting the behavior each helper MUST satisfy, importing the not-yet-existing helper from '../<helper>.ts' (RED until Plans 04/05 land). (1) pii-masker.test.ts: maskPII() replaces CPF, CNPJ, email, telefone, data-nasc, endereco, RG with the bracketed placeholders (PT-BR rules from reference §PII) and returns the placeholders-found list (IA-02 / Pitfall 6). (2) circuit-breaker.test.ts: THRESHOLD=5 failures within RESET_MS=60000 opens the breaker (canRequest()===false); half-open after reset allows 1 probe; success resets to CLOSED (IA-04 / RF-PL-15). (3) injection-detector.test.ts: the 8 detection patterns flag the known adversarial payloads (the reference's INJECTION_PATTERNS set), returning detected:true + the matched pattern (RF-PL-18). (4) ai-cost.test.ts: calculateCost() uses the verified COST_PER_TOKEN table — claude-sonnet-4-6 input 3e-6 / cached_read 0.3e-6 / output 15e-6; claude-haiku-4-5 1e-6 / 0.1e-6 / 5e-6; gpt-4o-mini 0.15e-6 / 0.075e-6 / 0.6e-6 — and cached tokens are billed at cached_read not input (IA-03). (5) ai-client.test.ts with a MOCKED Anthropic SDK (messages.parse returns a fixture carrying usage.cache_read_input_tokens) and a MOCKED supabase client, assert callAi() (a) masks PII before any log write, (b) picks Haiku for cv_summary / Sonnet for the other 6 call_types, (c) emits system + (vaga+rubric) blocks with cache_control type ephemeral, (d) after the breaker opens routes to OpenAI gpt-4o-mini with provider='openai' + error_code='anthropic_circuit_open', (e) INSERTs an ai_call_logs row carrying prompt_version, model_id, input_hash, output, cost_usd (IA-02/03/04). Pin mocked SDK imports to npm:@anthropic-ai/sdk@0.102.0 / npm:openai@6.42.0 / npm:zod@3.25.76 (re-verify with npm view at execute time — RESEARCH A1). Header comment in each file names which plan flips it GREEN and states NO real API call happens (orchestrator-decision #2).</action>
  <verify>
    <automated>command -v deno >/dev/null 2>&1 && deno test --no-check supabase/functions/_shared/__tests__/ 2>&1 | tail -20 || echo "deno not on PATH — confirm files + structure"; ls supabase/functions/_shared/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - All 5 Deno test files exist under supabase/functions/_shared/__tests__/.
    - source assertion: grep "IA-0" across the 5 files returns >=5 hits (each names the IA-0x behavior it gates).
    - ai-client.test.ts mocks Anthropic AND OpenAI AND supabase — no real network import; header states "no real API call (orchestrator-decision #2)".
    - ai-cost.test.ts asserts the exact COST_PER_TOKEN values for all 3 models.
    - Tests are RED (helpers absent) — failure is module-not-found or assertion, NOT a syntax error.
  </acceptance_criteria>
  <done>5 RED Deno test files exist with mocked SDKs, each gating a named IA-0x behavior, with no real network calls.</done>
</task>

<task type="auto">
  <name>Task 3: sync-prompts Deno RED test + SQL-smoke runbook</name>
  <read_first>
    - supabase/functions/_shared/__tests__/strict-schema.test.ts (Deno test analog)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md §Code Examples (content_hash SHA-256) + §Validation Architecture (Test Map) + §Common Pitfalls (4 = 42601, 5 = unique_active EXCLUDE race)
    - docs/conhecimento/prompts/templates/01-cv-summary.md (frontmatter shape)
    - docs/prds/m2-funil-rh/PRD-ai-prompt-library-m2.md §8.1 (RPC bodies) + §6.1 RF-PL-01/02/11
  </read_first>
  <files>scripts/__tests__/sync-prompts.test.ts, .planning/phases/09-ai-prompt-library-cost-infra/09-SQL-SMOKE-RUNBOOK.md</files>
  <action>(A) scripts/__tests__/sync-prompts.test.ts (Deno, RED, imports '../sync-prompts.ts'): assert (1) contentHash(system,user,fmNoHash) is a deterministic SHA-256 hex string — same input gives same hash, and reordered-but-equal frontmatter gives the same hash after canonical JSON.stringify; (2) frontmatter Zod validation rejects a missing required field and rejects a semver not matching ^\d+\.\d+\.\d+$ (RF-PL-01); (3) the UPSERT shape writes is_active=false + is_canary=false (RF-PL-07) and is idempotent on ON CONFLICT(content_hash) DO NOTHING. Header comment: flips GREEN in Plan 06. (B) 09-SQL-SMOKE-RUNBOOK.md: document the post-apply SQL smokes the [BLOCKING] migration task (Plan 07) must pass, each a copy-paste SQL block + expected result: SMOKE-1 all 6 tables exist with RLS enabled (ai_call_logs, prompt_versions, candidate_ai_decisions, ai_cost_daily, data_deletion_log, recruiter_alerts) via pg_tables + pg_policies; SMOKE-2 enums llm_call_type (7 values) / llm_provider / candidate_status created; SMOKE-3 unique_active_per_type EXCLUDE constraint blocks 2 active+non-canary rows of the same call_type (Pitfall 5); SMOKE-4 immutability trigger blocks UPDATE of system_template on a deployed row (RF-PL-04); SMOKE-5 promote_to_canary RAISEs P0001 for canary_pct outside 1..50, and 42501 for a non-administrador JWT (set via set_config request.jwt.claims — Phase 8 precedent); SMOKE-6 cost-anomaly trigger: INSERT an ai_cost_daily row above a PRD threshold and assert a net.http_post was queued (SELECT from net.http_request_queue) — IA-04; SMOKE-7 pg_cron jobs ai-cost-aggregation (01:30) + ai-logs-retention-cleanup (02:00) registered in cron.job. State the smokes run via Supabase MCP execute_sql against a throwaway fixture (ROLLBACK-free cleanup, Phase 8 precedent).</action>
  <verify>
    <automated>ls scripts/__tests__/sync-prompts.test.ts .planning/phases/09-ai-prompt-library-cost-infra/09-SQL-SMOKE-RUNBOOK.md && grep -c "SMOKE-" .planning/phases/09-ai-prompt-library-cost-infra/09-SQL-SMOKE-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - scripts/__tests__/sync-prompts.test.ts exists, imports '../sync-prompts.ts', and asserts hash determinism + Zod frontmatter rejection + idempotent UPSERT.
    - source assertion: grep -c "SMOKE-" on the runbook returns >=7.
    - The runbook names all 6 tables, the 3 enums, and the pg_net + pg_cron checks.
  </acceptance_criteria>
  <done>The sync-prompts RED Deno test and the 7-smoke SQL runbook exist with the migration-apply checks documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree -> CI | LGPD-04 grep guard scans committed source for forbidden product copy |
| test fixtures -> n/a | RED scaffolds only; no runtime trust boundary crossed this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Compliance (LGPD/RNF-12) | forbidden product copy in src/ + supabase/functions/ | mitigate | LGPD-04 Vitest grep guard fails CI on any of the 5 forbidden terms (Task 1) |
| T-09-02 | Information Disclosure | PII appearing in test fixtures | mitigate | Test fixtures use synthetic PII placeholders only; pii-masker.test.ts asserts masking before any log write |
| T-09-SC | Tampering | npm/Deno SDK installs (mocked in tests) | mitigate | SDKs are mocked here (no install); legitimacy gate lands in Plans 05/07 before any live deploy; slopcheck/human-verify per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- npx vitest run on the LGPD-04 guard executes (node:fs only, no resolution error).
- The 5 Deno _shared test files + the sync-prompts test exist and are RED against absent implementations.
- The SQL-smoke runbook contains >=7 SMOKE- blocks covering tables, enums, EXCLUDE constraint, immutability trigger, RPC auth, pg_net trigger, pg_cron.
</verification>

<success_criteria>
- 8 files created; every downstream Phase-9 surface has a calibrated RED test or a documented smoke.
- LGPD-04 guard regex proven correct via a sub-assertion against all 5 terms.
- No new tsc errors above baseline (test files only); npm run test:run discovers the new Vitest guard.
</success_criteria>

<output>
Create `.planning/phases/09-ai-prompt-library-cost-infra/09-01-SUMMARY.md` when done.
</output>
