---
phase: 09-ai-prompt-library-cost-infra
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts
  - docs/conhecimento/prompts/templates/01-cv-summary.md
  - docs/conhecimento/prompts/templates/02-cv-job-match.md
  - docs/conhecimento/prompts/templates/03-comparative-ranking.md
  - docs/conhecimento/prompts/templates/04-interview-guide.md
  - docs/conhecimento/prompts/templates/05-transcript-analysis.md
  - docs/conhecimento/prompts/templates/06-culture-fit-essay.md
  - docs/conhecimento/prompts/templates/07-work-sample-sjt.md
  - docs/conhecimento/prompts/CHANGELOG.md
autonomous: true
requirements: [IA-01]
must_haves:
  truths:
    - "The 7 templates carry the standardized frontmatter the CI sync script Zod-validates (per PRD §6.1 RF-PL-01 + IA-01)"
    - "00-shared-zod-schemas.ts imports zod >=3.25 (the messages.parse/zodOutputFormat helper peer-dep — RESEARCH Pitfall 1 / orchestrator-decision #6)"
    - "Templates contain no LGPD-04 forbidden product copy (RNF-12)"
  artifacts:
    - path: "docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts"
      provides: "shared Zod schemas + *_SCHEMA_VERSION exports, zod bumped to >=3.25"
      contains: "SCHEMA_VERSION"
    - path: "docs/conhecimento/prompts/templates/01-cv-summary.md"
      provides: "standardized frontmatter (semver + content_hash tbd + schema_version_required + model_id + fallback_model_id + temperature + max_tokens)"
      contains: "schema_version_required"
    - path: "docs/conhecimento/prompts/CHANGELOG.md"
      provides: "global SemVer changelog for the 7 prompts"
      min_lines: 10
  key_links:
    - from: "templates/*.md frontmatter"
      to: "scripts/sync-prompts.ts (Plan 06)"
      via: "Zod-validated frontmatter shape"
      pattern: "schema_version_required|content_hash|semver"
---

<objective>
Standardize the 7 prompt-template frontmatter blocks to the exact shape the CI sync script validates, and bump the shared Zod schemas to the version the current Anthropic/OpenAI structured-output helpers require — without renaming any file (filename = "current"; SemVer + hash live in frontmatter; DB is runtime; PRD decision #1/#4).

Purpose: IA-01 requires the 7 prompts to be a versioned library with hybrid git->DB versioning. The sync script (Plan 06) Zod-validates this frontmatter; a missing field or a non-SemVer string must fail the build (RF-PL-01). The zod bump (Pitfall 1) is load-bearing — messages.parse/zodOutputFormat needs zod >=3.25.
Output: 7 templates with standardized frontmatter, 00-shared-zod-schemas.ts on zod >=3.25, a CHANGELOG.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-PATTERNS.md

<interfaces>
<!-- Current frontmatter (already correct) — standardize the OTHER 6 to match -->
01-cv-summary.md frontmatter keys (the canonical shape):
  id, call_type, semver ("X.Y.Z" quoted), content_hash (literal: tbd), schema_version_required,
  model_id, fallback_model_id, temperature, max_tokens, change_summary, changed_by, created_at,
  estimated_cost_per_call_usd

Model assignment (PRD decision #5): cv_summary -> claude-haiku-4-5; the other 6 -> claude-sonnet-4-6.
All fallback_model_id -> gpt-4o-mini.

00-shared-zod-schemas.ts current import (line 11): import { z } from "npm:zod@3.22.0";
  -> bump to npm:zod@3.25.76 (re-verify with `npm view zod version` at execute time; must be >=3.25.0).
Existing *_SCHEMA_VERSION exports already present (lines 349-364) — keep, do not renumber.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize frontmatter on the 7 templates + author CHANGELOG.md</name>
  <read_first>
    - docs/conhecimento/prompts/templates/01-cv-summary.md (canonical frontmatter — replicate its key set on 02-07)
    - docs/conhecimento/prompts/templates/02-cv-job-match.md (and 03..07 — read each frontmatter to find missing/divergent keys)
    - docs/prds/m2-funil-rh/PRD-ai-prompt-library-m2.md §6.1 RF-PL-01/02/03 + §3.1 (templates refinement, no rename)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md §user_constraints (Area 1 — templates already on disk; refine only)
  </read_first>
  <files>docs/conhecimento/prompts/templates/01-cv-summary.md, docs/conhecimento/prompts/templates/02-cv-job-match.md, docs/conhecimento/prompts/templates/03-comparative-ranking.md, docs/conhecimento/prompts/templates/04-interview-guide.md, docs/conhecimento/prompts/templates/05-transcript-analysis.md, docs/conhecimento/prompts/templates/06-culture-fit-essay.md, docs/conhecimento/prompts/templates/07-work-sample-sjt.md, docs/conhecimento/prompts/CHANGELOG.md</files>
  <action>For each of the 7 templates, ensure the frontmatter carries the full canonical key set (the 01-cv-summary key list in the interfaces block). Set semver "1.0.0" (quoted), content_hash: tbd (CI overwrites at merge — RF-PL-02), schema_version_required matching the matching *_SCHEMA_VERSION export, model_id per the PRD decision-#5 assignment (cv_summary=claude-haiku-4-5, all others=claude-sonnet-4-6), fallback_model_id=gpt-4o-mini, temperature=0, max_tokens as already specified per template (do not lower), change_summary describing v1.0.0, changed_by, created_at. Do NOT rename any file. Do NOT touch the SYSTEM PROMPT / USER / few-shot body except to remove any forbidden product-copy term if present (LGPD-04 — replace with "avaliacao comportamental/cognitiva"). Then author CHANGELOG.md: one chronological line per template at v1.0.0 (SemVer + call_type + change_summary + author + date), plus a short header documenting MAJOR/MINOR/PATCH semantics (RF-PL-03).</action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && for f in 01-cv-summary 02-cv-job-match 03-comparative-ranking 04-interview-guide 05-transcript-analysis 06-culture-fit-essay 07-work-sample-sjt; do echo "== $f =="; grep -E "^(semver|content_hash|schema_version_required|model_id|fallback_model_id|temperature|max_tokens):" docs/conhecimento/prompts/templates/$f.md | wc -l; done; echo "forbidden scan:"; grep -riE "psic[oó]logo|teste\s+psicol|psicot[eé]cnico" docs/conhecimento/prompts/templates/*.md && echo "FOUND" || echo "CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: each of the 7 templates has all 7 key lines (semver, content_hash, schema_version_required, model_id, fallback_model_id, temperature, max_tokens) — the per-file count above is 7.
    - cv_summary model_id is claude-haiku-4-5; the other 6 are claude-sonnet-4-6; all fallback_model_id are gpt-4o-mini.
    - The forbidden-term scan over templates/*.md prints CLEAN.
    - CHANGELOG.md exists with >=7 version lines + a SemVer-semantics header.
  </acceptance_criteria>
  <done>All 7 templates carry the standardized Zod-validatable frontmatter, model assignment matches PRD decision #5, no forbidden copy, and CHANGELOG.md documents v1.0.0 for each.</done>
</task>

<task type="auto">
  <name>Task 2: Bump zod in 00-shared-zod-schemas.ts to >=3.25 (helper peer-dep)</name>
  <read_first>
    - docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts (line 11 import; lines 349-364 *_SCHEMA_VERSION exports)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md §Common Pitfalls (Pitfall 1 — SDK staleness) + §Standard Stack (zod >=3.25 REQUIRED)
  </read_first>
  <files>docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts</files>
  <action>Change the zod import specifier from npm:zod@3.22.0 to npm:zod@3.25.76 (re-verify the latest stable >=3.25 with `npm view zod version` at execute time per RESEARCH A1; if 3.25.76 is unavailable pin the current >=3.25 patch). Update the header doc-comment "Padrao: Zod 3.22+" to the new pinned version and note the bump reason (messages.parse/zodOutputFormat peer-dep ^3.25.0 || ^4.0.0). Do NOT change any schema shape, any .min()/.max()/.int() constraint, or any *_SCHEMA_VERSION value (the SDK auto-transforms unsupported JSON-Schema keywords into descriptions and re-validates client-side — RESEARCH Pattern 1; so existing constraints are safe). This file is the runtime contract Plan 05's ai-client imports.</action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && grep -n "npm:zod@" docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts && grep -c "SCHEMA_VERSION" docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: grep "npm:zod@" shows a version >=3.25 (no 3.22.0 remaining).
    - All 7 *_SCHEMA_VERSION exports + the SCHEMA_VERSIONS map are unchanged (grep count unchanged from baseline).
    - No schema shape edits (git diff touches only the import line + the header doc-comment).
  </acceptance_criteria>
  <done>00-shared-zod-schemas.ts imports zod >=3.25 with the bump reason documented, and all schema shapes + version exports are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git templates -> CI sync | frontmatter is the authoring source the CI script Zod-validates before DB UPSERT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Compliance (LGPD/RNF-12) | template product copy | mitigate | Task 1 forbidden-term scan over templates/*.md must print CLEAN; LGPD-04 guard (Plan 01) backstops |
| T-09-04 | Tampering | silent prompt edit without SemVer bump | mitigate | content_hash: tbd + RF-PL-04 immutability trigger (Plan 03) — any post-deploy edit needs a new SemVer row |
| T-09-05 | Tampering | stale zod breaking structured-output validation | mitigate | Task 2 bumps to >=3.25 (helper peer-dep) so client-side Zod re-validation works |
</threat_model>

<verification>
- All 7 templates pass the 7-key frontmatter count and the CLEAN forbidden scan.
- 00-shared-zod-schemas.ts imports zod >=3.25 with unchanged schema shapes.
- CHANGELOG.md present with v1.0.0 lines.
</verification>

<success_criteria>
- IA-01 frontmatter contract satisfied across the 7 templates (sync-script-ready).
- zod bumped to the helper-required version with zero schema regressions.
- No forbidden product copy in templates.
</success_criteria>

<output>
Create `.planning/phases/09-ai-prompt-library-cost-infra/09-02-SUMMARY.md` when done.
</output>
