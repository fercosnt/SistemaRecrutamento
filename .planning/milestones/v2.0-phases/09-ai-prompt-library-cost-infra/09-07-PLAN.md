---
phase: 09-ai-prompt-library-cost-infra
plan: 07
type: execute
wave: 4
depends_on: ["09-03", "09-05"]
files_modified:
  - database.types.ts
  - supabase/functions/cost-alerter/index.ts
autonomous: false
requirements: [IA-02, IA-04]
user_setup:
  - service: supabase
    why: "PROD migration apply (PL/pgSQL 42601) + Vault secrets for cost-alerter pg_net auth + EF deploy"
    env_vars:
      - name: project_url (Vault secret)
        source: "vault.create_secret('https://isljnozzlvckrgjjbjwp.supabase.co','project_url')"
      - name: edge_invoke_key (Vault secret)
        source: "vault.create_secret('<service_role_jwt>','edge_invoke_key')"
    dashboard_config:
      - task: "Apply the 4 migrations via Supabase MCP execute_sql, reconcile supabase_migrations version rows, confirm db push --linked 'up to date'"
        location: "Supabase MCP / SQL"
      - task: "Set Vault secrets project_url + edge_invoke_key"
        location: "Supabase SQL (vault.create_secret)"
      - task: "Deploy cost-alerter EF with --no-verify-jwt + set RESEND_API_KEY (optional, graceful-degradation)"
        location: "supabase functions deploy cost-alerter"
  - service: resend
    why: "cost-alerter email to DPO/RH lead (graceful degradation — row INSERT always happens; email best-effort)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend dashboard -> API Keys (human-gated; EF logs-and-skips if absent)"
must_haves:
  truths:
    - "The 4 migrations are applied LIVE in PROD (6 tables + 3 enums + RPCs + triggers + cron) and database.types.ts is regenerated at repo ROOT (IA-02 — build/types are a false-positive until this applies)"
    - "All 7 SQL smokes from 09-SQL-SMOKE-RUNBOOK.md PASS against live PROD (tables/enums/EXCLUDE/immutability/RPC-auth/pg_net/cron)"
    - "cost-alerter EF validates the Vault Bearer, ALWAYS INSERTs the recruiter_alerts row, and sends email best-effort (logs-and-skips if RESEND_API_KEY absent — orchestrator-decision #3) — IA-04"
  artifacts:
    - path: "supabase/functions/cost-alerter/index.ts"
      provides: "pg_net-triggered alerter: Bearer check + recruiter_alerts INSERT + best-effort Resend email"
      contains: "recruiter_alerts"
    - path: "database.types.ts"
      provides: "regenerated types including the 6 new AI tables"
      contains: "ai_call_logs"
  key_links:
    - from: "notify_cost_anomaly() trigger"
      to: "cost-alerter EF"
      via: "net.http_post Bearer"
      pattern: "recruiter_alerts"
    - from: "cost-alerter EF"
      to: "recruiter_alerts"
      via: "supabaseAdmin INSERT (always) + Resend (best-effort)"
      pattern: "from\\('recruiter_alerts'\\)"
---

<objective>
Apply the Phase-9 migrations to live PROD (the [BLOCKING] step that turns the authored schema into real infra), regenerate database.types.ts, run the 7 SQL smokes, and ship the cost-alerter EF that the cost-anomaly trigger calls.

Purpose: IA-02 (the logging tables must exist live — build/types pass WITHOUT the apply, a false-positive; the push is mandatory) and IA-04 (cost-alerter dispatches the alert + writes recruiter_alerts). PL/pgSQL is 42601-heavy -> apply via Supabase MCP (apply_migration/execute_sql) then reconcile, per CLAUDE.md / D-22.
Output: live schema + regenerated types + deployed cost-alerter EF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-SQL-SMOKE-RUNBOOK.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-PATTERNS.md

<interfaces>
<!-- apply path + EF analog -->
Migrations to apply (Plan 03): 20260609000001_schema, 20260609000002_rpcs, 20260609000003_cron, 20260609000004_seed.
Apply path (CLAUDE.md / D-22 / MEMORY Phase 6/7/8): Supabase MCP execute_sql / apply_migration bypasses the
  transaction-pooler 42601; then reconcile by writing version rows to supabase_migrations + `supabase db push --linked`
  must report "Remote database is up to date".
Types: database.types.ts lives at repo ROOT; regenerate via `npm run db:types` (never hand-edit).

cost-alerter EF analog: supabase/functions/submit-candidatura/index.ts (two-client D-23, jsonResponse contract,
  fire-and-forget downstream call L303-323). cost-alerter is server-internal (pg_net Bearer) -> deploy --no-verify-jwt
  but validate the shared Vault Bearer in-handler (RESEARCH Security "Cost-alerter EF open to public POST").
Resend graceful degradation (orchestrator-decision #3): the recruiter_alerts INSERT ALWAYS happens (autonomously
  testable); email send is best-effort — if RESEND_API_KEY is absent, log-and-skip (mirror submit-candidatura
  fire-and-forget). Do NOT crash the EF when the key is missing.
Dedup (Pitfall 7): before INSERT/email, check recruiter_alerts for a same-(threshold_violated, vaga_id, date) row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cost-alerter EF (Bearer check + always-INSERT recruiter_alerts + best-effort Resend)</name>
  <read_first>
    - supabase/functions/submit-candidatura/index.ts (two-client D-23, jsonResponse, fire-and-forget L303-323)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md §Architecture Pattern 3 (pg_net body shape) + §Security Domain (cost-alerter auth)
    - supabase/migrations/20260609000002_prompt_library_rpcs.sql (the notify_cost_anomaly body that POSTs here)
    - docs/prds/m2-funil-rh/PRD-ai-prompt-library-m2.md §6.6 RF-PL-30/31/32 (thresholds + recruiter_alerts)
  </read_first>
  <files>supabase/functions/cost-alerter/index.ts</files>
  <action>Implement cost-alerter/index.ts (Deno EF): accept the POST body from notify_cost_anomaly (alert_type/threshold_violated, vaga_id, date, value, threshold). (1) Validate the Authorization Bearer against the expected shared secret (the Vault edge_invoke_key / service_role) — reject 401 if absent/mismatch (server-internal endpoint, --no-verify-jwt at deploy means the handler MUST auth itself). (2) Dedup: query recruiter_alerts for a same-(threshold_violated, vaga_id, date) row; if present, return 200 idempotent (Pitfall 7). (3) ALWAYS INSERT a recruiter_alerts row via supabaseAdmin (service_role) with call_type/threshold_violated/vaga_id/candidato_id?/value/threshold/channel/created_at — this is the autonomously testable side effect. (4) Best-effort email via Resend: if Deno.env.get('RESEND_API_KEY') is set, fire-and-forget a POST to the Resend API to DPO/RH lead with a .catch that logs-and-skips; if the key is absent, console.warn and skip — NEVER crash (orchestrator-decision #3, mirror submit-candidatura fire-and-forget). Use the jsonResponse { ok, error_code?, message? } contract. Log code+summary only (no PII). pt-BR copy, no forbidden product terms. Do NOT deploy in this task (deploy is the blocking Task 2).</action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && f=supabase/functions/cost-alerter/index.ts; grep -c "recruiter_alerts\|RESEND_API_KEY\|Authorization\|Bearer" $f; echo "always-insert before email (insert line < resend line):"; awk "/from\\(.recruiter_alerts.\\)|insert/{ins=NR} /RESEND_API_KEY/{r=NR} END{ if(ins) print \"INSERT@\"ins; if(r) print \"RESEND@\"r }" $f</automated>
  </verify>
  <acceptance_criteria>
    - source: validates Authorization Bearer; rejects 401 on mismatch.
    - source: INSERTs recruiter_alerts unconditionally (the row write is not inside the RESEND_API_KEY branch).
    - source: RESEND_API_KEY absence is handled by log-and-skip (no throw), email is fire-and-forget .catch.
    - Dedup check against recruiter_alerts present (Pitfall 7).
    - No forbidden product copy; no PII in logs.
  </acceptance_criteria>
  <done>cost-alerter EF authenticates the Vault Bearer, always writes recruiter_alerts, and sends email best-effort with graceful degradation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Apply migrations to PROD + Vault secrets + EF deploy + db:types + 7 smokes</name>
  <what-built>The 4 authored migrations (Plan 03), the cost-alerter EF (Task 1), and the SQL-smoke runbook (Plan 01). Build + type-check pass WITHOUT this apply — a false-positive; the live apply is mandatory for IA-02/IA-04.</what-built>
  <how-to-verify>
    1. Apply the 4 migrations to PROD via Supabase MCP execute_sql / apply_migration (bypasses 42601 — CLAUDE.md / D-22): 20260609000001_schema, 20260609000002_rpcs, 20260609000003_cron, 20260609000004_seed (in order). Reconcile by writing version rows to supabase_migrations; confirm `supabase db push --linked` reports "Remote database is up to date".
    2. Set Vault secrets: vault.create_secret('https://isljnozzlvckrgjjbjwp.supabase.co','project_url') and vault.create_secret('<service_role_jwt>','edge_invoke_key').
    3. Regenerate types: `npm run db:types` (writes database.types.ts at repo ROOT). Confirm it now contains ai_call_logs / prompt_versions / recruiter_alerts.
    4. Deploy the EF: `supabase functions deploy cost-alerter --no-verify-jwt`. Optionally set RESEND_API_KEY (`supabase secrets set RESEND_API_KEY=...`) — if skipped, the EF logs-and-skips email (row INSERT still happens).
    5. Run ALL 7 smokes from 09-SQL-SMOKE-RUNBOOK.md via MCP execute_sql against a throwaway fixture (ROLLBACK-free cleanup, Phase 8 precedent): SMOKE-1 tables+RLS, SMOKE-2 enums, SMOKE-3 EXCLUDE constraint, SMOKE-4 immutability trigger, SMOKE-5 RPC auth (P0001 + 42501), SMOKE-6 cost-anomaly pg_net queued, SMOKE-7 cron jobs registered. Each must PASS.
    6. Watch for the two recurring bug classes (MEMORY Phase 8): enum-literal mismatches (22P02) and double-write/trigger ownership. Fix in the migration source + re-apply if a smoke fails.
  </how-to-verify>
  <resume-signal>Type "approved" with the 7 smoke results, the db:types grep confirmation, and the EF deploy version — or describe failures.</resume-signal>
  <verify>
    <human-check>All 4 migrations applied + reconciled (db push "up to date"); database.types.ts contains the 6 new tables; cost-alerter deployed; 7/7 SQL smokes PASS.</human-check>
  </verify>
  <acceptance_criteria>
    - `supabase db push --linked` reports "Remote database is up to date" after reconcile.
    - database.types.ts (repo ROOT) contains ai_call_logs, prompt_versions, recruiter_alerts (grep confirms).
    - cost-alerter EF deployed (active version reported).
    - 7/7 SQL smokes PASS (recorded in the resume signal / SUMMARY).
  </acceptance_criteria>
  <done>The schema is live in PROD, types regenerated, cost-alerter deployed, and all 7 SQL smokes pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_net trigger -> cost-alerter EF | internet-reachable POST; handler must self-authenticate |
| cost-alerter -> Resend | external email side-effect; best-effort, never blocks the row write |
| MCP execute_sql -> PROD | privileged live DDL; human-authorized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-22 | Spoofing | public POST to cost-alerter (--no-verify-jwt) | mitigate | handler validates Authorization Bearer against Vault edge_invoke_key; 401 on mismatch |
| T-09-23 | Denial of Service | duplicate alerts / email spam | mitigate | dedup by (threshold_violated, vaga_id, date) before INSERT/email (Pitfall 7) |
| T-09-24 | Information Disclosure | PII in alert email/log | mitigate | alert body carries only ids + numeric thresholds; log code+summary only |
| T-09-25 | Elevation of Privilege | service_role exposure in EF | mitigate | service_role read from EF env only; never client-side; --no-verify-jwt + Bearer self-auth |
| T-09-SC | Tampering | live PROD DDL via MCP | mitigate | human-authorized blocking checkpoint; classifier requires explicit allow-rule for mcp__supabase__execute_sql (MEMORY) |
</threat_model>

<verification>
- cost-alerter EF source: Bearer auth + unconditional recruiter_alerts INSERT + graceful Resend degradation.
- Live: 4 migrations applied + reconciled, types regenerated, EF deployed, 7/7 smokes PASS (human checkpoint).
</verification>

<success_criteria>
- IA-02 logging tables + IA-04 cost-alerter live in PROD with passing smokes.
- database.types.ts regenerated at ROOT; no hand-edit.
- cost-alerter resilient to a missing RESEND_API_KEY.
</success_criteria>

<output>
Create `.planning/phases/09-ai-prompt-library-cost-infra/09-07-SUMMARY.md` when done.
</output>
