---
phase: 09-ai-prompt-library-cost-infra
plan: 08
type: execute
wave: 5
depends_on: ["09-07"]
files_modified:
  - src/features/admin/ai-logs/services/aiLogsService.ts
  - src/features/admin/ai-logs/hooks/useAiLogs.ts
  - src/features/admin/ai-logs/components/AiLogsPage.tsx
  - src/features/admin/prompt-versions/services/promptVersionsService.ts
  - src/features/admin/prompt-versions/hooks/usePromptVersions.ts
  - src/features/admin/prompt-versions/components/PromptVersionsPage.tsx
  - src/features/admin/ai-costs/services/aiCostsService.ts
  - src/features/admin/ai-costs/hooks/useAiCosts.ts
  - src/features/admin/ai-costs/components/AiCostsPage.tsx
  - src/router/routes.tsx
autonomous: true
requirements: [IA-01, IA-02, IA-03]
must_haves:
  truths:
    - "Three read-only admin pages exist gated by RoleGuard role='administrador', rendering over the (empty-at-ship) schema with the UI-SPEC empty states (IA-01/02/03 admin review surface)"
    - "ai-logs reads ai_call_logs with an EXPLICIT column allowlist (never select('*')) — no PII column reaches the browser (Phase 8 security-gate precedent)"
    - "prompt-versions invokes the promote/rollback SECURITY DEFINER RPCs and surfaces the server RAISE message verbatim (UI-SPEC) — IA-01"
    - "No forbidden product copy on any admin page (LGPD-04 guard stays GREEN over src/)"
  artifacts:
    - path: "src/features/admin/ai-logs/components/AiLogsPage.tsx"
      provides: "filterable ai_call_logs table + detail modal + empty state"
      min_lines: 40
    - path: "src/features/admin/prompt-versions/components/PromptVersionsPage.tsx"
      provides: "version list + diff + promote/rollback RPC calls"
      min_lines: 40
    - path: "src/features/admin/ai-costs/components/AiCostsPage.tsx"
      provides: "recharts cost dashboard + paginated table + empty state"
      min_lines: 30
    - path: "src/router/routes.tsx"
      provides: "3 admin routes gated administrador"
      contains: "/admin/ai-logs"
  key_links:
    - from: "AiLogsPage"
      to: "ai_call_logs"
      via: "aiLogsService explicit-allowlist select"
      pattern: "ai_call_logs"
    - from: "PromptVersionsPage"
      to: "promote_canary_to_active / rollback_to_version RPC"
      via: "supabase.rpc + verbatim RAISE message"
      pattern: "\\.rpc\\("
---

<objective>
Build the 3 read-only internal admin pages (ai-logs, prompt-versions, ai-costs) per the approved UI-SPEC — reusing RHLayout + glass UI, gated by role administrador, working over the empty schema with the specified empty states. These are the LGPD-minimum review/compliance surface for the AI infra.

Purpose: IA-01 admin review/gold-standard UI (prompt-versions promote/rollback + diff), IA-02 ai-logs audit table, IA-03 ai-costs dashboard. Real AI data arrives Phase 10+, so empty states are the default state at ship.
Output: 3 feature folders (service/hook/page each) + 3 routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-UI-SPEC.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-PATTERNS.md

<interfaces>
<!-- exact analogs + the anti-select('*') contract -->
Layout: src/components/pages/DashboardRHPage.tsx (wraps RHLayout); RHLayout = BackgroundImage darkBlue + RHSidebar + RHTopBar.
Query keys analog (exact): src/features/vagas/hooks/useVagas.ts (hierarchical vagasKeys) -> aiLogsKeys/promptVersionsKeys/aiCostsKeys.
Service analog (exact): src/features/vagas/services/vagasService.ts (custom Error subclass + explicit .select() allowlist).
Route analog: src/router/routes.tsx (RoleGuard rows) — add 3 rows role="administrador" (NOT ['rh','administrador']).
Chart wrapper: src/components/ui/chart.tsx (recharts; --chart-1..5 tokens). Do NOT add @tanstack/react-table — use vendored table.tsx.

ai_call_logs allowlist (UI-SPEC L147-149 — NEVER select('*'); no curriculo_nome_original / raw PII):
  id, created_at, candidato_id, vaga_id, call_type, provider, model_id, prompt_version, success,
  parsed_score, cost_usd, latency_ms  (+ parsed_reasoning, raw_response only inside the row-detail modal)

RPC call sites: supabase.rpc('promote_to_canary'|'promote_canary_to_active'|'rollback_to_version', {...}) — surface the
  server RAISE message verbatim in a Sonner toast (UI-SPEC §Copywriting); map 42501 -> "Acesso restrito a administradores".
Empty states + copy: 09-UI-SPEC.md §Copywriting Contract (verbatim pt-BR strings).
Types: database.types.ts (repo ROOT, regenerated Plan 07) supplies the new table row types.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ai-logs page (service + hook + page + route) — explicit allowlist</name>
  <read_first>
    - src/features/vagas/services/vagasService.ts (error class + explicit .select allowlist analog)
    - src/features/vagas/hooks/useVagas.ts (hierarchical query keys analog)
    - src/components/pages/DashboardRHPage.tsx (RHLayout shell)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-UI-SPEC.md §/admin/ai-logs + §Copywriting (empty/error states)
    - src/router/routes.tsx (RoleGuard rows)
  </read_first>
  <files>src/features/admin/ai-logs/services/aiLogsService.ts, src/features/admin/ai-logs/hooks/useAiLogs.ts, src/features/admin/ai-logs/components/AiLogsPage.tsx, src/router/routes.tsx</files>
  <action>aiLogsService.ts: AiLogsServiceError subclass + listAiLogs(filters, pagination) reading ai_call_logs with the EXPLICIT column allowlist from the interfaces block (NEVER select('*'); parsed_reasoning + raw_response fetched only for the detail modal), server pagination 50. useAiLogs.ts: aiLogsKeys hierarchical + useQuery(staleTime 5min, retry 2). AiLogsPage.tsx: wrap RHLayout; GlassCard filter bar (4 Selects: Candidato, Vaga, Tipo de chamada, Status + Limpar filtros) over a GlassCard table.tsx with the UI-SPEC columns; row click -> Dialog showing parsed_reasoning + raw_response (mono JSON in scroll-area); Skeleton loading; the verbatim empty-state heading/body and error-state copy from UI-SPEC; status badge (success/warning/destructive). routes.tsx: add { path:'/admin/ai-logs', element: <RoleGuard role="administrador"><AiLogsPage/></RoleGuard> }. pt-BR copy, no forbidden terms.</action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && echo "no select star:"; grep -rn "select('\*')\|select(\"\*\")" src/features/admin/ai-logs/ && echo "BAD" || echo "OK"; echo "route:"; grep -c "/admin/ai-logs" src/router/routes.tsx; echo "lint growth:"; COUNT=$(npm run -s lint 2>&1 | grep -c "error TS"); echo "tsc=$COUNT (baseline ~293)"</automated>
  </verify>
  <acceptance_criteria>
    - source: no select('*') anywhere in src/features/admin/ai-logs/ (scan prints OK).
    - aiLogsService reads the explicit allowlist; parsed_reasoning/raw_response only in the modal path.
    - route /admin/ai-logs gated RoleGuard role="administrador".
    - tsc count does not exceed the frozen baseline (~293).
  </acceptance_criteria>
  <done>The ai-logs page renders the allowlisted ai_call_logs table with filters, detail modal, and the UI-SPEC empty state, gated administrador.</done>
</task>

<task type="auto">
  <name>Task 2: prompt-versions page (service + hook + page + route) — promote/rollback RPCs</name>
  <read_first>
    - src/features/vagas/services/vagasService.ts (error class) + the publish_vaga RPC call site pattern (PATTERNS.md §RPC call site)
    - src/features/vagas/hooks/useVagas.ts (query keys + mutation invalidation)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-UI-SPEC.md §/admin/prompt-versions + §Destructive Actions + §Copywriting
    - supabase/migrations/20260609000002_prompt_library_rpcs.sql (the RPC signatures + RAISE messages this UI surfaces)
  </read_first>
  <files>src/features/admin/prompt-versions/services/promptVersionsService.ts, src/features/admin/prompt-versions/hooks/usePromptVersions.ts, src/features/admin/prompt-versions/components/PromptVersionsPage.tsx, src/router/routes.tsx</files>
  <action>promptVersionsService.ts: list prompt_versions (explicit allowlist: id, call_type, semver, content_hash, is_active, is_canary, canary_pct, deployed_at, deprecated_at, schema_version_required + system_template/user_template for the diff view) grouped by call_type; promoteToCanary/promoteToActive/rollback wrappers calling supabase.rpc(...) and re-throwing with the server RAISE message preserved (map PG 42501 -> "Acesso restrito a administradores", domain P0001 -> message verbatim — submit-candidatura PG-code-map precedent). usePromptVersions.ts: promptVersionsKeys + useQuery + useMutation that invalidates on success. PromptVersionsPage.tsx: RHLayout; versions grouped by call_type (tabs.tsx or Accordion); each row shows semver, content_hash (8-char mono + tooltip full), status badges (is_active accent, is_canary warning, canary_pct), deployed_at, deprecated_at; "Comparar versoes" enabled at exactly 2 selected -> side-by-side mono diff of system_template + user_template (simple two-column, no diff lib); action buttons gated by row state with the UI-SPEC AlertDialog confirms (Promover para canario / Promover para ativa / Reverter); Sonner toast surfaces RPC RAISE verbatim on failure and the success message on ok; verbatim empty state. routes.tsx: add /admin/prompt-versions gated administrador. pt-BR, no forbidden terms.</action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && echo "rpc calls:"; grep -rc "\.rpc(" src/features/admin/prompt-versions/; echo "no select star:"; grep -rn "select('\*')" src/features/admin/prompt-versions/ && echo "BAD" || echo "OK"; echo "route:"; grep -c "/admin/prompt-versions" src/router/routes.tsx</automated>
  </verify>
  <acceptance_criteria>
    - source: promote/rollback call supabase.rpc(...) for the 3 RPC names; RAISE message surfaced verbatim.
    - no select('*'); explicit allowlist for the version list.
    - route /admin/prompt-versions gated administrador.
    - AlertDialog confirms present for the 3 state-changing actions (UI-SPEC §Destructive Actions).
  </acceptance_criteria>
  <done>The prompt-versions page lists versions by call_type with diff + promote/rollback RPC calls surfacing server messages verbatim, gated administrador.</done>
</task>

<task type="auto">
  <name>Task 3: ai-costs page (service + hook + page + route) + LGPD-04 guard GREEN over src/</name>
  <read_first>
    - src/components/ui/chart.tsx (recharts wrapper + --chart tokens)
    - src/features/vagas/services/vagasService.ts + useVagas.ts (analogs)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-UI-SPEC.md §/admin/ai-costs + §Color (chart palette) + §Copywriting
    - src/__tests__/guards/forbidden-strings.grep.test.ts (the guard to keep GREEN)
  </read_first>
  <files>src/features/admin/ai-costs/services/aiCostsService.ts, src/features/admin/ai-costs/hooks/useAiCosts.ts, src/features/admin/ai-costs/components/AiCostsPage.tsx, src/router/routes.tsx</files>
  <action>aiCostsService.ts: read ai_cost_daily (explicit allowlist: date, vaga_id, call_type, provider, call_count, total_input_tokens, total_output_tokens, total_cost_usd, error_count) with a month filter. useAiCosts.ts: aiCostsKeys + useQuery. AiCostsPage.tsx: RHLayout; month Select; three recharts in GlassCards via chart.tsx (line = daily cost 30d, bar = top 10 vagas, pie = by call_type) using --chart-1..5; annotate the R$0,50/candidato (RNF-10) + R$1,00 p95 guardrail as reference lines where supported; paginated ai_cost_daily table below; Skeleton loading; verbatim empty state ("Sem dados de custo no periodo" + the pg_cron note). routes.tsx: add /admin/ai-costs gated administrador. Then run the LGPD-04 grep guard over the now-complete src/ (all 3 pages) — fix any forbidden term to "avaliacao comportamental/cognitiva". pt-BR copy.</action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && echo "route:"; grep -c "/admin/ai-costs" src/router/routes.tsx; echo "LGPD-04 guard:"; npx vitest run src/__tests__/guards/forbidden-strings.grep.test.ts 2>&1 | tail -8; echo "build:"; npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - route /admin/ai-costs gated administrador; 3 recharts via the vendored chart.tsx (no ad-hoc hex, no @tanstack/react-table).
    - test command: forbidden-strings.grep.test.ts GREEN over the full src/ (all 3 admin pages clean).
    - npm run build exits 0.
    - tsc count does not exceed the frozen baseline.
  </acceptance_criteria>
  <done>The ai-costs page renders the cost dashboard + table with empty state, the LGPD-04 guard is GREEN over src/, and the build is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser -> ai_call_logs | RLS administrador-read + explicit column allowlist; PII columns never projected |
| admin browser -> promote/rollback RPC | RPC re-validates role in-body; UI cannot bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-26 | Information Disclosure | select('*') leaking PII columns to admin client | mitigate | explicit column allowlist on all 3 services (Phase 8 precedent reference_select_star_leaks_pii); raw_response/parsed_reasoning only in detail modal |
| T-09-27 | Elevation of Privilege | non-admin reaching admin pages | mitigate | RoleGuard role="administrador" on all 3 routes + RLS administrador-read on the tables |
| T-09-28 | Elevation of Privilege | unauthorized promote/rollback via UI | mitigate | RPCs are SECURITY DEFINER with in-body admin check; UI surfaces 42501 verbatim and cannot bypass |
| T-09-29 | Compliance (LGPD/RNF-12) | forbidden copy in admin pages | mitigate | Task 3 keeps the LGPD-04 guard GREEN over src/ |
</threat_model>

<verification>
- 3 routes gated administrador; no select('*') in any admin service; LGPD-04 guard GREEN over src/; build exit 0; tsc within baseline.
- Pages render empty states over the (empty-at-ship) schema per UI-SPEC.
</verification>

<success_criteria>
- IA-01/02/03 admin review surface complete (logs + versions + costs) with promote/rollback wired.
- No PII leakage (allowlist), no privilege bypass (RoleGuard + RLS + DEFINER), product language clean.
</success_criteria>

<output>
Create `.planning/phases/09-ai-prompt-library-cost-infra/09-08-SUMMARY.md` when done.
</output>
