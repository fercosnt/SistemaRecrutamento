---
phase: 09-ai-prompt-library-cost-infra
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - supabase/functions/_shared/pii-masker.ts
  - supabase/functions/_shared/injection-detector.ts
  - supabase/functions/_shared/circuit-breaker.ts
  - supabase/functions/_shared/ai-cost.ts
autonomous: true
requirements: [IA-02, IA-03, IA-04, LGPD-04]
must_haves:
  truths:
    - "pii-masker strips PT-BR PII (CPF/CNPJ/email/tel/data-nasc/endereco/RG) before any log write, flipping pii-masker.test.ts GREEN (IA-02 / Pitfall 6)"
    - "circuit-breaker opens at 5 failures / 60s and half-opens after reset, flipping circuit-breaker.test.ts GREEN (IA-04 / RF-PL-15)"
    - "ai-cost.calculateCost uses the verified COST_PER_TOKEN table billing cached tokens at cached_read, flipping ai-cost.test.ts GREEN (IA-03)"
    - "injection-detector flags the 8 adversarial patterns without an API call, flipping injection-detector.test.ts GREEN (RF-PL-18)"
    - "No forbidden product copy in these helpers (LGPD-04 guard stays GREEN over supabase/functions/)"
  artifacts:
    - path: "supabase/functions/_shared/pii-masker.ts"
      provides: "maskPII() PT-BR regex"
      contains: "maskPII"
    - path: "supabase/functions/_shared/circuit-breaker.ts"
      provides: "in-memory breaker 5/60s"
      contains: "canRequest"
    - path: "supabase/functions/_shared/ai-cost.ts"
      provides: "COST_PER_TOKEN + calculateCost"
      contains: "calculateCost"
    - path: "supabase/functions/_shared/injection-detector.ts"
      provides: "8-pattern detector"
      contains: "detect"
  key_links:
    - from: "ai-client.ts (Plan 05)"
      to: "pii-masker / circuit-breaker / ai-cost / injection-detector"
      via: "imports from ../<helper>.ts"
      pattern: "from './pii-masker"
---

<objective>
Implement the 4 stateless _shared helpers (PII masking, prompt-injection detection, circuit breaker, cost calculation) extracted from the 08-edge-function-reference.ts logic — flipping their Wave-0 Deno tests GREEN. No DB, no SDK, no network: these are pure utilities the ai-client (Plan 05) composes.

Purpose: IA-02 (PII masking before log), IA-03 (cost calc), IA-04 (circuit breaker fallback gate), RF-PL-18 (injection detection). Splitting the pure utilities from the SDK-heavy ai-client keeps each plan within context budget and lets this run in parallel with the migration authoring (Plan 03).
Output: 4 helper modules with GREEN Deno tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md
@.planning/phases/09-ai-prompt-library-cost-infra/09-PATTERNS.md

<interfaces>
<!-- Source logic to extract; the RED tests these flip GREEN -->
Reference logic: docs/conhecimento/prompts/templates/08-edge-function-reference.ts
  - CircuitBreaker class L47-77 (THRESHOLD, RESET_MS, canRequest/recordFailure/recordSuccess)
  - injection patterns L82-104 (the 8-pattern array + detect fn)
  - PII_RULES L106-139 + maskPII L115 (PT-BR regex set)
  - COST_PER_TOKEN L141-147 + calculateCost L148 (use the verified RESEARCH values, NOT necessarily the reference's if they differ)

Verified COST_PER_TOKEN (RESEARCH §Code Examples — authoritative):
  claude-sonnet-4-6: input 3e-6, cached_read 0.3e-6, output 15e-6
  claude-haiku-4-5:  input 1e-6, cached_read 0.1e-6, output 5e-6
  gpt-4o-mini:       input 0.15e-6, cached_read 0.075e-6, output 0.6e-6

RED tests (Plan 01): supabase/functions/_shared/__tests__/{pii-masker,injection-detector,circuit-breaker,ai-cost}.test.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pii-masker.ts + injection-detector.ts</name>
  <behavior>
    - maskPII("CPF 123.456.789-00") -> "[CPF]" placeholder; returns { masked, piiFound:['CPF'] }
    - maskPII covers CPF, CNPJ, email, telefone, data-nasc, endereco, RG (PT-BR)
    - maskPII does NOT mask skills/company names/job titles (AUDITORIA §5.3)
    - injection-detector flags the 8 known adversarial payloads, returns { detected:true, pattern }
    - injection-detector returns { detected:false } for benign CV text
  </behavior>
  <read_first>
    - docs/conhecimento/prompts/templates/08-edge-function-reference.ts (L82-139 — patterns + PII_RULES + maskPII)
    - supabase/functions/_shared/__tests__/pii-masker.test.ts + injection-detector.test.ts (the RED contracts to satisfy)
    - docs/conhecimento/prompts/AUDITORIA-LGPD-LOGGING-VERSIONING.md §5 (PII rules + §5.3 what NOT to mask)
  </read_first>
  <files>supabase/functions/_shared/pii-masker.ts, supabase/functions/_shared/injection-detector.ts</files>
  <action>Extract maskPII() into pii-masker.ts (Deno module, named export) using the reference PT-BR PII_RULES (CPF, CNPJ, email, telefone, data-nasc, endereco, RG); return { masked, piiFound } where piiFound is the list of placeholder names hit. Extract the 8-pattern detector into injection-detector.ts as detectPromptInjection(text) returning { detected, pattern? }. No DB, no SDK imports. Keep all human-facing strings in pt-BR and free of LGPD-04 forbidden terms. These are the modules the RED tests import from '../pii-masker.ts' / '../injection-detector.ts'.</action>
  <verify>
    <automated>command -v deno >/dev/null 2>&1 && deno test --no-check supabase/functions/_shared/__tests__/pii-masker.test.ts supabase/functions/_shared/__tests__/injection-detector.test.ts 2>&1 | tail -15 || echo "deno not on PATH — confirm files + named exports"; grep -l "export" supabase/functions/_shared/pii-masker.ts supabase/functions/_shared/injection-detector.ts</automated>
  </verify>
  <acceptance_criteria>
    - test command: pii-masker.test.ts + injection-detector.test.ts pass (GREEN) under deno test.
    - maskPII returns both masked text and a piiFound list; covers all 7 PT-BR PII categories.
    - detectPromptInjection flags >=8 patterns and returns the matched pattern.
    - source: no DB/SDK import in either file; no forbidden product copy.
  </acceptance_criteria>
  <done>pii-masker.ts and injection-detector.ts exist with named exports, flip their RED tests GREEN, and import nothing external.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: circuit-breaker.ts + ai-cost.ts</name>
  <behavior>
    - 5 recordFailure() within 60s -> canRequest() === false (OPEN)
    - after RESET_MS, canRequest() allows 1 probe (HALF-OPEN); recordSuccess() -> CLOSED
    - calculateCost('claude-sonnet-4-6', 1000, 200, 500) bills 800 input + 200 cached_read + 500 output at the verified rates
    - calculateCost returns 0 for an unknown model (defensive default)
  </behavior>
  <read_first>
    - docs/conhecimento/prompts/templates/08-edge-function-reference.ts (L47-77 CircuitBreaker, L141-149 cost)
    - supabase/functions/_shared/__tests__/circuit-breaker.test.ts + ai-cost.test.ts (RED contracts)
    - .planning/phases/09-ai-prompt-library-cost-infra/09-RESEARCH.md §Code Examples (verified COST_PER_TOKEN)
  </read_first>
  <files>supabase/functions/_shared/circuit-breaker.ts, supabase/functions/_shared/ai-cost.ts</files>
  <action>Implement circuit-breaker.ts as an in-memory CircuitBreaker (per-isolate, documented limitation per PRD §3.1) with THRESHOLD=5, RESET_MS=60000, exposing canRequest(), recordFailure(), recordSuccess() and the CLOSED/OPEN/HALF-OPEN states. Implement ai-cost.ts exporting the COST_PER_TOKEN table (the VERIFIED RESEARCH values in the interfaces block — use these, not the reference's, if they differ) and calculateCost(model, inputTokens, cachedTokens, outputTokens) = (inputTokens - cachedTokens)*input + cachedTokens*cached_read + outputTokens*output, defaulting to 0 for an unknown model. No DB/SDK. These flip circuit-breaker.test.ts and ai-cost.test.ts GREEN.</action>
  <verify>
    <automated>command -v deno >/dev/null 2>&1 && deno test --no-check supabase/functions/_shared/__tests__/circuit-breaker.test.ts supabase/functions/_shared/__tests__/ai-cost.test.ts 2>&1 | tail -15 || echo "deno not on PATH — confirm exports"; grep -c "claude-sonnet-4-6\|claude-haiku-4-5\|gpt-4o-mini" supabase/functions/_shared/ai-cost.ts</automated>
  </verify>
  <acceptance_criteria>
    - test command: circuit-breaker.test.ts + ai-cost.test.ts pass (GREEN).
    - ai-cost.ts COST_PER_TOKEN holds all 3 models with the exact verified rates.
    - circuit-breaker THRESHOLD=5 / RESET_MS=60000 with the 3 state methods.
    - source: no external import.
  </acceptance_criteria>
  <done>circuit-breaker.ts and ai-cost.ts exist, flip their RED tests GREEN, and carry the verified breaker thresholds + cost table.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm LGPD-04 guard GREEN over the new helpers</name>
  <read_first>
    - src/__tests__/guards/forbidden-strings.grep.test.ts (Plan 01 guard)
    - supabase/functions/_shared/pii-masker.ts, injection-detector.ts, circuit-breaker.ts, ai-cost.ts (the files just written)
  </read_first>
  <files>supabase/functions/_shared/pii-masker.ts</files>
  <action>Run the LGPD-04 grep guard against the now-populated supabase/functions/ tree. If any forbidden term slipped into a helper comment or string, replace it with "avaliacao comportamental/cognitiva" (or rephrase). This task asserts the guard is GREEN — it is the smoke-runtime gate for product language in the new EF helpers. No new file is created; the listed file is the most likely edit target if a fix is needed.</action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && npx vitest run src/__tests__/guards/forbidden-strings.grep.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - test command: forbidden-strings.grep.test.ts passes (GREEN) with supabase/functions/_shared/* present.
    - No forbidden term in any of the 4 new helpers.
  </acceptance_criteria>
  <done>The LGPD-04 guard is GREEN over src/ + supabase/functions/ including the 4 new helpers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate input -> log | pii-masker runs before any value is persisted (composed by Plan 05 audit-logger) |
| candidate input -> LLM API | injection-detector gates the call (short-circuits on detection) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Information Disclosure | raw PII reaching ai_call_logs | mitigate | pii-masker.ts strips 7 PT-BR PII categories; ai-client composes it before INSERT (Plan 05) |
| T-09-12 | Tampering | prompt injection in candidate input | mitigate | injection-detector.ts 8 patterns -> short-circuit, no API call, flag for human review (RF-PL-18) |
| T-09-13 | Denial of Service | provider outage cascading | mitigate | circuit-breaker opens at 5/60s gating the OpenAI fallback (Plan 05) |
| T-09-14 | Compliance (LGPD/RNF-12) | forbidden copy in helpers | mitigate | Task 3 keeps the LGPD-04 guard GREEN over supabase/functions/ |
</threat_model>

<verification>
- All 4 helper Deno tests GREEN; LGPD-04 guard GREEN over the new tree.
- No DB/SDK/network import in any helper (pure utilities).
</verification>

<success_criteria>
- IA-02/03/04 + RF-PL-18 utility layer complete and tested.
- ai-cost uses the verified COST_PER_TOKEN values.
- Product language clean.
</success_criteria>

<output>
Create `.planning/phases/09-ai-prompt-library-cost-infra/09-04-SUMMARY.md` when done.
</output>
