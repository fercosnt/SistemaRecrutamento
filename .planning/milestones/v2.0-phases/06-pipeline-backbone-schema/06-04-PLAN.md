---
phase: 06-pipeline-backbone-schema
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - supabase/migrations/20260607000005_avancar_etapa_trigger.sql
  - src/types/database.types.ts
autonomous: false
requirements: [FUNIL-02, FUNIL-03]
must_haves:
  truths:
    - "An UPDATE on candidaturas.etapa_atual advancing forward (including skipping stages) succeeds"
    - "An UPDATE regressing to an earlier etapa with empty etapa_justificativa raises an exception"
    - "The same regressing UPDATE WITH a justificativa succeeds"
    - "Setting a terminal (aprovado/rejeitado) from any stage succeeds"
    - "Every etapa transition writes exactly one historico_candidatura row with criterio_texto, ator, timestamp"
    - "ator resolves to auth.uid() for RH client UPDATEs and NULL for service_role/system writes"
    - "Decisions covered — D-06: forward advance free (incl. skipping stages), regression requires non-empty justificativa, terminals settable from any stage; D-07: trigger reads NEW.etapa_justificativa companion column and copies it to historico (atomic, no session/GUC state); D-09: historico_candidatura.ator is NULL-able (human=auth.uid(), system=NULL+auto_rejeitado), with the zero-auto-rejection guardrail living ONLY in decisao_final.por_usuario NOT NULL"
  artifacts:
    - path: "supabase/migrations/20260607000005_avancar_etapa_trigger.sql"
      provides: "PL/pgSQL avancar_etapa() BEFORE UPDATE trigger (SECURITY INVOKER) + audit write"
      contains: "avancar_etapa"
  key_links:
    - from: "avancar_etapa() trigger"
      to: "historico_candidatura"
      via: "INSERT in the same transaction as the candidaturas UPDATE"
      pattern: "INSERT INTO public.historico_candidatura"
    - from: "regression detection"
      to: "enum ordinal comparison"
      via: "NEW.etapa_atual < OLD.etapa_atual (declaration-order < )"
      pattern: "NEW.etapa_atual < OLD.etapa_atual"
---

<objective>
Author and apply the `avancar_etapa()` PL/pgSQL BEFORE UPDATE trigger on `candidaturas` — the
unbypassable transition validator + audit writer. Forward/skip transitions are free (D-06);
regression to an earlier etapa requires a non-empty etapa_justificativa or the UPDATE is blocked
(success criterion #2); terminals (aprovado/rejeitado) are allowed from any stage. Every transition
writes one historico_candidatura row capturing etapa_de/etapa_para, criterio_texto, ator (auth.uid()),
auto_rejeitado, and timestamp (FUNIL-03). Also confirm status_candidatura needs no change this phase.

Purpose: The DB IS the application logic here. The trigger makes the regression-block and the audit
trail structural and unbypassable — no client, EF, or future feature can advance an etapa without
the audit row landing in the same transaction. SECURITY INVOKER is load-bearing: it makes auth.uid()
resolve to the calling RH user (and NULL for service_role) — the exact D-09 actor-capture design.

Output: 1 PL/pgSQL migration + regenerated types. Contains CREATE FUNCTION + DO $$ → 42601 trap →
human-action SQL-Editor apply (autonomous:false).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pipeline-backbone-schema/06-PATTERNS.md
@.planning/phases/06-pipeline-backbone-schema/06-RESEARCH.md
@.planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md
@CLAUDE.md
@supabase/migrations/20260606000001_vaga_status_sync.sql

<interfaces>
- v2 enum (from 06-02, PIPELINE ORDER): inscricao < triagem < avaliacao_assincrona < entrevista_online < entrevista_presencial < decisao_final < aprovado < rejeitado
- Companion column (from 06-02): public.candidaturas.etapa_justificativa text
- Audit table (from 06-02): public.historico_candidatura (etapa_de, etapa_para, criterio_texto, ator, auto_rejeitado, criado_em)
- Actor: v_ator uuid := auth.uid(); — NULL for service_role/system (D-09); auto_rejeitado = (v_ator IS NULL)
- Trigger MUST be SECURITY INVOKER (omit SECURITY DEFINER — Pitfall 4) so auth.uid() resolves to the caller
- Idempotent bind via DO $$ + pg_trigger existence check (PATTERNS lines 77-92 analog)
- live status_candidatura values (no change needed per RESEARCH Q2): aguardando_resposta, em_analise, aprovado_proxima, rejeitado, finalizado
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author avancar_etapa() trigger migration</name>
  <files>supabase/migrations/20260607000005_avancar_etapa_trigger.sql</files>
  <read_first>
    - supabase/migrations/20260606000001_vaga_status_sync.sql (EXACT analog — the 42601 NOTE block lines 45-53 to copy verbatim; the BEFORE-trigger function skeleton lines 59-69 with NO SECURITY DEFINER; the idempotent DO $$ + pg_trigger bind lines 80-92)
    - .planning/phases/06-pipeline-backbone-schema/06-RESEARCH.md §"Pattern 2: Enum declaration order = computable regression" (terminal special-case + NEW < OLD) and §"Pattern 3: Actor capture in a plain trigger" (v_ator := auth.uid()) and §"Pitfall 4" (SECURITY DEFINER nullifies actor capture)
    - .planning/phases/06-pipeline-backbone-schema/06-PATTERNS.md §"..._avancar_etapa_trigger.sql" (the structure copy source + the new logic to author) and §"Shared Pattern C" (SECURITY INVOKER vs DEFINER rule)
    - CLAUDE.md §Commands (42601 workaround — no BEGIN/COMMIT wrapper)
  </read_first>
  <action>
    Author `public.avancar_etapa()` as a `RETURNS trigger LANGUAGE plpgsql` function — OMIT SECURITY
    DEFINER (defaults to SECURITY INVOKER; this is load-bearing for D-09 actor capture, Pitfall 4).
    Function body, fired BEFORE UPDATE OF etapa_atual ON public.candidaturas FOR EACH ROW:
    1. If `NEW.etapa_atual IS NOT DISTINCT FROM OLD.etapa_atual` → `RETURN NEW;` (no etapa change, no audit).
    2. Terminal special-case (D-06): `IF NEW.etapa_atual IN ('aprovado','rejeitado') THEN NULL;`  -- always allowed from any stage.
    3. `ELSIF NEW.etapa_atual < OLD.etapa_atual THEN` (regression via enum ordinal compare): `IF NEW.etapa_justificativa IS NULL OR btrim(NEW.etapa_justificativa) = '' THEN RAISE EXCEPTION 'Regressão de etapa exige justificativa preenchida'; END IF;`
    4. Forward / skip-ahead (NEW > OLD, non-terminal) → allowed freely (no extra condition needed).
    5. Capture actor + write audit (FUNIL-03, D-09): `v_ator uuid := auth.uid();` then `INSERT INTO public.historico_candidatura (candidatura_id, etapa_de, etapa_para, criterio_texto, ator, auto_rejeitado, criado_em) VALUES (NEW.id, OLD.etapa_atual, NEW.etapa_atual, NEW.etapa_justificativa, v_ator, (v_ator IS NULL), now());`
    6. `RETURN NEW;`
    Then bind idempotently via the DO $$ + pg_trigger existence-check pattern: trigger name e.g.
    `candidaturas_avancar_etapa_trg`, `BEFORE UPDATE OF etapa_atual ON public.candidaturas FOR EACH ROW EXECUTE FUNCTION public.avancar_etapa()`.
    Add `COMMENT ON FUNCTION public.avancar_etapa() IS '...'` (trailing COMMENT — part of why 42601 applies).
    Copy the Shared Pattern B header + the 42601 NOTE block verbatim. NO BEGIN/COMMIT wrapper.
    Append a short comment confirming status_candidatura is unchanged this phase (RESEARCH Q2 — leave as-is;
    its 5 values aguardando_resposta/em_analise/aprovado_proxima/rejeitado/finalizado stay).
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260607000005_avancar_etapa_trigger.sql > /tmp/trg.sql; grep -q "CREATE OR REPLACE FUNCTION public.avancar_etapa()" /tmp/trg.sql && grep -q "NEW.etapa_atual < OLD.etapa_atual" /tmp/trg.sql && grep -q "RAISE EXCEPTION" /tmp/trg.sql && grep -q "INSERT INTO public.historico_candidatura" /tmp/trg.sql && grep -q "auth.uid()" /tmp/trg.sql && ! grep -qi "SECURITY DEFINER" /tmp/trg.sql && ! grep -qi "^BEGIN;\|^COMMIT;" /tmp/trg.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Function is `LANGUAGE plpgsql` with NO `SECURITY DEFINER` (SECURITY INVOKER default).
    - Terminal special-case (`IN ('aprovado','rejeitado')`) precedes the regression check.
    - Regression detection uses `NEW.etapa_atual < OLD.etapa_atual` and RAISEs when justificativa is null/blank.
    - Audit INSERT into historico_candidatura captures `auth.uid()` as ator and `(v_ator IS NULL)` as auto_rejeitado.
    - Idempotent DO $$ + pg_trigger bind; 42601 NOTE present; no BEGIN/COMMIT wrapper.
  </acceptance_criteria>
  <done>avancar_etapa() trigger authored — SECURITY INVOKER, ordinal regression block, same-txn audit write.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [BLOCKING] Apply trigger via SQL Editor (42601 workaround) + run FUNIL-02/03 smokes + regen types</name>
  <files>src/types/database.types.ts</files>
  <read_first>
    - CLAUDE.md §Commands (42601 workaround: SQL Editor → migration repair --status applied <version> → db push verify)
    - .planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md §C (FUNIL-02 regression-block smoke) + §D (FUNIL-03 audit-trail smoke)
    - .planning/STATE.md Phase 4 ledger 04-01 (human-action checkpoint shape)
  </read_first>
  <action>Human-action checkpoint: apply the avancar_etapa() trigger migration (20260607000005) to the LIVE DB via the SQLSTATE 42601 SQL-Editor workaround, run the FUNIL-02 regression-block + FUNIL-03 audit-trail smokes, reconcile with migration repair, confirm db push is up to date, and regenerate database.types.ts. Detailed steps in <how-to-verify>.</action>
  <what-built>
    The avancar_etapa() PL/pgSQL trigger migration (20260607000005). It contains CREATE FUNCTION +
    DO $$ + COMMENT → hits the SQLSTATE 42601 transaction-pooler trap → MUST be applied manually via
    the Supabase SQL Editor, then reconciled with migration repair.
  </what-built>
  <how-to-verify>
    Fernando runs in his terminal / Supabase SQL Editor (project isljnozzlvckrgjjbjwp):
    1. Paste + run the trigger SQL (20260607000005) in the SQL Editor.
    2. Run the FUNIL-02 regression-block SMOKE (runbook §C) against a real candidatura at 'triagem':
       (a) forward/skip UPDATE to a later etapa → MUST succeed;
       (b) regress to 'inscricao' with empty etapa_justificativa → MUST raise the exception;
       (c) same regress WITH etapa_justificativa = 'motivo...' → MUST succeed;
       (d) set terminal 'rejeitado' from any stage → MUST succeed.
    3. Run the FUNIL-03 audit SMOKE (runbook §D): confirm each successful transition wrote one
       historico_candidatura row with etapa_de/etapa_para, criterio_texto, ator (= the RH user's auth.uid),
       auto_rejeitado, criado_em. Confirm a service_role/EF-context write would log ator IS NULL (D-09).
    4. `supabase migration repair --status applied 20260607000005`.
    5. Confirm `supabase db push --linked` reports "Remote database is up to date".
    6. `npm run db:types` → regenerate src/types/database.types.ts (never hand-edit).
    Paste the regression-block results + audit-row evidence + db push confirmation into the runbook.
    IMPORTANT: leave the test candidatura in a clean state (or restore from backup_m2) after smoking.
  </how-to-verify>
  <acceptance_criteria>
    - Regression with empty justificativa raises; regression WITH justificativa succeeds; forward/skip + terminal succeed.
    - Each transition produced exactly one historico_candidatura row with the correct etapa_de/etapa_para + ator.
    - `supabase db push --linked` reports "Remote database is up to date".
    - Regenerated database.types.ts includes the trigger-adjacent schema (no manual edits).
  </acceptance_criteria>
  <verify>
    <automated>npx supabase db push --linked 2>&1 | grep -qi "up to date" && echo OK</automated>
  </verify>
  <done>Trigger applied; regression without justificativa raises, forward/skip/terminal succeed; every transition logged in historico_candidatura with correct ator; db push up to date; types regenerated.</done>
  <resume-signal>Type "applied" with the 4 regression-block results + the audit-row evidence + db push confirmation, or describe the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH client → candidaturas UPDATE | RH attempts to advance/regress an etapa; trigger validates transition + records actor |
| service_role/EF → candidaturas UPDATE | system/auto writes (e.g. knockout in Phase 8) — auth.uid() resolves NULL, logged as system action |
| migration apply → live DB | PL/pgSQL apply via 42601 single-session workaround |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering / Elevation | Client bypasses pipeline rules via a direct regressing UPDATE | mitigate | BEFORE UPDATE trigger validates the transition in-DB (unbypassable); regression without justificativa RAISEs; RLS UPDATE policy (06-05) gates which role may UPDATE at all. |
| T-06-11 | Repudiation | A spoofed/forged actor in the audit trail | mitigate | `ator := auth.uid()` reads the verified JWT sub, not a client-passed id; SECURITY INVOKER preserves the caller context. |
| T-06-12 | Elevation of Privilege | SECURITY DEFINER misuse nullifies actor capture | mitigate | Trigger is SECURITY INVOKER (Pitfall 4); acceptance grep asserts absence of `SECURITY DEFINER`. |
| T-06-13 | Repudiation | A transition occurs with no audit row (silent state change) | mitigate | The audit INSERT runs in the SAME transaction as the UPDATE inside the trigger — atomic; no transition can land without its historico row. |
| T-06-14 | Tampering | Migration partial-apply via 42601 mid-PL/pgSQL | mitigate | Single-session SQL-Editor apply (D-22); idempotent DO $$ bind allows safe re-run; FUNIL-02/03 smokes gate completion. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure PL/pgSQL DDL + generated types. |
</threat_model>

<verification>
- Trigger migration passes structural grep gate (SECURITY INVOKER, ordinal regression, audit INSERT, no BEGIN/COMMIT).
- FUNIL-02 smoke: 4 transition cases behave per D-06 + success criterion #2.
- FUNIL-03 smoke: every transition logged with criterio/ator/timestamp.
- `supabase db push --linked` → "Remote database is up to date"; types regenerated.
</verification>

<success_criteria>
- ROADMAP success criterion #2: forward advance via trigger works; regression without justificativa blocked.
- ROADMAP success criterion #3: every transition (incl. system/auto) generates a historico_candidatura line.
</success_criteria>

<output>
Create `.planning/phases/06-pipeline-backbone-schema/06-04-SUMMARY.md` when done.
</output>
