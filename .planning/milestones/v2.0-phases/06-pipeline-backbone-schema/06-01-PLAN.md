---
phase: 06-pipeline-backbone-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md
autonomous: true
requirements: [FUNIL-01, FUNIL-02, FUNIL-03, FUNIL-04, LGPD-02]
must_haves:
  truths:
    - "A single runbook artifact holds the pre-cutover discovery query + the 5 phase audit queries"
    - "The discovery query result records the live etapa_atual distribution + row count BEFORE any DDL runs"
    - "Each downstream migration has a named SQL-smoke it must pass after apply"
  artifacts:
    - path: ".planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md"
      provides: "Discovery + 5 audit queries (FUNIL-01..04, LGPD-02) + result-capture sections"
      contains: "GROUP BY etapa_atual"
  key_links:
    - from: "06-SQL-SMOKE-RUNBOOK.md discovery section"
      to: "06-02 cutover USING CASE"
      via: "discovery result tells executor whether orphan logging is needed"
      pattern: "GROUP BY"
---

<objective>
Author the Wave-0 SQL-smoke runbook: the single source-of-truth markdown artifact holding the
pre-cutover discovery query and the five phase-gate audit queries (one per requirement). This
runbook is the canonical verification harness for the whole phase — every migration apply in
later plans runs its matching query from here and pastes the result back in.

Purpose: Phase 4 proved that autonomous green gates (build/lint/types) do NOT prove a live DB
change worked, because types come from config not the live DB (D-25..D-28). The SQL smoke is the
ONLY real proof for a schema phase. Running discovery BEFORE writing the cutover CASE removes
RESEARCH Assumptions A1/A5 (live row distribution unknown).

Output: `.planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md` with a discovery
section (run first, result captured) and 5 audit sections (FUNIL-01..04, LGPD-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pipeline-backbone-schema/06-VALIDATION.md
@.planning/phases/06-pipeline-backbone-schema/06-RESEARCH.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SQL-smoke runbook artifact</name>
  <files>.planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/06-pipeline-backbone-schema/06-RESEARCH.md §"Code Examples" (the discovery query + the 4 audit queries verbatim) and §"Validation Architecture → Phase Requirements → Test Map"
    - .planning/phases/06-pipeline-backbone-schema/06-VALIDATION.md §"Wave 0 Requirements" + §"Per-Task Verification Map"
    - .planning/ROADMAP.md Phase 6 §"Success Criteria" (the 5 verification targets the audits must prove)
  </read_first>
  <action>
    Create the markdown runbook with these sections, each containing the exact SQL (from RESEARCH
    §Code Examples) plus an empty "Result captured:" block the executor fills after running in the
    Supabase SQL Editor against project isljnozzlvckrgjjbjwp:

    (A) DISCOVERY (run FIRST, before any DDL — FUNIL-01 precondition):
      - `SELECT etapa_atual, count(*) FROM public.candidaturas GROUP BY 1 ORDER BY 2 DESC;`
      - `SELECT column_default FROM information_schema.columns WHERE table_schema='public' AND table_name='candidaturas' AND column_name='etapa_atual';`
      - `SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename IN ('historico_candidatura','decisao_final','bias_audit_log');` (expect 0 rows — confirms net-new tables absent)
      - Note inline: if discovery shows any etapa_atual value other than triagem/aprovado/rejeitado/entrevista_online/entrevista_presencial, the orphan rows collapse to triagem per D-05 and each MUST get a historico_candidatura audit line during cutover.

    (B) AUDIT FUNIL-01 (cutover, no data loss): post-cutover `SELECT etapa_atual, count(*) FROM public.candidaturas GROUP BY 1 ORDER BY 2 DESC;` — total row count MUST equal the discovery total; spell out the expected mapping (triagem stays, orphans → triagem).

    (C) AUDIT FUNIL-02 (regression block): the two-statement smoke from RESEARCH §"Regression-block smoke" — a regressing UPDATE to an earlier etapa with empty etapa_justificativa MUST raise an exception; same UPDATE WITH justificativa MUST succeed. Include a forward/skip UPDATE that MUST succeed (D-06).

    (D) AUDIT FUNIL-03 (audit trail): `SELECT candidatura_id, etapa_de, etapa_para, criterio_texto, ator, auto_rejeitado, criado_em FROM public.historico_candidatura ORDER BY criado_em DESC LIMIT 50;` plus `SELECT count(*) FROM public.historico_candidatura WHERE ator IS NULL AND auto_rejeitado = true;` (system-action rows).

    (E) AUDIT FUNIL-04 (RLS isolation): role-impersonated read instructions — as candidato A read own candidatura (visible) vs another candidato's (0 rows); as RH read all (visible). Plus structural proof: `SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname='public' AND tablename IN ('historico_candidatura','decisao_final','bias_audit_log');` — rowsecurity MUST be true for every new table. Plus `SELECT tablename, policyname, cmd FROM pg_policies WHERE schemaname='public' AND tablename IN ('candidaturas','historico_candidatura','decisao_final','bias_audit_log') ORDER BY 1,3;`.

    (F) AUDIT LGPD-02 (zero-auto-rejection guardrail): `SELECT count(*) AS auto_decisoes FROM public.decisao_final WHERE por_usuario IS NULL;` — MUST return 0.

    Header the file with a table mapping each audit section → requirement ID → which plan applies it
    (06-02 cutover = FUNIL-01; 06-04 trigger = FUNIL-02/03; 06-05 RLS = FUNIL-04; 06-03 decisao_final = LGPD-02).
    Do NOT invent SQL — copy from RESEARCH §Code Examples; this task only assembles + annotates.
  </action>
  <verify>
    <automated>grep -q "GROUP BY etapa_atual\|GROUP BY 1" ".planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md" && grep -q "por_usuario IS NULL" ".planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md" && grep -q "rowsecurity" ".planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The file contains all 6 sections (A discovery + B-F five audits), each with runnable SQL.
    - The LGPD-02 audit query `SELECT count(*) ... WHERE por_usuario IS NULL` is present verbatim and annotated "MUST return 0".
    - The FUNIL-04 audit includes both a role-impersonation read instruction AND the `pg_tables.rowsecurity` structural check across all 3 new tables.
    - A header table maps each requirement ID to the plan that applies its migration.
  </acceptance_criteria>
  <done>The runbook exists with discovery + 5 audits; downstream plans reference it by section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan authors a documentation artifact only — no DB change, no code, no trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-RB-01 | Tampering | Audit query correctness (a wrong/blind smoke gives false-green) | mitigate | Queries copied verbatim from RESEARCH §Code Examples (CITED from PostgreSQL docs); reviewer cross-checks each against the requirement it proves before downstream plans depend on it. |
| T-06-RB-02 | Information Disclosure | Capturing live PII (candidato data) into the runbook result blocks | mitigate | Result-capture sections record COUNTS and etapa distribution only — never row-level PII; instruct executor to paste aggregate output, not SELECT * dumps. |
</threat_model>

<verification>
- Runbook file exists and the grep gate passes.
- All 5 requirement IDs (FUNIL-01..04, LGPD-02) each have a dedicated audit section.
</verification>

<success_criteria>
- A single runbook artifact is the canonical SQL verification harness for the phase.
- Discovery query is positioned to run BEFORE the cutover CASE is written (removes Assumptions A1/A5).
</success_criteria>

<output>
Create `.planning/phases/06-pipeline-backbone-schema/06-01-SUMMARY.md` when done.
</output>
