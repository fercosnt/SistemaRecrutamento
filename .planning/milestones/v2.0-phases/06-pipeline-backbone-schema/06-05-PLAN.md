---
phase: 06-pipeline-backbone-schema
plan: 05
type: execute
wave: 4
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - supabase/migrations/20260607000006_rls_policies_m2_backbone.sql
  - .planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md
  - src/types/database.types.ts
autonomous: false
requirements: [FUNIL-04, LGPD-02]
must_haves:
  truths:
    - "A candidato can read only their own candidatura (another candidato's returns 0 rows)"
    - "RH/admin read candidaturas + historico_candidatura per JWT app_metadata.role"
    - "An RH/admin UPDATE on candidaturas.etapa_atual is permitted; candidato UPDATE is denied"
    - "RLS is enabled on 100% of the new M2 tables (candidaturas policies + historico_candidatura + decisao_final + bias_audit_log)"
    - "decisao_final client INSERT stays blocked (WITH CHECK false) — confirmed end-to-end"
    - "Decisions covered — D-08: etapa advance = direct UPDATE candidaturas (supabase-js) RLS-gated to role rh/administrador with the trigger validating the transition and ator=auth.uid(); decisao_final stays EF-only (WITH CHECK false), no per-click Edge Function for pipeline advance"
  artifacts:
    - path: "supabase/migrations/20260607000006_rls_policies_m2_backbone.sql"
      provides: "FUNIL-04 RLS: candidato isolation + RH/admin role reads + candidaturas UPDATE policy + historico policies"
      contains: "app_metadata,role"
  key_links:
    - from: "candidato SELECT policy on candidaturas"
      to: "candidatos.user_id = auth.uid()"
      via: "candidato_id IN (SELECT id FROM candidatos WHERE user_id = (select auth.uid()))"
      pattern: "user_id = \\(select auth.uid"
    - from: "rh_avanca_etapa UPDATE policy"
      to: "JWT role"
      via: "(select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador')"
      pattern: "app_metadata,role"
---

<objective>
Author and apply the FUNIL-04 RLS bundle that completes access control across the M2 backbone:
candidato row-isolation on candidaturas + historico_candidatura, RH/admin role-gated reads via the
JWT app_metadata.role claim, and the RH/administrador UPDATE policy on candidaturas (D-08) that makes
the direct-UPDATE pipeline-advance flow work. Then run the full FUNIL-04 role-impersonation smoke +
the structural pg_tables.rowsecurity / pg_policies audit, and finalize the runbook.

Purpose: RLS is THE enforcement boundary for this phase. Until the candidaturas UPDATE policy exists,
no RH can advance an etapa (the trigger from 06-04 only fires once an UPDATE passes RLS). The CRITICAL
correctness risk is copying the stale PRD §8.3 template: it uses `auth_user_id` (live column is
`user_id`) and `'admin'` (live role is `'administrador'`) — verbatim copy silently breaks every read.
Every policy here MUST use the live-verified identifiers + the shipped `(select auth.jwt() #>>
'{app_metadata,role}')` idiom.

Output: 1 RLS migration + runbook finalization + regenerated types. May contain GRANT/REVOKE adjacent
to policies → 42601-eligible → human-action SQL-Editor apply (autonomous:false; named candidate in the
schema_push_requirement).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pipeline-backbone-schema/06-PATTERNS.md
@.planning/phases/06-pipeline-backbone-schema/06-RESEARCH.md
@.planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md
@CLAUDE.md
@supabase/migrations/20260425000002_curriculos_bucket.sql
@supabase/migrations/20260420000002_unified_auth_role.sql

<interfaces>
VERIFIED live identifiers (use EXACTLY — PRD §8.3 is stale, do NOT copy it):
- Auth FK column: `user_id` on BOTH candidatos and usuarios_rh (NOT auth_user_id)
- Role values: 'rh', 'administrador', 'candidato' (NOT 'admin')
- Shipped RLS role idiom (Shared Pattern A, in production curriculos_bucket): `(select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador')`
- Candidato ownership idiom: `candidato_id IN (SELECT id FROM public.candidatos WHERE user_id = (select auth.uid()))`
- No existing candidaturas UPDATE policy to reconcile (PATTERNS confirmed only 5 storage policies exist)
- Tables needing policies: candidaturas (SELECT candidato-own + SELECT rh + UPDATE rh), historico_candidatura (SELECT candidato-own + SELECT rh). decisao_final + bias_audit_log policies already landed in 06-03 (verify, don't duplicate).
- decisao_final client INSERT already WITH CHECK false (06-03) — this plan re-verifies it end-to-end
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the M2 backbone RLS bundle migration</name>
  <files>supabase/migrations/20260607000006_rls_policies_m2_backbone.sql</files>
  <read_first>
    - supabase/migrations/20260425000002_curriculos_bucket.sql (L50-103 — the LIVE shipped CREATE POLICY idiom with the (select auth.jwt() #>> '{app_metadata,role}') wrapped form; copy this idiom, NOT the PRD)
    - supabase/migrations/20260420000002_unified_auth_role.sql (L40-81 — confirms role values 'rh'/'administrador'/'candidato' and WHERE user_id = ...)
    - .planning/phases/06-pipeline-backbone-schema/06-PATTERNS.md §"Shared Pattern A" (the verified corrections over PRD §8.3) + §"Shared Pattern B" (header; this file has no PL/pgSQL $$ so 42601 NOTE is optional but GRANT/REVOKE adjacency may still trigger it — include the NOTE defensively)
    - .planning/phases/06-pipeline-backbone-schema/06-RESEARCH.md §"Pitfall 1" (the corrected RLS template — auth_user_id→user_id, admin→administrador)
  </read_first>
  <action>
    Author RLS policies using ONLY the live-verified identifiers. Ensure RLS is enabled on candidaturas
    if not already (`ALTER TABLE public.candidaturas ENABLE ROW LEVEL SECURITY` — idempotent guard).
    Policies:
    - `candidato_le_propria_candidatura` ON public.candidaturas FOR SELECT USING (candidato_id IN (SELECT id FROM public.candidatos WHERE user_id = (select auth.uid()))).
    - `rh_le_candidaturas` ON public.candidaturas FOR SELECT USING ((select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador')).
    - `rh_avanca_etapa` ON public.candidaturas FOR UPDATE USING ((select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador')) WITH CHECK ((select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador')).  -- D-08: enables the direct-UPDATE pipeline advance; candidato has no UPDATE policy → denied.
    - `candidato_le_proprio_historico` ON public.historico_candidatura FOR SELECT USING (candidatura_id IN (SELECT id FROM public.candidaturas WHERE candidato_id IN (SELECT id FROM public.candidatos WHERE user_id = (select auth.uid())))).
    - `rh_le_historico` ON public.historico_candidatura FOR SELECT USING ((select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador')).
    Do NOT add an INSERT policy on historico_candidatura — it is written only by the SECURITY INVOKER trigger
    inside the candidaturas UPDATE transaction (the trigger runs with the caller's rights; the candidaturas
    UPDATE policy is what authorizes the whole transaction). Add a COMMENT explaining this.
    Do NOT re-create decisao_final / bias_audit_log policies (already in 06-03) — add a comment pointing there.
    Use `DROP POLICY IF EXISTS <name> ON <table>;` before each CREATE POLICY for idempotency.
    Shared Pattern B header + defensive 42601 NOTE (GRANT/REVOKE adjacency). NO BEGIN/COMMIT wrapper.
    ZERO occurrences of 'admin' (use 'administrador') or auth_user_id (use user_id).
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260607000006_rls_policies_m2_backbone.sql > /tmp/rls.sql; grep -q "rh_avanca_etapa" /tmp/rls.sql && grep -q "candidato_le_propria_candidatura" /tmp/rls.sql && grep -c "app_metadata,role" /tmp/rls.sql | grep -qv '^0$' && grep -q "user_id = (select auth.uid" /tmp/rls.sql && ! grep -q "'admin'\|auth_user_id" /tmp/rls.sql && ! grep -qi "^BEGIN;\|^COMMIT;" /tmp/rls.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - candidato SELECT policies on candidaturas + historico_candidatura scope via `user_id = (select auth.uid())`.
    - RH/admin SELECT + the `rh_avanca_etapa` UPDATE policy use `(select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador')`.
    - No candidato UPDATE policy on candidaturas (advance is RH/admin only, D-08).
    - No INSERT policy on historico_candidatura (trigger-written inside the authorized UPDATE txn); COMMENT explains.
    - ZERO `'admin'` / `auth_user_id` occurrences; idempotent DROP POLICY IF EXISTS guards; no BEGIN/COMMIT.
  </acceptance_criteria>
  <done>RLS bundle authored with corrected live identifiers + the candidaturas UPDATE policy.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [BLOCKING] Apply RLS via SQL Editor (42601 workaround) + full FUNIL-04 + LGPD-02 smokes + finalize runbook + regen types</name>
  <files>src/types/database.types.ts, .planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md</files>
  <read_first>
    - CLAUDE.md §Commands (42601 workaround)
    - .planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md §E (FUNIL-04 RLS role-impersonation + rowsecurity/pg_policies structural) + §F (LGPD-02 guardrail audit)
    - .planning/STATE.md Phase 4 ledger 04-01 (human-action checkpoint shape)
  </read_first>
  <action>Human-action checkpoint: apply the RLS bundle migration (20260607000006) to the LIVE DB via the SQLSTATE 42601 SQL-Editor workaround, run the full FUNIL-04 role-impersonation + structural smokes + the LGPD-02 guardrail audit, reconcile with migration repair, confirm db push is up to date, run npm run test:run, finalize the runbook, and regenerate database.types.ts. Detailed steps in <how-to-verify>.</action>
  <what-built>
    The M2 backbone RLS bundle (20260607000006) — candidato isolation, RH/admin role reads, and the
    candidaturas UPDATE policy. GRANT/REVOKE adjacency makes it 42601-eligible → manual SQL-Editor apply.
    This is the FINAL apply of the phase; after it, all five SQL audits must be green.
  </what-built>
  <how-to-verify>
    Fernando runs in his terminal / Supabase SQL Editor (project isljnozzlvckrgjjbjwp):
    1. Paste + run the RLS SQL (20260607000006) in the SQL Editor.
    2. FUNIL-04 role-impersonation SMOKE (runbook §E): as candidato A read own candidatura (visible) and
       another candidato's (0 rows); as RH read all candidaturas (visible); attempt a candidaturas UPDATE
       as candidato (denied) and as RH (allowed → fires the trigger from 06-04).
    3. FUNIL-04 structural SMOKE: `SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname='public' AND tablename IN ('candidaturas','historico_candidatura','decisao_final','bias_audit_log');` — rowsecurity MUST be true for all four. Run the pg_policies listing — confirm every expected policy exists with no 'admin'/auth_user_id residue.
    4. LGPD-02 SMOKE (runbook §F): `SELECT count(*) FROM public.decisao_final WHERE por_usuario IS NULL;` → 0. Also confirm a client INSERT into decisao_final is rejected (WITH CHECK false) end-to-end.
    5. `supabase migration repair --status applied 20260607000006`.
    6. Confirm `supabase db push --linked` reports "Remote database is up to date".
    7. `npm run db:types` → regenerate src/types/database.types.ts.
    8. Run `npm run test:run` — confirm no NEW regression (the single pre-existing LoadingProgress carryover from M1 is allowed).
    9. Mark runbook frontmatter `nyquist_compliant: true` and `wave_0_complete: true`; paste all five audit results into the runbook.
  </how-to-verify>
  <acceptance_criteria>
    - Candidato sees only own candidatura; RH/admin see all; candidato UPDATE denied, RH UPDATE allowed.
    - pg_tables.rowsecurity = true for all four tables (candidaturas + 3 new M2 tables — 100% RLS).
    - LGPD-02 audit returns 0; client INSERT into decisao_final rejected.
    - `supabase db push --linked` reports "Remote database is up to date"; types regenerated; `npm run test:run` no new regression.
    - Runbook updated: all five audits captured green, `nyquist_compliant: true`.
  </acceptance_criteria>
  <verify>
    <automated>npx supabase db push --linked 2>&1 | grep -qi "up to date" && echo OK</automated>
  </verify>
  <done>RLS applied; candidato isolation + RH/admin reads + UPDATE authority correct; rowsecurity true on all 4 tables; LGPD-02 audit returns 0; db push up to date; test:run no new regression; runbook nyquist_compliant:true; types regenerated.</done>
  <resume-signal>Type "applied" with the role-impersonation results + rowsecurity-all-true + LGPD-02=0 + db push confirmation, or describe the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidato client → candidaturas/historico_candidatura | untrusted; must read ONLY own rows, must NOT UPDATE etapa |
| RH/admin client → candidaturas | role-gated read + the only UPDATE path (advances pipeline, fires trigger) |
| any client → decisao_final INSERT | blocked entirely (WITH CHECK false) — re-verified here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Information Disclosure | Candidato reads another candidato's candidatura / audit trail | mitigate | RLS SELECT USING `candidato_id IN (SELECT id FROM candidatos WHERE user_id = (select auth.uid()))`; role-impersonation smoke proves cross-read returns 0 rows. |
| T-06-16 | Elevation of Privilege | Candidato self-advances their etapa via direct UPDATE | mitigate | No candidato UPDATE policy on candidaturas; only `rh_avanca_etapa` (role IN rh/administrador) grants UPDATE; smoke confirms candidato UPDATE denied. |
| T-06-17 | Tampering | Stale PRD §8.3 identifiers (`'admin'`/`auth_user_id`) silently match nothing → RLS effectively open or fully closed | mitigate | Policies use live-verified `user_id` + `'administrador'`; acceptance grep blocks `'admin'`/`auth_user_id`; role-impersonation smoke is the behavioral proof. |
| T-06-18 | Repudiation / Compliance (LGPD) | A NULL-actor decision slips through despite the guardrail | mitigate | decisao_final.por_usuario NOT NULL + WITH CHECK false re-verified end-to-end; LGPD-02 audit returns 0. |
| T-06-19 | Tampering | Migration partial-apply via 42601 (GRANT/REVOKE adjacency) | mitigate | Single-session SQL-Editor apply (D-22); DROP POLICY IF EXISTS guards make re-run safe; all five audits gate phase completion. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure RLS DDL + generated types. |
</threat_model>

<verification>
- RLS migration passes structural grep gate (corrected identifiers, no BEGIN/COMMIT).
- FUNIL-04 role-impersonation smoke: candidato isolation + RH/admin reads + UPDATE authority correct.
- 100% RLS: pg_tables.rowsecurity true for candidaturas + all 3 new M2 tables.
- LGPD-02 audit returns 0; client decisao_final INSERT rejected.
- All five phase SQL audits green; `npm run test:run` no new regression; types regenerated.
</verification>

<success_criteria>
- ROADMAP success criterion #4: candidato cannot read another candidatura; RH/admin read per JWT role; RLS on 100% of new M2 tables.
- ROADMAP success criterion #5 (re-confirmed end-to-end): no decision persists with por_usuario IS NULL.
- Phase gate: all 5 SQL audits green + db push "up to date" + types regenerated.
</success_criteria>

<output>
Create `.planning/phases/06-pipeline-backbone-schema/06-05-SUMMARY.md` when done.
</output>
