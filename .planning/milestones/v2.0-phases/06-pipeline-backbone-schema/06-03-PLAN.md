---
phase: 06-pipeline-backbone-schema
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - supabase/migrations/20260607000003_decisao_final.sql
  - supabase/migrations/20260607000004_bias_audit_log.sql
autonomous: true
requirements: [LGPD-02, FUNIL-04]
must_haves:
  truths:
    - "decisao_final exists with por_usuario uuid NOT NULL (structural zero-auto-rejection guardrail)"
    - "decisao_final.justificativa is NOT NULL with CHECK length >= 50"
    - "decisao_final.decisao is a non-null enum of aprovado/rejeitado/em_espera"
    - "client INSERT into decisao_final is blocked entirely (WITH CHECK false) — EF-only writes"
    - "bias_audit_log exists as a schema-only table (snapshot job deferred to Phase 15)"
    - "Decisions covered — D-02: decisao_final created COMPLETE now (por_usuario NOT NULL, justificativa NOT NULL CHECK length>=50, decisao enum, FKs, client INSERT WITH CHECK false) so the success-criterion-#5 guardrail is SQL-auditable already in Phase 6"
  artifacts:
    - path: "supabase/migrations/20260607000003_decisao_final.sql"
      provides: "Decision table with LGPD-02 structural guardrail + EF-only RLS"
      contains: "por_usuario uuid NOT NULL"
    - path: "supabase/migrations/20260607000004_bias_audit_log.sql"
      provides: "Bias audit log table (schema only this phase)"
      contains: "bias_audit_log"
  key_links:
    - from: "decisao_final client INSERT policy"
      to: "any client-side INSERT attempt"
      via: "WITH CHECK (false)"
      pattern: "WITH CHECK \\(false\\)"
    - from: "decisao_final.por_usuario NOT NULL"
      to: "LGPD-02 SQL audit"
      via: "structural constraint makes NULL-actor decision unrepresentable"
      pattern: "por_usuario uuid NOT NULL"
---

<objective>
Author the two structural-guardrail tables: `decisao_final` (D-02, created COMPLETE now with all
constraints so the LGPD-02 guardrail is SQL-auditable in Phase 6 per success criterion #5) and
`bias_audit_log` (schema-only this phase; the monthly snapshot job is LGPD-03 → Phase 15).

Purpose: The zero-auto-rejection guarantee (LGPD-02) lives as an unfalsifiable structural constraint
— `por_usuario uuid NOT NULL` plus client INSERT `WITH CHECK false` (EF-only writes via service_role).
No client, EF bug, or future feature can persist a decision without a human actor. decisao_final is
created in full now even though its feature (Phase 15) only writes the EF/UI on top.

Output: 2 migration files. Both are pure DDL with RLS — autonomous (no PL/pgSQL $$ blocks, so no
42601 trap; applied via standard db push, but the executor still runs the LGPD-02 SQL audit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pipeline-backbone-schema/06-PATTERNS.md
@.planning/phases/06-pipeline-backbone-schema/06-RESEARCH.md
@.planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md
@supabase/migrations/20260425000003_submit_candidatura_rpc.sql

<interfaces>
- Decisao enum (Claude's discretion name): `public.decisao_final_resultado AS ENUM ('aprovado','rejeitado','em_espera')`
- decisao_final FKs: candidatura_id → public.candidaturas(id) UNIQUE; por_usuario → auth.users(id)
- Role values for RLS reads: 'rh', 'administrador', 'candidato' (NOT 'admin'); auth FK column user_id (NOT auth_user_id)
- Live RLS idiom (Shared Pattern A): (select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador')
- bias_audit_log: schema-only — empty table + admin-only SELECT RLS; snapshot logic deferred (Assumption A4)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author decisao_final migration with structural guardrail</name>
  <files>supabase/migrations/20260607000003_decisao_final.sql</files>
  <read_first>
    - .planning/phases/06-pipeline-backbone-schema/06-RESEARCH.md §"Pattern 4: decisao_final structural guardrail" (the exact column list + WITH CHECK false policy)
    - .planning/phases/06-pipeline-backbone-schema/06-PATTERNS.md §"..._decisao_final.sql" (load-bearing constraints) + §"Shared Pattern A" (RLS idiom) + §"Shared Pattern C" (EF-write boundary rationale — decisao_final is EF-only, NOT a SECURITY DEFINER client RPC)
    - .planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md §F (the LGPD-02 audit this table must satisfy)
  </read_first>
  <action>
    Create `public.decisao_final_resultado AS ENUM ('aprovado','rejeitado','em_espera')`, then
    `public.decisao_final` with columns:
    `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`,
    `candidatura_id uuid NOT NULL UNIQUE REFERENCES public.candidaturas(id)`,
    `decisao public.decisao_final_resultado NOT NULL`,
    `justificativa text NOT NULL CHECK (length(justificativa) >= 50)`,
    `por_usuario uuid NOT NULL REFERENCES auth.users(id)`  -- NEVER null = LGPD-02 guardrail,
    `em timestamptz NOT NULL DEFAULT now()`,
    plus the LGPD Art. 20 forward-looking columns (Phase 15 writes them): `explicacao_solicitada_em timestamptz`, `revisao_solicitada_em timestamptz`, `revisao_resultado text`.
    Enable RLS. Add policies:
    - `FOR INSERT WITH CHECK (false)` — blocks ALL client INSERT (D-08; only EF service_role bypasses RLS).
    - `FOR SELECT USING ((select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador'))` — RH/admin read (Shared Pattern A).
    - candidato reads own decision via `candidatura_id IN (SELECT id FROM public.candidaturas WHERE candidato_id IN (SELECT id FROM public.candidatos WHERE user_id = (select auth.uid())))` (for the LGPD Art. 20 /candidato/explicacao endpoint in Phase 15).
    Do NOT use 'admin' or auth_user_id (PRD §8.3 is stale). Shared Pattern B header; no BEGIN/COMMIT wrapper.
    Add COMMENT noting por_usuario NOT NULL is the LGPD-02 structural guardrail (success criterion #5).
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260607000003_decisao_final.sql > /tmp/df.sql; grep -q "por_usuario uuid NOT NULL REFERENCES auth.users" /tmp/df.sql && grep -q "CHECK (length(justificativa) >= 50)" /tmp/df.sql && grep -q "WITH CHECK (false)" /tmp/df.sql && grep -qi "'aprovado'.*'rejeitado'.*'em_espera'\|em_espera" /tmp/df.sql && ! grep -q "'admin'\|auth_user_id" /tmp/df.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `por_usuario uuid NOT NULL REFERENCES auth.users(id)` present (the guardrail).
    - `justificativa text NOT NULL CHECK (length(justificativa) >= 50)` present.
    - decisao enum has exactly aprovado/rejeitado/em_espera.
    - Client INSERT policy is `WITH CHECK (false)`.
    - Zero occurrences of `'admin'` or `auth_user_id` (corrected identifiers only).
    - RLS enabled; RH/admin SELECT uses `(select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador')`.
  </acceptance_criteria>
  <done>decisao_final created complete with the LGPD-02 structural guardrail and EF-only INSERT.</done>
</task>

<task type="auto">
  <name>Task 2: Author bias_audit_log migration (schema-only)</name>
  <files>supabase/migrations/20260607000004_bias_audit_log.sql</files>
  <read_first>
    - .planning/phases/06-pipeline-backbone-schema/06-PATTERNS.md §"..._bias_audit_log.sql" (schema-only + admin-only RLS) + §"Shared Pattern A"
    - .planning/phases/06-pipeline-backbone-schema/06-RESEARCH.md §"Assumptions Log" A4 (snapshot job is Phase 15, create empty table + RLS only)
  </read_first>
  <action>
    Create `public.bias_audit_log` as a schema-only table for the future monthly selection-rate snapshot
    (LGPD-03, Phase 15 — NOT this phase). Minimal forward-compatible columns:
    `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`,
    `snapshot_em timestamptz NOT NULL DEFAULT now()`,
    `periodo text` (e.g. month label),
    `dados jsonb NOT NULL DEFAULT '{}'::jsonb` (selection-rate breakdown by raça/gênero/idade — EEOC 4/5 rule),
    `criado_em timestamptz NOT NULL DEFAULT now()`.
    Enable RLS. Add admin-only SELECT policy: `FOR SELECT USING ((select auth.jwt() #>> '{app_metadata,role}') = 'administrador')` (Shared Pattern A — bias audit is administrador-only, not rh). No INSERT policy
    this phase (snapshot job in Phase 15 writes via EF service_role). Shared Pattern B header; no BEGIN/COMMIT.
    Add COMMENT noting the snapshot job (LGPD-03) is deferred to Phase 15 — this is the schema placeholder only.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260607000004_bias_audit_log.sql > /tmp/ba.sql; grep -q "ENABLE ROW LEVEL SECURITY" /tmp/ba.sql && grep -q "= 'administrador'" /tmp/ba.sql && ! grep -q "= 'admin'" /tmp/ba.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Table created with id, snapshot_em, dados jsonb, criado_em.
    - RLS enabled; SELECT policy restricted to `= 'administrador'` (not 'admin', not rh).
    - No INSERT policy (deferred to Phase 15); COMMENT documents the deferral.
  </acceptance_criteria>
  <done>bias_audit_log schema placeholder created with admin-only read RLS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → decisao_final | client (RH/candidato via supabase-js) attempts INSERT/SELECT — must be blocked for INSERT, role-gated for SELECT |
| EF (service_role) → decisao_final | the only legitimate write path (Phase 15 EF); service_role bypasses RLS by design |
| client → bias_audit_log | only administrador may read; nobody writes from client this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Repudiation / Compliance (LGPD) | A decision persisted with no human actor | mitigate | `por_usuario uuid NOT NULL` makes a NULL-actor decision structurally unrepresentable; LGPD-02 audit `SELECT count(*) WHERE por_usuario IS NULL` → 0 proves it. |
| T-06-07 | Elevation of Privilege | Client INSERTs a self-favorable decision directly | mitigate | RLS client INSERT `WITH CHECK (false)` blocks ALL client writes; only EF service_role (Phase 15) writes. |
| T-06-08 | Tampering | Decision written with an empty/trivial justificativa to dodge accountability | mitigate | `CHECK (length(justificativa) >= 50)` enforces a substantive rationale structurally. |
| T-06-09 | Information Disclosure | A candidato reads another candidato's / a competitor's bias-audit data | mitigate | bias_audit_log SELECT restricted to `= 'administrador'`; decisao_final candidato SELECT scoped to own candidatura via user_id join. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure DDL + RLS; nothing to audit. |
</threat_model>

<verification>
- Both migration files pass structural grep gates (no 'admin', no auth_user_id).
- After apply (db push or, if 42601 surfaces, the SQL-Editor workaround), run runbook §F LGPD-02 audit → 0.
- pg_tables.rowsecurity = true for both new tables.
</verification>

<success_criteria>
- ROADMAP success criterion #5: SQL audit confirms no decision can persist with por_usuario IS NULL.
- decisao_final is complete now so Phase 15 only adds the EF/UI on top.
</success_criteria>

<output>
Create `.planning/phases/06-pipeline-backbone-schema/06-03-SUMMARY.md` when done.
</output>
