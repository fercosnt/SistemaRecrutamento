---
phase: 06-pipeline-backbone-schema
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - supabase/migrations/20260607000001_historico_candidatura.sql
  - supabase/migrations/20260607000002_etapa_processo_v2_cutover.sql
  - src/types/database.types.ts
autonomous: false
requirements: [FUNIL-01, FUNIL-03]
must_haves:
  truths:
    - "The legacy 10-value etapa_processo enum is replaced by the v2 8-value enum (6 stages + 2 terminals) declared in pipeline order"
    - "Every live candidaturas row maps to a valid v2 value with zero data loss (pre/post count match)"
    - "A defensive backup of candidaturas exists in schema backup_m2 before the legacy type is dropped"
    - "The candidaturas table has an etapa_justificativa companion column for the trigger to read"
    - "historico_candidatura exists with etapa_de/etapa_para enum cols, ator FK to auth.users, auto_rejeitado boolean"
    - "Decisions covered — D-01: backbone-only scope (enum v2 + historico table this phase; feature tables deferred to 7-15); D-03: in-place ALTER COLUMN ... USING (no table rebuild); D-04: defensive backup_m2.candidaturas_pre_funil snapshot before drop; D-05: live-row mapping (triagem/aprovado/rejeitado identity + orphan→triagem collapse with audit log); D-07: etapa_justificativa companion column added for the trigger to read"
  artifacts:
    - path: "supabase/migrations/20260607000001_historico_candidatura.sql"
      provides: "Append-only audit table FUNIL-03"
      contains: "historico_candidatura"
    - path: "supabase/migrations/20260607000002_etapa_processo_v2_cutover.sql"
      provides: "Backup + v2 enum + in-place ALTER USING cutover + companion column"
      contains: "etapa_processo_v2"
  key_links:
    - from: "cutover USING CASE"
      to: "live candidaturas.etapa_atual rows"
      via: "ALTER COLUMN ... TYPE ... USING (CASE ...)"
      pattern: "ALTER COLUMN etapa_atual TYPE"
    - from: "cutover rename step"
      to: "submit_candidatura_atomic RPC cast 'triagem'::public.etapa_processo"
      via: "RENAME TO etapa_processo restores canonical name so RPC keeps compiling"
      pattern: "RENAME TO etapa_processo"
---

<objective>
Author and apply the foundational schema cutover: create the append-only `historico_candidatura`
audit table (FUNIL-03), then perform the in-place live-data enum cutover (FUNIL-01) — defensive
backup → v2 enum in pipeline order → ALTER COLUMN USING CASE → re-default → drop legacy → rename
back to canonical `etapa_processo` → add the `etapa_justificativa` companion column.

Purpose: This is the structural foundation of the entire M2 pipeline. The enum encodes the 6
stages + 2 terminals as the DB source of truth, declared in pipeline order so the trigger (06-04)
can compute regression with native `<`/`>`. historico_candidatura is created FIRST so any orphan
rows collapsed during cutover (D-05) have an audit target.

Output: 2 migration files + regenerated database.types.ts. This plan mutates LIVE production data,
so the cutover apply is a human-action checkpoint (42601 workaround, autonomous:false).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pipeline-backbone-schema/06-PATTERNS.md
@.planning/phases/06-pipeline-backbone-schema/06-RESEARCH.md
@.planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md
@CLAUDE.md
@supabase/migrations/20260606000001_vaga_status_sync.sql
@supabase/migrations/20260425000003_submit_candidatura_rpc.sql

<interfaces>
Live facts VERIFIED against database.types.ts + migrations (use these exactly — do NOT copy PRD §8.3):
- Legacy `etapa_processo` (10 values): triagem, bigfive, disc, entrevista_online, raven, cultura, entrevista_presencial, aprovado, rejeitado, avaliacao_final
- v2 enum (8 values, PIPELINE ORDER): inscricao, triagem, avaliacao_assincrona, entrevista_online, entrevista_presencial, decisao_final, aprovado, rejeitado
- Name-identical survivors: triagem, entrevista_online, entrevista_presencial, aprovado, rejeitado
- Orphans collapse to triagem (D-05): bigfive, disc, raven, cultura, avaliacao_final
- `candidaturas` has NO etapa_justificativa column yet (confirmed absent) — this plan adds it
- `submit_candidatura_atomic` RPC casts 'triagem'::public.etapa_processo — keep canonical name so it needs no edit
- Backup schema: backup_m2 (Claude's discretion per D-04); companion column: etapa_justificativa text (per RESEARCH Open Q1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author historico_candidatura migration</name>
  <files>supabase/migrations/20260607000001_historico_candidatura.sql</files>
  <read_first>
    - supabase/migrations/20260606000001_vaga_status_sync.sql (migration header + 42601-note convention — Shared Pattern B; this file has NO PL/pgSQL so no 42601 note strictly needed, but follow the header skeleton)
    - .planning/phases/06-pipeline-backbone-schema/06-PATTERNS.md §"..._historico_candidatura.sql" (the verified legacy historico_acoes contrast + the columns the new table needs)
    - .planning/phases/06-pipeline-backbone-schema/06-RESEARCH.md §"Pitfall 5" (legacy historico_acoes coexistence — do NOT reuse, do NOT FK to usuarios_rh)
  </read_first>
  <action>
    Create `public.historico_candidatura` as an append-only audit table. Columns:
    `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`,
    `candidatura_id uuid NOT NULL REFERENCES public.candidaturas(id)`,
    `etapa_de public.etapa_processo` (NULL-able — first transition / orphan-collapse log has no prior etapa cleanly),
    `etapa_para public.etapa_processo NOT NULL`,
    `criterio_texto text` (the justificativa/criterio copied by the trigger from candidaturas.etapa_justificativa),
    `ator uuid REFERENCES auth.users(id)` (NULL-able per D-09 — auth.uid() for humans, NULL for system/service_role),
    `auto_rejeitado boolean NOT NULL DEFAULT false`,
    `criado_em timestamptz NOT NULL DEFAULT now()`.
    NOTE: reference `public.etapa_processo` — at apply time this table is created in the SAME SQL-Editor
    session AFTER the cutover renames v2 back to etapa_processo (see Task 3 apply ordering), OR sequence the
    file so it applies after the cutover. To keep the migration self-contained and ordering-safe, the executor
    applies the cutover (Task 2 file) FIRST in the SQL Editor session, then this table file — both in one session.
    Add index `(candidatura_id, criado_em)` (RESEARCH discretion). Add `COMMENT ON TABLE public.historico_candidatura`
    explaining it coexists with legacy `historico_acoes` (which FKs usuarios_rh.id, Figma-Make era) and is NOT the
    same table — the new one FKs auth.users and carries etapa_de/etapa_para + auto_rejeitado. Enable RLS on the
    table (`ALTER TABLE ... ENABLE ROW LEVEL SECURITY`) but do NOT add policies here — policies land in 06-05.
    Use the Shared Pattern B header. No BEGIN/COMMIT wrapper.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260607000001_historico_candidatura.sql | grep -c "auto_rejeitado boolean NOT NULL\|ator uuid REFERENCES auth.users\|etapa_para public.etapa_processo NOT NULL" | grep -qv '^0$' && grep -q "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260607000001_historico_candidatura.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Table has etapa_de/etapa_para typed `public.etapa_processo`, `ator uuid REFERENCES auth.users(id)` (NOT usuarios_rh), `auto_rejeitado boolean NOT NULL DEFAULT false`.
    - `ENABLE ROW LEVEL SECURITY` present; no CREATE POLICY in this file (deferred to 06-05).
    - COMMENT ON TABLE documents coexistence with legacy historico_acoes.
    - No BEGIN/COMMIT wrapper; Shared Pattern B header present.
  </acceptance_criteria>
  <done>historico_candidatura migration authored with correct FK target and audit columns.</done>
</task>

<task type="auto">
  <name>Task 2: Author etapa_processo v2 cutover migration (backup + enum + ALTER USING + companion column)</name>
  <files>supabase/migrations/20260607000002_etapa_processo_v2_cutover.sql</files>
  <read_first>
    - .planning/phases/06-pipeline-backbone-schema/06-RESEARCH.md §"Pattern 1: In-place enum cutover with a DEFAULT" (the exact DROP DEFAULT → CREATE TYPE → ALTER USING CASE → SET DEFAULT → DROP → RENAME sequence) and §"Pitfall 3" (DEFAULT must be dropped first) and §"Pitfall 6" (legacy values differ)
    - .planning/phases/06-pipeline-backbone-schema/06-PATTERNS.md §"..._etapa_processo_v2_cutover.sql" (PRECONDITION+SMOKE comment convention from the vaga_status_sync analog; verified live enum facts; keep canonical name; companion column ADD)
    - supabase/migrations/20260606000001_vaga_status_sync.sql (the 42601 inline NOTE block to copy verbatim — Shared Pattern B; PRECONDITION/SMOKE comment structure lines 32-43)
    - .planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md §discovery (the executor runs this BEFORE finalizing the USING CASE)
  </read_first>
  <action>
    Author the one-time cutover migration WITH the 42601 inline NOTE block (this file has no PL/pgSQL
    function but DOES run as a single SQL-Editor session per D-22; include the NOTE + PRECONDITION/SMOKE
    comment headers). Sequence EXACTLY (RESEARCH §Pattern 1):
    (0) `CREATE SCHEMA IF NOT EXISTS backup_m2;` then `CREATE TABLE backup_m2.candidaturas_pre_funil AS SELECT * FROM public.candidaturas;` (D-04 defensive backup BEFORE any drop).
    (1) `CREATE TYPE public.etapa_processo_v2 AS ENUM (...)` with the 8 values in PIPELINE ORDER: inscricao, triagem, avaliacao_assincrona, entrevista_online, entrevista_presencial, decisao_final, aprovado, rejeitado.
    (2) `ALTER TABLE public.candidaturas ALTER COLUMN etapa_atual DROP DEFAULT;` (Pitfall 3 — must precede the type change).
    (3) `ALTER TABLE public.candidaturas ALTER COLUMN etapa_atual TYPE public.etapa_processo_v2 USING (CASE etapa_atual::text WHEN 'triagem' THEN 'triagem' WHEN 'aprovado' THEN 'aprovado' WHEN 'rejeitado' THEN 'rejeitado' WHEN 'entrevista_online' THEN 'entrevista_online' WHEN 'entrevista_presencial' THEN 'entrevista_presencial' WHEN 'bigfive' THEN 'triagem' WHEN 'disc' THEN 'triagem' WHEN 'raven' THEN 'triagem' WHEN 'cultura' THEN 'triagem' WHEN 'avaliacao_final' THEN 'triagem' ELSE 'triagem' END::public.etapa_processo_v2);` — orphans collapse to triagem (D-05).
    (4) `ALTER TABLE public.candidaturas ALTER COLUMN etapa_atual SET DEFAULT 'triagem';` (new type).
    (5) `DROP TYPE public.etapa_processo;` then `ALTER TYPE public.etapa_processo_v2 RENAME TO etapa_processo;` — restores the CANONICAL name so submit_candidatura_atomic's 'triagem'::public.etapa_processo cast keeps compiling unedited (Assumption A2 / Shared Pattern D).
    (6) `ALTER TABLE public.candidaturas ADD COLUMN IF NOT EXISTS etapa_justificativa text;` (D-07 companion column the trigger reads in 06-04).
    Add a PRECONDITION comment = the discovery query, and a SMOKE comment = post-cutover GROUP BY count-match
    (from the runbook). No BEGIN/COMMIT wrapper. Add inline note: if discovery showed orphan rows, after the
    historico_candidatura table exists (Task 1, same session) INSERT one audit line per collapsed orphan
    (etapa_de=<legacy>, etapa_para='triagem', criterio_texto='colapso de valor legado órfão (D-05)', ator=NULL, auto_rejeitado=true).
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260607000002_etapa_processo_v2_cutover.sql > /tmp/cut.sql; grep -q "backup_m2.candidaturas_pre_funil" /tmp/cut.sql && grep -q "DROP DEFAULT" /tmp/cut.sql && grep -q "RENAME TO etapa_processo" /tmp/cut.sql && grep -q "ADD COLUMN IF NOT EXISTS etapa_justificativa text" /tmp/cut.sql && ! grep -qi "^BEGIN;\|^COMMIT;" /tmp/cut.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Backup (`backup_m2.candidaturas_pre_funil AS SELECT * FROM public.candidaturas`) appears BEFORE any DROP.
    - v2 enum declares all 8 values in pipeline order (inscricao first, terminals aprovado/rejeitado last).
    - DROP DEFAULT precedes ALTER COLUMN TYPE; SET DEFAULT follows it; RENAME restores `etapa_processo`.
    - `etapa_justificativa text` companion column added.
    - No `BEGIN;`/`COMMIT;` wrapper; 42601 NOTE + PRECONDITION/SMOKE comments present.
  </acceptance_criteria>
  <done>Cutover migration authored with the exact DEFAULT-safe sequence, backup, and companion column.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: [BLOCKING] Apply cutover + historico table via SQL Editor (42601 workaround) + regenerate types</name>
  <files>src/types/database.types.ts</files>
  <read_first>
    - CLAUDE.md §Commands (the SQLSTATE 42601 workaround: SQL Editor → migration repair --status applied <version> → db push verify; remove BEGIN/COMMIT)
    - .planning/phases/06-pipeline-backbone-schema/06-SQL-SMOKE-RUNBOOK.md (discovery section §A run FIRST; FUNIL-01 audit §B run AFTER)
    - .planning/STATE.md Phase 4 ledger 04-01 (the established human-action db-push checkpoint shape)
  </read_first>
  <action>Human-action checkpoint: apply the two authored migrations (cutover 20260607000002 + historico_candidatura 20260607000001) to the LIVE DB via the SQLSTATE 42601 SQL-Editor workaround, run the FUNIL-01 discovery + post-cutover smoke, reconcile with migration repair, confirm db push is up to date, and regenerate database.types.ts. Detailed steps in <how-to-verify>.</action>
  <what-built>
    Two migration files: 20260607000002 (live enum cutover, mutates production candidaturas) and
    20260607000001 (historico_candidatura audit table). These touch LIVE production data and contain
    DDL that hits the SQLSTATE 42601 transaction-pooler trap — they MUST be applied manually via the
    Supabase SQL Editor, not via `supabase db push`.
  </what-built>
  <how-to-verify>
    Fernando runs in his terminal / Supabase SQL Editor (project isljnozzlvckrgjjbjwp):
    1. Run the DISCOVERY queries (runbook §A) FIRST. Capture etapa_atual distribution + row count + confirm
       the 3 new tables are absent + confirm current column_default. Paste results into the runbook §A result block.
    2. If discovery shows orphan etapa values, note them — they will collapse to triagem and need audit lines.
    3. In ONE SQL Editor session: paste + run the cutover SQL (20260607000002) THEN the historico_candidatura
       SQL (20260607000001). If orphans existed, run the orphan-audit INSERTs after the table exists.
    4. Run the FUNIL-01 post-cutover SMOKE (runbook §B): `SELECT etapa_atual, count(*) FROM public.candidaturas GROUP BY 1 ORDER BY 2 DESC;` — total MUST equal the discovery total (zero data loss).
    5. Reconcile local migration state: `supabase migration repair --status applied 20260607000002` and `... 20260607000001`.
    6. Confirm `supabase db push --linked` reports "Remote database is up to date".
    7. Regenerate types: `npm run db:types` (writes src/types/database.types.ts — never hand-edit). Confirm the new
       etapa_processo enum has 8 values and candidaturas now has etapa_justificativa.
    Paste the SMOKE result + db push confirmation into the runbook before resuming.
  </how-to-verify>
  <acceptance_criteria>
    - Discovery result captured in runbook §A; post-cutover GROUP BY total equals discovery total (no data loss).
    - `supabase db push --linked` reports "Remote database is up to date".
    - Regenerated database.types.ts shows etapa_processo with 8 values + candidaturas.etapa_justificativa present.
    - If orphans existed, each has a historico_candidatura row with auto_rejeitado=true and ator IS NULL.
  </acceptance_criteria>
  <verify>
    <automated>npx supabase db push --linked 2>&1 | grep -qi "up to date" && echo OK</automated>
  </verify>
  <done>Cutover applied with zero data loss (post-cutover count = discovery count); db push reports up to date; database.types.ts regenerated with the 8-value enum + etapa_justificativa column.</done>
  <resume-signal>Type "applied" with the captured GROUP BY count-match + db push confirmation, or describe the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration apply → live production DB | DDL mutates live candidaturas.etapa_atual (untrusted partial-apply could corrupt state) |
| client → candidaturas (post-cutover) | future RH/candidato access — RLS deferred to 06-05; this plan creates the column the trigger guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Migration partial-apply / 42601 prepared-statement error mid-cutover | mitigate | Single-session SQL-Editor apply (D-22 workaround); defensive backup_m2.candidaturas_pre_funil snapshot taken BEFORE any DROP enables full restore; post-cutover count-match smoke gates completion. |
| T-06-02 | Tampering | Data loss — a live row maps to no valid v2 value | mitigate | USING CASE has an `ELSE 'triagem'` catch-all (D-05); pre/post GROUP BY count-match audit (FUNIL-01) proves zero loss. |
| T-06-03 | Repudiation | Orphan-collapse happens silently with no audit trail | mitigate | Each collapsed orphan gets a historico_candidatura row (auto_rejeitado=true, ator NULL) per D-05/D-09. |
| T-06-04 | Tampering | DEFAULT not dropped first → ALTER TYPE fails (Pitfall 3) | mitigate | Sequence enforces DROP DEFAULT before ALTER COLUMN TYPE; acceptance grep checks ordering. |
| T-06-05 | Denial of Service | Diverging final type name breaks submit_candidatura_atomic RPC | mitigate | RENAME restores canonical `etapa_processo`; acceptance grep `RENAME TO etapa_processo` (Assumption A2). |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — pure DDL + generated types; nothing to audit. |
</threat_model>

<verification>
- Both migration files pass their structural grep gates.
- Post-cutover FUNIL-01 smoke (runbook §B): row count matches discovery total.
- `supabase db push --linked` → "Remote database is up to date".
- Regenerated database.types.ts encodes the 8-value enum + etapa_justificativa column.
</verification>

<success_criteria>
- ROADMAP success criterion #1: legacy enum deprecated with backup; candidaturas exist with the new 6+2 enum.
- Companion column + historico table foundation laid for the trigger (06-04).
</success_criteria>

<output>
Create `.planning/phases/06-pipeline-backbone-schema/06-02-SUMMARY.md` when done.
</output>
