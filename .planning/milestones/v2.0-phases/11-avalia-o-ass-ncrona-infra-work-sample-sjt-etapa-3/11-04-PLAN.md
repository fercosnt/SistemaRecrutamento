---
phase: 11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3
plan: 04
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - database.types.ts
autonomous: false

requirements: [AVAL-02, AVAL-03, AVAL-09]

must_haves:
  truths:
    - "All 4 Phase-11 migrations are applied to live PROD (scores_candidato, perguntas+SJT seed, respostas_avaliacao, pontuar_sjt RPC) — version rows reconciled so db push reports 'up to date'"
    - "The work_sample_sjt prompt row is flipped is_active=false → true in PROD so loadPrompt resolves it (else the EF throws PromptNotConfiguredError) — the one-time manual activation per Phase-9/10 precedent"
    - "avaliar-redacao EF is deployed with JWT verify ON (NOT --no-verify-jwt — it is candidate-invoked), reusing the existing Phase-10 EF secrets"
    - "database.types.ts at repo ROOT is regenerated (3 new tables + tipo_score/status_score enums + pontuar_sjt) and committed"
    - "All SQL smokes from 11-VALIDATION.md pass against a live fixture candidatura (Σ peso, threshold→pendente_humano, non-owner 42501, wrong-etapa 42501, never-auto-reject, etapa-gate RLS in+out, candidato-DENY scores, idempotent upsert)"
  artifacts:
    - path: "database.types.ts"
      provides: "regenerated types with the 3 new tables + 2 enums + RPC"
      contains: "scores_candidato"
  key_links:
    - from: "PROD prompt_versions"
      to: "loadPrompt('work_sample_sjt')"
      via: "is_active=true flip"
      pattern: "work_sample_sjt"
---

<objective>
Apply the Phase-11 backend to live PROD: push the 4 migrations (handling the D-22 42601 risk on the PL/pgSQL RPC), flip the `work_sample_sjt` prompt active, deploy the `avaliar-redacao` EF JWT-ON, regenerate `database.types.ts`, and run all SQL smokes against a real fixture candidatura. This is a **[BLOCKING] non-autonomous** plan — the orchestrator applies it via the established Phase-10 Supabase MCP `apply_migration` path (records version rows, bypasses 42601) or `db push --linked`.

Purpose: Make the deterministic + AI scoring live and verified, with the never-auto-reject invariant proven by SQL smoke, before any UI exercises it.
Output: Live tables/RPC/EF/active-prompt + regenerated types + passing SQL smokes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-VALIDATION.md
@.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- The exact runtime steps. Precedent: Phase 9 Plan 09-07 + Phase 10 Plan 10-04. -->

D-22 push path (CLAUDE.md §Migrations):
- Table-only DDL (scores_candidato, perguntas, respostas_avaliacao) usually pushes clean via `supabase db push --linked`.
- The PL/pgSQL RPC (pontuar_sjt — $$ body + adjacent REVOKE/GRANT) is the 42601-risk file → apply via Supabase MCP `apply_migration`/`execute_sql` (records version row, no 42601) OR SQL-Editor manual + `supabase migration repair --status applied <version>`.

Prompt activation (seeded inactive — 20260609000004_prompt_library_seed.sql):
- `UPDATE public.prompt_versions SET is_active = true WHERE call_type = 'work_sample_sjt' AND semver = '1.0.0';` — one-time manual SQL (orchestrator-decision #2 / RESEARCH A8). loadPrompt reads is_active=true only.

EF deploy:
- `supabase functions deploy avaliar-redacao` (JWT verify ON — default; do NOT pass --no-verify-jwt; it is candidate-invoked, the gateway must verify the JWT). Reuses Phase-10 secrets (ANTHROPIC_API_KEY/OPENAI_API_KEY/SUPABASE_SERVICE_ROLE_KEY).

Types regen:
- `npm run db:types` → database.types.ts at repo ROOT (NOT src/). Commit it.

SQL smokes (from 11-VALIDATION.md):
- Use `set_config('request.jwt.claims', '{"sub":"<uuid>","app_metadata":{"role":"candidato"}}', true)` to simulate candidato/RH roles in a fixture, ROLLBACK-free cleanup.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Apply 4 migrations + flip work_sample_sjt active + deploy avaliar-redacao EF</name>
  <files>supabase/migrations/20260611000001_scores_candidato.sql, supabase/migrations/20260611000002_perguntas_sjt.sql, supabase/migrations/20260611000003_respostas_avaliacao.sql, supabase/migrations/20260611000004_pontuar_sjt_rpc.sql</files>
  <what-built>
    The 4 Phase-11 migrations (authored in Plan 11-02), the prompt activation SQL, and the avaliar-redacao EF deploy. The orchestrator applies these to live PROD via the Supabase MCP `apply_migration` path (Phase-10 precedent) — table-only DDL via db push, the pontuar_sjt RPC via MCP `apply_migration`/`execute_sql` to bypass the 42601 driver bug (D-22). Then the one-time `UPDATE prompt_versions SET is_active=true WHERE call_type='work_sample_sjt'`. Then `supabase functions deploy avaliar-redacao` JWT-ON.
  </what-built>
  <action>
    The orchestrator (not the autonomous executor) performs the live-infra apply, since this touches PROD: (1) apply the 3 table-only migrations via `supabase db push --linked`; (2) apply `20260611000004_pontuar_sjt_rpc.sql` via Supabase MCP `apply_migration`/`execute_sql` (bypasses the 42601 driver bug — D-22) and reconcile the version row so `db push` reports up-to-date; (3) run the one-time `UPDATE public.prompt_versions SET is_active=true WHERE call_type='work_sample_sjt' AND semver='1.0.0';`; (4) `supabase functions deploy avaliar-redacao` with JWT verify ON (no `--no-verify-jwt`), reusing the existing Phase-10 EF secrets. Then run the how-to-verify checks below before resuming.
  </action>
  <how-to-verify>
    1. Confirm the 4 migrations applied: `supabase db push --linked` reports "Remote database is up to date" (version rows reconciled).
    2. Confirm `scores_candidato`, `perguntas`, `respostas_avaliacao` tables + `tipo_score`/`status_score` enums + `pontuar_sjt(uuid,jsonb)` function exist in PROD (via `\d` / information_schema query).
    3. Confirm `SELECT is_active FROM prompt_versions WHERE call_type='work_sample_sjt'` returns `true`.
    4. Confirm `avaliar-redacao` shows ACTIVE in `supabase functions list`; an anon curl returns 401 (verify_jwt ON behavioral evidence — JWT-ON, not --no-verify-jwt).
    5. The dentista SJT seed rows exist: `SELECT count(*) FROM perguntas WHERE cargo='dentista'` ≥ 4 (3 MC + 1 case); ≥1 row for each of the other 7 cargos.
  </how-to-verify>
  <verify>Orchestrator confirms all 5 how-to-verify checks pass before resume.</verify>
  <done>The 4 migrations are live, work_sample_sjt is_active=true, the EF is deployed JWT-ON, and all 5 checks pass.</done>
  <resume-signal>Type "applied" once all 5 checks pass, or describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Regenerate database.types.ts + run all SQL smokes</name>
  <files>database.types.ts</files>
  <read_first>
    - .planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-VALIDATION.md (the SMOKE-N runbook)
    - supabase/migrations/20260611000004_pontuar_sjt_rpc.sql (the RPC under test)
    - CLAUDE.md §Commands (db:types regen)
  </read_first>
  <action>
    Run `npm run db:types` to regenerate `database.types.ts` at repo ROOT — it must now carry `scores_candidato`/`perguntas`/`respostas_avaliacao` row types + the `tipo_score`/`status_score` enums + the `pontuar_sjt` function signature. Commit it (`git -c core.hooksPath=/dev/null`). Then execute every SMOKE-N from 11-VALIDATION.md against a fixture candidatura at `etapa_atual='avaliacao_assincrona'` using `set_config('request.jwt.claims', ...)` to simulate candidato/RH roles (ROLLBACK-free cleanup, Phase-7/8 precedent): SMOKE-1 Σ peso correctness; SMOKE-2 `<mc_min_pct` OR atencao → `pendente_humano`; SMOKE-3 non-owner → 42501; SMOKE-4 wrong-etapa → 42501; SMOKE-5 after a failing score, `candidaturas.etapa_atual` UNCHANGED + zero new `historico_candidatura` row (RNF-07a); SMOKE-6 etapa-gate RLS: autosave OK in-etapa, DENIED post-advance; SMOKE-7 candidato SELECT scores_candidato → 0 rows, RH → rows; SMOKE-8 re-run pontuar_sjt UPDATEs the single MC row (no duplicate). Record each PASS/FAIL in the SUMMARY.
  </action>
  <verify>
    <automated>npm run db:types && grep -q "scores_candidato" database.types.ts && grep -q "tipo_score" database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `database.types.ts` (repo ROOT) contains `scores_candidato`, `perguntas`, `respostas_avaliacao`, `tipo_score`, `status_score`, and `pontuar_sjt`.
    - All 8 SMOKE-N pass; SMOKE-5 explicitly proves no etapa change and no historico row after a deliberately-failing score (RNF-07a).
    - SMOKE-3/4 prove 42501 on non-owner and wrong-etapa; SMOKE-7 proves candidato-DENY on scores_candidato.
  </acceptance_criteria>
  <done>Types regenerated + committed; all SQL smokes pass against live PROD; the never-auto-reject invariant is proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migration files → PROD DB | privileged apply; the 42601 driver bug can corrupt a partial apply |
| candidate JWT → deployed avaliar-redacao | gateway JWT verify must be ON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering | PL/pgSQL RPC apply (42601) | mitigate | Apply pontuar_sjt via Supabase MCP `apply_migration`/`execute_sql` (no 42601) + `migration repair --status applied`; verify `db push` reports up-to-date |
| T-11-04-02 | Elevation | avaliar-redacao deploy mode | mitigate | Deploy JWT-ON (no --no-verify-jwt); anon curl 401 confirms the gateway verifies the candidate JWT |
| T-11-04-03 | Info/compliance | live scoring | mitigate | SQL smokes prove candidato-DENY on scores_candidato (SMOKE-7) + never-auto-reject (SMOKE-5) before any UI uses the path |
| T-11-04-SC | Tampering | EF supply chain | mitigate | Deploy bundles the existing audited _shared deps; legitimacy checkpoint is the blocking-human apply gate (Task 1) |
</threat_model>

<verification>
- db push reports up-to-date; tables/enums/RPC live; work_sample_sjt is_active=true; EF ACTIVE JWT-ON.
- database.types.ts regenerated + committed.
- All 8 SMOKE-N pass.
</verification>

<success_criteria>
The Phase-11 backend is live and SQL-smoke-verified, with the never-auto-reject and candidato-DENY invariants proven, before the candidate UI exercises it.
</success_criteria>

<output>
Create `.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-04-SUMMARY.md` when done.
</output>
