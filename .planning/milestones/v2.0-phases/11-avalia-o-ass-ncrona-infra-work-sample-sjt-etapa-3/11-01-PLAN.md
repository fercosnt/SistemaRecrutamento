---
phase: 11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/avaliacao/components/__tests__/AvaliacaoContainer.test.tsx
  - src/features/avaliacao/hooks/__tests__/useAutosaveAvaliacao.test.ts
  - src/features/config-vaga/schemas/__tests__/testesAplicaveisSchema.test.ts
  - supabase/functions/avaliar-redacao/__tests__/index.test.ts
  - src/__tests__/guards/forbidden-strings.grep.test.ts
  - .planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-VALIDATION.md
autonomous: true
requirements: [AVAL-01, AVAL-02, AVAL-03, AVAL-09]

must_haves:
  truths:
    - "Every Phase-11 surface (container, autosave hook, testesAplicaveis SJT keys, avaliar-redacao EF) has a calibrated RED test that FAILS pre-implementation (smoke-runtime gate)"
    - "The deno EF test asserts the C1 authz cases (401 no-session, 403 non-owner, 403 wrong-etapa) before the EF exists — per D-22/C1 lesson [[reference_ef_authenticate_vs_authorize]]"
    - "The SQL-smoke runbook in 11-VALIDATION.md enumerates pontuar_sjt scoring/threshold/authz + etapa-gate RLS + scores_candidato candidato-DENY + never-auto-reject (RNF-07a) smokes"
    - "The LGPD-04 grep guard scans the new src/features/avaliacao/ paths AND the SJT seed migration text (SCAN_ROOTS extended to supabase/migrations)"
  artifacts:
    - path: "supabase/functions/avaliar-redacao/__tests__/index.test.ts"
      provides: "RED deno test: module-not-found + 401/403 authz cases (clone comparativo C1)"
      contains: "403"
    - path: "src/features/avaliacao/hooks/__tests__/useAutosaveAvaliacao.test.ts"
      provides: "RED vitest: 30s debounce + flush + RLS-42501 back-lock catch"
    - path: "src/features/config-vaga/schemas/__tests__/testesAplicaveisSchema.test.ts"
      provides: "RED case asserting SJT keys (tipo/cargo/itens_ids/bateria_size/threshold.mc_min_pct=60) parse"
      contains: "mc_min_pct"
    - path: ".planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-VALIDATION.md"
      provides: "SQL smoke runbook with task IDs + wave_0_complete flip"
  key_links:
    - from: "src/__tests__/guards/forbidden-strings.grep.test.ts"
      to: "src/features/avaliacao/ + supabase/migrations/ SJT seed"
      via: "SCAN_ROOTS array extension"
      pattern: "SCAN_ROOTS"
---

<objective>
Lay the Wave-0 RED test scaffolds and the SQL-smoke runbook for every Phase-11 surface BEFORE any implementation lands. This is the smoke-runtime gate established in Phase 4.1: each downstream surface (the `avaliar-redacao` EF, the autosave hook, the container, the testesAplicaveis SJT-key extension) gets a calibrated failing test, and the LGPD-04 forbidden-string guard is extended to cover the new feature dir + the SJT seed migration text.

Purpose: Guarantee that the migration/EF/UI plans that follow cannot be marked complete with green gates while the behavior is actually broken (the central Phase-4 lesson).
Output: 4 RED test files + 1 extended grep guard + a fleshed-out 11-VALIDATION.md runbook with concrete task IDs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-RESEARCH.md
@.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-VALIDATION.md

<interfaces>
<!-- Extracted from the live grep guard the executor extends. Use directly — no exploration needed. -->

From src/__tests__/guards/forbidden-strings.grep.test.ts:
- `const SCAN_ROOTS = ['src', 'supabase/functions'] as const` — the two scanned roots (line 48). The SJT seed migration lives in `supabase/migrations/` which is NOT scanned today → add `'supabase/migrations'` to SCAN_ROOTS so the seed copy is covered.
- `const FORBIDDEN = /teste\s+psicol[oó]gico|.../i` — 5 banned terms; do NOT touch the regex.
- The walk uses `readdirSync`/`statSync` over each SCAN_ROOT; adding a root is a one-line change.

From supabase/functions/comparativo-candidatos/__tests__/index.test.ts (the deno analog to clone):
- Tests inject `deps` ({ anthropic, openai, supabaseAdmin, supabaseUser }) into `handler(req, deps)` — NO real SDK/network.
- C1 cases asserted: 401 (no session via supabaseUser.auth.getUser returning null), 403 (role not rh/admin). The candidate clone swaps the 403 role-check for an OWNERSHIP + etapa check.

From src/features/config-vaga/schemas/testesAplicaveisSchema.ts (current shape to extend in Plan 11-03):
- `testeAplicavelSchema = z.object({ teste, obrigatorio, customizado, perguntas? })` — the SJT keys (tipo/cargo/itens_ids/bateria_size/threshold) are NOT there yet → the RED test asserts they parse.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deno RED test for avaliar-redacao + vitest RED stubs (autosave hook, container)</name>
  <files>supabase/functions/avaliar-redacao/__tests__/index.test.ts, src/features/avaliacao/hooks/__tests__/useAutosaveAvaliacao.test.ts, src/features/avaliacao/components/__tests__/AvaliacaoContainer.test.tsx</files>
  <read_first>
    - supabase/functions/comparativo-candidatos/__tests__/index.test.ts (clone target: deps injection, 401/403 cases)
    - supabase/functions/comparativo-candidatos/index.ts:64-119 (errorResponse + handler + two-client authz to mirror)
    - .planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-RESEARCH.md §Validation Architecture (Wave 0 Gaps lines 650-657)
    - src/features/cadastro/hooks/useCadastroDraft.ts (the buffer analog the autosave hook composes)
  </read_first>
  <action>
    Create `supabase/functions/avaliar-redacao/__tests__/index.test.ts` cloning the comparativo deno test: import `handler` + `AvaliarRedacaoDeps` from `../index.ts` (module does not exist yet → RED via module-not-found, which IS the calibrated failure). Assert these deno test cases against the not-yet-built handler: (a) 401 when `supabaseUser.auth.getUser()` returns null/error; (b) 403 when the authenticated user does NOT own the `candidatura_id`; (c) 403 when the user owns it but `etapa_atual !== 'avaliacao_assincrona'`; (d) given a mocked `callAi` returning fixed `dimension_scores` (1-5), the handler maps to a composite 0-25 and routes `<13/25 OR red_flags.length>0` to `status='pendente_humano'`; (e) the handler NEVER writes `candidaturas` (assert the mock supabaseAdmin `.from('candidaturas').update` is never called). Pin `deno.land/std@0.224.0/assert` per Phase-9 precedent. Mocks are dependency-injected — no real SDK import, no network.
    Create `src/features/avaliacao/hooks/__tests__/useAutosaveAvaliacao.test.ts` (vitest, RED via hook-not-found): assert (a) the hook debounces server upsert to a 30s flush window using fake timers; (b) a flush calls the injected upsert mutation exactly once per debounce window; (c) when the upsert rejects with a Supabase error whose code/`status` is `42501` (RLS back-lock), the hook surfaces a `locked` state rather than re-throwing — the neutral back-lock UX (Pitfall 3).
    Create `src/features/avaliacao/components/__tests__/AvaliacaoContainer.test.tsx` (vitest+RTL, RED via component-not-found): assert (a) it renders one card per pending teste from `vaga.testes_aplicaveis`; (b) cards show neutral status labels "Pendente"/"Concluído" and a "Tempo estimado: ~{N} min" line — NEVER any score/threshold/percent text (RNF-07a); (c) the empty state shows "Nenhuma avaliação pendente". Use the exact pt-BR copy strings from 11-UI-SPEC.md §Copywriting Contract.
  </action>
  <verify>
    <automated>deno test supabase/functions/avaliar-redacao 2>&1 | grep -qiE "module not found|error|FAILED" && npm run test:run -- useAutosaveAvaliacao AvaliacaoContainer 2>&1 | grep -qiE "Cannot find module|fail"</automated>
  </verify>
  <acceptance_criteria>
    - `deno test supabase/functions/avaliar-redacao` collects the suite and every case is RED (module-not-found is the calibrated failure, NOT a syntax error in the test file itself).
    - The deno test file contains the literal `403` for the non-owner AND wrong-etapa authz cases, and an assertion that no `candidaturas` update occurs.
    - `npm run test:run -- useAutosaveAvaliacao` is RED (hook module not found) and the test references `42501` and a 30s/fake-timer debounce.
    - `npm run test:run -- AvaliacaoContainer` is RED and asserts neutral copy ("Pendente"/"Concluído"/"Nenhuma avaliação pendente") with zero score/percent strings.
  </acceptance_criteria>
  <done>4 RED tests exist (1 deno + 2 vitest in this task), each failing because its target module does not exist yet — the calibrated Wave-0 contract.</done>
</task>

<task type="auto">
  <name>Task 2: testesAplicaveis SJT-key RED case + LGPD-04 grep guard extension</name>
  <files>src/features/config-vaga/schemas/__tests__/testesAplicaveisSchema.test.ts, src/__tests__/guards/forbidden-strings.grep.test.ts</files>
  <read_first>
    - src/features/config-vaga/schemas/testesAplicaveisSchema.ts (current 4-key shape to extend in 11-03)
    - src/features/config-vaga/schemas/__tests__/testesAplicaveisSchema.test.ts (if present — extend; else create)
    - src/__tests__/guards/forbidden-strings.grep.test.ts:46-58 (SCAN_ROOTS + FORBIDDEN regex)
    - .planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-RESEARCH.md lines 554-572 (extension shape)
  </read_first>
  <action>
    Extend (or create) `testesAplicaveisSchema.test.ts` with a RED case asserting a `testeAplicavelSchema.parse(...)` over an object carrying the NEW SJT keys succeeds: `tipo:'sjt'`, `cargo:'dentista'`, `itens_ids:['...']`, `bateria_size:4`, `threshold:{ mc_min_pct:60, case_min:13, flag_on_atencao:true }`. Since the schema does not yet accept these keys in a typed way (strict-less today, but the test pins the contract), also assert `threshold.mc_min_pct` defaults to 60 when omitted. This case stays RED until Plan 11-03 lands the extension. Keep all existing testesAplicaveis tests intact.
    Extend `forbidden-strings.grep.test.ts`: add `'supabase/migrations'` to the `SCAN_ROOTS` tuple so the SJT seed migration's candidate-facing scenario text is scanned for the 5 forbidden terms. Do NOT alter the `FORBIDDEN` regex or the RNF_12_TERMS sub-tests. The guard must stay GREEN after the extension (no forbidden term is in migrations today) — it fails only when a banned term is introduced.
  </action>
  <verify>
    <automated>npm run test:run -- testesAplicaveisSchema 2>&1 | grep -qiE "fail|mc_min_pct" && npm run test:run -- forbidden-strings 2>&1 | grep -qi "pass"</automated>
  </verify>
  <acceptance_criteria>
    - `testesAplicaveisSchema.test.ts` has a case referencing `mc_min_pct` + `tipo: 'sjt'` that is RED (the extension is not in 11-03 yet) — the calibrated failure.
    - `forbidden-strings.grep.test.ts` `SCAN_ROOTS` literally contains `'supabase/migrations'`, and the full guard suite PASSES (no banned term in current src/functions/migrations).
    - The `FORBIDDEN` regex and `RNF_12_TERMS` array are byte-identical to before (only SCAN_ROOTS changed).
  </acceptance_criteria>
  <done>SJT-key schema contract is pinned RED; LGPD-04 guard now covers the migrations root and stays green.</done>
</task>

<task type="auto">
  <name>Task 3: SQL-smoke runbook in 11-VALIDATION.md + wave_0 flip</name>
  <files>.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-VALIDATION.md</files>
  <read_first>
    - .planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-VALIDATION.md (skeleton to fill)
    - .planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-RESEARCH.md §Pattern 2/3 + Common Pitfalls 3/4/7 (the smokes to enumerate)
    - supabase/migrations/20260608000001_inscricao_knockout.sql (precedent for set_config request.jwt.claims fixture smokes)
  </read_first>
  <action>
    Replace the runbook skeleton with concrete numbered SQL smokes, each tied to a requirement + the plan that satisfies it. Enumerate at minimum: SMOKE-1 `pontuar_sjt` Σ-peso correctness (fixture candidatura + 3 seeded perguntas → expected v_score/v_max); SMOKE-2 `<mc_min_pct` OR ≥1 atencao → `scores_candidato.status='pendente_humano'`; SMOKE-3 `pontuar_sjt` raises `42501` for a non-owner via `set_config('request.jwt.claims', ...)` as another user; SMOKE-4 `pontuar_sjt` raises `42501` for the owner when `etapa_atual <> 'avaliacao_assincrona'`; SMOKE-5 after a deliberately-failing score, `candidaturas.etapa_atual` UNCHANGED and NO new `historico_candidatura` row (RNF-07a, Pitfall 4); SMOKE-6 etapa-gate RLS: candidato own-row autosave INSERT/UPDATE on `respostas_avaliacao` SUCCEEDS while in `avaliacao_assincrona` and is DENIED once etapa advances (Pitfall 3); SMOKE-7 candidato SELECT on `scores_candidato` returns zero rows (no policy → denied) while RH role reads them; SMOKE-8 re-running `pontuar_sjt` UPDATEs the single MC row, does not insert a duplicate (Pitfall 7 idempotency). Map each smoke to its wave/plan in the Per-Task Verification table. Flip `wave_0_complete: true` and leave `nyquist_compliant: false` (flips after the UI wave tests are GREEN).
  </action>
  <verify>
    <automated>grep -c "SMOKE-" ".planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-VALIDATION.md" | { read n; [ "$n" -ge 8 ]; } && grep -q "wave_0_complete: true" ".planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-VALIDATION.md"</automated>
  </verify>
  <acceptance_criteria>
    - 11-VALIDATION.md lists ≥8 numbered SMOKE-N entries covering pontuar_sjt scoring/threshold, non-owner 42501, wrong-etapa 42501, never-auto-reject (no etapa/historico change), etapa-gate RLS in+out, scores_candidato candidato-DENY, idempotent upsert.
    - Each smoke names the `set_config('request.jwt.claims', ...)` role it simulates where authz is tested.
    - `wave_0_complete: true` set; `nyquist_compliant: false` retained.
  </acceptance_criteria>
  <done>The SQL-smoke runbook is concrete and executable by Plan 11-04; wave-0 marked complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → CI | RED tests gate downstream plans; a falsely-green test would let a broken EF/RPC ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | grep guard SCAN_ROOTS | mitigate | Extend to `supabase/migrations` so the SJT seed scenario text is scanned for the 5 forbidden LGPD-04 terms; the RNF_12_TERMS sub-test proves the regex stays correct |
| T-11-01-02 | Repudiation | falsely-green Wave-0 test | mitigate | Every test is calibrated RED via module-not-found (smoke-runtime gate); the deno authz cases assert 401/403/non-etapa BEFORE the EF exists |
| T-11-01-SC | Tampering | npm/deno installs | accept | No package installs this plan (all deps vendored / existing pins) — no supply-chain surface |
</threat_model>

<verification>
- `deno test supabase/functions/avaliar-redacao` collects, all RED (module-not-found).
- `npm run test:run -- useAutosaveAvaliacao AvaliacaoContainer testesAplicaveisSchema` RED for the new surfaces.
- `npm run test:run -- forbidden-strings` GREEN with `supabase/migrations` in SCAN_ROOTS.
- 11-VALIDATION.md has ≥8 SMOKE-N entries; `wave_0_complete: true`.
</verification>

<success_criteria>
Every Phase-11 surface has a calibrated RED test that fails pre-implementation; the LGPD-04 guard covers the new feature dir + migrations; the SQL-smoke runbook is concrete and plan-mapped.
</success_criteria>

<output>
Create `.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-01-SUMMARY.md` when done.
</output>
