---
phase: 11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3
plan: 06
type: execute
wave: 5
depends_on: ["11-04", "11-05"]
files_modified:
  - src/features/avaliacao/services/scoresRhService.ts
  - src/features/avaliacao/hooks/useScorecardCandidato.ts
  - src/features/avaliacao/components/ScorecardAvaliacao.tsx
  - src/features/avaliacao/services/__tests__/scoresRhService.test.ts
  - src/features/avaliacao/components/index.ts
autonomous: true

requirements: [AVAL-02, AVAL-03]

must_haves:
  truths:
    - "The RH scorecard reads scores_candidato via an EXPLICIT column allowlist (never select('*')) and the candidate side reads zero score data (RLS + allowlist deny) — [[reference_select_star_leaks_pii]]"
    - "Every AI-derived score block carries the SugestaoIABadge (RNF-07a — reused, not re-authored)"
    - "pendente_humano (MC <threshold/≥1 atencao OR open-case <13/red_flag) surfaces a neutral 'Requer revisão humana' marker — the system never auto-rejects and never changes etapa by score"
    - "The scorecard shows the MC per-item breakdown + the open-case BARS dimensions (1-5 → composite 0-25) + citations/red_flags from metadata, in neutral/structured (not red/green pass-fail) presentation"
  artifacts:
    - path: "src/features/avaliacao/services/scoresRhService.ts"
      provides: "RH allowlist read of scores_candidato (no select('*'))"
    - path: "src/features/avaliacao/components/ScorecardAvaliacao.tsx"
      provides: "structured per-dimension RH scorecard + SugestaoIABadge + pendente_humano marker"
      contains: "SugestaoIABadge"
    - path: "src/features/avaliacao/services/__tests__/scoresRhService.test.ts"
      provides: "test that the read uses an allowlist + no select('*')"
  key_links:
    - from: "src/features/avaliacao/components/ScorecardAvaliacao.tsx"
      to: "SugestaoIABadge"
      via: "import { SugestaoIABadge } from triagem"
      pattern: "SugestaoIABadge"
    - from: "src/features/avaliacao/services/scoresRhService.ts"
      to: "scores_candidato"
      via: "allowlist select"
      pattern: "scores_candidato"
---

<objective>
Build the RH-facing read-only scorecard for the SJT scores: an allowlist-projected service read of `scores_candidato`, a hook, and a structured per-dimension component that reuses the `SugestaoIABadge` (RNF-07a) and shows a neutral "Requer revisão humana" marker on `pendente_humano`. The candidate never reaches this surface (RLS + allowlist deny).

Purpose: Give RH a defensible, structured view of the deterministic MC breakdown + the open-case BARS dimensions — with the AI always framed as a suggestion, never an auto-decision.
Output: 1 service + 1 hook + 1 component + 1 allowlist test + barrel export. Desktop RH shell (not the glass candidate shell).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-UI-SPEC.md
@.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-PATTERNS.md

<interfaces>
<!-- Exact contracts. Reuse, do not re-author. -->

From src/features/triagem/components/SugestaoIABadge.tsx (reuse verbatim):
- `export const SUGESTAO_IA_COPY = 'Sugestão da IA — decisão é sempre humana'`
- `export function SugestaoIABadge({ variant = 'full', className }: { variant?: 'full'|'compact', className?: string })` — accent #35BFAD + Sparkles; never colors a score, never on an action.

From src/features/triagem/services/triagemService.ts:128-138 (allowlist read idiom):
- `supabase.from('...').select('id, status, ...')` — explicit columns only, never `*`. RLS denies candidato; RH/admin allowed.

From scores_candidato (Plan 11-02 live schema):
- columns: id, candidatura_id, tipo, subtipo ('mc'|'caso_aberto'), pergunta_id, score, score_max, status ('sucesso'|'pendente_humano'|'falhou'), metadata jsonb, citacoes jsonb, red_flags jsonb.
- MC metadata: `{ respostas:[{pergunta_id,opcao_id,tag,peso}], has_atencao }`.
- open-case metadata: `{ dimension_scores:[{dimension,score_1_5,level,reasoning}], composite_0_25 }`.

From src/features/avaliacao/components/index.ts (the Plan-11-05 barrel to EXTEND — created in Wave 4):
- already exports AvaliacaoContainer + the 2 SJT screens; append the ScorecardAvaliacao export.

From 11-UI-SPEC.md §RH Scorecard View:
- structured per-dimension row/card; SugestaoIABadge on every AI block (full on panel header, compact inline); pendente_humano → neutral "Requer revisão humana" marker; neutral presentation (no red/green pass-fail tints); this is the desktop RH shell, inheriting the Phase-10 panel contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scoresRhService (allowlist read) + useScorecardCandidato hook</name>
  <files>src/features/avaliacao/services/scoresRhService.ts, src/features/avaliacao/hooks/useScorecardCandidato.ts, src/features/avaliacao/services/__tests__/scoresRhService.test.ts</files>
  <behavior>
    - scoresRhService.getScores(candidaturaId) selects an EXPLICIT column list from scores_candidato (id, tipo, subtipo, pergunta_id, score, score_max, status, metadata, citacoes, red_flags) — never select('*').
    - A test asserts the select string contains the allowlisted columns and does NOT contain '*'.
    - useScorecardCandidato wraps the service in a TanStack Query keyed by candidaturaId.
  </behavior>
  <read_first>
    - src/features/triagem/services/triagemService.ts:120-145 (allowlist idiom + error class)
    - src/features/avaliacao/services/avaliacaoService.ts (Plan 11-03 — share the error-class convention)
    - 11-UI-SPEC.md §RH Scorecard View
  </read_first>
  <action>
    Create `src/features/avaliacao/services/scoresRhService.ts` (camelCaseService convention, custom `ScoresRhServiceError`): `getScores(candidaturaId)` → `supabase.from('scores_candidato').select('id, tipo, subtipo, pergunta_id, score, score_max, status, metadata, citacoes, red_flags').eq('candidatura_id', candidaturaId)`. EXPLICIT allowlist only — never `select('*')`. RLS already denies candidato + allows RH/admin (Plan 11-02), so this read is role-gated at the DB; the service just must not over-project. Map errors to the custom class.
    Create `src/features/avaliacao/hooks/useScorecardCandidato.ts`: TanStack `useQuery` keyed `['scorecard-avaliacao', candidaturaId]` calling `scoresRhService.getScores`. staleTime 5min, retry 2 (project standard).
    Create `src/features/avaliacao/services/__tests__/scoresRhService.test.ts`: mock the supabase client, call `getScores`, assert the `.select(...)` argument contains each allowlisted column and does NOT contain `'*'` (the PII-leak guard at the test layer — [[reference_select_star_leaks_pii]]).
  </action>
  <verify>
    <automated>npm run test:run -- scoresRhService 2>&1 | grep -qi "pass" && ! grep -q "select('\\*')" src/features/avaliacao/services/scoresRhService.ts && grep -q "scores_candidato" src/features/avaliacao/services/scoresRhService.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- scoresRhService` PASSES; the test asserts an explicit allowlist and the absence of `'*'`.
    - The service reads `scores_candidato` with an explicit column list (no `select('*')`).
    - useScorecardCandidato is keyed by candidaturaId with the project staleTime/retry defaults.
  </acceptance_criteria>
  <done>RH read path established with allowlist projection; candidate is denied by RLS, RH over-projection prevented by test.</done>
</task>

<task type="auto">
  <name>Task 2: ScorecardAvaliacao component (structured + SugestaoIABadge + pendente_humano marker)</name>
  <files>src/features/avaliacao/components/ScorecardAvaliacao.tsx, src/features/avaliacao/components/index.ts</files>
  <read_first>
    - src/features/triagem/components/SugestaoIABadge.tsx (reuse — do not re-author)
    - src/features/avaliacao/hooks/useScorecardCandidato.ts (Task 1)
    - src/features/avaliacao/components/index.ts (Plan 11-05 barrel to extend)
    - 11-UI-SPEC.md §RH Scorecard View
  </read_first>
  <action>
    Create `src/features/avaliacao/components/ScorecardAvaliacao.tsx` (desktop RH shell — NOT the candidate glass shell; inherit the Phase-10 panel layout). Consume `useScorecardCandidato(candidaturaId)`. Render, per score row: for the MC score (subtipo='mc') a structured per-item breakdown from `metadata.respostas` (item, chosen tag, peso) + the score/score_max in neutral/structured presentation (no red/green pass-fail tints); for the open-case (subtipo='caso_aberto') a per-dimension list from `metadata.dimension_scores` (dimension, 1-5 level, reasoning) + the composite 0-25 + the `citacoes` + `red_flags`. Place a `<SugestaoIABadge variant="full" />` on the AI-derived panel header (open-case) and `variant="compact"` inline on each AI dimension — every AI-derived block carries it (RNF-07a). When `status === 'pendente_humano'` render a neutral "Requer revisão humana" marker (no alarm/red). When `status === 'falhou'` render a neutral "Pontuação indisponível" state. Loading = skeleton; empty = "Sem avaliações registradas ainda." Append the ScorecardAvaliacao export to the existing `components/index.ts` barrel (do not remove the Plan-11-05 exports).
  </action>
  <verify>
    <automated>grep -q "SugestaoIABadge" src/features/avaliacao/components/ScorecardAvaliacao.tsx && grep -q "Requer revisão humana" src/features/avaliacao/components/ScorecardAvaliacao.tsx && npm run test:run -- forbidden-strings 2>&1 | grep -qi "pass" && npm run build 2>&1 | tail -3 | grep -qiE "built|done"</automated>
  </verify>
  <acceptance_criteria>
    - Every AI-derived block carries `SugestaoIABadge` (full on header, compact inline) — reused, not re-authored.
    - `pendente_humano` renders the neutral "Requer revisão humana" marker; presentation is structured/neutral (no red/green pass-fail).
    - The MC per-item breakdown + open-case BARS dimensions + citations/red_flags render from `metadata`.
    - The barrel still exports the Plan-11-05 components (no removal); LGPD-04 grep GREEN; `npm run build` exits 0; tsc baseline unchanged.
  </acceptance_criteria>
  <done>RH scorecard renders structured SJT scores with the AI-suggestion guardrail and human-review framing; never an auto-decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH JWT → scores_candidato | role-gated read; over-projection would leak more than needed |
| candidate → scores_candidato | must be fully denied (RLS, no policy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Info disclosure | scoresRhService read | mitigate | Explicit column allowlist (never select('*')); a vitest asserts the select string excludes '*' ([[reference_select_star_leaks_pii]]); candidato denied by RLS at the DB |
| T-11-06-02 | (compliance) | AI-score framing | mitigate | SugestaoIABadge on every AI block (RNF-07a); pendente_humano → neutral "Requer revisão humana"; no red/green pass-fail; system never auto-rejects |
| T-11-06-SC | Tampering | shadcn primitives | accept | Reuses vendored primitives + the existing SugestaoIABadge — no new installs |
</threat_model>

<verification>
- `npm run test:run -- scoresRhService` GREEN (allowlist + no '*').
- ScorecardAvaliacao reuses SugestaoIABadge + renders "Requer revisão humana" on pendente_humano.
- LGPD-04 grep GREEN; `npm run build` exits 0.
</verification>

<success_criteria>
RH sees a structured, defensible scorecard for the SJT (MC breakdown + open-case BARS + citations), with the AI framed as a suggestion and human review required on threshold flags — never an auto-decision, and the candidate denied entirely.
</success_criteria>

<output>
Create `.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-06-SUMMARY.md` when done.
</output>
