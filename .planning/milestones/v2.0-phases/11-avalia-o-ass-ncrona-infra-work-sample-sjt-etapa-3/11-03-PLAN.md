---
phase: 11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/features/config-vaga/schemas/testesAplicaveisSchema.ts
  - src/features/avaliacao/schemas/respostaAvaliacaoSchema.ts
  - src/features/avaliacao/hooks/useAvaliacaoDraft.ts
  - src/features/avaliacao/hooks/useAutosaveAvaliacao.ts
  - src/features/avaliacao/services/avaliacaoService.ts
autonomous: true

requirements: [AVAL-01, AVAL-09, AVAL-02, AVAL-03]

must_haves:
  truths:
    - "testesAplicaveisSchema accepts the SJT keys (tipo:'sjt', cargo, itens_ids[], bateria_size, threshold:{mc_min_pct default 60, case_min default 13, flag_on_atencao default true}) while keeping the existing 4 keys intact (AVAL-01)"
    - "useAutosaveAvaliacao buffers to sessionStorage immediately and debounces a 30s server upsert to respostas_avaliacao, and catches the RLS 42501 back-lock to surface a neutral 'locked' state instead of an error (AVAL-09, Pitfall 3)"
    - "avaliacaoService reads candidatura/perguntas/respostas via EXPLICIT allowlist (never select('*')), calls the pontuar_sjt RPC for MC, and invokes the avaliar-redacao EF for the open case — client never computes/submits a score (Pitfall 5)"
    - "respostaAvaliacaoSchema shapes MC {pergunta_id,opcao_id}[] + open-case {pergunta_id,texto} with NO score field (server-derived only)"
  artifacts:
    - path: "src/features/config-vaga/schemas/testesAplicaveisSchema.ts"
      provides: "SJT-key extension (turns 11-01 RED case GREEN)"
      contains: "mc_min_pct"
    - path: "src/features/avaliacao/hooks/useAutosaveAvaliacao.ts"
      provides: "30s debounced server upsert + 42501 back-lock catch"
      contains: "42501"
    - path: "src/features/avaliacao/services/avaliacaoService.ts"
      provides: "allowlist reads + pontuar_sjt RPC + EF invoke"
    - path: "src/features/avaliacao/hooks/useAvaliacaoDraft.ts"
      provides: "sessionStorage dies-with-tab draft (LGPD)"
    - path: "src/features/avaliacao/schemas/respostaAvaliacaoSchema.ts"
      provides: "client answer shape, no score field"
  key_links:
    - from: "src/features/avaliacao/services/avaliacaoService.ts"
      to: "pontuar_sjt RPC + avaliar-redacao EF"
      via: "supabase.rpc('pontuar_sjt') + supabase.functions.invoke('avaliar-redacao')"
      pattern: "pontuar_sjt"
    - from: "src/features/avaliacao/hooks/useAutosaveAvaliacao.ts"
      to: "src/features/avaliacao/hooks/useAvaliacaoDraft.ts"
      via: "buffer-then-debounce composition"
      pattern: "useAvaliacaoDraft"
---

<objective>
Extend the Phase-7 `testesAplicaveisSchema` with the SJT configuration keys and scaffold the non-UI half of the `src/features/avaliacao/` feature: the client answer schema, the sessionStorage draft hook, the 30s debounced autosave hook (with the RLS back-lock catch), and the service layer (allowlist reads + pontuar_sjt RPC + avaliar-redacao EF invoke). This turns the 11-01 RED schema + autosave-hook tests GREEN.

Purpose: Provide the data/logic layer the candidate UI (Plan 11-05) and RH scorecard (Plan 11-06) consume, with the anti-tamper + back-lock + allowlist invariants baked in.
Output: 1 schema extension + 4 new feature files. No UI components yet (those are Plan 11-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-RESEARCH.md
@.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-PATTERNS.md

<interfaces>
<!-- Exact analog contracts. No exploration needed. -->

From src/features/config-vaga/schemas/testesAplicaveisSchema.ts (current shape to extend in place):
- `testeAplicavelSchema = z.object({ teste: z.string().min(1), obrigatorio: z.boolean(), customizado: z.boolean(), perguntas: z.array(z.string()).optional() })`
- Add optional SJT keys (RESEARCH lines 557-572): `tipo: z.literal('sjt').optional()`, `cargo: z.string().optional()`, `itens_ids: z.array(z.string()).optional()`, `bateria_size: z.number().int().positive().optional()`, `threshold: z.object({ mc_min_pct: z.number().default(60), case_min: z.number().default(13), flag_on_atencao: z.boolean().default(true) }).optional()`. Keep the 4 existing keys.

From src/features/cadastro/hooks/useCadastroDraft.ts (copy structure for useAvaliacaoDraft):
- sessionStorage (NOT localStorage — LGPD dies-with-tab); `save`/`load`/`clear` via useCallback; strip sensitive fields; `_savedAt` stamp; try/catch-swallow.

From src/features/triagem/services/triagemService.ts:128-138 (allowlist read idiom):
- `supabase.from('candidaturas').select('id, status, etapa_atual, ...')` — explicit columns only, never `*`.

From the Plan-11-01 RED tests this plan must satisfy:
- `useAutosaveAvaliacao.test.ts` expects a 30s fake-timer debounce, a single upsert per window, and a `locked` state when the upsert error code/status is `42501`.
- `testesAplicaveisSchema.test.ts` expects the SJT keys to parse and `threshold.mc_min_pct` to default to 60.

From Plan 11-02 (the backend this service calls):
- RPC `pontuar_sjt(p_candidatura_id uuid, p_respostas jsonb)` returns neutral `{ ok, registrado }`.
- EF `avaliar-redacao` body = `{ candidatura_id, pergunta_id, texto }`, returns neutral `{ ok:true }`.
- Tables: `respostas_avaliacao(candidatura_id, teste, respostas jsonb)` UNIQUE(candidatura_id, teste); `perguntas(id, cargo, cenario, formato, rubric, status)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: testesAplicaveis SJT extension + respostaAvaliacaoSchema + useAvaliacaoDraft</name>
  <files>src/features/config-vaga/schemas/testesAplicaveisSchema.ts, src/features/avaliacao/schemas/respostaAvaliacaoSchema.ts, src/features/avaliacao/hooks/useAvaliacaoDraft.ts</files>
  <behavior>
    - testeAplicavelSchema.parse with SJT keys (tipo/cargo/itens_ids/bateria_size/threshold) succeeds; threshold.mc_min_pct defaults to 60 when omitted.
    - testeAplicavelSchema still parses the legacy 4-key object (no regression).
    - respostaAvaliacaoSchema rejects any object carrying a `score`/`pontuacao` field.
    - useAvaliacaoDraft uses sessionStorage (not localStorage) and clears on demand.
  </behavior>
  <read_first>
    - src/features/config-vaga/schemas/testesAplicaveisSchema.ts (extend in place)
    - src/features/config-vaga/schemas/__tests__/testesAplicaveisSchema.test.ts (the RED case from 11-01)
    - src/features/cadastro/hooks/useCadastroDraft.ts (copy structure)
    - .planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-RESEARCH.md lines 557-572
  </read_first>
  <action>
    Extend `testesAplicaveisSchema.ts` in place: add the 5 optional SJT keys to `testeAplicavelSchema` per the interfaces block (tipo literal 'sjt', cargo, itens_ids[], bateria_size positive int, threshold object with mc_min_pct default 60 / case_min default 13 / flag_on_atencao default true). Keep `teste/obrigatorio/customizado/perguntas?` intact. Update the module JSDoc to note the F11 SJT extension (drop the "deferred to Phase 11" wording — it is now landed).
    Create `src/features/avaliacao/schemas/respostaAvaliacaoSchema.ts`: a discriminated/union Zod shape for the client answer — MC = `z.object({ tipo: z.literal('mc'), respostas: z.array(z.object({ pergunta_id: z.string().uuid(), opcao_id: z.string().uuid() })) })`; open-case = `z.object({ tipo: z.literal('caso_aberto'), pergunta_id: z.string().uuid(), texto: z.string().min(1) })`. Use `.strict()` so an injected `score`/`pontuacao` field is rejected (Pitfall 5 — score is server-derived). Export inferred types.
    Create `src/features/avaliacao/hooks/useAvaliacaoDraft.ts`: copy `useCadastroDraft` structure — `save`/`load`/`clear` over `sessionStorage` (NOT localStorage — LGPD dies-with-tab), `_savedAt` stamp, try/catch-swallow, keyed by `candidatura_id`+`teste`. Payload type = the SJT answer shape (no secrets to strip here, but keep the strip-pass for parity).
  </action>
  <verify>
    <automated>npm run test:run -- testesAplicaveisSchema 2>&1 | grep -qi "pass" && grep -q "mc_min_pct" src/features/config-vaga/schemas/testesAplicaveisSchema.ts && grep -q "sessionStorage" src/features/avaliacao/hooks/useAvaliacaoDraft.ts && grep -q "strict" src/features/avaliacao/schemas/respostaAvaliacaoSchema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- testesAplicaveisSchema` PASSES (the 11-01 RED case is now GREEN); `threshold.mc_min_pct` defaults to 60.
    - The 4 legacy keys still parse (no regression test fails).
    - respostaAvaliacaoSchema uses `.strict()` and has no `score` field; useAvaliacaoDraft uses `sessionStorage`.
  </acceptance_criteria>
  <done>SJT config keys land; client answer schema + LGPD draft hook in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useAutosaveAvaliacao (30s debounce + 42501 back-lock catch) + avaliacaoService</name>
  <files>src/features/avaliacao/hooks/useAutosaveAvaliacao.ts, src/features/avaliacao/services/avaliacaoService.ts</files>
  <behavior>
    - useAutosaveAvaliacao buffers to useAvaliacaoDraft immediately and flushes a server upsert once per 30s debounce window (fake-timer asserted).
    - When the upsert rejects with a Supabase error whose code/status is 42501, the hook sets a `locked` state and does NOT re-throw (neutral back-lock UX — Pitfall 3).
    - avaliacaoService.pontuarSjt calls supabase.rpc('pontuar_sjt', {...}) and returns the neutral payload.
    - avaliacaoService.avaliarRedacao invokes the avaliar-redacao EF via supabase.functions.invoke.
    - All reads use explicit column allowlists (no select('*')).
  </behavior>
  <read_first>
    - src/features/avaliacao/hooks/__tests__/useAutosaveAvaliacao.test.ts (the RED test from 11-01 to satisfy)
    - src/features/triagem/services/triagemService.ts:120-145 (allowlist read idiom + service error-class convention)
    - src/features/avaliacao/hooks/useAvaliacaoDraft.ts (the buffer this composes — from Task 1)
    - .planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-PATTERNS.md §useAutosaveAvaliacao + §avaliacaoService (lines 251-269)
  </read_first>
  <action>
    Create `src/features/avaliacao/hooks/useAutosaveAvaliacao.ts`: compose `useAvaliacaoDraft` (immediate sessionStorage buffer) + a TanStack `useMutation` that upserts `{candidatura_id, teste, respostas}` into `respostas_avaliacao`. Debounce the server flush to a 30s window (clear+set a timer on each change; flush on the trailing edge and on unmount). Wrap the mutation so a rejected upsert whose Supabase error `code === '42501'` (or status 403) sets a returned `locked: true` state (the etapa advanced — back-lock) WITHOUT re-throwing; other errors surface a transient "tentando novamente" state. Expose `{ status: 'idle'|'saving'|'saved'|'error'|'locked', flushNow }`.
    Create `src/features/avaliacao/services/avaliacaoService.ts` (camelCaseService convention, custom error class): `getAvaliacaoContext(candidaturaId)` reads the candidatura (allowlist `id, status, etapa_atual, vaga_id`) + its vaga `testes_aplicaveis` + the active `perguntas` for the cargo (allowlist `id, cargo, cenario, formato, tempo_est_min, rubric, status` — never select('*')); `loadResposta(candidaturaId, teste)` reads own-row `respostas_avaliacao`; `upsertResposta(...)`; `pontuarSjt(candidaturaId, respostas)` → `supabase.rpc('pontuar_sjt', { p_candidatura_id, p_respostas })`; `avaliarRedacao(body)` → `supabase.functions.invoke('avaliar-redacao', { body })`. The client NEVER computes or posts a score. Custom `AvaliacaoServiceError` for mapped failures.
  </action>
  <verify>
    <automated>npm run test:run -- useAutosaveAvaliacao 2>&1 | grep -qi "pass" && grep -q "42501" src/features/avaliacao/hooks/useAutosaveAvaliacao.ts && grep -q "pontuar_sjt" src/features/avaliacao/services/avaliacaoService.ts && ! grep -q "select('\\*')" src/features/avaliacao/services/avaliacaoService.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- useAutosaveAvaliacao` PASSES (30s debounce + single flush per window + 42501 → locked, no re-throw).
    - avaliacaoService calls `supabase.rpc('pontuar_sjt', ...)` and `supabase.functions.invoke('avaliar-redacao', ...)`; reads use explicit allowlists (no `select('*')`).
    - No code path in the service or hook submits a numeric score (anti-tamper).
  </acceptance_criteria>
  <done>Autosave with back-lock catch + the service layer wired to the deterministic RPC and the AI EF; no client-side scoring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → respostas_avaliacao | autosave writes; RLS etapa-gate enforces back-lock; client must degrade gracefully on 42501 |
| client → pontuar_sjt / avaliar-redacao | client posts only answers; score is server-derived |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | client score submission | mitigate | respostaAvaliacaoSchema `.strict()` rejects a score field; service posts only opcao_ids/text; scoring is server-side (RPC/EF) |
| T-11-03-02 | Tampering | back-lock bypass | mitigate | autosave catches RLS 42501 → `locked` state; the actual enforcement is the DB RLS (Plan 11-02) — the client merely surfaces it neutrally |
| T-11-03-03 | Info disclosure | service reads | mitigate | explicit allowlist on every candidatura/perguntas read (no select('*') — [[reference_select_star_leaks_pii]]); candidato never queries scores_candidato |
| T-11-03-SC | Tampering | npm installs | accept | No new packages — TanStack Query + zod are existing workspace deps |
</threat_model>

<verification>
- `npm run test:run -- testesAplicaveisSchema useAutosaveAvaliacao` GREEN.
- Service has no `select('*')` and no client-side score computation.
- `npm run build` exits 0; tsc baseline unchanged.
</verification>

<success_criteria>
The data/logic layer for the avaliação feature exists: SJT config keys parse, autosave debounces + handles the back-lock, and the service wires to the server-authoritative scoring with allowlist reads and no client scoring.
</success_criteria>

<output>
Create `.planning/phases/11-avalia-o-ass-ncrona-infra-work-sample-sjt-etapa-3/11-03-SUMMARY.md` when done.
</output>
