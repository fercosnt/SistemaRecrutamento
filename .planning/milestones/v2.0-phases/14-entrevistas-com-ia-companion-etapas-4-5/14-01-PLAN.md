---
phase: 14-entrevistas-com-ia-companion-etapas-4-5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/entrevista-schemas.ts
  - supabase/functions/_shared/cognitivo-schemas.ts
  - supabase/functions/_shared/cognitivo/scoring.ts
  - supabase/functions/_shared/cognitivo/scoring.test.ts
  - supabase/functions/avaliar-transcricao-entrevista/_local/derive-flags.ts
  - supabase/functions/avaliar-transcricao-entrevista/_local/derive-flags.test.ts
  - supabase/functions/gerar-guia-entrevista/_local/weak-dim-coverage.ts
  - supabase/functions/gerar-guia-entrevista/_local/weak-dim-coverage.test.ts
  - src/features/entrevista/__tests__/entrevista-contract.test.ts
  - src/features/entrevista/__tests__/entrevista-allowlist.test.ts
  - .planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-VALIDATION.md
autonomous: true
requirements: [ENTREV-01, ENTREV-03, ENTREV-04, ENTREV-05]
must_haves:
  truths:
    - "A calibrated failing test exists for every Phase-14 surface BEFORE its implementation lands (smoke-runtime gate, Phase-4 lesson)"
    - "The cognitive scorer is a pure deterministic CTT-soma module with a server-only key, never exposed to the candidate (ENTREV-05, per D-cognitive-tipo and resolved Q3)"
    - "The language/accent flag derivation is server-authoritative (score<3 && regional_markers_ignored===false), NOT asked of the LLM (ENTREV-03, resolved Q2)"
    - "EF body schemas are .strict() and carry NO score/band field (anti-tamper, RNF-07a)"
  artifacts:
    - path: "supabase/functions/_shared/entrevista-schemas.ts"
      provides: "Anti-tamper .strict() body schemas for both interview EFs (no score fields)"
      contains: ".strict()"
    - path: "supabase/functions/_shared/cognitivo/scoring.ts"
      provides: "Deterministic CTT-soma scorer + 5-faixa banding (scoreRaciocinio), server-only key"
    - path: "supabase/functions/avaliar-transcricao-entrevista/_local/derive-flags.ts"
      provides: "Server-side language/accent flag derivation (no LLM)"
    - path: "supabase/functions/_shared/cognitivo/scoring.test.ts"
      provides: "Golden test — 10 synthetic profiles → expected band"
    - path: ".planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-VALIDATION.md"
      provides: "SQL-smoke runbook for the apply wave"
  key_links:
    - from: "src/features/entrevista/__tests__/entrevista-contract.test.ts"
      to: "supabase/functions/_shared/entrevista-schemas.ts"
      via: "client body parses under the EF Zod schema"
      pattern: "AvaliarTranscricaoBodySchema|GerarGuiaBodySchema"
---

<objective>
Lay the Wave-0 RED test layer + the pure deterministic modules (cognitive scorer, flag derivation, weak-dim coverage) + the anti-tamper EF body schemas + the SQL-smoke runbook for Phase 14. Every downstream Phase-14 surface gets a calibrated failing test BEFORE its implementation lands (the smoke-runtime gate established in Phase 4.1).

Purpose: Phase 14 is the 5th AI-EF phase — almost nothing is greenfield at the AI/infra layer. This plan front-loads the testable contracts (schemas, scorer, flag predicate, coverage check) so the EF/migration wave (14-03) and the UI waves (14-05/06) implement against fixed, test-locked contracts. Cognitive scoring (resolved Q3) is a deterministic no-LLM module; the language/accent flag (resolved Q2) is server-derived.

Output: 2 EF body-schema files (.strict, no score fields), 1 deterministic cognitive scorer + golden test, 1 flag-derivation module + truth-table test, 1 weak-dim-coverage module + test, 2 frontend RED tests (contract + allowlist), 1 SQL-smoke runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-RESEARCH.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-PATTERNS.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-CONTEXT.md

<interfaces>
<!-- The EF output contracts (already on disk — the EFs in 14-03 consume these). Do NOT re-author them. -->
From docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts:
- `InterviewGuideSchema` (questions: .min(5).max(7); each question: bars_anchors .length(5), competency:string, type enum star|pei|situational|technical_probe|follow_up; format enum online|presencial|hibrido)
- `TranscriptAnalysisSchema` (competency_evaluations .min(1).max(8); each: score Score1to5, cited_evidence .min(0).max(3), bias_flags { content_dependent_only, regional_markers_ignored, disfluencies_ignored })

From supabase/functions/_shared/scores_candidato (live enum tipo_score): 'sjt','big_five','redacao','entrevista','cognitivo','decisao' — 'entrevista' + 'cognitivo' ALREADY EXIST (NO ALTER TYPE).

Cognitive band enum (PRD-cognitivo §8.2, snake_case pt-BR, 5 faixas): 'bem_abaixo' | 'abaixo' | 'na_media' | 'acima' | 'bem_acima'
Cognitive sections: matriz + letra_numero (CTT soma 0/1, subscores por seção + total).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EF body schemas (.strict, anti-tamper) + frontend contract RED test</name>
  <files>supabase/functions/_shared/entrevista-schemas.ts, supabase/functions/_shared/cognitivo-schemas.ts, src/features/entrevista/__tests__/entrevista-contract.test.ts</files>
  <read_first>
    - supabase/functions/_shared/redacao-schemas.ts (exact analog — .strict() body, no score fields, plain npm:zod@3.25.76; the doc-comment explains the anti-tamper contract)
    - supabase/functions/_shared/avaliacao-schemas.ts (AvaliarRedacaoBodySchema precedent SJT)
    - src/features/avaliacao/__tests__/redacao-contract.test.ts (the client↔EF source-probe contract test to clone — [[feedback_integration_contract_gap]])
  </read_first>
  <behavior>
    - GerarGuiaBodySchema rejects an extra injected score field (.strict()): accepts { candidatura_id, vaga_id, tipo: 'online'|'presencial' }; rejects { ...valid, score: 5 }
    - AvaliarTranscricaoBodySchema accepts { candidatura_id, transcricao } and rejects any extra field (.strict()); transcript length≥200 is revalidated in the EF (schema only min(1))
    - SubmitCognitivoBodySchema (the candidate's raw-picks body) accepts { candidatura_id, raw_responses, shuffle_seed, client_timings? } and rejects any injected score/band/banda field
    - The frontend contract test source-probes entrevista-schemas.ts proving it carries NO score/band token (anti-tamper); the client body shape parses under each schema
  </behavior>
  <action>Create `_shared/entrevista-schemas.ts` cloning `redacao-schemas.ts` verbatim (plain `import { z } from "npm:zod@3.25.76"`, NOT /v4 — body schemas use v3 .safeParse; /v4 is only for the SDK structured-output OUTPUT schemas in 14-03). Export `GerarGuiaBodySchema` ({ candidatura_id: z.string().min(1), vaga_id: z.string().min(1), tipo: z.enum(['online','presencial']) }).strict() and `AvaliarTranscricaoBodySchema` ({ candidatura_id: z.string().min(1), transcricao: z.string().min(1) }).strict() — NO score/band fields (anti-tamper per RNF-07a). Create `_shared/cognitivo-schemas.ts` exporting `SubmitCognitivoBodySchema` ({ candidatura_id: z.string().min(1), raw_responses: z.record(z.string(), z.number().int()), shuffle_seed: z.string().min(1), client_timings: z.array(z.number()).optional() }).strict() plus a `BandaCognitivaEnum` = z.enum(['bem_abaixo','abaixo','na_media','acima','bem_acima']) (PRD-cognitivo §8.2). Create the frontend RED test cloning `redacao-contract.test.ts`: source-probe that `entrevista-schemas.ts` contains zero score/band tokens, and that representative client bodies parse under each schema. Drop any `as never` casts after types regen (14-04) — note this in a code comment per [[feedback_integration_contract_gap]].</action>
  <verify>
    <automated>npm run test:run -- entrevista-contract</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "\.strict\(\)" supabase/functions/_shared/entrevista-schemas.ts` returns ≥2 matches
    - `grep -viE "^\s*//|describe|test|rejects|score field" supabase/functions/_shared/entrevista-schemas.ts | grep -ciE "score|band|nota|veredito|threshold" | grep -qx 0` (no score/band token in the live body schema code)
    - `npm run test:run -- entrevista-contract` runs; the source-probe assertion is GREEN (schema files exist + carry no score token); contract-parse assertions GREEN
    - `BandaCognitivaEnum` enumerates exactly the 5 pt-BR faixas
  </acceptance_criteria>
  <done>Both EF body-schema files exist (.strict, no score fields), the cognitive band enum is the 5 pt-BR faixas, and the frontend contract test passes its source-probe + parse assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic cognitive scorer (CTT soma + 5-faixa banding) + golden test</name>
  <files>supabase/functions/_shared/cognitivo/scoring.ts, supabase/functions/_shared/cognitivo/scoring.test.ts</files>
  <read_first>
    - supabase/functions/_shared/bigfive-scoring.ts (exact analog — server-only key, verbatim-from-source, deep module with a stable interface; the candidate posts only raw picks, server re-scores)
    - supabase/functions/_shared/bigfive-scoring.test.ts (golden-test structure — synthetic profiles → expected band; clone this shape)
    - docs/prds/m2-funil-rh/PRD-cognitivo-raciocinio.md §8.2 (CTT soma 0/1, subscores por seção matriz+letra_numero + total, banda enum 5 faixas, shuffle-reverse posture)
  </read_first>
  <behavior>
    - scoreRaciocinio(rawResponses, gabarito) returns { total, secoes: { matriz: {raw, n_itens}, letra_numero: {raw, n_itens} }, banda, flags } (PRD §8.2 shape)
    - CTT soma simples 0/1: a correct pick scores 1, wrong/blank 0; total = matriz.raw + letra_numero.raw
    - banding maps raw total → one of 5 faixas ('bem_abaixo'|'abaixo'|'na_media'|'acima'|'bem_acima') via the PRD §8.2 cutoffs (item-difficulty SAPA provisional reference + wide bands)
    - 10 synthetic profiles (known raw answers → expected banda) all match (the golden test)
    - A client-sent score is structurally impossible to influence — the scorer takes ONLY raw picks + the server-only gabarito
  </behavior>
  <action>Create `_shared/cognitivo/scoring.ts` as a deep module mirroring `bigfive-scoring.ts`'s posture: the gabarito/banding-cutoff constants are server-side only (the candidate NEVER receives them), transcribed verbatim from PRD-cognitivo §8.2 (cite the PRD line in a doc-comment). Export the stable interface `scoreRaciocinio(rawResponses: Record<string, number>, gabarito: Record<string, number>): { total, secoes: { matriz, letra_numero }, banda, flags }`. CTT soma 0/1 per item, sum per section, banding via the §8.2 5-faixa cutoffs (use the BandaCognitivaEnum values from cognitivo-schemas.ts — import the literal string union or duplicate as a const; keep DRY by re-exporting). Author `cognitivo/scoring.test.ts` cloning `bigfive-scoring.test.ts`: 10 synthetic profiles (raw answers → expected banda), asserting the external behavior (input→banda/flags), NOT the internal loop. This test calibrates RED first (scoring.ts initially throws or returns a stub) then GREEN once the soma+banding lands.</action>
  <verify>
    <automated>deno test supabase/functions/_shared/cognitivo/scoring.test.ts --allow-none 2>/dev/null || deno test supabase/functions/_shared/cognitivo/scoring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `deno test supabase/functions/_shared/cognitivo/scoring.test.ts` runs and the 10-profile golden battery is GREEN
    - `grep -cE "scoreRaciocinio" supabase/functions/_shared/cognitivo/scoring.ts` ≥1
    - scoring.ts contains no import of any client/candidate-facing module (server-only key — `grep -ciE "import .*(features|components)" supabase/functions/_shared/cognitivo/scoring.ts | grep -qx 0`)
    - The banding function returns only the 5 pt-BR faixa literals
  </acceptance_criteria>
  <done>scoreRaciocinio is a pure deterministic CTT-soma + 5-faixa-banding module with the gabarito server-side only; the 10-profile golden test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Flag-derivation + weak-dim-coverage modules + their truth-table tests + SQL-smoke runbook</name>
  <files>supabase/functions/avaliar-transcricao-entrevista/_local/derive-flags.ts, supabase/functions/avaliar-transcricao-entrevista/_local/derive-flags.test.ts, supabase/functions/gerar-guia-entrevista/_local/weak-dim-coverage.ts, supabase/functions/gerar-guia-entrevista/_local/weak-dim-coverage.test.ts, src/features/entrevista/__tests__/entrevista-allowlist.test.ts, .planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-VALIDATION.md</files>
  <read_first>
    - supabase/functions/avaliar-redacao-cultural/_local/compute-score.ts (analog — pure deterministic server-authoritative derivation with a colocated truth-table test; the module shape to mirror)
    - src/features/avaliacao/__tests__/ (allowlist-projection test style — test the network select, not the JSX; [[reference_select_star_leaks_pii]])
    - .planning/phases/13-redacao-cultural-revisao-humana/13-VALIDATION.md (SQL-smoke runbook format to clone, if present; else use the 11-VALIDATION/08-VALIDATION format)
  </read_first>
  <behavior>
    - deriveLanguageAccentFlag(parsed: TranscriptAnalysis) → { flag: boolean, blockedCompetencies: string[] }: a competency fires when score < 3 AND bias_flags.regional_markers_ignored === false (resolved Q2 / Assumptions Log A3); flag=true if any competency fires
    - Truth table: (score=2, regional_markers_ignored=false) → fires; (score=2, regional_markers_ignored=true) → does not fire; (score=4, regional_markers_ignored=false) → does not fire; empty competencies → flag=false
    - checkWeakDimCoverage(guide: InterviewGuide, weakDims: string[]) → { covered: boolean, missing: string[] }: every weak dim must be covered by some questions[].competency; missing lists uncovered weak dims (Pitfall 4)
    - allowlist test asserts the entrevistaService read uses an explicit column list (never select('*')) — RED until 14-05 lands the service (calibrated failure)
  </behavior>
  <action>Create `avaliar-transcricao-entrevista/_local/derive-flags.ts` mirroring `avaliar-redacao-cultural/_local/compute-score.ts`: a pure function `deriveLanguageAccentFlag` over the parsed `TranscriptAnalysisSchema` output returning `{ flag, blockedCompetencies }` with the predicate `score < 3 && bias_flags.regional_markers_ignored === false` (resolved Q2 — server-authoritative, NOT the LLM). Colocate `derive-flags.test.ts` as a truth table covering the four cases above. Create `gerar-guia-entrevista/_local/weak-dim-coverage.ts` exporting `checkWeakDimCoverage(guide, weakDims)` → `{ covered, missing }` (Pitfall 4 — assert each weak dim score<3 for online / <4 for presencial is covered by some `questions[].competency`); colocate `weak-dim-coverage.test.ts`. Create the frontend `entrevista-allowlist.test.ts` source-probing that the (future) `entrevistaService` read uses an explicit allowlist (calibrate RED — the service ships in 14-05). Author `14-VALIDATION.md` as the SQL-smoke runbook for the apply wave (14-04): smokes for (1) flag-blocks-`avancar_etapa` until `revisao_confirmada`, (2) `pontuar_cognitivo` RPC non-owner → 42501, (3) cognitive `status` NEVER auto-rejects (no candidaturas write), (4) `aplica_cognitivo=false` → no invite path, (5) `salvar_avaliacao_entrevista` non-owner RH → 42501, (6) candidate-DENY RLS on the new tables, (7) prompt rows `interview_guide`/`transcript_analysis` is_active=true post-hydration. Mark `nyquist_compliant: false` until the implementation waves flip the tests GREEN.</action>
  <verify>
    <automated>deno test supabase/functions/avaliar-transcricao-entrevista/_local/derive-flags.test.ts && deno test supabase/functions/gerar-guia-entrevista/_local/weak-dim-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `deno test .../derive-flags.test.ts` GREEN — the 4-case truth table passes
    - `deno test .../weak-dim-coverage.test.ts` GREEN
    - `grep -cE "regional_markers_ignored" supabase/functions/avaliar-transcricao-entrevista/_local/derive-flags.ts` ≥1 and the predicate is `score < 3` AND `=== false`
    - `14-VALIDATION.md` exists with ≥7 SQL-smoke blocks covering the apply-wave checks above and `nyquist_compliant: false`
    - `entrevista-allowlist.test.ts` exists and references an explicit column allowlist (no `select('*')`)
  </acceptance_criteria>
  <done>The flag-derivation + weak-dim-coverage modules are pure/tested, the allowlist RED test is calibrated, and the SQL-smoke runbook is authored for the apply wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → EF body | Candidate/RH-posted JSON crosses here; must be `.strict()` validated, no score field accepted |
| candidate → cognitive scorer | Raw picks cross here; the gabarito/banding key never crosses back to the candidate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | EF body schemas | mitigate | `.strict()` rejects any injected score/band field; frontend contract test source-probes the schema carries no score token (anti-tamper RNF-07a) |
| T-14-01-02 | Tampering | cognitive scorer key | mitigate | gabarito/banding cutoffs server-side only in scoring.ts; module imports nothing candidate-facing; golden test asserts behavior not internals |
| T-14-01-03 | Repudiation | flag derivation | mitigate | language/accent flag is server-derived deterministically (score<3 && regional_markers_ignored===false), never the LLM's decision; truth-table test locks the predicate |
| T-14-01-SC | Tampering | npm/deno installs | mitigate | zero new packages this phase (RESEARCH Package Legitimacy Audit); all imports reuse PROD-pinned specifiers |
</threat_model>

<verification>
- `npm run test:run -- entrevista-contract` GREEN (source-probe + parse contract)
- `deno test supabase/functions/_shared/cognitivo/scoring.test.ts` GREEN (10-profile golden battery)
- `deno test supabase/functions/avaliar-transcricao-entrevista/_local/derive-flags.test.ts` GREEN
- `deno test supabase/functions/gerar-guia-entrevista/_local/weak-dim-coverage.test.ts` GREEN
- `npm run test:run -- forbidden-strings.grep` GREEN (the guard already recursively scans the new EF + feature dirs)
- tsc baseline ≤305 (zero growth); commit via `git -c core.hooksPath=/dev/null`
</verification>

<success_criteria>
- Both EF body schemas are `.strict()` with NO score/band field (anti-tamper)
- The cognitive scorer is deterministic CTT-soma + 5-faixa banding with a server-only key; 10-profile golden test GREEN
- The language/accent flag is server-derived (resolved Q2); truth-table test GREEN
- The weak-dim-coverage check is tested (Pitfall 4)
- 14-VALIDATION.md SQL-smoke runbook authored for the apply wave
- Every downstream surface has a calibrated failing/passing test before its implementation (smoke-runtime gate)
</success_criteria>

<output>
Create `.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-01-SUMMARY.md` when done
</output>
