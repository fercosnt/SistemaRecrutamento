---
phase: 14-entrevistas-com-ia-companion-etapas-4-5
plan: 04
type: execute
wave: 3
depends_on: ["14-03"]
files_modified:
  - database.types.ts
autonomous: false
requirements: [ENTREV-01, ENTREV-03, ENTREV-04, ENTREV-05, ENTREV-02]
must_haves:
  truths:
    - "The 4 Phase-14 migrations are LIVE in PROD (applied via Supabase MCP apply_migration — bypasses 42601, writes the version row; D-22)"
    - "Both interview prompt rows (interview_guide + transcript_analysis) are hydrated with their real templates and is_active=true — content set BEFORE/without touching deployed_at (immutability trigger ordering, Pitfall 6)"
    - "Both EFs are deployed JWT-on; the cognitive items + the seed are live; database.types.ts is regenerated"
    - "The live SQL smokes PASS: flag-blocks-avancar_etapa, pontuar_cognitivo non-owner→42501, never-auto-reject, candidate-DENY RLS, aplica_cognitivo opt-in gate (RNF-07a holds)"
  artifacts:
    - path: "database.types.ts"
      provides: "Regenerated types reflecting the new tables/columns/RPCs (at repo ROOT)"
      contains: "entrevista_guias"
  key_links:
    - from: "prompt_versions(interview_guide, transcript_analysis)"
      to: "is_active=true + hydrated templates"
      via: "execute_sql UPDATE dollar-quoted BEFORE deployed_at"
      pattern: "is_active"
---

<objective>
[BLOCKING] Apply the 4 Phase-14 migrations to live PROD via Supabase MCP `apply_migration` (D-22), hydrate + activate the two interview prompt rows, deploy both EFs JWT-on, seed the cognitive items, regenerate `database.types.ts`, and run the live SQL smokes from `14-VALIDATION.md`. This is a high-blast-radius PROD-infra step — marked autonomous:false so execute-phase surfaces it as a human checkpoint (precedent: Phase 8/10/11/13 apply waves).

Purpose: Build + type checks pass WITHOUT the apply (types come from the generated `database.types.ts`), creating a false-positive verification — the EFs/RPCs/RLS only exist after the apply. The prompt rows are seeded `is_active=false` with placeholder content (Phase 9 seed `20260609000004`); they must be hydrated with the real `04-interview-guide.md`/`05-transcript-analysis.md` bodies and activated, content-first (the immutability trigger locks template/hash once `deployed_at` is set — Pitfall 6, Phase 13 `culture_fit_essay` precedent).

Output: live PROD schema + 2 active prompts + 2 deployed EFs + seeded cognitive items + regenerated types + green SQL smokes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-VALIDATION.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-03-SUMMARY.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- D-22 apply path: Supabase MCP apply_migration writes the version row + bypasses SQLSTATE 42601 on $$ bodies. Do NOT use supabase db push for these. Do NOT wrap in BEGIN/COMMIT. -->
Migrations to apply (in order): 20260624000001 (tables+RLS) → 20260624000002 (salvar_avaliacao_entrevista RPC) → 20260624000003 (pontuar_cognitivo RPC) → 20260624000004 (avancar_etapa flag guard).
Prompt hydration source: docs/conhecimento/prompts/templates/04-interview-guide.md + 05-transcript-analysis.md (system + user_template). UPDATE prompt_versions SET system_template/user_template (dollar-quoted execute_sql) + is_active=true WHERE call_type IN ('interview_guide','transcript_analysis') — do NOT touch deployed_at before content (Pitfall 6).
EF deploy: supabase functions deploy gerar-guia-entrevista + avaliar-transcricao-entrevista (JWT-on for both — RH-invoked; auto-bundles _shared).
Cognitive items seed: SEED_ITENS_RACIOCINIO (from 14-02) → cognitivo_itens (+ gabarito server-only).
Types regen: npm run db:types (writes database.types.ts at repo ROOT, NOT src/types/).
</interfaces>
</context>

<tasks>

<task type="auto" gate="blocking-human">
  <name>Task 1: [BLOCKING] Apply migrations via MCP + hydrate/activate prompts + seed cognitive items + regen types</name>
  <files>database.types.ts</files>
  <read_first>
    - CLAUDE.md §Commands (D-22 apply workaround — MCP apply_migration for PL/pgSQL $$ bodies; db:types regen)
    - supabase/migrations/20260624000001_entrevista_cognitivo_tables.sql ... 20260624000004_avancar_etapa_flag_guard.sql (the 4 authored migrations from 14-03)
    - docs/conhecimento/prompts/templates/04-interview-guide.md + docs/conhecimento/prompts/templates/05-transcript-analysis.md (the real prompt bodies to hydrate)
    - .planning/phases/13-redacao-cultural-revisao-humana/13-04-PLAN.md (the culture_fit_essay hydration+activation precedent — content-first, Pitfall 6 ordering)
  </read_first>
  <action>Apply the 4 migrations to live PROD in order via Supabase MCP `apply_migration` (D-22 — writes the version row, bypasses 42601; do NOT use db push; do NOT wrap in BEGIN/COMMIT). First confirm via a live SELECT that the two prompt rows are still `is_active=false` with placeholder content (Assumptions A1). Hydrate them: `execute_sql` UPDATE `prompt_versions` SET system_template + user_template (dollar-quoted, from 04-interview-guide.md / 05-transcript-analysis.md) + is_active=true WHERE call_type IN ('interview_guide','transcript_analysis') — content/activation BEFORE/without touching `deployed_at` (immutability trigger locks template/hash after deployed_at — Pitfall 6). Seed the cognitive items from `SEED_ITENS_RACIOCINIO` into `cognitivo_itens` (+ the server-only gabarito). Regenerate `database.types.ts` via `npm run db:types`. If MCP apply_migration is blocked by the classifier, surface the AskUserQuestion authorization (precedent: Phase 7/8 needed an explicit allow-rule for `mcp__supabase__execute_sql`).</action>
  <verify>
    <automated>grep -c "entrevista_guias\|cognitivo_itens\|aplica_cognitivo\|pontuar_cognitivo\|salvar_avaliacao_entrevista" database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `database.types.ts` contains `entrevista_guias`, `entrevista_analises`, `cognitivo_itens`, `cognitivo_respostas`, `aplica_cognitivo`, `pontuar_cognitivo`, `salvar_avaliacao_entrevista` (≥7 matches)
    - A live SELECT shows `prompt_versions` rows for `interview_guide` + `transcript_analysis` with `is_active=true` and a non-placeholder `system_template` (left(system_template,40) is not '[SEED PLACEHOLDER]')
    - `cognitivo_itens` has ≥20 live rows across both `secao` values
    - All 4 migration version rows are recorded (apply_migration wrote them)
  </acceptance_criteria>
  <done>The 4 migrations are live, both prompts are hydrated+active (content-first), cognitive items seeded, types regenerated.</done>
</task>

<task type="auto" gate="blocking-human">
  <name>Task 2: [BLOCKING] Deploy both EFs JWT-on + run the live SQL smokes</name>
  <files>.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-VALIDATION.md</files>
  <read_first>
    - .planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-VALIDATION.md (the SQL-smoke runbook from 14-01 — the 7 smoke blocks)
    - supabase/functions/gerar-guia-entrevista/index.ts + supabase/functions/avaliar-transcricao-entrevista/index.ts (the EFs to deploy)
    - .planning/phases/13-redacao-cultural-revisao-humana/13-04-PLAN.md (the EF-deploy + JWT-on + curl-no-auth-401 smoke precedent)
  </read_first>
  <action>Deploy both EFs via CLI `supabase functions deploy gerar-guia-entrevista` and `supabase functions deploy avaliar-transcricao-entrevista` (JWT-on for both — RH-invoked; auto-bundles `_shared`). Run the live SQL smokes from `14-VALIDATION.md` against PROD (use a disposable fixture + `set_config('request.jwt.claims', ...)` to simulate RH/candidato roles, ROLLBACK-free cleanup — precedent Phase 8/11): (1) flag-block — a candidatura with an entrevista_analises bloqueio_avanco=true + revisao_confirmada_em IS NULL → UPDATE candidaturas.etapa_atual past entrevista_online RAISEs; after setting revisao_confirmada_em → advance succeeds; (2) pontuar_cognitivo non-owner → 42501; (3) never-auto-reject — pontuar_cognitivo writes scores_candidato but ZERO candidaturas rows change; (4) candidate-DENY RLS — a candidato JWT SELECT on entrevista_guias/entrevista_analises returns 0 rows / denied; (5) aplica_cognitivo opt-in — a vaga with aplica_cognitivo=false produces no cognitive invite path; (6) salvar_avaliacao_entrevista non-owner RH → 42501; (7) anon curl to each EF → 401 (JWT-on behavioral evidence). Record PASS/FAIL per smoke in 14-VALIDATION.md and flip `nyquist_compliant: true` once the implementation tests + smokes are green.</action>
  <verify>
    <automated>N=$(grep -cE "PASS|✓" .planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-VALIDATION.md); test "$N" -ge 7 && ! grep -qiE "FAIL|✗" .planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-VALIDATION.md && echo "OK $N smokes PASS, zero FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Both EFs deployed (CLI reports ACTIVE); anon curl to each → 401 (JWT-on)
    - All 7 SQL smokes recorded PASS in 14-VALIDATION.md (flag-block, RPC denial, never-auto-reject, candidate-DENY RLS, opt-in gate, override-RPC denial, EF 401)
    - The flag-block smoke proves the block is server-authoritative inside avancar_etapa (not just UI) — RNF-07a / RF-24
    - No candidaturas row is auto-mutated by any scoring path (RNF-07a holds)
  </acceptance_criteria>
  <done>Both EFs are live JWT-on; all 7 live SQL smokes PASS; RNF-07a confirmed live; nyquist_compliant flipped true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → PROD schema | apply_migration mutates live PROD; high blast radius (human-gated) |
| anon → deployed EF | both EFs JWT-on; anon request must 401 at the gateway |
| candidato JWT → new tables | candidate-DENY RLS must hold live |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Tampering | live migration apply | mitigate | MCP apply_migration (D-22) writes the version row + bypasses 42601; human checkpoint gates the PROD mutation; no BEGIN/COMMIT wrapper |
| T-14-04-02 | Information disclosure | new tables live | mitigate | live candidate-DENY RLS smoke (smoke #4) proves a candidato JWT cannot read guias/analises/scores |
| T-14-04-03 | Repudiation/legal | scoring paths live | mitigate | live never-auto-reject smoke (smoke #3) proves no candidaturas row is auto-mutated (RNF-07a) |
| T-14-04-04 | Spoofing | EF auth | mitigate | both EFs deployed JWT-on; anon curl → 401 smoke (smoke #7); role-from-usuarios_rh in the EF body |
| T-14-04-05 | Tampering | prompt immutability | mitigate | hydrate content + is_active=true BEFORE touching deployed_at (Pitfall 6 ordering); verified via live SELECT |
| T-14-04-SC | Tampering | EF deploy bundle | mitigate | CLI deploy auto-bundles _shared (no .join bug — verified deno check in 14-03); anon-401 smoke confirms JWT-on |
</threat_model>

<verification>
- `database.types.ts` regenerated with the new tables/columns/RPCs
- both prompt rows hydrated + is_active=true (content-first, Pitfall 6)
- both EFs deployed JWT-on; anon → 401
- all 7 SQL smokes PASS in 14-VALIDATION.md; nyquist_compliant: true
- RNF-07a holds live (no auto-mutation of candidaturas)
- commit via `git -c core.hooksPath=/dev/null`
</verification>

<success_criteria>
- The Phase-14 server core is LIVE in PROD (migrations + prompts + EFs + seed)
- The language/accent flag block is server-authoritative (proven by the live flag-block smoke)
- Cognitive scoring never auto-rejects (proven by the live smoke)
- Candidate-DENY RLS + opt-in gate hold live
- The UI waves (14-05/06) can build against live types + live endpoints
</success_criteria>

<output>
Create `.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-04-SUMMARY.md` when done
</output>
