---
phase: 14-entrevistas-com-ia-companion-etapas-4-5
plan: 05
type: execute
wave: 4
depends_on: ["14-04"]
files_modified:
  - src/features/entrevista/services/entrevistaService.ts
  - src/features/entrevista/hooks/useEntrevistaScorecard.ts
  - src/features/entrevista/components/EntrevistaWorkspace.tsx
  - src/features/entrevista/components/EntrevistaDashboard.tsx
  - src/features/entrevista/components/GuiaEntrevistaPanel.tsx
  - src/features/entrevista/components/EntrevistaScorecardInline.tsx
  - src/features/entrevista/components/TranscricaoReviewPanel.tsx
  - src/features/entrevista/components/CognitivoBandCard.tsx
  - src/router/routes.tsx
autonomous: true
requirements: [ENTREV-01, ENTREV-02, ENTREV-03, ENTREV-04, ENTREV-05]
must_haves:
  truths:
    - "The RH gestor enters /rh/candidato/:id/entrevista and lands on the Painel do candidato tab — dashboard + 24h marker + guide + inline scorecard + transcript review (ENTREV-02)"
    - "Every AI-derived block carries SugestaoIABadge ('Sugestão da IA — decisão é sempre humana'); the candidate never reaches any RH surface (RNF-07a)"
    - "The language/accent flag block disables the Avançar etapa CTA until Confirmar revisão humana (ENTREV-03 / RF-24, resolved Q2)"
    - "The cognitive band is RH-only, CONTEXTUAL/não-eliminatório, never red/green; reject-by-cognitive-alone forces expanded justification + bias_audit_log (ENTREV-05)"
    - "All transcript/notes/score reads use an explicit allowlist projection, NEVER select('*') (per reference_select_star_leaks_pii)"
  artifacts:
    - path: "src/features/entrevista/services/entrevistaService.ts"
      provides: "Allowlist reads + RPC writes (salvar_avaliacao_entrevista) + EF invokes; custom error class"
    - path: "src/features/entrevista/components/EntrevistaWorkspace.tsx"
      provides: "RHLayout tabs host (Painel/Guia/Transcrição/Avaliação), default landing Painel do candidato"
      min_lines: 40
    - path: "src/features/entrevista/components/TranscricaoReviewPanel.tsx"
      provides: "Transcript paste → analyze → BARS dims + flag-block on avancar_etapa"
    - path: "src/features/entrevista/components/CognitivoBandCard.tsx"
      provides: "RH-only CONTEXTUAL cognitive band + reject-by-cognitive bias_audit_log gate"
  key_links:
    - from: "src/features/entrevista/services/entrevistaService.ts"
      to: "salvar_avaliacao_entrevista"
      via: "supabase.rpc"
      pattern: "rpc\\('salvar_avaliacao_entrevista'"
    - from: "src/router/routes.tsx"
      to: "EntrevistaWorkspace"
      via: "RoleGuard role=['rh','administrador']"
      pattern: "/rh/candidato/:id/entrevista"
---

<objective>
Build the RH/gestor interview workspace at `/rh/candidato/:id/entrevista` — the `RHLayout`-shelled tabs host (Painel do candidato / Guia de entrevista / Análise da transcrição / Avaliação da entrevista) with the candidate dashboard + 24h marker, the STAR/PEI guide panel (online/presencial generate CTAs), the inline-editable BARS scorecard (notas_humanas via the `salvar_avaliacao_entrevista` RPC), the transcript-analysis review panel (paste → analyze → BARS dims + the language/accent flag that blocks `avancar_etapa()`), and the RH-only CONTEXTUAL cognitive band card with the reject-by-cognitive bias_audit_log gate.

Purpose: This is the gestor-facing surface for ENTREV-01..05. Every component clones a PROD-green Phase 13 analog (`RedacaoReviewPanel`, `RedacaoOverrideForm`, `ScorecardAvaliacao`, `SugestaoIABadge`, `RevisaoHumanaMarker`). The UI honors RNF-07a (AI is always a suggestion, the human decides) and the anti-regional-bias flag block (RF-24). All reads use the allowlist projection (LGPD — transcript/notes are PII).

Output: the `src/features/entrevista/` feature (service/hooks/6 components) + the RH route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-UI-SPEC.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-PATTERNS.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-04-SUMMARY.md

<interfaces>
<!-- Reused VERBATIM — do NOT re-author: -->
SugestaoIABadge (src/features/triagem/components/SugestaoIABadge.tsx) — 'Sugestão da IA — decisão é sempre humana', #35BFAD, Sparkles. On EVERY AI-derived block. RH panel only.
ScorecardAvaliacao (src/features/avaliacao/components/ScorecardAvaliacao.tsx) — neutral structured scorecard + the "Contextual · não-eliminatório" badge (border-white/15 bg-white/5 text-white/70) + RevisaoHumanaMarker (CircleDashed pill).
RedacaoOverrideForm + RedacaoReviewPanel (src/features/triagem/components/) — BARS Slider rows + mandatory notes + decision + SugestaoIABadge two-column layout + RHLayout tabs host.
revisaoRedacaoService (src/features/triagem/services/) — allowlist reads (NEVER select('*')) + supabase.rpc + error-code map (42501→FORBIDDEN, 23514→INVALID_INPUT, P0002→NOT_FOUND).
useRedacaoRevisao (src/features/triagem/hooks/) — hierarchical query keys + useQuery(staleTime 5min, retry 2, enabled:!!id) + mutation invalidation.

Live (from 14-04): salvar_avaliacao_entrevista RPC, gerar-guia-entrevista EF, avaliar-transcricao-entrevista EF, entrevista_guias/entrevista_analises tables, vagas.entrevista_agendada_em + aplica_cognitivo, scores_candidato tipo='entrevista'/'cognitivo'.
The :id route param is a candidatura id → resolve its vaga via getVagaIdForCandidatura (RedacaoReviewPanel.tsx:147-153 analog).
Copy contract: 14-UI-SPEC §Copywriting Contract (verbatim pt-BR strings — Workspace heading "Entrevista", tabs, flag-block copy, CONTEXTUAL band, reject-by-cognitive dialog).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: entrevistaService (allowlist + RPC + EF invokes) + hooks + the allowlist test GREEN</name>
  <files>src/features/entrevista/services/entrevistaService.ts, src/features/entrevista/hooks/useEntrevistaScorecard.ts</files>
  <read_first>
    - src/features/triagem/services/revisaoRedacaoService.ts (exact analog — EntrevistaServiceError class, ENTREVISTA_ALLOWLIST const, supabase.rpc, error-code map; lines 23-37, 80-92, 137-153, 231-280)
    - src/features/triagem/hooks/useRedacaoRevisao.ts (query-key namespace + useQuery + mutation invalidation; lines 27-67)
    - src/features/entrevista/__tests__/entrevista-allowlist.test.ts (the RED test from 14-01 — flip GREEN by shipping the allowlist read)
    - database.types.ts (regenerated in 14-04 — the live table/RPC types; drop any `as never` casts now that types exist)
  </read_first>
  <behavior>
    - entrevistaService reads guide/transcript-analysis/scorecard/cognitive-band via an EXPLICIT column allowlist (never select('*')) — the 14-01 allowlist test flips GREEN
    - salvarAvaliacao(candidaturaId, scoresHumanos, notas) calls supabase.rpc('salvar_avaliacao_entrevista', ...) and maps 42501→FORBIDDEN, 23514→INVALID_INPUT, P0002/no_data_found→NOT_FOUND, else→NETWORK_ERROR
    - gerarGuia(candidaturaId, vagaId, tipo) and analisarTranscricao(candidaturaId, transcricao) invoke the live EFs; the client body parses under the 14-01 schemas (contract test stays GREEN)
    - confirmarRevisaoHumana sets entrevista_analises.revisao_confirmada_em (unblocks avancar_etapa) via an allowlisted update / RPC
  </behavior>
  <action>Create `entrevistaService.ts` cloning `revisaoRedacaoService.ts`: an `EntrevistaServiceError` class, an `ENTREVISTA_ALLOWLIST` const (explicit columns for guias/analises/scorecard/cognitive — NEVER '*' per [[reference_select_star_leaks_pii]]), allowlisted reads, `salvarAvaliacao` via `supabase.rpc('salvar_avaliacao_entrevista', { p_candidatura_id, p_scores_humanos, p_notas })` with the error-code map, `gerarGuia`/`analisarTranscricao` via `supabase.functions.invoke` of the two EFs, and `confirmarRevisaoHumana` (sets revisao_confirmada_em). Drop any `as never` casts now that `database.types.ts` is regenerated (14-04). Create `useEntrevistaScorecard.ts` (+ `useGuiaEntrevista`, `useTranscricaoAnalise` in the same file or a hooks barrel) cloning `useRedacaoRevisao`: an `entrevistaKeys` query-key namespace, `useQuery(staleTime 5min, retry 2, enabled:!!id)`, and mutations that invalidate the relevant key on success.</action>
  <verify>
    <automated>npm run test:run -- entrevista-allowlist && npm run test:run -- entrevista-contract</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "select\\('\\*'\\)" src/features/entrevista/services/entrevistaService.ts` returns 0 (no select('*'))
    - `grep -cE "ENTREVISTA_ALLOWLIST|allowlist" src/features/entrevista/services/entrevistaService.ts` ≥1
    - `grep -cE "rpc\\('salvar_avaliacao_entrevista'" src/features/entrevista/services/entrevistaService.ts` ≥1
    - `npm run test:run -- entrevista-allowlist` GREEN (the 14-01 RED test now passes)
    - `useEntrevistaScorecard` uses staleTime 5min + retry 2 + a query-key namespace
    - no `as never` cast remains in entrevistaService.ts
  </acceptance_criteria>
  <done>The service (allowlist reads + RPC/EF writes) + hooks ship; the allowlist + contract tests are GREEN; no select('*'); no as never casts.</done>
</task>

<task type="auto">
  <name>Task 2: EntrevistaWorkspace tabs host + EntrevistaDashboard (24h marker) + GuiaEntrevistaPanel + EntrevistaScorecardInline + route</name>
  <files>src/features/entrevista/components/EntrevistaWorkspace.tsx, src/features/entrevista/components/EntrevistaDashboard.tsx, src/features/entrevista/components/GuiaEntrevistaPanel.tsx, src/features/entrevista/components/EntrevistaScorecardInline.tsx, src/router/routes.tsx</files>
  <read_first>
    - src/features/triagem/components/RedacaoReviewPanel.tsx (RHLayout shell + tabs + per-AI-block SugestaoIABadge + :id→vaga resolution; lines 23-44, 60-119, 147-153, 202-228)
    - src/features/triagem/components/RedacaoOverrideForm.tsx (BARS Slider rows + mandatory notes + SugestaoIABadge; lines 196-315)
    - src/features/avaliacao/components/ScorecardAvaliacao.tsx (neutral structured presentation — the base for the inline scorecard)
    - src/router/routes.tsx (the existing /rh/candidato/:id/redacao RH route block to clone; lines ~349-356)
    - .planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-UI-SPEC.md (Copywriting Contract + Color §24h marker rules + Component Inventory)
  </read_first>
  <action>Create `EntrevistaWorkspace.tsx`: an `RHLayout` + `Glass` tabs host with 4 tabs (Painel do candidato / Guia de entrevista / Análise da transcrição / Avaliação da entrevista), DEFAULT landing tab = **Painel do candidato** (UI-SPEC primary anchor), role-gated, resolving the :id candidatura → its vaga. Create `EntrevistaDashboard.tsx`: etapa label + manually-scheduled datetime (`calendar` + time `select` writing vagas/candidatura `entrevista_agendada_em`) + the computed **24h marker** pill (amber `bg-amber-500/15 text-amber-300 border-amber-400/30` when <24h, neutral `border-white/20 bg-white/5 text-white/80` when ≥24h/unscheduled; `tooltip` = exact datetime) — pure client-side date math, NO email/calendar wiring (CONTEXT). Create `GuiaEntrevistaPanel.tsx`: renders the `gerar-guia-entrevista` output (5-7 STAR/PEI perguntas, BARS 1-5 anchors + dimensão) with `online`/`presencial` generate CTAs (presencial focus note "gaps score<4"), per-question weak-dim hint ("Cobre {dimensão} (score atual {n} < 3)"), header `SugestaoIABadge variant="full"`. Create `EntrevistaScorecardInline.tsx`: BARS `Slider` rows per competência (notas_humanas) + optional notes `textarea` + "Salvar avaliação" via `salvarAvaliacao` (the RPC), built on `ScorecardAvaliacao`'s neutral presentation (no red/green tint). Add the route `/rh/candidato/:id/entrevista` wrapped in `RoleGuard role={['rh','administrador']}` in routes.tsx. Use the verbatim pt-BR copy from 14-UI-SPEC §Copywriting Contract. Reuse `SugestaoIABadge`/`ScorecardAvaliacao` VERBATIM — do not re-author.</action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -cE "/rh/candidato/:id/entrevista" src/router/routes.tsx && grep -cE "SugestaoIABadge" src/features/entrevista/components/GuiaEntrevistaPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `/rh/candidato/:id/entrevista` route exists wrapped in `RoleGuard role={['rh','administrador']}`
    - `EntrevistaWorkspace` defaults to the Painel do candidato tab (grep the default tab value)
    - `EntrevistaDashboard` computes the 24h marker client-side (amber <24h / neutral ≥24h) with a tooltip naming the datetime
    - `GuiaEntrevistaPanel` carries `SugestaoIABadge variant="full"` and shows online/presencial CTAs + the per-question weak-dim hint
    - `EntrevistaScorecardInline` uses BARS Sliders + the salvar_avaliacao_entrevista RPC; no red/green tint on scores
    - `npm run test:run -- forbidden-strings.grep` GREEN
  </acceptance_criteria>
  <done>The workspace (tabs + dashboard + 24h marker + guide panel + inline scorecard) + the RH route ship; AI blocks carry SugestaoIABadge; copy is verbatim from UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: TranscricaoReviewPanel (flag-block on avancar_etapa) + CognitivoBandCard (CONTEXTUAL + bias_audit_log gate)</name>
  <files>src/features/entrevista/components/TranscricaoReviewPanel.tsx, src/features/entrevista/components/CognitivoBandCard.tsx</files>
  <read_first>
    - src/features/triagem/components/RedacaoReviewPanel.tsx (per-dimension {v}/5 + SugestaoIABadge variant="compact" + the 16px/1.6 reading idiom; lines 60-141)
    - src/features/avaliacao/components/ScorecardAvaliacao.tsx (the "Contextual · não-eliminatório" badge + RevisaoHumanaMarker pill; lines 48-55, 245-303)
    - .planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-UI-SPEC.md (§Color RH flag signals + §Copywriting Contract flag-block + RH cognitive band gate + reject-by-cognitive dialog)
    - database.types.ts (bias_audit_log columns: dados jsonb, periodo, snapshot_em, criado_em)
  </read_first>
  <action>Create `TranscricaoReviewPanel.tsx`: a transcript paste `textarea` (label "Cole a transcrição da entrevista") → "Analisar transcrição" calls `analisarTranscricao` → renders the BARS dimensions ({competência} — {score}/5, each with `SugestaoIABadge variant="compact"`) + citations + flags; the transcript + citations read at `text-base leading-relaxed` (16px/1.6+, never compressed). When the **language/accent flag** fires (score<3 — from the EF's bloqueio_avanco), render the destructive-tinted block (`bg-red-500/15 text-red-300 border-red-400/30`) + a `RevisaoHumanaMarker` + a tooltip naming the rule, and **disable the "Avançar etapa" CTA**; the only enabled path is "Confirmar revisão humana" (calls `confirmarRevisaoHumana` → sets revisao_confirmada_em → unblocks). Use the verbatim flag-block copy from 14-UI-SPEC. Create `CognitivoBandCard.tsx`: RH-only cognitive band display reusing the `ScorecardAvaliacao` "Contextual · não-eliminatório" badge VERBATIM — descriptive band ("Banda {n} de 5 — {rótulo}"), NO red/green tint, a tooltip "Contextual — nunca elimina sozinho. Decisão sempre humana.". The reject-by-cognitive-alone path forces an `alert-dialog` requiring an expanded justification `textarea` (obrigatória) and writes a `bias_audit_log` row (dados jsonb { candidatura, banda, motivo, flag_demografico }) before the rejection — copy verbatim from 14-UI-SPEC §RH cognitive band gate. The candidate NEVER sees this card (RH panel only).</action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -cE "Confirmar revisão humana|confirmarRevisaoHumana" src/features/entrevista/components/TranscricaoReviewPanel.tsx && grep -cE "Contextual|não-eliminatório|bias_audit_log" src/features/entrevista/components/CognitivoBandCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `TranscricaoReviewPanel` disables the "Avançar etapa" CTA when the language/accent flag fires and enables it only after "Confirmar revisão humana" (grep both)
    - The transcript reads at `text-base leading-relaxed` (never compressed below 16px)
    - Each BARS dimension carries `SugestaoIABadge variant="compact"`
    - `CognitivoBandCard` uses the "Contextual · não-eliminatório" badge verbatim, has no red/green tint on the band, and writes `bias_audit_log` on reject-by-cognitive (with the expanded justification gate)
    - `npm run test:run -- forbidden-strings.grep` GREEN (no "teste psicológico"/"QI")
  </acceptance_criteria>
  <done>The transcript review panel (flag-block on avancar_etapa) + the RH-only CONTEXTUAL cognitive band card (with the bias_audit_log reject gate) ship; RNF-07a + RF-24 honored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH client → service reads | transcript/notes/scores are PII; reads must use the allowlist projection |
| RH UI → funil advance | the flag block must gate the Avançar CTA (defense-in-depth over the server guard) |
| candidate → RH surface | the candidate must never reach any RH component (route role-gate + RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Information disclosure | entrevistaService reads | mitigate | explicit allowlist projection (never select('*')); the 14-01 allowlist test asserts the network select; RLS is row-level only ([[reference_select_star_leaks_pii]]) |
| T-14-05-02 | Elevation | /rh/candidato/:id/entrevista | mitigate | RoleGuard role=['rh','administrador']; the candidate route role-gate + candidate-DENY RLS keep candidates off the RH surface |
| T-14-05-03 | Repudiation/legal | reject-by-cognitive | mitigate | the cognitive band is CONTEXTUAL/não-eliminatório; reject-by-cognitive forces an expanded justification + a bias_audit_log row (RNF-07a / RF-27) |
| T-14-05-04 | Tampering (fairness) | flag-block CTA | mitigate | the Avançar CTA is disabled on the language/accent flag; the real block is the server guard in avancar_etapa (14-03) — UI is defense-in-depth (RF-24) |
| T-14-05-SC | Tampering | npm installs | mitigate | zero new packages; all shadcn primitives already vendored (UI-SPEC Registry Safety) |
</threat_model>

<verification>
- `npm run build` exits 0; tsc baseline ≤305
- `npm run test:run -- entrevista-allowlist` + `entrevista-contract` GREEN
- `npm run test:run -- forbidden-strings.grep` GREEN
- The RH route is RoleGuard-gated; every AI block carries SugestaoIABadge; no select('*'); no red/green on scores/band
- commit via `git -c core.hooksPath=/dev/null`
</verification>

<success_criteria>
- The RH gestor workspace at /rh/candidato/:id/entrevista ships (dashboard + 24h marker + guide + inline scorecard + transcript review + cognitive band)
- ENTREV-01..05 have an RH-facing surface; ENTREV-02's dashboard + inline scorecard + 24h marker are live
- The language/accent flag block gates the Avançar CTA (RF-24)
- The cognitive band is CONTEXTUAL with the bias_audit_log reject gate (ENTREV-05)
- All reads use the allowlist; no select('*'); RNF-07a honored throughout
</success_criteria>

<output>
Create `.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-05-SUMMARY.md` when done
</output>
