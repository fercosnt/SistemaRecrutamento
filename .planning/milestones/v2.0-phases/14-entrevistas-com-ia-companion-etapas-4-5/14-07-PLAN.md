---
phase: 14-entrevistas-com-ia-companion-etapas-4-5
plan: 07
type: execute
wave: 6
gap_closure: true
depends_on: ["14-03", "14-04", "14-05", "14-06"]
files_modified:
  - src/features/entrevista/services/entrevistaService.ts
  - src/features/entrevista/components/EntrevistaWorkspace.tsx
  - src/features/entrevista/components/EntrevistaScorecardInline.tsx
  - src/features/entrevista/components/TranscricaoReviewPanel.tsx
  - src/features/entrevista/components/EntrevistaDashboard.tsx
  - src/features/avaliacao-cognitiva/services/cognitivoService.ts
  - src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx
  - supabase/functions/avaliar-transcricao-entrevista/_local/derive-flags.ts
  - supabase/functions/avaliar-transcricao-entrevista/index.ts
  - supabase/migrations/20260625000001_phase14_gap_closure.sql
autonomous: true
requirements: [ENTREV-03, ENTREV-04, ENTREV-05]
must_haves:
  truths:
    - "CR-04: transcript competencies written by the EF reach every RH consumer — the read layer normalizes the EF's `competency` (EN) key to `competencia` (PT) so EntrevistaScorecardInline + TranscricaoReviewPanel render AI-seeded values (ENTREV-03). The EF write key is NOT renamed (gerar-guia's weakDimsFromScores keeps reading `competency` — IN-03)."
    - "CR-03 + WR-07: 'Confirmar revisão humana' routes through a SECURITY DEFINER RPC `confirmar_revisao_entrevista(p_analise_id)` that enforces role + vaga ownership and sets `revisao_confirmada_em`; the client no longer does a direct UPDATE on the SELECT-only table; the service asserts the marker landed (no silent 0-row success)."
    - "CR-02: the candidate's raw picks + proctoring context ARE persisted to `cognitivo_respostas` (server-side inside `pontuar_cognitivo`), and `submitProva` passes the collected shuffleSeed + proctoring events — the disclosure 'registramos quando a aba perde o foco' is now truthful."
    - "CR-01: `pontuar_cognitivo` RAISES (no_data_found) when the item bank is empty (v_n_total = 0) instead of silently persisting a misleading na_media score=0/score_max=0 row; the CC0 content seed stays deferred (cc0-cognitive-item-bank-sourcing.md)."
    - "WR-04: the RH read path (getAnalise/getScores/getGuia/getEntrevistaContexto) is vaga-ownership scoped at the DB — a recrutador (role='rh') reads interview/cognitive data ONLY for vagas they own; administrador sees all (closes the horizontal-access gap)."
    - "WR-06: deriveLanguageAccentFlag treats a missing `bias_flags` on an LLM competency entry as non-firing (never throws) so the EF still persists an analysis row (never-absent-persist invariant holds)."
    - "RNF-07a preserved: no AI/score path auto-rejects; the cognitive 'reject' button is relabeled to audit-only (the real rejection is the Phase-15 auditable decision) — WR-03."
  artifacts:
    - path: "supabase/migrations/20260625000001_phase14_gap_closure.sql"
      provides: "pontuar_cognitivo v_n_total=0 guard + cognitivo_respostas persist (CR-01/CR-02); confirmar_revisao_entrevista RPC (CR-03); vaga-scoped SELECT RLS (WR-04)"
      min_lines: 40
    - path: "src/features/entrevista/services/entrevistaService.ts"
      provides: "competencia normalization (CR-04), confirmar RPC reroute + readback (CR-03/WR-07), client 200-char guard + VALIDATION surfacing (WR-05)"
  key_links:
    - from: "src/features/entrevista/services/entrevistaService.ts"
      to: "confirmar_revisao_entrevista"
      via: "supabase.rpc"
      pattern: "rpc\\('confirmar_revisao_entrevista'"
    - from: "supabase/migrations/20260625000001_phase14_gap_closure.sql"
      to: "cognitivo_respostas"
      via: "INSERT inside pontuar_cognitivo"
      pattern: "INSERT INTO[\\s\\n]+public\\.cognitivo_respostas"
---

<objective>
Close the Phase 14 code-review blockers + high-value warnings (REVIEW.md: CR-01, CR-02, CR-03, CR-04, WR-04, WR-05, WR-06, WR-07, WR-03, IN-01, IN-04). Author all DB changes as ONE new migration file (do NOT apply to PROD — the orchestrator applies via Supabase MCP after you return). Fix the EF + client/service + UI code. Do NOT seed CC0 cognitive items (content deferral stays open).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-REVIEW.md
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
Read REVIEW.md first — it has exact file:line + fix guidance for every finding. The migrations already in PROD are `20260624000001..04`; your new file is `20260625000001_phase14_gap_closure.sql` and must be IDEMPOTENT-friendly (CREATE OR REPLACE FUNCTION; DROP POLICY IF EXISTS before CREATE POLICY). Follow the project's no-BEGIN/COMMIT-wrapper migration authoring convention (CLAUDE.md). The candidate-readable answer-key rule: NEVER expose gabarito; cognitivo_respostas stores raw picks only.

PROD-APPLY BOUNDARY: you author `.sql` as a FILE only. Do NOT run `supabase db push`, `apply_migration`, or any MCP/CLI apply — the orchestrator owns PROD apply + EF redeploy with the user's authorization. Mention in SUMMARY that the migration is authored-but-unapplied and the EF needs redeploy.

Commit convention: `git -c core.hooksPath=/dev/null commit ...` (allowlisted; the pre-commit tsc hook trips on the ~291-error baseline — do NOT try to drive tsc to 0). Do NOT touch the 3 pre-existing unstaged paths that are not yours (`src/components/pages/LoginRHPage.tsx`, `.planning/phases/11-*/11-HUMAN-UAT.md`, `.planning/ui-reviews/`). Stage only your own files with explicit `git add <path>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code-layer fixes — CR-04 normalize, CR-03/WR-07 RPC reroute, WR-05 guard, WR-03/WR-02/IN-01/IN-04 cleanups, WR-06 EF guard</name>
  <files>src/features/entrevista/services/entrevistaService.ts, src/features/entrevista/components/EntrevistaWorkspace.tsx, src/features/entrevista/components/EntrevistaScorecardInline.tsx, src/features/entrevista/components/TranscricaoReviewPanel.tsx, src/features/entrevista/components/EntrevistaDashboard.tsx, src/features/avaliacao-cognitiva/services/cognitivoService.ts, src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx, supabase/functions/avaliar-transcricao-entrevista/_local/derive-flags.ts</files>
  <read_first>
    - .planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-REVIEW.md (all findings — exact file:line)
    - src/features/entrevista/services/entrevistaService.ts (getAnalise, confirmarRevisaoHumana, analisarTranscricao, registrarRejeicaoCognitiva)
    - supabase/functions/avaliar-transcricao-entrevista/index.ts + _local/derive-flags.ts
  </read_first>
  <action>
  - CR-04: in entrevistaService read (getAnalise + any consumer of `entrevista_analises.competencias`/`citacoes`/`bias_flags`), normalize the EF's English `competency` key to `competencia` (e.g. `{ competencia: c.competencia ?? c.competency, score: c.score ?? null, ...c }`). Do this in the SERVICE READ LAYER ONLY — do NOT rename the EF write and do NOT change gerar-guia's weakDimsFromScores reader (IN-03: that consumer correctly keys on `competency`).
  - CR-03 + WR-07: change `confirmarRevisaoHumana` to call `supabase.rpc('confirmar_revisao_entrevista', { p_analise_id: analiseId })` (the RPC is authored in Task 2). Remove the direct `.from('entrevista_analises').update(...)`. Fix the false docstring (no review-fields trigger exists). After the RPC, assert success (RPC returns the updated row or a boolean); throw NOT_FOUND when nothing came back. No silent 0-row success.
  - WR-05: in `analisarTranscricao`, mirror the EF's MIN_TRANSCRICAO_LEN=200 floor client-side with a specific pt-BR message, and stop collapsing the EF `error_code:'VALIDATION'` into NETWORK_ERROR — surface validation distinctly.
  - WR-06: in derive-flags.ts `deriveLanguageAccentFlag`, guard `bias_flags`: `const bf = c.bias_flags ?? {}; const fires = (c.score ?? 5) < 3 && bf.regional_markers_ignored === false;` — missing field = non-firing, never throw.
  - WR-03: relabel the cognitive reject affordance in EntrevistaWorkspace + its handler so it no longer promises a rejection ("Registrar e rejeitar" → "Registrar no log de auditoria" and matching toast). It stays audit-only (the real auditable rejection is Phase 15). Keep the bias_audit_log write.
  - WR-02: wire `onAvancarEtapa` in EntrevistaWorkspace to TranscricaoReviewPanel. If no funil-advance service exists yet in this codebase, DISABLE the CTA with a tooltip "Avanço de etapa na decisão final" rather than leaving a dead button that silently no-ops (do not invent a new advance RPC here — that is Phase 15's surface). Pick whichever matches what exists; document the choice.
  - IN-01: in cognitivoService.submitProva remove the impossible `String(status) === '42501'` disjunct; collapse the duplicate-toast if/else in ProvaCognitivaScreen.handleSubmit. (The `_shuffleSeed`/proctoring params become USED in Task 2 — keep them.)
  - IN-04: in EntrevistaDashboard, format the timestamptz with explicit `timeZone: 'America/Sao_Paulo'` via Intl.DateTimeFormat (or document local-zone). Low-risk; keep it small.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && npm run test:run -- entrevista 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "competencia\s*\?\?\s*.*competency|competency.*competencia" src/features/entrevista/services/entrevistaService.ts` shows the normalization
    - `grep -cE "rpc\('confirmar_revisao_entrevista'" src/features/entrevista/services/entrevistaService.ts` ≥1 AND the direct `.update({ revisao_confirmada_em` is gone
    - `grep -cE "from\('entrevista_analises'\)[\s\S]*\.update\(" src/features/entrevista/services/entrevistaService.ts` returns 0
    - derive-flags.ts no longer reads `.bias_flags.` without a null-guard
    - `npm run build` exits 0; tsc baseline ≤305 (do not regress)
  </acceptance_criteria>
  <done>The competency mismatch, the silent-no-op confirmation, the EF throw-on-missing-field, and the cleanups are fixed in code; confirmation now calls the RPC authored in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: New migration file (authored, NOT applied) + wire submitProva persistence — CR-01, CR-02, CR-03, WR-04</name>
  <files>supabase/migrations/20260625000001_phase14_gap_closure.sql, src/features/avaliacao-cognitiva/services/cognitivoService.ts, src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx</files>
  <read_first>
    - supabase/migrations/20260624000003_pontuar_cognitivo_rpc.sql (the live RPC body to CREATE OR REPLACE)
    - supabase/migrations/20260624000001_entrevista_cognitivo_tables.sql (cognitivo_respostas schema + the SELECT/back-lock RLS; entrevista_analises/scores_candidato/guia SELECT policies at lines ~145-206; the candidaturas→vagas FK chain for ownership join)
    - supabase/migrations/20260624000002_salvar_avaliacao_entrevista_rpc.sql (the SECURITY DEFINER + role/ownership idiom to mirror for confirmar_revisao_entrevista)
  </read_first>
  <action>Author `supabase/migrations/20260625000001_phase14_gap_closure.sql` (no BEGIN/COMMIT wrapper, CREATE OR REPLACE + DROP POLICY IF EXISTS for idempotency):
  - CR-01: inside `pontuar_cognitivo`, after computing `v_n_total`, add `IF v_n_total = 0 THEN RAISE EXCEPTION 'prova cognitiva sem itens configurados' USING ERRCODE = 'no_data_found'; END IF;` BEFORE any INSERT into scores_candidato (refuse to persist a misleading na_media row).
  - CR-02: inside the same `pontuar_cognitivo` (CREATE OR REPLACE the whole function), persist the candidate's raw picks + proctoring to `public.cognitivo_respostas` (INSERT keyed by candidatura — raw_responses, shuffle_seed, completion_time_seconds, proctoring jsonb). Respect the existing back-lock (don't overwrite a locked row; upsert-or-skip per the existing etapa-gated policy). Keep it raw-picks-only (NEVER store gabarito/score in cognitivo_respostas). Add the function params needed to carry shuffle_seed + proctoring if not already present (keep `.strict` body-schema compatibility — coordinate with SubmitCognitivoBodySchema; if the schema needs a field, add it).
  - CR-03: new `confirmar_revisao_entrevista(p_analise_id uuid) RETURNS ... SECURITY DEFINER` — mirror salvar_avaliacao_entrevista's auth: resolve role from usuarios_rh; if role='rh' require the analise's candidatura→vaga `created_by = auth.uid()`; administrador bypasses ownership; set `revisao_confirmada_em = now()`; RETURN the updated row (or true). REVOKE from public/anon, GRANT EXECUTE to authenticated.
  - WR-04: scope the SELECT policies on `entrevista_analises`, the interview/cognitive `scores_candidato` reads, and `entrevista_guia` (and entrevista contexto reads) so role='rh' only sees rows whose candidatura→vaga `created_by = auth.uid()` (administrador sees all). DROP POLICY IF EXISTS the broad role-only SELECT, CREATE the ownership-scoped one. Keep candidate-DENY intact. Verify the join path (candidatura_id → candidaturas.vaga_id → vagas.created_by).
  - Wire `submitProva(candidaturaId, respostas, shuffleSeed, proctoringEvents)` in cognitivoService to actually pass shuffleSeed + the proctoring events collected by useProctoring; update ProvaCognitivaScreen.handleSubmit to read them from the hook and pass them. Keep the body matching the (possibly extended) SubmitCognitivoBodySchema.
  Do NOT apply this migration anywhere. Do NOT seed cognitivo_itens.</action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -cE "INSERT INTO[[:space:]]+public\.cognitivo_respostas|CREATE OR REPLACE FUNCTION public\.confirmar_revisao_entrevista|no_data_found" supabase/migrations/20260625000001_phase14_gap_closure.sql</automated>
  </verify>
  <acceptance_criteria>
    - the migration file CREATE OR REPLACEs pontuar_cognitivo with the v_n_total=0 guard AND a cognitivo_respostas persist
    - the migration defines confirmar_revisao_entrevista SECURITY DEFINER with role+ownership checks, REVOKE public/anon + GRANT authenticated
    - the migration DROP-POLICY-IF-EXISTS + CREATEs vaga-scoped SELECT policies (entrevista_analises + scores + guia), administrador-bypass, candidate-DENY preserved
    - submitProva passes shuffleSeed + proctoring events (no longer underscore-dead)
    - `npm run build` exits 0
    - NO `supabase db push`/apply/MCP was run (file authored only)
  </acceptance_criteria>
  <done>One idempotent migration file carries CR-01/CR-02/CR-03/WR-04; the client persists proctoring; nothing applied to PROD (orchestrator applies + redeploys EF after).</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; tsc baseline ≤305
- `npm run test:run -- entrevista` and `-- prova-cognitiva` GREEN (no regressions)
- migration authored as a file only; EF + client fixes committed
- SUMMARY notes: migration UNAPPLIED + EF `avaliar-transcricao-entrevista` needs redeploy (orchestrator does both)
- commit via `git -c core.hooksPath=/dev/null`; only own files staged
</verification>

<success_criteria>
- Phase 14 review blockers CR-01..CR-04 + WR-04/05/06/07 + WR-03/02 + IN-01/04 addressed
- competency scores reach the RH UI; confirmation goes through an authorized RPC with readback; cognitive picks+proctoring persist; empty-bank scoring refuses; RH read path is vaga-scoped; EF never throws on missing bias_flags
- RNF-07a preserved (no auto-reject); CC0 content seed stays deferred
</success_criteria>

<output>
Create `.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-07-SUMMARY.md` when done
</output>
