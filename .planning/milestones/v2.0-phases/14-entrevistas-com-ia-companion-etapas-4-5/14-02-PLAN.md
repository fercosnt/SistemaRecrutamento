---
phase: 14-entrevistas-com-ia-companion-etapas-4-5
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/conhecimento/cognitivo/LICENSE-CC0.md
  - docs/conhecimento/cognitivo/itens-raciocinio-cc0.json
  - docs/conhecimento/cognitivo/README.md
  - supabase/functions/_shared/cognitivo/item-bank.ts
autonomous: false
requirements: [ENTREV-05]
must_haves:
  truths:
    - "A CC0 (public-domain) cognitive item bank exists in-repo with its license committed — matrices + letter-number series, NEVER the legacy Raven .webp blobs (ENTREV-05, per the cc0_item_bank_dependency)"
    - "The item bank defines a stable ItemRaciocinio contract { id, secao, enunciado, alternativas[], gabarito_idx } that the seed migration (14-03) and scorer consume"
    - "The gabarito (superKey60) is committed server-side only and is never shipped to the candidate (RNF-07a / anti-tamper)"
  artifacts:
    - path: "docs/conhecimento/cognitivo/LICENSE-CC0.md"
      provides: "CC0 license + attribution for the item dataset (RNF-C3)"
    - path: "docs/conhecimento/cognitivo/itens-raciocinio-cc0.json"
      provides: "The CC0 item content (matriz + letra-número) + gabarito"
    - path: "supabase/functions/_shared/cognitivo/item-bank.ts"
      provides: "Typed ItemRaciocinio contract + the seed-items export consumed by 14-03"
  key_links:
    - from: "supabase/functions/_shared/cognitivo/item-bank.ts"
      to: "docs/conhecimento/cognitivo/itens-raciocinio-cc0.json"
      via: "imports/embeds the committed CC0 item content"
      pattern: "ItemRaciocinio"
---

<objective>
Source and commit the CC0 (creative-commons-zero / public-domain) cognitive reasoning item bank — matrices + letter-number series from the Harvard Dataverse ICAR dataset (doi:10.7910/DVN/TZJGAT) + the superKey60 gabarito — with its CC0 license committed, and define the stable `ItemRaciocinio` typed contract that the seed migration (14-03), the scorer (14-01), and the candidate prova (14-06) all consume.

Purpose: ENTREV-05 cannot ship without a seeded item bank, and there is NO in-repo CC0 item content (PATTERNS "No Analog Found"). The legacy Raven `.webp` blobs in git history MUST NOT be reused (PRD-cognitivo Q-C5 — CC0 items only; license-incompatible + adverse-impact). This is a content-acquisition step that crosses a license boundary (download + verify CC0 + commit), so it is a human checkpoint (autonomous:false): the executor prepares everything, then the user confirms the CC0 download/commit before it lands.

Output: the committed CC0 item JSON + LICENSE-CC0.md + README attribution + a typed `item-bank.ts` exposing the `ItemRaciocinio` contract and a `SEED_ITENS_RACIOCINIO` export for the 14-03 seed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-RESEARCH.md
@.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-CONTEXT.md

<interfaces>
<!-- The item-model the cognitive prova shell needs (PRD-cognitivo §8.5):
     legacy QuestaoRaven (1 composite image per item) → ItemRaciocinio (enunciado + discrete alternatives). -->
ItemRaciocinio { id: string; secao: 'matriz' | 'letra_numero'; enunciado: string; alternativas: string[]; gabarito_idx: number }
Sections: matriz + letra_numero. CC0 source: Harvard Dataverse doi:10.7910/DVN/TZJGAT (ICAR Matrix Reasoning + ICAR Letter-Number). Gabarito: superKey60 (SAPA, CC0). Target ~25-30 items (PRD-cognitivo §1.4 / §8.5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the ItemRaciocinio typed contract + the seed export skeleton</name>
  <files>supabase/functions/_shared/cognitivo/item-bank.ts, docs/conhecimento/cognitivo/README.md</files>
  <read_first>
    - docs/prds/m2-funil-rh/PRD-cognitivo-raciocinio.md §8.5 (item bank: matriz + letra-número, CC0 source/doi, superKey60 gabarito, ~25-30 items, bucket + LICENSE-CC0 + atribuição)
    - supabase/functions/_shared/cognitivo/scoring.ts (the scorer from 14-01 — the item-bank gabarito feeds scoreRaciocinio's gabarito arg)
    - docs/conhecimento/big-five/ (the docs/conhecimento sub-folder structure precedent for committing knowledge assets)
  </read_first>
  <action>Create `supabase/functions/_shared/cognitivo/item-bank.ts` exporting the typed `ItemRaciocinio` interface ({ id, secao: 'matriz'|'letra_numero', enunciado, alternativas, gabarito_idx }) and the `SEED_ITENS_RACIOCINIO: ItemRaciocinio[]` constant (which the 14-03 seed migration reads to populate `cognitivo_itens`). Also export a `buildGabarito(itens): Record<string,number>` helper (id → gabarito_idx) the scorer consumes server-side. Until the CC0 content lands in Task 2, `SEED_ITENS_RACIOCINIO` may be an empty/placeholder array with a doc-comment marking it [PENDING CC0 DOWNLOAD — Task 2]. Author `docs/conhecimento/cognitivo/README.md` documenting the bucket convention (`cognitivo-itens`), the CC0-only rule (NEVER the Raven blobs — cite PRD Q-C5), and where the license + attribution live.</action>
  <verify>
    <automated>deno check supabase/functions/_shared/cognitivo/item-bank.ts 2>/dev/null || deno test supabase/functions/_shared/cognitivo/item-bank.ts --allow-none 2>/dev/null; test -f docs/conhecimento/cognitivo/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "interface ItemRaciocinio|ItemRaciocinio =" supabase/functions/_shared/cognitivo/item-bank.ts` ≥1
    - `grep -cE "SEED_ITENS_RACIOCINIO" supabase/functions/_shared/cognitivo/item-bank.ts` ≥1
    - `grep -cE "buildGabarito" supabase/functions/_shared/cognitivo/item-bank.ts` ≥1
    - README documents the CC0-only rule and forbids the Raven .webp blobs
  </acceptance_criteria>
  <done>The ItemRaciocinio contract + seed-export skeleton + buildGabarito helper exist; README documents the CC0 convention.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [CHECKPOINT] Download + verify + commit the CC0 item content & license</name>
  <files>docs/conhecimento/cognitivo/itens-raciocinio-cc0.json, docs/conhecimento/cognitivo/LICENSE-CC0.md, supabase/functions/_shared/cognitivo/item-bank.ts</files>
  <read_first>
    - docs/prds/m2-funil-rh/PRD-cognitivo-raciocinio.md §8.5 + §Out of Scope (ICAR60-as-instrument forbidden; ONLY CC0 avulse items from the dataset; superKey60 gabarito)
    - docs/conhecimento/cognitivo/README.md (the convention authored in Task 1)
  </read_first>
  <what-built>The executor (a) prepares the download instructions for the ICAR CC0 dataset (Harvard Dataverse doi:10.7910/DVN/TZJGAT — Matrix Reasoning + Letter-Number Series + superKey60 gabarito), (b) transcribes ~25-30 CC0 items into `itens-raciocinio-cc0.json` shaped as `ItemRaciocinio[]` (matriz + letra_numero, enunciado + discrete alternativas + gabarito_idx), (c) writes `LICENSE-CC0.md` (the CC0 1.0 deed text + dataset attribution per RNF-C3), and (d) wires `SEED_ITENS_RACIOCINIO` in item-bank.ts to the committed JSON. The candidate-facing flow will NEVER receive `gabarito_idx` (server-only).</what-built>
  <how-to-verify>
    1. Confirm the items come from the CC0 dataset (doi:10.7910/DVN/TZJGAT), NOT the non-commercial icar-project.com bank, and NOT the legacy Raven `.webp` blobs (PRD Q-C5).
    2. Confirm `LICENSE-CC0.md` carries the CC0 1.0 deed + attribution.
    3. Confirm `itens-raciocinio-cc0.json` has ≥20 items across both `secao` values with valid `gabarito_idx` (0-based index into alternativas).
    4. Confirm `gabarito_idx` is present ONLY in the committed JSON / item-bank.ts (server-side), never echoed to a candidate surface.
    5. If the dataset is unavailable/blocked at execute time, the executor surfaces the exact download URL + manual steps for you to fetch and drop the file, then re-run.
  </how-to-verify>
  <action>Acquire the CC0 ICAR items (Harvard Dataverse doi:10.7910/DVN/TZJGAT) + superKey60 gabarito; transcribe ~25-30 items into `docs/conhecimento/cognitivo/itens-raciocinio-cc0.json` as `ItemRaciocinio[]` (both `secao` values); commit `LICENSE-CC0.md` (CC0 1.0 + attribution, RNF-C3); wire `SEED_ITENS_RACIOCINIO` in `item-bank.ts` to import/embed the committed JSON. NEVER use the Raven `.webp` blobs. If the autonomous download fails (license gate / network), STOP and present the exact download URL + manual drop-in steps as the checkpoint — do not fabricate items.</action>
  <verify>
    <automated>test -f docs/conhecimento/cognitivo/itens-raciocinio-cc0.json && test -f docs/conhecimento/cognitivo/LICENSE-CC0.md && node -e "const a=require('./docs/conhecimento/cognitivo/itens-raciocinio-cc0.json'); if(!Array.isArray(a)||a.length<20) process.exit(1); const secs=new Set(a.map(i=>i.secao)); if(!secs.has('matriz')||!secs.has('letra_numero')) process.exit(1); for(const i of a){ if(typeof i.gabarito_idx!=='number'||!Array.isArray(i.alternativas)) process.exit(1);} console.log('OK '+a.length+' items');"</automated>
  </verify>
  <acceptance_criteria>
    - `itens-raciocinio-cc0.json` parses as `ItemRaciocinio[]` with ≥20 items spanning both `matriz` and `letra_numero`, each with a numeric `gabarito_idx` and an `alternativas` array
    - `LICENSE-CC0.md` exists with the CC0 1.0 deed + dataset attribution
    - `SEED_ITENS_RACIOCINIO` in item-bank.ts is wired to the committed content (no longer the [PENDING] placeholder)
    - No `.webp` Raven blob is referenced anywhere in the new files
  </acceptance_criteria>
  <resume-signal>Type "approved" once the CC0 items + license are committed and verified, or paste the manual download steps you completed.</resume-signal>
  <done>The CC0 item bank + license are committed in-repo; SEED_ITENS_RACIOCINIO is populated; the gabarito stays server-side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external dataset → repo | CC0 item content + gabarito crosses a license boundary; only CC0 (commercial-OK) assets may land |
| item bank → candidate | the gabarito_idx must stay server-side; only enunciado + alternativas reach the candidate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Information disclosure | gabarito (answer key) | mitigate | gabarito_idx committed server-side only (JSON + item-bank.ts); the candidate prova (14-06) projects only enunciado + alternativas; never echoed to a candidate surface |
| T-14-02-02 | Repudiation (legal) | item license | mitigate | ONLY CC0 dataset items (doi:10.7910/DVN/TZJGAT) + LICENSE-CC0.md committed (RNF-C3); blocking human checkpoint verifies provenance; the non-commercial icar-project.com bank and Raven .webp blobs are forbidden |
| T-14-02-SC | Tampering | content acquisition | mitigate | human checkpoint confirms the source/provenance before commit; no package install (zero new packages this phase) |
</threat_model>

<verification>
- The committed JSON parses as `ItemRaciocinio[]` (≥20 items, both sections, valid gabarito_idx)
- `LICENSE-CC0.md` + attribution present (RNF-C3)
- `item-bank.ts` exposes `ItemRaciocinio`, `SEED_ITENS_RACIOCINIO`, `buildGabarito`
- No reference to the legacy Raven `.webp` blobs
- `npm run test:run -- forbidden-strings.grep` GREEN (no "teste psicológico"/"QI" in the new cognitive assets)
</verification>

<success_criteria>
- A CC0 cognitive item bank exists in-repo with its license committed
- The `ItemRaciocinio` contract is stable and consumed by the 14-03 seed + the 14-01 scorer + the 14-06 prova
- The gabarito is server-side only
- The legacy Raven blobs are NOT reused
</success_criteria>

<output>
Create `.planning/phases/14-entrevistas-com-ia-companion-etapas-4-5/14-02-SUMMARY.md` when done
</output>
