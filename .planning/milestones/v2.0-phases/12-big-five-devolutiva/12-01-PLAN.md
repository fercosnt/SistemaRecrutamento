---
phase: 12-big-five-devolutiva
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/bigfive-scoring.test.ts
  - supabase/functions/submit-bigfive-final/index.test.ts
  - supabase/functions/gerar-devolutiva-bigfive/index.test.ts
  - src/features/avaliacao/__tests__/bigfive-contract.test.ts
  - tests/forbidden-strings.grep.test.ts
  - .planning/phases/12-big-five-devolutiva/12-SMOKES.md
autonomous: true
requirements: [AVAL-04, AVAL-08]
must_haves:
  truths:
    - "Every Phase-12 production surface (scorer, both EFs, client contract, devolutiva) has a calibrated RED test that fails before its implementation lands (smoke-runtime gate, Phase-4 lesson)"
    - "The reverse-key set test asserts exactly 55 ids and the per-domain counts N7 E6 O12 A17 C13 (Pitfall 1 — D-12-AVAL-04 traceability)"
    - "The client↔EF contract test asserts the exact client Likert body parses in SubmitBigfiveFinalBodySchema and that .strict rejects an extra score field (D-12-AVAL-04 anti-tamper)"
    - "The LGPD-04 forbidden-strings grep guard covers the new 08-bigfive-devolutiva.md prompt template (D-12-AVAL-08 traceability)"
    - "A SQL smoke runbook enumerates the item-reader projection, candidate-deny on scores, candidate own-row devolutiva, and RH allowlist checks"
  artifacts:
    - path: "supabase/functions/_shared/bigfive-scoring.test.ts"
      provides: "RED scorer tests — reverse-set size/per-domain + golden-vector percentiles"
      min_lines: 40
    - path: "supabase/functions/submit-bigfive-final/index.test.ts"
      provides: "RED EF tests — auth/IDOR/etapa 403, never-touch-candidaturas, neutral payload"
      min_lines: 30
    - path: "supabase/functions/gerar-devolutiva-bigfive/index.test.ts"
      provides: "RED EF tests — band select, RF-19b guard, word-count retry/degrade"
      min_lines: 30
    - path: "src/features/avaliacao/__tests__/bigfive-contract.test.ts"
      provides: "client↔EF body contract test"
      min_lines: 20
    - path: ".planning/phases/12-big-five-devolutiva/12-SMOKES.md"
      provides: "SQL smoke runbook for the [BLOCKING] apply wave"
  key_links:
    - from: "tests/forbidden-strings.grep.test.ts"
      to: "docs/conhecimento/prompts/templates/08-bigfive-devolutiva.md"
      via: "glob include in the LGPD-04 grep guard"
      pattern: "08-bigfive-devolutiva"
---

<objective>
Author the Wave-0 RED test battery + the LGPD-04 grep extension + the SQL smoke runbook for Phase 12, BEFORE any implementation lands. This is the smoke-runtime gate (Phase-4 lesson D-25..D-28): every downstream surface gets a calibrated failing test first.

Purpose: De-risk the #1 scoring bug source (reverse-key transcription, Pitfall 1) and the anti-tamper contract (Pitfall 3) by pinning expectations as executable RED tests. Tests COMPILE under TS/Deno strict but FAIL at runtime (module-not-found) against the not-yet-written code — the failure IS the assertion.

Output: 4 RED test files + 1 extended grep guard + 1 SQL smoke runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-big-five-devolutiva/12-RESEARCH.md
@.planning/phases/12-big-five-devolutiva/12-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scorer + EF + contract RED tests</name>
  <files>supabase/functions/_shared/bigfive-scoring.test.ts, supabase/functions/submit-bigfive-final/index.test.ts, supabase/functions/gerar-devolutiva-bigfive/index.test.ts, src/features/avaliacao/__tests__/bigfive-contract.test.ts</files>
  <read_first>
    - `.planning/phases/12-big-five-devolutiva/12-RESEARCH.md` §Pattern Map (reverse-key IDs L268; per-domain counts; golden-vector strategy Pitfall 1) + §Validation Architecture (Test Map rows)
    - `docs/conhecimento/big-five/PESQUISA-big-five-ipip-neo-120-ptbr.md` L285-413 (55 reverse IDs, facet formula, T/percentile cubic, Python reference scorer to derive the golden expected values)
    - `supabase/functions/_shared/avaliacao-schemas.ts` L108-114 (`AvaliarRedacaoBodySchema` analog for the `.strict` body contract)
    - `src/features/avaliacao/services/avaliacaoService.ts` L251-322 (`pontuarSjt`/`avaliarRedacao` — the client submit shape analog)
    - `supabase/functions/_shared/pii-masker.test.ts` or any existing `deno test` file under `supabase/functions/_shared/` for the Deno test harness import style (`deno.land/std@0.224.0/assert`)
  </read_first>
  <action>
    Create `supabase/functions/_shared/bigfive-scoring.test.ts` (Deno test, import from `./bigfive-scoring.ts` which does not yet exist). Assert: (1) `REVERSED.size === 55`; (2) per-domain reverse counts N=7 E=6 O=12 A=17 C=13 by filtering the set through `FACET_TO_DOMAIN`; (3) `facetOf(id) === ((id-1)%30)+1` spot checks; (4) a golden test: feed a fixed 120-response vector (all-3 neutral plus one hand-built variant) into `score(respostas, norm_group)` and assert the exact 5 domain raws + the 5 percentiles + the 5 bands — compute the expected values once from the PESQUISA Python reference (cite the source line for each). These are RED (module-not-found) until 12-02 lands.
    Create `supabase/functions/submit-bigfive-final/index.test.ts` (Deno, mocked supabaseUser/supabaseAdmin deps per the Phase-9 dependency-injection precedent): assert the handler returns 403 when `auth.uid()` does not own the candidatura; 403 when `etapa_atual !== 'avaliacao_assincrona'`; that on success it INSERTs `scores_candidato` with `tipo='big_five'` and `status='sucesso'` and NEVER calls any `.from('candidaturas')` write (RNF-07a, D-12-AVAL-04); that the response body is the neutral `{ ok: true }` shape (never a score). RED until 12-03.
    Create `supabase/functions/gerar-devolutiva-bigfive/index.test.ts` (Deno, mocked callAi): assert band selection maps a given percentil to the correct 1-of-5 band per dim (cutoffs ≤15/16-35/36-64/65-84/≥85); the RF-19b guard refuses when the precondition score row `tipo !== 'big_five'`; word-count out-of-range triggers 1 retry then graceful-degrades to the raw template. RED until 12-04.
    Create `src/features/avaliacao/__tests__/bigfive-contract.test.ts` (Vitest, import the client-built body + `SubmitBigfiveFinalBodySchema`): assert the exact client Likert body (`{ candidatura_id: uuid, respostas: {1..120 → 1..5} }`) parses, that `.strict` REJECTS an extra `score` field, and that a 119-key or out-of-range value fails ([[feedback_integration_contract_gap]], Pitfall 3). RED until 12-02 ships the schema.
  </action>
  <verify>
    <automated>deno test supabase/functions/_shared/bigfive-scoring.test.ts 2>&1 | grep -qi "not found\|error" && echo RED-OK; npm run test:run -- bigfive-contract 2>&1 | grep -qi "fail\|cannot find" && echo CONTRACT-RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `bigfive-scoring.test.ts` exists; asserts `REVERSED.size === 55` and per-domain N7/E6/O12/A17/C13 literally; golden vector pins 5 raws + 5 percentiles + 5 bands with cited source lines.
    - `submit-bigfive-final/index.test.ts` asserts 403 non-owner, 403 wrong-etapa, `status='sucesso'` insert, zero `candidaturas` write, neutral `{ ok:true }`.
    - `gerar-devolutiva-bigfive/index.test.ts` asserts band cutoffs, RF-19b guard, word-count retry→degrade.
    - `bigfive-contract.test.ts` asserts `.strict` rejects extra `score` + the 120×1-5 happy body parses.
    - All four files FAIL at runtime (module/schema not found) — the calibrated RED contract.
  </acceptance_criteria>
  <done>4 RED test files exist; each fails because its target implementation does not yet exist (smoke-runtime gate honored).</done>
</task>

<task type="auto">
  <name>Task 2: LGPD-04 grep extension + SQL smoke runbook</name>
  <files>tests/forbidden-strings.grep.test.ts, .planning/phases/12-big-five-devolutiva/12-SMOKES.md</files>
  <read_first>
    - `tests/forbidden-strings.grep.test.ts` (the Phase-9 LGPD-04 guard — its glob/path list) — find via `grep -rn "forbidden" tests/ src/ supabase/ 2>/dev/null | grep -i grep`
    - `.planning/phases/12-big-five-devolutiva/12-RESEARCH.md` §Validation Architecture (Wave 0 Gaps — the smoke runbook checklist) + §Pitfall 4/7
    - `docs/conhecimento/big-five/templates-devolutiva.md` L241 ("Sensibilidade Emocional" not "Neuroticismo"), L42-44 (disclaimers)
    - `.planning/phases/08-*/08-*SMOKES*.md` or any prior SQL smoke runbook for the format (set_config request.jwt.claims pattern from STATE.md)
  </read_first>
  <action>
    Extend the existing Phase-9 LGPD-04 forbidden-strings grep guard (`tests/forbidden-strings.grep.test.ts` or its actual path) so its include-glob covers `docs/conhecimento/prompts/templates/08-bigfive-devolutiva.md` — the guard must fail only if a forbidden term ("teste psicológico", "diagnóstico", "transtorno", "neuroticismo" as a candidate-facing label) appears. Use `grep -v '^#' | grep -c` hygiene; do not add a bare `== 0` gate on unfiltered content. The new template file does not yet exist → the glob simply has no match yet (guard stays green); the assertion lands once 12-04 authors the template.
    Create `.planning/phases/12-big-five-devolutiva/12-SMOKES.md` — the SQL smoke runbook the [BLOCKING] apply wave (12-06) runs against live PROD. Enumerate, with the literal SQL and expected result for each: (1) `get_bigfive_itens()` returns rows projecting ONLY item_id/texto/ordem (no dimensao/faceta/reverse_keyed column present); (2) candidate JWT (set_config request.jwt.claims) SELECT on `scores_candidato` returns 0 rows / denied (candidate has NO read policy); (3) candidate own-row SELECT on `devolutivas_candidato` returns their row, a different candidate's uuid returns 0; (4) RH/'administrador' JWT reads both via allowlist; (5) `bigfive_itens` seed count = 120 and reverse_keyed count = 55. Each smoke = a fixture+ROLLBACK-free cleanup note per the Phase-8 precedent.
  </action>
  <verify>
    <automated>npm run test:run -- forbidden-strings 2>&1 | grep -qi "pass\|✓" && echo GREEN; test -f .planning/phases/12-big-five-devolutiva/12-SMOKES.md && grep -qc "get_bigfive_itens" .planning/phases/12-big-five-devolutiva/12-SMOKES.md && echo RUNBOOK-OK</automated>
  </verify>
  <acceptance_criteria>
    - The LGPD-04 grep guard's path/glob list literally includes `08-bigfive-devolutiva.md`; guard passes now (no file yet) and will assert once the template lands.
    - `12-SMOKES.md` exists with the 5 enumerated SQL smokes, each with literal SQL + expected result + the set_config jwt-claims pattern for the candidate/RH cases.
  </acceptance_criteria>
  <done>Grep guard covers the future prompt template; SQL smoke runbook authored for the apply wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test authoring → CI | RED tests must be calibrated (fail for the right reason), not syntax-broken |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | client-sent score | mitigate | contract test asserts `.strict` rejects an extra `score` field BEFORE the EF exists |
| T-12-02 | Information Disclosure | answer-key (reverse/dim) | mitigate | smoke runbook asserts `get_bigfive_itens` projects only item_id/texto/ordem |
| T-12-03 | Repudiation/compliance | LGPD-04 clinical language | mitigate | grep guard pre-wired to cover the new prompt template |
| T-12-SC | Tampering | npm/deno installs | accept | no new packages this phase (RESEARCH §Package Legitimacy: N/A) |
</threat_model>

<verification>
- `deno test` on the three EF/scorer test files all FAIL (module not found) — calibrated RED.
- `npm run test:run -- bigfive-contract` FAILS (schema not found) — calibrated RED.
- `npm run test:run -- forbidden-strings` PASSES (no template yet, guard glob extended).
- `npm run lint` baseline unchanged (~291); test files under test scope do not grow the tsc baseline beyond noted carryover.
</verification>

<success_criteria>
- 4 RED test files + 1 extended grep guard + 1 SQL smoke runbook committed.
- Every downstream Phase-12 surface has a failing test before its implementation (smoke-runtime gate).
- Reverse-key 55 + per-domain counts pinned; anti-tamper `.strict` contract pinned; RF-19b guard pinned; LGPD-04 grep wired.
</success_criteria>

<output>
Create `.planning/phases/12-big-five-devolutiva/12-01-SUMMARY.md` when done.
</output>
