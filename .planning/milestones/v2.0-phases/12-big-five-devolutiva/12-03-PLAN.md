---
phase: 12-big-five-devolutiva
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - supabase/functions/submit-bigfive-final/index.ts
autonomous: true
requirements: [AVAL-04]
must_haves:
  truths:
    - "submit-bigfive-final authorizes BEFORE acting: auth.getUser() (two-client D-23), validates auth.uid() owns the candidatura AND etapa_atual='avaliacao_assincrona', else 403 ([[reference_ef_authenticate_vs_authorize]]) — D-12-AVAL-04"
    - "The EF re-scores server-side via bigfive-scoring.score() from the client's Likert 1-5 answers only; it NEVER trusts or accepts a client score (.strict body)"
    - "On success the EF INSERTs scores_candidato tipo='big_five' status='sucesso' with the metadata jsonb, and NEVER writes candidaturas / never sets pendente_humano from a trait (RNF-07a, Pitfall 4)"
    - "The EF returns a neutral { ok:true } payload (+ devolutiva_id), never a score; it invokes gerar-devolutiva-bigfive inline and fires the n8n webhook fire-and-forget (D-12-AVAL-04 traceability)"
  artifacts:
    - path: "supabase/functions/submit-bigfive-final/index.ts"
      provides: "candidate-invoked scoring EF (TS-port scorer + authorize-then-act)"
      min_lines: 120
  key_links:
    - from: "supabase/functions/submit-bigfive-final/index.ts"
      to: "supabase/functions/_shared/bigfive-scoring.ts"
      via: "import score()"
      pattern: "from \"\\.\\./_shared/bigfive-scoring"
---

<objective>
Build `submit-bigfive-final` — the candidate-invoked server-side scoring EF — by copying `avaliar-redacao`'s authorize-then-act skeleton and swapping in the `bigfive-scoring` TS-port. Flips the 12-01 submit-EF RED tests GREEN.

Purpose: Anti-tamper (Pitfall 3) + never-reject (Pitfall 4) are the two invariants. The client sends only 1-5 Likert answers; the EF re-scores and persists `scores_candidato` with `status='sucesso'` ALWAYS, never touching `candidaturas`.

Output: one Deno EF (authored, deployed on the [BLOCKING] 12-06 wave).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-big-five-devolutiva/12-RESEARCH.md
@.planning/phases/12-big-five-devolutiva/12-CONTEXT.md

<interfaces>
From supabase/functions/_shared/bigfive-scoring.ts (12-02):
- export function score(respostas, norm_group): { dimensoes, facetas, norm_group }
- export function normGroupFromBirthDate(dob): NormGroup
From supabase/functions/_shared/avaliacao-schemas.ts (12-02):
- export const SubmitBigfiveFinalBodySchema  (.strict, respostas 120×1-5)
scores_candidato (live): tipo='big_five' already in enum; status enum incl 'sucesso'; RH SELECT uses 'administrador'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: submit-bigfive-final EF (authorize-then-act + TS-port scorer)</name>
  <files>supabase/functions/submit-bigfive-final/index.ts</files>
  <read_first>
    - `supabase/functions/avaliar-redacao/index.ts` L148-324 (the EXACT authorize-then-act skeleton: CORS, errorResponse, two-client D-23, Zod safeParse→400, ownership+etapa 403, neutral payload) + L330-363 (Deno.serve prod wiring + SDK npm: join trick)
    - `supabase/functions/_shared/bigfive-scoring.ts` (12-02 — `score()` + `normGroupFromBirthDate()`)
    - `supabase/functions/_shared/avaliacao-schemas.ts` (`SubmitBigfiveFinalBodySchema`)
    - `supabase/functions/submit-bigfive-final/index.test.ts` (the 12-01 RED contract this must satisfy)
    - `.planning/phases/12-big-five-devolutiva/12-RESEARCH.md` §Pattern 1, §Pitfall 4 (never-reject — do NOT copy avaliar-redacao's <13/25 pendente_humano threshold), §Pitfall 7 (redacted logs)
    - `supabase/functions/_shared/audit-logger.ts` (if the score write should be audited — match avaliar-redacao's logging discipline: ids/counts/status only)
  </read_first>
  <action>
    Create `supabase/functions/submit-bigfive-final/index.ts`. Copy `avaliar-redacao`'s skeleton verbatim (CORS headers, `errorResponse`, two-client: `supabaseUser` anon+Authorization for `auth.getUser()` ONLY, `supabaseAdmin` service_role for privileged reads/writes, `Deno.serve` wiring + the SDK `npm:` join trick). Steps in order: (1) `auth.getUser()` → 401 `UNAUTHORIZED` on fail; (2) `SubmitBigfiveFinalBodySchema.safeParse(raw)` → 400 `VALIDATION` on fail, then assert `Object.keys(respostas).length === 120` and ids cover 1..120; (3) `supabaseAdmin.from('candidaturas').select('id, candidato_id, vaga_id, etapa_atual, candidato:...dob')` (allowlist, never `*`) `.eq('id', body.candidatura_id).maybeSingle()` → 403 `FORBIDDEN` if missing OR `candidato_id !== user.id` OR `etapa_atual !== 'avaliacao_assincrona'`; (4) fetch the candidate's birth date (already on the candidato/candidatura join) → `normGroupFromBirthDate()`; (5) `score(body.respostas, norm_group)`; (6) INSERT `scores_candidato` `{ candidatura_id, tipo:'big_five', status:'sucesso', metadata: { dimensoes, facetas, norm_group } }` — NO `candidaturas` write, NO etapa change, NO `pendente_humano` threshold on any trait (RNF-07a, Pitfall 4); (7) invoke `gerar-devolutiva-bigfive` inline (await, gated on the new score row) and capture `devolutiva_id`; on its failure or >10s timeout, fire the n8n email webhook fire-and-forget and continue (RFB-11/24); (8) return neutral `{ ok: true, devolutiva_id }` — NEVER a score. `console.log` only ids/counts/status (Pitfall 7). Flips the 12-01 submit-EF tests GREEN.
  </action>
  <verify>
    <automated>deno test supabase/functions/submit-bigfive-final/index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 403 on non-owner candidatura; 403 on `etapa_atual !== 'avaliacao_assincrona'`; 401 on no/invalid JWT.
    - Body schema `.strict`; rejects extra `score`; requires 120 answers 1..5.
    - On success INSERTs `scores_candidato` tipo='big_five' status='sucesso' with the metadata jsonb; grep confirms ZERO `.from('candidaturas')` write/update and ZERO `pendente_humano` literal.
    - Returns `{ ok:true, devolutiva_id }` (neutral); logs contain no raw respostas/score.
    - The 12-01 `submit-bigfive-final/index.test.ts` suite passes.
  </acceptance_criteria>
  <done>The scoring EF is authored, passes its Wave-0 contract, honors never-reject + anti-tamper; deploy is the [BLOCKING] 12-06 wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| candidate browser → EF | untrusted Likert input crosses here; JWT verified server-side |
| EF (service_role) → DB | bypasses RLS → ownership MUST be checked in code first |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Elevation/Spoofing | submit-bigfive-final auth | mitigate | two-client D-23; `auth.getUser()` verifies JWT; service_role only after authorize ([[reference_ef_authenticate_vs_authorize]]) |
| T-12-09 | Info Disclosure (IDOR) | scoring another's candidatura | mitigate | `candidato_id !== user.id` → 403; etapa gate |
| T-12-10 | Tampering | client-sent score | mitigate | `.strict` body, server-side re-score, no score field |
| T-12-11 | Repudiation/compliance | auto-reject by trait | mitigate | status='sucesso' always; no `candidaturas` write; no trait threshold (RNF-07a) — grep guard |
| T-12-12 | Info Disclosure | score in logs | mitigate | redacted logs (ids/counts/status only), Pitfall 7 |
| T-12-SC | Tampering | npm/deno installs | accept | reuses pinned SDKs from avaliar-redacao; no new packages |
</threat_model>

<verification>
- `deno test submit-bigfive-final/index.test.ts` GREEN.
- `grep -c "from('candidaturas')" supabase/functions/submit-bigfive-final/index.ts` for write/update = 0 (read-only allowlist select is permitted; no insert/update).
- `grep -c "pendente_humano" supabase/functions/submit-bigfive-final/index.ts` = 0.
- `npm run lint` tsc baseline zero-growth (Deno EF outside tsc scope).
</verification>

<success_criteria>
- Authorize-then-act + TS-port scorer; never writes candidaturas; status='sucesso' always; neutral payload.
- 12-01 submit-EF RED tests flip GREEN.
</success_criteria>

<output>
Create `.planning/phases/12-big-five-devolutiva/12-03-SUMMARY.md` when done.
</output>
