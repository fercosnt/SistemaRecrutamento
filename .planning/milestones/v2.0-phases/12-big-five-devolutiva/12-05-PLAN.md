---
phase: 12-big-five-devolutiva
plan: 05
type: execute
wave: 4
depends_on: ["12-02", "12-03", "12-04"]
files_modified:
  - src/features/avaliacao/schemas/bigfiveSchema.ts
  - src/features/avaliacao/services/bigfiveService.ts
  - src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx
  - src/features/avaliacao/components/DevolutivaBigFiveView.tsx
  - src/features/avaliacao/services/scoresRhService.ts
  - src/router/routes.tsx
autonomous: true
requirements: [AVAL-04, AVAL-08]
must_haves:
  truths:
    - "The candidate answers the 120-item IPIP-NEO-120 PT-BR Likert questionnaire paginated 12x10 in the glass shell, autosaving every 30s via useAutosaveAvaliacao + respostas_avaliacao with back-lock, and NEVER sees a score during (RNF-07a) - D-12-AVAL-04"
    - "The questionnaire reads items via getBigfiveItens (RPC, item_id/texto/ordem only - no scoring key) and submits ONLY Likert 1-5 to submit-bigfive-final; the client body parses in SubmitBigfiveFinalBodySchema (contract test, no `as never`)"
    - "After submit the candidate sees their own devolutiva in-app (5 dims + percentil + band + ~150-200-word text + LGPD disclaimers) via own-row read of devolutivas_candidato - the ONLY place a candidate sees a percentile"
    - "The RH contextual scorecard reuses ScorecardAvaliacao + the scoresRhService allowlist (no select('*')), marked CONTEXTUAL/nao-eliminatorio with the SugestaoIABadge - D-12-AVAL-08"
  artifacts:
    - path: "src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx"
      provides: "120-item paginated Likert questionnaire (glass shell)"
      min_lines: 80
    - path: "src/features/avaliacao/components/DevolutivaBigFiveView.tsx"
      provides: "in-app 5-dim devolutiva view"
      min_lines: 60
    - path: "src/features/avaliacao/services/bigfiveService.ts"
      provides: "getBigfiveItens, submitBigfiveFinal, loadDevolutiva"
      min_lines: 40
  key_links:
    - from: "src/features/avaliacao/services/bigfiveService.ts"
      to: "submit-bigfive-final EF"
      via: "supabase.functions.invoke('submit-bigfive-final')"
      pattern: "submit-bigfive-final"
    - from: "src/router/routes.tsx"
      to: "BigFiveQuestionnaireScreen + DevolutivaBigFiveView"
      via: "route registration under /candidato/avaliacao/:candidaturaId"
      pattern: "bigfive"
---

<objective>
Build the candidate questionnaire + in-app devolutiva view + RH contextual scorecard + the client service/schema + routes - reusing the Phase-11 glass shell, autosave hook, and RH allowlist. Drops the `as never` casts now that the EF types are regenerated.

Purpose: This is the user-facing slice. Candidate answers 120 Likert items (never sees a score), then sees their respectful devolutiva; RH sees a CONTEXTUAL scorecard. The client<->EF contract (12-01) is honored end-to-end (Phase-11 C1/C2 lesson).

Output: 1 schema + 1 service + 2 components + scoresRhService extension + route registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-big-five-devolutiva/12-RESEARCH.md
@.planning/phases/12-big-five-devolutiva/12-CONTEXT.md

<interfaces>
Reuse (Phase 11):
- src/features/avaliacao/hooks/useAutosaveAvaliacao.ts - teste='big_five'; 30s debounce + 42501 back-lock
- src/features/avaliacao/services/avaliacaoService.ts L158-191 getOpcoesSjt (mirror for getBigfiveItens via get_bigfive_itens RPC), L232-244 upsertResposta, L251-322 pontuarSjt (submit shape - neutral ack, 42501->LOCKED throw)
- src/features/avaliacao/services/scoresRhService.ts L86-107 SCORES_ALLOWLIST + ScorecardAvaliacao.tsx (extend for big_five metadata; never '*')
- src/components/pages/DashboardCandidatoPage.tsx (glass shell to copy: BackgroundImage gradient over bg-[#00109E], sticky navbar, GlassCard)
- src/router/routes.tsx L200-228 (avaliacao container + mc/caso pattern)
Client body MUST parse in SubmitBigfiveFinalBodySchema (12-02). Likert PT-BR labels from ipip-neo-120-questions-pt-br.json: Muito inadequado / Relativamente inadequado / Nem adequado, nem inadequado / Relativamente adequado / Muito adequado.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bigfiveSchema + bigfiveService + RH scorecard allowlist</name>
  <files>src/features/avaliacao/schemas/bigfiveSchema.ts, src/features/avaliacao/services/bigfiveService.ts, src/features/avaliacao/services/scoresRhService.ts</files>
  <read_first>
    - `src/features/avaliacao/services/avaliacaoService.ts` L158-191 (getOpcoesSjt RPC consume), L232-244 (upsertResposta), L251-322 (pontuarSjt/avaliarRedacao submit + 42501->LOCKED throw)
    - `src/features/avaliacao/services/scoresRhService.ts` L86-107 (SCORES_ALLOWLIST - extend for big_five metadata, never `select('*')`)
    - `src/features/avaliacao/__tests__/bigfive-contract.test.ts` (12-01 - the client body shape this service must produce)
    - `.planning/phases/12-big-five-devolutiva/12-RESEARCH.md` Pattern Map (Code analogs) + Pitfall 7 (no select('*'))
    - `database.types.ts` (ROOT) - regenerated on the 12-06 apply wave; until then the new tables are absent -> use a narrow local type for big_five metadata and drop any `as never` cast after types regen
  </read_first>
  <action>
    Create `src/features/avaliacao/schemas/bigfiveSchema.ts` - the client Likert form schema (Zod, pt-BR): exactly 120 answers, each `z.number().int().min(1).max(5)`, keyed by item_id; build the submit body to match `SubmitBigfiveFinalBodySchema` (`{ candidatura_id, respostas }`) - the 12-01 contract test asserts this parses.
    Create `src/features/avaliacao/services/bigfiveService.ts` (camelCase, custom error class per convention): `getBigfiveItens()` calls the `get_bigfive_itens` RPC (mirror `getOpcoesSjt` - returns item_id/texto/ordem only); `submitBigfiveFinal(body)` invokes the `submit-bigfive-final` EF (model on `pontuarSjt`: neutral ack, map 42501->LOCKED throw, never expose a score); `loadDevolutiva(candidaturaId)` reads the candidate's own `devolutivas_candidato` row via an explicit allowlist select (never `*`). Use hierarchical query keys.
    Extend `src/features/avaliacao/services/scoresRhService.ts`: add the `big_five` branch to `SCORES_ALLOWLIST` / its types so RH reads the big_five `metadata` (dims/percentil/band) via the existing allowlist - never `select('*')`; mark the row CONTEXTUAL in the returned shape.
  </action>
  <verify>
    <automated>npm run test:run -- bigfive-contract && grep -c "select('\\*')" src/features/avaliacao/services/bigfiveService.ts | grep -qx 0 && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `bigfiveSchema.ts` builds a body that passes `SubmitBigfiveFinalBodySchema` (12-01 contract test GREEN, no `as never`).
    - `bigfiveService.ts` has `getBigfiveItens` (RPC, safe cols), `submitBigfiveFinal` (neutral ack + 42501->LOCKED), `loadDevolutiva` (own-row allowlist); zero `select('*')`.
    - `scoresRhService.ts` reads big_five via allowlist, marked CONTEXTUAL; no `select('*')`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Client schema + service + RH allowlist land; the contract test is GREEN end-to-end; no PII/score leak via select('*').</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BigFiveQuestionnaireScreen + DevolutivaBigFiveView + routes</name>
  <files>src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx, src/features/avaliacao/components/DevolutivaBigFiveView.tsx, src/router/routes.tsx</files>
  <read_first>
    - `src/components/pages/DashboardCandidatoPage.tsx` (glass shell: BackgroundImage gradient + bg-[#00109E] + sticky navbar + GlassCard - copy, do not invent a new shell, D-27)
    - `.planning/phases/12-big-five-devolutiva/12-RESEARCH.md` UI Notes Screen 1/2/3 (the FULL visual/interaction contract - pagination 12x10, Likert labels, autosave affordance, alert-dialog confirm copy, back-lock states verbatim, devolutiva dashboard + 5 pages + disclaimers, "Sensibilidade Emocional", neutral colors not rosa-doce)
    - `src/features/avaliacao/hooks/useAutosaveAvaliacao.ts` (reuse as-is, teste='big_five')
    - `src/features/avaliacao/components/ScorecardAvaliacao.tsx` (RH scorecard to extend/reuse for big_five - mark CONTEXTUAL + SugestaoIABadge)
    - `src/router/routes.tsx` L200-236 (avaliacao container; the legacy `/testes/bigfive` L236 is DEV-only - do NOT reuse for production)
    - `docs/conhecimento/big-five/templates-devolutiva.md` L24-44 (header + disclaimers rendered in the view)
  </read_first>
  <action>
    Create `src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx` per UI Notes Screen 1: copy the DashboardCandidato glass shell; render `getBigfiveItens` results paginated 12 pages x 10; each item = statement (`text-base` 16px) + a 5-point `radio-group` row using the JSON PT-BR labels (selected = `bg-white/30` glass-white, NOT accent); `progress` bar + "{respondidas}/120" neutral count (NO score, RNF-07a); intro page 0 = the PESQUISA intro copy + the emotional disclaimer; Voltar/Avancar nav, last page -> Concluir (alert-dialog confirm "Enviar avaliacao? Apos enviar, voce nao podera editar suas respostas." / "Enviar" / "Revisar"); autosave affordance top-right (Saving.../Salvo automaticamente `#35BFAD`/transient fail) via `useAutosaveAvaliacao` teste='big_five'; Concluir disabled until all 120 answered; submit -> `submitBigfiveFinal` -> toast.success("Avaliacao enviada com sucesso") -> route to devolutiva; back-lock/wrong-etapa states verbatim from UI Notes (neutral, Lock icon).
    Create `src/features/avaliacao/components/DevolutivaBigFiveView.tsx` per UI Notes Screen 2: header dashboard (5 rows: dim name + percentil cru + band + proportional bar; order Abertura, Conscienciosidade, Extroversao, Amabilidade, Sensibilidade Emocional - NEVER "Neuroticismo"); emotional disclaimer above; 5 dimension cards/tabs (title "{Dim}: {Banda}" + percentil + analogy + the ~150-200-word interpretive text from `loadDevolutiva`); fixed LGPD/CFP footer disclaimer on every devolutiva; neutral/professional colors (not rosa-doce, not red/green judgment).
    Register routes in `src/router/routes.tsx` under the Phase-11 avaliacao container: `/candidato/avaliacao/:candidaturaId/bigfive` (questionnaire) + `/candidato/avaliacao/:candidaturaId/bigfive/devolutiva` (view); add the Big Five card to the `/candidato/avaliacao/:id` container. Do NOT touch the legacy DEV `/testes/bigfive` route.
    Wire the RH big_five card into `ScorecardAvaliacao` (or a thin `ScorecardBigFive`) - CONTEXTUAL marker + SugestaoIABadge on AI-derived text; role-gated; reads via the 12-05-Task-1 allowlist.
  </action>
  <verify>
    <automated>npm run build && grep -c "Sensibilidade Emocional" src/features/avaliacao/components/DevolutivaBigFiveView.tsx && grep -ic "neuroticismo" src/features/avaliacao/components/DevolutivaBigFiveView.tsx | grep -qx 0 && echo LGPD-LABEL-OK</automated>
  </verify>
  <acceptance_criteria>
    - Questionnaire: 120 items paginated 12x10, 5-point radio-group with the JSON PT-BR labels, progress "{n}/120" with NO score shown, autosave affordance, Concluir disabled until 120 answered, alert-dialog confirm copy verbatim, back-lock/wrong-etapa states verbatim.
    - Devolutiva view: 5-dim dashboard + 5 cards + percentil + analogy + interpretive text + fixed LGPD/CFP footer; uses "Sensibilidade Emocional" (zero "neuroticismo" candidate-facing); neutral colors.
    - Routes `/candidato/avaliacao/:candidaturaId/bigfive` + `/devolutiva` registered; Big Five card added to the container; legacy `/testes/bigfive` untouched.
    - RH scorecard marked CONTEXTUAL + SugestaoIABadge; reads via allowlist.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Candidate questionnaire + devolutiva view + RH contextual scorecard wired; glass shell reused; routes registered; LGPD-04 label discipline held.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| candidate browser -> RPC/EF | reads only safe item cols; submits only Likert 1-5 |
| RH client -> scores_candidato | role-gated allowlist read; candidate side reads zero score data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-17 | Info Disclosure | answer-key in questionnaire | mitigate | reads via getBigfiveItens RPC (item_id/texto/ordem only); no dim/faceta/reverse client-side |
| T-12-18 | Tampering | client posts a score | mitigate | client body matches `.strict` SubmitBigfiveFinalBodySchema (contract test); 1-5 only |
| T-12-19 | Info Disclosure | score/PII via select('*') | mitigate | explicit allowlists in bigfiveService + scoresRhService ([[reference_select_star_leaks_pii]]) |
| T-12-20 | Info Disclosure | candidate sees a score during questionnaire | mitigate | neutral "{n}/120" only; no score/threshold/pass-fail copy (RNF-07a / UI Notes) |
| T-12-21 | Repudiation/compliance | clinical label in candidate UI | mitigate | "Sensibilidade Emocional" not "Neuroticismo"; "avaliacao comportamental" copy; LGPD-04 grep |
| T-12-SC | Tampering | npm installs | accept | no new packages; all shadcn primitives vendored |
</threat_model>

<verification>
- `npm run test:run -- bigfive-contract` GREEN end-to-end (client body parses; no `as never`).
- `grep -c "select('*')"` in bigfiveService.ts = 0.
- `grep -c "Sensibilidade Emocional"` in DevolutivaBigFiveView.tsx >= 1; `grep -ic "neuroticismo"` = 0.
- `npm run build` exits 0; tsc baseline zero-growth.
</verification>

<success_criteria>
- Candidate answers 120 Likert (never a score), then sees their own devolutiva; RH sees a CONTEXTUAL scorecard.
- Client<->EF contract honored end-to-end; no select('*') leak; LGPD-04 label discipline.
- AVAL-04 + AVAL-08 user-facing slices land.
</success_criteria>

<output>
Create `.planning/phases/12-big-five-devolutiva/12-05-SUMMARY.md` when done.
</output>
