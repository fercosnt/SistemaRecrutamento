---
phase: 12-big-five-devolutiva
plan: 04
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - supabase/functions/gerar-devolutiva-bigfive/index.ts
  - docs/conhecimento/prompts/templates/08-bigfive-devolutiva.md
  - docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts
autonomous: true
requirements: [AVAL-08]
must_haves:
  truths:
    - "gerar-devolutiva-bigfive guards on the precondition score row tipo='big_five' BEFORE generating — refuses any SJT/Redação score (RF-19b, Pitfall 6) — D-12-AVAL-08"
    - "For each of 5 dims it deterministically selects 1-of-5 official band templates by percentil (≤15/16-35/36-64/65-84/≥85), then uses callAi (Phase-9 infra) to ONLY personalize ~150-200 words/dim — never invent; Zod-validated output; retry 1× on word-count miss; graceful-degrade to raw template"
    - "The new bigfive_devolutiva prompt (git→DB, is_active flip on the apply wave) uses 'Sensibilidade Emocional' for N (never 'Neuroticismo'/'teste psicológico'/'diagnóstico' — LGPD-04) and renders the fixed LGPD/CFP disclaimers"
    - "It INSERTs devolutivas_candidato conteudo_jsonb + audit fields via service_role; the in-app devolutiva is the source of truth (n8n email is fire-and-forget) — D-12-AVAL-08"
  artifacts:
    - path: "supabase/functions/gerar-devolutiva-bigfive/index.ts"
      provides: "hybrid template+IA devolutiva EF"
      min_lines: 100
    - path: "docs/conhecimento/prompts/templates/08-bigfive-devolutiva.md"
      provides: "new bigfive_devolutiva prompt (frontmatter + system/user)"
      contains: "bigfive_devolutiva"
  key_links:
    - from: "supabase/functions/gerar-devolutiva-bigfive/index.ts"
      to: "_shared/ai-client.ts (loadPrompt/callAi)"
      via: "loadPrompt('bigfive_devolutiva')"
      pattern: "loadPrompt\\("
---

<objective>
Build `gerar-devolutiva-bigfive` (hybrid 25 band templates + bounded IA polish) + author the new `bigfive_devolutiva` prompt template + its structured-output Zod schema. Flips the 12-01 devolutiva-EF RED tests GREEN.

Purpose: RF-19b (never a devolutiva for non-big_five) + LGPD-04 (no clinical language) are the two compliance invariants. The IA only personalizes the curated CRP-safe templates — it never free-generates interpretive claims (Pitfall 5).

Output: one Deno EF + one prompt template + one shared-schema entry (deploy + is_active flip on the [BLOCKING] 12-06 wave).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-big-five-devolutiva/12-RESEARCH.md
@.planning/phases/12-big-five-devolutiva/12-CONTEXT.md

<interfaces>
From supabase/functions/_shared/ai-client.ts (Phase 9, live):
- loadPrompt(call_type, supabaseAdmin) → active git→DB row
- resolvedPromptFromLoaded(loaded, call_type, fallbackModel)
- callAi({ prompt, rawInput, schema, candidato_id, vaga_id }, { anthropic, openai, supabase }) → structured + audited + cost + retry + fallback
From supabase/functions/_shared/avaliacao-schemas.ts (12-02):
- export const BigfiveDevolutivaSchema  (the structured output shape)
Prompt template frontmatter analog: docs/conhecimento/prompts/templates/07-work-sample-sjt.md (id/call_type: work_sample_sjt; model sonnet; fallback gpt-4o-mini)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: bigfive_devolutiva prompt template + shared Zod schema</name>
  <files>docs/conhecimento/prompts/templates/08-bigfive-devolutiva.md, docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts</files>
  <read_first>
    - `docs/conhecimento/prompts/templates/07-work-sample-sjt.md` L1-40 (frontmatter shape: id/call_type/model/fallback/schema_version_required + system/user sections — model the new one on this)
    - `docs/conhecimento/big-five/templates-devolutiva.md` L10-21 (IA instruction rules — never invent, 150-200 words, no clinical claims), L24-44 (fixed header + LGPD/CFP disclaimers), L48-281 (the 25 band templates), L241 ("Sensibilidade Emocional" not "Neuroticismo")
    - `docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts` (header + SCHEMA_VERSIONS map — add the new entry)
    - `docs/prds/m2-funil-rh/PRD-bigfive-revisado.md` L233 (RFB-15 output schema), L117/235 (audit fields)
    - `.planning/phases/12-big-five-devolutiva/12-RESEARCH.md` §Pattern 3, §Pitfall 5
  </read_first>
  <action>
    Create `docs/conhecimento/prompts/templates/08-bigfive-devolutiva.md` modeled on `07-work-sample-sjt.md`: frontmatter `id: bigfive_devolutiva`, `call_type: bigfive_devolutiva`, `model: claude-sonnet-4-6` (Master default per A4), `fallback: gpt-4o-mini`, `schema_version_required: "1.0.0"`, plus the standard canonical key lines (match the 7 existing templates). System prompt content = the IA instruction rules from templates-devolutiva.md L10-21 (personalize name/cargo/percentil ONLY, 150-200 words/dim, never invent, no clinical claims) + the "Sensibilidade Emocional" rename rule (L241) + the requirement to keep the fixed disclaimers verbatim. User prompt = the per-dimension band template + the candidate's percentil/banda/nome/cargo placeholders. Run the LGPD-04 forbidden-term discipline on the authored text (no "teste psicológico"/"diagnóstico"/"transtorno"/"neuroticismo" as a label).
    Extend `docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts`: add `BigfiveDevolutivaSchema` (mirror the EF-side `BigfiveDevolutivaSchema` shape — `{ cabecalho, paginas:[{dim,banda,percentil,texto_interpretativo,palavras}]×5, disclaimer_emocional, disclaimer_lgpd_crp }`) + a `SCHEMA_VERSIONS['bigfive_devolutiva'] = '1.0.0'` entry; preserve all existing exports.
  </action>
  <verify>
    <automated>test -f docs/conhecimento/prompts/templates/08-bigfive-devolutiva.md && grep -qc "call_type: bigfive_devolutiva" docs/conhecimento/prompts/templates/08-bigfive-devolutiva.md && npm run test:run -- forbidden-strings && grep -qc "bigfive_devolutiva" docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `08-bigfive-devolutiva.md` has `id/call_type: bigfive_devolutiva`, sonnet model + gpt-4o-mini fallback, schema_version_required "1.0.0", and the IA-instruction system prompt + band-template user prompt.
    - The LGPD-04 grep guard (extended in 12-01) PASSES over the new template (no forbidden terms; uses "Sensibilidade Emocional").
    - `00-shared-zod-schemas.ts` has `BigfiveDevolutivaSchema` + the SCHEMA_VERSIONS entry; all prior exports intact.
  </acceptance_criteria>
  <done>The prompt template + shared schema are authored; LGPD-04 guard green; is_active flip deferred to 12-06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gerar-devolutiva-bigfive EF (band select + IA polish + RF-19b guard)</name>
  <files>supabase/functions/gerar-devolutiva-bigfive/index.ts</files>
  <read_first>
    - `supabase/functions/avaliar-redacao/index.ts` L148-363 (the EF skeleton + callAi usage + Deno.serve wiring to copy)
    - `supabase/functions/_shared/ai-client.ts` (loadPrompt / resolvedPromptFromLoaded / callAi signatures)
    - `docs/conhecimento/big-five/templates-devolutiva.md` L48-281 (the 25 templates the EF selects from by band) + L53/61/69/77/85 (band cutoffs per dim)
    - `supabase/functions/gerar-devolutiva-bigfive/index.test.ts` (the 12-01 RED contract: band select, RF-19b guard, word-count retry/degrade)
    - `.planning/phases/12-big-five-devolutiva/12-RESEARCH.md` §Pattern 3, §Pitfall 5/6
  </read_first>
  <action>
    Create `supabase/functions/gerar-devolutiva-bigfive/index.ts` (Deno, copy avaliar-redacao's CORS/errorResponse/two-client/Deno.serve). Inputs: the `scores_candidato` row id (or candidatura_id) it was invoked with — invoked internally by submit-bigfive-final. (1) RF-19b GUARD: load the score row via service_role; if `tipo !== 'big_five'` → refuse (Pitfall 6). (2) For each of the 5 dims: `band = bandOf(percentil)` using the cutoffs; select the matching 1-of-5 official template text from a bundled constant transcribed from templates-devolutiva.md. (3) `loadPrompt('bigfive_devolutiva', supabaseAdmin)` → `resolvedPromptFromLoaded(..., 'gpt-4o-mini')` → `callAi({ prompt, rawInput: officialTemplateForBand, schema: BigfiveDevolutivaSchema, candidato_id, vaga_id }, { anthropic, openai, supabase: supabaseAdmin })`. (4) Validate word-count 150-200/dim (Zod); if out of range retry 1×; if still off, graceful-degrade to the raw template text (never fail the candidate). (5) Assemble the `conteudo_jsonb` (cabecalho dashboard + 5 paginas + disclaimer_emocional + disclaimer_lgpd_crp); use "Sensibilidade Emocional" for N. (6) INSERT `devolutivas_candidato` via service_role with audit fields (modelo_ia, prompt_version). Logs = ids/counts/status only (Pitfall 7). Return `{ ok:true, devolutiva_id }`. Flips the 12-01 devolutiva-EF tests GREEN.
  </action>
  <verify>
    <automated>deno test supabase/functions/gerar-devolutiva-bigfive/index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - RF-19b guard: refuses when the precondition score row `tipo !== 'big_five'` (unit-tested).
    - Band selection maps percentil → correct 1-of-5 template per dim (cutoffs ≤15/16-35/36-64/65-84/≥85).
    - Word-count out-of-range → 1 retry → graceful-degrade to raw template (never throws to the candidate).
    - INSERTs `devolutivas_candidato` conteudo_jsonb + audit; uses callAi (not a raw SDK call); "Sensibilidade Emocional" for N.
    - The 12-01 `gerar-devolutiva-bigfive/index.test.ts` suite passes.
  </acceptance_criteria>
  <done>The devolutiva EF is authored, passes its Wave-0 contract, honors RF-19b + LGPD-04 + graceful-degrade; deploy + is_active flip is the [BLOCKING] 12-06 wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| EF → AI provider | candidate-derived percentiles sent to Anthropic; callAi masks/audits |
| EF → devolutivas_candidato | service_role write; candidate reads own-row only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Repudiation/compliance | LGPD/CFP clinical language | mitigate | curated templates + Zod word-count + LGPD-04 grep + fixed disclaimers + "Sensibilidade Emocional" (Pitfall 5) |
| T-12-14 | Tampering | devolutiva for wrong test | mitigate | RF-19b guard: precondition `tipo='big_five'` (Pitfall 6) |
| T-12-15 | Info Disclosure | IA invents social comparison | mitigate | IA personalizes only; never invents; graceful-degrade to template |
| T-12-16 | Info Disclosure | prompt injection | mitigate | no candidate free text (Likert only); callAi injection-detect still runs |
| T-12-SC | Tampering | npm/deno installs | accept | reuses Phase-9 pinned SDKs; no new packages |
</threat_model>

<verification>
- `deno test gerar-devolutiva-bigfive/index.test.ts` GREEN.
- `npm run test:run -- forbidden-strings` GREEN over the new template.
- `grep -c "callAi" supabase/functions/gerar-devolutiva-bigfive/index.ts` ≥ 1; no raw `anthropic.messages` direct call.
- `npm run lint` tsc baseline zero-growth (Deno EF + docs outside tsc scope).
</verification>

<success_criteria>
- Hybrid template+IA EF; RF-19b guard; LGPD-04 safe; graceful-degrade; persists devolutivas_candidato.
- 12-01 devolutiva-EF RED tests flip GREEN; LGPD-04 grep covers the new template.
</success_criteria>

<output>
Create `.planning/phases/12-big-five-devolutiva/12-04-SUMMARY.md` when done.
</output>
