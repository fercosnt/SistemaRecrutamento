---
phase: 12-big-five-devolutiva
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - supabase/functions/_shared/bigfive-scoring.ts
  - supabase/functions/_shared/avaliacao-schemas.ts
  - supabase/migrations/20260612000001_bigfive_itens.sql
  - supabase/migrations/20260612000002_devolutivas_candidato.sql
autonomous: true
requirements: [AVAL-04, AVAL-08]
must_haves:
  truths:
    - "The Johnson 2014 norm table (560 mean/sd values) is transcribed into bigfive-scoring.ts as a typed TS constant — the gating prerequisite that precedes any scoring EF (D-12-AVAL-04 / Open Q1)"
    - "score(respostas, norm_group) deterministically reverses the 55 keyed items, sums 30 facet raws, sums 5 domain raws, computes T-scores and percentiles via the cubic, and assigns bands — flipping the Wave-0 golden test GREEN"
    - "SubmitBigfiveFinalBodySchema is .strict, accepts exactly 120 answers each 1..5, and rejects any score field — flipping the client↔EF contract test GREEN (D-12-AVAL-04 anti-tamper)"
    - "bigfive_itens seeds all 120 items with dimensao/faceta/reverse_keyed derived per PESQUISA, exposed to candidates ONLY via the get_bigfive_itens SECURITY DEFINER reader (item_id/texto/ordem) — mirroring get_opcoes_sjt"
    - "devolutivas_candidato has own-row candidate RLS + RH allowlist; no candidate read policy leaks scores (D-12-AVAL-08)"
  artifacts:
    - path: "supabase/functions/_shared/bigfive-scoring.ts"
      provides: "REVERSED set, FACET_TO_DOMAIN, norm table constant, score() TS-port"
      min_lines: 120
    - path: "supabase/functions/_shared/avaliacao-schemas.ts"
      provides: "SubmitBigfiveFinalBodySchema (.strict) + BigfiveDevolutivaSchema"
      contains: "SubmitBigfiveFinalBodySchema"
    - path: "supabase/migrations/20260612000001_bigfive_itens.sql"
      provides: "bigfive_itens table + 120-item seed + get_bigfive_itens RPC + RLS"
      contains: "get_bigfive_itens"
    - path: "supabase/migrations/20260612000002_devolutivas_candidato.sql"
      provides: "devolutivas_candidato table + own-row + RH allowlist RLS"
      contains: "devolutivas_candidato"
  key_links:
    - from: "supabase/functions/_shared/bigfive-scoring.ts"
      to: "supabase/functions/_shared/bigfive-scoring.test.ts"
      via: "golden vector flips GREEN"
      pattern: "score\\("
---

<objective>
Build the deterministic data foundation: the scorer (norm table transcription FIRST — the gating prerequisite), the EF body/output Zod schemas, and the two new migrations (item bank + reader RPC; devolutivas table). No EFs or UI here — this plan flips the 12-01 scorer + contract RED tests GREEN.

Purpose: Pitfall 1 (reverse-key) and Pitfall 2 (missing norm table) are the two scoring-correctness risks; this plan resolves both with cited transcription + the golden test as the proof. Sequencing the norm table before the scoring EF (12-03) is mandatory per CONTEXT post-research.

Output: `bigfive-scoring.ts` (scorer + norms), extended `avaliacao-schemas.ts`, 2 migration files (authored, NOT applied — apply is the [BLOCKING] 12-06 wave).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-big-five-devolutiva/12-RESEARCH.md
@.planning/phases/12-big-five-devolutiva/12-CONTEXT.md

<interfaces>
From supabase/functions/_shared/avaliacao-schemas.ts (extend, do not break):
- export const AvaliarRedacaoBodySchema = z.object({...})  (L108) — the .strict body analog
- export const WorkSampleScoringSchema = z.object({...})    (L64) — structured-output analog
SubmitBigfiveFinalBodySchema (NEW): { candidatura_id: z.string().uuid(), respostas: z.record(z.string(), z.number().int().min(1).max(5)) }.strict()
metadata jsonb shape (12-CONTEXT L57): { dimensoes:[{dim,raw,percentil,banda}], facetas:[{faceta,raw}], norm_group }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Norm table + TS-port scorer + Zod schemas</name>
  <files>supabase/functions/_shared/bigfive-scoring.ts, supabase/functions/_shared/avaliacao-schemas.ts</files>
  <read_first>
    - `docs/conhecimento/big-five/PESQUISA-big-five-ipip-neo-120-ptbr.md` L282 (reverse fn), L285-294 + L329-340 (55 reverse IDs), L205-249 (facet formula + 30-facet→domain), L304-314 (T-score + cubic coeffs), L316-319 (band cutoffs), L321-413 (full Python reference scorer to port), L416-429 (norm strategy — where norm.py lives)
    - `.planning/phases/12-big-five-devolutiva/12-RESEARCH.md` §Pattern 2 (cubic coeffs + T formula), §Pitfall 1/2, §Open Q1/Q2 (norm_group = sex='N' + age band from birth date)
    - `supabase/functions/_shared/avaliacao-schemas.ts` L1-114 (full — extend at end, preserve all existing exports)
    - `supabase/functions/_shared/bigfive-scoring.test.ts` (the 12-01 golden test that this must satisfy)
  </read_first>
  <action>
    Create `supabase/functions/_shared/bigfive-scoring.ts` (Deno, `npm:zod@3.25.76` only if needed for types). Transcribe FIRST the Johnson 2014 international norm table — the 560 mean/sd values from `NeuroQuestAi/five-factor-e/ipipneo/norm.py` (8 groups × [5 domain mean + 5 domain sd + 30 facet mean + 30 facet sd]) — into a typed constant `NORMS: Record<NormGroup, {...}>`. If the executor cannot obtain norm.py, FALLBACK per CONTEXT post-research = the combined sex='N' adult group only, with a `// V1 fallback — precision is a V2/UAT refinement; Big Five is contextual` comment; pin the source file/commit in a header comment. Then port: `REVERSED` Set (55 ids verbatim from PESQUISA L289-294), `facetOf(id) = ((id-1)%30)+1`, `FACET_TO_DOMAIN` map (L342-348), `reverse(v) = 6-v`, the facet→domain raw sums, T-score `50 + 10*(raw - mean)/sd`, the percentile cubic (clamp 1..99, coeffs L358-361), and `band(percentil)` (≤15 muito_baixo / 16-35 mod_baixo / 36-64 medio / 65-84 mod_alto / ≥85 muito_alto). Export `score(respostas, norm_group): { dimensoes, facetas, norm_group }` matching the 12-CONTEXT metadata shape. Derive `norm_group` from sex='N' + age band (Open Q2); export a `normGroupFromBirthDate(dob)` helper. The golden test (12-01) is the correctness proof — make it GREEN.
    Extend `supabase/functions/_shared/avaliacao-schemas.ts`: append `SubmitBigfiveFinalBodySchema` (`.strict`, `respostas: z.record(z.string(), z.number().int().min(1).max(5))`, plus `candidatura_id` uuid) and `BigfiveDevolutivaSchema` (the structured-output shape per PRD RFB-15: `{ cabecalho, paginas:[{dim,banda,percentil,texto_interpretativo,palavras}]×5, disclaimer_emocional, disclaimer_lgpd_crp }`). Preserve every existing export. This flips the 12-01 contract test GREEN.
  </action>
  <verify>
    <automated>deno test supabase/functions/_shared/bigfive-scoring.test.ts && npm run test:run -- bigfive-contract</automated>
  </verify>
  <acceptance_criteria>
    - `bigfive-scoring.ts` exports `REVERSED` (size 55), `FACET_TO_DOMAIN`, `NORMS` constant (or documented sex='N' fallback), `score()`, `normGroupFromBirthDate()`.
    - The 12-01 scorer golden test passes: 5 domain raws + 5 percentiles + 5 bands match the pinned expected values.
    - The 12-01 `bigfive-contract` test passes: `.strict` rejects extra `score`; 120×1-5 body parses.
    - All pre-existing `avaliacao-schemas.ts` exports unchanged (grep confirms `AvaliarRedacaoBodySchema` + `WorkSampleScoringSchema` still present).
  </acceptance_criteria>
  <done>Scorer + norms + schemas land; the two relevant Wave-0 RED tests flip GREEN; no other suite regresses.</done>
</task>

<task type="auto">
  <name>Task 2: bigfive_itens + devolutivas_candidato migrations (authored, no-wrapper)</name>
  <files>supabase/migrations/20260612000001_bigfive_itens.sql, supabase/migrations/20260612000002_devolutivas_candidato.sql</files>
  <read_first>
    - `supabase/migrations/20260611000001_scores_candidato.sql` L35-91 (RLS pattern; RH uses `'administrador'` not `'admin'` L88; candidate NO read policy)
    - `supabase/migrations/20260611000002_perguntas_sjt.sql` + the `get_opcoes_sjt` definition (the SECURITY DEFINER reader to mirror)
    - `docs/conhecimento/big-five/fontes/ipip-neo-120-questions-pt-br.json` (the 120 item texts to seed — id 1-120)
    - `.planning/phases/12-big-five-devolutiva/12-RESEARCH.md` §Code Examples (the `get_bigfive_itens` + `bigfive_itens` DDL shapes), §Pitfall 8 (D-22 no-wrapper)
    - `CLAUDE.md` ## Commands (no-wrapper migration rule; the table+seed is plain DDL+INSERT — pushes clean; the `$$`-body RPC may need the MCP/SQL-Editor path on the apply wave)
  </read_first>
  <action>
    Create `supabase/migrations/20260612000001_bigfive_itens.sql` (D-22 no-wrapper — NO `BEGIN;/COMMIT;`): `CREATE TABLE public.bigfive_itens` (item_id int PK 1..120, texto text, dimensao char(1) CHECK IN O/C/E/A/N, faceta int CHECK 1..30, reverse_keyed boolean, ordem int); seed all 120 rows — texto from the on-disk PT-BR JSON, dimensao/faceta from `faceta=((id-1)%30)+1` + the FACET_TO_DOMAIN map, reverse_keyed = id ∈ the 55-id set (PESQUISA L289-294), ordem = item_id for V1; enable RLS with NO candidate SELECT policy on the base table; `CREATE OR REPLACE FUNCTION public.get_bigfive_itens() RETURNS TABLE(item_id int, texto text, ordem int) LANGUAGE sql SECURITY DEFINER SET search_path = public AS $$ SELECT item_id, texto, ordem FROM public.bigfive_itens ORDER BY ordem; $$;` + GRANT EXECUTE to authenticated. The seed count MUST be 120 with reverse_keyed=true count = 55 (the 12-SMOKES checks this).
    Create `supabase/migrations/20260612000002_devolutivas_candidato.sql` (no-wrapper): `CREATE TABLE public.devolutivas_candidato` (id uuid PK default gen_random_uuid(), candidatura_id uuid FK, candidato_id uuid FK, conteudo_jsonb jsonb, audit fields per PRD RFB-15 — modelo_ia, prompt_version, created_at); enable RLS; candidate own-row SELECT policy (`candidato_id = auth.uid()`); RH/'administrador' SELECT allowlist policy; service_role INSERT (EF write). NO candidate INSERT/UPDATE. Mirror the `scores_candidato` RLS authoring style.
    Do NOT apply these — application is the [BLOCKING] 12-06 wave via MCP apply_migration.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260612000001_bigfive_itens.sql && grep -qc "get_bigfive_itens" supabase/migrations/20260612000001_bigfive_itens.sql && grep -v '^--' supabase/migrations/20260612000001_bigfive_itens.sql | grep -ci "BEGIN;" | grep -qx 0 && echo NO-WRAPPER-OK; test -f supabase/migrations/20260612000002_devolutivas_candidato.sql && echo DEVOL-OK</automated>
  </verify>
  <acceptance_criteria>
    - `20260612000001_bigfive_itens.sql`: table + 120 seed rows + `get_bigfive_itens` SECURITY DEFINER (projects only item_id/texto/ordem) + RLS with no candidate base-table SELECT; no `BEGIN;/COMMIT;` wrapper.
    - `20260612000002_devolutivas_candidato.sql`: table + own-row candidate SELECT + RH 'administrador' allowlist + service_role INSERT; no candidate write; no wrapper.
    - Seed has exactly 120 items, 55 reverse_keyed=true (verifiable on the apply wave smoke).
    - Files are authored only — NOT applied.
  </acceptance_criteria>
  <done>Both migrations authored no-wrapper, ready for the [BLOCKING] apply wave; reader RPC mirrors get_opcoes_sjt; devolutiva RLS is own-row + RH allowlist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → DB read | candidate must read item texts but NEVER the scoring key |
| EF → scores_candidato | service_role write; candidate has no read path |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | answer-key exfiltration | mitigate | `get_bigfive_itens` projects only item_id/texto/ordem; base table has no candidate SELECT policy ([[reference_select_star_leaks_pii]]) |
| T-12-05 | Tampering | client-sent score | mitigate | `SubmitBigfiveFinalBodySchema.strict` has no score field; 1..5 range enforced |
| T-12-06 | Tampering | wrong reverse-key set | mitigate | 55-id set transcribed verbatim + golden test pins percentiles (Pitfall 1) |
| T-12-07 | Information Disclosure | scores_candidato leak to candidate | mitigate | no candidate read policy; devolutivas_candidato is the only candidate-facing row |
| T-12-SC | Tampering | npm/deno installs | accept | no new packages (RESEARCH §Package Legitimacy N/A) |
</threat_model>

<verification>
- `deno test bigfive-scoring.test.ts` GREEN (golden vector matches).
- `npm run test:run -- bigfive-contract` GREEN.
- Both migration files exist, no-wrapper, with the required objects.
- `npm run lint` tsc baseline zero-growth (Deno `_shared/` modules are outside tsc include scope; schema addition is Deno-only).
</verification>

<success_criteria>
- Norm table transcribed (or documented sex='N' fallback) BEFORE the scoring EF — gating order honored.
- Scorer + schemas flip the Wave-0 scorer + contract tests GREEN.
- Two migrations authored no-wrapper; reader RPC enforces the answer-key projection; devolutiva RLS own-row + RH allowlist.
</success_criteria>

<output>
Create `.planning/phases/12-big-five-devolutiva/12-02-SUMMARY.md` when done.
</output>
