---
phase: 17-navegacao-arquitetura-informacao
plan: 03
type: execute
wave: 2
depends_on: ["17-02"]
files_modified:
  - src/features/hub-candidato/components/HubCandidatoRH.tsx
  - src/features/hub-candidato/components/HubSection.tsx
  - src/features/hub-candidato/index.ts
  - src/components/pages/PerfilCandidatoRHPage.tsx
  - src/features/triagem/components/TriagemTable.tsx
  - src/components/RHSidebar.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-05: PerfilCandidatoRHPage (the 1864-line hardcoded mock) is replaced by a real service-backed hub — the new src/features/hub-candidato/ feature — with the DISC / Raven / manifesto tabs (M1 funnel concepts absent in M2) DROPPED; this resolves ENTREV-PERFIL-DUP-01."
    - "D-04: the entry flow TriagemTable to hub to workspace works; the existing TriagemTable name link to /rh/candidatos/:id stays but its destination CONTENT becomes the real hub, and the link is converted from a full-page <a href> to SPA navigation carrying the candidaturaId (row.id), NOT candidato.id (Pitfall 1)."
    - "D-06: the hub is guided by etapa_atual — the current-stage 'Abrir {label}' CTA is the single dominant turquoise affordance, passed stages are reachable via neutral 'Revisar {label}', and future stages render as 'Etapa ainda não iniciada' empty/locked state (never disappear)."
    - "D-07: the hub reflects the full pipeline (Identidade · Score de Triagem · Avaliação Assíncrona · Avaliação Cognitiva · Redação · Entrevista · Decisão Final) where every section reads a real features/* hook keyed by candidaturaId OR shows the explicit empty state — NEVER invented/hardcoded data (the sin of the mock); reads reuse allowlist-projected hooks (no new select('*'))."
    - "D-13: a role-gated 'Admin' item is added to RHSidebar visible ONLY for role==='administrador' (hidden for rh/candidato), routing to /admin/ai-logs as the default sub-nav target, with active-state when pathname startsWith /admin; sidebar visibility is cosmetic — RoleGuard + RLS on /admin/* stay the real control."
    - "D-15: the substantial new hub screen is born in src/features/hub-candidato/ per the hybrid architecture; the workspaces (entrevista/redação/decisão) are WIRED IN via funilNavMap, not redesigned."
  artifacts:
    - path: "src/features/hub-candidato/components/HubCandidatoRH.tsx"
      provides: "RHLayout-shelled, candidaturaId-keyed candidate hub composing real features/* hooks + etapa-guided CTAs"
      min_lines: 80
    - path: "src/features/hub-candidato/components/HubSection.tsx"
      provides: "Service-backed section wrapper with loading skeleton + future-stage + no-data empty states (UI-SPEC copy)"
    - path: "src/components/RHSidebar.tsx"
      provides: "Role-gated Admin nav item"
      contains: "administrador"
  key_links:
    - from: "src/features/hub-candidato/components/HubCandidatoRH.tsx"
      to: "src/lib/navegacao/funilNavMap.ts"
      via: "import funilNavMap for etapa-guided workspace CTAs"
      pattern: "funilNavMap"
    - from: "src/features/hub-candidato/components/HubCandidatoRH.tsx"
      to: "src/features/avaliacao/hooks/useScorecardCandidato.ts"
      via: "service-backed section read keyed by candidaturaId"
      pattern: "useScorecardCandidato"
    - from: "src/features/triagem/components/TriagemTable.tsx"
      to: "/rh/candidatos/:id"
      via: "SPA navigate to /rh/candidatos/${row.id}"
      pattern: "row\\.id"
    - from: "src/components/RHSidebar.tsx"
      to: "/admin/ai-logs"
      via: "handleMenuClick admin route + administrador gate"
      pattern: "/admin"
---

<objective>
Wire the RH side of the funnel: replace the 1864-line hardcoded `PerfilCandidatoRHPage` mock
with a real, service-backed, etapa-guided candidate HUB (new `src/features/hub-candidato/`
feature — D-05/D-07/D-15), fix the TriagemTable entry link to carry a candidaturaId via SPA
navigation (D-04/Pitfall 1), and add the role-gated Admin sidebar entry (D-13). The hub consumes
`funilNavMap` (17-02) so its "próximo passo" CTA opens the correct workspace for `etapa_atual`.

Purpose: This closes the audit's two QUEBRA RH journeys — "RH triando → avaliar no funil"
(TriagemTable now reaches the real workspaces, not the mock) and "Admin → telas de IA"
(sidebar entry). It flips the hub empty-state + RHSidebar.admin RED specs (17-01) GREEN.
Output: new hub feature (3 files) + 3 edited files. No new service/EF/migration — reads reuse
existing RLS-correct hooks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-RESEARCH.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-PATTERNS.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-UI-SPEC.md

<interfaces>
funilNavMap (from 17-02, src/lib/navegacao/funilNavMap.ts):
  funilNavMap[etapa].rotaWorkspaceRH(candidaturaId) -> string | null   (e.g. /rh/candidato/{id}/entrevista)
  funilNavMap[etapa].label / .ctaRH ("Abrir {label}")

Hub section -> real hook map (D-07 — each keyed by candidaturaId; reuse, do NOT re-fetch):
  Identidade + etapa chip       -> candidatura row in scope + ETAPA_M2_LABELS (triagemService)
  Score de Triagem (IA)         -> useTriagemPanel / useComparativo   (features/triagem/hooks)
  Avaliacao Assincrona          -> useScorecardCandidato(candidaturaId) (features/avaliacao/hooks/useScorecardCandidato.ts L16-39)
  Avaliacao Cognitiva           -> useEntrevistaScorecard(candidaturaId) filter tipo==='cognitivo' (features/entrevista/hooks)
  Redacao                       -> useRedacaoRevisao (features/triagem/hooks/useRedacaoRevisao.ts)
  Entrevista                    -> useEntrevistaContexto / useEntrevistaScorecard(candidaturaId) (features/entrevista/hooks)
  Decisao Final                 -> useConsolidacao(candidaturaId, vagaId) (features/decisao/hooks/useConsolidacao.ts) — vagaId from useEntrevistaContexto(candidaturaId).vaga_id

Shell analog (EntrevistaWorkspace.tsx L20-23, L61-67, L111-114):
  const { id } = useParams<{ id: string }>(); const candidaturaId = id ?? ''   (id = candidaturaId, Pitfall 1)
  <RHLayout> ... <h1 className="text-3xl font-semibold text-white md:text-4xl">{name}</h1> ... </RHLayout>
  Query hooks: enabled: !!candidaturaId, staleTime 5min, retry 2 (useScorecardCandidato.ts L16-39)

UI-SPEC empty-state copy (verbatim):
  future stage     -> heading "Etapa ainda não iniciada" / body "Esta etapa será liberada quando o candidato avançar no funil."
  reached, no data -> heading "Sem dados nesta etapa" / body "Nenhum registro foi gerado ainda para esta etapa."
  section error    -> "Não foi possível carregar esta seção." + "Tente recarregar a página."
UI-SPEC CTAs: current "Abrir {label}" (accent dominant) · passed "Revisar {label}" (neutral)

TriagemTable.tsx L324-325 (FIX): href to /rh/candidatos/${candidato?.id ?? ''} -> SPA navigate to /rh/candidatos/${row.id}. TriagemRow.id = candidaturaId (triagemService.ts L76).

RHSidebar.tsx (D-13 three touch points, L47-103):
  menuItems append (role==='administrador') { id:'admin', label:'Admin', icon:<ShieldCheck size={24}/> }
  getActivePageFromPath add: if (pathname.startsWith('/admin')) return 'admin'
  handleMenuClick routes add: 'admin': '/admin/ai-logs'
  role from useAuthStore((s)=>s.role); active-state reuses existing bg-[#35BFAD] class (L198-200)

PerfilCandidatoRHPage.tsx — the mock to retire: drop TesteType (L55), recharts radar/pie (L36-52), DISC/Raven/manifesto tabs (L216/L224/L257).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hub-candidato feature — HubSection (empty-state-aware) + HubCandidatoRH composition (D-05/D-06/D-07/D-15)</name>
  <files>src/features/hub-candidato/components/HubSection.tsx, src/features/hub-candidato/components/HubCandidatoRH.tsx, src/features/hub-candidato/index.ts</files>
  <read_first>
    - src/features/entrevista/components/EntrevistaWorkspace.tsx (FULL — shell shape: RHLayout host, useParams id=candidaturaId, useEntrevistaContexto then vaga_id resolution, skeleton/empty handling, h1 heading)
    - src/features/avaliacao/hooks/useScorecardCandidato.ts (L16-39 — query-key + useQuery + enabled:!!candidaturaId convention to mirror; do NOT write new fetch logic)
    - src/features/decisao/hooks/useConsolidacao.ts (L18-41 — the (candidaturaId, vagaId) signature)
    - src/features/explicacao/hooks/useExplicacao.ts (L31-65 — candidaturaId-keyed read)
    - src/features/triagem/services/triagemService.ts (L293-325 — ETAPA_M2_LABELS for the etapa chip + stage timeline)
    - src/lib/navegacao/funilNavMap.ts (from 17-02 — the etapa->workspace CTA source)
    - src/features/hub-candidato/components/__tests__/hubEmptyState.test.tsx (the 17-01 RED spec — match the exported component name it imports)
    - src/components/pages/PerfilCandidatoRHPage.tsx (L1-60, L210-260 — the mock structure being replaced; confirm DISC/Raven/manifesto tabs to DROP)
    - 17-PATTERNS.md (§HubCandidatoRH — shell+params+heading + hub-section->hook map + service-backed empty-state pattern)
    - 17-UI-SPEC.md (§Interaction Contract visual dominance; §Empty states copy; §Color accent reserved list + dark-glass AA contrast)
  </read_first>
  <behavior>
    - HubSection: given a future/not-yet-reached stage flag, renders heading "Etapa ainda não iniciada" + body "Esta etapa será liberada quando o candidato avançar no funil." and NO hardcoded numbers.
    - HubSection: given isLoading renders a skeleton; given reached + empty data renders "Sem dados nesta etapa" / "Nenhum registro foi gerado ainda para esta etapa."; given error renders "Não foi possível carregar esta seção."
    - HubCandidatoRH: reads candidaturaId from useParams; renders the candidate name header + etapa chip from ETAPA_M2_LABELS; renders one dominant accent "Abrir {label}" CTA for the current etapa whose target = funilNavMap[etapa].rotaWorkspaceRH(candidaturaId); future stages render HubSection future-empty (do not disappear).
  </behavior>
  <action>
    Create src/features/hub-candidato/components/HubSection.tsx — a presentational wrapper taking props { isLoading, isFutureStage, isEmpty, isError, heading, children } rendering the UI-SPEC states in order: skeleton (mirror EntrevistaWorkspace skeleton) when isLoading; future-stage empty ("Etapa ainda não iniciada" / "Esta etapa será liberada quando o candidato avançar no funil.") when isFutureStage; reached-no-data empty ("Sem dados nesta etapa" / "Nenhum registro foi gerado ainda para esta etapa.") when isEmpty; error ("Não foi possível carregar esta seção." / "Tente recarregar a página.") when isError; else render children. Glass surface on dark-enough glass (bg-black/30 or the #00109E/85 precedent so white/turquoise 14-16px body passes WCAG AA); NEVER bg-primary. Export the name the 17-01 hubEmptyState spec imports.

    Create src/features/hub-candidato/components/HubCandidatoRH.tsx (named export, RHLayout-shelled — the route RoleGuard provides the real gate). Read const { id } = useParams; const candidaturaId = id ?? '' (id = candidaturaId, Pitfall 1). Compose the FULL pipeline as sections (D-07), each calling its real hook from the interfaces map and feeding HubSection: Identidade (name header + etapa_atual chip via ETAPA_M2_LABELS + a stage timeline rendering all 8 stages — current=accent turquoise chip, passed=neutral "Revisar {label}", future=locked HubSection future-empty), Score de Triagem, Avaliação Assíncrona (useScorecardCandidato), Avaliação Cognitiva, Redação, Entrevista (resolve vagaId from useEntrevistaContexto(candidaturaId).vaga_id to feed Decisão), Decisão Final (useConsolidacao(candidaturaId, vagaId)). The SINGLE dominant accent CTA = "Abrir {label}" for etapa_atual -> onClick navigate(funilNavMap[etapa_atual].rotaWorkspaceRH(candidaturaId)) when non-null. Use SPA navigate(); product language "avaliação comportamental/cognitiva" (NEVER "teste psicológico"); NEVER hardcode scores/percentages; introduce NO score-driven auto-action (RNF-07a). DROP all DISC/Raven/manifesto/recharts concepts. Reuse the existing hooks verbatim — do NOT add a new candidate-facing select (avoids the select('*') PII-leak class). No fenced code blocks.

    Create src/features/hub-candidato/index.ts re-exporting HubCandidatoRH (named).
  </action>
  <verify>
    <automated>npm run test:run -- hubEmptyState 2>&1 | tail -15 ; npm run lint 2>&1 | tail -3 ; grep -rEc "bg-primary|recharts|teste psicológico" src/features/hub-candidato/</automated>
  </verify>
  <acceptance_criteria>
    - 17-01 hubEmptyState.test.tsx flips GREEN (future-stage empty renders the verbatim copy; no hardcoded numbers).
    - `grep -rEc "bg-primary|recharts|teste psicológico" src/features/hub-candidato/` === 0 across all files (broken token, dropped charting, banned product language all absent).
    - HubCandidatoRH references funilNavMap + at least useScorecardCandidato + useConsolidacao (real-hook composition, not hardcoded); no `select('*')` introduced.
    - candidaturaId is read from useParams (id) — no candidato.id used as the workspace key.
    - tsc baseline (~301) not grown.
  </acceptance_criteria>
  <done>hub-candidato feature renders the full etapa-guided pipeline from real hooks with empty states; mock concepts dropped; 17-01 hub spec green.</done>
</task>

<task type="auto">
  <name>Task 2: Mount hub + fix TriagemTable entry (D-04) + role-gated Admin sidebar (D-13)</name>
  <files>src/components/pages/PerfilCandidatoRHPage.tsx, src/features/triagem/components/TriagemTable.tsx, src/components/RHSidebar.tsx</files>
  <read_first>
    - src/components/pages/PerfilCandidatoRHPage.tsx (FULL header region — it is the component routes.tsx mounts at /rh/candidatos/:id; convert it to a thin wrapper rendering HubCandidatoRH while preserving the named export)
    - src/features/triagem/components/TriagemTable.tsx (L320-330 — the href to /rh/candidatos/${candidato?.id} link to fix; check for an existing useNavigate import / row.id availability)
    - src/features/triagem/services/triagemService.ts (L75-97 — confirm TriagemRow.id = candidaturaId, candidato.id is a different field)
    - src/components/RHSidebar.tsx (L47-103 FULL — menuItems array, getActivePageFromPath, handleMenuClick routes map, useAuthStore usage, existing active-state class bg-[#35BFAD] L198-200)
    - src/components/__tests__/RHSidebar.admin.test.tsx (the 17-01 RED spec — the role-gating contract to satisfy)
    - 17-PATTERNS.md (§RHSidebar three touch points; §routes.tsx — the mock is replaced by the hub, not redesigned)
    - 17-RESEARCH.md (Pitfall 1 candidaturaId; Pitfall 2 <a href> vs SPA; Security V4 sidebar visibility is cosmetic)
  </read_first>
  <action>
    (1) Retire the mock: edit src/components/pages/PerfilCandidatoRHPage.tsx so the route mount at /rh/candidatos/:id renders the new hub — convert the file to a thin wrapper that imports HubCandidatoRH from '@/features/hub-candidato' and returns it (named export PerfilCandidatoRHPage preserved so the routes.tsx import is unchanged; the 1864-line mock body, DISC/Raven/manifesto tabs, recharts, and all hardcoded data are removed). This keeps routes.tsx untouched (no same-wave collision with 17-02/17-05) while replacing the destination CONTENT per D-04/D-05.

    (2) Fix the TriagemTable entry (D-04 / Pitfall 1+2): change the name link from the raw href to /rh/candidatos/${candidato?.id} to SPA navigation carrying the candidaturaId — use useNavigate (or React Router <Link>) targeting /rh/candidatos/${row.id} (row.id = candidaturaId, verified triagemService.ts L76). Preserve the visible name text + styling; only swap the element + the id source. Keep keyboard accessibility (a <Link> or a button with role semantics, not a bare onClick div).

    (3) Add the Admin sidebar item (D-13): in src/components/RHSidebar.tsx read const role = useAuthStore((s) => s.role); append to menuItems, ONLY when role==='administrador', { id:'admin', label:'Admin', icon:<ShieldCheck size={24} /> } (import ShieldCheck from lucide-react, 24px to match siblings; visible "Admin" text label, no icon-only). Add to getActivePageFromPath: if (pathname.startsWith('/admin')) return 'admin'. Add to the handleMenuClick routes map: 'admin': '/admin/ai-logs' (default sub-nav target). The active-state reuses the existing bg-[#35BFAD] class. Add/retain aria-label on any reused icon-only control touched (sidebar collapse "Recolher menu", logout "Sair", mobile toggle "Abrir menu") — no a11y regression. Sidebar visibility is cosmetic; do NOT add a new access boundary — the /admin/* routes keep their RoleGuard + RLS.
  </action>
  <verify>
    <automated>npm run test:run -- RHSidebar.admin 2>&1 | tail -15 ; npm run build 2>&1 | tail -5 ; grep -c "administrador" src/components/RHSidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 17-01 RHSidebar.admin.test.tsx flips GREEN: Admin item present for administrador, absent for rh and candidato.
    - PerfilCandidatoRHPage.tsx is now a thin wrapper rendering HubCandidatoRH (`grep -Ec "recharts|DISC|Raven|manifesto" src/components/pages/PerfilCandidatoRHPage.tsx` === 0); the named export PerfilCandidatoRHPage is preserved (routes.tsx import unchanged → build exits 0).
    - TriagemTable no longer uses a raw href for the name link (`grep -c "href=.*candidatos" src/features/triagem/components/TriagemTable.tsx` === 0) and navigates to /rh/candidatos/${row.id} (candidaturaId) via SPA.
    - RHSidebar references administrador + ShieldCheck + /admin/ai-logs; the item gate is role === 'administrador'.
    - `npm run build` exits 0; tsc baseline (~301) not grown.
  </acceptance_criteria>
  <done>Hub mounted at /rh/candidatos/:id (mock retired); TriagemTable entry is SPA + candidaturaId; Admin sidebar item role-gated; 17-01 RHSidebar spec green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH user → hub reads | The hub reads candidate pipeline data via existing features/* hooks behind Supabase RLS; no new query/select. |
| sidebar role gate → /admin/* | The Admin item is visible only for administrador, but real access is the route RoleGuard + RLS (sidebar is cosmetic). |
| TriagemTable link → workspace | The link carries a candidaturaId; handing the wrong id (candidato.id) would render a blank workspace, not a privilege escalation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-AC | Elevation of Privilege | RHSidebar Admin item (D-13) | mitigate | Item gated on `useAuthStore(s=>s.role)==='administrador'`; cosmetic only — the underlying /admin/* routes retain `RoleGuard role="administrador"` + RLS (verified routes.tsx L490-524). Hiding is not the control; do not weaken the route guard. A candidato/rh never sees the item AND is denied at the route. |
| T-17-03-PII | Information Disclosure | hub pipeline reads (D-07) | mitigate | Reuse the existing allowlist-projected features/* hooks (useScorecardCandidato/useConsolidacao/useExplicacao/useEntrevista*); write NO new candidate-facing select — avoids the `select('*')` PII-leak class (MEMORY reference_select_star_leaks_pii). RLS is row-level; the hooks already project explicit columns. |
| T-17-03-ID | Spoofing/Tampering | TriagemTable id contract (Pitfall 1) | mitigate | The link carries `row.id` (candidaturaId) — the native key every workspace + hub hook expects. The documented id contract prevents handing a candidato.id where a candidaturaId is expected (a blank workspace / correctness+UX failure the gate catches). |
| T-17-03-SC | Tampering | npm installs | accept | ShieldCheck is from the already-installed lucide-react; zero new packages (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npm run test:run -- hubEmptyState RHSidebar.admin` → both GREEN (were RED in 17-01).
- `npm run build` → exits 0 (PerfilCandidatoRHPage export preserved; hub imports resolve).
- `grep -rEc "bg-primary|recharts|teste psicológico" src/features/hub-candidato/` → 0.
- `grep -c "href=.*candidatos" src/features/triagem/components/TriagemTable.tsx` → 0 (SPA navigation).
- `grep -c "administrador" src/components/RHSidebar.tsx` → ≥1 (role gate present).
- `npm run lint` → tsc baseline (~301) not grown.
</verification>

<success_criteria>
The RH funnel is navigable: TriagemTable → real hub (mock retired, full pipeline from real hooks with empty states, etapa-guided CTA) → the correct workspace via funilNavMap; the Admin sidebar item routes administrador to /admin/*; 17-01 hub + RHSidebar specs are GREEN; build + tsc clean; no PII-leak select, no bg-primary, no banned product language.
</success_criteria>

<output>
Create `.planning/phases/17-navegacao-arquitetura-informacao/17-03-SUMMARY.md` when done.
</output>
