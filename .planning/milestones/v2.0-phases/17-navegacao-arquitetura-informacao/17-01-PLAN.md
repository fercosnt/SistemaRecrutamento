---
phase: 17-navegacao-arquitetura-informacao
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/navegacao/__tests__/funilNavMap.test.ts
  - src/router/__tests__/routes.nav.test.tsx
  - src/components/__tests__/RHSidebar.admin.test.tsx
  - src/features/hub-candidato/components/__tests__/hubEmptyState.test.tsx
  - src/__tests__/guards/legacy-routes.grep.test.ts
  - e2e/navegacao.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-03: the Definition of Done is wiring proven by a navegability test gate — a Playwright nav smoke + Vitest unit/route/role/empty-state RED battery exists BEFORE the wiring lands (smoke-runtime gate, M1 Phase 4.1 precedent)."
    - "D-16: e2e/navegacao.spec.ts asserts the 4 audit-broken journeys resolve to the right route/heading (NOT data flow): (1) candidato pós-candidatura → avaliação via Dashboard; (2) RH TriagemTable → hub → each of 3 workspaces; (3) admin → sidebar → /admin/*; (4) URL inválida → 404. RH/admin journeys are gated behind E2E_AUTH_TEST_USERS (login-flow.spec.ts precedent)."
    - "D-17 (test contract): the funilNavMap RED unit test asserts exhaustiveness over all 8 EtapaFunilM2 values + candidaturaId-parameterized route fns + label reuse from ETAPA_M2_LABELS — failing until 17-02 lands funilNavMap.ts."
    - "D-14 (test contract): a route-table RED test asserts catch-all path:'*' resolves any unknown URL to NotFoundPage — failing until 17-02 adds the catch-all."
    - "D-13 (test contract): an RHSidebar RED test asserts the 'Admin' item renders ONLY for role==='administrador' and is hidden for 'rh'/'candidato' — failing until 17-03 adds the role-gated item."
    - "D-07 (test contract): a hub empty-state RED test asserts a future/not-reached stage renders 'Etapa ainda não iniciada' (never invented data) — failing until 17-03 lands the hub section component."
  artifacts:
    - path: "src/lib/navegacao/__tests__/funilNavMap.test.ts"
      provides: "RED exhaustiveness + candidaturaId-route-shape test for funilNavMap (D-17)"
    - path: "src/router/__tests__/routes.nav.test.tsx"
      provides: "RED catch-all-404 + redirect-target route-table test (D-08/D-14)"
    - path: "src/components/__tests__/RHSidebar.admin.test.tsx"
      provides: "RED admin-item role-gating test (D-13)"
    - path: "src/features/hub-candidato/components/__tests__/hubEmptyState.test.tsx"
      provides: "RED hub empty-state test (D-07)"
    - path: "src/__tests__/guards/legacy-routes.grep.test.ts"
      provides: "RED grep guard asserting confirmed-dead components have zero routes.tsx import/route/devNav reference (D-12)"
    - path: "e2e/navegacao.spec.ts"
      provides: "Playwright navegability smoke — 4 journeys, route/heading assertions, gated real-auth (D-16)"
  key_links:
    - from: "e2e/navegacao.spec.ts"
      to: "process.env.E2E_AUTH_TEST_USERS"
      via: "describeRealAuth gate (login-flow.spec.ts precedent)"
      pattern: "E2E_AUTH_TEST_USERS"
    - from: "src/lib/navegacao/__tests__/funilNavMap.test.ts"
      to: "EtapaFunilM2"
      via: "import from triagemService for exhaustiveness key set"
      pattern: "EtapaFunilM2"
---

<objective>
Author the calibrated RED test battery for Phase 17 BEFORE any wiring lands, honoring this
project's smoke-runtime gate precedent (M1 Phase 4.1): tests COMPILE under TS strict but FAIL
at runtime/collection against the pre-wiring code — the failure IS the assertion that the
navegability gap is detectable. Downstream waves (17-02..17-05) flip these GREEN.

Purpose: D-03 makes "wiring proven by a navegable-journey test gate" the Definition of Done.
The audit (§8) named the root lesson — the v2.0 milestone audit verified requirements per phase
but never exercised production navigation via the UI, so the orphan funnel passed. This battery
is that gate.
Output: 5 Vitest RED specs + 1 Playwright nav smoke spec. No production code in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-CONTEXT.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-RESEARCH.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-PATTERNS.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-UI-SPEC.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-VALIDATION.md

<interfaces>
<!-- Contracts the RED tests assert against. Extracted from codebase; executor uses these directly. -->

From src/features/triagem/services/triagemService.ts (L293-325) — the enum + labels the nav-map reuses:
EtapaFunilM2 = 'inscricao' | 'triagem' | 'avaliacao_assincrona' | 'entrevista_online' | 'entrevista_presencial' | 'decisao_final' | 'aprovado' | 'rejeitado'  (8 values)
ETAPA_M2_LABELS: Record<EtapaFunilM2, string>

funilNavMap contract the test asserts (NEW in 17-02, src/lib/navegacao/funilNavMap.ts):
  export const funilNavMap: Record<EtapaFunilM2, { label: string; rotaCandidato: (candidaturaId: string) => string | null; rotaWorkspaceRH: (candidaturaId: string) => string | null; ctaCandidato: string; ctaRH: string }>

Real-auth gate precedent (e2e/login-flow.spec.ts L20-39):
  const REAL_AUTH = process.env.E2E_AUTH_TEST_USERS === 'true'
  const describeRealAuth = REAL_AUTH ? test.describe : test.describe.skip
  TEST_USER from process.env.TEST_USER_EMAIL/PASSWORD

Grep-guard idiom precedent: src/__tests__/guards/devnav-gate.grep.test.ts (node:fs readFileSync + regex, ROOT = resolve(__dirname,'../../..'))

Workspace headings to assert (exact, verified):
  EntrevistaWorkspace.tsx L114 → <h1>Entrevista</h1>
  DecisaoFinalPage.tsx L131 → <h1>Decisão final</h1>
  RedacaoReviewPanel.tsx → tab "Revisão de redações" / "Redação do candidato" (RHLayout-shelled)
404 heading (UI-SPEC): "Página não encontrada"

Vitest include glob: **/__tests__/**/*.{test,spec}.{ts,tsx} — tests MUST live under a __tests__/ dir (devnav-gate precedent deviation).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED unit specs — funilNavMap exhaustiveness + route-table catch-all + RHSidebar admin gating</name>
  <read_first>
    - src/features/triagem/services/triagemService.ts (L293-325 — EtapaFunilM2 + ETAPA_M2_LABELS; the exhaustive key set + label source)
    - src/router/routes.tsx (L1-90 imports + L490-524 admin routes + the absence of any path:'*' — the catch-all gap to assert)
    - src/components/RHSidebar.tsx (L47-103 — menuItems / getActivePageFromPath / handleMenuClick; assert NO 'admin' item exists yet)
    - src/components/__tests__ or src/__tests__/ for a render-test analog (vitest + @testing-library/react render of a component) — mirror its setup; if RHSidebar needs router/store context, wrap with MemoryRouter + mock useAuthStore
    - 17-PATTERNS.md (§funilNavMap test analog; §RHSidebar three touch points)
  </read_first>
  <action>
    Create src/lib/navegacao/__tests__/funilNavMap.test.ts: import EtapaFunilM2 + ETAPA_M2_LABELS from @/features/triagem/services/triagemService and (dynamically, see below) the not-yet-existing funilNavMap from @/lib/navegacao/funilNavMap. Assert (a) every one of the 8 EtapaFunilM2 keys is present in funilNavMap (build the expected key array literally from the union members — inscricao, triagem, avaliacao_assincrona, entrevista_online, entrevista_presencial, decisao_final, aprovado, rejeitado); (b) each entry.label === ETAPA_M2_LABELS[etapa] (no parallel labels — D-17); (c) for avaliacao_assincrona, rotaCandidato('abc') === '/candidato/avaliacao/abc' and for rejeitado rotaCandidato('abc') === '/candidato/explicacao/abc'; (d) for entrevista_online, rotaWorkspaceRH('abc') === '/rh/candidato/abc/entrevista' and for decisao_final '/rh/candidato/abc/decisao' — i.e. every non-null route fn interpolates the candidaturaId, NOT a vaga/candidato id. Because importing a non-existent module breaks vitest suite COLLECTION (aborts the whole file), use the node:fs source-probe pattern from consolacaoContract.test.ts precedent for Part 2 if a static import would abort: a Part-1 block that statically imports and runs the assertions (RED = module-not-found at collection, the intended RED signal) is acceptable here since this whole file is Wave-0 RED — prefer the direct static import so the RED is "module not found until 17-02". Document in a file-top comment that RED = funilNavMap.ts absent until Plan 17-02.

    Create src/router/__tests__/routes.nav.test.tsx: import the routes array from @/router/routes. Assert a catch-all route object with path === '*' exists (find via routes.find or recursive flatten). Assert it resolves to a NotFoundPage element (assert by element type name or by a data-testid once mounted via createMemoryRouter at an unknown URL like '/rota/invalida/xyz' and RouterProvider render → getByText('Página não encontrada')). Assert that navigating to the OLD path '/rh/candidatos/:id' form still resolves (redirect target present per D-08) — at minimum assert no throw and that a known-good path '/rh/dashboard' still resolves. RED until 17-02 adds path:'*' + NotFoundPage + the redirect.

    Create src/components/__tests__/RHSidebar.admin.test.tsx: render <RHSidebar/> (wrap in MemoryRouter; mock @/store/authStore useAuthStore to return role). Assert: when role==='administrador' a nav element with accessible name /Admin/ is present; when role==='rh' it is absent; when role==='candidato' it is absent. RED until 17-03 adds the role-gated item. If RHSidebar requires more store fields (user, candidato, logout), stub them minimally per the store shape.

    Do NOT create production files. Use `it.todo`/`describe` only if a static import would crash collection for a sibling file; otherwise let the missing-module/missing-element be the RED. No fenced implementation beyond test code itself.
  </action>
  <verify>
    <automated>npm run test:run -- funilNavMap routes.nav RHSidebar.admin 2>&1 | tail -20 ; npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All three specs exist under a __tests__/ dir (collected by the vitest include glob).
    - Specs are RED: funilNavMap test fails with module-not-found (funilNavMap.ts absent); routes.nav test fails on missing path:'*' / NotFoundPage; RHSidebar.admin test fails on missing /Admin/ item for administrador.
    - `npm run lint` (tsc --noEmit) does NOT grow the baseline (~301) — net-new TS errors from these specs = 0 (add a file-scoped jest-dom triple-slash reference if a matcher errors, per the 04.1-01 precedent).
    - funilNavMap spec references the literal 8-key array and ETAPA_M2_LABELS (grep confirms `EtapaFunilM2` + `ETAPA_M2_LABELS` imports).
  </acceptance_criteria>
  <done>Three RED Vitest specs authored; each fails for the right structural reason; tsc baseline not grown.</done>
</task>

<task type="auto">
  <name>Task 2: RED hub empty-state spec + legacy-routes grep guard</name>
  <read_first>
    - src/components/pages/PerfilCandidatoRHPage.tsx (L1-60 — the mock's hardcoded strings: DISC/Raven/manifesto tab labels, recharts imports; the strings the grep guard asserts are GONE after 17-03/17-05)
    - src/router/routes.tsx (the import region L11-90 + devNavigationPages array near L533-567 + the legacy route objects; the three reference sites the grep guard scans)
    - src/__tests__/guards/devnav-gate.grep.test.ts (the node:fs + regex grep-guard idiom + ROOT resolution to mirror exactly)
    - 17-RESEARCH.md (§Legacy Cleanup Verdicts — the confirmed-dead set vs KEEP MeuPerfilPage)
    - 17-UI-SPEC.md (§Empty states — "Etapa ainda não iniciada" / "Esta etapa será liberada quando o candidato avançar no funil." verbatim copy)
  </read_first>
  <action>
    Create src/features/hub-candidato/components/__tests__/hubEmptyState.test.tsx: render the not-yet-existing hub section/empty-state component (import a named export the hub will provide — e.g. HubSection or EmptyState from @/features/hub-candidato/components/...). Assert that for a "future/not-yet-reached" stage the rendered output contains the verbatim UI-SPEC copy "Etapa ainda não iniciada" and body "Esta etapa será liberada quando o candidato avançar no funil." and contains NO hardcoded mock numbers ("45%", "Concluído"). RED = module-not-found until 17-03 lands the hub component. File-top comment names the dependency (Plan 17-03).

    Create src/__tests__/guards/legacy-routes.grep.test.ts mirroring devnav-gate.grep.test.ts (node:fs readFileSync of src/router/routes.tsx via ROOT=resolve(__dirname,'../../..')): assert that, for each confirmed-dead component name in the verified set — VagaLPPage, TesteBigFivePage, TesteDISCPage, TesteRavenPage, InstrucoesBigFivePage, InstrucoesDISCPage, InstrucoesRavenPage, ConclusaoTestesPage, QuestionarioPage, QuestionarioCulturaPage, InscricaoPage, GlassShowcase — routes.tsx contains ZERO references (no `import { X }`, no `<X />`, no path entry). Use `grep -v '^//'`-equivalent in-JS (strip lines starting with optional-whitespace `//` or `*` before counting) so a comment mentioning the name does not self-invalidate the gate. ALSO assert a positive control: 'MeuPerfilPage' (the KEEP file, RHTopBar.tsx:38 live entry) IS still referenced (>0) — guarding against an over-aggressive deletion. This guard is RED now (the dead components are still imported) and flips GREEN in 17-05.
  </action>
  <verify>
    <automated>npm run test:run -- hubEmptyState legacy-routes 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - hubEmptyState spec RED: module-not-found (hub component absent) — fails at collection/runtime, not on an assertion typo.
    - legacy-routes grep guard RED: the 12 confirmed-dead names still match in routes.tsx (>0 references), so the "zero references" assertion fails today; the MeuPerfilPage positive-control assertion PASSES (it is referenced).
    - Grep guard strips comment lines before counting (no self-invalidation); uses node:fs only (no child_process).
    - tsc baseline not grown.
  </acceptance_criteria>
  <done>Hub empty-state RED spec + legacy-routes grep guard authored; both RED for the correct reason; MeuPerfilPage KEEP positive-control green.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright navegability smoke — 4 journeys (D-16), gated real-auth</name>
  <read_first>
    - e2e/login-flow.spec.ts (L17-73 — REAL_AUTH gate, describeRealAuth, TEST_USER env pattern; the gate to reuse for RH/admin/candidate login journeys)
    - e2e/explicacao-flow.spec.ts (L39-67 — login(page) helper + page.goto + getByRole('heading',{name:/.../}) assertion idiom)
    - e2e/cadastro-flow.spec.ts (L23-90 — test.describe structure, imports, Date.now() fixtures)
    - playwright.config.ts (baseURL http://localhost:3003, 3 projects, timeout 60s)
    - 17-PATTERNS.md (§e2e/navegacao.spec.ts — the three combined analogs + workspace heading list)
  </read_first>
  <action>
    Create e2e/navegacao.spec.ts. Journey 4 (UNCONDITIONAL — no auth): page.goto('/rota/invalida/xyz') → expect getByRole('heading',{name:/Página não encontrada/i}) visible + expect a back-link (getByRole('link',{name:/Voltar/i})) visible. This is the only assertion that runs without live auth and it is RED today (no catch-all → React Router default error, not the NotFound heading).

    Journeys 1-3 under `const describeRealAuth = process.env.E2E_AUTH_TEST_USERS === 'true' ? test.describe : test.describe.skip` (login-flow.spec.ts precedent). (1) Candidato: login as TEST_USER → land on candidate area → navigate to /candidato/dashboard → click the step CTA (getByRole('button'|'link',{name:/Continuar para|Acompanhar/i})) → expect URL matches /\/candidato\/avaliacao\// OR the avaliação heading visible (route, not data). (2) RH: login-rh as the administrador test cred → open a vaga's TriagemTable (/rh/vagas/:id/candidatos) → click a candidate name → expect the hub heading visible → click "Abrir {label}" (getByRole name /Abrir /i) → expect each reachable workspace heading: "Entrevista", "Decisão final", or the Redação panel heading. (3) Admin: as administrador → expect sidebar getByRole({name:/Admin/i}) visible → click it → expect URL matches /\/admin\// and an admin heading visible.

    Add a file-top comment documenting the known smoke gap (RESEARCH A3): only `administrador` creds exist (0 `recrutador` rows), so the generic-`rh`-role path is not separately seeded; the admin cred covers journeys 2+3. RED today for all 4 (wiring absent). NEVER assert data flow (D-16) — only route/heading/CTA resolution.
  </action>
  <verify>
    <automated>npx playwright test navegacao --list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - e2e/navegacao.spec.ts parses and lists under all 3 Playwright projects (chromium + mobile-chrome + tablet).
    - Journey 4 (404) is unconditional (outside describeRealAuth); journeys 1-3 are inside the E2E_AUTH_TEST_USERS gate (grep confirms `E2E_AUTH_TEST_USERS`).
    - Running journey 4 against the current build is RED (no /Página não encontrada/ heading exists yet — React Router default error instead).
    - Assertions use getByRole heading/link/button (route+heading), NOT response-body/data assertions.
    - File-top comment records the `rh`-role seed gap (RESEARCH A3).
  </acceptance_criteria>
  <done>Navegability smoke spec authored with 4 journeys; journey-4 RED; RH/admin/candidate gated; lists in Playwright.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live Supabase (gated) | The RH/admin/candidate journeys log in against real auth only when E2E_AUTH_TEST_USERS=true; creds come from env, never committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-CRED | Information Disclosure | e2e/navegacao.spec.ts real-auth creds | mitigate | Read creds from process.env (TEST_USER_*/E2E_AUTH_TEST_USERS) per login-flow.spec.ts; never hardcode; gate journeys 1-3 behind describeRealAuth so CI without creds skips them. |
| T-17-01-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (RESEARCH Package Legitimacy Audit — N/A); no supply-chain surface. |
</threat_model>

<verification>
- `npm run test:run -- funilNavMap routes.nav RHSidebar.admin hubEmptyState legacy-routes` → all RED for the correct structural reason (module-not-found / missing element / dead-import still present).
- `npx playwright test navegacao --list` → spec parses; 4 journeys listed; journey-4 unconditional.
- `npm run lint` → tsc baseline (~301) not grown.
- 17-VALIDATION.md Wave 0 boxes can be checked + `nyquist_compliant`/`wave_0_complete` flipped true in its frontmatter by this plan's finalize step.
</verification>

<success_criteria>
6 RED specs authored (5 Vitest + 1 Playwright); each fails because the corresponding wiring is absent (the RED IS the gate); tsc baseline unchanged; no production code written; 17-VALIDATION.md Per-Task map populated for these specs.
</success_criteria>

<output>
Create `.planning/phases/17-navegacao-arquitetura-informacao/17-01-SUMMARY.md` when done.
</output>
