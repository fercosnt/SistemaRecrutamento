---
phase: 17-navegacao-arquitetura-informacao
plan: 02
type: execute
wave: 1
depends_on: ["17-01"]
files_modified:
  - src/lib/navegacao/funilNavMap.ts
  - src/components/pages/NotFoundPage.tsx
  - src/router/routes.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-17: src/lib/navegacao/funilNavMap.ts is the single source mapping every EtapaFunilM2 → { label, rotaCandidato, rotaWorkspaceRH, ctaCandidato, ctaRH }, reusing EtapaFunilM2 + ETAPA_M2_LABELS from triagemService (no parallel enum), with all :id route segments carrying a candidaturaId."
    - "D-14: a Beauty Smile glass NotFoundPage exists and routes.tsx has a catch-all path:'*' (appended LAST, no RoleGuard) resolving any unknown URL to it, with a role-aware back-link (candidato→/candidato/dashboard, rh|administrador→/rh/dashboard, null→/)."
    - "D-08: route inconsistency /rh/candidatos/:id (plural hub) vs /rh/candidato/:id/* (singular workspaces) is standardized to one canonical pattern with param-preserving redirects (a useParams wrapper, NOT a literal <Navigate to=':id'>) so existing links never break; the canonical hub route carries a candidaturaId."
    - "D-15: shared navigation logic lives in src/lib/navegacao/ (pure, React-free, Supabase-free — opcoesNormalize precedent); NotFoundPage edit stays a page in src/components/pages/ per the hybrid architecture."
    - "D-02: DevNavigationMenu stays untouched and DEV-gated in App.tsx — this plan does not remove or weaken it (the devnav-gate grep guard stays green)."
  artifacts:
    - path: "src/lib/navegacao/funilNavMap.ts"
      provides: "Exhaustive EtapaFunilM2 → nav entry map, candidaturaId-parameterized"
      contains: "export const funilNavMap"
    - path: "src/components/pages/NotFoundPage.tsx"
      provides: "Beauty Smile glass 404 with role-aware back-link"
      contains: "Página não encontrada"
    - path: "src/router/routes.tsx"
      provides: "catch-all path:'*' + normalization redirect(s)"
      contains: "path: '*'"
  key_links:
    - from: "src/lib/navegacao/funilNavMap.ts"
      to: "src/features/triagem/services/triagemService.ts"
      via: "import { EtapaFunilM2, ETAPA_M2_LABELS }"
      pattern: "from '@/features/triagem/services/triagemService'"
    - from: "src/router/routes.tsx"
      to: "src/components/pages/NotFoundPage.tsx"
      via: "catch-all route element"
      pattern: "path: '\\*'"
    - from: "src/components/pages/NotFoundPage.tsx"
      to: "src/store/authStore.ts"
      via: "useAuthStore role read for role-aware back-link"
      pattern: "useAuthStore"
---

<objective>
Lay the navigation foundation every downstream wiring plan consumes: the single-source
`funilNavMap` (D-17), the Beauty Smile glass `NotFoundPage` (D-14), and the `routes.tsx`
catch-all + route-normalization redirect (D-08/D-14). These are the two genuinely-new artifacts
(`funilNavMap.ts` pure derivation, `NotFoundPage.tsx` presentational) plus the route-table edits.

Purpose: The hub (17-03) and the candidate Dashboard CTA (17-04) BOTH import `funilNavMap`; the
E2E smoke (17-05) asserts the 404 + redirect. This plan flips the funilNavMap + route-table RED
specs (17-01) GREEN.
Output: 1 lib module + 1 page + route-table edits. No service/EF/migration (this is client routing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-RESEARCH.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-PATTERNS.md
@.planning/phases/17-navegacao-arquitetura-informacao/17-UI-SPEC.md

<interfaces>
From src/features/triagem/services/triagemService.ts (L293-325) — REUSE, do not recreate:
  export type EtapaFunilM2 = 'inscricao' | 'triagem' | 'avaliacao_assincrona' | 'entrevista_online' | 'entrevista_presencial' | 'decisao_final' | 'aprovado' | 'rejeitado'
  export const ETAPA_M2_LABELS: Record<EtapaFunilM2, string>

funilNavMap verified route targets (all :id = candidaturaId):
  inscricao              → rotaCandidato: null              | rotaWorkspaceRH: null
  triagem                → rotaCandidato: null              | rotaWorkspaceRH: null (hub itself)
  avaliacao_assincrona   → rotaCandidato: /candidato/avaliacao/${id} | rotaWorkspaceRH: null
  entrevista_online      → rotaCandidato: null              | rotaWorkspaceRH: /rh/candidato/${id}/entrevista
  entrevista_presencial  → rotaCandidato: null              | rotaWorkspaceRH: /rh/candidato/${id}/entrevista
  decisao_final          → rotaCandidato: null              | rotaWorkspaceRH: /rh/candidato/${id}/decisao
  aprovado               → rotaCandidato: null              | rotaWorkspaceRH: /rh/candidato/${id}/decisao
  rejeitado              → rotaCandidato: /candidato/explicacao/${id} | rotaWorkspaceRH: /rh/candidato/${id}/decisao
  ctaCandidato = "Continuar para {label}"  ·  ctaRH = "Abrir {label}"  (UI-SPEC copy)

From src/components/RoleGuard.tsx (L48-52) — ROLE_HOME shape to mirror (but use UI-SPEC back-link targets, NOT ROLE_HOME.candidato='/candidato/perfil'):
  candidato → "/candidato/dashboard" (per UI-SPEC §404)

NotFoundPage glass shell analog (DashboardCandidatoPage.tsx L92-99): BackgroundImage gradient + GlassCard centered. Imports: BackgroundImage, BeautySmileLogo, Glass/GlassCard/GlassButton from ../ui/glass, useAuthStore, useNavigate.

routes.tsx anchors: imports L11-90, admin routes L490-524, hub route /rh/candidatos/:id at L374-378 (currently <PerfilCandidatoRHPage/>), workspace routes L385-420, devNavigationPages near L533-567, 84 path: entries, NO path:'*' today.

opcoesNormalize.ts header doc (src/lib/opcoes/opcoesNormalize.ts L1-19) — mirror the "lives under src/lib/ so BOTH a feature and a page import it (acyclic feature→lib)" module-doc convention.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: funilNavMap.ts — single-source etapa→tela map (D-17)</name>
  <files>src/lib/navegacao/funilNavMap.ts</files>
  <read_first>
    - src/features/triagem/services/triagemService.ts (L293-325 — import EtapaFunilM2 + ETAPA_M2_LABELS verbatim; do NOT redeclare)
    - src/lib/opcoes/opcoesNormalize.ts (L1-19 — the module-doc convention + named-export pure-function style to mirror)
    - src/lib/navegacao/__tests__/funilNavMap.test.ts (the RED spec from 17-01 — this is the contract to satisfy)
    - src/router/routes.tsx (L385-420 — confirm the exact workspace route shapes /rh/candidato/:id/{entrevista,decisao} the map must emit; L195-215 for /candidato/avaliacao/:id + /candidato/explicacao/:id)
    - 17-PATTERNS.md (§funilNavMap — the FunilNavEntry interface + verified route-target table)
  </read_first>
  <behavior>
    - funilNavMap has exactly the 8 EtapaFunilM2 keys (Record<EtapaFunilM2,...> — TS exhaustiveness enforces it).
    - entry.label === ETAPA_M2_LABELS[etapa] for every key.
    - rotaCandidato('abc') === '/candidato/avaliacao/abc' for avaliacao_assincrona; '/candidato/explicacao/abc' for rejeitado; null for the rest.
    - rotaWorkspaceRH('abc') === '/rh/candidato/abc/entrevista' for entrevista_online/presencial; '/rh/candidato/abc/decisao' for decisao_final/aprovado/rejeitado; null for inscricao/triagem/avaliacao_assincrona.
    - ctaCandidato interpolates the label ("Continuar para {label}"); ctaRH = "Abrir {label}".
  </behavior>
  <action>
    Create src/lib/navegacao/funilNavMap.ts as a pure, React-free, Supabase-free module (mirror opcoesNormalize header doc explaining it lives in lib/ so BOTH the candidate Dashboard page and the RH hub feature import it acyclically per D-17). Import `type EtapaFunilM2` and `ETAPA_M2_LABELS` from '@/features/triagem/services/triagemService' — NEVER recreate the enum (a parallel copy reintroduces the 22P02 drift bug ETAPA_M2_LABELS was created to fix). Define and export `interface FunilNavEntry { label: string; rotaCandidato: (candidaturaId: string) => string | null; rotaWorkspaceRH: (candidaturaId: string) => string | null; ctaCandidato: string; ctaRH: string }`. Export `const funilNavMap: Record<EtapaFunilM2, FunilNavEntry>` with all 8 keys populated from the verified route-target table in <interfaces> — each label drawn from ETAPA_M2_LABELS, each route fn a candidaturaId arrow returning the exact path or null. Use named exports only (CLAUDE.md). Add a doc-comment noting that /candidato/prova-cognitiva/:id and /candidato/redacao/:id are sub-screens of /candidato/avaliacao/:id (the container fans out internally — A2), so avaliacao_assincrona routes to the avaliação container. No bg-primary, no fenced code blocks in this action.
  </action>
  <verify>
    <automated>npm run test:run -- funilNavMap 2>&1 | tail -15 ; npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 17-01's funilNavMap.test.ts flips GREEN (all 8 keys, label reuse, candidaturaId interpolation).
    - `grep -n "EtapaFunilM2\|ETAPA_M2_LABELS" src/lib/navegacao/funilNavMap.ts` shows IMPORTS from triagemService, NOT a local `type EtapaFunilM2 =` redeclaration.
    - Module imports zero React and zero Supabase (pure lib).
    - tsc baseline not grown.
  </acceptance_criteria>
  <done>funilNavMap.ts exhaustive over the 8 etapas, candidaturaId-keyed, reusing ETAPA_M2_LABELS; 17-01 unit spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NotFoundPage.tsx — Beauty Smile glass 404, role-aware back-link (D-14)</name>
  <files>src/components/pages/NotFoundPage.tsx</files>
  <read_first>
    - src/components/pages/DashboardCandidatoPage.tsx (L1-9 imports + L92-99 BackgroundImage glass shell skeleton to mirror)
    - src/components/RoleGuard.tsx (L48-52 ROLE_HOME shape — mirror the role-switch idiom but use UI-SPEC back-link targets)
    - src/store/authStore.ts (useAuthStore selector for `role`)
    - 17-UI-SPEC.md (§404 page — verbatim copy: "404" / "Página não encontrada" / body / 3 role-aware back-links; §Persona shells — 404 has NO persona navbar)
    - 17-PATTERNS.md (§NotFoundPage — imports + shell + role-aware back-link + bg-primary caveat)
  </read_first>
  <behavior>
    - Renders the display "404", heading "Página não encontrada", and the UI-SPEC body.
    - role==='candidato' → back-link "Voltar ao meu painel" → /candidato/dashboard.
    - role==='rh' || 'administrador' → "Voltar ao painel RH" → /rh/dashboard.
    - role==null → "Voltar ao início" → /.
    - Renders for any/unknown role without a persona navbar (standalone glass surface).
  </behavior>
  <action>
    Create src/components/pages/NotFoundPage.tsx (named export `NotFoundPage`, PascalCase). Mirror the DashboardCandidatoPage BackgroundImage glass skeleton: a centered GlassCard on BackgroundImage gradient, NO CandidatoNavbar/RHSidebar (must render for any/unknown role). Read `const role = useAuthStore((s) => s.role)`. Render the verbatim UI-SPEC copy: display numeral "404" (Montserrat 48px/600 — NO bold 700, the size carries weight), heading "Página não encontrada", body "O endereço que você tentou acessar não existe ou foi movido." Compute the role-aware back-link from a small switch on role per the UI-SPEC table and navigate via useNavigate()/GlassButton (NOT <a href> — SPA). The back-link is the single accent affordance (turquoise) and must be a ≥44px tap target (min-h-11). NEVER use bg-primary — use bg-[#00109E] or glass tokens (bg-white/NN / bg-[#35BFAD] accent). Use a visible text back-link (icon-only not allowed — a11y).
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -3 ; grep -c "bg-primary" src/components/pages/NotFoundPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists with named export NotFoundPage; contains the three verbatim strings ("404", "Página não encontrada", the body).
    - `grep -c "bg-primary" src/components/pages/NotFoundPage.tsx` === 0 (broken token avoided).
    - Role switch produces the 3 UI-SPEC back-link targets (/candidato/dashboard, /rh/dashboard, /); navigation uses useNavigate/GlassButton, not <a href>.
    - tsc baseline not grown.
  </acceptance_criteria>
  <done>NotFoundPage renders glass 404 with role-aware SPA back-link; no bg-primary; copy matches UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: routes.tsx — catch-all 404 + route normalization redirect (D-08/D-14)</name>
  <files>src/router/routes.tsx</files>
  <read_first>
    - src/router/routes.tsx (FULL — imports L11-90, /rh/candidatos/:id hub route L374-378, /rh/candidato/:id/* workspaces L385-420, end of routes array for the append-LAST catch-all, devNavigationPages L533-567)
    - src/router/__tests__/routes.nav.test.tsx (the 17-01 RED spec — the contract: catch-all present + redirect resolves)
    - src/features/triagem/components/TriagemTable.tsx (L324-325 — the existing <a href=/rh/candidatos/${candidato?.id}> link; note 17-03 will change it to row.id + SPA; THIS plan only normalizes the route table)
    - 17-RESEARCH.md (Pitfall 5 — <Navigate to=':id'> does NOT interpolate; use a useParams wrapper; Open Q1 — hub carries candidaturaId)
    - 17-PATTERNS.md (§routes.tsx — catch-all append-LAST + param-preserving redirect via wrapper + DO NOT introduce data-router redirect())
  </read_first>
  <action>
    Edit src/router/routes.tsx. (1) Import NotFoundPage from '../components/pages/NotFoundPage'. (2) Append the catch-all `{ path: '*', element: <NotFoundPage /> }` as the LAST entry in the routes array — NO RoleGuard (must render for any/unknown role). (3) Implement the D-08 normalization: choose ONE canonical pattern and lock that the hub route carries a candidaturaId (per RESEARCH Open Q1 — the hub keys on candidaturaId; this aligns with TriagemRow.id and every workspace useParams). Keep `/rh/candidatos/:id` (plural) as the canonical hub mount (it is the link TriagemTable already targets — D-04 says the link stays, only the destination CONTENT changes in 17-03), and add a param-preserving redirect for the singular `/rh/candidato/:id` BARE path (no sub-segment) → `/rh/candidatos/:id`, implemented as a tiny in-file wrapper component `function RedirectToHub() { const { id } = useParams<{id:string}>(); return <Navigate to={`/rh/candidatos/${id}`} replace /> }` (Pitfall 5 — NEVER a literal `<Navigate to="/rh/candidatos/:id">`). Do NOT touch the existing workspace sub-routes (/rh/candidato/:id/{entrevista,decisao,redacao}) — they keep their singular form and their RoleGuard. Document in a code comment that the canonical hub = plural + candidaturaId, workspaces = singular + candidaturaId, and the redirect bridges a bare-singular guess. (4) Do NOT delete any legacy import/route here — deletion is 17-05 (separate wave; avoids same-wave routes.tsx churn). (5) Leave DevNavigationMenu/App.tsx untouched (D-02).
  </action>
  <verify>
    <automated>npm run test:run -- routes.nav 2>&1 | tail -15 ; npm run test:run -- devnav-gate 2>&1 | tail -5 ; npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 17-01's routes.nav.test.tsx flips GREEN: catch-all path:'*' present → unknown URL renders "Página não encontrada"; a known-good route still resolves; the bare-singular hub redirect resolves param-preserving.
    - `grep -n "path: '\\*'" src/router/routes.tsx` returns the catch-all as the last route object.
    - No literal `<Navigate to="/rh/candidatos/:id"` string (Pitfall 5) — redirect goes through a useParams wrapper.
    - devnav-gate grep guard stays GREEN (App.tsx untouched — D-02).
    - `npm run build` exits 0 (no stale import); tsc baseline not grown.
  </acceptance_criteria>
  <done>Catch-all 404 appended; bare-singular hub redirect normalized param-preserving; build green; devnav gate intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unknown URL → router | Any/unknown path (incl. unauthenticated) hits the catch-all; the 404 must not leak protected route names or require a role. |
| old path → redirect | The normalization redirect maps a static internal path to another static internal path (never a user-supplied target). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-OR | Tampering | routes.tsx normalization redirect (D-08) | mitigate | Redirect target is a static internal path built only from the route `:id` param interpolated into a fixed template (`/rh/candidatos/${id}`); never derived from query/user input → no open-redirect. The :id stays a candidaturaId (Pitfall 1) — the wrapper does not swap id semantics. |
| T-17-02-404 | Information Disclosure | NotFoundPage role-aware back-link | mitigate | Back-link reads only the store `role` to pick one of 3 fixed internal targets; renders for null role; exposes NO protected route names or data (V7 error handling). No RoleGuard so it cannot redirect-loop a guard. |
| T-17-02-EOP | Elevation of Privilege | catch-all without RoleGuard | accept | The 404 is intentionally guard-free (must render for any role); it grants NO access — it is a terminal presentational page. Real access control stays on the actual /rh/* and /admin/* routes (RoleGuard + RLS, unchanged). |
| T-17-02-SC | Tampering | npm installs | accept | Zero packages installed (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npm run test:run -- funilNavMap routes.nav` → both GREEN (were RED in 17-01).
- `npm run test:run -- devnav-gate` → GREEN (D-02 intact).
- `npm run build` → exits 0; `npm run lint` → tsc baseline (~301) not grown.
- `grep -c "bg-primary" src/components/pages/NotFoundPage.tsx` → 0.
- No literal `<Navigate to="/rh/candidatos/:id"` in routes.tsx.
</verification>

<success_criteria>
funilNavMap.ts is the exhaustive candidaturaId-keyed single source reusing ETAPA_M2_LABELS; NotFoundPage renders the glass 404 with role-aware SPA back-link; routes.tsx has a catch-all + param-preserving normalization redirect; 17-01 funilNavMap + routes.nav specs are GREEN; build + tsc baseline clean; DevNav untouched.
</success_criteria>

<output>
Create `.planning/phases/17-navegacao-arquitetura-informacao/17-02-SUMMARY.md` when done.
</output>
