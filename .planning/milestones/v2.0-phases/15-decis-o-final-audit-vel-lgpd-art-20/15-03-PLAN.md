---
phase: 15-decis-o-final-audit-vel-lgpd-art-20
plan: 03
type: execute
wave: 2
depends_on: [15-02]
files_modified:
  - src/features/decisao/schemas/consolidacaoSchema.ts
  - src/features/decisao/schemas/decisaoSchema.ts
  - src/features/decisao/services/decisaoService.ts
  - src/features/decisao/hooks/useConsolidacao.ts
  - src/features/decisao/hooks/useRegistrarDecisao.ts
  - src/features/decisao/components/ConsolidacaoDashboard.tsx
  - src/features/decisao/components/RegistrarDecisaoForm.tsx
  - src/features/decisao/components/DecisaoFinalPage.tsx
  - src/features/decisao/components/__tests__/RegistrarDecisaoForm.test.tsx
  - src/features/decisao/services/__tests__/decisaoService.test.ts
autonomous: true
requirements: [DECISAO-01, DECISAO-02, DECISAO-03]
must_haves:
  truths:
    - "Per D-LOCK Área-1, DecisaoFinalPage renders the consolidar-decisao-final EF output (consolidated score + per-etapa breakdown + templated recommendation with SugestaoIABadge) and embeds the Phase-10 ComparativoScreen verbatim scoped to decisao_final finalists (DECISAO-02)"
    - "Per D-LOCK Área-2, RegistrarDecisaoForm gates the Registrar-decisão CTA on a decisao selection AND justificativa ≥50 chars (client mirror of the DB CHECK), confirms via alert-dialog, then calls registrar_decisao which fires the terminal transition (DECISAO-03)"
    - "The consolidated score is presented neutrally (no red/green) with the 'Agregado ponderado — não re-pontuado' caption; SugestaoIABadge appears ONLY on the recommendation block (RNF-07a)"
    - "consolidacaoSchema is the SHARED .strict() request contract imported by BOTH this client and the EF — closing the integration-contract-gap drift; Plan-01 contract test flips GREEN"
  artifacts:
    - path: "src/features/decisao/schemas/consolidacaoSchema.ts"
      provides: "shared .strict() consolidation request schema { candidatura_id, vaga_id }"
      contains: ".strict()"
    - path: "src/features/decisao/services/decisaoService.ts"
      provides: "allowlist reads + EF/RPC invoke + DecisaoServiceError"
      exports: ["decisaoService"]
    - path: "src/features/decisao/components/DecisaoFinalPage.tsx"
      provides: "RH consolidated decision page (dashboard + Comparativo embed + form)"
      min_lines: 40
  key_links:
    - from: "src/features/decisao/hooks/useConsolidacao.ts"
      to: "consolidar-decisao-final EF"
      via: "supabase.functions.invoke"
      pattern: "consolidar-decisao-final"
    - from: "src/features/decisao/components/DecisaoFinalPage.tsx"
      to: "src/features/triagem/components/ComparativoScreen.tsx"
      via: "verbatim import scoped to finalists"
      pattern: "ComparativoScreen|useComparativo"
    - from: "src/features/decisao/hooks/useRegistrarDecisao.ts"
      to: "registrar_decisao RPC"
      via: "supabase.rpc"
      pattern: "registrar_decisao"
---

<objective>
The RH consolidated decision surface (DECISAO-01/02/03 consumer side): the `src/features/decisao` feature — shared request schema, service (allowlist reads + EF/RPC invokes), hooks, and the three UI components (ConsolidacaoDashboard + RegistrarDecisaoForm + DecisaoFinalPage). Reuses the Phase-10 Comparativo verbatim. Route wiring is deferred to Plan 06.

Purpose: Composition over construction — every file copies a tested analog (15-PATTERNS.md). Flips the Plan-01 RegistrarDecisaoForm + decisaoService + contract Wave-0 tests GREEN.
Output: 10 files under src/features/decisao (8 source + 2 test-completions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-PATTERNS.md
@.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Analog files to copy (VERIFIED line refs from 15-PATTERNS.md). Use directly. -->

src/features/triagem/services/triagemService.ts (369 lines):
  :27-42 class TriagemServiceError extends Error { code } — copy as DecisaoServiceError (INVALID_INPUT|NETWORK_ERROR|DATABASE_ERROR|NOT_FOUND|UNAUTHORIZED)
  :243-275 invokeComparativo — supabase.functions.invoke + data?.ok error-code branching → copy for invokeConsolidacao('consolidar-decisao-final', { candidatura_id, vaga_id })
  :220-236 reprocessarAnalise — supabase.rpc + error-throw → copy for registrarDecisao calling rpc('registrar_decisao', {...})
  :341-369 updateCandidaturaEtapa / EtapaFunilM2 enum (reference only — the RPC owns the transition server-side)

src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx:
  :314-338 radio-group block → the 3-option decisao selector (Aprovar/Rejeitar/Manter em espera)
  :365-394 AlertDialog/AlertDialogTrigger asChild/AlertDialogAction onClick → irreversible confirm gate

REUSED VERBATIM (import, do NOT re-author):
  src/features/triagem/components/ComparativoScreen.tsx + hooks/useComparativo.ts + pdf/exportComparativo.ts
  src/features/triagem/components/SugestaoIABadge.tsx (SUGESTAO_IA_COPY='Sugestão da IA — decisão é sempre humana', accent #35BFAD)
  src/features/avaliacao/components/ScorecardAvaliacao.tsx (neutral badge pattern: border-white/15 bg-white/5 text-white/70)

useComparativo usage (RESEARCH §Code Examples): comparativo.mutate({ vagaId, candidaturaIds: finalistCandidaturaIds })
  finalists = candidaturas in this vaga that have a decisao_final row (decisaoService reads these via allowlist).

EF output JSON (from Plan-02 / Plan-01 interfaces): { consolidated:number|null, breakdown:[{etapa,normalized,weight,effective_weight,status}], recommendation:string }

decisaoSchema (zod, mirror config-vaga/schemas/pesosAvaliacaoSchema.ts shape):
  decisao: z.enum(['aprovado','rejeitado','em_espera']); justificativa: z.string().min(50, 'A justificativa precisa de pelo menos 50 caracteres.')

UI-SPEC copy contract (15-UI-SPEC §Copywriting) — use verbatim pt-BR strings (page heading 'Decisão final', caption 'Agregado ponderado — não re-pontuado', CTA 'Registrar decisão', counter '{n} / 50 mín.', toasts, alert-dialog rejeitado mentions LGPD Art. 20, etc.)
Color: consolidated score neutral text-3xl (no tint); breakdown ScorecardAvaliacao-neutral + N/A pill; rejeitado radio destructive tint, em_espera amber, aprovado/unselected neutral glass; SugestaoIABadge ONLY on recommendation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schemas + service + hooks (data layer)</name>
  <read_first>
    - src/features/triagem/services/triagemService.ts (:27-42 error class, :243-275 EF invoke, :220-236 RPC invoke)
    - src/features/config-vaga/schemas/pesosAvaliacaoSchema.ts (zod object+refine shape)
    - src/features/triagem/hooks/useComparativo.ts (:33-44 useMutation + toast.onError)
    - src/features/decisao/schemas/__tests__/consolidacaoContract.test.ts + src/features/decisao/services/__tests__/decisaoService.test.ts (the Wave-0 RED tests to flip GREEN)
  </read_first>
  <action>
    (a) src/features/decisao/schemas/consolidacaoSchema.ts — export `consolidacaoRequestSchema = z.object({ candidatura_id: z.string().uuid(), vaga_id: z.string().uuid() }).strict()` + `ConsolidacaoRequest` type + the `ConsolidacaoResponse` type (the EF output shape from interfaces). This is the SHARED contract the Plan-01 contract test imports.
    (b) src/features/decisao/schemas/decisaoSchema.ts — `decisaoSchema = z.object({ decisao: z.enum(['aprovado','rejeitado','em_espera']), justificativa: z.string().min(50, 'A justificativa precisa de pelo menos 50 caracteres.') })` + types.
    (c) src/features/decisao/services/decisaoService.ts (analog triagemService.ts) — `DecisaoServiceError` class; `getConsolidacao(candidaturaId, vagaId)` invoking the EF (copy invokeComparativo :243-275); `registrarDecisao({candidaturaId, decisao, justificativa})` via supabase.rpc('registrar_decisao', ...) (copy reprocessarAnalise :220-236); `listFinalistas(vagaId)` — ALLOWLIST read of decisao_final (`candidatura_id, decisao` — NEVER select('*')) to derive finalist candidaturaIds for the Comparativo; `getDecisaoAtual(candidaturaId)` — allowlist read (`decisao, justificativa, em`) to drive the append-only "Já existe uma decisão" note. NEVER select('*').
    (d) src/features/decisao/hooks/useConsolidacao.ts — useQuery over decisaoService.getConsolidacao (analog useComparativo, toast.onError).
    (e) src/features/decisao/hooks/useRegistrarDecisao.ts — useMutation over decisaoService.registrarDecisao; onSuccess toast.success 'Decisão registrada e etapa finalizada.' + invalidate; onError toast.error 'Não foi possível registrar a decisão. Tente novamente.'
  </action>
  <verify>
    <automated>npm run test:run -- consolidacaoContract decisaoService 2>&1 | tail -8 | grep -qiE "pass|✓" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- consolidacaoContract` GREEN (shared .strict() schema accepts valid, rejects unknown key)
    - `npm run test:run -- decisaoService` GREEN (terminal-transition mapping driven by the RPC; allowlist reads asserted)
    - `grep -c "select('\*')\|select(\"\*\")" src/features/decisao/services/decisaoService.ts` → 0
    - consolidacaoSchema exports `.strict()` request schema; decisaoSchema enforces min(50)
  </acceptance_criteria>
  <done>Data layer (schemas/service/hooks) for the RH decision surface; Wave-0 contract + decisaoService tests GREEN; no select('*').</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConsolidacaoDashboard + RegistrarDecisaoForm + DecisaoFinalPage (UI)</name>
  <read_first>
    - src/features/avaliacao/components/ScorecardAvaliacao.tsx (neutral badge pattern for breakdown rows)
    - src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx (:314-338 radio-group, :365-394 alert-dialog gate)
    - src/features/triagem/components/ComparativoScreen.tsx + hooks/useComparativo.ts + components/SugestaoIABadge.tsx (reuse verbatim)
    - src/features/admin/ai-costs/components/AiCostsPage.tsx (RHLayout + Glass shell + loading/error/empty states) + the Phase-14 EntrevistaWorkspace namespace (the /rh/candidato/:id/* shell to mirror)
    - src/features/decisao/components/__tests__/RegistrarDecisaoForm.test.tsx (Wave-0 RED test to flip GREEN)
    - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-UI-SPEC.md (copy + color contract)
  </read_first>
  <behavior>
    - RegistrarDecisaoForm: CTA disabled until a decisao is selected AND justificativa.length >= 50; char counter renders '{n} / 50 mín.'; too-short error 'A justificativa precisa de pelo menos 50 caracteres.'
    - rejeitado alert-dialog body mentions LGPD Art. 20 review; aprovado/em_espera dialog uses the neutral terminal copy
    - ConsolidacaoDashboard: missing etapa renders a neutral 'N/A' pill (no red/green); SugestaoIABadge renders ONLY in the recommendation block, never on the score or breakdown rows
  </behavior>
  <action>
    (a) src/features/decisao/components/ConsolidacaoDashboard.tsx — render useConsolidacao output: hero consolidated score (text-3xl neutral text-white + caption 'Agregado ponderado — não re-pontuado'); per-etapa breakdown table/list using ScorecardAvaliacao-neutral badges (border-white/15 bg-white/5 text-white/70), N/A pill + tooltip 'Etapa não aplicada — não pondera no agregado' for status!='present'; recommendation block with SugestaoIABadge variant="full" + advisory note 'Esta recomendação é gerada a partir do breakdown — a decisão é sempre humana.' Loading/error/empty states (UI-SPEC copy).
    (b) src/features/decisao/components/RegistrarDecisaoForm.tsx — react-hook-form + zodResolver(decisaoSchema); radio-group decisao (selected-state tints per UI-SPEC §Color); justificativa textarea + counter; alert-dialog confirm (rejeitado vs aprovado/em_espera copy); CTA gated per <behavior>; on confirm → useRegistrarDecisao.mutate. Append-only note when getDecisaoAtual returns an existing row.
    (c) src/features/decisao/components/DecisaoFinalPage.tsx — top-level RH page on RHLayout + Glass under /rh/candidato/:id/decisao (mirror EntrevistaWorkspace). Reads :id from route params; resolves vagaId via decisaoService. Stacks ConsolidacaoDashboard → ComparativoScreen embed (useComparativo.mutate scoped to listFinalistas(vagaId) candidaturaIds; empty-state copy 'Nenhum finalista para comparar ainda.') → RegistrarDecisaoForm. Optional tabs split per UI-SPEC (Dashboard default landing).
  </action>
  <verify>
    <automated>npm run test:run -- RegistrarDecisaoForm 2>&1 | tail -8 | grep -qiE "pass|✓" && npm run build 2>&1 | tail -3 | grep -qiE "built in|✓" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- RegistrarDecisaoForm` GREEN (client ≥50 gate + disabled CTA asserted)
    - `npm run build` exits 0
    - `grep -c "SugestaoIABadge" src/features/decisao/components/ConsolidacaoDashboard.tsx` ≥ 1 AND it appears in the recommendation block only (not on score/breakdown)
    - DecisaoFinalPage imports ComparativoScreen + useComparativo from src/features/triagem (verbatim reuse, no new comparison view)
    - `grep -v '^#' ... | grep -c "teste psicológico\|QI"` → 0 in the 3 components (LGPD-04; forbidden-strings.grep auto-covers)
    - tsc baseline ≤305 (`npm run lint`)
  </acceptance_criteria>
  <done>RH decision UI built (dashboard + form + page) reusing Comparativo/SugestaoIABadge/ScorecardAvaliacao; RegistrarDecisaoForm Wave-0 test GREEN; build 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH browser → consolidar-decisao-final EF | client invoke; EF re-authorizes (defense in depth; route RoleGuard wired in Plan 06) |
| RH form → registrar_decisao RPC | decision payload crosses into the terminal-transition RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Info Disclosure | decisaoService over-projects PII via select('*') | mitigate | allowlist reads only (listFinalistas, getDecisaoAtual); acceptance grep asserts no select('*') [[reference_select_star_leaks_pii]] |
| T-15-10 | Tampering | client-only justificativa validation bypassed | accept | client ≥50 is UX; the DB CHECK + registrar_decisao re-assert ≥50 are the truth (defense in depth) |
| T-15-11 | Spoofing | unauthorized RH reaches the page | mitigate | route RoleGuard role=['rh','administrador'] (Plan 06) + EF re-authorize (Plan 02) — layered |
| T-15-SC | Tampering | npm installs | mitigate | NO new packages — all primitives vendored; Comparativo/jspdf already installed |
</threat_model>

<verification>
- `npm run test:run -- consolidacaoContract decisaoService RegistrarDecisaoForm` → all GREEN
- `npm run build` → exit 0
- no select('*') in decisaoService; SugestaoIABadge only on recommendation; Comparativo reused verbatim
- tsc baseline ≤305
</verification>

<success_criteria>
- src/features/decisao complete (schema/service/hooks/3 components); 3 Wave-0 tests GREEN
- Consolidated score neutral; recommendation advisory-badged; Comparativo embedded scoped to finalists (DECISAO-02)
- Decision capture gated ≥50 + alert-dialog; RPC owns the terminal transition (DECISAO-03)
</success_criteria>

<output>
Create `.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-03-SUMMARY.md` when done
</output>
