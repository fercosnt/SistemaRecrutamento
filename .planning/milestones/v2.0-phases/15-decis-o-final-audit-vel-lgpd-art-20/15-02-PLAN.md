---
phase: 15-decis-o-final-audit-vel-lgpd-art-20
plan: 02
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - supabase/migrations/20260625100001_decisao_final_phase15.sql
  - supabase/functions/consolidar-decisao-final/index.ts
autonomous: true
requirements: [DECISAO-01, DECISAO-03, DECISAO-04, LGPD-03]
must_haves:
  truths:
    - "Per D-LOCK Área-2, registrar_decisao is a SECURITY DEFINER RPC: RH+own-vaga guard, writes decisao_final with por_usuario := auth.uid() (LGPD-02), aprovado/rejeitado fire the terminal avancar_etapa transition, em_espera writes the row with NO etapa change (Pitfall 5)"
    - "Per D-LOCK Área-3, solicitar_revisao_decisao + stamp_explicacao_acessada are SECURITY DEFINER own-row RPCs (candidato owns the candidatura) setting decisao_final.revisao_solicitada_em / explicacao_solicitada_em — NO new table (existing columns)"
    - "Per D-LOCK Área-4, gerar_bias_snapshot is an admin-only SECURITY DEFINER RPC deriving age server-side and writing ONE bias_audit_log row with banded aggregates only (no per-candidate PII)"
    - "Per D-LOCK Área-1, consolidar-decisao-final EF is DETERMINISTIC (no LLM): authorize-then-act, maps 4 weight keys → 2 score tables, normalizes each to 0-100, renormalizes weights over present etapas, big_five/cognitivo context-only, emits a templated recommendation"
    - "The EF never re-scores (reads already-recorded scores) and never auto-decides (RNF-07a); decision NEVER persists without a human actor (por_usuario NOT NULL)"
  artifacts:
    - path: "supabase/migrations/20260625100001_decisao_final_phase15.sql"
      provides: "4 SECURITY DEFINER RPCs: registrar_decisao, solicitar_revisao_decisao, stamp_explicacao_acessada, gerar_bias_snapshot"
      contains: "CREATE OR REPLACE FUNCTION"
    - path: "supabase/functions/consolidar-decisao-final/index.ts"
      provides: "deterministic consolidation EF (authorize-then-act, no callAi)"
      min_lines: 120
  key_links:
    - from: "supabase/functions/consolidar-decisao-final/index.ts"
      to: "usuarios_rh + vagas.created_by"
      via: "authorize: role from usuarios_rh, ownership via vagas.created_by"
      pattern: "usuarios_rh"
    - from: "supabase/migrations/20260625100001_decisao_final_phase15.sql"
      to: "candidaturas.etapa_atual"
      via: "registrar_decisao UPDATE fires avancar_etapa() for aprovado/rejeitado"
      pattern: "avancar_etapa|etapa_atual"
    - from: "supabase/migrations/20260625100001_decisao_final_phase15.sql"
      to: "decisao_final.por_usuario"
      via: "por_usuario := auth.uid() (LGPD-02 guardrail)"
      pattern: "por_usuario"
---

<objective>
Author the Phase-15 backend: ONE no-wrapper migration with 4 SECURITY DEFINER RPCs (the ONLY writers of decisao_final + bias_audit_log, since client INSERT/UPDATE are RLS-blocked) and the deterministic `consolidar-decisao-final` Edge Function. AUTHORED-NOT-APPLIED — the orchestrator applies to PROD in Plan 06 ([BLOCKING]).

Purpose: DECISAO-01 (EF aggregation), DECISAO-03 (decision capture RPC + terminal transition), DECISAO-04 (candidate own-row RPCs), LGPD-03 (bias snapshot RPC). This wave makes the Wave-0 Deno golden test (Task 1 of Plan 01) flip GREEN for the consolidation; the bias/decision RPC behavior is verified by SQL smokes in Plan 06.
Output: 1 migration (authored, 42601-safe) + 1 deployable EF (authored).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-RESEARCH.md
@.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- LIVE schema + analog contracts (VERIFIED 2026-06-25). Use directly — no codebase exploration. -->

decisao_final (LIVE, migrations/20260607000003) — RPC writes here; client INSERT blocked WITH CHECK(false), NO client UPDATE policy:
  id uuid PK, candidatura_id uuid NOT NULL UNIQUE → candidaturas(id),
  decisao public.decisao_final_resultado NOT NULL,   -- enum: 'aprovado'|'rejeitado'|'em_espera'
  justificativa text NOT NULL CHECK (length(justificativa) >= 50),
  por_usuario uuid NOT NULL → auth.users(id),         -- LGPD-02 guardrail
  em timestamptz DEFAULT now(),
  explicacao_solicitada_em timestamptz, revisao_solicitada_em timestamptz, revisao_resultado text
  RLS: rh_le_decisao_final (SELECT role IN rh/administrador), candidato_le_propria_decisao (SELECT own via candidatos.user_id=auth.uid())

bias_audit_log (LIVE, schema-only since Phase 6): id, snapshot_em timestamptz, periodo text, dados jsonb, criado_em timestamptz

scores_candidato (LIVE, Phase 11): id, candidatura_id, tipo (enum sjt/big_five/redacao/entrevista/cognitivo/decisao), subtipo, score, score_max, status, metadata jsonb
analise_candidato_vaga (LIVE, Phase 10): candidatura_id, score_match (int 0..100), status
vagas (LIVE): id, created_by (owner uuid — VERIFIED database.types.ts:2720), pesos_avaliacao jsonb (keys triagem/work_sample_sjt/redacao_cultural/entrevista, sum=100)
candidatos (LIVE): id, user_id, data_nascimento (date NOT NULL)
candidaturas (LIVE): id, candidato_id, vaga_id, etapa_atual

avancar_etapa() trigger (LIVE, migrations/20260607000005): terminal aprovado/rejeitado allowed from any stage; writes ONE historico_candidatura row same-txn; ator=auth.uid() survives SECURITY DEFINER. etapa_processo has terminal aprovado/rejeitado but NO em_espera.

Migration analog — supabase/migrations/20260625000001_phase14_gap_closure.sql:
  - candidate own-row guard (:91-103): EXISTS via candidaturas JOIN candidatos ON ca.user_id=auth.uid(); RAISE 'forbidden' errcode '42501'
  - RH role+own-vaga guard (:235-245): v_role := auth.jwt() #>> '{app_metadata,role}'; IN ('rh','administrador'); rh must own vaga.created_by
  - footer per RPC (:193-199,:265-269): REVOKE ALL FROM PUBLIC; GRANT EXECUTE TO authenticated; COMMENT ON FUNCTION
  - NO BEGIN/COMMIT wrapper (D-22); SET search_path = ''; RETURN updated row (readback, no silent no-op)

EF analog — supabase/functions/comparativo-candidatos/index.ts (352 lines):
  :36-50 imports (createClient ONLY — DROP all AI), :56-73 corsHeaders/jsonResponse/errorResponse/ErrorCode,
  :79-112 Deps interface + handler(req,deps), :114-190 authenticate(getUser)→authorize(role from usuarios_rh, vagas.created_by ownership, admin bypass),
  :194-200 allowlist read idiom, :288-352 redacted log + Deno.serve two-client wiring (JWT-on)

Consolidation request schema (shared with Plan 01 contract test + Plan 03 client) — must be .strict():
  z.object({ candidatura_id: z.string().uuid(), vaga_id: z.string().uuid() }).strict()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration — 4 SECURITY DEFINER RPCs (registrar_decisao + 2 candidate own-row + gerar_bias_snapshot)</name>
  <read_first>
    - supabase/migrations/20260625000001_phase14_gap_closure.sql (the exact authoring convention: own-row guard :91-103, RH+own-vaga guard :235-245, REVOKE/GRANT/COMMENT footer :193-199; no-wrapper header)
    - supabase/migrations/20260607000003_decisao_final.sql (the live table + RLS — confirms client INSERT/UPDATE blocked, so RPC is the ONLY writer)
    - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-PATTERNS.md (RPC pattern assignments) + 15-RESEARCH.md §Architecture Patterns 2 & 4, Pitfalls 2 & 5
  </read_first>
  <action>
    Create supabase/migrations/20260625100001_decisao_final_phase15.sql with the no-wrapper header (D-22, AUTHORED-NOT-APPLIED, applies via Supabase MCP apply_migration). Four SECURITY DEFINER functions, `SET search_path = ''`, each with REVOKE ALL FROM PUBLIC + GRANT EXECUTE TO authenticated + a COMMENT documenting the guard + RNF-07a invariant:

    (1) `registrar_decisao(p_candidatura_id uuid, p_decisao public.decisao_final_resultado, p_justificativa text)` — DECISAO-03 / Pattern 2. Guard: v_role from auth.jwt() #>> '{app_metadata,role}' IN ('rh','administrador'); resolve candidatura→vaga, if v_role='rh' require vagas.created_by = auth.uid() (administrador bypasses), mirror confirmar_revisao_entrevista :235-245. Re-assert length(p_justificativa) >= 50 (the DB CHECK is also live — defense in depth). UNIQUE(candidatura_id) reconciliation (Pitfall 2 / Open Q2 rec a): INSERT ... ON CONFLICT (candidatura_id) DO UPDATE SET decisao=excluded, justificativa=excluded, por_usuario=auth.uid(), em=now() — current row is UPSERT, amendment history lives in historico_candidatura via the trigger. por_usuario := auth.uid() ALWAYS (LGPD-02). Terminal map (Pitfall 5): IF p_decisao='aprovado' THEN UPDATE candidaturas SET etapa_atual='aprovado' WHERE id=p_candidatura_id; ELSIF p_decisao='rejeitado' THEN UPDATE ... 'rejeitado'; ELSE (em_espera) NO etapa change. The candidaturas UPDATE fires avancar_etapa() which writes the ONE historico_candidatura row — do NOT also manual-INSERT historico (Phase-8 survivor double-write lesson). RETURN the decisao_final row.

    (2) `solicitar_revisao_decisao(p_candidatura_id uuid)` — DECISAO-04. Candidate own-row guard (mirror pontuar_cognitivo :91-103: EXISTS candidaturas JOIN candidatos ON ca.user_id=auth.uid(), else RAISE 'forbidden' errcode '42501'). Require a decisao_final row with decisao='rejeitado' exists (reachability — Pitfall 6) else RAISE. UPDATE decisao_final SET revisao_solicitada_em = now() WHERE candidatura_id=p_candidatura_id AND revisao_solicitada_em IS NULL (idempotent — don't overwrite an existing request). RETURN the row.

    (3) `stamp_explicacao_acessada(p_candidatura_id uuid)` — DECISAO-04. Same candidate own-row guard. UPDATE decisao_final SET explicacao_solicitada_em = COALESCE(explicacao_solicitada_em, now()) WHERE candidatura_id=p_candidatura_id (stamp first-access only). RETURN the row.

    (4) `gerar_bias_snapshot(p_periodo text)` — LGPD-03 / Pattern 4. Admin-only guard (v_role = 'administrador' else RAISE 42501). Derive age server-side: `date_part('year', age(c.data_nascimento))` from candidatos joined through candidaturas that HAVE a decisao_final row (applicants := has decisao_final — Open Q4 rec). Bucket into bands 18-24/25-34/35-44/45-54/55+; per band compute applicants, selected (decisao='aprovado'), selection_rate; reference_band = highest rate; razao_4_5 = rate/reference_rate; flag = razao_4_5 < 0.8 (Pitfall 3 small_sample_warning if any band applicants < 30); count + report null/invalid data_nascimento exclusions in dados (Pitfall 4). INSERT ONE bias_audit_log(periodo, dados) with the self-describing jsonb shape from RESEARCH §EEOC 4/5 (metodo, limitacao 'apenas faixa etária — raça/gênero não coletados (LGPD-01)', populacao, faixa_referencia, bands[], n_total, small_sample_warning, excluidos_sem_data) — banded aggregates ONLY, NO per-candidate rows. RETURN the inserted row.
  </action>
  <verify>
    <automated>grep -cE "CREATE OR REPLACE FUNCTION" supabase/migrations/20260625100001_decisao_final_phase15.sql | grep -q 4 && grep -q "por_usuario" supabase/migrations/20260625100001_decisao_final_phase15.sql && grep -q "ON CONFLICT (candidatura_id)" supabase/migrations/20260625100001_decisao_final_phase15.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 4 `CREATE OR REPLACE FUNCTION`; each has SECURITY DEFINER + `SET search_path = ''` + REVOKE/GRANT/COMMENT footer
    - `grep -c "BEGIN;\|COMMIT;" ...` on lines that are NOT PL/pgSQL block delimiters → 0 wrapper (no transaction wrapper; D-22)
    - registrar_decisao uses `ON CONFLICT (candidatura_id) DO UPDATE`, sets por_usuario := auth.uid(), maps only aprovado/rejeitado to etapa_atual (NOT em_espera), and does NOT manual-INSERT historico_candidatura (`grep -c "INSERT INTO public.historico_candidatura\|INSERT INTO historico_candidatura" → 0`)
    - gerar_bias_snapshot inserts ONE bias_audit_log row with `dados` carrying bands[] + limitacao + small_sample_warning; `grep -q "date_part" ` for server-side age
    - the two candidate RPCs guard via `candidatos` ... `user_id = auth.uid()` and `errcode '42501'`
  </acceptance_criteria>
  <done>Migration authored (no-wrapper, 42601-safe) with 4 RPCs; registrar_decisao terminal map + LGPD-02 + append-only UPSERT; candidate own-row RPCs; admin bias snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author consolidar-decisao-final EF (deterministic aggregation, no LLM) — flips Wave-0 Deno test GREEN</name>
  <read_first>
    - supabase/functions/comparativo-candidatos/index.ts (the verbatim auth skeleton :114-190; CORS/error helpers :56-73; Deps+handler :79-112; redacted log + Deno.serve :288-352 — STRIP all callAi/Anthropic/OpenAI/zod-helper)
    - supabase/functions/consolidar-decisao-final/__tests__/index.test.ts (the Wave-0 RED golden test — implement to its assertions)
    - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-RESEARCH.md §Consolidation Aggregation (steps 1-7) + §Code Examples
  </read_first>
  <behavior>
    - Authorize: non-RH role → 403; role='rh' & vagas.created_by != user.id → 403; administrador bypasses ownership
    - triagem ← analise_candidato_vaga.score_match (0..100 as-is), PRESENT only if status='sucesso'
    - work_sample_sjt ← scores_candidato tipo='sjt': normalized = score/score_max*100, PRESENT only if status='sucesso'
    - redacao_cultural ← scores_candidato tipo='redacao': score/25*100 (or score/score_max*100), PRESENT only if status='sucesso'
    - entrevista ← scores_candidato tipo='entrevista': N/A unless status='sucesso' (Open Q1 — never weight a pendente_humano AI score)
    - effective_weight[k] = weight[k] / Σ(weight[present]); consolidated = Σ(normalized × effective_weight) over present; null if none present
    - big_five + cognitivo → breakdown rows status='context', weight=null, contribute 0
    - recommendation = deterministic template keyed on consolidated band + N/A count + any pendente/red-flag; NEVER an LLM call
    - never re-scores (no evaluation-EF invoke); never auto-decides
  </behavior>
  <action>
    Create supabase/functions/consolidar-decisao-final/index.ts cloning the comparativo-candidatos skeleton. Imports: `createClient` from esm.sh ONLY — NO ai-client, NO Anthropic/OpenAI, NO zodOutputFormat (Pitfall 8 — minimal deploy surface, deterministic). Copy corsHeaders/jsonResponse/errorResponse/ErrorCode verbatim. Validate the request body with the SHARED `.strict()` schema `{ candidatura_id: uuid, vaga_id: uuid }` (use static `npm:zod` import per project precedent — NEVER the `.join("npm:")` bug). Copy the authenticate→authorize block EXACTLY (role from usuarios_rh, recrutador→rh map, vagas.created_by ownership for rh, administrador bypass — [[reference_ef_authenticate_vs_authorize]]). Two privileged allowlist reads (NEVER select('*')): analise_candidato_vaga (candidatura_id, score_match, status) + scores_candidato (id, candidatura_id, tipo, subtipo, score, score_max, status, metadata). Read vaga.pesos_avaliacao. Implement the RESEARCH §Consolidation Aggregation algorithm steps 1-7 exactly (the <behavior> block). Emit JSON { consolidated, breakdown[], recommendation } per the Plan-01 interfaces contract. Copy the redacted console.log (ids/counts ONLY — NEVER scores/justificativa/PII) + the production Deno.serve two-client wiring. Deploy JWT-on (NO --no-verify-jwt — the orchestrator deploys in Plan 06).
  </action>
  <verify>
    <automated>cd supabase/functions && deno test consolidar-decisao-final/ 2>&1 | tail -8 | grep -qiE "ok|passed|test result.*0 failed" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `deno test consolidar-decisao-final/` flips GREEN (the Wave-0 golden test passes against the new handler)
    - `grep -c "callAi\|Anthropic\|OpenAI\|zodOutputFormat" supabase/functions/consolidar-decisao-final/index.ts` → 0 (no LLM, no AI import)
    - `grep -c "select('\*')\|select(\"\*\")" ...` → 0 (allowlist reads only)
    - `grep -q "usuarios_rh" ...` and `grep -q "created_by" ...` (authorize present)
    - `grep -q "effective_weight\|renormal\|status='sucesso'\|sucesso" ...` (renormalization over present etapas)
    - import uses static `npm:` (no `.join`): `grep -c "\.join(" ...` for an import line → 0
  </acceptance_criteria>
  <done>Deterministic consolidation EF authored; Wave-0 Deno golden test GREEN; no LLM, no select('*'), authorize-then-act intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → consolidar-decisao-final EF | untrusted JWT + body crosses into a privileged service_role read |
| candidate → solicitar_revisao_decisao / stamp_explicacao_acessada RPC | candidate-supplied candidatura_id crosses into an own-row UPDATE |
| admin → gerar_bias_snapshot RPC | admin trigger crosses into PII-adjacent age derivation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Elevation/Info Disclosure | consolidar-decisao-final EF (IDOR — RH reads another RH's candidate consolidation) | mitigate | authorize-then-act: role from usuarios_rh (NOT JWT claims), vagas.created_by ownership for rh, admin bypass — clone comparativo-candidatos :114-190 [[reference_ef_authenticate_vs_authorize]] |
| T-15-04 | Tampering/Repudiation | decision persisted with no human actor | mitigate | por_usuario := auth.uid() in registrar_decisao; DB por_usuario NOT NULL; client INSERT/UPDATE RLS-blocked → RPC is sole writer |
| T-15-05 | Elevation | candidate writes another candidate's decisao_final | mitigate | candidate own-row guard (candidatos.user_id=auth.uid()) in both candidate RPCs; errcode 42501 on mismatch |
| T-15-06 | Info Disclosure | bias snapshot re-identification via small bands / per-candidate rows | mitigate | banded aggregates only in dados jsonb (no per-candidate rows); small_sample_warning; age derived server-side, never persisted per-row |
| T-15-07 | Info Disclosure | EF logs leak scores/justificativa/PII | mitigate | redacted console.log (ids/counts only) copied from comparativo-candidatos :288 |
| T-15-08 | Elevation | non-admin generates bias snapshot | mitigate | gerar_bias_snapshot guards v_role='administrador' only; errcode 42501 |
| T-15-SC | Tampering | npm/deno installs | mitigate | NO new packages (createClient + zod already vendored in EF runtime); static npm: import only |
</threat_model>

<verification>
- `deno test supabase/functions/consolidar-decisao-final/` → GREEN
- Migration grep gates (4 functions, ON CONFLICT, por_usuario, no manual historico INSERT, no wrapper) pass
- `npm run lint` tsc baseline ≤305 (EF/migration outside tsc scope — zero growth)
- AUTHORED-NOT-APPLIED: migration + EF NOT yet on PROD (Plan 06 [BLOCKING] applies)
</verification>

<success_criteria>
- 4 RPCs authored with correct guards, terminal map, append-only UPSERT, LGPD-02 guardrail, admin bias snapshot
- Consolidation EF deterministic (no LLM), authorize-then-act, allowlist reads, renormalize-over-present — Wave-0 Deno golden test GREEN
- Both files 42601-safe / static-import-safe; ready for orchestrator PROD apply in Plan 06
</success_criteria>

<output>
Create `.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-02-SUMMARY.md` when done
</output>
