---
phase: 15-decis-o-final-audit-vel-lgpd-art-20
plan: 04
type: execute
wave: 2
depends_on: [15-02]
files_modified:
  - src/features/explicacao/services/explicacaoService.ts
  - src/features/explicacao/hooks/useExplicacao.ts
  - src/features/explicacao/components/ExplicacaoCandidatoPage.tsx
  - src/features/explicacao/components/SolicitarRevisaoCTA.tsx
  - src/features/explicacao/services/__tests__/explicacaoService.test.ts
  - e2e/explicacao-flow.spec.ts
autonomous: true
requirements: [DECISAO-04]
must_haves:
  truths:
    - "Per D-LOCK Área-3, ExplicacaoCandidatoPage is reachable only after a decisao_final with decisao='rejeitado' exists (RLS own-candidatura + reachability gate); otherwise it renders 'Esta página não está disponível' (Pitfall 6)"
    - "The candidate read uses an explicit own-row allowlist (decisao, justificativa, revisao_solicitada_em, revisao_resultado, explicacao_solicitada_em) and NEVER select('*'), NEVER joins scores_candidato — the candidate never sees a score/band/percentile (RNF-07a/LGPD-04, [[reference_select_star_leaks_pii]])"
    - "Per Open Q5, the candidate sees a respectful, non-clinical reason derived from the justificativa — not raw internal phrasing/scores; visiting the page stamps explicacao_solicitada_em"
    - "Per D-LOCK Área-3, 'Solicitar revisão por pessoa natural' sets decisao_final.revisao_solicitada_em via the own-row RPC + notifies vaga.created_by via the existing N8N webhook (distinct path), idempotent once requested"
  artifacts:
    - path: "src/features/explicacao/services/explicacaoService.ts"
      provides: "own-row allowlist read + stamp/revision RPC invokes + error class"
      exports: ["explicacaoService"]
    - path: "src/features/explicacao/components/ExplicacaoCandidatoPage.tsx"
      provides: "candidate glass-over-gradient LGPD Art. 20 explanation page (read-mostly)"
      min_lines: 40
  key_links:
    - from: "src/features/explicacao/services/explicacaoService.ts"
      to: "decisao_final (own-row allowlist)"
      via: "candidato_le_propria_decisao RLS policy + explicit columns"
      pattern: "revisao_solicitada_em|explicacao_solicitada_em"
    - from: "src/features/explicacao/components/SolicitarRevisaoCTA.tsx"
      to: "solicitar_revisao_decisao RPC"
      via: "supabase.rpc"
      pattern: "solicitar_revisao_decisao"
    - from: "src/features/explicacao/components/ExplicacaoCandidatoPage.tsx"
      to: "stamp_explicacao_acessada RPC"
      via: "useQuery mount stamps on visit"
      pattern: "stamp_explicacao_acessada"
---

<objective>
The candidate LGPD Art. 20 explanation surface (DECISAO-04): the `src/features/explicacao` feature — own-row allowlist service, hook, and the candidate page + revision CTA. Read-mostly variant of the ProvaCognitivaScreen glass-over-gradient shell. Route wiring is deferred to Plan 06.

Purpose: A transparency surface, not a score dump. The candidate sees a respectful non-clinical reason + high-level result, NEVER scores/bands. Reachable only after a rejection. Flips the Plan-01 explicacaoService allowlist Wave-0 test GREEN.
Output: 4 source files under src/features/explicacao + 1 test-completion + 1 E2E spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-PATTERNS.md
@.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Analog files (VERIFIED line refs from 15-PATTERNS.md). Use directly. -->

src/features/avaliacao-cognitiva/services/cognitivoService.ts (243 lines) — the candidate-data-layer analog:
  ~:58 COGNITIVO_ITENS_ALLOWLIST constant (explicit columns, NEVER '*') → copy as DECISAO_EXPLICACAO_ALLOWLIST
  :201-243 submitProva — supabase.rpc(...) + code==='42501'||status===403 handling → copy for solicitarRevisao / stampExplicacao
  :35-50 custom error class → copy as ExplicacaoServiceError

src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx — the candidate shell analog:
  :402-416 ScreenShell (BackgroundImage gradient + overlayColor=bg-black overlayOpacity=15 + container mx-auto px-4 max-w-2xl + py-20)
  :172-272 state-machine rendering (loading / load-error retry / not-available / done) as GlassPanel variant="white" blur="xl" cards
  :104-110 useQuery mount pattern → stamp explicacao_solicitada_em on visit
  :365-394 AlertDialog gate → the SolicitarRevisaoCTA confirm

LIVE schema / RLS (from Plan-02 interfaces):
  decisao_final candidate read relies on the LIVE candidato_le_propria_decisao RLS policy (scopes via candidatos.user_id=auth.uid()).
  Own-row allowlist columns: decisao, justificativa, revisao_solicitada_em, revisao_resultado, explicacao_solicitada_em. NEVER scores_candidato.
  RPCs (Plan 02): solicitar_revisao_decisao(p_candidatura_id), stamp_explicacao_acessada(p_candidatura_id) — own-row guarded, idempotent.

Open Q5 (RESEARCH): candidate sees a deterministic templated non-clinical reason keyed on decisao + (optionally a sanitized summary), NOT the raw internal justificativa verbatim. Reachability gate (Pitfall 6): only render content when decisao='rejeitado'; otherwise the not-available state.

UI-SPEC copy (15-UI-SPEC §Candidate LGPD Art. 20) — use verbatim pt-BR: heading 'Sobre a sua candidatura', result line 'Após avaliarmos seu processo, decidimos não seguir com a sua candidatura nesta vaga.', revision-right intro mentioning LGPD Art. 20, CTA 'Solicitar revisão por pessoa natural', confirm dialog copy, success/already-requested/error toasts, not-available + load-error states, 'Voltar ao painel'. min-h-[44px] on CTAs.
Color: candidate surface carries NO score/band/ratio/color verdict — only neutral glass card + respectful reason + revision CTA.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: explicacaoService (own-row allowlist + RPC invokes) + useExplicacao hook</name>
  <read_first>
    - src/features/avaliacao-cognitiva/services/cognitivoService.ts (~:58 allowlist constant, :35-50 error class, :201-243 RPC + 42501 handling)
    - src/features/explicacao/services/__tests__/explicacaoService.test.ts (the Wave-0 RED test to flip GREEN — asserts no '*', no scores)
    - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-PATTERNS.md §explicacaoService
  </read_first>
  <action>
    (a) src/features/explicacao/services/explicacaoService.ts — `DECISAO_EXPLICACAO_ALLOWLIST = 'decisao, justificativa, revisao_solicitada_em, revisao_resultado, explicacao_solicitada_em'` (NEVER '*', NEVER scores_candidato). `ExplicacaoServiceError` class (analog cognitivoService :35-50). `getExplicacao(candidaturaId)` — read decisao_final with the allowlist, scoped to the candidatura (RLS candidato_le_propria_decisao enforces own-row); RETURN null/not-available unless decisao='rejeitado' (reachability gate — Pitfall 6). Derive a respectful non-clinical `reason` (Open Q5) — a templated reason keyed on decisao; do NOT surface raw internal justificativa tone/scores. `stampExplicacao(candidaturaId)` — supabase.rpc('stamp_explicacao_acessada', ...) (copy submitProva :201-243 42501 handling). `solicitarRevisao(candidaturaId)` — supabase.rpc('solicitar_revisao_decisao', ...). NEVER expose a score/band/percentile.
    (b) src/features/explicacao/services/explicacaoService.ts (same file) — PIN the N8N notification path (resolves RESEARCH Open Q3 in favor of the thin-client idiom, NOT pg_net): inside `solicitarRevisao`, after the `solicitar_revisao_decisao` RPC resolves OK, fire a fire-and-forget `fetch(N8N_REVISAO_DECISAO_URL, { method:'POST', body: JSON.stringify({ event:'decisao.revisao_solicitada', timestamp, data:{ candidatura_id } }) }).catch(...)` — copy the submit-candidatura/index.ts:303-323 redacted fire-and-forget idiom (NO PII beyond ids; non-blocking; never throws into the mutation). Read the URL from an env constant (`VITE_N8N_REVISAO_DECISAO_URL`, distinct from the nova-candidatura webhook — RESEARCH §Runtime State). (c) src/features/explicacao/hooks/useExplicacao.ts — useQuery over getExplicacao (onSuccess fires stampExplicacao once — the visit stamp) + useMutation over solicitarRevisao (toast.success 'Solicitação enviada. A equipe responsável foi notificada.' / toast.error on failure). Keep the candidate-facing read free of any PII beyond the allowlist.
  </action>
  <verify>
    <automated>npm run test:run -- explicacaoService 2>&1 | tail -8 | grep -qiE "pass|✓" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- explicacaoService` GREEN (asserts allowlist string, no '*', no scores_candidato join)
    - `grep -c "select('\*')\|select(\"\*\")\|scores_candidato" src/features/explicacao/services/explicacaoService.ts` → 0
    - allowlist constant contains exactly the 5 named columns; reachability gate returns not-available unless decisao='rejeitado'
    - RPC invokes target solicitar_revisao_decisao + stamp_explicacao_acessada with 42501/403 handling
    - the explicacaoService test asserts solicitarRevisao fires the N8N webhook on RPC success (mock-fetch called once with the redacted body) AND that the mutation still resolves if the fetch rejects (non-blocking)
  </acceptance_criteria>
  <done>Candidate own-row data layer; explicacaoService Wave-0 test GREEN; no select('*'), no scores leak, reachability-gated.</done>
</task>

<task type="auto">
  <name>Task 2: ExplicacaoCandidatoPage + SolicitarRevisaoCTA + E2E reachability spec</name>
  <read_first>
    - src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx (:402-416 ScreenShell, :172-272 state machine, :104-110 useQuery mount, :365-394 alert-dialog)
    - e2e/ existing candidate-flow specs (the Playwright structure to mirror for explicacao-flow)
    - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-UI-SPEC.md (candidate copy + color contract)
  </read_first>
  <action>
    (a) src/features/explicacao/components/ExplicacaoCandidatoPage.tsx — full candidate page on the ProvaCognitivaScreen ScreenShell (BackgroundImage gradient + overlay 15 + max-w-2xl + py-20) using GlassPanel variant="white" blur="xl". State machine: loading / load-error (retry, 'Não foi possível carregar esta página.') / not-available ('Esta página não está disponível. Ela aparece somente quando há uma decisão de não seguir com a candidatura.' + 'Voltar ao painel') / content (heading 'Sobre a sua candidatura' + result line + reason eyebrow 'Por que esta decisão' + respectful reason body at text-base leading-relaxed + closing 'Agradecemos seu interesse...' + revision-right block + SolicitarRevisaoCTA + 'Voltar ao painel'). useQuery mount stamps explicacao_solicitada_em. If revisao_resultado present, render 'Resultado da revisão: {revisao_resultado}'. NEVER render a score/band/percentile.
    (b) src/features/explicacao/components/SolicitarRevisaoCTA.tsx — 'Solicitar revisão por pessoa natural' button (min-h-[44px]) gated by alert-dialog (title 'Solicitar revisão?', body + confirm/cancel copy). On confirm → useExplicacao.solicitarRevisao.mutate. Disabled + 'Você já solicitou a revisão desta decisão.' state with tooltip 'Solicitação registrada em {dd/mm/aaaa}' once revisao_solicitada_em is set.
    (c) e2e/explicacao-flow.spec.ts — Playwright scenarios mirroring existing candidate specs: (1) reachability gate — a candidatura WITHOUT a rejeitado decision shows the not-available state; (2) a rejeitado candidatura shows the explanation + the revision CTA; (3) requesting revision disables the CTA and shows the 'já solicitou' state. Use TEST_USER env vars (no committed credentials). Scenarios may be `.fixme`/list-only where they require a live rejected candidatura — list-parseable is sufficient (the live round-trip is HUMAN-UAT per 15-VALIDATION.md Manual-Only).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 | grep -qiE "built in|✓" && npx playwright test --list e2e/explicacao-flow.spec.ts 2>&1 | grep -qiE "explicacao|scenario|test" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npx playwright test --list e2e/explicacao-flow.spec.ts` parses ≥3 scenarios
    - `grep -v '^//' src/features/explicacao/components/ExplicacaoCandidatoPage.tsx | grep -cE "score|banda|percentil|/ 5|/ 25|teste psicológico|QI"` → 0 (no score/band/forbidden term reaches the candidate)
    - not-available state renders unless decisao='rejeitado'; CTA carries min-h-[44px]
    - tsc baseline ≤305 (`npm run lint`)
  </acceptance_criteria>
  <done>Candidate explanation page + revision CTA built on the glass shell; reachability-gated; E2E spec lists ≥3 scenarios; no score/band leak; build 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate browser → decisao_final read | own-row read scoped by RLS candidato_le_propria_decisao |
| candidate → solicitar_revisao_decisao / stamp_explicacao_acessada RPC | candidate-supplied candidatura_id crosses into own-row UPDATE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | Info Disclosure | candidate page leaks scores/band/raw justificativa | mitigate | own-row allowlist (5 columns), NEVER scores_candidato, templated non-clinical reason (Open Q5); acceptance grep asserts no score/band term reaches the component [[reference_select_star_leaks_pii]] |
| T-15-13 | Spoofing/Elevation | candidate reads/acts on another candidate's decision | mitigate | RLS candidato_le_propria_decisao on the read; own-row guard (candidatos.user_id=auth.uid()) in the RPCs (Plan 02); route RoleGuard role=candidato (Plan 06) |
| T-15-14 | Info Disclosure | page reachable before a rejection exists | mitigate | reachability gate (render content only when decisao='rejeitado'; not-available otherwise) — Pitfall 6 |
| T-15-15 | Repudiation | no record the explanation was provided | mitigate | stamp explicacao_solicitada_em on first visit (transparency evidence) |
| T-15-SC | Tampering | npm installs | mitigate | NO new packages — all primitives vendored |
</threat_model>

<verification>
- `npm run test:run -- explicacaoService` → GREEN (allowlist, no '*', no scores)
- `npm run build` → exit 0; `npx playwright test --list e2e/explicacao-flow.spec.ts` ≥3 scenarios
- no score/band/percentile/forbidden term on the candidate component; reachability-gated; min-h-[44px] CTAs
- tsc baseline ≤305
- Live round-trip (revision notification to RH) deferred to HUMAN-UAT per 15-VALIDATION.md
</verification>

<success_criteria>
- src/features/explicacao complete (service/hook/2 components); explicacaoService Wave-0 test GREEN
- Candidate sees respectful non-clinical reason + result, NEVER scores/bands; visit stamps explicacao_solicitada_em
- Revision CTA sets revisao_solicitada_em + notifies RH; idempotent once requested (DECISAO-04)
</success_criteria>

<output>
Create `.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-04-SUMMARY.md` when done
</output>
