---
phase: 15-decis-o-final-audit-vel-lgpd-art-20
plan: 06
type: execute
wave: 3
depends_on: [15-02, 15-03, 15-04, 15-05]
files_modified:
  - src/router/routes.tsx
  - database.types.ts
  - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-VALIDATION.md
autonomous: false
requirements: [DECISAO-01, DECISAO-02, DECISAO-03, DECISAO-04, LGPD-03]
must_haves:
  truths:
    - "Migration 20260625100001 is applied to PROD via Supabase MCP apply_migration (no BEGIN/COMMIT wrapper; 42601-safe) — the 4 RPCs (registrar_decisao, solicitar_revisao_decisao, stamp_explicacao_acessada, gerar_bias_snapshot) exist live"
    - "consolidar-decisao-final EF is deployed to PROD JWT-on (no --no-verify-jwt) and a no-auth curl returns 401"
    - "database.types.ts is regenerated so the client picks up the 4 new RPC signatures"
    - "The 3 routes are wired with the correct guards (/rh/candidato/:id/decisao RH+admin, /candidato/explicacao/:id candidato, /admin/bias-audit administrador)"
    - "Live SQL smokes confirm: por_usuario NOT NULL guardrail holds, justificativa <50 rejected by CHECK, aprovado/rejeitado fire the terminal transition + ONE audit row, em_espera makes no etapa change, bias snapshot writes banded aggregates only (no per-candidate PII)"
  artifacts:
    - path: "src/router/routes.tsx"
      provides: "3 new route blocks with RoleGuard"
      contains: "explicacao"
    - path: "database.types.ts"
      provides: "regenerated types with the 4 new RPC signatures"
      contains: "registrar_decisao"
  key_links:
    - from: "src/router/routes.tsx"
      to: "DecisaoFinalPage / ExplicacaoCandidatoPage / BiasAuditPage"
      via: "lazy/import + RoleGuard route blocks"
      pattern: "DecisaoFinalPage|ExplicacaoCandidatoPage|BiasAuditPage"
---

<objective>
[BLOCKING] PROD boundary for Phase 15. Apply the migration to PROD via Supabase MCP apply_migration, deploy the consolidar-decisao-final EF JWT-on, regenerate database.types.ts, wire the 3 routes with their guards, and run the live SQL smokes proving the guardrails (LGPD-02, terminal transition, bias no-PII). Build/types pass WITHOUT the apply (types from config) → this gate prevents a false-positive verification.

Purpose: The orchestrator's PROD boundary — migration + EF are AUTHORED-NOT-APPLIED until this plan. Routes own the shared src/router/routes.tsx (no parallel conflict). Run AFTER all authoring (Plans 02-05) is complete and BEFORE phase verification.
Output: live RPCs + deployed EF + regenerated types + wired routes + SQL-smoke evidence; nyquist_compliant verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-RESEARCH.md
@.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-PATTERNS.md
@.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Route wiring targets (VERIFIED line refs from routes.tsx + 15-PATTERNS.md §Route Wiring). -->

src/router/routes.tsx existing blocks to copy (each = import + a route object with RoleGuard):
  :271-275 path '/candidato/prova-cognitiva/:candidaturaId' → ProvaCognitivaScreen, RoleGuard role="candidato" → copy for '/candidato/explicacao/:id' → ExplicacaoCandidatoPage, role="candidato"
  :382-385 path '/rh/candidato/:id/entrevista' → EntrevistaWorkspace → copy for '/rh/candidato/:id/decisao' → DecisaoFinalPage, RoleGuard role={['rh','administrador']}
  :482-485 path '/admin/ai-costs' → AiCostsPage, role="administrador" → copy for '/admin/bias-audit' → BiasAuditPage, role="administrador"

Migration apply (CLAUDE.md / MEMORY): Supabase MCP apply_migration (execute_sql/apply_migration bypasses 42601, writes version row itself). project isljnozzlvckrgjjbjwp. NO BEGIN/COMMIT wrapper.
EF deploy (Phase 10/14 precedent): `supabase functions deploy consolidar-decisao-final` (JWT-on, no --no-verify-jwt). No-auth curl → 401 from the Auth gateway (verify_jwt ON behavioral evidence).
types regen: `npm run db:types` after apply.

SQL smokes (15-VALIDATION.md Wave 0 runbook + RESEARCH §Validation) — run via fixture + set_config request.jwt.claims, ROLLBACK-free cleanup:
  S1 LGPD-02: SELECT count(*) FROM decisao_final WHERE por_usuario IS NULL → 0
  S2 CHECK: INSERT/UPSERT via registrar_decisao with justificativa <50 → CHECK violation (or RPC RAISE)
  S3 terminal aprovado: registrar_decisao('aprovado') → candidaturas.etapa_atual='aprovado' + exactly 1 new historico_candidatura row
  S4 terminal rejeitado + em_espera: rejeitado → etapa_atual='rejeitado'; em_espera → etapa_atual UNCHANGED (still 'decisao_final'), no transition
  S5 candidate own-row: non-owner solicitar_revisao_decisao → 42501; owner on a rejeitado decision sets revisao_solicitada_em
  S6 bias snapshot: gerar_bias_snapshot writes ONE bias_audit_log row; dados has bands[] + limitacao + small_sample_warning and NO per-candidate identifiers
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the 3 routes with guards (src/router/routes.tsx)</name>
  <read_first>
    - src/router/routes.tsx (:271-275 candidate block, :382-385 RH entrevista block, :482-485 admin ai-costs block)
    - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-PATTERNS.md §Route Wiring
  </read_first>
  <action>
    Add 3 imports + 3 route blocks to src/router/routes.tsx, copying the existing namespaces verbatim:
    - import DecisaoFinalPage; route '/rh/candidato/:id/decisao' → DecisaoFinalPage wrapped in RoleGuard role={['rh','administrador']} (copy the entrevista block :382-385)
    - import ExplicacaoCandidatoPage; route '/candidato/explicacao/:id' → ExplicacaoCandidatoPage wrapped in RoleGuard role="candidato" (copy the prova-cognitiva block :271-275)
    - import BiasAuditPage; route '/admin/bias-audit' → BiasAuditPage wrapped in RoleGuard role="administrador" (copy the ai-costs block :482-485)
    Match the existing lazy/eager import style used by the neighboring blocks.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 | grep -qiE "built in|✓" && grep -q "explicacao" src/router/routes.tsx && grep -q "bias-audit" src/router/routes.tsx && grep -q "candidato/:id/decisao" src/router/routes.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with all 3 components imported
    - the 3 route paths present with the correct RoleGuard role each (RH+admin / candidato / administrador)
    - tsc baseline ≤305 (`npm run lint`)
  </acceptance_criteria>
  <done>3 routes wired with correct guards; build 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The Phase-15 backend is authored and ready for the PROD boundary: migration 20260625100001 (4 SECURITY DEFINER RPCs) + the consolidar-decisao-final EF. This task applies them to live PROD and regenerates types. Claude executes the apply via Supabase MCP + CLI; the human confirms the live evidence.
  </what-built>
  <how-to-verify>
    1. Apply the migration to PROD via Supabase MCP `apply_migration` (project isljnozzlvckrgjjbjwp) — name `decisao_final_phase15`, body = supabase/migrations/20260625100001_decisao_final_phase15.sql (no BEGIN/COMMIT wrapper; bypasses 42601). Confirm the 4 functions exist: `SELECT proname FROM pg_proc WHERE proname IN ('registrar_decisao','solicitar_revisao_decisao','stamp_explicacao_acessada','gerar_bias_snapshot')` → 4 rows.
    2. Deploy the EF JWT-on: `supabase functions deploy consolidar-decisao-final`. Confirm a no-auth `curl -X POST https://isljnozzlvckrgjjbjwp.supabase.co/functions/v1/consolidar-decisao-final` returns 401 (verify_jwt ON).
    3. (Types regen is executor-owned — see Task 3; do NOT rely on this checkpoint to refresh database.types.ts.)
    4. Run the SQL smokes S1-S6 (interfaces block) via fixture + `set_config request.jwt.claims`, ROLLBACK-free cleanup. Capture PASS for each:
       - S1 por_usuario IS NULL count → 0 (LGPD-02)
       - S2 justificativa <50 → CHECK/RAISE rejection
       - S3 aprovado → etapa_atual='aprovado' + exactly 1 new historico row (no double-write)
       - S4 rejeitado → 'rejeitado'; em_espera → etapa UNCHANGED
       - S5 non-owner revision RPC → 42501; owner on rejeitado sets revisao_solicitada_em
       - S6 gerar_bias_snapshot → 1 bias_audit_log row, dados banded-only (no per-candidate ids)
    5. If the pooler trips 42601 despite MCP, fall back to the D-22 SQL-Editor workaround + `migration repair --status applied`.
  </how-to-verify>
  <resume-signal>Type "approved" once the 4 RPCs exist live, the EF returns 401 no-auth, types regenerated, and S1-S6 all PASS — or describe which smoke failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-apply verification battery + nyquist close-out</name>
  <read_first>
    - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-VALIDATION.md (sign-off checklist)
  </read_first>
  <action>
    FIRST (executor-owned, after the [BLOCKING] apply approved): regenerate types via `npm run db:types`, then HARD-ASSERT `grep -q "registrar_decisao" database.types.ts` (fail Task 3 if the 4 new RPC signatures are absent — guards against a build passing on stale types; CLAUDE.md: database.types.ts is CLI-generated, NEVER hand-edited). Then: run the full Vitest suite + build + the forbidden-strings grep + the Deno EF test, and confirm the tsc baseline ≤305 (zero growth). Tick the 15-VALIDATION.md sign-off (all tasks have automated verify or Wave-0 dep; Wave 0 covered consolidation + EEOC + contract; feedback latency <20s) and confirm nyquist_compliant: true is set, wave_0_complete: true. Record the S1-S6 SQL-smoke PASS evidence + the EF 401 + the 4-RPC existence in the summary. Do NOT commit — the orchestrator commits.
  </action>
  <verify>
    <automated>grep -q "registrar_decisao" database.types.ts && npm run test:run 2>&1 | tail -5 | grep -qiE "pass|✓" && npm run build 2>&1 | tail -3 | grep -qiE "built in|✓" && npm run test:run -- forbidden-strings 2>&1 | tail -5 | grep -qiE "pass|✓" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run db:types` ran post-apply and `grep -q "registrar_decisao" database.types.ts` PASSES (4 RPC signatures live in types — not stale)
    - Full Vitest GREEN; `npm run build` exit 0; forbidden-strings GREEN; `deno test supabase/functions/consolidar-decisao-final/` GREEN
    - tsc baseline ≤305 (`npm run lint`)
    - 15-VALIDATION.md nyquist_compliant: true + sign-off checklist ticked; S1-S6 + EF-401 evidence recorded in the summary
  </acceptance_criteria>
  <done>Post-apply battery GREEN; nyquist verified; PROD evidence recorded; ready for phase verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → PROD database (apply_migration) | privileged DDL crosses into live PROD |
| executor → PROD EF deploy | new EF surface goes live (JWT-on) |
| client → 3 new routes | route RoleGuard is the first authorization layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-20 | Tampering | migration applied with a BEGIN/COMMIT wrapper trips 42601 / partial apply | mitigate | no-wrapper authoring (Plan 02 / D-22); MCP apply_migration; D-22 SQL-Editor fallback documented in the checkpoint |
| T-15-21 | Elevation | EF deployed with --no-verify-jwt (no auth gateway) | mitigate | deploy JWT-on; checkpoint confirms no-auth curl → 401 before approval |
| T-15-22 | Spoofing | a route missing its RoleGuard exposes a surface to the wrong persona | mitigate | Task 1 copies the guarded neighbor blocks; acceptance asserts the correct role per route |
| T-15-23 | Repudiation | the LGPD-02 / terminal-transition guardrails not proven live | mitigate | S1-S6 SQL smokes in the blocking checkpoint prove por_usuario NOT NULL, CHECK, terminal map, bias no-PII |
| T-15-SC | Tampering | npm/MCP installs | mitigate | NO new packages; MCP apply_migration is the sanctioned PROD path (Phase 6-14 precedent), not a package install |
</threat_model>

<verification>
- 4 RPCs exist live; consolidar-decisao-final EF returns 401 no-auth; database.types.ts has registrar_decisao
- 3 routes wired with correct guards; `npm run build` exit 0
- S1-S6 SQL smokes PASS (LGPD-02, CHECK, terminal transition single-audit-row, em_espera no-change, candidate own-row 42501, bias banded-only)
- Full Vitest + forbidden-strings + Deno EF test GREEN; tsc ≤305; nyquist_compliant true
</verification>

<success_criteria>
- Migration applied to PROD (4 RPCs live) + EF deployed JWT-on + types regenerated + 3 routes wired
- All 5 requirement IDs reachable end-to-end (RH dashboard/decision, candidate explanation, admin bias-audit)
- Live guardrails proven by SQL smokes; phase ready for /gsd:verify-work
</success_criteria>

<output>
Create `.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-06-SUMMARY.md` when done
</output>
