---
phase: 15-decis-o-final-audit-vel-lgpd-art-20
plan: 05
type: execute
wave: 2
depends_on: [15-02]
files_modified:
  - src/features/admin/bias-audit/biasMath.ts
  - src/features/admin/bias-audit/services/biasAuditService.ts
  - src/features/admin/bias-audit/hooks/useBiasAudit.ts
  - src/features/admin/bias-audit/components/BiasAuditPage.tsx
autonomous: true
requirements: [LGPD-03]
must_haves:
  truths:
    - "Per D-LOCK Área-4, biasMath is a pure deterministic module computing EEOC 4/5 (selection rate per age band, reference = highest-rate band, razao_4_5 = rate/reference, flag < 0.8, small_sample_warning) — flips the Plan-01 biasMath Wave-0 golden test GREEN"
    - "BiasAuditPage copies AiCostsPage verbatim in structure (RHLayout + GlassCard + table + CSV export), role-gated administrador, with an always-visible AGE-only limitation banner (race/gender not collected — LGPD-01) surfaced honestly"
    - "Per D-LOCK Área-4, 'Gerar snapshot' invokes gerar_bias_snapshot (admin RPC) writing ONE bias_audit_log row; the view reads the latest snapshot via an allowlist (NEVER select('*')) and renders bands with the <0.8 flag tint + reference-band micro-label"
    - "CSV export reuses the AiCostsPage blob-download idiom over the snapshot dados.bands[]"
  artifacts:
    - path: "src/features/admin/bias-audit/biasMath.ts"
      provides: "pure EEOC 4/5 + age-banding fns (computeAdverseImpact, bandFromAge)"
      exports: ["computeAdverseImpact", "bandFromAge"]
    - path: "src/features/admin/bias-audit/components/BiasAuditPage.tsx"
      provides: "admin bias-audit view (banner + table + snapshot + CSV)"
      min_lines: 40
  key_links:
    - from: "src/features/admin/bias-audit/services/biasAuditService.ts"
      to: "gerar_bias_snapshot RPC"
      via: "supabase.rpc"
      pattern: "gerar_bias_snapshot"
    - from: "src/features/admin/bias-audit/services/biasAuditService.ts"
      to: "bias_audit_log (allowlist read)"
      via: "explicit BIAS_AUDIT_COLUMNS"
      pattern: "id, snapshot_em, periodo, dados, criado_em"
---

<objective>
The admin bias-audit surface (LGPD-03): the `src/features/admin/bias-audit` feature — the pure EEOC 4/5 math module, the read+snapshot+CSV service, the hook, and the admin page modeled verbatim on AiCostsPage. AGE-only by necessity (race/gender not collected — LGPD-01), surfaced honestly. Route wiring is deferred to Plan 06.

Purpose: A defensible adverse-impact audit trail. The math (`biasMath`) is the TS mirror of the server-side gerar_bias_snapshot RPC (no-drift); the page presents the latest snapshot + CSV export. Flips the Plan-01 biasMath Wave-0 golden test GREEN.
Output: 4 files under src/features/admin/bias-audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-PATTERNS.md
@.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Analog files (VERIFIED from 15-PATTERNS.md) + the EEOC contract (from Plan-01 interfaces). -->

src/features/admin/ai-costs/services/aiCostsService.ts (87 lines):
  :17-42 BIAS_AUDIT analog — the columns constant + BiasAuditServiceError class shape
  :56-87 listAiCostDaily — allowlist read of the latest row → copy for listLatestSnapshot
  (RPC invoke for snapshot mirrors triagemService.reprocessarAnalise supabase.rpc shape)

src/features/admin/ai-costs/components/AiCostsPage.tsx:
  :130-178 RHLayout + header + loading(Skeleton in GlassCard)/error(retry)/empty branches → copy structurally
  :234-267 Table/TableHeader/TableBody block → copy for the age-band rows
  (CSV blob-download idiom is the cited template — build Blob + URL.createObjectURL from dados.bands[])

EEOC 4/5 contract (Plan-01 interfaces, CITED eeoc.gov):
  computeAdverseImpact(bands: {faixa,applicants,selected}[]) → per band {selection_rate, razao_4_5, flag} + reference_band + small_sample_warning
  selection_rate = selected/applicants; reference = highest rate; razao_4_5 = rate/reference_rate; flag = razao < 0.8; warning if any applicants < 30
  bandFromAge(age) → '18-24'|'25-34'|'35-44'|'45-54'|'55+'
  NOTE: biasMath.ts is the TS mirror of the server-side gerar_bias_snapshot SQL (Plan 02) — keep the formula identical (no-drift, the cognitivo bandaFromTotal TS/SQL precedent).

bias_audit_log (LIVE): id, snapshot_em, periodo text, dados jsonb (bands[]/metodo/limitacao/faixa_referencia/n_total/small_sample_warning per RESEARCH §EEOC 4/5), criado_em
gerar_bias_snapshot(p_periodo text) RPC (Plan 02) — admin-only, returns the inserted row.

UI-SPEC copy (15-UI-SPEC §Admin bias-audit) — verbatim pt-BR: heading 'Auditoria de viés', the always-visible limitation banner ('Esta auditoria considera apenas faixa etária. Raça e gênero não são coletados...'), section heading 'Selection rate por faixa etária (regra 4/5 EEOC)', table columns (Faixa etária / Selection rate / Razão 4/5), 'referência (maior taxa)' micro-label, 4/5 flag tooltip, 'Gerar snapshot' + 'Exportar CSV' (min-h-[44px]), success toasts, empty + period states.
Color: <0.8 flagged row destructive tint (bg-red-500/15 text-red-300 border-red-400/30); reference band neutral + 'referência' micro-label; bands ≥0.8 neutral. Route-gated RoleGuard role="administrador".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: biasMath pure module + biasAuditService + useBiasAudit (math + data layer)</name>
  <read_first>
    - src/features/admin/bias-audit/biasMath.test.ts (the Wave-0 RED golden test to flip GREEN — implement to its assertions)
    - src/features/admin/ai-costs/services/aiCostsService.ts (:17-42 columns+error class, :56-87 latest-row read; RPC invoke shape)
    - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-RESEARCH.md §EEOC 4/5 + 15-PATTERNS.md §biasMath / §biasAuditService
  </read_first>
  <behavior>
    - computeAdverseImpact: selection_rate=selected/applicants; reference_band=highest rate; razao_4_5=rate/reference_rate (reference=1.0); flag=razao<0.8; small_sample_warning=true if any band applicants<30
    - bandFromAge boundaries: 24→'18-24', 25→'25-34', 44→'35-44', 45→'45-54', 55→'55+'
    - null/invalid age surfaced as an excluded count, never silently dropped (Pitfall 4)
    - cited worked example: reference rate 0.50, other band 0.35 → razao 0.70 → flag true
  </behavior>
  <action>
    (a) src/features/admin/bias-audit/biasMath.ts — pure deterministic fns `computeAdverseImpact(bands)` + `bandFromAge(age)` + types, implementing the EEOC 4/5 formula per <behavior>. This is the TS mirror of the server-side gerar_bias_snapshot SQL (no-drift). No DB/network imports — pure.
    (b) src/features/admin/bias-audit/services/biasAuditService.ts (analog aiCostsService.ts) — `BIAS_AUDIT_COLUMNS = 'id, snapshot_em, periodo, dados, criado_em'` (NEVER '*'); `BiasAuditServiceError` class; `listLatestSnapshot()` — allowlist read of the latest bias_audit_log row (copy listAiCostDaily :56-87); `gerarSnapshot(periodo)` — supabase.rpc('gerar_bias_snapshot', { p_periodo }) (mirror reprocessarAnalise); `exportCsv(dados)` — build a CSV Blob + URL.createObjectURL download from dados.bands[] (the AiCostsPage blob idiom) with columns faixa/applicants/selected/selection_rate/razao_4_5/flag.
    (c) src/features/admin/bias-audit/hooks/useBiasAudit.ts — useQuery over listLatestSnapshot + useMutation over gerarSnapshot (toast.success 'Snapshot registrado em bias_audit_log.' + invalidate; toast.error on failure).
  </action>
  <verify>
    <automated>npm run test:run -- biasMath 2>&1 | tail -8 | grep -qiE "pass|✓" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- biasMath` GREEN (cited 0.70 example flags; bandFromAge boundaries; small_sample_warning; null-age exclusion)
    - `grep -c "select('\*')\|select(\"\*\")" src/features/admin/bias-audit/services/biasAuditService.ts` → 0
    - biasMath.ts has NO supabase/network import (pure)
    - gerarSnapshot targets `gerar_bias_snapshot`; BIAS_AUDIT_COLUMNS lists the 5 named columns
  </acceptance_criteria>
  <done>Pure EEOC 4/5 math + bias-audit data layer; biasMath Wave-0 golden test GREEN; no select('*'); TS mirrors the SQL RPC.</done>
</task>

<task type="auto">
  <name>Task 2: BiasAuditPage (admin view — banner + table + snapshot + CSV)</name>
  <read_first>
    - src/features/admin/ai-costs/components/AiCostsPage.tsx (:130-178 shell+states, :234-267 Table block, CSV download idiom)
    - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-UI-SPEC.md (admin copy + color contract)
  </read_first>
  <action>
    src/features/admin/bias-audit/components/BiasAuditPage.tsx — copy AiCostsPage verbatim in structure: RHLayout wrapper + header 'Auditoria de viés' + the ALWAYS-VISIBLE AGE-only limitation banner (verbatim UI-SPEC copy — honest, never hidden) + a GlassCard containing the selection-rate Table (columns Faixa etária / Selection rate / Razão 4/5) over the latest snapshot's dados.bands[]. Rows with razao_4_5 < 0.8 get the destructive tint (bg-red-500/15 text-red-300 border-red-400/30) + tooltip 'Razão de selection-rate < 0,8 vs a faixa de maior taxa — regra 4/5 EEOC (possível impacto adverso).'; the reference band (highest rate) renders neutral + a 'referência (maior taxa)' micro-label. 'Gerar snapshot' button (useBiasAudit.gerarSnapshot) + 'Exportar CSV' button (biasAuditService.exportCsv) — both min-h-[44px]. Loading (Skeleton)/error(retry)/empty ('Nenhum snapshot ainda' + body) states. Period label 'Período: {periodo} · gerado em {dd/mm/aaaa}'. No charts library (table suffices V1). Route gate RoleGuard role="administrador" is wired in Plan 06.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 | grep -qiE "built in|✓" && grep -q "apenas faixa etária" src/features/admin/bias-audit/components/BiasAuditPage.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - the AGE-only limitation banner copy ('apenas faixa etária' / race-gender-not-collected) is present and rendered unconditionally
    - the <0.8 flag tint + reference-band micro-label + 4/5 tooltip are present
    - 'Gerar snapshot' + 'Exportar CSV' buttons wired (min-h-[44px]); no charts library imported
    - `grep -v '^//' src/features/admin/bias-audit/components/BiasAuditPage.tsx | grep -cE "teste psicológico|QI"` → 0 (LGPD-04)
    - tsc baseline ≤305 (`npm run lint`)
  </acceptance_criteria>
  <done>Admin bias-audit view built (banner + table + snapshot + CSV) copying AiCostsPage; honest AGE-only limitation; flag tint + reference label; build 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → gerar_bias_snapshot RPC | admin trigger crosses into server-side age derivation |
| admin browser → bias_audit_log read | admin reads banded aggregates only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-16 | Info Disclosure | bias-audit re-identifies candidates via small bands or per-candidate rows | mitigate | snapshot persists banded aggregates only (Plan 02 RPC); small_sample_warning surfaced; biasAuditService reads allowlist columns only |
| T-15-17 | Elevation | non-admin generates/views snapshot | mitigate | gerar_bias_snapshot guards administrador (Plan 02); route RoleGuard role=administrador (Plan 06) — layered |
| T-15-18 | Compliance | the AGE-only limitation is hidden, implying a fuller audit than exists | mitigate | always-visible limitation banner (honest); acceptance grep asserts the banner copy is present |
| T-15-19 | Tampering | biasMath TS drifts from the SQL RPC, showing different ratios than persisted | mitigate | biasMath mirrors gerar_bias_snapshot formula 1:1 (no-drift precedent); Wave-0 golden test pins both to the cited EEOC example |
| T-15-SC | Tampering | npm installs | mitigate | NO new packages — no recharts needed (table suffices); all primitives vendored |
</threat_model>

<verification>
- `npm run test:run -- biasMath` → GREEN
- `npm run build` → exit 0; limitation banner present; <0.8 tint + reference label + tooltip present
- no select('*') in biasAuditService; no charts library; no forbidden term
- tsc baseline ≤305
- Live snapshot over a real population deferred to HUMAN-UAT per 15-VALIDATION.md
</verification>

<success_criteria>
- src/features/admin/bias-audit complete (biasMath/service/hook/page); biasMath Wave-0 golden test GREEN
- EEOC 4/5 over age bands with <0.8 flag + reference band; honest AGE-only limitation banner (LGPD-03)
- Snapshot via admin RPC writes banded aggregates only; CSV export reuses the AiCostsPage idiom
</success_criteria>

<output>
Create `.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-05-SUMMARY.md` when done
</output>
