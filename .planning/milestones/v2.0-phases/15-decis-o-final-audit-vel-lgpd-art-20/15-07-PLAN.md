---
phase: 15-decis-o-final-audit-vel-lgpd-art-20
plan: 07
type: execute
wave: 4
gap_closure: true
depends_on: ["15-02", "15-03", "15-05", "15-06"]
files_modified:
  - src/features/admin/bias-audit/components/BiasAuditPage.tsx
  - src/features/admin/bias-audit/biasMath.ts
  - src/features/admin/bias-audit/__tests__/biasMath.test.ts
  - src/features/decisao/services/decisaoService.ts
  - src/features/explicacao/services/explicacaoService.ts
  - src/features/admin/bias-audit/services/biasAuditService.ts
  - supabase/migrations/20260625100002_decisao_final_rh_vaga_scope.sql
autonomous: true
requirements: [DECISAO-03, DECISAO-04, LGPD-03]
must_haves:
  truths:
    - "WR-01: the bias-audit excluded-no-birthdate footnote renders — BiasAuditPage reads the key the SQL actually writes (`excluidos_sem_data`), and biasMath's type uses the same key (no excluded_sem_data/excluidos_sem_data mismatch)."
    - "WR-03: rh_le_decisao_final is vaga-ownership scoped — a role='rh' user reads decisao_final ONLY for candidaturas on vagas they own (vagas.created_by=auth.uid()); administrador bypasses; candidato own-row policy unchanged. Closes the RH horizontal-access gap (non-owning RH can no longer read peers' decisions/justificativa)."
    - "WR-02: biasMath.ts (the test oracle — it has ZERO production callers; the SQL RPC is the live scorer) is aligned to the SQL truth: field `excluidos_sem_data`, n_total = Σ applicants only, reference tie-break = highest rate then faixa ASC; the golden test asserts the SQL-matching values."
    - "WR-05: solicitar_revisao's no_data_found (no rejected decision) maps to a clear pt-BR message distinct from a generic network error."
  artifacts:
    - path: "supabase/migrations/20260625100002_decisao_final_rh_vaga_scope.sql"
      provides: "DROP+CREATE rh_le_decisao_final vaga-scoped (admin bypass), candidate policy untouched"
  key_links:
    - from: "supabase/migrations/20260625100002_decisao_final_rh_vaga_scope.sql"
      to: "rh_le_decisao_final"
      via: "DROP POLICY IF EXISTS + CREATE POLICY with created_by scope"
      pattern: "created_by"
---

<objective>
Close the Phase-15 code-review warnings WR-01, WR-02, WR-03, WR-04, WR-05. Author the WR-03 RLS migration as a FILE only (the orchestrator applies to PROD). Fix the client warnings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-REVIEW.md
@.planning/PROJECT.md

<interfaces>
Read 15-REVIEW.md WR-01..WR-05 for exact file:line + fix guidance. The live RLS target: `rh_le_decisao_final` is currently `(auth.jwt()#>>'{app_metadata,role}') = ANY(ARRAY['rh','administrador'])` — role-only, no created_by scope. Mirror the Phase-14 WR-04 fix shape from `supabase/migrations/20260625000001_phase14_gap_closure.sql` (rh_le_scores / rh_le_entrevista_analises: administrador bypass OR rh AND candidatura_id IN (SELECT c.id FROM candidaturas c JOIN vagas v ON v.id=c.vaga_id WHERE v.created_by=auth.uid())). DROP POLICY IF EXISTS rh_le_decisao_final then CREATE the scoped one. Do NOT touch candidato_le_propria_decisao or decisao_final_no_client_insert.

PROD BOUNDARY: author `supabase/migrations/20260625100002_decisao_final_rh_vaga_scope.sql` as a FILE only (no BEGIN/COMMIT wrapper). Do NOT run apply_migration/db push/MCP/db:types — the orchestrator applies it to PROD with the user's authorization.

biasMath.ts is the TEST ORACLE only (grep confirms zero non-test callers; the live scorer is the gerar_bias_snapshot SQL RPC). Align it to the SQL so the golden test actually verifies the SQL contract.

Commit via `git -c core.hooksPath=/dev/null`. Do NOT touch the 3 pre-existing not-yours paths (LoginRHPage.tsx, 11-HUMAN-UAT.md, .planning/ui-reviews/). Stage only your own files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-01/WR-02 bias-audit field + oracle alignment, WR-04 stale JSDoc, WR-05 revision toast</name>
  <files>src/features/admin/bias-audit/components/BiasAuditPage.tsx, src/features/admin/bias-audit/biasMath.ts, src/features/admin/bias-audit/__tests__/biasMath.test.ts, src/features/decisao/services/decisaoService.ts, src/features/explicacao/services/explicacaoService.ts, src/features/admin/bias-audit/services/biasAuditService.ts</files>
  <read_first>
    - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-REVIEW.md (WR-01,02,04,05 exact lines)
    - supabase/migrations/20260625100001_decisao_final_phase15.sql (the gerar_bias_snapshot dados jsonb keys — source of truth: excluidos_sem_data, n_total=Σ applicants, ORDER BY rate DESC faixa ASC)
    - src/features/admin/bias-audit/biasMath.ts + its __tests__
  </read_first>
  <action>
  - WR-01: in BiasAuditPage.tsx read `snapshot.dados.excluidos_sem_data` (the key the SQL writes), not `excluded_sem_data`. Rename the TS type field in biasMath.ts (BandResult/AdverseImpactResult) `excluded_sem_data` → `excluidos_sem_data` so the type matches the persisted jsonb.
  - WR-02: align biasMath.ts to the SQL truth (it is the test oracle, zero prod callers): (a) field rename above; (b) `n_total` = Σ applicants ONLY (exclude the excluded count, matching SQL v_n_total); (c) reference-band tie-break = highest selection_rate then faixa ASC (matching SQL `ORDER BY rate DESC, faixa ASC`), replacing the strict-`>` first-max scan. Update biasMath.test.ts expectations to the SQL-matching values. Add a header note that the SQL RPC is the production scorer and this module is the parity oracle.
  - WR-04: update the stale JSDoc in decisaoService.ts / explicacaoService.ts / biasAuditService.ts that still says `as never` / "AUTHORED-NOT-APPLIED" — the RPCs are live + typed now (15-06). Make the comments reflect reality.
  - WR-05: in explicacaoService solicitarRevisao, map the RPC `no_data_found` (no rejected decision exists) to a distinct clear pt-BR outcome/message (e.g. "Não há decisão rejeitada para revisar."), not the generic NETWORK_ERROR/"tente novamente".
  </action>
  <verify>
    <automated>npm run test:run -- biasMath explicacao 2>&1 | tail -6 && npm run build 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "excluded_sem_data" src/features/admin/bias-audit/biasMath.ts src/features/admin/bias-audit/components/BiasAuditPage.tsx` returns 0 (renamed everywhere)
    - `grep -c "excluidos_sem_data" src/features/admin/bias-audit/components/BiasAuditPage.tsx` ≥1
    - `npm run test:run -- biasMath` GREEN with the SQL-matching expectations (n_total = Σ applicants; tie-break faixa ASC)
    - the 3 services' RPC-call JSDoc no longer claims `as never`/AUTHORED-NOT-APPLIED
    - `npm run build` exit 0; tsc baseline ≤305
  </acceptance_criteria>
  <done>WR-01/02/04/05 closed in client code; biasMath oracle matches the SQL contract.</done>
</task>

<task type="auto">
  <name>Task 2: WR-03 — author the rh_le_decisao_final vaga-scope migration (FILE only)</name>
  <files>supabase/migrations/20260625100002_decisao_final_rh_vaga_scope.sql</files>
  <read_first>
    - supabase/migrations/20260625000001_phase14_gap_closure.sql (the rh_le_scores / rh_le_entrevista_analises vaga-scope policy shape to mirror)
    - 15-REVIEW.md WR-03
  </read_first>
  <action>Author `supabase/migrations/20260625100002_decisao_final_rh_vaga_scope.sql` (no BEGIN/COMMIT wrapper). `DROP POLICY IF EXISTS rh_le_decisao_final ON public.decisao_final;` then `CREATE POLICY rh_le_decisao_final ON public.decisao_final FOR SELECT USING ( (select auth.jwt() #>> '{app_metadata,role}') = 'administrador' OR ( (select auth.jwt() #>> '{app_metadata,role}') = 'rh' AND candidatura_id IN (SELECT c.id FROM public.candidaturas c JOIN public.vagas v ON v.id=c.vaga_id WHERE v.created_by = (select auth.uid())) ) );` Do NOT touch candidato_le_propria_decisao or decisao_final_no_client_insert. Add a header comment citing WR-03 + the Phase-14 WR-04 precedent. Do NOT apply.</action>
  <verify>
    <automated>grep -cE "DROP POLICY IF EXISTS rh_le_decisao_final|created_by = \\(select auth.uid\\(\\)\\)" supabase/migrations/20260625100002_decisao_final_rh_vaga_scope.sql</automated>
  </verify>
  <acceptance_criteria>
    - the migration DROPs + CREATEs rh_le_decisao_final with administrador-bypass OR rh-owns-vaga scope
    - candidato_le_propria_decisao + decisao_final_no_client_insert are NOT referenced
    - NO apply/push/MCP run (file authored only)
  </acceptance_criteria>
  <done>The WR-03 vaga-scope migration is authored as a file; the orchestrator applies it to PROD.</done>
</task>

</tasks>

<verification>
- `npm run build` exit 0; tsc ≤305; biasMath + explicacao tests GREEN
- migration authored as a file only (orchestrator applies + verifies the policy is vaga-scoped live)
- SUMMARY notes: migration 20260625100002 UNAPPLIED (orchestrator applies)
- commit via `git -c core.hooksPath=/dev/null`; own files only
</verification>

<success_criteria>
- Phase-15 review warnings WR-01..WR-05 addressed; the RH horizontal-access gap closed once the migration applies; the bias footnote renders; the oracle matches the SQL.
</success_criteria>

<output>
Create `.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-07-SUMMARY.md` when done
</output>
