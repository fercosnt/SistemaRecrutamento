---
phase: 15-decis-o-final-audit-vel-lgpd-art-20
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/functions/consolidar-decisao-final/__tests__/index.test.ts
  - src/features/admin/bias-audit/biasMath.test.ts
  - src/features/decisao/schemas/__tests__/consolidacaoContract.test.ts
  - src/__tests__/guards/forbidden-strings.grep.test.ts
  - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-VALIDATION.md
autonomous: true
requirements: [DECISAO-01, DECISAO-03, LGPD-03]
must_haves:
  truths:
    - "The consolidation aggregation has a calibrated RED golden test (weight→score mapping, 0-100 normalization, weight renormalization over present etapas, pendente/missing → N/A) that fails until the EF lands (DECISAO-01)"
    - "The EEOC 4/5 adverse-impact ratio has a calibrated RED golden test (selection rate, reference band, <0.8 flag, small-N warning) that fails until biasMath lands (LGPD-03)"
    - "A client↔EF contract test asserts the consolidation request body parses against the EF .strict() schema — closing the integration-contract-gap drift"
    - "forbidden-strings.grep reaches the 3 new feature dirs (auto via recursive SCAN_ROOTS) and a sanity-count assertion locks that coverage (LGPD-04)"
  artifacts:
    - path: "supabase/functions/consolidar-decisao-final/__tests__/index.test.ts"
      provides: "RED Deno golden test for consolidation aggregation + authorize"
      min_lines: 40
    - path: "src/features/admin/bias-audit/biasMath.test.ts"
      provides: "RED Vitest golden test for EEOC 4/5 + age banding"
      min_lines: 30
    - path: "src/features/decisao/schemas/__tests__/consolidacaoContract.test.ts"
      provides: "RED contract test client body ↔ EF .strict() schema"
      min_lines: 20
  key_links:
    - from: "supabase/functions/consolidar-decisao-final/__tests__/index.test.ts"
      to: "supabase/functions/consolidar-decisao-final/index.ts"
      via: "import of handler (module-not-found until Wave 1)"
      pattern: "consolidar-decisao-final/index"
    - from: "src/features/admin/bias-audit/biasMath.test.ts"
      to: "src/features/admin/bias-audit/biasMath.ts"
      via: "import of computeAdverseImpact / bandFromAge (module-not-found until Wave 2)"
      pattern: "biasMath"
---

<objective>
Wave 0 RED battery for Phase 15. Author the calibrated failing golden tests for the two genuinely-new deterministic pieces (the consolidation aggregation + the EEOC 4/5 age-band math), the client↔EF contract test (closes the [[feedback_integration_contract_gap]] drift), and lock the LGPD-04 forbidden-language coverage over the 3 new feature dirs. Tests COMPILE under TS/Deno but FAIL at runtime against not-yet-existing modules — the failure IS the assertion that the behavior is detectable (smoke-runtime gate, Phase-4 lesson).

Purpose: Calibrate every downstream Phase-15 surface against a failing test BEFORE its implementation lands. The consolidation normalization and the 4/5 ratio are deterministic → unit-testable golden tests per 15-VALIDATION.md Wave 0 requirements.
Output: 3 RED test files + a forbidden-strings sanity-count extension + nyquist_compliant flipped true in 15-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-RESEARCH.md
@.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-VALIDATION.md

<interfaces>
<!-- Contracts the Wave-0 tests assert against. Downstream waves implement these. -->

Consolidation EF input (from RESEARCH §Consolidation Aggregation; .strict() Zod body in Wave 1):
  { candidatura_id: string (uuid), vaga_id: string (uuid) }

Consolidation EF output JSON (the shape the golden test asserts):
  {
    consolidated: number | null,   // weighted aggregate over PRESENT etapas; null if none present
    breakdown: Array<{
      etapa: 'triagem'|'work_sample_sjt'|'redacao_cultural'|'entrevista'|'big_five'|'cognitivo',
      normalized: number | null,   // 0..100; null when N/A
      weight: number | null,       // raw weight from pesos_avaliacao; null for context-only big_five/cognitivo
      effective_weight: number | null, // renormalized over present; null when N/A or context-only
      status: 'present' | 'na' | 'context',
    }>,
    recommendation: string,         // deterministic templated; NO LLM
  }

Weight-key → score-source map (RESEARCH §Consolidation Aggregation, VERIFIED):
  - 'triagem'         ← analise_candidato_vaga.score_match (0..100 already)
  - 'work_sample_sjt' ← scores_candidato tipo='sjt'     (score/score_max*100)
  - 'redacao_cultural'← scores_candidato tipo='redacao'  (score/25*100)
  - 'entrevista'      ← scores_candidato tipo='entrevista' (N/A unless status='sucesso' — Open Q1 rec)
  - big_five/cognitivo ← scores_candidato (CONTEXT-ONLY, never weighted)
  PRESENT only when status='sucesso'; else N/A. effective_weight[k] = weight[k] / Σ(weight[present]).

EEOC 4/5 (RESEARCH §EEOC 4/5, CITED eeoc.gov):
  selection_rate(band)  = selected(band) / applicants(band)
  ratio(band)           = selection_rate(band) / selection_rate(reference_band)  // reference = highest rate
  FLAG when ratio < 0.80 ; small_sample_warning when any band applicants < 30
  age bands (Claude's discretion, documented): 18-24 / 25-34 / 35-44 / 45-54 / 55+
  selected := decisao='aprovado' ; applicants := has a decisao_final row (Open Q4 rec)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED golden test — consolidation aggregation + authorize (Deno)</name>
  <read_first>
    - supabase/functions/comparativo-candidatos/__tests__/index.test.ts (the injected-deps Deno handler-test analog — copy the mock-injection shape, no network)
    - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-RESEARCH.md §Consolidation Aggregation (steps 1-7) + §Validation (Wave 0 gaps)
  </read_first>
  <action>
    Create supabase/functions/consolidar-decisao-final/__tests__/index.test.ts as a calibrated RED Deno test importing `handler` from `../index.ts` (module-not-found until Wave 1 — that IS the RED). Mirror the comparativo-candidatos test's injected-deps shape: build `Deps` with mocked `supabaseAdmin`/`supabaseUser` (stubbed `.from().select().eq().maybeSingle()` chains + `auth.getUser`). Cover, per RESEARCH §Consolidation Aggregation: (a) heterogeneous-scale normalization — triagem score_match=80 → 80; sjt score=18/score_max=25 → 72; redacao score=20/25 → 80; entrevista status='pendente_humano' → N/A (Open Q1: weight ONLY status='sucesso'); (b) weight renormalization over PRESENT etapas (missing etapa → effective_weight redistributed, Σ effective_weight of present = 1.0); (c) big_five/cognitivo emitted as status='context' rows with weight=null, contributing 0; (d) NEVER re-scores (assert no evaluation-EF invoke in the handler path); (e) authorize: non-RH role → 403, role='rh' non-owner of vaga.created_by → 403, administrador bypasses. Use deno.land/std assert imports per the comparativo test. Header comment cites this plan + the RED contract (compiles, fails at runtime against the absent module).
  </action>
  <verify>
    <automated>cd supabase/functions && deno test consolidar-decisao-final/ 2>&1 | grep -qiE "module not found|no such file|cannot find" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "status: 'present'\|status: \"present\"\|present" supabase/functions/consolidar-decisao-final/__tests__/index.test.ts` ≥ 1
    - `deno test consolidar-decisao-final/` fails with module-not-found (not a syntax error) → calibrated RED
    - Test references all 6 etapa keys (triagem/work_sample_sjt/redacao_cultural/entrevista/big_five/cognitivo) and the 403 authorize cases
    - No network call in the test (injected deps only)
  </acceptance_criteria>
  <done>RED Deno golden test for DECISAO-01 aggregation + authorize exists and fails via module-not-found.</done>
</task>

<task type="auto">
  <name>Task 2: RED golden test — EEOC 4/5 + age banding (Vitest)</name>
  <read_first>
    - src/features/avaliacao-cognitiva (the deterministic-pure-fn + TS/SQL no-drift precedent `bandaFromTotal`, cited 20260625000001:138 in 15-PATTERNS.md)
    - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-RESEARCH.md §EEOC 4/5 Adverse-Impact Computation
  </read_first>
  <action>
    Create src/features/admin/bias-audit/biasMath.test.ts as a calibrated RED Vitest test importing the pure fns from `./biasMath` (module-not-found until Wave 2 — RED). Assert the EEOC formula per RESEARCH §EEOC 4/5: `computeAdverseImpact(bands)` where each band = {faixa, applicants, selected}; expect selection_rate = selected/applicants per band, reference_band = highest selection rate, razao_4_5 = rate/reference_rate, flag = razao_4_5 < 0.8. Use the cited worked example (band A rate 0.50 reference, band B rate 0.35 → ratio 0.70 → flag true). Assert `bandFromAge(age)` buckets into 18-24 / 25-34 / 35-44 / 45-54 / 55+ (boundary cases 24→'18-24', 25→'25-34', 55→'55+'). Assert `small_sample_warning` true when any band applicants < 30. Assert null/invalid birthdate handling is surfaced (an excluded-count is reported, candidates not silently dropped — Pitfall 4). Header comment cites this plan + the RED contract.
  </action>
  <verify>
    <automated>npm run test:run -- biasMath 2>&1 | grep -qiE "cannot find module|failed to resolve|Failed to load" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "razao_4_5\|0.8\|reference" src/features/admin/bias-audit/biasMath.test.ts` ≥ 2
    - `npm run test:run -- biasMath` fails via module-resolution error → calibrated RED
    - Test asserts the cited 0.70 worked example flags, and `bandFromAge` boundary cases
    - Test asserts small_sample_warning and null-birthdate exclusion accounting
  </acceptance_criteria>
  <done>RED Vitest golden test for LGPD-03 EEOC 4/5 + age banding exists and fails via module-not-found.</done>
</task>

<task type="auto">
  <name>Task 3: RED contract test (client↔EF body) + forbidden-strings coverage sanity-count</name>
  <read_first>
    - src/__tests__/guards/forbidden-strings.grep.test.ts (SCAN_ROOTS recursive over 'src'/'supabase/functions'/'supabase/migrations' — new dirs auto-covered; add a count assertion only)
    - .planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-VALIDATION.md §Wave 0 (contract test requirement)
  </read_first>
  <action>
    (a) Create src/features/decisao/schemas/__tests__/consolidacaoContract.test.ts as a calibrated RED Vitest test importing the shared request schema from `../consolidacaoSchema` (module-not-found until Wave 2 — RED). Assert a well-formed client body `{ candidatura_id: <uuid>, vaga_id: <uuid> }` parses against the schema's `.parse(...)`, and a body with an extra/unknown key is REJECTED (`.strict()` contract — this is the exact gap that broke Phase 11 SJT, [[feedback_integration_contract_gap]]). The Wave-1 EF and Wave-2 client BOTH import this same schema so the contract cannot drift.
    (b) Extend src/__tests__/guards/forbidden-strings.grep.test.ts: since SCAN_ROOTS already recurses 'src' + 'supabase/functions', the 3 new feature dirs are auto-covered the moment they exist — do NOT edit SCAN_ROOTS. Add ONE sanity-count `it(...)` asserting the scan reaches files under `src/features/decisao` OR `src/features/explicacao` OR `src/features/admin/bias-audit` once they exist (mirror the existing "scan actually reaches files under supabase/functions/" assertion at ~:128), using a tolerant guard so it passes pre-implementation (skip-if-absent) and locks coverage post-implementation. The forbidden-term assertion itself must remain GREEN (no forbidden term authored).
  </action>
  <verify>
    <automated>npm run test:run -- consolidacaoContract 2>&1 | grep -qiE "cannot find module|failed to resolve" && npm run test:run -- forbidden-strings 2>&1 | tail -5 | grep -qiE "pass|✓" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - consolidacaoContract.test.ts exists and fails via module-resolution (RED); asserts both accept-valid + reject-unknown-key
    - forbidden-strings.grep.test.ts has a new sanity-count `it` referencing at least one of the 3 new feature dirs; the forbidden-term assertion stays GREEN
    - `grep -n "SCAN_ROOTS" src/__tests__/guards/forbidden-strings.grep.test.ts` shows SCAN_ROOTS UNCHANGED (still 3 roots)
  </acceptance_criteria>
  <done>Contract test (RED) + forbidden-strings sanity-count (GREEN) authored; SCAN_ROOTS untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → modules under test | Wave-0 tests only import; no PROD write, no live network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | forbidden-strings SCAN_ROOTS edit could silently narrow LGPD-04 coverage | mitigate | Task 3 explicitly forbids editing SCAN_ROOTS; acceptance grep asserts it stays at 3 roots |
| T-15-02 | Repudiation | a non-calibrated test (passes against absent code) gives false confidence | mitigate | Each RED test must fail via module-not-found, not pass — verify block asserts the RED |
| T-15-SC | Tampering | npm/deno installs | mitigate | This plan installs NO packages; all imports are vendored test libs (vitest/deno std) — vacuously satisfied per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `deno test supabase/functions/consolidar-decisao-final/` → RED (module-not-found)
- `npm run test:run -- biasMath` → RED (module-resolution)
- `npm run test:run -- consolidacaoContract` → RED (module-resolution)
- `npm run test:run -- forbidden-strings` → GREEN (no forbidden term; sanity-count tolerant pre-implementation)
- `npm run lint` tsc baseline ≤305 (zero growth — test files only)
</verification>

<success_criteria>
- 3 RED golden/contract tests authored, each failing via module-not-found (calibrated, not syntax errors)
- forbidden-strings coverage of the 3 new feature dirs locked via sanity-count; SCAN_ROOTS unchanged
- 15-VALIDATION.md: nyquist_compliant flipped true, wave_0_complete true, Wave 0 checkboxes ticked
</success_criteria>

<output>
Create `.planning/phases/15-decis-o-final-audit-vel-lgpd-art-20/15-01-SUMMARY.md` when done
</output>
