---
phase: 16-compliance-a11y-hardening
plan: 03
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - src/features/entrevista/components/EntrevistaWorkspace.tsx
  - src/features/entrevista/components/EntrevistaDashboard.tsx
  - src/features/entrevista/components/EntrevistaScorecardInline.tsx
  - src/features/entrevista/components/GuiaEntrevistaPanel.tsx
  - src/features/triagem/components/RedacaoReviewPanel.tsx
  - src/features/avaliacao/components/SjtMultiplaEscolhaScreen.tsx
  - src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx
  - src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx
autonomous: true
requirements: [LGPD-05]
user_setup: []

must_haves:
  truths:
    - "R6 EntrevistaWorkspace tabs use the Radix Tabs pattern (not custom aria-pressed buttons) — AB-3"
    - "Candidate option/Likert groups (C3 SjtMultiplaEscolha, C5 BigFive) expose radiogroup name/role/state with arrow-key roving focus — AB-2/AB-3/AB-5 (FX-06)"
    - "C8 ProvaCognitivaScreen submit hint uses Radix Tooltip (keyboard/SR reachable), not native title= — AB-5/AB-7 (FX-09)"
    - "Interview/redacao/weight sliders expose aria-valuetext announcing the human-readable value — AB-2/AB-8 (FX-11)"
    - "Entrevista amber 24h pill + text-white/50-60 eyebrows clear AA at composite; dead Agendar CTA is disabled+named; autosave copy no longer over-promises — FX-07/08/12/13"
    - "axe serious|critical count for the mockable candidate screens (C3/C8) and EntrevistaWorkspace = 0 — LGPD-05"
  artifacts:
    - path: "src/features/entrevista/components/EntrevistaWorkspace.tsx"
      provides: "Radix Tabs migration (FX-04) + disabled+named Agendar CTA (FX-12)"
      contains: "Tabs"
    - path: "src/features/avaliacao/components/SjtMultiplaEscolhaScreen.tsx"
      provides: "radiogroup keyboard nav (FX-06)"
    - path: "src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx"
      provides: "Radix Tooltip submit hint (FX-09) + autosave-copy reconcile (FX-13)"
      contains: "Tooltip"
  key_links:
    - from: "src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx"
      to: "@/components/ui/tooltip"
      via: "import Tooltip — replaces native title="
      pattern: "from '@/components/ui/tooltip'"
    - from: "src/features/entrevista/components/EntrevistaScorecardInline.tsx"
      to: "slider aria-valuetext"
      via: "aria-valuetext readout forwarded to the Slider"
      pattern: "aria-valuetext"
---

<objective>
Fix the entrevista + candidate-avaliação a11y cluster: migrate the EntrevistaWorkspace
custom tabs to Radix Tabs (FX-04), give the candidate option/Likert groups keyboard-correct
radiogroup semantics (FX-06), replace the cognitive-prova native `title=` with a Radix
Tooltip (FX-09), add `aria-valuetext` to the interview/redacao/weight sliders (FX-11), fix
the entrevista amber 24h pill + low-alpha eyebrow contrast (FX-07/08), disable+name the dead
Agendar CTA (FX-12), and reconcile the autosave-copy mismatch (FX-13). Flip the Tier-A axe
loop GREEN for the mockable screens in this cluster.

Purpose: per UI-SPEC risk flags, C5 BigFive multiplies a single per-item radio violation
across 120 Likert items (the hardest score-tanker), C8 native `title=` is a guaranteed AB-5
fail, and R6 stacks tabs+sliders+eyebrows+dead-CTA. This is the second big mover toward
axe-core ≥90 (LGPD-05).

Output: eight edited components across src/features/entrevista, /triagem, /avaliacao,
/avaliacao-cognitiva; the Wave-0 Tier-A assertions for the mockable screens flip GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-compliance-a11y-hardening/16-UI-SPEC.md
@.planning/phases/16-compliance-a11y-hardening/16-RESEARCH.md

<interfaces>
<!-- Vendored Radix primitives — already in src/components/ui/. -->

From src/components/ui/tabs.tsx:        export { Tabs, TabsList, TabsTrigger, TabsContent }
From src/components/ui/radio-group.tsx: export { RadioGroup, RadioGroupItem }
From src/components/ui/tooltip.tsx:     export { Tooltip, TooltipTrigger, TooltipContent, TooltipProvider }
From src/components/ui/slider.tsx:      Slider forwards aria-* to SliderPrimitive.Root (add aria-valuetext)

RNF-07a / LGPD product-language guard: candidate-facing fixes must NOT surface a score/band/
percentile and must keep neutral copy. The Big Five never rejects; the SJT MC is determinstic
but candidate-facing copy stays neutral.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | presentation-tier component edits only |

No new attack surface; a11y/cleanup only; no migration; RLS unchanged. Candidate screens add/relabel ARIA only — no new score/band/PII surfaced (RNF-07a / LGPD product-language respected).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-RNF07a | Information Disclosure | candidate avaliação screens | accept | A11y fixes add aria-valuetext/roles + neutral tooltip copy; assert no score/band/percentile string is introduced on a candidate surface (acceptance grep). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EntrevistaWorkspace Radix Tabs (FX-04) + amber/eyebrow contrast (FX-07/08) + dead Agendar CTA (FX-12)</name>
  <read_first>
    - src/features/entrevista/components/EntrevistaWorkspace.tsx (lines ~117-133 custom aria-pressed tabs; line ~143 the dead Agendar CTA)
    - src/features/entrevista/components/EntrevistaDashboard.tsx (line ~124 the 24h amber pill; lines ~186 + ~194-200 the dead Agendar CTA + text-white/50-60 eyebrows)
    - src/features/entrevista/components/GuiaEntrevistaPanel.tsx (lines ~64,126 text-white/50-60 eyebrows)
    - src/components/ui/tabs.tsx + src/components/ui/tooltip.tsx
    - .planning/phases/16-compliance-a11y-hardening/16-RESEARCH.md §"FX-04" + §Pitfall 4 (composite amber)
    - .planning/phases/16-compliance-a11y-hardening/16-UI-SPEC.md §3b FX-04/07/08 + §3c FX-12
  </read_first>
  <behavior>
    - EntrevistaWorkspace tabs render as Radix Tabs (role=tab/tablist/tabpanel, arrow-key roving focus, aria-selected)
    - The 24h amber pill and the text-white/50 + text-white/60 eyebrows clear ≥4.5:1 at the composited glass background (bump alpha consistently — /50→/70, /60→/75; amber semantic unchanged)
    - The dead "Agendar entrevista" CTA renders `disabled` with a Radix tooltip naming that scheduling is V1-manual — no live no-op focusable control
  </behavior>
  <action>
    Migrate the custom `<button aria-pressed>` tabs at EntrevistaWorkspace.tsx:117-133 to vendored
    Radix `Tabs`/`TabsList`/`TabsTrigger`/`TabsContent` (`@/components/ui/tabs`), driving the existing
    active-section state via `value`/`onValueChange`. For FX-07/08: measure each amber pill
    (EntrevistaDashboard.tsx:124) and each `text-white/50`/`text-white/60` eyebrow
    (GuiaEntrevistaPanel.tsx:64,126; EntrevistaDashboard.tsx:186) at the composited background and bump
    the alpha (`/50`→`/70`, `/60`→`/75`; amber token darker) until axe color-contrast is green —
    apply the eyebrow bump consistently so the uppercase-eyebrow treatment stays uniform; keep the size
    token and the amber semantic. For FX-12: render the dead Agendar CTA (EntrevistaDashboard.tsx:194-200
    + EntrevistaWorkspace.tsx:143) as `disabled` and wrap a Radix Tooltip naming where scheduling happens
    (V1-manual), so it is not a focusable no-op control.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && npm run test:run -- Entrevista 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/entrevista/components/EntrevistaWorkspace.tsx` imports from `@/components/ui/tabs` (grep matches); no `aria-pressed` tab hand-roll remains (grep returns 0)
    - The Agendar CTA carries `disabled` (grep: `Agendar` site has `disabled`)
    - No bare `text-white/50` or `text-white/60` remains on the touched eyebrow lines (grep confirms the bump)
    - `npm run build` exits 0; `npm run -s lint` tsc ≤291
    - The Tier-A axe assertion for the mockable EntrevistaWorkspace subset reports no `serious`/`critical` tabs/contrast violation
  </acceptance_criteria>
  <done>EntrevistaWorkspace uses Radix Tabs, amber+eyebrows pass AA, dead Agendar CTA disabled+named, build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Candidate radiogroup keyboard nav (FX-06) — SjtMultiplaEscolha + BigFive</name>
  <read_first>
    - src/features/avaliacao/components/SjtMultiplaEscolhaScreen.tsx (the custom-radio option cards — confirm exact lines; only aria-checked, no keyboard nav)
    - src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx (the 120 Likert items as custom-radio — confirm exact lines)
    - src/components/ui/radio-group.tsx + src/components/ui/label.tsx
    - .planning/phases/16-compliance-a11y-hardening/16-RESEARCH.md §"FX-05/06" (same remedy) + §"Don't Hand-Roll" radiogroup row
    - .planning/phases/16-compliance-a11y-hardening/16-UI-SPEC.md §3b FX-06 + §"Likely axe risk" #2 (C5 multiplier)
  </read_first>
  <behavior>
    - Each SJT MC option group renders as a Radix RadioGroup with RadioGroupItem + Label pairing; arrow keys move selection, each option exposes role=radio + checked state + accessible name; min-h-[44px] preserved
    - Each Big Five Likert item group exposes radiogroup semantics with roving focus; the per-item violation no longer multiplies across the page
    - Candidate-facing copy stays neutral — no score/band/percentile introduced (RNF-07a)
  </behavior>
  <action>
    Replace the hand-rolled custom-radio option cards in SjtMultiplaEscolhaScreen.tsx and the Likert
    item groups in BigFiveQuestionnaireScreen.tsx with the vendored Radix `RadioGroup`/`RadioGroupItem`
    (`@/components/ui/radio-group`, Label htmlFor pairing), driving the existing selection state via
    `value`/`onValueChange`. This gives arrow-key roving focus + name/role/state for free — the FX-06
    remedy (same class as FX-05). Keep `min-h-[44px]`, the existing shuffle/soft-timer behavior, the
    option text, and the neutral candidate copy. Confirm each item-group exposes name+role+state under axe.
    Do NOT surface any score/band on these candidate surfaces (RNF-07a).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && npm run test:run -- "SjtMultipla|BigFive" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Both screens import from `@/components/ui/radio-group` (grep matches each file)
    - No hand-rolled `role="radio"` custom-radio remains in either file (grep returns 0)
    - No score/band/percentile string introduced on either candidate screen (grep: no new `score`/`percentil`/`banda` literal in the touched JSX)
    - `npm run build` exits 0; `npm run -s lint` tsc ≤291
    - The Tier-A axe assertion for the mockable C3 SjtMultiplaEscolha reports no `serious`/`critical` `aria-required-children`/`name-role-value` violation
  </acceptance_criteria>
  <done>Both candidate radio surfaces use Radix RadioGroup with keyboard nav, neutral copy preserved, build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cognitive tooltip (FX-09) + autosave copy (FX-13) + slider aria-valuetext (FX-11)</name>
  <read_first>
    - src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx (line ~367 the native `title=` submit-disabled hint; line ~63 the autosave-copy-mismatch intro)
    - src/features/entrevista/components/EntrevistaScorecardInline.tsx (line ~115 the BARS Slider with aria-label but no aria-valuetext)
    - src/features/triagem/components/RedacaoReviewPanel.tsx (the BARS override sliders)
    - src/components/ui/tooltip.tsx + src/components/ui/slider.tsx
    - .planning/phases/16-compliance-a11y-hardening/16-RESEARCH.md §"FX-09" + §"FX-11" code examples + §Pitfall 3 (axe under-tests live regions → AB-8 manual)
    - .planning/phases/16-compliance-a11y-hardening/16-UI-SPEC.md §3b FX-09/FX-11 + §3c FX-13
  </read_first>
  <behavior>
    - The cognitive submit-disabled hint renders via Radix Tooltip on a keyboard-focusable trigger (real button / tabIndex 0), not native title=
    - The cognitive intro copy no longer promises autosave the screen does not deliver — copy softened to not promise persistence (the conservative FX-13 branch), OR autosave wired with an aria-live "Salvo automaticamente" announce (accent Check reserved-#35BFAD); pick the softened-copy branch unless useAutosaveAvaliacao is already trivially available
    - Interview + redacao BARS sliders expose aria-valuetext (e.g. "{n} de 5") so SR users hear the readout; weight sliders expose "{pct}%"
  </behavior>
  <action>
    FX-09: replace the native `title=` submit hint at ProvaCognitivaScreen.tsx:367 with the vendored
    Radix `Tooltip` (`@/components/ui/tooltip`), wrapping a keyboard-focusable trigger (the submit
    `<Button>` inside a focusable `<span>` or a real button) so the hint is keyboard/SR reachable.
    FX-13: at ProvaCognitivaScreen.tsx:63 reconcile the autosave-copy mismatch — soften the intro copy
    so it does not promise persistence the screen lacks (default), unless wiring `useAutosaveAvaliacao` +
    the reserved accent Check "Salvo automaticamente" with `aria-live` is trivial here. FX-11: add
    `aria-valuetext` to the interview BARS slider (EntrevistaScorecardInline.tsx:115, e.g.
    `aria-valuetext={\`${n} de 5\`}`) and the RedacaoReviewPanel BARS override sliders, ensuring
    aria-valuemin/max/now are present; weight sliders use `aria-valuetext={\`${pct}%\`}`. Keep all
    existing behavior and the neutral candidate copy.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && npm run test:run -- "ProvaCognitiva|Scorecard|RedacaoReview" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx` imports from `@/components/ui/tooltip` (grep matches); no native `title={` submit hint remains at the FX-09 site (grep returns 0 for that pattern)
    - `aria-valuetext` appears on the EntrevistaScorecardInline slider and the RedacaoReviewPanel sliders (grep matches in both files)
    - The cognitive intro copy no longer promises autosave without delivering it (verified against the FX-13 branch chosen; documented in the SUMMARY)
    - `npm run build` exits 0; `npm run -s lint` tsc ≤291
    - The Tier-A axe assertion for the mockable C8 ProvaCognitivaScreen reports no `serious`/`critical` `button-name`/`aria-valid-attr-value` violation
  </acceptance_criteria>
  <done>Cognitive tooltip is Radix (keyboard-reachable), autosave copy reconciled, BARS/weight sliders carry aria-valuetext, build green.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 after all three tasks.
- `npm run -s lint` tsc ≤291 (no growth).
- `npx playwright test e2e/a11y.spec.ts --project=chromium` — Tier-A assertions for the mockable C3/C8 + EntrevistaWorkspace subset GREEN.
- AB-5/AB-6/AB-8 (radiogroup arrow-key nav, visible focus, autosave aria-live) noted for 16-HUMAN-UAT — axe under-tests these (RESEARCH Pitfall 3).
</verification>

<success_criteria>
- EntrevistaWorkspace Radix Tabs; candidate radios keyboard-correct; cognitive tooltip Radix; sliders aria-valuetext; amber/eyebrows AA; dead CTA disabled; autosave copy reconciled.
- Tier-A axe GREEN for the mockable screens in this cluster; build green; tsc no-growth; no score/band leaked to candidate surfaces.
</success_criteria>

<output>
Create `.planning/phases/16-compliance-a11y-hardening/16-03-SUMMARY.md` when done
</output>
