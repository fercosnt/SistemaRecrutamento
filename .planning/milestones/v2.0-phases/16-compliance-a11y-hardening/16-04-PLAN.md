---
phase: 16-compliance-a11y-hardening
plan: 04
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/components/pages/PerfilCandidatoRHPage.tsx
  - src/components/pages/SuporteRHPage.tsx
  - src/features/admin/bias-audit/biasMath.ts
  - src/features/admin/bias-audit/__tests__/biasMath.test.ts
  - src/features/vagas/types/vagasTypes.ts
  - src/components/pages/LoginRHPage.tsx
  - .github/workflows/ci.yml
autonomous: false
requirements: [LGPD-05]
user_setup: []

must_haves:
  truths:
    - "Zero debug console.log/debug/info/warn remains on the RH-path — the 16-01 grep guard flips GREEN (FX-14)"
    - "The dead biasMath runtime functions (computeAdverseImpact, bandFromAge) are removed but the live BandResult/AdverseImpactResult type exports are preserved — npm run build exits 0 (FX-15)"
    - "The two safe enum typos (clinica→clinico L581, big_five→bigfive L734) are fixed; the tempo_integral TIPO_VAGA_LABELS map is a decision and stays deferred"
    - "The staged LoginRHPage.tsx race+gate fix is committed and an RH cold-start login round-trip is verified (real RH account → /rh/dashboard with role ∈ {rh, administrador}, else HUMAN-UAT)"
    - "The tsc baseline drops below 291 and ci.yml is lowered to the new count (gate tightens, not loosens) — FOUND-08 trivial set"
  artifacts:
    - path: "src/features/admin/bias-audit/biasMath.ts"
      provides: "type exports kept, dead functions removed"
      contains: "export type"
    - path: "src/features/vagas/types/vagasTypes.ts"
      provides: "clinico + bigfive enum fixes"
      contains: "clinico"
    - path: "src/components/pages/LoginRHPage.tsx"
      provides: "committed race(3s)+gate({rh,administrador}) fix"
      contains: "administrador"
    - path: ".github/workflows/ci.yml"
      provides: "lowered tsc baseline number"
  key_links:
    - from: "src/__tests__/guards/rh-console.grep.test.ts"
      to: "RH-path source files"
      via: "grep guard flips GREEN after console removal"
      pattern: "console"
    - from: "src/features/admin/bias-audit/services/biasAuditService.ts"
      to: "src/features/admin/bias-audit/biasMath.ts"
      via: "import type { AdverseImpactResult, BandResult } — MUST stay live"
      pattern: "import type.*biasMath"
---

<objective>
Close the Phase 16 cleanups + the trivial tsc burn-down + the auth round-trip: remove the
RH-path debug console.log (FX-14, flips the 16-01 grep guard GREEN), surgically delete the
dead biasMath runtime functions while preserving the live type exports (FX-15), fix the two
safe enum typos in vagasTypes.ts (FOUND-08 trivial set), commit the already-staged
LoginRHPage.tsx race+gate fix and verify the RH login round-trip, and lower the ci.yml tsc
baseline to the new (dropped) count so the gate tightens.

Purpose: satisfies the second ROADMAP success criterion's "addressed opportunistically" clause
for the in-scope tech-debt (console.log + FOUND-08 trivial), closes the FX-14/FX-15 carry-in
cleanups, and lands the staged RH-login fix that unblocks RH access. NO new migration — the
auth-hook RLS+grant+hook chain is already PROD-verified complete (CONTEXT fact #1); auth work
is frontend-only (commit + verify).

Output: console-clean RH path, slimmed biasMath, two enum fixes, committed LoginRHPage,
lowered tsc baseline in ci.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-compliance-a11y-hardening/16-RESEARCH.md
@.planning/phases/16-compliance-a11y-hardening/16-UI-SPEC.md

<interfaces>
<!-- The biasMath split (RESEARCH Pitfall 2 — verified this session): -->
LIVE type exports (KEEP):  BandResult, AdverseImpactResult, AgeBand, BandInput, ComputeOptions,
                           FOUR_FIFTHS_THRESHOLD, SMALL_SAMPLE_FLOOR
  → imported by biasAuditService.ts (lines 22,32,126,135) + BiasAuditPage.tsx (lines 47,71,168)
DEAD runtime functions (REMOVE, zero non-test callers): bandFromAge (L102), computeAdverseImpact (L122)
  → only callers are biasMath.test.ts + the file itself

Enum-typo fixes (RESEARCH §"Enum-label one-liner fixes" — the ONLY safe enum edits):
  vagasTypes.ts L581  clinica → clinico   (TS2561 — Departamento union has 'clinico')
  vagasTypes.ts L734  big_five → bigfive  (TS2561 — EtapaProcesso uses 'bigfive')
  vagasTypes.ts L569  tempo_integral      → DECISION not a typo (TIPO_VAGA_LABELS map stale vs TipoVaga='CLT'|'PJ'); DEFER (see 16-01 backlog doc)

RH-path console.log targets (verified): PerfilCandidatoRHPage.tsx (~428,432,436,485,490) + SuporteRHPage.tsx (~102)

Staged LoginRHPage diff (CONTEXT fact #1 — already in working tree, NOT yet committed):
  poll 5×20ms=100ms → 60×50ms=3s (cold-DB fetchProfile race) AND gate role!=='administrador' → role!=='administrador' && role!=='rh'

CI baseline gate: .github/workflows/ci.yml unit job — `if [ "$COUNT" -gt 292 ]` (frozen baseline string says 292; live count 291). Lower it to the post-burn-down count.

Commits use `git -c core.hooksPath=/dev/null` (project convention — KEPT this phase). Do NOT re-enable the husky pre-commit hook.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → RH panel | LoginRHPage role gate (UX defense-in-depth) widened to {rh, administrador} |

No new attack surface; cleanup + frontend auth-race fix only; NO migration; RLS unchanged (the usuarios_rh RLS+grant+hook chain is already PROD-verified per CONTEXT fact #1).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-EoP | Elevation of Privilege | LoginRHPage widened gate (admin→{rh,admin}) | accept | The gate is UX-only; server RLS still scopes every RH read by usuarios_rh membership + vaga ownership (Phase 15 WR-03). Widening the client gate cannot grant data the RLS doesn't already permit. Verify in the RH-login round-trip. |
| T-16-04-Info | Information Disclosure | RH-path console.log | mitigate | FX-14 removes the debug console.* on the RH path; the 16-01 grep guard enforces zero going forward. |
| T-16-04-SC | Tampering | npm installs | accept | No new packages this phase. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: FX-14 remove RH-path console.* + FX-15 surgical biasMath dead-fn removal</name>
  <read_first>
    - src/components/pages/PerfilCandidatoRHPage.tsx (the ~6 console.log sites at ~428,432,436,485,490) and src/components/pages/SuporteRHPage.tsx (~102)
    - src/__tests__/guards/rh-console.grep.test.ts (the 16-01 guard this task flips GREEN)
    - src/features/admin/bias-audit/biasMath.ts (the LIVE type exports vs the DEAD bandFromAge L102 + computeAdverseImpact L122)
    - src/features/admin/bias-audit/services/biasAuditService.ts (lines 22,32,126,135 — the live type imports that MUST keep resolving) and BiasAuditPage.tsx (lines 47,71,168)
    - src/features/admin/bias-audit/__tests__/biasMath.test.ts (delete the dead-fn cases with the functions)
    - .planning/phases/16-compliance-a11y-hardening/16-RESEARCH.md §Pitfall 2 (biasMath is LIVE — flat delete BREAKS the build)
  </read_first>
  <action>
    FX-14: remove the debug `console.log` calls on the RH-path — delete the calls (or the dead handlers
    that only console.log) in PerfilCandidatoRHPage.tsx (~428,432,436,485,490) and SuporteRHPage.tsx (~102).
    Where a handler exists ONLY to console.log, remove the console call and leave a clean no-op/comment or
    wire the real action if trivially obvious; do not break call sites. The 16-01 `rh-console.grep` guard
    must flip GREEN.
    FX-15: in biasMath.ts remove ONLY the dead runtime functions `bandFromAge` (L102) and
    `computeAdverseImpact` (L122) plus any now-unused local constants — KEEP every exported type
    (`BandResult`, `AdverseImpactResult`, `AgeBand`, `BandInput`, `ComputeOptions`,
    `FOUR_FIFTHS_THRESHOLD`, `SMALL_SAMPLE_FLOOR`) because biasAuditService.ts + BiasAuditPage.tsx import
    them live (RESEARCH Pitfall 2 — a flat file delete breaks the build). Delete the corresponding
    dead-function test cases in biasMath.test.ts (or the whole file if it only tested those functions),
    leaving any type-level assertions intact. Confirm `npm run build` exits 0 (catches "BandResult is not
    exported" if the types were touched).
  </action>
  <verify>
    <automated>npm run test:run -- rh-console.grep 2>&1 | tail -4 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- rh-console.grep` is GREEN (zero RH-path console.* — FX-14 guard flips)
    - `biasMath.ts` no longer contains `function computeAdverseImpact` or `function bandFromAge` (grep returns 0) but STILL exports `BandResult` and `AdverseImpactResult` (grep matches `export.*BandResult` and `export.*AdverseImpactResult`)
    - `npm run build` exits 0 (the live type imports still resolve)
    - `npm run -s lint` tsc count ≤291 (does not rise)
  </acceptance_criteria>
  <done>RH path is console-clean (guard GREEN), biasMath dead functions removed with types preserved, build green.</done>
</task>

<task type="auto">
  <name>Task 2: FOUND-08 trivial enum fixes + lower ci.yml tsc baseline</name>
  <read_first>
    - src/features/vagas/types/vagasTypes.ts (L581 clinica, L734 big_five — the two safe fixes; L569 tempo_integral — DO NOT touch, it is the deferred decision)
    - src/features/vagas/services/vagasService.ts (check for any clinica/big_five consumer needing the same rename)
    - .github/workflows/ci.yml (the unit job `if [ "$COUNT" -gt 292 ]` baseline string + the "frozen baseline 292" comment)
    - .planning/phases/16-compliance-a11y-hardening/16-RESEARCH.md §Pitfall 1 (only clinica + big_five are safe one-liners; everything else structural → deferred) + §Open-Q3 (re-measure + lower the baseline)
    - .planning/todos/pending/found-08-tsc-burndown-tail.md (the structural tail this task does NOT chase)
  </read_first>
  <action>
    Apply ONLY the two confirmed-safe enum-label fixes in vagasTypes.ts: L581 `clinica:`→`clinico:`
    (TS2561 — Departamento union has 'clinico') and L734 `big_five:`→`bigfive:` (TS2561 — EtapaProcesso
    uses 'bigfive'). Mirror the same key in vagasService.ts if it consumes those literals. DO NOT touch
    L569 `tempo_integral` / the TIPO_VAGA_LABELS map — it is a decision vs the `'CLT'|'PJ'` union, deferred
    to the 16-01 backlog doc. If either enum fix produces a NEW error elsewhere it was load-bearing →
    revert that one and note it (RESEARCH Pitfall 1 warning sign). Re-measure the tsc count
    (`npm run -s lint | grep -c "error TS"`) and LOWER the ci.yml unit-job baseline (the `-gt 292` literal
    and the "frozen baseline 292" comment) to the new post-burn-down count so the gate tightens — never
    raise it. Keep the `core.hooksPath=/dev/null` husky bypass (do not re-enable the pre-commit hook).
  </action>
  <verify>
    <automated>npm run -s lint 2>&1 | grep -c "error TS" || true; npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - vagasTypes.ts contains `clinico:` and `bigfive:` (grep both) and no longer has the `clinica:`/`big_five:` typo keys on those lines
    - vagasTypes.ts L569 `tempo_integral` is UNCHANGED (the deferred decision)
    - The tsc count is strictly LESS than 291 after the fixes (the two TS2561 errors cleared, no new errors)
    - `.github/workflows/ci.yml` baseline literal is lowered to the new count (grep: the `-gt` number is < 292)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The two safe enum typos fixed, tsc baseline dropped, ci.yml baseline lowered, tempo_integral deferred, build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The staged `src/components/pages/LoginRHPage.tsx` race+gate fix has been committed
    (poll widened 100ms→3s for the cold-DB fetchProfile usuarios_rh round-trip; gate widened
    admin-only→{rh, administrador}). NO new migration was authored — the usuarios_rh RLS+grant+hook
    chain is already PROD-verified complete (CONTEXT fact #1). Claude has committed the staged file
    via `git -c core.hooksPath=/dev/null` and confirmed build + the Tier-A axe assertion for R1
    (/auth/login-rh) stays green.
  </what-built>
  <how-to-verify>
    A real RH-login round-trip is needed (no RH test account is confirmed in PROD — RESEARCH A5/Open-Q2),
    so this is a human verification. Steps:
    1. After an idle period (COLD-START — the stress case for the 3s poll window), go to `/auth/login-rh`.
    2. Log in with a real `recrutador` (role 'rh') account → confirm you land on `/rh/dashboard` and are
       NOT bounced to /vagas with "Esta conta não tem acesso ao painel RH."
    3. Log in with a real `administrador` account → confirm the same (no false "sem acesso" bounce).
    4. In devtools, inspect the JWT `app_metadata.role` (or the authStore role) → confirm it is
       'rh' or 'administrador' respectively.
    5. Confirm `/auth/login-rh` (R1) still renders the login form cleanly (axe AA bar).
    If no real RH account is available to test, record this as a deferred item in 16-HUMAN-UAT.md and
    approve to proceed (the frontend fix is committed; the live round-trip is the only manual gate).
  </how-to-verify>
  <resume-signal>Type "approved" (RH login reaches the panel with the correct role) or describe the bounce/role you saw</resume-signal>
</task>

</tasks>

<verification>
- `npm run test:run -- rh-console.grep` GREEN (FX-14).
- `npm run build` exits 0 (biasMath types preserved — FX-15).
- `npm run -s lint` tsc count < 291; ci.yml baseline lowered to match.
- LoginRHPage.tsx committed; RH cold-start login round-trip verified (or deferred to 16-HUMAN-UAT.md).
- `npx playwright test e2e/a11y.spec.ts --project=chromium` — R1 (/auth/login-rh) stays green.
</verification>

<success_criteria>
- RH path console-clean; biasMath dead functions removed with live types intact; the two safe enum typos fixed; tsc baseline dropped + ci.yml tightened.
- Staged LoginRHPage fix committed; RH login round-trip PASS (or documented deferral).
- NO migration authored (CONTEXT fact #1 honored).
</success_criteria>

<output>
Create `.planning/phases/16-compliance-a11y-hardening/16-04-SUMMARY.md` when done
</output>
