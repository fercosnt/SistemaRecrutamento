---
phase: 16-compliance-a11y-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - e2e/a11y.spec.ts
  - e2e/fixtures/a11y-session.ts
  - src/__tests__/guards/rh-console.grep.test.ts
  - .planning/todos/pending/hard-02-bundle-code-splitting.md
  - .planning/todos/pending/perf-01-cache-invalidation.md
  - .planning/todos/pending/found-08-tsc-burndown-tail.md
autonomous: true
requirements: [LGPD-05]
user_setup: []

must_haves:
  truths:
    - "e2e/a11y.spec.ts runs an UNCONDITIONAL Tier-A axe loop over the in-scope main M2 screens under a mocked Supabase session (candidate + RH roles)"
    - "The new Tier-A assertions are calibrated RED on the current tree (failing axe serious/critical) — the failure IS the Wave-0 gate that flips GREEN as FX fixes land (LGPD-05)"
    - "The existing public-route loop (Phase-5 zero-violations gate) is preserved untouched and still strict"
    - "A grep guard test fails if any console.log/debug remains on the RH-path (FX-14 contract, RED until cleanup lands)"
    - "HARD-02, PERF-01, and the tsc burn-down tail are written to backlog docs so the 'documented even where deferred' success criterion is met"
  artifacts:
    - path: "e2e/a11y.spec.ts"
      provides: "Tier-A main-M2 axe loop + mocked-session helper + .exclude() false-positive scaffold"
      contains: "AxeBuilder"
    - path: "e2e/fixtures/a11y-session.ts"
      provides: "makeJwt + mockSession(page, role) reusable fixture"
      exports: ["makeJwt", "mockSession"]
    - path: "src/__tests__/guards/rh-console.grep.test.ts"
      provides: "RH-path console.* grep guard (FX-14)"
      contains: "console"
    - path: ".planning/todos/pending/hard-02-bundle-code-splitting.md"
      provides: "HARD-02 deferral backlog doc"
    - path: ".planning/todos/pending/perf-01-cache-invalidation.md"
      provides: "PERF-01 deferral backlog doc"
    - path: ".planning/todos/pending/found-08-tsc-burndown-tail.md"
      provides: "FOUND-08 structural tsc tail deferral backlog doc"
  key_links:
    - from: "e2e/a11y.spec.ts"
      to: "e2e/fixtures/a11y-session.ts"
      via: "import { mockSession }"
      pattern: "import.*a11y-session"
    - from: "e2e/fixtures/a11y-session.ts"
      to: "RoleGuard role gate"
      via: "JWT app_metadata.role mocked so RoleGuard renders children"
      pattern: "app_metadata"
---

<objective>
Stand up the Wave-0 RED gates for Phase 16 BEFORE any FX fix lands: the unconditional
Tier-A axe loop over the in-scope main M2 screens, the FX-14 RH-console grep guard, and
the three documented-deferral backlog docs. The failing axe assertions and the failing
grep guard ARE the calibrated contracts that downstream waves flip GREEN — the
smoke-runtime gate precedent established in Phase 4.1.

Purpose: LGPD-05 is operationalized as "axe-core ≥90 (zero serious/critical) on the main
M2 screens, verified in CI." This plan creates the CI-enforced assertion that defines
"done," and satisfies the second ROADMAP success criterion's "documented even where
deferred" clause for the three out-of-scope tech-debt items.

Output: extended `e2e/a11y.spec.ts` (existing public loop untouched + new Tier-A loop),
a reusable `e2e/fixtures/a11y-session.ts`, the RH-console grep guard, and 3 backlog docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-compliance-a11y-hardening/16-RESEARCH.md
@.planning/phases/16-compliance-a11y-hardening/16-UI-SPEC.md
@.planning/phases/16-compliance-a11y-hardening/16-VALIDATION.md

<interfaces>
<!-- Existing idioms the executor reuses verbatim — no codebase exploration needed. -->

From e2e/a11y.spec.ts (existing — DO NOT loosen the public loop):
  - public `routes[]` loop asserts `expect(results.violations).toEqual([])` (zero ANY impact)
  - `.exclude(DEFERRED_SELECT_PLACEHOLDER='[data-slot="select-trigger"]')` + `.exclude(OTP_TRANSPARENT_INPUT='#token')` — the sanctioned tracked-false-positive mechanism

From e2e/perfil.spec.ts (the makeJwt + page.route mocked-session idiom — copy it):
  - `makeJwt(payload)` base64url header.body.fake-signature (supabase-js never re-verifies client-side)
  - `page.route('**/auth/v1/token?grant_type=password', r => r.fulfill({ access_token: jwt, ... }))`

From src/components/RoleGuard.tsx + src/features/auth/utils/extractRole.ts:
  - RoleGuard gates ONLY on authStore.role, derived from JWT `app_metadata.role` via extractRole
  - mocking the token endpoint with `app_metadata.role ∈ {candidato, rh, administrador}` renders the guarded children

From the UI-SPEC §1 scope set — the 16 in-scope screens (R1..R8 RH + C1..C9 candidate; C6/C9 low-risk kept in sweep). Tier-A/Tier-B split (resolves RESEARCH Open-Q1): **R6 EntrevistaWorkspace and R7 DecisaoFinalPage ARE confirmed mockable — include them in the unconditional Tier-A gate** (16-02/16-03 acceptance criteria depend on it). **Only R5 (redação, live redacoes_candidato + review state) and C5 (BigFive, 120-Likert heavy + live scores) default to Tier-B** (E2E_REAL_LOGIN skip-with-reason). All other in-scope screens are Tier-A. Explicit Tier-A list: R1,R2,R3,R4,R6,R7,R8,C1,C2,C3,C4,C6,C7,C8,C9; Tier-B: R5,C5.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app shell | mocked (unsigned) JWT injected via page.route in tests only |

No new attack surface; a11y/CI/cleanup only; no migration; RLS unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Spoofing | test `makeJwt` mock | accept | Mock lives only in e2e/fixtures + e2e/*.spec.ts (test-local page.route); never imported by src/. supabase-js never re-verifies client-side; RLS (server) is the real gate, unchanged. |
| T-16-SC | Tampering | npm installs | accept | No new packages this phase (@axe-core/playwright + axe-core already in lockfile; RESEARCH Package Legitimacy Audit pre-approved). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Tier-A mocked-session axe loop + reusable fixture</name>
  <read_first>
    - e2e/a11y.spec.ts (the existing public loop + the two `.exclude()` precedents — KEEP strict, do not touch)
    - e2e/perfil.spec.ts (the makeJwt + page.route('**/auth/v1/token**') mocked-session idiom to copy into the fixture)
    - src/components/RoleGuard.tsx + src/features/auth/utils/extractRole.ts (confirm RoleGuard reads authStore.role from JWT app_metadata.role)
    - .planning/phases/16-compliance-a11y-hardening/16-UI-SPEC.md §1 (the 16 in-scope screens + routes) and §4a (the Tier-A assertion shape)
    - .planning/phases/16-compliance-a11y-hardening/16-RESEARCH.md §Pattern 1 + Assumption A1 (Tier-A subset vs Tier-B remainder)
  </read_first>
  <action>
    Create `e2e/fixtures/a11y-session.ts` exporting `makeJwt(payload)` and `mockSession(page, role)`
    copied from the perfil.spec.ts idiom — `mockSession` fulfills `**/auth/v1/token?grant_type=password`
    with a JWT carrying `app_metadata.role` ∈ {candidato, rh, administrador} so RoleGuard renders children.
    Extend `e2e/a11y.spec.ts`: KEEP the existing public `routes[]` loop and its strict `toEqual([])`
    assertion + both `.exclude()` constants UNTOUCHED. ADD a new Tier-A block iterating the
    cleanly-mockable in-scope screens — the EXPLICIT Tier-A list is R1,R2,R3,R4,R6,R7,R8,C1,C2,C3,C4,C6,C7,C8,C9
    (R6 EntrevistaWorkspace + R7 DecisaoFinalPage ARE in Tier-A — 16-02/16-03 depend on this). Only R5 (redação)
    and C5 (BigFive 120-Likert) go to a Tier-B `E2E_REAL_LOGIN`-gated skip-with-reason block, NOT the unconditional gate.
    For each Tier-A screen: mockSession with the correct role, mock the per-screen `**/rest/v1/<table>**`
    reads needed to render the shell, page.goto the route, run `new AxeBuilder({ page })
    .withTags(['wcag2a','wcag2aa','wcag21a','wcag21aa']).analyze()`, then assert
    `results.violations.filter(v => v.impact==='serious'||v.impact==='critical')` is `toEqual([])`.
    Reuse the existing `.exclude(selector)` + tracking-comment mechanism for any genuine
    false-positive node — NEVER test.skip() a whole screen. This loop is calibrated RED on the
    current tree (FX defects not yet fixed) per the LGPD-05 / smoke-runtime contract.
  </action>
  <verify>
    <automated>npx playwright test e2e/a11y.spec.ts --project=chromium --list</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/fixtures/a11y-session.ts` exists and exports `makeJwt` + `mockSession` (grep: `export.*makeJwt` and `export.*mockSession` both match)
    - `e2e/a11y.spec.ts` still contains the original public-route `expect(results.violations).toEqual([])` line (grep confirms the strict public loop is intact)
    - `e2e/a11y.spec.ts` contains a Tier-A block asserting `v.impact === 'serious' || v.impact === 'critical'` filter (grep: `serious.*critical` matches)
    - `npx playwright test e2e/a11y.spec.ts --project=chromium --list` parses the new tests without a syntax error (the new Tier-A screen tests appear in the listing)
    - The unconditional Tier-A gate INCLUDES R6 (EntrevistaWorkspace) and R7 (DecisaoFinalPage); only R5 + C5 are in the Tier-B skip-with-reason block (grep: the Tier-A screen list contains DecisaoFinal + Entrevista routes)
    - Running the Tier-A loop on the current tree is RED (axe serious/critical present) for at least one high-density screen (R7 DecisaoFinalPage or R8 BiasAuditPage) — the failure is the calibrated Wave-0 gate, NOT a defect in this task
  </acceptance_criteria>
  <done>
    e2e/a11y.spec.ts has a working, parseable Tier-A mocked-session axe loop over the
    cleanly-mockable in-scope screens (serious/critical filter), the public loop is preserved
    strict, the reusable fixture exists, and the loop is RED on the unfixed tree.
  </done>
</task>

<task type="auto">
  <name>Task 2: FX-14 RH-console grep guard (RED until cleanup)</name>
  <read_first>
    - src/features/auth/utils/__tests__/pitfall7.grep.test.ts (the exact node:fs grep-guard pattern + collectFiles helper + sanity-count idiom to mirror)
    - src/components/pages/PerfilCandidatoRHPage.tsx (6 console.log sites: lines ~428,432,436,485,490) and src/components/pages/SuporteRHPage.tsx (1 site: line ~102) — the FX-14 targets
    - .planning/phases/16-compliance-a11y-hardening/16-UI-SPEC.md §3c FX-14
  </read_first>
  <action>
    Create `src/__tests__/guards/rh-console.grep.test.ts` mirroring the `pitfall7.grep.test.ts`
    structure (node:fs readFileSync scan, NOT child_process; skip `__tests__` subtrees so guard tests
    can name the tokens). Define an `RH_PATH_FILES` list covering the RH-path debug-log surfaces:
    `src/components/pages/PerfilCandidatoRHPage.tsx`, `src/components/pages/SuporteRHPage.tsx`, plus a
    recursive scan of `src/features/decisao`, `src/features/entrevista`, `src/features/triagem`
    (the RH feature dirs). Assert ZERO `console.(log|debug|info|warn)` matches across those files.
    Use a comment-aware match (strip leading `//` lines or `^\s*\*` JSDoc lines from the scanned text
    so a doc-comment mentioning "console" cannot self-invalidate the gate — mirror the pitfall7 guard's
    discipline). Include a sanity-count `it` asserting the resolved file count ≥ 2 so a future path typo
    fails loud. This guard is RED on the current tree (the 7 confirmed RH-path console.log sites) — the
    failure is the FX-14 contract that the cleanup plan flips GREEN.
  </action>
  <verify>
    <automated>npm run test:run -- rh-console.grep</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/guards/rh-console.grep.test.ts` exists and uses `node:fs` (grep: `from 'node:fs'` matches; `child_process` does NOT appear)
    - The guard's forbidden regex matches `console.(log|debug|info|warn)` (grep: `console\\\\.` pattern present)
    - On the current tree the guard is RED — `npm run test:run -- rh-console.grep` reports the PerfilCandidatoRHPage + SuporteRHPage console.log sites as violations (calibrated Wave-0 failure, expected)
    - A sanity-count `it` asserts resolved file count ≥ 2
  </acceptance_criteria>
  <done>
    The RH-console grep guard exists, is comment-safe, lists the RH-path surfaces, and fails
    RED on the current tree against the confirmed console.log sites.
  </done>
</task>

<task type="auto">
  <name>Task 3: Backlog deferral docs (HARD-02 / PERF-01 / tsc tail)</name>
  <read_first>
    - .planning/REQUIREMENTS.md lines 121-124 (the tech-debt list: PERF-01, HARD-02, FOUND-08, console.log)
    - .planning/phases/16-compliance-a11y-hardening/16-CONTEXT.md §Deferred Ideas (the exact deferral rationale + baselines)
    - .planning/phases/16-compliance-a11y-hardening/16-RESEARCH.md §Pitfall 1 (the structural tsc tail that stays deferred — TS2307 vendored primitives, TS2322/2339/2551 structural, the TipoVaga map decision)
    - .planning/todos/pending/ (existing dir + cc0-cognitive-item-bank-sourcing.md as the doc-shape precedent)
  </read_first>
  <action>
    Write three backlog docs under `.planning/todos/pending/` documenting the out-of-scope deferrals
    so the ROADMAP success criterion "tech-debt is triaged and addressed... documented even where
    deferred" is satisfied. (1) `hard-02-bundle-code-splitting.md`: HARD-02, 661 KiB monolithic bundle,
    Lighthouse warn-baseline 0.62–0.68, deferred because large/higher-risk; note the lighthouse CI job
    stays at its current baseline. (2) `perf-01-cache-invalidation.md`: PERF-01, ≤60s apply→display
    window on candidate profile, backend/data-freshness concern, deferred. (3) `found-08-tsc-burndown-tail.md`:
    the STRUCTURAL tsc tail that stays deferred per RESEARCH §Pitfall 1 — TS2307 (65, vendored
    src/components/ui/* versioned-import primitives, DO NOT touch), TS7006/TS2322/TS2339/TS2345
    (structural type decisions), TS2551 schema/form-shape mismatches in CriarEditarVagaPage +
    DadosProfissionaisStep, and the `tempo_integral` TIPO_VAGA_LABELS map (a decision vs the
    `'CLT'|'PJ'` union, NOT a typo) — plus the kept `core.hooksPath=/dev/null` husky bypass and the
    migration-version drift reconciliation. Each doc states what is deferred, why, and the success
    criterion it satisfies. NO implementation — documentation only.
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/hard-02-bundle-code-splitting.md && test -f .planning/todos/pending/perf-01-cache-invalidation.md && test -f .planning/todos/pending/found-08-tsc-burndown-tail.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under `.planning/todos/pending/`
    - `hard-02-bundle-code-splitting.md` mentions "661" and "Lighthouse" (grep both)
    - `perf-01-cache-invalidation.md` mentions "60s" and "perfil" (grep both)
    - `found-08-tsc-burndown-tail.md` mentions "TS2307" and "tempo_integral" and "core.hooksPath" (grep all three — the structural-tail + the kept bypass)
    - No source/test code changed by this task (only `.planning/todos/pending/*.md` added)
  </acceptance_criteria>
  <done>
    Three backlog docs exist documenting HARD-02, PERF-01, and the structural tsc tail (incl.
    the TipoVaga map decision + kept husky bypass + migration drift), satisfying the
    "documented even where deferred" success criterion.
  </done>
</task>

</tasks>

<verification>
- `npx playwright test e2e/a11y.spec.ts --project=chromium --list` parses (new Tier-A tests listed); public loop preserved strict.
- `npm run test:run -- rh-console.grep` is RED on the current tree (the FX-14 contract).
- Three backlog docs exist under `.planning/todos/pending/`.
- `npm run -s lint` tsc count unchanged at ≤291 (this plan adds only test/fixture/doc files; the fixture is under e2e/ outside the tsc src include).
</verification>

<success_criteria>
- The unconditional Tier-A axe loop exists in e2e/a11y.spec.ts and is RED on the unfixed tree (LGPD-05 gate defined).
- The RH-console grep guard exists and is RED (FX-14 contract defined).
- The three deferral backlog docs exist (second ROADMAP success criterion's "documented" clause satisfied).
- tsc baseline does not grow.
</success_criteria>

<output>
Create `.planning/phases/16-compliance-a11y-hardening/16-01-SUMMARY.md` when done
</output>
