---
phase: 13-reda-o-cultural-revis-o-humana
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/features/avaliacao/schemas/redacaoSchema.ts
  - src/features/avaliacao/services/redacaoService.ts
  - src/features/avaliacao/components/RedacaoCounter.tsx
  - src/features/avaliacao/components/RedacaoCronometro.tsx
  - src/features/avaliacao/components/RedacaoEditorScreen.tsx
  - src/features/avaliacao/components/AvaliacaoContainer.tsx
  - src/router/routes.tsx
autonomous: true
requirements: [AVAL-05, AVAL-06]

must_haves:
  truths:
    - "D-13 traceability: the candidate essay screen is one more 'teste' card in the existing Phase 11 AvaliacaoContainer (no new shell, D-27/UI-SPEC), with the 200-500-word hard min/max counter, reused useAutosaveAvaliacao (30s local + 30s DB + 42501 back-lock), and a neutral post-submit message — the candidate NEVER sees a score/color/threshold (RNF-07a)."
    - "The candidate read of redacoes_candidato uses an explicit allowlist string that EXCLUDES every verdict column (analise_ia, scores_dimensao, score_ponderado_0_100, classificacao_cor, red_flag_etico, flags, notas_revisor, decisao_revisor) — never select('*') (Pitfall 3)."
    - "The client submit posts only {candidatura_id, pergunta_id, texto} (no score field, .strict()) and invokes the avaliar-redacao-cultural EF, receiving a neutral ack."
  artifacts:
    - path: "src/features/avaliacao/components/RedacaoEditorScreen.tsx"
      provides: "candidate essay screen — glass shell + textarea + counter + cronometro + autosave + submit dialog"
      min_lines: 120
    - path: "src/features/avaliacao/components/RedacaoCounter.tsx"
      provides: "200ms-debounced 3-band word counter (muted/accent/amber) driving submit-disabled"
    - path: "src/features/avaliacao/services/redacaoService.ts"
      provides: "allowlist reads (no select('*')) + perguntas_redacao read + EF invoke neutral ack"
      contains: "REDACAO_CANDIDATO_ALLOWLIST"
  key_links:
    - from: "src/features/avaliacao/components/RedacaoEditorScreen.tsx"
      to: "src/features/avaliacao/hooks/useAutosaveAvaliacao.ts"
      via: "useAutosaveAvaliacao reused with teste='redacao'"
      pattern: "useAutosaveAvaliacao"
    - from: "src/features/avaliacao/services/redacaoService.ts"
      to: "avaliar-redacao-cultural"
      via: "supabase.functions.invoke"
      pattern: "invoke\\('avaliar-redacao-cultural'"
    - from: "src/features/avaliacao/components/AvaliacaoContainer.tsx"
      to: "/candidato/redacao/:candidaturaId"
      via: "handleOpenTeste redacao branch navigate"
      pattern: "redacao"
---

<objective>
Build the candidate-facing essay layer: the editor screen (as one more teste card in the existing AvaliacaoContainer), the 3-band word counter, the informative cronômetro, the client Zod schema, the allowlist service, the container `redacao` branch, and the candidate route. Covers AVAL-05 (editor + autosave + word gating) and the candidate half of AVAL-06 (submit → EF invoke, neutral ack).

Purpose: This is "SjtCasoAbertoScreen again, essay flavor" — near-exact reuse of the Phase 11 textarea + word-count + autosave + irreversible-submit machinery, with the UI-SPEC copy/color divergences (3-band counter, "Enviar redação", neutral post-submit). The candidate never sees a verdict; the 3-color system is RH-only (Plan 05). This plan runs parallel to Plan 02 (zero file overlap — Plan 02 owns supabase/, this owns src/features/avaliacao + routes).

Output: 1 schema + 1 service + 3 components + the container branch + 1 route, flipping the Plan 01 RED scaffolds (RedacaoCounter, redacaoService, redacao-contract) GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-reda-o-cultural-revis-o-humana/13-PATTERNS.md
@.planning/phases/13-reda-o-cultural-revis-o-humana/13-UI-SPEC.md

<interfaces>
<!-- Reused as-is — do NOT re-author: -->
From src/features/avaliacao/hooks/useAutosaveAvaliacao.ts:
  useAutosaveAvaliacao({ candidaturaId, teste, upsert }) → { update, flushNow, status, locked }
From src/features/avaliacao/services/avaliacaoService.ts:
  upsertResposta(candidaturaId, teste, payload)  — the 30s-debounce DB writer the hook calls
  getAvaliacaoContext(...)  — allowlist read precedent (.select('id, cargo, ...'), .eq('status','active'))
<!-- Plan 02 contract this client posts against (shared-contract test, Pitfall 5): -->
EF avaliar-redacao-cultural body: { candidatura_id, pergunta_id, texto }  → neutral { ok: true }
</interfaces>

<!-- The near-exact template to copy (textarea + count + autosave + submit dialog + back-lock): -->
@src/features/avaliacao/components/SjtCasoAbertoScreen.tsx
<!-- Autosave affordance + glass shell reuse: -->
@src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx
<!-- Service error-class + EF-invoke + allowlist precedent: -->
@src/features/avaliacao/services/avaliacaoService.ts
@src/features/avaliacao/schemas/respostaAvaliacaoSchema.ts
<!-- Container branch wiring point + route block to mirror: -->
@src/features/avaliacao/components/AvaliacaoContainer.tsx
@src/router/routes.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Client schema + allowlist service (flip redacaoService + contract GREEN)</name>
  <files>src/features/avaliacao/schemas/redacaoSchema.ts, src/features/avaliacao/services/redacaoService.ts</files>
  <read_first>
    - src/features/avaliacao/schemas/respostaAvaliacaoSchema.ts (the .strict() no-score anti-tamper convention)
    - src/features/avaliacao/services/avaliacaoService.ts (the AvaliacaoServiceError class ~lines 33-48; the avaliarRedacao EF-invoke ~lines 297-322; the getAvaliacaoContext allowlist read ~lines 93-156 with .eq('status','active'))
    - .planning/phases/13-reda-o-cultural-revis-o-humana/13-RESEARCH.md §Code Examples (the REDACAO_CANDIDATO_ALLOWLIST string + the neutral-ack EF invoke)
    - src/features/avaliacao/services/__tests__/redacaoService.test.ts + src/features/avaliacao/__tests__/redacao-contract.test.ts (the Plan 01 RED scaffolds this task flips GREEN)
  </read_first>
  <behavior>
    - respostaRedacaoSchema rejects an injected `score`/`pontuacao` field (.strict()).
    - respostaRedacaoSchema accepts {candidatura_id (uuid), pergunta_id (uuid), texto (min 1)}.
    - getRedacaoContext reads perguntas_redacao with an explicit column allowlist + .eq('ativa', true) — never select('*').
    - getMinhasRedacoes (candidate own-row read) uses REDACAO_CANDIDATO_ALLOWLIST = 'id, pergunta_id, texto, word_count, submetida_em, status_analise' — never select('*'); excludes all verdict columns.
    - enviarRedacao invokes 'avaliar-redacao-cultural' with {candidatura_id, pergunta_id, texto} and returns the neutral ack (never a score); EF error → RedacaoServiceError('NETWORK_ERROR'); a 42501/403 back-lock maps to LOCKED.
    - the redacao-contract test: respostaRedacaoSchema.parse(clientBody) succeeds AND the same body shape matches the EF body keys (Pitfall 5 shared contract).
  </behavior>
  <action>Create `redacaoSchema.ts` with `respostaRedacaoSchema = z.object({candidatura_id: z.string().uuid(), pergunta_id: z.string().uuid(), texto: z.string().min(1)}).strict()` (AVAL-06; the 200-500 word gate is UI-side — the client never posts a score). Create `redacaoService.ts` (AVAL-05/AVAL-06 / per D-13): a `RedacaoServiceError` class mirroring AvaliacaoServiceError; `getRedacaoContext(candidaturaId)` reading perguntas_redacao via an explicit allowlist ('id, codigo, texto, valor_primario, ordem' or the columns the screen needs) + `.eq('ativa', true)` (mirror getAvaliacaoContext); `getMinhasRedacoes(candidaturaId)` reading redacoes_candidato with the constant `REDACAO_CANDIDATO_ALLOWLIST = 'id, pergunta_id, texto, word_count, submetida_em, status_analise'` (Pitfall 3 — EXCLUDES analise_ia/scores_dimensao/score_ponderado_0_100/classificacao_cor/red_flag_etico/flags/scores_humanos/notas_revisor/decisao_revisor); `enviarRedacao(body)` invoking `supabase.functions.invoke('avaliar-redacao-cultural', { body })` returning the neutral ack (never a score — RNF-07a), with the LOCKED/INVALID_INPUT/NETWORK_ERROR error map copied from the bigfiveService branch. This flips the Plan 01 redacaoService + redacao-contract RED tests GREEN.</action>
  <verify>
    <automated>npm run test:run -- redacaoService redacao-contract 2>&1 | tail -8 && grep -q "REDACAO_CANDIDATO_ALLOWLIST" src/features/avaliacao/services/redacaoService.ts && ! grep -q "select('\*')\|select(\"\*\")" src/features/avaliacao/services/redacaoService.ts</automated>
  </verify>
  <done>redacaoSchema (.strict()) + redacaoService (allowlist reads, no select('*'), EF neutral-ack invoke) authored; Plan 01 redacaoService + redacao-contract tests GREEN; allowlist constant present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RedacaoCounter (3-band) + RedacaoCronometro (flip RedacaoCounter GREEN)</name>
  <files>src/features/avaliacao/components/RedacaoCounter.tsx, src/features/avaliacao/components/RedacaoCronometro.tsx</files>
  <read_first>
    - src/features/avaliacao/components/SjtCasoAbertoScreen.tsx (countWords lines 55-59 — reuse verbatim; the wordHelper/band logic to extract)
    - .planning/phases/13-reda-o-cultural-revis-o-humana/13-UI-SPEC.md §Color (the 3-band counter rule) + §Copywriting Contract (the exact counter helper copy)
    - src/features/avaliacao/components/__tests__/RedacaoCounter.test.tsx (the Plan 01 RED scaffold this flips GREEN)
  </read_first>
  <behavior>
    - <200 words → text-white/60 muted + helper "{N} palavras — mínimo 200" + isValid=false.
    - 200-500 words → #35BFAD accent + helper "{N} palavras" + isValid=true.
    - >500 words → text-amber-300/80 + helper "{N} palavras — máximo 500" + isValid=false.
    - counter updates with a 200ms debounce (not on every keystroke synchronously).
    - cronômetro renders "Tempo nesta redação: mm:ss" elapsed, no countdown, no alarm color, increments via setInterval.
  </behavior>
  <action>Create `RedacaoCounter.tsx` (AVAL-05): extract `countWords` verbatim from SjtCasoAbertoScreen (trim → split /\s+/ → length); render the 3-band code-of-colors per UI-SPEC §Color (muted `<200` / `#35BFAD` `200-500` / `text-amber-300/80` `>500`) — these are MECHANICAL length guidance, NOT a verdict (RNF-07a: a short counter means "too short", not "you failed"; below-min is NEUTRAL muted, never alarm-red). 200ms debounce on the displayed count. Expose `isValid` (200≤count≤500) so the screen drives submit-disabled. Create `RedacaoCronometro.tsx` (AVAL-05): a small useEffect + setInterval elapsed counter rendering "Tempo nesta redação: {mm:ss}" — informative only, NO countdown, NO alarm color (no in-repo analog; trivial timer per PATTERNS No-Analog guidance). This flips the Plan 01 RedacaoCounter RED test GREEN.</action>
  <verify>
    <automated>npm run test:run -- RedacaoCounter 2>&1 | tail -6</automated>
  </verify>
  <done>RedacaoCounter renders 3 bands with the exact UI-SPEC copy + 200ms debounce + isValid; RedacaoCronometro is informative-only (no countdown/alarm); Plan 01 RedacaoCounter test GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: RedacaoEditorScreen + AvaliacaoContainer redacao branch + route</name>
  <files>src/features/avaliacao/components/RedacaoEditorScreen.tsx, src/features/avaliacao/components/AvaliacaoContainer.tsx, src/router/routes.tsx</files>
  <read_first>
    - src/features/avaliacao/components/SjtCasoAbertoScreen.tsx (the full template: ScreenShell glass lines 272-286; autosave wiring lines 106-118; textarea + counter + submit AlertDialog lines 216-266; AutosaveIndicator lines 61-86; locked state lines 150-166)
    - src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx (the AutosaveAffordance accent #35BFAD Check, lines 79-103)
    - src/features/avaliacao/components/AvaliacaoContainer.tsx (handleOpenTeste lines 306-314 — the branch needing a 'redacao' case; the card-status switch lines 58-79)
    - src/router/routes.tsx (the /caso candidate route block ~lines 217-224 to mirror for /candidato/redacao/:candidaturaId)
    - .planning/phases/13-reda-o-cultural-revis-o-humana/13-UI-SPEC.md §Copywriting Contract (candidate essay screen copy table + submit confirmation + back-lock copy)
  </read_first>
  <action>Create `RedacaoEditorScreen.tsx` (AVAL-05/AVAL-06 / per D-13) by copying SjtCasoAbertoScreen's structure: the `ScreenShell` glass (BackgroundImage background="gradient" + max-w-2xl + GlassPanel variant="white" blur="xl" — do NOT author a new shell, D-27); reuse `useAutosaveAvaliacao({ candidaturaId, teste: 'redacao', upsert })` with the stable useCallback upsert closure (the essay is one more teste key); render the seeded pergunta prompt verbatim at 16px/1.5, the Textarea (onChange→update, onBlur→flushNow) with the placeholder copy, the `RedacaoCounter` (Task 2) driving submit-disabled, the `RedacaoCronometro` (Task 2), the AutosaveAffordance ("Salvo automaticamente" #35BFAD Check), and the submit `AlertDialog` (title "Enviar redação?", body "Você ainda pode revisá-la até concluir esta etapa.", confirm "Enviar" / cancel "Continuar escrevendo"). On confirm: call `redacaoService.enviarRedacao` → on success show the neutral post-submit message "Resposta registrada. Você pode revisar até concluir a etapa." (NO score/feedback — RF-R-06). Multi-question: "Pergunta {n} de {total}", "Próxima pergunta" CTA, "Redações concluídas." when all submitted. The `locked` state shows the neutral "Sua etapa avançou." copy (never an error toast — back-lock). All copy verbatim from UI-SPEC §Copywriting Contract. Then EDIT `AvaliacaoContainer.tsx` handleOpenTeste: add a `redacao` branch that navigates to `/candidato/redacao/${candidaturaId}` (mirror the existing big_five/caso branches); ensure the redação teste card surfaces from `vaga.testes_aplicaveis` (tipo='redacao'). Then EDIT `routes.tsx`: add `{ path: '/candidato/redacao/:candidaturaId', element: <RoleGuard role="candidato"><RedacaoEditorScreen /></RoleGuard> }` mirroring the /caso block.</action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -q "/candidato/redacao/:candidaturaId" src/router/routes.tsx && grep -q "redacao" src/features/avaliacao/components/AvaliacaoContainer.tsx && grep -q "useAutosaveAvaliacao" src/features/avaliacao/components/RedacaoEditorScreen.tsx</automated>
  </verify>
  <done>RedacaoEditorScreen reuses the glass shell + useAutosaveAvaliacao(teste='redacao') + RedacaoCounter + submit dialog with neutral post-submit; container has the redacao branch; route registered; build exits 0; no score/color shown to candidate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate browser → redacoes_candidato read | Own-row read must use a column allowlist; RLS is row-level only and cannot hide verdict columns. |
| candidate browser → essay EF | Client posts only text; word-count gate is UI affordance, server is authoritative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Info disclosure | candidate read leaks verdict columns (RNF-07a) | mitigate | REDACAO_CANDIDATO_ALLOWLIST excludes analise_ia/scores/cor/red_flag_etico/revisor; verify asserts no select('*'); the candidate UI never renders a score/color/threshold. |
| T-13-03-02 | Tampering | client posts a forged score field | mitigate | respostaRedacaoSchema is .strict() — an injected score/pontuacao is rejected; the EF revalidates word_count + derives score server-side anyway. |
| T-13-03-03 | Information disclosure | back-lock surfaces an alarming error revealing pipeline state | accept | The 42501 back-lock maps to a neutral "Sua etapa avançou." message (reused Phase 11/12 pattern) — no score, no alarm; low risk. |
| T-13-03-SC | Tampering | npm web installs | accept | Zero net-new web packages — all UI primitives (textarea/alert-dialog/etc.) vendored since Phase 7 (UI-SPEC Registry Safety). No [ASSUMED]/[SUS]. |
</threat_model>

<verification>
- `npm run test:run -- redacaoService redacao-contract RedacaoCounter` GREEN (3 of the Plan 01 RED scaffolds flipped)
- `npm run build` exits 0
- grep confirms no `select('*')` on any candidate read; REDACAO_CANDIDATO_ALLOWLIST present
- route `/candidato/redacao/:candidaturaId` registered; AvaliacaoContainer has the redacao branch
- candidate UI renders NO score/color/threshold (manual code review of RedacaoEditorScreen — only the 3-band length counter, which is mechanical guidance)
- tsc baseline does not grow beyond the documented carryover
</verification>

<success_criteria>
- Candidate essay screen is one more teste card in AvaliacaoContainer (no new shell); reuses useAutosaveAvaliacao(teste='redacao')
- 3-band counter (muted/accent/amber) drives submit-disabled at the 200-500 hard bounds
- Client submit posts only {candidatura_id, pergunta_id, texto} (.strict()) and invokes avaliar-redacao-cultural for a neutral ack
- Candidate reads use the verdict-excluding allowlist; no select('*'); no score/color ever shown
- Plan 01 RED scaffolds redacaoService + redacao-contract + RedacaoCounter flipped GREEN
</success_criteria>

<output>
Create `.planning/phases/13-reda-o-cultural-revis-o-humana/13-03-SUMMARY.md` when done
</output>
