---
phase: 13-reda-o-cultural-revis-o-humana
plan: 04
type: execute
wave: 3
depends_on: [13-02, 13-03]
files_modified:
  - database.types.ts
  - .planning/phases/13-reda-o-cultural-revis-o-humana/13-VALIDATION.md
autonomous: false
requirements: [AVAL-05, AVAL-06, AVAL-07]

must_haves:
  truths:
    - "D-13 traceability: the 4 Phase-13 migrations are LIVE in PROD (3 tables + seed 11 perguntas + review-fields trigger + salvar_revisao_redacao RPC), the avaliar-redacao-cultural EF is deployed JWT-on, and the culture_fit_essay prompt row is verified-synced + is_active=true — the blocking PROD wave per CONTEXT."
    - "All 8 SQL smokes from 13-VALIDATION.md PASS live: em_progresso RLS, redacoes_candidato RLS (candidate SELECT own + client INSERT denied + RH SELECT/UPDATE), review-fields trigger RAISE, notas≥50 CHECK, decisao enum CHECK, seed count = 11, never-writes-candidaturas invariant."
    - "The culture_fit_essay prompt system_template is verified to contain the real 06 template + 3 few-shot inline (NOT a [SEED PLACEHOLDER]) BEFORE activation; if it is a stub, sync-prompts hydrates it first."
  artifacts:
    - path: "database.types.ts"
      provides: "regenerated types including perguntas_redacao + redacoes_candidato + redacoes_candidato_em_progresso"
      contains: "redacoes_candidato"
    - path: ".planning/phases/13-reda-o-cultural-revis-o-humana/13-VALIDATION.md"
      provides: "the 8 SQL smokes marked PASS with live evidence"
  key_links:
    - from: "prompt_versions.culture_fit_essay"
      to: "is_active=true"
      via: "verified-sync then activate (MCP execute_sql)"
      pattern: "is_active"
---

<objective>
[BLOCKING / non-autonomous] Apply the Phase-13 DB + EF + prompt changes to live PROD: the 4 migrations (via Supabase MCP apply_migration — the D-22 path that bypasses 42601), the avaliar-redacao-cultural EF deploy (JWT-on), the culture_fit_essay prompt verify-sync-activate, the db:types regen, and the 8 live SQL smokes from 13-VALIDATION.md.

Purpose: This is the orchestrator-run production wave — every step touches live Supabase infra (MCP execute_sql for migrations, CLI functions deploy, prompt_versions UPDATE). It is `autonomous: false` because the apply, the EF deploy, and the prompt activation are high-blast-radius PROD operations gated behind explicit operator authorization (the Phase 6/7/8/10/11/12 precedent). The prompt row is the one piece of runtime config not derivable from git — its system_template content MUST be verified (Open Question 1) before activation.

Output: 4 migrations live, EF deployed, prompt active, database.types.ts regenerated (committed at repo ROOT, not src/types/), 8 SQL smokes PASS in 13-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-reda-o-cultural-revis-o-humana/13-VALIDATION.md
@.planning/phases/13-reda-o-cultural-revis-o-humana/13-RESEARCH.md

<!-- The 4 migrations authored in Plan 02 (apply these, do not re-author): -->
@supabase/migrations/20260623100001_perguntas_redacao.sql
@supabase/migrations/20260623100002_redacoes_candidato_em_progresso.sql
@supabase/migrations/20260623100003_redacoes_candidato.sql
@supabase/migrations/20260623100004_salvar_revisao_redacao_rpc.sql
<!-- The EF to deploy (authored in Plan 02): -->
@supabase/functions/avaliar-redacao-cultural/index.ts
<!-- The prompt template whose content the live row must match: -->
@docs/conhecimento/prompts/templates/06-culture-fit-essay.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply the 4 migrations to live PROD via Supabase MCP apply_migration</name>
  <files>supabase/migrations/20260623100001_perguntas_redacao.sql, supabase/migrations/20260623100002_redacoes_candidato_em_progresso.sql, supabase/migrations/20260623100003_redacoes_candidato.sql, supabase/migrations/20260623100004_salvar_revisao_redacao_rpc.sql</files>
  <action>Apply each Plan-02 migration to live PROD via Supabase MCP apply_migration in order (the D-22 path: writes the version row, bypasses 42601 even with the $$ trigger/RPC bodies). This is operator-gated PROD infra — pause for authorization.</action>
  <what-built>The 4 Phase-13 migrations (3 tables + seed + review RPC) authored in Plan 02 are ready to apply to live PROD via Supabase MCP apply_migration (the D-22 path: writes the version row, bypasses 42601 even with the $$ trigger/RPC bodies).</what-built>
  <how-to-verify>
    1. Apply each migration to PROD via Supabase MCP `apply_migration` in order: 20260623100001_perguntas_redacao → 20260623100002_redacoes_candidato_em_progresso → 20260623100003_redacoes_candidato → 20260623100004_salvar_revisao_redacao_rpc.
    2. Confirm via `execute_sql`: `SELECT count(*) FROM perguntas_redacao` returns 11; `SELECT count(*) FILTER (WHERE is_padrao) FROM perguntas_redacao` returns 1.
    3. Confirm the 3 tables + the trigger `redacao_rh_only_review_fields` + the function `salvar_revisao_redacao` exist (`SELECT to_regclass('public.redacoes_candidato')`, `SELECT proname FROM pg_proc WHERE proname='salvar_revisao_redacao'`).
    4. If `db push --linked` is attempted instead and trips 42601, fall back to MCP apply_migration (CLAUDE.md §Commands workaround).
  </how-to-verify>
  <verify>SELECT count(*) FROM perguntas_redacao returns 11; redacoes_candidato + trigger + salvar_revisao_redacao exist.</verify>
  <done>All 4 migrations live in PROD; perguntas_redacao count = 11 (1 is_padrao); 3 tables + review trigger + RPC exist.</done>
  <resume-signal>Type "applied" once all 4 migrations are live and the count/existence checks pass, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Verify-sync + activate culture_fit_essay prompt + deploy the EF (JWT-on)</name>
  <files>docs/conhecimento/prompts/templates/06-culture-fit-essay.md, supabase/functions/avaliar-redacao-cultural/index.ts</files>
  <action>Verify the live culture_fit_essay system_template is the real 06 template + 3 few-shot (not [SEED PLACEHOLDER]) via MCP execute_sql; sync-prompts hydrate if stub; then activate is_active=true and deploy the avaliar-redacao-cultural EF JWT-on. Operator-gated PROD infra — pause for authorization.</action>
  <what-built>The culture_fit_essay prompt row exists in prompt_versions with is_active=false; the avaliar-redacao-cultural EF is built and ready to deploy. The prompt's system_template must be VERIFIED to contain the real 06 template + 3 few-shot (not the [SEED PLACEHOLDER]) before activation (Open Question 1 / Assumption A1 — the one runtime-config item not derivable from git).</what-built>
  <how-to-verify>
    1. VERIFY prompt content via `execute_sql`: `SELECT semver, is_active, model_id, length(system_template) AS sys_len, left(system_template, 60) AS head FROM prompt_versions WHERE call_type='culture_fit_essay'`. If `head` starts with "[SEED PLACEHOLDER]" or sys_len is tiny, the row is unsynced → run `scripts/sync-prompts.ts` (git→DB) to hydrate the real 06 template + 3 few-shot inline, then re-verify.
    2. ACTIVATE once verified: `UPDATE prompt_versions SET is_active=true WHERE call_type='culture_fit_essay' AND semver='1.0.0'` (and ensure no other culture_fit_essay row is active — the unique_active_per_type EXCLUDE constraint).
    3. DEPLOY the EF: `supabase functions deploy avaliar-redacao-cultural` (auto-bundles _shared; JWT-on, candidate-invoked like avaliar-redacao/submit-bigfive-final). Confirm an anon curl with no Authorization returns 401 (verify_jwt ON behavioral evidence — EF body never executes).
    4. Confirm the EF env reuses the existing secrets (ANTHROPIC_API_KEY, OPENAI_API_KEY, SUPABASE_*) — no new secret needed (RESEARCH Runtime State Inventory).
  </how-to-verify>
  <verify>prompt head is NOT [SEED PLACEHOLDER]; is_active=true (single active row); anon curl to the EF returns 401.</verify>
  <done>culture_fit_essay verified-synced + is_active=true; avaliar-redacao-cultural EF deployed JWT-on; anon curl → 401.</done>
  <resume-signal>Type "deployed" once the prompt is verified-synced + is_active=true and the EF returns 401 on anon curl, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Regenerate database.types.ts + run + record the 8 SQL smokes PASS</name>
  <files>database.types.ts, .planning/phases/13-reda-o-cultural-revis-o-humana/13-VALIDATION.md</files>
  <read_first>
    - .planning/phases/13-reda-o-cultural-revis-o-humana/13-VALIDATION.md (the 8 SQL smokes authored in Plan 01 — run each live via MCP execute_sql with set_config request.jwt.claims fixtures)
    - CLAUDE.md §Commands (npm run db:types — regenerate database.types.ts at the repo ROOT, never src/types/, never edit by hand)
  </read_first>
  <action>After the migrations are live (Tasks 1-2), run `npm run db:types` to regenerate `database.types.ts` at the repo ROOT — it must now include perguntas_redacao, redacoes_candidato, redacoes_candidato_em_progresso, and the salvar_revisao_redacao function signature (AVAL-05/06/07). Then run the 8 SQL smokes from 13-VALIDATION.md live via MCP execute_sql, each using a disposable fixture + `set_config('request.jwt.claims', ...)` to simulate candidate vs RH roles (the Phase 8/11 sanctioned pattern, ROLLBACK-free cleanup): (1) em_progresso RLS — candidate R/W own only, RH SELECT, cross-candidate DENY; (2) redacoes_candidato candidate SELECT own only; (3) redacoes_candidato client INSERT denied (WITH CHECK false → 42501); (4) redacoes_candidato RH SELECT/UPDATE allowed; (5) review-fields trigger — RH UPDATE of `texto` → RAISE; (6) notas_revisor < 50 chars → CHECK violation; (7) decisao_revisor out-of-enum → CHECK violation; (8) salvar_revisao_redacao never writes candidaturas + seed count = 11. Record each smoke's live result (PASS + evidence) in 13-VALIDATION.md. Commit database.types.ts + the updated runbook via `git -c core.hooksPath=/dev/null`.</action>
  <verify>
    <automated>grep -q "redacoes_candidato" database.types.ts && grep -q "perguntas_redacao" database.types.ts && grep -ci "PASS" .planning/phases/13-reda-o-cultural-revis-o-humana/13-VALIDATION.md</automated>
  </verify>
  <done>database.types.ts regenerated at ROOT with the 3 new tables + RPC; all 8 SQL smokes PASS recorded with live evidence in 13-VALIDATION.md; committed via hook bypass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live PROD (MCP/CLI) | Migration apply, EF deploy, prompt activation are high-blast-radius — gated behind explicit operator authorization. |
| prompt_versions row → live AI behavior | Activating is_active=true changes runtime scoring; an unsynced stub template scores with a weak prompt. |
| anon caller → deployed EF | JWT-on deploy must reject unauthenticated calls at the gateway. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Tampering | migration applied with wrong role string silently denies RH reads | mitigate | Smokes 4 + 5 simulate the RH JWT (role='administrador') and assert SELECT/UPDATE succeed — a role-string mismatch fails the smoke before go-live. |
| T-13-04-02 | Integrity | prompt activated while system_template is a [SEED PLACEHOLDER] stub | mitigate | Blocking verify step reads length(system_template)+head before activation; sync-prompts hydrates if stub (Open Question 1 / A1). |
| T-13-04-03 | Elevation | EF deployed with verify_jwt OFF (anon can score) | mitigate | Deploy JWT-on; anon curl must return 401 (gateway evidence) before resume-signal. |
| T-13-04-04 | Repudiation | RNF-07a — confirm no path writes candidaturas | mitigate | Smoke 8 asserts salvar_revisao_redacao + the EF never write candidaturas; status stays pendente_humano. |
| T-13-04-SC | Tampering | EF deploy bundles npm packages | accept | Zero net-new packages; identical pins already green in PROD (RESEARCH Package Legitimacy Audit). No [ASSUMED]/[SUS]. |
</threat_model>

<verification>
- 4 migrations live (count=11, 3 tables + trigger + RPC exist)
- culture_fit_essay prompt verified-synced (no [SEED PLACEHOLDER]) + is_active=true; no second active row
- avaliar-redacao-cultural EF deployed JWT-on; anon curl → 401
- database.types.ts regenerated at ROOT with the 3 new tables + salvar_revisao_redacao
- 8 SQL smokes PASS in 13-VALIDATION.md with live evidence
- live EF essay-scored end-to-end is DEFERRED to human UAT (Phase 8/10/11 precedent — requires real candidate + invited candidatura + Anthropic call)
</verification>

<success_criteria>
- All 4 migrations applied to live PROD via MCP (no 42601)
- Prompt verified-synced + activated; EF deployed JWT-on
- db:types regenerated; 8 SQL smokes PASS
- No candidaturas write anywhere (RNF-07a holds live)
</success_criteria>

<output>
Create `.planning/phases/13-reda-o-cultural-revis-o-humana/13-04-SUMMARY.md` when done
</output>
