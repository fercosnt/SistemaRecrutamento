---
phase: 13-reda-o-cultural-revis-o-humana
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/essay-schemas.ts
  - supabase/functions/_shared/__tests__/essay-schemas.test.ts
  - supabase/functions/avaliar-redacao-cultural/_local/compute-score.ts
  - supabase/functions/avaliar-redacao-cultural/_local/compute-score.test.ts
  - src/features/avaliacao/components/__tests__/RedacaoCounter.test.tsx
  - src/features/avaliacao/services/__tests__/redacaoService.test.ts
  - src/features/avaliacao/__tests__/redacao-contract.test.ts
  - src/features/triagem/components/__tests__/RedacaoOverrideForm.test.tsx
  - src/features/triagem/components/__tests__/RedacaoSidebar.test.tsx
  - .planning/phases/13-reda-o-cultural-revis-o-humana/13-VALIDATION.md
autonomous: true
requirements: [AVAL-05, AVAL-06, AVAL-07]

must_haves:
  truths:
    - "D-13 traceability: every Phase-13 surface (EssayScoringV1 schema, computeScoreAndCors, candidate counter/service/contract, RH override/sidebar) has a calibrated FAILING test before its implementation lands — the smoke-runtime gate established in Phase 4.1."
    - "EssayScoringV1 schema is authored verbatim from PRD §8.4 (D1-D4 enum, .length(4), red_flag_etico boolean) on npm:zod@3.25.76/v4 — NOT the drifted canonical CultureFitEssaySchema."
    - "computeScoreAndCors is authored verbatim from PRD §8.3 — equal weights ×20, 3 caps (red_flag_etico→30, D1≤2→50, insufficient upstream), 3-color per-vaga threshold, tempo<90 flag."
  artifacts:
    - path: "supabase/functions/_shared/essay-schemas.ts"
      provides: "EssayScoringV1Schema + DimensionScoreSchema (PRD §8.4 verbatim, /v4 namespace)"
      contains: "EssayScoringV1Schema"
    - path: "supabase/functions/avaliar-redacao-cultural/_local/compute-score.ts"
      provides: "computeScoreAndCors + normalizeForHash (PRD §8.3 verbatim, deterministic)"
      exports: ["computeScoreAndCors", "normalizeForHash"]
    - path: "supabase/functions/avaliar-redacao-cultural/_local/compute-score.test.ts"
      provides: "table-driven deno test for caps + 3-color"
    - path: ".planning/phases/13-reda-o-cultural-revis-o-humana/13-VALIDATION.md"
      provides: "SQL-smoke runbook + Wave-0 coverage matrix"
  key_links:
    - from: "supabase/functions/_shared/essay-schemas.ts"
      to: "npm:zod@3.25.76/v4"
      via: "import { z } from 'npm:zod@3.25.76/v4'"
      pattern: "zod@3\\.25\\.76/v4"
---

<objective>
Author the Wave-0 RED test battery + the two load-bearing CONTRACTS every downstream Plan 13 builds against: the `EssayScoringV1` Zod schema (PRD §8.4) and the deterministic `computeScoreAndCors` scoring pipeline (PRD §8.3). Plus the SQL-smoke runbook (13-VALIDATION.md) that the [BLOCKING] PROD apply wave (Plan 04) executes.

Purpose: Phase 4.1 established the smoke-runtime gate — every surface must have a calibrated FAILING test before implementation. This plan front-loads the two pure, fully-testable artifacts (schema + scoring math) AND scaffolds RED tests for the EF/UI surfaces that Plans 02/03/05 implement. The schema and compute-score are interface-first: they are the contracts the EF (Plan 02) and the RH override recompute (Plan 05) import directly — no scavenger hunt.

Output: 2 implemented contract files (essay-schemas.ts GREEN, compute-score.ts GREEN) + 7 RED scaffolds (calibrated to FAIL until Plans 02/03/05 land their implementations) + 13-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-reda-o-cultural-revis-o-humana/13-CONTEXT.md
@.planning/phases/13-reda-o-cultural-revis-o-humana/13-RESEARCH.md
@.planning/phases/13-reda-o-cultural-revis-o-humana/13-PATTERNS.md

<interfaces>
<!-- The EssayScoringV1 shape and compute-score signature ARE this plan's deliverable. -->
<!-- Source of truth: docs/prds/m2-funil-rh/PRD-redacao-fit-cultural.md §8.3 + §8.4. -->
<!-- Read those PRD line ranges; they ship the exact code to transcribe. -->

EssayScoringV1 (PRD §8.4, lines 673-707): object with
  preprocessing_check { word_count:int, detected_writing_style: enum['formal','informal','mixed','outro'], style_neutralized_in_scoring: literal(true) },
  dimension_scores: array(DimensionScoreSchema).length(4),
  overall_score: number 0-100,
  qualitative_summary: string min50 max500,
  recommendation: enum['strong_fit','good_fit','neutral','weak_fit','misfit'],
  red_flag_etico: boolean,
  bias_audit { formality_did_not_affect_score:bool, regional_markers_treated_as_neutral:bool, grammar_errors_did_not_affect_content_score:bool }
DimensionScoreSchema: { dimension: enum['D1','D2','D3','D4'], dimension_name:string,
  cited_evidence: array({text:string min1, location:string}).max(2), reasoning: string min20,
  score: union(int 1-5, literal('insufficient_evidence')),
  level: enum['exemplary','proficient','developing','basic','inadequate','insufficient_evidence'] }

computeScoreAndCors(parsed, threshold{vermelho_max,amarelo_max}, wordCount, tempoSegundos)
  → { scoreGeral, classificacaoCor: 'verde'|'amarelo'|'vermelho', flags: string[], redFlagEtico: boolean }
normalizeForHash(texto) → lowercase + strip punctuation + collapse whitespace
</interfaces>

<!-- Green-EF zod /v4 precedent the schema import must clone (NOT plain npm:zod): -->
@supabase/functions/_shared/analise-schemas.ts
<!-- The drifted canonical schema to NOT copy (Pitfall 7): -->
@docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author EssayScoringV1 schema (GREEN) + RED schema test</name>
  <files>supabase/functions/_shared/essay-schemas.ts, supabase/functions/_shared/__tests__/essay-schemas.test.ts</files>
  <read_first>
    - docs/prds/m2-funil-rh/PRD-redacao-fit-cultural.md §8.4 (lines 669-707) — the verbatim schema to transcribe
    - supabase/functions/_shared/analise-schemas.ts (header + the `npm:zod@3.25.76/v4` import line) — the /v4 precedent
    - docs/conhecimento/prompts/templates/00-shared-zod-schemas.ts (CultureFitEssaySchema ~line 250) — the DRIFTED schema to NOT copy (Pitfall 7: it has dimension:z.string() not enum, 1..6 dims not .length(4), lacks red_flag_etico)
  </read_first>
  <behavior>
    - Valid EssayScoringV1 object with exactly 4 dimension_scores → .parse() succeeds.
    - Object with 3 or 5 dimension_scores → .parse() THROWS (.length(4) enforced).
    - Object missing red_flag_etico → .parse() THROWS (required boolean — load-bearing for the cap).
    - dimension value 'D5' or arbitrary string → THROWS (z.enum(['D1','D2','D3','D4'])).
    - score === 'insufficient_evidence' (literal) accepted in the union; score === 6 rejected (max 5).
    - style_neutralized_in_scoring === false → THROWS (z.literal(true)).
  </behavior>
  <action>Create `_shared/essay-schemas.ts` transcribing the PRD §8.4 `DimensionScoreSchema` + `EssayScoringV1Schema` + `type EssayScoringV1` VERBATIM (AVAL-06 / per D-13 binding-PRD decision). Import `z` from `npm:zod@3.25.76/v4` (the `/v4` namespace is load-bearing — the SDK helpers `require("zod/v4")`; a v3-namespace schema crashes the real Anthropic call — Pitfall 2/7). Copy the header doc-comment convention from `analise-schemas.ts` (verbatim-copy rule: docs/ is NOT in the EF deploy bundle so the schema is copied into `_shared/`). Do NOT branch or extend `avaliacao-schemas.ts` (that is the SJT schema on plain v3). Author the deno test asserting the six behaviors above against the schema. The test imports the schema and runs under `deno test`; it must PASS (GREEN — this is a real contract, not a stub).</action>
  <verify>
    <automated>deno test supabase/functions/_shared/__tests__/essay-schemas.test.ts</automated>
  </verify>
  <done>essay-schemas.ts exports EssayScoringV1Schema + EssayScoringV1 type; the `/v4` import is present; deno test GREEN (valid parses, ≠4 dims rejected, missing red_flag_etico rejected).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author computeScoreAndCors + normalizeForHash (GREEN) + table-driven RED test</name>
  <files>supabase/functions/avaliar-redacao-cultural/_local/compute-score.ts, supabase/functions/avaliar-redacao-cultural/_local/compute-score.test.ts</files>
  <read_first>
    - docs/prds/m2-funil-rh/PRD-redacao-fit-cultural.md §8.3 (lines 604-667) — the verbatim computeScoreAndCors + normalizeForHash to transcribe
    - supabase/functions/_shared/essay-schemas.ts (the EssayScoringV1 type from Task 1 — compute-score imports this type)
  </read_first>
  <behavior>
    - 4 valid dims all score 5, no red flag → scoreGeral = 100, cor = 'verde' (default threshold 40/64).
    - 4 dims all score 3 → scoreGeral = 60, cor = 'amarelo' (≤64, >40).
    - 4 dims all score 2 → scoreGeral = 40, cor = 'vermelho' (≤vermelho_max).
    - red_flag_etico=true with otherwise-high dims → scoreGeral capped at MIN(score,30), flag 'red_flag_etico', cor = 'vermelho'.
    - D1 score = 2 (others 5) → scoreGeral capped at MIN(score,50), flag 'situacao_generica_ou_inventada', cor = 'vermelho'.
    - one dim 'insufficient_evidence' → excluded from mean (validDims=3), flag '<dim>_insufficient_evidence'.
    - tempoSegundos < 90 → flag 'tempo_anormalmente_curto' added.
    - per-vaga threshold {vermelho_max:30, amarelo_max:55} respected over the default.
    - normalizeForHash('Olá, Mundo!  TESTE') === 'olá mundo teste' (lowercase + strip punctuation + collapse ws + trim).
    - flags array is deduped (Set).
  </behavior>
  <action>Create `avaliar-redacao-cultural/_local/compute-score.ts` transcribing the PRD §8.3 `computeScoreAndCors` and `normalizeForHash` VERBATIM (AVAL-06). The caps and 3-color are DETERMINISTIC EF code, NOT the LLM (anti-pattern: never let the LLM decide color/caps). Import the `EssayScoringV1` type from `../../_shared/essay-schemas.ts`. Author a TABLE-DRIVEN deno test covering all behaviors above (one case per row). The test must PASS GREEN.</action>
  <verify>
    <automated>deno test supabase/functions/avaliar-redacao-cultural/_local/compute-score.test.ts</automated>
  </verify>
  <done>compute-score.ts exports computeScoreAndCors + normalizeForHash; both caps + 3-color + tempo flag + dedup verified by table-driven deno test GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: RED scaffolds for EF/UI surfaces + 13-VALIDATION.md runbook</name>
  <files>src/features/avaliacao/components/__tests__/RedacaoCounter.test.tsx, src/features/avaliacao/services/__tests__/redacaoService.test.ts, src/features/avaliacao/__tests__/redacao-contract.test.ts, src/features/triagem/components/__tests__/RedacaoOverrideForm.test.tsx, src/features/triagem/components/__tests__/RedacaoSidebar.test.tsx, .planning/phases/13-reda-o-cultural-revis-o-humana/13-VALIDATION.md</files>
  <read_first>
    - .planning/phases/13-reda-o-cultural-revis-o-humana/13-RESEARCH.md §Validation Architecture (the Phase Requirements → Test Map + Wave 0 Gaps tables — they enumerate every scaffold)
    - .planning/phases/13-reda-o-cultural-revis-o-humana/13-UI-SPEC.md §Color (the candidate 3-band counter rule + RH 3-color triage + sidebar default filter/severity sort)
    - src/features/avaliacao/components/SjtCasoAbertoScreen.tsx (countWords lines 55-59 — the word-count primitive the counter reuses)
    - .planning/phases/12-big-five-devolutiva/12-VALIDATION.md (the SQL-smoke runbook format to mirror — MCP fixture + set_config request.jwt.claims)
  </read_first>
  <action>Author 5 RED Vitest scaffolds calibrated to FAIL with module-not-found (the implementations land in Plans 03/05) — this is the smoke-runtime gate (Phase 4.1 precedent): (1) `RedacaoCounter.test.tsx` — 3-band gating: <200 muted + submit-disabled, 200-500 accent #35BFAD + enabled, >500 amber + disabled (UI-SPEC §Color); (2) `redacaoService.test.ts` — assert the candidate read uses the explicit allowlist string `'id, pergunta_id, texto, word_count, submetida_em, status_analise'` and NEVER `select('*')` (Pitfall 3 / RNF-07a — assert the projection, not JSX); (3) `redacao-contract.test.ts` — the client submit body `{candidatura_id, pergunta_id, texto}` must `.parse()` against the EF body Zod (Pitfall 5 shared-contract test); (4) `RedacaoOverrideForm.test.tsx` — 4 BARS sliders recompute composite=mean×20 + color on change; notas <50 → Salvar disabled; decisao radio aprovado/reprovado/duvida (AVAL-07); (5) `RedacaoSidebar.test.tsx` — severity sort vermelho→amarelo→verde + default filter vermelho+amarelo (AVAL-07). Then author `13-VALIDATION.md`: the SQL-smoke runbook (MCP fixtures with `set_config request.jwt.claims`) covering — em_progresso RLS (candidate R/W own only, RH read), redacoes_candidato RLS (candidate SELECT own + client INSERT denied WITH CHECK false + RH SELECT/UPDATE), the BEFORE UPDATE review-fields trigger (RH UPDATE texto → RAISE), notas_revisor ≥50 CHECK, decisao_revisor enum CHECK, seed count assertion, and the never-writes-candidaturas invariant — PLUS the Wave-0 coverage matrix from RESEARCH §Validation Architecture. Mark each smoke with its target plan (apply-wave Plan 04 runs them live).</action>
  <verify>
    <automated>npm run test:run -- RedacaoCounter redacaoService redacao-contract RedacaoOverrideForm RedacaoSidebar 2>&1 | grep -E "No test files|Cannot find|FAIL|failed" || echo "EXPECTED: RED (module-not-found until Plans 03/05)"</automated>
  </verify>
  <done>5 Vitest scaffolds exist and FAIL with module-not-found (calibrated RED — not syntax errors); 13-VALIDATION.md contains the full SQL-smoke runbook + Wave-0 coverage matrix mapping each smoke to its apply-wave plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ PRD → EF `_shared/` | Schema must be COPIED into the deploy bundle; docs/ is not deployed. Drift between PRD §8.4 and the copy is the integrity risk. |
| LLM output → `EssayScoringV1Schema.parse()` | The AI returns untrusted structured output; the schema is the validation boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | essay-schemas.ts copied from drifted canonical (Pitfall 7) | mitigate | Transcribe PRD §8.4 verbatim; test asserts .length(4) + required red_flag_etico + D1-D4 enum — the drifted CultureFitEssaySchema fails these. |
| T-13-01-02 | Tampering | compute-score caps decided by LLM instead of EF code | mitigate | computeScoreAndCors is pure deterministic TS authored from PRD §8.3; table-driven test pins every cap/color. The LLM only emits raw 1-5 scores. |
| T-13-01-03 | Information disclosure | candidate read leaks verdict columns (RNF-07a) | mitigate | redacaoService RED test asserts the allowlist projection string excludes analise_ia/scores/cor/red_flag_etico/revisor — fails on select('*'). |
| T-13-01-SC | Tampering | npm/deno EF imports (zod/v4) | accept | Zero net-new packages — `npm:zod@3.25.76/v4` is the exact pin deployed green in analise-candidato-individual/gerar-devolutiva-bigfive (RESEARCH Package Legitimacy Audit). No [ASSUMED]/[SUS] package introduced. |
</threat_model>

<verification>
- `deno test supabase/functions/_shared/__tests__/essay-schemas.test.ts` GREEN
- `deno test supabase/functions/avaliar-redacao-cultural/_local/compute-score.test.ts` GREEN
- 5 Vitest scaffolds present and RED (module-not-found, calibrated)
- 13-VALIDATION.md exists with the SQL-smoke runbook + coverage matrix
- `npm run build` exits 0 (scaffolds compile under TS strict even while failing at runtime)
- LGPD-04: no forbidden product-language strings ("teste psicológico" etc.) in any new file (the seed texts / prompts are candidate-visible — grep guard scope)
</verification>

<success_criteria>
- EssayScoringV1Schema authored verbatim from PRD §8.4 on `npm:zod@3.25.76/v4`, GREEN deno test
- computeScoreAndCors + normalizeForHash authored verbatim from PRD §8.3, GREEN table-driven deno test
- 5 RED Vitest scaffolds calibrated to fail until Plans 03/05 implement
- 13-VALIDATION.md SQL-smoke runbook ready for the [BLOCKING] apply wave (Plan 04)
- tsc baseline does not grow (scaffolds are typed)
</success_criteria>

<output>
Create `.planning/phases/13-reda-o-cultural-revis-o-humana/13-01-SUMMARY.md` when done
</output>
