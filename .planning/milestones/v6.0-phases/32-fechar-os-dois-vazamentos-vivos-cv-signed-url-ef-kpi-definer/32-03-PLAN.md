---
phase: 32-fechar-os-dois-vazamentos-vivos-cv-signed-url-ef-kpi-definer
plan: 03
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - supabase/migrations/20260715000002_funil_kpis_and_rh_le_historico.sql
autonomous: true
requirements: [SEG-02]

must_haves:
  truths:
    - "SEG-02: funil_kpis(p_vaga_id uuid DEFAULT NULL) RETURNS jsonb is LANGUAGE plpgsql SECURITY DEFINER SET search_path='' with every object schema-qualified, internally scoped to v.created_by = auth.uid() (administrador bypasses via auth.jwt() app_metadata.role), REVOKE ALL FROM PUBLIC + GRANT EXECUTE TO authenticated"
    - "SEG-02: funil_kpis returns PII-free aggregates ONLY (median-time-per-stage via percentile_cont(0.5) over LEAD-derived deltas excluding NULL last-transition deltas; raw stage→stage conversion counts; current volume by etapa_atual) — no ator, no candidatos identity, no candidate id/name/email"
    - "SEG-02: rh_le_historico is DROP+CREATEd with the WR-04 vaga-scoped predicate (admin bypass OR rh AND candidatura_id IN candidaturas JOIN vagas WHERE created_by=auth.uid()); candidato_le_proprio_historico is untouched and NO INSERT policy is added (authored, applied in 32-04)"
  artifacts:
    - path: "supabase/migrations/20260715000002_funil_kpis_and_rh_le_historico.sql"
      provides: "funil_kpis SECURITY DEFINER RPC (median/conversion/volume, PII-safe jsonb) + rh_le_historico WR-04 hardening; no BEGIN/COMMIT wrapper"
      contains: "SECURITY DEFINER"
  key_links:
    - from: "funil_kpis"
      to: "historico_candidatura → candidaturas → vagas"
      via: "JOIN chain scoped by v.created_by = auth.uid() (admin bypass)"
      pattern: "created_by = "
    - from: "rh_le_historico"
      to: "candidaturas JOIN vagas.created_by"
      via: "WR-04 join (historico has no direct vaga_id)"
      pattern: "candidatura_id IN"
---

<objective>
Close leak 2 (SEG-02 — KPIs + audit trail). Author one migration composing two shipped-pattern copies: the `funil_kpis` SECURITY DEFINER RPC (the vaga-scoped, PII-free aggregate channel Phase 34's dashboard consumes) and the WR-04 hardening of `rh_le_historico` (the role-only policy Phase 24 explicitly deferred and never swept — the second live leak).

Both are verbatim copies: the DEFINER/jsonb/REVOKE-GRANT header from `get_avaliacao_status`, and the WR-04 join predicate from `redacao_rh_select`. The failure mode to avoid (P24 lesson) is inventing a new predicate instead of copying the one that already passes its smoke; and (Pitfall 2) leaving names unqualified under `SET search_path=''`.

Purpose: `funil_kpis` + the hardened `rh_le_historico` are belt-and-suspenders — the RPC returns PII-free aggregates (the RPC does not depend on the policy), and the policy closes direct client SELECTs on the audit trail.
Output: 1 new migration (authored, applied in 32-04). The behavioral proof (recruiter A sees no vaga-B numbers; zero PII; RLS deny) runs in 32-04 via seg32_smokes.sql.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-fechar-os-dois-vazamentos-vivos-cv-signed-url-ef-kpi-definer/32-PATTERNS.md

# Analogs to clone (exact line ranges in 32-PATTERNS.md):
@supabase/migrations/20260712100003_funil12_get_avaliacao_status.sql
@supabase/migrations/20260706110004_sec05_08_vaga_scope.sql
@supabase/migrations/20260607000006_rls_policies_m2_backbone.sql

<interfaces>
<!-- KPI inputs (all PII-safe columns) + the current role-only policy to replace. -->

Aggregation source (allowlist — NEVER select ator or any candidatos column):
  historico_candidatura(candidatura_id, etapa_de, etapa_para, criado_em)   -- NOT ator, NOT auto_rejeitado in the projection
  candidaturas(id, vaga_id, etapa_atual)
  vagas(id, created_by)
  etapa_processo enum: inscricao, triagem, avaliacao_assincrona, entrevista_online,
    entrevista_presencial, decisao_final, aprovado, rejeitado

funil_kpis contract (signature Claude's discretion within this shape; smoke calls it):
  public.funil_kpis(p_vaga_id uuid DEFAULT NULL) RETURNS jsonb
  scope: v_is_admin := (auth.jwt() #>> '{app_metadata,role}') = 'administrador'; v_uid := auth.uid()
         WHERE (v_is_admin OR v.created_by = v_uid) AND (p_vaga_id IS NULL OR v.id = p_vaga_id)
  jsonb keys (shape is discretion): median_time_per_stage, conversion_stage_to_stage, volume_by_stage

Current role-only policy to REPLACE (20260607000006:73-77):
  rh_le_historico USING ((select auth.jwt() #>> '{app_metadata,role}') IN ('rh','administrador'))   -- the leak

WR-04 predicate to COPY (redacao_rh_select, sec05_08_vaga_scope.sql:94-104):
  admin bypass OR (rh AND <id> IN (SELECT c.id FROM candidaturas c JOIN vagas v ON v.id=c.vaga_id WHERE v.created_by=(select auth.uid())))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: funil_kpis SECURITY DEFINER RPC (median / conversion / volume, PII-safe)</name>
  <files>supabase/migrations/20260715000002_funil_kpis_and_rh_le_historico.sql</files>
  <read_first>
    - supabase/migrations/20260712100003_funil12_get_avaliacao_status.sql:56-116 — DEFINER/jsonb header discipline, `SET search_path=''`, body scope-guard using auth.uid()/auth.jwt() (survive DEFINER — read the request GUC), REVOKE ALL FROM PUBLIC + GRANT EXECUTE TO authenticated (:115-116)
    - 32-RESEARCH.md § Code Examples #3 — the full CTE shape (scoped_hist → deltas via LEAD → median via percentile_cont; conversion counts; volume) + § Pitfall 2 (qualify everything) + § Pitfall 5 (exclude NULL LEAD deltas)
    - 32-PATTERNS.md § …_funil_kpis_and_rh_le_historico.sql (Analog A)
  </read_first>
  <action>
    Create supabase/migrations/20260715000002_funil_kpis_and_rh_le_historico.sql and author `CREATE OR REPLACE FUNCTION public.funil_kpis(p_vaga_id uuid DEFAULT NULL) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path = '' AS $$ … $$;`. In the DECLARE: `v_is_admin boolean := (auth.jwt() #>> '{app_metadata,role}') = 'administrador'; v_uid uuid := auth.uid();`. Body via CTEs — every object schema-qualified (`public.`, `auth.`) because `search_path=''` (Pitfall 2): (1) `scoped_hist` = `public.historico_candidatura h JOIN public.candidaturas c ON c.id=h.candidatura_id JOIN public.vagas v ON v.id=c.vaga_id WHERE (v_is_admin OR v.created_by = v_uid) AND (p_vaga_id IS NULL OR v.id = p_vaga_id)`, selecting ONLY `h.candidatura_id, h.etapa_de, h.etapa_para, h.criado_em, c.vaga_id` (NEVER `h.ator` or any candidatos column — PII-safe by construction); (2) `deltas` = `etapa_para AS stage, (LEAD(criado_em) OVER (PARTITION BY candidatura_id ORDER BY criado_em) - criado_em) AS dwell` — the dwell is NULL for each candidatura's last (in-progress) transition; (3) `median` = `percentile_cont(0.5) WITHIN GROUP (ORDER BY EXTRACT(EPOCH FROM dwell)) FROM deltas WHERE dwell IS NOT NULL GROUP BY stage` (excludes NULL deltas — Pitfall 5); (4) `conversion` = raw `etapa_de → etapa_para` counts `WHERE etapa_de IS NOT NULL GROUP BY etapa_de, etapa_para` (raw count in P32; cohort refinement K4 deferred to P34); (5) `volume` = current distribution over `public.candidaturas c JOIN public.vagas v` by `c.etapa_atual`, same owner-scope + p_vaga_id narrow (per RESEARCH Assumption A1 — current etapa_atual distribution). Assemble one `jsonb_build_object('median_time_per_stage', …, 'conversion_stage_to_stage', …, 'volume_by_stage', …)` with `COALESCE(…, '{}'::jsonb / '[]'::jsonb)` guards; `RETURN` it. After the function: `REVOKE ALL ON FUNCTION public.funil_kpis(uuid) FROM PUBLIC;` and `GRANT EXECUTE ON FUNCTION public.funil_kpis(uuid) TO authenticated;`. NO outer `BEGIN;…COMMIT;` (D-22). Author only — applied in 32-04.
  </action>
  <verify>
    <automated>F=supabase/migrations/20260715000002_funil_kpis_and_rh_le_historico.sql; test -f "$F" && grep -q "CREATE OR REPLACE FUNCTION public.funil_kpis(p_vaga_id uuid DEFAULT NULL)" "$F" && grep -q "SECURITY DEFINER" "$F" && grep -q "SET search_path = ''" "$F" && grep -q "percentile_cont" "$F" && grep -q "LEAD(criado_em)" "$F" && grep -q "REVOKE ALL ON FUNCTION public.funil_kpis(uuid) FROM PUBLIC" "$F" && grep -q "GRANT EXECUTE ON FUNCTION public.funil_kpis(uuid) TO authenticated" "$F" && grep -vE '^\s*--' "$F" | grep -c "\.ator" | (read n; [ "$n" -eq 0 ] && echo "no ator projection (PII-safe body)")</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists with `funil_kpis(p_vaga_id uuid DEFAULT NULL) RETURNS jsonb`, `SECURITY DEFINER`, `SET search_path = ''`, all objects schema-qualified.
    - Median uses `LEAD(criado_em)` + `percentile_cont(0.5)` and excludes NULL deltas (`WHERE dwell IS NOT NULL`).
    - Executable body (comments filtered) selects NO `ator` column and NO `candidatos` table — aggregates only.
    - `REVOKE ALL … FROM PUBLIC` + `GRANT EXECUTE … TO authenticated` present.
    - No `BEGIN;`/`COMMIT;` wrapper. File authored only (not applied here).
  </acceptance_criteria>
  <done>funil_kpis DEFINER RPC authored: search_path='', schema-qualified, median via LEAD+percentile_cont (NULL deltas excluded), raw conversion, current volume, PII-safe jsonb, REVOKE/GRANT; applied in 32-04.</done>
</task>

<task type="auto">
  <name>Task 2: rh_le_historico WR-04 hardening (append to the same migration)</name>
  <files>supabase/migrations/20260715000002_funil_kpis_and_rh_le_historico.sql</files>
  <read_first>
    - supabase/migrations/20260706110004_sec05_08_vaga_scope.sql:94-104 — the redacao_rh_select WR-04 join form to copy verbatim; :126-131 — where sec05_08 explicitly deferred rh_le_historico (this is the sweep)
    - supabase/migrations/20260607000006_rls_policies_m2_backbone.sql:61-77 — current role-only rh_le_historico (:73-77) + candidato_le_proprio_historico (:61-70, keep) + trigger note (:79-81, no INSERT policy needed)
    - 32-PATTERNS.md § …_funil_kpis_and_rh_le_historico.sql (Analog B)
  </read_first>
  <action>
    Append to supabase/migrations/20260715000002_funil_kpis_and_rh_le_historico.sql: `DROP POLICY IF EXISTS rh_le_historico ON public.historico_candidatura;` then `CREATE POLICY rh_le_historico ON public.historico_candidatura FOR SELECT TO authenticated USING ( (select auth.jwt() #>> '{app_metadata,role}') = 'administrador' OR ((select auth.jwt() #>> '{app_metadata,role}') = 'rh' AND candidatura_id IN (SELECT c.id FROM public.candidaturas c JOIN public.vagas v ON v.id = c.vaga_id WHERE v.created_by = (select auth.uid()))) );`. `historico_candidatura` has NO direct `vaga_id`, so scope via `candidatura_id → candidaturas → vagas.created_by` (WR-04 join form). Keep the `(select auth.jwt() …)` / `(select auth.uid())` subquery-wrapping verbatim (planner-cache idiom). Do NOT touch `candidato_le_proprio_historico` (candidate own-row audit trail stays intact). Add NO INSERT policy — `historico_candidatura` is written only by the `avancar_etapa` trigger inside the authorized `candidaturas` UPDATE txn. NO `BEGIN;…COMMIT;` wrapper. Author only — applied in 32-04.
  </action>
  <verify>
    <automated>F=supabase/migrations/20260715000002_funil_kpis_and_rh_le_historico.sql; grep -q "DROP POLICY IF EXISTS rh_le_historico ON public.historico_candidatura" "$F" && grep -q "candidatura_id IN" "$F" && grep -q "JOIN public.vagas v ON v.id = c.vaga_id" "$F" && grep -q "v.created_by = (select auth.uid())" "$F" && ! grep -q "candidato_le_proprio_historico" "$F" && ! grep -qi "INSERT" "$F" && echo "WR-04 hardening present; candidato policy untouched; no INSERT policy"</automated>
  </verify>
  <acceptance_criteria>
    - `rh_le_historico` is DROP+CREATEd with the WR-04 predicate: admin bypass OR (rh AND `candidatura_id IN (candidaturas JOIN vagas WHERE created_by = auth.uid())`).
    - The subquery-wrapping `(select auth.jwt() …)` / `(select auth.uid())` is preserved verbatim.
    - `candidato_le_proprio_historico` is NOT referenced (untouched); no INSERT policy is added.
    - Both objects (RPC + policy) live in the single migration `20260715000002_funil_kpis_and_rh_le_historico.sql`, no BEGIN/COMMIT wrapper.
  </acceptance_criteria>
  <done>rh_le_historico DROP+CREATEd with the WR-04 vaga-scoped predicate; candidato_le_proprio_historico untouched; no INSERT policy; appended to migration B; applied in 32-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH browser → RPC `funil_kpis` | untrusted `p_vaga_id`; owner-scope must be re-imposed inside the DEFINER body |
| RH browser → direct SELECT on `historico_candidatura` | RLS `rh_le_historico` is the only gate on the audit trail |
| DEFINER function → schema resolution | `search_path=''` closes the hijack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Information Disclosure | funil_kpis leaks candidate PII | mitigate | CTEs select only candidatura_id/etapa_*/criado_em/vaga_id — never ator/candidatos (Task 1); smoke (d) walks the jsonb in 32-04 |
| T-32-05 | Information Disclosure | recruiter A sees vaga-B KPIs | mitigate | internal `WHERE (v_is_admin OR v.created_by = v_uid)` scope (Task 1); smoke (b) in 32-04 |
| T-32-06 | Information Disclosure | recruiter A SELECTs vaga-B historico | mitigate | rh_le_historico WR-04 vaga-scoped predicate (Task 2); smoke (c) RLS deny in 32-04 |
| T-32-07 | Elevation of Privilege | search_path hijack of the DEFINER fn | mitigate | `SET search_path=''` + fully-qualified `public.`/`auth.` names (Task 1) |
| T-32-SC | Tampering (supply-chain) | package installs | accept | zero new packages; SQL-only migration, no install task |
</threat_model>

<verification>
- Structural greps confirm: DEFINER + `search_path=''` + `percentile_cont`/`LEAD` + REVOKE/GRANT (Task 1); WR-04 join + untouched candidate policy + no INSERT policy (Task 2).
- PII tripwire: executable body (comments filtered) selects no `ator` column.
- Behavioral proof (recruiter A sees no vaga-B numbers; zero PII in the jsonb; historico RLS deny; admin-sees-all + p_vaga_id narrows) runs in 32-04 via seg32_smokes.sql — the load-bearing gate.
- `npm run lint` unaffected (no TS change); `database.types.ts` regen is a 32-04 step.
</verification>

<success_criteria>
SEG-02 primitive authored:
- `funil_kpis` DEFINER RPC — vaga-scoped by construction, PII-free jsonb aggregates (median/conversion/volume), REVOKE/GRANT locked.
- `rh_le_historico` hardened to the WR-04 vaga-scoped predicate (the P24-deferred sweep), candidate policy intact, no INSERT policy.
Both in one migration, no BEGIN/COMMIT wrapper, ready for the 32-04 MCP apply + version-row reconcile + behavioral smokes.
</success_criteria>

<output>
Create `.planning/phases/32-fechar-os-dois-vazamentos-vivos-cv-signed-url-ef-kpi-definer/32-03-SUMMARY.md` when done
</output>
