---
phase: 32-fechar-os-dois-vazamentos-vivos-cv-signed-url-ef-kpi-definer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/get-curriculo-url/index.test.ts
  - supabase/tests/seg32_smokes.sql
  - src/__tests__/guards/no-service-role-src.grep.test.ts
  - src/features/vagas/services/__tests__/cvUploadService.test.ts
autonomous: true
requirements: [SEG-01, SEG-02]

must_haves:
  truths:
    - "SEG-01/SEG-02: the Phase 32 acceptance harness exists and is RED — the deno EF test, the JWT-impersonated seg32_smokes.sql, and the client guard/service tests all encode the vaga-scoped deny contract BEFORE any implementation (RED→GREEN discipline)"
    - "The deno EF test fails only because supabase/functions/get-curriculo-url/index.ts does not exist yet (module-resolution error), not a syntax error — it becomes GREEN in 32-02"
    - "seg32_smokes.sql contains the 5 labeled behavioral assertions (a)-(e) over a disposable, ROLLBACK-free, fixed-UUID fixture (2 recruiters + distinct vagas + 1 candidatura with a CV path + historico rows for a median)"
    - "The extended guard + updated cvUploadService.test.ts are RED because the client still calls createSignedUrl — they become GREEN in 32-02"
  artifacts:
    - path: "supabase/functions/get-curriculo-url/index.test.ts"
      provides: "deno unit test: 401 / 403-role / 403-owner / 404-NULL-CV / 200 branches (authoritative cross-recruiter 403 gate — no recruiter PROD account for a live curl)"
      contains: "loadHandler"
    - path: "supabase/tests/seg32_smokes.sql"
      provides: "JWT-impersonated behavioral smoke, assertions (a)-(e), disposable fixture, ROLLBACK-free cleanup"
      contains: "request.jwt.claims"
    - path: "src/__tests__/guards/no-service-role-src.grep.test.ts"
      provides: "extended bundle tripwire: no client createSignedUrl over curriculos survives in src/"
      contains: "createSignedUrl"
    - path: "src/features/vagas/services/__tests__/cvUploadService.test.ts"
      provides: "getSignedUrl block updated to mock supabase.functions.invoke instead of storage.createSignedUrl"
      contains: "functions"
  key_links:
    - from: "supabase/functions/get-curriculo-url/index.test.ts"
      to: "supabase/functions/get-curriculo-url/index.ts"
      via: "await import('./index.ts') in loadHandler"
      pattern: "loadHandler"
    - from: "supabase/tests/seg32_smokes.sql"
      to: "request.jwt.claims GUC"
      via: "set_config to impersonate recruiter A vs vaga B"
      pattern: "set_config\\('request.jwt.claims'"
---

<objective>
Author the complete Phase 32 acceptance harness FIRST, in a RED state, so both implementation plans (32-02 EF/policy/rewire, 32-03 RPC/RLS) build against a fixed, load-bearing behavioral contract rather than exploring. This is the Wave 0 requirement set from 32-VALIDATION.md.

The load-bearing gate for the whole phase is the JWT-impersonated behavioral smoke (`seg32_smokes.sql`) — P24 proved structural `pg_policies`/grep checks pass while a real leak remains (a REVOKE no-op, an OR-defeat). This plan writes that smoke plus the deno EF unit test (the authoritative cross-recruiter 403 gate, because there is no `role='recrutador'` PROD account for a live curl — RESEARCH Pitfall 6) plus the two client-side test surfaces.

Purpose: RED-first discipline — no implementation exists yet, so every test here MUST fail for a KNOWN, intended reason. 32-02 greens the four code-level tests; 32-04 applies the migrations + deploys the EF and greens the DB smoke.
Output: 2 new test files + 2 modified test files. Nothing under `src/` or `supabase/functions/*/index.ts` is implemented in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-fechar-os-dois-vazamentos-vivos-cv-signed-url-ef-kpi-definer/32-VALIDATION.md
@.planning/phases/32-fechar-os-dois-vazamentos-vivos-cv-signed-url-ef-kpi-definer/32-PATTERNS.md

# Analogs to clone (read the exact line ranges named in 32-PATTERNS.md):
@supabase/functions/submit-candidatura/index.test.ts
@supabase/tests/funil12_status_rpc_smoke.sql
@supabase/tests/sec02_smokes.sql
@src/__tests__/guards/no-service-role-src.grep.test.ts
@src/features/vagas/services/__tests__/cvUploadService.test.ts

<interfaces>
<!-- Contracts the harness encodes. The EF (32-02) and RPC (32-03) implement against these. -->

EF get-curriculo-url — request/response envelope (mirror comparativo-candidatos):
  Request  body: { candidatura_id: string }   // NEVER a raw path — forgeable
  Response ok:   { ok: true, signedUrl: string }        status 200
  Response err:  { ok: false, error_code: ErrorCode, message: string }
    ErrorCode = "UNAUTHORIZED"(401) | "FORBIDDEN"(403) | "VALIDATION"(400) | "NOT_FOUND"(404) | "SERVER_ERROR"(500)
  handler(req: Request, deps: { supabaseAdmin, supabaseUser }): Promise<Response>   // exported, testable

funil_kpis (32-03) — the smoke calls this signature:
  public.funil_kpis(p_vaga_id uuid DEFAULT NULL) RETURNS jsonb   // SECURITY DEFINER, aggregates ONLY

historico_candidatura columns available to the smoke (database.types.ts:1856-1886):
  candidatura_id, etapa_de, etapa_para, criado_em, ator, auto_rejeitado
candidaturas: etapa_atual, status, vaga_id, curriculo_url (nullable), created_at
vagas: id, created_by
etapa_processo enum: inscricao, triagem, avaliacao_assincrona, entrevista_online,
  entrevista_presencial, decisao_final, aprovado, rejeitado
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deno EF unit test (RED — target EF does not exist yet)</name>
  <files>supabase/functions/get-curriculo-url/index.test.ts</files>
  <read_first>
    - supabase/functions/submit-candidatura/index.test.ts — the exact harness to clone: `loadHandler()` (:135-153), `makeChainable` query-builder mock (:61-75), `makeMockSupabaseUser` (:123-133), `makeRequest` + assertion shape (:171-179)
    - 32-PATTERNS.md § `get-curriculo-url/index.test.ts` — spells out how to route mocks: from("usuarios_rh")→role row, from("candidaturas")→{curriculo_url,vaga_id}, from("vagas")→{created_by}, stub storage.from("curriculos").createSignedUrl→{data:{signedUrl},error:null}
    - 32-RESEARCH.md § Code Examples #1 (the handler shape the test targets) + § Validation Architecture Test Map
  </read_first>
  <action>
    Create supabase/functions/get-curriculo-url/index.test.ts cloning submit-candidatura/index.test.ts. Import `{ handler }` from "./index.ts" via a `loadHandler()` (`await import("./index.ts")`) — index.ts is guarded by `import.meta.main` so importing will NOT boot a server (it will fail to RESOLVE because the file is absent; that IS the intended RED). Build a `makeMockSupabaseAdmin({ role, cand, vaga, signedUrl })` helper using the `makeChainable` idiom that routes `from("usuarios_rh")` to a role row (`ativo:true, deleted_at:null`), `from("candidaturas")` to `{ curriculo_url, vaga_id }`, `from("vagas")` to `{ created_by }`, and stubs `storage.from("curriculos").createSignedUrl(path, 60)` to `{ data: { signedUrl }, error: null }`. Build `makeMockSupabaseUser(user|null)` for `auth.getUser()`. Cover exactly five branches with `assertEquals` on `res.status` and the parsed `error_code`/`signedUrl`: (1) no session → 401 UNAUTHORIZED; (2) authed `candidato` (role row absent → dbRole null) → 403 FORBIDDEN; (3) authed `rh` NOT owning the vaga (`vaga.created_by !== user.id`) → 403 FORBIDDEN — this is the authoritative cross-recruiter deny gate (RESEARCH Pitfall 6: no recruiter PROD account for a live curl); (4) candidatura missing or `curriculo_url` NULL → 404 NOT_FOUND; (5) owner `rh` (and separately `administrador` bypassing ownership) → 200 with a `signedUrl` string. Assert the `administrador` case does NOT read `vagas` (ownership bypass). Do NOT implement index.ts — this task authors the failing test only.
  </action>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/get-curriculo-url 2>&1 | grep -Eiq "module not found|cannot (find|resolve)|no such file|index.ts" && echo "RED-as-expected: EF missing"</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/functions/get-curriculo-url/index.test.ts exists and encodes all 5 branches (401 / 403-role / 403-owner / 404-NULL / 200).
    - `deno test` FAILS with a module-resolution error naming ./index.ts (RED because the EF is not authored yet), NOT a TypeScript/syntax error in the test itself.
    - The test imports `handler` (not a booted server) via `loadHandler()`; mocks are injected as `deps` (no network).
    - No file under supabase/functions/get-curriculo-url/index.ts is created by this task.
  </acceptance_criteria>
  <done>index.test.ts authored with all 5 branches; deno test fails only on the missing ./index.ts module (documented RED, GREEN in 32-02).</done>
</task>

<task type="auto">
  <name>Task 2: seg32_smokes.sql — JWT-impersonated behavioral smoke (authored, not applied)</name>
  <files>supabase/tests/seg32_smokes.sql</files>
  <read_first>
    - supabase/tests/funil12_status_rpc_smoke.sql — disposable fixture (:41-88), impersonate+assert (:94-107), walk-the-jsonb for PII (:109-123), ROLLBACK-free cleanup (:155-160)
    - supabase/tests/sec02_smokes.sql:26-92 — impersonate-and-deny form (42501 OR 0 rows = PASS; a returned value = FAIL)
    - 32-PATTERNS.md § seg32_smokes.sql — the 5 assertions (a)-(e) and the fixture shape (2 recruiters owning distinct vagas A/B, 1 candidatura on B's vaga with a CV path, a couple historico rows for a median)
    - 32-RESEARCH.md § Code Examples #3 (funil_kpis shape the smoke calls) + Pitfall 4/5
  </read_first>
  <action>
    Create supabase/tests/seg32_smokes.sql cloning the funil12_status_rpc_smoke.sql / sec02_smokes.sql idiom. Under `RESET ROLE`, build a disposable, idempotent, fixed-UUID fixture (pre-DELETE children then rows; real rows NEVER deleted): recruiter A + recruiter B as distinct owners, vagaA (`created_by`=A), vagaB (`created_by`=B), one candidatura on vagaB with a non-null `curriculo_url` path, and 2-3 `historico_candidatura` rows for that candidatura so `funil_kpis` has a non-empty median (direct INSERT into historico_candidatura is acceptable ONLY inside this privileged disposable fixture — it is not product code; the M6 no-direct-INSERT invariant governs product code only), **AND a disposable `storage.objects` row for the vagaB candidate's CV: `bucket_id='curriculos'`, `name` = the candidate's own-folder path `{candidato_user_id}/{fixed-uuid}.pdf` which is ALSO exactly the value stored in that candidatura's `curriculo_url` (owner = the candidate's `user_id`). This storage row is what assertion (a) reads directly — without it there is nothing for the Storage RLS to allow/deny.** Gate the fixture with `set_config('smoke.ready', 'y'|'n')` and a `RAISE NOTICE 'SEG-32 SKIP'` on build failure. Then, under `SET ROLE authenticated`, impersonate recruiter A via `set_config('request.jwt.claims', jsonb_build_object('sub', <A>, 'role','authenticated','app_metadata', jsonb_build_object('role','rh')))` and encode the labeled assertions, each `RAISE NOTICE 'PASS (x) …'` on the secure outcome and `RAISE EXCEPTION` on a leak: **(a) THE load-bearing Storage-policy proof (SEG-01 success-criterion #1, "verificado por smoke comportamental, não por inspeção de pg_policies"): a DIRECT impersonated query `SELECT count(*) FROM storage.objects WHERE bucket_id='curriculos' AND name = <the fixture CV path>` — under recruiter A's `rh` JWT it MUST return 0 rows (post-Migration-A the RH role-only branch is REMOVED → RLS denies), and as a POSITIVE CONTROL impersonating the OWNING candidate (`sub` = the candidate's user_id, `app_metadata.role`='candidato') the SAME query MUST return 1 row (the candidate own-folder branch is intact). This exercises the ACTUAL `storage.objects` bucket RLS that Migration A changes — it is NOT a reconstruction against `candidaturas`/`vagas`, and NOT the EF (which uses service_role and bypasses RLS entirely). The deno test is the EF-level twin; this assertion is the DB-policy twin.** (b) `public.funil_kpis()` as recruiter A returns NO numbers attributable to vagaB (aggregates scoped to A's owned vagas); (c) a direct `SELECT … FROM public.historico_candidatura` for vagaB's candidatura as recruiter A returns 0 rows / 42501 (RLS deny); (d) walk the `funil_kpis` jsonb and RAISE EXCEPTION if any key/path contains candidate identity (`ator`, candidate id, name, email) — PII-free by construction; (e) impersonate `administrador` and assert admin sees all AND `funil_kpis(p_vaga_id => vagaB)` narrows to that vaga. End with ROLLBACK-free cleanup (`set_config('request.jwt.claims','')`, `RESET ROLE`, DELETE the fixed-UUID fixture, clear `smoke.ready`). This file is authored now and RUN in 32-04 after apply — do not attempt to apply it here.
  </action>
  <verify>
    <automated>test -f supabase/tests/seg32_smokes.sql && grep -c "RAISE NOTICE 'PASS" supabase/tests/seg32_smokes.sql | (read n; [ "$n" -ge 5 ] && echo "5+ labeled assertions present") && grep -q "set_config('request.jwt.claims'" supabase/tests/seg32_smokes.sql && echo "impersonation idiom present" && grep -qE "FROM storage.objects" supabase/tests/seg32_smokes.sql && grep -q "bucket_id" supabase/tests/seg32_smokes.sql && echo "direct storage.objects RLS assertion present (SEG-01 behavioral proof)"</automated>
  </verify>
  <acceptance_criteria>
    - supabase/tests/seg32_smokes.sql exists with ≥5 `RAISE NOTICE 'PASS (a..e)'` assertion outcomes and matching `RAISE EXCEPTION` leak branches.
    - **Assertion (a) is a DIRECT `SELECT count(*) FROM storage.objects WHERE bucket_id='curriculos' AND name=<path>` (NOT a reconstruction against candidaturas/vagas, NOT the EF): recruiter-A `rh` JWT → 0 rows (deny, post-Migration-A); owning-candidate JWT → 1 row (own-folder branch intact). The fixture INSERTs the matching `storage.objects` row. This is the behavioral proof of the bucket-policy change (the migration-text grep is NOT sufficient — P24 REVOKE-no-op class).**
    - Fixture uses fixed UUIDs, idempotent pre-DELETE, `smoke.ready` gating, and ROLLBACK-free cleanup that deletes ONLY the fixed-UUID fixture rows (incl. the `storage.objects` row).
    - Impersonation uses `set_config('request.jwt.claims', …)` + `SET ROLE authenticated`; assertion (e) also impersonates `administrador`.
    - The file is NOT applied/run in this plan (behavioral run is the 32-04 phase gate).
  </acceptance_criteria>
  <done>seg32_smokes.sql authored with 5 assertions (a)-(e), disposable fixture, ROLLBACK-free cleanup; behavioral run deferred to 32-04.</done>
</task>

<task type="auto">
  <name>Task 3: extend bundle guard + update cvUploadService.test.ts (RED until 32-02 rewire)</name>
  <files>src/__tests__/guards/no-service-role-src.grep.test.ts, src/features/vagas/services/__tests__/cvUploadService.test.ts</files>
  <read_first>
    - src/__tests__/guards/no-service-role-src.grep.test.ts — `FORBIDDEN_TOKENS` (:50-61), `firstServiceRoleViolation` (:67-71), `collectFiles`/`isCommentLine` scaffold (:81-96), positive/negative contract `it()` cases (:125-151)
    - src/features/vagas/services/__tests__/cvUploadService.test.ts — `vi.hoisted` supabase mock (:23-39), `getSignedUrl` block T3.1/T3.2 (:230-260), Pitfall-7 console-spy path (:288-318)
    - 32-PATTERNS.md § no-service-role-src.grep.test.ts and § cvUploadService.test.ts (the exact mock swaps)
  </read_first>
  <action>
    (1) Extend src/__tests__/guards/no-service-role-src.grep.test.ts so a client-side `createSignedUrl` over the curriculos path cannot survive anywhere in `src/` (the EF is the only signer after Phase 32 — SEG-01 success criterion (e)). Add a `createSignedUrl` tripwire to `firstServiceRoleViolation` (either a new `FORBIDDEN_TOKENS` entry `{ name: 'createSignedUrl', re: /\bcreateSignedUrl\b/ }` or a focused positive assertion that `cvUploadService.ts` contains `functions.invoke('get-curriculo-url')` and NO `createSignedUrl` token). Add matching positive AND negative contract `it()` cases (mirror :125-151) so the guard is comment-aware and cannot self-trip. (2) Update src/features/vagas/services/__tests__/cvUploadService.test.ts: add a hoisted `mockInvoke = vi.fn()` and extend the `vi.mock('@/lib/supabase/client')` to expose `functions: { invoke: mockInvoke }` alongside `storage`. Rewrite the `getSignedUrl` block (T3.1 happy / T3.2 error) to call `getSignedUrl(candidaturaId)` and assert `mockInvoke` was called with `'get-curriculo-url'` and `{ body: { candidatura_id: candidaturaId } }`, returning `data.signedUrl`; T3.2 → `mockInvoke.mockResolvedValue({ data: null, error: {...} })` still throws `UPLOAD_FAILED`. Update the Pitfall-7 console-spy path (:299,318) to drive `getSignedUrl` through the invoke mock. Both surfaces are RED now because cvUploadService.ts still calls `createSignedUrl` — do NOT touch cvUploadService.ts in this plan (32-02 greens both).
  </action>
  <verify>
    <automated>npm run test:run -- src/__tests__/guards/no-service-role-src.grep.test.ts src/features/vagas/services/__tests__/cvUploadService.test.ts 2>&1 | grep -Eiq "fail|✕|×" && echo "RED-as-expected (guard scan + getSignedUrl block) until 32-02 rewire"</automated>
  </verify>
  <acceptance_criteria>
    - The guard's `firstServiceRoleViolation`/token unit-contract `it()` cases (function-level, comment-aware) PASS; the `collectFiles('src')` scan case FAILS because cvUploadService.ts still contains `createSignedUrl` (intended RED).
    - cvUploadService.test.ts `getSignedUrl` T3.1/T3.2 + Pitfall-7 path assert `functions.invoke('get-curriculo-url', { body: { candidatura_id } })`; they FAIL now because the service still calls `createSignedUrl` (intended RED — GREEN in 32-02).
    - No production file under `src/features/vagas/services/cvUploadService.ts` is modified by this task.
    - The RED is documented (right-reason failures), not a syntax error.
  </acceptance_criteria>
  <done>Guard extended with the createSignedUrl-over-curriculos tripwire; cvUploadService.test.ts getSignedUrl block mocks functions.invoke; both RED for the intended reason (GREEN in 32-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH browser → EF `get-curriculo-url` | untrusted `candidatura_id` crosses here; auth header is the only identity |
| RH browser → RPC `funil_kpis` | untrusted `p_vaga_id`; scope must be re-imposed server-side |
| RH browser → direct SELECT on `historico_candidatura` | RLS is the only gate on the audit trail |
| Build output → shipped bundle | service_role / client `createSignedUrl` must never reach `src/` |

## STRIDE Threat Register (encoded by this harness — proven in 32-02/32-04)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure / Elevation | recruiter A reads vaga-B CV | mitigate | deno test branch (3) 403-owner + smoke (a) — authored here |
| T-32-02 | Elevation of Privilege | candidato/anon reaches EF | mitigate | deno test branches (1) 401 / (2) 403-role — authored here |
| T-32-04 | Information Disclosure | funil_kpis leaks candidate PII | mitigate | smoke (d) walks the jsonb for `ator`/id/name/email — authored here |
| T-32-05 | Information Disclosure | recruiter A sees vaga-B KPIs | mitigate | smoke (b) — authored here |
| T-32-06 | Information Disclosure | recruiter A SELECTs vaga-B historico | mitigate | smoke (c) RLS deny (42501 / 0 rows) — authored here |
| T-32-08 | Information Disclosure | service_role / client createSignedUrl in bundle | mitigate | extended `no-service-role-src` guard — authored here |
| T-32-SC | Tampering (supply-chain) | npm/deno installs | accept | zero new packages; no install task in this plan |
</threat_model>

<verification>
- `deno test … get-curriculo-url` fails with module-resolution (RED — EF absent).
- `grep` confirms seg32_smokes.sql has ≥5 labeled assertions + impersonation idiom (behavioral run deferred to 32-04).
- `npm run test:run` on the guard + cvUploadService test shows intended RED (right-reason failures) until 32-02.
- `npm run lint` (tsc) stays green — these are test files; no production type changes.
</verification>

<success_criteria>
All four Wave 0 test surfaces from 32-VALIDATION.md exist and fail for their KNOWN, intended reasons:
- deno EF test RED (EF missing) → GREEN in 32-02
- guard scan + cvUploadService `getSignedUrl` block RED (client still signs) → GREEN in 32-02
- seg32_smokes.sql authored (5 assertions a-e, disposable fixture) → RUN GREEN in 32-04
No production code under `src/` or `supabase/functions/*/index.ts` is written in this plan. The intended end state is a documented RED harness — do NOT "fix" the RED by reverting.
</success_criteria>

<output>
Create `.planning/phases/32-fechar-os-dois-vazamentos-vivos-cv-signed-url-ef-kpi-definer/32-01-SUMMARY.md` when done
</output>
