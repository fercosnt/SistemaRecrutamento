---
phase: 31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil
plan: 03
type: execute
wave: 2
depends_on: [31-02]
files_modified:
  - src/features/triagem/components/RejeitarCandidaturaDialog.tsx
  - src/features/triagem/components/__tests__/RejeitarCandidaturaDialog.test.tsx
  - src/features/triagem/components/RetrocederCandidaturaDialog.tsx
autonomous: true
requirements: [OPER-02, OPER-03]

must_haves:
  truths:
    - "OPER-02: RejeitarCandidaturaDialog is the single shared reject surface (motivo Select + Textarea + live counter) reused across Kanban/Hub/Comparativo — confirm disabled until motivo != null AND btrim(justificativa).length >= 50"
    - "OPER-02: the client counter mirrors the server btrim gate (counts .trim().length, not raw .length) — the server RPC RAISE remains the authority"
    - "OPER-03: RetrocederCandidaturaDialog lists ONLY earlier non-terminal stages (lower enum ordinal) and requires a non-empty justificativa (NOT ≥50) mirroring the trigger's regression RAISE"
  artifacts:
    - path: "src/features/triagem/components/RejeitarCandidaturaDialog.tsx"
      provides: "shared reject dialog wired to useRejeitarCandidatura, light-modal styling"
      exports: ["RejeitarCandidaturaDialog"]
    - path: "src/features/triagem/components/RetrocederCandidaturaDialog.tsx"
      provides: "regress dialog (destino Select prior-ordinal + required Textarea) wired to useUpdateCandidaturaEtapa"
      exports: ["RetrocederCandidaturaDialog"]
  key_links:
    - from: "RejeitarCandidaturaDialog"
      to: "useRejeitarCandidatura"
      via: "mutation on confirm"
      pattern: "useRejeitarCandidatura"
    - from: "RetrocederCandidaturaDialog"
      to: "useUpdateCandidaturaEtapa"
      via: "mutation with justificativa on confirm"
      pattern: "useUpdateCandidaturaEtapa"
---

<objective>
Build the two shared dialogs that every RH surface (Kanban card menu, HubCandidatoRH action row, ComparativoScreen) mounts: `RejeitarCandidaturaDialog` (motivo `<Select>` + justificativa `<Textarea>` + live ≥50 counter, light-modal surface) and the small `RetrocederCandidaturaDialog` (destino `<Select>` limited to earlier non-terminal stages + required non-empty justificativa). Both are the single source of truth for their copy + gate — no forked copies.

Purpose: The dialogs are the client mirror of the server gates (OPER-02 ≥50, OPER-03 regression justificativa). Building them once (this plan) lets 31-04/05 wire three surfaces without duplicating the gate logic.
Output: `RejeitarCandidaturaDialog.tsx` + its component test (Wave 0 gap) + `RetrocederCandidaturaDialog.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-UI-SPEC.md
@.planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-PATTERNS.md

<interfaces>
<!-- Verified against live source. -->

Analog: src/features/decisao/components/RegistrarDecisaoForm.tsx
  - AlertDialog imports :24-34 (AlertDialog, AlertDialogAction/Cancel/Content/Description/Footer/Header/Title/Trigger from '@/components/ui/alert-dialog')
  - the ≥50 counter + gate :81-88 (`tooShort = justificativa.length < MIN`; `canSubmit = motivo!=null && !tooShort && !submitting`), counter span + helper :133-162
  - submitting spinner :173-179 (`<Loader2 className="animate-spin" /> Rejeitando…`)
  - confirm via AlertDialogAction onClick :195-197

Consumed from 31-02:
  useRejeitarCandidatura() → mutate({candidaturaId, motivo, justificativa})  // src/features/triagem/hooks/useRejeitarCandidatura.ts
  useUpdateCandidaturaEtapa() → mutate({candidaturaId, novaEtapa, justificativa?})  // src/features/vagas/hooks/useCandidaturas.ts (extended)
  type MotivoRejeicaoRh, EtapaFunilM2, ETAPA_M2_LABELS, ETAPA_M2_OPTIONS  // src/features/triagem/services/triagemService.ts

Vendored primitives (all present in src/components/ui/): alert-dialog, select, textarea, label, dropdown-menu, badge
Icons (lucide-react): X, ArrowLeft, Undo2, Loader2

Motivo enum → pt-BR labels (31-UI-SPEC §Motivo table):
  perfil_desalinhado→"Perfil desalinhado com a vaga" · reprovado_avaliacao→"Reprovado na avaliação" · reprovado_entrevista→"Reprovado na entrevista" · nao_compareceu→"Não compareceu" · desistencia→"Desistência do candidato" · outro→"Outro"

Funnel order for regress ordinal (derive earlier-stage filter from this array): inscricao < triagem < avaliacao_assincrona < entrevista_online < entrevista_presencial < decisao_final (terminals aprovado/rejeitado excluded).
Copy strings: 31-UI-SPEC §Copywriting Contract (verbatim).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RejeitarCandidaturaDialog (shared, light-modal) + component test</name>
  <read_first>
    - src/features/decisao/components/RegistrarDecisaoForm.tsx (the counter + ≥50 gate + AlertDialog confirm analog)
    - .planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-UI-SPEC.md (§Component Inventory, §Interaction States, §Copywriting Contract, §Motivo table)
    - src/components/ui/select.tsx (Radix Select API for the motivo dropdown)
  </read_first>
  <behavior>
    - Idle (open): motivo Select placeholder "Selecione o motivo", empty Textarea, counter "0 / 50 mín." in text-muted-foreground, confirm DISABLED.
    - Motivo chosen but justificativa.trim().length < 50: helper "A justificativa precisa de pelo menos 50 caracteres." shown (text-destructive), confirm DISABLED.
    - motivo set AND justificativa.trim().length >= 50: helper hidden, confirm ENABLED.
    - Leading/trailing whitespace does NOT fake a pass (counter + gate use .trim().length).
    - Confirm click → calls the reject mutation with {candidaturaId, motivo, justificativa}; while pending, confirm + cancel disabled and confirm shows "Rejeitando…".
  </behavior>
  <action>
    Create `src/features/triagem/components/RejeitarCandidaturaDialog.tsx` (PascalCase named export). Props: `{ candidaturaId: string; nome: string; trigger: React.ReactNode; onRejected?: () => void }` (the trigger button is supplied by each host surface so styling matches its host; the dialog body renders on the light AlertDialogContent). Reuse the AlertDialog container + counter + submitting-spinner + confirm-via-AlertDialogAction from RegistrarDecisaoForm. Add a motivo `<Select>` (6 options → pt-BR labels from the table) with placeholder "Selecione o motivo"; state `motivo: MotivoRejeicaoRh | null`. Justificativa `<Textarea>` with `JUSTIFICATIVA_MIN = 50`. Counter renders `{justificativa.trim().length} / 50 mín.` — the ONE deviation from the analog is counting `.trim().length` (mirror the server btrim), NOT raw `.length`. Gate: `canSubmit = motivo !== null && justificativa.trim().length >= 50 && !isPending`. Wire confirm to `useRejeitarCandidatura().mutate(...)`; on success call `onRejected?.()` (host closes/refetches). Use EXACT copy from 31-UI-SPEC §Copywriting Contract (title "Rejeitar {nome}?", description, labels, counter, helper, confirm "Rejeitar candidato", cancel "Cancelar", toasts already handled in the hook).

    Styling (31-UI-SPEC §Design System / §Color): the dialog body is a LIGHT AlertDialogContent (`bg-background`) → use `text-foreground`/`text-muted-foreground`/`text-destructive` tokens, NOT the analog's dark-glass `text-white/80`. Confirm action is destructive: `bg-destructive text-destructive-foreground hover:bg-destructive/90`. Accessibility: label htmlFor on Select + Textarea, counter `aria-live="polite"`, helper tied via `aria-describedby`.

    Write the component test FIRST at `src/features/triagem/components/__tests__/RejeitarCandidaturaDialog.test.tsx` covering the 6 behaviors (mock useRejeitarCandidatura; use @testing-library/react as the sibling ComparativoScreen.test.tsx does). Assert: confirm disabled at idle; disabled with motivo set + 40 chars; disabled with 60 spaces (trim); enabled with motivo + 60 real chars; confirm click fires mutate with the right payload.
  </action>
  <verify>
    <automated>npx vitest run src/features/triagem/components/__tests__/RejeitarCandidaturaDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run RejeitarCandidaturaDialog` is GREEN with all 6 behavior cases.
    - Confirm gate uses `.trim().length >= 50` AND `motivo !== null` (verified: 60-spaces case keeps confirm disabled).
    - Copy strings match 31-UI-SPEC §Copywriting Contract verbatim (title, description, motivo/justificativa labels, counter, helper, confirm/cancel).
    - No "teste psicológico" language; no score/número in the copy (RNF-12a / RNF-07a).
    - Dialog body uses light shadcn tokens (bg-background/text-foreground), confirm uses bg-destructive.
  </acceptance_criteria>
  <done>Shared reject dialog built + tested; the single source of truth for the reject gate across all 3 surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: RetrocederCandidaturaDialog (regress — earlier stages, required justificativa)</name>
  <read_first>
    - src/features/decisao/components/RegistrarDecisaoForm.tsx (Textarea + AlertDialog gate shape)
    - src/features/triagem/services/triagemService.ts (EtapaFunilM2 :302, ETAPA_M2_LABELS :325, ETAPA_M2_OPTIONS :337 — funnel order)
    - .planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-UI-SPEC.md (§Interaction States "Retroceder dialog", §Copywriting Contract regress rows, §Color "Retroceder is neutral")
  </read_first>
  <action>
    Create `src/features/triagem/components/RetrocederCandidaturaDialog.tsx` (named export). Props: `{ candidaturaId: string; nome: string; etapaAtual: EtapaFunilM2; trigger: React.ReactNode; onDone?: () => void }`. Destino `<Select>` lists ONLY stages whose funnel-order index is LOWER than `etapaAtual`'s index and that are non-terminal (never `aprovado`/`rejeitado`, never a forward stage) — derive by filtering the funnel-order array (inscricao..decisao_final) by index; labels via `ETAPA_M2_LABELS`. Justificativa `<Textarea>` required NON-EMPTY (`justificativa.trim().length > 0`) — NOT the ≥50 floor (that is reject-only); counter-free helper. Gate: `canSubmit = destino !== null && justificativa.trim().length > 0 && !isPending`. Confirm wires `useUpdateCandidaturaEtapa().mutate({ candidaturaId, novaEtapa: destino, justificativa })` — the extended hook forwards justificativa so the trigger sees the fresh value; the trigger's `RAISE 'Regressão de etapa exige justificativa preenchida'` is the server authority. Neutral (non-destructive, non-accent) styling per §Color; on success call `onDone?.()`. Copy from §Copywriting Contract regress rows (title "Retroceder {nome} para uma etapa anterior?", destino label, justificativa label + placeholder, confirm "Retroceder etapa"). Success toast is handled by the extended useUpdateCandidaturaEtapa hook.
  </action>
  <verify>
    <automated>npx vitest run src/features/triagem</automated>
  </verify>
  <acceptance_criteria>
    - Component compiles and the triagem test suite stays GREEN (`npx vitest run src/features/triagem`).
    - Destino Select excludes the current stage, all forward stages, and both terminals (only strictly-earlier non-terminal stages appear).
    - Confirm disabled until destino set AND justificativa trimmed non-empty; wires useUpdateCandidaturaEtapa with the justificativa forwarded.
    - Neutral styling (no bg-destructive, no accent) per 31-UI-SPEC §Color.
  </acceptance_criteria>
  <done>Regress dialog built; the extended advance/regress hook carries the required justificativa so the trigger records the fresh text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → dialog state | Motivo + free-text justificativa entered here; the dialog gate is UX only — the server RPC/trigger is the authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02 | Tampering | client ≥50 gate | mitigate (UX mirror) | Counter + confirm-gate count `.trim().length` (mirror server btrim) so leading/trailing whitespace cannot fake a pass; the server RAISE stays authoritative |
| T-31-04 | RNF-07a / RNF-12a (copy) | dialog copy | mitigate | Copy never implies auto-reject by score and never says "teste psicológico"; verified against 31-UI-SPEC §Copywriting language invariants |
| T-31-SC | Tampering (supply-chain) | npm installs | accept | Zero new packages; all primitives already vendored in src/components/ui/ |
</threat_model>

<verification>
- `npx vitest run src/features/triagem` GREEN.
- Visual/interaction correctness is confirmed at the end-of-phase live UAT (deferred, per prior-phase precedent) — the component test covers the gate logic.
</verification>

<success_criteria>
- Both dialogs exist as named-export PascalCase components consuming the 31-02 hooks.
- Reject gate uses trimmed ≥50 + motivo; regress gate uses trimmed non-empty + earlier-stage destino.
- Copy matches the approved UI-SPEC.
</success_criteria>

<output>
Create `.planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-03-SUMMARY.md` when done.
</output>
