---
phase: 31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil
plan: 04
type: execute
wave: 3
depends_on: [31-03]
files_modified:
  - src/components/KanbanBoard.tsx
  - src/features/hub-candidato/components/HubCandidatoRH.tsx
autonomous: true
requirements: [OPER-01, OPER-02, OPER-03]

must_haves:
  truths:
    - "OPER-01: RH can advance a candidate from ANY of the 6 working stages via the Kanban card menu and the HubCandidatoRH action row (not just drag-drop / not just Kanban)"
    - "OPER-02: the Rejeitar affordance on both surfaces opens the shared RejeitarCandidaturaDialog (motivo + ≥50 justificativa), never a bare status update"
    - "OPER-03: the Retroceder affordance on both surfaces opens RetrocederCandidaturaDialog; reject/regress affordances are hidden for terminal etapas (aprovado/rejeitado)"
  artifacts:
    - path: "src/components/KanbanBoard.tsx"
      provides: "card DropdownMenu (MoreVertical) with Avançar/Retroceder/Rejeitar items; drag-drop advance preserved"
      contains: "DropdownMenu"
    - path: "src/features/hub-candidato/components/HubCandidatoRH.tsx"
      provides: "action row beside the Abrir/CTA in the 'Próximo passo' block (lines 169-186)"
      contains: "RejeitarCandidaturaDialog"
  key_links:
    - from: "KanbanBoard card menu"
      to: "RejeitarCandidaturaDialog / RetrocederCandidaturaDialog"
      via: "menu items open the shared dialogs"
      pattern: "RejeitarCandidaturaDialog"
    - from: "HubCandidatoRH 'Próximo passo' block"
      to: "shared dialogs + useUpdateCandidaturaEtapa"
      via: "action row beside the dominant CTA"
      pattern: "RejeitarCandidaturaDialog"
---

<objective>
Wire the two shared dialogs (31-03) plus the advance/regress affordances into the two RH detail surfaces: the `KanbanBoard` card menu and the `HubCandidatoRH` "Próximo passo" action row. This delivers avançar / rejeitar / retroceder at ANY of the 6 stages (OPER-01/02/03), not just the drag-drop Kanban advance that exists today.

Purpose: OPER-01 requires advancing at any of the 6 stages; today the only per-card action is drag-drop advance. This plan adds an explicit keyboard-operable menu (Kanban) and an action row (Hub) that reuse the audited write-path.
Output: modified `KanbanBoard.tsx` (card DropdownMenu) + `HubCandidatoRH.tsx` (action row).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-UI-SPEC.md
@.planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-PATTERNS.md

<interfaces>
<!-- Verified against live source. -->

KanbanBoard.tsx (self):
  import { useUpdateCandidaturaEtapa } from '@/features/vagas/hooks/useCandidaturas'  // :31
  const WORKING_STAGES: EtapaFunilM2[] = [inscricao,triagem,avaliacao_assincrona,entrevista_online,entrevista_presencial,decisao_final]  // :50-57
  function getTerminalBadge(candidatura) → non-null for terminal (aprovado/rejeitado) cards  // :89-108
  CandidatoKanbanCard block :140-277; terminalBadge = getTerminalBadge(candidatura) :161; "Ver Perfil" GlassButton min-h-[32px] :262-272
  const { mutate: moveEtapa } = useUpdateCandidaturaEtapa()  // :374; moveEtapa({candidaturaId, novaEtapa}) :406

HubCandidatoRH.tsx (self) — this IS the "perfil" surface (PerfilCandidatoRHPage is a 5-line wrapper — DO NOT edit the wrapper):
  const candidaturaId = id ?? ''  // :80 (route :id IS a candidaturaId)
  const etapaAtual = (contexto?.etapa_atual as EtapaFunilM2) ?? null  // :87
  "Próximo passo" block :169-186 — renders ONLY when rotaWorkspaceAtual && entradaAtual (non-terminal); the dominant CTA is a turquoise button :180-184 inside `flex flex-wrap items-center justify-between gap-4` (:173). Mount the action row in THIS block, do NOT displace the CTA.

Consumed from 31-03 / 31-02:
  RejeitarCandidaturaDialog (props candidaturaId, nome, trigger, onRejected?)  // src/features/triagem/components/RejeitarCandidaturaDialog.tsx
  RetrocederCandidaturaDialog (props candidaturaId, nome, etapaAtual, trigger, onDone?)  // src/features/triagem/components/RetrocederCandidaturaDialog.tsx
  useUpdateCandidaturaEtapa() → mutate({candidaturaId, novaEtapa, justificativa?})

Vendored: src/components/ui/dropdown-menu.tsx ; icons MoreVertical/ArrowRight/ArrowLeft (lucide-react)
Advance next-stage: use the funnel-order array to compute the next WORKING_STAGE after etapaAtual (Kanban drag already infers this via moveEtapa; the menu "Avançar" uses the same moveEtapa/useUpdateCandidaturaEtapa).
Styling (31-UI-SPEC §Color): Rejeitar trigger on dark glass `border-red-500/40 bg-red-500/10 text-red-300 hover:bg-red-500/20`; Retroceder neutral `border-white/20 bg-white/5 text-white/80 hover:bg-white/10`; Avançar accent `bg-[#35BFAD] text-[#00109E]` (Kanban) / matches host.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KanbanBoard card DropdownMenu (Avançar / Retroceder / Rejeitar)</name>
  <read_first>
    - src/components/KanbanBoard.tsx (self — CandidatoKanbanCard :140-277, getTerminalBadge :89-108, moveEtapa/useUpdateCandidaturaEtapa :374/:406, "Ver Perfil" button :262-272)
    - src/components/ui/dropdown-menu.tsx (Radix DropdownMenu API)
    - .planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-UI-SPEC.md (§Component Inventory "Card action menu", §Accessibility)
  </read_first>
  <action>
    In `CandidatoKanbanCard`, add a `DropdownMenu` beside the existing "Ver Perfil" button: a `MoreVertical` icon `<button aria-label="Ações do candidato">` (min-h-[32px], matching "Ver Perfil"). Items: **Avançar** (reuses `moveEtapa`/`useUpdateCandidaturaEtapa` to the next WORKING_STAGE after the card's etapa behind a lightweight confirm — no free-text), **Retroceder** (opens `RetrocederCandidaturaDialog` with the card's candidatura.id + nome + etapaAtual), **Rejeitar** (opens `RejeitarCandidaturaDialog` with candidatura.id + nome; onRejected can rely on the hook's invalidation). Keep drag-drop advance (`useDrag`) unchanged — the menu is the keyboard/explicit path AND the ONLY reject/regress affordance on the card. Hide the Retroceder + Rejeitar items when `getTerminalBadge(candidatura)` is non-null (terminal cards); Avançar is also hidden on terminals (no forward stage). Pass the shared dialogs' `trigger` as DropdownMenuItem-styled elements OR control the dialogs' open state from the menu (executor's discretion — the dialogs already accept a `trigger` node). Do NOT add any bare `updateCandidaturaEtapa(id,'rejeitado')` path — reject goes exclusively through the dialog → RPC.
  </action>
  <verify>
    <automated>npx vitest run src/features/triagem && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - KanbanBoard compiles; triagem suite GREEN; tsc baseline (~104) not regressed.
    - Card has a `DropdownMenu` with `aria-label="Ações do candidato"` and items Avançar/Retroceder/Rejeitar.
    - Reject/Retroceder items hidden when the card is terminal (getTerminalBadge non-null); Rejeitar opens RejeitarCandidaturaDialog (no bare status write).
    - Drag-drop advance path unchanged.
  </acceptance_criteria>
  <done>Kanban cards expose avançar/retroceder/rejeitar through the shared dialogs at any working stage.</done>
</task>

<task type="auto">
  <name>Task 2: HubCandidatoRH "Próximo passo" action row</name>
  <read_first>
    - src/features/hub-candidato/components/HubCandidatoRH.tsx (self — "Próximo passo" block :169-186, candidaturaId :80, etapaAtual :87)
    - src/features/triagem/components/RejeitarCandidaturaDialog.tsx + RetrocederCandidaturaDialog.tsx (the dialogs to mount — from 31-03)
    - .planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-UI-SPEC.md (§Component Inventory "Action row on HubCandidatoRH", §Color)
  </read_first>
  <action>
    Inside the "Próximo passo" block (lines 169-186 — mount HERE, NOT in the PerfilCandidatoRHPage wrapper), add Avançar / Retroceder / Rejeitar affordances BESIDE the existing dominant "Abrir {etapa}" CTA, inside the same `flex flex-wrap items-center justify-between gap-4` row — do NOT displace the CTA. Avançar advances to the next WORKING_STAGE via `useUpdateCandidaturaEtapa().mutate({candidaturaId, novaEtapa})` behind a lightweight confirm; Retroceder opens `RetrocederCandidaturaDialog` (candidaturaId, nome, etapaAtual); Rejeitar opens `RejeitarCandidaturaDialog` (candidaturaId, nome). The block already renders only for non-terminal etapas (guarded by `rotaWorkspaceAtual && entradaAtual`), so the action group is naturally hidden on terminals; add an explicit terminal check only if needed. Styling per §Color: Retroceder neutral dark-glass trigger, Rejeitar `border-red-500/40 bg-red-500/10 text-red-300`, Avançar accent. Use the candidato display name already in scope for the dialog `{nome}` (or a sensible fallback if not present).
  </action>
  <verify>
    <automated>npx vitest run src/features/hub-candidato src/features/triagem && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - HubCandidatoRH compiles; suites GREEN; tsc baseline not regressed.
    - Action row mounted inside the "Próximo passo" block (169-186) beside the CTA — the dominant "Abrir {etapa}" CTA is not displaced.
    - Rejeitar opens RejeitarCandidaturaDialog; Retroceder opens RetrocederCandidaturaDialog; both hidden for terminal etapas.
    - PerfilCandidatoRHPage.tsx (the 5-line wrapper) is NOT edited.
  </acceptance_criteria>
  <done>The RH candidate detail surface exposes avançar/retroceder/rejeitar beside the primary CTA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH surface → shared dialog → hook → RPC | The surfaces only open dialogs / call the audited hook; no surface performs a privileged write directly — the DB RPC/trigger is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Repudiation | reject affordance | mitigate | Reject on both surfaces routes exclusively through RejeitarCandidaturaDialog → RPC (no bare status write); the trail is written by the trigger |
| T-31-05 | Integrity | advance/regress path | mitigate | Advance/regress use the extended useUpdateCandidaturaEtapa (single UPDATE, etapa_justificativa always SET); no direct historico INSERT |
| T-31-04 | RNF-07a | terminal-state UX | mitigate | Reject/regress affordances hidden on terminal cards; every transition is a human-initiated click (ator=auth.uid()) |
| T-31-SC | Tampering (supply-chain) | npm installs | accept | Zero new packages; dropdown-menu already vendored |
</threat_model>

<verification>
- `npx vitest run src/features/triagem src/features/hub-candidato` GREEN.
- `npm run lint` tsc baseline (~104) not regressed.
- End-to-end visual/interaction check deferred to the phase-close live UAT.
</verification>

<success_criteria>
- Both RH detail surfaces expose avançar/retroceder/rejeitar via the shared dialogs.
- No surface performs a bare reject status write.
- Terminal cards hide reject/regress.
</success_criteria>

<output>
Create `.planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-04-SUMMARY.md` when done.
</output>
