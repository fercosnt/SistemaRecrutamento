---
phase: 31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil
plan: 05
type: execute
wave: 3
depends_on: [31-03, 31-02]
files_modified:
  - src/features/triagem/components/ComparativoScreen.tsx
  - src/components/pages/ComparativoCandidatosPage.tsx
  - src/features/triagem/components/__tests__/ComparativoScreen.test.tsx
autonomous: true
requirements: [OPER-04, OPER-01]

must_haves:
  truths:
    - "OPER-04: the ComparativoScreen Rejeitar action now requires motivo + ≥50 justificativa via the shared RejeitarCandidaturaDialog and writes through the rejeitar_candidatura RPC — closing the funil-02 debt (the button was a bare updateCandidaturaEtapa(id,'rejeitado') with no justificativa)"
    - "OPER-01: the Avançar action on the comparativo is preserved unchanged (updateCandidaturaEtapa to the next stage)"
    - "The read-only embed contract is preserved: onAvancar?/onRejeitar? stay optional so the DecisaoFinalPage no-op embed (omits handlers on purpose) still renders read-only"
  artifacts:
    - path: "src/features/triagem/components/ComparativoScreen.tsx"
      provides: "Rejeitar replaced by RejeitarCandidaturaDialog; onRejeitar signature carries {motivo,justificativa}; Avançar block unchanged; showActions gate preserved"
      contains: "RejeitarCandidaturaDialog"
    - path: "src/components/pages/ComparativoCandidatosPage.tsx"
      provides: "handleRejeitar rewired to useRejeitarCandidatura (was bare updateCandidaturaEtapa(id,'rejeitado'))"
      contains: "useRejeitarCandidatura"
  key_links:
    - from: "ComparativoCandidatosPage handleRejeitar"
      to: "rejeitar_candidatura RPC"
      via: "useRejeitarCandidatura mutation (replaces bare updateCandidaturaEtapa(id,'rejeitado'))"
      pattern: "useRejeitarCandidatura"
    - from: "ComparativoScreen Rejeitar action"
      to: "RejeitarCandidaturaDialog"
      via: "shared dialog replaces the no-justificativa inline AlertDialog (:344-373)"
      pattern: "RejeitarCandidaturaDialog"
---

<objective>
Close the funil-02 debt (OPER-04): the ComparativoScreen "Rejeitar" action today calls `updateCandidaturaEtapa(candidaturaId, 'rejeitado')` with NO motivo/justificativa. Rewire it to the shared `RejeitarCandidaturaDialog` → `rejeitar_candidatura` RPC (motivo + ≥50 justificativa, audited). The "Avançar" action stays exactly as-is (OPER-01), and the read-only embed contract (optional handlers used by the DecisaoFinalPage embed) is preserved.

Purpose: This is the specific requirement OPER-04 — the comparativo reject buttons that "hoje são no-op"/unaudited become real audited writes through the same write-path as every other surface.
Output: modified `ComparativoScreen.tsx` + `ComparativoCandidatosPage.tsx` + rewired `ComparativoScreen.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-PATTERNS.md
@.planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-UI-SPEC.md

<interfaces>
<!-- Verified against live source. -->

ComparativoScreen.tsx (self):
  props: onAvancar?: (candidaturaId: string) => void  // :58 (optional — read-only embed omits it)
         onRejeitar?: (candidaturaId: string) => void  // :60 (optional — CHANGE its call site to carry {motivo,justificativa})
  const showActions = Boolean(onAvancar && onRejeitar)  // :121 — PRESERVE (gates the whole action row)
  Avançar block :314-341 (accent AlertDialog) — KEEP verbatim
  Rejeitar inline confirm AlertDialog :344-373 (no justificativa/motivo) — REPLACE with RejeitarCandidaturaDialog

ComparativoCandidatosPage.tsx (self):
  const invalidatePanel = () => queryClient.invalidateQueries({ queryKey: triagemKeys.all })  // :109-111
  handleAvancar(candidaturaId) → updateCandidaturaEtapa(candidaturaId, PROXIMA_ETAPA_APOS_TRIAGEM) → toast + invalidatePanel  // :113-121 — KEEP
  handleRejeitar(candidaturaId) → updateCandidaturaEtapa(candidaturaId, 'rejeitado') → toast + invalidatePanel  // :123-131 — REWIRE to useRejeitarCandidatura

Consumed from 31-03/31-02:
  RejeitarCandidaturaDialog (props candidaturaId, nome, trigger, onRejected?)  // src/features/triagem/components/
  useRejeitarCandidatura() → mutate({candidaturaId, motivo, justificativa})  // src/features/triagem/hooks/useRejeitarCandidatura.ts (already invalidates triagemKeys.all + candidaturasKeys.all + vagasKeys.all)

Read-only embed to protect: src/features/decisao/components/DecisaoFinalPage.tsx:194 renders <ComparativoScreen /> WITHOUT onAvancar/onRejeitar ON PURPOSE (read-only) — showActions must stay false there.
Existing test to rewire: src/features/triagem/components/__tests__/ComparativoScreen.test.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ComparativoScreen — replace inline reject with the shared dialog</name>
  <read_first>
    - src/features/triagem/components/ComparativoScreen.tsx (self — props :54-60, showActions :121, Avançar :314-341, Rejeitar inline AlertDialog :344-373)
    - src/features/triagem/components/RejeitarCandidaturaDialog.tsx (the shared dialog to mount — from 31-03)
    - src/features/decisao/components/DecisaoFinalPage.tsx (line ~194 — the read-only embed that omits handlers; confirm it still renders read-only)
  </read_first>
  <action>
    In `ComparativoScreen.tsx`: replace the current Rejeitar inline confirm `AlertDialog` (:344-373) with the shared `RejeitarCandidaturaDialog`, passing the candidate's candidaturaId + display name and a destructive-styled trigger button matching the existing dark-glass reject styling (`border-red-500/40 bg-red-500/10 text-red-300 hover:bg-red-500/20`). Change the `onRejeitar` prop signature so the parent receives the structured `{motivo, justificativa}` (or let the dialog call `useRejeitarCandidatura` directly and use `onRejeitar` only as an optional post-success callback — executor's discretion, but the RESULT must be that the reject goes through the RPC with motivo+justificativa). PRESERVE `showActions = Boolean(onAvancar && onRejeitar)` (:121) and keep the Avançar block (:314-341) verbatim — do NOT change advance. The read-only embed contract (both handlers optional) MUST hold so the DecisaoFinalPage embed at :194 still renders with no action row. Do NOT introduce any bare `updateCandidaturaEtapa(id,'rejeitado')`.
  </action>
  <verify>
    <automated>npx vitest run src/features/triagem && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - ComparativoScreen renders the shared RejeitarCandidaturaDialog for reject (no inline no-justificativa AlertDialog remains).
    - `showActions` gate preserved; when onAvancar/onRejeitar are omitted (DecisaoFinalPage embed) the action row does not render.
    - Avançar block unchanged.
    - tsc baseline not regressed; triagem suite compiles.
  </acceptance_criteria>
  <done>The comparativo reject action now requires motivo + ≥50 justificativa via the shared dialog; read-only embed still read-only.</done>
</task>

<task type="auto">
  <name>Task 2: ComparativoCandidatosPage rewire handleRejeitar → RPC + rewire the test</name>
  <read_first>
    - src/components/pages/ComparativoCandidatosPage.tsx (self — handleAvancar :113-121, handleRejeitar :123-131, invalidatePanel :109-111)
    - src/features/triagem/hooks/useRejeitarCandidatura.ts (from 31-02)
    - src/features/triagem/components/__tests__/ComparativoScreen.test.tsx (existing — rewire to assert the new reject path)
  </read_first>
  <action>
    In `ComparativoCandidatosPage.tsx`: rewire `handleRejeitar` to use `useRejeitarCandidatura().mutate({ candidaturaId, motivo, justificativa })` instead of `updateCandidaturaEtapa(candidaturaId, 'rejeitado')` — the structured motivo+justificativa come from the shared dialog. Keep `handleAvancar` on `updateCandidaturaEtapa(candidaturaId, PROXIMA_ETAPA_APOS_TRIAGEM)` (OPER-01, unchanged). The new hook already invalidates `triagemKeys.all` (+ candidaturasKeys/vagasKeys), so the existing `invalidatePanel()` call is redundant for reject but harmless — keep it or drop it for reject at discretion. Remove the now-unused bare reject import if it leaves a lint warning.

    Rewire `ComparativoScreen.test.tsx`: update/add an assertion that triggering reject routes through the new RPC path (the shared dialog → useRejeitarCandidatura), NOT the old `updateCandidaturaEtapa(id,'rejeitado')`. Assert the Avançar path is unchanged and that omitting handlers hides the action row (read-only embed contract).
  </action>
  <verify>
    <automated>npx vitest run src/features/triagem/components/__tests__/ComparativoScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run ComparativoScreen` GREEN with the reject-uses-new-RPC-path assertion.
    - `handleRejeitar` no longer calls `updateCandidaturaEtapa(id,'rejeitado')`; it uses `useRejeitarCandidatura`.
    - `handleAvancar` unchanged (still `updateCandidaturaEtapa(id, PROXIMA_ETAPA_APOS_TRIAGEM)`).
    - Read-only embed contract asserted (no action row when handlers omitted).
  </acceptance_criteria>
  <done>funil-02 closed: comparativo reject is an audited RPC write with motivo + justificativa; advance preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comparativo action → dialog/hook → RPC | The comparativo previously wrote reject with no justificativa (unauditable disposition); now it crosses the same audited RPC boundary as every other surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Repudiation | comparativo reject (funil-02) | mitigate | Reject rewired from bare `updateCandidaturaEtapa(id,'rejeitado')` (no justificativa/motivo) to RejeitarCandidaturaDialog → rejeitar_candidatura RPC (motivo + ≥50 + audited trail) |
| T-31-02 | Tampering | ≥50 gate on comparativo | mitigate | Reuses the shared dialog client mirror + the server RPC RAISE — no separate/weaker path exists on this surface |
| T-31-07 | Elevation | read-only embed | mitigate | showActions/optional-handler contract preserved — the DecisaoFinalPage embed renders read-only (no reject affordance) |
| T-31-SC | Tampering (supply-chain) | npm installs | accept | Zero new packages |
</threat_model>

<verification>
- `npx vitest run src/features/triagem` GREEN (including the rewired ComparativoScreen test).
- `npm run lint` tsc baseline not regressed.
- Grep confirms no remaining `updateCandidaturaEtapa(.*'rejeitado')` in ComparativoCandidatosPage.
</verification>

<success_criteria>
- Comparativo reject is an audited motivo+justificativa RPC write (OPER-04 closed).
- Advance unchanged (OPER-01); read-only embed preserved.
</success_criteria>

<output>
Create `.planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-05-SUMMARY.md` when done.
</output>
