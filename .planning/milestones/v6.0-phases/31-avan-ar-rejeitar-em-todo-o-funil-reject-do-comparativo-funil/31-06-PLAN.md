---
phase: 31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil
plan: 06
type: execute
wave: 4
depends_on: [31-01, 31-02, 31-03, 31-04, 31-05]
files_modified:
  - database.types.ts
autonomous: false
requirements: [OPER-01, OPER-02, OPER-03, OPER-04]

must_haves:
  truths:
    - "OPER-01/02/03/04: the migration is LIVE in PROD (enum motivo_rejeicao_rh + RPC rejeitar_candidatura exist; the two dead M1 overloads are gone) and the 5-assertion behavioral smoke passes GREEN via MCP"
    - "database.types.ts (ROOT) is regenerated: rejeitar_candidatura + motivo_rejeicao_rh appear, avancar_etapa(uuid,uuid) + rejeitar_candidato(uuid,text,uuid) disappear, tsc baseline (~104) not regressed"
    - "The live avancar_etapa() zero-arg trigger function still exists and is bound to candidaturas_avancar_etapa_trg (the DROP did not touch it)"
  artifacts:
    - path: "database.types.ts"
      provides: "regenerated types with the new RPC+enum and without the 2 dead overloads"
      contains: "rejeitar_candidatura"
  key_links:
    - from: "Supabase MCP apply_migration"
      to: "PROD enum + RPC + DROPs live"
      via: "apply_migration writes the version row (bypasses 42601)"
      pattern: "rejeitar_candidatura"
    - from: "MCP execute_sql oper31 smoke"
      to: "5 GREEN assertions"
      via: "behavioral proof of OPER-01/02/03/04 server invariants"
      pattern: "PASS"
---

<objective>
Apply the reject-RPC migration (31-01) to PROD via Supabase MCP `apply_migration`, run the behavioral smoke (31-01) via MCP `execute_sql` to prove all 5 server-authoritative invariants GREEN, then regenerate `database.types.ts` (repo ROOT) so the new RPC + enum appear and the two dead overloads disappear. This is the single `[BLOCKING]` non-autonomous gate — it runs AFTER all migration + code authoring (31-01..05) and BEFORE `/gsd:verify-work`.

Purpose: The repo convention (CLAUDE.md §Commands + STATE.md decision log) is that PL/pgSQL `$$`-body migrations are applied to PROD via MCP `apply_migration` (the transaction pooler throws 42601 on `supabase db push --linked`; MCP bypasses it and writes the version row). This plan performs that privileged live apply + proves it + syncs the generated types.
Output: LIVE enum + RPC + dropped overloads in PROD; 5 GREEN smoke assertions; regenerated `database.types.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-RESEARCH.md

<interfaces>
<!-- Apply-path facts, verified from repo convention (STATE.md decision log + CLAUDE.md §Commands). -->

Migration to apply: supabase/migrations/20260714100001_rejeitar_candidatura_rpc.sql (authored in 31-01)
Smoke to run: supabase/tests/oper31_rejeitar_candidatura_smokes.sql (authored in 31-01)

Apply path (repo convention):
  - Supabase MCP `apply_migration` (name = migration filename version) applies PL/pgSQL $$-bodies AND writes the supabase_migrations version row itself → `supabase db push --linked` reports "up to date" afterward. Do NOT use `db push --linked` to apply (42601 on the pooler).
  - Reconcile: if the version row is not written automatically, insert the version row so `db push` is clean (precedent: MCP apply writes it; earlier phases occasionally reconciled a drift row).
  - Types regen: `npm run db:types` (requires Supabase CLI) → overwrites database.types.ts at repo ROOT (NOT src/types/). NEVER hand-edit.

Smoke run: MCP `execute_sql` with the full oper31 smoke file body; expect 5 `NOTICE 'PASS (x)'` and ZERO `EXCEPTION 'FAIL'`. The smoke uses a disposable fixture (ROLLBACK-free cleanup) — no PROD candidatura is mutated.

Post-apply structural sanity (via execute_sql): confirm `public.avancar_etapa()` (zero-arg trigger fn) STILL EXISTS and is bound to candidaturas_avancar_etapa_trg; confirm `avancar_etapa(uuid,uuid)` and `rejeitar_candidato(uuid,text,uuid)` are GONE.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Apply migration via MCP + run smokes GREEN + regen types</name>
  <what-built>
    A DEFINER RPC `rejeitar_candidatura` + enum `motivo_rejeicao_rh` + two DROP FUNCTIONs (authored in 31-01), plus the TS service/hook/dialogs/surfaces (31-02..05). This task takes the privileged live action: apply to PROD, prove, and regenerate types.
  </what-built>
  <read_first>
    - supabase/migrations/20260714100001_rejeitar_candidatura_rpc.sql (the migration to apply — from 31-01)
    - supabase/tests/oper31_rejeitar_candidatura_smokes.sql (the smoke to run — from 31-01)
    - .planning/STATE.md (§Accumulated Context — the MCP apply_migration convention + the DROP-safety landmine)
  </read_first>
  <action>
    Perform, in order:
    1. **Pre-apply guard (execute_sql, read-only):** confirm `public.avancar_etapa()` (zero-arg) exists and is bound to `candidaturas_avancar_etapa_trg`, and diff the live body with `pg_get_functiondef` — abort if the migration file contains any zero-arg `avancar_etapa()` drop or `CASCADE` (Pitfall 1 HIGH-SEVERITY landmine).
    2. **Apply (MCP `apply_migration`):** apply `20260714100001_rejeitar_candidatura_rpc.sql`. MCP writes the version row (bypasses 42601). If the version row is not written, insert it so `supabase db push --linked` reports "up to date".
    3. **Post-apply structural sanity (execute_sql):** confirm `motivo_rejeicao_rh` enum exists with the 6 values; `rejeitar_candidatura(uuid, motivo_rejeicao_rh, text)` exists with `SECURITY DEFINER`; `avancar_etapa(uuid,uuid)` and `rejeitar_candidato(uuid,text,uuid)` are GONE; `avancar_etapa()` zero-arg STILL EXISTS + still bound to the trigger.
    4. **Run smokes (MCP `execute_sql`):** paste the full `oper31_rejeitar_candidatura_smokes.sql` body; require 5 `PASS` NOTICEs and ZERO `FAIL` EXCEPTION (asserts a/b/c/d/e — <50 RAISE, auto_rejeitado=false, regression empty→RAISE, exactly 1 historico row, cross-recruiter denied). If any assert FAILs, STOP — do not proceed to regen; report the failing assert.
    5. **Regenerate types:** `npm run db:types` → overwrites `database.types.ts` at repo ROOT. Confirm the regen ADDED `rejeitar_candidatura` + `motivo_rejeicao_rh` and REMOVED the two dead overloads.
    6. **Full gate:** `npm run test:run && npm run lint && npm run build` — full Vitest GREEN, tsc baseline (~104) not regressed (drop the `as never` cast in triagemService only if types now resolve cleanly), build 0 errors.

    This is a privileged PROD write (execute_sql for a migration may require an allow-rule / the operator's authorization per STATE.md). If the MCP call is blocked/auth-gated, surface the exact command for the operator and resume on approval.
  </action>
  <how-to-verify>
    1. Migration is live: `rejeitar_candidatura` RPC + `motivo_rejeicao_rh` enum present in PROD; the two dead overloads gone; `avancar_etapa()` trigger fn intact.
    2. All 5 smoke assertions returned PASS (no FAIL) via MCP execute_sql.
    3. `database.types.ts` regenerated (contains `rejeitar_candidatura`, no `avancar_etapa(uuid,uuid)` / `rejeitar_candidato(uuid,text,uuid)`).
    4. `npm run test:run && npm run lint && npm run build` all GREEN, tsc not regressed.
  </how-to-verify>
  <verify>
    <automated>grep -q "rejeitar_candidatura" database.types.ts && ! grep -qE "avancar_etapa:.*candidatura_uuid|rejeitar_candidato:" database.types.ts && npm run test:run && npm run lint 2>&1 | tail -3 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - PROD has the enum + RPC live and the two dead overloads dropped; `avancar_etapa()` trigger function still bound (confirmed via execute_sql).
    - The 5 behavioral smoke assertions all PASS via MCP execute_sql (no FAIL).
    - `database.types.ts` regenerated: contains `rejeitar_candidatura` + `motivo_rejeicao_rh`, no longer contains `avancar_etapa(uuid,uuid)` or `rejeitar_candidato(uuid,text,uuid)`.
    - `npm run test:run` GREEN, `npm run lint` tsc ~104 (not regressed), `npm run build` 0 errors.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the migration is live, the 5 smokes are GREEN, types are regenerated, and the full suite/build passes — or report the failing assert/step.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → PROD database | The privileged live apply crosses into PROD; a bad DROP or an unproven RPC would ship an IDOR or break funnel auditing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06 | Denial of Service (audit) | apply-time DROP FUNCTION | mitigate | Pre-apply guard diffs the live avancar_etapa() body + aborts if the migration contains a zero-arg drop or CASCADE; post-apply sanity confirms the trigger fn survived |
| T-31-01 | Elevation (IDOR) | live RPC | mitigate | Smoke assert (e) proves cross-recruiter reject is denied BEFORE the phase is declared done |
| T-31-02 | Tampering | live ≥50 gate | mitigate | Smoke assert (a) proves the server RAISE on <50 in PROD |
| T-31-04 | RNF-07a | live audit row | mitigate | Smoke assert (b) proves a human reject writes auto_rejeitado=false in PROD |
| T-31-05 | Integrity | live audit trail | mitigate | Smoke assert (d) proves exactly ONE historico row per transition in PROD |
| T-31-SC | Tampering (supply-chain) | npm installs | accept | Zero new packages; `npm run db:types` uses the already-installed Supabase CLI |
</threat_model>

<verification>
- MCP execute_sql structural sanity + 5 GREEN smoke assertions.
- `database.types.ts` regen confirmed (RPC/enum in, dead overloads out).
- `npm run test:run && npm run lint && npm run build` GREEN.
</verification>

<success_criteria>
- Migration LIVE in PROD; the trigger function is intact; the dead overloads are gone.
- All 5 behavioral smokes PASS.
- Types regenerated; full suite + build GREEN; tsc baseline held.
</success_criteria>

<output>
Create `.planning/phases/31-avan-ar-rejeitar-em-todo-o-funil-reject-do-comparativo-funil/31-06-SUMMARY.md` when done. Record: the applied migration version, the 5 smoke results, the tsc count, and any reconcile/allow-rule needed for the MCP apply.
</output>
