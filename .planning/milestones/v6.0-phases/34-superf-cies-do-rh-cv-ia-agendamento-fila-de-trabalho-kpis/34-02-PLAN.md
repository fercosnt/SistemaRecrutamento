---
phase: 34-superf-cies-do-rh-cv-ia-agendamento-fila-de-trabalho-kpis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/hub-candidato/services/analiseCandidatoService.ts
  - src/features/hub-candidato/hooks/useAnaliseCandidato.ts
  - src/features/hub-candidato/services/historicoCandidaturaService.ts
  - src/features/hub-candidato/hooks/useHistoricoCandidatura.ts
  - src/features/hub-candidato/components/CvButton.tsx
  - src/features/hub-candidato/components/AnaliseIABlock.tsx
  - src/features/hub-candidato/components/HistoricoBlock.tsx
  - src/features/hub-candidato/components/HubCandidatoRH.tsx
  - src/features/hub-candidato/services/__tests__/analiseCandidatoService.test.ts
  - src/features/hub-candidato/services/__tests__/historicoCandidaturaService.test.ts
  - src/features/hub-candidato/components/__tests__/AnaliseIABlock.test.tsx
  - src/features/hub-candidato/components/__tests__/CvButton.test.tsx
autonomous: true
requirements: [VISRH-01, VISRH-02, VISRH-03]
must_haves:
  truths:
    - "RH opens the candidate CV via a single button → cvUploadService.getSignedUrl(candidaturaId) → window.open(url,'_blank'); the 60s-TTL URL is NEVER put in a query cache and NEVER logged"
    - "The hub shows the FULL IA analysis — score_match band + pontos_fortes + gaps + flags IN FULL (no .slice(0,2)) — replacing the empty 'Score de Triagem' placeholder"
    - "The candidate never sees the IA block — it lives only in HubCandidatoRH (RH surface), not HubCandidato"
    - "A read-only Histórico feed renders historico_candidatura transitions (origem→destino, autor, data, justificativa) newest-first, allowlist-projected"
    - "Every new read names its columns explicitly — no select('*') in analiseCandidatoService or historicoCandidaturaService (RLS is row-level, not column-level)"
  artifacts:
    - path: "src/features/hub-candidato/services/analiseCandidatoService.ts"
      provides: "Allowlist read of the IA analysis for one candidatura (score_match, pontos_fortes, gaps, flags, analise_status)"
      contains: "AnaliseCandidatoServiceError"
    - path: "src/features/hub-candidato/services/historicoCandidaturaService.ts"
      provides: "Allowlist read of historico_candidatura (etapa_de, etapa_para, ator, criterio_texto, criado_em) newest-first"
      contains: "criterio_texto"
    - path: "src/features/hub-candidato/components/CvButton.tsx"
      provides: "Imperative CV open button (getSignedUrl → window.open) with own inline loading/error, never caches/logs the URL"
      contains: "getSignedUrl"
    - path: "src/features/hub-candidato/components/HubCandidatoRH.tsx"
      provides: "The 'Score de Triagem' placeholder replaced by the Análise da IA + CV block; a Histórico block added after Decisão Final"
      contains: "AnaliseIABlock"
  key_links:
    - from: "useAnaliseCandidato"
      to: "public.analise_candidato_vaga (rh_le_analise RLS)"
      via: "allowlist select filtered by candidatura_id"
      pattern: "score_match, pontos_fortes, gaps, flags"
    - from: "useHistoricoCandidatura"
      to: "public.historico_candidatura (rh_le_historico WR-04)"
      via: "allowlist select order criado_em desc"
      pattern: "order\\('criado_em'"
    - from: "CvButton"
      to: "get-curriculo-url EF via cvUploadService.getSignedUrl"
      via: "imperative on-click → window.open"
      pattern: "window\\.open"
---

<objective>
Wire the three RH-only candidate-visibility surfaces onto the hub, all against ALREADY-SHIPPED secure primitives (P32 CV EF `get-curriculo-url` + `rh_le_historico` WR-04; P10 `analise_candidato_vaga` RLS): a CV open button, the FULL IA analysis block (replacing the empty "Score de Triagem" placeholder), and a read-only Histórico activity feed. This plan depends on nothing in Phase 34 — the primitives it reads shipped in P32/P33/P10 — so it runs in wave 1 alongside the DB foundation.

Purpose: VISRH-01 (CV via signed-URL EF, owner/admin only) + VISRH-02 (full IA analysis, candidate never sees it) + VISRH-03 (read-only history feed). The failure mode is re-implementing a shipped primitive client-side (a client `createSignedUrl` reintroducing the leak P32 closed, a `.slice(0,2)` truncation, or a `select('*')` PII leak) — so every read is an explicit allowlist and the CV goes only through `getSignedUrl`.

Output: `analiseCandidatoService` + `useAnaliseCandidato`, `historicoCandidaturaService` + `useHistoricoCandidatura`, `CvButton` + `AnaliseIABlock` + `HistoricoBlock` components, and the `HubCandidatoRH` edits placing them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-superf-cies-do-rh-cv-ia-agendamento-fila-de-trabalho-kpis/34-CONTEXT.md
@.planning/phases/34-superf-cies-do-rh-cv-ia-agendamento-fila-de-trabalho-kpis/34-UI-SPEC.md
@.planning/phases/34-superf-cies-do-rh-cv-ia-agendamento-fila-de-trabalho-kpis/34-RESEARCH.md

# The surface this plan edits + the analog patterns to clone:
@src/features/hub-candidato/components/HubCandidatoRH.tsx
@src/features/hub-candidato/components/HubSection.tsx
@src/features/entrevista/services/entrevistaService.ts
@src/features/entrevista/hooks/useEntrevistaScorecard.ts
@src/features/triagem/components/TriagemTable.tsx
@src/features/vagas/services/cvUploadService.ts

<interfaces>
<!-- The EXACT reusable contracts. Do NOT explore; use these directly. -->

CV signer (P32-shipped, DO NOT re-implement): `getSignedUrl(candidaturaId: string): Promise<string>` from
`src/features/vagas/services/cvUploadService.ts:199-213` — invokes the `get-curriculo-url` EF, returns a 60s-TTL
signed URL, throws `CVUploadServiceError`. NEVER log the returned URL (Pitfall 2 / existing grep guard).

IA analysis source: table `public.analise_candidato_vaga` (RLS `rh_le_analise`, RH/admin SELECT; candidate DENIED).
Columns for the allowlist (20260610000001:31-49): `score_match int 0-100 nullable, pontos_fortes text[], gaps text[],
flags text[], status text ('pendente'|'sucesso'|'falhou')`. One row per candidatura (unique candidatura_id).
Alternatively read the flattened `v_triagem_panel` filtered `.eq('id', candidaturaId)` (columns score_match/
pontos_fortes/gaps/flags/analise_status) — either is RLS-gated vaga-scoped; direct-table by candidatura_id is simplest.

Histórico source: table `public.historico_candidatura` (RLS `rh_le_historico` WR-04, P32). Allowlist columns
(20260607000001): `etapa_de public.etapa_processo (nullable), etapa_para public.etapa_processo, criterio_texto text
(= the justificativa), ator uuid (nullable — NULL = system/service action), criado_em timestamptz`. Order criado_em DESC.

Score band (reuse, colorblind-safe): `scoreBandClass(score)` + the ScoreCell chip visual live in
`src/features/triagem/components/TriagemTable.tsx:103-110` (thresholds 70/40; null → neutral). The `.slice(0,2)` at
`TriagemTable.tsx:238-239` is vaga-table density ONLY — the hub renders pontos_fortes/gaps IN FULL.

Hub anchor points (`HubCandidatoRH.tsx`): the empty "Score de Triagem" placeholder is at :292-302 (REPLACE it with the
Análise da IA + CV block); insert Histórico as the LAST block, after the "Decisão Final" HubSection (:369-379). Reuse
`SugestaoIABadge` from `src/features/triagem/components/SugestaoIABadge` (RNF-07a disclaimer). ETAPA_M2_LABELS from
`src/features/triagem/services/triagemService`. `:id` route param IS candidaturaId (Pitfall 8).

Query-key + hook shape: clone `entrevistaKeys` + `useQuery(staleTime 5min, gcTime 5min, retry 2, enabled:!!id)` from
`useEntrevistaScorecard.ts:39-63`. Service module + `class XServiceError` + allowlist consts from `entrevistaService.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IA read layer + AnaliseIABlock (VISRH-02)</name>
  <files>src/features/hub-candidato/services/analiseCandidatoService.ts, src/features/hub-candidato/hooks/useAnaliseCandidato.ts, src/features/hub-candidato/components/AnaliseIABlock.tsx, src/features/hub-candidato/services/__tests__/analiseCandidatoService.test.ts, src/features/hub-candidato/components/__tests__/AnaliseIABlock.test.tsx</files>
  <behavior>
    - analiseCandidatoService.getAnalise(candidaturaId) selects ONLY the allowlist columns (score_match, pontos_fortes, gaps, flags, analise_status) — a test asserts the `.select()` argument contains none of `*` and names those columns.
    - Returns null when no analysis row exists; maps error → AnaliseCandidatoServiceError.
    - AnaliseIABlock renders pontos_fortes and gaps in FULL (a test with 5 pontos_fortes shows all 5 — no truncation to 2).
    - score_match renders as a band chip via scoreBandClass (number + color in one element); null score → neutral "—" chip.
    - The block carries the SugestaoIABadge (RNF-07a) and never uses banned clinical wording (RNF-12a).
  </behavior>
  <read_first>
    - `<interfaces>` — analise_candidato_vaga allowlist columns + scoreBandClass location + the .slice(0,2) is vaga-only
    - `src/features/entrevista/services/entrevistaService.ts:59-90,440-459` — allowlist const + service-error + read shape to clone
    - `src/features/triagem/components/TriagemTable.tsx:99-160` — scoreBandClass + ScoreCell chip visual + Sparkles accent + SugestaoIABadge placement
    - `src/features/entrevista/hooks/useEntrevistaScorecard.ts:39-63` — entrevistaKeys + useQuery config to mirror
    - `34-UI-SPEC.md` §"Análise da IA (VISRH-02)" copy table + §States Contract
  </read_first>
  <action>
    Create `analiseCandidatoService.ts`: an `ANALISE_HUB_ALLOWLIST` const (score_match, pontos_fortes, gaps, flags, status) + `AnaliseCandidatoServiceError` (mirror EntrevistaServiceError codes) + `getAnalise(candidaturaId)` reading `analise_candidato_vaga` (or `v_triagem_panel` `.eq('id', candidaturaId)`) with `.select(ANALISE_HUB_ALLOWLIST).eq('candidatura_id', candidaturaId).maybeSingle()`, returning an `AnaliseHubRow { score_match, pontos_fortes[], gaps[], flags[], analise_status }` or null. Never `select('*')`.
    Create `useAnaliseCandidato.ts`: an `analiseHubKeys` factory + `useAnaliseCandidato(candidaturaId)` mirroring the entrevista useQuery config (staleTime/gcTime 5min, retry 2, enabled:!!id).
    Create `AnaliseIABlock.tsx`: given `{ analise, isLoading, isError }`, render inside a `HubSection titulo="Análise da IA"` — score_match as a band chip using an imported/duplicated `scoreBandClass` (number+color, Sparkles accent), `Pontos fortes` and `Gaps` lists IN FULL (map the whole arrays — NO `.slice`), `flags` under `Sinais de atenção`, the `SugestaoIABadge`, and the UI-SPEC copy for empty (`Análise ainda não disponível`) / failed (`analise_status='falhou'`) states. Neutral descriptive framing for any behavioral band (RNF-12a). Do NOT render this component from any candidate route.
    Tests: analiseCandidatoService.test.ts asserts the projection allowlist (no `*`, names the 5 columns) + null-on-missing; AnaliseIABlock.test.tsx renders 5 pontos_fortes and asserts all 5 present (no truncation) + the score band chip + the disclaimer.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/hub-candidato/services/__tests__/analiseCandidatoService.test.ts src/features/hub-candidato/components/__tests__/AnaliseIABlock.test.tsx && ! grep -Eq "select\('\*'\)|\.slice\(0" src/features/hub-candidato/services/analiseCandidatoService.ts src/features/hub-candidato/components/AnaliseIABlock.tsx && echo IA_OK</automated>
  </verify>
  <acceptance_criteria>
    - Service reads the 5-column allowlist only; zero `select('*')`.
    - AnaliseIABlock renders pontos_fortes/gaps in FULL (test proves 5→5, no `.slice`).
    - score_match band chip (number+color) via scoreBandClass; SugestaoIABadge present; empty/failed copy per UI-SPEC.
    - Component imported only under `hub-candidato` (RH surface) — never a candidate route.
  </acceptance_criteria>
  <done>`IA_OK` prints; tests green; no star projection, no slice truncation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Histórico read layer + CvButton (VISRH-03 + VISRH-01)</name>
  <files>src/features/hub-candidato/services/historicoCandidaturaService.ts, src/features/hub-candidato/hooks/useHistoricoCandidatura.ts, src/features/hub-candidato/components/HistoricoBlock.tsx, src/features/hub-candidato/components/CvButton.tsx, src/features/hub-candidato/services/__tests__/historicoCandidaturaService.test.ts, src/features/hub-candidato/components/__tests__/CvButton.test.tsx</files>
  <behavior>
    - historicoCandidaturaService.listHistorico(candidaturaId) selects ONLY etapa_de, etapa_para, ator, criterio_texto, criado_em (test asserts no `*`) ordered criado_em DESC.
    - HistoricoBlock renders a read-only newest-first feed: `{origem} → {destino}`, autor, date (pt-BR `d 'de' MMM, HH:mm`), justificativa; no edit affordance.
    - CvButton on click calls getSignedUrl(candidaturaId) then window.open(url,'_blank'); the URL is NEVER stored in state/cache and NEVER passed to console.* (test spies console + asserts no signed-url string logged).
    - CvButton shows its own inline states: idle `Abrir currículo`, in-flight `Abrindo…` disabled aria-busy, error `Não foi possível abrir o currículo.`
  </behavior>
  <read_first>
    - `<interfaces>` — historico_candidatura allowlist (criterio_texto = justificativa, ator nullable) + getSignedUrl contract + never-log rule
    - `src/features/vagas/services/cvUploadService.ts:180-213` — getSignedUrl + the "DO NOT log data.signedUrl" note
    - `src/features/entrevista/services/entrevistaService.ts:59-92,196-259` — allowlist const + candidatura-keyed read to clone
    - `src/features/hub-candidato/components/HubSection.tsx` — the loading/error/empty wrapper + verbatim copy
    - `34-UI-SPEC.md` §"CV block" + §"Histórico" copy tables + §Accessibility (aria-busy/aria-live on the CV button)
  </read_first>
  <action>
    Create `historicoCandidaturaService.ts`: `HISTORICO_ALLOWLIST` const (etapa_de, etapa_para, ator, criterio_texto, criado_em) + `HistoricoCandidaturaServiceError` + `listHistorico(candidaturaId)` reading `historico_candidatura` with `.select(HISTORICO_ALLOWLIST).eq('candidatura_id', candidaturaId).order('criado_em', { ascending: false }).limit(100)`, returning `HistoricoRow[]`. Never `select('*')`.
    Create `useHistoricoCandidatura.ts`: a `historicoKeys` factory + `useHistoricoCandidatura(candidaturaId)` mirroring the entrevista useQuery config.
    Create `HistoricoBlock.tsx`: `{ rows, isLoading, isError }` → a `HubSection titulo="Histórico"` read-only vertical list newest-first, each row `{ETAPA_M2_LABELS[etapa_de] ?? '—'} → {ETAPA_M2_LABELS[etapa_para]}`, autor (render `ator` — when null show `Sistema`), `format(criado_em, "d 'de' MMM, HH:mm", { locale: ptBR })`, and `criterio_texto` as the justificativa. Empty/error copy per UI-SPEC. No buttons.
    Create `CvButton.tsx`: a `{ candidaturaId }` button, `FileText` icon `min-h-[44px]`, local `useState` for loading/error ONLY (never store the URL); on click → `await getSignedUrl(candidaturaId)` → `window.open(url, '_blank')`; catch → set the inline error copy. `aria-busy` while loading. Do NOT `console.*` the URL.
    Tests: historicoCandidaturaService.test.ts asserts the projection allowlist + desc order; CvButton.test.tsx mocks getSignedUrl + window.open, asserts window.open called with the URL and a console spy sees NO signed-url string.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/hub-candidato/services/__tests__/historicoCandidaturaService.test.ts src/features/hub-candidato/components/__tests__/CvButton.test.tsx && ! grep -Eq "select\('\*'\)" src/features/hub-candidato/services/historicoCandidaturaService.ts && ! grep -Eiq "console\.[a-z]+\([^)]*(signedurl|signed_url|url)" src/features/hub-candidato/components/CvButton.tsx && echo HIST_CV_OK</automated>
  </verify>
  <acceptance_criteria>
    - historicoCandidaturaService reads the 5-column allowlist only, criado_em DESC; zero `select('*')`.
    - HistoricoBlock is read-only (no edit/button affordance), newest-first, pt-BR date + justificativa.
    - CvButton opens the URL via window.open and never caches or logs it (console spy proves it); inline loading/error states present.
  </acceptance_criteria>
  <done>`HIST_CV_OK` prints; tests green; no star projection, no URL logging.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the three blocks into HubCandidatoRH (replace placeholder + append Histórico)</name>
  <files>src/features/hub-candidato/components/HubCandidatoRH.tsx</files>
  <read_first>
    - `src/features/hub-candidato/components/HubCandidatoRH.tsx:168-383` — the render tree: the "Próximo passo" block (:187-253), the "Score de Triagem" placeholder to REPLACE (:292-302), the "Decisão Final" block to insert Histórico AFTER (:369-379)
    - the Task 1/2 artifacts (`useAnaliseCandidato`, `AnaliseIABlock`, `useHistoricoCandidatura`, `HistoricoBlock`, `CvButton`)
    - `34-UI-SPEC.md` §"Surface 1" placement (CV + IA right after Próximo passo/timeline; Histórico at the very end)
  </read_first>
  <action>
    Edit `HubCandidatoRH.tsx`. Add hook calls near the other section reads (after `decisaoQuery`): `const analiseQuery = useAnaliseCandidato(candidaturaId)` and `const historicoQuery = useHistoricoCandidatura(candidaturaId)`. REPLACE the "Score de Triagem" `<HubSection>` placeholder (:292-302) with a new `<Glass variant="dark" blur="lg" className="rounded-xl p-6">` wrapper (or reuse HubSection) that renders the `CvButton` (candidaturaId) alongside the `AnaliseIABlock` (analise=analiseQuery.data, isLoading/isError from the query) — titled "Análise da IA", positioned right after the "Linha do funil" timeline (keep it near the top per UI-SPEC). APPEND a `<HistoricoBlock rows={historicoQuery.data ?? []} isLoading isError />` as the LAST block after the "Decisão Final" HubSection. Import the new components/hooks. Do NOT alter the OPER action row, the CTA, or the timeline. Keep candidaturaId as the sole id (Pitfall 8).
  </action>
  <verify>
    <automated>grep -Eq "useAnaliseCandidato|AnaliseIABlock" src/features/hub-candidato/components/HubCandidatoRH.tsx && grep -Eq "HistoricoBlock|useHistoricoCandidatura" src/features/hub-candidato/components/HubCandidatoRH.tsx && grep -Eq "CvButton" src/features/hub-candidato/components/HubCandidatoRH.tsx && ! grep -q "O score de triagem por IA é exibido no painel de triagem da vaga." src/features/hub-candidato/components/HubCandidatoRH.tsx && npm run test:run -- src/features/hub-candidato && ERR=$(npm run lint 2>&1 | grep -c "error TS" || true) && [ "$ERR" -le 104 ] && echo WIRE_OK</automated>
  </verify>
  <acceptance_criteria>
    - The empty "Score de Triagem" placeholder copy is GONE; AnaliseIABlock + CvButton render in its place after the timeline.
    - HistoricoBlock is the last block (after Decisão Final).
    - Existing hub tests (hubNotFound/hubEmptyState) + new tests pass; tsc ≤104.
    - No candidate route imports the IA block; candidaturaId is the only id used.
  </acceptance_criteria>
  <done>`WIRE_OK` prints; the three VISRH blocks are on the RH hub, placeholder removed, tsc holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH client → analise_candidato_vaga / historico_candidatura | RLS is row-level (vaga-scoped); a `select('*')` would leak columns the surface doesn't need |
| RH client → get-curriculo-url EF (via getSignedUrl) | The EF is the only privileged CV path (P32); the client must not re-implement signing nor cache/log the URL |
| RH hub component tree | The IA analysis is RH-only; it must never be imported by a candidate-facing route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02-01 | Information Disclosure | analiseCandidatoService / historicoCandidaturaService select('*') | mitigate | Explicit allowlist consts; grep gate forbids `select('*')`; service tests assert the projection |
| T-34-02-02 | Information Disclosure | CV signed URL cached/logged past 60s | mitigate | CvButton calls getSignedUrl imperatively, window.open immediately, never stores the URL; console-spy test + grep gate |
| T-34-02-03 | Information Disclosure | IA score/analysis leaking to the candidate | mitigate | AnaliseIABlock imported only under HubCandidatoRH (RH surface); candidate has NO rh_le_analise SELECT (DB-denied) |
| T-34-02-04 | Information Disclosure | CV IDOR (recruiter opens another vaga's CV) | accept | Enforced server-side by the P32 EF (authenticate-THEN-authorize, owner/admin); the client only passes candidatura_id, never a path |
| T-34-02-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new deps (all primitives shipped); no install task |
</threat_model>

<verification>
- CV opens via window.open; the URL is never cached/logged (console-spy test + grep).
- IA analysis renders in full (no slice); candidate never sees it (component confined to RH hub).
- Histórico feed is read-only, allowlist-projected, newest-first.
- No `select('*')` in either new service; tsc ≤104; hub tests pass.
</verification>

<success_criteria>
- `npm run test:run -- src/features/hub-candidato` green; `npm run lint` ≤104; `npm run build` green.
- Placeholder "Score de Triagem" copy removed; three VISRH blocks live on the RH hub.
</success_criteria>

<output>
Create `.planning/phases/34-superf-cies-do-rh-cv-ia-agendamento-fila-de-trabalho-kpis/34-02-SUMMARY.md` when done.
</output>
