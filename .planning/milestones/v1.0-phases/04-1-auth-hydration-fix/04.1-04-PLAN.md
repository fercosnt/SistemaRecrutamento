---
phase: 4.1
plan: 04
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/App.tsx
  - src/hooks/useSessionTimeout.ts
  - src/components/pages/LoginRHPage.tsx
  - src/store/adminAuthStore.ts
autonomous: true
requirements:
  - FOUND-12
gap_closure_targets:
  - INT-WARNING-2
must_haves:
  truths:
    - "src/store/adminAuthStore.ts no longer exists in the working tree (file deletion)"
    - "App.tsx uses an inline `useIsAdminAuthenticated` selector reading from useAuthStore (no more shim import)"
    - "useSessionTimeout reads from useAuthStore directly (no more `useAdminAuthStore` import)"
    - "LoginRHPage doc-comment no longer references `adminAuthStore` by name"
    - "FOUND-12 grep guard transitions from RED to GREEN: file deleted + zero stale imports"
    - "tsc baseline preserved at <= 320 errors after deletion + migrations"
  artifacts:
    - path: "src/App.tsx"
      provides: "Inline useIsAdminAuthenticated selector reading useAuthStore directly"
      contains: "useIsAdminAuthenticated"
    - path: "src/hooks/useSessionTimeout.ts"
      provides: "useAuthStore consumer (no more adminAuthStore shim)"
      contains: "from '@/store/authStore'"
    - path: "src/components/pages/LoginRHPage.tsx"
      provides: "Updated doc-comment removing adminAuthStore.ts reference"
      contains: "Phase 4.1"
  key_links:
    - from: "src/App.tsx useIsAdminAuthenticated"
      to: "useAuthStore selector returning isAuthenticated && (role === 'rh' || role === 'administrador')"
      via: "inline arrow function"
      pattern: "isAuthenticated && \\(s\\.role === 'rh' \\|\\| s\\.role === 'administrador'\\)"
    - from: "src/hooks/useSessionTimeout.ts"
      to: "useAuthStore"
      via: "direct import"
      pattern: "from '@/store/authStore'"
---

<objective>
Fecha FOUND-12 literal: deleta `src/store/adminAuthStore.ts` re-export shim e migra os 2 consumers reais (App.tsx, useSessionTimeout.ts) para consumir `useAuthStore` diretamente. Atualiza a doc-string de LoginRHPage.tsx para deixar de referenciar o nome do arquivo deletado.

Purpose: FOUND-12 atualmente partial (shim ainda existe). REQUIREMENTS.md texto diz "deletado". Phase 4.1 success criterion #5: ou (a) deletar o shim + migrar consumers, ou (b) reescrever REQUIREMENTS text. RESEARCH §Open Questions Q1 recomenda Path A (delete) — blast radius mínimo. INT-WARNING-2 é fechado.

Output: 3 arquivos editados (1 inline selector + 1 import migration + 1 doc-comment update), 1 arquivo deletado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-1-auth-hydration-fix/04.1-RESEARCH.md
@.planning/phases/04-1-auth-hydration-fix/04.1-PATTERNS.md
@.planning/phases/04-1-auth-hydration-fix/04.1-02-PLAN.md
@./CLAUDE.md

# Source files to modify
@src/App.tsx
@src/hooks/useSessionTimeout.ts
@src/components/pages/LoginRHPage.tsx
@src/store/adminAuthStore.ts

<interfaces>
<!-- Key types/exports the executor will need (extracted from codebase). -->

From src/store/adminAuthStore.ts (the shim being deleted) — the 2 hooks ACTUALLY used by consumers:
```
export const useIsAdminAuthenticated = () =>
  useAuthStore(
    (s) => s.isAuthenticated && (s.role === 'rh' || s.role === 'administrador')
  )

// Note: useAdminUser, useAdminRole, useAdminAuthLoading, useAdminPermission,
// useAdminHasRole are also exported but NOT consumed by App.tsx/useSessionTimeout.
// Re-export of useAdminAuthStore alias is consumed by useSessionTimeout.
```

From src/store/authStore.ts (the unified store consumers will read directly):
```
useAuthStore: <T>(selector: (s: AuthState) => T) => T
useAuthStore.getState(): AuthState
// AuthState fields used by the migration:
//   isAuthenticated: boolean
//   role: Role | null
//   adminUser: UsuarioRHRow | null  // legacy alias
//   logout: () => Promise<void>
```

From src/App.tsx (PRE-Phase-4.1 imports):
```
import { useAuthStore } from './store/authStore'
import { useIsAdminAuthenticated } from './store/adminAuthStore'   // DELETE this line in Plan 04
```

From src/App.tsx (PRE-Phase-4.1 usage, line 146):
```
const isAdminAuthenticated = useIsAdminAuthenticated()
useSessionTimeout(isAdminAuthenticated)
```

From src/hooks/useSessionTimeout.ts (PRE-Phase-4.1, lines 19 + 45):
```
import { useAdminAuthStore } from '@/store/adminAuthStore'
// ...
const { isAuthenticated, adminUser, logout } = useAdminAuthStore()
```

From src/components/pages/LoginRHPage.tsx (PRE-Phase-4.1, lines 16-21):
```
 * D-14 / Bug 2/3 fix:
 *   Os setters legados do antigo adminAuthStore forjavam role='administrador'
 *   no client-side sem ler o JWT. Esta page REMOVE esses setters; a derivação
 *   do role agora vem do listener supabase.auth.onAuthStateChange montado
 *   uma vez no RootLayout (App.tsx), que sincroniza authStore via setSession
 *   → extractRole (Plan 03-03 / Bug 1 fix) decodifica o JWT e popula role.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate App.tsx + useSessionTimeout imports + update LoginRHPage doc-comment</name>
  <files>
    src/App.tsx,
    src/hooks/useSessionTimeout.ts,
    src/components/pages/LoginRHPage.tsx
  </files>
  <read_first>
    src/App.tsx,
    src/hooks/useSessionTimeout.ts,
    src/components/pages/LoginRHPage.tsx,
    src/store/adminAuthStore.ts,
    src/store/authStore.ts,
    .planning/phases/04-1-auth-hydration-fix/04.1-PATTERNS.md
  </read_first>
  <action>
**Arquivo 1: `src/App.tsx`**

Migre `useIsAdminAuthenticated` de `./store/adminAuthStore` para um selector inline lendo `useAuthStore`.

Step 1.1 — REMOVER a linha de import (linha 26):

ANTES:
```
import { useAuthStore } from './store/authStore'
import { useIsAdminAuthenticated } from './store/adminAuthStore'
import { supabase } from './lib/supabase/client'
```

DEPOIS:
```
import { useAuthStore } from './store/authStore'
import { supabase } from './lib/supabase/client'
```

Step 1.2 — ADICIONAR um selector inline ANTES da definição de `RootLayout` (perto do queryClient ou logo antes da função `RootLayout` linha 145). Use este pattern (PATTERNS.md §EDIT App.tsx Inline replacement):

```
/**
 * Hook: usuário está autenticado como RH ou administrador?
 *
 * Phase 4.1 — substitui `useIsAdminAuthenticated` do `adminAuthStore.ts`
 * (re-export shim deletado para fechar FOUND-12). Consumido apenas em
 * RootLayout para gatear `useSessionTimeout` (timeout só para sessões RH).
 */
const useIsAdminAuthenticated = () =>
  useAuthStore((s) => s.isAuthenticated && (s.role === 'rh' || s.role === 'administrador'))
```

Step 1.3 — Verificar que o uso em RootLayout (linha 146) NÃO mudou:

```
function RootLayout() {
  const isAdminAuthenticated = useIsAdminAuthenticated()
  ...
}
```

NÃO modifique nenhuma outra parte de App.tsx (incluindo o useEffect editado em Plan 02 — preservar exatamente).

**Arquivo 2: `src/hooks/useSessionTimeout.ts`**

Migre o import + a desestruturação para `useAuthStore`.

Step 2.1 — Substituir linha 19:

ANTES:
```
import { useAdminAuthStore } from '@/store/adminAuthStore'
```

DEPOIS:
```
import { useAuthStore } from '@/store/authStore'
```

Step 2.2 — Substituir linha 45:

ANTES:
```
  const { isAuthenticated, adminUser, logout } = useAdminAuthStore()
```

DEPOIS:
```
  const { isAuthenticated, adminUser, logout } = useAuthStore()
```

NÃO modifique:
- O comportamento do hook (linhas 23-141 inalteradas).
- A doc-string `@example` (linha 36) menciona `useAdminAuthStore` — pode editar opcionalmente para `useAuthStore`, mas não é blocker (é apenas exemplo em comentário).
- O TypeScript shape: `useAuthStore()` retorna o mesmo `AuthState` que `useAdminAuthStore()` retornava (re-export do mesmo store), com os mesmos campos `isAuthenticated`, `adminUser`, `logout`.

Step 2.3 (opcional, recomendado) — Atualizar `@example` doc-comment linha 36:

ANTES:
```
 * @example
 * ```tsx
 * function RootLayout() {
 *   const { isAuthenticated } = useAdminAuthStore()
 *   useSessionTimeout(isAuthenticated) // Monitora apenas se autenticado
 *
 *   return <Outlet />
 * }
 * ```
```

DEPOIS:
```
 * @example
 * ```tsx
 * function RootLayout() {
 *   const { isAuthenticated } = useAuthStore()
 *   useSessionTimeout(isAuthenticated) // Monitora apenas se autenticado
 *
 *   return <Outlet />
 * }
 * ```
```

**Arquivo 3: `src/components/pages/LoginRHPage.tsx`**

Atualize a doc-string para deixar de mencionar `adminAuthStore` (referência fica obsoleta após delete).

Step 3.1 — Editar a doc-string (linhas 16-21). O conteúdo atual menciona "adminAuthStore" no texto:

ANTES (linhas 16-21):
```
 * D-14 / Bug 2/3 fix:
 *   Os setters legados do antigo adminAuthStore forjavam role='administrador'
 *   no client-side sem ler o JWT. Esta page REMOVE esses setters; a derivação
 *   do role agora vem do listener supabase.auth.onAuthStateChange montado
 *   uma vez no RootLayout (App.tsx), que sincroniza authStore via setSession
 *   → extractRole (Plan 03-03 / Bug 1 fix) decodifica o JWT e popula role.
```

DEPOIS:
```
 * D-14 / Bug 2/3 fix:
 *   Os setters legados do antigo store admin (deletado em Phase 4.1 — FOUND-12)
 *   forjavam role='administrador' no client-side sem ler o JWT. Esta page
 *   REMOVE esses setters; a derivação do role agora vem do listener
 *   supabase.auth.onAuthStateChange montado uma vez no RootLayout (App.tsx),
 *   que sincroniza authStore via hydrateFromSession (Phase 4.1 fix) →
 *   extractRole (Plan 03-03 / Bug 1 fix) decodifica o JWT e popula role.
```

NÃO modifique nenhuma outra parte do arquivo (LoginRHPage está em PHASE_3_AUTH_PATHS — Pitfall 7 obrigatório; não adicionar console.*).

**IMPORTANTE — preserve order:**
- Esta task NÃO deleta o adminAuthStore.ts ainda (Task 2 faz). Os 3 arquivos devem ser editados PRIMEIRO; depois Task 2 deleta o shim. Ordem é importante para o build não quebrar mid-task.
- Após esta Task 1: `tsc --noEmit` deve continuar passando — `adminAuthStore.ts` ainda existe (não há erro), mas nenhum consumer real importa dele. As exportações `useAdminUser`, `useAdminRole`, etc., do shim, ficam órfãs (sem consumer) — Task 2 deleta o arquivo inteiro.
  </action>
  <verify>
    <automated>grep -c "from './store/adminAuthStore'" src/App.tsx; grep -c "from '@/store/adminAuthStore'" src/hooks/useSessionTimeout.ts; grep -c "useIsAdminAuthenticated" src/App.tsx; grep -c "useAuthStore" src/hooks/useSessionTimeout.ts; npm run lint 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from './store/adminAuthStore'" src/App.tsx` returns 0 (import deleted)
    - `grep -c "from '@/store/adminAuthStore'" src/hooks/useSessionTimeout.ts` returns 0 (import deleted)
    - `grep -c "useAdminAuthStore" src/hooks/useSessionTimeout.ts` returns <= 1 (only in doc-comment example, optional)
    - `grep -c "useIsAdminAuthenticated" src/App.tsx` returns >= 2 (inline declaration + usage in RootLayout)
    - `grep -c "useAuthStore((s) => s.isAuthenticated && (s.role === 'rh' || s.role === 'administrador'))" src/App.tsx` returns 1 OR equivalent regex match
    - LoginRHPage.tsx doc-comment no longer contains the literal phrase `antigo adminAuthStore` — `grep -c "antigo adminAuthStore" src/components/pages/LoginRHPage.tsx` returns 0
    - `npm run lint 2>&1 | grep -c "error TS"` outputs `<= 320` (baseline preserved — adminAuthStore.ts still exists so no ts-resolve errors)
    - `npm run test:run -- src/features/auth/utils/__tests__/pitfall7.grep.test.ts` exits 0 (no new console.* in LoginRHPage which is in PHASE_3_AUTH_PATHS)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    App.tsx + useSessionTimeout migrated to consume useAuthStore directly; LoginRHPage doc-comment updated; adminAuthStore.ts STILL exists (Task 2 deletes); lint baseline + build green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete src/store/adminAuthStore.ts and verify FOUND-12 grep guard transitions to GREEN</name>
  <files>
    src/store/adminAuthStore.ts
  </files>
  <read_first>
    src/store/adminAuthStore.ts,
    src/store/__tests__/found12.test.ts,
    .planning/phases/04-1-auth-hydration-fix/04.1-PATTERNS.md
  </read_first>
  <action>
**Step 1 — Verificar pré-condições (zero stale imports):**

Antes de deletar, rode estas checks. Se qualquer uma falhar, NÃO delete e reabra Task 1 para fechar a referência pendente.

```bash
# Imports diretos do shim — devem ser 0
grep -rn "from\s*['\"]@/store/adminAuthStore['\"]" src/ --include="*.ts" --include="*.tsx" | grep -v __tests__ | grep -v adminAuthStore.ts
grep -rn "from\s*['\"]\.\./store/adminAuthStore['\"]" src/ --include="*.ts" --include="*.tsx" | grep -v __tests__
grep -rn "from\s*['\"]\./store/adminAuthStore['\"]" src/ --include="*.ts" --include="*.tsx" | grep -v __tests__

# Hooks específicos do shim (não-consumidos por outros files):
grep -rn "useAdminAuthStore\|useAdminUser\|useAdminRole\|useAdminPermission\|useAdminHasRole\|useAdminAuthLoading" src/ --include="*.ts" --include="*.tsx" | grep -v __tests__ | grep -v adminAuthStore.ts
```

Se algum desses retornar matches reais (não em comments), pare e adicione a migração à Task 1 antes de prosseguir.

Esperado: zero matches reais (LoginRHPage doc-comment menciona o nome em comment — não é um import; OK).

**Step 2 — Delete o arquivo:**

```bash
rm src/store/adminAuthStore.ts
```

Use o tool nativo do executor (não `Bash` shell direto se não for permitido); se `Bash` é o caminho, garanta que o working directory é o repo root.

**Step 3 — Verificar tsc baseline preservado:**

```bash
npm run lint 2>&1 | grep -c "error TS"
```

Esperado: <= 320 (baseline). Se aumentar, reverta o delete e investigue qual import pendente quebrou.

**Step 4 — Rodar FOUND-12 grep guard (transitions RED → GREEN):**

```bash
npm run test:run -- src/store/__tests__/found12.test.ts
```

Esperado: ambos os 2 testes passam GREEN:
- "src/store/adminAuthStore.ts has been deleted" → file no longer exists
- "no source files import from @/store/adminAuthStore" → grep finds zero matches

Se qualquer teste falhar, investigue o output e corrija o import pendente em Task 1.

**Step 5 — Smoke test final:**

```bash
npm run build
npm run dev   # smoke run em background; abrir http://localhost:3003 e checar console errors
```

Esperado: build green; dev boots without runtime errors. RH/admin sessions ainda funcionam (`useSessionTimeout` reads from `useAuthStore` direto + still receives `adminUser`/`isAuthenticated`/`logout`).

**ROLLBACK:** Se Step 5 quebrar, restore via `git checkout HEAD -- src/store/adminAuthStore.ts`. Depois investigue o consumer pendente.

**Pitfall 7:** zero impacto — adminAuthStore.ts não tinha console.*; sua deleção não afeta o grep.
  </action>
  <verify>
    <automated>test ! -f src/store/adminAuthStore.ts && echo "DELETED OK"; npm run test:run -- src/store/__tests__/found12.test.ts 2>&1 | tail -10; npm run lint 2>&1 | grep -c "error TS"; npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/store/adminAuthStore.ts` exits 0 (file no longer exists)
    - `npm run test:run -- src/store/__tests__/found12.test.ts` exits 0 (both grep guard tests GREEN)
    - `grep -rn "from\s*['\"]@/store/adminAuthStore" src/ --include='*.ts' --include='*.tsx' | grep -v __tests__` returns 0 lines
    - `grep -rn "from\s*['\"]\./store/adminAuthStore" src/ --include='*.ts' --include='*.tsx' | grep -v __tests__` returns 0 lines
    - `npm run lint 2>&1 | grep -c "error TS"` outputs `<= 320` (baseline preserved — no net-new errors from deletion)
    - `npm run build` exits 0
    - `npm run test:run -- src/features/auth/utils/__tests__/pitfall7.grep.test.ts` exits 0 (preserved)
    - `npm run test:run -- src/store/__tests__/authStore.test.ts src/components/__tests__/RoleGuard.test.tsx` exits 0 (Wave 1 tests still GREEN — deletion did not regress)
  </acceptance_criteria>
  <done>
    src/store/adminAuthStore.ts deleted; FOUND-12 grep guard GREEN (file gone + zero stale imports); tsc baseline preserved; build green; Wave 1 tests still GREEN; INT-WARNING-2 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inline selector ↔ unified store | App.tsx now reads role + isAuthenticated directly from useAuthStore — single source of truth, no shim layer |
| Hook consumer ↔ store API | useSessionTimeout consumes the same shape (`isAuthenticated`, `adminUser`, `logout`) directly from useAuthStore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.1-05 | Information Disclosure | adminAuthStore.ts deletion | mitigate | File removal eliminates a re-export shim that COULD permit accidental import drift in future code. Single source of truth simplifies audit. |
| T-4.1-15 | Tampering | LoginRHPage role gate | accept | Existing role gate (Plan 03-05 D-14 bounded polling 5×20ms) UNCHANGED in this plan; only doc-comment edited |
| T-4.1-16 | Spoofing | useSessionTimeout direct store consumer | accept | Hook still reads `adminUser` legacy alias from useAuthStore; identical shape; behavior preserved (30-min inactivity → logout via `logout()` action which calls supabase.auth.signOut) |
| T-4.1-17 | Elevation of Privilege | useIsAdminAuthenticated inline | mitigate | Selector reads `s.isAuthenticated && (s.role === 'rh' || s.role === 'administrador')` — same logic as the deleted shim; RH/admin gating preserved |
</threat_model>

<verification>
After Plan 04 completes:
1. `test ! -f src/store/adminAuthStore.ts` exits 0 (file deleted)
2. `npm run test:run -- src/store/__tests__/found12.test.ts` exits 0 (FOUND-12 GREEN)
3. `npm run test:run` exits 0 (full suite GREEN — no regressions)
4. `npm run build` exits 0
5. `npm run lint 2>&1 | grep -c "error TS"` outputs `<= 320`
6. `grep -rn "adminAuthStore" src/ --include='*.ts' --include='*.tsx' | grep -v __tests__` returns at most 1 match (LoginRHPage doc-comment historical reference — acceptable)
</verification>

<success_criteria>
- [ ] `src/store/adminAuthStore.ts` deleted from working tree
- [ ] App.tsx uses inline `useIsAdminAuthenticated` selector
- [ ] useSessionTimeout consumes useAuthStore directly
- [ ] LoginRHPage doc-comment no longer says "antigo adminAuthStore"
- [ ] FOUND-12 grep guard GREEN (both test cases)
- [ ] tsc baseline preserved at <= 320
- [ ] Production build passes
- [ ] No regression to Plan 02 hydration tests or Plan 03 submit handler tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-1-auth-hydration-fix/04.1-04-SUMMARY.md` summarizing:
- File deleted (4 hooks orphaned + their consumers — confirm zero stale imports)
- Migration diff (App.tsx selector + useSessionTimeout import)
- LoginRHPage doc-comment historical evolution
- FOUND-12 RED → GREEN transition logged
</output>
