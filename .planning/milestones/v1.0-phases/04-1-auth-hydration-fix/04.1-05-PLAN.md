---
phase: 4.1
plan: 05
type: execute
wave: 4
depends_on: [03, 04]
files_modified:
  - .planning/phases/04-1-auth-hydration-fix/04.1-UAT.md
  - .planning/phases/04-1-auth-hydration-fix/04.1-VERIFICATION.md
autonomous: false
requirements:
  - CAD-06
  - AUTH-01
  - VAGA-03
  - CAND-01
  - CAND-02
  - CAND-03
  - FOUND-12
gap_closure_targets:
  - INT-BLOCKER-1
  - INT-BLOCKER-2
  - INT-WARNING-2
  - INT-WARNING-3
  - FLOW-CADASTRO
  - FLOW-RECOVERY
  - FLOW-CANDIDATURA
must_haves:
  truths:
    - "Lint baseline preserved at or below 320 TS errors after Phase 4.1"
    - "Production build exits zero"
    - "Vitest full suite exits zero with all hydrateFromSession plus RoleGuard plus FOUND-12 plus Pitfall 7 plus existing tests green"
    - "Playwright auth-hydration spec runs with E2E_REAL_LOGIN and E2E_ALLOW_DB_WRITE env vars and SC-1 plus SC-2 green"
    - "Playwright unconditional spec SC-3 plus SC-4 runs in CI-like mode without env vars and is green"
    - "UAT runbook documents 4 manual scenarios with real-world evidence"
    - "Phase verification artifact asserts all 6 ROADMAP success criteria satisfied and traces 7 audit gap IDs to closure"
  artifacts:
    - path: ".planning/phases/04-1-auth-hydration-fix/04.1-UAT.md"
      provides: "Manual UAT runbook with 4 scenarios and sign-off section"
      contains: "Scenario 1 Cadastro fresh login"
    - path: ".planning/phases/04-1-auth-hydration-fix/04.1-VERIFICATION.md"
      provides: "Phase 4.1 verification artifact"
      contains: "INT-BLOCKER-1"
  key_links:
    - from: ".planning/phases/04-1-auth-hydration-fix/04.1-VERIFICATION.md"
      to: ".planning/v1.0-MILESTONE-AUDIT.md"
      via: "audit gap IDs"
      pattern: "INT-BLOCKER"
    - from: ".planning/phases/04-1-auth-hydration-fix/04.1-UAT.md"
      to: "live Supabase project"
      via: "real candidato and vaga seed"
      pattern: "candidato_id"
---

<objective>
Plan 05 fecha Phase 4.1 com a phase verification battery: UAT runbook (4 scenarios manuais), final automated battery (lint + build + vitest + playwright real-login), e o phase verification artifact `04.1-VERIFICATION.md` que traceia todos os 7 audit gap IDs + 6 ROADMAP success criteria. Esta plan TEM checkpoint humano (UAT exige Fernando — `autonomous: false`).

Purpose: Materializa o smoke-runtime gate (Phase 4 lição central D-25..D-28) — não basta gates verdes, o usuário precisa confirmar que a página é USABLE end-to-end. Este é o output que a milestone audit reclamou em Phase 4.

Output: 1 UAT runbook + 1 VERIFICATION.md + atualizações ao STATE.md/ROADMAP.md (orchestrator owned).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/04-1-auth-hydration-fix/04.1-RESEARCH.md
@.planning/phases/04-1-auth-hydration-fix/04.1-PATTERNS.md
@.planning/phases/04-1-auth-hydration-fix/04.1-VALIDATION.md
@.planning/phases/04-1-auth-hydration-fix/04.1-01-PLAN.md
@.planning/phases/04-1-auth-hydration-fix/04.1-02-PLAN.md
@.planning/phases/04-1-auth-hydration-fix/04.1-03-PLAN.md
@.planning/phases/04-1-auth-hydration-fix/04.1-04-PLAN.md
@./CLAUDE.md

# Phase 4 UAT runbook for reference structure
@.planning/phases/04-vagas-candidatura/04-08-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final automated verification battery (lint + build + Vitest + Playwright real-login)</name>
  <files>
    .planning/phases/04-1-auth-hydration-fix/04.1-VERIFICATION.md
  </files>
  <read_first>
    .planning/ROADMAP.md,
    .planning/v1.0-MILESTONE-AUDIT.md,
    .planning/phases/04-1-auth-hydration-fix/04.1-VALIDATION.md,
    .planning/phases/04-1-auth-hydration-fix/04.1-01-PLAN.md,
    .planning/phases/04-1-auth-hydration-fix/04.1-02-PLAN.md,
    .planning/phases/04-1-auth-hydration-fix/04.1-03-PLAN.md,
    .planning/phases/04-1-auth-hydration-fix/04.1-04-PLAN.md
  </read_first>
  <action>
**Step 1 — Capturar pre-phase tsc baseline (evidence preserved in summary):**

Antes de tudo (caso ainda não capturado em Plan 01-04 summary), rode:

```bash
cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento"
npm run lint 2>&1 | grep -c "error TS"
```

Esperado: `<= 320` (baseline FOUND-08 carryover de Phase 4.2). Se > 320, Phase 4.1 introduziu net-new TS errors — investigue qual plan e corrija ANTES de prosseguir.

**Step 2 — Production build:**

```bash
npm run build 2>&1 | tail -10
```

Esperado: build exits 0 (zero TS strict errors no caminho de build do Vite). Capture output para evidence.

**Step 3 — Full Vitest suite:**

```bash
npm run test:run 2>&1 | tail -15
```

Esperado: exits 0. Confirme contagem de testes passing >= contagem pre-Phase-4.1 (Phase 4 baseline 340 PASS / 1 FAIL conforme STATE.md). Phase 4.1 adiciona pelo menos 9 novos casos:
- 3 hydrateFromSession + 1 Pitfall 7 in authStore.test.ts
- 3 RoleGuard.test.tsx
- 2 found12.test.ts

Total esperado: >= 349 PASS.

**Step 4 — Playwright unconditional (SC-3, SC-4 + existing specs):**

```bash
npx playwright test e2e/auth-hydration.spec.ts -g "SC-3|SC-4" 2>&1 | tail -10
```

Esperado: SC-3 + SC-4 GREEN (env-gated SC-1 + SC-2 são `test.skip` sem variáveis).

**Step 5 — Playwright real-login (SC-1, SC-2, B-J12 — env-gated):**

REQUER variáveis de ambiente. Configure shell:

```bash
export E2E_REAL_LOGIN=1
export E2E_ALLOW_DB_WRITE=1
export TEST_USER_EMAIL=fernando@beautysmile.com.br
export TEST_USER_PASSWORD=<senha-do-Fernando>   # NÃO commitar
```

Rode:

```bash
npx playwright test e2e/auth-hydration.spec.ts e2e/candidatura-submit.spec.ts -g "SC-2|B-J12" 2>&1 | tail -15
```

Esperado: SC-2 + B-J12 GREEN — submit button enabled within 5s comprovando candidato hidratado após anon→login redirect.

Se SC-2 falhar com "Submit button still disabled at 5s timeout", a fix de Plan 02/03 não está completa — investigue logs do listener.

SC-1 (cadastro) requer fresh signup; opcional rodar nesta task se não há candidato test seed (será coberto por UAT manual em Task 2).

**Step 6 — Criar `.planning/phases/04-1-auth-hydration-fix/04.1-VERIFICATION.md`**

Estrutura completa do arquivo (use os valores capturados nos Steps 1-5).

Nota: <!-- FRONTMATTER-DELIMITER --> nas duas linhas abaixo deve ser substituído por três hifens literais (---) ao escrever o arquivo final. O sentinel evita conflito com o frontmatter do PLAN.md.

```markdown
<!-- FRONTMATTER-DELIMITER -->
phase: 4.1
slug: auth-hydration-fix-inserted
created: 2026-04-26
status: verified
nyquist_compliant: true
<!-- FRONTMATTER-DELIMITER -->

# Phase 4.1 — Verification Artifact

> Phase verification artifact closing INT-BLOCKER-1+2 + INT-WARNING-2+3 +
> 3 broken flows from `v1.0-MILESTONE-AUDIT.md`. Materializes the
> smoke-runtime gate (Phase 4 lição central D-25..D-28).

## Success Criteria — ROADMAP Phase 4.1

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | After /cadastro 4-step + auto-login, navigation lands on /candidato/perfil with candidato populated (no full-page reload) | ✓ verified | UAT scenario 1 (manual, captured in 04.1-UAT.md); SC-1 Playwright stub in e2e/auth-hydration.spec.ts |
| 2 | Anon /vagas/:slug → Candidatar → /auth/login?redirect=... → login → /candidato/candidatura/formulario/:slug with candidato populated; submit handler does NOT silent-return | ✓ verified | SC-2 Playwright (E2E_REAL_LOGIN=1) GREEN — submit button enabled within 5s; UAT scenario 2 |
| 3 | Recovery deeplink → setNewPassword → /candidato/perfil with candidato populated | ✓ verified | SC-3 Playwright (unconditional addInitScript) GREEN; UAT scenario 3 |
| 4 | JWT app_metadata.role missing → no infinite redirect loop on protected routes | ✓ verified | SC-4 Playwright (unconditional, mocked /rest/v1/) GREEN — loginRedirects <= 1 |
| 5 | src/store/adminAuthStore.ts literal status matches FOUND-12 text — file deleted | ✓ verified | Plan 04 deleted file; src/store/__tests__/found12.test.ts GREEN |
| 6 | New Playwright E2E covers full anonymous → login → submit candidatura path | ✓ verified | e2e/auth-hydration.spec.ts SC-2 + e2e/candidatura-submit.spec.ts B-J12 |

## Audit Gap Closures — `v1.0-MILESTONE-AUDIT.md`

| Gap ID | Status Pre-4.1 | Closure Mechanism | Status Post-4.1 |
|--------|----------------|-------------------|-----------------|
| INT-BLOCKER-1 | blocker | Plan 02: hydrateFromSession action + listener Pattern 1 (setTimeout(0)) → setSession no longer called by listener; fetchProfile populates candidato after every SIGNED_IN | ✓ closed |
| INT-BLOCKER-2 | blocker | Same as INT-BLOCKER-1 (root cause) + Plan 03: defense-in-depth await waitForCandidatoHydrated in submit handlers | ✓ closed |
| INT-WARNING-2 | warning | Plan 04: src/store/adminAuthStore.ts deleted + 2 imports migrated (App.tsx + useSessionTimeout.ts) | ✓ closed |
| INT-WARNING-3 | warning | Plan 02: RoleGuard fallbackTriedRef one-shot DB fallback when isAuthenticated && role===null | ✓ closed |
| FLOW-CADASTRO | broken | Plan 02 listener fix + Plan 03 await in CadastroMultiStepForm Step 4 | ✓ working (SC-1 + UAT-1) |
| FLOW-RECOVERY | broken | Plan 02 listener fix + Plan 03 await in RedefinirSenhaPage (happy + Pitfall 2 fallback) | ✓ working (SC-3 + UAT-3) |
| FLOW-CANDIDATURA | broken | Plan 02 listener fix + Plan 03 await in LoginCandidatoPage + FormularioCandidaturaPage replace silent return + disabled button | ✓ working (SC-2 + B-J12 + UAT-2) |

## Requirements Traceability

| Req ID | Phase 4.1 Coverage | Evidence |
|--------|--------------------|----------|
| FOUND-12 | Plan 04 (literal close — file deleted) | found12.test.ts GREEN; 04.1-04-SUMMARY.md |
| CAD-06 | Plan 03 (await hydration in cadastro Step 4) | UAT-1; existing CAD-06 logic preserved |
| AUTH-01 | Plan 03 (await hydration in LoginCandidatoPage) | UAT-2/SC-2; existing AUTH-01 error switch preserved |
| AUTH-04 | Plan 03 (await hydration in RedefinirSenhaPage happy + fallback) | UAT-3/SC-3 |
| VAGA-03 | Plan 03 (await hydration in LoginCandidatoPage + Form silent-return replaced) | UAT-2/SC-2 + B-J12 |
| CAND-01 | Plan 03 (FormularioCandidaturaPage disabled button + toast) | UAT-2/SC-2 + B-J12 |
| CAND-02 | Plan 03 (same surface as CAND-01) | UAT-2/SC-2 + B-J12 |
| CAND-03 | Plan 03 (same surface as CAND-01) | UAT-2/SC-2 + B-J12 |

## Automated Verification Battery — Final Output

> Captured 2026-04-26 (insira data real da execução).

```
$ npm run lint 2>&1 | grep -c "error TS"
<INSIRA NÚMERO>   # MUST be <= 320 (baseline)

$ npm run build 2>&1 | tail -3
<INSIRA OUTPUT — must show "vite build" exit 0>

$ npm run test:run 2>&1 | tail -5
<INSIRA OUTPUT — Test Files / Tests / passed counts>

$ npx playwright test e2e/auth-hydration.spec.ts e2e/candidatura-submit.spec.ts 2>&1 | tail -5
<INSIRA OUTPUT — passed / skipped counts>

$ E2E_REAL_LOGIN=1 E2E_ALLOW_DB_WRITE=1 npx playwright test e2e/auth-hydration.spec.ts -g "SC-2" 2>&1 | tail -5
<INSIRA OUTPUT — SC-2 PASS evidence>
```

## Pitfall 7 Preserved

```
$ npm run test:run -- src/features/auth/utils/__tests__/pitfall7.grep.test.ts 2>&1 | tail -3
<INSIRA OUTPUT — Test Files passed: 1, Tests passed: 1>
```

Surfaces touched in Phase 4.1 covered by `PHASE_3_AUTH_PATHS`:
- `src/store/authStore.ts` (existing)
- `src/components/RoleGuard.tsx` (added in Plan 01)
- `src/components/pages/LoginCandidatoPage.tsx` (existing)
- `src/components/pages/RedefinirSenhaPage.tsx` (existing)
- `src/features/auth/utils/waitForCandidatoHydrated.ts` (recursive scan via `src/features/auth`)

`src/components/pages/FormularioCandidaturaPage.tsx` covered by `PHASE_4_VAGAS_PATHS`.

## Lint Baseline (FOUND-08 carryover to Phase 4.2)

| Stage | Pre-4.1 | Post-4.1 | Net Change |
|-------|---------|----------|------------|
| TS errors | 320 | <INSIRA> | <INSIRA — must be 0 or negative> |

## Phase Sign-Off

- [ ] All 6 ROADMAP success criteria verified
- [ ] All 7 audit gap IDs closed
- [ ] Lint baseline preserved (<= 320)
- [ ] Build green
- [ ] Vitest suite green
- [ ] Playwright unconditional green
- [ ] Playwright real-login green (SC-2 + B-J12 minimum)
- [ ] UAT 4 scenarios PASS (Fernando sign-off in 04.1-UAT.md)
- [ ] STATE.md updated by orchestrator
- [ ] ROADMAP.md Phase 4.1 marked [x]

**Verifier:** Fernando + automated battery
**Date:** 2026-04-26
```

NÃO marque como verified até Task 2 (UAT) e Task 3 (final battery + sign-off) completarem.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | grep -c "error TS"; npm run build 2>&1 | tail -3; npm run test:run 2>&1 | tail -5; npx playwright test e2e/auth-hydration.spec.ts --list 2>&1 | tail -8; test -f .planning/phases/04-1-auth-hydration-fix/04.1-VERIFICATION.md && echo "VERIFICATION.md created"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint 2>&1 | grep -c "error TS"` outputs `<= 320` (lint baseline preserved)
    - `npm run build` exits 0
    - `npm run test:run` exits 0; output mentions >= 349 tests passed (340 baseline + at least 9 new from Phase 4.1)
    - `npx playwright test e2e/auth-hydration.spec.ts --list` lists 4 SC scenarios; SC-3 + SC-4 PASS without env vars
    - With `E2E_REAL_LOGIN=1 E2E_ALLOW_DB_WRITE=1` set: SC-2 PASSES (manual run; capture output)
    - File `.planning/phases/04-1-auth-hydration-fix/04.1-VERIFICATION.md` exists and contains the literal strings: `INT-BLOCKER-1`, `INT-BLOCKER-2`, `INT-WARNING-2`, `INT-WARNING-3`, `FLOW-CADASTRO`, `FLOW-RECOVERY`, `FLOW-CANDIDATURA`, `FOUND-12`
    - All 6 ROADMAP success criteria rows in 04.1-VERIFICATION.md table have a non-empty Evidence column
    - Pitfall 7 grep test still GREEN (`npm run test:run -- src/features/auth/utils/__tests__/pitfall7.grep.test.ts` exits 0)
  </acceptance_criteria>
  <done>
    Final automated battery captured (lint <= 320, build green, vitest >= 349 PASS, Playwright SC-3 + SC-4 green); SC-2 + B-J12 PASS with E2E_REAL_LOGIN=1; 04.1-VERIFICATION.md created with all 7 gap IDs traced and 6 success criteria documented (sign-off pending UAT in Task 2).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT runbook — 4 fresh-login scenarios with real Supabase</name>
  <what-built>
    Phase 4.1 implementation complete: hydrateFromSession action, App.tsx setTimeout(0) listener, RoleGuard one-shot fallback, waitForCandidatoHydrated utility, 4 submit handlers awaiting hydration, FormularioCandidaturaPage replaces silent return + disabled button, adminAuthStore.ts deleted, FOUND-12 grep guard GREEN.

    Automated battery passing (Plan 05 Task 1).

    Now Fernando must run 4 manual UAT scenarios against live Supabase to confirm the fix works END-TO-END and the page is USABLE (smoke-runtime gate per Phase 4 lição central D-25..D-28).
  </what-built>
  <how-to-verify>
**Setup (uma vez antes de iniciar UAT):**

1. Open http://localhost:3003 (`npm run dev` running).
2. DevTools → Application → Storage → "Clear site data" (full reset — closes all cookies + localStorage + sessionStorage). This is CRITICAL: bug only reproduces on fresh-login.
3. DevTools → Console panel open (watch for errors during flows).

**Scenario 1: FLOW-CADASTRO (CAD-06)**

1. Navigate to http://localhost:3003/cadastro.
2. Fill all 4 steps (Dados Pessoais, Endereço, Disponibilidade, Autorizações).
3. Use a fresh CPF + email (não duplique seed existente). LGPD checkbox marked.
4. Click "Finalizar cadastro" no Step 4.
5. Expected:
   - Toast: `Cadastro concluído! Bem-vindo(a), <PrimeiroNome>.`
   - Navigation lands on `/candidato/perfil`.
   - **Personal data fields are POPULATED** (nome_completo, email, etc — NOT empty).
   - Candidaturas list panel shows empty state (zero candidaturas — but panel is ENABLED, not disabled).
   - DevTools Console: zero red errors.
   - **No full-page reload required** to see the data.

PASS criterion: nome_completo visible on `/candidato/perfil` immediately after navigation, without F5.

**Scenario 2: FLOW-CANDIDATURA (anon → redirect → submit) — THE PRIMARY BUG**

1. Logout if logged in. Clear site data again.
2. Navigate to http://localhost:3003/vagas (anon).
3. Click any active vaga card → lands on `/vagas/<slug>`.
4. Click "Candidatar-se" (anon button) → redirects to `/auth/login?redirect=/candidato/candidatura/formulario/<slug>`.
5. Fill fernando@beautysmile.com.br + senha. Click "Entrar".
6. Expected:
   - Toast: `Login realizado com sucesso!`
   - Navigation lands on `/candidato/candidatura/formulario/<slug>`.
   - Page renders with vaga details + perguntas formulário visible.
   - **Submit button is INITIALLY disabled** (until hydration completes within ~3s) OR enabled IF hydration was already complete.
   - Within ~3-5s, button becomes enabled.
7. Upload PDF CV (< 5MB), answer perguntas.
8. Click "Enviar candidatura".
9. Expected:
   - **Submit handler does NOT silent-return** (this was the bug).
   - Toast confirming candidatura criada (or "voce ja se candidatou" if duplicate).
   - Navigation to `/candidato/perfil`.

PASS criterion: candidatura row appears in DB OR duplicate toast fires; submit was processed (not silent fail).

**Scenario 3: FLOW-RECOVERY (AUTH-04)**

1. Logout. Clear site data.
2. Navigate to http://localhost:3003/auth/esqueci-senha.
3. Enter fernando@beautysmile.com.br. Submit.
4. Open the email Gmail/inbox; click recovery link.
5. Land on `/auth/redefinir-senha`. Fill nova senha (forte).
6. Click "Alterar senha".
7. Expected:
   - Toast: `Senha alterada com sucesso.`
   - Navigation lands on `/candidato/perfil`.
   - **Personal data fields POPULATED** (same as Scenario 1).

PASS criterion: candidato.nome_completo visible on /candidato/perfil immediately.

**Scenario 4: INT-WARNING-3 (JWT role-null no loop) — DEFENSE TEST**

NOTE: Este scenario é difícil reproduzir manualmente sem desabilitar o JWT custom hook. SC-4 Playwright já cobre via mock. Você pode pular este scenario manual SE SC-4 está GREEN.

OPTIONAL manual reproduction:
1. Log in normally.
2. DevTools → Application → Local Storage → find `sb-isljnozzlvckrgjjbjwp-auth-token`.
3. Edit the JSON: locate `app_metadata` no `user` object dentro do JSON. Remove a chave `role`. Save.
4. Refresh the page on `/candidato/perfil`.
5. Expected:
   - Spinner aparece por ~200ms (LoadingDelay).
   - RoleGuard fallback dispara `initialize()` ONE-SHOT.
   - Eventualmente: ou hidrata via DB fallback (role volta para 'candidato' do DB), ou bouncea para `/auth/login` UMA vez (não loop).
   - **Browser DOES NOT enter infinite redirect loop** (verificar DevTools Network tab — deve haver no máximo 1-2 navegações para /auth/login).

PASS criterion: page settles within 5s; no infinite redirects.

---

**Fill out 04.1-UAT.md** as you run each scenario. Document:
- Real candidato_id + vaga_id from Studio query (sql panel)
- Console output (any unexpected errors)
- Screenshot OR DOM evidence (e.g., "nome_completo='Fernando Costa' visible at /candidato/perfil")
- PASS or FAIL per scenario; reason if FAIL

**Sign-off:** Type `UAT-APPROVED` after all 4 scenarios PASS, OR list FAIL details for fix iteration.
  </how-to-verify>
  <resume-signal>Type "UAT-APPROVED" once all 4 scenarios PASS and 04.1-UAT.md is filled with evidence; or describe failures for fix iteration.</resume-signal>
  <files>.planning/phases/04-1-auth-hydration-fix/04.1-UAT.md</files>
  <action>Fernando executa os 4 cenarios UAT manualmente conforme &lt;how-to-verify&gt; acima e preenche 04.1-UAT.md com evidence chain (candidato_id, vaga_id, console output, PASS/FAIL por cenario). Resume signal apos sign-off.</action>
  <verify>
    <automated>test -f .planning/phases/04-1-auth-hydration-fix/04.1-UAT.md && grep -q 'sign_off_by: fernando' .planning/phases/04-1-auth-hydration-fix/04.1-UAT.md</automated>
  </verify>
  <done>4 cenarios UAT executados; 04.1-UAT.md preenchido e sign-off por Fernando registrado; resume signal recebido pelo orchestrator.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize 04.1-UAT.md + 04.1-VERIFICATION.md sign-off</name>
  <files>
    .planning/phases/04-1-auth-hydration-fix/04.1-UAT.md,
    .planning/phases/04-1-auth-hydration-fix/04.1-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/04-1-auth-hydration-fix/04.1-UAT.md,
    .planning/phases/04-1-auth-hydration-fix/04.1-VERIFICATION.md,
    .planning/phases/04-vagas-candidatura/04-08-UAT.md
  </read_first>
  <action>
**Step 1 — Garantir que `04.1-UAT.md` existe** com a estrutura abaixo. Se Fernando não criou durante Task 2 checkpoint, crie agora preenchendo com seus dados de UAT-APPROVED resume signal.

```markdown
<!-- FRONTMATTER-DELIMITER -->
phase: 4.1
slug: auth-hydration-fix-inserted
created: 2026-04-26
status: completed
sign_off_by: fernando
sign_off_at: <ISO datetime>
<!-- FRONTMATTER-DELIMITER -->

# Phase 4.1 — UAT Runbook

> Manual UAT runbook closing the 3 broken flows from `v1.0-MILESTONE-AUDIT.md`
> per the smoke-runtime gate (Phase 4 lição central D-25..D-28).
> Executed against live Supabase project `isljnozzlvckrgjjbjwp` after
> Plan 04 deletion of adminAuthStore.ts.

## Pre-conditions

- npm run dev running (Vite dev server on http://localhost:3003)
- DevTools opened (Application + Console panels)
- "Clear site data" applied before EACH scenario (fresh-login is the WHOLE POINT)
- Test user: fernando@beautysmile.com.br

## Scenario 1: FLOW-CADASTRO (CAD-06)

**Steps:** Cadastro 4-step → tryAutoLogin → /candidato/perfil
**Status:** [PASS | FAIL]
**Evidence:**
- candidato_id: <UUID>
- nome_completo visible: <YES | NO>
- candidaturas panel state: <empty/enabled | disabled>
- console errors: <NONE | <list>>
- screenshot: (optional)

## Scenario 2: FLOW-CANDIDATURA (VAGA-03 + CAND-01..03) — Primary bug

**Steps:** Anon /vagas → /vagas/:slug → Candidatar → /auth/login?redirect → login → submit
**Status:** [PASS | FAIL]
**Evidence:**
- candidato_id: <UUID>
- vaga_id: <UUID>
- vaga slug: <slug>
- submit button enabled within: <Ns>
- candidatura row created: <YES + ID | NO>
- silent-fail observed: <YES | NO> (bug if YES)
- console errors: <NONE | <list>>

## Scenario 3: FLOW-RECOVERY (AUTH-04)

**Steps:** /auth/esqueci-senha → email link → /auth/redefinir-senha → /candidato/perfil
**Status:** [PASS | FAIL]
**Evidence:**
- email received: <YES + timestamp | NO>
- nova senha aceita: <YES | NO>
- nome_completo visible on /perfil: <YES | NO>
- console errors: <NONE | <list>>

## Scenario 4: INT-WARNING-3 (JWT role-null no loop)

**Steps:** Tamper localStorage to remove role → refresh → assert no loop
**Status:** [PASS | FAIL | SKIPPED — covered by SC-4 Playwright]
**Evidence:**
- redirects observed: <count>
- page settled within 5s: <YES | NO>
- spinner phase visible: <YES | NO>

## Sign-off

**Verified by:** Fernando
**Date:** 2026-04-26
**Decision:** [APPROVED | REJECTED — reason]
```

Substitua os placeholders com os valores capturados durante o UAT (Fernando deve ter passado-os no resume signal ou em discussion adjunta).

**Step 2 — Atualizar `04.1-VERIFICATION.md`**

- Mude `status: verified` (linha 4 do frontmatter — se já estava assim, OK).
- Preencha os blocos `<INSIRA ...>` com os valores reais:
  - tsc error count
  - npm run build output tail
  - npm run test:run output tail
  - npx playwright unconditional output tail
  - npx playwright SC-2 (real-login) output tail
- Marque todos os 8 itens em `## Phase Sign-Off` com `[x]`.
- Update final lines com `**Verifier:** Fernando + automated battery` e date real.

**Step 3 — Criar SUMMARY do plano**

Crie `.planning/phases/04-1-auth-hydration-fix/04.1-05-SUMMARY.md` resumindo:
- 5 plans executed (01..05) + waves (0..4)
- 7 audit gap IDs closed (link a VERIFICATION.md)
- 6 ROADMAP success criteria satisfied (link a UAT.md)
- 3 carryover decisions ou bugs identificados durante UAT (se algum) — para Phase 5 backlog
- Lint baseline final + delta (zero net-new ideal)
- Note ao orchestrator: marcar Phase 4.1 [x] em ROADMAP.md + atualizar STATE.md (orchestrator-owned, fora desta task).

**Step 4 — Não tocar ROADMAP.md / STATE.md / REQUIREMENTS.md aqui**

Esses arquivos são atualizados pelo orchestrator (`/gsd-progress` ou `/gsd-execute-phase --finalize`). Apenas deixe um item action no SUMMARY listando-os.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-1-auth-hydration-fix/04.1-UAT.md && test -f .planning/phases/04-1-auth-hydration-fix/04.1-VERIFICATION.md && test -f .planning/phases/04-1-auth-hydration-fix/04.1-05-SUMMARY.md && grep -c "APPROVED\|sign_off_by: fernando" .planning/phases/04-1-auth-hydration-fix/04.1-UAT.md && grep -c "status: verified" .planning/phases/04-1-auth-hydration-fix/04.1-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-1-auth-hydration-fix/04.1-UAT.md` exists with `status: completed` in frontmatter
    - 04.1-UAT.md contains all 4 scenarios (CADASTRO, CANDIDATURA, RECOVERY, INT-WARNING-3) with status filled (PASS or FAIL or SKIPPED)
    - File `.planning/phases/04-1-auth-hydration-fix/04.1-VERIFICATION.md` has `status: verified` in frontmatter
    - 04.1-VERIFICATION.md "Phase Sign-Off" checklist has all 8 items checked `[x]`
    - 04.1-VERIFICATION.md "Automated Verification Battery" section has actual output (no `<INSIRA>` placeholders left)
    - File `.planning/phases/04-1-auth-hydration-fix/04.1-05-SUMMARY.md` exists; lists the 7 gap IDs + 6 success criteria
    - `grep -c "INT-BLOCKER-1\\|INT-BLOCKER-2\\|INT-WARNING-2\\|INT-WARNING-3\\|FLOW-CADASTRO\\|FLOW-RECOVERY\\|FLOW-CANDIDATURA" .planning/phases/04-1-auth-hydration-fix/04.1-VERIFICATION.md` returns >= 7
    - `npm run lint 2>&1 | grep -c "error TS"` outputs `<= 320` (final lint baseline check)
    - `npm run build` exits 0
    - `npm run test:run` exits 0
  </acceptance_criteria>
  <done>
    UAT runbook fully filled with Fernando's sign-off; VERIFICATION.md `status: verified` + all 8 sign-off items checked + automated battery captured; SUMMARY.md authored; lint baseline + build + tests final-check green; ROADMAP.md / STATE.md updates left to orchestrator.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT runbook ↔ live Supabase | Fernando exercises real auth + DB writes against project `isljnozzlvckrgjjbjwp`; documented per Phase 4 evidence chain protocol |
| VERIFICATION.md ↔ ROADMAP.md success criteria | Verification artifact provides traceability matrix between code changes and the 6 declared phase success criteria + 7 audit gap IDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.1-18 | Information Disclosure | UAT runbook stores candidato_id + vaga_id | accept | UUIDs are non-PII identifiers; nome_completo (PII) NOT logged in 04.1-UAT.md (only verified visually). Phase 4 evidence chain established this convention. |
| T-4.1-19 | Tampering | E2E_TEST_PASSWORD env var | mitigate | Password lives in shell env / .env.local (already in .gitignore per Phase 3 D-19); never committed; never echoed in test output (Pitfall 7) |
| T-4.1-20 | Repudiation | Sign-off process | mitigate | 04.1-UAT.md has `sign_off_by: fernando` + `sign_off_at: <ISO datetime>`; git commit includes Co-Authored-By for traceability |
| T-4.1-21 | Information Disclosure | Test logs in 04.1-VERIFICATION.md | accept | Output captured is npm/vite/playwright stdout — no credentials emitted (Phase 3 + 4 redaction in service layer); double-check before commit |
</threat_model>

<verification>
After Plan 05 completes:
1. All 5 PLAN.md files have a corresponding SUMMARY.md in the phase directory
2. 04.1-UAT.md `status: completed`, signed by Fernando
3. 04.1-VERIFICATION.md `status: verified`, all 8 sign-off items checked
4. tsc baseline preserved: `npm run lint 2>&1 | grep -c "error TS"` outputs `<= 320`
5. Production build passes: `npm run build` exits 0
6. Full test suite passes: `npm run test:run` exits 0
7. Git working tree clean (or only documentation files modified for the orchestrator finalize step)
</verification>

<success_criteria>
- [ ] 04.1-UAT.md created with 4 scenarios documented + sign-off
- [ ] 04.1-VERIFICATION.md created and marked `status: verified`
- [ ] All 6 ROADMAP success criteria evidenced
- [ ] All 7 audit gap IDs traced to closure mechanism + post-status
- [ ] Lint baseline preserved (zero net-new TS errors)
- [ ] Build + Vitest + Playwright (unconditional + real-login) all green
- [ ] 04.1-05-SUMMARY.md exists referencing all 5 plans + carryover items if any
- [ ] Phase 4.1 ready for orchestrator to mark [x] in ROADMAP and update STATE.md
</success_criteria>

<output>
After completion, the orchestrator (`/gsd-execute-phase` finalize or `/gsd-progress`) will:
- Update `.planning/ROADMAP.md` Phase 4.1 entry to `[x] Phase 4.1` + completion date
- Update `.planning/STATE.md` Decisions log with Phase 4.1 closures (FOUND-12 literal, INT-BLOCKER-1+2 fix path, INT-WARNING-3 defense)
- Update `.planning/REQUIREMENTS.md` FOUND-12 row from `Pending — partial per audit + INT-WARNING-2` to `Complete (Phase 4.1)` with link to 04.1-VERIFICATION.md
- Commit with message including the 7 gap IDs closed
</output>
