---
phase: 4.1
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/components/pages/LoginCandidatoPage.tsx
  - src/components/pages/RedefinirSenhaPage.tsx
  - src/features/cadastro/components/CadastroMultiStepForm.tsx
  - src/components/pages/FormularioCandidaturaPage.tsx
autonomous: true
requirements:
  - CAD-06
  - AUTH-01
  - AUTH-04
  - VAGA-03
  - CAND-01
  - CAND-02
  - CAND-03
gap_closure_targets:
  - INT-BLOCKER-1
  - FLOW-CADASTRO
  - FLOW-RECOVERY
  - FLOW-CANDIDATURA
must_haves:
  truths:
    - "Submit handlers in 3 fresh-login pages (LoginCandidato, RedefinirSenha, CadastroStep4) await waitForCandidatoHydrated() before navigating to a candidato-only route"
    - "FormularioCandidaturaPage submit handler no longer returns silently when candidato is null — surfaces a pt-BR toast and the submit button is disabled until candidato hydrates"
    - "CadastroMultiStepForm preserves existing console.error in catch block (NOT in PHASE_3_AUTH_PATHS — not a Pitfall 7 violation)"
    - "RedefinirSenhaPage Pitfall 2 fallback (tryAutoLogin after session_expired) also awaits hydration before navigate"
  artifacts:
    - path: "src/components/pages/LoginCandidatoPage.tsx"
      provides: "signIn → waitForCandidatoHydrated → navigate flow (Pattern 2)"
      contains: "waitForCandidatoHydrated"
    - path: "src/components/pages/RedefinirSenhaPage.tsx"
      provides: "setNewPassword + Pitfall 2 fallback both await hydration before navigate"
      contains: "waitForCandidatoHydrated"
    - path: "src/features/cadastro/components/CadastroMultiStepForm.tsx"
      provides: "tryAutoLogin happy path awaits hydration before navigate (Pitfall 5)"
      contains: "waitForCandidatoHydrated"
    - path: "src/components/pages/FormularioCandidaturaPage.tsx"
      provides: "Replaces silent return with pt-BR toast + disables submit button until candidato hydrates"
      contains: "Sessão ainda carregando"
  key_links:
    - from: "src/components/pages/LoginCandidatoPage.tsx onSubmit"
      to: "@/features/auth/utils/waitForCandidatoHydrated"
      via: "await waitForCandidatoHydrated({ timeoutMs: 3000 }) after signIn before navigate"
      pattern: "waitForCandidatoHydrated"
    - from: "src/components/pages/FormularioCandidaturaPage.tsx submit button"
      to: "candidato store value"
      via: "disabled={!candidato || ...}"
      pattern: "disabled=\\{!candidato"
---

<objective>
Adiciona defense-in-depth nos 3 submit handlers que navegam imediatamente após auth state change (LoginCandidatoPage, RedefinirSenhaPage, CadastroMultiStepForm Step 4) e troca o silent-return em FormularioCandidaturaPage:233 por feedback explícito + button-disabled fail-safe. Esta é a camada complementar a Plan 02 (listener centralizado): submit handlers podem completar ANTES do `setTimeout(0)` ter resolvido a hidratação, então cada página crítica espera explicitamente.

Purpose: Fecha (no defense-in-depth layer) FLOW-CADASTRO, FLOW-RECOVERY, FLOW-CANDIDATURA. Mesmo que o listener Pattern 1 falhe ou demore, estes 4 sites garantem feedback ao usuário.

Output: 4 arquivos editados; ~10 LoC adicionadas, zero LoC removidas (preserve error handling existente).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-1-auth-hydration-fix/04.1-RESEARCH.md
@.planning/phases/04-1-auth-hydration-fix/04.1-PATTERNS.md
@.planning/phases/04-1-auth-hydration-fix/04.1-02-PLAN.md
@./CLAUDE.md

# Source files to modify (read each first to confirm current line numbers)
@src/components/pages/LoginCandidatoPage.tsx
@src/components/pages/RedefinirSenhaPage.tsx
@src/features/cadastro/components/CadastroMultiStepForm.tsx
@src/components/pages/FormularioCandidaturaPage.tsx

<interfaces>
<!-- Key types/exports the executor will need (extracted from codebase). -->

From src/features/auth/utils/index.ts (post Plan 02):
```
export { waitForCandidatoHydrated } from './waitForCandidatoHydrated'
```
Signature: `waitForCandidatoHydrated({ timeoutMs?: number } = {}): Promise<boolean>`
- Returns `true` when state.candidato becomes non-null AND state.role === 'candidato'
- Returns `false` on timeout OR when role !== 'candidato' (RH/admin path)

From src/components/pages/LoginCandidatoPage.tsx onSubmit (lines 110-123 PRE-Phase-4.1):
```
const onSubmit = async (data: LoginFormData) => {
  setLastError(null)
  try {
    await signIn({ email: data.email, senha: data.password, rememberMe: data.rememberMe ?? true })
    toast.success('Login realizado com sucesso!', { duration: 3000 })
    const target = resolveRedirect(searchParams.get('redirect'))
    navigate(target, { replace: true })
  } catch (err) { /* preserve linhas 124-176 — INVALID_CREDENTIALS / EMAIL_NOT_CONFIRMED / RATE_LIMITED / NETWORK_ERROR / SERVER_ERROR / default */ }
}
```

From src/components/pages/RedefinirSenhaPage.tsx onSubmit (lines 73-108 PRE-Phase-4.1):
```
const onSubmit = async (data: RedefinirSenhaFormData) => {
  try {
    await setNewPassword(data.nova_senha)
    toast.success('Senha alterada com sucesso.', { duration: 4000 })
    navigate('/candidato/perfil', { replace: true })
  } catch (err) {
    if (isAuthError(err)) {
      const looksExpired = err.code === 'SERVER_ERROR' && /sess(ã|a)o|expired|expirad/i.test(err.message)
      if (looksExpired && recovery.status === 'valid') {
        const ok = await tryAutoLogin(recovery.email, data.nova_senha)
        if (ok) {
          toast.success('Senha alterada com sucesso.', { duration: 4000 })
          navigate('/candidato/perfil', { replace: true })
          return
        }
        // ... fallback to /auth/login
      }
      // ... toast.error
    }
  }
}
```

From src/features/cadastro/components/CadastroMultiStepForm.tsx handleFormSubmit (lines 426-456 PRE-Phase-4.1):
```
await cadastrarCandidato(result.data)
const loggedIn = await tryAutoLogin(result.data.dadosPessoais.email, result.data.dadosPessoais.senha)
draft.clear()
setSubmitSuccess(true)
const primeiroNome = result.data.dadosPessoais.nome_completo.split(' ')[0]
if (loggedIn) {
  toast.success(`Cadastro concluído! Bem-vindo(a), ${primeiroNome}.`, { duration: 5000 })
  navigate('/candidato/perfil', { replace: true })
} else {
  toast.success('Cadastro concluído. Faça login para continuar.', { duration: 5000 })
  navigate('/auth/login?email=' + encodeURIComponent(result.data.dadosPessoais.email))
}
```

From src/components/pages/FormularioCandidaturaPage.tsx onSubmit (line 232-234 PRE-Phase-4.1):
```
const onSubmit = async (data: CandidaturaFormData) => {
  if (!cvFile || !user || !candidato || !vaga) return  // ⚠ silent return — the bug
  // ... existing submit logic preserved (linhas 235-440)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add waitForCandidatoHydrated to LoginCandidato + RedefinirSenha + Cadastro Step 4 submit handlers</name>
  <files>
    src/components/pages/LoginCandidatoPage.tsx,
    src/components/pages/RedefinirSenhaPage.tsx,
    src/features/cadastro/components/CadastroMultiStepForm.tsx
  </files>
  <read_first>
    src/components/pages/LoginCandidatoPage.tsx,
    src/components/pages/RedefinirSenhaPage.tsx,
    src/features/cadastro/components/CadastroMultiStepForm.tsx,
    src/features/auth/utils/index.ts,
    .planning/phases/04-1-auth-hydration-fix/04.1-PATTERNS.md
  </read_first>
  <behavior>
    - Grep: each of the 3 files imports waitForCandidatoHydrated
    - Grep: each of the 3 files calls `await waitForCandidatoHydrated({ timeoutMs: 3000 })` BEFORE navigate
    - Grep: in RedefinirSenhaPage, BOTH happy path AND tryAutoLogin Pitfall 2 fallback await hydration before navigate
    - Behavioral (manual): after fresh login on each page, candidato is non-null on the destination route
    - No regression: error handling switch in LoginCandidatoPage (INVALID_CREDENTIALS / EMAIL_NOT_CONFIRMED / RATE_LIMITED / NETWORK_ERROR / SERVER_ERROR) preserved unchanged
  </behavior>
  <action>
**Arquivo 1: `src/components/pages/LoginCandidatoPage.tsx`**

Adicione import + uma linha `await waitForCandidatoHydrated()` no onSubmit happy path.

Step 1.1 — Adicionar import (perto dos outros imports `@/features/auth/utils/...`):

```
import { waitForCandidatoHydrated } from '@/features/auth/utils'
```

Step 1.2 — Editar onSubmit (linhas 110-123). Insira a linha de await ENTRE `await signIn(...)` e `toast.success(...)`:

ANTES (linhas 113-118):
```
      await signIn({
        email: data.email,
        senha: data.password,
        rememberMe: data.rememberMe ?? true,
      })
      toast.success('Login realizado com sucesso!', { duration: 3000 })
```

DEPOIS:
```
      await signIn({
        email: data.email,
        senha: data.password,
        rememberMe: data.rememberMe ?? true,
      })
      // Phase 4.1 (RESEARCH §Pattern 2): defense-in-depth. Listener Pattern 1
      // (App.tsx) hydrates via setTimeout(0); submit handler may navigate
      // BEFORE fetchProfile resolves. Block until candidato populated OR
      // hard timeout (3s) — degrades gracefully (RoleGuard catches role).
      await waitForCandidatoHydrated({ timeoutMs: 3000 })
      toast.success('Login realizado com sucesso!', { duration: 3000 })
```

NÃO modifique:
- O bloco `try` envelope.
- O bloco `catch (err)` (linhas 124-176) — todo o switch de error codes preservado.
- `resolveRedirect(searchParams.get('redirect'))` (linha 122).
- `navigate(target, { replace: true })` (linha 123).

**Arquivo 2: `src/components/pages/RedefinirSenhaPage.tsx`**

Adicione import + DUAS linhas de await (happy path + Pitfall 2 fallback path).

Step 2.1 — Adicionar import:

```
import { waitForCandidatoHydrated } from '@/features/auth/utils'
```

Step 2.2 — Editar onSubmit happy path (linhas 73-79). Insira linha de await ENTRE `await setNewPassword(...)` e `toast.success(...)`:

ANTES:
```
  const onSubmit = async (data: RedefinirSenhaFormData) => {
    try {
      await setNewPassword(data.nova_senha)
      // Happy path: usuário continua autenticado via PASSWORD_RECOVERY
      // session (RESEARCH §Pitfall 2). Toast + navigate imediato (D-12).
      toast.success('Senha alterada com sucesso.', { duration: 4000 })
      navigate('/candidato/perfil', { replace: true })
```

DEPOIS:
```
  const onSubmit = async (data: RedefinirSenhaFormData) => {
    try {
      await setNewPassword(data.nova_senha)
      // Happy path: usuário continua autenticado via PASSWORD_RECOVERY
      // session (RESEARCH §Pitfall 2). Toast + navigate imediato (D-12).
      // Phase 4.1: aguarda hidratação antes de navegar para evitar
      // /candidato/perfil renderizar com candidato=null.
      await waitForCandidatoHydrated({ timeoutMs: 3000 })
      toast.success('Senha alterada com sucesso.', { duration: 4000 })
      navigate('/candidato/perfil', { replace: true })
```

Step 2.3 — Editar Pitfall 2 fallback (linhas 84-93). Insira linha de await DENTRO do `if (ok)` ENTRE `tryAutoLogin` retorno e `toast.success(...)`:

ANTES:
```
        if (looksExpired && recovery.status === 'valid') {
          const ok = await tryAutoLogin(recovery.email, data.nova_senha)
          if (ok) {
            toast.success('Senha alterada com sucesso.', { duration: 4000 })
            navigate('/candidato/perfil', { replace: true })
            return
          }
```

DEPOIS:
```
        if (looksExpired && recovery.status === 'valid') {
          const ok = await tryAutoLogin(recovery.email, data.nova_senha)
          if (ok) {
            // Phase 4.1: aguarda hidratação no fallback path tambem.
            await waitForCandidatoHydrated({ timeoutMs: 3000 })
            toast.success('Senha alterada com sucesso.', { duration: 4000 })
            navigate('/candidato/perfil', { replace: true })
            return
          }
```

NÃO modifique nenhuma outra parte do `catch`. NÃO modifique a navegação fallback `navigate('/auth/login', { replace: true })` (linha 97).

**Arquivo 3: `src/features/cadastro/components/CadastroMultiStepForm.tsx`**

Adicione import + uma linha de await no `if (loggedIn)` happy path.

Step 3.1 — Adicionar import (perto dos outros imports `@/features/auth/...`):

```
import { waitForCandidatoHydrated } from '@/features/auth/utils'
```

Step 3.2 — Editar handleFormSubmit (linhas 437-440). Insira linha de await DENTRO do `if (loggedIn)` ANTES de `toast.success(...)`:

ANTES:
```
      if (loggedIn) {
        toast.success(`Cadastro concluído! Bem-vindo(a), ${primeiroNome}.`, { duration: 5000 })
        navigate('/candidato/perfil', { replace: true })
      } else {
```

DEPOIS:
```
      if (loggedIn) {
        // Phase 4.1 (RESEARCH §Pitfall 5): tryAutoLogin retorna true ANTES
        // do listener SIGNED_IN ter rodado o callback. Aguarda hidratação
        // até `candidato !== null` para evitar /candidato/perfil render
        // com fields vazios.
        await waitForCandidatoHydrated({ timeoutMs: 3000 })
        toast.success(`Cadastro concluído! Bem-vindo(a), ${primeiroNome}.`, { duration: 5000 })
        navigate('/candidato/perfil', { replace: true })
      } else {
```

NÃO modifique:
- `await cadastrarCandidato(result.data)` (linha 426).
- `tryAutoLogin` chamada (linhas 428-431).
- `draft.clear()` + `setSubmitSuccess(true)` + `primeiroNome` derivação (linhas 433-435).
- O `else` branch (linhas 440-442).
- O `catch (err)` block (linhas 444-450) — `console.error('[CADASTRO] ...')` linha 448 PRESERVADO (cadastro não está em `PHASE_3_AUTH_PATHS`, não viola Pitfall 7).

**Pitfall 7 cross-check:** LoginCandidatoPage e RedefinirSenhaPage estão em `PHASE_3_AUTH_PATHS`; ambos não devem adicionar `console.*`. CadastroMultiStepForm está em `src/features/cadastro/components/` — fora dos paths cobertos.
  </action>
  <verify>
    <automated>grep -c "waitForCandidatoHydrated" src/components/pages/LoginCandidatoPage.tsx; grep -c "waitForCandidatoHydrated" src/components/pages/RedefinirSenhaPage.tsx; grep -c "waitForCandidatoHydrated" src/features/cadastro/components/CadastroMultiStepForm.tsx; npm run test:run -- src/features/auth/utils/__tests__/pitfall7.grep.test.ts 2>&1 | tail -8; npm run lint 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "waitForCandidatoHydrated" src/components/pages/LoginCandidatoPage.tsx` returns >= 2 (import + call)
    - `grep -c "waitForCandidatoHydrated" src/components/pages/RedefinirSenhaPage.tsx` returns >= 3 (import + happy path + fallback path)
    - `grep -c "waitForCandidatoHydrated" src/features/cadastro/components/CadastroMultiStepForm.tsx` returns >= 2 (import + call)
    - The line `await signIn({` still appears in LoginCandidatoPage.tsx — error handling switch unchanged
    - In RedefinirSenhaPage: `grep -c "tryAutoLogin" src/components/pages/RedefinirSenhaPage.tsx` returns >= 1 (Pitfall 2 fallback preserved)
    - `npm run test:run -- src/features/auth/utils/__tests__/pitfall7.grep.test.ts` exits 0 (no new console.* in candidato pages)
    - `npm run lint 2>&1 | grep -c "error TS"` outputs `<= 320` (baseline preserved)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    3 submit handlers await waitForCandidatoHydrated before navigate; RedefinirSenhaPage covers both happy and Pitfall 2 fallback paths; cadastro console.error in catch preserved (not a Pitfall 7 violation); lint baseline + build green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace silent return in FormularioCandidaturaPage onSubmit + disable submit button when candidato is null</name>
  <files>
    src/components/pages/FormularioCandidaturaPage.tsx
  </files>
  <read_first>
    src/components/pages/FormularioCandidaturaPage.tsx,
    .planning/phases/04-1-auth-hydration-fix/04.1-PATTERNS.md,
    .planning/phases/04-1-auth-hydration-fix/04.1-RESEARCH.md
  </read_first>
  <behavior>
    - Grep: onSubmit no longer has `if (!cvFile || !user || !candidato || !vaga) return` as a silent guard
    - Grep: pt-BR toast `Sessão ainda carregando` appears in onSubmit error path
    - Grep: submit button has `disabled={...}` expression including `!candidato`
    - Behavioral: clicking submit when candidato is null shows toast + does NOT silent-return
    - Behavioral: existing submit happy path (CV upload + EF + redirect) unchanged
  </behavior>
  <action>
Edite `src/components/pages/FormularioCandidaturaPage.tsx`. Substitua o silent return + disable o submit button.

**Step 1 — Substituir silent return** (linha 232-234)

ANTES:
```
  const onSubmit = async (data: CandidaturaFormData) => {
    if (!cvFile || !user || !candidato || !vaga) return

    let uploadedPath: string | null = cvPath
    try {
```

DEPOIS:
```
  const onSubmit = async (data: CandidaturaFormData) => {
    // Phase 4.1 (RESEARCH §Pattern 2 + §Pitfall 1): replace silent return
    // by explicit toast feedback. Distinguishes between session-not-yet-hydrated
    // (the actual bug) and missing CV (UX clarity).
    if (!candidato || !user) {
      toast.error('Sessão ainda carregando, tente novamente em instantes', {
        duration: 4000,
      })
      return
    }
    if (!cvFile) {
      toast.error('Anexe seu currículo antes de enviar', { duration: 4000 })
      return
    }
    if (!vaga) {
      toast.error('Vaga não encontrada. Atualize a página.', { duration: 4000 })
      return
    }

    let uploadedPath: string | null = cvPath
    try {
```

NÃO modifique:
- O try/catch envelope (linhas 236+).
- A lógica de upload + EF + N8N (linhas 237-440).
- O `handleLogout` (linhas 220-230).

**Step 2 — Disable submit button when candidato is null**

Localize o JSX do `<GlassButton type="submit" ...>` no render. (Pode estar em múltiplos elementos — apenas o submit button em torno de "Enviar candidatura" precisa ser editado.)

Use grep para encontrar a linha exata:

```
grep -n 'Enviar candidatura' src/components/pages/FormularioCandidaturaPage.tsx
```

Esperado: linha do botão de submit. Edite o `disabled` prop para incluir `!candidato`. Padrão geral:

ANTES (assumindo o pattern atual):
```
<GlassButton
  type="submit"
  disabled={cvUploading || form.formState.isSubmitting}
  ...
>
  Enviar candidatura
</GlassButton>
```

DEPOIS:
```
<GlassButton
  type="submit"
  disabled={!candidato || !cvFile || cvUploading || form.formState.isSubmitting}
  ...
>
  Enviar candidatura
</GlassButton>
```

Se o `disabled` já contém uma expressão diferente, MERGE: o resultado deve ter `!candidato || !cvFile` no início, mantendo as outras condições existentes via `||`.

**IMPORTANTE — pt-BR + Pitfall 7:**
- Toast strings em pt-BR (CLAUDE.md §Idioma).
- FormularioCandidaturaPage está em `PHASE_4_VAGAS_PATHS` (pitfall7.grep.test.ts:51) — zero `console.*` adicionados; preserve.
- NÃO adicione novos `console.*` em qualquer parte do arquivo.

**Step 3 — Behavioral smoke**

Após salvar, rode `npm run dev` e simule manualmente:
1. Login fresh em outro browser/incógnito.
2. Navegue a `/candidato/candidatura/formulario/<slug-existente>`.
3. Antes da hidratação completar (window de ~50-200ms), tente clicar o submit button.
4. Esperado: button está disabled (não permitia click) OU toast aparece "Sessão ainda carregando".

Se possível, abra DevTools → Application → Local Storage → clear `auth-storage` + `sb-*` antes do test para garantir fresh state.
  </action>
  <verify>
    <automated>grep -c "if (!cvFile || !user || !candidato || !vaga) return" src/components/pages/FormularioCandidaturaPage.tsx; grep -c "Sessão ainda carregando" src/components/pages/FormularioCandidaturaPage.tsx; grep -c "disabled={!candidato" src/components/pages/FormularioCandidaturaPage.tsx; npm run test:run -- src/features/auth/utils/__tests__/pitfall7.grep.test.ts 2>&1 | tail -8; npm run lint 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "if (!cvFile || !user || !candidato || !vaga) return" src/components/pages/FormularioCandidaturaPage.tsx` returns 0 (silent return REMOVED)
    - `grep -c "Sessão ainda carregando" src/components/pages/FormularioCandidaturaPage.tsx` returns >= 1
    - `grep -c "Anexe seu currículo antes de enviar" src/components/pages/FormularioCandidaturaPage.tsx` returns >= 1
    - `grep -c "disabled={!candidato" src/components/pages/FormularioCandidaturaPage.tsx` returns >= 1 (submit button gated)
    - `npm run test:run -- src/features/auth/utils/__tests__/pitfall7.grep.test.ts` exits 0 (zero new console.*)
    - `npm run lint 2>&1 | grep -c "error TS"` outputs `<= 320` (baseline preserved)
    - `npm run build` exits 0
    - The original happy path submit logic (uploadCV → EF → redirect) is unchanged — verify by `grep -c "submitCandidaturaWithRespostas\\|uploadCV" src/components/pages/FormularioCandidaturaPage.tsx` returns the SAME count as before this task (search both pre and post — count must match)
  </acceptance_criteria>
  <done>
    Silent return replaced by 3 distinct pt-BR toasts (session-not-hydrated / no-CV / no-vaga); submit button disabled when candidato is null; existing submit happy path untouched; Pitfall 7 grep still GREEN; lint baseline + build green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Submit handler ↔ Zustand store | Submit handler reads candidato via store; awaits hydration via polling helper; trust assumed for in-process state |
| User input (CV file + form) ↔ Edge Function | Disabled-button + toast guards prevent submit before candidato hydrated; EF still validates server-side via Zod + auth.getUser() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.1-11 | Information Disclosure | Submit handlers | mitigate | Pitfall 7: zero `console.*` added in LoginCandidatoPage / RedefinirSenhaPage / FormularioCandidaturaPage (all in PHASE_3_AUTH_PATHS or PHASE_4_VAGAS_PATHS); pt-BR toast strings do not include credentials/tokens |
| T-4.1-12 | Denial of Service | Submit handler hard timeout (3s) | accept | waitForCandidatoHydrated has 3s hard cap — worst case: navigate sem hidratação; RoleGuard role gate still runs server-side via RLS; degraded but not exploitable |
| T-4.1-13 | Spoofing | Disabled-button defense | accept | Defense in depth UX only; server-side EF (submit-candidatura) validates `auth.getUser()` + IDOR cross-check + RLS — client-side disabled cannot widen privilege |
| T-4.1-14 | Tampering | Pitfall 2 fallback in RedefinirSenhaPage | mitigate | tryAutoLogin in fallback uses recovery.email + new password — same code path as Phase 3 (no regression); waitForCandidatoHydrated only blocks after successful signIn |
</threat_model>

<verification>
After Plan 03 completes:
1. `npm run test:run` exits 0 (all unit + Pitfall 7 grep tests GREEN)
2. `npm run build` exits 0
3. `npm run lint 2>&1 | grep -c "error TS"` outputs `<= 320` (baseline preserved)
4. `grep -c "waitForCandidatoHydrated" src/components/pages/LoginCandidatoPage.tsx src/components/pages/RedefinirSenhaPage.tsx src/features/cadastro/components/CadastroMultiStepForm.tsx | awk -F: '{sum+=$2} END {print sum}'` returns >= 7 (2 + 3 + 2)
5. Manual smoke: fresh-login fluxo (clear localStorage + login) — candidato.nome_completo aparece em /candidato/perfil sem reload
</verification>

<success_criteria>
- [ ] LoginCandidatoPage onSubmit awaits hydration before navigate (preserves error handling switch)
- [ ] RedefinirSenhaPage onSubmit awaits hydration in BOTH happy path AND Pitfall 2 fallback path
- [ ] CadastroMultiStepForm Step 4 awaits hydration after tryAutoLogin (Pitfall 5 mitigation)
- [ ] FormularioCandidaturaPage silent return replaced by 3 pt-BR toasts + disabled submit button
- [ ] tsc baseline preserved at <= 320 errors
- [ ] Production build (npm run build) passes
- [ ] Pitfall 7 grep still GREEN
- [ ] No regression to existing error handling, error code switches, or navigation paths
</success_criteria>

<output>
After completion, create `.planning/phases/04-1-auth-hydration-fix/04.1-03-SUMMARY.md` summarizing:
- 4 files modified (line counts diff)
- Smoke results from manual fresh-login test
- Confirmation that Pitfall 7 grep still GREEN
- Reminder for Plan 04: FOUND-12 deletion + import migrations
</output>
