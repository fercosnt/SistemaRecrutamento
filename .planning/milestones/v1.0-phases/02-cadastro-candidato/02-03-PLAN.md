---
phase: 02-cadastro-candidato
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - supabase/functions/_shared/schemas.ts
  - supabase/functions/_shared/constants.ts
  - supabase/functions/cadastrar-candidato/index.ts
autonomous: false
requirements: [CAD-03, CAD-05, CAD-07]

must_haves:
  truths:
    - "A POST to the redeployed cadastrar-candidato with invalid Zod input returns status 400 and body containing `error_code: 'VALIDATION'` and `message` and optional `field`"
    - "A POST with a duplicate email returns body containing `error_code: 'EMAIL_EXISTS'` and `field: 'email'`"
    - "A POST with a duplicate CPF returns `error_code: 'CPF_EXISTS'` and `field: 'cpf'`"
    - "A successful cadastro creates a row in public.autorizacoes with column `policy_version = 'v1.0-2026-04'`"
    - "curl -X OPTIONS on the function URL returns 200 with CORS headers (no 401 — redeploy used --no-verify-jwt)"
  artifacts:
    - path: "supabase/functions/_shared/constants.ts"
      provides: "Deno-side POLICY_VERSION constant used in autorizacoes insert"
      contains: "POLICY_VERSION = 'v1.0-2026-04'"
      min_lines: 5
    - path: "supabase/functions/_shared/schemas.ts"
      provides: "CadastroErrorCode enum + CadastroErrorResponse interface + zodPathToFieldName helper"
      contains: "export type CadastroErrorCode"
    - path: "supabase/functions/cadastrar-candidato/index.ts"
      provides: "Structured error_code responses + policy_version on autorizacoes insert"
      contains: "error_code"
      min_lines: 250
  key_links:
    - from: "supabase/functions/cadastrar-candidato/index.ts"
      to: "supabase/functions/_shared/constants.ts"
      via: "import POLICY_VERSION"
      pattern: "import.*POLICY_VERSION"
    - from: "supabase/functions/cadastrar-candidato/index.ts"
      to: "supabase/functions/_shared/schemas.ts"
      via: "import zodPathToFieldName"
      pattern: "zodPathToFieldName"
    - from: "autorizacoes INSERT payload"
      to: "autorizacoes.policy_version column"
      via: "literal key in insert object"
      pattern: "policy_version:\\s*POLICY_VERSION"
---

<objective>
Evolve the Edge Function `cadastrar-candidato` response contract from `{ ok, error }` to `{ ok, error_code, message, field?, error (legacy alias) }` per D-05/D-08, and include `policy_version` in the autorizacoes insert per D-16. Add shared `_shared/constants.ts` (Deno-side `POLICY_VERSION`) and extend `_shared/schemas.ts` with the error-code types and a `zodPathToFieldName` helper.

Then [BLOCKING] redeploy the function with `--no-verify-jwt` — this fixes Phase 1 UAT Bug 4 (function currently returns 401 at the gateway because the initial Phase 1 deploy did not set the flag).

Purpose: D-05, D-08, D-16, CAD-07, and unblock of CAD-DEPLOY-01 carryover.

Output: Two new/patched Deno-runtime files + one redeployed Edge Function in prod, contract-aligned with the client evolution happening in parallel (02-04, 02-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-cadastro-candidato/02-CONTEXT.md
@.planning/phases/02-cadastro-candidato/02-RESEARCH.md
@.planning/phases/02-cadastro-candidato/02-PATTERNS.md
@.planning/phases/02-cadastro-candidato/02-UI-SPEC.md
@.planning/phases/02-cadastro-candidato/02-02-SUMMARY.md
@.planning/phases/01-foundation-saneada/KNOWN-ISSUES-CARRYOVER-PHASE-3.md
@supabase/functions/cadastrar-candidato/index.ts
@supabase/functions/_shared/schemas.ts

<interfaces>
Current Deno imports in `_shared/schemas.ts` (top of file) — must be preserved, we ONLY append:
  import { z } from 'https://esm.sh/zod@3'
  export const cadastroCandidatoSchema = z.object({ ... })
  export type CadastroCandidatoInput = z.infer<typeof cadastroCandidatoSchema>

Current `jsonResponse` helper in `cadastrar-candidato/index.ts` (L57-62) — keep, and add `errorResponse` wrapper:
  const corsHeaders = { 'Access-Control-Allow-Origin': '*', ... }
  function jsonResponse(body: unknown, status: number): Response { ... }

Existing autorizacoes insert (L232-247) that we EXTEND with `policy_version: POLICY_VERSION`:
  await supabaseAdmin.from('autorizacoes').insert({
    candidato_id: candidatoId,
    user_id: userId,
    autorizacao_uso_dados: input.autorizacoes.autorizacao_uso_dados,
    autorizacao_comunicacao: input.autorizacoes.autorizacao_comunicacao ?? true,
    autorizacao_retencao_curriculo: input.autorizacoes.autorizacao_retencao_curriculo ?? true,
    autorizacao_analise_video: input.autorizacoes.autorizacao_analise_video ?? false,
    ip_aceite: ipAceite,
    data_aceite: new Date().toISOString(),
  })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create _shared/constants.ts and extend _shared/schemas.ts with error types</name>
  <files>supabase/functions/_shared/constants.ts, supabase/functions/_shared/schemas.ts</files>
  <read_first>
    - supabase/functions/_shared/schemas.ts (current 148 lines — do NOT modify cadastroCandidatoSchema, sub-schemas, or validateCPF; only ADD new exports at the end)
    - .planning/phases/02-cadastro-candidato/02-PATTERNS.md (sections for `_shared/schemas.ts` PATCH and `_shared/constants.ts` NEW)
    - .planning/phases/02-cadastro-candidato/02-RESEARCH.md Pathway 4 L740-755 (zodPathToFieldName leaf-mapping rule) and Pathway 7 L906-907 (POLICY_VERSION = 'v1.0-2026-04' mirror)
  </read_first>
  <action>
Create NEW file `supabase/functions/_shared/constants.ts` with this exact content:

  /**
   * Constants shared among Edge Functions (Deno runtime).
   *
   * IMPORTANT: POLICY_VERSION must match src/features/cadastro/constants.ts.
   * When bumping version, update BOTH files in the same commit and grep the
   * repo for the old value to catch stray references. (D-16, Phase 2)
   */
  export const POLICY_VERSION = 'v1.0-2026-04' as const

APPEND the following block at the END of `supabase/functions/_shared/schemas.ts` (preserve every existing line above; do NOT reorder or modify cadastroCandidatoSchema, enderecoSchema, disponibilidadeSchema, autorizacoesSchema, or validateCPF):

  // ============================================================================
  // Phase 2 — Structured error contract (D-05, D-08)
  // ============================================================================

  export type CadastroErrorCode =
    | 'EMAIL_EXISTS'
    | 'CPF_EXISTS'
    | 'VALIDATION'
    | 'SERVER_ERROR'

  // `error` is a LEGACY ALIAS of `message` kept during the Phase 2 → 3 transition
  // so old clients (pre-redeploy) do not see `undefined` when the server returns
  // the new contract. Drop `error` in Phase 3.
  export interface CadastroErrorResponse {
    ok: false
    error_code: CadastroErrorCode
    message: string
    field?: string
    error?: string
  }

  export interface CadastroSuccessResponse {
    ok: true
    data: {
      userId: string
      candidatoId: string
      disponibilidadeId?: string
      autorizacoesId?: string
    }
  }

  // Maps a Zod issue path (e.g. ['endereco', 'cep'] or
  // ['autorizacoes', 'autorizacao_uso_dados']) to a FLAT field name (the leaf
  // segment). Client consumer maps this flat name back to a nested RHF path via
  // FIELD_TO_STEP_PATH in cadastroService.ts. Returns undefined when the path is
  // empty or the leaf is a number (array index — no such case in current schema
  // but defensively handled).
  export function zodPathToFieldName(
    path: (string | number)[] | undefined,
  ): string | undefined {
    if (!path || path.length === 0) return undefined
    const leaf = path[path.length - 1]
    return typeof leaf === 'string' ? leaf : undefined
  }

Do NOT modify any existing export in `_shared/schemas.ts`.
  </action>
  <verify>
    <automated>test -f supabase/functions/_shared/constants.ts &amp;&amp; grep -q "POLICY_VERSION = 'v1.0-2026-04' as const" supabase/functions/_shared/constants.ts &amp;&amp; grep -q "export type CadastroErrorCode" supabase/functions/_shared/schemas.ts &amp;&amp; grep -q "export interface CadastroErrorResponse" supabase/functions/_shared/schemas.ts &amp;&amp; grep -q "export function zodPathToFieldName" supabase/functions/_shared/schemas.ts &amp;&amp; grep -q "export const cadastroCandidatoSchema" supabase/functions/_shared/schemas.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/functions/_shared/constants.ts` exists and contains literal `POLICY_VERSION = 'v1.0-2026-04' as const`
    - `supabase/functions/_shared/schemas.ts` contains all FOUR literal strings: `export type CadastroErrorCode`, `export interface CadastroErrorResponse`, `export interface CadastroSuccessResponse`, `export function zodPathToFieldName`
    - `supabase/functions/_shared/schemas.ts` STILL contains `export const cadastroCandidatoSchema` (existing export preserved)
    - `supabase/functions/_shared/schemas.ts` STILL contains `export const enderecoSchema` and `export const autorizacoesSchema`
  </acceptance_criteria>
  <done>
    Shared types/constants are ready for the `cadastrar-candidato` index.ts rewrite in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch cadastrar-candidato/index.ts for structured errors + policy_version</name>
  <files>supabase/functions/cadastrar-candidato/index.ts</files>
  <read_first>
    - supabase/functions/cadastrar-candidato/index.ts (ALL 271 lines — every response path must be rewritten; you need the full structure)
    - supabase/functions/_shared/schemas.ts (Task 1 added CadastroErrorCode + zodPathToFieldName — import from here)
    - supabase/functions/_shared/constants.ts (Task 1 created POLICY_VERSION — import from here)
    - .planning/phases/02-cadastro-candidato/02-CONTEXT.md D-05, D-08, D-16
    - .planning/phases/02-cadastro-candidato/02-RESEARCH.md Pathway 1 (structured error shape) + Pitfall 7 (password redaction)
    - .planning/phases/02-cadastro-candidato/02-PATTERNS.md § `supabase/functions/cadastrar-candidato/index.ts` (PATCH — full directive)
  </read_first>
  <action>
Add imports at the top of `supabase/functions/cadastrar-candidato/index.ts` (alongside existing imports — DO NOT remove any existing import):

  import { POLICY_VERSION } from '../_shared/constants.ts'
  import {
    cadastroCandidatoSchema,
    zodPathToFieldName,
    type CadastroCandidatoInput,
    type CadastroErrorCode,
  } from '../_shared/schemas.ts'

Near the existing `jsonResponse` function (around L57-62), ADD a new helper BELOW it:

  function errorResponse(
    code: CadastroErrorCode,
    message: string,
    field?: string,
    status = 400,
  ): Response {
    // `error` is a legacy alias for `message` — kept so pre-redeploy clients
    // do not crash on `undefined`. Drop in Phase 3.
    const body: Record<string, unknown> = { ok: false, error_code: code, message, error: message }
    if (field !== undefined) body.field = field
    return jsonResponse(body, status)
  }

REPLACE each of the existing `return jsonResponse({ ok: false, error: ... }, status)` call sites with an `errorResponse(...)` call mapped as follows:

  Method not supported (L93):
    OLD: return jsonResponse({ ok: false, error: 'Método não suportado' }, 405)
    NEW: return errorResponse('SERVER_ERROR', 'Método não suportado', undefined, 405)

  Zod validation fail (L86):
    OLD: if (!parsed.success) { const firstIssue = parsed.error.errors[0]
         const message = firstIssue?.message || 'Dados de cadastro inválidos'
         return jsonResponse({ ok: false, error: message }, 400) }
    NEW: if (!parsed.success) {
         const firstIssue = parsed.error.errors[0]
         const message = firstIssue?.message || 'Dados de cadastro inválidos'
         const field = zodPathToFieldName(firstIssue?.path)
         return errorResponse('VALIDATION', message, field) }

  JSON-parse fail (L106):
    OLD: return jsonResponse({ ok: false, error: 'Corpo da requisição inválido (JSON malformado)' }, 400)
    NEW: return errorResponse('VALIDATION', 'Corpo da requisição inválido (JSON malformado)')

  Env config missing (around L132 — the branch checking SUPABASE_URL / service role):
    OLD: return jsonResponse({ ok: false, error: 'Configuração do servidor indisponível' }, 500)
    NEW: return errorResponse('SERVER_ERROR', 'Configuração do servidor indisponível', undefined, 500)

  auth.admin.createUser fail — "already registered" branch (around L170):
    OLD: return jsonResponse({ ok: false, error: 'Este email já está cadastrado.' }, 400)
    NEW: return errorResponse('EMAIL_EXISTS', 'Este email já está cadastrado.', 'email')

  auth.admin.createUser fail — other reason:
    OLD: return jsonResponse({ ok: false, error: <message> }, 400)
    NEW: return errorResponse('SERVER_ERROR', <message>, undefined, 400)

  candidatos unique-violation branch (L173-187): REPLACE the existing block with:
    if (candidatoError || !candidatoRow) {
      await supabaseAdmin.auth.admin.deleteUser(userId).catch((rollbackErr) => {
        console.error('[cadastrar-candidato] rollback failed', { userId, rollbackErr })
      })
      const raw = candidatoError?.message ?? ''
      if (raw.toLowerCase().includes('cpf')) {
        return errorResponse('CPF_EXISTS', 'Este CPF já está cadastrado.', 'cpf')
      }
      if (raw.toLowerCase().includes('email')) {
        return errorResponse('EMAIL_EXISTS', 'Este email já está cadastrado.', 'email')
      }
      return errorResponse('SERVER_ERROR', 'Não foi possível registrar o candidato.')
    }

EXTEND the autorizacoes insert (the one at L232-247). Keep every existing key; ADD `policy_version: POLICY_VERSION` to the insert object. The resulting insert call must include exactly these keys in any order:
  candidato_id, user_id, autorizacao_uso_dados, autorizacao_comunicacao,
  autorizacao_retencao_curriculo, autorizacao_analise_video, ip_aceite,
  data_aceite, policy_version

PASSWORD REDACTION AUDIT (Pitfall 7): open the file, search for `console.log`, `console.info`, `console.debug`. For EACH log call, verify the logged object does NOT transitively include `password`, `senha`, `confirmar_senha`, `input.password`, or `rawBody`. If any such log exists, replace the logged value with a redacted form (e.g., `{ email: input.email, hasPassword: Boolean(input.password) }`). Do NOT add new logs.

Do NOT change:
  - The success response envelope `{ ok: true, data: { userId, candidatoId, disponibilidadeId?, autorizacoesId? } }` (still emitted on happy path)
  - The disponibilidade insert (best-effort, unchanged)
  - CORS headers
  - The Zod schema consumed from `_shared/schemas.ts`
  </action>
  <verify>
    <automated>grep -q "import { POLICY_VERSION }" supabase/functions/cadastrar-candidato/index.ts &amp;&amp; grep -q "zodPathToFieldName" supabase/functions/cadastrar-candidato/index.ts &amp;&amp; grep -q "errorResponse(" supabase/functions/cadastrar-candidato/index.ts &amp;&amp; grep -q "'EMAIL_EXISTS'" supabase/functions/cadastrar-candidato/index.ts &amp;&amp; grep -q "'CPF_EXISTS'" supabase/functions/cadastrar-candidato/index.ts &amp;&amp; grep -q "'VALIDATION'" supabase/functions/cadastrar-candidato/index.ts &amp;&amp; grep -q "'SERVER_ERROR'" supabase/functions/cadastrar-candidato/index.ts &amp;&amp; grep -q "policy_version: POLICY_VERSION" supabase/functions/cadastrar-candidato/index.ts &amp;&amp; ! grep -qE "console\.(log|info|debug).*senha" supabase/functions/cadastrar-candidato/index.ts &amp;&amp; ! grep -qE "console\.(log|info|debug).*rawBody" supabase/functions/cadastrar-candidato/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - File contains `import { POLICY_VERSION } from '../_shared/constants.ts'`
    - File contains `zodPathToFieldName` (imported AND called at least once)
    - File contains `function errorResponse(` (new helper defined)
    - File contains all four literal strings: `'EMAIL_EXISTS'`, `'CPF_EXISTS'`, `'VALIDATION'`, `'SERVER_ERROR'`
    - File contains `policy_version: POLICY_VERSION` inside the autorizacoes insert payload
    - File contains NO `console.log` / `console.info` / `console.debug` that references `senha`, `password`, `confirmar_senha`, or `rawBody` (grep negative assertion)
    - File STILL contains `{ ok: true, data:` (success envelope preserved)
    - File STILL contains `supabase.auth.admin.createUser` (Phase 1 signUp logic preserved)
    - Each response path that was `jsonResponse({ ok: false, error: ..., }, N)` is replaced — `grep -c "ok: false, error:" supabase/functions/cadastrar-candidato/index.ts` returns 0
  </acceptance_criteria>
  <done>
    The Edge Function source emits the Phase 2 structured contract + writes policy_version. Ready for redeploy in Task 3.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Redeploy cadastrar-candidato with --no-verify-jwt flag</name>
  <what-built>
    Tasks 1 and 2 rewrote the Deno source of `cadastrar-candidato` and added `_shared/constants.ts`. To make this live, the operator must run the Supabase CLI deploy command. The critical difference from the Phase 1 deploy is the `--no-verify-jwt` flag, which Phase 1 UAT Test 9 flagged as missing (Bug 4 in KNOWN-ISSUES-CARRYOVER-PHASE-3.md) — without it, the gateway returns 401 Unauthorized for anonymous cadastro calls.

    This is also the Pitfall 3 mitigation moment: deploy the Edge Function BEFORE the client bundle goes live with the new error mapping (Wave 3 form-wiring plan 02-06 is the next client-facing plan; this ordering is preserved by the wave structure).
  </what-built>
  <how-to-verify>
    From the repo root, run:
      npx supabase functions deploy cadastrar-candidato --no-verify-jwt --project-ref isljnozzlvckrgjjbjwp

    Expected output: `Deployed Function cadastrar-candidato on project isljnozzlvckrgjjbjwp`. If the CLI prompts for login or project link, authenticate via `npx supabase login` first. If deploy fails with a Deno import error, re-check that `_shared/constants.ts` and `_shared/schemas.ts` are sibling files under `supabase/functions/_shared/` (the CLI walks the dep graph based on relative imports `../_shared/...`).

    Then smoke-test with three curl calls (replace `<ANON_KEY>` with the project's sb_publishable_ anon key from Supabase Dashboard → Project Settings → API):

      # 1. CORS preflight — expect 200
      curl -i -X OPTIONS \
        -H "Origin: http://localhost:3003" \
        -H "Access-Control-Request-Method: POST" \
        https://isljnozzlvckrgjjbjwp.supabase.co/functions/v1/cadastrar-candidato

      # 2. Empty-body POST — expect 400 with error_code VALIDATION (NOT 401)
      curl -i -X POST \
        -H "Authorization: Bearer <ANON_KEY>" \
        -H "apikey: <ANON_KEY>" \
        -H "Content-Type: application/json" \
        -d '{}' \
        https://isljnozzlvckrgjjbjwp.supabase.co/functions/v1/cadastrar-candidato

      # 3. Malformed JSON — expect 400 with error_code VALIDATION
      curl -i -X POST \
        -H "Authorization: Bearer <ANON_KEY>" \
        -H "apikey: <ANON_KEY>" \
        -H "Content-Type: application/json" \
        -d 'not json' \
        https://isljnozzlvckrgjjbjwp.supabase.co/functions/v1/cadastrar-candidato

    Verify:
      - Call 1 status 200 (CORS OK; previous state was 401 — that was Bug 4)
      - Call 2 body JSON contains `"error_code":"VALIDATION"` AND `"error":"..."` (legacy alias present)
      - Call 3 body JSON contains `"error_code":"VALIDATION"` and `"message":"Corpo da requisição inválido (JSON malformado)"`

    Resume signal: `approved — edge function redeployed, smoke tests pass` OR paste the failing curl output.
  </how-to-verify>
  <resume-signal>Type "approved — edge function redeployed, smoke tests pass" OR paste the full curl failure output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous browser → Edge Function | `--no-verify-jwt` INTENTIONALLY disables JWT gateway check (cadastro is pre-auth); compensated by server-side Zod validation |
| Edge Function → Postgres (service_role) | Bypass RLS intentionally to insert into auth.users + candidatos + autorizacoes |
| Shared Deno module `_shared/*` | Imported by relative path; CLI upload includes the whole dep graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Spoofing | Edge Function response contract transition | mitigate | Dual-field output (`error_code` + `message` + legacy `error` alias) during the window where a client bundle may still be the Phase 1 version. Legacy `error` is a verbatim copy of `message` — old clients display correctly; new clients ignore `error` in favor of `error_code`. Drop `error` in Phase 3. |
| T-02-04 | Information Disclosure | Password / senha in Edge Function logs | mitigate | Task 2 Pitfall 7 audit: grep all `console.*` calls, assert no `senha` / `password` / `rawBody` appears in log arguments. Negative grep in acceptance criteria. |
| T-02-05 | Tampering | `policy_version` field | mitigate | D-16: written by server only (import from `_shared/constants.ts`). Client CANNOT pass a policy_version value; the Zod schema does not accept it as input. Server-side is authoritative. |
| T-02-06 | DoS | Anonymous Edge Function abuse (no JWT required) | accept | `--no-verify-jwt` is intentional for signup. Supabase Auth rate-limits `auth.admin.createUser` ~30/hour/IP (per supabase.com/docs/guides/auth/rate-limits). DDoS beyond that requires paid-plan protections; out of MVP scope. |
| T-02-07 | Tampering | Legacy `error` alias leaking sensitive server state | mitigate | `errorResponse()` helper sets `error` = `message`. Both are pre-sanitized pt-BR cordial strings (D-08). No raw Postgres / SDK error leaks. |
</threat_model>

<verification>
After Task 3 completes:
- curl OPTIONS returns 200 (not 401 — Bug 4 fix verified)
- curl POST {} returns 400 with body JSON containing `"error_code":"VALIDATION"`
- curl POST `not json` returns 400 with body JSON containing `"error_code":"VALIDATION"` and message about JSON malformado
- Supabase Dashboard → Functions → cadastrar-candidato shows deployment timestamp within the last few minutes
</verification>

<success_criteria>
- D-05, D-08 response contract implemented and verified by smoke test
- D-16 policy_version inserted on every new autorizacoes row
- Phase 1 UAT Bug 4 resolved — Edge Function answers anonymous callers with 200/400, never 401 at the gateway
- Client evolution (02-05 services + 02-06 form wiring) can assume the new contract is live
</success_criteria>

<output>
After completion, create `.planning/phases/02-cadastro-candidato/02-03-SUMMARY.md`. Include:
- Output of the three curl smoke tests (redact the ANON_KEY value)
- Timestamp of the redeploy
- Any Pitfall 7 redactions applied to existing console.log calls (list the lines changed, before/after)
</output>
