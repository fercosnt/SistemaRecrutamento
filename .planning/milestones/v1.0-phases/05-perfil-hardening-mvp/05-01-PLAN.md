---
phase: 05-perfil-hardening-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .github/workflows/ci.yml
  - lighthouserc.js
  - e2e/a11y.spec.ts
  - e2e/perfil.spec.ts
  - e2e/job-application-flow.spec.ts
  - src/components/__tests__/ErrorBoundaryRoot.test.tsx
  - tests/guards/devnav-gate.grep.test.ts
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03, HARD-04, HARD-06, PERF-01, PERF-02]
user_setup:
  - service: github-actions
    why: "Real CI green check for HARD-01 (D-03) — the deterministic-core Tier-1 specs run mocked, no Supabase secrets needed. Tier-2 live secrets are a follow-up (Plan 05-04 documents them); this plan ships the Tier-1 unconditional pipeline."
    dashboard_config:
      - task: "After merge, confirm the CI workflow appears under the repo Actions tab and the first run is green (Tier-1 only — no secrets required)."
        location: "GitHub repo → Actions tab"

must_haves:
  truths:
    - "(D-03) A real GitHub Actions workflow (`.github/workflows/ci.yml`) runs Vitest + Playwright on push/PR — an actual green check, not a local runbook"
    - "(D-05) LHCI is wired into the CI pipeline with an >80 (minScore 0.8) Performance + Accessibility assertion budget; `lhci autorun --help` resolves and `lighthouserc.js` is a valid module"
    - "(D-04) `e2e/job-application-flow.spec.ts` no longer exists (legacy PRD-0005 duplicate pruned)"
    - "(D-04) Running `npm run test:e2e -- e2e/perfil.spec.ts` executes; Tier-2 scenarios are explicitly skip-with-reason, never fixme"
    - "(D-01) The perfil E2E scaffold (`e2e/perfil.spec.ts`) asserts real candidaturas + personal data render on `/candidato/perfil` (PERF-01/02 no-mock contract; verify+polish, no rework)"
    - "Running `npm run test:e2e -- e2e/a11y.spec.ts` executes (RED is acceptable in Wave 0 — the spec compiles and runs against real routes)"
    - "`@lhci/cli` and `@axe-core/playwright` are devDependencies in package.json at the researched versions"
    - "(D-10) A Vitest grep guard fails if the DevNav DEV gate is removed from App.tsx"
    - "(D-09) A Vitest render test asserts an ErrorBoundary catches a thrown child (scaffold for the HARD-03 App-root hoist in 05-03)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions pipeline: unit (lint + vitest) + e2e (playwright chromium) + lighthouse jobs"
      contains: "npm run lint"
    - path: "lighthouserc.js"
      provides: "LHCI config — preview server, mobile preset, minScore 0.8 Perf+A11y on public routes"
      contains: "minScore"
    - path: "e2e/a11y.spec.ts"
      provides: "axe-core per-route WCAG A/AA scan loop"
      contains: "AxeBuilder"
    - path: "e2e/perfil.spec.ts"
      provides: "PERF-01/02 E2E — Tier-1 mock + Tier-2 env-gated"
      contains: "candidato/perfil"
    - path: "src/components/__tests__/ErrorBoundaryRoot.test.tsx"
      provides: "HARD-03 render-catch scaffold"
    - path: "tests/guards/devnav-gate.grep.test.ts"
      provides: "HARD-06 DevNav DEV-gate grep guard"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "npm run lint / npm run test:run / npx playwright test --project=chromium / lhci autorun"
      via: "job steps"
      pattern: "playwright test"
    - from: "lighthouserc.js"
      to: "npm run preview (port 4173)"
      via: "startServerCommand"
      pattern: "preview"
---

<objective>
Wave 0 — stand up the quality-gate infrastructure Phase 5 needs before any UI/auth work: a first-ever GitHub Actions CI pipeline (D-03), Lighthouse CI config (D-05), an axe-core a11y spec (D-08), a perfil E2E spec (D-01), install the two new dev packages, prune the legacy duplicate spec (D-04), and scaffold the ErrorBoundary-at-root + DevNav grep guards (D-09/D-10).

Purpose: Establish the Nyquist sampling infrastructure so downstream waves (design-system, perfil polish, a11y, DB hygiene, OTP) have automated verification commands to gate against. This is the VALIDATION.md "Wave 0 Requirements" list — every item here unblocks a downstream `<automated>` verify.

Output: `.github/workflows/ci.yml`, `lighthouserc.js`, `e2e/a11y.spec.ts`, `e2e/perfil.spec.ts`, ErrorBoundary root render test, DevNav grep guard; legacy `e2e/job-application-flow.spec.ts` deleted; two devDependencies installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-perfil-hardening-mvp/05-RESEARCH.md
@.planning/phases/05-perfil-hardening-mvp/05-PATTERNS.md
@.planning/phases/05-perfil-hardening-mvp/05-VALIDATION.md

<interfaces>
<!-- Verified codebase facts (PATTERNS.md confirmed) -->
- Vite env var names (resolves RESEARCH A1): `src/lib/supabase/client.ts:19-20` reads `import.meta.env.VITE_SUPABASE_URL` + `import.meta.env.VITE_SUPABASE_ANON_KEY`. CI workflow `env:` MUST use exactly these names.
- npm scripts (verified): `dev` (vite, port 3003), `build` (vite build → build/), `preview` (vite preview, port 4173), `lint` (tsc --noEmit), `test:run` (vitest run), `test:e2e` (playwright test).
- `playwright.config.ts:71` chromium project; `:88` webServer.command = `npm run dev` (port 3003, baseURL http://localhost:3003), `reuseExistingServer: !process.env.CI`.
- DevNav gate (verified present, HARD-06 verify-only): `src/App.tsx:221` → `{import.meta.env.DEV && <DevNavigationMenu />}`.
- Route under test: `/candidato/perfil` → `MeuPerfilCandidatoPage` (`src/router/routes.tsx:144`).
- Legacy prune target (verified present, 22.9 KB): `e2e/job-application-flow.spec.ts` (PRD-0005 duplicate of `e2e/candidatura-submit.spec.ts`).
- New packages (verified npm registry 2026-06-06): `@lhci/cli@^0.15.1`, `@axe-core/playwright@^4.11.3`. Both official Google/Deque — Package Legitimacy Audit Approved; no human checkpoint.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install LHCI + axe; prune legacy spec; author CI + LHCI config</name>
  <files>package.json, package-lock.json, .github/workflows/ci.yml, lighthouserc.js, e2e/job-application-flow.spec.ts</files>
  <read_first>
    - .planning/phases/05-perfil-hardening-mvp/05-RESEARCH.md §Q1 (CI YAML verbatim) + §Q2 (lighthouserc.js verbatim) — these are the source patterns (no in-repo analog for either file).
    - .planning/phases/05-perfil-hardening-mvp/05-PATTERNS.md §"NEW .github/workflows/ci.yml" + §"NEW lighthouserc.js" — confirmed env var names, lint-gate-296 invariant, chromium project flag, port 4173.
    - package.json (scripts block — confirm `lint`/`test:run`/`build`/`preview` names before referencing them in YAML).
    - playwright.config.ts (confirm webServer + chromium project before the e2e job).
    - e2e/candidatura-submit.spec.ts (confirm it is the surviving duplicate before deleting job-application-flow.spec.ts).
  </read_first>
  <action>
    Install dev packages: `npm install -D @lhci/cli@^0.15.1 @axe-core/playwright@^4.11.3` (Package Legitimacy Audit Approved — official Google/Deque, no checkpoint). Delete `e2e/job-application-flow.spec.ts` (D-04 legacy PRD-0005 duplicate; verify `e2e/candidatura-submit.spec.ts` covers the same flow first). Author `.github/workflows/ci.yml` from RESEARCH §Q1 with three jobs: (a) `unit` — checkout + setup-node@v4 node 20 cache npm + `npm ci` + `npm run lint` (enforces the 296 zero-growth invariant — lint failure = CI red) + `npm run test:run`; (b) `e2e` — `npm ci` + `npx playwright install --with-deps chromium` + `npm run build` + `npx playwright test --project=chromium` with `env: VITE_SUPABASE_URL/VITE_SUPABASE_ANON_KEY` from `secrets.TEST_SUPABASE_*` and `E2E_AUTH_TEST_USERS: ''` (Tier-2 OFF → live specs skip-with-reason) + upload-artifact playwright-report; (c) `lighthouse` — `npm ci` + `npm run build` + `npm install -g @lhci/cli@0.15.1` + `lhci autorun`. Trigger on `push` (branches main + `backup/**`) and `pull_request`. Author `lighthouserc.js` from RESEARCH §Q2: `startServerCommand: 'npm run preview'`, `startServerReadyPattern: 'Local:'`, urls on `http://localhost:4173/` for the PUBLIC routes only (`/auth/login`, `/cadastro`, `/vagas` — perfil is auth-gated, audited manually per D-01/A2), `numberOfRuns: 3`, mobile default preset, assertions `categories:performance` + `categories:accessibility` both `['error', { minScore: 0.8, aggregationMethod: 'optimistic' }]`. Do NOT VITE_-prefix any service_role (Pitfall 2 / CLAUDE.md).
  </action>
  <verify>
    <automated>npx lhci autorun --help >/dev/null 2>&1 && node -e "require('./lighthouserc.js')" && test ! -f e2e/job-application-flow.spec.ts && grep -q "npm run lint" .github/workflows/ci.yml && grep -q "minScore" lighthouserc.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('./lighthouserc.js')"` exits 0 (valid module).
    - `npx lhci autorun --help` resolves (LHCI installed).
    - `test -f e2e/job-application-flow.spec.ts` returns false (file deleted).
    - `grep -c "minScore: 0.8" lighthouserc.js` == 2 (Perf + A11y budgets).
    - `.github/workflows/ci.yml` contains exactly `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` env keys; `grep -c "service_role" .github/workflows/ci.yml` (excluding comments) == 0 in any `VITE_`-prefixed position.
    - `package.json` devDependencies include `@lhci/cli` and `@axe-core/playwright`.
    - `npm run lint` baseline holds: tsc error count == 296 (zero growth).
  </acceptance_criteria>
  <done>CI + LHCI configs authored from RESEARCH verbatim; legacy spec pruned; both packages installed; lint baseline 296 preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Author a11y + perfil E2E specs (Wave 0 scaffolds)</name>
  <files>e2e/a11y.spec.ts, e2e/perfil.spec.ts</files>
  <read_first>
    - .planning/phases/05-perfil-hardening-mvp/05-PATTERNS.md §"NEW e2e/a11y.spec.ts" + §"NEW e2e/perfil.spec.ts" — the exact analog line refs.
    - e2e/login-flow.spec.ts (makeJwt + page.route('**/auth/v1/token...') mock pattern at ~640-666; RHF onBlur + #id locators at ~676-680).
    - e2e/candidatura-submit.spec.ts (Tier-2 env-gating `test.skip(!process.env.E2E_REAL_LOGIN, '...')` at ~58-61, 100-105; TEST_USER + login helper at ~22-39; Sonner DOM contract at ~153-155).
    - src/router/routes.tsx (confirm /candidato/perfil → MeuPerfilCandidatoPage at line 144).
  </read_first>
  <action>
    Author `e2e/a11y.spec.ts` from PATTERNS §a11y verbatim: import `AxeBuilder` from `@axe-core/playwright`; loop `const routes = ['/auth/login', '/auth/esqueci-senha', '/cadastro', '/vagas']`; per route `page.goto(route)` then `new AxeBuilder({ page }).withTags(['wcag2a','wcag2aa','wcag21a','wcag21aa']).analyze()` and `expect(results.violations).toEqual([])`. These public routes run unconditionally (Tier-1, no Supabase). Auth-gated routes (perfil) are NOT in this loop — they go to Tier-2 in perfil.spec.ts or manual axe. Author `e2e/perfil.spec.ts`: Tier-1 mock block reusing `makeJwt` + `page.route('**/auth/v1/token?grant_type=password', ...)` from login-flow to render `/candidato/perfil` with mocked auth + mocked candidaturas SELECT, asserting personal data + at least one candidatura row + status/etapa/date labels render (PERF-01/02 no-mock-data smoke at the test layer); plus a Tier-2 block gated `test.skip(!process.env.E2E_REAL_LOGIN, 'Requires E2E_REAL_LOGIN=1 — real auth round-trip')` using the `login()` helper that does a real round-trip and asserts real candidaturas render. Use `#id` locators + `.blur()` after `.fill()` (Pitfall 5). Assert logout reachable via `getByRole('button', { name: /sair/i })` (HARD-05 overlap). Wave-0 RED is acceptable: the a11y spec WILL surface violations (those get fixed in 05-04) and perfil polish lands in 05-03 — the spec must COMPILE and RUN, the assertions are the contract for downstream green.
  </action>
  <verify>
    <automated>npx playwright test e2e/a11y.spec.ts e2e/perfil.spec.ts --project=chromium --list 2>&1 | grep -q "perfil" && grep -q "AxeBuilder" e2e/a11y.spec.ts && grep -q "E2E_REAL_LOGIN" e2e/perfil.spec.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list` includes a11y route-loop tests (4 routes) and perfil Tier-1 + Tier-2 tests.
    - `grep -c "test.fixme" e2e/perfil.spec.ts` == 0 (D-04: skip-with-reason, never fixme).
    - `e2e/perfil.spec.ts` Tier-2 block uses `test.skip(!process.env.E2E_REAL_LOGIN, ...)` with a non-empty reason string.
    - Specs compile under TS strict (no new tsc errors — baseline 296 holds; Playwright specs are excluded from the app tsconfig, so confirm `npx playwright test --list` parses without error).
  </acceptance_criteria>
  <done>Both E2E specs compile and list; a11y scans 4 public routes; perfil has Tier-1 mock + Tier-2 skip-with-reason; baseline 296 preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ErrorBoundary root render test + DevNav grep guard (HARD-03/HARD-06 scaffolds)</name>
  <files>src/components/__tests__/ErrorBoundaryRoot.test.tsx, tests/guards/devnav-gate.grep.test.ts</files>
  <behavior>
    - ErrorBoundaryRoot test: rendering a child that throws inside the chosen canonical ErrorBoundary shows the fallback UI (not a white screen / unhandled throw). This is RED until 05-03 hoists it to App root — the test asserts the component CATCHES, scaffolding HARD-03.
    - DevNav grep guard: a Vitest test reads `src/App.tsx` via node:fs and asserts the regex `import\.meta\.env\.DEV\s*&&\s*<DevNavigationMenu` matches (HARD-06 stays gated). Fails if the gate is removed.
  </behavior>
  <read_first>
    - src/components/ErrorBoundary.tsx AND src/features/cadastro/components/ErrorBoundary.tsx — TWO ErrorBoundary files exist; pick the canonical one (the richer `src/components/ErrorBoundary.tsx`, 8.3 KB, has the BeautySmileLogo fallback). Document the choice; 05-03 hoists THIS one and deletes/re-exports the other.
    - src/App.tsx:221 (the DevNav gate line to guard).
    - tests/ existing grep-guard precedent (e.g. pitfall7.grep test) for the node:fs + regex idiom — search `*.grep.test.ts` for the established pattern.
  </read_first>
  <action>
    Create `src/components/__tests__/ErrorBoundaryRoot.test.tsx`: render a `<ThrowingChild />` wrapped in the canonical `ErrorBoundary` (choose `src/components/ErrorBoundary.tsx` as canonical — richer fallback), assert the fallback heading/text renders via `@testing-library/react` + the `@testing-library/jest-dom` triple-slash shim (per Phase 4.1 [04.1-01] precedent for `toBeInTheDocument`). Suppress the expected React error-boundary console noise. Create `tests/guards/devnav-gate.grep.test.ts` following the established node:fs grep-guard idiom (mirror pitfall7.grep): read `src/App.tsx`, assert `/import\.meta\.env\.DEV\s*&&\s*<DevNavigationMenu/` matches. Both are Wave 0 scaffolds — the grep guard passes GREEN now (gate present); the ErrorBoundary render test passes GREEN now if the canonical boundary already catches (it does — it is a complete class component), and 05-03 will additionally mount it at App root.
  </action>
  <verify>
    <automated>npm run test:run -- ErrorBoundaryRoot devnav-gate 2>&1 | grep -qE "(passed|PASS)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- ErrorBoundaryRoot` passes (fallback UI renders on child throw).
    - `npm run test:run -- devnav-gate` passes (DevNav DEV gate present in App.tsx).
    - Canonical ErrorBoundary choice documented in the test file header comment (`src/components/ErrorBoundary.tsx`).
    - Lint baseline 296 preserved (`npm run lint` tsc error count unchanged).
  </acceptance_criteria>
  <done>ErrorBoundary render test + DevNav grep guard pass; canonical boundary chosen and documented for 05-03 hoist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → GitHub secrets | Test Supabase URL/anon key injected as env; service_role (if ever added) must never cross into the client bundle |
| CI app build → browser bundle | `VITE_`-prefixed vars are inlined into the shipped JS — only anon-safe values allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Information Disclosure | `.github/workflows/ci.yml` env block | mitigate | Only `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` (anon-safe) are `VITE_`-prefixed; service_role is NEVER `VITE_`-prefixed and not present in this plan's Tier-1 pipeline (Pitfall 2 / CLAUDE.md hard rule). Grep gate asserts zero `VITE_*SERVICE_ROLE`. |
| T-05-01-02 | Tampering | npm installs (`@lhci/cli`, `@axe-core/playwright`) | mitigate | Package Legitimacy Audit (RESEARCH) Approved both as official Google/Deque packages with canonical repos + high downloads. Not [ASSUMED]/[SUS] → no blocking human checkpoint required. |
| T-05-01-03 | Denial of Service | LHCI auditing dev server | accept | Mitigated by design: LHCI audits the `vite preview` production build (port 4173), not dev (Pitfall 1); no runtime user impact (CI-only). |
| T-05-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck unavailable in research session; both packages verified directly on npm registry as first-party — disposition Approved, no [ASSUMED]/[SUS] gate triggered. |
</threat_model>

<verification>
- `npm run lint` → tsc error count == 296 (zero-growth invariant).
- `npm run test:run -- ErrorBoundaryRoot devnav-gate` → all pass.
- `npx playwright test --list` → parses; a11y (4 routes) + perfil (Tier-1 + Tier-2) listed; no fixme in perfil.
- `node -e "require('./lighthouserc.js')"` → exits 0.
- `npx lhci autorun --help` → resolves.
- `test ! -f e2e/job-application-flow.spec.ts` → true.
</verification>

<success_criteria>
- CI pipeline authored with unit + e2e + lighthouse jobs; lint-296 gate present.
- LHCI config valid, mobile preset, minScore 0.8 Perf+A11y on public routes.
- a11y + perfil E2E specs compile/list; perfil uses skip-with-reason (no fixme).
- ErrorBoundary render test + DevNav grep guard pass.
- Legacy job-application-flow.spec.ts pruned; both dev packages installed.
- Lint baseline 296 preserved throughout.
</success_criteria>

<output>
Create `.planning/phases/05-perfil-hardening-mvp/05-01-SUMMARY.md` when done.
</output>
