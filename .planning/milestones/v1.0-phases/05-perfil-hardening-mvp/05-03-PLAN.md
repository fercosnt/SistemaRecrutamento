---
phase: 05-perfil-hardening-mvp
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/App.tsx
  - src/components/ErrorBoundary.tsx
  - src/features/cadastro/components/ErrorBoundary.tsx
  - src/store/authStore.ts
  - src/components/layouts/CandidatoNavbar.tsx
  - src/components/pages/MeuPerfilCandidatoPage.tsx
  - src/components/pages/VagasPublicasPage.tsx
  - src/components/pages/VagaDetalhePage.tsx
  - src/components/pages/FormularioCandidaturaPage.tsx
  - src/features/cadastro/hooks/useFormToast.ts
  - src/features/cadastro/components/steps/EnderecoStep.tsx
  - src/features/auth/services/passwordService.ts
  - src/components/pages/RedefinirSenhaPage.tsx
autonomous: false
requirements: [PERF-01, PERF-02, HARD-03, HARD-06]

must_haves:
  truths:
    - "(D-09) A React render error anywhere in the app shows the ErrorBoundary fallback instead of a white screen — the ErrorBoundary is hoisted to the App root (HARD-03)"
    - "(D-01) /candidato/perfil renders the candidate's real personal data + real candidaturas list (status + etapa + date), confirmed by smoke-runtime — verify+polish only, no structural rework"
    - "(D-10) DevNavigationMenu is hidden in a production build (DEV gate verified intact — HARD-06)"
    - "authStore.logout surfaces signOut failures so the page-level catch can show an honest error toast (WR-01-09 root fix)"
    - "The candidate persona navbar is a single shared component, not copy-pasted across 4 pages (WR-02-09)"
    - "(D-12) The CEP 'encontrado' toast fires once per successful lookup, not in a loop (F-04.1-B)"
    - "(D-12) A 422 transient on first setNewPassword shows a friendly retry toast instead of a raw error (F-04.1-E)"
  artifacts:
    - path: "src/App.tsx"
      provides: "ErrorBoundary wrapping the router/app root (HARD-03 / D-09)"
      contains: "ErrorBoundary"
    - path: "src/components/layouts/CandidatoNavbar.tsx"
      provides: "Extracted candidate persona navbar (WR-02-09)"
      exports: ["CandidatoNavbar"]
    - path: "src/store/authStore.ts"
      provides: "logout that surfaces signOut errors (WR-01-09)"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/ErrorBoundary.tsx"
      via: "wrap RouterProvider / Outlet"
      pattern: "ErrorBoundary"
    - from: "src/components/pages/MeuPerfilCandidatoPage.tsx"
      to: "src/components/layouts/CandidatoNavbar.tsx"
      via: "import + render CandidatoNavbar"
      pattern: "CandidatoNavbar"
---

<objective>
Wave 2 — perfil verify+polish (D-01) + mechanical hardening + carryover-debt closure. Hoist the ErrorBoundary to the App root (HARD-03/D-09), verify the DevNav DEV gate (HARD-06/D-10), verify-and-polish MeuPerfilCandidatoPage real-data render (PERF-01/02/D-01), fix the WR-01-09 logout-swallows-errors root cause + extract the duplicated WR-02-09 persona navbar into a shared `<CandidatoNavbar />`, and fix the two quick UX bugs (D-12): the CEP toast loop (F-04.1-B) and the 422-retry friendly toast (F-04.1-E).

Purpose: Close PERF-01/02 (the perfil page is already built — this is verify-and-polish, NO structural rework per D-01), the two mechanical HARD items, and the candidate-facing carryover debt deferred from Phase 4. Runs AFTER the design-system batch (05-02) so contrast/token fixes are already in place when polishing perfil.

Output: ErrorBoundary at root, verified perfil render, shared CandidatoNavbar, root-fixed logout, two UX bug fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-perfil-hardening-mvp/05-RESEARCH.md
@.planning/phases/05-perfil-hardening-mvp/05-PATTERNS.md
@.planning/phases/04-vagas-candidatura/04-REVIEW-09.md

<interfaces>
<!-- Verified codebase facts -->
- TWO ErrorBoundary files: src/components/ErrorBoundary.tsx (8.3 KB, richer fallback w/ BeautySmileLogo) and src/features/cadastro/components/ErrorBoundary.tsx (6.1 KB). 05-01 Task 3 chose src/components/ErrorBoundary.tsx as canonical. Hoist THAT one; delete/re-export the other.
- App.tsx RootLayout return (~218-225): <Outlet /> + {import.meta.env.DEV && <DevNavigationMenu />} (line 221) + <Toaster position="top-right" /> (line 223). D-09: wrap the RouterProvider/app-export level so router-construction errors are also caught. D-10: DevNav gate VERIFY-ONLY (already correct; 05-01 added the grep guard).
- WR-01-09 (04-REVIEW-09.md): authStore.logout (authStore.ts:397-401) wraps signOut in try/catch, console.error-swallows, always clearAuth, returns Promise<void> that never rejects -> the 4 page-level catches are dead code. ROOT FIX (option 1): re-throw (or return {error}) after clearAuth() so the 4 call-sites (MeuPerfil + Formulario + Vagas + VagaDetalhe) catch real failures.
- WR-02-09 (04-REVIEW-09.md): ~55-line persona navbar JSX copy-pasted in 4 pages (MeuPerfilCandidatoPage:344-389, FormularioCandidaturaPage:494-534, VagasPublicasPage:222-276, VagaDetalhePage:255-309) + candidatoIniciais + handleLogout helpers. Extract <CandidatoNavbar showAreaLink={true} /> to src/components/layouts/CandidatoNavbar.tsx (per 04-REVIEW-09 fix block). MeuPerfil uses showAreaLink={false} (it IS the area).
- F-04.1-B (CEP toast loop): src/features/cadastro/hooks/useFormToast.ts:185 cepFound() toast; fired from EnderecoStep via a useEffect whose dependency array changes per render. Fix: stabilize the effect (ref guard / correct deps) so cepFound fires once per successful lookup.
- F-04.1-E (422 retry): src/features/auth/services/passwordService.ts setNewPassword (line 123) calls updateUser; a 422 transient on first attempt recovers on retry. Fix: map the transient 422 to a friendly retry toast at the RedefinirSenhaPage layer (do NOT auto-retry silently — surface a "tente novamente" toast).
- D-01: MeuPerfilCandidatoPage already wires useCandidato() + useCandidaturas() (no mock). VERIFY+POLISH ONLY — no structural rework of listing or first-candidatura progresso block.
- Toast (Sonner) mounted at App.tsx:223; toast.error/toast.success idiom established.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hoist ErrorBoundary to App root + verify DevNav gate (HARD-03/HARD-06, D-09/D-10)</name>
  <files>src/App.tsx, src/components/ErrorBoundary.tsx, src/features/cadastro/components/ErrorBoundary.tsx</files>
  <read_first>
    - src/components/ErrorBoundary.tsx (the canonical boundary chosen in 05-01; confirm getDerivedStateFromError + componentDidCatch + fallback).
    - src/features/cadastro/components/ErrorBoundary.tsx (the duplicate — decide delete vs re-export; check its importers first via grep).
    - src/App.tsx (full file — RootLayout ~218-225 + the top-level App export with RouterProvider; line 221 DevNav gate).
    - src/components/__tests__/ErrorBoundaryRoot.test.tsx (the 05-01 scaffold — the hoist must keep it green).
    - .planning/phases/05-perfil-hardening-mvp/05-PATTERNS.md "MODIFY src/App.tsx" (prefer wrapping at RouterProvider/app-export level).
  </read_first>
  <action>
    Wrap the app root in the canonical `<ErrorBoundary>` from src/components/ErrorBoundary.tsx — prefer wrapping at the top-level App export around `<RouterProvider router={router} />` so router-construction and provider errors are also caught (D-09). Before deleting/re-exporting src/features/cadastro/components/ErrorBoundary.tsx, grep its importers: if cadastro code imports it, convert that file to a re-export of the canonical one (or migrate the imports), then delete the duplicate body. Verify the DevNav gate at App.tsx:221 is unchanged (D-10 verify-only; the 05-01 grep guard enforces it). Confirm the 05-01 ErrorBoundaryRoot render test still passes.
  </action>
  <verify>
    <automated>grep -q "ErrorBoundary" src/App.tsx && npm run test:run -- ErrorBoundaryRoot devnav-gate && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx imports and renders the canonical ErrorBoundary wrapping the router/app root.
    - `npm run test:run -- ErrorBoundaryRoot` passes; `npm run test:run -- devnav-gate` passes (DEV gate intact).
    - The duplicate cadastro ErrorBoundary is either deleted (zero importers) or converted to a re-export — no orphaned import errors.
    - SMOKE-RUNTIME: temporarily throw in a child (or trust the render test) — confirm fallback renders, not a white screen. Record evidence.
    - Lint baseline 296 preserved.
  </acceptance_criteria>
  <done>ErrorBoundary hoisted to app root; DevNav gate verified; duplicate boundary resolved; scaffolds green; lint 296.</done>
</task>

<task type="auto">
  <name>Task 2: Root-fix logout (WR-01-09) + extract CandidatoNavbar (WR-02-09)</name>
  <files>src/store/authStore.ts, src/components/layouts/CandidatoNavbar.tsx, src/components/pages/MeuPerfilCandidatoPage.tsx, src/components/pages/VagasPublicasPage.tsx, src/components/pages/VagaDetalhePage.tsx, src/components/pages/FormularioCandidaturaPage.tsx</files>
  <read_first>
    - src/store/authStore.ts:397-401 (logout action — the swallow-and-clearAuth body).
    - .planning/phases/04-vagas-candidatura/04-REVIEW-09.md WR-01-09 (fix option 1: re-throw after clearAuth) + WR-02-09 (the CandidatoNavbar extraction fix block w/ showAreaLink prop).
    - src/components/pages/MeuPerfilCandidatoPage.tsx:344-389 (canonical persona navbar + candidatoIniciais + handleLogout — the reference shell).
    - src/components/pages/VagasPublicasPage.tsx:222-276, src/components/pages/VagaDetalhePage.tsx:255-309, src/components/pages/FormularioCandidaturaPage.tsx:494-534 (the 3 duplicate shells).
  </read_first>
  <action>
    WR-01-09 root fix: in src/store/authStore.ts logout, after clearAuth(), re-throw (or return {error}) so signOut failures propagate. Keep the internal console.error but stop swallowing — the contract becomes "logout may reject on signOut failure; clearAuth always runs first." WR-02-09: create src/components/layouts/CandidatoNavbar.tsx exporting `CandidatoNavbar({ showAreaLink = true })` per the 04-REVIEW-09 fix block — selects isAuthenticated/role/candidato/logout from useAuthStore, returns null if not a logged candidato, renders the sticky Glass navbar + BeautySmileLogo + Avatar(iniciais) + nome/email + "Area do candidato" link (gated by showAreaLink) + "Sair" GlassButton with a handleLogout that NOW does a real try/catch (toast.error on the re-thrown logout failure). Replace the 4 duplicated shells: MeuPerfilCandidatoPage uses `<CandidatoNavbar showAreaLink={false} />`; Vagas/VagaDetalhe/Formulario use `<CandidatoNavbar />`. Remove the now-dead inline candidatoIniciais/handleLogout helpers from the 4 pages. Use BeautySmileLogo with the canonical type reconciled in 05-02. Keep D-01: NO structural rework of perfil's listing/progresso — only the navbar swaps to the shared component.
  </action>
  <verify>
    <automated>grep -q "export function CandidatoNavbar\|export const CandidatoNavbar" src/components/layouts/CandidatoNavbar.tsx && grep -rl "CandidatoNavbar" src/components/pages/MeuPerfilCandidatoPage.tsx src/components/pages/VagasPublicasPage.tsx src/components/pages/VagaDetalhePage.tsx src/components/pages/FormularioCandidaturaPage.tsx && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layouts/CandidatoNavbar.tsx exports CandidatoNavbar with a showAreaLink prop.
    - All 4 pages import + render CandidatoNavbar; the inline ~55-line persona-shell JSX is removed from each (grep confirms the duplicated block no longer appears 4x).
    - authStore.logout re-throws/returns error on signOut failure (no longer returns an always-resolved void); grep confirms the swallow `return` was changed.
    - SMOKE-RUNTIME: load /vagas + /candidato/perfil — navbar renders identically (logo, avatar initials, nome, Sair); clicking Sair logs out. Record evidence.
    - npm run build exits 0; lint baseline 296.
  </acceptance_criteria>
  <done>logout surfaces errors at the store; CandidatoNavbar extracted and consumed by all 4 pages; duplicated shells removed; build green; lint 296.</done>
</task>

<task type="auto">
  <name>Task 3: Quick UX bug fixes — CEP toast loop (F-04.1-B) + 422 retry toast (F-04.1-E), D-12</name>
  <files>src/features/cadastro/hooks/useFormToast.ts, src/features/cadastro/components/steps/EnderecoStep.tsx, src/features/auth/services/passwordService.ts, src/components/pages/RedefinirSenhaPage.tsx</files>
  <read_first>
    - src/features/cadastro/hooks/useFormToast.ts:185-186 (cepFound/cepNotFound toasts).
    - src/features/cadastro/components/steps/EnderecoStep.tsx (the useEffect that fires cepFound — find the dependency array causing per-render re-fire, line ~177 area).
    - src/features/cadastro/hooks/useViaCEP.ts (the CEP lookup callback wiring).
    - src/features/auth/services/passwordService.ts:123-145 (setNewPassword + updateUser error handling).
    - src/components/pages/RedefinirSenhaPage.tsx (where setNewPassword is called + where to surface the friendly retry toast).
    - STATE.md [04.1-05] F-04.1-B + F-04.1-E descriptions.
  </read_first>
  <action>
    F-04.1-B: stabilize the CEP success toast so it fires ONCE per successful lookup, not on every render. In EnderecoStep, guard the cepFound() call with a ref (e.g. lastCepRef) or fix the useEffect dependency array so it only re-fires when the resolved CEP actually changes — not when an unstable dependency (inline object/callback) changes identity per render. F-04.1-E: at the RedefinirSenhaPage layer, detect the 422 transient from setNewPassword (the error surfaced by passwordService) and show a friendly pt-BR toast ("Houve um erro temporario. Tente novamente.") prompting the user to retry, instead of a raw/unhelpful error. Do NOT silently auto-retry (the user must re-submit). Preserve the existing Pitfall-7 redaction in passwordService (never log novaSenha) and the D-09 anti-enumeration neutral copy.
  </action>
  <verify>
    <automated>npm run test:run && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - EnderecoStep cepFound() is guarded so it cannot fire on a re-render without a new resolved CEP (ref guard or corrected deps present; grep/read confirms).
    - RedefinirSenhaPage surfaces a friendly retry toast on a 422 from setNewPassword (string present, e.g. "Tente novamente").
    - SMOKE-RUNTIME: in cadastro, enter a valid CEP once — "CEP encontrado" toast appears exactly once (not looping). Record evidence.
    - passwordService still never logs novaSenha (Pitfall 7 intact); existing recovery vitest specs stay green.
    - npm run test:run passes (no regression); lint baseline 296.
  </acceptance_criteria>
  <done>CEP toast fires once per lookup; 422 transient shows a friendly retry toast; no regression; lint 296.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Smoke-runtime gate — perfil real data + navbar + UX fixes</name>
  <what-built>
    ErrorBoundary at root, perfil verify+polish, shared CandidatoNavbar across 4 pages, root-fixed logout, CEP toast + 422 retry fixes. Phase 4.1-inherited smoke-runtime gate (D-01 verification of real-data render is the central PERF-01/02 acceptance).
  </what-built>
  <how-to-verify>
    1. Run `npm run build && npm run preview`. Log in as a candidate who has at least one candidatura.
    2. Visit /candidato/perfil — confirm: real personal data renders (nome, email, etc. — NOT placeholder/mock), and the candidaturas list shows real status badge + etapa label + inscricao date for each application (PERF-01/02). The "PROGRESSO NO PROCESSO SELETIVO" block reflects the first candidatura.
    3. Confirm the persona navbar (logo, avatar initials, nome, Sair) renders identically on /candidato/perfil, /vagas, /vagas/:slug, and the candidatura form.
    4. Click "Sair" — confirm logout works and (if you can simulate a network failure) an error toast appears on real failure (WR-01-09).
    5. In /cadastro Endereco step, enter a valid CEP — confirm "CEP encontrado" toast appears exactly ONCE (F-04.1-B).
    6. Confirm DevNavigationMenu is NOT visible in this production preview build (HARD-06).
  </how-to-verify>
  <resume-signal>Type "approved" if perfil shows real data, navbar is consistent, UX fixes work, and DevNav is hidden — or describe any mock data / broken navbar / looping toast.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase Auth | logout signOut crosses to GoTrue; a swallowed failure leaves a stale local session with no user feedback |
| RLS-scoped read → perfil render | useCandidaturas SELECT is RLS-protected; perfil renders only the logged candidate's own rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Repudiation | authStore.logout swallowing signOut errors (WR-01-09) | mitigate | Re-throw after clearAuth so a real signOut failure surfaces an honest error toast; clearAuth always runs first so local session is cleared regardless. |
| T-05-03-02 | Information Disclosure | passwordService setNewPassword logging | mitigate | Preserve Pitfall-7 redaction — never log novaSenha; the 422 retry toast carries no secret/token. |
| T-05-03-03 | Information Disclosure | perfil real-data render | accept | useCandidaturas is RLS-scoped to the logged candidate; no cross-tenant leakage (RLS enforced server-side per CLAUDE.md). |
| T-05-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. N/A. |
</threat_model>

<verification>
- npm run test:run passes (ErrorBoundaryRoot + devnav-gate + recovery specs green; no regression).
- grep ErrorBoundary in App.tsx matches; CandidatoNavbar exported + consumed by 4 pages.
- authStore.logout re-throws on failure.
- npm run build exits 0; lint baseline 296.
- Human smoke-runtime gate (Task 4) approved: real perfil data, consistent navbar, working UX fixes, DevNav hidden.
</verification>

<success_criteria>
- ErrorBoundary at app root (HARD-03); DevNav DEV-gated verified (HARD-06).
- /candidato/perfil shows real personal data + real candidaturas (PERF-01/02, D-01 verify+polish).
- WR-01-09 logout root-fixed; WR-02-09 CandidatoNavbar extracted across 4 pages.
- F-04.1-B CEP toast fires once; F-04.1-E 422 friendly retry toast.
- Smoke-runtime gate approved; lint baseline 296 preserved.
</success_criteria>

<output>
Create `.planning/phases/05-perfil-hardening-mvp/05-03-SUMMARY.md` when done.
</output>
