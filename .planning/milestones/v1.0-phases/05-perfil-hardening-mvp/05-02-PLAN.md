---
phase: 05-perfil-hardening-mvp
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/styles/globals.css
  - src/components/ui/input.tsx
  - src/components/ui/select.tsx
  - src/components/ui/glass.tsx
  - src/components/BeautySmileLogo.tsx
  - src/components/pages/MeuPerfilCandidatoPage.tsx
autonomous: false
requirements: [HARD-04, PERF-02]

must_haves:
  truths:
    - "(D-07) bg-primary resolves to rgb(0, 16, 158) at runtime (not transparent / not invalid hsl(#hex)) — the bg-primary token mismatch is repaired at its source"
    - "(D-07) Every semantic Tailwind token (background, foreground, card, popover, secondary, muted, accent, destructive, border, input, ring) resolves to a valid color (channel-triplet form)"
    - "(D-02) The shared Input primitive renders dark-readable text on glass without a per-consumer hex patch — fixed once at the primitive, not per-page"
    - "(D-02) The shadcn Select trigger shows readable resting (non-hover) text — the F-04.1-A dark-on-dark dropdown defect is gone"
    - "(D-14) GlassButton centers icon+text children (inline-flex) — no left-shoved content"
    - "(D-14) BeautySmileLogo accepts the type value used at MeuPerfilCandidatoPage:350 without a TS error (type union reconciled)"
    - "(D-07) Candidate-facing hex-literal workarounds (bg-[#00109E] etc.) swept back to semantic tokens render identically (smoke-runtime confirmed)"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "Semantic design tokens as HSL channel triplets (D-07 root-cause fix)"
      contains: "--primary: 234 100% 31%"
    - path: "src/components/ui/glass.tsx"
      provides: "GlassButton with inline-flex items-center justify-center (D-14)"
      contains: "inline-flex"
    - path: "src/components/BeautySmileLogo.tsx"
      provides: "Reconciled type union covering all call-site values (D-14)"
  key_links:
    - from: "src/components/ui/input.tsx"
      to: "--foreground / --input-background tokens"
      via: "text-foreground / bg-input-background classes resolving post-fix"
      pattern: "text-foreground"
    - from: "tailwind.config.js"
      to: "src/styles/globals.css channel triplets"
      via: "hsl(var(--x)) wrapper"
      pattern: "hsl"
---

<objective>
Wave 1 — the design-system root-cause batch. Fix the bg-primary token break at its source (D-07/D-26): convert the entire semantic token block in globals.css from invalid hex to HSL channel triplets so hsl(var(--x)) resolves. Then the shared Input + Select primitives (D-02) render correctly WITHOUT per-page patches, the dark-font-on-dark-glass defects (F-04.1-A/C, F-04-08-G) collapse, GlassButton gets its inline-flex fix (D-14), BeautySmileLogo's type union is reconciled (D-14), and the candidate-facing hex-literal workarounds are swept back to semantic tokens.

Purpose: This is the single highest-leverage fix in Phase 5 — one file (globals.css) restores every broken semantic token at once, and fixing the primitives once cascades to all consumers. It MUST land before the a11y/LHCI gates so accessibility measures the FIXED UI, and before perfil polish (05-03) so contrast is correct.

Output: Repaired token system + corrected Input/Select/GlassButton/Logo primitives + candidate-facing hex sweep, each swept page smoke-runtime-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-perfil-hardening-mvp/05-RESEARCH.md
@.planning/phases/05-perfil-hardening-mvp/05-PATTERNS.md
@.claude/skills/beauty-smile-design-system

<interfaces>
<!-- Verified codebase facts (PATTERNS.md confirmed) -->
- Token break root cause: tailwind.config.js lines 15-56 wrap EVERY color in hsl(var(--x)); src/styles/globals.css:46-65 defines ALL semantic tokens as hex -> emits hsl(#hex) = invalid CSS = transparent. --primary: var(--brand-primary) -> hsl(#00109E) is the surfaced bug.
- Fix path (RESEARCH Q5 recommended): channel-triplet conversion in globals.css :root. Keep raw hex palette (--brand-*, --neutral-*) for direct-hex consumers; DECOUPLE semantic tokens to triplets. Channel triplet for #00109E = 234 100% 31%. Channel-triplet path PRESERVES opacity modifiers (bg-primary/90, relied on per D-25). Do NOT drop the hsl() wrapper in the config.
- Input primitive (src/components/ui/input.tsx:1-26): already references text-foreground, bg-input-background, border-input, placeholder:text-muted-foreground — broken until token fix. Fix-in-place, no rewrite.
- Select trigger (src/components/ui/select.tsx:51-57): resting data-[placeholder]:text-muted-foreground + value text dark-on-dark; heavy [&_*]:hover:!text-white !important cascade masks the break. After token fix, resting text resolves contrast-safe -> simplify the hover !important overrides.
- GlassButton (src/components/ui/glass.tsx:147-192, base classes ~168-185): missing inline-flex items-center justify-center gap-2. Add it — cascades to 9 consumers, no per-consumer patch.
- BeautySmileLogo (src/components/BeautySmileLogo.tsx:3-7): type?: 'icon' | 'horizontal' | 'vertical'. INVALID call site: MeuPerfilCandidatoPage.tsx:350 passes type="symbol" (WR-02-09 / STATE.md [04-08] deviation b). FIX: widen the union to include 'symbol' (mapped to the icon glyph) OR correct the call site to type="icon". Pick whichever keeps the rendered glyph byte-identical and document it.
- 136 bg-[#...] hex literals across src/ (verified grep). Sweep CANDIDATE-FACING pages only this phase (login, recovery, vagas, candidatura, perfil + candidate shell); RH/M2 pages keep working hex literals.
- ErrorBoundary fallback (src/components/ErrorBoundary.tsx) intentionally uses hardcoded hex (from-[#00109E]) — LEAVE AS-IS; it must render even if the token system breaks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair semantic token block (globals.css channel triplets) + verify primitives — D-07/D-26/D-02</name>
  <files>src/styles/globals.css, src/components/ui/input.tsx, src/components/ui/select.tsx</files>
  <read_first>
    - src/styles/globals.css (full :root block, especially lines 6-66 — both the raw --brand-*/--neutral-* palette AND the semantic --background..--ring block).
    - tailwind.config.js (lines 15-56 — confirm every token is hsl(var(--x)) so the config side needs NO change).
    - src/components/ui/input.tsx (the full 26-line primitive — confirm token class names).
    - src/components/ui/select.tsx (SelectTrigger className lines ~51-57 — the hover !important cascade).
    - .planning/phases/05-perfil-hardening-mvp/05-RESEARCH.md Q5 + Pitfall 4 (the convert-entire-block-in-one-pass rule).
    - .claude/skills/beauty-smile-design-system (glass token conventions).
  </read_first>
  <action>
    In src/styles/globals.css :root, convert the ENTIRE semantic token block to HSL channel triplets in ONE pass (Pitfall 4 — partial conversion leaves siblings emitting hsl(#hex)): --background, --foreground, --card(+foreground), --popover(+foreground), --primary (234 100% 31%) + --primary-foreground, --secondary(+foreground), --muted(+foreground), --accent(+foreground), --destructive(+foreground), --border, --input, --input-background, --ring, and --chart-1..5 if referenced by the config. Compute each triplet deterministically from its current hex value (keep the raw --brand-*/--neutral-* hex palette intact for direct-hex consumers — only DECOUPLE the semantic tokens). Do NOT touch tailwind.config.js (config is already correct; dropping the hsl() wrapper would break opacity modifiers per D-25). Then in src/components/ui/input.tsx confirm the primitive renders correctly post-fix (no change expected — it already uses semantic tokens; verify-in-place per D-02). In src/components/ui/select.tsx, fix the resting (non-hover) trigger text to a contrast-safe semantic token now that tokens resolve, and simplify the masking [and_*]:hover:!text-white !important cascade to the minimum needed (D-02 + F-04.1-A dark-on-dark dropdown fix). Lift the cadastro form's known-good glass-input styling as the reference (F-04.1-C) — fix at the PRIMITIVE, never per-page.
  </action>
  <verify>
    <automated>grep -qE '^[[:space:]]*--primary:[[:space:]]*234[[:space:]]+100%[[:space:]]+31%' src/styles/globals.css && git diff --quiet tailwind.config.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep for `--primary: 234 100% 31%` in globals.css matches.
    - NO semantic token (--background/--foreground/--primary/--secondary/--muted/--accent/--destructive/--border/--input/--ring and their -foreground siblings) still resolves to a hex or var(--brand-*)/var(--neutral-*) value (grep returns zero matches across that block).
    - `git diff --quiet tailwind.config.js` exits 0 (no config change).
    - SMOKE-RUNTIME (Pitfall 6 — REQUIRED): npm run build && npm run preview, then load /auth/login; the primary button background is visibly blue (rgb(0,16,158)), input text is dark-readable on the glass field, and a Select dropdown shows readable resting text. Capture evidence (screenshot or computed background-color) in the SUMMARY. Autonomous green is NOT sufficient.
    - Lint baseline 296 preserved (npm run lint tsc error count unchanged).
  </acceptance_criteria>
  <done>Semantic tokens are valid channel triplets; bg-primary resolves blue; Input renders correctly; Select resting text is contrast-safe; smoke-runtime evidence captured.</done>
</task>

<task type="auto">
  <name>Task 2: GlassButton inline-flex + BeautySmileLogo type reconcile (D-14)</name>
  <files>src/components/ui/glass.tsx, src/components/BeautySmileLogo.tsx, src/components/pages/MeuPerfilCandidatoPage.tsx</files>
  <read_first>
    - src/components/ui/glass.tsx (GlassButton forwardRef lines ~147-192, base className ~168-185).
    - src/components/BeautySmileLogo.tsx (BeautySmileLogoProps lines 3-7 + the render switch on type).
    - src/components/pages/MeuPerfilCandidatoPage.tsx:350 (the type="symbol" invalid call site).
    - .planning/phases/04-vagas-candidatura/04-REVIEW-09.md WR-02-09 (the divergence ledger: MeuPerfil uses type="symbol", Vagas/VagaDetalhe use type="icon").
  </read_first>
  <action>
    In src/components/ui/glass.tsx GlassButton base className, add inline-flex items-center justify-center gap-2 (matches the shadcn Button convention) so icon+text children center/align correctly. Single-line primitive change — cascades to all 9 GlassButton consumers, no per-consumer patch. In src/components/BeautySmileLogo.tsx, resolve the type="symbol" mismatch (WR-02-09): widen the union to 'icon' | 'symbol' | 'horizontal' | 'vertical' mapping 'symbol' to the same glyph as 'icon' (so existing renders stay identical), OR correct the MeuPerfilCandidatoPage:350 call site to type="icon". Pick the option that keeps the rendered output byte-identical to current and document the choice + rationale in the SUMMARY (STATE.md [04-08] deviation b previously switched symbol->icon — reconcile to ONE canonical answer now). This must NOT introduce a TS error.
  </action>
  <verify>
    <automated>grep -q "inline-flex" src/components/ui/glass.tsx && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - grep for `inline-flex` in src/components/ui/glass.tsx matches.
    - npm run lint tsc error count == 296 (zero growth — the type="symbol" mismatch is resolved, not added to baseline).
    - SMOKE-RUNTIME: load a page with a GlassButton (e.g. /candidato/perfil logout button or /vagas) and confirm an icon+text GlassButton centers its content; capture evidence in SUMMARY.
    - BeautySmileLogo renders an identical glyph at MeuPerfilCandidatoPage:350 (visual confirm — no missing/broken logo).
  </acceptance_criteria>
  <done>GlassButton inline-flex added; BeautySmileLogo type mismatch reconciled to a single canonical answer; lint 296 holds.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep candidate-facing hex literals back to semantic tokens (smoke-runtime each)</name>
  <files>src/components/pages/MeuPerfilCandidatoPage.tsx</files>
  <read_first>
    - .planning/phases/05-perfil-hardening-mvp/05-RESEARCH.md Q5 "Safe sweep strategy" (file-by-file, smoke-runtime-gated) + Pitfall 4 (the F-04-08-D trap).
    - src/components/pages/MeuPerfilCandidatoPage.tsx (current bg-[#00109E] / hex-literal usages).
    - STATE.md [04-08] D-26 (the precedent hex workaround at LoginCandidatoPage:393 lineage).
  </read_first>
  <action>
    Sweep the candidate-facing hex-literal workarounds back to semantic tokens now that the token system resolves. Scope this phase to CANDIDATE-FACING pages only (RESEARCH Q5 recommendation): identify the bg-[#00109E] and sibling hex literals on the candidate pages (MeuPerfilCandidatoPage is the primary one owned by THIS plan; the login/recovery/vagas/candidatura pages are swept in their respective downstream plans 05-03 to avoid file-ownership conflicts — confirm via grep which hex literals live in MeuPerfilCandidatoPage and sweep only those here). Replace bg-[#00109E] -> bg-primary, bg-[#00109E]/90 -> bg-primary/90, etc. After EACH swap, re-render the page (smoke-runtime — Pitfall 6) to confirm no visual regression (the F-04-08-D trap: swapping while the token is still broken yields invisible buttons; the token fix in Task 1 must be confirmed first). Leave the ErrorBoundary fallback hex AS-IS (it must render token-independent). Do NOT sweep RH/M2 pages — they keep working hex literals.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - On MeuPerfilCandidatoPage, `grep -c "bg-\[#00109E\]" src/components/pages/MeuPerfilCandidatoPage.tsx` is reduced (swept to bg-primary) — record before/after counts in SUMMARY.
    - SMOKE-RUNTIME (REQUIRED): load /candidato/perfil (mocked or real auth), confirm the swept elements render with the correct blue background identical to pre-sweep; capture evidence.
    - npm run build exits 0.
    - Lint baseline 296 preserved.
  </acceptance_criteria>
  <done>MeuPerfilCandidatoPage candidate-facing hex literals swept to semantic tokens; each swept element smoke-runtime-confirmed; no visual regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Smoke-runtime gate — design-system batch renders correctly</name>
  <what-built>
    Token system repaired (channel triplets), Input/Select primitives corrected, GlassButton inline-flex, BeautySmileLogo type reconciled, MeuPerfilCandidatoPage hex sweep. This is the Phase 4.1-inherited smoke-runtime gate (Pitfall 6): autonomous green gates are NOT sufficient — this exact bug class (F-04-08-D) passed all Phase 4 automated gates while the page was unusable.
  </what-built>
  <how-to-verify>
    1. Run `npm run build && npm run preview` (serves build/ on http://localhost:4173).
    2. Visit http://localhost:4173/auth/login — confirm: primary "Entrar" button has a solid blue (#00109E) background, NOT transparent; input fields show dark-readable text on the glass; focus a field and confirm the focus ring is visible.
    3. Visit http://localhost:4173/cadastro — open a Select/dropdown (e.g. Estado): confirm the resting (un-hovered) option text is readable (NOT dark-on-dark / invisible) — this is the F-04.1-A fix.
    4. Visit http://localhost:4173/candidato/perfil (log in if needed) — confirm: BeautySmileLogo renders in the header, the logout GlassButton centers its icon+text, and the swept blue backgrounds look identical to before.
    5. Confirm no element that was blue is now transparent (the F-04-08-D regression check).
  </how-to-verify>
  <resume-signal>Type "approved" if the design system renders correctly across login/cadastro/perfil, or describe any dark-on-dark / transparent / mis-aligned element.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time CSS var resolution | globals.css token values feed tailwind hsl() wrapper at build; an invalid value silently produces transparent UI (availability/usability risk, not a classic security threat) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Denial of Service | globals.css partial token conversion | mitigate | Convert the ENTIRE semantic block in one pass (Pitfall 4); smoke-runtime gate (Task 4) catches any sibling token still emitting hsl(#hex) before "complete". |
| T-05-02-02 | Tampering | Select hover !important cascade simplification | accept | Simplifying [&_*]:hover:!text-white reduces a CSS override; no security surface — purely visual. Smoke-runtime confirms resting + hover states both readable. |
| T-05-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (Wave 0 already installed LHCI/axe). N/A. |
</threat_model>

<verification>
- grep `--primary: 234 100% 31%` in globals.css matches; no semantic token resolves to hex/brand/neutral var.
- `git diff --quiet tailwind.config.js` exits 0.
- grep `inline-flex` in glass.tsx matches.
- npm run build exits 0; npm run lint baseline 296.
- Human smoke-runtime gate (Task 4) approved: login/cadastro/perfil render with blue buttons, readable inputs, readable dropdowns, centered GlassButtons, correct logo.
</verification>

<success_criteria>
- bg-primary and all semantic tokens resolve to valid colors (channel triplets).
- Input + Select primitives render contrast-safe text on glass (D-02; F-04.1-A/C fixed at primitive).
- GlassButton inline-flex; BeautySmileLogo type reconciled to one canonical answer.
- Candidate-facing hex literals (MeuPerfilCandidatoPage) swept to semantic tokens with no visual regression.
- Smoke-runtime gate approved (Phase 4.1 inherited requirement).
- Lint baseline 296 preserved.
</success_criteria>

<output>
Create `.planning/phases/05-perfil-hardening-mvp/05-02-SUMMARY.md` when done.
</output>
