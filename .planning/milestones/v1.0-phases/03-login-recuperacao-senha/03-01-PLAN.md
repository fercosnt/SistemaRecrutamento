---
phase: 03-login-recuperacao-senha
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/features/auth/services/__tests__/authService.test.ts
  - src/features/auth/services/__tests__/passwordService.test.ts
  - src/features/auth/utils/__tests__/extractRole.test.ts
  - src/features/auth/utils/__tests__/rememberMeStorage.test.ts
  - src/features/auth/hooks/__tests__/useRecoverySession.test.ts
  - src/features/auth/hooks/__tests__/useRateLimitCooldown.test.ts
  - src/features/auth/schemas/__tests__/redefinirSenhaSchema.test.ts
  - .planning/phases/03-login-recuperacao-senha/03-01-DASHBOARD-AUDIT.md
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
requirements_addressed: [AUTH-03, AUTH-04]
tags: [wave0, jwt-decode, supabase-dashboard, test-stubs, nyquist]
user_setup:
  - service: supabase-dashboard
    why: "Verify Email OTP Expiration = 3600s (AUTH-03) and Redirect URL allow-list (AUTH-04)"
    dashboard_config:
      - task: "Set Auth > Providers > Email > Email OTP Expiration = 3600"
        location: "Supabase Dashboard → project isljnozzlvckrgjjbjwp → Auth → Providers → Email"
      - task: "Allow-list http://localhost:3003/auth/redefinir-senha (+ ?tipo=rh)"
        location: "Supabase Dashboard → Auth → URL Configuration → Redirect URLs"

must_haves:
  truths:
    - "jwt-decode@^4.0.0 is installed and importable from src/features/auth/*"
    - "Supabase Dashboard Email OTP Expiration = 3600s (1 hour) — verified via screenshot (mitigates T-03-09)"
    - "Supabase Dashboard Redirect URLs allow-list contains /auth/redefinir-senha (both variants) — verified via screenshot"
    - "All Wave 0 Vitest + Playwright test stub files exist and register pending specs (B1..B16 Behavior IDs referenced)"
  artifacts:
    - path: "package.json"
      provides: "jwt-decode dependency declared"
      contains: "jwt-decode"
    - path: "src/features/auth/services/__tests__/authService.test.ts"
      provides: "stubs covering B1, B2, B3, B4, B13, B14"
    - path: "src/features/auth/services/__tests__/passwordService.test.ts"
      provides: "stubs covering B9, B10"
    - path: "src/features/auth/utils/__tests__/extractRole.test.ts"
      provides: "stubs covering B7"
    - path: "src/features/auth/utils/__tests__/rememberMeStorage.test.ts"
      provides: "stubs covering B5, B6, B16"
    - path: "src/features/auth/hooks/__tests__/useRecoverySession.test.ts"
      provides: "stub covering B12"
    - path: "src/features/auth/hooks/__tests__/useRateLimitCooldown.test.ts"
      provides: "stub covering B4 hook layer"
    - path: "src/features/auth/schemas/__tests__/redefinirSenhaSchema.test.ts"
      provides: "stub covering B11"
    - path: ".planning/phases/03-login-recuperacao-senha/03-01-DASHBOARD-AUDIT.md"
      provides: "Dashboard audit runbook + screenshot evidence (OTP 3600s + Redirect URLs)"
  key_links:
    - from: "npm install jwt-decode"
      to: "package.json dependencies + node_modules/jwt-decode"
      via: "npm package manager"
      pattern: '"jwt-decode":\s*"\^?4'
---

<objective>
Install the sole new runtime dependency (`jwt-decode@^4.0.0`), author all Wave 0 Vitest/Playwright test stubs mapped to B1..B16 from 03-VALIDATION.md, and complete the Supabase Dashboard audit (OTP expiry 3600s + Redirect URL allow-list) — the two configuration gates that block AUTH-03 and AUTH-04 at runtime regardless of code correctness.

Purpose: Execute the Wave 0 gate described in RESEARCH.md §Environment Availability + §Validation Architecture. These items MUST land before any business-logic code (Wave 1+) because:
- `jwt-decode` blocks the Bug 1 fix (D-13) in Wave 1.
- OTP expiry = 3600s blocks AUTH-03 copy ("válido por 1 hora") from matching reality.
- Redirect URL allow-list blocks the email deeplink from landing at `/auth/redefinir-senha` (AUTH-04).

Output: `jwt-decode` installed, 7 Vitest stub files + 2 Playwright stub files already exist and are extended with pending/todo specs tagged `B<id>`, + a dashboard audit runbook document committed under the phase directory with a human-verified checkmark.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-login-recuperacao-senha/03-CONTEXT.md
@.planning/phases/03-login-recuperacao-senha/03-RESEARCH.md
@.planning/phases/03-login-recuperacao-senha/03-VALIDATION.md
@.planning/phases/03-login-recuperacao-senha/03-PATTERNS.md
@.planning/phases/01-foundation-saneada/KNOWN-ISSUES-CARRYOVER-PHASE-3.md
@e2e/login-flow.spec.ts
@e2e/password-recovery-flow.spec.ts
@src/features/cadastro/services/__tests__/authService.test.ts

<interfaces>
<!-- Every new auth test file imports from these contracts (still to be created — Wave 1+). -->
<!-- Stubs use `it.todo(...)` or `test.skip(...)` to register the specs WITHOUT importing -->
<!-- production code that does not yet exist. This is the Phase 2 Wave 0 pattern (02-01-PLAN). -->

Behavior ID map (from 03-VALIDATION.md):
- B1  Login candidato success → role='candidato' + redirect /candidato/perfil
- B2  Login wrong creds → INVALID_CREDENTIALS + generic copy (T-03-02)
- B3  email_not_confirmed → EMAIL_NOT_CONFIRMED + resend CTA
- B4  Rate limit → cooldown countdown + disabled submit (T-03-06)
- B5  Lembrar-me unchecked → sessionStorage (T-03-04, T-03-10) [Manual UAT primary]
- B6  Lembrar-me checked (default) → localStorage [Manual UAT primary]
- B7  extractRole reads JWT payload, not app_metadata (T-03-07; Bug 1)
- B8  LoginRH rejects non-administrador (T-03-01; Bug 2/3)
- B9  resetPasswordForEmail → neutral copy (T-03-02)
- B10 Recovery deeplink → PASSWORD_RECOVERY → form → updateUser
- B11 Password mismatch Zod error on submit (T-03-08)
- B12 Expired/single-use link → InvalidLinkState (T-03-05)
- B13 Network error → NETWORK_ERROR toast
- B14 Password NEVER in console.* (T-03-03)
- B15 Sonner toast renders in DOM for all auth flows
- B16 setRememberMeMode('session') wipes localStorage sb-* keys (T-03-04)

Existing Phase 2 test pattern to mirror (from src/features/cadastro/services/__tests__/authService.test.ts:25-37):
```typescript
vi.mock('@/lib/supabase/client', () => ({
  supabase: {
    auth: {
      signInWithPassword: vi.fn(),
      signOut: vi.fn(),
      resetPasswordForEmail: vi.fn(),
      updateUser: vi.fn(),
      resend: vi.fn(),
      onAuthStateChange: vi.fn(),
      getSession: vi.fn(),
      getUser: vi.fn(),
    },
  },
}))
```
</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → Supabase Dashboard | Human must configure OTP expiry + Redirect URLs; no code path enforces this |
| npm registry → local node_modules | New dependency `jwt-decode@^4.0.0` pulled into bundle (13.9 kB unpacked, 0 transitive deps) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | Supabase Dashboard OTP expiry (default 86400s) contradicts AUTH-03 "1h" copy | mitigate | Human verifies setting = 3600s + screenshots before/after into 03-01-DASHBOARD-AUDIT.md |
| T-03-09b | Info Disclosure | Missing `/auth/redefinir-senha` in Redirect URLs → deeplink redirects to Supabase-hosted page leaking session to wrong origin | mitigate | Human confirms allow-list + screenshots (blocks AUTH-04) |
| T-03-03 | Info Disclosure | `jwt-decode` hypothetically logging token on parse error | mitigate | Wave 1 extractRole wrappers use try/catch without logging; verified by later B14 grep |
| T-supply-chain | Tampering | Unknown npm package substitution | accept | `jwt-decode@4.0.0` is Auth0 OSS (MIT, 0 deps, 13.9 kB, published >1 year), verified via `npm view jwt-decode` in RESEARCH.md §Standard Stack |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Install jwt-decode@^4.0.0 and confirm import resolution</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json
    - .planning/phases/03-login-recuperacao-senha/03-RESEARCH.md (§Standard Stack, §Don't Hand-Roll — verifies 4.0.0 choice per D-20)
  </read_first>
  <action>
    Run `npm install jwt-decode@^4.0.0 --save` from the repository root. This adds `jwt-decode` to `dependencies` in package.json (NOT devDependencies — it is a runtime import consumed by `src/features/auth/utils/extractRole.ts` in Wave 1). Verify installation with `node -e "console.log(require('jwt-decode/package.json').version)"` — expect output matching `4.x.x`. Verify the export symbol `jwtDecode` exists with `node -e "console.log(Object.keys(require('jwt-decode')))"` — expect output containing `jwtDecode`. Do NOT install additional packages (no `@types/jwt-decode` — 4.x bundles types). If `npm install` triggers any lint/build/test hook failures unrelated to the new dependency, commit with `--no-verify` and document pre-existing carryover in the commit body (established Phase 1/2 pattern — Phase 3 files are not yet touched so the pre-commit tsc gate will pick up only carryover errors, which are acceptable).
  </action>
  <verify>
    <automated>node -e "const v = require('jwt-decode/package.json').version; if (!v.startsWith('4.')) { console.error('EXPECTED 4.x, GOT', v); process.exit(1); } else { console.log('ok:', v); }"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"jwt-decode":\s*"\^?4' package.json` exits 0 with exactly 1 match
    - `ls node_modules/jwt-decode/package.json` exits 0
    - `node -e "const { jwtDecode } = require('jwt-decode'); if (typeof jwtDecode !== 'function') process.exit(1);"` exits 0
    - No new entry in `dependencies` other than `jwt-decode` (no accidental co-installs)
    - `grep -E '"@types/jwt-decode"' package.json` exits 1 (zero matches — not needed; 4.x ships types)
    - commit message body documents `--no-verify` reason if used (pre-existing carryover per Phase 2 precedent); pattern: `grep -E '^(pre-existing carryover|Files touched by 03-01 pass tsc --noEmit cleanly)' <(git log -1 --format=%B)` — MISSING OK if hook passed without `--no-verify`
  </acceptance_criteria>
  <done>
    `jwt-decode@^4.0.0` is installed, importable via `import { jwtDecode } from 'jwt-decode'`, and declared in `package.json` `dependencies` section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 7 Vitest + 2 Playwright stub files tagged by Behavior ID</name>
  <files>
    src/features/auth/services/__tests__/authService.test.ts,
    src/features/auth/services/__tests__/passwordService.test.ts,
    src/features/auth/utils/__tests__/extractRole.test.ts,
    src/features/auth/utils/__tests__/rememberMeStorage.test.ts,
    src/features/auth/hooks/__tests__/useRecoverySession.test.ts,
    src/features/auth/hooks/__tests__/useRateLimitCooldown.test.ts,
    src/features/auth/schemas/__tests__/redefinirSenhaSchema.test.ts
  </files>
  <read_first>
    - src/features/cadastro/services/__tests__/authService.test.ts (Phase 2 stub/real test scaffolding)
    - .planning/phases/03-login-recuperacao-senha/03-VALIDATION.md (B1..B16 behavior table — EXACT source of truth)
    - .planning/phases/03-login-recuperacao-senha/03-RESEARCH.md (§Validation Architecture — coverage matrix)
  </read_first>
  <action>
    Create the 7 Vitest stub files listed in `<files>`. Each file MUST:
    1. Import `{ describe, it, expect }` from `'vitest'` (no other imports — the stubs DO NOT import production code that does not yet exist; production imports land in Waves 1-3).
    2. Contain at minimum ONE `describe(...)` block per target Behavior ID, with `it.todo('B<id>: <short behavior description>')` registering each pending spec. Use the exact Behavior IDs and descriptions from 03-VALIDATION.md §Critical Behaviors and the interfaces block above.
    3. Include a top-of-file JSDoc comment `/** Wave 0 stub — populated during Waves 1-3. Behaviors covered: B<ids>. See 03-VALIDATION.md. */`.

    Per-file Behavior ID mapping:
    - `authService.test.ts` → B1, B2, B3, B4, B13, B14
    - `passwordService.test.ts` → B9, B10
    - `extractRole.test.ts` → B7
    - `rememberMeStorage.test.ts` → B5, B6, B16
    - `useRecoverySession.test.ts` → B12
    - `useRateLimitCooldown.test.ts` → B4
    - `redefinirSenhaSchema.test.ts` → B11

    Also extend the existing `e2e/login-flow.spec.ts` and `e2e/password-recovery-flow.spec.ts` (files already exist — do NOT recreate them). Read each file first, then append a new `test.describe('Wave 0 stubs — 03-login-recuperacao-senha', () => { ... })` block at the END of each file. Inside, register `test.skip('B<id>: <description>', async () => { /* TODO Wave 3 */ })` entries matching:
    - login-flow.spec.ts → B1, B2, B3 (env-gated), B4 (env-gated), B8 (env-gated), B15
    - password-recovery-flow.spec.ts → B9, B10 (mocked), B12

    After writing, run `npm run test:run` and `npx playwright test --list --project=chromium` and assert both exit 0 — the stubs must parse without syntax errors. The `.todo`/`.skip` specs will appear in test output but will NOT fail.

    File skeleton example (apply same shape to each of the 7 Vitest files):
    ```typescript
    /**
     * Wave 0 stub — populated during Waves 1-3.
     * Behaviors covered: B1, B2, B3, B4, B13, B14.
     * See .planning/phases/03-login-recuperacao-senha/03-VALIDATION.md
     */
    import { describe, it } from 'vitest'

    describe('authService — Wave 0 stubs (03-login-recuperacao-senha)', () => {
      it.todo('B1: signIn with correct creds resolves; authStore listener picks up SIGNED_IN')
      it.todo('B2: signIn with wrong creds throws AuthError{code: INVALID_CREDENTIALS}')
      it.todo('B3: signIn with email_not_confirmed throws AuthError{code: EMAIL_NOT_CONFIRMED, field: email}')
      it.todo('B4: mapSupabaseError maps over_email_send_rate_limit → AuthError{code: RATE_LIMITED, retryAfterSeconds ≥ 1}')
      it.todo('B13: signIn with network exception throws AuthError{code: NETWORK_ERROR}')
      it.todo('B14: signIn never logs senha/password/access_token via console.* (Pitfall 7)')
    })
    ```

    Do NOT add any import of `'@/features/auth/...'` in the stubs — those modules are NOT yet created. This rule is enforced by the final grep check in acceptance criteria.
  </action>
  <verify>
    <automated>npm run test:run -- --reporter=default 2>&1 | tail -40; npm run test:run --silent --reporter=default 2>&1 | grep -E "^Tests?\s+[0-9]+\s+(todo|skip)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - All 7 Vitest files exist: `ls src/features/auth/services/__tests__/authService.test.ts src/features/auth/services/__tests__/passwordService.test.ts src/features/auth/utils/__tests__/extractRole.test.ts src/features/auth/utils/__tests__/rememberMeStorage.test.ts src/features/auth/hooks/__tests__/useRecoverySession.test.ts src/features/auth/hooks/__tests__/useRateLimitCooldown.test.ts src/features/auth/schemas/__tests__/redefinirSenhaSchema.test.ts` exits 0
    - Each Vitest file contains at least 1 `it.todo(...)` — `grep -cE "it\.todo\('B[0-9]+:" src/features/auth/**/__tests__/*.test.ts` sums to ≥ 16 Behavior-tagged todo specs across all 7 files
    - Each Vitest file has the Wave 0 header — `grep -cE "Wave 0 stub" src/features/auth/**/__tests__/*.test.ts` returns 7
    - `npm run test:run` exits 0 (todo specs do not fail)
    - `grep -rE "from '@/features/auth/" src/features/auth/services/__tests__/authService.test.ts src/features/auth/services/__tests__/passwordService.test.ts src/features/auth/utils/__tests__/extractRole.test.ts src/features/auth/utils/__tests__/rememberMeStorage.test.ts src/features/auth/hooks/__tests__/useRecoverySession.test.ts src/features/auth/hooks/__tests__/useRateLimitCooldown.test.ts src/features/auth/schemas/__tests__/redefinirSenhaSchema.test.ts` exits 1 (zero matches — no Wave 1+ imports leak into Wave 0 stubs)
    - e2e/login-flow.spec.ts contains new stub block — `grep -c "Wave 0 stubs — 03-login-recuperacao-senha" e2e/login-flow.spec.ts` returns 1
    - e2e/password-recovery-flow.spec.ts contains new stub block — `grep -c "Wave 0 stubs — 03-login-recuperacao-senha" e2e/password-recovery-flow.spec.ts` returns 1
    - `npx playwright test --list --project=chromium` exits 0 (stubs parse)
    - Files TOUCHED pass `npx tsc --noEmit` cleanly — `npx tsc --noEmit 2>&1 | grep -E "src/features/auth/(services|utils|hooks|schemas)/__tests__" | wc -l` returns 0
    - No Sonner versioned imports (defensive even though stubs shouldn't import sonner): `grep -rE "from 'sonner@" src/features/auth/` exits 1
  </acceptance_criteria>
  <done>
    9 test stub files (7 new Vitest + 2 extended Playwright) exist, parse cleanly, register at least 16 Behavior-tagged pending specs, and the full test suites remain green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Tasks 1-2 have installed `jwt-decode@^4.0.0` and registered 16+ Behavior-tagged test stubs. This checkpoint asks the human (user) to perform the Supabase Dashboard audit documented in RESEARCH.md §Environment Availability (Pitfall 4 + Pitfall 8), screenshot the current state, and record results in `.planning/phases/03-login-recuperacao-senha/03-01-DASHBOARD-AUDIT.md`.

    Claude HAS authored `03-01-DASHBOARD-AUDIT.md` as a runbook skeleton with empty "Current setting" + "Target setting" + "Screenshot URL" fields. The human must:
    1. Log into the Supabase Dashboard for project `isljnozzlvckrgjjbjwp`.
    2. Verify Email OTP Expiration.
    3. Verify Redirect URLs allow-list.
    4. Fill in the 4 checkboxes in the runbook.
    5. Drop screenshots into the runbook (or reference local paths).
    6. Resume with "approved" (all green) or "issues" with a list.
  </what-built>
  <how-to-verify>
    1. Open Supabase Dashboard: https://supabase.com/dashboard/project/isljnozzlvckrgjjbjwp
    2. Navigate to **Auth → Providers → Email**.
    3. Locate **Email OTP Expiration** field.
       - Expected: `3600` seconds (= 1 hour).
       - If different (e.g. default `86400` = 24h), change to `3600` and SAVE.
       - Take a screenshot showing the final value.
    4. Navigate to **Auth → URL Configuration → Redirect URLs**.
    5. Confirm the allow-list contains ALL of:
       - `http://localhost:3003/auth/redefinir-senha`
       - `http://localhost:3003/auth/redefinir-senha?tipo=rh` (if query-preserving match is required by the Dashboard UI; otherwise `http://localhost:3003/auth/redefinir-senha*` with wildcard)
       - (Optionally the production URL equivalent when prod is deployed — not blocking for Phase 3.)
       - If any are missing, ADD them and SAVE.
       - Take a screenshot showing the final allow-list.
    6. Open `.planning/phases/03-login-recuperacao-senha/03-01-DASHBOARD-AUDIT.md` and fill in:
       - `[ ] OTP expiry set to 3600s (was: <prior value>)` — check the box.
       - `[ ] Redirect URL #1 allow-listed` — check the box.
       - `[ ] Redirect URL #2 allow-listed` — check the box.
       - `[ ] Custom Access Token Hook still enabled (confirmed via jwt.io decode of a fresh access_token)` — check the box.
       - Paste or reference screenshot paths.
    7. Commit the audit document: `git add .planning/phases/03-login-recuperacao-senha/03-01-DASHBOARD-AUDIT.md && git commit -m "docs(03-01): Supabase Dashboard audit — OTP 3600s + Redirect URLs confirmed"`.
  </how-to-verify>
  <resume-signal>Type "approved" once all 4 audit checkboxes are checked and the audit doc is committed; or "issues" with a list of misconfigurations Claude should raise back to the user.</resume-signal>
</task>

</tasks>

<verification>
- `grep -E '"jwt-decode":\s*"\^?4' package.json` exits 0 with exactly 1 match.
- `find src/features/auth -path "*/__tests__/*.test.ts" | wc -l` ≥ 7.
- `grep -rcE "it\.todo\('B[0-9]+:" src/features/auth/**/__tests__/*.test.ts | awk -F: '{s+=$2} END {print s}'` returns ≥ 16.
- `.planning/phases/03-login-recuperacao-senha/03-01-DASHBOARD-AUDIT.md` exists with ALL 4 checkboxes checked.
- `npm run test:run` exits 0.
- `npx playwright test --list --project=chromium` exits 0.
</verification>

<success_criteria>
1. Dependency `jwt-decode@^4.0.0` is installed and declared in package.json.
2. 7 new Vitest test stub files exist under `src/features/auth/{services,utils,hooks,schemas}/__tests__/` with Behavior-tagged `it.todo(...)` specs summing ≥ 16.
3. Existing `e2e/login-flow.spec.ts` + `e2e/password-recovery-flow.spec.ts` each gain a "Wave 0 stubs — 03-login-recuperacao-senha" block with `test.skip(...)` entries for their respective Behavior IDs.
4. `.planning/phases/03-login-recuperacao-senha/03-01-DASHBOARD-AUDIT.md` is committed and shows 4 human-checked boxes plus screenshots.
5. `npm run test:run` and `npx playwright test --list --project=chromium` both exit 0.
6. No production-code imports leak into stub files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-login-recuperacao-senha/03-01-SUMMARY.md` per the template, documenting the jwt-decode version pinned, the exact stub file paths, the Behavior IDs covered, the Dashboard audit outcome (prior values → current values), and any notes about Supabase project-level quirks (e.g. if OTP was already at 3600s, state so explicitly).
</output>
