---
phase: 04-vagas-candidatura
plan: 09
plan_id: 04-09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/pages/VagasPublicasPage.tsx
  - src/components/pages/VagaDetalhePage.tsx
autonomous: false
gap_closure: true
source_uat: .planning/phases/04-vagas-candidatura/04-UAT.md
requirements:
  - VAGA-02
  - VAGA-03
user_setup: []

must_haves:
  truths:
    - "Card de vaga em /vagas (anon ou logado) apresenta botão 'Candidatar-se a esta vaga' em uma única linha — ícone alinhado ao texto, sem quebra, sem sobreposição com outros conteúdos do card."
    - "Página /vagas (lista) exibe header sticky com logo + avatar + nome do candidato + link 'Área do candidato' + botão 'Sair' QUANDO o candidato está autenticado (role === 'candidato'). NÃO exibe header quando anônimo (preserva VAGA-01 anon-browse)."
    - "Página /vagas/:identifier (detalhe) exibe o mesmo header sticky com logo + avatar + nome + link 'Área do candidato' + botão 'Sair' QUANDO autenticado como candidato. NÃO exibe header quando anônimo nem no estado VagaNotFoundState."
    - "Botão 'Voltar para vagas' no VagaNotFoundState (404 inline) renderiza ícone + texto em uma única linha — sem quebra de linha entre SVG e label."
    - "Logout disparado pelo botão 'Sair' nas páginas /vagas e /vagas/:identifier limpa a sessão (authStore.logout()) e redireciona para /auth/login replace:true — mesmo handler dos precedentes canônicos (MeuPerfilCandidatoPage / FormularioCandidaturaPage)."
  artifacts:
    - path: "src/components/pages/VagasPublicasPage.tsx"
      provides: "Persona shell sticky navbar guarded by isAuthenticated && role === 'candidato' + handleLogout + Área-do-candidato link"
      contains: "sticky top-0 z-50"
    - path: "src/components/pages/VagasPublicasPage.tsx"
      provides: "Card-action button com ícone+texto inline, sem quebra"
      contains: "inline-flex items-center justify-center gap-2 whitespace-nowrap"
    - path: "src/components/pages/VagaDetalhePage.tsx"
      provides: "Persona shell sticky navbar (mesma estrutura) no main render path"
      contains: "sticky top-0 z-50"
    - path: "src/components/pages/VagaDetalhePage.tsx"
      provides: "VagaNotFoundState back-button com ícone+texto inline"
      contains: "inline-flex items-center justify-center gap-2 whitespace-nowrap"
  key_links:
    - from: "src/components/pages/VagasPublicasPage.tsx"
      to: "@/store/authStore"
      via: "useAuthStore selectors (isAuthenticated, role, candidato, logout)"
      pattern: "useAuthStore\\(\\(state\\) => state\\.(isAuthenticated|role|candidato|logout)\\)"
    - from: "src/components/pages/VagasPublicasPage.tsx"
      to: "/candidato/perfil"
      via: "react-router Link 'Área do candidato'"
      pattern: "to=\"/candidato/perfil\""
    - from: "src/components/pages/VagaDetalhePage.tsx"
      to: "@/store/authStore"
      via: "useAuthStore selectors (role, candidato, logout) — isAuthenticated already imported"
      pattern: "useAuthStore\\(\\(state\\) => state\\.(role|candidato|logout)\\)"
    - from: "src/components/pages/VagaDetalhePage.tsx"
      to: "/candidato/perfil"
      via: "react-router Link 'Área do candidato'"
      pattern: "to=\"/candidato/perfil\""
---

<objective>
Fechar 3 gaps reportados no `04-UAT.md ## Gaps` (UAT phase-level executado 2026-04-26 com 9 pass / 1 issue / 2 side-findings). Esta é gap-closure de UI sobre uma fase já marcada como complete (8/8 plans + 3 carryovers); todo o código alvo é client-side, sem touchpoint de DB / Edge Function / RLS / migration.

Os 3 gaps:
1. **Minor** — Card-button "Candidatar-se a esta vaga" em `/vagas` quebra linha (ícone em uma linha, texto em outra) e parece sobrepor outros elementos do card.
2. **Major** — Páginas `/vagas` (lista) e `/vagas/:identifier` (detalhe) não exibem o persona shell quando o candidato está logado: sem botão "Sair", sem link "Área do candidato", sem avatar/nome.
3. **Minor** — Botão "Voltar para vagas" no `VagaNotFoundState` (404) também quebra linha (mesma classe de bug do gap 1).

Purpose: Restaurar o canonical persona shell pattern (D-27) em todas as páginas autenticadas do candidato e eliminar o gap recorrente de layout do GlassButton (`<svg/>` + texto sem `inline-flex`/`gap`/`whitespace-nowrap`).

Output:
- `VagasPublicasPage.tsx` com sticky navbar (auth-guarded) + card-button corrigido.
- `VagaDetalhePage.tsx` com sticky navbar (auth-guarded) + 404 back-button corrigido.
- Visual smoke check humano confirmando os 5 cenários (anon /vagas, logged-in /vagas, anon /vagas/:slug, logged-in /vagas/:slug, /vagas/slug-inexistente).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-vagas-candidatura/04-UAT.md
@.planning/phases/04-vagas-candidatura/04-CONTEXT.md
@.planning/phases/04-vagas-candidatura/04-08-SUMMARY.md

<!-- Precedentes canônicos do persona shell (D-27, post-Carryover-B) — copiar exatamente. -->
@src/components/pages/MeuPerfilCandidatoPage.tsx
@src/components/pages/FormularioCandidaturaPage.tsx

<!-- Targets desta gap-closure -->
@src/components/pages/VagasPublicasPage.tsx
@src/components/pages/VagaDetalhePage.tsx

<!-- Root context — GlassButton não inclui inline-flex/gap/whitespace-nowrap defaults -->
@src/components/ui/glass.tsx

<!-- AuthStore API (logout, isAuthenticated, role, candidato) -->
@src/store/authStore.ts

<interfaces>
<!-- Contratos relevantes que o executor precisa SEM explorar o codebase. -->

From src/store/authStore.ts (selectors usados pelo persona shell):
```typescript
// O store expõe `logout`, NÃO `signOut`. Os 2 precedentes canônicos chamam logout().
isAuthenticated: boolean
role: Role | null   // 'candidato' | 'rh' | 'administrador' | null
candidato: CandidatoRow | null  // tem .nome_completo, .email, .avatar_url
logout: () => Promise<void>
```

From src/components/pages/MeuPerfilCandidatoPage.tsx lines 264-283 (handleLogout canonical):
```tsx
const handleLogout = async () => {
  try {
    await logout();
    toast.success('Você saiu da sua conta com sucesso', {
      description: 'Até breve!',
    });
    navigate('/auth/login', { replace: true });
  } catch (error) {
    console.error('Erro ao fazer logout:', error);
    toast.error('Erro ao sair', { description: 'Tente novamente.' });
  }
};
```

From src/components/pages/MeuPerfilCandidatoPage.tsx lines 344-389 (canonical sticky navbar — fonte de verdade):
```tsx
<div className="w-full sticky top-0 z-50 mb-8">
  <Glass variant="white" blur="xl" className="border-b border-white/10">
    <div className="container mx-auto px-4 sm:px-6 py-4">
      <div className="flex items-center justify-between gap-4">
        {/* Logo e Nome do usuário */}
        <div className="flex items-center gap-4">
          <BeautySmileLogo type="symbol" size="sm" variant="white" />
          <div className="hidden sm:block h-8 w-px bg-white/20" />
          <div className="flex items-center gap-3">
            <Avatar className="w-10 h-10 border-2 border-white/30">
              <AvatarImage src={candidato?.avatar_url ?? undefined} />
              <AvatarFallback className="bg-[#35BFAD]/80 text-white text-sm">
                {iniciais}
              </AvatarFallback>
            </Avatar>
            <div className="hidden md:block">
              <p className="text-white font-medium drop-shadow-md leading-tight">
                {candidato?.nome_completo || 'Candidato'}
              </p>
              <p className="text-white/70 text-sm drop-shadow-sm leading-tight">
                {candidato?.email || ''}
              </p>
            </div>
          </div>
        </div>

        {/* Botão de Logout */}
        <GlassButton ...>
          <LogOut className="w-4 h-4" />
          <span className="hidden sm:inline">Sair</span>
        </GlassButton>
      </div>
    </div>
  </Glass>
</div>
```

From src/components/ui/glass.tsx lines 168-185 (GlassButton base — root cause):
```tsx
className={cn(
  blurVariants[blur],
  'backdrop-saturate-150',
  variantStyles[variant],
  'border',
  'rounded-xl shadow-lg',
  'px-6 py-3 cursor-pointer',
  'hover:bg-white/20 active:scale-95',
  'transition-all duration-200',
  // Falta: 'inline-flex items-center justify-center gap-2 whitespace-nowrap'
  className
)}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: VagasPublicasPage — adicionar persona shell + corrigir card-button (Gaps 1 + 2 partes)</name>
  <files>src/components/pages/VagasPublicasPage.tsx</files>
  <read_first>
    - src/components/pages/VagasPublicasPage.tsx (TARGET inteiro — 527 LoC, lê o arquivo todo antes de editar; o JSX começa na linha 163)
    - src/components/pages/MeuPerfilCandidatoPage.tsx lines 1-50 (imports canônicos: Avatar, AvatarFallback, AvatarImage de '../ui/avatar'; LogOut de 'lucide-react'; useAuthStore + useCandidato de '@/store/authStore'; Link de 'react-router-dom')
    - src/components/pages/MeuPerfilCandidatoPage.tsx lines 264-283 (handleLogout canonical — copiar 1:1)
    - src/components/pages/MeuPerfilCandidatoPage.tsx lines 337-389 (sticky navbar canonical — copiar estrutura, adaptando para incluir o link "Área do candidato")
    - src/components/pages/FormularioCandidaturaPage.tsx lines 486-535 (precedente post-Carryover-B aplicando o mesmo shell numa página de candidato — referência secundária)
    - src/store/authStore.ts (confirmar API: logout/isAuthenticated/role/candidato como selectors)
  </read_first>
  <behavior>
    - Quando NÃO autenticado: nenhum sticky header é renderizado; /vagas comporta-se exatamente como hoje (preservando VAGA-01 anon-browse).
    - Quando autenticado E role === 'candidato': sticky header aparece no topo com logo, divider, avatar+nome+email, link "Área do candidato" → /candidato/perfil, botão "Sair" → handleLogout.
    - Quando autenticado mas role !== 'candidato' (ex: rh, administrador navegando publicamente): nenhum header de candidato é renderizado (evita confusão de persona).
    - Card-button "Candidatar-se a esta vaga →" e seu sibling "Você já se candidatou a esta vaga" renderizam ícone+texto em uma única linha (Gap 1). Mesmo fix surgical para os 3 GlassButtons inline com SVG do empty-state e da paginação (Limpar filtros / Anterior / Próxima).
    - handleLogout: try-catch igual ao precedente; on success → toast + navigate('/auth/login', { replace: true }); on error → toast.error.
  </behavior>
  <action>
    **Step 1 — Adicionar imports** (no topo do arquivo, na seção de imports já existente):

    ```tsx
    import { Link, useNavigate } from 'react-router-dom'   // adicionar Link à linha existente
    import { Avatar, AvatarFallback, AvatarImage } from '../ui/avatar'
    import { LogOut, UserCircle } from 'lucide-react'   // adicionar LogOut + UserCircle à linha existente de lucide-react
    import { toast } from 'sonner'
    import { useAuthStore } from '@/store/authStore'
    ```

    **NOTA:** A linha existente `import { useNavigate } from 'react-router-dom'` (linha 17) deve virar `import { Link, useNavigate } from 'react-router-dom'`. A linha existente de lucide-react (linhas 21-31) deve receber `LogOut` e `UserCircle` adicionalmente.

    **Step 2 — Dentro do componente VagasPublicasPage (após `const navigate = useNavigate()`)** adicionar selectors do authStore + handler de logout. Usar `logout` (NÃO `signOut` — o store expõe `logout`):

    ```tsx
    // Persona shell — Phase 4 gap-closure (04-09 / D-27 canonical pattern)
    const isAuthenticated = useAuthStore((state) => state.isAuthenticated)
    const role = useAuthStore((state) => state.role)
    const candidato = useAuthStore((state) => state.candidato)
    const logout = useAuthStore((state) => state.logout)

    const showCandidatoShell = isAuthenticated && role === 'candidato'

    const candidatoIniciais = (candidato?.nome_completo || 'C')
      .split(' ')
      .map((n) => n[0])
      .join('')
      .toUpperCase()
      .slice(0, 2)

    // handleLogout — replica MeuPerfilCandidatoPage:264-283 exatamente.
    // Pitfall 7 awareness: console.error preservado pois é o pattern canonical;
    // não introduz PII no log (apenas a mensagem 'Erro ao fazer logout:').
    const handleLogout = async () => {
      try {
        await logout()
        toast.success('Você saiu da sua conta com sucesso', {
          description: 'Até breve!',
        })
        navigate('/auth/login', { replace: true })
      } catch (error) {
        console.error('Erro ao fazer logout:', error)
        toast.error('Erro ao sair', {
          description: 'Tente novamente.',
        })
      }
    }
    ```

    **Step 3 — Inserir o sticky navbar JSX**. No `return (...)`, dentro do `<BackgroundImage>` mas ANTES do `<div className="container mx-auto px-4 space-y-8">` (linha 171). Inserir EXATAMENTE este bloco:

    ```tsx
    {showCandidatoShell && (
      <div className="w-full sticky top-0 z-50 mb-8">
        <Glass variant="white" blur="xl" className="border-b border-white/10">
          <div className="container mx-auto px-4 sm:px-6 py-4">
            <div className="flex items-center justify-between gap-4">
              {/* Left: Logo + divider + Avatar + name */}
              <div className="flex items-center gap-4">
                <BeautySmileLogo type="symbol" size="sm" variant="white" />
                <div className="hidden sm:block h-8 w-px bg-white/20" />
                <div className="flex items-center gap-3">
                  <Avatar className="w-10 h-10 border-2 border-white/30">
                    <AvatarImage src={candidato?.avatar_url ?? undefined} />
                    <AvatarFallback className="bg-[#35BFAD]/80 text-white text-sm">
                      {candidatoIniciais}
                    </AvatarFallback>
                  </Avatar>
                  <div className="hidden md:block">
                    <p className="text-white font-medium drop-shadow-md leading-tight">
                      {candidato?.nome_completo || 'Candidato'}
                    </p>
                    <p className="text-white/70 text-sm drop-shadow-sm leading-tight">
                      {candidato?.email || ''}
                    </p>
                  </div>
                </div>
              </div>

              {/* Right: Área do candidato + Sair */}
              <div className="flex items-center gap-2">
                <Link to="/candidato/perfil" aria-label="Área do candidato">
                  <GlassButton
                    variant="white"
                    hover
                    className="inline-flex items-center justify-center gap-2 whitespace-nowrap text-white drop-shadow-sm"
                    aria-label="Área do candidato"
                  >
                    <UserCircle className="w-4 h-4" />
                    <span className="hidden sm:inline">Área do candidato</span>
                  </GlassButton>
                </Link>
                <GlassButton
                  variant="white"
                  hover
                  onClick={handleLogout}
                  className="inline-flex items-center justify-center gap-2 whitespace-nowrap text-white drop-shadow-sm"
                  aria-label="Sair"
                >
                  <LogOut className="w-4 h-4" />
                  <span className="hidden sm:inline">Sair</span>
                </GlassButton>
              </div>
            </div>
          </div>
        </Glass>
      </div>
    )}
    ```

    **NOTA importante sobre Link wrapping GlassButton:** O `<Link>` wraps o `<GlassButton>`. Como `GlassButton` é um `<button>`, isto produz um `<button>` dentro de um `<a>` que NÃO é HTML válido. Alternativa preferida: usar `onClick={() => navigate('/candidato/perfil')}` no GlassButton SEM wrap em Link. **Adotar esta alternativa** para manter a estrutura limpa:

    ```tsx
    <GlassButton
      variant="white"
      hover
      onClick={() => navigate('/candidato/perfil')}
      className="inline-flex items-center justify-center gap-2 whitespace-nowrap text-white drop-shadow-sm"
      aria-label="Área do candidato"
    >
      <UserCircle className="w-4 h-4" />
      <span className="hidden sm:inline">Área do candidato</span>
    </GlassButton>
    ```

    Substituir o bloco com Link pelo bloco com onClick acima. **NÃO IMPORTAR `Link` se não for usado em mais lugar nenhum no arquivo** — manter apenas `useNavigate` import.

    **Step 4 — Corrigir card-buttons (Gap 1 + paridade nos siblings)**. Localizar os GlassButtons das linhas 432-453 (já-aplicado + candidatar-se) e adicionar `inline-flex items-center justify-center gap-2 whitespace-nowrap` ao className. Aplicar o MESMO fix nos GlassButtons já-aplicado-disabled (linha 432-441), candidatar-se (linha 443-453), botões da paginação (linhas 465-486), e empty-state Limpar filtros (linhas 510-518) e filtros-ativos Limpar filtros (linhas 321-329) — para evitar futuras regressões na mesma página.

    Fix-pattern (aplicar em CADA um dos 6 GlassButtons da página):

    Antes (exemplo):
    ```tsx
    <GlassButton
      variant="white"
      hover
      className="w-full py-4 text-white drop-shadow-sm transition-all duration-200"
      ...
    >
    ```

    Depois:
    ```tsx
    <GlassButton
      variant="white"
      hover
      className="inline-flex items-center justify-center gap-2 whitespace-nowrap w-full py-4 text-white drop-shadow-sm transition-all duration-200"
      ...
    >
    ```

    `inline-flex` + `w-full` co-existem corretamente no Tailwind: `w-full` força largura intrínseca do bloco, `inline-flex` controla layout dos children. Verificado em produção pelos precedentes MeuPerfilCandidatoPage / FormularioCandidaturaPage que aplicam `flex items-center gap-2` no GlassButton.

    **Step 5 — Decisão arquitetural (registrar inline como comentário no topo do bloco do navbar):**

    ```tsx
    {/*
      Phase 4 / Plan 04-09 gap-closure:
      - 04-UAT.md Gap 2 (major): persona shell faltando em /vagas (logged-in candidato).
      - Replica D-27 canonical pattern (MeuPerfilCandidatoPage:344-389 / FormularioCandidaturaPage:494-534).
      - Acréscimo vs precedente: link "Área do candidato" → /candidato/perfil
        (ausente no MeuPerfil porque MeuPerfil JÁ É a área; faz sentido aqui).
      - Guard `showCandidatoShell` preserva VAGA-01 anon-browse: anônimos
        veem /vagas exatamente como antes (sem header).
    */}
    ```
  </action>
  <verify>
    <automated>
      cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && \
      grep -q "sticky top-0 z-50" src/components/pages/VagasPublicasPage.tsx && \
      grep -q "showCandidatoShell" src/components/pages/VagasPublicasPage.tsx && \
      grep -q "handleLogout" src/components/pages/VagasPublicasPage.tsx && \
      grep -q "useAuthStore" src/components/pages/VagasPublicasPage.tsx && \
      grep -q "Área do candidato" src/components/pages/VagasPublicasPage.tsx && \
      grep -q "/candidato/perfil" src/components/pages/VagasPublicasPage.tsx && \
      grep -q "BeautySmileLogo" src/components/pages/VagasPublicasPage.tsx && \
      grep -q "AvatarFallback" src/components/pages/VagasPublicasPage.tsx && \
      grep -q "LogOut" src/components/pages/VagasPublicasPage.tsx && \
      grep -q "UserCircle" src/components/pages/VagasPublicasPage.tsx && \
      grep -q "inline-flex items-center justify-center gap-2 whitespace-nowrap" src/components/pages/VagasPublicasPage.tsx && \
      [ "$(grep -c "inline-flex items-center justify-center gap-2 whitespace-nowrap" src/components/pages/VagasPublicasPage.tsx)" -ge 6 ] && \
      npm run lint
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "sticky top-0 z-50" src/components/pages/VagasPublicasPage.tsx` retorna 0
    - `grep -q "showCandidatoShell" src/components/pages/VagasPublicasPage.tsx` retorna 0
    - `grep -q "handleLogout" src/components/pages/VagasPublicasPage.tsx` retorna 0
    - `grep -q "useAuthStore" src/components/pages/VagasPublicasPage.tsx` retorna 0
    - `grep -q "Área do candidato" src/components/pages/VagasPublicasPage.tsx` retorna 0
    - `grep -q "/candidato/perfil" src/components/pages/VagasPublicasPage.tsx` retorna 0
    - `grep -c "inline-flex items-center justify-center gap-2 whitespace-nowrap" src/components/pages/VagasPublicasPage.tsx` ≥ 6 (1 navbar Sair + 1 navbar Área + 4 card/paginação/empty)
    - `npm run lint` exit code 0 (zero novos type errors)
    - Imports `Avatar`, `AvatarFallback`, `AvatarImage`, `LogOut`, `UserCircle`, `useAuthStore`, `toast` presentes (todos referenciados pelo novo JSX/handler — TS strict catch falhará se faltar)
  </acceptance_criteria>
  <done>
    Persona shell sticky aparece em /vagas SOMENTE quando logado como candidato. Card-button "Candidatar-se a esta vaga" e os outros 5 GlassButtons inline da página renderizam ícone+texto em uma única linha sem quebra. `npm run lint` passa sem novos erros TS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: VagaDetalhePage — adicionar persona shell + corrigir VagaNotFoundState back-button (Gaps 2 + 3)</name>
  <files>src/components/pages/VagaDetalhePage.tsx</files>
  <read_first>
    - src/components/pages/VagaDetalhePage.tsx (TARGET inteiro — 483 LoC; main render path começa em 202; VagaNotFoundState component em 54-85)
    - src/components/pages/VagasPublicasPage.tsx (apenas APÓS Task 1 completar — para reusar o pattern recém-aplicado)
    - src/components/pages/MeuPerfilCandidatoPage.tsx lines 264-283 (handleLogout canonical) e 337-389 (sticky navbar canonical)
    - src/store/authStore.ts (confirmar API: logout/role/candidato — VagaDetalhePage JÁ importa useAuthStore via linha 41)
  </read_first>
  <behavior>
    - VagaDetalhePage continua exibindo header center (BeautySmileLogo type="horizontal") e breadcrumb "Voltar para vagas". O sticky navbar é ADICIONAL, não substitui — aparece acima desses elementos quando autenticado.
    - Quando NÃO autenticado: nenhum sticky header. Página comporta-se como hoje.
    - Quando autenticado E role === 'candidato': sticky header aparece no topo (mesmo do /vagas). VagaNotFoundState NÃO recebe o header (404 é estado terminal e mantém minimalismo; gap 3 só endereça o back-button).
    - Loading state (linha 173-191): NÃO recebe o navbar (mantém UX de skeleton focado).
    - VagaNotFoundState back-button "Voltar para vagas" (linha 71-79) renderiza ícone+texto em uma única linha (Gap 3).
  </behavior>
  <action>
    **Step 1 — Adicionar imports** (no topo do arquivo). VagaDetalhePage JÁ importa `useAuthStore` (linha 41) e `useNavigate` (linha 23) e `Glass, GlassCard, GlassButton` (linha 26). Falta:

    ```tsx
    // Adicionar à linha de lucide-react existente (linhas 27-37): LogOut, UserCircle
    import {
      MapPin, Briefcase, Clock, Users, Calendar, CheckCircle2, Share2, ArrowLeft, Send,
      LogOut, UserCircle,
    } from 'lucide-react'

    // Novos imports
    import { Avatar, AvatarFallback, AvatarImage } from '../ui/avatar'
    import { BeautySmileLogo } from '../BeautySmileLogo'  // VagaDetalhePage JÁ importa BeautySmileLogo (linha 25) — PULAR se já existir
    ```

    **Confirmar imports existentes ANTES de adicionar duplicatas**: `BeautySmileLogo` já está em linha 25 do arquivo atual; NÃO duplicar. `useAuthStore` já está em linha 41; usar para extrair selectors adicionais.

    **Step 2 — Dentro do componente VagaDetalhePage, após o `const isAuthenticated = ...` (linha 90)**, adicionar selectors + handleLogout:

    ```tsx
    // Persona shell — Phase 4 gap-closure (04-09 / D-27 canonical pattern)
    const role = useAuthStore((state) => state.role)
    const candidato = useAuthStore((state) => state.candidato)
    const logout = useAuthStore((state) => state.logout)

    const showCandidatoShell = isAuthenticated && role === 'candidato'

    const candidatoIniciais = (candidato?.nome_completo || 'C')
      .split(' ')
      .map((n) => n[0])
      .join('')
      .toUpperCase()
      .slice(0, 2)

    // handleLogout — replica MeuPerfilCandidatoPage:264-283 exatamente.
    const handleLogout = async () => {
      try {
        await logout()
        toast.success('Você saiu da sua conta com sucesso', {
          description: 'Até breve!',
        })
        navigate('/auth/login', { replace: true })
      } catch (error) {
        console.error('Erro ao fazer logout:', error)
        toast.error('Erro ao sair', {
          description: 'Tente novamente.',
        })
      }
    }
    ```

    **Step 3 — Extrair o navbar para um sub-componente local** (mantém o main render path enxuto e permite reuso entre o caminho normal e — opcionalmente — o loading state):

    ```tsx
    // Sub-componente local definido DENTRO de VagaDetalhePage (closure sobre os selectors)
    // OU como helper top-level recebendo props. Adotar a versão inline (closure) para reduzir
    // boilerplate, conforme o padrão dos precedentes que inlinem o navbar JSX.
    ```

    Optar pela versão **inline** (sem extração). Inserir o navbar JSX no main render path (no return final em linha 202), ANTES do `<div className="container mx-auto px-4 space-y-8 max-w-4xl">` (linha 205):

    ```tsx
    {showCandidatoShell && (
      <div className="w-full sticky top-0 z-50 mb-8">
        <Glass variant="white" blur="xl" className="border-b border-white/10">
          <div className="container mx-auto px-4 sm:px-6 py-4">
            <div className="flex items-center justify-between gap-4">
              {/* Left: Logo + divider + Avatar + name */}
              <div className="flex items-center gap-4">
                <BeautySmileLogo type="symbol" size="sm" variant="white" />
                <div className="hidden sm:block h-8 w-px bg-white/20" />
                <div className="flex items-center gap-3">
                  <Avatar className="w-10 h-10 border-2 border-white/30">
                    <AvatarImage src={candidato?.avatar_url ?? undefined} />
                    <AvatarFallback className="bg-[#35BFAD]/80 text-white text-sm">
                      {candidatoIniciais}
                    </AvatarFallback>
                  </Avatar>
                  <div className="hidden md:block">
                    <p className="text-white font-medium drop-shadow-md leading-tight">
                      {candidato?.nome_completo || 'Candidato'}
                    </p>
                    <p className="text-white/70 text-sm drop-shadow-sm leading-tight">
                      {candidato?.email || ''}
                    </p>
                  </div>
                </div>
              </div>

              {/* Right: Área do candidato + Sair */}
              <div className="flex items-center gap-2">
                <GlassButton
                  variant="white"
                  hover
                  onClick={() => navigate('/candidato/perfil')}
                  className="inline-flex items-center justify-center gap-2 whitespace-nowrap text-white drop-shadow-sm"
                  aria-label="Área do candidato"
                >
                  <UserCircle className="w-4 h-4" />
                  <span className="hidden sm:inline">Área do candidato</span>
                </GlassButton>
                <GlassButton
                  variant="white"
                  hover
                  onClick={handleLogout}
                  className="inline-flex items-center justify-center gap-2 whitespace-nowrap text-white drop-shadow-sm"
                  aria-label="Sair"
                >
                  <LogOut className="w-4 h-4" />
                  <span className="hidden sm:inline">Sair</span>
                </GlassButton>
              </div>
            </div>
          </div>
        </Glass>
      </div>
    )}
    ```

    **NOTA — escopo do navbar:** Inserir SOMENTE no main render path (após linha 198 `if (!vagaData?.success || !vaga) return <VagaNotFoundState />`). NÃO inserir no loading state (linha 173-191) nem dentro de VagaNotFoundState (linha 54-85). Isto preserva UX minimalista do estado 404 (gap 3 endereça apenas o back-button do 404, não pede shell).

    **Step 4 — Corrigir VagaNotFoundState back-button (Gap 3)**. Localizar o GlassButton em linhas 71-79 e adicionar `inline-flex items-center justify-center gap-2 whitespace-nowrap` ao className. Adicionalmente, remover o `mr-2` do `<ArrowLeft>` pois o `gap-2` do parent agora controla espaçamento (evita double-spacing).

    Antes (linha 71-79):
    ```tsx
    <GlassButton
      variant="white"
      hover
      onClick={() => navigate('/vagas')}
      className="text-white font-semibold"
    >
      <ArrowLeft className="w-4 h-4 mr-2" />
      Voltar para vagas
    </GlassButton>
    ```

    Depois:
    ```tsx
    <GlassButton
      variant="white"
      hover
      onClick={() => navigate('/vagas')}
      className="inline-flex items-center justify-center gap-2 whitespace-nowrap text-white font-semibold"
    >
      <ArrowLeft className="w-4 h-4" />
      Voltar para vagas
    </GlassButton>
    ```

    **Step 5 — Aplicar o mesmo fix surgical aos outros 3 GlassButtons da página** (consistência + prevenção):
    - Share button linha 260-266 (`<Share2>` ícone-only — adicionar `inline-flex items-center justify-center` é cosmético mas mantém consistência; `whitespace-nowrap` não muda nada para ícone-only)
    - Sticky CTA hasApplied linha 458-465 e ! hasApplied linha 467-475 (ambos contêm ícone+texto e podem quebrar em viewport ≤320px)

    Para os 2 sticky CTA buttons:

    Antes (467-475):
    ```tsx
    <GlassButton
      variant="white"
      hover
      className="w-full py-4 text-white text-lg font-semibold"
      onClick={handleCandidatar}
    >
      <Send className="w-5 h-5 mr-2" />
      Candidatar-se a esta vaga
    </GlassButton>
    ```

    Depois:
    ```tsx
    <GlassButton
      variant="white"
      hover
      className="inline-flex items-center justify-center gap-2 whitespace-nowrap w-full py-4 text-white text-lg font-semibold"
      onClick={handleCandidatar}
    >
      <Send className="w-5 h-5" />
      Candidatar-se a esta vaga
    </GlassButton>
    ```

    Aplicar fix-pattern análogo (`inline-flex...gap-2 whitespace-nowrap` + remover `mr-2`) ao hasApplied button (458-465).
  </action>
  <verify>
    <automated>
      cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && \
      grep -q "sticky top-0 z-50" src/components/pages/VagaDetalhePage.tsx && \
      grep -q "showCandidatoShell" src/components/pages/VagaDetalhePage.tsx && \
      grep -q "handleLogout" src/components/pages/VagaDetalhePage.tsx && \
      grep -q "Área do candidato" src/components/pages/VagaDetalhePage.tsx && \
      grep -q "/candidato/perfil" src/components/pages/VagaDetalhePage.tsx && \
      grep -q "AvatarFallback" src/components/pages/VagaDetalhePage.tsx && \
      grep -q "LogOut" src/components/pages/VagaDetalhePage.tsx && \
      grep -q "UserCircle" src/components/pages/VagaDetalhePage.tsx && \
      grep -q "inline-flex items-center justify-center gap-2 whitespace-nowrap" src/components/pages/VagaDetalhePage.tsx && \
      [ "$(grep -c "inline-flex items-center justify-center gap-2 whitespace-nowrap" src/components/pages/VagaDetalhePage.tsx)" -ge 4 ] && \
      npm run lint && \
      npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "sticky top-0 z-50" src/components/pages/VagaDetalhePage.tsx` retorna 0
    - `grep -q "showCandidatoShell" src/components/pages/VagaDetalhePage.tsx` retorna 0
    - `grep -q "handleLogout" src/components/pages/VagaDetalhePage.tsx` retorna 0
    - `grep -q "Área do candidato" src/components/pages/VagaDetalhePage.tsx` retorna 0
    - `grep -q "/candidato/perfil" src/components/pages/VagaDetalhePage.tsx` retorna 0
    - `grep -c "inline-flex items-center justify-center gap-2 whitespace-nowrap" src/components/pages/VagaDetalhePage.tsx` ≥ 4 (Sair + Área + VagaNotFoundState back-button + sticky CTA Candidatar-se)
    - VagaNotFoundState back-button mantém o redirect `navigate('/vagas')` (verifiable via leitura visual; grep `navigate\\('/vagas'\\)` deve aparecer pelo menos 2× no arquivo — uma no breadcrumb handleVoltar e outra no VagaNotFoundState)
    - `npm run lint` exit code 0
    - `npm run build` exit code 0 (Vite production build sem erros)
    - Persona shell NÃO aparece dentro do JSX de `VagaNotFoundState` nem dentro do loading state JSX
  </acceptance_criteria>
  <done>
    Persona shell sticky aparece em /vagas/:identifier (main render path) SOMENTE quando logado como candidato. VagaNotFoundState back-button "Voltar para vagas" e os 2 sticky CTA buttons renderizam ícone+texto em uma única linha. `npm run lint` e `npm run build` passam sem novos erros.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual smoke check — confirmar fechamento dos 3 gaps em viewport real</name>
  <what-built>
    - VagasPublicasPage.tsx: persona shell auth-guarded + 6 GlassButtons com `inline-flex...gap-2 whitespace-nowrap`
    - VagaDetalhePage.tsx: persona shell auth-guarded + VagaNotFoundState back-button corrigido + 2 sticky CTA corrigidos
  </what-built>
  <how-to-verify>
    Pre-flight: `npm run dev` (Vite na porta 3003). Abrir DevTools (Network throttle off, Cache disabled).

    **Cenário 1 — /vagas anônimo (Gap 2 negativa: shell NÃO aparece + Gap 1 positiva: card-button OK):**
    1. Janela anônima/incognito → http://localhost:3003/vagas
    2. Esperado: NENHUM sticky navbar no topo da página (preserva VAGA-01 anon-browse).
    3. Esperado: Cards de vaga renderizam normalmente. Botão "Candidatar-se a esta vaga →" no card aparece com **ícone (se houver — note que neste card o botão não tem ícone explicito, é o "→" textual) e texto na MESMA linha**, ocupando largura total do card sem sobreposição com badge "Candidatura enviada" nem com "Publicada há N dias / N candidatos".

    **Cenário 2 — /vagas logado como candidato (Gap 2 positiva: shell aparece):**
    1. Login como candidato (fernando@beautysmile.com.br) em /auth/login.
    2. Após login, navegar para /vagas.
    3. Esperado: sticky navbar visível no topo, com:
       - Logo Beauty Smile (símbolo) à esquerda
       - Divider vertical
       - Avatar circular (ou iniciais "FC" em fallback) + nome "Fernando ..." + email (em viewport ≥ md)
       - Botão "Área do candidato" (ícone + texto, em md+; ícone-only em sm)
       - Botão "Sair" (ícone + texto, em md+; ícone-only em sm)
    4. Clicar em "Área do candidato" → navega para /candidato/perfil.
    5. Voltar para /vagas. Clicar em "Sair" → toast "Você saiu da sua conta com sucesso" + redirect para /auth/login. Sessão limpa (re-acessar /candidato/perfil deve redirecionar para login).

    **Cenário 3 — /vagas/:slug logado (Gap 2 positiva continua):**
    1. Login novamente como candidato. Em /vagas, clicar em qualquer card.
    2. Esperado: navega para /vagas/{slug}. Sticky navbar AINDA aparece no topo (mesmo conteúdo do cenário 2).
    3. Botão "Candidatar-se a esta vaga" no sticky CTA inferior renderiza ícone Send + texto na mesma linha.
    4. Botão "Área do candidato" no navbar funciona.
    5. Botão "Sair" no navbar funciona (logout + toast + redirect).

    **Cenário 4 — /vagas/:slug anônimo (Gap 2 negativa):**
    1. Janela anônima → cole URL direta de uma vaga (ex: /vagas/teste-coordenador-rh-sede)
    2. Esperado: detalhe da vaga renderiza. **NENHUM sticky navbar** (mantém UX original anon).
    3. Botão "Voltar para vagas" no breadcrumb superior funciona.
    4. Botão "Candidatar-se a esta vaga" no sticky CTA inferior renderiza ícone+texto na mesma linha. Clicar → redirect para /auth/login?redirect=... (VAGA-03 preserved).

    **Cenário 5 — /vagas/slug-inexistente (Gap 3):**
    1. Navegar para /vagas/this-slug-does-not-exist (anônimo OU logado — VagaNotFoundState não tem shell em ambos os casos por design).
    2. Esperado: VagaNotFoundState renderiza ("Vaga não encontrada / Vaga não encontrada ou não está mais ativa").
    3. **GAP 3 fix:** Botão "Voltar para vagas" renderiza ícone ArrowLeft + texto "Voltar para vagas" em **uma única linha**. Sem quebra.
    4. Clicar no botão → navega para /vagas (lista) corretamente.

    **Cenário 6 — Responsividade mobile (sm breakpoint, ~ 375px width):**
    1. DevTools → device toolbar → iPhone SE (375x667).
    2. /vagas logado: navbar mostra apenas o logo + avatar (sem nome/email — `hidden md:block`) + ícones-only nos botões "Área do candidato" e "Sair" (texto escondido por `hidden sm:inline`).
    3. /vagas/:slug 404: botão "Voltar para vagas" continua em uma linha mesmo no mobile (`whitespace-nowrap` previne quebra).
    4. Card-button "Candidatar-se a esta vaga →" continua em uma linha (mobile).

    **Acceptance criteria visuais (lista de bullets para responder ao orchestrator):**
    - [ ] Cenário 1: anon /vagas SEM navbar; card-button OK
    - [ ] Cenário 2: logged-in /vagas COM navbar; Área-do-candidato + Sair funcionam
    - [ ] Cenário 3: logged-in /vagas/:slug COM navbar; sticky CTA OK
    - [ ] Cenário 4: anon /vagas/:slug SEM navbar; CTA → /auth/login OK
    - [ ] Cenário 5: 404 back-button em uma linha; navega para /vagas
    - [ ] Cenário 6: mobile responsivo OK
  </how-to-verify>
  <files>src/components/pages/VagasPublicasPage.tsx, src/components/pages/VagaDetalhePage.tsx</files>
  <action>Run `npm run dev` and walk through the 6 cenários in <how-to-verify>. Wait at this checkpoint until human approves.</action>
  <verify>
    <automated>echo "Manual visual checkpoint — see <how-to-verify> in this task. No automated command runs here; the executor must wait for human resume-signal." && exit 0</automated>
  </verify>
  <done>6/6 visual cenários approved by human; type "approved" or list failures by cenário number.</done>
  <resume-signal>Type "approved" se 6/6 cenários passarem, OR descreva o que falhou (cenário N — comportamento observado vs esperado).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → React state (authStore) | Navbar reads `isAuthenticated`, `role`, `candidato` from Zustand. Trust source = JWT-derived state already validated por extractRole utility. |
| Browser → React Router | Logout dispara `navigate('/auth/login', { replace: true })` com path estático hard-coded. |
| React → Supabase Auth | `authStore.logout()` chama `supabase.auth.signOut()` que invalida sessão server-side + limpa cookies HttpOnly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Information Disclosure | VagasPublicasPage navbar | mitigate | Guard `showCandidatoShell = isAuthenticated && role === 'candidato'` previne renderização do PII (nome/email/avatar) para anônimos. Anon path nunca lê `candidato` em DOM. |
| T-04-09-02 | Information Disclosure | VagaDetalhePage navbar | mitigate | Mesmo guard `showCandidatoShell`. Adicionalmente, navbar não é renderizado em VagaNotFoundState nem no loading state (paths que precedem auth-aware data). |
| T-04-09-03 | Repudiation / Open redirect | handleLogout | accept | navigate path é literal `/auth/login` hard-coded; nenhum query param consumido no logout flow; sem risco de open-redirect. |
| T-04-09-04 | Tampering / Clickjacking | Sair button | accept | Mitigado por SameSite cookie attrs já configurados no Supabase Auth e por X-Frame-Options no SPA (Phase 1). UI gap-closure não introduz nova superfície de clickjacking. |
| T-04-09-05 | Elevation of Privilege | Persona shell exposure to non-candidato roles | mitigate | Guard exige `role === 'candidato'` (não apenas `isAuthenticated`). Usuários `rh`/`administrador` que naveguem para /vagas não veem o shell de candidato (evita confusão de persona + previne CSRF de logout cross-persona). |

**No high-severity threats; gap-closure é UI-only e reusa `authStore.logout()` que já é bem-testado em MeuPerfilCandidatoPage / FormularioCandidaturaPage. Nenhuma nova rota, nenhum novo endpoint, nenhum novo RLS policy, zero PII writes.**
</threat_model>

<verification>
**Plan-level automated gates** (run após Tasks 1+2 antes do checkpoint visual):

```bash
cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento"

# Gap 2 — persona shell em ambas as páginas
grep -q "sticky top-0 z-50" src/components/pages/VagasPublicasPage.tsx
grep -q "sticky top-0 z-50" src/components/pages/VagaDetalhePage.tsx
grep -q "showCandidatoShell" src/components/pages/VagasPublicasPage.tsx
grep -q "showCandidatoShell" src/components/pages/VagaDetalhePage.tsx

# Gap 2 — Área do candidato link em ambas
grep -q "Área do candidato" src/components/pages/VagasPublicasPage.tsx
grep -q "Área do candidato" src/components/pages/VagaDetalhePage.tsx
grep -q "/candidato/perfil" src/components/pages/VagasPublicasPage.tsx
grep -q "/candidato/perfil" src/components/pages/VagaDetalhePage.tsx

# Gap 2 — handleLogout presente em ambas
grep -q "handleLogout" src/components/pages/VagasPublicasPage.tsx
grep -q "handleLogout" src/components/pages/VagaDetalhePage.tsx

# Gaps 1 + 3 — fix surgical do GlassButton (counts cobrem todos os call sites tocados)
[ "$(grep -c "inline-flex items-center justify-center gap-2 whitespace-nowrap" src/components/pages/VagasPublicasPage.tsx)" -ge 6 ]
[ "$(grep -c "inline-flex items-center justify-center gap-2 whitespace-nowrap" src/components/pages/VagaDetalhePage.tsx)" -ge 4 ]

# Build + lint regression
npm run lint
npm run build
```

**Manual gate (Task 3 checkpoint):** 6 cenários visuais (anon+logged-in × /vagas+/vagas/:slug + 404 + mobile).
</verification>

<success_criteria>
- 3 gaps do `04-UAT.md ## Gaps` fechados (Gap 1 minor, Gap 2 major, Gap 3 minor)
- Persona shell canonical (D-27) replicado em VagasPublicasPage + VagaDetalhePage com guard `isAuthenticated && role === 'candidato'`
- Link "Área do candidato" → /candidato/perfil presente em ambas as páginas
- Botão "Sair" → handleLogout (logout + toast + navigate('/auth/login', {replace:true})) em ambas as páginas
- 10 GlassButtons total (6 em VagasPublicasPage + 4 em VagaDetalhePage) com `inline-flex items-center justify-center gap-2 whitespace-nowrap` impedindo quebra de linha entre ícone e texto
- `npm run lint` zero novos erros TS
- `npm run build` exit code 0
- Visual smoke 6/6 cenários PASS
- VAGA-02 + VAGA-03 traceability mantida (mesmas requirements IDs do escopo original Phase 4)
- Anon-browse de /vagas (VAGA-01) preservado: não-autenticados continuam vendo a página exatamente como antes
</success_criteria>

<output>
After Tasks 1+2 complete and Task 3 checkpoint approved, create:
`.planning/phases/04-vagas-candidatura/04-09-SUMMARY.md`

Summary should document:
- 3 gaps closed (mapping each to the targeted file + lines + diff size)
- Decision rationale: Option B (surgical) chosen over Option A (root fix to glass.tsx) — risk surface 197 GlassButton call sites em 24 arquivos vs touching exactly 10 buttons em 2 arquivos
- D-27 persona shell extension: link "Área do candidato" added (NEW vs MeuPerfilCandidatoPage precedent that has no such link)
- Phase 4 final state: 8 plans + 3 carryovers + 1 gap-closure plan = 12 PLAN files; UAT phase-level moves from 9/10 to 10/10 PASS
- No regressions on previously-passing tests 1, 3-9 (which did not touch the modified files in any meaningful way besides the GlassButton class additions)
</output>
