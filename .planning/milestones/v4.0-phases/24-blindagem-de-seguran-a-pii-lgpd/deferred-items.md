# Phase 24 — Deferred Items

Out-of-scope discoveries logged during execution. NOT fixed in-phase.

| Category | Item | Found During | Status | Notes |
|----------|------|--------------|--------|-------|
| Security / PII | `src/features/cadastro/services/n8nService.ts` hardcodes **9** n8n webhook URLs (`n8n.srv881294.hstgr.cloud`, test + production) directly in client source, and `sendToN8N`/`notifyCandidatoCriado` POST candidate **PII** (nome, email, telefone, **cpf**) from the browser. This is a SECOND bundle-webhook leak, distinct from the SEC-03 `fernandocosta.app.n8n.cloud` host. | 24-05 (SEC-03) | DEFERRED | OUT OF SCOPE for 24-05: the plan scopes SEC-03 to the 3 fernandocosta sites (candidaturasService ×2 + explicacaoService). The hstgr host contains NO `n8n.cloud`/`fernandocosta` literal, so the 24-05 grep guard neither covers nor false-flags it. This is a real PII-in-bundle leak (same class as SEC-03). **✅ ROUTED → Phase 26 (funil candidato)** by Fernando 2026-07-09 (cadastro is candidate-side): move dispatch server-side (pg_net + Vault, SEC-03 pattern) + delete the client URLs + PII payloads + bundle grep guard. Threat-flag surfaced in 24-05-SUMMARY, re-flagged by the P24 verifier + security auditor. |
| Test (pre-existing RED) | `src/features/avaliacao/services/__tests__/redacaoService.test.ts` — 3 tests fail against `redacaoService.ts:179`. | 24-07 (UX-08) | ✅ RESOLVED (86fe7ea) | Post-wave gate fix: the 24-03 SEC-02 rewire moved the candidate read from `.from().select(allowlist)` to `.rpc('get_minha_redacao')`; the old Phase-13 guard mocked only `.from()`. Updated the guard to the new network invariant (routes through the RPC, never base-table-selects redacoes_candidato, passes only the id). Full vitest 751/751 green. |
| Security / config | **SEC-03 n8n Vault secret `n8n_webhook_base` not created in PROD** — the server-side triggers (24-08) graceful-skip (no error) until it is set. | 24-08 (SEC-03 landing) | DEFERRED (human-action) | The value is Fernando's real n8n webhook base URL — an operational secret; not fabricated. The SEC-03 *security* requirement (no URL in the public bundle) is already met by the migration + 24-05 client-subtree removal. Set via `vault.create_secret('<n8n base url>', 'n8n_webhook_base')` or the Supabase dashboard to re-enable live dispatch. |
| Tech-debt | **`database.types.ts` regen + drop the confined RPC casts** (`get_cognitivo_itens`, `get_minha_redacao`) | 24-08 (SEC-01/02 landing) | DEFERRED → Phase 27 | The new RPCs are live but not in the generated types; 24-02/24-03 confined the untyped `.rpc` calls behind narrow casts that are self-contained and green. A 170k-char one-line regen mid-landing adds risk without security value → do it in Phase 27 (migration/ledger reconstruction, the natural home for a type regen) and drop the casts then. |
| Cleanup | **`submit-candidatura` redundant n8n `N8N_NOVA_CANDIDATURA_URL` env-var fire** — now that the SEC-03 `trg_n8n_nova_candidatura` trigger owns that dispatch canonically. | 24-09 (SEC-03/04 redeploy) | DEFERRED (bundle with the Vault secret) | No active double-fire today: the SEC-03 trigger graceful-skips until the `n8n_webhook_base` Vault secret is set. When Fernando sets that secret, drop the EF's env-var fire + redeploy `submit-candidatura` to avoid a double nova-candidatura webhook. |
