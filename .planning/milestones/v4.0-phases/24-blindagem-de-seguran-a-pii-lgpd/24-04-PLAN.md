---
phase: 24-blindagem-de-seguran-a-pii-lgpd
plan: 04
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - supabase/migrations/20260706110004_sec05_08_vaga_scope.sql
  - supabase/tests/sec05_08_smokes.sql
autonomous: true             # code-local authoring only; PROD apply is 24-08
requirements: [SEC-05, SEC-06, SEC-08]

must_haves:
  truths:
    - "SEC-05: a recruiter who is NOT the vaga owner reads 0 rows from analise_candidato_vaga and comparativo_solicitado — SELECT policies swap from role-only to vaga-scoped (created_by = auth.uid(), administrador bypass) per CONTEXT SEC-05 decision and the WR-04 template"
    - "SEC-08: base-table candidaturas RH policies (rh_le_candidaturas SELECT + rh_avanca_etapa UPDATE) are vaga-scoped (USING + WITH CHECK on UPDATE); candidato_le_propria_candidatura stays untouched"
    - "SEC-06: reprocessar_analise is ALREADY vaga-scoped (20260610000003:52-59) — regression-guard only, do NOT re-author; and redacoes_candidato RH policies (redacao_rh_select/update, confirmed role-only in A30/M15) are swapped to vaga-scoped via the candidaturas→vagas JOIN in this same migration"
    - "All predicates use the (select auth.jwt() #>> '{app_metadata,role}') planner-cache idiom verbatim from WR-04 (20260625000001:294-327); scores_candidato/entrevista_analises/entrevista_guias/decisao_final are ALREADY WR-04-scoped — regression-guard only, do NOT re-scope"
    - "No policy grants a write to candidaturas beyond the existing scoped UPDATE; nothing auto-rejects (RNF-07a) — only SELECT/UPDATE tightening"
  artifacts:
    - path: "supabase/migrations/20260706110004_sec05_08_vaga_scope.sql"
      provides: "vaga-scoped SELECT/UPDATE policies for analise_candidato_vaga, comparativo_solicitado, candidaturas, redacoes_candidato RH"
      contains: "created_by = (select auth.uid())"
  key_links:
    - from: "rh_le_analise / rh_le_comparativo / rh_le_candidaturas"
      to: "vagas.created_by ownership"
      via: "vaga_id IN (SELECT id FROM vagas WHERE created_by = auth.uid())"
      pattern: "vaga_id IN \\(SELECT id FROM public.vagas WHERE created_by"
    - from: "rh_avanca_etapa (UPDATE)"
      to: "USING + WITH CHECK vaga-scope"
      via: "both clauses scoped"
      pattern: "WITH CHECK"
---

<objective>
Close the horizontal (cross-recruiter) read leak: today a recruiter with `app_metadata.role='rh'` can read EVERY vaga's analysis, comparative and candidaturas because the SELECT policies are role-only, not ownership-scoped. Re-emit the shipped WR-04 predicate (administrador bypass + rh-own-vaga) onto the three role-only tables + the redacoes_candidato RH policies. This is verbatim precedent — the failure mode is inventing a new predicate instead of copying the one that already ships in 4 tables.

Purpose: A non-owner recruiter querying another recruiter's vaga gets 0 rows; the owner and administrador still get their rows. `reprocessar_analise` (already scoped) is regression-guarded, not rewritten.
Output: 1 migration file (5+ DROP/CREATE policy blocks), 1 SQL smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Migration FILE only — PROD apply is 24-08. NO outer BEGIN/COMMIT. This migration touches redacoes_candidato RH policies (redacao_rh_select/update) — a DIFFERENT set of policies than 24-03's candidate policy on the same table; the two migration files are disjoint (no files_modified overlap) and both land in 24-08. Do NOT touch redacao_candidato_select here (that is 24-03) and do NOT touch candidato_le_propria_candidatura.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-RESEARCH.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-PATTERNS.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-LIVE-STATE.md

<interfaces>
<!-- Copy the WR-04 predicate verbatim; the exact 5 replacement blocks are in RESEARCH §Code Examples lines 291-321 -->
WR-04 template:              supabase/migrations/20260625000001_phase14_gap_closure.sql:294-327
Current role-only (REPLACE): 20260610000001_analise_tables.sql:78-89 (rh_le_analise, rh_le_comparativo) · 20260607000006_rls_policies_m2_backbone.sql:44-58 (rh_le_candidaturas SELECT + rh_avanca_etapa UPDATE) · 20260623100003_redacoes_candidato.sql:124-135 (redacao_rh_select, redacao_rh_update)
Already-scoped (GUARD only): 20260610000003_reprocessar_rpc.sql:52-59 (reprocessar_analise) · scores_candidato/entrevista_analises/entrevista_guias/decisao_final (Phase 14/15)
Column facts:               analise_candidato_vaga.vaga_id, comparativo_solicitado.vaga_id, candidaturas.vaga_id exist DIRECTLY → scope on vaga_id; redacoes_candidato scopes via candidaturas→vagas JOIN
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vaga-scoped RLS migration (analise, comparativo, candidaturas, redacoes RH)</name>
  <files>supabase/migrations/20260706110004_sec05_08_vaga_scope.sql</files>
  <read_first>
    - RESEARCH §Code Examples "the three vaga-scoped policies (verbatim WR-04)" (lines 291-323) — copy the 5 DROP/CREATE blocks verbatim
    - `supabase/migrations/20260625000001_phase14_gap_closure.sql:294-327` (WR-04 template)
    - Current policies to replace: `20260610000001_analise_tables.sql:78-89`, `20260607000006_rls_policies_m2_backbone.sql:44-58`, `20260623100003_redacoes_candidato.sql:124-135`
    - PATTERNS §Wave B (lines 153-184) incl. the A30 adjacent-table note
    - 24-LIVE-STATE.md (confirm these are still role-only live)
  </read_first>
  <action>
    Author `20260706110004_sec05_08_vaga_scope.sql` with these DROP/CREATE blocks (copy the exact SQL from RESEARCH §Code Examples 291-321): (1) SEC-05 `rh_le_analise` on analise_candidato_vaga → FOR SELECT USING (role='administrador' OR (role='rh' AND vaga_id IN (SELECT id FROM public.vagas WHERE created_by = (select auth.uid())))). (2) SEC-05 `rh_le_comparativo` on comparativo_solicitado → same predicate on its vaga_id. (3) SEC-08 `rh_le_candidaturas` on candidaturas → same SELECT predicate. (4) SEC-08 `rh_avanca_etapa` on candidaturas → FOR UPDATE with BOTH USING and WITH CHECK scoped. (5) SEC-06 (A30 include) `redacao_rh_select` + `redacao_rh_update` on redacoes_candidato → scope via candidatura_id IN (SELECT c.id FROM candidaturas c JOIN vagas v ON v.id = c.vaga_id WHERE v.created_by = (select auth.uid())), admin bypass (redacoes has no direct vaga_id → JOIN). Use the `(select auth.jwt() #>> '{app_metadata,role}')` idiom everywhere. Leave candidato_le_propria_candidatura and redacao_candidato_select (24-03) untouched. Add a comment noting devolutivas_candidato (20260612000002:57) + historico_candidatura (20260607000006:74) are the remaining role-only tables, DEFERRED to Phase 25's funil-RH sweep (RESEARCH Open Q1). No outer BEGIN/COMMIT.
  </action>
  <acceptance_criteria>
    - Migration contains 6 CREATE POLICY blocks (rh_le_analise, rh_le_comparativo, rh_le_candidaturas, rh_avanca_etapa, redacao_rh_select, redacao_rh_update), each preceded by DROP POLICY IF EXISTS
    - rh_avanca_etapa has BOTH USING and WITH CHECK scoped to owned vagas
    - Every predicate contains `created_by = (select auth.uid())` and the `administrador` bypass branch
    - The migration does NOT alter candidato_le_propria_candidatura or redacao_candidato_select, and does NOT re-author reprocessar_analise / scores_candidato / entrevista_* / decisao_final
    - devolutivas_candidato + historico_candidatura defer note present; no candidaturas write beyond the scoped UPDATE (RNF-07a)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "CREATE POLICY" supabase/migrations/20260706110004_sec05_08_vaga_scope.sql ⇒ 6; grep -c "created_by = (select auth.uid())" ⇒ ≥6; grep -c "WITH CHECK" ⇒ ≥1; grep -c "candidato_le_propria_candidatura\|redacao_candidato_select" ⇒ 0. Behavioral non-owner 0-rows smoke runs in 24-08.</automated>
  </verify>
  <done>Three role-only tables + redacoes RH policies re-emitted vaga-scoped (WR-04); non-owner recruiter horizontally isolated; candidate + already-scoped paths untouched.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-05/06/08 SQL smoke (non-owner 0-rows + reprocessar regression-guard)</name>
  <files>supabase/tests/sec05_08_smokes.sql</files>
  <read_first>
    - RESEARCH §Validation SEC-05/06/08 rows (lines 457-458)
    - PATTERNS §Wave B — the reprocessar 42501 regression-guard note (lines 175-182)
    - M2 SECURITY-gate `set_config('request.jwt.claims', …)` idiom
  </read_first>
  <action>
    Create `supabase/tests/sec05_08_smokes.sql` with three jwt.claims fixtures: (owner-rh, non-owner-rh, administrador). For each of analise_candidato_vaga / comparativo_solicitado / candidaturas / redacoes_candidato, assert: non-owner-rh SELECT of another recruiter's vaga rows → 0 rows; owner-rh → rows present; administrador → all rows. Add an UPDATE smoke: non-owner-rh UPDATE candidaturas of a foreign vaga → 0 rows affected / denied (WITH CHECK). Add a SEC-06 regression-guard: non-owner-rh CALL reprocessar_analise on a foreign vaga → 42501 (insufficient_privilege). Add an RNF-07a regression: assert pontuar_* never wrote candidaturas (no status flip). ROLLBACK-free cleanup. Executed against PROD in 24-08.
  </action>
  <acceptance_criteria>
    - Smoke covers 4 tables × {owner→rows, non-owner→0, admin→all}
    - Includes the candidaturas UPDATE denial and the reprocessar_analise 42501 non-owner regression-guard
    - Includes an RNF-07a assertion (pontuar path did not write candidaturas)
    - Uses set_config('request.jwt.claims', …) for all three roles
  </acceptance_criteria>
  <verify>
    <automated>grep -c "set_config('request.jwt.claims'" supabase/tests/sec05_08_smokes.sql ⇒ ≥3; grep -ci "reprocessar_analise\|42501\|insufficient_privilege" ⇒ ≥1. The .sql executes against PROD in 24-08.</automated>
  </verify>
  <done>Repeatable non-owner-0-rows + reprocessar-42501 + RNF-07a smoke authored for 24-08 execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recruiter JWT (role='rh') → PostgREST | non-owner recruiter reads another recruiter's vaga analysis/comparative/candidaturas/verdict (horizontal access) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | Information Disclosure | role-only SELECT on analise_candidato_vaga/comparativo_solicitado leaks across recruiters | mitigate | vaga-scoped SELECT (created_by = auth.uid(), admin bypass); smoke proves non-owner → 0 rows |
| T-24-04-02 | Information Disclosure / Tampering | role-only SELECT+UPDATE on base candidaturas | mitigate | vaga-scoped rh_le_candidaturas + rh_avanca_etapa (USING+WITH CHECK); non-owner UPDATE denied |
| T-24-04-03 | Information Disclosure | role-only redacoes RH verdict read across recruiters (A30/M15) | mitigate | redacao_rh_select/update scoped via candidaturas→vagas JOIN |
| T-24-04-04 | Elevation of Privilege | reprocessar_analise regression | mitigate | already vaga-scoped; SQL smoke regression-guards non-owner → 42501 (no re-author) |
| T-24-04-SC | Tampering | package installs | accept | zero new packages (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- 6 policies re-emitted vaga-scoped; candidate + already-scoped tables untouched
- reprocessar regression-guard + RNF-07a assertion in the smoke
- Migration authored (no PROD apply here); no outer BEGIN/COMMIT
</verification>

<success_criteria>
- SEC-05: analise/comparativo non-owner → 0 rows
- SEC-08: candidaturas non-owner SELECT → 0 rows, UPDATE denied
- SEC-06: reprocessar regression-guarded (42501 non-owner) + redacoes RH scoped
</success_criteria>

<output>
Create `.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-04-SUMMARY.md` when done.
</output>
