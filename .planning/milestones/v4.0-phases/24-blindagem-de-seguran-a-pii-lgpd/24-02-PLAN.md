---
phase: 24-blindagem-de-seguran-a-pii-lgpd
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - supabase/migrations/20260706110001_sec01_cognitivo_gabarito.sql
  - supabase/migrations/20260706110002_sec07_rubric.sql
  - src/features/avaliacao-cognitiva/services/cognitivoService.ts
  - src/features/avaliacao/services/avaliacaoService.ts
  - supabase/tests/sec01_07_smokes.sql
  - src/features/avaliacao-cognitiva/__tests__/cognitivoService.rpc.test.ts
  - src/features/avaliacao/__tests__/avaliacaoService.rubric.test.ts
autonomous: true             # code-local authoring only; PROD apply is 24-08
requirements: [SEC-01, SEC-07]

must_haves:
  truths:
    - "SEC-01: a candidate reading cognitivo_itens can never obtain gabarito_idx — column REVOKE FROM authenticated,anon + candidate-DENY base row + a new SECURITY DEFINER RPC get_cognitivo_itens() that projects id/secao/enunciado/alternativas/ordem only (per CONTEXT SEC-01 decision: defense-in-depth REVOKE + RPC, not RPC-only)"
    - "SEC-07: the candidate-facing perguntas projection drops the rubric (BARS) column via explicit allowlist + column REVOKE FROM authenticated,anon (per CONTEXT SEC-07 decision)"
    - "SEC-01/07 use the column-REVOKE mechanism because NO authenticated client legitimately reads gabarito_idx or rubric (only DEFINER RPC / avaliar-redacao service_role) — distinct from SEC-02 which must NOT REVOKE (RESEARCH Pitfall 1)"
    - "The client rewire (cognitivoService.listItens → rpc) lands in the SAME plan as the migration so the candidate prova is never left with 0 rows (RESEARCH Pitfall 2)"
    - "No new/edited RPC writes candidaturas or auto-rejects; every RPC keeps SET search_path = '' and REVOKE PUBLIC / GRANT authenticated (RNF-07a)"
  artifacts:
    - path: "supabase/migrations/20260706110001_sec01_cognitivo_gabarito.sql"
      provides: "get_cognitivo_itens DEFINER RPC + REVOKE(gabarito_idx) + DROP auth_le_cognitivo_itens"
      contains: "get_cognitivo_itens"
    - path: "supabase/migrations/20260706110002_sec07_rubric.sql"
      provides: "REVOKE(rubric) ON perguntas FROM authenticated,anon"
      contains: "REVOKE"
    - path: "src/features/avaliacao-cognitiva/services/cognitivoService.ts"
      provides: "listItens rewired to .rpc('get_cognitivo_itens')"
      contains: "get_cognitivo_itens"
  key_links:
    - from: "cognitivoService.listItens"
      to: "get_cognitivo_itens RPC"
      via: ".rpc call (no .from('cognitivo_itens'))"
      pattern: "rpc\\('get_cognitivo_itens'"
    - from: "avaliacaoService perguntas projection"
      to: "perguntas (no rubric)"
      via: "allowlist select without rubric"
      pattern: "select\\((?!.*rubric)"
---

<objective>
Close the two column-secrecy leaks whose columns NO authenticated client legitimately reads, so the safe mechanism is column REVOKE + a SECURITY DEFINER reader (SEC-01 gabarito) and column REVOKE + client allowlist drop (SEC-07 rubric). This plan is deliberately paired and kept SEPARATE from SEC-02 (24-03) to reinforce the #1 landmine: REVOKE is valid here, forbidden there.

Purpose: A candidate who does `GET /rest/v1/cognitivo_itens?select=gabarito_idx` must receive 0 answer columns (SEC-01); the candidate perguntas projection must not carry the BARS `rubric` answer-key (SEC-07). Both bind at the Postgres boundary (column GRANT), with the client allowlist as defense-in-depth only.
Output: 2 migration files, 2 client-service rewires, 1 SQL smoke file, 2 vitest projection guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Migration FILES only — do NOT apply to PROD here (that is 24-08 via Supabase MCP). Author with NO outer `BEGIN;…COMMIT;` wrapper (CLAUDE.md §Migrations workaround). Read `24-LIVE-STATE.md` first: if Task-1 flagged a hidden authenticated reader of gabarito_idx/rubric (A3 FLAGGED), STOP and surface it — the REVOKE would break that reader.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-RESEARCH.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-PATTERNS.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-LIVE-STATE.md

<interfaces>
<!-- Analog (copy the shape from these — do NOT re-derive) -->
DEFINER answer-key reader:  supabase/migrations/20260612000001_bigfive_itens.sql:200-212 (get_bigfive_itens) · 20260611000002_perguntas_sjt.sql:107-130 (get_opcoes_sjt)
REVOKE idiom:               20260612000001_bigfive_itens.sql:211
SEC-01 target table:        20260624000001_entrevista_cognitivo_tables.sql — gabarito_idx L111, broad policy auth_le_cognitivo_itens L162-165
SEC-07 target column:       20260611000002_perguntas_sjt.sql — rubric L50, candidate row policy cand_le_perguntas_ativas L69-72
Client allowlist idiom:     cognitivoService.ts:59 (COGNITIVO_ITENS_ALLOWLIST), pontuar_cognitivo rpc idiom same file :215
SEC-07 leak site:           avaliacaoService.ts:136 — .select('id, cargo, cenario, formato, tempo_est_min, rubric, status')
Regression guard (keep):    prova-cognitiva.test.tsx:128-155 (COGNITIVO_ITENS_ALLOWLIST asserts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEC-01 migration + cognitivoService RPC rewire</name>
  <files>supabase/migrations/20260706110001_sec01_cognitivo_gabarito.sql, src/features/avaliacao-cognitiva/services/cognitivoService.ts</files>
  <read_first>
    - `supabase/migrations/20260612000001_bigfive_itens.sql:200-212` (get_bigfive_itens DEFINER reader — copy the shape) + `:211` (REVOKE idiom)
    - `supabase/migrations/20260624000001_entrevista_cognitivo_tables.sql:105-166` (cognitivo_itens table, gabarito_idx L111, auth_le_cognitivo_itens policy L162-165)
    - `src/features/avaliacao-cognitiva/services/cognitivoService.ts:59,154-179,215` (COGNITIVO_ITENS_ALLOWLIST, listItens, pontuar_cognitivo rpc idiom)
    - RESEARCH §Architecture "SECURITY DEFINER answer-key reader" (lines 111-131), Pitfall 2
    - 24-LIVE-STATE.md (Task-1 findings — confirm A3)
  </read_first>
  <action>
    Author `20260706110001_sec01_cognitivo_gabarito.sql`: create `public.get_cognitivo_itens()` returning TABLE(id uuid, secao text, enunciado text, alternativas jsonb, ordem int), LANGUAGE sql SECURITY DEFINER SET search_path = '', selecting those columns from public.cognitivo_itens ORDER BY ordem — NEVER gabarito_idx. Add `REVOKE ALL ON FUNCTION public.get_cognitivo_itens() FROM PUBLIC; GRANT EXECUTE ... TO authenticated`. Add defense-in-depth `REVOKE SELECT (gabarito_idx) ON public.cognitivo_itens FROM authenticated, anon`. Drop the broad candidate row policy `auth_le_cognitivo_itens` (candidate now reads only via the RPC). No outer BEGIN/COMMIT. Then rewire `cognitivoService.listItens` from `.from('cognitivo_itens').select(COGNITIVO_ITENS_ALLOWLIST)...` to `.rpc('get_cognitivo_itens')`, mirroring the existing `.rpc('pontuar_cognitivo')` idiom at :215. Keep the `COGNITIVO_ITENS_ALLOWLIST` constant and the prova-cognitiva.test.tsx regression as a second guard. cognitivo_itens is empty today (CC0 seed deferred to M5) so this leak is LATENT but must close before the seed — SEC-01 is the CC0 prerequisite.
  </action>
  <acceptance_criteria>
    - Migration contains `CREATE OR REPLACE FUNCTION public.get_cognitivo_itens()` with `SECURITY DEFINER` and `SET search_path = ''` and NO `gabarito_idx` in its projection
    - Migration contains `REVOKE SELECT (gabarito_idx) ON public.cognitivo_itens FROM authenticated, anon` and `DROP POLICY IF EXISTS auth_le_cognitivo_itens`
    - Migration has GRANT EXECUTE ... TO authenticated and REVOKE ... FROM PUBLIC; no outer BEGIN/COMMIT wrapper
    - cognitivoService.listItens calls `.rpc('get_cognitivo_itens')` and no longer `.from('cognitivo_itens').select(...)` for the item list
    - Migration writes nothing to candidaturas (RNF-07a): grep of the migration for `candidaturas` (non-comment) returns 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260706110001_sec01_cognitivo_gabarito.sql | grep -c "get_cognitivo_itens\|REVOKE SELECT (gabarito_idx)" ⇒ ≥2; grep -c "rpc('get_cognitivo_itens')" src/features/avaliacao-cognitiva/services/cognitivoService.ts ⇒ ≥1; npm run test:run -- prova-cognitiva passes. Behavioral candidato-DENY smoke runs against PROD in 24-08.</automated>
  </verify>
  <done>gabarito_idx unreachable via the base table (column REVOKE + dropped row policy); candidate reads items via get_cognitivo_itens; client rewired in lockstep.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-07 rubric column REVOKE + avaliacaoService allowlist drop</name>
  <files>supabase/migrations/20260706110002_sec07_rubric.sql, src/features/avaliacao/services/avaliacaoService.ts</files>
  <read_first>
    - `src/features/avaliacao/services/avaliacaoService.ts:135-136` (the ONLY candidate reader of rubric — `.select('id, cargo, cenario, formato, tempo_est_min, rubric, status')`)
    - `supabase/migrations/20260611000002_perguntas_sjt.sql:50` (rubric column), `:69-72` (cand_le_perguntas_ativas row policy — KEEP it; candidate still needs other perguntas columns)
    - `supabase/migrations/20260612000001_bigfive_itens.sql:211` (REVOKE idiom)
    - RESEARCH §Architecture SEC-07 row (line 109), 24-LIVE-STATE.md (A3)
  </read_first>
  <action>
    Author `20260706110002_sec07_rubric.sql`: add `REVOKE SELECT (rubric) ON public.perguntas FROM authenticated, anon` (only the `avaliar-redacao` EF via service_role legitimately reads rubric). Do NOT drop or alter `cand_le_perguntas_ativas` — the candidate still needs the other perguntas columns; only the single column is revoked. No outer BEGIN/COMMIT. Then edit `avaliacaoService.ts:136` to remove `rubric` from the select allowlist, leaving `'id, cargo, cenario, formato, tempo_est_min, status'`. This is the client defense-in-depth drop; the REVOKE is the boundary.
  </action>
  <acceptance_criteria>
    - Migration contains `REVOKE SELECT (rubric) ON public.perguntas FROM authenticated, anon` and does NOT touch cand_le_perguntas_ativas
    - avaliacaoService.ts perguntas select no longer contains the token `rubric`
    - Migration writes nothing to candidaturas (non-comment grep for candidaturas ⇒ 0)
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^--' supabase/migrations/20260706110002_sec07_rubric.sql | grep -c "REVOKE SELECT (rubric)" ⇒ 1; grep -c "rubric" src/features/avaliacao/services/avaliacaoService.ts (in the perguntas select) ⇒ 0. Behavioral smoke in 24-08.</automated>
  </verify>
  <done>Candidate perguntas projection carries no BARS rubric; the column is REVOKE-bound at Postgres.</done>
</task>

<task type="auto">
  <name>Task 3: SQL smoke file + vitest projection guards (SEC-01/07)</name>
  <files>supabase/tests/sec01_07_smokes.sql, src/features/avaliacao-cognitiva/__tests__/cognitivoService.rpc.test.ts, src/features/avaliacao/__tests__/avaliacaoService.rubric.test.ts</files>
  <read_first>
    - RESEARCH §Validation "Wave 0 Gaps" (lines 469-474) + §Test Map SEC-01/SEC-07 rows
    - The M2 SECURITY-gate idiom: `set_config('request.jwt.claims', …)` simulating candidato, asserting permission-denied / 0 columns (PATTERNS §Wave 0)
    - `src/features/avaliacao-cognitiva/__tests__/prova-cognitiva.test.tsx:128-155` (existing allowlist regression to mirror)
  </read_first>
  <action>
    Create `supabase/tests/sec01_07_smokes.sql` — a repeatable SQL smoke (M2 SECURITY-gate idiom) with a candidato fixture that `set_config('request.jwt.claims', '{"sub":"...","role":"authenticated","app_metadata":{"role":"candidato"}}', true)` then asserts: (a) `SELECT gabarito_idx FROM public.cognitivo_itens` raises permission-denied / returns no such accessible column; (b) `SELECT * FROM public.get_cognitivo_itens()` returns rows WITHOUT a gabarito_idx column; (c) `SELECT rubric FROM public.perguntas` raises permission-denied. Include ROLLBACK-free cleanup. This file is EXECUTED against PROD in 24-08; here it is authored + committed as the repeatable test. Also add two vitest guards: `cognitivoService.rpc.test.ts` asserting listItens calls `.rpc('get_cognitivo_itens')` (mock supabase, assert the rpc name, assert no `.from('cognitivo_itens')`); `avaliacaoService.rubric.test.ts` asserting the perguntas select string excludes `rubric`.
  </action>
  <acceptance_criteria>
    - sec01_07_smokes.sql exists and contains `set_config('request.jwt.claims'`, a gabarito_idx denial assertion, a get_cognitivo_itens no-key assertion, and a rubric denial assertion
    - cognitivoService.rpc.test.ts asserts the rpc call and the absence of a direct `.from('cognitivo_itens')` list read
    - avaliacaoService.rubric.test.ts asserts `rubric` is absent from the perguntas projection
    - Both vitest files pass under `npm run test:run`
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- cognitivoService.rpc avaliacaoService.rubric ⇒ green; grep -c "set_config('request.jwt.claims'" supabase/tests/sec01_07_smokes.sql ⇒ ≥1. The .sql executes against PROD in 24-08.</automated>
  </verify>
  <done>Repeatable candidato-DENY smoke authored; client projection guards green — the SEC-01/07 boundary is regression-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate JWT → PostgREST | `?select=gabarito_idx` / `?select=rubric` column disclosure (RLS never hides a column) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Information Disclosure | cognitivo_itens.gabarito_idx readable by any authenticated JWT | mitigate | Column REVOKE FROM authenticated,anon + DROP auth_le_cognitivo_itens + get_cognitivo_itens DEFINER reader (no key); SQL smoke proves candidato → permission denied |
| T-24-02-02 | Information Disclosure | perguntas.rubric (BARS answer key) in candidate projection | mitigate | REVOKE SELECT(rubric) FROM authenticated,anon + avaliacaoService allowlist drop; smoke proves candidato → permission denied |
| T-24-02-03 | Denial of Service | dropping the base row policy blanks the candidate prova | mitigate | client rewire to get_cognitivo_itens lands in the same plan (Pitfall 2); vitest asserts the rpc path |
| T-24-02-SC | Tampering | package installs | accept | zero new packages (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- Both migrations authored (no PROD apply here); no outer BEGIN/COMMIT; nothing writes candidaturas
- Client rewires landed in lockstep; vitest guards green
- SQL smoke file committed for 24-08 execution
</verification>

<success_criteria>
- SEC-01: gabarito_idx column-REVOKED + candidate reads via get_cognitivo_itens only
- SEC-07: rubric column-REVOKED + dropped from the candidate perguntas allowlist
- Mechanism is column-REVOKE (safe — no authenticated reader), explicitly distinct from SEC-02
</success_criteria>

<output>
Create `.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-02-SUMMARY.md` when done.
</output>
