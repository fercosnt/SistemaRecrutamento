---
phase: 24-blindagem-de-seguran-a-pii-lgpd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-LIVE-STATE.md
autonomous: false            # [BLOCKING] MCP PROD reads — must run before any migration is authored
requirements: [SEC-01, SEC-02, SEC-05, SEC-06, SEC-07, SEC-08, SEC-09, SEC-10, UX-08]
user_setup: []

must_haves:
  truths:
    - "The live PROD RLS/column-ACL state for every SEC-24 target table is captured before any migration is authored (RESEARCH Environment-Availability mandate)"
    - "SEC-09: the live auth_admin_le_usuarios_rh predicate is confirmed byte-for-behavior = FOR SELECT TO supabase_auth_admin USING (true) before the mirror migration is written (assumption A2)"
    - "SEC-10: backup_m2.candidaturas_pre_funil existence + PII column list is confirmed via to_regclass before the DROP is authored (assumption A1)"
    - "SEC-01/07: no authenticated client legitimately reads cognitivo_itens.gabarito_idx or perguntas.rubric — the column-REVOKE mechanism is validated as safe (assumption A3)"
    - "UX-08: bigfive_itens live active-row count (expected 120) is confirmed before the O6 deactivation is authored (assumption A5)"
  artifacts:
    - path: ".planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-LIVE-STATE.md"
      provides: "Live PROD policy/ACL/table-existence findings + per-assumption (A1-A7) go/no-go for each migration"
      contains: "auth_admin_le_usuarios_rh"
  key_links:
    - from: "24-LIVE-STATE.md"
      to: "24-02..24-08 migration authoring"
      via: "confirmed/flagged assumptions gate the authored SQL"
      pattern: "assumption A[0-9]"
---

<objective>
Run the mandatory live-state verification against PROD (via Supabase MCP) BEFORE any Phase 24 migration is authored. RESEARCH §Environment-Availability is explicit: live PROD RLS/policy/table state was INFERRED from migration files by the researcher — the executor must confirm it with the real database as the first task of the phase, because a divergence silently changes a migration (e.g., a different live auth_admin predicate would make SEC-09 a behavior change instead of a drift-fix; an already-dropped backup makes SEC-10 a no-op).

Purpose: De-risk every downstream migration by grounding it in real PROD state. The orchestrator already confirmed two key facts (backup table EXISTS; auth_admin policy = SELECT / supabase_auth_admin / USING true) — this plan records those plus the remaining reads and produces the go/no-go findings file the authoring waves depend on.
Output: `.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-LIVE-STATE.md` with per-table policy dumps, column ACLs, backup existence, and per-assumption (A1-A7) confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**[BLOCKING · non-autonomous]** Every task in this plan is a read-only Supabase MCP query against PROD. No files change except the findings file. Do NOT author or apply any migration in this plan.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-RESEARCH.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-PATTERNS.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture RLS + column-ACL state for candidate-secrecy + horizontal-scope tables (MCP)</name>
  <files>.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-LIVE-STATE.md</files>
  <read_first>
    - RESEARCH §Environment-Availability (lines 426-436) — the exact live-state queries mandated
    - RESEARCH §Runtime State Inventory + §Assumptions Log (A1, A3, A5, A7)
    - Migration anchors to diff against: `supabase/migrations/20260624000001_entrevista_cognitivo_tables.sql` (cognitivo_itens gabarito_idx L111, policy L162-165), `20260623100003_redacoes_candidato.sql` (verdict cols L64-83, candidate policy L107-115, RH policies L124-135), `20260611000002_perguntas_sjt.sql` (rubric L50, cand policy L69-72), `20260610000001_analise_tables.sql` (analise/comparativo role-only L78-89), `20260607000006_rls_policies_m2_backbone.sql` (candidaturas L44-58)
  </read_first>
  <action>
    Via Supabase MCP `execute_sql` (read-only), dump for each table: `SELECT policyname, cmd, roles, qual, with_check FROM pg_policies WHERE tablename IN ('cognitivo_itens','perguntas','redacoes_candidato','analise_candidato_vaga','comparativo_solicitado','candidaturas')`. Then capture column-level grants: `SELECT table_name, column_name, privilege_type, grantee FROM information_schema.column_privileges WHERE table_name IN ('cognitivo_itens','perguntas','redacoes_candidato') AND grantee IN ('authenticated','anon')` — specifically prove whether `cognitivo_itens.gabarito_idx`, `perguntas.rubric`, and the redacoes verdict columns (analise_ia, scores_dimensao, score_ponderado_0_100, classificacao_cor, red_flag_etico, flags, scores_humanos, notas_revisor, decisao_revisor) are currently SELECT-able by `authenticated`. Confirm assumption A3 (no authenticated client reads gabarito_idx/rubric — cross-check with a `grep -rn "gabarito_idx\|\.rubric" src/` showing only cognitivoService/avaliacaoService candidate paths) and A7 (candidate own-row redacao read is a base-table policy, so dropping it needs the client RPC rewire in the same wave). Record every result verbatim in 24-LIVE-STATE.md under a "Candidate-secrecy + horizontal-scope tables" section, with a per-requirement (SEC-01/02/05/07/08) go/no-go: does the researched mechanism still apply?
  </action>
  <acceptance_criteria>
    - 24-LIVE-STATE.md contains the pg_policies dump for all 6 tables (policyname/cmd/roles/qual present per row)
    - The column_privileges result explicitly states whether authenticated can SELECT gabarito_idx / rubric / the 9 redacoes verdict columns today
    - A3 is marked CONFIRMED (grep shows no authenticated RH reader of gabarito_idx/rubric) or FLAGGED with the offending reader
    - Each of SEC-01/02/05/07/08 has an explicit "mechanism holds" / "divergence: <detail>" line
  </acceptance_criteria>
  <verify>
    <automated>Supabase MCP execute_sql returns rows for the pg_policies + column_privileges queries above; results transcribed into 24-LIVE-STATE.md (grep -c 'SEC-0' 24-LIVE-STATE.md > 0). This is a PROD read — the behavioral SQL smokes run in 24-08.</automated>
  </verify>
  <done>Live policy + column-ACL state for the 6 candidate/horizontal tables recorded with per-requirement go/no-go; assumptions A3/A7 resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm SEC-09 auth_admin predicate, SEC-10 backup existence, UX-08 item count (MCP)</name>
  <files>.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-LIVE-STATE.md</files>
  <read_first>
    - RESEARCH §Assumptions Log (A1 backup exists, A2 auth_admin predicate), §Runtime State Inventory
    - `reference_auth_hook_rls_gap` memory (the live auth_admin chain is correct — "NÃO re-migrar")
    - Migration anchors: `20260420000002_unified_auth_role.sql:100-102` (GRANTs), `20260612000001_bigfive_itens.sql:72-192` (120-item seed)
  </read_first>
  <action>
    Via Supabase MCP `execute_sql`: (1) SEC-09 — `SELECT policyname, cmd, roles, qual FROM pg_policies WHERE tablename='usuarios_rh' AND policyname='auth_admin_le_usuarios_rh'` and confirm it equals `FOR SELECT TO supabase_auth_admin USING (true)` byte-for-behavior (assumption A2); also confirm `GRANT SELECT ON public.usuarios_rh TO supabase_auth_admin` is present via information_schema.role_table_grants. (2) SEC-10 — `SELECT to_regclass('backup_m2.candidaturas_pre_funil')` (assumption A1 — orchestrator says EXISTS) and, if non-null, `SELECT column_name FROM information_schema.columns WHERE table_schema='backup_m2' AND table_name='candidaturas_pre_funil'` to capture the PII column list for the LGPD erasure evidence record. (3) UX-08 — `SELECT count(*) FROM public.bigfive_itens` (expected 120) and `SELECT item_id, dimensao FROM public.bigfive_itens WHERE item_id IN (28,58,88,118)` confirming all four are dimensao='O' (assumption A5). (4) Note whether an `ativo` column already exists on bigfive_itens (idempotency for 24-07's ALTER). Record all under a "Declarations / erasure / item-bank" section with per-requirement (SEC-09/10, UX-08) go/no-go.
  </action>
  <acceptance_criteria>
    - 24-LIVE-STATE.md records the exact live auth_admin_le_usuarios_rh predicate; A2 marked CONFIRMED (matches USING true) or FLAGGED with the divergent predicate (which would make SEC-09 a behavior change)
    - to_regclass('backup_m2.candidaturas_pre_funil') result recorded; if non-null, the PII column list is captured (LGPD evidence); A1 resolved
    - bigfive_itens count recorded (=120 expected); {28,58,88,118} all confirmed dimensao='O'; A5 resolved; presence/absence of `ativo` column noted
  </acceptance_criteria>
  <verify>
    <automated>Supabase MCP execute_sql returns the auth_admin predicate, to_regclass value, and bigfive count; transcribed into 24-LIVE-STATE.md (grep -c "auth_admin_le_usuarios_rh\|to_regclass\|bigfive_itens" 24-LIVE-STATE.md >= 3). PROD read; behavioral proofs run in 24-08.</automated>
  </verify>
  <done>SEC-09 predicate, SEC-10 backup existence + column list, and UX-08 item-bank state confirmed; assumptions A1/A2/A5 resolved; findings file complete and gates the authoring waves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner-inference → live PROD | Migrations authored from migration-file inference may diverge from real PROD policy/ACL/table state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Tampering | SEC-09 mirror migration authored against a stale predicate | mitigate | Task 2 diffs the live auth_admin_le_usuarios_rh predicate before the mirror is written; FLAG halts authoring if it differs from USING true |
| T-24-01-02 | Information Disclosure | SEC-10 backup dropped without recording its PII contents (LGPD evidence) | mitigate | Task 2 captures the column list before the DROP is authored in 24-06 |
| T-24-01-03 | Denial of Service | SEC-01/07 column REVOKE breaks a hidden RH reader | mitigate | Task 1 confirms A3 (grep + column_privileges) — no authenticated client reads gabarito_idx/rubric |
| T-24-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 24 installs zero new packages (RESEARCH §Package Legitimacy Audit — vacuous); no supply-chain surface in this plan |
</threat_model>

<verification>
- 24-LIVE-STATE.md exists and covers all target tables + SEC-09 predicate + SEC-10 backup + UX-08 item count
- Every RESEARCH assumption A1-A7 is marked CONFIRMED or FLAGGED
- No migration authored or applied in this plan (read-only)
</verification>

<success_criteria>
- All live-state reads executed via Supabase MCP against PROD
- Findings recorded with per-requirement go/no-go
- Any divergence from research assumptions is flagged so the authoring waves adapt (not silently ship a wrong migration)
</success_criteria>

<output>
Create `.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-01-SUMMARY.md` when done, and the findings file `24-LIVE-STATE.md`.
</output>
