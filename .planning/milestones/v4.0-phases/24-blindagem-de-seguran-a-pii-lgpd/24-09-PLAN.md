---
phase: 24-blindagem-de-seguran-a-pii-lgpd
plan: 09
type: execute
wave: 4
depends_on: [24-08]
files_modified:
  - .planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-PROD-LANDING.md
autonomous: false            # [BLOCKING] redeploys Edge Functions to PROD + live smokes
requirements: [SEC-04, UX-08]

must_haves:
  truths:
    - "SEC-04: gerar-devolutiva-bigfive is redeployed (supabase functions deploy) so the Bearer self-auth guard is LIVE; a live no-Bearer/wrong-Bearer POST → 401 and the correct service Bearer → 200 (bundle-freeze: the EF only enforces the guard once redeployed)"
    - "UX-08: submit-bigfive-final is redeployed so it bundles the updated _shared/bigfive-scoring.ts (116 items + O prorate) and the active-id-set validateBody; a live 116-item submit succeeds and a 120-item submit is rejected — proving the OLD bundle is gone (reference_ef_shared_bundle_freeze)"
    - "verify_jwt is NOT changed for gerar-devolutiva-bigfive (the Bearer check is the real control); do not touch other EFs' verify_jwt (Phase 27/CI-13 owns config.toml)"
    - "The live submit smoke runs AFTER 24-08 applied the ativo migration (get_bigfive_itens=116) — the redeploy depends on the DB state, so this is Wave 4 after Wave 3"
    - "No redeploy path writes candidaturas or auto-rejects (RNF-07a)"
  artifacts:
    - path: ".planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-PROD-LANDING.md"
      provides: "EF redeploy log + live 401/200 (SEC-04) and 116-item submit (UX-08) smoke results"
      contains: "functions deploy"
  key_links:
    - from: "gerar-devolutiva-bigfive redeploy"
      to: "live Bearer enforcement"
      via: "no-Bearer POST → 401"
      pattern: "401"
    - from: "submit-bigfive-final redeploy"
      to: "live 116-item scorer bundle"
      via: "116-item submit → 200, 120-item → rejected"
      pattern: "116"
---

<objective>
Redeploy the two Edge Functions whose PROD behavior changed, so their new code is actually enforced. Edge Functions are bundle-frozen — editing `gerar-devolutiva-bigfive/index.ts` (SEC-04 Bearer guard) and `_shared/bigfive-scoring.ts` (UX-08 116-item scorer, bundled by `submit-bigfive-final`) has ZERO PROD effect until each function is redeployed. Then prove enforcement with live smokes.

Purpose: The devolutiva IDOR is closed live (401 without the internal Bearer); the Big Five submit accepts the 116-item set and computes the prorated O — the OLD 120-item bundle is gone.
Output: EF redeploy log + live SEC-04 (401/200) and UX-08 (116-item submit) smoke results appended to 24-PROD-LANDING.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**[BLOCKING · non-autonomous]** Redeploys to PROD. Runs AFTER 24-08 (the DB must already return 116 active items for the submit smoke). Redeploy with `supabase functions deploy <name>` (auto-bundles `_shared`). Do NOT change `verify_jwt` for gerar-devolutiva-bigfive (the Bearer check is the control; verify_jwt config is Phase-27/CI-13). If SEC-04 uses a `DEVOLUTIVA_INVOKE_SECRET`, ensure that EF env var is set in PROD before the live smoke (else it falls back to SERVICE_KEY).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-RESEARCH.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-PROD-LANDING.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Redeployed gerar-devolutiva-bigfive with the Bearer self-auth guard (SEC-04) and proved the IDOR is closed live.</what-built>
  <how-to-verify>
    Run `supabase functions deploy gerar-devolutiva-bigfive`. Confirm the deploy succeeded (new version). If a `DEVOLUTIVA_INVOKE_SECRET` is used, confirm it is set in the EF env (else the guard compares to SERVICE_KEY). Live smokes appended to 24-PROD-LANDING.md: (a) POST /functions/v1/gerar-devolutiva-bigfive with NO Authorization header → 401; (b) with a wrong Bearer → 401; (c) with the correct service Bearer (the value submit-bigfive-final uses) → 200 / handler proceeds. Use `get_edge_function` to confirm the deployed bundle contains the Bearer block (deployed-vs-local, per reference_ef_shared_bundle_freeze).
    Expected: unauthenticated/wrong callers get 401; only the internal service invoker succeeds.
  </how-to-verify>
  <resume-signal>Type "approved" once the SEC-04 live 401/401/200 smokes PASS, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Redeployed submit-bigfive-final (bundles the 116-item scorer) and proved the Big Five instrument works on 116 items with O prorated (UX-08).</what-built>
  <how-to-verify>
    Run `supabase functions deploy submit-bigfive-final`. Confirm the deploy succeeded. Live smokes appended to 24-PROD-LANDING.md: (a) submit a valid 116-item response (the active id set from get_bigfive_itens, non-contiguous — gaps at 28/58/88/118) → 200 and a scores row is written WITHOUT auto-rejecting the candidatura (RNF-07a — candidaturas.status unchanged); (b) submit a legacy 120-item body → rejected (400) proving the OLD bundle is gone; (c) confirm the resulting O domain reflects the ×6/5 prorate (compare against the golden-test expectation). Use `get_edge_function` to confirm the deployed bundle has the 116/active-set validateBody.
    Expected: 116-item submit scores successfully with prorated O; 120-item submit rejected; no auto-reject.
  </how-to-verify>
  <resume-signal>Type "approved" once the UX-08 live submit smokes PASS, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edited EF source → deployed EF bundle | bundle-freeze: the guard/scorer changes are inert until redeploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09-01 | Elevation / Information Disclosure | devolutiva IDOR still open because the EF was edited but not redeployed | mitigate | [BLOCKING] redeploy gerar-devolutiva-bigfive + live 401/200 smoke + deployed-vs-local diff |
| T-24-09-02 | Denial of Service / Tampering | submit runs the OLD 120-item bundle → every 116-item submit 400s | mitigate | redeploy submit-bigfive-final (bundles the scorer) + live 116-item submit smoke + 120-item rejection proof |
| T-24-09-03 | (RNF-07a) | submit auto-rejects on the prorated score | mitigate | live smoke asserts candidaturas.status unchanged after a successful submit |
| T-24-09-SC | Tampering | package installs | accept | zero new packages (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- gerar-devolutiva-bigfive redeployed; live 401/401/200 PASS; deployed bundle carries the Bearer block
- submit-bigfive-final redeployed; live 116-item submit → 200 (prorated O, no auto-reject); 120-item → 400
- verify_jwt untouched
</verification>

<success_criteria>
- SEC-04 IDOR closed live (Bearer-gated)
- UX-08 instrument live on 116 items with O prorated; old bundle gone
- RNF-07a preserved live
</success_criteria>

<output>
Create `.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-09-SUMMARY.md` when done; append EF redeploy + smoke results to `24-PROD-LANDING.md`.
</output>
