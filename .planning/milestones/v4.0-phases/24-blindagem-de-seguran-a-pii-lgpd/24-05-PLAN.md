---
phase: 24-blindagem-de-seguran-a-pii-lgpd
plan: 05
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - supabase/functions/gerar-devolutiva-bigfive/index.ts
  - supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts
  - supabase/migrations/20260706110005_sec03_n8n_serverside.sql
  - src/features/vagas/services/candidaturasService.ts
  - src/features/explicacao/services/explicacaoService.ts
  - src/__tests__/guards/n8n-bundle.grep.test.ts
autonomous: true             # code-local authoring only; EF redeploy is 24-09, migration+Vault land in 24-08
requirements: [SEC-03, SEC-04]

must_haves:
  truths:
    - "SEC-04: gerar-devolutiva-bigfive (currently ZERO auth) rejects any caller without the internal Bearer secret (401) and accepts only the service invoker — Bearer self-auth mirroring cost-alerter:90-113, per CONTEXT SEC-04 decision; role is NOT read from getUser().app_metadata (always undefined — RESEARCH Pitfall 4)"
    - "SEC-04 is server-to-server only (no src/ caller — assumption A4); Bearer self-auth is the sufficient IDOR closure — role+posse has no end-user caller to apply to"
    - "SEC-03: no n8n webhook URL (n8n.cloud / fernandocosta) ships in the public bundle — all three client dispatch sites (candidaturasService VITE_N8N_NOVA_CANDIDATURA + VITE_N8N_STATUS_UPDATE, explicacaoService VITE_N8N_REVISAO_DECISAO) delete their URL constant + VITE_ read + fetch(); dispatch moves server-side (pg_net + Vault) per CONTEXT SEC-03 decision"
    - "VITE_-prefixed vars are inlined into the public bundle at build — configurable ≠ private (RESEARCH Pitfall 5); the grep guard asserts n8n.cloud is absent from build/"
    - "No path writes candidaturas or auto-rejects (RNF-07a); the EF Bearer compare is exact-match on the shared secret"
  artifacts:
    - path: "supabase/functions/gerar-devolutiva-bigfive/index.ts"
      provides: "Bearer self-auth guard at the top of Deno.serve, before score_id parse"
      contains: "DEVOLUTIVA_INVOKE_SECRET"
    - path: "supabase/migrations/20260706110005_sec03_n8n_serverside.sql"
      provides: "server-side pg_net dispatch reading the n8n URL Vault secret"
      contains: "net.http_post"
    - path: "src/__tests__/guards/n8n-bundle.grep.test.ts"
      provides: "build-artifact grep guard for n8n URL"
      contains: "n8n"
  key_links:
    - from: "gerar-devolutiva-bigfive Deno.serve"
      to: "Bearer secret compare"
      via: "reject if bearer !== DEVOLUTIVA_INVOKE_SECRET ?? SERVICE_KEY"
      pattern: "Bearer"
    - from: "build/ bundle"
      to: "no n8n URL"
      via: "grep guard n8n.cloud|fernandocosta ⇒ 0"
      pattern: "n8n"
---

<objective>
Two infrastructure leaks: (1) SEC-04 — the `gerar-devolutiva-bigfive` Edge Function reads a candidate's devolutiva via service_role with ZERO caller authorization, an open IDOR; close it with Bearer self-auth (the EF is server-to-server only). (2) SEC-03 — three n8n webhook URLs are hardcoded in the client bundle (even the `VITE_N8N_*` "config" ships publicly); remove all client dispatch and move the trigger server-side via pg_net + a Vault-held URL.

Purpose: A candidate JWT (or any unauth caller) POSTing to the devolutiva EF gets 401; the n8n URL is no longer greppable in `build/`.
Output: EF guard + test, 1 pg_net dispatch migration, 2 client removals, 1 build-artifact grep guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

The EF code + client removals + migration FILE are authored here. The EF REDEPLOY is 24-09; the migration apply + the n8n URL Vault secret creation are 24-08. NO outer BEGIN/COMMIT in the migration. Do NOT read role from getUser() in the EF (Pitfall 4).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-RESEARCH.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-PATTERNS.md

<interfaces>
<!-- SEC-04 -->
Bearer self-auth analog:    supabase/functions/cost-alerter/index.ts:90-113 (Bearer compare vs SERVICE_KEY / override secret)
SEC-04 target handler:      gerar-devolutiva-bigfive/index.ts — Deno.serve L571, OPTIONS/method guard L572-578, env read (SERVICE_KEY) L580-589, score_id parse L699-708 → INSERT the Bearer block after L589, before L699
Only caller (service_role): submit-bigfive-final/index.ts:232-239 (no src/ browser caller — grep empty, A4)
EF test harness analog:     supabase/functions/avaliar-redacao/__tests__/index.test.ts (deps-injection)
<!-- SEC-03 -->
Server-side dispatch analog: supabase/migrations/20260610000003_reprocessar_rpc.sql:63-82 (pg_net + vault.decrypted_secrets, graceful skip if NULL)
Client leak sites:          candidaturasService.ts:70-71 (VITE_N8N_NOVA_CANDIDATURA + fallback), :82-83 (VITE_N8N_STATUS_UPDATE + fallback) · explicacaoService.ts:129-130 (VITE_N8N_REVISAO_DECISAO + fallback)
Build guard analog:         src/__tests__/guards/rh-console.grep.test.ts / forbidden-strings.grep.test.ts (node:fs recursive grep)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEC-04 Bearer self-auth in gerar-devolutiva-bigfive + authz test</name>
  <files>supabase/functions/gerar-devolutiva-bigfive/index.ts, supabase/functions/gerar-devolutiva-bigfive/__tests__/index.test.ts</files>
  <read_first>
    - `supabase/functions/cost-alerter/index.ts:90-113` (the Bearer self-auth pattern to copy)
    - `supabase/functions/gerar-devolutiva-bigfive/index.ts:571-589,699-708` (Deno.serve, env read, score_id parse — the insertion point)
    - `supabase/functions/avaliar-redacao/__tests__/index.test.ts` (deps-injection harness to clone for the authz cases)
    - RESEARCH §4 + Pitfall 4; PATTERNS §Wave C (lines 188-214)
  </read_first>
  <action>
    In `gerar-devolutiva-bigfive/index.ts`, insert a Bearer self-auth block at the top of the Deno.serve handler immediately AFTER the env read (~L589) and BEFORE the score_id parse (~L699): read the Authorization header, extract the Bearer token, compare (exact match) to `Deno.env.get('DEVOLUTIVA_INVOKE_SECRET') ?? SUPABASE_SERVICE_ROLE_KEY`; on missing/mismatched token return 401 with the CORS headers (mirror cost-alerter's shape). Do NOT call getUser() for role — this EF is server-to-server only; role reads from getUser().app_metadata are always undefined (Pitfall 4). Then extend `__tests__/index.test.ts` (clone the avaliar-redacao deps-injection style) with three cases: no-Bearer → 401, wrong-Bearer → 401, correct service Bearer → 200 (handler proceeds). The redeploy happens in 24-09.
  </action>
  <acceptance_criteria>
    - The Bearer block sits before the score_id parse and returns 401 on missing/wrong token; uses exact-match compare to DEVOLUTIVA_INVOKE_SECRET ?? SERVICE_KEY
    - No getUser()/app_metadata role read is added (Pitfall 4)
    - index.test.ts has the three authz cases (no-Bearer 401, wrong-Bearer 401, service-Bearer 200) and they pass under deno test
    - The handler writes nothing to candidaturas (RNF-07a — unchanged)
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/gerar-devolutiva-bigfive ⇒ green (incl. the 3 authz cases); grep -c "getUser().app_metadata\|app_metadata?.role" supabase/functions/gerar-devolutiva-bigfive/index.ts ⇒ 0. Live 401/200 smoke runs in 24-09 after redeploy.</automated>
  </verify>
  <done>Devolutiva EF gated by Bearer self-auth; IDOR closed at the handler; authz tests green.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-03 server-side pg_net dispatch migration + delete client n8n dispatch</name>
  <files>supabase/migrations/20260706110005_sec03_n8n_serverside.sql, src/features/vagas/services/candidaturasService.ts, src/features/explicacao/services/explicacaoService.ts</files>
  <read_first>
    - `supabase/migrations/20260610000003_reprocessar_rpc.sql:63-82` (pg_net + vault.decrypted_secrets dispatch, graceful skip if NULL)
    - `src/features/vagas/services/candidaturasService.ts:60-83` (the 2 VITE_N8N reads + fallbacks + fetch dispatch) + `src/features/explicacao/services/explicacaoService.ts:121-130` (VITE_N8N_REVISAO_DECISAO)
    - RESEARCH §Architecture SEC-03 + Pitfall 5; PATTERNS §Wave C (lines 216-236); RESEARCH Open Q3 (minimal M4 depth — full notificar-candidato EF is M5)
  </read_first>
  <action>
    Author `20260706110005_sec03_n8n_serverside.sql`: mirror `reprocessar_analise:63-82` — a trigger (or triggers) on the relevant candidaturas events that PERFORM net.http_post to the n8n URL read from `vault.decrypted_secrets` (secret name e.g. `n8n_webhook_base` or per-event names), with a graceful skip (RETURN) if the secret is NULL so a missing secret never raises. Fire the three events (nova-candidatura, status-candidatura, revisao-decisao) server-side. The Vault secret VALUE is created in 24-08 (not here). No outer BEGIN/COMMIT. Then DELETE from the client, in both services, the URL constants, the `import.meta.env.VITE_N8N_*` reads, the hardcoded `https://fernandocosta.app.n8n.cloud/...` fallbacks, AND the `fetch()` dispatch calls — plus the stale comments claiming VITE_ is the safe pattern. If removing the fetch leaves an unused function/param, remove it cleanly (avoid a fresh TS6133 that would inflate the 133 tsc baseline — the 22-03 lesson).
  </action>
  <acceptance_criteria>
    - Migration contains `net.http_post` reading a Vault secret with a graceful NULL skip (mirrors reprocessar_analise)
    - candidaturasService.ts and explicacaoService.ts contain NO `VITE_N8N`, NO `n8n.cloud`, NO `fernandocosta`, and NO fetch() to an n8n URL
    - tsc baseline not inflated (npm run lint ≤ 133); no new TS6133
    - Migration writes nothing that auto-rejects a candidate (RNF-07a)
  </acceptance_criteria>
  <verify>
    <automated>grep -rc "VITE_N8N\|n8n.cloud\|fernandocosta" src/features/vagas/services/candidaturasService.ts src/features/explicacao/services/explicacaoService.ts ⇒ 0; grep -c "net.http_post" supabase/migrations/20260706110005_sec03_n8n_serverside.sql ⇒ ≥1; npm run lint ⇒ ≤133. Vault secret + migration apply happen in 24-08.</automated>
  </verify>
  <done>n8n dispatch moved server-side; client carries no webhook URL; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: SEC-03 build-artifact grep guard</name>
  <files>src/__tests__/guards/n8n-bundle.grep.test.ts</files>
  <read_first>
    - `src/__tests__/guards/rh-console.grep.test.ts` (node:fs recursive grep pattern + comment-awareness) / `forbidden-strings.grep.test.ts`
    - RESEARCH §Validation SEC-03 row (line 455) + Pitfall 5
  </read_first>
  <action>
    Create `src/__tests__/guards/n8n-bundle.grep.test.ts` (node:fs, mirror the rh-console guard): after `npm run build`, recursively grep `build/` (assets/*.js) for `n8n.cloud` and `fernandocosta` and assert 0 matches. Also add a src-level assertion that no `VITE_N8N` token remains in `src/` (excluding comment-only lines via the existing comment-aware filter). Provide an allowed-literal sub-test proving the guard does not false-positive on unrelated strings. The guard's `build/`-grep leg is skipped if `build/` is absent (so unit runs don't require a build) but MUST run in CI where build precedes test.
  </action>
  <acceptance_criteria>
    - n8n-bundle.grep.test.ts asserts 0 matches of n8n.cloud|fernandocosta in build/ (when present) and 0 VITE_N8N in src/ (non-comment)
    - Has a no-false-positive sub-test
    - Passes under npm run test:run (with build/ present after npm run build)
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run test:run -- n8n-bundle.grep ⇒ green (0 n8n URL in build/, 0 VITE_N8N in src/).</automated>
  </verify>
  <done>Bundle grep guard locks the n8n URL out of the public build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any JWT → POST /functions/v1/gerar-devolutiva-bigfive | service_role read of another candidate's devolutiva (IDOR) |
| public browser → bundle.js | n8n webhook URL (incl. VITE_ vars) inlined into the shipped bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-01 | Elevation / Information Disclosure | gerar-devolutiva-bigfive has zero caller authz (IDOR) | mitigate | Bearer self-auth (exact-match) at top of Deno.serve; deno tests prove no-Bearer/wrong-Bearer → 401; live 401/200 in 24-09 |
| T-24-05-02 | Information Disclosure / DoS (forged events) | n8n URL in the public bundle (incl VITE_ inlined) | mitigate | server-side pg_net + Vault dispatch; delete all client URL/VITE_/fetch; build grep guard ⇒ 0 |
| T-24-05-03 | Spoofing | EF role read from getUser() (Pitfall 4) false-pass/fail | mitigate | server-to-server Bearer self-auth only; no role read added |
| T-24-05-SC | Tampering | package installs | accept | zero new packages (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- deno authz tests green (401/401/200); no getUser role read
- client carries no n8n URL/VITE_/fetch; migration authored; build grep guard green
- tsc ≤ 133; nothing writes candidaturas
</verification>

<success_criteria>
- SEC-04: devolutiva EF rejects unauthenticated/wrong-Bearer callers (IDOR closed)
- SEC-03: n8n URL absent from the public bundle; dispatch server-side
</success_criteria>

<output>
Create `.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-05-SUMMARY.md` when done.
</output>
