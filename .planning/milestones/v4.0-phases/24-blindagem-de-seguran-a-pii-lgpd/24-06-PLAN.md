---
phase: 24-blindagem-de-seguran-a-pii-lgpd
plan: 06
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - supabase/migrations/20260706110006_sec09_auth_admin_policy.sql
  - supabase/migrations/20260706110007_sec10_drop_backup.sql
  - src/components/pages/ConfiguracoesPage.tsx
  - src/components/pages/MeuPerfilPage.tsx
  - src/__tests__/guards/rh-console.grep.test.ts
autonomous: true             # code-local authoring only; migrations apply in 24-08
requirements: [SEC-09, SEC-10, SEC-11]

must_haves:
  truths:
    - "SEC-09: the live execute_sql-only policy auth_admin_le_usuarios_rh (FOR SELECT TO supabase_auth_admin USING (true), the custom_access_token_hook dependency) is declared in a migration file — mirror of the live policy, ZERO behavior change; NOT a re-migration (per CONTEXT SEC-09 decision + reference_auth_hook_rls_gap 'NÃO re-migrar')"
    - "SEC-10: backup_m2.candidaturas_pre_funil (PII snapshot from the 2026-06-07 cutover, outside LGPD erasure coverage) is DROPped (DROP TABLE IF EXISTS + DROP SCHEMA IF EXISTS backup_m2 CASCADE) per CONTEXT SEC-10 decision; existence + column list captured in 24-01 first"
    - "SEC-11: operational console.log removed from RH pages (esp. ConfiguracoesPage:491 candidate email) — FX-14 pattern; the rh-console grep guard is extended to cover ConfiguracoesPage + MeuPerfilPage; console.error is intentionally allowed (FX-14)"
    - "SEC-09 migration is idempotent (DROP POLICY IF EXISTS + CREATE) and matches the 24-01 live predicate byte-for-behavior before it is written; version-row reconcile is Phase 27, not here"
    - "No migration writes candidaturas or auto-rejects (RNF-07a) — declaration + erasure + log hygiene only"
  artifacts:
    - path: "supabase/migrations/20260706110006_sec09_auth_admin_policy.sql"
      provides: "declared mirror of auth_admin_le_usuarios_rh (drift-fix)"
      contains: "auth_admin_le_usuarios_rh"
    - path: "supabase/migrations/20260706110007_sec10_drop_backup.sql"
      provides: "LGPD erasure of the backup_m2 PII snapshot"
      contains: "DROP"
    - path: "src/__tests__/guards/rh-console.grep.test.ts"
      provides: "extended RH_PATH_FILES covering the two named pages"
      contains: "ConfiguracoesPage"
  key_links:
    - from: "auth_admin_le_usuarios_rh migration"
      to: "24-01 live predicate"
      via: "byte-for-behavior mirror confirmed"
      pattern: "supabase_auth_admin"
    - from: "rh-console guard"
      to: "ConfiguracoesPage/MeuPerfilPage"
      via: "RH_PATH_FILES includes both"
      pattern: "ConfiguracoesPage"
---

<objective>
Three low-severity but real hardening items: (SEC-09) end the execute_sql-only drift by declaring the `auth_admin_le_usuarios_rh` policy — the `custom_access_token_hook` dependency that, if lost in a DB rebuild, silently demotes every RH login to 'candidato'; (SEC-10) complete LGPD erasure by dropping the `backup_m2.candidaturas_pre_funil` PII snapshot that lives outside the erasure flow; (SEC-11) strip operational `console.log` from RH pages (including a candidate email) and extend the grep guard.

Purpose: A future `supabase db reset` keeps RH login working (policy in a file); the PII backup is gone; RH DevTools no longer prints candidate movement/emails.
Output: 2 migration files, 2 RH-page edits, 1 grep-guard extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Migration FILES only — PROD apply is 24-08. NO outer BEGIN/COMMIT. SEC-09 MUST match the 24-01-captured live predicate byte-for-behavior; if 24-01 FLAGGED a divergence, surface it before authoring (a differing predicate would make this a behavior change, which is out of scope). SEC-11 is client-only (ships with the frontend build; no PROD landing step).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-RESEARCH.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-PATTERNS.md
@.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-LIVE-STATE.md

<interfaces>
<!-- SEC-09 -->
GRANTs already shipped:   supabase/migrations/20260420000002_unified_auth_role.sql:100-102
Exact mirror to declare: RESEARCH §Code Examples lines 325-333 (idempotent GRANT + DROP POLICY IF EXISTS + CREATE POLICY auth_admin_le_usuarios_rh AS PERMISSIVE FOR SELECT TO supabase_auth_admin USING (true))
Live predicate source:   24-LIVE-STATE.md (24-01 Task 2) + reference_auth_hook_rls_gap
<!-- SEC-10 -->
Drop target:             backup_m2.candidaturas_pre_funil (existence + columns captured in 24-01)
<!-- SEC-11 -->
console.log inventory:   ConfiguracoesPage.tsx L418,437,474,491(candidate email),1255 · MeuPerfilPage.tsx L39,47,52 (console.log). VagasRHPage.tsx L90,136,158 + CriarEditarVagaPage.tsx L142 are console.error (FX-14 ALLOWS console.error — keep or DEV-gate)
Guard to extend:         src/__tests__/guards/rh-console.grep.test.ts:46-52 (RH_PATH_FILES); FORBIDDEN_CONSOLE = /console\.(log|debug|info|warn)\s*\(/ (does NOT forbid console.error)
FX-14 precedent:         PerfilCandidatoRHPage.tsx / SuporteRHPage.tsx cleanup
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEC-09 auth_admin policy mirror migration + SEC-10 backup drop migration</name>
  <files>supabase/migrations/20260706110006_sec09_auth_admin_policy.sql, supabase/migrations/20260706110007_sec10_drop_backup.sql</files>
  <read_first>
    - RESEARCH §Code Examples "SEC-09 — declare the auth-admin policy" (lines 325-333) + §6
    - `supabase/migrations/20260420000002_unified_auth_role.sql:100-102` (the GRANTs)
    - PATTERNS §Wave D (lines 240-263)
    - 24-LIVE-STATE.md (24-01 Task 2 — confirm the live auth_admin predicate = USING true and the backup existence + column list)
    - `reference_auth_hook_rls_gap` memory ("NÃO re-migrar")
  </read_first>
  <action>
    Author `20260706110006_sec09_auth_admin_policy.sql` as the idempotent mirror: `GRANT SELECT ON public.usuarios_rh TO supabase_auth_admin` (harmless repeat), `DROP POLICY IF EXISTS auth_admin_le_usuarios_rh ON public.usuarios_rh`, `CREATE POLICY auth_admin_le_usuarios_rh ON public.usuarios_rh AS PERMISSIVE FOR SELECT TO supabase_auth_admin USING (true)`. This must equal the 24-01-captured live predicate byte-for-behavior — zero behavior change (drift-fix only). Add an inline comment noting the version-row reconcile is Phase 27/DBMIG-01. Author `20260706110007_sec10_drop_backup.sql`: `DROP TABLE IF EXISTS backup_m2.candidaturas_pre_funil; DROP SCHEMA IF EXISTS backup_m2 CASCADE;` with an inline comment referencing the 24-01 column-list capture as the LGPD erasure evidence. Both files: no outer BEGIN/COMMIT.
  </action>
  <acceptance_criteria>
    - sec09 migration contains DROP POLICY IF EXISTS + CREATE POLICY auth_admin_le_usuarios_rh ... TO supabase_auth_admin USING (true) and matches the 24-01 live predicate
    - sec09 has a comment deferring version-row reconcile to Phase 27
    - sec10 migration contains DROP TABLE IF EXISTS backup_m2.candidaturas_pre_funil and DROP SCHEMA IF EXISTS backup_m2 CASCADE
    - Neither migration alters custom_access_token_hook behavior or writes candidaturas (RNF-07a)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "auth_admin_le_usuarios_rh" supabase/migrations/20260706110006_sec09_auth_admin_policy.sql ⇒ ≥2; grep -c "DROP TABLE IF EXISTS backup_m2\|DROP SCHEMA IF EXISTS backup_m2" supabase/migrations/20260706110007_sec10_drop_backup.sql ⇒ ≥2. Apply + to_regclass NULL smoke run in 24-08.</automated>
  </verify>
  <done>auth_admin policy declared (drift ended); backup PII drop authored — both idempotent, zero behavior change beyond the erasure.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-11 strip RH console.log + extend the grep guard</name>
  <files>src/components/pages/ConfiguracoesPage.tsx, src/components/pages/MeuPerfilPage.tsx, src/__tests__/guards/rh-console.grep.test.ts</files>
  <read_first>
    - RESEARCH §7 console.log inventory (lines 359-367) + PATTERNS §Wave D console table (lines 265-279)
    - `src/__tests__/guards/rh-console.grep.test.ts:46-59` (RH_PATH_FILES + FORBIDDEN_CONSOLE regex — allows console.error)
    - The FX-14 precedent (PerfilCandidatoRHPage/SuporteRHPage cleanup)
  </read_first>
  <action>
    Remove the operational `console.log` calls from `ConfiguracoesPage.tsx` (L418, 437, 474, 491 — the candidate-email reset log is the worst, 1255) and `MeuPerfilPage.tsx` (L39, 47, 52). Where a log sat inside a stub handler (MeuPerfilPage save stubs are M5), delete just the log, keep the stub. Do NOT touch `console.error` sites in VagasRHPage/CriarEditarVagaPage (FX-14 allows console.error) — leave them or optionally DEV-gate; state which in the SUMMARY. Then extend `RH_PATH_FILES` in `rh-console.grep.test.ts:46-52` to include `src/components/pages/ConfiguracoesPage.tsx` and `src/components/pages/MeuPerfilPage.tsx` so the guard fails if a forbidden console call returns. Keep the FORBIDDEN_CONSOLE regex + comment-awareness + the console.error-allowed sub-test as-is.
  </action>
  <acceptance_criteria>
    - ConfiguracoesPage.tsx and MeuPerfilPage.tsx contain zero `console.log|debug|info|warn` (non-comment)
    - RH_PATH_FILES includes both pages; the guard passes with them covered
    - console.error sites unchanged (or DEV-gated, noted in SUMMARY); no new TS6133 (tsc ≤ 133)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- rh-console.grep ⇒ green (covering both pages); grep -nE 'console\.(log|debug|info|warn)\s*\(' src/components/pages/ConfiguracoesPage.tsx src/components/pages/MeuPerfilPage.tsx | grep -v '^\s*//' ⇒ 0; npm run lint ⇒ ≤133.</automated>
  </verify>
  <done>RH pages print no operational candidate data; grep guard extended to lock it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB rebuild → GoTrue auth hook | lost auth_admin policy demotes every RH login to 'candidato' (self-lockout) |
| LGPD erasure flow → backup_m2 schema | candidate PII snapshot survives erasure outside the covered flow |
| RH browser DevTools | operational console.log prints candidate movement + email |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06-01 | Denial of Service | auth_admin_le_usuarios_rh execute_sql-only drift (rebuild demotes RH) | mitigate | declare the exact live policy in a migration file (idempotent mirror); grep-presence proof + Phase 27 rebuild seal |
| T-24-06-02 | Information Disclosure / Compliance | backup_m2.candidaturas_pre_funil PII outside erasure | mitigate | DROP TABLE + DROP SCHEMA CASCADE; 24-01 column-list captured as LGPD evidence; to_regclass NULL smoke in 24-08 |
| T-24-06-03 | Information Disclosure | RH console.log of candidate email/movement | mitigate | strip console.log (esp. ConfiguracoesPage:491); extend rh-console grep guard to both pages |
| T-24-06-SC | Tampering | package installs | accept | zero new packages (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- SEC-09 migration mirrors the 24-01 live predicate (byte-for-behavior); SEC-10 drop authored idempotent
- RH pages free of operational console.log; guard extended + green; tsc ≤ 133
- No migration writes candidaturas
</verification>

<success_criteria>
- SEC-09: policy declared in a file (drift ended)
- SEC-10: backup PII drop authored (erasure completeness)
- SEC-11: RH console.log stripped + guard extended
</success_criteria>

<output>
Create `.planning/phases/24-blindagem-de-seguran-a-pii-lgpd/24-06-SUMMARY.md` when done.
</output>
