---
phase: 23-ressurrei-o-da-stack-de-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/ai-client.ts
  - supabase/functions/_shared/circuit-breaker.ts
  - supabase/functions/_shared/__tests__/ai-client.test.ts
  - supabase/functions/_shared/__tests__/circuit-breaker.test.ts
autonomous: true
requirements: [AI-02, AI-03, AI-04, AI-05, AI-07]
must_haves:
  truths:
    - "Um timeout do SDK ('Request timed out.' / name APIConnectionTimeoutError) é retriável — o loop tenta de novo antes de cair no fallback OpenAI (AI-03)"
    - "O circuit breaker é uma instância COMPARTILHADA (sharedBreaker) que ABRE após THRESHOLD (≤ MAX_ATTEMPTS) falhas entre chamadas do mesmo isolate (AI-02)"
    - "Um env malformado (MAX_ATTEMPTS='abc') cai no default via parseIntEnv e a stack continua chamando a API (AI-07)"
    - "Uma chamada com timeout de 60s NÃO faz 3 tentativas — o cap de retry-budget mantém o total abaixo do teto ~150s do EF (AI-04)"
    - "O replay de idempotência só devolve linhas de SUCESSO; uma falha cacheada cai para uma chamada nova (AI-05)"
    - "AI-05 traceability: replay success-only destrava o reprocessamento do RH após falha cacheada"
  artifacts:
    - path: "supabase/functions/_shared/circuit-breaker.ts"
      provides: "sharedBreaker singleton + THRESHOLD env-config com invariante THRESHOLD ≤ MAX_ATTEMPTS"
      contains: "export const sharedBreaker"
    - path: "supabase/functions/_shared/ai-client.ts"
      provides: "parseIntEnv, isRetryable (timeout match), retry-budget cap, replay success-only, breaker default sharedBreaker"
      contains: "parseIntEnv"
    - path: "supabase/functions/_shared/__tests__/circuit-breaker.test.ts"
      provides: "THRESHOLD 5→3 + teste da invariante"
    - path: "supabase/functions/_shared/__tests__/ai-client.test.ts"
      provides: "timeout real shape, cap, replay-fail-fallthrough, parseIntEnv"
  key_links:
    - from: "ai-client.ts callAi"
      to: "sharedBreaker"
      via: "deps.breaker ?? sharedBreaker"
      pattern: "\\?\\? sharedBreaker"
    - from: "ai-client.ts isRetryable"
      to: "APIConnectionTimeoutError"
      via: "err.name + regex"
      pattern: "APIConnectionTimeoutError"
---

<objective>
Reanimar o núcleo de resiliência da stack de IA em `_shared/ai-client.ts` + `_shared/circuit-breaker.ts`: circuit breaker compartilhado que abre de verdade (AI-02), timeout do SDK que volta a ser retriável (AI-03), cap de retry-budget para chamadas longas caberem no teto do EF (AI-04), replay de idempotência regenerável (AI-05) e guarda de NaN nos envs numéricos (AI-07).

Purpose: hoje cada `callAi` cria um breaker novo (nunca abre), o timeout literal `"Request timed out."` NÃO casa a regex `/timeout/i` (timeout vira fatal na 1ª tentativa), um env malformado mata o loop (`while (attempt < NaN)`), e uma falha cacheada é devolvida para sempre. São correções puras/locais, Deno-testáveis, sem tocar PROD.
Output: dois módulos `_shared` corrigidos + dois arquivos de teste atualizados; corpus Deno verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-ressurrei-o-da-stack-de-ia/23-RESEARCH.md

<interfaces>
<!-- Contratos-chave já no código. Executor NÃO precisa explorar o codebase. -->

De supabase/functions/_shared/ai-client.ts (estado ATUAL a corrigir):
- `const MAX_ATTEMPTS = Number(Deno.env.get("MAX_ATTEMPTS") ?? "3");`            // :70 → parseIntEnv
- `const AI_CALL_TIMEOUT_MS = Number(Deno.env.get("AI_CALL_TIMEOUT_MS") ?? "25000");` // :78 → parseIntEnv
- `function isRetryable(err): boolean { ... return /529|overloaded|timeout|503|429/i.test(msg); }` // :234-239 — regex NÃO casa "timed out"
- retry loop `while (attempt < MAX_ATTEMPTS)` // :364 — sem cap de budget
- comentário ERRADO :378-382 ("casa /timeout/i") — corrigir
- `const breaker: BreakerLike = deps.breaker ?? new CircuitBreaker();` // :300 → sharedBreaker
- `async function tryIdempotencyReplay(...)` // :252-279 — devolve success=false; call site :304-309
- `effectiveTimeoutMs` já computado :297-299 (override timeoutMs)

De supabase/functions/_shared/circuit-breaker.ts:
- `export class CircuitBreaker { private readonly THRESHOLD = 5; private readonly RESET_MS = 60_000; canRequest()/recordSuccess()/recordFailure() }`

De supabase/functions/_shared/__tests__/circuit-breaker.test.ts:
- `const THRESHOLD = 5;` // :23 hard-coded — atualizar p/ 3
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseIntEnv (AI-07) + sharedBreaker singleton + THRESHOLD env-config invariante (AI-02)</name>
  <files>supabase/functions/_shared/ai-client.ts, supabase/functions/_shared/circuit-breaker.ts, supabase/functions/_shared/__tests__/circuit-breaker.test.ts, supabase/functions/_shared/__tests__/ai-client.test.ts</files>
  <read_first>
    - supabase/functions/_shared/ai-client.ts:60-78 (MAX_ATTEMPTS/AI_CALL_TIMEOUT_MS via Number(env)), :300 (breaker default)
    - supabase/functions/_shared/circuit-breaker.ts:24-59 (classe CircuitBreaker)
    - supabase/functions/_shared/__tests__/circuit-breaker.test.ts:22-51 (THRESHOLD=5 hard-coded)
    - 23-RESEARCH.md §AI-02 (linhas 234-253) + §AI-07 Code Examples (394-406) + Pitfall 3 (343-345)
  </read_first>
  <behavior>
    - parseIntEnv('MAX_ATTEMPTS','abc'→NaN) retorna o default 3; parseIntEnv('AI_CALL_TIMEOUT_MS','25s'→NaN) retorna 25000; valor ≤0 também cai no default
    - Uma instância FRESH de CircuitBreaker abre após THRESHOLD falhas; com CIRCUIT_BREAKER_THRESHOLD=10 e MAX_ATTEMPTS=3 a invariante força abertura em 3 (min)
    - callAi sem deps.breaker usa o sharedBreaker (default injetado)
  </behavior>
  <action>
    Adicionar em ai-client.ts um helper module-level exportado `parseIntEnv(name: string, fallback: number): number` (assinatura do 23-RESEARCH:396-402): lê Deno.env.get(name); se Number(raw) for finito E >0 retorna-o; senão console.warn + retorna fallback. Substituir os dois sites numéricos: `Number(Deno.env.get("MAX_ATTEMPTS") ?? "3")` (:70) → `parseIntEnv("MAX_ATTEMPTS", 3)` e `Number(Deno.env.get("AI_CALL_TIMEOUT_MS") ?? "25000")` (:78) → `parseIntEnv("AI_CALL_TIMEOUT_MS", 25000)`. Exportar parseIntEnv para reuso (circuit-breaker + AI-06).
    Em circuit-breaker.ts: manter o módulo PURO (sem import de rede/DB) — replicar um leitor de env-int local com a MESMA guarda de NaN (ou importar parseIntEnv de ai-client se não criar ciclo — decidir; preferir um guard local pequeno para não acoplar). Computar no topo do módulo: RAW_THRESHOLD = envInt("CIRCUIT_BREAKER_THRESHOLD", 3), MAX = envInt("MAX_ATTEMPTS", 3), THRESHOLD_EFFECTIVE = Math.min(RAW_THRESHOLD, MAX) (invariante THRESHOLD ≤ MAX_ATTEMPTS). Tornar o `private readonly THRESHOLD = 5` um parâmetro de constructor com default = THRESHOLD_EFFECTIVE (para os testes ainda poderem injetar um valor). Adicionar `export const sharedBreaker = new CircuitBreaker();` module-level (singleton por-isolate — limitação documentada, aceita).
    Em ai-client.ts:300 trocar `deps.breaker ?? new CircuitBreaker()` por `deps.breaker ?? sharedBreaker` e importar sharedBreaker de circuit-breaker.ts.
    Atualizar circuit-breaker.test.ts: mudar `const THRESHOLD = 5` (:23) para 3; manter os testes de instância fresh; ADICIONAR um teste da invariante (setar CIRCUIT_BREAKER_THRESHOLD=10 + MAX_ATTEMPTS=3 → abre após 3 falhas). Adicionar em ai-client.test.ts um teste que injeta um CircuitBreaker FRESH (nunca o sharedBreaker — Pitfall 3), registra THRESHOLD falhas via chamadas que rejeitam, e assere canRequest()===false / roteia p/ OpenAI depois disso.
  </action>
  <acceptance_criteria>
    - `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/_shared/__tests__/circuit-breaker.test.ts` VERDE com THRESHOLD=3 + teste de invariante passa
    - `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/_shared/__tests__/ai-client.test.ts` VERDE
    - `grep -c "export const sharedBreaker" supabase/functions/_shared/circuit-breaker.ts` == 1
    - `grep -c "?? sharedBreaker" supabase/functions/_shared/ai-client.ts` == 1
    - `grep -c "parseIntEnv" supabase/functions/_shared/ai-client.ts` >= 3
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/_shared/__tests__/circuit-breaker.test.ts supabase/functions/_shared/__tests__/ai-client.test.ts</automated>
  </verify>
  <done>THRESHOLD env-config com invariante ≤ MAX_ATTEMPTS; sharedBreaker injetado por default; parseIntEnv guarda os 2 envs numéricos; testes verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: isRetryable casa o timeout do SDK (AI-03) + cap de retry-budget (AI-04)</name>
  <files>supabase/functions/_shared/ai-client.ts, supabase/functions/_shared/__tests__/ai-client.test.ts</files>
  <read_first>
    - supabase/functions/_shared/ai-client.ts:225-239 (statusOf/isRetryable), :362-433 (retry loop), :378-382 (comentário ERRADO)
    - 23-RESEARCH.md §AI-03 (255-274, com o fix recomendado) + Open Q2 orçamento de retry (513-514) + §AI-04 (276-282)
    - supabase/functions/_shared/__tests__/ai-client.test.ts:257-270 (teste que MASCARA o bug com "529 overloaded")
  </read_first>
  <behavior>
    - isRetryable(new Error("Request timed out.")) === true (regex ampliada /tim(e|ed)\s*out/i)
    - isRetryable de um erro com name "APIConnectionTimeoutError" e sem status === true
    - Com timeoutMs=60000 o loop faz no máximo 2 tentativas (cap), não 3, antes de cair no fallback
  </behavior>
  <action>
    Reescrever isRetryable (:234-239): manter o check de status (RETRYABLE_STATUS); adicionar check por tipo — `const name = (err as {name?:string})?.name ?? (err as {constructor?:{name?:string}})?.constructor?.name; if (name === "APIConnectionTimeoutError") return true;` (cobre Anthropic E OpenAI sem import do SDK — 23-RESEARCH:93); ampliar a regex para `/529|overloaded|503|429|tim(e|ed)\s*out/i`. Corrigir o comentário :378-382 que afirma (erradamente) que `/timeout/i` casa "Request timed out." — descrever o matcher real (name + regex `tim(e|ed)\s*out`).
    Adicionar o cap de retry-budget: computar `effectiveMaxAttempts = effectiveTimeoutMs > 25000 ? Math.max(1, Math.min(MAX_ATTEMPTS, Math.floor(140000 / effectiveTimeoutMs))) : MAX_ATTEMPTS;` (para 60000 → floor(140000/60000)=2). Trocar o `while (attempt < MAX_ATTEMPTS)` (:364) e o `attempt < MAX_ATTEMPTS` interno (:427) por `effectiveMaxAttempts`. Comentar o motivo (3×60s+backoff estoura o teto ~150s idle do EF — 23-RESEARCH Open Q2).
    Corrigir o teste que mascara o bug: em ai-client.test.ts:257-270, trocar `new Error("anthropic 529 overloaded")` (no makeMockAnthropic failTimes) por um erro com a forma REAL do timeout — `Object.assign(new Error("Request timed out."), { name: "APIConnectionTimeoutError" })` — para o teste exercitar o matcher de timeout de verdade. Adicionar um teste do cap: com `timeoutMs: 60_000` + `failTimes: 2`, assere `anthropic.calls.length === 2` (cap) e que caiu p/ OpenAI (provider "openai").
  </action>
  <acceptance_criteria>
    - `grep -c "APIConnectionTimeoutError" supabase/functions/_shared/ai-client.ts` >= 1
    - `grep -c "tim(e|ed)" supabase/functions/_shared/ai-client.ts` >= 1
    - `grep -c "effectiveMaxAttempts" supabase/functions/_shared/ai-client.ts` >= 2
    - o teste de timeout em ai-client.test.ts referencia name "APIConnectionTimeoutError" / "Request timed out." (grep)
    - `deno test … ai-client.test.ts` VERDE (inclui o teste do cap: 2 tentativas com timeoutMs 60000)
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/_shared/__tests__/ai-client.test.ts</automated>
  </verify>
  <done>timeout retriável por name+regex; retry-budget capado p/ caber em ~150s; teste de mascaramento trocado pela forma real; verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: replay de idempotência só de SUCESSO (AI-05)</name>
  <files>supabase/functions/_shared/ai-client.ts, supabase/functions/_shared/__tests__/ai-client.test.ts</files>
  <read_first>
    - supabase/functions/_shared/ai-client.ts:252-279 (tryIdempotencyReplay — devolve success=false hoje), :304-309 (call site)
    - 23-RESEARCH.md §AI-05 (284-301, mapa de impacto por EF)
  </read_first>
  <behavior>
    - Uma linha ai_call_logs com success=false para a idempotency_key → callAi FAZ uma chamada real ao provedor (não replay)
    - Uma linha com success=true → replay (cost_usd 0, cache_hit true, sem chamar provedor)
  </behavior>
  <action>
    Em tryIdempotencyReplay: filtrar apenas linhas de SUCESSO — adicionar `.eq("success", true)` na cadeia do select OU retornar null quando `existing.success === false`. Remover a ramificação morta `flagged_for_human_review: existing.success === false ? true : undefined` (só sucesso replaya agora). Manter os retornos defensivos (sem key / sem select / erro → null; nunca lança). Assim uma falha cacheada cai para uma chamada nova (permitindo o RH reprocessar — AI-05).
    Adicionar em ai-client.test.ts um mock de supabase que implemente a cadeia `from("ai_call_logs").select(...).eq(...).maybeSingle()` (o makeMockSupabase atual só espiona insert — criar uma variante makeMockSupabaseWithReplay(row)). Teste A: row {success:false,...} → callAi deve chamar o provedor (anthropic.calls.length===1), não replay. Teste B: row {success:true, provider:"anthropic", output:{...}} → callAi retorna cache_hit/cost_usd 0 e anthropic.calls.length===0.
  </action>
  <acceptance_criteria>
    - `grep -c "success" supabase/functions/_shared/ai-client.ts` mostra o filtro no replay (eq success true OU guard success===false)
    - `deno test … ai-client.test.ts` VERDE: replay de success=false cai p/ chamada real; success=true replaya (0 chamadas)
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/_shared/__tests__/ai-client.test.ts</automated>
  </verify>
  <done>replay success-only; falhas regeneráveis; verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF runtime → provedor de IA (Anthropic/OpenAI) | chamada externa cara; retry/timeout/breaker governam custo e disponibilidade |
| env vars → runtime | env malformado pode derrubar a stack (NaN loop-death) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Denial of Service (financeiro) | retry loop 3×60s+backoff > teto 150s do EF | mitigate | cap de retry-budget: effectiveMaxAttempts = min(MAX_ATTEMPTS, floor(140000/effectiveTimeoutMs)) quando timeout>25s (Task 2) |
| T-23-01-02 | Denial of Service | env MAX_ATTEMPTS/AI_CALL_TIMEOUT_MS malformado → NaN → while(attempt<NaN) nunca roda → nenhuma chamada | mitigate | parseIntEnv guarda NaN/≤0 → default (Task 1) |
| T-23-01-03 | Tampering (integridade de replay) | replay devolvendo uma falha cacheada como resultado terminal | mitigate | replay só de linhas success=true; falha cai p/ chamada nova (Task 3) — NÃO permite que uma falha envenenada seja replayada como sucesso |
| T-23-01-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote instalado neste plano; SDKs (@anthropic-ai/sdk 0.102.0 / openai 6.42.0 / zod) pinados + `npm:` estáticos desde Phase 9 (23-RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- Corpus Deno completo verde: `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions`
- `npm run lint` (tsc) permanece no baseline pinado (Phase 22 = 133) — este plano não toca `src/`.
</verification>

<success_criteria>
- circuit-breaker.ts exporta sharedBreaker + THRESHOLD env-config com invariante ≤ MAX_ATTEMPTS
- ai-client.ts: parseIntEnv nos 2 envs, isRetryable casa timeout (name+regex), cap de retry-budget, replay success-only, default sharedBreaker
- 2 arquivos de teste atualizados (THRESHOLD 3, timeout real, cap, replay-fail-fallthrough); corpus Deno verde
</success_criteria>

<output>
Create `.planning/phases/23-ressurrei-o-da-stack-de-ia/23-01-SUMMARY.md` when done
</output>
