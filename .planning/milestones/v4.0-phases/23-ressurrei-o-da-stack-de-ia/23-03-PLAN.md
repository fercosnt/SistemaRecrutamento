---
phase: 23-ressurrei-o-da-stack-de-ia
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - supabase/functions/_shared/ai-client.ts
  - supabase/functions/_shared/__tests__/ai-client.test.ts
  - supabase/functions/cost-alerter/index.ts
  - supabase/functions/cost-alerter/messages.ts
  - supabase/functions/_shared/__tests__/cost-alerter-messages.test.ts
autonomous: true
requirements: [AI-06]
must_haves:
  truths:
    - "Um kill-switch PRÉ-chamada recusa novas chamadas de IA quando o gasto do dia (per-vaga) cruza um teto configurável — corta gasto em RUNTIME, não com 1 dia de atraso (AI-06)"
    - "O kill-switch é fail-OPEN: uma falha no lookup do custo NÃO bloqueia a chamada (o trigger é o backstop)"
    - "O canal candidate_cost_over_1 tem cópia pt-BR e é regression-guarded por teste (deixa de ser código morto não-testado) (AI-06)"
    - "AI-06 traceability: guardrail de custo com canal/janela reais — kill-switch em runtime é o único fix que corta gasto"
  artifacts:
    - path: "supabase/functions/_shared/ai-client.ts"
      provides: "kill-switch pré-chamada (SUM cost_usd do dia por vaga vs AI_DAILY_COST_CAP_USD)"
      contains: "AI_DAILY_COST_CAP_USD"
    - path: "supabase/functions/cost-alerter/messages.ts"
      provides: "alertMessage extraído (4 canais incl candidate_cost_over_1) — testável sem Deno.serve"
      contains: "candidate_cost_over_1"
    - path: "supabase/functions/_shared/__tests__/cost-alerter-messages.test.ts"
      provides: "teste dos 4 branches de alertMessage"
  key_links:
    - from: "callAi"
      to: "ai_call_logs (SUM cost_usd do dia por vaga)"
      via: "pre-call kill-switch"
      pattern: "cost_cap_exceeded"
---

<objective>
Cortar gasto de IA em RUNTIME (AI-06): adicionar um kill-switch PRÉ-chamada em `callAi` que soma o custo do dia corrente por vaga e recusa novas chamadas acima de um teto configurável — o único fix que corta gasto na hora (o alerta atual só detecta o dia seguinte às 01:30). Além disso, extrair `alertMessage` do cost-alerter para um módulo testável e cobrir os 4 canais (incluindo `candidate_cost_over_1`, hoje código morto não-testado).

Purpose: a cadeia de custo tem 4 furos (janela ~25h, escopo de fatia diária, canal candidate morto, dispatch silencioso). O corte real de gasto só acontece com um teto pré-chamada no runtime da EF (23-RESEARCH §AI-06 hole #5). A janela/escopo do trigger e o RAISE WARNING de Vault ausente são a migration do Plan 23-05.
Output: kill-switch em ai-client.ts + cost-alerter refatorado/testado; corpus Deno verde. NÃO redeploya (Plan 23-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-ressurrei-o-da-stack-de-ia/23-RESEARCH.md

<interfaces>
De ai-client.ts (após Plan 23-01): `parseIntEnv(name, fallback)` disponível; `callAi(args, deps)` com replay de idempotência :304-309, injection/mask/breaker :311-360. SupabaseLike já tem `select?` opcional (:165-174) para feature-detect.
Índice existente: `idx_ai_logs_vaga_cost (vaga_id, cost_usd) WHERE success=true` (soma per-vaga do dia é barata — 23-RESEARCH:316).
De cost-alerter/index.ts: `alertMessage(b: CostAnomalyBody): string` (:87-98) com 4 cases incl `candidate_cost_over_1` (:93) — NÃO exportado hoje; o handler usa Deno.serve (não unit-testável direto).
CostAnomalyBody = { alert_type, vaga_id, date, value, threshold } (:76-84).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kill-switch pré-chamada de custo em callAi (AI-06 corte em runtime)</name>
  <files>supabase/functions/_shared/ai-client.ts, supabase/functions/_shared/__tests__/ai-client.test.ts</files>
  <read_first>
    - supabase/functions/_shared/ai-client.ts:289-360 (callAi: replay :304-309, injection :311, mask :350, breaker :353)
    - supabase/functions/_shared/ai-cost.ts:38-49 (calculateCost — referência)
    - 23-RESEARCH.md §AI-06 os 4 furos (303-316), hole #5 kill-switch + idx_ai_logs_vaga_cost (316)
  </read_first>
  <behavior>
    - Com AI_DAILY_COST_CAP_USD baixo e um ai_call_logs cuja SUM(cost_usd) do dia/vaga já excede o teto → callAi retorna error_code 'cost_cap_exceeded' e NÃO chama o provedor (anthropic.calls.length===0)
    - Abaixo do teto → callAi procede normalmente
    - Se o lookup do custo falhar (select ausente/erro) → callAi PROCEDE (fail-open)
  </behavior>
  <action>
    Adicionar em callAi, DEPOIS do replay de idempotência (:309) e ANTES da detecção de injection (:311), um kill-switch pré-chamada: ler `const dailyCap = parseIntEnv("AI_DAILY_COST_CAP_USD", 50)` (teto de SEGURANÇA em runtime — documentar que é o hard-cap operacional, NÃO o threshold de negócio R$200/mês por vaga, que o trigger/cost-alerter cobre). Consultar defensivamente `supabase.from("ai_call_logs").select("cost_usd")` filtrado por vaga_id + created_at >= início do dia UTC corrente; somar cost_usd client-side. Se a soma >= dailyCap: logAiCall uma linha success=false com error_code "cost_cap_exceeded" e RETORNAR `{ provider: "none", parsed: { recommendation: "hold", flagged_for_human_review: true }, cost_usd: 0, latency_ms: Date.now()-start, cache_hit: false, prompt_version: prompt.prompt_version, error_code: "cost_cap_exceeded", flagged_for_human_review: true }` — SEM chamar nenhum provedor. Feature-detect o select (como tryIdempotencyReplay :259) e envolver em try/catch: qualquer erro/ausência → NÃO bloquear (fail-open; o trigger DB é o backstop e não podemos matar o funil por um lookup que falhou). Preservar RNF-07a: o kill-switch NÃO rejeita candidato — devolve 'hold' + flagged_for_human_review.
    Teste em ai-client.test.ts: criar um mock supabase cujo `from("ai_call_logs").select("cost_usd")...` (encadeável até gte/lt) devolva rows somando > cap; com env AI_DAILY_COST_CAP_USD setado baixo → callAi retorna error_code 'cost_cap_exceeded' e anthropic.calls.length===0. Um segundo teste: soma < cap → provider "anthropic" normal. Um terceiro: select que lança → callAi procede (fail-open).
  </action>
  <acceptance_criteria>
    - `grep -c "AI_DAILY_COST_CAP_USD" supabase/functions/_shared/ai-client.ts` >= 1
    - `grep -c "cost_cap_exceeded" supabase/functions/_shared/ai-client.ts` >= 1
    - `deno test … ai-client.test.ts` VERDE: over-cap → 0 chamadas de provedor + error_code cost_cap_exceeded; under-cap → normal; lookup-erro → fail-open
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/_shared/__tests__/ai-client.test.ts</automated>
  </verify>
  <done>gasto cortado em runtime via kill-switch pré-chamada; fail-open em erro de lookup; RNF-07a preservado (hold, nunca reject).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extrair alertMessage do cost-alerter + cobrir os 4 canais (candidate_cost_over_1)</name>
  <files>supabase/functions/cost-alerter/messages.ts, supabase/functions/cost-alerter/index.ts, supabase/functions/_shared/__tests__/cost-alerter-messages.test.ts</files>
  <read_first>
    - supabase/functions/cost-alerter/index.ts:76-98 (CostAnomalyBody + alertMessage com 4 cases; candidate_cost_over_1 :93)
    - 23-RESEARCH.md §AI-06 hole #3 (candidate_cost_over_1 é código morto — 312)
  </read_first>
  <behavior>
    - alertMessage({alert_type:'candidate_cost_over_1', value, threshold, ...}) retorna a cópia pt-BR de custo por candidato
    - os 4 branches (vaga_cost_over_200, error_rate, candidate_cost_over_1, default) retornam strings distintas e sem termos de produto proibidos
  </behavior>
  <action>
    Criar supabase/functions/cost-alerter/messages.ts exportando `interface CostAnomalyBody` (mover de index.ts) e `export function alertMessage(b: CostAnomalyBody): string` (mover as 4 cases inalteradas, incl candidate_cost_over_1). Em cost-alerter/index.ts, importar CostAnomalyBody + alertMessage de "./messages.ts" e remover as definições locais (o handler passa a usar o import — comportamento byte-idêntico). Criar supabase/functions/_shared/__tests__/cost-alerter-messages.test.ts (Deno) importando de "../../cost-alerter/messages.ts" (módulo puro, SEM Deno.serve) e asserindo os 4 branches: candidate_cost_over_1 retorna a cópia "por candidato"; error_rate menciona "%"; vaga_cost_over_200 menciona a vaga; default é o genérico. Guard extra: nenhuma string casa termos proibidos de produto (não introduzir "teste psicológico" etc.).
  </action>
  <acceptance_criteria>
    - `grep -c "candidate_cost_over_1" supabase/functions/cost-alerter/messages.ts` >= 1
    - cost-alerter/index.ts importa alertMessage de "./messages.ts" (grep `from './messages`)
    - `deno test … cost-alerter-messages.test.ts` VERDE (4 branches distintos)
    - corpus Deno completo VERDE (handler ainda compila com o import)
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions</automated>
  </verify>
  <done>alertMessage testável; candidate_cost_over_1 coberto por teste (deixa de ser código morto não-guardado).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF runtime → provedor de IA | sem teto pré-chamada, um loop/n8n pode gastar sem limite antes do alerta do dia seguinte |
| trigger DB → cost-alerter EF | Bearer Vault; o canal de alerta não pode ser silenciável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | Denial of Service (financeiro) | gasto descontrolado — guardrail detect-only com 1 dia de atraso; kill-switch quebrado = spend ilimitado | mitigate | kill-switch pré-chamada em runtime (Task 1) — corta na hora; fail-open só em erro de lookup (o trigger DB é backstop) |
| T-23-03-02 | Tampering / Availability | kill-switch bloqueando o funil por um lookup de custo que falhou | mitigate | fail-open: erro/ausência do select → procede; nunca mata a chamada por infra do próprio guard (Task 1) |
| T-23-03-03 | Repudiation | candidate_cost_over_1 morto/não-testado → alerta que nunca dispara | mitigate | extrair + testar os 4 canais (Task 2); a emissão pelo trigger fica no Plan 23-05 (migration) |
| T-23-03-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote instalado; imports estáticos preservados |
</threat_model>

<verification>
- Corpus Deno completo verde
- RNF-07a: o kill-switch retorna 'hold' + flagged_for_human_review — nunca rejeita candidato
</verification>

<success_criteria>
- callAi tem kill-switch pré-chamada por-vaga (AI_DAILY_COST_CAP_USD) fail-open; error_code cost_cap_exceeded testado
- alertMessage extraído + 4 canais testados (candidate_cost_over_1 incl); corpus Deno verde
</success_criteria>

<output>
Create `.planning/phases/23-ressurrei-o-da-stack-de-ia/23-03-SUMMARY.md` when done
</output>
