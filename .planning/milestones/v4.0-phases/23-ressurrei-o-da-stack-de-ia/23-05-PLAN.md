---
phase: 23-ressurrei-o-da-stack-de-ia
plan: 05
type: execute
wave: 3
depends_on: ["23-02", "23-03"]
files_modified:
  - supabase/migrations/2026XXXXXXXXXX_bigfive_devolutiva_enum.sql
  - supabase/migrations/2026XXXXXXXXXX_bigfive_devolutiva_seed.sql
  - supabase/migrations/2026XXXXXXXXXX_cost_guardrail_fix.sql
autonomous: false
requirements: [AI-01, AI-06]
must_haves:
  truths:
    - "public.llm_call_type aceita 'bigfive_devolutiva' — ai_call_logs.call_type e prompt_versions.call_type param de rejeitar 22P02 a devolutiva (AI-01)"
    - "Existe exatamente 1 row prompt_versions is_active=true para bigfive_devolutiva (semver 1.0.0) — loadPrompt resolve o prompt real (AI-01)"
    - "culture_fit_essay / transcript_analysis / interview_guide estão is_active=true em PROD (ativados se faltava) (AI-01)"
    - "O trigger notify_cost_anomaly emite candidate_cost_over_1 e faz RAISE WARNING quando os secrets Vault estão ausentes (não RETURN NEW mudo) (AI-06)"
    - "O ledger de migrations converge — supabase db push --linked responde 'up to date' (sem drift execute_sql-only)"
  artifacts:
    - path: "supabase/migrations/*_bigfive_devolutiva_enum.sql"
      provides: "ALTER TYPE llm_call_type ADD VALUE 'bigfive_devolutiva' (arquivo próprio — não editar 20260609000001)"
    - path: "supabase/migrations/*_bigfive_devolutiva_seed.sql"
      provides: "seed + activate prompt_versions row p/ bigfive_devolutiva"
    - path: "supabase/migrations/*_cost_guardrail_fix.sql"
      provides: "notify_cost_anomaly: candidate_cost_over_1 + RAISE WARNING de Vault ausente"
  key_links:
    - from: "gerar-devolutiva-bigfive loadPrompt('bigfive_devolutiva')"
      to: "prompt_versions row ativa"
      via: "enum + seed + activate"
      pattern: "bigfive_devolutiva"
---

<objective>
[BLOCKING · non-autonomous · escreve em PROD] Aterrissar em PROD o estado de runtime que o código dos Planos 23-02/03 pressupõe: estender o enum `llm_call_type` com `bigfive_devolutiva` (via Supabase MCP — não-transacional), semear + ativar sua row `prompt_versions`, ativar os 3 call_types cujo is_active em PROD não estava confirmado (culture_fit_essay/transcript_analysis/interview_guide), verificar a coluna `ai_call_logs.prompt_version` (Open Q3) e corrigir o trigger de custo (candidate_cost_over_1 + RAISE WARNING de Vault ausente — AI-06).

Purpose: `bigfive_devolutiva` NÃO é valor do enum → toda escrita em ai_call_logs da devolutiva falha (22P02) e loadPrompt lança PromptNotConfiguredError (por isso o catch estreitado do 23-02 500a até esta migration). `ADD VALUE` é não-transacional e não pode ser usado na mesma txn que insere com o valor → 2 migrations separadas, aplicadas via MCP `apply_migration` (precedente M2 — grava a version row, contorna 42601). NÃO editar a migration base 20260609000001 (DBMIG-01/Phase-27 reconstrói o banco — o enum extend precisa ser arquivo próprio p/ o ledger convergir).
Output: 3 migration files aplicados em PROD via MCP + ledger reconciliado + SQL smokes. Runbook exato abaixo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-ressurrei-o-da-stack-de-ia/23-RESEARCH.md

<interfaces>
Enum atual (20260609000001:54-62): 7 valores, SEM bigfive_devolutiva.
prompt_versions colunas seed (20260609000004:37-40): call_type, semver, content_hash, system_template, user_template, model_id, temperature, max_tokens, schema_version_required, is_active, is_canary, change_summary, changed_by. Ativação é UPDATE manual one-time por call_type (precedente Phase 10/11).
Template do prompt: docs/conhecimento/prompts/templates/08-bigfive-devolutiva.md (frontmatter + system/user templates).
notify_cost_anomaly(): 20260609000002_prompt_library_rpcs.sql (emite vaga_cost_over_200 + error_rate; candidate_cost_over_1 nunca emitido; RETURN NEW mudo quando Vault ausente).
Padrão MCP (memory M2): mcp__supabase__apply_migration grava a version row sozinho e contorna 42601 nos corpos $$; execute_sql p/ SELECTs de verificação e UPDATE de ativação.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 [BLOCKING]: verificar estado PROD dos prompt_versions + ativar call_types + Open Q3</name>
  <files>(nenhum arquivo — verificação/ativação PROD via Supabase MCP)</files>
  <read_first>
    - 23-RESEARCH.md §Runtime State Inventory (318-330), Open Q1 (508-511) + Q3 (516-517), Assumptions A1/A2 (501-502)
  </read_first>
  <what-built>
    Verificação do estado de runtime PROD que gate o resto do plano (nenhum código foi escrito ainda nesta task — são queries + 1 UPDATE de ativação).
  </what-built>
  <action>
    Runbook (Supabase MCP `execute_sql`, projeto isljnozzlvckrgjjbjwp):
    1. `SELECT call_type, is_active, semver, schema_version_required FROM public.prompt_versions ORDER BY call_type, is_active;` — confirmar que cv_job_match, comparative_ranking, work_sample_sjt estão is_active=true; identificar se culture_fit_essay / transcript_analysis / interview_guide têm row ativa.
    2. Para cada um dos 3 (culture_fit_essay/transcript_analysis/interview_guide) que NÃO tiver is_active=true: `UPDATE public.prompt_versions SET is_active=true WHERE call_type='<x>' AND semver='1.0.0';` — garantindo UMA row ativa por call_type (checar que não há duas ativas: a query 1 já revela).
    3. `SELECT column_name, data_type FROM information_schema.columns WHERE table_name='ai_call_logs' AND column_name IN ('prompt_version','prompt_version_id');` (Open Q3) — o audit-logger escreve `prompt_version` (text). Se a coluna NÃO existir, ADICIONAR na Task 3 (`ALTER TABLE public.ai_call_logs ADD COLUMN IF NOT EXISTS prompt_version text;`) — anotar a decisão.
    4. `SELECT enumlabel FROM pg_enum e JOIN pg_type t ON e.enumtypid=t.oid WHERE t.typname='llm_call_type' ORDER BY 1;` — confirmar os 7 valores atuais (baseline p/ a Task 2).
    Colar TODAS as saídas no SUMMARY. Elas gate as Tasks 2/3.
  </action>
  <how-to-verify>
    O SUMMARY deve conter: a tabela is_active dos call_types (com os 3 confirmados/ativados), a existência (ou não) de ai_call_logs.prompt_version, e a lista de labels do enum (7). Confirmar que cada call_type consumido por EF tem exatamente 1 row ativa.
  </how-to-verify>
  <resume-signal>Cole as saídas dos SELECTs e digite "verificado" (ou aponte divergências que precisem revisar o plano).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 [BLOCKING]: enum bigfive_devolutiva + seed + activate (via MCP)</name>
  <files>supabase/migrations/2026XXXXXXXXXX_bigfive_devolutiva_enum.sql, supabase/migrations/2026XXXXXXXXXX_bigfive_devolutiva_seed.sql</files>
  <read_first>
    - 23-RESEARCH.md §bigfive exceção (203-214, os 5 passos), Pitfall 2 (339-341, ADD VALUE não-transacional)
    - docs/conhecimento/prompts/templates/08-bigfive-devolutiva.md (system/user template + frontmatter)
    - supabase/migrations/20260609000004_prompt_library_seed.sql:37-40 (colunas do INSERT)
  </read_first>
  <what-built>
    Duas migrations PROD: (A) estende o enum; (B) semeia + ativa a row de prompt. Aplicadas via MCP (não-transacional — B não pode rodar na mesma txn que A).
  </what-built>
  <action>
    Runbook:
    1. Criar `supabase/migrations/<ts>_bigfive_devolutiva_enum.sql` contendo APENAS `ALTER TYPE public.llm_call_type ADD VALUE IF NOT EXISTS 'bigfive_devolutiva';` (arquivo próprio — NÃO editar 20260609000001; DBMIG-01/Phase-27). Aplicar via `mcp__supabase__apply_migration` (name: `bigfive_devolutiva_enum`) — grava a version row, contorna 42601, e o ADD VALUE commita sozinho.
    2. Criar `supabase/migrations/<ts+1>_bigfive_devolutiva_seed.sql` que faz INSERT em public.prompt_versions p/ bigfive_devolutiva: semver '1.0.0', schema_version_required '1.0.0', model_id 'claude-sonnet-4-6', temperature 0, max_tokens 1200, system_template + user_template do template 08, content_hash (digest/pgcrypto como no seed base), is_active=true, is_canary=false, change_summary 'Phase 23 AI-01 bigfive_devolutiva revival', changed_by 'phase-23'. Aplicar via `mcp__supabase__apply_migration` (name: `bigfive_devolutiva_seed`) — DEPOIS da migration A commitar (não usar o valor do enum na mesma txn do ADD VALUE).
    3. Reconciliar o ledger: `supabase db push --linked` deve responder "Remote database is up to date".
    4. SQL smokes via `mcp__supabase__execute_sql`: (a) `SELECT 'bigfive_devolutiva'::public.llm_call_type;` (aceita, sem 22P02); (b) `SELECT is_active, semver FROM public.prompt_versions WHERE call_type='bigfive_devolutiva';` (1 row, is_active=true, 1.0.0); (c) num bloco descartável BEGIN/ROLLBACK, `INSERT INTO public.ai_call_logs (call_type, ...) VALUES ('bigfive_devolutiva', ...)` aceita o enum.
  </action>
  <how-to-verify>
    Smokes (a)(b)(c) passam; `supabase db push --linked` = up to date; o SUMMARY cola as saídas dos 3 smokes.
  </how-to-verify>
  <resume-signal>Cole as saídas dos smokes + o resultado do db push e digite "aplicado".</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [BLOCKING]: fix do trigger de custo — candidate_cost_over_1 + RAISE WARNING (AI-06)</name>
  <files>supabase/migrations/2026XXXXXXXXXX_cost_guardrail_fix.sql</files>
  <read_first>
    - supabase/migrations/20260609000002_prompt_library_rpcs.sql (notify_cost_anomaly — thresholds emitidos + RETURN NEW mudo)
    - 23-RESEARCH.md §AI-06 furos #3 (canal candidate morto) e #4 (dispatch silencioso) (312-313)
  </read_first>
  <what-built>
    Migration PROD que recria notify_cost_anomaly() com o canal candidate_cost_over_1 emitido e RAISE WARNING quando os secrets Vault estão ausentes (não RETURN NEW mudo). Se a Task 1 achou ai_call_logs.prompt_version ausente, esta migration também adiciona a coluna.
  </what-built>
  <action>
    Runbook:
    1. Criar `supabase/migrations/<ts+2>_cost_guardrail_fix.sql` que CREATE OR REPLACE FUNCTION notify_cost_anomaly() com dois fixes concretos e verificáveis: (a) emitir `candidate_cost_over_1` — agregar por candidato (ou o escopo per-vaga+global que a estrutura da row permite) e disparar o net.http_post com alert_type='candidate_cost_over_1' quando o threshold por-candidato cruza; (b) trocar o `RETURN NEW` mudo (quando project_url/edge_invoke_key do Vault estão ausentes) por `RAISE WARNING 'notify_cost_anomaly: Vault secret ausente (project_url/edge_invoke_key) — dispatch pulado'` antes do RETURN. NÃO alterar o body-shape do net.http_post ({alert_type, vaga_id, date, value, threshold}) — o cost-alerter já roteia candidate_cost_over_1 (Plan 23-03). Se a Task 1 achou ai_call_logs.prompt_version ausente: adicionar `ALTER TABLE public.ai_call_logs ADD COLUMN IF NOT EXISTS prompt_version text;` nesta migration.
    2. Aplicar via `mcp__supabase__apply_migration` (name: `cost_guardrail_fix`).
    3. Reconciliar: `supabase db push --linked` = up to date.
    4. Verificação: `SELECT proname FROM pg_proc WHERE proname='notify_cost_anomaly';` existe; `SELECT jobname, schedule FROM cron.job WHERE jobname LIKE 'ai-cost%';` (confirmar o cron); tentar `SELECT name FROM vault.secrets WHERE name IN ('project_url','edge_invoke_key');` (se o role não puder ler, anotar — não é evidência de ausência; o graceful-skip agora RAISE WARNING).
  </action>
  <how-to-verify>
    A função recriada referencia candidate_cost_over_1 e RAISE WARNING (inspecionar `SELECT prosrc FROM pg_proc WHERE proname='notify_cost_anomaly'`); db push up to date; cron job presente. Colar as saídas no SUMMARY.
  </how-to-verify>
  <resume-signal>Cole o prosrc (trechos candidate_cost_over_1 + RAISE WARNING) + db push e digite "aplicado".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → schema PROD | ADD VALUE + seed alteram estado de runtime (enum + rows ativas) |
| trigger DB → cost-alerter | Vault Bearer; dispatch não pode falhar em silêncio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05-01 | Tampering / Integrity | enum extend na migration base quebra o rebuild do banco (DBMIG-01/Phase-27) | mitigate | ADD VALUE em ARQUIVO PRÓPRIO, aplicado via MCP; ledger reconciliado (db push up to date) — nunca editar 20260609000001 |
| T-23-05-02 | Repudiation | dispatch de custo pulado em silêncio (Vault ausente) | mitigate | RAISE WARNING em vez de RETURN NEW mudo (Task 3) |
| T-23-05-03 | Elevation of Privilege | seed/activate com credencial errada | accept | apply_migration roda com service_role sob contexto MCP autorizado pelo usuário; ativação one-time é padrão M2 |
| T-23-05-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote instalado |
</threat_model>

<verification>
- SQL smokes: `SELECT 'bigfive_devolutiva'::llm_call_type` aceita; 1 row ativa; ai_call_logs.call_type aceita o enum
- `supabase db push --linked` = "Remote database is up to date" (ledger convergido)
- notify_cost_anomaly recriada com candidate_cost_over_1 + RAISE WARNING
</verification>

<success_criteria>
- enum + seed + activate de bigfive_devolutiva em PROD; 3 call_types ativados; Open Q3 resolvido
- trigger de custo corrigido; ledger converge
- todas as saídas de MCP coladas no SUMMARY (evidência PROD)
</success_criteria>

<output>
Create `.planning/phases/23-ressurrei-o-da-stack-de-ia/23-05-SUMMARY.md` when done
</output>
