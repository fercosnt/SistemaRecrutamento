---
phase: 23-ressurrei-o-da-stack-de-ia
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - supabase/functions/_shared/prompt-loader.ts
  - supabase/functions/_shared/audit-logger.ts
  - supabase/functions/_shared/__tests__/prompt-loader.test.ts
  - supabase/functions/_shared/__tests__/prompt-catch.test.ts
  - scripts/sync-prompts.ts
  - supabase/functions/analise-candidato-individual/index.ts
  - supabase/functions/comparativo-candidatos/index.ts
  - supabase/functions/avaliar-redacao/index.ts
  - supabase/functions/avaliar-redacao-cultural/index.ts
  - supabase/functions/avaliar-transcricao-entrevista/index.ts
  - supabase/functions/gerar-guia-entrevista/index.ts
  - supabase/functions/gerar-devolutiva-bigfive/index.ts
autonomous: true
requirements: [AI-01, AI-04, UX-07]
must_haves:
  truths:
    - "Os 7 call_types resolvem o prompt REAL da library — SCHEMA_VERSIONS espelha o enum llm_call_type (5 chaves órfãs de Phase-9 removidas) (AI-01)"
    - "Um call_type sem versão compatível FALHA ALTO (erro estruturado → 500 pt-BR do handler), nunca degrada para um stub de 1 linha 0.0.0 (AI-01)"
    - "Quando um stub dispararia, um alarme é emitido (recruiter_alerts row threshold_violated='ai_prompt_stub_fired') — nunca roda morto em silêncio (AI-01)"
    - "avaliar-transcricao-entrevista passa timeoutMs 60s (perfil Sonnet/4000 tokens, env-overridable) (AI-04)"
    - "A devolutiva Big Five não injeta percentil cru no prompt do LLM — passa a banda qualitativa (UX-07)"
    - "sync-prompts conhece bigfive_devolutiva no array CALL_TYPES (AI-01)"
  artifacts:
    - path: "supabase/functions/_shared/prompt-loader.ts"
      provides: "SCHEMA_VERSIONS espelhando o enum (8 chaves incl bigfive_devolutiva; órfãs droppadas)"
      contains: "bigfive_devolutiva"
    - path: "supabase/functions/_shared/__tests__/prompt-loader.test.ts"
      provides: "sweep: todo call_type de EF ∈ SCHEMA_VERSIONS + órfãs ausentes"
    - path: "supabase/functions/_shared/audit-logger.ts"
      provides: "emitPromptStubAlert helper (recruiter_alerts row)"
      contains: "emitPromptStubAlert"
    - path: "scripts/sync-prompts.ts"
      provides: "CALL_TYPES += bigfive_devolutiva"
      contains: "bigfive_devolutiva"
  key_links:
    - from: "cada EF catch de loadPrompt"
      to: "emitPromptStubAlert + re-throw"
      via: "narrowed catch"
      pattern: "emitPromptStubAlert"
    - from: "gerar-devolutiva-bigfive userBlock"
      to: "banda (não percentil)"
      via: "buildDevolutivaUserBlock"
      pattern: "banda"
---

<objective>
Ressuscitar a prompt library nos 7 call_types (AI-01): realinhar `SCHEMA_VERSIONS` ao enum real `llm_call_type`, estreitar o catch das 7 EFs para propagar `SchemaVersionMismatchError`/`PromptNotConfiguredError` como erro estruturado (nunca o stub silencioso de 1 linha `0.0.0`), emitir um alarme `ai_prompt_stub_fired` no ponto de degradação, ensinar o sync-prompts sobre `bigfive_devolutiva`, passar o timeout de 60s no `avaliar-transcricao-entrevista` (AI-04) e remover o percentil cru do prompt da devolutiva (UX-07).

Purpose: 5 dos 7 call_types caem hoje num `catch {}` mudo que roda um prompt-stub de 12 palavras persistido como avaliação oficial. A causa é `SCHEMA_VERSIONS` conhecer chaves fictícias de Phase-9. O alarme "0.0.0" NÃO pode aterrissar em ai_call_logs (FK uuid) — o alarme correto é no catch. `bigfive_devolutiva` é a exceção profunda: nem está no enum (a migration é o Plan 23-05); até lá esta EF 500a por design (alarme honesto).
Output: prompt-loader realinhado + sweep-test + alert helper + 7 catches estreitados; corpus Deno verde. NÃO redeploya (isso é o Plan 23-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-ressurrei-o-da-stack-de-ia/23-RESEARCH.md

<interfaces>
De prompt-loader.ts (ESTADO ATUAL a corrigir):
- SCHEMA_VERSIONS (:33-42) tem 8 chaves, 5 ÓRFÃS: sjt_evaluation, interview_questions, interview_summary, reference_check, final_recommendation
- classes exportadas: `SchemaVersionMismatchError` (:58, .code="schema_version_mismatch"), `PromptNotConfiguredError` (:76, .code="prompt_not_configured")

Enum real public.llm_call_type (7 valores, 20260609000001:54-62):
  cv_summary, cv_job_match, comparative_ranking, interview_guide, transcript_analysis, culture_fit_essay, work_sample_sjt
  (bigfive_devolutiva NÃO está — Plan 23-05 adiciona)

Mapa EF → call_type + linha do catch/stub (23-RESEARCH tabela 171-227):
| EF | call_type | catch (linha) | stub prompt_version |
| analise-candidato-individual | cv_job_match | :230 | "0.0.0" (:235) — HOJE já roda real, mas estreitar mesmo assim |
| avaliar-redacao | work_sample_sjt | :238 | "0.0.0" (:243) |
| avaliar-redacao-cultural | culture_fit_essay | :230 | "0.0.0" (:235) |
| avaliar-transcricao-entrevista | transcript_analysis | :196 | "0.0.0" (:201) |
| comparativo-candidatos | comparative_ranking | :242 | "0.0.0" (:247) — HOJE roda real |
| gerar-guia-entrevista | interview_guide | :231 | "0.0.0" (:236) |
| gerar-devolutiva-bigfive | bigfive_devolutiva | :573 | "1.0.0" (:578) — exceção |

recruiter_alerts.threshold_violated é `text NOT NULL` (20260609000001:342) — aceita 'ai_prompt_stub_fired'.
gerar-guia-entrevista:270-274 já passa `timeoutMs: 60_000` (precedente AI-04).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SCHEMA_VERSIONS espelha o enum + sweep-test + sync-prompts + emitPromptStubAlert</name>
  <files>supabase/functions/_shared/prompt-loader.ts, supabase/functions/_shared/__tests__/prompt-loader.test.ts, scripts/sync-prompts.ts, supabase/functions/_shared/audit-logger.ts</files>
  <read_first>
    - supabase/functions/_shared/prompt-loader.ts:33-42 (SCHEMA_VERSIONS órfãs) + :125-130 (assertSchemaVersionCompat)
    - supabase/migrations/20260609000001_prompt_library_schema.sql:54-62 (enum) + :339-342 (recruiter_alerts.threshold_violated text)
    - scripts/sync-prompts.ts:31-40 (CALL_TYPES z.enum)
    - supabase/functions/_shared/audit-logger.ts:103-149 (logAiCall — padrão de insert defensivo + console.error code-only)
    - 23-RESEARCH.md §AI-01 map (171-201, fix alvo 190-200), órfãs (187), §alarme (229-232) + Pitfall 1 (334-337)
  </read_first>
  <behavior>
    - SCHEMA_VERSIONS contém as 8 chaves do enum + bigfive_devolutiva (todas "1.0.0") e NENHUMA das 5 órfãs
    - o sweep-test falha se um call_type que uma EF passa não estiver em SCHEMA_VERSIONS
    - emitPromptStubAlert(mockSupabase, "work_sample_sjt") insere 1 row em recruiter_alerts com threshold_violated='ai_prompt_stub_fired'; nunca lança
  </behavior>
  <action>
    Substituir SCHEMA_VERSIONS (:33-42) pelo alvo do 23-RESEARCH:190-200 — chaves = cv_summary, cv_job_match, comparative_ranking, interview_guide, transcript_analysis, culture_fit_essay, work_sample_sjt, bigfive_devolutiva (todas "1.0.0"). DROPAR as 5 órfãs (sjt_evaluation, interview_questions, interview_summary, reference_check, final_recommendation). Comentar que espelha o enum public.llm_call_type + a exceção bigfive_devolutiva (enum vem no Plan 23-05).
    Criar supabase/functions/_shared/__tests__/prompt-loader.test.ts (Deno): importar SCHEMA_VERSIONS; sobre uma lista hard-coded dos call_types que as 7 EFs passam a loadPrompt (cv_job_match, comparative_ranking, work_sample_sjt, culture_fit_essay, transcript_analysis, interview_guide, bigfive_devolutiva) assere que cada um ∈ SCHEMA_VERSIONS (regression guard do bug AI-01); e assere que as 5 chaves órfãs estão AUSENTES.
    Em scripts/sync-prompts.ts:32-40 adicionar "bigfive_devolutiva" ao array CALL_TYPES.
    Em audit-logger.ts adicionar e exportar `async function emitPromptStubAlert(supabaseAdmin: SupabaseLike, call_type: string): Promise<void>` que faz INSERT em recruiter_alerts { threshold_violated: 'ai_prompt_stub_fired', channel: 'ai_stack', message: `Prompt stub disparado para call_type='${call_type}' — versão de prompt não resolvida (falha alta).`, value: 0, threshold: 0, call_type: null, vaga_id: null, candidato_id: null, created_at: new Date().toISOString() }; try/catch que loga só o code (padrão logAiCall :142-148) — NUNCA lança (não pode quebrar o caminho de erro da EF).
  </action>
  <acceptance_criteria>
    - `grep -c "bigfive_devolutiva" supabase/functions/_shared/prompt-loader.ts` >= 1
    - `grep -c "sjt_evaluation\|reference_check\|final_recommendation\|interview_questions\|interview_summary" supabase/functions/_shared/prompt-loader.ts` == 0
    - `grep -c "bigfive_devolutiva" scripts/sync-prompts.ts` >= 1
    - `grep -c "emitPromptStubAlert" supabase/functions/_shared/audit-logger.ts` >= 1
    - `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/_shared/__tests__/prompt-loader.test.ts` VERDE
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/_shared/__tests__/prompt-loader.test.ts</automated>
  </verify>
  <done>SCHEMA_VERSIONS espelha o enum; sweep-guard adicionado; sync conhece bigfive; alert helper pronto.</done>
</task>

<task type="auto">
  <name>Task 2: Estreitar o catch nas 6 EFs enum-presentes + timeout 60s no transcricao (AI-01 + AI-04)</name>
  <files>supabase/functions/analise-candidato-individual/index.ts, supabase/functions/comparativo-candidatos/index.ts, supabase/functions/avaliar-redacao/index.ts, supabase/functions/avaliar-redacao-cultural/index.ts, supabase/functions/avaliar-transcricao-entrevista/index.ts, supabase/functions/gerar-guia-entrevista/index.ts, supabase/functions/_shared/__tests__/prompt-catch.test.ts</files>
  <read_first>
    - 23-RESEARCH.md tabela de catch por EF (219-227, linhas exatas) + §AI-04 (276-282) + §Onde estreitar (216-227) + Pitfall 6 (356-358)
    - supabase/functions/avaliar-transcricao-entrevista/index.ts:190-226 (catch :196 + call site callAi sem timeoutMs)
    - supabase/functions/gerar-guia-entrevista/index.ts:270-274 (precedente timeoutMs: 60_000)
    - supabase/functions/_shared/prompt-loader.ts:58/76 (SchemaVersionMismatchError/PromptNotConfiguredError exportadas)
  </read_first>
  <action>
    Em CADA uma das 6 EFs, no bloco `try { const loaded = await loadPrompt(...); resolved = resolvedPromptFromLoaded(...) } catch { resolved = { …stub…, prompt_version: "0.0.0" } }`: REMOVER o objeto stub inteiro e estreitar o catch para `catch (e) { if (e instanceof SchemaVersionMismatchError || e instanceof PromptNotConfiguredError) { await emitPromptStubAlert(supabaseAdmin, "<call_type>"); } throw e; }` — deixando o erro propagar para o try/catch externo do handler (que já retorna um 500 estruturado pt-BR). Importar SchemaVersionMismatchError, PromptNotConfiguredError (de _shared/prompt-loader.ts) e emitPromptStubAlert (de _shared/audit-logger.ts) em cada EF. A edição é IDÊNTICA nas 6 EFs — só muda o literal do call_type (ver mapa nas interfaces). Garantir (Pitfall 6) que o throw ocorre ANTES de qualquer escrita de resultado — o 500 não pode vir depois de uma linha de sucesso fabricada.
    APENAS em avaliar-transcricao-entrevista: além de estreitar o catch, adicionar `timeoutMs: parseIntEnv("TRANSCRICAO_TIMEOUT_MS", 60000)` aos args do callAi (AI-04 — espelha gerar-guia:274; importar parseIntEnv de _shared/ai-client.ts).
    Criar supabase/functions/_shared/__tests__/prompt-catch.test.ts (Deno) — como os handlers usam Deno.serve (difícil unit-testar isolado), testar o CONTRATO COMPARTILHADO: (a) `new SchemaVersionMismatchError("x","1","2") instanceof SchemaVersionMismatchError` e carrega `.code`; idem PromptNotConfiguredError; (b) emitPromptStubAlert com um mock supabase que espiona insert → grava exatamente 1 row em recruiter_alerts com threshold_violated='ai_prompt_stub_fired'. A remoção do stub por-EF é validada por grep (acceptance).
  </action>
  <acceptance_criteria>
    - `grep -rn '"0.0.0"' supabase/functions/analise-candidato-individual/index.ts supabase/functions/comparativo-candidatos/index.ts supabase/functions/avaliar-redacao/index.ts supabase/functions/avaliar-redacao-cultural/index.ts supabase/functions/avaliar-transcricao-entrevista/index.ts supabase/functions/gerar-guia-entrevista/index.ts | grep -v '//' | wc -l` == 0
    - `grep -c "emitPromptStubAlert" supabase/functions/avaliar-transcricao-entrevista/index.ts` >= 1
    - `grep -c "TRANSCRICAO_TIMEOUT_MS" supabase/functions/avaliar-transcricao-entrevista/index.ts` >= 1
    - `deno test … prompt-catch.test.ts` VERDE (instanceof + emitPromptStubAlert grava 1 row)
    - corpus Deno completo VERDE
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions</automated>
  </verify>
  <done>6 EFs falham alto + alarmam; transcricao recebe 60s; nenhum stub silencioso 0.0.0.</done>
</task>

<task type="auto">
  <name>Task 3: gerar-devolutiva-bigfive — catch estreitado + percentil fora do prompt (AI-01 + UX-07)</name>
  <files>supabase/functions/gerar-devolutiva-bigfive/index.ts</files>
  <read_first>
    - supabase/functions/gerar-devolutiva-bigfive/index.ts:565-620 (catch :573 stub "1.0.0"; callAiAdapter userBlock :595-598 com `## PERCENTIL\n${percentil}` :597), :90-103 (bandOf)
    - 23-RESEARCH.md §bigfive exceção profunda (203-214), §UX-07 tabela (371 — EF injeta percentil no prompt), Pitfall 5 (351-354 — Big Five NÃO é avaliativo → bandas NEUTRAS)
  </read_first>
  <action>
    Estreitar o catch em :573 — remover o objeto stub (prompt_version "1.0.0"); trocar por `catch (e) { if (e instanceof SchemaVersionMismatchError || e instanceof PromptNotConfiguredError) { await emitPromptStubAlert(supabaseAdmin, "bigfive_devolutiva"); } throw e; }`. Adicionar um comentário deixando explícito: até o Plan 23-05 aplicar o enum+seed, esta EF 500a por design (alarme honesto — bigfive_devolutiva não é valor válido do enum llm_call_type); uma vez live, loadPrompt resolve o prompt real.
    UX-07: extrair a construção do userBlock (:595-598) para uma função pura EXPORTADA `buildDevolutivaUserBlock(dimArgs: { dim_label: string; banda: Banda; rawInput: string })` que monta o bloco SEM o `## PERCENTIL\n${percentil}` — passar APENAS a banda qualitativa neutra (label pt-BR das 5 bandas: muito_baixo→"Muito baixo" … muito_alto→"Muito alto"). O dimArgs.percentil pode continuar sendo usado internamente para derivar a banda via bandOf, mas o dígito do percentil NUNCA entra no texto do prompt. Substituir o userBlock inline pela chamada a buildDevolutivaUserBlock. Adicionar um teste Deno (novo bloco em prompt-catch.test.ts OU um arquivo dedicado) que assere: buildDevolutivaUserBlock({dim_label,banda:'muito_alto',rawInput}) contém a label da banda ("Muito alto") e NÃO casa `/PERCENTIL/i` nem um dígito de percentil.
  </action>
  <acceptance_criteria>
    - `grep -c "## PERCENTIL" supabase/functions/gerar-devolutiva-bigfive/index.ts` == 0
    - `grep -c '"1.0.0"' supabase/functions/gerar-devolutiva-bigfive/index.ts | grep -v '//'` — sem stub literal remanescente no catch
    - `grep -c "buildDevolutivaUserBlock" supabase/functions/gerar-devolutiva-bigfive/index.ts` >= 1
    - teste Deno VERDE: buildDevolutivaUserBlock emite banda, não percentil (`/PERCENTIL/i` ausente)
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions</automated>
  </verify>
  <done>devolutiva falha alto pré-enum + alarma; prompt do LLM é banda-only (UX-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH/candidato → EF handler | um call_type órfão fazia a EF rodar um stub genérico persistido como avaliação oficial |
| EF catch → client | um 500 estreitado não pode vazar PII/stack na mensagem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Tampering (integridade da avaliação) | catch {} degradando SchemaVersionMismatch num stub 0.0.0 persistido como avaliação | mitigate | estreitar catch → propagar erro estruturado + emitPromptStubAlert; nunca rodar morto silencioso (Task 2/3) |
| T-23-02-02 | Information Disclosure | 500 estreitado vazando stack/PII da transcrição/redação | mitigate | re-throw p/ o handler externo que já retorna mensagem pt-BR genérica + error_code (Pitfall 6); NÃO logar payload bruto |
| T-23-02-03 | Tampering | prompt injection via transcrição/redação (não regredir) | mitigate | detectPromptInjection + maskPII permanecem dentro do callAi — este plano NÃO os toca (V5 ASVS preservado) |
| T-23-02-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote instalado; imports `npm:` estáticos preservados (23-RESEARCH §Package Legitimacy Audit) |

Nota de escopo: este plano NÃO aperta verify_jwt/autz das EFs (Phase 24) — só realinha prompt/catch.
</threat_model>

<verification>
- Corpus Deno completo verde: `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions`
- grep gates das acceptance (0 stubs "0.0.0" nas 6 EFs; 0 `## PERCENTIL` na devolutiva; órfãs ausentes)
</verification>

<success_criteria>
- SCHEMA_VERSIONS espelha o enum (8 chaves) sem as 5 órfãs; sweep-test guarda a regressão
- 7 EFs com catch estreitado (re-throw + emitPromptStubAlert); avaliar-transcricao com timeoutMs 60s
- devolutiva Big Five não injeta percentil cru no prompt; sync-prompts conhece bigfive_devolutiva; corpus Deno verde
</success_criteria>

<output>
Create `.planning/phases/23-ressurrei-o-da-stack-de-ia/23-02-SUMMARY.md` when done
</output>
