---
phase: 25-corre-o-do-funil-lado-rh-enums-colunas-contratos
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/pages/CandidatosRHPage.tsx
  - src/features/hub-candidato/components/HubCandidatoRH.tsx
  - src/features/hub-candidato/components/__tests__/hubNotFound.test.tsx
autonomous: true
requirements: [UX-03]

must_haves:
  truths:
    - "RH 'ver perfil' navigates with candidatura.id (the value /rh/candidatos/:id expects) — not candidato.id (UX-03)"
    - "When the hub id does not resolve, the hub renders an explicit in-shell 'Candidatura não encontrada' state with a 'Voltar aos candidatos' accent back-link — no silent degrade to a generic 'Candidato' / '—' header (UX-03)"
  artifacts:
    - path: "src/components/pages/CandidatosRHPage.tsx"
      provides: "handleVerPerfil passes candidatura.id at both call sites"
      contains: "candidatura.id"
    - path: "src/features/hub-candidato/components/HubCandidatoRH.tsx"
      provides: "in-shell GlassCard not-found state on unresolved candidaturaId"
      contains: "Candidatura não encontrada"
    - path: "src/features/hub-candidato/components/__tests__/hubNotFound.test.tsx"
      provides: "RTL proof the not-found renders on null contexto (not the generic header)"
      exports: []
  key_links:
    - from: "CandidatosRHPage.handleVerPerfil"
      to: "/rh/candidatos/:id (candidaturaId)"
      via: "candidatura.id navigate"
      pattern: "navigate\\(`/rh/candidatos/"
    - from: "HubCandidatoRH not-found"
      to: "/rh/candidatos"
      via: "GlassButton accent back-link"
      pattern: "Voltar aos candidatos"
---

<objective>
Fix the hub nav param (pass `candidatura.id`, not `candidato.id`, to `/rh/candidatos/:id` which `HubCandidatoRH` reads AS a candidaturaId) and add an explicit in-shell not-found state when the id does not resolve — replacing today's silent degrade to a generic `"Candidato" / "—"` header.

Purpose: The route param is a candidaturaId, but `CandidatosRHPage.handleVerPerfil` forwards `candidato.id`, so the hub loads the wrong (or no) context and degrades silently. UX-03.
Output: The nav-param fix (both list call sites) + a scoped `GlassCard` not-found state (persona shell preserved — NOT the global NotFoundPage) + an RTL test.
Note: the Kanban `onViewPerfil` forward is fixed in 25-02 (same UX-03) — this plan owns the list call sites + the hub 404; the two are file-disjoint and consistent (both pass candidatura.id).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-RESEARCH.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-PATTERNS.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-UI-SPEC.md

<interfaces>
<!-- The route param /rh/candidatos/:id IS a candidaturaId (HubCandidatoRH L78-79 reads it as candidaturaId). -->
CandidatosRHPage.handleVerPerfil L252-254: navigate(`/rh/candidatos/${candidatoId}`) — called with candidato?.id at L409/L774 (where candidato = candidatura.candidato); also forwarded to <KanbanBoard onViewPerfil=…/> L963 (Kanban call fixed in 25-02)
HubCandidatoRH L83-84: useEntrevistaContexto(candidaturaId) exposes isError + data==null; silent degrade at L104-105 (nomeCandidato ?? 'Candidato'; etapaLabel ? … : '—')
Analogs (in-shell glass not-found): NotFoundPage.tsx L61-97 (GlassCard variant dark + heading + body + single accent GlassButton back-link L88-95); AvaliacaoContainer.WrongEtapaState L226-252 (in-shell within layout). USE an in-shell state within RHLayout, NOT the global NotFoundPage (UI-SPEC §3).
RTL analog: src/features/hub-candidato/components/__tests__/hubEmptyState.test.tsx
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CandidatosRHPage nav — pass candidatura.id at both call sites</name>
  <files>src/components/pages/CandidatosRHPage.tsx</files>
  <read_first>
    - src/components/pages/CandidatosRHPage.tsx — handleVerPerfil L252-254, call sites L409 + L774 (candidato = candidatura.candidato), the <KanbanBoard onViewPerfil={handleVerPerfil}/> forward L963
    - src/features/hub-candidato/components/HubCandidatoRH.tsx L78-79 (proves :id IS candidaturaId)
    - 25-RESEARCH.md §4a; 25-PATTERNS.md "CandidatosRHPage.tsx"
  </read_first>
  <action>
    Rename the handleVerPerfil param to candidaturaId and change both list call sites (L409, L774) to pass `candidatura.id` (not `candidato?.id`). The onViewPerfil prop signature stays `(id: string) => void` (Kanban's own call is fixed in 25-02). Confirm the navigate target resolves in HubCandidatoRH as the candidaturaId it expects.
  </action>
  <acceptance_criteria>
    - `grep -q "candidatura.id" src/components/pages/CandidatosRHPage.tsx` succeeds AND the handleVerPerfil call sites no longer pass `candidato?.id`
    - `npm run build` succeeds; tsc count not increased
    - VALIDATION: UX-03 (nav passes candidatura.id)
  </acceptance_criteria>
  <verify>
    <automated>bash -c "grep -q 'candidatura.id' src/components/pages/CandidatosRHPage.tsx && npm run build >/dev/null 2>&1 && echo NAV_OK"</automated>
  </verify>
  <done>Both list call sites navigate with candidatura.id; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HubCandidatoRH in-shell not-found state + RTL test</name>
  <files>src/features/hub-candidato/components/HubCandidatoRH.tsx, src/features/hub-candidato/components/__tests__/hubNotFound.test.tsx</files>
  <read_first>
    - src/features/hub-candidato/components/HubCandidatoRH.tsx — useEntrevistaContexto L83-84 (isError/data==null), silent degrade L104-105, the RHLayout shell wrapping
    - src/components/pages/NotFoundPage.tsx L61-97 (glass composition + accent back-link L88-95 idiom to copy)
    - src/features/avaliacao/components/AvaliacaoContainer.tsx WrongEtapaState L226-252 (in-shell centered state within layout)
    - 25-UI-SPEC.md §3 (copy + accent GlassButton "Voltar aos candidatos" → /rh/candidatos, min-h-11, ArrowLeft aria-hidden); RTL analog hubEmptyState.test.tsx; 25-PATTERNS.md "HubCandidatoRH.tsx"
  </read_first>
  <behavior>
    - after the contexto query settles with no row (!loading && (isError || !contexto)), the hub renders a centered GlassCard inside RHLayout with heading "Candidatura não encontrada", the UI-SPEC §3 body copy, and a single accent GlassButton "Voltar aos candidatos" → /rh/candidatos
    - it does NOT render the generic "Candidato"/"—" header for an unresolved id
    - a resolving id still renders the normal hub (no regression)
  </behavior>
  <action>
    In HubCandidatoRH.tsx, when the contexto query resolves to no row for the candidaturaId (after load: `!loadingContexto && (errorContexto || !contexto)`), render the in-shell not-found GlassCard (UI-SPEC §3 copy: heading 20px/600 "Candidatura não encontrada"; body 16px/400 "Não encontramos essa candidatura. Ela pode ter sido removida ou o link está incorreto."; accent GlassButton with leading aria-hidden ArrowLeft, label "Voltar aos candidatos" → /rh/candidatos, min-h-11) — mirror the NotFoundPage back-link idiom but keep the RH shell. Do NOT use the global NotFoundPage. Write hubNotFound.test.tsx (RTL, mock useEntrevistaContexto to return null after load) asserting the not-found heading + back-link render and the generic header does not; RED first.
  </action>
  <acceptance_criteria>
    - `npm run test:run -- hubNotFound` passes
    - `grep -q "Candidatura não encontrada" src/features/hub-candidato/components/HubCandidatoRH.tsx` AND `grep -q "Voltar aos candidatos" src/features/hub-candidato/components/HubCandidatoRH.tsx` succeed
    - `npm run build` succeeds
    - VALIDATION: UX-03 (hub renders not-found on unresolved id)
  </acceptance_criteria>
  <verify>
    <automated>bash -c "npm run test:run -- hubNotFound >/dev/null 2>&1 && grep -q 'Candidatura não encontrada' src/features/hub-candidato/components/HubCandidatoRH.tsx && grep -q 'Voltar aos candidatos' src/features/hub-candidato/components/HubCandidatoRH.tsx && echo HUB404_OK"</automated>
  </verify>
  <done>Hub shows an explicit in-shell not-found + back-link on an unresolved id; test green; no silent degrade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route param → hub data load | a wrong id silently loads/degrades; not a privilege boundary (RH-only route under RoleGuard), but a correctness + information-integrity boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05-01 | (correctness) | wrong nav param loads the wrong/no candidatura | mitigate | pass candidatura.id at every call site (list here + Kanban in 25-02) |
| T-25-05-02 | Information Disclosure | not-found copy leaks candidate PII | mitigate | not-found copy is generic ("Não encontramos essa candidatura") — no name/email/id echoed |
| T-25-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | zero package installs this plan |
</threat_model>

<verification>
- `npm run test:run` green (hubNotFound); `npm run build` green.
- Both UX-03 halves consistent across 25-02 (Kanban) + 25-05 (list + 404).
- Visual confirmation of the 404 + nav is the deferred HUMAN-UAT.
</verification>

<success_criteria>
- Hub nav uses candidatura.id + an explicit in-shell not-found state (UX-03).
</success_criteria>

<output>
Create `.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-05-SUMMARY.md` when done.
</output>
