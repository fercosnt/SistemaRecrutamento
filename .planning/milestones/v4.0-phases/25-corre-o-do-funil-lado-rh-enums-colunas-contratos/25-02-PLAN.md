---
phase: 25-corre-o-do-funil-lado-rh-enums-colunas-contratos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/vagas/types/vagasTypes.ts
  - src/features/vagas/services/candidaturasService.ts
  - src/features/vagas/hooks/useCandidaturas.ts
  - src/components/KanbanBoard.tsx
  - src/components/modals/UpdateStatusModal.tsx
  - src/components/__tests__/KanbanBoard.test.tsx
autonomous: true
requirements: [FUNIL-03, FUNIL-06, FUNIL-02, UX-03]

must_haves:
  truths:
    - "The RH Kanban renders the 6 real etapa_processo stages and drag→drop moves a card via the M2 write-path (updateCandidaturaEtapa / avancar_etapa), not the raw auto-advance (FUNIL-03)"
    - "No live code selects/writes a dead M1 etapa value (bigfive/disc/raven/cultura/avaliacao_final) — getProximaEtapa auto-advance is deleted (FUNIL-06)"
    - "The UpdateStatusModal reject action routes through registrar_decisao with a mandatory ≥50-char justificativa — it can no longer status-only reject without a trail (FUNIL-02, complements D-25-01 guard)"
    - "Kanban 'ver perfil' forwards candidatura.id (not candidato.id) to /rh/candidatos/:id (UX-03)"
  artifacts:
    - path: "src/features/vagas/types/vagasTypes.ts"
      provides: "EtapaProcesso re-aliased to Database Enums.etapa_processo; dead value maps deleted"
      contains: "Database['public']['Enums']['etapa_processo']"
    - path: "src/components/KanbanBoard.tsx"
      provides: "6 real stage columns from ETAPA_M2_LABELS; drag→updateCandidaturaEtapa; terminal pills"
      contains: "avaliacao_assincrona"
    - path: "src/components/__tests__/KanbanBoard.test.tsx"
      provides: "regression net: 6 real columns, no M1 value, drag routes to the M2 mutation"
      exports: []
  key_links:
    - from: "KanbanBoard.handleDrop"
      to: "updateCandidaturaEtapa (M2)"
      via: "useUpdateCandidaturaEtapa mutation"
      pattern: "updateCandidaturaEtapa"
    - from: "UpdateStatusModal reject branch"
      to: "registrar_decisao RPC"
      via: "useRegistrarDecisao"
      pattern: "useRegistrarDecisao|registrarDecisao"
    - from: "KanbanBoard.onViewPerfil"
      to: "/rh/candidatos/:id (candidaturaId)"
      via: "candidatura.id forward"
      pattern: "candidatura\\.id"
---

<objective>
Cut the RH funnel over from the dead M1 `EtapaProcesso` 10-value enum to the live 8-value `etapa_processo`, rewire the Kanban onto the real 6-stage pipeline + the server-authoritative M2 write-path, delete the `getProximaEtapa` auto-advance (the A16 22P02 crash vector), and reroute the UpdateStatusModal reject through `registrar_decisao` so no rejection escapes the audit trail (FUNIL-02 frontend half; the DB guard from 25-01 is the backstop).

Purpose: The frontend never caught up to the M2 enum cutover — selecting a dead value throws Postgres 22P02, and the raw status-only reject bypasses the audit trail (A9). This plan makes RH operate over enums/columns that exist.
Output: Re-aliased types, a deleted auto-advance, a rewired Kanban with terminal pills, a reject modal that requires a justificativa, and a KanbanBoard regression test. Clears ≈ −6 tsc errors (candidaturasService −5, KanbanBoard −1) — re-pinned in 25-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-RESEARCH.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-PATTERNS.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-UI-SPEC.md

<interfaces>
<!-- Live single-source (the correct M2 path to rewire ONTO — do NOT hardcode a 2nd copy): src/features/triagem/services/triagemService.ts -->
EtapaFunilM2 (triagemService L302-310): 8-value hand-written mirror of DB etapa_processo
ETAPA_M2_LABELS (L325-334): Record<EtapaFunilM2,string> — the ONLY label source for Kanban columns
ETAPA_M2_OPTIONS (L337-348): includes both terminals (aprovado/rejeitado) for the panel filter
updateCandidaturaEtapa(candidaturaId, novaEtapa) (L350-378): the server-authoritative write; sets status='rejeitado' when novaEtapa==='rejeitado'; fires avancar_etapa (validates + audits)

Reuse (already exist — do NOT re-create): src/features/decisao/services/decisaoService.ts registrarDecisao({candidaturaId, decisao:'rejeitado', justificativa}) → .rpc('registrar_decisao', {p_candidatura_id, p_decisao, p_justificativa}); hook src/features/decisao/hooks/useRegistrarDecisao.ts
Kanban currently uses useUpdateCandidaturaStatus (src/features/vagas/hooks/useCandidaturas.ts L334) → the raw auto-advance path to REPLACE for the etapa-move.
UI-SPEC §1 binds the 6-column set/order/labels; terminals aprovado/rejeitado are NOT drop columns (render a card pill).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-alias EtapaProcesso + delete dead value maps + delete auto-advance</name>
  <files>src/features/vagas/types/vagasTypes.ts, src/features/vagas/services/candidaturasService.ts</files>
  <read_first>
    - src/features/vagas/types/vagasTypes.ts — EtapaProcesso union L200-210 (M1 10-value); dead maps ETAPA_PROCESSO_LABELS L604-615, ETAPA_PROGRESS L635-646, ETAPAS_SEQUENCIA L652-663, getProximaEtapa L670-680, ETAPA_TO_KANBAN L732+ (L734 big_five is a live tsc error); the 5 interface fields typed EtapaProcesso at L265/351/409/439/470
    - src/features/vagas/services/candidaturasService.ts — updateCandidaturaStatus L390-525: getProximaEtapa import L35, the auto-advance block L437-453 (getProximaEtapa('triagem')→'bigfive' = 22P02), raw update L464-467, SEC-11 no-log discipline L470-477
    - src/features/triagem/services/triagemService.ts EtapaFunilM2 L302-310 (Open Q4: collapse into the same DB-enum alias to kill the duplicate)
    - 25-RESEARCH.md §1e (consumer grep table), Pitfall 6; 25-PATTERNS.md "vagasTypes.ts" + "candidaturasService.ts"
  </read_first>
  <action>
    In vagasTypes.ts: re-alias `export type EtapaProcesso = Database['public']['Enums']['etapa_processo']` (Pitfall 6 — keep the NAME so the 5 interface fields self-correct; do NOT delete the type). DELETE the dead value maps ETAPAS_SEQUENCIA, getProximaEtapa, ETAPA_PROCESSO_LABELS, ETAPA_PROGRESS, ETAPA_TO_KANBAN (none have consumers outside this file per the RESEARCH grep table). Do NOT re-create a label map — ETAPA_M2_LABELS is the single source. Consider collapsing EtapaFunilM2 into the same alias (Open Q4) only if it is a clean drop-in.
    In candidaturasService.ts: DELETE the getProximaEtapa auto-advance block (L437-453) and its import (L35). The raw status-only path stays for non-etapa status changes, but the etapa-move no longer auto-advances via a dead map. Preserve the SEC-11 discipline (never log updateData). Confirm the file no longer imports getProximaEtapa/ETAPAS_SEQUENCIA.
  </action>
  <acceptance_criteria>
    - `grep -q "Database\['public'\]\['Enums'\]\['etapa_processo'\]" src/features/vagas/types/vagasTypes.ts` succeeds
    - `grep -c "getProximaEtapa" src/features/vagas/types/vagasTypes.ts src/features/vagas/services/candidaturasService.ts` == 0 (symbol deleted + unimported)
    - No dead M1 value literal remains in these two files: `grep -nE "'(bigfive|disc|raven|cultura|avaliacao_final)'" src/features/vagas/types/vagasTypes.ts src/features/vagas/services/candidaturasService.ts` returns nothing
    - `npx tsc --noEmit | grep -c "error TS"` is STRICTLY LESS than 128 (candidaturasService −5 clears)
  </acceptance_criteria>
  <verify>
    <automated>bash -c "grep -q \"Enums'\\]\\['etapa_processo'\\]\" src/features/vagas/types/vagasTypes.ts && [ \$(grep -c getProximaEtapa src/features/vagas/types/vagasTypes.ts src/features/vagas/services/candidaturasService.ts | awk -F: '{s+=\$2} END{print s}') -eq 0 ] && [ \$(npx tsc --noEmit 2>/dev/null | grep -c 'error TS') -lt 128 ] && echo CUTOVER_OK"</automated>
  </verify>
  <done>EtapaProcesso aliases the DB enum; dead value maps + auto-advance deleted; tsc count dropped below 128; no dead M1 literal in either file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Kanban rewire — 6 real columns + M2 drag + terminal pills + nav fix</name>
  <files>src/components/KanbanBoard.tsx, src/features/vagas/hooks/useCandidaturas.ts, src/components/__tests__/KanbanBoard.test.tsx</files>
  <read_first>
    - src/components/KanbanBoard.tsx — KANBAN_COLUMNS L44-57 (4 dead cols), groupedCandidaturas L283-308 (console.warn→triagem fallback L301-303), handleDrop L311-358 (→ useUpdateCandidaturaStatus), grid repeat(7) L366, onViewPerfil(candidato?.id) L176, KanbanColumn L192-273 (reuse), getScoreColor L62-68
    - src/features/triagem/services/triagemService.ts — ETAPA_M2_LABELS L325-334, ETAPA_M2_OPTIONS L337-348, updateCandidaturaEtapa L350-378
    - src/features/vagas/hooks/useCandidaturas.ts — useUpdateCandidaturaStatus L334 (the pattern to mirror for a new useUpdateCandidaturaEtapa mutation: queryClient invalidation of the candidaturas keys)
    - 25-UI-SPEC.md §1 (binding column set/order/glyph/hue table; terminals as card pills; grid repeat(6)); 25-PATTERNS.md "KanbanBoard.tsx"; existing RTL analog src/features/triagem/**/__tests__/TriagemTable.test.tsx
  </read_first>
  <behavior>
    - KANBAN_COLUMNS resolves to exactly the 6 working stages in funnel order: inscricao, triagem, avaliacao_assincrona, entrevista_online, entrevista_presencial, decisao_final — with labels === ETAPA_M2_LABELS[value] (no hardcoded 2nd copy)
    - groupedCandidaturas keys on the real 8 values; no console.warn fallback path executes for any live etapa
    - a drop onto a working column calls the M2 mutation (updateCandidaturaEtapa) with (candidaturaId, targetEtapa) — NOT useUpdateCandidaturaStatus
    - a candidatura in aprovado/rejeitado renders a terminal pill on the card and is NOT a drop column
    - onViewPerfil is invoked with candidatura.id
  </behavior>
  <action>
    Add `useUpdateCandidaturaEtapa` to useCandidaturas.ts (mirror useUpdateCandidaturaStatus: wrap triagemService.updateCandidaturaEtapa, invalidate the same candidaturas query keys on success). In KanbanBoard.tsx: replace KANBAN_COLUMNS with the 6 UI-SPEC §1 stages (labels from ETAPA_M2_LABELS; glyph aria-hidden; per-column gradient hue from the §1 table — Claude's discretion on exact glyph/hue). Rebuild groupedCandidaturas on the real 8 values; delete the console.warn→triagem fallback. Route handleDrop through useUpdateCandidaturaEtapa (fires avancar_etapa). Render terminals (aprovado/rejeitado) as a card pill using the existing status color language — NO 'Rejeitado' drop column (this also closes the reject vector at the UI). Adjust grid to repeat(6) (keep 2-col mobile / 4-col tablet). Change onViewPerfil to forward candidatura.id (UX-03). Reuse KanbanColumn/getScoreColor/Glass composition as-is. Write KanbanBoard.test.tsx (RTL, mock the hook) asserting the behaviors above; RED first, then GREEN.
  </action>
  <acceptance_criteria>
    - `npm run test:run -- KanbanBoard` passes
    - `grep -q "avaliacao_assincrona" src/components/KanbanBoard.tsx` succeeds AND `grep -nE "'(bigfive|disc|raven|cultura)'" src/components/KanbanBoard.tsx` returns nothing (no dead column)
    - `grep -q "updateCandidaturaEtapa" src/components/KanbanBoard.tsx src/features/vagas/hooks/useCandidaturas.ts` succeeds; `grep -c "console.warn" src/components/KanbanBoard.tsx` == 0
    - `grep -q "repeat(6" src/components/KanbanBoard.tsx` succeeds (grid corrected)
    - `npm run build` succeeds; tsc count not increased
    - VALIDATION invariant: FUNIL-03 (Kanban groups all real values / no M1 value)
  </acceptance_criteria>
  <verify>
    <automated>bash -c "npm run test:run -- KanbanBoard >/dev/null 2>&1 && grep -q avaliacao_assincrona src/components/KanbanBoard.tsx && ! grep -nE \"'(bigfive|disc|raven|cultura)'\" src/components/KanbanBoard.tsx && grep -q updateCandidaturaEtapa src/components/KanbanBoard.tsx && echo KANBAN_OK"</automated>
  </verify>
  <done>Kanban renders 6 real stages, drags via the M2 audited path, shows terminals as pills, forwards candidatura.id; regression test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reroute UpdateStatusModal reject through registrar_decisao</name>
  <files>src/components/modals/UpdateStatusModal.tsx</files>
  <read_first>
    - src/components/modals/UpdateStatusModal.tsx — useUpdateCandidaturaStatus wiring L34/L86, precisaMotivoRejeicao L116, reject submit L141, status labels/transitions L41-59
    - src/features/decisao/services/decisaoService.ts registrarDecisao + src/features/decisao/hooks/useRegistrarDecisao.ts (reuse; enforces justificativa via the DB RPC ≥50 + the schema mirror decisaoSchema.ts)
    - src/features/decisao/components/RegistrarDecisaoForm.tsx — the char-count / disabled-until-valid justificativa UX to mirror
    - 25-UI-SPEC.md §Interaction Contracts (reject disabled until justificativa entered; copy states rejection is recorded for audit RNF-07a/LGPD-02), §Copywriting "Rejeitar candidato"
    - 25-RESEARCH.md §1c (reroute is required — the guard is only a backstop); Open Q3 (reuse registrar_decisao)
  </read_first>
  <behavior>
    - selecting the reject transition requires a justificativa; the reject button stays disabled until a ≥50-char justificativa is entered
    - confirming reject calls registrar_decisao (via useRegistrarDecisao) with {candidaturaId, decisao:'rejeitado', justificativa} — NOT the raw useUpdateCandidaturaStatus status-only write
    - non-reject status transitions keep their existing useUpdateCandidaturaStatus path unchanged
  </behavior>
  <action>
    In UpdateStatusModal.tsx, route the reject branch (the `precisaMotivoRejeicao`/reject transition) through useRegistrarDecisao instead of useUpdateCandidaturaStatus. Add a justificativa textarea (reuse the RegistrarDecisaoForm char-count + disabled-until-≥50 idiom) and confirmation copy per UI-SPEC (rejection recorded for audit). Leave all non-reject transitions on the existing hook. Add/extend a unit test (mock both hooks) asserting: reject disabled until justificativa valid, reject calls registrar_decisao, a non-reject transition calls the status hook. RED first.
  </action>
  <acceptance_criteria>
    - `grep -q "useRegistrarDecisao\|registrarDecisao" src/components/modals/UpdateStatusModal.tsx` succeeds
    - `npm run test:run -- UpdateStatusModal` passes (reject-requires-justificativa + routes-to-registrar_decisao)
    - `npm run build` succeeds
    - VALIDATION: FUNIL-02 frontend half (complements the 25-01 guard + 25-07 SMOKE A/B)
  </acceptance_criteria>
  <verify>
    <automated>bash -c "grep -qE 'useRegistrarDecisao|registrarDecisao' src/components/modals/UpdateStatusModal.tsx && npm run test:run -- UpdateStatusModal >/dev/null 2>&1 && echo REROUTE_OK"</automated>
  </verify>
  <done>The modal reject requires a justificativa and writes through the audited registrar_decisao path; non-reject transitions unchanged; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH browser → candidaturas write | drag/drop + modal express intent; the audited write MUST be server-side (avancar_etapa / registrar_decisao), not a client status write |
| client reject UX → audit trail | a client-only justificativa check is bypassable — the DB guard (25-01) is the real teeth; this plan removes the UI path that skipped it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | Repudiation | UpdateStatusModal status-only reject (A9 UI path) | mitigate | reroute reject through registrar_decisao (justificativa + audit + sanctioned flag); DB guard backstops. 25-07 SMOKE A/B |
| T-25-02-02 | Tampering | Kanban drag writing a dead/invalid etapa (22P02) or via raw auto-advance | mitigate | drop routes through updateCandidaturaEtapa (avancar_etapa validates + audits); dead value maps deleted |
| T-25-02-03 | Information Disclosure | logging candidate movement/PII (SEC-11) | mitigate | preserve the existing no-log discipline in candidaturasService (never log updateData) |
| T-25-02-04 | Tampering | client bypasses the reject UI to raw-write status | accept (defense-in-depth) | UI removes the vector + no 'Rejeitado' drop column; the DB guard (25-01) is the authoritative control for a raw client write |
| T-25-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | zero package installs this plan |
</threat_model>

<verification>
- `npm run test:run` green (KanbanBoard + UpdateStatusModal suites); `npm run build` green.
- `npx tsc --noEmit | grep -c "error TS"` < 128 (measured — re-pinned in 25-08).
- No dead M1 etapa literal anywhere in the three touched source files.
- Runtime behavior of the reject reroute is confirmed by 25-07 SMOKE A/B (live) + the deferred HUMAN-UAT (Kanban drag visual).
</verification>

<success_criteria>
- Kanban operates on the real 6-stage enum via the audited M2 write-path (FUNIL-03).
- getProximaEtapa auto-advance + dead value maps deleted; no 22P02 vector (FUNIL-06).
- UpdateStatusModal reject requires a justificativa and writes through registrar_decisao (FUNIL-02).
- Kanban nav forwards candidatura.id (UX-03).
</success_criteria>

<output>
Create `.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-02-SUMMARY.md` when done.
</output>
