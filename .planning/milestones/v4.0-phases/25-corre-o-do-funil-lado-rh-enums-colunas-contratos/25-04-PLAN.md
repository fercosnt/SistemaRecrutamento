---
phase: 25-corre-o-do-funil-lado-rh-enums-colunas-contratos
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/testes/testeContract.ts
  - src/lib/testes/__tests__/testeContract.test.ts
  - src/features/avaliacao/components/AvaliacaoContainer.tsx
autonomous: true
requirements: [FUNIL-05]

must_haves:
  truths:
    - "Every cargoTemplate teste id maps deterministically to a container id the AvaliacaoContainer recognizes — no id falls through to the default branch (FUNIL-05)"
    - "work_sample_sjt → both sjt cards; redacao_cultural → redacao; cognitivo → cognitivo; big_five → big_five; triagem/entrevista → filtered out (not candidate-facing)"
    - "A contract test parses every template teste through the shared lib and asserts the emitted container ids ⊆ the container's exported recognized set (regress-guard)"
  artifacts:
    - path: "src/lib/testes/testeContract.ts"
      provides: "canonical template-id + container-id unions, CANDIDATE_FACING set, templateTesteToContainerCards mapping"
      contains: "templateTesteToContainerCards"
    - path: "src/lib/testes/__tests__/testeContract.test.ts"
      provides: "contract test — every cargoTemplate teste maps into the container's recognized set"
      exports: []
    - path: "src/features/avaliacao/components/AvaliacaoContainer.tsx"
      provides: "deriveCards filters candidate-facing + maps through the lib; exports the recognized-id set"
      contains: "templateTesteToContainerCards"
  key_links:
    - from: "AvaliacaoContainer.deriveCards"
      to: "templateTesteToContainerCards (lib)"
      via: "id mapping instead of verbatim t.teste copy"
      pattern: "templateTesteToContainerCards"
    - from: "testeContract.test.ts"
      to: "cargoTemplates.baseTestes + AvaliacaoContainer recognized-id set"
      via: "branch-map assertion (both sides, one test)"
      pattern: "cargoTemplate|CONTAINER_RECOGNIZED"
---

<objective>
Define ONE canonical template↔container test-id contract in a shared lib, rewire `AvaliacaoContainer.deriveCards` to filter candidate-facing tests and map through it (instead of copying `t.teste` verbatim), and add a contract test that parses every `cargoTemplates` teste through the lib and asserts each emitted container id is one the container recognizes.

Purpose: `cargoTemplates` emits `{triagem, work_sample_sjt, redacao_cultural, big_five, cognitivo, entrevista}`; the container only recognizes `{sjt_mc, sjt_caso_aberto, big_five, redacao}` — zero overlap except `big_five`. A real vaga's `work_sample_sjt`/`redacao_cultural`/`cognitivo` fall to the default label + accidental `target='mc'` (redacao routes wrong). This closes FUNIL-05 (the id contract only — actual reachability of cognitivo/SJT-by-cargo is Phase 26).
Output: `src/lib/testes/testeContract.ts` + the contract test (the key regress-guard, per feedback_integration_contract_gap) + the container rewire.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-RESEARCH.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-PATTERNS.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-VALIDATION.md
</context>

<feature>
  <name>Canonical test-id contract (template↔container)</name>
  <files>src/lib/testes/testeContract.ts, src/lib/testes/__tests__/testeContract.test.ts, src/features/avaliacao/components/AvaliacaoContainer.tsx</files>
  <read_first>
    - src/features/config-vaga/templates/cargoTemplates.ts — baseTestes L55-73 (emits triagem/work_sample_sjt/redacao_cultural/big_five/cognitivo/entrevista); getCargoTemplateDefaults deep-copy L227-247 (unrelated)
    - src/features/avaliacao/components/AvaliacaoContainer.tsx — deriveCards L254-270 (copies t.teste verbatim L259-260), testeLabel L56-72 (recognizes sjt_mc/sjt_caso_aberto/big_five|bigfive/redacao), handleOpenTeste L308-322 (redacao→/candidato/redacao/:id L311-313; big_five→target 'bigfive' L316-317; caso→'caso' L318-319; default→'mc')
    - src/features/config-vaga/schemas/testesAplicaveisSchema.ts L43 (TesteAplicavel['teste'] is z.string() today — unconstrained)
    - contract-test analog: src/features/**/__tests__/bigfive-contract.test.ts (Part 1 runtime-replica idiom); 25-RESEARCH.md §2d; 25-PATTERNS.md "testeContract.ts" + "AvaliacaoContainer.tsx" + Shared Patterns "Contract test"
  </read_first>
  <behavior>
    - templateTesteToContainerCards('work_sample_sjt') === ['sjt_mc','sjt_caso_aberto']
    - templateTesteToContainerCards('redacao_cultural') === ['redacao']
    - templateTesteToContainerCards('big_five') === ['big_five']
    - templateTesteToContainerCards('cognitivo') === ['cognitivo']
    - templateTesteToContainerCards('triagem') === [] and templateTesteToContainerCards('entrevista') === [] (not candidate-facing → filtered)
    - CONTRACT: for every entry in every cargoTemplates baseTestes, every id emitted by the lib is a member of AvaliacaoContainer's exported recognized-id set (no default-branch fall-through)
  </behavior>
  <implementation>
    RED: create src/lib/testes/testeContract.ts exporting the canonical template-id union {triagem, work_sample_sjt, redacao_cultural, big_five, cognitivo, entrevista}, the container-id union {sjt_mc, sjt_caso_aberto, big_five, redacao, cognitivo}, a CANDIDATE_FACING set, and `templateTesteToContainerCards(t): ContainerTeste[]` implementing the mapping above. Write testeContract.test.ts asserting the per-id mappings (RED until the lib exists → GREEN).
    Then: rewire AvaliacaoContainer.deriveCards to (a) filter to CANDIDATE_FACING template tests and (b) map each through templateTesteToContainerCards, replacing the verbatim t.teste copy; make testeLabel/handleOpenTeste consume the container-id union (add a cognitivo label + route stub — reachability is Phase 26). Export a `CONTAINER_RECOGNIZED` set (the ids testeLabel/handleOpenTeste handle without hitting default) so the test asserts against the real container, not a replica (feedback_integration_contract_gap — do not replicate both sides in the test). Extend testeContract.test.ts with the CONTRACT block iterating cargoTemplates → lib → assert ⊆ CONTAINER_RECOGNIZED.
    Optionally narrow TesteAplicavel['teste'] from z.string() to the canonical union (Claude's discretion — only if it does not cascade tsc errors).
    REFACTOR: ensure a single source (no duplicate id switch copies across deriveCards/handleOpenTeste/testeLabel — all consume the lib / the union).
  </implementation>
  <verify>
    <automated>bash -c "npm run test:run -- testeContract >/dev/null 2>&1 && grep -q templateTesteToContainerCards src/features/avaliacao/components/AvaliacaoContainer.tsx && npm run test:run -- AvaliacaoContainer >/dev/null 2>&1 && echo CONTRACT_OK"</automated>
  </verify>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored testes_aplicaveis jsonb → container render | ids are read at runtime; a mis-mapped id silently mis-routes a candidate to the wrong avaliação (or renders a non-candidate-facing card) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-01 | Tampering (integrity of routing) | verbatim t.teste copy → default 'mc' target for redacao_cultural/cognitivo | mitigate | shared-lib mapping + filter; contract test asserts no default-branch fall-through |
| T-25-04-02 | (contract rot) | mocks on both sides pass while the real contract breaks | mitigate | test imports the container's exported CONTAINER_RECOGNIZED set (real side), not a replica |
| T-25-04-03 | Information Disclosure | rendering a non-candidate-facing card (triagem/entrevista) to the candidate | mitigate | CANDIDATE_FACING filter drops them (→ []) |
| T-25-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | zero package installs this plan |
</threat_model>

<verification>
- `npm run test:run` green (testeContract + AvaliacaoContainer); the CONTRACT block is the regress-guard (VALIDATION invariant E).
- `npm run build` green; tsc count not increased.
- No stored jsonb is rewritten — mapping is read-time only (Runtime State Inventory: code-only).
</verification>

<success_criteria>
- One canonical lib maps every template teste to recognized container ids; deriveCards consumes it; contract test guards it (FUNIL-05).
- cognitivo carries a label + route stub only (reachability = Phase 26 — do NOT pull it forward).
</success_criteria>

<output>
Create `.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-04-SUMMARY.md` when done.
</output>
