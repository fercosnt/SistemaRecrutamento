---
phase: 25-corre-o-do-funil-lado-rh-enums-colunas-contratos
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RHSidebar.tsx
  - src/components/RHTopBar.tsx
  - src/features/decisao/components/DecisaoFinalPage.tsx
  - src/components/pages/ConfiguracoesPage.tsx
  - src/components/pages/MeuPerfilPage.tsx
autonomous: true
requirements: [UX-06]

must_haves:
  truths:
    - "The hardcoded 12/5 sidebar badges, the no-op RHTopBar global search, and the DecisaoFinalPage no-op avançar/rejeitar buttons are removed with no dangling gap (UX-06)"
    - "/rh/configuracoes (mock user mgmt A14) and RH MeuPerfilPage (stub save/senha/foto A37) show a clear 'not available' empty-state — shell + route + RoleGuard kept, real impl deferred to M5 (UX-06)"
    - "No mock user list / fake counts / stub handlers render on the gated screens (offboarding-LGPD minimum)"
  artifacts:
    - path: "src/components/pages/ConfiguracoesPage.tsx"
      provides: "empty-state replacing the mock user-management content region"
      contains: "Gestão de usuários ainda não disponível"
    - path: "src/components/pages/MeuPerfilPage.tsx"
      provides: "empty-state replacing the stub profile forms"
      contains: "Edição de perfil em breve"
    - path: "src/components/RHSidebar.tsx"
      provides: "badges removed"
      contains: ""
  key_links:
    - from: "ConfiguracoesPage / MeuPerfilPage"
      to: "GlassCard empty-state (M5 deferral)"
      via: "content-region replacement (shell/route/RoleGuard kept)"
      pattern: "ainda não disponível|em breve"
    - from: "DecisaoFinalPage"
      to: "registrar_decisao (real path elsewhere)"
      via: "hide the embedded no-op avançar/rejeitar"
      pattern: "onAvancar|onRejeitar"
---

<objective>
Sweep the dead RH affordances (hardcoded 12/5 sidebar badges, the no-op global search, the DecisaoFinalPage no-op avançar/rejeitar) and gate the two mock screens (`/rh/configuracoes` user-management A14, RH `MeuPerfilPage` A37) behind a clear "not available" empty-state — keeping the RH shell + route + RoleGuard, real feature deferred to M5.

Purpose: These affordances lie about capabilities and, for the mock user list, render fake candidate/user data (offboarding-LGPD minimum fix). UX-06. Removal (not disable) per the locked CONTEXT decision.
Output: Three trivial removals + two empty-state gatings. No new visual language — reuse the glass empty-state rhythm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-RESEARCH.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-PATTERNS.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-UI-SPEC.md

<interfaces>
<!-- Empty-state analog (reuse — no new visual language): AsyncState.EstadoVazio (centered py-12, heading text-base/semibold, body text-sm text-white/70) + AvaliacaoContainer glass empty block (muted lucide icon, p-12 text-center). Keep RH shell/route/RoleGuard. NO CTA (feature unavailable). Do NOT DEV-gate, do NOT delete routes. -->
RHSidebar.tsx: badge:12 L74, badge:5 L80 (render L224-227)
RHTopBar.tsx: handleSearch L31-35 (no-op), search box L84-95
DecisaoFinalPage.tsx: <ComparativoScreen onAvancar={()=>{}} onRejeitar={()=>{}}/> L194-207 (no-op buttons L197-198) — comparison content stays; the real decision path is registrar_decisao elsewhere on the page
ConfiguracoesPage.tsx (A14): mock user list L169-207, stub handlers L461-494 (toggle/excluir/reset), saves L417/436/473, dead M1 webhook names L153-157; route routes.tsx L407-414 (RoleGuard administrador)
MeuPerfilPage.tsx (A37): handleSalvarDados L38-40, handleAlterarSenha L42-49, handleAlterarFoto L51-53 (all TODO(M5)); route routes.tsx L399-406 (role rh/administrador)
UI-SPEC §2 copy table + §4 dead-affordance table + No-visual-regression rule.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove dead affordances (sidebar badges, topbar search, DecisaoFinalPage no-op buttons)</name>
  <files>src/components/RHSidebar.tsx, src/components/RHTopBar.tsx, src/features/decisao/components/DecisaoFinalPage.tsx</files>
  <read_first>
    - src/components/RHSidebar.tsx badge:12 L74 / badge:5 L80 (render L224-227)
    - src/components/RHTopBar.tsx handleSearch L31-35 + search box L84-95
    - src/features/decisao/components/DecisaoFinalPage.tsx embedded ComparativoScreen L194-207 (onAvancar/onRejeitar no-op L197-198)
    - 25-UI-SPEC.md §4 (remove not disable; No-visual-regression rule); 25-PATTERNS.md "Dead-affordance removals"
  </read_first>
  <action>
    Remove the badge element from the two RHSidebar items (never render "0"; the row reflows). Remove the RHTopBar search input + handleSearch handler (TopBar reflows around the remaining real controls). In DecisaoFinalPage, hide the two no-op avançar/rejeitar buttons of the embedded ComparativoScreen (do not render them; keep the comparison content — the real decision path is registrar_decisao elsewhere). After each removal: no empty container, dangling border, or orphaned label remains.
  </action>
  <acceptance_criteria>
    - `grep -c "badge: 12\|badge:12\|badge: 5\|badge:5" src/components/RHSidebar.tsx` == 0
    - `grep -c "handleSearch" src/components/RHTopBar.tsx` == 0
    - No `onAvancar={() => {}}` / `onRejeitar={() => {}}` no-op remains: `grep -cE "onAvancar=\{\(\) *=> *\{\}\}|onRejeitar=\{\(\) *=> *\{\}\}" src/features/decisao/components/DecisaoFinalPage.tsx` == 0
    - `npm run build` succeeds; existing tests green
  </acceptance_criteria>
  <verify>
    <automated>bash -c "[ \$(grep -cE 'badge: ?12|badge: ?5' src/components/RHSidebar.tsx) -eq 0 ] && [ \$(grep -c handleSearch src/components/RHTopBar.tsx) -eq 0 ] && [ \$(grep -cE 'onAvancar=\\{\\(\\) *=> *\\{\\}\\}|onRejeitar=\\{\\(\\) *=> *\\{\\}\\}' src/features/decisao/components/DecisaoFinalPage.tsx) -eq 0 ] && npm run build >/dev/null 2>&1 && echo SWEEP_OK"</automated>
  </verify>
  <done>Badges, no-op search, and no-op decision buttons removed; layouts reflow; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Gate /rh/configuracoes behind a 'not available' empty-state</name>
  <files>src/components/pages/ConfiguracoesPage.tsx</files>
  <read_first>
    - src/components/pages/ConfiguracoesPage.tsx — mock user list L169-207, stub handlers L461-494 + L417/436/473, dead M1 webhook names L153-157; the RH shell/header wrapping + route dependency (routes.tsx L407-414 RoleGuard administrador — do NOT touch the route)
    - src/components/ui/AsyncState.tsx EstadoVazio L104-110 (empty-state typography); AvaliacaoContainer glass empty block L155-170
    - 25-UI-SPEC.md §2 (copy table: icon Users/Settings, heading "Gestão de usuários ainda não disponível", body); 25-PATTERNS.md "ConfiguracoesPage.tsx + MeuPerfilPage.tsx"
  </read_first>
  <action>
    Replace the ENTIRE mock content region (fake user list + stub toggle/excluir/reset/save handlers + dead webhook block) with a single centered GlassCard empty-state (lucide Users/Settings icon, heading 20px/600 "Gestão de usuários ainda não disponível", body per UI-SPEC §2, py-12, NO CTA). KEEP the RH persona shell + page header/title + the route + RoleGuard. Do NOT DEV-gate, do NOT delete the route. Remove the now-unused mock state/handlers/imports so no fake user data or stub handler survives (offboarding-LGPD minimum).
  </action>
  <acceptance_criteria>
    - `grep -q "Gestão de usuários ainda não disponível" src/components/pages/ConfiguracoesPage.tsx` succeeds
    - No mock user-management handler survives: `grep -cE "handleToggle|handleExcluir|handleReset" src/components/pages/ConfiguracoesPage.tsx` == 0 (or none render)
    - The route is untouched: `grep -q "configuracoes" src/router/routes.tsx` still succeeds (sanity — file not edited here)
    - `npm run build` succeeds; tsc count not increased
  </acceptance_criteria>
  <verify>
    <automated>bash -c "grep -q 'Gestão de usuários ainda não disponível' src/components/pages/ConfiguracoesPage.tsx && npm run build >/dev/null 2>&1 && echo CONFIG_GATED_OK"</automated>
  </verify>
  <done>/rh/configuracoes shows a clear empty-state; mock user data + stub handlers gone; shell/route/RoleGuard kept; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Gate RH MeuPerfilPage behind an 'em breve' empty-state</name>
  <files>src/components/pages/MeuPerfilPage.tsx</files>
  <read_first>
    - src/components/pages/MeuPerfilPage.tsx — stub handlers handleSalvarDados L38-40, handleAlterarSenha L42-49, handleAlterarFoto L51-53 (all TODO(M5)); RH shell/header wrapping + route (routes.tsx L399-406 role rh/administrador — do NOT touch)
    - src/components/ui/AsyncState.tsx EstadoVazio L104-110; 25-UI-SPEC.md §2 (icon UserRound, heading "Edição de perfil em breve", body); 25-PATTERNS.md "ConfiguracoesPage.tsx + MeuPerfilPage.tsx"
  </read_first>
  <action>
    Replace the stub save/senha/foto forms with a single centered GlassCard empty-state (lucide UserRound icon, heading 20px/600 "Edição de perfil em breve", body per UI-SPEC §2, py-12, NO CTA). KEEP the RH shell + header + route + RoleGuard. Remove the now-unused stub handlers so no `console.log`/stub persists (SEC-11 already stripped logs; ensure none reappear).
  </action>
  <acceptance_criteria>
    - `grep -q "Edição de perfil em breve" src/components/pages/MeuPerfilPage.tsx` succeeds
    - `grep -cE "handleSalvarDados|handleAlterarSenha|handleAlterarFoto" src/components/pages/MeuPerfilPage.tsx` == 0 (stubs removed)
    - `npm run build` succeeds; tsc count not increased
    - VALIDATION: UX-06 (mock screens gated)
  </acceptance_criteria>
  <verify>
    <automated>bash -c "grep -q 'Edição de perfil em breve' src/components/pages/MeuPerfilPage.tsx && [ \$(grep -cE 'handleSalvarDados|handleAlterarSenha|handleAlterarFoto' src/components/pages/MeuPerfilPage.tsx) -eq 0 ] && npm run build >/dev/null 2>&1 && echo PERFIL_GATED_OK"</automated>
  </verify>
  <done>RH MeuPerfilPage shows an 'em breve' empty-state; stub handlers gone; shell/route kept; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gated mock screens → RH render | the mock user list rendered fake user/candidate identifiers; gating removes that PII-shaped render (offboarding-LGPD minimum) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06-01 | Information Disclosure | mock user list renders fake/real identifiers; stub handlers imply live user-mgmt | mitigate | replace content region with empty-state; remove mock state/handlers; keep RoleGuard (A14/A37 real impl → M5) |
| T-25-06-02 | Spoofing (capability lie) | no-op affordances imply actions that do nothing (search, badges, avançar/rejeitar) | mitigate | remove them (not disable); no dangling placeholder |
| T-25-06-03 | (availability) | deleting a route breaks RH nav / RoleGuard coverage | accept/avoid | routes + RoleGuard kept intact; only the content region changes |
| T-25-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | zero package installs this plan |
</threat_model>

<verification>
- `npm run build` + `npm run test:run` green.
- No mock user data, fake count, or stub handler renders on the gated screens.
- Routes + RoleGuard untouched (grep sanity).
- Visual confirmation of the empty-states is the deferred HUMAN-UAT.
</verification>

<success_criteria>
- Dead affordances removed; two mock screens gated with clear empty-states, shell/route/RoleGuard preserved (UX-06).
</success_criteria>

<output>
Create `.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-06-SUMMARY.md` when done.
</output>
