---
phase: 25-corre-o-do-funil-lado-rh-enums-colunas-contratos
plan: 08
type: execute
wave: 2
depends_on: ["25-02", "25-03", "25-04", "25-05", "25-06"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [FUNIL-04]

must_haves:
  truths:
    - "The CI tsc gate is re-pinned to the newly MEASURED error count after the Phase-25 fixes — not the stale 133 and not the false 'keep 128' assumption"
    - "The gate stays red-on-growth: CI fails only if the tsc error count exceeds the new pinned baseline"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "tsc baseline re-pinned to the measured post-Phase-25 count"
      contains: "frozen baseline"
  key_links:
    - from: "npx tsc --noEmit | grep -c 'error TS'"
      to: "ci.yml frozen baseline (L54/55/56 + header comment)"
      via: "measure-first, then re-pin"
      pattern: "if \\[ \"\\$COUNT\" -gt"
---

<objective>
Re-measure `npx tsc --noEmit | grep -c "error TS"` after the Phase-25 fixes land (enum cutover clears ≈ −6, phantom columns clear ≈ −8) and re-pin the CI tsc gate in `.github/workflows/ci.yml` from the stale **133** to the new measured number (expected ≈ 113–114) — keeping the red-on-growth semantics.

Purpose: The gate has been pinned at 133 since Phase 22 but the real count entering Phase 25 was 128 — CI passes silently until 134, so this phase's own reduction is not locked in unless re-measured. "Keep 128 green" is ALSO wrong (Pitfall 1). Measure-first is load-bearing.
Output: `ci.yml` re-pinned to the measured value. Runs after all Phase-25 frontend plans so the measurement reflects the final tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-RESEARCH.md
@.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-PATTERNS.md

<interfaces>
<!-- ci.yml current pins (VERIFIED): header comment L12-19 states "green value 133"; the gate step L51 label "frozen tsc baseline 133";
     L53 COUNT=$(npm run -s lint | grep -c "error TS"); L54 echo "…(frozen baseline: 133)"; L55 if [ "$COUNT" -gt 133 ]; L56 ::error…exceeds frozen baseline (133). Re-pin every occurrence. -->
Measured entering Phase 25: 128 (stale pin 133). Expected post-Phase-25 ≈ 113–114 (−14 clearance: candidaturasService −5, KanbanBoard −1, CriarEditarVagaPage phantom −8). Types regen (25-07) does NOT change the frontend-driven count.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Measure the post-fix tsc count and re-pin ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — the tsc gate step + header comment (L12-19 narrative, L51 label, L53-57 the COUNT/compare logic; every literal "133")
    - 25-RESEARCH.md §2c (tsc baseline impact, measured 128, expected ≈113-114) + Pitfall 1 ("the gate is 133, not 128"); 25-PATTERNS.md "ci.yml re-pin" (No Analog — measurement task)
  </read_first>
  <action>
    Run `npx tsc --noEmit 2>/dev/null | grep -c "error TS"` on the merged Phase-25 tree (all frontend plans landed) to get the exact new count N. Re-pin EVERY occurrence of the old baseline in ci.yml — the header narrative comment (L12-19), the step label (L51 "frozen tsc baseline 133"), the echo (L54), the compare (L55 `-gt 133`), and the error message (L56) — from `133` to `N`. Keep the red-on-growth logic (`-gt N`) unchanged in shape; do NOT change the measurement command. If N is unexpectedly ≥ 128, STOP and investigate (a phantom/enum fix regressed) rather than pinning a higher number.
  </action>
  <acceptance_criteria>
    - The pinned baseline in ci.yml equals the freshly measured N: `grep -oE "frozen baseline: ?[0-9]+" .github/workflows/ci.yml | grep -oE "[0-9]+"` equals `npx tsc --noEmit | grep -c "error TS"`
    - No literal `133` remains as the tsc baseline anywhere in the gate step/comment: `grep -c "133" .github/workflows/ci.yml` == 0 (or none refer to the tsc baseline)
    - N < 128 (the phase reduced the count); the compare is still `-gt` (red-on-growth)
    - `npm run build` unaffected (CI-config change only)
  </acceptance_criteria>
  <verify>
    <automated>bash -c "N=\$(npx tsc --noEmit 2>/dev/null | grep -c 'error TS'); PIN=\$(grep -oE 'frozen baseline: ?[0-9]+' .github/workflows/ci.yml | grep -oE '[0-9]+' | head -1); echo \"measured=\$N pinned=\$PIN\"; [ \"\$N\" = \"\$PIN\" ] && [ \"\$N\" -lt 128 ] && grep -q -- '-gt' .github/workflows/ci.yml && echo REPIN_OK"</automated>
  </verify>
  <done>ci.yml tsc baseline re-pinned to the measured post-Phase-25 count (< 128), red-on-growth preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate config → merge protection | a stale/loose baseline lets new type errors merge silently (regression net hole) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-08-01 | Tampering (silent regression) | tsc gate pinned above the real count | mitigate | measure-first; re-pin to the exact measured N; assert pinned == measured in verify |
| T-25-08-02 | (availability) | pinning too LOW makes CI red on an unrelated pre-existing error | mitigate | pin to the measured N exactly (not an aspirational lower number) |
| T-25-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | zero package installs this plan |
</threat_model>

<verification>
- Pinned baseline == freshly measured N; N < 128; red-on-growth (`-gt`) preserved.
- Runs after 25-02..25-06 so the measurement reflects the final tree.
</verification>

<success_criteria>
- The CI tsc gate is honestly pinned to the reduced post-Phase-25 count (FUNIL-04 success criterion: new type error is caught).
</success_criteria>

<output>
Create `.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-08-SUMMARY.md` when done (record the measured N).
</output>
