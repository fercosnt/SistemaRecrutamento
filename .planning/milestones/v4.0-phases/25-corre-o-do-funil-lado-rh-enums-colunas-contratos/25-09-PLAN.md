---
phase: 25-corre-o-do-funil-lado-rh-enums-colunas-contratos
plan: 09
type: execute
wave: 3
gap_closure: true
depends_on: ["25-05", "25-06"]
files_modified:
  - src/components/pages/CandidatosRHPage.tsx
  - src/features/vagas/services/vagasService.ts
  - src/features/vagas/hooks/useVagas.ts
  - src/features/vagas/services/__tests__/vagasService.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [UX-06]

must_haves:
  truths:
    - "The RH CandidatosRHPage no longer renders the 3 no-op dropdown items ('Enviar Email' / 'Enviar WhatsApp' / 'Exportar PDF') in either the card OR the table view — the dead menu is swept (UX-06)"
    - "The RH VagasRHPage per-vaga tiles ('Em análise' / 'Aprovados' / total) show REAL per-vaga candidatura counts for RH/administrador sessions instead of a hardcoded 0 (UX-06)"
    - "Anonymous /vagas visitors still get NO per-vaga counts (the WR-10 anon-safety skip is preserved — counts run only for authenticated sessions)"
  artifacts:
    - path: "src/features/vagas/services/vagasService.ts"
      provides: "enriquecerVaga runs the per-vaga status-count query when counts are requested (authenticated session), independent of candidatoId; anon still skips"
      contains: "includeCounts"
  key_links:
    - from: "useVagas"
      to: "listVagas / enriquecerVaga includeCounts"
      via: "authenticated-session flag from authStore role"
      pattern: "includeCounts"

verification: |
  - `npm run test:run` green (vagasService + any CandidatosRHPage/VagasRHPage suites).
  - `npm run build` green; `npx tsc --noEmit | grep -c "error TS"` NOT increased above 107.
  - grep: CandidatosRHPage.tsx has 0 'Enviar Email' / 'Enviar WhatsApp' / 'Exportar PDF' DropdownMenuItem.
---

<objective>
Close the two remaining UX-06 dead-affordance items flagged by 25-VERIFICATION (score 8/9): (1) remove the 3 no-op RH dropdown menu items in CandidatosRHPage, and (2) make the RH VagasRHPage per-vaga tiles show real counts instead of a structural 0. Then reconcile the REQUIREMENTS.md coverage table so the delivered FUNIL requirements read as complete.
</objective>

<context>
Root cause for (2): `vagasService.enriquecerVaga` (src/features/vagas/services/vagasService.ts:65-132) early-returns at L87-89 (`if (!candidatoId) return`) BEFORE the per-vaga status-count query (L111-125). That early-return is the WR-10 anon-safety gate — but it ALSO catches RH/administrador sessions (which have `authStore.candidato === null`), so the RH tiles never receive counts. The count query itself (L111-115: `.from('candidaturas').select('status').eq('vaga_id', vaga.id).is('deleted_at', null)`) is already RLS-scoped — for an RH session Phase-24 RLS returns candidaturas of vagas they own (administrador reads all); for anon it must NOT run (WR-10 blast-radius). `useVagas` (src/features/vagas/hooks/useVagas.ts:82-111) sources `candidato?.id` from `authStore` and passes it to `listVagas`.

The dead menu (1) is in src/components/pages/CandidatosRHPage.tsx — DropdownMenuItem 'Enviar Email' / 'Enviar WhatsApp' / 'Exportar PDF' with NO onClick, in BOTH the card view (~L444-456) and the table view (~L802-814), each wrapped by a DropdownMenuSeparator.

REQUIREMENTS.md still shows FUNIL-02/03/06/09/11 as Pending for Phase 25 even though the code + 25-07 live smokes delivered them (only 3 of 8 plans' docs-commits touched the coverage table).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sweep the 3 no-op RH dropdown items (card + table views)</name>
  <files>src/components/pages/CandidatosRHPage.tsx</files>
  <action>
    Remove the 'Enviar Email', 'Enviar WhatsApp', and 'Exportar PDF' DropdownMenuItem blocks (no onClick — dead affordances) from BOTH the card view (~L444-456) and the table view (~L802-814), together with the now-orphaned DropdownMenuSeparator(s) that only separated them. Keep the real, wired items (Ver Perfil, Aprovar, Rejeitar, etc.) and their separators intact. Remove any icon imports (Mail, MessageSquare, FileText) that become unused as a result (check with grep — do NOT remove an icon still used elsewhere in the file). Do not introduce any new no-op item.
  </action>
  <acceptance_criteria>
    - `grep -cE "Enviar Email|Enviar WhatsApp|Exportar PDF" src/components/pages/CandidatosRHPage.tsx` == 0
    - No unused-import tsc error introduced (remove now-dead icon imports); tsc count NOT increased above 107
    - `npm run build` green
  </acceptance_criteria>
  <done>The RH candidate dropdown shows only wired actions; the 3 no-op items are gone from both views.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Real per-vaga counts for authenticated (RH) sessions; anon still skipped</name>
  <files>src/features/vagas/services/vagasService.ts, src/features/vagas/hooks/useVagas.ts, src/features/vagas/services/__tests__/vagasService.test.ts</files>
  <action>
    Decouple the per-vaga count query from candidatoId. Add an `includeCounts?: boolean` input threaded from `useVagas` → `listVagas` → `enriquecerVaga` (extend the existing signatures; keep `candidatoId` for the hasUserApplied read only). In `enriquecerVaga`: keep the anon fast-path (when NEITHER candidatoId NOR includeCounts → return early, preserving WR-10), compute `hasUserApplied` only when `candidatoId` is present, and run the status-count query (total / emAnalise / aprovados) when `includeCounts` is true (independent of candidatoId). In `useVagas`, set `includeCounts` true for an AUTHENTICATED session (RH / administrador / candidato — read the role or user from authStore; anon → false). Preserve the exact bucket logic (em_analise, aprovado_proxima). Add/extend a unit test asserting: (a) an RH-style call (includeCounts=true, no candidatoId) populates total/emAnalise/aprovados from the mocked rows; (b) an anon-style call (no candidatoId, includeCounts falsy) does NOT issue the count query and leaves counts undefined/0. RED first for the new assertions.
  </action>
  <acceptance_criteria>
    - `grep -q "includeCounts" src/features/vagas/services/vagasService.ts src/features/vagas/hooks/useVagas.ts` succeeds
    - Anon path preserved: with neither candidatoId nor includeCounts, enriquecerVaga issues NO candidaturas query (assert via mock call-count)
    - `npm run test:run` green (vagasService suite incl. the new assertions)
    - `npm run build` green; tsc NOT increased above 107
  </acceptance_criteria>
  <done>RH VagasRHPage tiles receive real per-vaga counts; anon still gets none.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Reconcile REQUIREMENTS.md Phase-25 coverage</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Mark FUNIL-02, FUNIL-03, FUNIL-06, FUNIL-09, FUNIL-11 as Complete for Phase 25 in the coverage table (they are delivered — confirmed by code + the 25-07 live smokes). Use `gsd-sdk query requirements.mark-complete` for each if it works against this repo's format; otherwise edit the coverage table directly, matching the existing row style. Do NOT touch UX-06 status here (Tasks 1–2 deliver it; leave its final mark to the SUMMARY/verification).
  </action>
  <acceptance_criteria>
    - FUNIL-02/03/06/09/11 no longer read "Pending" for Phase 25 in REQUIREMENTS.md
  </acceptance_criteria>
  <done>REQUIREMENTS coverage reflects the delivered Phase-25 requirements.</done>
</task>

</tasks>

<success_criteria>
- The 2 open UX-06 dead-affordance items are closed (no-op menu removed; RH tiles show real counts, anon still skipped).
- REQUIREMENTS.md coverage reconciled.
- Gates green: test:run, build, tsc ≤ 107.
</success_criteria>

<output>
Create `.planning/phases/25-corre-o-do-funil-lado-rh-enums-colunas-contratos/25-09-SUMMARY.md` when done.
</output>
