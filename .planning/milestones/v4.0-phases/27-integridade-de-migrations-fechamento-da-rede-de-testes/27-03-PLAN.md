---
phase: 27-integridade-de-migrations-fechamento-da-rede-de-testes
plan: 03
type: execute
wave: 2
depends_on: [27-01, 27-02]
files_modified:
  - supabase/functions/submit-candidatura/index.ts
  - supabase/functions/submit-candidatura/index.test.ts
  - src/features/cadastro/__tests__/submitCandidaturaContract.test.ts
  - supabase/tests/submit_candidatura_atomic_smokes.sql
  - .github/workflows/ci.yml
autonomous: true
requirements: [CI-03, CI-07, DBMIG-02]
must_haves:
  truths:
    - "CI-03: submit-candidatura/index.ts exports a testable `handler(req, deps)` with the two supabase clients injected via deps (mirrors submit-bigfive-final); Deno.serve wraps it with the real clients"
    - "CI-03: the Deno EF test asserts 401 on missing/invalid auth (never writes), .strict() 400 on an extra body key, the submit_candidatura_atomic RPC arg shape, and error mapping (DUPLICATE_CANDIDATURA / VALIDATION)"
    - "CI-03/CI-07: a client contract test imports the REAL shared submitCandidaturaSchema and asserts a valid body parses AND an injected `score` key is rejected (.strict, RNF-07a)"
    - "CI-03/DBMIG-02: submit_candidatura_atomic_smokes.sql asserts knockout sanctioned-reject (one history row auto_rejeitado=true, ator NULL), survivor advance (etapa→triagem, auto_rejeitado=false), and dedup DUPLICATE — RED until the 27-05 apply"
    - "the CI tsc frozen baseline is re-measured on the final autonomous tree and re-pinned to the MEASURED value (never inflated; real ≈104)"
  artifacts:
    - path: "supabase/functions/submit-candidatura/index.ts"
      provides: "exported handler(req, deps) + Deno.serve wrapper (unchanged behavior)"
      contains: "export async function handler"
    - path: "supabase/functions/submit-candidatura/index.test.ts"
      provides: "Deno EF unit test (401 / .strict 400 / RPC shape / error mapping)"
      contains: "Deno.test"
    - path: "src/features/cadastro/__tests__/submitCandidaturaContract.test.ts"
      provides: "real .safeParse against shared submitCandidaturaSchema + anti-tamper reject"
      contains: "submitCandidaturaSchema"
    - path: "supabase/tests/submit_candidatura_atomic_smokes.sql"
      provides: "knockout/survivor/dedup + DBMIG-02 smokes (branch/PROD, RED until 27-05)"
      contains: "auto_rejeitado"
  key_links:
    - from: "src/features/cadastro/__tests__/submitCandidaturaContract.test.ts"
      to: "supabase/functions/_shared/schemas.ts (submitCandidaturaSchema)"
      via: "real import + safeParse"
      pattern: "submitCandidaturaSchema.safeParse"
    - from: "supabase/functions/submit-candidatura/index.test.ts"
      to: "supabase/functions/submit-candidatura/index.ts (handler)"
      via: "dynamic import of exported handler + injected mock clients"
      pattern: "handler\\(.*deps"
---

<objective>
Close CI-03 — the `submit-candidatura` EF + `submit_candidatura_atomic` RPC (the system's ONLY sanctioned auto-reject) has zero test coverage today. Deliver the three-layer strategy: (a) refactor `index.ts` to export a `handler(req, deps)` and write a Deno EF unit test (401 / `.strict()` 400 / RPC-shape / error mapping) that runs in CI; (b) a Vitest client contract test that `.safeParse`s the real shared `submitCandidaturaSchema` (overlaps CI-07, anti-tamper); (c) author the SQL smoke (`knockout / survivor / dedup` + DBMIG-02 assertions) that runs on the branch/PROD in the BLOCKING wave (RED until the 27-05 trigger apply). Finally, re-measure and re-pin the CI tsc baseline on the final autonomous tree (measure-last).

Purpose: The one auto-reject path must be regression-covered at the request layer (CI-runnable) and the RPC layer (smoke), and the anti-tamper contract must be a real parse.
Output: refactored handler, Deno test, client contract test, smoke SQL, re-pinned tsc gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md
@.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-PATTERNS.md

<interfaces>
<!-- The analog handler shape + the RPC/error-mapping contract. Executor clones the harness. -->
Analog (exported handler) — supabase/functions/submit-bigfive-final/index.ts:124:
  export async function handler(req: Request, deps: SubmitBigfiveFinalDeps): Promise<Response>
Analog test harness — submit-bigfive-final/index.test.ts:36,73-136,149-156,248-257:
  loadHandler() dynamic-imports ./index.ts; makeMockSupabaseAdmin (captures inserts/updates, resolves ownership row);
  makeMockSupabaseUser(user) → { auth: { getUser } }; asserts 401 never-writes + .strict() 400.

submit-candidatura today (must refactor) — supabase/functions/submit-candidatura/index.ts:
  :76  Deno.serve(async (req) => {...})            ← inline; NO exported handler
  :109 submitCandidaturaSchema.safeParse(raw)      ← body validation
  :145 supabaseUser.auth.getUser()                 ← auth
  :238 rpc('submit_candidatura_atomic', {...})     ← the RPC call to assert
  :11-12 map 23505 → DUPLICATE_CANDIDATURA, 23503 → VALIDATION

Shared schema (from 27-02) — supabase/functions/_shared/schemas.ts:201-232 submitCandidaturaSchema (bare `zod`, `.strict()`):
  { candidato_id uuid, vaga_id uuid, curriculo_url min(1), curriculo_nome min(1), curriculo_size int positive max 5MB, respostas array max(100) default([]) }.strict()
Client contract test import path (from src/features/cadastro/__tests__/): '../../../../supabase/functions/_shared/schemas'

Smoke analog — supabase/tests/funil01_pontuar_sjt_smokes.sql:21-40,45-66,135-151 (disposable fixture, smoke.ready skip-guard, SET ROLE authenticated + set_config request.jwt.claims, RAISE NOTICE 'PASS'/RAISE EXCEPTION 'FAIL', ROLLBACK-free cleanup)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CI-03 — refactor submit-candidatura/index.ts to export handler(req, deps)</name>
  <files>supabase/functions/submit-candidatura/index.ts</files>
  <behavior>
    - handler(req, deps) returns 401 when deps.supabaseUser.auth.getUser() yields no user, and never calls the RPC
    - handler returns 400 when the body fails submitCandidaturaSchema.safeParse (including an extra `.strict()` key)
    - handler calls deps.supabaseAdmin.rpc('submit_candidatura_atomic', {...}) with the validated args on the happy path
    - a 23505 from the RPC maps to DUPLICATE_CANDIDATURA; a 23503 maps to VALIDATION
    - Deno.serve wires the same real clients as before → runtime behavior is byte-equivalent
  </behavior>
  <read_first>
    - supabase/functions/submit-candidatura/index.ts (full — the inline Deno.serve at :76; auth :145; validate :109; rpc :238; error map :11-12,257,265-267)
    - supabase/functions/submit-bigfive-final/index.ts:124 + its deps type (the exact `handler(req, deps)` shape to mirror)
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-PATTERNS.md §"submit-candidatura/index.ts (MODIFY)" + PLANNER NOTE
    - depends on 27-02: submitCandidaturaSchema now imports bare `zod` and deno.json maps it — confirm the corpus is green first
  </read_first>
  <action>
    Refactor `submit-candidatura/index.ts` to extract the current inline `Deno.serve` body into an exported `handler(req: Request, deps: { supabaseAdmin; supabaseUser }): Promise<Response>`, exactly mirroring `submit-bigfive-final/index.ts:124`. Move the two client constructions (service_role admin + anon user client) OUT of the handler; `Deno.serve` builds them and passes `{ supabaseAdmin, supabaseUser }` into `handler`. Inside `handler`, keep every existing step in order: `submitCandidaturaSchema.safeParse(raw)` (→ 400 VALIDATION on failure), `supabaseUser.auth.getUser()` (→ 401 if no user, never touching the RPC), the candidatos ownership lookup on `supabaseAdmin`, and `supabaseAdmin.rpc('submit_candidatura_atomic', {...})` with the 23505→DUPLICATE_CANDIDATURA / 23503→VALIDATION mapping. Do NOT change any status code, error_code, message, or the RPC arg object — behavior must be byte-equivalent; only the seam changes.
  </action>
  <acceptance_criteria>
    - `grep -c "export async function handler" supabase/functions/submit-candidatura/index.ts` == 1
    - `grep -c "Deno.serve" supabase/functions/submit-candidatura/index.ts` == 1 (thin wrapper only)
    - the handler references `deps.supabaseAdmin` and `deps.supabaseUser` (clients no longer constructed inside the handler)
    - `grep -c "submit_candidatura_atomic" supabase/functions/submit-candidatura/index.ts` >= 1 (RPC call preserved)
    - full Deno corpus green: `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions` exits 0
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions</automated>
  </verify>
  <done>index.ts exports handler(req, deps); Deno.serve is a thin wrapper injecting the real clients; corpus green; behavior unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CI-03/CI-07 — Deno EF unit test + Vitest client contract test</name>
  <files>supabase/functions/submit-candidatura/index.test.ts, src/features/cadastro/__tests__/submitCandidaturaContract.test.ts</files>
  <behavior>
    - Deno test: no-session (getUser null) → 401 and admin.inserts/RPC calls length 0
    - Deno test: body with an extra field (`{...VALID_BODY, score:99}`) → 400
    - Deno test: happy path calls rpc('submit_candidatura_atomic', {...}) with the expected arg keys
    - Deno test: RPC 23505 → response error_code DUPLICATE_CANDIDATURA; 23503 → VALIDATION
    - Vitest contract: submitCandidaturaSchema.safeParse(validBody).success === true
    - Vitest contract: submitCandidaturaSchema.safeParse({...validBody, score:9}).success === false (.strict)
  </behavior>
  <read_first>
    - supabase/functions/submit-bigfive-final/index.test.ts:36,73-136,149-156,248-257 (harness to clone: loadHandler, makeMockSupabaseAdmin, makeMockSupabaseUser, 401 + .strict idioms)
    - src/features/decisao/schemas/__tests__/consolidacaoContract.test.ts (post-27-02 real-parse idiom to mirror for the client test)
    - supabase/functions/_shared/schemas.ts:201-232 (the exact valid body shape to build)
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md §4 layers (a)+(b)
  </read_first>
  <action>
    (a) Create `submit-candidatura/index.test.ts` cloning the `submit-bigfive-final/index.test.ts` harness: a `loadHandler()` dynamic import of `./index.ts`, `makeMockSupabaseAdmin` (captures inserts + records rpc calls, resolves the candidatos ownership row), `makeMockSupabaseUser(user)`. Write `Deno.test` cases for: (1) getUser null → 401 + zero inserts/RPC; (2) body `{...VALID_BODY, score:99}` → 400 (`.strict()`); (3) happy path → assert the mock recorded an `rpc('submit_candidatura_atomic', args)` call whose args object carries `candidato_id`/`vaga_id`/`respostas`; (4) inject a mock RPC error with code `23505` → response maps to `DUPLICATE_CANDIDATURA`, and `23503` → `VALIDATION`. (b) Create `src/features/cadastro/__tests__/submitCandidaturaContract.test.ts` importing the REAL `submitCandidaturaSchema` from `../../../../supabase/functions/_shared/schemas`; build the exact `{candidato_id, vaga_id, curriculo_url, curriculo_nome, curriculo_size, respostas}` body and assert `safeParse(body).success===true` and `safeParse({...body, score:9}).success===false`. No `@/` alias in the import (keeps tsc program roots minimal).
  </action>
  <acceptance_criteria>
    - `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/submit-candidatura` exits 0 with >= 4 test cases
    - the Deno test asserts zero writes on the 401 path (`admin.inserts.length === 0` and no recorded RPC call)
    - the Deno test asserts the DUPLICATE_CANDIDATURA and VALIDATION mappings
    - `npm run test:run -- submitCandidaturaContract` green; asserts both the valid-parse (true) and injected-`score` reject (false)
    - `npm run -s lint 2>&1 | grep -c "error TS"` ≤ 107 (client test adds no type errors)
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/submit-candidatura && npm run test:run -- submitCandidaturaContract</automated>
  </verify>
  <done>Deno EF test covers 401/.strict/RPC-shape/error-mapping; client contract test does a real anti-tamper safeParse against the shared schema.</done>
</task>

<task type="auto">
  <name>Task 3: CI-03/DBMIG-02 smoke SQL (branch/PROD) + measure-last tsc re-pin</name>
  <files>supabase/tests/submit_candidatura_atomic_smokes.sql, .github/workflows/ci.yml</files>
  <read_first>
    - supabase/tests/funil01_pontuar_sjt_smokes.sql:21-40,45-66,135-151 (disposable-fixture + impersonated-JWT + PASS/FAIL idiom to mirror)
    - supabase/migrations/20260709000014_submit_candidatura_flag.sql:38-195 (submit_candidatura_atomic: knockout sanctioned-reject at :146-150, survivor advance at :165-168, unique candidato_id+vaga_id → 23505)
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md §4 layer (c) + §2 (DBMIG-02 assertions)
    - .github/workflows/ci.yml:12-27 (baseline banner), :55-63 (the `-gt 107` tsc gate — re-pin target); STATE 25-08 (measure-first re-pin precedent)
  </read_first>
  <action>
    (a) Author `supabase/tests/submit_candidatura_atomic_smokes.sql` mirroring the `funil01_pontuar_sjt_smokes.sql` idiom: privileged setup discovering a REAL candidatos row + user_id, disposable fixture on fixed `27010001-*` UUIDs (idempotent delete-then-insert), `smoke.ready` skip-guard, `SET ROLE authenticated` + `set_config('request.jwt.claims', …)`, ROLLBACK-free cleanup that never deletes the discovered real candidato. Assertions: (1) knockout sanctioned-reject → `candidaturas.status='rejeitado'`, `etapa_atual='inscricao'`, `motivo_rejeicao='knockout_automatico'`, EXACTLY ONE `historico_candidatura` row with `auto_rejeitado=true, ator IS NULL`; (2) survivor advance → `etapa_atual` moves to `triagem` AND (post-DBMIG-02) its history row has `auto_rejeitado=false`; (3) dedup → a second insert with the same `candidato_id+vaga_id` raises the unique violation → DUPLICATE. Header comment: RED until the 27-05 trigger apply (assertion 2 depends on it); runs via MCP execute_sql / SQL Editor in the BLOCKING wave, NOT the CI runner. (b) Re-measure the tsc baseline on the full merged autonomous tree with the EXACT CI command `npm run -s lint 2>&1 | grep -c "error TS"`, and re-pin `ci.yml` — the `-gt 107` gate, the step label, the echo, and the `::error::` message — to the MEASURED value (expected ≈104, possibly lower after 27-01's CI-06 dedup). Update the baseline banner comment (`:12-27`) to record the new number and keep the prior 133/107 as labeled superseded history. NEVER inflate; NEVER leave 107 stale.
  </action>
  <acceptance_criteria>
    - `supabase/tests/submit_candidatura_atomic_smokes.sql` exists; `grep -c "auto_rejeitado" supabase/tests/submit_candidatura_atomic_smokes.sql` >= 2 (knockout true + survivor false assertions)
    - `grep -c "knockout_automatico\|DUPLICATE\|triagem" supabase/tests/submit_candidatura_atomic_smokes.sql` >= 3 (all three assertions present)
    - `grep -c "set_config('request.jwt.claims'" supabase/tests/submit_candidatura_atomic_smokes.sql` >= 1 (impersonation idiom)
    - MEASURED := `npm run -s lint 2>&1 | grep -c "error TS"`; the ci.yml gate `-gt <N>` uses exactly that MEASURED N: `grep -c "\-gt $(npm run -s lint 2>&1 | grep -c 'error TS')" .github/workflows/ci.yml` == 1
    - `grep -c "\-gt 107" .github/workflows/ci.yml` == 0 (stale pin removed) UNLESS the measured value is coincidentally 107
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/tests/submit_candidatura_atomic_smokes.sql && N=$(npm run -s lint 2>&1 | grep -c "error TS"); echo "measured tsc: $N"; grep -c -- "-gt $N" .github/workflows/ci.yml</automated>
  </verify>
  <done>Smoke SQL authored (knockout/survivor/dedup + DBMIG-02, RED until 27-05); tsc gate re-pinned to the measured value on the final autonomous tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon client → submit-candidatura EF | untrusted body + JWT; must 401 unauth and `.strict()`-reject injected keys before any write |
| EF → submit_candidatura_atomic RPC | the sole sanctioned auto-reject; knockout must be objective, survivor advance must not be marked a rejection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering | client injects `score`/`peso` into the submit body (RNF-07a) | mitigate | shared `.strict()` schema; both Deno (400) and Vitest (safeParse false) assert the reject |
| T-27-03-02 | Elevation | handler refactor accidentally weakens the getUser 401 gate | mitigate | Deno test asserts 401 + zero writes on no-session; behavior byte-equivalent |
| T-27-03-03 | Repudiation | survivor advance mislabeled auto_rejeitado in the audit trail | mitigate | smoke asserts survivor `auto_rejeitado=false` post-DBMIG-02; knockout still `true` |
| T-27-03-04 | Elevation | a score-driven auto-reject sneaks into a test as "expected" | mitigate | smoke asserts ONLY the objective knockout + dedup; no score path asserted (RNF-07a) |
| T-27-03-SC | Tampering | package installs | accept | none this phase |
</threat_model>

<verification>
- `deno test … supabase/functions/submit-candidatura` green (401/.strict/RPC-shape/error-map).
- `npm run test:run -- submitCandidaturaContract` green (real anti-tamper safeParse).
- Smoke SQL present with the three assertions + DBMIG-02 (runs in 27-05).
- ci.yml tsc gate pinned to the measured value; no stale 107 (unless measured==107).
</verification>

<success_criteria>
The sole sanctioned auto-reject is regression-covered at the request layer (CI-runnable) and authored for the RPC layer (smoke, runs in BLOCKING wave); anti-tamper is a real parse; the tsc gate is pinned to the measured baseline.
</success_criteria>

<output>
Create `.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-03-SUMMARY.md` when done.
</output>
