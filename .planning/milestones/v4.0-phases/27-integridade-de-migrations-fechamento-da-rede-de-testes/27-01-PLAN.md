---
phase: 27-integridade-de-migrations-fechamento-da-rede-de-testes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/entrevista/services/entrevistaService.ts
  - package.json
  - .github/workflows/ci.yml
  - scripts/__tests__/sync-prompts.test.ts
autonomous: true
requirements: [CI-06, CI-10, CI-15]
must_haves:
  truths:
    - "CI-06: entrevistaService imports the shared extractEfErrorCode from @/lib/efErrors; the inverted local copy is gone; the VALIDATION branch still fires"
    - "CI-10: a bundle regression fails BOTH `npm run build` (postbuild) AND a dedicated CI step running assert-chunks.mjs"
    - "CI-15: the sync-prompts Deno test runs in CI as a distinct step in the deno-test job — and type-checks clean: the sync-prompts.test.ts:30 module cast is repaired so `deno test` passes with type-check ON (no `--no-check`), matching the corpus job's blocking posture"
    - "tsc gate stays green (count ≤ 107); this plan does NOT re-pin the baseline — the measure-last re-pin is 27-03"
  artifacts:
    - path: "src/features/entrevista/services/entrevistaService.ts"
      provides: "single canonical extractEfErrorCode usage (no local reimplementation)"
      contains: "import { extractEfErrorCode } from '@/lib/efErrors'"
    - path: "package.json"
      provides: "assert:chunks script + postbuild lifecycle wiring assert-chunks.mjs"
      contains: "assert:chunks"
    - path: ".github/workflows/ci.yml"
      provides: "bundle-gate CI step + sync-prompts scripts Deno step"
      contains: "scripts/assert-chunks.mjs"
    - path: "scripts/__tests__/sync-prompts.test.ts"
      provides: "type-check-clean loadSync() module cast so the CI-15 Deno step passes with type-check ON"
      contains: "as unknown as"
  key_links:
    - from: "src/features/entrevista/services/entrevistaService.ts:637"
      to: "src/lib/efErrors.ts:38"
      via: "shared helper import (data, error) order"
      pattern: "extractEfErrorCode\\(data, error\\)"
    - from: "package.json postbuild"
      to: "scripts/assert-chunks.mjs"
      via: "npm lifecycle after vite build"
      pattern: "postbuild.*assert-chunks"
---

<objective>
Close the three "test-net wiring" requirements that are file-disjoint from the migration/schema work: dedup the inverted `extractEfErrorCode` copy in `entrevistaService.ts` onto the canonical `@/lib/efErrors` helper (CI-06), wire the already-existing `scripts/assert-chunks.mjs` bundle gate into build AND CI (CI-10), and run the existing `scripts/__tests__/sync-prompts.test.ts` Deno test in CI (CI-15) — repairing the latent TS2352 in the test's module cast that this file's first-ever type-checked CI run surfaces.

Purpose: These are the regression-net gates that turn latent defects into red CI. They touch only `entrevistaService.ts`, `package.json`, `scripts/__tests__/sync-prompts.test.ts`, and `ci.yml` — disjoint from every other Phase-27 plan, so they run first, in parallel.
Output: Deduplicated helper usage, a wired bundle gate, and a wired + type-check-clean sync-prompts CI step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md
@.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-PATTERNS.md

<interfaces>
<!-- Canonical helper the executor must reuse (do NOT re-implement). Extracted from src/lib/efErrors.ts -->
From src/lib/efErrors.ts:38 (canonical):
  export async function extractEfErrorCode(data: unknown, error: unknown): Promise<string | undefined>
  // reads error.context.json() FIRST, then data.error_code; returns undefined (not null) on failure

Current duplicate in src/features/entrevista/services/entrevistaService.ts:
  - :662  async function extractEfErrorCode(error, data): Promise<string | null>   ← inverted args, data-first, returns null. DELETE.
  - :637  const efCode = await extractEfErrorCode(error, data)                      ← swap call to (data, error)
  - the `if (efCode === 'VALIDATION')` compare is unaffected by null→undefined

<!-- CI-15 latent type defect. The test's ad-hoc module-cast shape is incompatible with the REAL exports. -->
From scripts/sync-prompts.ts (the REAL, closed contract — no index signature):
  export interface UpsertRow { call_type; semver; content_hash; system_template; ...; is_active: false; is_canary: false }   // :174-189
  export function buildUpsertRow(args: {...}): UpsertRow                                                                     // :196
  export type Frontmatter = z.infer<typeof frontmatterSchema>                                                               // :64
  export function validateFrontmatter(fm): { success: true; data: Frontmatter } | { success: false; error: unknown }        // :70

From scripts/__tests__/sync-prompts.test.ts:28-41 (the TS2352 source):
  return mod as { ... buildUpsertRow(...): Record<string, unknown> ... }   ← closed UpsertRow is NOT comparable to
                                                                             Record<string, unknown> ("Index signature
                                                                             for type 'string' is missing in type
                                                                             'UpsertRow'") → TS2352 with type-check ON.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI-06 — dedup extractEfErrorCode in entrevistaService onto @/lib/efErrors</name>
  <files>src/features/entrevista/services/entrevistaService.ts</files>
  <read_first>
    - src/lib/efErrors.ts:38-67 (canonical `(data, error) → Promise<string|undefined>`, error-context-first)
    - src/features/entrevista/services/entrevistaService.ts:27-28 (import block), :637 (call site), :662 (local dup)
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-PATTERNS.md §"entrevistaService.ts (CI-06 dedup)"
    - STATE.md Phase-22 Pitfall: a leftover unused local/import after a dedup is a fresh TS6133 that inflates the tsc count
  </read_first>
  <action>
    Delete the local `extractEfErrorCode(error, data): Promise<string|null>` function at `entrevistaService.ts:662`. Add `import { extractEfErrorCode } from '@/lib/efErrors'` to the top import block (~:27-28). Change the call site at `:637` from `extractEfErrorCode(error, data)` to `extractEfErrorCode(data, error)` (canonical arg order). Leave the `if (efCode === 'VALIDATION')` branch as-is (the `=== 'VALIDATION'` string compare is identical whether the miss returns `null` or `undefined`). After deleting the local fn, scan for any now-unused local type/import it referenced and prune it (noUnusedLocals → TS6133 would inflate tsc). Do NOT change any other behavior in the file.
  </action>
  <acceptance_criteria>
    - `grep -c "async function extractEfErrorCode" src/features/entrevista/services/entrevistaService.ts` == 0 (local dup removed)
    - `grep -c "import { extractEfErrorCode } from '@/lib/efErrors'" src/features/entrevista/services/entrevistaService.ts` == 1
    - `grep -n "extractEfErrorCode(data, error)" src/features/entrevista/services/entrevistaService.ts` matches at the former :637 call site
    - `npm run test:run` passes (efErrors + entrevista suites green)
    - `npm run -s lint 2>&1 | grep -c "error TS"` is ≤ 107 (no new type errors; expected ≤ 104)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run && npm run -s lint 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>entrevistaService uses the shared helper with canonical `(data, error)` order; no local reimplementation; tests + tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: CI-10 — wire assert-chunks.mjs into build (postbuild) and CI (dedicated step)</name>
  <files>package.json, .github/workflows/ci.yml</files>
  <read_first>
    - scripts/assert-chunks.mjs:31 (reads build/assets/*.js), :141-149 (exits non-zero on regression — 4 PERF-03 assertions)
    - package.json:95-111 (scripts block; build at :97, no postbuild today)
    - .github/workflows/ci.yml:83-96 (e2e job — already runs `npm run build` at :94)
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-PATTERNS.md §"CI-10 / CI-15 wiring"
  </read_first>
  <action>
    In `package.json` scripts, add `"assert:chunks": "node scripts/assert-chunks.mjs"` and `"postbuild": "node scripts/assert-chunks.mjs"`. The `postbuild` npm lifecycle runs automatically after `npm run build` (vite build writes `build/assets/*.js`, then assert-chunks reads them) so `npm run build` self-gates locally and every CI job that builds inherits the gate. Do NOT change `assert-chunks.mjs` itself or retune its `BASELINE_INDEX_BYTES`/`PRESPLIT_CHUNK_FLOOR` (tuned in Phase 19). In `ci.yml`, add a dedicated named step in the `e2e` job immediately AFTER the existing `npm run build` step (`:94`): `- name: Bundle gate (PERF-03)` running `node scripts/assert-chunks.mjs`, so CI emits a clean distinct failure signal for a bundle regression in addition to the postbuild self-gate.
  </action>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); process.exit(p.scripts['assert:chunks']&&p.scripts.postbuild?0:1)"` exits 0
    - `grep -c "scripts/assert-chunks.mjs" .github/workflows/ci.yml` >= 1 (dedicated e2e step)
    - `npm run build` completes and its postbuild runs assert-chunks (exit 0 on a clean bundle)
    - assert-chunks.mjs body is byte-unchanged: `git diff --stat scripts/assert-chunks.mjs` shows no change
  </acceptance_criteria>
  <verify>
    <automated>npm run build && node scripts/assert-chunks.mjs && grep -c "scripts/assert-chunks.mjs" .github/workflows/ci.yml</automated>
  </verify>
  <done>Bundle regression fails both `npm run build` (postbuild) and a dedicated CI step; script body untouched.</done>
</task>

<task type="auto">
  <name>Task 3: CI-15 — repair the sync-prompts.test.ts type cast, then run it in the deno-test CI job (type-check ON)</name>
  <files>scripts/__tests__/sync-prompts.test.ts, .github/workflows/ci.yml</files>
  <read_first>
    - scripts/__tests__/sync-prompts.test.ts:28-41 (the `loadSync()` module cast — `return mod as { … buildUpsertRow(): Record<string, unknown> … }`, the TS2352 source at :30)
    - scripts/sync-prompts.ts:64 (`export type Frontmatter`), :174-189 (the REAL closed `UpsertRow` interface — no index signature), :196 (`buildUpsertRow(): UpsertRow`), :70-76 (`validateFrontmatter(): { data: Frontmatter } | …`)
    - .github/workflows/ci.yml:73-81 (deno-test job; corpus step uses `--config supabase/functions/deno.json supabase/functions`)
    - scripts/sync-prompts.ts:287 (Supabase client built ONLY under `if (import.meta.main)` → importing the module builds no client → needs only --allow-read/--allow-env)
    - .planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-RESEARCH.md §5c
  </read_first>
  <action>
    STEP A — repair the latent type defect FIRST (the whole point of CI-15 is to type-check this never-CI-run file). Run `deno test --allow-env --allow-read scripts/__tests__/` (type-check ON by default): it FAILS `TS2352` at `sync-prompts.test.ts:30` because the `loadSync()` ad-hoc cast declares `buildUpsertRow(): Record<string, unknown>` while the real export returns the closed `UpsertRow` interface (no index signature → "Index signature for type 'string' is missing in type 'UpsertRow'"). Fix the cast: change `return mod as {` at `:30` to `return mod as unknown as {` — the standard double-cast-through-`unknown` idiom for narrowing a dynamically-imported module to a test-local shape (guaranteed to type-check regardless of the ad-hoc shape). OPTIONAL stronger fidelity: add a type-only `import type { UpsertRow, Frontmatter } from "../sync-prompts.ts";` at the top and replace the ad-hoc `buildUpsertRow(...): Record<string, unknown>` return with `UpsertRow` and `validateFrontmatter`'s `data: Record<string, unknown>` with `Frontmatter`, so the shape mirrors the real exports. Do NOT weaken the run to `--no-check` — RESEARCH §5c requires matching the corpus job's type-check-ON blocking posture. Confirm `deno test --allow-env --allow-read scripts/__tests__/` now exits 0 with type-check ON (7 cases pass, TS2352 gone). STEP B — wire it into CI: in `ci.yml`, add a SEPARATE step inside the existing `deno-test` job (reuses the same `denoland/setup-deno@v2`, gives a distinct failure signal), placed after the existing EF corpus step (`:81`): `- name: Deno scripts test (sync-prompts)` running `deno test --allow-env --allow-read scripts/__tests__/`. Do NOT pass `--config supabase/functions/deno.json` (that config's exclude is functions-scoped and the target root differs — mixing it is fragile). Do NOT merge this into the existing corpus command. Do NOT add `--no-check`. Keep it blocking (no continue-on-error), matching the corpus step's posture.
  </action>
  <acceptance_criteria>
    - `deno test --allow-env --allow-read scripts/__tests__/` exits 0 with type-check ON (no `--no-check`) — the TS2352 at :30 is gone and all 7 cases pass
    - `grep -c "as unknown as" scripts/__tests__/sync-prompts.test.ts` >= 1 (cast repaired) OR `grep -c "import type { UpsertRow" scripts/__tests__/sync-prompts.test.ts` >= 1 (real-types route)
    - `grep -c "scripts/__tests__/" .github/workflows/ci.yml` >= 1
    - the new CI step's command is exactly `deno test --allow-env --allow-read scripts/__tests__/` (no `--config`, no `--no-check`)
    - the existing EF corpus step is unchanged: `grep -c -- "--config supabase/functions/deno.json supabase/functions" .github/workflows/ci.yml` == 1
  </acceptance_criteria>
  <verify>
    <automated>deno test --allow-env --allow-read scripts/__tests__/ && grep -c "scripts/__tests__/" .github/workflows/ci.yml</automated>
  </verify>
  <done>sync-prompts.test.ts type-checks clean (TS2352 repaired); the Deno test runs as a distinct blocking CI step with type-check ON; the EF corpus command is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → merge gate | a bundle/type/test regression must turn CI red before merge to main |
| client service → Edge Function error surface | error-code extraction feeds user-facing branch logic (must not mis-read) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Tampering | bundle regression slips to prod (PERF-03) | mitigate | assert-chunks wired as postbuild (build self-gate) + dedicated CI step; script body untouched |
| T-27-01-02 | Repudiation/Info | wrong `extractEfErrorCode` arg order silently returns wrong code | mitigate | swap call to canonical `(data, error)`; efErrors + entrevista suites are the regression guard |
| T-27-01-03 | DoS | a service_role pipeline test (sync-prompts) rots undetected — or passes only under `--no-check` | mitigate | repair the TS2352 cast + run it in CI type-check-ON as a blocking step (no `--no-check` escape hatch) |
| T-27-01-SC | Tampering | npm/deno installs | accept | zero packages installed this phase; no new supply-chain surface |
</threat_model>

<verification>
- `npm run test:run` green (efErrors + entrevista suites regression-covered).
- `npm run -s lint 2>&1 | grep -c "error TS"` ≤ 107 (expected ≤ 104; this plan does NOT re-pin — 27-03 does).
- `npm run build` succeeds and postbuild runs assert-chunks clean.
- `deno test --allow-env --allow-read scripts/__tests__/` exits 0 with type-check ON (TS2352 repaired).
- `ci.yml` contains the bundle-gate step and the sync-prompts step (no `--no-check`).
</verification>

<success_criteria>
CI-06, CI-10, CI-15 are wired and green. entrevistaService uses only the shared helper; a bundle regression fails build + CI; the sync-prompts test type-checks clean and runs in CI. tsc gate remains green at the (still-107) baseline pending the measure-last re-pin in 27-03.
</success_criteria>

<output>
Create `.planning/phases/27-integridade-de-migrations-fechamento-da-rede-de-testes/27-01-SUMMARY.md` when done.
</output>
