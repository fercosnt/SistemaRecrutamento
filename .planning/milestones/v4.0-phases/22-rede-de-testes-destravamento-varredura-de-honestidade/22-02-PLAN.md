---
phase: 22-rede-de-testes-destravamento-varredura-de-honestidade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements: [CI-09, CI-11, CI-12]
must_haves:
  truths:
    - "No production dependency is pinned to wildcard `\"*\"` — the 8 wildcards are pinned to their lockfile-resolved exact versions (CI-09)"
    - "The vitest/@vitest/ui RCE and happy-dom code-exec CRITICAL/HIGH advisories are cleared for those 3 packages after the bump (CI-11)"
    - "The two never-imported deps (`motion`, `@supabase/auth-helpers-react`) are gone from package.json and node_modules (CI-12)"
    - "The full Vitest suite is green after the bump — no product behavior changed"
  artifacts:
    - path: "package.json"
      provides: "pinned wildcards, bumped dev-tooling, removed dead deps"
      contains: "@tiptap/core"
    - path: "package-lock.json"
      provides: "lockfile re-synced to the pinned/bumped/removed dependency set"
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "npm install re-sync after edits"
      pattern: "lockfileVersion"
---

<objective>
Harden the supply-chain surface: pin the 8 wildcard `"*"` production deps to their exact lockfile-resolved versions (ceiling, zero behavior change), bump the 3 vulnerable dev-tooling packages to their nearest patched versions to clear the vitest/@vitest/ui RCE + happy-dom code-exec advisories, and remove the 2 grep-confirmed never-imported deps.

Purpose: A wildcard `"*"` is not a version ceiling — it lets a compromised/breaking upstream in silently. The RCE/code-exec advisories are live attack surface in the dev toolchain. This is pure supply-chain threat mitigation (per 22-RESEARCH §Supply-chain, §Security Domain).
Output: `package.json` + `package-lock.json` re-synced; full Vitest suite green post-bump; `npm audit` clears the 3 named packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-RESEARCH.md
@.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-CONTEXT.md

<notes>
- 22-RESEARCH §Supply-chain + §Code Examples give the EXACT target versions. Re-verify at execute time (advisories shift): `npm view vitest version && npm view @vitest/ui version && npm view happy-dom version`.
- The "8 wildcards" = the 9 `"*"` deps in package.json MINUS `motion` (which CI-12 DELETES, not pins). See Pitfall 6. Current `"*"` deps: `@tiptap/core`, `@tiptap/extension-text-style`, `@tiptap/react`, `@tiptap/starter-kit`, `clsx`, `react-dnd`, `react-dnd-html5-backend`, `tailwind-merge` (pin these) + `motion` (delete).
- Lockfile-resolved exact versions per 22-RESEARCH §Code Examples: @tiptap/* = `3.10.1`, clsx = `2.1.1`, react-dnd = `16.0.1`, react-dnd-html5-backend = `16.0.1`, tailwind-merge = `3.3.1`. CONFIRM each against `package-lock.json` at execute time before writing (do not trust the doc blindly — read the lockfile).
- Bumps (dev): `vitest ^4.0.7 → ^4.1.9`, `@vitest/ui ^4.0.7 → ^4.1.9` (lockstep — same vulnerable path), `happy-dom ^20.0.10 → ^20.10.6`. All non-major; `vitest@4.1.x` peer-deps `vite ^6||^7` and the repo pins `vite 6.3.5` → no vite change forced.
- Removals grep-confirmed zero import sites: `motion`, `@supabase/auth-helpers-react` (see §Package Legitimacy Audit).
- Pitfall 7: happy-dom 20.0.10→20.10.6 spans minors — DOM-emulation quirks can shift. The locked decision mandates "rodar a suíte inteira" after the bump; treat any red as a bump-compat fix, not a product change.
- CONTEXT: NO permanent `npm audit` gate this phase (advisory flaky). Fix the current crit/high for the 3 named packages only; do not add an audit gate.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead deps + pin the 8 wildcards to exact lockfile versions</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - 22-RESEARCH.md §Package Legitimacy Audit + §Code Examples ("The 8 wildcards → exact pins") + Pitfall 6
    - package.json (the 9 `"*"` dep lines + the `motion` and `@supabase/auth-helpers-react` lines)
    - package-lock.json (read the resolved `"version"` for each of the 8 wildcard packages BEFORE pinning — the pin value is the lockfile truth, not the doc)
  </read_first>
  <action>
    First confirm zero import sites: `grep -rn "from ['\"]motion['\"]\|@supabase/auth-helpers-react" src supabase e2e scripts` must be empty. Then `npm uninstall motion @supabase/auth-helpers-react` (CI-12) — this removes both from `dependencies` and the lockfile. Then replace each of the 8 remaining `"*"` values in package.json `dependencies` with the EXACT version read from package-lock.json (CI-09): `@tiptap/core`, `@tiptap/extension-text-style`, `@tiptap/react`, `@tiptap/starter-kit`, `clsx`, `react-dnd`, `react-dnd-html5-backend`, `tailwind-merge`. Use exact pins (no `^`) per CONTEXT "teto de versão". Do NOT pin `motion` (it is deleted). Run `npm install` to re-sync the lockfile. Confirm no `"*"` remains in package.json.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const w=Object.entries({...p.dependencies}).filter(([k,v])=>v==='*');if(w.length){console.error('WILDCARDS',w);process.exit(1)};if(p.dependencies.motion||p.dependencies['@supabase/auth-helpers-react']){console.error('DEAD DEP PRESENT');process.exit(1)};console.log('ok: 0 wildcards, dead deps removed')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from ['\"]motion['\"]\|@supabase/auth-helpers-react" src supabase e2e scripts` returns nothing.
    - No `"*"` value remains in package.json `dependencies` (node check above exits 0).
    - `motion` and `@supabase/auth-helpers-react` are absent from both package.json and package-lock.json.
    - Each pinned version equals the version recorded in package-lock.json (exact, no `^`).
  </acceptance_criteria>
  <done>8 wildcards pinned to lockfile-exact versions; 2 dead deps removed; lockfile re-synced.</done>
</task>

<task type="auto">
  <name>Task 2: Bump vulnerable dev-tooling and confirm suite green + advisories cleared</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - 22-RESEARCH.md §Standard Stack → Supporting (version bumps) + §Security Domain (RCE/code-exec rows) + Pitfall 7
    - package.json devDependencies (`vitest`, `@vitest/ui`, `happy-dom` current versions)
  </read_first>
  <action>
    Re-verify nearest patched versions at execute time (advisories shift): `npm view vitest version && npm view @vitest/ui version && npm view happy-dom version`. Then `npm install -D vitest@^4.1.9 @vitest/ui@^4.1.9 happy-dom@^20.10.6` — bump vitest and @vitest/ui to the SAME version (lockstep; they share the vulnerable UI-server code path). Run the FULL Vitest suite `npm run test:run` (locked decision "rodar a suíte inteira"); any red is a bump-compat fix in a test, NOT a product change — fix it in the test. Confirm the 3 advisories cleared: `npm audit --json` shows no CRITICAL/HIGH for `vitest`, `@vitest/ui`, `happy-dom`. Do NOT add a permanent `npm audit` gate (CONTEXT). Note but do NOT fix the adjacent `vite@6.3.5` HIGH / `react-router@6.30.1` open-redirect findings — out of CI-11's named scope (record them for Phase 24/27).
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tail -5 && npm audit --json 2>/dev/null | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const a=JSON.parse(s);const bad=Object.entries(a.vulnerabilities||{}).filter(([k,v])=>['vitest','@vitest/ui','happy-dom'].includes(k)&&['critical','high'].includes(v.severity));if(bad.length){console.error('STILL VULNERABLE',bad.map(b=>b[0]));process.exit(1)}console.log('ok: named dev-tooling advisories cleared')})"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run` exits 0 (full Vitest suite green after the bump).
    - `npm audit --json` reports no CRITICAL/HIGH advisory for `vitest`, `@vitest/ui`, or `happy-dom`.
    - `vitest` and `@vitest/ui` are the same version; no `vite` version change forced.
    - package-lock.json re-synced.
  </acceptance_criteria>
  <done>The 3 named dev-tooling advisories are cleared, the full Vitest suite is green, and no product behavior changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev toolchain | Installed dev tools (vitest UI server, happy-dom compiler) execute code on the dev/CI machine |
| package.json version range → resolved install | `"*"` lets any upstream version in; exact pin is the ceiling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Elevation | vitest / @vitest/ui UI-server RCE (CRITICAL) | mitigate | Bump to `^4.1.9` (fix lands 4.1.0); confirm advisory cleared via `npm audit` |
| T-22-02-02 | Elevation | happy-dom module-compiler code-exec (HIGH) | mitigate | Bump to `^20.10.6` (fix lands 20.9.0); full-suite run confirms no drift |
| T-22-02-03 | Tampering | wildcard `"*"` prod deps (unbounded upstream) | mitigate | Pin all 8 to exact lockfile-resolved versions (version ceiling) |
| T-22-02-04 | Tampering | dead deps as latent attack surface | mitigate | Remove `motion` + `@supabase/auth-helpers-react` (0 import sites) |
| T-22-02-05 | Tampering | adjacent `vite@6.3.5` HIGH / `react-router@6.30.1` open-redirect | accept | Out of CI-11's named scope; documented for Phase 24/27 (react-router intersects UX-05) |
| T-22-02-SC | Tampering | package-manager installs (legitimacy) | accept | ZERO net-new packages — only bumps of already-present, battle-tested deps + removals. Per 22-RESEARCH §Package Legitimacy Audit, slopcheck N/A; no legitimacy checkpoint required |
</threat_model>

<verification>
- No `"*"` in package.json dependencies; `motion` / `@supabase/auth-helpers-react` absent everywhere.
- `npm run test:run` green post-bump; `npm audit` clears the 3 named packages.
- `git diff package-lock.json` shows only the pinned/bumped/removed set (no unrelated churn).
</verification>

<success_criteria>
The 8 wildcards are pinned to their exact lockfile versions, the vitest/@vitest/ui/happy-dom advisories are cleared, the 2 dead deps are removed, and the full Vitest suite stays green — the supply-chain surface is bounded with zero product behavior change.
</success_criteria>

<output>
Create `.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-02-SUMMARY.md` when done.
</output>
