---
phase: 22-rede-de-testes-destravamento-varredura-de-honestidade
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/guards/forbidden-strings.grep.test.ts
  - src/components/pages/LandingPage.tsx
autonomous: true
requirements: [UX-02]
must_haves:
  truths:
    - "The landing page no longer uses 'testes psicométricos' or 'análise de perfil' — replaced with RNF-12a 'avaliação comportamental/cognitiva' framing — UX-02"
    - "The landing offers a 'Já sou candidato' CTA routing to the candidate login (/auth/login) — UX-02"
    - "The forbidden-strings CI guard now catches BOTH marketing terms, so a regression cannot silently reintroduce them — UX-02"
  artifacts:
    - path: "src/__tests__/guards/forbidden-strings.grep.test.ts"
      provides: "extended FORBIDDEN regex covering the 2 marketing terms + regex-correctness sub-tests"
      contains: "psicom"
    - path: "src/components/pages/LandingPage.tsx"
      provides: "honest copy + 'Já sou candidato' CTA to /auth/login"
      contains: "Já sou candidato"
  key_links:
    - from: "src/__tests__/guards/forbidden-strings.grep.test.ts"
      to: "src/components/pages/LandingPage.tsx"
      via: "grep scan of src/ fails on forbidden marketing copy"
      pattern: "an[aá]lise\\\\s+de\\\\s+perfil"
---

<objective>
Make the landing copy honest (RNF-12a) and lock it with the existing CI guard: remove "testes psicométricos" (LandingPage.tsx:65) and "análise de perfil" (:90), replace with "avaliação comportamental/cognitiva" framing, add a "Já sou candidato" CTA to the candidate login, and EXTEND the `forbidden-strings.grep.test.ts` guard so those two marketing terms can never silently return.

Purpose: The product language rule (RNF-12a / LGPD-04) forbids clinical/psychometric framing. The guard already covers 5 clinical terms but NOT the 2 marketing terms UX-02 targets (per 22-RESEARCH §Landing Copy, Pattern 3). Extending the guard first makes it fail on the current landing (RED), then the copy fix turns it green.
Output: extended guard + honest landing copy + CTA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-RESEARCH.md
@.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-CONTEXT.md

<interfaces>
LandingPage.tsx anchors (verified):
- :65  "Testes psicométricos com design moderno e tecnológico" → replace with behavioral/cognitive framing
- :89-90  "Acompanhe seu progresso e análise de perfil" → replace ("...e sua avaliação comportamental")
- :29-44  hero button row: `GlassButton` "Ver Vagas" → navigate('/vagas'); "Área do RH" → navigate('/auth/login-rh'). Add "Já sou candidato" → navigate('/auth/login').
- Candidate login route is `/auth/login` (routes.tsx:147-148). CONTEXT writes `/login` as shorthand — the real path is `/auth/login`.

forbidden-strings.grep.test.ts (verified):
- FORBIDDEN regex (:58-59) currently: `teste\s+psicol[oó]gico|teste\s+psicot[eé]cnico|psicot[eé]cnico|laudo\s+psicol[oó]gico|psic[oó]logo`
- RNF_12_TERMS array (:63-69) drives `it.each` regex-correctness sub-tests
- SCAN_ROOTS includes `src` recursively → LandingPage.tsx is already in scope
- `it('FORBIDDEN regex does NOT match the approved replacement copy')` (:113) — extend the approved-copy assertion set
</interfaces>

<notes>
- 22-RESEARCH Pattern 3 + §Code Examples: add `testes?\s+psicom[eé]tricos?` and `an[aá]lise\s+de\s+perfil` (accent-tolerant) to FORBIDDEN, and add the two terms to RNF_12_TERMS so the `it.each` sub-tests prove the regex matches them.
- The guard self-excludes `__tests__` from its own scan, so its regex literal (which names the terms) does not trip it.
- This guard runs in CI via `npm run test:run` (the existing `unit` job) — no new workflow. The CI wiring/measure-pin is Plan 22-06.
- Keep the replacement copy free of ALL forbidden terms (both the 5 existing + 2 new) — the guard scans the whole `src/` tree.
</notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the forbidden-strings guard to cover the 2 marketing terms</name>
  <files>src/__tests__/guards/forbidden-strings.grep.test.ts</files>
  <read_first>
    - src/__tests__/guards/forbidden-strings.grep.test.ts:58-115 (FORBIDDEN regex, RNF_12_TERMS, the sub-tests + the "does NOT match approved copy" assertion)
    - 22-RESEARCH.md §Architecture Patterns → Pattern 3 + §Code Examples ("Extend the forbidden-strings guard")
  </read_first>
  <behavior>
    - FORBIDDEN matches "testes psicométricos" and "teste psicometrico" (accent-tolerant, singular/plural).
    - FORBIDDEN matches "análise de perfil" and "analise de perfil".
    - FORBIDDEN does NOT match the approved copy "avaliação comportamental/cognitiva".
    - The whole-src scan (`it('no forbidden term appears in src/...')`) FAILS while LandingPage still has the old copy, and PASSES after Task 2.
  </behavior>
  <action>
    Extend the `FORBIDDEN` regex (line ~58) by appending two alternations: `testes?\s+psicom[eé]tricos?` and `an[aá]lise\s+de\s+perfil` (accent + singular/plural tolerant). Add the two human-readable terms ("testes psicométricos", "análise de perfil") to `RNF_12_TERMS` so the existing `it.each(RNF_12_TERMS)` regex-correctness sub-test proves the regex matches them. Extend the "does NOT match approved replacement" assertion if needed to cover the behavioral/cognitive replacement copy. Run the guard now — the whole-src scan SHOULD fail against the un-fixed LandingPage (RED); this is expected and confirms the guard is live.
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | grep -iE "forbidden|RNF-12|psicom|perfil" | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "psicom|an[aá]lise\\\\s\\+de\\\\s\\+perfil" src/__tests__/guards/forbidden-strings.grep.test.ts` shows both new alternations present in FORBIDDEN.
    - `RNF_12_TERMS` includes "testes psicométricos" and "análise de perfil"; the `it.each` sub-tests pass.
    - Before Task 2: the whole-src scan test fails, pointing at LandingPage.tsx:65 and/or :90 (RED confirms the guard bites).
  </acceptance_criteria>
  <done>The guard covers both marketing terms and currently flags the un-fixed landing (RED) — regression-proofing the copy.</done>
</task>

<task type="auto">
  <name>Task 2: Fix landing copy + add "Já sou candidato" CTA (turn guard green)</name>
  <files>src/components/pages/LandingPage.tsx</files>
  <read_first>
    - src/components/pages/LandingPage.tsx:29-44 (hero button row) + :65 + :89-90 (the two forbidden strings)
    - src/router/routes.tsx:147-148 (confirm candidate login route `/auth/login`)
    - 22-RESEARCH.md §Code Examples ("Landing copy fix + CTA")
  </read_first>
  <action>
    Replace the :65 copy "Testes psicométricos com design moderno e tecnológico" with a behavioral/cognitive-framed line (e.g. "Avaliação comportamental e cognitiva com design moderno e tecnológico"). Replace the :90 copy "Acompanhe seu progresso e análise de perfil" with an honest line (e.g. "Acompanhe seu progresso e sua avaliação comportamental"). Wording is Claude's discretion within RNF-12a — must contain NONE of the 7 forbidden terms. Add a "Já sou candidato" `GlassButton` in the hero button row (near :29-44), styled consistently with "Ver Vagas"/"Área do RH", `onClick={() => navigate('/auth/login')}` (the real candidate login route). Run the guard — the whole-src scan must now pass (GREEN).
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | grep -iE "forbidden|RNF-12" | tail -10; echo "--- residual forbidden strings in landing ---"; grep -niE "psicom[eé]tric|an[aá]lise de perfil" src/components/pages/LandingPage.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -niE "psicom[eé]tric|an[aá]lise de perfil" src/components/pages/LandingPage.tsx` returns 0 matches.
    - `grep -c "Já sou candidato" src/components/pages/LandingPage.tsx` >= 1 and its onClick navigates to `/auth/login`.
    - `npm run test:run` — the forbidden-strings whole-src scan passes (GREEN) and the full suite is green.
    - `npm run lint` count not increased by this task.
  </acceptance_criteria>
  <done>Landing copy is honest, the "Já sou candidato" CTA reaches the candidate login, and the extended guard is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Product copy → candidate (public) | Public-facing marketing text is an RNF-12a / LGPD compliance surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | Repudiation / Compliance | forbidden psychometric framing on the landing (RNF-12a/LGPD-04) | mitigate | Replace copy with behavioral/cognitive framing AND extend the CI grep guard so it cannot silently return |
| T-22-04-02 | Tampering | CTA routes to a raw/wrong destination | mitigate | Static `navigate('/auth/login')` to the verified candidate login route (routes.tsx:147-148) — no user-controlled URL |
| T-22-04-SC | Tampering | package-manager installs | accept | Zero net-new packages; per 22-RESEARCH §Package Legitimacy Audit, slopcheck N/A |
</threat_model>

<verification>
- `npm run test:run` green including the extended forbidden-strings guard.
- No forbidden term remains anywhere in `src/` (guard whole-src scan passes).
- `npm run lint` not increased by the copy/CTA change.
</verification>

<success_criteria>
The landing reads honestly under RNF-12a, offers a "Já sou candidato" CTA to `/auth/login`, and the forbidden-strings guard now enforces both marketing terms — a silent copy regression is impossible.
</success_criteria>

<output>
Create `.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-04-SUMMARY.md` when done.
</output>
