---
phase: 22-rede-de-testes-destravamento-varredura-de-honestidade
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/login-flow.spec.ts
  - e2e/auth-hydration.spec.ts
  - e2e/vagas-browse.spec.ts
  - e2e/navegacao.spec.ts
  - e2e/explicacao-flow.spec.ts
  - e2e/candidatura-submit.spec.ts
  - e2e/perfil.spec.ts
  - e2e/prova-cognitiva.spec.ts
  - e2e/password-recovery-flow.spec.ts
  - e2e/fixtures/a11y-session.ts
  - e2e/README.md
  - .env.test.example
  - src/__tests__/guards/no-hardcoded-test-creds.grep.test.ts
autonomous: true
requirements: [CI-08]
must_haves:
  truths:
    - "No real test-account email/password literal appears anywhere under e2e/ — specs read from env and skip-if-unset — CI-08"
    - "`.env.test.example` documents every required test-credential KEY (no values), and `.env.test` stays gitignored — CI-08"
    - "A CI guard fails the build if a hardcoded credential literal reappears under e2e/ — CI-08"
  artifacts:
    - path: ".env.test.example"
      provides: "documented, value-free key list (candidato + admin + gating + supabase)"
      contains: "TEST_USER_EMAIL"
    - path: "src/__tests__/guards/no-hardcoded-test-creds.grep.test.ts"
      provides: "Vitest grep guard scanning e2e/ for real-cred literals"
      contains: "e2e"
  key_links:
    - from: "src/__tests__/guards/no-hardcoded-test-creds.grep.test.ts"
      to: "e2e/*.spec.ts"
      via: "fs scan fails on hardcoded cred literals"
      pattern: "fernando@beautysmile|teste123"
---

<objective>
Remove every hardcoded real test-account credential from the repo: strip the `|| 'fernando@beautysmile.com.br'` / `'teste123'` fallbacks from the E2E specs and fixtures (specs read env vars and skip-if-unset), scrub the stale creds from `e2e/README.md`, rewrite the outdated `.env.test.example` to document all required KEYS (never values), and add a CI grep guard so a hardcoded credential can never silently return.

Purpose: Real credentials in git are an Info-Disclosure surface and violate the CLAUDE.md hard rule (no secrets in repo). The skip-if-unset pattern already exists (vagas-browse.spec.ts:106) — this generalizes it and locks it with a guard (per 22-RESEARCH §Test Credentials, §Security Domain).
Output: de-hardcoded specs + fixtures, honest `.env.test.example`, and a `no-hardcoded-test-creds` guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-RESEARCH.md
@.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-CONTEXT.md

<interfaces>
Established skip-if-unset pattern (reuse — do NOT invent a new scheme):
```ts
// vagas-browse.spec.ts:106 / auth-hydration.spec.ts:51 / login-flow.spec.ts:39
const REAL_AUTH = process.env.E2E_AUTH_TEST_USERS === 'true'
const describeRealAuth = REAL_AUTH ? test.describe : test.describe.skip
// per-test:  test.skip(!process.env.TEST_USER_EMAIL, 'Requires TEST_USER_EMAIL (see .env.test.example)')
```
Hardcoded sites (grep-confirmed, 22-RESEARCH §Code Examples):
`login-flow.spec.ts:21`, `auth-hydration.spec.ts`, `vagas-browse.spec.ts:15`, `perfil.spec.ts:42`, `prova-cognitiva.spec.ts`, `navegacao.spec.ts`, `explicacao-flow.spec.ts`, `candidatura-submit.spec.ts`, `password-recovery-flow.spec.ts`, `e2e/fixtures/a11y-session.ts`, and `e2e/README.md`.
Env keys in play (enumerate from the specs + ci.yml at execute time): candidato (`TEST_USER_EMAIL`/`TEST_USER_PASSWORD`/`TEST_USER_NAME`), admin (`TEST_ADMIN_*`), gating (`E2E_AUTH_TEST_USERS`, `E2E_CANDIDATURA_ID`, `E2E_VAGA_ID`), supabase (`TEST_SUPABASE_URL`/`TEST_SUPABASE_ANON_KEY` per ci.yml:35-36, `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`).
</interfaces>

<notes>
- 22-RESEARCH §Runtime State Inventory: `.env.test` (gitignored) holds REAL creds and STAYS gitignored — do NOT touch it, do NOT print its values. `.env.test.example` (tracked) is STALE (still shows `fernando@…`/`teste123`, omits admin+gating keys) — this plan fixes it with KEYS ONLY.
- CONTEXT + 22-RESEARCH Open Question 2: keep real-auth E2E OFF by default (`E2E_AUTH_TEST_USERS=''` in ci.yml). CI-08 is HYGIENE — remove hardcoded fallbacks + document keys; do NOT enable the real-auth suite in CI and do NOT hard-require the secrets (absence must not break the pipeline).
- The anon key is public-by-design (already in ci.yml placeholders) — it is NOT a secret; only the email/password pairs are.
- The new guard lives under `src/__tests__/guards/` so Vitest (`**/__tests__/**`) picks it up and it rides the existing `unit` job — mirror `forbidden-strings.grep.test.ts` structure (recursive fs walk, self-exclude `__tests__`/`node_modules`).
- login-flow.spec.ts is also edited by Plan 22-06 (unused-import fix) — 22-06 depends on this plan, so the edits are sequential.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip hardcoded credential fallbacks from specs, fixtures, and README</name>
  <files>e2e/login-flow.spec.ts, e2e/auth-hydration.spec.ts, e2e/vagas-browse.spec.ts, e2e/navegacao.spec.ts, e2e/explicacao-flow.spec.ts, e2e/candidatura-submit.spec.ts, e2e/perfil.spec.ts, e2e/prova-cognitiva.spec.ts, e2e/password-recovery-flow.spec.ts, e2e/fixtures/a11y-session.ts, e2e/README.md</files>
  <read_first>
    - 22-RESEARCH.md §Code Examples ("De-hardcode test creds") + §Test Credentials
    - e2e/login-flow.spec.ts:20-40 (the `TEST_USER` block with `|| 'fernando@…'` + the existing REAL_AUTH gate)
    - e2e/vagas-browse.spec.ts:106 + e2e/auth-hydration.spec.ts:51 (the established skip-if-unset pattern to mirror)
  </read_first>
  <action>
    Enumerate every hardcoded credential literal: `grep -rnE "fernando@beautysmile|teste123|Candidato@2026|E2eAdmin|@teste\.com" e2e/`. In each spec/fixture, replace `process.env.X || 'literal'` fallbacks with `process.env.X!` (non-null assertion) and ensure a skip-if-unset guard covers the block — reuse the existing `REAL_AUTH`/`describeRealAuth` gate where present, or add `test.skip(!process.env.X, 'Requires X (see .env.test.example)')` per the vagas-browse.spec.ts:106 pattern. Do NOT introduce a new env-gating scheme. Scrub the stale creds from `e2e/README.md` (replace with `TEST_USER_EMAIL` / `TEST_USER_PASSWORD` key references and a pointer to `.env.test.example`). Do NOT touch `.env.test`. Keep `INVALID_CREDENTIALS` fixtures (e.g. `invalido@teste.com`) — those are intentional negative-path literals, not real accounts; but ensure the guard's pattern (Task 2) does NOT flag them (they are not real-account emails).
  </action>
  <verify>
    <automated>grep -rnE "fernando@beautysmile|teste123|Candidato@2026|E2eAdmin" e2e/ | grep -v "\.env" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "fernando@beautysmile|teste123|Candidato@2026|E2eAdmin" e2e/` returns 0 (specs, fixtures, README all clean).
    - Every de-hardcoded spec block is either behind `describeRealAuth`/`REAL_AUTH` or has a `test.skip(!process.env.X, …)` guard — no bare `process.env.X!` that would throw when unset in a non-skipped test.
    - `.env.test` is untouched (`git status` shows no change to it).
  </acceptance_criteria>
  <done>No real test-account credential literal remains under e2e/; specs skip cleanly when the env is unset.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite .env.test.example (keys only) + add the no-hardcoded-creds guard</name>
  <files>.env.test.example, src/__tests__/guards/no-hardcoded-test-creds.grep.test.ts</files>
  <read_first>
    - .env.test.example (current stale content — `fernando@…`/`teste123`)
    - .github/workflows/ci.yml:32-36 (the `TEST_SUPABASE_URL`/`TEST_SUPABASE_ANON_KEY` + `VITE_*` keys) — enumerate the real key names from the specs + ci.yml
    - src/__tests__/guards/forbidden-strings.grep.test.ts (mirror its recursive-walk / self-exclude structure for the new guard)
    - 22-RESEARCH.md §Runtime State Inventory (the 10-key list) + §Validation Architecture → Wave 0 Gaps
  </read_first>
  <action>
    Rewrite `.env.test.example` to document EVERY required key with a value-free placeholder + a one-line comment: candidato (`TEST_USER_EMAIL`, `TEST_USER_PASSWORD`, `TEST_USER_NAME`), admin (`TEST_ADMIN_EMAIL`, `TEST_ADMIN_PASSWORD`), gating (`E2E_AUTH_TEST_USERS`, `E2E_CANDIDATURA_ID`, `E2E_VAGA_ID`), supabase (`TEST_SUPABASE_URL`, `TEST_SUPABASE_ANON_KEY` and/or `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` — match what the specs/ci.yml actually read). Values MUST be placeholders (e.g. `TEST_USER_EMAIL=` or `your-test-candidate-email`) — NEVER a real email/password. Then create `src/__tests__/guards/no-hardcoded-test-creds.grep.test.ts` mirroring `forbidden-strings.grep.test.ts`: recursively walk `e2e/`, self-exclude `__tests__`/`node_modules`, and FAIL if any file matches a real-cred pattern (`fernando@beautysmile`, `teste123`, `Candidato@2026`, `E2eAdmin`, and the `@beautysmile.com.br`/`@teste.com` real-account password-pair shape) — while NOT flagging the intentional `INVALID_CREDENTIALS` negative literals. Include regex-correctness sub-tests (like the RNF_12 block).
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | grep -iE "hardcoded|cred" | tail -15; echo "--- example has no real creds ---"; grep -cE "fernando@beautysmile|teste123" .env.test.example</automated>
  </verify>
  <acceptance_criteria>
    - `.env.test.example` lists all required keys with placeholder (non-real) values; `grep -cE "fernando@beautysmile|teste123" .env.test.example` == 0.
    - `src/__tests__/guards/no-hardcoded-test-creds.grep.test.ts` exists, scans `e2e/`, and its whole-scan test passes (green after Task 1) with regex sub-tests proving the patterns match the banned literals.
    - `npm run test:run` green (the new guard + existing suite).
    - `git check-ignore .env.test` still reports it ignored (untouched, not committed).
  </acceptance_criteria>
  <done>`.env.test.example` documents the keys with no values, and the CI guard permanently blocks hardcoded credentials from returning under e2e/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo (git-tracked) → public | Anything committed is world-readable; real credentials must never cross this boundary |
| CI runner env/secrets → job logs | GitHub secrets must not be echoed into logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05-01 | Information Disclosure | real test-account creds hardcoded in specs/fixtures/README | mitigate | Remove all literals; specs read env + skip-if-unset; add a CI guard blocking regressions |
| T-22-05-02 | Information Disclosure | `.env.test.example` leaking real values | mitigate | Rewrite with KEYS ONLY (placeholder values); `.env.test` (real values) stays gitignored |
| T-22-05-03 | Information Disclosure | secrets leaked via CI logs | accept | Keep `TEST_*` as GitHub secrets, never echoed; anon key is public-by-design (already in ci.yml) |
| T-22-05-SC | Tampering | package-manager installs | accept | Zero net-new packages; per 22-RESEARCH §Package Legitimacy Audit, slopcheck N/A |
</threat_model>

<verification>
- `grep -rnE "fernando@beautysmile|teste123|Candidato@2026|E2eAdmin" e2e/` → 0.
- `.env.test.example` has key names, no real values; `.env.test` untouched + still gitignored.
- `npm run test:run` green including the new `no-hardcoded-test-creds` guard.
</verification>

<success_criteria>
No real test-account credential exists anywhere under e2e/ or in tracked env-example files, the required keys are documented value-free, and a CI guard prevents any hardcoded credential from silently returning — with the real-auth E2E suite still OFF by default.
</success_criteria>

<output>
Create `.planning/phases/22-rede-de-testes-destravamento-varredura-de-honestidade/22-05-SUMMARY.md` when done.
</output>
