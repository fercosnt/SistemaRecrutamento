---
phase: 26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/avaliacao/components/RedacaoEditorScreen.tsx
  - src/features/avaliacao/components/DevolutivaBigFiveView.tsx
  - src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx
  - src/features/explicacao/components/SolicitarRevisaoCTA.tsx
  - src/components/pages/SuporteRHPage.tsx
  - src/features/avaliacao/components/AvaliacaoContainer.tsx
  - src/__tests__/guards/wait-state-copy.grep.test.ts
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "UX-01 (D-Honest-Copy): all 6 candidate/RH wait-state screens use the canonical pt-BR string 'Acompanhe o andamento pelo seu painel.' — every 'avisaremos … por e-mail' / 'receberá … por e-mail' promise is removed entirely (the panel is the source of truth; there is no e-mail infra)."
    - "The 6 screens are: AvaliacaoContainer:209 (all-done), RedacaoEditorScreen:278, DevolutivaBigFiveView:157, ProvaCognitivaScreen:82 (+prose :18), SolicitarRevisaoCTA:45, SuporteRHPage:162-163 — matching the canonical patterns at VagaDetalhePage:319 / DashboardCandidatoPage:186."
    - "A new scoped CI grep guard bans re-introduction of the e-mail-promise pattern in exactly these 6 wait-state files, and a sub-test proves it does NOT flag the legitimate LGPD consent copy in AutorizacoesStep (:58/93/185) nor password-reset / RH-notify-toggle copy elsewhere."
    - "Only strings change — no layout/color/typography change (26-UI-SPEC §Copywriting Contract Notes); LGPD-04 product-language guard stays green (no 'teste psicológico')."
  artifacts:
    - path: "src/__tests__/guards/wait-state-copy.grep.test.ts"
      provides: "scoped regression net banning the e-mail-promise pattern in the 6 wait-state files"
      contains: "Acompanhe o andamento pelo seu painel"
  key_links:
    - from: "src/__tests__/guards/wait-state-copy.grep.test.ts"
      to: "the 6 wait-state screen files"
      via: "node:fs scan + regex ban /avisaremos[\\s\\S]*por e-?mail/i"
      pattern: "avisaremos"
---

<objective>
Replace every dishonest "avisaremos por e-mail" wait-state promise across the 6 candidate/RH screens with the canonical string "Acompanhe o andamento pelo seu painel." (UX-01), and add a scoped CI grep guard that bans the e-mail-promise pattern from re-appearing in those files while permitting legitimate transactional/consent e-mail copy elsewhere.

Purpose: The system has no e-mail notification infra; the dashboard/painel is the real status source. Honest copy + a durable regression net.
Output: 6 string edits (5 standalone screens + AvaliacaoContainer all-done line) + 1 new grep guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-CONTEXT.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-UI-SPEC.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md

<interfaces>
<!-- Canonical string (locked by CONTEXT + UI-SPEC): "Acompanhe o andamento pelo seu painel." -->
<!-- Correct reference patterns: VagaDetalhePage.tsx:319 , DashboardCandidatoPage.tsx:186 . -->
<!-- Verbatim replacement map (26-UI-SPEC §Copywriting Contract table):
       AvaliacaoContainer.tsx:209  → "Você concluiu todas as avaliações desta etapa. Acompanhe o andamento pelo seu painel."
       RedacaoEditorScreen.tsx:278 → "Acompanhe o andamento pelo seu painel."
       DevolutivaBigFiveView.tsx:157 → "Volte em alguns instantes. Acompanhe o andamento pelo seu painel."
       ProvaCognitivaScreen.tsx:82 (postSubmit) → "Prova registrada. Acompanhe o andamento pelo seu painel."  (+ update prose :18 to quote it)
       SolicitarRevisaoCTA.tsx:45 (dialogBody) → "Sua solicitação será enviada à equipe responsável, que revisará a decisão. Acompanhe o andamento pelo seu painel."
       SuporteRHPage.tsx:162-163 → "Recebemos sua solicitação e nossa equipe técnica irá analisá-la em breve. Acompanhe o andamento pelo seu painel." -->
<!-- Do NOT touch AutorizacoesStep.tsx:58/93/185 (legitimate LGPD consent). Guard analogs: n8n-bundle.grep.test.ts + forbidden-strings.grep.test.ts (node:fs + comment-aware + regex-correctness sub-test). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the 6 wait-state strings with the canonical panel copy</name>
  <files>src/features/avaliacao/components/RedacaoEditorScreen.tsx, src/features/avaliacao/components/DevolutivaBigFiveView.tsx, src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx, src/features/explicacao/components/SolicitarRevisaoCTA.tsx, src/components/pages/SuporteRHPage.tsx, src/features/avaliacao/components/AvaliacaoContainer.tsx</files>
  <read_first>
    - src/features/avaliacao/components/RedacaoEditorScreen.tsx (:278 current "Avisaremos sobre os próximos passos por e-mail.")
    - src/features/avaliacao/components/DevolutivaBigFiveView.tsx (:157 current "Volte em alguns instantes — avisaremos por e-mail quando estiver pronta.")
    - src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx (:82 postSubmit + :18 prose doc-comment)
    - src/features/explicacao/components/SolicitarRevisaoCTA.tsx (:45 COPY.dialogBody "…Avisaremos você sobre o resultado.")
    - src/components/pages/SuporteRHPage.tsx (:162-163 "…Você receberá atualizações por e-mail.")
    - src/features/avaliacao/components/AvaliacaoContainer.tsx (:209 all-done body — ONLY this line; do NOT touch deriveCards/CONTAINER_TESTE_CONFIG, those are 26-06)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-UI-SPEC.md §Copywriting Contract (verbatim map)
  </read_first>
  <action>
    Apply the verbatim replacement map from 26-UI-SPEC §Copywriting Contract to each of the 6 files (see the interfaces block for the exact target strings). Change ONLY the string literals — no layout, className, icon, color, or typography change. For ProvaCognitivaScreen, update BOTH the `postSubmit` string (:82) AND the doc-comment prose (:18) so it quotes the new `postSubmit` verbatim (keeps guard + docs honest). For AvaliacaoContainer, replace ONLY the :209 all-done body text; leave the rest of the file for plan 26-06 (which runs later, Wave 3, and must not revert this copy). Remove every "por e-mail" fragment — do not soften or keep it. Preserve the LGPD-04 product-language rule (no "teste psicológico").
  </action>
  <acceptance_criteria>
    - Canonical string present in all 6 files: `grep -rl "Acompanhe o andamento pelo seu painel" <the 6 files>` returns all 6.
    - No e-mail promise remains in the 6 files: `grep -REic "avisaremos[^.]*e-?mail|receber[áa][^.]*e-?mail|próximos passos por e-?mail" <the 6 files>` == 0.
    - ProvaCognitivaScreen prose (:18) quotes the new string (`grep -c "Acompanhe o andamento pelo seu painel" ProvaCognitivaScreen.tsx` >= 2 — postSubmit + prose).
    - `npm run test:run` green (existing DevolutivaBigFiveView / component tests still pass) AND `npm run lint` (tsc) not above 107.
    - LGPD-04 guard stays green (no forbidden product-language term introduced).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "cd '/Users/fernando/Cursor Repo/DB Sistema de recrutamento' && F='src/features/avaliacao/components/RedacaoEditorScreen.tsx src/features/avaliacao/components/DevolutivaBigFiveView.tsx src/features/avaliacao-cognitiva/components/ProvaCognitivaScreen.tsx src/features/explicacao/components/SolicitarRevisaoCTA.tsx src/components/pages/SuporteRHPage.tsx src/features/avaliacao/components/AvaliacaoContainer.tsx' && test \$(grep -rl 'Acompanhe o andamento pelo seu painel' \$F | wc -l | tr -d ' ') -eq 6 && test \$(grep -REic 'avisaremos[^.]*e-?mail|receber[áa][^.]*e-?mail' \$F) -eq 0 && echo OK"</automated>
  </verify>
  <done>All 6 wait-state screens carry the canonical panel copy, no e-mail promise remains, only strings changed, and the suite + tsc are green.</done>
</task>

<task type="auto">
  <name>Task 2: Author the scoped wait-state-copy CI grep guard</name>
  <files>src/__tests__/guards/wait-state-copy.grep.test.ts</files>
  <read_first>
    - src/__tests__/guards/forbidden-strings.grep.test.ts (:1-95 skeleton — node:fs collectFiles, ROOT resolve, FORBIDDEN regex, regex-correctness sub-test)
    - src/__tests__/guards/n8n-bundle.grep.test.ts (node:fs + comment-aware line filter analog)
    - src/features/cadastro/components/steps/AutorizacoesStep.tsx (:58/93/185 legitimate consent copy that MUST NOT be flagged)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md §Code Example 7 (guard spec) + 26-UI-SPEC §CI grep guard
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md §wait-state-copy guard
  </read_first>
  <action>
    Create `src/__tests__/guards/wait-state-copy.grep.test.ts` copying the `forbidden-strings.grep.test.ts` skeleton (pure `node:fs`, `ROOT = resolve(__dirname, '../../..')`, comment-aware line filter, a regex-correctness sub-test). SCOPE it to EXACTLY the 6 wait-state files (an explicit allowlist array of paths) — do NOT global-ban e-mail. Ban patterns `/avisaremos[\s\S]*por e-?mail/i` and `/receber[áa][\s\S]*por e-?mail/i`. Add: (a) a positive sub-test asserting each of the 6 files contains the canonical string "Acompanhe o andamento pelo seu painel."; (b) an allow sub-test asserting `AutorizacoesStep.tsx` consent copy is NOT flagged by the ban regex (and note password-reset / RH "Notificar candidato por email" toggle are out of scope). The guard rides the existing `npm run test:run` CI leg — no new workflow.
  </action>
  <acceptance_criteria>
    - File exists and scopes to the 6 files (an explicit path allowlist, not a global src walk): `grep -c "AvaliacaoContainer\|SuporteRHPage\|SolicitarRevisaoCTA\|ProvaCognitivaScreen\|RedacaoEditorScreen\|DevolutivaBigFiveView" file` >= 6.
    - Ban regex present: `grep -Ec "avisaremos|receber" file` >= 1; canonical positive-assertion present: `grep -c "Acompanhe o andamento pelo seu painel" file` >= 1.
    - No-false-positive sub-test references AutorizacoesStep: `grep -c "AutorizacoesStep" file` >= 1.
    - `npx vitest run src/__tests__/guards/wait-state-copy.grep.test.ts` passes (green because Task 1 already fixed the copy) AND `npm run lint` (tsc) not above 107.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "cd '/Users/fernando/Cursor Repo/DB Sistema de recrutamento' && f=src/__tests__/guards/wait-state-copy.grep.test.ts && test -f \$f && grep -c 'Acompanhe o andamento pelo seu painel' \$f && grep -c 'AutorizacoesStep' \$f && npx vitest run \$f && echo OK"</automated>
  </verify>
  <done>A scoped grep guard bans the e-mail-promise pattern in the 6 wait-state files, asserts the canonical string is present, and proves the consent copy is not false-flagged — green after Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| product copy → candidate/RH | Dishonest promises (e-mail that never arrives) are a trust/UX integrity issue; no data boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Repudiation/Integrity | dishonest "we'll e-mail you" copy re-introduced by a later edit | mitigate | scoped CI grep guard bans the pattern in the 6 wait-state files; rides npm run test:run |
| T-26-04-02 | (LGPD product-language) | a copy edit introduces a forbidden clinical term | mitigate | existing forbidden-strings guard stays green; new copy uses neutral language only |
| T-26-04-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase; N/A |
</threat_model>

<verification>
- Acceptance criteria pass; `npm run test:run` green; tsc ≤ 107.
- Note for 26-06 (Wave 3): the AvaliacaoContainer :209 copy is already corrected here — 26-06 must not revert it.
</verification>

<success_criteria>
All 6 wait-state screens carry the canonical panel copy (no e-mail promise anywhere), only strings changed, and the scoped grep guard is the durable UX-01 regression net proven green.
</success_criteria>

<output>
Create `.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-04-SUMMARY.md` when done.
</output>
