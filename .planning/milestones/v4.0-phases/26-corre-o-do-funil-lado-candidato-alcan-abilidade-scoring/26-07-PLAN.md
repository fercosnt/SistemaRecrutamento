---
phase: 26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring
plan: 07
type: execute
wave: 4
depends_on: ['26-01', '26-02', '26-03', '26-06']
files_modified:
  - supabase/tests/funil10_reinscricao_smoke.sql
autonomous: false
requirements: [FUNIL-01, FUNIL-07, FUNIL-08, FUNIL-10, FUNIL-12, n8n-2nd-leak]
must_haves:
  truths:
    - "[BLOCKING] All 4 Phase-26 migrations are LIVE on PROD via Supabase MCP apply_migration (NOT db push — 42601): 20260712100001 (pontuar_sjt v2), 100002 (pontuar_cognitivo gate), 100003 (get_avaliacao_status), 100004 (n8n trigger); version rows reconciled so db push reports up-to-date."
    - "FUNIL-10 (D-Reinscricao): the legacy UNVERSIONED unique index/constraint on candidaturas(candidato_id, vaga_id) WITHOUT a deleted_at filter is discovered via pg_indexes/pg_constraint, confirmed, and DROPped via MCP execute_sql — the partial keeper index (WHERE deleted_at IS NULL) remains; insert→soft-delete→re-insert no longer 23505s. (If the offender does not exist, FUNIL-10 is a proven no-op — the smoke still validates re-inscription.)"
    - "All SQL behavioral smokes PASS live on PROD (NOTICE=PASS, zero EXCEPTION): funil01_pontuar_sjt (dedup/full-battery-denominator/completeness/re-submit-lock/foreign-42501/no-candidaturas-write), funil08_pontuar_cognitivo (async + interview), funil12_status_rpc (booleans-only + IDOR-42501), funil10_reinscricao, n8n_novo_candidato (graceful-skip/no-PII/candidatos-unchanged). Structural greps alone are insufficient (Phase 24/25 lesson) — the smokes are the acceptance gate."
    - "RNF-07a proven live: after SJT + cognitivo scoring, candidaturas.status/etapa_atual are unchanged; no auto-rejection. Answer-key protection proven: get_avaliacao_status exposes no verdict."
    - "UAT prep (Open Q1): any [TESTE]-funil candidatura carrying an old-algorithm scores_candidato MC row gets a scoped one-time reset so a fresh complete submit proves the new logic — test data only, never a production candidate."
  artifacts:
    - path: "supabase/tests/funil10_reinscricao_smoke.sql"
      provides: "insert→soft-delete→re-insert smoke + pg_indexes assertion (only the partial index remains)"
      contains: "deleted_at"
  key_links:
    - from: "PROD candidaturas schema"
      to: "candidaturas_candidato_vaga_unique_idx (partial keeper)"
      via: "DROP the unfiltered twin, keep WHERE deleted_at IS NULL"
      pattern: "deleted_at IS NULL"
    - from: "Supabase MCP apply_migration"
      to: "PROD (pontuar_sjt / pontuar_cognitivo / get_avaliacao_status / trg_n8n_novo_candidato)"
      via: "apply each migration file, reconcile version rows, run smokes"
      pattern: "apply_migration"
---

<objective>
[BLOCKING · non-autonomous · writes PROD] Apply the four Phase-26 migration files to PROD via Supabase MCP `apply_migration`, discover + drop the legacy unfiltered unique index on `candidaturas` (FUNIL-10, an execution-time PROD step — not in any migration file), reconcile version rows, run every SQL behavioral smoke live, and confirm all PASS. This is the phase acceptance gate: build/type checks pass WITHOUT the apply (types come from config, not the live DB) — a false positive.

Purpose: The migrations authored in 26-01/02/03 and their smokes are inert until applied. This plan makes FUNIL-01/07/08/12 + n8n live and proves them behaviorally, and closes FUNIL-10 in PROD.
Output: Live schema changes + reconciled ledger + all smokes green + funil10 smoke file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-CONTEXT.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md
@.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md

<interfaces>
<!-- Apply mechanic (CLAUDE.md §Migrations + M2-M4 precedent 24-08/25-07): Supabase MCP apply_migration writes the version row itself and bypasses the 42601 "cannot insert multiple commands" error that db push --linked throws on $$ bodies. Do NOT run db push for these. NO BEGIN/COMMIT wrapper in the files. -->
<!-- Migration files (from 26-01/02/03): 20260712100001_funil01_pontuar_sjt_v2.sql, 20260712100002_funil08_pontuar_cognitivo_gate.sql, 20260712100003_funil12_get_avaliacao_status.sql, 20260712100004_n8n_novo_candidato.sql. -->
<!-- FUNIL-10 discovery (26-RESEARCH §Code Example 5): enumerate unique indexes on candidaturas(candidato_id,vaga_id) via pg_index + pg_get_indexdef; the KEEPER has WHERE (deleted_at IS NULL) (20260425000004), the OFFENDER has no WHERE. Also check pg_constraint (may be a UNIQUE CONSTRAINT → ALTER TABLE ... DROP CONSTRAINT). Verify existence BEFORE dropping (Assumption A1 — may be a no-op). -->
<!-- Smoke files (from 26-01/02/03): funil01_pontuar_sjt_smokes.sql, funil08_pontuar_cognitivo_smokes.sql, funil12_status_rpc_smoke.sql, n8n_novo_candidato_smoke.sql. Run via MCP execute_sql / SQL Editor. -->
<!-- Vault secret n8n_webhook_base is Fernando's human-action — do NOT create it here; the trigger graceful-skips (RETURN NEW) while NULL, which is the expected live behavior of the n8n smoke. -->
<!-- database.types.ts regen + baseline/ledger reconstruction (DBMIG-01) are Phase 27 — NOT this plan. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the 4 migrations to PROD via MCP + reconcile version rows</name>
  <files>(PROD — Supabase MCP; no repo file)</files>
  <read_first>
    - supabase/migrations/20260712100001_funil01_pontuar_sjt_v2.sql
    - supabase/migrations/20260712100002_funil08_pontuar_cognitivo_gate.sql
    - supabase/migrations/20260712100003_funil12_get_avaliacao_status.sql
    - supabase/migrations/20260712100004_n8n_novo_candidato.sql
    - CLAUDE.md §Migrations (the 42601 workaround + MCP apply mechanic)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md §Migration mechanics
  </read_first>
  <action>
    Apply each of the four migration files to PROD using Supabase MCP `apply_migration` (project `isljnozzlvckrgjjbjwp`), in dependency order (pontuar_sjt, pontuar_cognitivo gate, get_avaliacao_status, n8n trigger). `apply_migration` writes the version row itself and bypasses 42601 — do NOT use `supabase db push`. After applying, reconcile the local ledger (filename-vs-version drift is cosmetic; full reconstruction is Phase 27) and confirm `supabase db push --linked` reports the remote is up to date (or note the known version-row drift consistent with 24-08/25-07). Confirm each function/trigger exists live via a quick `execute_sql` (to_regprocedure on pontuar_sjt, pontuar_cognitivo 5-arg, get_avaliacao_status; trigger present on candidatos). Do NOT regen database.types.ts (Phase 27).
  </action>
  <acceptance_criteria>
    - Live `execute_sql` confirms all four objects exist: pontuar_sjt(uuid,jsonb), pontuar_cognitivo(uuid,jsonb,text,int,jsonb), get_avaliacao_status(uuid), and trigger trg_n8n_novo_candidato on public.candidatos.
    - pontuar_cognitivo etapa gate includes 'avaliacao_assincrona' live (verify via pg_get_functiondef).
    - Version rows for the four migrations exist in supabase_migrations (apply_migration wrote them); db push reports up-to-date or the documented cosmetic drift only.
  </acceptance_criteria>
  <verify>
    <automated>echo "Executed via Supabase MCP apply_migration + execute_sql verification (to_regprocedure/pg_get_functiondef/trigger presence). Evidence recorded in 26-07-SUMMARY.md."</automated>
  </verify>
  <done>All four migrations are live on PROD, objects verified present, version rows reconciled; no db push used.</done>
</task>

<task type="auto">
  <name>Task 2: FUNIL-10 discover + drop the legacy unfiltered index + author the reinscription smoke + UAT reset</name>
  <files>supabase/tests/funil10_reinscricao_smoke.sql</files>
  <read_first>
    - supabase/migrations/20260425000004_candidaturas_unique_constraint.sql (:41-43 the KEEPER partial index WHERE deleted_at IS NULL)
    - supabase/tests/sec05_08_smokes.sql (impersonation/cleanup idiom for the smoke)
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-RESEARCH.md §Code Example 5 + Pitfall 7 + §Open Q1 (UAT reset) + Assumption A1
    - .planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-PATTERNS.md §funil10 drop
  </read_first>
  <action>
    Via MCP `execute_sql` (read-only first): enumerate unique indexes on `public.candidaturas(candidato_id, vaga_id)` (pg_index + pg_get_indexdef) AND check pg_constraint for a UNIQUE CONSTRAINT on the same columns. Identify the OFFENDER = the unique index/constraint on (candidato_id, vaga_id) WITHOUT a `WHERE (deleted_at IS NULL)` clause; the KEEPER = the partial one from 20260425000004. Record both defs in the summary. If the offender exists, `DROP INDEX IF EXISTS public.<offender>` (or `ALTER TABLE public.candidaturas DROP CONSTRAINT IF EXISTS <offender>` if it is a constraint) — keep the partial index. If it does NOT exist, record FUNIL-10 as a proven no-op (re-inscription already works) and proceed to the smoke either way.
    Author `supabase/tests/funil10_reinscricao_smoke.sql` (disposable fixture): insert a candidatura for a (candidato,vaga), soft-delete it (set deleted_at), re-insert the same (candidato,vaga) → assert NO 23505 (NOTICE=PASS/EXCEPTION=FAIL); then assert via pg_indexes that ONLY the partial (WHERE deleted_at IS NULL) unique index on (candidato_id,vaga_id) remains. ROLLBACK-free cleanup of the disposable rows.
    UAT reset (Open Q1, test data ONLY): if a [TESTE]-funil candidatura used for live UAT carries an old-algorithm scores_candidato MC row (which the new re-submit lock would block), run a scoped `DELETE FROM scores_candidato WHERE candidatura_id = '<teste>' AND tipo='sjt' AND subtipo='mc'` so a fresh complete submit can prove the new logic. NEVER touch a production candidate's rows.
  </action>
  <acceptance_criteria>
    - The discovery output (both index defs, or a documented absence of the offender) is recorded in 26-07-SUMMARY.md.
    - After the drop (if any), a live pg_indexes query shows exactly ONE unique index on (candidato_id,vaga_id) and it carries `WHERE (deleted_at IS NULL)`.
    - `supabase/tests/funil10_reinscricao_smoke.sql` exists and asserts insert→soft-delete→re-insert without 23505 (`grep -Ec "RAISE NOTICE 'PASS" file` >= 1) + the pg_indexes assertion.
    - Running the funil10 smoke live returns PASS (no 23505 on re-insert).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "cd '/Users/fernando/Cursor Repo/DB Sistema de recrutamento' && f=supabase/tests/funil10_reinscricao_smoke.sql && test -f \$f && grep -c 'deleted_at' \$f && test \$(grep -Ec \"RAISE NOTICE 'PASS\" \$f) -ge 1 && echo OK"</automated>
  </verify>
  <done>The legacy unfiltered index is discovered and (if present) dropped, only the partial keeper remains, the reinscription smoke passes live, and any UAT test-data score row is reset — FUNIL-10 closed or proven a no-op.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run all Phase-26 behavioral smokes on PROD + confirm PASS</name>
  <files>(PROD — Supabase MCP execute_sql; no repo file)</files>
  <what-built>
    All four migrations are live and the FUNIL-10 index is resolved. This step runs every Phase-26 SQL behavioral smoke against PROD via MCP execute_sql / SQL Editor and confirms each emits NOTICE=PASS with zero EXCEPTION — the load-bearing acceptance gate (structural greps are insufficient; Phase 24/25 smokes caught leaks structural checks missed).
  </what-built>
  <action>
    Run each Phase-26 SQL behavioral smoke against PROD via Supabase MCP `execute_sql` (or the SQL Editor), one file at a time, capturing the NOTICE output as evidence for 26-07-SUMMARY.md: funil01_pontuar_sjt_smokes.sql, funil08_pontuar_cognitivo_smokes.sql, funil12_status_rpc_smoke.sql, funil10_reinscricao_smoke.sql, n8n_novo_candidato_smoke.sql. Then present the results for human confirmation (see how-to-verify). This task writes no repo file; it exercises the applied PROD surfaces.
  </action>
  <how-to-verify>
    1. Run each smoke via Supabase MCP execute_sql (or the SQL Editor), one file at a time, and read the NOTICE output:
       - funil01_pontuar_sjt_smokes.sql → PASS on: duplicate→EXCEPTION, complete honest submit→full-battery denominator, subset→bateria incompleta, foreign pergunta_id→42501, double submit→2nd 42501 + exactly ONE MC row, candidaturas.status/etapa_atual unchanged (RNF-07a).
       - funil08_pontuar_cognitivo_smokes.sql → PASS on: submit during avaliacao_assincrona registers AND interview stage still registers.
       - funil12_status_rpc_smoke.sql → PASS on: booleans-only (no score/status reachable) + foreign candidatura→42501.
       - funil10_reinscricao_smoke.sql → PASS on: insert→soft-delete→re-insert no 23505 + only the partial index remains.
       - n8n_novo_candidato_smoke.sql → PASS on: secret NULL → graceful-skip, no PII, candidatos row unchanged.
    2. Confirm EVERY smoke reports PASS and none raised an unexpected EXCEPTION.
    3. Confirm the disposable fixtures were cleaned up (no residue left in PROD).
    4. Note: the Vault secret `n8n_webhook_base` remains a Fernando human-action — the n8n dispatch stays dormant (graceful-skip) until he creates it; this is expected, not a failure.
  </how-to-verify>
  <verify>
    <automated>echo "Each smoke executed via Supabase MCP execute_sql; NOTICE=PASS output captured in 26-07-SUMMARY.md. Human confirms all five PASS with zero residue."</automated>
  </verify>
  <done>All five behavioral smokes report PASS on PROD with zero residue; the Vault secret is flagged as Fernando's human-action.</done>
  <resume-signal>Type "approved" once all five smokes report PASS on PROD with zero residue, or describe which smoke failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored migration → PROD | Applying to PROD is the moment behavior changes; the smokes are the only proof it is correct |
| candidate JWT → live RPCs | Every applied RPC must enforce ownership/etapa/neutral-projection over the wire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07-01 | Tampering | applied pontuar_sjt still manipulable (structural pass, behavior broken) | mitigate | funil01 live smoke proves dedup/denominator/completeness/re-submit-lock/membership over an impersonated JWT |
| T-26-07-02 | Information Disclosure | applied get_avaliacao_status leaks a verdict | mitigate | funil12 live smoke asserts booleans-only + IDOR 42501 |
| T-26-07-03 | Availability/Integrity | dropping the WRONG index breaks the duplicate-candidatura guard | mitigate | read-only discovery first; drop ONLY the unfiltered twin; assert the partial keeper remains; reinscription smoke |
| T-26-07-04 | Elevation/RNF-07a | applied scoring auto-rejects | mitigate | funil01/funil08 smokes assert candidaturas.status/etapa_atual unchanged |
| T-26-07-05 | Information Disclosure (LGPD) | n8n trigger emits PII / fires with a bad secret | mitigate | n8n smoke asserts graceful-skip + no-PII body + candidatos unchanged; Vault secret is a separate human-action |
| T-26-07-SC | Tampering | npm/pip/cargo installs | accept | No installs; MCP apply of authored SQL only; N/A |
</threat_model>

<verification>
- All five behavioral smokes report PASS live on PROD (Task 3 checkpoint).
- Live object existence + etapa-gate confirmed (Task 1); FUNIL-10 index resolved (Task 2).
- No `db push` used; version rows reconciled; database.types.ts regen deferred to Phase 27.
- This plan is BLOCKING and non-autonomous: it writes PROD and gates phase verification.
</verification>

<success_criteria>
FUNIL-01/07/08/10/12 + the n8n server-side dispatch are LIVE and behaviorally proven on PROD via the five SQL smokes (all PASS, RNF-07a + answer-key protection intact, zero residue), with the ledger reconciled and the Vault secret flagged as Fernando's human-action.
</success_criteria>

<output>
Create `.planning/phases/26-corre-o-do-funil-lado-candidato-alcan-abilidade-scoring/26-07-SUMMARY.md` when done — record the FUNIL-10 discovery output (both index defs or documented absence), the applied version rows, and each smoke's PASS evidence.
</output>
