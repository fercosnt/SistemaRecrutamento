---
phase: 20-refino-rh-editar-guia-de-entrevista-seed-001
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - src/features/entrevista/services/entrevistaService.ts
  - src/features/entrevista/hooks/useEntrevistaScorecard.ts
  - src/features/entrevista/__tests__/guia-normalize.test.ts
  - src/features/entrevista/__tests__/entrevista-contract.test.ts
  - src/features/entrevista/hooks/__tests__/useEntrevistaScorecard.test.ts
autonomous: true
requirements: [ENTREV-06, ENTREV-07, ENTREV-08]
must_haves:
  truths:
    - "saveGuiaEdits(candidaturaId, tipo, perguntas) calls supabase.rpc('save_entrevista_guia_edits', { p_candidatura_id, p_tipo, p_guia: { perguntas } }) and maps 42501 → FORBIDDEN via the existing mapRpcError (ENTREV-06/07/08 / Área 1)"
    - "normalizeGuia carries q.origem through and defaults missing/legacy → 'ia' (legacy guides are wholly AI-generated, A2) — the read layer is origem-aware (ENTREV-08 audit)"
    - "GuiaPergunta gains origem?: 'ia' | 'manual'; ENTREVISTA_GUIA_ALLOWLIST adds updated_at explicitly (NEVER select('*') — reference_select_star_leaks_pii / RESEARCH Pitfall 6)"
    - "useGuiaEntrevista exposes saveEdits mutation invalidating entrevistaKeys.guia(candidaturaId) — the same key the read query + gerar use (Área 2 batch-save)"
    - "The client→RPC payload carries NO score/band (anti-tamper) and the write never touches candidaturas (RNF-07a / ENTREV-08)"
  artifacts:
    - path: "src/features/entrevista/services/entrevistaService.ts"
      provides: "saveGuiaEdits write + origem-aware normalizeGuia + updated_at allowlist + GuiaPergunta.origem"
      contains: "save_entrevista_guia_edits"
    - path: "src/features/entrevista/hooks/useEntrevistaScorecard.ts"
      provides: "useGuiaEntrevista.saveEdits mutation invalidating the guide key"
      contains: "saveEdits"
  key_links:
    - from: "src/features/entrevista/services/entrevistaService.ts"
      to: "save_entrevista_guia_edits RPC"
      via: "supabase.rpc"
      pattern: "supabase\\.rpc\\('save_entrevista_guia_edits'"
    - from: "src/features/entrevista/hooks/useEntrevistaScorecard.ts"
      to: "entrevistaKeys.guia(candidaturaId)"
      via: "invalidateQueries onSuccess"
      pattern: "invalidateQueries"
---

<objective>
Add the data + server-state layer for guide editing: `saveGuiaEdits` (clone of `salvarAvaliacao`, swap the RPC name + payload), an origem-aware `normalizeGuia` (carry origem, default 'ia'), the `updated_at` allowlist extension, `GuiaPergunta.origem`, and the `useGuiaEntrevista.saveEdits` mutation. Extend the existing vitest suites.

Purpose: This is the contract the edit-mode UI (20-04) consumes. Defining it here as a clone-with-one-swap keeps the UI plan a pure interaction concern and lets the service tests (RPC payload, 42501 mapping, origem normalization, hook invalidation) prove ENTREV-06/07/08 deterministically.
Output: Service write + read normalization + hook mutation + extended vitest, all green at tsc ≤258.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-RESEARCH.md
@.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-PATTERNS.md
@.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-02-SUMMARY.md

<interfaces>
<!-- Sibling functions in the SAME file — clone these. Executor needs no exploration. -->

entrevistaService.ts (src/features/entrevista/services/entrevistaService.ts):
  L47:  export type TipoEntrevista = 'online' | 'presencial'
  L63:  export const ENTREVISTA_GUIA_ALLOWLIST = 'id, candidatura_id, tipo, guia, prompt_version, created_at'  ← add ', updated_at'
  L87:  export const ENTREVISTA_ALLOWLIST = ENTREVISTA_GUIA_ALLOWLIST  (the named allowlist getGuia uses)
  L95:  export interface GuiaPergunta { pergunta: string; dimensao?: string | null; score_atual?; ancoras_bars?; [k]: unknown }  ← add origem?: 'ia' | 'manual'
  L105: export interface EntrevistaGuiaRow { id; candidatura_id; tipo; guia; prompt_version; created_at }  ← add updated_at?: string | null
  L251: interface EfGuiaQuestion { question?; competency?; bars_anchors?; [k]: unknown }  ← origem? flows through [k] but stamp explicitly
  L274: export function normalizeGuia(guia) — maps questions[] → perguntas[] (pergunta from question, dimensao from competency)  ← add origem: q.origem === 'manual' ? 'manual' : 'ia'
  L294: export async function getGuia(candidaturaId) — select(ENTREVISTA_ALLOWLIST), order created_at DESC limit 1, returns normalizeGuia(row.guia)
  L412: function mapRpcError(error, fallbackMsg) — maps 42501→FORBIDDEN, 23514→INVALID_INPUT, no_data_found→NOT_FOUND  ← REUSE verbatim
  L446: export async function salvarAvaliacao(...) — the clone source: rpc + if(error) throw mapRpcError(...)

useEntrevistaScorecard.ts (src/features/entrevista/hooks/useEntrevistaScorecard.ts):
  L37: export const entrevistaKeys = { all, contexto(id), guia(id), analise(id), scorecard(id) }
  L67: export function useGuiaEntrevista(candidaturaId, vagaId) { query(entrevistaKeys.guia) ; gerar mutation invalidates guia key ; return { ...query, gerarGuia: gerar } }  ← add saveEdits, thread tipo
  L84: const gerar = useMutation({ mutationFn: (tipo) => gerarGuia(candidaturaId!, vagaId!, tipo), onSuccess: () => invalidateQueries(entrevistaKeys.guia(candidaturaId||'')) })

Test analogs:
  src/features/entrevista/__tests__/guia-normalize.test.ts (existing — extend with origem cases)
  src/features/entrevista/__tests__/entrevista-contract.test.ts (existing — extend anti-tamper: payload carries no score/band)
  src/features/entrevista/hooks/__tests__/useEntrevistaScorecard.test.ts (existing — vi.hoisted service mocks + vi.spyOn(queryClient,'invalidateQueries') + renderHook)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add saveGuiaEdits + origem-aware normalizeGuia + updated_at allowlist</name>
  <read_first>src/features/entrevista/services/entrevistaService.ts (the whole file — clone salvarAvaliacao L446, extend normalizeGuia L274, allowlist L63, GuiaPergunta L95), .planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-PATTERNS.md §entrevistaService section</read_first>
  <files>src/features/entrevista/services/entrevistaService.ts, src/features/entrevista/__tests__/guia-normalize.test.ts, src/features/entrevista/__tests__/entrevista-contract.test.ts</files>
  <behavior>
    - normalizeGuia(guia with questions[] missing origem) → perguntas[] each with origem === 'ia' (legacy default)
    - normalizeGuia(guia with a question origem:'manual') → that pergunta keeps origem === 'manual'
    - normalizeGuia(already pt-BR perguntas[] with origem) → passes through unchanged (defensive branch kept)
    - saveGuiaEdits('', tipo, []) throws EntrevistaServiceError INVALID_INPUT (candidaturaId obrigatório)
    - saveGuiaEdits with an RPC 42501 error → throws EntrevistaServiceError with code FORBIDDEN (via mapRpcError)
    - entrevista-contract: the rpc payload p_guia.perguntas carries NO score/band/score_atual-as-decision key — anti-tamper (the guide never feeds candidaturas)
  </behavior>
  <action>(1) Extend `GuiaPergunta` (L95) with `origem?: 'ia' | 'manual'`. (2) Extend `EntrevistaGuiaRow` (L105) with `updated_at?: string | null`. (3) Extend `ENTREVISTA_GUIA_ALLOWLIST` (L63) to append `, updated_at` — do NOT switch to select('*') (Pitfall 6 / reference_select_star_leaks_pii). (4) In `normalizeGuia` (L280-284 map), add `origem: q.origem === 'manual' ? 'manual' : 'ia'` so the read layer is origem-aware and legacy/missing defaults to 'ia' (A2); also carry origem through the already-pt-BR passthrough branch (L277) untouched (it already spreads). (5) Add `export async function saveGuiaEdits(candidaturaId: string, tipo: TipoEntrevista, perguntas: GuiaPergunta[]): Promise<EntrevistaGuiaRow | null>` cloning `salvarAvaliacao` (L446): guard empty candidaturaId → throw INVALID_INPUT; call `supabase.rpc('save_entrevista_guia_edits', { p_candidatura_id: candidaturaId, p_tipo: tipo, p_guia: { perguntas } })`; on error `throw mapRpcError(error, 'Não foi possível salvar as edições do guia. Tente novamente.')`; then `return getGuia(candidaturaId)` for the read-back. REUSE mapRpcError verbatim (do NOT write a new mapper). The RPC stores opaque jsonb; the client stamps origem:'manual' on added rows (in 20-04) — the service passes the array verbatim. Extend `guia-normalize.test.ts` with the origem cases and `entrevista-contract.test.ts` with the anti-tamper assertion. RNF-07a: saveGuiaEdits never writes candidaturas.</action>
  <verify>
    <automated>npm run test:run -- src/features/entrevista/__tests__/guia-normalize.test.ts src/features/entrevista/__tests__/entrevista-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "supabase.rpc('save_entrevista_guia_edits'" src/features/entrevista/services/entrevistaService.ts` matches with payload `{ p_candidatura_id, p_tipo, p_guia: { perguntas } }`
    - saveGuiaEdits maps 42501 → FORBIDDEN via mapRpcError (not a new mapper)
    - normalizeGuia defaults missing origem → 'ia'; preserves 'manual'
    - ENTREVISTA_GUIA_ALLOWLIST contains `updated_at`; no `select('*')` added
    - GuiaPergunta has `origem?: 'ia' | 'manual'`; EntrevistaGuiaRow has updated_at
    - entrevista-contract test asserts the RPC payload carries no score/band
    - tsc ≤ 258
  </acceptance_criteria>
  <done>Service write + origem-aware read + allowlist updated_at land; guia-normalize + entrevista-contract tests green; tsc held.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add useGuiaEntrevista.saveEdits mutation</name>
  <read_first>src/features/entrevista/hooks/useEntrevistaScorecard.ts (useGuiaEntrevista L67-91, gerar mutation L84-89, entrevistaKeys L37), src/features/entrevista/hooks/__tests__/useEntrevistaScorecard.test.ts (vi.hoisted mocks + invalidateQueries spy), .planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-PATTERNS.md §useEntrevistaScorecard section</read_first>
  <files>src/features/entrevista/hooks/useEntrevistaScorecard.ts, src/features/entrevista/hooks/__tests__/useEntrevistaScorecard.test.ts</files>
  <behavior>
    - useGuiaEntrevista returns a saveEdits mutation alongside gerarGuia
    - saveEdits.mutate({ tipo, perguntas }) calls saveGuiaEdits(candidaturaId, tipo, perguntas)
    - on success, invalidateQueries is called with entrevistaKeys.guia(candidaturaId) — the SAME key the read query + gerar use (panel re-renders with the saved guide)
  </behavior>
  <action>Inside `useGuiaEntrevista` (L67), import `saveGuiaEdits` + `GuiaPergunta` from the service (extend the existing import block) and add a `saveEdits` mutation cloning the `gerar` mutation (L84-89): `mutationFn: (vars: { tipo: TipoEntrevista; perguntas: GuiaPergunta[] }) => saveGuiaEdits(candidaturaId!, vars.tipo, vars.perguntas)`; `onSuccess: () => queryClient.invalidateQueries({ queryKey: entrevistaKeys.guia(candidaturaId || '') })` (the SAME key — L40 — so the read re-fetches). Thread `tipo` through the mutation vars (the guide can be online or presencial; the panel knows which it is editing). Return `{ ...query, gerarGuia: gerar, saveEdits }`. Extend the hook test: vi.hoisted mock `saveGuiaEdits`, render the hook, call `saveEdits.mutateAsync({ tipo:'online', perguntas:[...] })`, assert `saveGuiaEdits` was called with (candidaturaId, 'online', perguntas) and `queryClient.invalidateQueries` was called with the guia key (vi.spyOn pattern already in the test file).</action>
  <verify>
    <automated>npm run test:run -- src/features/entrevista/hooks/__tests__/useEntrevistaScorecard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "saveEdits" src/features/entrevista/hooks/useEntrevistaScorecard.ts` matches a useMutation returned from useGuiaEntrevista
    - saveEdits.onSuccess invalidates `entrevistaKeys.guia(candidaturaId)` (same key as gerar/read)
    - mutation vars carry `{ tipo, perguntas }`
    - hook test asserts saveGuiaEdits call args + the guide-key invalidation
    - tsc ≤ 258
  </acceptance_criteria>
  <done>saveEdits mutation exported from useGuiaEntrevista; invalidates the guide key; hook test green; tsc held.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client edit state → RPC payload | the RH-edited perguntas[] (incl. client-stamped origem) is serialized into the RPC call |
| service read → UI | normalizeGuia projects allowlisted columns; origem defaults must not misclassify provenance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | Tampering (client posts a score/band on the guide) | saveGuiaEdits payload | mitigate | the RPC stores opaque jsonb with no score semantics; entrevista-contract test asserts payload carries no score/band; guide never feeds candidaturas (RNF-07a) |
| T-20-10 | Info disclosure (over-projection) | getGuia / allowlist | mitigate | ENTREVISTA_GUIA_ALLOWLIST adds updated_at explicitly; never select('*') (RLS is row-level only — reference_select_star_leaks_pii) |
| T-20-11 | Repudiation (manual question loses provenance on read) | normalizeGuia | mitigate | origem carried through; legacy/missing → 'ia' (A2: legacy guides are wholly AI-generated, never manual) |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | zero external packages this phase |
</threat_model>

<verification>
- `npm run test:run -- src/features/entrevista` green (guia-normalize, entrevista-contract, useEntrevistaScorecard).
- `npm run lint` tsc ≤ 258.
- grep gates: saveGuiaEdits calls the RPC; saveEdits invalidates the guide key.
</verification>

<success_criteria>
- saveGuiaEdits clones salvarAvaliacao with the RPC name + `{ perguntas }` payload, maps 42501→FORBIDDEN.
- normalizeGuia is origem-aware (default 'ia'); allowlist carries updated_at (no star).
- useGuiaEntrevista.saveEdits invalidates the guide key (batch-save plumbing for 20-04).
- All extended vitest green; tsc held at ≤258.
</success_criteria>

<output>
Create `.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-03-SUMMARY.md` when done
</output>
