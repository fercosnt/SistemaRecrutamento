---
phase: 20-refino-rh-editar-guia-de-entrevista-seed-001
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260629000001_entrevista_guia_edits.sql
  - supabase/functions/gerar-guia-entrevista/_local/merge-preserve.test.ts
  - .planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/_smoke/save_entrevista_guia_edits.smoke.sql
autonomous: true
requirements: [ENTREV-08]
must_haves:
  truths:
    - "The migration file dedups entrevista_guias to the latest row per (candidatura_id, tipo) BEFORE adding UNIQUE (Área 1 / RESEARCH Pitfall 2)"
    - "The migration adds UNIQUE(candidatura_id, tipo) + updated_at, then creates save_entrevista_guia_edits as SECURITY DEFINER with search_path='' (Área 1 / ENTREV-08)"
    - "save_entrevista_guia_edits derives role from public.usuarios_rh (NOT auth.jwt claim) — the ENTREV-08 deviation (Área 1 / RESEARCH Pitfall 1)"
    - "The RPC REVOKEs PUBLIC + GRANTs authenticated and never writes public.candidaturas (RNF-07a / ENTREV-08)"
    - "A Deno test asserts a regen preserves an origem:'manual' question and a failed regen (guide=null) does NOT drop it (ENTREV-08 anti-silent-discard) — RED until the EF changes in 20-05"
    - "A SQL smoke script exists covering RPC DENY candidato/RH-no-posse → 42501, OK RH-with-posse, admin bypass, upsert idempotency, dedup, role-from-usuarios_rh-not-JWT"
  artifacts:
    - path: "supabase/migrations/20260629000001_entrevista_guia_edits.sql"
      provides: "dedup → updated_at → UNIQUE(candidatura_id,tipo) → save_entrevista_guia_edits RPC (role from usuarios_rh)"
      contains: "save_entrevista_guia_edits"
    - path: "supabase/functions/gerar-guia-entrevista/_local/merge-preserve.test.ts"
      provides: "Deno merge-preserve invariant test (RED-by-design until 20-05)"
      min_lines: 40
    - path: ".planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/_smoke/save_entrevista_guia_edits.smoke.sql"
      provides: "SQL smoke script (DENY/OK/upsert/dedup/role-from-table) for execute-against-PROD in 20-02"
      min_lines: 30
  key_links:
    - from: "supabase/migrations/20260629000001_entrevista_guia_edits.sql"
      to: "public.usuarios_rh"
      via: "SELECT role inside SECURITY DEFINER (auth-hook filter)"
      pattern: "FROM public.usuarios_rh"
    - from: "supabase/migrations/20260629000001_entrevista_guia_edits.sql"
      to: "ON CONFLICT (candidatura_id, tipo)"
      via: "upsert arbiter pre-created by ADD CONSTRAINT UNIQUE"
      pattern: "ON CONFLICT \\(candidatura_id, tipo\\)"
---

<objective>
Author (do NOT apply) the Phase-20 migration — the dedup → UNIQUE → updated_at → `save_entrevista_guia_edits` SECURITY DEFINER RPC — plus the two Wave-0 test scaffolds the later waves run: the Deno merge-preserve invariant test (RED until 20-05 changes the EF) and the SQL smoke script (executed against PROD in 20-02).

Purpose: This is the clone-with-one-swap heart of ENTREV-08 (the milestone's most security-sensitive write-path). Authoring the SQL and the tests first means 20-02 only has to APPLY + smoke, and 20-05 only has to make the merge test go green.
Output: One migration file, one Deno test, one SQL smoke script. NOTHING is applied to PROD in this plan (apply is the [BLOCKING] 20-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-CONTEXT.md
@.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-RESEARCH.md
@.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-PATTERNS.md

<interfaces>
<!-- Clone sources — executor uses these directly, no codebase exploration needed. -->

RPC skeleton to clone — supabase/migrations/20260624000002_salvar_avaliacao_entrevista_rpc.sql:
  CREATE OR REPLACE FUNCTION public.salvar_avaliacao_entrevista(...) RETURNS jsonb
    LANGUAGE plpgsql SECURITY DEFINER SET search_path = '' AS $$ ... $$;
  - own-vaga via candidaturas JOIN vagas on c.vaga_id; v_vaga_owner := v.created_by
  - THE LINE TO SWAP (≈L67): v_role := (select auth.jwt() #>> '{app_metadata,role}')
  - REVOKE ALL ... FROM PUBLIC; GRANT EXECUTE ... TO authenticated;

THE ONE SWAP — copy the EXACT filter from supabase/migrations/20260420000002_unified_auth_role.sql:43-56 (the auth-hook):
  SELECT role INTO v_role_db
    FROM public.usuarios_rh
   WHERE user_id = (select auth.uid())   -- GUC-based; survives SECURITY DEFINER (D-09)
     AND ativo = true
     AND deleted_at IS NULL
   LIMIT 1;
  v_role := CASE WHEN v_role_db = 'recrutador' THEN 'rh'
                 WHEN v_role_db = 'administrador' THEN 'administrador'
                 ELSE v_role_db END;

entrevista_guias schema (supabase/migrations/20260624000001_entrevista_cognitivo_tables.sql):
  (id uuid, candidatura_id uuid, tipo text CHECK online|presencial, guia jsonb,
   prompt_version text, created_at timestamptz). SELECT-only RLS for rh/administrador. NO updated_at, NO origem.

EF handler signature (supabase/functions/gerar-guia-entrevista/index.ts):
  export interface GerarGuiaDeps { anthropic; openai; supabaseAdmin; supabaseUser; zodOutputFormat; zodResponseFormat }  (L88)
  export async function handler(req: Request, deps: GerarGuiaDeps): Promise<Response>  (L137)
  Current persist (L318): await supabaseAdmin.from("entrevista_guias").insert({ candidatura_id, tipo, guia: guide ?? { incompleto: true, flags: persistFlags }, prompt_version })

Deno test analog: supabase/functions/gerar-guia-entrevista/_local/weak-dim-coverage.test.ts (same EF dir) +
  supabase/functions/analise-candidato-individual/__tests__/index.test.ts:35-75 (makeMockSupabase capturing .upsert(row,{onConflict}) into an array).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the dedup → updated_at → UNIQUE → RPC migration file</name>
  <read_first>supabase/migrations/20260624000002_salvar_avaliacao_entrevista_rpc.sql (clone skeleton), supabase/migrations/20260420000002_unified_auth_role.sql:43-56 (the usuarios_rh filter to swap in), supabase/migrations/20260624000001_entrevista_cognitivo_tables.sql (entrevista_guias schema + RLS), .planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-RESEARCH.md §Pattern 1 & §Pattern 2</read_first>
  <files>supabase/migrations/20260629000001_entrevista_guia_edits.sql</files>
  <action>Create the migration with statements in this LOAD-BEARING order (RESEARCH Pitfall 2): (1) dedup DELETE keeping the latest per (candidatura_id, tipo) via `DELETE FROM public.entrevista_guias g WHERE g.id NOT IN (SELECT DISTINCT ON (candidatura_id, tipo) id FROM public.entrevista_guias ORDER BY candidatura_id, tipo, created_at DESC, id DESC)`; (2) `ALTER TABLE ... ADD COLUMN IF NOT EXISTS updated_at timestamptz`, backfill `UPDATE ... SET updated_at = created_at WHERE updated_at IS NULL`, then `ALTER COLUMN updated_at SET DEFAULT now(), SET NOT NULL`; (3) `ADD CONSTRAINT entrevista_guias_candidatura_tipo_key UNIQUE (candidatura_id, tipo)`; (4) `CREATE OR REPLACE FUNCTION public.save_entrevista_guia_edits(p_candidatura_id uuid, p_tipo text, p_guia jsonb) RETURNS jsonb LANGUAGE plpgsql SECURITY DEFINER SET search_path = ''`. The function body: tipo guard (NOT IN online/presencial → RAISE check_violation); role from usuarios_rh per the §interfaces swap (recrutador→rh, administrador→administrador, else→DENY insufficient_privilege 42501); own-vaga via `candidaturas c JOIN vagas v ON v.id = c.vaga_id WHERE c.id = p_candidatura_id` → if v_vaga_owner IS NULL RAISE no_data_found, if v_role='rh' AND owner DISTINCT FROM auth.uid() RAISE insufficient_privilege 42501 (admin bypasses); optional jsonb shape guard (p_guia NULL or not object → check_violation); then `INSERT INTO public.entrevista_guias (candidatura_id, tipo, guia, updated_at) VALUES (p_candidatura_id, p_tipo, p_guia, now()) ON CONFLICT (candidatura_id, tipo) DO UPDATE SET guia = EXCLUDED.guia, updated_at = now()`; RETURN jsonb_build_object('ok', true, ...). Then `REVOKE ALL ON FUNCTION public.save_entrevista_guia_edits(uuid, text, jsonb) FROM PUBLIC` and `GRANT EXECUTE ... TO authenticated`. Per ENTREV-08: role MUST come from usuarios_rh NOT auth.jwt (do NOT copy the salvar_avaliacao JWT-claim line). RNF-07a: the body NEVER touches candidaturas. Per D-22 / CLAUDE.md 42601 workaround: NO `BEGIN;...COMMIT;` wrapper anywhere in the file — add an inline comment explaining the Supabase CLI driver wraps each migration in its own implicit transaction.</action>
  <verify>
    <automated>grep -q "FROM public.usuarios_rh" supabase/migrations/20260629000001_entrevista_guia_edits.sql && grep -q "ON CONFLICT (candidatura_id, tipo)" supabase/migrations/20260629000001_entrevista_guia_edits.sql && grep -q "REVOKE ALL ON FUNCTION public.save_entrevista_guia_edits" supabase/migrations/20260629000001_entrevista_guia_edits.sql && ! grep -iq "auth.jwt() #>> '{app_metadata,role}'" supabase/migrations/20260629000001_entrevista_guia_edits.sql && ! grep -iqE "^\s*BEGIN\s*;" supabase/migrations/20260629000001_entrevista_guia_edits.sql && echo MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - migration declares dedup BEFORE `ADD CONSTRAINT ... UNIQUE` (DISTINCT ON keep-latest); UNIQUE before CREATE FUNCTION
    - migration adds UNIQUE(candidatura_id, tipo) and the updated_at column
    - `save_entrevista_guia_edits` derives role from `public.usuarios_rh` (not `auth.jwt`) — grep finds `FROM public.usuarios_rh`, finds NO `auth.jwt() #>> '{app_metadata,role}'`
    - function is SECURITY DEFINER with `SET search_path = ''`, REVOKE PUBLIC + GRANT authenticated
    - no `BEGIN;`/`COMMIT;` wrapper (D-22 / 42601 workaround)
    - body contains no INSERT/UPDATE against `candidaturas` (RNF-07a)
  </acceptance_criteria>
  <done>Migration file exists, statement order is dedup→updated_at→UNIQUE→RPC, role-from-usuarios_rh, REVOKE/GRANT present, no BEGIN/COMMIT wrapper, never writes candidaturas.</done>
</task>

<task type="auto">
  <name>Task 2: Author the SQL smoke script + the Deno merge-preserve test (RED scaffold)</name>
  <read_first>.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-RESEARCH.md §Code Examples (DENY smoke skeleton L511-537) + §Validation Architecture, supabase/functions/gerar-guia-entrevista/_local/weak-dim-coverage.test.ts (Deno test idiom, same dir), supabase/functions/analise-candidato-individual/__tests__/index.test.ts:35-75 (makeMockSupabase capturing .upsert)</read_first>
  <files>.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/_smoke/save_entrevista_guia_edits.smoke.sql, supabase/functions/gerar-guia-entrevista/_local/merge-preserve.test.ts</files>
  <action>(a) Create the SQL smoke script wrapped in a ROLLBACK-able fixture (BEGIN; ... ROLLBACK; — this is a DISPOSABLE smoke, NOT a migration, so the wrapper IS wanted here): use `set_config('request.jwt.claims', json_build_object('sub', <user>, 'role','authenticated', 'app_metadata', json_build_object('role', <claim>))::text, true)` to simulate identities and call `public.save_entrevista_guia_edits(<cand>, 'online', <guia jsonb>)`. Cover six cases with expected outcomes as SQL comments: candidato (no usuarios_rh row) → 42501 insufficient_privilege; RH-without-ownership (usuarios_rh recrutador row but vaga.created_by != auth.uid()) → 42501; RH-with-ownership → ok=true; admin bypass (administrador row, any vaga) → ok=true; two saves of the same (candidatura_id, tipo) → assert exactly 1 row (upsert collapse); the dedup proof (seed 3 rows for one (cand,tipo), the dedup DELETE leaves 1). CRITICAL anti-claim case (Pitfall 1): a JWT whose `app_metadata.role` SAYS 'rh' but with NO `usuarios_rh` row → MUST DENY (proves role comes from the table, not the claim). Add a header comment that 20-02 fills the real <user>/<cand> UUIDs from PROD seed data and runs this via Supabase MCP `execute_sql`. (b) Create the Deno merge-preserve test: import the EF `handler` + the `GerarGuiaDeps` shape; build a `makeMockSupabase` (clone the analise-candidato-individual idiom) whose `.from('entrevista_guias').select(...).maybeSingle()` returns a CURRENT row containing one `{ origem: 'manual', pergunta/question: '...' }` question, and whose `.upsert(row, opts)` records the row into a captured array. Assert three behaviors: (1) after a successful regen the captured upserted `guia.questions`/`perguntas` STILL contains the manual question; (2) a failed regen (mock AI returns null → guide=null) does NOT drop the manual question from the upserted row; (3) freshly-generated questions are stamped `origem: 'ia'`. These assertions are EXPECTED TO FAIL now (the EF still `.insert()`s without merge) — annotate the test header `RED-by-design until 20-05`. Use the injected-deps path (handler(req, deps)); mock anthropic/openai so no network call.</action>
  <verify>
    <automated>test -f ".planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/_smoke/save_entrevista_guia_edits.smoke.sql" && grep -q "set_config('request.jwt.claims'" ".planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/_smoke/save_entrevista_guia_edits.smoke.sql" && grep -q "save_entrevista_guia_edits" ".planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/_smoke/save_entrevista_guia_edits.smoke.sql" && test -f supabase/functions/gerar-guia-entrevista/_local/merge-preserve.test.ts && grep -q "origem" supabase/functions/gerar-guia-entrevista/_local/merge-preserve.test.ts && echo SCAFFOLDS_OK</automated>
  </verify>
  <acceptance_criteria>
    - smoke SQL covers: candidato→42501, RH-no-posse→42501, RH-own→ok, admin bypass→ok, upsert→1 row, dedup→1 row, claim-says-rh-no-table-row→DENY
    - smoke SQL is ROLLBACK-wrapped and references `set_config('request.jwt.claims', ...)`
    - Deno test asserts manual question survives a successful regen AND a failed (guide=null) regen, and fresh questions carry origem:'ia'
    - Deno test is RED-by-design (annotated) — it tests behavior 20-05 will implement
    - Deno test uses injected deps + mocked anthropic/openai (no network)
  </acceptance_criteria>
  <done>Both scaffolds exist; smoke script enumerates all six+1 cases; Deno test encodes the merge-preserve invariant (currently RED).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (RH browser) → RPC | RH-supplied candidatura_id + guia jsonb crosses into a SECURITY DEFINER that bypasses RLS — the in-body guard IS the control |
| usuarios_rh table → role decision | role is read from the authoritative table, NOT the (possibly drifted/forged) JWT claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Elevation of Privilege (IDOR) | save_entrevista_guia_edits | mitigate | own-vaga guard: candidaturas→vagas.created_by = auth.uid(); RH-without-posse RAISE insufficient_privilege 42501; smoke asserts the DENY |
| T-20-02 | Spoofing (JWT-claim role) | save_entrevista_guia_edits role check | mitigate | role read from public.usuarios_rh (ativo + deleted_at IS NULL), NOT auth.jwt; smoke asserts claim-says-rh-no-row → DENY |
| T-20-03 | Elevation (candidate calls RPC) | GRANT scope | mitigate | usuarios_rh lookup returns no row → role NULL → 42501; REVOKE PUBLIC + GRANT authenticated only |
| T-20-04 | Tampering (manual question dropped on regen) | EF merge / Deno test | mitigate | merge-preserve invariant encoded in the Deno test (made green in 20-05); origem:'manual' is the audit field |
| T-20-05 | Tampering (guide write feeds candidaturas) | RPC body | mitigate | RNF-07a: RPC body never touches candidaturas; verified by source grep |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | zero external packages this phase (RESEARCH §Package Legitimacy Audit: not applicable); no install surface |
</threat_model>

<verification>
- `grep` gates in Task 1/2 pass (role-from-usuarios_rh, ON CONFLICT, REVOKE, no JWT-claim role, no BEGIN wrapper, smoke + Deno scaffolds present).
- The Deno merge-preserve test is RED-by-design here (no EF change yet) — it is NOT run as a gate in this plan; 20-05 turns it green.
- Migration is NOT applied (apply is the [BLOCKING] 20-02).
</verification>

<success_criteria>
- Migration file authored with the load-bearing statement order and the role-from-usuarios_rh swap (ENTREV-08).
- SQL smoke script enumerates all DENY/OK/upsert/dedup/role-source cases for 20-02 to execute.
- Deno merge-preserve test encodes the anti-silent-discard invariant (RED until 20-05).
- Nothing applied to PROD.
</success_criteria>

<output>
Create `.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-01-SUMMARY.md` when done
</output>
