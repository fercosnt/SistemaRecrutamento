---
phase: 20-refino-rh-editar-guia-de-entrevista-seed-001
plan: 04
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - supabase/functions/gerar-guia-entrevista/index.ts
autonomous: false
requirements: [ENTREV-08]
must_haves:
  truths:
    - "gerar-guia-entrevista changes INSERT → upsert ON CONFLICT (candidatura_id, tipo) so a regen collapses to one row (Área 2 / RESEARCH Pattern 3)"
    - "The merge preserves every origem:'manual' question: split currentQs by origem === 'manual', keep them, replace only the IA ones with fresh generation (ENTREV-08 anti-silent-discard)"
    - "A FAILED regen (guide == null) does NOT clobber manual questions — the merge runs BEFORE the never-absent `guide ?? {incompleto}` fallback (RESEARCH Pitfall 3)"
    - "Freshly-generated questions are stamped origem:'ia' post-parse (A1 — not via the LLM zod schema; the /v4 helper surface untouched)"
    - "The current row is read with select('guia') allowlist, NEVER select('*') (reference_select_star_leaks_pii); the two-client auth path + redacted log are UNCHANGED; EF never writes candidaturas (RNF-07a)"
    - "[BLOCKING] The EF is redeployed to PROD (gerar-guia-entrevista) — the merge logic is in index.ts not _shared, so no cross-EF drift (reference_ef_shared_bundle_freeze)"
  artifacts:
    - path: "supabase/functions/gerar-guia-entrevista/index.ts"
      provides: "read-merge-upsert preserving manual questions on regen + failed-regen guard + origem:'ia' stamp"
      contains: "onConflict"
  key_links:
    - from: "supabase/functions/gerar-guia-entrevista/index.ts"
      to: "entrevista_guias (UNIQUE candidatura_id,tipo)"
      via: ".upsert(row, { onConflict: 'candidatura_id,tipo' })"
      pattern: "onConflict.*candidatura_id"
    - from: "supabase/functions/gerar-guia-entrevista/index.ts"
      to: "current guia questions"
      via: "filter origem === 'manual' preserved"
      pattern: "origem"
---

<objective>
Change `gerar-guia-entrevista` from a blind `.insert()` to a read-merge-upsert that NEVER drops an `origem:'manual'` question (including the failed-regen path), stamps fresh questions `origem:'ia'`, and targets the new `UNIQUE(candidatura_id, tipo)` arbiter — then make the 20-01 Deno merge-preserve test green and [BLOCKING] redeploy the EF.

Purpose: This closes the central ENTREV-08 anti-silent-discard invariant — the EF was built to INSERT a new row per regen (orphaning edits); now a regen merges manual + fresh-IA into one row. This is the hard invariant CONTEXT Área 2 forbids violating.
Output: EF merge logic + green Deno test + redeployed EF. Live round-trip is Phase 21.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-RESEARCH.md
@.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-PATTERNS.md
@.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-02-SUMMARY.md
@supabase/functions/gerar-guia-entrevista/_local/merge-preserve.test.ts

<interfaces>
<!-- The exact change point + the upsert idiom to clone. -->

gerar-guia-entrevista/index.ts:
  L88:  export interface GerarGuiaDeps { anthropic; openai; supabaseAdmin; supabaseUser; zodOutputFormat; zodResponseFormat }
  L137: export async function handler(req, deps): Promise<Response>
  L141: const { anthropic, openai, supabaseAdmin, supabaseUser } = deps
  L143-199: two-client authenticate-THEN-authorize (role from usuarios_rh via supabaseAdmin; own-vaga via vagas.created_by; admin bypass) — UNCHANGED
  L312-316: persistFlags built (guide==null/prompt_injection → ia_sem_resultado; needsHumanFlag → weak_dim_uncovered)
  L318-323 (THE LINE TO CHANGE):
    await supabaseAdmin.from("entrevista_guias").insert({
      candidatura_id: body.candidatura_id, tipo: body.tipo,
      guia: guide ?? { incompleto: true, flags: persistFlags },
      prompt_version: resolved.prompt_version,
    });
  L325-333: redacted LGPD-02 log (ids/counts only) — UNCHANGED

Upsert idiom to clone — gerar-devolutiva-bigfive/index.ts:458-468 + analise-candidato-individual/index.ts:316:
  await supabaseAdmin.from(table).upsert({ ...row }, { onConflict: "candidatura_id" })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read-merge-upsert preserving manual questions (incl. failed-regen guard) + origem:'ia' stamp</name>
  <read_first>supabase/functions/gerar-guia-entrevista/index.ts (L300-336 — the persist block + log), supabase/functions/gerar-devolutiva-bigfive/index.ts:458-468 (upsert idiom), supabase/functions/gerar-guia-entrevista/_local/merge-preserve.test.ts (the RED test it must satisfy), .planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-RESEARCH.md §Pattern 3 + §Pitfall 3</read_first>
  <files>supabase/functions/gerar-guia-entrevista/index.ts</files>
  <action>Replace the blind `.insert()` (L318-323) with a read-merge-upsert. BEFORE the never-absent `guide ?? {incompleto}` fallback (Pitfall 3): (a) read the CURRENT row with the allowlist `.from("entrevista_guias").select("guia").eq("candidatura_id", body.candidatura_id).eq("tipo", body.tipo).maybeSingle()` — NEVER select('*') (reference_select_star_leaks_pii); (b) `const currentQs = (currentRow?.guia?.questions ?? currentRow?.guia?.perguntas ?? []) as Array<Record<string, unknown>>`; `const manualQs = currentQs.filter((q) => q.origem === "manual")` — these are PRESERVED, never dropped (identify by origem, NOT by text/order match — the field is authoritative and survives reorder/edit); (c) `const freshIaQs = (guide?.questions ?? []).map((q) => ({ ...q, origem: "ia" as const }))` — stamp post-parse (A1; do NOT touch the zod/v4 schema or helper surface); (d) `const mergedQuestions = [...manualQs, ...freshIaQs]`. Then UPSERT (was insert): `.from("entrevista_guias").upsert({ candidatura_id, tipo, guia: guide ? { ...guide, questions: mergedQuestions } : { incompleto: true, flags: persistFlags, questions: manualQs }, prompt_version: resolved.prompt_version, updated_at: new Date().toISOString() }, { onConflict: "candidatura_id,tipo" })`. The failed-regen branch (guide == null) MUST carry `manualQs` into the incompleto payload (so a failed regen does NOT clobber manual edits — the merge runs before the fallback). The two-client auth block (L143-199), the persistFlags build (L312-316), and the redacted log (L325-333) stay UNCHANGED. RNF-07a: the EF still never writes candidaturas. Keep all questions identified strictly by `origem === 'manual'`.</action>
  <verify>
    <automated>deno test --allow-read supabase/functions/gerar-guia-entrevista/_local/merge-preserve.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "onConflict.*candidatura_id" supabase/functions/gerar-guia-entrevista/index.ts` matches (upsert, not insert)
    - the merge reads current via `select("guia")` (allowlist), NOT `select('*')`
    - manual questions identified by `q.origem === "manual"` and prepended to the merged set
    - failed regen (guide==null) carries manualQs into the incompleto payload (does NOT drop them)
    - fresh questions stamped `origem: "ia"` post-parse (zod schema + /v4 helper untouched)
    - two-client auth block + redacted log unchanged; no candidaturas write
    - the 20-01 Deno merge-preserve test (manual-survives + failed-regen-keeps-manual + ia-stamp) is now GREEN
  </acceptance_criteria>
  <done>EF merges manual + fresh-IA on upsert; failed regen preserves manual; fresh stamped 'ia'; Deno test green; auth/log/RNF-07a untouched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Redeploy gerar-guia-entrevista to PROD</name>
  <files>supabase/functions/gerar-guia-entrevista/index.ts (redeployed to PROD, not edited here)</files>
  <action>[BLOCKING — PROD action, human-gated] Redeploy `gerar-guia-entrevista` to PROD so the Task-1 merge-preserve change goes live (the deployed bundle is frozen per-EF until redeployed — reference_ef_shared_bundle_freeze). Run `supabase functions deploy gerar-guia-entrevista` (JWT-ON, PROD precedent); if the CLI is unavailable, use Supabase MCP `deploy_edge_function` for `gerar-guia-entrevista`. Then confirm via MCP `get_edge_function` (or the dashboard) that the live source contains `onConflict` (the upsert), proving the new bundle is live. Do NOT run a live regen round-trip (Phase 21). Confirm with the user before deploying.</action>
  <what-built>The merge-preserve change to `supabase/functions/gerar-guia-entrevista/index.ts` (Task 1): INSERT → upsert ON CONFLICT (candidatura_id,tipo) + preserve origem:'manual' + origem:'ia' stamp + failed-regen guard.</what-built>
  <how-to-verify>
    The deployed EF keeps the old blind INSERT until redeployed (the bundle is frozen per-EF — reference_ef_shared_bundle_freeze). Redeploy via `supabase functions deploy gerar-guia-entrevista` (the merge logic is in index.ts, not _shared, so no cross-EF drift). This is JWT-ON (PROD precedent). If the CLI is unavailable, use Supabase MCP `deploy_edge_function` for `gerar-guia-entrevista`. This is a PROD action — confirm before deploying.
    After deploy, confirm via MCP `get_edge_function` (or the dashboard) that the deployed source contains `onConflict` (the upsert) — proving the new bundle is live, not the old INSERT. Do NOT run a live regen round-trip here (that is Phase 21); the deterministic merge invariant is already proven by the green Deno test.
  </how-to-verify>
  <verify>
    <human-check>gerar-guia-entrevista redeployed; get_edge_function shows `onConflict` (the upsert) in the live source; no live regen round-trip run.</human-check>
  </verify>
  <acceptance_criteria>
    - gerar-guia-entrevista redeployed to PROD (CLI or MCP deploy_edge_function)
    - the deployed source contains the upsert (`onConflict`), not the old INSERT
    - no live regen round-trip run here (deferred to Phase 21)
  </acceptance_criteria>
  <done>EF redeployed to PROD; live source shows the upsert; live round-trip deferred to Phase 21.</done>
  <resume-signal>Type "deployed" once the redeploy succeeded and get_edge_function shows the upsert in the live source, or describe the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI generation → persisted guide | the LLM output is merged with existing manual questions before persisting; a failed/poisoned generation must not destroy manual edits |
| deployed EF bundle → PROD behavior | the live bundle is frozen until redeploy; stale INSERT would orphan manual edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-12 | Tampering/Repudiation (manual question dropped by regen) | EF merge | mitigate | split by origem === 'manual'; keep all manual; Deno test asserts the invariant incl. failed-regen |
| T-20-13 | Tampering (failed/poisoned regen wipes the row) | guide ?? {incompleto} fallback | mitigate | merge runs BEFORE the fallback; incompleto payload carries manualQs (Pitfall 3) |
| T-20-14 | Info disclosure (over-projection on the read-current) | select on entrevista_guias | mitigate | read with `select("guia")` allowlist, never select('*') (reference_select_star_leaks_pii) |
| T-20-15 | Spoofing/EoP (EF auth regression) | two-client auth block | accept | block is UNCHANGED (already correct: role from usuarios_rh, own-vaga, admin bypass) — no edit to it |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | zero external packages; npm: imports stay static; zod/v4 pin untouched |
</threat_model>

<verification>
- `deno test --allow-read supabase/functions/gerar-guia-entrevista/_local/merge-preserve.test.ts` green (manual survives + failed-regen keeps manual + ia stamp).
- grep gate: index.ts has `onConflict` + `origem`, no `select('*')` on the read-current.
- After redeploy, the live source contains the upsert.
</verification>

<success_criteria>
- EF merge-preserve makes the 20-01 Deno test green (the ENTREV-08 hard invariant).
- Fresh questions stamped origem:'ia' post-parse; manual ones preserved by origem.
- Failed regen does not clobber manual edits.
- EF redeployed to PROD; auth/log/RNF-07a untouched; live round-trip deferred to Phase 21.
</success_criteria>

<output>
Create `.planning/phases/20-refino-rh-editar-guia-de-entrevista-seed-001/20-04-SUMMARY.md` when done
</output>
