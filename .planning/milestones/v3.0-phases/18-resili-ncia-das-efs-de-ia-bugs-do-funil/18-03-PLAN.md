---
phase: 18-resili-ncia-das-efs-de-ia-bugs-do-funil
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/consolidar-decisao-final/index.ts
  - supabase/functions/consolidar-decisao-final/__tests__/index.test.ts
  - src/features/avaliacao/services/__tests__/avaliacaoService.test.ts
autonomous: true
requirements: [FIX-01, FIX-02]
must_haves:
  truths:
    - "normalizeSjtComposite is exported from consolidar-decisao-final and unit-tested: caso_aberto pendente-único → null (D-Área3 FIX-01)"
    - "normalizeSjtComposite preserves the MC sucesso composite when a caso_aberto sub-row is pendente (returns 80 for 8/10) — does not zero the consolidado (D-Área3 FIX-01)"
    - "avaliacaoService getAvaliacaoContext queries perguntas with .eq('status','active') and returns rows — regression test would fail on legacy 'ativo' (D-Área3 FIX-02)"
    - "consolidar-decisao-final stays deterministic / NO-LLM — RNF-07a preserved (only adds export + test, no logic change)"
    - "The avaliacaoService read keeps its explicit column allowlist — never select('*') (no PII leak)"
  artifacts:
    - path: "supabase/functions/consolidar-decisao-final/index.ts"
      provides: "Exported normalizeSjtComposite (pure aggregation, already correct)"
      contains: "export function normalizeSjtComposite"
    - path: "supabase/functions/consolidar-decisao-final/__tests__/index.test.ts"
      provides: "FIX-01 regression cases (pending-only → null; MC preserved)"
      contains: "normalizeSjtComposite"
    - path: "src/features/avaliacao/services/__tests__/avaliacaoService.test.ts"
      provides: "FIX-02 regression (status='active' queried, rows returned)"
      contains: "status"
  key_links:
    - from: "supabase/functions/consolidar-decisao-final/__tests__/index.test.ts"
      to: "normalizeSjtComposite"
      via: "import { normalizeSjtComposite } from '../index.ts'"
      pattern: "normalizeSjtComposite"
    - from: "src/features/avaliacao/services/__tests__/avaliacaoService.test.ts"
      to: "avaliacaoService"
      via: "mocked supabase asserting .eq('status','active')"
      pattern: "status.*active"
---

<objective>
Lock the two already-fixed live bugs (FIX-01 `350e994`, FIX-02 `686c460`) with regression tests that would have caught each — without re-fixing the code. The fixes are correct in source; this plan adds the missing test layer + verifies `consolidar-decisao-final` is deployment-ready (the actual PROD redeploy is the [BLOCKING] Plan 07).

Purpose: FIX-01 — `consolidar-decisao-final` handles `work_sample_sjt='na'` + caso aberto pendente without zeroing the consolidado; FIX-02 — the avaliação screen loads perguntas regardless of the `status='active'` vs legacy `'ativo'` sentinel mismatch. Both are committed; the gap is a regression test that locks the behavior so a future edit can't silently reintroduce the bug.
Output: exported `normalizeSjtComposite` + 2 Deno cases; a Vitest test for `avaliacaoService` asserting `.eq('status','active')` returns rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-RESEARCH.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md

<interfaces>
<!-- Extracted from source — use directly. -->

normalizeSjtComposite (CURRENT, ~L173-182) — already correct; only add `export`:
```
function normalizeSjtComposite(sjtRows: ScoreRow[]): number | null {
  const confirmed = sjtRows.filter((r) => r.status === "sucesso" && r.score != null && r.score_max != null && r.score_max > 0);
  if (confirmed.length === 0) return null;          // FIX-01: pending-only → null
  const sumScore = confirmed.reduce((acc, r) => acc + (r.score as number), 0);
  const sumMax = confirmed.reduce((acc, r) => acc + (r.score_max as number), 0);
  if (sumMax === 0) return null;
  return (sumScore / sumMax) * 100;                 // MC sucesso preserved
}
```

avaliacaoService perguntas read (CURRENT, ~L133-139) — already correct; allowlist + .eq('status','active'):
```
const { data: perguntas } = await supabase.from('perguntas')
  .select('id, cargo, cenario, formato, tempo_est_min, rubric, status')  // allowlist — never select('*')
  .eq('status', 'active')   // FIX-02: canonical 'active'; legacy 'ativo' matched zero rows
```
getAvaliacaoContext also reads from('candidaturas') first (~L93-128) → the test mock must serve BOTH tables.

Test idiom: decisaoService.test.ts uses `vi.hoisted` + `vi.mock('@/lib/supabase/client')` + a `makeQuery` multi-table mock (~L23-54). Mirror it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export normalizeSjtComposite + add FIX-01 Deno regression cases</name>
  <files>supabase/functions/consolidar-decisao-final/index.ts, supabase/functions/consolidar-decisao-final/__tests__/index.test.ts</files>
  <read_first>
    - supabase/functions/consolidar-decisao-final/index.ts (normalizeSjtComposite ~L173-182; the comment above it explaining FIX-01; the deterministic/NO-LLM `buildRecommendation` below it to confirm no LLM)
    - supabase/functions/consolidar-decisao-final/__tests__/index.test.ts (existing file — match its assert idiom + import style)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md (`consolidar-decisao-final/__tests__/index.test.ts` section — exact 2-case shape)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-RESEARCH.md (Code Examples — FIX-01 Deno test)
  </read_first>
  <behavior>
    - Test FIX-01a: `normalizeSjtComposite([{ status:"pendente_humano", score:null, score_max:null }])` → `null` (caso_aberto pendente-único, no confirmed sub-row).
    - Test FIX-01b: `normalizeSjtComposite([{ status:"sucesso", score:8, score_max:10 }, { status:"pendente_humano", score:null, score_max:null }])` → `80` (MC preserved, caso_aberto not zeroing).
  </behavior>
  <action>
    Add the `export` keyword to the module-private `function normalizeSjtComposite` (~L173) so it is importable by the test. Make NO other change to its body — the FIX-01 logic is already correct (`350e994`); re-fixing is prohibited (CONTEXT Área 3). Add the two FIX-01 Deno cases to the existing `__tests__/index.test.ts` (or create the block if absent), importing `{ normalizeSjtComposite }` from `../index.ts`, using `assertEquals` from `std@0.224.0/assert`. The cases are the regression that would have caught the original bug. Confirm in the SUMMARY that `consolidar-decisao-final` remains deterministic / NO-LLM (RNF-07a) — this plan adds no LLM call.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && deno test --allow-read --allow-env supabase/functions/consolidar-decisao-final/__tests__/index.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function normalizeSjtComposite" supabase/functions/consolidar-decisao-final/index.ts` matches.
    - The function BODY is byte-unchanged from the committed `350e994` version (only `export` prepended).
    - Two FIX-01 cases exist asserting `null` (pending-only) and `80` (MC preserved with pending caso_aberto).
    - `deno test ... consolidar-decisao-final/__tests__/index.test.ts` exits 0.
  </acceptance_criteria>
  <done>normalizeSjtComposite exported (body untouched) and locked by 2 regression cases; consolidar stays NO-LLM.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add FIX-02 Vitest regression for avaliacaoService perguntas load</name>
  <files>src/features/avaliacao/services/__tests__/avaliacaoService.test.ts</files>
  <read_first>
    - src/features/avaliacao/services/avaliacaoService.ts (getAvaliacaoContext: candidaturas read ~L93-128, perguntas read ~L133-139 with `.eq('status','active')` + allowlist)
    - src/features/decisao/services/__tests__/decisaoService.test.ts (the `vi.hoisted` + `vi.mock('@/lib/supabase/client')` + `makeQuery` multi-table mock idiom to mirror)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md (`avaliacaoService test` section — mock shape + multi-table note)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-RESEARCH.md (Code Examples — FIX-02 Vitest test)
  </read_first>
  <behavior>
    - Test FIX-02: calling `getAvaliacaoContext` queries `perguntas` with `.eq('status','active')` (assert `toHaveBeenCalledWith('status','active')`) and returns the mocked rows (length > 0). On legacy `'ativo'` the mock-returned rows would be empty → the assertion guards the sentinel.
  </behavior>
  <action>
    Create `src/features/avaliacao/services/__tests__/avaliacaoService.test.ts` (the `__tests__/` dir already holds bigfiveService/redacaoService/scoresRhService tests). Mock `@/lib/supabase/client` via `vi.hoisted` so `from('perguntas').select(...).eq(...)` is spy-able and returns `{ data: [{ id:'p1', status:'active' }], error: null }`; the mock must ALSO serve the earlier `from('candidaturas')` read in `getAvaliacaoContext` (multi-table — mirror `decisaoService.test.ts` `makeQuery`). Assert `eq` was called with `('status','active')` and the returned perguntas length > 0. This is the regression that would have caught the `'ativo'`/`'active'` mismatch (`686c460`). Do NOT change the service; FIX-02 is already correct (re-fixing prohibited). Keep the test pure/mocked — no network.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && npm run test:run -- src/features/avaliacao/services/__tests__/avaliacaoService.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `src/features/avaliacao/services/__tests__/avaliacaoService.test.ts` exists.
    - A test asserts the perguntas query used `.eq('status','active')` (`toHaveBeenCalledWith('status','active')`) and returned rows (length > 0).
    - The mock serves both `from('candidaturas')` and `from('perguntas')`.
    - `avaliacaoService.ts` is unchanged (no re-fix).
    - `npm run test:run` for the file exits 0.
  </acceptance_criteria>
  <done>FIX-02 locked by a mocked-supabase regression asserting the canonical `'active'` sentinel returns rows; service untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RH client → avaliacaoService → DB (perguntas) | candidate answer-key-adjacent read; must stay allowlisted (no PII/answer-key leak) |
| consolidar EF (deterministic) → decisao record | aggregation must never auto-decide (RNF-07a) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-T | Tampering | consolidar consolidado | mitigate | `normalizeSjtComposite` body is byte-unchanged; consolidar stays deterministic / NO-LLM. RNF-07a preserved — no LLM, no auto-decision added. |
| T-18-03-ID | Information Disclosure | avaliacaoService perguntas read | mitigate | The read keeps its explicit column allowlist (`id,cargo,cenario,formato,tempo_est_min,rubric,status`) — never `select('*')` (reference_select_star_leaks_pii). Test does not relax it. |
| T-18-03-V | (Validation) | regression coverage gap | mitigate | The two bugs were live in PROD with zero test coverage; these tests close the Nyquist gap so a future edit can't silently regress the sentinel/aggregation. |
| T-18-SC | Tampering | imports | mitigate | No package added; tests use existing vitest/deno + static imports. No install → no slopcheck gate. |
</threat_model>

<verification>
- `deno test --allow-read --allow-env supabase/functions/consolidar-decisao-final/__tests__/index.test.ts` green.
- `npm run test:run -- src/features/avaliacao/services/__tests__/avaliacaoService.test.ts` green.
- `grep -n "export function normalizeSjtComposite" supabase/functions/consolidar-decisao-final/index.ts` matches.
- Diff confirms no behavioral change to `normalizeSjtComposite` body or `avaliacaoService` (only `export` + new test files).
</verification>

<success_criteria>
- FIX-01 and FIX-02 each locked by a regression test that would have caught the original live bug.
- No re-fix: the already-correct source is unchanged except for the `export` keyword.
- consolidar stays deterministic/NO-LLM; avaliacaoService read stays allowlisted.
- (PROD deploy verification of consolidar-decisao-final = [BLOCKING] Plan 07.)
</success_criteria>

<output>
Create `.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-03-SUMMARY.md` when done.
</output>
