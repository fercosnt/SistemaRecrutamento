---
phase: 18-resili-ncia-das-efs-de-ia-bugs-do-funil
plan: 06
type: execute
wave: 2
depends_on: ["18-04", "18-05"]
files_modified:
  - src/features/decisao/components/ConsolidacaoDashboard.tsx
  - src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx
  - src/features/avaliacao/components/SjtCasoAbertoScreen.tsx
  - src/features/avaliacao/components/RedacaoEditorScreen.tsx
  - src/features/triagem/components/ComparativoScreen.tsx
autonomous: true
requirements: [RESIL-03]
must_haves:
  truths:
    - "Every AI-backed candidate screen (BigFive, SJT caso aberto, redação) renders via <AsyncState> — loading/slow/error/retry, never a blank screen (D-Área2: adopt on candidate AI screens)"
    - "Every AI-backed RH screen (Consolidação, Comparativo) renders via <AsyncState> with retry (D-Área2: adopt on RH screens)"
    - "Each adopter passes errorCode pulled from its query/service error so AI_UNAVAILABLE shows the sobrecarga copy and other errors show generic (Specifics: 503 contract casa com <AsyncState>)"
    - "Each adopter's onRetry calls refetch()/re-invoke; slow-call screens (bigfive ~30s) show the slow copy + disable double-submit"
    - "ConsolidacaoDashboard's prior inline retry block is replaced by <AsyncState> (the exemplar becomes the shared default — no per-screen drift)"
  artifacts:
    - path: "src/features/decisao/components/ConsolidacaoDashboard.tsx"
      provides: "ConsolidacaoDashboard via <AsyncState> with errorCode"
      contains: "AsyncState"
    - path: "src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx"
      provides: "BigFive devolutiva via <AsyncState> (slow ~30s + error+retry)"
      contains: "AsyncState"
    - path: "src/features/triagem/components/ComparativoScreen.tsx"
      provides: "Comparativo via <AsyncState> (slow + error+retry, errorCode from invokeComparativo)"
      contains: "AsyncState"
  key_links:
    - from: "src/features/decisao/components/ConsolidacaoDashboard.tsx"
      to: "src/components/ui/AsyncState.tsx"
      via: "<AsyncState isLoading isError errorCode onRetry={refetch}>"
      pattern: "AsyncState"
    - from: "src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx"
      to: "src/components/ui/AsyncState.tsx"
      via: "<AsyncState> wrapping the devolutiva read region"
      pattern: "AsyncState"
---

<objective>
Adopt the shared `<AsyncState>` (Plan 04) on the five AI-backed screens, threading each screen's `errorCode` (from the now-wired services, Plan 05) so candidate and RH always see loading / slow / error / retry — never a blank screen — and overload shows the sobrecarga copy while other failures show generic.

Purpose: RESIL-03 adoption surface. The wrapper and the error_code plumbing exist; this plan connects them on the actual screens (per 18-UI-SPEC §Adoption Surface). `ConsolidacaoDashboard`'s existing inline retry becomes the shared default; the bare-skeleton candidate screens gain error+retry and the slow-call copy.
Output: five screens rendering their AI-backed read/result region via `<AsyncState>` with `errorCode` + `onRetry`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-UI-SPEC.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-04-SUMMARY.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-05-SUMMARY.md

<interfaces>
<!-- AsyncState API (Plan 04) + adoption table (18-UI-SPEC §Adoption Surface). -->

<AsyncState isLoading? isError? isEmpty? errorCode? slowAfterMs? onRetry? retrying? glass? copy? children>

Hook contract all adopters share (TanStack Query v5 — useConsolidacao.ts ~L33-41 template):
  useQuery({ ..., staleTime: 5*60*1000, retry: 2 }); read { data, isLoading, isError, error, refetch } directly — NO onError callback (removed in v5).
  onRetry={() => refetch()}; errorCode pulled from `error` (a *ServiceError carrying error_code in details after Plan 05).

| Screen | Persona | File | Current → target |
|--------|---------|------|------------------|
| ConsolidacaoDashboard | RH | src/features/decisao/components/ConsolidacaoDashboard.tsx | inline retry ~L90-127 → migrate to <AsyncState>; pass errorCode from useConsolidacao().error |
| BigFiveQuestionnaireScreen | Candidato | src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx | bare skeleton → slow (~30s) + error+retry |
| SjtCasoAbertoScreen | Candidato | src/features/avaliacao/components/SjtCasoAbertoScreen.tsx | loading only → error+retry |
| RedacaoEditorScreen | Candidato | src/features/avaliacao/components/RedacaoEditorScreen.tsx | has refetch + "Tentar novamente" → migrate to shared wrapper |
| ComparativoScreen | RH | src/features/triagem/components/ComparativoScreen.tsx | isGenerating → slow + error+retry; errorCode from invokeComparativo |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt <AsyncState> on the RH screens (Consolidação + Comparativo)</name>
  <files>src/features/decisao/components/ConsolidacaoDashboard.tsx, src/features/triagem/components/ComparativoScreen.tsx</files>
  <read_first>
    - src/features/decisao/components/ConsolidacaoDashboard.tsx (inline loading/error/retry ~L90-127 — the exemplar to migrate)
    - src/features/decisao/hooks/useConsolidacao.ts (TanStack v5 surface ~L29-41 — refetch/error)
    - src/features/triagem/components/ComparativoScreen.tsx (isGenerating + the invokeComparativo result)
    - src/components/ui/AsyncState.tsx (the wrapper API)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-UI-SPEC.md (Adoption Surface + States Contract)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md (`Adoption sites` table + hook contract)
  </read_first>
  <action>
    Migrate `ConsolidacaoDashboard`'s inline loading/error/retry block (~L90-127) to render its AI-backed result region inside `<AsyncState isLoading={...} isError={...} errorCode={...} onRetry={() => refetch()}>`. Pull `errorCode` from `useConsolidacao().error` (the `DecisaoServiceError.details.error_code` surfaced in Plan 05). Remove the now-duplicated inline AlertTriangle/GlassButton retry markup (the shared wrapper owns it — the exemplar becomes the default). For `ComparativoScreen`: wrap the comparativo result region in `<AsyncState>`, mapping `isGenerating` to the loading/slow state (slowAfterMs default 8000 → slow copy for the multi-second invoke), `isError`+`errorCode` from `invokeComparativo` (which now surfaces both MIXED_VAGA and AI_UNAVAILABLE via Plan 05), and `onRetry` re-invoking. PRESERVE the existing MIXED_VAGA UX (it already had its own copy path) — if MIXED_VAGA needs a distinct message, branch on that errorCode in the caller and pass the appropriate copy override; do NOT regress the MIXED_VAGA behavior. Use the TanStack v5 surface directly (no `onError`). Keep all existing data rendering inside `<AsyncState>` children (success path unchanged).
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && npm run lint 2>&1 | tail -3 && npm run test:run -- src/features/decisao src/features/triagem 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AsyncState" src/features/decisao/components/ConsolidacaoDashboard.tsx src/features/triagem/components/ComparativoScreen.tsx` matches in both.
    - ConsolidacaoDashboard passes `errorCode` from its query error and `onRetry={() => refetch()}`; its prior inline AlertTriangle/GlassButton retry block is removed (now owned by <AsyncState>).
    - ComparativoScreen's MIXED_VAGA outcome is preserved (existing triagem tests still green).
    - `npm run lint` baseline not regressed; decisao + triagem tests green.
  </acceptance_criteria>
  <done>Both RH screens render their AI-backed region via <AsyncState> with errorCode + retry; MIXED_VAGA preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt <AsyncState> on the candidate screens (BigFive + SJT + Redação)</name>
  <files>src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx, src/features/avaliacao/components/SjtCasoAbertoScreen.tsx, src/features/avaliacao/components/RedacaoEditorScreen.tsx</files>
  <read_first>
    - src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx (current bare skeleton + the devolutiva read; the submit button)
    - src/features/avaliacao/components/SjtCasoAbertoScreen.tsx (loading-only state)
    - src/features/avaliacao/components/RedacaoEditorScreen.tsx (existing refetch + "Tentar novamente" to migrate)
    - src/features/avaliacao/services/bigfiveService.ts + avaliacaoService.ts (error_code now surfaced — Plan 05)
    - src/components/ui/AsyncState.tsx (wrapper API)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-UI-SPEC.md (Adoption Surface + Slow-call submit affordance + 44px touch target)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md (`Adoption sites` table)
  </read_first>
  <action>
    Wrap each candidate screen's AI-backed read/result region in `<AsyncState>`: (a) `BigFiveQuestionnaireScreen` — the ~30s devolutiva read shows the slow copy after slowAfterMs (default 8000) + error+retry (replace the bare skeleton); pass `errorCode` from the devolutiva service error; the submit button keeps its inline in-flight pattern ("Enviando…" + disabled to prevent double-submit, 18-UI-SPEC §Slow-call submit affordance). (b) `SjtCasoAbertoScreen` — add error+retry to the loading-only state via `<AsyncState>` (in-flight "Processando…" on the submit). (c) `RedacaoEditorScreen` — migrate its existing `refetch` + "Tentar novamente" markup to `<AsyncState onRetry={() => refetch()}>` for consistency (the wrapper owns the retry button now). All retry buttons honor `min-h-[44px]` (carried by the wrapper). Pull `errorCode` from each screen's query/service error so AI_UNAVAILABLE → sobrecarga copy. NEVER leave a blank screen. Keep success-path data rendering inside `<AsyncState>` children. Use TanStack v5 directly (no `onError`).
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && npm run lint 2>&1 | tail -3 && npm run test:run -- src/features/avaliacao 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AsyncState" src/features/avaliacao/components/BigFiveQuestionnaireScreen.tsx src/features/avaliacao/components/SjtCasoAbertoScreen.tsx src/features/avaliacao/components/RedacaoEditorScreen.tsx` matches in all three.
    - BigFive shows the slow copy on the ~30s devolutiva (slowAfterMs) and the submit button stays disabled while in flight ("Enviando…").
    - RedacaoEditorScreen's prior bespoke "Tentar novamente" is now the shared <AsyncState> retry.
    - Each screen passes `errorCode` from its service error (AI_UNAVAILABLE → overload copy).
    - `npm run lint` baseline not regressed; avaliacao tests green.
  </acceptance_criteria>
  <done>All three candidate AI screens render via <AsyncState> (slow + error + retry, errorCode threaded); no blank screen; double-submit disabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service error → screen → <AsyncState> | the screen passes only a code, never raw error/PII; candidate-facing screens must not leak internal detail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06-ID | Information Disclosure | candidate AI screens error rendering | mitigate | Screens pass only `errorCode` (a string from the service `details.error_code`) into <AsyncState>; the wrapper renders static verbatim PT-BR copy — no raw error/stack/PII reaches the candidate (ASVS V7, reference_select_star_leaks_pii). |
| T-18-06-T | Tampering | retry action | mitigate | Retry re-runs a read/invoke (non-destructive); <AsyncState> disables it while retrying (no double-submit). No write to candidaturas; RNF-07a unaffected. |
| T-18-06-T2 | Tampering | ComparativoScreen MIXED_VAGA | mitigate | The MIXED_VAGA error path is preserved (regression-guarded by existing triagem tests) — adopting <AsyncState> must not collapse it into a generic error. |
| T-18-SC | Tampering | imports | mitigate | No package added; screens import the existing <AsyncState> + vendored primitives. No slopcheck gate. |
</threat_model>

<verification>
- `npm run test:run -- src/features/decisao src/features/triagem src/features/avaliacao` green.
- `npm run lint` baseline not regressed.
- `grep -rn "AsyncState" src/features/{decisao,triagem,avaliacao}/components/` matches all five screens.
- Manual visual check of slow/error/retry per screen is DEFERRED to Phase 21 live UAT (RESEARCH Manual-Only Verifications) — not gated here.
</verification>

<success_criteria>
- All five AI-backed screens render via <AsyncState> with loading/slow/error/retry — never a blank screen.
- errorCode threaded so AI_UNAVAILABLE shows the sobrecarga copy; generic errors show generic copy.
- ConsolidacaoDashboard's inline retry replaced by the shared default; MIXED_VAGA preserved.
- Live real-latency visual verification deferred to Phase 21.
</success_criteria>

<output>
Create `.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-06-SUMMARY.md` when done.
</output>
