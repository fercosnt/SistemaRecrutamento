---
phase: 18-resili-ncia-das-efs-de-ia-bugs-do-funil
plan: 07
type: execute
wave: 3
depends_on: ["18-01", "18-02", "18-03"]
files_modified: []
autonomous: false
requirements: [RESIL-01, RESIL-02, FIX-01]
user_setup: []
must_haves:
  truths:
    - "Every AI EF that imports callAi is redeployed to PROD so the _shared/ai-client.ts RESIL-01 change actually takes effect (Pitfall 6: _shared bundle freezes per-EF — partial deploy leaves stale EFs hanging)"
    - "gerar-devolutiva-bigfive is redeployed carrying the RESIL-02 parallelization"
    - "consolidar-decisao-final deploy state is verified in PROD to carry the FIX-01 commit (350e994) — redeployed if stale"
    - "Redeploy is a [BLOCKING] human-gated PROD action via Supabase MCP/CLI (M2 precedent); not auto-approvable"
    - "Live round-trip verification under real Anthropic overload is DEFERRED to Phase 21 — this plan only deploys + diffs, does not run a live UAT"
  artifacts: []
  key_links:
    - from: "supabase/functions/_shared/ai-client.ts (RESIL-01)"
      to: "all AI EFs deployed bundles"
      via: "supabase functions deploy / MCP deploy_edge_function per EF"
      pattern: "deploy"
---

<objective>
Deploy the Phase 18 EF code changes to PROD: redeploy EVERY AI EF that imports `callAi` (so the RESIL-01 `_shared/ai-client.ts` timeout change leaves the bundle freeze) plus `gerar-devolutiva-bigfive` (RESIL-02), and verify `consolidar-decisao-final` carries the FIX-01 commit (redeploy if stale). This is the single most important runtime-state item: until each EF is redeployed it keeps its frozen `_shared` bundle and still hangs.

Purpose: RESIL-01/RESIL-02/FIX-01 only take effect in PROD when the EFs are redeployed (Pitfall 6, `reference_ef_shared_bundle_freeze`). This is a `[BLOCKING]` human-gated PROD action (M2 precedent). Live round-trip verification under real Anthropic overload is DEFERRED to Phase 21 (PROD-01/02) to avoid duplicating UAT setup.
Output: redeployed AI EFs + a deployed-vs-local diff confirmation recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-RESEARCH.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-01-SUMMARY.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-02-SUMMARY.md
@.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-03-SUMMARY.md

<redeploy_set>
<!-- The 7 AI EFs importing callAi (verified by grep) + consolidar (verify). -->
EFs importing callAi (MUST redeploy — they bundle the changed _shared/ai-client.ts):
  - analise-candidato-individual
  - avaliar-redacao
  - avaliar-redacao-cultural
  - avaliar-transcricao-entrevista
  - comparativo-candidatos
  - gerar-guia-entrevista
  - gerar-devolutiva-bigfive   (also carries RESIL-02)
consolidar-decisao-final — does NOT import callAi (NO-LLM) but MUST be verified deployed with FIX-01 (350e994); redeploy if stale.
</redeploy_set>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate redeploy set + diff deployed-vs-local for each AI EF</name>
  <files></files>
  <read_first>
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-RESEARCH.md (Open Question 3 + Pitfall 6 + Runtime State Inventory — the redeploy set + verify consolidar)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-PATTERNS.md (`Deno EF redeploy = ALL callAi consumers` section — the exact EF list)
    - .planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-01-SUMMARY.md, 18-02-SUMMARY.md, 18-03-SUMMARY.md (what changed in each EF)
  </read_first>
  <action>
    Confirm the redeploy set against the live codebase: run `grep -rl "callAi" supabase/functions/*/index.ts` and reconcile with the redeploy_set list (expect the 7 EFs). For each AI EF in the set PLUS `consolidar-decisao-final`, use the Supabase MCP `get_edge_function` (or CLI equivalent) to diff the deployed bundle against local — record which EFs are stale (i.e. carry a frozen pre-Phase-18 `_shared/ai-client.ts` or, for bigfive, the pre-parallel handler; for consolidar, whether it carries FIX-01 `350e994`). Produce a clear stale-vs-current table in the SUMMARY. Do NOT deploy in this task — investigation + diff only. This resolves RESEARCH Open Question 3.
  </action>
  <verify>
    <automated>cd "/Users/fernando/Cursor Repo/DB Sistema de recrutamento" && grep -rl "callAi" supabase/functions/*/index.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - The redeploy set is confirmed by `grep -rl "callAi"` (7 EFs) + consolidar.
    - SUMMARY records a deployed-vs-local diff/status per EF (stale or current), including whether consolidar carries FIX-01.
    - No deploy performed in this task.
  </acceptance_criteria>
  <done>Redeploy set enumerated and each EF's deployed-vs-local state diffed; Open Question 3 resolved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Redeploy all AI EFs to PROD (human-gated)</name>
  <files></files>
  <action>
    [Human-gated PROD deploy — see what-built / how-to-verify]. After approval, Claude redeploys via Supabase MCP `deploy_edge_function` (or CLI `supabase functions deploy <name>`): all 7 callAi-importing EFs + verify/redeploy consolidar-decisao-final, then re-diff via `get_edge_function`.
  </action>
  <verify>
    <human-check>Post-deploy `get_edge_function` diff confirms each redeployed EF carries the Phase-18 `_shared/ai-client.ts` (and bigfive the parallel handler); EF logs show no ERR_MODULE_NOT_FOUND.</human-check>
  </verify>
  <what-built>
    Phase 18 EF code is merged and ready to deploy to PROD:
    - RESIL-01: `_shared/ai-client.ts` per-call timeout + maxRetries:0 + env-config (affects ALL 7 AI EFs via bundle).
    - RESIL-02: `gerar-devolutiva-bigfive` parallelized (5 dims, allSettled, 1 attempt, per-dim degrade).
    - FIX-01: `consolidar-decisao-final` (export added; FIX-01 logic from 350e994 already correct).
    All Deno + Vitest regression tests are green (Plans 01-03). Task 1 produced the deployed-vs-local diff table.
  </what-built>
  <how-to-verify>
    This is a PROD deploy gate. Approve to let Claude redeploy via Supabase MCP `deploy_edge_function` (or CLI `supabase functions deploy <name>`):
    1. Redeploy EVERY AI EF that imports callAi (Pitfall 6 — partial deploy leaves stale EFs hanging):
       analise-candidato-individual, avaliar-redacao, avaliar-redacao-cultural, avaliar-transcricao-entrevista, comparativo-candidatos, gerar-guia-entrevista, gerar-devolutiva-bigfive.
    2. Verify `consolidar-decisao-final` is deployed with FIX-01; redeploy if Task 1 flagged it stale.
    3. After deploy, re-run `get_edge_function` diff to confirm each bundle now carries the Phase-18 `_shared/ai-client.ts` (and bigfive the parallel handler).
    Preserve: static `npm:` imports (Pitfall 7 — no `.join("")` dynamic import), two-client authenticate-THEN-authorize, and the existing serve-wrapper authz on every EF (do NOT weaken). New env vars `AI_CALL_TIMEOUT_MS`/`MAX_ATTEMPTS` are OPTIONAL (code default-guards; absence is safe) — set them in EF secrets only if a non-default value is desired.
    NOTE: live round-trip verification under real Anthropic overload is DEFERRED to Phase 21 — do NOT run a live PROD UAT here.
  </how-to-verify>
  <resume-signal>Type "approved" to deploy, or describe concerns / which EFs to hold.</resume-signal>
  <acceptance_criteria>
    - All 7 callAi-importing EFs redeployed; post-deploy `get_edge_function` diff shows each now carries the Phase-18 `_shared/ai-client.ts`.
    - `gerar-devolutiva-bigfive` deployed bundle carries the parallel handler (RESIL-02).
    - `consolidar-decisao-final` confirmed deployed with FIX-01 (350e994) — redeployed if Task 1 flagged stale.
    - Static `npm:` imports + two-client authenticate-THEN-authorize preserved in every redeployed EF (no `ERR_MODULE_NOT_FOUND`, no weakened authz).
    - No live PROD round-trip UAT run (deferred to Phase 21).
  </acceptance_criteria>
  <done>Every AI EF redeployed to PROD carrying the Phase-18 _shared change; bigfive carries the parallel handler; consolidar confirmed with FIX-01; authz + static imports preserved; live UAT deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local EF code → PROD deployed bundle | a partial/stale deploy leaves some EFs hanging or running pre-Phase-18 authz/contract |
| deployer → PROD | privileged PROD action; human-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07-DoS | Denial of Service | partial redeploy | mitigate | Redeploy ALL 7 callAi-importing EFs (Pitfall 6); post-deploy `get_edge_function` diff confirms each carries the Phase-18 bundle. A missed EF keeps the 60-min hang. |
| T-18-07-T | Tampering | EF deploy bundle | mitigate | Static `npm:` imports preserved (Pitfall 7 — `reference_ef_npm_join_import_bug`); a dynamic import would 500 the EF. Diff verifies the deployed bundle. |
| T-18-07-EoP | Elevation of Privilege | serve-wrapper authz on redeploy | mitigate | The redeploy must carry the existing two-client authenticate-THEN-authorize + role/ownership checks (reference_ef_authenticate_vs_authorize, ASVS V4) — verify preserved post-deploy; no authz weakening introduced by Phase 18 edits. |
| T-18-07-ID | Information Disclosure | new env vars | accept | `AI_CALL_TIMEOUT_MS`/`MAX_ATTEMPTS` are non-secret ops tunables with safe defaults; absence is safe (code default-guards). No secret exposure. |
| T-18-07-SC | Tampering | PROD deploy gate | mitigate | Deploy is a `[BLOCKING]` human-gated checkpoint (not auto-approvable, `workflow.auto_advance` ignored) per M2 PROD precedent — a human authorizes the privileged action. |
</threat_model>

<verification>
- Post-deploy `get_edge_function` diff: each of the 7 callAi EFs carries the Phase-18 `_shared/ai-client.ts`.
- bigfive deployed bundle carries the parallel handler.
- consolidar-decisao-final confirmed deployed with FIX-01.
- EF logs show no `ERR_MODULE_NOT_FOUND` post-deploy (static imports intact).
- Live round-trip behavior under real overload = Phase 21 (NOT verified here).
</verification>

<success_criteria>
- All AI EFs importing callAi redeployed; the RESIL-01 timeout change is live in every one (no stale bundle).
- bigfive parallelization (RESIL-02) live; consolidar FIX-01 confirmed deployed.
- Static imports + authenticate-THEN-authorize preserved in every redeployed EF.
- Live PROD round-trip verification explicitly deferred to Phase 21.
</success_criteria>

<output>
Create `.planning/phases/18-resili-ncia-das-efs-de-ia-bugs-do-funil/18-07-SUMMARY.md` when done (include the deployed-vs-local diff table + which EFs were redeployed).
</output>
