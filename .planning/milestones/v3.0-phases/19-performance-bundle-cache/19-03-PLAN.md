---
phase: 19-performance-bundle-cache
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/features/entrevista/hooks/useEntrevistaScorecard.ts
  - src/features/entrevista/components/EntrevistaWorkspace.tsx
  - src/features/triagem/hooks/useRedacaoRevisao.ts
  - src/features/triagem/components/RedacaoReviewPanel.tsx
  - src/features/vagas/hooks/useCandidaturas.ts
autonomous: true
requirements: [PERF-04]
must_haves:
  truths:
    - "salvarAvaliacao success invalidates decisaoKeys.consolidacao(candidaturaId, vagaId) — the Decisão Final dashboard refreshes after an interview scorecard save (per CONTEXT Área 2 Gap A; PERF-04 closes PERF-01)"
    - "salvarRevisao success invalidates decisaoKeys.consolidacao(rowCandidaturaId, vagaId) using the per-row candidatura_id — not a broad namespace (per CONTEXT Área 2 Gap B — invalidação ALVO, não broad)"
    - "useCandidaturas (candidate dashboard, staleTime 1min) carries refetchOnWindowFocus:true so a cross-client status change is visible in ≤60s on refocus, while the GLOBAL default stays false (per CONTEXT Área 2 — per-query freshness, keep global default; ≤60s)"
    - "No new write to candidaturas by trait/score/idade; consolidacao stays read-only/advisory — RNF-07a preserved (per ROADMAP M3 invariant)"
  artifacts:
    - path: "src/features/entrevista/hooks/useEntrevistaScorecard.ts"
      provides: "vagaId param + targeted consolidacao invalidation on salvarAvaliacao"
      contains: "decisaoKeys.consolidacao"
    - path: "src/features/triagem/hooks/useRedacaoRevisao.ts"
      provides: "candidaturaId threaded through mutation variables + targeted consolidacao invalidation"
      contains: "decisaoKeys.consolidacao"
    - path: "src/features/vagas/hooks/useCandidaturas.ts"
      provides: "per-query refetchOnWindowFocus:true (staleTime 1min already)"
      contains: "refetchOnWindowFocus: true"
  key_links:
    - from: "src/features/entrevista/hooks/useEntrevistaScorecard.ts"
      to: "decisaoKeys.consolidacao"
      via: "onSuccess invalidateQueries with candidaturaId + new vagaId param"
      pattern: "invalidateQueries[\\s\\S]*?decisaoKeys\\.consolidacao"
    - from: "src/features/entrevista/components/EntrevistaWorkspace.tsx"
      to: "useEntrevistaScorecard"
      via: "pass already-available vagaId (contexto?.vaga_id) as 2nd positional arg"
      pattern: "useEntrevistaScorecard\\(candidaturaId, vagaId"
    - from: "src/features/triagem/components/RedacaoReviewPanel.tsx"
      to: "useRedacaoRevisao.salvarRevisao"
      via: "mutate variables carry selected.candidatura_id"
      pattern: "candidaturaId: selected\\.candidatura_id"
    - from: "src/features/vagas/hooks/useCandidaturas.ts"
      to: "TanStack Query"
      via: "per-query refetchOnWindowFocus:true paired with staleTime ≤60s"
      pattern: "refetchOnWindowFocus: true"
---

<objective>
PERF-04: a write by candidate or RH is visible in the candidate profile/dashboard in ≤60s. Two surgical invalidation-gap fixes plus one freshness toggle: (Gap A) `useEntrevistaScorecard.salvarAvaliacao` and (Gap B) `useRedacaoRevisao.salvarRevisao` add a TARGETED `decisaoKeys.consolidacao(candidaturaId, vagaId)` invalidation (threading the ids — the load-bearing detail); and `useCandidaturas` (the candidate dashboard read, staleTime already 1min) gets per-query `refetchOnWindowFocus:true` for the cross-client ≤60s guarantee, while the global default stays `false`.

Purpose: Closes tech-debt PERF-01 (PERF-04). The Decisão Final dashboard stops showing pre-write data; an open candidate tab re-reads a status change within ≤60s on refocus. Turns the two Plan-01 regression tests GREEN.
Output: 5 modified files (2 hooks + 2 call sites + 1 read), invalidation always TARGETED (never broad), RNF-07a preserved (no candidaturas writes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-performance-bundle-cache/19-CONTEXT.md
@.planning/phases/19-performance-bundle-cache/19-RESEARCH.md
@.planning/phases/19-performance-bundle-cache/19-PATTERNS.md
@.planning/phases/19-performance-bundle-cache/19-01-SUMMARY.md

<interfaces>
<!-- Exact code paths (file:line VERIFIED this session) + the regression tests from Plan 01. -->

decisaoKeys.consolidacao signature (useConsolidacao.ts:21-22):
  import { decisaoKeys } from '@/features/decisao/hooks/useConsolidacao'
  decisaoKeys.consolidacao(candidaturaId, vagaId) → [...decisaoKeys.all, 'consolidacao', candidaturaId, vagaId]

Gap A — useEntrevistaScorecard.ts (current L135-162):
  signature: useEntrevistaScorecard(candidaturaId: string|undefined, options?)
  salvar.onSuccess: invalidates entrevistaKeys.scorecard(candidaturaId||'')
  FIX: add `vagaId: string|undefined` as 2nd positional param (BEFORE options); in onSuccess,
       if (candidaturaId && vagaId) invalidateQueries({ queryKey: decisaoKeys.consolidacao(candidaturaId, vagaId) })
       (keep the existing scorecard invalidation). NOT decisaoKeys.all (broad is forbidden — CONTEXT ALVO).

Gap A call site — EntrevistaWorkspace.tsx:
  vagaId ALREADY computed at L68 (const vagaId = contexto?.vaga_id) and passed to useGuiaEntrevista(candidaturaId, vagaId) at L74.
  current scorecard call (L87): useEntrevistaScorecard(candidaturaId)
  FIX: useEntrevistaScorecard(candidaturaId, vagaId)  — mirror the L74 useGuiaEntrevista call.
  handleSalvarAvaliacao + onSalvar wiring UNCHANGED.

Gap B — useRedacaoRevisao.ts (current L54-67):
  salvar.mutationFn: ({ redacaoId, payload }) => salvarRevisao(redacaoId, payload)
  salvar.onSuccess: invalidates redacaoRevisaoKeys.queue(vagaId||'') + redacaoRevisaoKeys.duvidas()
  hook has `vagaId` (queue is per-vaga); per-essay candidatura_id is on the ROW (RedacaoReviewRow.candidatura_id, revisaoRedacaoService.ts:60).
  FIX: extend mutation variables to { redacaoId, candidaturaId, payload }; mutationFn STILL calls salvarRevisao(redacaoId, payload)
       (candidaturaId is for invalidation only, NOT passed to the service); onSuccess(_d, vars):
       if (vagaId && vars.candidaturaId) invalidateQueries({ queryKey: decisaoKeys.consolidacao(vars.candidaturaId, vagaId) })
       (keep the existing queue + duvidas invalidations).

Gap B call site — RedacaoReviewPanel.tsx:
  selected = rows.find(r => r.id === selectedId) (L176); selected.candidatura_id available.
  current mutate (L190-191): salvarRevisao.mutate({ redacaoId: selected.id, payload }, { onSuccess, onError })
  FIX: salvarRevisao.mutate({ redacaoId: selected.id, candidaturaId: selected.candidatura_id, payload }, {...})

Freshness — useCandidaturas.ts (current useQuery options L102-116):
  already has staleTime: 1*60*1000 (1min, ≤60s ✓); inherits global refetchOnWindowFocus:false.
  FIX: add per-query `refetchOnWindowFocus: true` (Pitfall 5 — needs BOTH refetchOnWindowFocus AND staleTime≤60s to fire).

Candidate-visible-read AUDIT (CONTEXT Área 2 quick audit; RESEARCH Open Q1 / A4) — VERIFIED this session:
  - useCandidaturas (DashboardCandidatoPage) → MUTABLE status, staleTime 1min → APPLY refetchOnWindowFocus:true (the known-required minimum).
  - useExplicacao (ExplicacaoCandidatoPage) → own-row rejection explanation, effectively static once a decision exists; NO staleTime override (inherits 5min); does NOT need the freshness pairing — DO NOT change.
  - DO NOT touch RH reads (useAllCandidaturas, useVagaCandidaturas) — out of scope (CONTEXT keeps RH reads as-is).
  Conclusion: useCandidaturas is the ONLY candidate-visible mutable-status read needing the pairing.

Plan 01 regression tests (turn GREEN here):
  src/features/entrevista/hooks/__tests__/useEntrevistaScorecard.test.ts (Gap A)
  src/features/triagem/hooks/__tests__/useRedacaoRevisao.test.ts (Gap B)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gap A — useEntrevistaScorecard vagaId param + targeted consolidacao invalidation + call site</name>
  <files>src/features/entrevista/hooks/useEntrevistaScorecard.ts, src/features/entrevista/components/EntrevistaWorkspace.tsx</files>
  <read_first>
    - src/features/entrevista/hooks/useEntrevistaScorecard.ts (L135-162 — signature + salvar mutation)
    - src/features/entrevista/hooks/useEntrevistaScorecard.ts (useGuiaEntrevista L66-91 — the sibling that already takes vagaId + invalidates on success, the exact pattern to mirror)
    - src/features/decisao/hooks/useConsolidacao.ts (decisaoKeys.consolidacao L19-25)
    - src/features/entrevista/components/EntrevistaWorkspace.tsx (vagaId at L68; useGuiaEntrevista call L74; useEntrevistaScorecard call L87)
    - src/features/entrevista/hooks/__tests__/useEntrevistaScorecard.test.ts (Plan 01 — the RED test this turns GREEN)
    - .planning/phases/19-performance-bundle-cache/19-PATTERNS.md (Gap A L248-294)
  </read_first>
  <behavior>
    - Test 1: useEntrevistaScorecard(candidaturaId, vagaId).salvarAvaliacao success → invalidateQueries called with decisaoKeys.consolidacao(candidaturaId, vagaId) (the Plan 01 Gap A test).
    - Test 2: the existing entrevistaKeys.scorecard invalidation still fires (not replaced).
    - Edge: when candidaturaId or vagaId is undefined, the consolidacao invalidation is skipped (guarded).
  </behavior>
  <action>
    In `src/features/entrevista/hooks/useEntrevistaScorecard.ts`: add `import { decisaoKeys } from '@/features/decisao/hooks/useConsolidacao'`. Add `vagaId: string | undefined` as the 2nd POSITIONAL parameter (BEFORE `options`). In `salvar.onSuccess`, AFTER the existing `entrevistaKeys.scorecard` invalidation, add `if (candidaturaId && vagaId) queryClient.invalidateQueries({ queryKey: decisaoKeys.consolidacao(candidaturaId, vagaId) })`. This is the TARGETED invalidation (CONTEXT Área 2 — ALVO); do NOT use `decisaoKeys.all` (broad is forbidden). Do NOT change the mutationFn or the service signature. RNF-07a: no write to candidaturas — only cache invalidation.

    In `src/features/entrevista/components/EntrevistaWorkspace.tsx`: change the scorecard hook call (L87) from `useEntrevistaScorecard(candidaturaId)` to `useEntrevistaScorecard(candidaturaId, vagaId)`, reusing the `vagaId` already computed at L68 (mirrors the L74 `useGuiaEntrevista(candidaturaId, vagaId)` call). Leave `handleSalvarAvaliacao` and the `onSalvar` wiring unchanged.
  </action>
  <verify>
    <automated>npm run test:run -- src/features/entrevista/hooks/__tests__/useEntrevistaScorecard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `useEntrevistaScorecard` signature is `(candidaturaId, vagaId, options?)`; onSuccess calls `invalidateQueries` with `decisaoKeys.consolidacao(candidaturaId, vagaId)`, guarded by both ids present.
    - The existing `entrevistaKeys.scorecard` invalidation is still present (additive, not replaced).
    - `EntrevistaWorkspace.tsx` calls `useEntrevistaScorecard(candidaturaId, vagaId)`.
    - The Plan 01 Gap A test (`useEntrevistaScorecard.test.ts`) is GREEN.
    - No `decisaoKeys.all` broad invalidation introduced; no write to `candidaturas` (RNF-07a).
    - `npm run lint` keeps tsc ≤ 258.
  </acceptance_criteria>
  <done>Gap A closed: scorecard save invalidates the targeted consolidacao key; call site threads the already-available vagaId; Gap A regression test GREEN; tsc ≤ 258.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gap B — useRedacaoRevisao thread candidatura_id + targeted invalidation; useCandidaturas freshness</name>
  <files>src/features/triagem/hooks/useRedacaoRevisao.ts, src/features/triagem/components/RedacaoReviewPanel.tsx, src/features/vagas/hooks/useCandidaturas.ts</files>
  <read_first>
    - src/features/triagem/hooks/useRedacaoRevisao.ts (salvar mutation L54-67; hook has vagaId)
    - src/features/triagem/components/RedacaoReviewPanel.tsx (selected L176; handleSalvar + mutate L189-191)
    - src/features/decisao/hooks/useConsolidacao.ts (decisaoKeys.consolidacao L19-25)
    - src/features/vagas/hooks/useCandidaturas.ts (useQuery options L102-116 — staleTime 1min already)
    - src/features/triagem/hooks/__tests__/useRedacaoRevisao.test.ts (Plan 01 — the RED test this turns GREEN)
    - .planning/phases/19-performance-bundle-cache/19-PATTERNS.md (Gap B L318-373; freshness L377-402)
  </read_first>
  <behavior>
    - Test 1: useRedacaoRevisao(vagaId).salvarRevisao success with variables { redacaoId, candidaturaId, payload } → invalidateQueries called with decisaoKeys.consolidacao(candidaturaId, vagaId) using the per-row candidatura_id (the Plan 01 Gap B test).
    - Test 2: the existing queue + duvidas invalidations still fire.
    - Edge: when vagaId or vars.candidaturaId is missing, the consolidacao invalidation is skipped.
    - useCandidaturas query options include refetchOnWindowFocus:true AND staleTime ≤ 60s.
  </behavior>
  <action>
    In `src/features/triagem/hooks/useRedacaoRevisao.ts`: add `import { decisaoKeys } from '@/features/decisao/hooks/useConsolidacao'`. Extend the `salvar` mutation variables type to `{ redacaoId: string; candidaturaId: string; payload: SalvarRevisaoPayload }` — but keep `mutationFn` calling `salvarRevisao(redacaoId, payload)` (candidaturaId is carried for invalidation ONLY; the service signature is unchanged). Change `onSuccess` to `onSuccess: (_d, vars)`; after the existing `redacaoRevisaoKeys.queue` + `redacaoRevisaoKeys.duvidas` invalidations, add `if (vagaId && vars.candidaturaId) queryClient.invalidateQueries({ queryKey: decisaoKeys.consolidacao(vars.candidaturaId, vagaId) })`. TARGETED only.

    In `src/features/triagem/components/RedacaoReviewPanel.tsx`: in `handleSalvar`, change the mutate call to `salvarRevisao.mutate({ redacaoId: selected.id, candidaturaId: selected.candidatura_id, payload }, { onSuccess, onError })` — `selected.candidatura_id` is the per-row id (RedacaoReviewRow.candidatura_id).

    In `src/features/vagas/hooks/useCandidaturas.ts`: in the `useCandidaturas` useQuery options (the one with `staleTime: 1 * 60 * 1000`), add `refetchOnWindowFocus: true` (per-query). Do NOT change the global QueryClient default in App.tsx (stays false — Pitfall 5 / Anti-Pattern: flipping the global refetches expensive RH/AI reads). Per the verified candidate-visible-read audit, useCandidaturas is the ONLY candidate-visible mutable-status read needing this — do NOT touch useExplicacao (static rejection explanation), useAllCandidaturas, or useVagaCandidaturas (RH reads, out of scope).
  </action>
  <verify>
    <automated>npm run test:run -- src/features/triagem/hooks/__tests__/useRedacaoRevisao.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `useRedacaoRevisao` salvar mutation variables include `candidaturaId`; mutationFn still calls `salvarRevisao(redacaoId, payload)` (service signature unchanged); onSuccess invalidates `decisaoKeys.consolidacao(vars.candidaturaId, vagaId)` guarded by both present.
    - Existing `redacaoRevisaoKeys.queue` + `redacaoRevisaoKeys.duvidas` invalidations still present.
    - `RedacaoReviewPanel.tsx` mutate call passes `candidaturaId: selected.candidatura_id`.
    - `useCandidaturas.ts` query options contain `refetchOnWindowFocus: true` AND `staleTime: 1 * 60 * 1000`; the global default in App.tsx is NOT changed.
    - `useExplicacao` / `useAllCandidaturas` / `useVagaCandidaturas` are unchanged.
    - The Plan 01 Gap B test (`useRedacaoRevisao.test.ts`) is GREEN.
    - `npm run lint` keeps tsc ≤ 258.
  </acceptance_criteria>
  <done>Gap B closed: redação review save invalidates the per-row targeted consolidacao key; candidate dashboard read refetches on focus (≤60s) without flipping the global; Gap B regression test GREEN; tsc ≤ 258.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client cache (TanStack Query) → Supabase (source of truth) | invalidateQueries triggers a re-read; the DB (RLS-gated) is authoritative |
| RH mutation → candidate-visible read | a write by RH must surface in the candidate's open tab within ≤60s |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Tampering | consolidacao invalidation scope | mitigate | Invalidation is TARGETED to `decisaoKeys.consolidacao(candidaturaId, vagaId)` (not broad `decisaoKeys.all`) — no over-broad cache churn, no cross-candidatura leakage of refetch. Acceptance asserts the exact key. |
| T-19-03-02 | Information Disclosure | refetchOnWindowFocus widening reads | mitigate | Applied PER-QUERY to `useCandidaturas` only (candidate's own-row list, already RLS-scoped by `candidato.id`); global default stays false so RH/AI reads are not refetched. No new data path. |
| T-19-03-03 | Tampering (integrity invariant) | consolidacao/candidaturas writes | accept/mitigate | RNF-07a: this plan only INVALIDATES (re-reads) the read-only/advisory consolidacao — it writes NOTHING to `candidaturas` by trait/score/idade. Acceptance asserts no candidaturas write introduced. |
| T-19-03-SC | Tampering | npm installs | mitigate | ZERO new packages (RESEARCH Package Legitimacy Audit: N/A) — supply-chain neutral. |
</threat_model>

<verification>
- `npm run test:run -- src/features/entrevista/hooks/__tests__/useEntrevistaScorecard.test.ts src/features/triagem/hooks/__tests__/useRedacaoRevisao.test.ts` GREEN (the two Plan 01 regression tests).
- `grep -n "decisaoKeys.consolidacao" src/features/entrevista/hooks/useEntrevistaScorecard.ts src/features/triagem/hooks/useRedacaoRevisao.ts` shows the targeted invalidation in both; `grep "decisaoKeys.all"` returns nothing new (no broad invalidation).
- `grep -n "refetchOnWindowFocus" src/features/vagas/hooks/useCandidaturas.ts` shows `true`; App.tsx global default still `false`.
- `npm run test:run` full suite GREEN; `npm run lint` tsc ≤ 258.
- Cross-client ≤60s live freshness is a Phase 21 manual/deferred UAT (not blocking here).
</verification>

<success_criteria>
- PERF-04 met (same-client path): both invalidation gaps closed with TARGETED consolidacao invalidation; the candidate dashboard read refetches on window focus with staleTime ≤60s.
- RNF-07a preserved (no candidaturas writes; consolidacao read-only/advisory).
- Both Plan 01 regression tests GREEN; full suite GREEN; tsc ≤ 258.
- Cross-client ≤60s live check deferred to Phase 21 UAT.
</success_criteria>

<output>
Create `.planning/phases/19-performance-bundle-cache/19-03-SUMMARY.md` when done.
</output>
