---
phase: 19-performance-bundle-cache
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/router/routes.tsx
  - src/App.tsx
  - vite.config.ts
  - src/features/triagem/components/ComparativoScreen.tsx
  - e2e/navegacao.spec.ts
autonomous: true
requirements: [PERF-03]
must_haves:
  truths:
    - "The candidate first paint loads only the eager route chunk + react-vendor — NOT the /rh/* /admin/* code, jsPDF, or recharts (per CONTEXT Área 1 — lazy-load /rh/* + /admin/*, candidate flows stay EAGER; PERF-03 closes HARD-02)"
    - "/rh/* and /admin/* routes still render correctly after navigation behind a branded Suspense fallback — no navigation regression (per ROADMAP Phase 19 success criterion 3)"
    - "RoleGuard stays OUTSIDE the lazy element — a candidate cannot reach an RH/admin route just because it is now a separate chunk (per CONTEXT Área 1 security + 19-RESEARCH §Security V4 — the load-bearing access-control check)"
    - "manualChunks groups react/react-dom/react-router/scheduler into ONE narrow react-vendor chunk — never a broad node_modules→vendor (per CONTEXT Área 1 + RESEARCH Pitfall 1 — avoids the prod-only circular-init blank screen)"
    - "jsPDF loads via dynamic import() at the export click in ComparativoScreen, not at top level (per CONTEXT Área 1 — heavy libs at call site)"
  artifacts:
    - path: "src/router/lazyNamed.ts"
      provides: "consumed: lazy adapter for every /rh/* /admin/* route (created in Plan 01)"
    - path: "src/components/ui/PageSkeleton.tsx"
      provides: "consumed: Suspense fallback (created in Plan 01)"
    - path: "vite.config.ts"
      provides: "build.rollupOptions.output.manualChunks narrow react-vendor"
      contains: "react-vendor"
    - path: "scripts/assert-chunks.mjs"
      provides: "consumed: PERF-03 build-output gate (created in Plan 01)"
  key_links:
    - from: "src/router/routes.tsx"
      to: "src/router/lazyNamed.ts"
      via: "lazyNamed(() => import('../components/pages/...'), 'PageName') for /rh/* /admin/*"
      pattern: "lazyNamed\\(\\(\\) => import"
    - from: "src/router/routes.tsx"
      to: "RoleGuard"
      via: "element: <RoleGuard ...><LazyPage/></RoleGuard> (guard OUTSIDE lazy)"
      pattern: "RoleGuard[\\s\\S]*?lazy|element:.*RoleGuard"
    - from: "src/App.tsx"
      to: "src/components/ui/PageSkeleton.tsx"
      via: "<Suspense fallback={<PageSkeleton/>}> around <Outlet/> in RootLayout"
      pattern: "Suspense[\\s\\S]*?PageSkeleton"
    - from: "src/features/triagem/components/ComparativoScreen.tsx"
      to: "../pdf/exportComparativo"
      via: "await import() in the export click handler"
      pattern: "await import\\(.*exportComparativo"
---

<objective>
PERF-03: stop the candidate mobile-first first paint from paying the 2,788 kB monolith. Three concrete build/runtime moves: (1) add `build.rollupOptions.output.manualChunks` peeling a NARROW react-vendor chunk; (2) convert every `/rh/*` + `/admin/*` route `element:` to a `lazyNamed` adapter wrapped in a single `<Suspense fallback={<PageSkeleton/>}>` in RootLayout — keeping all candidate/auth/public routes EAGER and every `RoleGuard` OUTSIDE the lazy boundary; (3) push jsPDF behind a call-site `await import()` at the export click (recharts splits automatically by being inside the lazy admin route).

Purpose: Closes tech-debt HARD-02 (PERF-03). The candidate never downloads RH/admin/jsPDF/recharts code.
Output: a code-split build verified by `scripts/assert-chunks.mjs` (react-vendor + separate lazy route chunks + jsPDF out of the eager chunk + eager index < baseline), with no navigation regression (E2E) and access control unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-performance-bundle-cache/19-CONTEXT.md
@.planning/phases/19-performance-bundle-cache/19-RESEARCH.md
@.planning/phases/19-performance-bundle-cache/19-PATTERNS.md
@.planning/phases/19-performance-bundle-cache/19-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 + codebase. No exploration needed. -->

lazyNamed (Plan 01 — src/router/lazyNamed.ts):
  lazyNamed<T>(loader: () => Promise<T>, name: keyof T) → React.LazyExoticComponent
  Usage in routes.tsx: const DashboardRHPage = lazyNamed(() => import('../components/pages/DashboardRHPage'), 'DashboardRHPage')

PageSkeleton (Plan 01 — src/components/ui/PageSkeleton.tsx): named export, glass Suspense fallback.

EAGER routes to KEEP STATIC (CONTEXT — candidate first paint; routes.tsx:11-43, 65, 73):
  Public: LandingPage, VagasPublicasPage, VagaDetalhePage, ManifestoPage
  Auth: CadastroPage, LoginCandidatoPage, LoginRHPage, EsqueciSenhaPage, RedefinirSenhaPage
  Candidate: DashboardCandidatoPage, MeuPerfilCandidatoPage, InstrucoesFormularioPage, FormularioCandidaturaPage
  Avaliação barrel (L36-43): AvaliacaoContainer, SjtMultiplaEscolhaScreen, SjtCasoAbertoScreen,
    BigFiveQuestionnaireScreen, DevolutivaBigFiveView, RedacaoEditorScreen
  ProvaCognitivaScreen (L65), ExplicacaoCandidatoPage (L73 — candidate route L271)
  Local tiny components: RedirectToHub (L94), NotFoundPage (catch-all)

RH/admin static imports to CONVERT to lazyNamed (routes.tsx:46-74):
  DashboardRHPage, CandidatosRHPage, PerfilCandidatoRHPage, VagasRHPage, VagaCandidatosRHPage,
  ComparativoCandidatosPage, CriarEditarVagaPage, ConfiguracoesPage, MeuPerfilPage, SuporteRHPage,
  RelatoriosRHPage (L46-56); RedacaoReviewPanel (L59); EntrevistaWorkspace (L62); DecisaoFinalPage (L72);
  AiLogsPage, PromptVersionsPage, AiCostsPage (L68-70); BiasAuditPage (L74).
  Each currently `import { X } from '...'` → replace with `const X = lazyNamed(() => import('...'), 'X')`.
  Keep the element wrapping IDENTICAL: element: <RoleGuard role={[...]}>< X /></RoleGuard>.

manualChunks recipe (RESEARCH Pattern 1 / PATTERNS L177-196 — current build block vite.config.ts:111-114):
  build: { target:'esnext', outDir:'build', rollupOptions: { output: { manualChunks(id) {
    if (id.includes('node_modules/react/') || id.includes('node_modules/react-dom/')
        || id.includes('node_modules/react-router') || id.includes('node_modules/scheduler/'))
      return 'react-vendor'
    // else return undefined → Rollup auto-chunks lazy routes + @radix-ui
  } } } }

App.tsx RootLayout return (current L219-225) → wrap <Outlet/> in <Suspense fallback={<PageSkeleton/>}>:
  add `import { Suspense } from 'react'` (to existing react import L19) + `import { PageSkeleton } from './components/ui/PageSkeleton'`.
  DO NOT touch the QueryClient defaults (refetchOnWindowFocus:false stays global — Plan 03 handles per-query).

ComparativoScreen.tsx (current top-level import L33-37; handleExport L119-131):
  top-level → `import type { ComparativeRankingView, RankedCandidate } from '../pdf/exportComparativo'`
  handleExport → make async; `const { exportComparativo } = await import('../pdf/exportComparativo')` then call it.
  CONFIRMED: DecisaoFinalPage.tsx already imports ONLY `import type { RankedCandidate }` (Pitfall 3 clear).

E2E (e2e/navegacao.spec.ts): describeRealAuth (L59, gated by E2E_AUTH_TEST_USERS), loginRH helper (L80-90)
  with the mandatory `.blur()` workaround (RHF onBlur + disabled={!isValid}). J2 (RH hub→workspaces, L153+)
  + J3 (admin sidebar→/admin/*, L173+) already navigate the lazy areas — EXTEND their assertions to confirm
  the page heading renders behind <Suspense> (getByRole heading, NOT data — D-16). Do NOT create a new spec.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: manualChunks narrow react-vendor + lazy-convert /rh/* /admin/* routes + Suspense boundary</name>
  <files>vite.config.ts, src/router/routes.tsx, src/App.tsx</files>
  <read_first>
    - vite.config.ts (current build block L111-114; resolve.alias L62-109 — orthogonal, do not touch)
    - src/router/routes.tsx (eager imports L11-43,65,73 KEEP; RH/admin imports L46-74 CONVERT; route element blocks ~L282-469 KEEP RoleGuard wrapping)
    - src/App.tsx (RootLayout return L219-225; react import L19; QueryClient defaults L36-45 DO NOT TOUCH)
    - src/router/lazyNamed.ts (Plan 01 adapter — the helper to call)
    - src/components/ui/PageSkeleton.tsx (Plan 01 fallback)
    - .planning/phases/19-performance-bundle-cache/19-PATTERNS.md (routes conversion L55-86; App.tsx L116-147; vite.config L164-199)
  </read_first>
  <action>
    In `vite.config.ts`, extend the existing `build` block (keep `target:'esnext'`, `outDir:'build'`) by adding `rollupOptions.output.manualChunks(id)` per the interfaces recipe: return `'react-vendor'` ONLY for `node_modules/react/`, `node_modules/react-dom/`, `node_modules/react-router`, `node_modules/scheduler/`; return undefined (fall through) for everything else so Rollup auto-chunks lazy routes and @radix-ui. Do NOT write a broad `if id.includes('node_modules') return 'vendor'` — that re-triggers the prod-only circular-init blank screen (RESEARCH Pitfall 1). Leave `resolve.alias` untouched (orthogonal to manualChunks).

    In `src/router/routes.tsx`, add `import { lazyNamed } from './lazyNamed'`. For EACH RH/admin page listed in the interfaces block (L46-74), DELETE its static `import { X } from '...'` line and add a top-level `const X = lazyNamed(() => import('...'), 'X')` (use the exact module path the original import used; keep the named-export name as the second arg). Leave ALL eager candidate/auth/public/avaliação/cognitivo/explicação imports STATIC (interfaces block). Leave RedirectToHub + NotFoundPage eager. CRITICAL: do NOT change any route `element:` block — every `<RoleGuard role={[...]}>< X /></RoleGuard>` stays byte-identical so the guard renders OUTSIDE the lazy boundary (the access-control invariant). `/rh/candidato/:id` keeps RedirectToHub; admin routes keep `role="administrador"`.

    In `src/App.tsx`, add `Suspense` to the existing `react` import and `import { PageSkeleton } from './components/ui/PageSkeleton'`. In the RootLayout return, wrap `<Outlet/>` in `<Suspense fallback={<PageSkeleton />}>` (single boundary covering all lazy routes). Leave `DevNavigationMenu` (DEV-gated) and `<Toaster>` siblings as-is. DO NOT modify the QueryClient defaults — the global `refetchOnWindowFocus: false` stays (Plan 03 owns per-query freshness).
  </action>
  <verify>
    <automated>npm run build && node scripts/assert-chunks.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `vite.config.ts` manualChunks returns `'react-vendor'` for react/react-dom/react-router/scheduler and undefined otherwise (no broad node_modules→vendor branch).
    - `npm run build` succeeds with NO Rollup circular-init / "Cannot access X before initialization" error.
    - `node scripts/assert-chunks.mjs` (Plan 01) EXITS ZERO: react-vendor chunk emitted, separate lazy /rh + /admin route chunks present, eager index-*.js < 2,788,270 bytes.
    - `grep -n "lazyNamed" src/router/routes.tsx` shows one lazyNamed per RH/admin page; `grep -c "RoleGuard" src/router/routes.tsx` is unchanged vs pre-edit (no guard dropped).
    - Every candidate/auth/public/avaliação import in routes.tsx remains a static `import` (not lazyNamed).
    - `npm run lint` keeps tsc ≤ 258.
  </acceptance_criteria>
  <done>Build emits react-vendor + separate lazy RH/admin chunks, eager index shrank below baseline, every RoleGuard intact outside the lazy element, assert-chunks green, tsc ≤ 258.</done>
</task>

<task type="auto">
  <name>Task 2: Dynamic import jsPDF at the export click + extend E2E lazy-route no-regression</name>
  <files>src/features/triagem/components/ComparativoScreen.tsx, e2e/navegacao.spec.ts</files>
  <read_first>
    - src/features/triagem/components/ComparativoScreen.tsx (top-level import L33-37; handleExport L119-131)
    - src/features/triagem/pdf/exportComparativo.ts (UNCHANGED — its static jspdf import L16-17 is now reachable only via the dynamic boundary)
    - src/features/decisao/components/DecisaoFinalPage.tsx (CONFIRM it imports only `import type { RankedCandidate }` — L36 — so no other static edge pulls jsPDF eagerly)
    - e2e/navegacao.spec.ts (describeRealAuth L59; loginRH + .blur() L80-90; J2 L153+; J3 L173+)
    - .planning/phases/19-performance-bundle-cache/19-PATTERNS.md (ComparativoScreen L203-244; E2E L442-444)
  </read_first>
  <action>
    In `src/features/triagem/components/ComparativoScreen.tsx`, change the top-level `import { exportComparativo, type ComparativeRankingView, type RankedCandidate } from '../pdf/exportComparativo'` to a TYPE-ONLY import: `import type { ComparativeRankingView, RankedCandidate } from '../pdf/exportComparativo'` (drop the runtime value). Make `handleExport` async and load the value at the call site: `const { exportComparativo } = await import('../pdf/exportComparativo')` then `exportComparativo(candidates)` — preserve the existing setIsGenerating(true)/try/catch toast.success/toast.error/finally setIsGenerating(false) shape and the W1 comment about passing resolved candidates. Do NOT modify `exportComparativo.ts` itself (its static jspdf import is now behind the dynamic boundary → Rollup emits jspdf+autotable in a separate async chunk; RESEARCH Pitfall 3). Verify no other eager-graph file value-imports exportComparativo (DecisaoFinalPage already type-only — confirm).

    In `e2e/navegacao.spec.ts`, EXTEND (do not create a new spec) the existing gated journeys: in J2 (RH hub → workspace) and J3 (admin → /admin/*), after navigation add/strengthen an assertion that the page heading renders (e.g. `getByRole('heading')` for the destination) so the lazy chunk resolving behind `<Suspense>` is proven — assert the ROUTE + heading, not data (D-16). Reuse the existing `loginRH` helper with the `.blur()` workaround verbatim; keep everything inside `describeRealAuth` (gated by `E2E_AUTH_TEST_USERS`).
  </action>
  <verify>
    <automated>npm run build && node scripts/assert-chunks.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `ComparativoScreen.tsx` has NO top-level value import of `exportComparativo` (only `import type`); handleExport is `async` and contains `await import('../pdf/exportComparativo')`.
    - `node scripts/assert-chunks.mjs` confirms jspdf/autotable bytes are NOT in the eager `index-*.js` (only in a separate dynamic chunk).
    - `e2e/navegacao.spec.ts` J2 + J3 assert the destination heading renders behind Suspense; no new spec file created; `.blur()` helper reused.
    - `npm run lint` keeps tsc ≤ 258.
    - (E2E run is gated by E2E_AUTH_TEST_USERS — if unset, the suite skips; live navigation is the Phase 21 UAT. The automated gate here is build + chunk assertion.)
  </acceptance_criteria>
  <done>jsPDF loads only at the export click (out of the eager chunk per assert-chunks), E2E extended to prove lazy /rh + /admin routes render behind Suspense, tsc ≤ 258.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → /rh/* /admin/* route chunk (public JS) | the lazy chunk JS is publicly fetchable; it carries UI code, not data |
| candidate role → RH/admin route element | RoleGuard must render BEFORE the lazy element resolves |
| build config → prod runtime | a manualChunks misconfig can ship a blank prod app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Elevation of Privilege | routes.tsx RoleGuard during the lazy rewrite | mitigate | RoleGuard stays OUTSIDE/around the lazy element verbatim — `element: <RoleGuard ...><LazyPage/></RoleGuard>`; acceptance asserts `grep -c RoleGuard` unchanged. A candidate hitting a now-separate /rh chunk still renders the guard first (denies before content). |
| T-19-02-02 | Information Disclosure | lazy RH/admin route chunk fetchable by anon client | accept | By-design / no new exposure: route chunks contain UI code only; all data access is RLS + EF-authorized server-side (RESEARCH §Security). Same posture as today's monolith (the code already ships in the public bundle). Splitting it out is neutral. |
| T-19-02-03 | Denial of Service (self-inflicted) | manualChunks circular-init blank prod screen | mitigate | NARROW react-vendor only (react/react-dom/react-router/scheduler); @radix-ui left to auto-chunking; mandatory `npm run build` + assert-chunks before merge (RESEARCH Pitfall 1). |
| T-19-02-04 | Availability | lazy route shows blank on chunk-load | mitigate | Single `<Suspense fallback={<PageSkeleton/>}>` in RootLayout — every lazy route shows the branded skeleton, never blank (RESEARCH Pattern 3). |
| T-19-02-SC | Tampering | npm installs | mitigate | ZERO new packages (RESEARCH Package Legitimacy Audit: N/A) — supply-chain neutral. |
</threat_model>

<verification>
- `npm run build` succeeds with no circular-init error; `node scripts/assert-chunks.mjs` exits zero (react-vendor + separate lazy /rh + /admin chunks + jsPDF out of index + eager index < 2,788,270 bytes).
- `grep -c "RoleGuard" src/router/routes.tsx` unchanged vs pre-edit; every RH/admin route element keeps its RoleGuard wrapping (access-control invariant).
- Candidate/auth/public/avaliação routes remain static imports.
- `npm run lint`: tsc ≤ 258.
- E2E (gated): J2 + J3 assert destination heading renders behind Suspense.
</verification>

<success_criteria>
- PERF-03 met: candidate eager chunk shrank below the 2,788 kB baseline; react-vendor + lazy /rh/* + /admin/* chunks emit separately; jsPDF + recharts excluded from the eager candidate path.
- No navigation regression; access control unchanged (RoleGuard outside every lazy element).
- assert-chunks green; tsc ≤ 258.
</success_criteria>

<output>
Create `.planning/phases/19-performance-bundle-cache/19-02-SUMMARY.md` when done.
</output>
