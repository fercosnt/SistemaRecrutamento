---
plan: 21-01
phase: 21-production-readiness-uats-live
title: Live PROD UAT execution — close M2/M3 deferred HUMAN-UATs
requirements: [PROD-01, PROD-02]
must_haves:
  - "PROD-01: P11 live UAT (Work-Sample/SJT open-case + redação scoring round-trip) executed in PROD and PASS"
  - "PROD-02: P16 deferred HUMAN-UATs (cold-start login RH, Tier-B axe R5/C5, keyboard roving-focus, BigFive aria-live) closed in PROD or re-deferred with recorded justification"
  - "Each UAT result (PASS / re-deferred) recorded with evidence (accounts/data used, findings) in the phase UAT artifact"
  - "M3 deferred UATs (P18-01/02/03, P19-01/02, P20-01/02) executed live and recorded"
  - "A human runbook is written so Fernando can independently re-validate every item"
truths:
  - "Phase 21 closes deferred live UATs; it builds no new feature (validation phase). Traceability: PROD-01/PROD-02."
  - "Invariant preserved: AI recommends, human decides (RNF-07a); no new candidaturas write by score; privileged write-paths authenticate-THEN-authorize."
objective: Execute the deferred M2/M3 live UATs against PROD Supabase, fix any defect found, record evidence, and produce a human re-validation runbook.
---

# Plan 21-01 — Live PROD UAT execution

Validation-only phase. Claude drives via Playwright + Supabase MCP + Chrome; fixes defects atomically; then writes the runbook. Env = local app (3003) → PROD Supabase.

## Batch A — Backend / deterministic (Supabase MCP + Playwright)

- **A1 · P16 #2 axe Tier-B (R5/C5):** `E2E_REAL_LOGIN=1 npx playwright test e2e/a11y.spec.ts --project=chromium`. PASS = 0 serious/critical on RedacaoReviewPanel + BigFiveQuestionnaireScreen.
- **A2 · P18-01 RESIL-01/02 (timing+logs):** Invoke an AI EF live (`gerar-devolutiva-bigfive`, `avaliar-redacao`) for a seeded candidatura; via `get_logs`/edge timing confirm per-call ~25s timeout (no 38–102s hang), retry/backoff present, clean `503 {error_code:'AI_UNAVAILABLE'}` on exhaustion (no 9× amplification), bigfive devolutiva completes (5 dims parallel). Literal 429/529 re-deferred.
- **A3 · P18-03 FIX-01 (consolidar):** Run `consolidar-decisao-final` (v4) for a candidatura in `work_sample_sjt='na'` + caso_aberto `pendente_humano`. Confirm consolidated score does not zero/trap, MC preserved, no NaN, recommendation copy not double-listing a pending entrevista.
- **A4 · server-side guarantees (re-confirm):** RLS for answer-key hidden, back-lock 42501, candidato→0 scores (pg_policies) — already green P11; re-confirm via `execute_sql`.

## Batch B — Visual / interactive (Chrome browser automation)

- **B1 · P19-01 lazy-route no-regression:** Navigate several `/rh/*` + `/admin/*` (and back). PASS = PageSkeleton flash on first visit, no flash on revisit, no blank screen, no console "Cannot access X before initialization"; candidate flows eager.
- **B2 · P19-02 cross-client ≤60s:** Two sessions (candidate tab + RH tab). RH changes status / saves scorecard / saves redação review → candidate dashboard reflects ≤60s on focus; RH consolidação updates immediately after save.
- **B3 · P18-02 AsyncState UX:** On the 5 AI screens, exercise slow/failing call → loading → slow note → legible error → visible "Tentar novamente"; AI_UNAVAILABLE = sobrecarga copy, else generic; no blank; DB-read screens neutral "Carregando…".
- **B4 · P20-01 RH edits guide:** As RH/admin, edit pergunta text+dimensão, add manual, delete, reorder, "Salvar edições", reload persists, Manual badge shows. Denial path (candidato/sem-posse → permission copy, no raw 42501/table leak).
- **B5 · P20-02 regen preserves manual:** After saving a manual question, "Gerar guia" (regen) → manual question survives (text + Manual badge) alongside fresh IA; failed/garbled regen does not clobber manual.
- **B6 · P16 #3 keyboard roving-focus:** Keyboard-only: Tabs (DecisaoFinal/EntrevistaWorkspace) arrow-move + Enter/Space; RadioGroup + SJT option groups roving; focus ring visible on glass (screenshot evidence).
- **B7 · P16 #4 aria-live (proxy):** Verify the autosave/save-state aria-live region exists + updates in DOM. Literal screen-reader announce re-deferred.
- **B8 · P11 #4 autosave 30s + locked-state:** Observe 30s debounced autosave affordance; after RH advances etapa, edit attempt shows neutral locked state (server 42501 already verified).

## Batch C — Reconcile from evidence (no re-run)
- **C1 · P11 #1/#2/#3:** PASS from 2026-06-26 live session (candidate flow fix F4 686c460; SJT/redação IA real; RH scorecard). Cite evidence in artifact.
- **C2 · P16 #1:** PASS from 2026-06-26 (cold-start login RH, recruiter@ + recrutador.rh@; LoginRHPage fix 464ead8). Cite evidence.

## Deliverables
1. Per-item evidence in `21-HUMAN-UAT.md` (PASS / re-deferred + justification).
2. Defect fixes committed atomically (if any).
3. Upstream VERIFICATION.md (18/19/20) flipped where green.
4. **Human runbook** (`21-RUNBOOK.md`) for Fernando to re-validate every item.
5. Clean up PROD test artifacts created during the session.

## Verification (must be TRUE)
- PROD-01 PASS recorded with evidence.
- PROD-02 closed or re-deferred-with-justification recorded with evidence.
- All M3 deferred UATs executed live + recorded.
- Runbook exists and is self-contained (accounts, URLs, steps, expected results).
- Any defect found is fixed + committed, or logged as a follow-up if out of scope.
