---
phase: 37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s
plan: 05
type: execute
wave: 4
depends_on: ["37-04"]
files_modified:
  - database.types.ts
  - .planning/todos/done/37-drift-prod-tabelas-notificacao.md
autonomous: true
requirements: [LEDGER-01, TIMELINE-01]

must_haves:
  truths:
    - "`database.types.ts` conhece `notificacoes_enviadas` (com `destinatario_original` e `modo`) e `config_sla_etapa` — a P40 consegue tipar a leitura do SLA sem `any`"
    - "O enum `status_notificacao` está tipado, com os 6 labels"
    - "O item de drift saiu de `pending/` e está em `done/` com um registro do que foi feito e do que ficou de fora"
    - "A regeneração não introduziu drift não relacionado: o diff de `database.types.ts` é explicável linha a linha"
  artifacts:
    - path: "database.types.ts"
      provides: "Tipos gerados incluindo as duas tabelas da fase e o enum status_notificacao"
      contains: "notificacoes_enviadas"
    - path: ".planning/todos/done/37-drift-prod-tabelas-notificacao.md"
      provides: "Item de drift resolvido, com seção de resolução citando os 4 arquivos de migration e os smokes"
      contains: "Resolução"
  key_links:
    - from: "database.types.ts"
      to: "DashboardCandidatoPage (P40 / TIMELINE-02)"
      via: "tipo gerado de config_sla_etapa consumido pela leitura do painel do candidato"
      pattern: "config_sla_etapa"
    - from: "database.types.ts"
      to: "EF notificar-candidato (P38)"
      via: "tipo gerado de notificacoes_enviadas com destinatario_original e modo"
      pattern: "destinatario_original"
---

<objective>
Fechar a fase: propagar o schema novo para os tipos que o código consome e arquivar o item de drift com o registro do que foi efetivamente resolvido.

Purpose: diferente da P36 (onde nenhum código cliente tocava a RPC e regenerar só traria drift), aqui há tabelas e colunas que o código VAI consumir — a P40 lê `config_sla_etapa` do painel do candidato e a P38 grava `destinatario_original`/`modo`. Sem a regeneração, a P40 começa com `any` e perde a única rede de proteção de tipos do projeto. E o item de drift precisa sair de `pending/` com um registro honesto: o que foi reconciliado, o que continua sem resposta (a origem do apply original é desconhecida) e o que foi deliberadamente deferido.
Output: `database.types.ts` regenerado + item de drift arquivado em `done/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/todos/pending/37-drift-prod-tabelas-notificacao.md
@.planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-CONTEXT.md

<interfaces>
<!-- Fatos operacionais já verificados. Não re-derivar. -->

Regeneração de tipos (package.json, script `db:types`):
  npx supabase gen types typescript --linked > database.types.ts
  → escreve na RAIZ do repo (`database.types.ts`, arquivo git-trackeado)
  → exige CLI autenticada e projeto linkado; se falhar, o redirecionamento `>` já
    TRUNCOU o arquivo. Fazer backup antes de rodar e restaurar em caso de falha.

Baseline de lint (débito de infra registrado no STATE):
  `npm run lint` é permanentemente não-zero contra um baseline PRÉ-EXISTENTE de 97 erros
  `tsc` em `src/**` (teto do CI = 104). O gate desta fase NÃO é "lint verde": é
  (1) contagem 97 → 97 e (2) nenhum erro citando arquivos desta fase.
  `.husky/pre-commit` roda `npm run lint` ⇒ commits usam `git commit --no-verify`
  com os dois números no corpo da mensagem.

Diretórios de todo do repo: `.planning/todos/pending/` · `.planning/todos/done/`.

O que esta fase NÃO resolveu (registrar como tal, não como resolvido):
  · a ORIGEM do apply original das duas migrations continua desconhecida
  · retenção/purga de `notificacoes_enviadas` — deferida a um milestone de LGPD-OPS (M8+)
  · coluna `reclamado_em` — deferida à P41
  · candidato-DENY explícito — deliberadamente NÃO adicionado (default-deny implícito)
  · divergência `updated_at` (inglês) vs `atualizado_em` (pt-BR) no resto do schema
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerar database.types.ts e conferir o diff</name>
  <files>database.types.ts</files>
  <read_first>
    - package.json (linha do script `db:types`) — o comando exato e o destino do redirecionamento
    - database.types.ts (as ~40 primeiras linhas e o índice de tabelas) — para reconhecer a forma do arquivo antes de sobrescrevê-lo
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-CONTEXT.md § "Escopo e Prova" — por que regenerar aqui e não na P36
  </read_first>
  <action>
    (1) Antes de qualquer coisa, copiar `database.types.ts` para um backup temporário fora do controle de versão. O script usa redirecionamento `>`, que trunca o arquivo ANTES de o comando rodar — uma CLI não autenticada deixa o arquivo vazio e o repo quebrado.

    (2) Executar `npm run db:types`.

    (3) Se o comando falhar (CLI não autenticada, projeto não linkado, erro de rede): restaurar o backup imediatamente, registrar o erro LITERAL no SUMMARY e ESCALAR como pendência para o orquestrador (que tem o caminho MCP) em vez de tentar editar `database.types.ts` à mão. Editar tipos gerados manualmente é proibido pelo CLAUDE.md. Neste caso a Task 2 ainda deve ser executada, e o SUMMARY deve declarar a regeneração como pendência aberta bloqueando a P40.

    (4) Se o comando tiver sucesso, conferir o diff com `git diff --stat database.types.ts` e `git diff database.types.ts`, e classificar CADA hunk em uma de duas categorias no SUMMARY: (i) esperado — tudo relativo a `notificacoes_enviadas`, `config_sla_etapa`, o enum `status_notificacao`, e as relações de FK para `candidaturas`/`candidatos`; (ii) inesperado — qualquer outra tabela, coluna, função ou enum que apareça, desapareça ou mude de forma. Hunks inesperados são evidência de OUTRO drift PROD→repo, potencialmente de outra fase, e devem ser registrados como novo item em `.planning/todos/pending/` em vez de aceitos em silêncio. Não reverter nada do arquivo gerado — o gerado é a verdade; o que se registra é a surpresa.

    (5) Confirmar por grep que o arquivo regenerado conhece: `notificacoes_enviadas`, `config_sla_etapa`, `destinatario_original`, `modo`, `dedupe_key`, `proxima_tentativa_em`, `status_notificacao`, `rotulo_candidato`.

    (6) Rodar `npm run lint` e registrar a contagem de erros `tsc`. Exigir 97 (o baseline) e exigir que nenhum erro cite `database.types.ts` nem qualquer arquivo desta fase. Se a contagem subir, investigar: tipo gerado novo que quebra um consumidor existente é um sinal real, não ruído. Rodar `npm run build` e `npm run test:run`.

    (7) Commit com `git commit --no-verify` (o hook `.husky/pre-commit` roda `npm run lint` e é permanentemente vermelho contra o baseline), com os números `97 → 97` no corpo da mensagem.

    NÃO editar `database.types.ts` manualmente em nenhuma hipótese.
  </action>
  <verify>
    <automated>test -s database.types.ts && grep -q "notificacoes_enviadas" database.types.ts && grep -q "config_sla_etapa" database.types.ts && grep -q "destinatario_original" database.types.ts && grep -q "dedupe_key" database.types.ts && grep -q "proxima_tentativa_em" database.types.ts && grep -q "status_notificacao" database.types.ts && grep -q "rotulo_candidato" database.types.ts && npm run build && npm run test:run && echo "types regenerados e gates de repo verdes"</automated>
  </verify>
  <acceptance_criteria>
    - `database.types.ts` é não-vazio e contém `notificacoes_enviadas`, `config_sla_etapa`, `destinatario_original`, `modo`, `dedupe_key`, `proxima_tentativa_em`, `rotulo_candidato` e o enum `status_notificacao`.
    - O enum `status_notificacao` no arquivo gerado lista os 6 labels (`pendente`, `enviado`, `entregue`, `falhou`, `bounce`, `reclamado`).
    - O SUMMARY contém a saída de `git diff --stat database.types.ts` e classifica cada hunk como esperado ou inesperado.
    - Se houver hunk inesperado, existe um novo arquivo em `.planning/todos/pending/` descrevendo o drift adicional encontrado.
    - `npm run lint` reporta 97 erros `tsc` (mesma contagem do baseline; teto CI 104) e nenhum deles cita `database.types.ts` nem arquivos desta fase — os dois números estão no SUMMARY.
    - `npm run build` e `npm run test:run` verdes.
    - `database.types.ts` NÃO foi editado manualmente (o diff é integralmente atribuível à saída do gerador).
    - Se o comando falhou: o backup foi restaurado, `database.types.ts` não está truncado, o erro literal está no SUMMARY e a regeneração está declarada como pendência aberta bloqueando a P40.
  </acceptance_criteria>
  <done>Os tipos do projeto conhecem a camada de dados de notificação, o diff é explicável hunk a hunk, e qualquer drift adicional descoberto pela regeneração foi registrado em vez de absorvido.</done>
</task>

<task type="auto">
  <name>Task 2: Arquivar o item de drift com o registro honesto do que ficou resolvido</name>
  <files>.planning/todos/done/37-drift-prod-tabelas-notificacao.md</files>
  <read_first>
    - .planning/todos/pending/37-drift-prod-tabelas-notificacao.md — o item a arquivar, com as perguntas que ele levantou
    - .planning/todos/done/funil-02-comparativo-reject-justificativa.md — o formato de item arquivado deste repo
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-CONTEXT.md § "Deferred Ideas" — o que foi deliberadamente NÃO resolvido
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-01-SUMMARY.md, 37-02-SUMMARY.md, 37-03-SUMMARY.md e 37-04-SUMMARY.md — os fatos a citar na seção de resolução
  </read_first>
  <action>
    Mover `.planning/todos/pending/37-drift-prod-tabelas-notificacao.md` para `.planning/todos/done/37-drift-prod-tabelas-notificacao.md` usando `git mv` (preserva o histórico), preservando o corpo original INTACTO — ele é o registro de como o drift foi descoberto e do estado do banco em 2026-07-22, e tem valor forense.

    Acrescentar ao FINAL do arquivo uma seção `## Resolução (Phase 37, 2026-07-22)` com quatro blocos:

    **Resolvido.** Os quatro arquivos de migration agora existentes (`20260721000001_notificacoes_enviadas.sql` e `20260721000002_config_sla_etapa.sql` reconstruídos e NÃO re-aplicados; `20260722000001_p36_vault_resend_reader.sql` da P36; `20260722000002_p37_notificacoes_lacunas.sql` aplicada nesta fase) com as quatro linhas correspondentes no ledger e `supabase db push --linked` limpo. As 3 lacunas fechadas, nomeando as colunas, a função, os dois triggers e o índice. A fidelidade provada pelo smoke `supabase/tests/p37_fidelidade_schema_smoke.sql` nos dois lados do apply, e as garantias provadas por `supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql` (citar os PASS de candidato-DENY, RH vaga-scoped e 23505). Citar a regeneração de `database.types.ts` (ou, se a Task 1 escalou, registrar como pendência aberta).

    **Corrigido em relação ao retrato original.** Enumerar cada divergência entre este documento (escrito por paráfrase) e o catálogo real, conforme a seção de divergências do `37-SCHEMA-VIVO.md` — em especial o literal de role da policy. Este bloco é o que impede que uma leitura futura deste arquivo reintroduza os erros da paráfrase.

    **Deliberadamente NÃO feito.** O candidato-DENY continua IMPLÍCITO (default-deny da RLS), com o racional travado: uma policy permissiva mal escrita ABRE acesso, a ausência de policy nunca abre. Reavaliar apenas se surgir uma policy de INSERT/UPDATE na tabela. `config_sla_etapa` NÃO foi re-seedada (estava 8/8 e coerente). Nenhum arquivo já presente no ledger foi editado.

    **Continua em aberto.** A ORIGEM do apply original das duas migrations permanece desconhecida — o drift foi reconciliado, mas a causa não foi. Retenção/purga de `notificacoes_enviadas` deferida a LGPD-OPS (M8+). Coluna `reclamado_em` deferida à P41. Divergência `updated_at` (inglês) vs `atualizado_em` (pt-BR) no resto do schema, não endereçada.

    Atualizar o frontmatter do arquivo (se houver) ou acrescentar um, com `status: done` e `resolved_at: 2026-07-22`, e `resolves_phase: 37`.

    NÃO reescrever nem "corrigir" o corpo original do documento — as imprecisões dele são parte do registro; a correção vive na seção nova.

    Commit com `git commit --no-verify` e os números de lint (97 → 97) no corpo.
  </action>
  <verify>
    <automated>test -f .planning/todos/done/37-drift-prod-tabelas-notificacao.md && test ! -f .planning/todos/pending/37-drift-prod-tabelas-notificacao.md && grep -q "## Resolução" .planning/todos/done/37-drift-prod-tabelas-notificacao.md && grep -q "20260722000002" .planning/todos/done/37-drift-prod-tabelas-notificacao.md && grep -q "p37_fidelidade_schema_smoke" .planning/todos/done/37-drift-prod-tabelas-notificacao.md && grep -q "p37_lacunas_rls_idempotencia_smokes" .planning/todos/done/37-drift-prod-tabelas-notificacao.md && grep -qi "continua em aberto\|Continua em aberto" .planning/todos/done/37-drift-prod-tabelas-notificacao.md && grep -qi "origem" .planning/todos/done/37-drift-prod-tabelas-notificacao.md && echo "item de drift arquivado com resolucao honesta"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/todos/pending/37-drift-prod-tabelas-notificacao.md` não existe mais e `.planning/todos/done/37-drift-prod-tabelas-notificacao.md` existe.
    - O movimento foi feito com `git mv` — `git log --follow .planning/todos/done/37-drift-prod-tabelas-notificacao.md` mostra o commit de criação original.
    - O corpo original do documento está preservado byte-a-byte acima da seção nova (`git diff` do move mostra apenas adição no final e, se aplicável, o frontmatter).
    - Existe a seção `## Resolução (Phase 37, 2026-07-22)` com os quatro blocos: "Resolvido", "Corrigido em relação ao retrato original", "Deliberadamente NÃO feito" e "Continua em aberto".
    - O bloco "Resolvido" nomeia os 4 arquivos de migration, os 2 arquivos de smoke, as 2 colunas novas, a função `tocar_atualizado_em`, os 2 triggers e o índice `idx_notif_retry`.
    - O bloco "Corrigido" enumera as divergências entre a paráfrase original e o catálogo real, incluindo o literal de role da policy.
    - O bloco "Deliberadamente NÃO feito" declara o candidato-DENY implícito com o racional, e o não-re-seed de `config_sla_etapa`.
    - O bloco "Continua em aberto" declara explicitamente que a ORIGEM do apply original permanece desconhecida.
    - O frontmatter contém `status: done`, `resolved_at: 2026-07-22` e `resolves_phase: 37`.
  </acceptance_criteria>
  <done>O item de drift está arquivado com um registro que distingue o que foi resolvido, o que foi corrigido na narrativa, o que foi deliberadamente deixado de fora e o que continua sem resposta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gerador de tipos (CLI → PROD) → `database.types.ts` | Um arquivo git-trackeado escrito por redirecionamento `>`; falha do comando corrompe o repo |
| Registro de débito → decisões futuras | Um item arquivado como "resolvido" quando não foi apaga a memória do problema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05-01 | Denial of Service | `npm run db:types` com CLI não autenticada trunca `database.types.ts` para 0 bytes e quebra o build de todo o projeto | mitigate | Backup obrigatório ANTES do comando + restauração em caso de falha + `test -s` no gate automatizado |
| T-37-05-02 | Tampering | Editar `database.types.ts` à mão para "consertar" um tipo faltante cria uma mentira que sobrevive à próxima regeneração | mitigate | Proibição explícita (CLAUDE.md) + acceptance criteria exige que o diff seja integralmente atribuível ao gerador; falha ⇒ escalar, não editar |
| T-37-05-03 | Information Disclosure | O arquivo gerado expõe a forma de TODAS as tabelas, inclusive RLS-sensíveis — mas é um artefato de tipos, não de dados | accept | Já é o padrão do repo desde o M1; contém apenas nomes e tipos de coluna, nenhum dado nem credencial |
| T-37-05-04 | Repudiation | Arquivar o item de drift como plenamente resolvido apagaria o fato de que a ORIGEM do apply original nunca foi descoberta — a mesma falha pode repetir | mitigate | Bloco "Continua em aberto" obrigatório, com a origem desconhecida nomeada; verificado por grep no gate |
| T-37-05-05 | Tampering | Hunk inesperado no diff dos tipos (outra tabela mudando de forma) absorvido em silêncio esconderia um SEGUNDO drift PROD→repo | mitigate | Classificação obrigatória de cada hunk + criação de novo item em `.planning/todos/pending/` para qualquer surpresa |
</threat_model>

<verification>
- `database.types.ts` não-vazio, contendo as duas tabelas, as colunas novas e o enum `status_notificacao`
- Cada hunk do diff classificado como esperado ou inesperado; surpresas registradas como novo todo
- `npm run lint` em 97 erros (baseline; teto CI 104), nenhum citando arquivos desta fase
- `npm run build` e `npm run test:run` verdes
- Item de drift em `.planning/todos/done/` com as quatro seções de resolução e histórico preservado por `git mv`
- Commits feitos com `--no-verify` e os números de lint no corpo
</verification>

<success_criteria>
1. A P40 pode ler `config_sla_etapa` com tipos gerados, e a P38 pode gravar `destinatario_original`/`modo` com tipos gerados — nenhuma das duas começa com `any`.
2. O item de drift está arquivado de forma honesta: distingue resolvido, corrigido, deliberadamente omitido e ainda em aberto.
3. Se a regeneração revelou outro drift PROD→repo, ele está registrado como item novo em vez de absorvido em silêncio.
</success_criteria>

<output>
Create `.planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-05-SUMMARY.md` when done
</output>
