---
phase: 37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s
plan: 04
type: execute
wave: 3
depends_on: ["37-02", "37-03"]
files_modified:
  - supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql (aplicada via MCP apply_migration — arquivo não é editado)
  - supabase_migrations.schema_migrations (reconcile via MCP execute_sql)
autonomous: false
requirements: [LEDGER-01, LEDGER-02, LEDGER-03, TIMELINE-01]

must_haves:
  truths:
    - "A fidelidade dos dois arquivos reconstruídos está provada contra o banco vivo ANTES de qualquer alteração — o baseline foi verificado, não presumido"
    - "A migration aditiva 20260722000002 está viva em PROD e o ledger tem a version com o prefixo do nome do arquivo (sem drift novo)"
    - "O smoke de fidelidade volta a passar DEPOIS do apply, no modo pós-aditiva — o schema mudou exatamente o previsto e nada mais"
    - "Um candidato JWT-impersonado real obtém ZERO linhas de notificacoes_enviadas com uma linha existindo na tabela"
    - "Um recrutador não-dono obtém ZERO linhas e o dono da vaga obtém >= 1 — a policy nega por escopo, não por estar quebrada"
    - "Uma dedupe_key repetida levanta 23505 empiricamente"
    - "notificacoes_enviadas termina com 0 linhas, como começou"
  artifacts:
    - path: ".planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-04-SUMMARY.md"
      provides: "Transcrição das saídas dos 3 runs de smoke + confirmação do apply e do reconcile do ledger"
      contains: "PASS"
  key_links:
    - from: "supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql"
      to: "supabase_migrations.schema_migrations"
      via: "MCP apply_migration + reconcile da version para 20260722000002"
      pattern: "20260722000002"
    - from: "supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql"
      to: "PROD (policy rh_le_notificacoes + uq_notif_dedupe)"
      via: "execução via MCP execute_sql; gate = contagem de PASS"
      pattern: "PASS"
---

<objective>
Executar em PROD, na ordem correta, os três atos de banco desta fase: provar o baseline, aplicar a aditiva, provar as garantias.

Purpose: a ordem é carga estrutural, não conveniência. O smoke de fidelidade em modo baseline SÓ é válido antes do apply — depois, o schema legitimamente tem 2 colunas, 1 constraint, 1 índice e 2 triggers a mais. Rodar o baseline primeiro é o que transforma "os arquivos parecem certos" em "os arquivos SÃO o banco". Aplicar antes de provar o baseline destruiria para sempre a evidência de que a reconstrução foi fiel.
Output: 1 migration viva em PROD, ledger reconciliado, 3 execuções de smoke transcritas no SUMMARY.

⚠ **Plano de checkpoint puro, `autonomous: false`.** Subagentes GSD não recebem os tools MCP do Supabase (bug upstream anthropics/claude-code#13898; comprovado na P36/Plano 36-04, que bateu nesse muro no meio da execução). O executor NÃO tenta rodar SQL nem `supabase db push`. Todas as três tarefas são do orquestrador/main thread, com o SQL exato pronto para colar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-CONTEXT.md
@.planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-SCHEMA-VIVO.md
@supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql
@supabase/tests/p37_fidelidade_schema_smoke.sql
@supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql

<interfaces>
<!-- Procedimento já estabelecido no repo. Não re-derivar. -->

Projeto Supabase (PROD): isljnozzlvckrgjjbjwp

Apply (CLAUDE.md § Migrations + precedente P32-04 / P36-04):
  MCP `apply_migration` com o conteúdo do arquivo — NUNCA `supabase db push --linked`
  (SQLSTATE 42601: corpo `$$` adjacente a outros statements no transaction pooler).
  `apply_migration` grava no ledger uma `version` de TIMESTAMP que não bate com o prefixo
  do nome do arquivo ⇒ RECONCILIAR `supabase_migrations.schema_migrations` para
  `20260722000002`, e então confirmar com `supabase db push --linked` reportando
  "Remote database is up to date".

Toggle do smoke de fidelidade: a variável `v_pos_aditiva` no topo de
supabase/tests/p37_fidelidade_schema_smoke.sql.
  false ⇒ assere o baseline (16 colunas / 5 constraints / 5 índices / 0 triggers)
  true  ⇒ assere o pós-aditiva (18 / 6 / 6 / 1 por tabela)

Ordem OBRIGATÓRIA: Task 1 (baseline) → Task 2 (apply + pós) → Task 3 (comportamental).
Inverter a ordem destrói a evidência do baseline de forma irrecuperável.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Provar a fidelidade da reconstrução contra o banco vivo (ANTES de qualquer apply)</name>
  <files>supabase/tests/p37_fidelidade_schema_smoke.sql (executado, não editado)</files>
  <read_first>
    - supabase/tests/p37_fidelidade_schema_smoke.sql — o arquivo a executar; conferir que `v_pos_aditiva` está em `false`
    - supabase/migrations/20260721000001_notificacoes_enviadas.sql e supabase/migrations/20260721000002_config_sla_etapa.sql — os arquivos cuja fidelidade está sendo julgada
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-CONTEXT.md § "Fidelidade provada por smoke de diff"
  </read_first>
  <what-built>
    Os planos 37-02 e 37-03 escreveram, sem tocar em PROD: os dois arquivos de migration reconstruídos, o smoke de fidelidade e a migration aditiva `20260722000002`. Nada foi aplicado ainda. Este checkpoint é o gate que decide se a reconstrução é confiável o bastante para a fase prosseguir.
  </what-built>
  <action>
    O orquestrador executa `supabase/tests/p37_fidelidade_schema_smoke.sql` em PROD via MCP `execute_sql`, com `v_pos_aditiva` em `false`, e transcreve TODOS os `NOTICE` emitidos no SUMMARY.

    GATE DE PARADA ABSOLUTO: se qualquer asserção levantar exceção, NÃO prosseguir para a Task 2. Nenhum apply acontece enquanto a reconstrução não for provada fiel. A divergência deve ser corrigida no arquivo reconstruído correspondente (voltando ao plano 37-02) e este gate re-executado do zero. É explicitamente PROIBIDO "ajustar o smoke para passar" — o smoke é a definição de fidelidade; enfraquecê-lo reintroduz o drift que a fase existe para eliminar.

    Registrar no SUMMARY: a contagem de PASS obtida vs. esperada, e a linha de RESUMO emitida pelo smoke.
  </action>
  <how-to-verify>
    1. Confirmar que `v_pos_aditiva` está `false` no arquivo.
    2. Rodar o conteúdo de `supabase/tests/p37_fidelidade_schema_smoke.sql` via MCP `execute_sql` no projeto `isljnozzlvckrgjjbjwp`.
    3. Conferir: nenhuma exceção E o número de `PASS` emitidos é igual ao número declarado no cabeçalho do smoke (um run sem exceção mas com asserções puladas NÃO é verde).
    4. Colar a saída literal dos NOTICE no `37-04-SUMMARY.md`.
    5. Se houver divergência: parar, listar cada campo divergente (esperado vs. encontrado) e devolver ao plano 37-02 para correção do ARQUIVO, nunca do smoke.
  </how-to-verify>
  <verify>
    <automated>grep -q "v_pos_aditiva" supabase/tests/p37_fidelidade_schema_smoke.sql && grep -qi "v_pos_aditiva[^;]*:=[^;]*false" supabase/tests/p37_fidelidade_schema_smoke.sql && echo "smoke de fidelidade pronto em modo baseline (execucao e do orquestrador via MCP)"</automated>
  </verify>
  <acceptance_criteria>
    - O smoke rodou via MCP `execute_sql` com `v_pos_aditiva = false` e NÃO levantou nenhuma exceção.
    - O número de `PASS` emitidos é igual ao número declarado no cabeçalho do smoke; a linha `RESUMO` está transcrita no SUMMARY.
    - A saída literal dos NOTICE está no `37-04-SUMMARY.md`.
    - Nenhuma migration foi aplicada nesta tarefa — `supabase_migrations.schema_migrations` ainda NÃO contém `20260722000002`.
    - Se houve divergência, o SUMMARY lista cada campo (esperado vs. encontrado) e a fase está parada aguardando correção em 37-02 — o smoke NÃO foi afrouxado.
  </acceptance_criteria>
  <resume-signal>Responder "aprovado" quando o smoke de fidelidade passar em modo baseline com a contagem completa de PASS, ou colar a lista de divergências.</resume-signal>
  <done>Está provado por execução que os dois arquivos reconstruídos descrevem exatamente o schema vivo — o repositório voltou a ser fonte da verdade, e isso é verificável, não afirmado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Aplicar a migration aditiva, reconciliar o ledger e re-provar a fidelidade em modo pós-aditiva</name>
  <files>supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql (aplicada), supabase_migrations.schema_migrations (reconciliada)</files>
  <read_first>
    - supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql — o SQL exato a aplicar
    - CLAUDE.md § "Migrations + db push — workaround conhecido (PL/pgSQL)" — por que MCP `apply_migration` e nunca `db push --linked`
    - .planning/phases/36-deliverability-sender-identity/36-04-PLAN.md (Task 2) — o precedente literal do apply + reconcile do ledger desta base de código
    - supabase/tests/p37_fidelidade_schema_smoke.sql — o toggle `v_pos_aditiva` e as contagens do modo pós-aditiva
  </read_first>
  <what-built>
    A Task 1 provou que o baseline do repo bate com PROD. A migration aditiva `20260722000002` está escrita e revisada, fechando as 3 lacunas (colunas de auditoria do modo teste, trigger de `atualizado_em`, índice parcial de retry). Nada dela está vivo ainda.
  </what-built>
  <action>
    (1) Aplicar o conteúdo de `supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql` em PROD via MCP `apply_migration`. Chamada privilegiada — solicitar autorização do operador no momento (auth gate). NÃO usar `supabase db push --linked`.

    (2) Reconciliar o ledger: `apply_migration` grava uma `version` de timestamp que não bate com o prefixo do arquivo. Via MCP `execute_sql`, alinhar a linha para `20260722000002`, conforme o bloco `<sql-para-colar>`. Depois confirmar com `npx supabase db push --linked`, que deve reportar que o remoto está atualizado.

    (3) Re-executar `supabase/tests/p37_fidelidade_schema_smoke.sql` com `v_pos_aditiva` em `true` e exigir verde. Esta segunda execução é o que prova que o apply mudou EXATAMENTE o previsto — 2 colunas, 1 constraint, 1 índice, 1 trigger por tabela — e nada mais. Um apply que também tivesse mexido em outra coisa falharia aqui.

    (4) Restaurar `v_pos_aditiva` para `false` no arquivo? NÃO — deixar em `true`, que passa a ser o estado corrente do banco, e anotar no SUMMARY que o modo `false` só é útil para auditar o baseline histórico. Se o executor tiver alterado o arquivo para rodar, o commit deve refletir `true`.

    (5) Transcrever no SUMMARY: a confirmação do `apply_migration`, o resultado do reconcile, a saída do `db push --linked` e a saída completa dos NOTICE do smoke pós-aditiva.
  </action>
  <sql-para-colar>
```sql
-- (1) Reconcile do ledger — alinhar a version gravada pelo apply_migration
--     ao prefixo do nome do arquivo. Rodar LOGO APÓS o apply.
--     Antes: inspecionar o que entrou.
select version, name
  from supabase_migrations.schema_migrations
 order by version desc
 limit 5;

-- Substituir <VERSION_GRAVADA> pela version de timestamp que o apply_migration criou.
update supabase_migrations.schema_migrations
   set version = '20260722000002',
       name    = '20260722000002_p37_notificacoes_lacunas'
 where version = '<VERSION_GRAVADA>';

-- (2) Confirmar o ledger final da fase
select version, name
  from supabase_migrations.schema_migrations
 where version in ('20260721000001','20260721000002','20260722000001','20260722000002')
 order by version;

-- (3) Prova estrutural rápida do que a aditiva criou (o smoke pós-aditiva é o gate real)
select column_name, data_type, is_nullable, column_default
  from information_schema.columns
 where table_schema='public' and table_name='notificacoes_enviadas'
   and column_name in ('destinatario_original','modo');

select conname, pg_get_constraintdef(oid)
  from pg_constraint
 where conrelid = 'public.notificacoes_enviadas'::regclass
   and conname = 'ck_notif_modo';

select tgname, tgrelid::regclass::text as tabela
  from pg_trigger
 where not tgisinternal
   and tgrelid in ('public.notificacoes_enviadas'::regclass,
                   'public.config_sla_etapa'::regclass)
 order by tabela, tgname;

select indexname, indexdef
  from pg_indexes
 where schemaname='public' and tablename='notificacoes_enviadas'
   and indexname = 'idx_notif_retry';

select p.proname, p.prosecdef, p.proconfig
  from pg_proc p join pg_namespace n on n.oid = p.pronamespace
 where n.nspname='public' and p.proname='tocar_atualizado_em';
```
  </sql-para-colar>
  <how-to-verify>
    1. `apply_migration` retornou sucesso (transcrever a confirmação).
    2. Rodar o bloco SQL acima; a consulta (2) deve listar as QUATRO versions: `20260721000001`, `20260721000002`, `20260722000001`, `20260722000002`.
    3. `npx supabase db push --linked` reporta que o remoto está atualizado.
    4. A consulta de colunas devolve `destinatario_original` (`text`, `NO`, sem default) e `modo` (`text`, `NO`, default `'teste'::text`).
    5. A consulta de trigger devolve exatamente 2 linhas: `trg_notificacoes_atualizado_em` e `trg_config_sla_atualizado_em`.
    6. A consulta de índice devolve `idx_notif_retry` com o predicado `WHERE` contendo `pendente` e `falhou`.
    7. `pg_proc` devolve `tocar_atualizado_em` com `prosecdef = false` e `proconfig` contendo `search_path=`.
    8. Re-rodar o smoke de fidelidade com `v_pos_aditiva = true`: sem exceção e com a contagem completa de PASS.
  </how-to-verify>
  <verify>
    <automated>test -f supabase/migrations/20260722000002_p37_notificacoes_lacunas.sql && npm run lint; echo "--- lint acima: exigir 97 erros tsc (baseline), teto CI 104, nenhum erro citando arquivos da fase ---"; npm run build && npm run test:run && echo "gates de repo verdes apos a migration viva"</automated>
  </verify>
  <acceptance_criteria>
    - A migration `20260722000002_p37_notificacoes_lacunas.sql` está aplicada em PROD via MCP `apply_migration`, com a confirmação transcrita no SUMMARY.
    - `supabase_migrations.schema_migrations` contém as quatro versions `20260721000001`, `20260721000002`, `20260722000001`, `20260722000002`, cada uma com `name` batendo o prefixo do arquivo correspondente.
    - `npx supabase db push --linked` reporta que o remoto está atualizado — nenhum drift novo criado por esta fase.
    - `information_schema.columns` mostra `destinatario_original` com `is_nullable = NO` e sem default, e `modo` com `is_nullable = NO` e default `'teste'::text`.
    - `pg_constraint` mostra `ck_notif_modo` com definição `CHECK ((modo = ANY (ARRAY['producao'::text, 'teste'::text])))` ou equivalente canônico do Postgres.
    - `pg_trigger` (não-internos) devolve exatamente 2 linhas: `trg_notificacoes_atualizado_em` em `notificacoes_enviadas` e `trg_config_sla_atualizado_em` em `config_sla_etapa`.
    - `pg_indexes` mostra `idx_notif_retry` com predicado parcial citando `pendente` e `falhou`.
    - `pg_proc` mostra `tocar_atualizado_em` com `prosecdef = false` e `proconfig` contendo `search_path=`.
    - O smoke de fidelidade em `v_pos_aditiva = true` roda sem exceção com a contagem completa de PASS, e a saída está transcrita no SUMMARY.
    - `npm run lint` mantém 97 erros `tsc` (baseline; teto CI 104), nenhum deles citando arquivos desta fase; `npm run build` e `npm run test:run` verdes.
  </acceptance_criteria>
  <resume-signal>Responder "aprovado" quando o apply, o reconcile do ledger e o smoke de fidelidade pós-aditiva estiverem todos verdes e transcritos, ou descrever o que falhou.</resume-signal>
  <done>As 3 lacunas estão vivas em PROD, o ledger tem 4 versions coerentes com 4 arquivos, `db push` reporta zero drift, e está provado que o apply mudou exatamente o previsto e nada além.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Rodar o smoke comportamental — candidato-DENY, RH vaga-scoped, idempotência, CHECKs e trigger</name>
  <files>supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql (executado, não editado)</files>
  <read_first>
    - supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql — o arquivo a executar; conferir o namespace de UUID da fixture e o número de PASS declarado no cabeçalho
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-SCHEMA-VIVO.md — seção J: a contagem inicial de `notificacoes_enviadas` (0), que deve ser restaurada ao final
    - .planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-CONTEXT.md § "Specific Ideas" — por que a prova precisa ser comportamental
  </read_first>
  <what-built>
    O schema completo está vivo em PROD: as duas tabelas do baseline mais as 3 lacunas fechadas pela `20260722000002`. Nenhuma garantia de segurança foi provada por comportamento ainda — só a forma do schema foi verificada.
  </what-built>
  <action>
    O orquestrador executa `supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql` em PROD via MCP `execute_sql` e transcreve TODOS os NOTICE no SUMMARY.

    Este smoke ESCREVE em PROD (fixture descartável em namespace de UUID fixo) e limpa ao final. Antes de rodar, confirmar via `select count(*) from public.notificacoes_enviadas;` que a tabela está com 0 linhas. Depois de rodar, confirmar de novo que voltou a 0 — a asserção (n) do próprio smoke já exige isso, mas a confirmação externa é o cinto.

    Se qualquer asserção falhar, PARAR e reportar sem "consertar rodando de novo". As asserções (g), (h) e (i) são de segurança: uma falha nelas significa que `notificacoes_enviadas` é legível por quem não deveria, e a fase não pode ser fechada. As asserções (a) e (b) são de idempotência: uma falha significa que o LEDGER-02 está morto e a P39 (fase de maior risco, double-send) perderia sua guarda durável.

    Se o smoke abortar no meio deixando fixture para trás, executar manualmente o bloco de cleanup do próprio arquivo (namespace `37010037-*`) e confirmar `count(*) = 0` antes de reportar.

    Registrar no SUMMARY, além dos NOTICE: a contagem de PASS obtida vs. declarada, e a contagem final de linhas de `notificacoes_enviadas`.
  </action>
  <sql-para-colar>
```sql
-- ANTES: confirmar tabela vazia
select count(*) as linhas_antes from public.notificacoes_enviadas;

-- >>> aqui: colar e executar o conteúdo integral de
--     supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql
--     via MCP execute_sql, e capturar TODOS os NOTICE emitidos.

-- DEPOIS: confirmar limpeza e ausência de identidade vazada
reset role;
select count(*) as linhas_depois from public.notificacoes_enviadas;
select current_user, current_setting('request.jwt.claims', true) as claims_residuais;

-- DEPOIS: confirmar que o seed de negócio não foi alterado (só atualizado_em pode ter mudado)
select etapa, prazo_valor, prazo_unidade, rotulo_candidato
  from public.config_sla_etapa
 order by etapa;
```
  </sql-para-colar>
  <how-to-verify>
    1. `linhas_antes` = 0.
    2. Rodar o smoke via MCP `execute_sql`; capturar todos os NOTICE.
    3. Nenhuma exceção E a contagem de `PASS` bate com a declarada no cabeçalho do arquivo.
    4. Conferir nominalmente os PASS críticos: (a) unique_violation 23505 em `uq_notif_dedupe`; (g) candidato impersonado ⇒ 0 linhas; (h) RH não-dono ⇒ 0 linhas; (i) RH dono ⇒ >= 1; (j) admin ⇒ >= 1; (n) cleanup ⇒ 0 linhas.
    5. `linhas_depois` = 0 e `claims_residuais` nulo/vazio com `current_user` de volta ao papel original.
    6. A consulta final de `config_sla_etapa` devolve as 8 linhas com `prazo_valor`, `prazo_unidade` e `rotulo_candidato` idênticos à seção J do `37-SCHEMA-VIVO.md`.
  </how-to-verify>
  <verify>
    <automated>test -f supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql && grep -q "request.jwt.claims" supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql && grep -q "uq_notif_dedupe" supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql && echo "smoke comportamental pronto (execucao e do orquestrador via MCP)"</automated>
  </verify>
  <acceptance_criteria>
    - O smoke rodou via MCP `execute_sql` sem levantar exceção, e a contagem de `PASS` emitidos é igual à declarada no cabeçalho.
    - PASS (a): a segunda inserção da mesma `dedupe_key` levantou `unique_violation` (23505) citando `uq_notif_dedupe` — provado empiricamente, não inferido de `pg_constraint`.
    - PASS (g): com a linha de fixture existindo, o candidato REAL impersonado (`request.jwt.claims` com `app_metadata.role = 'candidato'` + `SET ROLE authenticated`) obteve `count(*) = 0`.
    - PASS (h) e (i): recrutador não-dono obteve 0 e recrutador dono obteve >= 1 — o par prova escopo, não negação universal.
    - PASS (e) e (f): `atualizado_em` avançou estritamente após UPDATE nas duas tabelas, e as 3 colunas de negócio de `config_sla_etapa` ficaram idênticas.
    - PASS (k) e (l): os 3 CHECKs de `config_sla_etapa` foram violados sob demanda (23514) e `anon` leu as 8 linhas via `sla_public_read`.
    - PASS (n): `select count(*) from public.notificacoes_enviadas` = 0 ao final; `linhas_depois` confirmado externamente = 0.
    - `current_setting('request.jwt.claims', true)` está vazio/nulo após o run e `current_user` voltou ao papel original — nenhuma identidade impersonada vazou.
    - A saída literal de todos os NOTICE está transcrita no `37-04-SUMMARY.md`, sem nenhum endereço de e-mail de candidato.
  </acceptance_criteria>
  <resume-signal>Responder "aprovado" quando todos os PASS estiverem verdes e a tabela tiver voltado a 0 linhas, ou colar a asserção que falhou.</resume-signal>
  <done>LEDGER-02 e LEDGER-03 estão provados por comportamento em PROD: o candidato não lê o ledger, o RH lê só a própria vaga, a `dedupe_key` duplicada é rejeitada — e a tabela terminou vazia, como começou.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orquestrador (MCP, privilégio total) → PROD | Único caminho de escrita desta fase; opera fora do fluxo normal de `db push` |
| Repositório (declarado) → PROD (real) | O reconcile do ledger é o que impede a criação de um drift NOVO enquanto se corrige o antigo |
| Smoke (escreve fixture) → dados de PROD | Um smoke que não limpa polui o ledger que a P38 vai começar a usar |
| Candidato / RH (JWT) → `notificacoes_enviadas` | As fronteiras que a Task 3 existe para provar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04-01 | Repudiation | Aplicar a aditiva ANTES de provar o baseline destruiria irrecuperavelmente a evidência de que a reconstrução foi fiel | mitigate | Task 1 é gate de parada absoluto com ordem obrigatória declarada em `<interfaces>`; a Task 2 só existe depois dela |
| T-37-04-02 | Repudiation | `apply_migration` grava version de timestamp ≠ prefixo do arquivo, criando um drift NOVO enquanto se corrige o antigo | mitigate | Reconcile explícito para `20260722000002` + confirmação por `db push --linked`; acceptance criteria exige as 4 versions coerentes |
| T-37-04-03 | Tampering | Afrouxar o smoke de fidelidade para "fazer passar" reintroduziria exatamente o drift que a fase elimina | mitigate | Proibição explícita na Task 1: a divergência se corrige no ARQUIVO (volta ao 37-02), nunca no smoke |
| T-37-04-04 | Information Disclosure | Uma falha de RLS não detectada deixaria o ledger de PII legível pelo candidato assim que a P38 começasse a escrever nele | mitigate | Task 3 asserções (g)/(h)/(i)/(j) com impersonação real; falha nelas bloqueia o fecho da fase |
| T-37-04-05 | Tampering | Fixture do smoke deixada para trás poluiria o ledger que a P38 usa como fonte de verdade de idempotência | mitigate | Asserção (n) interna + confirmação externa `linhas_depois = 0` no bloco SQL da Task 3, com procedimento de cleanup manual em caso de abort |
| T-37-04-06 | Elevation of Privilege | `SET ROLE` / `request.jwt.claims` vazando da sessão do smoke para consultas posteriores do orquestrador | mitigate | `RESET ROLE` obrigatório e verificação explícita de `current_user` + `current_setting('request.jwt.claims', true)` após o run |
| T-37-04-07 | Denial of Service | `supabase db push --linked` disparado por engano sobre migration com corpo `$$` ⇒ SQLSTATE 42601 e estado parcial | mitigate | `apply_migration` é o único caminho de apply; `db push --linked` usado SOMENTE em modo de conferência pós-reconcile |
| T-37-04-08 | Information Disclosure | Transcrever no SUMMARY o e-mail real do candidato de teste usado na impersonação | mitigate | Acceptance criteria proíbe endereços de e-mail na transcrição; o smoke usa contagens e predicados |
</threat_model>

<verification>
- Smoke de fidelidade em `v_pos_aditiva = false` verde ANTES de qualquer apply
- `20260722000002` aplicada via MCP `apply_migration`; ledger com as 4 versions coerentes; `db push --linked` sem drift
- Smoke de fidelidade em `v_pos_aditiva = true` verde DEPOIS do apply
- Smoke comportamental verde: 23505 empírico, candidato 0 linhas, RH não-dono 0, RH dono >= 1, admin >= 1, CHECKs violados sob demanda, `anon` lê 8 linhas de SLA
- `notificacoes_enviadas` volta a 0 linhas; `config_sla_etapa` mantém as 8 linhas com as colunas de negócio inalteradas
- `current_user` e `request.jwt.claims` limpos após o run
- `npm run lint` em 97 erros (baseline; teto CI 104), `npm run build` e `npm run test:run` verdes
</verification>

<success_criteria>
1. O repositório e PROD estão reconciliados e a reconciliação é PROVADA, não afirmada — em ambos os lados do apply.
2. As 3 lacunas estão vivas em PROD e o apply mudou exatamente o previsto, sem efeitos colaterais.
3. As garantias de segurança e idempotência do ledger (LEDGER-02, LEDGER-03) estão provadas por comportamento em PROD, com identidades reais.
4. Nenhum drift novo foi criado: 4 arquivos de migration, 4 linhas de ledger, `db push` limpo.
</success_criteria>

<output>
Create `.planning/phases/37-camada-de-dados-de-notifica-o-notificacoes-enviadas-config-s/37-04-SUMMARY.md` when done
</output>
