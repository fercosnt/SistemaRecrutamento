---
phase: 40-timeline-de-prazo-no-painel-do-candidato
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - src/features/timeline/components/PrazoEstimadoLinha.tsx
  - src/features/timeline/components/index.ts
  - src/features/timeline/components/__tests__/PrazoEstimadoLinha.test.tsx
  - src/components/pages/DashboardCandidatoPage.tsx
autonomous: true
requirements: [TIMELINE-02]

must_haves:
  truths:
    - "Cada card de candidatura em estado de espera mostra a estimativa de prazo (rotulo_candidato) com ícone Clock e um chip 'Estimativa', abaixo do label da etapa"
    - "A estimativa NUNCA é countdown: zero Date/relógio/cálculo de tempo — só o texto estático do config"
    - "Etapa terminal (aprovado/rejeitado), stale (M1) ou falha do fetch → nenhuma linha (enhancement, nunca quebra o card)"
  artifacts:
    - path: "src/features/timeline/components/PrazoEstimadoLinha.tsx"
      provides: "Componente puro: rotulo string|null → linha Clock+texto+chip Estimativa, ou null"
      contains: "Estimativa"
      min_lines: 20
  key_links:
    - from: "src/components/pages/DashboardCandidatoPage.tsx"
      to: "src/features/timeline/hooks/useSlaEtapas.ts"
      via: "useSlaEtapas() + rotuloDeEspera(lookup.get(etapa_atual)) por card"
      pattern: "useSlaEtapas"
---

<objective>
Exibir a estimativa de prazo em cada card de espera do painel do candidato, enquadrada como estimativa e nunca como countdown.

Purpose: fecha o TIMELINE-02 (o *pull* que complementa o *push* do e-mail). O componente é puro (texto do config → linha estilizada), a página resolve o rótulo por etapa via o hook do 40-01, e o RNF é honrado por construção: não existe cálculo de tempo em lugar nenhum.
Output: componente `PrazoEstimadoLinha` + integração no `DashboardCandidatoPage` + teste de componente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/40-timeline-de-prazo-no-painel-do-candidato/40-CONTEXT.md
@src/components/pages/DashboardCandidatoPage.tsx

<interfaces>
<!-- Verificado; não re-derivar. -->

DashboardCandidatoPage: importa `Clock` (lucide) e `ETAPA_M2_LABELS`/`EtapaFunilM2`;
  renderiza um card por candidatura (~L300-390); `candidatura.etapa_atual as EtapaFunilM2`
  com DRIFT GUARD para etapas stale (M1). Glass UI (`GlassCard` etc.), mobile-first.

40-01 expõe: `useSlaEtapas()` (retorna lookup por etapa + isLoading/error) e
  `rotuloDeEspera(sla): string | null` (rotulo só quando prazo_valor não-nulo).

Design system: glass, `text-white/70` para caption muted; ícone decorativo `aria-hidden`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Componente puro PrazoEstimadoLinha</name>
  <files>src/features/timeline/components/PrazoEstimadoLinha.tsx, src/features/timeline/components/index.ts</files>
  <read_first>
    - .planning/phases/40-timeline-de-prazo-no-painel-do-candidato/40-CONTEXT.md — UI contract (estados, estilo, chip)
    - src/components/pages/DashboardCandidatoPage.tsx — o idioma de glass/Tailwind e o uso do ícone Clock no card
  </read_first>
  <action>
    Criar `src/features/timeline/components/PrazoEstimadoLinha.tsx`: componente **puro** `PrazoEstimadoLinha({ rotulo }: { rotulo: string | null })`. Se `rotulo` é null/vazio → retorna `null`. Senão renderiza uma linha: ícone `Clock` (lucide, `aria-hidden`, tamanho pequeno) + o texto `rotulo` em caption muted (`text-white/70`, text-sm) + um chip sutil "Estimativa" (pill glass muted). Sem nenhum `Date`, `setInterval`, `Intl` de tempo, nem cálculo — só render estático. Export nomeado.
    Criar `src/features/timeline/components/index.ts` reexportando `PrazoEstimadoLinha`.
  </action>
  <verify>
    <automated>F=src/features/timeline/components/PrazoEstimadoLinha.tsx; test -f "$F" && grep -q "Estimativa" "$F" && grep -q "Clock" "$F" && [ "$(grep -ciE 'setInterval|Date\.now|new Date|useEffect.*Date' "$F")" -eq 0 ] && echo "PrazoEstimadoLinha OK (sem countdown)"</automated>
  </verify>
  <acceptance_criteria>
    - `PrazoEstimadoLinha` retorna `null` quando `rotulo` é null/vazio.
    - Renderiza ícone Clock (`aria-hidden`) + texto + chip "Estimativa".
    - NÃO contém `setInterval`, `Date.now`, `new Date` nem qualquer cálculo de tempo (grep = 0).
  </acceptance_criteria>
  <done>Existe um componente puro que exibe a estimativa como texto estático, nunca countdown.</done>
</task>

<task type="auto">
  <name>Task 2: Wire no DashboardCandidatoPage (hook + render por card)</name>
  <files>src/components/pages/DashboardCandidatoPage.tsx</files>
  <read_first>
    - src/components/pages/DashboardCandidatoPage.tsx — onde o card renderiza o label da etapa (~L339-348) e o DRIFT GUARD
    - src/features/timeline/hooks/useSlaEtapas.ts (40-01) — a API do hook e `rotuloDeEspera`
    - src/features/timeline/components/PrazoEstimadoLinha.tsx (Task 1)
  </read_first>
  <action>
    No `DashboardCandidatoPage.tsx`: importar `useSlaEtapas` (+ `rotuloDeEspera`) de `@/features/timeline/hooks` e `PrazoEstimadoLinha` de `@/features/timeline/components`. Chamar `useSlaEtapas()` UMA vez no topo do componente (junto aos outros hooks). No render de cada card, logo abaixo da linha do label da etapa (`ETAPA_M2_LABELS[...]`), resolver `const rotulo = rotuloDeEspera(lookup.get(candidatura.etapa_atual))` e renderizar `<PrazoEstimadoLinha rotulo={rotulo} />`. Como `rotuloDeEspera` já devolve null para terminal/sem-prazo e o componente devolve null para rotulo vazio, etapas terminais/stale/loading naturalmente não mostram nada. Não alterar a UI de decisão existente (aprovado/rejeitado). Se o hook estiver loading/erro, `lookup` vazio ⇒ null ⇒ sem linha (enhancement silencioso).
  </action>
  <verify>
    <automated>F=src/components/pages/DashboardCandidatoPage.tsx; grep -q "useSlaEtapas" "$F" && grep -q "PrazoEstimadoLinha" "$F" && echo "dashboard wired"</automated>
  </verify>
  <acceptance_criteria>
    - `DashboardCandidatoPage.tsx` importa e chama `useSlaEtapas()` uma vez e renderiza `<PrazoEstimadoLinha>` por card, abaixo do label da etapa.
    - A resolução usa `rotuloDeEspera` (estado de espera só) + o DRIFT GUARD existente para etapa stale.
    - Nenhuma mudança na UI de decisão (aprovado/rejeitado) fora adicionar a linha de estimativa quando aplicável.
  </acceptance_criteria>
  <done>Cada card de espera mostra a estimativa; terminal/stale/loading não mostram nada.</done>
</task>

<task type="auto">
  <name>Task 3: Teste de componente (RTL)</name>
  <files>src/features/timeline/components/__tests__/PrazoEstimadoLinha.test.tsx</files>
  <read_first>
    - src/features/timeline/components/PrazoEstimadoLinha.tsx (Task 1)
    - src/components/pages/__tests__/DashboardCandidatoPage.funnel.test.tsx — o idioma de render RTL do repo (imports, screen, expect)
  </read_first>
  <action>
    Criar `src/features/timeline/components/__tests__/PrazoEstimadoLinha.test.tsx` (Vitest + React Testing Library): (1) com `rotulo="Em triagem — retorno em até 48 horas."` → o texto aparece e há um "Estimativa"; (2) com `rotulo={null}` → renderiza nada (`container` vazio / query retorna null); (3) o componente não introduz nenhum timer (assert estático: o texto renderizado é exatamente o rotulo, sem sufixo de contagem).
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/features/timeline/components/__tests__/PrazoEstimadoLinha.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/features/timeline/components/__tests__/PrazoEstimadoLinha.test.tsx` passa.
    - Um caso prova o render do rotulo + chip "Estimativa"; um caso prova o null com `rotulo={null}`.
  </acceptance_criteria>
  <done>O componente é provado por Vitest verde (render e null-path).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Config text → candidato | O `rotulo_candidato` é texto de config controlado por migration (não input de usuário) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02-01 | Correctness/UX | Interpretar a estimativa como promessa rígida (countdown) frustraria o candidato quando estourar | mitigate | Zero cálculo de tempo (grep-guard no componente); chip "Estimativa" + texto "em até"; RNF honrado por construção |
| T-40-02-02 | Availability | Falha do fetch de config quebrando o painel | mitigate | Lookup vazio ⇒ null ⇒ sem linha; o card renderiza normalmente (enhancement) |
| T-40-02-03 | Correctness | Etapa stale (M1) sem entrada no config renderizaria texto errado | mitigate | `rotuloDeEspera(undefined)` → null; espelha o DRIFT GUARD do funilNavMap |
</threat_model>

<verification>
- `PrazoEstimadoLinha` puro, sem countdown (grep de Date/setInterval = 0)
- `DashboardCandidatoPage` chama useSlaEtapas e renderiza a linha por card de espera
- `npx vitest run src/features/timeline/` passa (service + componente)
- `npm run lint` não introduz erro `tsc` novo (baseline ≤97); `npm run build` verde
</verification>

<success_criteria>
1. Cada card de espera mostra a estimativa de prazo (texto do config) com Clock + chip "Estimativa".
2. Terminal/stale/loading/erro → nenhuma linha (enhancement, nunca quebra o card).
3. Nunca countdown — zero cálculo de tempo; provado por grep-guard + teste.
</success_criteria>

<output>
Create `.planning/phases/40-timeline-de-prazo-no-painel-do-candidato/40-02-SUMMARY.md` when done
</output>
