---
phase: 40-timeline-de-prazo-no-painel-do-candidato
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/timeline/types/timelineTypes.ts
  - src/features/timeline/services/slaService.ts
  - src/features/timeline/hooks/useSlaEtapas.ts
  - src/features/timeline/hooks/index.ts
  - src/features/timeline/services/__tests__/slaService.test.ts
autonomous: true
requirements: [TIMELINE-02]

must_haves:
  truths:
    - "Existe uma camada de dados timeline que lê config_sla_etapa via anon client com allowlist explícita (etapa, prazo_valor, prazo_unidade, rotulo_candidato) — nunca select('*')"
    - "useSlaEtapas() cacheia a config estática com staleTime Infinity (só muda por migration) e expõe um lookup O(1) por etapa"
    - "O service tem classe de erro custom e é provado por Vitest com o supabase client mockado"
  artifacts:
    - path: "src/features/timeline/services/slaService.ts"
      provides: "slaService.listarSlaEtapas() → SlaEtapa[]; SlaServiceError; allowlist select"
      contains: "rotulo_candidato"
      min_lines: 30
    - path: "src/features/timeline/hooks/useSlaEtapas.ts"
      provides: "useSlaEtapas() TanStack Query, slaKeys, staleTime Infinity, Map<etapa,rotulo>"
      contains: "staleTime"
      min_lines: 25
  key_links:
    - from: "src/features/timeline/services/slaService.ts"
      to: "public.config_sla_etapa"
      via: "supabase anon select allowlist (RLS sla_public_read)"
      pattern: "config_sla_etapa"
---

<objective>
Construir a camada de dados da timeline: um service + hook que lêem `config_sla_etapa` (config estática non-PII, RLS public-read) e expõem um lookup por etapa para o painel do candidato consumir.

Purpose: o texto de estimativa candidate-facing já vive em `rotulo_candidato` (seedado na P37). Esta fase só precisa buscá-lo com disciplina (allowlist, cache agressivo de config estática) e entregar um lookup O(1). Zero PII, zero segredo, zero escrita.
Output: `src/features/timeline/` (types + service + hook) + teste Vitest do service.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/40-timeline-de-prazo-no-painel-do-candidato/40-CONTEXT.md
@src/features/agendamento/hooks/useMeuAgendamento.ts

<interfaces>
<!-- Convenções já verificadas; não re-derivar. -->

config_sla_etapa (P37, RLS sla_public_read anon+authenticated): etapa (PK, enum etapa_processo),
  prazo_valor (int|null), prazo_unidade (text|null), rotulo_candidato (text), atualizado_em.
  database.types.ts já tem o Row tipado.

Convenção de hook/keys (meuAgendamentoKeys, bigfiveKeys): `export const <dom>Keys = { all: ['<dom>'] as const, ... }`.
Service: classe de erro custom (camelCaseService.ts), select com allowlist, nunca select('*').
Client anon: `import { supabase } from '@/lib/supabase/client'`.
TanStack Query v5 no projeto (staleTime/retry).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + service (slaService.listarSlaEtapas + SlaServiceError)</name>
  <files>src/features/timeline/types/timelineTypes.ts, src/features/timeline/services/slaService.ts</files>
  <read_first>
    - .planning/phases/40-timeline-de-prazo-no-painel-do-candidato/40-CONTEXT.md — decisões travadas
    - database.types.ts — o Row de config_sla_etapa (nomes/tipos exatos das colunas)
    - src/features/agendamento/services/agendamentoCandidatoService.ts — o idioma de service (classe de erro custom, select allowlist, import do client anon)
  </read_first>
  <action>
    Criar `src/features/timeline/types/timelineTypes.ts`: tipo `SlaEtapa` = `{ etapa: string; prazo_valor: number | null; prazo_unidade: string | null; rotulo_candidato: string }` (derivar de `Database['public']['Tables']['config_sla_etapa']['Row']` via `Pick` para não divergir do gerado).

    Criar `src/features/timeline/services/slaService.ts`: classe `SlaServiceError extends Error` (padrão camelCaseService) e `listarSlaEtapas(): Promise<SlaEtapa[]>` que faz `supabase.from('config_sla_etapa').select('etapa, prazo_valor, prazo_unidade, rotulo_candidato')` (allowlist — NUNCA `select('*')`); em `error` lança `SlaServiceError` com a mensagem; retorna `data ?? []`.
  </action>
  <verify>
    <automated>F=src/features/timeline/services/slaService.ts; test -f "$F" && grep -q "config_sla_etapa" "$F" && grep -q "rotulo_candidato" "$F" && grep -q "SlaServiceError" "$F" && [ "$(grep -c "select('\*')" "$F")" -eq 0 ] && echo "slaService OK"</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/timeline/services/slaService.ts` existe; `listarSlaEtapas` faz select com allowlist explícita `etapa, prazo_valor, prazo_unidade, rotulo_candidato` e NÃO usa `select('*')`.
    - Lança `SlaServiceError` no caminho de erro do supabase.
    - `SlaEtapa` deriva do Row gerado (Pick), não redigita os tipos à mão de forma divergente.
  </acceptance_criteria>
  <done>A leitura de config_sla_etapa está encapsulada num service com allowlist e erro tipado.</done>
</task>

<task type="auto">
  <name>Task 2: Hook useSlaEtapas + query key + lookup Map</name>
  <files>src/features/timeline/hooks/useSlaEtapas.ts, src/features/timeline/hooks/index.ts</files>
  <read_first>
    - src/features/agendamento/hooks/useMeuAgendamento.ts — o idioma exato de query key + useQuery do projeto
    - src/features/timeline/services/slaService.ts (Task 1) — a função a consumir
  </read_first>
  <action>
    Criar `src/features/timeline/hooks/useSlaEtapas.ts`: `export const slaKeys = { all: ['sla-etapas'] as const }`. `useSlaEtapas()` usa `useQuery({ queryKey: slaKeys.all, queryFn: listarSlaEtapas, staleTime: Infinity })` (config estática; muda só por migration). Expor, além de `data/isLoading/error`, um lookup `rotuloPorEtapa: Map<string, SlaEtapa>` derivado via `select`/`useMemo` (ou um `Record`) para lookup O(1) por `etapa`. Um helper `rotuloDeEspera(sla: SlaEtapa | undefined): string | null` que retorna `rotulo_candidato` só quando `prazo_valor != null` (estado de espera) — senão `null`.
    Criar `src/features/timeline/hooks/index.ts` reexportando `useSlaEtapas`, `slaKeys`, `rotuloDeEspera`.
  </action>
  <verify>
    <automated>F=src/features/timeline/hooks/useSlaEtapas.ts; test -f "$F" && grep -q "staleTime" "$F" && grep -q "slaKeys" "$F" && grep -q "useQuery" "$F" && grep -q "rotuloDeEspera\|prazo_valor" "$F" && echo "useSlaEtapas OK"</automated>
  </verify>
  <acceptance_criteria>
    - `useSlaEtapas()` usa `useQuery` com `staleTime: Infinity` e a query key `slaKeys.all`.
    - Expõe um lookup por `etapa` (Map ou Record) para O(1).
    - `rotuloDeEspera` retorna `rotulo_candidato` só para etapas com `prazo_valor` não-nulo (estado de espera); senão null.
  </acceptance_criteria>
  <done>O painel pode chamar `useSlaEtapas()` e resolver o rótulo de estimativa por etapa em O(1).</done>
</task>

<task type="auto">
  <name>Task 3: Teste Vitest do service (supabase mockado)</name>
  <files>src/features/timeline/services/__tests__/slaService.test.ts</files>
  <read_first>
    - src/features/timeline/services/slaService.ts (Task 1) — o que testar
    - src/features/agendamento/services/__tests__/agendamentoCandidatoService.test.ts — o idioma de mock do supabase client no Vitest deste repo
  </read_first>
  <action>
    Criar `src/features/timeline/services/__tests__/slaService.test.ts` (Vitest): mockar `@/lib/supabase/client` espelhando o idioma do teste vizinho. Casos: (1) `listarSlaEtapas` chama `from('config_sla_etapa').select(<allowlist>)` e retorna as linhas mockadas; (2) o select NÃO é `*` (asserir o argumento passado ao `.select`); (3) `error` do supabase → lança `SlaServiceError`; (4) `data` null → retorna `[]`.
  </action>
  <verify>
    <automated>cd /Users/fernando/code/SistemaRecrutamento && npx vitest run src/features/timeline/services/__tests__/slaService.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/features/timeline/services/__tests__/slaService.test.ts` passa.
    - Um caso assere que o `.select` recebeu a allowlist (não `*`).
    - Um caso prova que erro do supabase vira `SlaServiceError`.
  </acceptance_criteria>
  <done>A camada de dados é provada por Vitest verde com o client mockado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client anon → config_sla_etapa | Leitura pública por design (RLS sla_public_read); a tabela é config non-PII |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01-01 | Information Disclosure | `select('*')` num futuro em que a tabela ganhe coluna sensível vazaria por anon | mitigate | Allowlist explícita; acceptance `grep -c "select('*')"`=0; teste assere o argumento |
| T-40-01-02 | Availability | Falha do fetch quebrando o painel | mitigate | Service devolve erro tipado; o consumo (40-02) trata como enhancement silencioso |
</threat_model>

<verification>
- `src/features/timeline/` existe com types+service+hook; select com allowlist, nunca `*`
- `npx vitest run src/features/timeline/` passa
- `npm run lint` mantém `tsc` de `src/**` em ≤97 (não introduz erro novo)
- `npm run build` verde
</verification>

<success_criteria>
1. Existe um service+hook que lê config_sla_etapa com allowlist e cacheia como config estática.
2. O painel pode resolver o rótulo de estimativa por etapa em O(1), com "estado de espera" já filtrado por prazo não-nulo.
3. Provado por Vitest verde.
</success_criteria>

<output>
Create `.planning/phases/40-timeline-de-prazo-no-painel-do-candidato/40-01-SUMMARY.md` when done
</output>
