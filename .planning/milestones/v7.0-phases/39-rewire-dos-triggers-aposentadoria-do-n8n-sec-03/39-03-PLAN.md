---
phase: 39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/p39_rewire_triggers_smoke.sql
autonomous: true
requirements: [DISPATCH-01, DISPATCH-02, DISPATCH-03, DISPATCH-04]

must_haves:
  truths:
    - "Existe supabase/tests/p39_rewire_triggers_smoke.sql — o gate Nyquist da fase, mirror de p37_lacunas_rls_idempotencia_smokes.sql (GUC smoke39.pass auto-exigido) + n8n_novo_candidato_smoke.sql (fixture descartável RLS-bypass + skip-se-secret-set)"
    - "Asserções ESTRUTURAIS (pg_get_functiondef, seguras em PROD vivo, sem INSERT): os 3 trg_notif_* têm o predicado certo (avaliacao_assincrona / aprovado,rejeitado+auto_rejeitado=false / survivor-guard / convite sem guard), body ids-only (evento+candidatura_id[+agendamento_id], zero PII), header Bearer, SECURITY DEFINER SET search_path='', wrapper EXCEPTION WHEN OTHERS"
    - "Asserção de CATÁLOGO: 0 triggers e 0 funções trg_n8n_* remanescentes; exatamente 3 triggers trg_notif_* (DISPATCH-03)"
    - "Asserções COMPORTAMENTAIS (fixture descartável, disposable-DB/pre-secret): graceful-skip + fail-open — INSERT de candidatura/histórico/agendamento COMMITA com o Vault ausente; survivor-guard — knockout não gera erro e (onde net._http_response é observável) 0 dispatch de confirmação"
  artifacts:
    - path: "supabase/tests/p39_rewire_triggers_smoke.sql"
      provides: "Smoke que prova DISPATCH-01..04 (estrutural + catálogo + graceful-skip/survivor-guard comportamental) contra Postgres descartável / PROD via MCP"
      contains: "smoke39.pass"
      min_lines: 200
  key_links:
    - from: "supabase/tests/p39_rewire_triggers_smoke.sql"
      to: "pg_proc / pg_trigger (catálogo)"
      via: "pg_get_functiondef + SELECT count(*) FROM pg_trigger WHERE tgname LIKE 'trg_n8n_%'"
      pattern: "trg_notif_transicao"
---

<objective>
Escrever o Wave-0 smoke `supabase/tests/p39_rewire_triggers_smoke.sql` — o gate de aceitação da fase de maior risco. Prova, contra o schema pós-migration (aplicada num Postgres descartável OU em PROD via MCP no 39-04), as 7 invariantes de segurança do rewire: (1) exatamente 1 e-mail por evento sem duplicata; (2) zero superfície de double-send remanescente; (3) funil avança com EF/secret indisponível (graceful-skip + fail-open); (4) zero PII no payload; (5) survivor-guard do knockout; (6) mapeamento evento→fonte correto; (7) decisão só humana.

Purpose: um schema pode ter a forma certa e vazar no comportamento. Este smoke prova por EXECUÇÃO — estruturalmente (via `pg_get_functiondef`, seguro em PROD vivo sem INSERT) para os predicados/ids-only/Bearer/DEFINER, por catálogo para a aposentadoria do n8n, e comportamentalmente (fixture descartável, graceful-skip/survivor-guard) onde o disparo não é observável em PROD sem gerar e-mail real.
Output: 1 arquivo `.sql`. **Zero contato com PROD** — este plano só ESCREVE o smoke; sua EXECUÇÃO via MCP é o gate do orquestrador no 39-04. As provas ponta-a-ponta que EXIGEM a EF viva + secret (net._http_response com dispatch real por evento; decisão-só-humana via impersonação de RH) são delegadas ao 39-04 Task 3, não a este smoke.

⚠ **SAFE-NOW.** O executor NÃO roda este SQL contra PROD. Se houver um Postgres descartável local, pode rodá-lo best-effort; senão, a verificação é o grep estrutural do arquivo (o run real é do orquestrador no 39-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03/39-RESEARCH.md
@.planning/phases/39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03/39-VALIDATION.md
@supabase/tests/n8n_novo_candidato_smoke.sql
@supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql

<interfaces>
<!-- CONTRATO TRAVADO (idêntico ao de 39-01) — o smoke assere EXATAMENTE estes objetos.
     Precedentes a espelhar: n8n_novo_candidato_smoke.sql (fixture descartável RLS-bypass +
     best-effort secret precondition + graceful-skip) e p37_lacunas_rls_idempotencia_smokes.sql
     (GUC de contagem auto-exigido: cada PASS incrementa `smoke39.pass`, a asserção final levanta
     exceção se o total ≠ declarado — um run todo-SKIP NÃO é verde). -->

Objetos criados por 39-01 (a asserir):
  function public.trg_notif_transicao()   → trigger trg_notif_transicao   AFTER INSERT ON public.historico_candidatura
  function public.trg_notif_confirmacao() → trigger trg_notif_confirmacao AFTER INSERT ON public.candidaturas
  function public.trg_notif_convite()     → trigger trg_notif_convite     AFTER INSERT ON public.agendamentos_entrevista

Predicados esperados (asserção via pg_get_functiondef contém a substring):
  transicao:   'avaliacao_assincrona' (ramo avanço) · 'aprovado'/'rejeitado' + 'auto_rejeitado = false' (ramo decisão)
  confirmacao: "NEW.status = 'rejeitado' OR NEW.opcao_knockout_id IS NOT NULL" (survivor-guard)
  convite:     body contém 'agendamento_id' (NEW.id)
Body ids-only (todas): jsonb_build_object com 'evento' + 'candidatura_id' [+ 'agendamento_id']; NENHUMA
  substring de PII (nome/email/cpf/telefone) no corpo de qualquer trg_notif_*.
Hardening (todas): 'SECURITY DEFINER', "search_path = ''", "Bearer ' || v_invoke_key", 'EXCEPTION WHEN OTHERS'.

Catálogo pós-DROP (asserção segura em PROD, sem INSERT):
  SELECT count(*) FROM pg_trigger WHERE tgname LIKE 'trg_n8n_%'  → 0
  SELECT count(*) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
    WHERE n.nspname='public' AND p.proname LIKE 'trg_n8n_%'      → 0
  SELECT count(*) FROM pg_trigger WHERE tgname LIKE 'trg_notif_%' AND NOT tgisinternal → 3

Diagnóstico do dispatch (para o run comportamental, onde pg_net vive):
  SELECT status_code, error_msg FROM net._http_response WHERE created > now()-interval '15 min';

Colunas de fixture (verificadas — database.types.ts): candidaturas(status enum status_candidatura inclui
  'rejeitado', opcao_knockout_id uuid|null, id, candidato_id, vaga_id, etapa_atual); historico_candidatura
  (candidatura_id, etapa_para NOT NULL enum etapa_processo, auto_rejeitado bool NOT NULL);
  agendamentos_entrevista(id, candidatura_id NOT NULL, data_hora, local_ou_link).
  ⚠ vagas.created_by TEM FK (P32 Pitfall 4) — usar usuarios_rh REAIS na fixture, nunca UUID sintético.

⚠ Precondição de segurança (mirror n8n_novo): a metade comportamental deve SKIP-com-NOTICE se
  vault.decrypted_secrets tiver project_url+edge_invoke_key setados (evita disparar e-mail real por
  fixture descartável em PROD). A metade estrutural + catálogo é sempre segura (não insere linhas).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Metade estrutural + catálogo (segura em PROD vivo, zero INSERT) — DISPATCH-01/02/03/04</name>
  <files>supabase/tests/p39_rewire_triggers_smoke.sql</files>
  <read_first>
    - supabase/tests/p37_lacunas_rls_idempotencia_smokes.sql — o padrão do GUC de contagem auto-exigido (`smoke37.pass`) + cabeçalho documentando cada asserção; espelhar como `smoke39.pass`
    - .planning/phases/39-.../39-RESEARCH.md § "Validation Architecture" (Test Map DISPATCH-01..04) + "O que provar" (7 invariantes) + "Diagnóstico de falha silenciosa"
    - .planning/phases/39-.../39-VALIDATION.md § "Per-Task Verification Map" + "Wave 0 Requirements"
    - o contrato `<interfaces>` acima (nomes de objeto + substrings de predicado + queries de catálogo)
  </read_first>
  <action>
    Criar `supabase/tests/p39_rewire_triggers_smoke.sql` com um cabeçalho-comentário no estilo do p37 (título, requirements DISPATCH-01..04, a lista das asserções e o gate `smoke39.pass`, e a nota de RUN: "via MCP execute_sql em PROD no 39-04 OU psql num Postgres descartável; a metade estrutural/catálogo NÃO insere linhas e é segura em PROD vivo").

    Inicializar o GUC de contagem (`set_config('smoke39.pass','0',false)` ou equivalente) e escrever as asserções ESTRUTURAIS + CATÁLOGO, cada uma emitindo `RAISE NOTICE 'PASS (x) ...'` e incrementando `smoke39.pass`, levantando exceção em falha:

    (a) transicao — `pg_get_functiondef('public.trg_notif_transicao()'::regprocedure)` contém `avaliacao_assincrona` E (`aprovado` E `rejeitado`) E `auto_rejeitado = false` (mapeamento evento→fonte, DISPATCH-01 / invariante 6).
    (b) confirmacao — o functiondef contém `opcao_knockout_id IS NOT NULL` e `status` = `'rejeitado'` no survivor-guard (DISPATCH-02 / invariante 5, estrutural).
    (c) convite — o functiondef contém `agendamento_id` no body (DISPATCH-02 / D-04).
    (d) ids-only / zero-PII — para os 3 functiondefs, asserir que NENHUM contém as substrings `nome`, `email`, `cpf`, `telefone` (case-insensitive) no corpo do jsonb_build_object do body (DISPATCH-04 / invariante 4). Usar uma checagem robusta (o body só referencia `evento`, `candidatura_id`, `agendamento_id`).
    (e) hardening — os 3 functiondefs contêm `SECURITY DEFINER`, `search_path = ''`, `Bearer ' || v_invoke_key` (header self-auth) e `EXCEPTION WHEN OTHERS` (DISPATCH-04 self-auth + fail-open estrutural).
    (f) catálogo pós-DROP — `count(*) pg_trigger WHERE tgname LIKE 'trg_n8n_%'` = 0 E `count(*) pg_proc public.trg_n8n_%` = 0 E `count(*) pg_trigger trg_notif_% NOT tgisinternal` = 3 (DISPATCH-03 / invariante 2).

    Terminar com a asserção-RESUMO que levanta exceção se `smoke39.pass` ≠ (nº de asserções estruturais declaradas) — mas deixar o total AJUSTÁVEL/parcial se a Task 2 (comportamental) adicionar asserções ao mesmo arquivo: preferir um contador seccional (ex.: exigir ≥6 PASS estruturais nesta seção) e deixar o RESUMO-final total para a Task 2 fechar. Documentar a contagem esperada no cabeçalho.

    NÃO inserir nenhuma linha nesta metade — só leituras de catálogo/`pg_get_functiondef`. Isso é o que a torna segura de rodar em PROD vivo pelo orquestrador.
  </action>
  <verify>
    <automated>f=supabase/tests/p39_rewire_triggers_smoke.sql; test -f "$f" && grep -q "smoke39.pass" "$f" && grep -q "pg_get_functiondef" "$f" && grep -q "trg_notif_transicao" "$f" && grep -q "trg_n8n_%" "$f" && grep -q "auto_rejeitado = false" "$f" && grep -q "opcao_knockout_id IS NOT NULL" "$f" && grep -q "agendamento_id" "$f" && echo "T1 OK — asserções estruturais + catálogo presentes"</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe, inicializa `smoke39.pass` e usa `RAISE NOTICE 'PASS ...'` por asserção + uma asserção que levanta exceção se a contagem não bater (gate auto-exigido, não "não levantou exceção").
    - Asserção (a): `pg_get_functiondef` de trg_notif_transicao contém `avaliacao_assincrona`, `aprovado`, `rejeitado` e `auto_rejeitado = false`.
    - Asserção (b): trg_notif_confirmacao contém o survivor-guard (`opcao_knockout_id IS NOT NULL`).
    - Asserção (c): trg_notif_convite contém `agendamento_id` no body.
    - Asserção (d): nenhum dos 3 functiondefs contém `nome`/`email`/`cpf`/`telefone` no body (zero-PII).
    - Asserção (e): os 3 contêm `SECURITY DEFINER`, `search_path = ''`, header Bearer e `EXCEPTION WHEN OTHERS`.
    - Asserção (f): catálogo — 0 `trg_n8n_*` (trigger e função) e 3 `trg_notif_*` triggers.
    - NENHUM `INSERT INTO` nesta metade (segura em PROD vivo); a contagem esperada está documentada no cabeçalho.
  </acceptance_criteria>
  <done>A metade que prova a FORMA está escrita: predicados corretos, body ids-only, hardening self-auth/fail-open, n8n aposentado do catálogo — tudo por leitura de catálogo, segura de rodar em PROD.</done>
</task>

<task type="auto">
  <name>Task 2: Metade comportamental — graceful-skip/fail-open + survivor-guard (fixture descartável) + RESUMO-final</name>
  <files>supabase/tests/p39_rewire_triggers_smoke.sql</files>
  <read_first>
    - supabase/tests/n8n_novo_candidato_smoke.sql — o precedente DIRETO da metade comportamental: fixture descartável RLS-bypass (auth.users + linha filha, UUID fixo idempotente), best-effort secret precondition (SKIP se secret set), prova de graceful-skip (INSERT COMMITA sem erro), cleanup ROLLBACK-free
    - .planning/phases/39-.../39-RESEARCH.md § "Pitfall 2" (fail-open) + "Pattern 2" (survivor-guard) + § "O que provar" invariantes 3 e 5
    - o próprio arquivo criado na Task 1 (append; manter o contador `smoke39.pass`)
  </read_first>
  <action>
    Fazer append no MESMO arquivo, espelhando `n8n_novo_candidato_smoke.sql`: um bloco SETUP privilegiado (RESET ROLE) que (i) limpa qualquer fixture anterior por namespace de UUID fixo `39010039-*`, (ii) faz a best-effort secret precondition — se `vault.decrypted_secrets` tiver `project_url`+`edge_invoke_key` setados, marcar `smoke.ready='n'` e fazer TODAS as asserções comportamentais SKIP-com-NOTICE (nunca disparar e-mail real por fixture em PROD), (iii) se o Vault estiver ausente/ilegível (estado do Postgres descartável / DEFINER-only), construir a fixture descartável.

    Asserções comportamentais (cada PASS incrementa `smoke39.pass`):
    (g) graceful-skip + fail-open (invariante 3) — com o Vault ausente, um INSERT de candidatura descartável (survivor: status<>'rejeitado', opcao_knockout_id NULL) COMMITA sem erro (o trigger faz RETURN NEW). Se o trigger levantasse, o INSERT falharia → a asserção prova que o funil avança sem a EF/secret.
    (h) survivor-guard não-erra (invariante 5) — um INSERT de candidatura knockout (status='rejeitado' OU opcao_knockout_id preenchido) também COMMITA sem erro (o guard faz RETURN NEW antes de qualquer leitura de Vault). Onde `net._http_response` for observável (pg_net vivo + secret set — caminho NÃO tomado em PROD por causa da precondição), documentar no comentário que o dispatch de confirmação seria 0 para o knockout; na ausência disso, a prova é o COMMIT-sem-erro + a asserção estrutural (b) da Task 1.
    (i) histórico + agendamento (invariante 3 estendida) — INSERTs descartáveis em `historico_candidatura` (um por `etapa_para` relevante) e `agendamentos_entrevista` COMMITAM sem erro (graceful-skip nas 3 fontes). Fixture usa `candidatura_id` da candidatura descartável e `usuarios_rh` REAL onde FK exigir.

    CLEANUP ROLLBACK-free: deletar as linhas descartáveis (namespace `39010039-*`) + `auth.users` descartável + resetar GUCs; asserção final de que as tabelas voltaram à contagem inicial. Fechar com o RESUMO-final: exceção se `smoke39.pass` ≠ total declarado (estruturais da Task 1 + comportamentais desta), ajustando o total no cabeçalho.

    Documentar no cabeçalho que a prova ponta-a-ponta do net._http_response com dispatch REAL por evento + a decisão-só-humana (impersonação de RH → auto_rejeitado=false → dispara decisao) são do 39-04 Task 3 (exigem EF viva + secret), NÃO deste smoke.
  </action>
  <verify>
    <automated>f=supabase/tests/p39_rewire_triggers_smoke.sql; grep -q "39010039-" "$f" && grep -qi "graceful" "$f" && grep -q "RESET ROLE" "$f" && grep -qi "vault.decrypted_secrets" "$f" && grep -qiE "knockout|opcao_knockout_id" "$f" && grep -qi "cleanup" "$f" && echo "T2 OK — fixture descartável + graceful-skip + survivor-guard + cleanup"</automated>
  </verify>
  <acceptance_criteria>
    - O bloco comportamental usa fixture descartável em namespace `39010039-*`, RLS-bypass no setup, e best-effort secret precondition que SKIP-com-NOTICE se o Vault estiver setado (nunca dispara e-mail real por fixture).
    - PASS (g): INSERT de candidatura survivor descartável COMMITA sem erro (graceful-skip/fail-open provado — o funil avança sem a EF/secret).
    - PASS (h): INSERT de candidatura knockout descartável COMMITA sem erro (survivor-guard faz RETURN NEW antes do Vault).
    - PASS (i): INSERTs descartáveis em historico_candidatura e agendamentos_entrevista COMMITAM sem erro; FKs satisfeitas com `usuarios_rh` reais.
    - CLEANUP: as tabelas terminam na contagem inicial (namespace `39010039-*` removido + auth.users descartável + GUCs resetados); asserção final confirma.
    - RESUMO-final levanta exceção se `smoke39.pass` ≠ total declarado; o cabeçalho documenta o total e delega net._http_response-ponta-a-ponta + decisão-só-humana ao 39-04.
  </acceptance_criteria>
  <done>O smoke prova por comportamento que o funil nunca bloqueia (graceful-skip + fail-open) e que o knockout não erra (survivor-guard), com fixture descartável que se limpa — e é honesto sobre o que só o gate ponta-a-ponta do 39-04 pode provar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke (fixture descartável) → dados de PROD | Um smoke que insere e não limpa, ou que dispara e-mail real, polui o funil/candidato |
| Asserção estrutural (catálogo) vs comportamental (INSERT) | A estrutural é segura em PROD vivo; a comportamental precisa da precondição de secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03-01 | Repudiation | Um run todo-SKIP mascara falha de fixture e passa como verde (o gate vazio) | mitigate | GUC `smoke39.pass` auto-exigido: o RESUMO levanta exceção se a contagem ≠ total (mirror p37 asserção (o)) |
| T-39-03-02 | Information Disclosure | Fixture descartável em PROD dispara e-mail real a um endereço arbitrário | mitigate | Best-effort secret precondition (SKIP se Vault setado) + a metade estrutural não insere nada; dispatch real só no 39-04 controlado (`*@resend.dev`) |
| T-39-03-03 | Tampering | Afrouxar o smoke para "passar" reintroduz o risco que a fase existe para provar ausente | mitigate | Asserções asseres substrings EXATAS de predicado/hardening; a contagem auto-exigida impede pular asserção silenciosamente |
| T-39-03-04 | Denial of Service | Fixture deixada para trás polui candidaturas/histórico que o funil real lê | mitigate | Cleanup ROLLBACK-free por namespace `39010039-*` + asserção final de contagem-inicial (mirror n8n_novo) |
</threat_model>

<verification>
- `supabase/tests/p39_rewire_triggers_smoke.sql` existe, com `smoke39.pass` auto-exigido
- Estrutural: predicados (avaliacao_assincrona / auto_rejeitado=false / survivor-guard / agendamento_id), zero-PII, hardening (DEFINER/search_path/Bearer/EXCEPTION) — via pg_get_functiondef
- Catálogo: 0 trg_n8n_* (trigger+função), 3 trg_notif_* triggers
- Comportamental: graceful-skip/fail-open (INSERT commita sem Vault) + survivor-guard (knockout não erra) + cleanup à contagem inicial
- Metade estrutural sem INSERT (segura em PROD); metade comportamental com secret-precondition SKIP
- Zero execução contra PROD neste plano (run é do orquestrador no 39-04)
</verification>

<success_criteria>
1. Existe o Wave-0 smoke que fecha o gap Nyquist da fase, mirror dos precedentes p37 + n8n_novo.
2. Prova estruturalmente os 4 predicados, o body ids-only, o self-auth Bearer e o fail-open, e por catálogo a aposentadoria do n8n.
3. Prova comportamentalmente graceful-skip/fail-open + survivor-guard com fixture descartável que se limpa.
4. É honesto: delega ao 39-04 a prova ponta-a-ponta (net._http_response + decisão-só-humana) que exige a EF viva.
</success_criteria>

<output>
Create `.planning/phases/39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03/39-03-SUMMARY.md` when done
</output>
