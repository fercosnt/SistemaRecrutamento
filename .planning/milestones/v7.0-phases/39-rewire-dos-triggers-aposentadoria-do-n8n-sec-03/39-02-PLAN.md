---
phase: 39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/submit-candidatura/index.ts
  - supabase/functions/submit-candidatura/index.test.ts
autonomous: true
requirements: [DISPATCH-03]

must_haves:
  truths:
    - "O bloco fire-and-forget que faz fetch(...) ao webhook n8n (URL de fallback hardcoded) foi REMOVIDO de submit-candidatura/index.ts — o único caminho de disparo n8n LIVE do funil deixa de existir no código (D-09)"
    - "Nenhuma referência a n8n / N8N_NOVA_CANDIDATURA_URL / fernandocosta.app.n8n.cloud permanece em submit-candidatura (grep = 0), incluindo o docstring do topo"
    - "O deno test de submit-candidatura passa verde sem o stub de fetch vestigial — a candidatura ainda é registrada e a resposta de sucesso (status/etapa_atual) preservada"
  artifacts:
    - path: "supabase/functions/submit-candidatura/index.ts"
      provides: "EF submit-candidatura sem o disparo n8n — pronta para redeploy no plano GATED 39-04"
      contains: "candidaturaId"
  key_links:
    - from: "supabase/functions/submit-candidatura/index.ts"
      to: "trg_notif_confirmacao (39-01)"
      via: "o disparo de confirmação passa a ser o trigger AFTER INSERT survivor-guarded, não o fetch n8n"
      pattern: "candidaturaId"
---

<objective>
Remover o disparo n8n LIVE embutido na EF `submit-candidatura` — o `fetch(...)` fire-and-forget pós-commit com URL de fallback HARDCODED (`https://fernandocosta.app.n8n.cloud/webhook/nova-candidatura`) — e ajustar o deno test que existia só para stubar esse fetch. Esta é a metade cross-artefato do DISPATCH-03: a migration (39-01) dropa os triggers n8n do catálogo, mas este `fetch` é o ÚNICO caminho de disparo n8n **vivo** e NÃO é dormente — desarmar a env var não o desliga (o fallback é hardcoded).

Purpose: eliminar a superfície de double-send real. Sem esta remoção, toda inscrição dispararia DOIS caminhos após o rewire (novo `trg_notif_confirmacao` → Beauty Smile + `fetch` n8n LIVE → n8n cloud). A confirmação passa a ser exclusivamente o trigger survivor-guarded (D-03) — que ainda refina o comportamento: o `fetch` atual NÃO suprime knockout (dispara para toda inscrição, inclusive auto-rejeitada); o trigger novo suprime.
Output: `index.ts` sem o bloco n8n + `index.test.ts` limpo. **Zero contato com PROD** — o REDEPLOY desta EF é o plano GATED 39-04 (deve ocorrer ANTES do apply da migration; Pitfall 3, ordem redeploy-antes-do-apply).

⚠ **SAFE-NOW.** Este plano só edita código local. Não faz deploy, não roda `supabase functions deploy`, não toca PROD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03/39-CONTEXT.md
@supabase/functions/submit-candidatura/index.ts
@supabase/functions/submit-candidatura/index.test.ts

<interfaces>
<!-- O bloco a remover (index.ts) e o acoplamento no test. Identificadores concretos. -->

index.ts — REMOVER integralmente a seção "5) Fire-and-forget N8N webhook AFTER COMMIT"
  (aprox. linhas 300-330): a const `N8N_NOVA_CANDIDATURA_URL` (com o fallback hardcoded
  `https://fernandocosta.app.n8n.cloud/webhook/nova-candidatura`), o `fetch(...).catch(...)`,
  e a linha do docstring do topo (linha 13: "7. Fire-and-forget N8N webhook AFTER COMMIT ...").
  PRESERVAR o passo "6) Success" (o `jsonResponse` com `candidaturaId`, `candidaturaUrl`,
  `status`, `etapa_atual`) — a renumeração de comentários (5→success) é cosmética e opcional.

index.test.ts — o teste stuba `globalThis.fetch` SÓ por causa desse fetch:
  · `withStubbedFetch` (linhas ~159-167) e o uso em ~204 (`await withStubbedFetch(() => handler(...))`)
  · o comentário ~155-158 explicando o stub do webhook
  · `sanitizeOps: false` (~198) com a justificativa "fire-and-forget webhook promise is intentionally
    not awaited" — após a remoção NÃO há mais promise não-aguardada; reavaliar se `sanitizeOps: false`
    ainda é necessário (se o teste passar verde com `sanitizeOps` default, remover o override + a nota;
    se algum outro recurso assíncrono exigir, manter mas atualizar a justificativa).

Comando de teste: `deno test` da função (o repo roda deno test por função; ver como as outras EFs
  rodam — não há --allow-net necessário após a remoção do fetch).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover o bloco fire-and-forget n8n de submit-candidatura/index.ts</name>
  <files>supabase/functions/submit-candidatura/index.ts</files>
  <read_first>
    - supabase/functions/submit-candidatura/index.ts — ler o bloco 5 (linhas ~300-330) + o docstring do topo (linha 13); confirmar que o passo 6 (Success) usa `candidaturaId`/`status`/`etapa_atual` do `rpcResult`
    - .planning/phases/39-.../39-CONTEXT.md § D-09 (o fetch é LIVE, não dormente; o fallback hardcoded; tem de ser removido + redeploy)
    - .planning/phases/39-.../39-RESEARCH.md § "Runtime State Inventory" (o item cross-artefato) + "Pitfall 3" (ordem redeploy-antes-do-apply — contexto de por que a remoção é aqui e o redeploy é 39-04)
  </read_first>
  <action>
    Remover integralmente da EF a seção "5) Fire-and-forget N8N webhook AFTER COMMIT — non-blocking" (a const `N8N_NOVA_CANDIDATURA_URL` incluindo o fallback hardcoded `https://fernandocosta.app.n8n.cloud/webhook/nova-candidatura`, o `fetch(N8N_NOVA_CANDIDATURA_URL, {...}).catch(...)` e todos os comentários WR-04/A5 que só descrevem esse disparo). Remover também a linha 13 do docstring do topo que lista "7. Fire-and-forget N8N webhook AFTER COMMIT".

    PRESERVAR intacto o passo de sucesso: o `const rpcResult`/`candidaturaId` e o `return jsonResponse({ ok:true, data:{ candidaturaId, candidaturaUrl:'/candidato/perfil', status: rpcResult.status, etapa_atual: rpcResult.etapa_atual } }, 200)`. A renumeração dos comentários de passo (o que era "6) Success" pode virar "5) Success") é opcional/cosmética; não alterar lógica.

    Não introduzir nenhum novo dispatch nem chamada de rede — a confirmação agora é responsabilidade EXCLUSIVA do trigger `trg_notif_confirmacao` (39-01). Não tocar em nenhum outro passo (validação, RPC atomic, tratamento de erro).
  </action>
  <verify>
    <automated>f=supabase/functions/submit-candidatura/index.ts; test -f "$f" && [ "$(grep -ci 'n8n' "$f")" = "0" ] && ! grep -q "N8N_NOVA_CANDIDATURA_URL" "$f" && ! grep -q "fernandocosta.app.n8n.cloud" "$f" && grep -q "candidaturaUrl: '/candidato/perfil'" "$f" && echo "T1 OK — zero n8n, success preservado"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci 'n8n' supabase/functions/submit-candidatura/index.ts` = 0 (case-insensitive, inclui o docstring).
    - Nenhuma ocorrência de `N8N_NOVA_CANDIDATURA_URL` nem `fernandocosta.app.n8n.cloud` no arquivo.
    - O `return jsonResponse` de sucesso preserva `candidaturaId`, `candidaturaUrl: '/candidato/perfil'`, `status` e `etapa_atual`.
    - Nenhum novo `fetch`/dispatch de rede foi adicionado; os passos de validação/RPC/erro estão inalterados.
  </acceptance_criteria>
  <done>O único caminho de disparo n8n LIVE do funil foi removido do código-fonte da EF; a confirmação passa a depender só do trigger survivor-guarded. Pronto para o redeploy GATED (39-04).</done>
</task>

<task type="auto">
  <name>Task 2: Limpar o stub de fetch vestigial no deno test + provar verde</name>
  <files>supabase/functions/submit-candidatura/index.test.ts</files>
  <read_first>
    - supabase/functions/submit-candidatura/index.test.ts — ler `withStubbedFetch` (~159-167), o uso em ~204, o comentário ~155-158 e `sanitizeOps: false` (~198)
    - o index.ts já editado na Task 1 (confirmar que não há mais fetch a stubar)
  </read_first>
  <action>
    Remover o helper `withStubbedFetch` e seu comentário explicativo (existiam SÓ para stubar o `fetch` do webhook n8n, agora inexistente). No caso de teste que usava `await withStubbedFetch(() => handler(makeRequest(VALID_BODY), deps))`, chamar `handler(...)` diretamente (sem o wrapper). Reavaliar `sanitizeOps: false`: se o teste passar verde com o default (não há mais promise fire-and-forget não-aguardada), remover o override e a nota "fire-and-forget webhook promise is intentionally not awaited"; se o runner acusar algum outro recurso pendente, manter o override mas reescrever a justificativa para o motivo real.

    Rodar o deno test da função e exigir verde. NÃO enfraquecer nenhuma asserção de comportamento existente (a candidatura ainda é registrada; a resposta de sucesso é a mesma). Não adicionar dependência nova.
  </action>
  <verify>
    <automated>f=supabase/functions/submit-candidatura/index.test.ts; ! grep -q "withStubbedFetch" "$f" && [ "$(grep -ci 'n8n' "$f")" = "0" ] && (command -v deno >/dev/null 2>&1 && deno test --allow-env --allow-read supabase/functions/submit-candidatura/index.test.ts || echo "deno ausente — rodar o deno test no gate do orquestrador; grep-gate passou"); echo "T2 grep OK"</automated>
  </verify>
  <acceptance_criteria>
    - `withStubbedFetch` não existe mais em index.test.ts e não há referência a `n8n` (`grep -ci n8n` = 0).
    - O caso de teste do happy-path chama `handler(...)` diretamente e passa; nenhuma asserção de comportamento foi removida ou afrouxada.
    - `sanitizeOps` está default (override removido) OU, se mantido, com justificativa atualizada que NÃO cita o webhook n8n.
    - `deno test` da função passa verde (se `deno` indisponível no ambiente do executor, o grep-gate passa e o `deno test` fica registrado para o gate do orquestrador — mas o alvo é verde local).
  </acceptance_criteria>
  <done>O test acompanha o código: sem stub de fetch morto, ainda provando que a candidatura é registrada e a resposta de sucesso preservada — a suíte reflete a EF sem n8n.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EF `submit-candidatura` deployada → n8n cloud | O `fetch` hardcoded é a superfície LIVE; permanece viva no artefato deployado até o redeploy (39-04) |
| Nova inscrição → dispatch de confirmação | Deve haver UMA fonte (o trigger); duas = double-send ao candidato |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02-01 | Tampering/Repudiation | Double-send: `fetch` n8n LIVE + `trg_notif_confirmacao` novo disparando ambos numa inscrição | mitigate | Remoção do bloco `fetch` no código (esta é a edição) + ordem de execute redeploy-antes-do-apply no 39-04 (Pitfall 3); `UNIQUE(dedupe_key)` como cinto durável |
| T-39-02-02 | Information Disclosure | URL n8n hardcoded no código (config ≠ privado; era enumerável/forjável) | mitigate | Remoção da const + fallback hardcoded; grep-guard `grep -ci n8n` = 0 |
| T-39-02-03 | Denial of Service | Regressão na EF: remover o bloco quebra o caminho de sucesso (candidatura não registrada) | mitigate | Task 1 preserva integralmente o passo de sucesso; Task 2 prova por `deno test` verde |
| T-39-02-SC | Tampering | Instalação de pacote via package-manager | accept | Zero pacotes novos (edição de remoção + limpeza de teste); N/A |
</threat_model>

<verification>
- `grep -ci n8n` em index.ts = 0 e em index.test.ts = 0
- Nenhuma referência a `N8N_NOVA_CANDIDATURA_URL` / `fernandocosta.app.n8n.cloud`
- `return jsonResponse` de sucesso preserva candidaturaId/status/etapa_atual
- `deno test` de submit-candidatura verde (sem stub de fetch vestigial)
- Zero deploy / zero contato com PROD neste plano
</verification>

<success_criteria>
1. O único disparo n8n LIVE do funil foi removido do código da EF `submit-candidatura`.
2. Nenhuma referência a n8n permanece na EF nem no seu teste.
3. O deno test passa verde, provando que a inscrição ainda é registrada e a resposta de sucesso preservada.
4. A EF está pronta para o redeploy GATED (39-04), que precede o apply da migration.
</success_criteria>

<output>
Create `.planning/phases/39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03/39-02-SUMMARY.md` when done
</output>
