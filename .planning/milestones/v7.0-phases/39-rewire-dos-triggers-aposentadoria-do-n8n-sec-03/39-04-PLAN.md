---
phase: 39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03
plan: 04
type: execute
wave: 2
depends_on: ["39-01", "39-02", "39-03"]
files_modified:
  - supabase/functions/submit-candidatura/index.ts (REDEPLOY via Supabase — arquivo não é editado aqui)
  - supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql (aplicada via MCP apply_migration — arquivo não é editado aqui)
  - supabase_migrations.schema_migrations (reconcile via MCP execute_sql)
autonomous: false
requirements: [DISPATCH-01, DISPATCH-02, DISPATCH-03, DISPATCH-04]

must_haves:
  truths:
    - "O apply da migration em PROD SÓ aconteceu DEPOIS de: (a) a EF notificar-candidato viva e smoke-testada (UAT-38-1) e resend_api_key no Vault (UAT-36-2); (b) o diff-before-drop (DBMIG-02) confirmar que os 4 corpos vivos trg_n8n_* são graceful-skip; (c) o redeploy de submit-candidatura SEM o bloco n8n"
    - "A ordem redeploy-antes-do-apply foi respeitada (Pitfall 3) — a janela resultante é confirmação-ausente por segundos, nunca double-send"
    - "Pós-apply o catálogo PROD tem 0 triggers/funções trg_n8n_* e exatamente 3 triggers trg_notif_*; o ledger schema_migrations tem a version 20260726000001 reconciliada; o smoke p39 roda verde via MCP"
    - "Um ciclo end-to-end por evento via *@resend.dev grava a linha certa em notificacoes_enviadas e net._http_response mostra 2xx (não 404/401); a decisão dispara só sob RH real (auto_rejeitado=false)"
  artifacts:
    - path: ".planning/phases/39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03/39-04-SUMMARY.md"
      provides: "Transcrição do gate, do diff-before-drop, do redeploy, do apply+reconcile, do smoke e do end-to-end por evento"
      contains: "PASS"
  key_links:
    - from: "supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql"
      to: "supabase_migrations.schema_migrations"
      via: "MCP apply_migration + reconcile da version para 20260726000001"
      pattern: "20260726000001"
    - from: "trg_notif_* (PROD)"
      to: "EF notificar-candidato → net._http_response"
      via: "net.http_post disparado por INSERT descartável por evento; inspeção de status_code 2xx"
      pattern: "net._http_response"
---

<objective>
Aterrissar o rewire em PROD, na ordem correta e só depois do gate: provar as pré-condições, diffar os corpos vivos antes de dropar, redeployar `submit-candidatura` sem n8n, aplicar a migration via MCP + reconciliar o ledger, e provar o resultado por catálogo + smoke + um ciclo end-to-end por evento.

Purpose: a ordem é carga estrutural, não conveniência. Um trigger que aponta para uma EF ausente dispara num 404 SILENCIOSO permanente (`net.http_post` é at-most-once, `net._http_response` retém 6h) — por isso o apply é GATED na EF viva. Aplicar a migration ANTES de redeployar `submit-candidatura` abre a janela de double-send (Pitfall 3). Dropar sem diffar os corpos vivos é dropar às cegas (DBMIG-02).
Output: migration viva em PROD, n8n aposentado do catálogo + do código deployado, ledger reconciliado, smoke verde, 1 e-mail correto por evento a `*@resend.dev`. Transcrito no SUMMARY.

⚠ **Plano de checkpoint puro, `autonomous: false`.** Subagentes GSD NÃO recebem os tools MCP do Supabase (bug upstream anthropics/claude-code#13898). O executor NÃO tenta rodar SQL, `apply_migration` nem `supabase functions deploy`. TODAS as tarefas são do orquestrador/main thread, com o SQL/comandos exatos prontos.

⚠ **BLOCKER EXPLÍCITO — este plano NÃO PODE começar até:**
  1. **UAT-38-1** — EF `notificar-candidato` DEPLOYADA e smoke-testada (`net.http_post` manual → e-mail a `*@resend.dev` → linha `enviado` no ledger). Verificado ao vivo 2026-07-26: a EF NÃO está na lista de functions deployadas.
  2. **UAT-36-2** — `resend_api_key` presente no Vault. Verificado ao vivo 2026-07-26: `vault.secrets` tem só `edge_invoke_key` + `project_url` (SEM `resend_api_key`).
  Sem os dois, os triggers apontariam para uma EF que grava `falhou` / dispara 404 — perda permanente at-most-once. Fernando limpa o gate em paralelo; o orquestrador RE-VERIFICA ao vivo antes de qualquer apply.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03/39-CONTEXT.md
@.planning/phases/39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03/39-RESEARCH.md
@.planning/phases/38-ef-notificar-candidato-comm/38-HUMAN-UAT.md
@supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql
@supabase/tests/p39_rewire_triggers_smoke.sql
@supabase/functions/submit-candidatura/index.ts

<interfaces>
<!-- Procedimento estabelecido no repo (P37-04 / P36-04). Não re-derivar. -->

Projeto Supabase (PROD): isljnozzlvckrgjjbjwp
Migration a aplicar: supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql

Apply (CLAUDE.md § Migrations + precedente P37-04):
  MCP `apply_migration` com o conteúdo do arquivo — NUNCA `supabase db push --linked` (SQLSTATE 42601
  em corpos `$$` no transaction pooler). `apply_migration` grava uma `version` de TIMESTAMP ≠ prefixo
  do arquivo ⇒ RECONCILIAR `supabase_migrations.schema_migrations` para `20260726000001`, e confirmar
  com `supabase db push --linked` reportando "Remote database is up to date".

Redeploy (D-09, ordem OBRIGATÓRIA antes do apply — Pitfall 3):
  `supabase functions deploy submit-candidatura` (com o index.ts já editado por 39-02). A janela
  entre o redeploy e o apply é confirmação-AUSENTE por segundos (perda at-most-once tolerável),
  MUITO preferível a double-send. Aplicar em janela de baixo tráfego.

Diff-before-drop (DBMIG-02, D-08): antes do apply, `pg_get_functiondef` das 4 funções vivas
  (trg_n8n_nova_candidatura, trg_n8n_status_candidatura, trg_n8n_revisao_decisao, trg_n8n_novo_candidato)
  confirmando que cada corpo é graceful-skip (lê n8n_webhook_base, RETURN NEW se NULL) — nunca dropar às cegas.

Ordem OBRIGATÓRIA das tasks: Task 1 (gate + diff + redeploy) → Task 2 (apply + reconcile + catálogo + smoke)
  → Task 3 (end-to-end + cleanup n8n cloud). Inverter Task 1↔Task 2 abre a janela de double-send.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Gate ao vivo (UAT-38-1 + UAT-36-2) + diff-before-drop (DBMIG-02) + redeploy de submit-candidatura</name>
  <files>supabase/functions/submit-candidatura/index.ts (redeployado, não editado)</files>
  <read_first>
    - .planning/phases/38-ef-notificar-candidato-comm/38-HUMAN-UAT.md — UAT-38-1 (smoke da EF) + o shape do net.http_post (passo 3); UAT-36-2 (resend_api_key no Vault)
    - .planning/phases/39-.../39-CONTEXT.md § D-08 (DBMIG-02 diff-before-drop) + D-09 (redeploy) + § "Vault (secrets vivos)"
    - .planning/phases/39-.../39-RESEARCH.md § "Pitfall 1" (404 silencioso — o porquê do gate) + "Pitfall 3" (redeploy-antes-do-apply) + "Runtime State Inventory"
    - supabase/functions/submit-candidatura/index.ts — confirmar (grep) que o bloco n8n já foi removido (39-02) ANTES de redeployar
  </read_first>
  <what-built>
    39-01 escreveu a migration; 39-02 removeu o bloco n8n de submit-candidatura; 39-03 escreveu o smoke. Nada tocou PROD. Este checkpoint é o gate que decide se a fase pode aterrissar — e prepara o terreno (diff + redeploy) sem ainda aplicar a migration.
  </what-built>
  <action>
    (1) GATE ao vivo — re-verificar via MCP, NÃO confiar no registro: `SELECT name FROM vault.secrets ORDER BY name;` deve conter `resend_api_key` (UAT-36-2); a EF `notificar-candidato` deve aparecer na lista de functions deployadas e ter um smoke `enviado` verde (UAT-38-1). Se QUALQUER um faltar, PARAR — a fase permanece GATED; registrar no SUMMARY qual gate está aberto e devolver a Fernando. Nenhum drop/redeploy/apply acontece com o gate aberto.

    (2) DIFF-BEFORE-DROP (DBMIG-02) — para cada uma das 4 funções a dropar, rodar `pg_get_functiondef` e confirmar que o corpo vivo é graceful-skip (lê `n8n_webhook_base`, `RETURN NEW` se NULL; nunca escreve tabela do funil). Transcrever no SUMMARY. Se algum corpo vivo divergir do esperado (ex.: já não é graceful-skip, ou faz mais que POST), PARAR e reportar — não dropar às cegas.

    (3) REDEPLOY (ordem obrigatória — ANTES do apply da Task 2): confirmar por grep que `submit-candidatura/index.ts` está sem n8n (39-02), então `supabase functions deploy submit-candidatura`. Após o redeploy, o único caminho de confirmação até o apply é AUSENTE (nem n8n nem trigger novo) — janela de segundos, at-most-once; aplicar em baixo tráfego. Registrar o timestamp do redeploy no SUMMARY.

    NÃO aplicar a migration nesta task. NÃO dropar nada manualmente (o DROP é da migration atômica na Task 2).
  </action>
  <sql-para-colar>
```sql
-- (1) GATE — re-verificar ao vivo (read-only)
select name from vault.secrets order by name;   -- exige 'resend_api_key' presente
-- (a lista de EFs deployadas + o smoke enviado da P38 são checados via MCP list functions / ledger)

-- (2) DIFF-BEFORE-DROP — os 4 corpos vivos (confirmar graceful-skip antes do DROP da Task 2)
select pg_get_functiondef('public.trg_n8n_nova_candidatura()'::regprocedure);
select pg_get_functiondef('public.trg_n8n_status_candidatura()'::regprocedure);
select pg_get_functiondef('public.trg_n8n_revisao_decisao()'::regprocedure);
select pg_get_functiondef('public.trg_n8n_novo_candidato()'::regprocedure);

-- (2b) inventário dos triggers vivos ANTES do apply (baseline p/ comparar na Task 2)
select tgname, tgrelid::regclass::text as tabela
  from pg_trigger where not tgisinternal
   and (tgname like 'trg_n8n_%' or tgname like 'trg_notif_%')
 order by tabela, tgname;
```
  </sql-para-colar>
  <how-to-verify>
    1. `vault.secrets` contém `resend_api_key`; a EF `notificar-candidato` está deployada e tem smoke `enviado` (UAT-38-1). Se não, PARAR (gate aberto).
    2. Os 4 `pg_get_functiondef` mostram corpos graceful-skip (lê `n8n_webhook_base`, RETURN NEW se NULL); transcritos no SUMMARY.
    3. `grep -ci n8n supabase/functions/submit-candidatura/index.ts` = 0 ANTES do redeploy.
    4. `supabase functions deploy submit-candidatura` retornou sucesso; timestamp registrado.
    5. O baseline (2b) mostra os 4 `trg_n8n_*` ainda vivos e 0 `trg_notif_*` (ainda não aplicados).
  </how-to-verify>
  <verify>
    <automated>grep -q "20260726000001_p39_rewire_triggers_aposenta_n8n" <(ls supabase/migrations) && [ "$(grep -ci 'n8n' supabase/functions/submit-candidatura/index.ts)" = "0" ] && echo "pré-condições de arquivo OK (migration existe, submit sem n8n) — apply/redeploy são do orquestrador via MCP/deploy"</automated>
  </verify>
  <acceptance_criteria>
    - Gate ao vivo confirmado: `resend_api_key` no Vault (UAT-36-2) E EF `notificar-candidato` deployada + smoke `enviado` (UAT-38-1) — ambos transcritos. Se aberto, a fase parou aqui sem tocar PROD.
    - Os 4 corpos `trg_n8n_*` foram diffados (`pg_get_functiondef`) e confirmados graceful-skip; transcritos no SUMMARY.
    - `submit-candidatura/index.ts` sem n8n (grep=0) e `supabase functions deploy submit-candidatura` bem-sucedido, com timestamp — ANTES de qualquer apply.
    - Nenhuma migration aplicada nesta task; catálogo ainda tem os 4 `trg_n8n_*` e 0 `trg_notif_*`.
  </acceptance_criteria>
  <resume-signal>Responder "aprovado" quando o gate estiver verde ao vivo, os 4 corpos diffados e o redeploy feito — ou colar qual gate está aberto / qual corpo divergiu.</resume-signal>
  <done>O gate está provado ao vivo (EF viva + secret), os corpos a dropar foram auditados, e a EF sem n8n está deployada — o terreno está pronto para o apply atômico, na ordem que impede o double-send.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Apply da migration via MCP + reconcile do ledger + verificação de catálogo + smoke p39 verde</name>
  <files>supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql (aplicada), supabase_migrations.schema_migrations (reconciliada), supabase/tests/p39_rewire_triggers_smoke.sql (executado)</files>
  <read_first>
    - supabase/migrations/20260726000001_p39_rewire_triggers_aposenta_n8n.sql — o SQL exato a aplicar
    - CLAUDE.md § "Migrations + db push — workaround" — por que MCP `apply_migration` e nunca `db push --linked`
    - .planning/phases/37-.../37-04-PLAN.md (Task 2) — o precedente literal do apply + reconcile do ledger desta base de código
    - supabase/tests/p39_rewire_triggers_smoke.sql — a contagem de PASS declarada no cabeçalho (o gate)
  </read_first>
  <what-built>
    A Task 1 provou o gate, diffou os corpos e redeployou a EF sem n8n. A migration atômica (DROP 4+4 n8n + CREATE 3 trg_notif_*) está escrita e revisada. Nada dela está vivo ainda.
  </what-built>
  <action>
    (1) APPLY — aplicar o conteúdo de `20260726000001_p39_rewire_triggers_aposenta_n8n.sql` via MCP `apply_migration` (chamada privilegiada — pedir autorização do operador no momento). NÃO usar `supabase db push --linked`.

    (2) RECONCILE — `apply_migration` grava uma version de timestamp ≠ prefixo do arquivo; via MCP `execute_sql`, alinhar a linha para `20260726000001` (bloco `<sql-para-colar>`), depois confirmar com `supabase db push --linked` reportando remoto atualizado.

    (3) CATÁLOGO — provar a nova topologia: 0 `trg_n8n_*` (trigger E função), exatamente 3 `trg_notif_*` triggers nas tabelas certas. Transcrever.

    (4) SMOKE — executar `supabase/tests/p39_rewire_triggers_smoke.sql` via MCP `execute_sql` e exigir a contagem de PASS declarada no cabeçalho (a metade estrutural + catálogo roda segura; a metade comportamental faz SKIP-com-NOTICE porque o secret AGORA está setado — registrar quais asserções passaram vs SKIP). Transcrever TODOS os NOTICE.

    GATE DE PARADA: se o apply falhar (42601 etc.), a version não reconciliar, o catálogo mostrar algum `trg_n8n_*` remanescente OU o smoke não bater a contagem, PARAR e reportar sem "rodar de novo até passar".
  </action>
  <sql-para-colar>
```sql
-- (2) Reconcile do ledger — LOGO após o apply_migration
select version, name from supabase_migrations.schema_migrations order by version desc limit 5;
update supabase_migrations.schema_migrations
   set version = '20260726000001',
       name    = '20260726000001_p39_rewire_triggers_aposenta_n8n'
 where version = '<VERSION_GRAVADA>';   -- substituir pela version de timestamp do apply
select version, name from supabase_migrations.schema_migrations
 where version = '20260726000001';

-- (3) Verificação de catálogo — a prova do rewire (não o build)
select count(*) as n8n_triggers from pg_trigger where tgname like 'trg_n8n_%';           -- 0
select count(*) as n8n_funcs from pg_proc p join pg_namespace n on n.oid=p.pronamespace
 where n.nspname='public' and p.proname like 'trg_n8n_%';                                 -- 0
select tgname, tgrelid::regclass::text as tabela
  from pg_trigger where not tgisinternal and tgname like 'trg_notif_%'
 order by tabela, tgname;   -- 3 linhas: trg_notif_confirmacao(candidaturas),
                            -- trg_notif_convite(agendamentos_entrevista), trg_notif_transicao(historico_candidatura)

-- (4) Confiança de que avancar_etapa/trg_candidaturas_analise/notify_cost_anomaly seguem intactos
select tgname, tgrelid::regclass::text as tabela from pg_trigger
 where tgname in ('candidaturas_avancar_etapa_trg','trg_candidaturas_analise') order by tgname;
```
  </sql-para-colar>
  <how-to-verify>
    1. `apply_migration` retornou sucesso (transcrever).
    2. Reconcile: a version `20260726000001` existe com o `name` do arquivo; `supabase db push --linked` reporta remoto atualizado.
    3. Catálogo: `n8n_triggers` = 0, `n8n_funcs` = 0; a query de `trg_notif_*` devolve EXATAMENTE 3 linhas nas tabelas certas.
    4. `avancar_etapa`/`trg_candidaturas_analise` seguem vivos (não foram tocados).
    5. Smoke p39 via MCP: a contagem de PASS bate o cabeçalho; NOTICE transcritos (comportamentais em SKIP porque o secret está setado — documentado).
  </how-to-verify>
  <verify>
    <automated>npm run build && npm run lint; echo "--- build/lint acima NÃO provam o rewire (tipos vêm do config, não do banco vivo) — a prova é o catálogo + smoke via MCP acima; baseline tsc ~97, teto CI 104 ---"</automated>
  </verify>
  <acceptance_criteria>
    - `20260726000001_p39_rewire_triggers_aposenta_n8n.sql` aplicada via MCP `apply_migration`, com confirmação transcrita.
    - `supabase_migrations.schema_migrations` tem a version `20260726000001` com o `name` do arquivo; `supabase db push --linked` reporta remoto atualizado (zero drift novo).
    - Catálogo: 0 triggers `trg_n8n_*`, 0 funções `trg_n8n_*`, exatamente 3 triggers `trg_notif_*` (confirmacao/convite/transicao) nas tabelas candidaturas/agendamentos_entrevista/historico_candidatura.
    - `candidaturas_avancar_etapa_trg` e `trg_candidaturas_analise` continuam vivos (D-12 preservado).
    - Smoke p39 rodou via MCP com a contagem de PASS do cabeçalho; NOTICE transcritos; comportamentais em SKIP (secret setado) documentado.
    - `npm run build`/`npm run lint` (baseline ~97 tsc, teto 104) verdes — mas o SUMMARY declara que estes NÃO provam o rewire.
  </acceptance_criteria>
  <resume-signal>Responder "aprovado" quando o apply, o reconcile, o catálogo (0 n8n / 3 notif) e o smoke estiverem verdes e transcritos — ou colar o que falhou.</resume-signal>
  <done>O rewire está vivo em PROD e provado por catálogo + smoke: o n8n está aposentado do banco, os 3 triggers canônicos existem, o ledger tem a version reconciliada, e os triggers ortogonais do funil seguem intactos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ciclo end-to-end por evento (*@resend.dev) + net._http_response + cleanup do n8n cloud</name>
  <files>.planning/phases/39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03/39-04-SUMMARY.md (transcrição)</files>
  <read_first>
    - .planning/phases/38-ef-notificar-candidato-comm/38-HUMAN-UAT.md — o shape do net.http_post + como o modo teste redireciona a `*@resend.dev`
    - .planning/phases/39-.../39-RESEARCH.md § "Diagnóstico de falha silenciosa" (net._http_response) + § "O que provar" invariante 7 (decisão só humana) + § "Open Questions" 1 (cleanup n8n cloud)
    - .planning/STATE.md § Blockers/Concerns — contas de teste PROD (candidato.funil@teste.com, recrutador real)
  </read_first>
  <what-built>
    O rewire está vivo (Task 2). Falta a prova ponta-a-ponta que só a EF viva permite: cada evento real do funil dispara EXATAMENTE 1 e-mail correto, sem duplicata, e a decisão só sob RH real — e fechar a superfície externa do n8n.
  </what-built>
  <action>
    (1) END-TO-END por evento (modo teste, destinos `*@resend.dev`) — usando as contas de teste PROD, exercitar cada uma das 4 fontes e provar 1 dispatch correto:
      · confirmação — inserir/registrar uma candidatura SURVIVOR real → `trg_notif_confirmacao` → 1 linha `enviado` em `notificacoes_enviadas` (evento='confirmacao'); e uma candidatura KNOCKOUT → 0 linha de confirmação (survivor-guard, invariante 5).
      · avanço — mover a candidatura para `avaliacao_assincrona` → `trg_notif_transicao` → 1 linha `avanco`.
      · decisão — registrar uma decisão como RH REAL (JWT do recrutador, via a RPC de decisão) → `auto_rejeitado=false` → 1 linha `decisao`; confirmar que um write de sistema/service_role NÃO dispararia (invariante 7 — a prova é o RH real produzir a linha; documentar).
      · convite — inserir um agendamento → `trg_notif_convite` → 1 linha `convite` com `agendamento_id`.
    Para cada um: inspecionar `net._http_response` (status_code 2xx, não 404/401) e a linha correspondente do ledger. Confirmar idempotência: re-disparar o MESMO evento não cria 2ª linha (colapsa no `UNIQUE(dedupe_key)`).

    (2) CLEANUP do n8n cloud (DISPATCH-03, fechar a superfície externa — Open Q1): checkpoint HUMANO — Fernando desativa/apaga a(s) workflow(s) na instância `fernandocosta.app.n8n.cloud` (fora do repo, ação de painel). Opcional: remover o secret morto `n8n_webhook_base` do Vault (faxina; não bloqueia). Registrar o que foi feito.

    (3) Registrar no SUMMARY: por evento, a linha do ledger + o status_code do net._http_response; a confirmação do knockout-suprimido; a idempotência; e o estado do cleanup n8n. NUNCA transcrever e-mail real de candidato — usar contagens/ids/status.

    Cleanup das linhas descartáveis criadas no end-to-end (se aplicável), confirmando que o ledger não fica poluído.
  </action>
  <sql-para-colar>
```sql
-- Diagnóstico do dispatch após cada evento (2xx = ok; >=400 ou error_msg = falha silenciosa)
select id, status_code, error_msg, timed_out, created
  from net._http_response
 where created > now() - interval '15 minutes'
 order by created desc;

-- Ledger: 1 linha por evento lógico, status enviado, sem duplicata
select evento, status, dedupe_key, provider_message_id, criado_em
  from public.notificacoes_enviadas
 where criado_em > now() - interval '30 minutes'
 order by criado_em desc;

-- Confirmar knockout suprimido: nenhuma linha 'confirmacao' para a candidatura knockout de teste
-- (substituir <candidatura_knockout_id>)
select count(*) as confirmacoes_knockout
  from public.notificacoes_enviadas
 where evento = 'confirmacao' and candidatura_id = '<candidatura_knockout_id>';   -- 0
```
  </sql-para-colar>
  <how-to-verify>
    1. Para cada um dos 4 eventos: 1 linha `enviado` no ledger com o `evento` certo e `net._http_response` 2xx.
    2. Knockout: 0 linha de confirmação (survivor-guard vivo).
    3. Decisão: a linha `decisao` só aparece com RH real (auto_rejeitado=false); documentado.
    4. Convite: a linha carrega `agendamento_id` no `dedupe_key`.
    5. Idempotência: re-disparo do mesmo evento não cria 2ª linha.
    6. n8n cloud: workflow(s) desativada(s)/apagada(s) por Fernando; estado registrado.
    7. SUMMARY sem nenhum e-mail real de candidato.
  </how-to-verify>
  <verify>
    <automated>echo "Verificação end-to-end é UAT humano via MCP/painel (net._http_response + ledger + n8n cloud) — não automatizável por subagente; gate é a transcrição no 39-04-SUMMARY"</automated>
  </verify>
  <acceptance_criteria>
    - Os 4 eventos (confirmação/avanço/decisão/convite) produziram EXATAMENTE 1 linha `enviado` cada em `notificacoes_enviadas`, com `net._http_response` 2xx (não 404/401) — transcrito.
    - Knockout suprimido: 0 linha de confirmação para a candidatura knockout de teste (invariante 5).
    - Decisão disparada só sob RH real (auto_rejeitado=false); a invariante 7 (nenhum veredito automático) está documentada.
    - Convite carrega `agendamento_id` no `dedupe_key`; re-disparo do mesmo evento não duplica linha (idempotência via UNIQUE(dedupe_key)).
    - Cleanup do n8n cloud registrado (workflow(s) desativada(s); `n8n_webhook_base` removido do Vault se Fernando optou) — a superfície externa do n8n está fechada.
    - O ledger não ficou poluído por fixture de teste; o SUMMARY não contém e-mail real de candidato.
  </acceptance_criteria>
  <resume-signal>Responder "aprovado" quando os 4 eventos dispararem 1 e-mail correto cada (2xx no ledger), o knockout for suprimido, a decisão só sob RH real e o n8n cloud fechado — ou colar o evento que falhou.</resume-signal>
  <done>O pipeline está provado ponta-a-ponta em PROD: cada evento do funil auto-dispara exatamente 1 e-mail correto, o knockout é suprimido, a decisão é só humana, e o n8n pessoal está totalmente aposentado (catálogo + código + cloud). SEC-03 resolvido por substituição.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orquestrador (MCP, privilégio total) → PROD | Único caminho de escrita desta fase; opera fora do `db push` normal |
| Gate (EF viva + secret) → apply | Aplicar sem a EF viva = 404 silencioso permanente (at-most-once) |
| Redeploy ↔ apply (fronteira não-transacional) | A ordem redeploy-antes-do-apply é o que impede o double-send |
| Repositório (declarado) → PROD (real) | O reconcile do ledger impede criar um drift NOVO (a causa do drift da P37 segue desconhecida) |
| Fixture end-to-end → candidato real | Modo teste + `*@resend.dev` impede spam a candidato real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04-01 | Repudiation | Apply antes da EF viva → 404 silencioso permanente (e-mail perdido, funil avança) | mitigate | GATE ao vivo na Task 1 (re-verifica UAT-38-1 + UAT-36-2 via MCP, não confia no registro); PARA se aberto |
| T-39-04-02 | Tampering/Repudiation | Double-send: apply da migration antes do redeploy de submit-candidatura | mitigate | Ordem obrigatória Task 1 (redeploy) → Task 2 (apply); janela resultante é confirmação-ausente, não dupla (Pitfall 3) |
| T-39-04-03 | Tampering | Dropar às cegas um corpo n8n que já não é graceful-skip | mitigate | DBMIG-02 diff-before-drop (`pg_get_functiondef` dos 4) transcrito antes do apply; PARA se divergir |
| T-39-04-04 | Repudiation | `apply_migration` grava version ≠ prefixo → drift novo enquanto se rewire | mitigate | Reconcile explícito para `20260726000001` + confirmação `db push --linked`; acceptance exige a version coerente |
| T-39-04-05 | Denial of Service | `db push --linked` sobre corpo `$$` → 42601 e estado parcial | mitigate | `apply_migration` é o único caminho de apply; `db push` só em modo conferência pós-reconcile |
| T-39-04-06 | Information Disclosure | Transcrever e-mail real de candidato no SUMMARY | mitigate | Acceptance proíbe e-mail real; usar contagens/ids/status; destinos `*@resend.dev` em modo teste |
| T-39-04-07 | Tampering | `trg_notif_*` fira dispatch real ao candidato durante a prova | mitigate | Modo teste redireciona a `*@resend.dev`; smoke comportamental faz SKIP com secret setado |
| T-39-04-SC | Tampering | Superfície externa do n8n cloud continua ativa após o rewire | mitigate | Cleanup humano da(s) workflow(s) n8n cloud (Task 3) + remoção opcional de `n8n_webhook_base` — fecha a aposentadoria de verdade |
</threat_model>

<verification>
- Gate ao vivo verde (resend_api_key no Vault + EF notificar-candidato deployada/smoked) ANTES de tudo
- 4 corpos trg_n8n_* diffados (graceful-skip) + submit-candidatura redeployado sem n8n, ANTES do apply
- Migration aplicada via MCP; ledger com version 20260726000001 reconciliada; db push sem drift
- Catálogo: 0 trg_n8n_* (trigger+função), 3 trg_notif_*; avancar_etapa/trg_candidaturas_analise intactos
- Smoke p39 verde via MCP (estrutural/catálogo PASS; comportamental SKIP com secret setado)
- End-to-end: 1 e-mail correto por evento a *@resend.dev (2xx no ledger); knockout suprimido; decisão só RH real; idempotência
- n8n cloud desativado (superfície externa fechada); SUMMARY sem e-mail real de candidato
</verification>

<success_criteria>
1. O apply só aterrissou depois do gate ao vivo, do diff-before-drop e do redeploy — na ordem que impede o 404 silencioso e o double-send.
2. O rewire está vivo e provado por catálogo + smoke: n8n aposentado do banco, 3 triggers canônicos, ledger reconciliado.
3. Cada evento do funil auto-dispara exatamente 1 e-mail correto (2xx), knockout suprimido, decisão só humana — provado end-to-end.
4. O n8n pessoal está totalmente aposentado (catálogo + código deployado + cloud) — SEC-03 resolvido por substituição.
</success_criteria>

<output>
Create `.planning/phases/39-rewire-dos-triggers-aposentadoria-do-n8n-sec-03/39-04-SUMMARY.md` when done
</output>
