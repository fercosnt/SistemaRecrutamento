---
phase: 41-reconcilia-o-de-entrega-retry-testing
plan: 05
type: execute
wave: 3
depends_on: ["41-02", "41-03", "41-04"]
files_modified:
  - supabase/migrations/20260727000001_p41_recon_retry.sql (aplicada via MCP apply_migration — arquivo não é editado aqui)
  - supabase_migrations.schema_migrations (reconcile via MCP execute_sql)
  - supabase/functions/resend-webhook/index.ts (DEPLOY via Supabase — arquivo não é editado aqui)
  - supabase/functions/notificar-candidato/index.ts (REDEPLOY via Supabase — arquivo não é editado aqui)
autonomous: false
requirements: [RECON-01, RECON-02, RECON-03]

must_haves:
  truths:
    - "A legitimidade de npm:svix@1.99.1 foi confirmada (npmjs.com/package/svix — lib oficial citada pela doc do Resend, sem postinstall) e o pacote aparece pinado no deno.lock após `deno check` — ANTES de qualquer deploy da EF de webhook"
    - "A migration 20260727000001 foi aplicada via MCP apply_migration (NÃO db push — 42601) + o ledger schema_migrations foi reconciliado para o prefixo do arquivo; o smoke p41 rodou verde via MCP (5/5, gate-GUC)"
    - "A EF resend-webhook foi deployada com verify_jwt=false e a EF notificar-candidato foi RE-deployada (após o refactor + branch retry do 41-01/41-04); a postura verify_jwt foi verificada ao vivo"
    - "O endpoint do webhook foi registrado no dashboard do Resend apontando para a URL da resend-webhook deployada, o signing secret whsec_ foi copiado e provisionado no Vault como resend_webhook_secret (ação humana do Fernando), e ler_resend_webhook_secret() devolve não-nulo"
    - "O cron job notif-retry-sweep está ativo em cron.job com schedule */15 * * * * e a varredura re-invoca a EF com Bearer edge_invoke_key (não service_role — Pitfall 5); o UAT ao vivo delivered@/bounced@/complained@resend.dev fica DEFERIDO atrás de DELIV-01, NÃO bloqueia o fecho da fase"
  artifacts:
    - path: ".planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-05-SUMMARY.md"
      provides: "Transcrição do gate de legitimidade, apply+reconcile, smoke, deploys, registro do webhook + provisionamento do secret, verificação do cron"
      contains: "PASS"
  key_links:
    - from: "supabase/migrations/20260727000001_p41_recon_retry.sql"
      to: "supabase_migrations.schema_migrations"
      via: "MCP apply_migration + reconcile da version para 20260727000001"
      pattern: "20260727000001"
    - from: "EF resend-webhook (deployada)"
      to: "vault (resend_webhook_secret)"
      via: "registro no dashboard Resend gera whsec_ → provisionado no Vault → ler_resend_webhook_secret() não-nulo"
      pattern: "resend_webhook_secret"
    - from: "cron.job (notif-retry-sweep)"
      to: "EF notificar-candidato (branch retry)"
      via: "net.http_post Bearer edge_invoke_key a cada 15 min"
      pattern: "notif-retry-sweep"
---

<objective>
Aterrissar a P41 em PROD, na ordem correta e só depois do gate: confirmar a legitimidade de `npm:svix`, aplicar a migration via MCP + reconciliar o ledger + rodar o smoke, deployar a EF `resend-webhook` + re-deployar `notificar-candidato`, e conduzir a ação humana do Fernando (registrar o endpoint no dashboard do Resend → copiar o whsec → provisionar no Vault). Verificar o cron ativo e a state machine viva.

Purpose: a ordem é carga estrutural. A EF de webhook não pode verificar assinatura sem o `resend_webhook_secret` no Vault, e o secret só existe depois que o endpoint é registrado no dashboard (que precisa da URL da EF deployada). A varredura só sai de `pendente`/`falhou` se o Bearer for `edge_invoke_key` (não service_role — invariante quebrada por rotação, Pitfall 5). O apply via `db push` quebra em 42601 nos corpos `$$` — só MCP `apply_migration` funciona.
Output: migration viva + ledger reconciliado + smoke verde, EF de webhook viva (verify_jwt=false) + notificar-candidato re-deployada, secret no Vault, cron `notif-retry-sweep` ativo. Transcrito no SUMMARY.

⚠ **Plano de checkpoint puro, `autonomous: false`.** Subagentes GSD NÃO recebem os tools MCP do Supabase (bug upstream anthropics/claude-code#13898). O executor NÃO tenta rodar SQL, `apply_migration`, `supabase functions deploy`, nem `cron.schedule`. TODAS as tarefas são do orquestrador/main thread, com o SQL/comandos exatos prontos.

⚠ **DEPENDE dos Planos 41-02 (EF de webhook escrita), 41-03 (migration + smoke escritos), 41-04 (branch retry na notificar-candidato).** Se algum não fechou, PARAR — não há o que deployar/aplicar.

⚠ **DELIV-01 ABERTO (aceito).** `rh.beautysmile.com.br` não está verificado no Resend → todo envio grava `falhou`. O UAT ao vivo (delivered@/bounced@/complained@resend.dev) fica DEFERIDO atrás de DELIV-01 e **NÃO é gate bloqueante desta fase** — a prova autônoma da fase é o CI mockado (41-01/02/04) + o smoke SQL (Task 1). A varredura é EXATAMENTE o mecanismo que recupera as linhas `falhou` acumuladas quando o Fernando verificar o domínio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-CONTEXT.md
@.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-RESEARCH.md
@.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-PATTERNS.md
@.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-02-SUMMARY.md
@.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-03-SUMMARY.md
@.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-04-SUMMARY.md

<interfaces>
<!-- Precedentes de PROD a seguir. -->

Apply via MCP + reconcile (precedente P37-04 / P39-04):
- `apply_migration` grava a version com timestamp próprio; o reconcile do ledger (`schema_migrations.version` → prefixo do arquivo `20260727000001`) é OBRIGATÓRIO após cada apply.
- NUNCA `supabase db push --linked` (42601 nos corpos `$$`).

Deploy de EF self-auth (precedente P38 / analise-candidato-individual):
- `supabase functions deploy resend-webhook --no-verify-jwt` (ou via MCP). Confirmar postura: curl sem assinatura → não deve escrever no ledger.
- Re-deploy: `supabase functions deploy notificar-candidato` (após o refactor). Confirmar Bearer self-auth (curl sem Bearer → 401).

Vault (precedente UAT-36-2):
- Provisionar `resend_webhook_secret` = ação humana (Fernando), literal, sem placeholder (ausência = NULL diagnosticável).
- `edge_invoke_key`/`project_url` já vivem no Vault. A EF `notificar-candidato` aceita o Bearer via `NOTIFICAR_SECRET` (== `edge_invoke_key`, setado na P38). A varredura DEVE usar o mesmo `edge_invoke_key` — Pitfall 5 (NÃO service_role, `823aa757…` ≠ `085073ec…`).

Smoke a rodar — supabase/tests/p41_recon_retry_smoke.sql (escrito no 41-03): via MCP execute_sql; gate-GUC 5/5.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Gate de legitimidade do npm:svix + apply da migration via MCP + reconcile + smoke</name>
  <files>supabase/migrations/20260727000001_p41_recon_retry.sql (aplicada via MCP — não editada), supabase_migrations.schema_migrations (reconcile via MCP)</files>
  <read_first>
    - .planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-RESEARCH.md § Package Legitimacy Audit (115-125) + Pitfall 7 (434-437)
    - supabase/migrations/20260727000001_p41_recon_retry.sql (a migration a aplicar)
    - supabase/tests/p41_recon_retry_smoke.sql (o smoke a rodar)
  </read_first>
  <what-built>
    A migration aditiva (bounce_em/reclamado_em + ler_resend_webhook_secret + varrer_retry_notificacoes + cron notif-retry-sweep) e o smoke gate-GUC foram escritos (41-03), sem contato com PROD.
  </what-built>
  <action>
    (1) LEGITIMIDADE `npm:svix@1.99.1` (T-41-SC — checkpoint NÃO auto-aprovável, `workflow.auto_advance` ignorado): confirmar em https://www.npmjs.com/package/svix que é a lib oficial da Svix (repo github.com/svix/svix-webhooks, sem `postinstall`), citada pela doc do Resend. Rodar `deno check` na EF `resend-webhook` e confirmar `svix` pinado no `deno.lock`. Se algo divergir do audit (versão/postinstall/proveniência), PARAR e reportar — nenhum deploy/apply acontece.
    (2) APPLY `20260727000001_p41_recon_retry.sql` via MCP `apply_migration` (NÃO `db push` — 42601). Sem wrapper BEGIN/COMMIT.
    (3) RECONCILE do ledger: `schema_migrations.version` → prefixo `20260727000001` (mirror P37-04/P39-04). Confirmar `supabase migration list --linked` alinhado (Local/Remote), zero pendência nova.
    (4) SMOKE `p41_recon_retry_smoke.sql` via MCP `execute_sql` — gate-GUC deve reportar RESUMO 5/5 (colunas novas, índices preservados, RPC revogada, def da varredura com predicado+edge_invoke_key+retry_id+split_part e sem service_role, cron job registrado). Run parcial reprova no RESUMO (z).
    O executor NÃO roda apply/execute_sql (subagente sem MCP) — estas etapas são do orquestrador; o executor só confere as pré-condições de arquivo.
  </action>
  <how-to-verify>
    1. `npm:svix@1.99.1` verificado legítimo (sem postinstall) + pinado no `deno.lock`.
    2. Migration aplicada via MCP; `schema_migrations` reconciliado ao prefixo `20260727000001`.
    3. `supabase migration list --linked` alinhado, zero pendência nova.
    4. Smoke p41 reporta RESUMO 5/5 (gate VERDE); `bounce_em`/`reclamado_em` existem; `notif-retry-sweep` em `cron.job`.
  </how-to-verify>
  <verify>
    <automated>test -f supabase/migrations/20260727000001_p41_recon_retry.sql && test -f supabase/tests/p41_recon_retry_smoke.sql && echo "pré-condições de arquivo OK — apply/reconcile/smoke são do orquestrador via MCP"</automated>
  </verify>
  <acceptance_criteria>
    - `npm:svix@1.99.1` verificado legítimo + pinado no `deno.lock`; nenhum postinstall.
    - Migration aplicada via MCP; `schema_migrations` tem a version reconciliada ao prefixo `20260727000001`; zero drift novo.
    - Smoke p41 reporta gate VERDE 5/5 (RESUMO z não levanta exceção).
    - `bounce_em`/`reclamado_em` existem em `notificacoes_enviadas`; `notif-retry-sweep` registrado em `cron.job` (`*/15 * * * *`).
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" com o RESUMO 5/5 do smoke + a version reconciliada, ou descreva a divergência.</resume-signal>
  <done>svix confirmado legítimo/pinado; migration viva + ledger reconciliado; smoke p41 verde 5/5 — as colunas, a RPC, a varredura e o cron existem em PROD.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy da EF resend-webhook + re-deploy da notificar-candidato + verificação de postura</name>
  <files>supabase/functions/resend-webhook/index.ts (deployada — não editada), supabase/functions/notificar-candidato/index.ts (redeployada — não editada)</files>
  <read_first>
    - supabase/functions/resend-webhook/index.ts (a EF a deployar)
    - supabase/functions/notificar-candidato/index.ts (a EF a re-deployar após 41-01/41-04)
    - supabase/config.toml (postura verify_jwt=false da resend-webhook)
    - .planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-RESEARCH.md Pitfall 2 (405-410) + Pitfall 5 (422-426)
  </read_first>
  <what-built>
    A EF `resend-webhook` (Svix verify + reconciliação, 41-02) e o branch retry de `notificar-candidato` (41-04) estão escritos e verdes em CI (suite Deno sem --allow-net).
  </what-built>
  <action>
    (1) DEPLOY `supabase functions deploy resend-webhook --no-verify-jwt` (ou via MCP). Confirmar que `npm:svix` resolveu no deploy (sem `ERR_MODULE_NOT_FOUND` — Pitfall 2) e que a EF sobe ACTIVE.
    (2) RE-DEPLOY `supabase functions deploy notificar-candidato` (traz o refactor + branch retry vivos). Confirmar ACTIVE, verify_jwt=false, e Bearer self-auth funcional (curl sem Bearer → 401; a EF NÃO é endpoint público de envio).
    (3) POSTURA resend-webhook: um POST sem headers Svix válidos deve responder 400 (assinatura inválida) e NÃO escrever no ledger — a EF é self-auth pela assinatura, não spoofable.
    (4) Anotar a URL pública da `resend-webhook` (`{project_url}/functions/v1/resend-webhook`) — insumo da Task 3.
    O executor NÃO roda `supabase functions deploy` nem curl contra PROD (subagente sem MCP) — é do orquestrador; o executor só confere que os arquivos das 2 EFs existem e a suite Deno está verde.
  </action>
  <how-to-verify>
    1. `resend-webhook` deployada ACTIVE, verify_jwt=false; `npm:svix` resolvido (sem ERR_MODULE_NOT_FOUND).
    2. `notificar-candidato` re-deployada ACTIVE; curl sem Bearer → 401.
    3. POST à resend-webhook sem assinatura Svix válida → 400, zero writes no ledger.
    4. URL pública da resend-webhook anotada.
  </how-to-verify>
  <verify>
    <automated>test -f supabase/functions/resend-webhook/index.ts && grep -A2 "\[functions.resend-webhook\]" supabase/config.toml | grep -q "verify_jwt = false" && echo "pré-condições de arquivo OK — deploy é do orquestrador"</automated>
  </verify>
  <acceptance_criteria>
    - `resend-webhook` deployada ACTIVE, verify_jwt=false; `npm:svix` resolvido no deploy (sem ERR_MODULE_NOT_FOUND).
    - `notificar-candidato` re-deployada ACTIVE; curl sem Bearer → 401 (self-auth intacta).
    - POST à resend-webhook sem assinatura Svix válida → 400, zero writes no ledger.
    - URL pública da resend-webhook anotada para o registro no dashboard.
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" com o status ACTIVE das 2 EFs + a URL da resend-webhook, ou descreva o erro de deploy.</resume-signal>
  <done>As 2 EFs estão vivas: resend-webhook (verify_jwt=false, svix resolvido, 400 sem assinatura) + notificar-candidato re-deployada (401 sem Bearer); URL anotada.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Registrar o webhook no dashboard do Resend + provisionar resend_webhook_secret no Vault + verificar o loop</name>
  <files>vault (resend_webhook_secret — ação humana), .planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-HUMAN-UAT.md (UAT deferido)</files>
  <read_first>
    - .planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-RESEARCH.md § Environment Availability (534-548) + Área 2 do CONTEXT (secret no Vault)
    - .planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-CONTEXT.md § Integration Points (95-102)
  </read_first>
  <what-built>
    A EF `resend-webhook` está viva com URL pública (Task 2). Falta a superfície EXTERNA (dashboard Resend) e o secret do Vault — ações que só o Fernando pode fazer (sem CLI/API para gerar o whsec do dashboard).
  </what-built>
  <action>
    (1) Fernando registra o endpoint em https://resend.com/webhooks apontando para `{project_url}/functions/v1/resend-webhook`, assinando os eventos `email.delivered` / `email.bounced` / `email.complained`.
    (2) Fernando copia o signing secret (`whsec_…`) que o dashboard exibe.
    (3) Provisionar no Vault como `resend_webhook_secret` (literal, sem placeholder — ausência = NULL diagnosticável, mirror UAT-36-2). Confirmar via MCP que `ler_resend_webhook_secret()` devolve não-nulo (sem imprimir o valor).
    (4) VERIFICAR O LOOP: confirmar que `cron.job` tem `notif-retry-sweep` ativo (`*/15 * * * *`) e que a varredura usa `edge_invoke_key` (não service_role — Pitfall 5). Opcional/seguro: `SELECT public.varrer_retry_notificacoes();` via MCP não deve levantar exceção (com o backlog `falhou` de DELIV-01, re-invoca a EF; a EF grava o resultado).
    (5) Registrar o UAT ao vivo como DEFERIDO (não-bloqueante): criar/atualizar `41-HUMAN-UAT.md` com o checklist delivered@/bounced@/complained@resend.dev a rodar QUANDO DELIV-01 fechar — a fase fecha sem ele.
    Provisionamento do Vault + registro no dashboard = ação humana do Fernando; a confirmação via MCP é do orquestrador. O executor NÃO tem acesso a nenhum dos dois.
  </action>
  <how-to-verify>
    1. Endpoint registrado no dashboard do Resend para os 3 eventos, apontando à URL da EF.
    2. `resend_webhook_secret` no Vault; `ler_resend_webhook_secret()` não-nulo (valor nunca exibido).
    3. `notif-retry-sweep` ativo em `cron.job`; a varredura usa `edge_invoke_key` (verificado no def).
    4. `41-HUMAN-UAT.md` registra o UAT ao vivo como deferido atrás de DELIV-01.
  </how-to-verify>
  <verify>
    <automated>grep -q "notif-retry-sweep" supabase/migrations/20260727000001_p41_recon_retry.sql && echo "cron declarado na migration — ativação/registro/secret são do orquestrador+Fernando"</automated>
  </verify>
  <acceptance_criteria>
    - Endpoint registrado no dashboard do Resend para os 3 eventos, apontando à URL da EF.
    - `resend_webhook_secret` provisionado no Vault; `ler_resend_webhook_secret()` não-nulo (valor nunca exibido).
    - `notif-retry-sweep` ativo em `cron.job`; a varredura usa `edge_invoke_key` (verificado no def).
    - `41-HUMAN-UAT.md` registra o UAT ao vivo como deferido atrás de DELIV-01 (não bloqueia o fecho).
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" com ler_resend_webhook_secret() não-nulo + cron ativo + UAT deferido registrado, ou descreva o bloqueio.</resume-signal>
  <done>Webhook registrado no dashboard, secret vivo no Vault, cron notif-retry-sweep ativo com edge_invoke_key, e o UAT ao vivo deferido atrás de DELIV-01 — o loop está fechado e a fase pode fechar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → deploy (svix) | Supply chain do único pacote novo da fase |
| dashboard Resend → Vault (whsec) | O secret de assinatura entra no sistema por ação humana; nunca em placeholder/log |
| MCP apply → PROD | Toda escrita em PROD passa por checkpoint do orquestrador (subagentes sem MCP) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-SC | Tampering | `npm:svix@1.99.1` (supply chain) | mitigate | Checkpoint de legitimidade NÃO auto-aprovável (npmjs.com/package/svix, sem postinstall, lib oficial citada pelo Resend) + pin no deno.lock ANTES do 1º deploy |
| T-41-18 | Information Disclosure | `resend_webhook_secret` exposto em placeholder/log | mitigate | Provisionado direto no Vault por ação humana, literal, sem placeholder; `ler_resend_webhook_secret()` nunca imprime o valor |
| T-41-19 | Spoofing | Endpoint de webhook público sem verificação | mitigate | `verify_jwt=false` + verificação Svix obrigatória; POST sem assinatura válida → 400, zero writes (verificado na Task 2) |
| T-41-20 | Elevation of Privilege / DoS | Varredura re-invoca a EF com Bearer errado (rotação) | mitigate | Verificar ao vivo que a varredura usa `edge_invoke_key` (== NOTIFICAR_SECRET), não service_role (Pitfall 5) |
| T-41-21 | Tampering | 42601 no apply de corpo `$$` | mitigate | Apply via MCP `apply_migration` (não db push) + reconcile — checkpoint do orquestrador |
</threat_model>

<verification>
- Migration viva em PROD (version 20260727000001 reconciliada); smoke p41 verde 5/5 via MCP.
- resend-webhook deployada (verify_jwt=false, svix resolvido) + notificar-candidato re-deployada (self-auth 401 sem Bearer).
- resend_webhook_secret no Vault (ler_resend_webhook_secret não-nulo); endpoint registrado no dashboard; cron notif-retry-sweep ativo com edge_invoke_key.
- UAT ao vivo delivered@/bounced@/complained@resend.dev DEFERIDO atrás de DELIV-01 (não bloqueia).
</verification>

<success_criteria>
- O loop fire-and-forget está fechado e vivo em PROD: webhook reconcilia por provider_message_id (RECON-02), a varredura pg_cron re-tenta pendente/falhou capado (RECON-03), a state machine completa (RECON-01).
- Prova autônoma = CI mockado (41-01/02/04) + smoke SQL (41-03); UAT ao vivo deferido atrás de DELIV-01, não bloqueante.
- Tudo via checkpoint do orquestrador (subagentes sem MCP).
</success_criteria>

<output>
Create `.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-05-SUMMARY.md` when done
</output>
