---
phase: 41-reconcilia-o-de-entrega-retry-testing
plan: 04
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - supabase/functions/notificar-candidato/index.ts
  - supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts
autonomous: true
requirements: [RECON-01, RECON-03]

must_haves:
  truths:
    - "Um body com `retry_id` presente entra no BRANCH RETRY: pula o claim-before-send (upsert ON CONFLICT) e re-tenta a linha EXISTENTE (SELECT por id) — resolvendo o problema de que um 2º disparo colapsa em skipped:duplicate porque o upsert ignoreDuplicates não retorna id"
    - "Guard de elegibilidade no branch retry: se a linha está ausente, ou status NOT IN ('pendente','falhou'), ou tentativas >= 5 → responde 200 { skipped: 'nao_elegivel' } sem enviar (respeita o cap 5)"
    - "No branch retry, as escritas de falha e de sucesso usam WHERE id = retry_id e INCREMENTAM tentativas (row.tentativas + 1); registrarFalha computa proxima_tentativa_em via computeProximaTentativa(novasTentativas) — no cap 5 fica null e a linha permanece falhou"
    - "O caminho NORMAL (sem retry_id) é preservado byte-a-byte: mesma self-auth, mesmo claim, mesmo render, mesma escrita de 2 fases; os testes pré-existentes continuam verdes"
    - "O fetch ao Resend inclui o header `Idempotency-Key` (dedupe_key ou retry_id) como cinto secundário de 24h (LEDGER-02) — um re-send dentro de 24h para a mesma chave é no-op no provedor"
  artifacts:
    - path: "supabase/functions/notificar-candidato/index.ts"
      provides: "branch retry gateado por retry_id + Idempotency-Key + escritas por id com incremento de tentativas"
      contains: "retry_id"
      min_lines: 320
    - path: "supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts"
      provides: "testes do branch retry (nao_elegivel, sucesso→enviado, non-2xx→falhou+backoff) via deps mockadas"
      contains: "retry_id"
  key_links:
    - from: "supabase/functions/notificar-candidato/index.ts"
      to: "notificacoes_enviadas (por id)"
      via: "branch retry: SELECT ... WHERE id = retry_id; UPDATE ... WHERE id = retry_id com tentativas incrementado"
      pattern: "id = retry_id|eq\\(\"id\", retry_id\\)"
    - from: "supabase/functions/notificar-candidato/index.ts"
      to: "helpers.computeProximaTentativa"
      via: "registrarFalha(novasTentativas) no branch retry usa row.tentativas + 1"
      pattern: "computeProximaTentativa"
    - from: "supabase/functions/notificar-candidato/index.ts"
      to: "https://api.resend.com/emails"
      via: "header Idempotency-Key no fetch (deps.fetchImpl)"
      pattern: "Idempotency-Key"
---

<objective>
Adicionar à EF `notificar-candidato` o **branch de retry** que a varredura `pg_cron` (Plano 41-03) aciona: um body com `retry_id` re-tenta uma linha `pendente`/`falhou` EXISTENTE em vez de colapsar em `skipped:duplicate`, incrementa `tentativas`, e recomputa `proxima_tentativa_em` pelo backoff — respeitando o cap 5. Fecha a rede de segurança do fire-and-forget (RECON-03) e completa as transições de status no retry (RECON-01).

Purpose: hoje um 2º disparo do mesmo evento colapsa em `skipped:duplicate` porque o `upsert onConflict dedupe_key ignoreDuplicates` não retorna id (index.ts:200-207). O retry precisa deliberadamente re-tentar a linha existente. A mudança é MÍNIMA e preserva o caminho normal byte-a-byte (RESEARCH Pattern 3).
Output: 2 arquivos modificados. **Zero contato com PROD** — só código + testes Deno. O redeploy da EF é o plano GATED 41-05.

⚠ **DEPENDE do Plano 41-01** — este plano assume que a EF JÁ foi refatorada para `handler(req, deps)` injetável e que `computeProximaTentativa` + `registrarFalha(novasTentativas)` já existem. Se 41-01 não fechou, PARAR: o branch de retry sem deps injetáveis não é mockável e o backoff seria re-hardcoded.

⚠ **SAFE-NOW.** Nenhuma tarefa faz deploy nem toca o banco.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-CONTEXT.md
@.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-RESEARCH.md
@.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-PATTERNS.md
@.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-01-SUMMARY.md

<interfaces>
<!-- Contratos que o executor consome. Extraídos do código + do 41-01. -->

EF pós-41-01 (deps injetáveis) — supabase/functions/notificar-candidato/index.ts:
- `export async function handler(req: Request, deps: NotificarDeps): Promise<Response>` com `deps = { supabaseAdmin, fetchImpl, serviceKey }`.
- `CorpoRequisicao { evento; candidatura_id; agendamento_id? }` (linhas 67-71) — estender com `retry_id?: string`.
- Passo 5 = claim-before-send (upsert ON CONFLICT dedupe_key ignoreDuplicates, linhas 176-207).
- `registrarFalha(novasTentativas = 1)` já parametrizado no 41-01 e usando `computeProximaTentativa`.
- fetch do Resend via `deps.fetchImpl("https://api.resend.com/emails", { headers: { Authorization: Bearer apiKey, "Content-Type": ... }, body })` (linhas 263-272).
- Escritas de sucesso (linhas 295-302, status='enviado' + provider_message_id + enviado_em) e de falha (via registrarFalha) usam `.eq("dedupe_key", dedupe_key)` no caminho normal.

Contrato do body de retry (do Plano 41-03, varrer_retry_notificacoes):
- `{ retry_id, evento, candidatura_id, agendamento_id? }` — agendamento_id só em convite (split_part do dedupe_key).

Backoff (Plano 41-01, helpers.ts): `computeProximaTentativa(novasTentativas)` → ISO ou null no cap 5.

Idempotency-Key do Resend (RESEARCH Pattern 3, linha 375): header em POST /emails, máx 256 chars, janela 24h — reusar `dedupe_key` (caminho normal) ou `retry_id`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Branch retry (gate por retry_id) + Idempotency-Key no fetch</name>
  <files>supabase/functions/notificar-candidato/index.ts</files>
  <read_first>
    - supabase/functions/notificar-candidato/index.ts (o arquivo pós-41-01 — handler injetável)
    - .planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-RESEARCH.md Pattern 3 (344-375)
    - .planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-PATTERNS.md § "notificar-candidato/index.ts (MODIFY)" (152-183)
    - .planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-01-SUMMARY.md (o que a refatoração deixou pronto)
  </read_first>
  <behavior>
    - body { retry_id, evento, candidatura_id } com linha status='falhou', tentativas=1: re-tenta; fetch mock 200 → UPDATE status='enviado' WHERE id=retry_id, provider_message_id gravado.
    - body { retry_id } com linha tentativas=5 (ou status='enviado'/'entregue', ou ausente): responde 200 { skipped: 'nao_elegivel' }, NENHUM fetch.
    - body { retry_id } com linha status='pendente', tentativas=2, fetch mock 429/non-2xx: UPDATE status='falhou' WHERE id=retry_id, tentativas=3, proxima_tentativa_em = computeProximaTentativa(3).
    - body SEM retry_id (caminho normal): fluxo atual inalterado (claim-before-send + escrita por dedupe_key).
    - fetch do Resend carrega header Idempotency-Key (dedupe_key no normal, retry_id no retry).
  </behavior>
  <action>
    Em `index.ts`: (1) aceitar `retry_id?: string` no parse do body (estender `CorpoRequisicao` + a validação; retry_id é opcional e string). (2) LOGO após o parse, se `retry_id` presente, entrar no BRANCH RETRY:
    - `SELECT id, status, tentativas, dedupe_key, evento, candidatura_id, agendamento_id? FROM notificacoes_enviadas WHERE id = retry_id` (allowlist de colunas; usar `.eq("id", retry_id).maybeSingle()`).
    - Guard: se ausente OU `status NOT IN ('pendente','falhou')` OU `tentativas >= 5` → `return jsonResponse({ ok: true, skipped: "nao_elegivel" }, 200)` (log via logSeguro).
    - Caso elegível: PULAR o passo 5 (claim). Reusar a resolução de dados (candidatura/candidato/vaga/agendamento por allowlist), o `exigirSinkTeste`, o render e o segredo do Vault do caminho normal — mas as ESCRITAS de falha/sucesso passam a keyar por `WHERE id = retry_id` e a INCREMENTAR: `novasTentativas = row.tentativas + 1`; falha → `registrarFalha(novasTentativas)` com `.eq("id", retry_id)`; sucesso → UPDATE status='enviado' + provider_message_id + enviado_em `.eq("id", retry_id)`.
    (3) Parametrizar `registrarFalha` para aceitar o alvo (dedupe_key no normal, id no retry) e `novasTentativas` — no cap 5 (`computeProximaTentativa` devolve null) a linha permanece `falhou` sem novo agendamento.
    (4) Adicionar o header `Idempotency-Key` ao objeto de headers do `deps.fetchImpl("https://api.resend.com/emails", ...)` — valor = `dedupe_key` (caminho normal) ou `retry_id` (branch retry). NUNCA logar a chave.
    Preservar o caminho normal (sem retry_id) exatamente como está. Manter a invariante fire-and-forget: qualquer falha grava `falhou` e retorna 200, nunca 5xx ao chamador.
  </action>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/notificar-candidato</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "retry_id" supabase/functions/notificar-candidato/index.ts` retorna ≥3 (parse + SELECT por id + escritas por id).
    - `grep -c "nao_elegivel" supabase/functions/notificar-candidato/index.ts` retorna ≥1 (guard de elegibilidade).
    - `grep -c "Idempotency-Key" supabase/functions/notificar-candidato/index.ts` retorna ≥1 (cinto secundário LEDGER-02).
    - `grep -Ec "eq\\(\"id\", *retry_id\\)|id = retry_id" supabase/functions/notificar-candidato/index.ts` retorna ≥1 (escritas keadas por id no retry).
    - A suite `deno test ... supabase/functions/notificar-candidato` termina 0 failed SEM `--allow-net`; o caminho normal segue verde.
  </acceptance_criteria>
  <done>Branch retry re-tenta linha existente por id, respeita o cap 5, incrementa tentativas com backoff, e o fetch carrega Idempotency-Key; caminho normal intacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes do branch retry (nao_elegivel, sucesso→enviado, non-2xx→falhou+backoff)</name>
  <files>supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts</files>
  <read_first>
    - supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts (o arquivo pós-41-01 — loadHandler + mocks)
    - supabase/functions/analise-candidato-individual/__tests__/index.test.ts:74-126, 277-299 (makeMockSupabase que grava updates + molde de caso via handler + mock)
    - .planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-PATTERNS.md § "notificar-candidato/__tests__" (203-212)
  </read_first>
  <behavior>
    - retry nao_elegivel: mock devolve row com tentativas=5 → handler responde 200 { skipped: 'nao_elegivel' }, o fetchImpl mock NÃO é chamado.
    - retry sucesso: mock devolve row status='falhou' tentativas=1 + candidatura/candidato/vaga resolvíveis; fetchImpl mock resolve 200 { id: 're_abc' } → há um UPDATE com status='enviado', provider_message_id='re_abc', keado por id.
    - retry non-2xx: fetchImpl mock resolve 429 → há um UPDATE status='falhou' com tentativas=2 e proxima_tentativa_em não-null (backoff).
    - caminho normal (sem retry_id): um caso prova que o claim-before-send ainda é exercido (upsert com onConflict dedupe_key).
  </behavior>
  <action>
    Estender `notificar-candidato.test.ts` (manter os casos puros + o caso de refactor do 41-01). Adicionar um `makeMockSupabase` que: (a) resolve `.from("notificacoes_enviadas").select(...).eq("id", ...).maybeSingle()` com a row injetada (status/tentativas), (b) registra `.from(t).update(patch).eq(col, val)` num array inspecionável, (c) resolve as leituras de candidatura/candidato/vaga por allowlist com fixtures resolvíveis, (d) resolve `.rpc("ler_resend_api_key")` com uma chave fake. Injetar `fetchImpl` mock (contando chamadas + devolvendo Response 200/429 sintético). Casos conforme behavior, via `handler(makeRequest(body, VALID_BEARER), deps)`. Provar: nao_elegivel não chama fetch; sucesso grava enviado+provider_message_id por id; non-2xx grava falhou com tentativas incrementado e proxima_tentativa_em presente. SEM `--allow-net` (fetch é o mock injetado).
  </action>
  <verify>
    <automated>deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions/notificar-candidato</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "nao_elegivel" supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts` retorna ≥1.
    - Existe caso provando: fetch NÃO chamado quando nao_elegivel; UPDATE status='enviado'+provider_message_id no sucesso; UPDATE status='falhou'+tentativas incrementado no non-2xx.
    - A suite roda 0 failed SEM `--allow-net`; a suite completa `deno test ... supabase/functions` (job blocking do CI) permanece verde.
  </acceptance_criteria>
  <done>O branch retry está coberto por testes com fetch/supabaseAdmin mockados: elegibilidade, sucesso, falha+backoff — tudo sem rede.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron/pg_net (retry_id) → EF | O branch retry só é acionado por um Bearer self-auth válido (mesma self-auth do caminho normal) |
| EF → Resend | O double-send é barrado por UNIQUE(dedupe_key) durável + Idempotency-Key de 24h |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-14 | Denial of Service | Retry infinito (loop de custo/e-mail) | mitigate | Guard `tentativas >= 5` → skipped:nao_elegivel; `computeProximaTentativa` devolve null no cap 5 → linha fica `falhou` sem novo agendamento |
| T-41-15 | Tampering | Double-send se duas varreduras se sobrepuserem | mitigate | Header `Idempotency-Key` (dedupe_key/retry_id, 24h no Resend) + UNIQUE(dedupe_key) durável já vivo (LEDGER-02) |
| T-41-16 | Information Disclosure | PII/segredo/chave em log no branch retry | mitigate | `logSeguro` (allowlist) é o único caminho de log; Idempotency-Key/apiKey nunca interpolados |
| T-41-17 | Spoofing | Envio a candidato real num run de teste no retry | mitigate | `exigirSinkTeste(dest.para, modo)` reusado no branch retry antes do fetch (DELIV-03) |
</threat_model>

<verification>
- `deno test --allow-env --allow-read --config supabase/functions/deno.json supabase/functions` verde (blocking do CI), SEM `--allow-net`.
- Branch retry re-tenta por id, respeita cap 5, incrementa tentativas com backoff; Idempotency-Key no fetch; caminho normal preservado.
</verification>

<success_criteria>
- EF `notificar-candidato` ganha o modo retry (retry_id) que a varredura aciona — re-tenta linha existente, cap 5, backoff, Idempotency-Key (RECON-01, RECON-03).
- Cobertura de teste com fetch mockado (sem chave viva) para elegibilidade/sucesso/falha.
- Zero contato com PROD; redeploy fica no plano GATED 41-05.
</success_criteria>

<output>
Create `.planning/phases/41-reconcilia-o-de-entrega-retry-testing/41-04-SUMMARY.md` when done
</output>
