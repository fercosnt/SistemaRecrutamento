---
phase: 38-ef-notificar-candidato-comm
plan: 03
type: execute
wave: 2
depends_on: ["38-01", "38-02"]
files_modified:
  - supabase/functions/notificar-candidato/index.ts
  - supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts
  - supabase/config.toml
autonomous: true
requirements: [COMM-01, COMM-02, COMM-03, COMM-04, COMM-05]

must_haves:
  truths:
    - "Existe a EF notificar-candidato: self-auth Bearer (compara com service_role), payload ids-only, resolve dados por allowlist explícita (nunca select('*')), reivindica idempotência via upsert ON CONFLICT dedupe_key, envia via fetch plano ao Resend e grava o resultado no ledger (COMM-01)"
    - "A EF produz o e-mail correto por evento: confirmação, avanço, convite (com anexo .ics de _shared/ics.ts) e decisão (cópia neutra) — via renderarEmail de _shared/email-templates.ts"
    - "O ledger é escrito em 2 fases: claim 'pendente' antes do envio; após o fetch, update para 'enviado'+provider_message_id OU 'falhou'+ultimo_erro+proxima_tentativa_em. Non-2xx do Resend grava falhou e a EF retorna 200 (fire-and-forget, nunca throw ao trigger)"
    - "Segredo RESEND_API_KEY vem só do Vault via rpc ler_resend_api_key(); nunca logado nem interpolado. Logs carregam só ids/evento/status"
    - "A EF é deployável dormente (verify_jwt=false em config.toml) e testável por deno test sem --allow-net (fetch ao Resend é construído por helper puro asserido offline)"
  artifacts:
    - path: "supabase/functions/notificar-candidato/index.ts"
      provides: "Handler self-auth + resolução por allowlist + claim idempotente + render + fetch Resend + ledger 2-fase + helpers puros exportados"
      contains: "ler_resend_api_key"
      min_lines: 150
    - path: "supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts"
      provides: "deno test dos helpers puros: dedupe_key, mapa de evento, corpo Resend (anexo só no convite), sem PII em log"
      contains: "dedupe_key"
      min_lines: 60
    - path: "supabase/config.toml"
      provides: "[functions.notificar-candidato] verify_jwt = false"
      contains: "notificar-candidato"
      min_lines: 1
  key_links:
    - from: "supabase/functions/notificar-candidato/index.ts"
      to: "supabase/functions/_shared/email-templates.ts"
      via: "renderarEmail(evento, dados)"
      pattern: "renderarEmail"
    - from: "supabase/functions/notificar-candidato/index.ts"
      to: "supabase/functions/_shared/ics.ts"
      via: "gerarIcsAgendamento + icsParaBase64 no anexo do convite"
      pattern: "icsParaBase64"
    - from: "supabase/functions/notificar-candidato/index.ts"
      to: "public.notificacoes_enviadas"
      via: "upsert ON CONFLICT dedupe_key (claim) + update por dedupe_key (resultado)"
      pattern: "dedupe_key"
---

<objective>
Construir a Edge Function `notificar-candidato`: o despachante fino, self-authenticating, que transforma um payload ids-only em um e-mail Beauty Smile enviado e auditado no ledger — deployável dormente e provado por deno test offline.

Purpose: é a superfície central do M7. Toda a segurança (self-auth, allowlist, candidato-DENY do ledger, segredo só-no-Vault) e a idempotência (claim-before-send contra `uq_notif_dedupe`) vivem aqui. A EF é invocada só por `net.http_post` manual nesta fase (o rewire de triggers é a P39), então ela nasce dormente e é provada ponta-a-ponta pelo smoke do 38-04.
Output: a EF + registro em config.toml + teste deno dos helpers puros. Zero npm novo (fetch plano ao Resend), zero rede no teste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/38-ef-notificar-candidato-comm/38-CONTEXT.md
@.planning/phases/38-ef-notificar-candidato-comm/38-RESEARCH.md
@supabase/functions/cost-alerter/index.ts
@supabase/functions/_shared/email-config.ts

<interfaces>
<!-- Tudo já verificado nos arquivos vivos; não re-derivar. -->

Self-auth (cost-alerter/index.ts ~102): Bearer = authHeader.slice(7).trim(); compara com
  `Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')` (override opcional via env). !bearer||≠ → 401.
  Deploy verify_jwt=false → o handler é o dono da auth. CORS+jsonResponse+errorResponse: copiar de cost-alerter.

Resend fetch (cost-alerter/index.ts ~216): POST https://api.resend.com/emails,
  headers { Authorization: `Bearer ${key}`, 'Content-Type':'application/json' },
  body JSON { from, to, subject, html, reply_to, attachments? }. Sucesso ⇒ 2xx + { id }.
  ⚠ VERIFICAR no execute (RESEARCH Q1): o sub-objeto attachments = { filename, content(base64) }
  contra https://resend.com/docs/api-reference/emails/send-email antes de finalizar o convite.

Ledger notificacoes_enviadas — Insert type (database.types.ts), obrigatórios:
  candidato_id, candidatura_id, dedupe_key, destinatario_email, destinatario_original, evento, template.
  Opcionais relevantes: status (enum, default), modo, provider_message_id, enviado_em,
  ultimo_erro, proxima_tentativa_em, tentativas. evento ∈ ('confirmacao','avanco','convite','decisao') (CHECK vivo).

email-config.ts: FROM, REPLY_TO, resolverModo(), resolverDestinatario(emailReal, evento, modo)
  → { para, destinatario_original, modo, redirecionado }. EventoNotificacao =
  'candidatura_recebida'|'avaliacao_liberada'|'convite_entrevista'|'decisao_final'.

Allowlist (database.types.ts, nomes reais): candidaturas(candidato_id, vaga_id, etapa_atual),
  candidatos(nome_completo, email), vagas(titulo), agendamentos_entrevista(id, data_hora, local_ou_link, tipo).

Vault: `supabaseAdmin.rpc('ler_resend_api_key')` (P36, sem argumento) — primeira consumidora.

config.toml: bloco `[functions.<nome>]` + `verify_jwt = false` (precedente linhas 23-24).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: notificar-candidato/index.ts — handler + helpers puros exportados</name>
  <files>supabase/functions/notificar-candidato/index.ts</files>
  <read_first>
    - supabase/functions/cost-alerter/index.ts — self-auth, CORS/jsonResponse/errorResponse, o fetch ao Resend, o padrão de retorno idempotente 200
    - supabase/functions/analise-candidato-individual/index.ts — allowlist explícita, createClient service_role, try/catch never-absent, deploy --no-verify-jwt
    - supabase/functions/_shared/email-config.ts — FROM/REPLY_TO/resolverModo/resolverDestinatario/EventoNotificacao (IMPORTAR, não reimplementar)
    - supabase/functions/_shared/email-templates.ts (plano 38-02) — renderarEmail
    - supabase/functions/_shared/ics.ts (plano 38-01) — gerarIcsAgendamento, icsParaBase64
    - database.types.ts — o Insert de notificacoes_enviadas e os nomes de coluna da allowlist
    - .planning/phases/38-ef-notificar-candidato-comm/38-RESEARCH.md § Q1,Q3,Q5 — request Resend, claim ON CONFLICT, allowlist
  </read_first>
  <action>
    Criar `supabase/functions/notificar-candidato/index.ts`. Importar (estático, topo): `createClient` de `https://esm.sh/@supabase/supabase-js@2` (mesma versão dos EFs vizinhos), `FROM/REPLY_TO/resolverModo/resolverDestinatario` + `type EventoNotificacao` de `../_shared/email-config.ts`, `renderarEmail` de `../_shared/email-templates.ts`, `gerarIcsAgendamento/icsParaBase64` de `../_shared/ics.ts`. Copiar CORS + `jsonResponse` + `errorResponse` de cost-alerter.

    Exportar helpers PUROS (para o teste offline):
    - `type EventoLedger = 'confirmacao'|'avanco'|'convite'|'decisao'`.
    - `mapearEvento(e: EventoLedger): EventoNotificacao` — mapa literal explícito (confirmacao→candidatura_recebida, avanco→avaliacao_liberada, convite→convite_entrevista, decisao→decisao_final).
    - `montarDedupeKey(e: EventoLedger, candidaturaId: string, agendamentoId?: string): string` — convite ⇒ `${agendamentoId}:convite`; senão `${candidaturaId}:${e}`.
    - `construirCorpoResend(args: { para; subject; html; icsBase64?: string }): object` — devolve o objeto de body do Resend ({ from: FROM, to, reply_to: REPLY_TO, subject, html, ...(icsBase64 ? { attachments:[{ filename:'entrevista-beautysmile.ics', content: icsBase64 }] } : {}) }). NÃO inclui a chave.
    - `logSeguro(obj)` — helper que só emite ids/evento/status/counts (allowlist de chaves), nunca email/nome/html/segredo.

    Handler `Deno.serve` (ou `serve`, espelhar cost-alerter):
    (1) OPTIONS → CORS; método ≠ POST → 405.
    (2) **Self-auth:** Bearer vs `SUPABASE_SERVICE_ROLE_KEY` (override opcional `NOTIFICAR_SECRET`). Ausente/≠ → 401.
    (3) **Parse ids-only:** `{ evento: EventoLedger, candidatura_id: string, agendamento_id?: string }`. Validar `evento` ∈ os 4; convite exige `agendamento_id`. Inválido → 400 VALIDATION.
    (4) `createClient(SUPABASE_URL, SERVICE_KEY, { auth:{autoRefreshToken:false,persistSession:false} })`.
    (5) **Resolver por allowlist** (nunca select('*')): candidatura (`candidato_id,vaga_id,etapa_atual`) por id → candidato (`nome_completo,email`) → vaga (`titulo`); se convite, agendamento (`id,data_hora,local_ou_link,tipo`) por `agendamento_id`. Qualquer linha ausente → 200 com log seguro `{ skipped:'dados_ausentes' }` (never-absent; não throw).
    (6) `eventoNotif = mapearEvento(evento)`; `modo = resolverModo()`; `dest = resolverDestinatario(candidato.email, eventoNotif, modo)`; `dedupe_key = montarDedupeKey(evento, candidatura_id, agendamento_id)`.
    (7) **Claim-before-send:** `upsert` em `notificacoes_enviadas` com { candidato_id, candidatura_id, dedupe_key, destinatario_email: dest.para, destinatario_original: dest.destinatario_original, evento, template: eventoNotif, status:'pendente', modo }, `{ onConflict:'dedupe_key', ignoreDuplicates:true }`, `.select('id')`. Se `data` vazio ⇒ já reivindicado ⇒ retornar `200 { ok:true, skipped:'duplicate' }` (log seguro).
    (8) **Render:** formatar `data_hora` em `America/Sao_Paulo` via `Intl.DateTimeFormat('pt-BR',{ timeZone:'America/Sao_Paulo', dateStyle:'full', timeStyle:'short' })` (só no convite). `const { subject, html } = renderarEmail(eventoNotif, { nomeCandidato, tituloVaga, dataHoraFmt?, localOuLink?, tipoEntrevista? })`.
    (9) **Anexo do convite:** só quando `evento==='convite'`: `icsBase64 = icsParaBase64(gerarIcsAgendamento({ id: agendamento.id, data_hora: agendamento.data_hora, local_ou_link: agendamento.local_ou_link }))`.
    (10) **Segredo:** `const { data: keyRow } = await supabaseAdmin.rpc('ler_resend_api_key')`; a chave é `keyRow` (string). Em `producao` sem chave → gravar falhou + 200 (não vazar 401 opaco). Em teste sem chave → o caminho real de rede não roda no CI (fetch é injetável/mockado); no smoke real a chave existe.
    (11) **Enviar:** `fetch('https://api.resend.com/emails', { method:'POST', headers:{ Authorization:`Bearer ${key}`, 'Content-Type':'application/json' }, body: JSON.stringify(construirCorpoResend({ para: dest.para, subject, html, icsBase64 })) })`. Usar uma indireção `const doFetch = deps?.fetch ?? fetch` para testabilidade.
    (12) **Ledger 2-fase (resultado):** `resp.ok` ⇒ `update { status:'enviado', provider_message_id: (await resp.json()).id, enviado_em: now }` where dedupe_key. Não-2xx OU throw de rede ⇒ `update { status:'falhou', ultimo_erro: `<status> <msg>` truncado, proxima_tentativa_em: now+intervalo, tentativas: 1 }`. NUNCA relançar — retornar `200 { ok:true }` (log seguro com evento+status).

    PROIBIÇÕES: nenhum `select('*')`; nenhum throw ao trigger em falha de envio; nenhum log de email/nome/html/segredo; nenhuma chave interpolada em log; nenhum retry interno (P41 faz); import runtime-constructed proibido (tudo estático).
  </action>
  <verify>
    <automated>F=supabase/functions/notificar-candidato/index.ts; test -f "$F" && grep -q "ler_resend_api_key" "$F" && grep -q "renderarEmail" "$F" && grep -q "icsParaBase64" "$F" && grep -q "onConflict" "$F" && grep -q "SUPABASE_SERVICE_ROLE_KEY" "$F" && grep -q "montarDedupeKey" "$F" && [ "$(grep -c "select('\\*')" "$F")" -eq 0 ] && grep -q "api.resend.com/emails" "$F" && echo "index.ts OK"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/functions/notificar-candidato/index.ts` existe e não contém `select('*')`.
    - Faz self-auth comparando o Bearer com `SUPABASE_SERVICE_ROLE_KEY` e responde 401 quando ausente/divergente.
    - Exporta `mapearEvento`, `montarDedupeKey`, `construirCorpoResend` (helpers puros).
    - `montarDedupeKey('convite', c, a)` retorna `${a}:convite`; para os outros retorna `${c}:${evento}`.
    - Reivindica idempotência com `upsert(..., { onConflict:'dedupe_key', ignoreDuplicates:true }).select('id')` e retorna `200 {skipped:'duplicate'}` quando o claim não traz linha.
    - Chama `renderarEmail` e, só no convite, `gerarIcsAgendamento`+`icsParaBase64` no anexo.
    - Lê a chave via `rpc('ler_resend_api_key')`; a chave nunca aparece em `console.*`.
    - Em falha de envio grava `status='falhou'` + `proxima_tentativa_em` e retorna 200 (não relança).
    - Import de `createClient` é estático no topo.
  </acceptance_criteria>
  <done>A EF resolve, reivindica, renderiza, envia e audita — sem vazar PII/segredo e sem quebrar o trigger em falha.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar a função em config.toml</name>
  <files>supabase/config.toml</files>
  <read_first>
    - supabase/config.toml (linhas ~23-30) — o bloco exato `[functions.<nome>]` + `verify_jwt = false`
  </read_first>
  <action>
    Adicionar ao `supabase/config.toml`, junto aos outros EFs JWT-OFF (self-auth), o bloco:
    `[functions.notificar-candidato]` seguido de `verify_jwt = false`. Manter o agrupamento/comentário existente (é um EF server-interno self-auth, mesmo grupo de cost-alerter/analise-candidato-individual). Não tocar nos outros blocos.
  </action>
  <verify>
    <automated>grep -A1 "\\[functions.notificar-candidato\\]" supabase/config.toml | grep -q "verify_jwt = false" && echo "config.toml registrado"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/config.toml` contém `[functions.notificar-candidato]` imediatamente seguido de `verify_jwt = false`.
    - Nenhum outro bloco `[functions.*]` foi alterado.
  </acceptance_criteria>
  <done>A EF nasce com JWT desligado — ela é a dona da própria auth (Bearer self-auth).</done>
</task>

<task type="auto">
  <name>Task 3: Teste deno offline dos helpers puros + wiring do corpo Resend</name>
  <files>supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts</files>
  <read_first>
    - supabase/functions/notificar-candidato/index.ts (Task 1) — os helpers exportados
    - supabase/functions/_shared/__tests__/ (vizinhos) — idioma de assert/Deno.test
    - .planning/phases/38-ef-notificar-candidato-comm/38-RESEARCH.md § Q7 — estratégia de teste sem --allow-net
  </read_first>
  <action>
    Criar `supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts` com `Deno.test`:

    (1) **dedupe_key:** `montarDedupeKey('convite','cand-1','agd-9') === 'agd-9:convite'`; `montarDedupeKey('avanco','cand-1') === 'cand-1:avanco'`; idem confirmacao/decisao.
    (2) **mapa de evento:** `mapearEvento` cobre os 4 (confirmacao→candidatura_recebida, avanco→avaliacao_liberada, convite→convite_entrevista, decisao→decisao_final).
    (3) **corpo Resend — anexo condicional:** `construirCorpoResend({ para, subject, html, icsBase64:'QUJD' })` inclui `attachments:[{ filename:'entrevista-beautysmile.ics', content:'QUJD' }]`; sem `icsBase64` NÃO inclui `attachments`. Em ambos, `from`===FROM, `reply_to`===REPLY_TO, `to`===para, e a chave da API NÃO aparece no objeto.
    (4) **sem PII em log:** se `logSeguro` for exportado, asserir que dado um objeto com `email`/`nome`, o payload logado não os contém (só ids/evento/status).

    Import de assert espelhando vizinhos. Sem --allow-net (os helpers são puros).
  </action>
  <verify>
    <automated>cd supabase/functions && deno test notificar-candidato/__tests__/notificar-candidato.test.ts --allow-env --allow-read 2>&1 | tail -5; deno test notificar-candidato/__tests__/notificar-candidato.test.ts --allow-env --allow-read >/dev/null 2>&1 && echo "notificar-candidato tests pass"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts` existe.
    - `deno test supabase/functions/notificar-candidato/__tests__/notificar-candidato.test.ts --allow-env --allow-read` sai 0.
    - Cobre dedupe_key (convite vs outros), o mapa dos 4 eventos, e o anexo condicional do corpo Resend.
    - Assere que o objeto de corpo do Resend não carrega a chave da API.
  </acceptance_criteria>
  <done>Os invariantes puros da EF (idempotência, mapa de evento, forma do request) são provados offline por deno test verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Chamador (trigger/net.http_post) → EF | verify_jwt=false; o Bearer self-auth é a fronteira |
| EF (service_role) → dados do candidato | RLS é row-level; `select('*')` vazaria PII — allowlist é a fronteira |
| EF → provedor externo (Resend) | Chave só-no-Vault; corpo carrega PII mínima necessária ao e-mail |
| EF → logs/observabilidade | PII e segredo nunca cruzam para o log |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03-01 | Spoofing | EF sem verify_jwt vira endpoint público spoofável | mitigate | Self-auth: Bearer === SUPABASE_SERVICE_ROLE_KEY; 401 caso contrário (teste manual no 38-04) |
| T-38-03-02 | Information Disclosure | `select('*')` vaza colunas PII do candidato/candidatura | mitigate | Allowlist explícita; acceptance `grep -c "select('*')"`=0 |
| T-38-03-03 | Information Disclosure | Segredo RESEND_API_KEY logado/interpolado | mitigate | Chave só via rpc; `logSeguro` allowlista chaves; nunca interpola em console.* |
| T-38-03-04 | Tampering/Repudiation | Double-send por invocação repetida do mesmo evento | mitigate | Claim-before-send atômico (`ON CONFLICT dedupe_key DO NOTHING`); duplicata ⇒ no-op 200 |
| T-38-03-05 | DoS | Throw 500 ao trigger em falha de Resend → net.http_post dropa silenciosamente e perde o registro | mitigate | Falha grava `falhou`+proxima_tentativa_em e retorna 200 (fire-and-forget) |
| T-38-03-06 | Information Disclosure | PII do candidato no payload do trigger (P39) | mitigate | Body ids-only; a EF resolve tudo server-side |
| T-38-03-07 | Injection | Dados do candidato no html do e-mail | mitigate | `escapeHtml` no módulo de templates (38-02) |
</threat_model>

<verification>
- `supabase/functions/notificar-candidato/index.ts` existe, self-auth, sem `select('*')`, importa ics+templates+email-config
- `[functions.notificar-candidato] verify_jwt = false` em config.toml
- `deno test supabase/functions/ --allow-env --allow-read` sai 0 (inclui os 3 planos)
- `npm run lint` mantém `tsc` de `src/**` em 97; `npm run build` verde
- A EF NÃO é deployada neste plano (deploy = 38-04, checkpoint do orquestrador)
</verification>

<success_criteria>
1. A EF existe, self-authenticating, resolve por allowlist, reivindica idempotência, renderiza os 4 eventos, envia via fetch ao Resend e grava no ledger (COMM-01).
2. Convite carrega o anexo `.ics` PUBLISH; decisão carrega a cópia neutra congelada (COMM-04, COMM-05).
3. Falha de envio nunca quebra o chamador — grava `falhou` e retorna 200; nenhum PII/segredo vaza em log.
4. Provada offline por deno test; deployável dormente (verify_jwt=false).
</success_criteria>

<output>
Create `.planning/phases/38-ef-notificar-candidato-comm/38-03-SUMMARY.md` when done
</output>
