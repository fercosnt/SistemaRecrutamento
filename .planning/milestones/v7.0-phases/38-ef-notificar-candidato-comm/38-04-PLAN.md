---
phase: 38-ef-notificar-candidato-comm
plan: 04
type: checkpoint
wave: 3
depends_on: ["38-03"]
files_modified: []
autonomous: false
requirements: [COMM-01]

must_haves:
  truths:
    - "A EF notificar-candidato está deployada em PROD dormente (verify_jwt=false), sem nenhum trigger apontando para ela ainda (o rewire é a P39)"
    - "Um net.http_post manual à EF (Vault Bearer) envia o e-mail correto a um endereço de teste resend.dev e grava uma linha status='enviado' com provider_message_id no ledger — a prova ponta-a-ponta do COMM-01 antes de qualquer trigger existir"
    - "Se o segredo RESEND_API_KEY não estiver no Vault (pendência UAT-36-2), o smoke trava explicitamente aqui — a EF é a cobradora do provisionamento, e a ausência é diagnóstico (NULL), não um 401 opaco"
  artifacts: []
  key_links:
    - from: "net.http_post (manual, orquestrador)"
      to: "supabase/functions/notificar-candidato"
      via: "Vault Bearer self-auth, corpo ids-only { evento, candidatura_id, agendamento_id? }"
      pattern: "notificar-candidato"
---

<objective>
Provar a EF ponta-a-ponta: deploy dormente + um disparo manual que faz o e-mail sair e o ledger registrar `enviado` — tudo antes de existir qualquer trigger.

Purpose: a EF só é segura para o rewire da P39 se estiver comprovadamente viva e correta. Este plano é o gate ponta-a-ponta do COMM-01 (critério 4) e a cobradora do provisionamento do Vault (UAT-36-2). Ambas as tarefas são **checkpoints do orquestrador** — GSD executor subagents não recebem tools MCP do Supabase (bug upstream, STATE.md Blockers); o deploy e o smoke são feitos pela main thread, nunca por executor autônomo.
Output: EF deployada dormente + evidência do smoke (e-mail em resend.dev + linha `enviado` no ledger). Nenhum arquivo de código muda aqui.

⚠ **Gate humano possível:** se o segredo `resend_api_key` não estiver no Vault (UAT-36-2 pendente do P36), o smoke trava e vira HUMAN-UAT. Isso é o comportamento correto — a EF é quem cobra a chave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/38-ef-notificar-candidato-comm/38-CONTEXT.md
@.planning/phases/38-ef-notificar-candidato-comm/38-RESEARCH.md

<interfaces>
<!-- Detalhes operacionais; o orquestrador executa via MCP Supabase. -->

Deploy dormente: `supabase functions deploy notificar-candidato --no-verify-jwt`
  (ou MCP deploy_edge_function). "Dormente" = nenhum trigger aponta pra ela (P39 faz o rewire).

Modo do smoke: `NOTIFICACOES_MODO` ausente ⇒ fail-safe `teste` ⇒ resolverDestinatario
  desvia para `delivered+<evento>@resend.dev` (sandbox Resend que aceita e descarta).

net.http_post (SQL, via MCP execute_sql pelo orquestrador):
  SELECT net.http_post(
    url := '<project_url>/functions/v1/notificar-candidato',
    headers := jsonb_build_object('Authorization','Bearer <edge_invoke_key do Vault>','Content-Type','application/json'),
    body := jsonb_build_object('evento','confirmacao','candidatura_id','<uuid de teste>')
  );

Contas/dados de teste (STATE.md): candidato `candidato.funil@teste.com`; usar uma candidatura
  existente que resolva candidato+vaga (para convite, um agendamento existente).

Vault: segredo `resend_api_key` — provisionamento é UAT-36-2 (ação humana do Fernando).
  Ausência ⇒ a EF em `teste` sem chave não alcança rede; em produção falharia explícito.
  Para o smoke real de envio, a chave PRECISA existir no Vault.
</interfaces>
</context>

<tasks>

<task type="manual">
  <name>Task 1: Deploy dormente da EF (checkpoint do orquestrador)</name>
  <files></files>
  <read_first>
    - .planning/STATE.md § Blockers/Concerns — subagentes não recebem MCP; deploy é do orquestrador
    - supabase/config.toml — confirmar `[functions.notificar-candidato] verify_jwt = false` (do 38-03)
  </read_first>
  <action>
    O ORQUESTRADOR (main thread) deploya a EF dormente via MCP `deploy_edge_function` (ou `supabase functions deploy notificar-candidato --no-verify-jwt`). Nenhum trigger é criado — a EF fica esperando invocação manual. Confirmar que a função aparece na lista de EFs do projeto (MCP `list_edge_functions`) com status ACTIVE.

    Pré-condição: os 3 planos de código (38-01/02/03) verdes em `deno test` e `[functions.notificar-candidato]` registrado.
  </action>
  <verify>
    <manual>MCP list_edge_functions inclui `notificar-candidato` com status ACTIVE; nenhum trigger novo referencia a EF (a P39 é quem cria).</manual>
  </verify>
  <acceptance_criteria>
    - A EF `notificar-candidato` está deployada e ACTIVE em PROD.
    - Foi deployada com verify_jwt desligado (self-auth é da EF).
    - Nenhum trigger aponta para ela ainda (permanece dormente até a P39).
  </acceptance_criteria>
  <done>A EF está viva em PROD, esperando o disparo manual do smoke.</done>
</task>

<task type="manual">
  <name>Task 2: Smoke ponta-a-ponta via net.http_post (checkpoint do orquestrador)</name>
  <files></files>
  <read_first>
    - .planning/phases/38-ef-notificar-candidato-comm/38-RESEARCH.md § Q7 (forma do smoke) e § Validation Architecture
    - .planning/STATE.md § Blockers/Concerns (contas de teste; UAT-36-2 do Vault)
  </read_first>
  <action>
    O ORQUESTRADOR executa, via MCP `execute_sql`:
    (1) **Pré-check do segredo:** confirmar que o Vault tem `resend_api_key` (`select` seguro que NÃO imprime o valor — só existência). Se ausente ⇒ PARAR e registrar HUMAN-UAT "UAT-38-1: provisionar `resend_api_key` no Vault (bloqueia o smoke)" — a EF é a cobradora; não inventar chave.
    (2) Escolher uma `candidatura_id` de teste que resolva candidato+vaga. Disparar `net.http_post` para a EF com `{ evento:'confirmacao', candidatura_id:'<uuid>' }` e Vault Bearer.
    (3) Aguardar e verificar dois efeitos: (a) uma linha nova em `notificacoes_enviadas` com `evento='confirmacao'`, `dedupe_key='<uuid>:confirmacao'`, `status='enviado'`, `provider_message_id` não-nulo e `destinatario_email` = `delivered+candidatura_recebida@resend.dev` (modo teste); (b) opcional — confirmar no dashboard Resend que o e-mail foi aceito.
    (4) **Idempotência:** disparar o MESMO net.http_post de novo; verificar que NENHUMA segunda linha é criada (ON CONFLICT dedupe_key) e a EF responde 200 `skipped:'duplicate'`.
    (5) Limpar a(s) linha(s) de teste do ledger (DELETE por dedupe_key de teste), já que a tabela é o audit trail de produção.

    Registrar a evidência (ids, status, provider_message_id mascarado) no SUMMARY.
  </action>
  <verify>
    <manual>Uma linha `status='enviado'` com provider_message_id nasce no ledger para o disparo; o re-disparo não cria segunda linha (idempotência provada); linhas de teste limpas depois.</manual>
  </verify>
  <acceptance_criteria>
    - Um `net.http_post` manual à EF dormente grava uma linha `enviado` com `provider_message_id` no ledger e envia a `delivered+<evento>@resend.dev` (COMM-01 critério 4).
    - Um segundo disparo idêntico NÃO cria segunda linha (idempotência `uq_notif_dedupe` provada em PROD).
    - Se o Vault não tiver `resend_api_key`, o plano registra a HUMAN-UAT de provisionamento em vez de fabricar chave.
    - As linhas de teste são removidas do ledger após a prova.
  </acceptance_criteria>
  <done>A EF está provada ponta-a-ponta e idempotente em PROD, dormente, pronta para o rewire de triggers da P39.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Orquestrador (MCP) → PROD | Deploy e smoke tocam PROD; subagente nunca (não tem MCP) |
| Smoke → candidato real | Modo teste desvia para resend.dev — nenhum e-mail alcança pessoa real |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04-01 | Information Disclosure | Smoke em modo produção mandaria e-mail a candidato real | mitigate | `NOTIFICACOES_MODO` ausente ⇒ fail-safe teste ⇒ resend.dev; verificar antes de disparar |
| T-38-04-02 | Repudiation | Linhas de teste poluindo o audit trail de produção | mitigate | DELETE das linhas de teste por dedupe_key após a prova (Task 2.5) |
| T-38-04-03 | Spoofing | Deploy com verify_jwt=true faria o net.http_post falhar / exigiria JWT de usuário | mitigate | Deploy `--no-verify-jwt`; a EF self-autha o Bearer |
| T-38-04-04 | DoS/Diagnóstico | Chave falsa no Vault daria 401 opaco em vez de ausência diagnosticável | mitigate | Não fabricar chave; ausência ⇒ HUMAN-UAT explícita (UAT-38-1) |
</threat_model>

<verification>
- A EF aparece ACTIVE em `list_edge_functions`, dormente (sem trigger)
- O smoke grava `enviado`+provider_message_id e prova idempotência no re-disparo
- Nenhum arquivo de código muda neste plano (files_modified vazio)
- Se bloqueado pelo Vault: HUMAN-UAT UAT-38-1 registrada; a fase fecha com o código verde e o smoke pendente (não-bloqueante para o código, bloqueante para o COMM-01 critério 4 live)
</verification>

<success_criteria>
1. A EF está deployada dormente em PROD (verify_jwt=false), sem trigger.
2. Um disparo manual prova o caminho ponta-a-ponta: e-mail em resend.dev + linha `enviado`+provider_message_id no ledger.
3. O re-disparo prova a idempotência em PROD (nenhuma segunda linha).
4. A ausência do segredo do Vault vira uma HUMAN-UAT explícita, nunca uma chave fabricada.
</success_criteria>

<output>
Create `.planning/phases/38-ef-notificar-candidato-comm/38-04-SUMMARY.md` when done
</output>
