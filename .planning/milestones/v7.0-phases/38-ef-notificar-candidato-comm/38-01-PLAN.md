---
phase: 38-ef-notificar-candidato-comm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/ics.ts
  - supabase/functions/_shared/__tests__/ics.test.ts
autonomous: true
requirements: [COMM-04]

must_haves:
  truths:
    - "O gerador RFC-5545 do M6 vive em supabase/functions/_shared/ics.ts como função pura, byte-idêntico ao original (METHOD:PUBLISH, CRLF, fold ≤75 octetos) — a EF do convite (COMM-04) importa daqui, não reimplementa"
    - "O .ics é convertível a base64 por um helper UTF-8-safe sem npm novo — nomes/locais acentuados não quebram o btoa"
    - "A fidelidade do port é provada por um teste deno que compara a saída contra um fixture conhecido, não por leitura"
  artifacts:
    - path: "supabase/functions/_shared/ics.ts"
      provides: "gerarIcsAgendamento (port verbatim do M6) + helpers toIcsUtc/escapeIcsText/foldIcsLine + icsParaBase64 UTF-8-safe"
      contains: "METHOD:PUBLISH"
      min_lines: 60
    - path: "supabase/functions/_shared/__tests__/ics.test.ts"
      provides: "Teste deno: paridade da saída .ics + CRLF + round-trip base64"
      contains: "PRODID:-//Beauty Smile//Recrutamento//PT-BR"
      min_lines: 40
  key_links:
    - from: "src/features/agendamento/services/agendamentoCandidatoService.ts"
      to: "supabase/functions/_shared/ics.ts"
      via: "port verbatim da função pura gerarIcsAgendamento + helpers"
      pattern: "gerarIcsAgendamento"
---

<objective>
Portar verbatim o gerador `.ics` (RFC-5545) do M6 para o mundo Deno das Edge Functions, como função pura reutilizável, e provar por execução que o port é fiel.

Purpose: não existe import compartilhável cross `src/`↔`supabase/functions/` (dois runtimes, dois tsconfig). O convite de entrevista (COMM-04) precisa do mesmo `.ics` que o candidato já baixa no painel do M6 — mesmo `PRODID`, mesmo `METHOD:PUBLISH`, mesmo fold de linha, mesmo CRLF. Copiar e "revisar por leitura" foi o que deixou o drift do M7 nascer; aqui a paridade é asserida por um teste que compara a saída contra um fixture.
Output: `_shared/ics.ts` (função pura + helper base64) + teste deno. Zero npm novo, zero rede.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/38-ef-notificar-candidato-comm/38-CONTEXT.md
@.planning/phases/38-ef-notificar-candidato-comm/38-RESEARCH.md
@src/features/agendamento/services/agendamentoCandidatoService.ts

<interfaces>
<!-- Fonte da verdade do port. Já lida; não re-derivar. -->

M6 gerador (src/features/agendamento/services/agendamentoCandidatoService.ts):
  `gerarIcsAgendamento(row: MeuAgendamentoRow): string` — pura. Depende de:
  `toIcsUtc(iso)`, `escapeIcsText(s)`, `foldIcsLine(line)`, constantes `ICS_SUMMARY`,
  `UMA_HORA_MS`, e lança `MeuAgendamentoServiceError('...', 'INVALID_INPUT')` em data_hora inválida.
  Usa apenas `row.id`, `row.data_hora`, `row.local_ou_link`.
  Saída: linhas VCALENDAR/VEVENT unidas por `\r\n`, com `METHOD:PUBLISH` e
  `PRODID:-//Beauty Smile//Recrutamento//PT-BR`, cada linha folded a ≤75 octetos.

Base64 UTF-8-safe em Deno (RESEARCH Q2): `btoa(String.fromCharCode(...new TextEncoder().encode(s)))`.
  `btoa` sozinho lança em char não-Latin1 (nome/local acentuado). NÃO usar `btoa(s)` direto.

Idioma de teste deno do repo: supabase/functions/_shared/__tests__/*.ts, rodado
  `deno test ... --allow-env --allow-read` (SEM --allow-net). Usa `Deno.test(...)` +
  asserts de `jsr:@std/assert` OU `https://deno.land/std/assert` (ver imports vizinhos).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portar gerarIcsAgendamento + helpers verbatim para _shared/ics.ts</name>
  <files>supabase/functions/_shared/ics.ts</files>
  <read_first>
    - src/features/agendamento/services/agendamentoCandidatoService.ts — o gerador e TODOS os seus helpers (toIcsUtc, escapeIcsText, foldIcsLine), constantes (ICS_SUMMARY, UMA_HORA_MS) e o tipo de linha usado por gerarIcsAgendamento; copiar byte-a-byte a lógica dessas funções
    - supabase/functions/_shared/email-config.ts — para espelhar o estilo "zero imports por design" de um módulo _shared (docblock no topo explicando por que não há imports)
    - .planning/phases/38-ef-notificar-candidato-comm/38-RESEARCH.md § Q2 e Q6 — idioma base64 e superfície do port
  </read_first>
  <action>
    Criar `supabase/functions/_shared/ics.ts` como módulo puro, sem imports externos.

    Exportar: (1) um tipo mínimo `IcsAgendamentoInput` com exatamente os campos que a função usa — `id: string`, `data_hora: string`, `local_ou_link: string | null` (substitui `MeuAgendamentoRow` do M6, que carrega campos irrelevantes). (2) `gerarIcsAgendamento(row: IcsAgendamentoInput): string` — corpo IDÊNTICO ao do M6: mesmas linhas VCALENDAR/VEVENT na mesma ordem, `VERSION:2.0`, `PRODID:-//Beauty Smile//Recrutamento//PT-BR`, `CALSCALE:GREGORIAN`, `METHOD:PUBLISH`, `UID:${row.id}@recrutamento.beautysmile`, DTSTAMP/DTSTART/DTEND(+1h), `SUMMARY:${escapeIcsText(ICS_SUMMARY)}`, LOCATION condicional, fold por linha, join com `\r\n`. (3) Os helpers `toIcsUtc`, `escapeIcsText`, `foldIcsLine` e as constantes `ICS_SUMMARY`, `UMA_HORA_MS`, portados verbatim. (4) `icsParaBase64(ics: string): string` — o helper UTF-8-safe `btoa(String.fromCharCode(...new TextEncoder().encode(ics)))`.

    Para o guard de `data_hora` inválida: NÃO importar `MeuAgendamentoServiceError` (é do mundo src). Lançar um `Error` local com a mesma mensagem (`'data_hora inválida — não é possível gerar o .ics'`) OU um erro tipado local mínimo — o comportamento (throw em `Number.isNaN(startMs)`) deve ser idêntico.

    Docblock no topo explicando: port verbatim do M6 (COMM-04), zero imports por design (roda em `deno test` sem --allow-net), e que a EF do convite importa `gerarIcsAgendamento` + `icsParaBase64` daqui.

    PROIBIÇÕES: não alterar `PRODID`, `METHOD`, a ordem das linhas, o fold ou o CRLF; não "melhorar" a lógica; não trocar `\r\n` por `\n`; não usar `btoa` direto sobre string não-encodada.
  </action>
  <verify>
    <automated>F=supabase/functions/_shared/ics.ts; test -f "$F" && grep -q "METHOD:PUBLISH" "$F" && grep -q "PRODID:-//Beauty Smile//Recrutamento//PT-BR" "$F" && grep -q "export function gerarIcsAgendamento" "$F" && grep -q "icsParaBase64" "$F" && grep -q 'join(.\\r\\n.)' "$F" && [ "$(grep -c 'from ' "$F")" -eq 0 ] && echo "ics.ts port OK"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/functions/_shared/ics.ts` existe e não contém nenhuma linha `import ... from` (zero imports).
    - Exporta `gerarIcsAgendamento(row: IcsAgendamentoInput): string` e `icsParaBase64(ics: string): string`.
    - Contém literalmente `METHOD:PUBLISH`, `PRODID:-//Beauty Smile//Recrutamento//PT-BR`, `VERSION:2.0`, `CALSCALE:GREGORIAN` e `@recrutamento.beautysmile`.
    - O join final das linhas usa `'\r\n'` (CRLF), não `'\n'`.
    - `icsParaBase64` usa `new TextEncoder().encode` antes de `btoa` (não `btoa` direto sobre a string).
    - Lança em `Number.isNaN` da data (guard preservado do M6).
  </acceptance_criteria>
  <done>O gerador `.ics` do M6 vive no mundo Deno como função pura fiel, pronto para a EF do convite importar.</done>
</task>

<task type="auto">
  <name>Task 2: Teste deno de paridade do .ics + round-trip base64</name>
  <files>supabase/functions/_shared/__tests__/ics.test.ts</files>
  <read_first>
    - supabase/functions/_shared/ics.ts (escrito na Task 1) — as exports a testar
    - supabase/functions/_shared/__tests__/ (qualquer *.test.ts vizinho) — o idioma exato de import de assert e a assinatura de `Deno.test` deste repo
    - src/features/agendamento/services/__tests__/agendamentoCandidatoService.test.ts — os casos que o M6 já cobre (para reusar um fixture equivalente de data/local)
  </read_first>
  <action>
    Criar `supabase/functions/_shared/__tests__/ics.test.ts` com `Deno.test` casos:

    (1) **Estrutura RFC-5545:** para um input fixo (`{ id: 'agd-123', data_hora: '2026-08-01T14:30:00-03:00', local_ou_link: 'Rua Exemplo, 100 — São Paulo' }`), a saída contém, em ordem, `BEGIN:VCALENDAR`, `VERSION:2.0`, `PRODID:-//Beauty Smile//Recrutamento//PT-BR`, `METHOD:PUBLISH`, `BEGIN:VEVENT`, `UID:agd-123@recrutamento.beautysmile`, `DTSTART:`, `DTEND:`, `SUMMARY:`, `LOCATION:`, `END:VEVENT`, `END:VCALENDAR`.
    (2) **CRLF:** a saída contém `\r\n` e NÃO contém nenhum `\n` que não seja precedido de `\r` (assert que `output.split('\r\n').join('')` não contém `\n`).
    (3) **LOCATION condicional:** com `local_ou_link: null` a saída NÃO contém `LOCATION:`.
    (4) **DTEND = DTSTART + 1h:** parse dos dois timestamps UTC e assert diferença de 3600s.
    (5) **Guard:** `gerarIcsAgendamento({ id:'x', data_hora:'', local_ou_link:null })` lança.
    (6) **Base64 round-trip:** `icsParaBase64(ics)` seguido de decode (`new TextDecoder().decode(Uint8Array.from(atob(b64), c=>c.charCodeAt(0)))`) reproduz a string original — incluindo os acentos de `São Paulo`.

    Import de assert espelhando os testes vizinhos (`jsr:@std/assert` ou `deno.land/std`). Nenhum `--allow-net` necessário.
  </action>
  <verify>
    <automated>cd supabase/functions && deno test _shared/__tests__/ics.test.ts --allow-env --allow-read 2>&1 | tail -5; test "${PIPESTATUS:-0}" = "0" || deno test _shared/__tests__/ics.test.ts --allow-env --allow-read >/dev/null 2>&1 && echo "ics tests pass"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/functions/_shared/__tests__/ics.test.ts` existe.
    - `deno test supabase/functions/_shared/__tests__/ics.test.ts --allow-env --allow-read` sai 0.
    - Há um caso asserindo `METHOD:PUBLISH` e o `PRODID` Beauty Smile na saída.
    - Há um caso de CRLF, um de LOCATION condicional, um de guard (throw) e um de round-trip base64 que preserva acento.
  </acceptance_criteria>
  <done>A paridade e a segurança de encoding do `.ics` portado são provadas por deno test verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Candidato-controlado (`local_ou_link`) → conteúdo do `.ics` | Texto do candidato/RH entra no VEVENT; `escapeIcsText` é a fronteira |
| Runtime src (M6) → runtime Deno (EF) | Um port infiel só aparece no e-mail do candidato, longe daqui |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-01 | Tampering | Port que troca `METHOD:PUBLISH` por REQUEST muda a semântica do convite (vira RSVP) | mitigate | Acceptance criteria exige `METHOD:PUBLISH` literal; teste (1) assere |
| T-38-01-02 | Tampering | Join com `\n` em vez de `\r\n` — Outlook rejeita o anexo | mitigate | Teste (2) assere CRLF |
| T-38-01-03 | Injection | `local_ou_link` não-escapado quebraria o VEVENT (`,`/`;`/newline) | mitigate | `escapeIcsText` portado verbatim; input com vírgula no fixture do teste |
| T-38-01-04 | DoS/Correctness | `btoa` direto lança em acento → convite nunca sai | mitigate | `icsParaBase64` UTF-8-safe; teste (6) usa `São Paulo` |
</threat_model>

<verification>
- `supabase/functions/_shared/ics.ts` existe, zero imports, com `gerarIcsAgendamento` + `icsParaBase64`
- `deno test supabase/functions/_shared/__tests__/ics.test.ts --allow-env --allow-read` sai 0
- `npm run lint` mantém a contagem `tsc` de `src/**` em 97 (EF/Deno não é type-checked pelo tsc do src)
- `npm run build` continua verde (não toca em src/)
</verification>

<success_criteria>
1. A EF do convite (plano 38-03) pode `import { gerarIcsAgendamento, icsParaBase64 } from '../_shared/ics.ts'` e obter um `.ics` idêntico ao que o candidato baixa no painel do M6.
2. Nomes e locais acentuados produzem base64 válido — o anexo não quebra.
3. A fidelidade é provada por deno test, não por revisão de leitura.
</success_criteria>

<output>
Create `.planning/phases/38-ef-notificar-candidato-comm/38-01-SUMMARY.md` when done
</output>
