---
phase: 38-ef-notificar-candidato-comm
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/_shared/email-templates.ts
  - supabase/functions/_shared/__tests__/email-templates.test.ts
autonomous: true
requirements: [COMM-02, COMM-03, COMM-05, COMM-06]

must_haves:
  truths:
    - "Os 4 e-mails (confirmação, avanço, convite, decisão) são renderizados por um módulo _shared hand-rolled com inline CSS e identidade Beauty Smile — nunca react-email (quebra no Deno edge)"
    - "A cópia de rejeição é uma constante neutra congelada que NUNCA interpola motivo/score/percentil/trait — D-15/RNF-07a — e um grep-guard executável prova a neutralidade"
    - "Todo valor interpolado vindo de dado do candidato/RH é HTML-escapado — o template não é vetor de injeção"
    - "Cada evento tem subject pt-BR próprio e um preheader; um wrapper compartilhado carrega header (logo) + footer LGPD transacional (sem opt-out, por decisão de kickoff)"
  artifacts:
    - path: "supabase/functions/_shared/email-templates.ts"
      provides: "layoutBase (wrapper) + 4 renderizadores de corpo + subjects + cópia de rejeição congelada + escapeHtml"
      contains: "escapeHtml"
      min_lines: 90
    - path: "supabase/functions/_shared/__tests__/email-templates.test.ts"
      provides: "Testes deno: renderiza os 4 eventos + grep-guard de tokens de scoring na rejeição + ausência de react-email"
      contains: "percentil"
      min_lines: 50
  key_links:
    - from: "supabase/functions/_shared/email-config.ts"
      to: "supabase/functions/_shared/email-templates.ts"
      via: "tipo EventoNotificacao reusado para chavear o renderizador"
      pattern: "EventoNotificacao"
---

<objective>
Construir os 4 templates de e-mail Beauty Smile como módulo Deno puro, hand-rolled, e congelar + provar a neutralidade da cópia de rejeição.

Purpose: react-email quebra no runtime Deno das EFs (RESEARCH/CONTEXT); os 4 e-mails são HTML table-based com inline CSS. A cópia de rejeição é o ponto sensível do milestone — D-15/RNF-07a proíbem qualquer vazamento de motivo/score. Congelar a string e provar por grep-guard que nenhum token de scoring aparece é o coração desta fase.
Output: `_shared/email-templates.ts` + teste deno com grep-guard. Zero npm novo, zero rede.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/38-ef-notificar-candidato-comm/38-CONTEXT.md
@.planning/phases/38-ef-notificar-candidato-comm/38-RESEARCH.md
@supabase/functions/_shared/email-config.ts

<interfaces>
<!-- Contratos já verificados; não re-derivar. -->

email-config.ts: exporta `EventoNotificacao = 'candidatura_recebida'|'avaliacao_liberada'|'convite_entrevista'|'decisao_final'`, `FROM`, `REPLY_TO`, `REMETENTE_NOME`. Este módulo NÃO reimplementa remetente.

Brand (skill beauty-smile-design-system): turquesa + deep blue, logo Beauty Smile.
  Em e-mail, o logo entra por URL hospedada em `<img>` com `alt` (RESEARCH — base64 inline
  não renderiza em todos os clientes). Puxar os hex e a URL do logo do SKILL.md; não inventar.

Cópia de rejeição CONGELADA (CONTEXT, D-15): exatamente
  "Sua candidatura não seguirá para as próximas etapas deste processo seletivo. Agradecemos
  sinceramente o seu interesse na Beauty Smile e desejamos sucesso na sua trajetória."
  Nunca interpolar motivo/score/percentil/trait/nota/critério.

Rodagem de teste: `deno test ... --allow-env --allow-read` (sem --allow-net).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: email-templates.ts — wrapper + 4 corpos + subjects + rejeição congelada</name>
  <files>supabase/functions/_shared/email-templates.ts</files>
  <read_first>
    - supabase/functions/_shared/email-config.ts — o tipo EventoNotificacao e o estilo docblock/zero-import de um módulo _shared
    - .claude/skills/beauty-smile-design-system/SKILL.md — cores (turquesa, deep blue), tipografia e a URL do logo para o header
    - .planning/phases/38-ef-notificar-candidato-comm/38-CONTEXT.md § "Templates Beauty Smile & cópia de rejeição" — decisões travadas
    - .planning/phases/38-ef-notificar-candidato-comm/38-RESEARCH.md § Q1, Q7 — o que a EF passa a cada template
  </read_first>
  <action>
    Criar `supabase/functions/_shared/email-templates.ts` (zero imports externos; pode importar o TIPO de email-config.ts via `import type`).

    Exportar:
    (1) `escapeHtml(s: string): string` — escapa `& < > " '` (proteção de injeção nos valores interpolados).
    (2) `layoutBase(params: { preheader: string; conteudoHtml: string }): string` — wrapper table-based inline-CSS: header com `<img src="<logo URL>" alt="Beauty Smile">`, área de conteúdo, e footer LGPD **transacional** (texto fixo: e-mail automático transacional referente ao processo seletivo, sem link de descadastro — decisão de kickoff). Preheader oculto no topo.
    (3) Um tipo `DadosEmail` com os campos que os corpos usam: `nomeCandidato: string`, `tituloVaga: string`, e opcionais para o convite `dataHoraFmt?: string` (já formatada em America/Sao_Paulo pela EF), `localOuLink?: string | null`, `tipoEntrevista?: string`.
    (4) Quatro renderizadores de corpo, cada um retornando `string` (HTML do conteúdo, sem o wrapper): `corpoConfirmacao(d)`, `corpoAvanco(d)`, `corpoConvite(d)`, `corpoDecisao(d)`. Todos escapam os valores via `escapeHtml`. `corpoDecisao` usa **exclusivamente** a constante congelada `COPY_REJEICAO` (exportada) — sem ramificar por motivo/score; um tom neutro único serve aprovado e não-aprovado (a decisão específica não muda a cópia sensível). O convite menciona data/hora, local/link e diz que o convite `.ics` está anexo.
    (5) `SUBJECTS: Record<EventoNotificacao, (d: DadosEmail) => string>` — subjects pt-BR por evento (ex.: confirmação "Recebemos sua candidatura — {vaga}"; avanço "Sua candidatura avançou — {vaga}"; convite "Convite de entrevista — {vaga}"; decisão "Atualização sobre sua candidatura — {vaga}").
    (6) `renderarEmail(evento: EventoNotificacao, d: DadosEmail): { subject: string; html: string }` — mapeia evento→corpo, envolve no `layoutBase`, resolve o subject. Este é o ponto único que a EF chama.

    PROIBIÇÕES: nenhum import de `@react-email/*` nem `react`; nenhuma interpolação de motivo/score/percentil/trait/nota/critério/ranking em nenhum corpo (especialmente `corpoDecisao`); logo nunca base64 inline; não usar CSS externo/`<link>` — tudo inline.
  </action>
  <verify>
    <automated>F=supabase/functions/_shared/email-templates.ts; test -f "$F" && grep -q "escapeHtml" "$F" && grep -q "COPY_REJEICAO" "$F" && grep -q "renderarEmail" "$F" && grep -q "layoutBase" "$F" && [ "$(grep -ciE 'react-email|from .react.' "$F")" -eq 0 ] && [ "$(grep -ciE 'motivo_rejeicao|percentil|\\btrait\\b|ranking' "$F")" -eq 0 ] && echo "email-templates.ts OK"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/functions/_shared/email-templates.ts` existe; não importa `react` nem `@react-email/*`.
    - Exporta `renderarEmail`, `layoutBase`, `escapeHtml`, `COPY_REJEICAO`, `SUBJECTS`.
    - `COPY_REJEICAO` é exatamente a string congelada do CONTEXT (comparação literal).
    - `corpoDecisao` referencia `COPY_REJEICAO` e não contém nenhuma interpolação de `motivo`, `score`, `percentil`, `trait`, `nota`, `critério` ou `ranking`.
    - Todos os corpos passam valores por `escapeHtml`.
    - O footer contém texto LGPD transacional e NÃO contém link de descadastro/opt-out.
  </acceptance_criteria>
  <done>Os 4 e-mails Beauty Smile são renderizáveis por uma única função, com a cópia de rejeição neutra congelada.</done>
</task>

<task type="auto">
  <name>Task 2: Teste deno — render dos 4 eventos + grep-guard da rejeição</name>
  <files>supabase/functions/_shared/__tests__/email-templates.test.ts</files>
  <read_first>
    - supabase/functions/_shared/email-templates.ts (Task 1) — as exports a testar
    - supabase/functions/_shared/__tests__/ (vizinhos) — idioma de import de assert e assinatura Deno.test
    - .planning/phases/38-ef-notificar-candidato-comm/38-RESEARCH.md § Validation Architecture — o grep-guard é o proof do COMM-06
  </read_first>
  <action>
    Criar `supabase/functions/_shared/__tests__/email-templates.test.ts` com `Deno.test`:

    (1) **Render dos 4 eventos:** `renderarEmail(evento, dados)` para cada um dos 4 `EventoNotificacao` retorna `{ subject, html }` com `subject` não-vazio e `html` contendo o nome escapado do candidato e o título da vaga. Fixture com um nome contendo `<` (ex.: `Ana <b>`) prova o escape (o html contém `Ana &lt;b&gt;`, não `<b>`).
    (2) **Convite:** o corpo de convite contém a data/hora formatada, o local/link e a palavra "anexo"/".ics".
    (3) **GREP-GUARD (COMM-06):** o html de `renderarEmail('decisao_final', ...)` NÃO casa a regex `/score|percentil|trait|motivo|nota|ranking|pontuaç|crit[ée]rio/i`. Falha alto se casar.
    (4) **Cópia congelada:** o html de decisão contém a `COPY_REJEICAO` literal.
    (5) **Sem react-email na fonte:** ler `email-templates.ts` e asserir que não contém `@react-email` nem `from "react"`.

    Import de assert espelhando vizinhos. Sem --allow-net.
  </action>
  <verify>
    <automated>cd supabase/functions && deno test _shared/__tests__/email-templates.test.ts --allow-env --allow-read 2>&1 | tail -5; deno test _shared/__tests__/email-templates.test.ts --allow-env --allow-read >/dev/null 2>&1 && echo "email-templates tests pass"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/functions/_shared/__tests__/email-templates.test.ts` existe.
    - `deno test supabase/functions/_shared/__tests__/email-templates.test.ts --allow-env --allow-read` sai 0.
    - Há um caso que renderiza os 4 eventos e um que prova o escape de HTML (`<` vira `&lt;`).
    - Há o grep-guard asserindo que o html de decisão NÃO casa `/score|percentil|trait|motivo|nota|ranking|pontuaç|crit[ée]rio/i`.
    - Há um caso asserindo ausência de `@react-email`/`react` na fonte do módulo.
  </acceptance_criteria>
  <done>A neutralidade da rejeição (D-15) e a renderização dos 4 e-mails são provadas por deno test verde — o grep-guard falha se algum token de scoring vazar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Dado do candidato/RH (nome, vaga, local) → HTML do e-mail | Valores entram no corpo; `escapeHtml` é a fronteira |
| Score/critério interno → cópia ao candidato | D-15: o modelo NUNCA expõe motivo/score ao candidato |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-01 | Information Disclosure | Template de rejeição interpola `motivo_rejeicao`/score → viola D-15/RNF-07a | mitigate | `COPY_REJEICAO` congelada; grep-guard (teste 3) falha se qualquer token de scoring aparecer |
| T-38-02-02 | Injection (XSS) | Nome do candidato com `<script>` renderizado cru no e-mail | mitigate | `escapeHtml` em todo valor; teste (1) prova com `Ana <b>` |
| T-38-02-03 | Availability | Import de `@react-email/*` quebra o runtime Deno da EF em prod | mitigate | Proibido; teste (5) assere ausência na fonte |
| T-38-02-04 | Compliance | Footer com opt-out sugeriria e-mail de marketing (é transacional) | accept | Footer transacional fixo sem descadastro (decisão de kickoff — transacional sem opt-out) |
</threat_model>

<verification>
- `supabase/functions/_shared/email-templates.ts` existe, sem react-email, com `renderarEmail`/`COPY_REJEICAO`
- `deno test supabase/functions/_shared/__tests__/email-templates.test.ts --allow-env --allow-read` sai 0
- O grep-guard reprova se um token de scoring for injetado na rejeição (provado por execução)
- `npm run lint` mantém `tsc` de `src/**` em 97; `npm run build` verde
</verification>

<success_criteria>
1. A EF (38-03) chama `renderarEmail(evento, dados)` e recebe subject+html corretos para os 4 eventos.
2. A cópia de rejeição é neutra, congelada e provada por grep-guard — nenhum token de scoring pode vazar sem quebrar o teste.
3. Valores do candidato são escapados; nenhum template usa react-email.
</success_criteria>

<output>
Create `.planning/phases/38-ef-notificar-candidato-comm/38-02-SUMMARY.md` when done
</output>
