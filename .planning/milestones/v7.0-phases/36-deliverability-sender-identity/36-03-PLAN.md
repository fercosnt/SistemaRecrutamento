---
phase: 36-deliverability-sender-identity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-resend-dominio.mjs
  - docs/runbooks/resend-dominio-envio.md
  - .planning/phases/36-deliverability-sender-identity/36-HUMAN-UAT.md
autonomous: true
requirements: [DELIV-01]
user_setup:
  - service: resend
    why: "Verificação do domínio de envio recruta.beautysmile.com.br (SPF/DKIM auto) + geração da chave PROD dedicada a notificações"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key (chave dedicada a notificações; NÃO reutilizar a do cost-alerter)"
    dashboard_config:
      - task: "Add Domain recruta.beautysmile.com.br na região sa-east-1 (São Paulo)"
        location: "Resend Dashboard -> Domains -> Add Domain"
      - task: "Desligar open tracking e click tracking nesse domínio"
        location: "Resend Dashboard -> Domains -> recruta.beautysmile.com.br -> Settings"
  - service: dns-beautysmile-com-br
    why: "Publicar os records MX/TXT/CNAME emitidos pelo Resend + o TXT _dmarc (o Resend NÃO publica DMARC)"
    dashboard_config:
      - task: "Criar os records em send.recruta… e <token>._domainkey.recruta… exatamente como o dashboard exibir"
        location: "Painel do provedor de DNS de beautysmile.com.br (provedor ainda desconhecido — item aberto do CONTEXT)"
      - task: "Criar TXT _dmarc.recruta.beautysmile.com.br com v=DMARC1; p=none; rua=mailto:dmarc@beautysmile.com.br"
        location: "Painel do provedor de DNS de beautysmile.com.br"

must_haves:
  truths:
    - "Existe um runbook passo-a-passo, provider-agnóstico, que o Fernando consegue seguir sozinho para verificar o domínio de envio"
    - "O runbook traz os valores canônicos (domínio, região, From, Reply-To, DMARC, nome do segredo no Vault) literais e copiáveis"
    - "Um comando opt-in reporta o status do domínio no Resend e QUAIS records ainda faltam — sem nunca rodar no CI e sem nunca imprimir a chave"
    - "Sem chave disponível, o checker é no-op com aviso claro e exit 0 (é opt-in, não gate)"
    - "O gate humano do DELIV-01 está registrado como HUMAN-UAT pendente com o checklist de 9 itens — a fase fecha sem esperar propagação DNS"
  artifacts:
    - path: "scripts/check-resend-dominio.mjs"
      provides: "Verificação opt-in do domínio via GET /domains e GET /domains/:id"
      min_lines: 70
      contains: "api.resend.com/domains"
    - path: "docs/runbooks/resend-dominio-envio.md"
      provides: "Runbook DELIV-01 provider-agnóstico + seção de provisionamento do segredo no Vault"
      contains: "_dmarc.recruta.beautysmile.com.br"
    - path: ".planning/phases/36-deliverability-sender-identity/36-HUMAN-UAT.md"
      provides: "Checklist de 9 itens do SC1, status pendente"
      contains: "UAT-36-1"
  key_links:
    - from: "docs/runbooks/resend-dominio-envio.md"
      to: "scripts/check-resend-dominio.mjs"
      via: "passo de verificação do runbook (npm run check:resend-dominio)"
      pattern: "check-resend-dominio"
    - from: ".planning/phases/36-deliverability-sender-identity/36-HUMAN-UAT.md"
      to: "docs/runbooks/resend-dominio-envio.md"
      via: "referência ao runbook a executar antes do UAT"
      pattern: "docs/runbooks/resend-dominio-envio\\.md"
---

<objective>
Entregar tudo que a engenharia PODE entregar do DELIV-01 sem acesso ao DNS nem ao dashboard: o runbook executável, a verificação automatizável e o registro formal do gate humano.

Purpose: DELIV-01 é ação humana/DNS. A fase não bloqueia na propagação (decisão travada do CONTEXT) — mas também não pode fechar com o gate invisível. Este plano converte "o Fernando precisa fazer umas coisas no Resend" em um procedimento com valores literais, ordem correta, armadilhas nomeadas e um checklist de aceite rastreável.
Output: `scripts/check-resend-dominio.mjs` + `docs/runbooks/resend-dominio-envio.md` (novo diretório) + `36-HUMAN-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-deliverability-sender-identity/36-CONTEXT.md
@.planning/phases/36-deliverability-sender-identity/36-RESEARCH.md
@.planning/phases/36-deliverability-sender-identity/36-VALIDATION.md
@scripts/assert-chunks.mjs
@.planning/milestones/v6.0-phases/35-painel-do-candidato-leitura-do-agendamento/35-HUMAN-UAT.md

<interfaces>
<!-- Valores canônicos e shapes de API. Todos verificados na pesquisa — não re-derivar nem inventar. -->

VALORES LITERAIS (devem aparecer copiáveis no runbook):
  Domínio de envio ......... recruta.beautysmile.com.br
  Região Resend ............ sa-east-1 (São Paulo) — decisão do usuário; determina o hostname do MX
                             (feedback-smtp.sa-east-1.amazonses.com) e TROCAR DEPOIS EXIGE RE-VERIFICAR o domínio
  From ..................... Beauty Smile Recrutamento <nao-responda@recruta.beautysmile.com.br>
  Reply-To ................. recrutamento@beautysmile.com.br
  DMARC .................... nome _dmarc.recruta.beautysmile.com.br
                             valor v=DMARC1; p=none; rua=mailto:dmarc@beautysmile.com.br   (o Resend NÃO publica isto)
  Segredo no Vault ......... resend_api_key   (snake_case — convenção dos segredos existentes project_url / edge_invoke_key)
  Endereços de teste ....... delivered@resend.dev · bounced@resend.dev · complained@resend.dev (aceitam +label)
  Tracking ................. open tracking e click tracking DESLIGADOS neste domínio (elimina o CNAME links.<domain>
                             e o risco de "link/domain mismatch" que dispara filtro de spam)

API do Resend (Q2 da pesquisa):
  GET https://api.resend.com/domains        → { object, has_more, data:[{ id, name, status, created_at, region, capabilities }] }  — SEM records[]
  GET https://api.resend.com/domains/:id    → tudo acima + open_tracking, click_tracking, tracking_subdomain,
                                              records:[{ record, name, type, ttl, value, priority, status }]
  POST https://api.resend.com/domains/:id/verify → dispara verificação assíncrona (NÃO chamar sem flag explícita)
  Auth: header Authorization: Bearer <chave>
  Enum de status do domínio: not_started · pending · verified · partially_verified · partially_failed · failed · temporary_failure

DKIM — NÃO HARDCODAR (Q1): a doc de API mostra CNAME com nome token-prefixado da SES; a doc de dashboard descreve TXT
com chave pública. Dois shapes em circulação. O runbook manda COPIAR o que o dashboard/`GET /domains/:id` exibir.

Débito conhecido a registrar como nota do runbook (Pitfall 9): supabase/functions/cost-alerter/index.ts:208 já consome
RESEND_API_KEY como env secret da EF e envia de alertas@beautysmile.app (TLD .app, provavelmente não verificado).
NÃO refatorar o cost-alerter nesta fase — a nota é informativa; o item formal de débito é escrito pelo Plano 36-04.

Alias npm: `npm run check:resend-dominio` é adicionado ao package.json pelo Plano 36-02 (mesma wave, dono exclusivo do
package.json). A verificação DESTE plano usa a invocação direta `node scripts/check-resend-dominio.mjs`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/check-resend-dominio.mjs — verificação opt-in, sem rede no CI, sem imprimir chave</name>
  <files>scripts/check-resend-dominio.mjs</files>
  <read_first>
    - scripts/assert-chunks.mjs — o estilo de script `.mjs` deste repo (shebang, docblock com postura, mensagens `✓`/`✗`, `process.exit` explícito, idioma inglês)
    - .planning/phases/36-deliverability-sender-identity/36-RESEARCH.md § Q2 (endpoints, shape das respostas, enum de status, e as 4 regras de higiene obrigatórias do script) e § Q1 (tracking desligado, região)
    - .planning/phases/36-deliverability-sender-identity/36-CONTEXT.md § Encerramento do Gate Humano — "Verificação opt-in: NUNCA no CI; no-op com aviso claro se não houver chave"
    - supabase/functions/cost-alerter/index.ts (linhas 204-222) — o padrão de graceful degradation quando a chave não está presente
  </read_first>
  <action>
    Criar `scripts/check-resend-dominio.mjs` (Node puro, `fetch` global — zero dependências novas), em inglês, com docblock declarando em destaque: **NEVER run this in CI** (faz rede; o CI roda sem chave viva por decisão do DELIV-03) e que é opt-in via `npm run check:resend-dominio`.

    Comportamento:
    (1) Lê a chave de `process.env.RESEND_API_KEY`. Se ausente ou vazia: imprimir um aviso claro dizendo como fornecê-la (variável de ambiente na invocação, nunca commitada) e sair com **exit 0** — é opt-in, não gate. Não lançar exceção.
    (2) `GET https://api.resend.com/domains` com header `Authorization: Bearer <chave>`. Procurar no array `data` a entrada cujo `name` é exatamente `recruta.beautysmile.com.br`. Se não achar: imprimir que o domínio ainda não existe na conta e apontar o passo do runbook `docs/runbooks/resend-dominio-envio.md`; sair com exit 1 (o comando foi pedido explicitamente, então a ausência é resultado acionável).
    (3) Com o `id` encontrado, `GET https://api.resend.com/domains/:id`. Imprimir: `status` agregado do domínio, `region` (esperado `sa-east-1`), `open_tracking` e `click_tracking` (esperado `false` em ambos — sinalizar `✗` se qualquer um for `true`), e uma tabela de `records[]` com `record`, `type`, `name` e `status` por linha, destacando com `✗` cada record cujo `status` não seja o de sucesso. Esse é o valor real do script: transformar "não verificou" em "faltou o TXT SPF em send".
    (4) Sair com exit 0 apenas quando `status === 'verified'` e nenhum record estiver pendente/falho e ambos os trackings estiverem desligados; caso contrário exit 1 com o resumo do que falta.
    (5) Imprimir, ao final, o lembrete de que o DMARC **não aparece** em `records[]` (o Resend não o publica) e o comando `dig +short TXT _dmarc.recruta.beautysmile.com.br` como verificação separada.

    Regras de higiene obrigatórias, todas verificáveis: NUNCA imprimir a chave nem o header `Authorization` — em resposta 401, imprimir apenas `401 — chave inválida ou ausente`; NUNCA chamar `POST /domains/:id/verify` a menos que o argumento de linha de comando `--verify` seja passado explicitamente (e, se implementado, deixá-lo claramente marcado como ação com efeito de estado); tratar erro de rede/timeout com mensagem legível e exit 1, sem stack trace cru.
  </action>
  <verify>
    <automated>env -u RESEND_API_KEY node scripts/check-resend-dominio.mjs; test $? -eq 0 && echo "opt-in no-op OK (exit 0 sem chave)"</automated>
  </verify>
  <acceptance_criteria>
    - `env -u RESEND_API_KEY node scripts/check-resend-dominio.mjs` termina com exit 0 e a saída contém um aviso mencionando `RESEND_API_KEY` e o caminho `docs/runbooks/resend-dominio-envio.md`.
    - `grep -c "NEVER\|NUNCA" scripts/check-resend-dominio.mjs` ≥ 1 e o docblock contém a proibição explícita de rodar no CI.
    - `grep -n "api.resend.com/domains" scripts/check-resend-dominio.mjs` retorna ≥ 2 linhas (list + get-by-id).
    - `grep -n "recruta.beautysmile.com.br" scripts/check-resend-dominio.mjs` retorna ≥ 1 linha e `grep -n "_dmarc.recruta.beautysmile.com.br" scripts/check-resend-dominio.mjs` retorna ≥ 1 linha.
    - `grep -n "open_tracking\|click_tracking" scripts/check-resend-dominio.mjs` retorna ≥ 1 linha.
    - `grep -nE "console\.(log|error|warn)\([^)]*(apiKey|API_KEY|Bearer|process\.env\.RESEND)" scripts/check-resend-dominio.mjs` retorna zero linhas (a chave nunca é impressa).
    - `grep -n "domains/.*verify" scripts/check-resend-dominio.mjs` — se presente, a mesma função exige o argumento `--verify` (`grep -c "\-\-verify" scripts/check-resend-dominio.mjs` ≥ 1).
    - `grep -c "check-resend-dominio" .github/workflows/ci.yml` retorna 0.
    - `npm run test:run` continua verde (`scripts/**` já está no `test.exclude` do Vitest).
  </acceptance_criteria>
  <done>Checker opt-in existe, é no-op com exit 0 sem chave, reporta status agregado + status por record + estado do tracking, nunca imprime a chave e está ausente do CI.</done>
</task>

<task type="auto">
  <name>Task 2: docs/runbooks/resend-dominio-envio.md — runbook DELIV-01 provider-agnóstico</name>
  <files>docs/runbooks/resend-dominio-envio.md</files>
  <read_first>
    - .planning/phases/36-deliverability-sender-identity/36-RESEARCH.md § Q1 (records, região, return-path, tracking), § Q3 (sintaxe do Vault e o comando exato de create_secret), § Q4 (endereços de teste + a restrição 403 do domínio não verificado) e § Common Pitfalls 1, 2, 3 e 9
    - .planning/phases/36-deliverability-sender-identity/36-CONTEXT.md § Identidade de Remetente + § Correções e Decisões Pós-Research (região sa-east-1, chave dedicada, nome do segredo)
    - scripts/check-resend-dominio.mjs (criado na Task 1) — o comando de verificação a referenciar
    - supabase/functions/cost-alerter/index.ts (linhas 204-222) — a divergência a registrar como nota (NÃO refatorar)
  </read_first>
  <action>
    Criar o diretório `docs/runbooks/` e o arquivo `docs/runbooks/resend-dominio-envio.md` (pt-BR — documentação de domínio). Estrutura obrigatória, na ordem operacional correta:

    (1) **Contexto e valores canônicos** — tabela com domínio, região, From, Reply-To, DMARC (nome e valor), nome do segredo no Vault (`resend_api_key`) e os endereços de teste. Todos literais e copiáveis. Destaque em bloco de aviso: a região `sa-east-1` é escolhida na CRIAÇÃO do domínio e determina o hostname do MX — trocar depois exige RE-VERIFICAR o domínio inteiro.

    (2) **Passo 1 — Add Domain no Resend**: adicionar `recruta.beautysmile.com.br` selecionando a região `sa-east-1`; deixar o return-path no default (`send`), que é de onde sai o subdomínio `send.recruta.beautysmile.com.br`; DESLIGAR open tracking e click tracking. Explicar que o Resend devolve um `records[]` pronto para copiar e que o record DKIM NÃO é transcrito neste runbook de propósito: existem dois shapes em circulação (CNAME token-prefixado da SES vs TXT com chave pública) e o correto é copiar exatamente o que o dashboard exibir.

    (3) **Passo 2 — Publicar os records no DNS**: instrução provider-agnóstica com duas subseções. "Se for Cloudflare": proxy em **DNS Only** (nuvem cinza, nunca laranja); omitir o sufixo do domínio ao colar o campo *Name* (a Cloudflare re-acrescenta e produz `send.recruta.beautysmile.com.br.beautysmile.com.br`); se a prioridade MX `10` já estiver em uso, usar `20`. "Se for Registro.br ou outro": alguns formulários exigem FQDN completo em vez de nome relativo — por isso a conferência com `dig` após salvar é obrigatória em qualquer provedor. Incluir uma linha registrando que o provedor de DNS de `beautysmile.com.br` é item aberto, com um campo para o Fernando preencher na execução.

    (4) **Passo 3 — Publicar o DMARC (MANUAL)**: seção separada e destacada como obrigatória, com o nome e o valor literais. Explicar que o Resend NÃO cria este record e que `verified` no dashboard se refere apenas aos records que ele mesmo emitiu — este é o modo de falha nº 1 desta operação.

    (5) **Passo 4 — Conferir com `dig` ANTES de clicar Verify**: os comandos `dig` para cada record (MX e TXT em `send.recruta.beautysmile.com.br`, o CNAME/TXT DKIM com o nome que o dashboard mostrou, e `dig +short TXT _dmarc.recruta.beautysmile.com.br`). Explicar a ordem: criar todos os records → confirmar por `dig` → só então clicar Verify. Documentar que `pending` é normal, `temporary_failure` significa "tente de novo depois" e que `failed` DEPOIS de propagação confirmada é o único sinal real de erro.

    (6) **Passo 5 — Verificar por script**: `npm run check:resend-dominio` (ou `node scripts/check-resend-dominio.mjs`), com a nota de que a chave é passada por variável de ambiente na invocação, nunca commitada, e que este script nunca roda no CI.

    (7) **Passo 6 — Chaves do Resend**: documentar as TRÊS chaves e seus escopos distintos — (a) test-mode para dev local, guardada no `.env` gitignored; (b) PROD dedicada a notificações, guardada APENAS no Supabase Vault sob o nome `resend_api_key`; (c) a legada do `cost-alerter`, que permanece como env secret da EF e NÃO é migrada nesta fase. Incluir o comando `vault.create_secret` exato (3 args posicionais: valor, nome, descrição) a ser executado via Supabase MCP quando a chave PROD real existir, com o aviso de que NÃO se cria placeholder (uma chave falsa produz 401 opaco; a ausência produz `NULL` legível e graceful skip) e o smoke de leitura `select name, length(decrypted_secret) from vault.decrypted_secrets where name = 'resend_api_key';`.

    (8) **Passo 7 — Smoke de envio e a restrição 403**: enquanto o domínio NÃO estiver verificado, o Resend restringe o envio ao e-mail da própria conta — então o primeiro smoke local usa o e-mail do Fernando como destino; DEPOIS de verificado, usar `delivered@resend.dev` (com `+label`). Registrar que os endereços de teste contam contra a cota da conta.

    (9) **Notas e débitos conhecidos**: (a) `cost-alerter` (`supabase/functions/cost-alerter/index.ts:208`) já consome `RESEND_API_KEY` como env secret e envia de `alertas@beautysmile.app` — TLD `.app`, provavelmente domínio não verificado, o que seria um bug latente de entregabilidade; NÃO é corrigido nesta fase (o item de débito formal é registrado pelo Plano 36-04). (b) DMARC começa em `p=none` (monitoramento); endurecer para `quarantine`/`reject` é deferido para depois de acumular relatórios `rua` — fora do M7.

    O runbook NÃO deve transcrever valores de DKIM nem tokens; NÃO deve conter nenhuma chave real; NÃO deve assumir um provedor de DNS específico fora das subseções condicionais.
  </action>
  <verify>
    <automated>test -f docs/runbooks/resend-dominio-envio.md && for s in "recruta.beautysmile.com.br" "sa-east-1" "nao-responda@recruta.beautysmile.com.br" "recrutamento@beautysmile.com.br" "_dmarc.recruta.beautysmile.com.br" "v=DMARC1; p=none" "resend_api_key" "vault.create_secret" "delivered@resend.dev" "check-resend-dominio" "DNS Only" "dig"; do grep -qF "$s" docs/runbooks/resend-dominio-envio.md || { echo "FALTA: $s"; exit 1; }; done; echo "runbook contém todos os valores canônicos"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/runbooks/resend-dominio-envio.md` existe e o comando de verify acima passa (todos os 12 literais presentes).
    - `grep -c "re_[A-Za-z0-9]" docs/runbooks/resend-dominio-envio.md` retorna 0 — nenhuma chave real ou parecida com real no runbook.
    - `grep -n "resend._domainkey" docs/runbooks/resend-dominio-envio.md` retorna zero linhas — o record DKIM não é hardcodado (o runbook manda copiar o que o dashboard exibir).
    - `grep -n "sa-east-1" docs/runbooks/resend-dominio-envio.md` retorna ≥ 1 linha, e o texto adjacente afirma explicitamente que trocar a região exige re-verificar o domínio.
    - O runbook cita `cost-alerter` e `beautysmile.app` na seção de notas/débitos: `grep -c "cost-alerter" docs/runbooks/resend-dominio-envio.md` ≥ 1.
    - O runbook documenta as três chaves distintas: `grep -c "test-mode\|cost-alerter\|Vault" docs/runbooks/resend-dominio-envio.md` ≥ 3.
    - `npm run build` continua verde — o runbook vive em `docs/`, fora do bundle, e nada nele é inlinado. (Deliberadamente NÃO invocar `scripts/assert-no-secrets.mjs` diretamente: esse artefato pertence ao Plano 36-02, que roda em worktree paralelo na mesma wave e cujo arquivo não existe aqui. A prova de bundle limpo é responsabilidade do 36-02 e do gate de fim de fase.)
  </acceptance_criteria>
  <done>Runbook existe em `docs/runbooks/`, provider-agnóstico no DNS, com os valores canônicos literais, a ordem operacional correta, as armadilhas nomeadas e o comando exato de provisionamento do Vault.</done>
</task>

<task type="auto">
  <name>Task 3: 36-HUMAN-UAT.md com o checklist de 9 itens do SC1, status pendente</name>
  <files>.planning/phases/36-deliverability-sender-identity/36-HUMAN-UAT.md</files>
  <read_first>
    - .planning/milestones/v6.0-phases/35-painel-do-candidato-leitura-do-agendamento/35-HUMAN-UAT.md — o formato canônico do repo (frontmatter com status/phase/source/started/updated, seções Current Test, Context, Tests com `status: pending`, Gaps)
    - .planning/phases/36-deliverability-sender-identity/36-VALIDATION.md § "HUMAN-UAT — SC1 (DELIV-01), 9 itens" — o checklist literal a reproduzir
    - docs/runbooks/resend-dominio-envio.md (criado na Task 2) — o procedimento que precede o UAT
    - .planning/ROADMAP.md § Phase 36 — o texto do Success Criterion 1
  </read_first>
  <action>
    Criar `.planning/phases/36-deliverability-sender-identity/36-HUMAN-UAT.md` seguindo o formato de `35-HUMAN-UAT.md`: frontmatter com `status: pending`, `phase: 36-deliverability-sender-identity`, `source: [36-VALIDATION.md]`, `started` e `updated` com a data de execução.

    Seção **Context**: explicar em duas ou três frases que a P36 fechou os trilhos de código (guard de bundle, contrato `email-config`, RPC leitora do Vault) e que o item restante é irredutivelmente humano — colocação em caixa de entrada não é observável por API, e a publicação dos records depende do painel de DNS. Registrar explicitamente que a fase NÃO bloqueia na propagação DNS (decisão travada do CONTEXT) e que a cadeia P37→P38→P39 prossegue; o gate precisa aterrissar antes do primeiro envio a candidato real (UAT da P41). Referenciar `docs/runbooks/resend-dominio-envio.md` como o procedimento a executar antes do UAT.

    Seção **Tests** com um item `UAT-36-1 — Domínio de envio verificado e e-mail real cai na INBOX (DELIV-01)` contendo os 9 passos do `36-VALIDATION.md`, verbatim quanto ao conteúdo: (1) domínio Verified no dashboard na região `sa-east-1`; (2) open e click tracking desligados; (3) `dig` confirma os 3-4 records do Resend + o TXT `_dmarc`; (4) enviar 1 e-mail de teste DE `nao-responda@recruta.beautysmile.com.br` PARA uma conta Gmail e uma Outlook/Hotmail pessoais; (5) em ambas, mensagem na Caixa de entrada (não Spam nem Promoções-com-aviso); (6) no Gmail, "Mostrar original" → `SPF: PASS`, `DKIM: PASS`, `DMARC: PASS`; (7) remetente exibido é "Beauty Smile Recrutamento"; (8) responder o e-mail → a resposta chega em `recrutamento@beautysmile.com.br`; (9) registrar data + prints neste arquivo. Fechar o item com `- **status:** pending`.

    Seção **Gaps**: registrar que nenhum must-have automatizado desta fase está vermelho e que o único item aberto é este UAT vivo, mais — se aplicável — o provisionamento do segredo no Vault, que depende da chave PROD real (tratado pelo Plano 36-05).

    NÃO marcar nada como aprovado. NÃO inventar prints nem datas de execução.
  </action>
  <verify>
    <automated>test -f .planning/phases/36-deliverability-sender-identity/36-HUMAN-UAT.md && grep -q "status: pending" .planning/phases/36-deliverability-sender-identity/36-HUMAN-UAT.md && grep -q "UAT-36-1" .planning/phases/36-deliverability-sender-identity/36-HUMAN-UAT.md && test "$(grep -cE '^[0-9]+\.' .planning/phases/36-deliverability-sender-identity/36-HUMAN-UAT.md)" -ge 9 && echo "HUMAN-UAT criado com os 9 itens e status pendente"</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe com frontmatter contendo `status: pending` e `phase: 36-deliverability-sender-identity`.
    - Contém o item `UAT-36-1` com pelo menos 9 passos numerados e termina com `**status:** pending`.
    - Contém os literais `sa-east-1`, `nao-responda@recruta.beautysmile.com.br`, `recrutamento@beautysmile.com.br`, `_dmarc`, `SPF: PASS`, `DKIM: PASS` e `DMARC: PASS`.
    - Referencia `docs/runbooks/resend-dominio-envio.md`.
    - `grep -ci "aprovado\|approved\|passed" .planning/phases/36-deliverability-sender-identity/36-HUMAN-UAT.md` — nenhuma ocorrência marca o UAT como concluído (as ocorrências de `PASS` são os critérios de header SPF/DKIM/DMARC, não status do UAT).
  </acceptance_criteria>
  <done>O gate humano do DELIV-01 está formalmente registrado, pendente e rastreável, com o checklist de 9 itens e o link para o runbook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo (público a quem tem acesso) → runbook | Documentação operacional é o lugar clássico onde uma credencial acaba colada por conveniência |
| `check-resend-dominio.mjs` → `api.resend.com` | Único componente desta fase que faz rede; carrega uma credencial no header |
| Zona DNS `beautysmile.com.br` → internet | Records mal publicados permitem que terceiros passem por remetente Beauty Smile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03-01 | Spoofing | Terceiro envia e-mail se passando pela Beauty Smile para candidatos (phishing sobre processo seletivo) | mitigate | Runbook impõe SPF + DKIM (auto do Resend) E o DMARC manual em `_dmarc.recruta.beautysmile.com.br` como passo separado e obrigatório; `p=none` com `rua` dá visibilidade; endurecimento deferido explicitamente |
| T-36-03-02 | Information Disclosure | Chave Resend colada no runbook ou impressa pelo checker (log/terminal compartilhado) | mitigate | Runbook documenta o comando `vault.create_secret` com placeholder textual e proíbe chave real; acceptance criteria grep-a `re_[A-Za-z0-9]` = 0 no runbook e a ausência de `console.*` com a chave no script |
| T-36-03-03 | Elevation of Privilege | Script de rede promovido a step de CI passa a exigir chave viva nos GitHub Secrets | mitigate | Docblock "NEVER in CI"; no-op com exit 0 sem chave; `grep -c "check-resend-dominio" .github/workflows/ci.yml` = 0 |
| T-36-03-04 | Tampering | Script dispara `POST /domains/:id/verify` sem intenção, gerando estado `pending`/`failed` ruidoso no provedor | mitigate | Verify só atrás da flag explícita `--verify`; o caminho default é somente leitura |
| T-36-03-05 | Repudiation | Gate humano "esquecido" — fase fecha e o primeiro envio real vai para spam sem ninguém ter verificado | mitigate | `36-HUMAN-UAT.md` com status `pending` e checklist de 9 itens, referenciado pelo VERIFICATION da fase e cobrado pelo UAT da P41 |
</threat_model>

<verification>
- `env -u RESEND_API_KEY node scripts/check-resend-dominio.mjs` → exit 0 com aviso (no-op opt-in)
- Todos os literais canônicos presentes em `docs/runbooks/resend-dominio-envio.md` (comando de verify da Task 2)
- `36-HUMAN-UAT.md` existe com `status: pending` e os 9 itens
- `grep -c "check-resend-dominio" .github/workflows/ci.yml` → 0
- `npm run test:run` verde e `npm run build` verde
  (NÃO invocar `scripts/assert-no-secrets.mjs` aqui — artefato do Plano 36-02, wave paralela; ver nota na Task 2)
</verification>

<success_criteria>
1. Um operador consegue verificar o domínio de envio seguindo apenas `docs/runbooks/resend-dominio-envio.md`, sem precisar reler CONTEXT/RESEARCH, e sem depender de qual provedor hospeda o DNS.
2. `node scripts/check-resend-dominio.mjs` reporta status agregado, status por record e estado do tracking quando uma chave é fornecida, e é no-op silencioso e exit 0 quando não é — nunca no CI, nunca imprimindo a chave.
3. O gate humano do DELIV-01 está registrado como HUMAN-UAT pendente com os 9 itens, permitindo que a fase feche sem esperar propagação DNS.
</success_criteria>

<output>
Create `.planning/phases/36-deliverability-sender-identity/36-03-SUMMARY.md` when done
</output>
