---
phase: 28-gest-o-de-usu-rios-rh-n-cleo-seguro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-LIVE-STATE.md
autonomous: false
requirements: [SEG-02, USR-07, USR-06]
must_haves:
  truths:
    - "SEG-02 traceability: the REAL live SELECT policy names on usuarios_rh (the qual=true leaks to DROP + auth_admin_le_usuarios_rh to PRESERVE) are captured, not assumed"
    - "USR-07 traceability: the live active-administrador count(*) is known (the anti-lockout floor) before any trigger lands"
    - "USR-06 traceability: the live logs_auditoria policies + function owners are captured so append-only hardening reconciles reality"
    - "USR-06 retention: the verbatim limpar_logs_antigos() body is captured (pg_get_functiondef) so 28-04's redefinition — excluding the usuario/seguranca categories — is a verified diff, not a blind CREATE OR REPLACE (M4/DBMIG-02 discipline)"
    - "The live custom_access_token_hook body is captured verbatim (pg_get_functiondef) so no blind CREATE OR REPLACE drops a guard"
    - "RESEARCH assumptions A1-A6 are each resolved to CONFIRMED or DEVIATION in 28-LIVE-STATE.md"
  artifacts:
    - path: ".planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-LIVE-STATE.md"
      provides: "Live DB capture: usuarios_rh + logs_auditoria pg_policies, hook body, triggers, admin count, owners, limpar_logs_antigos() body; A1-A6 resolution"
      min_lines: 40
  key_links:
    - from: "28-LIVE-STATE.md policy list"
      to: "28-04 rls_seg02.sql DROP statements"
      via: "authored migration drops the exact permissive names captured here"
      pattern: "usuarios_rh_.*read"
---

<objective>
Capture the LIVE state of every DB object this phase mutates, BEFORE any migration is authored — resolving RESEARCH assumptions A1-A6 against reality (the M4 24-01 "capture live state first" discipline). This is the non-negotiable precondition of the phase: the SEG-02 migration must DROP the REAL permissive policy names, the anti-lockout trigger must know the REAL admin floor, and the append-only hardening must reconcile the REAL logs_auditoria policies + owners.

Purpose: prevent the exact failure modes RESEARCH flags — dropping the wrong policy (leak persists), touching custom_access_token_hook blindly (M4/DBMIG-02 precedent: a blind CREATE OR REPLACE dropped a live guard), breaking the DEFINER audit INSERT because the owner is not BYPASSRLS, or redefining limpar_logs_antigos() without its verbatim body.
Output: `.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-LIVE-STATE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-RESEARCH.md
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-PATTERNS.md
@.planning/milestones/v4.0-phases/24-blindagem-de-seguran-a-pii-lgpd/24-LIVE-STATE.md

<interfaces>
<!-- The exact live objects to interrogate. Executor runs each via Supabase MCP execute_sql (read-only). -->
A1   pg_policies WHERE tablename='usuarios_rh' -> policyname, cmd, roles, qual, with_check (ORDER BY cmd, policyname)
A1b  pg_policies WHERE tablename='logs_auditoria' -> policyname, cmd, roles, qual, with_check
A1c  pg_get_functiondef of public.limpar_logs_antigos (the retention-purge DELETE path) -- verbatim body so 28-04 redefines it as a byte-preserved diff (exclude usuario/seguranca); if absent, record that
A2   pg_get_functiondef('public.custom_access_token_hook(jsonb)'::regprocedure)  -- verbatim hook body
A5   pg_get_triggerdef(oid) for each non-internal pg_trigger on 'public.usuarios_rh'::regclass
cnt  count(*) FROM usuarios_rh WHERE role='administrador' AND ativo AND deleted_at IS NULL  -- active-admin floor
A4   owner + rolbypassrls of proc log_auditoria + custom_access_token_hook; owner + rolbypassrls of tables usuarios_rh, logs_auditoria
A3   (dashboard, non-SQL) Auth -> SMTP config + rate limit; if not visible mark UNVERIFIED
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Capture live DB state via Supabase MCP execute_sql</name>
  <read_first>
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-RESEARCH.md (Assumptions Log A1-A6, Runtime State Inventory, Pitfall 4, Pitfall 5 retention caveat)
    - .planning/milestones/v4.0-phases/24-blindagem-de-seguran-a-pii-lgpd/24-LIVE-STATE.md (format + the 2026-07-07 usuarios_rh policy snapshot to diff against)
  </read_first>
  <action>
    Run each query in the interfaces block against PROD via mcp__supabase__execute_sql (read-only; no writes). autonomous:false — touches PROD; the operator authorizes the MCP calls. Record the raw result of each: (A1) the full usuarios_rh SELECT/INSERT/UPDATE/DELETE policy list with roles+qual; (A1b) the logs_auditoria policy list; (A1c) the verbatim limpar_logs_antigos() body (pg_get_functiondef — the only DELETE path on logs_auditoria, redefined in 28-04 to exclude the usuario/seguranca categories); (A2) the verbatim custom_access_token_hook body; (A5) every non-internal trigger on usuarios_rh with its pg_get_triggerdef; the active-administrador count(*); (A4) the owner + rolbypassrls of log_auditoria, usuarios_rh, logs_auditoria. Do NOT mutate anything. If any query errors, record the error verbatim (an absent object is itself a finding, e.g. A5 trigger_criar_preferencias_padrao, or an absent limpar_logs_antigos).
  </action>
  <verify>
    <automated>MISSING — PROD read via MCP; evidence is the captured rows pasted into Task 2's 28-LIVE-STATE.md. No local command reproduces a live-DB read.</automated>
  </verify>
  <acceptance_criteria>
    - All 7 query groups (A1, A1b, A1c, A2, A5, admin-count, A4) returned a result or a recorded error.
    - The active-administrador count is a concrete integer (expected >=1; e2e.admin@beautysmile.com.br).
    - The verbatim custom_access_token_hook body is captured (used to confirm it already filters `ativo AND deleted_at IS NULL` -> USR-04 needs NO hook change).
    - The verbatim limpar_logs_antigos() body is captured (source of truth for the 28-04 byte-preserved retention-exclusion diff).
  </acceptance_criteria>
  <done>Raw results for A1/A1b/A1c/A2/A5/count/A4 are in hand, ready to synthesize.</done>
</task>

<task type="auto">
  <name>Task 2: Synthesize 28-LIVE-STATE.md (resolve A1-A6)</name>
  <read_first>
    - .planning/milestones/v4.0-phases/24-blindagem-de-seguran-a-pii-lgpd/24-LIVE-STATE.md (doc structure to mirror)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-RESEARCH.md (Assumptions Log — the 6 claims to confirm/deviate; Open Question Q2 retention)
  </read_first>
  <action>
    Write 28-LIVE-STATE.md. For each assumption A1-A6 record: the claim, the captured evidence, and a verdict CONFIRMED or DEVIATION: what differs -> plan impact. Include an explicit "Policies to DROP" list (every permissive authenticated/anon SELECT policy on usuarios_rh with qual=true — e.g. usuarios_rh_authenticated_read, usuarios_rh_simple_read — plus any client INSERT/UPDATE/DELETE policy found) and a "Policies to PRESERVE" list (auth_admin_le_usuarios_rh — SEC-09). Include the logs_auditoria "INSERT policy to DROP" ("Sistema insere logs" if present) and confirm NO UPDATE/DELETE policy exists. State the active-admin floor (USR-07). State whether the hook needs touching (default: NO, per A2). State the owner/BYPASSRLS verdict for the DEFINER audit path (A4). Record the verbatim limpar_logs_antigos() body (or that it is absent) so 28-04 can CREATE OR REPLACE it — byte-preserving every line and only adding the categoria IN ('usuario','seguranca') exclusion (retention hardening, RESEARCH Q2). If A3 SMTP is unverified, state that the EF treats resetPasswordForEmail failure as non-fatal EMAIL_SEND_FAILED.
  </action>
  <verify>
    <automated>test -f .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-LIVE-STATE.md && grep -qi "Policies to DROP" .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-LIVE-STATE.md && grep -qi "auth_admin_le_usuarios_rh" .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-LIVE-STATE.md && grep -qi "limpar_logs_antigos" .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-LIVE-STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Each of A1-A6 has an explicit CONFIRMED/DEVIATION verdict.
    - "Policies to DROP" enumerates the exact live permissive SELECT policy names (source of truth for 28-04).
    - "Policies to PRESERVE" contains auth_admin_le_usuarios_rh.
    - A verdict is recorded for whether custom_access_token_hook is touched this phase (expected: NOT touched).
    - The verbatim limpar_logs_antigos() body is recorded (or its absence noted) so 28-04's retention exclusion is a verified diff.
  </acceptance_criteria>
  <done>28-LIVE-STATE.md exists and downstream authoring plans (28-04, 28-05) can reconcile against real names/owners/floor/retention-body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner-assumption -> live DB reality | RESEARCH A1-A6 are assumptions; authoring migrations against them (not the live state) risks dropping the wrong policy or clobbering a hook guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-09 | Tampering | custom_access_token_hook | mitigate | Capture the verbatim live body (pg_get_functiondef) so the phase can prove it already filters ativo/deleted_at and needs NO CREATE OR REPLACE (M4/DBMIG-02 precedent) |
| T-28-02 | Information Disclosure | usuarios_rh SELECT policies | mitigate | Capture the REAL permissive qual=true policy names so 28-04 drops the actual leak, not an assumed one |
| T-28-05 | Repudiation/Tampering | logs_auditoria owner/policies + limpar_logs_antigos | mitigate | Confirm the DEFINER audit owner is BYPASSRLS so dropping the authenticated INSERT policy (28-04) does not break audit writes; capture limpar_logs_antigos() so its retention exclusion is a verified diff |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no packages (RESEARCH Package Legitimacy Audit: no new installs) |
</threat_model>

<verification>
- 28-LIVE-STATE.md exists with A1-A6 verdicts, a "Policies to DROP" list, a "Policies to PRESERVE" list, the active-admin floor, the hook-touch decision, and the verbatim limpar_logs_antigos() body.
- No PROD object was mutated (read-only capture only).
</verification>

<success_criteria>
The exact live policy names, hook body, triggers, admin floor, function owners, and limpar_logs_antigos() body are documented — 28-04 and 28-05 can author migrations that reconcile REALITY.
</success_criteria>

<output>
Create `.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-01-SUMMARY.md` when done.
</output>
</output>
