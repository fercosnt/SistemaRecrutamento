---
phase: 28-gest-o-de-usu-rios-rh-n-cleo-seguro
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/usr_rh_seg02_smoke.sql
  - supabase/tests/usr_rh_anti_lockout_smoke.sql
  - supabase/tests/usr_rh_audit_append_only_smoke.sql
autonomous: true
requirements: [SEG-02, USR-07, USR-06]
must_haves:
  truths:
    - "SEG-02 traceability: a smoke impersonates candidato + recrutador JWTs and asserts SELECT * FROM usuarios_rh returns 0 rows, admin reads the full roster, own-row read works, auth_admin_le_usuarios_rh still present"
    - "USR-07 traceability: a smoke drives the last active admin to demote/deactivate/delete and asserts SQLSTATE P0001 (LAST_ADMIN) with 0 mutation; a 2-session case proves the advisory lock"
    - "USR-06 traceability: a smoke asserts one logs_auditoria row (categoria='usuario') per mutation — the gerir_usuario_rh_mutacao 'ativar' path AND criar_usuario_rh_com_audit both write exactly one atomically, plus the resetar_senha best-effort audit-row shape (acao='resetar_senha', EF-driven) — rollback-together on forced failure, and that candidato/recrutador/admin cannot INSERT/UPDATE/DELETE logs_auditoria (append-only)"
    - "Every smoke uses set_config request.jwt.claims + SET ROLE authenticated impersonation, disposable fixed-UUID fixtures, and ROLLBACK-free cleanup that never deletes a real row"
  artifacts:
    - path: "supabase/tests/usr_rh_seg02_smoke.sql"
      provides: "SEG-02 roster-leak / admin-roster / own-row / SEC-09-preserved behavioral smoke"
      min_lines: 60
    - path: "supabase/tests/usr_rh_anti_lockout_smoke.sql"
      provides: "USR-07 last-admin RAISE (P0001) + concurrency advisory-lock proof"
      min_lines: 50
    - path: "supabase/tests/usr_rh_audit_append_only_smoke.sql"
      provides: "USR-06 atomic mutate+audit (ativar + criar) + resetar_senha audit-shape + append-only INSERT/UPDATE/DELETE denial"
      min_lines: 60
  key_links:
    - from: "usr_rh_seg02_smoke.sql candidato/recrutador impersonation"
      to: "usuarios_rh RLS after 28-04"
      via: "asserts row projection = 0 rows (not a pg_policies grep)"
      pattern: "set_config\\('request.jwt.claims'"
---

<objective>
Author the three behavioral SQL smokes that are the load-bearing verification gate for SEG-02, USR-07, and USR-06 — BEFORE the migrations exist (Nyquist test-first). These are RED now (the policies/trigger/RPCs land in wave 2, apply in wave 3) and go GREEN when 28-08 runs them against PROD post-apply. They impersonate JWTs (set_config request.jwt.claims) and assert the ROW PROJECTION, not policy metadata — the exact M4/SEC-07/08 lesson that a structural pg_policies grep passes while a real leak persists.

Purpose: the security behavior each migration must produce is written as an executable assertion before the DDL is authored; the phase gate is these smokes returning PASS.
Output: three .sql smoke files under supabase/tests/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-PATTERNS.md
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-RESEARCH.md
@supabase/tests/submit_candidatura_atomic_smokes.sql

<interfaces>
<!-- The DB objects these smokes exercise (authored in 28-04 / 28-05). Assert BEHAVIOR, not names. -->
usuarios_rh RLS (28-04): admin-only SELECT via public.is_active_rh_admin(); own-row SELECT user_id=auth.uid(); NO client INSERT/UPDATE/DELETE; PRESERVE auth_admin_le_usuarios_rh.
anti-lockout trigger (28-05): tg_usuarios_rh_anti_lockout BEFORE UPDATE OR DELETE -> RAISE EXCEPTION USING ERRCODE='P0001' when the mutation would leave 0 active administradores.
mutate+audit RPC (28-05): public.gerir_usuario_rh_mutacao(p_actor uuid, p_target uuid, p_action text, p_novo_papel text) -> one tx: UPDATE usuarios_rh + PERFORM log_auditoria(p_categoria:='usuario', p_recurso_tipo:='usuarios_rh', ...).
create+audit RPC (28-05): public.criar_usuario_rh_com_audit(p_actor, p_user_id, p_email, p_nome, p_cargo, p_papel) -> INSERT usuarios_rh + PERFORM log_auditoria(p_acao:='criar', p_categoria:='usuario', ...) in one tx.
resetar_senha audit (EF-driven, 28-06): the EF calls log_auditoria(p_acao:='resetar_senha', p_categoria:='usuario', p_recurso_tipo:='usuarios_rh', p_recurso_id:=target, p_usuario_id:=actor, p_sucesso:=true) best-effort after the GoTrue reset (no DB mutation to atomically bind to).
append-only (28-04): logs_auditoria has NO authenticated/anon INSERT/UPDATE/DELETE policy; INSERT only via DEFINER path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEG-02 roster-leak behavioral smoke</name>
  <read_first>
    - supabase/tests/submit_candidatura_atomic_smokes.sql (lines 56-129 fixture + set_config impersonation; 136-177 RAISE NOTICE PASS / RAISE EXCEPTION FAIL; 248-271 ROLLBACK-free cleanup; 126-128 skip-guard)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-PATTERNS.md (section: supabase/tests/usr_rh_*_smoke.sql + Shared Pattern: Behavioral SQL smoke)
  </read_first>
  <action>
    Create supabase/tests/usr_rh_seg02_smoke.sql. Using the submit_candidatura idiom: RESET ROLE, discover the real admin row, build a disposable fixed-UUID fixture (a recrutador row + a candidato claims JSON). Cases, each a DO block that RAISE NOTICE 'PASS ...' or RAISE EXCEPTION '... FAIL ...': (1) impersonate a candidato JWT (set_config request.jwt.claims to a candidato uid, SET ROLE authenticated) and assert SELECT count(*) FROM usuarios_rh = 0; (2) impersonate a recrutador JWT and assert SELECT count(*) FROM usuarios_rh = 0 EXCEPT the recrutador's own row is visible via the own-row policy (assert own-row read returns exactly 1 and roster-minus-own = 0); (3) impersonate the admin JWT and assert count(*) FROM usuarios_rh >= the real roster count (full read); (4) assert a pg_policies row for auth_admin_le_usuarios_rh still exists with qual = 'true' (SEC-09 preserved). ROLLBACK-free cleanup deletes only the disposable fixture, never the discovered admin. Add a smoke.ready='n' skip-guard if the fixture cannot be built.
  </action>
  <verify>
    <automated>test -f supabase/tests/usr_rh_seg02_smoke.sql && grep -q "request.jwt.claims" supabase/tests/usr_rh_seg02_smoke.sql && grep -q "auth_admin_le_usuarios_rh" supabase/tests/usr_rh_seg02_smoke.sql</automated>
  </verify>
  <acceptance_criteria>
    - Asserts candidato AND recrutador impersonated reads of usuarios_rh roster = 0 rows (the leak-removal proof).
    - Asserts the recrutador own-row read returns exactly 1 (own-row policy mandatory for login).
    - Asserts admin reads the full roster and auth_admin_le_usuarios_rh is present + USING true.
    - Cleanup never deletes a real (non-fixture) row.
  </acceptance_criteria>
  <done>SEG-02 behavior is an executable assertion, RED until 28-04 lands the RLS + 28-08 runs it on PROD.</done>
</task>

<task type="auto">
  <name>Task 2: USR-07 anti-lockout behavioral smoke</name>
  <read_first>
    - supabase/tests/submit_candidatura_atomic_smokes.sql (same impersonation/fixture/cleanup idiom)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-RESEARCH.md (Code Examples: anti-lockout trigger; Pitfall 3 write-skew)
  </read_first>
  <action>
    Create supabase/tests/usr_rh_anti_lockout_smoke.sql. Build a disposable single-active-admin fixture (a fixed-UUID usuarios_rh row role='administrador', ativo, deleted_at NULL) in an isolated context so counting sees exactly one active admin among the fixture set — OR assert against the trigger directly by attempting to demote/deactivate/delete the LAST active admin and expecting SQLSTATE P0001. Cases: (1) UPDATE the last active admin SET role='recrutador' -> expect RAISE with SQLSTATE P0001 and assert the row is unchanged (0 mutation); (2) UPDATE ... SET ativo=false on the last active admin -> expect P0001; (3) DELETE the last active admin -> expect P0001; (4) demoting a NON-last admin (2 active admins present) succeeds. For concurrency, document a 2-session advisory-lock proof (two parallel demotions of two admins -> exactly one succeeds, >=1 admin remains) as a NOTE with the SQL, executed via two MCP sessions in 28-08 (single-session SQL cannot truly parallelize). Wrap each expected-failure in a BEGIN/EXCEPTION WHEN sqlstate 'P0001' handler that RAISE NOTICE 'PASS'. ROLLBACK-free cleanup of only the fixture.
  </action>
  <verify>
    <automated>test -f supabase/tests/usr_rh_anti_lockout_smoke.sql && grep -q "P0001" supabase/tests/usr_rh_anti_lockout_smoke.sql</automated>
  </verify>
  <acceptance_criteria>
    - Asserts demote / deactivate / delete of the LAST active admin each raise SQLSTATE P0001 with 0 mutation.
    - Asserts demoting a non-last admin succeeds (the trigger is not over-eager).
    - Includes the documented 2-session concurrency proof to run in 28-08.
  </acceptance_criteria>
  <done>USR-07 behavior is an executable assertion, RED until 28-05 lands the trigger + 28-08 runs it on PROD.</done>
</task>

<task type="auto">
  <name>Task 3: USR-06 atomic-audit + append-only behavioral smoke</name>
  <read_first>
    - supabase/tests/submit_candidatura_atomic_smokes.sql (fixture/impersonation/cleanup idiom)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-PATTERNS.md (section: usr_rh_mutacao_rpc.sql + logs_auditoria_append_only.sql)
  </read_first>
  <action>
    Create supabase/tests/usr_rh_audit_append_only_smoke.sql. Cases: (1) atomicity (mutate) — call gerir_usuario_rh_mutacao(actor, target, 'ativar') on a disposable non-admin fixture and assert exactly ONE new logs_auditoria row with categoria='usuario', recurso_tipo='usuarios_rh', recurso_id=target, usuario_id=actor; (2) atomicity (create) — call criar_usuario_rh_com_audit(actor, new_user_id, email, nome, cargo, papel) on a disposable fixture and assert exactly ONE new logs_auditoria row with categoria='usuario', acao='criar', recurso_tipo='usuarios_rh', recurso_id=<returned id>, usuario_id=actor (the create RPC writes row + audit atomically — not only the ativar path); (3) rollback-together — force the mutation to fail (e.g. pass a NOT_FOUND target -> P0002) and assert NO logs_auditoria row was written (audit and mutation commit/rollback together); (4) append-only INSERT — impersonate a candidato JWT and attempt INSERT INTO logs_auditoria(...) and assert it FAILS (RLS/GRANT denies), same for a recrutador JWT; (5) append-only UPDATE/DELETE — impersonate the admin JWT and attempt UPDATE and DELETE on an existing logs_auditoria row and assert both FAIL (no UPDATE/DELETE policy for anyone); (6) resetar_senha audit-shape (EF-driven — a SQL smoke cannot invoke GoTrue): directly PERFORM log_auditoria(p_usuario_id:=actor, p_usuario_tipo:='admin', p_acao:='resetar_senha', p_categoria:='usuario', p_recurso_tipo:='usuarios_rh', p_recurso_id:=target, p_sucesso:=true) — the EXACT best-effort shape the 28-06 EF writes after a reset — and assert exactly one categoria='usuario', acao='resetar_senha' row results; document inline that this is the EF-driven best-effort audit (no DB mutation to atomically bind to). ROLLBACK-free cleanup removes only disposable audit + fixture rows the smoke itself created (never a real audit row); guard the cleanup so it cannot delete rows it did not insert.
  </action>
  <verify>
    <automated>test -f supabase/tests/usr_rh_audit_append_only_smoke.sql && grep -q "categoria" supabase/tests/usr_rh_audit_append_only_smoke.sql && grep -q "logs_auditoria" supabase/tests/usr_rh_audit_append_only_smoke.sql && grep -q "criar_usuario_rh_com_audit" supabase/tests/usr_rh_audit_append_only_smoke.sql && grep -q "resetar_senha" supabase/tests/usr_rh_audit_append_only_smoke.sql</automated>
  </verify>
  <acceptance_criteria>
    - Asserts exactly one categoria='usuario' audit row per mutation for BOTH gerir_usuario_rh_mutacao('ativar') AND criar_usuario_rh_com_audit (atomic mutate+audit), and rollback-together on forced failure.
    - Asserts the resetar_senha best-effort audit-row shape (acao='resetar_senha', categoria='usuario') the EF writes — documented as EF-driven (SQL smoke cannot invoke GoTrue).
    - Asserts candidato + recrutador JWT INSERT into logs_auditoria is denied.
    - Asserts admin JWT UPDATE and DELETE on logs_auditoria are denied (append-only).
  </acceptance_criteria>
  <done>USR-06 behavior is an executable assertion, RED until 28-04/28-05 land + 28-08 runs it on PROD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated candidato/recrutador -> usuarios_rh roster | non-admin must read 0 roster rows (own-row only) |
| any role -> logs_auditoria | only the DEFINER audit path may INSERT; nobody may UPDATE/DELETE |
| concurrent admin mutations -> active-admin count | the invariant >=1 active admin must hold under a write-skew race |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | Information Disclosure | usuarios_rh SELECT | mitigate | SEG-02 smoke asserts candidato/recrutador roster read = 0 rows (row projection, not pg_policies) |
| T-28-04 | Denial of Service | last-admin lockout | mitigate | USR-07 smoke asserts demote/deactivate/delete of last admin raises P0001; 2-session concurrency proof |
| T-28-05 | Repudiation/Tampering | logs_auditoria forge/mutate | mitigate | append-only smoke asserts non-admin INSERT denied + admin UPDATE/DELETE denied + atomic mutate+audit for ativar & criar + resetar_senha audit-shape |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | no new packages (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- Three smoke files exist under supabase/tests/ with impersonation (set_config request.jwt.claims), disposable fixtures, and ROLLBACK-free cleanup.
- Each smoke is RED against current PROD (objects not yet applied) — GREEN is proven in 28-08.
</verification>

<success_criteria>
SEG-02, USR-07, and USR-06 each have an executable behavioral smoke authored before their migrations — the phase gate is these three returning PASS in 28-08.
</success_criteria>

<output>
Create `.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-03-SUMMARY.md` when done.
</output>
</output>
