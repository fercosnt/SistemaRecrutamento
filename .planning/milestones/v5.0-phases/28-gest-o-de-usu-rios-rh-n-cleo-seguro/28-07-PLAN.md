---
phase: 28-gest-o-de-usu-rios-rh-n-cleo-seguro
plan: 07
type: execute
wave: 3
depends_on: ["28-04", "28-05", "28-06"]
files_modified:
  - database.types.ts
autonomous: false
requirements: [SEG-01, SEG-02, USR-06, USR-07]
must_haves:
  truths:
    - "All four migrations (rls_seg02, anti_lockout, mutacao_rpc, logs_auditoria_append_only) are applied to PROD via Supabase MCP apply_migration, each writing its own version row (no 42601)"
    - "database.types.ts at repo ROOT is regenerated after apply and reflects any new objects"
    - "The gerenciar-usuario-rh Edge Function is deployed (JWT-ON) and returns 401 to an unauthenticated curl"
    - "custom_access_token_hook and auth_admin_le_usuarios_rh are unchanged after apply (RH login still resolves roles)"
  artifacts:
    - path: "database.types.ts"
      provides: "Regenerated Supabase types at repo ROOT after the migrations"
      min_lines: 100
  key_links:
    - from: "apply_migration (MCP)"
      to: "supabase_migrations version rows"
      via: "MCP writes the version row itself — bypasses the db push 42601 on PL/pgSQL bodies"
      pattern: "apply_migration"
---

<objective>
[BLOCKING] Apply the four authored migrations to PROD via Supabase MCP apply_migration (NOT `supabase db push` — the project-wide decision: db push trips 42601 on the $$ PL/pgSQL bodies; apply_migration writes its own version row and bypasses it), in dependency order; regenerate database.types.ts at the repo root; and deploy the gerenciar-usuario-rh Edge Function (JWT-ON). This is the single point where the phase touches production — scheduled AFTER all authoring (28-04/05/06) and BEFORE the behavioral smokes (28-08). autonomous:false — the operator authorizes each MCP apply + the deploy.

Purpose: land the SEG-02 RLS, anti-lockout trigger, mutate+audit RPCs, and append-only hardening live, plus the write-path EF, so 28-08 can prove the behavior on PROD.
Output: 4 migrations live + regenerated database.types.ts + deployed EF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-LIVE-STATE.md
@.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Apply order (by timestamp / dependency). Each via mcp__supabase__apply_migration(name, query). -->
1. 20260713000001_usr_rh_rls_seg02.sql          (DROP leaks + is_active_rh_admin + admin/own-row SELECT)
2. 20260713000002_usr_rh_anti_lockout.sql       (trigger fn + BEFORE UPDATE OR DELETE bind)
3. 20260713000003_usr_rh_mutacao_rpc.sql        (gerir_usuario_rh_mutacao + criar_usuario_rh_com_audit)
4. 20260713000004_logs_auditoria_append_only.sql (DROP 'Sistema insere logs' + REVOKE)
Then: regenerate database.types.ts at repo ROOT; deploy the EF:
  supabase functions deploy gerenciar-usuario-rh   (JWT-ON — default) OR mcp deploy_edge_function
Post-apply guards: SELECT pg_get_functiondef('public.custom_access_token_hook(jsonb)'::regprocedure) must equal the 28-LIVE-STATE capture (unchanged); auth_admin_le_usuarios_rh still present USING true.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Apply the 4 migrations to PROD via MCP apply_migration (in order)</name>
  <read_first>
    - CLAUDE.md (Migrations + db push workaround: use MCP apply_migration; NO BEGIN/COMMIT wrapper)
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-RESEARCH.md (Environment Availability: apply_migration bypasses 42601)
    - the 4 authored migration files under supabase/migrations/ (20260713000001..000004)
  </read_first>
  <action>
    For each migration file in timestamp order (000001 rls_seg02, 000002 anti_lockout, 000003 mutacao_rpc, 000004 append_only), call mcp__supabase__apply_migration with the migration name and the file's SQL body. Do NOT use `supabase db push` (42601 on $$ bodies). After each apply, sanity-check: for 000001 SELECT policyname FROM pg_policies WHERE tablename='usuarios_rh' shows usuarios_rh_admin_select + usuarios_rh_own_select present and the qual=true leaks gone; for 000002 pg_trigger has trg_usuarios_rh_anti_lockout; for 000003 the two RPCs exist (pg_proc); for 000004 "Sistema insere logs" is gone. If an apply errors, STOP and surface the error verbatim — do not proceed to the next migration.
  </action>
  <verify>
    <automated>MISSING — PROD apply via MCP; evidence is the post-apply pg_policies/pg_trigger/pg_proc checks recorded in the SUMMARY. No local command applies to PROD.</automated>
  </verify>
  <acceptance_criteria>
    - All 4 migrations applied via apply_migration with no 42601; each wrote its version row.
    - usuarios_rh policies show admin_select + own_select and NO qual=true leak; trg_usuarios_rh_anti_lockout exists; both RPCs exist; "Sistema insere logs" is gone.
  </acceptance_criteria>
  <done>The SEG-02 RLS, anti-lockout, RPCs, and append-only hardening are live on PROD.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Regenerate database.types.ts + verify hook/SEC-09 unchanged</name>
  <read_first>
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-LIVE-STATE.md (the captured custom_access_token_hook body to diff against)
    - CLAUDE.md (npm run db:types / database.types.ts at REPO ROOT, never hand-edited)
  </read_first>
  <action>
    Regenerate database.types.ts at the repo ROOT (npm run db:types or supabase gen types). Then run the post-apply guard: SELECT pg_get_functiondef('public.custom_access_token_hook(jsonb)'::regprocedure) via MCP and confirm it byte-matches the 28-LIVE-STATE capture (the phase must NOT have altered it); confirm a pg_policies row for auth_admin_le_usuarios_rh still exists with qual='true'. If either drifted, STOP and flag (the [[reference_auth_hook_rls_gap]] failure mode).
  </action>
  <verify>
    <automated>test -f database.types.ts && grep -qi "usuarios_rh" database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - database.types.ts regenerated at repo root; tsc baseline not inflated by the regen.
    - custom_access_token_hook body unchanged vs 28-LIVE-STATE; auth_admin_le_usuarios_rh still present USING true.
  </acceptance_criteria>
  <done>Types are fresh and the auth-hook dependency is proven intact.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Deploy gerenciar-usuario-rh Edge Function (JWT-ON)</name>
  <read_first>
    - .planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-RESEARCH.md (Environment Availability: functions deploy)
    - supabase/functions/gerenciar-usuario-rh/index.ts (the deployed target)
  </read_first>
  <action>
    Deploy the function: `supabase functions deploy gerenciar-usuario-rh` (JWT verification ON — the default; the handler does its own getUser authenticate + admin authorize). It auto-bundles _shared. After deploy, curl the function URL with NO Authorization header and confirm a 401 (unauthenticated rejection) — proving the SEG-01 gate is live. Confirm the 3 env vars (SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY) are auto-injected (present for every other EF). Do NOT expose or log the service_role key.
  </action>
  <verify>
    <automated>MISSING — live deploy + curl against PROD EF URL; evidence is the recorded 401 response in the SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - gerenciar-usuario-rh is deployed and reachable; a no-auth curl returns 401 (SEG-01 gate live).
    - The EF bundles _shared/usuario-rh-schemas.ts (no import error at cold start).
  </acceptance_criteria>
  <done>The write-path EF is live and rejects unauthenticated callers; 28-08 smokes can run against the live objects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored SQL -> live PROD schema | applying via the wrong path (db push) trips 42601; applying an unreconciled migration could clobber a live object |
| deployed EF -> public internet | the function URL is reachable; it must reject unauthenticated/non-admin callers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-09 | Tampering | custom_access_token_hook / SEC-09 policy | mitigate | Post-apply pg_get_functiondef diff proves the hook is unchanged; auth_admin_le_usuarios_rh still USING true |
| T-28-01 | Elevation of Privilege | deployed EF entrypoint | mitigate | Deploy JWT-ON; no-auth curl must return 401 before 28-08 proceeds |
| T-28-02 | Information Disclosure | usuarios_rh live policies | mitigate | Post-apply pg_policies check confirms the qual=true leaks are gone and admin/own-row present |
| T-28-SC | Tampering | migration/deploy supply path | accept | apply_migration + functions deploy are the standard project path; no new packages installed |
</threat_model>

<verification>
- 4 migrations live (version rows written), database.types.ts regenerated, EF deployed JWT-ON.
- Hook + SEC-09 policy unchanged; leaks gone; trigger + RPCs present.
</verification>

<success_criteria>
All Phase-28 DB objects and the EF are live on PROD via the sanctioned MCP path, with the auth-hook dependency proven intact — ready for behavioral smokes.
</success_criteria>

<output>
Create `.planning/phases/28-gest-o-de-usu-rios-rh-n-cleo-seguro/28-07-SUMMARY.md` when done.
</output>
