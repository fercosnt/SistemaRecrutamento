---
phase: 30-meu-perfil-rh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/perfil_rh_seg03_smoke.sql
  - src/features/auth/utils/__tests__/pitfall7.grep.test.ts
autonomous: true
requirements: [PERFIL-02, PERFIL-03, SEG-03]
must_haves:
  truths:
    - "SEG-03 has a load-bearing behavioral SQL smoke authored (impersonated JWT, disposable fixtures, ROLLBACK-free cleanup) that proves the RPC updates ONLY the caller's own row, cannot set role/ativo, and the Phase-28 self-promotion-denied state still holds — RED against current PROD, GREEN after 30-06 applies the RPC"
    - "The Pitfall-7 grep guard is extended to cover perfil-rh surfaces so no password/token/signed-URL can be logged (PERFIL-02 password redaction, PERFIL-03 signed-URL redaction), tolerant of the sources not existing yet in Wave 1"
  artifacts:
    - path: "supabase/tests/perfil_rh_seg03_smoke.sql"
      provides: "SEG-03 behavioral gate — own-row-only + role-unchanged + WR-01 regression"
      contains: "atualizar_meu_perfil_rh"
    - path: "src/features/auth/utils/__tests__/pitfall7.grep.test.ts"
      provides: "Pitfall-7 regression guard extended to perfil-rh"
      contains: "perfil-rh"
  key_links:
    - from: "supabase/tests/perfil_rh_seg03_smoke.sql"
      to: "public.atualizar_meu_perfil_rh"
      via: "impersonated recrutador JWT calls the RPC and asserts own-row-only + role-unchanged"
      pattern: "atualizar_meu_perfil_rh"
---

<objective>
Author the load-bearing SEG-03 behavioral SQL smoke (the axis gate) and extend the Pitfall-7 redaction guard to the perfil-rh feature — the harness that proves the phase's security invariants. The SEG-03 smoke is RED against current PROD (the RPC does not exist yet) and runs GREEN via MCP in 30-07 after 30-06 applies the migration; it is NOT part of the Vitest runner. The Pitfall-7 extension is GREEN in Wave 1 (tolerant of the not-yet-created sources, exactly like the Phase-4 block) and stays green as sources land clean.

Purpose: Nyquist — SEG-03 gets a behavioral gate before the RPC is built; the redaction guard exists before any perfil-rh code can leak a secret.
Output: `supabase/tests/perfil_rh_seg03_smoke.sql` (RED-by-design, MCP-run) + an extended `pitfall7.grep.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-meu-perfil-rh/30-RESEARCH.md

<read_first>
- `supabase/tests/usr_rh_seg02_smoke.sql` — THE analog: the exact impersonation idiom (RESET ROLE → privileged setup discovers a real admin + a free auth.users id + a disposable recrutador fixture at a fixed UUID; each case `set_config('request.jwt.claims', ...)` then `SET ROLE authenticated`; ROLLBACK-free cleanup; SKIP-with-NOTICE if the fixture can't be built). Its case (5) is the WR-01 self-promotion-denied assertion to re-prove verbatim.
- `src/features/auth/utils/__tests__/pitfall7.grep.test.ts` — the guard to extend; mirror the tolerant `PHASE_4_VAGAS_PATHS` block (collectFiles graceful-skips missing files) and the signed-URL FORBIDDEN regex (`signedurl|signed_url|?token=`).
- `.planning/phases/30-meu-perfil-rh/30-RESEARCH.md` §Q3 — the RPC signature `atualizar_meu_perfil_rh(p_nome text, p_avatar_url text DEFAULT NULL)` and the SET list that excludes role/ativo/cargo/email.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SEG-03 behavioral SQL smoke (RED, MCP-run)</name>
  <files>supabase/tests/perfil_rh_seg03_smoke.sql</files>
  <read_first>supabase/tests/usr_rh_seg02_smoke.sql (impersonation idiom + WR-01 case 5); 30-RESEARCH.md §Q3 (RPC signature + SET list)</read_first>
  <action>Author `supabase/tests/perfil_rh_seg03_smoke.sql` mirroring `usr_rh_seg02_smoke.sql` structure exactly. Header comment: Phase 30 / Plan 30-02, states RED-until-30-06, run via MCP execute_sql, ROLLBACK-free. SETUP (RESET ROLE, privileged): discover a real active `administrador` user_id and a free `auth.users` id, INSERT TWO disposable recrutador rows at fixed UUIDs (`30020003-...-001` = the caller; `30020003-...-002` = the second user whose row must stay untouched) so IDOR can be tested; store their user_ids + a captured `role`/`nome_completo` baseline via `set_config('smoke30.*', ...)`; SKIP-with-NOTICE (`smoke30.ready='n'`) if the fixture cannot be built. Cases (each: RESET ROLE → set request.jwt.claims to the impersonated sub → SET ROLE authenticated → assert; a NOTICE 'PASS ...' = pass, RAISE EXCEPTION = fail):
    (1) **own-row-only:** impersonate caller-recrutador, `PERFORM public.atualizar_meu_perfil_rh('SMOKE Novo Nome', NULL);` then RESET ROLE and assert (privileged) that the CALLER row `nome_completo='SMOKE Novo Nome'` AND the SECOND user's `nome_completo` is UNCHANGED (WHERE user_id=auth.uid() scoped the write to exactly one row).
    (2) **role/ativo untouched:** after case (1)'s call (any args), assert the caller's `role` is still 'recrutador' and `ativo` unchanged — role/ativo are NOT in the RPC SET list, so no args can escalate.
    (3) **COALESCE avatar:** call `atualizar_meu_perfil_rh('SMOKE Nome2', NULL)` after a prior avatar_url is seeded on the caller; assert the avatar_url is PRESERVED (NULL arg does not wipe it — COALESCE), then call with a non-null path and assert it updates.
    (4) **WR-01 regression (re-prove Phase-28):** impersonate the caller-recrutador and run `UPDATE public.usuarios_rh SET role='administrador' WHERE user_id=<caller>` directly (bypassing the RPC); GET DIAGNOSTICS row_count MUST be 0 (no client UPDATE policy — self-promotion still denied). Then privileged-assert the caller's role is still 'recrutador'.
    (5) **no client UPDATE policy (pg_policies regression):** RESET ROLE, assert `pg_policies` has ZERO `cmd='UPDATE'` client policy on `usuarios_rh` (Phase-28 state preserved).
    CLEANUP (ROLLBACK-free): reset request.jwt.claims + RESET ROLE, delete the two disposable fixtures' `preferencias_notificacoes` children then their `usuarios_rh` rows by fixed UUID; the discovered admin is REAL and NEVER deleted. Do NOT wrap in BEGIN/COMMIT.</action>
  <verify>
    <automated>test -f supabase/tests/perfil_rh_seg03_smoke.sql && grep -q "atualizar_meu_perfil_rh" supabase/tests/perfil_rh_seg03_smoke.sql && grep -q "smoke30.ready" supabase/tests/perfil_rh_seg03_smoke.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists; contains the 5 cases above, each with a distinct PASS NOTICE and a FAIL RAISE.
    - SETUP creates TWO disposable recrutador fixtures (caller + second user) so own-row-only vs IDOR is provable.
    - No `BEGIN;`/`COMMIT;` wrapper; cleanup is by fixed UUID and never deletes the discovered admin.
    - The WR-01 case re-proves a direct recrutador `UPDATE ... SET role='administrador'` affects 0 rows.
  </acceptance_criteria>
  <done>The SEG-03 smoke is authored, RED against current PROD (RPC absent), ready to run GREEN via MCP in 30-07.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the Pitfall-7 redaction guard to perfil-rh surfaces</name>
  <files>src/features/auth/utils/__tests__/pitfall7.grep.test.ts</files>
  <read_first>src/features/auth/utils/__tests__/pitfall7.grep.test.ts (the tolerant PHASE_4_VAGAS_PATHS block + the signed-URL FORBIDDEN regex)</read_first>
  <action>Add a `PHASE_30_PERFIL_PATHS` const listing the perfil-rh surfaces that will land in Waves 2-3: `src/features/perfil-rh/services`, `src/features/perfil-rh/hooks`, `src/features/perfil-rh/components`, `src/features/perfil-rh/schemas`, and `src/components/pages/MeuPerfilPage.tsx`. Add these to `ALL_PATHS` (or a new describe block) so the existing password/token FORBIDDEN regex scans them, AND add a Phase-30 signed-URL scan reusing the Phase-4 `signedurl|signed_url|signedURL|?token=` regex (avatar signed URLs must never be logged — PERFIL-03). Follow the tolerant `collectFiles` graceful-skip pattern so Wave-1 (sources absent) passes, and add a sanity `it` asserting `PHASE_30_PERFIL_PATHS.flatMap(collectFiles).length >= 0` (mirrors the Phase-4 tolerance test). Do NOT add a `>= N` hard floor for Phase-30 yet (sources land later).</action>
  <verify>
    <automated>npm run test:run -- src/features/auth/utils/__tests__/pitfall7.grep.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pitfall7.grep.test.ts` contains `perfil-rh` paths and passes GREEN in Wave 1 (sources absent → tolerant skip).
    - Both the password/token regex AND the signed-URL regex scan the perfil-rh paths.
    - `npm run lint` unchanged (tsc ≤104 — a test-file edit adds no production types).
  </acceptance_criteria>
  <done>The redaction guard now covers perfil-rh; any future console.* leak of a password/token/signed-URL in perfil-rh fails the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| impersonated recrutador JWT → usuarios_rh (RPC + direct UPDATE) | the untrusted self-service caller; the smoke proves the RPC contains it |
| perfil-rh source → console/logs | secrets must never cross into logs (Pitfall-7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Elevation of Privilege | atualizar_meu_perfil_rh | mitigate | SEG-03 smoke case (2)+(4): the RPC cannot set role/ativo; a direct recrutador UPDATE role='administrador' affects 0 rows (WR-01 regression). |
| T-30-02 | IDOR / Tampering | atualizar_meu_perfil_rh | mitigate | SEG-03 smoke case (1): with two fixtures, the write touches ONLY the caller's row (WHERE user_id=auth.uid()); the second user is untouched. |
| T-30-05 | Information Disclosure | perfil-rh services/components | mitigate | Pitfall-7 grep extension scans perfil-rh for password/token/signed-URL in any console.*. |
| T-30-SC | Tampering | package installs | accept | No new package installs this phase. |
</threat_model>

<verification>
- `perfil_rh_seg03_smoke.sql` present with all 5 cases + ROLLBACK-free cleanup.
- `pitfall7.grep.test.ts` extended to perfil-rh, GREEN.
- `npm run lint` tsc ≤104 (unchanged).
</verification>

<success_criteria>
The SEG-03 behavioral gate and the Pitfall-7 redaction guard exist before any RPC or perfil-rh code is written; the Vitest runner stays green (the smoke runs via MCP, not the runner).
</success_criteria>

<output>
Create `.planning/phases/30-meu-perfil-rh/30-02-SUMMARY.md` when done.
</output>
