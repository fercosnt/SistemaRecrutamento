---
phase: 30-meu-perfil-rh
plan: 04
type: execute
wave: 2
depends_on: ["30-02"]
files_modified:
  - src/features/perfil-rh/schemas/perfilRhSchemas.ts
  - src/features/perfil-rh/services/perfilRhService.ts
  - src/features/perfil-rh/services/__tests__/perfilRhService.test.ts
  - src/features/perfil-rh/hooks/usePerfilRh.ts
  - src/features/perfil-rh/hooks/__tests__/usePerfilRh.test.tsx
autonomous: true
requirements: [PERFIL-01, PERFIL-02, PERFIL-03]
must_haves:
  truths:
    - "perfilRhService reads the caller's own row via an EXPLICIT allowlist projection (id,user_id,nome_completo,email,cargo,role,avatar_url).eq('user_id', uid) — NEVER select('*') (PII projection, [[reference_select_star_leaks_pii]])"
    - "atualizarPerfil(nome, avatarPath) dispatches to rpc('atualizar_meu_perfil_rh', {p_nome, p_avatar_url}); alterarSenha(current, nova) re-auths via signInWithPassword then updateUser — wrong current maps to a typed WRONG_CURRENT error (PERFIL-02); uploadAvatar/validateAvatar mirror cvUploadService (≤2MB, png/jpeg/webp, {uid}/avatar.<ext> upsert, PATH persisted, Pitfall-7 redaction) (PERFIL-03)"
    - "usePerfilRh own-row query + atualizarPerfil/alterarSenha/uploadAvatar mutations invalidate the own-row query on success; useAtualizarPerfil ALSO refreshes the authStore adminUser (calls setAdminUser with the updated usuarios_rh row) so the RH panel chrome (RHTopBar/RHSidebar) reflects the new name/avatar WITHOUT a re-login (Success Criterion #1)"
    - "The avatar write path ALWAYS carries the currently-loaded nome: useUploadAvatar returns only the storage path — the caller then calls atualizarPerfil({ nome: <current loaded nome>, avatarPath }); the RPC SET is unconditional so an avatar-only save that omits nome would blank nome_completo (WARNING #3)"
  artifacts:
    - path: "src/features/perfil-rh/services/perfilRhService.ts"
      provides: "own-row read + RPC dispatch + password re-auth + avatar upload/validate/sign"
      exports: ["perfilRhService", "PerfilRhServiceError", "validateAvatar"]
      min_lines: 80
    - path: "src/features/perfil-rh/hooks/usePerfilRh.ts"
      provides: "own-row query + 3 mutations invalidating on success"
      exports: ["usePerfilRh", "perfilRhKeys"]
  key_links:
    - from: "perfilRhService.atualizarPerfil"
      to: "rpc('atualizar_meu_perfil_rh')"
      via: "supabase.rpc dispatch with {p_nome, p_avatar_url}"
      pattern: "atualizar_meu_perfil_rh"
    - from: "perfilRhService.readMeuPerfil"
      to: "usuarios_rh own row"
      via: ".select(allowlist).eq('user_id', uid) — never select('*')"
      pattern: "eq\\('user_id'"
---

<objective>
Build the perfil-rh client data layer: Zod schemas, the `perfilRhService` (own-row allowlist read + `atualizar_meu_perfil_rh` RPC dispatch + GoTrue password re-auth + avatar upload/validate/sign), and the `usePerfilRh` hooks — all landing GREEN with their Vitest tests in the same plan (RED→GREEN within the plan, tdd). PERFIL-01 (name persist), PERFIL-02 (password re-auth), PERFIL-03 (avatar own-folder) all get their service contract here. The RPC call is not yet in `database.types.ts` (regen happens in 30-06), so the `.rpc()` call carries a documented `as never` cast that 30-06 drops after regen.

Purpose: The single source of truth for how the profile page talks to the backend — consumed by the UI in 30-05.
Output: `src/features/perfil-rh/{schemas,services,hooks}` + their tests, Vitest GREEN, tsc ≤104.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/30-meu-perfil-rh/30-RESEARCH.md
@.planning/phases/30-meu-perfil-rh/30-UI-SPEC.md

<interfaces>
<!-- Extracted contracts the executor uses directly — no codebase exploration needed. -->

Own-row read allowlist (per planning guidance, NEVER select('*')):
  .select('id,user_id,nome_completo,email,cargo,role,avatar_url').eq('user_id', uid).single()

RPC (authored in 30-03, typed after 30-06 regen — cast `as never` until then):
  supabase.rpc('atualizar_meu_perfil_rh', { p_nome: nome, p_avatar_url: avatarPath ?? null })

usuarios_rh Row columns (database.types.ts): id, user_id, nome_completo, email, cargo, role, avatar_url (string|null), ativo, primeiro_acesso, data_ultimo_login, updated_at, updated_by, telefone, created_at, created_by, deleted_at

Caller identity: `useUser()` (src/store/authStore.ts) → Supabase User with `.id` and `.email`.
</interfaces>

<read_first>
- `src/features/vagas/services/cvUploadService.ts` — THE avatar analog: CVUploadServiceError code union, validateCV (size/mime), upload path schema, createSignedUrl, Pitfall-7 `{sizeKb, mime, hasFile}` redacted logs. Mirror as `validateAvatar`/`uploadAvatar`/`getAvatarSignedUrl` with codes FILE_TOO_LARGE/INVALID_MIME/UPLOAD_FAILED/UNAUTHORIZED. Avatar difference: `upsert:true`, stable path `{uid}/avatar.<ext>`, MIME set png/jpeg/webp, 2MB cap.
- `src/features/auth/services/passwordService.ts` — GoTrue idiom: `supabase.auth.signInWithPassword` + `supabase.auth.updateUser({password})` + Pitfall-7 (`{ hasPassword }` only, never the password). Wrong current password → AuthApiError → map to a typed WRONG_CURRENT code.
- `src/features/admin/services/usuariosRhService.ts` — the service-error-class + allowlist-read idiom (USUARIOS_RH_LIST_COLUMNS + `.select(allowlist)`); mirror the shape but for own-row.
- `src/features/admin/hooks/useUsuariosRh.ts` — the query-keys + query + mutation + invalidate-on-success idiom (staleTime, retry). For avatars use `staleTime ~55min` per RESEARCH §Q4 (sign-on-read, never persist signed URL).
- `src/features/admin/schemas/usuarioRhSchemas.ts` — the z.object + pt-BR messages idiom.
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: perfilRhSchemas + perfilRhService (own-row read, RPC, password re-auth, avatar) with tests</name>
  <files>src/features/perfil-rh/schemas/perfilRhSchemas.ts, src/features/perfil-rh/services/perfilRhService.ts, src/features/perfil-rh/services/__tests__/perfilRhService.test.ts</files>
  <read_first>src/features/vagas/services/cvUploadService.ts; src/features/auth/services/passwordService.ts; src/features/admin/services/usuariosRhService.ts; 30-UI-SPEC.md §Section 1/2</read_first>
  <behavior>
    - readMeuPerfil(uid): calls `.select('id,user_id,nome_completo,email,cargo,role,avatar_url').eq('user_id', uid)` (asserted: the projection string is the exact allowlist, NEVER '*') and returns the typed own row.
    - atualizarPerfil(nome, avatarPath): calls `supabase.rpc('atualizar_meu_perfil_rh', { p_nome: nome, p_avatar_url: avatarPath ?? null })` (asserted: rpc name + arg keys); a name-only save passes `p_avatar_url: null`.
    - alterarSenha(email, current, nova): calls signInWithPassword({email, password: current}); on AuthApiError → throws PerfilRhServiceError code WRONG_CURRENT (mapped to the "Senha atual" field); on success → calls updateUser({password: nova}); no logout asserted (no signOut call).
    - validateAvatar(file): throws FILE_TOO_LARGE (>2MB) / INVALID_MIME (not png|jpeg|webp); passes for a valid file.
    - uploadAvatar(file, uid): validates, then `supabase.storage.from('avatars-rh').upload('{uid}/avatar.<ext>', file, {upsert:true, contentType})`; returns the PATH (never a signed URL); Pitfall-7 log shape `{sizeKb, mime, hasFile}` only.
    - getAvatarSignedUrl(path): createSignedUrl(path, 3600); never logs the URL.
  </behavior>
  <action>Create `perfilRhSchemas.ts`: `perfilNomeSchema` (nome_completo min(3) max(255), pt-BR messages matching UI-SPEC) + `alterarSenhaSchema` (senha_atual required; nova_senha min(8, 'Mínimo 8 caracteres'); confirmar_senha; `.refine` nova===confirmar ('As senhas não conferem'), `.refine` nova!==atual ('A nova senha deve ser diferente da atual')) with typed exports. Create `perfilRhService.ts`: `PerfilRhServiceError` (code union WRONG_CURRENT|FILE_TOO_LARGE|INVALID_MIME|UPLOAD_FAILED|UNAUTHORIZED|WEAK_PASSWORD|NETWORK_ERROR|DATABASE_ERROR), `PerfilRhRow` = Pick of usuarios_rh Row (7 allowlist cols), `PERFIL_RH_COLUMNS` const, `validateAvatar`, `uploadAvatar`, `getAvatarSignedUrl`, `readMeuPerfil`, `atualizarPerfil`, `alterarSenha`, and a namespaced `perfilRhService` object. The `supabase.rpc(...)` call uses `as never` on the args with an inline comment `// TODO(30-06): drop `as never` once database.types.ts regen includes atualizar_meu_perfil_rh`. All console.* logs redacted (Pitfall-7). Author the full Vitest test file mocking `@/lib/supabase/client` (mirror the usuariosRhService.test.ts mock idiom) asserting every behavior above, INCLUDING an assertion that the select projection string contains no `*`.</action>
  <verify>
    <automated>npm run test:run -- src/features/perfil-rh/services && npm run lint 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - perfilRhService.test.ts is GREEN; asserts the allowlist projection (no '*'), the rpc name+args, the signInWithPassword→updateUser order, WRONG_CURRENT mapping, validateAvatar rejects >2MB + non-png/jpeg/webp, uploadAvatar path `{uid}/avatar.<ext>` upsert:true and returns the PATH.
    - `npm run lint` tsc ≤104 (the `as never` cast keeps the not-yet-typed RPC green).
    - No console.* logs a password/token/signed-URL (Pitfall-7 guard from 30-02 stays green).
  </acceptance_criteria>
  <done>The service contract for name/password/avatar is implemented and proven by unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: usePerfilRh hooks (own-row query + 3 mutations) with test</name>
  <files>src/features/perfil-rh/hooks/usePerfilRh.ts, src/features/perfil-rh/hooks/__tests__/usePerfilRh.test.tsx</files>
  <read_first>src/features/admin/hooks/useUsuariosRh.ts (keys + query + mutation + invalidate idiom); src/store/authStore.ts (useUser for uid/email)</read_first>
  <action>Create `perfilRhKeys` (`all: ['perfil-rh']`, `me: (uid) => [...all, 'me', uid]`). `usePerfilRh()`: reads `useUser()` for uid; `useQuery({ queryKey: perfilRhKeys.me(uid), queryFn: () => readMeuPerfil(uid), enabled: !!uid, staleTime: 5*60*1000, retry: 2 })`. Three `useMutation`s: `useAtualizarPerfil` (atualizarPerfil → toast.success('Perfil atualizado.') + invalidate me), `useAlterarSenha` (alterarSenha → toast.success('Senha alterada com sucesso.'); onError maps WRONG_CURRENT so the SenhaSection can set the field — return the error, do NOT toast WRONG_CURRENT here since it is a field error), `useUploadAvatar` (uploadAvatar → returns the path for the caller to pass to atualizarPerfil, invalidate me). All invalidate `perfilRhKeys.me(uid)` on success. **authStore refresh (WARNING/BLOCKER):** `useAtualizarPerfil` onSuccess ALSO refreshes the Zustand store so the RH panel chrome updates without a re-login — merge the edited fields onto the current `useAuthStore.getState().adminUser` (`{ ...adminUser, nome_completo: nome, avatar_url: avatarPath ?? adminUser.avatar_url, updated_at: now }`) and call `useAuthStore.getState().setAdminUser(mergedRow)` (setAdminUser already routes a `nome_completo`-bearing UsuarioRHRow into `adminUser`/`profile`/`role`), or refetch the own row and pass it. **Avatar contract (WARNING #3):** `useUploadAvatar` returns ONLY the storage path — it does NOT persist on its own; the caller must follow with `atualizarPerfil({ nome: <current loaded nome>, avatarPath })` so an avatar-only save never sends a null/blank nome (the RPC SET is unconditional). Document this in the hook's JSDoc. Author the hook test (renderHook + QueryClient wrapper, mocking the service AND the authStore) asserting the query calls readMeuPerfil, each mutation invalidates on success, AND that after a successful atualizarPerfil the store's setAdminUser is called with the updated nome/avatar_url.</action>
  <verify>
    <automated>npm run test:run -- src/features/perfil-rh/hooks && npm run lint 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - usePerfilRh.test.tsx GREEN; asserts the own-row query + invalidate-on-success for all three mutations.
    - useAtualizarPerfil onSuccess calls authStore.setAdminUser with the updated nome_completo + avatar_url (proven by a mocked-store assertion) so the panel chrome refreshes without re-login (BLOCKER).
    - The hook JSDoc documents that avatar persistence always flows through atualizarPerfil carrying the current nome (WARNING #3), never a standalone avatar-only write that could blank nome_completo.
    - `npm run lint` tsc ≤104.
  </acceptance_criteria>
  <done>The hooks layer exposes the own-row query + name/password/avatar mutations for the UI in 30-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → usuarios_rh (own-row read) | RLS is row-level; the projection must be an allowlist so no extra PII column leaks |
| client → GoTrue (password) | password/token never logged; re-auth before rotate |
| client → storage avatars-rh | own-folder RLS is authoritative; client validation is UX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Broken Authentication | perfilRhService.alterarSenha | mitigate | signInWithPassword(current) BEFORE updateUser(new); wrong current → WRONG_CURRENT typed error (field), no rotate. |
| T-30-05 | Information Disclosure | perfilRhService logs | mitigate | Pitfall-7: password/token/signed-URL never logged (`{hasPassword}` / `{sizeKb,mime,hasFile}` shapes only); 30-02 grep guard enforces. |
| T-30-06 | Information Disclosure | readMeuPerfil projection | mitigate | Explicit 7-col allowlist `.eq('user_id', uid)`, never select('*') — no extra-column PII leak. |
| T-30-SC | Tampering | package installs | accept | No new package installs this phase (all deps shipped). |
</threat_model>

<verification>
- `npm run test:run -- src/features/perfil-rh` GREEN (service + hooks).
- `npm run lint` tsc ≤104.
- Pitfall-7 grep guard (30-02) still GREEN over the new sources.
</verification>

<success_criteria>
The perfil-rh data layer is implemented, unit-proven, and tsc-clean (RPC cast documented for 30-06), ready for the UI in 30-05.
</success_criteria>

<output>
Create `.planning/phases/30-meu-perfil-rh/30-04-SUMMARY.md` when done.
</output>
