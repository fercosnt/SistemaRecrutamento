---
phase: 30-meu-perfil-rh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/30-meu-perfil-rh/30-LIVE-STATE.md
autonomous: false
requirements: [SEG-03]
must_haves:
  truths:
    - "The live PROD state that SEG-03 depends on is captured before any migration is authored: usuarios_rh has NO client UPDATE policy, the own-row SELECT policy is present, avatar_url exists, log_auditoria signature is known, and no atualizar_meu_perfil_rh exists yet"
  artifacts:
    - path: ".planning/phases/30-meu-perfil-rh/30-LIVE-STATE.md"
      provides: "A1-A6 live-state findings the migration author (30-03) and the SEG-03 smoke (30-02/30-07) build on"
      contains: "A1"
  key_links:
    - from: "30-03 migration authoring"
      to: "30-LIVE-STATE.md"
      via: "author reads the captured live policy/signature state before CREATE OR REPLACE"
      pattern: "30-LIVE-STATE"
---

<objective>
Capture the LIVE PROD state that Phase 30's write-path design depends on, BEFORE authoring the migration. Phase 28 dropped the client UPDATE policy on `usuarios_rh` (closing a self-promotion hole) and preserved the own-row SELECT policy; the profile RPC must be authored against the real current state, not assumptions. This is a [BLOCKING] capture: the operator runs the read-only queries via Supabase MCP `execute_sql` and the findings are written to `30-LIVE-STATE.md`.

Purpose: De-risk 30-03 (migration authoring) and 30-02/30-07 (SEG-03 smoke) — verify the live body of nothing needs `CREATE OR REPLACE` except the NEW RPC (M4/DBMIG-02 lesson: verify live before create).
Output: `.planning/phases/30-meu-perfil-rh/30-LIVE-STATE.md` with findings A1-A6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-meu-perfil-rh/30-RESEARCH.md
@.planning/phases/30-meu-perfil-rh/30-CONTEXT.md

<read_first>
- `.planning/milestones/v5.0-phases/28-*/28-LIVE-STATE.md` if archived, else the Phase-28 SUMMARYs — the format + the A1 finding (usuarios_rh has NO client UPDATE policy; own-row SELECT preserved; auth.uid() under DEFINER is GUC-based).
- `supabase/migrations/20260713000001_usr_rh_rls_seg02.sql` — the Phase-28 RLS rewrite that dropped the client write policies (the state this capture confirms).
- `docs/sql/sql/25-functions-configuracoes.sql:63-77` — the `log_auditoria` signature the RPC will PERFORM.
</read_first>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Capture live PROD state via Supabase MCP → 30-LIVE-STATE.md</name>
  <files>.planning/phases/30-meu-perfil-rh/30-LIVE-STATE.md</files>
  <read_first>.planning/phases/30-meu-perfil-rh/30-RESEARCH.md §Q3; docs/sql/sql/25-functions-configuracoes.sql:63-77; the Phase-28 28-LIVE-STATE.md format</read_first>
  <action>Run each read-only query against PROD via `mcp__supabase__execute_sql` (no writes; autonomous:false — the operator authorizes the MCP calls) and record the raw result of each, then write `.planning/phases/30-meu-perfil-rh/30-LIVE-STATE.md` under headings A1-A6 (mirror the Phase-28 LIVE-STATE format), each with a one-line "Author note" for 30-03:
    - **A1 — no client UPDATE policy on usuarios_rh:** `SELECT policyname, cmd, qual, with_check FROM pg_policies WHERE schemaname='public' AND tablename='usuarios_rh' ORDER BY cmd, policyname;` — confirm NO `cmd='UPDATE'` policy for a client role. If one is found, flag it as a BLOCKER (the RPC must remain the only write path).
    - **A2 — own-row SELECT policy present:** from the same result, confirm the own-row SELECT policy (`RH pode ler seu próprio perfil` or live equivalent) with a `user_id = auth.uid()` predicate.
    - **A3 — columns:** `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_schema='public' AND table_name='usuarios_rh' AND column_name IN ('avatar_url','nome_completo','updated_by','updated_at','user_id','id') ORDER BY column_name;` — all present, avatar_url nullable.
    - **A4 — log_auditoria signature:** `SELECT pg_get_function_identity_arguments(oid) FROM pg_proc WHERE proname='log_auditoria' AND pronamespace='public'::regnamespace;`.
    - **A5 — no existing RPC:** `SELECT proname FROM pg_proc WHERE proname='atualizar_meu_perfil_rh' AND pronamespace='public'::regnamespace;` — MUST return 0 rows (fresh CREATE).
    - **A6 — avatars-rh bucket:** `SELECT id, public, file_size_limit, allowed_mime_types FROM storage.buckets WHERE id='avatars-rh';` — record whether it exists.
    Do NOT mutate anything.</action>
  <verify>
    <automated>test -f .planning/phases/30-meu-perfil-rh/30-LIVE-STATE.md && grep -q "A1" .planning/phases/30-meu-perfil-rh/30-LIVE-STATE.md && grep -q "A5" .planning/phases/30-meu-perfil-rh/30-LIVE-STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `30-LIVE-STATE.md` exists with sections A1-A6, each carrying the actual query result (not a placeholder).
    - A1 explicitly records that NO client-role UPDATE policy exists on `usuarios_rh` (or flags a surviving one as a BLOCKER for 30-03).
    - A5 records 0 rows for `atualizar_meu_perfil_rh` (fresh CREATE).
  </acceptance_criteria>
  <done>Live PROD state is captured in 30-LIVE-STATE.md; the migration author (30-03) and the SEG-03 smoke (30-02) have a verified state to build against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → PROD (MCP execute_sql) | read-only introspection queries; no writes cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Elevation of Privilege | self-service write path (precondition) | mitigate | Confirm A1 (no surviving client UPDATE policy) so the DEFINER RPC remains the ONLY self-service write path — the foundation SEG-03 rests on. |
| T-30-SC | Tampering | package installs | accept | No new npm/pip/cargo installs this phase (composed from shipped deps); no legitimacy gate needed. |
</threat_model>

<verification>
- `30-LIVE-STATE.md` present with A1-A6.
- A1 = no client UPDATE policy on usuarios_rh (else BLOCKER surfaced).
- A5 = no existing atualizar_meu_perfil_rh.
</verification>

<success_criteria>
Live PROD state captured and written to `30-LIVE-STATE.md`; the migration author (30-03) and the SEG-03 smoke authors (30-02) have a verified state to build against.
</success_criteria>

<output>
Create `.planning/phases/30-meu-perfil-rh/30-01-SUMMARY.md` when done.
</output>
