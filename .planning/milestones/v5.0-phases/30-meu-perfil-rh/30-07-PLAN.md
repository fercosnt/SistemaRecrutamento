---
phase: 30-meu-perfil-rh
plan: 07
type: execute
wave: 4
depends_on: ["30-04", "30-05", "30-06"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/30-meu-perfil-rh/30-HUMAN-UAT.md
autonomous: false
requirements: [PERFIL-01, PERFIL-02, PERFIL-03, SEG-03]
must_haves:
  truths:
    - "The SEG-03 behavioral SQL smoke runs GREEN on PROD (own-row-only + role/ativo unchanged + COALESCE avatar + WR-01 self-promotion-denied regression + no client UPDATE policy) — SEG-03 proven, not asserted"
    - "The full phase gate is green: Vitest full suite + tsc ≤104 + build 0 + Pitfall-7 guard; PERFIL-01/02/03 + SEG-03 marked Complete in REQUIREMENTS.md, with the live GoTrue/avatar round-trips deferred to 30-HUMAN-UAT.md"
  artifacts:
    - path: ".planning/phases/30-meu-perfil-rh/30-HUMAN-UAT.md"
      provides: "the live round-trips that cannot be unit-verified (real password change, real avatar render, visual/AA sweep)"
      contains: "PERFIL-02"
  key_links:
    - from: "supabase/tests/perfil_rh_seg03_smoke.sql"
      to: "PROD (MCP execute_sql)"
      via: "operator runs the smoke; every case prints PASS"
      pattern: "PASS"
---

<objective>
Run the SEG-03 behavioral SQL smoke GREEN on PROD (via MCP `execute_sql`) now that the RPC is live (30-06), then close the phase gate: full Vitest + tsc ≤104 + build 0 + Pitfall-7 guard, mark PERFIL-01/02/03 + SEG-03 Complete in REQUIREMENTS.md, and record the live GoTrue/avatar round-trips that need a human in `30-HUMAN-UAT.md`. This is [BLOCKING] — the operator drives the PROD smoke.

Purpose: SEG-03 proven behaviorally (the axis gate); the phase is code-complete and requirement-traceable.
Output: SEG-03 smoke PASS on PROD, full gate green, requirements marked Complete, HUMAN-UAT authored.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-meu-perfil-rh/30-VALIDATION.md

<read_first>
- `supabase/tests/perfil_rh_seg03_smoke.sql` — the SEG-03 smoke to run GREEN (authored in 30-02).
- `.planning/phases/30-meu-perfil-rh/30-VALIDATION.md` §Manual-Only Verifications — the three live round-trips for HUMAN-UAT.
- `.planning/REQUIREMENTS.md` — the PERFIL-01/02/03 + SEG-03 rows to mark Complete (checklist + traceability).
- A prior phase's `*-HUMAN-UAT.md` (e.g. `08-HUMAN-UAT.md` / `10-HUMAN-UAT.md` if present) — the format to mirror.
</read_first>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Run the SEG-03 SQL smoke GREEN on PROD</name>
  <files>supabase/tests/perfil_rh_seg03_smoke.sql</files>
  <read_first>supabase/tests/perfil_rh_seg03_smoke.sql</read_first>
  <action>The operator pastes `supabase/tests/perfil_rh_seg03_smoke.sql` into `mcp__supabase__execute_sql` (it builds its own disposable fixtures + cleans up ROLLBACK-free). Confirm each case prints a PASS NOTICE (no EXCEPTION, no SKIP):
    - (1) own-row-only: the caller's nome_completo changed, the second user's UNCHANGED.
    - (2) role/ativo unchanged after any RPC args.
    - (3) COALESCE avatar: a NULL avatar arg preserves the existing photo; a non-null path updates it.
    - (4) WR-01 regression: a direct recrutador `UPDATE ... SET role='administrador'` affects 0 rows; role stays 'recrutador'.
    - (5) 0 client UPDATE policies on usuarios_rh.
    If it SKIPs (no free auth user / no admin), adjust the fixture to the live schema and rerun until GREEN. Confirm no disposable `30020003-...` rows remain and the real admin was never deleted.</action>
  <verify>
    <automated>grep -q "atualizar_meu_perfil_rh" supabase/tests/perfil_rh_seg03_smoke.sql</automated>
  </verify>
  <done>Every SEG-03 case prints PASS on PROD; SEG-03 is proven behaviorally against the live RPC + policies.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Full phase gate + mark requirements Complete + author HUMAN-UAT</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/30-meu-perfil-rh/30-HUMAN-UAT.md</files>
  <read_first>.planning/REQUIREMENTS.md; .planning/phases/30-meu-perfil-rh/30-VALIDATION.md §Manual-Only Verifications; a prior *-HUMAN-UAT.md</read_first>
  <action>Run the full gate and record it: `npm run test:run` (full Vitest — GREEN), `npm run lint` (tsc ≤104), `npm run build` (0 errors), and confirm the Pitfall-7 grep guard passes over perfil-rh. Mark PERFIL-01, PERFIL-02, PERFIL-03, SEG-03 Complete in `.planning/REQUIREMENTS.md` (checklist box + traceability note citing this phase's plans + the SEG-03 smoke). Author `.planning/phases/30-meu-perfil-rh/30-HUMAN-UAT.md` (mirror a prior HUMAN-UAT) with the three deferred live round-trips from 30-VALIDATION: (a) PERFIL-02 real password change → re-login works with the new password, session not dropped; (b) PERFIL-03 real avatar upload → the signed-URL image renders; (c) UI-SPEC visual/glass/AA sweep on the live /rh/perfil (axe Tier-A no-regress). Note the test account `e2e.admin@beautysmile.com.br` (and the first `recrutador` created by Phase 29) for exercising the recrutador path.</action>
  <verify>
    <automated>npm run test:run 2>&1 | tail -3 && npm run lint 2>&1 | tail -1 && npm run build 2>&1 | tail -1</automated>
  </verify>
  <done>Full Vitest + tsc ≤104 + build 0 + Pitfall-7 GREEN; PERFIL-01/02/03 + SEG-03 Complete in REQUIREMENTS.md; 30-HUMAN-UAT.md lists the three live round-trips with test accounts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| impersonated recrutador JWT → PROD | the smoke exercises the real live RPC + policies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Elevation of Privilege | atualizar_meu_perfil_rh (live) | mitigate | SEG-03 smoke case (2)+(4) GREEN on PROD: no RPC args escalate role; direct self-promotion UPDATE affects 0 rows. |
| T-30-02 | IDOR / Tampering | atualizar_meu_perfil_rh (live) | mitigate | SEG-03 smoke case (1) GREEN: the write touches only the caller's row; the second user is untouched. |
| T-30-05 | Information Disclosure | perfil-rh surfaces | mitigate | Pitfall-7 guard GREEN in the full gate. |
| T-30-SC | Tampering | package installs | accept | No new package installs this phase. |
</threat_model>

<verification>
- SEG-03 smoke: all 5 cases PASS on PROD.
- Full Vitest GREEN, tsc ≤104, build 0, Pitfall-7 GREEN.
- PERFIL-01/02/03 + SEG-03 Complete in REQUIREMENTS.md; 30-HUMAN-UAT.md authored.
</verification>

<success_criteria>
SEG-03 is proven behaviorally on PROD, the phase is code-complete and requirement-traceable, and the live round-trips are queued for human verification.
</success_criteria>

<output>
Create `.planning/phases/30-meu-perfil-rh/30-07-SUMMARY.md` when done.
</output>
