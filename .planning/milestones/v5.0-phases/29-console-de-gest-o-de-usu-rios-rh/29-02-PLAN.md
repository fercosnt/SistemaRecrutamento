---
phase: 29-console-de-gest-o-de-usu-rios-rh
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/features/admin/components/NovoUsuarioDialog.tsx
  - src/features/admin/components/__tests__/NovoUsuarioDialog.test.tsx
autonomous: true
requirements: [USR-02]

must_haves:
  truths:
    - "The administrador opens a 'Novo usuário' dialog, fills email/nome/cargo/papel, and submits — dispatching action:'criar' through the Phase-28 EF (USR-02)"
    - "On success the dialog closes, the roster query is invalidated, and an honest toast says an e-mail was sent for the user to set their own password"
    - "EMAIL_SEND_FAILED is treated as success-with-warning (user created; suggest 'Resetar senha' to resend) — not an error"
    - "EMAIL_EXISTS keeps the dialog open and shows a field error on email; VALIDATION is blocked client-side before invoke"
  artifacts:
    - path: "src/features/admin/components/NovoUsuarioDialog.tsx"
      provides: "Create-user Dialog (RHF register/Controller + zodResolver(novoUsuarioSchema)); honest helper; pending/disabled submit; error_code → field/toast mapping"
      min_lines: 90
      exports: ["NovoUsuarioDialog"]
    - path: "src/features/admin/components/__tests__/NovoUsuarioDialog.test.tsx"
      provides: "RTL: submit dispatches criar; success→toast+close+invalidate; EMAIL_SEND_FAILED→warning; EMAIL_EXISTS→email field error; client VALIDATION blocks invoke"
  key_links:
    - from: "src/features/admin/components/NovoUsuarioDialog.tsx"
      to: "useCriarUsuario mutation (useUsuariosRh)"
      via: "mutateAsync in onSubmit with try/catch for EMAIL_EXISTS field routing"
      pattern: "useCriarUsuario|criar"
    - from: "src/features/admin/components/NovoUsuarioDialog.tsx"
      to: "novoUsuarioSchema"
      via: "zodResolver in useForm"
      pattern: "zodResolver\\(novoUsuarioSchema"
---

<objective>
Build the "Novo usuário" create dialog: an RHF + Zod form (email, nome_completo, cargo, papel) that dispatches `action:'criar'` through the Phase-28 EF, with honest pt-BR copy, pending state, and the full `error_code` → field/toast mapping from the UI-SPEC.

Purpose: deliver USR-02 — the administrador creates the first-ever `recrutador`, who then receives an e-mail to set their own password. This closes the recruiter-path loop.
Output: `NovoUsuarioDialog.tsx` + its RTL test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-console-de-gest-o-de-usu-rios-rh/29-CONTEXT.md
@.planning/phases/29-console-de-gest-o-de-usu-rios-rh/29-UI-SPEC.md
@.planning/phases/29-console-de-gest-o-de-usu-rios-rh/29-PATTERNS.md

**Depends on 29-01:** consumes `novoUsuarioSchema` / `NovoUsuarioForm` / `PAPEL_OPTIONS` (schemas), `useCriarUsuario` (hook). Path: flat `src/features/admin/components/`. Forms use BARE RHF `register`/`Controller` + zodResolver (LoginRHPage idiom) — NOT the unused shadcn `<FormField>` primitive.

<interfaces>
From 29-01 (already built):
  novoUsuarioSchema, type NovoUsuarioForm = { email, nome_completo, cargo, papel: 'recrutador'|'administrador' }
  PAPEL_OPTIONS: { value:'recrutador'|'administrador'; label:string }[]
  useCriarUsuario(): mutation; mutateAsync(input) resolves { ok:true, warning?:'EMAIL_SEND_FAILED', data?:{userId,usuarioRhId} }; rejects UsuariosRhServiceError with .details.error_code ('EMAIL_EXISTS' carries field='email', plus VALIDATION/FORBIDDEN/SERVER_ERROR). onSuccess in the hook already toasts success/warning + invalidates; the dialog owns the ERROR branch.

UI-SPEC copy (verbatim — §Copywriting Contract):
  title "Novo usuário"; helper "O usuário receberá um e-mail para definir a própria senha e acessar o painel."
  submit "Criar usuário" / pending "Criando…"; Cancel "Cancelar"
  success (hook) "Usuário criado. Enviamos um e-mail para {email} definir a senha."
  EMAIL_SEND_FAILED (hook, warning) "Usuário criado, mas o e-mail de senha não pôde ser enviado. Use 'Resetar senha' na linha do usuário para reenviar."
  EMAIL_EXISTS (field on email) "Já existe um usuário com este e-mail."
  VALIDATION toast "Verifique os campos e tente novamente."; FORBIDDEN "Você não tem permissão para esta ação."
  SERVER_ERROR/unknown "Não foi possível concluir agora. Tente novamente em instantes."; NOT_FOUND "Usuário não encontrado."

Field layout (UI-SPEC §Create User Dialog): single column space-y-4; E-mail (Input type=email, placeholder "nome@beautysmile.com.br"), Nome completo (Input), Cargo (Input, placeholder "ex.: Analista de RH"), Papel (Select, default 'recrutador', items "Recrutador"/"Administrador"). Glass DialogContent, min-h-[44px] on submit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NovoUsuarioDialog — RHF + Zod create form (test-first)</name>
  <files>src/features/admin/components/NovoUsuarioDialog.tsx, src/features/admin/components/__tests__/NovoUsuarioDialog.test.tsx</files>
  <read_first>
    - src/features/config-vaga/components/BulkMarkDialog.tsx:56-97 (Dialog shell: controlled open/onOpenChange, glass DialogContent, DialogHeader/Title/Description, DialogFooter Cancelar + primary)
    - src/components/pages/LoginRHPage.tsx:78-92 (useForm({ resolver: zodResolver, mode:'onBlur', defaultValues })), :272-294 (Input + register + aria-invalid + inline errors.x.message), :363-376 (Controller for a non-native control — mirror for the papel Select)
    - src/features/decisao/components/RegistrarDecisaoForm.tsx:173-179 (pending Loader2 spinner + verb-in-progress label + disabled)
    - src/features/admin/schemas/usuarioRhSchemas.ts + src/features/admin/hooks/useUsuariosRh.ts (29-01 — the schema + mutation this consumes)
  </read_first>
  <behavior>
    - Renders a controlled Dialog with title "Novo usuário", 4 fields (email/nome_completo/cargo/papel), the honest helper text, and a DialogFooter (Cancelar + "Criar usuário").
    - papel Select defaults to 'recrutador' and offers exactly "Recrutador"/"Administrador".
    - Submitting a valid form calls the criar mutation with { email, nome_completo, cargo, papel } (action added by the service).
    - On resolve → dialog closes (onOpenChange(false)) and the form resets; the hook toasts success (or the EMAIL_SEND_FAILED warning) and invalidates — the dialog does NOT double-toast.
    - On reject with error_code==='EMAIL_EXISTS' → setError('email',{ message:'Já existe um usuário com este e-mail.' }) and the dialog STAYS open.
    - On reject VALIDATION → toast.error "Verifique os campos e tente novamente." (stays open); FORBIDDEN → "Você não tem permissão para esta ação."; SERVER_ERROR/unknown → "Não foi possível concluir agora. Tente novamente em instantes."; NOT_FOUND → "Usuário não encontrado.".
    - Client-side invalid input (empty nome, bad email) shows FormMessage and does NOT invoke the mutation (blocked before invoke).
    - While pending: submit shows "Criando…" + Loader2 + disabled (no double submit).
  </behavior>
  <action>
    Write the RTL test FIRST (RED) mocking `useUsuariosRh`'s `useCriarUsuario` (mutateAsync resolving / rejecting with the enumerated shapes) and `sonner`'s toast — enumerate every case in the behavior block (assert the mutateAsync body, close-on-success, EMAIL_EXISTS field error stays open, VALIDATION does NOT invoke, pending disables). Then implement `NovoUsuarioDialog.tsx` (GREEN): props `{ open, onOpenChange }`; `useForm<NovoUsuarioForm>({ resolver: zodResolver(novoUsuarioSchema), mode:'onBlur', defaultValues:{ email:'', nome_completo:'', cargo:'', papel:'recrutador' } })`; render the 4 fields with bare `register` (email/nome/cargo) + a `Controller`-wrapped `Select` for papel (map PAPEL_OPTIONS); inline `errors.x.message` with `role="alert"` + aria-invalid/aria-describedby (LoginRHPage idiom); the honest helper `text-xs text-white/60`; DialogFooter with Cancelar (closes, no side effect) + a submit button (min-h-[44px], pending spinner + "Criando…"). onSubmit calls `mutateAsync(values)` in try/catch: success → `onOpenChange(false)` + `reset()`; catch → read `(err as UsuariosRhServiceError).details?.error_code` and branch per the behavior (EMAIL_EXISTS → setError field + keep open; else toast + keep open). Use glass DialogContent per BulkMarkDialog. No fenced code in this action.
  </action>
  <acceptance_criteria>
    - `npm run test:run -- src/features/admin/components/__tests__/NovoUsuarioDialog.test.tsx` passes with the enumerated assertions.
    - A grep confirms `zodResolver(novoUsuarioSchema` and NO usage of shadcn `<FormField>`.
    - Copy strings match the UI-SPEC verbatim (title, helper, submit, pending, EMAIL_EXISTS field message).
    - `npm run lint` tsc ≤104; no `<FormField>` import from ui/form.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:run -- src/features/admin/components/__tests__/NovoUsuarioDialog.test.tsx</automated>
  </verify>
  <done>NovoUsuarioDialog test GREEN; create form dispatches criar; success closes+invalidates (via hook); EMAIL_SEND_FAILED is warning; EMAIL_EXISTS routes to the email field; client validation blocks invoke; pending disables submit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin form → gerenciar-usuario-rh EF | Untrusted form input crosses to the privileged create path; the EF authorizes + re-validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Spoofing/Repudiation (dishonest copy) | create helper + success toast | mitigate | Copy promises ONLY what the EF does (`resetPasswordForEmail → /auth/redefinir-senha?tipo=rh`): "receberá um e-mail para definir a própria senha" — no invented channels; EMAIL_SEND_FAILED honestly says the e-mail failed and to resend via 'Resetar senha' |
| T-29-05 | Tampering | papel field | mitigate | papel is a fixed Select over {Recrutador, Administrador} bound to novoUsuarioSchema's enum; free-text/extra keys are rejected client-side AND by the EF `.strict()` union — client is UX, EF authoritative |
| T-29-06 | Elevation of Privilege | create action | accept-by-design | Creating an `administrador` is an intended admin capability; authorization that the CALLER is an administrador is enforced server-side by the EF (not by this dialog) — the dialog never gates privilege on client role |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (RHF, zod, sonner, shadcn primitives already vendored) |
</threat_model>

<verification>
- `npm run test:run -- src/features/admin/components/__tests__/NovoUsuarioDialog.test.tsx` GREEN.
- `grep -n "FormField" src/features/admin/components/NovoUsuarioDialog.tsx` → 0 (bare RHF, not shadcn Form primitive).
- Copy audit: helper + toasts + field error match UI-SPEC §Copywriting Contract verbatim.
- `npx tsc --noEmit` ≤104; full `npm run test:run` green.
</verification>

<success_criteria>
- The admin creates an RH user (email + papel) end-to-end through the secure EF (USR-02).
- Honest copy: the user receives an e-mail to set their own password; EMAIL_SEND_FAILED surfaces as a resendable warning.
- EMAIL_EXISTS routes to the email field; client validation blocks bad submits.
</success_criteria>

<output>
Create `.planning/phases/29-console-de-gest-o-de-usu-rios-rh/29-02-SUMMARY.md` when done.
</output>
